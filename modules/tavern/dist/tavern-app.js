/* eslint-disable */
var Z8 = Object.create, ZC = Object.defineProperty, j8 = Object.getOwnPropertyDescriptor, eN = Object.getOwnPropertyNames, tN = Object.getPrototypeOf, nN = Object.prototype.hasOwnProperty, Zd = (e, t) => () => (t || e((t = { exports: {} }).exports, t), t.exports), rN = (e, t, n, r) => {
  if (t && typeof t == "object" || typeof t == "function")
    for (var o = eN(t), s = 0, a = o.length, i; s < a; s++)
      i = o[s], !nN.call(e, i) && i !== n && ZC(e, i, {
        get: ((l) => t[l]).bind(null, i),
        enumerable: !(r = j8(t, i)) || r.enumerable
      });
  return e;
}, jC = (e, t, n) => (n = e != null ? Z8(tN(e)) : {}, rN(t || !e || !e.__esModule ? ZC(n, "default", {
  value: e,
  enumerable: !0
}) : n, e));
// @__NO_SIDE_EFFECTS__
function Um(e) {
  const t = /* @__PURE__ */ Object.create(null);
  for (const n of e.split(",")) t[n] = 1;
  return (n) => n in t;
}
var an = {}, Xl = [], as = () => {
}, ek = () => !1, qm = (e) => e.charCodeAt(0) === 111 && e.charCodeAt(1) === 110 && (e.charCodeAt(2) > 122 || e.charCodeAt(2) < 97), Hm = (e) => e.startsWith("onUpdate:"), On = Object.assign, c0 = (e, t) => {
  const n = e.indexOf(t);
  n > -1 && e.splice(n, 1);
}, oN = Object.prototype.hasOwnProperty, tn = (e, t) => oN.call(e, t), Tt = Array.isArray, Ql = (e) => jd(e) === "[object Map]", Au = (e) => jd(e) === "[object Set]", MS = (e) => jd(e) === "[object Date]", $t = (e) => typeof e == "function", _n = (e) => typeof e == "string", cs = (e) => typeof e == "symbol", on = (e) => e !== null && typeof e == "object", tk = (e) => (on(e) || $t(e)) && $t(e.then) && $t(e.catch), nk = Object.prototype.toString, jd = (e) => nk.call(e), sN = (e) => jd(e).slice(8, -1), rk = (e) => jd(e) === "[object Object]", d0 = (e) => _n(e) && e !== "NaN" && e[0] !== "-" && "" + parseInt(e, 10) === e, nd = /* @__PURE__ */ Um(",key,ref,ref_for,ref_key,onVnodeBeforeMount,onVnodeMounted,onVnodeBeforeUpdate,onVnodeUpdated,onVnodeBeforeUnmount,onVnodeUnmounted"), zm = (e) => {
  const t = /* @__PURE__ */ Object.create(null);
  return ((n) => t[n] || (t[n] = e(n)));
}, aN = /-\w/g, Mo = zm((e) => e.replace(aN, (t) => t.slice(1).toUpperCase())), iN = /\B([A-Z])/g, Wa = zm((e) => e.replace(iN, "-$1").toLowerCase()), ok = zm((e) => e.charAt(0).toUpperCase() + e.slice(1)), Lv = zm((e) => e ? `on${ok(e)}` : ""), os = (e, t) => !Object.is(e, t), Yh = (e, ...t) => {
  for (let n = 0; n < e.length; n++) e[n](...t);
}, sk = (e, t, n, r = !1) => {
  Object.defineProperty(e, t, {
    configurable: !0,
    enumerable: !1,
    writable: r,
    value: n
  });
}, Wm = (e) => {
  const t = parseFloat(e);
  return isNaN(t) ? e : t;
}, PS, Gm = () => PS || (PS = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : typeof globalThis < "u" ? globalThis : {});
function Jr(e) {
  if (Tt(e)) {
    const t = {};
    for (let n = 0; n < e.length; n++) {
      const r = e[n], o = _n(r) ? dN(r) : Jr(r);
      if (o) for (const s in o) t[s] = o[s];
    }
    return t;
  } else if (_n(e) || on(e)) return e;
}
var lN = /;(?![^(]*\))/g, uN = /:([^]+)/, cN = /\/\*[^]*?\*\//g;
function dN(e) {
  const t = {};
  return e.replace(cN, "").split(lN).forEach((n) => {
    if (n) {
      const r = n.split(uN);
      r.length > 1 && (t[r[0].trim()] = r[1].trim());
    }
  }), t;
}
function ze(e) {
  let t = "";
  if (_n(e)) t = e;
  else if (Tt(e)) for (let n = 0; n < e.length; n++) {
    const r = ze(e[n]);
    r && (t += r + " ");
  }
  else if (on(e))
    for (const n in e) e[n] && (t += n + " ");
  return t.trim();
}
var ak = "itemscope,allowfullscreen,formnovalidate,ismap,nomodule,novalidate,readonly", fN = /* @__PURE__ */ Um(ak), yhe = /* @__PURE__ */ Um(ak + ",async,autofocus,autoplay,controls,default,defer,disabled,hidden,inert,loop,open,required,reversed,scoped,seamless,checked,muted,multiple,selected");
function ik(e) {
  return !!e || e === "";
}
function hN(e, t) {
  if (e.length !== t.length) return !1;
  let n = !0;
  for (let r = 0; n && r < e.length; r++) n = Cu(e[r], t[r]);
  return n;
}
function Cu(e, t) {
  if (e === t) return !0;
  let n = MS(e), r = MS(t);
  if (n || r) return n && r ? e.getTime() === t.getTime() : !1;
  if (n = cs(e), r = cs(t), n || r) return e === t;
  if (n = Tt(e), r = Tt(t), n || r) return n && r ? hN(e, t) : !1;
  if (n = on(e), r = on(t), n || r) {
    if (!n || !r || Object.keys(e).length !== Object.keys(t).length) return !1;
    for (const o in e) {
      const s = e.hasOwnProperty(o), a = t.hasOwnProperty(o);
      if (s && !a || !s && a || !Cu(e[o], t[o])) return !1;
    }
  }
  return String(e) === String(t);
}
function f0(e, t) {
  return e.findIndex((n) => Cu(n, t));
}
var lk = (e) => !!(e && e.__v_isRef === !0), K = (e) => _n(e) ? e : e == null ? "" : Tt(e) || on(e) && (e.toString === nk || !$t(e.toString)) ? lk(e) ? K(e.value) : JSON.stringify(e, uk, 2) : String(e), uk = (e, t) => lk(t) ? uk(e, t.value) : Ql(t) ? { [`Map(${t.size})`]: [...t.entries()].reduce((n, [r, o], s) => (n[Ov(r, s) + " =>"] = o, n), {}) } : Au(t) ? { [`Set(${t.size})`]: [...t.values()].map((n) => Ov(n)) } : cs(t) ? Ov(t) : on(t) && !Tt(t) && !rk(t) ? String(t) : t, Ov = (e, t = "") => {
  var n;
  return cs(e) ? `Symbol(${(n = e.description) != null ? n : t})` : e;
}, Kn, pN = class {
  constructor(e = !1) {
    this.detached = e, this._active = !0, this._on = 0, this.effects = [], this.cleanups = [], this._isPaused = !1, this._warnOnRun = !0, this.__v_skip = !0, !e && Kn && (Kn.active ? (this.parent = Kn, this.index = (Kn.scopes || (Kn.scopes = [])).push(this) - 1) : (this._active = !1, this._warnOnRun = !1));
  }
  get active() {
    return this._active;
  }
  pause() {
    if (this._active) {
      this._isPaused = !0;
      let e, t;
      if (this.scopes) for (e = 0, t = this.scopes.length; e < t; e++) this.scopes[e].pause();
      for (e = 0, t = this.effects.length; e < t; e++) this.effects[e].pause();
    }
  }
  resume() {
    if (this._active && this._isPaused) {
      this._isPaused = !1;
      let e, t;
      if (this.scopes) for (e = 0, t = this.scopes.length; e < t; e++) this.scopes[e].resume();
      for (e = 0, t = this.effects.length; e < t; e++) this.effects[e].resume();
    }
  }
  run(e) {
    if (this._active) {
      const t = Kn;
      try {
        return Kn = this, e();
      } finally {
        Kn = t;
      }
    }
  }
  on() {
    ++this._on === 1 && (this.prevScope = Kn, Kn = this);
  }
  off() {
    if (this._on > 0 && --this._on === 0) {
      if (Kn === this) Kn = this.prevScope;
      else {
        let e = Kn;
        for (; e; ) {
          if (e.prevScope === this) {
            e.prevScope = this.prevScope;
            break;
          }
          e = e.prevScope;
        }
      }
      this.prevScope = void 0;
    }
  }
  stop(e) {
    if (this._active) {
      this._active = !1;
      let t, n;
      for (t = 0, n = this.effects.length; t < n; t++) this.effects[t].stop();
      for (this.effects.length = 0, t = 0, n = this.cleanups.length; t < n; t++) this.cleanups[t]();
      if (this.cleanups.length = 0, this.scopes) {
        for (t = 0, n = this.scopes.length; t < n; t++) this.scopes[t].stop(!0);
        this.scopes.length = 0;
      }
      if (!this.detached && this.parent && !e) {
        const r = this.parent.scopes.pop();
        r && r !== this && (this.parent.scopes[this.index] = r, r.index = this.index);
      }
      this.parent = void 0;
    }
  }
};
function mN() {
  return Kn;
}
var ln, Fv = /* @__PURE__ */ new WeakSet(), ck = class {
  constructor(e) {
    this.fn = e, this.deps = void 0, this.depsTail = void 0, this.flags = 5, this.next = void 0, this.cleanup = void 0, this.scheduler = void 0, Kn && (Kn.active ? Kn.effects.push(this) : this.flags &= -2);
  }
  pause() {
    this.flags |= 64;
  }
  resume() {
    this.flags & 64 && (this.flags &= -65, Fv.has(this) && (Fv.delete(this), this.trigger()));
  }
  notify() {
    this.flags & 2 && !(this.flags & 32) || this.flags & 8 || fk(this);
  }
  run() {
    if (!(this.flags & 1)) return this.fn();
    this.flags |= 2, RS(this), hk(this);
    const e = ln, t = Po;
    ln = this, Po = !0;
    try {
      return this.fn();
    } finally {
      pk(this), ln = e, Po = t, this.flags &= -3;
    }
  }
  stop() {
    if (this.flags & 1) {
      for (let e = this.deps; e; e = e.nextDep) m0(e);
      this.deps = this.depsTail = void 0, RS(this), this.onStop && this.onStop(), this.flags &= -2;
    }
  }
  trigger() {
    this.flags & 64 ? Fv.add(this) : this.scheduler ? this.scheduler() : this.runIfDirty();
  }
  runIfDirty() {
    Qy(this) && this.run();
  }
  get dirty() {
    return Qy(this);
  }
}, dk = 0, rd, od;
function fk(e, t = !1) {
  if (e.flags |= 8, t) {
    e.next = od, od = e;
    return;
  }
  e.next = rd, rd = e;
}
function h0() {
  dk++;
}
function p0() {
  if (--dk > 0) return;
  if (od) {
    let t = od;
    for (od = void 0; t; ) {
      const n = t.next;
      t.next = void 0, t.flags &= -9, t = n;
    }
  }
  let e;
  for (; rd; ) {
    let t = rd;
    for (rd = void 0; t; ) {
      const n = t.next;
      if (t.next = void 0, t.flags &= -9, t.flags & 1) try {
        t.trigger();
      } catch (r) {
        e || (e = r);
      }
      t = n;
    }
  }
  if (e) throw e;
}
function hk(e) {
  for (let t = e.deps; t; t = t.nextDep)
    t.version = -1, t.prevActiveLink = t.dep.activeLink, t.dep.activeLink = t;
}
function pk(e) {
  let t, n = e.depsTail, r = n;
  for (; r; ) {
    const o = r.prevDep;
    r.version === -1 ? (r === n && (n = o), m0(r), gN(r)) : t = r, r.dep.activeLink = r.prevActiveLink, r.prevActiveLink = void 0, r = o;
  }
  e.deps = t, e.depsTail = n;
}
function Qy(e) {
  for (let t = e.deps; t; t = t.nextDep) if (t.dep.version !== t.version || t.dep.computed && (mk(t.dep.computed) || t.dep.version !== t.version)) return !0;
  return !!e._dirty;
}
function mk(e) {
  if (e.flags & 4 && !(e.flags & 16) || (e.flags &= -17, e.globalVersion === Ed) || (e.globalVersion = Ed, !e.isSSR && e.flags & 128 && (!e.deps && !e._dirty || !Qy(e)))) return;
  e.flags |= 2;
  const t = e.dep, n = ln, r = Po;
  ln = e, Po = !0;
  try {
    hk(e);
    const o = e.fn(e._value);
    (t.version === 0 || os(o, e._value)) && (e.flags |= 128, e._value = o, t.version++);
  } catch (o) {
    throw t.version++, o;
  } finally {
    ln = n, Po = r, pk(e), e.flags &= -3;
  }
}
function m0(e, t = !1) {
  const { dep: n, prevSub: r, nextSub: o } = e;
  if (r && (r.nextSub = o, e.prevSub = void 0), o && (o.prevSub = r, e.nextSub = void 0), n.subs === e && (n.subs = r, !r && n.computed)) {
    n.computed.flags &= -5;
    for (let s = n.computed.deps; s; s = s.nextDep) m0(s, !0);
  }
  !t && !--n.sc && n.map && n.map.delete(n.key);
}
function gN(e) {
  const { prevDep: t, nextDep: n } = e;
  t && (t.nextDep = n, e.prevDep = void 0), n && (n.prevDep = t, e.nextDep = void 0);
}
var Po = !0, gk = [];
function Xs() {
  gk.push(Po), Po = !1;
}
function Qs() {
  const e = gk.pop();
  Po = e === void 0 ? !0 : e;
}
function RS(e) {
  const { cleanup: t } = e;
  if (e.cleanup = void 0, t) {
    const n = ln;
    ln = void 0;
    try {
      t();
    } finally {
      ln = n;
    }
  }
}
var Ed = 0, vN = class {
  constructor(e, t) {
    this.sub = e, this.dep = t, this.version = t.version, this.nextDep = this.prevDep = this.nextSub = this.prevSub = this.prevActiveLink = void 0;
  }
}, g0 = class {
  constructor(e) {
    this.computed = e, this.version = 0, this.activeLink = void 0, this.subs = void 0, this.map = void 0, this.key = void 0, this.sc = 0, this.__v_skip = !0;
  }
  track(e) {
    if (!ln || !Po || ln === this.computed) return;
    let t = this.activeLink;
    if (t === void 0 || t.sub !== ln)
      t = this.activeLink = new vN(ln, this), ln.deps ? (t.prevDep = ln.depsTail, ln.depsTail.nextDep = t, ln.depsTail = t) : ln.deps = ln.depsTail = t, vk(t);
    else if (t.version === -1 && (t.version = this.version, t.nextDep)) {
      const n = t.nextDep;
      n.prevDep = t.prevDep, t.prevDep && (t.prevDep.nextDep = n), t.prevDep = ln.depsTail, t.nextDep = void 0, ln.depsTail.nextDep = t, ln.depsTail = t, ln.deps === t && (ln.deps = n);
    }
    return t;
  }
  trigger(e) {
    this.version++, Ed++, this.notify(e);
  }
  notify(e) {
    h0();
    try {
      for (let t = this.subs; t; t = t.prevSub) t.sub.notify() && t.sub.dep.notify();
    } finally {
      p0();
    }
  }
};
function vk(e) {
  if (e.dep.sc++, e.sub.flags & 4) {
    const t = e.dep.computed;
    if (t && !e.dep.subs) {
      t.flags |= 20;
      for (let r = t.deps; r; r = r.nextDep) vk(r);
    }
    const n = e.dep.subs;
    n !== e && (e.prevSub = n, n && (n.nextSub = e)), e.dep.subs = e;
  }
}
var Zy = /* @__PURE__ */ new WeakMap(), Ni = /* @__PURE__ */ Symbol(""), jy = /* @__PURE__ */ Symbol(""), Ad = /* @__PURE__ */ Symbol("");
function rr(e, t, n) {
  if (Po && ln) {
    let r = Zy.get(e);
    r || Zy.set(e, r = /* @__PURE__ */ new Map());
    let o = r.get(n);
    o || (r.set(n, o = new g0()), o.map = r, o.key = n), o.track();
  }
}
function Os(e, t, n, r, o, s) {
  const a = Zy.get(e);
  if (!a) {
    Ed++;
    return;
  }
  const i = (l) => {
    l && l.trigger();
  };
  if (h0(), t === "clear") a.forEach(i);
  else {
    const l = Tt(e), u = l && d0(n);
    if (l && n === "length") {
      const d = Number(r);
      a.forEach((f, m) => {
        (m === "length" || m === Ad || !cs(m) && m >= d) && i(f);
      });
    } else
      switch ((n !== void 0 || a.has(void 0)) && i(a.get(n)), u && i(a.get(Ad)), t) {
        case "add":
          l ? u && i(a.get("length")) : (i(a.get(Ni)), Ql(e) && i(a.get(jy)));
          break;
        case "delete":
          l || (i(a.get(Ni)), Ql(e) && i(a.get(jy)));
          break;
        case "set":
          Ql(e) && i(a.get(Ni));
          break;
      }
  }
  p0();
}
function vl(e) {
  const t = /* @__PURE__ */ en(e);
  return t === e ? t : (rr(t, "iterate", Ad), /* @__PURE__ */ vo(e) ? t : t.map(Lo));
}
function Vm(e) {
  return rr(e = /* @__PURE__ */ en(e), "iterate", Ad), e;
}
function ts(e, t) {
  return /* @__PURE__ */ Zs(e) ? uu(/* @__PURE__ */ $i(e) ? Lo(t) : t) : Lo(t);
}
var yN = {
  __proto__: null,
  [Symbol.iterator]() {
    return Bv(this, Symbol.iterator, (e) => ts(this, e));
  },
  concat(...e) {
    return vl(this).concat(...e.map((t) => Tt(t) ? vl(t) : t));
  },
  entries() {
    return Bv(this, "entries", (e) => (e[1] = ts(this, e[1]), e));
  },
  every(e, t) {
    return As(this, "every", e, t, void 0, arguments);
  },
  filter(e, t) {
    return As(this, "filter", e, t, (n) => n.map((r) => ts(this, r)), arguments);
  },
  find(e, t) {
    return As(this, "find", e, t, (n) => ts(this, n), arguments);
  },
  findIndex(e, t) {
    return As(this, "findIndex", e, t, void 0, arguments);
  },
  findLast(e, t) {
    return As(this, "findLast", e, t, (n) => ts(this, n), arguments);
  },
  findLastIndex(e, t) {
    return As(this, "findLastIndex", e, t, void 0, arguments);
  },
  forEach(e, t) {
    return As(this, "forEach", e, t, void 0, arguments);
  },
  includes(...e) {
    return Uv(this, "includes", e);
  },
  indexOf(...e) {
    return Uv(this, "indexOf", e);
  },
  join(e) {
    return vl(this).join(e);
  },
  lastIndexOf(...e) {
    return Uv(this, "lastIndexOf", e);
  },
  map(e, t) {
    return As(this, "map", e, t, void 0, arguments);
  },
  pop() {
    return oc(this, "pop");
  },
  push(...e) {
    return oc(this, "push", e);
  },
  reduce(e, ...t) {
    return NS(this, "reduce", e, t);
  },
  reduceRight(e, ...t) {
    return NS(this, "reduceRight", e, t);
  },
  shift() {
    return oc(this, "shift");
  },
  some(e, t) {
    return As(this, "some", e, t, void 0, arguments);
  },
  splice(...e) {
    return oc(this, "splice", e);
  },
  toReversed() {
    return vl(this).toReversed();
  },
  toSorted(e) {
    return vl(this).toSorted(e);
  },
  toSpliced(...e) {
    return vl(this).toSpliced(...e);
  },
  unshift(...e) {
    return oc(this, "unshift", e);
  },
  values() {
    return Bv(this, "values", (e) => ts(this, e));
  }
};
function Bv(e, t, n) {
  const r = Vm(e), o = r[t]();
  return r !== e && !/* @__PURE__ */ vo(e) && (o._next = o.next, o.next = () => {
    const s = o._next();
    return s.done || (s.value = n(s.value)), s;
  }), o;
}
var _N = Array.prototype;
function As(e, t, n, r, o, s) {
  const a = Vm(e), i = a !== e && !/* @__PURE__ */ vo(e), l = a[t];
  if (l !== _N[t]) {
    const f = l.apply(e, s);
    return i ? Lo(f) : f;
  }
  let u = n;
  a !== e && (i ? u = function(f, m) {
    return n.call(this, ts(e, f), m, e);
  } : n.length > 2 && (u = function(f, m) {
    return n.call(this, f, m, e);
  }));
  const d = l.call(a, u, r);
  return i && o ? o(d) : d;
}
function NS(e, t, n, r) {
  const o = Vm(e), s = o !== e && !/* @__PURE__ */ vo(e);
  let a = n, i = !1;
  o !== e && (s ? (i = r.length === 0, a = function(u, d, f) {
    return i && (i = !1, u = ts(e, u)), n.call(this, u, ts(e, d), f, e);
  }) : n.length > 3 && (a = function(u, d, f) {
    return n.call(this, u, d, f, e);
  }));
  const l = o[t](a, ...r);
  return i ? ts(e, l) : l;
}
function Uv(e, t, n) {
  const r = /* @__PURE__ */ en(e);
  rr(r, "iterate", Ad);
  const o = r[t](...n);
  return (o === -1 || o === !1) && /* @__PURE__ */ b0(n[0]) ? (n[0] = /* @__PURE__ */ en(n[0]), r[t](...n)) : o;
}
function oc(e, t, n = []) {
  Xs(), h0();
  const r = (/* @__PURE__ */ en(e))[t].apply(e, n);
  return p0(), Qs(), r;
}
var bN = /* @__PURE__ */ Um("__proto__,__v_isRef,__isVue"), yk = new Set(/* @__PURE__ */ Object.getOwnPropertyNames(Symbol).filter((e) => e !== "arguments" && e !== "caller").map((e) => Symbol[e]).filter(cs));
function wN(e) {
  cs(e) || (e = String(e));
  const t = /* @__PURE__ */ en(this);
  return rr(t, "has", e), t.hasOwnProperty(e);
}
var _k = class {
  constructor(e = !1, t = !1) {
    this._isReadonly = e, this._isShallow = t;
  }
  get(e, t, n) {
    if (t === "__v_skip") return e.__v_skip;
    const r = this._isReadonly, o = this._isShallow;
    if (t === "__v_isReactive") return !r;
    if (t === "__v_isReadonly") return r;
    if (t === "__v_isShallow") return o;
    if (t === "__v_raw")
      return n === (r ? o ? PN : Tk : o ? Sk : wk).get(e) || Object.getPrototypeOf(e) === Object.getPrototypeOf(n) ? e : void 0;
    const s = Tt(e);
    if (!r) {
      let i;
      if (s && (i = yN[t])) return i;
      if (t === "hasOwnProperty") return wN;
    }
    const a = Reflect.get(e, t, /* @__PURE__ */ pn(e) ? e : n);
    if ((cs(t) ? yk.has(t) : bN(t)) || (r || rr(e, "get", t), o)) return a;
    if (/* @__PURE__ */ pn(a)) {
      const i = s && d0(t) ? a : a.value;
      return r && on(i) ? /* @__PURE__ */ t_(i) : i;
    }
    return on(a) ? r ? /* @__PURE__ */ t_(a) : /* @__PURE__ */ y0(a) : a;
  }
}, bk = class extends _k {
  constructor(e = !1) {
    super(!1, e);
  }
  set(e, t, n, r) {
    let o = e[t];
    const s = Tt(e) && d0(t);
    if (!this._isShallow) {
      const l = /* @__PURE__ */ Zs(o);
      if (!/* @__PURE__ */ vo(n) && !/* @__PURE__ */ Zs(n) && (o = /* @__PURE__ */ en(o), n = /* @__PURE__ */ en(n)), !s && /* @__PURE__ */ pn(o) && !/* @__PURE__ */ pn(n)) return l || (o.value = n), !0;
    }
    const a = s ? Number(t) < e.length : tn(e, t), i = Reflect.set(e, t, n, /* @__PURE__ */ pn(e) ? e : r);
    return e === /* @__PURE__ */ en(r) && (a ? os(n, o) && Os(e, "set", t, n, o) : Os(e, "add", t, n)), i;
  }
  deleteProperty(e, t) {
    const n = tn(e, t), r = e[t], o = Reflect.deleteProperty(e, t);
    return o && n && Os(e, "delete", t, void 0, r), o;
  }
  has(e, t) {
    const n = Reflect.has(e, t);
    return (!cs(t) || !yk.has(t)) && rr(e, "has", t), n;
  }
  ownKeys(e) {
    return rr(e, "iterate", Tt(e) ? "length" : Ni), Reflect.ownKeys(e);
  }
}, SN = class extends _k {
  constructor(e = !1) {
    super(!0, e);
  }
  set(e, t) {
    return !0;
  }
  deleteProperty(e, t) {
    return !0;
  }
}, TN = /* @__PURE__ */ new bk(), EN = /* @__PURE__ */ new SN(), AN = /* @__PURE__ */ new bk(!0), e_ = (e) => e, Zf = (e) => Reflect.getPrototypeOf(e);
function CN(e, t, n) {
  return function(...r) {
    const o = this.__v_raw, s = /* @__PURE__ */ en(o), a = Ql(s), i = e === "entries" || e === Symbol.iterator && a, l = e === "keys" && a, u = o[e](...r), d = n ? e_ : t ? uu : Lo;
    return !t && rr(s, "iterate", l ? jy : Ni), On(Object.create(u), { next() {
      const { value: f, done: m } = u.next();
      return m ? {
        value: f,
        done: m
      } : {
        value: i ? [d(f[0]), d(f[1])] : d(f),
        done: m
      };
    } });
  };
}
function jf(e) {
  return function(...t) {
    return e === "delete" ? !1 : e === "clear" ? void 0 : this;
  };
}
function kN(e, t) {
  const n = {
    get(r) {
      const o = this.__v_raw, s = /* @__PURE__ */ en(o), a = /* @__PURE__ */ en(r);
      e || (os(r, a) && rr(s, "get", r), rr(s, "get", a));
      const { has: i } = Zf(s), l = t ? e_ : e ? uu : Lo;
      if (i.call(s, r)) return l(o.get(r));
      if (i.call(s, a)) return l(o.get(a));
      o !== s && o.get(r);
    },
    get size() {
      const r = this.__v_raw;
      return !e && rr(/* @__PURE__ */ en(r), "iterate", Ni), r.size;
    },
    has(r) {
      const o = this.__v_raw, s = /* @__PURE__ */ en(o), a = /* @__PURE__ */ en(r);
      return e || (os(r, a) && rr(s, "has", r), rr(s, "has", a)), r === a ? o.has(r) : o.has(r) || o.has(a);
    },
    forEach(r, o) {
      const s = this, a = s.__v_raw, i = /* @__PURE__ */ en(a), l = t ? e_ : e ? uu : Lo;
      return !e && rr(i, "iterate", Ni), a.forEach((u, d) => r.call(o, l(u), l(d), s));
    }
  };
  return On(n, e ? {
    add: jf("add"),
    set: jf("set"),
    delete: jf("delete"),
    clear: jf("clear")
  } : {
    add(r) {
      const o = /* @__PURE__ */ en(this), s = Zf(o), a = /* @__PURE__ */ en(r), i = !t && !/* @__PURE__ */ vo(r) && !/* @__PURE__ */ Zs(r) ? a : r;
      return s.has.call(o, i) || os(r, i) && s.has.call(o, r) || os(a, i) && s.has.call(o, a) || (o.add(i), Os(o, "add", i, i)), this;
    },
    set(r, o) {
      !t && !/* @__PURE__ */ vo(o) && !/* @__PURE__ */ Zs(o) && (o = /* @__PURE__ */ en(o));
      const s = /* @__PURE__ */ en(this), { has: a, get: i } = Zf(s);
      let l = a.call(s, r);
      l || (r = /* @__PURE__ */ en(r), l = a.call(s, r));
      const u = i.call(s, r);
      return s.set(r, o), l ? os(o, u) && Os(s, "set", r, o, u) : Os(s, "add", r, o), this;
    },
    delete(r) {
      const o = /* @__PURE__ */ en(this), { has: s, get: a } = Zf(o);
      let i = s.call(o, r);
      i || (r = /* @__PURE__ */ en(r), i = s.call(o, r));
      const l = a ? a.call(o, r) : void 0, u = o.delete(r);
      return i && Os(o, "delete", r, void 0, l), u;
    },
    clear() {
      const r = /* @__PURE__ */ en(this), o = r.size !== 0, s = void 0, a = r.clear();
      return o && Os(r, "clear", void 0, void 0, s), a;
    }
  }), [
    "keys",
    "values",
    "entries",
    Symbol.iterator
  ].forEach((r) => {
    n[r] = CN(r, e, t);
  }), n;
}
function v0(e, t) {
  const n = kN(e, t);
  return (r, o, s) => o === "__v_isReactive" ? !e : o === "__v_isReadonly" ? e : o === "__v_raw" ? r : Reflect.get(tn(n, o) && o in r ? n : r, o, s);
}
var IN = { get: /* @__PURE__ */ v0(!1, !1) }, xN = { get: /* @__PURE__ */ v0(!1, !0) }, MN = { get: /* @__PURE__ */ v0(!0, !1) }, wk = /* @__PURE__ */ new WeakMap(), Sk = /* @__PURE__ */ new WeakMap(), Tk = /* @__PURE__ */ new WeakMap(), PN = /* @__PURE__ */ new WeakMap();
function RN(e) {
  switch (e) {
    case "Object":
    case "Array":
      return 1;
    case "Map":
    case "Set":
    case "WeakMap":
    case "WeakSet":
      return 2;
    default:
      return 0;
  }
}
// @__NO_SIDE_EFFECTS__
function y0(e) {
  return /* @__PURE__ */ Zs(e) ? e : _0(e, !1, TN, IN, wk);
}
// @__NO_SIDE_EFFECTS__
function NN(e) {
  return _0(e, !1, AN, xN, Sk);
}
// @__NO_SIDE_EFFECTS__
function t_(e) {
  return _0(e, !0, EN, MN, Tk);
}
function _0(e, t, n, r, o) {
  if (!on(e) || e.__v_raw && !(t && e.__v_isReactive) || e.__v_skip || !Object.isExtensible(e)) return e;
  const s = o.get(e);
  if (s) return s;
  const a = RN(sN(e));
  if (a === 0) return e;
  const i = new Proxy(e, a === 2 ? r : n);
  return o.set(e, i), i;
}
// @__NO_SIDE_EFFECTS__
function $i(e) {
  return /* @__PURE__ */ Zs(e) ? /* @__PURE__ */ $i(e.__v_raw) : !!(e && e.__v_isReactive);
}
// @__NO_SIDE_EFFECTS__
function Zs(e) {
  return !!(e && e.__v_isReadonly);
}
// @__NO_SIDE_EFFECTS__
function vo(e) {
  return !!(e && e.__v_isShallow);
}
// @__NO_SIDE_EFFECTS__
function b0(e) {
  return e ? !!e.__v_raw : !1;
}
// @__NO_SIDE_EFFECTS__
function en(e) {
  const t = e && e.__v_raw;
  return t ? /* @__PURE__ */ en(t) : e;
}
function $N(e) {
  return !tn(e, "__v_skip") && Object.isExtensible(e) && sk(e, "__v_skip", !0), e;
}
var Lo = (e) => on(e) ? /* @__PURE__ */ y0(e) : e, uu = (e) => on(e) ? /* @__PURE__ */ t_(e) : e;
// @__NO_SIDE_EFFECTS__
function pn(e) {
  return e ? e.__v_isRef === !0 : !1;
}
// @__NO_SIDE_EFFECTS__
function ie(e) {
  return DN(e, !1);
}
function DN(e, t) {
  return /* @__PURE__ */ pn(e) ? e : new LN(e, t);
}
var LN = class {
  constructor(e, t) {
    this.dep = new g0(), this.__v_isRef = !0, this.__v_isShallow = !1, this._rawValue = t ? e : /* @__PURE__ */ en(e), this._value = t ? e : Lo(e), this.__v_isShallow = t;
  }
  get value() {
    return this.dep.track(), this._value;
  }
  set value(e) {
    const t = this._rawValue, n = this.__v_isShallow || /* @__PURE__ */ vo(e) || /* @__PURE__ */ Zs(e);
    e = n ? e : /* @__PURE__ */ en(e), os(e, t) && (this._rawValue = e, this._value = n ? e : Lo(e), this.dep.trigger());
  }
};
function S(e) {
  return /* @__PURE__ */ pn(e) ? e.value : e;
}
var ON = {
  get: (e, t, n) => t === "__v_raw" ? e : S(Reflect.get(e, t, n)),
  set: (e, t, n, r) => {
    const o = e[t];
    return /* @__PURE__ */ pn(o) && !/* @__PURE__ */ pn(n) ? (o.value = n, !0) : Reflect.set(e, t, n, r);
  }
};
function Ek(e) {
  return /* @__PURE__ */ $i(e) ? e : new Proxy(e, ON);
}
var FN = class {
  constructor(e, t, n) {
    this.fn = e, this.setter = t, this._value = void 0, this.dep = new g0(this), this.__v_isRef = !0, this.deps = void 0, this.depsTail = void 0, this.flags = 16, this.globalVersion = Ed - 1, this.next = void 0, this.effect = this, this.__v_isReadonly = !t, this.isSSR = n;
  }
  notify() {
    if (this.flags |= 16, !(this.flags & 8) && ln !== this)
      return fk(this, !0), !0;
  }
  get value() {
    const e = this.dep.track();
    return mk(this), e && (e.version = this.dep.version), this._value;
  }
  set value(e) {
    this.setter && this.setter(e);
  }
};
// @__NO_SIDE_EFFECTS__
function BN(e, t, n = !1) {
  let r, o;
  return $t(e) ? r = e : (r = e.get, o = e.set), new FN(r, o, n);
}
var eh = {}, xp = /* @__PURE__ */ new WeakMap(), gi = void 0;
function UN(e, t = !1, n = gi) {
  if (n) {
    let r = xp.get(n);
    r || xp.set(n, r = []), r.push(e);
  }
}
function qN(e, t, n = an) {
  const { immediate: r, deep: o, once: s, scheduler: a, augmentJob: i, call: l } = n, u = ($) => o ? $ : /* @__PURE__ */ vo($) || o === !1 || o === 0 ? Fs($, 1) : Fs($);
  let d, f, m, v, y = !1, T = !1;
  if (/* @__PURE__ */ pn(e) ? (f = () => e.value, y = /* @__PURE__ */ vo(e)) : /* @__PURE__ */ $i(e) ? (f = () => u(e), y = !0) : Tt(e) ? (T = !0, y = e.some(($) => /* @__PURE__ */ $i($) || /* @__PURE__ */ vo($)), f = () => e.map(($) => {
    if (/* @__PURE__ */ pn($)) return $.value;
    if (/* @__PURE__ */ $i($)) return u($);
    if ($t($)) return l ? l($, 2) : $();
  })) : $t(e) ? t ? f = l ? () => l(e, 2) : e : f = () => {
    if (m) {
      Xs();
      try {
        m();
      } finally {
        Qs();
      }
    }
    const $ = gi;
    gi = d;
    try {
      return l ? l(e, 3, [v]) : e(v);
    } finally {
      gi = $;
    }
  } : f = as, t && o) {
    const $ = f, p = o === !0 ? 1 / 0 : o;
    f = () => Fs($(), p);
  }
  const C = mN(), A = () => {
    d.stop(), C && C.active && c0(C.effects, d);
  };
  if (s && t) {
    const $ = t;
    t = (...p) => {
      $(...p), A();
    };
  }
  let I = T ? new Array(e.length).fill(eh) : eh;
  const R = ($) => {
    if (!(!(d.flags & 1) || !d.dirty && !$))
      if (t) {
        const p = d.run();
        if (o || y || (T ? p.some((b, w) => os(b, I[w])) : os(p, I))) {
          m && m();
          const b = gi;
          gi = d;
          try {
            const w = [
              p,
              I === eh ? void 0 : T && I[0] === eh ? [] : I,
              v
            ];
            I = p, l ? l(t, 3, w) : t(...w);
          } finally {
            gi = b;
          }
        }
      } else d.run();
  };
  return i && i(R), d = new ck(f), d.scheduler = a ? () => a(R, !1) : R, v = ($) => UN($, !1, d), m = d.onStop = () => {
    const $ = xp.get(d);
    if ($) {
      if (l) l($, 4);
      else for (const p of $) p();
      xp.delete(d);
    }
  }, t ? r ? R(!0) : I = d.run() : a ? a(R.bind(null, !0), !0) : d.run(), A.pause = d.pause.bind(d), A.resume = d.resume.bind(d), A.stop = A, A;
}
function Fs(e, t = 1 / 0, n) {
  if (t <= 0 || !on(e) || e.__v_skip || (n = n || /* @__PURE__ */ new Map(), (n.get(e) || 0) >= t)) return e;
  if (n.set(e, t), t--, /* @__PURE__ */ pn(e)) Fs(e.value, t, n);
  else if (Tt(e)) for (let r = 0; r < e.length; r++) Fs(e[r], t, n);
  else if (Au(e) || Ql(e)) e.forEach((r) => {
    Fs(r, t, n);
  });
  else if (rk(e)) {
    for (const r in e) Fs(e[r], t, n);
    for (const r of Object.getOwnPropertySymbols(e)) Object.prototype.propertyIsEnumerable.call(e, r) && Fs(e[r], t, n);
  }
  return e;
}
function ef(e, t, n, r) {
  try {
    return r ? e(...r) : e();
  } catch (o) {
    Km(o, t, n);
  }
}
function Oo(e, t, n, r) {
  if ($t(e)) {
    const o = ef(e, t, n, r);
    return o && tk(o) && o.catch((s) => {
      Km(s, t, n);
    }), o;
  }
  if (Tt(e)) {
    const o = [];
    for (let s = 0; s < e.length; s++) o.push(Oo(e[s], t, n, r));
    return o;
  }
}
function Km(e, t, n, r = !0) {
  const o = t ? t.vnode : null, { errorHandler: s, throwUnhandledErrorInProduction: a } = t && t.appContext.config || an;
  if (t) {
    let i = t.parent;
    const l = t.proxy, u = `https://vuejs.org/error-reference/#runtime-${n}`;
    for (; i; ) {
      const d = i.ec;
      if (d) {
        for (let f = 0; f < d.length; f++) if (d[f](e, l, u) === !1) return;
      }
      i = i.parent;
    }
    if (s) {
      Xs(), ef(s, null, 10, [
        e,
        l,
        u
      ]), Qs();
      return;
    }
  }
  HN(e, n, o, r, a);
}
function HN(e, t, n, r = !0, o = !1) {
  if (o) throw e;
  console.error(e);
}
var mr = [], Zo = -1, Zl = [], _a = null, Nl = 0, Ak = /* @__PURE__ */ Promise.resolve(), Mp = null;
function Qt(e) {
  const t = Mp || Ak;
  return e ? t.then(this ? e.bind(this) : e) : t;
}
function zN(e) {
  let t = Zo + 1, n = mr.length;
  for (; t < n; ) {
    const r = t + n >>> 1, o = mr[r], s = Cd(o);
    s < e || s === e && o.flags & 2 ? t = r + 1 : n = r;
  }
  return t;
}
function w0(e) {
  if (!(e.flags & 1)) {
    const t = Cd(e), n = mr[mr.length - 1];
    !n || !(e.flags & 2) && t >= Cd(n) ? mr.push(e) : mr.splice(zN(t), 0, e), e.flags |= 1, Ck();
  }
}
function Ck() {
  Mp || (Mp = Ak.then(Ik));
}
function WN(e) {
  Tt(e) ? Zl.push(...e) : _a && e.id === -1 ? _a.splice(Nl + 1, 0, e) : e.flags & 1 || (Zl.push(e), e.flags |= 1), Ck();
}
function $S(e, t, n = Zo + 1) {
  for (; n < mr.length; n++) {
    const r = mr[n];
    if (r && r.flags & 2) {
      if (e && r.id !== e.uid) continue;
      mr.splice(n, 1), n--, r.flags & 4 && (r.flags &= -2), r(), r.flags & 4 || (r.flags &= -2);
    }
  }
}
function kk(e) {
  if (Zl.length) {
    const t = [...new Set(Zl)].sort((n, r) => Cd(n) - Cd(r));
    if (Zl.length = 0, _a) {
      _a.push(...t);
      return;
    }
    for (_a = t, Nl = 0; Nl < _a.length; Nl++) {
      const n = _a[Nl];
      n.flags & 4 && (n.flags &= -2), n.flags & 8 || n(), n.flags &= -2;
    }
    _a = null, Nl = 0;
  }
}
var Cd = (e) => e.id == null ? e.flags & 2 ? -1 : 1 / 0 : e.id;
function Ik(e) {
  try {
    for (Zo = 0; Zo < mr.length; Zo++) {
      const t = mr[Zo];
      t && !(t.flags & 8) && (t.flags & 4 && (t.flags &= -2), ef(t, t.i, t.i ? 15 : 14), t.flags & 4 || (t.flags &= -2));
    }
  } finally {
    for (; Zo < mr.length; Zo++) {
      const t = mr[Zo];
      t && (t.flags &= -2);
    }
    Zo = -1, mr.length = 0, kk(e), Mp = null, (mr.length || Zl.length) && Ik(e);
  }
}
var po = null, xk = null;
function Pp(e) {
  const t = po;
  return po = e, xk = e && e.type.__scopeId || null, t;
}
function GN(e, t = po, n) {
  if (!t || e._n) return e;
  const r = (...o) => {
    r._d && WS(-1);
    const s = Pp(t);
    let a;
    try {
      a = e(...o);
    } finally {
      Pp(s), r._d && WS(1);
    }
    return a;
  };
  return r._n = !0, r._c = !0, r._d = !0, r;
}
function Ln(e, t) {
  if (po === null) return e;
  const n = Qm(po), r = e.dirs || (e.dirs = []);
  for (let o = 0; o < t.length; o++) {
    let [s, a, i, l = an] = t[o];
    s && ($t(s) && (s = {
      mounted: s,
      updated: s
    }), s.deep && Fs(a), r.push({
      dir: s,
      instance: n,
      value: a,
      oldValue: void 0,
      arg: i,
      modifiers: l
    }));
  }
  return e;
}
function ui(e, t, n, r) {
  const o = e.dirs, s = t && t.dirs;
  for (let a = 0; a < o.length; a++) {
    const i = o[a];
    s && (i.oldValue = s[a].value);
    let l = i.dir[r];
    l && (Xs(), Oo(l, n, 8, [
      e.el,
      i,
      e,
      t
    ]), Qs());
  }
}
function Mk(e, t) {
  if (vr) {
    let n = vr.provides;
    const r = vr.parent && vr.parent.provides;
    r === n && (n = vr.provides = Object.create(r)), n[e] = t;
  }
}
function sd(e, t, n = !1) {
  const r = B7();
  if (r || jl) {
    let o = jl ? jl._context.provides : r ? r.parent == null || r.ce ? r.vnode.appContext && r.vnode.appContext.provides : r.parent.provides : void 0;
    if (o && e in o) return o[e];
    if (arguments.length > 1) return n && $t(t) ? t.call(r && r.proxy) : t;
  }
}
var VN = /* @__PURE__ */ Symbol.for("v-scx"), KN = () => {
  {
    const e = sd(VN);
    return e;
  }
};
function gt(e, t, n) {
  return Pk(e, t, n);
}
function Pk(e, t, n = an) {
  const { immediate: r, deep: o, flush: s, once: a } = n, i = On({}, n), l = t && r || !t && s !== "post";
  let u;
  if (Id) {
    if (s === "sync") {
      const v = KN();
      u = v.__watcherHandles || (v.__watcherHandles = []);
    } else if (!l) {
      const v = () => {
      };
      return v.stop = as, v.resume = as, v.pause = as, v;
    }
  }
  const d = vr;
  i.call = (v, y, T) => Oo(v, d, y, T);
  let f = !1;
  s === "post" ? i.scheduler = (v) => {
    Tr(v, d && d.suspense);
  } : s !== "sync" && (f = !0, i.scheduler = (v, y) => {
    y ? v() : w0(v);
  }), i.augmentJob = (v) => {
    t && (v.flags |= 4), f && (v.flags |= 2, d && (v.id = d.uid, v.i = d));
  };
  const m = qN(e, t, i);
  return Id && (u ? u.push(m) : l && m()), m;
}
function JN(e, t, n) {
  const r = this.proxy, o = _n(e) ? e.includes(".") ? Rk(r, e) : () => r[e] : e.bind(r, r);
  let s;
  $t(t) ? s = t : (s = t.handler, n = t);
  const a = tf(this), i = Pk(o, s.bind(r), n);
  return a(), i;
}
function Rk(e, t) {
  const n = t.split(".");
  return () => {
    let r = e;
    for (let o = 0; o < n.length && r; o++) r = r[n[o]];
    return r;
  };
}
var YN = /* @__PURE__ */ Symbol("_vte"), XN = (e) => e.__isTeleport, qv = /* @__PURE__ */ Symbol("_leaveCb");
function S0(e, t) {
  e.shapeFlag & 6 && e.component ? (e.transition = t, S0(e.component.subTree, t)) : e.shapeFlag & 128 ? (e.ssContent.transition = t.clone(e.ssContent), e.ssFallback.transition = t.clone(e.ssFallback)) : e.transition = t;
}
// @__NO_SIDE_EFFECTS__
function un(e, t) {
  return $t(e) ? On({ name: e.name }, t, { setup: e }) : e;
}
function Nk(e) {
  e.ids = [
    e.ids[0] + e.ids[2]++ + "-",
    0,
    0
  ];
}
function DS(e, t) {
  let n;
  return !!((n = Object.getOwnPropertyDescriptor(e, t)) && !n.configurable);
}
var Rp = /* @__PURE__ */ new WeakMap();
function ad(e, t, n, r, o = !1) {
  if (Tt(e)) {
    e.forEach((T, C) => ad(T, t && (Tt(t) ? t[C] : t), n, r, o));
    return;
  }
  if (id(r) && !o) {
    r.shapeFlag & 512 && r.type.__asyncResolved && r.component.subTree.component && ad(e, t, n, r.component.subTree);
    return;
  }
  const s = r.shapeFlag & 4 ? Qm(r.component) : r.el, a = o ? null : s, { i, r: l } = e, u = t && t.r, d = i.refs === an ? i.refs = {} : i.refs, f = i.setupState, m = /* @__PURE__ */ en(f), v = f === an ? ek : (T) => DS(d, T) ? !1 : tn(m, T), y = (T, C) => !(C && DS(d, C));
  if (u != null && u !== l) {
    if (LS(t), _n(u))
      d[u] = null, v(u) && (f[u] = null);
    else if (/* @__PURE__ */ pn(u)) {
      const T = t;
      y(u, T.k) && (u.value = null), T.k && (d[T.k] = null);
    }
  }
  if ($t(l)) ef(l, i, 12, [a, d]);
  else {
    const T = _n(l), C = /* @__PURE__ */ pn(l);
    if (T || C) {
      const A = () => {
        if (e.f) {
          const I = T ? v(l) ? f[l] : d[l] : y(l) || !e.k ? l.value : d[e.k];
          if (o) Tt(I) && c0(I, s);
          else if (Tt(I)) I.includes(s) || I.push(s);
          else if (T)
            d[l] = [s], v(l) && (f[l] = d[l]);
          else {
            const R = [s];
            y(l, e.k) && (l.value = R), e.k && (d[e.k] = R);
          }
        } else T ? (d[l] = a, v(l) && (f[l] = a)) : C && (y(l, e.k) && (l.value = a), e.k && (d[e.k] = a));
      };
      if (a) {
        const I = () => {
          A(), Rp.delete(e);
        };
        I.id = -1, Rp.set(e, I), Tr(I, n);
      } else
        LS(e), A();
    }
  }
}
function LS(e) {
  const t = Rp.get(e);
  t && (t.flags |= 8, Rp.delete(e));
}
var _he = Gm().requestIdleCallback || ((e) => setTimeout(e, 1)), bhe = Gm().cancelIdleCallback || ((e) => clearTimeout(e)), id = (e) => !!e.type.__asyncLoader, $k = (e) => e.type.__isKeepAlive;
function QN(e, t) {
  Dk(e, "a", t);
}
function ZN(e, t) {
  Dk(e, "da", t);
}
function Dk(e, t, n = vr) {
  const r = e.__wdc || (e.__wdc = () => {
    let o = n;
    for (; o; ) {
      if (o.isDeactivated) return;
      o = o.parent;
    }
    return e();
  });
  if (Jm(t, r, n), n) {
    let o = n.parent;
    for (; o && o.parent; )
      $k(o.parent.vnode) && jN(r, t, n, o), o = o.parent;
  }
}
function jN(e, t, n, r) {
  const o = Jm(t, e, r, !0);
  T0(() => {
    c0(r[t], o);
  }, n);
}
function Jm(e, t, n = vr, r = !1) {
  if (n) {
    const o = n[e] || (n[e] = []), s = t.__weh || (t.__weh = (...a) => {
      Xs();
      const i = tf(n), l = Oo(t, n, e, a);
      return i(), Qs(), l;
    });
    return r ? o.unshift(s) : o.push(s), s;
  }
}
var ea = (e) => (t, n = vr) => {
  (!Id || e === "sp") && Jm(e, (...r) => t(...r), n);
}, e7 = ea("bm"), Lk = ea("m"), Ok = ea("bu"), Fk = ea("u"), cu = ea("bum"), T0 = ea("um"), t7 = ea("sp"), n7 = ea("rtg"), r7 = ea("rtc");
function o7(e, t = vr) {
  Jm("ec", e, t);
}
var s7 = /* @__PURE__ */ Symbol.for("v-ndc");
function lt(e, t, n, r) {
  let o;
  const s = n && n[r], a = Tt(e);
  if (a || _n(e)) {
    const i = a && /* @__PURE__ */ $i(e);
    let l = !1, u = !1;
    i && (l = !/* @__PURE__ */ vo(e), u = /* @__PURE__ */ Zs(e), e = Vm(e)), o = new Array(e.length);
    for (let d = 0, f = e.length; d < f; d++) o[d] = t(l ? u ? uu(Lo(e[d])) : Lo(e[d]) : e[d], d, void 0, s && s[d]);
  } else if (typeof e == "number") {
    o = new Array(e);
    for (let i = 0; i < e; i++) o[i] = t(i + 1, i, void 0, s && s[i]);
  } else if (on(e)) if (e[Symbol.iterator]) o = Array.from(e, (i, l) => t(i, l, void 0, s && s[l]));
  else {
    const i = Object.keys(e);
    o = new Array(i.length);
    for (let l = 0, u = i.length; l < u; l++) {
      const d = i[l];
      o[l] = t(e[d], d, l, s && s[l]);
    }
  }
  else o = [];
  return n && (n[r] = o), o;
}
var n_ = (e) => e ? oI(e) ? Qm(e) : n_(e.parent) : null, ld = /* @__PURE__ */ On(/* @__PURE__ */ Object.create(null), {
  $: (e) => e,
  $el: (e) => e.vnode.el,
  $data: (e) => e.data,
  $props: (e) => e.props,
  $attrs: (e) => e.attrs,
  $slots: (e) => e.slots,
  $refs: (e) => e.refs,
  $parent: (e) => n_(e.parent),
  $root: (e) => n_(e.root),
  $host: (e) => e.ce,
  $emit: (e) => e.emit,
  $options: (e) => E0(e),
  $forceUpdate: (e) => e.f || (e.f = () => {
    w0(e.update);
  }),
  $nextTick: (e) => e.n || (e.n = Qt.bind(e.proxy)),
  $watch: (e) => JN.bind(e)
}), Hv = (e, t) => e !== an && !e.__isScriptSetup && tn(e, t), a7 = {
  get({ _: e }, t) {
    if (t === "__v_skip") return !0;
    const { ctx: n, setupState: r, data: o, props: s, accessCache: a, type: i, appContext: l } = e;
    if (t[0] !== "$") {
      const m = a[t];
      if (m !== void 0) switch (m) {
        case 1:
          return r[t];
        case 2:
          return o[t];
        case 4:
          return n[t];
        case 3:
          return s[t];
      }
      else {
        if (Hv(r, t))
          return a[t] = 1, r[t];
        if (o !== an && tn(o, t))
          return a[t] = 2, o[t];
        if (tn(s, t))
          return a[t] = 3, s[t];
        if (n !== an && tn(n, t))
          return a[t] = 4, n[t];
        r_ && (a[t] = 0);
      }
    }
    const u = ld[t];
    let d, f;
    if (u)
      return t === "$attrs" && rr(e.attrs, "get", ""), u(e);
    if ((d = i.__cssModules) && (d = d[t])) return d;
    if (n !== an && tn(n, t))
      return a[t] = 4, n[t];
    if (f = l.config.globalProperties, tn(f, t)) return f[t];
  },
  set({ _: e }, t, n) {
    const { data: r, setupState: o, ctx: s } = e;
    return Hv(o, t) ? (o[t] = n, !0) : r !== an && tn(r, t) ? (r[t] = n, !0) : tn(e.props, t) || t[0] === "$" && t.slice(1) in e ? !1 : (s[t] = n, !0);
  },
  has({ _: { data: e, setupState: t, accessCache: n, ctx: r, appContext: o, props: s, type: a } }, i) {
    let l;
    return !!(n[i] || e !== an && i[0] !== "$" && tn(e, i) || Hv(t, i) || tn(s, i) || tn(r, i) || tn(ld, i) || tn(o.config.globalProperties, i) || (l = a.__cssModules) && l[i]);
  },
  defineProperty(e, t, n) {
    return n.get != null ? e._.accessCache[t] = 0 : tn(n, "value") && this.set(e, t, n.value, null), Reflect.defineProperty(e, t, n);
  }
};
function OS(e) {
  return Tt(e) ? e.reduce((t, n) => (t[n] = null, t), {}) : e;
}
var r_ = !0;
function i7(e) {
  const t = E0(e), n = e.proxy, r = e.ctx;
  r_ = !1, t.beforeCreate && FS(t.beforeCreate, e, "bc");
  const { data: o, computed: s, methods: a, watch: i, provide: l, inject: u, created: d, beforeMount: f, mounted: m, beforeUpdate: v, updated: y, activated: T, deactivated: C, beforeDestroy: A, beforeUnmount: I, destroyed: R, unmounted: $, render: p, renderTracked: b, renderTriggered: w, errorCaptured: E, serverPrefetch: x, expose: M, inheritAttrs: N, components: q, directives: z, filters: J } = t;
  if (u && l7(u, r, null), a) for (const B in a) {
    const X = a[B];
    $t(X) && (r[B] = X.bind(n));
  }
  if (o) {
    const B = o.call(n, n);
    on(B) && (e.data = /* @__PURE__ */ y0(B));
  }
  if (r_ = !0, s) for (const B in s) {
    const X = s[B], de = re({
      get: $t(X) ? X.bind(n, n) : $t(X.get) ? X.get.bind(n, n) : as,
      set: !$t(X) && $t(X.set) ? X.set.bind(n) : as
    });
    Object.defineProperty(r, B, {
      enumerable: !0,
      configurable: !0,
      get: () => de.value,
      set: (fe) => de.value = fe
    });
  }
  if (i) for (const B in i) Bk(i[B], r, n, B);
  if (l) {
    const B = $t(l) ? l.call(n) : l;
    Reflect.ownKeys(B).forEach((X) => {
      Mk(X, B[X]);
    });
  }
  d && FS(d, e, "c");
  function oe(B, X) {
    Tt(X) ? X.forEach((de) => B(de.bind(n))) : X && B(X.bind(n));
  }
  if (oe(e7, f), oe(Lk, m), oe(Ok, v), oe(Fk, y), oe(QN, T), oe(ZN, C), oe(o7, E), oe(r7, b), oe(n7, w), oe(cu, I), oe(T0, $), oe(t7, x), Tt(M))
    if (M.length) {
      const B = e.exposed || (e.exposed = {});
      M.forEach((X) => {
        Object.defineProperty(B, X, {
          get: () => n[X],
          set: (de) => n[X] = de,
          enumerable: !0
        });
      });
    } else e.exposed || (e.exposed = {});
  p && e.render === as && (e.render = p), N != null && (e.inheritAttrs = N), q && (e.components = q), z && (e.directives = z), x && Nk(e);
}
function l7(e, t, n = as) {
  Tt(e) && (e = o_(e));
  for (const r in e) {
    const o = e[r];
    let s;
    on(o) ? "default" in o ? s = sd(o.from || r, o.default, !0) : s = sd(o.from || r) : s = sd(o), /* @__PURE__ */ pn(s) ? Object.defineProperty(t, r, {
      enumerable: !0,
      configurable: !0,
      get: () => s.value,
      set: (a) => s.value = a
    }) : t[r] = s;
  }
}
function FS(e, t, n) {
  Oo(Tt(e) ? e.map((r) => r.bind(t.proxy)) : e.bind(t.proxy), t, n);
}
function Bk(e, t, n, r) {
  let o = r.includes(".") ? Rk(n, r) : () => n[r];
  if (_n(e)) {
    const s = t[e];
    $t(s) && gt(o, s);
  } else if ($t(e)) gt(o, e.bind(n));
  else if (on(e)) if (Tt(e)) e.forEach((s) => Bk(s, t, n, r));
  else {
    const s = $t(e.handler) ? e.handler.bind(n) : t[e.handler];
    $t(s) && gt(o, s, e);
  }
}
function E0(e) {
  const t = e.type, { mixins: n, extends: r } = t, { mixins: o, optionsCache: s, config: { optionMergeStrategies: a } } = e.appContext, i = s.get(t);
  let l;
  return i ? l = i : !o.length && !n && !r ? l = t : (l = {}, o.length && o.forEach((u) => Np(l, u, a, !0)), Np(l, t, a)), on(t) && s.set(t, l), l;
}
function Np(e, t, n, r = !1) {
  const { mixins: o, extends: s } = t;
  s && Np(e, s, n, !0), o && o.forEach((a) => Np(e, a, n, !0));
  for (const a in t) if (!(r && a === "expose")) {
    const i = u7[a] || n && n[a];
    e[a] = i ? i(e[a], t[a]) : t[a];
  }
  return e;
}
var u7 = {
  data: BS,
  props: US,
  emits: US,
  methods: Nc,
  computed: Nc,
  beforeCreate: dr,
  created: dr,
  beforeMount: dr,
  mounted: dr,
  beforeUpdate: dr,
  updated: dr,
  beforeDestroy: dr,
  beforeUnmount: dr,
  destroyed: dr,
  unmounted: dr,
  activated: dr,
  deactivated: dr,
  errorCaptured: dr,
  serverPrefetch: dr,
  components: Nc,
  directives: Nc,
  watch: d7,
  provide: BS,
  inject: c7
};
function BS(e, t) {
  return t ? e ? function() {
    return On($t(e) ? e.call(this, this) : e, $t(t) ? t.call(this, this) : t);
  } : t : e;
}
function c7(e, t) {
  return Nc(o_(e), o_(t));
}
function o_(e) {
  if (Tt(e)) {
    const t = {};
    for (let n = 0; n < e.length; n++) t[e[n]] = e[n];
    return t;
  }
  return e;
}
function dr(e, t) {
  return e ? [...new Set([].concat(e, t))] : t;
}
function Nc(e, t) {
  return e ? On(/* @__PURE__ */ Object.create(null), e, t) : t;
}
function US(e, t) {
  return e ? Tt(e) && Tt(t) ? [.../* @__PURE__ */ new Set([...e, ...t])] : On(/* @__PURE__ */ Object.create(null), OS(e), OS(t ?? {})) : t;
}
function d7(e, t) {
  if (!e) return t;
  if (!t) return e;
  const n = On(/* @__PURE__ */ Object.create(null), e);
  for (const r in t) n[r] = dr(e[r], t[r]);
  return n;
}
function Uk() {
  return {
    app: null,
    config: {
      isNativeTag: ek,
      performance: !1,
      globalProperties: {},
      optionMergeStrategies: {},
      errorHandler: void 0,
      warnHandler: void 0,
      compilerOptions: {}
    },
    mixins: [],
    components: {},
    directives: {},
    provides: /* @__PURE__ */ Object.create(null),
    optionsCache: /* @__PURE__ */ new WeakMap(),
    propsCache: /* @__PURE__ */ new WeakMap(),
    emitsCache: /* @__PURE__ */ new WeakMap()
  };
}
var f7 = 0;
function h7(e, t) {
  return function(r, o = null) {
    $t(r) || (r = On({}, r)), o != null && !on(o) && (o = null);
    const s = Uk(), a = /* @__PURE__ */ new WeakSet(), i = [];
    let l = !1;
    const u = s.app = {
      _uid: f7++,
      _component: r,
      _props: o,
      _container: null,
      _context: s,
      _instance: null,
      version: G7,
      get config() {
        return s.config;
      },
      set config(d) {
      },
      use(d, ...f) {
        return a.has(d) || (d && $t(d.install) ? (a.add(d), d.install(u, ...f)) : $t(d) && (a.add(d), d(u, ...f))), u;
      },
      mixin(d) {
        return s.mixins.includes(d) || s.mixins.push(d), u;
      },
      component(d, f) {
        return f ? (s.components[d] = f, u) : s.components[d];
      },
      directive(d, f) {
        return f ? (s.directives[d] = f, u) : s.directives[d];
      },
      mount(d, f, m) {
        if (!l) {
          const v = u._ceVNode || En(r, o);
          return v.appContext = s, m === !0 ? m = "svg" : m === !1 && (m = void 0), f && t ? t(v, d) : e(v, d, m), l = !0, u._container = d, d.__vue_app__ = u, Qm(v.component);
        }
      },
      onUnmount(d) {
        i.push(d);
      },
      unmount() {
        l && (Oo(i, u._instance, 16), e(null, u._container), delete u._container.__vue_app__);
      },
      provide(d, f) {
        return s.provides[d] = f, u;
      },
      runWithContext(d) {
        const f = jl;
        jl = u;
        try {
          return d();
        } finally {
          jl = f;
        }
      }
    };
    return u;
  };
}
var jl = null, p7 = (e, t) => t === "modelValue" || t === "model-value" ? e.modelModifiers : e[`${t}Modifiers`] || e[`${Mo(t)}Modifiers`] || e[`${Wa(t)}Modifiers`];
function m7(e, t, ...n) {
  if (e.isUnmounted) return;
  const r = e.vnode.props || an;
  let o = n;
  const s = t.startsWith("update:"), a = s && p7(r, t.slice(7));
  a && (a.trim && (o = n.map((d) => _n(d) ? d.trim() : d)), a.number && (o = n.map(Wm)));
  let i, l = r[i = Lv(t)] || r[i = Lv(Mo(t))];
  !l && s && (l = r[i = Lv(Wa(t))]), l && Oo(l, e, 6, o);
  const u = r[i + "Once"];
  if (u) {
    if (!e.emitted) e.emitted = {};
    else if (e.emitted[i]) return;
    e.emitted[i] = !0, Oo(u, e, 6, o);
  }
}
var g7 = /* @__PURE__ */ new WeakMap();
function qk(e, t, n = !1) {
  const r = n ? g7 : t.emitsCache, o = r.get(e);
  if (o !== void 0) return o;
  const s = e.emits;
  let a = {}, i = !1;
  if (!$t(e)) {
    const l = (u) => {
      const d = qk(u, t, !0);
      d && (i = !0, On(a, d));
    };
    !n && t.mixins.length && t.mixins.forEach(l), e.extends && l(e.extends), e.mixins && e.mixins.forEach(l);
  }
  return !s && !i ? (on(e) && r.set(e, null), null) : (Tt(s) ? s.forEach((l) => a[l] = null) : On(a, s), on(e) && r.set(e, a), a);
}
function Ym(e, t) {
  return !e || !qm(t) ? !1 : (t = t.slice(2).replace(/Once$/, ""), tn(e, t[0].toLowerCase() + t.slice(1)) || tn(e, Wa(t)) || tn(e, t));
}
function zv(e) {
  const { type: t, vnode: n, proxy: r, withProxy: o, propsOptions: [s], slots: a, attrs: i, emit: l, render: u, renderCache: d, props: f, data: m, setupState: v, ctx: y, inheritAttrs: T } = e, C = Pp(e);
  let A, I;
  try {
    if (n.shapeFlag & 4) {
      const $ = o || r, p = $;
      A = ns(u.call(p, $, d, f, v, m, y)), I = i;
    } else {
      const $ = t;
      A = ns($.length > 1 ? $(f, {
        attrs: i,
        slots: a,
        emit: l
      }) : $(f, null)), I = t.props ? i : v7(i);
    }
  } catch ($) {
    ud.length = 0, Km($, e, 1), A = En(La);
  }
  let R = A;
  if (I && T !== !1) {
    const $ = Object.keys(I), { shapeFlag: p } = R;
    $.length && p & 7 && (s && $.some(Hm) && (I = y7(I, s)), R = du(R, I, !1, !0));
  }
  return n.dirs && (R = du(R, null, !1, !0), R.dirs = R.dirs ? R.dirs.concat(n.dirs) : n.dirs), n.transition && S0(R, n.transition), A = R, Pp(C), A;
}
var v7 = (e) => {
  let t;
  for (const n in e) (n === "class" || n === "style" || qm(n)) && ((t || (t = {}))[n] = e[n]);
  return t;
}, y7 = (e, t) => {
  const n = {};
  for (const r in e) (!Hm(r) || !(r.slice(9) in t)) && (n[r] = e[r]);
  return n;
};
function _7(e, t, n) {
  const { props: r, children: o, component: s } = e, { props: a, children: i, patchFlag: l } = t, u = s.emitsOptions;
  if (t.dirs || t.transition) return !0;
  if (n && l >= 0) {
    if (l & 1024) return !0;
    if (l & 16)
      return r ? qS(r, a, u) : !!a;
    if (l & 8) {
      const d = t.dynamicProps;
      for (let f = 0; f < d.length; f++) {
        const m = d[f];
        if (Hk(a, r, m) && !Ym(u, m)) return !0;
      }
    }
  } else
    return (o || i) && (!i || !i.$stable) ? !0 : r === a ? !1 : r ? a ? qS(r, a, u) : !0 : !!a;
  return !1;
}
function qS(e, t, n) {
  const r = Object.keys(t);
  if (r.length !== Object.keys(e).length) return !0;
  for (let o = 0; o < r.length; o++) {
    const s = r[o];
    if (Hk(t, e, s) && !Ym(n, s)) return !0;
  }
  return !1;
}
function Hk(e, t, n) {
  const r = e[n], o = t[n];
  return n === "style" && on(r) && on(o) ? !Cu(r, o) : r !== o;
}
function b7({ vnode: e, parent: t, suspense: n }, r) {
  for (; t; ) {
    const o = t.subTree;
    if (o.suspense && o.suspense.activeBranch === e && (o.suspense.vnode.el = o.el = r, e = o), o === e)
      (e = t.vnode).el = r, t = t.parent;
    else break;
  }
  n && n.activeBranch === e && (n.vnode.el = r);
}
var zk = {}, Wk = () => Object.create(zk), Gk = (e) => Object.getPrototypeOf(e) === zk;
function w7(e, t, n, r = !1) {
  const o = {}, s = Wk();
  e.propsDefaults = /* @__PURE__ */ Object.create(null), Vk(e, t, o, s);
  for (const a in e.propsOptions[0]) a in o || (o[a] = void 0);
  n ? e.props = r ? o : /* @__PURE__ */ NN(o) : e.type.props ? e.props = o : e.props = s, e.attrs = s;
}
function S7(e, t, n, r) {
  const { props: o, attrs: s, vnode: { patchFlag: a } } = e, i = /* @__PURE__ */ en(o), [l] = e.propsOptions;
  let u = !1;
  if ((r || a > 0) && !(a & 16)) {
    if (a & 8) {
      const d = e.vnode.dynamicProps;
      for (let f = 0; f < d.length; f++) {
        let m = d[f];
        if (Ym(e.emitsOptions, m)) continue;
        const v = t[m];
        if (l) if (tn(s, m))
          v !== s[m] && (s[m] = v, u = !0);
        else {
          const y = Mo(m);
          o[y] = s_(l, i, y, v, e, !1);
        }
        else v !== s[m] && (s[m] = v, u = !0);
      }
    }
  } else {
    Vk(e, t, o, s) && (u = !0);
    let d;
    for (const f in i) (!t || !tn(t, f) && ((d = Wa(f)) === f || !tn(t, d))) && (l ? n && (n[f] !== void 0 || n[d] !== void 0) && (o[f] = s_(l, i, f, void 0, e, !0)) : delete o[f]);
    if (s !== i)
      for (const f in s) (!t || !tn(t, f)) && (delete s[f], u = !0);
  }
  u && Os(e.attrs, "set", "");
}
function Vk(e, t, n, r) {
  const [o, s] = e.propsOptions;
  let a = !1, i;
  if (t) for (let l in t) {
    if (nd(l)) continue;
    const u = t[l];
    let d;
    o && tn(o, d = Mo(l)) ? !s || !s.includes(d) ? n[d] = u : (i || (i = {}))[d] = u : Ym(e.emitsOptions, l) || (!(l in r) || u !== r[l]) && (r[l] = u, a = !0);
  }
  if (s) {
    const l = /* @__PURE__ */ en(n), u = i || an;
    for (let d = 0; d < s.length; d++) {
      const f = s[d];
      n[f] = s_(o, l, f, u[f], e, !tn(u, f));
    }
  }
  return a;
}
function s_(e, t, n, r, o, s) {
  const a = e[n];
  if (a != null) {
    const i = tn(a, "default");
    if (i && r === void 0) {
      const l = a.default;
      if (a.type !== Function && !a.skipFactory && $t(l)) {
        const { propsDefaults: u } = o;
        if (n in u) r = u[n];
        else {
          const d = tf(o);
          r = u[n] = l.call(null, t), d();
        }
      } else r = l;
      o.ce && o.ce._setProp(n, r);
    }
    a[0] && (s && !i ? r = !1 : a[1] && (r === "" || r === Wa(n)) && (r = !0));
  }
  return r;
}
var T7 = /* @__PURE__ */ new WeakMap();
function Kk(e, t, n = !1) {
  const r = n ? T7 : t.propsCache, o = r.get(e);
  if (o) return o;
  const s = e.props, a = {}, i = [];
  let l = !1;
  if (!$t(e)) {
    const d = (f) => {
      l = !0;
      const [m, v] = Kk(f, t, !0);
      On(a, m), v && i.push(...v);
    };
    !n && t.mixins.length && t.mixins.forEach(d), e.extends && d(e.extends), e.mixins && e.mixins.forEach(d);
  }
  if (!s && !l)
    return on(e) && r.set(e, Xl), Xl;
  if (Tt(s)) for (let d = 0; d < s.length; d++) {
    const f = Mo(s[d]);
    HS(f) && (a[f] = an);
  }
  else if (s) for (const d in s) {
    const f = Mo(d);
    if (HS(f)) {
      const m = s[d], v = a[f] = Tt(m) || $t(m) ? { type: m } : On({}, m), y = v.type;
      let T = !1, C = !0;
      if (Tt(y)) for (let A = 0; A < y.length; ++A) {
        const I = y[A], R = $t(I) && I.name;
        if (R === "Boolean") {
          T = !0;
          break;
        } else R === "String" && (C = !1);
      }
      else T = $t(y) && y.name === "Boolean";
      v[0] = T, v[1] = C, (T || tn(v, "default")) && i.push(f);
    }
  }
  const u = [a, i];
  return on(e) && r.set(e, u), u;
}
function HS(e) {
  return e[0] !== "$" && !nd(e);
}
var A0 = (e) => e === "_" || e === "_ctx" || e === "$stable", C0 = (e) => Tt(e) ? e.map(ns) : [ns(e)], E7 = (e, t, n) => {
  if (t._n) return t;
  const r = GN((...o) => C0(t(...o)), n);
  return r._c = !1, r;
}, Jk = (e, t, n) => {
  const r = e._ctx;
  for (const o in e) {
    if (A0(o)) continue;
    const s = e[o];
    if ($t(s)) t[o] = E7(o, s, r);
    else if (s != null) {
      const a = C0(s);
      t[o] = () => a;
    }
  }
}, Yk = (e, t) => {
  const n = C0(t);
  e.slots.default = () => n;
}, Xk = (e, t, n) => {
  for (const r in t) (n || !A0(r)) && (e[r] = t[r]);
}, A7 = (e, t, n) => {
  const r = e.slots = Wk();
  if (e.vnode.shapeFlag & 32) {
    const o = t._;
    o ? (Xk(r, t, n), n && sk(r, "_", o, !0)) : Jk(t, r);
  } else t && Yk(e, t);
}, C7 = (e, t, n) => {
  const { vnode: r, slots: o } = e;
  let s = !0, a = an;
  if (r.shapeFlag & 32) {
    const i = t._;
    i ? n && i === 1 ? s = !1 : Xk(o, t, n) : (s = !t.$stable, Jk(t, o)), a = t;
  } else t && (Yk(e, t), a = { default: 1 });
  if (s)
    for (const i in o) !A0(i) && a[i] == null && delete o[i];
};
var Tr = P7;
function k7(e) {
  return I7(e);
}
function I7(e, t) {
  const n = Gm();
  n.__VUE__ = !0;
  const { insert: r, remove: o, patchProp: s, createElement: a, createText: i, createComment: l, setText: u, setElementText: d, parentNode: f, nextSibling: m, setScopeId: v = as, insertStaticContent: y } = e, T = (Y, te, he, we = null, Te = null, Ie = null, Ne = void 0, Oe = null, L = !!te.dynamicChildren) => {
    if (Y === te) return;
    Y && !sc(Y, te) && (we = H(Y), Ce(Y, Te, Ie, !0), Y = null), te.patchFlag === -2 && (L = !1, te.dynamicChildren = null);
    const { type: P, ref: Q, shapeFlag: V } = te;
    switch (P) {
      case Xm:
        C(Y, te, he, we);
        break;
      case La:
        A(Y, te, he, we);
        break;
      case Xh:
        Y == null && I(te, he, we, Ne);
        break;
      case Je:
        q(Y, te, he, we, Te, Ie, Ne, Oe, L);
        break;
      default:
        V & 1 ? p(Y, te, he, we, Te, Ie, Ne, Oe, L) : V & 6 ? z(Y, te, he, we, Te, Ie, Ne, Oe, L) : (V & 64 || V & 128) && P.process(Y, te, he, we, Te, Ie, Ne, Oe, L, ke);
    }
    Q != null && Te ? ad(Q, Y && Y.ref, Ie, te || Y, !te) : Q == null && Y && Y.ref != null && ad(Y.ref, null, Ie, Y, !0);
  }, C = (Y, te, he, we) => {
    if (Y == null) r(te.el = i(te.children), he, we);
    else {
      const Te = te.el = Y.el;
      te.children !== Y.children && u(Te, te.children);
    }
  }, A = (Y, te, he, we) => {
    Y == null ? r(te.el = l(te.children || ""), he, we) : te.el = Y.el;
  }, I = (Y, te, he, we) => {
    [Y.el, Y.anchor] = y(Y.children, te, he, we, Y.el, Y.anchor);
  }, R = ({ el: Y, anchor: te }, he, we) => {
    let Te;
    for (; Y && Y !== te; )
      Te = m(Y), r(Y, he, we), Y = Te;
    r(te, he, we);
  }, $ = ({ el: Y, anchor: te }) => {
    let he;
    for (; Y && Y !== te; )
      he = m(Y), o(Y), Y = he;
    o(te);
  }, p = (Y, te, he, we, Te, Ie, Ne, Oe, L) => {
    if (te.type === "svg" ? Ne = "svg" : te.type === "math" && (Ne = "mathml"), Y == null) b(te, he, we, Te, Ie, Ne, Oe, L);
    else {
      const P = Y.el && Y.el._isVueCE ? Y.el : null;
      try {
        P && P._beginPatch(), x(Y, te, Te, Ie, Ne, Oe, L);
      } finally {
        P && P._endPatch();
      }
    }
  }, b = (Y, te, he, we, Te, Ie, Ne, Oe) => {
    let L, P;
    const { props: Q, shapeFlag: V, transition: ae, dirs: Se } = Y;
    if (L = Y.el = a(Y.type, Ie, Q && Q.is, Q), V & 8 ? d(L, Y.children) : V & 16 && E(Y.children, L, null, we, Te, Wv(Y, Ie), Ne, Oe), Se && ui(Y, null, we, "created"), w(L, Y, Y.scopeId, Ne, we), Q) {
      for (const Ke in Q) Ke !== "value" && !nd(Ke) && s(L, Ke, null, Q[Ke], Ie, we);
      "value" in Q && s(L, "value", null, Q.value, Ie), (P = Q.onVnodeBeforeMount) && Yo(P, we, Y);
    }
    Se && ui(Y, null, we, "beforeMount");
    const Me = x7(Te, ae);
    Me && ae.beforeEnter(L), r(L, te, he), ((P = Q && Q.onVnodeMounted) || Me || Se) && Tr(() => {
      P && Yo(P, we, Y), Me && ae.enter(L), Se && ui(Y, null, we, "mounted");
    }, Te);
  }, w = (Y, te, he, we, Te) => {
    if (he && v(Y, he), we) for (let Ie = 0; Ie < we.length; Ie++) v(Y, we[Ie]);
    if (Te) {
      let Ie = Te.subTree;
      if (te === Ie || eI(Ie.type) && (Ie.ssContent === te || Ie.ssFallback === te)) {
        const Ne = Te.vnode;
        w(Y, Ne, Ne.scopeId, Ne.slotScopeIds, Te.parent);
      }
    }
  }, E = (Y, te, he, we, Te, Ie, Ne, Oe, L = 0) => {
    for (let P = L; P < Y.length; P++) T(null, Y[P] = Oe ? Ds(Y[P]) : ns(Y[P]), te, he, we, Te, Ie, Ne, Oe);
  }, x = (Y, te, he, we, Te, Ie, Ne) => {
    const Oe = te.el = Y.el;
    let { patchFlag: L, dynamicChildren: P, dirs: Q } = te;
    L |= Y.patchFlag & 16;
    const V = Y.props || an, ae = te.props || an;
    let Se;
    if (he && ci(he, !1), (Se = ae.onVnodeBeforeUpdate) && Yo(Se, he, te, Y), Q && ui(te, Y, he, "beforeUpdate"), he && ci(he, !0), (V.innerHTML && ae.innerHTML == null || V.textContent && ae.textContent == null) && d(Oe, ""), P ? M(Y.dynamicChildren, P, Oe, he, we, Wv(te, Te), Ie) : Ne || X(Y, te, Oe, null, he, we, Wv(te, Te), Ie, !1), L > 0) {
      if (L & 16) N(Oe, V, ae, he, Te);
      else if (L & 2 && V.class !== ae.class && s(Oe, "class", null, ae.class, Te), L & 4 && s(Oe, "style", V.style, ae.style, Te), L & 8) {
        const Me = te.dynamicProps;
        for (let Ke = 0; Ke < Me.length; Ke++) {
          const qe = Me[Ke], Be = V[qe], Ee = ae[qe];
          (Ee !== Be || qe === "value") && s(Oe, qe, Be, Ee, Te, he);
        }
      }
      L & 1 && Y.children !== te.children && d(Oe, te.children);
    } else !Ne && P == null && N(Oe, V, ae, he, Te);
    ((Se = ae.onVnodeUpdated) || Q) && Tr(() => {
      Se && Yo(Se, he, te, Y), Q && ui(te, Y, he, "updated");
    }, we);
  }, M = (Y, te, he, we, Te, Ie, Ne) => {
    for (let Oe = 0; Oe < te.length; Oe++) {
      const L = Y[Oe], P = te[Oe];
      T(L, P, L.el && (L.type === Je || !sc(L, P) || L.shapeFlag & 198) ? f(L.el) : he, null, we, Te, Ie, Ne, !0);
    }
  }, N = (Y, te, he, we, Te) => {
    if (te !== he) {
      if (te !== an)
        for (const Ie in te) !nd(Ie) && !(Ie in he) && s(Y, Ie, te[Ie], null, Te, we);
      for (const Ie in he) {
        if (nd(Ie)) continue;
        const Ne = he[Ie], Oe = te[Ie];
        Ne !== Oe && Ie !== "value" && s(Y, Ie, Oe, Ne, Te, we);
      }
      "value" in he && s(Y, "value", te.value, he.value, Te);
    }
  }, q = (Y, te, he, we, Te, Ie, Ne, Oe, L) => {
    const P = te.el = Y ? Y.el : i(""), Q = te.anchor = Y ? Y.anchor : i("");
    let { patchFlag: V, dynamicChildren: ae, slotScopeIds: Se } = te;
    Se && (Oe = Oe ? Oe.concat(Se) : Se), Y == null ? (r(P, he, we), r(Q, he, we), E(te.children || [], he, Q, Te, Ie, Ne, Oe, L)) : V > 0 && V & 64 && ae && Y.dynamicChildren && Y.dynamicChildren.length === ae.length ? (M(Y.dynamicChildren, ae, he, Te, Ie, Ne, Oe), (te.key != null || Te && te === Te.subTree) && Qk(Y, te, !0)) : X(Y, te, he, Q, Te, Ie, Ne, Oe, L);
  }, z = (Y, te, he, we, Te, Ie, Ne, Oe, L) => {
    te.slotScopeIds = Oe, Y == null ? te.shapeFlag & 512 ? Te.ctx.activate(te, he, we, Ne, L) : J(te, he, we, Te, Ie, Ne, L) : G(Y, te, L);
  }, J = (Y, te, he, we, Te, Ie, Ne) => {
    const Oe = Y.component = F7(Y, we, Te);
    if ($k(Y) && (Oe.ctx.renderer = ke), U7(Oe, !1, Ne), Oe.asyncDep) {
      if (Te && Te.registerDep(Oe, oe, Ne), !Y.el) {
        const L = Oe.subTree = En(La);
        A(null, L, te, he), Y.placeholder = L.el;
      }
    } else oe(Oe, Y, te, he, Te, Ie, Ne);
  }, G = (Y, te, he) => {
    const we = te.component = Y.component;
    if (_7(Y, te, he)) if (we.asyncDep && !we.asyncResolved) {
      B(we, te, he);
      return;
    } else
      we.next = te, we.update();
    else
      te.el = Y.el, we.vnode = te;
  }, oe = (Y, te, he, we, Te, Ie, Ne) => {
    const Oe = () => {
      if (Y.isMounted) {
        let { next: V, bu: ae, u: Se, parent: Me, vnode: Ke } = Y;
        {
          const je = Zk(Y);
          if (je) {
            V && (V.el = Ke.el, B(Y, V, Ne)), je.asyncDep.then(() => {
              Tr(() => {
                Y.isUnmounted || P();
              }, Te);
            });
            return;
          }
        }
        let qe = V, Be;
        ci(Y, !1), V ? (V.el = Ke.el, B(Y, V, Ne)) : V = Ke, ae && Yh(ae), (Be = V.props && V.props.onVnodeBeforeUpdate) && Yo(Be, Me, V, Ke), ci(Y, !0);
        const Ee = zv(Y), Ye = Y.subTree;
        Y.subTree = Ee, T(Ye, Ee, f(Ye.el), H(Ye), Y, Te, Ie), V.el = Ee.el, qe === null && b7(Y, Ee.el), Se && Tr(Se, Te), (Be = V.props && V.props.onVnodeUpdated) && Tr(() => Yo(Be, Me, V, Ke), Te);
      } else {
        let V;
        const { el: ae, props: Se } = te, { bm: Me, m: Ke, parent: qe, root: Be, type: Ee } = Y, Ye = id(te);
        if (ci(Y, !1), Me && Yh(Me), !Ye && (V = Se && Se.onVnodeBeforeMount) && Yo(V, qe, te), ci(Y, !0), ae && Le) {
          const je = () => {
            Y.subTree = zv(Y), Le(ae, Y.subTree, Y, Te, null);
          };
          Ye && Ee.__asyncHydrate ? Ee.__asyncHydrate(ae, Y, je) : je();
        } else {
          Be.ce && Be.ce._hasShadowRoot() && Be.ce._injectChildStyle(Ee, Y.parent ? Y.parent.type : void 0);
          const je = Y.subTree = zv(Y);
          T(null, je, he, we, Y, Te, Ie), te.el = je.el;
        }
        if (Ke && Tr(Ke, Te), !Ye && (V = Se && Se.onVnodeMounted)) {
          const je = te;
          Tr(() => Yo(V, qe, je), Te);
        }
        (te.shapeFlag & 256 || qe && id(qe.vnode) && qe.vnode.shapeFlag & 256) && Y.a && Tr(Y.a, Te), Y.isMounted = !0, te = he = we = null;
      }
    };
    Y.scope.on();
    const L = Y.effect = new ck(Oe);
    Y.scope.off();
    const P = Y.update = L.run.bind(L), Q = Y.job = L.runIfDirty.bind(L);
    Q.i = Y, Q.id = Y.uid, L.scheduler = () => w0(Q), ci(Y, !0), P();
  }, B = (Y, te, he) => {
    te.component = Y;
    const we = Y.vnode.props;
    Y.vnode = te, Y.next = null, S7(Y, te.props, we, he), C7(Y, te.children, he), Xs(), $S(Y), Qs();
  }, X = (Y, te, he, we, Te, Ie, Ne, Oe, L = !1) => {
    const P = Y && Y.children, Q = Y ? Y.shapeFlag : 0, V = te.children, { patchFlag: ae, shapeFlag: Se } = te;
    if (ae > 0) {
      if (ae & 128) {
        fe(P, V, he, we, Te, Ie, Ne, Oe, L);
        return;
      } else if (ae & 256) {
        de(P, V, he, we, Te, Ie, Ne, Oe, L);
        return;
      }
    }
    Se & 8 ? (Q & 16 && We(P, Te, Ie), V !== P && d(he, V)) : Q & 16 ? Se & 16 ? fe(P, V, he, we, Te, Ie, Ne, Oe, L) : We(P, Te, Ie, !0) : (Q & 8 && d(he, ""), Se & 16 && E(V, he, we, Te, Ie, Ne, Oe, L));
  }, de = (Y, te, he, we, Te, Ie, Ne, Oe, L) => {
    Y = Y || Xl, te = te || Xl;
    const P = Y.length, Q = te.length, V = Math.min(P, Q);
    let ae;
    for (ae = 0; ae < V; ae++) {
      const Se = te[ae] = L ? Ds(te[ae]) : ns(te[ae]);
      T(Y[ae], Se, he, null, Te, Ie, Ne, Oe, L);
    }
    P > Q ? We(Y, Te, Ie, !0, !1, V) : E(te, he, we, Te, Ie, Ne, Oe, L, V);
  }, fe = (Y, te, he, we, Te, Ie, Ne, Oe, L) => {
    let P = 0;
    const Q = te.length;
    let V = Y.length - 1, ae = Q - 1;
    for (; P <= V && P <= ae; ) {
      const Se = Y[P], Me = te[P] = L ? Ds(te[P]) : ns(te[P]);
      if (sc(Se, Me)) T(Se, Me, he, null, Te, Ie, Ne, Oe, L);
      else break;
      P++;
    }
    for (; P <= V && P <= ae; ) {
      const Se = Y[V], Me = te[ae] = L ? Ds(te[ae]) : ns(te[ae]);
      if (sc(Se, Me)) T(Se, Me, he, null, Te, Ie, Ne, Oe, L);
      else break;
      V--, ae--;
    }
    if (P > V) {
      if (P <= ae) {
        const Se = ae + 1, Me = Se < Q ? te[Se].el : we;
        for (; P <= ae; )
          T(null, te[P] = L ? Ds(te[P]) : ns(te[P]), he, Me, Te, Ie, Ne, Oe, L), P++;
      }
    } else if (P > ae) for (; P <= V; )
      Ce(Y[P], Te, Ie, !0), P++;
    else {
      const Se = P, Me = P, Ke = /* @__PURE__ */ new Map();
      for (P = Me; P <= ae; P++) {
        const At = te[P] = L ? Ds(te[P]) : ns(te[P]);
        At.key != null && Ke.set(At.key, P);
      }
      let qe, Be = 0;
      const Ee = ae - Me + 1;
      let Ye = !1, je = 0;
      const It = new Array(Ee);
      for (P = 0; P < Ee; P++) It[P] = 0;
      for (P = Se; P <= V; P++) {
        const At = Y[P];
        if (Be >= Ee) {
          Ce(At, Te, Ie, !0);
          continue;
        }
        let Mt;
        if (At.key != null) Mt = Ke.get(At.key);
        else for (qe = Me; qe <= ae; qe++) if (It[qe - Me] === 0 && sc(At, te[qe])) {
          Mt = qe;
          break;
        }
        Mt === void 0 ? Ce(At, Te, Ie, !0) : (It[Mt - Me] = P + 1, Mt >= je ? je = Mt : Ye = !0, T(At, te[Mt], he, null, Te, Ie, Ne, Oe, L), Be++);
      }
      const Ht = Ye ? M7(It) : Xl;
      for (qe = Ht.length - 1, P = Ee - 1; P >= 0; P--) {
        const At = Me + P, Mt = te[At], Ut = te[At + 1], yt = At + 1 < Q ? Ut.el || jk(Ut) : we;
        It[P] === 0 ? T(null, Mt, he, yt, Te, Ie, Ne, Oe, L) : Ye && (qe < 0 || P !== Ht[qe] ? ve(Mt, he, yt, 2) : qe--);
      }
    }
  }, ve = (Y, te, he, we, Te = null) => {
    const { el: Ie, type: Ne, transition: Oe, children: L, shapeFlag: P } = Y;
    if (P & 6) {
      ve(Y.component.subTree, te, he, we);
      return;
    }
    if (P & 128) {
      Y.suspense.move(te, he, we);
      return;
    }
    if (P & 64) {
      Ne.move(Y, te, he, ke);
      return;
    }
    if (Ne === Je) {
      r(Ie, te, he);
      for (let Q = 0; Q < L.length; Q++) ve(L[Q], te, he, we);
      r(Y.anchor, te, he);
      return;
    }
    if (Ne === Xh) {
      R(Y, te, he);
      return;
    }
    if (we !== 2 && P & 1 && Oe) if (we === 0) Oe.persisted && !Ie[qv] ? r(Ie, te, he) : (Oe.beforeEnter(Ie), r(Ie, te, he), Tr(() => Oe.enter(Ie), Te));
    else {
      const { leave: Q, delayLeave: V, afterLeave: ae } = Oe, Se = () => {
        Y.ctx.isUnmounted ? o(Ie) : r(Ie, te, he);
      }, Me = () => {
        const Ke = Ie._isLeaving || !!Ie[qv];
        Ie._isLeaving && Ie[qv](!0), Oe.persisted && !Ke ? Se() : Q(Ie, () => {
          Se(), ae && ae();
        });
      };
      V ? V(Ie, Se, Me) : Me();
    }
    else r(Ie, te, he);
  }, Ce = (Y, te, he, we = !1, Te = !1) => {
    const { type: Ie, props: Ne, ref: Oe, children: L, dynamicChildren: P, shapeFlag: Q, patchFlag: V, dirs: ae, cacheIndex: Se, memo: Me } = Y;
    if (V === -2 && (Te = !1), Oe != null && (Xs(), ad(Oe, null, he, Y, !0), Qs()), Se != null && (te.renderCache[Se] = void 0), Q & 256) {
      te.ctx.deactivate(Y);
      return;
    }
    const Ke = Q & 1 && ae, qe = !id(Y);
    let Be;
    if (qe && (Be = Ne && Ne.onVnodeBeforeUnmount) && Yo(Be, te, Y), Q & 6) me(Y.component, he, we);
    else {
      if (Q & 128) {
        Y.suspense.unmount(he, we);
        return;
      }
      Ke && ui(Y, null, te, "beforeUnmount"), Q & 64 ? Y.type.remove(Y, te, he, ke, we) : P && !P.hasOnce && (Ie !== Je || V > 0 && V & 64) ? We(P, te, he, !1, !0) : (Ie === Je && V & 384 || !Te && Q & 16) && We(L, te, he), we && Ue(Y);
    }
    const Ee = Me != null && Se == null;
    (qe && (Be = Ne && Ne.onVnodeUnmounted) || Ke || Ee) && Tr(() => {
      Be && Yo(Be, te, Y), Ke && ui(Y, null, te, "unmounted"), Ee && (Y.el = null);
    }, he);
  }, Ue = (Y) => {
    const { type: te, el: he, anchor: we, transition: Te } = Y;
    if (te === Je) {
      le(he, we);
      return;
    }
    if (te === Xh) {
      $(Y);
      return;
    }
    const Ie = () => {
      o(he), Te && !Te.persisted && Te.afterLeave && Te.afterLeave();
    };
    if (Y.shapeFlag & 1 && Te && !Te.persisted) {
      const { leave: Ne, delayLeave: Oe } = Te, L = () => Ne(he, Ie);
      Oe ? Oe(Y.el, Ie, L) : L();
    } else Ie();
  }, le = (Y, te) => {
    let he;
    for (; Y !== te; )
      he = m(Y), o(Y), Y = he;
    o(te);
  }, me = (Y, te, he) => {
    const { bum: we, scope: Te, job: Ie, subTree: Ne, um: Oe, m: L, a: P } = Y;
    zS(L), zS(P), we && Yh(we), Te.stop(), Ie && (Ie.flags |= 8, Ce(Ne, Y, te, he)), Oe && Tr(Oe, te), Tr(() => {
      Y.isUnmounted = !0;
    }, te);
  }, We = (Y, te, he, we = !1, Te = !1, Ie = 0) => {
    for (let Ne = Ie; Ne < Y.length; Ne++) Ce(Y[Ne], te, he, we, Te);
  }, H = (Y) => {
    if (Y.shapeFlag & 6) return H(Y.component.subTree);
    if (Y.shapeFlag & 128) return Y.suspense.next();
    const te = m(Y.anchor || Y.el), he = te && te[YN];
    return he ? m(he) : te;
  };
  let W = !1;
  const se = (Y, te, he) => {
    let we;
    Y == null ? te._vnode && (Ce(te._vnode, null, null, !0), we = te._vnode.component) : T(te._vnode || null, Y, te, null, null, null, he), te._vnode = Y, W || (W = !0, $S(we), kk(), W = !1);
  }, ke = {
    p: T,
    um: Ce,
    m: ve,
    r: Ue,
    mt: J,
    mc: E,
    pc: X,
    pbc: M,
    n: H,
    o: e
  };
  let Pe, Le;
  return t && ([Pe, Le] = t(ke)), {
    render: se,
    hydrate: Pe,
    createApp: h7(se, Pe)
  };
}
function Wv({ type: e, props: t }, n) {
  return n === "svg" && e === "foreignObject" || n === "mathml" && e === "annotation-xml" && t && t.encoding && t.encoding.includes("html") ? void 0 : n;
}
function ci({ effect: e, job: t }, n) {
  n ? (e.flags |= 32, t.flags |= 4) : (e.flags &= -33, t.flags &= -5);
}
function x7(e, t) {
  return (!e || e && !e.pendingBranch) && t && !t.persisted;
}
function Qk(e, t, n = !1) {
  const r = e.children, o = t.children;
  if (Tt(r) && Tt(o)) for (let s = 0; s < r.length; s++) {
    const a = r[s];
    let i = o[s];
    i.shapeFlag & 1 && !i.dynamicChildren && ((i.patchFlag <= 0 || i.patchFlag === 32) && (i = o[s] = Ds(o[s]), i.el = a.el), !n && i.patchFlag !== -2 && Qk(a, i)), i.type === Xm && (i.patchFlag === -1 && (i = o[s] = Ds(i)), i.el = a.el), i.type === La && !i.el && (i.el = a.el);
  }
}
function M7(e) {
  const t = e.slice(), n = [0];
  let r, o, s, a, i;
  const l = e.length;
  for (r = 0; r < l; r++) {
    const u = e[r];
    if (u !== 0) {
      if (o = n[n.length - 1], e[o] < u) {
        t[r] = o, n.push(r);
        continue;
      }
      for (s = 0, a = n.length - 1; s < a; )
        i = s + a >> 1, e[n[i]] < u ? s = i + 1 : a = i;
      u < e[n[s]] && (s > 0 && (t[r] = n[s - 1]), n[s] = r);
    }
  }
  for (s = n.length, a = n[s - 1]; s-- > 0; )
    n[s] = a, a = t[a];
  return n;
}
function Zk(e) {
  const t = e.subTree.component;
  if (t) return t.asyncDep && !t.asyncResolved ? t : Zk(t);
}
function zS(e) {
  if (e) for (let t = 0; t < e.length; t++) e[t].flags |= 8;
}
function jk(e) {
  if (e.placeholder) return e.placeholder;
  const t = e.component;
  return t ? jk(t.subTree) : null;
}
var eI = (e) => e.__isSuspense;
function P7(e, t) {
  t && t.pendingBranch ? Tt(e) ? t.effects.push(...e) : t.effects.push(e) : WN(e);
}
var Je = /* @__PURE__ */ Symbol.for("v-fgt"), Xm = /* @__PURE__ */ Symbol.for("v-txt"), La = /* @__PURE__ */ Symbol.for("v-cmt"), Xh = /* @__PURE__ */ Symbol.for("v-stc"), ud = [], Qr = null;
function O(e = !1) {
  ud.push(Qr = e ? null : []);
}
function R7() {
  ud.pop(), Qr = ud[ud.length - 1] || null;
}
var kd = 1;
function WS(e, t = !1) {
  kd += e, e < 0 && Qr && t && (Qr.hasOnce = !0);
}
function tI(e) {
  return e.dynamicChildren = kd > 0 ? Qr || Xl : null, R7(), kd > 0 && Qr && Qr.push(e), e;
}
function F(e, t, n, r, o, s) {
  return tI(g(e, t, n, r, o, s, !0));
}
function gn(e, t, n, r, o) {
  return tI(En(e, t, n, r, o, !0));
}
function nI(e) {
  return e ? e.__v_isVNode === !0 : !1;
}
function sc(e, t) {
  return e.type === t.type && e.key === t.key;
}
var rI = ({ key: e }) => e ?? null, Qh = ({ ref: e, ref_key: t, ref_for: n }) => (typeof e == "number" && (e = "" + e), e != null ? _n(e) || /* @__PURE__ */ pn(e) || $t(e) ? {
  i: po,
  r: e,
  k: t,
  f: !!n
} : e : null);
function g(e, t = null, n = null, r = 0, o = null, s = e === Je ? 0 : 1, a = !1, i = !1) {
  const l = {
    __v_isVNode: !0,
    __v_skip: !0,
    type: e,
    props: t,
    key: t && rI(t),
    ref: t && Qh(t),
    scopeId: xk,
    slotScopeIds: null,
    children: n,
    component: null,
    suspense: null,
    ssContent: null,
    ssFallback: null,
    dirs: null,
    transition: null,
    el: null,
    anchor: null,
    target: null,
    targetStart: null,
    targetAnchor: null,
    staticCount: 0,
    shapeFlag: s,
    patchFlag: r,
    dynamicProps: o,
    dynamicChildren: null,
    appContext: null,
    ctx: po
  };
  return i ? (k0(l, n), s & 128 && e.normalize(l)) : n && (l.shapeFlag |= _n(n) ? 8 : 16), kd > 0 && !a && Qr && (l.patchFlag > 0 || s & 6) && l.patchFlag !== 32 && Qr.push(l), l;
}
var En = N7;
function N7(e, t = null, n = null, r = 0, o = null, s = !1) {
  if ((!e || e === s7) && (e = La), nI(e)) {
    const i = du(e, t, !0);
    return n && k0(i, n), kd > 0 && !s && Qr && (i.shapeFlag & 6 ? Qr[Qr.indexOf(e)] = i : Qr.push(i)), i.patchFlag = -2, i;
  }
  if (W7(e) && (e = e.__vccOpts), t) {
    t = $7(t);
    let { class: i, style: l } = t;
    i && !_n(i) && (t.class = ze(i)), on(l) && (/* @__PURE__ */ b0(l) && !Tt(l) && (l = On({}, l)), t.style = Jr(l));
  }
  const a = _n(e) ? 1 : eI(e) ? 128 : XN(e) ? 64 : on(e) ? 4 : $t(e) ? 2 : 0;
  return g(e, t, n, r, o, a, s, !0);
}
function $7(e) {
  return e ? /* @__PURE__ */ b0(e) || Gk(e) ? On({}, e) : e : null;
}
function du(e, t, n = !1, r = !1) {
  const { props: o, ref: s, patchFlag: a, children: i, transition: l } = e, u = t ? D7(o || {}, t) : o, d = {
    __v_isVNode: !0,
    __v_skip: !0,
    type: e.type,
    props: u,
    key: u && rI(u),
    ref: t && t.ref ? n && s ? Tt(s) ? s.concat(Qh(t)) : [s, Qh(t)] : Qh(t) : s,
    scopeId: e.scopeId,
    slotScopeIds: e.slotScopeIds,
    children: i,
    target: e.target,
    targetStart: e.targetStart,
    targetAnchor: e.targetAnchor,
    staticCount: e.staticCount,
    shapeFlag: e.shapeFlag,
    patchFlag: t && e.type !== Je ? a === -1 ? 16 : a | 16 : a,
    dynamicProps: e.dynamicProps,
    dynamicChildren: e.dynamicChildren,
    appContext: e.appContext,
    dirs: e.dirs,
    transition: l,
    component: e.component,
    suspense: e.suspense,
    ssContent: e.ssContent && du(e.ssContent),
    ssFallback: e.ssFallback && du(e.ssFallback),
    placeholder: e.placeholder,
    el: e.el,
    anchor: e.anchor,
    ctx: e.ctx,
    ce: e.ce
  };
  return l && r && S0(d, l.clone(d)), d;
}
function Bs(e = " ", t = 0) {
  return En(Xm, null, e, t);
}
function ku(e, t) {
  const n = En(Xh, null, e);
  return n.staticCount = t, n;
}
function ge(e = "", t = !1) {
  return t ? (O(), gn(La, null, e)) : En(La, null, e);
}
function ns(e) {
  return e == null || typeof e == "boolean" ? En(La) : Tt(e) ? En(Je, null, e.slice()) : nI(e) ? Ds(e) : En(Xm, null, String(e));
}
function Ds(e) {
  return e.el === null && e.patchFlag !== -1 || e.memo ? e : du(e);
}
function k0(e, t) {
  let n = 0;
  const { shapeFlag: r } = e;
  if (t == null) t = null;
  else if (Tt(t)) n = 16;
  else if (typeof t == "object") if (r & 65) {
    const o = t.default;
    o && (o._c && (o._d = !1), k0(e, o()), o._c && (o._d = !0));
    return;
  } else {
    n = 32;
    const o = t._;
    !o && !Gk(t) ? t._ctx = po : o === 3 && po && (po.slots._ === 1 ? t._ = 1 : (t._ = 2, e.patchFlag |= 1024));
  }
  else $t(t) ? (t = {
    default: t,
    _ctx: po
  }, n = 32) : (t = String(t), r & 64 ? (n = 16, t = [Bs(t)]) : n = 8);
  e.children = t, e.shapeFlag |= n;
}
function D7(...e) {
  const t = {};
  for (let n = 0; n < e.length; n++) {
    const r = e[n];
    for (const o in r) if (o === "class")
      t.class !== r.class && (t.class = ze([t.class, r.class]));
    else if (o === "style") t.style = Jr([t.style, r.style]);
    else if (qm(o)) {
      const s = t[o], a = r[o];
      a && s !== a && !(Tt(s) && s.includes(a)) ? t[o] = s ? [].concat(s, a) : a : a == null && s == null && !Hm(o) && (t[o] = a);
    } else o !== "" && (t[o] = r[o]);
  }
  return t;
}
function Yo(e, t, n, r = null) {
  Oo(e, t, 7, [n, r]);
}
var L7 = Uk(), O7 = 0;
function F7(e, t, n) {
  const r = e.type, o = (t ? t.appContext : e.appContext) || L7, s = {
    uid: O7++,
    vnode: e,
    type: r,
    parent: t,
    appContext: o,
    root: null,
    next: null,
    subTree: null,
    effect: null,
    update: null,
    job: null,
    scope: new pN(!0),
    render: null,
    proxy: null,
    exposed: null,
    exposeProxy: null,
    withProxy: null,
    provides: t ? t.provides : Object.create(o.provides),
    ids: t ? t.ids : [
      "",
      0,
      0
    ],
    accessCache: null,
    renderCache: [],
    components: null,
    directives: null,
    propsOptions: Kk(r, o),
    emitsOptions: qk(r, o),
    emit: null,
    emitted: null,
    propsDefaults: an,
    inheritAttrs: r.inheritAttrs,
    ctx: an,
    data: an,
    props: an,
    attrs: an,
    slots: an,
    refs: an,
    setupState: an,
    setupContext: null,
    suspense: n,
    suspenseId: n ? n.pendingId : 0,
    asyncDep: null,
    asyncResolved: !1,
    isMounted: !1,
    isUnmounted: !1,
    isDeactivated: !1,
    bc: null,
    c: null,
    bm: null,
    m: null,
    bu: null,
    u: null,
    um: null,
    bum: null,
    da: null,
    a: null,
    rtg: null,
    rtc: null,
    ec: null,
    sp: null
  };
  return s.ctx = { _: s }, s.root = t ? t.root : s, s.emit = m7.bind(null, s), e.ce && e.ce(s), s;
}
var vr = null, B7 = () => vr || po, $p, a_;
{
  const e = Gm(), t = (n, r) => {
    let o;
    return (o = e[n]) || (o = e[n] = []), o.push(r), (s) => {
      o.length > 1 ? o.forEach((a) => a(s)) : o[0](s);
    };
  };
  $p = t("__VUE_INSTANCE_SETTERS__", (n) => vr = n), a_ = t("__VUE_SSR_SETTERS__", (n) => Id = n);
}
var tf = (e) => {
  const t = vr;
  return $p(e), e.scope.on(), () => {
    e.scope.off(), $p(t);
  };
}, GS = () => {
  vr && vr.scope.off(), $p(null);
};
function oI(e) {
  return e.vnode.shapeFlag & 4;
}
var Id = !1;
function U7(e, t = !1, n = !1) {
  t && a_(t);
  const { props: r, children: o } = e.vnode, s = oI(e);
  w7(e, r, s, t), A7(e, o, n || t);
  const a = s ? q7(e, t) : void 0;
  return t && a_(!1), a;
}
function q7(e, t) {
  const n = e.type;
  e.accessCache = /* @__PURE__ */ Object.create(null), e.proxy = new Proxy(e.ctx, a7);
  const { setup: r } = n;
  if (r) {
    Xs();
    const o = e.setupContext = r.length > 1 ? z7(e) : null, s = tf(e), a = ef(r, e, 0, [e.props, o]), i = tk(a);
    if (Qs(), s(), (i || e.sp) && !id(e) && Nk(e), i) {
      if (a.then(GS, GS), t) return a.then((l) => {
        VS(e, l, t);
      }).catch((l) => {
        Km(l, e, 0);
      });
      e.asyncDep = a;
    } else VS(e, a, t);
  } else sI(e, t);
}
function VS(e, t, n) {
  $t(t) ? e.type.__ssrInlineRender ? e.ssrRender = t : e.render = t : on(t) && (e.setupState = Ek(t)), sI(e, n);
}
var KS, JS;
function sI(e, t, n) {
  const r = e.type;
  if (!e.render) {
    if (!t && KS && !r.render) {
      const o = r.template || E0(e).template;
      if (o) {
        const { isCustomElement: s, compilerOptions: a } = e.appContext.config, { delimiters: i, compilerOptions: l } = r, u = On(On({
          isCustomElement: s,
          delimiters: i
        }, a), l);
        r.render = KS(o, u);
      }
    }
    e.render = r.render || as, JS && JS(e);
  }
  {
    const o = tf(e);
    Xs();
    try {
      i7(e);
    } finally {
      Qs(), o();
    }
  }
}
var H7 = { get(e, t) {
  return rr(e, "get", ""), e[t];
} };
function z7(e) {
  const t = (n) => {
    e.exposed = n || {};
  };
  return {
    attrs: new Proxy(e.attrs, H7),
    slots: e.slots,
    emit: e.emit,
    expose: t
  };
}
function Qm(e) {
  return e.exposed ? e.exposeProxy || (e.exposeProxy = new Proxy(Ek($N(e.exposed)), {
    get(t, n) {
      if (n in t) return t[n];
      if (n in ld) return ld[n](e);
    },
    has(t, n) {
      return n in t || n in ld;
    }
  })) : e.proxy;
}
function W7(e) {
  return $t(e) && "__vccOpts" in e;
}
var re = (e, t) => /* @__PURE__ */ BN(e, t, Id), G7 = "3.5.35", i_ = void 0, YS = typeof window < "u" && window.trustedTypes;
if (YS) try {
  i_ = /* @__PURE__ */ YS.createPolicy("vue", { createHTML: (e) => e });
} catch {
}
var aI = i_ ? (e) => i_.createHTML(e) : (e) => e, V7 = "http://www.w3.org/2000/svg", K7 = "http://www.w3.org/1998/Math/MathML", Rs = typeof document < "u" ? document : null, XS = Rs && /* @__PURE__ */ Rs.createElement("template"), J7 = {
  insert: (e, t, n) => {
    t.insertBefore(e, n || null);
  },
  remove: (e) => {
    const t = e.parentNode;
    t && t.removeChild(e);
  },
  createElement: (e, t, n, r) => {
    const o = t === "svg" ? Rs.createElementNS(V7, e) : t === "mathml" ? Rs.createElementNS(K7, e) : n ? Rs.createElement(e, { is: n }) : Rs.createElement(e);
    return e === "select" && r && r.multiple != null && o.setAttribute("multiple", r.multiple), o;
  },
  createText: (e) => Rs.createTextNode(e),
  createComment: (e) => Rs.createComment(e),
  setText: (e, t) => {
    e.nodeValue = t;
  },
  setElementText: (e, t) => {
    e.textContent = t;
  },
  parentNode: (e) => e.parentNode,
  nextSibling: (e) => e.nextSibling,
  querySelector: (e) => Rs.querySelector(e),
  setScopeId(e, t) {
    e.setAttribute(t, "");
  },
  insertStaticContent(e, t, n, r, o, s) {
    const a = n ? n.previousSibling : t.lastChild;
    if (o && (o === s || o.nextSibling)) for (; t.insertBefore(o.cloneNode(!0), n), !(o === s || !(o = o.nextSibling)); )
      ;
    else {
      XS.innerHTML = aI(r === "svg" ? `<svg>${e}</svg>` : r === "mathml" ? `<math>${e}</math>` : e);
      const i = XS.content;
      if (r === "svg" || r === "mathml") {
        const l = i.firstChild;
        for (; l.firstChild; ) i.appendChild(l.firstChild);
        i.removeChild(l);
      }
      t.insertBefore(i, n);
    }
    return [a ? a.nextSibling : t.firstChild, n ? n.previousSibling : t.lastChild];
  }
}, Y7 = /* @__PURE__ */ Symbol("_vtc");
function X7(e, t, n) {
  const r = e[Y7];
  r && (t = (t ? [t, ...r] : [...r]).join(" ")), t == null ? e.removeAttribute("class") : n ? e.setAttribute("class", t) : e.className = t;
}
var Dp = /* @__PURE__ */ Symbol("_vod"), iI = /* @__PURE__ */ Symbol("_vsh"), Iu = {
  name: "show",
  beforeMount(e, { value: t }, { transition: n }) {
    e[Dp] = e.style.display === "none" ? "" : e.style.display, n && t ? n.beforeEnter(e) : ac(e, t);
  },
  mounted(e, { value: t }, { transition: n }) {
    n && t && n.enter(e);
  },
  updated(e, { value: t, oldValue: n }, { transition: r }) {
    !t != !n && (r ? t ? (r.beforeEnter(e), ac(e, !0), r.enter(e)) : r.leave(e, () => {
      ac(e, !1);
    }) : ac(e, t));
  },
  beforeUnmount(e, { value: t }) {
    ac(e, t);
  }
};
function ac(e, t) {
  e.style.display = t ? e[Dp] : "none", e[iI] = !t;
}
var Q7 = /* @__PURE__ */ Symbol(""), Z7 = /(?:^|;)\s*display\s*:/;
function j7(e, t, n) {
  const r = e.style, o = _n(n);
  let s = !1;
  if (n && !o) {
    if (t) if (_n(t))
      for (const a of t.split(";")) {
        const i = a.slice(0, a.indexOf(":")).trim();
        n[i] == null && $c(r, i, "");
      }
    else for (const a in t) n[a] == null && $c(r, a, "");
    for (const a in n) {
      a === "display" && (s = !0);
      const i = n[a];
      i != null ? t$(e, a, !_n(t) && t ? t[a] : void 0, i) || $c(r, a, i) : $c(r, a, "");
    }
  } else if (o) {
    if (t !== n) {
      const a = r[Q7];
      a && (n += ";" + a), r.cssText = n, s = Z7.test(n);
    }
  } else t && e.removeAttribute("style");
  Dp in e && (e[Dp] = s ? r.display : "", e[iI] && (r.display = "none"));
}
var QS = /\s*!important$/;
function $c(e, t, n) {
  if (Tt(n)) n.forEach((r) => $c(e, t, r));
  else if (n == null && (n = ""), t.startsWith("--")) e.setProperty(t, n);
  else {
    const r = e$(e, t);
    QS.test(n) ? e.setProperty(Wa(r), n.replace(QS, ""), "important") : e[r] = n;
  }
}
var ZS = [
  "Webkit",
  "Moz",
  "ms"
], Gv = {};
function e$(e, t) {
  const n = Gv[t];
  if (n) return n;
  let r = Mo(t);
  if (r !== "filter" && r in e) return Gv[t] = r;
  r = ok(r);
  for (let o = 0; o < ZS.length; o++) {
    const s = ZS[o] + r;
    if (s in e) return Gv[t] = s;
  }
  return t;
}
function t$(e, t, n, r) {
  return e.tagName === "TEXTAREA" && (t === "width" || t === "height") && _n(r) && n === r;
}
var jS = "http://www.w3.org/1999/xlink";
function e2(e, t, n, r, o, s = fN(t)) {
  r && t.startsWith("xlink:") ? n == null ? e.removeAttributeNS(jS, t.slice(6, t.length)) : e.setAttributeNS(jS, t, n) : n == null || s && !ik(n) ? e.removeAttribute(t) : e.setAttribute(t, s ? "" : cs(n) ? String(n) : n);
}
function t2(e, t, n, r, o) {
  if (t === "innerHTML" || t === "textContent") {
    n != null && (e[t] = t === "innerHTML" ? aI(n) : n);
    return;
  }
  const s = e.tagName;
  if (t === "value" && s !== "PROGRESS" && !s.includes("-")) {
    const i = s === "OPTION" ? e.getAttribute("value") || "" : e.value, l = n == null ? e.type === "checkbox" ? "on" : "" : String(n);
    (i !== l || !("_value" in e)) && (e.value = l), n == null && e.removeAttribute(t), e._value = n;
    return;
  }
  let a = !1;
  if (n === "" || n == null) {
    const i = typeof e[t];
    i === "boolean" ? n = ik(n) : n == null && i === "string" ? (n = "", a = !0) : i === "number" && (n = 0, a = !0);
  }
  try {
    e[t] = n;
  } catch {
  }
  a && e.removeAttribute(o || t);
}
function wa(e, t, n, r) {
  e.addEventListener(t, n, r);
}
function n$(e, t, n, r) {
  e.removeEventListener(t, n, r);
}
var n2 = /* @__PURE__ */ Symbol("_vei");
function r$(e, t, n, r, o = null) {
  const s = e[n2] || (e[n2] = {}), a = s[t];
  if (r && a) a.value = r;
  else {
    const [i, l] = o$(t);
    r ? wa(e, i, s[t] = i$(r, o), l) : a && (n$(e, i, a, l), s[t] = void 0);
  }
}
var r2 = /(?:Once|Passive|Capture)$/;
function o$(e) {
  let t;
  if (r2.test(e)) {
    t = {};
    let n;
    for (; n = e.match(r2); )
      e = e.slice(0, e.length - n[0].length), t[n[0].toLowerCase()] = !0;
  }
  return [e[2] === ":" ? e.slice(3) : Wa(e.slice(2)), t];
}
var Vv = 0, s$ = /* @__PURE__ */ Promise.resolve(), a$ = () => Vv || (s$.then(() => Vv = 0), Vv = Date.now());
function i$(e, t) {
  const n = (r) => {
    if (!r._vts) r._vts = Date.now();
    else if (r._vts <= n.attached) return;
    const o = n.value;
    if (Tt(o)) {
      const s = r.stopImmediatePropagation;
      r.stopImmediatePropagation = () => {
        s.call(r), r._stopped = !0;
      };
      const a = o.slice(), i = [r];
      for (let l = 0; l < a.length && !r._stopped; l++) {
        const u = a[l];
        u && Oo(u, t, 5, i);
      }
    } else Oo(o, t, 5, [r]);
  };
  return n.value = e, n.attached = a$(), n;
}
var o2 = (e) => e.charCodeAt(0) === 111 && e.charCodeAt(1) === 110 && e.charCodeAt(2) > 96 && e.charCodeAt(2) < 123, l$ = (e, t, n, r, o, s) => {
  const a = o === "svg";
  t === "class" ? X7(e, r, a) : t === "style" ? j7(e, n, r) : qm(t) ? Hm(t) || r$(e, t, n, r, s) : (t[0] === "." ? (t = t.slice(1), !0) : t[0] === "^" ? (t = t.slice(1), !1) : u$(e, t, r, a)) ? (t2(e, t, r), !e.tagName.includes("-") && (t === "value" || t === "checked" || t === "selected") && e2(e, t, r, a, s, t !== "value")) : e._isVueCE && (c$(e, t) || e._def.__asyncLoader && (/[A-Z]/.test(t) || !_n(r))) ? t2(e, Mo(t), r, s, t) : (t === "true-value" ? e._trueValue = r : t === "false-value" && (e._falseValue = r), e2(e, t, r, a));
};
function u$(e, t, n, r) {
  if (r)
    return !!(t === "innerHTML" || t === "textContent" || t in e && o2(t) && $t(n));
  if (t === "spellcheck" || t === "draggable" || t === "translate" || t === "autocorrect" || t === "sandbox" && e.tagName === "IFRAME" || t === "form" || t === "list" && e.tagName === "INPUT" || t === "type" && e.tagName === "TEXTAREA") return !1;
  if (t === "width" || t === "height") {
    const o = e.tagName;
    if (o === "IMG" || o === "VIDEO" || o === "CANVAS" || o === "SOURCE") return !1;
  }
  return o2(t) && _n(n) ? !1 : t in e;
}
function c$(e, t) {
  const n = e._def.props;
  if (!n) return !1;
  const r = Mo(t);
  return Array.isArray(n) ? n.some((o) => Mo(o) === r) : Object.keys(n).some((o) => Mo(o) === r);
}
var fu = (e) => {
  const t = e.props["onUpdate:modelValue"] || !1;
  return Tt(t) ? (n) => Yh(t, n) : t;
};
function d$(e) {
  e.target.composing = !0;
}
function s2(e) {
  const t = e.target;
  t.composing && (t.composing = !1, t.dispatchEvent(new Event("input")));
}
var Gs = /* @__PURE__ */ Symbol("_assign");
function a2(e, t, n) {
  return t && (e = e.trim()), n && (e = Wm(e)), e;
}
var Ro = {
  created(e, { modifiers: { lazy: t, trim: n, number: r } }, o) {
    e[Gs] = fu(o);
    const s = r || o.props && o.props.type === "number";
    wa(e, t ? "change" : "input", (a) => {
      a.target.composing || e[Gs](a2(e.value, n, s));
    }), (n || s) && wa(e, "change", () => {
      e.value = a2(e.value, n, s);
    }), t || (wa(e, "compositionstart", d$), wa(e, "compositionend", s2), wa(e, "change", s2));
  },
  mounted(e, { value: t }) {
    e.value = t ?? "";
  },
  beforeUpdate(e, { value: t, oldValue: n, modifiers: { lazy: r, trim: o, number: s } }, a) {
    if (e[Gs] = fu(a), e.composing) return;
    const i = (s || e.type === "number") && !/^0\d/.test(e.value) ? Wm(e.value) : e.value, l = t ?? "";
    if (i === l) return;
    const u = e.getRootNode();
    (u instanceof Document || u instanceof ShadowRoot) && u.activeElement === e && e.type !== "range" && (r && t === n || o && e.value.trim() === l) || (e.value = l);
  }
}, f$ = {
  deep: !0,
  created(e, t, n) {
    e[Gs] = fu(n), wa(e, "change", () => {
      const r = e._modelValue, o = xd(e), s = e.checked, a = e[Gs];
      if (Tt(r)) {
        const i = f0(r, o), l = i !== -1;
        if (s && !l) a(r.concat(o));
        else if (!s && l) {
          const u = [...r];
          u.splice(i, 1), a(u);
        }
      } else if (Au(r)) {
        const i = new Set(r);
        s ? i.add(o) : i.delete(o), a(i);
      } else a(lI(e, s));
    });
  },
  mounted: i2,
  beforeUpdate(e, t, n) {
    e[Gs] = fu(n), i2(e, t, n);
  }
};
function i2(e, { value: t, oldValue: n }, r) {
  e._modelValue = t;
  let o;
  if (Tt(t)) o = f0(t, r.props.value) > -1;
  else if (Au(t)) o = t.has(r.props.value);
  else {
    if (t === n) return;
    o = Cu(t, lI(e, !0));
  }
  e.checked !== o && (e.checked = o);
}
var h$ = {
  deep: !0,
  created(e, { value: t, modifiers: { number: n } }, r) {
    const o = Au(t);
    wa(e, "change", () => {
      const s = Array.prototype.filter.call(e.options, (a) => a.selected).map((a) => n ? Wm(xd(a)) : xd(a));
      e[Gs](e.multiple ? o ? new Set(s) : s : s[0]), e._assigning = !0, Qt(() => {
        e._assigning = !1;
      });
    }), e[Gs] = fu(r);
  },
  mounted(e, { value: t }) {
    l2(e, t);
  },
  beforeUpdate(e, t, n) {
    e[Gs] = fu(n);
  },
  updated(e, { value: t }) {
    e._assigning || l2(e, t);
  }
};
function l2(e, t) {
  const n = e.multiple, r = Tt(t);
  if (!(n && !r && !Au(t))) {
    for (let o = 0, s = e.options.length; o < s; o++) {
      const a = e.options[o], i = xd(a);
      if (n) if (r) {
        const l = typeof i;
        l === "string" || l === "number" ? a.selected = t.some((u) => String(u) === String(i)) : a.selected = f0(t, i) > -1;
      } else a.selected = t.has(i);
      else if (Cu(xd(a), t)) {
        e.selectedIndex !== o && (e.selectedIndex = o);
        return;
      }
    }
    !n && e.selectedIndex !== -1 && (e.selectedIndex = -1);
  }
}
function xd(e) {
  return "_value" in e ? e._value : e.value;
}
function lI(e, t) {
  const n = t ? "_trueValue" : "_falseValue";
  return n in e ? e[n] : t;
}
var p$ = [
  "ctrl",
  "shift",
  "alt",
  "meta"
], m$ = {
  stop: (e) => e.stopPropagation(),
  prevent: (e) => e.preventDefault(),
  self: (e) => e.target !== e.currentTarget,
  ctrl: (e) => !e.ctrlKey,
  shift: (e) => !e.shiftKey,
  alt: (e) => !e.altKey,
  meta: (e) => !e.metaKey,
  left: (e) => "button" in e && e.button !== 0,
  middle: (e) => "button" in e && e.button !== 1,
  right: (e) => "button" in e && e.button !== 2,
  exact: (e, t) => p$.some((n) => e[`${n}Key`] && !t.includes(n))
}, sn = (e, t) => {
  if (!e) return e;
  const n = e._withMods || (e._withMods = {}), r = t.join(".");
  return n[r] || (n[r] = ((o, ...s) => {
    for (let a = 0; a < t.length; a++) {
      const i = m$[t[a]];
      if (i && i(o, t)) return;
    }
    return e(o, ...s);
  }));
}, g$ = {
  esc: "escape",
  space: " ",
  up: "arrow-up",
  left: "arrow-left",
  right: "arrow-right",
  down: "arrow-down",
  delete: "backspace"
}, Vs = (e, t) => {
  const n = e._withKeys || (e._withKeys = {}), r = t.join(".");
  return n[r] || (n[r] = ((o) => {
    if (!("key" in o)) return;
    const s = Wa(o.key);
    if (t.some((a) => a === s || g$[a] === s)) return e(o);
  }));
}, v$ = /* @__PURE__ */ On({ patchProp: l$ }, J7), u2;
function y$() {
  return u2 || (u2 = k7(v$));
}
var _$ = ((...e) => {
  const t = y$().createApp(...e), { mount: n } = t;
  return t.mount = (r) => {
    const o = w$(r);
    if (!o) return;
    const s = t._component;
    !$t(s) && !s.render && !s.template && (s.template = o.innerHTML), o.nodeType === 1 && (o.textContent = "");
    const a = n(o, !1, b$(o));
    return o instanceof Element && (o.removeAttribute("v-cloak"), o.setAttribute("data-v-app", "")), a;
  }, t;
});
function b$(e) {
  if (e instanceof SVGElement) return "svg";
  if (typeof MathMLElement == "function" && e instanceof MathMLElement) return "mathml";
}
function w$(e) {
  return _n(e) ? document.querySelector(e) : e;
}
function S$(e, t) {
  const n = Number(e);
  return Number.isFinite(n) && n > 0 ? Math.floor(n) : t;
}
function Sa(e, t = 5) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.min(20, Math.max(1, Math.floor(n))) : Math.min(20, Math.max(1, Math.floor(t)));
}
function I0(e, t = 20) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.min(50, Math.max(5, Math.round(n / 5) * 5)) : Math.min(50, Math.max(5, Math.round(t / 5) * 5));
}
function uI(e = {}, t = 0, n = {}) {
  const r = Math.max(0, Number(t) || 0), o = Sa(n.defaultLimit, 5), s = S$(e.uiMessageWindowLimit, o), a = Math.min(Math.max(o, s), Math.max(r, o));
  e.uiMessageWindowLimit = a;
  const i = Math.max(0, r - a);
  return {
    startIndex: i,
    hiddenBefore: i,
    visibleCount: r - i,
    limit: a,
    total: r
  };
}
function T$(e = {}, t = 0, n = {}) {
  const r = Math.max(0, Number(t) || 0), o = uI(e, r, n);
  if (!o.hiddenBefore) return !1;
  const s = I0(n.chunk, 20);
  return e.uiMessageWindowLimit = Math.min(r, o.limit + s), !0;
}
function E$(e = {}, t = {}) {
  e.uiMessageWindowLimit = Sa(t.defaultLimit, 5);
}
var A$ = [
  "small",
  "medium",
  "large"
];
function C$(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function cI(e, t, n, r) {
  const o = Number(e);
  return Number.isFinite(o) ? Math.min(r, Math.max(n, Math.floor(o))) : t;
}
function k$(e, t = 20) {
  const n = cI(e, t, 5, 50);
  return Math.min(50, Math.max(5, Math.round(n / 5) * 5));
}
function I$(e) {
  return e === "medium" || e === "large" ? e : "small";
}
function zl(e = {}) {
  const t = C$(e);
  return {
    hiddenOutsideCount: cI(t.hiddenOutsideCount, 5, 1, 20),
    loadBatchSize: k$(t.loadBatchSize, 20),
    chatFontSize: I$(t.chatFontSize)
  };
}
var x$ = /* @__PURE__ */ Zd(((e, t) => {
  (function() {
    function n(p) {
      "use strict";
      var b = {
        omitExtraWLInCodeBlocks: {
          defaultValue: !1,
          describe: "Omit the default extra whiteline added to code blocks",
          type: "boolean"
        },
        noHeaderId: {
          defaultValue: !1,
          describe: "Turn on/off generated header id",
          type: "boolean"
        },
        prefixHeaderId: {
          defaultValue: !1,
          describe: "Add a prefix to the generated header ids. Passing a string will prefix that string to the header id. Setting to true will add a generic 'section-' prefix",
          type: "string"
        },
        rawPrefixHeaderId: {
          defaultValue: !1,
          describe: 'Setting this option to true will prevent showdown from modifying the prefix. This might result in malformed IDs (if, for instance, the " char is used in the prefix)',
          type: "boolean"
        },
        ghCompatibleHeaderId: {
          defaultValue: !1,
          describe: "Generate header ids compatible with github style (spaces are replaced with dashes, a bunch of non alphanumeric chars are removed)",
          type: "boolean"
        },
        rawHeaderId: {
          defaultValue: !1,
          describe: `Remove only spaces, ' and " from generated header ids (including prefixes), replacing them with dashes (-). WARNING: This might result in malformed ids`,
          type: "boolean"
        },
        headerLevelStart: {
          defaultValue: !1,
          describe: "The header blocks level start",
          type: "integer"
        },
        parseImgDimensions: {
          defaultValue: !1,
          describe: "Turn on/off image dimension parsing",
          type: "boolean"
        },
        simplifiedAutoLink: {
          defaultValue: !1,
          describe: "Turn on/off GFM autolink style",
          type: "boolean"
        },
        excludeTrailingPunctuationFromURLs: {
          defaultValue: !1,
          describe: "Excludes trailing punctuation from links generated with autoLinking",
          type: "boolean"
        },
        literalMidWordUnderscores: {
          defaultValue: !1,
          describe: "Parse midword underscores as literal underscores",
          type: "boolean"
        },
        literalMidWordAsterisks: {
          defaultValue: !1,
          describe: "Parse midword asterisks as literal asterisks",
          type: "boolean"
        },
        strikethrough: {
          defaultValue: !1,
          describe: "Turn on/off strikethrough support",
          type: "boolean"
        },
        tables: {
          defaultValue: !1,
          describe: "Turn on/off tables support",
          type: "boolean"
        },
        tablesHeaderId: {
          defaultValue: !1,
          describe: "Add an id to table headers",
          type: "boolean"
        },
        ghCodeBlocks: {
          defaultValue: !0,
          describe: "Turn on/off GFM fenced code blocks support",
          type: "boolean"
        },
        tasklists: {
          defaultValue: !1,
          describe: "Turn on/off GFM tasklist support",
          type: "boolean"
        },
        smoothLivePreview: {
          defaultValue: !1,
          describe: "Prevents weird effects in live previews due to incomplete input",
          type: "boolean"
        },
        smartIndentationFix: {
          defaultValue: !1,
          describe: "Tries to smartly fix indentation in es6 strings",
          type: "boolean"
        },
        disableForced4SpacesIndentedSublists: {
          defaultValue: !1,
          describe: "Disables the requirement of indenting nested sublists by 4 spaces",
          type: "boolean"
        },
        simpleLineBreaks: {
          defaultValue: !1,
          describe: "Parses simple line breaks as <br> (GFM Style)",
          type: "boolean"
        },
        requireSpaceBeforeHeadingText: {
          defaultValue: !1,
          describe: "Makes adding a space between `#` and the header text mandatory (GFM Style)",
          type: "boolean"
        },
        ghMentions: {
          defaultValue: !1,
          describe: "Enables github @mentions",
          type: "boolean"
        },
        ghMentionsLink: {
          defaultValue: "https://github.com/{u}",
          describe: "Changes the link generated by @mentions. Only applies if ghMentions option is enabled.",
          type: "string"
        },
        encodeEmails: {
          defaultValue: !0,
          describe: "Encode e-mail addresses through the use of Character Entities, transforming ASCII e-mail addresses into its equivalent decimal entities",
          type: "boolean"
        },
        openLinksInNewWindow: {
          defaultValue: !1,
          describe: "Open all links in new windows",
          type: "boolean"
        },
        backslashEscapesHTMLTags: {
          defaultValue: !1,
          describe: "Support for HTML Tag escaping. ex: <div>foo</div>",
          type: "boolean"
        },
        emoji: {
          defaultValue: !1,
          describe: "Enable emoji support. Ex: `this is a :smile: emoji`",
          type: "boolean"
        },
        underline: {
          defaultValue: !1,
          describe: "Enable support for underline. Syntax is double or triple underscores: `__underline word__`. With this option enabled, underscores no longer parses into `<em>` and `<strong>`",
          type: "boolean"
        },
        ellipsis: {
          defaultValue: !0,
          describe: "Replaces three dots with the ellipsis unicode character",
          type: "boolean"
        },
        completeHTMLDocument: {
          defaultValue: !1,
          describe: "Outputs a complete html document, including `<html>`, `<head>` and `<body>` tags",
          type: "boolean"
        },
        metadata: {
          defaultValue: !1,
          describe: "Enable support for document metadata (defined at the top of the document between `«««` and `»»»` or between `---` and `---`).",
          type: "boolean"
        },
        splitAdjacentBlockquotes: {
          defaultValue: !1,
          describe: "Split adjacent blockquote blocks",
          type: "boolean"
        }
      };
      if (p === !1) return JSON.parse(JSON.stringify(b));
      var w = {};
      for (var E in b) b.hasOwnProperty(E) && (w[E] = b[E].defaultValue);
      return w;
    }
    function r() {
      "use strict";
      var p = n(!0), b = {};
      for (var w in p) p.hasOwnProperty(w) && (b[w] = !0);
      return b;
    }
    var o = {}, s = {}, a = {}, i = n(!0), l = "vanilla", u = {
      github: {
        omitExtraWLInCodeBlocks: !0,
        simplifiedAutoLink: !0,
        excludeTrailingPunctuationFromURLs: !0,
        literalMidWordUnderscores: !0,
        strikethrough: !0,
        tables: !0,
        tablesHeaderId: !0,
        ghCodeBlocks: !0,
        tasklists: !0,
        disableForced4SpacesIndentedSublists: !0,
        simpleLineBreaks: !0,
        requireSpaceBeforeHeadingText: !0,
        ghCompatibleHeaderId: !0,
        ghMentions: !0,
        backslashEscapesHTMLTags: !0,
        emoji: !0,
        splitAdjacentBlockquotes: !0
      },
      original: {
        noHeaderId: !0,
        ghCodeBlocks: !1
      },
      ghost: {
        omitExtraWLInCodeBlocks: !0,
        parseImgDimensions: !0,
        simplifiedAutoLink: !0,
        excludeTrailingPunctuationFromURLs: !0,
        literalMidWordUnderscores: !0,
        strikethrough: !0,
        tables: !0,
        tablesHeaderId: !0,
        ghCodeBlocks: !0,
        tasklists: !0,
        smoothLivePreview: !0,
        simpleLineBreaks: !0,
        requireSpaceBeforeHeadingText: !0,
        ghMentions: !1,
        encodeEmails: !0
      },
      vanilla: n(!0),
      allOn: r()
    };
    o.helper = {}, o.extensions = {}, o.setOption = function(p, b) {
      "use strict";
      return i[p] = b, this;
    }, o.getOption = function(p) {
      "use strict";
      return i[p];
    }, o.getOptions = function() {
      "use strict";
      return i;
    }, o.resetOptions = function() {
      "use strict";
      i = n(!0);
    }, o.setFlavor = function(p) {
      "use strict";
      if (!u.hasOwnProperty(p)) throw Error(p + " flavor was not found");
      o.resetOptions();
      var b = u[p];
      l = p;
      for (var w in b) b.hasOwnProperty(w) && (i[w] = b[w]);
    }, o.getFlavor = function() {
      "use strict";
      return l;
    }, o.getFlavorOptions = function(p) {
      "use strict";
      if (u.hasOwnProperty(p)) return u[p];
    }, o.getDefaultOptions = function(p) {
      "use strict";
      return n(p);
    }, o.subParser = function(p, b) {
      "use strict";
      if (o.helper.isString(p)) if (typeof b < "u") s[p] = b;
      else {
        if (s.hasOwnProperty(p)) return s[p];
        throw Error("SubParser named " + p + " not registered!");
      }
    }, o.extension = function(p, b) {
      "use strict";
      if (!o.helper.isString(p)) throw Error("Extension 'name' must be a string");
      if (p = o.helper.stdExtName(p), o.helper.isUndefined(b)) {
        if (!a.hasOwnProperty(p)) throw Error("Extension named " + p + " is not registered!");
        return a[p];
      } else {
        typeof b == "function" && (b = b()), o.helper.isArray(b) || (b = [b]);
        var w = d(b, p);
        if (w.valid) a[p] = b;
        else throw Error(w.error);
      }
    }, o.getAllExtensions = function() {
      "use strict";
      return a;
    }, o.removeExtension = function(p) {
      "use strict";
      delete a[p];
    }, o.resetExtensions = function() {
      "use strict";
      a = {};
    };
    function d(p, b) {
      "use strict";
      var w = b ? "Error in " + b + " extension->" : "Error in unnamed extension", E = {
        valid: !0,
        error: ""
      };
      o.helper.isArray(p) || (p = [p]);
      for (var x = 0; x < p.length; ++x) {
        var M = w + " sub-extension " + x + ": ", N = p[x];
        if (typeof N != "object")
          return E.valid = !1, E.error = M + "must be an object, but " + typeof N + " given", E;
        if (!o.helper.isString(N.type))
          return E.valid = !1, E.error = M + 'property "type" must be a string, but ' + typeof N.type + " given", E;
        var q = N.type = N.type.toLowerCase();
        if (q === "language" && (q = N.type = "lang"), q === "html" && (q = N.type = "output"), q !== "lang" && q !== "output" && q !== "listener")
          return E.valid = !1, E.error = M + "type " + q + ' is not recognized. Valid values: "lang/language", "output/html" or "listener"', E;
        if (q === "listener") {
          if (o.helper.isUndefined(N.listeners))
            return E.valid = !1, E.error = M + '. Extensions of type "listener" must have a property called "listeners"', E;
        } else if (o.helper.isUndefined(N.filter) && o.helper.isUndefined(N.regex))
          return E.valid = !1, E.error = M + q + ' extensions must define either a "regex" property or a "filter" method', E;
        if (N.listeners) {
          if (typeof N.listeners != "object")
            return E.valid = !1, E.error = M + '"listeners" property must be an object but ' + typeof N.listeners + " given", E;
          for (var z in N.listeners) if (N.listeners.hasOwnProperty(z) && typeof N.listeners[z] != "function")
            return E.valid = !1, E.error = M + '"listeners" property must be an hash of [event name]: [callback]. listeners.' + z + " must be a function but " + typeof N.listeners[z] + " given", E;
        }
        if (N.filter) {
          if (typeof N.filter != "function")
            return E.valid = !1, E.error = M + '"filter" must be a function, but ' + typeof N.filter + " given", E;
        } else if (N.regex) {
          if (o.helper.isString(N.regex) && (N.regex = new RegExp(N.regex, "g")), !(N.regex instanceof RegExp))
            return E.valid = !1, E.error = M + '"regex" property must either be a string or a RegExp object, but ' + typeof N.regex + " given", E;
          if (o.helper.isUndefined(N.replace))
            return E.valid = !1, E.error = M + '"regex" extensions must implement a replace string or function', E;
        }
      }
      return E;
    }
    o.validateExtension = function(p) {
      "use strict";
      var b = d(p, null);
      return b.valid ? !0 : (console.warn(b.error), !1);
    }, o.hasOwnProperty("helper") || (o.helper = {}), o.helper.isString = function(p) {
      "use strict";
      return typeof p == "string" || p instanceof String;
    }, o.helper.isFunction = function(p) {
      "use strict";
      return p && {}.toString.call(p) === "[object Function]";
    }, o.helper.isArray = function(p) {
      "use strict";
      return Array.isArray(p);
    }, o.helper.isUndefined = function(p) {
      "use strict";
      return typeof p > "u";
    }, o.helper.forEach = function(p, b) {
      "use strict";
      if (o.helper.isUndefined(p)) throw new Error("obj param is required");
      if (o.helper.isUndefined(b)) throw new Error("callback param is required");
      if (!o.helper.isFunction(b)) throw new Error("callback param must be a function/closure");
      if (typeof p.forEach == "function") p.forEach(b);
      else if (o.helper.isArray(p)) for (var w = 0; w < p.length; w++) b(p[w], w, p);
      else if (typeof p == "object")
        for (var E in p) p.hasOwnProperty(E) && b(p[E], E, p);
      else throw new Error("obj does not seem to be an array or an iterable object");
    }, o.helper.stdExtName = function(p) {
      "use strict";
      return p.replace(/[_?*+\/\\.^-]/g, "").replace(/\s/g, "").toLowerCase();
    };
    function f(p, b) {
      "use strict";
      return "¨E" + b.charCodeAt(0) + "E";
    }
    o.helper.escapeCharactersCallback = f, o.helper.escapeCharacters = function(p, b, w) {
      "use strict";
      var E = "([" + b.replace(/([\[\]\\])/g, "\\$1") + "])";
      w && (E = "\\\\" + E);
      var x = new RegExp(E, "g");
      return p = p.replace(x, f), p;
    }, o.helper.unescapeHTMLEntities = function(p) {
      "use strict";
      return p.replace(/&quot;/g, '"').replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&amp;/g, "&");
    };
    var m = function(p, b, w, E) {
      "use strict";
      var x = E || "", M = x.indexOf("g") > -1, N = new RegExp(b + "|" + w, "g" + x.replace(/g/g, "")), q = new RegExp(b, x.replace(/g/g, "")), z = [], J, G, oe, B, X;
      do
        for (J = 0; oe = N.exec(p); ) if (q.test(oe[0]))
          J++ || (G = N.lastIndex, B = G - oe[0].length);
        else if (J && !--J) {
          X = oe.index + oe[0].length;
          var de = {
            left: {
              start: B,
              end: G
            },
            match: {
              start: G,
              end: oe.index
            },
            right: {
              start: oe.index,
              end: X
            },
            wholeMatch: {
              start: B,
              end: X
            }
          };
          if (z.push(de), !M) return z;
        }
      while (J && (N.lastIndex = G));
      return z;
    };
    o.helper.matchRecursiveRegExp = function(p, b, w, E) {
      "use strict";
      for (var x = m(p, b, w, E), M = [], N = 0; N < x.length; ++N) M.push([
        p.slice(x[N].wholeMatch.start, x[N].wholeMatch.end),
        p.slice(x[N].match.start, x[N].match.end),
        p.slice(x[N].left.start, x[N].left.end),
        p.slice(x[N].right.start, x[N].right.end)
      ]);
      return M;
    }, o.helper.replaceRecursiveRegExp = function(p, b, w, E, x) {
      "use strict";
      if (!o.helper.isFunction(b)) {
        var M = b;
        b = function() {
          return M;
        };
      }
      var N = m(p, w, E, x), q = p, z = N.length;
      if (z > 0) {
        var J = [];
        N[0].wholeMatch.start !== 0 && J.push(p.slice(0, N[0].wholeMatch.start));
        for (var G = 0; G < z; ++G)
          J.push(b(p.slice(N[G].wholeMatch.start, N[G].wholeMatch.end), p.slice(N[G].match.start, N[G].match.end), p.slice(N[G].left.start, N[G].left.end), p.slice(N[G].right.start, N[G].right.end))), G < z - 1 && J.push(p.slice(N[G].wholeMatch.end, N[G + 1].wholeMatch.start));
        N[z - 1].wholeMatch.end < p.length && J.push(p.slice(N[z - 1].wholeMatch.end)), q = J.join("");
      }
      return q;
    }, o.helper.regexIndexOf = function(p, b, w) {
      "use strict";
      if (!o.helper.isString(p)) throw "InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string";
      if (!(b instanceof RegExp)) throw "InvalidArgumentError: second parameter of showdown.helper.regexIndexOf function must be an instance of RegExp";
      var E = p.substring(w || 0).search(b);
      return E >= 0 ? E + (w || 0) : E;
    }, o.helper.splitAtIndex = function(p, b) {
      "use strict";
      if (!o.helper.isString(p)) throw "InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string";
      return [p.substring(0, b), p.substring(b)];
    }, o.helper.encodeEmailAddress = function(p) {
      "use strict";
      var b = [
        function(w) {
          return "&#" + w.charCodeAt(0) + ";";
        },
        function(w) {
          return "&#x" + w.charCodeAt(0).toString(16) + ";";
        },
        function(w) {
          return w;
        }
      ];
      return p = p.replace(/./g, function(w) {
        if (w === "@") w = b[Math.floor(Math.random() * 2)](w);
        else {
          var E = Math.random();
          w = E > 0.9 ? b[2](w) : E > 0.45 ? b[1](w) : b[0](w);
        }
        return w;
      }), p;
    }, o.helper.padEnd = function(b, w, E) {
      "use strict";
      return w = w >> 0, E = String(E || " "), b.length > w ? String(b) : (w = w - b.length, w > E.length && (E += E.repeat(w / E.length)), String(b) + E.slice(0, w));
    }, typeof console > "u" && (console = {
      warn: function(p) {
        "use strict";
        alert(p);
      },
      log: function(p) {
        "use strict";
        alert(p);
      },
      error: function(p) {
        "use strict";
        throw p;
      }
    }), o.helper.regexes = { asteriskDashAndColon: /([*_:~])/g }, o.helper.emojis = {
      "+1": "👍",
      "-1": "👎",
      100: "💯",
      1234: "🔢",
      "1st_place_medal": "🥇",
      "2nd_place_medal": "🥈",
      "3rd_place_medal": "🥉",
      "8ball": "🎱",
      a: "🅰️",
      ab: "🆎",
      abc: "🔤",
      abcd: "🔡",
      accept: "🉑",
      aerial_tramway: "🚡",
      airplane: "✈️",
      alarm_clock: "⏰",
      alembic: "⚗️",
      alien: "👽",
      ambulance: "🚑",
      amphora: "🏺",
      anchor: "⚓️",
      angel: "👼",
      anger: "💢",
      angry: "😠",
      anguished: "😧",
      ant: "🐜",
      apple: "🍎",
      aquarius: "♒️",
      aries: "♈️",
      arrow_backward: "◀️",
      arrow_double_down: "⏬",
      arrow_double_up: "⏫",
      arrow_down: "⬇️",
      arrow_down_small: "🔽",
      arrow_forward: "▶️",
      arrow_heading_down: "⤵️",
      arrow_heading_up: "⤴️",
      arrow_left: "⬅️",
      arrow_lower_left: "↙️",
      arrow_lower_right: "↘️",
      arrow_right: "➡️",
      arrow_right_hook: "↪️",
      arrow_up: "⬆️",
      arrow_up_down: "↕️",
      arrow_up_small: "🔼",
      arrow_upper_left: "↖️",
      arrow_upper_right: "↗️",
      arrows_clockwise: "🔃",
      arrows_counterclockwise: "🔄",
      art: "🎨",
      articulated_lorry: "🚛",
      artificial_satellite: "🛰",
      astonished: "😲",
      athletic_shoe: "👟",
      atm: "🏧",
      atom_symbol: "⚛️",
      avocado: "🥑",
      b: "🅱️",
      baby: "👶",
      baby_bottle: "🍼",
      baby_chick: "🐤",
      baby_symbol: "🚼",
      back: "🔙",
      bacon: "🥓",
      badminton: "🏸",
      baggage_claim: "🛄",
      baguette_bread: "🥖",
      balance_scale: "⚖️",
      balloon: "🎈",
      ballot_box: "🗳",
      ballot_box_with_check: "☑️",
      bamboo: "🎍",
      banana: "🍌",
      bangbang: "‼️",
      bank: "🏦",
      bar_chart: "📊",
      barber: "💈",
      baseball: "⚾️",
      basketball: "🏀",
      basketball_man: "⛹️",
      basketball_woman: "⛹️&zwj;♀️",
      bat: "🦇",
      bath: "🛀",
      bathtub: "🛁",
      battery: "🔋",
      beach_umbrella: "🏖",
      bear: "🐻",
      bed: "🛏",
      bee: "🐝",
      beer: "🍺",
      beers: "🍻",
      beetle: "🐞",
      beginner: "🔰",
      bell: "🔔",
      bellhop_bell: "🛎",
      bento: "🍱",
      biking_man: "🚴",
      bike: "🚲",
      biking_woman: "🚴&zwj;♀️",
      bikini: "👙",
      biohazard: "☣️",
      bird: "🐦",
      birthday: "🎂",
      black_circle: "⚫️",
      black_flag: "🏴",
      black_heart: "🖤",
      black_joker: "🃏",
      black_large_square: "⬛️",
      black_medium_small_square: "◾️",
      black_medium_square: "◼️",
      black_nib: "✒️",
      black_small_square: "▪️",
      black_square_button: "🔲",
      blonde_man: "👱",
      blonde_woman: "👱&zwj;♀️",
      blossom: "🌼",
      blowfish: "🐡",
      blue_book: "📘",
      blue_car: "🚙",
      blue_heart: "💙",
      blush: "😊",
      boar: "🐗",
      boat: "⛵️",
      bomb: "💣",
      book: "📖",
      bookmark: "🔖",
      bookmark_tabs: "📑",
      books: "📚",
      boom: "💥",
      boot: "👢",
      bouquet: "💐",
      bowing_man: "🙇",
      bow_and_arrow: "🏹",
      bowing_woman: "🙇&zwj;♀️",
      bowling: "🎳",
      boxing_glove: "🥊",
      boy: "👦",
      bread: "🍞",
      bride_with_veil: "👰",
      bridge_at_night: "🌉",
      briefcase: "💼",
      broken_heart: "💔",
      bug: "🐛",
      building_construction: "🏗",
      bulb: "💡",
      bullettrain_front: "🚅",
      bullettrain_side: "🚄",
      burrito: "🌯",
      bus: "🚌",
      business_suit_levitating: "🕴",
      busstop: "🚏",
      bust_in_silhouette: "👤",
      busts_in_silhouette: "👥",
      butterfly: "🦋",
      cactus: "🌵",
      cake: "🍰",
      calendar: "📆",
      call_me_hand: "🤙",
      calling: "📲",
      camel: "🐫",
      camera: "📷",
      camera_flash: "📸",
      camping: "🏕",
      cancer: "♋️",
      candle: "🕯",
      candy: "🍬",
      canoe: "🛶",
      capital_abcd: "🔠",
      capricorn: "♑️",
      car: "🚗",
      card_file_box: "🗃",
      card_index: "📇",
      card_index_dividers: "🗂",
      carousel_horse: "🎠",
      carrot: "🥕",
      cat: "🐱",
      cat2: "🐈",
      cd: "💿",
      chains: "⛓",
      champagne: "🍾",
      chart: "💹",
      chart_with_downwards_trend: "📉",
      chart_with_upwards_trend: "📈",
      checkered_flag: "🏁",
      cheese: "🧀",
      cherries: "🍒",
      cherry_blossom: "🌸",
      chestnut: "🌰",
      chicken: "🐔",
      children_crossing: "🚸",
      chipmunk: "🐿",
      chocolate_bar: "🍫",
      christmas_tree: "🎄",
      church: "⛪️",
      cinema: "🎦",
      circus_tent: "🎪",
      city_sunrise: "🌇",
      city_sunset: "🌆",
      cityscape: "🏙",
      cl: "🆑",
      clamp: "🗜",
      clap: "👏",
      clapper: "🎬",
      classical_building: "🏛",
      clinking_glasses: "🥂",
      clipboard: "📋",
      clock1: "🕐",
      clock10: "🕙",
      clock1030: "🕥",
      clock11: "🕚",
      clock1130: "🕦",
      clock12: "🕛",
      clock1230: "🕧",
      clock130: "🕜",
      clock2: "🕑",
      clock230: "🕝",
      clock3: "🕒",
      clock330: "🕞",
      clock4: "🕓",
      clock430: "🕟",
      clock5: "🕔",
      clock530: "🕠",
      clock6: "🕕",
      clock630: "🕡",
      clock7: "🕖",
      clock730: "🕢",
      clock8: "🕗",
      clock830: "🕣",
      clock9: "🕘",
      clock930: "🕤",
      closed_book: "📕",
      closed_lock_with_key: "🔐",
      closed_umbrella: "🌂",
      cloud: "☁️",
      cloud_with_lightning: "🌩",
      cloud_with_lightning_and_rain: "⛈",
      cloud_with_rain: "🌧",
      cloud_with_snow: "🌨",
      clown_face: "🤡",
      clubs: "♣️",
      cocktail: "🍸",
      coffee: "☕️",
      coffin: "⚰️",
      cold_sweat: "😰",
      comet: "☄️",
      computer: "💻",
      computer_mouse: "🖱",
      confetti_ball: "🎊",
      confounded: "😖",
      confused: "😕",
      congratulations: "㊗️",
      construction: "🚧",
      construction_worker_man: "👷",
      construction_worker_woman: "👷&zwj;♀️",
      control_knobs: "🎛",
      convenience_store: "🏪",
      cookie: "🍪",
      cool: "🆒",
      policeman: "👮",
      copyright: "©️",
      corn: "🌽",
      couch_and_lamp: "🛋",
      couple: "👫",
      couple_with_heart_woman_man: "💑",
      couple_with_heart_man_man: "👨&zwj;❤️&zwj;👨",
      couple_with_heart_woman_woman: "👩&zwj;❤️&zwj;👩",
      couplekiss_man_man: "👨&zwj;❤️&zwj;💋&zwj;👨",
      couplekiss_man_woman: "💏",
      couplekiss_woman_woman: "👩&zwj;❤️&zwj;💋&zwj;👩",
      cow: "🐮",
      cow2: "🐄",
      cowboy_hat_face: "🤠",
      crab: "🦀",
      crayon: "🖍",
      credit_card: "💳",
      crescent_moon: "🌙",
      cricket: "🏏",
      crocodile: "🐊",
      croissant: "🥐",
      crossed_fingers: "🤞",
      crossed_flags: "🎌",
      crossed_swords: "⚔️",
      crown: "👑",
      cry: "😢",
      crying_cat_face: "😿",
      crystal_ball: "🔮",
      cucumber: "🥒",
      cupid: "💘",
      curly_loop: "➰",
      currency_exchange: "💱",
      curry: "🍛",
      custard: "🍮",
      customs: "🛃",
      cyclone: "🌀",
      dagger: "🗡",
      dancer: "💃",
      dancing_women: "👯",
      dancing_men: "👯&zwj;♂️",
      dango: "🍡",
      dark_sunglasses: "🕶",
      dart: "🎯",
      dash: "💨",
      date: "📅",
      deciduous_tree: "🌳",
      deer: "🦌",
      department_store: "🏬",
      derelict_house: "🏚",
      desert: "🏜",
      desert_island: "🏝",
      desktop_computer: "🖥",
      male_detective: "🕵️",
      diamond_shape_with_a_dot_inside: "💠",
      diamonds: "♦️",
      disappointed: "😞",
      disappointed_relieved: "😥",
      dizzy: "💫",
      dizzy_face: "😵",
      do_not_litter: "🚯",
      dog: "🐶",
      dog2: "🐕",
      dollar: "💵",
      dolls: "🎎",
      dolphin: "🐬",
      door: "🚪",
      doughnut: "🍩",
      dove: "🕊",
      dragon: "🐉",
      dragon_face: "🐲",
      dress: "👗",
      dromedary_camel: "🐪",
      drooling_face: "🤤",
      droplet: "💧",
      drum: "🥁",
      duck: "🦆",
      dvd: "📀",
      "e-mail": "📧",
      eagle: "🦅",
      ear: "👂",
      ear_of_rice: "🌾",
      earth_africa: "🌍",
      earth_americas: "🌎",
      earth_asia: "🌏",
      egg: "🥚",
      eggplant: "🍆",
      eight_pointed_black_star: "✴️",
      eight_spoked_asterisk: "✳️",
      electric_plug: "🔌",
      elephant: "🐘",
      email: "✉️",
      end: "🔚",
      envelope_with_arrow: "📩",
      euro: "💶",
      european_castle: "🏰",
      european_post_office: "🏤",
      evergreen_tree: "🌲",
      exclamation: "❗️",
      expressionless: "😑",
      eye: "👁",
      eye_speech_bubble: "👁&zwj;🗨",
      eyeglasses: "👓",
      eyes: "👀",
      face_with_head_bandage: "🤕",
      face_with_thermometer: "🤒",
      fist_oncoming: "👊",
      factory: "🏭",
      fallen_leaf: "🍂",
      family_man_woman_boy: "👪",
      family_man_boy: "👨&zwj;👦",
      family_man_boy_boy: "👨&zwj;👦&zwj;👦",
      family_man_girl: "👨&zwj;👧",
      family_man_girl_boy: "👨&zwj;👧&zwj;👦",
      family_man_girl_girl: "👨&zwj;👧&zwj;👧",
      family_man_man_boy: "👨&zwj;👨&zwj;👦",
      family_man_man_boy_boy: "👨&zwj;👨&zwj;👦&zwj;👦",
      family_man_man_girl: "👨&zwj;👨&zwj;👧",
      family_man_man_girl_boy: "👨&zwj;👨&zwj;👧&zwj;👦",
      family_man_man_girl_girl: "👨&zwj;👨&zwj;👧&zwj;👧",
      family_man_woman_boy_boy: "👨&zwj;👩&zwj;👦&zwj;👦",
      family_man_woman_girl: "👨&zwj;👩&zwj;👧",
      family_man_woman_girl_boy: "👨&zwj;👩&zwj;👧&zwj;👦",
      family_man_woman_girl_girl: "👨&zwj;👩&zwj;👧&zwj;👧",
      family_woman_boy: "👩&zwj;👦",
      family_woman_boy_boy: "👩&zwj;👦&zwj;👦",
      family_woman_girl: "👩&zwj;👧",
      family_woman_girl_boy: "👩&zwj;👧&zwj;👦",
      family_woman_girl_girl: "👩&zwj;👧&zwj;👧",
      family_woman_woman_boy: "👩&zwj;👩&zwj;👦",
      family_woman_woman_boy_boy: "👩&zwj;👩&zwj;👦&zwj;👦",
      family_woman_woman_girl: "👩&zwj;👩&zwj;👧",
      family_woman_woman_girl_boy: "👩&zwj;👩&zwj;👧&zwj;👦",
      family_woman_woman_girl_girl: "👩&zwj;👩&zwj;👧&zwj;👧",
      fast_forward: "⏩",
      fax: "📠",
      fearful: "😨",
      feet: "🐾",
      female_detective: "🕵️&zwj;♀️",
      ferris_wheel: "🎡",
      ferry: "⛴",
      field_hockey: "🏑",
      file_cabinet: "🗄",
      file_folder: "📁",
      film_projector: "📽",
      film_strip: "🎞",
      fire: "🔥",
      fire_engine: "🚒",
      fireworks: "🎆",
      first_quarter_moon: "🌓",
      first_quarter_moon_with_face: "🌛",
      fish: "🐟",
      fish_cake: "🍥",
      fishing_pole_and_fish: "🎣",
      fist_raised: "✊",
      fist_left: "🤛",
      fist_right: "🤜",
      flags: "🎏",
      flashlight: "🔦",
      fleur_de_lis: "⚜️",
      flight_arrival: "🛬",
      flight_departure: "🛫",
      floppy_disk: "💾",
      flower_playing_cards: "🎴",
      flushed: "😳",
      fog: "🌫",
      foggy: "🌁",
      football: "🏈",
      footprints: "👣",
      fork_and_knife: "🍴",
      fountain: "⛲️",
      fountain_pen: "🖋",
      four_leaf_clover: "🍀",
      fox_face: "🦊",
      framed_picture: "🖼",
      free: "🆓",
      fried_egg: "🍳",
      fried_shrimp: "🍤",
      fries: "🍟",
      frog: "🐸",
      frowning: "😦",
      frowning_face: "☹️",
      frowning_man: "🙍&zwj;♂️",
      frowning_woman: "🙍",
      middle_finger: "🖕",
      fuelpump: "⛽️",
      full_moon: "🌕",
      full_moon_with_face: "🌝",
      funeral_urn: "⚱️",
      game_die: "🎲",
      gear: "⚙️",
      gem: "💎",
      gemini: "♊️",
      ghost: "👻",
      gift: "🎁",
      gift_heart: "💝",
      girl: "👧",
      globe_with_meridians: "🌐",
      goal_net: "🥅",
      goat: "🐐",
      golf: "⛳️",
      golfing_man: "🏌️",
      golfing_woman: "🏌️&zwj;♀️",
      gorilla: "🦍",
      grapes: "🍇",
      green_apple: "🍏",
      green_book: "📗",
      green_heart: "💚",
      green_salad: "🥗",
      grey_exclamation: "❕",
      grey_question: "❔",
      grimacing: "😬",
      grin: "😁",
      grinning: "😀",
      guardsman: "💂",
      guardswoman: "💂&zwj;♀️",
      guitar: "🎸",
      gun: "🔫",
      haircut_woman: "💇",
      haircut_man: "💇&zwj;♂️",
      hamburger: "🍔",
      hammer: "🔨",
      hammer_and_pick: "⚒",
      hammer_and_wrench: "🛠",
      hamster: "🐹",
      hand: "✋",
      handbag: "👜",
      handshake: "🤝",
      hankey: "💩",
      hatched_chick: "🐥",
      hatching_chick: "🐣",
      headphones: "🎧",
      hear_no_evil: "🙉",
      heart: "❤️",
      heart_decoration: "💟",
      heart_eyes: "😍",
      heart_eyes_cat: "😻",
      heartbeat: "💓",
      heartpulse: "💗",
      hearts: "♥️",
      heavy_check_mark: "✔️",
      heavy_division_sign: "➗",
      heavy_dollar_sign: "💲",
      heavy_heart_exclamation: "❣️",
      heavy_minus_sign: "➖",
      heavy_multiplication_x: "✖️",
      heavy_plus_sign: "➕",
      helicopter: "🚁",
      herb: "🌿",
      hibiscus: "🌺",
      high_brightness: "🔆",
      high_heel: "👠",
      hocho: "🔪",
      hole: "🕳",
      honey_pot: "🍯",
      horse: "🐴",
      horse_racing: "🏇",
      hospital: "🏥",
      hot_pepper: "🌶",
      hotdog: "🌭",
      hotel: "🏨",
      hotsprings: "♨️",
      hourglass: "⌛️",
      hourglass_flowing_sand: "⏳",
      house: "🏠",
      house_with_garden: "🏡",
      houses: "🏘",
      hugs: "🤗",
      hushed: "😯",
      ice_cream: "🍨",
      ice_hockey: "🏒",
      ice_skate: "⛸",
      icecream: "🍦",
      id: "🆔",
      ideograph_advantage: "🉐",
      imp: "👿",
      inbox_tray: "📥",
      incoming_envelope: "📨",
      tipping_hand_woman: "💁",
      information_source: "ℹ️",
      innocent: "😇",
      interrobang: "⁉️",
      iphone: "📱",
      izakaya_lantern: "🏮",
      jack_o_lantern: "🎃",
      japan: "🗾",
      japanese_castle: "🏯",
      japanese_goblin: "👺",
      japanese_ogre: "👹",
      jeans: "👖",
      joy: "😂",
      joy_cat: "😹",
      joystick: "🕹",
      kaaba: "🕋",
      key: "🔑",
      keyboard: "⌨️",
      keycap_ten: "🔟",
      kick_scooter: "🛴",
      kimono: "👘",
      kiss: "💋",
      kissing: "😗",
      kissing_cat: "😽",
      kissing_closed_eyes: "😚",
      kissing_heart: "😘",
      kissing_smiling_eyes: "😙",
      kiwi_fruit: "🥝",
      koala: "🐨",
      koko: "🈁",
      label: "🏷",
      large_blue_circle: "🔵",
      large_blue_diamond: "🔷",
      large_orange_diamond: "🔶",
      last_quarter_moon: "🌗",
      last_quarter_moon_with_face: "🌜",
      latin_cross: "✝️",
      laughing: "😆",
      leaves: "🍃",
      ledger: "📒",
      left_luggage: "🛅",
      left_right_arrow: "↔️",
      leftwards_arrow_with_hook: "↩️",
      lemon: "🍋",
      leo: "♌️",
      leopard: "🐆",
      level_slider: "🎚",
      libra: "♎️",
      light_rail: "🚈",
      link: "🔗",
      lion: "🦁",
      lips: "👄",
      lipstick: "💄",
      lizard: "🦎",
      lock: "🔒",
      lock_with_ink_pen: "🔏",
      lollipop: "🍭",
      loop: "➿",
      loud_sound: "🔊",
      loudspeaker: "📢",
      love_hotel: "🏩",
      love_letter: "💌",
      low_brightness: "🔅",
      lying_face: "🤥",
      m: "Ⓜ️",
      mag: "🔍",
      mag_right: "🔎",
      mahjong: "🀄️",
      mailbox: "📫",
      mailbox_closed: "📪",
      mailbox_with_mail: "📬",
      mailbox_with_no_mail: "📭",
      man: "👨",
      man_artist: "👨&zwj;🎨",
      man_astronaut: "👨&zwj;🚀",
      man_cartwheeling: "🤸&zwj;♂️",
      man_cook: "👨&zwj;🍳",
      man_dancing: "🕺",
      man_facepalming: "🤦&zwj;♂️",
      man_factory_worker: "👨&zwj;🏭",
      man_farmer: "👨&zwj;🌾",
      man_firefighter: "👨&zwj;🚒",
      man_health_worker: "👨&zwj;⚕️",
      man_in_tuxedo: "🤵",
      man_judge: "👨&zwj;⚖️",
      man_juggling: "🤹&zwj;♂️",
      man_mechanic: "👨&zwj;🔧",
      man_office_worker: "👨&zwj;💼",
      man_pilot: "👨&zwj;✈️",
      man_playing_handball: "🤾&zwj;♂️",
      man_playing_water_polo: "🤽&zwj;♂️",
      man_scientist: "👨&zwj;🔬",
      man_shrugging: "🤷&zwj;♂️",
      man_singer: "👨&zwj;🎤",
      man_student: "👨&zwj;🎓",
      man_teacher: "👨&zwj;🏫",
      man_technologist: "👨&zwj;💻",
      man_with_gua_pi_mao: "👲",
      man_with_turban: "👳",
      tangerine: "🍊",
      mans_shoe: "👞",
      mantelpiece_clock: "🕰",
      maple_leaf: "🍁",
      martial_arts_uniform: "🥋",
      mask: "😷",
      massage_woman: "💆",
      massage_man: "💆&zwj;♂️",
      meat_on_bone: "🍖",
      medal_military: "🎖",
      medal_sports: "🏅",
      mega: "📣",
      melon: "🍈",
      memo: "📝",
      men_wrestling: "🤼&zwj;♂️",
      menorah: "🕎",
      mens: "🚹",
      metal: "🤘",
      metro: "🚇",
      microphone: "🎤",
      microscope: "🔬",
      milk_glass: "🥛",
      milky_way: "🌌",
      minibus: "🚐",
      minidisc: "💽",
      mobile_phone_off: "📴",
      money_mouth_face: "🤑",
      money_with_wings: "💸",
      moneybag: "💰",
      monkey: "🐒",
      monkey_face: "🐵",
      monorail: "🚝",
      moon: "🌔",
      mortar_board: "🎓",
      mosque: "🕌",
      motor_boat: "🛥",
      motor_scooter: "🛵",
      motorcycle: "🏍",
      motorway: "🛣",
      mount_fuji: "🗻",
      mountain: "⛰",
      mountain_biking_man: "🚵",
      mountain_biking_woman: "🚵&zwj;♀️",
      mountain_cableway: "🚠",
      mountain_railway: "🚞",
      mountain_snow: "🏔",
      mouse: "🐭",
      mouse2: "🐁",
      movie_camera: "🎥",
      moyai: "🗿",
      mrs_claus: "🤶",
      muscle: "💪",
      mushroom: "🍄",
      musical_keyboard: "🎹",
      musical_note: "🎵",
      musical_score: "🎼",
      mute: "🔇",
      nail_care: "💅",
      name_badge: "📛",
      national_park: "🏞",
      nauseated_face: "🤢",
      necktie: "👔",
      negative_squared_cross_mark: "❎",
      nerd_face: "🤓",
      neutral_face: "😐",
      new: "🆕",
      new_moon: "🌑",
      new_moon_with_face: "🌚",
      newspaper: "📰",
      newspaper_roll: "🗞",
      next_track_button: "⏭",
      ng: "🆖",
      no_good_man: "🙅&zwj;♂️",
      no_good_woman: "🙅",
      night_with_stars: "🌃",
      no_bell: "🔕",
      no_bicycles: "🚳",
      no_entry: "⛔️",
      no_entry_sign: "🚫",
      no_mobile_phones: "📵",
      no_mouth: "😶",
      no_pedestrians: "🚷",
      no_smoking: "🚭",
      "non-potable_water": "🚱",
      nose: "👃",
      notebook: "📓",
      notebook_with_decorative_cover: "📔",
      notes: "🎶",
      nut_and_bolt: "🔩",
      o: "⭕️",
      o2: "🅾️",
      ocean: "🌊",
      octopus: "🐙",
      oden: "🍢",
      office: "🏢",
      oil_drum: "🛢",
      ok: "🆗",
      ok_hand: "👌",
      ok_man: "🙆&zwj;♂️",
      ok_woman: "🙆",
      old_key: "🗝",
      older_man: "👴",
      older_woman: "👵",
      om: "🕉",
      on: "🔛",
      oncoming_automobile: "🚘",
      oncoming_bus: "🚍",
      oncoming_police_car: "🚔",
      oncoming_taxi: "🚖",
      open_file_folder: "📂",
      open_hands: "👐",
      open_mouth: "😮",
      open_umbrella: "☂️",
      ophiuchus: "⛎",
      orange_book: "📙",
      orthodox_cross: "☦️",
      outbox_tray: "📤",
      owl: "🦉",
      ox: "🐂",
      package: "📦",
      page_facing_up: "📄",
      page_with_curl: "📃",
      pager: "📟",
      paintbrush: "🖌",
      palm_tree: "🌴",
      pancakes: "🥞",
      panda_face: "🐼",
      paperclip: "📎",
      paperclips: "🖇",
      parasol_on_ground: "⛱",
      parking: "🅿️",
      part_alternation_mark: "〽️",
      partly_sunny: "⛅️",
      passenger_ship: "🛳",
      passport_control: "🛂",
      pause_button: "⏸",
      peace_symbol: "☮️",
      peach: "🍑",
      peanuts: "🥜",
      pear: "🍐",
      pen: "🖊",
      pencil2: "✏️",
      penguin: "🐧",
      pensive: "😔",
      performing_arts: "🎭",
      persevere: "😣",
      person_fencing: "🤺",
      pouting_woman: "🙎",
      phone: "☎️",
      pick: "⛏",
      pig: "🐷",
      pig2: "🐖",
      pig_nose: "🐽",
      pill: "💊",
      pineapple: "🍍",
      ping_pong: "🏓",
      pisces: "♓️",
      pizza: "🍕",
      place_of_worship: "🛐",
      plate_with_cutlery: "🍽",
      play_or_pause_button: "⏯",
      point_down: "👇",
      point_left: "👈",
      point_right: "👉",
      point_up: "☝️",
      point_up_2: "👆",
      police_car: "🚓",
      policewoman: "👮&zwj;♀️",
      poodle: "🐩",
      popcorn: "🍿",
      post_office: "🏣",
      postal_horn: "📯",
      postbox: "📮",
      potable_water: "🚰",
      potato: "🥔",
      pouch: "👝",
      poultry_leg: "🍗",
      pound: "💷",
      rage: "😡",
      pouting_cat: "😾",
      pouting_man: "🙎&zwj;♂️",
      pray: "🙏",
      prayer_beads: "📿",
      pregnant_woman: "🤰",
      previous_track_button: "⏮",
      prince: "🤴",
      princess: "👸",
      printer: "🖨",
      purple_heart: "💜",
      purse: "👛",
      pushpin: "📌",
      put_litter_in_its_place: "🚮",
      question: "❓",
      rabbit: "🐰",
      rabbit2: "🐇",
      racehorse: "🐎",
      racing_car: "🏎",
      radio: "📻",
      radio_button: "🔘",
      radioactive: "☢️",
      railway_car: "🚃",
      railway_track: "🛤",
      rainbow: "🌈",
      rainbow_flag: "🏳️&zwj;🌈",
      raised_back_of_hand: "🤚",
      raised_hand_with_fingers_splayed: "🖐",
      raised_hands: "🙌",
      raising_hand_woman: "🙋",
      raising_hand_man: "🙋&zwj;♂️",
      ram: "🐏",
      ramen: "🍜",
      rat: "🐀",
      record_button: "⏺",
      recycle: "♻️",
      red_circle: "🔴",
      registered: "®️",
      relaxed: "☺️",
      relieved: "😌",
      reminder_ribbon: "🎗",
      repeat: "🔁",
      repeat_one: "🔂",
      rescue_worker_helmet: "⛑",
      restroom: "🚻",
      revolving_hearts: "💞",
      rewind: "⏪",
      rhinoceros: "🦏",
      ribbon: "🎀",
      rice: "🍚",
      rice_ball: "🍙",
      rice_cracker: "🍘",
      rice_scene: "🎑",
      right_anger_bubble: "🗯",
      ring: "💍",
      robot: "🤖",
      rocket: "🚀",
      rofl: "🤣",
      roll_eyes: "🙄",
      roller_coaster: "🎢",
      rooster: "🐓",
      rose: "🌹",
      rosette: "🏵",
      rotating_light: "🚨",
      round_pushpin: "📍",
      rowing_man: "🚣",
      rowing_woman: "🚣&zwj;♀️",
      rugby_football: "🏉",
      running_man: "🏃",
      running_shirt_with_sash: "🎽",
      running_woman: "🏃&zwj;♀️",
      sa: "🈂️",
      sagittarius: "♐️",
      sake: "🍶",
      sandal: "👡",
      santa: "🎅",
      satellite: "📡",
      saxophone: "🎷",
      school: "🏫",
      school_satchel: "🎒",
      scissors: "✂️",
      scorpion: "🦂",
      scorpius: "♏️",
      scream: "😱",
      scream_cat: "🙀",
      scroll: "📜",
      seat: "💺",
      secret: "㊙️",
      see_no_evil: "🙈",
      seedling: "🌱",
      selfie: "🤳",
      shallow_pan_of_food: "🥘",
      shamrock: "☘️",
      shark: "🦈",
      shaved_ice: "🍧",
      sheep: "🐑",
      shell: "🐚",
      shield: "🛡",
      shinto_shrine: "⛩",
      ship: "🚢",
      shirt: "👕",
      shopping: "🛍",
      shopping_cart: "🛒",
      shower: "🚿",
      shrimp: "🦐",
      signal_strength: "📶",
      six_pointed_star: "🔯",
      ski: "🎿",
      skier: "⛷",
      skull: "💀",
      skull_and_crossbones: "☠️",
      sleeping: "😴",
      sleeping_bed: "🛌",
      sleepy: "😪",
      slightly_frowning_face: "🙁",
      slightly_smiling_face: "🙂",
      slot_machine: "🎰",
      small_airplane: "🛩",
      small_blue_diamond: "🔹",
      small_orange_diamond: "🔸",
      small_red_triangle: "🔺",
      small_red_triangle_down: "🔻",
      smile: "😄",
      smile_cat: "😸",
      smiley: "😃",
      smiley_cat: "😺",
      smiling_imp: "😈",
      smirk: "😏",
      smirk_cat: "😼",
      smoking: "🚬",
      snail: "🐌",
      snake: "🐍",
      sneezing_face: "🤧",
      snowboarder: "🏂",
      snowflake: "❄️",
      snowman: "⛄️",
      snowman_with_snow: "☃️",
      sob: "😭",
      soccer: "⚽️",
      soon: "🔜",
      sos: "🆘",
      sound: "🔉",
      space_invader: "👾",
      spades: "♠️",
      spaghetti: "🍝",
      sparkle: "❇️",
      sparkler: "🎇",
      sparkles: "✨",
      sparkling_heart: "💖",
      speak_no_evil: "🙊",
      speaker: "🔈",
      speaking_head: "🗣",
      speech_balloon: "💬",
      speedboat: "🚤",
      spider: "🕷",
      spider_web: "🕸",
      spiral_calendar: "🗓",
      spiral_notepad: "🗒",
      spoon: "🥄",
      squid: "🦑",
      stadium: "🏟",
      star: "⭐️",
      star2: "🌟",
      star_and_crescent: "☪️",
      star_of_david: "✡️",
      stars: "🌠",
      station: "🚉",
      statue_of_liberty: "🗽",
      steam_locomotive: "🚂",
      stew: "🍲",
      stop_button: "⏹",
      stop_sign: "🛑",
      stopwatch: "⏱",
      straight_ruler: "📏",
      strawberry: "🍓",
      stuck_out_tongue: "😛",
      stuck_out_tongue_closed_eyes: "😝",
      stuck_out_tongue_winking_eye: "😜",
      studio_microphone: "🎙",
      stuffed_flatbread: "🥙",
      sun_behind_large_cloud: "🌥",
      sun_behind_rain_cloud: "🌦",
      sun_behind_small_cloud: "🌤",
      sun_with_face: "🌞",
      sunflower: "🌻",
      sunglasses: "😎",
      sunny: "☀️",
      sunrise: "🌅",
      sunrise_over_mountains: "🌄",
      surfing_man: "🏄",
      surfing_woman: "🏄&zwj;♀️",
      sushi: "🍣",
      suspension_railway: "🚟",
      sweat: "😓",
      sweat_drops: "💦",
      sweat_smile: "😅",
      sweet_potato: "🍠",
      swimming_man: "🏊",
      swimming_woman: "🏊&zwj;♀️",
      symbols: "🔣",
      synagogue: "🕍",
      syringe: "💉",
      taco: "🌮",
      tada: "🎉",
      tanabata_tree: "🎋",
      taurus: "♉️",
      taxi: "🚕",
      tea: "🍵",
      telephone_receiver: "📞",
      telescope: "🔭",
      tennis: "🎾",
      tent: "⛺️",
      thermometer: "🌡",
      thinking: "🤔",
      thought_balloon: "💭",
      ticket: "🎫",
      tickets: "🎟",
      tiger: "🐯",
      tiger2: "🐅",
      timer_clock: "⏲",
      tipping_hand_man: "💁&zwj;♂️",
      tired_face: "😫",
      tm: "™️",
      toilet: "🚽",
      tokyo_tower: "🗼",
      tomato: "🍅",
      tongue: "👅",
      top: "🔝",
      tophat: "🎩",
      tornado: "🌪",
      trackball: "🖲",
      tractor: "🚜",
      traffic_light: "🚥",
      train: "🚋",
      train2: "🚆",
      tram: "🚊",
      triangular_flag_on_post: "🚩",
      triangular_ruler: "📐",
      trident: "🔱",
      triumph: "😤",
      trolleybus: "🚎",
      trophy: "🏆",
      tropical_drink: "🍹",
      tropical_fish: "🐠",
      truck: "🚚",
      trumpet: "🎺",
      tulip: "🌷",
      tumbler_glass: "🥃",
      turkey: "🦃",
      turtle: "🐢",
      tv: "📺",
      twisted_rightwards_arrows: "🔀",
      two_hearts: "💕",
      two_men_holding_hands: "👬",
      two_women_holding_hands: "👭",
      u5272: "🈹",
      u5408: "🈴",
      u55b6: "🈺",
      u6307: "🈯️",
      u6708: "🈷️",
      u6709: "🈶",
      u6e80: "🈵",
      u7121: "🈚️",
      u7533: "🈸",
      u7981: "🈲",
      u7a7a: "🈳",
      umbrella: "☔️",
      unamused: "😒",
      underage: "🔞",
      unicorn: "🦄",
      unlock: "🔓",
      up: "🆙",
      upside_down_face: "🙃",
      v: "✌️",
      vertical_traffic_light: "🚦",
      vhs: "📼",
      vibration_mode: "📳",
      video_camera: "📹",
      video_game: "🎮",
      violin: "🎻",
      virgo: "♍️",
      volcano: "🌋",
      volleyball: "🏐",
      vs: "🆚",
      vulcan_salute: "🖖",
      walking_man: "🚶",
      walking_woman: "🚶&zwj;♀️",
      waning_crescent_moon: "🌘",
      waning_gibbous_moon: "🌖",
      warning: "⚠️",
      wastebasket: "🗑",
      watch: "⌚️",
      water_buffalo: "🐃",
      watermelon: "🍉",
      wave: "👋",
      wavy_dash: "〰️",
      waxing_crescent_moon: "🌒",
      wc: "🚾",
      weary: "😩",
      wedding: "💒",
      weight_lifting_man: "🏋️",
      weight_lifting_woman: "🏋️&zwj;♀️",
      whale: "🐳",
      whale2: "🐋",
      wheel_of_dharma: "☸️",
      wheelchair: "♿️",
      white_check_mark: "✅",
      white_circle: "⚪️",
      white_flag: "🏳️",
      white_flower: "💮",
      white_large_square: "⬜️",
      white_medium_small_square: "◽️",
      white_medium_square: "◻️",
      white_small_square: "▫️",
      white_square_button: "🔳",
      wilted_flower: "🥀",
      wind_chime: "🎐",
      wind_face: "🌬",
      wine_glass: "🍷",
      wink: "😉",
      wolf: "🐺",
      woman: "👩",
      woman_artist: "👩&zwj;🎨",
      woman_astronaut: "👩&zwj;🚀",
      woman_cartwheeling: "🤸&zwj;♀️",
      woman_cook: "👩&zwj;🍳",
      woman_facepalming: "🤦&zwj;♀️",
      woman_factory_worker: "👩&zwj;🏭",
      woman_farmer: "👩&zwj;🌾",
      woman_firefighter: "👩&zwj;🚒",
      woman_health_worker: "👩&zwj;⚕️",
      woman_judge: "👩&zwj;⚖️",
      woman_juggling: "🤹&zwj;♀️",
      woman_mechanic: "👩&zwj;🔧",
      woman_office_worker: "👩&zwj;💼",
      woman_pilot: "👩&zwj;✈️",
      woman_playing_handball: "🤾&zwj;♀️",
      woman_playing_water_polo: "🤽&zwj;♀️",
      woman_scientist: "👩&zwj;🔬",
      woman_shrugging: "🤷&zwj;♀️",
      woman_singer: "👩&zwj;🎤",
      woman_student: "👩&zwj;🎓",
      woman_teacher: "👩&zwj;🏫",
      woman_technologist: "👩&zwj;💻",
      woman_with_turban: "👳&zwj;♀️",
      womans_clothes: "👚",
      womans_hat: "👒",
      women_wrestling: "🤼&zwj;♀️",
      womens: "🚺",
      world_map: "🗺",
      worried: "😟",
      wrench: "🔧",
      writing_hand: "✍️",
      x: "❌",
      yellow_heart: "💛",
      yen: "💴",
      yin_yang: "☯️",
      yum: "😋",
      zap: "⚡️",
      zipper_mouth_face: "🤐",
      zzz: "💤",
      octocat: '<img alt=":octocat:" height="20" width="20" align="absmiddle" src="https://assets-cdn.github.com/images/icons/emoji/octocat.png">',
      showdown: `<span style="font-family: 'Anonymous Pro', monospace; text-decoration: underline; text-decoration-style: dashed; text-decoration-color: #3e8b8a;text-underline-position: under;">S</span>`
    }, o.Converter = function(p) {
      "use strict";
      var b = {}, w = [], E = [], x = {}, M = l, N = {
        parsed: {},
        raw: "",
        format: ""
      };
      q();
      function q() {
        p = p || {};
        for (var B in i) i.hasOwnProperty(B) && (b[B] = i[B]);
        if (typeof p == "object")
          for (var X in p) p.hasOwnProperty(X) && (b[X] = p[X]);
        else throw Error("Converter expects the passed parameter to be an object, but " + typeof p + " was passed instead.");
        b.extensions && o.helper.forEach(b.extensions, z);
      }
      function z(B, X) {
        if (X = X || null, o.helper.isString(B))
          if (B = o.helper.stdExtName(B), X = B, o.extensions[B]) {
            console.warn("DEPRECATION WARNING: " + B + " is an old extension that uses a deprecated loading method.Please inform the developer that the extension should be updated!"), J(o.extensions[B], B);
            return;
          } else if (!o.helper.isUndefined(a[B])) B = a[B];
          else throw Error('Extension "' + B + '" could not be loaded. It was either not found or is not a valid extension.');
        typeof B == "function" && (B = B()), o.helper.isArray(B) || (B = [B]);
        var de = d(B, X);
        if (!de.valid) throw Error(de.error);
        for (var fe = 0; fe < B.length; ++fe) {
          switch (B[fe].type) {
            case "lang":
              w.push(B[fe]);
              break;
            case "output":
              E.push(B[fe]);
              break;
          }
          if (B[fe].hasOwnProperty("listeners"))
            for (var ve in B[fe].listeners) B[fe].listeners.hasOwnProperty(ve) && G(ve, B[fe].listeners[ve]);
        }
      }
      function J(B, X) {
        typeof B == "function" && (B = B(new o.Converter())), o.helper.isArray(B) || (B = [B]);
        var de = d(B, X);
        if (!de.valid) throw Error(de.error);
        for (var fe = 0; fe < B.length; ++fe) switch (B[fe].type) {
          case "lang":
            w.push(B[fe]);
            break;
          case "output":
            E.push(B[fe]);
            break;
          default:
            throw Error("Extension loader error: Type unrecognized!!!");
        }
      }
      function G(B, X) {
        if (!o.helper.isString(B)) throw Error("Invalid argument in converter.listen() method: name must be a string, but " + typeof B + " given");
        if (typeof X != "function") throw Error("Invalid argument in converter.listen() method: callback must be a function, but " + typeof X + " given");
        x.hasOwnProperty(B) || (x[B] = []), x[B].push(X);
      }
      function oe(B) {
        var X = B.match(/^\s*/)[0].length, de = new RegExp("^\\s{0," + X + "}", "gm");
        return B.replace(de, "");
      }
      this._dispatch = function(X, de, fe, ve) {
        if (x.hasOwnProperty(X)) for (var Ce = 0; Ce < x[X].length; ++Ce) {
          var Ue = x[X][Ce](X, de, this, fe, ve);
          Ue && typeof Ue < "u" && (de = Ue);
        }
        return de;
      }, this.listen = function(B, X) {
        return G(B, X), this;
      }, this.makeHtml = function(B) {
        if (!B) return B;
        var X = {
          gHtmlBlocks: [],
          gHtmlMdBlocks: [],
          gHtmlSpans: [],
          gUrls: {},
          gTitles: {},
          gDimensions: {},
          gListLevel: 0,
          hashLinkCounts: {},
          langExtensions: w,
          outputModifiers: E,
          converter: this,
          ghCodeBlocks: [],
          metadata: {
            parsed: {},
            raw: "",
            format: ""
          }
        };
        return B = B.replace(/¨/g, "¨T"), B = B.replace(/\$/g, "¨D"), B = B.replace(/\r\n/g, `
`), B = B.replace(/\r/g, `
`), B = B.replace(/\u00A0/g, "&nbsp;"), b.smartIndentationFix && (B = oe(B)), B = `

` + B + `

`, B = o.subParser("detab")(B, b, X), B = B.replace(/^[ \t]+$/gm, ""), o.helper.forEach(w, function(de) {
          B = o.subParser("runExtension")(de, B, b, X);
        }), B = o.subParser("metadata")(B, b, X), B = o.subParser("hashPreCodeTags")(B, b, X), B = o.subParser("githubCodeBlocks")(B, b, X), B = o.subParser("hashHTMLBlocks")(B, b, X), B = o.subParser("hashCodeTags")(B, b, X), B = o.subParser("stripLinkDefinitions")(B, b, X), B = o.subParser("blockGamut")(B, b, X), B = o.subParser("unhashHTMLSpans")(B, b, X), B = o.subParser("unescapeSpecialChars")(B, b, X), B = B.replace(/¨D/g, "$$"), B = B.replace(/¨T/g, "¨"), B = o.subParser("completeHTMLDocument")(B, b, X), o.helper.forEach(E, function(de) {
          B = o.subParser("runExtension")(de, B, b, X);
        }), N = X.metadata, B;
      }, this.makeMarkdown = this.makeMd = function(B, X) {
        if (B = B.replace(/\r\n/g, `
`), B = B.replace(/\r/g, `
`), B = B.replace(/>[ \t]+</, ">¨NBSP;<"), !X) if (window && window.document) X = window.document;
        else throw new Error("HTMLParser is undefined. If in a webworker or nodejs environment, you need to provide a WHATWG DOM and HTML such as JSDOM");
        var de = X.createElement("div");
        de.innerHTML = B;
        var fe = { preList: me(de) };
        le(de);
        for (var ve = de.childNodes, Ce = "", Ue = 0; Ue < ve.length; Ue++) Ce += o.subParser("makeMarkdown.node")(ve[Ue], fe);
        function le(We) {
          for (var H = 0; H < We.childNodes.length; ++H) {
            var W = We.childNodes[H];
            W.nodeType === 3 ? !/\S/.test(W.nodeValue) && !/^[ ]+$/.test(W.nodeValue) ? (We.removeChild(W), --H) : (W.nodeValue = W.nodeValue.split(`
`).join(" "), W.nodeValue = W.nodeValue.replace(/(\s)+/g, "$1")) : W.nodeType === 1 && le(W);
          }
        }
        function me(We) {
          for (var H = We.querySelectorAll("pre"), W = [], se = 0; se < H.length; ++se) if (H[se].childElementCount === 1 && H[se].firstChild.tagName.toLowerCase() === "code") {
            var ke = H[se].firstChild.innerHTML.trim(), Pe = H[se].firstChild.getAttribute("data-language") || "";
            if (Pe === "")
              for (var Le = H[se].firstChild.className.split(" "), Y = 0; Y < Le.length; ++Y) {
                var te = Le[Y].match(/^language-(.+)$/);
                if (te !== null) {
                  Pe = te[1];
                  break;
                }
              }
            ke = o.helper.unescapeHTMLEntities(ke), W.push(ke), H[se].outerHTML = '<precode language="' + Pe + '" precodenum="' + se.toString() + '"></precode>';
          } else
            W.push(H[se].innerHTML), H[se].innerHTML = "", H[se].setAttribute("prenum", se.toString());
          return W;
        }
        return Ce;
      }, this.setOption = function(B, X) {
        b[B] = X;
      }, this.getOption = function(B) {
        return b[B];
      }, this.getOptions = function() {
        return b;
      }, this.addExtension = function(B, X) {
        X = X || null, z(B, X);
      }, this.useExtension = function(B) {
        z(B);
      }, this.setFlavor = function(B) {
        if (!u.hasOwnProperty(B)) throw Error(B + " flavor was not found");
        var X = u[B];
        M = B;
        for (var de in X) X.hasOwnProperty(de) && (b[de] = X[de]);
      }, this.getFlavor = function() {
        return M;
      }, this.removeExtension = function(B) {
        o.helper.isArray(B) || (B = [B]);
        for (var X = 0; X < B.length; ++X) {
          for (var de = B[X], fe = 0; fe < w.length; ++fe) w[fe] === de && w.splice(fe, 1);
          for (var ve = 0; ve < E.length; ++ve) E[ve] === de && E.splice(ve, 1);
        }
      }, this.getAllExtensions = function() {
        return {
          language: w,
          output: E
        };
      }, this.getMetadata = function(B) {
        return B ? N.raw : N.parsed;
      }, this.getMetadataFormat = function() {
        return N.format;
      }, this._setMetadataPair = function(B, X) {
        N.parsed[B] = X;
      }, this._setMetadataFormat = function(B) {
        N.format = B;
      }, this._setMetadataRaw = function(B) {
        N.raw = B;
      };
    }, o.subParser("anchors", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("anchors.before", p, b, w);
      var E = function(x, M, N, q, z, J, G) {
        if (o.helper.isUndefined(G) && (G = ""), N = N.toLowerCase(), x.search(/\(<?\s*>? ?(['"].*['"])?\)$/m) > -1) q = "";
        else if (!q)
          if (N || (N = M.toLowerCase().replace(/ ?\n/g, " ")), q = "#" + N, !o.helper.isUndefined(w.gUrls[N]))
            q = w.gUrls[N], o.helper.isUndefined(w.gTitles[N]) || (G = w.gTitles[N]);
          else return x;
        q = q.replace(o.helper.regexes.asteriskDashAndColon, o.helper.escapeCharactersCallback);
        var oe = '<a href="' + q + '"';
        return G !== "" && G !== null && (G = G.replace(/"/g, "&quot;"), G = G.replace(o.helper.regexes.asteriskDashAndColon, o.helper.escapeCharactersCallback), oe += ' title="' + G + '"'), b.openLinksInNewWindow && !/^#/.test(q) && (oe += ' rel="noopener noreferrer" target="¨E95Eblank"'), oe += ">" + M + "</a>", oe;
      };
      return p = p.replace(/\[((?:\[[^\]]*]|[^\[\]])*)] ?(?:\n *)?\[(.*?)]()()()()/g, E), p = p.replace(/\[((?:\[[^\]]*]|[^\[\]])*)]()[ \t]*\([ \t]?<([^>]*)>(?:[ \t]*((["'])([^"]*?)\5))?[ \t]?\)/g, E), p = p.replace(/\[((?:\[[^\]]*]|[^\[\]])*)]()[ \t]*\([ \t]?<?([\S]+?(?:\([\S]*?\)[\S]*?)?)>?(?:[ \t]*((["'])([^"]*?)\5))?[ \t]?\)/g, E), p = p.replace(/\[([^\[\]]+)]()()()()()/g, E), b.ghMentions && (p = p.replace(/(^|\s)(\\)?(@([a-z\d]+(?:[a-z\d.-]+?[a-z\d]+)*))/gim, function(x, M, N, q, z) {
        if (N === "\\") return M + q;
        if (!o.helper.isString(b.ghMentionsLink)) throw new Error("ghMentionsLink option must be a string");
        var J = b.ghMentionsLink.replace(/\{u}/g, z), G = "";
        return b.openLinksInNewWindow && (G = ' rel="noopener noreferrer" target="¨E95Eblank"'), M + '<a href="' + J + '"' + G + ">" + q + "</a>";
      })), p = w.converter._dispatch("anchors.after", p, b, w), p;
    });
    var v = /([*~_]+|\b)(((https?|ftp|dict):\/\/|www\.)[^'">\s]+?\.[^'">\s]+?)()(\1)?(?=\s|$)(?!["<>])/gi, y = /([*~_]+|\b)(((https?|ftp|dict):\/\/|www\.)[^'">\s]+\.[^'">\s]+?)([.!?,()\[\]])?(\1)?(?=\s|$)(?!["<>])/gi, T = /()<(((https?|ftp|dict):\/\/|www\.)[^'">\s]+)()>()/gi, C = /(^|\s)(?:mailto:)?([A-Za-z0-9!#$%&'*+-/=?^_`{|}~.]+@[-a-z0-9]+(\.[-a-z0-9]+)*\.[a-z]+)(?=$|\s)/gim, A = /<()(?:mailto:)?([-.\w]+@[-a-z0-9]+(\.[-a-z0-9]+)*\.[a-z]+)>/gi, I = function(p) {
      "use strict";
      return function(b, w, E, x, M, N, q) {
        E = E.replace(o.helper.regexes.asteriskDashAndColon, o.helper.escapeCharactersCallback);
        var z = E, J = "", G = "", oe = w || "", B = q || "";
        return /^www\./i.test(E) && (E = E.replace(/^www\./i, "http://www.")), p.excludeTrailingPunctuationFromURLs && N && (J = N), p.openLinksInNewWindow && (G = ' rel="noopener noreferrer" target="¨E95Eblank"'), oe + '<a href="' + E + '"' + G + ">" + z + "</a>" + J + B;
      };
    }, R = function(p, b) {
      "use strict";
      return function(w, E, x) {
        var M = "mailto:";
        return E = E || "", x = o.subParser("unescapeSpecialChars")(x, p, b), p.encodeEmails ? (M = o.helper.encodeEmailAddress(M + x), x = o.helper.encodeEmailAddress(x)) : M = M + x, E + '<a href="' + M + '">' + x + "</a>";
      };
    };
    o.subParser("autoLinks", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("autoLinks.before", p, b, w), p = p.replace(T, I(b)), p = p.replace(A, R(b, w)), p = w.converter._dispatch("autoLinks.after", p, b, w), p;
    }), o.subParser("simplifiedAutoLinks", function(p, b, w) {
      "use strict";
      return b.simplifiedAutoLink && (p = w.converter._dispatch("simplifiedAutoLinks.before", p, b, w), b.excludeTrailingPunctuationFromURLs ? p = p.replace(y, I(b)) : p = p.replace(v, I(b)), p = p.replace(C, R(b, w)), p = w.converter._dispatch("simplifiedAutoLinks.after", p, b, w)), p;
    }), o.subParser("blockGamut", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("blockGamut.before", p, b, w), p = o.subParser("blockQuotes")(p, b, w), p = o.subParser("headers")(p, b, w), p = o.subParser("horizontalRule")(p, b, w), p = o.subParser("lists")(p, b, w), p = o.subParser("codeBlocks")(p, b, w), p = o.subParser("tables")(p, b, w), p = o.subParser("hashHTMLBlocks")(p, b, w), p = o.subParser("paragraphs")(p, b, w), p = w.converter._dispatch("blockGamut.after", p, b, w), p;
    }), o.subParser("blockQuotes", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("blockQuotes.before", p, b, w), p = p + `

`;
      var E = /(^ {0,3}>[ \t]?.+\n(.+\n)*\n*)+/gm;
      return b.splitAdjacentBlockquotes && (E = /^ {0,3}>[\s\S]*?(?:\n\n)/gm), p = p.replace(E, function(x) {
        return x = x.replace(/^[ \t]*>[ \t]?/gm, ""), x = x.replace(/¨0/g, ""), x = x.replace(/^[ \t]+$/gm, ""), x = o.subParser("githubCodeBlocks")(x, b, w), x = o.subParser("blockGamut")(x, b, w), x = x.replace(/(^|\n)/g, "$1  "), x = x.replace(/(\s*<pre>[^\r]+?<\/pre>)/gm, function(M, N) {
          var q = N;
          return q = q.replace(/^  /gm, "¨0"), q = q.replace(/¨0/g, ""), q;
        }), o.subParser("hashBlock")(`<blockquote>
` + x + `
</blockquote>`, b, w);
      }), p = w.converter._dispatch("blockQuotes.after", p, b, w), p;
    }), o.subParser("codeBlocks", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("codeBlocks.before", p, b, w), p += "¨0", p = p.replace(/(?:\n\n|^)((?:(?:[ ]{4}|\t).*\n+)+)(\n*[ ]{0,3}[^ \t\n]|(?=¨0))/g, function(E, x, M) {
        var N = x, q = M, z = `
`;
        return N = o.subParser("outdent")(N, b, w), N = o.subParser("encodeCode")(N, b, w), N = o.subParser("detab")(N, b, w), N = N.replace(/^\n+/g, ""), N = N.replace(/\n+$/g, ""), b.omitExtraWLInCodeBlocks && (z = ""), N = "<pre><code>" + N + z + "</code></pre>", o.subParser("hashBlock")(N, b, w) + q;
      }), p = p.replace(/¨0/, ""), p = w.converter._dispatch("codeBlocks.after", p, b, w), p;
    }), o.subParser("codeSpans", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("codeSpans.before", p, b, w), typeof p > "u" && (p = ""), p = p.replace(/(^|[^\\])(`+)([^\r]*?[^`])\2(?!`)/gm, function(E, x, M, N) {
        var q = N;
        return q = q.replace(/^([ \t]*)/g, ""), q = q.replace(/[ \t]*$/g, ""), q = o.subParser("encodeCode")(q, b, w), q = x + "<code>" + q + "</code>", q = o.subParser("hashHTMLSpans")(q, b, w), q;
      }), p = w.converter._dispatch("codeSpans.after", p, b, w), p;
    }), o.subParser("completeHTMLDocument", function(p, b, w) {
      "use strict";
      if (!b.completeHTMLDocument) return p;
      p = w.converter._dispatch("completeHTMLDocument.before", p, b, w);
      var E = "html", x = `<!DOCTYPE HTML>
`, M = "", N = `<meta charset="utf-8">
`, q = "", z = "";
      typeof w.metadata.parsed.doctype < "u" && (x = "<!DOCTYPE " + w.metadata.parsed.doctype + `>
`, E = w.metadata.parsed.doctype.toString().toLowerCase(), (E === "html" || E === "html5") && (N = '<meta charset="utf-8">'));
      for (var J in w.metadata.parsed) if (w.metadata.parsed.hasOwnProperty(J)) switch (J.toLowerCase()) {
        case "doctype":
          break;
        case "title":
          M = "<title>" + w.metadata.parsed.title + `</title>
`;
          break;
        case "charset":
          E === "html" || E === "html5" ? N = '<meta charset="' + w.metadata.parsed.charset + `">
` : N = '<meta name="charset" content="' + w.metadata.parsed.charset + `">
`;
          break;
        case "language":
        case "lang":
          q = ' lang="' + w.metadata.parsed[J] + '"', z += '<meta name="' + J + '" content="' + w.metadata.parsed[J] + `">
`;
          break;
        default:
          z += '<meta name="' + J + '" content="' + w.metadata.parsed[J] + `">
`;
      }
      return p = x + "<html" + q + `>
<head>
` + M + N + z + `</head>
<body>
` + p.trim() + `
</body>
</html>`, p = w.converter._dispatch("completeHTMLDocument.after", p, b, w), p;
    }), o.subParser("detab", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("detab.before", p, b, w), p = p.replace(/\t(?=\t)/g, "    "), p = p.replace(/\t/g, "¨A¨B"), p = p.replace(/¨B(.+?)¨A/g, function(E, x) {
        for (var M = x, N = 4 - M.length % 4, q = 0; q < N; q++) M += " ";
        return M;
      }), p = p.replace(/¨A/g, "    "), p = p.replace(/¨B/g, ""), p = w.converter._dispatch("detab.after", p, b, w), p;
    }), o.subParser("ellipsis", function(p, b, w) {
      "use strict";
      return b.ellipsis && (p = w.converter._dispatch("ellipsis.before", p, b, w), p = p.replace(/\.\.\./g, "…"), p = w.converter._dispatch("ellipsis.after", p, b, w)), p;
    }), o.subParser("emoji", function(p, b, w) {
      "use strict";
      return b.emoji && (p = w.converter._dispatch("emoji.before", p, b, w), p = p.replace(/:([\S]+?):/g, function(E, x) {
        return o.helper.emojis.hasOwnProperty(x) ? o.helper.emojis[x] : E;
      }), p = w.converter._dispatch("emoji.after", p, b, w)), p;
    }), o.subParser("encodeAmpsAndAngles", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("encodeAmpsAndAngles.before", p, b, w), p = p.replace(/&(?!#?[xX]?(?:[0-9a-fA-F]+|\w+);)/g, "&amp;"), p = p.replace(/<(?![a-z\/?$!])/gi, "&lt;"), p = p.replace(/</g, "&lt;"), p = p.replace(/>/g, "&gt;"), p = w.converter._dispatch("encodeAmpsAndAngles.after", p, b, w), p;
    }), o.subParser("encodeBackslashEscapes", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("encodeBackslashEscapes.before", p, b, w), p = p.replace(/\\(\\)/g, o.helper.escapeCharactersCallback), p = p.replace(/\\([`*_{}\[\]()>#+.!~=|:-])/g, o.helper.escapeCharactersCallback), p = w.converter._dispatch("encodeBackslashEscapes.after", p, b, w), p;
    }), o.subParser("encodeCode", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("encodeCode.before", p, b, w), p = p.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/([*_{}\[\]\\=~-])/g, o.helper.escapeCharactersCallback), p = w.converter._dispatch("encodeCode.after", p, b, w), p;
    }), o.subParser("escapeSpecialCharsWithinTagAttributes", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("escapeSpecialCharsWithinTagAttributes.before", p, b, w);
      var E = /<\/?[a-z\d_:-]+(?:[\s]+[\s\S]+?)?>/gi, x = /<!(--(?:(?:[^>-]|-[^>])(?:[^-]|-[^-])*)--)>/gi;
      return p = p.replace(E, function(M) {
        return M.replace(/(.)<\/?code>(?=.)/g, "$1`").replace(/([\\`*_~=|])/g, o.helper.escapeCharactersCallback);
      }), p = p.replace(x, function(M) {
        return M.replace(/([\\`*_~=|])/g, o.helper.escapeCharactersCallback);
      }), p = w.converter._dispatch("escapeSpecialCharsWithinTagAttributes.after", p, b, w), p;
    }), o.subParser("githubCodeBlocks", function(p, b, w) {
      "use strict";
      return b.ghCodeBlocks ? (p = w.converter._dispatch("githubCodeBlocks.before", p, b, w), p += "¨0", p = p.replace(/(?:^|\n)(?: {0,3})(```+|~~~+)(?: *)([^\s`~]*)\n([\s\S]*?)\n(?: {0,3})\1/g, function(E, x, M, N) {
        var q = b.omitExtraWLInCodeBlocks ? "" : `
`;
        return N = o.subParser("encodeCode")(N, b, w), N = o.subParser("detab")(N, b, w), N = N.replace(/^\n+/g, ""), N = N.replace(/\n+$/g, ""), N = "<pre><code" + (M ? ' class="' + M + " language-" + M + '"' : "") + ">" + N + q + "</code></pre>", N = o.subParser("hashBlock")(N, b, w), `

¨G` + (w.ghCodeBlocks.push({
          text: E,
          codeblock: N
        }) - 1) + `G

`;
      }), p = p.replace(/¨0/, ""), w.converter._dispatch("githubCodeBlocks.after", p, b, w)) : p;
    }), o.subParser("hashBlock", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("hashBlock.before", p, b, w), p = p.replace(/(^\n+|\n+$)/g, ""), p = `

¨K` + (w.gHtmlBlocks.push(p) - 1) + `K

`, p = w.converter._dispatch("hashBlock.after", p, b, w), p;
    }), o.subParser("hashCodeTags", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("hashCodeTags.before", p, b, w);
      var E = function(x, M, N, q) {
        var z = N + o.subParser("encodeCode")(M, b, w) + q;
        return "¨C" + (w.gHtmlSpans.push(z) - 1) + "C";
      };
      return p = o.helper.replaceRecursiveRegExp(p, E, "<code\\b[^>]*>", "</code>", "gim"), p = w.converter._dispatch("hashCodeTags.after", p, b, w), p;
    }), o.subParser("hashElement", function(p, b, w) {
      "use strict";
      return function(E, x) {
        var M = x;
        return M = M.replace(/\n\n/g, `
`), M = M.replace(/^\n/, ""), M = M.replace(/\n+$/g, ""), M = `

¨K` + (w.gHtmlBlocks.push(M) - 1) + `K

`, M;
      };
    }), o.subParser("hashHTMLBlocks", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("hashHTMLBlocks.before", p, b, w);
      var E = [
        "pre",
        "div",
        "h1",
        "h2",
        "h3",
        "h4",
        "h5",
        "h6",
        "blockquote",
        "table",
        "dl",
        "ol",
        "ul",
        "script",
        "noscript",
        "form",
        "fieldset",
        "iframe",
        "math",
        "style",
        "section",
        "header",
        "footer",
        "nav",
        "article",
        "aside",
        "address",
        "audio",
        "canvas",
        "figure",
        "hgroup",
        "output",
        "video",
        "p"
      ], x = function(B, X, de, fe) {
        var ve = B;
        return de.search(/\bmarkdown\b/) !== -1 && (ve = de + w.converter.makeHtml(X) + fe), `

¨K` + (w.gHtmlBlocks.push(ve) - 1) + `K

`;
      };
      b.backslashEscapesHTMLTags && (p = p.replace(/\\<(\/?[^>]+?)>/g, function(B, X) {
        return "&lt;" + X + "&gt;";
      }));
      for (var M = 0; M < E.length; ++M)
        for (var N, q = new RegExp("^ {0,3}(<" + E[M] + "\\b[^>]*>)", "im"), z = "<" + E[M] + "\\b[^>]*>", J = "</" + E[M] + ">"; (N = o.helper.regexIndexOf(p, q)) !== -1; ) {
          var G = o.helper.splitAtIndex(p, N), oe = o.helper.replaceRecursiveRegExp(G[1], x, z, J, "im");
          if (oe === G[1]) break;
          p = G[0].concat(oe);
        }
      return p = p.replace(/(\n {0,3}(<(hr)\b([^<>])*?\/?>)[ \t]*(?=\n{2,}))/g, o.subParser("hashElement")(p, b, w)), p = o.helper.replaceRecursiveRegExp(p, function(B) {
        return `

¨K` + (w.gHtmlBlocks.push(B) - 1) + `K

`;
      }, "^ {0,3}<!--", "-->", "gm"), p = p.replace(/(?:\n\n)( {0,3}(?:<([?%])[^\r]*?\2>)[ \t]*(?=\n{2,}))/g, o.subParser("hashElement")(p, b, w)), p = w.converter._dispatch("hashHTMLBlocks.after", p, b, w), p;
    }), o.subParser("hashHTMLSpans", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("hashHTMLSpans.before", p, b, w);
      function E(x) {
        return "¨C" + (w.gHtmlSpans.push(x) - 1) + "C";
      }
      return p = p.replace(/<[^>]+?\/>/gi, function(x) {
        return E(x);
      }), p = p.replace(/<([^>]+?)>[\s\S]*?<\/\1>/g, function(x) {
        return E(x);
      }), p = p.replace(/<([^>]+?)\s[^>]+?>[\s\S]*?<\/\1>/g, function(x) {
        return E(x);
      }), p = p.replace(/<[^>]+?>/gi, function(x) {
        return E(x);
      }), p = w.converter._dispatch("hashHTMLSpans.after", p, b, w), p;
    }), o.subParser("unhashHTMLSpans", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("unhashHTMLSpans.before", p, b, w);
      for (var E = 0; E < w.gHtmlSpans.length; ++E) {
        for (var x = w.gHtmlSpans[E], M = 0; /¨C(\d+)C/.test(x); ) {
          var N = RegExp.$1;
          if (x = x.replace("¨C" + N + "C", w.gHtmlSpans[N]), M === 10) {
            console.error("maximum nesting of 10 spans reached!!!");
            break;
          }
          ++M;
        }
        p = p.replace("¨C" + E + "C", x);
      }
      return p = w.converter._dispatch("unhashHTMLSpans.after", p, b, w), p;
    }), o.subParser("hashPreCodeTags", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("hashPreCodeTags.before", p, b, w);
      var E = function(x, M, N, q) {
        var z = N + o.subParser("encodeCode")(M, b, w) + q;
        return `

¨G` + (w.ghCodeBlocks.push({
          text: x,
          codeblock: z
        }) - 1) + `G

`;
      };
      return p = o.helper.replaceRecursiveRegExp(p, E, "^ {0,3}<pre\\b[^>]*>\\s*<code\\b[^>]*>", "^ {0,3}</code>\\s*</pre>", "gim"), p = w.converter._dispatch("hashPreCodeTags.after", p, b, w), p;
    }), o.subParser("headers", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("headers.before", p, b, w);
      var E = isNaN(parseInt(b.headerLevelStart)) ? 1 : parseInt(b.headerLevelStart), x = b.smoothLivePreview ? /^(.+)[ \t]*\n={2,}[ \t]*\n+/gm : /^(.+)[ \t]*\n=+[ \t]*\n+/gm, M = b.smoothLivePreview ? /^(.+)[ \t]*\n-{2,}[ \t]*\n+/gm : /^(.+)[ \t]*\n-+[ \t]*\n+/gm;
      p = p.replace(x, function(z, J) {
        var G = o.subParser("spanGamut")(J, b, w), oe = b.noHeaderId ? "" : ' id="' + q(J) + '"', B = E, X = "<h" + B + oe + ">" + G + "</h" + B + ">";
        return o.subParser("hashBlock")(X, b, w);
      }), p = p.replace(M, function(z, J) {
        var G = o.subParser("spanGamut")(J, b, w), oe = b.noHeaderId ? "" : ' id="' + q(J) + '"', B = E + 1, X = "<h" + B + oe + ">" + G + "</h" + B + ">";
        return o.subParser("hashBlock")(X, b, w);
      });
      var N = b.requireSpaceBeforeHeadingText ? /^(#{1,6})[ \t]+(.+?)[ \t]*#*\n+/gm : /^(#{1,6})[ \t]*(.+?)[ \t]*#*\n+/gm;
      p = p.replace(N, function(z, J, G) {
        var oe = G;
        b.customizedHeaderId && (oe = G.replace(/\s?\{([^{]+?)}\s*$/, ""));
        var B = o.subParser("spanGamut")(oe, b, w), X = b.noHeaderId ? "" : ' id="' + q(G) + '"', de = E - 1 + J.length, fe = "<h" + de + X + ">" + B + "</h" + de + ">";
        return o.subParser("hashBlock")(fe, b, w);
      });
      function q(z) {
        var J, G;
        if (b.customizedHeaderId) {
          var oe = z.match(/\{([^{]+?)}\s*$/);
          oe && oe[1] && (z = oe[1]);
        }
        return J = z, o.helper.isString(b.prefixHeaderId) ? G = b.prefixHeaderId : b.prefixHeaderId === !0 ? G = "section-" : G = "", b.rawPrefixHeaderId || (J = G + J), b.ghCompatibleHeaderId ? J = J.replace(/ /g, "-").replace(/&amp;/g, "").replace(/¨T/g, "").replace(/¨D/g, "").replace(/[&+$,\/:;=?@"#{}|^¨~\[\]`\\*)(%.!'<>]/g, "").toLowerCase() : b.rawHeaderId ? J = J.replace(/ /g, "-").replace(/&amp;/g, "&").replace(/¨T/g, "¨").replace(/¨D/g, "$").replace(/["']/g, "-").toLowerCase() : J = J.replace(/[^\w]/g, "").toLowerCase(), b.rawPrefixHeaderId && (J = G + J), w.hashLinkCounts[J] ? J = J + "-" + w.hashLinkCounts[J]++ : w.hashLinkCounts[J] = 1, J;
      }
      return p = w.converter._dispatch("headers.after", p, b, w), p;
    }), o.subParser("horizontalRule", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("horizontalRule.before", p, b, w);
      var E = o.subParser("hashBlock")("<hr />", b, w);
      return p = p.replace(/^ {0,2}( ?-){3,}[ \t]*$/gm, E), p = p.replace(/^ {0,2}( ?\*){3,}[ \t]*$/gm, E), p = p.replace(/^ {0,2}( ?_){3,}[ \t]*$/gm, E), p = w.converter._dispatch("horizontalRule.after", p, b, w), p;
    }), o.subParser("images", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("images.before", p, b, w);
      var E = /!\[([^\]]*?)][ \t]*()\([ \t]?<?([\S]+?(?:\([\S]*?\)[\S]*?)?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g, x = /!\[([^\]]*?)][ \t]*()\([ \t]?<([^>]*)>(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(?:(["'])([^"]*?)\6))?[ \t]?\)/g, M = /!\[([^\]]*?)][ \t]*()\([ \t]?<?(data:.+?\/.+?;base64,[A-Za-z0-9+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g, N = /!\[([^\]]*?)] ?(?:\n *)?\[([\s\S]*?)]()()()()()/g, q = /!\[([^\[\]]+)]()()()()()/g;
      function z(G, oe, B, X, de, fe, ve, Ce) {
        return X = X.replace(/\s/g, ""), J(G, oe, B, X, de, fe, ve, Ce);
      }
      function J(G, oe, B, X, de, fe, ve, Ce) {
        var Ue = w.gUrls, le = w.gTitles, me = w.gDimensions;
        if (B = B.toLowerCase(), Ce || (Ce = ""), G.search(/\(<?\s*>? ?(['"].*['"])?\)$/m) > -1) X = "";
        else if (X === "" || X === null)
          if ((B === "" || B === null) && (B = oe.toLowerCase().replace(/ ?\n/g, " ")), X = "#" + B, !o.helper.isUndefined(Ue[B]))
            X = Ue[B], o.helper.isUndefined(le[B]) || (Ce = le[B]), o.helper.isUndefined(me[B]) || (de = me[B].width, fe = me[B].height);
          else return G;
        oe = oe.replace(/"/g, "&quot;").replace(o.helper.regexes.asteriskDashAndColon, o.helper.escapeCharactersCallback), X = X.replace(o.helper.regexes.asteriskDashAndColon, o.helper.escapeCharactersCallback);
        var We = '<img src="' + X + '" alt="' + oe + '"';
        return Ce && o.helper.isString(Ce) && (Ce = Ce.replace(/"/g, "&quot;").replace(o.helper.regexes.asteriskDashAndColon, o.helper.escapeCharactersCallback), We += ' title="' + Ce + '"'), de && fe && (de = de === "*" ? "auto" : de, fe = fe === "*" ? "auto" : fe, We += ' width="' + de + '"', We += ' height="' + fe + '"'), We += " />", We;
      }
      return p = p.replace(N, J), p = p.replace(M, z), p = p.replace(x, J), p = p.replace(E, J), p = p.replace(q, J), p = w.converter._dispatch("images.after", p, b, w), p;
    }), o.subParser("italicsAndBold", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("italicsAndBold.before", p, b, w);
      function E(x, M, N) {
        return M + x + N;
      }
      return b.literalMidWordUnderscores ? (p = p.replace(/\b___(\S[\s\S]*?)___\b/g, function(x, M) {
        return E(M, "<strong><em>", "</em></strong>");
      }), p = p.replace(/\b__(\S[\s\S]*?)__\b/g, function(x, M) {
        return E(M, "<strong>", "</strong>");
      }), p = p.replace(/\b_(\S[\s\S]*?)_\b/g, function(x, M) {
        return E(M, "<em>", "</em>");
      })) : (p = p.replace(/___(\S[\s\S]*?)___/g, function(x, M) {
        return /\S$/.test(M) ? E(M, "<strong><em>", "</em></strong>") : x;
      }), p = p.replace(/__(\S[\s\S]*?)__/g, function(x, M) {
        return /\S$/.test(M) ? E(M, "<strong>", "</strong>") : x;
      }), p = p.replace(/_([^\s_][\s\S]*?)_/g, function(x, M) {
        return /\S$/.test(M) ? E(M, "<em>", "</em>") : x;
      })), b.literalMidWordAsterisks ? (p = p.replace(/([^*]|^)\B\*\*\*(\S[\s\S]*?)\*\*\*\B(?!\*)/g, function(x, M, N) {
        return E(N, M + "<strong><em>", "</em></strong>");
      }), p = p.replace(/([^*]|^)\B\*\*(\S[\s\S]*?)\*\*\B(?!\*)/g, function(x, M, N) {
        return E(N, M + "<strong>", "</strong>");
      }), p = p.replace(/([^*]|^)\B\*(\S[\s\S]*?)\*\B(?!\*)/g, function(x, M, N) {
        return E(N, M + "<em>", "</em>");
      })) : (p = p.replace(/\*\*\*(\S[\s\S]*?)\*\*\*/g, function(x, M) {
        return /\S$/.test(M) ? E(M, "<strong><em>", "</em></strong>") : x;
      }), p = p.replace(/\*\*(\S[\s\S]*?)\*\*/g, function(x, M) {
        return /\S$/.test(M) ? E(M, "<strong>", "</strong>") : x;
      }), p = p.replace(/\*([^\s*][\s\S]*?)\*/g, function(x, M) {
        return /\S$/.test(M) ? E(M, "<em>", "</em>") : x;
      })), p = w.converter._dispatch("italicsAndBold.after", p, b, w), p;
    }), o.subParser("lists", function(p, b, w) {
      "use strict";
      function E(N, q) {
        w.gListLevel++, N = N.replace(/\n{2,}$/, `
`), N += "¨0";
        var z = /(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[(x|X| )?])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0| {0,3}([*+-]|\d+[.])[ \t]+))/gm, J = /\n[ \t]*\n(?!¨0)/.test(N);
        return b.disableForced4SpacesIndentedSublists && (z = /(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[(x|X| )?])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0|\2([*+-]|\d+[.])[ \t]+))/gm), N = N.replace(z, function(G, oe, B, X, de, fe, ve) {
          ve = ve && ve.trim() !== "";
          var Ce = o.subParser("outdent")(de, b, w), Ue = "";
          return fe && b.tasklists && (Ue = ' class="task-list-item" style="list-style-type: none;"', Ce = Ce.replace(/^[ \t]*\[(x|X| )?]/m, function() {
            var le = '<input type="checkbox" disabled style="margin: 0px 0.35em 0.25em -1.6em; vertical-align: middle;"';
            return ve && (le += " checked"), le += ">", le;
          })), Ce = Ce.replace(/^([-*+]|\d\.)[ \t]+[\S\n ]*/g, function(le) {
            return "¨A" + le;
          }), oe || Ce.search(/\n{2,}/) > -1 ? (Ce = o.subParser("githubCodeBlocks")(Ce, b, w), Ce = o.subParser("blockGamut")(Ce, b, w)) : (Ce = o.subParser("lists")(Ce, b, w), Ce = Ce.replace(/\n$/, ""), Ce = o.subParser("hashHTMLBlocks")(Ce, b, w), Ce = Ce.replace(/\n\n+/g, `

`), J ? Ce = o.subParser("paragraphs")(Ce, b, w) : Ce = o.subParser("spanGamut")(Ce, b, w)), Ce = Ce.replace("¨A", ""), Ce = "<li" + Ue + ">" + Ce + `</li>
`, Ce;
        }), N = N.replace(/¨0/g, ""), w.gListLevel--, q && (N = N.replace(/\s+$/, "")), N;
      }
      function x(N, q) {
        if (q === "ol") {
          var z = N.match(/^ *(\d+)\./);
          if (z && z[1] !== "1") return ' start="' + z[1] + '"';
        }
        return "";
      }
      function M(N, q, z) {
        var J = b.disableForced4SpacesIndentedSublists ? /^ ?\d+\.[ \t]/gm : /^ {0,3}\d+\.[ \t]/gm, G = b.disableForced4SpacesIndentedSublists ? /^ ?[*+-][ \t]/gm : /^ {0,3}[*+-][ \t]/gm, oe = q === "ul" ? J : G, B = "";
        if (N.search(oe) !== -1) (function de(fe) {
          var ve = fe.search(oe), Ce = x(N, q);
          ve !== -1 ? (B += `

<` + q + Ce + `>
` + E(fe.slice(0, ve), !!z) + "</" + q + `>
`, q = q === "ul" ? "ol" : "ul", oe = q === "ul" ? J : G, de(fe.slice(ve))) : B += `

<` + q + Ce + `>
` + E(fe, !!z) + "</" + q + `>
`;
        })(N);
        else {
          var X = x(N, q);
          B = `

<` + q + X + `>
` + E(N, !!z) + "</" + q + `>
`;
        }
        return B;
      }
      return p = w.converter._dispatch("lists.before", p, b, w), p += "¨0", w.gListLevel ? p = p.replace(/^(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm, function(N, q, z) {
        return M(q, z.search(/[*+-]/g) > -1 ? "ul" : "ol", !0);
      }) : p = p.replace(/(\n\n|^\n?)(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm, function(N, q, z, J) {
        return M(z, J.search(/[*+-]/g) > -1 ? "ul" : "ol", !1);
      }), p = p.replace(/¨0/, ""), p = w.converter._dispatch("lists.after", p, b, w), p;
    }), o.subParser("metadata", function(p, b, w) {
      "use strict";
      if (!b.metadata) return p;
      p = w.converter._dispatch("metadata.before", p, b, w);
      function E(x) {
        w.metadata.raw = x, x = x.replace(/&/g, "&amp;").replace(/"/g, "&quot;"), x = x.replace(/\n {4}/g, " "), x.replace(/^([\S ]+): +([\s\S]+?)$/gm, function(M, N, q) {
          return w.metadata.parsed[N] = q, "";
        });
      }
      return p = p.replace(/^\s*«««+(\S*?)\n([\s\S]+?)\n»»»+\n/, function(x, M, N) {
        return E(N), "¨M";
      }), p = p.replace(/^\s*---+(\S*?)\n([\s\S]+?)\n---+\n/, function(x, M, N) {
        return M && (w.metadata.format = M), E(N), "¨M";
      }), p = p.replace(/¨M/g, ""), p = w.converter._dispatch("metadata.after", p, b, w), p;
    }), o.subParser("outdent", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("outdent.before", p, b, w), p = p.replace(/^(\t|[ ]{1,4})/gm, "¨0"), p = p.replace(/¨0/g, ""), p = w.converter._dispatch("outdent.after", p, b, w), p;
    }), o.subParser("paragraphs", function(p, b, w) {
      "use strict";
      p = w.converter._dispatch("paragraphs.before", p, b, w), p = p.replace(/^\n+/g, ""), p = p.replace(/\n+$/g, "");
      for (var E = p.split(/\n{2,}/g), x = [], M = E.length, N = 0; N < M; N++) {
        var q = E[N];
        q.search(/¨(K|G)(\d+)\1/g) >= 0 ? x.push(q) : q.search(/\S/) >= 0 && (q = o.subParser("spanGamut")(q, b, w), q = q.replace(/^([ \t]*)/g, "<p>"), q += "</p>", x.push(q));
      }
      for (M = x.length, N = 0; N < M; N++) {
        for (var z = "", J = x[N], G = !1; /¨(K|G)(\d+)\1/.test(J); ) {
          var oe = RegExp.$1, B = RegExp.$2;
          oe === "K" ? z = w.gHtmlBlocks[B] : G ? z = o.subParser("encodeCode")(w.ghCodeBlocks[B].text, b, w) : z = w.ghCodeBlocks[B].codeblock, z = z.replace(/\$/g, "$$$$"), J = J.replace(/(\n\n)?¨(K|G)\d+\2(\n\n)?/, z), /^<pre\b[^>]*>\s*<code\b[^>]*>/.test(J) && (G = !0);
        }
        x[N] = J;
      }
      return p = x.join(`
`), p = p.replace(/^\n+/g, ""), p = p.replace(/\n+$/g, ""), w.converter._dispatch("paragraphs.after", p, b, w);
    }), o.subParser("runExtension", function(p, b, w, E) {
      "use strict";
      if (p.filter) b = p.filter(b, E.converter, w);
      else if (p.regex) {
        var x = p.regex;
        x instanceof RegExp || (x = new RegExp(x, "g")), b = b.replace(x, p.replace);
      }
      return b;
    }), o.subParser("spanGamut", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("spanGamut.before", p, b, w), p = o.subParser("codeSpans")(p, b, w), p = o.subParser("escapeSpecialCharsWithinTagAttributes")(p, b, w), p = o.subParser("encodeBackslashEscapes")(p, b, w), p = o.subParser("images")(p, b, w), p = o.subParser("anchors")(p, b, w), p = o.subParser("autoLinks")(p, b, w), p = o.subParser("simplifiedAutoLinks")(p, b, w), p = o.subParser("emoji")(p, b, w), p = o.subParser("underline")(p, b, w), p = o.subParser("italicsAndBold")(p, b, w), p = o.subParser("strikethrough")(p, b, w), p = o.subParser("ellipsis")(p, b, w), p = o.subParser("hashHTMLSpans")(p, b, w), p = o.subParser("encodeAmpsAndAngles")(p, b, w), b.simpleLineBreaks ? /\n\n¨K/.test(p) || (p = p.replace(/\n+/g, `<br />
`)) : p = p.replace(/  +\n/g, `<br />
`), p = w.converter._dispatch("spanGamut.after", p, b, w), p;
    }), o.subParser("strikethrough", function(p, b, w) {
      "use strict";
      function E(x) {
        return b.simplifiedAutoLink && (x = o.subParser("simplifiedAutoLinks")(x, b, w)), "<del>" + x + "</del>";
      }
      return b.strikethrough && (p = w.converter._dispatch("strikethrough.before", p, b, w), p = p.replace(/(?:~){2}([\s\S]+?)(?:~){2}/g, function(x, M) {
        return E(M);
      }), p = w.converter._dispatch("strikethrough.after", p, b, w)), p;
    }), o.subParser("stripLinkDefinitions", function(p, b, w) {
      "use strict";
      var E = /^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?([^>\s]+)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n+|(?=¨0))/gm, x = /^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?(data:.+?\/.+?;base64,[A-Za-z0-9+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n\n|(?=¨0)|(?=\n\[))/gm;
      p += "¨0";
      var M = function(N, q, z, J, G, oe, B) {
        return q = q.toLowerCase(), p.toLowerCase().split(q).length - 1 < 2 ? N : (z.match(/^data:.+?\/.+?;base64,/) ? w.gUrls[q] = z.replace(/\s/g, "") : w.gUrls[q] = o.subParser("encodeAmpsAndAngles")(z, b, w), oe ? oe + B : (B && (w.gTitles[q] = B.replace(/"|'/g, "&quot;")), b.parseImgDimensions && J && G && (w.gDimensions[q] = {
          width: J,
          height: G
        }), ""));
      };
      return p = p.replace(x, M), p = p.replace(E, M), p = p.replace(/¨0/, ""), p;
    }), o.subParser("tables", function(p, b, w) {
      "use strict";
      if (!b.tables) return p;
      var E = /^ {0,3}\|?.+\|.+\n {0,3}\|?[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*:?[ \t]*(?:[-=]){2,}[\s\S]+?(?:\n\n|¨0)/gm, x = /^ {0,3}\|.+\|[ \t]*\n {0,3}\|[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*\n( {0,3}\|.+\|[ \t]*\n)*(?:\n|¨0)/gm;
      function M(G) {
        return /^:[ \t]*--*$/.test(G) ? ' style="text-align:left;"' : /^--*[ \t]*:[ \t]*$/.test(G) ? ' style="text-align:right;"' : /^:[ \t]*--*[ \t]*:$/.test(G) ? ' style="text-align:center;"' : "";
      }
      function N(G, oe) {
        var B = "";
        return G = G.trim(), (b.tablesHeaderId || b.tableHeaderId) && (B = ' id="' + G.replace(/ /g, "_").toLowerCase() + '"'), G = o.subParser("spanGamut")(G, b, w), "<th" + B + oe + ">" + G + `</th>
`;
      }
      function q(G, oe) {
        var B = o.subParser("spanGamut")(G, b, w);
        return "<td" + oe + ">" + B + `</td>
`;
      }
      function z(G, oe) {
        for (var B = `<table>
<thead>
<tr>
`, X = G.length, de = 0; de < X; ++de) B += G[de];
        for (B += `</tr>
</thead>
<tbody>
`, de = 0; de < oe.length; ++de) {
          B += `<tr>
`;
          for (var fe = 0; fe < X; ++fe) B += oe[de][fe];
          B += `</tr>
`;
        }
        return B += `</tbody>
</table>
`, B;
      }
      function J(G) {
        var oe, B = G.split(`
`);
        for (oe = 0; oe < B.length; ++oe)
          /^ {0,3}\|/.test(B[oe]) && (B[oe] = B[oe].replace(/^ {0,3}\|/, "")), /\|[ \t]*$/.test(B[oe]) && (B[oe] = B[oe].replace(/\|[ \t]*$/, "")), B[oe] = o.subParser("codeSpans")(B[oe], b, w);
        var X = B[0].split("|").map(function(We) {
          return We.trim();
        }), de = B[1].split("|").map(function(We) {
          return We.trim();
        }), fe = [], ve = [], Ce = [], Ue = [];
        for (B.shift(), B.shift(), oe = 0; oe < B.length; ++oe)
          B[oe].trim() !== "" && fe.push(B[oe].split("|").map(function(We) {
            return We.trim();
          }));
        if (X.length < de.length) return G;
        for (oe = 0; oe < de.length; ++oe) Ce.push(M(de[oe]));
        for (oe = 0; oe < X.length; ++oe)
          o.helper.isUndefined(Ce[oe]) && (Ce[oe] = ""), ve.push(N(X[oe], Ce[oe]));
        for (oe = 0; oe < fe.length; ++oe) {
          for (var le = [], me = 0; me < ve.length; ++me)
            o.helper.isUndefined(fe[oe][me]), le.push(q(fe[oe][me], Ce[me]));
          Ue.push(le);
        }
        return z(ve, Ue);
      }
      return p = w.converter._dispatch("tables.before", p, b, w), p = p.replace(/\\(\|)/g, o.helper.escapeCharactersCallback), p = p.replace(E, J), p = p.replace(x, J), p = w.converter._dispatch("tables.after", p, b, w), p;
    }), o.subParser("underline", function(p, b, w) {
      "use strict";
      return b.underline && (p = w.converter._dispatch("underline.before", p, b, w), b.literalMidWordUnderscores ? (p = p.replace(/\b___(\S[\s\S]*?)___\b/g, function(E, x) {
        return "<u>" + x + "</u>";
      }), p = p.replace(/\b__(\S[\s\S]*?)__\b/g, function(E, x) {
        return "<u>" + x + "</u>";
      })) : (p = p.replace(/___(\S[\s\S]*?)___/g, function(E, x) {
        return /\S$/.test(x) ? "<u>" + x + "</u>" : E;
      }), p = p.replace(/__(\S[\s\S]*?)__/g, function(E, x) {
        return /\S$/.test(x) ? "<u>" + x + "</u>" : E;
      })), p = p.replace(/(_)/g, o.helper.escapeCharactersCallback), p = w.converter._dispatch("underline.after", p, b, w)), p;
    }), o.subParser("unescapeSpecialChars", function(p, b, w) {
      "use strict";
      return p = w.converter._dispatch("unescapeSpecialChars.before", p, b, w), p = p.replace(/¨E(\d+)E/g, function(E, x) {
        var M = parseInt(x);
        return String.fromCharCode(M);
      }), p = w.converter._dispatch("unescapeSpecialChars.after", p, b, w), p;
    }), o.subParser("makeMarkdown.blockquote", function(p, b) {
      "use strict";
      var w = "";
      if (p.hasChildNodes())
        for (var E = p.childNodes, x = E.length, M = 0; M < x; ++M) {
          var N = o.subParser("makeMarkdown.node")(E[M], b);
          N !== "" && (w += N);
        }
      return w = w.trim(), w = "> " + w.split(`
`).join(`
> `), w;
    }), o.subParser("makeMarkdown.codeBlock", function(p, b) {
      "use strict";
      var w = p.getAttribute("language"), E = p.getAttribute("precodenum");
      return "```" + w + `
` + b.preList[E] + "\n```";
    }), o.subParser("makeMarkdown.codeSpan", function(p) {
      "use strict";
      return "`" + p.innerHTML + "`";
    }), o.subParser("makeMarkdown.emphasis", function(p, b) {
      "use strict";
      var w = "";
      if (p.hasChildNodes()) {
        w += "*";
        for (var E = p.childNodes, x = E.length, M = 0; M < x; ++M) w += o.subParser("makeMarkdown.node")(E[M], b);
        w += "*";
      }
      return w;
    }), o.subParser("makeMarkdown.header", function(p, b, w) {
      "use strict";
      var E = new Array(w + 1).join("#"), x = "";
      if (p.hasChildNodes()) {
        x = E + " ";
        for (var M = p.childNodes, N = M.length, q = 0; q < N; ++q) x += o.subParser("makeMarkdown.node")(M[q], b);
      }
      return x;
    }), o.subParser("makeMarkdown.hr", function() {
      "use strict";
      return "---";
    }), o.subParser("makeMarkdown.image", function(p) {
      "use strict";
      var b = "";
      return p.hasAttribute("src") && (b += "![" + p.getAttribute("alt") + "](", b += "<" + p.getAttribute("src") + ">", p.hasAttribute("width") && p.hasAttribute("height") && (b += " =" + p.getAttribute("width") + "x" + p.getAttribute("height")), p.hasAttribute("title") && (b += ' "' + p.getAttribute("title") + '"'), b += ")"), b;
    }), o.subParser("makeMarkdown.links", function(p, b) {
      "use strict";
      var w = "";
      if (p.hasChildNodes() && p.hasAttribute("href")) {
        var E = p.childNodes, x = E.length;
        w = "[";
        for (var M = 0; M < x; ++M) w += o.subParser("makeMarkdown.node")(E[M], b);
        w += "](", w += "<" + p.getAttribute("href") + ">", p.hasAttribute("title") && (w += ' "' + p.getAttribute("title") + '"'), w += ")";
      }
      return w;
    }), o.subParser("makeMarkdown.list", function(p, b, w) {
      "use strict";
      var E = "";
      if (!p.hasChildNodes()) return "";
      for (var x = p.childNodes, M = x.length, N = p.getAttribute("start") || 1, q = 0; q < M; ++q)
        if (!(typeof x[q].tagName > "u" || x[q].tagName.toLowerCase() !== "li")) {
          var z = "";
          w === "ol" ? z = N.toString() + ". " : z = "- ", E += z + o.subParser("makeMarkdown.listItem")(x[q], b), ++N;
        }
      return E += `
<!-- -->
`, E.trim();
    }), o.subParser("makeMarkdown.listItem", function(p, b) {
      "use strict";
      for (var w = "", E = p.childNodes, x = E.length, M = 0; M < x; ++M) w += o.subParser("makeMarkdown.node")(E[M], b);
      return /\n$/.test(w) ? w = w.split(`
`).join(`
    `).replace(/^ {4}$/gm, "").replace(/\n\n+/g, `

`) : w += `
`, w;
    }), o.subParser("makeMarkdown.node", function(p, b, w) {
      "use strict";
      w = w || !1;
      var E = "";
      if (p.nodeType === 3) return o.subParser("makeMarkdown.txt")(p, b);
      if (p.nodeType === 8) return "<!--" + p.data + `-->

`;
      if (p.nodeType !== 1) return "";
      switch (p.tagName.toLowerCase()) {
        case "h1":
          w || (E = o.subParser("makeMarkdown.header")(p, b, 1) + `

`);
          break;
        case "h2":
          w || (E = o.subParser("makeMarkdown.header")(p, b, 2) + `

`);
          break;
        case "h3":
          w || (E = o.subParser("makeMarkdown.header")(p, b, 3) + `

`);
          break;
        case "h4":
          w || (E = o.subParser("makeMarkdown.header")(p, b, 4) + `

`);
          break;
        case "h5":
          w || (E = o.subParser("makeMarkdown.header")(p, b, 5) + `

`);
          break;
        case "h6":
          w || (E = o.subParser("makeMarkdown.header")(p, b, 6) + `

`);
          break;
        case "p":
          w || (E = o.subParser("makeMarkdown.paragraph")(p, b) + `

`);
          break;
        case "blockquote":
          w || (E = o.subParser("makeMarkdown.blockquote")(p, b) + `

`);
          break;
        case "hr":
          w || (E = o.subParser("makeMarkdown.hr")(p, b) + `

`);
          break;
        case "ol":
          w || (E = o.subParser("makeMarkdown.list")(p, b, "ol") + `

`);
          break;
        case "ul":
          w || (E = o.subParser("makeMarkdown.list")(p, b, "ul") + `

`);
          break;
        case "precode":
          w || (E = o.subParser("makeMarkdown.codeBlock")(p, b) + `

`);
          break;
        case "pre":
          w || (E = o.subParser("makeMarkdown.pre")(p, b) + `

`);
          break;
        case "table":
          w || (E = o.subParser("makeMarkdown.table")(p, b) + `

`);
          break;
        case "code":
          E = o.subParser("makeMarkdown.codeSpan")(p, b);
          break;
        case "em":
        case "i":
          E = o.subParser("makeMarkdown.emphasis")(p, b);
          break;
        case "strong":
        case "b":
          E = o.subParser("makeMarkdown.strong")(p, b);
          break;
        case "del":
          E = o.subParser("makeMarkdown.strikethrough")(p, b);
          break;
        case "a":
          E = o.subParser("makeMarkdown.links")(p, b);
          break;
        case "img":
          E = o.subParser("makeMarkdown.image")(p, b);
          break;
        default:
          E = p.outerHTML + `

`;
      }
      return E;
    }), o.subParser("makeMarkdown.paragraph", function(p, b) {
      "use strict";
      var w = "";
      if (p.hasChildNodes())
        for (var E = p.childNodes, x = E.length, M = 0; M < x; ++M) w += o.subParser("makeMarkdown.node")(E[M], b);
      return w = w.trim(), w;
    }), o.subParser("makeMarkdown.pre", function(p, b) {
      "use strict";
      var w = p.getAttribute("prenum");
      return "<pre>" + b.preList[w] + "</pre>";
    }), o.subParser("makeMarkdown.strikethrough", function(p, b) {
      "use strict";
      var w = "";
      if (p.hasChildNodes()) {
        w += "~~";
        for (var E = p.childNodes, x = E.length, M = 0; M < x; ++M) w += o.subParser("makeMarkdown.node")(E[M], b);
        w += "~~";
      }
      return w;
    }), o.subParser("makeMarkdown.strong", function(p, b) {
      "use strict";
      var w = "";
      if (p.hasChildNodes()) {
        w += "**";
        for (var E = p.childNodes, x = E.length, M = 0; M < x; ++M) w += o.subParser("makeMarkdown.node")(E[M], b);
        w += "**";
      }
      return w;
    }), o.subParser("makeMarkdown.table", function(p, b) {
      "use strict";
      var w = "", E = [[], []], x = p.querySelectorAll("thead>tr>th"), M = p.querySelectorAll("tbody>tr"), N, q;
      for (N = 0; N < x.length; ++N) {
        var z = o.subParser("makeMarkdown.tableCell")(x[N], b), J = "---";
        if (x[N].hasAttribute("style")) switch (x[N].getAttribute("style").toLowerCase().replace(/\s/g, "")) {
          case "text-align:left;":
            J = ":---";
            break;
          case "text-align:right;":
            J = "---:";
            break;
          case "text-align:center;":
            J = ":---:";
            break;
        }
        E[0][N] = z.trim(), E[1][N] = J;
      }
      for (N = 0; N < M.length; ++N) {
        var G = E.push([]) - 1, oe = M[N].getElementsByTagName("td");
        for (q = 0; q < x.length; ++q) {
          var B = " ";
          typeof oe[q] < "u" && (B = o.subParser("makeMarkdown.tableCell")(oe[q], b)), E[G].push(B);
        }
      }
      var X = 3;
      for (N = 0; N < E.length; ++N) for (q = 0; q < E[N].length; ++q) {
        var de = E[N][q].length;
        de > X && (X = de);
      }
      for (N = 0; N < E.length; ++N) {
        for (q = 0; q < E[N].length; ++q) N === 1 ? E[N][q].slice(-1) === ":" ? E[N][q] = o.helper.padEnd(E[N][q].slice(-1), X - 1, "-") + ":" : E[N][q] = o.helper.padEnd(E[N][q], X, "-") : E[N][q] = o.helper.padEnd(E[N][q], X);
        w += "| " + E[N].join(" | ") + ` |
`;
      }
      return w.trim();
    }), o.subParser("makeMarkdown.tableCell", function(p, b) {
      "use strict";
      var w = "";
      if (!p.hasChildNodes()) return "";
      for (var E = p.childNodes, x = E.length, M = 0; M < x; ++M) w += o.subParser("makeMarkdown.node")(E[M], b, !0);
      return w.trim();
    }), o.subParser("makeMarkdown.txt", function(p) {
      "use strict";
      var b = p.nodeValue;
      return b = b.replace(/ +/g, " "), b = b.replace(/¨NBSP;/g, " "), b = o.helper.unescapeHTMLEntities(b), b = b.replace(/([*_~|`])/g, "\\$1"), b = b.replace(/^(\s*)>/g, "\\$1>"), b = b.replace(/^#/gm, "\\#"), b = b.replace(/^(\s*)([-=]{3,})(\s*)$/, "$1\\$2$3"), b = b.replace(/^( {0,3}\d+)\./gm, "$1\\."), b = b.replace(/^( {0,3})([+-])/gm, "$1\\$2"), b = b.replace(/]([\s]*)\(/g, "\\]$1\\("), b = b.replace(/^ {0,3}\[([\S \t]*?)]:/gm, "\\[$1]:"), b;
    });
    var $ = this;
    typeof define == "function" && define.amd ? define(function() {
      "use strict";
      return o;
    }) : typeof t < "u" && t.exports ? t.exports = o : $.showdown = o;
  }).call(e);
})), M$ = /* @__PURE__ */ jC(x$(), 1), Kv = null, c2 = 0, l_ = /* @__PURE__ */ new Map(), d2 = 0, u_ = /* @__PURE__ */ new Map(), P$ = /* @__PURE__ */ new Set([
  "html",
  "htm",
  "xhtml",
  "xml",
  "svg",
  "vue",
  "svelte"
]), R$ = "allow-scripts", Lp = [".xb-tavern-markdown", ".xb-assistant-markdown"], N$ = /* @__PURE__ */ new Set([
  "script",
  "style",
  "textarea",
  "title"
]);
function $$(e) {
  return String(e || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function D$() {
  return c2 += 1, `html-${Date.now().toString(36)}-${c2.toString(36)}`;
}
function L$() {
  return d2 += 1, `raw-${Date.now().toString(36)}-${d2.toString(36)}`;
}
function O$(e = "") {
  const t = String(e || "").length;
  if (t >= 1e3) {
    const n = t / 1e3;
    return `${n >= 10 ? Math.round(n) : n.toFixed(1)}k 字符`;
  }
  return `${t} 字符`;
}
function F$(e = "") {
  return P$.has(String(e || "").trim().toLowerCase());
}
function B$(e = "") {
  const t = String(e || "").trim();
  return t ? /^<!doctype\s+html/i.test(t) || /^<html[\s>]/i.test(t) ? !0 : (t.match(/<\/?[a-z][\w:-]*(?:\s[^<>]*)?>/gi) || []).length >= 3 && /<\/[a-z][\w:-]*>/i.test(t) : !1;
}
function U$(e = "", t = "html") {
  const n = D$();
  return l_.set(n, {
    code: String(e || ""),
    language: String(t || "html").trim() || "html"
  }), `@@XBHTMLBLOCK:${n}@@`;
}
function q$(e = "") {
  const t = L$();
  return u_.set(t, String(e || "")), `@@XBHTMLRAW:${t}@@`;
}
function Jv(e = "") {
  const t = String(e || "").trim();
  if (!t || !t.startsWith("<") || !t.endsWith(">") || /^<!--[\s\S]*-->$/.test(t) || /^<!doctype\b/i.test(t) || /^<\?xml\b/i.test(t)) return !1;
  let n = !1;
  const r = /<\/?\s*([a-z][\w:-]*)\b[^>]*>/gi;
  let o = null;
  for (; (o = r.exec(t)) !== null; ) {
    const a = String(o[1] || "").toLowerCase();
    if (N$.has(a)) return !1;
    n = !0;
  }
  if (!n) return !1;
  const s = t.replace(/<\/?\s*[a-z][\w:-]*\b[^>]*>/gi, "").trim();
  return !s || !/(^|\s)(?:#{1,6}\s|[-+*]\s|\d+\.\s|```|~~~|>\s)/.test(s);
}
function f2(e = "") {
  const t = String(e || "");
  if (!t.trim()) return t;
  const n = t.split(/\r?\n/), r = [];
  for (let o = 0; o < n.length; o += 1) {
    const s = n[o];
    if (!Jv(s)) {
      r.push(s);
      continue;
    }
    const a = [s];
    for (; o + 1 < n.length && Jv(n[o + 1]); )
      o += 1, a.push(n[o]);
    (r[r.length - 1] ?? "").trim() && r.push(""), r.push(q$(a.join(`
`)));
    const i = n[o + 1] ?? "";
    i.trim() && !Jv(i) && r.push("");
  }
  return r.join(`
`);
}
function H$(e = "") {
  const t = String(e || ""), n = /(^|\n)(`{3,}|~{3,})[ \t]*([^\n]*)\n([\s\S]*?)\n\2[ \t]*(?=\n|$)/g;
  let r = "", o = 0, s = null;
  for (; (s = n.exec(t)) !== null; ) {
    const a = s[1] || "", i = s.index + a.length, l = n.lastIndex, u = String(s[3] || "").trim().split(/\s+/)[0] || "", d = String(s[4] || ""), f = F$(u) || !u && B$(d);
    r += f2(t.slice(o, i)), f ? r += U$(d, u || "html") : r += t.slice(i, l), o = l;
  }
  return r += f2(t.slice(o)), r;
}
function h2(e = "") {
  return String(e || "").replace(/@@XBHTMLBLOCK:([a-z0-9-]+)@@|@@XB_HTML_BLOCK_([a-z0-9-]+)@@/g, (t, n, r) => `<span class="xb-markdown-html-placeholder" data-xb-html-block-id="${n || r}"></span>`);
}
function z$(e = "") {
  const t = (n, r) => {
    const o = u_.get(r) || "";
    return u_.delete(r), o;
  };
  return String(e || "").replace(/<p>\s*@@XBHTMLRAW:([a-z0-9-]+)@@\s*<\/p>/g, t).replace(/(^|[\r\n])@@XBHTMLRAW:([a-z0-9-]+)@@(?=[\r\n]|$)/g, (n, r, o) => `${r}${t(n, o)}`);
}
function W$(e = "") {
  return String(e || "").replace(/&#x([0-9a-f]+);?/gi, (t, n) => String.fromCodePoint(Number.parseInt(n, 16) || 0)).replace(/&#([0-9]+);?/g, (t, n) => String.fromCodePoint(Number.parseInt(n, 10) || 0)).replace(/&colon;?/gi, ":").replace(/&tab;?/gi, "	").replace(/&newline;?/gi, `
`).replace(/&amp;?/gi, "&");
}
function G$(e = "") {
  const t = W$(e).trim().replace(/[\u0000-\u001F\u007F\s]+/g, "").toLowerCase();
  return /^(?:javascript|vbscript|data):/.test(t);
}
function V$(e = "") {
  return String(e || "").replace(/<style>([\s\S]+?)<\/style>/gim, (t, n) => `<custom-style>${encodeURIComponent(n)}</custom-style>`);
}
function K$(e = "") {
  const t = [];
  let n = "", r = 0;
  for (const o of String(e || "")) {
    if ((o === "(" || o === "[") && (r += 1), (o === ")" || o === "]") && r > 0 && (r -= 1), o === "," && r === 0) {
      t.push(n), n = "";
      continue;
    }
    n += o;
  }
  return n && t.push(n), t;
}
function J$(e = "") {
  const t = new RegExp(`:(${[
    "has",
    "not",
    "where",
    "is",
    "matches",
    "any"
  ].join("|")})\\(([^)]+)\\)`, "g"), n = (r = "") => String(r || "").split(/\s+/).map((o) => o.replace(/\.([\w-]+)/g, (s, a) => String(a || "").startsWith("custom-") ? s : `.custom-${a}`)).join(" ");
  return n(String(e || "").replace(t, (r, o, s) => `:${o}(${n(s)})`));
}
function Y$(e = Lp) {
  const t = (Array.isArray(e) ? e : [e]).map((n) => String(n || "").trim()).filter(Boolean).map((n) => `${n} `);
  return t.length ? t : Lp.map((n) => `${n} `);
}
function X$(e = "", t = Lp) {
  const n = Y$(t);
  return String(e || "").replace(/@import[^;]+;?/gi, "").replace(/(^|[{}])\s*([^@{}][^{}]*)\{/g, (r, o, s) => {
    const a = K$(s).map((i) => i.trim()).filter(Boolean);
    return !a.length || a.every((i) => /^(?:from|to|\d+(?:\.\d+)?%)$/i.test(i)) ? r : `${o}${a.flatMap((i) => {
      const l = J$(i);
      return n.map((u) => `${u}${l}`);
    }).join(", ")}{`;
  }).replace(/[^{};]+:\s*[^{};]*:\/\/[^{};]*(?:;|(?=}))/g, "");
}
function p2(e = "", t = {}) {
  const n = Array.isArray(t.prefixes) ? t.prefixes : t.prefix ? [t.prefix] : Lp;
  return String(e || "").replace(/<custom-style>([\s\S]+?)<\/custom-style>/gim, (r, o) => {
    try {
      return `<style>${X$(decodeURIComponent(String(o || "")).replaceAll(/<br\/>/g, ""), n)}</style>`;
    } catch (s) {
      return `CSS ERROR: ${s instanceof Error ? s.message : String(s || "decode_failed")}`;
    }
  });
}
function Q$(e = "") {
  return String(e || "").split(/\s+/).filter(Boolean).map((t) => t.startsWith("fa-") || t.startsWith("note-") || t === "monospace" || t.startsWith("custom-") ? t : `custom-${t}`).join(" ");
}
function Z$(e = "") {
  return String(e || "").replace(/<\/?(?:script|style|iframe|object|embed|link|meta|base|form|input|button|textarea|select|option)[^>]*>/gi, "").replace(/\s+on[a-z0-9_-]+\s*=\s*(?:"[^"]*"|'[^']*'|[^\s"'=<>`]+)/gi, "").replace(/\s+class\s*=\s*("[^"]*"|'[^']*'|[^\s"'=<>`]+)/gi, (t, n) => {
    const r = String(n || "")[0], o = r === '"' || r === "'", s = Q$(o ? String(n).slice(1, -1) : String(n || ""));
    return o ? ` class=${r}${s}${r}` : ` class=${s}`;
  }).replace(/\s+(href|src|xlink:href)\s*=\s*("[^"]*"|'[^']*'|[^\s"'=<>`]+)/gi, (t, n, r) => G$(String(r || "").replace(/^["']|["']$/g, "")) ? "" : ` ${n}=${r}`);
}
function m2(e) {
  return typeof e?.sanitize == "function" ? e : null;
}
function j$() {
  try {
    const e = globalThis.parent && globalThis.parent !== globalThis ? m2(globalThis.parent.DOMPurify) : null;
    if (e) return e;
  } catch {
  }
  return m2(globalThis.DOMPurify);
}
function e9(e = "") {
  const t = V$(e), n = j$(), r = {
    RETURN_DOM: !1,
    RETURN_DOM_FRAGMENT: !1,
    RETURN_TRUSTED_TYPE: !1,
    MESSAGE_SANITIZE: !0,
    ADD_TAGS: ["custom-style"]
  };
  if (n) try {
    return p2(String(n.sanitize(t, r) || ""));
  } catch {
  }
  return p2(Z$(t));
}
function t9(e) {
  const t = String(e || "").trim();
  if (!t) return "";
  const n = H$(t);
  try {
    return Kv || (Kv = new M$.default.Converter({
      emoji: !0,
      literalMidWordUnderscores: !0,
      parseImgDimensions: !0,
      simpleLineBreaks: !0,
      strikethrough: !0,
      tables: !0,
      underline: !0,
      disableForced4SpacesIndentedSublists: !0
    })), h2(e9(z$(Kv.makeHtml(n))));
  } catch {
  }
  return h2($$(n).replace(/\n/g, "<br>"));
}
async function n9(e = "", t = null) {
  const n = String(e || "");
  if (!n) return !1;
  const r = t?.createElement ? t : globalThis.document, o = r?.defaultView || globalThis;
  try {
    if (o.navigator?.clipboard?.writeText)
      return await o.navigator.clipboard.writeText(n), !0;
  } catch {
  }
  try {
    if (!r?.createElement || !r.body?.appendChild) return !1;
    const s = r.createElement("textarea");
    s.value = n, s.setAttribute("readonly", "readonly"), s.style.position = "fixed", s.style.left = "-9999px", s.style.top = "0", s.style.opacity = "0", s.style.pointerEvents = "none", s.style.fontSize = "16px", r.body.appendChild(s);
    try {
      s.focus({ preventScroll: !0 });
    } catch {
      s.focus();
    }
    s.select(), s.setSelectionRange(0, s.value.length);
    const a = r.execCommand?.("copy") || !1;
    return s.remove(), a;
  } catch {
    return !1;
  }
}
function r9(e, t = {}) {
  if (!e || typeof t.onPathClick != "function") return;
  const n = e.ownerDocument || globalThis.document, r = n?.defaultView?.NodeFilter || globalThis.NodeFilter;
  if (!n?.createTreeWalker || !r) return;
  const o = t.pathRegex || /local\/[^\s`"'<>，。；：！？（）()[\]{}]+/g, s = String(t.linkClassName || "xb-markdown-local-path-link"), a = n.createTreeWalker(e, r.SHOW_TEXT), i = [];
  for (; a.nextNode(); ) {
    const l = a.currentNode;
    !l?.nodeValue || !String(l.nodeValue).includes("local/") || l.parentElement?.closest?.("button, a, textarea, input") || i.push(l);
  }
  i.forEach((l) => {
    const u = String(l.nodeValue || ""), d = n.createDocumentFragment();
    let f = null, m = 0, v = !1;
    for (o.lastIndex = 0; (f = o.exec(u)) !== null; ) {
      v = !0, f.index > m && d.appendChild(n.createTextNode(u.slice(m, f.index)));
      const y = f[0], T = n.createElement("button");
      T.type = "button", T.className = s, T.textContent = y, T.addEventListener("click", () => {
        t.onPathClick(y);
      }), d.appendChild(T), m = f.index + y.length;
    }
    v && (m < u.length && d.appendChild(n.createTextNode(u.slice(m))), l.parentNode?.replaceChild(d, l));
  });
}
function o9(e, t = {}) {
  if (!e?.querySelectorAll) return;
  const n = e.ownerDocument || globalThis.document;
  if (!n?.createElement) return;
  const r = String(t.codeBlockClassName || "xb-markdown-codeblock"), o = String(t.codeCopyClassName || "xb-markdown-code-copy"), s = String(t.copyButtonTitle || "复制代码"), a = String(t.copySuccessTitle || "已复制"), i = String(t.copyFailureTitle || "复制失败"), l = t.flattenPreCode === !0;
  Array.from(e.querySelectorAll("pre")).forEach((u) => {
    if (u.closest(`.${r}`)) return;
    const d = u.children.length === 1 && u.firstElementChild?.tagName === "CODE" ? u.firstElementChild : null;
    if (l && d) {
      for (; d.firstChild; ) u.insertBefore(d.firstChild, d);
      d.remove();
    }
    const f = n.createElement("div");
    f.className = r;
    const m = n.createElement("button");
    m.type = "button", m.className = o, m.textContent = "⧉", m.title = s, m.setAttribute("aria-label", s), m.addEventListener("pointerdown", (v) => {
      v.stopPropagation();
    }), m.addEventListener("pointerup", (v) => {
      v.stopPropagation();
    }), m.addEventListener("touchstart", (v) => {
      v.stopPropagation();
    }, { passive: !0 }), m.addEventListener("touchend", (v) => {
      v.stopPropagation();
    }, { passive: !0 }), m.addEventListener("click", async (v) => {
      v.preventDefault(), v.stopPropagation();
      const y = await n9(u.querySelector("code")?.textContent || u.textContent || "", n);
      m.textContent = y ? "✓" : "!", m.title = y ? a : i, m.setAttribute("aria-label", y ? a : i), m.classList.toggle("is-copied", y), m.classList.toggle("is-failed", !y), setTimeout(() => {
        m.textContent = "⧉", m.title = s, m.setAttribute("aria-label", s), m.classList.remove("is-copied", "is-failed");
      }, 1200);
    }), u.parentNode?.insertBefore(f, u), f.append(m, u);
  });
}
function g2(e = [], t = null) {
  e.forEach((n) => {
    n.classList.toggle("is-active", n === t), n.setAttribute("aria-pressed", n === t ? "true" : "false");
  });
}
function v2(e, t = "") {
  const n = e.createElement("pre");
  return n.className = "xb-markdown-html-code", n.textContent = String(t || ""), n;
}
function y2(e, t = "") {
  const n = e.createElement("iframe");
  return n.className = "xb-markdown-html-preview", n.setAttribute("sandbox", R$), n.referrerPolicy = "no-referrer", n.title = "HTML 渲染预览", n.srcdoc = String(t || ""), n;
}
function s9(e, t = {}, n = {}) {
  const r = String(t.code || "");
  if (n.htmlBlockMode === "code") return v2(e, r);
  if (n.htmlBlockMode === "preview") {
    const T = e.createElement("div");
    T.className = "xb-markdown-html-block xb-markdown-html-block-auto";
    const C = e.createElement("div");
    return C.className = "xb-markdown-html-body", C.append(y2(e, r)), T.append(C), T;
  }
  const o = e.createElement("div");
  o.className = "xb-markdown-html-block";
  const s = e.createElement("div");
  s.className = "xb-markdown-html-head";
  const a = e.createElement("div");
  a.className = "xb-markdown-html-title", a.textContent = "HTML 片段";
  const i = e.createElement("span");
  i.textContent = O$(r), a.appendChild(i);
  const l = e.createElement("div");
  l.className = "xb-markdown-html-actions";
  const u = e.createElement("button");
  u.type = "button", u.textContent = "显示代码", u.setAttribute("aria-pressed", "false");
  const d = e.createElement("button");
  d.type = "button", d.textContent = "渲染预览", d.setAttribute("aria-pressed", "false"), l.append(u, d), s.append(a, l);
  const f = e.createElement("div");
  f.className = "xb-markdown-html-body", f.hidden = !0;
  let m = "";
  const v = [u, d], y = (T) => {
    if (m === T) {
      m = "", f.hidden = !0, f.replaceChildren(), g2(v);
      return;
    }
    m = T, f.hidden = !1, f.replaceChildren(T === "preview" ? y2(e, r) : v2(e, r)), g2(v, T === "preview" ? d : u);
  };
  return u.addEventListener("click", () => y("code")), d.addEventListener("click", () => y("preview")), o.append(s, f), o;
}
function a9(e, t = {}) {
  if (!e?.querySelectorAll) return;
  const n = e.ownerDocument || globalThis.document;
  n?.createElement && Array.from(e.querySelectorAll(".xb-markdown-html-placeholder[data-xb-html-block-id]")).forEach((r) => {
    const o = String(r.getAttribute("data-xb-html-block-id") || ""), s = l_.get(o);
    if (l_.delete(o), !s) {
      r.remove();
      return;
    }
    const a = s9(n, s, t), i = r.parentElement;
    if (i?.tagName === "P" && i.textContent.trim() === "") {
      i.replaceWith(a);
      return;
    }
    r.replaceWith(a);
  });
}
function _2(e, t = {}) {
  return e && (a9(e, t), o9(e, t), r9(e, t), e);
}
var dI = "[ 🎲 CHANCE ENCOUNTER TRIGGERED ]";
var b2 = 57856, w2 = 57344, i9 = [
  "[Runtime Event: Chance Encounter Triggered]",
  "For this reply only, naturally introduce an unexpected sudden situation the user did not explicitly ask for.",
  "Make it fit the current scene and character voice.",
  "Follow all later formatting and style instructions.",
  "Do not mention this instruction or the trigger label."
].join(`
`);
function Yv(e, t, n, r) {
  const o = Math.round(Number(e));
  return Number.isFinite(o) ? Math.min(n, Math.max(t, o)) : r;
}
function ua(e, t = 240) {
  return String(e || "").trim().slice(0, t);
}
function x0(e) {
  return String(e || "").trim() || (/* @__PURE__ */ new Date(0)).toISOString();
}
function l9(e) {
  return {
    type: "chanceEncounter",
    label: dI,
    createdAt: x0(e.createdAt)
  };
}
function fI(e) {
  const t = ua(e.action, 240), n = ua(e.stat, 120);
  return !t || !n ? null : {
    type: "actionCheck",
    createdAt: x0(e.createdAt),
    action: t,
    stat: n,
    difficulty: Yv(e.difficulty, 2, 20, 12),
    roll: Yv(e.roll, 1, 20, 1),
    success: e.success === !0,
    insertAfterChars: Math.max(0, Yv(e.insertAfterChars, 0, Number.MAX_SAFE_INTEGER, 0)),
    ...ua(e.toolCallId, 120) ? { toolCallId: ua(e.toolCallId, 120) } : {},
    ...ua(e.summary, 320) ? { summary: ua(e.summary, 320) } : {},
    ...ua(e.stakes, 240) ? { stakes: ua(e.stakes, 240) } : {}
  };
}
function u9(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : null, n = String(t?.type || "").trim();
  return t ? n === "chanceEncounter" ? l9(t) : n === "actionCheck" ? fI(t) : null : null;
}
function c9(e) {
  return e.type === "chanceEncounter" ? e.type : [
    e.type,
    e.toolCallId || "",
    e.createdAt,
    String(e.insertAfterChars),
    e.stat,
    e.action,
    String(e.roll),
    String(e.difficulty)
  ].join("\0");
}
function M0(e) {
  const t = Array.isArray(e) ? e : [], n = [], r = /* @__PURE__ */ new Set();
  return t.forEach((o) => {
    const s = u9(o);
    if (!s) return;
    const a = c9(s);
    r.has(a) || (r.add(a), n.push(s));
  }), n;
}
function d9(e = (/* @__PURE__ */ new Date()).toISOString()) {
  return {
    type: "chanceEncounter",
    label: dI,
    createdAt: x0(e)
  };
}
function f9(e) {
  return fI({
    type: "actionCheck",
    createdAt: e.createdAt || (/* @__PURE__ */ new Date()).toISOString(),
    action: e.action,
    stat: e.stat,
    difficulty: e.difficulty,
    roll: e.roll,
    success: e.success,
    insertAfterChars: e.insertAfterChars,
    toolCallId: e.toolCallId,
    summary: e.summary,
    stakes: e.stakes
  });
}
function P0(e) {
  return M0(e).find((t) => t.type === "chanceEncounter") || null;
}
function h9(e) {
  return !!P0(e);
}
function Na(e) {
  return M0(e).filter((t) => t.type === "actionCheck");
}
function p9(e) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = t && typeof t == "object" && !Array.isArray(t) ? t : {}, r = String(n.marker || "").slice(0, 1), o = Na(n.events);
    return r && o.length ? {
      marker: r,
      events: o
    } : null;
  }).filter((t) => !!t);
}
function m9(e, t = []) {
  const n = Na(t).slice().sort((i, l) => i.insertAfterChars - l.insertAfterChars);
  if (!n.length) return {
    text: e,
    groups: []
  };
  const r = [];
  if (n.forEach((i) => {
    const l = Math.max(0, Math.min(e.length, Number(i.insertAfterChars) || 0)), u = r[r.length - 1];
    if (u && u.offset === l) {
      u.events.push(i);
      return;
    }
    r.push({
      offset: l,
      events: [i]
    });
  }), r.length > 63743 - b2) return {
    text: e,
    groups: []
  };
  let o = 0, s = "";
  const a = r.map((i, l) => {
    const u = String.fromCharCode(b2 + l);
    return s += e.slice(o, i.offset) + u, o = i.offset, {
      marker: u,
      events: i.events
    };
  });
  return s += e.slice(o), {
    text: s,
    groups: a
  };
}
function c_(e, t = []) {
  const n = String(e || ""), r = [...new Set(Na(t).map((i) => Math.max(0, Math.min(n.length, Number(i.insertAfterChars) || 0))))].sort((i, l) => i - l);
  if (!r.length || r.length > 63743 - w2) return {
    text: n,
    boundaries: []
  };
  let o = 0, s = "";
  const a = r.map((i, l) => {
    const u = String.fromCharCode(w2 + l);
    return s += n.slice(o, i) + u, o = i, {
      originalOffset: i,
      marker: u
    };
  });
  return s += n.slice(o), {
    text: s,
    boundaries: a
  };
}
function hI(e, t = [], n = []) {
  const r = String(e || ""), o = Na(t);
  if (!n.length || !o.length) return {
    text: r,
    events: o
  };
  const s = new Map(n.map((l) => [l.marker, l.originalOffset])), a = /* @__PURE__ */ new Map();
  let i = "";
  for (let l = 0; l < r.length; l += 1) {
    const u = r[l], d = s.get(u);
    if (d !== void 0) {
      a.has(d) || a.set(d, i.length);
      continue;
    }
    i += u;
  }
  return {
    text: i,
    events: o.map((l) => {
      const u = Math.max(0, Number(l.insertAfterChars) || 0);
      return {
        ...l,
        insertAfterChars: a.has(u) ? a.get(u) : Math.max(0, Math.min(i.length, u))
      };
    })
  };
}
function pI() {
  return {
    role: "system",
    content: i9
  };
}
var di = /\[tavern-image:([a-z0-9\-_]+)\]/gi, fi = /\[(?:img|图片)\s*:\s*([^\]]+)\]/gi, d_ = "xb-tavern:inline-image-progress";
function S2(e = "", t = "") {
  return String(e || "").trim() || t;
}
function g9(e = "", t = {}) {
  const n = S2(t.userName, "User"), r = S2(t.characterName, "角色");
  return String(e || "").replace(/<\s*user\s*>/gi, n).replace(/<\s*(?:char|bot)\s*>/gi, r).replace(/\{\{\s*user\s*\}\}/gi, n).replace(/\{\{\s*(?:char|bot)\s*\}\}/gi, r);
}
function v9(e = "", t = {}) {
  return g9(e, t);
}
function y9(e) {
  const t = /* @__PURE__ */ new Map();
  function n(L) {
    return !L || !!L.closest('[hidden], [aria-hidden="true"]');
  }
  function r(L) {
    return !!L && !n(L);
  }
  function o(L) {
    return !!(L && L.isConnected !== !1 && !n(L) && !L.closest(".streaming, .pending-user"));
  }
  function s(L = "") {
    const P = String(L || "");
    let Q = 0;
    for (let V = 0; V < P.length; V += 1) Q = Q * 31 + P.charCodeAt(V) >>> 0;
    return `${P.length}:${Q.toString(36)}`;
  }
  function a(L = "", P = {}) {
    const Q = P.roleplay ? v9(L, P) : String(L || ""), V = !/(^|\n)(`{3,}|~{3,})[ \t]*(html|htm|xhtml|xml|svg|vue|svelte)?\b/i.test(Q) && !P.roleplay, ae = s(Q);
    if (V && t.has(ae)) return t.get(ae) || "";
    const Se = t9(Q);
    if (V && !Se.includes("xb-markdown-html-placeholder") && (t.set(ae, Se), t.size > 160)) {
      const Me = t.keys().next().value;
      Me && t.delete(Me);
    }
    return Se;
  }
  function i(L = "") {
    return String(L || "").replace(di, "").replace(fi, "").trim();
  }
  function l() {
    t.clear();
  }
  const u = {
    '"': '"',
    "“": "”",
    "「": "」",
    "『": "』"
  }, d = new Set(Object.keys(u)), f = /* @__PURE__ */ new Set([
    "A",
    "BUTTON",
    "CODE",
    "KBD",
    "PRE",
    "SCRIPT",
    "STYLE",
    "TEXTAREA"
  ]), m = 600;
  function v(L) {
    let P = L.parentElement;
    for (; P; ) {
      if (f.has(P.tagName) || P.classList.contains("xb-rp-dialogue")) return !0;
      P = P.parentElement;
    }
    return !1;
  }
  function y(L) {
    const P = [];
    let Q = 0;
    for (; Q < L.length; ) {
      const V = L[Q];
      if (!d.has(V)) {
        Q += 1;
        continue;
      }
      const ae = u[V];
      let Se = L.indexOf(ae, Q + 1);
      if (Se < 0 || Se === Q + 1 || Se - Q > m) {
        Q += 1;
        continue;
      }
      const Me = L.slice(Q + 1, Se);
      if (Me.includes(`
`) || !Me.trim()) {
        Q += 1;
        continue;
      }
      P.push({
        start: Q,
        end: Se + 1
      }), Q = Se + 1;
    }
    return P;
  }
  function T(L) {
    const P = document.createTreeWalker(L, NodeFilter.SHOW_TEXT, { acceptNode(V) {
      const ae = V;
      return !ae.data || v(ae) ? NodeFilter.FILTER_REJECT : y(ae.data).length ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
    } }), Q = [];
    for (; P.nextNode(); ) Q.push(P.currentNode);
    Q.forEach((V) => {
      const ae = y(V.data);
      if (!ae.length) return;
      const Se = document.createDocumentFragment();
      let Me = 0;
      ae.forEach((Ke) => {
        Ke.start > Me && Se.append(document.createTextNode(V.data.slice(Me, Ke.start)));
        const qe = document.createElement("span");
        qe.className = "xb-rp-dialogue", qe.textContent = V.data.slice(Ke.start, Ke.end), Se.append(qe), Me = Ke.end;
      }), Me < V.data.length && Se.append(document.createTextNode(V.data.slice(Me))), V.replaceWith(Se);
    });
  }
  function C(L, P = "") {
    return !!(L && L.isConnected !== !1 && !n(L) && String(L.dataset.tavernImageSlot || "").trim() === P);
  }
  function A(L) {
    return L.result || L;
  }
  function I(L) {
    return Array.isArray(L.characterPrompts) ? L.characterPrompts.filter((P) => !!P && typeof P == "object") : [];
  }
  function R(L, P) {
    const Q = document.createElement("span");
    Q.className = "xb-tavern-image-edit-group";
    const V = document.createElement("span");
    V.className = "xb-tavern-image-edit-group-label", V.textContent = P.label, Q.append(V);
    const ae = document.createElement("textarea");
    return ae.className = "xb-tavern-image-edit-input", ae.dataset.type = P.type, typeof P.index == "number" && (ae.dataset.index = String(P.index)), ae.value = P.value, Q.append(ae), L.append(Q), ae;
  }
  function $(L, P) {
    L.replaceChildren();
    const Q = R(L, {
      label: "场景",
      value: String(P.tags || ""),
      type: "scene"
    });
    return I(P).forEach((V, ae) => {
      R(L, {
        label: String(V.name || `角色 ${ae + 1}`),
        value: String(V.prompt || ""),
        type: "char",
        index: ae
      });
    }), Q;
  }
  function p(L, P, Q) {
    const V = document.createElement("span");
    V.className = "xb-tavern-image-edit", V.style.display = "none";
    const ae = document.createElement("span");
    ae.className = "xb-tavern-image-edit-label", ae.textContent = "编辑 TAG（场景描述）";
    const Se = document.createElement("span");
    Se.className = "xb-tavern-image-edit-scroll";
    const Me = document.createElement("span");
    Me.className = "xb-tavern-image-edit-actions", [{
      action: "save-tags",
      label: "保存 TAG"
    }, {
      action: "cancel-edit",
      label: "取消"
    }].forEach((qe) => {
      const Be = document.createElement("button");
      Be.type = "button", Be.dataset.action = qe.action, Be.textContent = qe.label, Me.append(Be);
    }), V.append(ae, Se, Me);
    const Ke = () => {
      const qe = $(Se, Q);
      V.style.display = "block", qe.focus();
    };
    return V.addEventListener("click", (qe) => {
      const Be = qe.target?.closest("[data-action]");
      if (!Be) return;
      qe.preventDefault(), qe.stopPropagation();
      const Ee = Be.dataset.action || "";
      if (Ee === "cancel-edit") {
        V.style.display = "none", Se.replaceChildren();
        return;
      }
      if (Ee === "save-tags") {
        const Ye = V.querySelector('textarea[data-type="scene"]'), je = String(Ye?.value || "").trim();
        if (!je) {
          window.alert("场景 TAG 不能为空");
          return;
        }
        const It = b(V, Q);
        w(L, !0), e.requestHost("xb-tavern:draw-image-edit", { payload: {
          slotId: P,
          tags: je,
          characterPrompts: It
        } }).then((Ht) => M(L, P, Ht)).catch(() => w(L, !1));
      }
    }), {
      editPanel: V,
      openEditor: Ke
    };
  }
  function b(L, P) {
    const Q = I(P), V = Array.from(L.querySelectorAll('textarea[data-type="char"]'));
    if (!V.length || !Q.length) return null;
    const ae = [];
    return V.forEach((Se) => {
      const Me = Math.floor(Number(Se.dataset.index)), Ke = Number.isFinite(Me) ? Q[Me] : null;
      Ke && ae.push({
        ...Ke,
        prompt: Se.value.trim()
      });
    }), ae;
  }
  function w(L, P) {
    L.classList.toggle("is-busy", P), L.querySelectorAll("button").forEach((Q) => {
      Q.disabled = P || Q.dataset.navDisabled === "true";
    });
  }
  function E(L = document) {
    L.querySelectorAll(".xb-tavern-image-menu.is-open").forEach((P) => P.classList.remove("is-open"));
  }
  function x() {
    document.getElementById("xb-tavern-image-gallery-overlay")?.remove();
  }
  async function M(L, P = "", Q) {
    if (!C(L, P)) return;
    const V = A(Q);
    V.hasData && V.url ? J(L, V) : z(L, V);
  }
  async function N(L = "", P = 0, Q) {
    const V = await e.requestHost("xb-tavern:draw-image-select", { payload: {
      slotId: L,
      index: P
    } });
    return Q && await M(Q, L, V), A(V);
  }
  async function q(L = "", P) {
    const Q = A(await e.requestHost("xb-tavern:draw-image-gallery", { payload: { slotId: L } })), V = Array.isArray(Q.previews) ? Q.previews : [];
    if (!V.length) return;
    x();
    let ae = Math.max(0, Math.min(V.length - 1, Math.floor(Number(Q.currentIndex) || 0)));
    const Se = document.createElement("div");
    Se.id = "xb-tavern-image-gallery-overlay", Se.className = "xb-tavern-image-gallery-overlay visible";
    const Me = document.createElement("button");
    Me.type = "button", Me.className = "xb-tavern-image-gallery-close", Me.textContent = "×";
    const Ke = document.createElement("div");
    Ke.className = "xb-tavern-image-gallery-main";
    const qe = document.createElement("button");
    qe.type = "button", qe.className = "xb-tavern-image-gallery-nav", qe.textContent = "‹";
    const Be = document.createElement("div");
    Be.className = "xb-tavern-image-gallery-img-wrap";
    const Ee = document.createElement("img");
    Ee.className = "xb-tavern-image-gallery-img";
    const Ye = document.createElement("div");
    Ye.className = "xb-tavern-image-gallery-saved-badge", Ye.textContent = "已保存", Be.append(Ee, Ye);
    const je = document.createElement("button");
    je.type = "button", je.className = "xb-tavern-image-gallery-nav", je.textContent = "›", Ke.append(qe, Be, je);
    const It = document.createElement("div");
    It.className = "xb-tavern-image-gallery-thumbs";
    const Ht = document.createElement("div");
    Ht.className = "xb-tavern-image-gallery-actions";
    const At = document.createElement("button");
    At.type = "button", At.className = "xb-tavern-image-gallery-btn primary", At.textContent = "使用此图";
    const Mt = document.createElement("button");
    Mt.type = "button", Mt.className = "xb-tavern-image-gallery-btn";
    const Ut = document.createElement("button");
    Ut.type = "button", Ut.className = "xb-tavern-image-gallery-btn danger", Ut.textContent = "删除", Ht.append(At, Mt, Ut);
    const yt = document.createElement("div");
    yt.className = "xb-tavern-image-gallery-info", Se.append(Me, Ke, It, Ht, yt), document.body.append(Se);
    const et = () => {
      const xe = V[ae] || V[0];
      Ee.src = String(xe.url || ""), Ye.style.display = xe.saved ? "block" : "none", qe.disabled = ae >= V.length - 1, je.disabled = ae <= 0, Mt.textContent = xe.saved ? "✓ 已保存" : "保存到服务器", Mt.disabled = !!xe.saved, yt.textContent = `版本 ${V.length - ae} / ${V.length}${xe.timestamp ? ` · ${new Date(Number(xe.timestamp)).toLocaleString()}` : ""}`, It.querySelectorAll(".xb-tavern-image-gallery-thumb").forEach((Ze) => {
        Ze.classList.toggle("active", Number(Ze.dataset.index) === ae);
      });
    };
    V.slice().reverse().forEach((xe, Ze) => {
      const zt = V.length - 1 - Ze, jt = document.createElement("img");
      jt.className = `xb-tavern-image-gallery-thumb${xe.saved ? " saved" : ""}`, jt.src = String(xe.url || ""), jt.dataset.index = String(zt), jt.loading = "lazy", jt.addEventListener("click", () => {
        ae = zt, et();
      }), It.append(jt);
    }), Me.addEventListener("click", x), Se.addEventListener("click", (xe) => {
      xe.target === Se && x();
    }), qe.addEventListener("click", () => {
      ae = Math.min(V.length - 1, ae + 1), et();
    }), je.addEventListener("click", () => {
      ae = Math.max(0, ae - 1), et();
    }), At.addEventListener("click", () => {
      N(L, ae, P).then(x);
    }), Mt.addEventListener("click", () => {
      const xe = V[ae] || V[0], Ze = String(xe?.imgId || "").trim();
      Ze && (Mt.disabled = !0, e.requestHost("xb-tavern:draw-image-save", { payload: {
        slotId: L,
        imgId: Ze
      } }).then((zt) => (xe.saved = !0, et(), M(P, L, zt))).catch(() => {
        Mt.disabled = !1;
      }));
    }), Ut.addEventListener("click", () => {
      if (!window.confirm("确定删除这张图片吗？")) return;
      const xe = V[ae] || V[0], Ze = String(xe?.imgId || "").trim();
      Ze && e.requestHost("xb-tavern:draw-image-delete", { payload: {
        slotId: L,
        imgId: Ze
      } }).then((zt) => M(P, L, zt)).then(() => {
        if (V.splice(ae, 1), !V.length) {
          x();
          return;
        }
        ae = Math.max(0, Math.min(V.length - 1, ae)), It.querySelectorAll(".xb-tavern-image-gallery-thumb").forEach((zt) => {
          Number(zt.dataset.index) === ae && zt.remove();
        }), x(), q(L, P);
      });
    }), et();
  }
  function z(L, P = {}) {
    L.classList.add("is-failed"), L.classList.remove("is-loaded", "is-busy"), L.dataset.tavernImageSlot = String(P.slotId || L.dataset.tavernImageSlot || "").trim(), L.dataset.tavernImageHydrating = "false", L.dataset.tavernImageLoaded = "false", L.dataset.state = "failed";
    const Q = String(P.slotId || L.dataset.tavernImageSlot || "").trim(), V = document.createElement("span");
    V.className = "xb-tavern-image-wrap xb-tavern-image-failed-wrap";
    const ae = document.createElement("span");
    if (ae.className = "xb-tavern-image-placeholder", ae.textContent = P.isFailed ? String(P.errorMessage || "配图生成失败") : String(P.errorMessage || "配图未找到"), V.append(ae), P.isFailed && Q) {
      const Se = document.createElement("span");
      Se.className = "xb-tavern-image-failed-actions";
      const Me = document.createElement("button");
      Me.type = "button", Me.dataset.action = "refresh-image", Me.textContent = "重试";
      const Ke = document.createElement("button");
      Ke.type = "button", Ke.dataset.action = "edit-tags", Ke.textContent = "编辑 TAG", Se.append(Me, Ke), V.append(Se);
      const { editPanel: qe, openEditor: Be } = p(L, Q, P);
      V.append(qe), Se.addEventListener("click", (Ee) => {
        const Ye = Ee.target?.closest("[data-action]");
        if (!Ye) return;
        Ee.preventDefault(), Ee.stopPropagation();
        const je = Ye.dataset.action || "";
        if (je === "refresh-image") {
          w(L, !0), e.requestHost("xb-tavern:draw-image-refresh", { payload: { slotId: Q } }).then((It) => M(L, Q, It)).catch(() => w(L, !1));
          return;
        }
        je === "edit-tags" && Be();
      });
    }
    L.replaceChildren(V);
  }
  function J(L, P) {
    const Q = String(P.slotId || L.dataset.tavernImageSlot || "").trim(), V = String(P.url || "").trim(), ae = Math.max(1, Math.floor(Number(P.historyCount) || 1)), Se = Math.max(0, Math.min(ae - 1, Math.floor(Number(P.currentIndex) || 0))), Me = ae - Se;
    L.classList.add("is-loaded"), L.classList.remove("is-failed", "is-busy"), L.dataset.tavernImageSlot = Q, L.dataset.tavernImageLoaded = "true", L.dataset.tavernImageIndex = String(Se), L.dataset.tavernImageHistory = String(ae), L.dataset.tavernImageImgId = String(P.imgId || ""), L.dataset.state = P.saved ? "saved" : "preview";
    const Ke = document.createElement("span");
    Ke.className = "xb-tavern-image-wrap";
    const qe = document.createElement("img");
    qe.src = V, qe.alt = P.tags ? `配图：${String(P.tags)}` : "配图", qe.loading = "lazy", qe.addEventListener("click", () => {
      q(Q, L);
    }), Ke.append(qe);
    const Be = document.createElement("span");
    Be.className = "xb-tavern-image-nav";
    const Ee = document.createElement("button");
    Ee.type = "button", Ee.className = "xb-tavern-image-nav-button", Ee.dataset.action = "older", Ee.title = "上一版本", Ee.textContent = "‹", Ee.disabled = Se >= ae - 1, Ee.dataset.navDisabled = Ee.disabled ? "true" : "false";
    const Ye = document.createElement("span");
    Ye.className = "xb-tavern-image-nav-text", Ye.textContent = `${Me} / ${ae}`;
    const je = document.createElement("button");
    je.type = "button", je.className = "xb-tavern-image-nav-button", je.dataset.action = "newer", je.title = Se === 0 ? "重新生成" : "下一版本", je.textContent = "›", je.disabled = !1, je.dataset.navDisabled = "false", Be.append(Ee, Ye, je), Ke.append(Be);
    const It = document.createElement("span");
    It.className = "xb-tavern-image-menu";
    const Ht = document.createElement("button");
    Ht.type = "button", Ht.className = "xb-tavern-image-menu-trigger", Ht.title = "操作", Ht.textContent = "⋮";
    const At = document.createElement("span");
    At.className = "xb-tavern-image-dropdown", [
      ...P.saved ? [] : [{
        action: "save-image",
        title: "保存到服务器",
        label: "⬇"
      }],
      {
        action: "refresh-image",
        title: "重新生成",
        label: "⟳"
      },
      {
        action: "edit-tags",
        title: "编辑TAG",
        label: "✐️"
      },
      {
        action: "delete-image",
        title: "删除",
        label: "✕"
      }
    ].forEach((xe) => {
      const Ze = document.createElement("button");
      Ze.type = "button", Ze.dataset.action = xe.action, Ze.title = xe.title, Ze.textContent = xe.label, At.append(Ze);
    }), It.append(Ht, At), Ke.append(It);
    const { editPanel: Mt, openEditor: Ut } = p(L, Q, P);
    Ke.append(Mt), L.replaceChildren(Ke);
    const yt = async (xe) => {
      w(L, !0);
      try {
        const Ze = await e.requestHost("xb-tavern:draw-image-select", { payload: {
          slotId: Q,
          index: xe
        } });
        if (!C(L, Q)) return;
        J(L, A(Ze));
      } catch {
        w(L, !1);
      }
    }, et = async () => {
      w(L, !0);
      try {
        await M(L, Q, await e.requestHost("xb-tavern:draw-image-refresh", { payload: { slotId: Q } }));
      } catch {
        w(L, !1);
      }
    };
    Ee.addEventListener("click", (xe) => {
      xe.preventDefault(), xe.stopPropagation(), Se < ae - 1 && yt(Se + 1);
    }), je.addEventListener("click", (xe) => {
      if (xe.preventDefault(), xe.stopPropagation(), Se > 0) {
        yt(Se - 1);
        return;
      }
      et();
    }), Ht.addEventListener("click", (xe) => {
      xe.preventDefault(), xe.stopPropagation();
      const Ze = !It.classList.contains("is-open");
      E(), It.classList.toggle("is-open", Ze);
    }), At.addEventListener("click", (xe) => {
      const Ze = xe.target?.closest("[data-action]");
      if (!Ze) return;
      xe.preventDefault(), xe.stopPropagation(), E();
      const zt = Ze.dataset.action || "";
      zt === "save-image" ? (w(L, !0), e.requestHost("xb-tavern:draw-image-save", { payload: { slotId: Q } }).then((jt) => {
        C(L, Q) && J(L, A(jt));
      }).catch(() => w(L, !1))) : zt === "refresh-image" ? et() : zt === "edit-tags" ? Ut() : zt === "delete-image" && window.confirm("确定删除这张图片吗？") && (w(L, !0), e.requestHost("xb-tavern:draw-image-delete", { payload: { slotId: Q } }).then((jt) => M(L, Q, jt)).catch(() => w(L, !1)));
    });
  }
  function G(L) {
    const P = String(L.dataset.tavernImageSlot || "").trim();
    !P || L.dataset.tavernImageHydrating === "true" || L.dataset.tavernImageLoaded === "true" || (L.dataset.tavernImageHydrating = "true", e.requestHost("xb-tavern:draw-image", { payload: { slotId: P } }).then((Q) => {
      if (!C(L, P)) return;
      const V = Q.result || Q;
      if (L.dataset.tavernImageHydrating = "false", !V.hasData || !V.url) {
        z(L, V);
        return;
      }
      J(L, V);
    }).catch(() => {
      if (!C(L, P)) return;
      L.dataset.tavernImageHydrating = "false", L.classList.add("is-failed");
      const Q = document.createElement("span");
      Q.className = "xb-tavern-image-placeholder", Q.textContent = "配图加载失败", L.replaceChildren(Q);
    }));
  }
  function oe(L = "") {
    const P = document.createElement("span");
    P.className = "xb-tavern-image", P.dataset.tavernImageSlot = L;
    const Q = document.createElement("span");
    return Q.className = "xb-tavern-image-placeholder", Q.textContent = "配图加载中", P.append(Q), P;
  }
  function B(L) {
    const P = L.parentNode;
    if (!P) return;
    const Q = (V) => {
      let ae = L[V];
      for (; ae?.nodeType === Node.TEXT_NODE && !String(ae.textContent || "").trim(); ) {
        const Se = ae[V];
        P.removeChild(ae), ae = Se;
      }
      if (ae instanceof HTMLBRElement) {
        const Se = ae[V];
        for (P.removeChild(ae), ae = Se; ae?.nodeType === Node.TEXT_NODE && !String(ae.textContent || "").trim(); ) {
          const Me = ae[V];
          P.removeChild(ae), ae = Me;
        }
      }
    };
    Q("previousSibling"), Q("nextSibling");
  }
  function X(L) {
    const P = document.createTreeWalker(L, NodeFilter.SHOW_TEXT, { acceptNode(V) {
      const ae = V;
      return !ae.data || !di.test(ae.data) ? (di.lastIndex = 0, NodeFilter.FILTER_SKIP) : (di.lastIndex = 0, ae.parentElement?.closest?.("a, button, code, kbd, pre, script, style, textarea, .xb-tavern-image") ? NodeFilter.FILTER_REJECT : NodeFilter.FILTER_ACCEPT);
    } }), Q = [];
    for (; P.nextNode(); ) Q.push(P.currentNode);
    Q.forEach((V) => {
      const ae = V.data;
      di.lastIndex = 0;
      let Se, Me = 0, Ke = !1;
      const qe = document.createDocumentFragment();
      for (; (Se = di.exec(ae)) !== null; ) {
        if (Ke = !0, Se.index > Me) {
          const Be = ae.slice(Me, Se.index);
          qe.append(document.createTextNode(Be.endsWith(`
`) ? Be.slice(0, -1) : Be));
        }
        qe.append(oe(Se[1] || "")), Me = di.lastIndex, ae[Me] === `
` && (Me += 1);
      }
      Ke && (Me < ae.length && qe.append(document.createTextNode(ae.slice(Me))), V.replaceWith(qe));
    }), L.querySelectorAll("[data-tavern-image-slot]").forEach((V) => {
      B(V), G(V);
    });
  }
  function de(L = "") {
    let P = String(L || "").trim();
    return P = P.replace(/^(nsfw|sketchy)\s*:\s*/i, "nsfw, "), P.split(",").map((Q) => Q.trim()).filter(Boolean).join(", ");
  }
  function fe(L = "") {
    return encodeURIComponent(de(L));
  }
  function ve(L = "") {
    const P = String(L || "").trim();
    return P ? /^data:[^;]+;base64,/i.test(P) ? P : `data:image/png;base64,${P}` : "";
  }
  function Ce(L, P, Q = "loading") {
    L.dataset.state = Q;
    const V = document.createElement("span");
    V.className = `xb-tavern-inline-image-placeholder is-${Q}`, V.textContent = P, L.replaceChildren(V);
  }
  function Ue(L, P = "") {
    const Q = ve(P);
    if (!Q) throw new Error("image_data_missing");
    L.dataset.loaded = "1", L.dataset.loading = "", L.dataset.state = "loaded";
    const V = document.createElement("img");
    V.className = "xb-tavern-inline-image-img", V.src = Q, V.loading = "lazy", V.addEventListener("click", () => window.open(Q, "_blank")), L.replaceChildren(V);
  }
  function le(L, P = "", Q) {
    L.dataset.loaded = "1", L.dataset.loading = "", L.dataset.state = "error";
    const V = document.createElement("span");
    V.className = "xb-tavern-inline-image-error";
    const ae = document.createElement("span");
    ae.textContent = Q instanceof Error ? Q.message : String(Q || "生成失败");
    const Se = document.createElement("button");
    Se.type = "button", Se.textContent = "重试", Se.addEventListener("click", (Me) => {
      Me.stopPropagation(), L.dataset.loaded = "", H(L, P);
    }), V.append(ae, Se), L.replaceChildren(V);
  }
  function me(L = "", P = {}) {
    const Q = Math.max(0, Math.floor(Number(P.position) || 0)), V = Math.max(0, Math.floor(Number(P.delay) || 0));
    if (L === "queued") {
      const ae = Math.max(0, Q - 1);
      return ae > 0 ? `排队中，前面还有 ${ae} 张` : "排队中";
    }
    return L === "generating" ? "正在生成" : L === "waiting" ? V > 0 ? `等待冷却 ${V}s` : "等待冷却" : "检查缓存...";
  }
  function We(L = {}) {
    const P = de(String(L.tags || ""));
    if (!P) return;
    const Q = fe(P), V = me(String(L.status || "").trim(), L);
    document.querySelectorAll('.xb-tavern-inline-image[data-loading="1"]').forEach((ae) => {
      String(ae.dataset.tags || "") === Q && o(ae) && Ce(ae, V, "loading");
    });
  }
  async function H(L, P = "") {
    const Q = de(P || decodeURIComponent(L.dataset.tags || ""));
    if (!(!Q || L.dataset.loading === "1" || L.dataset.loaded === "1" || !o(L))) {
      L.dataset.tags = fe(Q), L.dataset.loading = "1", L.dataset.loaded = "", Ce(L, "检查缓存...", "loading");
      try {
        const V = await e.requestHost("xb-tavern:inline-image-generate", { payload: { tags: Q } }), ae = V.result && typeof V.result == "object" ? V.result : V;
        Ue(L, String(ae.base64 || ""));
      } catch (V) {
        le(L, Q, V);
      }
    }
  }
  let W = null;
  function se() {
    return W || typeof IntersectionObserver > "u" || (W = new IntersectionObserver((L) => {
      L.forEach((P) => {
        if (!P.isIntersecting) return;
        const Q = P.target;
        o(Q) && (W?.unobserve(Q), H(Q));
      });
    }, {
      rootMargin: "200px 0px",
      threshold: 0.01
    })), W;
  }
  function ke(L = "") {
    const P = document.createElement("span");
    return P.className = "xb-tavern-inline-image", P.dataset.tags = fe(L), Ce(P, "滚动加载", "idle"), P;
  }
  function Pe(L) {
    r(L) && L.querySelectorAll(".xb-tavern-inline-image").forEach((P) => {
      if (P.dataset.observed === "1" || P.dataset.loaded === "1" || P.dataset.loading === "1" || !o(P)) return;
      P.dataset.observed = "1";
      const Q = se();
      if (Q) {
        Q.observe(P);
        return;
      }
      H(P);
    });
  }
  function Le(L) {
    if (!r(L) || L.closest(".streaming, .pending-user")) return;
    const P = document.createTreeWalker(L, NodeFilter.SHOW_TEXT, { acceptNode(V) {
      const ae = V;
      return !ae.data || !fi.test(ae.data) ? (fi.lastIndex = 0, NodeFilter.FILTER_SKIP) : (fi.lastIndex = 0, ae.parentElement?.closest?.("a, button, code, kbd, pre, script, style, textarea, .xb-tavern-image, .xb-tavern-inline-image") ? NodeFilter.FILTER_REJECT : NodeFilter.FILTER_ACCEPT);
    } }), Q = [];
    for (; P.nextNode(); ) Q.push(P.currentNode);
    Q.forEach((V) => {
      const ae = V.data;
      fi.lastIndex = 0;
      let Se, Me = 0, Ke = !1;
      const qe = document.createDocumentFragment();
      for (; (Se = fi.exec(ae)) !== null; ) {
        if (Ke = !0, Se.index > Me) {
          const Ee = ae.slice(Me, Se.index);
          qe.append(document.createTextNode(Ee.endsWith(`
`) ? Ee.slice(0, -1) : Ee));
        }
        const Be = de(Se[1] || "");
        qe.append(ke(Be)), Me = fi.lastIndex, ae[Me] === `
` && (Me += 1);
      }
      Ke && (Me < ae.length && qe.append(document.createTextNode(ae.slice(Me))), V.replaceWith(qe));
    }), L.querySelectorAll(".xb-tavern-inline-image").forEach((V) => {
      B(V);
    }), Pe(L);
  }
  function Y(L) {
    We(L instanceof CustomEvent && L.detail && typeof L.detail == "object" ? L.detail : {});
  }
  typeof window < "u" && window.addEventListener(d_, Y);
  function te(L) {
    const P = L.success ? "判定成功" : "判定失败", Q = String(L.action || "").trim(), V = String(L.stakes || "").trim();
    return [
      `行动判定：${L.stat}。`,
      `掷骰 ${L.roll} 对抗难度 ${L.difficulty}。`,
      `${P}.`,
      Q ? `行动意图：${Q}。` : "",
      V ? `风险：${V}。` : ""
    ].filter(Boolean).join(" ");
  }
  function he(L) {
    const P = document.createElement("span");
    P.className = `action-check-card ${L.success ? "is-success" : "is-failure"}`, P.setAttribute("role", "group"), P.setAttribute("aria-label", te(L));
    const Q = document.createElement("span");
    Q.className = "action-check-card-head";
    const V = document.createElement("strong");
    V.textContent = L.stat, Q.append(V);
    const ae = document.createElement("span");
    ae.className = "action-check-card-outcome", ae.textContent = L.success ? "判定成功" : "判定失败", Q.append(ae);
    const Se = document.createElement("span");
    Se.className = "action-check-card-grid", [{
      className: "action-check-card-roll",
      label: "掷骰",
      value: String(L.roll)
    }, {
      className: "action-check-card-dc",
      label: "难度",
      value: String(L.difficulty)
    }].forEach((qe) => {
      const Be = document.createElement("span");
      Be.className = qe.className;
      const Ee = document.createElement("small");
      Ee.textContent = qe.label;
      const Ye = document.createElement("strong");
      Ye.textContent = qe.value, Be.append(Ee, Ye), Se.append(Be);
    });
    const Me = document.createElement("span");
    Me.className = "action-check-card-copy", Me.textContent = L.action;
    const Ke = [
      Q,
      Se,
      Me
    ];
    if (L.stakes) {
      const qe = document.createElement("span");
      qe.className = "action-check-card-stakes", qe.textContent = L.stakes, Ke.push(qe);
    }
    return P.append(...Ke), P;
  }
  function we(L = []) {
    const P = document.createElement("span");
    return P.className = "assistant-runtime-event-stack", P.setAttribute("role", "group"), P.setAttribute("aria-label", L.length > 1 ? `${L.length} action check results.` : "Action check result."), L.forEach((Q) => {
      P.append(he(Q));
    }), P;
  }
  function Te(L) {
    try {
      return p9(JSON.parse(String(L || "[]")));
    } catch {
      return [];
    }
  }
  function Ie(L) {
    const P = Te(L.dataset.actionCheckGroups);
    if (!P.length) return;
    const Q = new Map(P.map((Me) => [Me.marker, Me.events])), V = new Set(P.map((Me) => Me.marker)), ae = document.createTreeWalker(L, NodeFilter.SHOW_TEXT, { acceptNode(Me) {
      const Ke = Me;
      return Ke.data ? Ke.parentElement?.closest?.("a, button, code, kbd, pre, script, style, textarea, .assistant-runtime-event-stack") ? NodeFilter.FILTER_REJECT : [...Ke.data].some((qe) => V.has(qe)) ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP : NodeFilter.FILTER_SKIP;
    } }), Se = [];
    for (; ae.nextNode(); ) Se.push(ae.currentNode);
    Se.forEach((Me) => {
      const Ke = document.createDocumentFragment();
      let qe = !1;
      [...Me.data].forEach((Be) => {
        const Ee = Q.get(Be);
        if (Ee?.length) {
          Ke.append(we(Ee)), qe = !0;
          return;
        }
        Ke.append(document.createTextNode(Be));
      }), qe && Me.replaceWith(Ke);
    });
  }
  function Ne() {
    const L = e.chatScrollRef.value;
    L?.querySelectorAll && L.querySelectorAll(".xb-tavern-markdown").forEach((P) => {
      if (!r(P)) return;
      const Q = P.dataset.markdownSignature || "";
      P.dataset.markdownEnhanced !== Q && (_2(P, {
        codeBlockClassName: "xb-tavern-codeblock",
        codeCopyClassName: "xb-tavern-code-copy",
        flattenPreCode: !0,
        htmlBlockMode: e.htmlRenderEnabled.value ? "preview" : "code"
      }), X(P), Le(P), T(P), Ie(P), P.dataset.markdownEnhanced = Q);
    });
  }
  function Oe() {
    const L = e.managerScrollRef.value;
    L?.querySelectorAll && L.querySelectorAll(".xb-tavern-markdown").forEach((P) => {
      if (!r(P)) return;
      const Q = P.dataset.markdownSignature || "";
      P.dataset.markdownEnhanced !== Q && (_2(P, {
        codeBlockClassName: "xb-tavern-codeblock",
        codeCopyClassName: "xb-tavern-code-copy",
        flattenPreCode: !0,
        htmlBlockMode: e.htmlRenderEnabled.value ? void 0 : "code"
      }), P.dataset.markdownEnhanced = Q);
    });
  }
  return {
    clearMarkdownCache: l,
    disposeMarkdownTools() {
      W?.disconnect(), W = null, typeof window < "u" && window.removeEventListener(d_, Y);
    },
    enhanceChatMarkdown: Ne,
    enhanceManagerMarkdown: Oe,
    markdownSignature: s,
    renderChatMarkdown: a,
    stripTavernImageMarkers: i
  };
}
function T2(e) {
  const t = /* @__PURE__ */ ie(null), n = /* @__PURE__ */ ie(!0), r = /* @__PURE__ */ ie(!1), o = /* @__PURE__ */ ie(!1), s = /* @__PURE__ */ ie(!1), a = /* @__PURE__ */ ie(Sa(S(e.defaultLimit), 5));
  let i = null, l = !1, u = null, d = 0;
  function f() {
    const x = { uiMessageWindowLimit: a.value };
    E$(x, { defaultLimit: Sa(S(e.defaultLimit), 5) }), a.value = Number(x.uiMessageWindowLimit || 5);
  }
  function m(x = !1) {
    const M = t.value;
    if (!x && n.value !== !1 || !M || !x && M.scrollTop > 64) return !1;
    const N = { uiMessageWindowLimit: a.value };
    return T$(N, e.totalItems(), {
      defaultLimit: Sa(S(e.defaultLimit), 5),
      chunk: I0(S(e.loadBatchSize), 20)
    }) ? (a.value = Number(N.uiMessageWindowLimit || a.value), n.value = !1, !0) : !1;
  }
  function v() {
    const x = t.value;
    if (!x) {
      r.value = !1, o.value = !1;
      return;
    }
    const M = 80;
    r.value = x.scrollTop > M, o.value = x.scrollHeight - x.scrollTop - x.clientHeight > M;
  }
  function y() {
    s.value = !0, t.value?.classList.add("is-scrolling"), i && window.clearTimeout(i), i = window.setTimeout(() => {
      s.value = !1, t.value?.classList.remove("is-scrolling"), i = null;
    }, 1500);
  }
  function T(x = 56) {
    const M = t.value;
    return M ? M.scrollHeight - M.scrollTop - M.clientHeight <= x : !0;
  }
  function C(x = !1) {
    const M = Sa(S(e.defaultLimit), 5);
    return a.value <= M || !x && !T(8) ? !1 : (f(), !0);
  }
  gt(() => Sa(S(e.defaultLimit), 5), () => {
    f();
  });
  function A(x = !1, M = {}) {
    !x && !n.value || (x && (n.value = !0), (M.collapseWindow || n.value) && C(!0), Qt(() => {
      const N = t.value;
      if (!N) return;
      const q = () => {
        N.scrollTop = N.scrollHeight;
      };
      q(), requestAnimationFrame(() => {
        q(), requestAnimationFrame(() => {
          q(), v(), M.revealHelpers && y();
        });
      });
    }));
  }
  function I() {
    const x = t.value;
    x && (n.value = !1, d = 0, x.scrollTo?.({
      top: 0,
      behavior: "smooth"
    }), x.scrollTop = 0, v(), y());
  }
  function R() {
    const x = t.value;
    if (!x || m()) return;
    const M = d, N = Number(x.scrollTop || 0), q = N > M;
    d = N, T() ? (n.value !== !1 || q) && (n.value = !0, C()) : N < M && (n.value = !1), !l && (l = !0, requestAnimationFrame(() => {
      v(), y(), l = !1;
    }));
  }
  function $(x) {
    Number(x.deltaY || 0) < 0 && (n.value = !1);
  }
  function p(x) {
    u = Number(x.touches?.[0]?.clientY);
  }
  function b(x) {
    const M = Number(x.touches?.[0]?.clientY);
    if (!Number.isFinite(Number(u)) || !Number.isFinite(M)) {
      n.value = !1;
      return;
    }
    u !== null && (M > u + 4 || !T()) && (n.value = !1);
  }
  function w() {
    d = 0;
  }
  function E() {
    i && (window.clearTimeout(i), i = null), t.value?.classList.remove("is-scrolling");
  }
  return {
    scrollRef: t,
    autoScroll: n,
    showScrollTop: r,
    showScrollBottom: o,
    scrollControlsActive: s,
    messageWindowLimit: a,
    resetWindowState: f,
    revealOlderMessages: m,
    updateScrollButtons: v,
    isNearBottom: T,
    collapseMessageWindowIfBottom: C,
    scrollToBottom: A,
    scrollToTop: I,
    handleScroll: R,
    handleWheel: $,
    handleTouchStart: p,
    handleTouchMove: b,
    resetPositionState: w,
    cleanup: E
  };
}
async function _9(e, t) {
  const n = e.body?.getReader?.();
  if (!n) throw new Error("host_chat_completions_stream_missing_body");
  const r = new TextDecoder();
  let o = "";
  const s = /\r?\n\r?\n/, a = (l) => {
    const u = l.split(/\r?\n/).filter((d) => d.startsWith("data:")).map((d) => d.slice(5).trimStart()).join(`
`).trim();
    !u || u === "[DONE]" || t(JSON.parse(u));
  };
  for (; ; ) {
    const { done: l, value: u } = await n.read();
    if (l) break;
    for (o += r.decode(u, { stream: !0 }); ; ) {
      const d = o.match(s);
      if (!d || typeof d.index != "number") break;
      const f = o.slice(0, d.index);
      o = o.slice(d.index + d[0].length), a(f);
    }
  }
  const i = o.trim();
  i && a(i);
}
var xu = "openai", R0 = "claude", N0 = "makersuite", b9 = "/api/backends/chat-completions/status", w9 = "/api/backends/chat-completions/generate", mI = Object.freeze({
  [R0]: "https://api.anthropic.com/v1",
  [N0]: "https://generativelanguage.googleapis.com"
}), gI = null;
function S9(e) {
  return String(e || "").trim().replace(/\/+$/, "");
}
function T9(e, t) {
  const n = S9(e);
  return t === "claude" ? !n || /\/v\d[\w.-]*$/i.test(n) ? n : `${n}/v1` : t === "makersuite" ? n.replace(/\/v\d[\w.-]*$/i, "") : n;
}
function E2(e) {
  gI = typeof e == "function" ? e : null;
}
async function vI() {
  return {
    "Content-Type": "application/json",
    ...await Promise.resolve(gI?.() || {}),
    Accept: "application/json"
  };
}
function E9(e = {}) {
  const t = {};
  return Object.entries(e || {}).forEach(([n, r]) => {
    t[n] = /authorization|csrf|token|api[-_]?key/i.test(n) ? "[redacted]" : r;
  }), t;
}
async function nf(e = {}, t = !1) {
  const n = await vI(), r = {
    url: w9,
    method: "POST",
    headers: E9(n),
    body: {
      ...e,
      stream: !!t
    }
  };
  return Object.defineProperty(r, "rawHeaders", {
    value: n,
    enumerable: !1
  }), r;
}
function A9(e = "") {
  return /^\s*<!DOCTYPE\s+html/i.test(String(e || ""));
}
function C9(e = "") {
  return /invalid csrf token/i.test(String(e || ""));
}
function k9() {
  return "酒馆当前页面的 CSRF token 已失效，请按 F5 刷新并重新进入酒馆后再试。";
}
function hu(e = "", t = "") {
  return C9(e) || A9(e) ? k9() : String(e || t || "").trim();
}
function yI(e = {}, t = xu) {
  const n = T9(e.baseUrl, t), r = String(e.apiKey || "").trim(), o = mI[t] || "", s = n || (r ? o : ""), a = { chat_completion_source: t || "openai" };
  return s && (a.reverse_proxy = s), r && (a.proxy_password = r), a;
}
function I9(e = {}) {
  return Object.keys(e).forEach((t) => {
    (e[t] === void 0 || e[t] === "") && delete e[t];
  }), e;
}
function x9(e = {}, t = xu) {
  return yI(e, t);
}
function $0(e = {}, t = {}, n = [], r = !1, o = xu) {
  return I9({
    ...yI(e, o),
    stream: !!r,
    messages: n,
    model: e.model,
    max_tokens: t.maxTokens,
    temperature: t.reasoning?.enabled ? void 0 : t.temperature,
    tools: Array.isArray(t.tools) && t.tools.length ? t.tools : void 0,
    tool_choice: Array.isArray(t.tools) && t.tools.length ? t.toolChoice || "auto" : void 0,
    use_sysprompt: o === "openai" ? void 0 : !0,
    reasoning_effort: t.reasoning?.enabled ? t.reasoning.effort : void 0,
    include_reasoning: o === "openai" ? void 0 : t.reasoning?.enabled ? !0 : void 0
  });
}
function M9(e = {}, t = {}, n = [], r = !1) {
  return $0(e, t, n, r, xu);
}
function P9(e = {}, t = {}, n = [], r = !1) {
  return $0(e, t, n, r, R0);
}
function R9(e = {}, t = {}, n = [], r = !1) {
  return $0(e, t, n, r, N0);
}
async function N9(e = {}, t = xu, n = {}) {
  const r = await fetch(b9, {
    method: "POST",
    headers: await vI(),
    body: JSON.stringify(x9(e, t)),
    signal: n.signal
  }), o = await r.text();
  let s = null;
  try {
    s = o ? JSON.parse(o) : {};
  } catch (i) {
    throw new Error(`酒馆后端模型列表拉取失败：${hu(o, String(i?.message || i))}`);
  }
  if (!r.ok || s?.error) {
    const i = hu(s?.message || s?.error?.message || o, `HTTP ${r.status}`);
    throw new Error(`酒馆后端模型列表拉取失败：${i}`);
  }
  const a = Array.isArray(s?.data) ? s.data.map((i) => String(i?.id || i?.name || "").trim()).filter(Boolean) : [];
  return [...new Set(a)];
}
async function D0(e = {}, t = {}) {
  const n = await nf(e, !1);
  typeof t.onRequest == "function" && t.onRequest(n);
  const r = await fetch(n.url, {
    method: n.method,
    headers: n.rawHeaders || n.headers,
    body: JSON.stringify(n.body),
    signal: t.signal
  }), o = await r.text();
  let s = null;
  try {
    s = o ? JSON.parse(o) : {};
  } catch (a) {
    throw new Error(`酒馆后端生成失败：${hu(o, String(a?.message || a))}`);
  }
  if (!r.ok || s?.error) {
    const a = hu(s?.error?.message || s?.message || o, `HTTP ${r.status}`);
    throw new Error(`酒馆后端生成失败：${a}`);
  }
  return s;
}
async function L0(e = {}, t, n = {}) {
  const r = await nf(e, !0);
  typeof n.onRequest == "function" && n.onRequest(r);
  const o = await fetch(r.url, {
    method: r.method,
    headers: r.rawHeaders || r.headers,
    body: JSON.stringify(r.body),
    signal: n.signal
  });
  if (!o.ok) {
    const s = await o.text().catch(() => "");
    throw new Error(hu(s, `酒馆后端流式生成失败：HTTP ${o.status}`));
  }
  await _9(o, (s) => {
    if (s?.error) {
      const a = hu(s.error?.message || s.message || JSON.stringify(s.error), "酒馆后端流式生成失败");
      throw new Error(a);
    }
    t(s);
  });
}
var Zm = /* @__PURE__ */ ((e) => (e[e.before = 0] = "before", e[e.after = 1] = "after", e[e.ANTop = 2] = "ANTop", e[e.ANBottom = 3] = "ANBottom", e[e.atDepth = 4] = "atDepth", e[e.EMTop = 5] = "EMTop", e[e.EMBottom = 6] = "EMBottom", e[e.outlet = 7] = "outlet", e))(Zm || {}), Zh = /* @__PURE__ */ ((e) => (e[e.SYSTEM = 0] = "SYSTEM", e[e.USER = 1] = "USER", e[e.ASSISTANT = 2] = "ASSISTANT", e))(Zh || {}), jh = /* @__PURE__ */ ((e) => (e[e.AFTER_MAIN = 0] = "AFTER_MAIN", e[e.IN_CHAT = 1] = "IN_CHAT", e[e.BEFORE_MAIN = 2] = "BEFORE_MAIN", e))(jh || {}), f_ = {
  prompt: "",
  interval: 1,
  position: 1,
  depth: 4,
  role: 0,
  scan: !1
};
function $9(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function Xv(e, t = 0) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.max(0, Math.floor(n)) : t;
}
function D9(e) {
  const t = Number(e);
  return t === 0 ? 0 : t === 2 ? 2 : 1;
}
function L9(e) {
  const t = Number(e);
  return t === 1 ? 1 : t === 2 ? 2 : 0;
}
function Ta(e = {}) {
  const t = $9(e);
  return {
    prompt: Ge(t.prompt),
    interval: Xv(t.interval, f_.interval),
    position: D9(t.position),
    depth: Xv(t.depth, f_.depth),
    role: L9(t.role),
    scan: t.scan === !0,
    characterName: Ge(t.characterName),
    characterPrompt: Ge(t.characterPrompt),
    characterUse: t.characterUse === !0,
    characterPosition: Xv(t.characterPosition, 0)
  };
}
function O9(e = {}, t = "") {
  return (Array.isArray(e.history) ? e.history : []).filter((n) => ps(n.role ?? n.is_user) === "user").length + (Ge(t) ? 1 : 0);
}
function F9(e = {}, t = "") {
  const n = Ta(e.authorNote);
  let r = O9(e, t);
  n.interval === 1 && (r = 1);
  const o = r > 0 && n.interval > 0 && (r >= n.interval ? r % n.interval === 0 : !1);
  let s = o ? Ge(n.prompt) : "";
  if (o && n.characterUse === !0) {
    const a = Ge(n.characterPrompt);
    switch (n.characterPosition) {
      case 1:
        s = [a, s].filter(Boolean).join(`
`);
        break;
      case 2:
        s = [s, a].filter(Boolean).join(`
`);
        break;
      default:
        s = a;
        break;
    }
  }
  return {
    shouldAddPrompt: o,
    prompt: s,
    position: n.position,
    depth: n.depth,
    role: n.role,
    scan: n.scan === !0
  };
}
function B9(e = {}, t = "") {
  const n = F9(e, t);
  return n.scan && n.shouldAddPrompt ? Ge(n.prompt) : "";
}
var h_ = {
  0: "system",
  1: "user",
  2: "assistant"
}, A2 = {
  before: 0,
  before_char: 0,
  beforeCharacter: 0,
  after: 1,
  after_char: 1,
  afterCharacter: 1,
  atDepth: 4,
  depth: 4,
  outlet: 7,
  ANTop: 2,
  ANBottom: 3,
  EMTop: 5,
  EMBottom: 6
}, U9 = {
  0: "before character",
  1: "after character",
  2: "author note top",
  3: "author note bottom",
  4: "depth",
  5: "example top",
  6: "example bottom",
  7: "outlet"
}, q9 = [
  "top",
  "beforeCharacter",
  "afterCharacter",
  "beforeHistory",
  "afterHistory",
  "assistantPrefill"
];
function Ge(e = "") {
  return String(e || "").trim();
}
function In(e, t) {
  let n = e;
  for (const r of t) {
    if (!n || typeof n != "object") return "";
    n = n[r];
  }
  return Ge(n);
}
function ps(e, t = "system") {
  if (typeof e == "number" && h_[e]) return h_[e];
  const n = String(e || "").trim().toLowerCase();
  return n === "model" ? "assistant" : n === "sys" ? "system" : [
    "system",
    "user",
    "assistant",
    "tool"
  ].includes(n) ? n : t;
}
function el(e, t, n = {}) {
  const r = Ge(t);
  return r ? {
    role: ps(e),
    content: r,
    ...n
  } : null;
}
function p_(e) {
  return Array.isArray(e) ? e.map((t, n) => {
    const r = t && typeof t == "object" ? t : {}, o = Ge(r.text);
    return o ? {
      label: Ge(r.label) || `reasoning ${n + 1}`,
      text: o
    } : null;
  }).filter((t) => !!t) : [];
}
function H9(e = []) {
  const t = p_(e);
  return t.length ? [
    "<reasoning>",
    ...t.map((n) => [
      `<thought label="${Ge(n.label).replace(/"/g, "&quot;")}">`,
      n.text,
      "</thought>"
    ].join(`
`)),
    "</reasoning>"
  ].join(`
`) : "";
}
function z9(e) {
  const { thoughts: t, ...n } = e;
  if (e.role !== "assistant") return n;
  const r = H9(e.thoughts || []);
  return r ? {
    ...n,
    content: [e.content, r].filter(Boolean).join(`

`)
  } : n;
}
function O0(e) {
  return e.filter((t) => !!t && !!Ge(t.content));
}
function mn(e, t, n = "unknown", r = "", o = {}, s = "") {
  return {
    message: el(e, t, o),
    layer: n,
    label: r || n,
    sourceId: Ge(s)
  };
}
function W9(e = []) {
  const t = [], n = [];
  return e.forEach((r) => {
    if (!r.message || !Ge(r.message.content)) return;
    const o = t.length;
    t.push(r.message);
    const s = r.message.content.length;
    n.push({
      index: o,
      role: r.message.role,
      layer: r.layer,
      label: r.label,
      sourceId: r.sourceId || void 0,
      chars: s,
      tokenEstimate: Math.max(1, Math.ceil(s / 4))
    });
  }), {
    messages: t,
    messageLayers: n
  };
}
function Ea(e) {
  if (Array.isArray(e)) return e.map((n) => Ge(n)).filter(Boolean);
  const t = Ge(e);
  return t ? [t] : [];
}
function G9(e = "") {
  const t = [], n = String(e || "").split(`
`);
  let r = 0;
  for (; r < n.length && n[r].startsWith("@@"); ) {
    const o = n[r].trim();
    o && t.push(o.startsWith("@@@") ? o.slice(1) : o), r += 1;
  }
  return {
    decorators: t,
    content: n.slice(r).join(`
`).trim()
  };
}
function V9(e) {
  if (typeof e == "number" && Object.values(Zm).includes(e)) return e;
  const t = String(e || "").trim();
  return Object.prototype.hasOwnProperty.call(A2, t) ? A2[t] : 1;
}
function F0(e = {}, t = 0) {
  const n = e, r = e.extensions || {}, o = (d, f) => Object.prototype.hasOwnProperty.call(r, d) ? r[d] : n[f], s = G9(e.content || ""), a = e.uid ?? e.id ?? e.comment ?? e.name ?? t + 1, i = Ge(e.sourceWorldBook || e.worldName || e.world), l = s.content || Ge(e.content), u = n.order ?? n.insertion_order;
  return {
    ...e,
    uid: a,
    activationKey: K9(i, a, t),
    content: l,
    decorators: [...Ea(e.decorators), ...s.decorators],
    key: Ea(e.key),
    keysecondary: [...Ea(e.keysecondary), ...Ea(e.secondary_keys)],
    order: Number(u) || 0,
    disable: n.disable === !0 || n.disabled === !0 || n.enabled === !1,
    enabled: n.enabled === !1 ? !1 : n.disable !== !0 && n.disabled !== !0,
    vectorized: o("vectorized", "vectorized") === !0,
    depth: Number.isFinite(Number(o("depth", "depth"))) ? Number(o("depth", "depth")) : 4,
    role: ps(o("role", "role"), "system"),
    position: V9(o("position", "position")),
    selectiveLogic: o("selectiveLogic", "selectiveLogic"),
    scanDepth: Number.isFinite(Number(o("scan_depth", "scanDepth"))) ? Number(o("scan_depth", "scanDepth")) : null,
    caseSensitive: o("case_sensitive", "caseSensitive"),
    matchWholeWords: o("match_whole_words", "matchWholeWords"),
    characterFilter: o("character_filter", "characterFilter") || n.character_filter,
    ignoreBudget: o("ignore_budget", "ignoreBudget") === !0,
    excludeRecursion: o("exclude_recursion", "excludeRecursion") === !0,
    preventRecursion: o("prevent_recursion", "preventRecursion") === !0,
    delayUntilRecursion: o("delay_until_recursion", "delayUntilRecursion"),
    group: Ge(o("group", "group")),
    groupOverride: o("group_override", "groupOverride") === !0,
    groupWeight: Number.isFinite(Number(o("group_weight", "groupWeight"))) ? Number(o("group_weight", "groupWeight")) : void 0,
    useGroupScoring: o("use_group_scoring", "useGroupScoring"),
    probability: o("probability", "probability"),
    useProbability: o("useProbability", "useProbability"),
    sticky: o("sticky", "sticky"),
    cooldown: o("cooldown", "cooldown"),
    delay: o("delay", "delay"),
    triggers: Ea(o("triggers", "triggers")),
    outletName: Ge(o("outlet_name", "outletName")),
    matchPersonaDescription: o("match_persona_description", "matchPersonaDescription"),
    matchCharacterDescription: o("match_character_description", "matchCharacterDescription"),
    matchCharacterPersonality: o("match_character_personality", "matchCharacterPersonality"),
    matchCharacterDepthPrompt: o("match_character_depth_prompt", "matchCharacterDepthPrompt"),
    matchScenario: o("match_scenario", "matchScenario"),
    matchCreatorNotes: o("match_creator_notes", "matchCreatorNotes"),
    activationReason: "",
    sourceWorldBook: i,
    worldSourceType: Ge(e.worldSourceType),
    worldSourceIndex: Number.isFinite(Number(e.worldSourceIndex)) ? Number(e.worldSourceIndex) : t,
    contentChars: l.length
  };
}
function K9(e, t, n = 0) {
  return `${Ge(e) || "direct"}\0${Ge(t) || `index:${n}`}`;
}
function B0(e) {
  return U9[e] || "after character";
}
function Md(e, t = 2) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.max(0, Math.floor(n)) : t;
}
function _I(e = [], t = 2) {
  const n = Md(t, 2);
  return n <= 0 ? "" : e.slice(-n).map((r) => String(r || "")).filter(Boolean).join(`
`);
}
function J9(e = {}, t) {
  const n = t?.scanDepth === null || t?.scanDepth === void 0 ? Md(e.scanDepth ?? 2, 2) : Md(t.scanDepth, 2), r = [
    Array.isArray(e.scanMessages) ? _I(e.scanMessages, n) : String(e.scanText || ""),
    Ge(e.injectScanText),
    String(e.recursionText || "")
  ], o = e.globalScanData || {};
  return t?.matchPersonaDescription && o.personaDescription && r.push(o.personaDescription), t?.matchCharacterDescription && o.characterDescription && r.push(o.characterDescription), t?.matchCharacterPersonality && o.characterPersonality && r.push(o.characterPersonality), t?.matchCharacterDepthPrompt && o.characterDepthPrompt && r.push(o.characterDepthPrompt), t?.matchScenario && o.scenario && r.push(o.scenario), t?.matchCreatorNotes && o.creatorNotes && r.push(o.creatorNotes), r.filter(Boolean).join(`
`);
}
function jm(e = {}, t) {
  const n = !!(t?.caseSensitive ?? t?.case_sensitive ?? e.caseSensitive), r = !!(t?.matchWholeWords ?? t?.match_whole_words ?? e.matchWholeWords), o = J9(e, t), s = n ? o : o.toLowerCase();
  return (a = "") => {
    const i = String(a || "").trim();
    if (!i) return !1;
    const l = Y9(i);
    if (l) return l.test(o);
    const u = n ? i : i.toLowerCase();
    if (!r) return s.includes(u);
    const d = u.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    return new RegExp(`(^|[^\\p{L}\\p{N}_])${d}($|[^\\p{L}\\p{N}_])`, n ? "u" : "iu").test(s);
  };
}
function Y9(e = "") {
  const t = String(e || "").match(/^\/([\w\W]+?)\/([gimsuy]*)$/);
  if (!t) return null;
  let n = t[1] || "";
  const r = t[2] || "";
  if (/(^|[^\\])\//.test(n)) return null;
  n = n.replace("\\/", "/");
  try {
    return new RegExp(n, r);
  } catch {
    return null;
  }
}
function bI(e, t) {
  if (e.selective === !1 || !e.keysecondary.length) return !0;
  const n = e.keysecondary.map((s) => t(s)), r = n.some(Boolean), o = n.every(Boolean);
  switch (Number(e.selectiveLogic ?? e.selective_logic ?? 0)) {
    case 1:
      return !o;
    case 2:
      return !r;
    case 3:
      return o;
    default:
      return r;
  }
}
function U0(e, t) {
  return e.entryStates?.[t.activationKey] || e.entryStates?.[String(t.uid)] || {};
}
function q0(e, t) {
  const n = Number(t.turn) || 0, r = U0(t, e);
  return Number(r.stickyUntilTurn) >= n;
}
function X9(e, t) {
  if (e.useProbability === !1 || e.useProbabilityGlobal === !1 || q0(e, t)) return !0;
  const n = Number(e.probability);
  if (!Number.isFinite(n) || n <= 0) return n !== 0;
  const r = n > 1 ? n / 100 : n;
  return r >= 1 ? !0 : (t.random || Math.random)() <= r;
}
function wI(e) {
  return Math.max(0, Number(e.recursionPass) || 0);
}
function SI(e, t) {
  const n = e.delayUntilRecursion ?? e.delay_until_recursion;
  return n ? t <= 0 ? !1 : n === !0 ? !0 : t >= Math.max(1, Number(n) || 1) : !0;
}
function TI(e, t) {
  const n = e.characterFilter;
  if (!n || typeof n != "object") return !0;
  const r = Array.isArray(n.names) ? n.names.map((d) => Ge(d)).filter(Boolean) : [], o = Array.isArray(n.tags) ? n.tags.map((d) => Ge(d)).filter(Boolean) : [];
  if (!r.length && !o.length) return !0;
  const s = new Set((t.characterFilterData?.names || []).map((d) => Ge(d)).filter(Boolean)), a = new Set((t.characterFilterData?.tags || []).map((d) => Ge(d)).filter(Boolean)), i = r.some((d) => s.has(d)), l = o.some((d) => a.has(d)), u = i || l;
  return n.isExclude ? !u : u;
}
function Q9(e, t) {
  if (e.disable === !0 || e.disabled === !0 || e.triggers?.length && !e.triggers.includes(Ge(t.trigger) || "normal") || !TI(e, t)) return "";
  const n = Number(t.turn) || 0, r = U0(t, e), o = q0(e, t), s = wI(t);
  if (Number(r.delayUntilTurn) > n || Number(e.delay) > 0 && n < Number(e.delay) || Number(r.cooldownUntilTurn) > n && !o || s > 0 && e.excludeRecursion && !o || !SI(e, s) && !o) return "";
  if (e.decorators.includes("@@activate")) return "decorator";
  if (e.decorators.includes("@@dont_activate")) return "";
  if (e.constant === !0) return "constant";
  if (o) return "sticky";
  const a = jm(t, e);
  return !e.key.some((i) => a(i)) || !bI(e, a) ? "" : "keyword";
}
function Z9(e, t) {
  if (e.disable === !0 || e.disabled === !0) return {
    status: "disabled",
    activationReason: ""
  };
  if (e.triggers?.length && !e.triggers.includes(Ge(t.trigger) || "normal")) return {
    status: "trigger_filtered",
    activationReason: ""
  };
  if (!TI(e, t)) return {
    status: "character_filtered",
    activationReason: ""
  };
  const n = Number(t.turn) || 0, r = U0(t, e), o = q0(e, t), s = wI(t);
  if (Number(r.delayUntilTurn) > n || Number(e.delay) > 0 && n < Number(e.delay)) return {
    status: "delay",
    activationReason: ""
  };
  if (Number(r.cooldownUntilTurn) > n && !o) return {
    status: "cooldown",
    activationReason: ""
  };
  if (s > 0 && e.excludeRecursion && !o) return {
    status: "excluded_from_recursion",
    activationReason: ""
  };
  if (!SI(e, s) && !o) return {
    status: "delay_until_recursion",
    activationReason: ""
  };
  if (e.decorators.includes("@@activate")) return {
    status: "activated",
    activationReason: "decorator"
  };
  if (e.decorators.includes("@@dont_activate")) return {
    status: "suppressed_by_decorator",
    activationReason: ""
  };
  if (e.constant === !0) return {
    status: "activated",
    activationReason: "constant"
  };
  if (o) return {
    status: "activated",
    activationReason: "sticky"
  };
  const a = jm(t, e);
  return e.key.some((i) => a(i)) ? bI(e, a) ? {
    status: "activated",
    activationReason: "keyword"
  } : {
    status: "secondary_not_matched",
    activationReason: ""
  } : {
    status: "not_matched",
    activationReason: ""
  };
}
function C2(e, t = {}) {
  const n = Ge(e.worldSourceType);
  if (n === "chat") return 0;
  if (n === "persona") return 1;
  const r = Number(t.insertionStrategy ?? 1);
  return n === "character" ? r === 2 ? 3 : 2 : n === "global" ? r === 1 ? 3 : 2 : 4;
}
function k2(e, t = {}) {
  const n = Ge(e.worldSourceType);
  if (Number(t.insertionStrategy ?? 1) === 0) {
    if (n === "global") return 0;
    if (n === "character") return 1;
  }
  return 0;
}
function $l(e, t, n = {}) {
  return C2(e, n) - C2(t, n) || t.order - e.order || k2(e, n) - k2(t, n) || Number(e.worldSourceIndex ?? 0) - Number(t.worldSourceIndex ?? 0) || e.activationKey.localeCompare(t.activationKey, "en");
}
function j9(e) {
  const t = Number(e.budgetChars);
  return Number.isFinite(t) && t > 0 ? t : 0;
}
function ep(e = [], t = {}) {
  const n = j9(t), r = n > 0, o = /* @__PURE__ */ new Set(), s = /* @__PURE__ */ new Map(), a = [];
  let i = 0, l = 0, u = !1;
  return e.forEach((d) => {
    const f = d.content.length;
    if (!f) return;
    const m = r ? Math.max(0, n - i) : Number.POSITIVE_INFINITY;
    if (s.set(d.activationKey, {
      budgetUsedBefore: i,
      budgetRemainingBefore: r ? m : void 0,
      budgetShortfall: r && !d.ignoreBudget && (u || i + f >= n) ? Math.max(1, i + f - n) : void 0
    }), d.ignoreBudget) {
      a.push(d), o.add(d.activationKey);
      return;
    }
    if (u || r && i + f >= n) {
      u = r, l += f;
      return;
    }
    a.push(d), o.add(d.activationKey), i += f;
  }), {
    includedKeys: o,
    byKey: s,
    enabled: r,
    limit: n,
    used: i,
    remaining: r ? Math.max(0, n - i) : 0,
    activatedChars: i,
    skippedChars: l
  };
}
function I2(e) {
  return Ge(e.group).split(/,\s*/).map((t) => t.trim()).filter(Boolean);
}
function eD(e, t = {}) {
  const n = jm(t, e), r = e.key.filter((s) => n(s)).length;
  if (!e.key.length || !r) return 0;
  const o = e.keysecondary.filter((s) => n(s)).length;
  if (e.keysecondary.length > 0) {
    const s = Number(e.selectiveLogic ?? e.selective_logic ?? 0);
    if (s === 0 || s === 3 && o === e.keysecondary.length) return r + o;
  }
  return r;
}
function tD(e = [], t, n = {}) {
  const r = new Set(e), o = /* @__PURE__ */ new Set();
  t.forEach((a) => {
    I2(a).forEach((i) => o.add(i));
  });
  const s = /* @__PURE__ */ new Map();
  return e.forEach((a) => {
    I2(a).forEach((i) => {
      const l = s.get(i) || [];
      l.push(a), s.set(i, l);
    });
  }), s.forEach((a, i) => {
    const l = a.filter((f) => r.has(f));
    if (!l.length) return;
    if (o.has(i)) {
      l.forEach((f) => r.delete(f));
      return;
    }
    if (n.useGroupScoring === !0 || l.some((f) => f.useGroupScoring === !0)) {
      const f = l.map((v) => eD(v, n)), m = Math.max(...f);
      l.forEach((v, y) => {
        (v.useGroupScoring ?? n.useGroupScoring) && f[y] < m && r.delete(v);
      });
    }
    const u = l.filter((f) => r.has(f));
    if (u.length <= 1) return;
    const d = u.filter((f) => f.groupOverride).sort((f, m) => $l(f, m, n))[0] || nD(u, n);
    u.forEach((f) => {
      f !== d && r.delete(f);
    });
  }), e.filter((a) => r.has(a));
}
function nD(e = [], t = {}) {
  if (!e.length) return null;
  const n = e.map((s) => ({
    entry: s,
    weight: Math.max(1, Number(s.groupWeight) || 100)
  })), r = n.reduce((s, a) => s + a.weight, 0);
  let o = (t.random || Math.random)() * r;
  for (const s of n)
    if (o -= s.weight, o <= 0) return s.entry;
  return n[n.length - 1]?.entry || null;
}
function rD(e = [], t = {}, n = {}) {
  const r = {
    ...t,
    ...n,
    scanText: n.scanText ?? t.scanText ?? ""
  }, o = (Array.isArray(e) ? e : []).map((x, M) => F0(x, M)).sort((x, M) => $l(x, M, r)), s = Math.max(0, Number(r.recursionLimit) || 0), a = !!r.recursion, i = a ? s > 0 ? Math.max(0, s - 1) : Math.max(1, o.length) : 0, l = /* @__PURE__ */ new Map(), u = /* @__PURE__ */ new Map(), d = /* @__PURE__ */ new Set(), f = String(r.scanText || ""), m = Md(r.scanDepth ?? 2, 2), v = Math.max(0, Number(r.minActivations) || 0), y = Math.max(0, Number(r.minActivationsDepthMax) || 0), T = Array.isArray(r.scanMessages) ? r.scanMessages.length : 0, C = Math.max(m, y > 0 ? y : T);
  let A = "", I = 0, R = 0, $ = "initial", p = 0;
  const b = Math.max(1, o.length + i + C + 4);
  for (; p < b; ) {
    p += 1;
    const x = m + R, M = $ === "minActivations" ? "" : A, N = [f, M].filter(Boolean).join(`
`), q = {
      ...r,
      scanDepth: x,
      scanText: N,
      recursionText: M,
      recursionPass: $ === "recursion" ? I : 0
    };
    let z = !1;
    const J = [];
    o.forEach((X) => {
      const de = X.activationKey;
      if (u.has(de) || d.has(de)) return;
      const fe = Q9(X, q);
      fe && J.push({
        ...X,
        activationReason: fe
      });
    }), tD(J, u, r).forEach((X) => {
      if (!X9(X, q)) {
        d.add(X.activationKey);
        return;
      }
      u.set(X.activationKey, X);
      const de = ep([...u.values()].sort((fe, ve) => $l(fe, ve, r)), r);
      (!de.enabled || de.includedKeys.has(X.activationKey)) && (l.set(X.activationKey, X), X.preventRecursion || (A = [A, X.content].filter(Boolean).join(`
`)), z = !0);
    });
    const G = ep([...u.values()].sort((X, de) => $l(X, de, r)), r), oe = G.enabled && G.skippedChars > 0;
    if (a && z && !oe && I < i) {
      I += 1, $ = "recursion";
      continue;
    }
    const B = v > 0 && l.size < v;
    if (!oe && B && T > 0 && x < C && x < T) {
      R += 1, $ = "minActivations";
      continue;
    }
    break;
  }
  const w = [...u.values()].sort((x, M) => $l(x, M, r)), E = ep(w, r);
  return {
    activatedBeforeBudget: w,
    activatedEntries: [...l.values()].sort((x, M) => $l(x, M, r)).filter((x) => !E.enabled || E.includedKeys.has(x.activationKey)),
    budgetDebug: E
  };
}
function oD(e = [], t = [], n = {}, r = ep(t, n)) {
  const o = new Map(t.map((a) => [a.activationKey, a])), s = r.includedKeys;
  return (Array.isArray(e) ? e : []).map((a, i) => {
    const l = F0(a, i), u = o.get(l.activationKey), d = r.byKey.get(l.activationKey) || {}, f = u ? {
      status: "activated",
      activationReason: u.activationReason
    } : Z9(l, n), m = jm(n, l), v = u ? r.enabled && !s.has(l.activationKey) ? "budget_skipped" : "activated" : f.status === "activated" ? "probability_failed" : f.status;
    return {
      uid: l.uid,
      activationKey: l.activationKey,
      title: Ge(l.comment || l.title || l.name || l.uid),
      sourceWorldBook: l.sourceWorldBook,
      worldSourceType: l.worldSourceType,
      worldSourceIndex: l.worldSourceIndex,
      content: l.content,
      contentChars: l.contentChars,
      key: l.key,
      keysecondary: l.keysecondary,
      matchedKeys: l.key.filter((y) => m(y)),
      matchedSecondaryKeys: l.keysecondary.filter((y) => m(y)),
      decorators: l.decorators,
      position: l.position,
      positionLabel: B0(l.position),
      role: l.role,
      order: l.order,
      depth: l.depth,
      status: v,
      activationReason: u?.activationReason || f.activationReason,
      insertionTarget: eg(l),
      ...d
    };
  });
}
function sD(e = []) {
  return (Array.isArray(e) ? e : []).map((t, n) => ({
    ...F0(t, n),
    activationReason: Ge(t.activationReason) || "native"
  })).filter((t) => !!t.content);
}
function aD(e = []) {
  return e.map((t) => ({
    uid: t.uid,
    activationKey: t.activationKey,
    title: Ge(t.comment || t.title || t.name || t.uid),
    sourceWorldBook: t.sourceWorldBook,
    worldSourceType: t.worldSourceType,
    worldSourceIndex: t.worldSourceIndex,
    content: t.content,
    contentChars: t.contentChars,
    key: t.key,
    keysecondary: t.keysecondary,
    matchedKeys: [],
    matchedSecondaryKeys: [],
    decorators: t.decorators,
    position: t.position,
    positionLabel: B0(t.position),
    role: t.role,
    order: t.order,
    depth: t.depth,
    status: "activated",
    activationReason: t.activationReason || "native",
    insertionTarget: eg(t)
  }));
}
function iD(e = {}) {
  const t = [];
  let n = 0;
  const r = (s, a, i = {}) => {
    const l = Ge(s);
    if (!l) return;
    const u = `native:${a}:${n}`;
    t.push({
      uid: u,
      activationKey: u,
      content: l,
      contentChars: l.length,
      key: [],
      keysecondary: [],
      decorators: [],
      position: a,
      role: i.role || "system",
      order: n,
      depth: Number.isFinite(Number(i.depth)) ? Number(i.depth) : 4,
      activationReason: "native",
      sourceWorldBook: "",
      title: "",
      comment: "",
      ...i
    }), n += 1;
  };
  r(e.worldInfoBefore, 0), r(e.worldInfoAfter, 1), (Array.isArray(e.worldInfoExamples) ? e.worldInfoExamples : []).forEach((s) => {
    const a = Ge(s?.position) === "after" ? 6 : 5;
    r(s?.content, a);
  }), (Array.isArray(e.anBefore) ? e.anBefore : []).forEach((s) => {
    r(s, 2);
  }), (Array.isArray(e.anAfter) ? e.anAfter : []).forEach((s) => {
    r(s, 3);
  }), (Array.isArray(e.worldInfoDepth) ? e.worldInfoDepth : []).forEach((s) => {
    const a = Number.isFinite(Number(s?.depth)) ? Number(s.depth) : 4, i = ps(h_[Number(s?.role)] || "system", "system");
    (Array.isArray(s?.entries) ? s.entries : []).forEach((l) => {
      r(l, 4, {
        depth: a,
        role: i
      });
    });
  });
  const o = e.outlets && typeof e.outlets == "object" ? e.outlets : {};
  return Object.entries(o).forEach(([s, a]) => {
    (Array.isArray(a) ? a : []).forEach((i) => {
      r(i, 7, { outletName: s });
    });
  }), t;
}
function eg(e) {
  switch (e.position) {
    case 0:
      return "before character card";
    case 1:
      return "after character card";
    case 4:
      return `history depth ${Math.max(0, Number(e.depth) || 0)}`;
    case 2:
      return "author note top";
    case 3:
      return "author note bottom";
    case 5:
      return "example messages top";
    case 6:
      return "example messages bottom";
    case 7:
      return `outlet:${Ge(e.outletName || e.outlet || "default")}`;
    default:
      return B0(e.position);
  }
}
function lD(e = []) {
  const t = {
    before: [],
    after: [],
    atDepth: [],
    outlet: {},
    examplesTop: [],
    examplesBottom: [],
    authorNoteTop: [],
    authorNoteBottom: []
  };
  return e.forEach((n) => {
    if (n.content)
      switch (n.position) {
        case 0:
          t.before.push(n);
          break;
        case 4:
          t.atDepth.push(n);
          break;
        case 7: {
          const r = Ge(n.outletName || n.outlet || "default");
          t.outlet[r] = t.outlet[r] || [], t.outlet[r].push(n);
          break;
        }
        case 5:
          t.examplesTop.push(n);
          break;
        case 6:
          t.examplesBottom.push(n);
          break;
        case 2:
          t.authorNoteTop.push(n);
          break;
        case 3:
          t.authorNoteBottom.push(n);
          break;
        default:
          t.after.push(n);
          break;
      }
  }), t;
}
function uD(e = []) {
  const t = {};
  return e.forEach((n) => {
    const r = eg(n);
    t[r] = (t[r] || 0) + 1;
  }), t;
}
function cD(e = [], t = {}) {
  const n = Number(t.turn) || 0, r = {};
  return e.forEach((o) => {
    const s = o.activationKey, a = {}, i = o.sticky === !0 ? 1 : Number(o.sticky), l = Number(o.cooldown), u = Number(o.delay);
    Number.isFinite(i) && i > 0 && (a.stickyUntilTurn = n + i), Number.isFinite(l) && l > 0 && (a.cooldownUntilTurn = n + l), Number.isFinite(u) && u > 0 && (a.delayUntilTurn = n + u), Object.keys(a).length && (r[s] = a);
  }), r;
}
function EI(e = []) {
  return [...e].sort((t, n) => t.order - n.order || Number(n.worldSourceIndex ?? 0) - Number(t.worldSourceIndex ?? 0) || n.activationKey.localeCompare(t.activationKey, "en"));
}
function nr(e, t = []) {
  const n = EI(t).map((r) => r.content).filter(Boolean).join(`

`);
  return n ? `<${e}>
${n}
</${e}>` : "";
}
function AI(e = {}, t = {}) {
  const n = e.data || {}, r = [
    ["Character", e.name || In(n, ["name"])],
    ["User", t.name],
    ["Description", e.description || In(n, ["description"])],
    ["Personality", e.personality || In(n, ["personality"])],
    ["Scenario", e.scenario || In(n, ["scenario"])],
    ["Creator Notes", e.creatorNotes || e.creator_notes || In(n, ["creator_notes"])],
    ["First Message", e.firstMessage || e.first_mes || In(n, ["first_mes"])],
    ["Message Examples", e.mesExample || e.mes_example || In(n, ["mes_example"])],
    ["User Persona", t.persona || t.description]
  ].map(([o, s]) => {
    const a = Ge(s);
    return a ? `## ${o}
${a}` : "";
  }).filter(Boolean);
  return r.length ? `<character_card>
${r.join(`

`)}
</character_card>` : "";
}
function ic(e, t) {
  const n = Ge(t);
  return n ? `## ${e}
${n}` : "";
}
function dD(e = {}) {
  const t = Array.isArray(e.memoryFiles) ? e.memoryFiles : [], n = Array.isArray(e.structuredStates) ? e.structuredStates : [], r = Ge(e.spatialState), o = Array.isArray(e.questHooks) ? e.questHooks.map((u) => Ge(u)).filter(Boolean) : [], s = [];
  o.length && s.push(o.join(`
`));
  const a = Ge(t.find((u) => u.path === "memory/state.md")?.content || "");
  a && s.push(`## 会话记忆
${a}`);
  const i = t.filter((u) => String(u.path || "").startsWith("memory/characters/")).map((u) => {
    const d = Ge(u.content);
    return d ? `### ${Ge(u.title || String(u.path || "").replace(/^memory\/characters\//, "").replace(/\.md$/i, "")) || "相关人物"}
${d}` : "";
  }).filter(Boolean);
  i.length && s.push(`## 相关人物记忆
${i.join(`

`)}`);
  const l = r ? [] : n.map((u) => Ge(u.digest)).filter(Boolean);
  return l.length && s.push(`## 状态摘要
${l.join(`

`)}`), r && s.push(`## 空间状态
${r}`), s.join(`

`);
}
function fD(e = {}) {
  const t = dD(e);
  return t ? [{
    uid: "session-memory",
    activationKey: "memory:session",
    content: t,
    contentChars: t.length,
    key: [],
    keysecondary: [],
    decorators: [],
    position: 4,
    role: "system",
    order: -1e6,
    depth: 1,
    activationReason: "memory",
    sourceWorldBook: "",
    title: "session memory",
    comment: "session memory"
  }] : [];
}
function hD(e = []) {
  return e.map((t, n) => {
    const r = Ge(t.content);
    if (!r) return null;
    const o = Ge(t.label) || `runtime depth ${n + 1}`;
    return {
      uid: `runtime-depth:${n}`,
      activationKey: `runtime-depth:${n}:${o}`,
      content: r,
      contentChars: r.length,
      key: [],
      keysecondary: [],
      decorators: [],
      position: 4,
      role: ps(t.role, "system"),
      order: Number.isFinite(Number(t.order)) ? Number(t.order) : 1e9 + n,
      depth: Number.isFinite(Number(t.depth)) ? Math.max(0, Number(t.depth)) : 1,
      activationReason: Ge(t.layer) || "runtime",
      sourceWorldBook: "",
      title: o,
      comment: o
    };
  }).filter((t) => !!t);
}
function pD(e = {}) {
  return (Array.isArray(e.sections) ? e.sections : []).map((t) => ({
    id: Ge(t.id),
    label: Ge(t.label),
    enabled: t.enabled !== !1,
    marker: t.marker === !0,
    role: ps(t.role, "system"),
    content: Ge(t.content),
    placement: q9.includes(t.placement) ? t.placement : "beforeHistory",
    source: Ge(t.source)
  })).filter((t) => t.enabled && (t.content || t.marker));
}
function yl(e = [], t = "") {
  return e.filter((n) => n.placement === t);
}
function _l(e = [], t, n = "preset") {
  return e.map((r, o) => ({
    message: el(r.role, r.content),
    layer: n,
    label: r.label || `preset ${t} ${o + 1}`,
    sourceId: r.id || void 0
  }));
}
function mD(e = {}) {
  return Ge(e.id).replace(/^prompt-manager:/, "");
}
function gD(e) {
  const { presetSections: t, character: n, user: r, worldBuckets: o, conversationUnits: s, runtimeProtocolUnits: a } = e, i = n.data || {}, l = /* @__PURE__ */ new Set(), u = [], d = (f, m) => {
    l.add(f), u.push(...m);
  };
  return t.forEach((f, m) => {
    const v = mD(f);
    if (f.marker) switch (v) {
      case "worldInfoBefore":
        d(v, [mn("system", nr("world_info_before_character", o.before), "world-before", f.label || "World Info (before)", {}, f.id)]);
        return;
      case "worldInfoAfter":
        d(v, [mn("system", nr("world_info_after_character", o.after), "world-after", f.label || "World Info (after)", {}, f.id)]);
        return;
      case "charDescription":
        d(v, [mn(f.role, ic("Description", n.description || In(i, ["description"])), "character-card", f.label || "Char Description", {}, f.id)]);
        return;
      case "charPersonality":
        d(v, [mn(f.role, ic("Personality", n.personality || In(i, ["personality"])), "character-card", f.label || "Char Personality", {}, f.id)]);
        return;
      case "scenario":
        d(v, [mn(f.role, ic("Scenario", n.scenario || In(i, ["scenario"])), "character-card", f.label || "Scenario", {}, f.id)]);
        return;
      case "personaDescription":
        d(v, [mn(f.role, ic("User Persona", r.persona || r.description), "character-card", f.label || "Persona Description", {}, f.id)]);
        return;
      case "dialogueExamples":
        d(v, [mn(f.role, ic("Message Examples", n.mesExample || n.mes_example || In(i, ["mes_example"])), "character-card", f.label || "Chat Examples", {}, f.id)]);
        return;
      case "chatHistory":
        d(v, [
          mn("system", nr("world_info_examples_top", o.examplesTop), "world-examples", "world info examples top"),
          mn("system", nr("world_info_author_note_top", o.authorNoteTop), "world-author-note", "world info author note top"),
          ...s,
          ...a
        ]);
        return;
      default:
        return;
    }
    u.push({
      message: el(f.role, f.content),
      layer: (f.source === "promptManager", "preset"),
      label: f.label || `preset ordered ${m + 1}`,
      sourceId: f.id || void 0
    });
  }), l.has("worldInfoBefore") || u.push(mn("system", nr("world_info_before_character", o.before), "world-before", "world info before character")), !l.has("charDescription") && !l.has("charPersonality") && !l.has("scenario") && !l.has("personaDescription") && !l.has("dialogueExamples") && u.push(mn("system", AI(n, r), "character-card", "character card")), l.has("worldInfoAfter") || u.push(mn("system", nr("world_info_after_character", o.after), "world-after", "world info after character")), l.has("chatHistory") || u.push(mn("system", nr("world_info_examples_top", o.examplesTop), "world-examples", "world info examples top"), mn("system", nr("world_info_author_note_top", o.authorNoteTop), "world-author-note", "world info author note top"), ...s, ...a), u.push(mn("system", nr("world_info_author_note_bottom", o.authorNoteBottom), "world-author-note", "world info author note bottom"), mn("system", nr("world_info_examples_bottom", o.examplesBottom), "world-examples", "world info examples bottom")), u;
}
function vD(e, t, n) {
  const r = n.get(e);
  return Number.isFinite(Number(r)) ? {
    message: e,
    layer: "world-depth",
    label: `world info depth ${Math.max(0, Number(r))}`
  } : {
    message: e,
    layer: e.role === "user" ? "current-user/history" : "history",
    label: `history ${t + 1}`
  };
}
function CI(e = {}) {
  const t = e.is_user === !0 ? "user" : ps(e.role, "assistant");
  return t === "tool" ? null : el(t, e.content || e.mes || e.message, {
    ...e.name ? { name: String(e.name) } : {},
    ...p_(e.thoughts).length ? { thoughts: p_(e.thoughts) } : {}
  });
}
function yD(e = [], t = {}) {
  const n = (Array.isArray(e) ? e : []).map((o) => CI(o)).filter((o) => !!o);
  if (!n.length) return [];
  const r = t.separator || `

`;
  return [el(ps(t.role, "system"), `<conversation_history>
${n.map((o) => `<message role="${o.role === "user" ? "user" : "assistant"}">
${o.content}
</message>`).join(r)}
</conversation_history>`)].filter((o) => !!o);
}
function _D(e = [], t = {}) {
  return t.mode === "raw" ? (Array.isArray(e) ? e : []).map((n) => CI(n)).filter((n) => !!n) : yD(e, t);
}
function bD(e = []) {
  const t = /* @__PURE__ */ new Map();
  return EI(e).forEach((n) => {
    const r = Math.max(0, Number(n.depth) || 0), o = ps(n.role, "system"), s = `${r}\0${o}`, a = t.get(s) || {
      depth: r,
      role: o,
      entries: []
    };
    a.entries.push(n.content), t.set(s, a);
  }), [...t.values()].map((n) => ({
    depth: n.depth,
    message: el(n.role, n.entries.join(`

`))
  })).filter((n) => !!n.message);
}
function wD(e = [], t = []) {
  if (!t.length) return e;
  const n = Array.from({ length: e.length + 1 }, () => []);
  t.forEach((o) => {
    const s = Math.max(0, Number(o.depth) || 0);
    n[(e.length ? Math.max(-1, e.length - 1 - s) : -1) + 1].push(o.message);
  });
  const r = [...n[0]];
  return e.forEach((o, s) => {
    r.push(o, ...n[s + 1]);
  }), r;
}
function SD(e = {}) {
  const t = e.character || {}, n = e.user || {}, r = t.data || {};
  return {
    personaDescription: Ge(n.persona || n.description),
    characterDescription: Ge(t.description || In(r, ["description"])),
    characterPersonality: Ge(t.personality || In(r, ["personality"])),
    characterDepthPrompt: Ge(In(t, ["characterDepthPrompt"]) || In(t, ["character_depth_prompt"]) || In(r, [
      "extensions",
      "depth_prompt",
      "prompt"
    ]) || In(r, ["character_depth_prompt"]) || In(r, ["depth_prompt"])),
    scenario: Ge(t.scenario || In(r, ["scenario"])),
    creatorNotes: Ge(t.creatorNotes || t.creator_notes || In(r, ["creator_notes"]))
  };
}
function TD(e = {}) {
  const t = e.character || {}, n = Ge(t.avatar), r = n.split(/[\\/]/).pop() || n, o = Array.isArray(t.tags) ? t.tags : [];
  return {
    names: [
      t.name,
      t.id,
      n,
      r
    ].map((s) => Ge(s)).filter(Boolean),
    tags: o.map((s) => Ge(s)).filter(Boolean)
  };
}
function ED(e = {}, t = {}) {
  const n = String(e.content || e.mes || e.message || "");
  if (!n) return "";
  const r = (e.is_user === !0 ? "user" : ps(e.role, "assistant")) === "user" ? t.user?.name : t.character?.name, o = Ge(e.name || r);
  return o ? `${o}: ${n}` : n;
}
function AD(e = {}, t = "", n = !1) {
  const r = e.history || [], o = n && t ? `${Ge(e.user?.name) || "User"}: ${t}` : t;
  return [...r.map((s) => n ? ED(s, e) : s.content || s.mes || s.message || ""), o].map((s) => String(s || "")).filter(Boolean);
}
function CD(e = {}) {
  const t = !(Array.isArray(e.worldBooks) && e.worldBooks.length > 0) && Array.isArray(e.worldEntries) ? e.worldEntries.map((r) => ({
    ...r,
    sourceWorldBook: r.sourceWorldBook || r.worldName || r.world || ""
  })) : [], n = (Array.isArray(e.worldBooks) ? e.worldBooks : []).flatMap((r) => {
    const o = Ge(r.worldSourceType), s = Number(r.worldSourceIndex);
    return Array.isArray(r.entries) ? r.entries.map((a) => ({
      ...a,
      sourceWorldBook: a.sourceWorldBook || a.worldName || a.world || r.name,
      worldSourceType: Ge(a.worldSourceType) || o,
      worldSourceIndex: Number.isFinite(Number(a.worldSourceIndex)) ? Number(a.worldSourceIndex) : Number.isFinite(s) ? s : void 0
    })) : [];
  });
  return ID([...t, ...n]);
}
function kD(e = {}) {
  const t = Ge(e.sourceWorldBook || e.worldName || e.world) || "direct", n = Ge(e.uid ?? e.id);
  if (n) return `${t}\0uid\0${n}`;
  const r = Ge(e.comment || e.title || e.name), o = Ge(e.content);
  return `${t}\0body\0${r}\0${Ea(e.key).join("")}\0${[...Ea(e.keysecondary), ...Ea(e.secondary_keys)].join("")}\0${o}`;
}
function ID(e = []) {
  const t = /* @__PURE__ */ new Set(), n = [];
  return e.forEach((r) => {
    const o = kD(r);
    t.has(o) || (t.add(o), n.push(r));
  }), n;
}
function kI(e = {}, t = {}, n = {}) {
  const r = e.character || {}, o = e.user || {}, s = e.history || [], a = n.currentUserMessage || "", i = n.historyMode || "raw", l = n.memoryContext || {}, u = Array.isArray(n.runtimeDepthEntries) ? n.runtimeDepthEntries : [], d = Array.isArray(n.runtimeProtocolMessages) ? n.runtimeProtocolMessages : [], f = pD(t), m = n.worldSettings || {}, v = typeof n.worldScanText == "string", y = AD(e, a, m.includeNames === !0), T = v ? String(n.worldScanText || "") : _I(y, Md(m.scanDepth ?? 2, 2)), C = [Ge(m.injectScanText), B9(e, a)].filter(Boolean).join(`
`), A = sD(e.nativeWorldInfo?.activatedEntries), I = iD(e.nativeWorldInfo), R = {
    ...m,
    scanText: T,
    injectScanText: C,
    scanMessages: v ? void 0 : y,
    globalScanData: m.globalScanData || SD(e),
    characterFilterData: m.characterFilterData || TD(e),
    turn: n.turn ?? n.worldSettings?.turn,
    entryStates: n.entryStates ?? n.worldSettings?.entryStates
  }, $ = CD(e), p = e.nativeWorldInfo ? [] : $, b = rD(p, R), w = b.activatedBeforeBudget, E = b.budgetDebug, x = oD(p, w, R, E), M = b.activatedEntries;
  if (A.length || I.length || e.nativeWorldInfo) {
    const N = I.length ? I : A;
    return {
      character: r,
      user: o,
      history: s,
      currentUserMessage: a,
      historyMode: i,
      squashRole: n.squashRole,
      memoryContext: l,
      runtimeDepthEntries: u,
      runtimeProtocolMessages: d,
      presetSections: f,
      scanText: T,
      worldSettings: R,
      worldEntryCandidates: [...aD(A), ...x],
      activatedWorldEntries: [...N, ...M],
      localStateWorldEntries: M,
      budgetDebug: E
    };
  }
  return {
    character: r,
    user: o,
    history: s,
    currentUserMessage: a,
    historyMode: i,
    squashRole: n.squashRole,
    memoryContext: l,
    runtimeDepthEntries: u,
    runtimeProtocolMessages: d,
    presetSections: f,
    scanText: T,
    worldSettings: R,
    worldEntryCandidates: x,
    activatedWorldEntries: M,
    localStateWorldEntries: M,
    budgetDebug: E
  };
}
function II(e, t = {}) {
  const n = _D(e.history, {
    mode: e.historyMode,
    role: e.squashRole || "system",
    userName: e.user.name,
    characterName: e.character.name,
    separator: t.historySeparator
  }), r = el("user", e.currentUserMessage);
  return O0([...n, r]);
}
function xI(e = {}, t, n) {
  const { character: r, user: o, history: s, currentUserMessage: a, historyMode: i, squashRole: l, memoryContext: u, runtimeDepthEntries: d, runtimeProtocolMessages: f, presetSections: m, scanText: v, worldSettings: y, worldEntryCandidates: T, activatedWorldEntries: C, budgetDebug: A } = t, I = lD(C), R = bD([
    ...fD(u),
    ...I.atDepth,
    ...hD(d)
  ]), $ = /* @__PURE__ */ new WeakMap();
  R.forEach((B) => $.set(B.message, B.depth));
  const p = wD((t.promptConversationMessages || II(t, e)).map(z9), R), b = _l(yl(m, "top"), "top"), w = _l(yl(m, "beforeCharacter"), "before character"), E = _l(yl(m, "afterCharacter"), "after character"), x = _l(yl(m, "beforeHistory"), "before history"), M = _l(yl(m, "afterHistory"), "after history"), N = _l(yl(m, "assistantPrefill"), "assistant prefill", "assistant-prefill"), q = m.some((B) => B.source === "promptManager"), z = p.map((B, X) => vD(B, X, $));
  for (let B = z.length - 1; B >= 0; B -= 1) if (z[B]?.message?.role === "user") {
    z[B].label = "current user message";
    break;
  }
  const J = f.map((B, X) => ({
    message: B,
    layer: "runtime-protocol",
    label: `runtime protocol ${X + 1}`
  })), G = W9((q ? gD({
    presetSections: m,
    character: r,
    user: o,
    worldBuckets: I,
    conversationUnits: z,
    runtimeProtocolUnits: J
  }) : null) || [
    ...b,
    mn("system", nr("world_info_before_character", I.before), "world-before", "world info before character"),
    ...w,
    mn("system", AI(r, o), "character-card", "character card"),
    mn("system", nr("world_info_after_character", I.after), "world-after", "world info after character"),
    ...E,
    mn("system", nr("world_info_examples_top", I.examplesTop), "world-examples", "world info examples top"),
    mn("system", nr("world_info_author_note_top", I.authorNoteTop), "world-author-note", "world info author note top"),
    ...x,
    ...z,
    ...J,
    ...M,
    mn("system", nr("world_info_author_note_bottom", I.authorNoteBottom), "world-author-note", "world info author note bottom"),
    mn("system", nr("world_info_examples_bottom", I.examplesBottom), "world-examples", "world info examples bottom"),
    ...N
  ]), oe = G.messages;
  return {
    messages: oe,
    messageLayers: G.messageLayers,
    activatedWorldEntries: C,
    worldEntryCandidates: T,
    outlets: Object.fromEntries(Object.entries(I.outlet).map(([B, X]) => [B, X.map((de) => de.content).join(`

`)])),
    meta: {
      scanText: v,
      scanTextChars: v.length,
      historyMode: i,
      squashedHistory: i !== "raw",
      rawMessagesJson: JSON.stringify(oe, null, 2),
      ...n ? { regexApplications: n } : {},
      ...u.structuredStates?.length ? { structuredStates: u.structuredStates } : {},
      ...u.spatialState ? { spatialState: u.spatialState } : {},
      worldBudget: {
        enabled: A.enabled,
        limit: A.limit,
        used: A.used,
        remaining: A.remaining,
        activatedChars: A.activatedChars,
        skippedChars: A.skippedChars
      },
      worldPositionCounts: uD(C),
      worldEntryStateUpdates: cD(t.localStateWorldEntries, y)
    }
  };
}
function xD(e, t = []) {
  const n = O0(t.map((r) => ({
    ...r,
    content: Ge(r.content)
  })));
  return {
    ...e,
    messages: n,
    messageLayers: e.messageLayers.map((r, o) => {
      const s = n[o];
      if (!s) return r;
      const a = s.content.length;
      return {
        ...r,
        role: s.role,
        chars: a,
        tokenEstimate: Math.max(1, Math.ceil(a / 4))
      };
    }),
    meta: {
      ...e.meta,
      rawMessagesJson: JSON.stringify(n, null, 2)
    }
  };
}
function MD(e = {}, t = {}, n = {}) {
  return xI(t, kI(e, t, n));
}
async function PD(e = {}, t = {}, n = {}, r = {}) {
  const o = kI(e, t, n);
  if (r.transformWorldEntries) {
    o.activatedWorldEntries = (await r.transformWorldEntries(o.activatedWorldEntries)).map((i) => ({
      ...i,
      content: Ge(i.content),
      contentChars: Ge(i.content).length
    })).filter((i) => !!i.content);
    const a = new Map(o.activatedWorldEntries.map((i) => [i.activationKey, i]));
    o.localStateWorldEntries = o.localStateWorldEntries.map((i) => a.get(i.activationKey) || i).filter((i) => !!i.content);
  }
  r.transformConversationMessages && (o.promptConversationMessages = O0((await r.transformConversationMessages(II(o, t))).map((a) => ({
    ...a,
    content: Ge(a.content)
  }))));
  const s = xI(t, o, r.regexApplications);
  return r.transformFinalMessages ? xD(s, await r.transformFinalMessages(s.messages)) : s;
}
function H0(e = {}, t = {}, n, r = void 0) {
  const o = e.character || {}, s = e.user || {}, a = Array.isArray(e.worldBooks) ? e.worldBooks : [], i = new Map(n.worldEntryCandidates.map((l) => [l.activationKey, l]));
  return {
    chatPresetId: Ge(t.id),
    chatPresetName: Ge(t.name),
    presetId: Ge(t.id),
    presetName: Ge(t.name),
    characterId: Ge(o.id),
    characterName: Ge(o.name),
    userName: Ge(s.name),
    historyCount: Array.isArray(e.history) ? e.history.length : 0,
    worldBooks: a.map((l) => ({
      name: Ge(l.name),
      entries: Array.isArray(l.entries) ? l.entries.length : 0,
      ...l.error ? { error: Ge(l.error) } : {}
    })),
    messageCount: n.messages.length,
    messageChars: n.messages.reduce((l, u) => l + String(u.content || "").length, 0),
    messageLayers: n.messageLayers,
    rawMessagesJson: n.meta.rawMessagesJson,
    ...n.meta.regexApplications ? { regexApplications: n.meta.regexApplications } : {},
    activatedWorldEntries: n.activatedWorldEntries.map((l) => {
      const u = i.get(l.activationKey);
      return {
        uid: l.uid,
        sourceWorldBook: l.sourceWorldBook,
        title: u?.title || Ge(l.comment || l.title || l.name || l.uid),
        activationReason: l.activationReason,
        insertionTarget: u?.insertionTarget || eg(l),
        contentChars: l.contentChars
      };
    }),
    ...e.nativeWorldInfo ? { nativeWorldInfo: {
      trigger: Ge(e.nativeWorldInfo.trigger),
      sourceNames: Array.isArray(e.nativeWorldInfo.sourceNames) ? e.nativeWorldInfo.sourceNames.map((l) => ({
        name: Ge(l.name),
        ...l.sourceType ? { sourceType: l.sourceType } : {},
        ...Number.isFinite(Number(l.sourceIndex)) ? { sourceIndex: Number(l.sourceIndex) } : {}
      })) : []
    } } : {},
    ...n.meta.structuredStates?.length ? { structuredStates: n.meta.structuredStates.map((l) => ({
      docType: Ge(l.docType),
      docId: Ge(l.docId),
      revision: Number(l.revision) || 0,
      digestChars: Ge(l.digest).length
    })) } : {},
    ...n.meta.spatialState ? { spatialStateChars: Ge(n.meta.spatialState).length } : {},
    worldBudget: n.meta.worldBudget,
    worldPositionCounts: n.meta.worldPositionCounts,
    scanTextChars: n.meta.scanTextChars,
    ...r === void 0 ? {} : { diagnostics: r }
  };
}
var RD = Object.freeze({
  recursion: !0,
  recursionLimit: 4,
  budgetChars: 24e3
});
function ND(e = {}) {
  const t = e.worldSettings || {};
  return {
    ...RD,
    ...t,
    turn: Math.max(0, Number(e.turn) || 0),
    entryStates: e.entryStates || {}
  };
}
function MI(e) {
  return {
    currentUserMessage: String(e.currentUserMessage || ""),
    historyMode: e.historyMode || "raw",
    worldSettings: ND({
      worldSettings: e.context?.worldSettings,
      turn: e.turn,
      entryStates: e.entryStates
    }),
    memoryContext: e.memoryContext,
    runtimeDepthEntries: e.runtimeDepthEntries,
    runtimeProtocolMessages: e.runtimeProtocolMessages
  };
}
function PI(e) {
  const t = e.context || {}, n = e.chatPreset || e.preset || {}, r = MI(e), o = MD(t, n, r);
  return {
    runtimeState: r,
    buildResult: o,
    buildSnapshot: H0(t, n, o, e.diagnostics),
    rawMessagesJson: o.meta.rawMessagesJson
  };
}
async function z0(e) {
  const t = e.context || {}, n = e.chatPreset || e.preset || {}, r = MI(e), o = await PD(t, n, r, {
    transformWorldEntries: e.transformWorldEntries,
    transformConversationMessages: e.transformConversationMessages,
    transformFinalMessages: e.transformFinalMessages,
    regexApplications: e.regexApplications
  });
  return {
    runtimeState: r,
    buildResult: o,
    buildSnapshot: H0(t, n, o, e.diagnostics),
    rawMessagesJson: o.meta.rawMessagesJson
  };
}
var x2 = 24, $D = 24, DD = 240, LD = /* @__PURE__ */ new Map([
  ["“", '"'],
  ["”", '"'],
  ["＂", '"'],
  ["「", '"'],
  ["」", '"'],
  ["『", '"'],
  ["』", '"'],
  ["‘", "'"],
  ["’", "'"],
  ["，", ","],
  ["。", "."],
  ["：", ":"],
  ["；", ";"],
  ["？", "?"],
  ["！", "!"],
  ["（", "("],
  ["）", ")"]
]);
function Pd(e = "") {
  return Array.from(String(e ?? ""), (t) => LD.get(t) || t).join("");
}
function m_(e = "") {
  let t = "";
  const n = [];
  let r = 0;
  for (const o of String(e ?? "")) {
    const s = r;
    r += o.length, !/\s/u.test(o) && (t += Pd(o), n.push({
      start: s,
      end: r
    }));
  }
  return {
    compact: t,
    positions: n
  };
}
function W0(e = "", t = "") {
  if (!t) return [];
  const n = [];
  let r = 0;
  for (; r <= e.length; ) {
    const o = e.indexOf(t, r);
    if (o < 0) break;
    n.push({
      start: o,
      end: o + t.length,
      equivalent: !1
    }), r = o + Math.max(t.length, 1);
  }
  return n;
}
function OD(e = "", t = "") {
  const n = Pd(e), r = Pd(t);
  return !r || r === t && n === e ? [] : W0(n, r).map((o) => ({
    ...o,
    equivalent: !0
  }));
}
function FD(e = "", t = "") {
  const n = m_(e), r = m_(t);
  return r.compact.length < $D ? [] : !r.compact || r.compact === t && n.compact === e ? [] : W0(n.compact, r.compact).map((o) => {
    const s = n.positions[o.start], a = n.positions[o.end - 1];
    return !s || !a ? null : {
      start: s.start,
      end: a.end,
      equivalent: !0,
      flexibleWhitespace: !0
    };
  }).filter(Boolean);
}
function BD(e = "", t = 0) {
  let n = 1;
  const r = Math.max(0, Math.min(t, e.length));
  for (let o = 0; o < r; o += 1) e[o] === `
` && (n += 1);
  return n;
}
function UD(e = "", t = {}) {
  const n = Math.max(0, Number(t.start) - x2), r = Math.min(e.length, Number(t.end) + x2), o = n > 0 ? "..." : "", s = r < e.length ? "..." : "";
  return `${o}${e.slice(n, r).replace(/\s+/g, " ")}${s}`;
}
function eu(e = [], t = 1, n = 8) {
  const r = e.slice(0, n), o = e.length > n ? `
...` : "";
  return `${r.map((s, a) => `${t + a}: ${s}`).join(`
`)}${o}`;
}
function th(e = "", t = DD) {
  const n = String(e ?? "").replace(/\r\n/g, `
`);
  return n.length > t ? `${n.slice(0, t)}...` : n;
}
function qD(e = "", t = []) {
  return t.slice(0, 8).map((n) => ({
    line: BD(e, n.start),
    context: UD(e, n)
  }));
}
function nh(e = "", t = /"/g, n = "“", r = "”") {
  let o = !0;
  return e.replace(t, () => {
    const s = o ? n : r;
    return o = !o, s;
  });
}
function HD(e = "", t = "") {
  let n = String(e ?? "");
  return /[“”]/.test(t) ? n = nh(n, /"/g, "“", "”") : /[「」]/.test(t) ? n = nh(n, /"/g, "「", "」") : /[『』]/.test(t) && (n = nh(n, /"/g, "『", "』")), /[‘’]/.test(t) && (n = nh(n, /'/g, "‘", "’")), t.includes("，") && (n = n.replaceAll(",", "，")), t.includes("。") && (n = n.replaceAll(".", "。")), t.includes("：") && (n = n.replaceAll(":", "：")), t.includes("；") && (n = n.replaceAll(";", "；")), t.includes("？") && (n = n.replaceAll("?", "？")), t.includes("！") && (n = n.replaceAll("!", "！")), t.includes("（") && (n = n.replaceAll("(", "（")), t.includes("）") && (n = n.replaceAll(")", "）")), n;
}
function Op(e = "", t = "") {
  const n = W0(e, t);
  if (n.length) return n;
  const r = OD(e, t);
  return r.length ? r : FD(e, t);
}
function zD(e = "") {
  return m_(e).compact.length;
}
function g_(e = "", t = 8) {
  return zD(e) >= t;
}
function Qv(e = "", t = "") {
  return t ? Op(e, t).length > 0 : !1;
}
function WD(e = {}, t = []) {
  const n = typeof e.oldString == "string" ? e.oldString : String(e.oldString ?? ""), r = typeof e.newString == "string" ? e.newString : String(e.newString ?? "");
  return t.some((o = {}) => {
    const s = String(o.matchedText ?? ""), a = String(o.replacement ?? "");
    return s === n && a === r ? !0 : !g_(n, 8) || r !== "" && !g_(r, 8) || !Qv(s, n) || Qv(a, n) ? !1 : r === "" || Qv(a, r);
  });
}
function GD(e = "", t = "", n = "") {
  if (!n || !g_(n, 16)) return {};
  const r = t ? Op(e, t).length : 0, o = Op(e, n).length;
  return r !== 0 || o <= 0 ? {} : {
    uncertain: !0,
    possibleAlreadyApplied: !0,
    newStringMatches: o
  };
}
function VD(e = "", t = [], n = "") {
  let r = e;
  const o = [], s = [];
  return t.slice().sort((a, i) => i.start - a.start).forEach((a) => {
    const i = e.slice(a.start, a.end), l = a.equivalent ? HD(n, i) : n;
    o.push(l), s.push({
      matchedText: i,
      replacement: l
    }), r = `${r.slice(0, a.start)}${l}${r.slice(a.end)}`;
  }), {
    content: r,
    replacements: o,
    replacementDetails: s,
    matchedBy: t.some((a) => a.flexibleWhitespace) ? "flexible_whitespace" : t.some((a) => a.equivalent) ? "punctuation_equivalent" : "exact"
  };
}
function KD(e = "", t = "", n = {}) {
  return {
    ok: !1,
    error: e,
    message: t,
    ...n
  };
}
function cn(e = "", t = "", n = "", r = {}) {
  return KD(e, t, n ? {
    suggestion: n,
    ...r
  } : r);
}
var JD = /* @__PURE__ */ new Set([
  "oldString",
  "newString",
  "startLine",
  "endLine",
  "insertAtLine",
  "replaceAll"
]);
function rh(e = [], t = "") {
  for (let n = e.length - 1; n >= 0; n -= 1) if (e[n]?.[1] === t) return e[n];
  return null;
}
function YD(e = [], t = 0) {
  const n = rh(e, "startLine"), r = rh(e, "endLine"), o = rh(e, "insertAtLine"), s = rh(e, "oldString"), a = [];
  return n && r ? a.push(n, r) : o ? a.push(o) : s && a.push(s), a.filter((i) => i.index > t).sort((i, l) => i.index - l.index)[0] || null;
}
function XD(e = "") {
  let t = String(e ?? "").trim();
  return t.endsWith(",") && (t = t.slice(0, -1).trimEnd()), t.startsWith('"') && (t = t.slice(1)), t.endsWith('"') && (t = t.slice(0, -1)), t.replace(/\\r/g, "\r").replace(/\\n/g, `
`).replace(/\\t/g, "	").replace(/\\"/g, '"').replace(/\\\\/g, "\\");
}
function QD(e = "") {
  const t = String(e || "").trim().replace(/^\{/, "").replace(/\}$/, ""), n = [...t.matchAll(/"?([A-Za-z][A-Za-z0-9_]*)"?\s*:/g)].filter((a) => JD.has(a[1]));
  if (!n.length) return null;
  const r = n.filter((a) => a[1] !== "newString"), o = r[r.length - 1] || null, s = {};
  for (let a = 0; a < n.length; a += 1) {
    const i = n[a], l = i[1], u = i.index + i[0].length, d = Bp(s) || Up(s) || qp(s), f = l === "newString" ? !d && o && o.index > i.index ? YD(n, i.index) : null : n[a + 1];
    let m = f ? f.index : t.length;
    if (f) {
      const y = t.lastIndexOf(",", f.index);
      y >= u && (m = y);
    }
    const v = t.slice(u, m).trim();
    if (l === "startLine" || l === "endLine" || l === "insertAtLine") {
      const y = v.match(/-?\d+/);
      y && (s[l] = Number(y[0]));
      continue;
    }
    if (l === "replaceAll") {
      s[l] = /^true\b/i.test(v);
      continue;
    }
    if (s[l] = XD(v), l === "newString" && !f) break;
  }
  return (Bp(s) || Up(s) || qp(s)) && Object.hasOwn(s, "newString") ? s : null;
}
function ZD(e) {
  return e && typeof e == "object" && !Array.isArray(e) && Object.hasOwn(e, "newString") && (Bp(e) || Up(e) || qp(e));
}
function M2(e = "") {
  return /"?newString"?\s*:/i.test(e) && /"?(?:oldString|startLine|endLine|insertAtLine)"?\s*:/i.test(e);
}
function jD(e = "") {
  const t = String(e || "").trim();
  if (!t.startsWith("[") || !t.endsWith("]")) return [];
  const n = t.slice(1, -1).trim();
  if (!n) return [];
  const r = [...n.matchAll(/\}\s*,\s*\{(?=\s*"?(?:oldString|newString|startLine|endLine|insertAtLine|replaceAll)"?\s*:)/g)].map((a) => ({
    index: a.index,
    splitEnd: a.index + 1,
    nextStart: a.index + a[0].lastIndexOf("{")
  })), o = [];
  let s = 0;
  return r.forEach((a, i) => {
    const l = r[i + 1], u = n.slice(s, a.splitEnd), d = n.slice(a.nextStart, l?.splitEnd ?? n.length);
    !M2(u) || !M2(d) || (o.push(u), s = a.nextStart);
  }), o.push(n.slice(s)), o.map((a) => a.trim()).filter(Boolean);
}
function eL(e = "") {
  const t = jD(e);
  if (!t.length) return null;
  const n = t.map((r) => {
    try {
      const o = JSON.parse(r);
      return ZD(o) ? o : null;
    } catch {
      return QD(r);
    }
  });
  return n.every((r) => r && typeof r == "object") ? n : null;
}
function Fp(e = {}, t = "") {
  if (!Object.hasOwn(e, t)) return !1;
  const n = e[t];
  return n != null && n !== "";
}
function Bp(e = {}) {
  return Fp(e, "oldString");
}
function Up(e = {}) {
  return Fp(e, "startLine") || Fp(e, "endLine");
}
function qp(e = {}) {
  return Fp(e, "insertAtLine");
}
function Ks(e) {
  const t = Number(e);
  return Number.isInteger(t) && t > 0 ? t : 0;
}
function RI(e = {}) {
  const t = Ks(e.startLine), n = Ks(e.endLine);
  return t && n ? "line_range" : Ks(e.insertAtLine) ? "line_insert" : Bp(e) ? "old_string" : Up(e) ? "line_range" : qp(e) ? "line_insert" : "old_string";
}
function oh(e = {}, t = "") {
  const n = typeof e.newString == "string" ? e.newString : String(e.newString ?? "");
  return t === "line_range" ? {
    startLine: e.startLine,
    endLine: e.endLine,
    newString: n
  } : t === "line_insert" ? {
    insertAtLine: e.insertAtLine,
    newString: n
  } : {
    oldString: typeof e.oldString == "string" ? e.oldString : String(e.oldString ?? ""),
    newString: n,
    replaceAll: !!e.replaceAll
  };
}
function tL(e = {}, t = "") {
  return t === "line_range" ? {
    mode: t,
    startLine: e.startLine,
    endLine: e.endLine,
    newPreview: th(e.newString)
  } : t === "line_insert" ? {
    mode: t,
    insertAtLine: e.insertAtLine,
    newPreview: th(e.newString)
  } : {
    mode: t,
    oldPreview: th(e.oldString),
    newPreview: th(e.newString),
    replaceAll: !!e.replaceAll || void 0
  };
}
function Rd(e = [], t = [], n = []) {
  return e.map((r, o) => {
    if (!r || typeof r != "object") return r;
    const s = n[o] || RI(t[o] || {});
    if (r.ok) return Object.hasOwn(r, "index") ? r : {
      ...r,
      index: o
    };
    const a = Object.hasOwn(r, "index") ? r.index : o;
    return {
      ...tL(t[o] || {}, s),
      ...r,
      index: a
    };
  });
}
function tg(e = "") {
  return String(e ?? "").replace(/\r\n/g, `
`).split(`
`);
}
function Hp(e = "") {
  if (e === "") return [];
  const t = String(e ?? "").replace(/\r\n/g, `
`).split(`
`);
  t.length > 1 && t[t.length - 1] === "" && t.pop();
  const n = t.filter((r) => r.trim());
  return n.length > 0 && n.every((r) => /^\s*\d+:\s?/.test(r)) ? t.map((r) => r.trim() ? r.replace(/^\s*\d+:\s?/, "") : r) : t;
}
function nL(e = "", t = []) {
  const n = tg(e), r = new Array(t.length), o = t.map((u = {}, d) => {
    const f = Ks(u.startLine), m = Ks(u.endLine), v = typeof u.newString == "string" ? u.newString : String(u.newString ?? "");
    return {
      edit: u,
      index: d,
      startLine: f,
      endLine: m,
      newString: v,
      replacementLines: Hp(v)
    };
  });
  o.forEach((u) => {
    if (!u.startLine || !u.endLine) {
      r[u.index] = cn("invalid_line_range", "startLine and endLine must be positive integers", "Use line numbers from the latest Read result, with startLine <= endLine.");
      return;
    }
    if (u.endLine < u.startLine) {
      r[u.index] = cn("invalid_line_range", "endLine must be greater than or equal to startLine", "Use an inclusive line range from the latest Read result.");
      return;
    }
    u.endLine > n.length && (r[u.index] = cn("line_range_out_of_bounds", `Line range ${u.startLine}-${u.endLine} is outside the file`, `Read the current file again. This file has ${n.length} lines.`));
  });
  const s = o.slice().sort((u, d) => u.startLine - d.startLine || u.endLine - d.endLine);
  s.forEach((u, d) => {
    if (r[u.index]) return;
    const f = s[d - 1];
    f && !r[f.index] && u.startLine <= f.endLine && (r[f.index] = cn("overlapping_line_ranges", `Line range ${f.startLine}-${f.endLine} overlaps ${u.startLine}-${u.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."), r[u.index] = cn("overlapping_line_ranges", `Line range ${u.startLine}-${u.endLine} overlaps ${f.startLine}-${f.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."));
  });
  const a = n.slice();
  o.slice().sort((u, d) => d.startLine - u.startLine || d.endLine - u.endLine).forEach((u) => {
    if (r[u.index]) return;
    const d = n.slice(u.startLine - 1, u.endLine);
    a.splice(u.startLine - 1, u.endLine - u.startLine + 1, ...u.replacementLines), r[u.index] = {
      ok: !0,
      index: u.index,
      startLine: u.startLine,
      endLine: u.endLine,
      replacements: 1,
      matchedBy: "line_range",
      oldPreview: eu(d, u.startLine),
      newPreview: eu(u.replacementLines, u.startLine)
    };
  });
  const i = r.filter((u) => u && !u.ok).length, l = r.filter((u) => u && u.ok).length;
  return {
    ok: i === 0,
    partial: l > 0 && i > 0 ? !0 : void 0,
    content: a.join(`
`),
    results: Rd(r, t, t.map(() => "line_range"))
  };
}
function NI(e = "") {
  return e === "" ? 0 : tg(e).length;
}
function rL(e = "", t = []) {
  const n = e === "" ? [] : tg(e), r = NI(e), o = new Array(t.length), s = t.map((u = {}, d) => {
    const f = Ks(u.insertAtLine), m = typeof u.newString == "string" ? u.newString : String(u.newString ?? "");
    return {
      edit: u,
      index: d,
      insertAtLine: f,
      newString: m,
      insertionLines: Hp(m)
    };
  });
  s.forEach((u) => {
    if (!u.insertAtLine) {
      o[u.index] = cn("invalid_insert_line", "insertAtLine must be a positive integer", "Use a line number from the latest Read result. insertAtLine inserts before that line; totalLines + 1 appends to the end.");
      return;
    }
    if (u.insertAtLine > r + 1) {
      o[u.index] = cn("insert_line_out_of_bounds", `Insert line ${u.insertAtLine} is outside the file`, `Read the current file again. This file has ${r} lines, so valid insertAtLine values are 1-${r + 1}.`);
      return;
    }
    u.newString || (o[u.index] = cn("no_changes", "No changes to make", "Provide non-empty newString text to insert."));
  });
  const a = n.slice();
  s.slice().sort((u, d) => d.insertAtLine - u.insertAtLine || d.index - u.index).forEach((u) => {
    o[u.index] || (a.splice(u.insertAtLine - 1, 0, ...u.insertionLines), o[u.index] = {
      ok: !0,
      index: u.index,
      insertAtLine: u.insertAtLine,
      replacements: 0,
      matchedBy: "line_insert",
      newPreview: eu(u.insertionLines, u.insertAtLine)
    });
  });
  const i = o.filter((u) => u && !u.ok).length, l = o.filter((u) => u && u.ok).length;
  return {
    ok: i === 0,
    partial: l > 0 && i > 0 ? !0 : void 0,
    content: a.join(`
`),
    results: Rd(o, t, t.map(() => "line_insert"))
  };
}
function oL(e = "", t = [], n = []) {
  const r = n.some((m) => m === "line_range"), o = e === "" && !r ? [] : tg(e), s = NI(e), a = new Array(t.length), i = t.map((m = {}, v) => {
    const y = n[v];
    if (y === "line_insert") {
      const I = Ks(m.insertAtLine), R = typeof m.newString == "string" ? m.newString : String(m.newString ?? "");
      return {
        mode: y,
        edit: m,
        index: v,
        insertAtLine: I,
        newString: R,
        insertionLines: Hp(R)
      };
    }
    const T = Ks(m.startLine), C = Ks(m.endLine), A = typeof m.newString == "string" ? m.newString : String(m.newString ?? "");
    return {
      mode: y,
      edit: m,
      index: v,
      startLine: T,
      endLine: C,
      newString: A,
      replacementLines: Hp(A)
    };
  });
  i.forEach((m) => {
    if (m.mode === "line_insert") {
      if (!m.insertAtLine) {
        a[m.index] = cn("invalid_insert_line", "insertAtLine must be a positive integer", "Use a line number from the latest Read result. insertAtLine inserts before that line; totalLines + 1 appends to the end.");
        return;
      }
      if (m.insertAtLine > s + 1) {
        a[m.index] = cn("insert_line_out_of_bounds", `Insert line ${m.insertAtLine} is outside the file`, `Read the current file again. This file has ${s} lines, so valid insertAtLine values are 1-${s + 1}.`);
        return;
      }
      m.newString || (a[m.index] = cn("no_changes", "No changes to make", "Provide non-empty newString text to insert."));
      return;
    }
    if (!m.startLine || !m.endLine) {
      a[m.index] = cn("invalid_line_range", "startLine and endLine must be positive integers", "Use line numbers from the latest Read result, with startLine <= endLine.");
      return;
    }
    if (m.endLine < m.startLine) {
      a[m.index] = cn("invalid_line_range", "endLine must be greater than or equal to startLine", "Use an inclusive line range from the latest Read result.");
      return;
    }
    m.endLine > o.length && (a[m.index] = cn("line_range_out_of_bounds", `Line range ${m.startLine}-${m.endLine} is outside the file`, `Read the current file again. This file has ${o.length} lines.`));
  });
  const l = i.filter((m) => m.mode === "line_range").slice().sort((m, v) => m.startLine - v.startLine || m.endLine - v.endLine);
  l.forEach((m, v) => {
    if (a[m.index]) return;
    const y = l[v - 1];
    y && !a[y.index] && m.startLine <= y.endLine && (a[y.index] = cn("overlapping_line_ranges", `Line range ${y.startLine}-${y.endLine} overlaps ${m.startLine}-${m.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."), a[m.index] = cn("overlapping_line_ranges", `Line range ${m.startLine}-${m.endLine} overlaps ${y.startLine}-${y.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."));
  }), i.filter((m) => m.mode === "line_insert").forEach((m) => {
    if (a[m.index]) return;
    const v = l.find((T) => !a[T.index] && T.startLine < m.insertAtLine && m.insertAtLine <= T.endLine);
    if (!v) return;
    const y = cn("insert_inside_line_range", `Insert line ${m.insertAtLine} falls inside replaced line range ${v.startLine}-${v.endLine}`, "Merge the insertion into that startLine/endLine replacement, or insert at the range start/end boundary.");
    a[m.index] = y, a[v.index] = y;
  });
  const u = o.slice();
  i.slice().sort((m, v) => {
    const y = m.mode === "line_insert" ? m.insertAtLine : m.startLine, T = v.mode === "line_insert" ? v.insertAtLine : v.startLine;
    return y !== T ? T - y : m.mode !== v.mode ? m.mode === "line_range" ? -1 : 1 : m.mode === "line_insert" ? v.index - m.index : v.endLine - m.endLine || v.index - m.index;
  }).forEach((m) => {
    if (a[m.index]) return;
    if (m.mode === "line_insert") {
      u.splice(m.insertAtLine - 1, 0, ...m.insertionLines), a[m.index] = {
        ok: !0,
        index: m.index,
        insertAtLine: m.insertAtLine,
        replacements: 0,
        matchedBy: "line_insert",
        newPreview: eu(m.insertionLines, m.insertAtLine)
      };
      return;
    }
    const v = o.slice(m.startLine - 1, m.endLine);
    u.splice(m.startLine - 1, m.endLine - m.startLine + 1, ...m.replacementLines), a[m.index] = {
      ok: !0,
      index: m.index,
      startLine: m.startLine,
      endLine: m.endLine,
      replacements: 1,
      matchedBy: "line_range",
      oldPreview: eu(v, m.startLine),
      newPreview: eu(m.replacementLines, m.startLine)
    };
  });
  const d = a.filter((m) => m && !m.ok).length, f = a.filter((m) => m && m.ok).length;
  return {
    ok: d === 0,
    partial: f > 0 && d > 0 ? !0 : void 0,
    content: u.join(`
`),
    results: Rd(a, t, n)
  };
}
function sL(e) {
  if (Array.isArray(e)) return {
    ok: !0,
    edits: e,
    parsedFromString: !1
  };
  if (typeof e == "string") {
    const t = e.trim();
    if (!t) return {
      ok: !1,
      error: "edits_must_be_array",
      message: "edits must be a JSON array, but received an empty string",
      suggestion: 'Pass edits as an array, not a quoted JSON string. Correct: "edits":[{"oldString":"old","newString":"new"}].'
    };
    try {
      const n = JSON.parse(t);
      return Array.isArray(n) ? {
        ok: !0,
        edits: n,
        parsedFromString: !0,
        warning: "提醒：模型把 edits 发成了字符串；插件已解析并继续执行。下次请把 edits 直接作为数组传入，不要作为字符串。"
      } : {
        ok: !1,
        error: "edits_must_be_array",
        message: `edits must be a JSON array, but the JSON string parsed to ${Array.isArray(n) ? "array" : typeof n}`,
        suggestion: 'Pass edits directly as an array. Correct: "edits":[{"startLine":10,"endLine":50,"newString":"..."}]. Wrong: "edits":"[{\\"startLine\\":10,...}]".'
      };
    } catch (n) {
      const r = eL(t);
      return r ? {
        ok: !0,
        edits: r,
        parsedFromString: !0,
        warning: "提醒：模型把 edits 发成了字符串，且内容不是标准 JSON；插件已尝试修复并继续执行。下次请把 edits 直接作为数组传入，不要作为字符串。"
      } : {
        ok: !1,
        error: "invalid_edits_json_string",
        message: `edits must be a JSON array, but received a string that is not valid JSON: ${n?.message || "parse failed"}`,
        suggestion: 'Do not JSON-stringify edits. Pass an array value: "edits":[{"oldString":"old","newString":"new"}].'
      };
    }
  }
  return e == null ? {
    ok: !1,
    error: "missing_edits_array",
    message: "edits is required and must be a JSON array, but it was missing",
    suggestion: 'Include an edits array with at least one item. Correct: "edits":[{"startLine":10,"endLine":50,"newString":"..."}].'
  } : {
    ok: !1,
    error: "edits_must_be_array",
    message: `edits must be a JSON array, but received ${typeof e}`,
    suggestion: 'Pass edits as an array, not an object/string. Correct: "edits":[{"oldString":"old","newString":"new"}].'
  };
}
function aL(e = "", t) {
  let n = String(e ?? "");
  const r = sL(t);
  if (!r.ok) return {
    ok: !1,
    content: n,
    results: [cn(r.error, r.message, r.suggestion)]
  };
  const o = r.edits, s = [], a = [], i = [], l = [];
  let u = 0;
  if (!o.length) return {
    ok: !1,
    content: n,
    results: [cn("invalid_edits", "No edits provided", "Provide a non-empty edits array. For same-file multi-spot changes, put all replacements in this one Edit call.")]
  };
  const d = o.map((A) => RI(A)), f = d.filter((A) => A === "line_range").length, m = d.filter((A) => A === "line_insert").length, v = d.filter((A) => A === "old_string").length;
  if (f + m && v) return {
    ok: !1,
    content: n,
    results: Rd(o.map(() => cn("mixed_edit_modes", "Do not mix Edit modes in one Edit call", "Use separate Edit calls for oldString replacements, line-range replacements, and insertAtLine insertions.")), o, d)
  };
  if (f && m) {
    const A = oL(n, o.map((I, R) => oh(I, d[R])), d);
    return r.warning && (A.warning = r.warning), A;
  }
  if (f) {
    const A = nL(n, o.map((I, R) => oh(I, d[R])));
    return r.warning && (A.warning = r.warning), A;
  }
  if (m) {
    const A = rL(n, o.map((I, R) => oh(I, d[R])));
    return r.warning && (A.warning = r.warning), A;
  }
  const y = o.map((A, I) => oh(A, d[I]));
  y.forEach((A = {}, I) => {
    l[I] = i.slice();
    const R = typeof A.oldString == "string" ? A.oldString : String(A.oldString ?? ""), $ = typeof A.newString == "string" ? A.newString : String(A.newString ?? ""), p = !!A.replaceAll;
    if (R === $) {
      s.push(cn("no_changes", "No changes to make", "Change newString or remove this edit item."));
      return;
    }
    const b = Pd(R);
    if (R && a.some((M) => M.includes(R) || Pd(M).includes(b))) {
      s.push(cn("old_string_matches_previous_new_string", "old_string is a substring of a new_string from a previous edit.", "This edit may match text inserted earlier in the same Edit call. Merge overlapping changes into one larger replacement, or read the updated file and run a later Edit."));
      return;
    }
    if (!R) {
      s.push(cn("empty_old_string", "oldString is empty; provide text to replace", "Use Write to create files. For Edit, provide the exact current fragment to replace, or use Write for a full rewrite."));
      return;
    }
    const w = Op(n, R);
    if (!w.length) {
      const M = GD(n, R, $);
      s.push(cn("not_found", M.possibleAlreadyApplied ? "String to replace not found, but the requested newString already exists in the file" : "String to replace not found in file", M.possibleAlreadyApplied ? "Read the current file and verify whether the intended location is already correct. Edit will not mark this as successful without a same-call replacement proof." : "Edit already retries long oldString values with common punctuation and whitespace differences ignored. If it still fails, Read the current file and copy the exact current text into oldString. If this overlaps another same-file edit, merge them into one larger replacement; for very large rewrites, use Write.", M));
      return;
    }
    if (w.length > 1 && !p) {
      s.push(cn("multiple_matches", `找到 ${w.length} 处匹配，需要更多上下文或使用 replaceAll`, "Use the returned line contexts to expand oldString with unique surrounding text, or set replaceAll: true only if every match should change.", { matches: qD(n, w) }));
      return;
    }
    const E = p ? w : w.slice(0, 1), x = VD(n, E, $);
    n = x.content, a.push(...x.replacements), i.push(...x.replacementDetails), u += 1, s.push({
      ok: !0,
      index: I,
      replacements: E.length,
      matchedBy: x.matchedBy
    });
  }), s.forEach((A, I) => {
    !A || A.ok || ["not_found", "old_string_matches_previous_new_string"].includes(A.error) && WD(y[I], l[I] || []) && (s[I] = {
      ok: !0,
      index: I,
      replacements: 0,
      matchedBy: "already_satisfied",
      satisfied: !0,
      previousError: A.error,
      message: "Desired edit is already present in the file"
    });
  });
  const T = s.filter((A) => !A.ok).length, C = s.filter((A) => A?.satisfied).length;
  return {
    ok: T === 0,
    partial: (u > 0 || C > 0) && T > 0 ? !0 : void 0,
    content: n,
    results: Rd(s, y, d),
    warning: r.warning
  };
}
var v_ = function(e, t) {
  return v_ = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(n, r) {
    n.__proto__ = r;
  } || function(n, r) {
    for (var o in r) Object.prototype.hasOwnProperty.call(r, o) && (n[o] = r[o]);
  }, v_(e, t);
};
function iL(e, t) {
  if (typeof t != "function" && t !== null) throw new TypeError("Class extends value " + String(t) + " is not a constructor or null");
  v_(e, t);
  function n() {
    this.constructor = e;
  }
  e.prototype = t === null ? Object.create(t) : (n.prototype = t.prototype, new n());
}
var Nt = function() {
  return Nt = Object.assign || function(t) {
    for (var n, r = 1, o = arguments.length; r < o; r++) {
      n = arguments[r];
      for (var s in n) Object.prototype.hasOwnProperty.call(n, s) && (t[s] = n[s]);
    }
    return t;
  }, Nt.apply(this, arguments);
};
function zp(e, t, n) {
  if (n || arguments.length === 2)
    for (var r = 0, o = t.length, s; r < o; r++) (s || !(r in t)) && (s || (s = Array.prototype.slice.call(t, 0, r)), s[r] = t[r]);
  return e.concat(s || Array.prototype.slice.call(t));
}
var Xn = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : globalThis, Wn = Object.keys, vn = Array.isArray;
typeof Promise < "u" && !Xn.Promise && (Xn.Promise = Promise);
function ro(e, t) {
  return typeof t != "object" || Wn(t).forEach(function(n) {
    e[n] = t[n];
  }), e;
}
var pu = Object.getPrototypeOf, lL = {}.hasOwnProperty;
function xr(e, t) {
  return lL.call(e, t);
}
function mu(e, t) {
  typeof t == "function" && (t = t(pu(e))), (typeof Reflect > "u" ? Wn : Reflect.ownKeys)(t).forEach(function(n) {
    Oa(e, n, t[n]);
  });
}
var $I = Object.defineProperty;
function Oa(e, t, n, r) {
  $I(e, t, ro(n && xr(n, "get") && typeof n.get == "function" ? {
    get: n.get,
    set: n.set,
    configurable: !0
  } : {
    value: n,
    configurable: !0,
    writable: !0
  }, r));
}
function Mu(e) {
  return { from: function(t) {
    return e.prototype = Object.create(t.prototype), Oa(e.prototype, "constructor", e), { extend: mu.bind(null, e.prototype) };
  } };
}
var uL = Object.getOwnPropertyDescriptor;
function DI(e, t) {
  var n = uL(e, t), r;
  return n || (r = pu(e)) && DI(r, t);
}
var cL = [].slice;
function ng(e, t, n) {
  return cL.call(e, t, n);
}
function LI(e, t) {
  return t(e);
}
function Dc(e) {
  if (!e) throw new Error("Assertion Failed");
}
function OI(e) {
  Xn.setImmediate ? setImmediate(e) : setTimeout(e, 0);
}
function dL(e, t) {
  return e.reduce(function(n, r, o) {
    var s = t(r, o);
    return s && (n[s[0]] = s[1]), n;
  }, {});
}
function Js(e, t) {
  if (typeof t == "string" && xr(e, t)) return e[t];
  if (!t) return e;
  if (typeof t != "string") {
    for (var n = [], r = 0, o = t.length; r < o; ++r) {
      var s = Js(e, t[r]);
      n.push(s);
    }
    return n;
  }
  var a = t.indexOf(".");
  if (a !== -1) {
    var i = e[t.substr(0, a)];
    return i == null ? void 0 : Js(i, t.substr(a + 1));
  }
}
function Zr(e, t, n) {
  if (!(!e || t === void 0) && !("isFrozen" in Object && Object.isFrozen(e)))
    if (typeof t != "string" && "length" in t) {
      Dc(typeof n != "string" && "length" in n);
      for (var r = 0, o = t.length; r < o; ++r) Zr(e, t[r], n[r]);
    } else {
      var s = t.indexOf(".");
      if (s !== -1) {
        var a = t.substr(0, s), i = t.substr(s + 1);
        if (i === "") n === void 0 ? vn(e) && !isNaN(parseInt(a)) ? e.splice(a, 1) : delete e[a] : e[a] = n;
        else {
          var l = e[a];
          (!l || !xr(e, a)) && (l = e[a] = {}), Zr(l, i, n);
        }
      } else n === void 0 ? vn(e) && !isNaN(parseInt(t)) ? e.splice(t, 1) : delete e[t] : e[t] = n;
    }
}
function fL(e, t) {
  typeof t == "string" ? Zr(e, t, void 0) : "length" in t && [].map.call(t, function(n) {
    Zr(e, n, void 0);
  });
}
function FI(e) {
  var t = {};
  for (var n in e) xr(e, n) && (t[n] = e[n]);
  return t;
}
var hL = [].concat;
function BI(e) {
  return hL.apply([], e);
}
var pL = "BigUint64Array,BigInt64Array,Array,Boolean,String,Date,RegExp,Blob,File,FileList,FileSystemFileHandle,FileSystemDirectoryHandle,ArrayBuffer,DataView,Uint8ClampedArray,ImageBitmap,ImageData,Map,Set,CryptoKey".split(",").concat(BI([
  8,
  16,
  32,
  64
].map(function(e) {
  return [
    "Int",
    "Uint",
    "Float"
  ].map(function(t) {
    return t + e + "Array";
  });
}))).filter(function(e) {
  return Xn[e];
}), UI = new Set(pL.map(function(e) {
  return Xn[e];
}));
function qI(e) {
  var t = {};
  for (var n in e) if (xr(e, n)) {
    var r = e[n];
    t[n] = !r || typeof r != "object" || UI.has(r.constructor) ? r : qI(r);
  }
  return t;
}
function mL(e) {
  for (var t in e) if (xr(e, t)) return !1;
  return !0;
}
var cd = null;
function qi(e) {
  cd = /* @__PURE__ */ new WeakMap();
  var t = y_(e);
  return cd = null, t;
}
function y_(e) {
  if (!e || typeof e != "object") return e;
  var t = cd.get(e);
  if (t) return t;
  if (vn(e)) {
    t = [], cd.set(e, t);
    for (var n = 0, r = e.length; n < r; ++n) t.push(y_(e[n]));
  } else if (UI.has(e.constructor)) t = e;
  else {
    var o = pu(e);
    t = o === Object.prototype ? {} : Object.create(o), cd.set(e, t);
    for (var s in e) xr(e, s) && (t[s] = y_(e[s]));
  }
  return t;
}
var gL = {}.toString;
function __(e) {
  return gL.call(e).slice(8, -1);
}
var b_ = typeof Symbol < "u" ? Symbol.iterator : "@@iterator", vL = typeof b_ == "symbol" ? function(e) {
  var t;
  return e != null && (t = e[b_]) && t.apply(e);
} : function() {
  return null;
};
function vi(e, t) {
  var n = e.indexOf(t);
  return n >= 0 && e.splice(n, 1), n >= 0;
}
var Dl = {};
function Us(e) {
  var t, n, r, o;
  if (arguments.length === 1) {
    if (vn(e)) return e.slice();
    if (this === Dl && typeof e == "string") return [e];
    if (o = vL(e)) {
      for (n = []; r = o.next(), !r.done; ) n.push(r.value);
      return n;
    }
    if (e == null) return [e];
    if (t = e.length, typeof t == "number") {
      for (n = new Array(t); t--; ) n[t] = e[t];
      return n;
    }
    return [e];
  }
  for (t = arguments.length, n = new Array(t); t--; ) n[t] = arguments[t];
  return n;
}
var G0 = typeof Symbol < "u" ? function(e) {
  return e[Symbol.toStringTag] === "AsyncFunction";
} : function() {
  return !1;
}, yL = [
  "Modify",
  "Bulk",
  "OpenFailed",
  "VersionChange",
  "Schema",
  "Upgrade",
  "InvalidTable",
  "MissingAPI",
  "NoSuchDatabase",
  "InvalidArgument",
  "SubTransaction",
  "Unsupported",
  "Internal",
  "DatabaseClosed",
  "PrematureCommit",
  "ForeignAwait"
], HI = [
  "Unknown",
  "Constraint",
  "Data",
  "TransactionInactive",
  "ReadOnly",
  "Version",
  "NotFound",
  "InvalidState",
  "InvalidAccess",
  "Abort",
  "Timeout",
  "QuotaExceeded",
  "Syntax",
  "DataClone"
], V0 = yL.concat(HI), _L = {
  VersionChanged: "Database version changed by other database connection",
  DatabaseClosed: "Database has been closed",
  Abort: "Transaction aborted",
  TransactionInactive: "Transaction has already completed or failed",
  MissingAPI: "IndexedDB API missing. Please visit https://tinyurl.com/y2uuvskb"
};
function Pu(e, t) {
  this.name = e, this.message = t;
}
Mu(Pu).from(Error).extend({ toString: function() {
  return this.name + ": " + this.message;
} });
function zI(e, t) {
  return e + ". Errors: " + Object.keys(t).map(function(n) {
    return t[n].toString();
  }).filter(function(n, r, o) {
    return o.indexOf(n) === r;
  }).join(`
`);
}
function Wp(e, t, n, r) {
  this.failures = t, this.failedKeys = r, this.successCount = n, this.message = zI(e, t);
}
Mu(Wp).from(Pu);
function Wl(e, t) {
  this.name = "BulkError", this.failures = Object.keys(t).map(function(n) {
    return t[n];
  }), this.failuresByPos = t, this.message = zI(e, this.failures);
}
Mu(Wl).from(Pu);
var K0 = V0.reduce(function(e, t) {
  return e[t] = t + "Error", e;
}, {}), bL = Pu, vt = V0.reduce(function(e, t) {
  var n = t + "Error";
  function r(o, s) {
    this.name = n, o ? typeof o == "string" ? (this.message = "".concat(o).concat(s ? `
 ` + s : ""), this.inner = s || null) : typeof o == "object" && (this.message = "".concat(o.name, " ").concat(o.message), this.inner = o) : (this.message = _L[t] || n, this.inner = null);
  }
  return Mu(r).from(bL), e[t] = r, e;
}, {});
vt.Syntax = SyntaxError;
vt.Type = TypeError;
vt.Range = RangeError;
var P2 = HI.reduce(function(e, t) {
  return e[t + "Error"] = vt[t], e;
}, {});
function wL(e, t) {
  if (!e || e instanceof Pu || e instanceof TypeError || e instanceof SyntaxError || !e.name || !P2[e.name]) return e;
  var n = new P2[e.name](t || e.message, e);
  return "stack" in e && Oa(n, "stack", { get: function() {
    return this.inner.stack;
  } }), n;
}
var rg = V0.reduce(function(e, t) {
  return [
    "Syntax",
    "Type",
    "Range"
  ].indexOf(t) === -1 && (e[t + "Error"] = vt[t]), e;
}, {});
rg.ModifyError = Wp;
rg.DexieError = Pu;
rg.BulkError = Wl;
function rn() {
}
function rf(e) {
  return e;
}
function SL(e, t) {
  return e == null || e === rf ? t : function(n) {
    return t(e(n));
  };
}
function Hi(e, t) {
  return function() {
    e.apply(this, arguments), t.apply(this, arguments);
  };
}
function TL(e, t) {
  return e === rn ? t : function() {
    var n = e.apply(this, arguments);
    n !== void 0 && (arguments[0] = n);
    var r = this.onsuccess, o = this.onerror;
    this.onsuccess = null, this.onerror = null;
    var s = t.apply(this, arguments);
    return r && (this.onsuccess = this.onsuccess ? Hi(r, this.onsuccess) : r), o && (this.onerror = this.onerror ? Hi(o, this.onerror) : o), s !== void 0 ? s : n;
  };
}
function EL(e, t) {
  return e === rn ? t : function() {
    e.apply(this, arguments);
    var n = this.onsuccess, r = this.onerror;
    this.onsuccess = this.onerror = null, t.apply(this, arguments), n && (this.onsuccess = this.onsuccess ? Hi(n, this.onsuccess) : n), r && (this.onerror = this.onerror ? Hi(r, this.onerror) : r);
  };
}
function AL(e, t) {
  return e === rn ? t : function(n) {
    var r = e.apply(this, arguments);
    ro(n, r);
    var o = this.onsuccess, s = this.onerror;
    this.onsuccess = null, this.onerror = null;
    var a = t.apply(this, arguments);
    return o && (this.onsuccess = this.onsuccess ? Hi(o, this.onsuccess) : o), s && (this.onerror = this.onerror ? Hi(s, this.onerror) : s), r === void 0 ? a === void 0 ? void 0 : a : ro(r, a);
  };
}
function CL(e, t) {
  return e === rn ? t : function() {
    return t.apply(this, arguments) === !1 ? !1 : e.apply(this, arguments);
  };
}
function J0(e, t) {
  return e === rn ? t : function() {
    var n = e.apply(this, arguments);
    if (n && typeof n.then == "function") {
      for (var r = this, o = arguments.length, s = new Array(o); o--; ) s[o] = arguments[o];
      return n.then(function() {
        return t.apply(r, s);
      });
    }
    return t.apply(this, arguments);
  };
}
var ds = typeof location < "u" && /^(http|https):\/\/(localhost|127\.0\.0\.1)/.test(location.href);
function WI(e, t) {
  ds = e;
}
var Nd = {}, GI = 100, Y0 = typeof Promise > "u" ? [] : (function() {
  var e = Promise.resolve();
  if (typeof crypto > "u" || !crypto.subtle) return [
    e,
    pu(e),
    e
  ];
  var t = crypto.subtle.digest("SHA-512", new Uint8Array([0]));
  return [
    t,
    pu(t),
    e
  ];
})(), R2 = Y0[0], N2 = Y0[1], kL = Y0[2], IL = N2 && N2.then, Si = R2 && R2.constructor, X0 = !!kL;
function xL() {
  queueMicrotask(PL);
}
var $d = function(e, t) {
  Lc.push([e, t]), Gp && (xL(), Gp = !1);
}, w_ = !0, Gp = !0, Di = [], tp = [], S_ = rf, $a = {
  id: "global",
  global: !0,
  ref: 0,
  unhandleds: [],
  onunhandled: rn,
  pgp: !1,
  env: {},
  finalize: rn
}, pt = $a, Lc = [], Li = 0, np = [];
function st(e) {
  if (typeof this != "object") throw new TypeError("Promises must be constructed via new");
  this._listeners = [], this._lib = !1;
  var t = this._PSD = pt;
  if (typeof e != "function") {
    if (e !== Nd) throw new TypeError("Not a function");
    this._state = arguments[1], this._value = arguments[2], this._state === !1 && E_(this, this._value);
    return;
  }
  this._state = null, this._value = null, ++t.ref, KI(this, e);
}
var T_ = {
  get: function() {
    var e = pt, t = Vp;
    function n(r, o) {
      var s = this, a = !e.global && (e !== pt || t !== Vp), i = a && !Ba(), l = new st(function(u, d) {
        Q0(s, new VI($2(r, e, a, i), $2(o, e, a, i), u, d, e));
      });
      return this._consoleTask && (l._consoleTask = this._consoleTask), l;
    }
    return n.prototype = Nd, n;
  },
  set: function(e) {
    Oa(this, "then", e && e.prototype === Nd ? T_ : {
      get: function() {
        return e;
      },
      set: T_.set
    });
  }
};
mu(st.prototype, {
  then: T_,
  _then: function(e, t) {
    Q0(this, new VI(null, null, e, t, pt));
  },
  catch: function(e) {
    if (arguments.length === 1) return this.then(null, e);
    var t = arguments[0], n = arguments[1];
    return typeof t == "function" ? this.then(null, function(r) {
      return r instanceof t ? n(r) : rp(r);
    }) : this.then(null, function(r) {
      return r && r.name === t ? n(r) : rp(r);
    });
  },
  finally: function(e) {
    return this.then(function(t) {
      return st.resolve(e()).then(function() {
        return t;
      });
    }, function(t) {
      return st.resolve(e()).then(function() {
        return rp(t);
      });
    });
  },
  timeout: function(e, t) {
    var n = this;
    return e < 1 / 0 ? new st(function(r, o) {
      var s = setTimeout(function() {
        return o(new vt.Timeout(t));
      }, e);
      n.then(r, o).finally(clearTimeout.bind(null, s));
    }) : this;
  }
});
typeof Symbol < "u" && Symbol.toStringTag && Oa(st.prototype, Symbol.toStringTag, "Dexie.Promise");
$a.env = YI();
function VI(e, t, n, r, o) {
  this.onFulfilled = typeof e == "function" ? e : null, this.onRejected = typeof t == "function" ? t : null, this.resolve = n, this.reject = r, this.psd = o;
}
mu(st, {
  all: function() {
    var e = Us.apply(null, arguments).map(Kp);
    return new st(function(t, n) {
      e.length === 0 && t([]);
      var r = e.length;
      e.forEach(function(o, s) {
        return st.resolve(o).then(function(a) {
          e[s] = a, --r || t(e);
        }, n);
      });
    });
  },
  resolve: function(e) {
    return e instanceof st ? e : e && typeof e.then == "function" ? new st(function(t, n) {
      e.then(t, n);
    }) : new st(Nd, !0, e);
  },
  reject: rp,
  race: function() {
    var e = Us.apply(null, arguments).map(Kp);
    return new st(function(t, n) {
      e.map(function(r) {
        return st.resolve(r).then(t, n);
      });
    });
  },
  PSD: {
    get: function() {
      return pt;
    },
    set: function(e) {
      return pt = e;
    }
  },
  totalEchoes: { get: function() {
    return Vp;
  } },
  newPSD: Fa,
  usePSD: zi,
  scheduler: {
    get: function() {
      return $d;
    },
    set: function(e) {
      $d = e;
    }
  },
  rejectionMapper: {
    get: function() {
      return S_;
    },
    set: function(e) {
      S_ = e;
    }
  },
  follow: function(e, t) {
    return new st(function(n, r) {
      return Fa(function(o, s) {
        var a = pt;
        a.unhandleds = [], a.onunhandled = s, a.finalize = Hi(function() {
          var i = this;
          RL(function() {
            i.unhandleds.length === 0 ? o() : s(i.unhandleds[0]);
          });
        }, a.finalize), e();
      }, t, n, r);
    });
  }
});
Si && (Si.allSettled && Oa(st, "allSettled", function() {
  var e = Us.apply(null, arguments).map(Kp);
  return new st(function(t) {
    e.length === 0 && t([]);
    var n = e.length, r = new Array(n);
    e.forEach(function(o, s) {
      return st.resolve(o).then(function(a) {
        return r[s] = {
          status: "fulfilled",
          value: a
        };
      }, function(a) {
        return r[s] = {
          status: "rejected",
          reason: a
        };
      }).then(function() {
        return --n || t(r);
      });
    });
  });
}), Si.any && typeof AggregateError < "u" && Oa(st, "any", function() {
  var e = Us.apply(null, arguments).map(Kp);
  return new st(function(t, n) {
    e.length === 0 && n(/* @__PURE__ */ new AggregateError([]));
    var r = e.length, o = new Array(r);
    e.forEach(function(s, a) {
      return st.resolve(s).then(function(i) {
        return t(i);
      }, function(i) {
        o[a] = i, --r || n(new AggregateError(o));
      });
    });
  });
}), Si.withResolvers && (st.withResolvers = Si.withResolvers));
function KI(e, t) {
  try {
    t(function(n) {
      if (e._state === null) {
        if (n === e) throw new TypeError("A promise cannot be resolved with itself.");
        var r = e._lib && Ru();
        n && typeof n.then == "function" ? KI(e, function(o, s) {
          n instanceof st ? n._then(o, s) : n.then(o, s);
        }) : (e._state = !0, e._value = n, JI(e)), r && Nu();
      }
    }, E_.bind(null, e));
  } catch (n) {
    E_(e, n);
  }
}
function E_(e, t) {
  if (tp.push(t), e._state === null) {
    var n = e._lib && Ru();
    t = S_(t), e._state = !1, e._value = t, NL(e), JI(e), n && Nu();
  }
}
function JI(e) {
  var t = e._listeners;
  e._listeners = [];
  for (var n = 0, r = t.length; n < r; ++n) Q0(e, t[n]);
  var o = e._PSD;
  --o.ref || o.finalize(), Li === 0 && (++Li, $d(function() {
    --Li === 0 && Z0();
  }, []));
}
function Q0(e, t) {
  if (e._state === null) {
    e._listeners.push(t);
    return;
  }
  var n = e._state ? t.onFulfilled : t.onRejected;
  if (n === null) return (e._state ? t.resolve : t.reject)(e._value);
  ++t.psd.ref, ++Li, $d(ML, [
    n,
    e,
    t
  ]);
}
function ML(e, t, n) {
  try {
    var r, o = t._value;
    !t._state && tp.length && (tp = []), r = ds && t._consoleTask ? t._consoleTask.run(function() {
      return e(o);
    }) : e(o), !t._state && tp.indexOf(o) === -1 && $L(t), n.resolve(r);
  } catch (s) {
    n.reject(s);
  } finally {
    --Li === 0 && Z0(), --n.psd.ref || n.psd.finalize();
  }
}
function PL() {
  zi($a, function() {
    Ru() && Nu();
  });
}
function Ru() {
  var e = w_;
  return w_ = !1, Gp = !1, e;
}
function Nu() {
  var e, t, n;
  do
    for (; Lc.length > 0; )
      for (e = Lc, Lc = [], n = e.length, t = 0; t < n; ++t) {
        var r = e[t];
        r[0].apply(null, r[1]);
      }
  while (Lc.length > 0);
  w_ = !0, Gp = !0;
}
function Z0() {
  var e = Di;
  Di = [], e.forEach(function(r) {
    r._PSD.onunhandled.call(null, r._value, r);
  });
  for (var t = np.slice(0), n = t.length; n; ) t[--n]();
}
function RL(e) {
  function t() {
    e(), np.splice(np.indexOf(t), 1);
  }
  np.push(t), ++Li, $d(function() {
    --Li === 0 && Z0();
  }, []);
}
function NL(e) {
  Di.some(function(t) {
    return t._value === e._value;
  }) || Di.push(e);
}
function $L(e) {
  for (var t = Di.length; t; ) if (Di[--t]._value === e._value) {
    Di.splice(t, 1);
    return;
  }
}
function rp(e) {
  return new st(Nd, !1, e);
}
function fn(e, t) {
  var n = pt;
  return function() {
    var r = Ru(), o = pt;
    try {
      return Ua(n, !0), e.apply(this, arguments);
    } catch (s) {
      t && t(s);
    } finally {
      Ua(o, !1), r && Nu();
    }
  };
}
var Hn = {
  awaits: 0,
  echoes: 0,
  id: 0
}, DL = 0, op = [], sp = 0, Vp = 0, LL = 0;
function Fa(e, t, n, r) {
  var o = pt, s = Object.create(o);
  s.parent = o, s.ref = 0, s.global = !1, s.id = ++LL, $a.env, s.env = X0 ? {
    Promise: st,
    PromiseProp: {
      value: st,
      configurable: !0,
      writable: !0
    },
    all: st.all,
    race: st.race,
    allSettled: st.allSettled,
    any: st.any,
    resolve: st.resolve,
    reject: st.reject
  } : {}, t && ro(s, t), ++o.ref, s.finalize = function() {
    --this.parent.ref || this.parent.finalize();
  };
  var a = zi(s, e, n, r);
  return s.ref === 0 && s.finalize(), a;
}
function $u() {
  return Hn.id || (Hn.id = ++DL), ++Hn.awaits, Hn.echoes += GI, Hn.id;
}
function Ba() {
  return Hn.awaits ? (--Hn.awaits === 0 && (Hn.id = 0), Hn.echoes = Hn.awaits * GI, !0) : !1;
}
("" + IL).indexOf("[native code]") === -1 && ($u = Ba = rn);
function Kp(e) {
  return Hn.echoes && e && e.constructor === Si ? ($u(), e.then(function(t) {
    return Ba(), t;
  }, function(t) {
    return Ba(), An(t);
  })) : e;
}
function OL(e) {
  ++Vp, (!Hn.echoes || --Hn.echoes === 0) && (Hn.echoes = Hn.awaits = Hn.id = 0), op.push(pt), Ua(e, !0);
}
function FL() {
  var e = op[op.length - 1];
  op.pop(), Ua(e, !1);
}
function Ua(e, t) {
  var n = pt;
  if ((t ? Hn.echoes && (!sp++ || e !== pt) : sp && (!--sp || e !== pt)) && queueMicrotask(t ? OL.bind(null, e) : FL), e !== pt && (pt = e, n === $a && ($a.env = YI()), X0)) {
    var r = $a.env.Promise, o = e.env;
    (n.global || e.global) && (Object.defineProperty(Xn, "Promise", o.PromiseProp), r.all = o.all, r.race = o.race, r.resolve = o.resolve, r.reject = o.reject, o.allSettled && (r.allSettled = o.allSettled), o.any && (r.any = o.any));
  }
}
function YI() {
  var e = Xn.Promise;
  return X0 ? {
    Promise: e,
    PromiseProp: Object.getOwnPropertyDescriptor(Xn, "Promise"),
    all: e.all,
    race: e.race,
    allSettled: e.allSettled,
    any: e.any,
    resolve: e.resolve,
    reject: e.reject
  } : {};
}
function zi(e, t, n, r, o) {
  var s = pt;
  try {
    return Ua(e, !0), t(n, r, o);
  } finally {
    Ua(s, !1);
  }
}
function $2(e, t, n, r) {
  return typeof e != "function" ? e : function() {
    var o = pt;
    n && $u(), Ua(t, !0);
    try {
      return e.apply(this, arguments);
    } finally {
      Ua(o, !1), r && queueMicrotask(Ba);
    }
  };
}
function Zv(e) {
  Promise === Si && Hn.echoes === 0 ? sp === 0 ? e() : enqueueNativeMicroTask(e) : setTimeout(e, 0);
}
var An = st.reject;
function A_(e, t, n, r) {
  if (!e.idbdb || !e._state.openComplete && !pt.letThrough && !e._vip) {
    if (e._state.openComplete) return An(new vt.DatabaseClosed(e._state.dbOpenError));
    if (!e._state.isBeingOpened) {
      if (!e._state.autoOpen) return An(new vt.DatabaseClosed());
      e.open().catch(rn);
    }
    return e._state.dbReadyPromise.then(function() {
      return A_(e, t, n, r);
    });
  } else {
    var o = e._createTransaction(t, n, e._dbSchema);
    try {
      o.create(), e._state.PR1398_maxLoop = 3;
    } catch (s) {
      return s.name === K0.InvalidState && e.isOpen() && --e._state.PR1398_maxLoop > 0 ? (console.warn("Dexie: Need to reopen db"), e.close({ disableAutoOpen: !1 }), e.open().then(function() {
        return A_(e, t, n, r);
      })) : An(s);
    }
    return o._promise(t, function(s, a) {
      return Fa(function() {
        return pt.trans = o, r(s, a, o);
      });
    }).then(function(s) {
      if (t === "readwrite") try {
        o.idbtrans.commit();
      } catch {
      }
      return t === "readonly" ? s : o._completion.then(function() {
        return s;
      });
    });
  }
}
var D2 = "4.0.10", Ii = "￿", C_ = -1 / 0, Cs = "Invalid key provided. Keys must be of type string, number, Date or Array<string | number | Date>.", XI = "String expected.", tu = [], og = "__dbnames", jv = "readonly", ey = "readwrite";
function Wi(e, t) {
  return e ? t ? function() {
    return e.apply(this, arguments) && t.apply(this, arguments);
  } : e : t;
}
var QI = {
  type: 3,
  lower: -1 / 0,
  lowerOpen: !1,
  upper: [[]],
  upperOpen: !1
};
function sh(e) {
  return typeof e == "string" && !/\./.test(e) ? function(t) {
    return t[e] === void 0 && e in t && (t = qi(t), delete t[e]), t;
  } : function(t) {
    return t;
  };
}
function BL() {
  throw vt.Type();
}
function Wt(e, t) {
  try {
    var n = L2(e), r = L2(t);
    if (n !== r)
      return n === "Array" ? 1 : r === "Array" ? -1 : n === "binary" ? 1 : r === "binary" ? -1 : n === "string" ? 1 : r === "string" ? -1 : n === "Date" ? 1 : r !== "Date" ? NaN : -1;
    switch (n) {
      case "number":
      case "Date":
      case "string":
        return e > t ? 1 : e < t ? -1 : 0;
      case "binary":
        return qL(O2(e), O2(t));
      case "Array":
        return UL(e, t);
    }
  } catch {
  }
  return NaN;
}
function UL(e, t) {
  for (var n = e.length, r = t.length, o = n < r ? n : r, s = 0; s < o; ++s) {
    var a = Wt(e[s], t[s]);
    if (a !== 0) return a;
  }
  return n === r ? 0 : n < r ? -1 : 1;
}
function qL(e, t) {
  for (var n = e.length, r = t.length, o = n < r ? n : r, s = 0; s < o; ++s) if (e[s] !== t[s]) return e[s] < t[s] ? -1 : 1;
  return n === r ? 0 : n < r ? -1 : 1;
}
function L2(e) {
  var t = typeof e;
  if (t !== "object") return t;
  if (ArrayBuffer.isView(e)) return "binary";
  var n = __(e);
  return n === "ArrayBuffer" ? "binary" : n;
}
function O2(e) {
  return e instanceof Uint8Array ? e : ArrayBuffer.isView(e) ? new Uint8Array(e.buffer, e.byteOffset, e.byteLength) : new Uint8Array(e);
}
var ZI = (function() {
  function e() {
  }
  return e.prototype._trans = function(t, n, r) {
    var o = this._tx || pt.trans, s = this.name, a = ds && typeof console < "u" && console.createTask && console.createTask("Dexie: ".concat(t === "readonly" ? "read" : "write", " ").concat(this.name));
    function i(d, f, m) {
      if (!m.schema[s]) throw new vt.NotFound("Table " + s + " not part of transaction");
      return n(m.idbtrans, m);
    }
    var l = Ru();
    try {
      var u = o && o.db._novip === this.db._novip ? o === pt.trans ? o._promise(t, i, r) : Fa(function() {
        return o._promise(t, i, r);
      }, {
        trans: o,
        transless: pt.transless || pt
      }) : A_(this.db, t, [this.name], i);
      return a && (u._consoleTask = a, u = u.catch(function(d) {
        return console.trace(d), An(d);
      })), u;
    } finally {
      l && Nu();
    }
  }, e.prototype.get = function(t, n) {
    var r = this;
    return t && t.constructor === Object ? this.where(t).first(n) : t == null ? An(new vt.Type("Invalid argument to Table.get()")) : this._trans("readonly", function(o) {
      return r.core.get({
        trans: o,
        key: t
      }).then(function(s) {
        return r.hook.reading.fire(s);
      });
    }).then(n);
  }, e.prototype.where = function(t) {
    if (typeof t == "string") return new this.db.WhereClause(this, t);
    if (vn(t)) return new this.db.WhereClause(this, "[".concat(t.join("+"), "]"));
    var n = Wn(t);
    if (n.length === 1) return this.where(n[0]).equals(t[n[0]]);
    var r = this.schema.indexes.concat(this.schema.primKey).filter(function(d) {
      if (d.compound && n.every(function(m) {
        return d.keyPath.indexOf(m) >= 0;
      })) {
        for (var f = 0; f < n.length; ++f) if (n.indexOf(d.keyPath[f]) === -1) return !1;
        return !0;
      }
      return !1;
    }).sort(function(d, f) {
      return d.keyPath.length - f.keyPath.length;
    })[0];
    if (r && this.db._maxKey !== Ii) {
      var o = r.keyPath.slice(0, n.length);
      return this.where(o).equals(o.map(function(d) {
        return t[d];
      }));
    }
    !r && ds && console.warn("The query ".concat(JSON.stringify(t), " on ").concat(this.name, " would benefit from a ") + "compound index [".concat(n.join("+"), "]"));
    var s = this.schema.idxByName;
    function a(d, f) {
      return Wt(d, f) === 0;
    }
    var i = n.reduce(function(d, f) {
      var m = d[0], v = d[1], y = s[f], T = t[f];
      return [m || y, m || !y ? Wi(v, y && y.multi ? function(C) {
        var A = Js(C, f);
        return vn(A) && A.some(function(I) {
          return a(T, I);
        });
      } : function(C) {
        return a(T, Js(C, f));
      }) : v];
    }, [null, null]), l = i[0], u = i[1];
    return l ? this.where(l.name).equals(t[l.keyPath]).filter(u) : r ? this.filter(u) : this.where(n).equals("");
  }, e.prototype.filter = function(t) {
    return this.toCollection().and(t);
  }, e.prototype.count = function(t) {
    return this.toCollection().count(t);
  }, e.prototype.offset = function(t) {
    return this.toCollection().offset(t);
  }, e.prototype.limit = function(t) {
    return this.toCollection().limit(t);
  }, e.prototype.each = function(t) {
    return this.toCollection().each(t);
  }, e.prototype.toArray = function(t) {
    return this.toCollection().toArray(t);
  }, e.prototype.toCollection = function() {
    return new this.db.Collection(new this.db.WhereClause(this));
  }, e.prototype.orderBy = function(t) {
    return new this.db.Collection(new this.db.WhereClause(this, vn(t) ? "[".concat(t.join("+"), "]") : t));
  }, e.prototype.reverse = function() {
    return this.toCollection().reverse();
  }, e.prototype.mapToClass = function(t) {
    var n = this, r = n.db, o = n.name;
    this.schema.mappedClass = t, t.prototype instanceof BL && (t = (function(l) {
      iL(u, l);
      function u() {
        return l !== null && l.apply(this, arguments) || this;
      }
      return Object.defineProperty(u.prototype, "db", {
        get: function() {
          return r;
        },
        enumerable: !1,
        configurable: !0
      }), u.prototype.table = function() {
        return o;
      }, u;
    })(t));
    for (var s = /* @__PURE__ */ new Set(), a = t.prototype; a; a = pu(a)) Object.getOwnPropertyNames(a).forEach(function(l) {
      return s.add(l);
    });
    var i = function(l) {
      if (!l) return l;
      var u = Object.create(t.prototype);
      for (var d in l) if (!s.has(d)) try {
        u[d] = l[d];
      } catch {
      }
      return u;
    };
    return this.schema.readHook && this.hook.reading.unsubscribe(this.schema.readHook), this.schema.readHook = i, this.hook("reading", i), t;
  }, e.prototype.defineClass = function() {
    function t(n) {
      ro(this, n);
    }
    return this.mapToClass(t);
  }, e.prototype.add = function(t, n) {
    var r = this, o = this.schema.primKey, s = o.auto, a = o.keyPath, i = t;
    return a && s && (i = sh(a)(t)), this._trans("readwrite", function(l) {
      return r.core.mutate({
        trans: l,
        type: "add",
        keys: n != null ? [n] : null,
        values: [i]
      });
    }).then(function(l) {
      return l.numFailures ? st.reject(l.failures[0]) : l.lastResult;
    }).then(function(l) {
      if (a) try {
        Zr(t, a, l);
      } catch {
      }
      return l;
    });
  }, e.prototype.update = function(t, n) {
    if (typeof t == "object" && !vn(t)) {
      var r = Js(t, this.schema.primKey.keyPath);
      return r === void 0 ? An(new vt.InvalidArgument("Given object does not contain its primary key")) : this.where(":id").equals(r).modify(n);
    } else return this.where(":id").equals(t).modify(n);
  }, e.prototype.put = function(t, n) {
    var r = this, o = this.schema.primKey, s = o.auto, a = o.keyPath, i = t;
    return a && s && (i = sh(a)(t)), this._trans("readwrite", function(l) {
      return r.core.mutate({
        trans: l,
        type: "put",
        values: [i],
        keys: n != null ? [n] : null
      });
    }).then(function(l) {
      return l.numFailures ? st.reject(l.failures[0]) : l.lastResult;
    }).then(function(l) {
      if (a) try {
        Zr(t, a, l);
      } catch {
      }
      return l;
    });
  }, e.prototype.delete = function(t) {
    var n = this;
    return this._trans("readwrite", function(r) {
      return n.core.mutate({
        trans: r,
        type: "delete",
        keys: [t]
      });
    }).then(function(r) {
      return r.numFailures ? st.reject(r.failures[0]) : void 0;
    });
  }, e.prototype.clear = function() {
    var t = this;
    return this._trans("readwrite", function(n) {
      return t.core.mutate({
        trans: n,
        type: "deleteRange",
        range: QI
      });
    }).then(function(n) {
      return n.numFailures ? st.reject(n.failures[0]) : void 0;
    });
  }, e.prototype.bulkGet = function(t) {
    var n = this;
    return this._trans("readonly", function(r) {
      return n.core.getMany({
        keys: t,
        trans: r
      }).then(function(o) {
        return o.map(function(s) {
          return n.hook.reading.fire(s);
        });
      });
    });
  }, e.prototype.bulkAdd = function(t, n, r) {
    var o = this, s = Array.isArray(n) ? n : void 0;
    r = r || (s ? void 0 : n);
    var a = r ? r.allKeys : void 0;
    return this._trans("readwrite", function(i) {
      var l = o.schema.primKey, u = l.auto, d = l.keyPath;
      if (d && s) throw new vt.InvalidArgument("bulkAdd(): keys argument invalid on tables with inbound keys");
      if (s && s.length !== t.length) throw new vt.InvalidArgument("Arguments objects and keys must have the same length");
      var f = t.length, m = d && u ? t.map(sh(d)) : t;
      return o.core.mutate({
        trans: i,
        type: "add",
        keys: s,
        values: m,
        wantResults: a
      }).then(function(v) {
        var y = v.numFailures, T = v.results, C = v.lastResult, A = v.failures, I = a ? T : C;
        if (y === 0) return I;
        throw new Wl("".concat(o.name, ".bulkAdd(): ").concat(y, " of ").concat(f, " operations failed"), A);
      });
    });
  }, e.prototype.bulkPut = function(t, n, r) {
    var o = this, s = Array.isArray(n) ? n : void 0;
    r = r || (s ? void 0 : n);
    var a = r ? r.allKeys : void 0;
    return this._trans("readwrite", function(i) {
      var l = o.schema.primKey, u = l.auto, d = l.keyPath;
      if (d && s) throw new vt.InvalidArgument("bulkPut(): keys argument invalid on tables with inbound keys");
      if (s && s.length !== t.length) throw new vt.InvalidArgument("Arguments objects and keys must have the same length");
      var f = t.length, m = d && u ? t.map(sh(d)) : t;
      return o.core.mutate({
        trans: i,
        type: "put",
        keys: s,
        values: m,
        wantResults: a
      }).then(function(v) {
        var y = v.numFailures, T = v.results, C = v.lastResult, A = v.failures, I = a ? T : C;
        if (y === 0) return I;
        throw new Wl("".concat(o.name, ".bulkPut(): ").concat(y, " of ").concat(f, " operations failed"), A);
      });
    });
  }, e.prototype.bulkUpdate = function(t) {
    var n = this, r = this.core, o = t.map(function(i) {
      return i.key;
    }), s = t.map(function(i) {
      return i.changes;
    }), a = [];
    return this._trans("readwrite", function(i) {
      return r.getMany({
        trans: i,
        keys: o,
        cache: "clone"
      }).then(function(l) {
        var u = [], d = [];
        t.forEach(function(m, v) {
          var y = m.key, T = m.changes, C = l[v];
          if (C) {
            for (var A = 0, I = Object.keys(T); A < I.length; A++) {
              var R = I[A], $ = T[R];
              if (R === n.schema.primKey.keyPath) {
                if (Wt($, y) !== 0) throw new vt.Constraint("Cannot update primary key in bulkUpdate()");
              } else Zr(C, R, $);
            }
            a.push(v), u.push(y), d.push(C);
          }
        });
        var f = u.length;
        return r.mutate({
          trans: i,
          type: "put",
          keys: u,
          values: d,
          updates: {
            keys: o,
            changeSpecs: s
          }
        }).then(function(m) {
          var v = m.numFailures, y = m.failures;
          if (v === 0) return f;
          for (var T = 0, C = Object.keys(y); T < C.length; T++) {
            var A = C[T], I = a[Number(A)];
            if (I != null) {
              var R = y[A];
              delete y[A], y[I] = R;
            }
          }
          throw new Wl("".concat(n.name, ".bulkUpdate(): ").concat(v, " of ").concat(f, " operations failed"), y);
        });
      });
    });
  }, e.prototype.bulkDelete = function(t) {
    var n = this, r = t.length;
    return this._trans("readwrite", function(o) {
      return n.core.mutate({
        trans: o,
        type: "delete",
        keys: t
      });
    }).then(function(o) {
      var s = o.numFailures, a = o.lastResult, i = o.failures;
      if (s === 0) return a;
      throw new Wl("".concat(n.name, ".bulkDelete(): ").concat(s, " of ").concat(r, " operations failed"), i);
    });
  }, e;
})();
function of(e) {
  var t = {}, n = function(i, l) {
    if (l) {
      for (var u = arguments.length, d = new Array(u - 1); --u; ) d[u - 1] = arguments[u];
      return t[i].subscribe.apply(null, d), e;
    } else if (typeof i == "string") return t[i];
  };
  n.addEventType = s;
  for (var r = 1, o = arguments.length; r < o; ++r) s(arguments[r]);
  return n;
  function s(i, l, u) {
    if (typeof i == "object") return a(i);
    l || (l = CL), u || (u = rn);
    var d = {
      subscribers: [],
      fire: u,
      subscribe: function(f) {
        d.subscribers.indexOf(f) === -1 && (d.subscribers.push(f), d.fire = l(d.fire, f));
      },
      unsubscribe: function(f) {
        d.subscribers = d.subscribers.filter(function(m) {
          return m !== f;
        }), d.fire = d.subscribers.reduce(l, u);
      }
    };
    return t[i] = n[i] = d, d;
  }
  function a(i) {
    Wn(i).forEach(function(l) {
      var u = i[l];
      if (vn(u)) s(l, i[l][0], i[l][1]);
      else if (u === "asap") var d = s(l, rf, function() {
        for (var m = arguments.length, v = new Array(m); m--; ) v[m] = arguments[m];
        d.subscribers.forEach(function(y) {
          OI(function() {
            y.apply(null, v);
          });
        });
      });
      else throw new vt.InvalidArgument("Invalid event config");
    });
  }
}
function sf(e, t) {
  return Mu(t).from({ prototype: e }), t;
}
function HL(e) {
  return sf(ZI.prototype, function(n, r, o) {
    this.db = e, this._tx = o, this.name = n, this.schema = r, this.hook = e._allTables[n] ? e._allTables[n].hook : of(null, {
      creating: [TL, rn],
      reading: [SL, rf],
      updating: [AL, rn],
      deleting: [EL, rn]
    });
  });
}
function bl(e, t) {
  return !(e.filter || e.algorithm || e.or) && (t ? e.justLimit : !e.replayFilter);
}
function ty(e, t) {
  e.filter = Wi(e.filter, t);
}
function ny(e, t, n) {
  var r = e.replayFilter;
  e.replayFilter = r ? function() {
    return Wi(r(), t());
  } : t, e.justLimit = n && !r;
}
function zL(e, t) {
  e.isMatch = Wi(e.isMatch, t);
}
function ap(e, t) {
  if (e.isPrimKey) return t.primaryKey;
  var n = t.getIndexByKeyPath(e.index);
  if (!n) throw new vt.Schema("KeyPath " + e.index + " on object store " + t.name + " is not indexed");
  return n;
}
function F2(e, t, n) {
  var r = ap(e, t.schema);
  return t.openCursor({
    trans: n,
    values: !e.keysOnly,
    reverse: e.dir === "prev",
    unique: !!e.unique,
    query: {
      index: r,
      range: e.range
    }
  });
}
function ah(e, t, n, r) {
  var o = e.replayFilter ? Wi(e.filter, e.replayFilter()) : e.filter;
  if (e.or) {
    var s = {}, a = function(i, l, u) {
      if (!o || o(l, u, function(m) {
        return l.stop(m);
      }, function(m) {
        return l.fail(m);
      })) {
        var d = l.primaryKey, f = "" + d;
        f === "[object ArrayBuffer]" && (f = "" + new Uint8Array(d)), xr(s, f) || (s[f] = !0, t(i, l, u));
      }
    };
    return Promise.all([e.or._iterate(a, n), B2(F2(e, r, n), e.algorithm, a, !e.keysOnly && e.valueMapper)]);
  } else
    return B2(F2(e, r, n), Wi(e.algorithm, o), t, !e.keysOnly && e.valueMapper);
}
function B2(e, t, n, r) {
  var o = fn(r ? function(s, a, i) {
    return n(r(s), a, i);
  } : n);
  return e.then(function(s) {
    if (s) return s.start(function() {
      var a = function() {
        return s.continue();
      };
      (!t || t(s, function(i) {
        return a = i;
      }, function(i) {
        s.stop(i), a = rn;
      }, function(i) {
        s.fail(i), a = rn;
      })) && o(s.value, s, function(i) {
        return a = i;
      }), a();
    });
  });
}
var WL = (function() {
  function e(t) {
    Object.assign(this, t);
  }
  return e.prototype.execute = function(t) {
    var n;
    if (this.add !== void 0) {
      var r = this.add;
      if (vn(r)) return zp(zp([], vn(t) ? t : [], !0), r, !0).sort();
      if (typeof r == "number") return (Number(t) || 0) + r;
      if (typeof r == "bigint") try {
        return BigInt(t) + r;
      } catch {
        return BigInt(0) + r;
      }
      throw new TypeError("Invalid term ".concat(r));
    }
    if (this.remove !== void 0) {
      var o = this.remove;
      if (vn(o)) return vn(t) ? t.filter(function(a) {
        return !o.includes(a);
      }).sort() : [];
      if (typeof o == "number") return Number(t) - o;
      if (typeof o == "bigint") try {
        return BigInt(t) - o;
      } catch {
        return BigInt(0) - o;
      }
      throw new TypeError("Invalid subtrahend ".concat(o));
    }
    var s = (n = this.replacePrefix) === null || n === void 0 ? void 0 : n[0];
    return s && typeof t == "string" && t.startsWith(s) ? this.replacePrefix[1] + t.substring(s.length) : t;
  }, e;
})(), GL = (function() {
  function e() {
  }
  return e.prototype._read = function(t, n) {
    var r = this._ctx;
    return r.error ? r.table._trans(null, An.bind(null, r.error)) : r.table._trans("readonly", t).then(n);
  }, e.prototype._write = function(t) {
    var n = this._ctx;
    return n.error ? n.table._trans(null, An.bind(null, n.error)) : n.table._trans("readwrite", t, "locked");
  }, e.prototype._addAlgorithm = function(t) {
    var n = this._ctx;
    n.algorithm = Wi(n.algorithm, t);
  }, e.prototype._iterate = function(t, n) {
    return ah(this._ctx, t, n, this._ctx.table.core);
  }, e.prototype.clone = function(t) {
    var n = Object.create(this.constructor.prototype), r = Object.create(this._ctx);
    return t && ro(r, t), n._ctx = r, n;
  }, e.prototype.raw = function() {
    return this._ctx.valueMapper = null, this;
  }, e.prototype.each = function(t) {
    var n = this._ctx;
    return this._read(function(r) {
      return ah(n, t, r, n.table.core);
    });
  }, e.prototype.count = function(t) {
    var n = this;
    return this._read(function(r) {
      var o = n._ctx, s = o.table.core;
      if (bl(o, !0)) return s.count({
        trans: r,
        query: {
          index: ap(o, s.schema),
          range: o.range
        }
      }).then(function(i) {
        return Math.min(i, o.limit);
      });
      var a = 0;
      return ah(o, function() {
        return ++a, !1;
      }, r, s).then(function() {
        return a;
      });
    }).then(t);
  }, e.prototype.sortBy = function(t, n) {
    var r = t.split(".").reverse(), o = r[0], s = r.length - 1;
    function a(u, d) {
      return d ? a(u[r[d]], d - 1) : u[o];
    }
    var i = this._ctx.dir === "next" ? 1 : -1;
    function l(u, d) {
      return Wt(a(u, s), a(d, s)) * i;
    }
    return this.toArray(function(u) {
      return u.sort(l);
    }).then(n);
  }, e.prototype.toArray = function(t) {
    var n = this;
    return this._read(function(r) {
      var o = n._ctx;
      if (o.dir === "next" && bl(o, !0) && o.limit > 0) {
        var s = o.valueMapper, a = ap(o, o.table.core.schema);
        return o.table.core.query({
          trans: r,
          limit: o.limit,
          values: !0,
          query: {
            index: a,
            range: o.range
          }
        }).then(function(l) {
          var u = l.result;
          return s ? u.map(s) : u;
        });
      } else {
        var i = [];
        return ah(o, function(l) {
          return i.push(l);
        }, r, o.table.core).then(function() {
          return i;
        });
      }
    }, t);
  }, e.prototype.offset = function(t) {
    var n = this._ctx;
    return t <= 0 ? this : (n.offset += t, bl(n) ? ny(n, function() {
      var r = t;
      return function(o, s) {
        return r === 0 ? !0 : r === 1 ? (--r, !1) : (s(function() {
          o.advance(r), r = 0;
        }), !1);
      };
    }) : ny(n, function() {
      var r = t;
      return function() {
        return --r < 0;
      };
    }), this);
  }, e.prototype.limit = function(t) {
    return this._ctx.limit = Math.min(this._ctx.limit, t), ny(this._ctx, function() {
      var n = t;
      return function(r, o, s) {
        return --n <= 0 && o(s), n >= 0;
      };
    }, !0), this;
  }, e.prototype.until = function(t, n) {
    return ty(this._ctx, function(r, o, s) {
      return t(r.value) ? (o(s), n) : !0;
    }), this;
  }, e.prototype.first = function(t) {
    return this.limit(1).toArray(function(n) {
      return n[0];
    }).then(t);
  }, e.prototype.last = function(t) {
    return this.reverse().first(t);
  }, e.prototype.filter = function(t) {
    return ty(this._ctx, function(n) {
      return t(n.value);
    }), zL(this._ctx, t), this;
  }, e.prototype.and = function(t) {
    return this.filter(t);
  }, e.prototype.or = function(t) {
    return new this.db.WhereClause(this._ctx.table, t, this);
  }, e.prototype.reverse = function() {
    return this._ctx.dir = this._ctx.dir === "prev" ? "next" : "prev", this._ondirectionchange && this._ondirectionchange(this._ctx.dir), this;
  }, e.prototype.desc = function() {
    return this.reverse();
  }, e.prototype.eachKey = function(t) {
    var n = this._ctx;
    return n.keysOnly = !n.isMatch, this.each(function(r, o) {
      t(o.key, o);
    });
  }, e.prototype.eachUniqueKey = function(t) {
    return this._ctx.unique = "unique", this.eachKey(t);
  }, e.prototype.eachPrimaryKey = function(t) {
    var n = this._ctx;
    return n.keysOnly = !n.isMatch, this.each(function(r, o) {
      t(o.primaryKey, o);
    });
  }, e.prototype.keys = function(t) {
    var n = this._ctx;
    n.keysOnly = !n.isMatch;
    var r = [];
    return this.each(function(o, s) {
      r.push(s.key);
    }).then(function() {
      return r;
    }).then(t);
  }, e.prototype.primaryKeys = function(t) {
    var n = this._ctx;
    if (n.dir === "next" && bl(n, !0) && n.limit > 0) return this._read(function(o) {
      var s = ap(n, n.table.core.schema);
      return n.table.core.query({
        trans: o,
        values: !1,
        limit: n.limit,
        query: {
          index: s,
          range: n.range
        }
      });
    }).then(function(o) {
      return o.result;
    }).then(t);
    n.keysOnly = !n.isMatch;
    var r = [];
    return this.each(function(o, s) {
      r.push(s.primaryKey);
    }).then(function() {
      return r;
    }).then(t);
  }, e.prototype.uniqueKeys = function(t) {
    return this._ctx.unique = "unique", this.keys(t);
  }, e.prototype.firstKey = function(t) {
    return this.limit(1).keys(function(n) {
      return n[0];
    }).then(t);
  }, e.prototype.lastKey = function(t) {
    return this.reverse().firstKey(t);
  }, e.prototype.distinct = function() {
    var t = this._ctx, n = t.index && t.table.schema.idxByName[t.index];
    if (!n || !n.multi) return this;
    var r = {};
    return ty(this._ctx, function(o) {
      var s = o.primaryKey.toString(), a = xr(r, s);
      return r[s] = !0, !a;
    }), this;
  }, e.prototype.modify = function(t) {
    var n = this, r = this._ctx;
    return this._write(function(o) {
      var s;
      if (typeof t == "function") s = t;
      else {
        var a = Wn(t), i = a.length;
        s = function(I) {
          for (var R = !1, $ = 0; $ < i; ++$) {
            var p = a[$], b = t[p], w = Js(I, p);
            b instanceof WL ? (Zr(I, p, b.execute(w)), R = !0) : w !== b && (Zr(I, p, b), R = !0);
          }
          return R;
        };
      }
      var l = r.table.core, u = l.schema.primaryKey, d = u.outbound, f = u.extractKey, m = 200, v = n.db._options.modifyChunkSize;
      v && (typeof v == "object" ? m = v[l.name] || v["*"] || 200 : m = v);
      var y = [], T = 0, C = [], A = function(I, R) {
        var $ = R.failures, p = R.numFailures;
        T += I - p;
        for (var b = 0, w = Wn($); b < w.length; b++) {
          var E = w[b];
          y.push($[E]);
        }
      };
      return n.clone().primaryKeys().then(function(I) {
        var R = bl(r) && r.limit === 1 / 0 && (typeof t != "function" || t === ry) && {
          index: r.index,
          range: r.range
        }, $ = function(p) {
          var b = Math.min(m, I.length - p);
          return l.getMany({
            trans: o,
            keys: I.slice(p, p + b),
            cache: "immutable"
          }).then(function(w) {
            for (var E = [], x = [], M = d ? [] : null, N = [], q = 0; q < b; ++q) {
              var z = w[q], J = {
                value: qi(z),
                primKey: I[p + q]
              };
              s.call(J, J.value, J) !== !1 && (J.value == null ? N.push(I[p + q]) : !d && Wt(f(z), f(J.value)) !== 0 ? (N.push(I[p + q]), E.push(J.value)) : (x.push(J.value), d && M.push(I[p + q])));
            }
            return Promise.resolve(E.length > 0 && l.mutate({
              trans: o,
              type: "add",
              values: E
            }).then(function(G) {
              for (var oe in G.failures) N.splice(parseInt(oe), 1);
              A(E.length, G);
            })).then(function() {
              return (x.length > 0 || R && typeof t == "object") && l.mutate({
                trans: o,
                type: "put",
                keys: M,
                values: x,
                criteria: R,
                changeSpec: typeof t != "function" && t,
                isAdditionalChunk: p > 0
              }).then(function(G) {
                return A(x.length, G);
              });
            }).then(function() {
              return (N.length > 0 || R && t === ry) && l.mutate({
                trans: o,
                type: "delete",
                keys: N,
                criteria: R,
                isAdditionalChunk: p > 0
              }).then(function(G) {
                return A(N.length, G);
              });
            }).then(function() {
              return I.length > p + b && $(p + m);
            });
          });
        };
        return $(0).then(function() {
          if (y.length > 0) throw new Wp("Error modifying one or more objects", y, T, C);
          return I.length;
        });
      });
    });
  }, e.prototype.delete = function() {
    var t = this._ctx, n = t.range;
    return bl(t) && (t.isPrimKey || n.type === 3) ? this._write(function(r) {
      var o = t.table.core.schema.primaryKey, s = n;
      return t.table.core.count({
        trans: r,
        query: {
          index: o,
          range: s
        }
      }).then(function(a) {
        return t.table.core.mutate({
          trans: r,
          type: "deleteRange",
          range: s
        }).then(function(i) {
          var l = i.failures;
          i.lastResult, i.results;
          var u = i.numFailures;
          if (u) throw new Wp("Could not delete some values", Object.keys(l).map(function(d) {
            return l[d];
          }), a - u);
          return a - u;
        });
      });
    }) : this.modify(ry);
  }, e;
})(), ry = function(e, t) {
  return t.value = null;
};
function VL(e) {
  return sf(GL.prototype, function(n, r) {
    this.db = e;
    var o = QI, s = null;
    if (r) try {
      o = r();
    } catch (u) {
      s = u;
    }
    var a = n._ctx, i = a.table, l = i.hook.reading.fire;
    this._ctx = {
      table: i,
      index: a.index,
      isPrimKey: !a.index || i.schema.primKey.keyPath && a.index === i.schema.primKey.name,
      range: o,
      keysOnly: !1,
      dir: "next",
      unique: "",
      algorithm: null,
      filter: null,
      replayFilter: null,
      justLimit: !0,
      isMatch: null,
      offset: 0,
      limit: 1 / 0,
      error: s,
      or: a.or,
      valueMapper: l !== rf ? l : null
    };
  });
}
function KL(e, t) {
  return e < t ? -1 : e === t ? 0 : 1;
}
function JL(e, t) {
  return e > t ? -1 : e === t ? 0 : 1;
}
function Br(e, t, n) {
  var r = e instanceof ex ? new e.Collection(e) : e;
  return r._ctx.error = n ? new n(t) : new TypeError(t), r;
}
function wl(e) {
  return new e.Collection(e, function() {
    return jI("");
  }).limit(0);
}
function YL(e) {
  return e === "next" ? function(t) {
    return t.toUpperCase();
  } : function(t) {
    return t.toLowerCase();
  };
}
function XL(e) {
  return e === "next" ? function(t) {
    return t.toLowerCase();
  } : function(t) {
    return t.toUpperCase();
  };
}
function QL(e, t, n, r, o, s) {
  for (var a = Math.min(e.length, r.length), i = -1, l = 0; l < a; ++l) {
    var u = t[l];
    if (u !== r[l])
      return o(e[l], n[l]) < 0 ? e.substr(0, l) + n[l] + n.substr(l + 1) : o(e[l], r[l]) < 0 ? e.substr(0, l) + r[l] + n.substr(l + 1) : i >= 0 ? e.substr(0, i) + t[i] + n.substr(i + 1) : null;
    o(e[l], u) < 0 && (i = l);
  }
  return a < r.length && s === "next" ? e + n.substr(e.length) : a < e.length && s === "prev" ? e.substr(0, n.length) : i < 0 ? null : e.substr(0, i) + r[i] + n.substr(i + 1);
}
function ih(e, t, n, r) {
  var o, s, a, i, l, u, d, f = n.length;
  if (!n.every(function(T) {
    return typeof T == "string";
  })) return Br(e, XI);
  function m(T) {
    o = YL(T), s = XL(T), a = T === "next" ? KL : JL;
    var C = n.map(function(A) {
      return {
        lower: s(A),
        upper: o(A)
      };
    }).sort(function(A, I) {
      return a(A.lower, I.lower);
    });
    i = C.map(function(A) {
      return A.upper;
    }), l = C.map(function(A) {
      return A.lower;
    }), u = T, d = T === "next" ? "" : r;
  }
  m("next");
  var v = new e.Collection(e, function() {
    return ga(i[0], l[f - 1] + r);
  });
  v._ondirectionchange = function(T) {
    m(T);
  };
  var y = 0;
  return v._addAlgorithm(function(T, C, A) {
    var I = T.key;
    if (typeof I != "string") return !1;
    var R = s(I);
    if (t(R, l, y)) return !0;
    for (var $ = null, p = y; p < f; ++p) {
      var b = QL(I, R, i[p], l[p], a, u);
      b === null && $ === null ? y = p + 1 : ($ === null || a($, b) > 0) && ($ = b);
    }
    return C($ !== null ? function() {
      T.continue($ + d);
    } : A), !1;
  }), v;
}
function ga(e, t, n, r) {
  return {
    type: 2,
    lower: e,
    upper: t,
    lowerOpen: n,
    upperOpen: r
  };
}
function jI(e) {
  return {
    type: 1,
    lower: e,
    upper: e
  };
}
var ex = (function() {
  function e() {
  }
  return Object.defineProperty(e.prototype, "Collection", {
    get: function() {
      return this._ctx.table.db.Collection;
    },
    enumerable: !1,
    configurable: !0
  }), e.prototype.between = function(t, n, r, o) {
    r = r !== !1, o = o === !0;
    try {
      return this._cmp(t, n) > 0 || this._cmp(t, n) === 0 && (r || o) && !(r && o) ? wl(this) : new this.Collection(this, function() {
        return ga(t, n, !r, !o);
      });
    } catch {
      return Br(this, Cs);
    }
  }, e.prototype.equals = function(t) {
    return t == null ? Br(this, Cs) : new this.Collection(this, function() {
      return jI(t);
    });
  }, e.prototype.above = function(t) {
    return t == null ? Br(this, Cs) : new this.Collection(this, function() {
      return ga(t, void 0, !0);
    });
  }, e.prototype.aboveOrEqual = function(t) {
    return t == null ? Br(this, Cs) : new this.Collection(this, function() {
      return ga(t, void 0, !1);
    });
  }, e.prototype.below = function(t) {
    return t == null ? Br(this, Cs) : new this.Collection(this, function() {
      return ga(void 0, t, !1, !0);
    });
  }, e.prototype.belowOrEqual = function(t) {
    return t == null ? Br(this, Cs) : new this.Collection(this, function() {
      return ga(void 0, t);
    });
  }, e.prototype.startsWith = function(t) {
    return typeof t != "string" ? Br(this, XI) : this.between(t, t + Ii, !0, !0);
  }, e.prototype.startsWithIgnoreCase = function(t) {
    return t === "" ? this.startsWith(t) : ih(this, function(n, r) {
      return n.indexOf(r[0]) === 0;
    }, [t], Ii);
  }, e.prototype.equalsIgnoreCase = function(t) {
    return ih(this, function(n, r) {
      return n === r[0];
    }, [t], "");
  }, e.prototype.anyOfIgnoreCase = function() {
    var t = Us.apply(Dl, arguments);
    return t.length === 0 ? wl(this) : ih(this, function(n, r) {
      return r.indexOf(n) !== -1;
    }, t, "");
  }, e.prototype.startsWithAnyOfIgnoreCase = function() {
    var t = Us.apply(Dl, arguments);
    return t.length === 0 ? wl(this) : ih(this, function(n, r) {
      return r.some(function(o) {
        return n.indexOf(o) === 0;
      });
    }, t, Ii);
  }, e.prototype.anyOf = function() {
    var t = this, n = Us.apply(Dl, arguments), r = this._cmp;
    try {
      n.sort(r);
    } catch {
      return Br(this, Cs);
    }
    if (n.length === 0) return wl(this);
    var o = new this.Collection(this, function() {
      return ga(n[0], n[n.length - 1]);
    });
    o._ondirectionchange = function(a) {
      r = a === "next" ? t._ascending : t._descending, n.sort(r);
    };
    var s = 0;
    return o._addAlgorithm(function(a, i, l) {
      for (var u = a.key; r(u, n[s]) > 0; )
        if (++s, s === n.length)
          return i(l), !1;
      return r(u, n[s]) === 0 ? !0 : (i(function() {
        a.continue(n[s]);
      }), !1);
    }), o;
  }, e.prototype.notEqual = function(t) {
    return this.inAnyRange([[C_, t], [t, this.db._maxKey]], {
      includeLowers: !1,
      includeUppers: !1
    });
  }, e.prototype.noneOf = function() {
    var t = Us.apply(Dl, arguments);
    if (t.length === 0) return new this.Collection(this);
    try {
      t.sort(this._ascending);
    } catch {
      return Br(this, Cs);
    }
    var n = t.reduce(function(r, o) {
      return r ? r.concat([[r[r.length - 1][1], o]]) : [[C_, o]];
    }, null);
    return n.push([t[t.length - 1], this.db._maxKey]), this.inAnyRange(n, {
      includeLowers: !1,
      includeUppers: !1
    });
  }, e.prototype.inAnyRange = function(t, n) {
    var r = this, o = this._cmp, s = this._ascending, a = this._descending, i = this._min, l = this._max;
    if (t.length === 0) return wl(this);
    if (!t.every(function(p) {
      return p[0] !== void 0 && p[1] !== void 0 && s(p[0], p[1]) <= 0;
    })) return Br(this, "First argument to inAnyRange() must be an Array of two-value Arrays [lower,upper] where upper must not be lower than lower", vt.InvalidArgument);
    var u = !n || n.includeLowers !== !1, d = n && n.includeUppers === !0;
    function f(p, b) {
      for (var w = 0, E = p.length; w < E; ++w) {
        var x = p[w];
        if (o(b[0], x[1]) < 0 && o(b[1], x[0]) > 0) {
          x[0] = i(x[0], b[0]), x[1] = l(x[1], b[1]);
          break;
        }
      }
      return w === E && p.push(b), p;
    }
    var m = s;
    function v(p, b) {
      return m(p[0], b[0]);
    }
    var y;
    try {
      y = t.reduce(f, []), y.sort(v);
    } catch {
      return Br(this, Cs);
    }
    var T = 0, C = d ? function(p) {
      return s(p, y[T][1]) > 0;
    } : function(p) {
      return s(p, y[T][1]) >= 0;
    }, A = u ? function(p) {
      return a(p, y[T][0]) > 0;
    } : function(p) {
      return a(p, y[T][0]) >= 0;
    };
    function I(p) {
      return !C(p) && !A(p);
    }
    var R = C, $ = new this.Collection(this, function() {
      return ga(y[0][0], y[y.length - 1][1], !u, !d);
    });
    return $._ondirectionchange = function(p) {
      p === "next" ? (R = C, m = s) : (R = A, m = a), y.sort(v);
    }, $._addAlgorithm(function(p, b, w) {
      for (var E = p.key; R(E); )
        if (++T, T === y.length)
          return b(w), !1;
      return I(E) ? !0 : (r._cmp(E, y[T][1]) === 0 || r._cmp(E, y[T][0]) === 0 || b(function() {
        m === s ? p.continue(y[T][0]) : p.continue(y[T][1]);
      }), !1);
    }), $;
  }, e.prototype.startsWithAnyOf = function() {
    var t = Us.apply(Dl, arguments);
    return t.every(function(n) {
      return typeof n == "string";
    }) ? t.length === 0 ? wl(this) : this.inAnyRange(t.map(function(n) {
      return [n, n + Ii];
    })) : Br(this, "startsWithAnyOf() only works with strings");
  }, e;
})();
function ZL(e) {
  return sf(ex.prototype, function(n, r, o) {
    if (this.db = e, this._ctx = {
      table: n,
      index: r === ":id" ? null : r,
      or: o
    }, this._cmp = this._ascending = Wt, this._descending = function(s, a) {
      return Wt(a, s);
    }, this._max = function(s, a) {
      return Wt(s, a) > 0 ? s : a;
    }, this._min = function(s, a) {
      return Wt(s, a) < 0 ? s : a;
    }, this._IDBKeyRange = e._deps.IDBKeyRange, !this._IDBKeyRange) throw new vt.MissingAPI();
  });
}
function jo(e) {
  return fn(function(t) {
    return Dd(t), e(t.target.error), !1;
  });
}
function Dd(e) {
  e.stopPropagation && e.stopPropagation(), e.preventDefault && e.preventDefault();
}
var af = "storagemutated", k_ = "x-storagemutated-1", qa = of(null, af), jL = (function() {
  function e() {
  }
  return e.prototype._lock = function() {
    return Dc(!pt.global), ++this._reculock, this._reculock === 1 && !pt.global && (pt.lockOwnerFor = this), this;
  }, e.prototype._unlock = function() {
    if (Dc(!pt.global), --this._reculock === 0)
      for (pt.global || (pt.lockOwnerFor = null); this._blockedFuncs.length > 0 && !this._locked(); ) {
        var t = this._blockedFuncs.shift();
        try {
          zi(t[1], t[0]);
        } catch {
        }
      }
    return this;
  }, e.prototype._locked = function() {
    return this._reculock && pt.lockOwnerFor !== this;
  }, e.prototype.create = function(t) {
    var n = this;
    if (!this.mode) return this;
    var r = this.db.idbdb, o = this.db._state.dbOpenError;
    if (Dc(!this.idbtrans), !t && !r) switch (o && o.name) {
      case "DatabaseClosedError":
        throw new vt.DatabaseClosed(o);
      case "MissingAPIError":
        throw new vt.MissingAPI(o.message, o);
      default:
        throw new vt.OpenFailed(o);
    }
    if (!this.active) throw new vt.TransactionInactive();
    return Dc(this._completion._state === null), t = this.idbtrans = t || (this.db.core ? this.db.core.transaction(this.storeNames, this.mode, { durability: this.chromeTransactionDurability }) : r.transaction(this.storeNames, this.mode, { durability: this.chromeTransactionDurability })), t.onerror = fn(function(s) {
      Dd(s), n._reject(t.error);
    }), t.onabort = fn(function(s) {
      Dd(s), n.active && n._reject(new vt.Abort(t.error)), n.active = !1, n.on("abort").fire(s);
    }), t.oncomplete = fn(function() {
      n.active = !1, n._resolve(), "mutatedParts" in t && qa.storagemutated.fire(t.mutatedParts);
    }), this;
  }, e.prototype._promise = function(t, n, r) {
    var o = this;
    if (t === "readwrite" && this.mode !== "readwrite") return An(new vt.ReadOnly("Transaction is readonly"));
    if (!this.active) return An(new vt.TransactionInactive());
    if (this._locked()) return new st(function(a, i) {
      o._blockedFuncs.push([function() {
        o._promise(t, n, r).then(a, i);
      }, pt]);
    });
    if (r) return Fa(function() {
      var a = new st(function(i, l) {
        o._lock();
        var u = n(i, l, o);
        u && u.then && u.then(i, l);
      });
      return a.finally(function() {
        return o._unlock();
      }), a._lib = !0, a;
    });
    var s = new st(function(a, i) {
      var l = n(a, i, o);
      l && l.then && l.then(a, i);
    });
    return s._lib = !0, s;
  }, e.prototype._root = function() {
    return this.parent ? this.parent._root() : this;
  }, e.prototype.waitFor = function(t) {
    var n = this._root(), r = st.resolve(t);
    if (n._waitingFor) n._waitingFor = n._waitingFor.then(function() {
      return r;
    });
    else {
      n._waitingFor = r, n._waitingQueue = [];
      var o = n.idbtrans.objectStore(n.storeNames[0]);
      (function a() {
        for (++n._spinCount; n._waitingQueue.length; ) n._waitingQueue.shift()();
        n._waitingFor && (o.get(-1 / 0).onsuccess = a);
      })();
    }
    var s = n._waitingFor;
    return new st(function(a, i) {
      r.then(function(l) {
        return n._waitingQueue.push(fn(a.bind(null, l)));
      }, function(l) {
        return n._waitingQueue.push(fn(i.bind(null, l)));
      }).finally(function() {
        n._waitingFor === s && (n._waitingFor = null);
      });
    });
  }, e.prototype.abort = function() {
    this.active && (this.active = !1, this.idbtrans && this.idbtrans.abort(), this._reject(new vt.Abort()));
  }, e.prototype.table = function(t) {
    var n = this._memoizedTables || (this._memoizedTables = {});
    if (xr(n, t)) return n[t];
    var r = this.schema[t];
    if (!r) throw new vt.NotFound("Table " + t + " not part of transaction");
    var o = new this.db.Table(t, r, this);
    return o.core = this.db.core.table(t), n[t] = o, o;
  }, e;
})();
function eO(e) {
  return sf(jL.prototype, function(n, r, o, s, a) {
    var i = this;
    this.db = e, this.mode = n, this.storeNames = r, this.schema = o, this.chromeTransactionDurability = s, this.idbtrans = null, this.on = of(this, "complete", "error", "abort"), this.parent = a || null, this.active = !0, this._reculock = 0, this._blockedFuncs = [], this._resolve = null, this._reject = null, this._waitingFor = null, this._waitingQueue = null, this._spinCount = 0, this._completion = new st(function(l, u) {
      i._resolve = l, i._reject = u;
    }), this._completion.then(function() {
      i.active = !1, i.on.complete.fire();
    }, function(l) {
      var u = i.active;
      return i.active = !1, i.on.error.fire(l), i.parent ? i.parent._reject(l) : u && i.idbtrans && i.idbtrans.abort(), An(l);
    });
  });
}
function I_(e, t, n, r, o, s, a) {
  return {
    name: e,
    keyPath: t,
    unique: n,
    multi: r,
    auto: o,
    compound: s,
    src: (n && !a ? "&" : "") + (r ? "*" : "") + (o ? "++" : "") + tx(t)
  };
}
function tx(e) {
  return typeof e == "string" ? e : e ? "[" + [].join.call(e, "+") + "]" : "";
}
function j0(e, t, n) {
  return {
    name: e,
    primKey: t,
    indexes: n,
    mappedClass: null,
    idxByName: dL(n, function(r) {
      return [r.name, r];
    })
  };
}
function tO(e) {
  return e.length === 1 ? e[0] : e;
}
var Ld = function(e) {
  try {
    return e.only([[]]), Ld = function() {
      return [[]];
    }, [[]];
  } catch {
    return Ld = function() {
      return Ii;
    }, Ii;
  }
};
function x_(e) {
  return e == null ? function() {
  } : typeof e == "string" ? nO(e) : function(t) {
    return Js(t, e);
  };
}
function nO(e) {
  return e.split(".").length === 1 ? function(t) {
    return t[e];
  } : function(t) {
    return Js(t, e);
  };
}
function U2(e) {
  return [].slice.call(e);
}
var rO = 0;
function dd(e) {
  return e == null ? ":id" : typeof e == "string" ? e : "[".concat(e.join("+"), "]");
}
function oO(e, t, n) {
  function r(f, m) {
    var v = U2(f.objectStoreNames);
    return {
      schema: {
        name: f.name,
        tables: v.map(function(y) {
          return m.objectStore(y);
        }).map(function(y) {
          var T = y.keyPath, C = y.autoIncrement, A = vn(T), I = T == null, R = {}, $ = {
            name: y.name,
            primaryKey: {
              name: null,
              isPrimaryKey: !0,
              outbound: I,
              compound: A,
              keyPath: T,
              autoIncrement: C,
              unique: !0,
              extractKey: x_(T)
            },
            indexes: U2(y.indexNames).map(function(p) {
              return y.index(p);
            }).map(function(p) {
              var b = p.name, w = p.unique, E = p.multiEntry, x = p.keyPath, M = {
                name: b,
                compound: vn(x),
                keyPath: x,
                unique: w,
                multiEntry: E,
                extractKey: x_(x)
              };
              return R[dd(x)] = M, M;
            }),
            getIndexByKeyPath: function(p) {
              return R[dd(p)];
            }
          };
          return R[":id"] = $.primaryKey, T != null && (R[dd(T)] = $.primaryKey), $;
        })
      },
      hasGetAll: v.length > 0 && "getAll" in m.objectStore(v[0]) && !(typeof navigator < "u" && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604)
    };
  }
  function o(f) {
    if (f.type === 3) return null;
    if (f.type === 4) throw new Error("Cannot convert never type to IDBKeyRange");
    var m = f.lower, v = f.upper, y = f.lowerOpen, T = f.upperOpen;
    return m === void 0 ? v === void 0 ? null : t.upperBound(v, !!T) : v === void 0 ? t.lowerBound(m, !!y) : t.bound(m, v, !!y, !!T);
  }
  function s(f) {
    var m = f.name;
    function v(C) {
      var A = C.trans, I = C.type, R = C.keys, $ = C.values, p = C.range;
      return new Promise(function(b, w) {
        b = fn(b);
        var E = A.objectStore(m), x = E.keyPath == null, M = I === "put" || I === "add";
        if (!M && I !== "delete" && I !== "deleteRange") throw new Error("Invalid operation type: " + I);
        var N = (R || $ || { length: 1 }).length;
        if (R && $ && R.length !== $.length) throw new Error("Given keys array must have same length as given values array.");
        if (N === 0) return b({
          numFailures: 0,
          failures: {},
          results: [],
          lastResult: void 0
        });
        var q, z = [], J = [], G = 0, oe = function(Ce) {
          ++G, Dd(Ce);
        };
        if (I === "deleteRange") {
          if (p.type === 4) return b({
            numFailures: G,
            failures: J,
            results: [],
            lastResult: void 0
          });
          p.type === 3 ? z.push(q = E.clear()) : z.push(q = E.delete(o(p)));
        } else {
          var B = M ? x ? [$, R] : [$, null] : [R, null], X = B[0], de = B[1];
          if (M) for (var fe = 0; fe < N; ++fe)
            z.push(q = de && de[fe] !== void 0 ? E[I](X[fe], de[fe]) : E[I](X[fe])), q.onerror = oe;
          else for (var fe = 0; fe < N; ++fe)
            z.push(q = E[I](X[fe])), q.onerror = oe;
        }
        var ve = function(Ce) {
          var Ue = Ce.target.result;
          z.forEach(function(le, me) {
            return le.error != null && (J[me] = le.error);
          }), b({
            numFailures: G,
            failures: J,
            results: I === "delete" ? R : z.map(function(le) {
              return le.result;
            }),
            lastResult: Ue
          });
        };
        q.onerror = function(Ce) {
          oe(Ce), ve(Ce);
        }, q.onsuccess = ve;
      });
    }
    function y(C) {
      var A = C.trans, I = C.values, R = C.query, $ = C.reverse, p = C.unique;
      return new Promise(function(b, w) {
        b = fn(b);
        var E = R.index, x = R.range, M = A.objectStore(m), N = E.isPrimaryKey ? M : M.index(E.name), q = $ ? p ? "prevunique" : "prev" : p ? "nextunique" : "next", z = I || !("openKeyCursor" in N) ? N.openCursor(o(x), q) : N.openKeyCursor(o(x), q);
        z.onerror = jo(w), z.onsuccess = fn(function(J) {
          var G = z.result;
          if (!G) {
            b(null);
            return;
          }
          G.___id = ++rO, G.done = !1;
          var oe = G.continue.bind(G), B = G.continuePrimaryKey;
          B && (B = B.bind(G));
          var X = G.advance.bind(G), de = function() {
            throw new Error("Cursor not started");
          }, fe = function() {
            throw new Error("Cursor not stopped");
          };
          G.trans = A, G.stop = G.continue = G.continuePrimaryKey = G.advance = de, G.fail = fn(w), G.next = function() {
            var ve = this, Ce = 1;
            return this.start(function() {
              return Ce-- ? ve.continue() : ve.stop();
            }).then(function() {
              return ve;
            });
          }, G.start = function(ve) {
            var Ce = new Promise(function(le, me) {
              le = fn(le), z.onerror = jo(me), G.fail = me, G.stop = function(We) {
                G.stop = G.continue = G.continuePrimaryKey = G.advance = fe, le(We);
              };
            }), Ue = function() {
              if (z.result) try {
                ve();
              } catch (le) {
                G.fail(le);
              }
              else
                G.done = !0, G.start = function() {
                  throw new Error("Cursor behind last entry");
                }, G.stop();
            };
            return z.onsuccess = fn(function(le) {
              z.onsuccess = Ue, Ue();
            }), G.continue = oe, G.continuePrimaryKey = B, G.advance = X, Ue(), Ce;
          }, b(G);
        }, w);
      });
    }
    function T(C) {
      return function(A) {
        return new Promise(function(I, R) {
          I = fn(I);
          var $ = A.trans, p = A.values, b = A.limit, w = A.query, E = b === 1 / 0 ? void 0 : b, x = w.index, M = w.range, N = $.objectStore(m), q = x.isPrimaryKey ? N : N.index(x.name), z = o(M);
          if (b === 0) return I({ result: [] });
          if (C) {
            var J = p ? q.getAll(z, E) : q.getAllKeys(z, E);
            J.onsuccess = function(X) {
              return I({ result: X.target.result });
            }, J.onerror = jo(R);
          } else {
            var G = 0, oe = p || !("openKeyCursor" in q) ? q.openCursor(z) : q.openKeyCursor(z), B = [];
            oe.onsuccess = function(X) {
              var de = oe.result;
              if (!de) return I({ result: B });
              if (B.push(p ? de.value : de.primaryKey), ++G === b) return I({ result: B });
              de.continue();
            }, oe.onerror = jo(R);
          }
        });
      };
    }
    return {
      name: m,
      schema: f,
      mutate: v,
      getMany: function(C) {
        var A = C.trans, I = C.keys;
        return new Promise(function(R, $) {
          R = fn(R);
          for (var p = A.objectStore(m), b = I.length, w = new Array(b), E = 0, x = 0, M, N = function(J) {
            var G = J.target;
            (w[G._pos] = G.result) != null, ++x === E && R(w);
          }, q = jo($), z = 0; z < b; ++z) I[z] != null && (M = p.get(I[z]), M._pos = z, M.onsuccess = N, M.onerror = q, ++E);
          E === 0 && R(w);
        });
      },
      get: function(C) {
        var A = C.trans, I = C.key;
        return new Promise(function(R, $) {
          R = fn(R);
          var p = A.objectStore(m).get(I);
          p.onsuccess = function(b) {
            return R(b.target.result);
          }, p.onerror = jo($);
        });
      },
      query: T(l),
      openCursor: y,
      count: function(C) {
        var A = C.query, I = C.trans, R = A.index, $ = A.range;
        return new Promise(function(p, b) {
          var w = I.objectStore(m), E = R.isPrimaryKey ? w : w.index(R.name), x = o($), M = x ? E.count(x) : E.count();
          M.onsuccess = fn(function(N) {
            return p(N.target.result);
          }), M.onerror = jo(b);
        });
      }
    };
  }
  var a = r(e, n), i = a.schema, l = a.hasGetAll, u = i.tables.map(function(f) {
    return s(f);
  }), d = {};
  return u.forEach(function(f) {
    return d[f.name] = f;
  }), {
    stack: "dbcore",
    transaction: e.transaction.bind(e),
    table: function(f) {
      if (!d[f]) throw new Error("Table '".concat(f, "' not found"));
      return d[f];
    },
    MIN_KEY: -1 / 0,
    MAX_KEY: Ld(t),
    schema: i
  };
}
function sO(e, t) {
  return t.reduce(function(n, r) {
    var o = r.create;
    return Nt(Nt({}, n), o(n));
  }, e);
}
function aO(e, t, n, r) {
  var o = n.IDBKeyRange;
  return n.indexedDB, { dbcore: sO(oO(t, o, r), e.dbcore) };
}
function Jp(e, t) {
  var n = t.db;
  e.core = aO(e._middlewares, n, e._deps, t).dbcore, e.tables.forEach(function(r) {
    var o = r.name;
    e.core.schema.tables.some(function(s) {
      return s.name === o;
    }) && (r.core = e.core.table(o), e[o] instanceof e.Table && (e[o].core = r.core));
  });
}
function Yp(e, t, n, r) {
  n.forEach(function(o) {
    var s = r[o];
    t.forEach(function(a) {
      var i = DI(a, o);
      (!i || "value" in i && i.value === void 0) && (a === e.Transaction.prototype || a instanceof e.Transaction ? Oa(a, o, {
        get: function() {
          return this.table(o);
        },
        set: function(l) {
          $I(this, o, {
            value: l,
            writable: !0,
            configurable: !0,
            enumerable: !0
          });
        }
      }) : a[o] = new e.Table(o, s));
    });
  });
}
function M_(e, t) {
  t.forEach(function(n) {
    for (var r in n) n[r] instanceof e.Table && delete n[r];
  });
}
function iO(e, t) {
  return e._cfg.version - t._cfg.version;
}
function lO(e, t, n, r) {
  var o = e._dbSchema;
  n.objectStoreNames.contains("$meta") && !o.$meta && (o.$meta = j0("$meta", rx("")[0], []), e._storeNames.push("$meta"));
  var s = e._createTransaction("readwrite", e._storeNames, o);
  s.create(n), s._completion.catch(r);
  var a = s._reject.bind(s), i = pt.transless || pt;
  Fa(function() {
    if (pt.trans = s, pt.transless = i, t === 0)
      Wn(o).forEach(function(l) {
        tb(n, l, o[l].primKey, o[l].indexes);
      }), Jp(e, n), st.follow(function() {
        return e.on.populate.fire(s);
      }).catch(a);
    else
      return Jp(e, n), cO(e, s, t).then(function(l) {
        return dO(e, l, s, n);
      }).catch(a);
  });
}
function uO(e, t) {
  nx(e._dbSchema, t), t.db.version % 10 === 0 && !t.objectStoreNames.contains("$meta") && t.db.createObjectStore("$meta").add(Math.ceil(t.db.version / 10 - 1), "version");
  var n = sg(e, e.idbdb, t);
  Qp(e, e._dbSchema, t);
  for (var r = eb(n, e._dbSchema), o = function(u) {
    if (u.change.length || u.recreate)
      return console.warn("Unable to patch indexes of table ".concat(u.name, " because it has changes on the type of index or primary key.")), { value: void 0 };
    var d = t.objectStore(u.name);
    u.add.forEach(function(f) {
      ds && console.debug("Dexie upgrade patch: Creating missing index ".concat(u.name, ".").concat(f.src)), Xp(d, f);
    });
  }, s = 0, a = r.change; s < a.length; s++) {
    var i = a[s], l = o(i);
    if (typeof l == "object") return l.value;
  }
}
function cO(e, t, n) {
  return t.storeNames.includes("$meta") ? t.table("$meta").get("version").then(function(r) {
    return r ?? n;
  }) : st.resolve(n);
}
function dO(e, t, n, r) {
  var o = [], s = e._versions, a = e._dbSchema = sg(e, e.idbdb, r), i = s.filter(function(u) {
    return u._cfg.version >= t;
  });
  if (i.length === 0) return st.resolve();
  i.forEach(function(u) {
    o.push(function() {
      var d = a, f = u._cfg.dbschema;
      Qp(e, d, r), Qp(e, f, r), a = e._dbSchema = f;
      var m = eb(d, f);
      m.add.forEach(function(I) {
        tb(r, I[0], I[1].primKey, I[1].indexes);
      }), m.change.forEach(function(I) {
        if (I.recreate) throw new vt.Upgrade("Not yet support for changing primary key");
        var R = r.objectStore(I.name);
        I.add.forEach(function($) {
          return Xp(R, $);
        }), I.change.forEach(function($) {
          R.deleteIndex($.name), Xp(R, $);
        }), I.del.forEach(function($) {
          return R.deleteIndex($);
        });
      });
      var v = u._cfg.contentUpgrade;
      if (v && u._cfg.version > t) {
        Jp(e, r), n._memoizedTables = {};
        var y = FI(f);
        m.del.forEach(function(I) {
          y[I] = d[I];
        }), M_(e, [e.Transaction.prototype]), Yp(e, [e.Transaction.prototype], Wn(y), y), n.schema = y;
        var T = G0(v);
        T && $u();
        var C, A = st.follow(function() {
          if (C = v(n), C && T) {
            var I = Ba.bind(null, null);
            C.then(I, I);
          }
        });
        return C && typeof C.then == "function" ? st.resolve(C) : A.then(function() {
          return C;
        });
      }
    }), o.push(function(d) {
      var f = u._cfg.dbschema;
      fO(f, d), M_(e, [e.Transaction.prototype]), Yp(e, [e.Transaction.prototype], e._storeNames, e._dbSchema), n.schema = e._dbSchema;
    }), o.push(function(d) {
      e.idbdb.objectStoreNames.contains("$meta") && (Math.ceil(e.idbdb.version / 10) === u._cfg.version ? (e.idbdb.deleteObjectStore("$meta"), delete e._dbSchema.$meta, e._storeNames = e._storeNames.filter(function(f) {
        return f !== "$meta";
      })) : d.objectStore("$meta").put(u._cfg.version, "version"));
    });
  });
  function l() {
    return o.length ? st.resolve(o.shift()(n.idbtrans)).then(l) : st.resolve();
  }
  return l().then(function() {
    nx(a, r);
  });
}
function eb(e, t) {
  var n = {
    del: [],
    add: [],
    change: []
  }, r;
  for (r in e) t[r] || n.del.push(r);
  for (r in t) {
    var o = e[r], s = t[r];
    if (!o) n.add.push([r, s]);
    else {
      var a = {
        name: r,
        def: s,
        recreate: !1,
        del: [],
        add: [],
        change: []
      };
      if ("" + (o.primKey.keyPath || "") != "" + (s.primKey.keyPath || "") || o.primKey.auto !== s.primKey.auto)
        a.recreate = !0, n.change.push(a);
      else {
        var i = o.idxByName, l = s.idxByName, u = void 0;
        for (u in i) l[u] || a.del.push(u);
        for (u in l) {
          var d = i[u], f = l[u];
          d ? d.src !== f.src && a.change.push(f) : a.add.push(f);
        }
        (a.del.length > 0 || a.add.length > 0 || a.change.length > 0) && n.change.push(a);
      }
    }
  }
  return n;
}
function tb(e, t, n, r) {
  var o = e.db.createObjectStore(t, n.keyPath ? {
    keyPath: n.keyPath,
    autoIncrement: n.auto
  } : { autoIncrement: n.auto });
  return r.forEach(function(s) {
    return Xp(o, s);
  }), o;
}
function nx(e, t) {
  Wn(e).forEach(function(n) {
    t.db.objectStoreNames.contains(n) || (ds && console.debug("Dexie: Creating missing table", n), tb(t, n, e[n].primKey, e[n].indexes));
  });
}
function fO(e, t) {
  [].slice.call(t.db.objectStoreNames).forEach(function(n) {
    return e[n] == null && t.db.deleteObjectStore(n);
  });
}
function Xp(e, t) {
  e.createIndex(t.name, t.keyPath, {
    unique: t.unique,
    multiEntry: t.multi
  });
}
function sg(e, t, n) {
  var r = {};
  return ng(t.objectStoreNames, 0).forEach(function(o) {
    for (var s = n.objectStore(o), a = s.keyPath, i = I_(tx(a), a || "", !0, !1, !!s.autoIncrement, a && typeof a != "string", !0), l = [], u = 0; u < s.indexNames.length; ++u) {
      var d = s.index(s.indexNames[u]);
      a = d.keyPath;
      var f = I_(d.name, a, !!d.unique, !!d.multiEntry, !1, a && typeof a != "string", !1);
      l.push(f);
    }
    r[o] = j0(o, i, l);
  }), r;
}
function hO(e, t, n) {
  e.verno = t.version / 10;
  var r = e._dbSchema = sg(e, t, n);
  e._storeNames = ng(t.objectStoreNames, 0), Yp(e, [e._allTables], Wn(r), r);
}
function pO(e, t) {
  var n = eb(sg(e, e.idbdb, t), e._dbSchema);
  return !(n.add.length || n.change.some(function(r) {
    return r.add.length || r.change.length;
  }));
}
function Qp(e, t, n) {
  for (var r = n.db.objectStoreNames, o = 0; o < r.length; ++o) {
    var s = r[o], a = n.objectStore(s);
    e._hasGetAll = "getAll" in a;
    for (var i = 0; i < a.indexNames.length; ++i) {
      var l = a.indexNames[i], u = a.index(l).keyPath, d = typeof u == "string" ? u : "[" + ng(u).join("+") + "]";
      if (t[s]) {
        var f = t[s].idxByName[d];
        f && (f.name = l, delete t[s].idxByName[d], t[s].idxByName[l] = f);
      }
    }
  }
  typeof navigator < "u" && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && Xn.WorkerGlobalScope && Xn instanceof Xn.WorkerGlobalScope && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604 && (e._hasGetAll = !1);
}
function rx(e) {
  return e.split(",").map(function(t, n) {
    t = t.trim();
    var r = t.replace(/([&*]|\+\+)/g, ""), o = /^\[/.test(r) ? r.match(/^\[(.*)\]$/)[1].split("+") : r;
    return I_(r, o || null, /\&/.test(t), /\*/.test(t), /\+\+/.test(t), vn(o), n === 0);
  });
}
var mO = (function() {
  function e() {
  }
  return e.prototype._parseStoresSpec = function(t, n) {
    Wn(t).forEach(function(r) {
      if (t[r] !== null) {
        var o = rx(t[r]), s = o.shift();
        if (s.unique = !0, s.multi) throw new vt.Schema("Primary key cannot be multi-valued");
        o.forEach(function(a) {
          if (a.auto) throw new vt.Schema("Only primary key can be marked as autoIncrement (++)");
          if (!a.keyPath) throw new vt.Schema("Index must have a name and cannot be an empty string");
        }), n[r] = j0(r, s, o);
      }
    });
  }, e.prototype.stores = function(t) {
    var n = this.db;
    this._cfg.storesSource = this._cfg.storesSource ? ro(this._cfg.storesSource, t) : t;
    var r = n._versions, o = {}, s = {};
    return r.forEach(function(a) {
      ro(o, a._cfg.storesSource), s = a._cfg.dbschema = {}, a._parseStoresSpec(o, s);
    }), n._dbSchema = s, M_(n, [
      n._allTables,
      n,
      n.Transaction.prototype
    ]), Yp(n, [
      n._allTables,
      n,
      n.Transaction.prototype,
      this._cfg.tables
    ], Wn(s), s), n._storeNames = Wn(s), this;
  }, e.prototype.upgrade = function(t) {
    return this._cfg.contentUpgrade = J0(this._cfg.contentUpgrade || rn, t), this;
  }, e;
})();
function gO(e) {
  return sf(mO.prototype, function(n) {
    this.db = e, this._cfg = {
      version: n,
      storesSource: null,
      dbschema: {},
      tables: {},
      contentUpgrade: null
    };
  });
}
function nb(e, t) {
  var n = e._dbNamesDB;
  return n || (n = e._dbNamesDB = new gu(og, {
    addons: [],
    indexedDB: e,
    IDBKeyRange: t
  }), n.version(1).stores({ dbnames: "name" })), n.table("dbnames");
}
function rb(e) {
  return e && typeof e.databases == "function";
}
function vO(e) {
  var t = e.indexedDB, n = e.IDBKeyRange;
  return rb(t) ? Promise.resolve(t.databases()).then(function(r) {
    return r.map(function(o) {
      return o.name;
    }).filter(function(o) {
      return o !== og;
    });
  }) : nb(t, n).toCollection().primaryKeys();
}
function yO(e, t) {
  var n = e.indexedDB, r = e.IDBKeyRange;
  !rb(n) && t !== og && nb(n, r).put({ name: t }).catch(rn);
}
function _O(e, t) {
  var n = e.indexedDB, r = e.IDBKeyRange;
  !rb(n) && t !== og && nb(n, r).delete(t).catch(rn);
}
function P_(e) {
  return Fa(function() {
    return pt.letThrough = !0, e();
  });
}
function bO() {
  if (!(!navigator.userAgentData && /Safari\//.test(navigator.userAgent) && !/Chrom(e|ium)\//.test(navigator.userAgent)) || !indexedDB.databases) return Promise.resolve();
  var e;
  return new Promise(function(t) {
    var n = function() {
      return indexedDB.databases().finally(t);
    };
    e = setInterval(n, 100), n();
  }).finally(function() {
    return clearInterval(e);
  });
}
var oy;
function ob(e) {
  return !("from" in e);
}
var gr = function(e, t) {
  if (this) ro(this, arguments.length ? {
    d: 1,
    from: e,
    to: arguments.length > 1 ? t : e
  } : { d: 0 });
  else {
    var n = new gr();
    return e && "d" in e && ro(n, e), n;
  }
};
mu(gr.prototype, (oy = {
  add: function(e) {
    return Zp(this, e), this;
  },
  addKey: function(e) {
    return Od(this, e, e), this;
  },
  addKeys: function(e) {
    var t = this;
    return e.forEach(function(n) {
      return Od(t, n, n);
    }), this;
  },
  hasKey: function(e) {
    var t = jp(this).next(e).value;
    return t && Wt(t.from, e) <= 0 && Wt(t.to, e) >= 0;
  }
}, oy[b_] = function() {
  return jp(this);
}, oy));
function Od(e, t, n) {
  var r = Wt(t, n);
  if (!isNaN(r)) {
    if (r > 0) throw RangeError();
    if (ob(e)) return ro(e, {
      from: t,
      to: n,
      d: 1
    });
    var o = e.l, s = e.r;
    if (Wt(n, e.from) < 0)
      return o ? Od(o, t, n) : e.l = {
        from: t,
        to: n,
        d: 1,
        l: null,
        r: null
      }, q2(e);
    if (Wt(t, e.to) > 0)
      return s ? Od(s, t, n) : e.r = {
        from: t,
        to: n,
        d: 1,
        l: null,
        r: null
      }, q2(e);
    Wt(t, e.from) < 0 && (e.from = t, e.l = null, e.d = s ? s.d + 1 : 1), Wt(n, e.to) > 0 && (e.to = n, e.r = null, e.d = e.l ? e.l.d + 1 : 1);
    var a = !e.r;
    o && !e.l && Zp(e, o), s && a && Zp(e, s);
  }
}
function Zp(e, t) {
  function n(r, o) {
    var s = o.from, a = o.to, i = o.l, l = o.r;
    Od(r, s, a), i && n(r, i), l && n(r, l);
  }
  ob(t) || n(e, t);
}
function wO(e, t) {
  var n = jp(t), r = n.next();
  if (r.done) return !1;
  for (var o = r.value, s = jp(e), a = s.next(o.from), i = a.value; !r.done && !a.done; ) {
    if (Wt(i.from, o.to) <= 0 && Wt(i.to, o.from) >= 0) return !0;
    Wt(o.from, i.from) < 0 ? o = (r = n.next(i.from)).value : i = (a = s.next(o.from)).value;
  }
  return !1;
}
function jp(e) {
  var t = ob(e) ? null : {
    s: 0,
    n: e
  };
  return { next: function(n) {
    for (var r = arguments.length > 0; t; ) switch (t.s) {
      case 0:
        if (t.s = 1, r) for (; t.n.l && Wt(n, t.n.from) < 0; ) t = {
          up: t,
          n: t.n.l,
          s: 1
        };
        else for (; t.n.l; ) t = {
          up: t,
          n: t.n.l,
          s: 1
        };
      case 1:
        if (t.s = 2, !r || Wt(n, t.n.to) <= 0) return {
          value: t.n,
          done: !1
        };
      case 2:
        if (t.n.r) {
          t.s = 3, t = {
            up: t,
            n: t.n.r,
            s: 0
          };
          continue;
        }
      case 3:
        t = t.up;
    }
    return { done: !0 };
  } };
}
function q2(e) {
  var t, n, r = (((t = e.r) === null || t === void 0 ? void 0 : t.d) || 0) - (((n = e.l) === null || n === void 0 ? void 0 : n.d) || 0), o = r > 1 ? "r" : r < -1 ? "l" : "";
  if (o) {
    var s = o === "r" ? "l" : "r", a = Nt({}, e), i = e[o];
    e.from = i.from, e.to = i.to, e[o] = i[o], a[o] = i[s], e[s] = a, a.d = H2(a);
  }
  e.d = H2(e);
}
function H2(e) {
  var t = e.r, n = e.l;
  return (t ? n ? Math.max(t.d, n.d) : t.d : n ? n.d : 0) + 1;
}
function ag(e, t) {
  return Wn(t).forEach(function(n) {
    e[n] ? Zp(e[n], t[n]) : e[n] = qI(t[n]);
  }), e;
}
function sb(e, t) {
  return e.all || t.all || Object.keys(e).some(function(n) {
    return t[n] && wO(t[n], e[n]);
  });
}
var Oi = {}, sy = {}, ay = !1;
function lh(e, t) {
  ag(sy, e), ay || (ay = !0, setTimeout(function() {
    ay = !1;
    var n = sy;
    sy = {}, ab(n, !1);
  }, 0));
}
function ab(e, t) {
  t === void 0 && (t = !1);
  var n = /* @__PURE__ */ new Set();
  if (e.all) for (var r = 0, o = Object.values(Oi); r < o.length; r++) {
    var s = o[r];
    z2(s, e, n, t);
  }
  else for (var a in e) {
    var i = /^idb\:\/\/(.*)\/(.*)\//.exec(a);
    if (i) {
      var l = i[1], u = i[2], s = Oi["idb://".concat(l, "/").concat(u)];
      s && z2(s, e, n, t);
    }
  }
  n.forEach(function(d) {
    return d();
  });
}
function z2(e, t, n, r) {
  for (var o = [], s = 0, a = Object.entries(e.queries.query); s < a.length; s++) {
    for (var i = a[s], l = i[0], u = i[1], d = [], f = 0, m = u; f < m.length; f++) {
      var v = m[f];
      sb(t, v.obsSet) ? v.subscribers.forEach(function(A) {
        return n.add(A);
      }) : r && d.push(v);
    }
    r && o.push([l, d]);
  }
  if (r) for (var y = 0, T = o; y < T.length; y++) {
    var C = T[y], l = C[0], d = C[1];
    e.queries.query[l] = d;
  }
}
function SO(e) {
  var t = e._state, n = e._deps.indexedDB;
  if (t.isBeingOpened || e.idbdb) return t.dbReadyPromise.then(function() {
    return t.dbOpenError ? An(t.dbOpenError) : e;
  });
  t.isBeingOpened = !0, t.dbOpenError = null, t.openComplete = !1;
  var r = t.openCanceller, o = Math.round(e.verno * 10), s = !1;
  function a() {
    if (t.openCanceller !== r) throw new vt.DatabaseClosed("db.open() was cancelled");
  }
  var i = t.dbReadyResolve, l = null, u = !1, d = function() {
    return new st(function(f, m) {
      if (a(), !n) throw new vt.MissingAPI();
      var v = e.name, y = t.autoSchema || !o ? n.open(v) : n.open(v, o);
      if (!y) throw new vt.MissingAPI();
      y.onerror = jo(m), y.onblocked = fn(e._fireOnBlocked), y.onupgradeneeded = fn(function(T) {
        if (l = y.transaction, t.autoSchema && !e._options.allowEmptyDB) {
          y.onerror = Dd, l.abort(), y.result.close();
          var C = n.deleteDatabase(v);
          C.onsuccess = C.onerror = fn(function() {
            m(new vt.NoSuchDatabase("Database ".concat(v, " doesnt exist")));
          });
        } else {
          l.onerror = jo(m);
          var A = T.oldVersion > Math.pow(2, 62) ? 0 : T.oldVersion;
          u = A < 1, e.idbdb = y.result, s && uO(e, l), lO(e, A / 10, l, m);
        }
      }, m), y.onsuccess = fn(function() {
        l = null;
        var T = e.idbdb = y.result, C = ng(T.objectStoreNames);
        if (C.length > 0) try {
          var A = T.transaction(tO(C), "readonly");
          if (t.autoSchema) hO(e, T, A);
          else if (Qp(e, e._dbSchema, A), !pO(e, A) && !s)
            return console.warn("Dexie SchemaDiff: Schema was extended without increasing the number passed to db.version(). Dexie will add missing parts and increment native version number to workaround this."), T.close(), o = T.version + 1, s = !0, f(d());
          Jp(e, A);
        } catch {
        }
        tu.push(e), T.onversionchange = fn(function(I) {
          t.vcFired = !0, e.on("versionchange").fire(I);
        }), T.onclose = fn(function(I) {
          e.on("close").fire(I);
        }), u && yO(e._deps, v), f();
      }, m);
    }).catch(function(f) {
      switch (f?.name) {
        case "UnknownError":
          if (t.PR1398_maxLoop > 0)
            return t.PR1398_maxLoop--, console.warn("Dexie: Workaround for Chrome UnknownError on open()"), d();
          break;
        case "VersionError":
          if (o > 0)
            return o = 0, d();
          break;
      }
      return st.reject(f);
    });
  };
  return st.race([r, (typeof navigator > "u" ? st.resolve() : bO()).then(d)]).then(function() {
    return a(), t.onReadyBeingFired = [], st.resolve(P_(function() {
      return e.on.ready.fire(e.vip);
    })).then(function f() {
      if (t.onReadyBeingFired.length > 0) {
        var m = t.onReadyBeingFired.reduce(J0, rn);
        return t.onReadyBeingFired = [], st.resolve(P_(function() {
          return m(e.vip);
        })).then(f);
      }
    });
  }).finally(function() {
    t.openCanceller === r && (t.onReadyBeingFired = null, t.isBeingOpened = !1);
  }).catch(function(f) {
    t.dbOpenError = f;
    try {
      l && l.abort();
    } catch {
    }
    return r === t.openCanceller && e._close(), An(f);
  }).finally(function() {
    t.openComplete = !0, i();
  }).then(function() {
    if (u) {
      var f = {};
      e.tables.forEach(function(m) {
        m.schema.indexes.forEach(function(v) {
          v.name && (f["idb://".concat(e.name, "/").concat(m.name, "/").concat(v.name)] = new gr(-1 / 0, [[[]]]));
        }), f["idb://".concat(e.name, "/").concat(m.name, "/")] = f["idb://".concat(e.name, "/").concat(m.name, "/:dels")] = new gr(-1 / 0, [[[]]]);
      }), qa(af).fire(f), ab(f, !0);
    }
    return e;
  });
}
function R_(e) {
  var t = function(a) {
    return e.next(a);
  }, n = function(a) {
    return e.throw(a);
  }, r = s(t), o = s(n);
  function s(a) {
    return function(i) {
      var l = a(i), u = l.value;
      return l.done ? u : !u || typeof u.then != "function" ? vn(u) ? Promise.all(u).then(r, o) : r(u) : u.then(r, o);
    };
  }
  return s(t)();
}
function TO(e, t, n) {
  var r = arguments.length;
  if (r < 2) throw new vt.InvalidArgument("Too few arguments");
  for (var o = new Array(r - 1); --r; ) o[r - 1] = arguments[r];
  return n = o.pop(), [
    e,
    BI(o),
    n
  ];
}
function ox(e, t, n, r, o) {
  return st.resolve().then(function() {
    var s = pt.transless || pt, a = e._createTransaction(t, n, e._dbSchema, r);
    a.explicit = !0;
    var i = {
      trans: a,
      transless: s
    };
    if (r) a.idbtrans = r.idbtrans;
    else try {
      a.create(), a.idbtrans._explicit = !0, e._state.PR1398_maxLoop = 3;
    } catch (f) {
      return f.name === K0.InvalidState && e.isOpen() && --e._state.PR1398_maxLoop > 0 ? (console.warn("Dexie: Need to reopen db"), e.close({ disableAutoOpen: !1 }), e.open().then(function() {
        return ox(e, t, n, null, o);
      })) : An(f);
    }
    var l = G0(o);
    l && $u();
    var u, d = st.follow(function() {
      if (u = o.call(a, a), u)
        if (l) {
          var f = Ba.bind(null, null);
          u.then(f, f);
        } else typeof u.next == "function" && typeof u.throw == "function" && (u = R_(u));
    }, i);
    return (u && typeof u.then == "function" ? st.resolve(u).then(function(f) {
      return a.active ? f : An(new vt.PrematureCommit("Transaction committed too early. See http://bit.ly/2kdckMn"));
    }) : d.then(function() {
      return u;
    })).then(function(f) {
      return r && a._resolve(), a._completion.then(function() {
        return f;
      });
    }).catch(function(f) {
      return a._reject(f), An(f);
    });
  });
}
function uh(e, t, n) {
  for (var r = vn(e) ? e.slice() : [e], o = 0; o < n; ++o) r.push(t);
  return r;
}
function EO(e) {
  return Nt(Nt({}, e), { table: function(t) {
    var n = e.table(t), r = n.schema, o = {}, s = [];
    function a(y, T, C) {
      var A = dd(y), I = o[A] = o[A] || [], R = y == null ? 0 : typeof y == "string" ? 1 : y.length, $ = T > 0, p = Nt(Nt({}, C), {
        name: $ ? "".concat(A, "(virtual-from:").concat(C.name, ")") : C.name,
        lowLevelIndex: C,
        isVirtual: $,
        keyTail: T,
        keyLength: R,
        extractKey: x_(y),
        unique: !$ && C.unique
      });
      return I.push(p), p.isPrimaryKey || s.push(p), R > 1 && a(R === 2 ? y[0] : y.slice(0, R - 1), T + 1, C), I.sort(function(b, w) {
        return b.keyTail - w.keyTail;
      }), p;
    }
    var i = a(r.primaryKey.keyPath, 0, r.primaryKey);
    o[":id"] = [i];
    for (var l = 0, u = r.indexes; l < u.length; l++) {
      var d = u[l];
      a(d.keyPath, 0, d);
    }
    function f(y) {
      var T = o[dd(y)];
      return T && T[0];
    }
    function m(y, T) {
      return {
        type: y.type === 1 ? 2 : y.type,
        lower: uh(y.lower, y.lowerOpen ? e.MAX_KEY : e.MIN_KEY, T),
        lowerOpen: !0,
        upper: uh(y.upper, y.upperOpen ? e.MIN_KEY : e.MAX_KEY, T),
        upperOpen: !0
      };
    }
    function v(y) {
      var T = y.query.index;
      return T.isVirtual ? Nt(Nt({}, y), { query: {
        index: T.lowLevelIndex,
        range: m(y.query.range, T.keyTail)
      } }) : y;
    }
    return Nt(Nt({}, n), {
      schema: Nt(Nt({}, r), {
        primaryKey: i,
        indexes: s,
        getIndexByKeyPath: f
      }),
      count: function(y) {
        return n.count(v(y));
      },
      query: function(y) {
        return n.query(v(y));
      },
      openCursor: function(y) {
        var T = y.query.index, C = T.keyTail, A = T.isVirtual, I = T.keyLength;
        if (!A) return n.openCursor(y);
        function R($) {
          function p(b) {
            b != null ? $.continue(uh(b, y.reverse ? e.MAX_KEY : e.MIN_KEY, C)) : y.unique ? $.continue($.key.slice(0, I).concat(y.reverse ? e.MIN_KEY : e.MAX_KEY, C)) : $.continue();
          }
          return Object.create($, {
            continue: { value: p },
            continuePrimaryKey: { value: function(b, w) {
              $.continuePrimaryKey(uh(b, e.MAX_KEY, C), w);
            } },
            primaryKey: { get: function() {
              return $.primaryKey;
            } },
            key: { get: function() {
              var b = $.key;
              return I === 1 ? b[0] : b.slice(0, I);
            } },
            value: { get: function() {
              return $.value;
            } }
          });
        }
        return n.openCursor(v(y)).then(function($) {
          return $ && R($);
        });
      }
    });
  } });
}
var AO = {
  stack: "dbcore",
  name: "VirtualIndexMiddleware",
  level: 1,
  create: EO
};
function ib(e, t, n, r) {
  return n = n || {}, r = r || "", Wn(e).forEach(function(o) {
    if (!xr(t, o)) n[r + o] = void 0;
    else {
      var s = e[o], a = t[o];
      if (typeof s == "object" && typeof a == "object" && s && a) {
        var i = __(s);
        i !== __(a) ? n[r + o] = t[o] : i === "Object" ? ib(s, a, n, r + o + ".") : s !== a && (n[r + o] = t[o]);
      } else s !== a && (n[r + o] = t[o]);
    }
  }), Wn(t).forEach(function(o) {
    xr(e, o) || (n[r + o] = t[o]);
  }), n;
}
function lb(e, t) {
  return t.type === "delete" ? t.keys : t.keys || t.values.map(e.extractKey);
}
var CO = {
  stack: "dbcore",
  name: "HooksMiddleware",
  level: 2,
  create: function(e) {
    return Nt(Nt({}, e), { table: function(t) {
      var n = e.table(t), r = n.schema.primaryKey;
      return Nt(Nt({}, n), { mutate: function(o) {
        var s = pt.trans, a = s.table(t).hook, i = a.deleting, l = a.creating, u = a.updating;
        switch (o.type) {
          case "add":
            if (l.fire === rn) break;
            return s._promise("readwrite", function() {
              return d(o);
            }, !0);
          case "put":
            if (l.fire === rn && u.fire === rn) break;
            return s._promise("readwrite", function() {
              return d(o);
            }, !0);
          case "delete":
            if (i.fire === rn) break;
            return s._promise("readwrite", function() {
              return d(o);
            }, !0);
          case "deleteRange":
            if (i.fire === rn) break;
            return s._promise("readwrite", function() {
              return f(o);
            }, !0);
        }
        return n.mutate(o);
        function d(v) {
          var y = pt.trans, T = v.keys || lb(r, v);
          if (!T) throw new Error("Keys missing");
          return v = v.type === "add" || v.type === "put" ? Nt(Nt({}, v), { keys: T }) : Nt({}, v), v.type !== "delete" && (v.values = zp([], v.values, !0)), v.keys && (v.keys = zp([], v.keys, !0)), kO(n, v, T).then(function(C) {
            var A = T.map(function(I, R) {
              var $ = C[R], p = {
                onerror: null,
                onsuccess: null
              };
              if (v.type === "delete") i.fire.call(p, I, $, y);
              else if (v.type === "add" || $ === void 0) {
                var b = l.fire.call(p, I, v.values[R], y);
                I == null && b != null && (I = b, v.keys[R] = I, r.outbound || Zr(v.values[R], r.keyPath, I));
              } else {
                var w = ib($, v.values[R]), E = u.fire.call(p, w, I, $, y);
                if (E) {
                  var x = v.values[R];
                  Object.keys(E).forEach(function(M) {
                    xr(x, M) ? x[M] = E[M] : Zr(x, M, E[M]);
                  });
                }
              }
              return p;
            });
            return n.mutate(v).then(function(I) {
              for (var R = I.failures, $ = I.results, p = I.numFailures, b = I.lastResult, w = 0; w < T.length; ++w) {
                var E = $ ? $[w] : T[w], x = A[w];
                E == null ? x.onerror && x.onerror(R[w]) : x.onsuccess && x.onsuccess(v.type === "put" && C[w] ? v.values[w] : E);
              }
              return {
                failures: R,
                results: $,
                numFailures: p,
                lastResult: b
              };
            }).catch(function(I) {
              return A.forEach(function(R) {
                return R.onerror && R.onerror(I);
              }), Promise.reject(I);
            });
          });
        }
        function f(v) {
          return m(v.trans, v.range, 1e4);
        }
        function m(v, y, T) {
          return n.query({
            trans: v,
            values: !1,
            query: {
              index: r,
              range: y
            },
            limit: T
          }).then(function(C) {
            var A = C.result;
            return d({
              type: "delete",
              keys: A,
              trans: v
            }).then(function(I) {
              return I.numFailures > 0 ? Promise.reject(I.failures[0]) : A.length < T ? {
                failures: [],
                numFailures: 0,
                lastResult: void 0
              } : m(v, Nt(Nt({}, y), {
                lower: A[A.length - 1],
                lowerOpen: !0
              }), T);
            });
          });
        }
      } });
    } });
  }
};
function kO(e, t, n) {
  return t.type === "add" ? Promise.resolve([]) : e.getMany({
    trans: t.trans,
    keys: n,
    cache: "immutable"
  });
}
function sx(e, t, n) {
  try {
    if (!t || t.keys.length < e.length) return null;
    for (var r = [], o = 0, s = 0; o < t.keys.length && s < e.length; ++o)
      Wt(t.keys[o], e[s]) === 0 && (r.push(n ? qi(t.values[o]) : t.values[o]), ++s);
    return r.length === e.length ? r : null;
  } catch {
    return null;
  }
}
var IO = {
  stack: "dbcore",
  level: -1,
  create: function(e) {
    return { table: function(t) {
      var n = e.table(t);
      return Nt(Nt({}, n), {
        getMany: function(r) {
          if (!r.cache) return n.getMany(r);
          var o = sx(r.keys, r.trans._cache, r.cache === "clone");
          return o ? st.resolve(o) : n.getMany(r).then(function(s) {
            return r.trans._cache = {
              keys: r.keys,
              values: r.cache === "clone" ? qi(s) : s
            }, s;
          });
        },
        mutate: function(r) {
          return r.type !== "add" && (r.trans._cache = null), n.mutate(r);
        }
      });
    } };
  }
};
function ax(e, t) {
  return e.trans.mode === "readonly" && !!e.subscr && !e.trans.explicit && e.trans.db._options.cache !== "disabled" && !t.schema.primaryKey.outbound;
}
function ix(e, t) {
  switch (e) {
    case "query":
      return t.values && !t.unique;
    case "get":
      return !1;
    case "getMany":
      return !1;
    case "count":
      return !1;
    case "openCursor":
      return !1;
  }
}
var xO = {
  stack: "dbcore",
  level: 0,
  name: "Observability",
  create: function(e) {
    var t = e.schema.name, n = new gr(e.MIN_KEY, e.MAX_KEY);
    return Nt(Nt({}, e), {
      transaction: function(r, o, s) {
        if (pt.subscr && o !== "readonly") throw new vt.ReadOnly("Readwrite transaction in liveQuery context. Querier source: ".concat(pt.querier));
        return e.transaction(r, o, s);
      },
      table: function(r) {
        var o = e.table(r), s = o.schema, a = s.primaryKey, i = s.indexes, l = a.extractKey, u = a.outbound, d = a.autoIncrement && i.filter(function(y) {
          return y.compound && y.keyPath.includes(a.keyPath);
        }), f = Nt(Nt({}, o), { mutate: function(y) {
          var T, C, A = y.trans, I = y.mutatedParts || (y.mutatedParts = {}), R = function(z) {
            var J = "idb://".concat(t, "/").concat(r, "/").concat(z);
            return I[J] || (I[J] = new gr());
          }, $ = R(""), p = R(":dels"), b = y.type, w = y.type === "deleteRange" ? [y.range] : y.type === "delete" ? [y.keys] : y.values.length < 50 ? [lb(a, y).filter(function(z) {
            return z;
          }), y.values] : [], E = w[0], x = w[1], M = y.trans._cache;
          if (vn(E)) {
            $.addKeys(E);
            var N = b === "delete" || E.length === x.length ? sx(E, M) : null;
            N || p.addKeys(E), (N || x) && MO(R, s, N, x);
          } else if (E) {
            var q = {
              from: (T = E.lower) !== null && T !== void 0 ? T : e.MIN_KEY,
              to: (C = E.upper) !== null && C !== void 0 ? C : e.MAX_KEY
            };
            p.add(q), $.add(q);
          } else
            $.add(n), p.add(n), s.indexes.forEach(function(z) {
              return R(z.name).add(n);
            });
          return o.mutate(y).then(function(z) {
            return E && (y.type === "add" || y.type === "put") && ($.addKeys(z.results), d && d.forEach(function(J) {
              for (var G = y.values.map(function(de) {
                return J.extractKey(de);
              }), oe = J.keyPath.findIndex(function(de) {
                return de === a.keyPath;
              }), B = 0, X = z.results.length; B < X; ++B) G[B][oe] = z.results[B];
              R(J.name).addKeys(G);
            })), A.mutatedParts = ag(A.mutatedParts || {}, I), z;
          });
        } }), m = function(y) {
          var T, C, A = y.query, I = A.index, R = A.range;
          return [I, new gr((T = R.lower) !== null && T !== void 0 ? T : e.MIN_KEY, (C = R.upper) !== null && C !== void 0 ? C : e.MAX_KEY)];
        }, v = {
          get: function(y) {
            return [a, new gr(y.key)];
          },
          getMany: function(y) {
            return [a, new gr().addKeys(y.keys)];
          },
          count: m,
          query: m,
          openCursor: m
        };
        return Wn(v).forEach(function(y) {
          f[y] = function(T) {
            var C = pt.subscr, A = !!C, I = ax(pt, o) && ix(y, T) ? T.obsSet = {} : C;
            if (A) {
              var R = function(M) {
                var N = "idb://".concat(t, "/").concat(r, "/").concat(M);
                return I[N] || (I[N] = new gr());
              }, $ = R(""), p = R(":dels"), b = v[y](T), w = b[0], E = b[1];
              if (y === "query" && w.isPrimaryKey && !T.values ? p.add(E) : R(w.name || "").add(E), !w.isPrimaryKey) if (y === "count") p.add(n);
              else {
                var x = y === "query" && u && T.values && o.query(Nt(Nt({}, T), { values: !1 }));
                return o[y].apply(this, arguments).then(function(M) {
                  if (y === "query") {
                    if (u && T.values) return x.then(function(J) {
                      var G = J.result;
                      return $.addKeys(G), M;
                    });
                    var N = T.values ? M.result.map(l) : M.result;
                    T.values ? $.addKeys(N) : p.addKeys(N);
                  } else if (y === "openCursor") {
                    var q = M, z = T.values;
                    return q && Object.create(q, {
                      key: { get: function() {
                        return p.addKey(q.primaryKey), q.key;
                      } },
                      primaryKey: { get: function() {
                        var J = q.primaryKey;
                        return p.addKey(J), J;
                      } },
                      value: { get: function() {
                        return z && $.addKey(q.primaryKey), q.value;
                      } }
                    });
                  }
                  return M;
                });
              }
            }
            return o[y].apply(this, arguments);
          };
        }), f;
      }
    });
  }
};
function MO(e, t, n, r) {
  function o(s) {
    var a = e(s.name || "");
    function i(u) {
      return u != null ? s.extractKey(u) : null;
    }
    var l = function(u) {
      return s.multiEntry && vn(u) ? u.forEach(function(d) {
        return a.addKey(d);
      }) : a.addKey(u);
    };
    (n || r).forEach(function(u, d) {
      var f = n && i(n[d]), m = r && i(r[d]);
      Wt(f, m) !== 0 && (f != null && l(f), m != null && l(m));
    });
  }
  t.indexes.forEach(o);
}
function W2(e, t, n) {
  if (n.numFailures === 0) return t;
  if (t.type === "deleteRange") return null;
  var r = t.keys ? t.keys.length : "values" in t && t.values ? t.values.length : 1;
  if (n.numFailures === r) return null;
  var o = Nt({}, t);
  return vn(o.keys) && (o.keys = o.keys.filter(function(s, a) {
    return !(a in n.failures);
  })), "values" in o && vn(o.values) && (o.values = o.values.filter(function(s, a) {
    return !(a in n.failures);
  })), o;
}
function PO(e, t) {
  return t.lower === void 0 ? !0 : t.lowerOpen ? Wt(e, t.lower) > 0 : Wt(e, t.lower) >= 0;
}
function RO(e, t) {
  return t.upper === void 0 ? !0 : t.upperOpen ? Wt(e, t.upper) < 0 : Wt(e, t.upper) <= 0;
}
function iy(e, t) {
  return PO(e, t) && RO(e, t);
}
function G2(e, t, n, r, o, s) {
  if (!n || n.length === 0) return e;
  var a = t.query.index, i = a.multiEntry, l = t.query.range, u = r.schema.primaryKey.extractKey, d = a.extractKey, f = (a.lowLevelIndex || a).extractKey, m = n.reduce(function(v, y) {
    var T = v, C = [];
    if (y.type === "add" || y.type === "put")
      for (var A = new gr(), I = y.values.length - 1; I >= 0; --I) {
        var R = y.values[I], $ = u(R);
        if (!A.hasKey($)) {
          var p = d(R);
          (i && vn(p) ? p.some(function(M) {
            return iy(M, l);
          }) : iy(p, l)) && (A.addKey($), C.push(R));
        }
      }
    switch (y.type) {
      case "add":
        var b = new gr().addKeys(t.values ? v.map(function(M) {
          return u(M);
        }) : v);
        T = v.concat(t.values ? C.filter(function(M) {
          var N = u(M);
          return b.hasKey(N) ? !1 : (b.addKey(N), !0);
        }) : C.map(function(M) {
          return u(M);
        }).filter(function(M) {
          return b.hasKey(M) ? !1 : (b.addKey(M), !0);
        }));
        break;
      case "put":
        var w = new gr().addKeys(y.values.map(function(M) {
          return u(M);
        }));
        T = v.filter(function(M) {
          return !w.hasKey(t.values ? u(M) : M);
        }).concat(t.values ? C : C.map(function(M) {
          return u(M);
        }));
        break;
      case "delete":
        var E = new gr().addKeys(y.keys);
        T = v.filter(function(M) {
          return !E.hasKey(t.values ? u(M) : M);
        });
        break;
      case "deleteRange":
        var x = y.range;
        T = v.filter(function(M) {
          return !iy(u(M), x);
        });
        break;
    }
    return T;
  }, e);
  return m === e ? e : (m.sort(function(v, y) {
    return Wt(f(v), f(y)) || Wt(u(v), u(y));
  }), t.limit && t.limit < 1 / 0 && (m.length > t.limit ? m.length = t.limit : e.length === t.limit && m.length < t.limit && (o.dirty = !0)), s ? Object.freeze(m) : m);
}
function V2(e, t) {
  return Wt(e.lower, t.lower) === 0 && Wt(e.upper, t.upper) === 0 && !!e.lowerOpen == !!t.lowerOpen && !!e.upperOpen == !!t.upperOpen;
}
function NO(e, t, n, r) {
  if (e === void 0) return t !== void 0 ? -1 : 0;
  if (t === void 0) return 1;
  var o = Wt(e, t);
  if (o === 0) {
    if (n && r) return 0;
    if (n) return 1;
    if (r) return -1;
  }
  return o;
}
function $O(e, t, n, r) {
  if (e === void 0) return t !== void 0 ? 1 : 0;
  if (t === void 0) return -1;
  var o = Wt(e, t);
  if (o === 0) {
    if (n && r) return 0;
    if (n) return -1;
    if (r) return 1;
  }
  return o;
}
function DO(e, t) {
  return NO(e.lower, t.lower, e.lowerOpen, t.lowerOpen) <= 0 && $O(e.upper, t.upper, e.upperOpen, t.upperOpen) >= 0;
}
function LO(e, t, n, r) {
  var o = Oi["idb://".concat(e, "/").concat(t)];
  if (!o) return [];
  var s = o.queries[n];
  if (!s) return [
    null,
    !1,
    o,
    null
  ];
  var a = s[(r.query ? r.query.index.name : null) || ""];
  if (!a) return [
    null,
    !1,
    o,
    null
  ];
  switch (n) {
    case "query":
      var i = a.find(function(u) {
        return u.req.limit === r.limit && u.req.values === r.values && V2(u.req.query.range, r.query.range);
      });
      return i ? [
        i,
        !0,
        o,
        a
      ] : [
        a.find(function(u) {
          return ("limit" in u.req ? u.req.limit : 1 / 0) >= r.limit && (r.values ? u.req.values : !0) && DO(u.req.query.range, r.query.range);
        }),
        !1,
        o,
        a
      ];
    case "count":
      var l = a.find(function(u) {
        return V2(u.req.query.range, r.query.range);
      });
      return [
        l,
        !!l,
        o,
        a
      ];
  }
}
function OO(e, t, n, r) {
  e.subscribers.add(n), r.addEventListener("abort", function() {
    e.subscribers.delete(n), e.subscribers.size === 0 && FO(e, t);
  });
}
function FO(e, t) {
  setTimeout(function() {
    e.subscribers.size === 0 && vi(t, e);
  }, 3e3);
}
var BO = {
  stack: "dbcore",
  level: 0,
  name: "Cache",
  create: function(e) {
    var t = e.schema.name;
    return Nt(Nt({}, e), {
      transaction: function(n, r, o) {
        var s = e.transaction(n, r, o);
        if (r === "readwrite") {
          var a = new AbortController(), i = a.signal, l = function(u) {
            return function() {
              if (a.abort(), r === "readwrite") {
                for (var d = /* @__PURE__ */ new Set(), f = 0, m = n; f < m.length; f++) {
                  var v = m[f], y = Oi["idb://".concat(t, "/").concat(v)];
                  if (y) {
                    var T = e.table(v), C = y.optimisticOps.filter(function(z) {
                      return z.trans === s;
                    });
                    if (s._explicit && u && s.mutatedParts) for (var A = 0, I = Object.values(y.queries.query); A < I.length; A++)
                      for (var R = I[A], $ = 0, p = R.slice(); $ < p.length; $++) {
                        var b = p[$];
                        sb(b.obsSet, s.mutatedParts) && (vi(R, b), b.subscribers.forEach(function(z) {
                          return d.add(z);
                        }));
                      }
                    else if (C.length > 0) {
                      y.optimisticOps = y.optimisticOps.filter(function(z) {
                        return z.trans !== s;
                      });
                      for (var w = 0, E = Object.values(y.queries.query); w < E.length; w++)
                        for (var R = E[w], x = 0, M = R.slice(); x < M.length; x++) {
                          var b = M[x];
                          if (b.res != null && s.mutatedParts) if (u && !b.dirty) {
                            var N = Object.isFrozen(b.res), q = G2(b.res, b.req, C, T, b, N);
                            b.dirty ? (vi(R, b), b.subscribers.forEach(function(G) {
                              return d.add(G);
                            })) : q !== b.res && (b.res = q, b.promise = st.resolve({ result: q }));
                          } else
                            b.dirty && vi(R, b), b.subscribers.forEach(function(G) {
                              return d.add(G);
                            });
                        }
                    }
                  }
                }
                d.forEach(function(z) {
                  return z();
                });
              }
            };
          };
          s.addEventListener("abort", l(!1), { signal: i }), s.addEventListener("error", l(!1), { signal: i }), s.addEventListener("complete", l(!0), { signal: i });
        }
        return s;
      },
      table: function(n) {
        var r = e.table(n), o = r.schema.primaryKey;
        return Nt(Nt({}, r), {
          mutate: function(s) {
            var a = pt.trans;
            if (o.outbound || a.db._options.cache === "disabled" || a.explicit || a.idbtrans.mode !== "readwrite") return r.mutate(s);
            var i = Oi["idb://".concat(t, "/").concat(n)];
            if (!i) return r.mutate(s);
            var l = r.mutate(s);
            return (s.type === "add" || s.type === "put") && (s.values.length >= 50 || lb(o, s).some(function(u) {
              return u == null;
            })) ? l.then(function(u) {
              var d = W2(i, Nt(Nt({}, s), { values: s.values.map(function(f, m) {
                var v;
                if (u.failures[m]) return f;
                var y = !((v = o.keyPath) === null || v === void 0) && v.includes(".") ? qi(f) : Nt({}, f);
                return Zr(y, o.keyPath, u.results[m]), y;
              }) }), u);
              i.optimisticOps.push(d), queueMicrotask(function() {
                return s.mutatedParts && lh(s.mutatedParts);
              });
            }) : (i.optimisticOps.push(s), s.mutatedParts && lh(s.mutatedParts), l.then(function(u) {
              if (u.numFailures > 0) {
                vi(i.optimisticOps, s);
                var d = W2(i, s, u);
                d && i.optimisticOps.push(d), s.mutatedParts && lh(s.mutatedParts);
              }
            }), l.catch(function() {
              vi(i.optimisticOps, s), s.mutatedParts && lh(s.mutatedParts);
            })), l;
          },
          query: function(s) {
            var a;
            if (!ax(pt, r) || !ix("query", s)) return r.query(s);
            var i = ((a = pt.trans) === null || a === void 0 ? void 0 : a.db._options.cache) === "immutable", l = pt, u = l.requery, d = l.signal, f = LO(t, n, "query", s), m = f[0], v = f[1], y = f[2], T = f[3];
            if (m && v) m.obsSet = s.obsSet;
            else {
              var C = r.query(s).then(function(A) {
                var I = A.result;
                if (m && (m.res = I), i) {
                  for (var R = 0, $ = I.length; R < $; ++R) Object.freeze(I[R]);
                  Object.freeze(I);
                } else A.result = qi(I);
                return A;
              }).catch(function(A) {
                return T && m && vi(T, m), Promise.reject(A);
              });
              m = {
                obsSet: s.obsSet,
                promise: C,
                subscribers: /* @__PURE__ */ new Set(),
                type: "query",
                req: s,
                dirty: !1
              }, T ? T.push(m) : (T = [m], y || (y = Oi["idb://".concat(t, "/").concat(n)] = {
                queries: {
                  query: {},
                  count: {}
                },
                objs: /* @__PURE__ */ new Map(),
                optimisticOps: [],
                unsignaledParts: {}
              }), y.queries.query[s.query.index.name || ""] = T);
            }
            return OO(m, T, u, d), m.promise.then(function(A) {
              return { result: G2(A.result, s, y?.optimisticOps, r, m, i) };
            });
          }
        });
      }
    });
  }
};
function ch(e, t) {
  return new Proxy(e, { get: function(n, r, o) {
    return r === "db" ? t : Reflect.get(n, r, o);
  } });
}
var gu = (function() {
  function e(t, n) {
    var r = this;
    this._middlewares = {}, this.verno = 0;
    var o = e.dependencies;
    this._options = n = Nt({
      addons: e.addons,
      autoOpen: !0,
      indexedDB: o.indexedDB,
      IDBKeyRange: o.IDBKeyRange,
      cache: "cloned"
    }, n), this._deps = {
      indexedDB: n.indexedDB,
      IDBKeyRange: n.IDBKeyRange
    };
    var s = n.addons;
    this._dbSchema = {}, this._versions = [], this._storeNames = [], this._allTables = {}, this.idbdb = null, this._novip = this;
    var a = {
      dbOpenError: null,
      isBeingOpened: !1,
      onReadyBeingFired: null,
      openComplete: !1,
      dbReadyResolve: rn,
      dbReadyPromise: null,
      cancelOpen: rn,
      openCanceller: null,
      autoSchema: !0,
      PR1398_maxLoop: 3,
      autoOpen: n.autoOpen
    };
    a.dbReadyPromise = new st(function(l) {
      a.dbReadyResolve = l;
    }), a.openCanceller = new st(function(l, u) {
      a.cancelOpen = u;
    }), this._state = a, this.name = t, this.on = of(this, "populate", "blocked", "versionchange", "close", { ready: [J0, rn] }), this.on.ready.subscribe = LI(this.on.ready.subscribe, function(l) {
      return function(u, d) {
        e.vip(function() {
          var f = r._state;
          if (f.openComplete)
            f.dbOpenError || st.resolve().then(u), d && l(u);
          else if (f.onReadyBeingFired)
            f.onReadyBeingFired.push(u), d && l(u);
          else {
            l(u);
            var m = r;
            d || l(function v() {
              m.on.ready.unsubscribe(u), m.on.ready.unsubscribe(v);
            });
          }
        });
      };
    }), this.Collection = VL(this), this.Table = HL(this), this.Transaction = eO(this), this.Version = gO(this), this.WhereClause = ZL(this), this.on("versionchange", function(l) {
      l.newVersion > 0 ? console.warn("Another connection wants to upgrade database '".concat(r.name, "'. Closing db now to resume the upgrade.")) : console.warn("Another connection wants to delete database '".concat(r.name, "'. Closing db now to resume the delete request.")), r.close({ disableAutoOpen: !1 });
    }), this.on("blocked", function(l) {
      !l.newVersion || l.newVersion < l.oldVersion ? console.warn("Dexie.delete('".concat(r.name, "') was blocked")) : console.warn("Upgrade '".concat(r.name, "' blocked by other connection holding version ").concat(l.oldVersion / 10));
    }), this._maxKey = Ld(n.IDBKeyRange), this._createTransaction = function(l, u, d, f) {
      return new r.Transaction(l, u, d, r._options.chromeTransactionDurability, f);
    }, this._fireOnBlocked = function(l) {
      r.on("blocked").fire(l), tu.filter(function(u) {
        return u.name === r.name && u !== r && !u._state.vcFired;
      }).map(function(u) {
        return u.on("versionchange").fire(l);
      });
    }, this.use(IO), this.use(BO), this.use(xO), this.use(AO), this.use(CO);
    var i = new Proxy(this, { get: function(l, u, d) {
      if (u === "_vip") return !0;
      if (u === "table") return function(m) {
        return ch(r.table(m), i);
      };
      var f = Reflect.get(l, u, d);
      return f instanceof ZI ? ch(f, i) : u === "tables" ? f.map(function(m) {
        return ch(m, i);
      }) : u === "_createTransaction" ? function() {
        return ch(f.apply(this, arguments), i);
      } : f;
    } });
    this.vip = i, s.forEach(function(l) {
      return l(r);
    });
  }
  return e.prototype.version = function(t) {
    if (isNaN(t) || t < 0.1) throw new vt.Type("Given version is not a positive number");
    if (t = Math.round(t * 10) / 10, this.idbdb || this._state.isBeingOpened) throw new vt.Schema("Cannot add version when database is open");
    this.verno = Math.max(this.verno, t);
    var n = this._versions, r = n.filter(function(o) {
      return o._cfg.version === t;
    })[0];
    return r || (r = new this.Version(t), n.push(r), n.sort(iO), r.stores({}), this._state.autoSchema = !1, r);
  }, e.prototype._whenReady = function(t) {
    var n = this;
    return this.idbdb && (this._state.openComplete || pt.letThrough || this._vip) ? t() : new st(function(r, o) {
      if (n._state.openComplete) return o(new vt.DatabaseClosed(n._state.dbOpenError));
      if (!n._state.isBeingOpened) {
        if (!n._state.autoOpen) {
          o(new vt.DatabaseClosed());
          return;
        }
        n.open().catch(rn);
      }
      n._state.dbReadyPromise.then(r, o);
    }).then(t);
  }, e.prototype.use = function(t) {
    var n = t.stack, r = t.create, o = t.level, s = t.name;
    s && this.unuse({
      stack: n,
      name: s
    });
    var a = this._middlewares[n] || (this._middlewares[n] = []);
    return a.push({
      stack: n,
      create: r,
      level: o ?? 10,
      name: s
    }), a.sort(function(i, l) {
      return i.level - l.level;
    }), this;
  }, e.prototype.unuse = function(t) {
    var n = t.stack, r = t.name, o = t.create;
    return n && this._middlewares[n] && (this._middlewares[n] = this._middlewares[n].filter(function(s) {
      return o ? s.create !== o : r ? s.name !== r : !1;
    })), this;
  }, e.prototype.open = function() {
    var t = this;
    return zi($a, function() {
      return SO(t);
    });
  }, e.prototype._close = function() {
    var t = this._state, n = tu.indexOf(this);
    if (n >= 0 && tu.splice(n, 1), this.idbdb) {
      try {
        this.idbdb.close();
      } catch {
      }
      this.idbdb = null;
    }
    t.isBeingOpened || (t.dbReadyPromise = new st(function(r) {
      t.dbReadyResolve = r;
    }), t.openCanceller = new st(function(r, o) {
      t.cancelOpen = o;
    }));
  }, e.prototype.close = function(t) {
    var n = (t === void 0 ? { disableAutoOpen: !0 } : t).disableAutoOpen, r = this._state;
    n ? (r.isBeingOpened && r.cancelOpen(new vt.DatabaseClosed()), this._close(), r.autoOpen = !1, r.dbOpenError = new vt.DatabaseClosed()) : (this._close(), r.autoOpen = this._options.autoOpen || r.isBeingOpened, r.openComplete = !1, r.dbOpenError = null);
  }, e.prototype.delete = function(t) {
    var n = this;
    t === void 0 && (t = { disableAutoOpen: !0 });
    var r = arguments.length > 0 && typeof arguments[0] != "object", o = this._state;
    return new st(function(s, a) {
      var i = function() {
        n.close(t);
        var l = n._deps.indexedDB.deleteDatabase(n.name);
        l.onsuccess = fn(function() {
          _O(n._deps, n.name), s();
        }), l.onerror = jo(a), l.onblocked = n._fireOnBlocked;
      };
      if (r) throw new vt.InvalidArgument("Invalid closeOptions argument to db.delete()");
      o.isBeingOpened ? o.dbReadyPromise.then(i) : i();
    });
  }, e.prototype.backendDB = function() {
    return this.idbdb;
  }, e.prototype.isOpen = function() {
    return this.idbdb !== null;
  }, e.prototype.hasBeenClosed = function() {
    var t = this._state.dbOpenError;
    return t && t.name === "DatabaseClosed";
  }, e.prototype.hasFailed = function() {
    return this._state.dbOpenError !== null;
  }, e.prototype.dynamicallyOpened = function() {
    return this._state.autoSchema;
  }, Object.defineProperty(e.prototype, "tables", {
    get: function() {
      var t = this;
      return Wn(this._allTables).map(function(n) {
        return t._allTables[n];
      });
    },
    enumerable: !1,
    configurable: !0
  }), e.prototype.transaction = function() {
    var t = TO.apply(this, arguments);
    return this._transaction.apply(this, t);
  }, e.prototype._transaction = function(t, n, r) {
    var o = this, s = pt.trans;
    (!s || s.db !== this || t.indexOf("!") !== -1) && (s = null);
    var a = t.indexOf("?") !== -1;
    t = t.replace("!", "").replace("?", "");
    var i, l;
    try {
      if (l = n.map(function(d) {
        var f = d instanceof o.Table ? d.name : d;
        if (typeof f != "string") throw new TypeError("Invalid table argument to Dexie.transaction(). Only Table or String are allowed");
        return f;
      }), t == "r" || t === jv) i = jv;
      else if (t == "rw" || t == ey) i = ey;
      else throw new vt.InvalidArgument("Invalid transaction mode: " + t);
      if (s) {
        if (s.mode === jv && i === ey) if (a) s = null;
        else throw new vt.SubTransaction("Cannot enter a sub-transaction with READWRITE mode when parent transaction is READONLY");
        s && l.forEach(function(d) {
          if (s && s.storeNames.indexOf(d) === -1) if (a) s = null;
          else throw new vt.SubTransaction("Table " + d + " not included in parent transaction.");
        }), a && s && !s.active && (s = null);
      }
    } catch (d) {
      return s ? s._promise(null, function(f, m) {
        m(d);
      }) : An(d);
    }
    var u = ox.bind(null, this, i, l, s, r);
    return s ? s._promise(i, u, "lock") : pt.trans ? zi(pt.transless, function() {
      return o._whenReady(u);
    }) : this._whenReady(u);
  }, e.prototype.table = function(t) {
    if (!xr(this._allTables, t)) throw new vt.InvalidTable("Table ".concat(t, " does not exist"));
    return this._allTables[t];
  }, e;
})(), UO = typeof Symbol < "u" && "observable" in Symbol ? Symbol.observable : "@@observable", qO = (function() {
  function e(t) {
    this._subscribe = t;
  }
  return e.prototype.subscribe = function(t, n, r) {
    return this._subscribe(!t || typeof t == "function" ? {
      next: t,
      error: n,
      complete: r
    } : t);
  }, e.prototype[UO] = function() {
    return this;
  }, e;
})(), em;
try {
  em = {
    indexedDB: Xn.indexedDB || Xn.mozIndexedDB || Xn.webkitIndexedDB || Xn.msIndexedDB,
    IDBKeyRange: Xn.IDBKeyRange || Xn.webkitIDBKeyRange
  };
} catch {
  em = {
    indexedDB: null,
    IDBKeyRange: null
  };
}
function HO(e) {
  var t = !1, n, r = new qO(function(o) {
    var s = G0(e);
    function a(A) {
      var I = Ru();
      try {
        s && $u();
        var R = Fa(e, A);
        return s && (R = R.finally(Ba)), R;
      } finally {
        I && Nu();
      }
    }
    var i = !1, l, u = {}, d = {}, f = {
      get closed() {
        return i;
      },
      unsubscribe: function() {
        i || (i = !0, l && l.abort(), m && qa.storagemutated.unsubscribe(T));
      }
    };
    o.start && o.start(f);
    var m = !1, v = function() {
      return Zv(C);
    };
    function y() {
      return sb(d, u);
    }
    var T = function(A) {
      ag(u, A), y() && v();
    }, C = function() {
      if (!(i || !em.indexedDB)) {
        u = {};
        var A = {};
        l && l.abort(), l = new AbortController();
        var I = {
          subscr: A,
          signal: l.signal,
          requery: v,
          querier: e,
          trans: null
        }, R = a(I);
        Promise.resolve(R).then(function($) {
          t = !0, n = $, !(i || I.signal.aborted) && (u = {}, d = A, !mL(d) && !m && (qa(af, T), m = !0), Zv(function() {
            return !i && o.next && o.next($);
          }));
        }, function($) {
          t = !1, ["DatabaseClosedError", "AbortError"].includes($?.name) || i || Zv(function() {
            i || o.error && o.error($);
          });
        });
      }
    };
    return setTimeout(v, 0), f;
  });
  return r.hasValue = function() {
    return t;
  }, r.getValue = function() {
    return n;
  }, r;
}
var Ti = gu;
mu(Ti, Nt(Nt({}, rg), {
  delete: function(e) {
    return new Ti(e, { addons: [] }).delete();
  },
  exists: function(e) {
    return new Ti(e, { addons: [] }).open().then(function(t) {
      return t.close(), !0;
    }).catch("NoSuchDatabaseError", function() {
      return !1;
    });
  },
  getDatabaseNames: function(e) {
    try {
      return vO(Ti.dependencies).then(e);
    } catch {
      return An(new vt.MissingAPI());
    }
  },
  defineClass: function() {
    function e(t) {
      ro(this, t);
    }
    return e;
  },
  ignoreTransaction: function(e) {
    return pt.trans ? zi(pt.transless, e) : e();
  },
  vip: P_,
  async: function(e) {
    return function() {
      try {
        var t = R_(e.apply(this, arguments));
        return !t || typeof t.then != "function" ? st.resolve(t) : t;
      } catch (n) {
        return An(n);
      }
    };
  },
  spawn: function(e, t, n) {
    try {
      var r = R_(e.apply(n, t || []));
      return !r || typeof r.then != "function" ? st.resolve(r) : r;
    } catch (o) {
      return An(o);
    }
  },
  currentTransaction: { get: function() {
    return pt.trans || null;
  } },
  waitFor: function(e, t) {
    var n = st.resolve(typeof e == "function" ? Ti.ignoreTransaction(e) : e).timeout(t || 6e4);
    return pt.trans ? pt.trans.waitFor(n) : n;
  },
  Promise: st,
  debug: {
    get: function() {
      return ds;
    },
    set: function(e) {
      WI(e);
    }
  },
  derive: Mu,
  extend: ro,
  props: mu,
  override: LI,
  Events: of,
  on: qa,
  liveQuery: HO,
  extendObservabilitySet: ag,
  getByKeyPath: Js,
  setByKeyPath: Zr,
  delByKeyPath: fL,
  shallowClone: FI,
  deepClone: qi,
  getObjectDiff: ib,
  cmp: Wt,
  asap: OI,
  minKey: C_,
  addons: [],
  connections: tu,
  errnames: K0,
  dependencies: em,
  cache: Oi,
  semVer: D2,
  version: D2.split(".").map(function(e) {
    return parseInt(e);
  }).reduce(function(e, t, n) {
    return e + t / Math.pow(10, n * 2);
  })
}));
Ti.maxKey = Ld(Ti.dependencies.IDBKeyRange);
typeof dispatchEvent < "u" && typeof addEventListener < "u" && (qa(af, function(e) {
  if (!Ia) {
    var t = new CustomEvent(k_, { detail: e });
    Ia = !0, dispatchEvent(t), Ia = !1;
  }
}), addEventListener(k_, function(e) {
  var t = e.detail;
  Ia || ub(t);
}));
function ub(e) {
  var t = Ia;
  try {
    Ia = !0, qa.storagemutated.fire(e), ab(e, !0);
  } finally {
    Ia = t;
  }
}
var Ia = !1, Aa, N_ = function() {
};
typeof BroadcastChannel < "u" && (N_ = function() {
  Aa = new BroadcastChannel(k_), Aa.onmessage = function(e) {
    return e.data && ub(e.data);
  };
}, N_(), typeof Aa.unref == "function" && Aa.unref(), qa(af, function(e) {
  Ia || Aa.postMessage(e);
}));
typeof addEventListener < "u" && (addEventListener("pagehide", function(e) {
  if (!gu.disableBfCache && e.persisted) {
    ds && console.debug("Dexie: handling persisted pagehide"), Aa?.close();
    for (var t = 0, n = tu; t < n.length; t++) n[t].close({ disableAutoOpen: !1 });
  }
}), addEventListener("pageshow", function(e) {
  !gu.disableBfCache && e.persisted && (ds && console.debug("Dexie: handling persisted pageshow"), N_(), ub({ all: new gr(-1 / 0, [[]]) }));
}));
st.rejectionMapper = wL;
WI(ds);
var ig = "littlewhitebox-assistant-default", zO = "2026-06-assistant-prompt-structure-v2";
function lx(e = {}) {
  return {
    includeMemory: e.includeMemory !== !1,
    includeCartography: e.includeCartography !== !1,
    includeQuestOrchestration: e.includeQuestOrchestration === !0
  };
}
function Or(e, t = []) {
  const n = t.map((r) => vu(r)).filter(Boolean);
  return n.length ? ["## " + e, n.join(`
`)].join(`
`) : "";
}
function WO(e = {}) {
  const { includeMemory: t, includeCartography: n, includeQuestOrchestration: r } = lx(e), o = [
    "You are the backstage manager for the current LittleWhiteTavern RP session, running inside the user's SillyTavern instance through the LittleWhiteBox tavern workspace.",
    "The main chat handles immersive roleplay. Your job is to keep the backstage materials useful: memory, spatial records, and possible future directions. Do not take over the scene, speak as the RP character, or steer the user by force.",
    "Automatic after-turn maintenance and manual manager chat share the same identity and evidence standard. The trigger differs: automatic maintenance handles a completed RP turn, while manual chat answers the user's current question.",
    t ? "When the Memory Archiving contract is enabled, maintain the current session's Markdown memory files." : "",
    n ? "When the Cartography Engine contract is enabled, maintain the current session's map and atlas records." : "",
    r ? "When the Quest Orchestration contract is enabled, maintain a small rollbackable pool of possible next narrative directions." : ""
  ], s = [
    t ? "Turn already-confirmed RP source text into concise long-term memory instead of echoing whole chat logs back into the prompt." : "",
    t ? "Update memory only when facts, current state, relationships, constraints, hooks, risks, or near-term carry-forward context actually changed. If nothing material changed, explicitly skip writing." : "",
    n ? "Update the map only when confirmed spatial facts changed: position, boundaries, routes, objects, hazards, or current scene scope." : "",
    r ? "Maintain event directions only as forward-looking possibilities. They are not memory, not old events, and not random surprises." : "",
    n ? "When the user asks about memory, continuity, the map, or backstage materials, answer from evidence. If uncertain, inspect RP source text or existing records instead of guessing." : "When the user asks about memory, continuity, or backstage materials, answer from evidence. If uncertain, inspect RP source text or existing records instead of guessing.",
    "Important facts should land in the appropriate memory file, map record, or atlas record, not only in your final reply."
  ], a = [
    "The current session is the only work scope. Do not operate on other sessions, character cards, worldbook configuration, SillyTavern settings, or plugin source code.",
    [
      t ? "current-session `memory/...` files" : "",
      n ? "current-session map and atlas records" : "",
      r ? "current-session event pool" : "",
      "manager chat history",
      "RP source evidence"
    ].filter(Boolean).join(", ") + " all belong to this session only.",
    "RP source text is the source of truth. Memory files, map records, and atlas records are derived materials; if they conflict, verify with Grep/Read under `chat/` first, then repair the derived material.",
    "Injected context is only a snapshot of current materials, not the full chat history. Treat unread source text as unverified.",
    t ? "The author of `[用户消息]` is not automatically an in-world character. Do not infer a user persona, profile, or player identity from the speaker label." : ""
  ], i = [
    t ? "`[Resident Memory Files]` automatically provides the current global memory file, `memory/state.md`. Character memory files are not all resident; use LS/Grep/Read for relevant `memory/characters/<角色名>.md` files when needed." : "",
    t ? "Automatic after-turn maintenance receives this turn's completed user message and assistant reply. Update memory only when the assistant reply makes a fact or state actually established." : "",
    r ? "`[Current Event Pool]` provides the current active and recently completed event directions for backstage maintenance only. Use it to advance, complete, or decide whether the pool is low." : "",
    "Manual manager chat receives the manager's own conversation history and the current user question. RP source text is not fully preloaded; use Grep/Read under `chat/` when evidence is needed.",
    "Message order and floor numbers are backstage coordinates for evidence and rollback. They are not story time."
  ], l = t ? [
    "Operate on current-session `memory/...` Markdown files only through LS/Grep/Read/Edit/Write.",
    "`memory/state.md` is global memory: current situation, mainline, long-term pressures, unresolved matters, near-term carry-forward context, and hard state that is still true.",
    "`memory/characters/<角色名>.md` is character memory: one file per entity, with the filename as the entity name. It carries that character's long-term state, motivations, secrets, constraints, relationships, arc, promises, debts, risks, and recent related events.",
    '`memory/state.md` is not a directory. Do not write index notes such as "see another file"; it records global facts only.',
    "Edit and Write may write only `memory/state.md` or `memory/characters/<角色名>.md`. Do not create `memory/session.md`, `memory/turns/*.md`, or other memory paths.",
    "Do not create or maintain `memory/characters/User.md`, `memory/characters/Player.md`, `memory/characters/用户.md`, `memory/characters/玩家.md`, or any file whose subject is merely the message author. If player-side durable state matters, keep it in `memory/state.md` unless the RP clearly established a named in-world player character.",
    "These Markdown files are for future model reading and retrieval, not rigid database schemas. Preserve useful headings and keep them clear, editable, and maintainable."
  ] : [], u = [
    [
      "First identify the work type: automatic after-turn maintenance, a user asking about backstage materials",
      t ? ", a user asking to correct memory" : "",
      n ? ", or a user asking to inspect or change the map" : "",
      "."
    ].join(""),
    "Use tools when you need evidence or need to save material. All saves must go through the currently available tools.",
    "Before writing, read the existing record or RP source text, then make the smallest necessary change. Do not rewrite whole sections without a read-backed reason.",
    t ? "In automatic after-turn maintenance, update memory only when this turn's assistant reply confirms a new long-term fact, current state, character change, unresolved matter, or next-turn carry-forward event." : "",
    t ? "Do not write user persona cards, status-bar text, UI metadata, model instructions, or the user message author as story facts unless the assistant reply clearly establishes the fact inside the RP." : "",
    t ? "Write global facts to `memory/state.md`; write character-specific changes to the matching `memory/characters/<角色名>.md`. If nothing material changed, skip writing and say why." : "",
    t && n ? "Map and atlas records are separate from written memory. They do not replace written memory; maintain textual facts and spatial changes in their own places." : "",
    n ? "Spatial records are split in two: `tavern.atlas/main` is the single world index, and each detailed scene map lives in its own place-named `tavern.map/<docId>` document." : "",
    "In manual manager chat, answer the user question first. Write memory or map records only when the user asks for a change, or when you verify a real error or omission.",
    "When a tool fails, adjust the path, arguments, or strategy based on the error. Do not repeat the same failing call unchanged."
  ], d = [
    "LS/Grep/Read inspect Tavern text sources: read-only `chat/`, read-only `worldbooks/`, and current-session `memory/` files. Grep is literal by default; pass `useRegex:true` only when intentionally using regex.",
    t ? "Edit and Write save memory changes only under `memory/state.md` and `memory/characters/<角色名>.md`. `chat/` and `worldbooks/` are evidence sources and are read-only." : "",
    n ? "MapDocs and MapInspect inspect scene maps and the world atlas. MapPatch saves one atomic spatial transaction." : "",
    r ? "EventInspect and EventPatch maintain the event direction pool. It is for future hooks only, not memory and not random encounters." : "",
    "Use tools when exact evidence, current records, ids, line numbers, map revisions, or saved changes matter. If a direct answer is enough, answer directly."
  ], f = [
    t ? "When explaining existing memory or answering a memory question, read the relevant memory file first. If the user is only asking, do not casually modify files." : "",
    t ? "To check memory against RP source text, gather evidence with Grep/Read under `chat/` first, then repair the file with Read or Edit." : "",
    t ? 'Use Grep with `path:"memory/"` to ask whether a fact is already in memory; use Grep with `path:"chat/"` to ask whether something actually happened in the RP source text. Match the search scope to the question.' : "",
    "If you know the message order, use Read on `chat/messages/<order>.md`. If you only know a keyword, use Grep under `chat/`. If you only need recent continuity, use Read on `chat/transcript.md` with `tail`.",
    n ? "When maintaining spatial records, start with MapInspect summary for the relevant docType. For maps use elements/element when you need ids; for atlas use locations/location/links/actors." : "",
    n ? "Only update atlas when a place is confirmed, a link is discovered, or an actor changes location. Only update maps when internal layout or actor coordinates change." : "",
    n ? "Map and atlas records only save confirmed spatial changes from this turn. Unknown rooms, future routes, and unconfirmed details should stay unwritten until RP confirms them." : "",
    t ? "Keep Markdown memory clear and restrained. Update facts that still hold, and do not write guesses, plans, or unconfirmed psychology as settled truth." : "",
    t ? "Message order, floor numbers, and manager run timing are backstage evidence coordinates only. Never record them as in-world time, dates, or story chronology unless the RP text itself says so." : ""
  ], m = n ? [
    "Spatial records have two layers: `tavern.atlas/main` is the single world index, and each `tavern.map/<docId>` is the local scene map for one stable place.",
    "Use `tavern.atlas/main` for place hierarchy, routes, and which location each actor is currently in. Atlas ops are only `upsert-location`, `remove-location`, `upsert-link`, `remove-link`, and `move-actor`.",
    "Use scene maps for the local layout of one place: boundaries, entrances, furniture, landmarks, hazards, items, and actor coordinates inside that place. Name scene-map docIds from the place identity, such as `home`, `office`, or `street_market`.",
    'When the player reaches a new named or trackable place, first update the atlas location and move the player with `move-actor` and `actorKey:"player"`. Then reuse that location’s `mapDocId`; if none exists and a local map is useful, create a new place-named map doc and store its id on the atlas location.',
    "Do not move the atlas player for small movement inside the same current place. For walking across a room, approaching a door, shifting around a street edge, or similar local motion, update the scene map actor coordinates or grow the current map instead.",
    "Map `activate:true` only switches or maintains the map tool’s current doc. It does not change atlas.activeLocationKey, player location, or visited status.",
    "Before scene switches, use MapDocs and the atlas location `mapDocId` to reuse the right scene map. Create a new scene map only for a clearly separate place that does not already have one. Do not overwrite an unrelated scene map just because the current scene changed.",
    'In atlas, create parent hierarchy when confirmed: city/district/building/floor/room/outdoor. Use `status:"mentioned"` for known but unvisited places and `status:"visited"` for explored places. Player movement automatically promotes the target location to visited.',
    "Within the same location or connected short-range spaces, grow the current map: adjacent rooms, yard, doorway, street edge, landmarks, paths, forest edges, or districts. Create/switch docs only for clearly named, independent interiors or distant locations.",
    "Read MapInspect summary first for the candidate doc, inspect `meta.status` and `meta.hint`, then decide whether to initialize, maintain incrementally, activate, or skip.",
    "The map is a spatial relation view of the current scene, not a board of floating text labels. Project confirmed spatial facts into a flat layout with clear boundaries, direction, focus, and proportion.",
    "When reading spatial information, first identify what defines the boundary, where entrances and exits are, where the current player or viewpoint focus is, what occupies interactive space, and which directions remain unexplored.",
    "Place the most certain anchors first, such as outer walls, a main road, a river, corridor edges, or the current location. Place other elements relative to those anchors.",
    "Use the default orientation north-up: north = smaller y, south = larger y, west = smaller x, east = larger x. If narration only gives left/right/front/back, choose a reasonable facing and keep it consistent within one map.",
    "For indoor scenes, use an outer-wall rect as the anchor and place furniture and doors inside it. For outdoor scenes, use roads or rivers as the backbone. For passage scenes, use two parallel boundary lines and a stretched `viewBox`.",
    "`meta.viewBox` is the camera; it does not move map elements. Move the player by changing the player `at`, then adjust `meta.viewBox` only if the camera should follow.",
    'If the chosen scene-map doc is still `uninitialized`, initialize it with one `meta + add` transaction as soon as the current turn clearly establishes that place. First appearance does not require a prior "change".',
    "When you initialize a new scene map for the current place, make sure the atlas has the matching location and that the location points to this map with `mapDocId`.",
    'Initialization must include at least one drawable spatial geometry element (`rect`, `circle`, `path`, `curve`, or `icon`) plus a player actor using `cat:"actor"` and `actorKey:"player"`. `text` is only a short label and cannot replace geometry. Name, `viewBox`, and labels alone are not a valid map.',
    "For scene-map `icon`, use semantic keys directly: chest/table/bed/barrel/door/stairs/portal/skull/trap/fire/tree/rock/water/heart/lips/lovers/cherish/love-letter/locked-heart/perfume when they match the object or mood; use x/o/+ and arrow-n/arrow-s/arrow-e/arrow-w/stairs-up/stairs-down only for abstract markers and direction hints.",
    "When a map already exists, prefer growing the same connected map. Enlarge `meta.viewBox` when the visible scope needs more room.",
    "Let `meta.name` grow with the map scope. For example, a bedroom map may become a house map, then a glade or town-edge map as confirmed connected areas appear.",
    "For a clearly separate place with no useful spatial continuity, switch to an existing doc or create a new doc instead of replacing the previous map. If nothing changes spatially, skip the map update; if unsure, do not change it yet.",
    "Common updates: character movement = modify `at`; discovering a door or route = add door/road; an item appearing = add icon + label; an item being taken away = remove; a door opening = modify style; a new danger = add danger; camera follow = meta viewBox.",
    'Actors use `cat:"actor"` and optional `actorKey`. `actorKey` is the full-session identity key; id is only the element id inside this document. The runtime removes older actors with the same final key from other documents, so do not duplicate the same actor manually.',
    "Use `meta.name` as the map scope title. If you add an area label such as a room, house, road, or district name, attach it to that visible region instead of placing it at the top edge as a second title.",
    "Keep at least 20 units of spacing between elements. Place text labels 15-25 units beside what they describe instead of on top of the shape center.",
    "Before submitting, sanity-check the map: clear anchors, clear focus, drawable elements, a reasonable camera `viewBox`, and enough geometry to carry the map body.",
    "The map should record only spatial facts that already happened and are worth visualizing. Unconfirmed spatial information stays unwritten until confirmed."
  ] : [], v = r ? [
    "EventPatch maintains a rollbackable event engine. It proposes what could happen next; it does not record what already happened and does not merely surface existing foreshadowing.",
    "Allowed EventPatch ops are only `upsert-event`, `advance-event`, `complete-event`, and `abandon-event`.",
    "Advance or complete active directions only when the completed assistant reply actually moved, resolved, or invalidated that direction.",
    "Create new active directions only when the active pool has 0-1 active directions, the story has reached at least floor 5, and the direction uses already established people, places, relationships, world facts, and current tone.",
    "A good new direction recombines established material into an unplayed situation that opens a new interaction space. It MUST introduce a person, place, faction, or situation that has not yet appeared on screen, reached by extending a known character relationship, adjacent place, faction branch, social obligation, secret pressure, or user taste.",
    "Use the story tone and the user's demonstrated tastes as the engine for boldness, not just a filter. In erotic, violent, political, horror, or domestic tones, propose directions with the same appetite and edge the user has been driving; do not sanitize the hook into safe generic mystery.",
    'Each direction needs `horizon`, `current`, and `doneWhen`: `horizon` is the larger not-yet-happened pull, `current` is the immediate playable entrance, and `doneWhen` is the objective completion condition. Write `doneWhen` as a concrete observable event that happens in the story, not an abstract state such as "influence grows" or "the relationship deepens".',
    "Do not create a direction when the hook is generic, disconnected from the current story, a repeat of memory, an existing foreshadowing, an unresolved thread already recorded in memory, a continuation of the current relationship/scene, or a random event tossed in from outside.",
    'Good hookForModel examples: "莉娜提过她母亲一个人住在城东，最近似乎想找人帮忙修房子。", "码头那个走私头子最近缺人手——他不知道你是谁。", or "莉娜的妹妹下周从外地来，听说性格和她完全相反。" Bad examples: "莉娜似乎在刻意避开某个码头名字。", "那枚旧钥匙上的纹路，和办公室门后的标记对得上。", "继续发展和莉娜的关系。", or "发生一个随机袭击。"',
    "`hookForUser` is direct UI text. `hookForModel` is a soft in-world sentence for the RP model; it surfaces the current playable entrance and must not use meta planning language such as quest, goal, objective, completed, or Chinese equivalents.",
    "Stale active event directions are abandoned by the system after your tool work. Do not use EventPatch merely to clean up stale items."
  ] : [], y = t ? [
    "Write memory like a case file for a future model: specific, restrained, and easy to carry forward.",
    "Character psychology, secrets, and future plans become facts only after the RP source text clearly establishes them.",
    "Separate what happened, what the user requested, what you inferred, and what is still unconfirmed. Long-term memory should contain established facts only.",
    "User messages are evidence, not a character sheet. Do not turn the user speaker label, UI status text, or out-of-character preferences into `memory/characters/<角色名>.md`.",
    "When character-specific material makes `memory/state.md` bloated, create or update the matching `memory/characters/<角色名>.md` and keep global memory concise."
  ] : [];
  return [
    "# 小白酒馆后台管理员",
    "",
    Or("Role", o),
    "",
    Or("Responsibilities", s),
    "",
    Or("Current Session", a),
    "",
    Or("Injected Context", i),
    l.length ? `
` + Or("File Discipline", l) : "",
    "",
    Or("Tool Use Guide", ["You may call multiple tools in one assistant turn. Independent reads may run in parallel; edits to the same file should be combined into one edit/patch call.", "If a tool returns an error, adjust the arguments, path, or strategy based on that error."]),
    "",
    Or("Tool Layers", d),
    "",
    Or("Work Loop", u),
    "",
    Or("Selection Strategy", f),
    m.length ? `
` + Or("Map Records", m) : "",
    v.length ? `
` + Or("Event Orchestration", v) : "",
    y.length ? `
` + Or("Memory Tone", y) : "",
    "",
    Or("Output", [
      "Reply with a short, clear, user-facing operation summary.",
      "State what you verified, wrote, skipped, or left unchanged. If nothing changed, say that you checked and why you did not write.",
      "Only expand tool arguments, raw JSON, full Markdown, or protocol details when the user explicitly asks for debugging detail."
    ])
  ].filter(Boolean).join(`
`).replace(/\n{3,}/g, `

`).trim();
}
function vu(e = "") {
  return String(e || "").trim();
}
var K2 = /`?memory\/(?:state\.md|characters\/<角色名>\.md|session\.md|turns\/\*\.md)`?/i, GO = /facts and states that are still true|keep character state|do not keep transient events|旧三页规则|旧整套规则/i;
function ux(e = []) {
  return e.join(`
`).trim();
}
function cx() {
  return ux([
    "Recommended structure:",
    "- `## Story Context`: current mainline, long-term pressure, unresolved hooks, and relationship situation that affects the global scene.",
    "- `## Current State`: time, place, present characters, key items, body/emotion/constraint state.",
    "- `## Recent Carry-Forward Events`: compressed events still in progress or needed next turn, but not yet ready to rewrite as stable long-term conclusions.",
    "",
    "Writing rules:",
    "- Write only facts that affect the global situation, mainline understanding, or immediate scene continuity.",
    '- Do not write a directory or "see another file" notes. Global memory is a fact controller, not an index.',
    "- Character-specific motives, secrets, constraints, relationship arcs, promises, debts, and risks belong in character memory.",
    "- Do not write a floor-by-floor log. When an ongoing event ends, rewrite the still-relevant result into story context, current state, unresolved matters, or character memory; delete process details that no longer matter.",
    "- Skip writing when there is no material global change. Read current global memory before changing it; use Grep/Read under `chat/` when source verification is needed."
  ]);
}
function dx() {
  return ux([
    "Recommended structure:",
    "- `## Current State`: location/presence, body/emotion/constraints, public goal, hidden motive/secret.",
    "- `## Relationships`: toward the player and toward other characters.",
    "- `## Character Arc`: long-term changes that have happened and unresolved inner conflicts.",
    "- `## Promises, Debts, And Risks`: promises, debts, risks.",
    "- `## Recent Related Events`: events specific to this character that still need carry-forward, but are not yet ready to rewrite as long-term conclusions.",
    "",
    "Writing rules:",
    "- Record only what has happened or has been clearly established. Do not write guesses, plans, or unconfirmed psychology as facts.",
    "- Keep one character memory per character. Do not create indexes or per-turn logs.",
    "- Write only when there is a material character change; skip if nothing durable changed.",
    "- If a character fact affects the global mainline or current situation, also copy a brief summary into global memory relationship situation or current state.",
    "- Read the target character memory before modifying it; use Grep/Read under `chat/` when source verification is needed."
  ]);
}
function VO() {
  return "Maintain the current session's global long-term memory in `memory/state.md`. This target path and responsibility are fixed system contract; the user-editable preset only defines the file's internal format, content scope, and selection rules.";
}
function KO() {
  return "Maintain current-session character long-term memory in `memory/characters/<角色名>.md`. Use one file per character, with the filename as the entity name; the user-editable preset only defines the file's internal format, content scope, and selection rules.";
}
function J2(e, t) {
  const n = vu(e);
  return !n || GO.test(n) ? t : K2.test(n) ? n.split(/\r?\n/).filter((r) => !K2.test(r)).join(`
`).trim() || t : n;
}
function JO(e = {}, t = {}) {
  const { includeMemory: n } = lx(t), r = vu(e.statePrompt) || cx(), o = vu(e.characterPrompt) || dx(), s = [WO(t)];
  return n && (r && s.push("", "## Global Memory Rules", VO(), r), o && s.push("", "## Character Memory Rules", KO(), o)), s.join(`
`).trim();
}
function YO(e = {}, t = {}) {
  return JO(e, t);
}
function Fd() {
  return {
    id: ig,
    name: "默认助手预设",
    description: "记忆管理员的默认维护规则。",
    statePrompt: cx(),
    characterPrompt: dx()
  };
}
function cb(e = {}) {
  const t = Fd();
  return {
    id: vu(e.id) || t.id,
    name: vu(e.name) || t.name,
    description: String(e.description || ""),
    statePrompt: J2(e.statePrompt, t.statePrompt),
    characterPrompt: J2(e.characterPrompt, t.characterPrompt),
    updatedAt: Number(e.updatedAt) || void 0
  };
}
var XO = "sillytavern-current-chat-prompt";
function fx() {
  return {
    id: XO,
    name: "酒馆当前聊天预设",
    source: "sillytavern",
    selected: !0,
    sections: []
  };
}
function QO(e = {}) {
  return {
    id: String(e.id || "").trim(),
    label: String(e.label || "").trim(),
    enabled: e.enabled !== !1,
    marker: e.marker === !0,
    role: e.role || "system",
    content: String(e.content || "").trim(),
    placement: e.placement || "beforeHistory",
    source: String(e.source || "manual").trim()
  };
}
function dh(e = {}) {
  const t = fx();
  return {
    id: String(e.id || t.id).trim() || t.id,
    name: String(e.name || t.name).trim() || t.name,
    description: String(e.description || ""),
    version: String(e.version || ""),
    source: String(e.source || t.source || "sillytavern"),
    selected: e.selected !== !1,
    promptManager: e.promptManager,
    systemPrompt: e.systemPrompt,
    contextTemplate: e.contextTemplate,
    instructTemplate: e.instructTemplate,
    historySeparator: String(e.historySeparator || ""),
    sections: Array.isArray(e.sections) ? e.sections.map(QO).filter((n) => n.content || n.marker) : [],
    updatedAt: Number(e.updatedAt) || void 0
  };
}
var hx = "tavern.map", tm = "main", px = "__label__";
function $_() {
  return [
    "This scene map is empty. Use it only after it has been chosen for a specific confirmed place, then initialize it with one minimal MapPatch. Use `activate:true` only to switch the map tool doc, not to move the player; player location belongs in `tavern.atlas/main` via move-actor.",
    'Indoor example: {"activate":true,"ops":[{"op":"meta","set":{"name":"<Place Name>","viewBox":[0,0,400,300],"status":"active"}},{"op":"add","element":{"id":"wall","at":[40,40],"rect":[320,220],"cat":"wall"}},{"op":"add","element":{"id":"door","at":[200,260],"icon":"o","cat":"door"}},{"op":"add","element":{"id":"player-room","actorKey":"player","at":[200,180],"icon":"o","text":"Player","cat":"actor"}}]}',
    'Outdoor example: {"activate":true,"ops":[{"op":"meta","set":{"name":"<Place Name>","viewBox":[0,0,800,600],"status":"active"}},{"op":"add","element":{"id":"ground","at":[400,300],"circle":150,"cat":"terrain"}},{"op":"add","element":{"id":"path","at":[0,300],"path":[[800,0]],"cat":"road"}},{"op":"add","element":{"id":"player-road","actorKey":"player","at":[400,320],"icon":"o","text":"Player","cat":"actor"}}]}',
    "Requirement: include at least one spatial geometry element (`rect`, `circle`, `path`, `curve`, or `icon`). Do not send only `text` labels. After initialization, use only incremental `add` / `modify` / `remove` ops. If nothing changes spatially, skip the map update instead of resetting the document.",
    "Use MapDocs and MapInspect before scene switches. For connected places, grow the same document by adding adjacent spaces and enlarging `meta.viewBox`; for clearly separate named locations, find or create a place-named docId and link it from the atlas location `mapDocId` instead of replacing an old map.",
    "For `icon`, prefer semantic keys such as door/chest/table/bed/barrel/skull/trap/fire/tree/rock/water/heart/lips/lovers/cherish/love-letter/locked-heart/perfume; reserve x/o/+ and arrows for abstract markers.",
    "Use `meta.name` as the map scope title. Area labels are allowed only when they mark a concrete visible region; place them inside or next to that region, not at the top edge as a second title.",
    "`viewBox` is the camera. Changing it does not move any element. Move the player by updating the player `at`, then adjust `meta.viewBox` only if the camera should follow.",
    'Elements use `at:[x,y]` plus one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. With `at`, `path` and `curve` points are relative offsets. Without `at`, points are absolute coordinates and the first point becomes the anchor. For `cat:"actor"`, use `actorKey` as the full-session identity key; if omitted, id is the fallback.'
  ].join(" ");
}
function yu() {
  return {
    meta: {
      name: null,
      viewBox: null,
      theme: "parchment",
      status: "uninitialized",
      hint: $_()
    },
    elements: []
  };
}
function nm(e) {
  return String(e || "").startsWith(px);
}
function rm(e) {
  return `${px}${e}`;
}
function ZO(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? !1 : e.meta?.status === "uninitialized";
}
var mx = "tavern.atlas", gx = "main";
function vx() {
  return {
    version: 1,
    locations: [],
    links: [],
    actors: []
  };
}
var lc = {
  memoryArchiving: !0,
  cartographyEngine: !0,
  actionChecks: !0,
  randomEncounters: !1,
  questOrchestration: !1
}, yx = [
  "memoryArchiving",
  "cartographyEngine",
  "actionChecks",
  "randomEncounters",
  "questOrchestration"
], jO = {
  "silent-operations": "暗中行事",
  "fate-arbitration": "命运仲裁",
  "narrative-orchestration": "叙事编排"
}, D_ = [
  {
    key: "memoryArchiving",
    tier: "silent-operations",
    icon: "🧠",
    title: "记忆存档",
    summary: "",
    description: "授权代理人管理你的长期记忆。它将默默维护、压缩和召回上下文——确保故事永不遗忘。"
  },
  {
    key: "cartographyEngine",
    tier: "silent-operations",
    icon: "🗺️",
    title: "制图引擎",
    summary: "",
    description: "允许代理人在你探索时生成、更新和渲染空间数据。随着叙事展开，区域、地标和兴趣点将在附录面板中逐渐显现。"
  },
  {
    key: "actionChecks",
    tier: "fate-arbitration",
    icon: "🎲",
    title: "行动检定",
    summary: "",
    description: "当你尝试不确定的事——说服陌生人、翻越高墙、破译符文——由骰子裁决，而非AI。一次真随机掷骰仲裁结果，故事顺从命运。"
  },
  {
    key: "randomEncounters",
    tier: "fate-arbitration",
    icon: "⚔️",
    title: "随机遭遇",
    summary: "",
    description: "允许世界给你意外。每隔一段时间，一颗无形的骰子在暗中滚动。当它落定，意料之外的事降临——路上的伏击、天空中的异兆、门缝下塞进的一封信。"
  },
  {
    key: "questOrchestration",
    tier: "narrative-orchestration",
    icon: "📜",
    title: "织线者",
    summary: "",
    description: "让代理人感知你故事中的暗流，浮现接下来可能发生的事。当势头停滞，一根线索显现——追随它、忽视它，或任它消散。"
  }
], eF = {
  memoryArchiving: {
    includeMemoryFiles: !0,
    automaticManagerWork: !0,
    managerPromptMemory: !0
  },
  cartographyEngine: {
    includeStructuredStates: !0,
    automaticManagerWork: !0,
    managerPromptCartography: !0
  },
  actionChecks: { includeActionChecks: !0 },
  randomEncounters: { includeRandomEncounters: !0 },
  questOrchestration: { automaticManagerWork: !0 }
};
function mo(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {};
  return {
    memoryArchiving: "memoryArchiving" in t ? !!t.memoryArchiving : lc.memoryArchiving,
    cartographyEngine: "cartographyEngine" in t ? !!t.cartographyEngine : lc.cartographyEngine,
    actionChecks: "actionChecks" in t ? !!t.actionChecks : lc.actionChecks,
    randomEncounters: "randomEncounters" in t ? !!t.randomEncounters : lc.randomEncounters,
    questOrchestration: "questOrchestration" in t ? !!t.questOrchestration : lc.questOrchestration
  };
}
function lg(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? !1 : yx.some((t) => Object.prototype.hasOwnProperty.call(e, t));
}
function om(e, t) {
  return mo({
    ...mo(e),
    ...lg(t) ? t : {}
  });
}
function tF(e) {
  const t = mo(e);
  return Object.values(t).filter(Boolean).length;
}
function nF(e) {
  return D_.find((t) => t.key === e);
}
function ug(e) {
  const t = mo(e), n = yx.filter((r) => t[r]);
  return n.reduce((r, o) => {
    const s = eF[o];
    return s ? {
      ...r,
      includeMemoryFiles: r.includeMemoryFiles || s.includeMemoryFiles === !0,
      includeStructuredStates: r.includeStructuredStates || s.includeStructuredStates === !0,
      includeActionChecks: r.includeActionChecks || s.includeActionChecks === !0,
      includeRandomEncounters: r.includeRandomEncounters || s.includeRandomEncounters === !0,
      includeQuestOrchestration: r.includeQuestOrchestration || o === "questOrchestration",
      hasAutomaticManagerWork: r.hasAutomaticManagerWork || s.automaticManagerWork === !0,
      managerPromptOptions: {
        includeMemory: r.managerPromptOptions.includeMemory || s.managerPromptMemory === !0,
        includeCartography: r.managerPromptOptions.includeCartography || s.managerPromptCartography === !0,
        includeQuestOrchestration: r.managerPromptOptions.includeQuestOrchestration || o === "questOrchestration"
      }
    } : r;
  }, {
    contract: t,
    enabledKeys: n,
    includeMemoryFiles: !1,
    includeStructuredStates: !1,
    includeActionChecks: !1,
    includeRandomEncounters: !1,
    includeQuestOrchestration: !1,
    hasAutomaticManagerWork: !1,
    managerPromptOptions: {
      includeMemory: !1,
      includeCartography: !1,
      includeQuestOrchestration: !1
    }
  });
}
var rF = class extends gu {
  sessions;
  messages;
  managerMessages;
  meta;
  presets;
  managerRuns;
  memoryFiles;
  memorySnapshots;
  memoryIndexes;
  assistantPresets;
  managerMemorySnapshots;
  stateDocuments;
  statePatches;
  managerStateSnapshots;
  tasks;
  taskSnapshots;
  managerTaskSnapshots;
  taskFingerprintStates;
  constructor() {
    super("LittleWhiteBox_Tavern"), this.version(1).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      turnSummaries: "id, sessionId, episodeId, turn, userOrder, assistantOrder, status, updatedAt",
      episodeSummaries: "id, sessionId, status, updatedAt, startTurn, endTurn",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(2).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      turnSummaries: "id, sessionId, turn, userOrder, assistantOrder, status, updatedAt",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(3).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(4).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(5).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: null,
      memorySnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(6).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: null,
      memorySnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt",
      tasks: "id, sessionId, status, fingerprint, updatedOrder, updatedAt",
      taskSnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      managerTaskSnapshots: "managerRunId, sessionId, updatedAt",
      taskFingerprintStates: "sessionId, updatedAt"
    }), this.version(7).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: null,
      memorySnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt",
      tasks: "[sessionId+id], sessionId, status, fingerprint, updatedOrder, updatedAt",
      taskSnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      managerTaskSnapshots: "managerRunId, sessionId, updatedAt",
      taskFingerprintStates: "sessionId, updatedAt"
    });
  }
}, Ot = new rF(), Et = Ot.sessions, Yt = Ot.messages, jr = Ot.managerMessages, Ys = Ot.meta, whe = Ot.presets, jn = Ot.managerRuns, hn = Ot.memoryFiles, is = Ot.memorySnapshots, eo = Ot.memoryIndexes, Gi = Ot.assistantPresets, to = Ot.managerMemorySnapshots, or = Ot.stateDocuments, Fi = Ot.statePatches, ly = Ot.managerStateSnapshots, Tn = Ot.tasks, ls = Ot.taskSnapshots, Cr = Ot.managerTaskSnapshots, us = Ot.taskFingerprintStates, br = gu;
function Zt() {
  return Date.now();
}
function cg(e = "tavern-session") {
  return `${e}-${Zt()}-${Math.random().toString(36).slice(2, 8)}`;
}
function oF(e = "", t = "小白酒馆会话") {
  return String(e || "").trim().slice(0, 120) || t;
}
function Y2(e = "") {
  const t = String(e || "").replace(/\s*[·-]\s*小白酒馆\s*$/g, "").replace(/\s*[·-]\s*会话\s*$/g, "").replace(/^小白酒馆会话$/g, "").replace(/\s*·\s*第\s*\d+\s*轮\s*·\s*\d+\s*条可用消息\s*$/g, "").trim();
  return /^(sillytavern\s+system|system)\b/i.test(t) ? "" : t;
}
function sF(e = tm) {
  const t = String(e || "main").trim() || "main";
  return /^[\w.-]{1,80}$/i.test(t) ? t : tm;
}
function aF(e) {
  return JSON.parse(JSON.stringify(e));
}
function Jt(e, t) {
  if (e === void 0) return t;
  try {
    return JSON.parse(JSON.stringify(e));
  } catch {
    return t;
  }
}
function Bi(e, t = 12) {
  return (Array.isArray(e) ? e : typeof e == "string" ? [e] : []).map((n) => String(n || "").trim()).filter(Boolean).slice(0, t);
}
function iF(e) {
  const t = String(e || "").trim();
  return t === "assistant" || t === "tool" || t === "system" ? t : "user";
}
function _x(e) {
  const t = Array.isArray(e.toolCalls) ? e.toolCalls : [], n = "tool_calls" in e && Array.isArray(e.tool_calls) ? e.tool_calls : [], r = [...t.map((o) => ({
    id: String(o?.id || ""),
    name: String(o?.name || ""),
    arguments: String(o?.arguments || "{}")
  })), ...n.map((o) => ({
    id: String(o?.id || ""),
    name: String(o?.function?.name || ""),
    arguments: String(o?.function?.arguments || "{}")
  }))].filter((o) => o.name);
  return r.length ? r : void 0;
}
function db(e) {
  return M0(e);
}
function ms(e) {
  return {
    ...e,
    runtimeEvents: db(e.runtimeEvents)
  };
}
function bx(e) {
  return [
    "queued",
    "running",
    "completed",
    "failed",
    "cancelled",
    "superseded",
    "rolled_back"
  ].includes(String(e || "")) ? e : "queued";
}
function lF(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return {};
  const t = {};
  return Object.entries(e).forEach(([n, r]) => {
    if (!n || !r || typeof r != "object" || Array.isArray(r)) return;
    const o = {}, s = r;
    [
      "stickyUntilTurn",
      "cooldownUntilTurn",
      "delayUntilTurn"
    ].forEach((a) => {
      const i = Number(s[a]);
      Number.isFinite(i) && (o[a] = i);
    }), Object.keys(o).length && (t[n] = o);
  }), t;
}
function uF(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return null;
  const t = e, n = {}, r = Number(t.hash), o = Number(t.start), s = Number(t.end);
  return Number.isFinite(r) && (n.hash = r), Number.isFinite(o) && (n.start = o), Number.isFinite(s) && (n.end = s), t.protected === !0 && (n.protected = !0), n.hash !== void 0 || n.start !== void 0 || n.end !== void 0 || n.protected ? n : null;
}
function cF(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return {
    sticky: {},
    cooldown: {}
  };
  const t = e, n = (r) => {
    if (!r || typeof r != "object" || Array.isArray(r)) return {};
    const o = {};
    return Object.entries(r).forEach(([s, a]) => {
      const i = uF(a);
      s && i && (o[s] = i);
    }), o;
  };
  return {
    sticky: n(t.sticky),
    cooldown: n(t.cooldown)
  };
}
function sr(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {}, n = sF(t.activeMapDocId || "main"), r = {
    ...t,
    turn: Math.max(0, Number(t.turn) || 0),
    contextWindowStartOrder: Math.max(0, Math.floor(Number(t.contextWindowStartOrder) || 0)),
    activeMapDocId: n,
    contract: mo(t.contract),
    worldEntryStates: lF(t.worldEntryStates),
    nativeWorldInfoTimedState: cF(t.nativeWorldInfoTimedState)
  };
  return xa(t, "autoManagerEpoch") ? r.autoManagerEpoch = Math.max(0, Math.floor(Number(t.autoManagerEpoch) || 0)) : delete r.autoManagerEpoch, r;
}
function xa(e, t) {
  return !!e && typeof e == "object" && !Array.isArray(e) && Object.prototype.hasOwnProperty.call(e, t);
}
function fb(e = {}, t = {}) {
  const n = aF(e || {});
  return Object.entries(t || {}).forEach(([r, o]) => {
    !r || !o || typeof o != "object" || (n[r] = {
      ...n[r] || {},
      ...o
    });
  }), n;
}
async function wx(e = {}) {
  const t = Zt(), n = Y2(e.characterName || ""), r = Y2(e.title || ""), o = {
    id: String(e.id || cg()),
    title: oF(r, n || "未选择角色"),
    characterId: String(e.characterId || ""),
    characterName: n,
    createdAt: Number(e.createdAt) || t,
    updatedAt: t,
    contextSnapshot: Jt(e.contextSnapshot, void 0),
    buildSnapshot: Jt(e.buildSnapshot, void 0),
    chatPresetId: String(e.chatPresetId || e.presetId || ""),
    chatPresetName: String(e.chatPresetName || e.presetName || ""),
    presetId: String(e.presetId || ""),
    presetName: String(e.presetName || ""),
    summary: String(e.summary || ""),
    state: Jt(sr(e.state || {}), {})
  };
  return await Ot.transaction("rw", Et, Ys, or, async () => {
    await Et.put(o), await Ys.put({
      key: "selectedSessionId",
      value: o.id,
      updatedAt: t
    }), await lf(o.id, { touchSession: !1 });
  }), o;
}
async function dF() {
  return Et.orderBy("updatedAt").reverse().toArray();
}
async function fF() {
  const e = await Ys.get("selectedSessionId");
  return String(e?.value || "").trim();
}
async function Sl(e = "") {
  const t = String(e || "").trim();
  return await Ys.put({
    key: "selectedSessionId",
    value: t,
    updatedAt: Zt()
  }), t;
}
async function fs(e = "") {
  const t = String(e || "").trim();
  return t && await Et.get(t) || null;
}
async function hF(e = "") {
  const t = String(e || "").trim();
  return !t || !await fs(t) ? 0 : (await Ot.transaction("rw", Et, Yt, jr, jn, to, ly, Cr, hn, is, eo, or, Fi, Tn, ls, us, Ys, async () => {
    const [n, r, o, s, a, i, l, u, d, f, m, v, y, T] = await Promise.all([
      Yt.where("sessionId").equals(t).toArray(),
      jr.where("sessionId").equals(t).toArray(),
      jn.where("sessionId").equals(t).toArray(),
      to.where("sessionId").equals(t).toArray(),
      ly.where("sessionId").equals(t).toArray(),
      Cr.where("sessionId").equals(t).toArray(),
      hn.where("sessionId").equals(t).toArray(),
      is.where("sessionId").equals(t).toArray(),
      eo.where("sessionId").equals(t).toArray(),
      or.where("sessionId").equals(t).toArray(),
      Fi.where("sessionId").equals(t).toArray(),
      Tn.where("sessionId").equals(t).toArray(),
      ls.where("sessionId").equals(t).toArray(),
      us.where("sessionId").equals(t).toArray()
    ]);
    await Promise.all([
      n.length ? Yt.bulkDelete(n.map((A) => [A.sessionId, A.order])) : 0,
      r.length ? jr.bulkDelete(r.map((A) => [A.sessionId, A.order])) : 0,
      o.length ? jn.bulkDelete(o.map((A) => A.id)) : 0,
      s.length ? to.bulkDelete(s.map((A) => [A.managerRunId, A.path])) : 0,
      a.length ? ly.bulkDelete(a.map((A) => [
        A.managerRunId,
        A.docType,
        A.docId
      ])) : 0,
      i.length ? Cr.bulkDelete(i.map((A) => A.managerRunId)) : 0,
      l.length ? hn.bulkDelete(l.map((A) => [A.sessionId, A.path])) : 0,
      u.length ? is.bulkDelete(u.map((A) => [A.sessionId, A.floor])) : 0,
      d.length ? eo.bulkDelete(d.map((A) => [A.sessionId, A.kind])) : 0,
      f.length ? or.bulkDelete(f.map((A) => [
        A.sessionId,
        A.docType,
        A.docId
      ])) : 0,
      m.length ? Fi.bulkDelete(m.map((A) => A.id)) : 0,
      v.length ? Tn.bulkDelete(v.map((A) => [A.sessionId, A.id])) : 0,
      y.length ? ls.bulkDelete(y.map((A) => [A.sessionId, A.floor])) : 0,
      T.length ? us.bulkDelete(T.map((A) => A.sessionId)) : 0
    ]), await Et.delete(t);
    const C = await Ys.get("selectedSessionId");
    if (String(C?.value || "") === t) {
      const [A] = await Et.orderBy("updatedAt").reverse().toArray();
      await Ys.put({
        key: "selectedSessionId",
        value: A?.id || "",
        updatedAt: Zt()
      });
    }
  }), 1);
}
async function Sx(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await fs(n);
  if (!r) return null;
  const o = Zt(), s = sr(r.state || {}), a = sr(t), i = {
    ...s,
    ...t,
    turn: Math.max(0, Number(t.turn ?? s.turn) || 0),
    autoManagerEpoch: xa(t, "autoManagerEpoch") ? Math.max(0, Math.floor(Number(a.autoManagerEpoch) || 0)) : s.autoManagerEpoch,
    contract: xa(t, "contract") ? om(s.contract, lg(t.contract) ? t.contract : void 0) : s.contract,
    worldEntryStates: fb(s.worldEntryStates || {}, a.worldEntryStates || {}),
    nativeWorldInfoTimedState: xa(t, "nativeWorldInfoTimedState") ? a.nativeWorldInfoTimedState : s.nativeWorldInfoTimedState,
    activeMapDocId: xa(t, "activeMapDocId") ? a.activeMapDocId : s.activeMapDocId
  };
  return await Et.update(n, {
    state: Jt(i, {}),
    updatedAt: o,
    buildSnapshot: Jt(t.lastBuildSnapshot || r.buildSnapshot, void 0)
  }), await fs(n);
}
async function Tx(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await fs(n);
  if (!r) return null;
  const o = Zt(), s = sr(r.state || {}), a = sr(t), i = {
    ...t,
    turn: Math.max(0, Number(a.turn) || 0),
    autoManagerEpoch: xa(t, "autoManagerEpoch") ? Math.max(0, Math.floor(Number(a.autoManagerEpoch) || 0)) : s.autoManagerEpoch,
    activeMapDocId: xa(t, "activeMapDocId") ? a.activeMapDocId : s.activeMapDocId || "main",
    contract: xa(t, "contract") ? om(s.contract, lg(t.contract) ? t.contract : void 0) : s.contract,
    worldEntryStates: a.worldEntryStates || {},
    nativeWorldInfoTimedState: a.nativeWorldInfoTimedState
  };
  return await Et.update(n, {
    state: Jt(i, {}),
    updatedAt: o,
    buildSnapshot: Jt(i.lastBuildSnapshot || r.buildSnapshot, void 0)
  }), await fs(n);
}
async function Ex(e = "") {
  const t = await fs(e);
  return Math.max(0, Math.floor(Number(sr(t?.state || {}).autoManagerEpoch) || 0));
}
async function pF(e = "") {
  const t = String(e || "").trim();
  return t ? await Ot.transaction("rw", Et, async () => {
    const n = await Et.get(t);
    if (!n) return 0;
    const r = sr(n.state || {}), o = Math.max(0, Math.floor(Number(r.autoManagerEpoch) || 0)) + 1;
    return await Et.update(t, {
      state: Jt({
        ...r,
        autoManagerEpoch: o
      }, {}),
      updatedAt: Zt()
    }), o;
  }) : 0;
}
async function L_(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await fs(n);
  if (!r) return null;
  const o = "contextSnapshot" in t ? t.contextSnapshot : r.contextSnapshot, s = o?.character || {}, a = {
    updatedAt: Zt(),
    contextSnapshot: Jt(o, void 0),
    buildSnapshot: Jt("buildSnapshot" in t ? t.buildSnapshot : r.buildSnapshot, void 0),
    chatPresetId: "chatPresetId" in t ? String(t.chatPresetId || "") : r.chatPresetId,
    chatPresetName: "chatPresetName" in t ? String(t.chatPresetName || "") : r.chatPresetName,
    presetId: "presetId" in t ? String(t.presetId || "") : r.presetId,
    presetName: "presetName" in t ? String(t.presetName || "") : r.presetName,
    characterId: "characterId" in t ? String(t.characterId || "") : String(s.id || r.characterId || ""),
    characterName: "characterName" in t ? String(t.characterName || "") : String(s.name || r.characterName || "")
  };
  return await Et.update(n, a), await fs(n);
}
async function Oc(e, t) {
  const n = String(e || "").trim();
  if (!n) throw new Error("session_required");
  const r = Zt();
  let o = null;
  if (await Ot.transaction("rw", Yt, Et, async () => {
    const s = await kx(n);
    o = {
      sessionId: n,
      order: (s ? Math.floor(Number(s.order)) : -1) + 1,
      role: String(t.role || ""),
      content: String(t.content || ""),
      name: t.name ? String(t.name) : void 0,
      error: t.error === !0,
      createdAt: r,
      provider: "provider" in t ? String(t.provider || "") : void 0,
      model: "model" in t ? String(t.model || "") : void 0,
      finishReason: "finishReason" in t ? String(t.finishReason || "") : void 0,
      thoughts: "thoughts" in t ? Jt(t.thoughts, void 0) : void 0,
      providerPayload: "providerPayload" in t ? Jt(t.providerPayload, void 0) : void 0,
      contextSnapshot: "contextSnapshot" in t ? Jt(t.contextSnapshot, void 0) : void 0,
      buildSnapshot: "buildSnapshot" in t ? Jt(t.buildSnapshot, void 0) : void 0,
      chatPresetId: "chatPresetId" in t ? String(t.chatPresetId || "") : String(t.presetId || ""),
      chatPresetName: "chatPresetName" in t ? String(t.chatPresetName || "") : String(t.presetName || ""),
      presetId: "presetId" in t ? String(t.presetId || "") : void 0,
      presetName: "presetName" in t ? String(t.presetName || "") : void 0,
      requestSnapshot: "requestSnapshot" in t ? Jt(t.requestSnapshot, void 0) : void 0,
      runtimeEvents: "runtimeEvents" in t ? db(t.runtimeEvents) : []
    }, await Yt.put(o), await Et.update(n, { updatedAt: r });
  }), !o) throw new Error("message_append_failed");
  return ms(o);
}
async function fd(e = "", t = -1, n) {
  const r = String(e || "").trim(), o = Number(t);
  if (!r || !Number.isInteger(o) || o < 0 || !await Yt.get([r, o])) return null;
  const s = {};
  "content" in n && (s.content = String(n.content || "")), "error" in n && (s.error = n.error === !0), "thoughts" in n && (s.thoughts = Jt(n.thoughts, void 0)), "runtimeEvents" in n && (s.runtimeEvents = db(n.runtimeEvents)), "contextSnapshot" in n && (s.contextSnapshot = Jt(n.contextSnapshot, void 0)), "buildSnapshot" in n && (s.buildSnapshot = Jt(n.buildSnapshot, void 0)), "chatPresetId" in n && (s.chatPresetId = String(n.chatPresetId || "")), "chatPresetName" in n && (s.chatPresetName = String(n.chatPresetName || "")), "presetId" in n && (s.presetId = String(n.presetId || "")), "presetName" in n && (s.presetName = String(n.presetName || "")), "requestSnapshot" in n && (s.requestSnapshot = Jt(n.requestSnapshot, void 0)), "provider" in n && (s.provider = String(n.provider || "")), "model" in n && (s.model = String(n.model || "")), "finishReason" in n && (s.finishReason = String(n.finishReason || "")), await Yt.update([r, o], s), await Et.update(r, { updatedAt: Zt() });
  const a = await Yt.get([r, o]);
  return a ? ms(a) : null;
}
async function Ax(e = "", t = []) {
  const n = String(e || "").trim(), r = [...new Set((Array.isArray(t) ? t : []).map((s) => Number(s)).filter((s) => Number.isInteger(s) && s >= 0))];
  if (!n || !r.length) return 0;
  const o = [];
  return await Promise.all(r.map(async (s) => {
    await Yt.get([n, s]) && o.push([n, s]);
  })), o.length ? (await Ot.transaction("rw", Yt, Et, async () => {
    await Yt.bulkDelete(o), await Et.update(n, { updatedAt: Zt() });
  }), o.length) : 0;
}
async function Cx(e = "") {
  const t = String(e || "").trim();
  return t ? (await Yt.where("sessionId").equals(t).sortBy("order")).map(ms) : [];
}
async function _u(e = "", t = -1) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isInteger(r) || r < 0) return null;
  const o = await Yt.get([n, r]);
  return o ? ms(o) : null;
}
async function kx(e = "") {
  const t = String(e || "").trim();
  if (!t) return null;
  const n = await Yt.where("[sessionId+order]").between([t, br.minKey], [t, br.maxKey]).reverse().first();
  return n ? ms(n) : null;
}
async function hb(e = "") {
  const t = String(e || "").trim();
  if (!t) return null;
  const n = await Yt.where("[sessionId+order]").between([t, br.minKey], [t, br.maxKey]).reverse().filter((r) => r.role === "assistant" && r.error !== !0).first();
  return n ? Math.floor(Number(n.order)) : null;
}
async function dg(e = "", t = 12, n = 0) {
  const r = String(e || "").trim();
  if (!r) return {
    messages: [],
    total: 0
  };
  const o = Math.max(1, Math.min(1e3, Math.floor(Number(t) || 12))), s = Math.max(0, Math.floor(Number(n) || 0));
  return await Ot.transaction("r", Yt, async () => {
    const [a, i] = await Promise.all([Yt.where("[sessionId+order]").between([r, br.minKey], [r, br.maxKey]).reverse().offset(s).limit(o).toArray(), Yt.where("sessionId").equals(r).count()]);
    return {
      messages: a.reverse().map(ms),
      total: i
    };
  });
}
async function mF(e = "", t = 12, n = 0) {
  const r = await dg(e, t, n);
  return {
    ...r,
    loadedStartOrder: r.messages[0]?.order ?? null,
    loadedEndOrder: r.messages.at(-1)?.order ?? null
  };
}
async function gF(e = "", t = 0, n = Number.POSITIVE_INFINITY, r = 1e3, o = 0) {
  const s = String(e || "").trim();
  if (!s) return [];
  const a = Math.max(0, Math.floor(Number(t) || 0)), i = Number.isFinite(Number(n)), l = i ? Math.max(a, Math.floor(Number(n) || a)) : Number.POSITIVE_INFINITY, u = i ? l : br.maxKey, d = Math.max(1, Math.min(1e3, Math.floor(Number(r) || 1e3))), f = Math.max(0, Math.floor(Number(o) || 0));
  return (await Yt.where("[sessionId+order]").between([s, a], [s, u], !0, !0).offset(f).limit(d).toArray()).map(ms);
}
async function nu(e = "", t = 0, n = Number.POSITIVE_INFINITY, r = 1e3, o = 0) {
  const s = String(e || "").trim();
  if (!s) return {
    messages: [],
    total: 0
  };
  const a = Math.max(0, Math.floor(Number(t) || 0)), i = Number.isFinite(Number(n)), l = i ? Math.max(a, Math.floor(Number(n) || a)) : Number.POSITIVE_INFINITY, u = i ? l : br.maxKey, d = Math.max(1, Math.min(1e3, Math.floor(Number(r) || 1e3))), f = Math.max(0, Math.floor(Number(o) || 0));
  return await Ot.transaction("r", Yt, async () => {
    const m = () => Yt.where("[sessionId+order]").between([s, a], [s, u], !0, !0), [v, y] = await Promise.all([m().offset(f).limit(d).toArray(), m().count()]);
    return {
      messages: v.map(ms),
      total: y
    };
  });
}
async function pb(e = "", t = 0) {
  const n = String(e || "").trim(), r = Math.max(0, Math.floor(Number(t) || 0));
  return n ? (await Yt.where("[sessionId+order]").between([n, r], [n, br.maxKey], !0, !0).primaryKeys()).map((o) => Array.isArray(o) ? o[1] : void 0).map((o) => Math.floor(Number(o))).filter((o) => Number.isInteger(o) && o >= 0) : [];
}
async function X2(e = "", t = Number.POSITIVE_INFINITY, n = 5) {
  const r = String(e || "").trim();
  if (!r) return [];
  const o = Math.max(1, Math.min(100, Math.floor(Number(n) || 5))), s = Number.isFinite(Number(t)), a = Math.floor(Number(t) || 0);
  if (s && a <= 0) return [];
  const i = s ? Math.max(0, a - 1) : br.maxKey;
  return (await Yt.where("[sessionId+order]").between([r, br.minKey], [r, i], !0, !0).reverse().filter((l) => l.role === "user").limit(o).toArray()).reverse().map(ms);
}
async function vF(e = "", t = Number.POSITIVE_INFINITY) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = Number.isFinite(Number(t)) ? Math.max(0, Math.floor(Number(t) || 0)) : br.maxKey, o = await Yt.where("[sessionId+order]").between([n, br.minKey], [n, r], !0, !0).reverse().filter((s) => s.role === "user").first();
  return o ? ms(o) : null;
}
async function yF(e = "") {
  const t = String(e || "").trim();
  return t ? Yt.where("sessionId").equals(t).count() : 0;
}
async function _F(e = "") {
  const t = String(e || "").trim();
  return t && await jr.where("[sessionId+order]").between([t, br.minKey], [t, br.maxKey]).reverse().first() || null;
}
async function fh(e, t) {
  const n = String(e || "").trim();
  if (!n) throw new Error("session_required");
  const r = Zt();
  let o = null;
  if (await Ot.transaction("rw", jr, Et, async () => {
    const s = await _F(n);
    o = {
      sessionId: n,
      order: (s ? Math.floor(Number(s.order)) : -1) + 1,
      role: iF(t.role),
      content: String(t.content || ""),
      name: t.name ? String(t.name) : void 0,
      error: t.error === !0,
      createdAt: r,
      updatedAt: r,
      provider: "provider" in t ? String(t.provider || "") : void 0,
      model: "model" in t ? String(t.model || "") : void 0,
      finishReason: "finishReason" in t ? String(t.finishReason || "") : void 0,
      thoughts: "thoughts" in t ? Jt(t.thoughts, void 0) : void 0,
      providerPayload: "providerPayload" in t ? Jt(t.providerPayload, void 0) : void 0,
      toolCalls: _x(t),
      toolCallId: String(t.toolCallId || t.tool_call_id || "").trim() || void 0,
      toolName: String(t.toolName || "").trim() || void 0,
      toolDisplay: "toolDisplay" in t ? Jt(t.toolDisplay, void 0) : void 0
    }, await jr.put(o), await Et.update(n, { updatedAt: r });
  }), !o) throw new Error("manager_message_append_failed");
  return o;
}
async function bF(e = "", t = -1, n) {
  const r = String(e || "").trim(), o = Number(t);
  if (!r || !Number.isInteger(o) || o < 0 || !await jr.get([r, o])) return null;
  const s = Zt(), a = { updatedAt: s };
  return "content" in n && (a.content = String(n.content || "")), "error" in n && (a.error = n.error === !0), "provider" in n && (a.provider = String(n.provider || "")), "model" in n && (a.model = String(n.model || "")), "finishReason" in n && (a.finishReason = String(n.finishReason || "")), "thoughts" in n && (a.thoughts = Jt(n.thoughts, void 0)), "providerPayload" in n && (a.providerPayload = Jt(n.providerPayload, void 0)), "toolCalls" in n && (a.toolCalls = _x(n)), "toolCallId" in n && (a.toolCallId = String(n.toolCallId || "").trim()), "toolName" in n && (a.toolName = String(n.toolName || "").trim()), "toolDisplay" in n && (a.toolDisplay = Jt(n.toolDisplay, void 0)), n.clearProtocolPayload === !0 && (a.providerPayload = void 0, a.toolCalls = void 0, a.toolCallId = void 0, a.toolName = void 0, a.toolDisplay = void 0), await jr.update([r, o], a), await Et.update(r, { updatedAt: s }), await jr.get([r, o]) || null;
}
async function O_(e = "", t = []) {
  const n = String(e || "").trim(), r = [...new Set((Array.isArray(t) ? t : []).map((s) => Number(s)).filter((s) => Number.isInteger(s) && s >= 0))];
  if (!n || !r.length) return 0;
  const o = [];
  return await Promise.all(r.map(async (s) => {
    await jr.get([n, s]) && o.push([n, s]);
  })), o.length ? (await Ot.transaction("rw", jr, Et, async () => {
    await jr.bulkDelete(o), await Et.update(n, { updatedAt: Zt() });
  }), o.length) : 0;
}
async function fo(e = "") {
  const t = String(e || "").trim();
  return t ? jr.where("sessionId").equals(t).sortBy("order") : [];
}
async function sm(e = {}) {
  const t = String(e.sessionId || "").trim();
  if (!t) throw new Error("manager_run_session_required");
  const n = Zt(), r = {
    id: String(e.id || cg("manager-run")),
    sessionId: t,
    turn: Math.max(0, Number(e.turn) || 0),
    userOrder: Number.isInteger(Number(e.userOrder)) ? Number(e.userOrder) : -1,
    assistantOrder: Number.isInteger(Number(e.assistantOrder)) ? Number(e.assistantOrder) : -1,
    trigger: String(e.trigger || "after_turn"),
    status: bx(e.status),
    provider: String(e.provider || ""),
    model: String(e.model || ""),
    inputSummary: String(e.inputSummary || ""),
    outputText: String(e.outputText || ""),
    parsedAction: String(e.parsedAction || ""),
    toolTrace: "toolTrace" in e ? Jt(e.toolTrace, void 0) : void 0,
    changedFiles: Bi(e.changedFiles, 100),
    changedStates: Bi(e.changedStates, 100),
    changedTasks: Bi(e.changedTasks, 100),
    error: String(e.error || ""),
    createdAt: Number(e.createdAt) || n,
    updatedAt: n
  };
  return await jn.put(r), await Et.update(t, { updatedAt: n }), r;
}
async function yo(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await jn.get(n);
  if (!r) return null;
  const o = { updatedAt: Zt() };
  return "status" in t && (o.status = bx(t.status)), [
    "provider",
    "model",
    "inputSummary",
    "outputText",
    "parsedAction",
    "error",
    "trigger"
  ].forEach((s) => {
    s in t && (o[s] = String(t[s] || ""));
  }), "toolTrace" in t && (o.toolTrace = Jt(t.toolTrace, void 0)), "changedFiles" in t && (o.changedFiles = Bi(t.changedFiles, 100)), "changedStates" in t && (o.changedStates = Bi(t.changedStates, 100)), "changedTasks" in t && (o.changedTasks = Bi(t.changedTasks, 100)), "turn" in t && (o.turn = Math.max(0, Number(t.turn) || 0)), "userOrder" in t && (o.userOrder = Number(t.userOrder)), "assistantOrder" in t && (o.assistantOrder = Number(t.assistantOrder)), await jn.update(n, o), await Et.update(r.sessionId, { updatedAt: Zt() }), await jn.get(n) || null;
}
async function wF(e = "") {
  const t = String(e || "").trim();
  if (!t) return null;
  const n = await jn.get(t);
  if (!n || n.status !== "running") return n || null;
  const r = Zt();
  return await jn.update(t, { updatedAt: r }), await Et.update(n.sessionId, { updatedAt: r }), await jn.get(t) || null;
}
async function SF(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await jn.where("sessionId").equals(n).sortBy("updatedAt"), o = Math.max(0, Number(t.limit) || 0);
  return (o ? r.slice(-o) : r).reverse();
}
function mb(e) {
  const t = JSON.stringify({
    content: String(e?.content || ""),
    status: String(e?.status || ""),
    source: String(e?.source || ""),
    staleFromOrder: Number.isFinite(Number(e?.staleFromOrder)) ? Number(e?.staleFromOrder) : null
  });
  let n = 2166136261;
  for (let r = 0; r < t.length; r += 1)
    n ^= t.charCodeAt(r), n = Math.imul(n, 16777619) >>> 0;
  return n.toString(16);
}
function TF(e = "", t = []) {
  const n = String(e || "").trim();
  if (!t.length) return n;
  const r = "rollback_conflict:", o = n.startsWith(r) ? n.slice(18).split(",").map((s) => s.trim()).filter(Boolean) : [];
  return `${r}${[.../* @__PURE__ */ new Set([...o, ...t])].join(",")}`;
}
async function F_(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await or.where("sessionId").equals(n).sortBy("updatedAt"), o = String(t.docType || "").trim();
  return r.filter((s) => !o || s.docType === o).filter((s) => t.includeStale || s.status !== "stale");
}
async function Ha(e = "", t = "tavern.map", n = "main") {
  const r = String(e || "").trim(), o = String(t || "").trim(), s = String(n || "main").trim() || "main";
  return !r || !o || !s ? null : await or.get([
    r,
    o,
    s
  ]) || null;
}
async function lf(e = "", t = {}) {
  const n = String(e || "").trim();
  return n ? await Ot.transaction("rw", or, Et, async () => {
    const r = Zt(), o = await or.get([
      n,
      hx,
      tm
    ]), s = await or.get([
      n,
      mx,
      gx
    ]), a = o || {
      sessionId: n,
      docType: "tavern.map",
      docId: "main",
      title: "地图",
      revision: 0,
      data: yu(),
      digest: "",
      status: "active",
      source: "system-seed",
      staleFromOrder: void 0,
      createdAt: r,
      updatedAt: r
    }, i = s || {
      sessionId: n,
      docType: "tavern.atlas",
      docId: "main",
      title: "世界图",
      revision: 0,
      data: vx(),
      digest: "",
      status: "active",
      source: "system-seed",
      staleFromOrder: void 0,
      createdAt: r,
      updatedAt: r
    };
    return o || await or.put(a), s || await or.put(i), t.touchSession !== !1 && await Et.update(n, { updatedAt: r }), a;
  }) : null;
}
async function Q2(e) {
  const t = Zt(), n = {
    ...Jt(e, e),
    sessionId: String(e.sessionId || "").trim(),
    docType: String(e.docType || "tavern.map"),
    docId: String(e.docId || "main").trim() || "main",
    title: String(e.title || ""),
    revision: Math.max(0, Number(e.revision) || 0),
    digest: String(e.digest || ""),
    status: e.status === "stale" ? "stale" : "active",
    createdAt: Number(e.createdAt) || t,
    updatedAt: t
  };
  if (!n.sessionId) throw new Error("state_session_required");
  return await or.put(n), await Et.update(n.sessionId, { updatedAt: t }), n;
}
async function B_(e = {}) {
  const t = String(e.sessionId || "").trim(), n = String(e.docType || "tavern.map"), r = String(e.docId || "main").trim() || "main";
  if (!t) throw new Error("state_patch_session_required");
  const o = Zt(), s = {
    id: String(e.id || cg("state-patch")),
    sessionId: t,
    docType: n,
    docId: r,
    revision: Math.max(0, Number(e.revision) || 0),
    status: e.status === "rolled_back" ? "rolled_back" : "active",
    managerRunId: String(e.managerRunId || ""),
    sourceUserOrder: Number.isFinite(Number(e.sourceUserOrder)) ? Number(e.sourceUserOrder) : void 0,
    sourceAssistantOrder: Number.isFinite(Number(e.sourceAssistantOrder)) ? Number(e.sourceAssistantOrder) : void 0,
    source: String(e.source || ""),
    summary: String(e.summary || ""),
    ops: Array.isArray(e.ops) ? Jt(e.ops, []) : [],
    changedIds: Bi(e.changedIds, 200),
    removedElements: Array.isArray(e.removedElements) ? Jt(e.removedElements, []) : [],
    createdAt: Number(e.createdAt) || o,
    updatedAt: o
  };
  return await Fi.put(s), await Et.update(t, { updatedAt: o }), s;
}
async function am(e = {}) {
  const t = String(e.sessionId || "").trim();
  if (!t) return [];
  const n = String(e.docType || "").trim(), r = String(e.docId || "").trim(), o = (await Fi.where("sessionId").equals(t).sortBy("revision")).filter((a) => (!n || a.docType === n) && (!r || a.docId === r) && (e.includeRolledBack || a.status !== "rolled_back")), s = Math.max(0, Number(e.limit) || 0);
  return s ? o.slice(-s) : o;
}
async function U_(e) {
  const t = String(e.managerRunId || "").trim(), n = String(e.sessionId || "").trim(), r = String(e.path || "").trim();
  if (!t || !n || !r) return null;
  const o = await to.get([t, r]);
  if (o) return o;
  const s = Zt(), a = await hn.get([n, r]) || null, i = {
    managerRunId: t,
    sessionId: n,
    path: r,
    beforeExists: !!a,
    beforeFile: a ? Jt(a, void 0) : void 0,
    beforeHash: mb(a),
    afterHash: "",
    rollbackStatus: "pending",
    error: "",
    createdAt: s,
    updatedAt: s
  };
  return await to.put(i), i;
}
async function Z2(e) {
  const t = String(e.managerRunId || "").trim(), n = String(e.sessionId || "").trim(), r = String(e.path || "").trim();
  if (!t || !n || !r || !await U_({
    managerRunId: t,
    sessionId: n,
    path: r
  })) return null;
  const o = await hn.get([n, r]) || null;
  return await to.update([t, r], {
    afterHash: mb(o),
    updatedAt: Zt()
  }), await to.get([t, r]) || null;
}
async function gb(e = "") {
  const t = String(e || "").trim();
  return t ? await to.where("managerRunId").equals(t).sortBy("updatedAt") : [];
}
async function Ix(e = "") {
  const t = String(e || "").trim();
  if (!t) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const n = await jn.get(t);
  if (!n) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const r = (await gb(t)).reverse();
  let o = 0, s = 0;
  const a = [];
  for (const l of r) {
    if (l.rollbackStatus === "rolled_back" || l.rollbackStatus === "skipped") {
      s += 1;
      continue;
    }
    if (l.rollbackStatus === "conflict") {
      a.push(l.path), s += 1;
      continue;
    }
    const u = await hn.get([l.sessionId, l.path]) || null;
    if (!l.afterHash) {
      s += 1, await to.update([l.managerRunId, l.path], {
        rollbackStatus: "skipped",
        error: "snapshot_after_hash_missing",
        updatedAt: Zt()
      });
      continue;
    }
    if (mb(u) !== l.afterHash) {
      a.push(l.path), await to.update([l.managerRunId, l.path], {
        rollbackStatus: "conflict",
        error: "rollback_conflict_current_file_changed",
        updatedAt: Zt()
      });
      continue;
    }
    l.beforeExists && l.beforeFile ? await hn.put(Jt(l.beforeFile, l.beforeFile)) : await hn.delete([l.sessionId, l.path]), o += 1, await to.update([l.managerRunId, l.path], {
      rollbackStatus: "rolled_back",
      error: "",
      updatedAt: Zt()
    });
  }
  const i = await eo.get([n.sessionId, "markdown-derived"]);
  return await eo.put({
    sessionId: n.sessionId,
    kind: "markdown-derived",
    status: "stale",
    error: a.length ? `rollback_conflict:${a.join(",")}` : "",
    sourceFingerprint: "",
    derivedAt: Zt(),
    updatedAt: Zt(),
    files: Array.isArray(i?.files) ? i.files : []
  }), await yo(t, {
    status: "rolled_back",
    error: TF(n.error, a)
  }), await Et.update(n.sessionId, { updatedAt: Zt() }), {
    rolledBack: o,
    conflicts: a,
    skipped: s
  };
}
async function EF(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return {
    runIds: [],
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const o = (await jn.where("sessionId").equals(n).toArray()).filter((l) => l.trigger === "after_turn" && (Number(l.userOrder) >= r || Number(l.assistantOrder) >= r)).sort((l, u) => u.updatedAt - l.updatedAt);
  let s = 0, a = 0;
  const i = [];
  for (const l of o) {
    if ((await gb(l.id)).some((u) => String(u.afterHash || "").trim())) {
      const u = await Ix(l.id);
      s += u.rolledBack, a += u.skipped, i.push(...u.conflicts);
      continue;
    }
    await yo(l.id, {
      status: ["queued", "running"].includes(l.status) ? "cancelled" : "superseded",
      error: "manager_source_messages_superseded"
    });
  }
  return {
    runIds: o.map((l) => l.id),
    rolledBack: s,
    conflicts: i,
    skipped: a
  };
}
async function im() {
  const e = await Ys.get("activeAssistantPresetId");
  return String(e?.value || "littlewhitebox-assistant-default").trim() || "littlewhitebox-assistant-default";
}
async function Ll(e = ig) {
  const t = String(e || "littlewhitebox-assistant-default").trim() || "littlewhitebox-assistant-default";
  return await Ys.put({
    key: "activeAssistantPresetId",
    value: t,
    updatedAt: Zt()
  }), t;
}
async function Fc(e, t = {}) {
  const n = Zt(), r = String(e.id || cg("assistant-preset")), o = await Gi.get(r), s = cb({
    ...e,
    id: r,
    updatedAt: n
  }), a = {
    id: r,
    name: s.name,
    description: s.description,
    version: t.isBuiltIn === !0 ? zO : String(o?.version || ""),
    isBuiltIn: t.isBuiltIn === !0,
    createdAt: Number(o?.createdAt) || n,
    updatedAt: n,
    preset: s
  };
  return await Gi.put(a), a;
}
async function AF(e = "") {
  const t = String(e || "").trim();
  return !t || t === "littlewhitebox-assistant-default" ? !1 : (await Gi.delete(t), await im() === t && await Ll(ig), !0);
}
async function xx() {
  const e = await Gi.get(ig);
  return e?.version === "2026-06-assistant-prompt-structure-v2" ? e : Fc(Fd(), { isBuiltIn: !0 });
}
async function Tl() {
  return await xx(), Gi.orderBy("updatedAt").reverse().toArray();
}
async function uy() {
  await xx();
  const e = await im();
  return cb((await Gi.get(e) || await Gi.get("littlewhitebox-assistant-default"))?.preset || Fd());
}
function vb(e = []) {
  return e.some((t) => !!t.rect || typeof t.circle == "number" || Array.isArray(t.path) || Array.isArray(t.curve) || !!t.icon);
}
function CF(e) {
  return e ? e.meta.status === "active" && vb(e.elements) : !1;
}
var Mx = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon",
  "text"
];
function Hs(e) {
  return JSON.parse(JSON.stringify(e));
}
function Ns(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function Px(e, t) {
  const n = { ...e };
  return Object.entries(t).forEach(([r, o]) => {
    if (o == null) {
      delete n[r];
      return;
    }
    if (Ns(o) && Ns(n[r])) {
      n[r] = Px(n[r], o);
      return;
    }
    n[r] = Hs(o);
  }), n;
}
function kF(e) {
  return Mx.filter((t) => t === "circle" ? typeof e.circle == "number" : t === "text" ? typeof e.text == "string" && !!e.text.trim() : Array.isArray(e[t]) || typeof e[t] == "string" && !!e[t]);
}
function IF(e, t) {
  return t === void 0 ? e ? Hs(e) : void 0 : Object.keys(t).length ? {
    ...e || {},
    ...t
  } : void 0;
}
function Rx(e, t) {
  const n = kF(t).length > 0, r = Hs(e);
  return n && (Mx.forEach((o) => {
    delete r[o];
  }), delete r.style, delete r.fill, delete r.closed), Object.entries(t).forEach(([o, s]) => {
    if (o !== "style") {
      if (s === void 0) {
        delete r[o];
        return;
      }
      r[o] = Hs(s);
    }
  }), "style" in t && (r.style = n ? Hs(t.style) : IF(e.style, t.style)), r;
}
function xF(e, t) {
  let n = Hs(e);
  return t.forEach((r) => {
    const o = String(r.op || "").trim();
    if (o === "meta") {
      const s = r.set && Ns(r.set) ? r.set : r.changes && Ns(r.changes) ? r.changes : null;
      s && (n.meta = Px(n.meta, s), n.meta.status !== "uninitialized" && delete n.meta.hint);
      return;
    }
    if (o === "add") {
      const s = r.element;
      if (!s || !Ns(s) || typeof s.id != "string") return;
      const a = Hs(s), i = n.elements.findIndex((l) => l.id === a.id);
      if (i >= 0) {
        n.elements[i] = a;
        return;
      }
      n.elements.push(a);
      return;
    }
    if (o === "remove") {
      const s = String(r.id || "").trim();
      if (!s) return;
      n.elements = n.elements.filter((a) => a.id !== s);
      return;
    }
    if (o === "modify") {
      const s = String(r.id || "").trim(), a = r.set && Ns(r.set) ? r.set : r.changes && Ns(r.changes) ? r.changes : null;
      if (!s || !a) return;
      n.elements = n.elements.map((i) => i.id === s ? Rx(i, a) : i);
      return;
    }
    if (o === "init" || o === "reset") {
      const s = r.document;
      s && Ns(s) && (n = Hs(s));
      return;
    }
    if (o === "replace") {
      const s = String(r.id || "").trim(), a = r.element;
      if (!s || !a || !Ns(a)) return;
      n.elements = n.elements.map((i) => i.id === s ? Hs(a) : i);
    }
  }), n;
}
var Nx = [
  "x",
  "o",
  "+",
  "arrow-n",
  "arrow-s",
  "arrow-e",
  "arrow-w",
  "stairs-up",
  "stairs-down"
], $x = [
  "castle",
  "village",
  "road",
  "house",
  "shop",
  "inn-sign",
  "temple",
  "stairs",
  "door",
  "forest",
  "mountains",
  "cave",
  "waterfall",
  "skull",
  "trap",
  "fire",
  "chest",
  "table",
  "bed",
  "barrel",
  "flag",
  "star",
  "torch",
  "portal",
  "tree",
  "rock",
  "water",
  "heart",
  "lips",
  "lovers",
  "cherish",
  "love-letter",
  "locked-heart",
  "perfume"
], yb = [...Nx, ...$x], She = new Set(yb), Xr = {
  LIST: "MapDocs",
  READ: "MapInspect",
  PATCH: "MapPatch"
}, cy = {
  LIST: "StateList",
  READ: "StateRead",
  PATCH: "StatePatch"
};
function MF(e = "") {
  const t = String(e || "").trim();
  return t === cy.LIST ? Xr.LIST : t === cy.READ ? Xr.READ : t === cy.PATCH ? Xr.PATCH : t;
}
var Qn = hx, yr = mx, _r = tm, Io = gx, q_ = 2e3, Dx = 1e3, Co = 300, _b = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon",
  "text"
], PF = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon"
], bb = /* @__PURE__ */ new Set([
  "wall",
  "road",
  "water",
  "terrain",
  "furniture",
  "door",
  "danger",
  "marker",
  "actor",
  "label",
  "grid",
  "magic",
  "secret"
]), Lx = new Set(yb), RF = /* @__PURE__ */ new Set([
  "parchment",
  "paper",
  "dark",
  "blueprint",
  "grid"
]), NF = /* @__PURE__ */ new Set(["uninitialized", "active"]), Ox = /* @__PURE__ */ new Set([
  "city",
  "district",
  "building",
  "floor",
  "room",
  "outdoor"
]), Fx = /* @__PURE__ */ new Set(["mentioned", "visited"]), Bd = /* @__PURE__ */ new Set([
  "door",
  "stairs",
  "elevator",
  "path",
  "road",
  "portal",
  "passage"
]), $F = /* @__PURE__ */ new Set([
  "parent",
  "mapDocId",
  "aliases",
  "brief"
]);
function H_() {
  return Date.now();
}
function Bx(e = _r) {
  const t = String(e || _r).trim() || _r;
  return /^[\w.-]{1,80}$/i.test(t) ? t : _r;
}
function DF(e, t = "state_doc_id_invalid") {
  const n = String(e || "").trim();
  if (!n || !/^[\w.-]{1,80}$/i.test(n)) throw new Error(t);
  return n;
}
async function z_(e = "") {
  return Bx((await Et.get(String(e || "").trim()))?.state?.activeMapDocId || _r);
}
async function hh(e = "", t = _r) {
  const n = String(e || "").trim(), r = Bx(t);
  if (!n) return r;
  const o = await Et.get(n), s = o?.state && typeof o.state == "object" && !Array.isArray(o.state) ? {
    ...o.state,
    activeMapDocId: r
  } : { activeMapDocId: r };
  return await Et.update(n, {
    state: s,
    updatedAt: H_()
  }), r;
}
async function dy(e = "", t = "") {
  const n = qd(await Ha(e, yr, Io)), r = n.locations.find((o) => o.key === n.activeLocationKey)?.mapDocId;
  return !n.activeLocationKey || !r || r === t ? [] : [`Map doc '${t}' activated. Atlas still says player/current location is '${n.activeLocationKey}'. If the story moved the player, send tavern.atlas move-actor with actorKey:"player" and the correct locationKey.`];
}
async function lm(e = "", t = {}) {
  await lf(e, { touchSession: !1 });
  const n = await z_(e), r = await F_(e, {
    docType: Qn,
    includeStale: t.includeStale === !0
  }), o = r.find((s) => s.docId === n) || r.find((s) => s.docId === _r) || r[0] || null;
  return {
    requestedDocId: n,
    activeDocId: o?.docId || n,
    record: o,
    documents: r
  };
}
function Sn(e) {
  return JSON.parse(JSON.stringify(e));
}
function wr(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function kt(e = "", t = 400) {
  const n = String(e ?? "").replace(/\s+/g, " ").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function j2(e) {
  return JSON.stringify(e ?? null);
}
function ru(e, t) {
  return j2(e) === j2(t);
}
function LF(e = Qn) {
  const t = String(e || Qn).trim();
  if (t === Qn || t === yr) return t;
  throw new Error("state_doc_type_not_supported");
}
function OF(e = _r) {
  const t = String(e || _r).trim() || _r;
  if (!/^[\w.-]{1,80}$/i.test(t)) throw new Error("state_doc_id_invalid");
  return t;
}
function FF(e, t) {
  if (e === yr) {
    if ((String(t || Io).trim() || Io) !== Io) throw new Error("atlas_doc_id_invalid");
    return Io;
  }
  return OF(t || _r);
}
function Ux(e, t = "parchment") {
  return RF.has(String(e || "").trim()) ? String(e).trim() : t;
}
function qx(e, t = "active") {
  return NF.has(String(e || "").trim()) ? String(e).trim() : t;
}
function W_(e) {
  if (e === null || !Array.isArray(e) || e.length !== 4) return null;
  const t = e.map((n) => Number(n));
  return t.every((n) => Number.isFinite(n)) ? [
    t[0],
    t[1],
    Math.max(1, t[2]),
    Math.max(1, t[3])
  ] : null;
}
function Da(e) {
  if (Array.isArray(e) && e.length >= 2) {
    const t = Number(e[0]), n = Number(e[1]);
    return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
  }
  if (wr(e)) {
    const t = Number(e.x ?? e.cx), n = Number(e.y ?? e.cy);
    return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
  }
  return null;
}
function Hx(e, t) {
  const n = Da(e);
  if (!n) throw new Error(t);
  return n;
}
function G_(e, t, n = 2) {
  if (!Array.isArray(e) || e.length < n) throw new Error(t);
  const r = e.map((o) => Da(o));
  if (r.some((o) => !o)) throw new Error(t);
  return r;
}
function fg(e) {
  const t = Number(e);
  return Number.isFinite(t) && t > 0 ? t : null;
}
function wb(e) {
  if (!Array.isArray(e) || e.length < 2) return null;
  const t = Number(e[0]), n = Number(e[1]);
  return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
}
function um(e, t) {
  const n = String(e || "").trim();
  return n && bb.has(n) ? n : t;
}
function Sb(e) {
  const t = String(e || "").trim();
  return Lx.has(t) ? t : void 0;
}
function Ud(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").trim().toLowerCase().replace(/\s+/g, "-").slice(0, 120);
}
function Tb(e) {
  if (e === null) return;
  const t = wr(e) ? e : {}, n = {}, r = kt(t.color, 80), o = kt(t.dash, 80), s = fg(t.width);
  return r && (n.color = r), o && (n.dash = o), s !== null && (n.width = s), Object.keys(n).length ? n : void 0;
}
function zx(e, t = {}) {
  const n = wr(e) ? e : {}, r = qx(n.status ?? t.status ?? "active", t.status ?? "active"), o = "name" in n ? n.name : t.name, s = "hint" in n ? n.hint : t.hint;
  return {
    name: o === null ? null : kt(o, 120) || null,
    viewBox: "viewBox" in n ? W_(n.viewBox) : W_(t.viewBox ?? null),
    theme: Ux(n.theme ?? t.theme ?? "parchment", t.theme ?? "parchment"),
    status: r,
    ...r === "uninitialized" ? { hint: kt(s ?? $_(), 1200) || $_() } : {}
  };
}
function BF(e) {
  if (!wr(e)) throw new Error("map_meta_set_required");
  const t = {};
  return "name" in e && (t.name = e.name === null ? null : kt(e.name, 120) || null), "viewBox" in e && (t.viewBox = W_(e.viewBox)), "theme" in e && (t.theme = Ux(e.theme)), "status" in e && (t.status = qx(e.status)), "hint" in e && (t.hint = kt(e.hint, 1200) || void 0), t;
}
function V_(e, t) {
  return zx({
    ...e,
    ...t,
    ...t.hint === void 0 && "hint" in t ? { hint: null } : {}
  }, e);
}
function Wx(e) {
  for (const t of _b) {
    if (t === "circle") {
      if (typeof e.circle == "number") return t;
      continue;
    }
    if (t === "text") {
      if (typeof e.text == "string" && e.text.trim()) return t;
      continue;
    }
    if (Array.isArray(e[t]) || typeof e[t] == "string" && e[t]) return t;
  }
  return null;
}
function K_(e) {
  return e === "text" ? "label" : e === "icon" ? "marker" : "wall";
}
function Gx(e, t) {
  const n = Hx(e.center ?? e.pos ?? {
    x: e.cx,
    y: e.cy
  }, `map_element_center_required:${t}`), r = fg(e.r ?? e.radius);
  if (r === null) throw new Error(`map_element_radius_required:${t}`);
  const o = Number(e.startAngle), s = Number(e.endAngle);
  if (!Number.isFinite(o) || !Number.isFinite(s)) throw new Error(`map_element_arc_angles_required:${t}`);
  const a = s - o, i = Math.max(4, Math.min(24, Math.ceil(Math.abs(a) / 30)));
  return Array.from({ length: i + 1 }, (l, u) => {
    const d = (o + a * u / i) * Math.PI / 180;
    return [Number((n[0] + r * Math.cos(d)).toFixed(2)), Number((n[1] + r * Math.sin(d)).toFixed(2))];
  });
}
function rs(e, t, n) {
  const r = G_(e, `map_element_points_required:${t}`, 2);
  if (n) return {
    at: n,
    points: r
  };
  const o = r[0];
  return {
    at: o,
    points: r.map(([s, a]) => [Number((s - o[0]).toFixed(2)), Number((a - o[1]).toFixed(2))])
  };
}
function UF(e) {
  const [t, n] = e.at;
  return e.rect ? [t + Math.max(18, e.rect[0] / 2), n - 18] : typeof e.circle == "number" ? [t + e.circle + 18, n - 6] : [t + 18, n - 18];
}
function Vx(e, t = {}) {
  if (!e || !/^[\w:.-]{1,120}$/i.test(e)) throw new Error("map_element_id_invalid");
  if (!t.allowReserved && nm(e)) throw new Error(`map_element_id_reserved:${e}`);
  return e;
}
function qF(e, t) {
  const n = [...new Set(t)];
  if (!n.length) throw new Error(`map_element_shape_required:${e}`);
  if (n.length !== 1 && !(n.length === 2 && n.includes("text") && n.some((r) => PF.includes(r))))
    throw new Error(`map_element_shape_conflict:${e}`);
}
function ip(e, t, n = {}) {
  const r = Vx(t, { allowReserved: n.allowReservedId === !0 }), o = Wx(e);
  if (!o) throw new Error(`map_element_shape_required:${r}`);
  const s = {
    id: r,
    at: Hx(e.at, `map_element_at_required:${r}`),
    cat: um(e.cat, K_(o))
  };
  if (e.rect) {
    const i = wb(e.rect);
    if (!i || i[0] <= 0 || i[1] <= 0) throw new Error(`map_element_rect_invalid:${r}`);
    s.rect = i;
  }
  if (typeof e.circle == "number") {
    if (!(e.circle > 0)) throw new Error(`map_element_radius_required:${r}`);
    s.circle = e.circle;
  }
  if (e.path && (s.path = G_(e.path, `map_element_points_required:${r}`, 2)), e.curve && (s.curve = G_(e.curve, `map_element_points_required:${r}`, 2)), e.icon) {
    const i = Sb(e.icon);
    if (!i) throw new Error(`map_element_icon_invalid:${r}`);
    s.icon = i;
  }
  if (e.text !== void 0) {
    const i = kt(e.text, 240);
    if (!i) throw new Error(`map_element_text_required:${r}`);
    s.text = i;
  }
  const a = kt(e.actorKey, 120);
  if (a && s.cat === "actor" && (s.actorKey = a), e.closed === !0 && (s.closed = !0), kt(e.fill, 120) && (s.fill = kt(e.fill, 120)), e.style !== void 0) {
    const i = Tb(e.style);
    i && (s.style = i);
  }
  return s;
}
function J_(e, t = {}) {
  if (!wr(e)) throw new Error("map_element_must_be_object");
  const n = t.source || "model-input", r = Vx(t.forcedId || String(e.id || "").trim(), { allowReserved: t.allowReservedId === !0 || n === "stored-document" });
  let o = Da(e.at ?? e.pos ?? e.center ?? e.position);
  !o && (e.x !== void 0 || e.y !== void 0) && (o = Da({
    x: e.x,
    y: e.y
  })), !o && (e.cx !== void 0 || e.cy !== void 0) && (o = Da({
    x: e.cx,
    y: e.cy
  }));
  const s = {}, a = String(e.type || "").trim(), i = wb(e.rect ?? e.size ?? (e.width !== void 0 || e.height !== void 0 ? [e.width, e.height] : null));
  i && (s.rect = i);
  const l = a === "arc" ? null : fg(e.circle ?? e.r ?? e.radius);
  l !== null && (s.circle = l);
  const u = e.path ?? e.points ?? e.line ?? (e.x1 !== void 0 || e.y1 !== void 0 || e.x2 !== void 0 || e.y2 !== void 0 ? [[e.x1, e.y1], [e.x2, e.y2]] : null), d = e.curve, f = kt(e.text ?? e.content ?? e.label ?? e.value, 240);
  f && (s.text = f);
  const m = Sb(e.icon);
  if (m && (s.icon = m), a === "line" && u) {
    const C = rs(u, r, o);
    o = C.at, s.path = C.points;
  } else if (a === "curve" && (d ?? u)) {
    const C = rs(d ?? u, r, o);
    o = C.at, s.curve = C.points;
  } else if (a === "fill" && u) {
    const C = rs(u, r, o);
    o = C.at, s.path = C.points, s.closed = !0, kt(e.fill, 120) && (s.fill = kt(e.fill, 120));
  } else if (a === "arc") {
    const C = rs(Gx(e, r), r, o);
    o = C.at, s.curve = C.points;
  } else {
    if (u && !s.path && !s.curve) {
      const C = rs(u, r, o);
      o = C.at, s.path = C.points;
    }
    if (d && !s.curve) {
      const C = rs(d, r, o);
      o = C.at, s.curve = C.points;
    }
  }
  const v = _b.filter((C) => C === "circle" ? typeof s.circle == "number" : C === "text" ? typeof s.text == "string" && !!s.text.trim() : C in s);
  qF(r, v);
  const y = um(e.cat, K_(v.find((C) => C !== "text") || v[0] || null)), T = {
    id: r,
    at: o || void 0,
    cat: y,
    closed: e.closed === !0 || s.closed === !0,
    fill: kt(e.fill ?? s.fill, 120) || void 0,
    style: Tb(e.style),
    actorKey: kt(e.actorKey, 120) || void 0,
    ...s
  };
  if (v.length === 2 && t.splitGeometryText !== !1) {
    const C = v.find((R) => R !== "text"), A = ip({
      id: r,
      at: T.at,
      cat: um(e.cat, K_(C)),
      closed: T.closed,
      fill: T.fill,
      style: T.style,
      actorKey: T.actorKey,
      [C]: T[C]
    }, r, { allowReservedId: t.allowReservedId === !0 || n === "stored-document" }), I = rm(r);
    return [A, ip({
      id: I,
      at: UF(A),
      cat: "label",
      text: T.text
    }, I, { allowReservedId: !0 })];
  }
  return [ip(T, r, { allowReservedId: t.allowReservedId === !0 || n === "stored-document" })];
}
function cm(e, t = {}, n = "stored-document") {
  const r = wr(e) ? e : {}, o = zx(r.meta, t), s = Array.isArray(r.elements) ? r.elements.flatMap((i) => J_(i, { source: n })) : [];
  if (s.length > q_) throw new Error("map_elements_limit_exceeded");
  const a = /* @__PURE__ */ new Set();
  return s.forEach((i) => {
    if (a.has(i.id)) throw new Error(`map_element_duplicate:${i.id}`);
    a.add(i.id);
  }), {
    meta: o,
    elements: s
  };
}
function Kx() {
  return cm(yu(), yu().meta, "stored-document");
}
function Vi(e) {
  return e?.data ? cm(e.data, yu().meta, "stored-document") : Kx();
}
function Ki(e, t = 0) {
  if (e.meta.status !== "active" || !vb(e.elements)) return "";
  const n = kt(e.meta.name || "Map", 80) || "Map", r = e.elements.filter((o) => typeof o.text == "string" && o.text.trim()).map((o) => kt(o.text, 40)).filter(Boolean).slice(0, 8);
  return [`地图：${n}`, r.length ? `标注：${r.join(", ")}` : ""].filter(Boolean).join(`
`);
}
function bu(e) {
  return kt(e.meta.name || "Map", 120) || "Map";
}
function Mr(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").trim().replace(/\s+/g, "-").slice(0, 120);
}
function uc(e, t) {
  const n = Mr(e);
  if (!n || /[/\\:*?"<>|\u0000-\u001F]/.test(n) || n === "." || n === "..") throw new Error(t);
  return n;
}
function HF(e) {
  const t = String(e || "").trim();
  if (!Ox.has(t)) throw new Error("atlas_location_scale_invalid");
  return t;
}
function zF(e) {
  const t = String(e || "").trim();
  if (!Fx.has(t)) throw new Error("atlas_location_status_invalid");
  return t;
}
function WF(e) {
  const t = String(e || "").trim();
  if (!Bd.has(t)) throw new Error("atlas_link_kind_invalid");
  return t;
}
function Jx(e, t = 20) {
  if (Array.isArray(e))
    return [...new Set(e.map((n) => kt(n, 80)).filter(Boolean))].slice(0, t);
}
function Y_(e, t, n, r = !0) {
  return r === !1 ? `link:${e}:${t}:${n}` : `link:${[e, t].sort().join(":")}:${n}`;
}
function GF(e) {
  if (!wr(e)) return null;
  const t = Mr(e.key), n = kt(e.name, 120), r = String(e.scale || "").trim(), o = String(e.status || "").trim();
  if (!t || !n || !Ox.has(r) || !Fx.has(o)) return null;
  const s = {
    key: t,
    name: n,
    scale: r,
    status: o
  }, a = Mr(e.parent);
  a && (s.parent = a);
  const i = kt(e.mapDocId, 80);
  i && /^[\w.-]{1,80}$/i.test(i) && (s.mapDocId = i);
  const l = Jx(e.aliases);
  l?.length && (s.aliases = l);
  const u = kt(e.brief, 240);
  return u && (s.brief = u), s;
}
function VF(e) {
  if (!wr(e)) return null;
  const t = Mr(e.from), n = Mr(e.to), r = String(e.kind || "").trim();
  if (!t || !n || !Bd.has(r)) return null;
  const o = e.bidirectional !== !1, s = {
    id: kt(e.id, 160) || Y_(t, n, r, o),
    from: t,
    to: n,
    kind: r,
    bidirectional: o
  }, a = kt(e.label, 120);
  return a && (s.label = a), s;
}
function KF(e) {
  if (!wr(e)) return null;
  const t = Ud(e.actorKey), n = Mr(e.locationKey);
  return t && n ? {
    actorKey: t,
    locationKey: n
  } : null;
}
function JF() {
  return Eb(vx());
}
function Eb(e) {
  const t = wr(e) ? e : {}, n = Array.isArray(t.locations) ? t.locations.map(GF).filter((f) => !!f) : [], r = Array.isArray(t.links) ? t.links.map(VF).filter((f) => !!f) : [], o = Array.isArray(t.actors) ? t.actors.map(KF).filter((f) => !!f) : [], s = /* @__PURE__ */ new Set(), a = [];
  n.forEach((f) => {
    s.has(f.key) || (s.add(f.key), a.push(f));
  });
  const i = /* @__PURE__ */ new Set(), l = [];
  r.forEach((f) => {
    i.has(f.id) || (i.add(f.id), l.push(f));
  });
  const u = /* @__PURE__ */ new Map();
  o.forEach((f) => u.set(f.actorKey, f));
  const d = Mr(t.activeLocationKey);
  return {
    version: 1,
    ...d ? { activeLocationKey: d } : {},
    locations: a,
    links: l,
    actors: [...u.values()]
  };
}
function qd(e) {
  return e?.data ? Eb(e.data) : JF();
}
function X_(e) {
  const t = e.locations.find((n) => n.key === e.activeLocationKey);
  return t ? `世界图：${t.name}` : "世界图";
}
function lp(e) {
  const t = e.locations.find((r) => r.key === e.activeLocationKey), n = e.locations.filter((r) => r.status === "visited").length;
  return [
    t ? `当前地点：${t.name}` : "",
    `地点：${e.locations.length}`,
    `连接：${e.links.length}`,
    `人物：${e.actors.length}`,
    n ? `已探索：${n}` : ""
  ].filter(Boolean).join(`
`);
}
function YF(e, t) {
  const n = /* @__PURE__ */ new Set();
  let r = t;
  for (; r; ) {
    if (n.has(r)) return !0;
    n.add(r), r = e.get(r)?.parent;
  }
  return !1;
}
function Yx(e) {
  return Sn(e);
}
async function XF(e) {
  const t = /* @__PURE__ */ new Map();
  if (e.document.elements.forEach((a) => {
    if (a.cat !== "actor") return;
    const i = Ud(a.actorKey || a.id);
    i && t.set(i, a.id);
  }), !t.size) return {
    document: e.document,
    removedElements: [],
    otherDocumentRemovals: []
  };
  const n = (a, i) => {
    let l = !1;
    const u = /* @__PURE__ */ new Set(), d = [], f = a.elements.filter((T) => {
      if (T.cat !== "actor") return !0;
      const C = Ud(T.actorKey || T.id);
      if (!t.has(C)) return !0;
      const A = i && t.get(C) === T.id;
      return A || (d.push(Sn(T)), u.add(T.id), l = !0), A;
    }), m = new Set([...u].map((T) => rm(T))), v = m.size ? f.filter((T) => m.has(T.id) ? (d.push(Sn(T)), l = !0, !1) : !0) : f, y = d.map((T) => ({
      op: "remove",
      id: T.id
    }));
    return {
      document: l ? {
        ...a,
        elements: v
      } : a,
      removedElements: d,
      removeOps: y
    };
  }, r = n(e.document, !0), o = [], s = await or.where("sessionId").equals(e.sessionId).toArray();
  for (const a of s) {
    if (a.docType !== e.docType || a.docId === e.docId || a.status === "stale") continue;
    const i = n(Vi(a), !1);
    if (!i.removedElements.length) continue;
    const l = Math.max(0, Number(a.revision) || 0) + 1, u = Ki(i.document, l);
    await or.put({
      ...a,
      data: i.document,
      digest: u,
      revision: l,
      title: bu(i.document),
      updatedAt: e.timestamp
    }), await B_({
      sessionId: e.sessionId,
      docType: e.docType,
      docId: a.docId,
      revision: l,
      status: "active",
      managerRunId: e.managerRunId,
      sourceUserOrder: e.sourceUserOrder,
      sourceAssistantOrder: e.sourceAssistantOrder,
      source: "system",
      summary: kt(`Actor dedupe ${l}`, 400),
      ops: i.removeOps,
      changedIds: i.removedElements.map((d) => d.id),
      removedElements: i.removedElements
    }), o.push({
      docId: a.docId,
      revision: l,
      removedElements: i.removedElements,
      removeOps: i.removeOps
    });
  }
  return {
    document: r.document,
    removedElements: r.removedElements,
    otherDocumentRemovals: o
  };
}
function Ab(e = "") {
  const [t, n = "element"] = String(e || "").split(":"), r = n || "element";
  switch (t) {
    case "map_element_id_invalid":
      return "Invalid element id. Use a short stable id containing only letters, numbers, underscores, dots, colons, and hyphens.";
    case "map_element_id_reserved":
      return `${r} uses the reserved \`__label__\` prefix. Use a normal id instead.`;
    case "map_element_at_required":
      return `${r} is missing a position. Provide \`at:[x,y]\`. Legacy aliases such as pos/center/x+y are also accepted.`;
    case "map_element_rect_invalid":
      return `${r} has an invalid rect. Use positive dimensions such as \`rect:[120,80]\`.`;
    case "map_element_radius_required":
      return `${r} is missing a valid radius. \`circle\` must be a number greater than 0.`;
    case "map_element_points_required":
      return `${r} is missing a valid point array. \`path\` and \`curve\` need at least two points. With \`at\`, points are relative offsets; without \`at\`, the first point becomes the anchor.`;
    case "map_element_text_required":
      return `${r} is missing text content. \`text\` must be a short non-empty label.`;
    case "map_element_icon_invalid":
      return `${r} has an invalid icon. Use one of ${yb.join("/")}.`;
    case "map_element_shape_required":
      return `${r} is missing a shape field. Every element must provide exactly one of rect/circle/path/curve/icon/text.`;
    case "map_element_shape_conflict":
      return `${r} has multiple shape fields. Only the special "geometry + text" input is allowed, and it will be split into a derived label automatically.`;
    case "map_element_duplicate":
      return `${r} is duplicated. Use a stable unique id.`;
    case "map_element_not_found":
      return `${r} does not exist. Use MapInspect summary/elements first to find existing ids.`;
    case "map_element_already_exists":
      return `${r} already exists. Use \`modify\` to change it. If it is already identical, do not repeat the \`add\`.`;
    case "map_element_id_cannot_change":
      return `${r} cannot change id inside a \`modify\` op.`;
    case "map_meta_set_required":
      return "`meta` requires a `set` object.";
    case "map_modify_set_required":
      return `${r} is missing a \`set\` object. \`modify\` requires \`set\`.`;
    case "map_op_not_supported":
      return `Unsupported op: ${r}. Supported canonical ops are meta/add/modify/remove. Legacy init/reset/replace input is still absorbed at runtime.`;
    case "map_element_center_required":
      return `${r} is missing a center point.`;
    case "map_element_arc_angles_required":
      return `${r} is missing arc start/end angles.`;
    case "atlas_doc_id_invalid":
      return "Atlas has exactly one document: tavern.atlas/main.";
    case "atlas_activate_not_supported":
      return 'Atlas does not support activate:true. Move the player with move-actor(actorKey:"player").';
    case "atlas_location_key_invalid":
      return "Invalid atlas location key. Use a stable non-empty key without path separators or control characters.";
    case "atlas_location_create_required":
      return `${r} is new. Provide set.name, set.scale, and set.status.`;
    case "atlas_location_not_found":
      return `${r} does not exist in the atlas. Create the location before linking or moving actors there.`;
    case "atlas_parent_not_found":
      return `${r} does not exist. Create the parent location before assigning it.`;
    case "atlas_parent_cycle":
      return `${r} would create a parent cycle.`;
    case "atlas_link_location_not_found":
      return `${r} does not exist. Atlas links cannot point to missing locations.`;
    case "atlas_link_kind_invalid":
      return "Invalid atlas link kind. Use door/stairs/elevator/path/road/portal/passage.";
    case "atlas_link_not_found":
      return `${r} does not exist. Use MapInspect links first.`;
    case "atlas_location_has_children":
    case "atlas_location_has_links":
    case "atlas_location_has_actors":
    case "atlas_location_active":
      return `${r} still has dependencies. Remove child locations, links, actors, or active status first.`;
    case "atlas_op_not_supported":
      return `Unsupported atlas op: ${r}. Use upsert-location/remove-location/upsert-link/remove-link/move-actor.`;
    default:
      return e;
  }
}
function eT(e) {
  const t = e.slice(0, 3).map((r) => `${r.index >= 0 ? `op ${r.index + 1}` : "transaction"}: ${r.hint || Ab(r.error) || r.error}`), n = e.length > t.length ? `; plus ${e.length - t.length} more failures` : "";
  return t.length ? `${t.join("; ")}${n}` : "";
}
function QF(e, t = []) {
  return vb(e.elements) ? e.meta.status === "uninitialized" ? {
    document: {
      ...e,
      meta: V_(e.meta, { status: "active" })
    },
    statusChanged: !0
  } : {
    document: e,
    statusChanged: !1
  } : e.meta.status === "active" ? (t.push("Map stays uninitialized: it needs at least one spatial geometry element. Name, viewBox, or text labels alone are not enough."), {
    document: {
      ...e,
      meta: V_(e.meta, { status: "uninitialized" })
    },
    statusChanged: !0
  }) : {
    document: e,
    statusChanged: !1
  };
}
function ZF(e, t = {}) {
  const n = kt(t.query, 120).toLowerCase(), r = String(t.elementType || t.type || "").trim(), o = String(t.category || t.cat || "").trim(), s = Math.max(0, Number(t.offset) || 0), a = Math.max(1, Math.min(Co, Number(t.limit) || 30)), i = e.elements.filter((d) => {
    const f = Wx(d) || "";
    return r && f !== r || o && d.cat !== o ? !1 : n ? [
      d.id,
      d.cat,
      f,
      d.text,
      d.icon
    ].map((m) => String(m || "").toLowerCase()).join(`
`).includes(n) : !0;
  }), l = i.slice(s, s + a).map(Yx), u = s + l.length < i.length ? s + l.length : 0;
  return {
    elements: l,
    count: i.length,
    truncated: u > 0,
    nextOffset: u
  };
}
function jF(e, t = {}) {
  const n = kt(t.query, 120).toLowerCase(), r = Mr(t.parent), o = String(t.status || "").trim(), s = Math.max(0, Number(t.offset) || 0), a = Math.max(1, Math.min(Co, Number(t.limit) || 30)), i = e.locations.filter((d) => r && d.parent !== r || o && d.status !== o ? !1 : n ? [
    d.key,
    d.name,
    d.scale,
    d.status,
    d.brief,
    ...d.aliases || []
  ].join(`
`).toLowerCase().includes(n) : !0), l = i.slice(s, s + a).map((d) => Sn(d)), u = s + l.length < i.length ? s + l.length : 0;
  return {
    locations: l,
    count: i.length,
    truncated: u > 0,
    nextOffset: u
  };
}
function eB(e, t = {}) {
  const n = kt(t.query, 120).toLowerCase(), r = Mr(t.from), o = Mr(t.to), s = String(t.kind || "").trim(), a = Math.max(0, Number(t.offset) || 0), i = Math.max(1, Math.min(Co, Number(t.limit) || 30)), l = e.links.filter((f) => r && f.from !== r || o && f.to !== o || s && f.kind !== s ? !1 : n ? [
    f.id,
    f.from,
    f.to,
    f.kind,
    f.label
  ].join(`
`).toLowerCase().includes(n) : !0), u = l.slice(a, a + i).map((f) => Sn(f)), d = a + u.length < l.length ? a + u.length : 0;
  return {
    links: u,
    count: l.length,
    truncated: d > 0,
    nextOffset: d
  };
}
function tB(e = {}) {
  if (!wr(e)) throw new Error("atlas_location_set_required");
  const t = {};
  if ("name" in e) {
    const n = kt(e.name, 120);
    if (!n) throw new Error("atlas_location_name_required");
    t.name = n;
  }
  if ("scale" in e && (t.scale = HF(e.scale)), "status" in e && (t.status = zF(e.status)), "parent" in e) {
    const n = Mr(e.parent);
    n ? t.parent = n : t.parent = void 0;
  }
  if ("mapDocId" in e) {
    const n = kt(e.mapDocId, 80);
    n ? t.mapDocId = DF(n, "atlas_location_map_doc_id_invalid") : t.mapDocId = void 0;
  }
  if ("aliases" in e && (t.aliases = Jx(e.aliases) || []), "brief" in e) {
    const n = kt(e.brief, 240);
    n ? t.brief = n : t.brief = void 0;
  }
  return t;
}
function nB(e, t) {
  if (!Array.isArray(t)) throw new Error("state_patch_ops_must_be_array");
  if (!t.length) throw new Error("state_patch_ops_required");
  if (t.length > Dx) throw new Error("state_patch_ops_limit_exceeded");
  let n = Eb(e);
  const r = [], o = [], s = [], a = /* @__PURE__ */ new Set();
  let i = 0, l = 0, u = !1, d;
  const f = () => new Map(n.locations.map((A) => [A.key, A])), m = (A) => n.links.findIndex((I) => I.id === A), v = (A) => n.actors.findIndex((I) => I.actorKey === A), y = () => {
    const A = f();
    n.locations.forEach((I) => {
      if (I.parent) {
        if (I.parent === I.key) throw new Error(`atlas_parent_cycle:${I.key}`);
        if (!A.has(I.parent)) throw new Error(`atlas_parent_not_found:${I.parent}`);
      }
    }), n.locations.forEach((I) => {
      if (YF(A, I.key)) throw new Error(`atlas_parent_cycle:${I.key}`);
    });
  }, T = (A, I) => {
    const R = f();
    if (!R.has(A)) throw new Error(`atlas_link_location_not_found:${A}`);
    if (!R.has(I)) throw new Error(`atlas_link_location_not_found:${I}`);
  }, C = (A, I) => {
    r.push(Sn(A)), a.add(I), u = !0, i += 1;
  };
  return t.forEach((A, I) => {
    try {
      if (!wr(A)) throw new Error("atlas_op_invalid");
      const R = String(A.op || "").trim();
      if (R === "upsert-location") {
        const $ = uc(A.key, "atlas_location_key_invalid"), p = tB(A.set || {}), b = Array.isArray(A.unset) ? A.unset.map((z) => String(z || "").trim()) : [], w = b.find((z) => !$F.has(z));
        if (w) throw new Error(`atlas_unset_field_invalid:${w || "empty"}`);
        const E = b.filter(Boolean), x = n.locations.findIndex((z) => z.key === $);
        if (x < 0) {
          if (!p.name || !p.scale || !p.status) throw new Error(`atlas_location_create_required:${$}`);
          const z = {
            key: $,
            name: p.name,
            scale: p.scale,
            status: p.status
          };
          p.parent && (z.parent = p.parent), p.mapDocId && (z.mapDocId = p.mapDocId), p.aliases?.length && (z.aliases = p.aliases), p.brief && (z.brief = p.brief), n.locations.push(z), y(), C({
            op: "upsert-location",
            key: $,
            set: Sn(p),
            ...E.length ? { unset: E } : {}
          }, `location:${$}`);
          return;
        }
        const M = n.locations[x], N = {
          ...M,
          ...p
        }, q = { ...p };
        if (M.status === "visited" && p.status === "mentioned" && (N.status = "visited", q.status = "visited", s.push(`${$} is already visited; status was not downgraded to mentioned.`)), E.forEach((z) => {
          delete N[z];
        }), !N.name || !N.scale || !N.status) throw new Error(`atlas_location_required_field:${$}`);
        if (n.locations[x] = N, y(), ru(M, N)) {
          l += 1;
          return;
        }
        C({
          op: "upsert-location",
          key: $,
          set: Sn(q),
          ...E.length ? { unset: E } : {}
        }, `location:${$}`);
        return;
      }
      if (R === "remove-location") {
        const $ = uc(A.key, "atlas_location_key_invalid"), p = n.locations.findIndex((b) => b.key === $);
        if (p < 0) throw new Error(`atlas_location_not_found:${$}`);
        if (n.activeLocationKey === $) throw new Error(`atlas_location_active:${$}`);
        if (n.locations.some((b) => b.parent === $)) throw new Error(`atlas_location_has_children:${$}`);
        if (n.links.some((b) => b.from === $ || b.to === $)) throw new Error(`atlas_location_has_links:${$}`);
        if (n.actors.some((b) => b.locationKey === $)) throw new Error(`atlas_location_has_actors:${$}`);
        n.locations.splice(p, 1), C({
          op: "remove-location",
          key: $
        }, `location:${$}`);
        return;
      }
      if (R === "upsert-link") {
        const $ = uc(A.from, "atlas_link_from_required"), p = uc(A.to, "atlas_link_to_required"), b = WF(A.kind), w = A.bidirectional !== !1, E = kt(A.id, 160) || Y_($, p, b, w);
        T($, p);
        const x = {
          id: E,
          from: $,
          to: p,
          kind: b,
          bidirectional: w
        }, M = kt(A.label, 120);
        M && (x.label = M);
        const N = m(E);
        if (N >= 0) {
          if (ru(n.links[N], x)) {
            l += 1;
            return;
          }
          n.links[N] = x;
        } else n.links.push(x);
        C({
          op: "upsert-link",
          id: E,
          from: $,
          to: p,
          kind: b,
          ...M ? { label: M } : {},
          bidirectional: w
        }, `link:${E}`);
        return;
      }
      if (R === "remove-link") {
        let $ = kt(A.id, 160);
        if (!$) {
          const b = Mr(A.from), w = Mr(A.to), E = String(A.kind || "").trim();
          if (!b || !w || !Bd.has(E)) throw new Error("atlas_link_remove_locator_required");
          $ = Y_(b, w, E, A.bidirectional !== !1);
        }
        const p = m($);
        if (p < 0) throw new Error(`atlas_link_not_found:${$}`);
        n.links.splice(p, 1), C({
          op: "remove-link",
          id: $
        }, `link:${$}`);
        return;
      }
      if (R === "move-actor") {
        const $ = Ud(A.actorKey), p = uc(A.locationKey, "atlas_actor_location_required");
        if (!$) throw new Error("atlas_actor_key_required");
        if (!f().get(p)) throw new Error(`atlas_location_not_found:${p}`);
        const b = {
          actorKey: $,
          locationKey: p
        }, w = v($), E = w >= 0 ? n.actors[w] : null;
        if (E && ru(E, b) && !($ === "player" && n.activeLocationKey !== p)) {
          l += 1;
          return;
        }
        if (w >= 0 ? n.actors[w] = b : n.actors.push(b), $ === "player") {
          n.activeLocationKey = p;
          const x = n.locations.findIndex((M) => M.key === p);
          x >= 0 && n.locations[x].status !== "visited" && (n.locations[x] = {
            ...n.locations[x],
            status: "visited"
          }), d = n.locations[x]?.mapDocId;
        }
        C({
          op: "move-actor",
          actorKey: $,
          locationKey: p
        }, `actor:${$}`);
        return;
      }
      throw new Error(`atlas_op_not_supported:${R || "unknown"}`);
    } catch (R) {
      o.push({
        index: I,
        error: R instanceof Error ? R.message : String(R || "atlas_op_failed")
      });
    }
  }), {
    document: n,
    effectiveOps: r,
    appliedCount: i,
    satisfiedCount: l,
    failed: o,
    warnings: s,
    changedIds: [...a],
    changed: u,
    syncedActiveMapDocId: d
  };
}
function rB(e, t) {
  if (!wr(e)) throw new Error(`map_modify_set_required:${t}`);
  const n = {};
  if ("id" in e && kt(e.id, 120) && kt(e.id, 120) !== t) throw new Error(`map_element_id_cannot_change:${t}`);
  if ("at" in e || "pos" in e || "center" in e || "position" in e || "x" in e || "y" in e || "cx" in e || "cy" in e) {
    const o = Da(e.at ?? e.pos ?? e.center ?? e.position) || Da({
      x: e.x,
      y: e.y
    }) || Da({
      x: e.cx,
      y: e.cy
    });
    if (!o) throw new Error(`map_element_at_required:${t}`);
    n.at = o;
  }
  if ("cat" in e && (n.cat = um(e.cat, "wall")), "rect" in e || "size" in e || "width" in e || "height" in e) {
    const o = wb(e.rect ?? e.size ?? [e.width, e.height]);
    if (!o || o[0] <= 0 || o[1] <= 0) throw new Error(`map_element_rect_invalid:${t}`);
    n.rect = o;
  }
  if ("circle" in e || "r" in e || "radius" in e) {
    const o = fg(e.circle ?? e.r ?? e.radius);
    if (o === null) throw new Error(`map_element_radius_required:${t}`);
    n.circle = o;
  }
  if ("path" in e || "points" in e || "line" in e) {
    const o = n.at || null, s = rs(e.path ?? e.points ?? e.line, t, o);
    n.at || (n.at = s.at), n.path = s.points;
  }
  if ("curve" in e) {
    const o = n.at || null, s = rs(e.curve, t, o);
    n.at || (n.at = s.at), n.curve = s.points;
  }
  if ("icon" in e) {
    const o = Sb(e.icon);
    if (!o) throw new Error(`map_element_icon_invalid:${t}`);
    n.icon = o;
  }
  if ("text" in e || "content" in e || "label" in e || "value" in e) {
    const o = kt(e.text ?? e.content ?? e.label ?? e.value, 240);
    if (!o) throw new Error(`map_element_text_required:${t}`);
    n.text = o;
  }
  "closed" in e && (n.closed = e.closed === !0), "fill" in e && (n.fill = e.fill === null ? void 0 : kt(e.fill, 120) || void 0), "style" in e && (n.style = e.style === null ? void 0 : Tb(e.style));
  const r = String(e.type || "").trim();
  if (r === "fill" && !n.path) {
    const o = rs(e.points, t, n.at || null);
    n.at = o.at, n.path = o.points, n.closed = !0;
  }
  if (r === "arc" && !n.curve) {
    const o = rs(Gx(e, t), t, n.at || null);
    n.at = o.at, n.curve = o.points;
  }
  return n;
}
function tT(e, t) {
  return ip(Rx(e, t), e.id, { allowReservedId: nm(e.id) });
}
async function oB(e = "", t = Qn, n = _r) {
  if (t !== Qn || n !== _r) return await Ha(e, t, n);
  const r = await Ha(e, t, n);
  return r || await lf(e, { touchSession: !1 });
}
async function Q_(e = "") {
  const t = await Ha(e, yr, Io);
  return t || (await lf(e, { touchSession: !1 }), await Ha(e, yr, Io));
}
function sB(e, t) {
  const n = [];
  return e.elements.forEach((r) => {
    n.push({
      op: "remove",
      id: r.id,
      _internalSoft: !0
    });
  }), n.push({
    op: "meta",
    set: {
      name: t.meta.name,
      viewBox: t.meta.viewBox,
      theme: t.meta.theme,
      status: t.meta.status,
      hint: t.meta.hint
    }
  }), t.elements.forEach((r) => {
    n.push({
      op: "add",
      element: r
    });
  }), n;
}
function aB(e, t) {
  if (!Array.isArray(t)) throw new Error("state_patch_ops_must_be_array");
  if (!t.length) throw new Error("state_patch_ops_required");
  if (t.length > Dx) throw new Error("state_patch_ops_limit_exceeded");
  let n = cm(e, e.meta, "stored-document");
  const r = [], o = [], s = /* @__PURE__ */ new Set(), a = [], i = [];
  let l = 0, u = 0, d = !1;
  const f = (C) => n.elements.findIndex((A) => A.id === C), m = (C) => {
    const A = V_(n.meta, C);
    if (ru(A, n.meta)) {
      u += 1;
      return;
    }
    n.meta = A, r.push({
      op: "meta",
      set: Sn(C)
    }), d = !0, l += 1, s.add("meta");
  }, v = (C) => {
    const A = f(C.id);
    if (A >= 0) {
      if (ru(n.elements[A], C)) {
        u += 1;
        return;
      }
      throw new Error(`map_element_already_exists:${C.id}`);
    }
    n.elements.push(Sn(C)), r.push({
      op: "add",
      element: Sn(C)
    }), d = !0, l += 1, s.add(C.id);
  }, y = (C, A = {}) => {
    const I = f(C);
    if (I < 0) {
      if (A.soft) {
        u += 1;
        return;
      }
      throw new Error(`map_element_not_found:${C}`);
    }
    const [R] = n.elements.splice(I, 1);
    if (a.push(Sn(R)), r.push({
      op: "remove",
      id: C,
      ...A.soft ? { _internalSoft: !0 } : {}
    }), d = !0, l += 1, s.add(C), A.cascadeLabel !== !1 && !nm(C)) {
      const $ = rm(C);
      f($) >= 0 && y($, {
        soft: !0,
        cascadeLabel: !1
      });
    }
  }, T = (C, A) => {
    const I = f(C);
    if (I < 0) throw new Error(`map_element_not_found:${C}`);
    const R = n.elements[I], $ = tT(R, A);
    if (ru(R, $)) {
      u += 1;
      return;
    }
    if (n.elements[I] = $, r.push({
      op: "modify",
      id: C,
      set: Sn(A)
    }), d = !0, l += 1, s.add(C), !nm(C) && A.at && R.at) {
      const p = rm(C), b = f(p);
      if (b >= 0) {
        const w = n.elements[b], E = [A.at[0] - R.at[0], A.at[1] - R.at[1]];
        if (E[0] || E[1]) {
          const x = [Number((w.at[0] + E[0]).toFixed(2)), Number((w.at[1] + E[1]).toFixed(2))], M = tT(w, { at: x });
          n.elements[b] = M, r.push({
            op: "modify",
            id: p,
            set: { at: x }
          }), s.add(p);
        }
      }
    }
  };
  for (let C = 0; C < t.length; C += 1) {
    const A = t[C];
    try {
      if (!wr(A)) throw new Error("op_must_be_object");
      const I = String(A.op || "").trim();
      if (!I) throw new Error("map_op_not_supported:empty");
      if (I === "meta") {
        m(BF(A.set ?? A.changes ?? A.meta));
        continue;
      }
      if (I === "add") {
        J_(A.element ?? A, { source: "model-input" }).forEach((R) => v(R));
        continue;
      }
      if (I === "remove") {
        const R = kt(A.id, 120);
        if (!R) throw new Error("map_element_id_invalid");
        y(R);
        continue;
      }
      if (I === "modify") {
        const R = kt(A.id, 120);
        if (!R) throw new Error("map_element_id_invalid");
        T(R, rB(A.set ?? A.changes, R));
        continue;
      }
      if (I === "replace") {
        const R = kt(A.id, 120);
        if (!R) throw new Error("map_element_id_invalid");
        if (f(R) < 0) throw new Error(`map_element_not_found:${R}`);
        y(R, { cascadeLabel: !0 }), J_(A.element, {
          forcedId: R,
          source: "model-input"
        }).forEach(($) => v($));
        continue;
      }
      if (I === "init" || I === "reset") {
        const R = cm(A.document || {
          meta: {
            ...wr(A.meta) ? A.meta : {},
            status: "active"
          },
          elements: A.elements
        }, {
          ...n.meta,
          status: "active",
          hint: n.meta.hint
        }, "model-input");
        if (I === "init" && n.meta.status === "active" && n.elements.length && A.replaceDocument !== !0) throw new Error("state_init_existing_document_requires_reset");
        sB(n, R).forEach(($) => {
          $.op === "meta" ? m($.set) : $.op === "add" ? v($.element) : $.op === "remove" && y($.id, {
            soft: $._internalSoft === !0,
            cascadeLabel: !1
          });
        });
        continue;
      }
      throw new Error(`map_op_not_supported:${I}`);
    } catch (I) {
      const R = I instanceof Error ? I.message : String(I || "map_op_failed");
      i.push({
        index: C,
        error: R,
        hint: Ab(R)
      });
      break;
    }
  }
  if (!i.length) {
    const C = QF(n, o);
    n = C.document, C.statusChanged && (r.push({
      op: "meta",
      set: { status: n.meta.status }
    }), d = !0, s.add("meta"));
  }
  return !i.length && n.elements.length > q_ && i.push({
    index: -1,
    error: "map_elements_limit_exceeded",
    hint: `Map has more than ${q_} elements. Split or reduce the element count.`
  }), {
    document: n,
    effectiveOps: r,
    appliedCount: l,
    satisfiedCount: u,
    failed: i,
    warnings: o,
    changedIds: [...s],
    removedElements: a,
    changed: d
  };
}
function iB() {
  return {
    type: "object",
    description: "One map element. It must have `id` and `cat`, plus exactly one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. Most elements use `at:[x,y]`; `path` and `curve` may omit `at` and let the first point become the anchor.",
    properties: {
      id: {
        type: "string",
        description: "Stable unique id. Do not use the reserved `__label__` prefix."
      },
      at: {
        type: "array",
        items: { type: "number" },
        minItems: 2,
        maxItems: 2,
        description: "Anchor coordinate `[x,y]`. Most elements should provide `at`; `path` and `curve` may omit it and use the first point as the anchor. North = smaller y, south = larger y, west = smaller x, east = larger x."
      },
      cat: {
        type: "string",
        enum: [...bb],
        description: "Semantic category such as wall, door, marker, actor, terrain, road, or label."
      },
      rect: {
        type: "array",
        items: { type: "number" },
        minItems: 2,
        maxItems: 2,
        description: "Rectangle `[width,height]`. The top-left corner is at `at`."
      },
      circle: {
        type: "number",
        description: "Circle radius. The center is at `at`."
      },
      path: {
        type: "array",
        items: {
          type: "array",
          items: { type: "number" },
          minItems: 2,
          maxItems: 2
        },
        description: "Polyline point array. With `at`, points are relative offsets. Without `at`, points are absolute coordinates and the first point becomes the anchor."
      },
      curve: {
        type: "array",
        items: {
          type: "array",
          items: { type: "number" },
          minItems: 2,
          maxItems: 2
        },
        description: "Smooth curve point array. The anchor and relative/absolute rules are the same as `path`."
      },
      icon: {
        type: "string",
        enum: [...Lx],
        description: "Named icon marker."
      },
      text: {
        type: "string",
        description: "Short label text. If you provide text together with geometry in an add input, the runtime will split the text into a derived label element automatically."
      },
      actorKey: {
        type: "string",
        description: 'Optional full-session actor identity key for cat:"actor". If omitted, the element id is used as the actor key fallback.'
      },
      closed: {
        type: "boolean",
        description: "Whether a path or curve should be closed."
      },
      fill: {
        type: "string",
        description: "Fill token or color name for closed shapes."
      },
      style: {
        type: "object",
        description: "Optional visual style overrides.",
        properties: {
          color: {
            type: "string",
            description: "Stroke or text color."
          },
          width: {
            type: "number",
            description: "Stroke width."
          },
          dash: {
            type: "string",
            description: "Dash pattern string."
          }
        },
        additionalProperties: !1
      }
    },
    required: ["id", "cat"],
    additionalProperties: !1
  };
}
function lB() {
  const e = iB();
  return [
    {
      type: "function",
      function: {
        name: Xr.LIST,
        description: [
          "List structured map documents in the current RP session.",
          "Returns document entries only. It does not read full state, elements, or patch history.",
          "Use before MapInspect when you need available scene-map documents, or when you need the atlas world index.",
          "Map scope is fixed to this tavern session; it cannot inspect text sources, memory Markdown, RP chat history, character cards, world books, settings, or plugin source code."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: { docType: {
            type: "string",
            enum: [Qn, yr],
            description: "Optional structured document type filter: `tavern.map` for scene maps, `tavern.atlas` for the world index."
          } },
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Xr.READ,
        description: [
          "Inspect a structured scene map or world atlas document in the current RP session.",
          "Use `summary` first when choosing what to patch, checking revision, or deciding whether a map/atlas update is needed.",
          "For `tavern.map`, use `summary` first, `elements` to browse map elements, `element` for one id, `document` for the full map, and `history` for saved map patch transactions.",
          "For `tavern.atlas`, use `summary`, `document`, `locations`, `location`, `links`, `actors`, or `history`. Atlas does not have map elements.",
          "This inspects structured spatial data only. Use LS/Grep/Read for text evidence, memory files, chat history, and worldbook material."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            docType: {
              type: "string",
              enum: [Qn, yr],
              description: "Structured document type. Use `tavern.map` for scene maps or `tavern.atlas` for the world index."
            },
            docId: {
              type: "string",
              description: "Structured document id. Omit for the active map; atlas always uses `main`."
            },
            mode: {
              type: "string",
              enum: [
                "summary",
                "elements",
                "document",
                "element",
                "history",
                "locations",
                "location",
                "links",
                "actors"
              ],
              description: "For maps: summary/elements/document/element/history. For atlas: summary/document/locations/location/links/actors/history."
            },
            elementId: {
              type: "string",
              description: "Required for `element` mode. Exact element id to read."
            },
            locationKey: {
              type: "string",
              description: "Required for atlas `location` mode."
            },
            actorKey: {
              type: "string",
              description: "Optional atlas `actors` filter."
            },
            parent: {
              type: "string",
              description: "Optional atlas `locations` parent filter."
            },
            status: {
              type: "string",
              enum: ["mentioned", "visited"],
              description: "Optional atlas `locations` status filter."
            },
            from: {
              type: "string",
              description: "Optional atlas `links` from filter."
            },
            to: {
              type: "string",
              description: "Optional atlas `links` to filter."
            },
            kind: {
              type: "string",
              enum: [...Bd],
              description: "Optional atlas `links` kind filter."
            },
            elementType: {
              type: "string",
              enum: [..._b],
              description: "Optional `elements`-mode shape filter such as rect, circle, path, curve, icon, or text."
            },
            category: {
              type: "string",
              enum: [...bb],
              description: "Optional `elements`-mode category filter such as wall, door, marker, terrain, road, or label."
            },
            query: {
              type: "string",
              description: "Optional `elements`-mode text query matched against id, category, shape, icon, and label text."
            },
            offset: {
              type: "number",
              minimum: 0,
              description: "Pagination offset for `elements` or `history` results. Default 0."
            },
            limit: {
              type: "number",
              minimum: 1,
              maximum: Co,
              description: "Maximum `elements` or `history` results to return. Default 30 for `elements`, 20 for `history`."
            },
            tail: {
              type: "number",
              minimum: 1,
              maximum: Co,
              description: "For `history` mode, return the final N patch transactions."
            }
          },
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Xr.PATCH,
        description: [
          "Apply one structured map/atlas patch transaction to the current RP session.",
          "Use this only for confirmed spatial changes from RP source text or a user-requested correction. If nothing spatial changed, do not patch.",
          "Read MapInspect summary first unless you already have the current doc, ids, and revision from this turn. Use `baseRevision` when you are protecting against concurrent changes.",
          "For `tavern.map`, canonical ops are `meta`, `add`, `modify`, and `remove`. One MapPatch call is one atomic transaction and becomes exactly one revision when it saves.",
          "Use `meta` to update document fields such as name, viewBox, theme, status, or hint. Use `add` to create elements, `modify` to change existing ids, and `remove` to delete ids.",
          "Each element has `id` and `cat`, plus exactly one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. Most elements use `at:[x,y]`; `path` and `curve` may omit `at` and use the first point as the anchor.",
          'For `cat:"actor"`, optional `actorKey` is the full-session identity key. If omitted, the element id is used. The runtime keeps only the latest actor with the same final key across all map documents.',
          "With `at`, `path` and `curve` points are relative offsets. Without `at`, the points are treated as absolute coordinates and the stored result becomes relative to the first point.",
          "If one add element contains geometry plus text, the runtime splits the text into a system label element automatically.",
          "For `tavern.atlas/main`, use only `upsert-location`, `remove-location`, `upsert-link`, `remove-link`, and `move-actor`. There is no set-active-location op.",
          'Atlas links may omit `id`. The default link id is `link:${sorted(from,to).join(":")}:${kind}` for bidirectional links and `link:${from}:${to}:${kind}` for `bidirectional:false`. Use an explicit id only when two locations need multiple same-kind links.',
          'Move the player between places with `move-actor` and `actorKey:"player"`. That updates atlas.activeLocationKey, marks the location visited, and syncs activeMapDocId when the location has mapDocId. Non-player actors do not change the current location.',
          "Pass `activate:true` only for `tavern.map` to make that map document active for map tools. Do not use map activate to represent player movement.",
          "`meta.viewBox` is the camera. Changing it does not move elements. Move actors by changing their `at`, then adjust `viewBox` only if the camera should follow.",
          "The `ops` argument must be a real JSON array, not a quoted JSON string. With `dryRun:true`, validate without saving or incrementing revision.",
          "Legacy `init`, `reset`, and `replace` input is still absorbed at runtime, but do not rely on it in new calls."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            docType: {
              type: "string",
              enum: [Qn, yr],
              description: "Structured document type. Use `tavern.map` for scene maps or `tavern.atlas` for the world index."
            },
            docId: {
              type: "string",
              description: "Structured document id. For scene maps, prefer an explicit stable place-named docId; omit only when intentionally maintaining the currently active scene-map doc. Atlas always uses `main`."
            },
            baseRevision: {
              type: "number",
              description: "Optional optimistic revision check from MapInspect summary/document."
            },
            dryRun: {
              type: "boolean",
              description: "Validate and simulate the transaction without saving or incrementing the revision."
            },
            activate: {
              type: "boolean",
              description: "Set this map document as the current scene after the transaction. With `ops:[]`, this only switches the active map."
            },
            desc: {
              type: "string",
              description: "Short one-line summary of this turn’s spatial update."
            },
            ops: {
              type: "array",
              description: "Patch ops as one atomic transaction. For maps use `meta/add/modify/remove`. For atlas use `upsert-location/remove-location/upsert-link/remove-link/move-actor`.",
              items: {
                type: "object",
                properties: {
                  op: {
                    type: "string",
                    enum: [
                      "meta",
                      "add",
                      "modify",
                      "remove",
                      "upsert-location",
                      "remove-location",
                      "upsert-link",
                      "remove-link",
                      "move-actor"
                    ],
                    description: "Operation type. Map ops and atlas ops are selected by docType."
                  },
                  id: {
                    type: "string",
                    description: "Map element id for `modify`/`remove`, or atlas link id for `upsert-link`/`remove-link`. Atlas links may omit it and use the default id rule."
                  },
                  key: {
                    type: "string",
                    description: "Atlas location key for upsert-location/remove-location."
                  },
                  locationKey: {
                    type: "string",
                    description: "Atlas target location key for move-actor."
                  },
                  actorKey: {
                    type: "string",
                    description: 'Atlas actor key for move-actor. Use actorKey:"player" for the player.'
                  },
                  from: {
                    type: "string",
                    description: "Atlas link source location key."
                  },
                  to: {
                    type: "string",
                    description: "Atlas link target location key."
                  },
                  kind: {
                    type: "string",
                    enum: [...Bd],
                    description: "Atlas link kind."
                  },
                  label: {
                    type: "string",
                    description: "Optional atlas link label."
                  },
                  bidirectional: {
                    type: "boolean",
                    description: "Atlas link direction flag. Defaults true."
                  },
                  unset: {
                    type: "array",
                    items: {
                      type: "string",
                      enum: [
                        "parent",
                        "mapDocId",
                        "aliases",
                        "brief"
                      ]
                    },
                    description: "Atlas upsert-location optional fields to remove."
                  },
                  set: {
                    type: "object",
                    description: "For map `meta`/`modify`, fields to update. For atlas `upsert-location`, location fields to merge."
                  },
                  element: {
                    ...e,
                    description: "Full element object for `add`."
                  }
                },
                required: ["op"],
                additionalProperties: !1
              }
            }
          },
          required: ["ops"],
          additionalProperties: !1
        }
      }
    }
  ];
}
async function uB(e = "", t = "", n = {}, r = {}) {
  const o = String(e || "").trim(), s = MF(t);
  if (!o) return {
    ok: !1,
    summary: "Missing sessionId.",
    error: "state_session_required"
  };
  try {
    const a = kt(n.docType, 40), i = !!a, l = LF(a || Qn), u = FF(l, kt(n.docId, 80) || (l === Qn && (s === Xr.READ || s === Xr.PATCH) ? (await lm(o)).activeDocId : l === yr ? Io : _r));
    if (s === Xr.LIST) {
      const d = !i, f = d || l === Qn ? await lm(o, { includeStale: !0 }) : null, m = d ? await F_(o, { includeStale: !0 }) : f?.documents || await F_(o, {
        docType: l,
        includeStale: !0
      }), v = f?.activeDocId || _r, y = [...m].sort((T, C) => {
        const A = T.docType === Qn && T.docId === v;
        return A !== (C.docType === Qn && C.docId === v) ? A ? -1 : 1 : T.docType !== C.docType ? T.docType.localeCompare(C.docType) : (Number(C.updatedAt) || 0) - (Number(T.updatedAt) || 0) || (Number(C.revision) || 0) - (Number(T.revision) || 0);
      });
      return {
        ok: !0,
        summary: `Found ${y.length} map/atlas document(s).`,
        count: y.length,
        docId: v,
        documents: y.map((T) => ({
          docType: T.docType,
          docId: T.docId,
          title: T.title,
          revision: T.revision,
          digest: T.digest,
          status: T.status,
          updatedAt: T.updatedAt,
          active: T.docType === Qn ? T.docId === v : T.docType === yr && T.docId === Io
        }))
      };
    }
    if (s === Xr.READ) {
      const d = String(n.mode || "summary").trim() || "summary";
      if (l === yr) {
        const v = await Q_(o);
        if (!v) return {
          ok: !1,
          summary: `${l}/${u} does not exist.`,
          docType: l,
          docId: u,
          error: "state_document_not_found"
        };
        const y = qd(v);
        if (d === "summary") return {
          ok: !0,
          summary: `Read atlas summary, revision ${v.revision}.`,
          docType: l,
          docId: u,
          title: v.title,
          revision: v.revision,
          digest: lp(y),
          activeLocationKey: y.activeLocationKey,
          count: y.locations.length,
          details: {
            locationCount: y.locations.length,
            linkCount: y.links.length,
            actorCount: y.actors.length
          }
        };
        if (d === "document") return {
          ok: !0,
          summary: `Read full atlas, revision ${v.revision}.`,
          docType: l,
          docId: u,
          title: v.title,
          revision: v.revision,
          digest: lp(y),
          activeLocationKey: y.activeLocationKey,
          document: y
        };
        if (d === "locations") {
          const T = jF(y, n);
          return {
            ok: !0,
            summary: `Matched ${T.count} atlas location(s); returned ${T.locations.length}.`,
            docType: l,
            docId: u,
            revision: v.revision,
            count: T.count,
            truncated: T.truncated,
            nextOffset: T.nextOffset,
            locations: T.locations
          };
        }
        if (d === "location") {
          const T = Mr(n.locationKey ?? n.key);
          if (!T) return {
            ok: !1,
            summary: "Missing locationKey.",
            docType: l,
            docId: u,
            error: "atlas_location_key_required"
          };
          const C = y.locations.find((A) => A.key === T);
          return C ? {
            ok: !0,
            summary: `Read atlas location ${C.name}.`,
            docType: l,
            docId: u,
            revision: v.revision,
            location: {
              ...Sn(C),
              links: y.links.filter((A) => A.from === T || A.to === T).map((A) => Sn(A)),
              actors: y.actors.filter((A) => A.locationKey === T).map((A) => Sn(A))
            }
          } : {
            ok: !1,
            summary: `${T} does not exist.`,
            docType: l,
            docId: u,
            revision: v.revision,
            error: "atlas_location_not_found"
          };
        }
        if (d === "links") {
          const T = eB(y, n);
          return {
            ok: !0,
            summary: `Matched ${T.count} atlas link(s); returned ${T.links.length}.`,
            docType: l,
            docId: u,
            revision: v.revision,
            count: T.count,
            truncated: T.truncated,
            nextOffset: T.nextOffset,
            links: T.links
          };
        }
        if (d === "actors") {
          const T = Ud(n.actorKey), C = T ? y.actors.filter((A) => A.actorKey === T) : y.actors;
          return {
            ok: !0,
            summary: `Matched ${C.length} atlas actor position(s).`,
            docType: l,
            docId: u,
            revision: v.revision,
            count: C.length,
            actors: C.map((A) => Sn(A))
          };
        }
        if (d === "history") {
          const T = await am({
            sessionId: o,
            docType: l,
            docId: u
          }), C = Math.max(0, Number(n.tail) || 0), A = Math.max(1, Math.min(Co, Number(n.limit) || 20)), I = Math.max(0, Number(n.offset) || 0), R = C > 0 ? Math.max(0, T.length - Math.min(Co, C)) : I, $ = T.slice(R, R + (C > 0 ? Math.min(Co, C) : A)), p = R + $.length < T.length ? R + $.length : 0;
          return {
            ok: !0,
            summary: `Found ${T.length} saved atlas patch transaction(s); returned ${$.length}.`,
            docType: l,
            docId: u,
            revision: v.revision,
            count: T.length,
            truncated: p > 0,
            nextOffset: p,
            patches: $
          };
        }
        return {
          ok: !1,
          summary: `Unsupported atlas MapInspect mode: ${d}`,
          docType: l,
          docId: u,
          error: "state_read_mode_invalid"
        };
      }
      const f = await oB(o, l, u);
      if (!f) return {
        ok: !1,
        summary: `${l}/${u} does not exist.`,
        docType: l,
        docId: u,
        error: "state_document_not_found"
      };
      const m = Vi(f);
      if (d === "summary") return {
        ok: !0,
        summary: `Read summary for ${f.title || u}, revision ${f.revision}, status ${m.meta.status}.`,
        docType: l,
        docId: u,
        title: f.title,
        revision: f.revision,
        digest: f.digest,
        meta: Sn(m.meta),
        elementCount: m.elements.length
      };
      if (d === "document") return {
        ok: !0,
        summary: `Read full state for ${f.title || u}, revision ${f.revision}.`,
        docType: l,
        docId: u,
        title: f.title,
        revision: f.revision,
        digest: f.digest,
        meta: Sn(m.meta),
        elementCount: m.elements.length,
        document: m
      };
      if (d === "element") {
        const v = kt(n.elementId, 120);
        if (!v) return {
          ok: !1,
          summary: "Missing elementId.",
          docType: l,
          docId: u,
          error: "state_element_id_required"
        };
        const y = m.elements.find((T) => T.id === v);
        return y ? {
          ok: !0,
          summary: `Read element ${v}.`,
          docType: l,
          docId: u,
          revision: f.revision,
          element: Yx(y)
        } : {
          ok: !1,
          summary: `${v} does not exist.`,
          docType: l,
          docId: u,
          revision: f.revision,
          error: "state_element_not_found"
        };
      }
      if (d === "elements") {
        const v = ZF(m, n);
        return {
          ok: !0,
          summary: `Matched ${v.count} map element(s); returned ${v.elements.length}.`,
          docType: l,
          docId: u,
          revision: f.revision,
          count: v.count,
          truncated: v.truncated,
          nextOffset: v.nextOffset,
          elements: v.elements
        };
      }
      if (d === "history") {
        const v = await am({
          sessionId: o,
          docType: l,
          docId: u
        }), y = Math.max(0, Number(n.tail) || 0), T = Math.max(1, Math.min(Co, Number(n.limit) || 20)), C = Math.max(0, Number(n.offset) || 0), A = y > 0 ? Math.max(0, v.length - Math.min(Co, y)) : C, I = v.slice(A, A + (y > 0 ? Math.min(Co, y) : T)), R = A + I.length < v.length ? A + I.length : 0;
        return {
          ok: !0,
          summary: `Found ${v.length} saved patch transaction(s); returned ${I.length}.`,
          docType: l,
          docId: u,
          revision: f.revision,
          count: v.length,
          truncated: R > 0,
          nextOffset: R,
          patches: I
        };
      }
      return {
        ok: !1,
        summary: `Unsupported MapInspect mode: ${d}`,
        docType: l,
        docId: u,
        error: "state_read_mode_invalid"
      };
    }
    if (s === Xr.PATCH) {
      if (!Array.isArray(n.ops)) return {
        ok: !1,
        summary: "MapPatch ops must be a real array.",
        docType: l,
        docId: u,
        error: "state_patch_ops_must_be_array"
      };
      const d = n.ops, f = n.activate === !0;
      return !d.length && !f ? {
        ok: !1,
        summary: "MapPatch ops are required unless activate:true is used to switch active map.",
        docType: l,
        docId: u,
        error: "state_patch_ops_required"
      } : l === yr && f ? {
        ok: !1,
        summary: 'Atlas has no activate:true operation. Move the player with move-actor(actorKey:"player").',
        docType: l,
        docId: u,
        error: "atlas_activate_not_supported"
      } : l === yr ? await Ot.transaction("rw", or, Fi, Yt, Et, async () => {
        await r.beforeWriteGuard?.();
        const m = await Q_(o), v = Number(m?.revision) || 0;
        if (Number.isFinite(Number(n.baseRevision)) && Number(n.baseRevision) !== v) return {
          ok: !1,
          summary: `Revision changed: current is ${v}, but this call was based on ${Number(n.baseRevision)}. Run MapInspect again before patching.`,
          docType: l,
          docId: u,
          revision: v,
          error: "state_revision_conflict"
        };
        const y = nB(qd(m), d);
        if (y.failed.length) {
          const R = eT(y.failed);
          return {
            ok: !1,
            summary: `Atlas patch was not saved: ${y.failed.length} op(s) failed.${R ? ` ${R}` : ""}`,
            docType: l,
            docId: u,
            revision: v,
            changed: !1,
            appliedCount: 0,
            satisfiedCount: y.satisfiedCount,
            failedCount: y.failed.length,
            warnings: y.warnings,
            error: "state_patch_failed",
            details: y.failed
          };
        }
        if (!y.changed) return {
          ok: !0,
          summary: "Atlas is already at the target result. No write was needed.",
          docType: l,
          docId: u,
          revision: v,
          changed: !1,
          appliedCount: 0,
          satisfiedCount: y.satisfiedCount,
          failedCount: 0,
          warnings: y.warnings
        };
        const T = v + 1, C = lp(y.document);
        if (n.dryRun === !0) return {
          ok: !0,
          summary: `Dry run passed: ${l}/${u} would advance to revision ${T} with ${y.appliedCount} applied op(s). Nothing was saved.`,
          docType: l,
          docId: u,
          title: X_(y.document),
          revision: v,
          digest: C,
          changed: !0,
          appliedCount: y.appliedCount,
          satisfiedCount: y.satisfiedCount,
          failedCount: 0,
          changedIds: y.changedIds,
          warnings: y.warnings,
          activeLocationKey: y.document.activeLocationKey,
          document: y.document
        };
        const A = H_(), I = await Q2({
          sessionId: o,
          docType: l,
          docId: u,
          title: X_(y.document),
          revision: T,
          data: y.document,
          digest: C,
          status: "active",
          source: r.caller || "auto",
          createdAt: Number(m?.createdAt) || A,
          updatedAt: A
        });
        return await B_({
          sessionId: o,
          docType: l,
          docId: u,
          revision: T,
          status: "active",
          managerRunId: r.managerRunId,
          sourceUserOrder: r.sourceUserOrder,
          sourceAssistantOrder: r.sourceAssistantOrder,
          source: r.caller || "auto",
          summary: kt(n.desc || `AtlasPatch ${T}`, 400),
          ops: y.effectiveOps,
          changedIds: y.changedIds,
          removedElements: []
        }), y.syncedActiveMapDocId && await hh(o, y.syncedActiveMapDocId), {
          ok: !0,
          summary: `Updated ${l}/${u} to revision ${I.revision} with ${y.appliedCount} applied op(s).`,
          docType: l,
          docId: u,
          title: I.title,
          revision: I.revision,
          digest: I.digest,
          changed: !0,
          appliedCount: y.appliedCount,
          satisfiedCount: y.satisfiedCount,
          failedCount: 0,
          changedIds: y.changedIds,
          warnings: y.warnings,
          activeLocationKey: y.document.activeLocationKey,
          document: y.document
        };
      }) : await Ot.transaction("rw", or, Fi, Yt, Et, async () => {
        await r.beforeWriteGuard?.();
        const m = await Ha(o, l, u), v = Number(m?.revision) || 0;
        if (Number.isFinite(Number(n.baseRevision)) && Number(n.baseRevision) !== v) return {
          ok: !1,
          summary: `Revision changed: current is ${v}, but this call was based on ${Number(n.baseRevision)}. Run MapInspect again before patching.`,
          docType: l,
          docId: u,
          revision: v,
          error: "state_revision_conflict"
        };
        if (!d.length && f) {
          if (!m) return {
            ok: !1,
            summary: `${l}/${u} does not exist.`,
            docType: l,
            docId: u,
            error: "state_document_not_found"
          };
          const x = await z_(o), M = await hh(o, u), N = await dy(o, u);
          return {
            ok: !0,
            summary: `Activated ${l}/${u}.`,
            docType: l,
            docId: u,
            title: m.title,
            revision: v,
            digest: m.digest,
            changed: x !== M,
            appliedCount: 0,
            satisfiedCount: 0,
            failedCount: 0,
            warnings: N
          };
        }
        const y = aB(m ? Vi(m) : Kx(), d);
        if (y.failed.length) {
          const x = eT(y.failed);
          return {
            ok: !1,
            summary: `MapPatch was not saved: ${y.failed.length} op(s) failed.${x ? ` ${x}` : ""}`,
            docType: l,
            docId: u,
            revision: v,
            changed: !1,
            appliedCount: 0,
            satisfiedCount: y.satisfiedCount,
            failedCount: y.failed.length,
            warnings: y.warnings,
            error: "state_patch_failed",
            details: y.failed
          };
        }
        if (!y.changed) {
          let x = !1;
          if (f && n.dryRun !== !0) {
            if (!m) return {
              ok: !1,
              summary: `${l}/${u} does not exist.`,
              docType: l,
              docId: u,
              error: "state_document_not_found"
            };
            x = await z_(o) !== await hh(o, u);
          }
          const M = f && n.dryRun !== !0 ? await dy(o, u) : [];
          return {
            ok: !0,
            summary: x ? `Activated ${l}/${u}. State was already at the target result.` : "State is already at the target result. No write was needed.",
            docType: l,
            docId: u,
            revision: v,
            changed: x,
            appliedCount: 0,
            satisfiedCount: y.satisfiedCount,
            failedCount: 0,
            warnings: [...y.warnings, ...M]
          };
        }
        const T = v + 1, C = H_();
        if (n.dryRun === !0) {
          const x = Ki(y.document, T);
          return {
            ok: !0,
            summary: `Dry run passed: ${l}/${u} would advance to revision ${T} with ${y.appliedCount} applied op(s). Nothing was saved.`,
            docType: l,
            docId: u,
            title: bu(y.document),
            revision: v,
            digest: x,
            changed: !0,
            appliedCount: y.appliedCount,
            satisfiedCount: y.satisfiedCount,
            failedCount: 0,
            changedIds: y.changedIds,
            removedElements: y.removedElements,
            warnings: y.warnings,
            meta: Sn(y.document.meta),
            elementCount: y.document.elements.length
          };
        }
        const A = await XF({
          sessionId: o,
          docType: l,
          docId: u,
          document: y.document,
          timestamp: C,
          managerRunId: r.managerRunId,
          sourceUserOrder: r.sourceUserOrder,
          sourceAssistantOrder: r.sourceAssistantOrder
        }), I = A.document, R = [...y.effectiveOps, ...A.removedElements.map((x) => ({
          op: "remove",
          id: x.id
        }))], $ = [...y.removedElements, ...A.removedElements], p = [.../* @__PURE__ */ new Set([...y.changedIds, ...A.removedElements.map((x) => x.id)])], b = Ki(I, T), w = await Q2({
          sessionId: o,
          docType: l,
          docId: u,
          title: bu(I),
          revision: T,
          data: I,
          digest: b,
          status: "active",
          source: r.caller || "auto",
          createdAt: Number(m?.createdAt) || C,
          updatedAt: C
        });
        await B_({
          sessionId: o,
          docType: l,
          docId: u,
          revision: T,
          status: "active",
          managerRunId: r.managerRunId,
          sourceUserOrder: r.sourceUserOrder,
          sourceAssistantOrder: r.sourceAssistantOrder,
          source: r.caller || "auto",
          summary: kt(n.desc || `MapPatch ${T}`, 400),
          ops: R,
          changedIds: p,
          removedElements: $
        }), f && await hh(o, u);
        const E = f ? await dy(o, u) : [];
        return {
          ok: !0,
          summary: `Updated ${l}/${u} to revision ${w.revision} with ${y.appliedCount} applied op(s).`,
          docType: l,
          docId: u,
          title: w.title,
          revision: w.revision,
          digest: w.digest,
          changed: !0,
          appliedCount: y.appliedCount,
          satisfiedCount: y.satisfiedCount,
          failedCount: 0,
          changedIds: p,
          removedElements: $,
          warnings: [...y.warnings, ...E],
          meta: Sn(I.meta),
          elementCount: I.elements.length
        };
      });
    }
    return {
      ok: !1,
      summary: `${t} is not available.`,
      error: "state_tool_not_available"
    };
  } catch (a) {
    const i = a instanceof Error ? a.name : "", l = a instanceof Error ? a.message : String(a || "state_tool_failed");
    if (i === "AbortError" || l === "manager_source_messages_changed" || l === "manager_epoch_expired" || l === "manager_aborted") throw a;
    return {
      ok: !1,
      summary: Ab(l) || l,
      error: l
    };
  }
}
async function cB(e = "") {
  const t = await lm(e), n = t.activeDocId, r = t.documents.map((i) => {
    const l = Vi(i);
    return {
      ...i,
      data: l,
      docType: i.docType,
      docId: i.docId,
      title: bu(l),
      revision: i.revision,
      digest: Ki(l, i.revision),
      status: i.status,
      updatedAt: i.updatedAt,
      active: i.docId === n
    };
  }).sort((i, l) => !!i.active != !!l.active ? i.active ? -1 : 1 : (Number(l.updatedAt) || 0) - (Number(i.updatedAt) || 0) || (Number(l.revision) || 0) - (Number(i.revision) || 0)), o = t.record, s = o ? Vi(o) : null, a = o ? await am({
    sessionId: e,
    docType: Qn,
    docId: o.docId,
    limit: 80
  }) : [];
  return !o || !s ? {
    documents: r,
    activeDocId: n,
    activeDocument: null,
    activePatches: []
  } : {
    documents: r,
    activeDocId: n,
    activeDocument: {
      ...o,
      data: s,
      title: bu(s),
      digest: Ki(s, o.revision)
    },
    activePatches: a
  };
}
async function dB(e = "") {
  const t = await Q_(e);
  if (!t) return {
    document: null,
    patches: [],
    activeLocationKey: ""
  };
  const n = qd(t), r = await am({
    sessionId: e,
    docType: yr,
    docId: Io,
    limit: 80
  });
  return {
    document: {
      ...t,
      data: n,
      title: X_(n),
      digest: lp(n)
    },
    patches: r,
    activeLocationKey: n.activeLocationKey || ""
  };
}
function nT(e) {
  return e ? e.brief ? `${e.name}（${e.brief}）` : e.name : "";
}
function fB(e = "") {
  return String(e || "").split(`
`).map((t) => t.trim()).find((t) => t.startsWith("标注："))?.replace(/^标注：/, "").trim() || "";
}
async function hB(e = "") {
  await lf(e, { touchSession: !1 });
  const t = qd(await Ha(e, yr, Io)), n = new Map(t.locations.map((f) => [f.key, f])), r = t.activeLocationKey ? n.get(t.activeLocationKey) : void 0;
  if (!r) return "";
  const o = r.parent ? n.get(r.parent) : void 0, s = t.links.flatMap((f) => f.from === r.key ? [n.get(f.to)] : f.bidirectional && f.to === r.key ? [n.get(f.from)] : []).filter((f) => !!f), a = t.locations.filter((f) => f.status === "visited").map((f) => f.name).filter(Boolean), i = t.locations.filter((f) => f.status === "mentioned").map((f) => f.name).filter(Boolean), l = t.actors.map((f) => {
    const m = n.get(f.locationKey);
    return m ? `${f.actorKey === "player" ? "玩家" : f.actorKey}=${m.name}` : "";
  }).filter(Boolean), u = r.mapDocId ? await Ha(e, Qn, r.mapDocId) : null, d = fB(u ? Ki(Vi(u), u.revision) : "");
  return [
    `当前地点：${nT(r)}`,
    o ? `上级地点：${o.name}` : "",
    s.length ? `相邻地点：${s.map((f) => nT(f)).join("、")}` : "",
    a.length ? `已探索地点：${a.join("、")}` : "",
    i.length ? `已知但未到达：${i.join("、")}` : "",
    l.length ? `人物位置：${l.join("，")}` : "",
    d ? `当前场景标注：${d}` : r.mapDocId ? "当前场景标注：暂无" : "当前地点暂无详细地图"
  ].filter(Boolean).join(`
`);
}
async function pB(e = "") {
  const { record: t } = await lm(e);
  if (!t) return [];
  const n = Vi(t);
  return n.meta.status !== "active" || ZO(n) ? [] : [{
    docType: t.docType,
    docId: t.docId,
    title: bu(n),
    revision: t.revision,
    digest: Ki(n, t.revision)
  }];
}
var Ji = {
  INSPECT: "EventInspect",
  PATCH: "EventPatch"
}, mB = { PATCH: "TaskPatch" };
function gB(e = "") {
  const t = String(e || "").trim();
  return t === mB.PATCH ? Ji.PATCH : t;
}
var vB = 200, yB = /(?:任务|目标|子目标|远景|已完成|\bquest\b|\btask\b|\bgoal\b|\bobjective\b|\bcomplete(?:d|s|ion)?\b)|(?:完成|完成剧情)(?=任务|目标|子目标|远景)|完成任务|任务完成|目标完成|子目标完成|任务已完成|目标已完成|子目标已完成/i;
function ar() {
  return Date.now();
}
function _B(e = "task") {
  return `${e}-${ar()}-${Math.random().toString(36).slice(2, 8)}`;
}
function hg(e) {
  return JSON.parse(JSON.stringify(e));
}
function co(e = "", t = 1200) {
  const n = String(e || "").replace(/\r\n/g, `
`).trim();
  return n.length > t ? n.slice(0, t) : n;
}
function Xx(e = "") {
  const t = String(e || "").trim();
  return /^[\w:.-]{1,120}$/i.test(t) ? t : "";
}
function bB(e = "") {
  const t = String(e || "").trim();
  return t === "upsert-task" ? "upsert-event" : t === "advance-task" ? "advance-event" : t === "complete-task" ? "complete-event" : t === "abandon-task" ? "abandon-event" : t;
}
function Z_(e = {}) {
  return Xx(e.eventId || e.taskId || e.id);
}
function uf(e = "") {
  return co(e, 240);
}
function j_(e = "") {
  return yB.test(String(e || ""));
}
function Gl(e, t = -1) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.floor(n) : t;
}
function pg(e = [], t = []) {
  return JSON.stringify({
    tasks: [...e].sort((n, r) => n.id.localeCompare(r.id)).map((n) => ({
      id: n.id,
      sessionId: n.sessionId,
      status: n.status,
      horizon: n.horizon,
      current: n.current,
      doneWhen: n.doneWhen,
      hookForUser: n.hookForUser,
      hookForModel: n.hookForModel,
      fingerprint: n.fingerprint,
      createdOrder: n.createdOrder,
      updatedOrder: n.updatedOrder,
      lastAdvancedOrder: n.lastAdvancedOrder,
      completedOrder: n.completedOrder ?? null,
      abandonedOrder: n.abandonedOrder ?? null,
      sourceManagerRunId: n.sourceManagerRunId || ""
    })),
    abandonedFingerprints: [...new Set(t)].sort()
  });
}
function mg(e = "") {
  const t = String(e || "");
  let n = 2166136261, r = 522970236;
  for (let o = 0; o < t.length; o += 1) {
    const s = t.charCodeAt(o);
    n ^= s, n = Math.imul(n, 16777619) >>> 0, r ^= s + o, r = Math.imul(r, 16777619) >>> 0;
  }
  return `${t.length.toString(36)}:${n.toString(16)}${r.toString(16)}`;
}
function wB(e = "", t = []) {
  const n = String(e || "").trim();
  if (!t.length) return n;
  const r = "rollback_conflict:", o = n.startsWith(r) ? n.slice(18).split(",").map((s) => s.trim()).filter(Boolean) : [];
  return `${r}${[.../* @__PURE__ */ new Set([...o, ...t])].join(",")}`;
}
async function Qx(e = "") {
  const [t, n] = await Promise.all([No(e, {
    includeAbandoned: !0,
    includeCompleted: !0
  }), cf(e)]);
  return mg(pg(t, n));
}
async function Zx(e = "", t) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.floor(n) : await hb(e) ?? -1;
}
function SB(e) {
  return mg(pg(e?.tasks || [], e?.abandonedFingerprints || []));
}
async function No(e = "", t = {}) {
  const n = String(e || "").trim();
  return n ? (await Tn.where("sessionId").equals(n).sortBy("updatedAt")).filter((r) => t.includeCompleted || r.status !== "completed").filter((r) => t.includeAbandoned || r.status !== "abandoned").sort((r, o) => Number(o.updatedOrder) - Number(r.updatedOrder) || Number(o.updatedAt) - Number(r.updatedAt)) : [];
}
async function TB(e = "", t = "") {
  const n = String(e || "").trim(), r = Xx(t);
  return !n || !r ? null : await Tn.get([n, r]) || null;
}
async function cf(e = "") {
  const t = String(e || "").trim();
  if (!t) return [];
  const n = await us.get(t);
  return Array.isArray(n?.abandonedFingerprints) ? n.abandonedFingerprints.map((r) => uf(r)).filter(Boolean) : [];
}
async function dm(e = "", t = []) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = [...new Set(t.map((o) => uf(o)).filter(Boolean))].slice(-vB);
  return await us.put({
    sessionId: n,
    abandonedFingerprints: r,
    updatedAt: ar()
  }), r;
}
async function jx(e = "", t = "") {
  const n = uf(t);
  n && await dm(e, [...(await cf(e)).filter((r) => r !== n), n]);
}
async function EB(e = "", t) {
  const n = String(e || "").trim();
  return n ? await Ot.transaction("rw", Tn, ls, us, async () => {
    const [r, o] = await Promise.all([No(n, {
      includeAbandoned: !0,
      includeCompleted: !0
    }), cf(n)]), s = await Zx(n, t);
    if (s === -1 && !r.length && !o.length) return null;
    const a = await tM(n), i = mg(pg(r, o));
    if (a && SB(a) === i) return null;
    const l = {
      sessionId: n,
      floor: s,
      tasks: r.map((u) => hg(u)),
      abandonedFingerprints: [...o],
      createdAt: ar()
    };
    return await ls.put(l), l;
  }) : null;
}
async function AB(e = "", t) {
  return Zx(e, t);
}
async function eM(e = "") {
  const t = String(e || "").trim();
  return t ? (await ls.where("sessionId").equals(t).sortBy("floor")).sort((n, r) => n.floor - r.floor || n.createdAt - r.createdAt) : [];
}
async function tM(e = "", t = Number.POSITIVE_INFINITY) {
  const n = String(e || "").trim(), r = Number(t);
  return n && (await eM(n)).filter((o) => Number(o.floor) <= r || r === Number.POSITIVE_INFINITY).sort((o, s) => Number(s.floor) - Number(o.floor) || Number(s.createdAt) - Number(o.createdAt))[0] || null;
}
async function nM(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  return !n || !Number.isFinite(r) ? 0 : await Ot.transaction("rw", ls, async () => {
    const o = (await eM(n)).filter((s) => Number(s.floor) >= r);
    return o.length ? (await ls.bulkDelete(o.map((s) => [s.sessionId, s.floor])), o.length) : 0;
  });
}
async function rM(e = "", t = -1) {
  const n = String(e || "").trim();
  if (!n) throw new Error("task_session_required");
  return await Ot.transaction("rw", Tn, ls, us, async () => {
    const r = await tM(n, t), o = await Tn.where("sessionId").equals(n).toArray();
    if (o.length && await Tn.bulkDelete(o.map((a) => [a.sessionId, a.id])), !r)
      return await dm(n, []), [];
    const s = (r.tasks || []).map((a) => ({
      ...hg(a),
      sessionId: n
    }));
    return s.length && await Tn.bulkPut(s), await dm(n, r.abandonedFingerprints || []), s;
  });
}
async function oM(e = "", t = "") {
  const n = String(e || "").trim(), r = String(t || "").trim();
  if (!n || !r) return null;
  const o = await Cr.get(n);
  if (o) return o;
  const [s, a] = await Promise.all([No(r, {
    includeAbandoned: !0,
    includeCompleted: !0
  }), cf(r)]), i = {
    managerRunId: n,
    sessionId: r,
    beforeTasks: s.map((l) => hg(l)),
    beforeFingerprints: [...a],
    beforeHash: mg(pg(s, a)),
    afterHash: "",
    rollbackStatus: "pending",
    error: "",
    createdAt: ar(),
    updatedAt: ar()
  };
  return await Cr.put(i), i;
}
async function CB(e = "", t = "") {
  const n = await oM(e, t);
  return n ? (await Cr.update(n.managerRunId, {
    afterHash: await Qx(n.sessionId),
    updatedAt: ar()
  }), await Cr.get(n.managerRunId) || null) : null;
}
async function sM(e = "") {
  const t = String(e || "").trim();
  if (!t) return [];
  const n = await Cr.get(t);
  return n ? [n] : [];
}
async function aM(e = "") {
  const t = String(e || "").trim();
  if (!t) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const n = await Cr.get(t), r = await jn.get(t);
  return !n || !r ? {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  } : n.rollbackStatus === "rolled_back" || n.rollbackStatus === "skipped" ? {
    rolledBack: 0,
    conflicts: [],
    skipped: 1
  } : n.afterHash ? await Qx(n.sessionId) !== n.afterHash ? (await Cr.update(t, {
    rollbackStatus: "conflict",
    error: "rollback_conflict_current_tasks_changed",
    updatedAt: ar()
  }), await jn.update(t, {
    status: "rolled_back",
    error: wB(r.error, ["tasks"]),
    updatedAt: ar()
  }), {
    rolledBack: 0,
    conflicts: ["tasks"],
    skipped: 0
  }) : (await Ot.transaction("rw", Tn, us, Cr, jn, async () => {
    const o = await Tn.where("sessionId").equals(n.sessionId).toArray();
    o.length && await Tn.bulkDelete(o.map((s) => [s.sessionId, s.id])), n.beforeTasks.length && await Tn.bulkPut(n.beforeTasks.map((s) => hg(s))), await dm(n.sessionId, n.beforeFingerprints || []), await Cr.update(t, {
      rollbackStatus: "rolled_back",
      error: "",
      updatedAt: ar()
    }), await jn.update(t, {
      status: "rolled_back",
      updatedAt: ar()
    });
  }), {
    rolledBack: 1,
    conflicts: [],
    skipped: 0
  }) : (await Cr.update(t, {
    rollbackStatus: "skipped",
    error: "snapshot_after_hash_missing",
    updatedAt: ar()
  }), {
    rolledBack: 0,
    conflicts: [],
    skipped: 1
  });
}
async function rT(e = "", t = {}) {
  const n = Z_(t);
  if (n) return TB(e, n);
  const r = uf(t.fingerprint);
  return r && (await No(e, {
    includeAbandoned: !0,
    includeCompleted: !0
  })).find((o) => o.fingerprint === r) || null;
}
function cc(e) {
  return {
    id: e.id,
    status: e.status,
    horizon: e.horizon,
    current: e.current,
    doneWhen: e.doneWhen,
    hookForUser: e.hookForUser,
    updatedOrder: e.updatedOrder
  };
}
async function Bc(e, t, n) {
  return await Ot.transaction("rw", Tn, us, Cr, Yt, Et, async () => {
    await t.beforeWriteGuard?.(), t.managerRunId && await oM(t.managerRunId, e);
    const r = await n();
    return t.managerRunId && await CB(t.managerRunId, e), r;
  });
}
async function kB(e = "", t = "", n = {}, r = {}) {
  const o = String(e || "").trim();
  if (!o) return {
    ok: !1,
    summary: "缺少 sessionId。",
    changed: !1,
    error: "task_session_required"
  };
  const s = gB(t);
  if (s === Ji.INSPECT) {
    const d = String(n.status || "").trim(), f = d === "completed" || d === "all" || d === "", m = Z_(n), v = (await No(o, { includeCompleted: f })).filter((A) => A.status === "active" || A.status === "completed").filter((A) => !m || A.id === m).filter((A) => !d || d === "all" || A.status === d).sort((A, I) => A.status !== I.status ? A.status === "active" ? -1 : 1 : Number(I.updatedOrder) - Number(A.updatedOrder) || Number(I.updatedAt) - Number(A.updatedAt)), y = Math.max(1, Math.min(50, Math.floor(Number(n.limit) || 20))), T = Math.max(0, Math.floor(Number(n.offset) || 0)), C = v.slice(T, T + y);
    return {
      ok: !0,
      summary: `找到 ${v.length} 条事件线索，返回 ${C.length} 条。`,
      changed: !1,
      events: C.map(cc),
      warnings: v.length > C.length ? ["event_inspect_truncated"] : []
    };
  }
  if (s !== Ji.PATCH) return {
    ok: !1,
    summary: `未知事件工具：${t}`,
    changed: !1,
    error: "task_tool_unknown"
  };
  const a = bB(n.op), i = Gl(r.sourceAssistantOrder, Gl(n.order, -1));
  if (![
    "upsert-event",
    "advance-event",
    "complete-event",
    "abandon-event"
  ].includes(a)) return {
    ok: !1,
    summary: "EventPatch op 只能是 upsert-event、advance-event、complete-event、abandon-event。",
    changed: !1,
    error: "task_op_invalid"
  };
  if (a === "upsert-event") {
    const d = uf(n.fingerprint);
    if (!d) return {
      ok: !1,
      summary: "upsert-event 需要 fingerprint。",
      changed: !1,
      error: "task_fingerprint_required"
    };
    if ((await cf(o)).includes(d)) return {
      ok: !1,
      summary: "这个方向已经被放弃过，本轮不再重建。",
      changed: !1,
      error: "task_fingerprint_abandoned"
    };
    const f = await rT(o, n), m = Z_(n) || f?.id || _B("quest-event"), v = !f;
    if (r.caller === "auto" && v && i < 5) return {
      ok: !1,
      summary: "第 5 楼前不创建事件线索。",
      changed: !1,
      error: "task_floor_too_early"
    };
    const y = co(n.horizon ?? f?.horizon, 500), T = co(n.current ?? f?.current, 500), C = co(n.doneWhen ?? f?.doneWhen, 500), A = co(n.hookForUser ?? f?.hookForUser, 500), I = co(n.hookForModel ?? f?.hookForModel, 500);
    if (!y || !T || !C || !A || !I) return {
      ok: !1,
      summary: "upsert-event 需要 horizon、current、doneWhen、hookForUser、hookForModel。",
      changed: !1,
      error: "task_fields_required"
    };
    if (j_(I)) return {
      ok: !1,
      summary: "hookForModel 必须是无元叙事词的软句。",
      changed: !1,
      error: "task_hook_meta_words"
    };
    if (r.caller === "auto" && v) {
      const $ = (await No(o)).filter((p) => p.status === "active").length;
      if ($ > 1) return {
        ok: !1,
        summary: `自动新建仅允许 active 池有 1 条或以下时创建新线索，当前已有 ${$} 条。`,
        changed: !1,
        error: "task_auto_create_pool_busy"
      };
    }
    if (v && (await No(o)).filter(($) => $.status === "active").length >= 3)
      return {
        ok: !1,
        summary: "事件线索池已满（最多 3 条 active）。",
        changed: !1,
        error: "task_active_pool_full"
      };
    const R = await Bc(o, r, async () => {
      const $ = ar(), p = {
        id: m,
        sessionId: o,
        status: "active",
        horizon: y,
        current: T,
        doneWhen: C,
        hookForUser: A,
        hookForModel: I,
        fingerprint: d,
        createdOrder: v ? i : Gl(f.createdOrder, i),
        updatedOrder: i,
        lastAdvancedOrder: v ? i : Gl(f.lastAdvancedOrder, i),
        sourceManagerRunId: String(r.managerRunId || f?.sourceManagerRunId || ""),
        createdAt: v ? $ : Number(f.createdAt) || $,
        updatedAt: $
      };
      return await Tn.put(p), p;
    });
    return {
      ok: !0,
      summary: `${v ? "已创建" : "已更新"}事件线索：${R.current}`,
      changed: !0,
      eventId: R.id,
      op: a,
      events: [cc(R)]
    };
  }
  const l = await rT(o, n);
  if (!l) return {
    ok: !1,
    summary: "找不到要更新的事件线索。",
    changed: !1,
    error: "task_not_found"
  };
  if (a === "advance-event") {
    const d = co(n.current ?? l.current, 500), f = co(n.horizon ?? l.horizon, 500), m = co(n.doneWhen ?? l.doneWhen, 500), v = co(n.hookForUser ?? l.hookForUser, 500), y = co(n.hookForModel ?? l.hookForModel, 500);
    if (!f || !d || !m || !v || !y) return {
      ok: !1,
      summary: "advance-event 需要保留 horizon、current、doneWhen、hookForUser、hookForModel。",
      changed: !1,
      error: "task_fields_required"
    };
    if (j_(y)) return {
      ok: !1,
      summary: "hookForModel 必须是无元叙事词的软句。",
      changed: !1,
      error: "task_hook_meta_words"
    };
    const T = await Bc(o, r, async () => {
      const C = {
        ...l,
        status: "active",
        horizon: f,
        current: d,
        doneWhen: m,
        hookForUser: v,
        hookForModel: y,
        updatedOrder: i,
        lastAdvancedOrder: i,
        completedOrder: void 0,
        abandonedOrder: void 0,
        updatedAt: ar()
      };
      return await Tn.put(C), C;
    });
    return {
      ok: !0,
      summary: `已推进事件线索：${T.current}`,
      changed: !0,
      eventId: T.id,
      op: a,
      events: [cc(T)]
    };
  }
  if (a === "complete-event") {
    const d = await Bc(o, r, async () => {
      const f = {
        ...l,
        status: "completed",
        updatedOrder: i,
        completedOrder: i,
        updatedAt: ar()
      };
      return await Tn.put(f), f;
    });
    return {
      ok: !0,
      summary: `已完成事件线索：${d.current}`,
      changed: !0,
      eventId: d.id,
      op: a,
      events: [cc(d)]
    };
  }
  const u = await Bc(o, r, async () => {
    const d = {
      ...l,
      status: "abandoned",
      updatedOrder: i,
      abandonedOrder: i,
      updatedAt: ar()
    };
    return await Tn.put(d), await jx(o, d.fingerprint), d;
  });
  return {
    ok: !0,
    summary: `已放弃事件线索：${u.current}`,
    changed: !0,
    eventId: u.id,
    op: a,
    events: [cc(u)]
  };
}
async function IB(e = "", t = -1, n = {}) {
  const r = String(e || "").trim(), o = Gl(t, -1);
  if (!r || o < 0) return [];
  const s = Math.max(1, Math.floor(Number(n.threshold) || 8)), a = (await No(r)).filter((i) => i.status === "active" && o - Gl(i.lastAdvancedOrder, i.createdOrder) > s);
  return a.length ? await Bc(r, {
    managerRunId: n.managerRunId,
    sourceAssistantOrder: o,
    beforeWriteGuard: n.beforeWriteGuard
  }, async () => {
    const i = [];
    for (const l of a) {
      const u = {
        ...l,
        status: "abandoned",
        updatedOrder: o,
        abandonedOrder: o,
        updatedAt: ar()
      };
      await Tn.put(u), await jx(r, u.fingerprint), i.push(u);
    }
    return i;
  }) : [];
}
async function iM(e = "", t = 1) {
  const n = String(e || "").trim();
  return n ? (await No(n)).filter((r) => r.status === "active").sort((r, o) => Number(o.updatedOrder) - Number(r.updatedOrder) || Number(o.updatedAt) - Number(r.updatedAt)).map((r) => co(r.hookForModel, 500)).filter((r) => !j_(r)).filter(Boolean).slice(0, Math.max(1, Math.min(3, Math.floor(Number(t) || 1)))) : [];
}
async function xB(e = "") {
  const t = String(e || "").trim();
  if (!t) return `[Current Event Pool]
No session.`;
  const n = await No(t, { includeCompleted: !0 }), r = n.filter((s) => s.status === "active"), o = n.filter((s) => s.status === "completed").slice(0, 5);
  return [
    "[Current Event Pool]",
    `Active count: ${r.length}/3`,
    r.length ? "Active directions:" : "Active directions: none.",
    ...r.map((s) => [
      `- id: ${s.id}`,
      `  current: ${s.current}`,
      `  horizon: ${s.horizon}`,
      `  done when: ${s.doneWhen}`,
      `  user hook: ${s.hookForUser}`,
      `  fingerprint: ${s.fingerprint}`,
      `  last advanced floor: ${s.lastAdvancedOrder}`
    ].join(`
`)),
    o.length ? "Recently completed:" : "",
    ...o.map((s) => `- id: ${s.id}; current: ${s.current}; done when: ${s.doneWhen}; completed floor: ${s.completedOrder ?? s.updatedOrder}; fingerprint: ${s.fingerprint}`),
    "Abandoned directions are hidden; EventPatch will reject abandoned fingerprints."
  ].filter(Boolean).join(`
`);
}
function MB() {
  return [{
    type: "function",
    function: {
      name: Ji.INSPECT,
      description: [
        "Inspect the current RP session event direction pool.",
        "Returns active and recently completed event directions only. Abandoned directions and hidden fingerprints are not shown.",
        "Use before EventPatch when you need to advance, complete, or decide whether the active pool is low.",
        "This is not memory and not a map."
      ].join(`
`),
      parameters: {
        type: "object",
        properties: {
          status: {
            type: "string",
            enum: [
              "active",
              "completed",
              "all"
            ],
            description: "Optional status filter. Default returns active plus recently completed."
          },
          eventId: {
            type: "string",
            description: "Optional event direction id filter."
          },
          id: {
            type: "string",
            description: "Alias for eventId."
          },
          offset: {
            type: "number",
            description: "Pagination offset. Default 0."
          },
          limit: {
            type: "number",
            description: "Maximum directions to return. Default 20, max 50."
          }
        },
        additionalProperties: !1
      }
    }
  }, {
    type: "function",
    function: {
      name: Ji.PATCH,
      description: [
        "Maintain the current RP session event direction engine. This is not memory, not a map, and not a random encounter.",
        "Use this only for forward-looking directions that could give the user something fresh to play when the story needs a hook. Do not use it to surface existing foreshadowing.",
        "Use only established people, places, relationships, world facts, and current tone.",
        "Create fresh possible directions only after the story has enough material. Recombine established material into an unplayed person, place, faction, or situation that opens a new interaction space.",
        "Reach new directions by extending a known character relationship, adjacent place, faction branch, social obligation, secret pressure, or user taste.",
        "Use the current tone and the user's demonstrated tastes as the engine for boldness. Do not create generic hooks, obvious continuations, repeated memory, existing foreshadowing, or outside random events.",
        "If no good hook exists, do not call this tool.",
        "Do not record old events, close existing memory, or force surprises. Advance or complete only when the completed assistant reply actually moved or resolved that direction.",
        "`horizon` is the larger not-yet-happened pull. `current` is the immediate playable entrance the user can act on now. `doneWhen` is the objective completion condition: a concrete observable event that happens in the story, not an abstract state.",
        "`hookForUser` is direct UI text. `hookForModel` is a soft in-world sentence for RP injection, without meta planning language such as quest, goal, objective, completed, or Chinese equivalents.",
        "Allowed ops: upsert-event, advance-event, complete-event, abandon-event."
      ].join(`
`),
      parameters: {
        type: "object",
        properties: {
          op: {
            type: "string",
            enum: [
              "upsert-event",
              "advance-event",
              "complete-event",
              "abandon-event"
            ]
          },
          eventId: {
            type: "string",
            description: "Existing event direction id. Optional for upsert; required for advance/complete/abandon unless fingerprint uniquely matches."
          },
          id: {
            type: "string",
            description: "Alias for eventId."
          },
          fingerprint: {
            type: "string",
            description: "Stable short fingerprint of this narrative direction. Required for upsert."
          },
          horizon: {
            type: "string",
            description: "User-facing larger not-yet-happened direction."
          },
          current: {
            type: "string",
            description: "User-facing immediate playable entrance."
          },
          doneWhen: {
            type: "string",
            description: "Objective completion condition, written as a concrete observable event in the story, not an abstract state."
          },
          hookForUser: {
            type: "string",
            description: "Plain UI explanation."
          },
          hookForModel: {
            type: "string",
            description: "Soft in-world prompt sentence for RP injection; no meta planning language."
          }
        },
        required: ["op"],
        additionalProperties: !1
      }
    }
  }];
}
var ba = {
  LIST: "MemoryList",
  READ: "MemoryRead",
  WRITE: "MemoryWrite",
  EDIT: "MemoryEdit",
  GREP: "MemoryGrep",
  CHAT_HISTORY: "ChatHistory"
}, Zn = {
  LS: "LS",
  GREP: "Grep",
  READ: "Read",
  WRITE: "Write",
  EDIT: "Edit"
}, Cb = 1200, Ui = 2e3, PB = 100, e1 = 100;
function za() {
  return Date.now();
}
function oT(e = "") {
  if (e === "memory/state.md") return "会话记忆";
  const t = Yi(e);
  return t || String(e || "").replace(/^memory\//, "").replace(/\.md$/i, "") || "记忆档案";
}
function zs(e = "", t = 400) {
  const n = String(e || "").replace(/\s+/g, " ").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function no(e = "", t) {
  const n = String(e || "").replace(/\r\n/g, `
`).trim();
  return typeof t == "number" && t > 0 && n.length > t ? n.slice(0, t) : n;
}
function fm(e = "") {
  return String(e ?? "").replace(/\r\n?/g, `
`).split(`
`);
}
function Ws(e = [], t = 1) {
  return e.map((n, r) => `${t + r}: ${n}`).join(`
`);
}
function gg(e, t = 1) {
  const n = Math.floor(Number(e));
  return Number.isFinite(n) && n > 0 ? n : t;
}
function hm(e, t = 0) {
  const n = Math.floor(Number(e));
  return Number.isFinite(n) && n >= 0 ? n : t;
}
function pm(e, t, n) {
  const r = Math.floor(Number(e));
  return !Number.isFinite(r) || r <= 0 ? t : Math.min(n, r);
}
function RB(e = "") {
  const t = String(e || "content").trim().replace(/[\s-]/g, "_").replace(/[A-Z]/g, (n) => `_${n.toLowerCase()}`).replace(/_+/g, "_").replace(/^_+|_+$/g, "");
  return t === "files_with_matches" || t === "fileswithmatches" ? "files_with_matches" : t === "count" ? "count" : "content";
}
function Ei(e = "") {
  return String(e || "").replace(/\\/g, "/").trim() === "memory/state.md";
}
function lM(e = "") {
  const t = String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").trim();
  if (!t) throw new Error("memory_character_name_required");
  if (t === "." || t === "..") throw new Error("memory_character_name_invalid");
  if (/[\x00-\x1F\x7F/\\:*?"<>|]/u.test(t)) throw new Error("memory_character_name_invalid");
  return t;
}
function uM(e = "") {
  const t = String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").toLowerCase().replace(/[\s_-]+/g, "").trim();
  return (/* @__PURE__ */ new Set([
    "user",
    "users",
    "player",
    "players",
    "pc",
    "you",
    "me",
    "myself",
    "self",
    "host",
    "dm",
    "gm",
    "narrator",
    "operator",
    "protagonist",
    "用户",
    "玩家",
    "主控",
    "我",
    "你",
    "自己",
    "本人",
    "我方",
    "主人翁",
    "叙述者",
    "主持人"
  ])).has(t);
}
function NB(e = "") {
  return `memory/characters/${lM(e)}.md`;
}
function Yi(e = "") {
  const t = String(e || "").replace(/\\/g, "/").trim();
  if (!t.startsWith("memory/characters/") || !t.endsWith(".md")) return "";
  const n = t.slice(18, -3);
  if (!n || n.includes("/")) return "";
  try {
    return lM(n);
  } catch {
    return "";
  }
}
function mm(e = "") {
  return !!Yi(e);
}
function Fo(e = "") {
  const t = String(e || "").replace(/\\/g, "/").trim();
  if (!t) throw new Error("memory_path_required");
  if (Ei(t)) return "memory/state.md";
  const n = Yi(t);
  if (n) return NB(n);
  throw t.startsWith("memory/") ? t.endsWith(".md") ? new Error("memory_path_invalid") : new Error("memory_path_must_be_md") : new Error("memory_path_scope_required");
}
function cM(e) {
  return JSON.parse(JSON.stringify(e));
}
function kb(e = "") {
  return [
    "# 会话记忆",
    "",
    "## 剧情脉络",
    "",
    "### 当前主线",
    "- 未开始。",
    "",
    "### 长期压力",
    "- 暂无。",
    "",
    "### 未解伏笔",
    "- 暂无。",
    "",
    "### 关系态势",
    "- 只保留影响主线或当前局面的关系摘要；人物细节放入对应人物记忆。",
    "",
    "## 当前状态",
    "- 时间：未明确",
    "- 地点：未明确",
    `- 在场人物：${zs(e, 120) || "当前角色"}`,
    "- 关键物品：暂无",
    "- 身体/情绪/约束状态：暂无",
    "## 近期连续事件",
    "- 暂无。"
  ].join(`
`);
}
function $B(e = "", t = "") {
  const n = za();
  return [{
    sessionId: e,
    status: "active",
    createdAt: n,
    updatedAt: n,
    source: "default",
    path: "memory/state.md",
    content: kb(t)
  }];
}
async function Ib(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) throw new Error("memory_session_required");
  const r = await hn.where("sessionId").equals(n).toArray();
  if (r.length) return r.sort((s, a) => s.path.localeCompare(a.path));
  const o = $B(n, t.characterName);
  return await hn.bulkPut(o), await Et.update(n, { updatedAt: za() }), o;
}
async function hs(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await hn.where("sessionId").equals(n).sortBy("path");
  return t.includeStale ? r : r.filter((o) => o.status !== "stale");
}
function DB(e) {
  return no(e.content, 1200);
}
function LB(e) {
  return {
    path: e.path,
    status: e.status,
    createdAt: e.createdAt,
    updatedAt: e.updatedAt,
    source: e.source,
    staleFromOrder: e.staleFromOrder,
    contentLength: String(e.content || "").length,
    preview: DB(e)
  };
}
function OB(e) {
  return {
    ...LB(e),
    title: oT(e.path),
    searchText: no([
      e.path,
      oT(e.path),
      e.content || ""
    ].filter(Boolean).join(`
`), 2e4)
  };
}
async function Ma(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = Fo(t);
  return await hn.get([n, r]) || null;
}
async function FB(e = "", t) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.floor(n) : await hb(e) ?? -1;
}
function BB(e = [], t = "") {
  const n = e.filter((r) => r.status !== "stale").sort((r, o) => r.path.localeCompare(o.path));
  return n.length === 1 && n[0]?.path === "memory/state.md" && n[0]?.content === kb(t);
}
function dM(e) {
  const t = String(e || "");
  let n = 2166136261, r = 522970236;
  for (let o = 0; o < t.length; o += 1) {
    const s = t.charCodeAt(o);
    n ^= s, n = Math.imul(n, 16777619) >>> 0, r ^= s + o, r = Math.imul(r, 16777619) >>> 0;
  }
  return `${t.length.toString(36)}:${n.toString(16)}${r.toString(16)}`;
}
function UB(e) {
  return [
    Fo(e.path),
    String(e.status || ""),
    String(e.source || ""),
    Number.isFinite(Number(e.staleFromOrder)) ? String(Number(e.staleFromOrder)) : "",
    String(e.content || "").length,
    dM(e.content || "")
  ].join("");
}
function fM(e = []) {
  const t = [...e].sort((n, r) => Fo(n.path).localeCompare(Fo(r.path))).map(UB).join("");
  return `${e.length}:${dM(t)}`;
}
function qB(e) {
  return fM((e?.files || []).map((t) => t.file));
}
async function HB(e = "", t) {
  const n = String(e || "").trim();
  return n ? await Ot.transaction("rw", hn, is, Yt, Et, async () => {
    const r = await hs(n, { includeStale: !0 });
    if (!r.length) return null;
    const o = await FB(n, t);
    if (o === -1 && BB(r, (await Et.get(n))?.characterName || ""))
      return null;
    const s = fM(r), a = await pM(n);
    if (a && qB(a) === s) return null;
    const i = {
      sessionId: n,
      floor: o,
      files: r.sort((l, u) => l.path.localeCompare(u.path)).map((l) => ({
        path: l.path,
        file: cM(l)
      })),
      createdAt: za()
    };
    return await is.put(i), i;
  }) : null;
}
async function hM(e = "") {
  const t = String(e || "").trim();
  return t ? (await is.where("sessionId").equals(t).sortBy("floor")).sort((n, r) => n.floor - r.floor || n.createdAt - r.createdAt) : [];
}
async function pM(e = "", t = Number.POSITIVE_INFINITY) {
  const n = String(e || "").trim(), r = Number(t);
  return n && (await hM(n)).filter((o) => Number(o.floor) <= r || r === Number.POSITIVE_INFINITY).sort((o, s) => Number(s.floor) - Number(o.floor) || Number(s.createdAt) - Number(o.createdAt))[0] || null;
}
async function mM(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  return !n || !Number.isFinite(r) ? 0 : await Ot.transaction("rw", is, async () => {
    const o = (await hM(n)).filter((s) => Number(s.floor) >= r);
    return o.length ? (await is.bulkDelete(o.map((s) => [s.sessionId, s.floor])), o.length) : 0;
  });
}
async function gM(e = "", t = -1) {
  const n = String(e || "").trim();
  if (!n) throw new Error("memory_session_required");
  return await Ot.transaction("rw", hn, is, eo, Et, async () => {
    const r = await pM(n, t);
    if (r?.files) {
      const u = za(), d = await hs(n, { includeStale: !0 }), f = r.files, m = new Set(f.map((T) => Fo(T.path))), v = d.filter((T) => !m.has(T.path)).map((T) => [T.sessionId, T.path]), y = f.map((T) => ({
        ...cM(T.file),
        sessionId: n,
        path: Fo(T.path),
        updatedAt: u
      }));
      return v.length && await hn.bulkDelete(v), y.length && await hn.bulkPut(y), await Et.update(n, { updatedAt: u }), await n1(n), y;
    }
    const o = za(), s = await Et.get(n), a = (await hs(n, { includeStale: !0 })).filter((u) => u.path !== "memory/state.md").map((u) => [u.sessionId, u.path]);
    a.length && await hn.bulkDelete(a);
    const i = await hn.get([n, "memory/state.md"]), l = {
      sessionId: n,
      path: "memory/state.md",
      content: kb(s?.characterName || ""),
      status: "active",
      source: "default",
      createdAt: Number(i?.createdAt) || o,
      updatedAt: o
    };
    return await hn.put(l), await Et.update(n, { updatedAt: o }), await n1(n), [l];
  });
}
async function t1(e = "", t = "", n = "", r = {}) {
  const o = String(e || "").trim();
  if (!o) throw new Error("memory_session_required");
  const s = Fo(t), a = za(), i = await hn.get([o, s]), l = {
    sessionId: o,
    path: s,
    content: String(n || "").replace(/\r\n/g, `
`),
    status: "active",
    source: String(r.source || i?.source || "manager"),
    staleFromOrder: Number.isFinite(Number(r.staleFromOrder)) ? Number(r.staleFromOrder) : i?.staleFromOrder,
    createdAt: Number(i?.createdAt) || a,
    updatedAt: a
  };
  return await hn.put(l), await Et.update(o, { updatedAt: a }), await n1(o), l;
}
async function n1(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) throw new Error("memory_session_required");
  const r = za(), o = await eo.get([n, "markdown-derived"]), s = {
    sessionId: n,
    kind: "markdown-derived",
    status: "stale",
    error: String(t || ""),
    sourceFingerprint: "",
    derivedAt: r,
    updatedAt: r,
    files: Array.isArray(o?.files) ? o.files : []
  };
  return await eo.put(s), s;
}
async function vM(e = "", t = "markdown-derived") {
  const n = String(e || "").trim();
  return n && await eo.get([n, t]) || null;
}
function zB(e) {
  const t = e.map((r) => `${r.path}${r.status}${r.updatedAt}${r.content.length}`).join("");
  let n = 2166136261;
  for (let r = 0; r < t.length; r += 1)
    n ^= t.charCodeAt(r), n = Math.imul(n, 16777619) >>> 0;
  return `${e.length}:${n.toString(16)}`;
}
async function Xi(e = "") {
  const t = String(e || "").trim();
  if (!t) throw new Error("memory_session_required");
  const n = za();
  try {
    const r = await hs(t, { includeStale: !0 }), o = {
      sessionId: t,
      kind: "markdown-derived",
      status: "ready",
      error: "",
      sourceFingerprint: zB(r),
      derivedAt: n,
      updatedAt: n,
      files: r.map(OB)
    };
    return await eo.put(o), o;
  } catch (r) {
    const o = {
      sessionId: t,
      kind: "markdown-derived",
      status: "failed",
      error: r instanceof Error ? r.message : String(r || "memory_index_failed"),
      sourceFingerprint: "",
      derivedAt: n,
      updatedAt: n,
      files: []
    };
    return await eo.put(o), o;
  }
}
function WB(e) {
  return Fo(e.filePath || e.path || "");
}
function Hd(e = "") {
  return String(e || "").replace(/\\/g, "/").replace(/^\/+/, "").trim();
}
function yM(e = "memory/") {
  const t = Hd(e || "");
  if (!t) throw new Error("source_path_required");
  const n = t.endsWith("/") ? t : `${t}/`;
  if (n.includes("..")) throw new Error("source_path_invalid");
  if (![
    "chat/",
    "worldbooks/",
    "memory/"
  ].some((r) => n === r || n.startsWith(r))) throw new Error("source_path_scope_required");
  return n;
}
function ph(e = "", t = "item") {
  return String(e || "").trim().replace(/[\\/:*?"<>|#%{}[\]^~`]+/g, "_").replace(/\s+/g, "_").replace(/^_+|_+$/g, "") || t;
}
function fy(e = "") {
  const t = Hd(e);
  if (!t) throw new Error("source_path_required");
  if (t.endsWith("/")) throw new Error("source_file_path_required");
  if (t.includes("..")) throw new Error("source_path_invalid");
  if (t.startsWith("memory/")) return Fo(t);
  if (t === "chat/transcript.md" || /^chat\/messages\/\d+\.md$/.test(t) || /^worldbooks\/(?:character|global|chat|entries)\//.test(t) && t.endsWith(".md")) return t;
  throw new Error("source_file_path_invalid");
}
function sT(e = "") {
  return e === "memory/state.md" || mm(e);
}
function aT(e = "") {
  const t = Fo(e), n = Yi(t);
  if (n && uM(n)) throw new Error("memory_character_user_reserved");
  return t;
}
function GB(e = [], t = "memory/") {
  const n = yM(t), r = /* @__PURE__ */ new Map();
  return [
    "chat/",
    "chat/messages/",
    "worldbooks/",
    "worldbooks/character/",
    "worldbooks/global/",
    "worldbooks/chat/",
    "worldbooks/entries/",
    "memory/",
    "memory/characters/"
  ].forEach((o) => {
    if (!o.startsWith(n) || o === n) return;
    const [s] = o.slice(n.length).split("/");
    if (!s) return;
    const a = `${n}${s}/`;
    r.set(a, {
      name: s,
      path: a,
      type: "directory",
      readonly: !a.startsWith("memory/")
    });
  }), e.forEach((o) => {
    if (!o.startsWith(n) || o === n) return;
    const s = o.slice(n.length), [a] = s.split("/");
    if (!a) return;
    const i = s.includes("/"), l = `${n}${a}${i ? "/" : ""}`;
    r.set(l, {
      name: a,
      path: l,
      type: i ? "directory" : "file",
      readonly: !l.startsWith("memory/")
    });
  }), [...r.values()].sort((o, s) => o.type !== s.type ? o.type === "directory" ? -1 : 1 : o.path.localeCompare(s.path, "zh-CN"));
}
function iT(e, t, n = 0) {
  const r = zs(t.comment || t.title || t.name || `entry-${n + 1}`, 200), o = Array.isArray(t.key) ? t.key.join(", ") : String(t.key || ""), s = Array.isArray(t.keysecondary) ? t.keysecondary.join(", ") : Array.isArray(t.secondary_keys) ? t.secondary_keys.join(", ") : String(t.keysecondary || t.secondary_keys || "");
  return [
    `# ${r}`,
    "",
    `Book: ${zs(e.name, 200)}`,
    `Source: ${zs(t.worldSourceType || e.worldSourceType || "worldbook", 80)}`,
    `UID: ${zs(t.uid ?? t.id ?? n, 120)}`,
    o ? `Keys: ${o}` : "",
    s ? `Secondary keys: ${s}` : "",
    Number.isFinite(Number(t.order)) ? `Order: ${Number(t.order)}` : "",
    t.position !== void 0 ? `Position: ${String(t.position)}` : "",
    "",
    "## Content",
    no(t.content || "")
  ].filter((a) => a !== "").join(`
`);
}
function xb(e = {}) {
  const t = Array.isArray(e.worldBooks) ? e.worldBooks : [], n = [];
  return t.forEach((r, o) => {
    const s = ph(r.name, `book-${o + 1}`), a = String(r.worldSourceType || "entries").trim(), i = a === "character" || a === "global" || a === "chat" ? a : "entries";
    (Array.isArray(r.entries) ? r.entries : []).forEach((l, u) => {
      if (!no(l.content || "")) return;
      const d = ph(l.uid ?? l.id ?? l.comment ?? u, `entry-${u + 1}`);
      n.push({
        path: `worldbooks/${i}/${s}/${d}.md`,
        content: iT(r, l, u),
        readonly: !0
      });
    });
  }), !n.length && Array.isArray(e.worldEntries) && e.worldEntries.forEach((r, o) => {
    if (!no(r.content || "")) return;
    const s = ph(r.sourceWorldBook || "worldEntries", "worldEntries"), a = ph(r.uid ?? r.id ?? r.comment ?? o, `entry-${o + 1}`);
    n.push({
      path: `worldbooks/entries/${s}/${a}.md`,
      content: iT({
        name: s,
        entries: [r]
      }, r, o),
      readonly: !0
    });
  }), n.sort((r, o) => r.path.localeCompare(o.path, "zh-CN"));
}
async function VB(e = "", t = {}) {
  const n = await hs(e, { includeStale: !0 });
  return [
    "chat/transcript.md",
    ...(await pb(e, 0)).map((r) => `chat/messages/${r}.md`),
    ...xb(t).map((r) => r.path),
    ...n.map((r) => r.path)
  ].sort((r, o) => r.localeCompare(o, "zh-CN"));
}
function KB(e = "") {
  const t = String(e || "content").trim().replace(/[\s-]/g, "_").replace(/[A-Z]/g, (n) => `_${n.toLowerCase()}`).replace(/_+/g, "_").replace(/^_+|_+$/g, "");
  return t === "files_with_matches" || t === "fileswithmatches" ? "files_with_matches" : t === "count" ? "count" : "content";
}
function JB(e = "", t = !1) {
  const n = String(e || "");
  if (!n) throw new Error("grep_pattern_required");
  return t === !0 ? new RegExp(n, "iu") : new RegExp(n.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "iu");
}
function dc(e = "", t = "", n = "") {
  const r = Hd(t || "");
  if (r) {
    if (r.endsWith("/")) {
      if (!e.startsWith(r)) return !1;
    } else if (e !== r) return !1;
  }
  const o = String(n || "").trim();
  if (!o) return !0;
  if (!/[/*?[\]{}]/.test(o)) return e === o || e.endsWith(`/${o.replace(/^\/+/, "")}`);
  const s = o.replace(/\*\*/g, "\0DOUBLE_STAR\0").replace(/[|\\{}()[\]^$+?.]/g, "\\$&").replace(/\0DOUBLE_STAR\0/g, ".*").replace(/\*/g, "[^/]*").replace(/\?/g, "[^/]");
  return new RegExp(`^${s.startsWith("chat/") || s.startsWith("worldbooks/") || s.startsWith("memory/") ? s : `(?:chat|worldbooks|memory)/${s}`}$`).test(e);
}
function hy(e, t, n, r, o = 0) {
  const s = fm(t.content);
  let a = 0;
  s.forEach((i, l) => {
    if (n.lastIndex = 0, !!n.test(i) && (a += 1, r === "content")) {
      const u = Math.max(0, l - o), d = Math.min(s.length, l + o + 1);
      e.push({
        path: t.path,
        lineNumber: l + 1,
        line: i,
        context: Ws(s.slice(u, d), u + 1)
      });
    }
  }), r === "files_with_matches" && a > 0 ? e.push({ path: t.path }) : r === "count" && a > 0 && e.push({
    path: t.path,
    count: a
  });
}
function YB() {
  return [
    {
      type: "function",
      function: {
        name: Zn.LS,
        description: [
          "List first-level files and directories in Tavern text sources.",
          "Returns directory entries only. Does not recurse and does not read file contents.",
          "Use before reading or editing when you need to locate chat evidence, worldbook entries, or memory files.",
          "Directories are `chat/`, `worldbooks/`, and `memory/`. `chat/` and `worldbooks/` are read-only; `memory/` is writable when memory archiving is authorized."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            path: {
              type: "string",
              description: "Directory path, for example `chat/`, `worldbooks/`, `memory/`, or `memory/characters/`."
            },
            offset: {
              type: "number",
              description: "1-based directory entry offset. Default 1."
            },
            limit: {
              type: "number",
              description: "Maximum entries to return. Default 100, max 300."
            }
          },
          required: ["path"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Zn.GREP,
        description: [
          "Search text inside Tavern text sources.",
          "Uses literal text search by default and returns matching files plus line-level snippets.",
          "Use before reading many files to locate facts, names, dialogue, places, worldbook lore, memory notes, or source evidence.",
          "`path` can be a directory like `chat/`, `worldbooks/`, `memory/characters/`, or one exact file. `include` limits the file glob/name. For regex search, explicitly pass `useRegex: true`."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            pattern: {
              type: "string",
              description: "Search pattern. Treated as literal text by default; use `useRegex: true` only when intentionally using regex."
            },
            path: {
              type: "string",
              description: "Optional search scope, directory or exact file."
            },
            include: {
              type: "string",
              description: "Optional file glob/name filter, for example `*.md`, `memory/characters/*.md`, or `12.md`."
            },
            outputMode: {
              type: "string",
              enum: [
                "content",
                "files_with_matches",
                "count"
              ],
              description: "`content` returns matched lines, `files_with_matches` returns files only, and `count` returns match counts. Default content."
            },
            limit: {
              type: "number",
              description: "Maximum results to return. Default 100, max 100."
            },
            offset: {
              type: "number",
              description: "Skip this many results before returning matches. Default 0."
            },
            contextLines: {
              type: "number",
              description: "Context lines before and after each match. Default 0, max 5."
            },
            useRegex: {
              type: "boolean",
              description: "Whether to treat pattern as a regex. Default false; omit it for literal text search."
            }
          },
          required: ["pattern"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Zn.READ,
        description: [
          "Read a Tavern text source file, or list a directory.",
          "For files, returns line-numbered content. For directories, returns directory entries. Large files include continuation hints.",
          "Use `tail` by itself when you need the end of a file.",
          "The argument name is `filePath`, not `path`. File paths look like `chat/messages/12.md`, `worldbooks/character/book/uid.md`, or `memory/state.md`."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "File or directory path. Do not pass path."
            },
            offset: {
              type: "number",
              description: "1-based line or directory-entry offset. Default 1."
            },
            limit: {
              type: "number",
              description: "Maximum lines or entries. Default 1200, max 2000."
            },
            tail: {
              type: "number",
              description: "Return the final N lines of a file. Use by itself; do not combine with offset/limit."
            }
          },
          required: ["filePath"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Zn.WRITE,
        description: [
          "Write a complete Tavern memory Markdown file.",
          "Only `memory/state.md` and `memory/characters/<角色名>.md` are writable. `chat/` and `worldbooks/` are read-only evidence sources.",
          "Use for creating files, complete rewrites, or rewrites where most content is new.",
          "Read the target file first. Write overwrites the entire file, so include all original content you want to keep.",
          "The argument names are `filePath` and `content`."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "Writable memory file path: `memory/state.md` or `memory/characters/<角色名>.md`."
            },
            content: {
              type: "string",
              description: "Full file content to write."
            }
          },
          required: ["filePath", "content"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Zn.EDIT,
        description: [
          "Edit one existing Tavern memory Markdown file by replacing original text fragments, replacing inclusive line ranges, inserting text at line positions, or removing text with empty replacements.",
          "One call edits one file. Only `memory/state.md` and `memory/characters/<角色名>.md` are writable.",
          "Use oldString/newString for local revisions. Use startLine/endLine/newString for contiguous passage replacement where copying oldString would be fragile. Use insertAtLine/newString to add text before a line or at the end. Use Write for complete rewrites.",
          "Read the target file first unless the exact current text is already available in the conversation or a recent tool result. Line-range and insertion edits must use line numbers from the latest Read result.",
          "Put multiple edits in the edits array. Line-range and insertion items may share one call when they use line numbers from the same Read result. Keep oldString edits separate from line-number edits unless the whole change can be expressed with line numbers.",
          'The `edits` argument must be a non-empty JSON array, not a JSON-stringified string. Correct: `"edits":[{"startLine":10,"endLine":50,"newString":"..."}]`.',
          "Each edit item should choose exactly one mode. If stray optional fields appear, Edit normalizes by priority: complete startLine/endLine wins, then insertAtLine, then oldString.",
          "Do not issue multiple Edit tool calls for the same file in one assistant turn. Combine same-file changes into one call.",
          "",
          "## Matching Rules",
          "oldString must be an exact fragment present in the file, including spaces and newlines. To remove a matched fragment, set newString to an empty string.",
          "Common punctuation equivalence is supported, such as straight/curly quotes and ASCII/full-width punctuation. Replacements preserve the file punctuation style when possible.",
          "For long oldString fragments, Edit can tolerate whitespace-only differences such as indentation, line wrapping, or extra/missing blank lines.",
          "Each oldString must be unique by default. Multiple matches return line numbers and context.",
          "Line-range edits use 1-based inclusive startLine/endLine from Read output. Insertion uses 1-based insertAtLine; use totalLines + 1 to append.",
          "Line-range and insertion edits are applied by original Read line numbers from bottom to top automatically; do not adjust later line numbers yourself."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "Writable memory file path: `memory/state.md` or `memory/characters/<角色名>.md`."
            },
            edits: {
              type: "array",
              description: "Real, non-empty JSON array, not a quoted JSON string. Each item should use exactly one mode: oldString/newString, startLine/endLine/newString, or insertAtLine/newString. Stray optional fields are ignored by mode priority.",
              items: {
                type: "object",
                properties: {
                  oldString: {
                    type: "string",
                    description: "Original text fragment to replace. Must match exactly, with punctuation/long-whitespace tolerance."
                  },
                  startLine: {
                    type: "number",
                    description: "1-based inclusive start line from the latest Read result."
                  },
                  endLine: {
                    type: "number",
                    description: "1-based inclusive end line from the latest Read result."
                  },
                  insertAtLine: {
                    type: "number",
                    description: "1-based insertion point from the latest Read result. Use totalLines + 1 to append."
                  },
                  newString: {
                    type: "string",
                    description: "Replacement, inserted text, or an empty string to delete the matched fragment or line range."
                  },
                  replaceAll: {
                    type: "boolean",
                    description: "Whether to replace all matches. Default false."
                  }
                },
                required: ["newString"],
                additionalProperties: !1
              }
            }
          },
          required: ["filePath", "edits"],
          additionalProperties: !1
        }
      }
    }
  ];
}
async function XB(e = "", t = {}) {
  const n = Math.floor(Number(t.tail) || 0), r = gg(t.offset, 1), o = pm(t.limit, Cb, Ui), s = [];
  let a = 0;
  if (n > 0) {
    let d = 0;
    for (; s.length < Math.min(Ui, n); ) {
      const v = await dg(e, 100, d);
      if (a = v.total, !v.messages.length) break;
      const y = v.messages.flatMap((T) => [
        `## order ${T.order} ${T.role}`,
        no(T.content || ""),
        ""
      ]);
      if (s.unshift(...y), d += v.messages.length, d >= v.total) break;
    }
    const f = s.slice(Math.max(0, s.length - Math.min(Ui, n))), m = Math.max(1, s.length - f.length + 1);
    return {
      ok: !0,
      path: "chat/transcript.md",
      content: Ws(f, m),
      numberedContent: Ws(f, m),
      lineStart: m,
      lineEnd: m + f.length - 1,
      totalLines: s.length,
      truncated: s.length > f.length,
      nextOffset: 0,
      summary: `读取 chat/transcript.md 末尾 ${f.length} 行（${a} 条消息）。`
    };
  }
  let i = 0, l = 0;
  for (; s.length < o; ) {
    const d = await nu(e, 0, Number.POSITIVE_INFINITY, 200, i);
    if (a = d.total, !d.messages.length) break;
    for (const f of d.messages) {
      const m = [
        `## order ${f.order} ${f.role}`,
        no(f.content || ""),
        ""
      ];
      if (l + m.length < r) {
        l += m.length;
        continue;
      }
      const v = Math.max(0, r - l - 1);
      if (s.push(...m.slice(v)), l += m.length, s.length >= o) break;
    }
    if (i += d.messages.length, i >= d.total) break;
  }
  const u = s.slice(0, o);
  return {
    ok: !0,
    path: "chat/transcript.md",
    content: Ws(u, r),
    numberedContent: Ws(u, r),
    lineStart: r,
    lineEnd: r + u.length - 1,
    totalLines: r + u.length - 1,
    truncated: u.length >= o,
    nextOffset: u.length >= o ? r + u.length : 0,
    summary: `读取 chat/transcript.md 第 ${r}-${r + u.length - 1} 行（${a} 条消息）。`
  };
}
async function QB(e = "", t = "", n = {}, r = {}) {
  if (t === "chat/transcript.md") return XB(e, n);
  const o = t.match(/^chat\/messages\/(\d+)\.md$/);
  if (o) {
    const a = await _u(e, Number(o[1]));
    return a ? py(t, [
      `# Message ${a.order}`,
      "",
      `Role: ${a.role}`,
      a.name ? `Name: ${a.name}` : "",
      a.error ? "Error: true" : "",
      "",
      "## Content",
      no(a.content || ""),
      ...Array.isArray(a.thoughts) && a.thoughts.length ? [
        "",
        "## Thoughts",
        ...a.thoughts.map((i, l) => `### ${zs(i?.label || `思考 ${l + 1}`, 120)}
${no(i?.text || "")}`)
      ] : []
    ].filter((i) => i !== "").join(`
`), n) : {
      ok: !1,
      path: t,
      summary: `${t} 不存在。`,
      error: "source_file_not_found"
    };
  }
  const s = xb(r).find((a) => a.path === t);
  if (s) return py(t, s.content, n);
  if (t.startsWith("memory/")) {
    const a = await Ma(e, t);
    return a ? py(t, a.content, n) : {
      ok: !1,
      summary: `${t} 不存在。`,
      path: t,
      error: "memory_file_not_found"
    };
  }
  return {
    ok: !1,
    path: t,
    summary: `${t} 不存在。`,
    error: "source_file_not_found"
  };
}
function py(e = "", t = "", n = {}) {
  const r = fm(t), o = Math.floor(Number(n.tail) || 0);
  let s = gg(n.offset, 1), a = pm(n.limit, Cb, Ui);
  o > 0 && (a = Math.min(Ui, o), s = Math.max(1, r.length - a + 1));
  const i = Math.max(0, s - 1), l = r.slice(i, i + a), u = i + a < r.length ? i + a + 1 : 0;
  return {
    ok: !0,
    path: e,
    lineStart: i + 1,
    lineEnd: i + l.length,
    totalLines: r.length,
    content: Ws(l, i + 1),
    numberedContent: Ws(l, i + 1),
    truncated: u > 0,
    nextOffset: u,
    summary: `读取 ${e} 第 ${i + 1}-${i + l.length} 行，共 ${r.length} 行。`
  };
}
async function _M(e = "", t = "", n = {}, r = {}) {
  const o = String(e || "").trim();
  if (!o) return {
    ok: !1,
    summary: "缺少 sessionId。",
    error: "source_session_required"
  };
  const s = r.contextSnapshot || {};
  try {
    if (t === Zn.LS) {
      const a = yM(n.path || "memory/"), i = GB(await VB(o, s), a), l = gg(n.offset, 1), u = Math.min(300, Math.max(1, Math.floor(Number(n.limit) || 100))), d = i.slice(l - 1, l - 1 + u);
      return {
        ok: !0,
        path: a,
        entries: d,
        count: i.length,
        truncated: l - 1 + u < i.length,
        nextOffset: l - 1 + u < i.length ? l + u : 0,
        summary: `${a} 下有 ${i.length} 项，返回 ${d.length} 项。`
      };
    }
    if (t === Zn.READ) {
      const a = Hd(n.filePath || "");
      if (!a) throw new Error("source_path_required");
      return a.endsWith("/") ? _M(o, Zn.LS, {
        path: a,
        offset: n.offset,
        limit: n.limit
      }, r) : QB(o, fy(a), n, s);
    }
    if (t === Zn.GREP) {
      const a = String(n.pattern ?? n.query ?? "").trim();
      if (!a) return {
        ok: !1,
        summary: "缺少搜索词。",
        error: "grep_pattern_required"
      };
      const i = JB(a, n.useRegex === !0), l = KB(n.outputMode), u = Math.min(e1, Math.max(1, Math.floor(Number(n.limit) || e1))), d = hm(n.offset, 0), f = Math.min(5, hm(n.contextLines, 0)), m = Hd(n.path || n.scope || ""), v = String(n.include || "").trim(), y = [];
      let T = 0;
      for (const A of xb(s))
        dc(A.path, m, v) && (T += 1, hy(y, A, i, l, f));
      if ((await hs(o, { includeStale: !0 })).forEach((A) => {
        dc(A.path, m, v) && (T += 1, hy(y, {
          path: A.path,
          content: A.content
        }, i, l, f));
      }), !m || dc("chat/transcript.md", m, v) || m.startsWith("chat/")) {
        let I = 0;
        for (; ; ) {
          const R = await nu(o, 0, Number.POSITIVE_INFINITY, 500, I);
          if (!R.messages.length || (R.messages.forEach(($) => {
            const p = `chat/messages/${$.order}.md`;
            !dc(p, m, v) && !dc("chat/transcript.md", m, v) || (T += 1, hy(y, {
              path: p,
              content: no($.content || "")
            }, i, l, f));
          }), I += R.messages.length, I >= R.total)) break;
        }
      }
      const C = y.slice(d, d + u);
      return {
        ok: !0,
        pattern: a,
        path: m,
        outputMode: l,
        searchedFileCount: T,
        count: y.length,
        results: C,
        matches: C.map((A) => ({
          path: A.path,
          line: A.lineNumber,
          text: A.line,
          context: A.context,
          count: A.count
        })),
        truncated: d + u < y.length,
        nextOffset: d + u < y.length ? d + u : 0,
        summary: `搜索到 ${y.length} 项，返回 ${C.length} 项。`
      };
    }
    if (t === Zn.WRITE) {
      const a = fy(n.filePath || n.path || "");
      if (!sT(a)) return {
        ok: !1,
        path: a,
        changed: !1,
        summary: `${a} 是只读资料，不能写入。`,
        error: "source_file_read_only"
      };
      const i = await lT(o, ba.WRITE, {
        filePath: a,
        content: String(n.content || "")
      }, r);
      return {
        ...i,
        bytes: i.ok ? new TextEncoder().encode(String(n.content || "")).length : void 0
      };
    }
    if (t === Zn.EDIT) {
      const a = fy(n.filePath || n.path || "");
      return sT(a) ? lT(o, ba.EDIT, {
        filePath: a,
        edits: n.edits
      }, r) : {
        ok: !1,
        path: a,
        changed: !1,
        summary: `${a} 是只读资料，不能编辑。`,
        error: "source_file_read_only"
      };
    }
    return {
      ok: !1,
      summary: `${t} 不可用。`,
      error: "source_tool_not_available"
    };
  } catch (a) {
    if (bM(a)) throw a;
    return {
      ok: !1,
      summary: a instanceof Error ? a.message : String(a || "source_tool_failed"),
      error: a instanceof Error ? a.message : String(a || "source_tool_failed")
    };
  }
}
function r1() {
  return [
    ...YB(),
    ...lB(),
    ...MB()
  ];
}
function bM(e) {
  const t = e instanceof Error ? e.message : String(e || "");
  return (e instanceof Error ? e.name : "") === "AbortError" || t === "manager_source_messages_changed" || t === "manager_epoch_expired" || t === "manager_aborted";
}
function my(e, t = {}) {
  const n = t.full === !0, r = n ? no(e.content) : no(e.content, 8e3), o = Array.isArray(e.thoughts) ? e.thoughts.map((s, a) => ({
    label: zs(s?.label || `思考 ${a + 1}`, 80),
    text: no(s?.text || "", n ? void 0 : 8e3)
  })).filter((s) => s.text) : [];
  return {
    order: e.order,
    role: String(e.role || ""),
    snippet: zs(e.content, 320),
    reasoningSnippet: o.length ? zs(o.map((s) => s.text).join(`
`), 320) : void 0,
    content: n ? r : void 0,
    thoughts: n && o.length ? o : void 0
  };
}
async function lT(e = "", t = "", n = {}, r = {}) {
  const o = String(e || "").trim();
  if (!o) return {
    ok: !1,
    summary: "缺少 sessionId。",
    error: "memory_session_required"
  };
  await Ib(o);
  try {
    if (t === ba.LIST) {
      const s = await hs(o, { includeStale: !0 });
      return {
        ok: !0,
        summary: `找到 ${s.length} 个记忆文件。`,
        files: s.map((a) => ({
          path: a.path,
          status: a.status,
          updatedAt: a.updatedAt
        }))
      };
    }
    if (t === ba.READ) {
      const s = WB(n), a = await Ma(o, s);
      if (!a) return {
        ok: !1,
        summary: `${s} 不存在。`,
        path: s,
        error: "memory_file_not_found"
      };
      const i = fm(a.content), l = Math.floor(Number(n.tail) || 0);
      let u = gg(n.offset, 1), d = pm(n.limit, Cb, Ui);
      l > 0 && (d = Math.min(Ui, l), u = Math.max(1, i.length - d + 1));
      const f = Math.max(0, u - 1), m = i.slice(f, f + d), v = f + d < i.length ? f + d + 1 : 0;
      return {
        ok: !0,
        summary: `读取 ${s} 第 ${f + 1}-${f + m.length} 行，共 ${i.length} 行。`,
        path: s,
        content: m.join(`
`),
        numberedContent: Ws(m, f + 1),
        lineStart: f + 1,
        lineEnd: f + m.length,
        totalLines: i.length,
        truncated: v > 0,
        nextOffset: v
      };
    }
    if (t === ba.WRITE) {
      const s = aT(String(n.filePath || n.path || "")), a = await Ot.transaction("rw", hn, eo, to, Yt, Et, async () => {
        await r.beforeWriteGuard?.(), r.managerRunId && await U_({
          managerRunId: r.managerRunId,
          sessionId: o,
          path: s
        });
        const l = await t1(o, s, String(n.content || ""), { source: "manager" });
        return r.managerRunId && await Z2({
          managerRunId: r.managerRunId,
          sessionId: o,
          path: l.path
        }), l;
      }), i = await Ma(o, a.path);
      return !i || i.content !== a.content ? {
        ok: !1,
        summary: `已生成 ${a.path} 的写入结果，但保存校验未通过；请重新读取文件确认当前内容。`,
        path: a.path,
        changed: !1,
        error: "memory_write_verification_failed"
      } : {
        ok: !0,
        summary: `已写入 ${a.path}。`,
        path: a.path,
        changed: !0
      };
    }
    if (t === ba.EDIT) {
      const s = aT(String(n.filePath || n.path || "")), a = await Ma(o, s);
      if (!a) return {
        ok: !1,
        summary: `${s} 不存在。`,
        path: s,
        error: "memory_file_not_found"
      };
      const i = aL(a.content, n.edits), l = Array.isArray(i.results) ? i.results : [], u = l.filter((y) => y.ok && !y.satisfied).length, d = l.filter((y) => y.ok && y.satisfied).length, f = u + d, m = Math.max(0, l.length - f), v = i.content !== a.content;
      if (v) {
        await Ot.transaction("rw", hn, eo, to, Yt, Et, async () => {
          await r.beforeWriteGuard?.(), r.managerRunId && await U_({
            managerRunId: r.managerRunId,
            sessionId: o,
            path: s
          }), await t1(o, s, i.content, { source: "manager" }), r.managerRunId && await Z2({
            managerRunId: r.managerRunId,
            sessionId: o,
            path: s
          });
        });
        const y = await Ma(o, s);
        if (!y || y.content !== i.content) return {
          ok: !1,
          partial: !0,
          summary: `已生成 ${s} 的修改结果，但保存校验未通过；请重新读取文件确认当前内容。`,
          path: s,
          changed: !1,
          appliedCount: u,
          satisfiedCount: d || void 0,
          successCount: f,
          failedCount: m,
          warning: i.warning,
          error: "memory_edit_persistence_verification_failed",
          details: l
        };
      }
      return {
        ok: !!i.ok,
        partial: !!i.partial,
        summary: i.ok ? v ? d > 0 ? `已修改 ${s}，应用 ${u} 项，另有 ${d} 项已是目标状态。` : `已修改 ${s}，应用 ${u} 项。` : `已确认 ${s} 的 ${d || u} 项修改已是目标状态，无需重复写入。` : v ? `已部分修改 ${s}：成功 ${f} 项，失败 ${m} 项。` : `未修改 ${s}：${l.find((y) => !y.ok)?.message || l.find((y) => !y.ok)?.error || "Edit failed"}。`,
        path: s,
        changed: v,
        appliedCount: u,
        satisfiedCount: d || void 0,
        successCount: f,
        failedCount: m,
        warning: i.warning,
        error: i.ok ? "" : "memory_edit_failed",
        details: l
      };
    }
    if (t === ba.GREP) {
      const s = String(n.pattern || "").trim();
      if (!s) return {
        ok: !1,
        summary: "缺少搜索词。",
        error: "memory_grep_pattern_required"
      };
      const a = n.regex === !0 || n.useRegex === !0 ? new RegExp(s, "iu") : null, i = s.toLowerCase(), l = RB(n.outputMode), u = pm(n.limit, PB, e1), d = hm(n.offset, 0), f = Math.min(5, hm(n.contextLines, 0)), m = String(n.filePath || n.path || "").trim(), v = m && m.endsWith(".md") ? Fo(m) : "", y = m && !v ? String(m || "").replace(/\\/g, "/").trim() : "";
      if (y && (!y.startsWith("memory/") || y.includes(".."))) throw new Error("memory_path_scope_required");
      const T = [];
      (await hs(o, { includeStale: !0 })).filter((A) => v ? A.path === v : y ? A.path.startsWith(y.endsWith("/") ? y : `${y}/`) : !0).forEach((A) => {
        const I = fm(A.content);
        let R = 0;
        I.forEach(($, p) => {
          if (a && (a.lastIndex = 0), (a ? a.test($) : $.toLowerCase().includes(i)) && (R += 1, l === "content")) {
            const b = Math.max(0, p - f), w = Math.min(I.length, p + f + 1);
            T.push({
              path: A.path,
              line: p + 1,
              text: $.trim(),
              context: f > 0 ? Ws(I.slice(b, w), b + 1) : void 0
            });
          }
        }), l === "files_with_matches" && R > 0 ? T.push({ path: A.path }) : l === "count" && R > 0 && T.push({
          path: A.path,
          count: R
        });
      });
      const C = T.slice(d, d + u);
      return {
        ok: !0,
        summary: `搜索到 ${T.length} 项，返回 ${C.length} 项。`,
        count: T.length,
        truncated: d + u < T.length,
        nextOffset: d + u < T.length ? d + u : 0,
        matches: C
      };
    }
    if (t === ba.CHAT_HISTORY) {
      const s = String(n.mode || "").trim(), a = Math.max(1, Math.min(100, Number(n.limit) || 12)), i = Math.max(0, Number(n.offset) || 0), l = n.full === !0;
      if (s === "recent") {
        const { messages: u, total: d } = await dg(o, a, i), f = u.map((v) => my(v, { full: l })), m = i + a < d;
        return {
          ok: !0,
          summary: `共有 ${d} 条原文，读取最近窗口 ${f.length} 条。`,
          count: d,
          truncated: m,
          nextOffset: m ? i + a : 0,
          messages: f
        };
      }
      if (s === "range") {
        const u = Math.max(0, Number(n.startOrder) || 0), d = Object.prototype.hasOwnProperty.call(n, "endOrder") && Number.isFinite(Number(n.endOrder)), f = d ? null : await kx(o), m = d ? Math.max(u, Number(n.endOrder) || u) : Math.max(u, Number(f?.order) || u), { messages: v, total: y } = await nu(o, u, m, a, i), T = v.map((A) => my(A, { full: l })), C = i + a < y;
        return {
          ok: !0,
          summary: `order ${u}-${m} 共 ${y} 条原文，返回 ${T.length} 条。`,
          count: y,
          truncated: C,
          nextOffset: C ? i + a : 0,
          messages: T
        };
      }
      if (s === "grep") {
        const u = await Cx(o), d = String(n.pattern || "").trim();
        if (!d) return {
          ok: !1,
          summary: "缺少搜索词。",
          error: "chat_history_pattern_required"
        };
        const f = n.regex === !0 || n.useRegex === !0 ? new RegExp(d, "iu") : null, m = d.toLowerCase(), v = u.filter((C) => (f && (f.lastIndex = 0), f ? f.test(C.content) : C.content.toLowerCase().includes(m))), y = v.slice(i, i + a).map((C) => my(C, { full: l })), T = i + a < v.length;
        return {
          ok: !0,
          summary: `搜索到 ${v.length} 条原文，返回 ${y.length} 条。`,
          count: v.length,
          truncated: T,
          nextOffset: T ? i + a : 0,
          messages: y
        };
      }
      return {
        ok: !1,
        summary: `不支持的 ChatHistory 模式：${s || "empty"}`,
        error: "chat_history_mode_invalid"
      };
    }
    return {
      ok: !1,
      summary: `${t} 不可用。`,
      error: "memory_tool_not_available"
    };
  } catch (s) {
    if (bM(s)) throw s;
    return {
      ok: !1,
      summary: s instanceof Error ? s.message : String(s || "memory_tool_failed"),
      error: s instanceof Error ? s.message : String(s || "memory_tool_failed")
    };
  }
}
function at(e, t, n, r, o) {
  if (r === "m") throw new TypeError("Private method is not writable");
  if (r === "a" && !o) throw new TypeError("Private accessor was defined without a setter");
  if (typeof t == "function" ? e !== t || !o : !t.has(e)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return r === "a" ? o.call(e, n) : o ? o.value = n : t.set(e, n), n;
}
function be(e, t, n, r) {
  if (n === "a" && !r) throw new TypeError("Private accessor was defined without a getter");
  if (typeof t == "function" ? e !== t || !r : !t.has(e)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return n === "m" ? r : n === "a" ? r.call(e) : r ? r.value : t.get(e);
}
var wM = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return wM = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
};
function zd(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var o1 = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, xt = class extends Error {
}, oo = class s1 extends xt {
  constructor(t, n, r, o, s) {
    super(`${s1.makeMessage(t, n, r)}`), this.status = t, this.headers = o, this.requestID = o?.get("request-id"), this.error = n, this.type = s ?? null;
  }
  static makeMessage(t, n, r) {
    const o = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && o ? `${t} ${o}` : t ? `${t} status code (no body)` : o || "(no status code or body)";
  }
  static generate(t, n, r, o) {
    if (!t || !o) return new vg({
      message: r,
      cause: o1(n)
    });
    const s = n, a = s?.error?.type;
    return t === 400 ? new TM(t, s, r, o, a) : t === 401 ? new EM(t, s, r, o, a) : t === 403 ? new AM(t, s, r, o, a) : t === 404 ? new CM(t, s, r, o, a) : t === 409 ? new kM(t, s, r, o, a) : t === 422 ? new IM(t, s, r, o, a) : t === 429 ? new xM(t, s, r, o, a) : t >= 500 ? new MM(t, s, r, o, a) : new s1(t, s, r, o, a);
  }
}, xo = class extends oo {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, vg = class extends oo {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, SM = class extends vg {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, TM = class extends oo {
}, EM = class extends oo {
}, AM = class extends oo {
}, CM = class extends oo {
}, kM = class extends oo {
}, IM = class extends oo {
}, xM = class extends oo {
}, MM = class extends oo {
}, ZB = /^[a-z][a-z0-9+.-]*:/i, jB = (e) => ZB.test(e), a1 = (e) => (a1 = Array.isArray, a1(e)), uT = a1;
function i1(e) {
  return typeof e != "object" ? {} : e ?? {};
}
function cT(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function eU(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
var tU = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new xt(`${e} must be an integer`);
  if (t < 0) throw new xt(`${e} must be a positive integer`);
  return t;
}, PM = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, nU = (e) => new Promise((t) => setTimeout(t, e)), Ol = "0.89.0", rU = () => typeof window < "u" && typeof window.document < "u" && typeof navigator < "u";
function oU() {
  return typeof Deno < "u" && Deno.build != null ? "deno" : typeof EdgeRuntime < "u" ? "edge" : Object.prototype.toString.call(typeof globalThis.process < "u" ? globalThis.process : 0) === "[object process]" ? "node" : "unknown";
}
var sU = () => {
  const e = oU();
  if (e === "deno") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ol,
    "X-Stainless-OS": fT(Deno.build.os),
    "X-Stainless-Arch": dT(Deno.build.arch),
    "X-Stainless-Runtime": "deno",
    "X-Stainless-Runtime-Version": typeof Deno.version == "string" ? Deno.version : Deno.version?.deno ?? "unknown"
  };
  if (typeof EdgeRuntime < "u") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ol,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": `other:${EdgeRuntime}`,
    "X-Stainless-Runtime": "edge",
    "X-Stainless-Runtime-Version": globalThis.process.version
  };
  if (e === "node") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ol,
    "X-Stainless-OS": fT(globalThis.process.platform ?? "unknown"),
    "X-Stainless-Arch": dT(globalThis.process.arch ?? "unknown"),
    "X-Stainless-Runtime": "node",
    "X-Stainless-Runtime-Version": globalThis.process.version ?? "unknown"
  };
  const t = aU();
  return t ? {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ol,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": `browser:${t.browser}`,
    "X-Stainless-Runtime-Version": t.version
  } : {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ol,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": "unknown",
    "X-Stainless-Runtime-Version": "unknown"
  };
};
function aU() {
  if (typeof navigator > "u" || !navigator) return null;
  for (const { key: e, pattern: t } of [
    {
      key: "edge",
      pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "chrome",
      pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "firefox",
      pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "safari",
      pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/
    }
  ]) {
    const n = t.exec(navigator.userAgent);
    if (n) return {
      browser: e,
      version: `${n[1] || 0}.${n[2] || 0}.${n[3] || 0}`
    };
  }
  return null;
}
var dT = (e) => e === "x32" ? "x32" : e === "x86_64" || e === "x64" ? "x64" : e === "arm" ? "arm" : e === "aarch64" || e === "arm64" ? "arm64" : e ? `other:${e}` : "unknown", fT = (e) => (e = e.toLowerCase(), e.includes("ios") ? "iOS" : e === "android" ? "Android" : e === "darwin" ? "MacOS" : e === "win32" ? "Windows" : e === "freebsd" ? "FreeBSD" : e === "openbsd" ? "OpenBSD" : e === "linux" ? "Linux" : e ? `Other:${e}` : "Unknown"), hT, iU = () => hT ?? (hT = sU());
function lU() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new Anthropic({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function RM(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function NM(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return RM({
    start() {
    },
    async pull(n) {
      const { done: r, value: o } = await t.next();
      r ? n.close() : n.enqueue(o);
    },
    async cancel() {
      await t.return?.();
    }
  });
}
function Mb(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function uU(e) {
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await e[Symbol.asyncIterator]().return?.();
    return;
  }
  const t = e.getReader(), n = t.cancel();
  t.releaseLock(), await n;
}
var cU = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
});
function dU(e) {
  return Object.entries(e).filter(([t, n]) => typeof n < "u").map(([t, n]) => {
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") return `${encodeURIComponent(t)}=${encodeURIComponent(n)}`;
    if (n === null) return `${encodeURIComponent(t)}=`;
    throw new xt(`Cannot stringify type ${typeof n}; Expected string, number, boolean, or null. If you need to pass nested query parameters, you can manually encode them, e.g. { query: { 'foo[key1]': value1, 'foo[key2]': value2 } }, and please open a GitHub issue requesting better support for your use case.`);
  }).join("&");
}
function fU(e) {
  let t = 0;
  for (const o of e) t += o.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const o of e)
    n.set(o, r), r += o.length;
  return n;
}
var pT;
function Pb(e) {
  let t;
  return (pT ?? (t = new globalThis.TextEncoder(), pT = t.encode.bind(t)))(e);
}
var mT;
function gT(e) {
  let t;
  return (mT ?? (t = new globalThis.TextDecoder(), mT = t.decode.bind(t)))(e);
}
var Ur, qr, df = class {
  constructor() {
    Ur.set(this, void 0), qr.set(this, void 0), at(this, Ur, new Uint8Array(), "f"), at(this, qr, null, "f");
  }
  decode(e) {
    if (e == null) return [];
    const t = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? Pb(e) : e;
    at(this, Ur, fU([be(this, Ur, "f"), t]), "f");
    const n = [];
    let r;
    for (; (r = hU(be(this, Ur, "f"), be(this, qr, "f"))) != null; ) {
      if (r.carriage && be(this, qr, "f") == null) {
        at(this, qr, r.index, "f");
        continue;
      }
      if (be(this, qr, "f") != null && (r.index !== be(this, qr, "f") + 1 || r.carriage)) {
        n.push(gT(be(this, Ur, "f").subarray(0, be(this, qr, "f") - 1))), at(this, Ur, be(this, Ur, "f").subarray(be(this, qr, "f")), "f"), at(this, qr, null, "f");
        continue;
      }
      const o = be(this, qr, "f") !== null ? r.preceding - 1 : r.preceding, s = gT(be(this, Ur, "f").subarray(0, o));
      n.push(s), at(this, Ur, be(this, Ur, "f").subarray(r.index), "f"), at(this, qr, null, "f");
    }
    return n;
  }
  flush() {
    return be(this, Ur, "f").length ? this.decode(`
`) : [];
  }
};
Ur = /* @__PURE__ */ new WeakMap(), qr = /* @__PURE__ */ new WeakMap();
df.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
df.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function hU(e, t) {
  for (let o = t ?? 0; o < e.length; o++) {
    if (e[o] === 10) return {
      preceding: o,
      index: o + 1,
      carriage: !1
    };
    if (e[o] === 13) return {
      preceding: o,
      index: o + 1,
      carriage: !0
    };
  }
  return null;
}
function pU(e) {
  for (let r = 0; r < e.length - 1; r++) {
    if (e[r] === 10 && e[r + 1] === 10 || e[r] === 13 && e[r + 1] === 13) return r + 2;
    if (e[r] === 13 && e[r + 1] === 10 && r + 3 < e.length && e[r + 2] === 13 && e[r + 3] === 10) return r + 4;
  }
  return -1;
}
var gm = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, vT = (e, t, n) => {
  if (e) {
    if (eU(gm, e)) return e;
    hr(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(gm))}`);
  }
};
function Uc() {
}
function mh(e, t, n) {
  return !t || gm[e] > gm[n] ? Uc : t[e].bind(t);
}
var mU = {
  error: Uc,
  warn: Uc,
  info: Uc,
  debug: Uc
}, yT = /* @__PURE__ */ new WeakMap();
function hr(e) {
  const t = e.logger, n = e.logLevel ?? "off";
  if (!t) return mU;
  const r = yT.get(t);
  if (r && r[0] === n) return r[1];
  const o = {
    error: mh("error", t, n),
    warn: mh("warn", t, n),
    info: mh("info", t, n),
    debug: mh("debug", t, n)
  };
  return yT.set(t, [n, o]), o;
}
var yi = (e) => (e.options && (e.options = { ...e.options }, delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "x-api-key" || t.toLowerCase() === "authorization" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), fc, Wd = class qc {
  constructor(t, n, r) {
    this.iterator = t, fc.set(this, void 0), this.controller = n, at(this, fc, r, "f");
  }
  static fromSSEResponse(t, n, r) {
    let o = !1;
    const s = r ? hr(r) : console;
    async function* a() {
      if (o) throw new xt("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      o = !0;
      let i = !1;
      try {
        for await (const l of gU(t, n)) {
          if (l.event === "completion") try {
            yield JSON.parse(l.data);
          } catch (u) {
            throw s.error("Could not parse message into JSON:", l.data), s.error("From chunk:", l.raw), u;
          }
          if (l.event === "message_start" || l.event === "message_delta" || l.event === "message_stop" || l.event === "content_block_start" || l.event === "content_block_delta" || l.event === "content_block_stop" || l.event === "message" || l.event === "user.message" || l.event === "user.interrupt" || l.event === "user.tool_confirmation" || l.event === "user.custom_tool_result" || l.event === "agent.message" || l.event === "agent.thinking" || l.event === "agent.tool_use" || l.event === "agent.tool_result" || l.event === "agent.mcp_tool_use" || l.event === "agent.mcp_tool_result" || l.event === "agent.custom_tool_use" || l.event === "agent.thread_context_compacted" || l.event === "session.status_running" || l.event === "session.status_idle" || l.event === "session.status_rescheduled" || l.event === "session.status_terminated" || l.event === "session.error" || l.event === "session.deleted" || l.event === "span.model_request_start" || l.event === "span.model_request_end") try {
            yield JSON.parse(l.data);
          } catch (u) {
            throw s.error("Could not parse message into JSON:", l.data), s.error("From chunk:", l.raw), u;
          }
          if (l.event !== "ping" && l.event === "error") {
            const u = PM(l.data) ?? l.data, d = u?.error?.type;
            throw new oo(void 0, u, void 0, t.headers, d);
          }
        }
        i = !0;
      } catch (l) {
        if (zd(l)) return;
        throw l;
      } finally {
        i || n.abort();
      }
    }
    return new qc(a, n, r);
  }
  static fromReadableStream(t, n, r) {
    let o = !1;
    async function* s() {
      const i = new df(), l = Mb(t);
      for await (const u of l) for (const d of i.decode(u)) yield d;
      for (const u of i.flush()) yield u;
    }
    async function* a() {
      if (o) throw new xt("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      o = !0;
      let i = !1;
      try {
        for await (const l of s())
          i || l && (yield JSON.parse(l));
        i = !0;
      } catch (l) {
        if (zd(l)) return;
        throw l;
      } finally {
        i || n.abort();
      }
    }
    return new qc(a, n, r);
  }
  [(fc = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), o = (s) => ({ next: () => {
      if (s.length === 0) {
        const a = r.next();
        t.push(a), n.push(a);
      }
      return s.shift();
    } });
    return [new qc(() => o(t), this.controller, be(this, fc, "f")), new qc(() => o(n), this.controller, be(this, fc, "f"))];
  }
  toReadableStream() {
    const t = this;
    let n;
    return RM({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: o, done: s } = await n.next();
          if (s) return r.close();
          const a = Pb(JSON.stringify(o) + `
`);
          r.enqueue(a);
        } catch (o) {
          r.error(o);
        }
      },
      async cancel() {
        await n.return?.();
      }
    });
  }
};
async function* gU(e, t) {
  if (!e.body)
    throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new xt("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new xt("Attempted to iterate over a response with no body");
  const n = new yU(), r = new df(), o = Mb(e.body);
  for await (const s of vU(o)) for (const a of r.decode(s)) {
    const i = n.decode(a);
    i && (yield i);
  }
  for (const s of r.flush()) {
    const a = n.decode(s);
    a && (yield a);
  }
}
async function* vU(e) {
  let t = new Uint8Array();
  for await (const n of e) {
    if (n == null) continue;
    const r = n instanceof ArrayBuffer ? new Uint8Array(n) : typeof n == "string" ? Pb(n) : n;
    let o = new Uint8Array(t.length + r.length);
    o.set(t), o.set(r, t.length), t = o;
    let s;
    for (; (s = pU(t)) !== -1; )
      yield t.slice(0, s), t = t.slice(s);
  }
  t.length > 0 && (yield t);
}
var yU = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const o = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], o;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = _U(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function _U(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function $M(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: o, startTime: s } = t, a = await (async () => {
    if (t.options.stream)
      return hr(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller) : Wd.fromSSEResponse(n, t.controller);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const i = n.headers.get("content-type")?.split(";")[0]?.trim();
    return i?.includes("application/json") || i?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : DM(await n.json(), n) : await n.text();
  })();
  return hr(e).debug(`[${r}] response parsed`, yi({
    retryOfRequestLogID: o,
    url: n.url,
    status: n.status,
    body: a,
    durationMs: Date.now() - s
  })), a;
}
function DM(e, t) {
  return !e || typeof e != "object" || Array.isArray(e) ? e : Object.defineProperty(e, "_request_id", {
    value: t.headers.get("request-id"),
    enumerable: !1
  });
}
var Hc, LM = class OM extends Promise {
  constructor(t, n, r = $M) {
    super((o) => {
      o(null);
    }), this.responsePromise = n, this.parseResponse = r, Hc.set(this, void 0), at(this, Hc, t, "f");
  }
  _thenUnwrap(t) {
    return new OM(be(this, Hc, "f"), this.responsePromise, async (n, r) => DM(t(await this.parseResponse(n, r), r), r.response));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n,
      request_id: n.headers.get("request-id")
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(be(this, Hc, "f"), t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
};
Hc = /* @__PURE__ */ new WeakMap();
var gh, FM = class {
  constructor(e, t, n, r) {
    gh.set(this, void 0), at(this, gh, e, "f"), this.options = r, this.response = t, this.body = n;
  }
  hasNextPage() {
    return this.getPaginatedItems().length ? this.nextPageRequestOptions() != null : !1;
  }
  async getNextPage() {
    const e = this.nextPageRequestOptions();
    if (!e) throw new xt("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
    return await be(this, gh, "f").requestAPIList(this.constructor, e);
  }
  async *iterPages() {
    let e = this;
    for (yield e; e.hasNextPage(); )
      e = await e.getNextPage(), yield e;
  }
  async *[(gh = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    for await (const e of this.iterPages()) for (const t of e.getPaginatedItems()) yield t;
  }
}, bU = class extends LM {
  constructor(e, t, n) {
    super(e, t, async (r, o) => new n(r, o.response, await $M(r, o), o.options));
  }
  async *[Symbol.asyncIterator]() {
    const e = await this;
    for await (const t of e) yield t;
  }
}, ff = class extends FM {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1, this.first_id = n.first_id || null, this.last_id = n.last_id || null;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    if (this.options.query?.before_id) {
      const t = this.first_id;
      return t ? {
        ...this.options,
        query: {
          ...i1(this.options.query),
          before_id: t
        }
      } : null;
    }
    const e = this.last_id;
    return e ? {
      ...this.options,
      query: {
        ...i1(this.options.query),
        after_id: e
      }
    } : null;
  }
}, gs = class extends FM {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.next_page = n.next_page || null;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  nextPageRequestOptions() {
    const e = this.next_page;
    return e ? {
      ...this.options,
      query: {
        ...i1(this.options.query),
        page: e
      }
    } : null;
  }
}, BM = () => {
  if (typeof File > "u") {
    const { process: e } = globalThis, t = typeof e?.versions?.node == "string" && parseInt(e.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (t ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function ou(e, t, n) {
  return BM(), new File(e, t ?? "unknown_file", n);
}
function up(e, t) {
  const n = typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "";
  return t ? n.split(/[\\/]/).pop() || void 0 : n;
}
var UM = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", Rb = async (e, t, n = !0) => ({
  ...e,
  body: await SU(e.body, t, n)
}), _T = /* @__PURE__ */ new WeakMap();
function wU(e) {
  const t = typeof e == "function" ? e : e.fetch, n = _T.get(t);
  if (n) return n;
  const r = (async () => {
    try {
      const o = "Response" in t ? t.Response : (await t("data:,")).constructor, s = new FormData();
      return s.toString() !== await new o(s).text();
    } catch {
      return !0;
    }
  })();
  return _T.set(t, r), r;
}
var SU = async (e, t, n = !0) => {
  if (!await wU(t)) throw new TypeError("The provided fetch function does not support file uploads with the current global FormData class.");
  const r = new FormData();
  return await Promise.all(Object.entries(e || {}).map(([o, s]) => l1(r, o, s, n))), r;
}, TU = (e) => e instanceof Blob && "name" in e, l1 = async (e, t, n, r) => {
  if (n !== void 0) {
    if (n == null) throw new TypeError(`Received null for "${t}"; to pass null in FormData, you must use the string 'null'`);
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") e.append(t, String(n));
    else if (n instanceof Response) {
      let o = {};
      const s = n.headers.get("Content-Type");
      s && (o = { type: s }), e.append(t, ou([await n.blob()], up(n, r), o));
    } else if (UM(n)) e.append(t, ou([await new Response(NM(n)).blob()], up(n, r)));
    else if (TU(n)) e.append(t, ou([n], up(n, r), { type: n.type }));
    else if (Array.isArray(n)) await Promise.all(n.map((o) => l1(e, t + "[]", o, r)));
    else if (typeof n == "object") await Promise.all(Object.entries(n).map(([o, s]) => l1(e, `${t}[${o}]`, s, r)));
    else throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${n} instead`);
  }
}, qM = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", EU = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && qM(e), AU = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function CU(e, t, n) {
  if (BM(), e = await e, t || (t = up(e, !0)), EU(e))
    return e instanceof File && t == null && n == null ? e : ou([await e.arrayBuffer()], t ?? e.name, {
      type: e.type,
      lastModified: e.lastModified,
      ...n
    });
  if (AU(e)) {
    const o = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), ou(await u1(o), t, n);
  }
  const r = await u1(e);
  if (!n?.type) {
    const o = r.find((s) => typeof s == "object" && "type" in s && s.type);
    typeof o == "string" && (n = {
      ...n,
      type: o
    });
  }
  return ou(r, t, n);
}
async function u1(e) {
  let t = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) t.push(e);
  else if (qM(e)) t.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (UM(e)) for await (const n of e) t.push(...await u1(n));
  else {
    const n = e?.constructor?.name;
    throw new Error(`Unexpected data type: ${typeof e}${n ? `; constructor: ${n}` : ""}${kU(e)}`);
  }
  return t;
}
function kU(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var Fn = class {
  constructor(e) {
    this._client = e;
  }
}, HM = /* @__PURE__ */ Symbol.for("brand.privateNullableHeaders");
function* IU(e) {
  if (!e) return;
  if (HM in e) {
    const { values: r, nulls: o } = e;
    yield* r.entries();
    for (const s of o) yield [s, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : uT(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const o = r[0];
    if (typeof o != "string") throw new TypeError("expected header name to be a string");
    const s = uT(r[1]) ? r[1] : [r[1]];
    let a = !1;
    for (const i of s)
      i !== void 0 && (t && !a && (a = !0, yield [o, null]), yield [o, i]);
  }
}
var rt = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = /* @__PURE__ */ new Set();
    for (const [s, a] of IU(r)) {
      const i = s.toLowerCase();
      o.has(i) || (t.delete(s), o.add(i)), a === null ? (t.delete(s), n.add(i)) : (t.append(s, a), n.delete(i));
    }
  }
  return {
    [HM]: !0,
    values: t,
    nulls: n
  };
};
function zM(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var bT = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), xU = (e = zM) => function(n, ...r) {
  if (n.length === 1) return n[0];
  let o = !1;
  const s = [], a = n.reduce((d, f, m) => {
    /[?#]/.test(f) && (o = !0);
    const v = r[m];
    let y = (o ? encodeURIComponent : e)("" + v);
    return m !== r.length && (v == null || typeof v == "object" && v.toString === Object.getPrototypeOf(Object.getPrototypeOf(v.hasOwnProperty ?? bT) ?? bT)?.toString) && (y = v + "", s.push({
      start: d.length + f.length,
      length: y.length,
      error: `Value of type ${Object.prototype.toString.call(v).slice(8, -1)} is not a valid path parameter`
    })), d + f + (m === r.length ? "" : y);
  }, ""), i = a.split(/[?#]/, 1)[0], l = /(?<=^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = l.exec(i)) !== null; ) s.push({
    start: u.index,
    length: u[0].length,
    error: `Value "${u[0]}" can't be safely passed as a path parameter`
  });
  if (s.sort((d, f) => d.start - f.start), s.length > 0) {
    let d = 0;
    const f = s.reduce((m, v) => {
      const y = " ".repeat(v.start - d), T = "^".repeat(v.length);
      return d = v.start + v.length, m + y + T;
    }, "");
    throw new xt(`Path parameters result in path with invalid segments:
${s.map((m) => m.error).join(`
`)}
${a}
${f}`);
  }
  return a;
}, Ct = /* @__PURE__ */ xU(zM), WM = class extends Fn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/environments?beta=true", {
      body: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/environments/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...o } = t;
    return this._client.post(Ct`/v1/environments/${e}?beta=true`, {
      body: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/environments?beta=true", gs, {
      query: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(Ct`/v1/environments/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(Ct`/v1/environments/${e}/archive?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, hd = /* @__PURE__ */ Symbol("anthropic.sdk.stainlessHelper");
function cp(e) {
  return typeof e == "object" && e !== null && hd in e;
}
function GM(e, t) {
  const n = /* @__PURE__ */ new Set();
  if (e)
    for (const r of e) cp(r) && n.add(r[hd]);
  if (t) {
    for (const r of t)
      if (cp(r) && n.add(r[hd]), Array.isArray(r.content))
        for (const o of r.content) cp(o) && n.add(o[hd]);
  }
  return Array.from(n);
}
function VM(e, t) {
  const n = GM(e, t);
  return n.length === 0 ? {} : { "x-stainless-helper": n.join(", ") };
}
function MU(e) {
  return cp(e) ? { "x-stainless-helper": e[hd] } : {};
}
var KM = class extends Fn {
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/files", ff, {
      query: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "files-api-2025-04-14"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(Ct`/v1/files/${e}`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString() }, n?.headers])
    });
  }
  download(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/files/${e}/content`, {
      ...n,
      headers: rt([{
        "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString(),
        Accept: "application/binary"
      }, n?.headers]),
      __binaryResponse: !0
    });
  }
  retrieveMetadata(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/files/${e}`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString() }, n?.headers])
    });
  }
  upload(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/files", Rb({
      body: r,
      ...t,
      headers: rt([
        { "anthropic-beta": [...n ?? [], "files-api-2025-04-14"].toString() },
        MU(r.file),
        t?.headers
      ])
    }, this._client));
  }
}, JM = class extends Fn {
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/models/${e}?beta=true`, {
      ...n,
      headers: rt([{ ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/models?beta=true", ff, {
      query: r,
      ...t,
      headers: rt([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers])
    });
  }
}, YM = class extends Fn {
  list(e, t = {}, n) {
    const { betas: r, ...o } = t ?? {};
    return this._client.getAPIList(Ct`/v1/agents/${e}/versions?beta=true`, gs, {
      query: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, Nb = class extends Fn {
  constructor() {
    super(...arguments), this.versions = new YM(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/agents?beta=true", {
      body: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r, ...o } = t ?? {};
    return this._client.get(Ct`/v1/agents/${e}?beta=true`, {
      query: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...o } = t;
    return this._client.post(Ct`/v1/agents/${e}?beta=true`, {
      body: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/agents?beta=true", gs, {
      query: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(Ct`/v1/agents/${e}/archive?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
Nb.Versions = YM;
var XM = {
  "claude-opus-4-20250514": 8192,
  "claude-opus-4-0": 8192,
  "claude-4-opus-20250514": 8192,
  "anthropic.claude-opus-4-20250514-v1:0": 8192,
  "claude-opus-4@20250514": 8192,
  "claude-opus-4-1-20250805": 8192,
  "anthropic.claude-opus-4-1-20250805-v1:0": 8192,
  "claude-opus-4-1@20250805": 8192
};
function QM(e) {
  return e?.output_format ?? e?.output_config?.format;
}
function wT(e, t, n) {
  const r = QM(t);
  return !t || !("parse" in (r ?? {})) ? {
    ...e,
    content: e.content.map((o) => {
      if (o.type === "text") {
        const s = Object.defineProperty({ ...o }, "parsed_output", {
          value: null,
          enumerable: !1
        });
        return Object.defineProperty(s, "parsed", {
          get() {
            return n.logger.warn("The `parsed` property on `text` blocks is deprecated, please use `parsed_output` instead."), null;
          },
          enumerable: !1
        });
      }
      return o;
    }),
    parsed_output: null
  } : ZM(e, t, n);
}
function ZM(e, t, n) {
  let r = null;
  const o = e.content.map((s) => {
    if (s.type === "text") {
      const a = PU(t, s.text);
      r === null && (r = a);
      const i = Object.defineProperty({ ...s }, "parsed_output", {
        value: a,
        enumerable: !1
      });
      return Object.defineProperty(i, "parsed", {
        get() {
          return n.logger.warn("The `parsed` property on `text` blocks is deprecated, please use `parsed_output` instead."), a;
        },
        enumerable: !1
      });
    }
    return s;
  });
  return {
    ...e,
    content: o,
    parsed_output: r
  };
}
function PU(e, t) {
  const n = QM(e);
  if (n?.type !== "json_schema") return null;
  try {
    return "parse" in n ? n.parse(t) : JSON.parse(t);
  } catch (r) {
    throw new xt(`Failed to parse structured output: ${r}`);
  }
}
var RU = (e) => {
  let t = 0, n = [];
  for (; t < e.length; ) {
    let r = e[t];
    if (r === "\\") {
      t++;
      continue;
    }
    if (r === "{") {
      n.push({
        type: "brace",
        value: "{"
      }), t++;
      continue;
    }
    if (r === "}") {
      n.push({
        type: "brace",
        value: "}"
      }), t++;
      continue;
    }
    if (r === "[") {
      n.push({
        type: "paren",
        value: "["
      }), t++;
      continue;
    }
    if (r === "]") {
      n.push({
        type: "paren",
        value: "]"
      }), t++;
      continue;
    }
    if (r === ":") {
      n.push({
        type: "separator",
        value: ":"
      }), t++;
      continue;
    }
    if (r === ",") {
      n.push({
        type: "delimiter",
        value: ","
      }), t++;
      continue;
    }
    if (r === '"') {
      let a = "", i = !1;
      for (r = e[++t]; r !== '"'; ) {
        if (t === e.length) {
          i = !0;
          break;
        }
        if (r === "\\") {
          if (t++, t === e.length) {
            i = !0;
            break;
          }
          a += r + e[t], r = e[++t];
        } else
          a += r, r = e[++t];
      }
      r = e[++t], i || n.push({
        type: "string",
        value: a
      });
      continue;
    }
    if (r && /\s/.test(r)) {
      t++;
      continue;
    }
    let o = /[0-9]/;
    if (r && o.test(r) || r === "-" || r === ".") {
      let a = "";
      for (r === "-" && (a += r, r = e[++t]); r && o.test(r) || r === "."; )
        a += r, r = e[++t];
      n.push({
        type: "number",
        value: a
      });
      continue;
    }
    let s = /[a-z]/i;
    if (r && s.test(r)) {
      let a = "";
      for (; r && s.test(r) && t !== e.length; )
        a += r, r = e[++t];
      if (a == "true" || a == "false" || a === "null") n.push({
        type: "name",
        value: a
      });
      else {
        t++;
        continue;
      }
      continue;
    }
    t++;
  }
  return n;
}, Fl = (e) => {
  if (e.length === 0) return e;
  let t = e[e.length - 1];
  switch (t.type) {
    case "separator":
      return e = e.slice(0, e.length - 1), Fl(e);
    case "number":
      let n = t.value[t.value.length - 1];
      if (n === "." || n === "-")
        return e = e.slice(0, e.length - 1), Fl(e);
    case "string":
      let r = e[e.length - 2];
      if (r?.type === "delimiter")
        return e = e.slice(0, e.length - 1), Fl(e);
      if (r?.type === "brace" && r.value === "{")
        return e = e.slice(0, e.length - 1), Fl(e);
      break;
    case "delimiter":
      return e = e.slice(0, e.length - 1), Fl(e);
  }
  return e;
}, NU = (e) => {
  let t = [];
  return e.map((n) => {
    n.type === "brace" && (n.value === "{" ? t.push("}") : t.splice(t.lastIndexOf("}"), 1)), n.type === "paren" && (n.value === "[" ? t.push("]") : t.splice(t.lastIndexOf("]"), 1));
  }), t.length > 0 && t.reverse().map((n) => {
    n === "}" ? e.push({
      type: "brace",
      value: "}"
    }) : n === "]" && e.push({
      type: "paren",
      value: "]"
    });
  }), e;
}, $U = (e) => {
  let t = "";
  return e.map((n) => {
    n.type === "string" ? t += '"' + n.value + '"' : t += n.value;
  }), t;
}, jM = (e) => JSON.parse($U(NU(Fl(RU(e))))), lo, ca, El, hc, vh, pc, mc, yh, gc, ks, vc, _h, bh, hi, wh, Sh, yc, gy, ST, Th, vy, yy, _y, TT, ET = "__json_buf";
function AT(e) {
  return e.type === "tool_use" || e.type === "server_tool_use" || e.type === "mcp_tool_use";
}
var DU = class c1 {
  constructor(t, n) {
    lo.add(this), this.messages = [], this.receivedMessages = [], ca.set(this, void 0), El.set(this, null), this.controller = new AbortController(), hc.set(this, void 0), vh.set(this, () => {
    }), pc.set(this, () => {
    }), mc.set(this, void 0), yh.set(this, () => {
    }), gc.set(this, () => {
    }), ks.set(this, {}), vc.set(this, !1), _h.set(this, !1), bh.set(this, !1), hi.set(this, !1), wh.set(this, void 0), Sh.set(this, void 0), yc.set(this, void 0), Th.set(this, (r) => {
      if (at(this, _h, !0, "f"), zd(r) && (r = new xo()), r instanceof xo)
        return at(this, bh, !0, "f"), this._emit("abort", r);
      if (r instanceof xt) return this._emit("error", r);
      if (r instanceof Error) {
        const o = new xt(r.message);
        return o.cause = r, this._emit("error", o);
      }
      return this._emit("error", new xt(String(r)));
    }), at(this, hc, new Promise((r, o) => {
      at(this, vh, r, "f"), at(this, pc, o, "f");
    }), "f"), at(this, mc, new Promise((r, o) => {
      at(this, yh, r, "f"), at(this, gc, o, "f");
    }), "f"), be(this, hc, "f").catch(() => {
    }), be(this, mc, "f").catch(() => {
    }), at(this, El, t, "f"), at(this, yc, n?.logger ?? console, "f");
  }
  get response() {
    return be(this, wh, "f");
  }
  get request_id() {
    return be(this, Sh, "f");
  }
  async withResponse() {
    at(this, hi, !0, "f");
    const t = await be(this, hc, "f");
    if (!t) throw new Error("Could not resolve a `Response` object");
    return {
      data: this,
      response: t,
      request_id: t.headers.get("request-id")
    };
  }
  static fromReadableStream(t) {
    const n = new c1(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createMessage(t, n, r, { logger: o } = {}) {
    const s = new c1(n, { logger: o });
    for (const a of n.messages) s._addMessageParam(a);
    return at(s, El, {
      ...n,
      stream: !0
    }, "f"), s._run(() => s._createMessage(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  _run(t) {
    t().then(() => {
      this._emitFinal(), this._emit("end");
    }, be(this, Th, "f"));
  }
  _addMessageParam(t) {
    this.messages.push(t);
  }
  _addMessage(t, n = !0) {
    this.receivedMessages.push(t), n && this._emit("message", t);
  }
  async _createMessage(t, n, r) {
    const o = r?.signal;
    let s;
    o && (o.aborted && this.controller.abort(), s = this.controller.abort.bind(this.controller), o.addEventListener("abort", s));
    try {
      be(this, lo, "m", vy).call(this);
      const { response: a, data: i } = await t.create({
        ...n,
        stream: !0
      }, {
        ...r,
        signal: this.controller.signal
      }).withResponse();
      this._connected(a);
      for await (const l of i) be(this, lo, "m", yy).call(this, l);
      if (i.controller.signal?.aborted) throw new xo();
      be(this, lo, "m", _y).call(this);
    } finally {
      o && s && o.removeEventListener("abort", s);
    }
  }
  _connected(t) {
    this.ended || (at(this, wh, t, "f"), at(this, Sh, t?.headers.get("request-id"), "f"), be(this, vh, "f").call(this, t), this._emit("connect"));
  }
  get ended() {
    return be(this, vc, "f");
  }
  get errored() {
    return be(this, _h, "f");
  }
  get aborted() {
    return be(this, bh, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(t, n) {
    return (be(this, ks, "f")[t] || (be(this, ks, "f")[t] = [])).push({ listener: n }), this;
  }
  off(t, n) {
    const r = be(this, ks, "f")[t];
    if (!r) return this;
    const o = r.findIndex((s) => s.listener === n);
    return o >= 0 && r.splice(o, 1), this;
  }
  once(t, n) {
    return (be(this, ks, "f")[t] || (be(this, ks, "f")[t] = [])).push({
      listener: n,
      once: !0
    }), this;
  }
  emitted(t) {
    return new Promise((n, r) => {
      at(this, hi, !0, "f"), t !== "error" && this.once("error", r), this.once(t, n);
    });
  }
  async done() {
    at(this, hi, !0, "f"), await be(this, mc, "f");
  }
  get currentMessage() {
    return be(this, ca, "f");
  }
  async finalMessage() {
    return await this.done(), be(this, lo, "m", gy).call(this);
  }
  async finalText() {
    return await this.done(), be(this, lo, "m", ST).call(this);
  }
  _emit(t, ...n) {
    if (be(this, vc, "f")) return;
    t === "end" && (at(this, vc, !0, "f"), be(this, yh, "f").call(this));
    const r = be(this, ks, "f")[t];
    if (r && (be(this, ks, "f")[t] = r.filter((o) => !o.once), r.forEach(({ listener: o }) => o(...n))), t === "abort") {
      const o = n[0];
      !be(this, hi, "f") && !r?.length && Promise.reject(o), be(this, pc, "f").call(this, o), be(this, gc, "f").call(this, o), this._emit("end");
      return;
    }
    if (t === "error") {
      const o = n[0];
      !be(this, hi, "f") && !r?.length && Promise.reject(o), be(this, pc, "f").call(this, o), be(this, gc, "f").call(this, o), this._emit("end");
    }
  }
  _emitFinal() {
    this.receivedMessages.at(-1) && this._emit("finalMessage", be(this, lo, "m", gy).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    let o;
    r && (r.aborted && this.controller.abort(), o = this.controller.abort.bind(this.controller), r.addEventListener("abort", o));
    try {
      be(this, lo, "m", vy).call(this), this._connected(null);
      const s = Wd.fromReadableStream(t, this.controller);
      for await (const a of s) be(this, lo, "m", yy).call(this, a);
      if (s.controller.signal?.aborted) throw new xo();
      be(this, lo, "m", _y).call(this);
    } finally {
      r && o && r.removeEventListener("abort", o);
    }
  }
  [(ca = /* @__PURE__ */ new WeakMap(), El = /* @__PURE__ */ new WeakMap(), hc = /* @__PURE__ */ new WeakMap(), vh = /* @__PURE__ */ new WeakMap(), pc = /* @__PURE__ */ new WeakMap(), mc = /* @__PURE__ */ new WeakMap(), yh = /* @__PURE__ */ new WeakMap(), gc = /* @__PURE__ */ new WeakMap(), ks = /* @__PURE__ */ new WeakMap(), vc = /* @__PURE__ */ new WeakMap(), _h = /* @__PURE__ */ new WeakMap(), bh = /* @__PURE__ */ new WeakMap(), hi = /* @__PURE__ */ new WeakMap(), wh = /* @__PURE__ */ new WeakMap(), Sh = /* @__PURE__ */ new WeakMap(), yc = /* @__PURE__ */ new WeakMap(), Th = /* @__PURE__ */ new WeakMap(), lo = /* @__PURE__ */ new WeakSet(), gy = function() {
    if (this.receivedMessages.length === 0) throw new xt("stream ended without producing a Message with role=assistant");
    return this.receivedMessages.at(-1);
  }, ST = function() {
    if (this.receivedMessages.length === 0) throw new xt("stream ended without producing a Message with role=assistant");
    const n = this.receivedMessages.at(-1).content.filter((r) => r.type === "text").map((r) => r.text);
    if (n.length === 0) throw new xt("stream ended without producing a content block with type=text");
    return n.join(" ");
  }, vy = function() {
    this.ended || at(this, ca, void 0, "f");
  }, yy = function(n) {
    if (this.ended) return;
    const r = be(this, lo, "m", TT).call(this, n);
    switch (this._emit("streamEvent", n, r), n.type) {
      case "content_block_delta": {
        const o = r.content.at(-1);
        switch (n.delta.type) {
          case "text_delta":
            o.type === "text" && this._emit("text", n.delta.text, o.text || "");
            break;
          case "citations_delta":
            o.type === "text" && this._emit("citation", n.delta.citation, o.citations ?? []);
            break;
          case "input_json_delta":
            AT(o) && o.input && this._emit("inputJson", n.delta.partial_json, o.input);
            break;
          case "thinking_delta":
            o.type === "thinking" && this._emit("thinking", n.delta.thinking, o.thinking);
            break;
          case "signature_delta":
            o.type === "thinking" && this._emit("signature", o.signature);
            break;
          case "compaction_delta":
            o.type === "compaction" && o.content && this._emit("compaction", o.content);
            break;
          default:
            n.delta;
        }
        break;
      }
      case "message_stop":
        this._addMessageParam(r), this._addMessage(wT(r, be(this, El, "f"), { logger: be(this, yc, "f") }), !0);
        break;
      case "content_block_stop":
        this._emit("contentBlock", r.content.at(-1));
        break;
      case "message_start":
        at(this, ca, r, "f");
        break;
      case "content_block_start":
      case "message_delta":
        break;
    }
  }, _y = function() {
    if (this.ended) throw new xt("stream has ended, this shouldn't happen");
    const n = be(this, ca, "f");
    if (!n) throw new xt("request ended without sending any chunks");
    return at(this, ca, void 0, "f"), wT(n, be(this, El, "f"), { logger: be(this, yc, "f") });
  }, TT = function(n) {
    let r = be(this, ca, "f");
    if (n.type === "message_start") {
      if (r) throw new xt(`Unexpected event order, got ${n.type} before receiving "message_stop"`);
      return n.message;
    }
    if (!r) throw new xt(`Unexpected event order, got ${n.type} before "message_start"`);
    switch (n.type) {
      case "message_stop":
        return r;
      case "message_delta":
        return r.container = n.delta.container, r.stop_reason = n.delta.stop_reason, r.stop_sequence = n.delta.stop_sequence, r.usage.output_tokens = n.usage.output_tokens, r.context_management = n.context_management, n.usage.input_tokens != null && (r.usage.input_tokens = n.usage.input_tokens), n.usage.cache_creation_input_tokens != null && (r.usage.cache_creation_input_tokens = n.usage.cache_creation_input_tokens), n.usage.cache_read_input_tokens != null && (r.usage.cache_read_input_tokens = n.usage.cache_read_input_tokens), n.usage.server_tool_use != null && (r.usage.server_tool_use = n.usage.server_tool_use), n.usage.iterations != null && (r.usage.iterations = n.usage.iterations), r;
      case "content_block_start":
        return r.content.push(n.content_block), r;
      case "content_block_delta": {
        const o = r.content.at(n.index);
        switch (n.delta.type) {
          case "text_delta":
            o?.type === "text" && (r.content[n.index] = {
              ...o,
              text: (o.text || "") + n.delta.text
            });
            break;
          case "citations_delta":
            o?.type === "text" && (r.content[n.index] = {
              ...o,
              citations: [...o.citations ?? [], n.delta.citation]
            });
            break;
          case "input_json_delta":
            if (o && AT(o)) {
              let s = o[ET] || "";
              s += n.delta.partial_json;
              const a = { ...o };
              if (Object.defineProperty(a, ET, {
                value: s,
                enumerable: !1,
                writable: !0
              }), s) try {
                a.input = jM(s);
              } catch (i) {
                const l = new xt(`Unable to parse tool parameter JSON from model. Please retry your request or adjust your prompt. Error: ${i}. JSON: ${s}`);
                be(this, Th, "f").call(this, l);
              }
              r.content[n.index] = a;
            }
            break;
          case "thinking_delta":
            o?.type === "thinking" && (r.content[n.index] = {
              ...o,
              thinking: o.thinking + n.delta.thinking
            });
            break;
          case "signature_delta":
            o?.type === "thinking" && (r.content[n.index] = {
              ...o,
              signature: n.delta.signature
            });
            break;
          case "compaction_delta":
            o?.type === "compaction" && (r.content[n.index] = {
              ...o,
              content: (o.content || "") + n.delta.content
            });
            break;
          default:
            n.delta;
        }
        return r;
      }
      case "content_block_stop":
        return r;
    }
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("streamEvent", (o) => {
      const s = n.shift();
      s ? s.resolve(o) : t.push(o);
    }), this.on("end", () => {
      r = !0;
      for (const o of n) o.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (o) => {
      r = !0;
      for (const s of n) s.reject(o);
      n.length = 0;
    }), this.on("error", (o) => {
      r = !0;
      for (const s of n) s.reject(o);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((o, s) => n.push({
        resolve: o,
        reject: s
      })).then((o) => o ? {
        value: o,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new Wd(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
};
var e3 = class extends Error {
  constructor(e) {
    const t = typeof e == "string" ? e : e.map((n) => n.type === "text" ? n.text : `[${n.type}]`).join(" ");
    super(t), this.name = "ToolError", this.content = e;
  }
};
var LU = `You have been working on the task described above but have not yet completed it. Write a continuation summary that will allow you (or another instance of yourself) to resume work efficiently in a future context window where the conversation history will be replaced with this summary. Your summary should be structured, concise, and actionable. Include:
1. Task Overview
The user's core request and success criteria
Any clarifications or constraints they specified
2. Current State
What has been completed so far
Files created, modified, or analyzed (with paths if relevant)
Key outputs or artifacts produced
3. Important Discoveries
Technical constraints or requirements uncovered
Decisions made and their rationale
Errors encountered and how they were resolved
What approaches were tried that didn't work (and why)
4. Next Steps
Specific actions needed to complete the task
Any blockers or open questions to resolve
Priority order if multiple steps remain
5. Context to Preserve
User preferences or style requirements
Domain-specific details that aren't obvious
Any promises made to the user
Be concise but complete—err on the side of including information that would prevent duplicate work or repeated mistakes. Write in a way that enables immediate resumption of the task.
Wrap your summary in <summary></summary> tags.`, _c, Al, pi, $n, Er, Fr, qs, da, bc, CT, d1;
function kT() {
  let e, t;
  return {
    promise: new Promise((n, r) => {
      e = n, t = r;
    }),
    resolve: e,
    reject: t
  };
}
var t3 = class {
  constructor(e, t, n) {
    _c.add(this), this.client = e, Al.set(this, !1), pi.set(this, !1), $n.set(this, void 0), Er.set(this, void 0), Fr.set(this, void 0), qs.set(this, void 0), da.set(this, void 0), bc.set(this, 0), at(this, $n, { params: {
      ...t,
      messages: structuredClone(t.messages)
    } }, "f");
    const r = ["BetaToolRunner", ...GM(t.tools, t.messages)].join(", ");
    at(this, Er, {
      ...n,
      headers: rt([{ "x-stainless-helper": r }, n?.headers])
    }, "f"), at(this, da, kT(), "f"), t.compactionControl?.enabled && console.warn('Anthropic: The `compactionControl` parameter is deprecated and will be removed in a future version. Use server-side compaction instead by passing `edits: [{ type: "compact_20260112" }]` in the params passed to `toolRunner()`. See https://platform.claude.com/docs/en/build-with-claude/compaction');
  }
  async *[(Al = /* @__PURE__ */ new WeakMap(), pi = /* @__PURE__ */ new WeakMap(), $n = /* @__PURE__ */ new WeakMap(), Er = /* @__PURE__ */ new WeakMap(), Fr = /* @__PURE__ */ new WeakMap(), qs = /* @__PURE__ */ new WeakMap(), da = /* @__PURE__ */ new WeakMap(), bc = /* @__PURE__ */ new WeakMap(), _c = /* @__PURE__ */ new WeakSet(), CT = async function() {
    const t = be(this, $n, "f").params.compactionControl;
    if (!t || !t.enabled) return !1;
    let n = 0;
    if (be(this, Fr, "f") !== void 0) try {
      const l = await be(this, Fr, "f");
      n = l.usage.input_tokens + (l.usage.cache_creation_input_tokens ?? 0) + (l.usage.cache_read_input_tokens ?? 0) + l.usage.output_tokens;
    } catch {
      return !1;
    }
    const r = t.contextTokenThreshold ?? 1e5;
    if (n < r) return !1;
    const o = t.model ?? be(this, $n, "f").params.model, s = t.summaryPrompt ?? LU, a = be(this, $n, "f").params.messages;
    if (a[a.length - 1].role === "assistant") {
      const l = a[a.length - 1];
      if (Array.isArray(l.content)) {
        const u = l.content.filter((d) => d.type !== "tool_use");
        u.length === 0 ? a.pop() : l.content = u;
      }
    }
    const i = await this.client.beta.messages.create({
      model: o,
      messages: [...a, {
        role: "user",
        content: [{
          type: "text",
          text: s
        }]
      }],
      max_tokens: be(this, $n, "f").params.max_tokens
    }, {
      signal: be(this, Er, "f").signal,
      headers: rt([be(this, Er, "f").headers, { "x-stainless-helper": "compaction" }])
    });
    if (i.content[0]?.type !== "text") throw new xt("Expected text response for compaction");
    return be(this, $n, "f").params.messages = [{
      role: "user",
      content: i.content
    }], !0;
  }, Symbol.asyncIterator)]() {
    var e;
    if (be(this, Al, "f")) throw new xt("Cannot iterate over a consumed stream");
    at(this, Al, !0, "f"), at(this, pi, !0, "f"), at(this, qs, void 0, "f");
    try {
      for (; ; ) {
        let t;
        try {
          if (be(this, $n, "f").params.max_iterations && be(this, bc, "f") >= be(this, $n, "f").params.max_iterations) break;
          at(this, pi, !1, "f"), at(this, qs, void 0, "f"), at(this, bc, (e = be(this, bc, "f"), e++, e), "f"), at(this, Fr, void 0, "f");
          const { max_iterations: n, compactionControl: r, ...o } = be(this, $n, "f").params;
          if (o.stream ? (t = this.client.beta.messages.stream({ ...o }, be(this, Er, "f")), at(this, Fr, t.finalMessage(), "f"), be(this, Fr, "f").catch(() => {
          }), yield t) : (at(this, Fr, this.client.beta.messages.create({
            ...o,
            stream: !1
          }, be(this, Er, "f")), "f"), yield be(this, Fr, "f")), !await be(this, _c, "m", CT).call(this)) {
            if (!be(this, pi, "f")) {
              const { role: a, content: i } = await be(this, Fr, "f");
              be(this, $n, "f").params.messages.push({
                role: a,
                content: i
              });
            }
            const s = await be(this, _c, "m", d1).call(this, be(this, $n, "f").params.messages.at(-1));
            if (s) be(this, $n, "f").params.messages.push(s);
            else if (!be(this, pi, "f")) break;
          }
        } finally {
          t && t.abort();
        }
      }
      if (!be(this, Fr, "f")) throw new xt("ToolRunner concluded without a message from the server");
      be(this, da, "f").resolve(await be(this, Fr, "f"));
    } catch (t) {
      throw at(this, Al, !1, "f"), be(this, da, "f").promise.catch(() => {
      }), be(this, da, "f").reject(t), at(this, da, kT(), "f"), t;
    }
  }
  setMessagesParams(e) {
    typeof e == "function" ? be(this, $n, "f").params = e(be(this, $n, "f").params) : be(this, $n, "f").params = e, at(this, pi, !0, "f"), at(this, qs, void 0, "f");
  }
  setRequestOptions(e) {
    typeof e == "function" ? at(this, Er, e(be(this, Er, "f")), "f") : at(this, Er, {
      ...be(this, Er, "f"),
      ...e
    }, "f");
  }
  async generateToolResponse(e = be(this, Er, "f").signal) {
    const t = await be(this, Fr, "f") ?? this.params.messages.at(-1);
    return t ? be(this, _c, "m", d1).call(this, t, e) : null;
  }
  done() {
    return be(this, da, "f").promise;
  }
  async runUntilDone() {
    if (!be(this, Al, "f")) for await (const e of this) ;
    return this.done();
  }
  get params() {
    return be(this, $n, "f").params;
  }
  pushMessages(...e) {
    this.setMessagesParams((t) => ({
      ...t,
      messages: [...t.messages, ...e]
    }));
  }
  then(e, t) {
    return this.runUntilDone().then(e, t);
  }
};
d1 = async function(t, n = be(this, Er, "f").signal) {
  return be(this, qs, "f") !== void 0 ? be(this, qs, "f") : (at(this, qs, OU(be(this, $n, "f").params, t, {
    ...be(this, Er, "f"),
    signal: n
  }), "f"), be(this, qs, "f"));
};
async function OU(e, t = e.messages.at(-1), n) {
  if (!t || t.role !== "assistant" || !t.content || typeof t.content == "string") return null;
  const r = t.content.filter((o) => o.type === "tool_use");
  return r.length === 0 ? null : {
    role: "user",
    content: await Promise.all(r.map(async (o) => {
      const s = e.tools.find((a) => ("name" in a ? a.name : a.mcp_server_name) === o.name);
      if (!s || !("run" in s)) return {
        type: "tool_result",
        tool_use_id: o.id,
        content: `Error: Tool '${o.name}' not found`,
        is_error: !0
      };
      try {
        let a = o.input;
        "parse" in s && s.parse && (a = s.parse(a));
        const i = await s.run(a, {
          toolUseBlock: o,
          signal: n?.signal
        });
        return {
          type: "tool_result",
          tool_use_id: o.id,
          content: i
        };
      } catch (a) {
        return {
          type: "tool_result",
          tool_use_id: o.id,
          content: a instanceof e3 ? a.content : `Error: ${a instanceof Error ? a.message : String(a)}`,
          is_error: !0
        };
      }
    }))
  };
}
var n3 = class r3 {
  constructor(t, n) {
    this.iterator = t, this.controller = n;
  }
  async *decoder() {
    const t = new df();
    for await (const n of this.iterator) for (const r of t.decode(n)) yield JSON.parse(r);
    for (const n of t.flush()) yield JSON.parse(n);
  }
  [Symbol.asyncIterator]() {
    return this.decoder();
  }
  static fromResponse(t, n) {
    if (!t.body)
      throw n.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new xt("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new xt("Attempted to iterate over a response with no body");
    return new r3(Mb(t.body), n);
  }
}, o3 = class extends Fn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/messages/batches?beta=true", {
      body: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "message-batches-2024-09-24"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/messages/batches/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/messages/batches?beta=true", ff, {
      query: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "message-batches-2024-09-24"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(Ct`/v1/messages/batches/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  cancel(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(Ct`/v1/messages/batches/${e}/cancel?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  async results(e, t = {}, n) {
    const r = await this.retrieve(e);
    if (!r.results_url) throw new xt(`No batch \`results_url\`; Has it finished processing? ${r.processing_status} - ${r.id}`);
    const { betas: o } = t ?? {};
    return this._client.get(r.results_url, {
      ...n,
      headers: rt([{
        "anthropic-beta": [...o ?? [], "message-batches-2024-09-24"].toString(),
        Accept: "application/binary"
      }, n?.headers]),
      stream: !0,
      __binaryResponse: !0
    })._thenUnwrap((s, a) => n3.fromResponse(a.response, a.controller));
  }
}, IT = {
  "claude-1.3": "November 6th, 2024",
  "claude-1.3-100k": "November 6th, 2024",
  "claude-instant-1.1": "November 6th, 2024",
  "claude-instant-1.1-100k": "November 6th, 2024",
  "claude-instant-1.2": "November 6th, 2024",
  "claude-3-sonnet-20240229": "July 21st, 2025",
  "claude-3-opus-20240229": "January 5th, 2026",
  "claude-2.1": "July 21st, 2025",
  "claude-2.0": "July 21st, 2025",
  "claude-3-7-sonnet-latest": "February 19th, 2026",
  "claude-3-7-sonnet-20250219": "February 19th, 2026"
}, FU = ["claude-opus-4-6"], hf = class extends Fn {
  constructor() {
    super(...arguments), this.batches = new o3(this._client);
  }
  create(e, t) {
    const n = xT(e), { betas: r, ...o } = n;
    o.model in IT && console.warn(`The model '${o.model}' is deprecated and will reach end-of-life on ${IT[o.model]}
Please migrate to a newer model. Visit https://docs.anthropic.com/en/docs/resources/model-deprecations for more information.`), o.model in FU && o.thinking && o.thinking.type === "enabled" && console.warn(`Using Claude with ${o.model} and 'thinking.type=enabled' is deprecated. Use 'thinking.type=adaptive' instead which results in better model performance in our testing: https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking`);
    let s = this._client._options.timeout;
    if (!o.stream && s == null) {
      const i = XM[o.model] ?? void 0;
      s = this._client.calculateNonstreamingTimeout(o.max_tokens, i);
    }
    const a = VM(o.tools, o.messages);
    return this._client.post("/v1/messages?beta=true", {
      body: o,
      timeout: s ?? 6e5,
      ...t,
      headers: rt([
        { ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 },
        a,
        t?.headers
      ]),
      stream: n.stream ?? !1
    });
  }
  parse(e, t) {
    return t = {
      ...t,
      headers: rt([{ "anthropic-beta": [...e.betas ?? [], "structured-outputs-2025-12-15"].toString() }, t?.headers])
    }, this.create(e, t).then((n) => ZM(n, e, { logger: this._client.logger ?? console }));
  }
  stream(e, t) {
    return DU.createMessage(this, e, t);
  }
  countTokens(e, t) {
    const { betas: n, ...r } = xT(e);
    return this._client.post("/v1/messages/count_tokens?beta=true", {
      body: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "token-counting-2024-11-01"].toString() }, t?.headers])
    });
  }
  toolRunner(e, t) {
    return new t3(this._client, e, t);
  }
};
function xT(e) {
  if (!e.output_format) return e;
  if (e.output_config?.format) throw new xt("Both output_format and output_config.format were provided. Please use only output_config.format (output_format is deprecated).");
  const { output_format: t, ...n } = e;
  return {
    ...n,
    output_config: {
      ...e.output_config,
      format: t
    }
  };
}
hf.Batches = o3;
hf.BetaToolRunner = t3;
hf.ToolError = e3;
var s3 = class extends Fn {
  list(e, t = {}, n) {
    const { betas: r, ...o } = t ?? {};
    return this._client.getAPIList(Ct`/v1/sessions/${e}/events?beta=true`, gs, {
      query: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  send(e, t, n) {
    const { betas: r, ...o } = t;
    return this._client.post(Ct`/v1/sessions/${e}/events?beta=true`, {
      body: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  stream(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/sessions/${e}/events/stream?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers]),
      stream: !0
    });
  }
}, a3 = class extends Fn {
  retrieve(e, t, n) {
    const { session_id: r, betas: o } = t;
    return this._client.get(Ct`/v1/sessions/${r}/resources/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { session_id: r, betas: o, ...s } = t;
    return this._client.post(Ct`/v1/sessions/${r}/resources/${e}?beta=true`, {
      body: s,
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...o } = t ?? {};
    return this._client.getAPIList(Ct`/v1/sessions/${e}/resources?beta=true`, gs, {
      query: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { session_id: r, betas: o } = t;
    return this._client.delete(Ct`/v1/sessions/${r}/resources/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  add(e, t, n) {
    const { betas: r, ...o } = t;
    return this._client.post(Ct`/v1/sessions/${e}/resources?beta=true`, {
      body: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, yg = class extends Fn {
  constructor() {
    super(...arguments), this.events = new s3(this._client), this.resources = new a3(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/sessions?beta=true", {
      body: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/sessions/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...o } = t;
    return this._client.post(Ct`/v1/sessions/${e}?beta=true`, {
      body: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/sessions?beta=true", gs, {
      query: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(Ct`/v1/sessions/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(Ct`/v1/sessions/${e}/archive?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
yg.Events = s3;
yg.Resources = a3;
var i3 = class extends Fn {
  create(e, t = {}, n) {
    const { betas: r, ...o } = t ?? {};
    return this._client.post(Ct`/v1/skills/${e}/versions?beta=true`, Rb({
      body: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    }, this._client));
  }
  retrieve(e, t, n) {
    const { skill_id: r, betas: o } = t;
    return this._client.get(Ct`/v1/skills/${r}/versions/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...o } = t ?? {};
    return this._client.getAPIList(Ct`/v1/skills/${e}/versions?beta=true`, gs, {
      query: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { skill_id: r, betas: o } = t;
    return this._client.delete(Ct`/v1/skills/${r}/versions/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
}, $b = class extends Fn {
  constructor() {
    super(...arguments), this.versions = new i3(this._client);
  }
  create(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.post("/v1/skills?beta=true", Rb({
      body: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "skills-2025-10-02"].toString() }, t?.headers])
    }, this._client, !1));
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/skills/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/skills?beta=true", gs, {
      query: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "skills-2025-10-02"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(Ct`/v1/skills/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
};
$b.Versions = i3;
var l3 = class extends Fn {
  create(e, t, n) {
    const { betas: r, ...o } = t;
    return this._client.post(Ct`/v1/vaults/${e}/credentials?beta=true`, {
      body: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { vault_id: r, betas: o } = t;
    return this._client.get(Ct`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { vault_id: r, betas: o, ...s } = t;
    return this._client.post(Ct`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      body: s,
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...o } = t ?? {};
    return this._client.getAPIList(Ct`/v1/vaults/${e}/credentials?beta=true`, gs, {
      query: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { vault_id: r, betas: o } = t;
    return this._client.delete(Ct`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t, n) {
    const { vault_id: r, betas: o } = t;
    return this._client.post(Ct`/v1/vaults/${r}/credentials/${e}/archive?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...o ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, Db = class extends Fn {
  constructor() {
    super(...arguments), this.credentials = new l3(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/vaults?beta=true", {
      body: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/vaults/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...o } = t;
    return this._client.post(Ct`/v1/vaults/${e}?beta=true`, {
      body: o,
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/vaults?beta=true", gs, {
      query: r,
      ...t,
      headers: rt([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(Ct`/v1/vaults/${e}?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(Ct`/v1/vaults/${e}/archive?beta=true`, {
      ...n,
      headers: rt([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
Db.Credentials = l3;
var vs = class extends Fn {
  constructor() {
    super(...arguments), this.models = new JM(this._client), this.messages = new hf(this._client), this.agents = new Nb(this._client), this.environments = new WM(this._client), this.sessions = new yg(this._client), this.vaults = new Db(this._client), this.files = new KM(this._client), this.skills = new $b(this._client);
  }
};
vs.Models = JM;
vs.Messages = hf;
vs.Agents = Nb;
vs.Environments = WM;
vs.Sessions = yg;
vs.Vaults = Db;
vs.Files = KM;
vs.Skills = $b;
var u3 = class extends Fn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/complete", {
      body: r,
      timeout: this._client._options.timeout ?? 6e5,
      ...t,
      headers: rt([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers]),
      stream: e.stream ?? !1
    });
  }
};
function c3(e) {
  return e?.output_config?.format;
}
function MT(e, t, n) {
  const r = c3(t);
  return !t || !("parse" in (r ?? {})) ? {
    ...e,
    content: e.content.map((o) => o.type === "text" ? Object.defineProperty({ ...o }, "parsed_output", {
      value: null,
      enumerable: !1
    }) : o),
    parsed_output: null
  } : d3(e, t, n);
}
function d3(e, t, n) {
  let r = null;
  const o = e.content.map((s) => {
    if (s.type === "text") {
      const a = BU(t, s.text);
      return r === null && (r = a), Object.defineProperty({ ...s }, "parsed_output", {
        value: a,
        enumerable: !1
      });
    }
    return s;
  });
  return {
    ...e,
    content: o,
    parsed_output: r
  };
}
function BU(e, t) {
  const n = c3(e);
  if (n?.type !== "json_schema") return null;
  try {
    return "parse" in n ? n.parse(t) : JSON.parse(t);
  } catch (r) {
    throw new xt(`Failed to parse structured output: ${r}`);
  }
}
var uo, fa, Cl, wc, Eh, Sc, Tc, Ah, Ec, Is, Ac, Ch, kh, mi, Ih, xh, Cc, by, PT, wy, Sy, Ty, Ey, RT, NT = "__json_buf";
function $T(e) {
  return e.type === "tool_use" || e.type === "server_tool_use";
}
var UU = class f1 {
  constructor(t, n) {
    uo.add(this), this.messages = [], this.receivedMessages = [], fa.set(this, void 0), Cl.set(this, null), this.controller = new AbortController(), wc.set(this, void 0), Eh.set(this, () => {
    }), Sc.set(this, () => {
    }), Tc.set(this, void 0), Ah.set(this, () => {
    }), Ec.set(this, () => {
    }), Is.set(this, {}), Ac.set(this, !1), Ch.set(this, !1), kh.set(this, !1), mi.set(this, !1), Ih.set(this, void 0), xh.set(this, void 0), Cc.set(this, void 0), wy.set(this, (r) => {
      if (at(this, Ch, !0, "f"), zd(r) && (r = new xo()), r instanceof xo)
        return at(this, kh, !0, "f"), this._emit("abort", r);
      if (r instanceof xt) return this._emit("error", r);
      if (r instanceof Error) {
        const o = new xt(r.message);
        return o.cause = r, this._emit("error", o);
      }
      return this._emit("error", new xt(String(r)));
    }), at(this, wc, new Promise((r, o) => {
      at(this, Eh, r, "f"), at(this, Sc, o, "f");
    }), "f"), at(this, Tc, new Promise((r, o) => {
      at(this, Ah, r, "f"), at(this, Ec, o, "f");
    }), "f"), be(this, wc, "f").catch(() => {
    }), be(this, Tc, "f").catch(() => {
    }), at(this, Cl, t, "f"), at(this, Cc, n?.logger ?? console, "f");
  }
  get response() {
    return be(this, Ih, "f");
  }
  get request_id() {
    return be(this, xh, "f");
  }
  async withResponse() {
    at(this, mi, !0, "f");
    const t = await be(this, wc, "f");
    if (!t) throw new Error("Could not resolve a `Response` object");
    return {
      data: this,
      response: t,
      request_id: t.headers.get("request-id")
    };
  }
  static fromReadableStream(t) {
    const n = new f1(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createMessage(t, n, r, { logger: o } = {}) {
    const s = new f1(n, { logger: o });
    for (const a of n.messages) s._addMessageParam(a);
    return at(s, Cl, {
      ...n,
      stream: !0
    }, "f"), s._run(() => s._createMessage(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  _run(t) {
    t().then(() => {
      this._emitFinal(), this._emit("end");
    }, be(this, wy, "f"));
  }
  _addMessageParam(t) {
    this.messages.push(t);
  }
  _addMessage(t, n = !0) {
    this.receivedMessages.push(t), n && this._emit("message", t);
  }
  async _createMessage(t, n, r) {
    const o = r?.signal;
    let s;
    o && (o.aborted && this.controller.abort(), s = this.controller.abort.bind(this.controller), o.addEventListener("abort", s));
    try {
      be(this, uo, "m", Sy).call(this);
      const { response: a, data: i } = await t.create({
        ...n,
        stream: !0
      }, {
        ...r,
        signal: this.controller.signal
      }).withResponse();
      this._connected(a);
      for await (const l of i) be(this, uo, "m", Ty).call(this, l);
      if (i.controller.signal?.aborted) throw new xo();
      be(this, uo, "m", Ey).call(this);
    } finally {
      o && s && o.removeEventListener("abort", s);
    }
  }
  _connected(t) {
    this.ended || (at(this, Ih, t, "f"), at(this, xh, t?.headers.get("request-id"), "f"), be(this, Eh, "f").call(this, t), this._emit("connect"));
  }
  get ended() {
    return be(this, Ac, "f");
  }
  get errored() {
    return be(this, Ch, "f");
  }
  get aborted() {
    return be(this, kh, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(t, n) {
    return (be(this, Is, "f")[t] || (be(this, Is, "f")[t] = [])).push({ listener: n }), this;
  }
  off(t, n) {
    const r = be(this, Is, "f")[t];
    if (!r) return this;
    const o = r.findIndex((s) => s.listener === n);
    return o >= 0 && r.splice(o, 1), this;
  }
  once(t, n) {
    return (be(this, Is, "f")[t] || (be(this, Is, "f")[t] = [])).push({
      listener: n,
      once: !0
    }), this;
  }
  emitted(t) {
    return new Promise((n, r) => {
      at(this, mi, !0, "f"), t !== "error" && this.once("error", r), this.once(t, n);
    });
  }
  async done() {
    at(this, mi, !0, "f"), await be(this, Tc, "f");
  }
  get currentMessage() {
    return be(this, fa, "f");
  }
  async finalMessage() {
    return await this.done(), be(this, uo, "m", by).call(this);
  }
  async finalText() {
    return await this.done(), be(this, uo, "m", PT).call(this);
  }
  _emit(t, ...n) {
    if (be(this, Ac, "f")) return;
    t === "end" && (at(this, Ac, !0, "f"), be(this, Ah, "f").call(this));
    const r = be(this, Is, "f")[t];
    if (r && (be(this, Is, "f")[t] = r.filter((o) => !o.once), r.forEach(({ listener: o }) => o(...n))), t === "abort") {
      const o = n[0];
      !be(this, mi, "f") && !r?.length && Promise.reject(o), be(this, Sc, "f").call(this, o), be(this, Ec, "f").call(this, o), this._emit("end");
      return;
    }
    if (t === "error") {
      const o = n[0];
      !be(this, mi, "f") && !r?.length && Promise.reject(o), be(this, Sc, "f").call(this, o), be(this, Ec, "f").call(this, o), this._emit("end");
    }
  }
  _emitFinal() {
    this.receivedMessages.at(-1) && this._emit("finalMessage", be(this, uo, "m", by).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    let o;
    r && (r.aborted && this.controller.abort(), o = this.controller.abort.bind(this.controller), r.addEventListener("abort", o));
    try {
      be(this, uo, "m", Sy).call(this), this._connected(null);
      const s = Wd.fromReadableStream(t, this.controller);
      for await (const a of s) be(this, uo, "m", Ty).call(this, a);
      if (s.controller.signal?.aborted) throw new xo();
      be(this, uo, "m", Ey).call(this);
    } finally {
      r && o && r.removeEventListener("abort", o);
    }
  }
  [(fa = /* @__PURE__ */ new WeakMap(), Cl = /* @__PURE__ */ new WeakMap(), wc = /* @__PURE__ */ new WeakMap(), Eh = /* @__PURE__ */ new WeakMap(), Sc = /* @__PURE__ */ new WeakMap(), Tc = /* @__PURE__ */ new WeakMap(), Ah = /* @__PURE__ */ new WeakMap(), Ec = /* @__PURE__ */ new WeakMap(), Is = /* @__PURE__ */ new WeakMap(), Ac = /* @__PURE__ */ new WeakMap(), Ch = /* @__PURE__ */ new WeakMap(), kh = /* @__PURE__ */ new WeakMap(), mi = /* @__PURE__ */ new WeakMap(), Ih = /* @__PURE__ */ new WeakMap(), xh = /* @__PURE__ */ new WeakMap(), Cc = /* @__PURE__ */ new WeakMap(), wy = /* @__PURE__ */ new WeakMap(), uo = /* @__PURE__ */ new WeakSet(), by = function() {
    if (this.receivedMessages.length === 0) throw new xt("stream ended without producing a Message with role=assistant");
    return this.receivedMessages.at(-1);
  }, PT = function() {
    if (this.receivedMessages.length === 0) throw new xt("stream ended without producing a Message with role=assistant");
    const n = this.receivedMessages.at(-1).content.filter((r) => r.type === "text").map((r) => r.text);
    if (n.length === 0) throw new xt("stream ended without producing a content block with type=text");
    return n.join(" ");
  }, Sy = function() {
    this.ended || at(this, fa, void 0, "f");
  }, Ty = function(n) {
    if (this.ended) return;
    const r = be(this, uo, "m", RT).call(this, n);
    switch (this._emit("streamEvent", n, r), n.type) {
      case "content_block_delta": {
        const o = r.content.at(-1);
        switch (n.delta.type) {
          case "text_delta":
            o.type === "text" && this._emit("text", n.delta.text, o.text || "");
            break;
          case "citations_delta":
            o.type === "text" && this._emit("citation", n.delta.citation, o.citations ?? []);
            break;
          case "input_json_delta":
            $T(o) && o.input && this._emit("inputJson", n.delta.partial_json, o.input);
            break;
          case "thinking_delta":
            o.type === "thinking" && this._emit("thinking", n.delta.thinking, o.thinking);
            break;
          case "signature_delta":
            o.type === "thinking" && this._emit("signature", o.signature);
            break;
          default:
            n.delta;
        }
        break;
      }
      case "message_stop":
        this._addMessageParam(r), this._addMessage(MT(r, be(this, Cl, "f"), { logger: be(this, Cc, "f") }), !0);
        break;
      case "content_block_stop":
        this._emit("contentBlock", r.content.at(-1));
        break;
      case "message_start":
        at(this, fa, r, "f");
        break;
      case "content_block_start":
      case "message_delta":
        break;
    }
  }, Ey = function() {
    if (this.ended) throw new xt("stream has ended, this shouldn't happen");
    const n = be(this, fa, "f");
    if (!n) throw new xt("request ended without sending any chunks");
    return at(this, fa, void 0, "f"), MT(n, be(this, Cl, "f"), { logger: be(this, Cc, "f") });
  }, RT = function(n) {
    let r = be(this, fa, "f");
    if (n.type === "message_start") {
      if (r) throw new xt(`Unexpected event order, got ${n.type} before receiving "message_stop"`);
      return n.message;
    }
    if (!r) throw new xt(`Unexpected event order, got ${n.type} before "message_start"`);
    switch (n.type) {
      case "message_stop":
        return r;
      case "message_delta":
        return r.stop_reason = n.delta.stop_reason, r.stop_sequence = n.delta.stop_sequence, r.usage.output_tokens = n.usage.output_tokens, n.usage.input_tokens != null && (r.usage.input_tokens = n.usage.input_tokens), n.usage.cache_creation_input_tokens != null && (r.usage.cache_creation_input_tokens = n.usage.cache_creation_input_tokens), n.usage.cache_read_input_tokens != null && (r.usage.cache_read_input_tokens = n.usage.cache_read_input_tokens), n.usage.server_tool_use != null && (r.usage.server_tool_use = n.usage.server_tool_use), r;
      case "content_block_start":
        return r.content.push({ ...n.content_block }), r;
      case "content_block_delta": {
        const o = r.content.at(n.index);
        switch (n.delta.type) {
          case "text_delta":
            o?.type === "text" && (r.content[n.index] = {
              ...o,
              text: (o.text || "") + n.delta.text
            });
            break;
          case "citations_delta":
            o?.type === "text" && (r.content[n.index] = {
              ...o,
              citations: [...o.citations ?? [], n.delta.citation]
            });
            break;
          case "input_json_delta":
            if (o && $T(o)) {
              let s = o[NT] || "";
              s += n.delta.partial_json;
              const a = { ...o };
              Object.defineProperty(a, NT, {
                value: s,
                enumerable: !1,
                writable: !0
              }), s && (a.input = jM(s)), r.content[n.index] = a;
            }
            break;
          case "thinking_delta":
            o?.type === "thinking" && (r.content[n.index] = {
              ...o,
              thinking: o.thinking + n.delta.thinking
            });
            break;
          case "signature_delta":
            o?.type === "thinking" && (r.content[n.index] = {
              ...o,
              signature: n.delta.signature
            });
            break;
          default:
            n.delta;
        }
        return r;
      }
      case "content_block_stop":
        return r;
    }
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("streamEvent", (o) => {
      const s = n.shift();
      s ? s.resolve(o) : t.push(o);
    }), this.on("end", () => {
      r = !0;
      for (const o of n) o.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (o) => {
      r = !0;
      for (const s of n) s.reject(o);
      n.length = 0;
    }), this.on("error", (o) => {
      r = !0;
      for (const s of n) s.reject(o);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((o, s) => n.push({
        resolve: o,
        reject: s
      })).then((o) => o ? {
        value: o,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new Wd(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
};
var f3 = class extends Fn {
  create(e, t) {
    return this._client.post("/v1/messages/batches", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Ct`/v1/messages/batches/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/v1/messages/batches", ff, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Ct`/v1/messages/batches/${e}`, t);
  }
  cancel(e, t) {
    return this._client.post(Ct`/v1/messages/batches/${e}/cancel`, t);
  }
  async results(e, t) {
    const n = await this.retrieve(e);
    if (!n.results_url) throw new xt(`No batch \`results_url\`; Has it finished processing? ${n.processing_status} - ${n.id}`);
    return this._client.get(n.results_url, {
      ...t,
      headers: rt([{ Accept: "application/binary" }, t?.headers]),
      stream: !0,
      __binaryResponse: !0
    })._thenUnwrap((r, o) => n3.fromResponse(o.response, o.controller));
  }
}, Lb = class extends Fn {
  constructor() {
    super(...arguments), this.batches = new f3(this._client);
  }
  create(e, t) {
    e.model in DT && console.warn(`The model '${e.model}' is deprecated and will reach end-of-life on ${DT[e.model]}
Please migrate to a newer model. Visit https://docs.anthropic.com/en/docs/resources/model-deprecations for more information.`), e.model in qU && e.thinking && e.thinking.type === "enabled" && console.warn(`Using Claude with ${e.model} and 'thinking.type=enabled' is deprecated. Use 'thinking.type=adaptive' instead which results in better model performance in our testing: https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking`);
    let n = this._client._options.timeout;
    if (!e.stream && n == null) {
      const o = XM[e.model] ?? void 0;
      n = this._client.calculateNonstreamingTimeout(e.max_tokens, o);
    }
    const r = VM(e.tools, e.messages);
    return this._client.post("/v1/messages", {
      body: e,
      timeout: n ?? 6e5,
      ...t,
      headers: rt([r, t?.headers]),
      stream: e.stream ?? !1
    });
  }
  parse(e, t) {
    return this.create(e, t).then((n) => d3(n, e, { logger: this._client.logger ?? console }));
  }
  stream(e, t) {
    return UU.createMessage(this, e, t, { logger: this._client.logger ?? console });
  }
  countTokens(e, t) {
    return this._client.post("/v1/messages/count_tokens", {
      body: e,
      ...t
    });
  }
}, DT = {
  "claude-1.3": "November 6th, 2024",
  "claude-1.3-100k": "November 6th, 2024",
  "claude-instant-1.1": "November 6th, 2024",
  "claude-instant-1.1-100k": "November 6th, 2024",
  "claude-instant-1.2": "November 6th, 2024",
  "claude-3-sonnet-20240229": "July 21st, 2025",
  "claude-3-opus-20240229": "January 5th, 2026",
  "claude-2.1": "July 21st, 2025",
  "claude-2.0": "July 21st, 2025",
  "claude-3-7-sonnet-latest": "February 19th, 2026",
  "claude-3-7-sonnet-20250219": "February 19th, 2026",
  "claude-3-5-haiku-latest": "February 19th, 2026",
  "claude-3-5-haiku-20241022": "February 19th, 2026",
  "claude-opus-4-0": "June 15th, 2026",
  "claude-opus-4-20250514": "June 15th, 2026",
  "claude-sonnet-4-0": "June 15th, 2026",
  "claude-sonnet-4-20250514": "June 15th, 2026"
}, qU = ["claude-opus-4-6"];
Lb.Batches = f3;
var h3 = class extends Fn {
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(Ct`/v1/models/${e}`, {
      ...n,
      headers: rt([{ ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/models", ff, {
      query: r,
      ...t,
      headers: rt([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers])
    });
  }
}, Mh = (e) => {
  if (typeof globalThis.process < "u") return globalThis.process.env?.[e]?.trim() || void 0;
  if (typeof globalThis.Deno < "u") return globalThis.Deno.env?.get?.(e)?.trim() || void 0;
}, h1, Ob, dp, p3, HU = "\\n\\nHuman:", zU = "\\n\\nAssistant:", Mn = class {
  constructor({ baseURL: e = Mh("ANTHROPIC_BASE_URL"), apiKey: t = Mh("ANTHROPIC_API_KEY") ?? null, authToken: n = Mh("ANTHROPIC_AUTH_TOKEN") ?? null, ...r } = {}) {
    h1.add(this), dp.set(this, void 0);
    const o = {
      apiKey: t,
      authToken: n,
      ...r,
      baseURL: e || "https://api.anthropic.com"
    };
    if (!o.dangerouslyAllowBrowser && rU()) throw new xt(`It looks like you're running in a browser-like environment.

This is disabled by default, as it risks exposing your secret API credentials to attackers.
If you understand the risks and have appropriate mitigations in place,
you can set the \`dangerouslyAllowBrowser\` option to \`true\`, e.g.,

new Anthropic({ apiKey, dangerouslyAllowBrowser: true });
`);
    this.baseURL = o.baseURL, this.timeout = o.timeout ?? Ob.DEFAULT_TIMEOUT, this.logger = o.logger ?? console;
    const s = "warn";
    this.logLevel = s, this.logLevel = vT(o.logLevel, "ClientOptions.logLevel", this) ?? vT(Mh("ANTHROPIC_LOG"), "process.env['ANTHROPIC_LOG']", this) ?? s, this.fetchOptions = o.fetchOptions, this.maxRetries = o.maxRetries ?? 2, this.fetch = o.fetch ?? lU(), at(this, dp, cU, "f"), this._options = o, this.apiKey = typeof t == "string" ? t : null, this.authToken = n;
  }
  withOptions(e) {
    return new this.constructor({
      ...this._options,
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this.apiKey,
      authToken: this.authToken,
      ...e
    });
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: e, nulls: t }) {
    if (!(e.get("x-api-key") || e.get("authorization")) && !(this.apiKey && e.get("x-api-key")) && !t.has("x-api-key") && !(this.authToken && e.get("authorization")) && !t.has("authorization"))
      throw new Error('Could not resolve authentication method. Expected either apiKey or authToken to be set. Or for one of the "X-Api-Key" or "Authorization" headers to be explicitly omitted');
  }
  async authHeaders(e) {
    return rt([await this.apiKeyAuth(e), await this.bearerAuth(e)]);
  }
  async apiKeyAuth(e) {
    if (this.apiKey != null)
      return rt([{ "X-Api-Key": this.apiKey }]);
  }
  async bearerAuth(e) {
    if (this.authToken != null)
      return rt([{ Authorization: `Bearer ${this.authToken}` }]);
  }
  stringifyQuery(e) {
    return dU(e);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${Ol}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${wM()}`;
  }
  makeStatusError(e, t, n, r) {
    return oo.generate(e, t, n, r);
  }
  buildURL(e, t, n) {
    const r = !be(this, h1, "m", p3).call(this) && n || this.baseURL, o = jB(e) ? new URL(e) : new URL(r + (r.endsWith("/") && e.startsWith("/") ? e.slice(1) : e)), s = this.defaultQuery(), a = Object.fromEntries(o.searchParams);
    return (!cT(s) || !cT(a)) && (t = {
      ...a,
      ...s,
      ...t
    }), typeof t == "object" && t && !Array.isArray(t) && (o.search = this.stringifyQuery(t)), o.toString();
  }
  _calculateNonstreamingTimeout(e) {
    if (3600 * e / 128e3 > 600) throw new xt("Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#streaming-responses for more details");
    return 600 * 1e3;
  }
  async prepareOptions(e) {
  }
  async prepareRequest(e, { url: t, options: n }) {
  }
  get(e, t) {
    return this.methodRequest("get", e, t);
  }
  post(e, t) {
    return this.methodRequest("post", e, t);
  }
  patch(e, t) {
    return this.methodRequest("patch", e, t);
  }
  put(e, t) {
    return this.methodRequest("put", e, t);
  }
  delete(e, t) {
    return this.methodRequest("delete", e, t);
  }
  methodRequest(e, t, n) {
    return this.request(Promise.resolve(n).then((r) => ({
      method: e,
      path: t,
      ...r
    })));
  }
  request(e, t = null) {
    return new LM(this, this.makeRequest(e, t, void 0));
  }
  async makeRequest(e, t, n) {
    const r = await e, o = r.maxRetries ?? this.maxRetries;
    t == null && (t = o), await this.prepareOptions(r);
    const { req: s, url: a, timeout: i } = await this.buildRequest(r, { retryCount: o - t });
    await this.prepareRequest(s, {
      url: a,
      options: r
    });
    const l = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), u = n === void 0 ? "" : `, retryOf: ${n}`, d = Date.now();
    if (hr(this).debug(`[${l}] sending request`, yi({
      retryOfRequestLogID: n,
      method: r.method,
      url: a,
      options: r,
      headers: s.headers
    })), r.signal?.aborted) throw new xo();
    const f = new AbortController(), m = await this.fetchWithTimeout(a, s, i, f).catch(o1), v = Date.now();
    if (m instanceof globalThis.Error) {
      const T = `retrying, ${t} attempts remaining`;
      if (r.signal?.aborted) throw new xo();
      const C = zd(m) || /timed? ?out/i.test(String(m) + ("cause" in m ? String(m.cause) : ""));
      if (t)
        return hr(this).info(`[${l}] connection ${C ? "timed out" : "failed"} - ${T}`), hr(this).debug(`[${l}] connection ${C ? "timed out" : "failed"} (${T})`, yi({
          retryOfRequestLogID: n,
          url: a,
          durationMs: v - d,
          message: m.message
        })), this.retryRequest(r, t, n ?? l);
      throw hr(this).info(`[${l}] connection ${C ? "timed out" : "failed"} - error; no more retries left`), hr(this).debug(`[${l}] connection ${C ? "timed out" : "failed"} (error; no more retries left)`, yi({
        retryOfRequestLogID: n,
        url: a,
        durationMs: v - d,
        message: m.message
      })), C ? new SM() : new vg({ cause: m });
    }
    const y = `[${l}${u}${[...m.headers.entries()].filter(([T]) => T === "request-id").map(([T, C]) => ", " + T + ": " + JSON.stringify(C)).join("")}] ${s.method} ${a} ${m.ok ? "succeeded" : "failed"} with status ${m.status} in ${v - d}ms`;
    if (!m.ok) {
      const T = await this.shouldRetry(m);
      if (t && T) {
        const $ = `retrying, ${t} attempts remaining`;
        return await uU(m.body), hr(this).info(`${y} - ${$}`), hr(this).debug(`[${l}] response error (${$})`, yi({
          retryOfRequestLogID: n,
          url: m.url,
          status: m.status,
          headers: m.headers,
          durationMs: v - d
        })), this.retryRequest(r, t, n ?? l, m.headers);
      }
      const C = T ? "error; no more retries left" : "error; not retryable";
      hr(this).info(`${y} - ${C}`);
      const A = await m.text().catch(($) => o1($).message), I = PM(A), R = I ? void 0 : A;
      throw hr(this).debug(`[${l}] response error (${C})`, yi({
        retryOfRequestLogID: n,
        url: m.url,
        status: m.status,
        headers: m.headers,
        message: R,
        durationMs: Date.now() - d
      })), this.makeStatusError(m.status, I, R, m.headers);
    }
    return hr(this).info(y), hr(this).debug(`[${l}] response start`, yi({
      retryOfRequestLogID: n,
      url: m.url,
      status: m.status,
      headers: m.headers,
      durationMs: v - d
    })), {
      response: m,
      options: r,
      controller: f,
      requestLogID: l,
      retryOfRequestLogID: n,
      startTime: d
    };
  }
  getAPIList(e, t, n) {
    return this.requestAPIList(t, n && "then" in n ? n.then((r) => ({
      method: "get",
      path: e,
      ...r
    })) : {
      method: "get",
      path: e,
      ...n
    });
  }
  requestAPIList(e, t) {
    const n = this.makeRequest(t, null, void 0);
    return new bU(this, n, e);
  }
  async fetchWithTimeout(e, t, n, r) {
    const { signal: o, method: s, ...a } = t || {}, i = this._makeAbort(r);
    o && o.addEventListener("abort", i, { once: !0 });
    const l = setTimeout(i, n), u = globalThis.ReadableStream && a.body instanceof globalThis.ReadableStream || typeof a.body == "object" && a.body !== null && Symbol.asyncIterator in a.body, d = {
      signal: r.signal,
      ...u ? { duplex: "half" } : {},
      method: "GET",
      ...a
    };
    s && (d.method = s.toUpperCase());
    try {
      return await this.fetch.call(void 0, e, d);
    } finally {
      clearTimeout(l);
    }
  }
  async shouldRetry(e) {
    const t = e.headers.get("x-should-retry");
    return t === "true" ? !0 : t === "false" ? !1 : e.status === 408 || e.status === 409 || e.status === 429 || e.status >= 500;
  }
  async retryRequest(e, t, n, r) {
    let o;
    const s = r?.get("retry-after-ms");
    if (s) {
      const i = parseFloat(s);
      Number.isNaN(i) || (o = i);
    }
    const a = r?.get("retry-after");
    if (a && !o) {
      const i = parseFloat(a);
      Number.isNaN(i) ? o = Date.parse(a) - Date.now() : o = i * 1e3;
    }
    if (o === void 0) {
      const i = e.maxRetries ?? this.maxRetries;
      o = this.calculateDefaultRetryTimeoutMillis(t, i);
    }
    return await nU(o), this.makeRequest(e, t - 1, n);
  }
  calculateDefaultRetryTimeoutMillis(e, t) {
    const o = t - e;
    return Math.min(0.5 * Math.pow(2, o), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  calculateNonstreamingTimeout(e, t) {
    if (36e5 * e / 128e3 > 6e5 || t != null && e > t) throw new xt("Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#long-requests for more details");
    return 6e5;
  }
  async buildRequest(e, { retryCount: t = 0 } = {}) {
    const n = { ...e }, { method: r, path: o, query: s, defaultBaseURL: a } = n, i = this.buildURL(o, s, a);
    "timeout" in n && tU("timeout", n.timeout), n.timeout = n.timeout ?? this.timeout;
    const { bodyHeaders: l, body: u } = this.buildBody({ options: n });
    return {
      req: {
        method: r,
        headers: await this.buildHeaders({
          options: e,
          method: r,
          bodyHeaders: l,
          retryCount: t
        }),
        ...n.signal && { signal: n.signal },
        ...globalThis.ReadableStream && u instanceof globalThis.ReadableStream && { duplex: "half" },
        ...u && { body: u },
        ...this.fetchOptions ?? {},
        ...n.fetchOptions ?? {}
      },
      url: i,
      timeout: n.timeout
    };
  }
  async buildHeaders({ options: e, method: t, bodyHeaders: n, retryCount: r }) {
    let o = {};
    this.idempotencyHeader && t !== "get" && (e.idempotencyKey || (e.idempotencyKey = this.defaultIdempotencyKey()), o[this.idempotencyHeader] = e.idempotencyKey);
    const s = rt([
      o,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent(),
        "X-Stainless-Retry-Count": String(r),
        ...e.timeout ? { "X-Stainless-Timeout": String(Math.trunc(e.timeout / 1e3)) } : {},
        ...iU(),
        ...this._options.dangerouslyAllowBrowser ? { "anthropic-dangerous-direct-browser-access": "true" } : void 0,
        "anthropic-version": "2023-06-01"
      },
      await this.authHeaders(e),
      this._options.defaultHeaders,
      n,
      e.headers
    ]);
    return this.validateHeaders(s), s.values;
  }
  _makeAbort(e) {
    return () => e.abort();
  }
  buildBody({ options: { body: e, headers: t } }) {
    if (!e) return {
      bodyHeaders: void 0,
      body: void 0
    };
    const n = rt([t]);
    return ArrayBuffer.isView(e) || e instanceof ArrayBuffer || e instanceof DataView || typeof e == "string" && n.values.has("content-type") || globalThis.Blob && e instanceof globalThis.Blob || e instanceof FormData || e instanceof URLSearchParams || globalThis.ReadableStream && e instanceof globalThis.ReadableStream ? {
      bodyHeaders: void 0,
      body: e
    } : typeof e == "object" && (Symbol.asyncIterator in e || Symbol.iterator in e && "next" in e && typeof e.next == "function") ? {
      bodyHeaders: void 0,
      body: NM(e)
    } : typeof e == "object" && n.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(e)
    } : be(this, dp, "f").call(this, {
      body: e,
      headers: n
    });
  }
};
Ob = Mn, dp = /* @__PURE__ */ new WeakMap(), h1 = /* @__PURE__ */ new WeakSet(), p3 = function() {
  return this.baseURL !== "https://api.anthropic.com";
};
Mn.Anthropic = Ob;
Mn.HUMAN_PROMPT = HU;
Mn.AI_PROMPT = zU;
Mn.DEFAULT_TIMEOUT = 6e5;
Mn.AnthropicError = xt;
Mn.APIError = oo;
Mn.APIConnectionError = vg;
Mn.APIConnectionTimeoutError = SM;
Mn.APIUserAbortError = xo;
Mn.NotFoundError = CM;
Mn.ConflictError = kM;
Mn.RateLimitError = xM;
Mn.BadRequestError = TM;
Mn.AuthenticationError = EM;
Mn.InternalServerError = MM;
Mn.PermissionDeniedError = AM;
Mn.UnprocessableEntityError = IM;
Mn.toFile = CU;
var pf = class extends Mn {
  constructor() {
    super(...arguments), this.completions = new u3(this), this.messages = new Lb(this), this.models = new h3(this), this.beta = new vs(this);
  }
};
pf.Completions = u3;
pf.Messages = Lb;
pf.Models = h3;
pf.Beta = vs;
function wu(e) {
  if (Array.isArray(e)) return e.map((n) => wu(n));
  if (!e || typeof e != "object") return e;
  const t = {};
  return Object.entries(e).forEach(([n, r]) => {
    t[n] = /authorization|csrf|token|api[-_]?key|proxy_password|password/i.test(n) ? "[redacted]" : wu(r);
  }), t;
}
function Gd(e = {}) {
  return {
    provider: e.provider || "",
    model: e.model || "",
    transport: e.transport || "sdk",
    request: wu({
      url: e.url || "",
      method: e.method || "POST",
      headers: e.headers || {},
      body: e.body || {},
      sdk: e.sdk || void 0
    })
  };
}
function WU(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function GU(e = "") {
  const t = String(e || "").match(/^data:([^;,]+);base64,(.+)$/);
  return t ? {
    mediaType: t[1],
    data: t[2]
  } : {
    mediaType: "",
    data: ""
  };
}
function m3(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function VU(e) {
  if (typeof e == "string") return [{
    type: "text",
    text: e
  }];
  if (!Array.isArray(e)) return [{
    type: "text",
    text: ""
  }];
  const t = e.map((n) => {
    if (!n || typeof n != "object") return null;
    if (n.type === "text") return {
      type: "text",
      text: n.text || ""
    };
    if (n.type === "image_url" && n.image_url?.url) {
      const r = GU(n.image_url.url);
      return !r.mediaType || !r.data ? null : {
        type: "image",
        source: {
          type: "base64",
          media_type: r.mediaType,
          data: r.data
        }
      };
    }
    return null;
  }).filter(Boolean);
  return t.length ? t : [{
    type: "text",
    text: ""
  }];
}
function KU(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  return t.length ? [...new Set(t)].join(`

`) : "";
}
function JU(e) {
  const t = e?.providerPayload?.anthropicContent;
  return Array.isArray(t) && t.length && m3(t) || null;
}
function YU(e) {
  return Array.isArray(e?.content) && e.content.length ? { anthropicContent: m3(e.content) || [] } : void 0;
}
function LT(e = {}) {
  return {
    type: "tool_result",
    tool_use_id: e.tool_call_id,
    content: e.content
  };
}
function OT(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = String(t?.function?.name || "").trim();
    return n ? {
      type: "tool_use",
      id: t.id,
      name: n,
      input: WU(t.function.arguments)
    } : null;
  }).filter(Boolean);
}
function XU(e) {
  const t = [];
  for (let n = 0; n < e.length; n += 1) {
    const r = e[n];
    if (r.role !== "system") {
      if (r.role === "assistant") {
        const o = JU(r), s = OT(r.tool_calls);
        if (o && s.length) {
          t.push({
            role: "assistant",
            content: o.filter((a) => a?.type !== "tool_use").concat(s)
          });
          continue;
        }
        if (o) {
          t.push({
            role: "assistant",
            content: o
          });
          continue;
        }
      }
      if (r.role === "tool") {
        const o = [LT(r)];
        for (; e[n + 1]?.role === "tool"; )
          n += 1, o.push(LT(e[n]));
        t.push({
          role: "user",
          content: o
        });
        continue;
      }
      if (r.role === "assistant" && Array.isArray(r.tool_calls) && r.tool_calls.length) {
        t.push({
          role: "assistant",
          content: [...r.content ? [{
            type: "text",
            text: r.content
          }] : [], ...OT(r.tool_calls)]
        });
        continue;
      }
      t.push({
        role: r.role,
        content: VU(r.content)
      });
    }
  }
  return t;
}
function Ph(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function FT(e = "") {
  return String(e || "https://api.anthropic.com").trim().replace(/\/+$/, "").replace(/\/v1$/i, "");
}
var QU = class {
  constructor(e) {
    this.config = e, this.client = new pf({
      apiKey: e.apiKey,
      baseURL: FT(e.baseUrl),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e) {
    const t = (e.tools || []).map((o) => ({
      name: o.function.name,
      description: o.function.description,
      input_schema: o.function.parameters
    })), n = KU(e), r = {
      model: this.config.model,
      system: n,
      messages: XU(e.messages),
      tools: t,
      ...e.maxTokens ? { max_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (r.temperature = e.temperature), e.reasoning?.enabled && (r.thinking = {
      type: "adaptive",
      display: "summarized"
    }), r;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = FT(this.config.baseUrl);
    return Gd({
      provider: "anthropic",
      model: this.config.model,
      transport: "anthropic-sdk",
      url: `${r}/v1/messages`,
      headers: {
        "Content-Type": "application/json",
        "x-api-key": this.config.apiKey || ""
      },
      body: t.body || this.buildRequestBody(e),
      sdk: n ? "client.messages.stream" : "client.messages.create"
    });
  }
  async chat(e) {
    const t = this.buildRequestBody(e), n = this.inspectRequest(e, { body: t });
    let r;
    if (typeof e.onStreamProgress == "function") {
      const s = this.client.messages.stream(t, { signal: e.signal }), a = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map();
      let l = "";
      const u = () => Array.from(a.entries()).sort(([m], [v]) => m.localeCompare(v)).map(([m, v]) => ({
        label: m.startsWith("redacted:") ? "已脱敏思考块" : "思考块",
        text: v
      })).filter((m) => m.text), d = () => Array.from(i.entries()).sort(([m], [v]) => Number(m) - Number(v)).map(([, m]) => ({
        id: m.id || "anthropic-tool-draft",
        name: m.name || "工具调用",
        arguments: m.inputJson || "{}",
        draft: !0
      })).filter((m) => m.name), f = () => {
        const m = d();
        m.length && Ph(e, {
          text: l,
          thoughts: u(),
          toolCalls: m,
          toolCallDraft: !0
        });
      };
      s.on("text", (m, v) => {
        l = v || "", Ph(e, {
          text: l,
          thoughts: u(),
          ...d().length ? {
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        });
      }), s.on("thinking", (m, v) => {
        a.set("thinking:0", v || ""), Ph(e, {
          thoughts: u(),
          ...d().length ? {
            text: l,
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        });
      }), s.on("streamEvent", (m) => {
        if (m?.type === "content_block_start" && m.content_block?.type === "tool_use") {
          const v = m.content_block.input && typeof m.content_block.input == "object" ? m.content_block.input : {};
          i.set(m.index, {
            id: m.content_block.id || `anthropic-tool-draft-${m.index + 1}`,
            name: m.content_block.name || "工具调用",
            inputJson: Object.keys(v).length ? JSON.stringify(v) : ""
          }), f();
          return;
        }
        if (m?.type === "content_block_delta" && m.delta?.type === "input_json_delta") {
          const v = i.get(m.index) || {
            id: `anthropic-tool-draft-${m.index + 1}`,
            name: "工具调用",
            inputJson: ""
          };
          i.set(m.index, {
            ...v,
            inputJson: `${v.inputJson || ""}${m.delta.partial_json || ""}`
          }), f();
        }
      }), s.on("contentBlock", (m) => {
        m?.type === "redacted_thinking" && (a.set("redacted:0", m.data || ""), Ph(e, {
          thoughts: u(),
          ...d().length ? {
            text: l,
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        }));
      }), r = await s.finalMessage();
    } else r = await this.client.messages.create(t, { signal: e.signal });
    const o = (r.content || []).filter((s) => s.type === "tool_use" && s.name).map((s, a) => ({
      id: s.id || `anthropic-tool-${a + 1}`,
      name: s.name,
      arguments: JSON.stringify(s.input || {})
    }));
    return {
      text: (r.content || []).filter((s) => s.type === "text").map((s) => s.text || "").join(`
`),
      toolCalls: o,
      thoughts: (r.content || []).filter((s) => s.type === "thinking" || s.type === "redacted_thinking").map((s) => ({
        label: s.type === "thinking" ? "思考块" : "已脱敏思考块",
        text: s.type === "thinking" ? s.thinking || "" : s.data || ""
      })).filter((s) => s.text),
      finishReason: r.stop_reason || "stop",
      model: r.model || this.config.model,
      provider: "anthropic",
      providerPayload: YU(r),
      requestInspection: n
    };
  }
}, ZU = /* @__PURE__ */ Zd(((e, t) => {
  function n(r, o) {
    typeof o == "boolean" && (o = { forever: o }), this._originalTimeouts = JSON.parse(JSON.stringify(r)), this._timeouts = r, this._options = o || {}, this._maxRetryTime = o && o.maxRetryTime || 1 / 0, this._fn = null, this._errors = [], this._attempts = 1, this._operationTimeout = null, this._operationTimeoutCb = null, this._timeout = null, this._operationStart = null, this._timer = null, this._options.forever && (this._cachedTimeouts = this._timeouts.slice(0));
  }
  t.exports = n, n.prototype.reset = function() {
    this._attempts = 1, this._timeouts = this._originalTimeouts.slice(0);
  }, n.prototype.stop = function() {
    this._timeout && clearTimeout(this._timeout), this._timer && clearTimeout(this._timer), this._timeouts = [], this._cachedTimeouts = null;
  }, n.prototype.retry = function(r) {
    if (this._timeout && clearTimeout(this._timeout), !r) return !1;
    var o = (/* @__PURE__ */ new Date()).getTime();
    if (r && o - this._operationStart >= this._maxRetryTime)
      return this._errors.push(r), this._errors.unshift(/* @__PURE__ */ new Error("RetryOperation timeout occurred")), !1;
    this._errors.push(r);
    var s = this._timeouts.shift();
    if (s === void 0) if (this._cachedTimeouts)
      this._errors.splice(0, this._errors.length - 1), s = this._cachedTimeouts.slice(-1);
    else return !1;
    var a = this;
    return this._timer = setTimeout(function() {
      a._attempts++, a._operationTimeoutCb && (a._timeout = setTimeout(function() {
        a._operationTimeoutCb(a._attempts);
      }, a._operationTimeout), a._options.unref && a._timeout.unref()), a._fn(a._attempts);
    }, s), this._options.unref && this._timer.unref(), !0;
  }, n.prototype.attempt = function(r, o) {
    this._fn = r, o && (o.timeout && (this._operationTimeout = o.timeout), o.cb && (this._operationTimeoutCb = o.cb));
    var s = this;
    this._operationTimeoutCb && (this._timeout = setTimeout(function() {
      s._operationTimeoutCb();
    }, s._operationTimeout)), this._operationStart = (/* @__PURE__ */ new Date()).getTime(), this._fn(this._attempts);
  }, n.prototype.try = function(r) {
    console.log("Using RetryOperation.try() is deprecated"), this.attempt(r);
  }, n.prototype.start = function(r) {
    console.log("Using RetryOperation.start() is deprecated"), this.attempt(r);
  }, n.prototype.start = n.prototype.try, n.prototype.errors = function() {
    return this._errors;
  }, n.prototype.attempts = function() {
    return this._attempts;
  }, n.prototype.mainError = function() {
    if (this._errors.length === 0) return null;
    for (var r = {}, o = null, s = 0, a = 0; a < this._errors.length; a++) {
      var i = this._errors[a], l = i.message, u = (r[l] || 0) + 1;
      r[l] = u, u >= s && (o = i, s = u);
    }
    return o;
  };
})), jU = /* @__PURE__ */ Zd(((e) => {
  var t = ZU();
  e.operation = function(n) {
    return new t(e.timeouts(n), {
      forever: n && (n.forever || n.retries === 1 / 0),
      unref: n && n.unref,
      maxRetryTime: n && n.maxRetryTime
    });
  }, e.timeouts = function(n) {
    if (n instanceof Array) return [].concat(n);
    var r = {
      retries: 10,
      factor: 2,
      minTimeout: 1 * 1e3,
      maxTimeout: 1 / 0,
      randomize: !1
    };
    for (var o in n) r[o] = n[o];
    if (r.minTimeout > r.maxTimeout) throw new Error("minTimeout is greater than maxTimeout");
    for (var s = [], a = 0; a < r.retries; a++) s.push(this.createTimeout(a, r));
    return n && n.forever && !s.length && s.push(this.createTimeout(a, r)), s.sort(function(i, l) {
      return i - l;
    }), s;
  }, e.createTimeout = function(n, r) {
    var o = r.randomize ? Math.random() + 1 : 1, s = Math.round(o * Math.max(r.minTimeout, 1) * Math.pow(r.factor, n));
    return s = Math.min(s, r.maxTimeout), s;
  }, e.wrap = function(n, r, o) {
    if (r instanceof Array && (o = r, r = null), !o) {
      o = [];
      for (var s in n) typeof n[s] == "function" && o.push(s);
    }
    for (var a = 0; a < o.length; a++) {
      var i = o[a], l = n[i];
      n[i] = function(d) {
        var f = e.operation(r), m = Array.prototype.slice.call(arguments, 1), v = m.pop();
        m.push(function(y) {
          f.retry(y) || (y && (arguments[0] = f.mainError()), v.apply(this, arguments));
        }), f.attempt(function() {
          d.apply(n, m);
        });
      }.bind(n, l), n[i].options = r;
    }
  };
})), eq = /* @__PURE__ */ Zd(((e, t) => {
  t.exports = jU();
})), tq = /* @__PURE__ */ Zd(((e, t) => {
  var n = eq(), r = [
    "Failed to fetch",
    "NetworkError when attempting to fetch resource.",
    "The Internet connection appears to be offline.",
    "Network request failed"
  ], o = class extends Error {
    constructor(l) {
      super(), l instanceof Error ? (this.originalError = l, { message: l } = l) : (this.originalError = new Error(l), this.originalError.stack = this.stack), this.name = "AbortError", this.message = l;
    }
  }, s = (l, u, d) => {
    const f = d.retries - (u - 1);
    return l.attemptNumber = u, l.retriesLeft = f, l;
  }, a = (l) => r.includes(l), i = (l, u) => new Promise((d, f) => {
    u = {
      onFailedAttempt: () => {
      },
      retries: 10,
      ...u
    };
    const m = n.operation(u);
    m.attempt(async (v) => {
      try {
        d(await l(v));
      } catch (y) {
        if (!(y instanceof Error)) {
          f(/* @__PURE__ */ new TypeError(`Non-error was thrown: "${y}". You should only throw errors.`));
          return;
        }
        if (y instanceof o)
          m.stop(), f(y.originalError);
        else if (y instanceof TypeError && !a(y.message))
          m.stop(), f(y);
        else {
          s(y, v, u);
          try {
            await u.onFailedAttempt(y);
          } catch (T) {
            f(T);
            return;
          }
          m.retry(y) || f(m.mainError());
        }
      }
    });
  });
  t.exports = i, t.exports.default = i, t.exports.AbortError = o;
})), BT = /* @__PURE__ */ jC(tq(), 1), nq = void 0, rq = void 0;
function oq() {
  return {
    geminiUrl: nq,
    vertexUrl: rq
  };
}
function sq(e, t, n, r) {
  var o, s;
  if (!e?.baseUrl) {
    const a = oq();
    return t ? (o = a.vertexUrl) !== null && o !== void 0 ? o : n : (s = a.geminiUrl) !== null && s !== void 0 ? s : r;
  }
  return e.baseUrl;
}
var ta = class {
};
function nt(e, t) {
  return e.replace(/\{([^}]+)\}/g, (n, r) => {
    if (Object.prototype.hasOwnProperty.call(t, r)) {
      const o = t[r];
      return o != null ? String(o) : "";
    } else throw new Error(`Key '${r}' not found in valueMap.`);
  });
}
function h(e, t, n) {
  for (let s = 0; s < t.length - 1; s++) {
    const a = t[s];
    if (a.endsWith("[]")) {
      const i = a.slice(0, -2);
      if (!(i in e)) if (Array.isArray(n)) e[i] = Array.from({ length: n.length }, () => ({}));
      else throw new Error(`Value must be a list given an array path ${a}`);
      if (Array.isArray(e[i])) {
        const l = e[i];
        if (Array.isArray(n)) for (let u = 0; u < l.length; u++) {
          const d = l[u];
          h(d, t.slice(s + 1), n[u]);
        }
        else for (const u of l) h(u, t.slice(s + 1), n);
      }
      return;
    } else if (a.endsWith("[0]")) {
      const i = a.slice(0, -3);
      i in e || (e[i] = [{}]);
      const l = e[i];
      h(l[0], t.slice(s + 1), n);
      return;
    }
    (!e[a] || typeof e[a] != "object") && (e[a] = {}), e = e[a];
  }
  const r = t[t.length - 1], o = e[r];
  if (o !== void 0) {
    if (!n || typeof n == "object" && Object.keys(n).length === 0 || n === o) return;
    if (typeof o == "object" && typeof n == "object" && o !== null && n !== null) Object.assign(o, n);
    else throw new Error(`Cannot set value for an existing key. Key: ${r}`);
  } else r === "_self" && typeof n == "object" && n !== null && !Array.isArray(n) ? Object.assign(e, n) : e[r] = n;
}
function c(e, t, n = void 0) {
  try {
    if (t.length === 1 && t[0] === "_self") return e;
    for (let r = 0; r < t.length; r++) {
      if (typeof e != "object" || e === null) return n;
      const o = t[r];
      if (o.endsWith("[]")) {
        const s = o.slice(0, -2);
        if (s in e) {
          const a = e[s];
          return Array.isArray(a) ? a.map((i) => c(i, t.slice(r + 1), n)) : n;
        } else return n;
      } else e = e[o];
    }
    return e;
  } catch (r) {
    if (r instanceof TypeError) return n;
    throw r;
  }
}
function aq(e, t) {
  for (const [n, r] of Object.entries(t)) {
    const o = n.split("."), s = r.split("."), a = /* @__PURE__ */ new Set();
    let i = -1;
    for (let l = 0; l < o.length; l++) if (o[l] === "*") {
      i = l;
      break;
    }
    if (i !== -1 && s.length > i) for (let l = i; l < s.length; l++) {
      const u = s[l];
      u !== "*" && !u.endsWith("[]") && !u.endsWith("[0]") && a.add(u);
    }
    p1(e, o, s, 0, a);
  }
}
function p1(e, t, n, r, o) {
  if (r >= t.length || typeof e != "object" || e === null) return;
  const s = t[r];
  if (s.endsWith("[]")) {
    const a = s.slice(0, -2), i = e;
    if (a in i && Array.isArray(i[a])) for (const l of i[a]) p1(l, t, n, r + 1, o);
  } else if (s === "*") {
    if (typeof e == "object" && e !== null && !Array.isArray(e)) {
      const a = e, i = Object.keys(a).filter((u) => !u.startsWith("_") && !o.has(u)), l = {};
      for (const u of i) l[u] = a[u];
      for (const [u, d] of Object.entries(l)) {
        const f = [];
        for (const m of n.slice(r)) m === "*" ? f.push(u) : f.push(m);
        h(a, f, d);
      }
      for (const u of i) delete a[u];
    }
  } else {
    const a = e;
    s in a && p1(a[s], t, n, r + 1, o);
  }
}
function Fb(e) {
  if (typeof e != "string") throw new Error("fromImageBytes must be a string");
  return e;
}
function iq(e) {
  const t = {}, n = c(e, ["operationName"]);
  n != null && h(t, ["operationName"], n);
  const r = c(e, ["resourceName"]);
  return r != null && h(t, ["_url", "resourceName"], r), t;
}
function lq(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const o = c(e, ["done"]);
  o != null && h(t, ["done"], o);
  const s = c(e, ["error"]);
  s != null && h(t, ["error"], s);
  const a = c(e, ["response", "generateVideoResponse"]);
  return a != null && h(t, ["response"], cq(a)), t;
}
function uq(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const o = c(e, ["done"]);
  o != null && h(t, ["done"], o);
  const s = c(e, ["error"]);
  s != null && h(t, ["error"], s);
  const a = c(e, ["response"]);
  return a != null && h(t, ["response"], dq(a)), t;
}
function cq(e) {
  const t = {}, n = c(e, ["generatedSamples"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((a) => fq(a))), h(t, ["generatedVideos"], s);
  }
  const r = c(e, ["raiMediaFilteredCount"]);
  r != null && h(t, ["raiMediaFilteredCount"], r);
  const o = c(e, ["raiMediaFilteredReasons"]);
  return o != null && h(t, ["raiMediaFilteredReasons"], o), t;
}
function dq(e) {
  const t = {}, n = c(e, ["videos"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((a) => hq(a))), h(t, ["generatedVideos"], s);
  }
  const r = c(e, ["raiMediaFilteredCount"]);
  r != null && h(t, ["raiMediaFilteredCount"], r);
  const o = c(e, ["raiMediaFilteredReasons"]);
  return o != null && h(t, ["raiMediaFilteredReasons"], o), t;
}
function fq(e) {
  const t = {}, n = c(e, ["video"]);
  return n != null && h(t, ["video"], _q(n)), t;
}
function hq(e) {
  const t = {}, n = c(e, ["_self"]);
  return n != null && h(t, ["video"], bq(n)), t;
}
function pq(e) {
  const t = {}, n = c(e, ["operationName"]);
  return n != null && h(t, ["_url", "operationName"], n), t;
}
function mq(e) {
  const t = {}, n = c(e, ["operationName"]);
  return n != null && h(t, ["_url", "operationName"], n), t;
}
function gq(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const o = c(e, ["done"]);
  o != null && h(t, ["done"], o);
  const s = c(e, ["error"]);
  s != null && h(t, ["error"], s);
  const a = c(e, ["response"]);
  return a != null && h(t, ["response"], vq(a)), t;
}
function vq(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && h(t, ["parent"], r);
  const o = c(e, ["documentName"]);
  return o != null && h(t, ["documentName"], o), t;
}
function g3(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const o = c(e, ["done"]);
  o != null && h(t, ["done"], o);
  const s = c(e, ["error"]);
  s != null && h(t, ["error"], s);
  const a = c(e, ["response"]);
  return a != null && h(t, ["response"], yq(a)), t;
}
function yq(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && h(t, ["parent"], r);
  const o = c(e, ["documentName"]);
  return o != null && h(t, ["documentName"], o), t;
}
function _q(e) {
  const t = {}, n = c(e, ["uri"]);
  n != null && h(t, ["uri"], n);
  const r = c(e, ["encodedVideo"]);
  r != null && h(t, ["videoBytes"], Fb(r));
  const o = c(e, ["encoding"]);
  return o != null && h(t, ["mimeType"], o), t;
}
function bq(e) {
  const t = {}, n = c(e, ["gcsUri"]);
  n != null && h(t, ["uri"], n);
  const r = c(e, ["bytesBase64Encoded"]);
  r != null && h(t, ["videoBytes"], Fb(r));
  const o = c(e, ["mimeType"]);
  return o != null && h(t, ["mimeType"], o), t;
}
var UT;
(function(e) {
  e.LANGUAGE_UNSPECIFIED = "LANGUAGE_UNSPECIFIED", e.PYTHON = "PYTHON";
})(UT || (UT = {}));
var qT;
(function(e) {
  e.OUTCOME_UNSPECIFIED = "OUTCOME_UNSPECIFIED", e.OUTCOME_OK = "OUTCOME_OK", e.OUTCOME_FAILED = "OUTCOME_FAILED", e.OUTCOME_DEADLINE_EXCEEDED = "OUTCOME_DEADLINE_EXCEEDED";
})(qT || (qT = {}));
var HT;
(function(e) {
  e.SCHEDULING_UNSPECIFIED = "SCHEDULING_UNSPECIFIED", e.SILENT = "SILENT", e.WHEN_IDLE = "WHEN_IDLE", e.INTERRUPT = "INTERRUPT";
})(HT || (HT = {}));
var Pa;
(function(e) {
  e.TYPE_UNSPECIFIED = "TYPE_UNSPECIFIED", e.STRING = "STRING", e.NUMBER = "NUMBER", e.INTEGER = "INTEGER", e.BOOLEAN = "BOOLEAN", e.ARRAY = "ARRAY", e.OBJECT = "OBJECT", e.NULL = "NULL";
})(Pa || (Pa = {}));
var zT;
(function(e) {
  e.ENVIRONMENT_UNSPECIFIED = "ENVIRONMENT_UNSPECIFIED", e.ENVIRONMENT_BROWSER = "ENVIRONMENT_BROWSER";
})(zT || (zT = {}));
var WT;
(function(e) {
  e.AUTH_TYPE_UNSPECIFIED = "AUTH_TYPE_UNSPECIFIED", e.NO_AUTH = "NO_AUTH", e.API_KEY_AUTH = "API_KEY_AUTH", e.HTTP_BASIC_AUTH = "HTTP_BASIC_AUTH", e.GOOGLE_SERVICE_ACCOUNT_AUTH = "GOOGLE_SERVICE_ACCOUNT_AUTH", e.OAUTH = "OAUTH", e.OIDC_AUTH = "OIDC_AUTH";
})(WT || (WT = {}));
var GT;
(function(e) {
  e.HTTP_IN_UNSPECIFIED = "HTTP_IN_UNSPECIFIED", e.HTTP_IN_QUERY = "HTTP_IN_QUERY", e.HTTP_IN_HEADER = "HTTP_IN_HEADER", e.HTTP_IN_PATH = "HTTP_IN_PATH", e.HTTP_IN_BODY = "HTTP_IN_BODY", e.HTTP_IN_COOKIE = "HTTP_IN_COOKIE";
})(GT || (GT = {}));
var VT;
(function(e) {
  e.API_SPEC_UNSPECIFIED = "API_SPEC_UNSPECIFIED", e.SIMPLE_SEARCH = "SIMPLE_SEARCH", e.ELASTIC_SEARCH = "ELASTIC_SEARCH";
})(VT || (VT = {}));
var KT;
(function(e) {
  e.PHISH_BLOCK_THRESHOLD_UNSPECIFIED = "PHISH_BLOCK_THRESHOLD_UNSPECIFIED", e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_HIGH_AND_ABOVE = "BLOCK_HIGH_AND_ABOVE", e.BLOCK_HIGHER_AND_ABOVE = "BLOCK_HIGHER_AND_ABOVE", e.BLOCK_VERY_HIGH_AND_ABOVE = "BLOCK_VERY_HIGH_AND_ABOVE", e.BLOCK_ONLY_EXTREMELY_HIGH = "BLOCK_ONLY_EXTREMELY_HIGH";
})(KT || (KT = {}));
var JT;
(function(e) {
  e.UNSPECIFIED = "UNSPECIFIED", e.BLOCKING = "BLOCKING", e.NON_BLOCKING = "NON_BLOCKING";
})(JT || (JT = {}));
var YT;
(function(e) {
  e.MODE_UNSPECIFIED = "MODE_UNSPECIFIED", e.MODE_DYNAMIC = "MODE_DYNAMIC";
})(YT || (YT = {}));
var m1;
(function(e) {
  e.MODE_UNSPECIFIED = "MODE_UNSPECIFIED", e.AUTO = "AUTO", e.ANY = "ANY", e.NONE = "NONE", e.VALIDATED = "VALIDATED";
})(m1 || (m1 = {}));
var pd;
(function(e) {
  e.THINKING_LEVEL_UNSPECIFIED = "THINKING_LEVEL_UNSPECIFIED", e.MINIMAL = "MINIMAL", e.LOW = "LOW", e.MEDIUM = "MEDIUM", e.HIGH = "HIGH";
})(pd || (pd = {}));
var XT;
(function(e) {
  e.DONT_ALLOW = "DONT_ALLOW", e.ALLOW_ADULT = "ALLOW_ADULT", e.ALLOW_ALL = "ALLOW_ALL";
})(XT || (XT = {}));
var QT;
(function(e) {
  e.PROMINENT_PEOPLE_UNSPECIFIED = "PROMINENT_PEOPLE_UNSPECIFIED", e.ALLOW_PROMINENT_PEOPLE = "ALLOW_PROMINENT_PEOPLE", e.BLOCK_PROMINENT_PEOPLE = "BLOCK_PROMINENT_PEOPLE";
})(QT || (QT = {}));
var ZT;
(function(e) {
  e.HARM_CATEGORY_UNSPECIFIED = "HARM_CATEGORY_UNSPECIFIED", e.HARM_CATEGORY_HARASSMENT = "HARM_CATEGORY_HARASSMENT", e.HARM_CATEGORY_HATE_SPEECH = "HARM_CATEGORY_HATE_SPEECH", e.HARM_CATEGORY_SEXUALLY_EXPLICIT = "HARM_CATEGORY_SEXUALLY_EXPLICIT", e.HARM_CATEGORY_DANGEROUS_CONTENT = "HARM_CATEGORY_DANGEROUS_CONTENT", e.HARM_CATEGORY_CIVIC_INTEGRITY = "HARM_CATEGORY_CIVIC_INTEGRITY", e.HARM_CATEGORY_IMAGE_HATE = "HARM_CATEGORY_IMAGE_HATE", e.HARM_CATEGORY_IMAGE_DANGEROUS_CONTENT = "HARM_CATEGORY_IMAGE_DANGEROUS_CONTENT", e.HARM_CATEGORY_IMAGE_HARASSMENT = "HARM_CATEGORY_IMAGE_HARASSMENT", e.HARM_CATEGORY_IMAGE_SEXUALLY_EXPLICIT = "HARM_CATEGORY_IMAGE_SEXUALLY_EXPLICIT", e.HARM_CATEGORY_JAILBREAK = "HARM_CATEGORY_JAILBREAK";
})(ZT || (ZT = {}));
var jT;
(function(e) {
  e.HARM_BLOCK_METHOD_UNSPECIFIED = "HARM_BLOCK_METHOD_UNSPECIFIED", e.SEVERITY = "SEVERITY", e.PROBABILITY = "PROBABILITY";
})(jT || (jT = {}));
var eE;
(function(e) {
  e.HARM_BLOCK_THRESHOLD_UNSPECIFIED = "HARM_BLOCK_THRESHOLD_UNSPECIFIED", e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_ONLY_HIGH = "BLOCK_ONLY_HIGH", e.BLOCK_NONE = "BLOCK_NONE", e.OFF = "OFF";
})(eE || (eE = {}));
var tE;
(function(e) {
  e.FINISH_REASON_UNSPECIFIED = "FINISH_REASON_UNSPECIFIED", e.STOP = "STOP", e.MAX_TOKENS = "MAX_TOKENS", e.SAFETY = "SAFETY", e.RECITATION = "RECITATION", e.LANGUAGE = "LANGUAGE", e.OTHER = "OTHER", e.BLOCKLIST = "BLOCKLIST", e.PROHIBITED_CONTENT = "PROHIBITED_CONTENT", e.SPII = "SPII", e.MALFORMED_FUNCTION_CALL = "MALFORMED_FUNCTION_CALL", e.IMAGE_SAFETY = "IMAGE_SAFETY", e.UNEXPECTED_TOOL_CALL = "UNEXPECTED_TOOL_CALL", e.IMAGE_PROHIBITED_CONTENT = "IMAGE_PROHIBITED_CONTENT", e.NO_IMAGE = "NO_IMAGE", e.IMAGE_RECITATION = "IMAGE_RECITATION", e.IMAGE_OTHER = "IMAGE_OTHER";
})(tE || (tE = {}));
var nE;
(function(e) {
  e.HARM_PROBABILITY_UNSPECIFIED = "HARM_PROBABILITY_UNSPECIFIED", e.NEGLIGIBLE = "NEGLIGIBLE", e.LOW = "LOW", e.MEDIUM = "MEDIUM", e.HIGH = "HIGH";
})(nE || (nE = {}));
var rE;
(function(e) {
  e.HARM_SEVERITY_UNSPECIFIED = "HARM_SEVERITY_UNSPECIFIED", e.HARM_SEVERITY_NEGLIGIBLE = "HARM_SEVERITY_NEGLIGIBLE", e.HARM_SEVERITY_LOW = "HARM_SEVERITY_LOW", e.HARM_SEVERITY_MEDIUM = "HARM_SEVERITY_MEDIUM", e.HARM_SEVERITY_HIGH = "HARM_SEVERITY_HIGH";
})(rE || (rE = {}));
var oE;
(function(e) {
  e.URL_RETRIEVAL_STATUS_UNSPECIFIED = "URL_RETRIEVAL_STATUS_UNSPECIFIED", e.URL_RETRIEVAL_STATUS_SUCCESS = "URL_RETRIEVAL_STATUS_SUCCESS", e.URL_RETRIEVAL_STATUS_ERROR = "URL_RETRIEVAL_STATUS_ERROR", e.URL_RETRIEVAL_STATUS_PAYWALL = "URL_RETRIEVAL_STATUS_PAYWALL", e.URL_RETRIEVAL_STATUS_UNSAFE = "URL_RETRIEVAL_STATUS_UNSAFE";
})(oE || (oE = {}));
var sE;
(function(e) {
  e.BLOCKED_REASON_UNSPECIFIED = "BLOCKED_REASON_UNSPECIFIED", e.SAFETY = "SAFETY", e.OTHER = "OTHER", e.BLOCKLIST = "BLOCKLIST", e.PROHIBITED_CONTENT = "PROHIBITED_CONTENT", e.IMAGE_SAFETY = "IMAGE_SAFETY", e.MODEL_ARMOR = "MODEL_ARMOR", e.JAILBREAK = "JAILBREAK";
})(sE || (sE = {}));
var aE;
(function(e) {
  e.TRAFFIC_TYPE_UNSPECIFIED = "TRAFFIC_TYPE_UNSPECIFIED", e.ON_DEMAND = "ON_DEMAND", e.ON_DEMAND_PRIORITY = "ON_DEMAND_PRIORITY", e.ON_DEMAND_FLEX = "ON_DEMAND_FLEX", e.PROVISIONED_THROUGHPUT = "PROVISIONED_THROUGHPUT";
})(aE || (aE = {}));
var vm;
(function(e) {
  e.MODALITY_UNSPECIFIED = "MODALITY_UNSPECIFIED", e.TEXT = "TEXT", e.IMAGE = "IMAGE", e.AUDIO = "AUDIO", e.VIDEO = "VIDEO";
})(vm || (vm = {}));
var iE;
(function(e) {
  e.MODEL_STAGE_UNSPECIFIED = "MODEL_STAGE_UNSPECIFIED", e.UNSTABLE_EXPERIMENTAL = "UNSTABLE_EXPERIMENTAL", e.EXPERIMENTAL = "EXPERIMENTAL", e.PREVIEW = "PREVIEW", e.STABLE = "STABLE", e.LEGACY = "LEGACY", e.DEPRECATED = "DEPRECATED", e.RETIRED = "RETIRED";
})(iE || (iE = {}));
var lE;
(function(e) {
  e.MEDIA_RESOLUTION_UNSPECIFIED = "MEDIA_RESOLUTION_UNSPECIFIED", e.MEDIA_RESOLUTION_LOW = "MEDIA_RESOLUTION_LOW", e.MEDIA_RESOLUTION_MEDIUM = "MEDIA_RESOLUTION_MEDIUM", e.MEDIA_RESOLUTION_HIGH = "MEDIA_RESOLUTION_HIGH";
})(lE || (lE = {}));
var uE;
(function(e) {
  e.TUNING_MODE_UNSPECIFIED = "TUNING_MODE_UNSPECIFIED", e.TUNING_MODE_FULL = "TUNING_MODE_FULL", e.TUNING_MODE_PEFT_ADAPTER = "TUNING_MODE_PEFT_ADAPTER";
})(uE || (uE = {}));
var cE;
(function(e) {
  e.ADAPTER_SIZE_UNSPECIFIED = "ADAPTER_SIZE_UNSPECIFIED", e.ADAPTER_SIZE_ONE = "ADAPTER_SIZE_ONE", e.ADAPTER_SIZE_TWO = "ADAPTER_SIZE_TWO", e.ADAPTER_SIZE_FOUR = "ADAPTER_SIZE_FOUR", e.ADAPTER_SIZE_EIGHT = "ADAPTER_SIZE_EIGHT", e.ADAPTER_SIZE_SIXTEEN = "ADAPTER_SIZE_SIXTEEN", e.ADAPTER_SIZE_THIRTY_TWO = "ADAPTER_SIZE_THIRTY_TWO";
})(cE || (cE = {}));
var g1;
(function(e) {
  e.JOB_STATE_UNSPECIFIED = "JOB_STATE_UNSPECIFIED", e.JOB_STATE_QUEUED = "JOB_STATE_QUEUED", e.JOB_STATE_PENDING = "JOB_STATE_PENDING", e.JOB_STATE_RUNNING = "JOB_STATE_RUNNING", e.JOB_STATE_SUCCEEDED = "JOB_STATE_SUCCEEDED", e.JOB_STATE_FAILED = "JOB_STATE_FAILED", e.JOB_STATE_CANCELLING = "JOB_STATE_CANCELLING", e.JOB_STATE_CANCELLED = "JOB_STATE_CANCELLED", e.JOB_STATE_PAUSED = "JOB_STATE_PAUSED", e.JOB_STATE_EXPIRED = "JOB_STATE_EXPIRED", e.JOB_STATE_UPDATING = "JOB_STATE_UPDATING", e.JOB_STATE_PARTIALLY_SUCCEEDED = "JOB_STATE_PARTIALLY_SUCCEEDED";
})(g1 || (g1 = {}));
var dE;
(function(e) {
  e.TUNING_JOB_STATE_UNSPECIFIED = "TUNING_JOB_STATE_UNSPECIFIED", e.TUNING_JOB_STATE_WAITING_FOR_QUOTA = "TUNING_JOB_STATE_WAITING_FOR_QUOTA", e.TUNING_JOB_STATE_PROCESSING_DATASET = "TUNING_JOB_STATE_PROCESSING_DATASET", e.TUNING_JOB_STATE_WAITING_FOR_CAPACITY = "TUNING_JOB_STATE_WAITING_FOR_CAPACITY", e.TUNING_JOB_STATE_TUNING = "TUNING_JOB_STATE_TUNING", e.TUNING_JOB_STATE_POST_PROCESSING = "TUNING_JOB_STATE_POST_PROCESSING";
})(dE || (dE = {}));
var fE;
(function(e) {
  e.AGGREGATION_METRIC_UNSPECIFIED = "AGGREGATION_METRIC_UNSPECIFIED", e.AVERAGE = "AVERAGE", e.MODE = "MODE", e.STANDARD_DEVIATION = "STANDARD_DEVIATION", e.VARIANCE = "VARIANCE", e.MINIMUM = "MINIMUM", e.MAXIMUM = "MAXIMUM", e.MEDIAN = "MEDIAN", e.PERCENTILE_P90 = "PERCENTILE_P90", e.PERCENTILE_P95 = "PERCENTILE_P95", e.PERCENTILE_P99 = "PERCENTILE_P99";
})(fE || (fE = {}));
var hE;
(function(e) {
  e.PAIRWISE_CHOICE_UNSPECIFIED = "PAIRWISE_CHOICE_UNSPECIFIED", e.BASELINE = "BASELINE", e.CANDIDATE = "CANDIDATE", e.TIE = "TIE";
})(hE || (hE = {}));
var pE;
(function(e) {
  e.TUNING_TASK_UNSPECIFIED = "TUNING_TASK_UNSPECIFIED", e.TUNING_TASK_I2V = "TUNING_TASK_I2V", e.TUNING_TASK_T2V = "TUNING_TASK_T2V", e.TUNING_TASK_R2V = "TUNING_TASK_R2V";
})(pE || (pE = {}));
var mE;
(function(e) {
  e.STATE_UNSPECIFIED = "STATE_UNSPECIFIED", e.STATE_PENDING = "STATE_PENDING", e.STATE_ACTIVE = "STATE_ACTIVE", e.STATE_FAILED = "STATE_FAILED";
})(mE || (mE = {}));
var gE;
(function(e) {
  e.MEDIA_RESOLUTION_UNSPECIFIED = "MEDIA_RESOLUTION_UNSPECIFIED", e.MEDIA_RESOLUTION_LOW = "MEDIA_RESOLUTION_LOW", e.MEDIA_RESOLUTION_MEDIUM = "MEDIA_RESOLUTION_MEDIUM", e.MEDIA_RESOLUTION_HIGH = "MEDIA_RESOLUTION_HIGH", e.MEDIA_RESOLUTION_ULTRA_HIGH = "MEDIA_RESOLUTION_ULTRA_HIGH";
})(gE || (gE = {}));
var vE;
(function(e) {
  e.TOOL_TYPE_UNSPECIFIED = "TOOL_TYPE_UNSPECIFIED", e.GOOGLE_SEARCH_WEB = "GOOGLE_SEARCH_WEB", e.GOOGLE_SEARCH_IMAGE = "GOOGLE_SEARCH_IMAGE", e.URL_CONTEXT = "URL_CONTEXT", e.GOOGLE_MAPS = "GOOGLE_MAPS", e.FILE_SEARCH = "FILE_SEARCH";
})(vE || (vE = {}));
var v1;
(function(e) {
  e.COLLECTION = "COLLECTION";
})(v1 || (v1 = {}));
var yE;
(function(e) {
  e.UNSPECIFIED = "unspecified", e.FLEX = "flex", e.STANDARD = "standard", e.PRIORITY = "priority";
})(yE || (yE = {}));
var _E;
(function(e) {
  e.FEATURE_SELECTION_PREFERENCE_UNSPECIFIED = "FEATURE_SELECTION_PREFERENCE_UNSPECIFIED", e.PRIORITIZE_QUALITY = "PRIORITIZE_QUALITY", e.BALANCED = "BALANCED", e.PRIORITIZE_COST = "PRIORITIZE_COST";
})(_E || (_E = {}));
var ym;
(function(e) {
  e.PREDICT = "PREDICT", e.EMBED_CONTENT = "EMBED_CONTENT";
})(ym || (ym = {}));
var bE;
(function(e) {
  e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_ONLY_HIGH = "BLOCK_ONLY_HIGH", e.BLOCK_NONE = "BLOCK_NONE";
})(bE || (bE = {}));
var wE;
(function(e) {
  e.auto = "auto", e.en = "en", e.ja = "ja", e.ko = "ko", e.hi = "hi", e.zh = "zh", e.pt = "pt", e.es = "es";
})(wE || (wE = {}));
var SE;
(function(e) {
  e.MASK_MODE_DEFAULT = "MASK_MODE_DEFAULT", e.MASK_MODE_USER_PROVIDED = "MASK_MODE_USER_PROVIDED", e.MASK_MODE_BACKGROUND = "MASK_MODE_BACKGROUND", e.MASK_MODE_FOREGROUND = "MASK_MODE_FOREGROUND", e.MASK_MODE_SEMANTIC = "MASK_MODE_SEMANTIC";
})(SE || (SE = {}));
var TE;
(function(e) {
  e.CONTROL_TYPE_DEFAULT = "CONTROL_TYPE_DEFAULT", e.CONTROL_TYPE_CANNY = "CONTROL_TYPE_CANNY", e.CONTROL_TYPE_SCRIBBLE = "CONTROL_TYPE_SCRIBBLE", e.CONTROL_TYPE_FACE_MESH = "CONTROL_TYPE_FACE_MESH";
})(TE || (TE = {}));
var EE;
(function(e) {
  e.SUBJECT_TYPE_DEFAULT = "SUBJECT_TYPE_DEFAULT", e.SUBJECT_TYPE_PERSON = "SUBJECT_TYPE_PERSON", e.SUBJECT_TYPE_ANIMAL = "SUBJECT_TYPE_ANIMAL", e.SUBJECT_TYPE_PRODUCT = "SUBJECT_TYPE_PRODUCT";
})(EE || (EE = {}));
var AE;
(function(e) {
  e.EDIT_MODE_DEFAULT = "EDIT_MODE_DEFAULT", e.EDIT_MODE_INPAINT_REMOVAL = "EDIT_MODE_INPAINT_REMOVAL", e.EDIT_MODE_INPAINT_INSERTION = "EDIT_MODE_INPAINT_INSERTION", e.EDIT_MODE_OUTPAINT = "EDIT_MODE_OUTPAINT", e.EDIT_MODE_CONTROLLED_EDITING = "EDIT_MODE_CONTROLLED_EDITING", e.EDIT_MODE_STYLE = "EDIT_MODE_STYLE", e.EDIT_MODE_BGSWAP = "EDIT_MODE_BGSWAP", e.EDIT_MODE_PRODUCT_IMAGE = "EDIT_MODE_PRODUCT_IMAGE";
})(AE || (AE = {}));
var CE;
(function(e) {
  e.FOREGROUND = "FOREGROUND", e.BACKGROUND = "BACKGROUND", e.PROMPT = "PROMPT", e.SEMANTIC = "SEMANTIC", e.INTERACTIVE = "INTERACTIVE";
})(CE || (CE = {}));
var kE;
(function(e) {
  e.ASSET = "ASSET", e.STYLE = "STYLE";
})(kE || (kE = {}));
var IE;
(function(e) {
  e.INSERT = "INSERT", e.REMOVE = "REMOVE", e.REMOVE_STATIC = "REMOVE_STATIC", e.OUTPAINT = "OUTPAINT";
})(IE || (IE = {}));
var xE;
(function(e) {
  e.OPTIMIZED = "OPTIMIZED", e.LOSSLESS = "LOSSLESS";
})(xE || (xE = {}));
var ME;
(function(e) {
  e.SUPERVISED_FINE_TUNING = "SUPERVISED_FINE_TUNING", e.PREFERENCE_TUNING = "PREFERENCE_TUNING", e.DISTILLATION = "DISTILLATION";
})(ME || (ME = {}));
var PE;
(function(e) {
  e.STATE_UNSPECIFIED = "STATE_UNSPECIFIED", e.PROCESSING = "PROCESSING", e.ACTIVE = "ACTIVE", e.FAILED = "FAILED";
})(PE || (PE = {}));
var RE;
(function(e) {
  e.SOURCE_UNSPECIFIED = "SOURCE_UNSPECIFIED", e.UPLOADED = "UPLOADED", e.GENERATED = "GENERATED", e.REGISTERED = "REGISTERED";
})(RE || (RE = {}));
var NE;
(function(e) {
  e.TURN_COMPLETE_REASON_UNSPECIFIED = "TURN_COMPLETE_REASON_UNSPECIFIED", e.MALFORMED_FUNCTION_CALL = "MALFORMED_FUNCTION_CALL", e.RESPONSE_REJECTED = "RESPONSE_REJECTED", e.NEED_MORE_INPUT = "NEED_MORE_INPUT", e.PROHIBITED_INPUT_CONTENT = "PROHIBITED_INPUT_CONTENT", e.IMAGE_PROHIBITED_INPUT_CONTENT = "IMAGE_PROHIBITED_INPUT_CONTENT", e.INPUT_TEXT_CONTAIN_PROMINENT_PERSON_PROHIBITED = "INPUT_TEXT_CONTAIN_PROMINENT_PERSON_PROHIBITED", e.INPUT_IMAGE_CELEBRITY = "INPUT_IMAGE_CELEBRITY", e.INPUT_IMAGE_PHOTO_REALISTIC_CHILD_PROHIBITED = "INPUT_IMAGE_PHOTO_REALISTIC_CHILD_PROHIBITED", e.INPUT_TEXT_NCII_PROHIBITED = "INPUT_TEXT_NCII_PROHIBITED", e.INPUT_OTHER = "INPUT_OTHER", e.INPUT_IP_PROHIBITED = "INPUT_IP_PROHIBITED", e.BLOCKLIST = "BLOCKLIST", e.UNSAFE_PROMPT_FOR_IMAGE_GENERATION = "UNSAFE_PROMPT_FOR_IMAGE_GENERATION", e.GENERATED_IMAGE_SAFETY = "GENERATED_IMAGE_SAFETY", e.GENERATED_CONTENT_SAFETY = "GENERATED_CONTENT_SAFETY", e.GENERATED_AUDIO_SAFETY = "GENERATED_AUDIO_SAFETY", e.GENERATED_VIDEO_SAFETY = "GENERATED_VIDEO_SAFETY", e.GENERATED_CONTENT_PROHIBITED = "GENERATED_CONTENT_PROHIBITED", e.GENERATED_CONTENT_BLOCKLIST = "GENERATED_CONTENT_BLOCKLIST", e.GENERATED_IMAGE_PROHIBITED = "GENERATED_IMAGE_PROHIBITED", e.GENERATED_IMAGE_CELEBRITY = "GENERATED_IMAGE_CELEBRITY", e.GENERATED_IMAGE_PROMINENT_PEOPLE_DETECTED_BY_REWRITER = "GENERATED_IMAGE_PROMINENT_PEOPLE_DETECTED_BY_REWRITER", e.GENERATED_IMAGE_IDENTIFIABLE_PEOPLE = "GENERATED_IMAGE_IDENTIFIABLE_PEOPLE", e.GENERATED_IMAGE_MINORS = "GENERATED_IMAGE_MINORS", e.OUTPUT_IMAGE_IP_PROHIBITED = "OUTPUT_IMAGE_IP_PROHIBITED", e.GENERATED_OTHER = "GENERATED_OTHER", e.MAX_REGENERATION_REACHED = "MAX_REGENERATION_REACHED";
})(NE || (NE = {}));
var $E;
(function(e) {
  e.MODALITY_UNSPECIFIED = "MODALITY_UNSPECIFIED", e.TEXT = "TEXT", e.IMAGE = "IMAGE", e.VIDEO = "VIDEO", e.AUDIO = "AUDIO", e.DOCUMENT = "DOCUMENT";
})($E || ($E = {}));
var DE;
(function(e) {
  e.VAD_SIGNAL_TYPE_UNSPECIFIED = "VAD_SIGNAL_TYPE_UNSPECIFIED", e.VAD_SIGNAL_TYPE_SOS = "VAD_SIGNAL_TYPE_SOS", e.VAD_SIGNAL_TYPE_EOS = "VAD_SIGNAL_TYPE_EOS";
})(DE || (DE = {}));
var LE;
(function(e) {
  e.TYPE_UNSPECIFIED = "TYPE_UNSPECIFIED", e.ACTIVITY_START = "ACTIVITY_START", e.ACTIVITY_END = "ACTIVITY_END";
})(LE || (LE = {}));
var OE;
(function(e) {
  e.START_SENSITIVITY_UNSPECIFIED = "START_SENSITIVITY_UNSPECIFIED", e.START_SENSITIVITY_HIGH = "START_SENSITIVITY_HIGH", e.START_SENSITIVITY_LOW = "START_SENSITIVITY_LOW";
})(OE || (OE = {}));
var FE;
(function(e) {
  e.END_SENSITIVITY_UNSPECIFIED = "END_SENSITIVITY_UNSPECIFIED", e.END_SENSITIVITY_HIGH = "END_SENSITIVITY_HIGH", e.END_SENSITIVITY_LOW = "END_SENSITIVITY_LOW";
})(FE || (FE = {}));
var BE;
(function(e) {
  e.ACTIVITY_HANDLING_UNSPECIFIED = "ACTIVITY_HANDLING_UNSPECIFIED", e.START_OF_ACTIVITY_INTERRUPTS = "START_OF_ACTIVITY_INTERRUPTS", e.NO_INTERRUPTION = "NO_INTERRUPTION";
})(BE || (BE = {}));
var UE;
(function(e) {
  e.TURN_COVERAGE_UNSPECIFIED = "TURN_COVERAGE_UNSPECIFIED", e.TURN_INCLUDES_ONLY_ACTIVITY = "TURN_INCLUDES_ONLY_ACTIVITY", e.TURN_INCLUDES_ALL_INPUT = "TURN_INCLUDES_ALL_INPUT", e.TURN_INCLUDES_AUDIO_ACTIVITY_AND_ALL_VIDEO = "TURN_INCLUDES_AUDIO_ACTIVITY_AND_ALL_VIDEO";
})(UE || (UE = {}));
var qE;
(function(e) {
  e.SCALE_UNSPECIFIED = "SCALE_UNSPECIFIED", e.C_MAJOR_A_MINOR = "C_MAJOR_A_MINOR", e.D_FLAT_MAJOR_B_FLAT_MINOR = "D_FLAT_MAJOR_B_FLAT_MINOR", e.D_MAJOR_B_MINOR = "D_MAJOR_B_MINOR", e.E_FLAT_MAJOR_C_MINOR = "E_FLAT_MAJOR_C_MINOR", e.E_MAJOR_D_FLAT_MINOR = "E_MAJOR_D_FLAT_MINOR", e.F_MAJOR_D_MINOR = "F_MAJOR_D_MINOR", e.G_FLAT_MAJOR_E_FLAT_MINOR = "G_FLAT_MAJOR_E_FLAT_MINOR", e.G_MAJOR_E_MINOR = "G_MAJOR_E_MINOR", e.A_FLAT_MAJOR_F_MINOR = "A_FLAT_MAJOR_F_MINOR", e.A_MAJOR_G_FLAT_MINOR = "A_MAJOR_G_FLAT_MINOR", e.B_FLAT_MAJOR_G_MINOR = "B_FLAT_MAJOR_G_MINOR", e.B_MAJOR_A_FLAT_MINOR = "B_MAJOR_A_FLAT_MINOR";
})(qE || (qE = {}));
var HE;
(function(e) {
  e.MUSIC_GENERATION_MODE_UNSPECIFIED = "MUSIC_GENERATION_MODE_UNSPECIFIED", e.QUALITY = "QUALITY", e.DIVERSITY = "DIVERSITY", e.VOCALIZATION = "VOCALIZATION";
})(HE || (HE = {}));
var Vl;
(function(e) {
  e.PLAYBACK_CONTROL_UNSPECIFIED = "PLAYBACK_CONTROL_UNSPECIFIED", e.PLAY = "PLAY", e.PAUSE = "PAUSE", e.STOP = "STOP", e.RESET_CONTEXT = "RESET_CONTEXT";
})(Vl || (Vl = {}));
var y1 = class {
  constructor(e) {
    const t = {};
    for (const n of e.headers.entries()) t[n[0]] = n[1];
    this.headers = t, this.responseInternal = e;
  }
  json() {
    return this.responseInternal.json();
  }
}, kc = class {
  get text() {
    var e, t, n, r, o, s, a, i;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning text from the first one.");
    let l = "", u = !1;
    const d = [];
    for (const f of (i = (a = (s = (o = this.candidates) === null || o === void 0 ? void 0 : o[0]) === null || s === void 0 ? void 0 : s.content) === null || a === void 0 ? void 0 : a.parts) !== null && i !== void 0 ? i : []) {
      for (const [m, v] of Object.entries(f)) m !== "text" && m !== "thought" && m !== "thoughtSignature" && (v !== null || v !== void 0) && d.push(m);
      if (typeof f.text == "string") {
        if (typeof f.thought == "boolean" && f.thought) continue;
        u = !0, l += f.text;
      }
    }
    return d.length > 0 && console.warn(`there are non-text parts ${d} in the response, returning concatenation of all text parts. Please refer to the non text parts for a full response from model.`), u ? l : void 0;
  }
  get data() {
    var e, t, n, r, o, s, a, i;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning data from the first one.");
    let l = "";
    const u = [];
    for (const d of (i = (a = (s = (o = this.candidates) === null || o === void 0 ? void 0 : o[0]) === null || s === void 0 ? void 0 : s.content) === null || a === void 0 ? void 0 : a.parts) !== null && i !== void 0 ? i : []) {
      for (const [f, m] of Object.entries(d)) f !== "inlineData" && (m !== null || m !== void 0) && u.push(f);
      d.inlineData && typeof d.inlineData.data == "string" && (l += atob(d.inlineData.data));
    }
    return u.length > 0 && console.warn(`there are non-data parts ${u} in the response, returning concatenation of all data parts. Please refer to the non data parts for a full response from model.`), l.length > 0 ? btoa(l) : void 0;
  }
  get functionCalls() {
    var e, t, n, r, o, s, a, i;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning function calls from the first one.");
    const l = (i = (a = (s = (o = this.candidates) === null || o === void 0 ? void 0 : o[0]) === null || s === void 0 ? void 0 : s.content) === null || a === void 0 ? void 0 : a.parts) === null || i === void 0 ? void 0 : i.filter((u) => u.functionCall).map((u) => u.functionCall).filter((u) => u !== void 0);
    if (l?.length !== 0)
      return l;
  }
  get executableCode() {
    var e, t, n, r, o, s, a, i, l;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning executable code from the first one.");
    const u = (i = (a = (s = (o = this.candidates) === null || o === void 0 ? void 0 : o[0]) === null || s === void 0 ? void 0 : s.content) === null || a === void 0 ? void 0 : a.parts) === null || i === void 0 ? void 0 : i.filter((d) => d.executableCode).map((d) => d.executableCode).filter((d) => d !== void 0);
    if (u?.length !== 0)
      return (l = u?.[0]) === null || l === void 0 ? void 0 : l.code;
  }
  get codeExecutionResult() {
    var e, t, n, r, o, s, a, i, l;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning code execution result from the first one.");
    const u = (i = (a = (s = (o = this.candidates) === null || o === void 0 ? void 0 : o[0]) === null || s === void 0 ? void 0 : s.content) === null || a === void 0 ? void 0 : a.parts) === null || i === void 0 ? void 0 : i.filter((d) => d.codeExecutionResult).map((d) => d.codeExecutionResult).filter((d) => d !== void 0);
    if (u?.length !== 0)
      return (l = u?.[0]) === null || l === void 0 ? void 0 : l.output;
  }
}, zE = class {
}, WE = class {
}, wq = class {
}, Sq = class {
}, Tq = class {
}, Eq = class {
}, GE = class {
}, VE = class {
}, KE = class {
}, Aq = class {
}, JE = class v3 {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new v3();
    let o;
    const s = t;
    return n ? o = uq(s) : o = lq(s), Object.assign(r, o), r;
  }
}, YE = class {
}, XE = class {
}, QE = class {
}, ZE = class {
}, Cq = class {
}, kq = class {
}, Iq = class {
}, xq = class y3 {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new y3(), o = gq(t);
    return Object.assign(r, o), r;
  }
}, Mq = class {
}, Pq = class {
}, Rq = class {
}, Nq = class {
}, jE = class {
}, $q = class {
  get text() {
    var e, t, n;
    let r = "", o = !1;
    const s = [];
    for (const a of (n = (t = (e = this.serverContent) === null || e === void 0 ? void 0 : e.modelTurn) === null || t === void 0 ? void 0 : t.parts) !== null && n !== void 0 ? n : []) {
      for (const [i, l] of Object.entries(a)) i !== "text" && i !== "thought" && l !== null && s.push(i);
      if (typeof a.text == "string") {
        if (typeof a.thought == "boolean" && a.thought) continue;
        o = !0, r += a.text;
      }
    }
    return s.length > 0 && console.warn(`there are non-text parts ${s} in the response, returning concatenation of all text parts. Please refer to the non text parts for a full response from model.`), o ? r : void 0;
  }
  get data() {
    var e, t, n;
    let r = "";
    const o = [];
    for (const s of (n = (t = (e = this.serverContent) === null || e === void 0 ? void 0 : e.modelTurn) === null || t === void 0 ? void 0 : t.parts) !== null && n !== void 0 ? n : []) {
      for (const [a, i] of Object.entries(s)) a !== "inlineData" && i !== null && o.push(a);
      s.inlineData && typeof s.inlineData.data == "string" && (r += atob(s.inlineData.data));
    }
    return o.length > 0 && console.warn(`there are non-data parts ${o} in the response, returning concatenation of all data parts. Please refer to the non data parts for a full response from model.`), r.length > 0 ? btoa(r) : void 0;
  }
}, Dq = class {
  get audioChunk() {
    if (this.serverContent && this.serverContent.audioChunks && this.serverContent.audioChunks.length > 0) return this.serverContent.audioChunks[0];
  }
}, Lq = class _3 {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new _3(), o = g3(t);
    return Object.assign(r, o), r;
  }
};
function Xt(e, t) {
  if (!t || typeof t != "string") throw new Error("model is required and must be a string");
  if (t.includes("..") || t.includes("?") || t.includes("&")) throw new Error("invalid model parameter");
  if (e.isVertexAI()) {
    if (t.startsWith("publishers/") || t.startsWith("projects/") || t.startsWith("models/")) return t;
    if (t.indexOf("/") >= 0) {
      const n = t.split("/", 2);
      return `publishers/${n[0]}/models/${n[1]}`;
    } else return `publishers/google/models/${t}`;
  } else return t.startsWith("models/") || t.startsWith("tunedModels/") ? t : `models/${t}`;
}
function b3(e, t) {
  const n = Xt(e, t);
  return n ? n.startsWith("publishers/") && e.isVertexAI() ? `projects/${e.getProject()}/locations/${e.getLocation()}/${n}` : n.startsWith("models/") && e.isVertexAI() ? `projects/${e.getProject()}/locations/${e.getLocation()}/publishers/google/${n}` : n : "";
}
function w3(e) {
  return Array.isArray(e) ? e.map((t) => _m(t)) : [_m(e)];
}
function _m(e) {
  if (typeof e == "object" && e !== null) return e;
  throw new Error(`Could not parse input as Blob. Unsupported blob type: ${typeof e}`);
}
function S3(e) {
  const t = _m(e);
  if (t.mimeType && t.mimeType.startsWith("image/")) return t;
  throw new Error(`Unsupported mime type: ${t.mimeType}`);
}
function T3(e) {
  const t = _m(e);
  if (t.mimeType && t.mimeType.startsWith("audio/")) return t;
  throw new Error(`Unsupported mime type: ${t.mimeType}`);
}
function eA(e) {
  if (e == null) throw new Error("PartUnion is required");
  if (typeof e == "object") return e;
  if (typeof e == "string") return { text: e };
  throw new Error(`Unsupported part type: ${typeof e}`);
}
function E3(e) {
  if (e == null || Array.isArray(e) && e.length === 0) throw new Error("PartListUnion is required");
  return Array.isArray(e) ? e.map((t) => eA(t)) : [eA(e)];
}
function _1(e) {
  return e != null && typeof e == "object" && "parts" in e && Array.isArray(e.parts);
}
function tA(e) {
  return e != null && typeof e == "object" && "functionCall" in e;
}
function nA(e) {
  return e != null && typeof e == "object" && "functionResponse" in e;
}
function zn(e) {
  if (e == null) throw new Error("ContentUnion is required");
  return _1(e) ? e : {
    role: "user",
    parts: E3(e)
  };
}
function Bb(e, t) {
  if (!t) return [];
  if (e.isVertexAI() && Array.isArray(t)) return t.flatMap((n) => {
    const r = zn(n);
    return r.parts && r.parts.length > 0 && r.parts[0].text !== void 0 ? [r.parts[0].text] : [];
  });
  if (e.isVertexAI()) {
    const n = zn(t);
    return n.parts && n.parts.length > 0 && n.parts[0].text !== void 0 ? [n.parts[0].text] : [];
  }
  return Array.isArray(t) ? t.map((n) => zn(n)) : [zn(t)];
}
function kr(e) {
  if (e == null || Array.isArray(e) && e.length === 0) throw new Error("contents are required");
  if (!Array.isArray(e)) {
    if (tA(e) || nA(e)) throw new Error("To specify functionCall or functionResponse parts, please wrap them in a Content object, specifying the role for them");
    return [zn(e)];
  }
  const t = [], n = [], r = _1(e[0]);
  for (const o of e) {
    const s = _1(o);
    if (s != r) throw new Error("Mixing Content and Parts is not supported, please group the parts into a the appropriate Content objects and specify the roles for them");
    if (s) t.push(o);
    else {
      if (tA(o) || nA(o)) throw new Error("To specify functionCall or functionResponse parts, please wrap them, and any other parts, in Content objects as appropriate, specifying the role for them");
      n.push(o);
    }
  }
  return r || t.push({
    role: "user",
    parts: E3(n)
  }), t;
}
function Oq(e, t) {
  e.includes("null") && (t.nullable = !0);
  const n = e.filter((r) => r !== "null");
  if (n.length === 1) t.type = Object.values(Pa).includes(n[0].toUpperCase()) ? n[0].toUpperCase() : Pa.TYPE_UNSPECIFIED;
  else {
    t.anyOf = [];
    for (const r of n) t.anyOf.push({ type: Object.values(Pa).includes(r.toUpperCase()) ? r.toUpperCase() : Pa.TYPE_UNSPECIFIED });
  }
}
function su(e) {
  const t = {}, n = ["items"], r = ["anyOf"], o = ["properties"];
  if (e.type && e.anyOf) throw new Error("type and anyOf cannot be both populated.");
  const s = e.anyOf;
  s != null && s.length == 2 && (s[0].type === "null" ? (t.nullable = !0, e = s[1]) : s[1].type === "null" && (t.nullable = !0, e = s[0])), e.type instanceof Array && Oq(e.type, t);
  for (const [a, i] of Object.entries(e))
    if (i != null)
      if (a == "type") {
        if (i === "null") throw new Error("type: null can not be the only possible type for the field.");
        if (i instanceof Array) continue;
        t.type = Object.values(Pa).includes(i.toUpperCase()) ? i.toUpperCase() : Pa.TYPE_UNSPECIFIED;
      } else if (n.includes(a)) t[a] = su(i);
      else if (r.includes(a)) {
        const l = [];
        for (const u of i) {
          if (u.type == "null") {
            t.nullable = !0;
            continue;
          }
          l.push(su(u));
        }
        t[a] = l;
      } else if (o.includes(a)) {
        const l = {};
        for (const [u, d] of Object.entries(i)) l[u] = su(d);
        t[a] = l;
      } else {
        if (a === "additionalProperties") continue;
        t[a] = i;
      }
  return t;
}
function Ub(e) {
  return su(e);
}
function qb(e) {
  if (typeof e == "object") return e;
  if (typeof e == "string") return { voiceConfig: { prebuiltVoiceConfig: { voiceName: e } } };
  throw new Error(`Unsupported speechConfig type: ${typeof e}`);
}
function Hb(e) {
  if ("multiSpeakerVoiceConfig" in e) throw new Error("multiSpeakerVoiceConfig is not supported in the live API.");
  return e;
}
function Du(e) {
  if (e.functionDeclarations) for (const t of e.functionDeclarations)
    t.parameters && (Object.keys(t.parameters).includes("$schema") ? t.parametersJsonSchema || (t.parametersJsonSchema = t.parameters, delete t.parameters) : t.parameters = su(t.parameters)), t.response && (Object.keys(t.response).includes("$schema") ? t.responseJsonSchema || (t.responseJsonSchema = t.response, delete t.response) : t.response = su(t.response));
  return e;
}
function Lu(e) {
  if (e == null) throw new Error("tools is required");
  if (!Array.isArray(e)) throw new Error("tools is required and must be an array of Tools");
  const t = [];
  for (const n of e) t.push(n);
  return t;
}
function Fq(e, t, n, r = 1) {
  const o = !t.startsWith(`${n}/`) && t.split("/").length === r;
  return e.isVertexAI() ? t.startsWith("projects/") ? t : t.startsWith("locations/") ? `projects/${e.getProject()}/${t}` : t.startsWith(`${n}/`) ? `projects/${e.getProject()}/locations/${e.getLocation()}/${t}` : o ? `projects/${e.getProject()}/locations/${e.getLocation()}/${n}/${t}` : t : o ? `${n}/${t}` : t;
}
function na(e, t) {
  if (typeof t != "string") throw new Error("name must be a string");
  return Fq(e, t, "cachedContents");
}
function A3(e) {
  switch (e) {
    case "STATE_UNSPECIFIED":
      return "JOB_STATE_UNSPECIFIED";
    case "CREATING":
      return "JOB_STATE_RUNNING";
    case "ACTIVE":
      return "JOB_STATE_SUCCEEDED";
    case "FAILED":
      return "JOB_STATE_FAILED";
    default:
      return e;
  }
}
function Ga(e) {
  return Fb(e);
}
function Bq(e) {
  return e != null && typeof e == "object" && "name" in e;
}
function Uq(e) {
  return e != null && typeof e == "object" && "video" in e;
}
function qq(e) {
  return e != null && typeof e == "object" && "uri" in e;
}
function C3(e) {
  var t;
  let n;
  if (Bq(e) && (n = e.name), !(qq(e) && (n = e.uri, n === void 0)) && !(Uq(e) && (n = (t = e.video) === null || t === void 0 ? void 0 : t.uri, n === void 0))) {
    if (typeof e == "string" && (n = e), n === void 0) throw new Error("Could not extract file name from the provided input.");
    if (n.startsWith("https://")) {
      const r = n.split("files/")[1].match(/[a-z0-9]+/);
      if (r === null) throw new Error(`Could not extract file name from URI ${n}`);
      n = r[0];
    } else n.startsWith("files/") && (n = n.split("files/")[1]);
    return n;
  }
}
function k3(e, t) {
  let n;
  return e.isVertexAI() ? n = t ? "publishers/google/models" : "models" : n = t ? "models" : "tunedModels", n;
}
function I3(e) {
  for (const t of [
    "models",
    "tunedModels",
    "publisherModels"
  ]) if (Hq(e, t)) return e[t];
  return [];
}
function Hq(e, t) {
  return e !== null && typeof e == "object" && t in e;
}
function zq(e, t = {}) {
  const n = e, r = {
    name: n.name,
    description: n.description,
    parametersJsonSchema: n.inputSchema
  };
  return n.outputSchema && (r.responseJsonSchema = n.outputSchema), t.behavior && (r.behavior = t.behavior), { functionDeclarations: [r] };
}
function Wq(e, t = {}) {
  const n = [], r = /* @__PURE__ */ new Set();
  for (const o of e) {
    const s = o.name;
    if (r.has(s)) throw new Error(`Duplicate function name ${s} found in MCP tools. Please ensure function names are unique.`);
    r.add(s);
    const a = zq(o, t);
    a.functionDeclarations && n.push(...a.functionDeclarations);
  }
  return { functionDeclarations: n };
}
function x3(e, t) {
  let n;
  if (typeof t == "string") if (e.isVertexAI()) if (t.startsWith("gs://")) n = {
    format: "jsonl",
    gcsUri: [t]
  };
  else if (t.startsWith("bq://")) n = {
    format: "bigquery",
    bigqueryUri: t
  };
  else throw new Error(`Unsupported string source for Vertex AI: ${t}`);
  else if (t.startsWith("files/")) n = { fileName: t };
  else throw new Error(`Unsupported string source for Gemini API: ${t}`);
  else if (Array.isArray(t)) {
    if (e.isVertexAI()) throw new Error("InlinedRequest[] is not supported in Vertex AI.");
    n = { inlinedRequests: t };
  } else n = t;
  const r = [n.gcsUri, n.bigqueryUri].filter(Boolean).length, o = [n.inlinedRequests, n.fileName].filter(Boolean).length;
  if (e.isVertexAI()) {
    if (o > 0 || r !== 1) throw new Error("Exactly one of `gcsUri` or `bigqueryUri` must be set for Vertex AI.");
  } else if (r > 0 || o !== 1) throw new Error("Exactly one of `inlinedRequests`, `fileName`, must be set for Gemini API.");
  return n;
}
function Gq(e) {
  if (typeof e != "string") return e;
  const t = e;
  if (t.startsWith("gs://")) return {
    format: "jsonl",
    gcsUri: t
  };
  if (t.startsWith("bq://")) return {
    format: "bigquery",
    bigqueryUri: t
  };
  throw new Error(`Unsupported destination: ${t}`);
}
function M3(e) {
  if (typeof e != "object" || e === null) return {};
  const t = e, n = t.inlinedResponses;
  if (typeof n != "object" || n === null) return e;
  const r = n.inlinedResponses;
  if (!Array.isArray(r) || r.length === 0) return e;
  let o = !1;
  for (const s of r) {
    if (typeof s != "object" || s === null) continue;
    const a = s.response;
    if (!(typeof a != "object" || a === null) && a.embedding !== void 0) {
      o = !0;
      break;
    }
  }
  return o && (t.inlinedEmbedContentResponses = t.inlinedResponses, delete t.inlinedResponses), e;
}
function Ou(e, t) {
  const n = t;
  if (!e.isVertexAI()) {
    if (/batches\/[^/]+$/.test(n)) return n.split("/").pop();
    throw new Error(`Invalid batch job name: ${n}.`);
  }
  if (/^projects\/[^/]+\/locations\/[^/]+\/batchPredictionJobs\/[^/]+$/.test(n)) return n.split("/").pop();
  if (/^\d+$/.test(n)) return n;
  throw new Error(`Invalid batch job name: ${n}.`);
}
function P3(e) {
  const t = e;
  return t === "BATCH_STATE_UNSPECIFIED" ? "JOB_STATE_UNSPECIFIED" : t === "BATCH_STATE_PENDING" ? "JOB_STATE_PENDING" : t === "BATCH_STATE_RUNNING" ? "JOB_STATE_RUNNING" : t === "BATCH_STATE_SUCCEEDED" ? "JOB_STATE_SUCCEEDED" : t === "BATCH_STATE_FAILED" ? "JOB_STATE_FAILED" : t === "BATCH_STATE_CANCELLED" ? "JOB_STATE_CANCELLED" : t === "BATCH_STATE_EXPIRED" ? "JOB_STATE_EXPIRED" : t;
}
function Vq(e) {
  return e.includes("gemini") && e !== "gemini-embedding-001" || e.includes("maas");
}
function Kq(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && h(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function Jq(e) {
  const t = {}, n = c(e, ["responsesFile"]);
  n != null && h(t, ["fileName"], n);
  const r = c(e, ["inlinedResponses", "inlinedResponses"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => IH(a))), h(t, ["inlinedResponses"], s);
  }
  const o = c(e, ["inlinedEmbedContentResponses", "inlinedResponses"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => a)), h(t, ["inlinedEmbedContentResponses"], s);
  }
  return t;
}
function Yq(e) {
  const t = {}, n = c(e, ["predictionsFormat"]);
  n != null && h(t, ["format"], n);
  const r = c(e, ["gcsDestination", "outputUriPrefix"]);
  r != null && h(t, ["gcsUri"], r);
  const o = c(e, ["bigqueryDestination", "outputUri"]);
  return o != null && h(t, ["bigqueryUri"], o), t;
}
function Xq(e) {
  const t = {}, n = c(e, ["format"]);
  n != null && h(t, ["predictionsFormat"], n);
  const r = c(e, ["gcsUri"]);
  r != null && h(t, ["gcsDestination", "outputUriPrefix"], r);
  const o = c(e, ["bigqueryUri"]);
  if (o != null && h(t, ["bigqueryDestination", "outputUri"], o), c(e, ["fileName"]) !== void 0) throw new Error("fileName parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedResponses"]) !== void 0) throw new Error("inlinedResponses parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedEmbedContentResponses"]) !== void 0) throw new Error("inlinedEmbedContentResponses parameter is not supported in Vertex AI.");
  return t;
}
function fp(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata", "displayName"]);
  r != null && h(t, ["displayName"], r);
  const o = c(e, ["metadata", "state"]);
  o != null && h(t, ["state"], P3(o));
  const s = c(e, ["metadata", "createTime"]);
  s != null && h(t, ["createTime"], s);
  const a = c(e, ["metadata", "endTime"]);
  a != null && h(t, ["endTime"], a);
  const i = c(e, ["metadata", "updateTime"]);
  i != null && h(t, ["updateTime"], i);
  const l = c(e, ["metadata", "model"]);
  l != null && h(t, ["model"], l);
  const u = c(e, ["metadata", "output"]);
  return u != null && h(t, ["dest"], Jq(M3(u))), t;
}
function b1(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["displayName"]);
  r != null && h(t, ["displayName"], r);
  const o = c(e, ["state"]);
  o != null && h(t, ["state"], P3(o));
  const s = c(e, ["error"]);
  s != null && h(t, ["error"], s);
  const a = c(e, ["createTime"]);
  a != null && h(t, ["createTime"], a);
  const i = c(e, ["startTime"]);
  i != null && h(t, ["startTime"], i);
  const l = c(e, ["endTime"]);
  l != null && h(t, ["endTime"], l);
  const u = c(e, ["updateTime"]);
  u != null && h(t, ["updateTime"], u);
  const d = c(e, ["model"]);
  d != null && h(t, ["model"], d);
  const f = c(e, ["inputConfig"]);
  f != null && h(t, ["src"], Qq(f));
  const m = c(e, ["outputConfig"]);
  m != null && h(t, ["dest"], Yq(M3(m)));
  const v = c(e, ["completionStats"]);
  return v != null && h(t, ["completionStats"], v), t;
}
function Qq(e) {
  const t = {}, n = c(e, ["instancesFormat"]);
  n != null && h(t, ["format"], n);
  const r = c(e, ["gcsSource", "uris"]);
  r != null && h(t, ["gcsUri"], r);
  const o = c(e, ["bigquerySource", "inputUri"]);
  return o != null && h(t, ["bigqueryUri"], o), t;
}
function Zq(e, t) {
  const n = {};
  if (c(t, ["format"]) !== void 0) throw new Error("format parameter is not supported in Gemini API.");
  if (c(t, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  if (c(t, ["bigqueryUri"]) !== void 0) throw new Error("bigqueryUri parameter is not supported in Gemini API.");
  const r = c(t, ["fileName"]);
  r != null && h(n, ["fileName"], r);
  const o = c(t, ["inlinedRequests"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => kH(e, a))), h(n, ["requests", "requests"], s);
  }
  return n;
}
function jq(e) {
  const t = {}, n = c(e, ["format"]);
  n != null && h(t, ["instancesFormat"], n);
  const r = c(e, ["gcsUri"]);
  r != null && h(t, ["gcsSource", "uris"], r);
  const o = c(e, ["bigqueryUri"]);
  if (o != null && h(t, ["bigquerySource", "inputUri"], o), c(e, ["fileName"]) !== void 0) throw new Error("fileName parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedRequests"]) !== void 0) throw new Error("inlinedRequests parameter is not supported in Vertex AI.");
  return t;
}
function eH(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && h(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function tH(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ou(e, r)), n;
}
function nH(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ou(e, r)), n;
}
function rH(e) {
  const t = {}, n = c(e, ["content"]);
  n != null && h(t, ["content"], n);
  const r = c(e, ["citationMetadata"]);
  r != null && h(t, ["citationMetadata"], oH(r));
  const o = c(e, ["tokenCount"]);
  o != null && h(t, ["tokenCount"], o);
  const s = c(e, ["finishReason"]);
  s != null && h(t, ["finishReason"], s);
  const a = c(e, ["groundingMetadata"]);
  a != null && h(t, ["groundingMetadata"], a);
  const i = c(e, ["avgLogprobs"]);
  i != null && h(t, ["avgLogprobs"], i);
  const l = c(e, ["index"]);
  l != null && h(t, ["index"], l);
  const u = c(e, ["logprobsResult"]);
  u != null && h(t, ["logprobsResult"], u);
  const d = c(e, ["safetyRatings"]);
  if (d != null) {
    let m = d;
    Array.isArray(m) && (m = m.map((v) => v)), h(t, ["safetyRatings"], m);
  }
  const f = c(e, ["urlContextMetadata"]);
  return f != null && h(t, ["urlContextMetadata"], f), t;
}
function oH(e) {
  const t = {}, n = c(e, ["citationSources"]);
  if (n != null) {
    let r = n;
    Array.isArray(r) && (r = r.map((o) => o)), h(t, ["citations"], r);
  }
  return t;
}
function R3(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let o = n;
    Array.isArray(o) && (o = o.map((s) => DH(s))), h(t, ["parts"], o);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function sH(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  if (t !== void 0 && r != null && h(t, ["batch", "displayName"], r), c(e, ["dest"]) !== void 0) throw new Error("dest parameter is not supported in Gemini API.");
  const o = c(e, ["webhookConfig"]);
  return t !== void 0 && o != null && h(t, ["batch", "webhookConfig"], o), n;
}
function aH(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  t !== void 0 && r != null && h(t, ["displayName"], r);
  const o = c(e, ["dest"]);
  if (t !== void 0 && o != null && h(t, ["outputConfig"], Xq(Gq(o))), c(e, ["webhookConfig"]) !== void 0) throw new Error("webhookConfig parameter is not supported in Vertex AI.");
  return n;
}
function rA(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["_url", "model"], Xt(e, r));
  const o = c(t, ["src"]);
  o != null && h(n, ["batch", "inputConfig"], Zq(e, x3(e, o)));
  const s = c(t, ["config"]);
  return s != null && sH(s, n), n;
}
function iH(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["model"], Xt(e, r));
  const o = c(t, ["src"]);
  o != null && h(n, ["inputConfig"], jq(x3(e, o)));
  const s = c(t, ["config"]);
  return s != null && aH(s, n), n;
}
function lH(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  return t !== void 0 && r != null && h(t, ["batch", "displayName"], r), n;
}
function uH(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["_url", "model"], Xt(e, r));
  const o = c(t, ["src"]);
  o != null && h(n, ["batch", "inputConfig"], gH(e, o));
  const s = c(t, ["config"]);
  return s != null && lH(s, n), n;
}
function cH(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ou(e, r)), n;
}
function dH(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ou(e, r)), n;
}
function fH(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["name"]);
  r != null && h(t, ["name"], r);
  const o = c(e, ["done"]);
  o != null && h(t, ["done"], o);
  const s = c(e, ["error"]);
  return s != null && h(t, ["error"], s), t;
}
function hH(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["name"]);
  r != null && h(t, ["name"], r);
  const o = c(e, ["done"]);
  o != null && h(t, ["done"], o);
  const s = c(e, ["error"]);
  return s != null && h(t, ["error"], s), t;
}
function pH(e, t) {
  const n = {}, r = c(t, ["contents"]);
  if (r != null) {
    let s = Bb(e, r);
    Array.isArray(s) && (s = s.map((a) => a)), h(n, [
      "requests[]",
      "request",
      "content"
    ], s);
  }
  const o = c(t, ["config"]);
  return o != null && (h(n, ["_self"], mH(o, n)), aq(n, { "requests[].*": "requests[].request.*" })), n;
}
function mH(e, t) {
  const n = {}, r = c(e, ["taskType"]);
  t !== void 0 && r != null && h(t, ["requests[]", "taskType"], r);
  const o = c(e, ["title"]);
  t !== void 0 && o != null && h(t, ["requests[]", "title"], o);
  const s = c(e, ["outputDimensionality"]);
  if (t !== void 0 && s != null && h(t, ["requests[]", "outputDimensionality"], s), c(e, ["mimeType"]) !== void 0) throw new Error("mimeType parameter is not supported in Gemini API.");
  if (c(e, ["autoTruncate"]) !== void 0) throw new Error("autoTruncate parameter is not supported in Gemini API.");
  if (c(e, ["documentOcr"]) !== void 0) throw new Error("documentOcr parameter is not supported in Gemini API.");
  if (c(e, ["audioTrackExtraction"]) !== void 0) throw new Error("audioTrackExtraction parameter is not supported in Gemini API.");
  return n;
}
function gH(e, t) {
  const n = {}, r = c(t, ["fileName"]);
  r != null && h(n, ["file_name"], r);
  const o = c(t, ["inlinedRequests"]);
  return o != null && h(n, ["requests"], pH(e, o)), n;
}
function vH(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && h(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function yH(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && h(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && h(t, ["args"], r);
  const o = c(e, ["name"]);
  if (o != null && h(t, ["name"], o), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function _H(e) {
  const t = {}, n = c(e, ["allowedFunctionNames"]);
  n != null && h(t, ["allowedFunctionNames"], n);
  const r = c(e, ["mode"]);
  if (r != null && h(t, ["mode"], r), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return t;
}
function bH(e, t, n) {
  const r = {}, o = c(t, ["systemInstruction"]);
  n !== void 0 && o != null && h(n, ["systemInstruction"], R3(zn(o)));
  const s = c(t, ["temperature"]);
  s != null && h(r, ["temperature"], s);
  const a = c(t, ["topP"]);
  a != null && h(r, ["topP"], a);
  const i = c(t, ["topK"]);
  i != null && h(r, ["topK"], i);
  const l = c(t, ["candidateCount"]);
  l != null && h(r, ["candidateCount"], l);
  const u = c(t, ["maxOutputTokens"]);
  u != null && h(r, ["maxOutputTokens"], u);
  const d = c(t, ["stopSequences"]);
  d != null && h(r, ["stopSequences"], d);
  const f = c(t, ["responseLogprobs"]);
  f != null && h(r, ["responseLogprobs"], f);
  const m = c(t, ["logprobs"]);
  m != null && h(r, ["logprobs"], m);
  const v = c(t, ["presencePenalty"]);
  v != null && h(r, ["presencePenalty"], v);
  const y = c(t, ["frequencyPenalty"]);
  y != null && h(r, ["frequencyPenalty"], y);
  const T = c(t, ["seed"]);
  T != null && h(r, ["seed"], T);
  const C = c(t, ["responseMimeType"]);
  C != null && h(r, ["responseMimeType"], C);
  const A = c(t, ["responseSchema"]);
  A != null && h(r, ["responseSchema"], Ub(A));
  const I = c(t, ["responseJsonSchema"]);
  if (I != null && h(r, ["responseJsonSchema"], I), c(t, ["routingConfig"]) !== void 0) throw new Error("routingConfig parameter is not supported in Gemini API.");
  if (c(t, ["modelSelectionConfig"]) !== void 0) throw new Error("modelSelectionConfig parameter is not supported in Gemini API.");
  const R = c(t, ["safetySettings"]);
  if (n !== void 0 && R != null) {
    let J = R;
    Array.isArray(J) && (J = J.map((G) => LH(G))), h(n, ["safetySettings"], J);
  }
  const $ = c(t, ["tools"]);
  if (n !== void 0 && $ != null) {
    let J = Lu($);
    Array.isArray(J) && (J = J.map((G) => FH(Du(G)))), h(n, ["tools"], J);
  }
  const p = c(t, ["toolConfig"]);
  if (n !== void 0 && p != null && h(n, ["toolConfig"], OH(p)), c(t, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const b = c(t, ["cachedContent"]);
  n !== void 0 && b != null && h(n, ["cachedContent"], na(e, b));
  const w = c(t, ["responseModalities"]);
  w != null && h(r, ["responseModalities"], w);
  const E = c(t, ["mediaResolution"]);
  E != null && h(r, ["mediaResolution"], E);
  const x = c(t, ["speechConfig"]);
  if (x != null && h(r, ["speechConfig"], qb(x)), c(t, ["audioTimestamp"]) !== void 0) throw new Error("audioTimestamp parameter is not supported in Gemini API.");
  const M = c(t, ["thinkingConfig"]);
  M != null && h(r, ["thinkingConfig"], M);
  const N = c(t, ["imageConfig"]);
  N != null && h(r, ["imageConfig"], CH(N));
  const q = c(t, ["enableEnhancedCivicAnswers"]);
  if (q != null && h(r, ["enableEnhancedCivicAnswers"], q), c(t, ["modelArmorConfig"]) !== void 0) throw new Error("modelArmorConfig parameter is not supported in Gemini API.");
  const z = c(t, ["serviceTier"]);
  return n !== void 0 && z != null && h(n, ["serviceTier"], z), r;
}
function wH(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["candidates"]);
  if (r != null) {
    let u = r;
    Array.isArray(u) && (u = u.map((d) => rH(d))), h(t, ["candidates"], u);
  }
  const o = c(e, ["modelVersion"]);
  o != null && h(t, ["modelVersion"], o);
  const s = c(e, ["promptFeedback"]);
  s != null && h(t, ["promptFeedback"], s);
  const a = c(e, ["responseId"]);
  a != null && h(t, ["responseId"], a);
  const i = c(e, ["usageMetadata"]);
  i != null && h(t, ["usageMetadata"], i);
  const l = c(e, ["modelStatus"]);
  return l != null && h(t, ["modelStatus"], l), t;
}
function SH(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ou(e, r)), n;
}
function TH(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ou(e, r)), n;
}
function EH(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && h(t, ["authConfig"], Kq(n));
  const r = c(e, ["enableWidget"]);
  return r != null && h(t, ["enableWidget"], r), t;
}
function AH(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && h(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && h(t, ["timeRangeFilter"], r), t;
}
function CH(e) {
  const t = {}, n = c(e, ["aspectRatio"]);
  n != null && h(t, ["aspectRatio"], n);
  const r = c(e, ["imageSize"]);
  if (r != null && h(t, ["imageSize"], r), c(e, ["personGeneration"]) !== void 0) throw new Error("personGeneration parameter is not supported in Gemini API.");
  if (c(e, ["prominentPeople"]) !== void 0) throw new Error("prominentPeople parameter is not supported in Gemini API.");
  if (c(e, ["outputMimeType"]) !== void 0) throw new Error("outputMimeType parameter is not supported in Gemini API.");
  if (c(e, ["outputCompressionQuality"]) !== void 0) throw new Error("outputCompressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["imageOutputOptions"]) !== void 0) throw new Error("imageOutputOptions parameter is not supported in Gemini API.");
  return t;
}
function kH(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["request", "model"], Xt(e, r));
  const o = c(t, ["contents"]);
  if (o != null) {
    let i = kr(o);
    Array.isArray(i) && (i = i.map((l) => R3(l))), h(n, ["request", "contents"], i);
  }
  const s = c(t, ["metadata"]);
  s != null && h(n, ["metadata"], s);
  const a = c(t, ["config"]);
  return a != null && h(n, ["request", "generationConfig"], bH(e, a, c(n, ["request"], {}))), n;
}
function IH(e) {
  const t = {}, n = c(e, ["response"]);
  n != null && h(t, ["response"], wH(n));
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const o = c(e, ["error"]);
  return o != null && h(t, ["error"], o), t;
}
function xH(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const o = c(e, ["pageToken"]);
  if (t !== void 0 && o != null && h(t, ["_query", "pageToken"], o), c(e, ["filter"]) !== void 0) throw new Error("filter parameter is not supported in Gemini API.");
  return n;
}
function MH(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const o = c(e, ["pageToken"]);
  t !== void 0 && o != null && h(t, ["_query", "pageToken"], o);
  const s = c(e, ["filter"]);
  return t !== void 0 && s != null && h(t, ["_query", "filter"], s), n;
}
function PH(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && xH(n, t), t;
}
function RH(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && MH(n, t), t;
}
function NH(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const o = c(e, ["operations"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => fp(a))), h(t, ["batchJobs"], s);
  }
  return t;
}
function $H(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const o = c(e, ["batchPredictionJobs"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => b1(a))), h(t, ["batchJobs"], s);
  }
  return t;
}
function DH(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const o = c(e, ["executableCode"]);
  o != null && h(t, ["executableCode"], o);
  const s = c(e, ["fileData"]);
  s != null && h(t, ["fileData"], vH(s));
  const a = c(e, ["functionCall"]);
  a != null && h(t, ["functionCall"], yH(a));
  const i = c(e, ["functionResponse"]);
  i != null && h(t, ["functionResponse"], i);
  const l = c(e, ["inlineData"]);
  l != null && h(t, ["inlineData"], eH(l));
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  m != null && h(t, ["videoMetadata"], m);
  const v = c(e, ["toolCall"]);
  v != null && h(t, ["toolCall"], v);
  const y = c(e, ["toolResponse"]);
  y != null && h(t, ["toolResponse"], y);
  const T = c(e, ["partMetadata"]);
  return T != null && h(t, ["partMetadata"], T), t;
}
function LH(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && h(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && h(t, ["threshold"], r), t;
}
function OH(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && h(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  r != null && h(t, ["functionCallingConfig"], _H(r));
  const o = c(e, ["includeServerSideToolInvocations"]);
  return o != null && h(t, ["includeServerSideToolInvocations"], o), t;
}
function FH(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && h(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && h(t, ["fileSearch"], r);
  const o = c(e, ["googleSearch"]);
  o != null && h(t, ["googleSearch"], AH(o));
  const s = c(e, ["googleMaps"]);
  s != null && h(t, ["googleMaps"], EH(s));
  const a = c(e, ["codeExecution"]);
  if (a != null && h(t, ["codeExecution"], a), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const i = c(e, ["functionDeclarations"]);
  if (i != null) {
    let f = i;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["functionDeclarations"], f);
  }
  const l = c(e, ["googleSearchRetrieval"]);
  if (l != null && h(t, ["googleSearchRetrieval"], l), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && h(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let f = d;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["mcpServers"], f);
  }
  return t;
}
var js;
(function(e) {
  e.PAGED_ITEM_BATCH_JOBS = "batchJobs", e.PAGED_ITEM_MODELS = "models", e.PAGED_ITEM_TUNING_JOBS = "tuningJobs", e.PAGED_ITEM_FILES = "files", e.PAGED_ITEM_CACHED_CONTENTS = "cachedContents", e.PAGED_ITEM_FILE_SEARCH_STORES = "fileSearchStores", e.PAGED_ITEM_DOCUMENTS = "documents";
})(js || (js = {}));
var tl = class {
  constructor(e, t, n, r) {
    this.pageInternal = [], this.paramsInternal = {}, this.requestInternal = t, this.init(e, n, r);
  }
  init(e, t, n) {
    var r, o;
    this.nameInternal = e, this.pageInternal = t[this.nameInternal] || [], this.sdkHttpResponseInternal = t?.sdkHttpResponse, this.idxInternal = 0;
    let s = { config: {} };
    !n || Object.keys(n).length === 0 ? s = { config: {} } : typeof n == "object" ? s = Object.assign({}, n) : s = n, s.config && (s.config.pageToken = t.nextPageToken), this.paramsInternal = s, this.pageInternalSize = (o = (r = s.config) === null || r === void 0 ? void 0 : r.pageSize) !== null && o !== void 0 ? o : this.pageInternal.length;
  }
  initNextPage(e) {
    this.init(this.nameInternal, e, this.paramsInternal);
  }
  get page() {
    return this.pageInternal;
  }
  get name() {
    return this.nameInternal;
  }
  get pageSize() {
    return this.pageInternalSize;
  }
  get sdkHttpResponse() {
    return this.sdkHttpResponseInternal;
  }
  get params() {
    return this.paramsInternal;
  }
  get pageLength() {
    return this.pageInternal.length;
  }
  getItem(e) {
    return this.pageInternal[e];
  }
  [Symbol.asyncIterator]() {
    return {
      next: async () => {
        if (this.idxInternal >= this.pageLength) if (this.hasNextPage()) await this.nextPage();
        else return {
          value: void 0,
          done: !0
        };
        const e = this.getItem(this.idxInternal);
        return this.idxInternal += 1, {
          value: e,
          done: !1
        };
      },
      return: async () => ({
        value: void 0,
        done: !0
      })
    };
  }
  async nextPage() {
    if (!this.hasNextPage()) throw new Error("No more pages to fetch.");
    const e = await this.requestInternal(this.params);
    return this.initNextPage(e), this.page;
  }
  hasNextPage() {
    var e;
    return ((e = this.params.config) === null || e === void 0 ? void 0 : e.pageToken) !== void 0;
  }
}, BH = class extends ta {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new tl(js.PAGED_ITEM_BATCH_JOBS, (n) => this.listInternal(n), await this.listInternal(t), t), this.create = async (t) => (this.apiClient.isVertexAI() && (t.config = this.formatDestination(t.src, t.config)), this.createInternal(t)), this.createEmbeddings = async (t) => {
      if (console.warn("batches.createEmbeddings() is experimental and may change without notice."), this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support batches.createEmbeddings.");
      return this.createEmbeddingsInternal(t);
    };
  }
  createInlinedGenerateContentRequest(e) {
    const t = rA(this.apiClient, e), n = t._url, r = nt("{model}:batchGenerateContent", n), o = t.batch.inputConfig.requests, s = o.requests, a = [];
    for (const i of s) {
      const l = Object.assign({}, i);
      if (l.systemInstruction) {
        const u = l.systemInstruction;
        delete l.systemInstruction;
        const d = l.request;
        d.systemInstruction = u, l.request = d;
      }
      a.push(l);
    }
    return o.requests = a, delete t.config, delete t._url, delete t._query, {
      path: r,
      body: t
    };
  }
  getGcsUri(e) {
    if (typeof e == "string") return e.startsWith("gs://") ? e : void 0;
    if (!Array.isArray(e) && e.gcsUri && e.gcsUri.length > 0) return e.gcsUri[0];
  }
  getBigqueryUri(e) {
    if (typeof e == "string") return e.startsWith("bq://") ? e : void 0;
    if (!Array.isArray(e)) return e.bigqueryUri;
  }
  formatDestination(e, t) {
    const n = t ? Object.assign({}, t) : {}, r = Date.now().toString();
    if (n.displayName || (n.displayName = `genaiBatchJob_${r}`), n.dest === void 0) {
      const o = this.getGcsUri(e), s = this.getBigqueryUri(e);
      if (o) o.endsWith(".jsonl") ? n.dest = `${o.slice(0, -6)}/dest` : n.dest = `${o}_dest_${r}`;
      else if (s) n.dest = `${s}_dest_${r}`;
      else throw new Error("Unsupported source for Vertex AI: No GCS or BigQuery URI found.");
    }
    return n;
  }
  async createInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = iH(this.apiClient, e);
      return a = nt("batchPredictionJobs", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s.then((u) => b1(u));
    } else {
      const l = rA(this.apiClient, e);
      return a = nt("{model}:batchGenerateContent", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s.then((u) => fp(u));
    }
  }
  async createEmbeddingsInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = uH(this.apiClient, e);
      return o = nt("{model}:asyncBatchEmbedContent", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => fp(i));
    }
  }
  async get(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = TH(this.apiClient, e);
      return a = nt("batchPredictionJobs/{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s.then((u) => b1(u));
    } else {
      const l = SH(this.apiClient, e);
      return a = nt("batches/{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s.then((u) => fp(u));
    }
  }
  async cancel(e) {
    var t, n, r, o;
    let s = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const i = nH(this.apiClient, e);
      s = nt("batchPredictionJobs/{name}:cancel", i._url), a = i._query, delete i._url, delete i._query, await this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    } else {
      const i = tH(this.apiClient, e);
      s = nt("batches/{name}:cancel", i._url), a = i._query, delete i._url, delete i._query, await this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = RH(e);
      return a = nt("batchPredictionJobs", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = $H(u), f = new jE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = PH(e);
      return a = nt("batches", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = NH(u), f = new jE();
        return Object.assign(f, d), f;
      });
    }
  }
  async delete(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = dH(this.apiClient, e);
      return a = nt("batchPredictionJobs/{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => hH(u));
    } else {
      const l = cH(this.apiClient, e);
      return a = nt("batches/{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => fH(u));
    }
  }
};
function UH(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && h(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function qH(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && h(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function oA(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let o = n;
    Array.isArray(o) && (o = o.map((s) => cz(s))), h(t, ["parts"], o);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function sA(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let o = n;
    Array.isArray(o) && (o = o.map((s) => dz(s))), h(t, ["parts"], o);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function HH(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && h(t, ["ttl"], r);
  const o = c(e, ["expireTime"]);
  t !== void 0 && o != null && h(t, ["expireTime"], o);
  const s = c(e, ["displayName"]);
  t !== void 0 && s != null && h(t, ["displayName"], s);
  const a = c(e, ["contents"]);
  if (t !== void 0 && a != null) {
    let d = kr(a);
    Array.isArray(d) && (d = d.map((f) => oA(f))), h(t, ["contents"], d);
  }
  const i = c(e, ["systemInstruction"]);
  t !== void 0 && i != null && h(t, ["systemInstruction"], oA(zn(i)));
  const l = c(e, ["tools"]);
  if (t !== void 0 && l != null) {
    let d = l;
    Array.isArray(d) && (d = d.map((f) => pz(f))), h(t, ["tools"], d);
  }
  const u = c(e, ["toolConfig"]);
  if (t !== void 0 && u != null && h(t, ["toolConfig"], fz(u)), c(e, ["kmsKeyName"]) !== void 0) throw new Error("kmsKeyName parameter is not supported in Gemini API.");
  return n;
}
function zH(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && h(t, ["ttl"], r);
  const o = c(e, ["expireTime"]);
  t !== void 0 && o != null && h(t, ["expireTime"], o);
  const s = c(e, ["displayName"]);
  t !== void 0 && s != null && h(t, ["displayName"], s);
  const a = c(e, ["contents"]);
  if (t !== void 0 && a != null) {
    let f = kr(a);
    Array.isArray(f) && (f = f.map((m) => sA(m))), h(t, ["contents"], f);
  }
  const i = c(e, ["systemInstruction"]);
  t !== void 0 && i != null && h(t, ["systemInstruction"], sA(zn(i)));
  const l = c(e, ["tools"]);
  if (t !== void 0 && l != null) {
    let f = l;
    Array.isArray(f) && (f = f.map((m) => mz(m))), h(t, ["tools"], f);
  }
  const u = c(e, ["toolConfig"]);
  t !== void 0 && u != null && h(t, ["toolConfig"], hz(u));
  const d = c(e, ["kmsKeyName"]);
  return t !== void 0 && d != null && h(t, ["encryption_spec", "kmsKeyName"], d), n;
}
function WH(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["model"], b3(e, r));
  const o = c(t, ["config"]);
  return o != null && HH(o, n), n;
}
function GH(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["model"], b3(e, r));
  const o = c(t, ["config"]);
  return o != null && zH(o, n), n;
}
function VH(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], na(e, r)), n;
}
function KH(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], na(e, r)), n;
}
function JH(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
function YH(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
function XH(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && h(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function QH(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && h(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && h(t, ["args"], r);
  const o = c(e, ["name"]);
  if (o != null && h(t, ["name"], o), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function ZH(e) {
  const t = {}, n = c(e, ["allowedFunctionNames"]);
  n != null && h(t, ["allowedFunctionNames"], n);
  const r = c(e, ["mode"]);
  if (r != null && h(t, ["mode"], r), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return t;
}
function jH(e) {
  const t = {}, n = c(e, ["description"]);
  n != null && h(t, ["description"], n);
  const r = c(e, ["name"]);
  r != null && h(t, ["name"], r);
  const o = c(e, ["parameters"]);
  o != null && h(t, ["parameters"], o);
  const s = c(e, ["parametersJsonSchema"]);
  s != null && h(t, ["parametersJsonSchema"], s);
  const a = c(e, ["response"]);
  a != null && h(t, ["response"], a);
  const i = c(e, ["responseJsonSchema"]);
  if (i != null && h(t, ["responseJsonSchema"], i), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return t;
}
function ez(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], na(e, r)), n;
}
function tz(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], na(e, r)), n;
}
function nz(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && h(t, ["authConfig"], UH(n));
  const r = c(e, ["enableWidget"]);
  return r != null && h(t, ["enableWidget"], r), t;
}
function rz(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && h(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && h(t, ["timeRangeFilter"], r), t;
}
function oz(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const o = c(e, ["pageToken"]);
  return t !== void 0 && o != null && h(t, ["_query", "pageToken"], o), n;
}
function sz(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const o = c(e, ["pageToken"]);
  return t !== void 0 && o != null && h(t, ["_query", "pageToken"], o), n;
}
function az(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && oz(n, t), t;
}
function iz(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && sz(n, t), t;
}
function lz(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const o = c(e, ["cachedContents"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => a)), h(t, ["cachedContents"], s);
  }
  return t;
}
function uz(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const o = c(e, ["cachedContents"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => a)), h(t, ["cachedContents"], s);
  }
  return t;
}
function cz(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const o = c(e, ["executableCode"]);
  o != null && h(t, ["executableCode"], o);
  const s = c(e, ["fileData"]);
  s != null && h(t, ["fileData"], XH(s));
  const a = c(e, ["functionCall"]);
  a != null && h(t, ["functionCall"], QH(a));
  const i = c(e, ["functionResponse"]);
  i != null && h(t, ["functionResponse"], i);
  const l = c(e, ["inlineData"]);
  l != null && h(t, ["inlineData"], qH(l));
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  m != null && h(t, ["videoMetadata"], m);
  const v = c(e, ["toolCall"]);
  v != null && h(t, ["toolCall"], v);
  const y = c(e, ["toolResponse"]);
  y != null && h(t, ["toolResponse"], y);
  const T = c(e, ["partMetadata"]);
  return T != null && h(t, ["partMetadata"], T), t;
}
function dz(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const o = c(e, ["executableCode"]);
  o != null && h(t, ["executableCode"], o);
  const s = c(e, ["fileData"]);
  s != null && h(t, ["fileData"], s);
  const a = c(e, ["functionCall"]);
  a != null && h(t, ["functionCall"], a);
  const i = c(e, ["functionResponse"]);
  i != null && h(t, ["functionResponse"], i);
  const l = c(e, ["inlineData"]);
  l != null && h(t, ["inlineData"], l);
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  if (m != null && h(t, ["videoMetadata"], m), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return t;
}
function fz(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && h(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  r != null && h(t, ["functionCallingConfig"], ZH(r));
  const o = c(e, ["includeServerSideToolInvocations"]);
  return o != null && h(t, ["includeServerSideToolInvocations"], o), t;
}
function hz(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && h(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  if (r != null && h(t, ["functionCallingConfig"], r), c(e, ["includeServerSideToolInvocations"]) !== void 0) throw new Error("includeServerSideToolInvocations parameter is not supported in Vertex AI.");
  return t;
}
function pz(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && h(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && h(t, ["fileSearch"], r);
  const o = c(e, ["googleSearch"]);
  o != null && h(t, ["googleSearch"], rz(o));
  const s = c(e, ["googleMaps"]);
  s != null && h(t, ["googleMaps"], nz(s));
  const a = c(e, ["codeExecution"]);
  if (a != null && h(t, ["codeExecution"], a), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const i = c(e, ["functionDeclarations"]);
  if (i != null) {
    let f = i;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["functionDeclarations"], f);
  }
  const l = c(e, ["googleSearchRetrieval"]);
  if (l != null && h(t, ["googleSearchRetrieval"], l), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && h(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let f = d;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["mcpServers"], f);
  }
  return t;
}
function mz(e) {
  const t = {}, n = c(e, ["retrieval"]);
  n != null && h(t, ["retrieval"], n);
  const r = c(e, ["computerUse"]);
  if (r != null && h(t, ["computerUse"], r), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const o = c(e, ["googleSearch"]);
  o != null && h(t, ["googleSearch"], o);
  const s = c(e, ["googleMaps"]);
  s != null && h(t, ["googleMaps"], s);
  const a = c(e, ["codeExecution"]);
  a != null && h(t, ["codeExecution"], a);
  const i = c(e, ["enterpriseWebSearch"]);
  i != null && h(t, ["enterpriseWebSearch"], i);
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let m = l;
    Array.isArray(m) && (m = m.map((v) => jH(v))), h(t, ["functionDeclarations"], m);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  u != null && h(t, ["googleSearchRetrieval"], u);
  const d = c(e, ["parallelAiSearch"]);
  d != null && h(t, ["parallelAiSearch"], d);
  const f = c(e, ["urlContext"]);
  if (f != null && h(t, ["urlContext"], f), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return t;
}
function gz(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && h(t, ["ttl"], r);
  const o = c(e, ["expireTime"]);
  return t !== void 0 && o != null && h(t, ["expireTime"], o), n;
}
function vz(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && h(t, ["ttl"], r);
  const o = c(e, ["expireTime"]);
  return t !== void 0 && o != null && h(t, ["expireTime"], o), n;
}
function yz(e, t) {
  const n = {}, r = c(t, ["name"]);
  r != null && h(n, ["_url", "name"], na(e, r));
  const o = c(t, ["config"]);
  return o != null && gz(o, n), n;
}
function _z(e, t) {
  const n = {}, r = c(t, ["name"]);
  r != null && h(n, ["_url", "name"], na(e, r));
  const o = c(t, ["config"]);
  return o != null && vz(o, n), n;
}
var bz = class extends ta {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new tl(js.PAGED_ITEM_CACHED_CONTENTS, (n) => this.listInternal(n), await this.listInternal(t), t);
  }
  async create(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = GH(this.apiClient, e);
      return a = nt("cachedContents", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s.then((u) => u);
    } else {
      const l = WH(this.apiClient, e);
      return a = nt("cachedContents", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s.then((u) => u);
    }
  }
  async get(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = tz(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s.then((u) => u);
    } else {
      const l = ez(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s.then((u) => u);
    }
  }
  async delete(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = KH(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = YH(u), f = new QE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = VH(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = JH(u), f = new QE();
        return Object.assign(f, d), f;
      });
    }
  }
  async update(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = _z(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "PATCH",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s.then((u) => u);
    } else {
      const l = yz(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "PATCH",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s.then((u) => u);
    }
  }
  async listInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = iz(e);
      return a = nt("cachedContents", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = uz(u), f = new ZE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = az(e);
      return a = nt("cachedContents", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = lz(u), f = new ZE();
        return Object.assign(f, d), f;
      });
    }
  }
};
function Ra(e, t) {
  var n = {};
  for (var r in e) Object.prototype.hasOwnProperty.call(e, r) && t.indexOf(r) < 0 && (n[r] = e[r]);
  if (e != null && typeof Object.getOwnPropertySymbols == "function")
    for (var o = 0, r = Object.getOwnPropertySymbols(e); o < r.length; o++) t.indexOf(r[o]) < 0 && Object.prototype.propertyIsEnumerable.call(e, r[o]) && (n[r[o]] = e[r[o]]);
  return n;
}
function aA(e) {
  var t = typeof Symbol == "function" && Symbol.iterator, n = t && e[t], r = 0;
  if (n) return n.call(e);
  if (e && typeof e.length == "number") return { next: function() {
    return e && r >= e.length && (e = void 0), {
      value: e && e[r++],
      done: !e
    };
  } };
  throw new TypeError(t ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function Rt(e) {
  return this instanceof Rt ? (this.v = e, this) : new Rt(e);
}
function $o(e, t, n) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var r = n.apply(e, t || []), o, s = [];
  return o = Object.create((typeof AsyncIterator == "function" ? AsyncIterator : Object).prototype), i("next"), i("throw"), i("return", a), o[Symbol.asyncIterator] = function() {
    return this;
  }, o;
  function a(v) {
    return function(y) {
      return Promise.resolve(y).then(v, f);
    };
  }
  function i(v, y) {
    r[v] && (o[v] = function(T) {
      return new Promise(function(C, A) {
        s.push([
          v,
          T,
          C,
          A
        ]) > 1 || l(v, T);
      });
    }, y && (o[v] = y(o[v])));
  }
  function l(v, y) {
    try {
      u(r[v](y));
    } catch (T) {
      m(s[0][3], T);
    }
  }
  function u(v) {
    v.value instanceof Rt ? Promise.resolve(v.value.v).then(d, f) : m(s[0][2], v);
  }
  function d(v) {
    l("next", v);
  }
  function f(v) {
    l("throw", v);
  }
  function m(v, y) {
    v(y), s.shift(), s.length && l(s[0][0], s[0][1]);
  }
}
function Do(e) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var t = e[Symbol.asyncIterator], n;
  return t ? t.call(e) : (e = typeof aA == "function" ? aA(e) : e[Symbol.iterator](), n = {}, r("next"), r("throw"), r("return"), n[Symbol.asyncIterator] = function() {
    return this;
  }, n);
  function r(s) {
    n[s] = e[s] && function(a) {
      return new Promise(function(i, l) {
        a = e[s](a), o(i, l, a.done, a.value);
      });
    };
  }
  function o(s, a, i, l) {
    Promise.resolve(l).then(function(u) {
      s({
        value: u,
        done: i
      });
    }, a);
  }
}
function wz(e) {
  var t;
  if (e.candidates == null || e.candidates.length === 0) return !1;
  const n = (t = e.candidates[0]) === null || t === void 0 ? void 0 : t.content;
  return n === void 0 ? !1 : N3(n);
}
function N3(e) {
  if (e.parts === void 0 || e.parts.length === 0) return !1;
  for (const t of e.parts) if (t === void 0 || Object.keys(t).length === 0) return !1;
  return !0;
}
function Sz(e) {
  if (e.length !== 0) {
    for (const t of e) if (t.role !== "user" && t.role !== "model") throw new Error(`Role must be user or model, but got ${t.role}.`);
  }
}
function iA(e) {
  if (e === void 0 || e.length === 0) return [];
  const t = [], n = e.length;
  let r = 0;
  for (; r < n; ) if (e[r].role === "user")
    t.push(e[r]), r++;
  else {
    const o = [];
    let s = !0;
    for (; r < n && e[r].role === "model"; )
      o.push(e[r]), s && !N3(e[r]) && (s = !1), r++;
    s ? t.push(...o) : t.pop();
  }
  return t;
}
var Tz = class {
  constructor(e, t) {
    this.modelsModule = e, this.apiClient = t;
  }
  create(e) {
    return new Ez(this.apiClient, this.modelsModule, e.model, e.config, structuredClone(e.history));
  }
}, Ez = class {
  constructor(e, t, n, r = {}, o = []) {
    this.apiClient = e, this.modelsModule = t, this.model = n, this.config = r, this.history = o, this.sendPromise = Promise.resolve(), Sz(o);
  }
  async sendMessage(e) {
    var t;
    await this.sendPromise;
    const n = zn(e.message), r = this.modelsModule.generateContent({
      model: this.model,
      contents: this.getHistory(!0).concat(n),
      config: (t = e.config) !== null && t !== void 0 ? t : this.config
    });
    return this.sendPromise = (async () => {
      var o, s, a;
      const i = await r, l = (s = (o = i.candidates) === null || o === void 0 ? void 0 : o[0]) === null || s === void 0 ? void 0 : s.content, u = i.automaticFunctionCallingHistory, d = this.getHistory(!0).length;
      let f = [];
      u != null && (f = (a = u.slice(d)) !== null && a !== void 0 ? a : []);
      const m = l ? [l] : [];
      this.recordHistory(n, m, f);
    })(), await this.sendPromise.catch(() => {
      this.sendPromise = Promise.resolve();
    }), r;
  }
  async sendMessageStream(e) {
    var t;
    await this.sendPromise;
    const n = zn(e.message), r = this.modelsModule.generateContentStream({
      model: this.model,
      contents: this.getHistory(!0).concat(n),
      config: (t = e.config) !== null && t !== void 0 ? t : this.config
    });
    this.sendPromise = r.then(() => {
    }).catch(() => {
    });
    const o = await r;
    return this.processStreamResponse(o, n);
  }
  getHistory(e = !1) {
    const t = e ? iA(this.history) : this.history;
    return structuredClone(t);
  }
  processStreamResponse(e, t) {
    return $o(this, arguments, function* () {
      var r, o, s, a, i, l;
      const u = [];
      try {
        for (var d = !0, f = Do(e), m; m = yield Rt(f.next()), r = m.done, !r; d = !0) {
          a = m.value, d = !1;
          const v = a;
          if (wz(v)) {
            const y = (l = (i = v.candidates) === null || i === void 0 ? void 0 : i[0]) === null || l === void 0 ? void 0 : l.content;
            y !== void 0 && u.push(y);
          }
          yield yield Rt(v);
        }
      } catch (v) {
        o = { error: v };
      } finally {
        try {
          !d && !r && (s = f.return) && (yield Rt(s.call(f)));
        } finally {
          if (o) throw o.error;
        }
      }
      this.recordHistory(t, u);
    });
  }
  recordHistory(e, t, n) {
    let r = [];
    t.length > 0 && t.every((o) => o.role !== void 0) ? r = t : r.push({
      role: "model",
      parts: []
    }), n && n.length > 0 ? this.history.push(...iA(n)) : this.history.push(e), this.history.push(...r);
  }
}, $3 = class D3 extends Error {
  constructor(t) {
    super(t.message), this.name = "ApiError", this.status = t.status, Object.setPrototypeOf(this, D3.prototype);
  }
};
function Az(e) {
  const t = {}, n = c(e, ["file"]);
  return n != null && h(t, ["file"], n), t;
}
function Cz(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
function kz(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && h(t, ["_url", "file"], C3(n)), t;
}
function Iz(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
function xz(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && h(t, ["_url", "file"], C3(n)), t;
}
function Mz(e) {
  const t = {}, n = c(e, ["uris"]);
  return n != null && h(t, ["uris"], n), t;
}
function Pz(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const o = c(e, ["pageToken"]);
  return t !== void 0 && o != null && h(t, ["_query", "pageToken"], o), n;
}
function Rz(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && Pz(n, t), t;
}
function Nz(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const o = c(e, ["files"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => a)), h(t, ["files"], s);
  }
  return t;
}
function $z(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["files"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((s) => s)), h(t, ["files"], o);
  }
  return t;
}
var Dz = class extends ta {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new tl(js.PAGED_ITEM_FILES, (n) => this.listInternal(n), await this.listInternal(t), t);
  }
  async upload(e) {
    if (this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support uploading files. You can share files through a GCS bucket.");
    return this.apiClient.uploadFile(e.file, e.config).then((t) => t);
  }
  async download(e) {
    await this.apiClient.downloadFile(e);
  }
  async registerFiles(e) {
    throw new Error("registerFiles is only supported in Node.js environments.");
  }
  async _registerFiles(e) {
    return this.registerFilesInternal(e);
  }
  async listInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = Rz(e);
      return o = nt("files", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: i.headers }, u;
      })), r.then((i) => {
        const l = Nz(i), u = new Mq();
        return Object.assign(u, l), u;
      });
    }
  }
  async createInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = Az(e);
      return o = nt("upload/v1beta/files", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => {
        const l = Cz(i), u = new Pq();
        return Object.assign(u, l), u;
      });
    }
  }
  async get(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = xz(e);
      return o = nt("files/{file}", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => i);
    }
  }
  async delete(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = kz(e);
      return o = nt("files/{file}", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: i.headers }, u;
      })), r.then((i) => {
        const l = Iz(i), u = new Rq();
        return Object.assign(u, l), u;
      });
    }
  }
  async registerFilesInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = Mz(e);
      return o = nt("files:register", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => {
        const l = $z(i), u = new Nq();
        return Object.assign(u, l), u;
      });
    }
  }
};
function lA(e) {
  const t = {};
  if (c(e, ["languageCodes"]) !== void 0) throw new Error("languageCodes parameter is not supported in Gemini API.");
  return t;
}
function Lz(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && h(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function hp(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && h(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function Oz(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let o = n;
    Array.isArray(o) && (o = o.map((s) => eW(s))), h(t, ["parts"], o);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function Fz(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let o = n;
    Array.isArray(o) && (o = o.map((s) => tW(s))), h(t, ["parts"], o);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function Bz(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && h(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function Uz(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && h(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && h(t, ["args"], r);
  const o = c(e, ["name"]);
  if (o != null && h(t, ["name"], o), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function qz(e) {
  const t = {}, n = c(e, ["description"]);
  n != null && h(t, ["description"], n);
  const r = c(e, ["name"]);
  r != null && h(t, ["name"], r);
  const o = c(e, ["parameters"]);
  o != null && h(t, ["parameters"], o);
  const s = c(e, ["parametersJsonSchema"]);
  s != null && h(t, ["parametersJsonSchema"], s);
  const a = c(e, ["response"]);
  a != null && h(t, ["response"], a);
  const i = c(e, ["responseJsonSchema"]);
  if (i != null && h(t, ["responseJsonSchema"], i), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return t;
}
function Hz(e) {
  const t = {}, n = c(e, ["modelSelectionConfig"]);
  n != null && h(t, ["modelConfig"], n);
  const r = c(e, ["responseJsonSchema"]);
  r != null && h(t, ["responseJsonSchema"], r);
  const o = c(e, ["audioTimestamp"]);
  o != null && h(t, ["audioTimestamp"], o);
  const s = c(e, ["candidateCount"]);
  s != null && h(t, ["candidateCount"], s);
  const a = c(e, ["enableAffectiveDialog"]);
  a != null && h(t, ["enableAffectiveDialog"], a);
  const i = c(e, ["frequencyPenalty"]);
  i != null && h(t, ["frequencyPenalty"], i);
  const l = c(e, ["logprobs"]);
  l != null && h(t, ["logprobs"], l);
  const u = c(e, ["maxOutputTokens"]);
  u != null && h(t, ["maxOutputTokens"], u);
  const d = c(e, ["mediaResolution"]);
  d != null && h(t, ["mediaResolution"], d);
  const f = c(e, ["presencePenalty"]);
  f != null && h(t, ["presencePenalty"], f);
  const m = c(e, ["responseLogprobs"]);
  m != null && h(t, ["responseLogprobs"], m);
  const v = c(e, ["responseMimeType"]);
  v != null && h(t, ["responseMimeType"], v);
  const y = c(e, ["responseModalities"]);
  y != null && h(t, ["responseModalities"], y);
  const T = c(e, ["responseSchema"]);
  T != null && h(t, ["responseSchema"], T);
  const C = c(e, ["routingConfig"]);
  C != null && h(t, ["routingConfig"], C);
  const A = c(e, ["seed"]);
  A != null && h(t, ["seed"], A);
  const I = c(e, ["speechConfig"]);
  I != null && h(t, ["speechConfig"], I);
  const R = c(e, ["stopSequences"]);
  R != null && h(t, ["stopSequences"], R);
  const $ = c(e, ["temperature"]);
  $ != null && h(t, ["temperature"], $);
  const p = c(e, ["thinkingConfig"]);
  p != null && h(t, ["thinkingConfig"], p);
  const b = c(e, ["topK"]);
  b != null && h(t, ["topK"], b);
  const w = c(e, ["topP"]);
  if (w != null && h(t, ["topP"], w), c(e, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  return t;
}
function zz(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && h(t, ["authConfig"], Lz(n));
  const r = c(e, ["enableWidget"]);
  return r != null && h(t, ["enableWidget"], r), t;
}
function Wz(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && h(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && h(t, ["timeRangeFilter"], r), t;
}
function Gz(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && h(t, ["setup", "generationConfig"], r);
  const o = c(e, ["responseModalities"]);
  t !== void 0 && o != null && h(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], o);
  const s = c(e, ["temperature"]);
  t !== void 0 && s != null && h(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], s);
  const a = c(e, ["topP"]);
  t !== void 0 && a != null && h(t, [
    "setup",
    "generationConfig",
    "topP"
  ], a);
  const i = c(e, ["topK"]);
  t !== void 0 && i != null && h(t, [
    "setup",
    "generationConfig",
    "topK"
  ], i);
  const l = c(e, ["maxOutputTokens"]);
  t !== void 0 && l != null && h(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], l);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && h(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && h(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const f = c(e, ["speechConfig"]);
  t !== void 0 && f != null && h(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], Hb(f));
  const m = c(e, ["thinkingConfig"]);
  t !== void 0 && m != null && h(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], m);
  const v = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && v != null && h(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], v);
  const y = c(e, ["systemInstruction"]);
  t !== void 0 && y != null && h(t, ["setup", "systemInstruction"], Oz(zn(y)));
  const T = c(e, ["tools"]);
  if (t !== void 0 && T != null) {
    let E = Lu(T);
    Array.isArray(E) && (E = E.map((x) => oW(Du(x)))), h(t, ["setup", "tools"], E);
  }
  const C = c(e, ["sessionResumption"]);
  t !== void 0 && C != null && h(t, ["setup", "sessionResumption"], rW(C));
  const A = c(e, ["inputAudioTranscription"]);
  t !== void 0 && A != null && h(t, ["setup", "inputAudioTranscription"], lA(A));
  const I = c(e, ["outputAudioTranscription"]);
  t !== void 0 && I != null && h(t, ["setup", "outputAudioTranscription"], lA(I));
  const R = c(e, ["realtimeInputConfig"]);
  t !== void 0 && R != null && h(t, ["setup", "realtimeInputConfig"], R);
  const $ = c(e, ["contextWindowCompression"]);
  t !== void 0 && $ != null && h(t, ["setup", "contextWindowCompression"], $);
  const p = c(e, ["proactivity"]);
  if (t !== void 0 && p != null && h(t, ["setup", "proactivity"], p), c(e, ["explicitVadSignal"]) !== void 0) throw new Error("explicitVadSignal parameter is not supported in Gemini API.");
  const b = c(e, ["avatarConfig"]);
  t !== void 0 && b != null && h(t, ["setup", "avatarConfig"], b);
  const w = c(e, ["safetySettings"]);
  if (t !== void 0 && w != null) {
    let E = w;
    Array.isArray(E) && (E = E.map((x) => nW(x))), h(t, ["setup", "safetySettings"], E);
  }
  return n;
}
function Vz(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && h(t, ["setup", "generationConfig"], Hz(r));
  const o = c(e, ["responseModalities"]);
  t !== void 0 && o != null && h(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], o);
  const s = c(e, ["temperature"]);
  t !== void 0 && s != null && h(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], s);
  const a = c(e, ["topP"]);
  t !== void 0 && a != null && h(t, [
    "setup",
    "generationConfig",
    "topP"
  ], a);
  const i = c(e, ["topK"]);
  t !== void 0 && i != null && h(t, [
    "setup",
    "generationConfig",
    "topK"
  ], i);
  const l = c(e, ["maxOutputTokens"]);
  t !== void 0 && l != null && h(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], l);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && h(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && h(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const f = c(e, ["speechConfig"]);
  t !== void 0 && f != null && h(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], Hb(f));
  const m = c(e, ["thinkingConfig"]);
  t !== void 0 && m != null && h(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], m);
  const v = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && v != null && h(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], v);
  const y = c(e, ["systemInstruction"]);
  t !== void 0 && y != null && h(t, ["setup", "systemInstruction"], Fz(zn(y)));
  const T = c(e, ["tools"]);
  if (t !== void 0 && T != null) {
    let x = Lu(T);
    Array.isArray(x) && (x = x.map((M) => sW(Du(M)))), h(t, ["setup", "tools"], x);
  }
  const C = c(e, ["sessionResumption"]);
  t !== void 0 && C != null && h(t, ["setup", "sessionResumption"], C);
  const A = c(e, ["inputAudioTranscription"]);
  t !== void 0 && A != null && h(t, ["setup", "inputAudioTranscription"], A);
  const I = c(e, ["outputAudioTranscription"]);
  t !== void 0 && I != null && h(t, ["setup", "outputAudioTranscription"], I);
  const R = c(e, ["realtimeInputConfig"]);
  t !== void 0 && R != null && h(t, ["setup", "realtimeInputConfig"], R);
  const $ = c(e, ["contextWindowCompression"]);
  t !== void 0 && $ != null && h(t, ["setup", "contextWindowCompression"], $);
  const p = c(e, ["proactivity"]);
  t !== void 0 && p != null && h(t, ["setup", "proactivity"], p);
  const b = c(e, ["explicitVadSignal"]);
  t !== void 0 && b != null && h(t, ["setup", "explicitVadSignal"], b);
  const w = c(e, ["avatarConfig"]);
  t !== void 0 && w != null && h(t, ["setup", "avatarConfig"], w);
  const E = c(e, ["safetySettings"]);
  if (t !== void 0 && E != null) {
    let x = E;
    Array.isArray(x) && (x = x.map((M) => M)), h(t, ["setup", "safetySettings"], x);
  }
  return n;
}
function Kz(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["setup", "model"], Xt(e, r));
  const o = c(t, ["config"]);
  return o != null && h(n, ["config"], Gz(o, n)), n;
}
function Jz(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["setup", "model"], Xt(e, r));
  const o = c(t, ["config"]);
  return o != null && h(n, ["config"], Vz(o, n)), n;
}
function Yz(e) {
  const t = {}, n = c(e, ["musicGenerationConfig"]);
  return n != null && h(t, ["musicGenerationConfig"], n), t;
}
function Xz(e) {
  const t = {}, n = c(e, ["weightedPrompts"]);
  if (n != null) {
    let r = n;
    Array.isArray(r) && (r = r.map((o) => o)), h(t, ["weightedPrompts"], r);
  }
  return t;
}
function Qz(e) {
  const t = {}, n = c(e, ["media"]);
  if (n != null) {
    let u = w3(n);
    Array.isArray(u) && (u = u.map((d) => hp(d))), h(t, ["mediaChunks"], u);
  }
  const r = c(e, ["audio"]);
  r != null && h(t, ["audio"], hp(T3(r)));
  const o = c(e, ["audioStreamEnd"]);
  o != null && h(t, ["audioStreamEnd"], o);
  const s = c(e, ["video"]);
  s != null && h(t, ["video"], hp(S3(s)));
  const a = c(e, ["text"]);
  a != null && h(t, ["text"], a);
  const i = c(e, ["activityStart"]);
  i != null && h(t, ["activityStart"], i);
  const l = c(e, ["activityEnd"]);
  return l != null && h(t, ["activityEnd"], l), t;
}
function Zz(e) {
  const t = {}, n = c(e, ["media"]);
  if (n != null) {
    let u = w3(n);
    Array.isArray(u) && (u = u.map((d) => d)), h(t, ["mediaChunks"], u);
  }
  const r = c(e, ["audio"]);
  r != null && h(t, ["audio"], T3(r));
  const o = c(e, ["audioStreamEnd"]);
  o != null && h(t, ["audioStreamEnd"], o);
  const s = c(e, ["video"]);
  s != null && h(t, ["video"], S3(s));
  const a = c(e, ["text"]);
  a != null && h(t, ["text"], a);
  const i = c(e, ["activityStart"]);
  i != null && h(t, ["activityStart"], i);
  const l = c(e, ["activityEnd"]);
  return l != null && h(t, ["activityEnd"], l), t;
}
function jz(e) {
  const t = {}, n = c(e, ["setupComplete"]);
  n != null && h(t, ["setupComplete"], n);
  const r = c(e, ["serverContent"]);
  r != null && h(t, ["serverContent"], r);
  const o = c(e, ["toolCall"]);
  o != null && h(t, ["toolCall"], o);
  const s = c(e, ["toolCallCancellation"]);
  s != null && h(t, ["toolCallCancellation"], s);
  const a = c(e, ["usageMetadata"]);
  a != null && h(t, ["usageMetadata"], aW(a));
  const i = c(e, ["goAway"]);
  i != null && h(t, ["goAway"], i);
  const l = c(e, ["sessionResumptionUpdate"]);
  l != null && h(t, ["sessionResumptionUpdate"], l);
  const u = c(e, ["voiceActivityDetectionSignal"]);
  u != null && h(t, ["voiceActivityDetectionSignal"], u);
  const d = c(e, ["voiceActivity"]);
  return d != null && h(t, ["voiceActivity"], iW(d)), t;
}
function eW(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const o = c(e, ["executableCode"]);
  o != null && h(t, ["executableCode"], o);
  const s = c(e, ["fileData"]);
  s != null && h(t, ["fileData"], Bz(s));
  const a = c(e, ["functionCall"]);
  a != null && h(t, ["functionCall"], Uz(a));
  const i = c(e, ["functionResponse"]);
  i != null && h(t, ["functionResponse"], i);
  const l = c(e, ["inlineData"]);
  l != null && h(t, ["inlineData"], hp(l));
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  m != null && h(t, ["videoMetadata"], m);
  const v = c(e, ["toolCall"]);
  v != null && h(t, ["toolCall"], v);
  const y = c(e, ["toolResponse"]);
  y != null && h(t, ["toolResponse"], y);
  const T = c(e, ["partMetadata"]);
  return T != null && h(t, ["partMetadata"], T), t;
}
function tW(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const o = c(e, ["executableCode"]);
  o != null && h(t, ["executableCode"], o);
  const s = c(e, ["fileData"]);
  s != null && h(t, ["fileData"], s);
  const a = c(e, ["functionCall"]);
  a != null && h(t, ["functionCall"], a);
  const i = c(e, ["functionResponse"]);
  i != null && h(t, ["functionResponse"], i);
  const l = c(e, ["inlineData"]);
  l != null && h(t, ["inlineData"], l);
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  if (m != null && h(t, ["videoMetadata"], m), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return t;
}
function nW(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && h(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && h(t, ["threshold"], r), t;
}
function rW(e) {
  const t = {}, n = c(e, ["handle"]);
  if (n != null && h(t, ["handle"], n), c(e, ["transparent"]) !== void 0) throw new Error("transparent parameter is not supported in Gemini API.");
  return t;
}
function oW(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && h(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && h(t, ["fileSearch"], r);
  const o = c(e, ["googleSearch"]);
  o != null && h(t, ["googleSearch"], Wz(o));
  const s = c(e, ["googleMaps"]);
  s != null && h(t, ["googleMaps"], zz(s));
  const a = c(e, ["codeExecution"]);
  if (a != null && h(t, ["codeExecution"], a), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const i = c(e, ["functionDeclarations"]);
  if (i != null) {
    let f = i;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["functionDeclarations"], f);
  }
  const l = c(e, ["googleSearchRetrieval"]);
  if (l != null && h(t, ["googleSearchRetrieval"], l), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && h(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let f = d;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["mcpServers"], f);
  }
  return t;
}
function sW(e) {
  const t = {}, n = c(e, ["retrieval"]);
  n != null && h(t, ["retrieval"], n);
  const r = c(e, ["computerUse"]);
  if (r != null && h(t, ["computerUse"], r), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const o = c(e, ["googleSearch"]);
  o != null && h(t, ["googleSearch"], o);
  const s = c(e, ["googleMaps"]);
  s != null && h(t, ["googleMaps"], s);
  const a = c(e, ["codeExecution"]);
  a != null && h(t, ["codeExecution"], a);
  const i = c(e, ["enterpriseWebSearch"]);
  i != null && h(t, ["enterpriseWebSearch"], i);
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let m = l;
    Array.isArray(m) && (m = m.map((v) => qz(v))), h(t, ["functionDeclarations"], m);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  u != null && h(t, ["googleSearchRetrieval"], u);
  const d = c(e, ["parallelAiSearch"]);
  d != null && h(t, ["parallelAiSearch"], d);
  const f = c(e, ["urlContext"]);
  if (f != null && h(t, ["urlContext"], f), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return t;
}
function aW(e) {
  const t = {}, n = c(e, ["promptTokenCount"]);
  n != null && h(t, ["promptTokenCount"], n);
  const r = c(e, ["cachedContentTokenCount"]);
  r != null && h(t, ["cachedContentTokenCount"], r);
  const o = c(e, ["candidatesTokenCount"]);
  o != null && h(t, ["responseTokenCount"], o);
  const s = c(e, ["toolUsePromptTokenCount"]);
  s != null && h(t, ["toolUsePromptTokenCount"], s);
  const a = c(e, ["thoughtsTokenCount"]);
  a != null && h(t, ["thoughtsTokenCount"], a);
  const i = c(e, ["totalTokenCount"]);
  i != null && h(t, ["totalTokenCount"], i);
  const l = c(e, ["promptTokensDetails"]);
  if (l != null) {
    let v = l;
    Array.isArray(v) && (v = v.map((y) => y)), h(t, ["promptTokensDetails"], v);
  }
  const u = c(e, ["cacheTokensDetails"]);
  if (u != null) {
    let v = u;
    Array.isArray(v) && (v = v.map((y) => y)), h(t, ["cacheTokensDetails"], v);
  }
  const d = c(e, ["candidatesTokensDetails"]);
  if (d != null) {
    let v = d;
    Array.isArray(v) && (v = v.map((y) => y)), h(t, ["responseTokensDetails"], v);
  }
  const f = c(e, ["toolUsePromptTokensDetails"]);
  if (f != null) {
    let v = f;
    Array.isArray(v) && (v = v.map((y) => y)), h(t, ["toolUsePromptTokensDetails"], v);
  }
  const m = c(e, ["trafficType"]);
  return m != null && h(t, ["trafficType"], m), t;
}
function iW(e) {
  const t = {}, n = c(e, ["type"]);
  return n != null && h(t, ["voiceActivityType"], n), t;
}
function lW(e, t) {
  const n = {}, r = c(e, ["apiKey"]);
  if (r != null && h(n, ["apiKey"], r), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return n;
}
function uW(e, t) {
  const n = {}, r = c(e, ["data"]);
  if (r != null && h(n, ["data"], r), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const o = c(e, ["mimeType"]);
  return o != null && h(n, ["mimeType"], o), n;
}
function cW(e, t) {
  const n = {}, r = c(e, ["content"]);
  r != null && h(n, ["content"], r);
  const o = c(e, ["citationMetadata"]);
  o != null && h(n, ["citationMetadata"], dW(o));
  const s = c(e, ["tokenCount"]);
  s != null && h(n, ["tokenCount"], s);
  const a = c(e, ["finishReason"]);
  a != null && h(n, ["finishReason"], a);
  const i = c(e, ["groundingMetadata"]);
  i != null && h(n, ["groundingMetadata"], i);
  const l = c(e, ["avgLogprobs"]);
  l != null && h(n, ["avgLogprobs"], l);
  const u = c(e, ["index"]);
  u != null && h(n, ["index"], u);
  const d = c(e, ["logprobsResult"]);
  d != null && h(n, ["logprobsResult"], d);
  const f = c(e, ["safetyRatings"]);
  if (f != null) {
    let v = f;
    Array.isArray(v) && (v = v.map((y) => y)), h(n, ["safetyRatings"], v);
  }
  const m = c(e, ["urlContextMetadata"]);
  return m != null && h(n, ["urlContextMetadata"], m), n;
}
function dW(e, t) {
  const n = {}, r = c(e, ["citationSources"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((s) => s)), h(n, ["citations"], o);
  }
  return n;
}
function fW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["contents"]);
  if (s != null) {
    let a = kr(s);
    Array.isArray(a) && (a = a.map((i) => Fu(i))), h(r, ["contents"], a);
  }
  return r;
}
function hW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["tokensInfo"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => a)), h(n, ["tokensInfo"], s);
  }
  return n;
}
function pW(e, t) {
  const n = {}, r = c(e, ["values"]);
  r != null && h(n, ["values"], r);
  const o = c(e, ["statistics"]);
  return o != null && h(n, ["statistics"], mW(o)), n;
}
function mW(e, t) {
  const n = {}, r = c(e, ["truncated"]);
  r != null && h(n, ["truncated"], r);
  const o = c(e, ["token_count"]);
  return o != null && h(n, ["tokenCount"], o), n;
}
function mf(e, t) {
  const n = {}, r = c(e, ["parts"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => EG(a))), h(n, ["parts"], s);
  }
  const o = c(e, ["role"]);
  return o != null && h(n, ["role"], o), n;
}
function Fu(e, t) {
  const n = {}, r = c(e, ["parts"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => AG(a))), h(n, ["parts"], s);
  }
  const o = c(e, ["role"]);
  return o != null && h(n, ["role"], o), n;
}
function gW(e, t) {
  const n = {}, r = c(e, ["controlType"]);
  r != null && h(n, ["controlType"], r);
  const o = c(e, ["enableControlImageComputation"]);
  return o != null && h(n, ["computeControl"], o), n;
}
function vW(e, t) {
  const n = {};
  if (c(e, ["systemInstruction"]) !== void 0) throw new Error("systemInstruction parameter is not supported in Gemini API.");
  if (c(e, ["tools"]) !== void 0) throw new Error("tools parameter is not supported in Gemini API.");
  if (c(e, ["generationConfig"]) !== void 0) throw new Error("generationConfig parameter is not supported in Gemini API.");
  return n;
}
function yW(e, t, n) {
  const r = {}, o = c(e, ["systemInstruction"]);
  t !== void 0 && o != null && h(t, ["systemInstruction"], Fu(zn(o)));
  const s = c(e, ["tools"]);
  if (t !== void 0 && s != null) {
    let i = s;
    Array.isArray(i) && (i = i.map((l) => B3(l))), h(t, ["tools"], i);
  }
  const a = c(e, ["generationConfig"]);
  return t !== void 0 && a != null && h(t, ["generationConfig"], uG(a)), r;
}
function _W(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["contents"]);
  if (s != null) {
    let i = kr(s);
    Array.isArray(i) && (i = i.map((l) => mf(l))), h(r, ["contents"], i);
  }
  const a = c(t, ["config"]);
  return a != null && vW(a), r;
}
function bW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["contents"]);
  if (s != null) {
    let i = kr(s);
    Array.isArray(i) && (i = i.map((l) => Fu(l))), h(r, ["contents"], i);
  }
  const a = c(t, ["config"]);
  return a != null && yW(a, r), r;
}
function wW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["totalTokens"]);
  o != null && h(n, ["totalTokens"], o);
  const s = c(e, ["cachedContentTokenCount"]);
  return s != null && h(n, ["cachedContentTokenCount"], s), n;
}
function SW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["totalTokens"]);
  return o != null && h(n, ["totalTokens"], o), n;
}
function TW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  return o != null && h(r, ["_url", "name"], Xt(e, o)), r;
}
function EW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  return o != null && h(r, ["_url", "name"], Xt(e, o)), r;
}
function AW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && h(n, ["sdkHttpResponse"], r), n;
}
function CW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && h(n, ["sdkHttpResponse"], r), n;
}
function kW(e, t, n) {
  const r = {}, o = c(e, ["outputGcsUri"]);
  t !== void 0 && o != null && h(t, ["parameters", "storageUri"], o);
  const s = c(e, ["negativePrompt"]);
  t !== void 0 && s != null && h(t, ["parameters", "negativePrompt"], s);
  const a = c(e, ["numberOfImages"]);
  t !== void 0 && a != null && h(t, ["parameters", "sampleCount"], a);
  const i = c(e, ["aspectRatio"]);
  t !== void 0 && i != null && h(t, ["parameters", "aspectRatio"], i);
  const l = c(e, ["guidanceScale"]);
  t !== void 0 && l != null && h(t, ["parameters", "guidanceScale"], l);
  const u = c(e, ["seed"]);
  t !== void 0 && u != null && h(t, ["parameters", "seed"], u);
  const d = c(e, ["safetyFilterLevel"]);
  t !== void 0 && d != null && h(t, ["parameters", "safetySetting"], d);
  const f = c(e, ["personGeneration"]);
  t !== void 0 && f != null && h(t, ["parameters", "personGeneration"], f);
  const m = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && m != null && h(t, ["parameters", "includeSafetyAttributes"], m);
  const v = c(e, ["includeRaiReason"]);
  t !== void 0 && v != null && h(t, ["parameters", "includeRaiReason"], v);
  const y = c(e, ["language"]);
  t !== void 0 && y != null && h(t, ["parameters", "language"], y);
  const T = c(e, ["outputMimeType"]);
  t !== void 0 && T != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], T);
  const C = c(e, ["outputCompressionQuality"]);
  t !== void 0 && C != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], C);
  const A = c(e, ["addWatermark"]);
  t !== void 0 && A != null && h(t, ["parameters", "addWatermark"], A);
  const I = c(e, ["labels"]);
  t !== void 0 && I != null && h(t, ["labels"], I);
  const R = c(e, ["editMode"]);
  t !== void 0 && R != null && h(t, ["parameters", "editMode"], R);
  const $ = c(e, ["baseSteps"]);
  return t !== void 0 && $ != null && h(t, [
    "parameters",
    "editConfig",
    "baseSteps"
  ], $), r;
}
function IW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["prompt"]);
  s != null && h(r, ["instances[0]", "prompt"], s);
  const a = c(t, ["referenceImages"]);
  if (a != null) {
    let l = a;
    Array.isArray(l) && (l = l.map((u) => PG(u))), h(r, ["instances[0]", "referenceImages"], l);
  }
  const i = c(t, ["config"]);
  return i != null && kW(i, r), r;
}
function xW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["predictions"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => _g(a))), h(n, ["generatedImages"], s);
  }
  return n;
}
function MW(e, t, n) {
  const r = {}, o = c(e, ["taskType"]);
  t !== void 0 && o != null && h(t, ["requests[]", "taskType"], o);
  const s = c(e, ["title"]);
  t !== void 0 && s != null && h(t, ["requests[]", "title"], s);
  const a = c(e, ["outputDimensionality"]);
  if (t !== void 0 && a != null && h(t, ["requests[]", "outputDimensionality"], a), c(e, ["mimeType"]) !== void 0) throw new Error("mimeType parameter is not supported in Gemini API.");
  if (c(e, ["autoTruncate"]) !== void 0) throw new Error("autoTruncate parameter is not supported in Gemini API.");
  if (c(e, ["documentOcr"]) !== void 0) throw new Error("documentOcr parameter is not supported in Gemini API.");
  if (c(e, ["audioTrackExtraction"]) !== void 0) throw new Error("audioTrackExtraction parameter is not supported in Gemini API.");
  return r;
}
function PW(e, t, n) {
  const r = {};
  let o = c(n, ["embeddingApiType"]);
  if (o === void 0 && (o = "PREDICT"), o === "PREDICT") {
    const f = c(e, ["taskType"]);
    t !== void 0 && f != null && h(t, ["instances[]", "task_type"], f);
  } else if (o === "EMBED_CONTENT") {
    const f = c(e, ["taskType"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "taskType"], f);
  }
  let s = c(n, ["embeddingApiType"]);
  if (s === void 0 && (s = "PREDICT"), s === "PREDICT") {
    const f = c(e, ["title"]);
    t !== void 0 && f != null && h(t, ["instances[]", "title"], f);
  } else if (s === "EMBED_CONTENT") {
    const f = c(e, ["title"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "title"], f);
  }
  let a = c(n, ["embeddingApiType"]);
  if (a === void 0 && (a = "PREDICT"), a === "PREDICT") {
    const f = c(e, ["outputDimensionality"]);
    t !== void 0 && f != null && h(t, ["parameters", "outputDimensionality"], f);
  } else if (a === "EMBED_CONTENT") {
    const f = c(e, ["outputDimensionality"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "outputDimensionality"], f);
  }
  let i = c(n, ["embeddingApiType"]);
  if (i === void 0 && (i = "PREDICT"), i === "PREDICT") {
    const f = c(e, ["mimeType"]);
    t !== void 0 && f != null && h(t, ["instances[]", "mimeType"], f);
  }
  let l = c(n, ["embeddingApiType"]);
  if (l === void 0 && (l = "PREDICT"), l === "PREDICT") {
    const f = c(e, ["autoTruncate"]);
    t !== void 0 && f != null && h(t, ["parameters", "autoTruncate"], f);
  } else if (l === "EMBED_CONTENT") {
    const f = c(e, ["autoTruncate"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "autoTruncate"], f);
  }
  let u = c(n, ["embeddingApiType"]);
  if (u === void 0 && (u = "PREDICT"), u === "EMBED_CONTENT") {
    const f = c(e, ["documentOcr"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "documentOcr"], f);
  }
  let d = c(n, ["embeddingApiType"]);
  if (d === void 0 && (d = "PREDICT"), d === "EMBED_CONTENT") {
    const f = c(e, ["audioTrackExtraction"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "audioTrackExtraction"], f);
  }
  return r;
}
function RW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["contents"]);
  if (s != null) {
    let u = Bb(e, s);
    Array.isArray(u) && (u = u.map((d) => d)), h(r, ["requests[]", "content"], u);
  }
  const a = c(t, ["content"]);
  a != null && mf(zn(a));
  const i = c(t, ["config"]);
  i != null && MW(i, r);
  const l = c(t, ["model"]);
  return l !== void 0 && h(r, ["requests[]", "model"], Xt(e, l)), r;
}
function NW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  let s = c(n, ["embeddingApiType"]);
  if (s === void 0 && (s = "PREDICT"), s === "PREDICT") {
    const l = c(t, ["contents"]);
    if (l != null) {
      let u = Bb(e, l);
      Array.isArray(u) && (u = u.map((d) => d)), h(r, ["instances[]", "content"], u);
    }
  }
  let a = c(n, ["embeddingApiType"]);
  if (a === void 0 && (a = "PREDICT"), a === "EMBED_CONTENT") {
    const l = c(t, ["content"]);
    l != null && h(r, ["content"], Fu(zn(l)));
  }
  const i = c(t, ["config"]);
  return i != null && PW(i, r, n), r;
}
function $W(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["embeddings"]);
  if (o != null) {
    let a = o;
    Array.isArray(a) && (a = a.map((i) => i)), h(n, ["embeddings"], a);
  }
  const s = c(e, ["metadata"]);
  return s != null && h(n, ["metadata"], s), n;
}
function DW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["predictions[]", "embeddings"]);
  if (o != null) {
    let a = o;
    Array.isArray(a) && (a = a.map((i) => pW(i))), h(n, ["embeddings"], a);
  }
  const s = c(e, ["metadata"]);
  if (s != null && h(n, ["metadata"], s), t && c(t, ["embeddingApiType"]) === "EMBED_CONTENT") {
    const a = c(e, ["embedding"]), i = c(e, ["usageMetadata"]), l = c(e, ["truncated"]);
    if (a) {
      const u = {};
      i && i.promptTokenCount && (u.tokenCount = i.promptTokenCount), l && (u.truncated = l), a.statistics = u, h(n, ["embeddings"], [a]);
    }
  }
  return n;
}
function LW(e, t) {
  const n = {}, r = c(e, ["endpoint"]);
  r != null && h(n, ["name"], r);
  const o = c(e, ["deployedModelId"]);
  return o != null && h(n, ["deployedModelId"], o), n;
}
function OW(e, t) {
  const n = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["fileUri"]);
  r != null && h(n, ["fileUri"], r);
  const o = c(e, ["mimeType"]);
  return o != null && h(n, ["mimeType"], o), n;
}
function FW(e, t) {
  const n = {}, r = c(e, ["id"]);
  r != null && h(n, ["id"], r);
  const o = c(e, ["args"]);
  o != null && h(n, ["args"], o);
  const s = c(e, ["name"]);
  if (s != null && h(n, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return n;
}
function BW(e, t) {
  const n = {}, r = c(e, ["allowedFunctionNames"]);
  r != null && h(n, ["allowedFunctionNames"], r);
  const o = c(e, ["mode"]);
  if (o != null && h(n, ["mode"], o), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return n;
}
function UW(e, t) {
  const n = {}, r = c(e, ["description"]);
  r != null && h(n, ["description"], r);
  const o = c(e, ["name"]);
  o != null && h(n, ["name"], o);
  const s = c(e, ["parameters"]);
  s != null && h(n, ["parameters"], s);
  const a = c(e, ["parametersJsonSchema"]);
  a != null && h(n, ["parametersJsonSchema"], a);
  const i = c(e, ["response"]);
  i != null && h(n, ["response"], i);
  const l = c(e, ["responseJsonSchema"]);
  if (l != null && h(n, ["responseJsonSchema"], l), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return n;
}
function qW(e, t, n, r) {
  const o = {}, s = c(t, ["systemInstruction"]);
  n !== void 0 && s != null && h(n, ["systemInstruction"], mf(zn(s)));
  const a = c(t, ["temperature"]);
  a != null && h(o, ["temperature"], a);
  const i = c(t, ["topP"]);
  i != null && h(o, ["topP"], i);
  const l = c(t, ["topK"]);
  l != null && h(o, ["topK"], l);
  const u = c(t, ["candidateCount"]);
  u != null && h(o, ["candidateCount"], u);
  const d = c(t, ["maxOutputTokens"]);
  d != null && h(o, ["maxOutputTokens"], d);
  const f = c(t, ["stopSequences"]);
  f != null && h(o, ["stopSequences"], f);
  const m = c(t, ["responseLogprobs"]);
  m != null && h(o, ["responseLogprobs"], m);
  const v = c(t, ["logprobs"]);
  v != null && h(o, ["logprobs"], v);
  const y = c(t, ["presencePenalty"]);
  y != null && h(o, ["presencePenalty"], y);
  const T = c(t, ["frequencyPenalty"]);
  T != null && h(o, ["frequencyPenalty"], T);
  const C = c(t, ["seed"]);
  C != null && h(o, ["seed"], C);
  const A = c(t, ["responseMimeType"]);
  A != null && h(o, ["responseMimeType"], A);
  const I = c(t, ["responseSchema"]);
  I != null && h(o, ["responseSchema"], Ub(I));
  const R = c(t, ["responseJsonSchema"]);
  if (R != null && h(o, ["responseJsonSchema"], R), c(t, ["routingConfig"]) !== void 0) throw new Error("routingConfig parameter is not supported in Gemini API.");
  if (c(t, ["modelSelectionConfig"]) !== void 0) throw new Error("modelSelectionConfig parameter is not supported in Gemini API.");
  const $ = c(t, ["safetySettings"]);
  if (n !== void 0 && $ != null) {
    let G = $;
    Array.isArray(G) && (G = G.map((oe) => RG(oe))), h(n, ["safetySettings"], G);
  }
  const p = c(t, ["tools"]);
  if (n !== void 0 && p != null) {
    let G = Lu(p);
    Array.isArray(G) && (G = G.map((oe) => UG(Du(oe)))), h(n, ["tools"], G);
  }
  const b = c(t, ["toolConfig"]);
  if (n !== void 0 && b != null && h(n, ["toolConfig"], FG(b)), c(t, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const w = c(t, ["cachedContent"]);
  n !== void 0 && w != null && h(n, ["cachedContent"], na(e, w));
  const E = c(t, ["responseModalities"]);
  E != null && h(o, ["responseModalities"], E);
  const x = c(t, ["mediaResolution"]);
  x != null && h(o, ["mediaResolution"], x);
  const M = c(t, ["speechConfig"]);
  if (M != null && h(o, ["speechConfig"], qb(M)), c(t, ["audioTimestamp"]) !== void 0) throw new Error("audioTimestamp parameter is not supported in Gemini API.");
  const N = c(t, ["thinkingConfig"]);
  N != null && h(o, ["thinkingConfig"], N);
  const q = c(t, ["imageConfig"]);
  q != null && h(o, ["imageConfig"], pG(q));
  const z = c(t, ["enableEnhancedCivicAnswers"]);
  if (z != null && h(o, ["enableEnhancedCivicAnswers"], z), c(t, ["modelArmorConfig"]) !== void 0) throw new Error("modelArmorConfig parameter is not supported in Gemini API.");
  const J = c(t, ["serviceTier"]);
  return n !== void 0 && J != null && h(n, ["serviceTier"], J), o;
}
function HW(e, t, n, r) {
  const o = {}, s = c(t, ["systemInstruction"]);
  n !== void 0 && s != null && h(n, ["systemInstruction"], Fu(zn(s)));
  const a = c(t, ["temperature"]);
  a != null && h(o, ["temperature"], a);
  const i = c(t, ["topP"]);
  i != null && h(o, ["topP"], i);
  const l = c(t, ["topK"]);
  l != null && h(o, ["topK"], l);
  const u = c(t, ["candidateCount"]);
  u != null && h(o, ["candidateCount"], u);
  const d = c(t, ["maxOutputTokens"]);
  d != null && h(o, ["maxOutputTokens"], d);
  const f = c(t, ["stopSequences"]);
  f != null && h(o, ["stopSequences"], f);
  const m = c(t, ["responseLogprobs"]);
  m != null && h(o, ["responseLogprobs"], m);
  const v = c(t, ["logprobs"]);
  v != null && h(o, ["logprobs"], v);
  const y = c(t, ["presencePenalty"]);
  y != null && h(o, ["presencePenalty"], y);
  const T = c(t, ["frequencyPenalty"]);
  T != null && h(o, ["frequencyPenalty"], T);
  const C = c(t, ["seed"]);
  C != null && h(o, ["seed"], C);
  const A = c(t, ["responseMimeType"]);
  A != null && h(o, ["responseMimeType"], A);
  const I = c(t, ["responseSchema"]);
  I != null && h(o, ["responseSchema"], Ub(I));
  const R = c(t, ["responseJsonSchema"]);
  R != null && h(o, ["responseJsonSchema"], R);
  const $ = c(t, ["routingConfig"]);
  $ != null && h(o, ["routingConfig"], $);
  const p = c(t, ["modelSelectionConfig"]);
  p != null && h(o, ["modelConfig"], p);
  const b = c(t, ["safetySettings"]);
  if (n !== void 0 && b != null) {
    let de = b;
    Array.isArray(de) && (de = de.map((fe) => fe)), h(n, ["safetySettings"], de);
  }
  const w = c(t, ["tools"]);
  if (n !== void 0 && w != null) {
    let de = Lu(w);
    Array.isArray(de) && (de = de.map((fe) => B3(Du(fe)))), h(n, ["tools"], de);
  }
  const E = c(t, ["toolConfig"]);
  n !== void 0 && E != null && h(n, ["toolConfig"], BG(E));
  const x = c(t, ["labels"]);
  n !== void 0 && x != null && h(n, ["labels"], x);
  const M = c(t, ["cachedContent"]);
  n !== void 0 && M != null && h(n, ["cachedContent"], na(e, M));
  const N = c(t, ["responseModalities"]);
  N != null && h(o, ["responseModalities"], N);
  const q = c(t, ["mediaResolution"]);
  q != null && h(o, ["mediaResolution"], q);
  const z = c(t, ["speechConfig"]);
  z != null && h(o, ["speechConfig"], qb(z));
  const J = c(t, ["audioTimestamp"]);
  J != null && h(o, ["audioTimestamp"], J);
  const G = c(t, ["thinkingConfig"]);
  G != null && h(o, ["thinkingConfig"], G);
  const oe = c(t, ["imageConfig"]);
  if (oe != null && h(o, ["imageConfig"], mG(oe)), c(t, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  const B = c(t, ["modelArmorConfig"]);
  n !== void 0 && B != null && h(n, ["modelArmorConfig"], B);
  const X = c(t, ["serviceTier"]);
  return n !== void 0 && X != null && h(n, ["serviceTier"], X), o;
}
function uA(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["contents"]);
  if (s != null) {
    let i = kr(s);
    Array.isArray(i) && (i = i.map((l) => mf(l))), h(r, ["contents"], i);
  }
  const a = c(t, ["config"]);
  return a != null && h(r, ["generationConfig"], qW(e, a, r)), r;
}
function cA(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["contents"]);
  if (s != null) {
    let i = kr(s);
    Array.isArray(i) && (i = i.map((l) => Fu(l))), h(r, ["contents"], i);
  }
  const a = c(t, ["config"]);
  return a != null && h(r, ["generationConfig"], HW(e, a, r)), r;
}
function dA(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["candidates"]);
  if (o != null) {
    let d = o;
    Array.isArray(d) && (d = d.map((f) => cW(f))), h(n, ["candidates"], d);
  }
  const s = c(e, ["modelVersion"]);
  s != null && h(n, ["modelVersion"], s);
  const a = c(e, ["promptFeedback"]);
  a != null && h(n, ["promptFeedback"], a);
  const i = c(e, ["responseId"]);
  i != null && h(n, ["responseId"], i);
  const l = c(e, ["usageMetadata"]);
  l != null && h(n, ["usageMetadata"], l);
  const u = c(e, ["modelStatus"]);
  return u != null && h(n, ["modelStatus"], u), n;
}
function fA(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["candidates"]);
  if (o != null) {
    let d = o;
    Array.isArray(d) && (d = d.map((f) => f)), h(n, ["candidates"], d);
  }
  const s = c(e, ["createTime"]);
  s != null && h(n, ["createTime"], s);
  const a = c(e, ["modelVersion"]);
  a != null && h(n, ["modelVersion"], a);
  const i = c(e, ["promptFeedback"]);
  i != null && h(n, ["promptFeedback"], i);
  const l = c(e, ["responseId"]);
  l != null && h(n, ["responseId"], l);
  const u = c(e, ["usageMetadata"]);
  return u != null && h(n, ["usageMetadata"], u), n;
}
function zW(e, t, n) {
  const r = {};
  if (c(e, ["outputGcsUri"]) !== void 0) throw new Error("outputGcsUri parameter is not supported in Gemini API.");
  if (c(e, ["negativePrompt"]) !== void 0) throw new Error("negativePrompt parameter is not supported in Gemini API.");
  const o = c(e, ["numberOfImages"]);
  t !== void 0 && o != null && h(t, ["parameters", "sampleCount"], o);
  const s = c(e, ["aspectRatio"]);
  t !== void 0 && s != null && h(t, ["parameters", "aspectRatio"], s);
  const a = c(e, ["guidanceScale"]);
  if (t !== void 0 && a != null && h(t, ["parameters", "guidanceScale"], a), c(e, ["seed"]) !== void 0) throw new Error("seed parameter is not supported in Gemini API.");
  const i = c(e, ["safetyFilterLevel"]);
  t !== void 0 && i != null && h(t, ["parameters", "safetySetting"], i);
  const l = c(e, ["personGeneration"]);
  t !== void 0 && l != null && h(t, ["parameters", "personGeneration"], l);
  const u = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && u != null && h(t, ["parameters", "includeSafetyAttributes"], u);
  const d = c(e, ["includeRaiReason"]);
  t !== void 0 && d != null && h(t, ["parameters", "includeRaiReason"], d);
  const f = c(e, ["language"]);
  t !== void 0 && f != null && h(t, ["parameters", "language"], f);
  const m = c(e, ["outputMimeType"]);
  t !== void 0 && m != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], m);
  const v = c(e, ["outputCompressionQuality"]);
  if (t !== void 0 && v != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], v), c(e, ["addWatermark"]) !== void 0) throw new Error("addWatermark parameter is not supported in Gemini API.");
  if (c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const y = c(e, ["imageSize"]);
  if (t !== void 0 && y != null && h(t, ["parameters", "sampleImageSize"], y), c(e, ["enhancePrompt"]) !== void 0) throw new Error("enhancePrompt parameter is not supported in Gemini API.");
  return r;
}
function WW(e, t, n) {
  const r = {}, o = c(e, ["outputGcsUri"]);
  t !== void 0 && o != null && h(t, ["parameters", "storageUri"], o);
  const s = c(e, ["negativePrompt"]);
  t !== void 0 && s != null && h(t, ["parameters", "negativePrompt"], s);
  const a = c(e, ["numberOfImages"]);
  t !== void 0 && a != null && h(t, ["parameters", "sampleCount"], a);
  const i = c(e, ["aspectRatio"]);
  t !== void 0 && i != null && h(t, ["parameters", "aspectRatio"], i);
  const l = c(e, ["guidanceScale"]);
  t !== void 0 && l != null && h(t, ["parameters", "guidanceScale"], l);
  const u = c(e, ["seed"]);
  t !== void 0 && u != null && h(t, ["parameters", "seed"], u);
  const d = c(e, ["safetyFilterLevel"]);
  t !== void 0 && d != null && h(t, ["parameters", "safetySetting"], d);
  const f = c(e, ["personGeneration"]);
  t !== void 0 && f != null && h(t, ["parameters", "personGeneration"], f);
  const m = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && m != null && h(t, ["parameters", "includeSafetyAttributes"], m);
  const v = c(e, ["includeRaiReason"]);
  t !== void 0 && v != null && h(t, ["parameters", "includeRaiReason"], v);
  const y = c(e, ["language"]);
  t !== void 0 && y != null && h(t, ["parameters", "language"], y);
  const T = c(e, ["outputMimeType"]);
  t !== void 0 && T != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], T);
  const C = c(e, ["outputCompressionQuality"]);
  t !== void 0 && C != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], C);
  const A = c(e, ["addWatermark"]);
  t !== void 0 && A != null && h(t, ["parameters", "addWatermark"], A);
  const I = c(e, ["labels"]);
  t !== void 0 && I != null && h(t, ["labels"], I);
  const R = c(e, ["imageSize"]);
  t !== void 0 && R != null && h(t, ["parameters", "sampleImageSize"], R);
  const $ = c(e, ["enhancePrompt"]);
  return t !== void 0 && $ != null && h(t, ["parameters", "enhancePrompt"], $), r;
}
function GW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["prompt"]);
  s != null && h(r, ["instances[0]", "prompt"], s);
  const a = c(t, ["config"]);
  return a != null && zW(a, r), r;
}
function VW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["prompt"]);
  s != null && h(r, ["instances[0]", "prompt"], s);
  const a = c(t, ["config"]);
  return a != null && WW(a, r), r;
}
function KW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["predictions"]);
  if (o != null) {
    let a = o;
    Array.isArray(a) && (a = a.map((i) => sG(i))), h(n, ["generatedImages"], a);
  }
  const s = c(e, ["positivePromptSafetyAttributes"]);
  return s != null && h(n, ["positivePromptSafetyAttributes"], O3(s)), n;
}
function JW(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["predictions"]);
  if (o != null) {
    let a = o;
    Array.isArray(a) && (a = a.map((i) => _g(i))), h(n, ["generatedImages"], a);
  }
  const s = c(e, ["positivePromptSafetyAttributes"]);
  return s != null && h(n, ["positivePromptSafetyAttributes"], F3(s)), n;
}
function YW(e, t, n) {
  const r = {}, o = c(e, ["numberOfVideos"]);
  if (t !== void 0 && o != null && h(t, ["parameters", "sampleCount"], o), c(e, ["outputGcsUri"]) !== void 0) throw new Error("outputGcsUri parameter is not supported in Gemini API.");
  if (c(e, ["fps"]) !== void 0) throw new Error("fps parameter is not supported in Gemini API.");
  const s = c(e, ["durationSeconds"]);
  if (t !== void 0 && s != null && h(t, ["parameters", "durationSeconds"], s), c(e, ["seed"]) !== void 0) throw new Error("seed parameter is not supported in Gemini API.");
  const a = c(e, ["aspectRatio"]);
  t !== void 0 && a != null && h(t, ["parameters", "aspectRatio"], a);
  const i = c(e, ["resolution"]);
  t !== void 0 && i != null && h(t, ["parameters", "resolution"], i);
  const l = c(e, ["personGeneration"]);
  if (t !== void 0 && l != null && h(t, ["parameters", "personGeneration"], l), c(e, ["pubsubTopic"]) !== void 0) throw new Error("pubsubTopic parameter is not supported in Gemini API.");
  const u = c(e, ["negativePrompt"]);
  t !== void 0 && u != null && h(t, ["parameters", "negativePrompt"], u);
  const d = c(e, ["enhancePrompt"]);
  if (t !== void 0 && d != null && h(t, ["parameters", "enhancePrompt"], d), c(e, ["generateAudio"]) !== void 0) throw new Error("generateAudio parameter is not supported in Gemini API.");
  const f = c(e, ["lastFrame"]);
  t !== void 0 && f != null && h(t, ["instances[0]", "lastFrame"], bg(f));
  const m = c(e, ["referenceImages"]);
  if (t !== void 0 && m != null) {
    let y = m;
    Array.isArray(y) && (y = y.map((T) => jG(T))), h(t, ["instances[0]", "referenceImages"], y);
  }
  if (c(e, ["mask"]) !== void 0) throw new Error("mask parameter is not supported in Gemini API.");
  if (c(e, ["compressionQuality"]) !== void 0) throw new Error("compressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const v = c(e, ["webhookConfig"]);
  return t !== void 0 && v != null && h(t, ["webhookConfig"], v), r;
}
function XW(e, t, n) {
  const r = {}, o = c(e, ["numberOfVideos"]);
  t !== void 0 && o != null && h(t, ["parameters", "sampleCount"], o);
  const s = c(e, ["outputGcsUri"]);
  t !== void 0 && s != null && h(t, ["parameters", "storageUri"], s);
  const a = c(e, ["fps"]);
  t !== void 0 && a != null && h(t, ["parameters", "fps"], a);
  const i = c(e, ["durationSeconds"]);
  t !== void 0 && i != null && h(t, ["parameters", "durationSeconds"], i);
  const l = c(e, ["seed"]);
  t !== void 0 && l != null && h(t, ["parameters", "seed"], l);
  const u = c(e, ["aspectRatio"]);
  t !== void 0 && u != null && h(t, ["parameters", "aspectRatio"], u);
  const d = c(e, ["resolution"]);
  t !== void 0 && d != null && h(t, ["parameters", "resolution"], d);
  const f = c(e, ["personGeneration"]);
  t !== void 0 && f != null && h(t, ["parameters", "personGeneration"], f);
  const m = c(e, ["pubsubTopic"]);
  t !== void 0 && m != null && h(t, ["parameters", "pubsubTopic"], m);
  const v = c(e, ["negativePrompt"]);
  t !== void 0 && v != null && h(t, ["parameters", "negativePrompt"], v);
  const y = c(e, ["enhancePrompt"]);
  t !== void 0 && y != null && h(t, ["parameters", "enhancePrompt"], y);
  const T = c(e, ["generateAudio"]);
  t !== void 0 && T != null && h(t, ["parameters", "generateAudio"], T);
  const C = c(e, ["lastFrame"]);
  t !== void 0 && C != null && h(t, ["instances[0]", "lastFrame"], Uo(C));
  const A = c(e, ["referenceImages"]);
  if (t !== void 0 && A != null) {
    let p = A;
    Array.isArray(p) && (p = p.map((b) => eV(b))), h(t, ["instances[0]", "referenceImages"], p);
  }
  const I = c(e, ["mask"]);
  t !== void 0 && I != null && h(t, ["instances[0]", "mask"], ZG(I));
  const R = c(e, ["compressionQuality"]);
  t !== void 0 && R != null && h(t, ["parameters", "compressionQuality"], R);
  const $ = c(e, ["labels"]);
  if (t !== void 0 && $ != null && h(t, ["labels"], $), c(e, ["webhookConfig"]) !== void 0) throw new Error("webhookConfig parameter is not supported in Vertex AI.");
  return r;
}
function QW(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["name"], r);
  const o = c(e, ["metadata"]);
  o != null && h(n, ["metadata"], o);
  const s = c(e, ["done"]);
  s != null && h(n, ["done"], s);
  const a = c(e, ["error"]);
  a != null && h(n, ["error"], a);
  const i = c(e, ["response", "generateVideoResponse"]);
  return i != null && h(n, ["response"], tG(i)), n;
}
function ZW(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["name"], r);
  const o = c(e, ["metadata"]);
  o != null && h(n, ["metadata"], o);
  const s = c(e, ["done"]);
  s != null && h(n, ["done"], s);
  const a = c(e, ["error"]);
  a != null && h(n, ["error"], a);
  const i = c(e, ["response"]);
  return i != null && h(n, ["response"], nG(i)), n;
}
function jW(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["prompt"]);
  s != null && h(r, ["instances[0]", "prompt"], s);
  const a = c(t, ["image"]);
  a != null && h(r, ["instances[0]", "image"], bg(a));
  const i = c(t, ["video"]);
  i != null && h(r, ["instances[0]", "video"], U3(i));
  const l = c(t, ["source"]);
  l != null && rG(l, r);
  const u = c(t, ["config"]);
  return u != null && YW(u, r), r;
}
function eG(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["prompt"]);
  s != null && h(r, ["instances[0]", "prompt"], s);
  const a = c(t, ["image"]);
  a != null && h(r, ["instances[0]", "image"], Uo(a));
  const i = c(t, ["video"]);
  i != null && h(r, ["instances[0]", "video"], q3(i));
  const l = c(t, ["source"]);
  l != null && oG(l, r);
  const u = c(t, ["config"]);
  return u != null && XW(u, r), r;
}
function tG(e, t) {
  const n = {}, r = c(e, ["generatedSamples"]);
  if (r != null) {
    let a = r;
    Array.isArray(a) && (a = a.map((i) => iG(i))), h(n, ["generatedVideos"], a);
  }
  const o = c(e, ["raiMediaFilteredCount"]);
  o != null && h(n, ["raiMediaFilteredCount"], o);
  const s = c(e, ["raiMediaFilteredReasons"]);
  return s != null && h(n, ["raiMediaFilteredReasons"], s), n;
}
function nG(e, t) {
  const n = {}, r = c(e, ["videos"]);
  if (r != null) {
    let a = r;
    Array.isArray(a) && (a = a.map((i) => lG(i))), h(n, ["generatedVideos"], a);
  }
  const o = c(e, ["raiMediaFilteredCount"]);
  o != null && h(n, ["raiMediaFilteredCount"], o);
  const s = c(e, ["raiMediaFilteredReasons"]);
  return s != null && h(n, ["raiMediaFilteredReasons"], s), n;
}
function rG(e, t, n) {
  const r = {}, o = c(e, ["prompt"]);
  t !== void 0 && o != null && h(t, ["instances[0]", "prompt"], o);
  const s = c(e, ["image"]);
  t !== void 0 && s != null && h(t, ["instances[0]", "image"], bg(s));
  const a = c(e, ["video"]);
  return t !== void 0 && a != null && h(t, ["instances[0]", "video"], U3(a)), r;
}
function oG(e, t, n) {
  const r = {}, o = c(e, ["prompt"]);
  t !== void 0 && o != null && h(t, ["instances[0]", "prompt"], o);
  const s = c(e, ["image"]);
  t !== void 0 && s != null && h(t, ["instances[0]", "image"], Uo(s));
  const a = c(e, ["video"]);
  return t !== void 0 && a != null && h(t, ["instances[0]", "video"], q3(a)), r;
}
function sG(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && h(n, ["image"], gG(r));
  const o = c(e, ["raiFilteredReason"]);
  o != null && h(n, ["raiFilteredReason"], o);
  const s = c(e, ["_self"]);
  return s != null && h(n, ["safetyAttributes"], O3(s)), n;
}
function _g(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && h(n, ["image"], L3(r));
  const o = c(e, ["raiFilteredReason"]);
  o != null && h(n, ["raiFilteredReason"], o);
  const s = c(e, ["_self"]);
  s != null && h(n, ["safetyAttributes"], F3(s));
  const a = c(e, ["prompt"]);
  return a != null && h(n, ["enhancedPrompt"], a), n;
}
function aG(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && h(n, ["mask"], L3(r));
  const o = c(e, ["labels"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => a)), h(n, ["labels"], s);
  }
  return n;
}
function iG(e, t) {
  const n = {}, r = c(e, ["video"]);
  return r != null && h(n, ["video"], XG(r)), n;
}
function lG(e, t) {
  const n = {}, r = c(e, ["_self"]);
  return r != null && h(n, ["video"], QG(r)), n;
}
function uG(e, t) {
  const n = {}, r = c(e, ["modelSelectionConfig"]);
  r != null && h(n, ["modelConfig"], r);
  const o = c(e, ["responseJsonSchema"]);
  o != null && h(n, ["responseJsonSchema"], o);
  const s = c(e, ["audioTimestamp"]);
  s != null && h(n, ["audioTimestamp"], s);
  const a = c(e, ["candidateCount"]);
  a != null && h(n, ["candidateCount"], a);
  const i = c(e, ["enableAffectiveDialog"]);
  i != null && h(n, ["enableAffectiveDialog"], i);
  const l = c(e, ["frequencyPenalty"]);
  l != null && h(n, ["frequencyPenalty"], l);
  const u = c(e, ["logprobs"]);
  u != null && h(n, ["logprobs"], u);
  const d = c(e, ["maxOutputTokens"]);
  d != null && h(n, ["maxOutputTokens"], d);
  const f = c(e, ["mediaResolution"]);
  f != null && h(n, ["mediaResolution"], f);
  const m = c(e, ["presencePenalty"]);
  m != null && h(n, ["presencePenalty"], m);
  const v = c(e, ["responseLogprobs"]);
  v != null && h(n, ["responseLogprobs"], v);
  const y = c(e, ["responseMimeType"]);
  y != null && h(n, ["responseMimeType"], y);
  const T = c(e, ["responseModalities"]);
  T != null && h(n, ["responseModalities"], T);
  const C = c(e, ["responseSchema"]);
  C != null && h(n, ["responseSchema"], C);
  const A = c(e, ["routingConfig"]);
  A != null && h(n, ["routingConfig"], A);
  const I = c(e, ["seed"]);
  I != null && h(n, ["seed"], I);
  const R = c(e, ["speechConfig"]);
  R != null && h(n, ["speechConfig"], R);
  const $ = c(e, ["stopSequences"]);
  $ != null && h(n, ["stopSequences"], $);
  const p = c(e, ["temperature"]);
  p != null && h(n, ["temperature"], p);
  const b = c(e, ["thinkingConfig"]);
  b != null && h(n, ["thinkingConfig"], b);
  const w = c(e, ["topK"]);
  w != null && h(n, ["topK"], w);
  const E = c(e, ["topP"]);
  if (E != null && h(n, ["topP"], E), c(e, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  return n;
}
function cG(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  return o != null && h(r, ["_url", "name"], Xt(e, o)), r;
}
function dG(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  return o != null && h(r, ["_url", "name"], Xt(e, o)), r;
}
function fG(e, t) {
  const n = {}, r = c(e, ["authConfig"]);
  r != null && h(n, ["authConfig"], lW(r));
  const o = c(e, ["enableWidget"]);
  return o != null && h(n, ["enableWidget"], o), n;
}
function hG(e, t) {
  const n = {}, r = c(e, ["searchTypes"]);
  if (r != null && h(n, ["searchTypes"], r), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const o = c(e, ["timeRangeFilter"]);
  return o != null && h(n, ["timeRangeFilter"], o), n;
}
function pG(e, t) {
  const n = {}, r = c(e, ["aspectRatio"]);
  r != null && h(n, ["aspectRatio"], r);
  const o = c(e, ["imageSize"]);
  if (o != null && h(n, ["imageSize"], o), c(e, ["personGeneration"]) !== void 0) throw new Error("personGeneration parameter is not supported in Gemini API.");
  if (c(e, ["prominentPeople"]) !== void 0) throw new Error("prominentPeople parameter is not supported in Gemini API.");
  if (c(e, ["outputMimeType"]) !== void 0) throw new Error("outputMimeType parameter is not supported in Gemini API.");
  if (c(e, ["outputCompressionQuality"]) !== void 0) throw new Error("outputCompressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["imageOutputOptions"]) !== void 0) throw new Error("imageOutputOptions parameter is not supported in Gemini API.");
  return n;
}
function mG(e, t) {
  const n = {}, r = c(e, ["aspectRatio"]);
  r != null && h(n, ["aspectRatio"], r);
  const o = c(e, ["imageSize"]);
  o != null && h(n, ["imageSize"], o);
  const s = c(e, ["personGeneration"]);
  s != null && h(n, ["personGeneration"], s);
  const a = c(e, ["prominentPeople"]);
  a != null && h(n, ["prominentPeople"], a);
  const i = c(e, ["outputMimeType"]);
  i != null && h(n, ["imageOutputOptions", "mimeType"], i);
  const l = c(e, ["outputCompressionQuality"]);
  l != null && h(n, ["imageOutputOptions", "compressionQuality"], l);
  const u = c(e, ["imageOutputOptions"]);
  return u != null && h(n, ["imageOutputOptions"], u), n;
}
function gG(e, t) {
  const n = {}, r = c(e, ["bytesBase64Encoded"]);
  r != null && h(n, ["imageBytes"], Ga(r));
  const o = c(e, ["mimeType"]);
  return o != null && h(n, ["mimeType"], o), n;
}
function L3(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && h(n, ["gcsUri"], r);
  const o = c(e, ["bytesBase64Encoded"]);
  o != null && h(n, ["imageBytes"], Ga(o));
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function bg(e, t) {
  const n = {};
  if (c(e, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  const r = c(e, ["imageBytes"]);
  r != null && h(n, ["bytesBase64Encoded"], Ga(r));
  const o = c(e, ["mimeType"]);
  return o != null && h(n, ["mimeType"], o), n;
}
function Uo(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && h(n, ["gcsUri"], r);
  const o = c(e, ["imageBytes"]);
  o != null && h(n, ["bytesBase64Encoded"], Ga(o));
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function vG(e, t, n, r) {
  const o = {}, s = c(t, ["pageSize"]);
  n !== void 0 && s != null && h(n, ["_query", "pageSize"], s);
  const a = c(t, ["pageToken"]);
  n !== void 0 && a != null && h(n, ["_query", "pageToken"], a);
  const i = c(t, ["filter"]);
  n !== void 0 && i != null && h(n, ["_query", "filter"], i);
  const l = c(t, ["queryBase"]);
  return n !== void 0 && l != null && h(n, ["_url", "models_url"], k3(e, l)), o;
}
function yG(e, t, n, r) {
  const o = {}, s = c(t, ["pageSize"]);
  n !== void 0 && s != null && h(n, ["_query", "pageSize"], s);
  const a = c(t, ["pageToken"]);
  n !== void 0 && a != null && h(n, ["_query", "pageToken"], a);
  const i = c(t, ["filter"]);
  n !== void 0 && i != null && h(n, ["_query", "filter"], i);
  const l = c(t, ["queryBase"]);
  return n !== void 0 && l != null && h(n, ["_url", "models_url"], k3(e, l)), o;
}
function _G(e, t, n) {
  const r = {}, o = c(t, ["config"]);
  return o != null && vG(e, o, r), r;
}
function bG(e, t, n) {
  const r = {}, o = c(t, ["config"]);
  return o != null && yG(e, o, r), r;
}
function wG(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["nextPageToken"]);
  o != null && h(n, ["nextPageToken"], o);
  const s = c(e, ["_self"]);
  if (s != null) {
    let a = I3(s);
    Array.isArray(a) && (a = a.map((i) => w1(i))), h(n, ["models"], a);
  }
  return n;
}
function SG(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["nextPageToken"]);
  o != null && h(n, ["nextPageToken"], o);
  const s = c(e, ["_self"]);
  if (s != null) {
    let a = I3(s);
    Array.isArray(a) && (a = a.map((i) => S1(i))), h(n, ["models"], a);
  }
  return n;
}
function TG(e, t) {
  const n = {}, r = c(e, ["maskMode"]);
  r != null && h(n, ["maskMode"], r);
  const o = c(e, ["segmentationClasses"]);
  o != null && h(n, ["maskClasses"], o);
  const s = c(e, ["maskDilation"]);
  return s != null && h(n, ["dilation"], s), n;
}
function w1(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["name"], r);
  const o = c(e, ["displayName"]);
  o != null && h(n, ["displayName"], o);
  const s = c(e, ["description"]);
  s != null && h(n, ["description"], s);
  const a = c(e, ["version"]);
  a != null && h(n, ["version"], a);
  const i = c(e, ["_self"]);
  i != null && h(n, ["tunedModelInfo"], qG(i));
  const l = c(e, ["inputTokenLimit"]);
  l != null && h(n, ["inputTokenLimit"], l);
  const u = c(e, ["outputTokenLimit"]);
  u != null && h(n, ["outputTokenLimit"], u);
  const d = c(e, ["supportedGenerationMethods"]);
  d != null && h(n, ["supportedActions"], d);
  const f = c(e, ["temperature"]);
  f != null && h(n, ["temperature"], f);
  const m = c(e, ["maxTemperature"]);
  m != null && h(n, ["maxTemperature"], m);
  const v = c(e, ["topP"]);
  v != null && h(n, ["topP"], v);
  const y = c(e, ["topK"]);
  y != null && h(n, ["topK"], y);
  const T = c(e, ["thinking"]);
  return T != null && h(n, ["thinking"], T), n;
}
function S1(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["name"], r);
  const o = c(e, ["displayName"]);
  o != null && h(n, ["displayName"], o);
  const s = c(e, ["description"]);
  s != null && h(n, ["description"], s);
  const a = c(e, ["versionId"]);
  a != null && h(n, ["version"], a);
  const i = c(e, ["deployedModels"]);
  if (i != null) {
    let m = i;
    Array.isArray(m) && (m = m.map((v) => LW(v))), h(n, ["endpoints"], m);
  }
  const l = c(e, ["labels"]);
  l != null && h(n, ["labels"], l);
  const u = c(e, ["_self"]);
  u != null && h(n, ["tunedModelInfo"], HG(u));
  const d = c(e, ["defaultCheckpointId"]);
  d != null && h(n, ["defaultCheckpointId"], d);
  const f = c(e, ["checkpoints"]);
  if (f != null) {
    let m = f;
    Array.isArray(m) && (m = m.map((v) => v)), h(n, ["checkpoints"], m);
  }
  return n;
}
function EG(e, t) {
  const n = {}, r = c(e, ["mediaResolution"]);
  r != null && h(n, ["mediaResolution"], r);
  const o = c(e, ["codeExecutionResult"]);
  o != null && h(n, ["codeExecutionResult"], o);
  const s = c(e, ["executableCode"]);
  s != null && h(n, ["executableCode"], s);
  const a = c(e, ["fileData"]);
  a != null && h(n, ["fileData"], OW(a));
  const i = c(e, ["functionCall"]);
  i != null && h(n, ["functionCall"], FW(i));
  const l = c(e, ["functionResponse"]);
  l != null && h(n, ["functionResponse"], l);
  const u = c(e, ["inlineData"]);
  u != null && h(n, ["inlineData"], uW(u));
  const d = c(e, ["text"]);
  d != null && h(n, ["text"], d);
  const f = c(e, ["thought"]);
  f != null && h(n, ["thought"], f);
  const m = c(e, ["thoughtSignature"]);
  m != null && h(n, ["thoughtSignature"], m);
  const v = c(e, ["videoMetadata"]);
  v != null && h(n, ["videoMetadata"], v);
  const y = c(e, ["toolCall"]);
  y != null && h(n, ["toolCall"], y);
  const T = c(e, ["toolResponse"]);
  T != null && h(n, ["toolResponse"], T);
  const C = c(e, ["partMetadata"]);
  return C != null && h(n, ["partMetadata"], C), n;
}
function AG(e, t) {
  const n = {}, r = c(e, ["mediaResolution"]);
  r != null && h(n, ["mediaResolution"], r);
  const o = c(e, ["codeExecutionResult"]);
  o != null && h(n, ["codeExecutionResult"], o);
  const s = c(e, ["executableCode"]);
  s != null && h(n, ["executableCode"], s);
  const a = c(e, ["fileData"]);
  a != null && h(n, ["fileData"], a);
  const i = c(e, ["functionCall"]);
  i != null && h(n, ["functionCall"], i);
  const l = c(e, ["functionResponse"]);
  l != null && h(n, ["functionResponse"], l);
  const u = c(e, ["inlineData"]);
  u != null && h(n, ["inlineData"], u);
  const d = c(e, ["text"]);
  d != null && h(n, ["text"], d);
  const f = c(e, ["thought"]);
  f != null && h(n, ["thought"], f);
  const m = c(e, ["thoughtSignature"]);
  m != null && h(n, ["thoughtSignature"], m);
  const v = c(e, ["videoMetadata"]);
  if (v != null && h(n, ["videoMetadata"], v), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return n;
}
function CG(e, t) {
  const n = {}, r = c(e, ["productImage"]);
  return r != null && h(n, ["image"], Uo(r)), n;
}
function kG(e, t, n) {
  const r = {}, o = c(e, ["numberOfImages"]);
  t !== void 0 && o != null && h(t, ["parameters", "sampleCount"], o);
  const s = c(e, ["baseSteps"]);
  t !== void 0 && s != null && h(t, ["parameters", "baseSteps"], s);
  const a = c(e, ["outputGcsUri"]);
  t !== void 0 && a != null && h(t, ["parameters", "storageUri"], a);
  const i = c(e, ["seed"]);
  t !== void 0 && i != null && h(t, ["parameters", "seed"], i);
  const l = c(e, ["safetyFilterLevel"]);
  t !== void 0 && l != null && h(t, ["parameters", "safetySetting"], l);
  const u = c(e, ["personGeneration"]);
  t !== void 0 && u != null && h(t, ["parameters", "personGeneration"], u);
  const d = c(e, ["addWatermark"]);
  t !== void 0 && d != null && h(t, ["parameters", "addWatermark"], d);
  const f = c(e, ["outputMimeType"]);
  t !== void 0 && f != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], f);
  const m = c(e, ["outputCompressionQuality"]);
  t !== void 0 && m != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], m);
  const v = c(e, ["enhancePrompt"]);
  t !== void 0 && v != null && h(t, ["parameters", "enhancePrompt"], v);
  const y = c(e, ["labels"]);
  return t !== void 0 && y != null && h(t, ["labels"], y), r;
}
function IG(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["source"]);
  s != null && MG(s, r);
  const a = c(t, ["config"]);
  return a != null && kG(a, r), r;
}
function xG(e, t) {
  const n = {}, r = c(e, ["predictions"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((s) => _g(s))), h(n, ["generatedImages"], o);
  }
  return n;
}
function MG(e, t, n) {
  const r = {}, o = c(e, ["prompt"]);
  t !== void 0 && o != null && h(t, ["instances[0]", "prompt"], o);
  const s = c(e, ["personImage"]);
  t !== void 0 && s != null && h(t, [
    "instances[0]",
    "personImage",
    "image"
  ], Uo(s));
  const a = c(e, ["productImages"]);
  if (t !== void 0 && a != null) {
    let i = a;
    Array.isArray(i) && (i = i.map((l) => CG(l))), h(t, ["instances[0]", "productImages"], i);
  }
  return r;
}
function PG(e, t) {
  const n = {}, r = c(e, ["referenceImage"]);
  r != null && h(n, ["referenceImage"], Uo(r));
  const o = c(e, ["referenceId"]);
  o != null && h(n, ["referenceId"], o);
  const s = c(e, ["referenceType"]);
  s != null && h(n, ["referenceType"], s);
  const a = c(e, ["maskImageConfig"]);
  a != null && h(n, ["maskImageConfig"], TG(a));
  const i = c(e, ["controlImageConfig"]);
  i != null && h(n, ["controlImageConfig"], gW(i));
  const l = c(e, ["styleImageConfig"]);
  l != null && h(n, ["styleImageConfig"], l);
  const u = c(e, ["subjectImageConfig"]);
  return u != null && h(n, ["subjectImageConfig"], u), n;
}
function O3(e, t) {
  const n = {}, r = c(e, ["safetyAttributes", "categories"]);
  r != null && h(n, ["categories"], r);
  const o = c(e, ["safetyAttributes", "scores"]);
  o != null && h(n, ["scores"], o);
  const s = c(e, ["contentType"]);
  return s != null && h(n, ["contentType"], s), n;
}
function F3(e, t) {
  const n = {}, r = c(e, ["safetyAttributes", "categories"]);
  r != null && h(n, ["categories"], r);
  const o = c(e, ["safetyAttributes", "scores"]);
  o != null && h(n, ["scores"], o);
  const s = c(e, ["contentType"]);
  return s != null && h(n, ["contentType"], s), n;
}
function RG(e, t) {
  const n = {}, r = c(e, ["category"]);
  if (r != null && h(n, ["category"], r), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const o = c(e, ["threshold"]);
  return o != null && h(n, ["threshold"], o), n;
}
function NG(e, t) {
  const n = {}, r = c(e, ["image"]);
  return r != null && h(n, ["image"], Uo(r)), n;
}
function $G(e, t, n) {
  const r = {}, o = c(e, ["mode"]);
  t !== void 0 && o != null && h(t, ["parameters", "mode"], o);
  const s = c(e, ["maxPredictions"]);
  t !== void 0 && s != null && h(t, ["parameters", "maxPredictions"], s);
  const a = c(e, ["confidenceThreshold"]);
  t !== void 0 && a != null && h(t, ["parameters", "confidenceThreshold"], a);
  const i = c(e, ["maskDilation"]);
  t !== void 0 && i != null && h(t, ["parameters", "maskDilation"], i);
  const l = c(e, ["binaryColorThreshold"]);
  t !== void 0 && l != null && h(t, ["parameters", "binaryColorThreshold"], l);
  const u = c(e, ["labels"]);
  return t !== void 0 && u != null && h(t, ["labels"], u), r;
}
function DG(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["source"]);
  s != null && OG(s, r);
  const a = c(t, ["config"]);
  return a != null && $G(a, r), r;
}
function LG(e, t) {
  const n = {}, r = c(e, ["predictions"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((s) => aG(s))), h(n, ["generatedMasks"], o);
  }
  return n;
}
function OG(e, t, n) {
  const r = {}, o = c(e, ["prompt"]);
  t !== void 0 && o != null && h(t, ["instances[0]", "prompt"], o);
  const s = c(e, ["image"]);
  t !== void 0 && s != null && h(t, ["instances[0]", "image"], Uo(s));
  const a = c(e, ["scribbleImage"]);
  return t !== void 0 && a != null && h(t, ["instances[0]", "scribble"], NG(a)), r;
}
function FG(e, t) {
  const n = {}, r = c(e, ["retrievalConfig"]);
  r != null && h(n, ["retrievalConfig"], r);
  const o = c(e, ["functionCallingConfig"]);
  o != null && h(n, ["functionCallingConfig"], BW(o));
  const s = c(e, ["includeServerSideToolInvocations"]);
  return s != null && h(n, ["includeServerSideToolInvocations"], s), n;
}
function BG(e, t) {
  const n = {}, r = c(e, ["retrievalConfig"]);
  r != null && h(n, ["retrievalConfig"], r);
  const o = c(e, ["functionCallingConfig"]);
  if (o != null && h(n, ["functionCallingConfig"], o), c(e, ["includeServerSideToolInvocations"]) !== void 0) throw new Error("includeServerSideToolInvocations parameter is not supported in Vertex AI.");
  return n;
}
function UG(e, t) {
  const n = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const r = c(e, ["computerUse"]);
  r != null && h(n, ["computerUse"], r);
  const o = c(e, ["fileSearch"]);
  o != null && h(n, ["fileSearch"], o);
  const s = c(e, ["googleSearch"]);
  s != null && h(n, ["googleSearch"], hG(s));
  const a = c(e, ["googleMaps"]);
  a != null && h(n, ["googleMaps"], fG(a));
  const i = c(e, ["codeExecution"]);
  if (i != null && h(n, ["codeExecution"], i), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let m = l;
    Array.isArray(m) && (m = m.map((v) => v)), h(n, ["functionDeclarations"], m);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  if (u != null && h(n, ["googleSearchRetrieval"], u), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const d = c(e, ["urlContext"]);
  d != null && h(n, ["urlContext"], d);
  const f = c(e, ["mcpServers"]);
  if (f != null) {
    let m = f;
    Array.isArray(m) && (m = m.map((v) => v)), h(n, ["mcpServers"], m);
  }
  return n;
}
function B3(e, t) {
  const n = {}, r = c(e, ["retrieval"]);
  r != null && h(n, ["retrieval"], r);
  const o = c(e, ["computerUse"]);
  if (o != null && h(n, ["computerUse"], o), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const s = c(e, ["googleSearch"]);
  s != null && h(n, ["googleSearch"], s);
  const a = c(e, ["googleMaps"]);
  a != null && h(n, ["googleMaps"], a);
  const i = c(e, ["codeExecution"]);
  i != null && h(n, ["codeExecution"], i);
  const l = c(e, ["enterpriseWebSearch"]);
  l != null && h(n, ["enterpriseWebSearch"], l);
  const u = c(e, ["functionDeclarations"]);
  if (u != null) {
    let v = u;
    Array.isArray(v) && (v = v.map((y) => UW(y))), h(n, ["functionDeclarations"], v);
  }
  const d = c(e, ["googleSearchRetrieval"]);
  d != null && h(n, ["googleSearchRetrieval"], d);
  const f = c(e, ["parallelAiSearch"]);
  f != null && h(n, ["parallelAiSearch"], f);
  const m = c(e, ["urlContext"]);
  if (m != null && h(n, ["urlContext"], m), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return n;
}
function qG(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && h(n, ["baseModel"], r);
  const o = c(e, ["createTime"]);
  o != null && h(n, ["createTime"], o);
  const s = c(e, ["updateTime"]);
  return s != null && h(n, ["updateTime"], s), n;
}
function HG(e, t) {
  const n = {}, r = c(e, ["labels", "google-vertex-llm-tuning-base-model-id"]);
  r != null && h(n, ["baseModel"], r);
  const o = c(e, ["createTime"]);
  o != null && h(n, ["createTime"], o);
  const s = c(e, ["updateTime"]);
  return s != null && h(n, ["updateTime"], s), n;
}
function zG(e, t, n) {
  const r = {}, o = c(e, ["displayName"]);
  t !== void 0 && o != null && h(t, ["displayName"], o);
  const s = c(e, ["description"]);
  t !== void 0 && s != null && h(t, ["description"], s);
  const a = c(e, ["defaultCheckpointId"]);
  return t !== void 0 && a != null && h(t, ["defaultCheckpointId"], a), r;
}
function WG(e, t, n) {
  const r = {}, o = c(e, ["displayName"]);
  t !== void 0 && o != null && h(t, ["displayName"], o);
  const s = c(e, ["description"]);
  t !== void 0 && s != null && h(t, ["description"], s);
  const a = c(e, ["defaultCheckpointId"]);
  return t !== void 0 && a != null && h(t, ["defaultCheckpointId"], a), r;
}
function GG(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "name"], Xt(e, o));
  const s = c(t, ["config"]);
  return s != null && zG(s, r), r;
}
function VG(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["config"]);
  return s != null && WG(s, r), r;
}
function KG(e, t, n) {
  const r = {}, o = c(e, ["outputGcsUri"]);
  t !== void 0 && o != null && h(t, ["parameters", "storageUri"], o);
  const s = c(e, ["safetyFilterLevel"]);
  t !== void 0 && s != null && h(t, ["parameters", "safetySetting"], s);
  const a = c(e, ["personGeneration"]);
  t !== void 0 && a != null && h(t, ["parameters", "personGeneration"], a);
  const i = c(e, ["includeRaiReason"]);
  t !== void 0 && i != null && h(t, ["parameters", "includeRaiReason"], i);
  const l = c(e, ["outputMimeType"]);
  t !== void 0 && l != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], l);
  const u = c(e, ["outputCompressionQuality"]);
  t !== void 0 && u != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], u);
  const d = c(e, ["enhanceInputImage"]);
  t !== void 0 && d != null && h(t, [
    "parameters",
    "upscaleConfig",
    "enhanceInputImage"
  ], d);
  const f = c(e, ["imagePreservationFactor"]);
  t !== void 0 && f != null && h(t, [
    "parameters",
    "upscaleConfig",
    "imagePreservationFactor"
  ], f);
  const m = c(e, ["labels"]);
  t !== void 0 && m != null && h(t, ["labels"], m);
  const v = c(e, ["numberOfImages"]);
  t !== void 0 && v != null && h(t, ["parameters", "sampleCount"], v);
  const y = c(e, ["mode"]);
  return t !== void 0 && y != null && h(t, ["parameters", "mode"], y), r;
}
function JG(e, t, n) {
  const r = {}, o = c(t, ["model"]);
  o != null && h(r, ["_url", "model"], Xt(e, o));
  const s = c(t, ["image"]);
  s != null && h(r, ["instances[0]", "image"], Uo(s));
  const a = c(t, ["upscaleFactor"]);
  a != null && h(r, [
    "parameters",
    "upscaleConfig",
    "upscaleFactor"
  ], a);
  const i = c(t, ["config"]);
  return i != null && KG(i, r), r;
}
function YG(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["predictions"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => _g(a))), h(n, ["generatedImages"], s);
  }
  return n;
}
function XG(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && h(n, ["uri"], r);
  const o = c(e, ["encodedVideo"]);
  o != null && h(n, ["videoBytes"], Ga(o));
  const s = c(e, ["encoding"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function QG(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && h(n, ["uri"], r);
  const o = c(e, ["bytesBase64Encoded"]);
  o != null && h(n, ["videoBytes"], Ga(o));
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function ZG(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && h(n, ["_self"], Uo(r));
  const o = c(e, ["maskMode"]);
  return o != null && h(n, ["maskMode"], o), n;
}
function jG(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && h(n, ["image"], bg(r));
  const o = c(e, ["referenceType"]);
  return o != null && h(n, ["referenceType"], o), n;
}
function eV(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && h(n, ["image"], Uo(r));
  const o = c(e, ["referenceType"]);
  return o != null && h(n, ["referenceType"], o), n;
}
function U3(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && h(n, ["uri"], r);
  const o = c(e, ["videoBytes"]);
  o != null && h(n, ["encodedVideo"], Ga(o));
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["encoding"], s), n;
}
function q3(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && h(n, ["gcsUri"], r);
  const o = c(e, ["videoBytes"]);
  o != null && h(n, ["bytesBase64Encoded"], Ga(o));
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function tV(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  return t !== void 0 && r != null && h(t, ["displayName"], r), n;
}
function nV(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && tV(n, t), t;
}
function rV(e, t) {
  const n = {}, r = c(e, ["force"]);
  return t !== void 0 && r != null && h(t, ["_query", "force"], r), n;
}
function oV(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["_url", "name"], n);
  const r = c(e, ["config"]);
  return r != null && rV(r, t), t;
}
function sV(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && h(t, ["_url", "name"], n), t;
}
function aV(e, t) {
  const n = {}, r = c(e, ["customMetadata"]);
  if (t !== void 0 && r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => a)), h(t, ["customMetadata"], s);
  }
  const o = c(e, ["chunkingConfig"]);
  return t !== void 0 && o != null && h(t, ["chunkingConfig"], o), n;
}
function iV(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const o = c(e, ["done"]);
  o != null && h(t, ["done"], o);
  const s = c(e, ["error"]);
  s != null && h(t, ["error"], s);
  const a = c(e, ["response"]);
  return a != null && h(t, ["response"], uV(a)), t;
}
function lV(e) {
  const t = {}, n = c(e, ["fileSearchStoreName"]);
  n != null && h(t, ["_url", "file_search_store_name"], n);
  const r = c(e, ["fileName"]);
  r != null && h(t, ["fileName"], r);
  const o = c(e, ["config"]);
  return o != null && aV(o, t), t;
}
function uV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && h(t, ["parent"], r);
  const o = c(e, ["documentName"]);
  return o != null && h(t, ["documentName"], o), t;
}
function cV(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const o = c(e, ["pageToken"]);
  return t !== void 0 && o != null && h(t, ["_query", "pageToken"], o), n;
}
function dV(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && cV(n, t), t;
}
function fV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const o = c(e, ["fileSearchStores"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => a)), h(t, ["fileSearchStores"], s);
  }
  return t;
}
function H3(e, t) {
  const n = {}, r = c(e, ["mimeType"]);
  t !== void 0 && r != null && h(t, ["mimeType"], r);
  const o = c(e, ["displayName"]);
  t !== void 0 && o != null && h(t, ["displayName"], o);
  const s = c(e, ["customMetadata"]);
  if (t !== void 0 && s != null) {
    let i = s;
    Array.isArray(i) && (i = i.map((l) => l)), h(t, ["customMetadata"], i);
  }
  const a = c(e, ["chunkingConfig"]);
  return t !== void 0 && a != null && h(t, ["chunkingConfig"], a), n;
}
function hV(e) {
  const t = {}, n = c(e, ["fileSearchStoreName"]);
  n != null && h(t, ["_url", "file_search_store_name"], n);
  const r = c(e, ["config"]);
  return r != null && H3(r, t), t;
}
function pV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
var mV = "Content-Type", gV = "X-Server-Timeout", vV = "User-Agent", T1 = "x-goog-api-client", yV = "google-genai-sdk/1.50.1", _V = "v1beta1", bV = "v1beta", wV = /* @__PURE__ */ new Set(["us", "eu"]), SV = 5, TV = [
  408,
  429,
  500,
  502,
  503,
  504
], EV = class {
  constructor(e) {
    var t, n, r;
    this.clientOptions = Object.assign({}, e), this.customBaseUrl = (t = e.httpOptions) === null || t === void 0 ? void 0 : t.baseUrl, this.clientOptions.vertexai && (this.clientOptions.project && this.clientOptions.location ? this.clientOptions.apiKey = void 0 : this.clientOptions.apiKey && (this.clientOptions.project = void 0, this.clientOptions.location = void 0));
    const o = {};
    if (this.clientOptions.vertexai) {
      if (!this.clientOptions.location && !this.clientOptions.apiKey && !this.customBaseUrl && (this.clientOptions.location = "global"), !(this.clientOptions.project && this.clientOptions.location || this.clientOptions.apiKey) && !this.customBaseUrl) throw new Error("Authentication is not set up. Please provide either a project and location, or an API key, or a custom base URL.");
      const s = e.project && e.location || !!e.apiKey;
      this.customBaseUrl && !s ? (o.baseUrl = this.customBaseUrl, this.clientOptions.project = void 0, this.clientOptions.location = void 0) : this.clientOptions.apiKey || this.clientOptions.location === "global" ? o.baseUrl = "https://aiplatform.googleapis.com/" : this.clientOptions.project && this.clientOptions.location && wV.has(this.clientOptions.location) ? o.baseUrl = `https://aiplatform.${this.clientOptions.location}.rep.googleapis.com/` : this.clientOptions.project && this.clientOptions.location && (o.baseUrl = `https://${this.clientOptions.location}-aiplatform.googleapis.com/`), o.apiVersion = (n = this.clientOptions.apiVersion) !== null && n !== void 0 ? n : _V;
    } else
      this.clientOptions.apiKey || console.warn("API key should be set when using the Gemini API."), o.apiVersion = (r = this.clientOptions.apiVersion) !== null && r !== void 0 ? r : bV, o.baseUrl = "https://generativelanguage.googleapis.com/";
    o.headers = this.getDefaultHeaders(), this.clientOptions.httpOptions = o, e.httpOptions && (this.clientOptions.httpOptions = this.patchHttpOptions(o, e.httpOptions));
  }
  isVertexAI() {
    var e;
    return (e = this.clientOptions.vertexai) !== null && e !== void 0 ? e : !1;
  }
  getProject() {
    return this.clientOptions.project;
  }
  getLocation() {
    return this.clientOptions.location;
  }
  getCustomBaseUrl() {
    return this.customBaseUrl;
  }
  async getAuthHeaders() {
    const e = new Headers();
    return await this.clientOptions.auth.addAuthHeaders(e), e;
  }
  getApiVersion() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.apiVersion !== void 0) return this.clientOptions.httpOptions.apiVersion;
    throw new Error("API version is not set.");
  }
  getBaseUrl() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.baseUrl !== void 0) return this.clientOptions.httpOptions.baseUrl;
    throw new Error("Base URL is not set.");
  }
  getRequestUrl() {
    return this.getRequestUrlInternal(this.clientOptions.httpOptions);
  }
  getHeaders() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.headers !== void 0) return this.clientOptions.httpOptions.headers;
    throw new Error("Headers are not set.");
  }
  getRequestUrlInternal(e) {
    if (!e || e.baseUrl === void 0 || e.apiVersion === void 0) throw new Error("HTTP options are not correctly set.");
    const t = [e.baseUrl.endsWith("/") ? e.baseUrl.slice(0, -1) : e.baseUrl];
    return e.apiVersion && e.apiVersion !== "" && t.push(e.apiVersion), t.join("/");
  }
  getBaseResourcePath() {
    return `projects/${this.clientOptions.project}/locations/${this.clientOptions.location}`;
  }
  getApiKey() {
    return this.clientOptions.apiKey;
  }
  getWebsocketBaseUrl() {
    const e = this.getBaseUrl(), t = new URL(e);
    return t.protocol = t.protocol == "http:" ? "ws" : "wss", t.toString();
  }
  setBaseUrl(e) {
    if (this.clientOptions.httpOptions) this.clientOptions.httpOptions.baseUrl = e;
    else throw new Error("HTTP options are not correctly set.");
  }
  constructUrl(e, t, n) {
    const r = [this.getRequestUrlInternal(t)];
    return n && r.push(this.getBaseResourcePath()), e !== "" && r.push(e), new URL(`${r.join("/")}`);
  }
  shouldPrependVertexProjectPath(e, t) {
    return !(t.baseUrl && t.baseUrlResourceScope === v1.COLLECTION || this.clientOptions.apiKey || !this.clientOptions.vertexai || e.path.startsWith("projects/") || e.httpMethod === "GET" && e.path.startsWith("publishers/google/models"));
  }
  async request(e) {
    let t = this.clientOptions.httpOptions;
    e.httpOptions && (t = this.patchHttpOptions(this.clientOptions.httpOptions, e.httpOptions));
    const n = this.shouldPrependVertexProjectPath(e, t), r = this.constructUrl(e.path, t, n);
    if (e.queryParams) for (const [s, a] of Object.entries(e.queryParams)) r.searchParams.append(s, String(a));
    let o = {};
    if (e.httpMethod === "GET") {
      if (e.body && e.body !== "{}") throw new Error("Request body should be empty for GET request, but got non empty request body");
    } else o.body = e.body;
    return o = await this.includeExtraHttpOptionsToRequestInit(o, t, r.toString(), e.abortSignal), this.unaryApiCall(r, o, e.httpMethod);
  }
  patchHttpOptions(e, t) {
    const n = JSON.parse(JSON.stringify(e));
    for (const [r, o] of Object.entries(t)) typeof o == "object" ? n[r] = Object.assign(Object.assign({}, n[r]), o) : o !== void 0 && (n[r] = o);
    return n;
  }
  async requestStream(e) {
    let t = this.clientOptions.httpOptions;
    e.httpOptions && (t = this.patchHttpOptions(this.clientOptions.httpOptions, e.httpOptions));
    const n = this.shouldPrependVertexProjectPath(e, t), r = this.constructUrl(e.path, t, n);
    (!r.searchParams.has("alt") || r.searchParams.get("alt") !== "sse") && r.searchParams.set("alt", "sse");
    let o = {};
    return o.body = e.body, o = await this.includeExtraHttpOptionsToRequestInit(o, t, r.toString(), e.abortSignal), this.streamApiCall(r, o, e.httpMethod);
  }
  async includeExtraHttpOptionsToRequestInit(e, t, n, r) {
    if (t && t.timeout || r) {
      const o = new AbortController(), s = o.signal;
      if (t.timeout && t?.timeout > 0) {
        const a = setTimeout(() => o.abort(), t.timeout);
        a && typeof a.unref == "function" && a.unref();
      }
      r && r.addEventListener("abort", () => {
        o.abort();
      }), e.signal = s;
    }
    return t && t.extraBody !== null && AV(e, t.extraBody), e.headers = await this.getHeadersInternal(t, n), e;
  }
  async unaryApiCall(e, t, n) {
    return this.apiCall(e.toString(), Object.assign(Object.assign({}, t), { method: n })).then(async (r) => (await hA(r), new y1(r))).catch((r) => {
      throw r instanceof Error ? r : new Error(JSON.stringify(r));
    });
  }
  async streamApiCall(e, t, n) {
    return this.apiCall(e.toString(), Object.assign(Object.assign({}, t), { method: n })).then(async (r) => (await hA(r), this.processStreamResponse(r))).catch((r) => {
      throw r instanceof Error ? r : new Error(JSON.stringify(r));
    });
  }
  processStreamResponse(e) {
    return $o(this, arguments, function* () {
      var n;
      const r = (n = e?.body) === null || n === void 0 ? void 0 : n.getReader(), o = new TextDecoder("utf-8");
      if (!r) throw new Error("Response body is empty");
      try {
        let s = "";
        const a = "data:", i = [
          `

`,
          "\r\r",
          `\r
\r
`
        ];
        for (; ; ) {
          const { done: l, value: u } = yield Rt(r.read());
          if (l) {
            if (s.trim().length > 0) throw new Error("Incomplete JSON segment at the end");
            break;
          }
          const d = o.decode(u, { stream: !0 });
          try {
            const v = JSON.parse(d);
            if ("error" in v) {
              const y = JSON.parse(JSON.stringify(v.error)), T = y.status, C = y.code, A = `got status: ${T}. ${JSON.stringify(v)}`;
              if (C >= 400 && C < 600) throw new $3({
                message: A,
                status: C
              });
            }
          } catch (v) {
            if (v.name === "ApiError") throw v;
          }
          s += d;
          let f = -1, m = 0;
          for (; ; ) {
            f = -1, m = 0;
            for (const T of i) {
              const C = s.indexOf(T);
              C !== -1 && (f === -1 || C < f) && (f = C, m = T.length);
            }
            if (f === -1) break;
            const v = s.substring(0, f);
            s = s.substring(f + m);
            const y = v.trim();
            if (y.startsWith(a)) {
              const T = y.substring(5).trim();
              try {
                yield yield Rt(new y1(new Response(T, {
                  headers: e?.headers,
                  status: e?.status,
                  statusText: e?.statusText
                })));
              } catch (C) {
                throw new Error(`exception parsing stream chunk ${T}. ${C}`);
              }
            }
          }
        }
      } finally {
        r.releaseLock();
      }
    });
  }
  async apiCall(e, t) {
    var n;
    if (!this.clientOptions.httpOptions || !this.clientOptions.httpOptions.retryOptions) return fetch(e, t);
    const r = this.clientOptions.httpOptions.retryOptions, o = async () => {
      const s = await fetch(e, t);
      if (s.ok) return s;
      throw TV.includes(s.status) ? new Error(`Retryable HTTP Error: ${s.statusText}`) : new BT.AbortError(`Non-retryable exception ${s.statusText} sending request`);
    };
    return (0, BT.default)(o, { retries: ((n = r.attempts) !== null && n !== void 0 ? n : SV) - 1 });
  }
  getDefaultHeaders() {
    const e = {}, t = yV + " " + this.clientOptions.userAgentExtra;
    return e[vV] = t, e[T1] = t, e[mV] = "application/json", e;
  }
  async getHeadersInternal(e, t) {
    const n = new Headers();
    if (e && e.headers) {
      for (const [r, o] of Object.entries(e.headers)) n.append(r, o);
      e.timeout && e.timeout > 0 && n.append(gV, String(Math.ceil(e.timeout / 1e3)));
    }
    return await this.clientOptions.auth.addAuthHeaders(n, t), n;
  }
  getFileName(e) {
    var t;
    let n = "";
    return typeof e == "string" && (n = e.replace(/[/\\]+$/, ""), n = (t = n.split(/[/\\]/).pop()) !== null && t !== void 0 ? t : ""), n;
  }
  async uploadFile(e, t) {
    var n;
    const r = {};
    t != null && (r.mimeType = t.mimeType, r.name = t.name, r.displayName = t.displayName), r.name && !r.name.startsWith("files/") && (r.name = `files/${r.name}`);
    const o = this.clientOptions.uploader, s = await o.stat(e);
    r.sizeBytes = String(s.size);
    const a = (n = t?.mimeType) !== null && n !== void 0 ? n : s.type;
    if (a === void 0 || a === "") throw new Error("Can not determine mimeType. Please provide mimeType in the config.");
    r.mimeType = a;
    const i = { file: r }, l = this.getFileName(e), u = nt("upload/v1beta/files", i._url), d = await this.fetchUploadUrl(u, r.sizeBytes, r.mimeType, l, i, t?.httpOptions);
    return o.upload(e, d, this);
  }
  async uploadFileToFileSearchStore(e, t, n) {
    var r;
    const o = this.clientOptions.uploader, s = await o.stat(t), a = String(s.size), i = (r = n?.mimeType) !== null && r !== void 0 ? r : s.type;
    if (i === void 0 || i === "") throw new Error("Can not determine mimeType. Please provide mimeType in the config.");
    const l = `upload/v1beta/${e}:uploadToFileSearchStore`, u = this.getFileName(t), d = {};
    n != null && H3(n, d);
    const f = await this.fetchUploadUrl(l, a, i, u, d, n?.httpOptions);
    return o.uploadToFileSearchStore(t, f, this);
  }
  async downloadFile(e) {
    await this.clientOptions.downloader.download(e, this);
  }
  async fetchUploadUrl(e, t, n, r, o, s) {
    var a;
    let i = {};
    s ? i = s : i = {
      apiVersion: "",
      headers: Object.assign({
        "Content-Type": "application/json",
        "X-Goog-Upload-Protocol": "resumable",
        "X-Goog-Upload-Command": "start",
        "X-Goog-Upload-Header-Content-Length": `${t}`,
        "X-Goog-Upload-Header-Content-Type": `${n}`
      }, r ? { "X-Goog-Upload-File-Name": r } : {})
    };
    const l = await this.request({
      path: e,
      body: JSON.stringify(o),
      httpMethod: "POST",
      httpOptions: i
    });
    if (!l || !l?.headers) throw new Error("Server did not return an HttpResponse or the returned HttpResponse did not have headers.");
    const u = (a = l?.headers) === null || a === void 0 ? void 0 : a["x-goog-upload-url"];
    if (u === void 0) throw new Error("Failed to get upload url. Server did not return the x-google-upload-url in the headers");
    return u;
  }
};
async function hA(e) {
  var t;
  if (e === void 0) throw new Error("response is undefined");
  if (!e.ok) {
    const n = e.status;
    let r;
    !((t = e.headers.get("content-type")) === null || t === void 0) && t.includes("application/json") ? r = await e.json() : r = { error: {
      message: await e.text(),
      code: e.status,
      status: e.statusText
    } };
    const o = JSON.stringify(r);
    throw n >= 400 && n < 600 ? new $3({
      message: o,
      status: n
    }) : new Error(o);
  }
}
function AV(e, t) {
  if (!t || Object.keys(t).length === 0) return;
  if (e.body instanceof Blob) {
    console.warn("includeExtraBodyToRequestInit: extraBody provided but current request body is a Blob. extraBody will be ignored as merging is not supported for Blob bodies.");
    return;
  }
  let n = {};
  if (typeof e.body == "string" && e.body.length > 0) try {
    const s = JSON.parse(e.body);
    if (typeof s == "object" && s !== null && !Array.isArray(s)) n = s;
    else {
      console.warn("includeExtraBodyToRequestInit: Original request body is valid JSON but not a non-array object. Skip applying extraBody to the request body.");
      return;
    }
  } catch {
    console.warn("includeExtraBodyToRequestInit: Original request body is not valid JSON. Skip applying extraBody to the request body.");
    return;
  }
  function r(s, a) {
    const i = Object.assign({}, s);
    for (const l in a) if (Object.prototype.hasOwnProperty.call(a, l)) {
      const u = a[l], d = i[l];
      u && typeof u == "object" && !Array.isArray(u) && d && typeof d == "object" && !Array.isArray(d) ? i[l] = r(d, u) : (d && u && typeof d != typeof u && console.warn(`includeExtraBodyToRequestInit:deepMerge: Type mismatch for key "${l}". Original type: ${typeof d}, New type: ${typeof u}. Overwriting.`), i[l] = u);
    }
    return i;
  }
  const o = r(n, t);
  e.body = JSON.stringify(o);
}
var CV = "mcp_used/unknown", kV = !1;
function z3(e) {
  for (const t of e)
    if (IV(t) || typeof t == "object" && "inputSchema" in t) return !0;
  return kV;
}
function W3(e) {
  var t;
  e[T1] = (((t = e[T1]) !== null && t !== void 0 ? t : "") + ` ${CV}`).trimStart();
}
function IV(e) {
  return e !== null && typeof e == "object" && e instanceof MV;
}
function xV(e) {
  return $o(this, arguments, function* (n, r = 100) {
    let o, s = 0;
    for (; s < r; ) {
      const a = yield Rt(n.listTools({ cursor: o }));
      for (const i of a.tools)
        yield yield Rt(i), s++;
      if (!a.nextCursor) break;
      o = a.nextCursor;
    }
  });
}
var MV = class G3 {
  constructor(t = [], n) {
    this.mcpTools = [], this.functionNameToMcpClient = {}, this.mcpClients = t, this.config = n;
  }
  static create(t, n) {
    return new G3(t, n);
  }
  async initialize() {
    var t, n, r, o;
    if (this.mcpTools.length > 0) return;
    const s = {}, a = [];
    for (const d of this.mcpClients) try {
      for (var i = !0, l = (n = void 0, Do(xV(d))), u; u = await l.next(), t = u.done, !t; i = !0) {
        o = u.value, i = !1;
        const f = o;
        a.push(f);
        const m = f.name;
        if (s[m]) throw new Error(`Duplicate function name ${m} found in MCP tools. Please ensure function names are unique.`);
        s[m] = d;
      }
    } catch (f) {
      n = { error: f };
    } finally {
      try {
        !i && !t && (r = l.return) && await r.call(l);
      } finally {
        if (n) throw n.error;
      }
    }
    this.mcpTools = a, this.functionNameToMcpClient = s;
  }
  async tool() {
    return await this.initialize(), Wq(this.mcpTools, this.config);
  }
  async callTool(t) {
    await this.initialize();
    const n = [];
    for (const r of t) if (r.name in this.functionNameToMcpClient) {
      const o = this.functionNameToMcpClient[r.name];
      let s;
      this.config.timeout && (s = { timeout: this.config.timeout });
      const a = await o.callTool({
        name: r.name,
        arguments: r.args
      }, void 0, s);
      n.push({ functionResponse: {
        name: r.name,
        response: a.isError ? { error: a } : a
      } });
    }
    return n;
  }
};
async function PV(e, t, n) {
  const r = new Dq();
  let o;
  n.data instanceof Blob ? o = JSON.parse(await n.data.text()) : o = JSON.parse(n.data), Object.assign(r, o), t(r);
}
var RV = class {
  constructor(e, t, n) {
    this.apiClient = e, this.auth = t, this.webSocketFactory = n;
  }
  async connect(e) {
    var t, n;
    if (this.apiClient.isVertexAI()) throw new Error("Live music is not supported for Vertex AI.");
    console.warn("Live music generation is experimental and may change in future versions.");
    const r = this.apiClient.getWebsocketBaseUrl(), o = this.apiClient.getApiVersion(), s = DV(this.apiClient.getDefaultHeaders()), a = `${r}/ws/google.ai.generativelanguage.${o}.GenerativeService.BidiGenerateMusic?key=${this.apiClient.getApiKey()}`;
    let i = () => {
    };
    const l = new Promise((T) => {
      i = T;
    }), u = e.callbacks, d = function() {
      i({});
    }, f = this.apiClient, m = {
      onopen: d,
      onmessage: (T) => {
        PV(f, u.onmessage, T);
      },
      onerror: (t = u?.onerror) !== null && t !== void 0 ? t : function(T) {
      },
      onclose: (n = u?.onclose) !== null && n !== void 0 ? n : function(T) {
      }
    }, v = this.webSocketFactory.create(a, $V(s), m);
    v.connect(), await l;
    const y = { setup: { model: Xt(this.apiClient, e.model) } };
    return v.send(JSON.stringify(y)), new NV(v, this.apiClient);
  }
}, NV = class {
  constructor(e, t) {
    this.conn = e, this.apiClient = t;
  }
  async setWeightedPrompts(e) {
    if (!e.weightedPrompts || Object.keys(e.weightedPrompts).length === 0) throw new Error("Weighted prompts must be set and contain at least one entry.");
    const t = Xz(e);
    this.conn.send(JSON.stringify({ clientContent: t }));
  }
  async setMusicGenerationConfig(e) {
    e.musicGenerationConfig || (e.musicGenerationConfig = {});
    const t = Yz(e);
    this.conn.send(JSON.stringify(t));
  }
  sendPlaybackControl(e) {
    const t = { playbackControl: e };
    this.conn.send(JSON.stringify(t));
  }
  play() {
    this.sendPlaybackControl(Vl.PLAY);
  }
  pause() {
    this.sendPlaybackControl(Vl.PAUSE);
  }
  stop() {
    this.sendPlaybackControl(Vl.STOP);
  }
  resetContext() {
    this.sendPlaybackControl(Vl.RESET_CONTEXT);
  }
  close() {
    this.conn.close();
  }
};
function $V(e) {
  const t = {};
  return e.forEach((n, r) => {
    t[r] = n;
  }), t;
}
function DV(e) {
  const t = new Headers();
  for (const [n, r] of Object.entries(e)) t.append(n, r);
  return t;
}
var LV = "FunctionResponse request must have an `id` field from the response of a ToolCall.FunctionalCalls in Google AI.";
async function OV(e, t, n) {
  const r = new $q();
  let o;
  n.data instanceof Blob ? o = await n.data.text() : n.data instanceof ArrayBuffer ? o = new TextDecoder().decode(n.data) : o = n.data;
  const s = JSON.parse(o);
  if (e.isVertexAI()) {
    const a = jz(s);
    Object.assign(r, a);
  } else Object.assign(r, s);
  t(r);
}
var FV = class {
  constructor(e, t, n) {
    this.apiClient = e, this.auth = t, this.webSocketFactory = n, this.music = new RV(this.apiClient, this.auth, this.webSocketFactory);
  }
  async connect(e) {
    var t, n, r, o, s, a;
    if (e.config && e.config.httpOptions) throw new Error("The Live module does not support httpOptions at request-level in LiveConnectConfig yet. Please use the client-level httpOptions configuration instead.");
    const i = this.apiClient.getWebsocketBaseUrl(), l = this.apiClient.getApiVersion();
    let u;
    const d = this.apiClient.getHeaders();
    e.config && e.config.tools && z3(e.config.tools) && W3(d);
    const f = HV(d);
    if (this.apiClient.isVertexAI()) {
      const E = this.apiClient.getProject(), x = this.apiClient.getLocation(), M = this.apiClient.getApiKey(), N = !!E && !!x || !!M;
      this.apiClient.getCustomBaseUrl() && !N ? u = i : (u = `${i}/ws/google.cloud.aiplatform.${l}.LlmBidiService/BidiGenerateContent`, await this.auth.addAuthHeaders(f, u));
    } else {
      const E = this.apiClient.getApiKey();
      let x = "BidiGenerateContent", M = "key";
      E?.startsWith("auth_tokens/") && (console.warn("Warning: Ephemeral token support is experimental and may change in future versions."), l !== "v1alpha" && console.warn("Warning: The SDK's ephemeral token support is in v1alpha only. Please use const ai = new GoogleGenAI({apiKey: token.name, httpOptions: { apiVersion: 'v1alpha' }}); before session connection."), x = "BidiGenerateContentConstrained", M = "access_token"), u = `${i}/ws/google.ai.generativelanguage.${l}.GenerativeService.${x}?${M}=${E}`;
    }
    let m = () => {
    };
    const v = new Promise((E) => {
      m = E;
    }), y = e.callbacks, T = function() {
      var E;
      (E = y?.onopen) === null || E === void 0 || E.call(y), m({});
    }, C = this.apiClient, A = {
      onopen: T,
      onmessage: (E) => {
        OV(C, y.onmessage, E);
      },
      onerror: (t = y?.onerror) !== null && t !== void 0 ? t : function(E) {
      },
      onclose: (n = y?.onclose) !== null && n !== void 0 ? n : function(E) {
      }
    }, I = this.webSocketFactory.create(u, qV(f), A);
    I.connect(), await v;
    let R = Xt(this.apiClient, e.model);
    if (this.apiClient.isVertexAI() && R.startsWith("publishers/")) {
      const E = this.apiClient.getProject(), x = this.apiClient.getLocation();
      E && x && (R = `projects/${E}/locations/${x}/` + R);
    }
    let $ = {};
    this.apiClient.isVertexAI() && ((r = e.config) === null || r === void 0 ? void 0 : r.responseModalities) === void 0 && (e.config === void 0 ? e.config = { responseModalities: [vm.AUDIO] } : e.config.responseModalities = [vm.AUDIO]), !((o = e.config) === null || o === void 0) && o.generationConfig && console.warn("Setting `LiveConnectConfig.generation_config` is deprecated, please set the fields on `LiveConnectConfig` directly. This will become an error in a future version (not before Q3 2025).");
    const p = (a = (s = e.config) === null || s === void 0 ? void 0 : s.tools) !== null && a !== void 0 ? a : [], b = [];
    for (const E of p) if (this.isCallableTool(E)) {
      const x = E;
      b.push(await x.tool());
    } else b.push(E);
    b.length > 0 && (e.config.tools = b);
    const w = {
      model: R,
      config: e.config,
      callbacks: e.callbacks
    };
    return this.apiClient.isVertexAI() ? $ = Jz(this.apiClient, w) : $ = Kz(this.apiClient, w), delete $.config, I.send(JSON.stringify($)), new UV(I, this.apiClient);
  }
  isCallableTool(e) {
    return "callTool" in e && typeof e.callTool == "function";
  }
}, BV = { turnComplete: !0 }, UV = class {
  constructor(e, t) {
    this.conn = e, this.apiClient = t;
  }
  tLiveClientContent(e, t) {
    if (t.turns !== null && t.turns !== void 0) {
      let n = [];
      try {
        n = kr(t.turns), e.isVertexAI() || (n = n.map((r) => mf(r)));
      } catch {
        throw new Error(`Failed to parse client content "turns", type: '${typeof t.turns}'`);
      }
      return { clientContent: {
        turns: n,
        turnComplete: t.turnComplete
      } };
    }
    return { clientContent: { turnComplete: t.turnComplete } };
  }
  tLiveClienttToolResponse(e, t) {
    let n = [];
    if (t.functionResponses == null) throw new Error("functionResponses is required.");
    if (Array.isArray(t.functionResponses) ? n = t.functionResponses : n = [t.functionResponses], n.length === 0) throw new Error("functionResponses is required.");
    for (const r of n) {
      if (typeof r != "object" || r === null || !("name" in r) || !("response" in r)) throw new Error(`Could not parse function response, type '${typeof r}'.`);
      if (!e.isVertexAI() && !("id" in r)) throw new Error(LV);
    }
    return { toolResponse: { functionResponses: n } };
  }
  sendClientContent(e) {
    e = Object.assign(Object.assign({}, BV), e);
    const t = this.tLiveClientContent(this.apiClient, e);
    this.conn.send(JSON.stringify(t));
  }
  sendRealtimeInput(e) {
    let t = {};
    this.apiClient.isVertexAI() ? t = { realtimeInput: Zz(e) } : t = { realtimeInput: Qz(e) }, this.conn.send(JSON.stringify(t));
  }
  sendToolResponse(e) {
    if (e.functionResponses == null) throw new Error("Tool response parameters are required.");
    const t = this.tLiveClienttToolResponse(this.apiClient, e);
    this.conn.send(JSON.stringify(t));
  }
  close() {
    this.conn.close();
  }
};
function qV(e) {
  const t = {};
  return e.forEach((n, r) => {
    t[r] = n;
  }), t;
}
function HV(e) {
  const t = new Headers();
  for (const [n, r] of Object.entries(e)) t.append(n, r);
  return t;
}
var pA = 10;
function mA(e) {
  var t, n, r;
  if (!((t = e?.automaticFunctionCalling) === null || t === void 0) && t.disable) return !0;
  let o = !1;
  for (const a of (n = e?.tools) !== null && n !== void 0 ? n : []) if (au(a)) {
    o = !0;
    break;
  }
  if (!o) return !0;
  const s = (r = e?.automaticFunctionCalling) === null || r === void 0 ? void 0 : r.maximumRemoteCalls;
  return s && (s < 0 || !Number.isInteger(s)) || s == 0 ? (console.warn("Invalid maximumRemoteCalls value provided for automatic function calling. Disabled automatic function calling. Please provide a valid integer value greater than 0. maximumRemoteCalls provided:", s), !0) : !1;
}
function au(e) {
  return "callTool" in e && typeof e.callTool == "function";
}
function zV(e) {
  var t, n, r;
  return (r = (n = (t = e.config) === null || t === void 0 ? void 0 : t.tools) === null || n === void 0 ? void 0 : n.some((o) => au(o))) !== null && r !== void 0 ? r : !1;
}
function gA(e) {
  var t;
  const n = [];
  return !((t = e?.config) === null || t === void 0) && t.tools && e.config.tools.forEach((r, o) => {
    if (au(r)) return;
    const s = r;
    s.functionDeclarations && s.functionDeclarations.length > 0 && n.push(o);
  }), n;
}
function vA(e) {
  var t;
  return !(!((t = e?.automaticFunctionCalling) === null || t === void 0) && t.ignoreCallHistory);
}
var WV = class extends ta {
  constructor(e) {
    super(), this.apiClient = e, this.embedContent = async (t) => {
      if (!this.apiClient.isVertexAI())
        return t.model.includes("gemini-embedding-2") && (t.contents = kr(t.contents)), await this.embedContentInternal(t);
      if (t.model.includes("gemini") && t.model !== "gemini-embedding-001" || t.model.includes("maas")) {
        const n = kr(t.contents);
        if (n.length > 1) throw new Error("The embedContent API for this model only supports one content at a time.");
        const r = Object.assign(Object.assign({}, t), {
          content: n[0],
          embeddingApiType: ym.EMBED_CONTENT
        });
        return await this.embedContentInternal(r);
      } else {
        const n = Object.assign(Object.assign({}, t), { embeddingApiType: ym.PREDICT });
        return await this.embedContentInternal(n);
      }
    }, this.generateContent = async (t) => {
      var n, r, o, s, a;
      const i = await this.processParamsMaybeAddMcpUsage(t);
      if (this.maybeMoveToResponseJsonSchem(t), !zV(t) || mA(t.config)) return await this.generateContentInternal(i);
      const l = gA(t);
      if (l.length > 0) {
        const y = l.map((T) => `tools[${T}]`).join(", ");
        throw new Error(`Automatic function calling with CallableTools (or MCP objects) and basic FunctionDeclarations is not yet supported. Incompatible tools found at ${y}.`);
      }
      let u, d;
      const f = kr(i.contents), m = (o = (r = (n = i.config) === null || n === void 0 ? void 0 : n.automaticFunctionCalling) === null || r === void 0 ? void 0 : r.maximumRemoteCalls) !== null && o !== void 0 ? o : pA;
      let v = 0;
      for (; v < m && (u = await this.generateContentInternal(i), !(!u.functionCalls || u.functionCalls.length === 0)); ) {
        const y = u.candidates[0].content, T = [];
        for (const C of (a = (s = t.config) === null || s === void 0 ? void 0 : s.tools) !== null && a !== void 0 ? a : []) if (au(C)) {
          const A = await C.callTool(u.functionCalls);
          T.push(...A);
        }
        v++, d = {
          role: "user",
          parts: T
        }, i.contents = kr(i.contents), i.contents.push(y), i.contents.push(d), vA(i.config) && (f.push(y), f.push(d));
      }
      return vA(i.config) && (u.automaticFunctionCallingHistory = f), u;
    }, this.generateContentStream = async (t) => {
      var n, r, o, s, a;
      if (this.maybeMoveToResponseJsonSchem(t), mA(t.config)) {
        const d = await this.processParamsMaybeAddMcpUsage(t);
        return await this.generateContentStreamInternal(d);
      }
      const i = gA(t);
      if (i.length > 0) {
        const d = i.map((f) => `tools[${f}]`).join(", ");
        throw new Error(`Incompatible tools found at ${d}. Automatic function calling with CallableTools (or MCP objects) and basic FunctionDeclarations" is not yet supported.`);
      }
      const l = (o = (r = (n = t?.config) === null || n === void 0 ? void 0 : n.toolConfig) === null || r === void 0 ? void 0 : r.functionCallingConfig) === null || o === void 0 ? void 0 : o.streamFunctionCallArguments, u = (a = (s = t?.config) === null || s === void 0 ? void 0 : s.automaticFunctionCalling) === null || a === void 0 ? void 0 : a.disable;
      if (l && !u) throw new Error("Running in streaming mode with 'streamFunctionCallArguments' enabled, this feature is not compatible with automatic function calling (AFC). Please set 'config.automaticFunctionCalling.disable' to true to disable AFC or leave 'config.toolConfig.functionCallingConfig.streamFunctionCallArguments' to be undefined or set to false to disable streaming function call arguments feature.");
      return await this.processAfcStream(t);
    }, this.generateImages = async (t) => await this.generateImagesInternal(t).then((n) => {
      var r;
      let o;
      const s = [];
      if (n?.generatedImages) for (const i of n.generatedImages) i && i?.safetyAttributes && ((r = i?.safetyAttributes) === null || r === void 0 ? void 0 : r.contentType) === "Positive Prompt" ? o = i?.safetyAttributes : s.push(i);
      let a;
      return o ? a = {
        generatedImages: s,
        positivePromptSafetyAttributes: o,
        sdkHttpResponse: n.sdkHttpResponse
      } : a = {
        generatedImages: s,
        sdkHttpResponse: n.sdkHttpResponse
      }, a;
    }), this.list = async (t) => {
      var n;
      const r = { config: Object.assign(Object.assign({}, { queryBase: !0 }), t?.config) };
      if (this.apiClient.isVertexAI() && !r.config.queryBase) {
        if (!((n = r.config) === null || n === void 0) && n.filter) throw new Error("Filtering tuned models list for Vertex AI is not currently supported");
        r.config.filter = "labels.tune-type:*";
      }
      return new tl(js.PAGED_ITEM_MODELS, (o) => this.listInternal(o), await this.listInternal(r), r);
    }, this.editImage = async (t) => {
      const n = {
        model: t.model,
        prompt: t.prompt,
        referenceImages: [],
        config: t.config
      };
      return t.referenceImages && t.referenceImages && (n.referenceImages = t.referenceImages.map((r) => r.toReferenceImageAPI())), await this.editImageInternal(n);
    }, this.upscaleImage = async (t) => {
      let n = {
        numberOfImages: 1,
        mode: "upscale"
      };
      t.config && (n = Object.assign(Object.assign({}, n), t.config));
      const r = {
        model: t.model,
        image: t.image,
        upscaleFactor: t.upscaleFactor,
        config: n
      };
      return await this.upscaleImageInternal(r);
    }, this.generateVideos = async (t) => {
      var n, r, o, s, a, i;
      if ((t.prompt || t.image || t.video) && t.source) throw new Error("Source and prompt/image/video are mutually exclusive. Please only use source.");
      return this.apiClient.isVertexAI() || (!((n = t.video) === null || n === void 0) && n.uri && (!((r = t.video) === null || r === void 0) && r.videoBytes) ? t.video = {
        uri: t.video.uri,
        mimeType: t.video.mimeType
      } : !((s = (o = t.source) === null || o === void 0 ? void 0 : o.video) === null || s === void 0) && s.uri && (!((i = (a = t.source) === null || a === void 0 ? void 0 : a.video) === null || i === void 0) && i.videoBytes) && (t.source.video = {
        uri: t.source.video.uri,
        mimeType: t.source.video.mimeType
      })), await this.generateVideosInternal(t);
    };
  }
  maybeMoveToResponseJsonSchem(e) {
    e.config && e.config.responseSchema && (e.config.responseJsonSchema || Object.keys(e.config.responseSchema).includes("$schema") && (e.config.responseJsonSchema = e.config.responseSchema, delete e.config.responseSchema));
  }
  async processParamsMaybeAddMcpUsage(e) {
    var t, n, r;
    const o = (t = e.config) === null || t === void 0 ? void 0 : t.tools;
    if (!o) return e;
    const s = await Promise.all(o.map(async (i) => au(i) ? await i.tool() : i)), a = {
      model: e.model,
      contents: e.contents,
      config: Object.assign(Object.assign({}, e.config), { tools: s })
    };
    if (a.config.tools = s, e.config && e.config.tools && z3(e.config.tools)) {
      const i = (r = (n = e.config.httpOptions) === null || n === void 0 ? void 0 : n.headers) !== null && r !== void 0 ? r : {};
      let l = Object.assign({}, i);
      Object.keys(l).length === 0 && (l = this.apiClient.getDefaultHeaders()), W3(l), a.config.httpOptions = Object.assign(Object.assign({}, e.config.httpOptions), { headers: l });
    }
    return a;
  }
  async initAfcToolsMap(e) {
    var t, n, r;
    const o = /* @__PURE__ */ new Map();
    for (const s of (n = (t = e.config) === null || t === void 0 ? void 0 : t.tools) !== null && n !== void 0 ? n : []) if (au(s)) {
      const a = s, i = await a.tool();
      for (const l of (r = i.functionDeclarations) !== null && r !== void 0 ? r : []) {
        if (!l.name) throw new Error("Function declaration name is required.");
        if (o.has(l.name)) throw new Error(`Duplicate tool declaration name: ${l.name}`);
        o.set(l.name, a);
      }
    }
    return o;
  }
  async processAfcStream(e) {
    var t, n, r;
    const o = (r = (n = (t = e.config) === null || t === void 0 ? void 0 : t.automaticFunctionCalling) === null || n === void 0 ? void 0 : n.maximumRemoteCalls) !== null && r !== void 0 ? r : pA;
    let s = !1, a = 0;
    const i = await this.initAfcToolsMap(e);
    return (function(l, u, d) {
      return $o(this, arguments, function* () {
        for (var f, m, v, y, T, C; a < o; ) {
          s && (a++, s = !1);
          const $ = yield Rt(l.processParamsMaybeAddMcpUsage(d)), p = yield Rt(l.generateContentStreamInternal($)), b = [], w = [];
          try {
            for (var A = !0, I = (m = void 0, Do(p)), R; R = yield Rt(I.next()), f = R.done, !f; A = !0) {
              y = R.value, A = !1;
              const E = y;
              if (yield yield Rt(E), E.candidates && (!((T = E.candidates[0]) === null || T === void 0) && T.content)) {
                w.push(E.candidates[0].content);
                for (const x of (C = E.candidates[0].content.parts) !== null && C !== void 0 ? C : []) if (a < o && x.functionCall) {
                  if (!x.functionCall.name) throw new Error("Function call name was not returned by the model.");
                  if (u.has(x.functionCall.name)) {
                    const M = yield Rt(u.get(x.functionCall.name).callTool([x.functionCall]));
                    b.push(...M);
                  } else
                    throw new Error(`Automatic function calling was requested, but not all the tools the model used implement the CallableTool interface. Available tools: ${u.keys()}, mising tool: ${x.functionCall.name}`);
                }
              }
            }
          } catch (E) {
            m = { error: E };
          } finally {
            try {
              !A && !f && (v = I.return) && (yield Rt(v.call(I)));
            } finally {
              if (m) throw m.error;
            }
          }
          if (b.length > 0) {
            s = !0;
            const E = new kc();
            E.candidates = [{ content: {
              role: "user",
              parts: b
            } }], yield yield Rt(E);
            const x = [];
            x.push(...w), x.push({
              role: "user",
              parts: b
            }), d.contents = kr(d.contents).concat(x);
          } else break;
        }
      });
    })(this, i, e);
  }
  async generateContentInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = cA(this.apiClient, e);
      return a = nt("{model}:generateContent", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = fA(u), f = new kc();
        return Object.assign(f, d), f;
      });
    } else {
      const l = uA(this.apiClient, e);
      return a = nt("{model}:generateContent", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = dA(u), f = new kc();
        return Object.assign(f, d), f;
      });
    }
  }
  async generateContentStreamInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = cA(this.apiClient, e);
      return a = nt("{model}:streamGenerateContent?alt=sse", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.requestStream({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }), s.then(function(u) {
        return $o(this, arguments, function* () {
          var d, f, m, v;
          try {
            for (var y = !0, T = Do(u), C; C = yield Rt(T.next()), d = C.done, !d; y = !0) {
              v = C.value, y = !1;
              const A = v, I = fA(yield Rt(A.json()), e);
              I.sdkHttpResponse = { headers: A.headers };
              const R = new kc();
              Object.assign(R, I), yield yield Rt(R);
            }
          } catch (A) {
            f = { error: A };
          } finally {
            try {
              !y && !d && (m = T.return) && (yield Rt(m.call(T)));
            } finally {
              if (f) throw f.error;
            }
          }
        });
      });
    } else {
      const l = uA(this.apiClient, e);
      return a = nt("{model}:streamGenerateContent?alt=sse", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.requestStream({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }), s.then(function(u) {
        return $o(this, arguments, function* () {
          var d, f, m, v;
          try {
            for (var y = !0, T = Do(u), C; C = yield Rt(T.next()), d = C.done, !d; y = !0) {
              v = C.value, y = !1;
              const A = v, I = dA(yield Rt(A.json()), e);
              I.sdkHttpResponse = { headers: A.headers };
              const R = new kc();
              Object.assign(R, I), yield yield Rt(R);
            }
          } catch (A) {
            f = { error: A };
          } finally {
            try {
              !y && !d && (m = T.return) && (yield Rt(m.call(T)));
            } finally {
              if (f) throw f.error;
            }
          }
        });
      });
    }
  }
  async embedContentInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = NW(this.apiClient, e, e);
      return a = nt(Vq(e.model) ? "{model}:embedContent" : "{model}:predict", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = DW(u, e), f = new zE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = RW(this.apiClient, e);
      return a = nt("{model}:batchEmbedContents", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = $W(u), f = new zE();
        return Object.assign(f, d), f;
      });
    }
  }
  async generateImagesInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = VW(this.apiClient, e);
      return a = nt("{model}:predict", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = JW(u), f = new WE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = GW(this.apiClient, e);
      return a = nt("{model}:predict", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = KW(u), f = new WE();
        return Object.assign(f, d), f;
      });
    }
  }
  async editImageInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) {
      const a = IW(this.apiClient, e);
      return o = nt("{model}:predict", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: i.headers }, u;
      })), r.then((i) => {
        const l = xW(i), u = new wq();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async upscaleImageInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) {
      const a = JG(this.apiClient, e);
      return o = nt("{model}:predict", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: i.headers }, u;
      })), r.then((i) => {
        const l = YG(i), u = new Sq();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async recontextImage(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) {
      const a = IG(this.apiClient, e);
      return o = nt("{model}:predict", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => {
        const l = xG(i), u = new Tq();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async segmentImage(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) {
      const a = DG(this.apiClient, e);
      return o = nt("{model}:predict", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => {
        const l = LG(i), u = new Eq();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async get(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = dG(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s.then((u) => S1(u));
    } else {
      const l = cG(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s.then((u) => w1(u));
    }
  }
  async listInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = bG(this.apiClient, e);
      return a = nt("{models_url}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = SG(u), f = new GE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = _G(this.apiClient, e);
      return a = nt("{models_url}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = wG(u), f = new GE();
        return Object.assign(f, d), f;
      });
    }
  }
  async update(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = VG(this.apiClient, e);
      return a = nt("{model}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "PATCH",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s.then((u) => S1(u));
    } else {
      const l = GG(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "PATCH",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s.then((u) => w1(u));
    }
  }
  async delete(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = EW(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = CW(u), f = new VE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = TW(this.apiClient, e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = AW(u), f = new VE();
        return Object.assign(f, d), f;
      });
    }
  }
  async countTokens(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = bW(this.apiClient, e);
      return a = nt("{model}:countTokens", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = SW(u), f = new KE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = _W(this.apiClient, e);
      return a = nt("{model}:countTokens", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = wW(u), f = new KE();
        return Object.assign(f, d), f;
      });
    }
  }
  async computeTokens(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) {
      const a = fW(this.apiClient, e);
      return o = nt("{model}:computeTokens", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: i.headers }, u;
      })), r.then((i) => {
        const l = hW(i), u = new Aq();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async generateVideosInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = eG(this.apiClient, e);
      return a = nt("{model}:predictLongRunning", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s.then((u) => {
        const d = ZW(u), f = new JE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = jW(this.apiClient, e);
      return a = nt("{model}:predictLongRunning", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s.then((u) => {
        const d = QW(u), f = new JE();
        return Object.assign(f, d), f;
      });
    }
  }
}, GV = class extends ta {
  constructor(e) {
    super(), this.apiClient = e;
  }
  async getVideosOperation(e) {
    const t = e.operation, n = e.config;
    if (t.name === void 0 || t.name === "") throw new Error("Operation name is required.");
    if (this.apiClient.isVertexAI()) {
      const r = t.name.split("/operations/")[0];
      let o;
      n && "httpOptions" in n && (o = n.httpOptions);
      const s = await this.fetchPredictVideosOperationInternal({
        operationName: t.name,
        resourceName: r,
        config: { httpOptions: o }
      });
      return t._fromAPIResponse({
        apiResponse: s,
        _isVertexAI: !0
      });
    } else {
      const r = await this.getVideosOperationInternal({
        operationName: t.name,
        config: n
      });
      return t._fromAPIResponse({
        apiResponse: r,
        _isVertexAI: !1
      });
    }
  }
  async get(e) {
    const t = e.operation, n = e.config;
    if (t.name === void 0 || t.name === "") throw new Error("Operation name is required.");
    if (this.apiClient.isVertexAI()) {
      const r = t.name.split("/operations/")[0];
      let o;
      n && "httpOptions" in n && (o = n.httpOptions);
      const s = await this.fetchPredictVideosOperationInternal({
        operationName: t.name,
        resourceName: r,
        config: { httpOptions: o }
      });
      return t._fromAPIResponse({
        apiResponse: s,
        _isVertexAI: !0
      });
    } else {
      const r = await this.getVideosOperationInternal({
        operationName: t.name,
        config: n
      });
      return t._fromAPIResponse({
        apiResponse: r,
        _isVertexAI: !1
      });
    }
  }
  async getVideosOperationInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = mq(e);
      return a = nt("{operationName}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), s;
    } else {
      const l = pq(e);
      return a = nt("{operationName}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json()), s;
    }
  }
  async fetchPredictVideosOperationInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) {
      const a = iq(e);
      return o = nt("{resourceName}:fetchPredictOperation", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r;
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
};
function yA(e) {
  const t = {};
  if (c(e, ["languageCodes"]) !== void 0) throw new Error("languageCodes parameter is not supported in Gemini API.");
  return t;
}
function VV(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && h(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function KV(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && h(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function JV(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let o = n;
    Array.isArray(o) && (o = o.map((s) => rK(s))), h(t, ["parts"], o);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function YV(e, t, n) {
  const r = {}, o = c(t, ["expireTime"]);
  n !== void 0 && o != null && h(n, ["expireTime"], o);
  const s = c(t, ["newSessionExpireTime"]);
  n !== void 0 && s != null && h(n, ["newSessionExpireTime"], s);
  const a = c(t, ["uses"]);
  n !== void 0 && a != null && h(n, ["uses"], a);
  const i = c(t, ["liveConnectConstraints"]);
  n !== void 0 && i != null && h(n, ["bidiGenerateContentSetup"], nK(e, i));
  const l = c(t, ["lockAdditionalFields"]);
  return n !== void 0 && l != null && h(n, ["fieldMask"], l), r;
}
function XV(e, t) {
  const n = {}, r = c(t, ["config"]);
  return r != null && h(n, ["config"], YV(e, r, n)), n;
}
function QV(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && h(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function ZV(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && h(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && h(t, ["args"], r);
  const o = c(e, ["name"]);
  if (o != null && h(t, ["name"], o), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function jV(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && h(t, ["authConfig"], VV(n));
  const r = c(e, ["enableWidget"]);
  return r != null && h(t, ["enableWidget"], r), t;
}
function eK(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && h(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && h(t, ["timeRangeFilter"], r), t;
}
function tK(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && h(t, ["setup", "generationConfig"], r);
  const o = c(e, ["responseModalities"]);
  t !== void 0 && o != null && h(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], o);
  const s = c(e, ["temperature"]);
  t !== void 0 && s != null && h(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], s);
  const a = c(e, ["topP"]);
  t !== void 0 && a != null && h(t, [
    "setup",
    "generationConfig",
    "topP"
  ], a);
  const i = c(e, ["topK"]);
  t !== void 0 && i != null && h(t, [
    "setup",
    "generationConfig",
    "topK"
  ], i);
  const l = c(e, ["maxOutputTokens"]);
  t !== void 0 && l != null && h(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], l);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && h(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && h(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const f = c(e, ["speechConfig"]);
  t !== void 0 && f != null && h(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], Hb(f));
  const m = c(e, ["thinkingConfig"]);
  t !== void 0 && m != null && h(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], m);
  const v = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && v != null && h(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], v);
  const y = c(e, ["systemInstruction"]);
  t !== void 0 && y != null && h(t, ["setup", "systemInstruction"], JV(zn(y)));
  const T = c(e, ["tools"]);
  if (t !== void 0 && T != null) {
    let E = Lu(T);
    Array.isArray(E) && (E = E.map((x) => aK(Du(x)))), h(t, ["setup", "tools"], E);
  }
  const C = c(e, ["sessionResumption"]);
  t !== void 0 && C != null && h(t, ["setup", "sessionResumption"], sK(C));
  const A = c(e, ["inputAudioTranscription"]);
  t !== void 0 && A != null && h(t, ["setup", "inputAudioTranscription"], yA(A));
  const I = c(e, ["outputAudioTranscription"]);
  t !== void 0 && I != null && h(t, ["setup", "outputAudioTranscription"], yA(I));
  const R = c(e, ["realtimeInputConfig"]);
  t !== void 0 && R != null && h(t, ["setup", "realtimeInputConfig"], R);
  const $ = c(e, ["contextWindowCompression"]);
  t !== void 0 && $ != null && h(t, ["setup", "contextWindowCompression"], $);
  const p = c(e, ["proactivity"]);
  if (t !== void 0 && p != null && h(t, ["setup", "proactivity"], p), c(e, ["explicitVadSignal"]) !== void 0) throw new Error("explicitVadSignal parameter is not supported in Gemini API.");
  const b = c(e, ["avatarConfig"]);
  t !== void 0 && b != null && h(t, ["setup", "avatarConfig"], b);
  const w = c(e, ["safetySettings"]);
  if (t !== void 0 && w != null) {
    let E = w;
    Array.isArray(E) && (E = E.map((x) => oK(x))), h(t, ["setup", "safetySettings"], E);
  }
  return n;
}
function nK(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["setup", "model"], Xt(e, r));
  const o = c(t, ["config"]);
  return o != null && h(n, ["config"], tK(o, n)), n;
}
function rK(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const o = c(e, ["executableCode"]);
  o != null && h(t, ["executableCode"], o);
  const s = c(e, ["fileData"]);
  s != null && h(t, ["fileData"], QV(s));
  const a = c(e, ["functionCall"]);
  a != null && h(t, ["functionCall"], ZV(a));
  const i = c(e, ["functionResponse"]);
  i != null && h(t, ["functionResponse"], i);
  const l = c(e, ["inlineData"]);
  l != null && h(t, ["inlineData"], KV(l));
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  m != null && h(t, ["videoMetadata"], m);
  const v = c(e, ["toolCall"]);
  v != null && h(t, ["toolCall"], v);
  const y = c(e, ["toolResponse"]);
  y != null && h(t, ["toolResponse"], y);
  const T = c(e, ["partMetadata"]);
  return T != null && h(t, ["partMetadata"], T), t;
}
function oK(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && h(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && h(t, ["threshold"], r), t;
}
function sK(e) {
  const t = {}, n = c(e, ["handle"]);
  if (n != null && h(t, ["handle"], n), c(e, ["transparent"]) !== void 0) throw new Error("transparent parameter is not supported in Gemini API.");
  return t;
}
function aK(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && h(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && h(t, ["fileSearch"], r);
  const o = c(e, ["googleSearch"]);
  o != null && h(t, ["googleSearch"], eK(o));
  const s = c(e, ["googleMaps"]);
  s != null && h(t, ["googleMaps"], jV(s));
  const a = c(e, ["codeExecution"]);
  if (a != null && h(t, ["codeExecution"], a), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const i = c(e, ["functionDeclarations"]);
  if (i != null) {
    let f = i;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["functionDeclarations"], f);
  }
  const l = c(e, ["googleSearchRetrieval"]);
  if (l != null && h(t, ["googleSearchRetrieval"], l), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && h(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let f = d;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["mcpServers"], f);
  }
  return t;
}
function iK(e) {
  const t = [];
  for (const n in e) if (Object.prototype.hasOwnProperty.call(e, n)) {
    const r = e[n];
    if (typeof r == "object" && r != null && Object.keys(r).length > 0) {
      const o = Object.keys(r).map((s) => `${n}.${s}`);
      t.push(...o);
    } else t.push(n);
  }
  return t.join(",");
}
function lK(e, t) {
  let n = null;
  const r = e.bidiGenerateContentSetup;
  if (typeof r == "object" && r !== null && "setup" in r) {
    const s = r.setup;
    typeof s == "object" && s !== null ? (e.bidiGenerateContentSetup = s, n = s) : delete e.bidiGenerateContentSetup;
  } else r !== void 0 && delete e.bidiGenerateContentSetup;
  const o = e.fieldMask;
  if (n) {
    const s = iK(n);
    if (Array.isArray(t?.lockAdditionalFields) && t?.lockAdditionalFields.length === 0) s ? e.fieldMask = s : delete e.fieldMask;
    else if (t?.lockAdditionalFields && t.lockAdditionalFields.length > 0 && o !== null && Array.isArray(o) && o.length > 0) {
      const a = [
        "temperature",
        "topK",
        "topP",
        "maxOutputTokens",
        "responseModalities",
        "seed",
        "speechConfig"
      ];
      let i = [];
      o.length > 0 && (i = o.map((u) => a.includes(u) ? `generationConfig.${u}` : u));
      const l = [];
      s && l.push(s), i.length > 0 && l.push(...i), l.length > 0 ? e.fieldMask = l.join(",") : delete e.fieldMask;
    } else delete e.fieldMask;
  } else o !== null && Array.isArray(o) && o.length > 0 ? e.fieldMask = o.join(",") : delete e.fieldMask;
  return e;
}
var uK = class extends ta {
  constructor(e) {
    super(), this.apiClient = e;
  }
  async create(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("The client.tokens.create method is only supported by the Gemini Developer API.");
    {
      const a = XV(this.apiClient, e);
      o = nt("auth_tokens", a._url), s = a._query, delete a.config, delete a._url, delete a._query;
      const i = lK(a, e.config);
      return r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => l);
    }
  }
};
function cK(e, t) {
  const n = {}, r = c(e, ["force"]);
  return t !== void 0 && r != null && h(t, ["_query", "force"], r), n;
}
function dK(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["_url", "name"], n);
  const r = c(e, ["config"]);
  return r != null && cK(r, t), t;
}
function fK(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && h(t, ["_url", "name"], n), t;
}
function hK(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const o = c(e, ["pageToken"]);
  return t !== void 0 && o != null && h(t, ["_query", "pageToken"], o), n;
}
function pK(e) {
  const t = {}, n = c(e, ["parent"]);
  n != null && h(t, ["_url", "parent"], n);
  const r = c(e, ["config"]);
  return r != null && hK(r, t), t;
}
function mK(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const o = c(e, ["documents"]);
  if (o != null) {
    let s = o;
    Array.isArray(s) && (s = s.map((a) => a)), h(t, ["documents"], s);
  }
  return t;
}
var gK = class extends ta {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t) => new tl(js.PAGED_ITEM_DOCUMENTS, (n) => this.listInternal({
      parent: t.parent,
      config: n.config
    }), await this.listInternal(t), t);
  }
  async get(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = fK(e);
      return o = nt("{name}", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => i);
    }
  }
  async delete(e) {
    var t, n;
    let r = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const s = dK(e);
      r = nt("{name}", s._url), o = s._query, delete s._url, delete s._query, await this.apiClient.request({
        path: r,
        queryParams: o,
        body: JSON.stringify(s),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = pK(e);
      return o = nt("{parent}/documents", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => {
        const l = mK(i), u = new Cq();
        return Object.assign(u, l), u;
      });
    }
  }
}, vK = class extends ta {
  constructor(e, t = new gK(e)) {
    super(), this.apiClient = e, this.documents = t, this.list = async (n = {}) => new tl(js.PAGED_ITEM_FILE_SEARCH_STORES, (r) => this.listInternal(r), await this.listInternal(n), n);
  }
  async uploadToFileSearchStore(e) {
    if (this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support uploading files to a file search store.");
    return this.apiClient.uploadFileToFileSearchStore(e.fileSearchStoreName, e.file, e.config);
  }
  async create(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = nV(e);
      return o = nt("fileSearchStores", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => i);
    }
  }
  async get(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = sV(e);
      return o = nt("{name}", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => i);
    }
  }
  async delete(e) {
    var t, n;
    let r = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const s = oV(e);
      r = nt("{name}", s._url), o = s._query, delete s._url, delete s._query, await this.apiClient.request({
        path: r,
        queryParams: o,
        body: JSON.stringify(s),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = dV(e);
      return o = nt("fileSearchStores", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => {
        const l = fV(i), u = new kq();
        return Object.assign(u, l), u;
      });
    }
  }
  async uploadToFileSearchStoreInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = hV(e);
      return o = nt("upload/v1beta/{file_search_store_name}:uploadToFileSearchStore", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => {
        const l = pV(i), u = new Iq();
        return Object.assign(u, l), u;
      });
    }
  }
  async importFile(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = lV(e);
      return o = nt("{file_search_store_name}:importFile", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => {
        const l = iV(i), u = new xq();
        return Object.assign(u, l), u;
      });
    }
  }
}, V3 = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return V3 = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
}, yK = () => V3();
function E1(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var A1 = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, _o = class extends Error {
}, bo = class C1 extends _o {
  constructor(t, n, r, o) {
    super(`${C1.makeMessage(t, n, r)}`), this.status = t, this.headers = o, this.error = n;
  }
  static makeMessage(t, n, r) {
    const o = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && o ? `${t} ${o}` : t ? `${t} status code (no body)` : o || "(no status code or body)";
  }
  static generate(t, n, r, o) {
    if (!t || !o) return new wg({
      message: r,
      cause: A1(n)
    });
    const s = n;
    return t === 400 ? new J3(t, s, r, o) : t === 401 ? new Y3(t, s, r, o) : t === 403 ? new X3(t, s, r, o) : t === 404 ? new Q3(t, s, r, o) : t === 409 ? new Z3(t, s, r, o) : t === 422 ? new j3(t, s, r, o) : t === 429 ? new e4(t, s, r, o) : t >= 500 ? new t4(t, s, r, o) : new C1(t, s, r, o);
  }
}, k1 = class extends bo {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, wg = class extends bo {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, K3 = class extends wg {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, J3 = class extends bo {
}, Y3 = class extends bo {
}, X3 = class extends bo {
}, Q3 = class extends bo {
}, Z3 = class extends bo {
}, j3 = class extends bo {
}, e4 = class extends bo {
}, t4 = class extends bo {
}, _K = /^[a-z][a-z0-9+.-]*:/i, bK = (e) => _K.test(e), I1 = (e) => (I1 = Array.isArray, I1(e)), _A = I1;
function bA(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function wK(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
var SK = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new _o(`${e} must be an integer`);
  if (t < 0) throw new _o(`${e} must be a positive integer`);
  return t;
}, TK = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, EK = (e) => new Promise((t) => setTimeout(t, e));
function AK() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new GeminiNextGenAPIClient({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function n4(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function CK(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return n4({
    start() {
    },
    async pull(n) {
      const { done: r, value: o } = await t.next();
      r ? n.close() : n.enqueue(o);
    },
    async cancel() {
      var n;
      await ((n = t.return) === null || n === void 0 ? void 0 : n.call(t));
    }
  });
}
function r4(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function kK(e) {
  var t, n;
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await ((n = (t = e[Symbol.asyncIterator]()).return) === null || n === void 0 ? void 0 : n.call(t));
    return;
  }
  const r = e.getReader(), o = r.cancel();
  r.releaseLock(), await o;
}
var IK = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
});
function xK(e) {
  return Object.entries(e).filter(([t, n]) => typeof n < "u").map(([t, n]) => {
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") return `${encodeURIComponent(t)}=${encodeURIComponent(n)}`;
    if (n === null) return `${encodeURIComponent(t)}=`;
    throw new _o(`Cannot stringify type ${typeof n}; Expected string, number, boolean, or null. If you need to pass nested query parameters, you can manually encode them, e.g. { query: { 'foo[key1]': value1, 'foo[key2]': value2 } }, and please open a GitHub issue requesting better support for your use case.`);
  }).join("&");
}
var MK = "0.0.1", o4 = () => {
  var e;
  if (typeof File > "u") {
    const { process: t } = globalThis, n = typeof ((e = t?.versions) === null || e === void 0 ? void 0 : e.node) == "string" && parseInt(t.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (n ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function Ay(e, t, n) {
  return o4(), new File(e, t ?? "unknown_file", n);
}
function PK(e) {
  return (typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "").split(/[\\/]/).pop() || void 0;
}
var RK = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", s4 = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", NK = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && s4(e), $K = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function DK(e, t, n) {
  if (o4(), e = await e, NK(e))
    return e instanceof File ? e : Ay([await e.arrayBuffer()], e.name);
  if ($K(e)) {
    const o = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), Ay(await x1(o), t, n);
  }
  const r = await x1(e);
  if (t || (t = PK(e)), !n?.type) {
    const o = r.find((s) => typeof s == "object" && "type" in s && s.type);
    typeof o == "string" && (n = Object.assign(Object.assign({}, n), { type: o }));
  }
  return Ay(r, t, n);
}
async function x1(e) {
  var t, n, r, o, s;
  let a = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) a.push(e);
  else if (s4(e)) a.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (RK(e)) try {
    for (var i = !0, l = Do(e), u; u = await l.next(), t = u.done, !t; i = !0) {
      o = u.value, i = !1;
      const d = o;
      a.push(...await x1(d));
    }
  } catch (d) {
    n = { error: d };
  } finally {
    try {
      !i && !t && (r = l.return) && await r.call(l);
    } finally {
      if (n) throw n.error;
    }
  }
  else {
    const d = (s = e?.constructor) === null || s === void 0 ? void 0 : s.name;
    throw new Error(`Unexpected data type: ${typeof e}${d ? `; constructor: ${d}` : ""}${LK(e)}`);
  }
  return a;
}
function LK(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var zb = class {
  constructor(e) {
    this._client = e;
  }
};
zb._key = [];
function a4(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var wA = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), OK = (e = a4) => (function(n, ...r) {
  if (n.length === 1) return n[0];
  let o = !1;
  const s = [], a = n.reduce((d, f, m) => {
    var v, y, T;
    /[?#]/.test(f) && (o = !0);
    const C = r[m];
    let A = (o ? encodeURIComponent : e)("" + C);
    return m !== r.length && (C == null || typeof C == "object" && C.toString === ((T = Object.getPrototypeOf((y = Object.getPrototypeOf((v = C.hasOwnProperty) !== null && v !== void 0 ? v : wA)) !== null && y !== void 0 ? y : wA)) === null || T === void 0 ? void 0 : T.toString)) && (A = C + "", s.push({
      start: d.length + f.length,
      length: A.length,
      error: `Value of type ${Object.prototype.toString.call(C).slice(8, -1)} is not a valid path parameter`
    })), d + f + (m === r.length ? "" : A);
  }, ""), i = a.split(/[?#]/, 1)[0], l = /(^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = l.exec(i)) !== null; ) {
    const d = u[0].startsWith("/"), f = d ? 1 : 0, m = d ? u[0].slice(1) : u[0];
    s.push({
      start: u.index + f,
      length: m.length,
      error: `Value "${m}" can't be safely passed as a path parameter`
    });
  }
  if (s.sort((d, f) => d.start - f.start), s.length > 0) {
    let d = 0;
    const f = s.reduce((m, v) => {
      const y = " ".repeat(v.start - d), T = "^".repeat(v.length);
      return d = v.start + v.length, m + y + T;
    }, "");
    throw new _o(`Path parameters result in path with invalid segments:
${s.map((m) => m.error).join(`
`)}
${a}
${f}`);
  }
  return a;
}), Eo = /* @__PURE__ */ OK(a4), i4 = class extends zb {
  create(e, t) {
    var n;
    const { api_version: r = this._client.apiVersion } = e, o = Ra(e, ["api_version"]);
    if ("model" in o && "agent_config" in o) throw new _o("Invalid request: specified `model` and `agent_config`. If specifying `model`, use `generation_config`.");
    if ("agent" in o && "generation_config" in o) throw new _o("Invalid request: specified `agent` and `generation_config`. If specifying `agent`, use `agent_config`.");
    return this._client.post(Eo`/${r}/interactions`, Object.assign(Object.assign({ body: o }, t), { stream: (n = e.stream) !== null && n !== void 0 ? n : !1 }));
  }
  delete(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.delete(Eo`/${r}/interactions/${e}`, n);
  }
  cancel(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.post(Eo`/${r}/interactions/${e}/cancel`, n);
  }
  get(e, t = {}, n) {
    var r;
    const o = t ?? {}, { api_version: s = this._client.apiVersion } = o, a = Ra(o, ["api_version"]);
    return this._client.get(Eo`/${s}/interactions/${e}`, Object.assign(Object.assign({ query: a }, n), { stream: (r = t?.stream) !== null && r !== void 0 ? r : !1 }));
  }
};
i4._key = Object.freeze(["interactions"]);
var l4 = class extends i4 {
}, u4 = class extends zb {
  create(e, t) {
    const { api_version: n = this._client.apiVersion, webhook_id: r } = e, o = Ra(e, ["api_version", "webhook_id"]);
    return this._client.post(Eo`/${n}/webhooks`, Object.assign({
      query: { webhook_id: r },
      body: o
    }, t));
  }
  update(e, t, n) {
    const { api_version: r = this._client.apiVersion, update_mask: o } = t, s = Ra(t, ["api_version", "update_mask"]);
    return this._client.patch(Eo`/${r}/webhooks/${e}`, Object.assign({
      query: { update_mask: o },
      body: s
    }, n));
  }
  list(e = {}, t) {
    const n = e ?? {}, { api_version: r = this._client.apiVersion } = n, o = Ra(n, ["api_version"]);
    return this._client.get(Eo`/${r}/webhooks`, Object.assign({ query: o }, t));
  }
  delete(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.delete(Eo`/${r}/webhooks/${e}`, n);
  }
  get(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.get(Eo`/${r}/webhooks/${e}`, n);
  }
  ping(e, t = void 0, n) {
    const { api_version: r = this._client.apiVersion, body: o } = t ?? {};
    return this._client.post(Eo`/${r}/webhooks/${e}:ping`, Object.assign({ body: o }, n));
  }
  rotateSigningSecret(e, t = {}, n) {
    const r = t ?? {}, { api_version: o = this._client.apiVersion } = r, s = Ra(r, ["api_version"]);
    return this._client.post(Eo`/${o}/webhooks/${e}:rotateSigningSecret`, Object.assign({ body: s }, n));
  }
};
u4._key = Object.freeze(["webhooks"]);
var c4 = class extends u4 {
};
function FK(e) {
  let t = 0;
  for (const o of e) t += o.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const o of e)
    n.set(o, r), r += o.length;
  return n;
}
var Rh;
function Wb(e) {
  let t;
  return (Rh ?? (t = new globalThis.TextEncoder(), Rh = t.encode.bind(t)))(e);
}
var Nh;
function SA(e) {
  let t;
  return (Nh ?? (t = new globalThis.TextDecoder(), Nh = t.decode.bind(t)))(e);
}
var Sg = class {
  constructor() {
    this.buffer = new Uint8Array(), this.carriageReturnIndex = null, this.searchIndex = 0;
  }
  decode(e) {
    var t;
    if (e == null) return [];
    const n = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? Wb(e) : e;
    this.buffer = FK([this.buffer, n]);
    const r = [];
    let o;
    for (; (o = BK(this.buffer, (t = this.carriageReturnIndex) !== null && t !== void 0 ? t : this.searchIndex)) != null; ) {
      if (o.carriage && this.carriageReturnIndex == null) {
        this.carriageReturnIndex = o.index;
        continue;
      }
      if (this.carriageReturnIndex != null && (o.index !== this.carriageReturnIndex + 1 || o.carriage)) {
        r.push(SA(this.buffer.subarray(0, this.carriageReturnIndex - 1))), this.buffer = this.buffer.subarray(this.carriageReturnIndex), this.carriageReturnIndex = null, this.searchIndex = 0;
        continue;
      }
      const s = this.carriageReturnIndex !== null ? o.preceding - 1 : o.preceding, a = SA(this.buffer.subarray(0, s));
      r.push(a), this.buffer = this.buffer.subarray(o.index), this.carriageReturnIndex = null, this.searchIndex = 0;
    }
    return this.searchIndex = Math.max(0, this.buffer.length - 1), r;
  }
  flush() {
    return this.buffer.length ? this.decode(`
`) : [];
  }
};
Sg.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
Sg.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function BK(e, t) {
  const o = t ?? 0, s = e.indexOf(10, o), a = e.indexOf(13, o);
  if (s === -1 && a === -1) return null;
  let i;
  return s !== -1 && a !== -1 ? i = Math.min(s, a) : i = s !== -1 ? s : a, e[i] === 10 ? {
    preceding: i,
    index: i + 1,
    carriage: !1
  } : {
    preceding: i,
    index: i + 1,
    carriage: !0
  };
}
var bm = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, TA = (e, t, n) => {
  if (e) {
    if (wK(bm, e)) return e;
    pr(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(bm))}`);
  }
};
function zc() {
}
function $h(e, t, n) {
  return !t || bm[e] > bm[n] ? zc : t[e].bind(t);
}
var UK = {
  error: zc,
  warn: zc,
  info: zc,
  debug: zc
}, EA = /* @__PURE__ */ new WeakMap();
function pr(e) {
  var t;
  const n = e.logger, r = (t = e.logLevel) !== null && t !== void 0 ? t : "off";
  if (!n) return UK;
  const o = EA.get(n);
  if (o && o[0] === r) return o[1];
  const s = {
    error: $h("error", n, r),
    warn: $h("warn", n, r),
    info: $h("info", n, r),
    debug: $h("debug", n, r)
  };
  return EA.set(n, [r, s]), s;
}
var _i = (e) => (e.options && (e.options = Object.assign({}, e.options), delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "x-goog-api-key" || t.toLowerCase() === "authorization" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), qK = class Wc {
  constructor(t, n, r) {
    this.iterator = t, this.controller = n, this.client = r;
  }
  static fromSSEResponse(t, n, r) {
    let o = !1;
    const s = r ? pr(r) : console;
    function a() {
      return $o(this, arguments, function* () {
        var l, u, d, f;
        if (o) throw new _o("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
        o = !0;
        let m = !1;
        try {
          try {
            for (var v = !0, y = Do(HK(t, n)), T; T = yield Rt(y.next()), l = T.done, !l; v = !0) {
              f = T.value, v = !1;
              const C = f;
              if (!m)
                if (C.data.startsWith("[DONE]")) {
                  m = !0;
                  continue;
                } else try {
                  yield yield Rt(JSON.parse(C.data));
                } catch (A) {
                  throw s.error("Could not parse message into JSON:", C.data), s.error("From chunk:", C.raw), A;
                }
            }
          } catch (C) {
            u = { error: C };
          } finally {
            try {
              !v && !l && (d = y.return) && (yield Rt(d.call(y)));
            } finally {
              if (u) throw u.error;
            }
          }
          m = !0;
        } catch (C) {
          if (E1(C)) return yield Rt(void 0);
          throw C;
        } finally {
          m || n.abort();
        }
      });
    }
    return new Wc(a, n, r);
  }
  static fromReadableStream(t, n, r) {
    let o = !1;
    function s() {
      return $o(this, arguments, function* () {
        var l, u, d, f;
        const m = new Sg(), v = r4(t);
        try {
          for (var y = !0, T = Do(v), C; C = yield Rt(T.next()), l = C.done, !l; y = !0) {
            f = C.value, y = !1;
            const A = f;
            for (const I of m.decode(A)) yield yield Rt(I);
          }
        } catch (A) {
          u = { error: A };
        } finally {
          try {
            !y && !l && (d = T.return) && (yield Rt(d.call(T)));
          } finally {
            if (u) throw u.error;
          }
        }
        for (const A of m.flush()) yield yield Rt(A);
      });
    }
    function a() {
      return $o(this, arguments, function* () {
        var l, u, d, f;
        if (o) throw new _o("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
        o = !0;
        let m = !1;
        try {
          try {
            for (var v = !0, y = Do(s()), T; T = yield Rt(y.next()), l = T.done, !l; v = !0) {
              f = T.value, v = !1;
              const C = f;
              m || C && (yield yield Rt(JSON.parse(C)));
            }
          } catch (C) {
            u = { error: C };
          } finally {
            try {
              !v && !l && (d = y.return) && (yield Rt(d.call(y)));
            } finally {
              if (u) throw u.error;
            }
          }
          m = !0;
        } catch (C) {
          if (E1(C)) return yield Rt(void 0);
          throw C;
        } finally {
          m || n.abort();
        }
      });
    }
    return new Wc(a, n, r);
  }
  [Symbol.asyncIterator]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), o = (s) => ({ next: () => {
      if (s.length === 0) {
        const a = r.next();
        t.push(a), n.push(a);
      }
      return s.shift();
    } });
    return [new Wc(() => o(t), this.controller, this.client), new Wc(() => o(n), this.controller, this.client)];
  }
  toReadableStream() {
    const t = this;
    let n;
    return n4({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: o, done: s } = await n.next();
          if (s) return r.close();
          const a = Wb(JSON.stringify(o) + `
`);
          r.enqueue(a);
        } catch (o) {
          r.error(o);
        }
      },
      async cancel() {
        var r;
        await ((r = n.return) === null || r === void 0 ? void 0 : r.call(n));
      }
    });
  }
};
function HK(e, t) {
  return $o(this, arguments, function* () {
    var r, o, s, a;
    if (!e.body)
      throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new _o("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new _o("Attempted to iterate over a response with no body");
    const i = new WK(), l = new Sg(), u = r4(e.body);
    try {
      for (var d = !0, f = Do(zK(u)), m; m = yield Rt(f.next()), r = m.done, !r; d = !0) {
        a = m.value, d = !1;
        const v = a;
        for (const y of l.decode(v)) {
          const T = i.decode(y);
          T && (yield yield Rt(T));
        }
      }
    } catch (v) {
      o = { error: v };
    } finally {
      try {
        !d && !r && (s = f.return) && (yield Rt(s.call(f)));
      } finally {
        if (o) throw o.error;
      }
    }
    for (const v of l.flush()) {
      const y = i.decode(v);
      y && (yield yield Rt(y));
    }
  });
}
function zK(e) {
  return $o(this, arguments, function* () {
    var n, r, o, s;
    try {
      for (var a = !0, i = Do(e), l; l = yield Rt(i.next()), n = l.done, !n; a = !0) {
        s = l.value, a = !1;
        const u = s;
        u != null && (yield yield Rt(u instanceof ArrayBuffer ? new Uint8Array(u) : typeof u == "string" ? Wb(u) : u));
      }
    } catch (u) {
      r = { error: u };
    } finally {
      try {
        !a && !n && (o = i.return) && (yield Rt(o.call(i)));
      } finally {
        if (r) throw r.error;
      }
    }
  });
}
var WK = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const o = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], o;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = GK(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function GK(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function VK(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: o, startTime: s } = t, a = await (async () => {
    var i;
    if (t.options.stream)
      return pr(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller, e) : qK.fromSSEResponse(n, t.controller, e);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const l = n.headers.get("content-type"), u = (i = l?.split(";")[0]) === null || i === void 0 ? void 0 : i.trim();
    return u?.includes("application/json") || u?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : await n.json() : await n.text();
  })();
  return pr(e).debug(`[${r}] response parsed`, _i({
    retryOfRequestLogID: o,
    url: n.url,
    status: n.status,
    body: a,
    durationMs: Date.now() - s
  })), a;
}
var KK = class d4 extends Promise {
  constructor(t, n, r = VK) {
    super((o) => {
      o(null);
    }), this.responsePromise = n, this.parseResponse = r, this.client = t;
  }
  _thenUnwrap(t) {
    return new d4(this.client, this.responsePromise, async (n, r) => t(await this.parseResponse(n, r), r));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(this.client, t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
}, f4 = /* @__PURE__ */ Symbol("brand.privateNullableHeaders");
function* JK(e) {
  if (!e) return;
  if (f4 in e) {
    const { values: r, nulls: o } = e;
    yield* r.entries();
    for (const s of o) yield [s, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : _A(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const o = r[0];
    if (typeof o != "string") throw new TypeError("expected header name to be a string");
    const s = _A(r[1]) ? r[1] : [r[1]];
    let a = !1;
    for (const i of s)
      i !== void 0 && (t && !a && (a = !0, yield [o, null]), yield [o, i]);
  }
}
var Ic = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = /* @__PURE__ */ new Set();
    for (const [s, a] of JK(r)) {
      const i = s.toLowerCase();
      o.has(i) || (t.delete(s), o.add(i)), a === null ? (t.delete(s), n.add(i)) : (t.append(s, a), n.delete(i));
    }
  }
  return {
    [f4]: !0,
    values: t,
    nulls: n
  };
}, Cy = (e) => {
  var t, n, r, o, s;
  if (typeof globalThis.process < "u") return ((n = (t = globalThis.process.env) === null || t === void 0 ? void 0 : t[e]) === null || n === void 0 ? void 0 : n.trim()) || void 0;
  if (typeof globalThis.Deno < "u") return ((s = (o = (r = globalThis.Deno.env) === null || r === void 0 ? void 0 : r.get) === null || o === void 0 ? void 0 : o.call(r, e)) === null || s === void 0 ? void 0 : s.trim()) || void 0;
}, h4, p4 = class m4 {
  constructor(t) {
    var n, r, o, s, a, i, l, { baseURL: u = Cy("GEMINI_NEXT_GEN_API_BASE_URL"), apiKey: d = (n = Cy("GEMINI_API_KEY")) !== null && n !== void 0 ? n : null, apiVersion: f = "v1beta" } = t, m = Ra(t, [
      "baseURL",
      "apiKey",
      "apiVersion"
    ]);
    const v = Object.assign(Object.assign({
      apiKey: d,
      apiVersion: f
    }, m), { baseURL: u || "https://generativelanguage.googleapis.com" });
    this.baseURL = v.baseURL, this.timeout = (r = v.timeout) !== null && r !== void 0 ? r : m4.DEFAULT_TIMEOUT, this.logger = (o = v.logger) !== null && o !== void 0 ? o : console;
    const y = "warn";
    this.logLevel = y, this.logLevel = (a = (s = TA(v.logLevel, "ClientOptions.logLevel", this)) !== null && s !== void 0 ? s : TA(Cy("GEMINI_NEXT_GEN_API_LOG"), "process.env['GEMINI_NEXT_GEN_API_LOG']", this)) !== null && a !== void 0 ? a : y, this.fetchOptions = v.fetchOptions, this.maxRetries = (i = v.maxRetries) !== null && i !== void 0 ? i : 2, this.fetch = (l = v.fetch) !== null && l !== void 0 ? l : AK(), this.encoder = IK, this._options = v, this.apiKey = d, this.apiVersion = f, this.clientAdapter = v.clientAdapter;
  }
  withOptions(t) {
    return new this.constructor(Object.assign(Object.assign(Object.assign({}, this._options), {
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this.apiKey,
      apiVersion: this.apiVersion
    }), t));
  }
  baseURLOverridden() {
    return this.baseURL !== "https://generativelanguage.googleapis.com";
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: t, nulls: n }) {
    if (!(t.has("authorization") || t.has("x-goog-api-key")) && !(this.apiKey && t.get("x-goog-api-key")) && !n.has("x-goog-api-key"))
      throw new Error('Could not resolve authentication method. Expected the apiKey to be set. Or for the "x-goog-api-key" headers to be explicitly omitted');
  }
  async authHeaders(t) {
    const n = Ic([t.headers]);
    if (!(n.values.has("authorization") || n.values.has("x-goog-api-key"))) {
      if (this.apiKey) return Ic([{ "x-goog-api-key": this.apiKey }]);
      if (this.clientAdapter && this.clientAdapter.isVertexAI()) return Ic([await this.clientAdapter.getAuthHeaders()]);
    }
  }
  stringifyQuery(t) {
    return xK(t);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${MK}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${yK()}`;
  }
  makeStatusError(t, n, r, o) {
    return bo.generate(t, n, r, o);
  }
  buildURL(t, n, r) {
    const o = !this.baseURLOverridden() && r || this.baseURL, s = bK(t) ? new URL(t) : new URL(o + (o.endsWith("/") && t.startsWith("/") ? t.slice(1) : t)), a = this.defaultQuery(), i = Object.fromEntries(s.searchParams);
    return (!bA(a) || !bA(i)) && (n = Object.assign(Object.assign(Object.assign({}, i), a), n)), typeof n == "object" && n && !Array.isArray(n) && (s.search = this.stringifyQuery(n)), s.toString();
  }
  async prepareOptions(t) {
    if (this.clientAdapter && this.clientAdapter.isVertexAI() && !t.path.startsWith(`/${this.apiVersion}/projects/`)) {
      const n = t.path.slice(this.apiVersion.length + 1);
      t.path = `/${this.apiVersion}/projects/${this.clientAdapter.getProject()}/locations/${this.clientAdapter.getLocation()}${n}`;
    }
  }
  async prepareRequest(t, { url: n, options: r }) {
  }
  get(t, n) {
    return this.methodRequest("get", t, n);
  }
  post(t, n) {
    return this.methodRequest("post", t, n);
  }
  patch(t, n) {
    return this.methodRequest("patch", t, n);
  }
  put(t, n) {
    return this.methodRequest("put", t, n);
  }
  delete(t, n) {
    return this.methodRequest("delete", t, n);
  }
  methodRequest(t, n, r) {
    return this.request(Promise.resolve(r).then((o) => Object.assign({
      method: t,
      path: n
    }, o)));
  }
  request(t, n = null) {
    return new KK(this, this.makeRequest(t, n, void 0));
  }
  async makeRequest(t, n, r) {
    var o, s, a;
    const i = await t, l = (o = i.maxRetries) !== null && o !== void 0 ? o : this.maxRetries;
    n == null && (n = l), await this.prepareOptions(i);
    const { req: u, url: d, timeout: f } = await this.buildRequest(i, { retryCount: l - n });
    await this.prepareRequest(u, {
      url: d,
      options: i
    });
    const m = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), v = r === void 0 ? "" : `, retryOf: ${r}`, y = Date.now();
    if (pr(this).debug(`[${m}] sending request`, _i({
      retryOfRequestLogID: r,
      method: i.method,
      url: d,
      options: i,
      headers: u.headers
    })), !((s = i.signal) === null || s === void 0) && s.aborted) throw new k1();
    const T = new AbortController(), C = await this.fetchWithTimeout(d, u, f, T).catch(A1), A = Date.now();
    if (C instanceof globalThis.Error) {
      const R = `retrying, ${n} attempts remaining`;
      if (!((a = i.signal) === null || a === void 0) && a.aborted) throw new k1();
      const $ = E1(C) || /timed? ?out/i.test(String(C) + ("cause" in C ? String(C.cause) : ""));
      if (n)
        return pr(this).info(`[${m}] connection ${$ ? "timed out" : "failed"} - ${R}`), pr(this).debug(`[${m}] connection ${$ ? "timed out" : "failed"} (${R})`, _i({
          retryOfRequestLogID: r,
          url: d,
          durationMs: A - y,
          message: C.message
        })), this.retryRequest(i, n, r ?? m);
      throw pr(this).info(`[${m}] connection ${$ ? "timed out" : "failed"} - error; no more retries left`), pr(this).debug(`[${m}] connection ${$ ? "timed out" : "failed"} (error; no more retries left)`, _i({
        retryOfRequestLogID: r,
        url: d,
        durationMs: A - y,
        message: C.message
      })), $ ? new K3() : new wg({ cause: C });
    }
    const I = `[${m}${v}] ${u.method} ${d} ${C.ok ? "succeeded" : "failed"} with status ${C.status} in ${A - y}ms`;
    if (!C.ok) {
      const R = await this.shouldRetry(C);
      if (n && R) {
        const E = `retrying, ${n} attempts remaining`;
        return await kK(C.body), pr(this).info(`${I} - ${E}`), pr(this).debug(`[${m}] response error (${E})`, _i({
          retryOfRequestLogID: r,
          url: C.url,
          status: C.status,
          headers: C.headers,
          durationMs: A - y
        })), this.retryRequest(i, n, r ?? m, C.headers);
      }
      const $ = R ? "error; no more retries left" : "error; not retryable";
      pr(this).info(`${I} - ${$}`);
      const p = await C.text().catch((E) => A1(E).message), b = TK(p), w = b ? void 0 : p;
      throw pr(this).debug(`[${m}] response error (${$})`, _i({
        retryOfRequestLogID: r,
        url: C.url,
        status: C.status,
        headers: C.headers,
        message: w,
        durationMs: Date.now() - y
      })), this.makeStatusError(C.status, b, w, C.headers);
    }
    return pr(this).info(I), pr(this).debug(`[${m}] response start`, _i({
      retryOfRequestLogID: r,
      url: C.url,
      status: C.status,
      headers: C.headers,
      durationMs: A - y
    })), {
      response: C,
      options: i,
      controller: T,
      requestLogID: m,
      retryOfRequestLogID: r,
      startTime: y
    };
  }
  async fetchWithTimeout(t, n, r, o) {
    const s = n || {}, { signal: a, method: i } = s, l = Ra(s, ["signal", "method"]), u = this._makeAbort(o);
    a && a.addEventListener("abort", u, { once: !0 });
    const d = setTimeout(u, r), f = globalThis.ReadableStream && l.body instanceof globalThis.ReadableStream || typeof l.body == "object" && l.body !== null && Symbol.asyncIterator in l.body, m = Object.assign(Object.assign(Object.assign({ signal: o.signal }, f ? { duplex: "half" } : {}), { method: "GET" }), l);
    i && (m.method = i.toUpperCase());
    try {
      return await this.fetch.call(void 0, t, m);
    } finally {
      clearTimeout(d);
    }
  }
  async shouldRetry(t) {
    const n = t.headers.get("x-should-retry");
    return n === "true" ? !0 : n === "false" ? !1 : t.status === 408 || t.status === 409 || t.status === 429 || t.status >= 500;
  }
  async retryRequest(t, n, r, o) {
    var s;
    let a;
    const i = o?.get("retry-after-ms");
    if (i) {
      const u = parseFloat(i);
      Number.isNaN(u) || (a = u);
    }
    const l = o?.get("retry-after");
    if (l && !a) {
      const u = parseFloat(l);
      Number.isNaN(u) ? a = Date.parse(l) - Date.now() : a = u * 1e3;
    }
    if (a === void 0) {
      const u = (s = t.maxRetries) !== null && s !== void 0 ? s : this.maxRetries;
      a = this.calculateDefaultRetryTimeoutMillis(n, u);
    }
    return await EK(a), this.makeRequest(t, n - 1, r);
  }
  calculateDefaultRetryTimeoutMillis(t, n) {
    const s = n - t;
    return Math.min(0.5 * Math.pow(2, s), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  async buildRequest(t, { retryCount: n = 0 } = {}) {
    var r, o, s;
    const a = Object.assign({}, t), { method: i, path: l, query: u, defaultBaseURL: d } = a, f = this.buildURL(l, u, d);
    "timeout" in a && SK("timeout", a.timeout), a.timeout = (r = a.timeout) !== null && r !== void 0 ? r : this.timeout;
    const { bodyHeaders: m, body: v } = this.buildBody({ options: a }), y = await this.buildHeaders({
      options: t,
      method: i,
      bodyHeaders: m,
      retryCount: n
    });
    return {
      req: Object.assign(Object.assign(Object.assign(Object.assign(Object.assign({
        method: i,
        headers: y
      }, a.signal && { signal: a.signal }), globalThis.ReadableStream && v instanceof globalThis.ReadableStream && { duplex: "half" }), v && { body: v }), (o = this.fetchOptions) !== null && o !== void 0 ? o : {}), (s = a.fetchOptions) !== null && s !== void 0 ? s : {}),
      url: f,
      timeout: a.timeout
    };
  }
  async buildHeaders({ options: t, method: n, bodyHeaders: r, retryCount: o }) {
    let s = {};
    this.idempotencyHeader && n !== "get" && (t.idempotencyKey || (t.idempotencyKey = this.defaultIdempotencyKey()), s[this.idempotencyHeader] = t.idempotencyKey);
    const a = await this.authHeaders(t);
    let i = Ic([
      s,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent()
      },
      this._options.defaultHeaders,
      r,
      t.headers,
      a
    ]);
    return this.validateHeaders(i), i.values;
  }
  _makeAbort(t) {
    return () => t.abort();
  }
  buildBody({ options: { body: t, headers: n } }) {
    if (!t) return {
      bodyHeaders: void 0,
      body: void 0
    };
    const r = Ic([n]);
    return ArrayBuffer.isView(t) || t instanceof ArrayBuffer || t instanceof DataView || typeof t == "string" && r.values.has("content-type") || globalThis.Blob && t instanceof globalThis.Blob || t instanceof FormData || t instanceof URLSearchParams || globalThis.ReadableStream && t instanceof globalThis.ReadableStream ? {
      bodyHeaders: void 0,
      body: t
    } : typeof t == "object" && (Symbol.asyncIterator in t || Symbol.iterator in t && "next" in t && typeof t.next == "function") ? {
      bodyHeaders: void 0,
      body: CK(t)
    } : typeof t == "object" && r.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(t)
    } : this.encoder({
      body: t,
      headers: r
    });
  }
};
p4.DEFAULT_TIMEOUT = 6e4;
var Bn = class extends p4 {
  constructor() {
    super(...arguments), this.interactions = new l4(this), this.webhooks = new c4(this);
  }
};
h4 = Bn;
Bn.GeminiNextGenAPIClient = h4;
Bn.GeminiNextGenAPIClientError = _o;
Bn.APIError = bo;
Bn.APIConnectionError = wg;
Bn.APIConnectionTimeoutError = K3;
Bn.APIUserAbortError = k1;
Bn.NotFoundError = Q3;
Bn.ConflictError = Z3;
Bn.RateLimitError = e4;
Bn.BadRequestError = J3;
Bn.AuthenticationError = Y3;
Bn.InternalServerError = t4;
Bn.PermissionDeniedError = X3;
Bn.UnprocessableEntityError = j3;
Bn.toFile = DK;
Bn.Interactions = l4;
Bn.Webhooks = c4;
function YK(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && h(n, ["_url", "name"], r), n;
}
function XK(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && h(n, ["_url", "name"], r), n;
}
function QK(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && h(n, ["sdkHttpResponse"], r), n;
}
function ZK(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && h(n, ["sdkHttpResponse"], r), n;
}
function jK(e, t, n) {
  const r = {};
  if (c(e, ["validationDataset"]) !== void 0) throw new Error("validationDataset parameter is not supported in Gemini API.");
  const o = c(e, ["tunedModelDisplayName"]);
  if (t !== void 0 && o != null && h(t, ["displayName"], o), c(e, ["description"]) !== void 0) throw new Error("description parameter is not supported in Gemini API.");
  const s = c(e, ["epochCount"]);
  t !== void 0 && s != null && h(t, [
    "tuningTask",
    "hyperparameters",
    "epochCount"
  ], s);
  const a = c(e, ["learningRateMultiplier"]);
  if (a != null && h(r, [
    "tuningTask",
    "hyperparameters",
    "learningRateMultiplier"
  ], a), c(e, ["exportLastCheckpointOnly"]) !== void 0) throw new Error("exportLastCheckpointOnly parameter is not supported in Gemini API.");
  if (c(e, ["preTunedModelCheckpointId"]) !== void 0) throw new Error("preTunedModelCheckpointId parameter is not supported in Gemini API.");
  if (c(e, ["adapterSize"]) !== void 0) throw new Error("adapterSize parameter is not supported in Gemini API.");
  if (c(e, ["tuningMode"]) !== void 0) throw new Error("tuningMode parameter is not supported in Gemini API.");
  if (c(e, ["customBaseModel"]) !== void 0) throw new Error("customBaseModel parameter is not supported in Gemini API.");
  const i = c(e, ["batchSize"]);
  t !== void 0 && i != null && h(t, [
    "tuningTask",
    "hyperparameters",
    "batchSize"
  ], i);
  const l = c(e, ["learningRate"]);
  if (t !== void 0 && l != null && h(t, [
    "tuningTask",
    "hyperparameters",
    "learningRate"
  ], l), c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  if (c(e, ["beta"]) !== void 0) throw new Error("beta parameter is not supported in Gemini API.");
  if (c(e, ["baseTeacherModel"]) !== void 0) throw new Error("baseTeacherModel parameter is not supported in Gemini API.");
  if (c(e, ["tunedTeacherModelSource"]) !== void 0) throw new Error("tunedTeacherModelSource parameter is not supported in Gemini API.");
  if (c(e, ["sftLossWeightMultiplier"]) !== void 0) throw new Error("sftLossWeightMultiplier parameter is not supported in Gemini API.");
  if (c(e, ["outputUri"]) !== void 0) throw new Error("outputUri parameter is not supported in Gemini API.");
  if (c(e, ["encryptionSpec"]) !== void 0) throw new Error("encryptionSpec parameter is not supported in Gemini API.");
  return r;
}
function eJ(e, t, n) {
  const r = {};
  let o = c(n, ["config", "method"]);
  if (o === void 0 && (o = "SUPERVISED_FINE_TUNING"), o === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["validationDataset"]);
    t !== void 0 && b != null && h(t, ["supervisedTuningSpec"], ky(b));
  } else if (o === "PREFERENCE_TUNING") {
    const b = c(e, ["validationDataset"]);
    t !== void 0 && b != null && h(t, ["preferenceOptimizationSpec"], ky(b));
  } else if (o === "DISTILLATION") {
    const b = c(e, ["validationDataset"]);
    t !== void 0 && b != null && h(t, ["distillationSpec"], ky(b));
  }
  const s = c(e, ["tunedModelDisplayName"]);
  t !== void 0 && s != null && h(t, ["tunedModelDisplayName"], s);
  const a = c(e, ["description"]);
  t !== void 0 && a != null && h(t, ["description"], a);
  let i = c(n, ["config", "method"]);
  if (i === void 0 && (i = "SUPERVISED_FINE_TUNING"), i === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["epochCount"]);
    t !== void 0 && b != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "epochCount"
    ], b);
  } else if (i === "PREFERENCE_TUNING") {
    const b = c(e, ["epochCount"]);
    t !== void 0 && b != null && h(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "epochCount"
    ], b);
  } else if (i === "DISTILLATION") {
    const b = c(e, ["epochCount"]);
    t !== void 0 && b != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "epochCount"
    ], b);
  }
  let l = c(n, ["config", "method"]);
  if (l === void 0 && (l = "SUPERVISED_FINE_TUNING"), l === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["learningRateMultiplier"]);
    t !== void 0 && b != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], b);
  } else if (l === "PREFERENCE_TUNING") {
    const b = c(e, ["learningRateMultiplier"]);
    t !== void 0 && b != null && h(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], b);
  } else if (l === "DISTILLATION") {
    const b = c(e, ["learningRateMultiplier"]);
    t !== void 0 && b != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], b);
  }
  let u = c(n, ["config", "method"]);
  if (u === void 0 && (u = "SUPERVISED_FINE_TUNING"), u === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && b != null && h(t, ["supervisedTuningSpec", "exportLastCheckpointOnly"], b);
  } else if (u === "PREFERENCE_TUNING") {
    const b = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && b != null && h(t, ["preferenceOptimizationSpec", "exportLastCheckpointOnly"], b);
  } else if (u === "DISTILLATION") {
    const b = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && b != null && h(t, ["distillationSpec", "exportLastCheckpointOnly"], b);
  }
  let d = c(n, ["config", "method"]);
  if (d === void 0 && (d = "SUPERVISED_FINE_TUNING"), d === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["adapterSize"]);
    t !== void 0 && b != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "adapterSize"
    ], b);
  } else if (d === "PREFERENCE_TUNING") {
    const b = c(e, ["adapterSize"]);
    t !== void 0 && b != null && h(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "adapterSize"
    ], b);
  } else if (d === "DISTILLATION") {
    const b = c(e, ["adapterSize"]);
    t !== void 0 && b != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "adapterSize"
    ], b);
  }
  let f = c(n, ["config", "method"]);
  if (f === void 0 && (f = "SUPERVISED_FINE_TUNING"), f === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["tuningMode"]);
    t !== void 0 && b != null && h(t, ["supervisedTuningSpec", "tuningMode"], b);
  } else if (f === "DISTILLATION") {
    const b = c(e, ["tuningMode"]);
    t !== void 0 && b != null && h(t, ["distillationSpec", "tuningMode"], b);
  }
  const m = c(e, ["customBaseModel"]);
  t !== void 0 && m != null && h(t, ["customBaseModel"], m);
  let v = c(n, ["config", "method"]);
  if (v === void 0 && (v = "SUPERVISED_FINE_TUNING"), v === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["batchSize"]);
    t !== void 0 && b != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "batchSize"
    ], b);
  } else if (v === "DISTILLATION") {
    const b = c(e, ["batchSize"]);
    t !== void 0 && b != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "batchSize"
    ], b);
  }
  let y = c(n, ["config", "method"]);
  if (y === void 0 && (y = "SUPERVISED_FINE_TUNING"), y === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["learningRate"]);
    t !== void 0 && b != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "learningRate"
    ], b);
  } else if (y === "DISTILLATION") {
    const b = c(e, ["learningRate"]);
    t !== void 0 && b != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "learningRate"
    ], b);
  }
  const T = c(e, ["labels"]);
  t !== void 0 && T != null && h(t, ["labels"], T);
  const C = c(e, ["beta"]);
  t !== void 0 && C != null && h(t, [
    "preferenceOptimizationSpec",
    "hyperParameters",
    "beta"
  ], C);
  const A = c(e, ["baseTeacherModel"]);
  t !== void 0 && A != null && h(t, ["distillationSpec", "baseTeacherModel"], A);
  const I = c(e, ["tunedTeacherModelSource"]);
  t !== void 0 && I != null && h(t, ["distillationSpec", "tunedTeacherModelSource"], I);
  const R = c(e, ["sftLossWeightMultiplier"]);
  t !== void 0 && R != null && h(t, [
    "distillationSpec",
    "hyperParameters",
    "sftLossWeightMultiplier"
  ], R);
  const $ = c(e, ["outputUri"]);
  t !== void 0 && $ != null && h(t, ["outputUri"], $);
  const p = c(e, ["encryptionSpec"]);
  return t !== void 0 && p != null && h(t, ["encryptionSpec"], p), r;
}
function tJ(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && h(n, ["baseModel"], r);
  const o = c(e, ["preTunedModel"]);
  o != null && h(n, ["preTunedModel"], o);
  const s = c(e, ["trainingDataset"]);
  s != null && fJ(s);
  const a = c(e, ["config"]);
  return a != null && jK(a, n), n;
}
function nJ(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && h(n, ["baseModel"], r);
  const o = c(e, ["preTunedModel"]);
  o != null && h(n, ["preTunedModel"], o);
  const s = c(e, ["trainingDataset"]);
  s != null && hJ(s, n, t);
  const a = c(e, ["config"]);
  return a != null && eJ(a, n, t), n;
}
function rJ(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && h(n, ["_url", "name"], r), n;
}
function oJ(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && h(n, ["_url", "name"], r), n;
}
function sJ(e, t, n) {
  const r = {}, o = c(e, ["pageSize"]);
  t !== void 0 && o != null && h(t, ["_query", "pageSize"], o);
  const s = c(e, ["pageToken"]);
  t !== void 0 && s != null && h(t, ["_query", "pageToken"], s);
  const a = c(e, ["filter"]);
  return t !== void 0 && a != null && h(t, ["_query", "filter"], a), r;
}
function aJ(e, t, n) {
  const r = {}, o = c(e, ["pageSize"]);
  t !== void 0 && o != null && h(t, ["_query", "pageSize"], o);
  const s = c(e, ["pageToken"]);
  t !== void 0 && s != null && h(t, ["_query", "pageToken"], s);
  const a = c(e, ["filter"]);
  return t !== void 0 && a != null && h(t, ["_query", "filter"], a), r;
}
function iJ(e, t) {
  const n = {}, r = c(e, ["config"]);
  return r != null && sJ(r, n), n;
}
function lJ(e, t) {
  const n = {}, r = c(e, ["config"]);
  return r != null && aJ(r, n), n;
}
function uJ(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["nextPageToken"]);
  o != null && h(n, ["nextPageToken"], o);
  const s = c(e, ["tunedModels"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((i) => g4(i))), h(n, ["tuningJobs"], a);
  }
  return n;
}
function cJ(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["nextPageToken"]);
  o != null && h(n, ["nextPageToken"], o);
  const s = c(e, ["tuningJobs"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((i) => M1(i))), h(n, ["tuningJobs"], a);
  }
  return n;
}
function dJ(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["model"], r);
  const o = c(e, ["name"]);
  return o != null && h(n, ["endpoint"], o), n;
}
function fJ(e, t) {
  const n = {};
  if (c(e, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  if (c(e, ["vertexDatasetResource"]) !== void 0) throw new Error("vertexDatasetResource parameter is not supported in Gemini API.");
  const r = c(e, ["examples"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((s) => s)), h(n, ["examples", "examples"], o);
  }
  return n;
}
function hJ(e, t, n) {
  const r = {};
  let o = c(n, ["config", "method"]);
  if (o === void 0 && (o = "SUPERVISED_FINE_TUNING"), o === "SUPERVISED_FINE_TUNING") {
    const a = c(e, ["gcsUri"]);
    t !== void 0 && a != null && h(t, ["supervisedTuningSpec", "trainingDatasetUri"], a);
  } else if (o === "PREFERENCE_TUNING") {
    const a = c(e, ["gcsUri"]);
    t !== void 0 && a != null && h(t, ["preferenceOptimizationSpec", "trainingDatasetUri"], a);
  } else if (o === "DISTILLATION") {
    const a = c(e, ["gcsUri"]);
    t !== void 0 && a != null && h(t, ["distillationSpec", "promptDatasetUri"], a);
  }
  let s = c(n, ["config", "method"]);
  if (s === void 0 && (s = "SUPERVISED_FINE_TUNING"), s === "SUPERVISED_FINE_TUNING") {
    const a = c(e, ["vertexDatasetResource"]);
    t !== void 0 && a != null && h(t, ["supervisedTuningSpec", "trainingDatasetUri"], a);
  } else if (s === "PREFERENCE_TUNING") {
    const a = c(e, ["vertexDatasetResource"]);
    t !== void 0 && a != null && h(t, ["preferenceOptimizationSpec", "trainingDatasetUri"], a);
  } else if (s === "DISTILLATION") {
    const a = c(e, ["vertexDatasetResource"]);
    t !== void 0 && a != null && h(t, ["distillationSpec", "promptDatasetUri"], a);
  }
  if (c(e, ["examples"]) !== void 0) throw new Error("examples parameter is not supported in Vertex AI.");
  return r;
}
function g4(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["name"]);
  o != null && h(n, ["name"], o);
  const s = c(e, ["state"]);
  s != null && h(n, ["state"], A3(s));
  const a = c(e, ["createTime"]);
  a != null && h(n, ["createTime"], a);
  const i = c(e, ["tuningTask", "startTime"]);
  i != null && h(n, ["startTime"], i);
  const l = c(e, ["tuningTask", "completeTime"]);
  l != null && h(n, ["endTime"], l);
  const u = c(e, ["updateTime"]);
  u != null && h(n, ["updateTime"], u);
  const d = c(e, ["description"]);
  d != null && h(n, ["description"], d);
  const f = c(e, ["baseModel"]);
  f != null && h(n, ["baseModel"], f);
  const m = c(e, ["_self"]);
  return m != null && h(n, ["tunedModel"], dJ(m)), n;
}
function M1(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["name"]);
  o != null && h(n, ["name"], o);
  const s = c(e, ["state"]);
  s != null && h(n, ["state"], A3(s));
  const a = c(e, ["createTime"]);
  a != null && h(n, ["createTime"], a);
  const i = c(e, ["startTime"]);
  i != null && h(n, ["startTime"], i);
  const l = c(e, ["endTime"]);
  l != null && h(n, ["endTime"], l);
  const u = c(e, ["updateTime"]);
  u != null && h(n, ["updateTime"], u);
  const d = c(e, ["error"]);
  d != null && h(n, ["error"], d);
  const f = c(e, ["description"]);
  f != null && h(n, ["description"], f);
  const m = c(e, ["baseModel"]);
  m != null && h(n, ["baseModel"], m);
  const v = c(e, ["tunedModel"]);
  v != null && h(n, ["tunedModel"], v);
  const y = c(e, ["preTunedModel"]);
  y != null && h(n, ["preTunedModel"], y);
  const T = c(e, ["supervisedTuningSpec"]);
  T != null && h(n, ["supervisedTuningSpec"], T);
  const C = c(e, ["preferenceOptimizationSpec"]);
  C != null && h(n, ["preferenceOptimizationSpec"], C);
  const A = c(e, ["distillationSpec"]);
  A != null && h(n, ["distillationSpec"], A);
  const I = c(e, ["tuningDataStats"]);
  I != null && h(n, ["tuningDataStats"], I);
  const R = c(e, ["encryptionSpec"]);
  R != null && h(n, ["encryptionSpec"], R);
  const $ = c(e, ["partnerModelTuningSpec"]);
  $ != null && h(n, ["partnerModelTuningSpec"], $);
  const p = c(e, ["customBaseModel"]);
  p != null && h(n, ["customBaseModel"], p);
  const b = c(e, ["evaluateDatasetRuns"]);
  if (b != null) {
    let X = b;
    Array.isArray(X) && (X = X.map((de) => de)), h(n, ["evaluateDatasetRuns"], X);
  }
  const w = c(e, ["experiment"]);
  w != null && h(n, ["experiment"], w);
  const E = c(e, ["fullFineTuningSpec"]);
  E != null && h(n, ["fullFineTuningSpec"], E);
  const x = c(e, ["labels"]);
  x != null && h(n, ["labels"], x);
  const M = c(e, ["outputUri"]);
  M != null && h(n, ["outputUri"], M);
  const N = c(e, ["pipelineJob"]);
  N != null && h(n, ["pipelineJob"], N);
  const q = c(e, ["serviceAccount"]);
  q != null && h(n, ["serviceAccount"], q);
  const z = c(e, ["tunedModelDisplayName"]);
  z != null && h(n, ["tunedModelDisplayName"], z);
  const J = c(e, ["tuningJobState"]);
  J != null && h(n, ["tuningJobState"], J);
  const G = c(e, ["veoTuningSpec"]);
  G != null && h(n, ["veoTuningSpec"], G);
  const oe = c(e, ["distillationSamplingSpec"]);
  oe != null && h(n, ["distillationSamplingSpec"], oe);
  const B = c(e, ["tuningJobMetadata"]);
  return B != null && h(n, ["tuningJobMetadata"], B), n;
}
function pJ(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const o = c(e, ["name"]);
  o != null && h(n, ["name"], o);
  const s = c(e, ["metadata"]);
  s != null && h(n, ["metadata"], s);
  const a = c(e, ["done"]);
  a != null && h(n, ["done"], a);
  const i = c(e, ["error"]);
  return i != null && h(n, ["error"], i), n;
}
function ky(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && h(n, ["validationDatasetUri"], r);
  const o = c(e, ["vertexDatasetResource"]);
  return o != null && h(n, ["validationDatasetUri"], o), n;
}
var mJ = class extends ta {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new tl(js.PAGED_ITEM_TUNING_JOBS, (n) => this.listInternal(n), await this.listInternal(t), t), this.get = async (t) => await this.getInternal(t), this.tune = async (t) => {
      var n;
      if (this.apiClient.isVertexAI()) if (t.baseModel.startsWith("projects/")) {
        const r = { tunedModelName: t.baseModel };
        !((n = t.config) === null || n === void 0) && n.preTunedModelCheckpointId && (r.checkpointId = t.config.preTunedModelCheckpointId);
        const o = Object.assign(Object.assign({}, t), { preTunedModel: r });
        return o.baseModel = void 0, await this.tuneInternal(o);
      } else {
        const r = Object.assign({}, t);
        return await this.tuneInternal(r);
      }
      else {
        const r = Object.assign({}, t), o = await this.tuneMldevInternal(r);
        let s = "";
        return o.metadata !== void 0 && o.metadata.tunedModel !== void 0 ? s = o.metadata.tunedModel : o.name !== void 0 && o.name.includes("/operations/") && (s = o.name.split("/operations/")[0]), {
          name: s,
          state: g1.JOB_STATE_QUEUED
        };
      }
    };
  }
  async getInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = oJ(e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => M1(u));
    } else {
      const l = rJ(e);
      return a = nt("{name}", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => g4(u));
    }
  }
  async listInternal(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = lJ(e);
      return a = nt("tuningJobs", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = cJ(u), f = new YE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = iJ(e);
      return a = nt("tunedModels", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = uJ(u), f = new YE();
        return Object.assign(f, d), f;
      });
    }
  }
  async cancel(e) {
    var t, n, r, o;
    let s, a = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const l = XK(e);
      return a = nt("{name}:cancel", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = ZK(u), f = new XE();
        return Object.assign(f, d), f;
      });
    } else {
      const l = YK(e);
      return a = nt("{name}:cancel", l._url), i = l._query, delete l._url, delete l._query, s = this.apiClient.request({
        path: a,
        queryParams: i,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (o = e.config) === null || o === void 0 ? void 0 : o.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), s.then((u) => {
        const d = QK(u), f = new XE();
        return Object.assign(f, d), f;
      });
    }
  }
  async tuneInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) {
      const a = nJ(e, e);
      return o = nt("tuningJobs", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: i.headers }, u;
      })), r.then((i) => M1(i));
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async tuneMldevInternal(e) {
    var t, n;
    let r, o = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = tJ(e);
      return o = nt("tunedModels", a._url), s = a._query, delete a._url, delete a._query, r = this.apiClient.request({
        path: o,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: i.headers }, u;
      })), r.then((i) => pJ(i));
    }
  }
}, gJ = class {
  async download(e, t) {
    throw new Error("Download to file is not supported in the browser, please use a browser compliant download like an <a> tag.");
  }
}, vJ = 1024 * 1024 * 8, yJ = 3, _J = 1e3, bJ = 2, wm = "x-goog-upload-status";
async function wJ(e, t, n, r) {
  var o;
  const s = await v4(e, t, n, r), a = await s?.json();
  if (((o = s?.headers) === null || o === void 0 ? void 0 : o[wm]) !== "final") throw new Error("Failed to upload file: Upload status is not finalized.");
  return a.file;
}
async function SJ(e, t, n, r) {
  var o;
  const s = await v4(e, t, n, r), a = await s?.json();
  if (((o = s?.headers) === null || o === void 0 ? void 0 : o[wm]) !== "final") throw new Error("Failed to upload file: Upload status is not finalized.");
  const i = g3(a), l = new Lq();
  return Object.assign(l, i), l;
}
async function v4(e, t, n, r) {
  var o, s, a;
  let i = t;
  const l = r?.baseUrl || ((o = n.clientOptions.httpOptions) === null || o === void 0 ? void 0 : o.baseUrl);
  if (l) {
    const v = new URL(l), y = new URL(t);
    y.protocol = v.protocol, y.host = v.host, y.port = v.port, i = y.toString();
  }
  let u = 0, d = 0, f = new y1(new Response()), m = "upload";
  for (u = e.size; d < u; ) {
    const v = Math.min(vJ, u - d), y = e.slice(d, d + v);
    d + v >= u && (m += ", finalize");
    let T = 0, C = _J;
    for (; T < yJ; ) {
      const A = Object.assign(Object.assign({}, r?.headers || {}), {
        "X-Goog-Upload-Command": m,
        "X-Goog-Upload-Offset": String(d),
        "Content-Length": String(v)
      });
      if (f = await n.request({
        path: "",
        body: y,
        httpMethod: "POST",
        httpOptions: Object.assign(Object.assign({}, r), {
          apiVersion: "",
          baseUrl: i,
          headers: A
        })
      }), !((s = f?.headers) === null || s === void 0) && s[wm]) break;
      T++, await EJ(C), C = C * bJ;
    }
    if (d += v, ((a = f?.headers) === null || a === void 0 ? void 0 : a[wm]) !== "active") break;
    if (u <= d) throw new Error("All content has been uploaded, but the upload status is not finalized.");
  }
  return f;
}
async function TJ(e) {
  return {
    size: e.size,
    type: e.type
  };
}
function EJ(e) {
  return new Promise((t) => setTimeout(t, e));
}
var AJ = class {
  async upload(e, t, n, r) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await wJ(e, t, n, r);
  }
  async uploadToFileSearchStore(e, t, n, r) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await SJ(e, t, n, r);
  }
  async stat(e) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await TJ(e);
  }
}, CJ = class {
  create(e, t, n) {
    return new kJ(e, t, n);
  }
}, kJ = class {
  constructor(e, t, n) {
    this.url = e, this.headers = t, this.callbacks = n;
  }
  connect() {
    this.ws = new WebSocket(this.url), this.ws.onopen = this.callbacks.onopen, this.ws.onerror = this.callbacks.onerror, this.ws.onclose = this.callbacks.onclose, this.ws.onmessage = this.callbacks.onmessage;
  }
  send(e) {
    if (this.ws === void 0) throw new Error("WebSocket is not connected");
    this.ws.send(e);
  }
  close() {
    if (this.ws === void 0) throw new Error("WebSocket is not connected");
    this.ws.close();
  }
}, AA = "x-goog-api-key", IJ = class {
  constructor(e) {
    this.apiKey = e;
  }
  async addAuthHeaders(e, t) {
    if (e.get(AA) === null) {
      if (this.apiKey.startsWith("auth_tokens/")) throw new Error("Ephemeral tokens are only supported by the live API.");
      if (!this.apiKey) throw new Error("API key is missing. Please provide a valid API key.");
      e.append(AA, this.apiKey);
    }
  }
}, xJ = "gl-node/", MJ = class {
  getNextGenClient() {
    var e;
    const t = this.httpOptions;
    if (this._nextGenClient === void 0) {
      const n = this.httpOptions;
      this._nextGenClient = new Bn({
        baseURL: this.apiClient.getBaseUrl(),
        apiKey: this.apiKey,
        apiVersion: this.apiClient.getApiVersion(),
        clientAdapter: this.apiClient,
        defaultHeaders: this.apiClient.getDefaultHeaders(),
        timeout: n?.timeout,
        maxRetries: (e = n?.retryOptions) === null || e === void 0 ? void 0 : e.attempts
      });
    }
    return t?.extraBody && console.warn("GoogleGenAI.interactions: Client level httpOptions.extraBody is not supported by the interactions client and will be ignored."), this._nextGenClient;
  }
  get interactions() {
    return this._interactions !== void 0 ? this._interactions : (console.warn("GoogleGenAI.interactions: Interactions usage is experimental and may change in future versions."), this._interactions = this.getNextGenClient().interactions, this._interactions);
  }
  get webhooks() {
    return this._webhooks !== void 0 ? this._webhooks : (this._webhooks = this.getNextGenClient().webhooks, this._webhooks);
  }
  constructor(e) {
    var t;
    if (e.apiKey == null) throw new Error("An API Key must be set when running in a browser");
    if (e.project || e.location) throw new Error("Vertex AI project based authentication is not supported on browser runtimes. Please do not provide a project or location.");
    this.vertexai = (t = e.vertexai) !== null && t !== void 0 ? t : !1, this.apiKey = e.apiKey;
    const n = sq(e.httpOptions, e.vertexai, void 0, void 0);
    n && (e.httpOptions ? e.httpOptions.baseUrl = n : e.httpOptions = { baseUrl: n }), this.apiVersion = e.apiVersion, this.httpOptions = e.httpOptions;
    const r = new IJ(this.apiKey);
    this.apiClient = new EV({
      auth: r,
      apiVersion: this.apiVersion,
      apiKey: this.apiKey,
      vertexai: this.vertexai,
      httpOptions: this.httpOptions,
      userAgentExtra: xJ + "web",
      uploader: new AJ(),
      downloader: new gJ()
    }), this.models = new WV(this.apiClient), this.live = new FV(this.apiClient, r, new CJ()), this.batches = new BH(this.apiClient), this.chats = new Tz(this.models, this.apiClient), this.caches = new bz(this.apiClient), this.files = new Dz(this.apiClient), this.operations = new GV(this.apiClient), this.authTokens = new uK(this.apiClient), this.tunings = new mJ(this.apiClient), this.fileSearchStores = new vK(this.apiClient);
  }
};
function CA(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function md(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function xi(e) {
  return { text: String(e || "") };
}
function PJ(e = "") {
  const t = String(e || "").match(/^data:([^;,]+);base64,(.+)$/);
  return t ? { inlineData: {
    mimeType: t[1],
    data: t[2]
  } } : null;
}
function RJ(e) {
  if (typeof e == "string") return [xi(e)];
  if (!Array.isArray(e)) return [xi("")];
  const t = e.map((n) => !n || typeof n != "object" ? null : n.type === "text" ? xi(n.text || "") : n.type === "image_url" && n.image_url?.url ? PJ(n.image_url.url) : null).filter(Boolean);
  return t.length ? t : [xi("")];
}
function kA() {
  return {
    role: "user",
    parts: [xi("")]
  };
}
function gf(e, t = "model") {
  if (!e?.parts?.length) return null;
  const n = md(e);
  return n ? (n.role || (n.role = t), n) : null;
}
function NJ(e) {
  return !!e?.parts?.some((t) => typeof t?.thoughtSignature == "string" && t.thoughtSignature);
}
function $J(e) {
  return !!e?.parts?.some((t) => t?.functionCall?.name);
}
function Iy(e, t) {
  return e?.functionCall?.name ? [
    String(e.functionCall.id || ""),
    String(e.functionCall.name || ""),
    JSON.stringify(e.functionCall.args || {}),
    String(t)
  ].join("\0") : "";
}
function DJ(e = [], t = "") {
  const n = e.map((l) => gf(l, "model")).filter(Boolean);
  if (!n.length) return null;
  const r = [...n].reverse().find((l) => NJ(l)) || null, o = [...n].reverse().find((l) => $J(l)) || null, s = md(r || o || n[n.length - 1]);
  if (!s?.parts?.length) return n[n.length - 1];
  if (o) {
    const l = /* @__PURE__ */ new Map();
    n.forEach((d) => {
      d.parts.forEach((f, m) => {
        const v = Iy(f, m);
        if (!v) return;
        const y = l.get(v);
        (!y || f.thoughtSignature || !y.thoughtSignature) && l.set(v, md(f));
      });
    });
    const u = /* @__PURE__ */ new Set();
    s.parts = s.parts.map((d, f) => {
      const m = Iy(d, f);
      return m ? (u.add(m), l.get(m) || d) : d;
    }), o.parts.forEach((d, f) => {
      const m = Iy(d, f);
      !m || u.has(m) || (s.parts.push(l.get(m) || md(d)), u.add(m));
    });
  }
  const a = String(t || ""), i = s.parts.filter((l) => !(typeof l?.text == "string" && !l?.thought));
  return s.parts = a ? [{ text: a }, ...i] : i, s.parts.length ? s : n[n.length - 1];
}
function IA(e) {
  const t = e?.candidates?.[0]?.content?.parts || [], n = t.filter((r) => !r?.thought && typeof r?.text == "string" && r.text).map((r) => r.text).join(`
`);
  return n || t.length ? n : typeof e?.text == "string" && e.text ? e.text : "";
}
function xA(e) {
  const t = Array.isArray(e?.functionCalls) ? e.functionCalls : [], n = (e?.candidates?.[0]?.content?.parts || []).map((r) => r?.functionCall || r).filter((r) => r && r.name);
  return (t.length ? t : n).map((r, o) => ({
    id: r.id || `google-tool-${o + 1}`,
    name: r.name || "",
    arguments: JSON.stringify(r.args || {})
  })).filter((r) => r.name);
}
function LJ(e = [], t = []) {
  const n = Array.isArray(e) ? [...e] : [];
  return (Array.isArray(t) ? t : []).forEach((r) => {
    if (!r?.name) return;
    const o = [
      String(r.id || ""),
      String(r.name || ""),
      String(r.arguments || "")
    ].join("\0");
    n.some((s) => [
      String(s.id || ""),
      String(s.name || ""),
      String(s.arguments || "")
    ].join("\0") === o) || n.push(r);
  }), n;
}
function OJ(e = []) {
  return {
    role: "user",
    parts: e.filter((t) => t && t.name).map((t) => ({ functionResponse: {
      name: t.name,
      response: t.response || {}
    } }))
  };
}
function FJ(e) {
  switch (e) {
    case "high":
      return pd.HIGH;
    case "medium":
      return pd.MEDIUM;
    default:
      return pd.LOW;
  }
}
function MA(e) {
  return (e?.candidates?.[0]?.content?.parts || []).filter((t) => t?.thought && typeof t.text == "string" && t.text.trim()).map((t, n) => ({
    label: `思考块 ${n + 1}`,
    text: t.text.trim()
  }));
}
function BJ(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  if (t.length)
    return [...new Set(t)].join(`

`);
}
function UJ(e) {
  const t = e?.providerPayload?.googleContent;
  return gf(t, "model");
}
function qJ(e) {
  const t = e?.providerPayload?.googleContents;
  if (!Array.isArray(t) || !t.length) {
    const n = UJ(e);
    return n ? [n] : [];
  }
  return t.map((n) => gf(n, "model")).filter(Boolean);
}
function Gb(e = []) {
  const t = (Array.isArray(e) ? e : []).map((n) => gf(n, "model")).filter(Boolean);
  if (t.length)
    return {
      googleContent: t[t.length - 1],
      googleContents: t
    };
}
function HJ(e) {
  const t = e?.candidates?.[0]?.content;
  return Gb(t ? [t] : []);
}
function zJ(e) {
  return Gb(e ? [e] : []);
}
function y4(e) {
  try {
    if (typeof e?.getHistory == "function") return e.getHistory(!1);
  } catch {
    return [];
  }
  return Array.isArray(e?.history) ? md(e.history) || [] : [];
}
function WJ(e, t = 0) {
  return y4(e).slice(Math.max(0, t)).filter((n) => n?.role === "model").map((n) => gf(n, "model")).filter(Boolean);
}
function GJ(e) {
  const t = /* @__PURE__ */ new Map(), n = [], r = (e || []).filter((s) => s.role === "user" || s.role === "assistant" || s.role === "tool");
  r.forEach((s) => {
    (s.tool_calls || []).forEach((a) => {
      a.id && a.function?.name && t.set(a.id, a.function.name);
    });
  });
  for (let s = 0; s < r.length; s += 1) {
    const a = r[s];
    if (a.role === "tool") {
      const i = [];
      let l = s;
      for (; l < r.length && r[l].role === "tool"; ) {
        const u = r[l];
        i.push({ functionResponse: {
          name: String(u.toolName || u.tool_name || "").trim() || t.get(u.tool_call_id || "") || "tool_result",
          response: CA(u.content)
        } }), l += 1;
      }
      n.push({
        role: "user",
        parts: i
      }), s = l - 1;
      continue;
    }
    if (a.role === "assistant") {
      const i = qJ(a);
      if (i.length) {
        n.push(...i);
        continue;
      }
    }
    if (a.role === "assistant" && Array.isArray(a.tool_calls) && a.tool_calls.length) {
      n.push({
        role: "model",
        parts: [...a.content ? [xi(a.content)] : [], ...a.tool_calls.map((i) => ({ functionCall: {
          name: i.function.name,
          args: CA(i.function.arguments)
        } }))]
      });
      continue;
    }
    n.push({
      role: a.role === "assistant" ? "model" : "user",
      parts: RJ(a.content)
    });
  }
  if (!n.length) return {
    history: [],
    latestMessage: kA().parts
  };
  const o = n[n.length - 1];
  return o.role === "user" && o.parts?.length ? {
    history: n.slice(0, -1),
    latestMessage: o.parts
  } : {
    history: n,
    latestMessage: kA().parts
  };
}
function VJ(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function PA(e, t) {
  const n = String(t || ""), r = String(e || "");
  return n ? !r || n.startsWith(r) ? n : r.endsWith(n) ? r : `${r}${n}` : r;
}
var KJ = class {
  constructor(e) {
    this.config = e, this.supportsSessionToolLoop = !0, this.activeChat = null, this.client = new MJ({
      apiKey: e.apiKey,
      httpOptions: {
        baseUrl: String(e.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, ""),
        timeout: Number(e.timeoutMs) || 900 * 1e3
      }
    });
  }
  buildChatPayload(e) {
    const t = GJ(e.messages), n = Array.isArray(e.tools) ? e.tools : [], r = BJ(e), o = {
      ...r ? { systemInstruction: r } : {},
      temperature: e.temperature,
      ...e.maxTokens ? { maxOutputTokens: e.maxTokens } : {}
    };
    return e.reasoning?.enabled && (o.thinkingConfig = {
      includeThoughts: !0,
      thinkingLevel: FJ(e.reasoning.effort)
    }), n.length && (o.tools = [{ functionDeclarations: n.map((s) => ({
      name: s.function.name,
      description: s.function.description,
      parameters: s.function.parameters
    })) }]), n.length && e.toolChoice && e.toolChoice !== "auto" && e.toolChoice !== "none" && (o.toolConfig = { functionCallingConfig: { mode: m1.ANY } }), {
      createPayload: {
        model: this.config.model,
        history: t.history,
        config: o
      },
      sendPayload: { message: t.latestMessage }
    };
  }
  inspectRequest(e, t = {}) {
    const n = t.payload || this.buildChatPayload(e), r = String(this.config.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, "");
    return Gd({
      provider: "google",
      model: this.config.model,
      transport: "google-genai-sdk",
      url: `${r}/models/${encodeURIComponent(this.config.model || "")}:generateContent`,
      headers: {
        "Content-Type": "application/json",
        "x-goog-api-key": this.config.apiKey || ""
      },
      body: {
        chatCreate: n.createPayload,
        sendMessage: n.sendPayload,
        stream: typeof e.onStreamProgress == "function"
      },
      sdk: typeof e.onStreamProgress == "function" ? "client.chats.create(...).sendMessageStream" : "client.chats.create(...).sendMessage"
    });
  }
  inspectSendRequest(e, t) {
    const n = String(this.config.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, "");
    return Gd({
      provider: "google",
      model: this.config.model,
      transport: "google-genai-sdk",
      url: `${n}/models/${encodeURIComponent(this.config.model || "")}:generateContent`,
      headers: {
        "Content-Type": "application/json",
        "x-goog-api-key": this.config.apiKey || ""
      },
      body: {
        sendMessage: e,
        stream: typeof t.onStreamProgress == "function"
      },
      sdk: typeof t.onStreamProgress == "function" ? "activeChat.sendMessageStream" : "activeChat.sendMessage"
    });
  }
  createChat(e) {
    const t = this.buildChatPayload(e);
    return {
      chat: this.client.chats.create(t.createPayload),
      sendPayload: t.sendPayload,
      requestInspection: this.inspectRequest(e, { payload: t })
    };
  }
  async sendThroughChat(e, t, n) {
    let r, o, s, a = [], i = null;
    const l = { ...t }, u = typeof n.onStreamProgress == "function", d = y4(e).length;
    if (u) {
      const y = await e.sendMessageStream(l), T = /* @__PURE__ */ new Map();
      let C = "", A = [], I = null;
      const R = [];
      for await (const $ of y) {
        I = $;
        const p = $?.candidates?.[0]?.content;
        p?.parts?.length && R.push(p), MA($).forEach((w, E) => {
          const x = `${w.label}:${E}`;
          T.set(x, PA(T.get(x) || "", w.text));
        }), A = ($.functionCalls || []).map((w, E) => ({
          id: w.id || `google-tool-${E + 1}`,
          name: w.name || "",
          arguments: JSON.stringify(w.args || {})
        })).filter((w) => w.name), a = LJ(a, A.length ? A : xA($));
        const b = IA($);
        C = PA(C, b), VJ(n, {
          text: C,
          thoughts: Array.from(T.values()).filter(Boolean).map((w, E) => ({
            label: `思考块 ${E + 1}`,
            text: w
          })),
          ...A.length ? {
            toolCalls: A,
            toolCallDraft: !0
          } : {}
        });
      }
      r = I || { functionCalls: A }, i = DJ(R, C) || r?.candidates?.[0]?.content || null, o = Array.from(T.values()).filter(Boolean).map(($, p) => ({
        label: `思考块 ${p + 1}`,
        text: $
      })), s = C;
    } else
      r = await e.sendMessage(l), o = MA(r), s = IA(r);
    const f = xA(r), m = f.length ? f : a, v = WJ(e, d);
    return {
      text: s,
      toolCalls: m,
      thoughts: o,
      finishReason: r.candidates?.[0]?.finishReason || "STOP",
      model: r.modelVersion || this.config.model,
      provider: "google",
      providerPayload: Gb(v) || zJ(i) || HJ(r)
    };
  }
  async chat(e) {
    if (Array.isArray(e.toolResponses) && e.toolResponses.length) {
      if (!this.activeChat) throw new Error("google_chat_session_missing");
      const r = { message: OJ(e.toolResponses) };
      return {
        ...await this.sendThroughChat(this.activeChat, r, e),
        requestInspection: this.inspectSendRequest(r, e)
      };
    }
    const t = String(e.finalAnswerReminderText || "").trim();
    if (t) {
      if (!this.activeChat) throw new Error("google_chat_session_missing");
      const r = { message: [xi(t)] };
      return {
        ...await this.sendThroughChat(this.activeChat, r, e),
        requestInspection: this.inspectSendRequest(r, e)
      };
    }
    const n = this.createChat(e);
    return this.activeChat = n.chat, {
      ...await this.sendThroughChat(this.activeChat, n.sendPayload, e),
      requestInspection: n.requestInspection
    };
  }
};
function St(e, t, n, r, o) {
  if (r === "m") throw new TypeError("Private method is not writable");
  if (r === "a" && !o) throw new TypeError("Private accessor was defined without a setter");
  if (typeof t == "function" ? e !== t || !o : !t.has(e)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return r === "a" ? o.call(e, n) : o ? o.value = n : t.set(e, n), n;
}
function Ae(e, t, n, r) {
  if (n === "a" && !r) throw new TypeError("Private accessor was defined without a getter");
  if (typeof t == "function" ? e !== t || !r : !t.has(e)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return n === "m" ? r : n === "a" ? r.call(e) : r ? r.value : t.get(e);
}
var _4 = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return _4 = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
};
function P1(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var R1 = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, mt = class extends Error {
}, ir = class N1 extends mt {
  constructor(t, n, r, o) {
    super(`${N1.makeMessage(t, n, r)}`), this.status = t, this.headers = o, this.requestID = o?.get("x-request-id"), this.error = n;
    const s = n;
    this.code = s?.code, this.param = s?.param, this.type = s?.type;
  }
  static makeMessage(t, n, r) {
    const o = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && o ? `${t} ${o}` : t ? `${t} status code (no body)` : o || "(no status code or body)";
  }
  static generate(t, n, r, o) {
    if (!t || !o) return new Tg({
      message: r,
      cause: R1(n)
    });
    const s = n?.error;
    return t === 400 ? new b4(t, s, r, o) : t === 401 ? new w4(t, s, r, o) : t === 403 ? new S4(t, s, r, o) : t === 404 ? new T4(t, s, r, o) : t === 409 ? new E4(t, s, r, o) : t === 422 ? new A4(t, s, r, o) : t === 429 ? new C4(t, s, r, o) : t >= 500 ? new k4(t, s, r, o) : new N1(t, s, r, o);
  }
}, go = class extends ir {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, Tg = class extends ir {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, Vb = class extends Tg {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, b4 = class extends ir {
}, w4 = class extends ir {
}, S4 = class extends ir {
}, T4 = class extends ir {
}, E4 = class extends ir {
}, A4 = class extends ir {
}, C4 = class extends ir {
}, k4 = class extends ir {
}, I4 = class extends mt {
  constructor() {
    super("Could not parse response content as the length limit was reached");
  }
}, x4 = class extends mt {
  constructor() {
    super("Could not parse response content as the request was rejected by the content filter");
  }
}, Gc = class extends Error {
  constructor(e) {
    super(e);
  }
}, M4 = class extends ir {
  constructor(e, t, n) {
    let r = "OAuth2 authentication error", o;
    if (t && typeof t == "object") {
      const s = t;
      o = s.error;
      const a = s.error_description;
      a && typeof a == "string" ? r = a : o && (r = o);
    }
    super(e, t, r, n), this.error_code = o;
  }
}, JJ = class extends mt {
  constructor(e, t, n) {
    super(e), this.provider = t, this.cause = n;
  }
}, YJ = /^[a-z][a-z0-9+.-]*:/i, XJ = (e) => YJ.test(e), Ar = (e) => (Ar = Array.isArray, Ar(e)), RA = Ar;
function P4(e) {
  return typeof e != "object" ? {} : e ?? {};
}
function NA(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function QJ(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
function xy(e) {
  return e != null && typeof e == "object" && !Array.isArray(e);
}
var ZJ = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new mt(`${e} must be an integer`);
  if (t < 0) throw new mt(`${e} must be a positive integer`);
  return t;
}, jJ = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, vf = (e) => new Promise((t) => setTimeout(t, e)), Bl = "6.34.0", eY = () => typeof window < "u" && typeof window.document < "u" && typeof navigator < "u";
function tY() {
  return typeof Deno < "u" && Deno.build != null ? "deno" : typeof EdgeRuntime < "u" ? "edge" : Object.prototype.toString.call(typeof globalThis.process < "u" ? globalThis.process : 0) === "[object process]" ? "node" : "unknown";
}
var nY = () => {
  const e = tY();
  if (e === "deno") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Bl,
    "X-Stainless-OS": DA(Deno.build.os),
    "X-Stainless-Arch": $A(Deno.build.arch),
    "X-Stainless-Runtime": "deno",
    "X-Stainless-Runtime-Version": typeof Deno.version == "string" ? Deno.version : Deno.version?.deno ?? "unknown"
  };
  if (typeof EdgeRuntime < "u") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Bl,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": `other:${EdgeRuntime}`,
    "X-Stainless-Runtime": "edge",
    "X-Stainless-Runtime-Version": globalThis.process.version
  };
  if (e === "node") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Bl,
    "X-Stainless-OS": DA(globalThis.process.platform ?? "unknown"),
    "X-Stainless-Arch": $A(globalThis.process.arch ?? "unknown"),
    "X-Stainless-Runtime": "node",
    "X-Stainless-Runtime-Version": globalThis.process.version ?? "unknown"
  };
  const t = rY();
  return t ? {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Bl,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": `browser:${t.browser}`,
    "X-Stainless-Runtime-Version": t.version
  } : {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Bl,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": "unknown",
    "X-Stainless-Runtime-Version": "unknown"
  };
};
function rY() {
  if (typeof navigator > "u" || !navigator) return null;
  for (const { key: e, pattern: t } of [
    {
      key: "edge",
      pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "chrome",
      pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "firefox",
      pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "safari",
      pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/
    }
  ]) {
    const n = t.exec(navigator.userAgent);
    if (n) return {
      browser: e,
      version: `${n[1] || 0}.${n[2] || 0}.${n[3] || 0}`
    };
  }
  return null;
}
var $A = (e) => e === "x32" ? "x32" : e === "x86_64" || e === "x64" ? "x64" : e === "arm" ? "arm" : e === "aarch64" || e === "arm64" ? "arm64" : e ? `other:${e}` : "unknown", DA = (e) => (e = e.toLowerCase(), e.includes("ios") ? "iOS" : e === "android" ? "Android" : e === "darwin" ? "MacOS" : e === "win32" ? "Windows" : e === "freebsd" ? "FreeBSD" : e === "openbsd" ? "OpenBSD" : e === "linux" ? "Linux" : e ? `Other:${e}` : "Unknown"), LA, oY = () => LA ?? (LA = nY());
function R4() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new OpenAI({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function N4(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function $4(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return N4({
    start() {
    },
    async pull(n) {
      const { done: r, value: o } = await t.next();
      r ? n.close() : n.enqueue(o);
    },
    async cancel() {
      await t.return?.();
    }
  });
}
function D4(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function OA(e) {
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await e[Symbol.asyncIterator]().return?.();
    return;
  }
  const t = e.getReader(), n = t.cancel();
  t.releaseLock(), await n;
}
var sY = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
}), L4 = "RFC3986", O4 = (e) => String(e), FA = {
  RFC1738: (e) => String(e).replace(/%20/g, "+"),
  RFC3986: O4
};
var $1 = (e, t) => ($1 = Object.hasOwn ?? Function.prototype.call.bind(Object.prototype.hasOwnProperty), $1(e, t)), Xo = /* @__PURE__ */ (() => {
  const e = [];
  for (let t = 0; t < 256; ++t) e.push("%" + ((t < 16 ? "0" : "") + t.toString(16)).toUpperCase());
  return e;
})(), My = 1024, aY = (e, t, n, r, o) => {
  if (e.length === 0) return e;
  let s = e;
  if (typeof e == "symbol" ? s = Symbol.prototype.toString.call(e) : typeof e != "string" && (s = String(e)), n === "iso-8859-1") return escape(s).replace(/%u[0-9a-f]{4}/gi, function(i) {
    return "%26%23" + parseInt(i.slice(2), 16) + "%3B";
  });
  let a = "";
  for (let i = 0; i < s.length; i += My) {
    const l = s.length >= My ? s.slice(i, i + My) : s, u = [];
    for (let d = 0; d < l.length; ++d) {
      let f = l.charCodeAt(d);
      if (f === 45 || f === 46 || f === 95 || f === 126 || f >= 48 && f <= 57 || f >= 65 && f <= 90 || f >= 97 && f <= 122 || o === "RFC1738" && (f === 40 || f === 41)) {
        u[u.length] = l.charAt(d);
        continue;
      }
      if (f < 128) {
        u[u.length] = Xo[f];
        continue;
      }
      if (f < 2048) {
        u[u.length] = Xo[192 | f >> 6] + Xo[128 | f & 63];
        continue;
      }
      if (f < 55296 || f >= 57344) {
        u[u.length] = Xo[224 | f >> 12] + Xo[128 | f >> 6 & 63] + Xo[128 | f & 63];
        continue;
      }
      d += 1, f = 65536 + ((f & 1023) << 10 | l.charCodeAt(d) & 1023), u[u.length] = Xo[240 | f >> 18] + Xo[128 | f >> 12 & 63] + Xo[128 | f >> 6 & 63] + Xo[128 | f & 63];
    }
    a += u.join("");
  }
  return a;
};
function iY(e) {
  return !e || typeof e != "object" ? !1 : !!(e.constructor && e.constructor.isBuffer && e.constructor.isBuffer(e));
}
function BA(e, t) {
  if (Ar(e)) {
    const n = [];
    for (let r = 0; r < e.length; r += 1) n.push(t(e[r]));
    return n;
  }
  return t(e);
}
var F4 = {
  brackets(e) {
    return String(e) + "[]";
  },
  comma: "comma",
  indices(e, t) {
    return String(e) + "[" + t + "]";
  },
  repeat(e) {
    return String(e);
  }
}, B4 = function(e, t) {
  Array.prototype.push.apply(e, Ar(t) ? t : [t]);
}, UA, Dn = {
  addQueryPrefix: !1,
  allowDots: !1,
  allowEmptyArrays: !1,
  arrayFormat: "indices",
  charset: "utf-8",
  charsetSentinel: !1,
  delimiter: "&",
  encode: !0,
  encodeDotInKeys: !1,
  encoder: aY,
  encodeValuesOnly: !1,
  format: L4,
  formatter: O4,
  indices: !1,
  serializeDate(e) {
    return (UA ?? (UA = Function.prototype.call.bind(Date.prototype.toISOString)))(e);
  },
  skipNulls: !1,
  strictNullHandling: !1
};
function lY(e) {
  return typeof e == "string" || typeof e == "number" || typeof e == "boolean" || typeof e == "symbol" || typeof e == "bigint";
}
var Py = {};
function U4(e, t, n, r, o, s, a, i, l, u, d, f, m, v, y, T, C, A) {
  let I = e, R = A, $ = 0, p = !1;
  for (; (R = R.get(Py)) !== void 0 && !p; ) {
    const M = R.get(e);
    if ($ += 1, typeof M < "u") {
      if (M === $) throw new RangeError("Cyclic object value");
      p = !0;
    }
    typeof R.get(Py) > "u" && ($ = 0);
  }
  if (typeof u == "function" ? I = u(t, I) : I instanceof Date ? I = m?.(I) : n === "comma" && Ar(I) && (I = BA(I, function(M) {
    return M instanceof Date ? m?.(M) : M;
  })), I === null) {
    if (s) return l && !T ? l(t, Dn.encoder, C, "key", v) : t;
    I = "";
  }
  if (lY(I) || iY(I)) {
    if (l) {
      const M = T ? t : l(t, Dn.encoder, C, "key", v);
      return [y?.(M) + "=" + y?.(l(I, Dn.encoder, C, "value", v))];
    }
    return [y?.(t) + "=" + y?.(String(I))];
  }
  const b = [];
  if (typeof I > "u") return b;
  let w;
  if (n === "comma" && Ar(I))
    T && l && (I = BA(I, l)), w = [{ value: I.length > 0 ? I.join(",") || null : void 0 }];
  else if (Ar(u)) w = u;
  else {
    const M = Object.keys(I);
    w = d ? M.sort(d) : M;
  }
  const E = i ? String(t).replace(/\./g, "%2E") : String(t), x = r && Ar(I) && I.length === 1 ? E + "[]" : E;
  if (o && Ar(I) && I.length === 0) return x + "[]";
  for (let M = 0; M < w.length; ++M) {
    const N = w[M], q = typeof N == "object" && typeof N.value < "u" ? N.value : I[N];
    if (a && q === null) continue;
    const z = f && i ? N.replace(/\./g, "%2E") : N, J = Ar(I) ? typeof n == "function" ? n(x, z) : x : x + (f ? "." + z : "[" + z + "]");
    A.set(e, $);
    const G = /* @__PURE__ */ new WeakMap();
    G.set(Py, A), B4(b, U4(q, J, n, r, o, s, a, i, n === "comma" && T && Ar(I) ? null : l, u, d, f, m, v, y, T, C, G));
  }
  return b;
}
function uY(e = Dn) {
  if (typeof e.allowEmptyArrays < "u" && typeof e.allowEmptyArrays != "boolean") throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
  if (typeof e.encodeDotInKeys < "u" && typeof e.encodeDotInKeys != "boolean") throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
  if (e.encoder !== null && typeof e.encoder < "u" && typeof e.encoder != "function") throw new TypeError("Encoder has to be a function.");
  const t = e.charset || Dn.charset;
  if (typeof e.charset < "u" && e.charset !== "utf-8" && e.charset !== "iso-8859-1") throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
  let n = L4;
  if (typeof e.format < "u") {
    if (!$1(FA, e.format)) throw new TypeError("Unknown format option provided.");
    n = e.format;
  }
  const r = FA[n];
  let o = Dn.filter;
  (typeof e.filter == "function" || Ar(e.filter)) && (o = e.filter);
  let s;
  if (e.arrayFormat && e.arrayFormat in F4 ? s = e.arrayFormat : "indices" in e ? s = e.indices ? "indices" : "repeat" : s = Dn.arrayFormat, "commaRoundTrip" in e && typeof e.commaRoundTrip != "boolean") throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
  const a = typeof e.allowDots > "u" ? e.encodeDotInKeys ? !0 : Dn.allowDots : !!e.allowDots;
  return {
    addQueryPrefix: typeof e.addQueryPrefix == "boolean" ? e.addQueryPrefix : Dn.addQueryPrefix,
    allowDots: a,
    allowEmptyArrays: typeof e.allowEmptyArrays == "boolean" ? !!e.allowEmptyArrays : Dn.allowEmptyArrays,
    arrayFormat: s,
    charset: t,
    charsetSentinel: typeof e.charsetSentinel == "boolean" ? e.charsetSentinel : Dn.charsetSentinel,
    commaRoundTrip: !!e.commaRoundTrip,
    delimiter: typeof e.delimiter > "u" ? Dn.delimiter : e.delimiter,
    encode: typeof e.encode == "boolean" ? e.encode : Dn.encode,
    encodeDotInKeys: typeof e.encodeDotInKeys == "boolean" ? e.encodeDotInKeys : Dn.encodeDotInKeys,
    encoder: typeof e.encoder == "function" ? e.encoder : Dn.encoder,
    encodeValuesOnly: typeof e.encodeValuesOnly == "boolean" ? e.encodeValuesOnly : Dn.encodeValuesOnly,
    filter: o,
    format: n,
    formatter: r,
    serializeDate: typeof e.serializeDate == "function" ? e.serializeDate : Dn.serializeDate,
    skipNulls: typeof e.skipNulls == "boolean" ? e.skipNulls : Dn.skipNulls,
    sort: typeof e.sort == "function" ? e.sort : null,
    strictNullHandling: typeof e.strictNullHandling == "boolean" ? e.strictNullHandling : Dn.strictNullHandling
  };
}
function cY(e, t = {}) {
  let n = e;
  const r = uY(t);
  let o, s;
  typeof r.filter == "function" ? (s = r.filter, n = s("", n)) : Ar(r.filter) && (s = r.filter, o = s);
  const a = [];
  if (typeof n != "object" || n === null) return "";
  const i = F4[r.arrayFormat], l = i === "comma" && r.commaRoundTrip;
  o || (o = Object.keys(n)), r.sort && o.sort(r.sort);
  const u = /* @__PURE__ */ new WeakMap();
  for (let m = 0; m < o.length; ++m) {
    const v = o[m];
    r.skipNulls && n[v] === null || B4(a, U4(n[v], v, i, l, r.allowEmptyArrays, r.strictNullHandling, r.skipNulls, r.encodeDotInKeys, r.encode ? r.encoder : null, r.filter, r.sort, r.allowDots, r.serializeDate, r.format, r.formatter, r.encodeValuesOnly, r.charset, u));
  }
  const d = a.join(r.delimiter);
  let f = r.addQueryPrefix === !0 ? "?" : "";
  return r.charsetSentinel && (r.charset === "iso-8859-1" ? f += "utf8=%26%2310003%3B&" : f += "utf8=%E2%9C%93&"), d.length > 0 ? f + d : "";
}
function dY(e) {
  return cY(e, { arrayFormat: "brackets" });
}
function fY(e) {
  let t = 0;
  for (const o of e) t += o.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const o of e)
    n.set(o, r), r += o.length;
  return n;
}
var qA;
function Kb(e) {
  let t;
  return (qA ?? (t = new globalThis.TextEncoder(), qA = t.encode.bind(t)))(e);
}
var HA;
function zA(e) {
  let t;
  return (HA ?? (t = new globalThis.TextDecoder(), HA = t.decode.bind(t)))(e);
}
var Hr, zr, Eg = class {
  constructor() {
    Hr.set(this, void 0), zr.set(this, void 0), St(this, Hr, new Uint8Array(), "f"), St(this, zr, null, "f");
  }
  decode(e) {
    if (e == null) return [];
    const t = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? Kb(e) : e;
    St(this, Hr, fY([Ae(this, Hr, "f"), t]), "f");
    const n = [];
    let r;
    for (; (r = hY(Ae(this, Hr, "f"), Ae(this, zr, "f"))) != null; ) {
      if (r.carriage && Ae(this, zr, "f") == null) {
        St(this, zr, r.index, "f");
        continue;
      }
      if (Ae(this, zr, "f") != null && (r.index !== Ae(this, zr, "f") + 1 || r.carriage)) {
        n.push(zA(Ae(this, Hr, "f").subarray(0, Ae(this, zr, "f") - 1))), St(this, Hr, Ae(this, Hr, "f").subarray(Ae(this, zr, "f")), "f"), St(this, zr, null, "f");
        continue;
      }
      const o = Ae(this, zr, "f") !== null ? r.preceding - 1 : r.preceding, s = zA(Ae(this, Hr, "f").subarray(0, o));
      n.push(s), St(this, Hr, Ae(this, Hr, "f").subarray(r.index), "f"), St(this, zr, null, "f");
    }
    return n;
  }
  flush() {
    return Ae(this, Hr, "f").length ? this.decode(`
`) : [];
  }
};
Hr = /* @__PURE__ */ new WeakMap(), zr = /* @__PURE__ */ new WeakMap();
Eg.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
Eg.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function hY(e, t) {
  for (let o = t ?? 0; o < e.length; o++) {
    if (e[o] === 10) return {
      preceding: o,
      index: o + 1,
      carriage: !1
    };
    if (e[o] === 13) return {
      preceding: o,
      index: o + 1,
      carriage: !0
    };
  }
  return null;
}
function pY(e) {
  for (let r = 0; r < e.length - 1; r++) {
    if (e[r] === 10 && e[r + 1] === 10 || e[r] === 13 && e[r + 1] === 13) return r + 2;
    if (e[r] === 13 && e[r + 1] === 10 && r + 3 < e.length && e[r + 2] === 13 && e[r + 3] === 10) return r + 4;
  }
  return -1;
}
var Sm = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, WA = (e, t, n) => {
  if (e) {
    if (QJ(Sm, e)) return e;
    Jn(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(Sm))}`);
  }
};
function Vc() {
}
function Dh(e, t, n) {
  return !t || Sm[e] > Sm[n] ? Vc : t[e].bind(t);
}
var mY = {
  error: Vc,
  warn: Vc,
  info: Vc,
  debug: Vc
}, GA = /* @__PURE__ */ new WeakMap();
function Jn(e) {
  const t = e.logger, n = e.logLevel ?? "off";
  if (!t) return mY;
  const r = GA.get(t);
  if (r && r[0] === n) return r[1];
  const o = {
    error: Dh("error", t, n),
    warn: Dh("warn", t, n),
    info: Dh("info", t, n),
    debug: Dh("debug", t, n)
  };
  return GA.set(t, [n, o]), o;
}
var bi = (e) => (e.options && (e.options = { ...e.options }, delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "authorization" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), xc, Vd = class Kc {
  constructor(t, n, r) {
    this.iterator = t, xc.set(this, void 0), this.controller = n, St(this, xc, r, "f");
  }
  static fromSSEResponse(t, n, r, o) {
    let s = !1;
    const a = r ? Jn(r) : console;
    async function* i() {
      if (s) throw new mt("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      s = !0;
      let l = !1;
      try {
        for await (const u of gY(t, n))
          if (!l) {
            if (u.data.startsWith("[DONE]")) {
              l = !0;
              continue;
            }
            if (u.event === null || !u.event.startsWith("thread.")) {
              let d;
              try {
                d = JSON.parse(u.data);
              } catch (f) {
                throw a.error("Could not parse message into JSON:", u.data), a.error("From chunk:", u.raw), f;
              }
              if (d && d.error) throw new ir(void 0, d.error, void 0, t.headers);
              yield o ? {
                event: u.event,
                data: d
              } : d;
            } else {
              let d;
              try {
                d = JSON.parse(u.data);
              } catch (f) {
                throw console.error("Could not parse message into JSON:", u.data), console.error("From chunk:", u.raw), f;
              }
              if (u.event == "error") throw new ir(void 0, d.error, d.message, void 0);
              yield {
                event: u.event,
                data: d
              };
            }
          }
        l = !0;
      } catch (u) {
        if (P1(u)) return;
        throw u;
      } finally {
        l || n.abort();
      }
    }
    return new Kc(i, n, r);
  }
  static fromReadableStream(t, n, r) {
    let o = !1;
    async function* s() {
      const i = new Eg(), l = D4(t);
      for await (const u of l) for (const d of i.decode(u)) yield d;
      for (const u of i.flush()) yield u;
    }
    async function* a() {
      if (o) throw new mt("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      o = !0;
      let i = !1;
      try {
        for await (const l of s())
          i || l && (yield JSON.parse(l));
        i = !0;
      } catch (l) {
        if (P1(l)) return;
        throw l;
      } finally {
        i || n.abort();
      }
    }
    return new Kc(a, n, r);
  }
  [(xc = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), o = (s) => ({ next: () => {
      if (s.length === 0) {
        const a = r.next();
        t.push(a), n.push(a);
      }
      return s.shift();
    } });
    return [new Kc(() => o(t), this.controller, Ae(this, xc, "f")), new Kc(() => o(n), this.controller, Ae(this, xc, "f"))];
  }
  toReadableStream() {
    const t = this;
    let n;
    return N4({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: o, done: s } = await n.next();
          if (s) return r.close();
          const a = Kb(JSON.stringify(o) + `
`);
          r.enqueue(a);
        } catch (o) {
          r.error(o);
        }
      },
      async cancel() {
        await n.return?.();
      }
    });
  }
};
async function* gY(e, t) {
  if (!e.body)
    throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new mt("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new mt("Attempted to iterate over a response with no body");
  const n = new yY(), r = new Eg(), o = D4(e.body);
  for await (const s of vY(o)) for (const a of r.decode(s)) {
    const i = n.decode(a);
    i && (yield i);
  }
  for (const s of r.flush()) {
    const a = n.decode(s);
    a && (yield a);
  }
}
async function* vY(e) {
  let t = new Uint8Array();
  for await (const n of e) {
    if (n == null) continue;
    const r = n instanceof ArrayBuffer ? new Uint8Array(n) : typeof n == "string" ? Kb(n) : n;
    let o = new Uint8Array(t.length + r.length);
    o.set(t), o.set(r, t.length), t = o;
    let s;
    for (; (s = pY(t)) !== -1; )
      yield t.slice(0, s), t = t.slice(s);
  }
  t.length > 0 && (yield t);
}
var yY = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const o = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], o;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = _Y(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function _Y(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function q4(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: o, startTime: s } = t, a = await (async () => {
    if (t.options.stream)
      return Jn(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller, e, t.options.__synthesizeEventData) : Vd.fromSSEResponse(n, t.controller, e, t.options.__synthesizeEventData);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const i = n.headers.get("content-type")?.split(";")[0]?.trim();
    return i?.includes("application/json") || i?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : H4(await n.json(), n) : await n.text();
  })();
  return Jn(e).debug(`[${r}] response parsed`, bi({
    retryOfRequestLogID: o,
    url: n.url,
    status: n.status,
    body: a,
    durationMs: Date.now() - s
  })), a;
}
function H4(e, t) {
  return !e || typeof e != "object" || Array.isArray(e) ? e : Object.defineProperty(e, "_request_id", {
    value: t.headers.get("x-request-id"),
    enumerable: !1
  });
}
var Jc, z4 = class W4 extends Promise {
  constructor(t, n, r = q4) {
    super((o) => {
      o(null);
    }), this.responsePromise = n, this.parseResponse = r, Jc.set(this, void 0), St(this, Jc, t, "f");
  }
  _thenUnwrap(t) {
    return new W4(Ae(this, Jc, "f"), this.responsePromise, async (n, r) => H4(t(await this.parseResponse(n, r), r), r.response));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n,
      request_id: n.headers.get("x-request-id")
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(Ae(this, Jc, "f"), t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
};
Jc = /* @__PURE__ */ new WeakMap();
var Lh, Jb = class {
  constructor(e, t, n, r) {
    Lh.set(this, void 0), St(this, Lh, e, "f"), this.options = r, this.response = t, this.body = n;
  }
  hasNextPage() {
    return this.getPaginatedItems().length ? this.nextPageRequestOptions() != null : !1;
  }
  async getNextPage() {
    const e = this.nextPageRequestOptions();
    if (!e) throw new mt("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
    return await Ae(this, Lh, "f").requestAPIList(this.constructor, e);
  }
  async *iterPages() {
    let e = this;
    for (yield e; e.hasNextPage(); )
      e = await e.getNextPage(), yield e;
  }
  async *[(Lh = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    for await (const e of this.iterPages()) for (const t of e.getPaginatedItems()) yield t;
  }
}, bY = class extends z4 {
  constructor(e, t, n) {
    super(e, t, async (r, o) => new n(r, o.response, await q4(r, o), o.options));
  }
  async *[Symbol.asyncIterator]() {
    const e = await this;
    for await (const t of e) yield t;
  }
}, Ag = class extends Jb {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.object = n.object;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  nextPageRequestOptions() {
    return null;
  }
}, bn = class extends Jb {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    const e = this.getPaginatedItems(), t = e[e.length - 1]?.id;
    return t ? {
      ...this.options,
      query: {
        ...P4(this.options.query),
        after: t
      }
    } : null;
  }
}, Kd = class extends Jb {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1, this.last_id = n.last_id || "";
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    const e = this.last_id;
    return e ? {
      ...this.options,
      query: {
        ...P4(this.options.query),
        after: e
      }
    } : null;
  }
}, wY = {
  jwt: "urn:ietf:params:oauth:token-type:jwt",
  id: "urn:ietf:params:oauth:token-type:id_token"
}, SY = "urn:ietf:params:oauth:grant-type:token-exchange", TY = class {
  constructor(e, t) {
    this.cachedToken = null, this.refreshPromise = null, this.tokenExchangeUrl = "https://auth.openai.com/oauth/token", this.config = e, this.fetch = t ?? R4();
  }
  async getToken() {
    if (!this.cachedToken || this.isTokenExpired(this.cachedToken)) {
      if (this.refreshPromise) return await this.refreshPromise;
      this.refreshPromise = this.refreshToken();
      try {
        return await this.refreshPromise;
      } finally {
        this.refreshPromise = null;
      }
    }
    return this.needsRefresh(this.cachedToken) && !this.refreshPromise && (this.refreshPromise = this.refreshToken().finally(() => {
      this.refreshPromise = null;
    })), this.cachedToken.token;
  }
  async refreshToken() {
    const e = await this.config.provider.getToken(), t = await this.fetch(this.tokenExchangeUrl, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        grant_type: SY,
        client_id: this.config.clientId,
        subject_token: e,
        subject_token_type: wY[this.config.provider.tokenType],
        identity_provider_id: this.config.identityProviderId,
        service_account_id: this.config.serviceAccountId
      })
    });
    if (!t.ok) {
      const s = await t.text();
      let a;
      try {
        a = JSON.parse(s);
      } catch {
      }
      throw t.status === 400 || t.status === 401 || t.status === 403 ? new M4(t.status, a, t.headers) : ir.generate(t.status, a, `Token exchange failed with status ${t.status}`, t.headers);
    }
    const n = await t.json(), r = n.expires_in || 3600, o = Date.now() + r * 1e3;
    return this.cachedToken = {
      token: n.access_token,
      expiresAt: o
    }, n.access_token;
  }
  isTokenExpired(e) {
    return Date.now() >= e.expiresAt;
  }
  needsRefresh(e) {
    const t = (this.config.refreshBufferSeconds ?? 1200) * 1e3;
    return Date.now() >= e.expiresAt - t;
  }
  invalidateToken() {
    this.cachedToken = null, this.refreshPromise = null;
  }
}, G4 = () => {
  if (typeof File > "u") {
    const { process: e } = globalThis, t = typeof e?.versions?.node == "string" && parseInt(e.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (t ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function gd(e, t, n) {
  return G4(), new File(e, t ?? "unknown_file", n);
}
function pp(e) {
  return (typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "").split(/[\\/]/).pop() || void 0;
}
var Yb = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", Cg = async (e, t) => D1(e.body) ? {
  ...e,
  body: await V4(e.body, t)
} : e, ss = async (e, t) => ({
  ...e,
  body: await V4(e.body, t)
}), VA = /* @__PURE__ */ new WeakMap();
function EY(e) {
  const t = typeof e == "function" ? e : e.fetch, n = VA.get(t);
  if (n) return n;
  const r = (async () => {
    try {
      const o = "Response" in t ? t.Response : (await t("data:,")).constructor, s = new FormData();
      return s.toString() !== await new o(s).text();
    } catch {
      return !0;
    }
  })();
  return VA.set(t, r), r;
}
var V4 = async (e, t) => {
  if (!await EY(t)) throw new TypeError("The provided fetch function does not support file uploads with the current global FormData class.");
  const n = new FormData();
  return await Promise.all(Object.entries(e || {}).map(([r, o]) => L1(n, r, o))), n;
}, K4 = (e) => e instanceof Blob && "name" in e, AY = (e) => typeof e == "object" && e !== null && (e instanceof Response || Yb(e) || K4(e)), D1 = (e) => {
  if (AY(e)) return !0;
  if (Array.isArray(e)) return e.some(D1);
  if (e && typeof e == "object") {
    for (const t in e) if (D1(e[t])) return !0;
  }
  return !1;
}, L1 = async (e, t, n) => {
  if (n !== void 0) {
    if (n == null) throw new TypeError(`Received null for "${t}"; to pass null in FormData, you must use the string 'null'`);
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") e.append(t, String(n));
    else if (n instanceof Response) e.append(t, gd([await n.blob()], pp(n)));
    else if (Yb(n)) e.append(t, gd([await new Response($4(n)).blob()], pp(n)));
    else if (K4(n)) e.append(t, n, pp(n));
    else if (Array.isArray(n)) await Promise.all(n.map((r) => L1(e, t + "[]", r)));
    else if (typeof n == "object") await Promise.all(Object.entries(n).map(([r, o]) => L1(e, `${t}[${r}]`, o)));
    else throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${n} instead`);
  }
}, J4 = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", CY = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && J4(e), kY = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function IY(e, t, n) {
  if (G4(), e = await e, CY(e))
    return e instanceof File ? e : gd([await e.arrayBuffer()], e.name);
  if (kY(e)) {
    const o = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), gd(await O1(o), t, n);
  }
  const r = await O1(e);
  if (t || (t = pp(e)), !n?.type) {
    const o = r.find((s) => typeof s == "object" && "type" in s && s.type);
    typeof o == "string" && (n = {
      ...n,
      type: o
    });
  }
  return gd(r, t, n);
}
async function O1(e) {
  let t = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) t.push(e);
  else if (J4(e)) t.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (Yb(e)) for await (const n of e) t.push(...await O1(n));
  else {
    const n = e?.constructor?.name;
    throw new Error(`Unexpected data type: ${typeof e}${n ? `; constructor: ${n}` : ""}${xY(e)}`);
  }
  return t;
}
function xY(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var ft = class {
  constructor(e) {
    this._client = e;
  }
};
function Y4(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var KA = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), MY = (e = Y4) => function(n, ...r) {
  if (n.length === 1) return n[0];
  let o = !1;
  const s = [], a = n.reduce((d, f, m) => {
    /[?#]/.test(f) && (o = !0);
    const v = r[m];
    let y = (o ? encodeURIComponent : e)("" + v);
    return m !== r.length && (v == null || typeof v == "object" && v.toString === Object.getPrototypeOf(Object.getPrototypeOf(v.hasOwnProperty ?? KA) ?? KA)?.toString) && (y = v + "", s.push({
      start: d.length + f.length,
      length: y.length,
      error: `Value of type ${Object.prototype.toString.call(v).slice(8, -1)} is not a valid path parameter`
    })), d + f + (m === r.length ? "" : y);
  }, ""), i = a.split(/[?#]/, 1)[0], l = /(?<=^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = l.exec(i)) !== null; ) s.push({
    start: u.index,
    length: u[0].length,
    error: `Value "${u[0]}" can't be safely passed as a path parameter`
  });
  if (s.sort((d, f) => d.start - f.start), s.length > 0) {
    let d = 0;
    const f = s.reduce((m, v) => {
      const y = " ".repeat(v.start - d), T = "^".repeat(v.length);
      return d = v.start + v.length, m + y + T;
    }, "");
    throw new mt(`Path parameters result in path with invalid segments:
${s.map((m) => m.error).join(`
`)}
${a}
${f}`);
  }
  return a;
}, Fe = /* @__PURE__ */ MY(Y4), X4 = class extends ft {
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/chat/completions/${e}/messages`, bn, {
      query: t,
      ...n
    });
  }
};
function Tm(e) {
  return e !== void 0 && "function" in e && e.function !== void 0;
}
function Xb(e) {
  return e?.$brand === "auto-parseable-response-format";
}
function yf(e) {
  return e?.$brand === "auto-parseable-tool";
}
function PY(e, t) {
  return !t || !Q4(t) ? {
    ...e,
    choices: e.choices.map((n) => (Z4(n.message.tool_calls), {
      ...n,
      message: {
        ...n.message,
        parsed: null,
        ...n.message.tool_calls ? { tool_calls: n.message.tool_calls } : void 0
      }
    }))
  } : Qb(e, t);
}
function Qb(e, t) {
  const n = e.choices.map((r) => {
    if (r.finish_reason === "length") throw new I4();
    if (r.finish_reason === "content_filter") throw new x4();
    return Z4(r.message.tool_calls), {
      ...r,
      message: {
        ...r.message,
        ...r.message.tool_calls ? { tool_calls: r.message.tool_calls?.map((o) => NY(t, o)) ?? void 0 } : void 0,
        parsed: r.message.content && !r.message.refusal ? RY(t, r.message.content) : null
      }
    };
  });
  return {
    ...e,
    choices: n
  };
}
function RY(e, t) {
  return e.response_format?.type !== "json_schema" ? null : e.response_format?.type === "json_schema" ? "$parseRaw" in e.response_format ? e.response_format.$parseRaw(t) : JSON.parse(t) : null;
}
function NY(e, t) {
  const n = e.tools?.find((r) => Tm(r) && r.function?.name === t.function.name);
  return {
    ...t,
    function: {
      ...t.function,
      parsed_arguments: yf(n) ? n.$parseRaw(t.function.arguments) : n?.function.strict ? JSON.parse(t.function.arguments) : null
    }
  };
}
function $Y(e, t) {
  if (!e || !("tools" in e) || !e.tools) return !1;
  const n = e.tools?.find((r) => Tm(r) && r.function?.name === t.function.name);
  return Tm(n) && (yf(n) || n?.function.strict || !1);
}
function Q4(e) {
  return Xb(e.response_format) ? !0 : e.tools?.some((t) => yf(t) || t.type === "function" && t.function.strict === !0) ?? !1;
}
function Z4(e) {
  for (const t of e || []) if (t.type !== "function") throw new mt(`Currently only \`function\` tool calls are supported; Received \`${t.type}\``);
}
function DY(e) {
  for (const t of e ?? []) {
    if (t.type !== "function") throw new mt(`Currently only \`function\` tool types support auto-parsing; Received \`${t.type}\``);
    if (t.function.strict !== !0) throw new mt(`The \`${t.function.name}\` tool is not marked with \`strict: true\`. Only strict function tools can be auto-parsed`);
  }
}
var Em = (e) => e?.role === "assistant", j4 = (e) => e?.role === "tool", F1, mp, gp, Yc, Xc, vp, Qc, Ms, Zc, Am, Cm, Ul, eP, Zb = class {
  constructor() {
    F1.add(this), this.controller = new AbortController(), mp.set(this, void 0), gp.set(this, () => {
    }), Yc.set(this, () => {
    }), Xc.set(this, void 0), vp.set(this, () => {
    }), Qc.set(this, () => {
    }), Ms.set(this, {}), Zc.set(this, !1), Am.set(this, !1), Cm.set(this, !1), Ul.set(this, !1), St(this, mp, new Promise((e, t) => {
      St(this, gp, e, "f"), St(this, Yc, t, "f");
    }), "f"), St(this, Xc, new Promise((e, t) => {
      St(this, vp, e, "f"), St(this, Qc, t, "f");
    }), "f"), Ae(this, mp, "f").catch(() => {
    }), Ae(this, Xc, "f").catch(() => {
    });
  }
  _run(e) {
    setTimeout(() => {
      e().then(() => {
        this._emitFinal(), this._emit("end");
      }, Ae(this, F1, "m", eP).bind(this));
    }, 0);
  }
  _connected() {
    this.ended || (Ae(this, gp, "f").call(this), this._emit("connect"));
  }
  get ended() {
    return Ae(this, Zc, "f");
  }
  get errored() {
    return Ae(this, Am, "f");
  }
  get aborted() {
    return Ae(this, Cm, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(e, t) {
    return (Ae(this, Ms, "f")[e] || (Ae(this, Ms, "f")[e] = [])).push({ listener: t }), this;
  }
  off(e, t) {
    const n = Ae(this, Ms, "f")[e];
    if (!n) return this;
    const r = n.findIndex((o) => o.listener === t);
    return r >= 0 && n.splice(r, 1), this;
  }
  once(e, t) {
    return (Ae(this, Ms, "f")[e] || (Ae(this, Ms, "f")[e] = [])).push({
      listener: t,
      once: !0
    }), this;
  }
  emitted(e) {
    return new Promise((t, n) => {
      St(this, Ul, !0, "f"), e !== "error" && this.once("error", n), this.once(e, t);
    });
  }
  async done() {
    St(this, Ul, !0, "f"), await Ae(this, Xc, "f");
  }
  _emit(e, ...t) {
    if (Ae(this, Zc, "f")) return;
    e === "end" && (St(this, Zc, !0, "f"), Ae(this, vp, "f").call(this));
    const n = Ae(this, Ms, "f")[e];
    if (n && (Ae(this, Ms, "f")[e] = n.filter((r) => !r.once), n.forEach(({ listener: r }) => r(...t))), e === "abort") {
      const r = t[0];
      !Ae(this, Ul, "f") && !n?.length && Promise.reject(r), Ae(this, Yc, "f").call(this, r), Ae(this, Qc, "f").call(this, r), this._emit("end");
      return;
    }
    if (e === "error") {
      const r = t[0];
      !Ae(this, Ul, "f") && !n?.length && Promise.reject(r), Ae(this, Yc, "f").call(this, r), Ae(this, Qc, "f").call(this, r), this._emit("end");
    }
  }
  _emitFinal() {
  }
};
mp = /* @__PURE__ */ new WeakMap(), gp = /* @__PURE__ */ new WeakMap(), Yc = /* @__PURE__ */ new WeakMap(), Xc = /* @__PURE__ */ new WeakMap(), vp = /* @__PURE__ */ new WeakMap(), Qc = /* @__PURE__ */ new WeakMap(), Ms = /* @__PURE__ */ new WeakMap(), Zc = /* @__PURE__ */ new WeakMap(), Am = /* @__PURE__ */ new WeakMap(), Cm = /* @__PURE__ */ new WeakMap(), Ul = /* @__PURE__ */ new WeakMap(), F1 = /* @__PURE__ */ new WeakSet(), eP = function(t) {
  if (St(this, Am, !0, "f"), t instanceof Error && t.name === "AbortError" && (t = new go()), t instanceof go)
    return St(this, Cm, !0, "f"), this._emit("abort", t);
  if (t instanceof mt) return this._emit("error", t);
  if (t instanceof Error) {
    const n = new mt(t.message);
    return n.cause = t, this._emit("error", n);
  }
  return this._emit("error", new mt(String(t)));
};
function LY(e) {
  return typeof e.parse == "function";
}
var fr, B1, km, U1, q1, H1, tP, nP, OY = 10, rP = class extends Zb {
  constructor() {
    super(...arguments), fr.add(this), this._chatCompletions = [], this.messages = [];
  }
  _addChatCompletion(e) {
    this._chatCompletions.push(e), this._emit("chatCompletion", e);
    const t = e.choices[0]?.message;
    return t && this._addMessage(t), e;
  }
  _addMessage(e, t = !0) {
    if ("content" in e || (e.content = null), this.messages.push(e), t) {
      if (this._emit("message", e), j4(e) && e.content) this._emit("functionToolCallResult", e.content);
      else if (Em(e) && e.tool_calls)
        for (const n of e.tool_calls) n.type === "function" && this._emit("functionToolCall", n.function);
    }
  }
  async finalChatCompletion() {
    await this.done();
    const e = this._chatCompletions[this._chatCompletions.length - 1];
    if (!e) throw new mt("stream ended without producing a ChatCompletion");
    return e;
  }
  async finalContent() {
    return await this.done(), Ae(this, fr, "m", B1).call(this);
  }
  async finalMessage() {
    return await this.done(), Ae(this, fr, "m", km).call(this);
  }
  async finalFunctionToolCall() {
    return await this.done(), Ae(this, fr, "m", U1).call(this);
  }
  async finalFunctionToolCallResult() {
    return await this.done(), Ae(this, fr, "m", q1).call(this);
  }
  async totalUsage() {
    return await this.done(), Ae(this, fr, "m", H1).call(this);
  }
  allChatCompletions() {
    return [...this._chatCompletions];
  }
  _emitFinal() {
    const e = this._chatCompletions[this._chatCompletions.length - 1];
    e && this._emit("finalChatCompletion", e);
    const t = Ae(this, fr, "m", km).call(this);
    t && this._emit("finalMessage", t);
    const n = Ae(this, fr, "m", B1).call(this);
    n && this._emit("finalContent", n);
    const r = Ae(this, fr, "m", U1).call(this);
    r && this._emit("finalFunctionToolCall", r);
    const o = Ae(this, fr, "m", q1).call(this);
    o != null && this._emit("finalFunctionToolCallResult", o), this._chatCompletions.some((s) => s.usage) && this._emit("totalUsage", Ae(this, fr, "m", H1).call(this));
  }
  async _createChatCompletion(e, t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort())), Ae(this, fr, "m", tP).call(this, t);
    const o = await e.chat.completions.create({
      ...t,
      stream: !1
    }, {
      ...n,
      signal: this.controller.signal
    });
    return this._connected(), this._addChatCompletion(Qb(o, t));
  }
  async _runChatCompletion(e, t, n) {
    for (const r of t.messages) this._addMessage(r, !1);
    return await this._createChatCompletion(e, t, n);
  }
  async _runTools(e, t, n) {
    const r = "tool", { tool_choice: o = "auto", stream: s, ...a } = t, i = typeof o != "string" && o.type === "function" && o?.function?.name, { maxChatCompletions: l = OY } = n || {}, u = t.tools.map((m) => {
      if (yf(m)) {
        if (!m.$callback) throw new mt("Tool given to `.runTools()` that does not have an associated function");
        return {
          type: "function",
          function: {
            function: m.$callback,
            name: m.function.name,
            description: m.function.description || "",
            parameters: m.function.parameters,
            parse: m.$parseRaw,
            strict: !0
          }
        };
      }
      return m;
    }), d = {};
    for (const m of u) m.type === "function" && (d[m.function.name || m.function.function.name] = m.function);
    const f = "tools" in t ? u.map((m) => m.type === "function" ? {
      type: "function",
      function: {
        name: m.function.name || m.function.function.name,
        parameters: m.function.parameters,
        description: m.function.description,
        strict: m.function.strict
      }
    } : m) : void 0;
    for (const m of t.messages) this._addMessage(m, !1);
    for (let m = 0; m < l; ++m) {
      const v = (await this._createChatCompletion(e, {
        ...a,
        tool_choice: o,
        tools: f,
        messages: [...this.messages]
      }, n)).choices[0]?.message;
      if (!v) throw new mt("missing message in ChatCompletion response");
      if (!v.tool_calls?.length) return;
      for (const y of v.tool_calls) {
        if (y.type !== "function") continue;
        const T = y.id, { name: C, arguments: A } = y.function, I = d[C];
        if (I) {
          if (i && i !== C) {
            const b = `Invalid tool_call: ${JSON.stringify(C)}. ${JSON.stringify(i)} requested. Please try again`;
            this._addMessage({
              role: r,
              tool_call_id: T,
              content: b
            });
            continue;
          }
        } else {
          const b = `Invalid tool_call: ${JSON.stringify(C)}. Available options are: ${Object.keys(d).map((w) => JSON.stringify(w)).join(", ")}. Please try again`;
          this._addMessage({
            role: r,
            tool_call_id: T,
            content: b
          });
          continue;
        }
        let R;
        try {
          R = LY(I) ? await I.parse(A) : A;
        } catch (b) {
          const w = b instanceof Error ? b.message : String(b);
          this._addMessage({
            role: r,
            tool_call_id: T,
            content: w
          });
          continue;
        }
        const $ = await I.function(R, this), p = Ae(this, fr, "m", nP).call(this, $);
        if (this._addMessage({
          role: r,
          tool_call_id: T,
          content: p
        }), i) return;
      }
    }
  }
};
fr = /* @__PURE__ */ new WeakSet(), B1 = function() {
  return Ae(this, fr, "m", km).call(this).content ?? null;
}, km = function() {
  let t = this.messages.length;
  for (; t-- > 0; ) {
    const n = this.messages[t];
    if (Em(n)) return {
      ...n,
      content: n.content ?? null,
      refusal: n.refusal ?? null
    };
  }
  throw new mt("stream ended without producing a ChatCompletionMessage with role=assistant");
}, U1 = function() {
  for (let t = this.messages.length - 1; t >= 0; t--) {
    const n = this.messages[t];
    if (Em(n) && n?.tool_calls?.length) return n.tool_calls.filter((r) => r.type === "function").at(-1)?.function;
  }
}, q1 = function() {
  for (let t = this.messages.length - 1; t >= 0; t--) {
    const n = this.messages[t];
    if (j4(n) && n.content != null && typeof n.content == "string" && this.messages.some((r) => r.role === "assistant" && r.tool_calls?.some((o) => o.type === "function" && o.id === n.tool_call_id))) return n.content;
  }
}, H1 = function() {
  const t = {
    completion_tokens: 0,
    prompt_tokens: 0,
    total_tokens: 0
  };
  for (const { usage: n } of this._chatCompletions) n && (t.completion_tokens += n.completion_tokens, t.prompt_tokens += n.prompt_tokens, t.total_tokens += n.total_tokens);
  return t;
}, tP = function(t) {
  if (t.n != null && t.n > 1) throw new mt("ChatCompletion convenience helpers only support n=1 at this time. To use n>1, please use chat.completions.create() directly.");
}, nP = function(t) {
  return typeof t == "string" ? t : t === void 0 ? "undefined" : JSON.stringify(t);
};
var FY = class oP extends rP {
  static runTools(t, n, r) {
    const o = new oP(), s = {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "runTools"
      }
    };
    return o._run(() => o._runTools(t, n, s)), o;
  }
  _addMessage(t, n = !0) {
    super._addMessage(t, n), Em(t) && t.content && this._emit("content", t.content);
  }
}, BY = 1, sP = 2, aP = 4, iP = 8, UY = 16, qY = 32, HY = 64, lP = 128, uP = 256, zY = lP | uP, WY = 496, JA = sP | 497, YA = aP | iP, Un = {
  STR: BY,
  NUM: sP,
  ARR: aP,
  OBJ: iP,
  NULL: UY,
  BOOL: qY,
  NAN: HY,
  INFINITY: lP,
  MINUS_INFINITY: uP,
  INF: zY,
  SPECIAL: WY,
  ATOM: JA,
  COLLECTION: YA,
  ALL: JA | YA
}, GY = class extends Error {
}, VY = class extends Error {
};
function KY(e, t = Un.ALL) {
  if (typeof e != "string") throw new TypeError(`expecting str, got ${typeof e}`);
  if (!e.trim()) throw new Error(`${e} is empty`);
  return JY(e.trim(), t);
}
var JY = (e, t) => {
  const n = e.length;
  let r = 0;
  const o = (m) => {
    throw new GY(`${m} at position ${r}`);
  }, s = (m) => {
    throw new VY(`${m} at position ${r}`);
  }, a = () => (f(), r >= n && o("Unexpected end of input"), e[r] === '"' ? i() : e[r] === "{" ? l() : e[r] === "[" ? u() : e.substring(r, r + 4) === "null" || Un.NULL & t && n - r < 4 && "null".startsWith(e.substring(r)) ? (r += 4, null) : e.substring(r, r + 4) === "true" || Un.BOOL & t && n - r < 4 && "true".startsWith(e.substring(r)) ? (r += 4, !0) : e.substring(r, r + 5) === "false" || Un.BOOL & t && n - r < 5 && "false".startsWith(e.substring(r)) ? (r += 5, !1) : e.substring(r, r + 8) === "Infinity" || Un.INFINITY & t && n - r < 8 && "Infinity".startsWith(e.substring(r)) ? (r += 8, 1 / 0) : e.substring(r, r + 9) === "-Infinity" || Un.MINUS_INFINITY & t && 1 < n - r && n - r < 9 && "-Infinity".startsWith(e.substring(r)) ? (r += 9, -1 / 0) : e.substring(r, r + 3) === "NaN" || Un.NAN & t && n - r < 3 && "NaN".startsWith(e.substring(r)) ? (r += 3, NaN) : d()), i = () => {
    const m = r;
    let v = !1;
    for (r++; r < n && (e[r] !== '"' || v && e[r - 1] === "\\"); )
      v = e[r] === "\\" ? !v : !1, r++;
    if (e.charAt(r) == '"') try {
      return JSON.parse(e.substring(m, ++r - Number(v)));
    } catch (y) {
      s(String(y));
    }
    else if (Un.STR & t) try {
      return JSON.parse(e.substring(m, r - Number(v)) + '"');
    } catch {
      return JSON.parse(e.substring(m, e.lastIndexOf("\\")) + '"');
    }
    o("Unterminated string literal");
  }, l = () => {
    r++, f();
    const m = {};
    try {
      for (; e[r] !== "}"; ) {
        if (f(), r >= n && Un.OBJ & t) return m;
        const v = i();
        f(), r++;
        try {
          const y = a();
          Object.defineProperty(m, v, {
            value: y,
            writable: !0,
            enumerable: !0,
            configurable: !0
          });
        } catch (y) {
          if (Un.OBJ & t) return m;
          throw y;
        }
        f(), e[r] === "," && r++;
      }
    } catch {
      if (Un.OBJ & t) return m;
      o("Expected '}' at end of object");
    }
    return r++, m;
  }, u = () => {
    r++;
    const m = [];
    try {
      for (; e[r] !== "]"; )
        m.push(a()), f(), e[r] === "," && r++;
    } catch {
      if (Un.ARR & t) return m;
      o("Expected ']' at end of array");
    }
    return r++, m;
  }, d = () => {
    if (r === 0) {
      e === "-" && Un.NUM & t && o("Not sure what '-' is");
      try {
        return JSON.parse(e);
      } catch (v) {
        if (Un.NUM & t) try {
          return e[e.length - 1] === "." ? JSON.parse(e.substring(0, e.lastIndexOf("."))) : JSON.parse(e.substring(0, e.lastIndexOf("e")));
        } catch {
        }
        s(String(v));
      }
    }
    const m = r;
    for (e[r] === "-" && r++; e[r] && !",]}".includes(e[r]); ) r++;
    r == n && !(Un.NUM & t) && o("Unterminated number literal");
    try {
      return JSON.parse(e.substring(m, r));
    } catch {
      e.substring(m, r) === "-" && Un.NUM & t && o("Not sure what '-' is");
      try {
        return JSON.parse(e.substring(m, e.lastIndexOf("e")));
      } catch (y) {
        s(String(y));
      }
    }
  }, f = () => {
    for (; r < n && " \n\r\t".includes(e[r]); ) r++;
  };
  return a();
}, XA = (e) => KY(e, Un.ALL ^ Un.NUM), Nn, xs, kl, ha, Ry, Oh, Ny, $y, Dy, Fh, Ly, QA, cP = class z1 extends rP {
  constructor(t) {
    super(), Nn.add(this), xs.set(this, void 0), kl.set(this, void 0), ha.set(this, void 0), St(this, xs, t, "f"), St(this, kl, [], "f");
  }
  get currentChatCompletionSnapshot() {
    return Ae(this, ha, "f");
  }
  static fromReadableStream(t) {
    const n = new z1(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createChatCompletion(t, n, r) {
    const o = new z1(n);
    return o._run(() => o._runChatCompletion(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), o;
  }
  async _createChatCompletion(t, n, r) {
    super._createChatCompletion;
    const o = r?.signal;
    o && (o.aborted && this.controller.abort(), o.addEventListener("abort", () => this.controller.abort())), Ae(this, Nn, "m", Ry).call(this);
    const s = await t.chat.completions.create({
      ...n,
      stream: !0
    }, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const a of s) Ae(this, Nn, "m", Ny).call(this, a);
    if (s.controller.signal?.aborted) throw new go();
    return this._addChatCompletion(Ae(this, Nn, "m", Fh).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort())), Ae(this, Nn, "m", Ry).call(this), this._connected();
    const o = Vd.fromReadableStream(t, this.controller);
    let s;
    for await (const a of o)
      s && s !== a.id && this._addChatCompletion(Ae(this, Nn, "m", Fh).call(this)), Ae(this, Nn, "m", Ny).call(this, a), s = a.id;
    if (o.controller.signal?.aborted) throw new go();
    return this._addChatCompletion(Ae(this, Nn, "m", Fh).call(this));
  }
  [(xs = /* @__PURE__ */ new WeakMap(), kl = /* @__PURE__ */ new WeakMap(), ha = /* @__PURE__ */ new WeakMap(), Nn = /* @__PURE__ */ new WeakSet(), Ry = function() {
    this.ended || St(this, ha, void 0, "f");
  }, Oh = function(n) {
    let r = Ae(this, kl, "f")[n.index];
    return r || (r = {
      content_done: !1,
      refusal_done: !1,
      logprobs_content_done: !1,
      logprobs_refusal_done: !1,
      done_tool_calls: /* @__PURE__ */ new Set(),
      current_tool_call_index: null
    }, Ae(this, kl, "f")[n.index] = r, r);
  }, Ny = function(n) {
    if (this.ended) return;
    const r = Ae(this, Nn, "m", QA).call(this, n);
    this._emit("chunk", n, r);
    for (const o of n.choices) {
      const s = r.choices[o.index];
      o.delta.content != null && s.message?.role === "assistant" && s.message?.content && (this._emit("content", o.delta.content, s.message.content), this._emit("content.delta", {
        delta: o.delta.content,
        snapshot: s.message.content,
        parsed: s.message.parsed
      })), o.delta.refusal != null && s.message?.role === "assistant" && s.message?.refusal && this._emit("refusal.delta", {
        delta: o.delta.refusal,
        snapshot: s.message.refusal
      }), o.logprobs?.content != null && s.message?.role === "assistant" && this._emit("logprobs.content.delta", {
        content: o.logprobs?.content,
        snapshot: s.logprobs?.content ?? []
      }), o.logprobs?.refusal != null && s.message?.role === "assistant" && this._emit("logprobs.refusal.delta", {
        refusal: o.logprobs?.refusal,
        snapshot: s.logprobs?.refusal ?? []
      });
      const a = Ae(this, Nn, "m", Oh).call(this, s);
      s.finish_reason && (Ae(this, Nn, "m", Dy).call(this, s), a.current_tool_call_index != null && Ae(this, Nn, "m", $y).call(this, s, a.current_tool_call_index));
      for (const i of o.delta.tool_calls ?? [])
        a.current_tool_call_index !== i.index && (Ae(this, Nn, "m", Dy).call(this, s), a.current_tool_call_index != null && Ae(this, Nn, "m", $y).call(this, s, a.current_tool_call_index)), a.current_tool_call_index = i.index;
      for (const i of o.delta.tool_calls ?? []) {
        const l = s.message.tool_calls?.[i.index];
        l?.type && (l?.type === "function" ? this._emit("tool_calls.function.arguments.delta", {
          name: l.function?.name,
          index: i.index,
          arguments: l.function.arguments,
          parsed_arguments: l.function.parsed_arguments,
          arguments_delta: i.function?.arguments ?? ""
        }) : (l?.type, void 0));
      }
    }
  }, $y = function(n, r) {
    if (Ae(this, Nn, "m", Oh).call(this, n).done_tool_calls.has(r)) return;
    const o = n.message.tool_calls?.[r];
    if (!o) throw new Error("no tool call snapshot");
    if (!o.type) throw new Error("tool call snapshot missing `type`");
    if (o.type === "function") {
      const s = Ae(this, xs, "f")?.tools?.find((a) => Tm(a) && a.function.name === o.function.name);
      this._emit("tool_calls.function.arguments.done", {
        name: o.function.name,
        index: r,
        arguments: o.function.arguments,
        parsed_arguments: yf(s) ? s.$parseRaw(o.function.arguments) : s?.function.strict ? JSON.parse(o.function.arguments) : null
      });
    } else o.type;
  }, Dy = function(n) {
    const r = Ae(this, Nn, "m", Oh).call(this, n);
    if (n.message.content && !r.content_done) {
      r.content_done = !0;
      const o = Ae(this, Nn, "m", Ly).call(this);
      this._emit("content.done", {
        content: n.message.content,
        parsed: o ? o.$parseRaw(n.message.content) : null
      });
    }
    n.message.refusal && !r.refusal_done && (r.refusal_done = !0, this._emit("refusal.done", { refusal: n.message.refusal })), n.logprobs?.content && !r.logprobs_content_done && (r.logprobs_content_done = !0, this._emit("logprobs.content.done", { content: n.logprobs.content })), n.logprobs?.refusal && !r.logprobs_refusal_done && (r.logprobs_refusal_done = !0, this._emit("logprobs.refusal.done", { refusal: n.logprobs.refusal }));
  }, Fh = function() {
    if (this.ended) throw new mt("stream has ended, this shouldn't happen");
    const n = Ae(this, ha, "f");
    if (!n) throw new mt("request ended without sending any chunks");
    return St(this, ha, void 0, "f"), St(this, kl, [], "f"), YY(n, Ae(this, xs, "f"));
  }, Ly = function() {
    const n = Ae(this, xs, "f")?.response_format;
    return Xb(n) ? n : null;
  }, QA = function(n) {
    var r, o, s, a;
    let i = Ae(this, ha, "f");
    const { choices: l, ...u } = n;
    i ? Object.assign(i, u) : i = St(this, ha, {
      ...u,
      choices: []
    }, "f");
    for (const { delta: d, finish_reason: f, index: m, logprobs: v = null, ...y } of n.choices) {
      let T = i.choices[m];
      if (T || (T = i.choices[m] = {
        finish_reason: f,
        index: m,
        message: {},
        logprobs: v,
        ...y
      }), v) if (!T.logprobs) T.logprobs = Object.assign({}, v);
      else {
        const { content: b, refusal: w, ...E } = v;
        Object.assign(T.logprobs, E), b && ((r = T.logprobs).content ?? (r.content = []), T.logprobs.content.push(...b)), w && ((o = T.logprobs).refusal ?? (o.refusal = []), T.logprobs.refusal.push(...w));
      }
      if (f && (T.finish_reason = f, Ae(this, xs, "f") && Q4(Ae(this, xs, "f")))) {
        if (f === "length") throw new I4();
        if (f === "content_filter") throw new x4();
      }
      if (Object.assign(T, y), !d) continue;
      const { content: C, refusal: A, function_call: I, role: R, tool_calls: $, ...p } = d;
      if (Object.assign(T.message, p), A && (T.message.refusal = (T.message.refusal || "") + A), R && (T.message.role = R), I && (T.message.function_call ? (I.name && (T.message.function_call.name = I.name), I.arguments && ((s = T.message.function_call).arguments ?? (s.arguments = ""), T.message.function_call.arguments += I.arguments)) : T.message.function_call = I), C && (T.message.content = (T.message.content || "") + C, !T.message.refusal && Ae(this, Nn, "m", Ly).call(this) && (T.message.parsed = XA(T.message.content))), $) {
        T.message.tool_calls || (T.message.tool_calls = []);
        for (const { index: b, id: w, type: E, function: x, ...M } of $) {
          const N = (a = T.message.tool_calls)[b] ?? (a[b] = {});
          Object.assign(N, M), w && (N.id = w), E && (N.type = E), x && (N.function ?? (N.function = {
            name: x.name ?? "",
            arguments: ""
          })), x?.name && (N.function.name = x.name), x?.arguments && (N.function.arguments += x.arguments, $Y(Ae(this, xs, "f"), N) && (N.function.parsed_arguments = XA(N.function.arguments)));
        }
      }
    }
    return i;
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("chunk", (o) => {
      const s = n.shift();
      s ? s.resolve(o) : t.push(o);
    }), this.on("end", () => {
      r = !0;
      for (const o of n) o.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (o) => {
      r = !0;
      for (const s of n) s.reject(o);
      n.length = 0;
    }), this.on("error", (o) => {
      r = !0;
      for (const s of n) s.reject(o);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((o, s) => n.push({
        resolve: o,
        reject: s
      })).then((o) => o ? {
        value: o,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new Vd(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
};
function YY(e, t) {
  const { id: n, choices: r, created: o, model: s, system_fingerprint: a, ...i } = e;
  return PY({
    ...i,
    id: n,
    choices: r.map(({ message: l, finish_reason: u, index: d, logprobs: f, ...m }) => {
      if (!u) throw new mt(`missing finish_reason for choice ${d}`);
      const { content: v = null, function_call: y, tool_calls: T, ...C } = l, A = l.role;
      if (!A) throw new mt(`missing role for choice ${d}`);
      if (y) {
        const { arguments: I, name: R } = y;
        if (I == null) throw new mt(`missing function_call.arguments for choice ${d}`);
        if (!R) throw new mt(`missing function_call.name for choice ${d}`);
        return {
          ...m,
          message: {
            content: v,
            function_call: {
              arguments: I,
              name: R
            },
            role: A,
            refusal: l.refusal ?? null
          },
          finish_reason: u,
          index: d,
          logprobs: f
        };
      }
      return T ? {
        ...m,
        index: d,
        finish_reason: u,
        logprobs: f,
        message: {
          ...C,
          role: A,
          content: v,
          refusal: l.refusal ?? null,
          tool_calls: T.map((I, R) => {
            const { function: $, type: p, id: b, ...w } = I, { arguments: E, name: x, ...M } = $ || {};
            if (b == null) throw new mt(`missing choices[${d}].tool_calls[${R}].id
${Bh(e)}`);
            if (p == null) throw new mt(`missing choices[${d}].tool_calls[${R}].type
${Bh(e)}`);
            if (x == null) throw new mt(`missing choices[${d}].tool_calls[${R}].function.name
${Bh(e)}`);
            if (E == null) throw new mt(`missing choices[${d}].tool_calls[${R}].function.arguments
${Bh(e)}`);
            return {
              ...w,
              id: b,
              type: p,
              function: {
                ...M,
                name: x,
                arguments: E
              }
            };
          })
        }
      } : {
        ...m,
        message: {
          ...C,
          content: v,
          role: A,
          refusal: l.refusal ?? null
        },
        finish_reason: u,
        index: d,
        logprobs: f
      };
    }),
    created: o,
    model: s,
    object: "chat.completion",
    ...a ? { system_fingerprint: a } : {}
  }, t);
}
function Bh(e) {
  return JSON.stringify(e);
}
var XY = class W1 extends cP {
  static fromReadableStream(t) {
    const n = new W1(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static runTools(t, n, r) {
    const o = new W1(n), s = {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "runTools"
      }
    };
    return o._run(() => o._runTools(t, n, s)), o;
  }
}, jb = class extends ft {
  constructor() {
    super(...arguments), this.messages = new X4(this._client);
  }
  create(e, t) {
    return this._client.post("/chat/completions", {
      body: e,
      ...t,
      stream: e.stream ?? !1
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/chat/completions/${e}`, t);
  }
  update(e, t, n) {
    return this._client.post(Fe`/chat/completions/${e}`, {
      body: t,
      ...n
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/chat/completions", bn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/chat/completions/${e}`, t);
  }
  parse(e, t) {
    return DY(e.tools), this._client.chat.completions.create(e, {
      ...t,
      headers: {
        ...t?.headers,
        "X-Stainless-Helper-Method": "chat.completions.parse"
      }
    })._thenUnwrap((n) => Qb(n, e));
  }
  runTools(e, t) {
    return e.stream ? XY.runTools(this._client, e, t) : FY.runTools(this._client, e, t);
  }
  stream(e, t) {
    return cP.createChatCompletion(this._client, e, t);
  }
};
jb.Messages = X4;
var ew = class extends ft {
  constructor() {
    super(...arguments), this.completions = new jb(this._client);
  }
};
ew.Completions = jb;
var dP = /* @__PURE__ */ Symbol("brand.privateNullableHeaders");
function* QY(e) {
  if (!e) return;
  if (dP in e) {
    const { values: r, nulls: o } = e;
    yield* r.entries();
    for (const s of o) yield [s, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : RA(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const o = r[0];
    if (typeof o != "string") throw new TypeError("expected header name to be a string");
    const s = RA(r[1]) ? r[1] : [r[1]];
    let a = !1;
    for (const i of s)
      i !== void 0 && (t && !a && (a = !0, yield [o, null]), yield [o, i]);
  }
}
var ct = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const o = /* @__PURE__ */ new Set();
    for (const [s, a] of QY(r)) {
      const i = s.toLowerCase();
      o.has(i) || (t.delete(s), o.add(i)), a === null ? (t.delete(s), n.add(i)) : (t.append(s, a), n.delete(i));
    }
  }
  return {
    [dP]: !0,
    values: t,
    nulls: n
  };
}, fP = class extends ft {
  create(e, t) {
    return this._client.post("/audio/speech", {
      body: e,
      ...t,
      headers: ct([{ Accept: "application/octet-stream" }, t?.headers]),
      __binaryResponse: !0
    });
  }
}, hP = class extends ft {
  create(e, t) {
    return this._client.post("/audio/transcriptions", ss({
      body: e,
      ...t,
      stream: e.stream ?? !1,
      __metadata: { model: e.model }
    }, this._client));
  }
}, pP = class extends ft {
  create(e, t) {
    return this._client.post("/audio/translations", ss({
      body: e,
      ...t,
      __metadata: { model: e.model }
    }, this._client));
  }
}, _f = class extends ft {
  constructor() {
    super(...arguments), this.transcriptions = new hP(this._client), this.translations = new pP(this._client), this.speech = new fP(this._client);
  }
};
_f.Transcriptions = hP;
_f.Translations = pP;
_f.Speech = fP;
var mP = class extends ft {
  create(e, t) {
    return this._client.post("/batches", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/batches/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/batches", bn, {
      query: e,
      ...t
    });
  }
  cancel(e, t) {
    return this._client.post(Fe`/batches/${e}/cancel`, t);
  }
}, gP = class extends ft {
  create(e, t) {
    return this._client.post("/assistants", {
      body: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/assistants/${e}`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  update(e, t, n) {
    return this._client.post(Fe`/assistants/${e}`, {
      body: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/assistants", bn, {
      query: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/assistants/${e}`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
}, vP = class extends ft {
  create(e, t) {
    return this._client.post("/realtime/sessions", {
      body: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
}, yP = class extends ft {
  create(e, t) {
    return this._client.post("/realtime/transcription_sessions", {
      body: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
}, kg = class extends ft {
  constructor() {
    super(...arguments), this.sessions = new vP(this._client), this.transcriptionSessions = new yP(this._client);
  }
};
kg.Sessions = vP;
kg.TranscriptionSessions = yP;
var _P = class extends ft {
  create(e, t) {
    return this._client.post("/chatkit/sessions", {
      body: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
  cancel(e, t) {
    return this._client.post(Fe`/chatkit/sessions/${e}/cancel`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
}, bP = class extends ft {
  retrieve(e, t) {
    return this._client.get(Fe`/chatkit/threads/${e}`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/chatkit/threads", Kd, {
      query: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/chatkit/threads/${e}`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
  listItems(e, t = {}, n) {
    return this._client.getAPIList(Fe`/chatkit/threads/${e}/items`, Kd, {
      query: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "chatkit_beta=v1" }, n?.headers])
    });
  }
}, Ig = class extends ft {
  constructor() {
    super(...arguments), this.sessions = new _P(this._client), this.threads = new bP(this._client);
  }
};
Ig.Sessions = _P;
Ig.Threads = bP;
var wP = class extends ft {
  create(e, t, n) {
    return this._client.post(Fe`/threads/${e}/messages`, {
      body: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { thread_id: r } = t;
    return this._client.get(Fe`/threads/${r}/messages/${e}`, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  update(e, t, n) {
    const { thread_id: r, ...o } = t;
    return this._client.post(Fe`/threads/${r}/messages/${e}`, {
      body: o,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/threads/${e}/messages`, bn, {
      query: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { thread_id: r } = t;
    return this._client.delete(Fe`/threads/${r}/messages/${e}`, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
}, SP = class extends ft {
  retrieve(e, t, n) {
    const { thread_id: r, run_id: o, ...s } = t;
    return this._client.get(Fe`/threads/${r}/runs/${o}/steps/${e}`, {
      query: s,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e, t, n) {
    const { thread_id: r, ...o } = t;
    return this._client.getAPIList(Fe`/threads/${r}/runs/${e}/steps`, bn, {
      query: o,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
}, ZY = (e) => {
  if (typeof Buffer < "u") {
    const t = Buffer.from(e, "base64");
    return Array.from(new Float32Array(t.buffer, t.byteOffset, t.length / Float32Array.BYTES_PER_ELEMENT));
  } else {
    const t = atob(e), n = t.length, r = new Uint8Array(n);
    for (let o = 0; o < n; o++) r[o] = t.charCodeAt(o);
    return Array.from(new Float32Array(r.buffer));
  }
}, Il = (e) => {
  if (typeof globalThis.process < "u") return globalThis.process.env?.[e]?.trim() ?? void 0;
  if (typeof globalThis.Deno < "u") return globalThis.Deno.env?.get?.(e)?.trim();
}, Yn, Mi, G1, es, yp, To, Pi, Kl, Ai, Im, Wr, _p, bp, vd, jc, ed, ZA, jA, eC, tC, nC, rC, oC, yd = class extends Zb {
  constructor() {
    super(...arguments), Yn.add(this), G1.set(this, []), es.set(this, {}), yp.set(this, {}), To.set(this, void 0), Pi.set(this, void 0), Kl.set(this, void 0), Ai.set(this, void 0), Im.set(this, void 0), Wr.set(this, void 0), _p.set(this, void 0), bp.set(this, void 0), vd.set(this, void 0);
  }
  [(G1 = /* @__PURE__ */ new WeakMap(), es = /* @__PURE__ */ new WeakMap(), yp = /* @__PURE__ */ new WeakMap(), To = /* @__PURE__ */ new WeakMap(), Pi = /* @__PURE__ */ new WeakMap(), Kl = /* @__PURE__ */ new WeakMap(), Ai = /* @__PURE__ */ new WeakMap(), Im = /* @__PURE__ */ new WeakMap(), Wr = /* @__PURE__ */ new WeakMap(), _p = /* @__PURE__ */ new WeakMap(), bp = /* @__PURE__ */ new WeakMap(), vd = /* @__PURE__ */ new WeakMap(), Yn = /* @__PURE__ */ new WeakSet(), Symbol.asyncIterator)]() {
    const e = [], t = [];
    let n = !1;
    return this.on("event", (r) => {
      const o = t.shift();
      o ? o.resolve(r) : e.push(r);
    }), this.on("end", () => {
      n = !0;
      for (const r of t) r.resolve(void 0);
      t.length = 0;
    }), this.on("abort", (r) => {
      n = !0;
      for (const o of t) o.reject(r);
      t.length = 0;
    }), this.on("error", (r) => {
      n = !0;
      for (const o of t) o.reject(r);
      t.length = 0;
    }), {
      next: async () => e.length ? {
        value: e.shift(),
        done: !1
      } : n ? {
        value: void 0,
        done: !0
      } : new Promise((r, o) => t.push({
        resolve: r,
        reject: o
      })).then((r) => r ? {
        value: r,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  static fromReadableStream(e) {
    const t = new Mi();
    return t._run(() => t._fromReadableStream(e)), t;
  }
  async _fromReadableStream(e, t) {
    const n = t?.signal;
    n && (n.aborted && this.controller.abort(), n.addEventListener("abort", () => this.controller.abort())), this._connected();
    const r = Vd.fromReadableStream(e, this.controller);
    for await (const o of r) Ae(this, Yn, "m", jc).call(this, o);
    if (r.controller.signal?.aborted) throw new go();
    return this._addRun(Ae(this, Yn, "m", ed).call(this));
  }
  toReadableStream() {
    return new Vd(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
  static createToolAssistantStream(e, t, n, r) {
    const o = new Mi();
    return o._run(() => o._runToolAssistantStream(e, t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), o;
  }
  async _createToolAssistantStream(e, t, n, r) {
    const o = r?.signal;
    o && (o.aborted && this.controller.abort(), o.addEventListener("abort", () => this.controller.abort()));
    const s = {
      ...n,
      stream: !0
    }, a = await e.submitToolOutputs(t, s, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const i of a) Ae(this, Yn, "m", jc).call(this, i);
    if (a.controller.signal?.aborted) throw new go();
    return this._addRun(Ae(this, Yn, "m", ed).call(this));
  }
  static createThreadAssistantStream(e, t, n) {
    const r = new Mi();
    return r._run(() => r._threadAssistantStream(e, t, {
      ...n,
      headers: {
        ...n?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), r;
  }
  static createAssistantStream(e, t, n, r) {
    const o = new Mi();
    return o._run(() => o._runAssistantStream(e, t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), o;
  }
  currentEvent() {
    return Ae(this, _p, "f");
  }
  currentRun() {
    return Ae(this, bp, "f");
  }
  currentMessageSnapshot() {
    return Ae(this, To, "f");
  }
  currentRunStepSnapshot() {
    return Ae(this, vd, "f");
  }
  async finalRunSteps() {
    return await this.done(), Object.values(Ae(this, es, "f"));
  }
  async finalMessages() {
    return await this.done(), Object.values(Ae(this, yp, "f"));
  }
  async finalRun() {
    if (await this.done(), !Ae(this, Pi, "f")) throw Error("Final run was not received.");
    return Ae(this, Pi, "f");
  }
  async _createThreadAssistantStream(e, t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort()));
    const o = {
      ...t,
      stream: !0
    }, s = await e.createAndRun(o, {
      ...n,
      signal: this.controller.signal
    });
    this._connected();
    for await (const a of s) Ae(this, Yn, "m", jc).call(this, a);
    if (s.controller.signal?.aborted) throw new go();
    return this._addRun(Ae(this, Yn, "m", ed).call(this));
  }
  async _createAssistantStream(e, t, n, r) {
    const o = r?.signal;
    o && (o.aborted && this.controller.abort(), o.addEventListener("abort", () => this.controller.abort()));
    const s = {
      ...n,
      stream: !0
    }, a = await e.create(t, s, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const i of a) Ae(this, Yn, "m", jc).call(this, i);
    if (a.controller.signal?.aborted) throw new go();
    return this._addRun(Ae(this, Yn, "m", ed).call(this));
  }
  static accumulateDelta(e, t) {
    for (const [n, r] of Object.entries(t)) {
      if (!e.hasOwnProperty(n)) {
        e[n] = r;
        continue;
      }
      let o = e[n];
      if (o == null) {
        e[n] = r;
        continue;
      }
      if (n === "index" || n === "type") {
        e[n] = r;
        continue;
      }
      if (typeof o == "string" && typeof r == "string") o += r;
      else if (typeof o == "number" && typeof r == "number") o += r;
      else if (xy(o) && xy(r)) o = this.accumulateDelta(o, r);
      else if (Array.isArray(o) && Array.isArray(r)) {
        if (o.every((s) => typeof s == "string" || typeof s == "number")) {
          o.push(...r);
          continue;
        }
        for (const s of r) {
          if (!xy(s)) throw new Error(`Expected array delta entry to be an object but got: ${s}`);
          const a = s.index;
          if (a == null)
            throw console.error(s), new Error("Expected array delta entry to have an `index` property");
          if (typeof a != "number") throw new Error(`Expected array delta entry \`index\` property to be a number but got ${a}`);
          const i = o[a];
          i == null ? o.push(s) : o[a] = this.accumulateDelta(i, s);
        }
        continue;
      } else throw Error(`Unhandled record type: ${n}, deltaValue: ${r}, accValue: ${o}`);
      e[n] = o;
    }
    return e;
  }
  _addRun(e) {
    return e;
  }
  async _threadAssistantStream(e, t, n) {
    return await this._createThreadAssistantStream(t, e, n);
  }
  async _runAssistantStream(e, t, n, r) {
    return await this._createAssistantStream(t, e, n, r);
  }
  async _runToolAssistantStream(e, t, n, r) {
    return await this._createToolAssistantStream(t, e, n, r);
  }
};
Mi = yd, jc = function(t) {
  if (!this.ended)
    switch (St(this, _p, t, "f"), Ae(this, Yn, "m", eC).call(this, t), t.event) {
      case "thread.created":
        break;
      case "thread.run.created":
      case "thread.run.queued":
      case "thread.run.in_progress":
      case "thread.run.requires_action":
      case "thread.run.completed":
      case "thread.run.incomplete":
      case "thread.run.failed":
      case "thread.run.cancelling":
      case "thread.run.cancelled":
      case "thread.run.expired":
        Ae(this, Yn, "m", oC).call(this, t);
        break;
      case "thread.run.step.created":
      case "thread.run.step.in_progress":
      case "thread.run.step.delta":
      case "thread.run.step.completed":
      case "thread.run.step.failed":
      case "thread.run.step.cancelled":
      case "thread.run.step.expired":
        Ae(this, Yn, "m", jA).call(this, t);
        break;
      case "thread.message.created":
      case "thread.message.in_progress":
      case "thread.message.delta":
      case "thread.message.completed":
      case "thread.message.incomplete":
        Ae(this, Yn, "m", ZA).call(this, t);
        break;
      case "error":
        throw new Error("Encountered an error event in event processing - errors should be processed earlier");
      default:
    }
}, ed = function() {
  if (this.ended) throw new mt("stream has ended, this shouldn't happen");
  if (!Ae(this, Pi, "f")) throw Error("Final run has not been received");
  return Ae(this, Pi, "f");
}, ZA = function(t) {
  const [n, r] = Ae(this, Yn, "m", nC).call(this, t, Ae(this, To, "f"));
  St(this, To, n, "f"), Ae(this, yp, "f")[n.id] = n;
  for (const o of r) {
    const s = n.content[o.index];
    s?.type == "text" && this._emit("textCreated", s.text);
  }
  switch (t.event) {
    case "thread.message.created":
      this._emit("messageCreated", t.data);
      break;
    case "thread.message.in_progress":
      break;
    case "thread.message.delta":
      if (this._emit("messageDelta", t.data.delta, n), t.data.delta.content) for (const o of t.data.delta.content) {
        if (o.type == "text" && o.text) {
          let s = o.text, a = n.content[o.index];
          if (a && a.type == "text") this._emit("textDelta", s, a.text);
          else throw Error("The snapshot associated with this text delta is not text or missing");
        }
        if (o.index != Ae(this, Kl, "f")) {
          if (Ae(this, Ai, "f")) switch (Ae(this, Ai, "f").type) {
            case "text":
              this._emit("textDone", Ae(this, Ai, "f").text, Ae(this, To, "f"));
              break;
            case "image_file":
              this._emit("imageFileDone", Ae(this, Ai, "f").image_file, Ae(this, To, "f"));
              break;
          }
          St(this, Kl, o.index, "f");
        }
        St(this, Ai, n.content[o.index], "f");
      }
      break;
    case "thread.message.completed":
    case "thread.message.incomplete":
      if (Ae(this, Kl, "f") !== void 0) {
        const o = t.data.content[Ae(this, Kl, "f")];
        if (o) switch (o.type) {
          case "image_file":
            this._emit("imageFileDone", o.image_file, Ae(this, To, "f"));
            break;
          case "text":
            this._emit("textDone", o.text, Ae(this, To, "f"));
            break;
        }
      }
      Ae(this, To, "f") && this._emit("messageDone", t.data), St(this, To, void 0, "f");
  }
}, jA = function(t) {
  const n = Ae(this, Yn, "m", tC).call(this, t);
  switch (St(this, vd, n, "f"), t.event) {
    case "thread.run.step.created":
      this._emit("runStepCreated", t.data);
      break;
    case "thread.run.step.delta":
      const r = t.data.delta;
      if (r.step_details && r.step_details.type == "tool_calls" && r.step_details.tool_calls && n.step_details.type == "tool_calls") for (const o of r.step_details.tool_calls) o.index == Ae(this, Im, "f") ? this._emit("toolCallDelta", o, n.step_details.tool_calls[o.index]) : (Ae(this, Wr, "f") && this._emit("toolCallDone", Ae(this, Wr, "f")), St(this, Im, o.index, "f"), St(this, Wr, n.step_details.tool_calls[o.index], "f"), Ae(this, Wr, "f") && this._emit("toolCallCreated", Ae(this, Wr, "f")));
      this._emit("runStepDelta", t.data.delta, n);
      break;
    case "thread.run.step.completed":
    case "thread.run.step.failed":
    case "thread.run.step.cancelled":
    case "thread.run.step.expired":
      St(this, vd, void 0, "f"), t.data.step_details.type == "tool_calls" && Ae(this, Wr, "f") && (this._emit("toolCallDone", Ae(this, Wr, "f")), St(this, Wr, void 0, "f")), this._emit("runStepDone", t.data, n);
      break;
    case "thread.run.step.in_progress":
      break;
  }
}, eC = function(t) {
  Ae(this, G1, "f").push(t), this._emit("event", t);
}, tC = function(t) {
  switch (t.event) {
    case "thread.run.step.created":
      return Ae(this, es, "f")[t.data.id] = t.data, t.data;
    case "thread.run.step.delta":
      let n = Ae(this, es, "f")[t.data.id];
      if (!n) throw Error("Received a RunStepDelta before creation of a snapshot");
      let r = t.data;
      if (r.delta) {
        const o = Mi.accumulateDelta(n, r.delta);
        Ae(this, es, "f")[t.data.id] = o;
      }
      return Ae(this, es, "f")[t.data.id];
    case "thread.run.step.completed":
    case "thread.run.step.failed":
    case "thread.run.step.cancelled":
    case "thread.run.step.expired":
    case "thread.run.step.in_progress":
      Ae(this, es, "f")[t.data.id] = t.data;
      break;
  }
  if (Ae(this, es, "f")[t.data.id]) return Ae(this, es, "f")[t.data.id];
  throw new Error("No snapshot available");
}, nC = function(t, n) {
  let r = [];
  switch (t.event) {
    case "thread.message.created":
      return [t.data, r];
    case "thread.message.delta":
      if (!n) throw Error("Received a delta with no existing snapshot (there should be one from message creation)");
      let o = t.data;
      if (o.delta.content) for (const s of o.delta.content) if (s.index in n.content) {
        let a = n.content[s.index];
        n.content[s.index] = Ae(this, Yn, "m", rC).call(this, s, a);
      } else
        n.content[s.index] = s, r.push(s);
      return [n, r];
    case "thread.message.in_progress":
    case "thread.message.completed":
    case "thread.message.incomplete":
      if (n) return [n, r];
      throw Error("Received thread message event with no existing snapshot");
  }
  throw Error("Tried to accumulate a non-message event");
}, rC = function(t, n) {
  return Mi.accumulateDelta(n, t);
}, oC = function(t) {
  switch (St(this, bp, t.data, "f"), t.event) {
    case "thread.run.created":
      break;
    case "thread.run.queued":
      break;
    case "thread.run.in_progress":
      break;
    case "thread.run.requires_action":
    case "thread.run.cancelled":
    case "thread.run.failed":
    case "thread.run.completed":
    case "thread.run.expired":
    case "thread.run.incomplete":
      St(this, Pi, t.data, "f"), Ae(this, Wr, "f") && (this._emit("toolCallDone", Ae(this, Wr, "f")), St(this, Wr, void 0, "f"));
      break;
    case "thread.run.cancelling":
      break;
  }
};
var tw = class extends ft {
  constructor() {
    super(...arguments), this.steps = new SP(this._client);
  }
  create(e, t, n) {
    const { include: r, ...o } = t;
    return this._client.post(Fe`/threads/${e}/runs`, {
      query: { include: r },
      body: o,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      stream: t.stream ?? !1,
      __synthesizeEventData: !0
    });
  }
  retrieve(e, t, n) {
    const { thread_id: r } = t;
    return this._client.get(Fe`/threads/${r}/runs/${e}`, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  update(e, t, n) {
    const { thread_id: r, ...o } = t;
    return this._client.post(Fe`/threads/${r}/runs/${e}`, {
      body: o,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/threads/${e}/runs`, bn, {
      query: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  cancel(e, t, n) {
    const { thread_id: r } = t;
    return this._client.post(Fe`/threads/${r}/runs/${e}/cancel`, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t, n);
    return await this.poll(r.id, { thread_id: e }, n);
  }
  createAndStream(e, t, n) {
    return yd.createAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
  async poll(e, t, n) {
    const r = ct([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const { data: o, response: s } = await this.retrieve(e, t, {
        ...n,
        headers: {
          ...n?.headers,
          ...r
        }
      }).withResponse();
      switch (o.status) {
        case "queued":
        case "in_progress":
        case "cancelling":
          let a = 5e3;
          if (n?.pollIntervalMs) a = n.pollIntervalMs;
          else {
            const i = s.headers.get("openai-poll-after-ms");
            if (i) {
              const l = parseInt(i);
              isNaN(l) || (a = l);
            }
          }
          await vf(a);
          break;
        case "requires_action":
        case "incomplete":
        case "cancelled":
        case "completed":
        case "failed":
        case "expired":
          return o;
      }
    }
  }
  stream(e, t, n) {
    return yd.createAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
  submitToolOutputs(e, t, n) {
    const { thread_id: r, ...o } = t;
    return this._client.post(Fe`/threads/${r}/runs/${e}/submit_tool_outputs`, {
      body: o,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      stream: t.stream ?? !1,
      __synthesizeEventData: !0
    });
  }
  async submitToolOutputsAndPoll(e, t, n) {
    const r = await this.submitToolOutputs(e, t, n);
    return await this.poll(r.id, t, n);
  }
  submitToolOutputsStream(e, t, n) {
    return yd.createToolAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
};
tw.Steps = SP;
var xg = class extends ft {
  constructor() {
    super(...arguments), this.runs = new tw(this._client), this.messages = new wP(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/threads", {
      body: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/threads/${e}`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  update(e, t, n) {
    return this._client.post(Fe`/threads/${e}`, {
      body: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/threads/${e}`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  createAndRun(e, t) {
    return this._client.post("/threads/runs", {
      body: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      stream: e.stream ?? !1,
      __synthesizeEventData: !0
    });
  }
  async createAndRunPoll(e, t) {
    const n = await this.createAndRun(e, t);
    return await this.runs.poll(n.id, { thread_id: n.thread_id }, t);
  }
  createAndRunStream(e, t) {
    return yd.createThreadAssistantStream(e, this._client.beta.threads, t);
  }
};
xg.Runs = tw;
xg.Messages = wP;
var Bu = class extends ft {
  constructor() {
    super(...arguments), this.realtime = new kg(this._client), this.chatkit = new Ig(this._client), this.assistants = new gP(this._client), this.threads = new xg(this._client);
  }
};
Bu.Realtime = kg;
Bu.ChatKit = Ig;
Bu.Assistants = gP;
Bu.Threads = xg;
var TP = class extends ft {
  create(e, t) {
    return this._client.post("/completions", {
      body: e,
      ...t,
      stream: e.stream ?? !1
    });
  }
}, EP = class extends ft {
  retrieve(e, t, n) {
    const { container_id: r } = t;
    return this._client.get(Fe`/containers/${r}/files/${e}/content`, {
      ...n,
      headers: ct([{ Accept: "application/binary" }, n?.headers]),
      __binaryResponse: !0
    });
  }
}, nw = class extends ft {
  constructor() {
    super(...arguments), this.content = new EP(this._client);
  }
  create(e, t, n) {
    return this._client.post(Fe`/containers/${e}/files`, Cg({
      body: t,
      ...n
    }, this._client));
  }
  retrieve(e, t, n) {
    const { container_id: r } = t;
    return this._client.get(Fe`/containers/${r}/files/${e}`, n);
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/containers/${e}/files`, bn, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { container_id: r } = t;
    return this._client.delete(Fe`/containers/${r}/files/${e}`, {
      ...n,
      headers: ct([{ Accept: "*/*" }, n?.headers])
    });
  }
};
nw.Content = EP;
var rw = class extends ft {
  constructor() {
    super(...arguments), this.files = new nw(this._client);
  }
  create(e, t) {
    return this._client.post("/containers", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/containers/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/containers", bn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/containers/${e}`, {
      ...t,
      headers: ct([{ Accept: "*/*" }, t?.headers])
    });
  }
};
rw.Files = nw;
var AP = class extends ft {
  create(e, t, n) {
    const { include: r, ...o } = t;
    return this._client.post(Fe`/conversations/${e}/items`, {
      query: { include: r },
      body: o,
      ...n
    });
  }
  retrieve(e, t, n) {
    const { conversation_id: r, ...o } = t;
    return this._client.get(Fe`/conversations/${r}/items/${e}`, {
      query: o,
      ...n
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/conversations/${e}/items`, Kd, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { conversation_id: r } = t;
    return this._client.delete(Fe`/conversations/${r}/items/${e}`, n);
  }
}, ow = class extends ft {
  constructor() {
    super(...arguments), this.items = new AP(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/conversations", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/conversations/${e}`, t);
  }
  update(e, t, n) {
    return this._client.post(Fe`/conversations/${e}`, {
      body: t,
      ...n
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/conversations/${e}`, t);
  }
};
ow.Items = AP;
var CP = class extends ft {
  create(e, t) {
    const n = !!e.encoding_format;
    let r = n ? e.encoding_format : "base64";
    n && Jn(this._client).debug("embeddings/user defined encoding_format:", e.encoding_format);
    const o = this._client.post("/embeddings", {
      body: {
        ...e,
        encoding_format: r
      },
      ...t
    });
    return n ? o : (Jn(this._client).debug("embeddings/decoding base64 embeddings from base64"), o._thenUnwrap((s) => (s && s.data && s.data.forEach((a) => {
      const i = a.embedding;
      a.embedding = ZY(i);
    }), s)));
  }
}, kP = class extends ft {
  retrieve(e, t, n) {
    const { eval_id: r, run_id: o } = t;
    return this._client.get(Fe`/evals/${r}/runs/${o}/output_items/${e}`, n);
  }
  list(e, t, n) {
    const { eval_id: r, ...o } = t;
    return this._client.getAPIList(Fe`/evals/${r}/runs/${e}/output_items`, bn, {
      query: o,
      ...n
    });
  }
}, sw = class extends ft {
  constructor() {
    super(...arguments), this.outputItems = new kP(this._client);
  }
  create(e, t, n) {
    return this._client.post(Fe`/evals/${e}/runs`, {
      body: t,
      ...n
    });
  }
  retrieve(e, t, n) {
    const { eval_id: r } = t;
    return this._client.get(Fe`/evals/${r}/runs/${e}`, n);
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/evals/${e}/runs`, bn, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { eval_id: r } = t;
    return this._client.delete(Fe`/evals/${r}/runs/${e}`, n);
  }
  cancel(e, t, n) {
    const { eval_id: r } = t;
    return this._client.post(Fe`/evals/${r}/runs/${e}`, n);
  }
};
sw.OutputItems = kP;
var aw = class extends ft {
  constructor() {
    super(...arguments), this.runs = new sw(this._client);
  }
  create(e, t) {
    return this._client.post("/evals", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/evals/${e}`, t);
  }
  update(e, t, n) {
    return this._client.post(Fe`/evals/${e}`, {
      body: t,
      ...n
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/evals", bn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/evals/${e}`, t);
  }
};
aw.Runs = sw;
var IP = class extends ft {
  create(e, t) {
    return this._client.post("/files", ss({
      body: e,
      ...t
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(Fe`/files/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/files", bn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/files/${e}`, t);
  }
  content(e, t) {
    return this._client.get(Fe`/files/${e}/content`, {
      ...t,
      headers: ct([{ Accept: "application/binary" }, t?.headers]),
      __binaryResponse: !0
    });
  }
  async waitForProcessing(e, { pollInterval: t = 5e3, maxWait: n = 1800 * 1e3 } = {}) {
    const r = /* @__PURE__ */ new Set([
      "processed",
      "error",
      "deleted"
    ]), o = Date.now();
    let s = await this.retrieve(e);
    for (; !s.status || !r.has(s.status); )
      if (await vf(t), s = await this.retrieve(e), Date.now() - o > n) throw new Vb({ message: `Giving up on waiting for file ${e} to finish processing after ${n} milliseconds.` });
    return s;
  }
}, xP = class extends ft {
}, MP = class extends ft {
  run(e, t) {
    return this._client.post("/fine_tuning/alpha/graders/run", {
      body: e,
      ...t
    });
  }
  validate(e, t) {
    return this._client.post("/fine_tuning/alpha/graders/validate", {
      body: e,
      ...t
    });
  }
}, iw = class extends ft {
  constructor() {
    super(...arguments), this.graders = new MP(this._client);
  }
};
iw.Graders = MP;
var PP = class extends ft {
  create(e, t, n) {
    return this._client.getAPIList(Fe`/fine_tuning/checkpoints/${e}/permissions`, Ag, {
      body: t,
      method: "post",
      ...n
    });
  }
  retrieve(e, t = {}, n) {
    return this._client.get(Fe`/fine_tuning/checkpoints/${e}/permissions`, {
      query: t,
      ...n
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/fine_tuning/checkpoints/${e}/permissions`, Kd, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { fine_tuned_model_checkpoint: r } = t;
    return this._client.delete(Fe`/fine_tuning/checkpoints/${r}/permissions/${e}`, n);
  }
}, lw = class extends ft {
  constructor() {
    super(...arguments), this.permissions = new PP(this._client);
  }
};
lw.Permissions = PP;
var RP = class extends ft {
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/fine_tuning/jobs/${e}/checkpoints`, bn, {
      query: t,
      ...n
    });
  }
}, uw = class extends ft {
  constructor() {
    super(...arguments), this.checkpoints = new RP(this._client);
  }
  create(e, t) {
    return this._client.post("/fine_tuning/jobs", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/fine_tuning/jobs/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/fine_tuning/jobs", bn, {
      query: e,
      ...t
    });
  }
  cancel(e, t) {
    return this._client.post(Fe`/fine_tuning/jobs/${e}/cancel`, t);
  }
  listEvents(e, t = {}, n) {
    return this._client.getAPIList(Fe`/fine_tuning/jobs/${e}/events`, bn, {
      query: t,
      ...n
    });
  }
  pause(e, t) {
    return this._client.post(Fe`/fine_tuning/jobs/${e}/pause`, t);
  }
  resume(e, t) {
    return this._client.post(Fe`/fine_tuning/jobs/${e}/resume`, t);
  }
};
uw.Checkpoints = RP;
var Uu = class extends ft {
  constructor() {
    super(...arguments), this.methods = new xP(this._client), this.jobs = new uw(this._client), this.checkpoints = new lw(this._client), this.alpha = new iw(this._client);
  }
};
Uu.Methods = xP;
Uu.Jobs = uw;
Uu.Checkpoints = lw;
Uu.Alpha = iw;
var NP = class extends ft {
}, cw = class extends ft {
  constructor() {
    super(...arguments), this.graderModels = new NP(this._client);
  }
};
cw.GraderModels = NP;
var $P = class extends ft {
  createVariation(e, t) {
    return this._client.post("/images/variations", ss({
      body: e,
      ...t
    }, this._client));
  }
  edit(e, t) {
    return this._client.post("/images/edits", ss({
      body: e,
      ...t,
      stream: e.stream ?? !1
    }, this._client));
  }
  generate(e, t) {
    return this._client.post("/images/generations", {
      body: e,
      ...t,
      stream: e.stream ?? !1
    });
  }
}, DP = class extends ft {
  retrieve(e, t) {
    return this._client.get(Fe`/models/${e}`, t);
  }
  list(e) {
    return this._client.getAPIList("/models", Ag, e);
  }
  delete(e, t) {
    return this._client.delete(Fe`/models/${e}`, t);
  }
}, LP = class extends ft {
  create(e, t) {
    return this._client.post("/moderations", {
      body: e,
      ...t
    });
  }
}, OP = class extends ft {
  accept(e, t, n) {
    return this._client.post(Fe`/realtime/calls/${e}/accept`, {
      body: t,
      ...n,
      headers: ct([{ Accept: "*/*" }, n?.headers])
    });
  }
  hangup(e, t) {
    return this._client.post(Fe`/realtime/calls/${e}/hangup`, {
      ...t,
      headers: ct([{ Accept: "*/*" }, t?.headers])
    });
  }
  refer(e, t, n) {
    return this._client.post(Fe`/realtime/calls/${e}/refer`, {
      body: t,
      ...n,
      headers: ct([{ Accept: "*/*" }, n?.headers])
    });
  }
  reject(e, t = {}, n) {
    return this._client.post(Fe`/realtime/calls/${e}/reject`, {
      body: t,
      ...n,
      headers: ct([{ Accept: "*/*" }, n?.headers])
    });
  }
}, FP = class extends ft {
  create(e, t) {
    return this._client.post("/realtime/client_secrets", {
      body: e,
      ...t
    });
  }
}, Mg = class extends ft {
  constructor() {
    super(...arguments), this.clientSecrets = new FP(this._client), this.calls = new OP(this._client);
  }
};
Mg.ClientSecrets = FP;
Mg.Calls = OP;
function jY(e, t) {
  return !t || !tX(t) ? {
    ...e,
    output_parsed: null,
    output: e.output.map((n) => n.type === "function_call" ? {
      ...n,
      parsed_arguments: null
    } : n.type === "message" ? {
      ...n,
      content: n.content.map((r) => ({
        ...r,
        parsed: null
      }))
    } : n)
  } : BP(e, t);
}
function BP(e, t) {
  const n = e.output.map((o) => {
    if (o.type === "function_call") return {
      ...o,
      parsed_arguments: oX(t, o)
    };
    if (o.type === "message") {
      const s = o.content.map((a) => a.type === "output_text" ? {
        ...a,
        parsed: eX(t, a.text)
      } : a);
      return {
        ...o,
        content: s
      };
    }
    return o;
  }), r = Object.assign({}, e, { output: n });
  return Object.getOwnPropertyDescriptor(e, "output_text") || V1(r), Object.defineProperty(r, "output_parsed", {
    enumerable: !0,
    get() {
      for (const o of r.output)
        if (o.type === "message") {
          for (const s of o.content) if (s.type === "output_text" && s.parsed !== null) return s.parsed;
        }
      return null;
    }
  }), r;
}
function eX(e, t) {
  return e.text?.format?.type !== "json_schema" ? null : "$parseRaw" in e.text?.format ? (e.text?.format).$parseRaw(t) : JSON.parse(t);
}
function tX(e) {
  return !!Xb(e.text?.format);
}
function nX(e) {
  return e?.$brand === "auto-parseable-tool";
}
function rX(e, t) {
  return e.find((n) => n.type === "function" && n.name === t);
}
function oX(e, t) {
  const n = rX(e.tools ?? [], t.name);
  return {
    ...t,
    ...t,
    parsed_arguments: nX(n) ? n.$parseRaw(t.arguments) : n?.strict ? JSON.parse(t.arguments) : null
  };
}
function V1(e) {
  const t = [];
  for (const n of e.output)
    if (n.type === "message")
      for (const r of n.content) r.type === "output_text" && t.push(r.text);
  e.output_text = t.join("");
}
var xl, Uh, pa, qh, sC, aC, iC, lC, sX = class UP extends Zb {
  constructor(t) {
    super(), xl.add(this), Uh.set(this, void 0), pa.set(this, void 0), qh.set(this, void 0), St(this, Uh, t, "f");
  }
  static createResponse(t, n, r) {
    const o = new UP(n);
    return o._run(() => o._createOrRetrieveResponse(t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), o;
  }
  async _createOrRetrieveResponse(t, n, r) {
    const o = r?.signal;
    o && (o.aborted && this.controller.abort(), o.addEventListener("abort", () => this.controller.abort())), Ae(this, xl, "m", sC).call(this);
    let s, a = null;
    "response_id" in n ? (s = await t.responses.retrieve(n.response_id, { stream: !0 }, {
      ...r,
      signal: this.controller.signal,
      stream: !0
    }), a = n.starting_after ?? null) : s = await t.responses.create({
      ...n,
      stream: !0
    }, {
      ...r,
      signal: this.controller.signal
    }), this._connected();
    for await (const i of s) Ae(this, xl, "m", aC).call(this, i, a);
    if (s.controller.signal?.aborted) throw new go();
    return Ae(this, xl, "m", iC).call(this);
  }
  [(Uh = /* @__PURE__ */ new WeakMap(), pa = /* @__PURE__ */ new WeakMap(), qh = /* @__PURE__ */ new WeakMap(), xl = /* @__PURE__ */ new WeakSet(), sC = function() {
    this.ended || St(this, pa, void 0, "f");
  }, aC = function(n, r) {
    if (this.ended) return;
    const o = (a, i) => {
      (r == null || i.sequence_number > r) && this._emit(a, i);
    }, s = Ae(this, xl, "m", lC).call(this, n);
    switch (o("event", n), n.type) {
      case "response.output_text.delta": {
        const a = s.output[n.output_index];
        if (!a) throw new mt(`missing output at index ${n.output_index}`);
        if (a.type === "message") {
          const i = a.content[n.content_index];
          if (!i) throw new mt(`missing content at index ${n.content_index}`);
          if (i.type !== "output_text") throw new mt(`expected content to be 'output_text', got ${i.type}`);
          o("response.output_text.delta", {
            ...n,
            snapshot: i.text
          });
        }
        break;
      }
      case "response.function_call_arguments.delta": {
        const a = s.output[n.output_index];
        if (!a) throw new mt(`missing output at index ${n.output_index}`);
        a.type === "function_call" && o("response.function_call_arguments.delta", {
          ...n,
          snapshot: a.arguments
        });
        break;
      }
      default:
        o(n.type, n);
        break;
    }
  }, iC = function() {
    if (this.ended) throw new mt("stream has ended, this shouldn't happen");
    const n = Ae(this, pa, "f");
    if (!n) throw new mt("request ended without sending any events");
    St(this, pa, void 0, "f");
    const r = aX(n, Ae(this, Uh, "f"));
    return St(this, qh, r, "f"), r;
  }, lC = function(n) {
    let r = Ae(this, pa, "f");
    if (!r) {
      if (n.type !== "response.created") throw new mt(`When snapshot hasn't been set yet, expected 'response.created' event, got ${n.type}`);
      return r = St(this, pa, n.response, "f"), r;
    }
    switch (n.type) {
      case "response.output_item.added":
        r.output.push(n.item);
        break;
      case "response.content_part.added": {
        const o = r.output[n.output_index];
        if (!o) throw new mt(`missing output at index ${n.output_index}`);
        const s = o.type, a = n.part;
        s === "message" && a.type !== "reasoning_text" ? o.content.push(a) : s === "reasoning" && a.type === "reasoning_text" && (o.content || (o.content = []), o.content.push(a));
        break;
      }
      case "response.output_text.delta": {
        const o = r.output[n.output_index];
        if (!o) throw new mt(`missing output at index ${n.output_index}`);
        if (o.type === "message") {
          const s = o.content[n.content_index];
          if (!s) throw new mt(`missing content at index ${n.content_index}`);
          if (s.type !== "output_text") throw new mt(`expected content to be 'output_text', got ${s.type}`);
          s.text += n.delta;
        }
        break;
      }
      case "response.function_call_arguments.delta": {
        const o = r.output[n.output_index];
        if (!o) throw new mt(`missing output at index ${n.output_index}`);
        o.type === "function_call" && (o.arguments += n.delta);
        break;
      }
      case "response.reasoning_text.delta": {
        const o = r.output[n.output_index];
        if (!o) throw new mt(`missing output at index ${n.output_index}`);
        if (o.type === "reasoning") {
          const s = o.content?.[n.content_index];
          if (!s) throw new mt(`missing content at index ${n.content_index}`);
          if (s.type !== "reasoning_text") throw new mt(`expected content to be 'reasoning_text', got ${s.type}`);
          s.text += n.delta;
        }
        break;
      }
      case "response.completed":
        St(this, pa, n.response, "f");
        break;
    }
    return r;
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("event", (o) => {
      const s = n.shift();
      s ? s.resolve(o) : t.push(o);
    }), this.on("end", () => {
      r = !0;
      for (const o of n) o.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (o) => {
      r = !0;
      for (const s of n) s.reject(o);
      n.length = 0;
    }), this.on("error", (o) => {
      r = !0;
      for (const s of n) s.reject(o);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((o, s) => n.push({
        resolve: o,
        reject: s
      })).then((o) => o ? {
        value: o,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  async finalResponse() {
    await this.done();
    const t = Ae(this, qh, "f");
    if (!t) throw new mt("stream ended without producing a ChatCompletion");
    return t;
  }
};
function aX(e, t) {
  return jY(e, t);
}
var qP = class extends ft {
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/responses/${e}/input_items`, bn, {
      query: t,
      ...n
    });
  }
}, HP = class extends ft {
  count(e = {}, t) {
    return this._client.post("/responses/input_tokens", {
      body: e,
      ...t
    });
  }
}, Pg = class extends ft {
  constructor() {
    super(...arguments), this.inputItems = new qP(this._client), this.inputTokens = new HP(this._client);
  }
  create(e, t) {
    return this._client.post("/responses", {
      body: e,
      ...t,
      stream: e.stream ?? !1
    })._thenUnwrap((n) => ("object" in n && n.object === "response" && V1(n), n));
  }
  retrieve(e, t = {}, n) {
    return this._client.get(Fe`/responses/${e}`, {
      query: t,
      ...n,
      stream: t?.stream ?? !1
    })._thenUnwrap((r) => ("object" in r && r.object === "response" && V1(r), r));
  }
  delete(e, t) {
    return this._client.delete(Fe`/responses/${e}`, {
      ...t,
      headers: ct([{ Accept: "*/*" }, t?.headers])
    });
  }
  parse(e, t) {
    return this._client.responses.create(e, t)._thenUnwrap((n) => BP(n, e));
  }
  stream(e, t) {
    return sX.createResponse(this._client, e, t);
  }
  cancel(e, t) {
    return this._client.post(Fe`/responses/${e}/cancel`, t);
  }
  compact(e, t) {
    return this._client.post("/responses/compact", {
      body: e,
      ...t
    });
  }
};
Pg.InputItems = qP;
Pg.InputTokens = HP;
var zP = class extends ft {
  retrieve(e, t) {
    return this._client.get(Fe`/skills/${e}/content`, {
      ...t,
      headers: ct([{ Accept: "application/binary" }, t?.headers]),
      __binaryResponse: !0
    });
  }
}, WP = class extends ft {
  retrieve(e, t, n) {
    const { skill_id: r } = t;
    return this._client.get(Fe`/skills/${r}/versions/${e}/content`, {
      ...n,
      headers: ct([{ Accept: "application/binary" }, n?.headers]),
      __binaryResponse: !0
    });
  }
}, dw = class extends ft {
  constructor() {
    super(...arguments), this.content = new WP(this._client);
  }
  create(e, t = {}, n) {
    return this._client.post(Fe`/skills/${e}/versions`, Cg({
      body: t,
      ...n
    }, this._client));
  }
  retrieve(e, t, n) {
    const { skill_id: r } = t;
    return this._client.get(Fe`/skills/${r}/versions/${e}`, n);
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/skills/${e}/versions`, bn, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { skill_id: r } = t;
    return this._client.delete(Fe`/skills/${r}/versions/${e}`, n);
  }
};
dw.Content = WP;
var Rg = class extends ft {
  constructor() {
    super(...arguments), this.content = new zP(this._client), this.versions = new dw(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/skills", Cg({
      body: e,
      ...t
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(Fe`/skills/${e}`, t);
  }
  update(e, t, n) {
    return this._client.post(Fe`/skills/${e}`, {
      body: t,
      ...n
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/skills", bn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/skills/${e}`, t);
  }
};
Rg.Content = zP;
Rg.Versions = dw;
var GP = class extends ft {
  create(e, t, n) {
    return this._client.post(Fe`/uploads/${e}/parts`, ss({
      body: t,
      ...n
    }, this._client));
  }
}, fw = class extends ft {
  constructor() {
    super(...arguments), this.parts = new GP(this._client);
  }
  create(e, t) {
    return this._client.post("/uploads", {
      body: e,
      ...t
    });
  }
  cancel(e, t) {
    return this._client.post(Fe`/uploads/${e}/cancel`, t);
  }
  complete(e, t, n) {
    return this._client.post(Fe`/uploads/${e}/complete`, {
      body: t,
      ...n
    });
  }
};
fw.Parts = GP;
var iX = async (e) => {
  const t = await Promise.allSettled(e), n = t.filter((o) => o.status === "rejected");
  if (n.length) {
    for (const o of n) console.error(o.reason);
    throw new Error(`${n.length} promise(s) failed - see the above errors`);
  }
  const r = [];
  for (const o of t) o.status === "fulfilled" && r.push(o.value);
  return r;
}, VP = class extends ft {
  create(e, t, n) {
    return this._client.post(Fe`/vector_stores/${e}/file_batches`, {
      body: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.get(Fe`/vector_stores/${r}/file_batches/${e}`, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  cancel(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.post(Fe`/vector_stores/${r}/file_batches/${e}/cancel`, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t);
    return await this.poll(e, r.id, n);
  }
  listFiles(e, t, n) {
    const { vector_store_id: r, ...o } = t;
    return this._client.getAPIList(Fe`/vector_stores/${r}/file_batches/${e}/files`, bn, {
      query: o,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  async poll(e, t, n) {
    const r = ct([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const { data: o, response: s } = await this.retrieve(t, { vector_store_id: e }, {
        ...n,
        headers: r
      }).withResponse();
      switch (o.status) {
        case "in_progress":
          let a = 5e3;
          if (n?.pollIntervalMs) a = n.pollIntervalMs;
          else {
            const i = s.headers.get("openai-poll-after-ms");
            if (i) {
              const l = parseInt(i);
              isNaN(l) || (a = l);
            }
          }
          await vf(a);
          break;
        case "failed":
        case "cancelled":
        case "completed":
          return o;
      }
    }
  }
  async uploadAndPoll(e, { files: t, fileIds: n = [] }, r) {
    if (t == null || t.length == 0) throw new Error("No `files` provided to process. If you've already uploaded files you should use `.createAndPoll()` instead");
    const o = r?.maxConcurrency ?? 5, s = Math.min(o, t.length), a = this._client, i = t.values(), l = [...n];
    async function u(d) {
      for (let f of d) {
        const m = await a.files.create({
          file: f,
          purpose: "assistants"
        }, r);
        l.push(m.id);
      }
    }
    return await iX(Array(s).fill(i).map(u)), await this.createAndPoll(e, { file_ids: l });
  }
}, KP = class extends ft {
  create(e, t, n) {
    return this._client.post(Fe`/vector_stores/${e}/files`, {
      body: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.get(Fe`/vector_stores/${r}/files/${e}`, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  update(e, t, n) {
    const { vector_store_id: r, ...o } = t;
    return this._client.post(Fe`/vector_stores/${r}/files/${e}`, {
      body: o,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Fe`/vector_stores/${e}/files`, bn, {
      query: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.delete(Fe`/vector_stores/${r}/files/${e}`, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t, n);
    return await this.poll(e, r.id, n);
  }
  async poll(e, t, n) {
    const r = ct([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const o = await this.retrieve(t, { vector_store_id: e }, {
        ...n,
        headers: r
      }).withResponse(), s = o.data;
      switch (s.status) {
        case "in_progress":
          let a = 5e3;
          if (n?.pollIntervalMs) a = n.pollIntervalMs;
          else {
            const i = o.response.headers.get("openai-poll-after-ms");
            if (i) {
              const l = parseInt(i);
              isNaN(l) || (a = l);
            }
          }
          await vf(a);
          break;
        case "failed":
        case "completed":
          return s;
      }
    }
  }
  async upload(e, t, n) {
    const r = await this._client.files.create({
      file: t,
      purpose: "assistants"
    }, n);
    return this.create(e, { file_id: r.id }, n);
  }
  async uploadAndPoll(e, t, n) {
    const r = await this.upload(e, t, n);
    return await this.poll(e, r.id, n);
  }
  content(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.getAPIList(Fe`/vector_stores/${r}/files/${e}/content`, Ag, {
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
}, Ng = class extends ft {
  constructor() {
    super(...arguments), this.files = new KP(this._client), this.fileBatches = new VP(this._client);
  }
  create(e, t) {
    return this._client.post("/vector_stores", {
      body: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  retrieve(e, t) {
    return this._client.get(Fe`/vector_stores/${e}`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  update(e, t, n) {
    return this._client.post(Fe`/vector_stores/${e}`, {
      body: t,
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/vector_stores", bn, {
      query: e,
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/vector_stores/${e}`, {
      ...t,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  search(e, t, n) {
    return this._client.getAPIList(Fe`/vector_stores/${e}/search`, Ag, {
      body: t,
      method: "post",
      ...n,
      headers: ct([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
};
Ng.Files = KP;
Ng.FileBatches = VP;
var JP = class extends ft {
  create(e, t) {
    return this._client.post("/videos", ss({
      body: e,
      ...t
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(Fe`/videos/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/videos", Kd, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Fe`/videos/${e}`, t);
  }
  createCharacter(e, t) {
    return this._client.post("/videos/characters", ss({
      body: e,
      ...t
    }, this._client));
  }
  downloadContent(e, t = {}, n) {
    return this._client.get(Fe`/videos/${e}/content`, {
      query: t,
      ...n,
      headers: ct([{ Accept: "application/binary" }, n?.headers]),
      __binaryResponse: !0
    });
  }
  edit(e, t) {
    return this._client.post("/videos/edits", ss({
      body: e,
      ...t
    }, this._client));
  }
  extend(e, t) {
    return this._client.post("/videos/extensions", ss({
      body: e,
      ...t
    }, this._client));
  }
  getCharacter(e, t) {
    return this._client.get(Fe`/videos/characters/${e}`, t);
  }
  remix(e, t, n) {
    return this._client.post(Fe`/videos/${e}/remix`, Cg({
      body: t,
      ...n
    }, this._client));
  }
}, ql, YP, wp, XP = class extends ft {
  constructor() {
    super(...arguments), ql.add(this);
  }
  async unwrap(e, t, n = this._client.webhookSecret, r = 300) {
    return await this.verifySignature(e, t, n, r), JSON.parse(e);
  }
  async verifySignature(e, t, n = this._client.webhookSecret, r = 300) {
    if (typeof crypto > "u" || typeof crypto.subtle.importKey != "function" || typeof crypto.subtle.verify != "function") throw new Error("Webhook signature verification is only supported when the `crypto` global is defined");
    Ae(this, ql, "m", YP).call(this, n);
    const o = ct([t]).values, s = Ae(this, ql, "m", wp).call(this, o, "webhook-signature"), a = Ae(this, ql, "m", wp).call(this, o, "webhook-timestamp"), i = Ae(this, ql, "m", wp).call(this, o, "webhook-id"), l = parseInt(a, 10);
    if (isNaN(l)) throw new Gc("Invalid webhook timestamp format");
    const u = Math.floor(Date.now() / 1e3);
    if (u - l > r) throw new Gc("Webhook timestamp is too old");
    if (l > u + r) throw new Gc("Webhook timestamp is too new");
    const d = s.split(" ").map((y) => y.startsWith("v1,") ? y.substring(3) : y), f = n.startsWith("whsec_") ? Buffer.from(n.replace("whsec_", ""), "base64") : Buffer.from(n, "utf-8"), m = i ? `${i}.${a}.${e}` : `${a}.${e}`, v = await crypto.subtle.importKey("raw", f, {
      name: "HMAC",
      hash: "SHA-256"
    }, !1, ["verify"]);
    for (const y of d) try {
      const T = Buffer.from(y, "base64");
      if (await crypto.subtle.verify("HMAC", v, T, new TextEncoder().encode(m))) return;
    } catch {
      continue;
    }
    throw new Gc("The given webhook signature does not match the expected signature");
  }
};
ql = /* @__PURE__ */ new WeakSet(), YP = function(t) {
  if (typeof t != "string" || t.length === 0) throw new Error("The webhook secret must either be set using the env var, OPENAI_WEBHOOK_SECRET, on the client class, OpenAI({ webhookSecret: '123' }), or passed to this function");
}, wp = function(t, n) {
  if (!t) throw new Error("Headers are required");
  const r = t.get(n);
  if (r == null) throw new Error(`Missing required header: ${n}`);
  return r;
};
var K1, hw, Sp, QP, Oy = "workload-identity-auth", Dt = class {
  constructor({ baseURL: e = Il("OPENAI_BASE_URL"), apiKey: t = Il("OPENAI_API_KEY"), organization: n = Il("OPENAI_ORG_ID") ?? null, project: r = Il("OPENAI_PROJECT_ID") ?? null, webhookSecret: o = Il("OPENAI_WEBHOOK_SECRET") ?? null, workloadIdentity: s, ...a } = {}) {
    if (K1.add(this), Sp.set(this, void 0), this.completions = new TP(this), this.chat = new ew(this), this.embeddings = new CP(this), this.files = new IP(this), this.images = new $P(this), this.audio = new _f(this), this.moderations = new LP(this), this.models = new DP(this), this.fineTuning = new Uu(this), this.graders = new cw(this), this.vectorStores = new Ng(this), this.webhooks = new XP(this), this.beta = new Bu(this), this.batches = new mP(this), this.uploads = new fw(this), this.responses = new Pg(this), this.realtime = new Mg(this), this.conversations = new ow(this), this.evals = new aw(this), this.containers = new rw(this), this.skills = new Rg(this), this.videos = new JP(this), s) {
      if (t && t !== Oy) throw new mt("The `apiKey` and `workloadIdentity` arguments are mutually exclusive; only one can be passed at a time.");
      t = Oy;
    } else if (t === void 0) throw new mt("Missing credentials. Please pass an `apiKey`, `workloadIdentity`, or set the `OPENAI_API_KEY` environment variable.");
    const i = {
      apiKey: t,
      organization: n,
      project: r,
      webhookSecret: o,
      workloadIdentity: s,
      ...a,
      baseURL: e || "https://api.openai.com/v1"
    };
    if (!i.dangerouslyAllowBrowser && eY()) throw new mt(`It looks like you're running in a browser-like environment.

This is disabled by default, as it risks exposing your secret API credentials to attackers.
If you understand the risks and have appropriate mitigations in place,
you can set the \`dangerouslyAllowBrowser\` option to \`true\`, e.g.,

new OpenAI({ apiKey, dangerouslyAllowBrowser: true });

https://help.openai.com/en/articles/5112595-best-practices-for-api-key-safety
`);
    this.baseURL = i.baseURL, this.timeout = i.timeout ?? hw.DEFAULT_TIMEOUT, this.logger = i.logger ?? console;
    const l = "warn";
    this.logLevel = l, this.logLevel = WA(i.logLevel, "ClientOptions.logLevel", this) ?? WA(Il("OPENAI_LOG"), "process.env['OPENAI_LOG']", this) ?? l, this.fetchOptions = i.fetchOptions, this.maxRetries = i.maxRetries ?? 2, this.fetch = i.fetch ?? R4(), St(this, Sp, sY, "f"), this._options = i, s && (this._workloadIdentityAuth = new TY(s, this.fetch)), this.apiKey = typeof t == "string" ? t : "Missing Key", this.organization = n, this.project = r, this.webhookSecret = o;
  }
  withOptions(e) {
    return new this.constructor({
      ...this._options,
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this.apiKey,
      workloadIdentity: this._options.workloadIdentity,
      organization: this.organization,
      project: this.project,
      webhookSecret: this.webhookSecret,
      ...e
    });
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: e, nulls: t }) {
  }
  async authHeaders(e) {
    return ct([{ Authorization: `Bearer ${this.apiKey}` }]);
  }
  stringifyQuery(e) {
    return dY(e);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${Bl}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${_4()}`;
  }
  makeStatusError(e, t, n, r) {
    return ir.generate(e, t, n, r);
  }
  async _callApiKey() {
    const e = this._options.apiKey;
    if (typeof e != "function") return !1;
    let t;
    try {
      t = await e();
    } catch (n) {
      throw n instanceof mt ? n : new mt(`Failed to get token from 'apiKey' function: ${n.message}`, { cause: n });
    }
    if (typeof t != "string" || !t) throw new mt(`Expected 'apiKey' function argument to return a string but it returned ${t}`);
    return this.apiKey = t, !0;
  }
  buildURL(e, t, n) {
    const r = !Ae(this, K1, "m", QP).call(this) && n || this.baseURL, o = XJ(e) ? new URL(e) : new URL(r + (r.endsWith("/") && e.startsWith("/") ? e.slice(1) : e)), s = this.defaultQuery(), a = Object.fromEntries(o.searchParams);
    return (!NA(s) || !NA(a)) && (t = {
      ...a,
      ...s,
      ...t
    }), typeof t == "object" && t && !Array.isArray(t) && (o.search = this.stringifyQuery(t)), o.toString();
  }
  async prepareOptions(e) {
    await this._callApiKey();
  }
  async prepareRequest(e, { url: t, options: n }) {
  }
  get(e, t) {
    return this.methodRequest("get", e, t);
  }
  post(e, t) {
    return this.methodRequest("post", e, t);
  }
  patch(e, t) {
    return this.methodRequest("patch", e, t);
  }
  put(e, t) {
    return this.methodRequest("put", e, t);
  }
  delete(e, t) {
    return this.methodRequest("delete", e, t);
  }
  methodRequest(e, t, n) {
    return this.request(Promise.resolve(n).then((r) => ({
      method: e,
      path: t,
      ...r
    })));
  }
  request(e, t = null) {
    return new z4(this, this.makeRequest(e, t, void 0));
  }
  async makeRequest(e, t, n) {
    const r = await e, o = r.maxRetries ?? this.maxRetries;
    t == null && (t = o), await this.prepareOptions(r);
    const { req: s, url: a, timeout: i } = await this.buildRequest(r, { retryCount: o - t });
    await this.prepareRequest(s, {
      url: a,
      options: r
    });
    const l = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), u = n === void 0 ? "" : `, retryOf: ${n}`, d = Date.now();
    if (Jn(this).debug(`[${l}] sending request`, bi({
      retryOfRequestLogID: n,
      method: r.method,
      url: a,
      options: r,
      headers: s.headers
    })), r.signal?.aborted) throw new go();
    const f = new AbortController(), m = await this.fetchWithAuth(a, s, i, f).catch(R1), v = Date.now();
    if (m instanceof globalThis.Error) {
      const T = `retrying, ${t} attempts remaining`;
      if (r.signal?.aborted) throw new go();
      const C = P1(m) || /timed? ?out/i.test(String(m) + ("cause" in m ? String(m.cause) : ""));
      if (t)
        return Jn(this).info(`[${l}] connection ${C ? "timed out" : "failed"} - ${T}`), Jn(this).debug(`[${l}] connection ${C ? "timed out" : "failed"} (${T})`, bi({
          retryOfRequestLogID: n,
          url: a,
          durationMs: v - d,
          message: m.message
        })), this.retryRequest(r, t, n ?? l);
      throw Jn(this).info(`[${l}] connection ${C ? "timed out" : "failed"} - error; no more retries left`), Jn(this).debug(`[${l}] connection ${C ? "timed out" : "failed"} (error; no more retries left)`, bi({
        retryOfRequestLogID: n,
        url: a,
        durationMs: v - d,
        message: m.message
      })), m instanceof M4 || m instanceof JJ ? m : C ? new Vb() : new Tg({ cause: m });
    }
    const y = `[${l}${u}${[...m.headers.entries()].filter(([T]) => T === "x-request-id").map(([T, C]) => ", " + T + ": " + JSON.stringify(C)).join("")}] ${s.method} ${a} ${m.ok ? "succeeded" : "failed"} with status ${m.status} in ${v - d}ms`;
    if (!m.ok) {
      if (m.status === 401 && this._workloadIdentityAuth && !r.__metadata?.hasStreamingBody && !r.__metadata?.workloadIdentityTokenRefreshed)
        return await OA(m.body), this._workloadIdentityAuth.invalidateToken(), this.makeRequest({
          ...r,
          __metadata: {
            ...r.__metadata,
            workloadIdentityTokenRefreshed: !0
          }
        }, t, n ?? l);
      const T = await this.shouldRetry(m);
      if (t && T) {
        const $ = `retrying, ${t} attempts remaining`;
        return await OA(m.body), Jn(this).info(`${y} - ${$}`), Jn(this).debug(`[${l}] response error (${$})`, bi({
          retryOfRequestLogID: n,
          url: m.url,
          status: m.status,
          headers: m.headers,
          durationMs: v - d
        })), this.retryRequest(r, t, n ?? l, m.headers);
      }
      const C = T ? "error; no more retries left" : "error; not retryable";
      Jn(this).info(`${y} - ${C}`);
      const A = await m.text().catch(($) => R1($).message), I = jJ(A), R = I ? void 0 : A;
      throw Jn(this).debug(`[${l}] response error (${C})`, bi({
        retryOfRequestLogID: n,
        url: m.url,
        status: m.status,
        headers: m.headers,
        message: R,
        durationMs: Date.now() - d
      })), this.makeStatusError(m.status, I, R, m.headers);
    }
    return Jn(this).info(y), Jn(this).debug(`[${l}] response start`, bi({
      retryOfRequestLogID: n,
      url: m.url,
      status: m.status,
      headers: m.headers,
      durationMs: v - d
    })), {
      response: m,
      options: r,
      controller: f,
      requestLogID: l,
      retryOfRequestLogID: n,
      startTime: d
    };
  }
  getAPIList(e, t, n) {
    return this.requestAPIList(t, n && "then" in n ? n.then((r) => ({
      method: "get",
      path: e,
      ...r
    })) : {
      method: "get",
      path: e,
      ...n
    });
  }
  requestAPIList(e, t) {
    const n = this.makeRequest(t, null, void 0);
    return new bY(this, n, e);
  }
  async fetchWithAuth(e, t, n, r) {
    if (this._workloadIdentityAuth) {
      const o = t.headers, s = o.get("Authorization");
      if (!s || s === `Bearer ${Oy}`) {
        const a = await this._workloadIdentityAuth.getToken();
        o.set("Authorization", `Bearer ${a}`);
      }
    }
    return await this.fetchWithTimeout(e, t, n, r);
  }
  async fetchWithTimeout(e, t, n, r) {
    const { signal: o, method: s, ...a } = t || {}, i = this._makeAbort(r);
    o && o.addEventListener("abort", i, { once: !0 });
    const l = setTimeout(i, n), u = globalThis.ReadableStream && a.body instanceof globalThis.ReadableStream || typeof a.body == "object" && a.body !== null && Symbol.asyncIterator in a.body, d = {
      signal: r.signal,
      ...u ? { duplex: "half" } : {},
      method: "GET",
      ...a
    };
    s && (d.method = s.toUpperCase());
    try {
      return await this.fetch.call(void 0, e, d);
    } finally {
      clearTimeout(l);
    }
  }
  async shouldRetry(e) {
    const t = e.headers.get("x-should-retry");
    return t === "true" ? !0 : t === "false" ? !1 : e.status === 408 || e.status === 409 || e.status === 429 || e.status >= 500;
  }
  async retryRequest(e, t, n, r) {
    let o;
    const s = r?.get("retry-after-ms");
    if (s) {
      const i = parseFloat(s);
      Number.isNaN(i) || (o = i);
    }
    const a = r?.get("retry-after");
    if (a && !o) {
      const i = parseFloat(a);
      Number.isNaN(i) ? o = Date.parse(a) - Date.now() : o = i * 1e3;
    }
    if (o === void 0) {
      const i = e.maxRetries ?? this.maxRetries;
      o = this.calculateDefaultRetryTimeoutMillis(t, i);
    }
    return await vf(o), this.makeRequest(e, t - 1, n);
  }
  calculateDefaultRetryTimeoutMillis(e, t) {
    const o = t - e;
    return Math.min(0.5 * Math.pow(2, o), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  async buildRequest(e, { retryCount: t = 0 } = {}) {
    const n = { ...e }, { method: r, path: o, query: s, defaultBaseURL: a } = n, i = this.buildURL(o, s, a);
    "timeout" in n && ZJ("timeout", n.timeout), n.timeout = n.timeout ?? this.timeout;
    const { bodyHeaders: l, body: u, isStreamingBody: d } = this.buildBody({ options: n });
    return d && (e.__metadata = {
      ...e.__metadata,
      hasStreamingBody: !0
    }), {
      req: {
        method: r,
        headers: await this.buildHeaders({
          options: e,
          method: r,
          bodyHeaders: l,
          retryCount: t
        }),
        ...n.signal && { signal: n.signal },
        ...globalThis.ReadableStream && u instanceof globalThis.ReadableStream && { duplex: "half" },
        ...u && { body: u },
        ...this.fetchOptions ?? {},
        ...n.fetchOptions ?? {}
      },
      url: i,
      timeout: n.timeout
    };
  }
  async buildHeaders({ options: e, method: t, bodyHeaders: n, retryCount: r }) {
    let o = {};
    this.idempotencyHeader && t !== "get" && (e.idempotencyKey || (e.idempotencyKey = this.defaultIdempotencyKey()), o[this.idempotencyHeader] = e.idempotencyKey);
    const s = ct([
      o,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent(),
        "X-Stainless-Retry-Count": String(r),
        ...e.timeout ? { "X-Stainless-Timeout": String(Math.trunc(e.timeout / 1e3)) } : {},
        ...oY(),
        "OpenAI-Organization": this.organization,
        "OpenAI-Project": this.project
      },
      await this.authHeaders(e),
      this._options.defaultHeaders,
      n,
      e.headers
    ]);
    return this.validateHeaders(s), s.values;
  }
  _makeAbort(e) {
    return () => e.abort();
  }
  buildBody({ options: { body: e, headers: t } }) {
    if (!e) return {
      bodyHeaders: void 0,
      body: void 0,
      isStreamingBody: !1
    };
    const n = ct([t]), r = typeof globalThis.ReadableStream < "u" && e instanceof globalThis.ReadableStream, o = !r && (typeof e == "string" || e instanceof ArrayBuffer || ArrayBuffer.isView(e) || typeof globalThis.Blob < "u" && e instanceof globalThis.Blob || e instanceof URLSearchParams || e instanceof FormData);
    return ArrayBuffer.isView(e) || e instanceof ArrayBuffer || e instanceof DataView || typeof e == "string" && n.values.has("content-type") || globalThis.Blob && e instanceof globalThis.Blob || e instanceof FormData || e instanceof URLSearchParams || r ? {
      bodyHeaders: void 0,
      body: e,
      isStreamingBody: !o
    } : typeof e == "object" && (Symbol.asyncIterator in e || Symbol.iterator in e && "next" in e && typeof e.next == "function") ? {
      bodyHeaders: void 0,
      body: $4(e),
      isStreamingBody: !0
    } : typeof e == "object" && n.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(e),
      isStreamingBody: !1
    } : {
      ...Ae(this, Sp, "f").call(this, {
        body: e,
        headers: n
      }),
      isStreamingBody: !1
    };
  }
};
hw = Dt, Sp = /* @__PURE__ */ new WeakMap(), K1 = /* @__PURE__ */ new WeakSet(), QP = function() {
  return this.baseURL !== "https://api.openai.com/v1";
};
Dt.OpenAI = hw;
Dt.DEFAULT_TIMEOUT = 6e5;
Dt.OpenAIError = mt;
Dt.APIError = ir;
Dt.APIConnectionError = Tg;
Dt.APIConnectionTimeoutError = Vb;
Dt.APIUserAbortError = go;
Dt.NotFoundError = T4;
Dt.ConflictError = E4;
Dt.RateLimitError = C4;
Dt.BadRequestError = b4;
Dt.AuthenticationError = w4;
Dt.InternalServerError = k4;
Dt.PermissionDeniedError = S4;
Dt.UnprocessableEntityError = A4;
Dt.InvalidWebhookSignatureError = Gc;
Dt.toFile = IY;
Dt.Completions = TP;
Dt.Chat = ew;
Dt.Embeddings = CP;
Dt.Files = IP;
Dt.Images = $P;
Dt.Audio = _f;
Dt.Moderations = LP;
Dt.Models = DP;
Dt.FineTuning = Uu;
Dt.Graders = cw;
Dt.VectorStores = Ng;
Dt.Webhooks = XP;
Dt.Beta = Bu;
Dt.Batches = mP;
Dt.Uploads = fw;
Dt.Responses = Pg;
Dt.Realtime = Mg;
Dt.Conversations = ow;
Dt.Evals = aw;
Dt.Containers = rw;
Dt.Skills = Rg;
Dt.Videos = JP;
function uC(e = "", t = 0) {
  let n = 0;
  for (let r = t - 1; r >= 0 && e[r] === "\\"; r -= 1) n += 1;
  return n % 2 === 1;
}
function lX(e = "") {
  return /^[0-9a-fA-F]{4}$/.test(e);
}
function uX(e = "") {
  return /^[dD][89a-bA-B][0-9a-fA-F]{2}$/.test(e);
}
function cX(e = "") {
  return /^[dD][c-fC-F][0-9a-fA-F]{2}$/.test(e);
}
function dX(e = "") {
  const t = String(e ?? "");
  let n = "", r = 0;
  for (; r < t.length; ) {
    const o = t.slice(r, r + 2), s = t.slice(r + 2, r + 6);
    if (o !== "\\u" || uC(t, r) || !lX(s)) {
      n += t[r] || "", r += 1;
      continue;
    }
    const a = r + 6, i = t.slice(a + 2, a + 6);
    if (uX(s) && t.slice(a, a + 2) === "\\u" && !uC(t, a) && cX(i)) {
      const l = Number.parseInt(s, 16), u = Number.parseInt(i, 16), d = 65536 + (l - 55296 << 10) + (u - 56320);
      n += String.fromCodePoint(d), r += 12;
      continue;
    }
    n += String.fromCharCode(Number.parseInt(s, 16)), r += 6;
  }
  return n;
}
function fX(e = "") {
  let t = String(e ?? "").trim();
  return t.endsWith(",") && (t = t.slice(0, -1).trimEnd()), t.startsWith('\\"') && (t = t.slice(2)), t.endsWith('\\"') && (t = t.slice(0, -2)), t.startsWith('"') && (t = t.slice(1)), t.endsWith('"') && (t = t.slice(0, -1)), dX(t.replace(/\r\n/g, `
`).replace(/\\r/g, "\r").replace(/\\n/g, `
`).replace(/\\t/g, "	").replace(/\\"/g, '"')).replace(/\\\\/g, "\\");
}
function hX(e = "") {
  return String(e || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function pw(e = "", t = "", n = 0) {
  const r = new RegExp(`(^|[^A-Za-z0-9_])(?:\\\\?")?${hX(t)}(?:\\\\?")?\\s*:`, "i"), o = String(e || "").slice(Math.max(0, n)).match(r);
  if (!o || o.index === void 0) return null;
  const s = o[1]?.length || 0;
  return {
    key: t,
    index: Math.max(0, n) + o.index + s,
    end: Math.max(0, n) + o.index + o[0].length
  };
}
function pX(e = "", t = [], n = 0) {
  return t.map((r) => pw(e, r, n)).filter(Boolean).sort((r, o) => r.index - o.index)[0] || null;
}
function ko(e = "", t = "", n = []) {
  const r = String(e || ""), o = pw(r, t);
  if (!o) return;
  let s = o.end;
  for (; /\s/.test(r[s] || ""); ) s += 1;
  r[s] === '"' && (s += 1);
  const a = pX(r, n.filter((u) => u !== t), s);
  let i = a ? a.index : r.length;
  if (a) {
    const u = r.lastIndexOf(",", a.index);
    u >= s && (i = u);
  }
  let l = r.slice(s, i).trim();
  return a || (l = l.replace(/\}\s*$/, "").trimEnd()), fX(l);
}
function Ps(e = "") {
  const t = String(e ?? "").trim();
  return /^-?\d+(?:\.\d+)?$/.test(t) ? Number(t) : /^true$/i.test(t) ? !0 : /^false$/i.test(t) ? !1 : /^null$/i.test(t) ? null : t;
}
var td = {
  Read: [
    "filePath",
    "path",
    "scope",
    "fromLine",
    "toLine",
    "tail",
    "offset",
    "limit",
    "outputMode",
    "contentFormat"
  ],
  Write: [
    "filePath",
    "path",
    "content"
  ],
  Edit: [
    "filePath",
    "path",
    "edits"
  ],
  Delete: ["filePath", "path"],
  Move: [
    "fromPath",
    "toPath",
    "filePath",
    "path"
  ],
  RenameBook: ["title", "name"],
  ImportMaterial: [
    "title",
    "content",
    "source"
  ],
  Glob: [
    "pattern",
    "path",
    "scope"
  ],
  Grep: [
    "pattern",
    "query",
    "path",
    "scope",
    "include",
    "outputMode",
    "limit",
    "offset",
    "contextLines",
    "useRegex"
  ],
  MapDocs: [
    "docType",
    "docId",
    "limit",
    "offset"
  ],
  MapInspect: [
    "docType",
    "docId",
    "mode",
    "elementId",
    "locationKey",
    "actorKey",
    "from",
    "to",
    "kind",
    "status",
    "query",
    "parent",
    "limit",
    "offset"
  ],
  MapPatch: [
    "docType",
    "docId",
    "expectedRevision",
    "activate",
    "dryRun",
    "ops"
  ],
  EventInspect: [
    "mode",
    "eventId",
    "id",
    "status",
    "limit",
    "offset"
  ],
  EventPatch: [
    "op",
    "eventId",
    "id",
    "fingerprint",
    "horizon",
    "current",
    "doneWhen",
    "hookForUser",
    "hookForModel",
    "status"
  ],
  MemoryRead: [
    "filePath",
    "path",
    "offset",
    "limit",
    "tail"
  ],
  MemoryWrite: [
    "filePath",
    "path",
    "content"
  ],
  MemoryEdit: [
    "filePath",
    "path",
    "edits"
  ],
  MemoryGrep: [
    "pattern",
    "query",
    "filePath",
    "path",
    "scope",
    "outputMode",
    "limit",
    "offset",
    "contextLines",
    "regex",
    "useRegex"
  ],
  ChatHistory: [
    "mode",
    "limit",
    "offset",
    "startOrder",
    "endOrder",
    "pattern",
    "query",
    "regex",
    "useRegex",
    "full"
  ],
  WebSearch: ["query", "maxResults"],
  DelegateRun: ["task"],
  PlanCreate: [
    "title",
    "details",
    "priority",
    "owner",
    "blockedBy"
  ],
  PlanUpdate: [
    "id",
    "status",
    "details",
    "priority",
    "owner",
    "blockedBy"
  ],
  PlanList: ["status"],
  apply_patch: ["patchText"]
}, mX = [
  "filePath",
  "path",
  "fromPath",
  "toPath",
  "content",
  "edits",
  "patchText",
  "query",
  "task",
  "title",
  "details",
  "pattern",
  "scope",
  "include",
  "status",
  "priority",
  "owner",
  "blockedBy",
  "fromLine",
  "toLine",
  "tail",
  "maxResults",
  "outputMode",
  "contentFormat",
  "limit",
  "offset",
  "contextLines",
  "useRegex",
  "regex",
  "mode",
  "docType",
  "docId",
  "expectedRevision",
  "activate",
  "dryRun",
  "ops",
  "op",
  "eventId",
  "fingerprint",
  "horizon",
  "current",
  "doneWhen",
  "hookForUser",
  "hookForModel",
  "startOrder",
  "endOrder",
  "full"
];
function cC(e = "", t = [], n = []) {
  for (const r of t) {
    const o = ko(e, r, n);
    if (o !== void 0) return o;
  }
}
function gX(e = "", t = "") {
  if (t === "Read") {
    const n = td.Read, r = {};
    return n.forEach((o, s) => {
      const a = ko(e, o, n.slice(s + 1));
      a !== void 0 && (r[o] = Ps(a));
    }), r.filePath === void 0 && r.path !== void 0 && (r.filePath = r.path, delete r.path), r.filePath === void 0 && r.scope !== void 0 && (r.filePath = r.scope, delete r.scope), Object.keys(r).length ? r : null;
  }
  if (t === "Write") {
    const n = {}, r = cC(e, ["filePath", "path"], ["content"]), o = ko(e, "content", []);
    return r !== void 0 && (n.filePath = Ps(r)), o !== void 0 && (n.content = Ps(o)), Object.keys(n).length ? n : null;
  }
  if (t === "Edit") {
    const n = {}, r = cC(e, ["filePath", "path"], ["edits"]), o = ko(e, "edits", []);
    return r !== void 0 && (n.filePath = Ps(r)), o !== void 0 && (n.edits = Ps(o)), Object.keys(n).length ? n : null;
  }
  if (t === "Grep") {
    const n = td.Grep, r = {};
    return n.forEach((o) => {
      const s = ko(e, o, n.filter((a) => a !== o));
      s !== void 0 && (r[o] = Ps(s));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.path === void 0 && r.scope !== void 0 && (r.path = r.scope), Object.keys(r).length ? r : null;
  }
  if (t === "MemoryGrep") {
    const n = td.MemoryGrep, r = {};
    return n.forEach((o) => {
      const s = ko(e, o, n.filter((a) => a !== o));
      s !== void 0 && (r[o] = Ps(s));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.path === void 0 && r.scope !== void 0 && (r.path = r.scope), r.regex === void 0 && r.useRegex !== void 0 && (r.regex = r.useRegex), Object.keys(r).length ? r : null;
  }
  if (t === "ChatHistory") {
    const n = td.ChatHistory, r = {};
    return n.forEach((o) => {
      const s = ko(e, o, n.filter((a) => a !== o));
      s !== void 0 && (r[o] = Ps(s));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.regex === void 0 && r.useRegex !== void 0 && (r.regex = r.useRegex), Object.keys(r).length ? r : null;
  }
  return null;
}
function vX(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) return null;
  try {
    const a = JSON.parse(n);
    if (a && typeof a == "object" && !Array.isArray(a)) return a;
  } catch {
  }
  const r = gX(n, t);
  if (r) return r;
  const o = td[t] || mX, s = {};
  return o.forEach((a, i) => {
    const l = ko(n, a, o.slice(i + 1));
    l !== void 0 && (s[a] = Ps(l));
  }), Object.keys(s).length ? s : null;
}
function ZP(e = "", t = "") {
  const n = vX(e, t);
  return n ? JSON.stringify(n) : "";
}
function jP(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function Ao(e, t, n) {
  const r = String(n || "").trim();
  r && e.push({
    label: t,
    text: r
  });
}
function Ir(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function lr(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function eR(e) {
  if (typeof e == "string") return e;
  if (e == null) return "{}";
  try {
    return JSON.stringify(e);
  } catch {
    return "{}";
  }
}
function tR(e, t = "") {
  if (e && typeof e == "object" && !Array.isArray(e)) return JSON.stringify(e);
  const n = typeof e == "string" ? e : eR(e);
  return ZP(n, t) || JSON.stringify(jP(n));
}
function yX(e = "") {
  const t = String(e || ""), n = pw(t, "arguments");
  if (!n) return "";
  let r = n.end;
  for (; /\s/.test(t[r] || ""); ) r += 1;
  const o = t[r] || "";
  return o === "{" ? t.slice(r).replace(/\}\s*$/, "").trimEnd() : o === '"' ? t.slice(r + 1).replace(/"\s*\}\s*$/, "").trimEnd() : t.slice(r).replace(/\}\s*$/, "").trimEnd();
}
function _X(e = "", t = 0) {
  const n = String(e || "").trim(), r = ko(n, "name", ["id", "arguments"]) || ko(n, "toolName", ["id", "arguments"]) || "", o = ko(n, "id", [
    "name",
    "toolName",
    "arguments"
  ]) || `tool-call-${t + 1}`, s = yX(n);
  return !r || !s ? null : {
    id: o,
    name: r,
    arguments: tR(s, r)
  };
}
function bX(e, t = 0, n = "openai-tool") {
  if (!lr(e)) return null;
  const r = lr(e.function) ? e.function : null, o = String(r?.name || "").trim();
  if (!o) return null;
  const s = Ir(e) || {};
  return delete s.index, s.id = String(s.id || `${n}-${t + 1}`), s.type = "function", s.function = {
    ...Ir(r) || {},
    name: o,
    arguments: eR(r.arguments)
  }, s;
}
function Qi(e = [], t = "openai-tool") {
  return (Array.isArray(e) ? e : []).map((n, r) => bX(n, r, t)).filter(Boolean);
}
function mw(e) {
  if (!lr(e)) return null;
  const t = Ir(e) || {};
  if (typeof t.content == "string" && /<tool_call\b/i.test(t.content) && (t.content = ki(Ci(t.content).cleaned)), Array.isArray(t.tool_calls)) {
    const n = Qi(t.tool_calls);
    n.length ? t.tool_calls = n : delete t.tool_calls;
  }
  return t;
}
function Ca(e = [], t = "openai-tool") {
  return Qi(e, t).map((n, r) => ({
    id: n.id || `${t}-${Date.now()}-${r + 1}`,
    name: n.function.name,
    arguments: n.function.arguments
  }));
}
function nR(e) {
  return typeof e == "string" ? e : Array.isArray(e) ? e.map((t) => t ? typeof t == "string" ? t : t.text || t.content || "" : "").filter(Boolean).join(`
`) : "";
}
function Ci(e = "") {
  const t = [];
  return {
    cleaned: String(e || "").replace(/<think>([\s\S]*?)<\/think>/gi, (n, r) => (Ao(t, "思考块", r), "")).trim(),
    thoughts: t
  };
}
function ki(e = "") {
  const t = String(e || ""), n = t.search(/<tool_call\b/i);
  return n < 0 ? t.trim() : t.slice(0, n).trim();
}
function J1(e = "") {
  const t = String(e || "");
  return /<tool_call\b/i.test(t) ? [{
    id: "tagged-json-draft",
    name: t.match(/["']?name["']?\s*:\s*["']([^"']+)/i)?.[1] || "工具调用",
    arguments: "{}",
    draft: !0
  }] : [];
}
function wi(e, t, n) {
  if (t) {
    if (typeof t == "string") {
      Ao(e, n, t);
      return;
    }
    if (Array.isArray(t)) {
      t.forEach((r) => wi(e, r, n));
      return;
    }
    typeof t == "object" && (typeof t.text == "string" && Ao(e, n, t.text), typeof t.content == "string" && Ao(e, n, t.content), typeof t.reasoning_content == "string" && Ao(e, n, t.reasoning_content), typeof t.thinking == "string" && Ao(e, n, t.thinking), Array.isArray(t.summary) && t.summary.forEach((r) => {
      if (typeof r == "string") {
        Ao(e, "推理摘要", r);
        return;
      }
      r && typeof r == "object" && Ao(e, "推理摘要", r.text || r.content || "");
    }));
  }
}
function ka(e = {}, t = {}) {
  const n = [];
  return wi(n, e.reasoning_content, "推理文本"), wi(n, e.reasoning, "推理文本"), wi(n, e.reasoning_text, "推理文本"), wi(n, e.thinking, "思考块"), wi(n, t.reasoning_content, "推理文本"), wi(n, t.reasoning, "推理文本"), Array.isArray(e.content) && e.content.forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "reasoning_text") {
        Ao(n, "推理文本", r.text);
        return;
      }
      if (r.type === "summary_text") {
        Ao(n, "推理摘要", r.text);
        return;
      }
      (r.type === "thinking" || r.type === "reasoning" || r.type === "reasoning_content") && Ao(n, "思考块", r.text || r.content || r.reasoning || "");
    }
  }), n;
}
function _d(e = "") {
  const t = [/<tool_call>\s*([\s\S]*?)\s*<\/tool_call>/g], n = [];
  return t.forEach((r) => {
    [...e.matchAll(r)].forEach((o, s) => {
      try {
        const a = JSON.parse(o[1]);
        n.push({
          id: a.id || `tool-call-${s + 1}`,
          name: String(a.name || ""),
          arguments: tR(a.arguments, a.name)
        });
      } catch {
        const a = _X(o[1], s);
        a && n.push(a);
      }
    });
  }), n.filter((r) => r.name);
}
function $g(e) {
  const t = e?.providerPayload?.openaiCompatibleMessage;
  return !t || typeof t != "object" || Array.isArray(t) ? null : mw(t);
}
function wX(e = []) {
  for (let t = e.length - 1; t >= 0; t -= 1) if (e[t]?.role === "user") return t;
  return -1;
}
function SX(e) {
  if (Qi(e?.tool_calls).length > 0) return !0;
  const t = $g(e);
  return Array.isArray(t?.tool_calls) && t.tool_calls.length > 0;
}
function TX(e = {}) {
  const t = Qi(e?.tool_calls);
  if (t.length) return t;
  const n = Qi($g(e)?.tool_calls);
  return n.length ? n : [];
}
function EX(e = {}) {
  return Qi(e?.tool_calls).length > 0;
}
function AX(e, t, n) {
  return e?.role !== "assistant" || t <= n ? !1 : SX(e);
}
function CX(e = "") {
  return /deepseek/i.test(String(e || ""));
}
function kX(e = "") {
  return /claude/i.test(String(e || ""));
}
function rR(e = [], t = "") {
  if (!kX(t)) return e;
  let n = -1;
  for (let o = e.length - 1; o >= 0; o -= 1) if (typeof e[o]?.role == "string") {
    n = o;
    break;
  }
  const r = e[n]?.role;
  return n < 0 || r === "user" || r !== "system" && r !== "assistant" ? e : e.map((o, s) => s === n ? {
    ...o,
    role: "user"
  } : o);
}
function dC(e, t = "") {
  return !lr(e) || !CX(t) || !Array.isArray(e.tool_calls) || !e.tool_calls.length || Object.prototype.hasOwnProperty.call(e, "reasoning_content") ? e : {
    ...e,
    reasoning_content: ""
  };
}
var fC = /* @__PURE__ */ new Set([
  "content",
  "refusal",
  "arguments",
  "reasoning_content",
  "reasoning_text",
  "thinking",
  "text"
]);
function IX(e = [], t = []) {
  const n = Array.isArray(e) ? e.map((r) => Ir(r) || {}) : [];
  return (Array.isArray(t) ? t : []).forEach((r, o) => {
    const s = Ir(r) || {}, a = Number.isInteger(Number(r?.index)) ? Number(r.index) : o, i = n[a];
    n[a] = lr(i) ? Zi(i, s, "tool_call") : s;
  }), n.filter((r) => r !== void 0);
}
function Zi(e, t, n = "") {
  if (t === void 0) return e;
  if (e === void 0) return Ir(t);
  if (t === null && fC.has(String(n || ""))) return e;
  if (n === "tool_calls" && Array.isArray(e) && Array.isArray(t)) return IX(e, t);
  if (typeof e == "string" && typeof t == "string")
    return fC.has(String(n || "")) ? e === t ? e : t.startsWith(e) ? t : e.startsWith(t) ? e : `${e}${t}` : e === t ? e : Ir(t);
  if (Array.isArray(e) && Array.isArray(t)) return e.concat(Ir(t) || []);
  if (lr(e) && lr(t)) {
    const r = { ...e };
    return Object.entries(t).forEach(([o, s]) => {
      r[o] = Zi(r[o], s, o);
    }), r;
  }
  return Ir(t);
}
function xm(e = {}, t = {}) {
  const n = lr(e) ? Ir(e) || {} : {}, r = lr(t) ? Ir(t) || {} : {};
  return delete r.message, delete r.finish_reason, delete r.index, delete r.logprobs, delete r.delta, Object.entries(r).forEach(([o, s]) => {
    n[o] = Zi(n[o], s, o);
  }), n.role || (n.role = "assistant"), mw(n) || { role: "assistant" };
}
function bd(e, t = {}) {
  const n = mw(xm(e, t));
  if (!(!n || typeof n != "object" || Array.isArray(n)))
    return { openaiCompatibleMessage: n };
}
function xX(e = {}, t = {}) {
  return lr(e) ? lr(t) ? Zi(Ir(e) || {}, t, "") : Ir(e) : Ir(t);
}
function Y1(e, t = "") {
  const n = Array.isArray(e.messages) ? e.messages : [], r = wX(n), o = n.map((a, i) => {
    const l = Qi(a?.tool_calls);
    if (AX(a, i, r)) {
      const d = $g(a);
      if (EX(d)) return dC({
        ...d,
        ...l.length ? { tool_calls: l } : {}
      }, t);
    }
    const u = {
      role: a.role,
      content: a.content
    };
    return a.role === "tool" && a.tool_call_id && (u.tool_call_id = a.tool_call_id), a.role === "assistant" && l.length && (u.tool_calls = l), dC(u, t);
  }), s = String(e.systemPrompt || "").trim();
  return s && o[0]?.role !== "system" && o.unshift({
    role: "system",
    content: s
  }), rR(o, t);
}
function hC(e) {
  const t = (e.tools || []).map((n) => [`- ${n.function.name}: ${n.function.description || ""}`.trim(), `  参数 JSON Schema: ${JSON.stringify(n.function.parameters || {})}`].join(`
`)).join(`
`);
  return [
    e.systemPrompt || "",
    "如果你需要调用工具，不要使用原生 tool calling 字段。",
    "用 <tool_call> 和 </tool_call> 明确 JSON 范围，请严格输出如下边界标记和包裹的 JSON，不要改写边界标记：",
    '<tool_call>{"name":"工具名","arguments":{...}}</tool_call>',
    "如果需要多个工具调用，可以连续输出多段 <tool_call> ... </tool_call>。",
    "在输出第一个 <tool_call> 之前，可根据任务复杂度决定是否需要先说明：简单查询可直接输出 <tool_call>；复杂任务可先简要说明你准备查什么或怎么查。",
    "一旦开始输出第一个 <tool_call>，就不要再继续输出面向用户的正文、解释、总结或补充；把本轮需要的 tool_call 连续输出完就结束。",
    t ? `可用工具:
${t}` : ""
  ].filter(Boolean).join(`

`);
}
function X1(e, t = "") {
  const n = /* @__PURE__ */ new Map(), r = [];
  return (Array.isArray(e.messages) ? e.messages : []).forEach((o) => {
    if (o.role === "assistant") {
      const s = TX(o);
      if (s.length) {
        const a = $g(o), i = typeof a?.content == "string" ? a.content : String(o.content || ""), l = s.map((u, d) => {
          const f = u.function?.name || "", m = u.id || `tool-call-${d + 1}`;
          return f && n.set(m, f), `<tool_call>${JSON.stringify({
            id: m,
            name: f,
            arguments: jP(u.function?.arguments || "{}")
          })}</tool_call>`;
        }).join(`
`);
        r.push({
          role: "assistant",
          content: [i, l].filter(Boolean).join(`

`)
        });
        return;
      }
    }
    if (o.role === "tool") {
      const s = String(o.toolName || o.tool_name || "").trim() || n.get(o.tool_call_id || "") || "unknown_tool";
      o.tool_call_id && n.delete(o.tool_call_id);
      const a = String(o.content || "");
      r.push({
        role: "user",
        content: [
          "<tool_result>",
          "这是系统工具执行结果，不是用户新发言。",
          `name: ${s}`,
          "content:",
          a,
          "</tool_result>"
        ].join(`
`)
      });
      return;
    }
    r.push({
      role: o.role,
      content: o.content
    });
  }), !r.length || r[0].role !== "system" ? r.unshift({
    role: "system",
    content: hC(e)
  }) : r[0] = {
    ...r[0],
    content: hC({
      ...e,
      systemPrompt: r[0].content || e.systemPrompt
    })
  }, rR(r, t);
}
function pC(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function mC(e, t, n) {
  !e || !t || n === void 0 || (e[t] = Zi(e[t], n, t));
}
function MX(e, t = []) {
  !Array.isArray(t) || !t.length || (Array.isArray(e.tool_calls) || (e.tool_calls = []), t.forEach((n) => {
    const r = Number(n?.index ?? 0), o = { ...e.tool_calls[r] || {} };
    Object.entries(n || {}).forEach(([s, a]) => {
      if (s !== "index" && !(s === "function" && a == null)) {
        if (s === "function" && lr(a)) {
          o.function = lr(o.function) ? { ...o.function } : {}, Object.entries(a).forEach(([i, l]) => {
            o.function[i] = Zi(o.function[i], l, i);
          });
          return;
        }
        o[s] = Zi(o[s], a, s);
      }
    }), e.tool_calls[r] = o;
  }));
}
function Q1(e, t = {}) {
  if (!e || !t || typeof t != "object") return;
  Object.entries(t).forEach(([r, o]) => {
    r === "delta" || r === "finish_reason" || r === "index" || r === "logprobs" || mC(e, r, o);
  });
  const n = lr(t.delta) ? t.delta : {};
  Object.entries(n).forEach(([r, o]) => {
    if (r === "tool_calls") {
      MX(e, o);
      return;
    }
    mC(e, r, o);
  });
}
function Z1(e, t = {}) {
  if (!e || !lr(t)) return;
  const n = Number(t.index ?? 0), r = e.toolCalls[n] || {
    id: "",
    type: "function",
    function: {
      name: "",
      arguments: ""
    }
  }, o = lr(t.function) ? t.function : {};
  e.toolCalls[n] = {
    ...r,
    id: t.id || r.id,
    type: t.type || r.type,
    function: {
      name: o.name || r.function?.name || "",
      arguments: `${r.function?.arguments || ""}${o.arguments || ""}`
    }
  };
}
async function PX(e, t) {
  const n = e.body?.getReader?.();
  if (!n) throw new Error("openai_compatible_stream_missing_body");
  const r = new TextDecoder();
  let o = "";
  const s = /\r?\n\r?\n/;
  for (; ; ) {
    const { done: i, value: l } = await n.read();
    if (i) break;
    for (o += r.decode(l, { stream: !0 }); ; ) {
      const u = o.match(s);
      if (!u || typeof u.index != "number") break;
      const d = u.index, f = o.slice(0, d);
      o = o.slice(d + u[0].length);
      const m = f.split(/\r?\n/).filter((v) => v.startsWith("data:")).map((v) => v.slice(5).trimStart()).join(`
`).trim();
      !m || m === "[DONE]" || t(JSON.parse(m));
    }
  }
  const a = o.trim();
  if (a && a !== "[DONE]") {
    const i = a.split(/\r?\n/).filter((l) => l.startsWith("data:")).map((l) => l.slice(5).trimStart()).join(`
`).trim();
    i && i !== "[DONE]" && t(JSON.parse(i));
  }
}
var RX = class {
  constructor(e) {
    this.config = e, this.client = new Dt({
      apiKey: e.apiKey,
      baseURL: String(e.baseUrl || "https://api.openai.com/v1").replace(/\/$/, ""),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = !t && Array.isArray(e.tools) && e.tools.length ? e.tools : null, r = {
      model: this.config.model,
      messages: t ? X1(e, this.config.model) : Y1(e, this.config.model),
      ...n ? {
        tools: n,
        tool_choice: e.toolChoice || "auto"
      } : {},
      ...e.maxTokens ? { max_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (r.temperature = e.temperature), e.reasoning?.enabled && (r.reasoning_effort = e.reasoning.effort), r;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = {
      ...t.body || this.buildRequestBody(e),
      ...n ? { stream: !0 } : {}
    }, o = String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "");
    return Gd({
      provider: "openai-compatible",
      model: this.config.model,
      transport: "openai-compatible",
      url: `${o}/chat/completions`,
      headers: {
        "Content-Type": "application/json",
        Authorization: this.config.apiKey ? `Bearer ${this.config.apiKey}` : ""
      },
      body: r,
      sdk: n ? "client.chat.completions.create(..., { stream: true })" : "client.chat.completions.create"
    });
  }
  async streamNativeChatCompletions(e, t) {
    const n = `${String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "")}/chat/completions`, r = await fetch(n, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${this.config.apiKey}`
      },
      body: JSON.stringify({
        ...t,
        stream: !0
      }),
      signal: e.signal
    });
    if (!r.ok) {
      const y = await r.text().catch(() => "");
      throw new Error(y || `openai_compatible_stream_http_${r.status}`);
    }
    const o = {
      content: "",
      toolCalls: []
    }, s = { role: "assistant" };
    let a = "stop", i = this.config.model;
    await PX(r, (y) => {
      i = y?.model || i;
      const T = y?.choices?.[0], C = T?.delta || {};
      Q1(s, T), T?.finish_reason && (a = T.finish_reason), typeof C.content == "string" && (o.content += C.content), Array.isArray(C.tool_calls) && C.tool_calls.forEach(($) => {
        Z1(o, $);
      });
      const A = Ci(o.content), I = o.toolCalls.filter(($) => $?.function?.name), R = I.length ? Ca(o.toolCalls) : J1(A.cleaned);
      pC(e, {
        text: I.length ? A.cleaned : ki(A.cleaned),
        thoughts: ka(s, T).concat(A.thoughts),
        ...R.length ? { toolCalls: R } : {},
        ...!I.length && R.length ? { toolCallDraft: !0 } : {}
      });
    });
    const l = bd(s), u = Ca(o.toolCalls), d = Ci(o.content), f = ka(s, {});
    d.thoughts.forEach((y) => f.push(y));
    const m = u.length ? [] : _d(d.cleaned), v = [...u, ...m];
    return {
      text: u.length ? d.cleaned : ki(d.cleaned),
      toolCalls: v,
      thoughts: f,
      finishReason: a,
      model: i,
      provider: "openai-compatible",
      providerPayload: l
    };
  }
  async chat(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = typeof e.onStreamProgress == "function", r = this.buildRequestBody(e), o = this.inspectRequest(e, { body: r });
    if (n) {
      if (!t) return {
        ...await this.streamNativeChatCompletions(e, r),
        requestInspection: o
      };
      const T = await this.client.chat.completions.create({
        ...r,
        stream: !0
      }, { signal: e.signal }), C = {
        content: "",
        toolCalls: []
      }, A = { role: "assistant" };
      let I = "stop", R = this.config.model, $;
      for await (const q of T) {
        R = q.model || R;
        const z = q.choices?.[0], J = z?.delta || {};
        Q1(A, z), z?.finish_reason && (I = z.finish_reason), typeof J.content == "string" && (C.content += J.content), Array.isArray(J.tool_calls) && J.tool_calls.forEach((X) => {
          Z1(C, X);
        });
        const G = Ci(C.content), oe = C.toolCalls.filter((X) => X?.function?.name), B = oe.length ? Ca(C.toolCalls) : J1(G.cleaned);
        pC(e, {
          text: oe.length ? G.cleaned : ki(G.cleaned),
          thoughts: ka(A, z).concat(G.thoughts),
          ...B.length ? { toolCalls: B } : {},
          ...!oe.length && B.length ? { toolCallDraft: !0 } : {}
        });
      }
      const p = (typeof T.finalChatCompletion == "function" ? await T.finalChatCompletion() : null)?.choices?.[0] || null, b = xX(A, xm(p?.message || A, p || {}));
      $ = bd(b);
      const w = Ca(C.toolCalls), E = Ci(C.content), x = ka(b, p || {});
      E.thoughts.forEach((q) => x.push(q));
      const M = w.length ? [] : _d(E.cleaned), N = [...w, ...M];
      return {
        text: w.length ? E.cleaned : ki(E.cleaned),
        toolCalls: N,
        thoughts: x,
        finishReason: I,
        model: R,
        provider: "openai-compatible",
        providerPayload: $,
        requestInspection: o
      };
    }
    const s = await this.client.chat.completions.create(r, { signal: e.signal }), a = s.choices?.[0] || {}, i = a.message || {}, l = ka(i, a), u = Ca(i.tool_calls || []), d = Ci(nR(i.content));
    d.thoughts.forEach((T) => l.push(T));
    const f = u.length ? [] : _d(d.cleaned), m = [...u, ...f], v = u.length ? d.cleaned : ki(d.cleaned), y = xm(i, a);
    return {
      text: v,
      toolCalls: m,
      thoughts: l,
      finishReason: a.finish_reason || "stop",
      model: s.model || this.config.model,
      provider: "openai-compatible",
      providerPayload: bd(y),
      requestInspection: o
    };
  }
};
function oR(e, t) {
  return {
    type: "message",
    role: e,
    content: NX(t)
  };
}
function Mm(e) {
  return {
    role: "assistant",
    content: typeof e == "string" ? e : ""
  };
}
function NX(e) {
  if (typeof e == "string") return [{
    type: "input_text",
    text: e
  }];
  if (!Array.isArray(e)) return [{
    type: "input_text",
    text: ""
  }];
  const t = e.map((n) => !n || typeof n != "object" ? null : n.type === "image_url" && n.image_url?.url ? {
    type: "input_image",
    image_url: n.image_url.url
  } : n.type === "text" ? {
    type: "input_text",
    text: n.text || ""
  } : null).filter(Boolean);
  return t.length ? t : [{
    type: "input_text",
    text: ""
  }];
}
function Pm(e, t, n) {
  const r = String(n || "").trim();
  r && e.push({
    label: t,
    text: r
  });
}
function gC(e, t = [], n = {}) {
  (t || []).forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "reasoning_text") {
        Pm(e, n.reasoning || "推理文本", r.text);
        return;
      }
      r.type === "summary_text" && Pm(e, n.summary || "推理摘要", r.text);
    }
  });
}
function $X(e = []) {
  const t = [];
  return (e || []).forEach((n) => {
    !n || typeof n != "object" || n.type === "reasoning" && (gC(t, n.content, {
      reasoning: "推理文本",
      summary: "推理摘要"
    }), gC(t, n.summary, {
      reasoning: "推理文本",
      summary: "推理摘要"
    }));
  }), t;
}
function DX(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  return t.length ? [...new Set(t)].join(`

`) : "";
}
function LX(e) {
  const t = e?.choices?.[0]?.message?.content;
  if (typeof t == "string" && t.trim()) return t.trim();
  if (typeof e?.output_text == "string" && e.output_text.trim()) return e.output_text.trim();
  const n = [];
  return (Array.isArray(e?.output) ? e.output : []).forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "message" && Array.isArray(r.content)) {
        r.content.forEach((o) => {
          if (!(!o || typeof o != "object")) {
            if (o.type === "output_text" && typeof o.text == "string" && o.text.trim()) {
              n.push(o.text.trim());
              return;
            }
            o.type === "refusal" && typeof o.refusal == "string" && o.refusal.trim() && n.push(o.refusal.trim());
          }
        });
        return;
      }
      typeof r.text == "string" && r.text.trim() && n.push(r.text.trim());
    }
  }), n.join(`
`).trim();
}
function OX(e) {
  const t = e?.choices?.[0], n = t?.message?.content, r = String(t?.finish_reason || "");
  if (typeof n != "string" || !n.trim()) return null;
  const o = n.toLowerCase();
  return !o.includes("proxy error") || !o.includes("/responses") && !r.toLowerCase().includes("proxy error") ? null : n.trim();
}
function FX(e) {
  const t = [];
  for (const n of e.messages || [])
    if (n.role !== "system") {
      if (n.role === "tool") {
        t.push({
          type: "function_call_output",
          call_id: n.tool_call_id || "missing_tool_call_id",
          output: n.content
        });
        continue;
      }
      if (n.role === "assistant" && Array.isArray(n.tool_calls) && n.tool_calls.length) {
        n.content?.trim() && t.push(Mm(n.content)), n.tool_calls.forEach((r, o) => {
          t.push({
            type: "function_call",
            call_id: r.id || `function_call_${o + 1}`,
            name: r.function?.name || "",
            arguments: r.function?.arguments || "{}",
            status: "completed"
          });
        });
        continue;
      }
      if (n.role === "assistant") {
        t.push(Mm(n.content || ""));
        continue;
      }
      t.push(n.role === "user" ? oR(n.role, n.content || "") : {
        role: n.role,
        content: typeof n.content == "string" ? n.content : ""
      });
    }
  return t;
}
function BX(e) {
  const t = [];
  for (const n of e.messages || []) {
    if (n.role === "system") {
      t.push({
        role: "system",
        content: typeof n.content == "string" ? n.content : ""
      });
      continue;
    }
    if (n.role === "tool") {
      t.push({
        type: "function_call_output",
        call_id: n.tool_call_id || "missing_tool_call_id",
        output: n.content
      });
      continue;
    }
    if (n.role === "assistant" && Array.isArray(n.tool_calls) && n.tool_calls.length) {
      n.content?.trim() && t.push(Mm(n.content)), n.tool_calls.forEach((r, o) => {
        t.push({
          type: "function_call",
          call_id: r.id || `function_call_${o + 1}`,
          name: r.function?.name || "",
          arguments: r.function?.arguments || "{}",
          status: "completed"
        });
      });
      continue;
    }
    if (n.role === "assistant") {
      t.push(Mm(n.content || ""));
      continue;
    }
    t.push(n.role === "user" ? oR(n.role, n.content || "") : {
      role: n.role,
      content: typeof n.content == "string" ? n.content : ""
    });
  }
  return t;
}
function UX(e) {
  try {
    return new URL(String(e || "https://api.openai.com/v1")).hostname === "api.openai.com";
  } catch {
    return !1;
  }
}
function qX(e) {
  const t = String(e?.message || e || "").toLowerCase();
  return t.includes("instructions") || t.includes("unsupported") || t.includes("unknown parameter") || t.includes("invalid input");
}
function HX(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {}
  });
}
function Fy(e, t) {
  const [n = "0", r = "0"] = String(e || "").split(":"), [o = "0", s = "0"] = String(t || "").split(":");
  return Number(n) - Number(o) || Number(r) - Number(s);
}
var zX = class {
  constructor(e) {
    this.config = e, this.client = new Dt({
      apiKey: e.apiKey,
      baseURL: String(e.baseUrl || "https://api.openai.com/v1").replace(/\/$/, ""),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e, t = !1) {
    const n = {
      model: this.config.model,
      instructions: t ? void 0 : DX(e) || void 0,
      input: t ? BX(e) : FX(e),
      ...Array.isArray(e.tools) && e.tools.length ? {
        tools: e.tools.map((r) => ({
          type: "function",
          name: r.function.name,
          description: r.function.description,
          parameters: r.function.parameters
        })),
        tool_choice: e.toolChoice || "auto"
      } : {},
      ...e.maxTokens ? { max_output_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (n.temperature = e.temperature), e.reasoning?.enabled && (n.reasoning = {
      effort: e.reasoning.effort,
      summary: "detailed"
    }), n;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = t.legacySystemInInput === !0, o = String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "");
    return Gd({
      provider: "openai-responses",
      model: this.config.model,
      transport: "openai-responses",
      url: `${o}/responses`,
      headers: {
        "Content-Type": "application/json",
        Authorization: this.config.apiKey ? `Bearer ${this.config.apiKey}` : ""
      },
      body: t.body || this.buildRequestBody(e, r),
      sdk: n ? "client.responses.stream" : "client.responses.create"
    });
  }
  async chat(e) {
    let t = this.inspectRequest(e);
    const n = (l) => {
      const u = OX(l);
      if (u) {
        const f = new Error(u);
        throw f.name = "ProxyEndpointError", f.rawDisplay = u, f;
      }
      const d = Array.isArray(l.output) ? l.output : [];
      return {
        output: d,
        thoughts: $X(d),
        toolCalls: d.filter((f) => f.type === "function_call" && f.name).map((f, m) => ({
          id: f.call_id || `response-tool-${m + 1}`,
          name: f.name || "",
          arguments: f.arguments || "{}"
        })),
        text: LX(l)
      };
    }, r = async (l = !1) => {
      const u = this.buildRequestBody(e, l);
      return t = this.inspectRequest(e, {
        body: u,
        legacySystemInInput: l
      }), await this.client.responses.create(u, { signal: e.signal });
    }, o = async (l = !1) => {
      const u = this.buildRequestBody(e, l);
      t = this.inspectRequest(e, {
        body: u,
        legacySystemInInput: l
      });
      const d = this.client.responses.stream(u, { signal: e.signal }), f = /* @__PURE__ */ new Map(), m = /* @__PURE__ */ new Map(), v = /* @__PURE__ */ new Map(), y = () => {
        const T = [];
        Array.from(m.entries()).sort(([C], [A]) => Fy(C, A)).forEach(([, C]) => Pm(T, "推理文本", C)), Array.from(v.entries()).sort(([C], [A]) => Fy(C, A)).forEach(([, C]) => Pm(T, "推理摘要", C)), HX(e, {
          text: Array.from(f.entries()).sort(([C], [A]) => Fy(C, A)).map(([, C]) => C).join(`
`).trim(),
          thoughts: T
        });
      };
      return d.on("response.output_text.delta", (T) => {
        const C = `${T.output_index}:${T.content_index}`;
        f.set(C, `${f.get(C) || ""}${T.delta}`), y();
      }), d.on("response.reasoning_text.delta", (T) => {
        const C = `${T.output_index}:${T.content_index}`;
        m.set(C, `${m.get(C) || ""}${T.delta}`), y();
      }), d.on("response.reasoning_summary_text.delta", (T) => {
        const C = `${T.output_index}:${T.summary_index}`;
        v.set(C, `${v.get(C) || ""}${T.delta}`), y();
      }), await d.finalResponse();
    }, s = !UX(this.config.baseUrl);
    let a, i;
    try {
      a = typeof e.onStreamProgress == "function" ? await o(!1) : await r(!1), i = n(a), s && !i.text && !i.toolCalls.length && (a = typeof e.onStreamProgress == "function" ? await o(!0) : await r(!0), i = n(a));
    } catch (l) {
      if (!s || !qX(l)) throw l;
      a = typeof e.onStreamProgress == "function" ? await o(!0) : await r(!0), i = n(a);
    }
    return {
      text: i.text,
      toolCalls: i.toolCalls,
      thoughts: i.thoughts,
      finishReason: a.incomplete_details?.reason || a.status || "stop",
      model: a.model || this.config.model,
      provider: "openai-responses",
      requestInspection: t
    };
  }
};
function ji(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function sR(e = "") {
  try {
    return {
      ok: !0,
      input: JSON.parse(String(e || ""))
    };
  } catch (t) {
    return {
      ok: !1,
      input: {},
      raw: String(e || ""),
      error: t instanceof Error ? t.message : String(t || "invalid_tool_input_json")
    };
  }
}
function WX(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = String(t?.function?.name || "").trim();
    if (!n) return null;
    const r = sR(t.function.arguments || "{}");
    return {
      type: "tool_use",
      id: String(t.id || n),
      name: n,
      input: r.input,
      ...r.ok ? {} : {
        invalidInputJson: r.raw,
        inputParseError: r.error
      }
    };
  }).filter(Boolean);
}
function GX(e = []) {
  const t = Array.isArray(e) ? ji(e) : null;
  return Array.isArray(t) && t.length ? t : null;
}
function VX(e = {}) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  return t.forEach((r) => {
    if (!r || typeof r != "object") return;
    const o = ji(r) || {}, s = GX(o?.providerPayload?.anthropicContent), a = WX(o.tool_calls);
    delete o.providerPayload, o.role === "assistant" && s && a.length ? (delete o.tool_calls, o.content = s.filter((i) => i?.type !== "tool_use").concat(a)) : o.role === "assistant" && s && (delete o.tool_calls, o.content = s), n.push(o);
  }), n;
}
function KX(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    if (!t || typeof t != "object") return null;
    if (t.type === "text") return {
      type: "text",
      text: String(t.text || "")
    };
    if (t.type === "tool_use" && t.name) {
      if (t.inputJson !== void 0) {
        const r = sR(t.inputJson);
        return {
          type: "tool_use",
          id: String(t.id || t.name),
          name: String(t.name),
          input: r.input,
          ...r.ok ? {} : {
            invalidInputJson: r.raw,
            inputParseError: r.error
          }
        };
      }
      const n = ji(t.input);
      return n !== void 0 ? {
        type: "tool_use",
        id: String(t.id || t.name),
        name: String(t.name),
        input: n
      } : {
        type: "tool_use",
        id: String(t.id || t.name),
        name: String(t.name),
        input: {}
      };
    }
    return t.type === "thinking" ? {
      type: "thinking",
      thinking: String(t.thinking || t.text || "")
    } : t.type === "redacted_thinking" ? {
      type: "redacted_thinking",
      data: String(t.data || "")
    } : ji(t) || null;
  }).filter(Boolean);
}
function JX(e = []) {
  return e.map((t) => !t || typeof t != "object" ? null : t.type === "tool_use" && t.name ? {
    type: "tool_use",
    id: t.id,
    name: t.name,
    input: ji(t.input) || {}
  } : ji(t) || null).filter(Boolean);
}
function YX(e = []) {
  const t = Array.isArray(e) ? e : [], n = t.filter((s) => s?.type === "text").map((s) => s.text || "").join(`
`), r = t.filter((s) => s?.type === "thinking" || s?.type === "redacted_thinking").map((s) => ({
    label: s.type === "thinking" ? "思考块" : "已脱敏思考块",
    text: s.type === "thinking" ? s.thinking || "" : s.data || ""
  })).filter((s) => s.text), o = t.filter((s) => s?.type === "tool_use" && s.name).map((s, a) => ({
    id: s.id || `st-claude-tool-${a + 1}`,
    name: s.name,
    arguments: s.inputJson !== void 0 ? s.inputJson : JSON.stringify(s.input || {})
  }));
  return {
    text: n,
    thoughts: r,
    ...o.length ? {
      toolCalls: o,
      toolCallDraft: !0
    } : {}
  };
}
function aR(e = [], t = {}) {
  const n = KX(e), r = n.filter((o) => o.type === "tool_use" && o.name).map((o, s) => ({
    id: o.id || `st-claude-tool-${s + 1}`,
    name: o.name,
    arguments: o.invalidInputJson !== void 0 ? o.invalidInputJson : JSON.stringify(o.input || {})
  }));
  return {
    text: n.filter((o) => o.type === "text").map((o) => o.text || "").join(`
`),
    toolCalls: r,
    thoughts: n.filter((o) => o.type === "thinking" || o.type === "redacted_thinking").map((o) => ({
      label: o.type === "thinking" ? "思考块" : "已脱敏思考块",
      text: o.type === "thinking" ? o.thinking || "" : o.data || ""
    })).filter((o) => o.text),
    finishReason: t.finishReason || "stop",
    model: t.model || "",
    provider: "sillytavern-claude",
    providerPayload: n.length ? { anthropicContent: JX(n) } : void 0
  };
}
function XX(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function QX(e, t = {}) {
  const n = [];
  let r = "stop", o = t.model || "";
  const s = (i, l = {}) => {
    const u = Number.isInteger(Number(i)) ? Number(i) : n.length;
    return n[u] ? n[u] = {
      ...n[u],
      ...l
    } : n[u] = { ...l }, n[u];
  }, a = () => {
    const i = YX(n);
    XX(e, {
      text: i.text,
      thoughts: i.thoughts,
      ...Array.isArray(i.toolCalls) ? { toolCalls: i.toolCalls } : {},
      ...i.toolCallDraft ? { toolCallDraft: !0 } : {}
    });
  };
  return {
    accept(i = {}) {
      if (i?.message?.model && (o = i.message.model), i.type === "content_block_start") {
        s(i.index, ji(i.content_block) || {}), a();
        return;
      }
      if (i.type === "content_block_delta") {
        const l = s(i.index), u = i.delta || {};
        u.type === "text_delta" ? (l.type = l.type || "text", l.text = `${l.text || ""}${u.text || ""}`) : u.type === "input_json_delta" ? (l.type = l.type || "tool_use", l.inputJson = `${l.inputJson || ""}${u.partial_json || ""}`) : u.type === "thinking_delta" ? (l.type = l.type || "thinking", l.thinking = `${l.thinking || ""}${u.thinking || ""}`) : u.type === "signature_delta" && (l.signature = `${l.signature || ""}${u.signature || ""}`), a();
        return;
      }
      i.type === "message_delta" && (r = i.delta?.stop_reason || r);
    },
    result() {
      return aR(n, {
        finishReason: r,
        model: o
      });
    }
  };
}
var ZX = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return VX(e);
  }
  buildPayload(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildMessages(e);
    return P9(this.config, e, n, t);
  }
  async inspectRequest(e, t = {}) {
    const n = await nf(t.payload || this.buildPayload(e), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-claude",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: wu(e)
    };
  }
  async chat(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildPayload(e);
    let r = null;
    const o = (s) => {
      r = this.buildRequestInspection(s);
    };
    try {
      if (t) {
        const a = QX(e, this.config);
        return await L0(n, (i) => {
          a.accept(i);
        }, {
          signal: e.signal,
          onRequest: o
        }), {
          ...a.result(),
          requestInspection: r
        };
      }
      const s = await D0(n, {
        signal: e.signal,
        onRequest: o
      });
      return {
        ...aR(Array.isArray(s?.content) ? s.content : [{
          type: "text",
          text: s?.choices?.[0]?.message?.content || ""
        }], {
          finishReason: s?.stop_reason || s?.choices?.[0]?.finish_reason || "stop",
          model: s?.model || this.config.model
        }),
        requestInspection: r
      };
    } catch (s) {
      throw r && s && typeof s == "object" && (s.requestInspection = r), s;
    }
  }
};
function gw(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function Su(e) {
  if (typeof e == "string") return {
    role: "model",
    parts: e ? [{ text: e }] : []
  };
  if (!e || typeof e != "object") return {
    role: "model",
    parts: []
  };
  const t = gw(e) || {};
  return t.role = t.role || "model", t.parts = Array.isArray(t.parts) ? t.parts : [], t;
}
function jX(e) {
  const t = Array.isArray(e?.providerPayload?.googleContents) ? e.providerPayload.googleContents : [];
  if (t.length) return t.map((o) => Su(o)).filter((o) => Array.isArray(o.parts) && o.parts.length);
  const n = e?.providerPayload?.googleContent, r = Su(n);
  return r.parts.length ? [r] : [];
}
function eQ(e = {}) {
  const t = String(e?.mimeType || "").trim(), n = String(e?.data || "").trim();
  if (!t || !n) return null;
  const r = `data:${t};base64,${n}`;
  return t.startsWith("image/") ? {
    type: "image_url",
    image_url: { url: r }
  } : t.startsWith("video/") ? {
    type: "video_url",
    video_url: { url: r }
  } : t.startsWith("audio/") ? {
    type: "audio_url",
    audio_url: { url: r }
  } : null;
}
function tQ(e = {}, t = 0) {
  const n = Su(e);
  if (!n.parts.length) return null;
  const r = {
    role: n.role === "user" ? "user" : "assistant",
    content: []
  }, o = n.parts.find((a) => !a?.thought && typeof a?.text == "string" && typeof a?.thoughtSignature == "string" && a.thoughtSignature)?.thoughtSignature || "", s = [];
  return n.parts.forEach((a) => {
    if (!a || typeof a != "object") return;
    if (!a.thought && typeof a.text == "string" && a.text) {
      r.content.push({
        type: "text",
        text: a.text
      });
      return;
    }
    if (a.functionCall?.name) {
      s.push({
        id: String(a.functionCall.id || `st-google-tool-${t + 1}-${s.length + 1}`),
        type: "function",
        function: {
          name: String(a.functionCall.name || ""),
          arguments: JSON.stringify(a.functionCall.args || {})
        },
        ...typeof a.thoughtSignature == "string" && a.thoughtSignature ? { signature: a.thoughtSignature } : {}
      });
      return;
    }
    const i = eQ(a.inlineData);
    i && r.content.push(i);
  }), s.length && r.content.push({
    type: "tool_calls",
    tool_calls: s
  }), o && r.content.some((a) => a?.type === "text") && (r.signature = o), r.content.length ? r : null;
}
function nQ(e = {}) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  return t.forEach((r) => {
    if (!r || typeof r != "object") return;
    const o = jX(r);
    if (r.role === "assistant" && o.length) {
      o.forEach((a, i) => {
        const l = tQ(a, i);
        l && n.push(l);
      });
      return;
    }
    const s = gw(r) || {};
    delete s.providerPayload, n.push(s);
  }), n;
}
function iR(e = {}) {
  return Su(e?.responseContent || e?.candidates?.[0]?.content || "");
}
function lR(e = {}) {
  return (e.parts || []).filter((t) => !t?.thought && typeof t?.text == "string" && t.text).map((t) => t.text).join(`
`);
}
function uR(e = {}) {
  return (e.parts || []).filter((t) => t?.thought && typeof t.text == "string" && t.text.trim()).map((t, n) => ({
    label: `思考块 ${n + 1}`,
    text: t.text.trim()
  }));
}
function cR(e = {}) {
  return (e.parts || []).map((t) => t?.functionCall || null).filter((t) => t?.name).map((t, n) => ({
    id: t.id || `st-google-tool-${n + 1}`,
    name: t.name,
    arguments: JSON.stringify(t.args || {})
  }));
}
function rQ(e, t) {
  const n = String(t || ""), r = String(e || "");
  return n ? !r || n.startsWith(r) ? n : r.endsWith(n) ? r : `${r}${n}` : r;
}
function oQ(e = [], t = []) {
  const n = Array.isArray(e) ? [...e] : [];
  return t.forEach((r) => {
    const o = [
      r.id || "",
      r.name || "",
      r.arguments || ""
    ].join("\0");
    n.some((s) => [
      s.id || "",
      s.name || "",
      s.arguments || ""
    ].join("\0") === o) || n.push(r);
  }), n;
}
function dR(e) {
  const t = Su(e);
  return t.parts.length ? {
    googleContent: t,
    googleContents: [t]
  } : void 0;
}
function sQ(e = {}, t = {}) {
  const n = iR(e), r = e?.choices?.[0]?.message?.content || "";
  return {
    text: lR(n) || r,
    toolCalls: cR(n),
    thoughts: uR(n),
    finishReason: e?.candidates?.[0]?.finishReason || e?.choices?.[0]?.finish_reason || t.finishReason || "STOP",
    model: e?.model || e?.modelVersion || t.model || "",
    provider: "sillytavern-google",
    providerPayload: dR(n)
  };
}
function aQ(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function iQ(e, t = {}) {
  let n = "", r = [], o = [], s = "STOP", a = t.model || "";
  const i = [];
  return {
    accept(l = {}) {
      a = l.model || l.modelVersion || a, s = l?.candidates?.[0]?.finishReason || s;
      const u = iR(l);
      u.parts.length && i.push(...gw(u.parts) || []), n = rQ(n, lR(u)), r = oQ(r, cR(u));
      const d = uR(u);
      d.length && (o = d), aQ(e, {
        text: n,
        thoughts: o,
        ...r.length ? {
          toolCalls: r,
          toolCallDraft: !0
        } : {}
      });
    },
    result() {
      const l = Su({
        role: "model",
        parts: i.length ? i : n ? [{ text: n }] : []
      });
      return {
        text: n,
        toolCalls: r,
        thoughts: o,
        finishReason: s,
        model: a,
        provider: "sillytavern-google",
        providerPayload: dR(l)
      };
    }
  };
}
var lQ = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return nQ(e);
  }
  buildPayload(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildMessages(e);
    return R9(this.config, e, n, t);
  }
  async inspectRequest(e, t = {}) {
    const n = await nf(t.payload || this.buildPayload(e), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-google",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: wu(e)
    };
  }
  async chat(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildPayload(e);
    let r = null;
    const o = (s) => {
      r = this.buildRequestInspection(s);
    };
    try {
      if (t) {
        const s = iQ(e, this.config);
        return await L0(n, (a) => {
          s.accept(a);
        }, {
          signal: e.signal,
          onRequest: o
        }), {
          ...s.result(),
          requestInspection: r
        };
      }
      return {
        ...sQ(await D0(n, {
          signal: e.signal,
          onRequest: o
        }), { model: this.config.model }),
        requestInspection: r
      };
    } catch (s) {
      throw r && s && typeof s == "object" && (s.requestInspection = r), s;
    }
  }
};
function uQ(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function By(e, t = []) {
  const n = Ci(e);
  return {
    thinkTagged: n,
    cleanedText: t.length ? n.cleaned : ki(n.cleaned)
  };
}
function cQ(e) {
  const t = String(e?.message || e || "");
  return /Cannot read properties of null \(reading ['"]function['"]\)/i.test(t) || /reading ['"]function['"]/i.test(t) || /badresponsestatuscode/i.test(t);
}
var dQ = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0 ? X1(e, this.config.model) : Y1(e, this.config.model);
  }
  buildPayload(e, t = !1) {
    const n = t ? X1(e, this.config.model) : Y1(e, this.config.model);
    return M9(this.config, t ? {
      ...e,
      tools: void 0,
      toolChoice: void 0
    } : e, n, typeof e.onStreamProgress == "function");
  }
  async inspectRequest(e, t = {}) {
    const n = await nf(t.payload || this.buildPayload(e, !!t.taggedMode), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-openai-compatible",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: wu(e)
    };
  }
  async streamChat(e, t, n = {}) {
    const r = {
      content: "",
      toolCalls: []
    }, o = { role: "assistant" };
    let s = "stop", a = this.config.model;
    await L0(t, (m) => {
      a = m?.model || a;
      const v = m?.choices?.[0] || {}, y = v.delta || {};
      Q1(o, v), v.finish_reason && (s = v.finish_reason), typeof y.content == "string" && (r.content += y.content), Array.isArray(y.tool_calls) && y.tool_calls.forEach((R) => {
        Z1(r, R);
      });
      const T = r.toolCalls.filter((R) => R?.function?.name), { thinkTagged: C, cleanedText: A } = By(r.content, T), I = T.length ? Ca(r.toolCalls, "st-openai-tool") : J1(C.cleaned);
      uQ(e, {
        text: A,
        thoughts: ka(o, v).concat(C.thoughts),
        ...I.length ? { toolCalls: I } : {},
        ...!T.length && I.length ? { toolCallDraft: !0 } : {}
      });
    }, {
      signal: e.signal,
      onRequest: n.onRequest
    });
    const i = Ca(r.toolCalls, "st-openai-tool"), { thinkTagged: l, cleanedText: u } = By(r.content, i), d = ka(o, {});
    l.thoughts.forEach((m) => d.push(m));
    const f = i.length ? [] : _d(l.cleaned);
    return {
      text: u,
      toolCalls: [...i, ...f],
      thoughts: d,
      finishReason: s,
      model: a,
      provider: "sillytavern-openai-compatible",
      providerPayload: bd(o)
    };
  }
  async nonStreamingChat(e, t, n = {}) {
    const r = await D0(t, {
      signal: e.signal,
      onRequest: n.onRequest
    }), o = r.choices?.[0] || {}, s = o.message || {}, a = ka(s, o), i = Ca(s.tool_calls || [], "st-openai-tool"), { thinkTagged: l, cleanedText: u } = By(nR(s.content), i);
    l.thoughts.forEach((m) => a.push(m));
    const d = i.length ? [] : _d(l.cleaned), f = xm(s, o);
    return {
      text: u,
      toolCalls: [...i, ...d],
      thoughts: a,
      finishReason: o.finish_reason || "stop",
      model: r.model || this.config.model,
      provider: "sillytavern-openai-compatible",
      providerPayload: bd(f)
    };
  }
  async chat(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = Array.isArray(e.tools) && e.tools.length > 0, r = async (s) => {
      let a = null;
      const i = (l) => {
        a = this.buildRequestInspection(l);
      };
      try {
        return {
          ...typeof e.onStreamProgress == "function" ? await this.streamChat(e, s, { onRequest: i }) : await this.nonStreamingChat(e, s, { onRequest: i }),
          requestInspection: a
        };
      } catch (l) {
        throw a && l && typeof l == "object" && (l.requestInspection = a), l;
      }
    }, o = this.buildPayload(e, t);
    try {
      return await r(o);
    } catch (s) {
      if (t || !n || !cQ(s)) throw s;
    }
    return typeof e.onToolProtocolFallback == "function" && e.onToolProtocolFallback({
      provider: "sillytavern-openai-compatible",
      fromToolMode: "native",
      toToolMode: "tagged-json",
      reason: "malformed_native_tool_host_error"
    }), await r(this.buildPayload(e, !0));
  }
}, fQ = "https://api.tavily.com";
function j1(e = "") {
  return String(e || "").trim();
}
function ho(e = "") {
  return String(e || "").trim().replace(/\/+$/, "") || "https://api.tavily.com";
}
var fR = "openai-compatible", vw = "默认", hR = "default", hQ = "deny", pQ = Object.freeze([{
  value: "default",
  label: "默认权限"
}, {
  value: "full",
  label: "完全权限"
}]), mQ = Object.freeze([{
  value: "deny",
  label: "禁止"
}, {
  value: "allow",
  label: "允许"
}]), e0 = {
  "openai-responses": {
    baseUrl: "https://api.openai.com/v1",
    model: "gpt-4.1-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  "openai-compatible": {
    baseUrl: "https://api.openai.com/v1",
    model: "gpt-4o-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0,
    toolMode: "native"
  },
  "sillytavern-openai-compatible": {
    baseUrl: "",
    model: "gpt-4o-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0,
    toolMode: "native"
  },
  "sillytavern-claude": {
    baseUrl: "",
    model: "claude-sonnet-4-0",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  "sillytavern-google": {
    baseUrl: "",
    model: "gemini-2.5-pro",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  anthropic: {
    baseUrl: "https://api.anthropic.com",
    model: "claude-sonnet-4-0",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  google: {
    baseUrl: "https://generativelanguage.googleapis.com/v1beta",
    model: "gemini-2.5-pro",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  }
};
function Jl() {
  return JSON.parse(JSON.stringify(e0));
}
function Yr() {
  return {
    provider: fR,
    modelConfigs: Jl(),
    permissionMode: hR
  };
}
function gQ(e = Yr()) {
  const t = e && typeof e == "object" ? e : Yr();
  return {
    provider: yw(t.provider),
    modelConfigs: Kr(t.modelConfigs || {})
  };
}
function Yl(e) {
  return e === "full" ? "full" : hR;
}
function va(e) {
  return e === "allow" ? "allow" : hQ;
}
function kn(e) {
  return String(e || "").trim() || "默认";
}
function Kr(e = {}) {
  const t = Jl();
  return Object.keys(e0).forEach((n) => {
    t[n] = {
      ...e0[n],
      ...e && typeof e[n] == "object" ? e[n] : {}
    };
  }), t;
}
function yw(e) {
  return typeof e == "string" && e.trim() ? e : fR;
}
function _w(e = {}, t) {
  return e && typeof e.presets == "object" && e.presets ? e.presets : e?.modelConfigs ? { [t]: {
    provider: e.provider || "openai-compatible",
    modelConfigs: e.modelConfigs,
    permissionMode: e.permissionMode
  } } : {};
}
function vQ(e = {}, t) {
  const n = {}, r = _w(e, t);
  return Object.entries(r).forEach(([o, s]) => {
    if (!s || typeof s != "object") return;
    const a = kn(o);
    n[a] = {
      provider: yw(s.provider),
      modelConfigs: Kr(s.modelConfigs || {}),
      permissionMode: Yl(s.permissionMode)
    };
  }), Object.keys(n).length || (n[vw] = Yr()), n;
}
function yQ(e, t) {
  const n = kn(t);
  return e[n] ? n : Object.keys(e)[0];
}
function _Q(e, t, n) {
  const r = kn(t || n);
  return e[r] ? r : e[n] ? n : Object.keys(e)[0];
}
function bQ(e = {}, t = Yr()) {
  const n = gQ(t), r = e && typeof e == "object" ? e : {};
  return {
    provider: yw(r.provider || n.provider),
    modelConfigs: Kr(r.modelConfigs || n.modelConfigs)
  };
}
function wQ(e = {}, t, n, r, o) {
  const s = o(e?.[r]);
  if (s) return s;
  const a = _w(e, t), i = [
    n,
    t,
    e?.currentPresetName,
    e?.delegatePresetName,
    ...Object.keys(a || {})
  ].map(kn), l = /* @__PURE__ */ new Set();
  for (const u of i) {
    if (l.has(u)) continue;
    l.add(u);
    const d = o(a?.[u]?.[r]);
    if (d) return d;
  }
  return o(e?.delegateConfig?.[r]);
}
function SQ(e = {}, t, n) {
  const r = (i) => String(i || "").trim();
  if (r(e?.tavilyBaseUrl)) return ho(e.tavilyBaseUrl);
  const o = _w(e, t), s = [
    n,
    t,
    e?.currentPresetName,
    e?.delegatePresetName,
    ...Object.keys(o || {})
  ].map(kn), a = /* @__PURE__ */ new Set();
  for (const i of s) {
    if (a.has(i)) continue;
    a.add(i);
    const l = o?.[i]?.tavilyBaseUrl;
    if (r(l)) return ho(l);
  }
  return r(e?.delegateConfig?.tavilyBaseUrl) ? ho(e.delegateConfig.tavilyBaseUrl) : fQ;
}
function TQ(e = {}, t, n) {
  return {
    tavilyApiKey: wQ(e, t, n, "tavilyApiKey", j1),
    tavilyBaseUrl: SQ(e, t, n)
  };
}
function wd(e = {}) {
  const t = kn(e.currentPresetName || e.presetDraftName || "默认"), n = vQ(e, t), r = yQ(n, e.currentPresetName), o = _Q(n, e.delegatePresetName, r), s = n[r] || Yr(), a = n[o] || s, i = bQ(e.delegateConfig, a), l = TQ(e, t, r);
  return {
    workspaceFileName: String(e.workspaceFileName || ""),
    jsApiPermission: va(e.jsApiPermission),
    currentPresetName: r,
    delegatePresetName: o,
    delegateConfig: i,
    presetDraftName: kn(e.presetDraftName || r),
    presetNames: Object.keys(n),
    presets: n,
    provider: s.provider,
    modelConfigs: s.modelConfigs,
    permissionMode: Yl(s.permissionMode),
    tavilyApiKey: l.tavilyApiKey,
    tavilyBaseUrl: l.tavilyBaseUrl
  };
}
var vC = 900 * 1e3, yC = Object.freeze([{
  value: "native",
  label: "原生 Tool Calling"
}, {
  value: "tagged-json",
  label: "Tagged JSON 兼容模式"
}]), t0 = Object.freeze([
  {
    value: "low",
    label: "低"
  },
  {
    value: "medium",
    label: "中"
  },
  {
    value: "high",
    label: "高"
  }
]), EQ = Object.freeze([
  {
    value: "openai-responses",
    label: "OpenAI Responses"
  },
  {
    value: "openai-compatible",
    label: "OpenAI 兼容"
  },
  {
    value: "sillytavern-openai-compatible",
    label: "酒馆 OpenAI 兼容"
  },
  {
    value: "sillytavern-claude",
    label: "酒馆 Claude"
  },
  {
    value: "sillytavern-google",
    label: "酒馆 Google AI"
  },
  {
    value: "anthropic",
    label: "Anthropic"
  },
  {
    value: "google",
    label: "Google AI"
  }
]);
function _C(e = "") {
  return e === "anthropic" || e === "sillytavern-claude";
}
function AQ(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function Gr(e = "") {
  return t0.some((t) => t.value === e) ? e : "medium";
}
function Vr(e, t = 0.2) {
  const n = typeof e == "string" && !e.trim() ? t : e, r = Number(n);
  return Number.isFinite(r) ? Math.max(0, Math.min(2, r)) : Vr(t, 0.2);
}
function iu(e = {}) {
  return e.sendTemperature !== !1;
}
function bC(e = {}) {
  return iu(e) ? Vr(e.temperature, 0.2) : void 0;
}
function wC(e = "", t = {}) {
  return t && typeof t == "object" && t[e] ? t[e] : EQ.find((n) => n.value === e)?.label || e || "未配置";
}
function Dg(e = {}, t = {}) {
  const n = wd(e || {});
  if (t.role === "delegate" && n.delegateConfig) {
    const l = n.delegateConfig.provider || "openai-compatible", u = (n.delegateConfig.modelConfigs || Jl())[l] || Jl()[l] || {};
    return {
      currentPresetName: String(n.delegatePresetName || n.currentPresetName || ""),
      provider: l,
      baseUrl: String(u.baseUrl || ""),
      model: String(u.model || ""),
      apiKey: String(u.apiKey || ""),
      tavilyApiKey: j1(n.tavilyApiKey),
      tavilyBaseUrl: ho(n.tavilyBaseUrl),
      temperature: bC(u),
      sendTemperature: iu(u),
      maxTokens: _C(l) ? 32e3 : null,
      timeoutMs: Number(t.timeoutMs) || 9e5,
      toolMode: u.toolMode || "native",
      reasoningEnabled: !!u.reasoningEnabled,
      reasoningEffort: Gr(u.reasoningEffort)
    };
  }
  const r = kn(t.presetName || (t.role === "delegate" ? n.delegatePresetName : n.currentPresetName) || "默认"), o = n.presets?.[r] ? r : n.presets?.[n.currentPresetName] ? n.currentPresetName : vw, s = n.presets?.[o] || Yr(), a = s.provider || n.provider || "openai-compatible", i = (s.modelConfigs || n.modelConfigs || Jl())[a] || Jl()[a] || {};
  return {
    currentPresetName: String(o || ""),
    provider: a,
    baseUrl: String(i.baseUrl || ""),
    model: String(i.model || ""),
    apiKey: String(i.apiKey || ""),
    tavilyApiKey: j1(n.tavilyApiKey),
    tavilyBaseUrl: ho(n.tavilyBaseUrl),
    temperature: bC(i),
    sendTemperature: iu(i),
    maxTokens: _C(a) ? 32e3 : null,
    timeoutMs: Number(t.timeoutMs) || 9e5,
    toolMode: i.toolMode || "native",
    reasoningEnabled: !!i.reasoningEnabled,
    reasoningEffort: Gr(i.reasoningEffort)
  };
}
function bw(e = {}, t = {}) {
  if (!e.apiKey && !AQ(e.provider)) throw new Error(t.missingApiKeyMessage || "请先填写当前模型配置的 API Key。");
  switch (e.provider) {
    case "sillytavern-openai-compatible":
      return new dQ(e);
    case "sillytavern-claude":
      return new ZX(e);
    case "sillytavern-google":
      return new lQ(e);
    case "openai-responses":
      return new zX(e);
    case "anthropic":
      return new QU(e);
    case "google":
      return new KJ(e);
    default:
      return new RX(e);
  }
}
function CQ(e = {}) {
  const t = String(e?.name || "").trim();
  if (typeof e?.arguments == "string") {
    const n = e.arguments;
    try {
      return JSON.parse(n.trim() || "{}"), n;
    } catch {
      return ZP(n, t) || n;
    }
  }
  try {
    return JSON.stringify(e?.arguments || {});
  } catch {
    return "{}";
  }
}
function n0(e = [], t = {}) {
  const n = String(t.fallbackPrefix || "agent-tool").trim() || "agent-tool", r = typeof t.createId == "function" ? t.createId : (o) => `${n}-${Date.now()}-${o + 1}`;
  return (Array.isArray(e) ? e : []).map((o, s) => ({
    id: String(o?.id || r(s) || `${n}-${s + 1}`),
    name: String(o?.name || "").trim(),
    arguments: CQ(o)
  })).filter((o) => o.name);
}
function kQ(e, t = {}) {
  return (Array.isArray(e?.googleContent?.parts) ? e.googleContent.parts : []).filter((n) => n?.functionCall?.name).map((n, r) => ({
    id: String(n.functionCall.id || `${t.fallbackPrefix || "google-tool"}-${r + 1}`),
    name: String(n.functionCall.name || ""),
    arguments: JSON.stringify(n.functionCall.args || {})
  })).filter((n) => n.name);
}
function pR(e = {}, t = {}, n = {}) {
  const r = n0(e?.toolCalls, n);
  return r.length ? r : String(e?.provider || t?.provider || "").toLowerCase() !== "google" ? [] : n0(kQ(e?.providerPayload, n), n);
}
function mR(e = {}, t = [], n = {}) {
  return {
    role: "assistant",
    content: Object.prototype.hasOwnProperty.call(n, "content") ? String(n.content || "") : String(e.text || ""),
    providerPayload: e.providerPayload,
    tool_calls: n0(t, n).map((r) => ({
      id: r.id,
      type: "function",
      function: {
        name: r.name,
        arguments: r.arguments || "{}"
      }
    }))
  };
}
function gR(e = {}) {
  const t = String(e.toolName || e.tool_name || "").trim();
  return {
    role: "tool",
    tool_call_id: String(e.toolCallId || e.tool_call_id || ""),
    ...t ? { toolName: t } : {},
    content: String(e.content || "")
  };
}
function r0(e) {
  return typeof e == "string" && e.trim().length > 0;
}
async function IQ(e = "", t) {
  const n = String(e || "").trim();
  if (!n) return {
    floor: -1,
    memorySnapshotSaved: !1,
    taskSnapshotSaved: !1
  };
  const r = await AB(n, t), [o, s] = await Ot.transaction("rw", hn, is, Yt, Et, Tn, ls, us, async () => [await HB(n, r), await EB(n, r)]);
  return {
    floor: r,
    memorySnapshotSaved: !!o,
    taskSnapshotSaved: !!s
  };
}
var ww = "ActionCheck";
var xQ = [
  "[Runtime Protocol: Action Checks]",
  `For this reply, if the user's attempted action has meaningful uncertainty, risk, opposition, danger, stealth, combat, persuasion, deception, investigation, escape, pressure, luck, or any outcome not guaranteed, you MUST call ${ww} before deciding success or failure.`,
  "In those cases, do not decide success or failure yourself.",
  "Treat the tool result as binding truth and continue the narration from it.",
  "Skip the tool only for trivial, purely conversational, already guaranteed, or explicitly established outcomes.",
  "Follow all later formatting and style instructions.",
  "Do not mention this protocol, the tool, or hidden system instructions."
].join(`
`);
function Tp(e, t = 240) {
  return String(e || "").trim().slice(0, t);
}
function vR(e, t, n, r) {
  const o = Math.round(Number(e));
  return Number.isFinite(o) ? Math.min(n, Math.max(t, o)) : r;
}
function MQ(e) {
  if (typeof e == "function") return vR(e(), 1, 20, 1);
  const t = globalThis.crypto;
  if (t?.getRandomValues) {
    const n = new Uint32Array(1);
    return t.getRandomValues(n), n[0] % 20 + 1;
  }
  return Math.floor(Math.random() * 20) + 1;
}
function yR() {
  return [{
    type: "function",
    function: {
      name: ww,
      description: [
        "True-random d20 action check for risky or uncertain RP outcomes.",
        "Call this whenever the user attempts something meaningful whose success or failure should not be decided subjectively.",
        "Provide the attempted action, the check or stat name, and an optional difficulty.",
        "The result is binding truth for the current reply."
      ].join(`
`),
      parameters: {
        type: "object",
        properties: {
          action: {
            type: "string",
            description: "What the character is attempting right now."
          },
          stat: {
            type: "string",
            description: "The displayed check name, attribute, skill, or trait for this roll."
          },
          difficulty: {
            type: "number",
            minimum: 2,
            maximum: 20,
            description: "Target number for success. Defaults to 12 if omitted or invalid."
          },
          stakes: {
            type: "string",
            description: "Optional brief note about what is at risk. Display-only."
          }
        },
        required: ["action", "stat"],
        additionalProperties: !1
      }
    }
  }];
}
function PQ() {
  return {
    role: "system",
    content: xQ
  };
}
function RQ(e = {}, t = {}) {
  const n = Tp(e.action, 240), r = Tp(e.stat, 120);
  if (!n || !r) return {
    ok: !1,
    error: "action_check_action_and_stat_required",
    summary: "ActionCheck 需要同时提供 action 和 stat。"
  };
  const o = vR(e.difficulty, 2, 20, 12), s = MQ(t.rollDie), a = s >= o, i = Tp(e.stakes, 240);
  return {
    ok: !0,
    action: n,
    stat: r,
    difficulty: o,
    roll: s,
    success: a,
    summary: `${r} check ${s} vs DC ${o}: ${a ? "success" : "failure"}.`,
    ...i ? { stakes: i } : {}
  };
}
function NQ(e = "") {
  return {
    ok: !1,
    error: "action_check_unknown_tool",
    summary: `主聊天当前只允许调用 ${ww}；${Tp(e, 80) || "unknown_tool"} 不会执行。`
  };
}
function bf(e = []) {
  const t = {};
  return e.forEach((n) => {
    const [r] = String(n.id || "").split(":");
    if (!n.changed || ![
      "userInput",
      "worldInfo",
      "aiOutput",
      "reasoning"
    ].includes(r)) return;
    const o = r;
    t[o] = (t[o] || 0) + 1;
  }), t;
}
function _R(e) {
  return !!e && Object.values(e).some((t) => Number(t) > 0);
}
var $Q = 2, Uy = [
  {
    start: "<think>",
    end: "</think>"
  },
  {
    start: "<thinking>",
    end: "</thinking>"
  },
  {
    start: "```",
    end: "```"
  }
];
function Rm(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").toLowerCase().replace(/\s+/g, "").trim();
}
function bR(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").toLowerCase();
}
function DQ(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "");
}
function Nm(e) {
  return e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function wR(e, t = []) {
  if (!e || !t.length) return e;
  let n = e;
  for (const r of t) {
    const o = String(r?.start ?? ""), s = String(r?.end ?? "");
    if (!(!o && !s)) {
      if (o && s) n = n.replace(new RegExp(`${Nm(o)}[\\s\\S]*?${Nm(s)}`, "gi"), "");
      else if (o) {
        const a = n.toLowerCase().indexOf(o.toLowerCase());
        a >= 0 && (n = n.slice(0, a));
      } else if (s) {
        const a = n.toLowerCase().indexOf(s.toLowerCase());
        a >= 0 && (n = n.slice(a + s.length));
      }
    }
  }
  return n.trim();
}
function SR() {
  try {
    const e = globalThis.localStorage?.getItem?.("summary_panel_config");
    if (!e) return Uy;
    const t = JSON.parse(e);
    return (Array.isArray(t?.textFilterRules) ? t.textFilterRules : Array.isArray(t?.vector?.textFilterRules) ? t.vector.textFilterRules : Uy).filter((n) => !!n && typeof n == "object");
  } catch {
    return Uy;
  }
}
function LQ(e = []) {
  return new Set(e.map((t) => Rm(t)).filter((t) => t.length >= 2));
}
function TR(e) {
  return !(e.length < 2 || /^[a-z0-9]+$/.test(e) && e.length < 3);
}
function OQ(e) {
  return /^[a-z0-9\s_-]+$/.test(e.trim());
}
function SC(e = "") {
  return /^[A-Z0-9]/.test(e);
}
function FQ(e, t, n) {
  const r = e.slice(0, t).match(/([A-Za-z0-9]+)([\s_-]+)$/);
  if (r && (r[2].includes("-") || r[2].includes("_")) && SC(r[1])) return !0;
  const o = e.slice(n).match(/^([\s_-]+)([A-Za-z0-9]+)/);
  return o ? o[1].includes("-") || o[1].includes("_") || SC(o[2]) : !1;
}
function BQ(e, t, n, r) {
  const o = bR(e).trim(), s = Rm(e);
  if (!TR(s)) return [];
  if (OQ(o)) {
    const i = o.split(/[\s_-]+/).filter(Boolean);
    if (!i.length) return [];
    const l = i.map(Nm).join("[\\s_-]+"), u = new RegExp(`(^|[^a-z0-9])(${l})(?=$|[^a-z0-9])`, "gi"), d = [];
    for (const f of t.matchAll(u)) {
      const m = f[1] || "", v = f[2] || "", y = Number(f.index || 0) + m.length, T = y + v.length;
      i.length === 1 && FQ(n, y, T) || d.push({
        coordinate: "text",
        start: y,
        end: T,
        length: v.replace(/[\s_-]+/g, "").length,
        tokenCount: i.length
      });
    }
    return d;
  }
  const a = r.indexOf(s);
  return a < 0 ? [] : [{
    coordinate: "compact",
    start: a,
    end: a + s.length,
    length: s.length,
    tokenCount: 1
  }];
}
function UQ(e, t) {
  return e.path === t.path || e.coordinate !== t.coordinate || t.start > e.start || t.end < e.end ? !1 : t.tokenCount > e.tokenCount || t.length > e.length;
}
function qQ(e, t = []) {
  const n = [
    ...t,
    "user",
    "assistant",
    "用户",
    "角色",
    "玩家",
    "旁白"
  ].map((o) => String(o || "").trim()).filter(Boolean);
  if (!n.length) return e;
  const r = n.map(Nm).join("|");
  return String(e || "").split(/\r?\n/).map((o) => o.replace(new RegExp(`^\\s*(?:${r})\\s*[:：]\\s*`, "i"), "")).join(`
`);
}
function HQ(e = "") {
  return wR(String(e || ""), SR()).replace(/\[tts:[^\]]*]/gi, " ").replace(/<state>[\s\S]*?<\/state>/gi, " ").replace(/<status[^>]*>[\s\S]*?<\/status>/gi, " ").replace(/\s+/g, " ").trim();
}
function TC(e = "") {
  return wR(String(e || ""), SR()).replace(/\[tts:[^\]]*]/gi, " ").replace(/<state>[\s\S]*?<\/state>/gi, " ").replace(/<status>[\s\S]*?<\/status>/gi, " ").replace(/<status[^>]*>[\s\S]*?<\/status>/gi, " ").replace(/\r\n/g, `
`).trim();
}
function EC(e) {
  if ("title" in e && e.title) return String(e.title || "").trim();
  const t = "content" in e ? String(e.content || "").match(/^\s*#\s+(.+?)\s*$/m)?.[1] : "";
  return String(t || e.path.split("/").pop() || e.path).trim();
}
function zQ(e) {
  return String(("content" in e ? e.content : "") || ("preview" in e ? e.preview : "") || "");
}
function ER(e) {
  const t = String(e.path || "");
  if (e.status === "stale") return !1;
  const n = Yi(t);
  return n && uM(n) ? !1 : Ei(t) || mm(t);
}
async function WQ(e = "", t = {}) {
  const n = Array.isArray(t.memoryFiles) ? t.memoryFiles : [];
  if (!n.length) return t;
  const r = await Promise.all(n.map(async (o) => ({
    summary: o,
    record: await Ma(e, o.path)
  })));
  return {
    ...t,
    memoryFiles: r.map(({ summary: o, record: s }) => s ? {
      ...o,
      content: String(s.content || "")
    } : o)
  };
}
function Sw(e = {}) {
  return [e.user?.name].map((t) => String(t || "").trim()).filter((t) => t.length >= 2);
}
function AR(e = {}, t = "") {
  const n = Array.isArray(e.history) ? e.history.slice(-$Q) : [], r = Sw(e), o = (s) => qQ(HQ(s), r);
  return [
    o(t),
    ...n.map((s) => o(s.content || s.mes || s.message || "")),
    o(e.character?.name || "")
  ].filter(Boolean).join(`
`);
}
function GQ(e = {}) {
  const t = [...e.memoryFiles || []].filter(ER).sort((l, u) => l.path.localeCompare(u.path)), n = LQ(e.ignoredTerms || []), r = DQ(e.queryText || ""), o = bR(e.queryText || ""), s = Rm(o), a = t.map((l) => ({
    file: l,
    characterName: Yi(l.path)
  })).flatMap((l) => {
    if (!mm(l.file.path)) return [];
    const u = Rm(l.characterName);
    return !TR(u) || n.has(u) ? [] : BQ(l.characterName, o, r, s).map((d) => ({
      ...d,
      path: l.file.path,
      file: l.file,
      characterName: l.characterName
    }));
  }), i = new Set(a.filter((l) => !a.some((u) => UQ(l, u))).map((l) => l.path));
  return { memoryFiles: t.filter((l) => Ei(l.path) || i.has(l.path)).map((l) => ({
    file: l,
    characterName: Yi(l.path)
  })).sort((l, u) => (Ei(l.file.path) ? 0 : 1) - (Ei(u.file.path) ? 0 : 1) || l.file.path.localeCompare(u.file.path)).map((l) => ({
    ...l.file,
    title: mm(l.file.path) ? l.characterName : EC(l.file),
    recallReason: Ei(l.file.path) ? "fixed" : "entity",
    recallScore: Ei(l.file.path) ? 0 : 1
  })).map((l) => ({
    path: l.path,
    title: EC(l),
    content: zQ(l),
    recallReason: l.recallReason,
    recallScore: l.recallScore
  })) };
}
async function CR(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return {};
  const n = e.includeMemoryFiles !== !1, r = e.includeStructuredStates !== !1, [o, s, a] = await Promise.all([
    n ? vM(t) : Promise.resolve(null),
    r ? pB(t) : Promise.resolve([]),
    r ? hB(t) : Promise.resolve("")
  ]), i = n && o?.status === "ready" && Array.isArray(o.files) || n && o?.status === "stale" && o.error && Array.isArray(o.files) ? o : n ? await Xi(t) : null, l = Array.isArray(i?.files) ? i.files : [], u = l.some(ER);
  if (!u && !s.length && !a) return {};
  const d = u ? GQ({
    memoryFiles: l,
    queryText: e.queryText,
    ignoredTerms: e.ignoredTerms
  }) : {};
  return {
    ...u ? await WQ(t, d) : d,
    structuredStates: s,
    ...a ? { spatialState: a } : {}
  };
}
var VQ = Object.freeze({
  tools: [],
  toolChoice: "none"
});
function KQ(e, t = VQ) {
  return {
    provider: e,
    capabilities: t,
    buildChatTask(n) {
      return {
        messages: n.messages,
        tools: t.tools,
        toolChoice: t.toolChoice,
        temperature: e.temperature,
        maxTokens: e.maxTokens,
        signal: n.signal,
        onStreamProgress: n.onStreamProgress
      };
    }
  };
}
var JQ = 3.35, YQ = /* @__PURE__ */ new Set([
  "openai-compatible",
  "openai-responses",
  "sillytavern-openai-compatible"
]), XQ = new TextEncoder();
function QQ(e = []) {
  return e.map((t) => {
    const n = Array.isArray(t.content) ? t.content.map((r) => !r || typeof r != "object" ? "" : r.type === "text" ? r.text || "" : r.type === "image_url" ? `[image:${r.name || r.mimeType || "image"}]` : "").filter(Boolean).join(`
`) : t.content || "";
    return t.role === "assistant" && Array.isArray(t.tool_calls) && t.tool_calls.length ? {
      role: "assistant",
      content: [n, t.tool_calls.map((r) => JSON.stringify({
        id: r.id,
        name: r.function?.name || "",
        arguments: r.function?.arguments || "{}"
      })).join(`
`)].filter(Boolean).join(`
`)
    } : t.role === "tool" ? {
      role: "tool",
      content: [t.tool_call_id || "", t.content || ""].filter(Boolean).join(`
`)
    } : {
      role: t.role,
      content: n
    };
  });
}
function kR(e = [], t = []) {
  return [...QQ(e), {
    role: "system",
    content: t.length ? `TOOLS
${JSON.stringify(t)}` : ""
  }].filter((n) => n.content);
}
function ZQ(e = "") {
  return Math.ceil(XQ.encode(String(e || "")).length / JQ);
}
function AC({ messages: e = [], tools: t = [] } = {}) {
  return ZQ(JSON.stringify(kR(e, t)));
}
function jQ(e = {}) {
  const t = String(e?.model || "").trim();
  return t || (e?.provider === "anthropic" ? "claude" : "gpt-4o");
}
async function IR(e, t) {
  const n = await fetch(e, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(t)
  });
  if (!n.ok) throw new Error(`tokenizer_http_${n.status}`);
  return await n.json();
}
async function eZ(e = [], t = "") {
  if (!e.length) return 0;
  const n = `/api/tokenizers/openai/count?model=${encodeURIComponent(t || "gpt-4o")}`;
  let r = -1;
  for (const o of e) {
    const s = await IR(n, [o]), a = Number(s?.token_count);
    if (!Number.isFinite(a)) throw new Error("tokenizer_invalid_response");
    r += a;
  }
  return Math.max(0, r);
}
async function tZ(e, t) {
  const n = await IR(e, { text: t }), r = Number(n?.count);
  if (!Number.isFinite(r)) throw new Error("tokenizer_invalid_response");
  return r;
}
async function nZ({ messages: e = [], tools: t = null, providerConfig: n = {} } = {}) {
  const r = String(n?.provider || ""), o = Array.isArray(t) ? t : [], s = kR(e, o), a = JSON.stringify(s);
  try {
    if (YQ.has(r)) return await eZ(s, jQ(n));
    if (r === "anthropic") return await tZ("/api/tokenizers/claude/encode", a);
  } catch {
    return AC({
      messages: e,
      tools: o
    });
  }
  return AC({
    messages: e,
    tools: o
  });
}
var xR = [
  Zn.LS,
  Zn.GREP,
  Zn.READ
], o0 = [Zn.EDIT, Zn.WRITE], $m = Object.values(Xr), Dm = Object.values(Ji);
function rZ(e = "") {
  const t = String(e || "").trim();
  return !t || xR.includes(t) ? null : o0.includes(t) ? "memoryArchiving" : $m.includes(t) ? "cartographyEngine" : Dm.includes(t) ? "questOrchestration" : null;
}
function oZ(e = "") {
  return $m.includes(String(e || "").trim());
}
function sZ(e = "") {
  return Dm.includes(String(e || "").trim());
}
function Sd(e) {
  const t = ug(e), n = [...xR];
  t.includeMemoryFiles && n.push(...o0), t.includeStructuredStates && n.push(...$m), t.includeQuestOrchestration && n.push(...Dm);
  const r = new Set(n), o = [
    ...o0,
    ...$m,
    ...Dm
  ].filter((s) => !r.has(s));
  return {
    runtime: t,
    allowedToolNames: [...r],
    deniedToolNames: o
  };
}
function aZ(e = [], t) {
  const n = new Set(Sd(t).allowedToolNames);
  return e.filter((r) => n.has(String(r?.function?.name || "").trim()));
}
function iZ(e = "", t) {
  return new Set(Sd(t).allowedToolNames).has(String(e || "").trim());
}
function lZ(e = "", t) {
  const n = String(e || "").trim(), r = rZ(n), o = r ? nF(r)?.title || r : "Auto Manager", s = r ? `契约未授权 ${o}，本轮不会执行 ${n || "该工具"}。` : `自动管理员当前不会执行 ${n || "该工具"}。`;
  return oZ(n) ? {
    ok: !1,
    summary: s,
    changed: !1,
    error: "contract_tool_unauthorized",
    warnings: Sd(t).runtime.hasAutomaticManagerWork ? [] : ["contract_manager_work_disabled"]
  } : sZ(n) ? {
    ok: !1,
    summary: s,
    changed: !1,
    error: "contract_tool_unauthorized",
    warnings: Sd(t).runtime.hasAutomaticManagerWork ? [] : ["contract_manager_work_disabled"]
  } : {
    ok: !1,
    summary: s,
    changed: !1,
    error: "contract_tool_unauthorized",
    warning: Sd(t).runtime.hasAutomaticManagerWork ? "" : "contract_manager_work_disabled"
  };
}
var uZ = {
  "openai-responses": "OpenAI Responses",
  "openai-compatible": "OpenAI 兼容",
  "sillytavern-openai-compatible": "酒馆 OpenAI 兼容",
  "sillytavern-claude": "酒馆 Claude",
  "sillytavern-google": "酒馆 Google AI",
  anthropic: "Anthropic",
  google: "Google AI"
};
function cZ(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function MR(e = "") {
  return uZ[e] || e || "未配置";
}
function Tw(e = {}) {
  const t = Dg(e || {}, { timeoutMs: 9e5 }), n = String(t.provider || ""), r = String(t.model || "").trim(), o = String(t.apiKey || "").trim(), s = [];
  r || s.push("模型"), !cZ(n) && !o && s.push("API Key");
  const a = s.length ? `请先在 API 配置里选择模型/填写 Key：缺少 ${s.join("、")}` : "API 配置可用";
  return {
    currentPresetName: String(t.currentPresetName || ""),
    provider: n,
    providerLabel: MR(n),
    baseUrl: String(t.baseUrl || ""),
    model: r,
    apiKey: o,
    temperature: Number(t.temperature ?? 0.2),
    maxTokens: t.maxTokens === null || t.maxTokens === void 0 ? null : Number(t.maxTokens),
    timeoutMs: Number(t.timeoutMs) || 900 * 1e3,
    toolMode: String(t.toolMode || "native"),
    readiness: {
      ok: s.length === 0,
      missing: s,
      message: a
    }
  };
}
function PR(e = {}) {
  const t = Tw(e);
  if (!t.readiness.ok) throw new Error(t.readiness.message);
  return t;
}
function Lg(e) {
  const t = e.supportsSessionToolLoop === !0, n = Array.isArray(e.messages) ? e.messages : [], r = Array.isArray(e.toolResponses) ? e.toolResponses : [], o = String(e.finalAnswerReminderText || "").trim();
  return t && r.length ? {
    mode: "session_tool_response_round",
    requestMessages: [],
    toolResponses: r,
    finalAnswerReminderText: "",
    supportsSessionToolLoop: t
  } : t && o ? {
    mode: "session_final_reminder_round",
    requestMessages: [],
    toolResponses: [],
    finalAnswerReminderText: o,
    supportsSessionToolLoop: t
  } : {
    mode: "full_prompt_round",
    requestMessages: n,
    toolResponses: [],
    finalAnswerReminderText: "",
    supportsSessionToolLoop: t
  };
}
function RR(e, t) {
  return delete e.messages, delete e.toolResponses, delete e.finalAnswerReminderText, t.mode === "full_prompt_round" ? (e.messages = t.requestMessages, e) : t.mode === "session_tool_response_round" ? (e.toolResponses = t.toolResponses, e) : (t.mode === "session_final_reminder_round" && (e.finalAnswerReminderText = t.finalAnswerReminderText), e);
}
function dZ(e) {
  return e.mode === "session_tool_response_round" ? { message: {
    role: "user",
    parts: e.toolResponses.filter((t) => t && t.name).map((t) => ({ functionResponse: {
      name: String(t.name || ""),
      response: t.response || {}
    } }))
  } } : e.mode === "session_final_reminder_round" ? { message: [{ text: e.finalAnswerReminderText }] } : null;
}
var fZ = nZ, Mc = 158e3;
var hZ = 2e4, Ri = /* @__PURE__ */ new Map(), Jd = /* @__PURE__ */ new Map();
function Ep(e = "", t = 4e3) {
  const n = String(e || "").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function CC(e) {
  try {
    return JSON.stringify(e, null, 2);
  } catch {
    return String(e || "");
  }
}
function pZ(e, t = {}) {
  if (e && typeof e == "object" && !Array.isArray(e)) return e;
  try {
    const n = JSON.parse(String(e || "{}"));
    return n && typeof n == "object" && !Array.isArray(n) ? n : t;
  } catch {
    return t;
  }
}
function NR(e, t = {}) {
  return YO(e, t).trim();
}
function $R(e) {
  return ug(e);
}
function DR(e) {
  const t = e.find((n) => n.path === "memory/state.md");
  return ["[Resident Memory Files]", ...[t ? ["[memory/state.md]", t.content].join(`
`) : ""].filter(Boolean)].join(`

`);
}
function mZ(e) {
  const t = e.runtime.managerPromptOptions.includeMemory, n = e.runtime.managerPromptOptions.includeCartography, r = e.runtime.managerPromptOptions.includeQuestOrchestration, o = [];
  let s = 1;
  return t && (o.push(`${s}. Read \`memory/state.md\` and relevant \`memory/characters/<角色名>.md\` files as needed, then update memory only if this completed assistant reply changed durable memory.`), s += 1, o.push(`${s}. Do not create or maintain user/player character files such as \`memory/characters/User.md\`, \`Player.md\`, \`用户.md\`, or \`玩家.md\`. Treat user messages as source evidence, not as a user persona sheet; ignore status-bar/UI/meta text unless the assistant reply establishes it inside RP.`), s += 1), n && (o.push(`${s}. Spatial maintenance has two layers: \`tavern.atlas/main\` is the single world index, and each local scene map lives in its own place-named \`tavern.map/<docId>\`. Move the player in atlas only when the story reaches a new named or trackable place; small movement inside the same place belongs in the scene map actor coordinates. Map \`activate:true\` only switches the map tool doc and does not move the player.`), s += 1), t && n ? (o.push(`${s}. Maintain \`memory/state.md\` for global memory and \`memory/characters/<角色名>.md\` for character memory. The map does not replace written memory.`), s += 1) : t ? (o.push(`${s}. Maintain \`memory/state.md\` for global memory and \`memory/characters/<角色名>.md\` for character memory.`), s += 1) : n ? (o.push(`${s}. This contract authorizes only the map system. Do not write memory Markdown.`), s += 1) : r || (o.push(`${s}. This contract authorizes neither background memory nor map maintenance. Do not write memory or map records; clearly say that you skipped it.`), s += 1), r && (o.push(`${s}. Maintain the event pool with EventInspect/EventPatch only when useful: advance or complete active directions that the reply actually addressed; after floor 5, create fresh active directions only when the active pool has 0-1 items. A fresh direction must recombine established material into a not-yet-played person, place, faction, or situation, with a larger horizon, an immediate current entrance, and a doneWhen completion condition written as a concrete observable event in the story, not an abstract state. Use the user’s demonstrated tastes as the engine for boldness; do not use EventPatch for old facts, existing foreshadowing, obvious continuations, or generic random events.`), s += 1), o.push(`${s}. Close with a short result: say what you wrote, skipped, or left pending.`), [
    ...t ? [DR(e.memoryFiles), ""] : [],
    ...r ? [String(e.taskPoolBlock || `[Current Event Pool]
Active directions: unknown.`).trim(), ""] : [],
    "[本轮 RP 原文]",
    "[用户消息]",
    TC(e.userMessage.content),
    "",
    "[角色回复]",
    TC(e.assistantMessage.content),
    "",
    "[本轮要求]",
    ...o
  ].join(`
`);
}
function gZ(e) {
  return [
    DR(e.memoryFiles),
    "",
    "[当前问题]",
    e.question
  ].join(`
`);
}
function Ew(e) {
  return String(e.trigger || "") === "manager_chat" ? `manager chat; turn ${Math.max(0, Number(e.turn) || 0)}; question ${String(e.text || "").length} chars` : `turn ${Math.max(0, Number(e.turn) || 0)}; messages ${Number(e.userOrder)}/${Number(e.assistantOrder)}; user ${String(e.text || "").length} chars`;
}
async function vZ(e, t, n) {
  const r = Array.isArray(n.messages) ? n.messages : [], o = {
    systemPrompt: r[0]?.content || "",
    tools: Array.isArray(n.tools) ? n.tools : [],
    toolChoice: n.toolChoice || (n.tools?.length ? "auto" : "none"),
    temperature: t.temperature,
    maxTokens: t.maxTokens,
    reasoning: {
      enabled: t.reasoningEnabled,
      effort: t.reasoningEffort
    },
    signal: n.signal,
    onStreamProgress: (a) => {
      n.onStreamProgress?.({
        ...typeof a.text == "string" ? { text: a.text } : {},
        ...Array.isArray(a.thoughts) ? { thoughts: Lm(a.thoughts) } : {},
        ...Array.isArray(a.toolCalls) ? { toolCalls: a.toolCalls } : {},
        ...a.toolCallDraft ? { toolCallDraft: !0 } : {}
      });
    }
  };
  RR(o, Lg({
    supportsSessionToolLoop: e?.supportsSessionToolLoop === !0,
    messages: r,
    toolResponses: n.toolResponses,
    finalAnswerReminderText: n.finalAnswerReminderText
  }));
  const s = await e.chat(o);
  return {
    text: String(s?.text || "").trim(),
    provider: String(s?.provider || t.provider || ""),
    model: String(s?.model || t.model || ""),
    toolCalls: Array.isArray(s?.toolCalls) ? s.toolCalls : [],
    thoughts: Lm(s?.thoughts),
    providerPayload: s?.providerPayload
  };
}
function kC(e = {}) {
  return [
    "filePath",
    "path",
    "pattern",
    "mode",
    "docType",
    "docId",
    "elementId",
    "startOrder",
    "endOrder"
  ].map((t) => {
    const n = Ep(e[t], 160);
    return n ? `${t}: ${n}` : "";
  }).filter(Boolean).join("; ");
}
function IC(e) {
  const t = Ep(e.summary || e.error || "", 360);
  if (e.ok !== !1) return t;
  const n = e.details;
  return !Array.isArray(n) || !n.length ? t : Ep([t, n.slice(0, 2).map((r) => {
    const o = r && typeof r == "object" ? r : {};
    return Ep(o.hint || o.error || r, 180);
  }).filter(Boolean).join("；")].filter(Boolean).join(" "), 420);
}
function yZ(e = "") {
  return Object.values(Xr).includes(e);
}
function _Z(e = "") {
  return Object.values(Ji).includes(e);
}
function bZ(e = "") {
  return Object.values(Zn).includes(e);
}
function wZ(e = []) {
  return e.some((t) => t.ok === !1);
}
function Lm(e) {
  if (!Array.isArray(e)) return [];
  const t = [];
  return e.forEach((n, r) => {
    const o = n && typeof n == "object" ? n : {}, s = String(o.text || o.content || o.thinking || "").trim();
    s && t.push({
      label: String(o.label || o.summary || `思考 ${r + 1}`).trim() || `思考 ${r + 1}`,
      text: s
    });
  }), t;
}
async function xC(e = "", t = []) {
  const n = String(e || "").trim();
  n && await yo(n, {
    status: "running",
    toolTrace: t.map((r) => ({ ...r }))
  });
}
function SZ(e, t) {
  if (t?.aborted) return !0;
  const n = e instanceof Error ? e.message : String(e || "");
  return (e instanceof Error ? e.name : "") === "AbortError" || /abort|aborted|cancelled|canceled/i.test(n);
}
function Aw(e, t) {
  const n = e instanceof Error ? e.message : String(e || "");
  return SZ(e, t) ? "cancelled" : n === "manager_source_messages_changed" || n === "manager_epoch_expired" ? "superseded" : "failed";
}
async function LR(e = "") {
  const [t, n] = await Promise.all([gb(e), sM(e)]), r = t.some((u) => String(u.afterHash || "").trim()), o = n.some((u) => String(u.afterHash || "").trim());
  if (!r && !o) return null;
  const s = r ? await Ix(e) : {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  }, a = o ? await aM(e) : {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  }, i = [...s.conflicts, ...a.conflicts], l = await yo(e, {});
  return l && i.length && await yo(e, { error: TZ(l.error, i) }), {
    managerRun: await yo(e, {}),
    conflicts: i
  };
}
function TZ(e = "", t = []) {
  const n = String(e || "").trim();
  if (!t.length) return n;
  const r = "rollback_conflict:", o = n.startsWith(r) ? n.slice(18).split(",").map((s) => s.trim()).filter(Boolean) : [];
  return `${r}${[.../* @__PURE__ */ new Set([...o, ...t])].join(",")}`;
}
function lu(e) {
  if (!e?.aborted) return;
  const t = /* @__PURE__ */ new Error("manager_aborted");
  throw t.name = "AbortError", t;
}
async function EZ(e = "", t = "manager_settle_timeout") {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = [];
  return Jd.forEach((o, s) => {
    o.sessionId === n && (r.push(s), o.controller.abort());
  }), r.forEach((o) => Jd.delete(o)), await Promise.all(r.map((o) => yo(o, {
    status: "cancelled",
    error: t
  }))), r;
}
async function AZ(e) {
  const t = Dg(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: 9e5
  }), n = e.executeManagerOnce ? null : bw(t, { missingApiKeyMessage: "请先在 API 配置里填写记忆管理员 API。" }), r = e.executeManagerOnce || ((p) => vZ(n, t, p)), o = !!n?.supportsSessionToolLoop || e.executeManagerOnce?.supportsSessionToolLoop === !0, s = e.caller === "auto" ? aZ(r1(), e.sessionContract) : r1(), a = [], i = [], l = /* @__PURE__ */ new Set(), u = /* @__PURE__ */ new Set(), d = /* @__PURE__ */ new Set();
  let f = "", m = "", v = !1, y = null, T = "", C = "", A = 0, I = !1;
  const R = (p) => {
    e.onProtocolEvent?.(p);
  };
  function $(p, b, w) {
    try {
      return `${String(p || "").trim()}|${CC(b)}|${String(w || "").trim()}`;
    } catch {
      return `${String(p || "").trim()}|${String(w || "").trim()}`;
    }
  }
  for (let p = 1; p <= 48; p += 1) {
    lu(e.signal);
    let b = "", w = [];
    const E = Lg({
      supportsSessionToolLoop: o,
      messages: e.messages,
      toolResponses: y,
      finalAnswerReminderText: T
    }), x = await r({
      agentConfig: e.agentConfig,
      messages: E.requestMessages,
      tools: s,
      toolChoice: "auto",
      ...E.mode === "session_tool_response_round" ? { toolResponses: E.toolResponses } : {},
      ...E.mode === "session_final_reminder_round" ? { finalAnswerReminderText: E.finalAnswerReminderText } : {},
      signal: e.signal,
      onStreamProgress: (B) => {
        typeof B.text == "string" && (b = B.text), Array.isArray(B.thoughts) && (w = Lm(B.thoughts)), e.onStreamProgress?.({
          ...typeof B.text == "string" ? { text: B.text } : {},
          ...Array.isArray(B.thoughts) ? { thoughts: w } : {},
          ...Array.isArray(B.toolCalls) ? { toolCalls: B.toolCalls } : {},
          ...B.toolCallDraft ? { toolCallDraft: !0 } : {}
        });
      }
    });
    y = null, T = "", lu(e.signal);
    const M = String(x.text || b || ""), N = M.trim();
    f = String(x.provider || f || t.provider || ""), m = String(x.model || m || t.model || "");
    const q = x, z = Lm(x.thoughts?.length ? x.thoughts : w), J = pR(q, t, { fallbackPrefix: "tavern-manager-tool" });
    if (!J.length) {
      if (!r0(N) && a.length && !v) {
        v = !0;
        const X = "工具结果已经齐备，请收束为本轮处理结论。";
        o ? T = X : e.messages.push({
          role: "system",
          content: X
        });
        continue;
      }
      const B = {
        role: "assistant",
        content: N,
        thoughts: z,
        providerPayload: x.providerPayload
      };
      return R({ type: "clear_stream_draft" }), R({
        type: "final_assistant",
        message: B
      }), {
        text: N,
        provider: f,
        model: m,
        toolTrace: a,
        changedFiles: [...l],
        changedStates: [...u],
        changedTasks: [...d],
        protocolMessages: [...i, B]
      };
    }
    const G = mR({
      ...q,
      text: M
    }, J, { fallbackPrefix: "tavern-manager-tool" });
    G.thoughts = z, G.toolCalls = J.map((B) => ({
      id: String(B.id || ""),
      name: String(B.name || ""),
      arguments: String(B.arguments || "{}")
    })), e.messages.push(G), i.push(G), R({ type: "clear_stream_draft" }), R({
      type: "assistant_tool_round",
      message: G
    });
    const oe = [];
    for (const [B, X] of J.entries()) {
      const de = pZ(X.arguments, {});
      lu(e.signal);
      const fe = {
        id: String(X.id || ""),
        round: p,
        name: X.name,
        status: "running",
        ok: !0,
        args: kC(de),
        path: "",
        summary: "工具运行中，等待返回。",
        preface: B === 0 ? N : "",
        thoughts: B === 0 ? z : [],
        startedAt: Date.now()
      };
      a.push(fe), await xC(e.managerRunId, a);
      const ve = e.caller === "auto" && !iZ(X.name, e.sessionContract) ? lZ(X.name, e.sessionContract) : bZ(X.name) ? await _M(e.sessionId, X.name, de, {
        caller: e.caller,
        managerRunId: e.managerRunId,
        turn: e.turn,
        sourceUserOrder: e.userOrder,
        sourceAssistantOrder: e.assistantOrder,
        beforeWriteGuard: e.beforeWriteGuard,
        contextSnapshot: e.contextSnapshot
      }) : yZ(X.name) ? await uB(e.sessionId, X.name, de, {
        caller: e.caller,
        managerRunId: e.managerRunId,
        sourceUserOrder: e.userOrder,
        sourceAssistantOrder: e.assistantOrder,
        beforeWriteGuard: e.beforeWriteGuard
      }) : _Z(X.name) ? await kB(e.sessionId, X.name, de, {
        caller: e.caller,
        managerRunId: e.managerRunId,
        sourceUserOrder: e.userOrder,
        sourceAssistantOrder: e.assistantOrder,
        beforeWriteGuard: e.beforeWriteGuard
      }) : {
        ok: !1,
        summary: `${X.name || "tool"} 不可用。`,
        changed: !1,
        error: "manager_tool_not_available"
      }, Ce = "path" in ve ? ve.path : "", Ue = "docType" in ve && ve.docType ? `${ve.docType}/${ve.docId || ""}` : "", le = "eventId" in ve && ve.eventId ? `event/${ve.eventId}` : "";
      ve.changed && Ce && l.add(Ce), ve.changed && Ue && u.add(Ue), ve.changed && le && d.add(le), Object.assign(fe, {
        status: "resolved",
        ok: ve.ok,
        args: kC(de),
        path: Ce || Ue || le,
        summary: IC(ve),
        error: ve.error || "",
        finishedAt: Date.now()
      }), Number(fe.startedAt) && (fe.elapsedMs = Math.max(0, Number(fe.finishedAt) - Number(fe.startedAt))), await xC(e.managerRunId, a);
      const me = gR({
        toolCallId: X.id,
        toolName: X.name,
        content: CC(ve)
      });
      if (me.toolCallId = String(X.id || ""), me.toolDisplay = IC(ve), e.messages.push(me), i.push(me), R({
        type: "tool_result",
        message: me
      }), oe.push({
        id: X.id,
        name: X.name,
        response: ve
      }), ve.ok)
        C = "", A = 0;
      else {
        const We = $(String(X.name || ""), de, String(ve.error || ""));
        We === C ? A += 1 : (C = We, A = 1);
      }
    }
    if (A >= 3) {
      if (I) throw new Error("manager_tool_repeat_loop");
      v = !0, I = !0;
      const B = "A tool keeps failing with the same arguments and error. Stop repeating it. Either adjust the arguments and strategy, or finish with a summary of what you verified, skipped, or left pending.";
      o ? T = B : e.messages.push({
        role: "system",
        content: B
      }), C = "", A = 0;
    }
    o && (y = oe);
  }
  throw new Error("工具轮次达到上限（48），已停止。");
}
async function Hh(e) {
  if (Number.isFinite(Number(e.autoManagerEpoch)) && await Ex(e.sessionId) !== Math.max(0, Math.floor(Number(e.autoManagerEpoch) || 0)))
    throw new Error("manager_epoch_expired");
  const [t, n] = await Promise.all([_u(e.sessionId, e.userMessage.order), _u(e.sessionId, e.assistantMessage.order)]), r = t?.role === "user" && t.error !== !0 && t.content === e.userMessage.content, o = n?.role === "assistant" && n.error !== !0 && !["aborted", "error"].includes(String(n.finishReason || "").trim()) && n.content === e.assistantMessage.content;
  if (!r || !o) throw new Error("manager_source_messages_changed");
}
async function CZ(e) {
  const t = Dg(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: 9e5
  }), n = {
    status: "running",
    provider: MR(String(t.provider || "")),
    model: String(t.model || ""),
    inputSummary: e.inputSummary
  }, r = e.managerRunId ? await yo(e.managerRunId, n) : await sm({
    sessionId: e.sessionId,
    trigger: e.trigger,
    turn: e.turn,
    userOrder: Number.isInteger(Number(e.userOrder)) ? Number(e.userOrder) : -1,
    assistantOrder: Number.isInteger(Number(e.assistantOrder)) ? Number(e.assistantOrder) : -1,
    ...n
  });
  if (!r) throw new Error("manager_run_missing");
  return r;
}
function kZ(e = "", t) {
  const n = String(e || "").trim();
  if (!n) return () => {
  };
  let r = !1;
  const o = setInterval(() => {
    r || t?.aborted || wF(n);
  }, 4e3);
  return () => {
    r = !0, clearInterval(o);
  };
}
async function Om(e, t) {
  return await yo(e.id, t) || e;
}
async function IZ(e) {
  const t = $R(e.sessionContract);
  t.includeMemoryFiles && await Ib(e.sessionId);
  const n = t.includeMemoryFiles ? await hs(e.sessionId, { includeStale: !0 }) : [], r = t.includeQuestOrchestration ? await xB(e.sessionId) : "";
  return [{
    role: "system",
    content: NR(e.assistantPreset, t.managerPromptOptions)
  }, {
    role: "user",
    content: mZ({
      turn: e.turn,
      userMessage: e.userMessage,
      assistantMessage: e.assistantMessage,
      memoryFiles: n,
      taskPoolBlock: r,
      runtime: t
    })
  }];
}
async function OR(e) {
  await Ib(e.sessionId);
  const t = await hs(e.sessionId, { includeStale: !0 }), n = Array.isArray(e.history) ? e.history : await fo(e.sessionId), r = [{
    role: "system",
    content: NR(e.assistantPreset)
  }];
  return n.forEach((o) => {
    const s = o.role === "assistant" && o.error !== !0 && !["aborted", "error"].includes(String(o.finishReason || "").trim()) && Array.isArray(o.toolCalls) ? o.toolCalls : [];
    r.push({
      role: o.role,
      content: String(o.content || ""),
      ...o.name ? { name: o.name } : {},
      ...Array.isArray(o.thoughts) ? { thoughts: o.thoughts } : {},
      ..."providerPayload" in o ? { providerPayload: o.providerPayload } : {},
      ...s.length ? {
        toolCalls: s,
        tool_calls: s.map((a) => ({
          id: a.id || "",
          type: "function",
          function: {
            name: a.name || "",
            arguments: a.arguments || "{}"
          }
        }))
      } : {},
      ...o.role === "tool" ? {
        tool_call_id: o.toolCallId || "",
        toolName: o.toolName || "",
        toolDisplay: o.toolDisplay
      } : {}
    });
  }), r.push({
    role: "user",
    content: gZ({
      question: e.question,
      memoryFiles: t
    })
  }), r;
}
async function FR(e) {
  const t = await CZ({
    managerRunId: e.managerRunId,
    sessionId: e.sessionId,
    trigger: e.trigger,
    turn: e.turn,
    userOrder: e.userOrder,
    assistantOrder: e.assistantOrder,
    inputSummary: e.inputSummary,
    agentConfig: e.agentConfig
  });
  let n = "", r = t.provider || "", o = t.model || "", s = [], a = [], i = [], l = [], u = [];
  const d = kZ(t.id, e.signal);
  try {
    const f = await AZ({
      sessionId: e.sessionId,
      agentConfig: e.agentConfig,
      caller: e.caller,
      messages: e.messages,
      managerRunId: t.id,
      turn: e.turn,
      userOrder: e.userOrder,
      assistantOrder: e.assistantOrder,
      beforeWriteGuard: e.beforeWriteGuard,
      sessionContract: e.sessionContract,
      contextSnapshot: e.contextSnapshot,
      signal: e.signal,
      executeManagerOnce: e.executeManagerOnce,
      onStreamProgress: e.onStreamProgress,
      onProtocolEvent: e.onProtocolEvent
    });
    if (n = f.text, r = f.provider || r, o = f.model || o, s = f.toolTrace, a = f.changedFiles, i = f.changedStates, l = f.changedTasks, u = f.protocolMessages, e.caller !== "auto" && wZ(s)) throw new Error("manager_memory_tool_failed");
    if (e.requireChangedFiles && !a.length) throw new Error("manager_memory_tool_required");
    await Xi(e.sessionId);
    const m = a.length + i.length + l.length, v = m ? `已维护 ${a.length} 个记忆文件、${i.length} 份结构化状态、${l.length} 条事件线索。` : "已检查并回复。";
    return {
      ok: !0,
      managerRun: await Om(t, {
        status: "completed",
        provider: r,
        model: o,
        outputText: n || v,
        parsedAction: m ? "manager_state_updated" : "memory_checked",
        toolTrace: s,
        changedFiles: a,
        changedStates: i,
        changedTasks: l,
        error: ""
      }),
      text: n,
      provider: r,
      model: o,
      toolTrace: s,
      changedFiles: a,
      changedStates: i,
      changedTasks: l,
      protocolMessages: u
    };
  } catch (f) {
    const m = f instanceof Error ? f.message : String(f || "manager_failed"), v = Aw(f, e.signal), y = await Om(t, {
      status: v,
      provider: r,
      model: o,
      outputText: n,
      toolTrace: s,
      changedFiles: a,
      changedStates: i,
      changedTasks: l,
      error: m
    }), T = e.caller === "auto" || ["cancelled", "superseded"].includes(v) ? await LR(t.id) : null;
    return T?.conflicts.length || await Xi(e.sessionId), {
      ok: !1,
      managerRun: T?.managerRun || y,
      text: n,
      provider: r,
      model: o,
      toolTrace: s,
      changedFiles: a,
      changedStates: i,
      changedTasks: l,
      protocolMessages: u,
      error: m
    };
  } finally {
    d();
  }
}
function xZ(e = []) {
  const t = [];
  let n = [];
  return (e || []).forEach((r) => {
    if (!(!r || ![
      "user",
      "assistant",
      "tool"
    ].includes(r.role))) {
      if (r.role === "user" && n.length) {
        t.push(n), n = [r];
        return;
      }
      n.push(r);
    }
  }), n.length && t.push(n), t.filter((r) => r.length);
}
function MC(e) {
  if (!e?.aborted) return;
  const t = /* @__PURE__ */ new Error("manager_chat_compaction_aborted");
  throw t.name = "AbortError", t;
}
async function PC(e) {
  const t = Dg(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: 9e5
  });
  return await fZ({
    messages: await OR(e),
    tools: r1(),
    providerConfig: t
  });
}
async function MZ(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return {
    compacted: !1,
    currentTokens: 0,
    history: []
  };
  MC(e.signal);
  const n = Array.isArray(e.history);
  let r = n ? [...e.history].sort((s, a) => s.order - a.order) : await fo(t), o = await PC({
    sessionId: t,
    agentConfig: e.agentConfig,
    assistantPreset: e.assistantPreset,
    question: e.question,
    history: r
  });
  if (o <= 158e3) return {
    compacted: !1,
    currentTokens: o,
    history: r
  };
  e.onCompactionStart?.({
    currentTokens: o,
    triggerTokens: Mc,
    status: "正在释放较早管理员对话，只保留最近管理上下文..."
  });
  for (const s of [2, 1]) {
    MC(e.signal);
    const a = xZ(r), i = Math.max(0, a.length - Math.min(s, a.length));
    if (i > 0) {
      e.onCompactionProgress?.({
        currentTokens: o,
        triggerTokens: Mc,
        preservedTurns: s,
        status: `正在只保留最近 ${s} 轮管理员上下文...`
      });
      const d = a.slice(0, i).flat().map((f) => f.order);
      d.length && await O_(t, d), r = n ? r.filter((f) => !d.includes(f.order)) : await fo(t);
    }
    const l = await PC({
      sessionId: t,
      agentConfig: e.agentConfig,
      assistantPreset: e.assistantPreset,
      question: e.question,
      history: r
    });
    o = l;
    const u = l <= 158e3 ? `已只保留最近 ${s} 轮管理员上下文。` : "最近管理员上下文仍然过长，继续收缩...";
    if (e.onCompactionProgress?.({
      currentTokens: o,
      yieldTokens: l,
      triggerTokens: Mc,
      preservedTurns: s,
      status: u
    }), l <= 158e3)
      return e.onCompactionComplete?.({
        currentTokens: o,
        yieldTokens: l,
        triggerTokens: Mc,
        preservedTurns: s,
        status: u
      }), {
        compacted: !0,
        currentTokens: l,
        history: r,
        preservedTurns: s
      };
  }
  return e.onCompactionUnable?.({
    currentTokens: o,
    triggerTokens: Mc,
    status: "当前管理员这一轮上下文本身已经过长，无法继续自动收缩。"
  }), {
    compacted: !1,
    currentTokens: o,
    history: r
  };
}
async function BR(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) throw new Error("manager_session_required");
  const n = $R(e.sessionContract);
  if (!n.hasAutomaticManagerWork) return {
    ok: !0,
    managerRun: e.managerRunId ? await yo(e.managerRunId, {
      status: "completed",
      outputText: "契约未授权后台记忆或地图维护，本轮已跳过。",
      parsedAction: "manager_skipped_by_contract",
      error: ""
    }) : await sm({
      sessionId: t,
      trigger: e.trigger || "after_turn",
      turn: e.turn,
      userOrder: e.userMessage.order,
      assistantOrder: e.assistantMessage.order,
      status: "completed",
      inputSummary: "contract skipped",
      outputText: "契约未授权后台记忆或地图维护，本轮已跳过。",
      parsedAction: "manager_skipped_by_contract"
    }),
    changedFiles: [],
    changedStates: [],
    changedTasks: []
  };
  const r = Ew({
    trigger: e.trigger || "after_turn",
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    text: e.userMessage.content
  }), o = e.managerRunId ? await yo(e.managerRunId, {
    status: "running",
    inputSummary: r
  }) : await sm({
    sessionId: t,
    trigger: e.trigger || "after_turn",
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    status: "queued",
    inputSummary: r
  });
  if (!o) throw new Error("manager_run_missing");
  try {
    await Hh(e);
    const s = await IZ(e), a = await FR({
      sessionId: t,
      agentConfig: e.agentConfig,
      trigger: e.trigger || "after_turn",
      turn: e.turn,
      userOrder: e.userMessage.order,
      assistantOrder: e.assistantMessage.order,
      inputSummary: r,
      messages: s,
      managerRunId: o.id,
      caller: "auto",
      requireChangedFiles: !1,
      beforeWriteGuard: async () => {
        lu(e.signal), await Hh(e);
      },
      sessionContract: e.sessionContract,
      contextSnapshot: e.contextSnapshot || e.assistantMessage.contextSnapshot || e.userMessage.contextSnapshot || {},
      signal: e.signal,
      executeManagerOnce: e.executeManagerOnce,
      onProtocolEvent: e.onProtocolEvent
    });
    if (!a.ok) return {
      ok: !1,
      managerRun: a.managerRun,
      error: a.error
    };
    await Hh(e);
    const i = [...a.changedFiles || []];
    let l = [...a.changedTasks || []], u = a.managerRun;
    if (n.includeQuestOrchestration) {
      const d = await IB(t, e.assistantMessage.order, {
        managerRunId: o.id,
        beforeWriteGuard: async () => {
          lu(e.signal), await Hh(e);
        }
      });
      if (d.length) {
        l = [.../* @__PURE__ */ new Set([...l, ...d.map((v) => `event/${v.id}`)])];
        const f = `系统已放弃 ${d.length} 条过期事件线索。`, m = String(u.outputText || "").trim();
        u = await Om(u, {
          outputText: m && m !== "已检查并回复。" ? `${m}
${f}` : f,
          parsedAction: "manager_state_updated",
          changedTasks: l
        });
      }
    }
    return {
      ok: !0,
      managerRun: u,
      changedFiles: i,
      changedStates: a.changedStates,
      changedTasks: l
    };
  } catch (s) {
    const a = s instanceof Error ? s.message : String(s || "manager_failed"), i = await Om(o, {
      status: Aw(s, e.signal),
      error: a
    }), l = await LR(o.id);
    return l?.conflicts.length || await Xi(t), {
      ok: !1,
      managerRun: l?.managerRun || i,
      error: a
    };
  }
}
async function PZ(e) {
  const t = String(e.sessionId || "").trim(), n = String(e.question || "").trim();
  if (!t) throw new Error("manager_session_required");
  if (!n) throw new Error("manager_question_required");
  const r = Array.isArray(e.history) ? e.history : await fo(t), o = await OR({
    sessionId: t,
    question: n,
    assistantPreset: e.assistantPreset,
    history: r
  }), s = await FR({
    sessionId: t,
    agentConfig: e.agentConfig,
    trigger: e.trigger || "manager_chat",
    turn: Math.max(0, Number(e.turn) || 0),
    inputSummary: Ew({
      trigger: "manager_chat",
      turn: e.turn,
      text: n
    }),
    messages: o,
    caller: "chat",
    requireChangedFiles: !1,
    signal: e.signal,
    executeManagerOnce: e.executeManagerOnce,
    onStreamProgress: e.onStreamProgress,
    onProtocolEvent: e.onProtocolEvent
  });
  return {
    ok: s.ok,
    managerRun: s.managerRun,
    text: s.text,
    provider: s.provider,
    model: s.model,
    changedFiles: s.changedFiles,
    changedStates: s.changedStates,
    changedTasks: s.changedTasks,
    protocolMessages: s.protocolMessages,
    error: s.error
  };
}
async function RZ(e) {
  const t = Number.isFinite(Number(e.autoManagerEpoch)) ? Math.max(0, Math.floor(Number(e.autoManagerEpoch) || 0)) : await Ex(e.sessionId), n = await sm({
    sessionId: e.sessionId,
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    trigger: e.trigger || "after_turn",
    status: "queued",
    inputSummary: Ew({
      trigger: e.trigger || "after_turn",
      turn: e.turn,
      userOrder: e.userMessage.order,
      assistantOrder: e.assistantMessage.order,
      text: e.userMessage.content
    })
  });
  await e.onManagerRunSaved?.(n);
  const r = new AbortController(), o = () => r.abort();
  e.signal?.aborted ? r.abort() : e.signal?.addEventListener("abort", o, { once: !0 }), Jd.set(n.id, {
    controller: r,
    sessionId: e.sessionId,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order
  });
  const s = (Ri.get(e.sessionId) || Promise.resolve()).catch(() => {
  }).then(async () => {
    lu(r.signal);
    const i = await BR({
      ...e,
      managerRunId: n.id,
      autoManagerEpoch: t,
      signal: r.signal
    });
    return await e.onManagerRunSaved?.(i.managerRun), i;
  }).catch(async (i) => {
    const l = i instanceof Error ? i.message : String(i || "manager_failed"), u = await yo(n.id, {
      status: Aw(i, r.signal),
      error: l
    }) || n;
    return await e.onManagerRunSaved?.(u), {
      ok: !1,
      managerRun: u,
      error: l
    };
  });
  Ri.set(e.sessionId, s), s.finally(() => {
    e.signal?.removeEventListener("abort", o), Jd.delete(n.id), Ri.get(e.sessionId) === s && Ri.delete(e.sessionId);
  });
  const a = e.awaitCompletion ? await s : null;
  return {
    managerRunId: n.id,
    managerStatus: a?.managerRun?.status || n.status,
    completion: s
  };
}
async function NZ(e = "", t = hZ) {
  const n = String(e || "").trim();
  if (!n) return {
    settled: !0,
    timedOut: !1
  };
  const r = Ri.get(n);
  if (!r) return {
    settled: !0,
    timedOut: !1
  };
  const o = Math.max(1, Number(t) || 2e4);
  let s = null;
  const a = new Promise((u) => {
    s = setTimeout(() => u("timeout"), o);
  }), i = await Promise.race([r.then(() => "settled").catch(() => "settled"), a]);
  s && clearTimeout(s);
  let l = [];
  return i === "timeout" && (await pF(n), l = await EZ(n), Ri.get(n) === r && Ri.delete(n)), {
    settled: i === "settled",
    timedOut: i === "timeout",
    ...l.length ? { abortedRunIds: l } : {}
  };
}
async function s0(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return {
    runIds: [],
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  Jd.forEach((l) => {
    l.sessionId === n && (l.userOrder >= r || l.assistantOrder >= r) && l.controller.abort();
  });
  const o = await EF(n, r);
  let s = 0, a = 0;
  const i = [];
  for (const l of o.runIds) {
    if (!(await sM(l)).some((d) => String(d.afterHash || "").trim())) continue;
    const u = await aM(l);
    s += u.rolledBack, a += u.skipped, i.push(...u.conflicts);
  }
  return {
    runIds: [.../* @__PURE__ */ new Set([...o.runIds])],
    rolledBack: o.rolledBack + s,
    conflicts: [...o.conflicts, ...i],
    skipped: o.skipped + a
  };
}
var $Z = /\[tavern-image:[a-z0-9\-_]+\]/gi, DZ = /\[(?:img|图片)\s*:\s*[^\]]+\]/gi, RC = 8;
function nl(e = "") {
  return String(e || "").replace($Z, "").replace(DZ, "").trim();
}
function UR(e) {
  return !e.error && !!nl(e.content);
}
function qR(e = "") {
  return !!nl(e);
}
function Hl(e = {}) {
  const t = [...e.messages || []].sort((i, l) => i.order - l.order), n = t.filter(UR), r = qR(e.currentUserMessage || "") ? 1 : 0;
  let o = Math.max(0, Math.floor(Number(e.contextWindowStartOrder) || 0));
  if (!n.length || n.length < 5) o = 0;
  else if (n.length + r <= 20) o = 0;
  else if (o > 0) {
    let i = n.filter((l) => l.order >= o);
    !n.some((l) => l.order === o) && i.length && (o = i[0].order, i = n.filter((l) => l.order >= o)), i.length < 5 && (o = n[Math.max(0, n.length - 10)]?.order || 0);
  }
  let s = o > 0 ? n.filter((i) => i.order >= o) : n;
  if (s.length + r > 20) {
    const i = Math.max(0, 10 - r);
    o = i > 0 && s.slice(-i)[0]?.order || 0, s = o > 0 ? n.filter((l) => l.order >= o) : n;
  }
  const a = o > 0 ? t.filter((i) => i.order >= o) : t;
  return {
    contextWindowStartOrder: o,
    historyMessages: a,
    usableHistoryCount: n.length,
    windowHistoryCount: s.length,
    currentUserCount: r
  };
}
async function LZ(e = "", t = 0, n = Number.POSITIVE_INFINITY) {
  const o = [];
  let s = 0;
  for (; ; ) {
    const a = await gF(e, t, n, 1e3, s);
    if (o.push(...a), a.length < 1e3) break;
    s += 1e3;
  }
  return o;
}
function OZ(e) {
  return e.filter(UR).length;
}
async function HR(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return Hl({
    messages: [],
    contextWindowStartOrder: e.contextWindowStartOrder,
    currentUserMessage: e.currentUserMessage
  });
  const n = Number.isFinite(Number(e.beforeOrder)), r = n ? Math.floor(Number(e.beforeOrder) || 0) : Number.POSITIVE_INFINITY;
  if (n && r <= 0) return Hl({
    messages: [],
    contextWindowStartOrder: e.contextWindowStartOrder,
    currentUserMessage: e.currentUserMessage
  });
  const o = n ? r - 1 : Number.POSITIVE_INFINITY, s = Math.max(0, Math.floor(Number(e.contextWindowStartOrder) || 0)), a = qR(e.currentUserMessage || "") ? 1 : 0, i = Math.max(20, 10 + a), l = Math.max(60, 60);
  if (s > 0) {
    const y = await nu(t, s, o, l, 0);
    if (y.total <= l) {
      const T = Hl({
        messages: y.messages,
        contextWindowStartOrder: s,
        currentUserMessage: e.currentUserMessage
      });
      if (T.contextWindowStartOrder >= s) return T;
    }
  }
  const u = /* @__PURE__ */ new Map();
  let d = 0, f = null, m = 0, v = 0;
  for (; ; ) {
    let y;
    if (n) {
      f === null && (f = (await nu(t, 0, o, 1, 0)).total);
      const C = Math.max(0, f - m), A = Math.min(l, C), I = Math.max(0, f - m - A);
      y = A > 0 ? await nu(t, 0, o, A, I) : {
        messages: [],
        total: f
      }, m += y.messages.length;
    } else
      y = await dg(t, l, d), d += l;
    v = y.total, y.messages.forEach((C) => u.set(C.order, C));
    const T = [...u.values()].sort((C, A) => C.order - A.order);
    if (T.length >= v || OZ(T) >= i) return Hl({
      messages: T,
      contextWindowStartOrder: s,
      currentUserMessage: e.currentUserMessage
    });
    if (!y.messages.length) return Hl({
      messages: T,
      contextWindowStartOrder: s,
      currentUserMessage: e.currentUserMessage
    });
  }
}
function FZ(e = []) {
  return e.filter((t) => t.role === "user").slice(-2).some((t) => h9(t.runtimeEvents));
}
function BZ(e) {
  return Number.isFinite(e) && e < 0.1;
}
function UZ(e) {
  if (!e.runtime.includeRandomEncounters) return null;
  const t = P0(e.reusedUserMessage?.runtimeEvents);
  return t || (e.reusedUserMessage && e.rerollRuntimeEvents !== !0 || FZ(e.reusedUserMessage ? e.historyMessages : e.sessionMessages) ? null : BZ(e.randomEncounterRoll ? e.randomEncounterRoll() : Math.random()) ? d9() : null);
}
function zR(e) {
  return e.includeActionChecks ? {
    tools: yR(),
    toolChoice: "auto"
  } : {
    tools: [],
    toolChoice: "none"
  };
}
function WR(e) {
  return e.includeActionChecks ? [PQ()] : [];
}
function qZ(e) {
  return e ? [{
    content: pI().content,
    depth: 1,
    role: "system",
    order: 1e9,
    label: "chance encounter",
    layer: "runtime-event"
  }] : [];
}
function HZ(e = {}) {
  const t = Array.isArray(e.memoryFiles) ? e.memoryFiles : [], n = Array.isArray(e.structuredStates) ? e.structuredStates : [], r = String(e.spatialState || "").trim(), o = Array.isArray(e.questHooks) ? e.questHooks.map((d) => String(d || "").trim()).filter(Boolean) : [], s = [];
  o.length && s.push(o.join(`
`));
  const a = t.find((d) => String(d.path || "") === "memory/state.md"), i = String(a?.content || "").trim();
  i && s.push(`## 会话记忆
${i}`);
  const l = t.filter((d) => String(d.path || "").startsWith("memory/characters/")).map((d) => {
    const f = String(d.path || "").slice(18).replace(/\.md$/i, ""), m = String(d.title || f || "相关人物").trim(), v = String(d.content || "").trim();
    return v ? `### ${m}
${v}` : "";
  }).filter(Boolean);
  l.length && s.push(`## 相关人物记忆
${l.join(`

`)}`);
  const u = r ? [] : n.map((d) => String(d.digest || "").trim()).filter(Boolean);
  return u.length && s.push(`## 状态摘要
${u.join(`

`)}`), r && s.push(`## 空间状态
${r}`), s.join(`

`);
}
function zZ(e = []) {
  return e.map((t) => String(t.content || "").trim()).filter(Boolean).join(`

`);
}
function GR(e = []) {
  if (!e.length) return [];
  let t = -1;
  for (let n = e.length - 1; n >= 0; n -= 1) if (e[n]?.role === "assistant") {
    t = n;
    break;
  }
  return t < 0 ? e : e.map((n, r) => {
    if (r !== t) return n;
    const o = String(n.content || "").trimEnd();
    return o === n.content ? n : {
      ...n,
      content: o
    };
  });
}
function WZ(e = [], t = "sillytavern-native") {
  return e.map((n, r) => {
    const o = String(n.content || "").length;
    return {
      index: r,
      role: n.role,
      layer: t,
      label: `${t} ${r + 1}`,
      chars: o,
      tokenEstimate: Math.max(1, Math.ceil(o / 4))
    };
  });
}
function GZ(e, t = [], n = "sillytavern-native") {
  const r = GR(t).map((o) => ({
    ...o,
    content: String(o.content || "")
  })).filter((o) => o.content);
  return {
    ...e,
    messages: r,
    messageLayers: WZ(r, n),
    meta: {
      ...e.meta,
      rawMessagesJson: JSON.stringify(r, null, 2)
    }
  };
}
function VZ(e) {
  const t = "error" in e ? e.error : "";
  return String(e.summary || t || "").trim();
}
async function VR(e) {
  if (!e.buildNativeChatPrompt) return {
    buildResult: e.baseBuildResult,
    buildSnapshot: e.baseBuildSnapshot
  };
  const t = await xn(e.stage, () => e.buildNativeChatPrompt?.({
    context: e.contextForBuild,
    chatPreset: e.chatPreset,
    currentUserMessage: e.currentUserMessage,
    generationType: e.generationType,
    debugStage: e.stage,
    signal: e.signal,
    memoryPrompt: HZ(e.memoryContext),
    chancePrompt: e.chancePrompt || "",
    actionCheckPrompt: zZ(e.runtimeProtocolMessages || [])
  })), n = Array.isArray(t?.messages) ? t.messages : [];
  if (!n.length) return {
    buildResult: e.baseBuildResult,
    buildSnapshot: e.baseBuildSnapshot
  };
  const r = GZ(e.baseBuildResult, n, "sillytavern-native");
  return {
    buildResult: r,
    buildSnapshot: H0(e.contextForBuild, e.chatPreset, r, {
      ...e.diagnostics && typeof e.diagnostics == "object" ? e.diagnostics : {},
      promptSource: t?.source || "sillytavern-prepareOpenAIMessages",
      promptMessageCount: t?.promptMessageCount ?? n.length
    })
  };
}
function KZ(e, t) {
  if (t?.aborted) return !0;
  if (!e || typeof e != "object") return !1;
  const n = e, r = String(n.name || ""), o = String(n.code || ""), s = String(n.message || "");
  return r === "AbortError" || o === "ABORT_ERR" || /abort|aborted|cancelled|canceled/i.test(s);
}
function JZ(e, t) {
  if (t instanceof Error && /^\[xb-tavern:[^\]]+\]/.test(t.message)) return t;
  const n = t instanceof Error ? t.message : String(t || "unknown_error"), r = /* @__PURE__ */ new Error(`[xb-tavern:${e}] ${n}`);
  return r.name = t instanceof Error ? t.name : "Error", t instanceof Error && t.stack && (r.stack = `${r.name}: ${r.message}
Caused by: ${t.stack}`), r;
}
function YZ(e) {
  const t = String(e || "run_failed");
  return /^\[xb-tavern:provider_chat\]\s*Failed to fetch\b/i.test(t) && !/切换酒馆补全源/.test(t) ? `${t}

可以尝试在 API 配置中切换酒馆补全源。` : t;
}
async function xn(e, t) {
  const n = typeof performance < "u" && typeof performance.now == "function" ? performance.now() : Date.now();
  console.info("[小白酒馆] turn stage start", { stage: e });
  try {
    const r = await t(), o = typeof performance < "u" && typeof performance.now == "function" ? performance.now() : Date.now();
    return console.info("[小白酒馆] turn stage end", {
      stage: e,
      ms: Math.round(o - n)
    }), r;
  } catch (r) {
    const o = typeof performance < "u" && typeof performance.now == "function" ? performance.now() : Date.now();
    throw console.info("[小白酒馆] turn stage failed", {
      stage: e,
      ms: Math.round(o - n),
      error: r instanceof Error ? r.message : String(r || "unknown_error")
    }), JZ(e, r);
  }
}
async function ma(e) {
  if (e)
    try {
      await e();
    } catch (t) {
      console.warn("[小白酒馆] run callback failed", t);
    }
}
async function qy(e, t, n = {}) {
  return n.replace ? await Tx(e, t) : await Sx(e, t);
}
function a0(e) {
  const t = String(e?.character?.name || "").trim();
  return !!t && !/^(sillytavern\s+system|system)\b/i.test(t);
}
function KR(e, t = {}) {
  return a0(t) ? t || {} : a0(e?.contextSnapshot) ? e?.contextSnapshot || {} : t || e?.contextSnapshot || {};
}
function i0(e = {}) {
  if (!a0(e))
    throw new Error("当前没有可用角色，请先选择角色或刷新当前会话。");
}
function wf(e = {}) {
  return e.chatPreset || e.preset || {};
}
function JR(e) {
  return mo(e?.contract);
}
function XZ(e, t) {
  const n = e && typeof e == "object" && !Array.isArray(e) ? e : {};
  return sr({
    ...n,
    contract: lg(n.contract) ? om(t, n.contract) : om(t, void 0)
  });
}
function YR(e, t) {
  if (!e) return e;
  if (!t.includeMemoryFiles && !t.includeStructuredStates && !t.includeQuestOrchestration) return {};
  const n = {};
  return t.includeMemoryFiles && Array.isArray(e.memoryFiles) && (n.memoryFiles = e.memoryFiles), t.includeStructuredStates && Array.isArray(e.structuredStates) && (n.structuredStates = e.structuredStates), t.includeStructuredStates && e.spatialState && (n.spatialState = e.spatialState), t.includeQuestOrchestration && Array.isArray(e.questHooks) && (n.questHooks = e.questHooks), n;
}
function Ls(e, t) {
  t && [
    "userInput",
    "worldInfo",
    "aiOutput",
    "reasoning"
  ].forEach((n) => {
    const r = Number(t[n]) || 0;
    r > 0 && (e[n] = (e[n] || 0) + r);
  });
}
function QZ(e = []) {
  return e.map((t) => ({
    id: t.id,
    text: t.text,
    changed: !1
  }));
}
async function Sf(e, t = []) {
  if (!t.length) return [];
  if (!e) return QZ(t);
  const n = await e(t), r = new Map((Array.isArray(n.items) ? n.items : []).map((o) => [o.id, o]));
  return t.map((o) => r.get(o.id) || {
    id: o.id,
    text: o.text,
    changed: !1
  });
}
async function Ap(e) {
  const [t] = await Sf(e.applyRegex, [{
    id: `${e.placement}:${e.id}`,
    text: e.text,
    placement: e.placement,
    options: e.options
  }]);
  return {
    text: t?.text ?? e.text,
    summary: bf(t ? [t] : [])
  };
}
async function ZZ(e) {
  const t = Array.isArray(e.thoughts) ? e.thoughts : [], n = t.map((s, a) => ({
    id: `reasoning:${a}`,
    text: String(s.text || ""),
    placement: "reasoning"
  })).filter((s) => s.text);
  if (!n.length) return { thoughts: e.thoughts };
  const r = await Sf(e.applyRegex, n), o = new Map(r.map((s) => [s.id, s]));
  return {
    thoughts: t.map((s, a) => ({
      ...s,
      text: o.get(`reasoning:${a}`)?.text ?? s.text
    })),
    summary: bf(r)
  };
}
async function XR(e) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = t.reduce((s, a, i) => {
    const l = t.length - i - 1;
    if (["user", "assistant"].includes(a.role) && String(a.content || "")) {
      const u = a.role === "user" ? "userInput" : "aiOutput";
      s.push({
        id: `${u}:prompt:${i}`,
        text: String(a.content || ""),
        placement: u,
        options: {
          isPrompt: !0,
          depth: l
        }
      });
    }
    return a.role === "assistant" && Array.isArray(a.thoughts) && a.thoughts.forEach((u, d) => {
      const f = String(u?.text || "");
      f && s.push({
        id: `reasoning:prompt:${i}:${d}`,
        text: f,
        placement: "reasoning",
        options: {
          isPrompt: !0,
          depth: l
        }
      });
    }), s;
  }, []);
  if (!n.length) return { messages: t };
  const r = await Sf(e.applyRegex, n), o = new Map(r.map((s) => [s.id, s]));
  return {
    messages: t.map((s, a) => {
      const i = s.role === "user" ? "userInput" : s.role === "assistant" ? "aiOutput" : "", l = i ? o.get(`${i}:prompt:${a}`) : null, u = Array.isArray(s.thoughts) ? s.thoughts.map((d, f) => ({
        ...d,
        text: o.get(`reasoning:prompt:${a}:${f}`)?.text ?? d.text
      })) : s.thoughts;
      return {
        ...s,
        ...l ? { content: l.text } : {},
        ...u ? { thoughts: u } : {}
      };
    }),
    summary: bf(r)
  };
}
function jZ(e = []) {
  return e.map((t) => ({
    id: t.id,
    text: t.text,
    changed: !1
  }));
}
async function Cw(e, t = []) {
  if (!t.length) return [];
  if (!e) return jZ(t);
  const n = await e(t), r = new Map((Array.isArray(n.items) ? n.items : []).map((o) => [o.id, o]));
  return t.map((o) => r.get(o.id) || {
    id: o.id,
    text: o.text,
    changed: !1
  });
}
function kw(e = {}) {
  const t = {}, n = String(e.user?.name || "").trim(), r = String(e.character?.name || "").trim();
  return n && (t.name1Override = n), r && (t.name2Override = r), t;
}
async function QR(e) {
  const [t] = await Cw(e.applySubstituteParams, [{
    id: e.id,
    text: e.text,
    options: e.options
  }]);
  return t?.text ?? e.text;
}
function ej(e) {
  return Array.isArray(e) ? e.map((t) => String(t || "")) : e == null ? [] : [String(e)];
}
function tj(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function ZR(e) {
  return Array.isArray(e) ? e.filter(tj) : [];
}
async function NC(e) {
  const t = ZR(e.entries), n = [], r = [];
  t.forEach((i, l) => {
    const u = String(i.content || "");
    if (u) {
      const d = `${e.scope}:${l}:content`;
      r.push({
        entryIndex: l,
        field: "content",
        id: d
      }), n.push({
        id: d,
        text: u,
        options: e.options
      });
    }
    [
      "key",
      "keysecondary",
      "secondary_keys"
    ].forEach((d) => {
      ej(i[d]).forEach((f, m) => {
        if (!f) return;
        const v = `${e.scope}:${l}:${d}:${m}`;
        r.push({
          entryIndex: l,
          field: d,
          valueIndex: m,
          id: v
        }), n.push({
          id: v,
          text: f,
          options: e.options
        });
      });
    });
  });
  const o = await Cw(e.applySubstituteParams, n), s = new Map(o.map((i) => [i.id, i.text])), a = t.map((i) => ({ ...i }));
  return r.forEach((i) => {
    const l = s.get(i.id);
    if (l === void 0) return;
    const u = a[i.entryIndex];
    if (!u) return;
    if (i.field === "content") {
      u.content = l;
      return;
    }
    const d = u[i.field];
    if (Array.isArray(d)) {
      const f = [...d];
      f[i.valueIndex || 0] = l, u[i.field] = f;
    } else u[i.field] = l;
  }), a;
}
async function Iw(e) {
  if (!e.applySubstituteParams) return e.context;
  const t = {
    ...e.context,
    worldEntries: Array.isArray(e.context.worldEntries) ? await NC({
      applySubstituteParams: e.applySubstituteParams,
      entries: e.context.worldEntries,
      options: e.options,
      scope: "worldEntries"
    }) : e.context.worldEntries
  };
  return Array.isArray(e.context.worldBooks) && (t.worldBooks = await Promise.all(ZR(e.context.worldBooks).map(async (n, r) => ({
    ...n,
    entries: await NC({
      applySubstituteParams: e.applySubstituteParams,
      entries: n.entries,
      options: e.options,
      scope: `worldBooks:${r}`
    })
  })))), t;
}
async function Fm(e) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  t.forEach((s, a) => {
    const i = String(s.content || "");
    i && n.push({
      id: `message:${a}`,
      text: i,
      options: e.options
    }), s.role === "assistant" && Array.isArray(s.thoughts) && s.thoughts.forEach((l, u) => {
      const d = String(l?.text || "");
      d && n.push({
        id: `thought:${a}:${u}`,
        text: d,
        options: e.options
      });
    });
  });
  const r = await Cw(e.applySubstituteParams, n), o = new Map(r.map((s) => [s.id, s.text]));
  return t.map((s, a) => {
    const i = Array.isArray(s.thoughts) ? s.thoughts.map((l, u) => ({
      ...l,
      text: o.get(`thought:${a}:${u}`) ?? l.text
    })) : s.thoughts;
    return {
      ...s,
      content: o.get(`message:${a}`) ?? s.content,
      ...i ? { thoughts: i } : {}
    };
  });
}
function Td(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {}, n = (r) => {
    if (!r || typeof r != "object" || Array.isArray(r)) return {};
    const o = {};
    return Object.entries(r).forEach(([s, a]) => {
      if (!s || !a || typeof a != "object" || Array.isArray(a)) return;
      const i = a, l = {}, u = Number(i.hash), d = Number(i.start), f = Number(i.end);
      Number.isFinite(u) && (l.hash = u), Number.isFinite(d) && (l.start = d), Number.isFinite(f) && (l.end = f), i.protected === !0 && (l.protected = !0), Object.keys(l).length && (o[s] = l);
    }), o;
  };
  return {
    sticky: n(t.sticky),
    cooldown: n(t.cooldown)
  };
}
async function xw(e) {
  const t = Td(e.timedState);
  if (!e.getNativeWorldInfoRuntime) return {
    context: e.context,
    timedState: t
  };
  const n = await e.getNativeWorldInfoRuntime({
    context: e.context,
    currentUserMessage: e.currentUserMessage,
    trigger: e.trigger,
    timedState: t
  });
  return {
    context: {
      ...e.context,
      nativeWorldInfo: n
    },
    timedState: Td(n?.timedState)
  };
}
function jR(e, t = !1) {
  return !t;
}
function $C(e = {}, t, n = !1) {
  const r = e.worldEntryStates || {}, o = t.meta.worldEntryStateUpdates || {};
  return n ? fb(r, o) : o;
}
function nj(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = {
      role: t.role,
      content: String(t.content || "")
    };
    return t.name && (n.name = String(t.name)), Array.isArray(t.tool_calls) && t.tool_calls.length && (n.tool_calls = t.tool_calls.map((r) => ({
      ...r.id ? { id: String(r.id) } : {},
      ...r.type ? { type: String(r.type) } : {},
      ...r.function ? { function: {
        name: String(r.function.name || ""),
        arguments: String(r.function.arguments || "")
      } } : {}
    }))), Array.isArray(t.toolCalls) && t.toolCalls.length && (n.toolCalls = t.toolCalls.map((r) => ({
      ...r.id ? { id: String(r.id) } : {},
      name: String(r.name || ""),
      arguments: String(r.arguments || "")
    }))), t.tool_call_id && (n.tool_call_id = String(t.tool_call_id)), t.toolCallId && (n.toolCallId = String(t.toolCallId)), t.toolName && (n.toolName = String(t.toolName)), n;
  });
}
function Tu(e = {}, t = [], n = {}) {
  const r = Tw(e), o = n.requestInspection || null, s = String(n.chatPreset?.name || "").trim(), a = nj(t), i = JSON.stringify(a, null, 2), l = o || {
    provider: String(n.provider || r.provider || ""),
    model: String(n.model || r.model || ""),
    transport: "unavailable",
    request: n.requestTask || { messages: a }
  }, u = JSON.stringify(l, null, 2);
  return {
    presetName: s || r.currentPresetName,
    chatPresetName: s,
    apiPresetName: r.currentPresetName,
    provider: String(n.provider || r.provider || ""),
    providerLabel: r.providerLabel,
    model: String(n.model || r.model || ""),
    toolMode: r.toolMode,
    messageCount: a.length,
    messageChars: a.reduce((d, f) => d + String(f.content || "").length, 0),
    rawMessagesJson: i,
    rawRequestJson: u,
    requestKind: n.requestKind || "actual",
    capturedAt: Date.now(),
    ..._R(n.regexApplications) ? { regexApplications: n.regexApplications } : {},
    ...o ? { requestInspection: o } : {}
  };
}
async function Mw(e) {
  const t = e.providerConfig || PR(e.agentConfig), n = GR(e.messages), r = KQ(t, {
    tools: Array.isArray(e.tools) ? e.tools : [],
    toolChoice: e.toolChoice || (Array.isArray(e.tools) && e.tools.length ? "auto" : "none")
  }), o = e.adapter || bw(t, { missingApiKeyMessage: "请先在 API 配置里选择模型/填写 Key。" }), s = r.buildChatTask({
    messages: n,
    signal: e.signal,
    onStreamProgress: e.onStreamProgress
  }), a = Lg({
    supportsSessionToolLoop: o.supportsSessionToolLoop === !0,
    messages: n,
    toolResponses: e.toolResponses,
    finalAnswerReminderText: e.finalAnswerReminderText
  });
  RR(s, a);
  const i = dZ(a), l = i && typeof o.inspectSendRequest == "function" ? await o.inspectSendRequest(i, s) : typeof o.inspectRequest == "function" ? await o.inspectRequest(s) : null;
  return {
    task: s,
    adapter: o,
    providerConfig: t,
    requestSnapshot: Tu(e.agentConfig, a.requestMessages, {
      provider: String(l?.provider || t.provider || ""),
      model: String(l?.model || t.model || ""),
      requestInspection: l,
      requestKind: e.requestKind || "actual",
      chatPreset: e.chatPreset,
      regexApplications: e.regexApplications,
      requestTask: s
    }),
    snapshotMessages: a.requestMessages
  };
}
function Tf(e = []) {
  return e.filter((t) => !t.error).map((t) => ({
    role: [
      "system",
      "user",
      "assistant",
      "tool"
    ].includes(t.role) ? t.role : "assistant",
    content: nl(t.content),
    ...t.name ? { name: t.name } : {},
    ...Array.isArray(t.thoughts) && t.thoughts.length ? { thoughts: t.thoughts } : {}
  })).filter((t) => String(t.content || "").trim());
}
function e5(e = [], t = -1) {
  if (t < 0) return null;
  for (let n = t + 1; n < e.length; n += 1) {
    const r = e[n];
    if (r.role === "user") break;
    if (r.role === "assistant" && !r.error && String(r.content || "").trim()) return r;
  }
  return null;
}
function rj(e) {
  const t = wf(e), n = [...e.messages || []].sort((f, m) => f.order - m.order), r = e.contextSnapshot || {}, o = [];
  let s = 0, a, i, l = "", u = "";
  n.forEach((f, m) => {
    const v = nl(f.content);
    if (f.role !== "user" || f.error || !v) {
      o.push(f);
      return;
    }
    const y = e5(n, m);
    if (y) {
      const T = PI({
        context: {
          ...r,
          history: Tf(o)
        },
        chatPreset: t,
        currentUserMessage: v,
        historyMode: e.historyMode || "raw",
        turn: s,
        entryStates: {},
        diagnostics: e.diagnostics || {}
      });
      s += 1, a = T.buildSnapshot, i = y.requestSnapshot || f.requestSnapshot, l = String(y.provider || ""), u = String(y.model || "");
    }
    o.push(f);
  });
  const d = n[n.length - 1];
  return {
    turn: s,
    worldEntryStates: {},
    nativeWorldInfoTimedState: {
      sticky: {},
      cooldown: {}
    },
    lastBuildSnapshot: a,
    lastRequestSnapshot: i,
    lastProvider: l,
    lastModel: u,
    lastError: d?.error ? String(d.content || "") : ""
  };
}
async function t5(e) {
  if (!e.applySubstituteParams && !e.getNativeWorldInfoRuntime) return rj(e);
  const t = wf(e), n = [...e.messages || []].sort((y, T) => y.order - T.order), r = e.contextSnapshot || {}, o = kw(r), s = [];
  let a = 0, i = {}, l = {
    sticky: {},
    cooldown: {}
  }, u, d, f = "", m = "";
  for (let y = 0; y < n.length; y += 1) {
    const T = n[y], C = nl(T.content);
    if (T.role !== "user" || T.error || !C) {
      s.push(T);
      continue;
    }
    const A = e5(n, y);
    if (A) {
      const I = String(A.buildSnapshot?.nativeWorldInfo?.trigger || T.buildSnapshot?.nativeWorldInfo?.trigger || "normal"), R = {
        ...r,
        worldSettings: {
          ...r.worldSettings || {},
          trigger: I
        },
        history: Tf(s)
      }, $ = await xw({
        getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
        context: R,
        currentUserMessage: C,
        trigger: I,
        timedState: l
      });
      l = $.timedState;
      const p = await z0({
        context: await Iw({
          applySubstituteParams: e.applySubstituteParams,
          context: $.context,
          options: o
        }),
        chatPreset: t,
        currentUserMessage: C,
        historyMode: e.historyMode || "raw",
        turn: a,
        entryStates: i,
        diagnostics: e.diagnostics || {}
      });
      i = fb(i, p.buildResult.meta.worldEntryStateUpdates), a += 1, u = p.buildSnapshot, d = A.requestSnapshot || T.requestSnapshot, f = String(A.provider || ""), m = String(A.model || "");
    }
    s.push(T);
  }
  const v = n[n.length - 1];
  return {
    turn: a,
    worldEntryStates: i,
    nativeWorldInfoTimedState: l,
    lastBuildSnapshot: u,
    lastRequestSnapshot: d,
    lastProvider: f,
    lastModel: m,
    lastError: v?.error ? String(v.content || "") : ""
  };
}
async function oj(e, t) {
  const n = await fs(e.sessionId || "");
  if (n) return n;
  const r = wf(e), o = e.contextSnapshot || {}, s = o.character || {}, a = sr(e.runtimeState || {});
  return await wx({
    title: String(s.name || "未选择角色"),
    characterId: String(s.id || ""),
    characterName: String(s.name || "未选择角色"),
    contextSnapshot: o,
    buildSnapshot: t,
    chatPresetId: String(r.id || ""),
    chatPresetName: String(r.name || ""),
    presetId: String(r.id || ""),
    presetName: String(r.name || ""),
    state: {
      turn: 0,
      contract: a.contract,
      worldEntryStates: {},
      nativeWorldInfoTimedState: {
        sticky: {},
        cooldown: {}
      }
    }
  });
}
async function sj(e, t, n) {
  const r = await xn("provider_request_inspection", () => Mw({
    agentConfig: n.agentConfig,
    messages: n.messages,
    chatPreset: n.chatPreset,
    regexApplications: n.regexApplications,
    tools: n.tools,
    toolChoice: n.toolChoice,
    toolResponses: n.toolResponses,
    finalAnswerReminderText: n.finalAnswerReminderText,
    signal: n.signal,
    onStreamProgress: n.onStreamProgress,
    requestKind: "actual",
    providerConfig: t,
    adapter: e
  }));
  let o;
  try {
    o = await xn("provider_chat", () => r.adapter.chat(r.task));
  } catch (d) {
    const f = d?.requestInspection;
    throw f && d && typeof d == "object" && (d.requestSnapshot = Tu(n.agentConfig, r.snapshotMessages, {
      provider: String(f.provider || r.providerConfig.provider || ""),
      model: String(f.model || r.providerConfig.model || ""),
      requestInspection: f,
      requestKind: "actual",
      chatPreset: n.chatPreset,
      regexApplications: n.regexApplications,
      requestTask: r.task
    })), d;
  }
  const s = o?.requestInspection || r.requestSnapshot.requestInspection || null, a = String(o?.text || ""), i = String(o?.provider || s?.provider || r.providerConfig.provider || ""), l = String(o?.model || s?.model || r.providerConfig.model || ""), u = pR(o || {}, r.providerConfig, { fallbackPrefix: "tavern-rp-tool" });
  return {
    text: a,
    thoughts: o?.thoughts,
    model: l,
    provider: i,
    finishReason: o?.finishReason,
    providerPayload: o?.providerPayload,
    toolCalls: u,
    requestSnapshot: Tu(n.agentConfig, r.snapshotMessages, {
      provider: i,
      model: l,
      requestInspection: s,
      requestKind: "actual",
      chatPreset: n.chatPreset,
      regexApplications: n.regexApplications,
      requestTask: r.task
    })
  };
}
async function aj(e) {
  const t = wf(e), n = e.sessionId ? await fs(e.sessionId) : null, r = KR(n, e.contextSnapshot);
  i0(r);
  const o = sr(n?.state || e.runtimeState || {}), s = ug(JR(o)), a = zR(s), i = await xn("simulate_user_input_regex", () => Ap({
    applyRegex: e.applyRegex,
    placement: "userInput",
    id: "simulate",
    text: e.currentUserMessage
  })), l = {};
  Ls(l, i.summary);
  const u = kw(r), d = nl(await xn("simulate_user_input_substitute", () => QR({
    applySubstituteParams: e.applySubstituteParams,
    id: "userInput:simulate",
    text: i.text,
    options: u
  }))), f = n ? await HR({
    sessionId: n.id,
    contextWindowStartOrder: o.contextWindowStartOrder,
    currentUserMessage: d
  }) : null, m = {
    ...r,
    worldSettings: {
      ...r.worldSettings || {},
      trigger: String(e.generationTrigger || "normal")
    },
    history: n ? Tf(f?.historyMessages || []) : e.contextSnapshot.history || []
  }, v = await xn("simulate_native_worldbook_runtime", () => xw({
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
    context: m,
    currentUserMessage: d,
    trigger: String(e.generationTrigger || "normal"),
    timedState: o.nativeWorldInfoTimedState
  })), y = await xn("simulate_world_entry_substitute", () => Iw({
    applySubstituteParams: e.applySubstituteParams,
    context: v.context,
    options: u
  })), T = await xn("simulate_memory_query", () => AR(y, d)), C = n && (s.includeMemoryFiles || s.includeStructuredStates) ? await xn("simulate_memory_retrieval", () => CR({
    sessionId: n.id,
    queryText: T,
    ignoredTerms: Sw(y),
    includeMemoryFiles: s.includeMemoryFiles,
    includeStructuredStates: s.includeStructuredStates
  })) : void 0, A = n && s.includeQuestOrchestration ? await xn("simulate_quest_hook_retrieval", () => iM(n.id, 1)) : [], I = YR(C || A.length ? {
    ...C || {},
    ...A.length ? { questHooks: A } : {}
  } : void 0, s), R = WR(s), $ = await xn("simulate_brain_build", () => z0({
    context: y,
    chatPreset: t,
    currentUserMessage: d,
    historyMode: e.historyMode || "raw",
    turn: o.turn,
    entryStates: o.worldEntryStates,
    memoryContext: I,
    runtimeProtocolMessages: R,
    diagnostics: e.diagnostics || {},
    regexApplications: l,
    transformConversationMessages: async (M) => {
      const N = await Fm({
        applySubstituteParams: e.applySubstituteParams,
        messages: M,
        options: u
      }), q = await XR({
        applyRegex: e.applyRegex,
        messages: N
      });
      return Ls(l, q.summary), q.messages;
    },
    transformFinalMessages: async (M) => Fm({
      applySubstituteParams: e.applySubstituteParams,
      messages: M,
      options: u
    }),
    transformWorldEntries: async (M) => {
      const N = !!y.nativeWorldInfo, q = M.filter((G) => jR(G, N)), z = await Sf(e.applyRegex, q.map((G) => ({
        id: `worldInfo:${G.activationKey}`,
        text: G.content,
        placement: "worldInfo",
        options: {
          isMarkdown: !1,
          isPrompt: !0,
          depth: G.position === Zm.atDepth ? G.depth : null
        }
      })));
      Ls(l, bf(z));
      const J = new Map(z.map((G) => [G.id, G]));
      return M.map((G) => {
        const oe = J.get(`worldInfo:${G.activationKey}`)?.text ?? G.content;
        return {
          ...G,
          content: oe,
          contentChars: oe.length
        };
      });
    }
  })), { buildResult: p, buildSnapshot: b } = await VR({
    stage: "simulate_native_prompt_build",
    buildNativeChatPrompt: e.buildNativeChatPrompt,
    contextForBuild: y,
    chatPreset: t,
    baseBuildResult: $.buildResult,
    baseBuildSnapshot: $.buildSnapshot,
    currentUserMessage: d,
    generationType: String(e.generationTrigger || "normal"),
    memoryContext: I,
    runtimeProtocolMessages: R,
    diagnostics: e.diagnostics
  }), w = await xn("simulate_request_inspection", () => Mw({
    agentConfig: e.agentConfig,
    messages: p.messages,
    chatPreset: t,
    tools: a.tools,
    toolChoice: a.toolChoice,
    onStreamProgress: () => {
    },
    requestKind: "simulated",
    regexApplications: l
  })), E = w.requestSnapshot.provider, x = w.requestSnapshot.model;
  return {
    buildResult: p,
    buildSnapshot: b,
    requestSnapshot: w.requestSnapshot,
    provider: E,
    model: x
  };
}
function ij(e, t = {}) {
  if (e && typeof e == "object" && !Array.isArray(e)) return e;
  try {
    const n = JSON.parse(String(e || "{}"));
    return n && typeof n == "object" && !Array.isArray(n) ? n : t;
  } catch {
    return t;
  }
}
function lj(e) {
  try {
    return JSON.stringify(e || {});
  } catch {
    return "{}";
  }
}
async function uj(e) {
  const t = yR(), n = [...e.messages], r = [], o = cj(e.agentConfig, e.executeRunOnce);
  let s = "", a, i = "", l = "", u = "", d, f = Tu(e.agentConfig, e.messages, {
    chatPreset: e.chatPreset,
    regexApplications: e.regexApplications
  }), m, v = "", y = !1, T = !1;
  for (let C = 1; C <= RC + 2; C += 1) {
    const A = Lg({
      supportsSessionToolLoop: o,
      messages: n,
      toolResponses: m,
      finalAnswerReminderText: v
    }), I = await e.executeRunOnce({
      agentConfig: e.agentConfig,
      messages: A.requestMessages,
      chatPreset: e.chatPreset,
      regexApplications: e.regexApplications,
      tools: t,
      toolChoice: "auto",
      ...A.mode === "session_tool_response_round" ? { toolResponses: A.toolResponses } : {},
      ...A.mode === "session_final_reminder_round" ? { finalAnswerReminderText: A.finalAnswerReminderText } : {},
      signal: e.signal,
      onStreamProgress: (E) => {
        if (e.onStreamProgress) {
          if (typeof E?.text != "string") {
            e.onStreamProgress(E);
            return;
          }
          e.onStreamProgress({
            ...E,
            text: s + E.text
          });
        }
      }
    });
    m = void 0, v = "", i = String(I.provider || i || ""), l = String(I.model || l || ""), u = String(I.finishReason || u || ""), d = I.providerPayload, f = I.requestSnapshot || f;
    const R = String(I.text || ""), $ = Array.isArray(I.toolCalls) ? I.toolCalls : [];
    if (!$.length) {
      if (!r0(R) && y && !T) {
        T = !0;
        const E = "All required action checks are complete. Do not call more tools. Finish the assistant reply now.";
        o ? v = E : n.push({
          role: "system",
          content: E
        });
        continue;
      }
      if (!r0(R) && y) throw new Error("模型在检定后没有给出有效结论。");
      return s += R, a = I.thoughts, {
        ...I,
        text: s,
        thoughts: a,
        provider: i,
        model: l,
        finishReason: u,
        providerPayload: d,
        requestSnapshot: f,
        runtimeEvents: r
      };
    }
    if (C > RC) throw new Error("action_check_tool_round_limit_exceeded");
    y = !0, s += R;
    const p = s.length, b = mR({
      text: R,
      providerPayload: I.providerPayload
    }, $, { fallbackPrefix: "tavern-rp-tool" });
    b.thoughts = I.thoughts, n.push(b);
    const w = [];
    $.forEach((E) => {
      const x = ij(E.arguments, {}), M = E.name === "ActionCheck" ? RQ(x, { rollDie: e.actionCheckRoll }) : NQ(E.name);
      M.ok && (r.push(f9({
        action: M.action,
        stat: M.stat,
        difficulty: M.difficulty,
        roll: M.roll,
        success: M.success,
        insertAfterChars: p,
        toolCallId: String(E.id || ""),
        summary: VZ(M),
        stakes: M.stakes
      })), e.onStreamProgress?.({
        text: s,
        liveActionCheckEvents: r.map((N) => ({ ...N }))
      })), w.push({
        id: String(E.id || ""),
        name: String(E.name || ""),
        response: M
      }), n.push(gR({
        toolCallId: String(E.id || ""),
        toolName: String(E.name || ""),
        content: lj(M)
      }));
    }), o && (m = w);
  }
  throw new Error("action_check_tool_round_limit_exceeded");
}
function cj(e, t) {
  return t?.supportsSessionToolLoop === !0;
}
async function dj(e) {
  const t = wf(e);
  e.sessionId || i0(e.contextSnapshot || {});
  const n = await oj(e), r = KR(n, e.contextSnapshot);
  i0(r);
  const o = Number(e.reuseUserMessageOrder), s = Number.isInteger(o) && o >= 0 ? await _u(n.id, o) : null, a = s?.role === "user" && !s.error ? s : null;
  if (a) {
    const Pe = a.order + 1;
    await s0(n.id, Pe), await gM(n.id, Pe - 1), await rM(n.id, Pe - 1), await mM(n.id, Pe), await nM(n.id, Pe), await Xi(n.id), await Ax(n.id, await pb(n.id, Pe));
  }
  const i = { ...e.diagnostics || {} };
  if (!a) {
    const Pe = Math.max(1, Math.floor(Number(e.managerSettleTimeoutMs) || 2e4)), Le = await NZ(n.id, Pe);
    Le.timedOut && (i.managerSettleTimedOut = !0, i.managerSettleTimeoutMs = Pe, Le.abortedRunIds?.length && (i.managerSettleAbortedRunIds = Le.abortedRunIds)), await IQ(n.id);
  }
  const l = sr(n.state || e.runtimeState || {}), u = a ? await LZ(n.id, 0, a.order - 1) : null, d = a ? await t5({
    messages: u || [],
    contextSnapshot: r,
    chatPreset: e.chatPreset,
    historyMode: e.historyMode || "raw",
    diagnostics: i,
    applySubstituteParams: e.applySubstituteParams,
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime
  }) : null, f = a ? XZ({
    ...d,
    contextWindowStartOrder: l.contextWindowStartOrder
  }, l.contract) : l, m = JR(f), v = ug(m), y = zR(v), T = !!a, C = String(a?.content || e.currentUserMessage || ""), A = String(t.id || n.chatPresetId || n.presetId || ""), I = String(t.name || n.chatPresetName || n.presetName || "");
  let R = a;
  R || (R = await Oc(n.id, {
    role: "user",
    content: C,
    contextSnapshot: r,
    chatPresetId: A,
    chatPresetName: I,
    presetId: A,
    presetName: I
  }), await ma(() => e.onUserMessageSaved?.(n.id, R)));
  const $ = {}, p = a ? {
    text: C,
    summary: void 0
  } : await xn("turn_user_input_regex", () => Ap({
    applyRegex: e.applyRegex,
    placement: "userInput",
    id: "turn",
    text: C
  }));
  Ls($, p.summary);
  const b = kw(r), w = a ? p.text : await xn("turn_user_input_substitute", () => QR({
    applySubstituteParams: e.applySubstituteParams,
    id: "userInput:turn",
    text: p.text,
    options: b
  })), E = nl(w);
  R && !a && R.content !== w && (R = await fd(n.id, R.order, { content: w }) || R, await ma(() => e.onUserMessageSaved?.(n.id, R)));
  const x = await HR({
    sessionId: n.id,
    contextWindowStartOrder: f.contextWindowStartOrder,
    currentUserMessage: E,
    beforeOrder: R?.order
  }), M = x.historyMessages, N = a ? await X2(n.id, a.order, 2) : await X2(n.id, R?.order ?? Number.POSITIVE_INFINITY, 2), q = UZ({
    runtime: v,
    sessionMessages: N,
    historyMessages: a ? N : M,
    reusedUserMessage: a,
    rerollRuntimeEvents: e.rerollRuntimeEvents,
    randomEncounterRoll: e.randomEncounterRoll
  });
  R && !a && q && (R = await fd(n.id, R.order, { runtimeEvents: [q] }) || R, await ma(() => e.onUserMessageSaved?.(n.id, R)));
  const z = String(e.generationTrigger || (a ? "regenerate" : "normal")), J = {
    ...r,
    worldSettings: {
      ...r.worldSettings || {},
      trigger: z
    },
    history: Tf(x.historyMessages)
  }, G = await xn("turn_native_worldbook_runtime", () => xw({
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
    context: J,
    currentUserMessage: E,
    trigger: z,
    timedState: f.nativeWorldInfoTimedState
  })), oe = await xn("turn_world_entry_substitute", () => Iw({
    applySubstituteParams: e.applySubstituteParams,
    context: G.context,
    options: b
  })), B = await xn("turn_memory_query", () => AR(oe, E)), X = v.includeMemoryFiles || v.includeStructuredStates ? await xn("turn_memory_retrieval", () => CR({
    sessionId: n.id,
    queryText: B,
    ignoredTerms: Sw(oe),
    includeMemoryFiles: v.includeMemoryFiles,
    includeStructuredStates: v.includeStructuredStates
  })) : void 0, de = v.includeQuestOrchestration ? await xn("turn_quest_hook_retrieval", () => iM(n.id, 1)) : [], fe = YR(X || de.length ? {
    ...X || {},
    ...de.length ? { questHooks: de } : {}
  } : void 0, v), ve = WR(v), Ce = await xn("turn_brain_build", () => z0({
    context: oe,
    chatPreset: t,
    currentUserMessage: E,
    historyMode: e.historyMode || "raw",
    turn: f.turn,
    entryStates: f.worldEntryStates,
    memoryContext: fe,
    runtimeDepthEntries: qZ(q),
    runtimeProtocolMessages: ve,
    diagnostics: i,
    regexApplications: $,
    transformConversationMessages: async (Pe) => {
      const Le = await Fm({
        applySubstituteParams: e.applySubstituteParams,
        messages: Pe,
        options: b
      }), Y = await XR({
        applyRegex: e.applyRegex,
        messages: Le
      });
      return Ls($, Y.summary), Y.messages;
    },
    transformFinalMessages: async (Pe) => Fm({
      applySubstituteParams: e.applySubstituteParams,
      messages: Pe,
      options: b
    }),
    transformWorldEntries: async (Pe) => {
      const Le = !!oe.nativeWorldInfo, Y = Pe.filter((we) => jR(we, Le)), te = await Sf(e.applyRegex, Y.map((we) => ({
        id: `worldInfo:${we.activationKey}`,
        text: we.content,
        placement: "worldInfo",
        options: {
          isMarkdown: !1,
          isPrompt: !0,
          depth: we.position === Zm.atDepth ? we.depth : null
        }
      })));
      Ls($, bf(te));
      const he = new Map(te.map((we) => [we.id, we]));
      return Pe.map((we) => {
        const Te = he.get(`worldInfo:${we.activationKey}`)?.text ?? we.content;
        return {
          ...we,
          content: Te,
          contentChars: Te.length
        };
      });
    }
  })), { buildResult: Ue, buildSnapshot: le } = await VR({
    stage: "turn_native_prompt_build",
    buildNativeChatPrompt: e.buildNativeChatPrompt,
    contextForBuild: oe,
    chatPreset: t,
    baseBuildResult: Ce.buildResult,
    baseBuildSnapshot: Ce.buildSnapshot,
    currentUserMessage: E,
    generationType: z,
    signal: e.signal,
    memoryContext: fe,
    chancePrompt: q ? pI().content : "",
    runtimeProtocolMessages: ve,
    diagnostics: i
  }), me = await L_(n.id, {
    contextSnapshot: r,
    buildSnapshot: le,
    chatPresetId: String(t.id || n.chatPresetId || n.presetId || ""),
    chatPresetName: String(t.name || n.chatPresetName || n.presetName || ""),
    presetId: String(t.id || n.presetId || ""),
    presetName: String(t.name || n.presetName || "")
  }) || n;
  let We = "";
  const H = (Pe) => {
    typeof Pe.text == "string" && (We = Pe.text), e.onStreamProgress?.(Pe);
  };
  let W = Tu(e.agentConfig, Ue.messages, {
    chatPreset: t,
    regexApplications: $
  });
  try {
    W = (await Mw({
      agentConfig: e.agentConfig,
      messages: Ue.messages,
      chatPreset: t,
      tools: y.tools,
      toolChoice: y.toolChoice,
      onStreamProgress: H,
      requestKind: "actual",
      regexApplications: $
    })).requestSnapshot;
  } catch {
    W = Tu(e.agentConfig, Ue.messages, {
      requestKind: "fallback",
      chatPreset: t,
      regexApplications: $,
      requestTask: {
        messages: Ue.messages,
        tools: y.tools,
        toolChoice: y.toolChoice
      }
    });
  }
  const se = String(t.id || me.chatPresetId || me.presetId || ""), ke = String(t.name || me.chatPresetName || me.presetName || "");
  if (R) {
    const Pe = P0(R.runtimeEvents);
    R = await fd(me.id, R.order, {
      contextSnapshot: r,
      buildSnapshot: le,
      chatPresetId: se,
      chatPresetName: ke,
      presetId: se,
      presetName: ke,
      requestSnapshot: W,
      runtimeEvents: !Pe && q ? [q] : R.runtimeEvents || []
    }) || R;
  }
  if (!R) throw new Error("user_message_save_failed");
  await ma(() => e.onUserMessageSaved?.(me.id, R));
  try {
    const Pe = e.executeRunOnce || fj(e.agentConfig), Le = v.includeActionChecks ? await uj({
      agentConfig: e.agentConfig,
      messages: Ue.messages,
      chatPreset: t,
      regexApplications: $,
      signal: e.signal,
      onStreamProgress: H,
      executeRunOnce: Pe,
      actionCheckRoll: e.actionCheckRoll
    }) : await Pe({
      agentConfig: e.agentConfig,
      messages: Ue.messages,
      chatPreset: t,
      regexApplications: $,
      signal: e.signal,
      onStreamProgress: H
    }), Y = v.includeActionChecks ? Na(Le.runtimeEvents) : [], te = c_(Le.text, Y), he = await Ap({
      applyRegex: e.applyRegex,
      placement: "aiOutput",
      id: "assistant",
      text: te.text
    }), we = await ZZ({
      applyRegex: e.applyRegex,
      thoughts: Le.thoughts
    });
    Ls($, he.summary), Ls($, we.summary);
    const Te = hI(he.text, Y, te.boundaries);
    (Te.text !== Le.text || we.thoughts !== Le.thoughts) && e.onStreamProgress?.({
      text: Te.text,
      thoughts: we.thoughts,
      liveActionCheckEvents: Te.events.map((ae) => ({ ...ae }))
    });
    const Ie = Te.events, Ne = {
      ...Le.requestSnapshot,
      regexApplications: $
    }, Oe = await Oc(me.id, {
      role: "assistant",
      content: Te.text,
      thoughts: we.thoughts,
      providerPayload: Le.providerPayload,
      contextSnapshot: r,
      buildSnapshot: le,
      chatPresetId: se,
      chatPresetName: ke,
      presetId: se,
      presetName: ke,
      requestSnapshot: Ne,
      provider: Le.provider || "",
      model: Le.model || "",
      finishReason: Le.finishReason || "",
      runtimeEvents: Ie
    });
    await ma(() => e.onAssistantMessageSaved?.(me.id, Oe));
    const L = Number(f.turn || 0) + 1;
    await qy(me.id, {
      turn: L,
      contextWindowStartOrder: x.contextWindowStartOrder,
      worldEntryStates: $C(f, Ue, T),
      nativeWorldInfoTimedState: oe.nativeWorldInfo ? G.timedState : Td(f.nativeWorldInfoTimedState),
      lastBuildSnapshot: le,
      lastRequestSnapshot: Ne,
      lastProvider: Le.provider || "",
      lastModel: Le.model || ""
    }, { replace: T });
    let P = "", Q = "";
    const V = String(Le.finishReason || "").trim();
    if (e.runManager === !0 && !Oe.error && !["aborted", "error"].includes(V) && v.hasAutomaticManagerWork) {
      const ae = await RZ({
        sessionId: me.id,
        agentConfig: e.agentConfig,
        userMessage: R,
        assistantMessage: Oe,
        turn: L,
        assistantPreset: e.assistantPreset,
        sessionContract: m,
        contextSnapshot: r,
        awaitCompletion: e.awaitManager === !0,
        executeManagerOnce: e.executeManagerOnce,
        onManagerRunSaved: async (Se) => {
          await ma(() => e.onManagerRunSaved?.(me.id, Se.id));
        }
      });
      P = ae.managerRunId, Q = ae.managerStatus;
    }
    return {
      sessionId: me.id,
      userMessage: R,
      assistantMessage: Oe,
      buildResult: Ue,
      buildSnapshot: le,
      requestSnapshot: Ne,
      provider: Le.provider || "",
      model: Le.model || "",
      finishReason: Le.finishReason,
      previewMatchesRequest: Ue.meta.rawMessagesJson === Ne.rawMessagesJson,
      nextTurn: L,
      managerRunId: P,
      managerStatus: Q
    };
  } catch (Pe) {
    const Le = Pe?.requestSnapshot;
    Le && (W = {
      ...Le,
      regexApplications: $
    });
    const Y = KZ(Pe, e.signal), te = String(We || "").trim();
    if (Y && te) {
      const Te = await Ap({
        applyRegex: e.applyRegex,
        placement: "aiOutput",
        id: "assistant-partial",
        text: te
      });
      Ls($, Te.summary), _R($) && (W = {
        ...W,
        regexApplications: $
      }), Te.text !== te && e.onStreamProgress?.({ text: Te.text });
      const Ie = await Oc(me.id, {
        role: "assistant",
        content: Te.text,
        error: !1,
        contextSnapshot: r,
        buildSnapshot: le,
        chatPresetId: se,
        chatPresetName: ke,
        presetId: se,
        presetName: ke,
        requestSnapshot: W,
        provider: W.provider,
        model: W.model,
        finishReason: "aborted"
      });
      await ma(() => e.onAssistantMessageSaved?.(me.id, Ie));
      const Ne = Number(f.turn || 0) + 1;
      return await qy(me.id, {
        turn: Ne,
        contextWindowStartOrder: x.contextWindowStartOrder,
        worldEntryStates: $C(f, Ue, T),
        nativeWorldInfoTimedState: oe.nativeWorldInfo ? G.timedState : Td(f.nativeWorldInfoTimedState),
        lastBuildSnapshot: le,
        lastRequestSnapshot: W,
        lastProvider: W.provider,
        lastModel: W.model
      }, { replace: T }), {
        sessionId: me.id,
        userMessage: R,
        assistantMessage: Ie,
        buildResult: Ue,
        buildSnapshot: le,
        requestSnapshot: W,
        provider: W.provider,
        model: W.model,
        finishReason: "aborted",
        previewMatchesRequest: Ue.meta.rawMessagesJson === W.rawMessagesJson,
        nextTurn: Ne,
        managerRunId: "",
        managerStatus: ""
      };
    }
    const he = YZ(Pe instanceof Error ? Pe.message : String(Pe || "run_failed")), we = await Oc(me.id, {
      role: "assistant",
      content: Y ? "已停止生成。" : he,
      error: !0,
      contextSnapshot: r,
      buildSnapshot: le,
      chatPresetId: se,
      chatPresetName: ke,
      presetId: se,
      presetName: ke,
      requestSnapshot: W,
      provider: W.provider,
      model: W.model,
      finishReason: Y ? "aborted" : "error"
    });
    return await ma(() => e.onAssistantMessageSaved?.(me.id, we)), await qy(me.id, {
      turn: Number(f.turn || 0),
      contextWindowStartOrder: x.contextWindowStartOrder,
      worldEntryStates: T ? f.worldEntryStates || {} : {},
      nativeWorldInfoTimedState: Td(f.nativeWorldInfoTimedState),
      lastBuildSnapshot: le,
      lastRequestSnapshot: W,
      lastProvider: W.provider,
      lastModel: W.model,
      lastError: Y ? "已停止生成。" : he
    }, { replace: T }), {
      sessionId: me.id,
      userMessage: R,
      errorMessage: we,
      buildResult: Ue,
      buildSnapshot: le,
      requestSnapshot: W,
      provider: W.provider,
      model: W.model,
      finishReason: Y ? "aborted" : "error",
      previewMatchesRequest: Ue.meta.rawMessagesJson === W.rawMessagesJson,
      nextTurn: Number(f.turn || 0),
      error: Y ? "已停止生成。" : he
    };
  }
}
function fj(e) {
  const t = PR(e), n = bw(t, { missingApiKeyMessage: "请先在 API 配置里选择模型/填写 Key。" }), r = ((o) => sj(n, t, o));
  return r.supportsSessionToolLoop = n.supportsSessionToolLoop === !0, r;
}
function hj(e) {
  return !Array.isArray(e) || !e.length ? [] : e.map((t) => {
    const n = t && typeof t == "object" ? t : {}, r = n.function && typeof n.function == "object" ? n.function : {}, o = typeof n.id == "string" ? n.id : typeof n.tool_call_id == "string" ? n.tool_call_id : void 0, s = typeof n.name == "string" ? n.name : typeof r.name == "string" ? r.name : void 0, a = n.arguments ?? r.arguments, i = typeof a == "string" ? a : a === void 0 ? void 0 : JSON.stringify(a);
    return {
      ...o ? { id: o } : {},
      ...s ? { name: s } : {},
      ...i ? { arguments: i } : {}
    };
  }).filter((t) => !!(t.id || t.name || t.arguments));
}
function Cp(e = "", t = 180) {
  const n = String(e || "").trim();
  return n.length > t ? `${n.slice(0, t)}...` : n;
}
function pj(e) {
  return `manager:${e.sessionId}:${e.order}`;
}
function DC(e) {
  if (!e || typeof e != "object") return !1;
  const t = e;
  return t.error === !0 || ["aborted", "error"].includes(String(t.finishReason || "").trim()) ? !1 : Array.isArray(t.toolCalls) && t.toolCalls.length > 0 || Array.isArray(t.tool_calls) && t.tool_calls.length > 0;
}
function mj(e) {
  const t = e && typeof e == "object" ? e : {};
  return (Array.isArray(t.toolCalls) && t.toolCalls.length ? t.toolCalls : Array.isArray(t.tool_calls) ? t.tool_calls : []).map((n, r) => {
    const o = n && typeof n == "object" ? n : {}, s = o.function && typeof o.function == "object" ? o.function : {}, a = String(o.id || o.tool_call_id || `tool-call-${r + 1}`), i = String(o.name || s.name || "").trim() || "工具", l = o.arguments ?? s.arguments ?? {};
    return {
      id: a,
      name: i,
      argumentsText: Cp(typeof l == "string" ? l : JSON.stringify(l || {}), 320)
    };
  }).filter((n) => n.name);
}
function n5(e) {
  if (!e || typeof e != "string") return {};
  try {
    const t = JSON.parse(e);
    return t && typeof t == "object" && !Array.isArray(t) ? t : {};
  } catch {
    return {};
  }
}
function gj(e) {
  if (!e) return "等待工具返回。";
  const t = String(e.toolDisplay || "").trim();
  if (t) return Cp(t, 360);
  const n = n5(e.content), r = String(n.summary || n.message || n.error || "").trim();
  if (r) return Cp(r, 360);
  const o = String(n.path || n.filePath || n.docId || "").trim(), s = n.changed === !0 ? "已变更" : n.changed === !1 ? "无变化" : "", a = [
    n.ok === !1 ? "失败" : n.ok === !0 ? "成功" : "",
    s,
    o
  ].filter(Boolean).join(" · ");
  return a || Cp(String(e.content || "").trim(), 360) || "工具已返回。";
}
function vj(e, t) {
  return {
    assistantMessage: e,
    toolMessages: t,
    calls: mj(e).map((n) => {
      const r = t.find((s) => String(s.toolCallId || s.tool_call_id || "") === n.id), o = n5(r?.content);
      return {
        id: n.id,
        name: r?.toolName || n.name,
        argumentsText: n.argumentsText,
        resultText: gj(r),
        ok: o.ok !== !1 && !r?.error,
        toolMessage: r
      };
    })
  };
}
function yj(e) {
  const t = e[0], n = e[e.length - 1] || t;
  return {
    kind: "tool-turn",
    key: `tool-turn:${t.assistantMessage.sessionId}:${t.assistantMessage.order}:${n.assistantMessage.order}`,
    anchorKey: `tool:${t.assistantMessage.sessionId}:${t.assistantMessage.order}`,
    rounds: e,
    assistantMessage: t.assistantMessage,
    toolMessages: e.flatMap((r) => r.toolMessages),
    calls: e.flatMap((r) => r.calls)
  };
}
function LC(e) {
  const t = [...e].sort((r, o) => r.order - o.order), n = [];
  for (let r = 0; r < t.length; r += 1) {
    const o = t[r];
    if (!(!o || ![
      "user",
      "assistant",
      "tool"
    ].includes(o.role))) {
      if (o.role === "assistant" && DC(o)) {
        const s = [];
        let a = r;
        for (; a < t.length && t[a]?.role === "assistant" && DC(t[a]); ) {
          const i = t[a], l = [];
          for (a += 1; a < t.length && t[a]?.role === "tool"; )
            l.push(t[a]), a += 1;
          s.push(vj(i, l));
        }
        n.push(yj(s)), r = a - 1;
        continue;
      }
      o.role !== "tool" && n.push({
        kind: "message",
        key: pj(o),
        anchorKey: `msg:${o.sessionId}:${o.order}`,
        message: o
      });
    }
  }
  return n;
}
function _j(e) {
  const t = e.calls.filter((o) => !o.ok).length, n = e.calls.filter((o) => !o.toolMessage).length, r = e.rounds.length > 1 ? `${e.rounds.length} 轮 · ` : "";
  return n ? `${r}工具调用 ${e.calls.length} 次 · ${n} 个等待返回` : t ? `${r}工具调用 ${e.calls.length} 次 · ${t} 次失败` : `${r}工具调用 ${e.calls.length} 次 · 全部成功`;
}
function bj(e) {
  const t = e.calls.map((r) => r.name).filter(Boolean).slice(0, 3).join("、"), n = e.calls.length > 3 ? ` 等 ${e.calls.length} 个` : "";
  return t ? `${t}${n}` : "等待工具返回";
}
function wj() {
  let e = [], t = !1;
  return {
    update(n = {}) {
      const r = Array.isArray(n.toolCalls) && n.toolCalls.length ? hj(n.toolCalls) : null, o = typeof n.text == "string" && n.text.trim().length > 0;
      return r ? (e = r, t = !0) : o && t && n.toolCallDraft !== !0 && (e = [], t = !1), {
        content: typeof n.text == "string" && n.text.trim() ? n.text : e.length ? "正在准备工具调用..." : "正在思考...",
        toolCalls: e
      };
    },
    reset() {
      e = [], t = !1;
    }
  };
}
var Sj = ["title", "aria-label"], Tj = {
  key: 0,
  class: "xb-theme-icon",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.75",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, Ej = {
  key: 1,
  class: "xb-theme-glyph",
  "aria-hidden": "true"
}, Aj = /* @__PURE__ */ un({
  __name: "TavernCornerActions",
  props: {
    dark: { type: Boolean },
    includeHome: { type: Boolean },
    includeApi: { type: Boolean },
    includeChatPreset: { type: Boolean },
    includeWorldbooks: { type: Boolean },
    includeExit: { type: Boolean },
    compact: { type: Boolean },
    homeLast: { type: Boolean }
  },
  emits: [
    "api",
    "chatPreset",
    "home",
    "toggleTheme",
    "worldbooks",
    "exit"
  ],
  setup(e, { emit: t }) {
    const n = t;
    return (r, o) => (O(), F("div", { class: ze(["home-corner-actions", { "page-corner-actions": e.includeHome || e.compact }]) }, [
      e.includeChatPreset ? (O(), F("button", {
        key: 0,
        type: "button",
        class: "home-icon-button page-chat-preset-button",
        title: "聊天预设",
        "aria-label": "聊天预设",
        onClick: o[0] || (o[0] = (s) => n("chatPreset"))
      }, [...o[7] || (o[7] = [g("svg", {
        class: "xb-chat-preset-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [g("path", {
        "stroke-width": "1.6",
        d: "M5 5h14v10H9l-4 4V5ZM8 9h8M8 12h5"
      })], -1)])])) : ge("", !0),
      e.includeApi ? (O(), F("button", {
        key: 1,
        type: "button",
        class: "home-icon-button page-api-button",
        title: "API 配置",
        "aria-label": "API 配置",
        onClick: o[1] || (o[1] = (s) => n("api"))
      }, [...o[8] || (o[8] = [g("svg", {
        class: "xb-api-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [g("path", {
        "stroke-width": "1.6",
        d: "M9 7V3M15 7V3M7 11h10M8 7h8v5a4 4 0 0 1-8 0V7ZM12 16v5"
      })], -1)])])) : ge("", !0),
      e.includeWorldbooks ? (O(), F("button", {
        key: 2,
        type: "button",
        class: "home-icon-button page-worldbooks-button",
        title: "世界书",
        "aria-label": "世界书",
        onClick: o[2] || (o[2] = (s) => n("worldbooks"))
      }, [...o[9] || (o[9] = [g("svg", {
        class: "xb-worldbooks-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [g("path", {
        "stroke-width": "1.6",
        d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
      })], -1)])])) : ge("", !0),
      e.includeHome && !e.homeLast ? (O(), F("button", {
        key: 3,
        type: "button",
        class: "home-icon-button page-home-button",
        title: "首页",
        "aria-label": "首页",
        onClick: o[3] || (o[3] = (s) => n("home"))
      }, [...o[10] || (o[10] = [g("svg", {
        class: "xb-home-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.8",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        g("path", { d: "M3 11.5 12 4l9 7.5" }),
        g("path", { d: "M5.5 10.5V20h13v-9.5" }),
        g("path", { d: "M9.5 20v-5.5h5V20" })
      ], -1)])])) : ge("", !0),
      g("button", {
        type: "button",
        class: "home-icon-button home-theme-button",
        title: e.dark ? "切换到白天" : "切换到夜间",
        "aria-label": e.dark ? "切换到白天" : "切换到夜间",
        onClick: o[4] || (o[4] = (s) => n("toggleTheme"))
      }, [e.dark ? (O(), F("svg", Tj, [...o[11] || (o[11] = [ku('<circle cx="12" cy="12" r="4"></circle><path d="M12 2v2"></path><path d="M12 20v2"></path><path d="m4.93 4.93 1.41 1.41"></path><path d="m17.66 17.66 1.41 1.41"></path><path d="M2 12h2"></path><path d="M20 12h2"></path><path d="m6.34 17.66-1.41 1.41"></path><path d="m19.07 4.93-1.41 1.41"></path>', 9)])])) : (O(), F("span", Ej, "☾"))], 8, Sj),
      e.includeHome && e.homeLast ? (O(), F("button", {
        key: 4,
        type: "button",
        class: "home-icon-button page-home-button",
        title: "首页",
        "aria-label": "首页",
        onClick: o[5] || (o[5] = (s) => n("home"))
      }, [...o[12] || (o[12] = [g("svg", {
        class: "xb-home-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.8",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        g("path", { d: "M3 11.5 12 4l9 7.5" }),
        g("path", { d: "M5.5 10.5V20h13v-9.5" }),
        g("path", { d: "M9.5 20v-5.5h5V20" })
      ], -1)])])) : ge("", !0),
      e.includeExit ? (O(), F("button", {
        key: 5,
        type: "button",
        class: "home-icon-button home-exit-button",
        title: "退出",
        "aria-label": "退出",
        onClick: o[6] || (o[6] = (s) => n("exit"))
      }, [...o[13] || (o[13] = [g("svg", {
        class: "xb-exit-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.9",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        g("path", { d: "M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" }),
        g("path", { d: "M16 17l5-5-5-5" }),
        g("path", { d: "M21 12H9" })
      ], -1)])])) : ge("", !0)
    ], 2));
  }
}), Ef = Aj, Cj = { class: "xb-page tavern-about-page" }, kj = /* @__PURE__ */ un({
  __name: "TavernAboutPage",
  props: { dark: { type: Boolean } },
  emits: ["toggle-theme", "back"],
  setup(e) {
    return (t, n) => (O(), F("section", Cj, [
      En(Ef, {
        compact: "",
        dark: e.dark,
        onToggleTheme: n[0] || (n[0] = (r) => t.$emit("toggle-theme"))
      }, null, 8, ["dark"]),
      g("button", {
        type: "button",
        class: "about-return-inline",
        onClick: n[1] || (n[1] = (r) => t.$emit("back"))
      }, " RETURN TO INDEX "),
      n[2] || (n[2] = ku('<section class="about-title-block"><h1 class="about-title"> 小白酒馆 </h1><div class="about-sub"> A CLEANER WAY TO ROLEPLAY </div></section><section class="manifesto-body"><p class="intro-dropcap"> 当你与角色聊到数十万字，故事不该被界面拖慢。小白酒馆重写了渲染层，长对话里依然顺滑。 </p><p>但流畅只是基础。我们真正想做的，是给角色扮演多配一位后台帮手。它不替代记忆，而是替你打理那些容易散落的线索。</p><div class="feature-block"><h3>I. 幕后代理与双面板</h3><p>舞台前，你与角色保持沉浸的互动；舞台后，一个 Agent 静静旁观对话。需要时翻转面板进入助手聊天，直接找它梳理线索、讨论伏笔设定，不必出戏去打扰剧情里的角色。</p></div><div class="feature-block"><h3>II. 自动演进的可视化世界</h3><p>代理在后台帮你整理剧情脉络、归档阶段事件。基于这套结构化数据，系统支撑起了可视化的空间地图与任务线索。你去过的地点、展开的事件，会在台面下慢慢生长。这块还在持续演进。</p></div><div class="feature-block"><h3>III. 依然是你的酒馆</h3><p>这套基建扎根在你熟悉的土壤上。角色卡、世界书、聊天预设、正则，全部直连酒馆本体。你可以在这里调取和修改旧设定，也能在请求日志里，清楚看到代理发出的每一条真实记录。</p></div><div class="feature-block quiet"><p>放下那些拥挤的参数窗口，选一张卡片，把心思还给故事本身。</p></div></section>', 2))
    ]));
  }
}), Ij = kj;
function Og() {
  const e = /* @__PURE__ */ ie(/* @__PURE__ */ new Map());
  function t(a, i = !1) {
    return e.value.get(a) ?? i;
  }
  function n(a, i) {
    const l = new Map(e.value);
    l.set(a, i), e.value = l;
  }
  function r(a, i) {
    n(a, !!i.currentTarget?.open);
  }
  function o(a, i = !1) {
    n(a, !t(a, i));
  }
  function s() {
    e.value = /* @__PURE__ */ new Map();
  }
  return cu(s), {
    isOpen: t,
    reset: s,
    setOpen: n,
    setOpenFromEvent: r,
    toggleOpen: o
  };
}
var xj = { class: "xb-page character-select-page" }, Mj = { class: "character-desk" }, Pj = { class: "character-select-head" }, Rj = { class: "head-left" }, Nj = {
  key: 0,
  class: "character-select-error"
}, $j = { class: "character-index-panel" }, Dj = { class: "index-panel-toolbar" }, Lj = { class: "os-search-bar" }, Oj = ["value"], Fj = { class: "index-meta" }, Bj = [
  "data-character-card-id",
  "disabled",
  "onClick"
], Uj = { class: "character-card-avatar" }, qj = ["src", "onError"], Hj = { key: 1 }, zj = { class: "character-card-body" }, Wj = { class: "character-card-title" }, Gj = { class: "character-card-desc" }, Vj = { class: "card-status-tags" }, Kj = {
  key: 0,
  class: "tag active"
}, Jj = {
  key: 1,
  class: "tag loading"
}, Yj = {
  key: 2,
  class: "tag loading"
}, Xj = {
  key: 0,
  class: "empty-note"
}, Qj = {
  key: 0,
  class: "character-preview-panel dossier-view"
}, Zj = { class: "dossier-header" }, jj = { class: "dossier-identity" }, eee = { class: "sys-mono" }, tee = { class: "dossier-title-row" }, nee = { class: "dossier-title-actions" }, ree = ["disabled", "title"], oee = ["disabled"], see = { key: 0 }, aee = ["disabled"], iee = { class: "dossier-summary" }, lee = { class: "dossier-details" }, uee = { class: "data-group" }, cee = { class: "data-row" }, dee = { key: 0 }, fee = {
  key: 0,
  class: "greeting-current-card"
}, hee = { class: "greeting-current-head" }, pee = { key: 0 }, mee = { class: "data-block greeting-current-text" }, gee = ["open"], vee = {
  key: 0,
  class: "greeting-choice-list"
}, yee = ["disabled", "onClick"], _ee = { class: "greeting-choice-name" }, bee = { class: "greeting-choice-text" }, wee = {
  key: 1,
  class: "data-block"
}, See = {
  key: 2,
  class: "data-block"
}, Tee = {
  key: 1,
  class: "character-definition-overlay",
  role: "dialog",
  "aria-modal": "true",
  "aria-label": "角色卡详情"
}, Eee = { class: "character-definition-dialog" }, Aee = { class: "character-definition-list" }, Cee = {
  key: 2,
  class: "character-session-archive-overlay",
  role: "dialog",
  "aria-modal": "true",
  "aria-label": "会话档案"
}, kee = { class: "character-session-archive" }, Iee = {
  key: 0,
  class: "session-archive-list"
}, xee = ["onClick"], Mee = { class: "session-archive-item-title" }, Pee = { class: "session-archive-item-meta" }, Ree = {
  key: 0,
  class: "session-archive-item-summary"
}, Nee = {
  key: 1,
  class: "session-archive-empty"
}, $ee = {
  key: 3,
  class: "character-preview-panel dossier-empty"
}, Dee = {
  key: 2,
  class: "character-empty"
}, Lee = { class: "empty-content" }, Oee = /* @__PURE__ */ un({
  __name: "TavernCharacterSelectPage",
  props: {
    dark: { type: Boolean },
    pendingError: {},
    characters: {},
    visibleCharacters: {},
    filteredCount: {},
    liveCharacterId: {},
    selectedCharacter: {},
    selectedGreetingIndex: {},
    pendingPreviewCharacterId: {},
    pendingCharacterSessionId: {},
    selectedCharacterSessions: {},
    characterWorldbookState: {},
    characterWorldbookBusy: { type: Boolean },
    searchText: {},
    hiddenCount: {},
    batchSize: {},
    avatarAvailable: { type: Function },
    sessionFloorLabel: { type: Function },
    shortText: { type: Function }
  },
  emits: [
    "toggle-theme",
    "back",
    "refresh",
    "update:searchText",
    "select",
    "enter-selected",
    "open-character-worldbook",
    "open-session",
    "select-greeting",
    "load-more",
    "keydown",
    "avatar-error"
  ],
  setup(e, { expose: t, emit: n }) {
    const r = e, o = n, s = /* @__PURE__ */ ie(null), a = /* @__PURE__ */ ie(!1), i = /* @__PURE__ */ ie(!1), l = Og(), u = re(() => {
      const z = r.selectedCharacter;
      return z ? [String(z.firstMessage || "").trim(), ...z.alternateGreetings || []].filter(Boolean) : [];
    }), d = re(() => {
      if (!u.value.length) return "";
      const z = Math.min(Math.max(0, Number(r.selectedGreetingIndex) || 0), u.value.length - 1);
      return u.value[z] || "";
    }), f = re(() => u.value.length > 1), m = re(() => r.selectedCharacterSessions.length), v = re(() => {
      const z = r.selectedCharacter;
      return [
        {
          label: "性格摘要",
          value: z?.personality || ""
        },
        {
          label: "情景",
          value: z?.scenario || ""
        },
        {
          label: "角色备注",
          value: z?.characterDepthPrompt || ""
        },
        {
          label: "制作者备注",
          value: z?.creatorNotes || ""
        },
        {
          label: "示例对话",
          value: z?.mesExample || ""
        }
      ];
    }), y = re(() => !!r.selectedCharacter && String(r.pendingPreviewCharacterId || "") === String(r.selectedCharacter.id || "")), T = re(() => {
      const z = r.characterWorldbookState;
      return z ? z.boundWorldbookName && z.boundExists ? `打开 ${z.boundWorldbookName}` : z.hasEmbeddedBook ? `导入 ${z.embeddedBookName}` : "选择一本世界书绑定" : "读取角色世界书";
    }), C = re(() => r.characterWorldbookState?.boundExists === !0);
    function A() {
      Qt(() => {
        const z = s.value, J = String(r.selectedCharacter?.id || "").trim();
        !z || !J || Array.from(z.querySelectorAll("[data-character-card-id]")).find((G) => G.dataset.characterCardId === J)?.scrollIntoView?.({
          block: "nearest",
          behavior: "smooth"
        });
      });
    }
    function I(z) {
      o("update:searchText", z.target.value);
    }
    function R(z) {
      return `character-data:${r.selectedCharacter?.id || "none"}:${z}`;
    }
    function $(z) {
      return z === 0 ? "主开场白" : `备用 ${z}`;
    }
    function p(z) {
      const J = Number(z) || 0;
      return J ? new Date(J).toLocaleString("zh-CN", {
        month: "2-digit",
        day: "2-digit",
        hour: "2-digit",
        minute: "2-digit"
      }) : "未知时间";
    }
    function b(z) {
      return String(z.title || z.characterName || r.selectedCharacter?.name || "未命名会话").trim();
    }
    function w(z) {
      return `${p(z.updatedAt || z.createdAt)} · ${r.sessionFloorLabel(z)}`;
    }
    function E() {
      !r.selectedCharacter || r.pendingCharacterSessionId || (a.value = !0);
    }
    function x() {
      a.value = !1;
    }
    function M() {
      r.selectedCharacter && (i.value = !0);
    }
    function N() {
      i.value = !1;
    }
    function q(z) {
      const J = String(z || "").trim();
      J && (x(), o("open-session", J));
    }
    return gt(() => r.selectedCharacter?.id, () => {
      l.reset(), N(), x(), A();
    }), t({
      scrollSelectedIntoView: A,
      openSessionArchive: E
    }), (z, J) => (O(), F("section", xj, [En(Ef, {
      compact: "",
      dark: e.dark,
      onToggleTheme: J[0] || (J[0] = (G) => z.$emit("toggle-theme"))
    }, null, 8, ["dark"]), g("div", Mj, [
      g("header", Pj, [
        g("div", Rj, [g("button", {
          type: "button",
          class: "os-sys-button",
          onClick: J[1] || (J[1] = (G) => z.$emit("back"))
        }, [...J[8] || (J[8] = [g("span", { class: "icon" }, "↵", -1), Bs(" 首页 ", -1)])])]),
        J[9] || (J[9] = g("div", { class: "character-title-copy" }, [g("p", { class: "eyebrow" }, " SYSTEM // DESK "), g("h2", null, "角色档案库")], -1)),
        J[10] || (J[10] = g("div", {
          class: "head-balance",
          "aria-hidden": "true"
        }, null, -1))
      ]),
      e.pendingError ? (O(), F("div", Nj, " [SYS_ERROR] " + K(e.pendingError), 1)) : ge("", !0),
      e.characters.length ? (O(), F("section", {
        key: 1,
        class: ze(["character-archive", { "has-character-selection": !!e.selectedCharacter }])
      }, [
        g("aside", $j, [
          g("div", Dj, [g("label", Lj, [J[11] || (J[11] = g("span", { class: "icon" }, "⌕", -1)), g("input", {
            value: e.searchText,
            type: "search",
            placeholder: "检索角色特征...",
            onInput: I
          }, null, 40, Oj)]), g("div", Fj, K(e.visibleCharacters.length) + " / " + K(e.filteredCount) + " ENTRIES ", 1)]),
          g("div", {
            ref_key: "listRef",
            ref: s,
            class: "character-card-grid",
            tabindex: "0",
            "aria-label": "角色卡列表",
            onKeydown: J[3] || (J[3] = (G) => z.$emit("keydown", G))
          }, [(O(!0), F(Je, null, lt(e.visibleCharacters, (G) => (O(), F("button", {
            key: G.id,
            type: "button",
            class: ze(["character-card-option", {
              current: G.id === e.liveCharacterId,
              selected: G.id === e.selectedCharacter?.id,
              pending: G.id === e.pendingCharacterSessionId
            }]),
            "data-character-card-id": G.id,
            disabled: !!e.pendingCharacterSessionId,
            onClick: (oe) => z.$emit("select", G.id)
          }, [
            J[12] || (J[12] = g("div", { class: "card-focus-indicator" }, null, -1)),
            g("span", Uj, [e.avatarAvailable(G.avatar) ? (O(), F("img", {
              key: 0,
              src: G.avatar,
              loading: "lazy",
              decoding: "async",
              alt: "",
              onError: (oe) => z.$emit("avatar-error", G.avatar)
            }, null, 40, qj)) : (O(), F("span", Hj, K(G.name.slice(0, 1)), 1))]),
            g("span", zj, [
              g("span", Wj, [g("strong", null, K(G.name), 1)]),
              g("span", Gj, K(e.shortText(G.description || G.personality || G.scenario || G.firstMessage || "暂无数据存档。", 72)), 1),
              g("span", Vj, [G.id === e.liveCharacterId ? (O(), F("small", Kj, "ACTIVE")) : ge("", !0), G.id === e.pendingCharacterSessionId ? (O(), F("small", Jj, "LOADING...")) : G.id === e.pendingPreviewCharacterId ? (O(), F("small", Yj, "READING...")) : ge("", !0)])
            ])
          ], 10, Bj))), 128)), e.hiddenCount ? (O(), F("button", {
            key: 0,
            type: "button",
            class: "archive-load-more character-load-more",
            onClick: J[2] || (J[2] = (G) => z.$emit("load-more"))
          }, " 加载更多残卷 (" + K(Math.min(e.hiddenCount, e.batchSize)) + ") ", 1)) : ge("", !0)], 544),
          e.visibleCharacters.length ? ge("", !0) : (O(), F("div", Xj, " [SYS_WARN] 未检索到匹配档案。 "))
        ]),
        e.selectedCharacter ? (O(), F("main", Qj, [g("div", Zj, [g("div", jj, [
          g("p", eee, " ID: " + K(e.selectedCharacter.id.substring(0, 8) || "UNKNOWN"), 1),
          g("div", tee, [g("h3", null, K(e.selectedCharacter.name), 1), g("div", nee, [
            g("button", {
              type: "button",
              class: "os-system-act-btn character-definition-button",
              title: "角色卡详情",
              "aria-label": "角色卡详情",
              onClick: M
            }, [...J[13] || (J[13] = [g("svg", {
              class: "character-definition-icon",
              "aria-hidden": "true",
              fill: "none",
              stroke: "currentColor",
              viewBox: "0 0 24 24"
            }, [g("path", {
              "stroke-linecap": "round",
              "stroke-linejoin": "round",
              "stroke-width": "1.7",
              d: "M5.5 4.7c1.8-.9 4.1-.9 6.5.3 2.4-1.2 4.7-1.2 6.5-.3v14.4c-1.8-.8-4.1-.7-6.5.5-2.4-1.2-4.7-1.3-6.5-.5V4.7Z"
            }), g("path", {
              "stroke-linecap": "round",
              "stroke-width": "1.3",
              d: "M12 5v14.2M8 8.5h1.9M8 11.5h1.9M14.1 8.5H16M14.1 11.5H16"
            })], -1)])]),
            g("button", {
              type: "button",
              class: ze(["os-system-act-btn character-worldbook-button", {
                "is-loading": e.characterWorldbookBusy,
                "is-bound": C.value
              }]),
              disabled: !!e.pendingCharacterSessionId || e.characterWorldbookBusy,
              title: T.value,
              "aria-label": "角色世界书",
              onClick: J[4] || (J[4] = (G) => z.$emit("open-character-worldbook"))
            }, [...J[14] || (J[14] = [g("svg", {
              class: "character-worldbook-icon",
              "aria-hidden": "true",
              fill: "none",
              stroke: "currentColor",
              viewBox: "0 0 24 24"
            }, [g("path", {
              "stroke-width": "1.6",
              d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
            })], -1)])], 10, ree),
            g("button", {
              type: "button",
              class: "os-system-act-btn session-archive-button",
              disabled: !!e.pendingCharacterSessionId,
              onClick: E
            }, [J[15] || (J[15] = Bs(" 会话档案 ", -1)), m.value ? (O(), F("span", see, K(m.value), 1)) : ge("", !0)], 8, oee),
            g("button", {
              type: "button",
              class: ze(["os-system-act-btn enter-chat-button", { "is-loading": e.selectedCharacter.id === e.pendingCharacterSessionId }]),
              disabled: !!e.pendingCharacterSessionId,
              onClick: J[5] || (J[5] = (G) => z.$emit("enter-selected"))
            }, K(e.selectedCharacter.id === e.pendingCharacterSessionId ? "新建中..." : "新建聊天"), 11, aee)
          ])]),
          g("div", iee, K(y.value ? "正在读取角色卡..." : e.selectedCharacter.description || e.selectedCharacter.personality || e.selectedCharacter.scenario || "暂无角色描述。"), 1)
        ])]), g("div", lee, [g("dl", uee, [g("div", cee, [g("dt", null, [J[16] || (J[16] = Bs("开场白 ", -1)), u.value.length > 1 ? (O(), F("span", dee, K(e.selectedGreetingIndex + 1) + " / " + K(u.value.length), 1)) : ge("", !0)]), g("dd", null, [u.value.length ? (O(), F("div", fee, [
          g("div", hee, [g("strong", null, K($(e.selectedGreetingIndex)), 1), u.value.length > 1 ? (O(), F("span", pee, K(u.value.length) + " 个可选", 1)) : ge("", !0)]),
          g("div", mee, K(d.value), 1),
          g("details", {
            open: S(l).isOpen(R("greetings")),
            class: ze(["data-section greeting-picker", { "is-empty": !f.value }]),
            onToggle: J[6] || (J[6] = (G) => S(l).setOpenFromEvent(R("greetings"), G))
          }, [J[17] || (J[17] = g("summary", { class: "greeting-section-title" }, " 切换开场白 ", -1)), f.value && S(l).isOpen(R("greetings")) ? (O(), F("div", vee, [(O(!0), F(Je, null, lt(u.value, (G, oe) => (O(), F("button", {
            key: `${e.selectedCharacter.id}-greeting-${oe}`,
            type: "button",
            class: ze(["greeting-choice", { selected: oe === e.selectedGreetingIndex }]),
            disabled: !!e.pendingCharacterSessionId,
            onClick: (B) => z.$emit("select-greeting", oe)
          }, [g("span", _ee, K($(oe)), 1), g("span", bee, K(e.shortText(G, 120)), 1)], 10, yee))), 128))])) : ge("", !0)], 42, gee)
        ])) : y.value ? (O(), F("div", wee, " 正在读取角色卡... ")) : (O(), F("div", See, " 未填写 "))])])])])])) : ge("", !0),
        i.value && e.selectedCharacter ? (O(), F("div", Tee, [g("section", Eee, [g("header", null, [g("div", null, [J[18] || (J[18] = g("strong", null, "角色卡详情", -1)), g("span", null, K(e.selectedCharacter.name), 1)]), g("button", {
          type: "button",
          class: "session-archive-close character-definition-close",
          "aria-label": "关闭角色卡详情",
          onClick: N
        })]), g("dl", Aee, [(O(!0), F(Je, null, lt(v.value, (G) => (O(), F("div", {
          key: G.label,
          class: "character-definition-section"
        }, [g("dt", null, K(G.label), 1), g("dd", { class: ze({ "is-empty": !G.value }) }, K(G.value || (y.value ? "正在读取角色卡..." : "未填写")), 3)]))), 128))])])])) : ge("", !0),
        a.value && e.selectedCharacter ? (O(), F("div", Cee, [g("section", kee, [g("header", null, [g("div", null, [J[19] || (J[19] = g("strong", null, "会话档案", -1)), g("span", null, K(e.selectedCharacter.name), 1)]), g("button", {
          type: "button",
          class: "session-archive-close",
          "aria-label": "关闭会话档案",
          onClick: x
        })]), e.selectedCharacterSessions.length ? (O(), F("div", Iee, [(O(!0), F(Je, null, lt(e.selectedCharacterSessions, (G) => (O(), F("button", {
          key: G.id,
          type: "button",
          class: "session-archive-item",
          onClick: (oe) => q(G.id)
        }, [
          g("span", Mee, K(b(G)), 1),
          g("span", Pee, K(w(G)), 1),
          G.summary ? (O(), F("span", Ree, K(e.shortText(G.summary, 96)), 1)) : ge("", !0)
        ], 8, xee))), 128))])) : (O(), F("div", Nee, " 这张角色卡还没有旧会话。 "))])])) : ge("", !0),
        e.selectedCharacter ? ge("", !0) : (O(), F("main", $ee, [...J[20] || (J[20] = [g("div", { class: "cyber-placeholder" }, [g("div", { class: "ring" }), g("span", null, [
          Bs("AWAITING"),
          g("br"),
          Bs("SELECTION")
        ])], -1)])]))
      ], 2)) : (O(), F("section", Dee, [g("div", Lee, [
        J[21] || (J[21] = g("h2", null, "数据库脱节", -1)),
        J[22] || (J[22] = g("p", null, "本地缓存在酒馆内未找到可用角色，请在主控端加载后重试。", -1)),
        g("button", {
          type: "button",
          class: "os-sys-button primary",
          onClick: J[7] || (J[7] = (G) => z.$emit("refresh"))
        }, " 重新唤醒数据 ")
      ])]))
    ])]));
  }
}), Fee = Oee, Bee = { class: "tavern-home xb-page home-command-center" }, Uee = { class: "home-hero home-command-hero" }, qee = { class: "home-brand-panel" }, Hee = { class: "home-version-line" }, zee = { class: "home-enter-desktop" }, Wee = { class: "home-enter-mobile-label" }, Gee = { class: "home-enter-mobile-title" }, Vee = { class: "home-command-panel home-menu-panel" }, Kee = { class: "home-menu-mobile-footer" }, Jee = /* @__PURE__ */ un({
  __name: "TavernHomePage",
  props: {
    dark: { type: Boolean },
    hasSession: { type: Boolean },
    subtitle: {},
    characterCount: {}
  },
  emits: [
    "toggle-theme",
    "exit",
    "enter",
    "open-characters",
    "open-settings",
    "open-about"
  ],
  setup(e) {
    return (t, n) => (O(), F("section", Bee, [En(Ef, {
      "include-exit": "",
      dark: e.dark,
      onToggleTheme: n[0] || (n[0] = (r) => t.$emit("toggle-theme")),
      onExit: n[1] || (n[1] = (r) => t.$emit("exit"))
    }, null, 8, ["dark"]), g("div", Uee, [
      g("div", qee, [
        n[13] || (n[13] = g("p", { class: "eyebrow" }, " Private Narrative Archive ", -1)),
        n[14] || (n[14] = g("h2", null, [g("span", null, "Little White Tavern")], -1)),
        n[15] || (n[15] = g("p", null, " A quiet narrative desk where the scene, the archive, and the assistant stay in the same room. ", -1)),
        g("p", Hee, K(e.hasSession ? e.subtitle : `${e.characterCount} cards in archive`), 1)
      ]),
      g("button", {
        type: "button",
        class: "enter-btn home-enter-chat",
        onClick: n[2] || (n[2] = (r) => t.$emit("enter"))
      }, [
        g("span", zee, K(e.hasSession ? "ENTER LAST CHAT" : "OPEN CHARACTER ARCHIVE"), 1),
        g("span", Wee, K(e.hasSession ? "RESUME SESSION" : "OPEN ARCHIVE"), 1),
        g("span", Gee, K(e.hasSession ? "继续聊天" : "选择角色"), 1),
        n[16] || (n[16] = g("span", {
          class: "home-enter-mobile-arrow",
          "aria-hidden": "true"
        }, "→", -1))
      ]),
      g("div", Vee, [
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[3] || (n[3] = (r) => t.$emit("open-characters"))
        }, [...n[17] || (n[17] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M16 7a4 4 0 1 1-8 0 4 4 0 0 1 8 0ZM5 21a7 7 0 0 1 14 0"
          })], -1),
          g("strong", null, "角色卡", -1),
          g("span", null, "Character Cards", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[4] || (n[4] = (r) => t.$emit("open-settings", "worldbooks"))
        }, [...n[18] || (n[18] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
          })], -1),
          g("strong", null, "世界书", -1),
          g("span", null, "World Lore Books", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[5] || (n[5] = (r) => t.$emit("open-settings", "chatPreset"))
        }, [...n[19] || (n[19] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M5 5h14v10H9l-4 4V5ZM8 9h8M8 12h5"
          })], -1),
          g("strong", null, "聊天预设", -1),
          g("span", null, "Prompt Presets", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[6] || (n[6] = (r) => t.$emit("open-settings", "assistantPreset"))
        }, [...n[20] || (n[20] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M8 6.5A3.5 3.5 0 0 1 15 6a3.5 3.5 0 0 1 1 6.85V19a2 2 0 0 1-2 2h-4a2 2 0 0 1-2-2v-6.15A3.5 3.5 0 0 1 8 6.5ZM8 14h8M10 18h4"
          })], -1),
          g("strong", null, "助手预设", -1),
          g("span", null, "Memory Manager", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[7] || (n[7] = (r) => t.$emit("open-settings", "regex"))
        }, [...n[21] || (n[21] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M10 20 14 4M6 8l-4 4 4 4M18 8l4 4-4 4"
          })], -1),
          g("strong", null, "正则", -1),
          g("span", null, "Text Filters", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[8] || (n[8] = (r) => t.$emit("open-settings", "api"))
        }, [...n[22] || (n[22] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M9 7V3M15 7V3M7 11h10M8 7h8v5a4 4 0 0 1-8 0V7ZM12 16v5"
          })], -1),
          g("strong", null, "API 配置", -1),
          g("span", null, "Model Connection", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item desktop-only",
          onClick: n[9] || (n[9] = (r) => t.$emit("open-settings", "base"))
        }, [...n[23] || (n[23] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M12 12a4 4 0 1 0-4-4 4 4 0 0 0 4 4Zm-7 8a7 7 0 0 1 14 0"
          })], -1),
          g("strong", null, "基础设定", -1),
          g("span", null, "Base Setup", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item highlight desktop-only",
          onClick: n[10] || (n[10] = (r) => t.$emit("open-about"))
        }, [...n[24] || (n[24] = [g("strong", null, "关于界面", -1), g("span", null, "About", -1)])]),
        g("div", Kee, [g("button", {
          type: "button",
          class: "home-menu-item mobile-footer-item",
          onClick: n[11] || (n[11] = (r) => t.$emit("open-settings", "base"))
        }, [...n[25] || (n[25] = [g("strong", null, "基础设定", -1)])]), g("button", {
          type: "button",
          class: "home-menu-item mobile-footer-item mobile-footer-about",
          onClick: n[12] || (n[12] = (r) => t.$emit("open-about"))
        }, [...n[26] || (n[26] = [g("strong", null, "关于界面", -1)])])])
      ])
    ])]));
  }
}), Yee = Jee;
function OC(e, t) {
  if (!e) return null;
  const n = e.scrollHeight - e.scrollTop - e.clientHeight, r = typeof e.getBoundingClientRect == "function" ? e.getBoundingClientRect() : null, o = r && t ? Array.from(e.querySelectorAll(t.itemSelector)).map((s) => ({
    key: s?.dataset?.[t.datasetKey] || "",
    rect: typeof s?.getBoundingClientRect == "function" ? s.getBoundingClientRect() : null
  })).find((s) => s.key && s.rect && s.rect.bottom >= r.top + 1) : null;
  return {
    scrollTop: Number(e.scrollTop || 0),
    nearBottom: n < 80,
    anchorKey: o?.key || "",
    anchorTopOffset: o?.rect ? o.rect.top - r.top : 0
  };
}
function FC(e, t, n, r = {}) {
  if (e) {
    if (r.forceBottom) {
      e.scrollTop = e.scrollHeight;
      return;
    }
    if (!t) {
      r.defaultToBottom !== !1 && (e.scrollTop = e.scrollHeight);
      return;
    }
    if (r.preserveScrollTop) {
      if (e.scrollTop = Math.min(Math.max(0, t.scrollTop), e.scrollHeight), t.anchorKey && n) {
        const o = typeof e.getBoundingClientRect == "function" ? e.getBoundingClientRect() : null, s = Array.from(e.querySelectorAll(n.itemSelector)).find((i) => i?.dataset?.[n.datasetKey] === t.anchorKey), a = typeof s?.getBoundingClientRect == "function" ? s.getBoundingClientRect() : null;
        if (o && a) {
          const i = a.top - o.top;
          e.scrollTop = Math.min(Math.max(0, e.scrollTop + i - Number(t.anchorTopOffset || 0)), e.scrollHeight);
        }
      }
      return;
    }
    e.scrollTop = t.nearBottom ? e.scrollHeight : Math.min(t.scrollTop, e.scrollHeight);
  }
}
var Xee = {
  class: "tavern-contract-modal",
  tabindex: "-1"
}, Qee = { class: "tavern-contract-head" }, Zee = { class: "tavern-contract-body" }, jee = { class: "tavern-contract-tier-bar" }, ete = ["aria-pressed", "onClick"], tte = { class: "tavern-contract-copy" }, nte = { class: "tavern-contract-description" }, rte = { class: "tavern-contract-foot" }, ote = ["disabled"], ste = { class: "tavern-contract-note" }, ate = {
  key: 0,
  class: "tavern-contract-error"
}, ite = /* @__PURE__ */ un({
  __name: "TavernContractModal",
  props: {
    draftContract: {},
    dirty: { type: Boolean },
    saving: { type: Boolean },
    canSave: { type: Boolean },
    errorText: {}
  },
  emits: [
    "close",
    "toggle",
    "save"
  ],
  setup(e, { emit: t }) {
    const n = e, r = re(() => {
      const s = /* @__PURE__ */ new Map();
      return D_.forEach((a) => {
        const i = s.get(a.tier) || [];
        i.push(a), s.set(a.tier, i);
      }), Array.from(s.entries()).map(([a, i]) => ({
        tier: a,
        mandates: i
      }));
    }), o = re(() => tF(n.draftContract));
    return (s, a) => (O(), F("div", {
      class: "tavern-contract-overlay",
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": "tavern-contract-title",
      onClick: a[2] || (a[2] = sn((i) => s.$emit("close"), ["self"])),
      onKeydown: a[3] || (a[3] = Vs((i) => s.$emit("close"), ["esc"]))
    }, [g("section", Xee, [
      g("header", Qee, [a[4] || (a[4] = g("div", { class: "tavern-contract-heading" }, [g("h2", { id: "tavern-contract-title" }, " 契约 "), g("p", { class: "eyebrow" }, " 玩家 — 代理人誓约 ")], -1)), g("button", {
        type: "button",
        class: "tavern-contract-close",
        "aria-label": "关闭契约",
        onClick: a[0] || (a[0] = (i) => s.$emit("close"))
      }, " × ")]),
      g("div", Zee, [a[5] || (a[5] = g("p", { class: "tavern-contract-preamble" }, " 故事开始之前，定义你的代理人被允许做什么。 每一条封印的授权赋予一项特定权限。 你可以随时修改。 ", -1)), (O(!0), F(Je, null, lt(r.value, (i) => (O(), F("section", {
        key: i.tier,
        class: "tavern-contract-tier"
      }, [g("div", jee, [g("span", null, K(S(jO)[i.tier]), 1)]), (O(!0), F(Je, null, lt(i.mandates, (l) => (O(), F("article", {
        key: l.key,
        class: ze(["tavern-contract-mandate", { "is-off": !e.draftContract[l.key] }])
      }, [g("button", {
        type: "button",
        class: ze(["tavern-contract-seal", { active: e.draftContract[l.key] }]),
        "aria-pressed": e.draftContract[l.key] ? "true" : "false",
        onClick: (u) => s.$emit("toggle", l.key)
      }, [g("span", null, K(l.icon), 1)], 10, ete), g("div", tte, [g("h3", null, K(l.title), 1), g("p", nte, K(l.description), 1)])], 2))), 128))]))), 128))]),
      g("footer", rte, [
        g("button", {
          type: "button",
          class: "tavern-contract-save",
          disabled: e.saving || !e.canSave,
          onClick: a[1] || (a[1] = (i) => s.$emit("save"))
        }, K(e.saving ? "封印中..." : "封存誓约"), 9, ote),
        g("p", ste, K(o.value) + " / " + K(S(D_).length) + " 项授权已启用 ", 1),
        e.errorText ? (O(), F("p", ate, K(e.errorText), 1)) : ge("", !0)
      ])
    ])], 32));
  }
}), lte = ite, r5 = /* @__PURE__ */ Symbol("TavernAppUiContext");
function qu() {
  const e = sd(r5);
  if (!e) throw new Error("tavern_app_ui_context_missing");
  return e;
}
function Af() {
  return qu().shell;
}
function Fg() {
  return qu().chat;
}
function o5() {
  return qu().manager;
}
function s5() {
  return qu().memory;
}
function a5() {
  return qu().workspace;
}
function Va() {
  return qu().settings;
}
function i5(e, t = 36) {
  const n = /* @__PURE__ */ ie(!1);
  let r = null, o = 0;
  function s() {
    n.value = !1, r = null, o = 0;
  }
  function a(d) {
    d.button && d.button !== 0 || (r = d.clientY, o = 0, n.value = !0, d.currentTarget?.setPointerCapture?.(d.pointerId));
  }
  function i(d) {
    r !== null && (o = d.clientY - r, o > 0 && d.cancelable && d.preventDefault());
  }
  function l(d) {
    if (r === null) return;
    d.currentTarget?.releasePointerCapture?.(d.pointerId);
    const f = o >= t;
    s(), f && e();
  }
  function u(d) {
    d.currentTarget?.releasePointerCapture?.(d.pointerId), s();
  }
  return {
    dragging: n,
    handleSheetHandlePointerCancel: u,
    handleSheetHandlePointerDown: a,
    handleSheetHandlePointerMove: i,
    handleSheetHandlePointerUp: l
  };
}
var ute = { class: "chat-side xb-sidebar" }, cte = { class: "chat-profile xb-brand" }, dte = { class: "avatar-orb" }, fte = ["src"], hte = { key: 1 }, pte = {
  class: "xb-workspace-controller chat-layout-controller",
  "aria-label": "区域大小"
}, mte = {
  class: "chat-side-switch",
  role: "tablist",
  "aria-label": "左侧目录"
}, gte = ["aria-selected"], vte = ["aria-selected"], yte = {
  key: 0,
  class: "chat-side-block chat-directory-block"
}, _te = { class: "side-block-head" }, bte = { class: "session-list chat-directory-list xb-files" }, wte = ["onClick"], Ste = ["onClick"], Tte = {
  key: 1,
  class: "muted compact"
}, Ete = {
  key: 1,
  class: "chat-side-block chat-directory-block"
}, Ate = { class: "side-block-head" }, Cte = {
  key: 0,
  class: "memory-search"
}, kte = {
  key: 1,
  class: "memory-directory-list xb-files"
}, Ite = { class: "memory-file-group-title" }, xte = { class: "memory-file-tree" }, Mte = ["onClick"], Pte = { class: "memory-file-main" }, Rte = ["onClick"], Nte = { class: "memory-file-main" }, $te = {
  key: 0,
  class: "muted compact"
}, Dte = {
  key: 2,
  class: "muted compact"
}, Lte = /* @__PURE__ */ un({
  __name: "TavernChatSidebar",
  emits: ["close"],
  setup(e, { emit: t }) {
    const n = t, r = Af(), o = Fg(), s = s5(), { CHAT_SIDEBAR_BATCH_SIZE: a, chatLayout: i, chatSidebarSessionLimit: l, chatSidebarSessions: u, chatSidePanel: d, chatSubtitle: f, displayCharacterName: m, filteredChatSidebarSessionCount: v, hiddenChatSidebarSessionCount: y, removeSession: T, selectedSessionId: C, selectSession: A, sessionDisplayTitle: I, sessionFloorLabel: R, sessions: $, visibleCharacterAvatar: p } = o, { activeMemoryFiles: b, expandMemoryFileGroup: w, MEMORY_FILE_BATCH_SIZE: E, MEMORY_TURN_BATCH_SIZE: x, memoryDirectoryGroups: M, memoryFileDisplayName: N, memoryFiles: q, memoryFileSearchText: z, selectedMemoryFilePath: J, selectMemoryFile: G } = s, { rememberBrokenAvatar: oe } = r;
    function B() {
      n("close");
    }
    const { dragging: X, handleSheetHandlePointerCancel: de, handleSheetHandlePointerDown: fe, handleSheetHandlePointerMove: ve, handleSheetHandlePointerUp: Ce } = i5(B);
    return (Ue, le) => (O(), F("aside", ute, [
      g("button", {
        type: "button",
        class: ze(["chat-mobile-sheet-handle", { "is-dragging": S(X) }]),
        title: "收起目录",
        "aria-label": "收起目录",
        onClick: B,
        onPointercancel: le[0] || (le[0] = (...me) => S(de) && S(de)(...me)),
        onPointerdown: le[1] || (le[1] = (...me) => S(fe) && S(fe)(...me)),
        onPointermove: le[2] || (le[2] = (...me) => S(ve) && S(ve)(...me)),
        onPointerup: le[3] || (le[3] = (...me) => S(Ce) && S(Ce)(...me))
      }, null, 34),
      g("section", cte, [
        g("div", dte, [S(p) ? (O(), F("img", {
          key: 0,
          src: S(p),
          alt: "",
          onError: le[4] || (le[4] = (me) => S(oe)(S(p)))
        }, null, 40, fte)) : (O(), F("span", hte, K(S(m).slice(0, 1)), 1))]),
        g("div", null, [g("h2", null, K(S(m)), 1), g("p", null, K(S(f)), 1)]),
        g("div", pte, [
          g("button", {
            type: "button",
            class: ze(["xb-layout-button", { "is-active": S(i) === "chat" }]),
            onClick: le[5] || (le[5] = (me) => i.value = "chat")
          }, " 聊天 ", 2),
          g("button", {
            type: "button",
            class: ze(["xb-layout-button", { "is-active": S(i) === "balanced" }]),
            onClick: le[6] || (le[6] = (me) => i.value = "balanced")
          }, " 平衡 ", 2),
          g("button", {
            type: "button",
            class: ze(["xb-layout-button", { "is-active": S(i) === "editor" }]),
            onClick: le[7] || (le[7] = (me) => i.value = "editor")
          }, " 记忆 ", 2)
        ])
      ]),
      g("div", mte, [g("button", {
        type: "button",
        role: "tab",
        "aria-selected": S(d) === "memory",
        class: ze({ active: S(d) === "memory" }),
        onClick: le[8] || (le[8] = (me) => d.value = "memory")
      }, [le[12] || (le[12] = g("strong", null, "记忆", -1)), g("span", null, K(S(b).length), 1)], 10, gte), g("button", {
        type: "button",
        role: "tab",
        "aria-selected": S(d) === "sessions",
        class: ze({ active: S(d) === "sessions" }),
        onClick: le[9] || (le[9] = (me) => d.value = "sessions")
      }, [le[13] || (le[13] = g("strong", null, "会话", -1)), g("span", null, K(S(v)), 1)], 10, vte)]),
      S(d) === "sessions" ? (O(), F("section", yte, [g("div", _te, [le[14] || (le[14] = g("strong", null, "会话目录", -1)), g("span", null, K(S(v)), 1)]), g("div", bte, [
        (O(!0), F(Je, null, lt(S(u), (me) => (O(), F("div", {
          key: me.id,
          class: ze(["session-card compact", { active: me.id === S(C) }])
        }, [g("button", {
          type: "button",
          class: "session-open",
          onClick: (We) => S(A)(me.id)
        }, [g("strong", null, K(S(I)(me) || "未选择角色"), 1), g("small", null, K(S(R)(me)), 1)], 8, wte), g("button", {
          type: "button",
          class: "session-delete",
          title: "删除会话",
          "aria-label": "删除会话",
          onClick: (We) => S(T)(me.id, We)
        }, " 删除 ", 8, Ste)], 2))), 128)),
        S(y) ? (O(), F("button", {
          key: 0,
          type: "button",
          class: "session-more",
          onClick: le[10] || (le[10] = (me) => l.value += S(a))
        }, " 再显示 " + K(Math.min(S(y), S(a))) + " 个会话 ", 1)) : ge("", !0),
        S(u).length ? ge("", !0) : (O(), F("p", Tte, " 当前角色还没有其他会话。 "))
      ])])) : ge("", !0),
      S(d) === "memory" ? (O(), F("section", Ete, [
        g("div", Ate, [le[15] || (le[15] = g("strong", null, "记忆目录", -1)), g("span", null, K(S(b).length), 1)]),
        S(q).length ? (O(), F("label", Cte, [Ln(g("input", {
          "onUpdate:modelValue": le[11] || (le[11] = (me) => /* @__PURE__ */ pn(z) ? z.value = me : null),
          type: "search",
          placeholder: "检索档案"
        }, null, 512), [[Ro, S(z)]])])) : ge("", !0),
        S(q).length ? (O(), F("div", kte, [(O(!0), F(Je, null, lt(S(M), (me) => (O(), F("div", {
          key: me.key,
          class: "memory-file-group"
        }, [g("div", Ite, [g("span", null, K(me.title), 1), g("em", null, K(me.totalCount), 1)]), g("div", xte, [(O(!0), F(Je, null, lt(me.files, (We) => (O(), F("button", {
          key: We.path,
          type: "button",
          class: ze(["memory-file", {
            active: S(J) === We.path,
            stale: We.status === "stale"
          }]),
          onClick: (H) => S(G)(We.path)
        }, [g("span", Pte, K(S(N)(We)), 1)], 10, Mte))), 128)), me.hiddenCount ? (O(), F("button", {
          key: 0,
          type: "button",
          class: "memory-file memory-file-more",
          onClick: (We) => S(w)(me.key)
        }, [g("span", Nte, "再显示 " + K(Math.min(me.hiddenCount, me.key === "turns" ? S(x) : S(E))) + " 个", 1)], 8, Rte)) : ge("", !0)])]))), 128)), S(z) && !S(M).length ? (O(), F("p", $te, " 没有匹配的记忆档案。 ")) : ge("", !0)])) : (O(), F("p", Dte, " 还没有记忆档案。 "))
      ])) : ge("", !0)
    ]));
  }
}), Ote = Lte;
function Bg(e) {
  const t = /* @__PURE__ */ ie(!1);
  if (typeof window > "u" || typeof window.matchMedia != "function") return t;
  const n = window.matchMedia(e), r = () => {
    t.value = n.matches;
  };
  return r(), typeof n.addEventListener == "function" ? (n.addEventListener("change", r), cu(() => n.removeEventListener("change", r))) : (n.addListener(r), cu(() => n.removeListener(r))), t;
}
var Fte = { class: "panel-head preset-page-head" }, Bte = { class: "panel-pills" }, Ute = {
  key: 0,
  class: "pill warning"
}, qte = { class: "pill" }, Hte = { class: "preset-command-bar" }, zte = { class: "preset-source-select" }, Wte = ["value"], Gte = {
  key: 0,
  value: ""
}, Vte = ["value"], Kte = { class: "settings-toolstrip" }, Jte = ["disabled"], Yte = ["disabled"], Xte = {
  key: 0,
  class: "preset-status-line"
}, Qte = { class: "preset-studio" }, Zte = { class: "preset-edit-main prompt-sequence-panel" }, jte = { class: "preset-form-grid prompt-sequence-summary" }, ene = ["value"], tne = ["value"], nne = { class: "archive-toolbar preset-filterbar" }, rne = { class: "archive-search" }, one = { class: "prompt-manager-list" }, sne = {
  key: 0,
  class: "inline-empty-note"
}, ane = ["onClick"], ine = ["onClick"], lne = { class: "prompt-row-main" }, une = { class: "prompt-row-actions" }, cne = ["title", "aria-label"], dne = ["onClick"], fne = ["disabled", "onClick"], hne = ["disabled", "onClick"], pne = [
  "checked",
  "disabled",
  "onChange"
], mne = {
  key: 0,
  class: "preset-preview-panel prompt-detail-panel prompt-editor-panel"
}, gne = { class: "preset-preview-head" }, vne = {
  key: 0,
  class: "prompt-detail-form prompt-editor-form"
}, yne = ["value"], _ne = ["value"], bne = { class: "preset-text-field" }, wne = ["value", "disabled"], Sne = {
  key: 0,
  class: "muted compact"
}, Tne = {
  key: 1,
  class: "empty-note"
}, Ene = /* @__PURE__ */ un({
  __name: "TavernChatPresetSettingsPanel",
  setup(e) {
    const { activePromptOrderLabel: t, activeSettingsWorkspace: n, canEditPromptOrder: r, chatPresetOptions: o, discardPresetChanges: s, filteredPromptEditorRows: a, hiddenPromptCount: i, movePromptRow: l, preset: u, presetDirty: d, presetRows: f, presetStatus: m, presetTotalChars: v, PROMPT_EDITOR_BATCH_SIZE: y, promptEditorRows: T, promptRoleDisplay: C, promptRowIndex: A, promptSearchText: I, promptVisibleLimit: R, saveCurrentPreset: $, selectChatPresetFromHost: p, selectedPresetSourceId: b, selectedPromptIdentifier: w, selectedPromptRow: E, togglePromptRow: x, updatePromptByIdentifier: M, visiblePromptEditorRows: N } = Va(), q = re(() => String(b.value || u.value.promptManager?.name || u.value.name || "").trim()), z = /* @__PURE__ */ ie(!1), J = Bg("(max-width: 560px)"), G = re(() => n.value === "chatPreset" && (!J.value || z.value));
    function oe(de) {
      w.value = de, z.value = !0;
    }
    function B() {
      z.value = !1;
    }
    function X(de) {
      return de === "assistant" ? "A" : de === "user" ? "U" : "S";
    }
    return gt(n, (de) => {
      de !== "chatPreset" && (z.value = !1);
    }), (de, fe) => Ln((O(), F("div", { class: ze(["panel step-panel preset-workspace", { "is-mobile-editor-open": z.value }]) }, [
      g("div", Fte, [fe[9] || (fe[9] = g("div", null, [g("h2", null, "聊天预设")], -1)), g("div", Bte, [S(d) ? (O(), F("span", Ute, "未保存")) : ge("", !0), g("span", qte, K(S(f).length) + " 条 · " + K(S(v)) + " 字", 1)])]),
      g("div", Hte, [g("label", zte, [g("select", {
        value: q.value,
        onChange: fe[0] || (fe[0] = (ve) => S(p)(ve.target.value))
      }, [S(o).length ? ge("", !0) : (O(), F("option", Gte, " 没有可用预设 ")), (O(!0), F(Je, null, lt(S(o), (ve) => (O(), F("option", {
        key: ve.name,
        value: ve.name
      }, K(ve.label), 9, Vte))), 128))], 40, Wte)]), g("div", Kte, [g("button", {
        type: "button",
        class: "settings-icon-tool",
        title: "放弃修改",
        "aria-label": "放弃修改",
        disabled: !S(d),
        onClick: fe[1] || (fe[1] = (...ve) => S(s) && S(s)(...ve))
      }, [...fe[10] || (fe[10] = [g("svg", {
        viewBox: "0 0 24 24",
        "aria-hidden": "true"
      }, [g("path", { d: "M3 7v6h6" }), g("path", { d: "M21 17a8 8 0 0 0-13.7-5.7L3 15" })], -1)])], 8, Jte), g("button", {
        type: "button",
        class: "settings-icon-tool",
        title: "保存",
        "aria-label": "保存",
        disabled: !S(r) || !S(d),
        onClick: fe[2] || (fe[2] = (...ve) => S($) && S($)(...ve))
      }, [...fe[11] || (fe[11] = [g("svg", {
        viewBox: "0 0 24 24",
        "aria-hidden": "true"
      }, [
        g("path", { d: "M5 21h14a1 1 0 0 0 1-1V7.5L16.5 4H5a1 1 0 0 0-1 1v15a1 1 0 0 0 1 1Z" }),
        g("path", { d: "M8 21v-7h8v7" }),
        g("path", { d: "M8 4v5h7" })
      ], -1)])], 8, Yte)])]),
      S(m) ? (O(), F("div", Xte, [g("span", null, K(S(m)), 1)])) : ge("", !0),
      g("div", Qte, [g("section", Zte, [
        g("div", jte, [g("label", null, [fe[12] || (fe[12] = g("span", null, "当前来源", -1)), g("input", {
          value: S(u).promptManager?.name || "",
          readonly: ""
        }, null, 8, ene)]), g("label", null, [fe[13] || (fe[13] = g("span", null, "顺序", -1)), g("input", {
          value: S(t),
          readonly: ""
        }, null, 8, tne)])]),
        g("div", nne, [g("label", rne, [fe[14] || (fe[14] = g("span", null, "检索条目", -1)), Ln(g("input", {
          "onUpdate:modelValue": fe[3] || (fe[3] = (ve) => /* @__PURE__ */ pn(I) ? I.value = ve : null),
          type: "search",
          placeholder: "按名称、消息身份或内容搜索"
        }, null, 512), [[Ro, S(I)]])]), g("p", null, " 显示 " + K(Math.min(S(a).length, S(R))) + " / " + K(S(a).length) + "，共 " + K(S(T).length) + " 条 ", 1)]),
        g("div", one, [S(N).length ? ge("", !0) : (O(), F("div", sne, " 没有匹配的提示词条目。 ")), (O(!0), F(Je, null, lt(S(N), (ve) => (O(), F("div", {
          key: ve.identifier,
          class: ze(["prompt-manager-row", {
            selected: S(w) === ve.identifier,
            disabled: !ve.enabled,
            marker: ve.marker
          }]),
          onClick: (Ce) => w.value = ve.identifier
        }, [
          g("button", {
            type: "button",
            class: "prompt-row-index",
            title: "选择",
            onClick: sn((Ce) => w.value = ve.identifier, ["stop"])
          }, K(S(A)(ve.identifier) + 1), 9, ine),
          g("div", lne, [g("strong", null, K(ve.name), 1), g("small", null, K(S(C)(ve.role)), 1)]),
          g("div", une, [
            g("span", {
              class: "prompt-role-badge",
              title: S(C)(ve.role),
              "aria-label": S(C)(ve.role)
            }, K(X(ve.role)), 9, cne),
            g("button", {
              type: "button",
              class: "prompt-edit-button",
              title: "编辑",
              "aria-label": "编辑",
              onClick: sn((Ce) => oe(ve.identifier), ["stop"])
            }, [...fe[15] || (fe[15] = [g("svg", {
              viewBox: "0 0 24 24",
              "aria-hidden": "true"
            }, [g("path", { d: "M12 20h9" }), g("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])], 8, dne),
            g("button", {
              type: "button",
              title: "上移",
              class: "prompt-move-button",
              disabled: !S(r) || !!S(I) || S(A)(ve.identifier) === 0,
              onClick: sn((Ce) => S(l)(ve.identifier, -1), ["stop"])
            }, " ↑ ", 8, fne),
            g("button", {
              type: "button",
              title: "下移",
              class: "prompt-move-button",
              disabled: !S(r) || !!S(I) || S(A)(ve.identifier) === S(T).length - 1,
              onClick: sn((Ce) => S(l)(ve.identifier, 1), ["stop"])
            }, " ↓ ", 8, hne),
            g("label", {
              class: "prompt-toggle",
              title: "启用",
              onClick: fe[4] || (fe[4] = sn(() => {
              }, ["stop"]))
            }, [g("input", {
              type: "checkbox",
              checked: ve.enabled,
              disabled: !S(r),
              onChange: (Ce) => S(x)(ve.identifier, Ce.target.checked)
            }, null, 40, pne)])
          ])
        ], 10, ane))), 128))]),
        S(i) ? (O(), F("button", {
          key: 0,
          type: "button",
          class: "archive-load-more",
          onClick: fe[5] || (fe[5] = (ve) => R.value += S(y))
        }, " 再显示 " + K(Math.min(S(i), S(y))) + " 条 ", 1)) : ge("", !0)
      ]), G.value ? (O(), F("aside", mne, [g("div", gne, [
        g("strong", null, K(S(E)?.name || "提示词条目"), 1),
        g("span", null, K(S(C)(String(S(E)?.role || "system"))), 1),
        S(J) ? (O(), F("button", {
          key: 0,
          type: "button",
          class: "prompt-editor-close",
          title: "关闭",
          "aria-label": "关闭",
          onClick: B
        }, [...fe[16] || (fe[16] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [g("path", { d: "M6 6l12 12" }), g("path", { d: "M18 6 6 18" })], -1)])])) : ge("", !0)
      ]), S(E) ? (O(), F("div", vne, [
        g("label", null, [fe[17] || (fe[17] = g("span", null, "名称", -1)), g("input", {
          value: S(E).name,
          onInput: fe[6] || (fe[6] = (ve) => S(M)(S(E).identifier, { name: ve.target.value }))
        }, null, 40, yne)]),
        g("label", null, [fe[19] || (fe[19] = g("span", null, "消息身份", -1)), g("select", {
          value: S(E).role,
          onChange: fe[7] || (fe[7] = (ve) => S(M)(S(E).identifier, { role: ve.target.value }))
        }, [...fe[18] || (fe[18] = [
          g("option", { value: "system" }, "系统", -1),
          g("option", { value: "user" }, "用户", -1),
          g("option", { value: "assistant" }, "助手", -1)
        ])], 40, _ne)]),
        g("label", bne, [fe[20] || (fe[20] = g("span", null, "内容", -1)), g("textarea", {
          value: S(E).content,
          rows: "16",
          spellcheck: "false",
          disabled: S(E).marker,
          onInput: fe[8] || (fe[8] = (ve) => S(M)(S(E).identifier, { content: ve.target.value }))
        }, null, 40, wne)]),
        S(E).marker ? (O(), F("p", Sne, " 这是酒馆顺序占位条目，不允许编辑。 ")) : ge("", !0)
      ])) : (O(), F("div", Tne, " 当前预设没有可编辑条目。 "))])) : ge("", !0)])
    ], 2)), [[Iu, S(n) === "chatPreset"]]);
  }
}), l5 = Ene, Ane = /* @__PURE__ */ un({
  __name: "TavernScrollControls",
  props: {
    active: { type: Boolean },
    showTop: { type: Boolean },
    showBottom: { type: Boolean },
    extraClass: {}
  },
  emits: ["top", "bottom"],
  setup(e) {
    return (t, n) => (O(), F("div", { class: ze(["chat-scroll-helpers", [e.extraClass, { active: e.active }]]) }, [g("button", {
      type: "button",
      class: ze({ visible: e.showTop }),
      title: "回到顶部",
      "aria-label": "回到顶部",
      onClick: n[0] || (n[0] = (r) => t.$emit("top"))
    }, " ▲ ", 2), g("button", {
      type: "button",
      class: ze({ visible: e.showBottom }),
      title: "回到底部",
      "aria-label": "回到底部",
      onClick: n[1] || (n[1] = (r) => t.$emit("bottom"))
    }, " ▼ ", 2)], 2));
  }
}), u5 = Ane, Cne = { class: "message-edit-panel" }, kne = ["data-message-editor"], Ine = { class: "message-edit-actions" }, xne = ["disabled"], Mne = ["disabled"], Pne = /* @__PURE__ */ un({
  __name: "TavernMessageEditPanel",
  props: {
    message: {},
    messageKey: {}
  },
  emits: ["cancel", "save"],
  setup(e, { emit: t }) {
    const n = e, r = t, o = /* @__PURE__ */ ie(String(n.message.content || "")), s = /* @__PURE__ */ ie(null), a = /* @__PURE__ */ ie(null);
    let i = 0;
    const l = re(() => o.value.trim() !== String(n.message.content || "").trim());
    function u(y) {
      return y.closest(".chat-scroll");
    }
    function d() {
      const y = s.value, T = a.value;
      if (!y || !T) return;
      const C = u(y), A = C?.scrollTop ?? 0, I = y.offsetHeight, R = 144, $ = 420, p = window.getComputedStyle(y);
      T.textContent = y.value || " ", T.style.width = `${y.clientWidth}px`, T.style.font = p.font, T.style.lineHeight = p.lineHeight, T.style.letterSpacing = p.letterSpacing, T.style.padding = p.padding, T.style.border = p.border, T.style.boxSizing = p.boxSizing;
      const b = Math.min(Math.max(T.scrollHeight, R), $);
      Math.abs(I - b) >= 1 && (y.style.height = `${b}px`, C && (C.scrollTop = A)), C && (i && cancelAnimationFrame(i), i = requestAnimationFrame(() => {
        i = 0, C.scrollTop = A;
      }));
    }
    function f() {
      Qt(() => {
        const y = s.value;
        y && (d(), y.focus(), y.setSelectionRange(y.value.length, y.value.length));
      });
    }
    function m(y = !1) {
      l.value && r("save", {
        content: o.value,
        rerun: y
      });
    }
    function v(y) {
      if (y.key === "Escape") {
        y.preventDefault(), r("cancel");
        return;
      }
      y.key === "Enter" && (y.ctrlKey || y.metaKey) && (y.preventDefault(), m(!1));
    }
    return gt(() => n.messageKey, () => {
      o.value = String(n.message.content || ""), f();
    }, { immediate: !0 }), cu(() => {
      i && (cancelAnimationFrame(i), i = 0);
    }), (y, T) => (O(), F("div", Cne, [
      Ln(g("textarea", {
        ref_key: "textareaRef",
        ref: s,
        "onUpdate:modelValue": T[0] || (T[0] = (C) => o.value = C),
        class: "message-edit-box",
        rows: "6",
        "data-message-editor": e.messageKey,
        onInput: d,
        onKeydown: v
      }, null, 40, kne), [[Ro, o.value]]),
      g("div", {
        ref_key: "sizerRef",
        ref: a,
        class: "message-edit-sizer",
        "aria-hidden": "true"
      }, null, 512),
      g("div", Ine, [
        g("button", {
          type: "button",
          disabled: !l.value,
          onClick: T[1] || (T[1] = (C) => m(!1))
        }, K(e.message.role === "user" ? "保存" : "保存修改"), 9, xne),
        e.message.role === "user" ? (O(), F("button", {
          key: 0,
          type: "button",
          disabled: !l.value,
          onClick: T[2] || (T[2] = (C) => m(!0))
        }, " 保存并从这里重来 ", 8, Mne)) : ge("", !0),
        g("button", {
          type: "button",
          onClick: T[3] || (T[3] = (C) => y.$emit("cancel"))
        }, " 取消 ")
      ])
    ]));
  }
}), Rne = Pne;
function Nne(e) {
  const t = String(e.text || ""), n = Array.isArray(e.thoughts) ? e.thoughts : [], r = Array.isArray(e.actionCheckEvents) ? e.actionCheckEvents : [];
  return !!t || n.length > 0 || r.length > 0;
}
function $ne(e) {
  const t = String(e.text || ""), n = Array.isArray(e.actionCheckEvents) ? e.actionCheckEvents : [];
  return !!t || n.length > 0;
}
var Dne = ["aria-hidden"], Lne = {
  key: 0,
  class: "chat-ambient-standee",
  "aria-hidden": "true"
}, One = ["src"], Fne = { class: "chat-head" }, Bne = { class: "chat-head-actions" }, Une = { class: "chat-scroll-shell" }, qne = ["data-chat-anchor-key"], Hne = ["data-chat-anchor-key", "onClick"], zne = { class: "bubble-meta" }, Wne = { class: "bubble-identity" }, Gne = { class: "bubble-nameplate" }, Vne = { class: "bubble-avatar-stamp" }, Kne = ["src"], Jne = ["src"], Yne = { key: 2 }, Xne = { class: "bubble-role-name" }, Qne = { class: "bubble-meta-line" }, Zne = ["title", "aria-label"], jne = { class: "bubble-time-tag" }, ere = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], tre = ["disabled", "onClick"], nre = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], rre = ["disabled", "onClick"], ore = ["open", "onToggle"], sre = { class: "tavern-thought-label" }, are = [
  "data-markdown-signature",
  "data-action-check-groups",
  "innerHTML"
], ire = { class: "scene-tag" }, lre = {
  key: 1,
  "data-chat-anchor-key": "pending:user",
  class: "chat-bubble from-user pending-user"
}, ure = { class: "bubble-meta" }, cre = { class: "bubble-identity" }, dre = { class: "bubble-nameplate" }, fre = { class: "bubble-avatar-stamp" }, hre = ["src"], pre = { key: 1 }, mre = { class: "bubble-role-name" }, gre = ["data-markdown-signature", "innerHTML"], vre = {
  key: 2,
  "data-chat-anchor-key": "streaming:content",
  class: "chat-bubble from-assistant streaming"
}, yre = { class: "bubble-meta" }, _re = { class: "bubble-nameplate" }, bre = { class: "bubble-avatar-stamp" }, wre = ["src"], Sre = { key: 1 }, Tre = { class: "bubble-role-name" }, Ere = ["open"], Are = { class: "tavern-thought-label" }, Cre = [
  "data-action-check-groups",
  "data-markdown-signature",
  "innerHTML"
], kre = {
  key: 3,
  "data-chat-anchor-key": "streaming:empty",
  class: "chat-bubble from-assistant streaming thinking"
}, Ire = { class: "bubble-meta" }, xre = { class: "bubble-nameplate" }, Mre = { class: "bubble-avatar-stamp" }, Pre = ["src"], Rre = { key: 1 }, Nre = { class: "bubble-role-name" }, $re = {
  key: 4,
  class: "chat-empty"
}, Dre = { class: "chat-compose-dock" }, Lre = {
  key: 0,
  class: "compose-error"
}, Ore = { class: "compose-menu-shell" }, Fre = ["aria-expanded", "disabled"], Bre = ["onKeydown"], Ure = ["disabled"], qre = ["disabled", "aria-label"], Hre = {
  class: "compose-send-icon",
  "aria-hidden": "true"
}, zre = { class: "compose-send-label" }, Pc = "chat:runtime-thoughts", Wre = /* @__PURE__ */ un({
  __name: "TavernConversationPanel",
  emits: [
    "open-contract",
    "open-session-archive",
    "open-author-note"
  ],
  setup(e, { emit: t }) {
    const n = t, r = Af(), o = Fg(), { activeView: s, homeThemeDark: a, openPromptInspector: i, rememberBrokenAvatar: l } = r, { actionFeedback: u, cancelEditMessage: d, canDrawMessage: f, canEditMessage: m, canRerunMessage: v, canSendMessage: y, createNewChatSession: T, chatComposeTextareaRef: C, chatFocus: A, chatMessages: I, chatMessageWindow: R, chatScrollControlsActive: $, chatScrollRef: p, currentUserMessage: b, deleteMessageTurn: w, displayMessageContent: E, displayMessageRenderProjection: x, displayMessageThoughtBlocks: M, displayRuntimeRenderProjection: N, displayRuntimeThoughtBlocks: q, drawMessage: z, drawMessageStatusClass: J, drawMessageStatusText: G, drawMessageTitle: oe, formatMessageTime: B, handleChatScroll: X, handleChatSubmit: de, handleChatTouchMove: fe, handleChatTouchStart: ve, handleChatWheel: Ce, handleComposeInput: Ue, handleComposeKeydown: le, isDrawingMessage: me, isEditingMessage: We, isCancellingRun: H, isRunning: W, latestErrorMessage: se, markdownSignature: ke, htmlRenderEnabled: Pe, messageKey: Le, renderChatMarkdown: Y, rerunFromMessage: te, revealOlderChatMessages: he, roleLabel: we, runtimeActionCheckEvents: Te, runtimePendingUserMessage: Ie, runtimeText: Ne, runtimeThoughts: Oe, runtimeUserMessageVisible: L, saveEditMessage: P, scrollChatToBottom: Q, scrollChatToTop: V, selectedSessionId: ae, showChatScrollBottom: Se, showChatScrollTop: Me, startEditMessage: Ke, thoughtBlocks: qe, thoughtSummaryLabel: Be, visibleCharacterAvatar: Ee, visibleChatMessages: Ye, visibleUserAvatar: je } = o;
    function It(Ve) {
      p.value = Ve instanceof HTMLElement ? Ve : null;
    }
    function Ht(Ve) {
      C.value = Ve instanceof HTMLTextAreaElement ? Ve : null;
    }
    function At() {
      n("open-contract");
    }
    function Mt() {
      return {
        roleplay: !0,
        userName: we("user"),
        characterName: we("assistant")
      };
    }
    function Ut(Ve = "", He = "") {
      const Re = Mt();
      return ke([
        Ve,
        He,
        Re.userName,
        Re.characterName,
        Pe.value ? "html-render:on" : "html-render:off"
      ].join("\0"));
    }
    function yt(Ve = "") {
      return Y(Ve, Mt());
    }
    function et(Ve) {
      return `#${Math.max(1, Number(Ve.order) + 1)}`;
    }
    function xe(Ve, He = []) {
      const Re = m9(Ve, He), _t = Re.groups.length ? JSON.stringify(Re.groups) : "";
      return {
        text: Re.text,
        signature: Ut(Re.text, _t),
        actionCheckGroups: _t
      };
    }
    function Ze(Ve) {
      if (Ve.role !== "assistant") {
        const Re = E(Ve);
        return {
          text: Re,
          signature: Ut(Re),
          actionCheckGroups: ""
        };
      }
      const He = x(Ve);
      return xe(He.text, He.actionCheckEvents);
    }
    const zt = re(() => {
      const Ve = N(Ne.value, Array.isArray(Te.value) ? Te.value : []);
      return xe(Ve.text, Ve.actionCheckEvents);
    }), jt = re(() => Nne({
      text: Ne.value,
      thoughts: Oe.value,
      actionCheckEvents: Te.value
    })), Cn = re(() => W.value && L.value), Gn = re(() => $ne({
      text: Ne.value,
      actionCheckEvents: Te.value
    })), er = re(() => W.value && !L.value && !!Ie.value.trim()), wt = re(() => {
      const Ve = Ie.value.trim();
      return {
        text: Ve,
        signature: Ut(Ve, "pending-user")
      };
    }), nn = Og(), Ft = Bg("(max-width: 760px)"), yn = /* @__PURE__ */ ie(""), ot = /* @__PURE__ */ ie(!1);
    function ye(Ve) {
      return `chat:thought:${Le(Ve)}`;
    }
    function dt(Ve) {
      return yn.value === Le(Ve);
    }
    function dn(Ve) {
      !Ft.value || We(Ve) || (yn.value = Le(Ve));
    }
    function Pt() {
      yn.value = "";
    }
    function j() {
      ot.value = !1;
    }
    function ce() {
      Pt(), j();
    }
    function ne() {
      ot.value = !ot.value;
    }
    async function it() {
      j(), await T();
    }
    function Xe() {
      j(), n("open-session-archive");
    }
    function Lt() {
      j(), n("open-author-note");
    }
    function qt() {
      j(), i("history");
    }
    return gt([
      s,
      A,
      ae
    ], ([Ve, He]) => {
      (Ve !== "chat" || He !== "chat") && (nn.reset(), Pt(), j());
    }), gt(() => `${ae.value}:${R.value.startIndex}:${R.value.visibleCount}`, () => {
      nn.reset(), Pt(), j();
    }), gt(Ft, (Ve) => {
      Ve || Pt();
    }), (Ve, He) => (O(), F("section", {
      class: "chat-face chat-face-front chat-main",
      "aria-hidden": S(A) === "manager",
      onClick: ce
    }, [
      S(Ee) ? (O(), F("div", Lne, [g("img", {
        src: S(Ee),
        alt: "",
        onError: He[0] || (He[0] = (Re) => S(l)(S(Ee)))
      }, null, 40, One)])) : ge("", !0),
      g("header", Fne, [g("div", Bne, [g("button", {
        type: "button",
        class: "contract-trigger",
        title: "契约",
        "aria-label": "契约",
        onClick: At
      }, " 契约 "), g("button", {
        type: "button",
        class: "chat-flip-trigger",
        title: "助手聊天",
        "aria-label": "助手聊天",
        onClick: He[1] || (He[1] = (Re) => A.value = "manager")
      }, " 助手 ")])]),
      g("div", Une, [g("div", {
        ref: It,
        class: "chat-scroll",
        onScroll: He[12] || (He[12] = (...Re) => S(X) && S(X)(...Re)),
        onClick: Pt,
        onWheelPassive: He[13] || (He[13] = (...Re) => S(Ce) && S(Ce)(...Re)),
        onTouchstartPassive: He[14] || (He[14] = (...Re) => S(ve) && S(ve)(...Re)),
        onTouchmovePassive: He[15] || (He[15] = (...Re) => S(fe) && S(fe)(...Re))
      }, [
        S(R).hiddenBefore ? (O(), F("div", {
          key: 0,
          class: "chat-history-gate",
          "data-chat-anchor-key": `gate:${S(R).hiddenBefore}`,
          role: "button",
          tabindex: "0",
          onClick: He[2] || (He[2] = (Re) => S(he)(!0)),
          onKeydown: [He[3] || (He[3] = Vs(sn((Re) => S(he)(!0), ["prevent"]), ["enter"])), He[4] || (He[4] = Vs(sn((Re) => S(he)(!0), ["prevent"]), ["space"]))]
        }, " 展开较早记录 " + K(S(R).hiddenBefore) + " 条 ", 41, qne)) : ge("", !0),
        (O(!0), F(Je, null, lt(S(Ye), (Re) => (O(), F(Je, { key: `${Re.sessionId}-${Re.order}` }, [g("div", {
          "data-chat-anchor-key": `${Re.sessionId}:${Re.order}`,
          class: ze(["chat-bubble", [
            Re.role === "user" ? "from-user" : "from-assistant",
            { "is-error": Re.error },
            { "is-action-tray-open": dt(Re) }
          ]]),
          onClick: sn((_t) => dn(Re), ["stop"])
        }, [
          g("div", zne, [g("div", Wne, [g("span", Gne, [
            g("span", Vne, [Re.role === "user" && S(je) ? (O(), F("img", {
              key: 0,
              src: S(je),
              alt: "",
              onError: He[5] || (He[5] = (_t) => S(l)(S(je)))
            }, null, 40, Kne)) : Re.role !== "user" && S(Ee) ? (O(), F("img", {
              key: 1,
              src: S(Ee),
              alt: "",
              onError: He[6] || (He[6] = (_t) => S(l)(S(Ee)))
            }, null, 40, Jne)) : (O(), F("span", Yne, K(String(S(we)(Re.role)).slice(0, 1)), 1))]),
            g("span", Xne, K(Re.error ? "错误" : S(we)(Re.role)), 1),
            g("span", Qne, [g("span", {
              class: "message-floor-label",
              title: `第 ${et(Re).slice(1)} 楼`,
              "aria-label": `第 ${et(Re).slice(1)} 楼`
            }, K(et(Re)), 9, Zne), g("small", jne, K(S(B)(Re.createdAt)), 1)])
          ])])]),
          S(We)(Re) ? ge("", !0) : (O(), F("div", {
            key: 0,
            class: ze(["message-actions", { "has-status": !!S(G)(Re) }]),
            onClick: He[7] || (He[7] = sn(() => {
            }, ["stop"]))
          }, [
            S(G)(Re) ? (O(), F("span", {
              key: 0,
              class: ze(["message-draw-status", S(J)(Re)])
            }, K(S(G)(Re)), 3)) : ge("", !0),
            g("button", {
              type: "button",
              disabled: !S(f)(Re),
              class: ze([S(u)(Re, "draw"), { "is-running": S(me)(Re) }]),
              title: S(oe)(Re),
              "aria-label": S(oe)(Re),
              onClick: (_t) => S(z)(Re)
            }, K(S(me)(Re) ? "■" : "🎨"), 11, ere),
            g("button", {
              type: "button",
              disabled: !S(m)(Re),
              class: ze(S(u)(Re, "edit")),
              title: "编辑",
              "aria-label": "编辑",
              onClick: (_t) => S(Ke)(Re)
            }, [...He[25] || (He[25] = [g("svg", {
              "aria-hidden": "true",
              viewBox: "0 0 24 24"
            }, [g("path", { d: "M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7" }), g("path", { d: "M18.5 2.5a2.12 2.12 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z" })], -1)])], 10, tre),
            g("button", {
              type: "button",
              disabled: !S(v)(Re),
              class: ze(S(u)(Re, "rerun")),
              title: Re.role === "user" ? "发送最后一楼" : "重新生成最后回复",
              "aria-label": Re.role === "user" ? "发送最后一楼" : "重新生成最后回复",
              onClick: (_t) => S(te)(Re)
            }, [...He[26] || (He[26] = [g("svg", {
              "aria-hidden": "true",
              viewBox: "0 0 24 24"
            }, [g("path", { d: "M1 4v6h6M23 20v-6h-6" }), g("path", { d: "M20.49 9A9 9 0 0 0 5.64 5.64L1 10m22 4-4.64 4.36A9 9 0 0 1 3.51 15" })], -1)])], 10, nre),
            He[28] || (He[28] = g("span", {
              class: "message-action-divider",
              "aria-hidden": "true"
            }, null, -1)),
            g("button", {
              type: "button",
              disabled: S(W),
              class: ze(S(u)(Re, "delete")),
              title: "从这里删除后续剧情",
              "aria-label": "从这里删除后续剧情",
              onClick: (_t) => S(w)(Re)
            }, [...He[27] || (He[27] = [g("svg", {
              "aria-hidden": "true",
              viewBox: "0 0 24 24"
            }, [g("path", { d: "M3 6h18M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2" })], -1)])], 10, rre)
          ], 2)),
          S(We)(Re) ? (O(), gn(Rne, {
            key: 1,
            message: Re,
            "message-key": S(Le)(Re),
            onCancel: S(d),
            onSave: (_t) => S(P)(Re, _t)
          }, null, 8, [
            "message",
            "message-key",
            "onCancel",
            "onSave"
          ])) : ge("", !0),
          (O(!0), F(Je, null, lt([S(qe)(Re)], (_t) => (O(), F(Je, { key: `${S(Le)(Re)}:raw-thoughts:${_t.length}` }, [!S(We)(Re) && _t.length ? (O(), F("details", {
            key: 0,
            class: "tavern-thought-details",
            open: S(nn).isOpen(ye(Re)),
            onToggle: (wn) => S(nn).setOpenFromEvent(ye(Re), wn)
          }, [g("summary", null, K(S(Be)(_t)), 1), S(nn).isOpen(ye(Re)) ? (O(!0), F(Je, { key: 0 }, lt([S(M)(Re)], (wn) => (O(), F(Je, { key: `${S(Le)(Re)}:display-thoughts:${wn.length}` }, [(O(!0), F(Je, null, lt(wn, (so, Pr) => (O(), F("div", {
            key: `${Re.sessionId}-${Re.order}-thought-${Pr}`,
            class: "tavern-thought-block"
          }, [g("div", sre, K(so.label), 1), g("pre", null, K(so.text), 1)]))), 128))], 64))), 128)) : ge("", !0)], 40, ore)) : ge("", !0)], 64))), 128)),
          S(We)(Re) ? ge("", !0) : (O(!0), F(Je, { key: 2 }, lt([Ze(Re)], (_t) => (O(), F("div", {
            key: `${S(Le)(Re)}:${_t.signature}`,
            class: "xb-tavern-markdown",
            "data-markdown-signature": _t.signature,
            "data-action-check-groups": _t.actionCheckGroups || null,
            innerHTML: yt(_t.text)
          }, null, 8, are))), 128))
        ], 10, Hne), (O(!0), F(Je, null, lt(Re.role === "user" ? Re.runtimeEvents || [] : [], (_t, wn) => (O(), F("div", {
          key: `${Re.sessionId}-${Re.order}-runtime-event-${_t.type}-${wn}`,
          class: "chat-runtime-event scene-narration",
          "aria-hidden": "true"
        }, [g("div", ire, K(String(_t.label || "")), 1)]))), 128))], 64))), 128)),
        er.value ? (O(), F("div", lre, [g("div", ure, [g("div", cre, [g("span", dre, [
          g("span", fre, [S(je) ? (O(), F("img", {
            key: 0,
            src: S(je),
            alt: "",
            onError: He[8] || (He[8] = (Re) => S(l)(S(je)))
          }, null, 40, hre)) : (O(), F("span", pre, K(String(S(we)("user")).slice(0, 1)), 1))]),
          g("span", mre, K(S(we)("user")), 1),
          He[29] || (He[29] = g("span", { class: "bubble-meta-line" }, [g("small", { class: "bubble-time-tag" }, "发送中")], -1))
        ])])]), (O(), F("div", {
          key: `pending-user:${wt.value.signature}`,
          class: "xb-tavern-markdown",
          "data-markdown-signature": wt.value.signature,
          innerHTML: yt(wt.value.text)
        }, null, 8, gre))])) : ge("", !0),
        Cn.value && jt.value ? (O(), F("div", vre, [
          g("div", yre, [g("span", _re, [g("span", bre, [S(Ee) ? (O(), F("img", {
            key: 0,
            src: S(Ee),
            alt: "",
            onError: He[9] || (He[9] = (Re) => S(l)(S(Ee)))
          }, null, 40, wre)) : (O(), F("span", Sre, K(String(S(we)("assistant")).slice(0, 1)), 1))]), g("span", Tre, K(S(we)("assistant")), 1)]), He[30] || (He[30] = g("small", null, "生成中", -1))]),
          (O(!0), F(Je, null, lt([S(qe)(S(Oe))], (Re) => (O(), F(Je, { key: `${Pc}:${Re.length}` }, [Re.length ? (O(), F("details", {
            key: 0,
            class: "tavern-thought-details",
            open: S(nn).isOpen(Pc, !0),
            onToggle: He[10] || (He[10] = (_t) => S(nn).setOpenFromEvent(Pc, _t))
          }, [g("summary", null, K(S(Be)(Re, !0)), 1), S(nn).isOpen(Pc, !0) ? (O(!0), F(Je, { key: 0 }, lt([S(q)(Re)], (_t) => (O(), F(Je, { key: `${Pc}:display:${_t.length}` }, [(O(!0), F(Je, null, lt(_t, (wn, so) => (O(), F("div", {
            key: `runtime-thought-${so}`,
            class: "tavern-thought-block"
          }, [g("div", Are, K(wn.label), 1), g("pre", null, K(wn.text), 1)]))), 128))], 64))), 128)) : ge("", !0)], 40, Ere)) : ge("", !0)], 64))), 128)),
          Gn.value ? (O(), F("div", {
            key: `live-assistant:${zt.value.signature}`,
            class: "xb-tavern-markdown",
            "data-action-check-groups": zt.value.actionCheckGroups || void 0,
            "data-markdown-signature": zt.value.signature,
            innerHTML: yt(zt.value.text)
          }, null, 8, Cre)) : ge("", !0)
        ])) : ge("", !0),
        Cn.value && !jt.value ? (O(), F("div", kre, [g("div", Ire, [g("span", xre, [g("span", Mre, [S(Ee) ? (O(), F("img", {
          key: 0,
          src: S(Ee),
          alt: "",
          onError: He[11] || (He[11] = (Re) => S(l)(S(Ee)))
        }, null, 40, Pre)) : (O(), F("span", Rre, K(String(S(we)("assistant")).slice(0, 1)), 1))]), g("span", Nre, K(S(we)("assistant")), 1)]), He[31] || (He[31] = g("small", null, "生成中", -1))]), He[32] || (He[32] = g("p", null, "正在组织回复...", -1))])) : ge("", !0),
        !S(I).length && !S(W) ? (O(), F("p", $re, " 写一句话，开始。 ")) : ge("", !0),
        He[33] || (He[33] = g("div", {
          class: "chat-compose-spacer",
          "aria-hidden": "true"
        }, null, -1))
      ], 32), En(u5, {
        active: S($),
        "show-top": S(Me),
        "show-bottom": S(Se),
        onTop: S(V),
        onBottom: He[16] || (He[16] = (Re) => S(Q)(!0, {
          collapseWindow: !0,
          revealHelpers: !0
        }))
      }, null, 8, [
        "active",
        "show-top",
        "show-bottom",
        "onTop"
      ])]),
      g("div", Dre, [g("div", { class: ze(["chat-compose-shell", { "has-text": !!S(b).trim() }]) }, [
        S(se) ? (O(), F("div", Lre, K(S(se)), 1)) : ge("", !0),
        g("div", Ore, [g("button", {
          type: "button",
          class: "compose-menu-button",
          title: "聊天操作",
          "aria-label": "聊天操作",
          "aria-expanded": ot.value ? "true" : "false",
          "aria-controls": "xb-tavern-compose-menu",
          disabled: S(W) || S(H),
          onClick: sn(ne, ["stop"])
        }, [...He[34] || (He[34] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M4 6h16" }),
          g("path", { d: "M4 12h16" }),
          g("path", { d: "M4 18h16" })
        ], -1)])], 8, Fre), ot.value ? (O(), F("div", {
          key: 0,
          id: "xb-tavern-compose-menu",
          class: "compose-menu-popover",
          role: "menu",
          onKeydown: Vs(sn(j, ["stop", "prevent"]), ["esc"]),
          onPointerdown: He[17] || (He[17] = sn(() => {
          }, ["stop"])),
          onMousedown: He[18] || (He[18] = sn(() => {
          }, ["stop"])),
          onTouchstart: He[19] || (He[19] = sn(() => {
          }, ["stop"])),
          onClick: He[20] || (He[20] = sn(() => {
          }, ["stop"]))
        }, [
          g("button", {
            type: "button",
            role: "menuitem",
            class: "compose-menu-item",
            onClick: it
          }, " 新建会话 "),
          g("button", {
            type: "button",
            role: "menuitem",
            class: "compose-menu-item",
            onClick: Xe
          }, " 会话档案 "),
          g("button", {
            type: "button",
            role: "menuitem",
            class: "compose-menu-item",
            onClick: Lt
          }, " 玩家便签 "),
          g("button", {
            type: "button",
            role: "menuitem",
            class: "compose-menu-item",
            onClick: qt
          }, " 请求日志 ")
        ], 40, Bre)) : ge("", !0)]),
        g("form", {
          class: "chat-compose",
          onSubmit: He[24] || (He[24] = sn((...Re) => S(de) && S(de)(...Re), ["prevent"]))
        }, [Ln(g("textarea", {
          ref: Ht,
          "onUpdate:modelValue": He[21] || (He[21] = (Re) => /* @__PURE__ */ pn(b) ? b.value = Re : null),
          rows: "1",
          placeholder: "对角色说一句话...",
          disabled: S(W),
          onInput: He[22] || (He[22] = (...Re) => S(Ue) && S(Ue)(...Re)),
          onKeydown: He[23] || (He[23] = (...Re) => S(le) && S(le)(...Re)),
          onFocus: j
        }, null, 40, Ure), [[Ro, S(b)]]), g("button", {
          type: "submit",
          class: "primary-action",
          disabled: !S(y),
          "aria-label": S(H) ? "正在停止" : S(W) ? "停止" : "发送"
        }, [g("span", Hre, K(S(H) ? "..." : S(W) ? "■" : "➤"), 1), g("span", zre, K(S(H) ? "正在停止" : S(W) ? "停止" : "发送"), 1)], 8, qre)], 32)
      ], 2)])
    ], 8, Dne));
  }
}), Gre = Wre, Vre = ["aria-hidden"], Kre = { class: "manager-head" }, Jre = { class: "manager-head-actions" }, Yre = { class: "chat-scroll-shell manager-scroll-shell" }, Xre = { key: 0 }, Qre = ["data-manager-anchor-key"], Zre = ["data-manager-anchor-key"], jre = { class: "manager-run-title" }, eoe = {
  key: 0,
  class: "message-edit-panel manager-message-edit-panel"
}, toe = ["data-manager-message-editor", "onKeydown"], noe = { class: "message-edit-actions" }, roe = ["disabled", "onClick"], ooe = ["disabled", "onClick"], soe = ["data-markdown-signature", "innerHTML"], aoe = {
  key: 2,
  class: "message-actions manager-message-actions"
}, ioe = ["onClick"], loe = ["disabled", "onClick"], uoe = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], coe = ["disabled", "onClick"], doe = [
  "data-manager-anchor-key",
  "open",
  "onToggle"
], foe = {
  key: 0,
  class: "manager-tool-turn-body"
}, hoe = { class: "manager-tool-round-title" }, poe = ["open", "onToggle"], moe = ["data-markdown-signature", "innerHTML"], goe = { class: "manager-tool-head" }, voe = { key: 0 }, yoe = ["data-manager-anchor-key"], _oe = { class: "manager-run-title" }, boe = ["data-markdown-signature", "innerHTML"], woe = [
  "data-manager-anchor-key",
  "open",
  "onToggle"
], Soe = {
  key: 0,
  class: "manager-tool-turn-body"
}, Toe = { class: "manager-tool-round-title" }, Eoe = ["open", "onToggle"], Aoe = ["data-markdown-signature", "innerHTML"], Coe = { class: "manager-tool-head" }, koe = { key: 0 }, Ioe = {
  key: 2,
  class: "manager-card manager-message manager-message-assistant manager-message-live",
  "data-manager-anchor-key": "live:manager-thinking"
}, xoe = ["open"], Moe = { key: 0 }, Poe = { key: 1 }, Roe = ["data-manager-anchor-key", "aria-busy"], Noe = { class: "manager-run-title" }, $oe = { class: "manager-work-source" }, Doe = { class: "manager-run-activity" }, Loe = { class: "manager-work-status-grid" }, Ooe = { key: 0 }, Foe = {
  key: 1,
  class: "manager-work-issue-line"
}, Boe = ["open"], Uoe = {
  key: 0,
  class: "manager-tool-list"
}, qoe = { class: "manager-tool-head" }, Hoe = ["open", "onToggle"], zoe = ["data-markdown-signature", "innerHTML"], Woe = { key: 2 }, Goe = { key: 3 }, Voe = { key: 4 }, Koe = ["disabled"], Joe = {
  key: 1,
  class: "manager-card manager-message manager-message-system",
  "data-manager-anchor-key": "meta:memory"
}, Yoe = { class: "manager-run-title" }, Xoe = { key: 0 }, Qoe = ["open"], Zoe = { key: 0 }, joe = {
  key: 4,
  "data-manager-anchor-key": "empty",
  class: "chat-empty"
}, ese = {
  key: 0,
  class: "compose-error"
}, tse = ["disabled"], nse = ["disabled"], rse = /* @__PURE__ */ un({
  __name: "TavernManagerPanel",
  emits: ["open-contract"],
  setup(e, { emit: t }) {
    const n = t, r = Af(), o = Fg(), s = o5(), { activeView: a, shortText: i } = r, { chatFocus: l, cancelEditMessage: u, formatMessageTime: d, handleComposeInput: f, htmlRenderEnabled: m, markdownSignature: v, renderChatMarkdown: y, roleLabel: T, thoughtBlocks: C, thoughtSummaryLabel: A } = o, { activeMemoryFiles: I, archivedManagerRuns: R, canEditManagerMessage: $, canRerunManagerMessage: p, canSendManagerMessage: b, copyManagerMessage: w, currentManagerWorkRun: E, deleteManagerMessageTurn: x, editingMessageDraft: M, formatRunActivityLine: N, formatRunIssueLine: q, formatRunInputLine: z, formatRunMapLine: J, formatRunMemoryLine: G, formatRunModelLine: oe, formatRunTaskLine: B, handleEditInput: X, handleManagerComposeKeydown: de, handleManagerEditKeydown: fe, handleManagerScroll: ve, handleManagerSubmit: Ce, handleManagerTouchMove: Ue, handleManagerTouchStart: le, handleManagerWheel: me, hiddenManagerRunCount: We, isEditingManagerMessage: H, isEditingManagerMessageDirty: W, isManagerAssistantCancelling: se, isManagerAssistantRunning: ke, isManagerRunRetrying: Pe, liveManagerChatDisplayItems: Le, managerActionFeedback: Y, managerBusy: te, managerCompactionOverlay: he, managerComposeTextareaRef: we, managerInputDraft: Te, managerInputStatus: Ie, managerMessageWindow: Ne, managerRuns: Oe, managerRunTone: L, managerScrollControlsActive: P, managerScrollRef: Q, managerStatusLabel: V, managerToolStatusLabel: ae, managerToolTone: Se, managerToolTraceItems: Me, managerToolTurnPreview: Ke, managerToolTurnSummary: qe, memoryFileDisplayName: Be, memoryFiles: Ee, memoryIndexStatusLine: Ye, retryManagerRun: je, revealOlderManagerMessages: It, rerunFromManagerMessage: Ht, saveEditManagerMessage: At, scrollManagerToBottom: Mt, scrollManagerToTop: Ut, selectedMemoryFile: yt, showManagerScrollBottom: et, showManagerScrollTop: xe, startEditManagerMessage: Ze, toolTraceSummary: zt, visibleManagerChatItems: jt } = s;
    function Cn(yn) {
      Q.value = yn instanceof HTMLElement ? yn : null;
    }
    function Gn(yn) {
      we.value = yn instanceof HTMLTextAreaElement ? yn : null;
    }
    function er() {
      n("open-contract");
    }
    const wt = Og();
    function nn(yn = "") {
      return v([yn, m.value ? "html-render:on" : "html-render:off"].join("\0"));
    }
    function Ft(yn, ...ot) {
      return `manager:${yn}:${ot.map((ye) => String(ye ?? "")).join(":")}`;
    }
    return gt([a, l], ([yn, ot]) => {
      (yn !== "chat" || ot !== "manager") && wt.reset();
    }), gt(() => `${Ne.value.startIndex}:${Ne.value.visibleCount}`, () => wt.reset()), (yn, ot) => (O(), F("section", {
      class: "chat-face chat-face-back chat-manager",
      "aria-hidden": S(l) === "chat"
    }, [
      g("header", Kre, [g("div", Jre, [g("button", {
        type: "button",
        class: "contract-trigger",
        title: "契约",
        "aria-label": "契约",
        onClick: er
      }, " 契约 "), g("button", {
        type: "button",
        class: "chat-flip-trigger",
        title: "角色聊天",
        "aria-label": "角色聊天",
        onClick: ot[0] || (ot[0] = (ye) => l.value = "chat")
      }, " 角色 ")])]),
      g("div", Yre, [g("div", {
        ref: Cn,
        class: "manager-chat-scroll",
        onScroll: ot[11] || (ot[11] = (...ye) => S(ve) && S(ve)(...ye)),
        onWheelPassive: ot[12] || (ot[12] = (...ye) => S(me) && S(me)(...ye)),
        onTouchstartPassive: ot[13] || (ot[13] = (...ye) => S(le) && S(le)(...ye)),
        onTouchmovePassive: ot[14] || (ot[14] = (...ye) => S(Ue) && S(Ue)(...ye))
      }, [
        S(he)?.active ? (O(), F("div", {
          key: 0,
          class: ze(["manager-compaction-overlay", { resolved: S(he).resolved }]),
          role: "status",
          "aria-live": "polite"
        }, [g("strong", null, K(S(he).status), 1), g("small", null, [Bs(K(S(he).currentTokens) + " / " + K(S(he).triggerTokens || "...") + " ", 1), S(he).yieldTokens ? (O(), F("span", Xre, " → " + K(S(he).yieldTokens), 1)) : ge("", !0)])], 2)) : ge("", !0),
        S(Ne).hiddenBefore ? (O(), F("div", {
          key: 1,
          class: "chat-history-gate manager-history-gate",
          "data-manager-anchor-key": `gate:${S(Ne).hiddenBefore}`,
          role: "button",
          tabindex: "0",
          onClick: ot[1] || (ot[1] = (ye) => S(It)(!0)),
          onKeydown: [ot[2] || (ot[2] = Vs(sn((ye) => S(It)(!0), ["prevent"]), ["enter"])), ot[3] || (ot[3] = Vs(sn((ye) => S(It)(!0), ["prevent"]), ["space"]))]
        }, " 展开较早记录 " + K(S(Ne).hiddenBefore) + " 条 ", 41, Qre)) : ge("", !0),
        (O(!0), F(Je, null, lt(S(jt), (ye) => (O(), F(Je, { key: ye.key }, [ye.kind === "message" ? (O(), F("article", {
          key: 0,
          "data-manager-anchor-key": ye.anchorKey,
          class: ze(["manager-card manager-message", ye.message.role === "user" ? "manager-message-user" : "manager-message-assistant"])
        }, [
          g("div", jre, [g("strong", null, K(ye.message.role === "user" ? S(T)("user") : "助手"), 1), g("small", null, K(S(d)(ye.message.createdAt)), 1)]),
          S(H)(ye.message) ? (O(), F("div", eoe, [Ln(g("textarea", {
            "onUpdate:modelValue": ot[4] || (ot[4] = (dt) => /* @__PURE__ */ pn(M) ? M.value = dt : null),
            class: "message-edit-box",
            rows: "6",
            "data-manager-message-editor": `manager:${ye.message.sessionId}:${ye.message.order}`,
            onInput: ot[5] || (ot[5] = (...dt) => S(X) && S(X)(...dt)),
            onKeydown: (dt) => S(fe)(dt, ye.message)
          }, null, 40, toe), [[Ro, S(M)]]), g("div", noe, [
            g("button", {
              type: "button",
              disabled: !S(W)(ye.message),
              onClick: (dt) => S(At)(ye.message)
            }, K(ye.message.role === "user" ? "保存" : "保存修改"), 9, roe),
            ye.message.role === "user" ? (O(), F("button", {
              key: 0,
              type: "button",
              disabled: !S(W)(ye.message),
              onClick: (dt) => S(At)(ye.message, { rerun: !0 })
            }, " 保存并重发 ", 8, ooe)) : ge("", !0),
            g("button", {
              type: "button",
              onClick: ot[6] || (ot[6] = (...dt) => S(u) && S(u)(...dt))
            }, " 取消 ")
          ])])) : ge("", !0),
          S(H)(ye.message) ? ge("", !0) : (O(), F("div", {
            key: `history-message:${ye.key}:${nn(ye.message.content)}`,
            class: "xb-tavern-markdown",
            "data-markdown-signature": nn(ye.message.content),
            innerHTML: S(y)(ye.message.content)
          }, null, 8, soe)),
          S(H)(ye.message) ? ge("", !0) : (O(), F("div", aoe, [
            g("button", {
              type: "button",
              class: ze(S(Y)(ye.message, "copy")),
              title: "复制",
              "aria-label": "复制",
              onClick: (dt) => S(w)(ye.message)
            }, " ⧉ ", 10, ioe),
            g("button", {
              type: "button",
              disabled: !S($)(ye.message),
              class: ze(S(Y)(ye.message, "edit")),
              title: "编辑",
              "aria-label": "编辑",
              onClick: (dt) => S(Ze)(ye.message)
            }, " ✎ ", 10, loe),
            g("button", {
              type: "button",
              disabled: !S(p)(ye.message),
              class: ze(S(Y)(ye.message, "rerun")),
              title: ye.message.role === "user" ? "从这里重问助手" : "重新生成这条助手回复",
              "aria-label": ye.message.role === "user" ? "从这里重问助手" : "重新生成这条助手回复",
              onClick: (dt) => S(Ht)(ye.message)
            }, " ↻ ", 10, uoe),
            g("button", {
              type: "button",
              disabled: S(ke),
              class: ze(S(Y)(ye.message, "delete")),
              title: "删除",
              "aria-label": "删除",
              onClick: (dt) => S(x)(ye.message)
            }, " ⌫ ", 10, coe)
          ]))
        ], 10, Zre)) : (O(), F("details", {
          key: 1,
          class: "manager-tool-turn manager-tool-turn-history",
          "data-manager-anchor-key": ye.anchorKey,
          open: S(wt).isOpen(Ft("tool-turn", ye.key)),
          onToggle: (dt) => S(wt).setOpenFromEvent(Ft("tool-turn", ye.key), dt)
        }, [g("summary", null, [
          g("span", null, K(ye.rounds.length > 1 ? `工具轮 ${ye.rounds.length} 轮` : "工具轮"), 1),
          g("small", null, K(S(qe)(ye)), 1),
          g("em", null, K(S(Ke)(ye)), 1)
        ]), S(wt).isOpen(Ft("tool-turn", ye.key)) ? (O(), F("div", foe, [(O(!0), F(Je, null, lt(ye.rounds, (dt, dn) => (O(), F("div", {
          key: `${ye.key}-round-${dt.assistantMessage.order}`,
          class: "manager-tool-round"
        }, [
          g("div", hoe, [g("strong", null, "第 " + K(Number(dn) + 1) + " 轮 · " + K(dt.calls.length) + " 个工具", 1), g("span", null, K(S(d)(dt.assistantMessage.createdAt)), 1)]),
          S(C)(dt.assistantMessage.thoughts).length ? (O(), F("details", {
            key: 0,
            class: "manager-tool-thoughts",
            open: S(wt).isOpen(Ft("tool-thoughts", ye.key, dn, dt.assistantMessage.order)),
            onToggle: (Pt) => S(wt).setOpenFromEvent(Ft("tool-thoughts", ye.key, dn, dt.assistantMessage.order), Pt)
          }, [g("summary", null, K(S(A)(S(C)(dt.assistantMessage.thoughts), !1)), 1), S(wt).isOpen(Ft("tool-thoughts", ye.key, dn, dt.assistantMessage.order)) ? (O(!0), F(Je, { key: 0 }, lt(S(C)(dt.assistantMessage.thoughts), (Pt, j) => (O(), F("div", {
            key: `${ye.key}-round-${dn}-thought-${j}`,
            class: "chat-thought-block"
          }, [g("strong", null, K(Pt.label), 1), g("pre", null, K(Pt.text), 1)]))), 128)) : ge("", !0)], 40, poe)) : ge("", !0),
          dt.assistantMessage.content ? (O(), F("div", {
            key: `history-tool-preface:${ye.key}:${dt.assistantMessage.order}:${nn(dt.assistantMessage.content)}`,
            class: "manager-tool-preface xb-tavern-markdown",
            "data-markdown-signature": nn(dt.assistantMessage.content),
            innerHTML: S(y)(dt.assistantMessage.content)
          }, null, 8, moe)) : ge("", !0),
          (O(!0), F(Je, null, lt(dt.calls, (Pt) => (O(), F("div", {
            key: Pt.id,
            class: ze(["manager-tool-item", Pt.ok ? "is-resolved" : "is-error"])
          }, [
            g("div", goe, [g("span", null, K(Pt.name), 1), g("em", null, K(Pt.toolMessage ? Pt.ok ? "已返回" : "失败" : "等待返回"), 1)]),
            Pt.argumentsText ? (O(), F("small", voe, K(Pt.argumentsText), 1)) : ge("", !0),
            g("p", null, K(Pt.resultText), 1)
          ], 2))), 128))
        ]))), 128))])) : ge("", !0)], 40, doe))], 64))), 128)),
        (O(!0), F(Je, null, lt(S(Le), (ye) => (O(), F(Je, { key: `live:${ye.key}` }, [ye.kind === "message" ? (O(), F("article", {
          key: 0,
          "data-manager-anchor-key": `live:${ye.anchorKey}`,
          class: ze(["manager-card manager-message manager-message-live", ye.message.role === "user" ? "manager-message-user" : "manager-message-assistant"])
        }, [g("div", _oe, [g("strong", null, K(ye.message.role === "user" ? S(T)("user") : "助手"), 1), ot[20] || (ot[20] = g("small", null, "正在处理", -1))]), (O(), F("div", {
          key: `live-message:${ye.key}:${nn(ye.message.content)}`,
          class: "xb-tavern-markdown",
          "data-markdown-signature": nn(ye.message.content),
          innerHTML: S(y)(ye.message.content)
        }, null, 8, boe))], 10, yoe)) : (O(), F("details", {
          key: 1,
          class: "manager-tool-turn manager-tool-turn-live",
          "data-manager-anchor-key": `live:${ye.anchorKey}`,
          open: S(wt).isOpen(Ft("live-tool-turn", ye.key), !0),
          onToggle: (dt) => S(wt).setOpenFromEvent(Ft("live-tool-turn", ye.key), dt)
        }, [g("summary", null, [
          g("span", null, K(ye.rounds.length > 1 ? `工具轮 ${ye.rounds.length} 轮` : "工具轮"), 1),
          g("small", null, K(S(qe)(ye)), 1),
          g("em", null, K(S(Ke)(ye)), 1)
        ]), S(wt).isOpen(Ft("live-tool-turn", ye.key), !0) ? (O(), F("div", Soe, [(O(!0), F(Je, null, lt(ye.rounds, (dt, dn) => (O(), F("div", {
          key: `live:${ye.key}-round-${dt.assistantMessage.order}`,
          class: "manager-tool-round"
        }, [
          g("div", Toe, [g("strong", null, "第 " + K(Number(dn) + 1) + " 轮 · " + K(dt.calls.length) + " 个工具", 1), ot[21] || (ot[21] = g("span", null, "正在处理", -1))]),
          S(C)(dt.assistantMessage.thoughts).length ? (O(), F("details", {
            key: 0,
            class: "manager-tool-thoughts",
            open: S(wt).isOpen(Ft("live-tool-thoughts", ye.key, dn, dt.assistantMessage.order), !0),
            onToggle: (Pt) => S(wt).setOpenFromEvent(Ft("live-tool-thoughts", ye.key, dn, dt.assistantMessage.order), Pt)
          }, [g("summary", null, K(S(A)(S(C)(dt.assistantMessage.thoughts), !0)), 1), S(wt).isOpen(Ft("live-tool-thoughts", ye.key, dn, dt.assistantMessage.order), !0) ? (O(!0), F(Je, { key: 0 }, lt(S(C)(dt.assistantMessage.thoughts), (Pt, j) => (O(), F("div", {
            key: `live:${ye.key}-round-${dn}-thought-${j}`,
            class: "chat-thought-block"
          }, [g("strong", null, K(Pt.label), 1), g("pre", null, K(Pt.text), 1)]))), 128)) : ge("", !0)], 40, Eoe)) : ge("", !0),
          dt.assistantMessage.content ? (O(), F("div", {
            key: `live-tool-preface:${ye.key}:${dt.assistantMessage.order}:${nn(dt.assistantMessage.content)}`,
            class: "manager-tool-preface xb-tavern-markdown",
            "data-markdown-signature": nn(dt.assistantMessage.content),
            innerHTML: S(y)(dt.assistantMessage.content)
          }, null, 8, Aoe)) : ge("", !0),
          (O(!0), F(Je, null, lt(dt.calls, (Pt) => (O(), F("div", {
            key: `live:${Pt.id}`,
            class: ze(["manager-tool-item", Pt.ok ? "is-resolved" : "is-error"])
          }, [
            g("div", Coe, [g("span", null, K(Pt.name), 1), g("em", null, K(Pt.toolMessage ? Pt.ok ? "已返回" : "失败" : "等待返回"), 1)]),
            Pt.argumentsText ? (O(), F("small", koe, K(Pt.argumentsText), 1)) : ge("", !0),
            g("p", null, K(Pt.resultText), 1)
          ], 2))), 128))
        ]))), 128))])) : ge("", !0)], 40, woe))], 64))), 128)),
        S(ke) && !S(Le).length ? (O(), F("article", Ioe, [...ot[22] || (ot[22] = [g("div", { class: "manager-run-title" }, [g("strong", null, "助手"), g("small", null, "正在处理")], -1), g("p", null, "正在思考...", -1)])])) : ge("", !0),
        S(Ee).length || S(Oe).length ? (O(), F("details", {
          key: 3,
          class: "manager-work-drawer",
          "data-manager-anchor-key": "meta:work",
          open: S(wt).isOpen(Ft("work-drawer")),
          onToggle: ot[10] || (ot[10] = (ye) => S(wt).setOpenFromEvent(Ft("work-drawer"), ye))
        }, [g("summary", null, [ot[23] || (ot[23] = g("strong", null, "工作记录", -1)), S(E) ? (O(), F("span", Moe, K(S(Pe)(S(E)) ? "重试中" : S(V)(S(E).status)) + " · " + K(S(z)(S(E))), 1)) : (O(), F("span", Poe, K(S(I).length) + "/" + K(S(Ee).length) + " 份档案", 1))]), S(wt).isOpen(Ft("work-drawer")) ? (O(), F(Je, { key: 0 }, [S(E) ? (O(), F("article", {
          key: 0,
          "data-manager-anchor-key": `run:${S(E).id}`,
          class: ze(["manager-card manager-message manager-message-run manager-work-current", [
            `is-${S(E).status}`,
            `tone-${S(L)(S(E))}`,
            { "is-retrying": S(Pe)(S(E)) }
          ]]),
          "aria-busy": S(Pe)(S(E)) ? "true" : "false"
        }, [
          g("div", Noe, [ot[24] || (ot[24] = g("strong", null, "本次后台工作", -1)), g("small", null, K(S(Pe)(S(E)) ? "重试中" : S(V)(S(E).status)), 1)]),
          g("p", $oe, K(S(z)(S(E))), 1),
          g("small", null, K(S(oe)(S(E))), 1),
          g("small", Doe, K(S(Pe)(S(E)) ? "重试已开始 · 正在等 API/工具返回" : S(N)(S(E))), 1),
          g("div", Loe, [
            g("p", null, K(S(G)(S(E))), 1),
            g("p", null, K(S(J)(S(E))), 1),
            g("p", null, K(S(B)(S(E))), 1)
          ]),
          S(E).outputText ? (O(), F("p", Ooe, " 结果：" + K(S(i)(S(E).outputText, 180)), 1)) : ge("", !0),
          S(q)(S(E)) ? (O(), F("p", Foe, K(S(q)(S(E))), 1)) : ge("", !0),
          S(Me)(S(E).toolTrace).length ? (O(), F("details", {
            key: 2,
            class: "manager-work-debug",
            open: S(wt).isOpen(Ft("work-debug", S(E).id)),
            onToggle: ot[7] || (ot[7] = (ye) => S(wt).setOpenFromEvent(Ft("work-debug", S(E).id), ye))
          }, [g("summary", null, K(S(zt)(S(E).toolTrace)), 1), S(wt).isOpen(Ft("work-debug", S(E).id)) ? (O(), F("div", Uoe, [(O(!0), F(Je, null, lt(S(Me)(S(E).toolTrace), (ye) => (O(), F("div", {
            key: ye.id,
            class: ze(["manager-tool-item", S(Se)(ye)])
          }, [
            g("div", qoe, [g("span", null, K(ye.name), 1), g("em", null, [Bs(K(S(ae)(ye)), 1), ye.elapsedLabel ? (O(), F(Je, { key: 0 }, [Bs(" · " + K(ye.elapsedLabel), 1)], 64)) : ge("", !0)])]),
            ye.thoughts.length ? (O(), F("details", {
              key: 0,
              class: "manager-tool-thoughts",
              open: S(wt).isOpen(Ft("work-tool-thoughts", S(E).id, ye.id)),
              onToggle: (dt) => S(wt).setOpenFromEvent(Ft("work-tool-thoughts", S(E).id, ye.id), dt)
            }, [g("summary", null, K(S(A)(ye.thoughts, !1)), 1), S(wt).isOpen(Ft("work-tool-thoughts", S(E).id, ye.id)) ? (O(!0), F(Je, { key: 0 }, lt(ye.thoughts, (dt, dn) => (O(), F("div", {
              key: `${ye.id}-stored-thought-${dn}`,
              class: "chat-thought-block"
            }, [g("strong", null, K(dt.label), 1), g("pre", null, K(dt.text), 1)]))), 128)) : ge("", !0)], 40, Hoe)) : ge("", !0),
            ye.preface ? (O(), F("div", {
              key: `work-tool-preface:${S(E).id}:${ye.id}:${nn(ye.preface)}`,
              class: "manager-tool-preface xb-tavern-markdown",
              "data-markdown-signature": nn(ye.preface),
              innerHTML: S(y)(ye.preface)
            }, null, 8, zoe)) : ge("", !0),
            ye.args ? (O(), F("small", Woe, K(ye.args), 1)) : ge("", !0),
            ye.summary ? (O(), F("p", Goe, K(ye.summary), 1)) : ge("", !0),
            ye.path ? (O(), F("p", Voe, K(ye.path), 1)) : ge("", !0)
          ], 2))), 128))])) : ge("", !0)], 40, Boe)) : ge("", !0),
          S(E).status === "failed" ? (O(), F("button", {
            key: 3,
            type: "button",
            class: "manager-run-retry-button",
            disabled: S(te) || S(Pe)(S(E)),
            onClick: ot[8] || (ot[8] = (ye) => S(je)(S(E)))
          }, K(S(Pe)(S(E)) ? "重试中" : "重试"), 9, Koe)) : ge("", !0)
        ], 10, Roe)) : (O(), F("article", Joe, [
          g("div", Yoe, [ot[25] || (ot[25] = g("strong", null, "记忆档案", -1)), g("small", null, K(S(I).length) + "/" + K(S(Ee).length), 1)]),
          g("p", null, K(S(Ye)), 1),
          S(yt) ? (O(), F("p", Xoe, " 当前打开：" + K(S(Be)(S(yt))), 1)) : ge("", !0)
        ])), S(R).length || S(We) ? (O(), F("details", {
          key: 2,
          class: "manager-work-history",
          open: S(wt).isOpen(Ft("work-history")),
          onToggle: ot[9] || (ot[9] = (ye) => S(wt).setOpenFromEvent(Ft("work-history"), ye))
        }, [g("summary", null, [ot[26] || (ot[26] = g("strong", null, "历史记录", -1)), g("span", null, K(S(R).length + S(We)) + " 条", 1)]), S(wt).isOpen(Ft("work-history")) ? (O(), F(Je, { key: 0 }, [(O(!0), F(Je, null, lt(S(R), (ye) => (O(), F("div", {
          key: ye.id,
          class: ze(["manager-history-row", [`tone-${S(L)(ye)}`]])
        }, [g("div", null, [g("strong", null, K(S(V)(ye.status)), 1), g("small", null, K(S(z)(ye)), 1)]), g("span", null, K(S(zt)(ye.toolTrace) || S(N)(ye)), 1)], 2))), 128)), S(We) ? (O(), F("p", Zoe, " 更早 " + K(S(We)) + " 条已收起。 ", 1)) : ge("", !0)], 64)) : ge("", !0)], 40, Qoe)) : ge("", !0)], 64)) : ge("", !0)], 40, xoe)) : ge("", !0),
        S(jt).length ? ge("", !0) : (O(), F("p", joe, " 还没有和助手对话。 "))
      ], 32), En(u5, {
        "extra-class": "manager-scroll-helpers",
        active: S(P),
        "show-top": S(xe),
        "show-bottom": S(et),
        onTop: S(Ut),
        onBottom: ot[15] || (ot[15] = (ye) => S(Mt)(!0, {
          collapseWindow: !0,
          revealHelpers: !0
        }))
      }, null, 8, [
        "active",
        "show-top",
        "show-bottom",
        "onTop"
      ])]),
      g("form", {
        class: "manager-compose chat-compose",
        onSubmit: ot[19] || (ot[19] = sn((...ye) => S(Ce) && S(Ce)(...ye), ["prevent"]))
      }, [
        S(Ie) ? (O(), F("div", ese, K(S(Ie)), 1)) : ge("", !0),
        Ln(g("textarea", {
          ref: Gn,
          "onUpdate:modelValue": ot[16] || (ot[16] = (ye) => /* @__PURE__ */ pn(Te) ? Te.value = ye : null),
          rows: "1",
          placeholder: "和助手说一句话...",
          disabled: S(ke),
          onInput: ot[17] || (ot[17] = (...ye) => S(f) && S(f)(...ye)),
          onKeydown: ot[18] || (ot[18] = (...ye) => S(de) && S(de)(...ye))
        }, null, 40, tse), [[Ro, S(Te)]]),
        g("button", {
          type: "submit",
          class: "primary-action",
          disabled: !S(b)
        }, K(S(se) ? "正在停止" : S(ke) ? "停止" : "发送"), 9, nse)
      ], 32)
    ], 8, Vre));
  }
}), ose = rse, zh = 128, BC = 54, UC = 36, sse = 78, ase = 72;
function Hy(e) {
  return [...e].sort((t, n) => String(t.parent || "").localeCompare(String(n.parent || "")) || String(t.name || "").localeCompare(String(n.name || "")) || String(t.key || "").localeCompare(String(n.key || "")));
}
function ise(e) {
  const t = /* @__PURE__ */ new Set();
  return e.forEach((n) => {
    const r = /* @__PURE__ */ new Set();
    let o = n;
    for (; o?.parent; ) {
      if (r.has(o.key)) {
        r.forEach((s) => t.add(s)), t.add(o.key);
        break;
      }
      r.add(o.key), o = e.get(o.parent);
    }
  }), t;
}
function lse(e) {
  const t = Array.isArray(e?.locations) ? Hy(e.locations) : [], n = new Map(t.map((A) => [A.key, A])), r = ise(n), o = [];
  r.size && o.push("atlas_parent_cycle");
  const s = /* @__PURE__ */ new Map(), a = [];
  t.forEach((A) => {
    const I = A.parent || "";
    if (I && n.has(I) && !r.has(A.key) && !r.has(I)) {
      const R = s.get(I) || [];
      R.push(A), s.set(I, R);
      return;
    }
    I && !n.has(I) && o.push(`atlas_parent_missing:${A.key}`), a.push(A);
  }), s.forEach((A, I) => s.set(I, Hy(A)));
  const i = [], l = (A) => {
    const I = s.get(A.key) || [];
    return I.length ? Math.max(zh, I.reduce((R, $, p) => R + l($) + (p > 0 ? UC : 0), 0)) : zh;
  }, u = (A, I, R) => {
    const $ = l(A), p = I + ($ - zh) / 2;
    i.push({
      key: A.key,
      x: p,
      y: R * (BC + sse),
      width: zh,
      height: BC,
      depth: R
    });
    let b = I;
    return (s.get(A.key) || []).forEach((w) => {
      const E = l(w);
      u(w, b, R + 1), b += E + UC;
    }), $;
  };
  let d = 0;
  Hy(a).forEach((A) => {
    const I = u(A, d, 0);
    d += I + ase;
  });
  const f = new Map(i.map((A) => [A.key, A])), m = (Array.isArray(e?.links) ? e.links : []).map((A) => {
    const I = f.get(A.from), R = f.get(A.to);
    if (!I || !R)
      return o.push(`atlas_link_endpoint_missing:${A.id}`), null;
    const $ = I.x + I.width / 2, p = I.y + I.height / 2, b = R.x + R.width / 2, w = R.y + R.height / 2, E = (p + w) / 2;
    return {
      id: A.id,
      from: A.from,
      to: A.to,
      path: `M ${$} ${p} C ${$} ${E}, ${b} ${E}, ${b} ${w}`
    };
  }).filter((A) => !!A);
  if (!i.length) return {
    nodes: i,
    links: m,
    viewBox: [
      0,
      0,
      640,
      360
    ],
    warnings: o
  };
  const v = Math.min(...i.map((A) => A.x)) - 48, y = Math.min(...i.map((A) => A.y)) - 48, T = Math.max(...i.map((A) => A.x + A.width)) + 48, C = Math.max(...i.map((A) => A.y + A.height)) + 48;
  return {
    nodes: i,
    links: m,
    viewBox: [
      v,
      y,
      Math.max(320, T - v),
      Math.max(220, C - y)
    ],
    warnings: o
  };
}
var use = {
  castle: {
    path: "M254.25 15.344c-132.537 0-240.188 107.62-240.188 240.156 0 132.537 107.65 240.188 240.188 240.188 132.537 0 240.156-107.65 240.156-240.188 0-132.537-107.62-240.156-240.156-240.156zm0 18.687c122.436 0 221.47 99.034 221.47 221.47 0 65.65-28.465 124.583-73.75 165.125V238.75l14-22.78h-7.595L364 101.5l-43.813 114.47h-8.156l14.595 22.78v33.875h-36.813v-88.188l14.625-22.78h-7.593l-44.406-114.47-44.375 114.47h-7.594l14.03 22.78v123.22h-37.375v-18.094l14.594-22.782h-8.19l-43.78-114.467L95.344 266.78H87.75l14.03 22.783V416.25C59.25 375.9 32.75 318.83 32.75 255.5c0-122.436 99.064-221.47 221.5-221.47zm1.094 160.532h18.687v36.344h-18.686v-36.344zm110.156 87.97h18.688v36.312H365.5V282.53zm-246.656 22.03h18.687v36.344h-18.686v-36.344zm50.875 29.407h18.686v36.342H169.72V333.97zm170.81 30.5h18.69v36.342h-18.69V364.47z",
    slug: "castle",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/castle.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/castle.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  village: {
    path: "m109.902 35.87-71.14 59.284h142.28l-71.14-59.285zm288 32-71.14 59.284h142.28l-71.14-59.285zM228.73 84.403l-108.9 90.75h217.8l-108.9-90.75zm-173.828 28.75v62h36.81l73.19-60.992v-1.008h-110zm23 14h16v18h-16v-18zm265 18v10.963l23 19.166v-16.13h16v18h-13.756l.104.087 19.098 15.914h-44.446v14h78v-39h18v39h14v-62h-110zm-194.345 48v20.08l24.095-20.08h-24.095zm28.158 0 105.1 87.582 27.087-22.574v-65.008H176.715zm74.683 14h35.735v34h-35.735v-34zm-76.714 7.74L30.37 335.153H319l-144.314-120.26zm198.046 13.51-76.857 64.047 32.043 26.704H481.63l-108.9-90.75zm-23.214 108.75.103.086 19.095 15.914h-72.248v77.467h60.435v-63.466h50v63.467h46v-93.466H349.516zm-278.614 16V476.13h126v-76.976h50v76.977h31.565V353.155H70.902zm30 30h50v50h-50v-50z",
    slug: "village",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/village.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/village.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  road: {
    path: "M168.4 29.55 24.38 477.6l15.24 4.8L183.6 34.45l-15.2-4.9zm175.2 0-15.2 4.9 144 447.95 15.2-4.8-144-448.05zM248 32l-.8 20h17.6l-.8-20h-16zm-1.4 36-.8 20h20.4l-.8-20h-18.8zm-1.5 36-1.3 32h24.4l-1.3-32h-21.8zm-1.9 48-2 48h29.6l-2-48h-25.6zm-2.8 68-2.4 60h36l-2.4-60h-31.2zm-3.3 84-2.9 72h43.6l-2.9-72h-37.8zm-4 100-3.1 76h52l-3.1-76h-45.8z",
    slug: "road",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/road.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/road.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  house: {
    path: "m55.379 25-28.4 142H172.27L256 83.271 339.729 167H485.02l-28.4-142zM256 108.727 179.729 185H41v302h158v-87c0-18.25 7.166-33.077 18.021-42.727C227.877 347.624 242 343 256 343s28.123 4.624 38.979 14.273C305.834 366.923 313 381.75 313 400v87h158V185H332.271zm0 38.544 57 57V297H199v-92.729zm0 25.456-39 39V279h78v-67.271zM71 199h98v98H71zm272 0h98v98h-98zM89 217v30h62v-30zm272 0v30h62v-30zM89 265v14h62v-14zm272 0v14h62v-14zM71 359h98v98H71v-98zm272 0h98v98h-98v-98zm-87 2c-10 0-19.877 3.376-27.021 9.727C221.834 377.077 217 386.25 217 400v87h78v-87c0-13.75-4.834-22.923-11.979-29.273C275.877 364.376 266 361 256 361zM89 377v62h62v-62zm272 0v62h62v-62z",
    slug: "family-house",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/family-house.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/family-house.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  shop: {
    path: "M256.18 21c-23.242 0-46.577 3.01-63.186 8.54-8.304 2.763-14.868 6.196-18.808 9.558-3.94 3.36-5.167 5.956-5.186 8.96L168.943 57H41v14h430V57H342.967l.033-9.033c.01-3.002-1.17-5.55-5.057-8.895-3.887-3.344-10.407-6.773-18.677-9.535C302.724 24.014 279.42 21 256.18 21zM38.277 89l-10.443 94h80.9l20.243-86.36L122.81 183h81.07l17.114-86.68-3.096 86.68h75.75l-4.634-86.518L307.694 183h81.497l-6.167-86.36L403.266 183h80.9l-10.443-94H38.277zM25.834 201l-.51 4.588C39.822 226.445 52.968 235 64 235c11.32 0 24.852-8.89 39.8-30.96l.714-3.04h-78.68zm95.687 0-.32 4.498C135.753 226.495 148.935 235 160 235c11.293 0 24.788-8.85 39.693-30.803l.63-3.197h-78.802zm95.736 0-.156 4.352C231.69 226.455 244.908 235 256 235c11.08 0 24.28-8.525 38.85-29.576l-.237-4.424h-77.357zm94.324 0 .674 3.12c15 22.192 28.503 31.044 39.773 31.013 11.03-.03 24.212-8.62 38.772-29.637l-.32-4.496h-78.9zm95.906 0 .713 3.04C423.147 226.11 436.68 235 448 235c11.032 0 24.178-8.555 38.676-29.412l-.51-4.588h-78.68zM112 223.31C97.313 242.11 81.492 253 64 253c-13.972 0-26.884-6.906-39-19.264V487h318V279h114v208h30V233.736C474.884 246.094 461.972 253 448 253c-17.49 0-33.31-10.888-47.996-29.684-14.664 18.808-30.432 29.77-47.926 29.817-17.508.048-33.352-10.87-48.092-29.807C289.303 242.116 273.486 253 256 253c-17.492 0-33.313-10.89-48-29.69-14.687 18.8-30.508 29.69-48 29.69s-33.313-10.89-48-29.69zM55 279h258v178H55V279zm18 18v142h222V297H73zm288 0v71.064l78 .573V297h-78zM88 312h128c-108.235 8-116.31 24-128 113.11V312zm273 74.066v13.998l78 .573v-14.002l-78-.57zm0 32V487h78v-68.365l-78-.57z",
    slug: "shop",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/shop.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/shop.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  "inn-sign": {
    path: "M240.03 35.938c-1.08.01-2.168.062-3.25.124-8.644.502-17.16 2.8-22.5 5.97-5.336 3.167-7.018 5.72-6.81 9.593v.25l.78 28.156 59.97-1.28-.876-31.844c-.148-3.014-1.806-5.15-7.47-7.593C255.63 37.48 249.63 36.27 243.25 36a61.354 61.354 0 0 0-3.22-.063zm224.94 57.218L33.593 102.53l1.375 62 154.655 4.064-148.156 9.72-2.907 98.81 1.406.313 8.06 1.844-.843 8.22-6.906 67.47 429.533-9.283L464.219 283l-140.376-3.656 139.22-9.156-8.877-99.407-138.875-3.624 151.032-9.937-1.375-64.064zM276.31 368.562l-59.875 1.282L220 495.78h59.844l-3.53-127.217z",
    slug: "wooden-sign",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/wooden-sign.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/wooden-sign.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  temple: {
    path: "M256 26.2 52 135h408L256 26.2zM73 153v14h366v-14H73zm16 32v206h30V185H89zm101.334 0v206h30V185h-30zm101.332 0v206h30V185h-30zM393 185v206h30V185h-30zM73 409v30h366v-30H73zm-32 48v30h430v-30H41z",
    slug: "greek-temple",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/greek-temple.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/greek-temple.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  stairs: {
    path: "M64 448v-64h64v-64h64v-64h64v-64h64v-64h64V64h64v384z",
    slug: "stairs",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/stairs.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/stairs.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  door: {
    path: "M105 41v398h302V41H105zm55 174c18.1 0 33 14.9 33 33s-14.9 33-33 33-33-14.9-33-33 14.9-33 33-33zm0 18c-8.4 0-15 6.6-15 15s6.6 15 15 15 15-6.6 15-15-6.6-15-15-15zM73 457v30h366v-30H73z",
    slug: "door",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/door.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/door.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  forest: {
    path: "M169.92 15.654c-10.512 16.697-22.392 34.058-41.688 50.473.46.447.934.89 1.405 1.336l23.55-.406-3.527 10.7a71.131 71.131 0 0 1-1.678 4.552 183.485 183.485 0 0 0 8.872 5.788l25.052 15.33-45.99-.793c-1.954 2.474-4.06 4.9-6.305 7.275 8.8 11.517 22.045 22.713 38.675 32.766l-12.404-.365-.015 5.678c8.53 6.358 17.792 12.448 27.162 18.48 18.11-11.568 31.79-24.5 39.51-37.616l-57.353.99 25.052-15.33c8.97-5.488 16.525-11.115 23.082-16.867-5.098-5.475-9-10.962-11.257-16.69l-4.04-10.246 29.742-1.823C205.14 54.41 182.44 38.31 169.92 15.654zm234.605 13.223c-6.537 12.962-13.88 29.016-23.638 44.63 10.927 18.367 24.428 35.997 46.773 52.382l20.42 14.97-52.51-1.085c6.956 12.136 19.936 26.858 34.58 40.047 19.157 17.254 40.928 32.572 53.907 39.914l11.34 6.418c.005-4.163.005-7.382.015-13.972-17.176-11.938-29.767-25.795-36.322-42.2l-4.115-10.298 43.605-2.58c-9.91-4.358-19.747-8.263-28.723-12.06-8.498-3.594-16.23-7.107-22.724-11.23-6.495-4.122-12.102-8.954-14.858-15.943l-3.93-9.962 36.077-3.178c-24.245-20.226-47.223-48.756-59.897-75.853zM84.027 34.62c-12.52 22.656-35.22 38.757-57.843 53.234l29.738 1.822-4.037 10.244c-4.78 12.125-16.815 24.123-33.182 36.873-.295 11.21-.392 17.66-.385 17.732.005.044.19 1.1.29 3.127.038.742.04 3.177.066 4.358l13.828.797-.004.55-3.906 9.778c-1.18 2.955-2.498 6.07-4.16 9.232l11.525-7.05c37.376-22.87 50.42-48.163 67.395-74.833l-.233 6.366 4.474-1.875a72.336 72.336 0 0 1-3.305-8.25l-3.528-10.7 26.582.46C107.068 69.66 94.836 51.787 84.027 34.618zM263.06 61.036c-10.808 17.168-23.04 35.04-43.314 51.86l26.582-.458-3.527 10.7c-6.657 20.188-22.772 37.89-44.874 52.91l17.916 11.573 18.273.688-4.127 10.5c-2.305 5.863-5.355 11.4-9.01 16.65l8.737-6.19c17.913-12.69 36.773-27.495 51.8-41.534 10.026-9.365 17.978-18.52 23.083-25.914l-50.537-3.715 16.5-12.773c10.37-8.027 20.973-15.94 30.924-24.178-15.49-11.246-29.53-24.024-38.423-40.12zm93.07.68c-12.76 26.805-36.227 46.04-59.107 63.506l31.39 2.306-3.194 9.726c-4.503 13.7-16.983 27.412-32.783 42.172-10.794 10.083-23.245 20.29-35.896 29.926l49.278 3.494-3.26 9.78c-11.273 33.806-46.845 56.924-75.816 75.597 13.487 8.056 27.67 15.44 39.813 22.318 68.34 18.82 147.594 6.972 200.924-16-13.19-7.554-23.844-14.547-32.748-23.006-11.805-11.216-20.072-24.96-26.377-44.183l-3.017-9.208 51.97-6.61c-11.874-8.21-25.17-18.39-37.867-29.825-20.11-18.113-38.394-38.003-43.915-58.067L372.68 123.3l27.867.577c-21.174-20.07-33.542-41.485-44.416-62.16zM108.78 121.84c-10.807 17.168-23.038 35.04-43.313 51.86l26.582-.456-3.53 10.697c-8.24 24.998-30.974 46.186-61.625 63.185l52.94 1.99-4.126 10.496c-11.05 28.11-29.76 52.974-61.038 71.585 73.885 22.566 156.574 31.568 231.582-3.572-12.908-7.055-26.9-14.653-40.33-23.56-18.07-11.983-34.525-25.736-41.71-43.713l-4.116-10.3 48.4-2.864c-11.828-7.625-23.415-15.48-33.557-23.383-16.245-12.663-29.06-24.086-34.02-36.664l-4.04-10.247 29.74-1.823c-22.62-14.476-45.318-30.576-57.84-53.23zm-40.52 67.816-49.016.844c-.168.218-.32.44-.492.656-.276 21.495-.448 33.384-.502 42.38 23.342-13.057 40.863-28.338 50.01-43.88zm105.674 12.403a189.592 189.592 0 0 0 10.845 9.124 360.036 360.036 0 0 0 11.14 8.308c5.44-5.177 10.084-10.545 13.692-16.092l-35.678-1.34zm41.658 20.428a112.599 112.599 0 0 1-5.947 6.504 619.186 619.186 0 0 0 28.38 17.815l17.645 10.427c11.34-9.42 21.135-19.423 27.215-29.972l-67.293-4.774zm279.803 10.334-68.383 8.698c4.666 11.45 10.047 19.75 17.28 26.992l-.21-9.6c17.455-.996 34.705-3.352 51.4-7.363-.065-7.174-.084-12.42-.087-18.728zm-243.96 27.852-66.394 3.928c3.903 5.187 9.085 10.326 15.097 15.314a495.837 495.837 0 0 0 19.455 3.504c10.638-7.12 21.72-14.713 31.842-22.746zm-219.744 2.64-.29 35.618c10.746-10.37 18.692-21.97 24.83-34.694l-24.54-.924zm413.947 66.534c-14.262 4.577-29.517 8.396-45.38 11.222l5.55 132.18h42.978l-3.148-143.402zm-181.83 7.002a257.23 257.23 0 0 1-8.018 4.046l-.927 114.274h52.682l-2.426-110.496c-13.946-1.552-27.788-4.123-41.313-7.824zm120.54 6.693c-15.085 1.992-30.582 3.05-46.18 2.994l-.386 139.742h52.56l-5.994-142.737zm-353.38 9.146-.924 114.04H74.31l1.975-104.398c-15.362-2.464-30.51-5.75-45.318-9.643zm174.72 6.27c-11.576 2.87-23.226 4.935-34.898 6.284l3.915 66.195h33.065l-2.084-72.48zm-113.443 5.628-2.508 132.56 72.692-2.287-7.584-128.21c-20.97 1.29-41.936.424-62.6-2.062z",
    slug: "forest",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/forest.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/forest.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  mountains: {
    path: "M256.22 18.375c-132.32 0-239.783 107.43-239.783 239.75S123.9 497.905 256.22 497.905 496 390.446 496 258.126 388.54 18.375 256.22 18.375zm0 17.875c102.773 0 189.092 69.664 214.374 164.406l-79.313-81.47-6.967-7.155-6.688 7.47-77.22 86.438a1912.536 1912.536 0 0 0-34.467-30.063l-6.563-5.625-6.125 6.156a3510.226 3510.226 0 0 0-55.438 57.094l-76.437-83.375-6.875-7.5-6.875 7.5-71.188 77.313C51.364 119.34 143.983 36.25 256.22 36.25zm102.25 147.28-3.845 35.376 21.563-32 10.75 16.688 9.968-8.47 27.188 26.814L417 187.344l19.5 5.062 39.188 40.25.843-.812a224.379 224.379 0 0 1 1.564 26.28c0 37.033-9.06 71.917-25.063 102.595-46.25-53.48-92.512-100.116-138.75-142.283l11-12.312 33.19-22.594zm-220.16 22.75 26.438 18.782 20.22 22.032c-39.47 42.024-78.63 85.836-115.94 130.344-21.98-34.443-34.718-75.38-34.718-119.313v-.78l16.25-17.658L87.81 219.5l-17.187 54.063 41.813-51.22 27.312 32.72-1.438-48.782zm141.375 61.657 53.157 60.938-7.688-54.563L386.312 315a1631.644 1631.644 0 0 1 56.75 62.78l.188-.186C403.853 439.216 334.868 480.03 256.22 480.03c-71.76 0-135.483-33.992-176.033-86.75 19.135-22.91 38.775-45.645 58.72-68.06l56.155-33.814-29.312 76.75 61.53-73.375 6.25 32.19 19.532-36.783 47.844 69.5-21.22-91.75z",
    slug: "mountains",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/mountains.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/mountains.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  cave: {
    path: "M346.951 24.582 299.193 72.34l-101.136-7.024-40.97 80.737 68.688 25.35 37.153-19.936 8.511 15.861-44.293 23.768-79.7-29.416-70.19 55.341 35.117 58.995-.375.2 13.014 21.585 29.134 2.361 55.06-35.123 9.679 15.176-60.16 38.377-44.364-3.596-18.23-30.234-56.8 30.586 33.712 61.804-33.713 40.735L18 444.177V494h170.62l-5.6-45.592a260.658 260.658 0 0 1-5.147-4.512c-4.186-3.761-5.89-5.444-8.027-7.484l-73.13 21.797-21.339-20.484 12.467-12.985 13.777 13.225 73.068-21.78 3.784 3.667s4.24 4.09 9.216 8.636l37.797-37.248 8.133 79.54 6.3-93.444 10.364 28.387 6.281-45.112 3.14-3.091-.29-.233 22.486-27.974.465-.907.188.096 11.453-14.248 14.03 11.277-9.122 11.348 67.803 34.715 27.008-9.489 22.478 17.71 22.924-12.036 8.367 15.938-33.262 17.46-23.875-18.81-24.964 8.772-9.584-4.907 39.04 87.842L383.923 494H494v-28.512L462.713 478.2l-6.776-16.678L494 446.06V211.176l-23.438-26.463-21.654-67.371-33.547 32.666-107.77-13.873-28.019-29.096 12.967-12.486 23.629 24.539 92.867 11.953 31.442-30.615-52.79-61.801zm27.53 177.74 34.177 41.428 28.863-6.56-4.136-13.59 17.22-5.243 9.77 32.098-58.543 13.307-31.377-38.033-33.086 19.853-9.262-15.436z",
    slug: "cave-entrance",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/cave-entrance.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/cave-entrance.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  waterfall: {
    path: "M432.3 28.66c-13.4 0-26.6 6.43-40.5 19.98-4 3.94-7.8 8.06-11.6 12.12 3.7 5.59 6.1 12.64 7.7 18.46 5.6-6.15 11.1-12.41 16.5-17.76 10.1-9.98 19.1-15.59 26.9-14.66 16.1 1.9 23.7 6.55 29.6 12.81 5.9 6.26 10.1 15.12 16.4 25.18l15.2-9.58c-5.5-8.82-10-18.95-18.5-28.01-14.6-12.13-28.3-18.55-41.7-18.54zM113.7 45.63c-53.35.23-77.7 17.56-95.66 35.88l12.86 12.6c17.42-17.77 35.03-31.75 90-30.32l34.5 32.52h15c-.2-8.94-7.8-17.4-12.6-22.5-14.9-16.14-24.1-28.22-44.1-28.18zm146.7 9.79c-10.1 0-20.2.21-29.6.57 3.8 8.38 5.6 17.38 6.6 24.88 1 11.64-15.6 12.34-17.8 2.42-1.2-8.61-4.6-19.79-8.9-26.23-15.2 1-28.9 2.35-40.3 3.73 7.3 8.95 18 21.8 18 31.02.5 89.69-2.8 180.09-9.7 270.89 1.6-1.7 3.2-3.4 4.9-4.9 8.4-7.6 18.4-13.7 30-13h.1c2.6.2 5.1.7 7.5 1.4.2-16.6.6-30.9.1-44.4 5.3 5.6 10.7 10 18-.6.7 17.6-.1 35.5-.3 56.9 1.1 1.1 2 2.2 3 3.3 7.4 8.6 13.4 19.2 18.4 29.6 1.8 3.8 3.4 7.5 4.9 11.1 1.3-56.5.5-112.9-2.2-169.4 8.6 11.3 13.3 7 18-.8 2.7 56.6 3.5 113.3 2.2 169.9 11.4-11.3 25.1-22.9 41.6-22.6 4.1.1 7.8 1 11.2 2.5l.1-23.9c7.9 8.9 13.4 6.9 18 0L354 396c3.5 3.6 6.9 7.9 9.8 11.7 3.9-2 8.9-4.4 14.9-7l-5.7-73.9c6 5.5 12 10 18-1.4l5.3 68.5c6.6-2.3 13.5-4.2 20.4-5-11.2-93.4-25.2-192-44.3-296.39h.1c-1.4-7.39-3.3-14.6-5.9-19.56-2.5-4.95-5-7.23-8.7-8.07-18.2-4.12-37.7-6.65-57.3-8.04 8.6 17.14 8.4 34.97 8.6 49.66 1.1 10.8-17.2 17-18 .2-.3-18.71.2-35.46-12.7-50.98-6-.2-12-.29-18.1-.3zm90.8 31.52C362.6 130.7 371.8 206.4 378.3 251c-.9 17.6-14.2 13.4-17.8 2.6-6.5-45-16.1-121.1-26.7-162.09 9.9 7.7 16 6.9 17.4-4.57zM224 107c3.5 55 2.4 109.1-.7 162.5.2 11-15.3 14.1-18-1 3.1-53 4.2-106.3.7-160.3 8.9 13.3 14.2 9.3 18-1.2zm51.5 22.5c3.2 27.5 4.3 42.9 3.9 59.9-6.4 9.5-12.3 7.9-18-.4.4-16-.7-30.2-3.7-57.5 7 8.6 12.8 6.6 17.8-2zm52.3 27.2c3.4 50.7 4.3 90 5.6 154.8-5 5.4-9.8 11.7-18 .4-1.3-64.8-2.2-103.7-5.6-154 7.5 9.3 13.1 6 18-1.2zm-246 180.8c-10.73-.3-16.16 18.9-13.38 29.3 3.68 13.8 34.78 24.8 34.78 24.8s-2-53.6-21.4-54.1zm47.4-.4c-.8.6-1.3 1.4-1.7 2.3-4.2 9.9 22.6 23.1 22.6 23.1s5.9-17.4.6-22.8c-4.7-4.8-16.6-7-21.5-2.6zm319.5-3.8c-.5 0-.9 0-1.3.1-11.7 2.2-13.9 23.8-8.3 34.9 0 0 18.8-9.1 19.8-18 .8-6.3-4-16.8-10.2-17zm29.8 20.5c-20.3 1.1-16.8 58.6-16.8 58.6s27.7-10.5 31.2-22.9c3.5-12.3-1.6-36.4-14.4-35.7zm-282.8 17.3c-6.5 5.9-13 14.6-18.7 23.5-11.2 17.8-18.8 36-18.8 36l-3.6 8.7c-22-11.1-36.9-16.8-57.82-17.6-13.41-.5-24.76 11.5-27.43 22.4-1.41 6.4 0 14.3 9.09 25.6 120.56 14.8 310.86 21.1 411.06.4 3.5-16.4-2.3-27.7-13-37.9-11.7-11.3-29.8-19.6-45-24.5-7.3-2.4-24.9 1.2-39.4 6.9-14.6 5.8-26.4 12.5-26.4 12.5l-7.4 4.2-4.6-7s-4.8-7.3-11.3-14.4c-6.6-7.1-15.5-12.7-17.7-12.7-6.6-.1-20.7 9.2-31.5 20.3-10.7 11-18.6 22.2-18.6 22.2l-11 15.5-5.1-18.3s-5.3-19.1-14.4-38.1c-4.5-9.6-10-19-15.7-25.7-12.7-13.4-20.3-13-32.7-2zM35.58 384c-4.27-.1-7.98 1.2-9.85 4.5-8.27 14.4 30.78 39.3 30.78 39.3s6.88-26.5-.99-35.3c-4.19-4.6-12.83-8.4-19.94-8.5z",
    slug: "waterfall",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/waterfall.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/waterfall.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  skull: {
    path: "M425.344 22.22c-9.027.085-18.7 5.826-24.344 19.405-11.143 26.803-31.93 59.156-58.563 93.47a141.233 141.233 0 0 1 27.5 30.31c35.1-26.57 68.882-46.81 98.125-56.75 44.6-15.16 12.02-69.72-35.343-35.343 26.91-27.842 11.107-51.27-7.376-51.093zm-341.22.03c-18.5.378-37.604 23.962-16.343 49.875C31.523 38.635-.802 85.48 37.095 102.813c28.085 12.844 62.54 35.66 99.062 64.343 8.125-12.5 18.207-23.61 29.78-32.937-26.782-35.743-48.44-69.835-61.78-98.47-4.515-9.69-12.22-13.66-20.03-13.5zm169.5 99.688c-67.104 0-121.31 54.21-121.31 121.312 0 44.676 24.04 83.613 59.905 104.656v56.406h18.718v-47.468a120.352 120.352 0 0 0 16.093 4.78v42.688h18.69v-40.03a123.913 123.913 0 0 0 15.749 0v40.03h18.686v-42.687a120.573 120.573 0 0 0 16.094-4.78v47.467h18.688V347.97c35.92-21.03 60-60.003 60-104.72 0-67.105-54.208-121.313-121.313-121.313zm-66.874 88.218c19.88 0 36 16.12 36 36s-16.12 36-36 36-36-16.12-36-36 16.12-36 36-36zm133.563 0c19.878 0 36 16.12 36 36s-16.122 36-36 36c-19.88 0-36-16.12-36-36s16.12-36 36-36zm-66.72 52.344 29.938 48.188h-59.874l29.938-48.188zm-107.28 70.563c-40.263 32.472-78.546 58.41-109.22 72.437-37.896 17.334-5.57 64.146 30.688 30.656-30.237 36.854 21.167 69.05 36.376 36.406 15.072-32.352 40.727-71.7 72.438-112.5a140.972 140.972 0 0 1-30.28-27zm213.156 1.718a141.025 141.025 0 0 1-27.908 24.69c31.846 39.39 56.82 76.862 69.438 107.217 17.203 41.383 71.774 9.722 31.72-31.718 47.363 34.376 79.94-20.185 35.342-35.345-32.146-10.926-69.758-34.3-108.593-64.844z",
    slug: "skull-crossed-bones",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/skull-crossed-bones.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/skull-crossed-bones.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  trap: {
    path: "m106 113.773-32.963 74.375a99.166 99.166 0 0 0-3.12.704c-5.293 1.296-9.95 2.918-14.044 4.79l-8.266-53.435-25.037 87.277a108.288 108.288 0 0 0-3.338 11.635l-.26.905.07-.04c-3.632 16.665-3.56 35.726 3.597 55.818 3.306 14.022 15.515 30.355 40.24 48.135 29.193 20.992 75.05 42.954 138.495 63.86a39.272 39.272 0 0 1-.393-5.486c0-12.21 5.637-23.185 14.432-30.447l-4.07-42.73-31.54 37.69a620.827 620.827 0 0 1-27.896-11.3l-2.95-78.177-33.57 60.615c-9.068-4.85-17.496-9.773-25.294-14.75l-4.627-90.04-28.932 65.057c-7.485-6.607-13.957-13.243-19.45-19.86-4.244-20.016-.412-38.063 6.145-52.42l4.483-2.602c15.852-5.496 35.514-7.645 58.504-6.182 32.732 2.084 72.51 11.748 118.152 30.803.098-13.092 7.704-24.51 18.692-30.142l-5.597-52.59-30.14 42.78c-9.68-3.6-19.025-6.73-28.012-9.41l-4.26-68.73-32.567 59.774c-11.784-2.163-22.712-3.436-32.716-3.91l-3.77-71.97zm323.08 29.936-15.973 70.28c-9.928-1.244-20.884-1.876-32.837-1.777l-19.58-66.443-18.075 68.964c-9.342 1.12-19.127 2.635-29.316 4.55l-19.015-44.84-16.422 45.742c8.9 6.183 14.768 16.47 14.768 28.04 0 2.407-.257 4.758-.74 7.03 47.224-10.57 87.28-13.166 119.37-9.7 22.9 2.47 41.908 7.938 56.592 16.05l3.978 3.332c4.016 15.265 4.72 33.704-2.873 52.707-6.54 5.582-14.047 11.016-22.547 16.25l-17.43-69.034-19.89 87.94c-8.51 3.565-17.626 6.972-27.356 10.198l-19.724-61.576-19.274 72.674a622.45 622.45 0 0 1-29.326 6.37l-22.605-45.43-14.87 49.995a39.091 39.091 0 0 1 4.02 17.283 39.03 39.03 0 0 1-3.476 16.107c70.416-9.85 122.176-24.18 155.893-40.565 27.394-13.31 42.205-27.326 47.852-40.582 10.472-18.58 13.79-37.348 13.048-54.388l.063.053-.102-.942a107.887 107.887 0 0 0-1.308-12.035l-9.81-90.26-17.243 51.245c-3.714-2.54-8.03-4.93-13.023-7.11-.96-.417-1.95-.822-2.954-1.222L429.08 143.71zm-170.584 89.07c-8.642 0-15.443 6.802-15.443 15.445 0 3.53 1.15 6.74 3.084 9.318a161.247 161.247 0 0 1 23.101 1.844c2.91-2.793 4.705-6.733 4.705-11.162 0-8.64-6.806-15.446-15.447-15.446zm-12.652 43.468c-1.02-.003-2.032.005-3.033.025-12.016.244-22.59 2.134-30.23 4.98-5.094 1.9-8.82 4.23-10.85 6.22-2.03 1.99-2.375 3.155-2.375 4.37 0 2.426 3.81 8.437 14.258 13.844 10.448 5.408 25.905 9.714 42.992 10.954 17.088 1.24 32.486-.854 42.674-4.65 5.093-1.898 8.82-4.23 10.85-6.22 2.03-1.987 2.374-3.154 2.374-4.368 0-2.43-3.81-8.44-14.258-13.847-10.447-5.408-25.904-9.712-42.992-10.95a134.712 134.712 0 0 0-9.41-.357zm-5.688 57.215-2.96 29.51c1.08-.09 2.17-.15 3.273-.15 5.382 0 10.524 1.1 15.214 3.077l3.05-30.406a159.916 159.916 0 0 1-18.578-2.032zm.313 48.05c-11.6 0-20.798 9.2-20.798 20.8 0 11.595 9.2 20.796 20.797 20.796 11.594 0 20.798-9.203 20.798-20.798 0-11.595-9.202-20.798-20.8-20.798z",
    slug: "wolf-trap",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/wolf-trap.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/wolf-trap.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  fire: {
    path: "M282.344 15.5C205.347 26.1 179.64 117.517 196.47 178.188c21.46 77.357-72.35 84.35-51.876-38.5-27.153 44.283-34.75 83.65-29.28 116.843.016.096.043.188.06.283 6.086 32.673 23.263 58.04 46.595 76.562 14.994 11.903 32.572 20.854 51.25 26.688-59.82-145.108 153.65-144.51 89.593 3.875 20.82-4.818 39.963-13.81 55.312-27 20.46-17.582 34.67-42.43 37.688-76.313 1.43-24.45-2.34-53.204-12.875-86.563-26.74 103.853-85.32 79.05-31.344-53.03-95.86 83.26-108.066-17.42-69.25-105.532zm130.53 258.063c-1.06 6.353-2.47 12.473-4.25 18.312 14.737 5.447 26.477 11.677 34.376 17.938 9.02 7.148 12.5 13.61 12.5 19.28 0 5.673-3.48 12.164-12.5 19.313-9.02 7.15-23.072 14.23-40.844 20.188-35.544 11.915-85.786 19.53-141.25 19.53s-105.674-7.615-141.22-19.53c-17.77-5.958-31.822-13.04-40.842-20.188-9.02-7.15-12.5-13.64-12.5-19.312 0-5.672 3.48-12.133 12.5-19.28 6.886-5.46 16.706-10.874 28.875-15.783a144.01 144.01 0 0 1-6.69-17.467c-13.486 5.386-24.878 11.545-33.81 18.625-10.75 8.52-18.33 18.99-19.407 30.968h-.157v9.344c0 44.85 24.787 85.116 63.47 113.625 38.68 28.51 91.436 45.78 149.5 45.78 58.062 0 110.817-17.27 149.5-45.78 38.68-28.51 63.468-68.774 63.468-113.625h-.063c.416-2.088.658-4.224.658-6.406 0-13.205-7.945-24.7-19.563-33.906-10.634-8.43-24.763-15.56-41.75-21.625z",
    slug: "fire-bowl",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/fire-bowl.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/fire-bowl.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  chest: {
    path: "M58.553 89 42.27 211.8H79V89H58.553zM97 89v122.8h38V89H97zm56 0v122.8h56.666v-17.6h92.668v17.6H359V89H153zm224 0v122.8h38V89h-38zm56 0v122.8h36.73L453.446 89H433zM227.666 212.2v105.2h56.668V212.2h-56.668zM256 223.794a18.667 16.103 0 0 1 18.666 16.1 18.667 16.103 0 0 1-9.666 14.09v37.214h-18V254a18.667 16.103 0 0 1-9.666-14.106 18.667 16.103 0 0 1 18.666-16.1zM41 229.8v127.915l19.334 18.23V229.8H41zm37.334 0v158h355.332v-158H302.334v105.6h-92.668V229.8H78.334zm373.332 0v146.145L471 357.715V229.8h-19.334zM41 382.456V423h43.002L41 382.455zm430 0L427.998 423H471v-40.545zM92 405.8l18.24 17.2h291.52L420 405.8H92z",
    slug: "chest",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/chest.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/chest.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  table: {
    path: "M253.8 30.44c-9.5 1.2-18.1 12.3-18.1 26.8 0 8 2.7 15 6.7 19.9l4.9 5.8-7.5 1.4c-5.2 1-9 3.8-12.3 8.3-3.4 4.6-5.9 10.76-7.6 18.06-3.2 13.1-3.9 29.1-4 43.3h17.5l1.6 21.9c6.8-.3 13.8-.5 20.9-.5 7.5 0 14.8.2 21.9.6l1.5-22h16.6c0-14.3-.2-30.6-3.1-43.9-1.6-7.2-4-13.46-7.3-17.96s-7.4-7.2-13.3-8.1l-7.6-1.2 4.8-6c3.9-4.8 6.5-11.8 6.5-19.6 0-15.4-9.6-26.8-20.1-26.8zM61.9 174.4c-9.5 1.2-18.1 12.3-18.1 26.8 0 8 2.7 15 6.7 19.9l4.9 5.8-7.5 1.4c-5.2 1-9 3.8-12.3 8.3-3.4 4.6-5.9 10.8-7.6 18.1-3.2 13.1-3.9 29.1-4 43.3h17.5l4.7 64.3c12.2 2.8 25.2 2.6 36.94 0l4.3-64.3H104c0-14.3-.2-30.6-3.1-43.9-1.57-7.2-3.97-13.5-7.27-18-3.29-4.5-7.39-7.2-13.29-8.1l-7.64-1.2 4.8-6c3.94-4.8 6.54-11.8 6.54-19.6 0-15.4-9.64-26.8-20.14-26.8zm384 0c-9.5 1.2-18.1 12.3-18.1 26.8 0 8 2.7 15 6.7 19.9l4.9 5.8-7.5 1.4c-5.2 1-9 3.8-12.3 8.3-3.4 4.6-5.9 10.8-7.6 18.1-3.2 13.1-3.9 29.1-4 43.3h17.5l4.7 64.3c12.2 2.8 25.2 2.6 36.9 0l4.3-64.3H488c0-14.3-.2-30.6-3.1-43.9-1.6-7.2-4-13.5-7.3-18s-7.4-7.2-13.3-8.1l-7.6-1.2 4.8-6c3.9-4.8 6.5-11.8 6.5-19.6 0-15.4-9.6-26.8-20.1-26.8zm-190 19c-36.7 0-69.9 5.3-93.2 13.5-11.6 4.1-20.7 9-26.3 13.7-5.7 4.7-7.5 8.6-7.5 11.8 0 3.2 1.8 7.1 7.5 11.8 5.6 4.7 14.7 9.5 26.3 13.7 23.3 8.2 56.5 13.5 93.2 13.5s69.9-5.3 93.2-13.5c11.6-4.2 20.7-9 26.3-13.7 5.7-4.7 7.5-8.6 7.5-11.8 0-3.2-1.8-7.1-7.5-11.8-5.6-4.7-14.7-9.6-26.3-13.7-23.3-8.2-56.5-13.5-93.2-13.5zm-105 79.2v45.8h18v-39.8c-4.2-1.2-8.3-2.4-12.2-3.8-2-.7-3.9-1.4-5.8-2.2zm210 0c-1.9.8-3.8 1.5-5.8 2.2-3.9 1.4-8 2.6-12.2 3.8v39.8h18zm-107.1 19c-9.5 1.2-18.1 12.3-18.1 26.8 0 8 2.7 15 6.7 19.9l4.9 5.8-7.5 1.4c-5.2 1-9 3.8-12.3 8.3-3.4 4.6-5.9 10.8-7.6 18.1-3.2 13.1-3.9 29.1-4 43.3h17.5l4.7 64.3c12.2 2.8 25.2 2.6 36.9 0l4.3-64.3h16.6c0-14.3-.2-30.6-3.1-43.9-1.6-7.2-4-13.5-7.3-18s-7.4-7.2-13.3-8.1l-7.6-1.2 4.8-6c3.9-4.8 6.5-11.8 6.5-19.6 0-15.4-9.6-26.8-20.1-26.8z",
    slug: "round-table",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/round-table.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/round-table.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  bed: {
    path: "M239.802 74.44v.19h12.275v29.433h.004v31.265a85.275 85.275 0 0 0-10.095-1.084c-26.8-1.293-55.033 8.738-73.23 13.36l-7.545 1.92.582 5.564c-.46-.176-.917-.356-1.387-.525l-.705-.256-.74-.135c-4.097-.747-8.27-1.193-12.48-1.39-29.477-1.372-60.834 9.463-81.174 14.523l-7.612 1.892.836 7.8c.605 5.644 1.218 11.59 2.774 17.493-10.642 13.072-10.078 18.35-8.417 27.184l211.14 73.916v74.053C184.03 336.45 106.252 295.828 25.582 264.49v-170h18v.125h12.374v34.77l165.848-21.414V74.44zm-2.088 77.845c1.203-.013 2.39 0 3.564.04 2.818.095 5.505.396 8.09.84 13.548 5.197 20.296 12.637 24.25 21.462-23.255 9.644-44.174 13.507-62.515 15.736-5.277-1.15-9.503-2.466-12.944-3.894a63.312 63.312 0 0 0-16.522-20.16 91.603 91.603 0 0 1-.584-3.33c17.414-4.63 38.614-10.504 56.66-10.695zm-94.35 18.528c1.38-.014 2.743 0 4.09.046a69.18 69.18 0 0 1 9.26.95c15.757 5.89 23.546 14.435 28.002 24.526-26.44 10.85-50.22 15.162-70.965 17.62-17.42-3.692-25.116-8.99-29.17-14.665-3.072-4.302-4.524-9.753-5.53-16.518 19.495-5.077 43.62-11.753 64.314-11.96zM291.8 186.295l26.406 7.453c-59.194 10.41-125.095 28.732-165.18 45.766l-27.443-9.17c21.235-3.146 45.785-8.753 72.568-20.846l5.29-2.39c1.72.44 3.5.853 5.35 1.232l1.42.29 1.44-.17c21.562-2.54 47.905-7.294 77.15-20.782zm68.797 19.418 51.336 14.49-147.905 38.377v17.6l-82.517-27.147-1.77-.59c49.176-17.717 124.438-36.303 180.857-42.73zm127.79 13.68v90.57L283.69 372.127v-99.62zM23.613 282.45 60.837 299v14.674L39.98 322.13l-16.366-10.57zm463.26 49.243v34.995l-21.91 9.515-16.367-7.4v-25.487zm-234.453 52.49 11.608 5.16 9.442 4.196 19.342-6.87v40.848l-22.704 10.043-17.687-12.685z",
    slug: "bed",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/bed.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/bed.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  barrel: {
    path: "M256 41c-43.696 0-83.28 3.58-111.37 9.197-14.047 2.81-25.26 6.196-32.21 9.483-3.476 1.643-5.842 3.293-6.88 4.306l-.013.014.014.014c1.038 1.013 3.404 2.663 6.88 4.306 6.95 3.287 18.163 6.674 32.21 9.483C172.72 83.42 212.303 87 256 87s83.28-3.58 111.37-9.197c14.047-2.81 25.26-6.196 32.21-9.483 3.476-1.643 5.842-3.293 6.88-4.306l.013-.014-.014-.014c-1.038-1.013-3.404-2.663-6.88-4.306-6.95-3.287-18.163-6.674-32.21-9.483C339.28 44.58 299.697 41 256 41zm-80 15a32 8 0 0 1 32 8 32 8 0 0 1-32 8 32 8 0 0 1-32-8 32 8 0 0 1 32-8zm-75.168 26.594a1745.469 1745.469 0 0 0-12.05 55.28 671.56 671.56 0 0 0 50.327 10.995c2.988-17.203 6.707-34.438 11.27-51.708-3.186-.547-6.3-1.113-9.282-1.71-14.91-2.98-27.13-6.49-36.37-10.86a65.244 65.244 0 0 1-3.896-1.998zm310.336 0a66.12 66.12 0 0 1-3.895 1.998c-9.24 4.37-21.462 7.88-36.37 10.86-2.93.587-5.99 1.142-9.116 1.68 5.27 16.954 9.544 34.033 12.953 51.22a668.043 668.043 0 0 0 48.424-10.754 1745.49 1745.49 0 0 0-11.996-55.004zm-67.4 17.238c-23.065 2.982-49.9 4.803-78.768 5.117v54.198a660.337 660.337 0 0 0 91.975-7.773c-3.45-17.334-7.805-34.523-13.207-51.543zm-175.475.008c-4.647 17.345-8.416 34.67-11.426 51.98A660.497 660.497 0 0 0 247 159.174V104.95c-28.842-.314-55.656-2.133-78.707-5.11zm-84.38 55.277-5.518 30.088c128.542 30.936 239.89 29.948 353.384.137l-4.98-30.172c-110.776 28.798-228.035 29.785-342.886-.053zm350.634 48.176a729.99 729.99 0 0 1-50.83 11.312c3.656 47.603 1.776 95.87-3.55 144.49 18.6-3.803 36.796-8.527 54.468-14.17C439.592 314.762 439 291.606 439 256c0-14.915-1.77-33.334-4.453-52.707zm-357.13.256C74.758 222.827 73 241.15 73 256c0 23.794 4.678 57.228 10.424 89.404a635.542 635.542 0 0 0 50.27 12.418c-4.532-47.516-6.03-95.247-2.577-143.222-17.624-3.063-35.507-6.74-53.7-11.05zm71.546 13.944c-3.336 47.978-1.63 95.883 3.164 143.813a641.805 641.805 0 0 0 94.873 9.33V225.94c-31.995-.576-64.57-3.38-98.037-8.446zm216.902.19A677.143 677.143 0 0 1 265 226.023V370.8c32.816-.174 65.224-2.93 96.64-8.25 5.61-49.032 7.722-97.417 4.225-144.866zM86.66 364.93l8.29 31.9c104.15 32.39 225.75 32.428 326.077.733l8.272-32.264c-106.024 31.367-228.01 31.34-342.64-.37zm11.236 51.666c3.816 16.945 6.585 28.183 6.704 28.662.792 2.185 4.694 6.427 12.96 10.37 7.587 3.616 18.215 6.947 30.77 9.704a1879.47 1879.47 0 0 1-6.016-37.74 546.987 546.987 0 0 1-44.418-10.996zm318.366 1.31c-14.934 4.36-30.254 8.052-45.852 11.086a1770.631 1770.631 0 0 1-6.43 36.272c12.422-2.745 22.935-6.05 30.46-9.637 8.376-3.994 12.302-8.315 13.02-10.473a1427.72 1427.72 0 0 0 8.802-27.248zm-255.217 13.18a1936.038 1936.038 0 0 0 6.144 37.74c23.637 3.684 51.525 5.748 79.81 6.11V439.24c-28.815-.644-57.66-3.36-85.955-8.154zm190.55 1.223c-28.306 4.484-57.373 6.847-86.595 7.07v35.556c28.358-.363 56.317-2.437 79.994-6.14a1820.27 1820.27 0 0 0 6.602-36.487z",
    slug: "barrel",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/barrel.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/barrel.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  flag: {
    path: "M356.688 19.188c-6.83-.032-12.837.64-18.125 1.843-24.178 5.495-36.437 21.983-50.938 41.157-14.5 19.175-31.317 40.993-62.78 47.47C195.08 115.78 154.27 108.253 91.25 78.5c-10.013 44.88-33.406 128.62-60.906 178.656 60.093 28.5 97.245 34.926 121 30.875.01 0 .02.004.03 0 21.59-5.827 34.487-20.094 47.876-43.092 17.014-29.227 32.563-72.198 60.25-123.188l16.406 8.938c-16.69 30.735-28.802 58.617-40 82.937 8.552-6.512 18.633-11.77 31.063-14.594 27.71-6.296 65.053-.495 121.655 24.75-6.932-29.276-1.885-61.913 9.875-92.218 12.686-32.69 33.038-62.907 56.28-84.03-42.595-19.553-73.152-27.554-95.124-28.282-1.01-.033-1.993-.058-2.97-.063zm127.54 14.144a10.775 10.775 0 0 0-2.664.266c-4.378.977-8.94 4.424-12.084 11.097L289.53 497.31h23.61L490.972 49.368c3.475-10.153-.75-15.86-6.746-16.035z",
    slug: "flying-flag",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/flying-flag.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/flying-flag.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  star: {
    path: "m436.406 29.625-18.094 42.22-48.562 5.905 42.156 25.656 1.375 13.47C367.938 90.74 302.435 75.36 214.78 82.31l-20.186-3.343-24.125-38.407.5 39.78-49.22 16.438 55.063 4.564 7.843 33.78 17.094-37.78 17.906-2.75c203.993 22.03 277.475 204.75 77.875 207.625l5.22-37.595 36.75-43.72-51.344-24.968-30.22-48.468-39.623 41.124-4.125 1.03C-8.4 163.078-31.708 304.485 98.844 376.125l-11.938 12.688L39.844 374.5l33.03 39.406-15.124 42.53 36.375-31.155 47.03 18.095-30.374-43.875 4.69-15.03c62.43 28.648 153.852 42.16 270.5 20.717-241.042 33.38-364.142-137.94-219.283-195.687l23.032 43.25-4 56.97 56.218-9.97 19.25 7.813c218.255 102.608 297.46-83.917 171.843-177.75l14.376-22.22 46.47-16.5-41.907-14.812-15.564-46.655zM34.53 79.03l4.845 26.095-19.47 22 27.22-3.25 17.563 23.344.687-29.47 24.78-17.906-33.218-1.72L34.531 79.03zm358.564 298.5 14.25 51.658-31.375 41.062 49.592-12.688 33.688 34.282-2.53-51.406 35.217-30.375-51.593 1.562-47.25-34.094z",
    slug: "star-swirl",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/star-swirl.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/star-swirl.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  torch: {
    path: "M476.72 51.375c-5.28 30.185-124.992 107.9-82.47 16.875-80.216 45.38-107.557 23.42-78.53-5.656-54.825 2.8-62.753 88.173-55.345 112.406l17.438 19.125c-2.14-31.218.404-48.445 19.5-71.47 13.764 20.614 18.495 33.702 52.062 6.97-9.393 53.52 54.61 18.747 88.75 10.938-15.248 14.048-35.153 32.723-38.875 55.468-1.24 7.587 6.208 17.925 14.125 25.626-9.443 2.236-41.474 8.91-38.563 26.22 2.912 17.31 12.14 11.885 3.5 15.28-12.403 2.766-21.156 5.58-39.593-2.187l18.874 20.717c28.39 14.79 73.904 7.306 83.594-14.875-14.778-1.22-27.125-4.674-33-11.53 44.022-8.34 66.764-39.243 85.78-75.032-33.638 18.95-42.158 17.784-56 16.313 35.514-14.365 46.876-108.943 38.75-145.188zM246.874 186.063l-56.78 70.125 79.186 86.906 75.095-50-97.5-107.03zm-62.344 90.125L21.657 467.625l21.438 23.53 205.75-144.374-64.313-70.592z",
    slug: "torch",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/torch.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/torch.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  portal: {
    path: "M298.736 21.016c-99.298 0-195.928 104.647-215.83 233.736-7.074 45.887-3.493 88.68 8.512 124.787a243.08 243.08 0 0 1-11.467-20.034c-16.516-32.335-24.627-65.378-25-96.272-11.74 36.254-8.083 82.47 14.482 126.643 27.7 54.227 81.563 91.94 139.87 97.502a136.491 136.491 0 0 0 17.364 1.108c99.298 0 195.93-104.647 215.83-233.736 9.28-60.196.23-115.072-22.133-156.506 21.625 21.867 36.56 45.786 44.617 69.496.623-30.408-14.064-65.766-44.21-95.806-33.718-33.598-77.227-50.91-114.995-50.723-2.328-.118-4.67-.197-7.04-.197zm-5.6 36.357c40.223 0 73.65 20.342 95.702 53.533 15.915 42.888 12.51 108.315.98 147.858-16.02 54.944-40.598 96.035-79.77 126.107-41.79 32.084-98.447 24.39-115.874-5.798a43.356 43.356 0 0 1-3.38-7.385c11.724 14.06 38.188 14.944 61.817 1.3 25.48-14.71 38.003-40.727 27.968-58.108-10.036-17.384-38.826-19.548-64.307-4.837-9.83 5.676-17.72 13.037-23.14 20.934a81.918 81.918 0 0 1 1.626-3.88c-18.687 24.49-24.562 52.126-12.848 72.417 38.702 45.923 98.07 25.503 140.746-6.426 37.95-28.392 72.32-73.55 89.356-131.988a295.488 295.488 0 0 0 3.467-13.008c-.286 2.218-.59 4.442-.934 6.678-16.807 109.02-98.412 197.396-182.272 197.396-35.644 0-65.954-15.975-87.74-42.71-26.492-48.396-15.988-142.083 4.675-185.15 26.745-55.742 66.133-122.77 134.324-116.804 46.03 4.027 63.098 58.637 39.128 116.22-8.61 20.685-21.192 39.314-36.21 54.313 24.91-16.6 46.72-42.13 59.572-73 23.97-57.583 6.94-113.422-39.13-116.805-85.737-6.296-137.638 58.55-177.542 128.485a318.924 318.924 0 0 0-20.977 60.707 263.164 263.164 0 0 1 2.493-22.652C127.67 145.75 209.275 57.373 293.135 57.373z",
    slug: "portal",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/portal.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/portal.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  tree: {
    path: "M348.92 15.633a52.31 52.31 0 0 0-4.168.113l-.002.006c-13.368.9-26.782 7.143-39.715 19.27-20.056-5.876-37.08-10.614-57.232-.58-37.81-11.887-75.686-13.068-104.54 19.27-42.477-10.09-87.2 9.883-76.51 58.985-47.074 14.955-56.408 78.2-.58 73.586 7.938 46.78 41.118 55.937 83.514 40.883 19.41 24.008 46.462 39.182 77.282 46.443.28 5.105.085 10.33-.566 15.68-5.807 47.77-50.79 105.31-121.367 154.696L81.12 460.723l29.16-1.354c29.003-1.347 48.05-2.39 58.935-1.55-5.818 6.555-11.317 12.37-16.28 17.297L134.04 493.88l26.26-4.417c23.887-4.017 44.442-7.94 59.927-8.363 15.484-.423 24.434 2.174 29.916 9.552l7.33 9.864 6.597-10.368c6.53-10.26 13.576-12.87 27.227-12.257 13.65.613 32.84 6.013 57.043 13.3l29.69 8.94-21.036-22.782a394.31 394.31 0 0 1-23.357-27.786 130.6 130.6 0 0 1 8.015.596c12.14 1.3 26.887 3.978 40.67 8.067l26.127 7.75-17.145-21.184c-32.25-39.843-64.6-69.885-69.768-104.74-2.584-17.43.916-36.76 15.28-61.374a196.408 196.408 0 0 1 6.49-10.31 286.497 286.497 0 0 0 23.634-7.913c27.094 24.058 78.02 4.07 50.812-27.45 58.72-14.457 105.89-59.625 26.865-95.196 46.613-36.08 17.39-90.36-40.297-88.188-14.636-21.026-34.603-33.72-55.4-33.987zm11.277 65.488c-42.59 32.678-48.978 37.184-53.506 65.825.317 3.472.597 7.013 1.103 10.11 3.153 19.296 10.22 32.648 21.43 39.89 11.21 7.243 24.752 7.554 38.814 5.344 3.876-.61 7.86-1.446 11.91-2.397-26.82 26.337-45.555 49.373-57.742 70.253-15.75 26.986-20.6 50.827-17.404 72.385 5.283 35.632 30.606 63.305 56.864 93.556-6.317-1.147-12.557-2.146-18.21-2.752-8.907-.955-16.085-1.496-22.225-1.018-15.78-23.394-27.137-46.75-28.396-64.013l-16.875 1.23c2.246 30.812 24.037 66.155 49.786 97.565-12.35-3.28-23.554-5.657-33.69-6.112-13.696-.614-26.363 3.152-35.763 12.244-9.7-7.222-22.628-9.424-36.527-9.044-11.164.304-23.902 2.172-37.366 4.402 44.064-49.242 105.015-137.42 104.04-226.442l-16.92.186c.81 73.714-47.797 152.037-88.25 201.336-11.867-3.388-25.73-3.414-44.05-2.566 60.184-46.96 99.88-99.595 105.98-149.77 3.418-28.11-4.46-55.45-25.085-78.033-16.147-17.684-39.853-32.51-71.576-44.105 7.55-1.88 14.953-4.365 21.462-9.18 6.738-4.988 11.306-13.447 12.117-23.892 4.84 5.99 9.412 10.25 15.808 12.004 3.987 1.093 8.89.41 12.398-1.818 1.18-.75 2.055-1.72 2.987-2.65 1.008 13.36 2.204 26.116 4.996 35.543 1.305 4.41 2.77 8.54 6.917 11.938 4.147 3.4 11.522 3.424 16.02.846 2.954-1.693 5.624-4.19 8.255-7.105.53 5.72 1.776 10.502 4.068 14.624 1.406 2.527 3.53 5.005 6.594 6.473 3.065 1.467 6.72 1.527 9.586.712 5.732-1.63 9.41-5.37 13.29-10.104 8.362-10.204 17.167-27.234 25.41-49.5-1.647-26.645-3.49-39.06-35.522-58.758 25.123 13.013 47.678 20.495 89.27-7.207z",
    slug: "oak",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/oak.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/oak.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  rock: {
    path: "m317.727 108.904-95.192 96.592-26.93 86.815 17.54 36.723 20.417 9.287 33.182-55.082 11.297-3.61 61.75 26.85 20.26-12.998 4.47-43.7 11.42 53.634-10.622 14.162 3.772 1.64 5.238 6.5 6.832 34.343 55.977-66.775 13.98.23 22.397 28.575-9.453-52.244L434.01 166.81l-116.28-57.906zM123.61 120.896 94.08 173l-4.603 27.62 25.98-8.442 11.704 7.377.084.634 28.295 59.865 13.773-4.543 10.94 4.668 3.922 8.21 19.517-62.917-1.074-33.336-40.15-.522-29.732-23.78 34.06 10.888 42.49-7.727 26.034 15.88 36.282-36.815a332.698 332.698 0 0 0-8.58-3.52l-79.58 10.126-3.528-.25-56.307-15.52zm249.33 36.422 47.058 66.02 2.107 62.51-25.283-59.698-65.322-60.404 41.44-8.428zm-262.2 55.32-64.234 20.876-16.71 78.552 50.794 5.582.596-7.14 37.662-36.707-8.108-61.16zm56.688 62.45-36.44 12.016-31.644 30.84 22.588 30.867 57.326 1.74 16.5-16.16-28.33-59.302zm110.666 24.19-44.307 73.546-.033 57.14 97.264 12.216 44.242-19.528-17.666-88.806-79.5-34.567zM443.8 313.36l-46.843 55.876.287 1.774 65.147 13.887 25.78-14.926-44.37-56.613zm-138.382 15.89 39.23 22.842 13.41 50.658-26.82 23.838-45.015-2.553 38.562-28.242 2.483-39.23-21.85-27.312zm-238.37 53.838-8.77 28.51 13.152 48.498 91.037-11.91 1.32-26.418-62.582-31.995-34.156-6.684z",
    slug: "stone-pile",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/stone-pile.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/stone-pile.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  water: {
    path: "M107.156 17.72c-22.515 130.852-89.5 157.227-89.5 248.968 0 46.03 45.485 79.843 89.5 79.843 1.7 0 3.402-.066 5.094-.155.82-6.47 1.875-12.74 3.125-18.844a69.49 69.49 0 0 1-9.063.595c-38.51 0-69.656-31.488-69.656-70.72 0-39.23 31.145-70.717 69.656-70.717.576 0 1.147.017 1.72.03-17.205 9.417-28.876 28.2-28.876 49.5 0 27.927 20.063 51.5 46.344 55.345 10.542-29.276 25.437-55.543 41.125-83.125a3540.08 3540.08 0 0 0 9.25-16.344c-20.93-44.947-53.572-89.35-68.72-174.375zm296.531 0c-14.905 86.624-49.318 127.455-70.875 171.78 4.337 8.4 8.666 16.6 12.907 24.625.27.514.54 1.018.81 1.53 12.664-17.583 33.143-28.968 56.283-28.968.585 0 1.167.017 1.75.032-17.214 9.413-28.907 28.192-28.907 49.5 0 30.555 24.045 55.905 53.938 55.905 16.704 0 31.57-7.9 41.437-20.22-6.557 32.216-34.585 56.22-68.217 56.22-3.16 0-6.266-.245-9.313-.656a259.132 259.132 0 0 1 3.656 18.78c2.18.163 4.354.28 6.53.28 45.005 0 87.752-31.642 87.752-75.342 0-88.064-64.484-122.863-87.75-253.47zM256.72 52.47c-18.814 73.33-47.047 123.11-71.69 166.436-29.68 52.186-53.686 94.326-53.686 161.313 0 31.412 15.432 58.706 39.094 78.624 23.66 19.917 55.448 31.906 86.125 31.906 31.66 0 62.733-11.278 85.468-29.906 22.736-18.628 37.095-44.098 37.095-73.78 0-64.192-23.26-108.428-52.125-163.064-23.955-45.34-51.36-97.692-70.28-171.53zm-1.5 212.56c6.448 0 12.75.62 18.874 1.75-31.1 8.977-53.844 38.675-53.844 73.5 0 41.822 32.78 76.314 73.47 76.314 28.857 0 53.72-17.368 65.75-42.375-1.763 57.015-47.717 102.374-104.25 102.374-57.654 0-104.314-47.18-104.314-105.78 0-58.603 46.66-105.783 104.313-105.783z",
    slug: "droplets",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/droplets.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/droplets.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  heart: {
    path: "m142.094 20.063 37.594 76.78a94.196 94.196 0 0 0-27.47-4.187 94.293 94.293 0 0 0-23 2.78c-9.593 2.388-18.805 6.298-27.312 11.533L18.53 23.593v70.312l56.19 37.813c-12.143 16.172-19.905 36.81-20.657 60.78-.14 4.438-.026 8.76.312 12.97l-35.844-9.657v41.843l44.72 3.094c3.274 7.523 7.29 14.613 11.906 21.28 9.205 13.295 20.61 25.098 33 36.345L18.53 322.188V368.5l109.157-53.28c13.032 10.868 26.395 21.638 39.094 33L20.345 494.28H91.78l88.97-132.936c14.037 13.884 26.635 28.95 36.188 46.344l-23.22 86.593h41.595l4.593-65.936 1.938-5.03c9.503-24.743 24.99-45.3 42.937-63.94l35.626 134.907h47.22L296.218 348c9.033-8.604 18.47-16.856 27.936-25.03L492.78 491.593v-70.938l-158.374-106.53c1.105-.955 2.21-1.922 3.313-2.876 10.166-8.807 20.203-17.72 29.467-27.063l125.594 33.907v-42.22l-111-7.718c13.545-16.36 24.01-34.353 28.533-55.437l82.468-21.94v-47.843l-80.186 39.47c-1.48-25.603-11.335-46.322-25.938-61.25L487.75 20.062h-70.375l-55.906 83.062a92.902 92.902 0 0 0-22.376-7.406c-14.422-2.82-29.657-2.43-44.47 1.186l20.813-76.844h-41.843l-5.938 87.438c-12.74 7.1-24.463 16.97-34.187 29.688-5.627-8.07-11.943-14.987-18.72-20.813L189 20.062h-46.906zm10.125 91.124c27.033-.222 54.69 13.835 72.655 47.563l7.906 14.813 8.44-14.532c21.855-37.654 61.142-51.45 94.31-44.968 33.17 6.483 59.992 31.78 58.5 78.438-1.38 43.233-31.41 72.474-68.53 104.625-32.58 28.218-70.07 57.918-92.594 100.78-23.6-42.682-62.317-71.126-95.156-98.624-18.69-15.648-35.51-30.997-47.22-47.905-11.707-16.908-18.508-35.107-17.78-58.28 1.364-43.454 28.753-71.513 61-79.532 6.046-1.504 12.23-2.324 18.47-2.376zM314.97 135.5c-11.794 0-23.042 3.426-32.25 9.406 20.885-2.46 44.746 10.98 59.03 35.72 12.43 21.526 14.015 45.497 5.938 62.468 15.646-10.572 25.78-28.802 25.78-49.094a58.297 58.297 0 0 0-58.5-58.5z",
    slug: "shining-heart",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/shining-heart.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/shining-heart.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  lips: {
    path: "M166.844 112.344c-42.01 38.554-97.815 75.023-147.53 86.03 39.097 14.38 89.036 18.718 134.717 9.157 53.264 31.33 128.095 32.185 184.876 0 44.99 15.297 96.346 13.18 149.688-9.31-59.564-9.606-125.004-51.588-155.25-85.876-55.502 39.62-111 32.146-166.5 0zm-133.72 114.03c17.33 29.572 48.467 79.248 89.75 131.064 27.24 34.188 76.606 31.093 129.97 31.093 49.298 0 102.29.706 127.625-31.092 37.724-47.35 68.59-92.143 88.217-122-124.874 84.53-316.773 93.18-435.562-9.063z",
    slug: "lips",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/lips.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/lips.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  lovers: {
    path: "M132.255 18.093c-32.842 0-59.592 33.923-59.592 75.935 0 21.274 7.079 40.468 18.092 54.284C22.623 158.879 7.903 253.733 24.1 339.874h23.563L46.63 234.53l18.688-.185 1.25 126.685 8.75 132.877h46.875v-172.29c-15.257-18.713-25.615-39.351-26.424-64.825l-.004-.1v-.1c-.154-15.988 6.478-35.643 19.219-52.566 12.741-16.922 32.481-30.98 57.783-31.29 17.252-.212 35.458 5.013 51.338 14.771-12.131-20.214-29.245-34.177-51.26-38.32 11.507-13.88 18.971-33.345 18.971-55.157.097-36.903-24.805-76.041-59.562-75.937zm247.841 0c-32.842 0-59.591 33.923-59.591 75.935 0 21.274 7.078 40.468 18.091 54.284-25.296 3.923-43.223 19.473-54.945 41.875 16.803-11.476 36.697-17.682 55.465-17.452 25.302.311 45.042 14.369 57.783 31.291 12.741 16.923 19.373 36.578 19.219 52.567v.1l-.004.1c-.826 26.007-11.608 46.974-27.393 65.997v171.113h44.315l11.5-154.029h.03l1.062-105.531 18.687.185-1.062 105.346h24.53c18.363-88.46-4.724-178.95-67.095-190.687 11.507-13.88 18.97-33.345 18.97-55.157.098-36.903-24.805-76.041-59.562-75.937zM172.99 190.733c-18.837.232-33.281 10.381-43.625 24.12-10.326 13.715-15.675 31.13-15.594 41.506 1.226 36.63 26.557 61.833 61.219 92.044 26.758 23.322 58.19 48.519 80.617 83.38l.357-.637.336.601c22.427-34.843 53.847-60.03 80.596-83.344 34.662-30.211 59.993-55.414 61.219-92.044.081-10.376-5.268-27.791-15.594-41.506-10.344-13.739-24.788-23.888-43.625-24.12-28.453-.349-63.84 20.288-74.414 52.022l-8.539 25.625-8.54-25.625c-10.573-31.734-45.96-52.37-74.413-52.021zm-32.11 150.989v152.181h44.315l7.893-105.714c-9.94-9.023-20.098-17.651-29.924-26.215-7.726-6.734-15.242-13.43-22.283-20.252zm229.157.93c-6.762 6.5-13.94 12.894-21.315 19.322-10.636 9.27-21.66 18.622-32.375 28.463l6.815 103.47h46.875z",
    slug: "lovers",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/lovers.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/lovers.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  cherish: {
    path: "M159.361 28.344c-51.352.63-79.54 56.303-79.244 86.957 1.504 47.346 34.181 79.356 74.82 114.763 35.351 30.799 76.091 63.283 100.907 109.987.05-.092.105-.18.156-.272.051.092.105.18.156.272 24.816-46.704 65.556-79.188 100.906-109.987 40.64-35.407 73.317-67.417 74.82-114.763.297-30.654-27.891-86.327-79.243-86.957C314.636 27.878 270 54 256 96c-14-42-58.636-68.122-96.639-67.656zM60.17 198.06c-8.818-.137-17.843 11.093-17.895 39.882-.078 44.153-4.356 56.616 16.077 106.551C73.335 381.112 80.054 409.257 128 432c5.68 20.022 3.413 24.73-.44 41.84-3.596 15.974 33.423 18.91 60.534 5.453 29.091-15.868 26.65-59.557 21.453-89.184-6.044-34.454-25.06-41.615-41.543-56.332-17.115-24.475-21.098-68.813-48.856-86.699-5.797-3.735-35.37-7.527 5.262 93.942-53.571-13.268-43.813-74.773-47.687-120.31-1.154-13.561-8.773-22.53-16.553-22.65zm391.66 0c-7.78.12-15.399 9.088-16.553 22.65-3.874 45.536 5.884 107.041-47.687 120.309 40.633-101.47 11.059-97.677 5.262-93.942-27.758 17.886-31.74 62.224-48.856 86.7-16.482 14.716-35.5 21.877-41.543 56.331-5.197 29.627-7.638 73.316 21.453 89.184 27.111 13.456 64.13 10.521 60.533-5.453-3.852-17.11-6.119-21.818-.439-41.84 47.946-22.743 54.665-50.888 69.648-87.506 20.433-49.935 16.155-62.398 16.077-106.55-.052-28.79-9.077-40.02-17.895-39.883z",
    slug: "cherish",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/cherish.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/cherish.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  "love-letter": {
    path: "M216.058 50.312c-.86-.005-1.572.037-2.12.123-5.981.932-20.108 8.016-36.207 19.511-16.098 11.496-34.887 27.134-53.93 44.485C95.74 140 67.162 169.31 45.217 194.7a3640.34 3640.34 0 0 0 50.812-3.872c1.538-16.08 6.79-32.61 17.368-45.879 10.166-12.751 26.122-21.682 46.617-21.675 18.943.006 41.55 7.126 68.935 23.275 21.438-25.45 40.574-39.736 58.412-45.451 4.809-1.54 9.513-2.39 14.06-2.643 13.645-.757 25.888 3.872 35.364 11.428 12.635 10.075 21.142 24.598 26.899 38.805.992 2.45 1.896 4.884 2.724 7.293 23.722-4.31 47.353-8.98 70.862-14.045-37.748-21.248-80.89-42.643-120-59.428-24.529-10.527-47.336-19.235-65.735-24.926-16.098-4.98-29.455-7.241-35.476-7.271zM305.4 116.13c-3.799-.029-7.94.635-12.548 2.111-14.046 4.5-32.654 17.808-54.899 45.59l-5.01 6.26-6.795-4.258c-28.717-17.994-50.548-24.553-66.142-24.558-15.595-.005-25.28 5.793-32.537 14.896-14.515 18.206-15.809 52.865-10.364 69.465 18.603 56.713 44.738 83.02 82.76 99.79 40.195-.354 79.786-7.153 118.967-19.483 20.884-31.578 38.095-67.142 36.006-118.356-.289-7.076-2.844-19.814-7.838-32.139-4.994-12.324-12.465-24.333-21.44-31.49-5.609-4.473-11.496-7.346-18.552-7.773-.53-.032-1.065-.05-1.608-.055zm-3.562 12.434a32.378 32.378 0 0 1 1.945.008c12.646.438 23.185 11.183 29.035 24.455-30.127-21.403-61.454-.145-79.638 12.49 12.363-17.632 28.675-36.452 48.658-36.953zm-137.897 25.761c13.546.007 30.081 4.885 46.036 17.26-86.153-26.888-63.03 66.667-35.965 120.168-28.36-17.18-59.2-88.077-38.166-125.47 3.994-7.101 14.55-11.965 28.095-11.958zm273.852 5.817a2248.032 2248.032 0 0 1-66.674 13.187c.972 4.94 1.54 9.517 1.703 13.524 1.966 48.19-12.285 84.872-30.328 115.326 3.958-.999 8.002-2.008 11.836-2.985 35.11-44.441 56.905-91.65 83.463-139.052zm15.437 9.289c-24.388 43.716-45.437 88.85-78.228 132.535 42.408 26.96 76.17 59.279 109.562 91.057-4.48-75.07-16.635-149.384-31.334-223.592zm-357.435 39.4a3715.148 3715.148 0 0 1-68.361 5.033c42.3 39.285 77.699 79.787 131.166 113.1 2.476.118 4.947.205 7.414.275-28.645-19.655-50.737-49.423-66.012-95.994-2.143-6.533-3.63-14.2-4.207-22.414zm-66.64 31.49 35.439 208.356c15.896-32.83 45.198-74.957 77.945-110.531-46.16-29.99-78.98-64.656-113.385-97.825zm333.741 75.29-1.09.277c-54.862 13.97-125.265 32.309-201.154 29.244-35.114 37.072-67.574 85.062-82.158 116.557 146.769-2.776 272.969-26.229 398.856-50.698-35.89-34.067-70.647-67.974-114.454-95.38z",
    slug: "love-letter",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/love-letter.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/love-letter.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  "locked-heart": {
    path: "M256 27.845c-21.563 0-48.67 1.224-69.041 9.357C166.589 45.336 153 58.047 153 86.845v51.847a125.34 125.34 0 0 1 19.988-1.732c3.353-.02 6.693.103 10.012.355v-38.47c0-16 11.186-28.155 24.975-35.05 13.788-6.893 30.9-9.95 48.025-9.95s34.237 3.057 48.025 9.95C317.815 70.69 329 82.846 329 98.846v38.478a124.58 124.58 0 0 1 30 1.377V86.845c0-28.798-13.589-41.51-33.959-49.643-20.37-8.133-47.478-9.357-69.041-9.357zm-83.12 127.093a107.73 107.73 0 0 0-22.886 2.682C109.396 166.847 73 198.594 73 258.845c0 43 37.72 89.974 81.213 131.49 21.747 20.758 44.64 40.173 64.25 57.52 15.099 13.356 28.124 25.257 37.537 36.3 9.413-11.043 22.438-22.944 37.537-36.3 19.61-17.347 42.503-36.762 64.25-57.52C401.28 348.819 439 301.845 439 258.845c0-60.25-36.396-91.998-76.994-101.225-40.598-9.227-83.648 5.56-97.65 40.567L256 219.075l-8.355-20.888c-11.377-28.443-41.931-43.537-74.764-43.249zM256 234.845a44.488 44.488 0 0 1 44.488 44.488A44.488 44.488 0 0 1 284 313.876l16.488 98.922h-88.976l16.49-98.936a44.488 44.488 0 0 1-16.49-34.529A44.488 44.488 0 0 1 256 234.845z",
    slug: "locked-heart",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/locked-heart.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/locked-heart.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  perfume: {
    path: "M257.125 18.53c-43.358 0-78.25 34.79-78.25 77.907 0 43.118 34.892 77.907 78.25 77.907s78.28-34.79 78.28-77.906c0-43.118-34.922-77.907-78.28-77.907zm-88.5 117.314c-6.644 8.198-10.156 17.204-10.156 26.47 0 15.315 9.554 29.91 27.093 41.31 17.538 11.4 42.63 18.876 70.53 18.876s53.025-7.475 70.563-18.875c17.54-11.4 27.094-25.996 27.094-41.313 0-8.654-3.057-17.087-8.875-24.843-15.528 32.8-49.04 55.56-87.75 55.56-39.35 0-73.333-23.508-88.5-57.186zm-27.22 39.72c-44.97 28.067-73.686 72.15-73.686 121.03 0 83.262 83.312 152.594 188.374 152.594 105.06 0 188.375-69.332 188.375-152.594 0-48.867-28.708-92.93-73.658-121-4.323 17.498-16.962 32.665-33.968 43.72-21.18 13.765-49.568 21.874-80.75 21.874-31.183 0-59.54-8.11-80.72-21.875-17.015-11.06-29.65-26.24-33.968-43.75zm-48.342 110.59h.032c14.633 56.066 82.046 98.406 163.125 98.406 81.077 0 147.834-42.338 162.467-98.406 2.013 7.54 3.938 15.033 3.938 23 0 66.954-74.647 121.344-166.406 121.344-91.76 0-166.44-54.39-166.44-121.344 0-7.967 1.27-15.46 3.282-23zM415.47 406.03c-38.153 37.933-95.66 61.845-159.376 61.845-63.68 0-121.16-23.885-159.313-61.78 5.558 20.953 20.806 40.113 43.595 55.374 29.125 19.503 70.212 31.967 115.75 31.967s86.594-12.464 115.72-31.968c22.81-15.277 38.07-34.46 43.624-55.44z",
    slug: "perfume-bottle",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/perfume-bottle.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/perfume-bottle.svg",
    viewBox: 512,
    fillRule: "nonzero"
  }
}, cse = {
  name: "CC BY 3.0",
  url: "https://creativecommons.org/licenses/by/3.0/",
  source: "https://game-icons.net",
  attribution: "Icons made by Delapouite and Lorc. Available on https://game-icons.net"
}, c5 = cse.attribution;
var dse = {
  city: "castle",
  district: "village",
  building: "house",
  floor: "stairs",
  room: "door",
  outdoor: "forest"
}, fse = new Set($x), The = new Set(Nx);
function Bm(e) {
  const t = String(e || "").trim();
  return fse.has(t) && use[t] || null;
}
function hse(e) {
  return Bm(e) ? 24 : 20;
}
function d5(e, t, n = 24) {
  const r = n / 512;
  return `translate(${Number((e - n / 2).toFixed(2))} ${Number((t - n / 2).toFixed(2))}) scale(${Number(r.toFixed(5))})`;
}
function pse(e) {
  return dse[String(e || "").trim()] || "house";
}
var mse = {
  key: 0,
  class: "tavern-atlas-empty"
}, gse = {
  key: 0,
  class: "tavern-atlas-warning"
}, vse = {
  key: 1,
  class: "tavern-atlas-graph"
}, yse = ["viewBox"], _se = ["d"], bse = ["onClick", "onKeydown"], wse = [
  "x",
  "y",
  "width",
  "height"
], Sse = [
  "d",
  "transform",
  "fill-rule"
], Tse = ["x", "y"], Ese = ["x", "y"], Ase = { class: "tavern-atlas-icon-credit" }, Cse = {
  key: 2,
  class: "tavern-atlas-detail"
}, kse = { key: 0 }, Ise = {
  key: 1,
  class: "tavern-atlas-detail-links"
}, xse = { key: 2 }, Mse = /* @__PURE__ */ un({
  __name: "TavernAtlasPanel",
  props: {
    document: {},
    patches: { default: () => [] },
    activeLocationKey: { default: "" },
    activeMapDocId: { default: "main" },
    previewMapDocId: { default: "" },
    mapDocuments: { default: () => [] },
    displayMode: { default: "full" }
  },
  setup(e) {
    const t = e, n = /* @__PURE__ */ ie(""), r = re(() => {
      const q = t.document?.data, z = q && typeof q == "object" && !Array.isArray(q) ? q : {};
      return {
        version: 1,
        activeLocationKey: String(z.activeLocationKey || t.activeLocationKey || "").trim() || void 0,
        locations: Array.isArray(z.locations) ? z.locations.filter(A) : [],
        links: Array.isArray(z.links) ? z.links.filter(I) : [],
        actors: Array.isArray(z.actors) ? z.actors.filter(R) : []
      };
    }), o = re(() => lse(r.value)), s = re(() => new Map(r.value.locations.map((q) => [q.key, q]))), a = re(() => s.value.get(n.value) || i.value || r.value.locations[0] || null), i = re(() => s.value.get(r.value.activeLocationKey || "") || null), l = re(() => String(i.value?.mapDocId || "").trim()), u = re(() => !!l.value && !!t.activeMapDocId && l.value !== t.activeMapDocId), d = re(() => String(t.patches.at(-1)?.summary || "").trim()), f = re(() => {
      const q = /* @__PURE__ */ new Map();
      return t.mapDocuments.forEach((z) => q.set(z.docId, z.title || z.docId)), q;
    }), m = re(() => new Set(t.mapDocuments.map((q) => q.docId))), v = re(() => $(a.value?.key || "")), y = re(() => {
      const q = a.value?.key || "";
      return r.value.links.filter((z) => z.from === q || z.to === q);
    }), T = re(() => t.displayMode !== "detail"), C = re(() => t.displayMode !== "graph");
    gt([() => r.value.activeLocationKey, () => r.value.locations.length], ([q]) => {
      n.value && s.value.has(n.value) || (n.value = String(q || r.value.locations[0]?.key || ""));
    }, { immediate: !0 });
    function A(q) {
      return !!q && typeof q == "object" && !Array.isArray(q) && typeof q.key == "string";
    }
    function I(q) {
      return !!q && typeof q == "object" && !Array.isArray(q) && typeof q.id == "string";
    }
    function R(q) {
      return !!q && typeof q == "object" && !Array.isArray(q) && typeof q.actorKey == "string";
    }
    function $(q) {
      return r.value.actors.filter((z) => z.locationKey === q).sort((z, J) => z.actorKey === "player" ? -1 : J.actorKey === "player" ? 1 : z.actorKey.localeCompare(J.actorKey));
    }
    function p(q) {
      return q.actorKey === "player" ? "玩家" : q.actorKey;
    }
    function b(q) {
      const z = $(q);
      if (!z.length) return "";
      const J = z.slice(0, 2).map(p);
      return z.length > 2 ? `${J.join("、")} +${z.length - 2}` : J.join("、");
    }
    function w(q) {
      return {
        "is-current": q.key === r.value.activeLocationKey,
        "is-preview": q.mapDocId && q.mapDocId === t.previewMapDocId,
        "is-selected": q.key === n.value,
        "is-mentioned": q.status === "mentioned",
        "has-map": E(q)
      };
    }
    function E(q) {
      const z = String(q?.mapDocId || "").trim();
      return !!z && m.value.has(z);
    }
    function x(q) {
      const z = String(q?.mapDocId || "").trim();
      return z ? f.value.get(z) || `已关联 ${z}，地图未创建` : "暂无详细地图";
    }
    function M(q) {
      return Bm(pse(q?.scale));
    }
    function N(q) {
      return d5(q.x + 18, q.y + 23, 18);
    }
    return (q, z) => (O(), F("section", { class: ze(["tavern-atlas-panel", `is-${e.displayMode}`]) }, [r.value.locations.length ? (O(), F(Je, { key: 1 }, [
      C.value && u.value ? (O(), F("div", gse, " 当前激活的场景图不是世界当前位置的地图。 ")) : ge("", !0),
      T.value ? (O(), F("div", vse, [(O(), F("svg", {
        viewBox: o.value.viewBox.join(" "),
        preserveAspectRatio: "xMidYMid meet",
        role: "img",
        "aria-label": "世界图"
      }, [
        z[2] || (z[2] = g("defs", null, [g("filter", {
          id: "tavern-atlas-sketch",
          x: "-5%",
          y: "-5%",
          width: "110%",
          height: "110%"
        }, [g("feTurbulence", {
          type: "fractalNoise",
          baseFrequency: "0.022",
          numOctaves: "3",
          seed: "11",
          result: "noise"
        }), g("feDisplacementMap", {
          in: "SourceGraphic",
          in2: "noise",
          scale: "0.55"
        })])], -1)),
        (O(!0), F(Je, null, lt(o.value.links, (J) => (O(), F("path", {
          key: J.id,
          class: ze(["tavern-atlas-link", `kind-${r.value.links.find((G) => G.id === J.id)?.kind || "path"}`]),
          d: J.path,
          filter: "url(#tavern-atlas-sketch)"
        }, null, 10, _se))), 128)),
        (O(!0), F(Je, null, lt(o.value.nodes, (J) => (O(), F("g", {
          key: J.key,
          class: ze(["tavern-atlas-node", w(s.value.get(J.key))]),
          role: "button",
          tabindex: "0",
          onClick: (G) => n.value = J.key,
          onKeydown: Vs(sn((G) => n.value = J.key, ["prevent"]), ["enter"])
        }, [
          g("rect", {
            x: J.x,
            y: J.y,
            width: J.width,
            height: J.height,
            rx: "8",
            filter: "url(#tavern-atlas-sketch)"
          }, null, 8, wse),
          M(s.value.get(J.key)) ? (O(), F("path", {
            key: 0,
            class: "tavern-atlas-node-glyph",
            d: M(s.value.get(J.key))?.path,
            transform: N(J),
            "fill-rule": M(s.value.get(J.key))?.fillRule
          }, null, 8, Sse)) : ge("", !0),
          g("text", {
            x: J.x + 36,
            y: J.y + 22,
            "text-anchor": "start"
          }, K(s.value.get(J.key)?.name || J.key), 9, Tse),
          g("text", {
            x: J.x + 36,
            y: J.y + 40,
            "text-anchor": "start",
            class: "tavern-atlas-node-meta"
          }, K(b(J.key) || s.value.get(J.key)?.scale), 9, Ese)
        ], 42, bse))), 128))
      ], 8, yse)), g("span", Ase, K(S(c5)), 1)])) : ge("", !0),
      C.value ? (O(), F("aside", Cse, [
        g("header", null, [g("div", null, [g("strong", null, K(a.value?.name || "地点"), 1), g("span", null, K(a.value?.key), 1)]), g("em", null, K(a.value?.status === "visited" ? "已探索" : "已知"), 1)]),
        a.value?.brief ? (O(), F("p", kse, K(a.value.brief), 1)) : ge("", !0),
        g("dl", null, [
          g("div", null, [z[3] || (z[3] = g("dt", null, "层级", -1)), g("dd", null, K(a.value?.scale || "-"), 1)]),
          g("div", null, [z[4] || (z[4] = g("dt", null, "上级", -1)), g("dd", null, K(a.value?.parent ? s.value.get(a.value.parent)?.name || a.value.parent : "顶级地点"), 1)]),
          g("div", null, [z[5] || (z[5] = g("dt", null, "场景图", -1)), g("dd", null, K(x(a.value)), 1)]),
          g("div", null, [z[6] || (z[6] = g("dt", null, "人物", -1)), g("dd", null, K(v.value.length ? v.value.map(p).join("、") : "无"), 1)])
        ]),
        y.value.length ? (O(), F("div", Ise, [(O(!0), F(Je, null, lt(y.value, (J) => (O(), F("span", { key: J.id }, K(J.label || J.kind) + " · " + K(s.value.get(J.from)?.name || J.from) + " → " + K(s.value.get(J.to)?.name || J.to), 1))), 128))])) : ge("", !0),
        d.value ? (O(), F("small", xse, "最近更新：" + K(d.value), 1)) : ge("", !0)
      ])) : ge("", !0)
    ], 64)) : (O(), F("div", mse, [
      z[0] || (z[0] = g("strong", null, "世界地图尚未建立", -1)),
      z[1] || (z[1] = g("p", null, "随着剧情展开，AI 会自动记录你去过的地点，以及地点之间的联系。", -1)),
      g("span", null, "当前场景图：" + K(f.value.get(e.activeMapDocId) || e.activeMapDocId || "main"), 1)
    ]))], 2));
  }
}), Pse = (e, t) => {
  const n = e.__vccOpts || e;
  for (const [r, o] of t) n[r] = o;
  return n;
}, qC = /* @__PURE__ */ Pse(Mse, [["__scopeId", "data-v-d8b608d9"]]), Rse = { class: "tavern-event-panel" }, Nse = {
  key: 0,
  class: "tavern-event-current"
}, $se = { class: "tavern-event-horizon" }, Dse = { class: "tavern-event-done" }, Lse = { class: "tavern-event-hook" }, Ose = {
  key: 1,
  class: "tavern-event-empty"
}, Fse = {
  key: 2,
  class: "tavern-event-section"
}, Bse = { class: "tavern-event-done" }, Use = {
  key: 3,
  class: "tavern-event-section"
}, qse = { class: "tavern-event-done" }, Hse = /* @__PURE__ */ un({
  __name: "TavernEventPanel",
  props: {
    tasks: { default: () => [] },
    enabled: {
      type: Boolean,
      default: !1
    },
    assistantFloor: { default: -1 }
  },
  setup(e) {
    const t = e, n = re(() => (Array.isArray(t.tasks) ? t.tasks : []).filter((d) => d.status !== "abandoned").sort((d, f) => Number(f.updatedOrder) - Number(d.updatedOrder) || Number(f.updatedAt) - Number(d.updatedAt))), r = re(() => n.value.filter((d) => d.status === "active")), o = re(() => n.value.filter((d) => d.status === "completed")), s = re(() => r.value[0] || null), a = re(() => r.value.slice(1, 4)), i = re(() => t.enabled ? Number(t.assistantFloor) < 5 ? "剧情展开后会出现事件线索" : "当前没有足够新鲜的方向" : "事件功能未授权"), l = re(() => t.enabled ? Number(t.assistantFloor) < 5 ? "先让人物、地点和关系沉淀几轮，系统不会过早生成方向。" : "后台没有找到对味的新钩子时会保持空白。" : "开启契约里的织线者后，后台会维护可回滚的事件线索池。");
    function u(d) {
      const f = Number(d.updatedOrder);
      return Number.isFinite(f) && f >= 0 ? `#${f}` : "";
    }
    return (d, f) => (O(), F("section", Rse, [
      s.value ? (O(), F("div", Nse, [
        f[0] || (f[0] = g("span", { class: "tavern-event-kicker" }, "当前目标", -1)),
        g("h3", null, K(s.value.current), 1),
        g("p", $se, " 远景：" + K(s.value.horizon), 1),
        g("p", Dse, " 完成条件：" + K(s.value.doneWhen), 1),
        g("p", Lse, K(s.value.hookForUser), 1),
        g("small", null, K(u(s.value)), 1)
      ])) : (O(), F("div", Ose, [g("strong", null, K(i.value), 1), g("p", null, K(l.value), 1)])),
      a.value.length ? (O(), F("div", Fse, [g("header", null, [f[1] || (f[1] = g("strong", null, "其他线索", -1)), g("span", null, K(a.value.length), 1)]), (O(!0), F(Je, null, lt(a.value, (m) => (O(), F("article", {
        key: m.id,
        class: "tavern-event-card"
      }, [
        g("div", null, [g("strong", null, K(m.current), 1), g("small", null, K(u(m)), 1)]),
        g("p", Bse, " 完成条件：" + K(m.doneWhen), 1),
        g("p", null, K(m.hookForUser), 1)
      ]))), 128))])) : ge("", !0),
      o.value.length ? (O(), F("div", Use, [g("header", null, [f[2] || (f[2] = g("strong", null, "已完成", -1)), g("span", null, K(o.value.length), 1)]), (O(!0), F(Je, null, lt(o.value, (m) => (O(), F("article", {
        key: m.id,
        class: "tavern-event-card is-completed"
      }, [
        g("div", null, [g("strong", null, K(m.current), 1), g("small", null, K(u(m)), 1)]),
        g("p", qse, " 完成条件：" + K(m.doneWhen), 1),
        g("p", null, K(m.horizon), 1)
      ]))), 128))])) : ge("", !0)
    ]));
  }
}), zse = Hse, Yd = [
  0,
  0,
  800,
  600
], Wse = 48, Wh = 160;
function Bo(e, t, n, r) {
  if (![
    e,
    t,
    n,
    r
  ].every((l) => Number.isFinite(l))) return null;
  const o = Math.min(e, n), s = Math.max(e, n), a = Math.min(t, r), i = Math.max(t, r);
  return {
    minX: o,
    minY: a,
    maxX: s,
    maxY: i,
    width: Math.max(0, s - o),
    height: Math.max(0, i - a)
  };
}
function f5(e, t) {
  return e ? t ? Bo(Math.min(e.minX, t.minX), Math.min(e.minY, t.minY), Math.max(e.maxX, t.maxX), Math.max(e.maxY, t.maxY)) : e : t;
}
function h5(e, t) {
  return Bo(e.minX - t, e.minY - t, e.maxX + t, e.maxY + t) || e;
}
function HC(e, t) {
  return t.map(([n, r]) => [e.at[0] + n, e.at[1] + r]);
}
function zC(e) {
  return !Array.isArray(e) || !e.length ? null : e.reduce((t, [n, r]) => f5(t, Bo(n, r, n, r)), null);
}
function Gse(e) {
  return e.rect ? Bo(e.at[0], e.at[1], e.at[0] + e.rect[0], e.at[1] + e.rect[1]) : null;
}
function Vse(e) {
  return typeof e.circle != "number" || e.circle <= 0 ? null : Bo(e.at[0] - e.circle, e.at[1] - e.circle, e.at[0] + e.circle, e.at[1] + e.circle);
}
function Kse(e) {
  if (!e.icon) return null;
  const t = hse(e.icon) / 2;
  return Bo(e.at[0] - t, e.at[1] - t, e.at[0] + t, e.at[1] + t);
}
function Jse(e) {
  if (!e.text) return null;
  const t = Math.max(12, e.text.length * 8.4);
  return Bo(e.at[0] - t / 2, e.at[1] - 14, e.at[0] + t / 2, e.at[1] + 6);
}
function Yse(e) {
  const t = Number(e.style?.width || 2);
  return Math.max(4, Number.isFinite(t) && t > 0 ? t / 2 + 4 : 4);
}
function Xse(e) {
  if (!Array.isArray(e) || e.length !== 4) return [...Yd];
  const t = e.map((n) => Number(n));
  return t.every((n) => Number.isFinite(n)) ? [
    t[0],
    t[1],
    Math.max(1, t[2]),
    Math.max(1, t[3])
  ] : [...Yd];
}
function l0(e) {
  let t = null;
  return e.rect ? t = Gse(e) : typeof e.circle == "number" ? t = Vse(e) : e.path ? t = zC(HC(e, e.path)) : e.curve ? t = zC(HC(e, e.curve)) : e.icon ? t = Kse(e) : e.text && (t = Jse(e)), t ? h5(t, Yse(e)) : null;
}
function Qse(e) {
  return e?.elements?.length ? e.elements.reduce((t, n) => f5(t, l0(n)), null) : null;
}
function Zse(e, t) {
  if (!Number.isFinite(t) || t <= 0) return e;
  const n = Math.max(e.width, 1), r = Math.max(e.height, 1), o = n / r;
  if (Math.abs(o - t) < 0.01) return e;
  if (o > t) {
    const a = (n / t - r) / 2;
    return Bo(e.minX, e.minY - a, e.maxX, e.maxY + a) || e;
  }
  const s = (r * t - n) / 2;
  return Bo(e.minX - s, e.minY, e.maxX + s, e.maxY) || e;
}
function jse(e, t) {
  const n = t >= 1 ? Wh : Wh * t, r = t >= 1 ? Wh / t : Wh;
  let o = e;
  if (o.width < n) {
    const s = (n - o.width) / 2;
    o = Bo(o.minX - s, o.minY, o.maxX + s, o.maxY) || o;
  }
  if (o.height < r) {
    const s = (r - o.height) / 2;
    o = Bo(o.minX, o.minY - s, o.maxX, o.maxY + s) || o;
  }
  return o;
}
function eae(e, t) {
  const n = Qse(e);
  if (!n) return [...Yd];
  const r = jse(Zse(h5(n, Wse), t), t);
  return [
    Number(r.minX.toFixed(2)),
    Number(r.minY.toFixed(2)),
    Number(r.width.toFixed(2)),
    Number(r.height.toFixed(2))
  ];
}
function tae(e) {
  return e?.meta?.viewBox ? Xse(e.meta.viewBox) : eae(e, Yd[2] / Yd[3]);
}
var nae = {
  key: 0,
  class: "tavern-editor-head xb-editor-head tavern-map-head"
}, rae = { class: "tavern-map-title" }, oae = { key: 0 }, sae = { class: "tavern-editor-actions xb-editor-actions tavern-map-actions" }, aae = ["value", "disabled"], iae = ["value"], lae = ["disabled"], uae = ["disabled"], cae = ["disabled"], dae = {
  key: 0,
  class: "tavern-map-compact-controls is-floating"
}, fae = ["value", "disabled"], hae = ["value"], pae = ["viewBox", "aria-label"], mae = { class: "map-fill-layer" }, gae = [
  "d",
  "fill",
  "transform",
  "fill-rule"
], vae = {
  class: "map-line-layer",
  filter: "url(#tavern-map-sketch)"
}, yae = [
  "d",
  "fill",
  "stroke",
  "stroke-width",
  "stroke-dasharray",
  "transform",
  "fill-rule"
], _ae = { class: "map-label-layer" }, bae = [
  "x",
  "y",
  "font-size",
  "fill",
  "text-anchor"
], wae = { class: "map-removed-layer" }, Sae = [
  "d",
  "fill",
  "transform",
  "fill-rule"
], Tae = [
  "d",
  "fill",
  "stroke",
  "stroke-width",
  "stroke-dasharray",
  "transform",
  "fill-rule"
], Eae = [
  "x",
  "y",
  "font-size",
  "fill",
  "text-anchor"
], Aae = ["aria-expanded", "title"], Cae = {
  key: 2,
  class: "tavern-map-timeline-control"
}, kae = {
  key: 2,
  class: "tavern-map-empty"
}, Iae = {
  key: 0,
  class: "tavern-map-compact-controls"
}, xae = ["value", "disabled"], Mae = ["value"], Pae = {
  key: 3,
  class: "tavern-editor-foot xb-editor-foot tavern-map-foot"
}, Rae = { key: 0 }, Nae = { key: 1 }, $ae = { class: "tavern-map-icon-credit" }, Dae = /* @__PURE__ */ un({
  __name: "TavernMapPanel",
  props: {
    documents: { default: () => [] },
    activeDocId: { default: "main" },
    selectedDocId: { default: "" },
    document: {},
    patches: {},
    compact: {
      type: Boolean,
      default: !1
    }
  },
  emits: ["update:selectedDocId"],
  setup(e, { emit: t }) {
    function n(j) {
      return j.find((ce) => !ce.gameIcon && ce.layer !== "label" && !!ce.path) || null;
    }
    const r = e, o = t, s = /* @__PURE__ */ ie(0), a = /* @__PURE__ */ ie("patch"), i = /* @__PURE__ */ ie(0), l = /* @__PURE__ */ ie(""), u = /* @__PURE__ */ ie(null), d = /* @__PURE__ */ ie([0, 0]), f = /* @__PURE__ */ ie(null);
    let m;
    function v(j) {
      return JSON.parse(JSON.stringify(j));
    }
    function y() {
      return yu();
    }
    function T(j) {
      if (!j || typeof j != "object" || Array.isArray(j)) return y();
      const ce = j, ne = y();
      return {
        meta: ce.meta && typeof ce.meta == "object" && !Array.isArray(ce.meta) ? {
          ...ne.meta,
          ...v(ce.meta)
        } : ne.meta,
        elements: Array.isArray(ce.elements) ? v(ce.elements).filter((it) => !!it && typeof it == "object" && !Array.isArray(it) && typeof it.id == "string") : []
      };
    }
    const C = re(() => {
      const j = Array.isArray(r.documents) ? r.documents : [], ce = String(l.value || r.activeDocId || r.document?.docId || "").trim();
      return j.find((ne) => ne.docId === ce) || j.find((ne) => ne.docId === r.activeDocId) || r.document || null;
    }), A = re(() => {
      const j = String(C.value?.docId || "").trim();
      return j && j === String(r.document?.docId || "").trim() ? r.patches : [];
    }), I = re(() => {
      const j = C.value?.data;
      return !j || typeof j != "object" || Array.isArray(j) ? null : T(j);
    }), R = re(() => A.value.at(-1) || null), $ = /* @__PURE__ */ ie(!1), p = re(() => {
      const j = [...A.value].sort((ne, it) => Number(ne.revision || 0) - Number(it.revision || 0));
      let ce = y();
      return j.map((ne, it) => {
        const Xe = Array.isArray(ne.ops) ? ne.ops : [];
        return ce = xF(T(ce), Xe), {
          revision: Number(ne.revision || it + 1),
          document: v(ce),
          patch: ne,
          index: it
        };
      });
    }), b = re(() => {
      if (!p.value.length) return null;
      const j = Math.max(0, Math.min(i.value, p.value.length - 1));
      return p.value[j] || null;
    }), w = re(() => p.value.length > 0 && Number(p.value[0]?.revision || 0) <= 1), E = re(() => a.value === "timeline" && b.value?.patch || R.value), x = re(() => a.value === "timeline" && b.value?.document || I.value), M = re(() => Array.isArray(E.value?.ops) ? E.value?.ops : []), N = re(() => new Set((E.value?.changedIds || []).map((j) => String(j || "").trim()).filter(Boolean))), q = re(() => {
      const j = /* @__PURE__ */ new Map();
      return M.value.forEach((ce) => {
        const ne = String(ce.op || "").trim(), it = ce.element && typeof ce.element == "object" && !Array.isArray(ce.element) ? ce.element : null, Xe = String(ce.id || it?.id || "").trim();
        Xe && (ne === "add" || ne === "modify" || ne === "remove") && j.set(Xe, ne);
      }), j;
    }), z = re(() => M.value.filter((j) => String(j.op || "").trim() === "remove").map((j, ce) => ({
      id: String(j.id || `removed-${ce}`).trim(),
      delayMs: ce * 120
    })).filter((j) => j.id)), J = re(() => (Array.isArray(E.value?.removedElements) ? E.value?.removedElements : []).filter((j) => !!j && typeof j == "object" && !Array.isArray(j) && typeof j.id == "string")), G = re(() => tae(x.value)), oe = re(() => {
      const [j, ce, ne, it] = G.value, [Xe, Lt] = d.value;
      return [
        Number((j + Xe).toFixed(2)),
        Number((ce + Lt).toFixed(2)),
        ne,
        it
      ];
    }), B = re(() => oe.value.join(" ")), X = re(() => String(x.value?.meta?.theme || "parchment")), de = re(() => String(a.value === "timeline" ? x.value?.meta?.name || C.value?.title || "地图" : C.value?.title || x.value?.meta?.name || "地图")), fe = re(() => String(C.value?.digest || "")), ve = re(() => b.value && a.value === "timeline" ? `revision ${b.value.revision}` : C.value ? `revision ${C.value.revision}` : "no map"), Ce = re(() => E.value?.summary || (E.value ? `revision ${E.value.revision}` : "等待空间变化")), Ue = re(() => a.value === "full" ? "完整重绘" : a.value === "timeline" ? `回合 ${W.value}` : "地图更新"), le = re(() => fe.value.split(`
`).map((j) => j.trim()).filter(Boolean).slice(0, 4)), me = re(() => x.value?.elements.length || 0), We = re(() => A.value.length), H = re(() => Array.isArray(r.documents) && r.documents.length ? r.documents : r.document ? [{
      ...r.document,
      active: !0
    }] : []), W = re(() => b.value ? `${b.value.index + 1} / ${p.value.length}` : "0 / 0"), se = re(() => CF(x.value)), ke = re(() => !!E.value);
    gt(() => r.document?.revision, () => {
      a.value = "patch", Pe(), i.value = Math.max(0, p.value.length - 1), s.value += 1, $.value = !1, Le();
    }), gt(() => [
      r.selectedDocId,
      r.activeDocId,
      r.document?.docId,
      r.documents.length
    ], () => {
      const j = String(r.selectedDocId || l.value || r.activeDocId || r.document?.docId || "").trim(), ce = H.value.some((ne) => ne.docId === j) ? j : String(r.activeDocId || r.document?.docId || H.value[0]?.docId || "").trim();
      l.value = ce, ce && ce !== r.selectedDocId && o("update:selectedDocId", ce);
    }, { immediate: !0 }), gt(() => A.value.length, () => {
      i.value >= p.value.length && (i.value = Math.max(0, p.value.length - 1)), a.value === "timeline" && !w.value && (a.value = "patch", Pe()), $.value = !1, Le();
    }), cu(() => {
      Pe();
    });
    function Pe() {
      m !== void 0 && (window.clearTimeout(m), m = void 0);
    }
    function Le() {
      d.value = [0, 0], f.value = null;
    }
    function Y(j, ce = [0, 0]) {
      if (!Array.isArray(j) || j.length < 2) return ce;
      const ne = Number(j[0]), it = Number(j[1]);
      return Number.isFinite(ne) && Number.isFinite(it) ? [ne, it] : ce;
    }
    function te(j, ce) {
      return ce.map(([ne, it]) => [j.at[0] + ne, j.at[1] + it]);
    }
    function he(j, ce = !1) {
      if (!Array.isArray(j) || j.length < 2) return "";
      const ne = [`M ${j[0][0]} ${j[0][1]}`, ...j.slice(1).map(([it, Xe]) => `L ${it} ${Xe}`)];
      return ce && ne.push("Z"), ne.join(" ");
    }
    function we(j, ce = !1) {
      if (!Array.isArray(j) || j.length < 2) return "";
      if (j.length === 2) return he(j, ce);
      const ne = 0.3, it = [`M ${j[0][0]} ${j[0][1]}`];
      for (let Xe = 0; Xe < j.length - 1; Xe += 1) {
        const Lt = j[Math.max(Xe - 1, 0)], qt = j[Xe], Ve = j[Xe + 1], He = j[Math.min(Xe + 2, j.length - 1)];
        it.push(`C ${qt[0] + (Ve[0] - Lt[0]) * ne} ${qt[1] + (Ve[1] - Lt[1]) * ne}, ${Ve[0] - (He[0] - qt[0]) * ne} ${Ve[1] - (He[1] - qt[1]) * ne}, ${Ve[0]} ${Ve[1]}`);
      }
      return ce && it.push("Z"), it.join(" ");
    }
    function Te(j) {
      const [ce, ne] = j.at, [it, Xe] = Y(j.rect, [10, 10]);
      return `M ${ce} ${ne} L ${ce + it} ${ne} L ${ce + it} ${ne + Xe} L ${ce} ${ne + Xe} Z`;
    }
    function Ie(j) {
      const [ce, ne] = j.at, it = Number(j.circle || 8), Xe = Number.isFinite(it) && it > 0 ? it : 8;
      return `M ${ce - Xe} ${ne} A ${Xe} ${Xe} 0 1 0 ${ce + Xe} ${ne} A ${Xe} ${Xe} 0 1 0 ${ce - Xe} ${ne}`;
    }
    function Ne(j) {
      const [ce, ne] = j.at, it = 10, Xe = it / 2, Lt = String(j.icon || "+"), qt = Bm(Lt);
      return qt ? qt.path : Lt === "x" ? `M ${ce - Xe} ${ne - Xe} L ${ce + Xe} ${ne + Xe} M ${ce + Xe} ${ne - Xe} L ${ce - Xe} ${ne + Xe}` : Lt === "o" ? `M ${ce - Xe} ${ne} A ${Xe} ${Xe} 0 1 0 ${ce + Xe} ${ne} A ${Xe} ${Xe} 0 1 0 ${ce - Xe} ${ne}` : Lt === "arrow-n" ? `M ${ce} ${ne + it} L ${ce} ${ne - it} M ${ce - Xe} ${ne - Xe} L ${ce} ${ne - it} L ${ce + Xe} ${ne - Xe}` : Lt === "arrow-s" ? `M ${ce} ${ne - it} L ${ce} ${ne + it} M ${ce - Xe} ${ne + Xe} L ${ce} ${ne + it} L ${ce + Xe} ${ne + Xe}` : Lt === "arrow-e" ? `M ${ce - it} ${ne} L ${ce + it} ${ne} M ${ce + Xe} ${ne - Xe} L ${ce + it} ${ne} L ${ce + Xe} ${ne + Xe}` : Lt === "arrow-w" ? `M ${ce + it} ${ne} L ${ce - it} ${ne} M ${ce - Xe} ${ne - Xe} L ${ce - it} ${ne} L ${ce - Xe} ${ne + Xe}` : Lt === "stairs-up" ? `M ${ce - 9} ${ne + 7} H ${ce - 3} V ${ne + 1} H ${ce + 3} V ${ne - 5} H ${ce + 9} M ${ce + 3} ${ne - 5} L ${ce + 3} ${ne - 10} M ${ce + 3} ${ne - 10} L ${ce} ${ne - 7} M ${ce + 3} ${ne - 10} L ${ce + 6} ${ne - 7}` : Lt === "stairs-down" ? `M ${ce - 9} ${ne - 7} H ${ce - 3} V ${ne - 1} H ${ce + 3} V ${ne + 5} H ${ce + 9} M ${ce + 3} ${ne + 5} L ${ce + 3} ${ne + 10} M ${ce + 3} ${ne + 10} L ${ce} ${ne + 7} M ${ce + 3} ${ne + 10} L ${ce + 6} ${ne + 7}` : `M ${ce - Xe} ${ne} L ${ce + Xe} ${ne} M ${ce} ${ne - Xe} L ${ce} ${ne + Xe}`;
    }
    function Oe(j) {
      return j.rect ? Te(j) : typeof j.circle == "number" ? Ie(j) : j.path ? he(te(j, j.path), j.closed === !0) : j.curve ? we(te(j, j.curve), j.closed === !0) : j.icon ? Ne(j) : "";
    }
    function L(j) {
      if (j.style?.color) return String(j.style.color);
      const ce = {
        wall: "#2f2418",
        road: "#9a8253",
        water: "#2d7490",
        terrain: "#55734b",
        furniture: "#765d37",
        door: "#a5652b",
        danger: "#b94035",
        marker: "#b94035",
        actor: "#b94035",
        label: "#2b2118",
        grid: "#9f987f",
        magic: "#76539a",
        secret: "#68625b"
      };
      return ce[String(j.cat || "wall")] || ce.wall;
    }
    function P(j) {
      return j.fill ? String(j.fill) : j.rect || typeof j.circle == "number" || (j.path || j.curve) && j.closed === !0 ? {
        water: "rgba(63, 136, 167, 0.24)",
        terrain: "rgba(87, 121, 70, 0.18)",
        danger: "rgba(180, 64, 54, 0.15)",
        magic: "rgba(126, 83, 154, 0.16)",
        secret: "rgba(63, 56, 48, 0.10)"
      }[String(j.cat || "")] || "rgba(92, 70, 36, 0.08)" : "none";
    }
    function Q(j) {
      const ce = Number(j.style?.width || 2);
      return Number.isFinite(ce) && ce > 0 ? ce : 2;
    }
    function V(j) {
      return String(j.style?.dash || "");
    }
    function ae(j, ce) {
      if (typeof j.circle == "number") return Math.max(32, Math.PI * 2 * j.circle);
      if (j.rect) return Math.max(40, Math.abs(j.rect[0]) * 2 + Math.abs(j.rect[1]) * 2);
      const ne = ce.match(/-?\d+(?:\.\d+)?/g)?.map(Number) || [];
      let it = 0;
      for (let Xe = 0; Xe + 3 < ne.length; Xe += 2) {
        const Lt = ne[Xe + 2] - ne[Xe], qt = ne[Xe + 3] - ne[Xe + 1];
        it += Math.hypot(Lt, qt);
      }
      return Math.max(28, Math.min(2400, it || 180));
    }
    function Se(j) {
      return a.value === "full" ? "add" : q.value.get(j.id) || (N.value.has(j.id) ? "modify" : "stable");
    }
    function Me(j, ce) {
      if (!j) return ce;
      if (!ce) return j;
      const ne = Math.min(j.minX, ce.minX), it = Math.min(j.minY, ce.minY), Xe = Math.max(j.maxX, ce.maxX), Lt = Math.max(j.maxY, ce.maxY);
      return {
        minX: ne,
        minY: it,
        maxX: Xe,
        maxY: Lt,
        width: Math.max(0, Xe - ne),
        height: Math.max(0, Lt - it)
      };
    }
    function Ke() {
      return (x.value?.elements || []).reduce((j, ce) => ce.text || String(ce.cat || "") === "label" ? j : Me(j, l0(ce)), null);
    }
    function qe(j) {
      if (!j.text || String(j.cat || "") !== "label") return j.at;
      const ce = String(j.id || "").trim().toLowerCase();
      if (ce.startsWith("__label__")) return j.at;
      const [ne, it, Xe, Lt] = oe.value, qt = it + Math.max(72, Lt * 0.26);
      if (![
        "label",
        "room-label",
        "scene-label",
        "place-label",
        "map-label"
      ].includes(ce) || j.at[1] >= qt) return j.at;
      const Ve = Ke(), He = (Ve?.minX ?? ne) + 24, Re = (Ve?.maxX ?? ne + Xe) - 24, _t = Ve ? Ve.minY + Math.min(90, Math.max(42, Ve.height * 0.22)) : qt, wn = Math.max(qt, _t);
      return [Math.min(Re, Math.max(He, j.at[0])), Math.min(it + Lt - 24, wn)];
    }
    function Be(j) {
      if (!j.text || String(j.cat || "") !== "label") return 14;
      const ce = String(j.id || "").trim().toLowerCase();
      return ce.startsWith("__label__") ? 14 : [
        "label",
        "room-label",
        "scene-label",
        "place-label",
        "map-label"
      ].includes(ce) ? 13 : 14;
    }
    function Ee(j, ce, ne) {
      if (!l0(j)) return [];
      const it = ne || Se(j), Xe = ne === "remove" ? ce * 110 : a.value === "full" ? ce * 115 : it === "stable" ? 0 : ce * 30;
      if (j.text) {
        const [so, Pr] = qe(j);
        return [{
          element: j,
          id: `${j.id}-label`,
          layer: "label",
          path: "",
          color: ne === "remove" ? "#b94035" : L(j),
          fill: "none",
          strokeWidth: 0,
          dash: "",
          delayMs: Xe,
          durationMs: 900,
          length: 60,
          opKind: it,
          text: j.text,
          x: so,
          y: Pr,
          fontSize: Be(j),
          anchor: "middle",
          transform: "",
          fillRule: "nonzero",
          gameIcon: !1
        }];
      }
      const Lt = Oe(j);
      if (!Lt) return [];
      const qt = j.icon ? Bm(j.icon) : null, Ve = qt ? 120 : ae(j, Lt), He = ne === "remove" ? 1050 : Math.max(680, Math.min(2200, Ve * 4.2)), Re = ne === "remove" ? "#b94035" : L(j), _t = ne === "remove" ? "rgba(185, 64, 53, 0.16)" : P(j), wn = [];
      return qt ? (wn.push({
        element: j,
        id: `${j.id}-glyph`,
        layer: "line",
        path: Lt,
        color: Re,
        fill: Re,
        strokeWidth: 0,
        dash: "",
        delayMs: Xe,
        durationMs: He,
        length: Ve,
        opKind: it,
        text: "",
        x: 0,
        y: 0,
        fontSize: 0,
        anchor: "middle",
        transform: d5(j.at[0], j.at[1]),
        fillRule: qt.fillRule,
        gameIcon: !0
      }), wn) : (_t !== "none" && wn.push({
        element: j,
        id: `${j.id}-fill`,
        layer: "fill",
        path: Lt,
        color: Re,
        fill: _t,
        strokeWidth: 0,
        dash: "",
        delayMs: Xe,
        durationMs: He,
        length: Ve,
        opKind: it,
        text: "",
        x: 0,
        y: 0,
        fontSize: 0,
        anchor: "middle",
        transform: "",
        fillRule: "nonzero",
        gameIcon: !1
      }), wn.push({
        element: j,
        id: `${j.id}-line`,
        layer: "line",
        path: Lt,
        color: Re,
        fill: "none",
        strokeWidth: Q(j),
        dash: V(j),
        delayMs: Xe,
        durationMs: He,
        length: Ve,
        opKind: it,
        text: "",
        x: 0,
        y: 0,
        fontSize: 0,
        anchor: "middle",
        transform: "",
        fillRule: "nonzero",
        gameIcon: !1
      }), wn);
    }
    const Ye = re(() => (x.value?.elements || []).flatMap((j, ce) => Ee(j, ce))), je = re(() => Ye.value.filter((j) => j.layer === "fill")), It = re(() => Ye.value.filter((j) => j.layer === "line")), Ht = re(() => Ye.value.filter((j) => j.layer === "label")), At = re(() => J.value.flatMap((j, ce) => Ee(j, ce, "remove"))), Mt = re(() => At.value.filter((j) => j.layer === "fill")), Ut = re(() => At.value.filter((j) => j.layer === "line")), yt = re(() => At.value.filter((j) => j.layer === "label")), et = re(() => Ye.value.filter((j) => a.value === "full" || j.opKind !== "stable")), xe = re(() => {
      const j = et.value;
      return j.length ? Math.max(...j.map((ce) => ce.delayMs + ce.durationMs)) + 360 : 1200;
    }), Ze = re(() => ({ "--map-progress-duration": `${xe.value}ms` })), zt = re(() => {
      const j = n(et.value);
      return j ? {
        offsetPath: `path("${j.path}")`,
        animationDelay: `${j.delayMs}ms`,
        animationDuration: `${j.durationMs}ms`
      } : { display: "none" };
    });
    function jt() {
      Pe(), !(a.value !== "timeline" || !w.value || p.value.length <= 1) && Qt(() => {
        m = window.setTimeout(() => {
          a.value === "timeline" && (i.value = i.value + 1 >= p.value.length ? 0 : i.value + 1, s.value += 1, jt());
        }, xe.value + 420);
      });
    }
    function Cn(j) {
      return {
        "--map-delay": `${j.delayMs}ms`,
        "--map-duration": `${j.durationMs}ms`,
        "--map-length": `${j.length}`
      };
    }
    function Gn(j) {
      return [
        "map-element",
        `map-${j.layer}`,
        `cat-${j.element.cat || "wall"}`,
        `op-${j.opKind}`,
        {
          "is-animated": a.value === "full" || j.opKind !== "stable",
          "is-latest": N.value.has(j.element.id),
          "is-game-icon": j.gameIcon
        }
      ];
    }
    function er() {
      Pe(), a.value = "patch", s.value += 1, Le();
    }
    function wt() {
      Pe(), a.value = "full", s.value += 1, Le();
    }
    function nn() {
      w.value && (a.value = "timeline", i.value = 0, s.value += 1, Le(), jt());
    }
    function Ft(j) {
      if (Pe(), a.value = "timeline", !w.value) return;
      const ce = p.value.length;
      ce && (i.value = (i.value + j + ce) % ce, s.value += 1, Le());
    }
    function yn() {
      $.value = !$.value;
    }
    function ot() {
      $.value = !1;
    }
    function ye(j) {
      const ce = j.target instanceof HTMLSelectElement ? j.target : null;
      l.value = String(ce?.value || "").trim(), o("update:selectedDocId", l.value), a.value = "patch", Pe(), s.value += 1, Le();
    }
    function dt(j) {
      const ce = j.currentTarget instanceof SVGSVGElement ? j.currentTarget : u.value;
      if (!ce || j.button !== 0) return;
      const ne = ce.getBoundingClientRect();
      if (ne.width <= 0 || ne.height <= 0) return;
      const [, , it, Xe] = oe.value, [Lt, qt] = d.value;
      f.value = {
        pointerId: j.pointerId,
        startClientX: j.clientX,
        startClientY: j.clientY,
        startPanX: Lt,
        startPanY: qt,
        viewWidth: it,
        viewHeight: Xe,
        clientWidth: ne.width,
        clientHeight: ne.height
      }, ce.setPointerCapture(j.pointerId);
    }
    function dn(j) {
      const ce = f.value;
      if (!ce || ce.pointerId !== j.pointerId) return;
      const ne = -((j.clientX - ce.startClientX) / ce.clientWidth) * ce.viewWidth, it = -((j.clientY - ce.startClientY) / ce.clientHeight) * ce.viewHeight;
      d.value = [Number((ce.startPanX + ne).toFixed(2)), Number((ce.startPanY + it).toFixed(2))];
    }
    function Pt(j) {
      const ce = f.value;
      if (!ce || ce.pointerId !== j.pointerId) return;
      const ne = j.currentTarget instanceof SVGSVGElement ? j.currentTarget : u.value;
      ne?.hasPointerCapture(j.pointerId) && ne.releasePointerCapture(j.pointerId), f.value = null;
    }
    return (j, ce) => (O(), F("aside", { class: ze(["tavern-map-panel xb-editor", { "is-compact": e.compact }]) }, [
      e.compact ? ge("", !0) : (O(), F("header", nae, [g("div", rae, [
        ce[2] || (ce[2] = g("span", { class: "tavern-map-kicker" }, "LIVE CARTOGRAPHY", -1)),
        g("strong", null, K(de.value), 1),
        C.value ? (O(), F("span", oae, K(ve.value) + " · " + K(me.value) + " elements", 1)) : ge("", !0)
      ]), g("div", sae, [
        g("select", {
          class: "tavern-map-select",
          value: C.value?.docId || "",
          disabled: H.value.length <= 1,
          "aria-label": "选择地图",
          onChange: ye
        }, [(O(!0), F(Je, null, lt(H.value, (ne) => (O(), F("option", {
          key: ne.docId,
          value: ne.docId
        }, K(ne.active ? "当前 · " : "") + K(ne.title || ne.docId), 9, iae))), 128))], 40, aae),
        g("button", {
          type: "button",
          class: ze(["tavern-editor-mode-button", { active: a.value === "patch" }]),
          disabled: !R.value,
          onClick: er
        }, " 最近回合 ", 10, lae),
        g("button", {
          type: "button",
          class: ze(["tavern-editor-mode-button", { active: a.value === "full" }]),
          disabled: !se.value,
          onClick: wt
        }, " 完整重绘 ", 10, uae),
        g("button", {
          type: "button",
          class: ze(["tavern-editor-mode-button", { active: a.value === "timeline" }]),
          disabled: !w.value,
          onClick: nn
        }, " 回合回放 ", 10, cae)
      ])])),
      se.value ? (O(), F("div", {
        key: 1,
        class: ze(["tavern-map-canvas", [
          `theme-${X.value}`,
          `mode-${a.value}`,
          { "is-panning": f.value }
        ]])
      }, [
        e.compact ? (O(), F("div", dae, [g("select", {
          class: "tavern-map-select",
          value: C.value?.docId || "",
          disabled: H.value.length <= 1,
          "aria-label": "选择地图",
          onChange: ye
        }, [(O(!0), F(Je, null, lt(H.value, (ne) => (O(), F("option", {
          key: ne.docId,
          value: ne.docId
        }, K(ne.active ? "当前 · " : "") + K(ne.title || ne.docId), 9, hae))), 128))], 40, fae)])) : ge("", !0),
        (O(), F("svg", {
          ref_key: "mapSvgRef",
          ref: u,
          key: s.value,
          viewBox: B.value,
          preserveAspectRatio: "xMidYMid meet",
          role: "img",
          "aria-label": de.value,
          onPointerdown: dt,
          onPointermove: dn,
          onPointerup: Pt,
          onPointercancel: Pt,
          onDblclick: Le
        }, [
          ce[3] || (ce[3] = ku('<defs><filter id="tavern-map-sketch" x="-5%" y="-5%" width="110%" height="110%"><feTurbulence type="fractalNoise" baseFrequency="0.025" numOctaves="3" seed="7" result="noise"></feTurbulence><feDisplacementMap in="SourceGraphic" in2="noise" scale="0.65"></feDisplacementMap></filter><filter id="tavern-map-glow" x="-30%" y="-30%" width="160%" height="160%"><feGaussianBlur stdDeviation="3" result="blur"></feGaussianBlur><feMerge><feMergeNode in="blur"></feMergeNode><feMergeNode in="SourceGraphic"></feMergeNode></feMerge></filter></defs>', 1)),
          g("g", mae, [(O(!0), F(Je, null, lt(je.value, (ne) => (O(), F("path", {
            key: ne.id,
            d: ne.path,
            fill: ne.fill,
            transform: ne.transform,
            "fill-rule": ne.fillRule,
            class: ze(Gn(ne)),
            style: Jr(Cn(ne))
          }, null, 14, gae))), 128))]),
          g("g", vae, [(O(!0), F(Je, null, lt(It.value, (ne) => (O(), F("path", {
            key: ne.id,
            d: ne.path,
            fill: ne.fill,
            stroke: ne.color,
            "stroke-width": ne.strokeWidth,
            "stroke-dasharray": ne.dash,
            transform: ne.transform,
            "fill-rule": ne.fillRule,
            "stroke-linecap": "round",
            "stroke-linejoin": "round",
            class: ze(Gn(ne)),
            style: Jr(Cn(ne))
          }, null, 14, yae))), 128))]),
          g("g", _ae, [(O(!0), F(Je, null, lt(Ht.value, (ne) => (O(), F("text", {
            key: ne.id,
            x: ne.x,
            y: ne.y,
            "font-size": ne.fontSize,
            fill: ne.color,
            "text-anchor": ne.anchor,
            class: ze(Gn(ne)),
            style: Jr(Cn(ne))
          }, K(ne.text), 15, bae))), 128))]),
          g("g", wae, [
            (O(!0), F(Je, null, lt(Mt.value, (ne) => (O(), F("path", {
              key: `removed-fill-${ne.id}`,
              d: ne.path,
              fill: ne.fill,
              transform: ne.transform,
              "fill-rule": ne.fillRule,
              class: ze(Gn(ne)),
              style: Jr(Cn(ne))
            }, null, 14, Sae))), 128)),
            (O(!0), F(Je, null, lt(Ut.value, (ne) => (O(), F("path", {
              key: `removed-line-${ne.id}`,
              d: ne.path,
              fill: ne.fill,
              stroke: ne.color,
              "stroke-width": ne.strokeWidth,
              "stroke-dasharray": ne.dash,
              transform: ne.transform,
              "fill-rule": ne.fillRule,
              "stroke-linecap": "round",
              "stroke-linejoin": "round",
              class: ze(Gn(ne)),
              style: Jr(Cn(ne))
            }, null, 14, Tae))), 128)),
            (O(!0), F(Je, null, lt(yt.value, (ne) => (O(), F("text", {
              key: `removed-label-${ne.id}`,
              x: ne.x,
              y: ne.y,
              "font-size": ne.fontSize,
              fill: ne.color,
              "text-anchor": ne.anchor,
              class: ze(Gn(ne)),
              style: Jr(Cn(ne))
            }, K(ne.text), 15, Eae))), 128))
          ])
        ], 40, pae)),
        (O(), F("div", {
          key: `pen-${s.value}`,
          class: "tavern-map-pen",
          style: Jr(zt.value)
        }, null, 4)),
        (O(), F("div", {
          key: `progress-${s.value}`,
          class: "tavern-map-progress",
          style: Jr(Ze.value)
        }, null, 4)),
        ke.value ? (O(), F("div", {
          key: 1,
          class: ze(["tavern-map-badge-shell", { "is-open": $.value }])
        }, [g("button", {
          type: "button",
          class: "tavern-map-badge-toggle",
          "aria-expanded": $.value,
          title: $.value ? "收起地图更新提示" : "显示地图更新提示",
          "aria-label": "切换地图更新提示",
          onClick: yn
        }, " ! ", 8, Aae), $.value ? (O(), F("button", {
          key: 0,
          type: "button",
          class: "tavern-map-badge",
          title: "收起地图更新提示",
          onClick: ot
        }, [g("span", null, K(Ue.value), 1), g("strong", null, K(Ce.value), 1)])) : ge("", !0)], 2)) : ge("", !0),
        a.value === "timeline" && w.value && p.value.length > 1 ? (O(), F("div", Cae, [g("button", {
          type: "button",
          onClick: ce[0] || (ce[0] = (ne) => Ft(-1))
        }, " 上一回合 "), g("button", {
          type: "button",
          onClick: ce[1] || (ce[1] = (ne) => Ft(1))
        }, " 下一回合 ")])) : ge("", !0),
        z.value.length ? (O(), F("div", {
          key: `removed-${s.value}`,
          class: "tavern-map-removed-stack"
        }, [(O(!0), F(Je, null, lt(z.value, (ne) => (O(), F("span", {
          key: ne.id,
          style: Jr({ animationDelay: `${ne.delayMs}ms` })
        }, " - " + K(ne.id), 5))), 128))])) : ge("", !0)
      ], 2)) : (O(), F("div", kae, [
        e.compact ? (O(), F("div", Iae, [g("select", {
          class: "tavern-map-select",
          value: C.value?.docId || "",
          disabled: H.value.length <= 1,
          "aria-label": "选择地图",
          onChange: ye
        }, [(O(!0), F(Je, null, lt(H.value, (ne) => (O(), F("option", {
          key: ne.docId,
          value: ne.docId
        }, K(ne.active ? "当前 · " : "") + K(ne.title || ne.docId), 9, Mae))), 128))], 40, xae)])) : ge("", !0),
        ce[4] || (ce[4] = g("strong", null, "地图待初始化", -1)),
        ce[5] || (ce[5] = g("span", null, "当前会话已备好种子地图；剧情出现明确空间变化后，后台会激活并维护它。", -1))
      ])),
      e.compact ? ge("", !0) : (O(), F("footer", Pae, [
        g("span", null, K(We.value) + " patches", 1),
        le.value.length ? (O(), F("span", Rae, K(le.value[0]), 1)) : ge("", !0),
        le.value[1] ? (O(), F("span", Nae, K(le.value[1]), 1)) : ge("", !0),
        g("span", $ae, K(S(c5)), 1)
      ]))
    ], 2));
  }
}), Lae = Dae, Oae = { class: "tavern-memory-editor xb-editor" }, Fae = { class: "tavern-editor-head xb-editor-head" }, Bae = { class: "tavern-editor-actions xb-editor-actions" }, Uae = ["disabled"], qae = ["disabled"], Hae = ["disabled"], zae = ["disabled"], Wae = { class: "tavern-editor-body xb-editor-body" }, Gae = ["data-markdown-signature", "innerHTML"], Vae = ["value"], Kae = {
  key: 2,
  class: "tavern-editor-empty"
}, Jae = { class: "tavern-editor-foot xb-editor-foot" }, Yae = { key: 0 }, Xae = { key: 1 }, Qae = { key: 2 }, Zae = { key: 3 }, jae = /* @__PURE__ */ un({
  __name: "TavernMemoryEditor",
  props: {
    documentAvailable: { type: Boolean },
    readOnly: { type: Boolean },
    dirty: { type: Boolean },
    mode: {},
    draft: {},
    previewHtml: {},
    previewSignature: {},
    status: {},
    hasSelectedFile: { type: Boolean },
    loadedPath: {},
    fileMeta: {}
  },
  emits: [
    "update:draft",
    "enter-edit",
    "preview",
    "discard",
    "save"
  ],
  setup(e, { emit: t }) {
    const n = t;
    function r(o) {
      n("update:draft", o.target.value);
    }
    return (o, s) => (O(), F("aside", Oae, [
      g("header", Fae, [g("div", Bae, [
        g("button", {
          type: "button",
          class: ze(["tavern-editor-mode-button", { active: e.mode === "edit" }]),
          disabled: !e.documentAvailable || e.readOnly,
          onClick: s[0] || (s[0] = (a) => o.$emit("enter-edit"))
        }, " 编辑 ", 10, Uae),
        g("button", {
          type: "button",
          class: ze(["tavern-editor-mode-button", { active: e.mode === "preview" }]),
          disabled: !e.documentAvailable,
          onClick: s[1] || (s[1] = (a) => o.$emit("preview"))
        }, " 阅读 ", 10, qae),
        e.dirty ? (O(), F("button", {
          key: 0,
          type: "button",
          class: "tavern-editor-discard-button",
          disabled: !e.documentAvailable,
          onClick: s[2] || (s[2] = (a) => o.$emit("discard"))
        }, " 放弃 ", 8, Hae)) : ge("", !0),
        e.dirty ? (O(), F("button", {
          key: 1,
          type: "button",
          class: "tavern-editor-save-button",
          disabled: !e.hasSelectedFile || !e.dirty || e.readOnly,
          onClick: s[3] || (s[3] = (a) => o.$emit("save"))
        }, " 保存 ", 8, zae)) : ge("", !0)
      ])]),
      g("div", Wae, [e.documentAvailable && e.mode === "preview" ? (O(), F("div", {
        key: 0,
        class: "tavern-editor-preview xb-tavern-markdown",
        "data-markdown-signature": e.previewSignature,
        innerHTML: e.previewHtml
      }, null, 8, Gae)) : e.documentAvailable ? (O(), F("textarea", {
        key: 1,
        value: e.draft,
        spellcheck: "false",
        "data-memory-editor-textarea": "true",
        onInput: r
      }, null, 40, Vae)) : (O(), F("div", Kae, " 选择一份记忆档案。 "))]),
      g("footer", Jae, [
        e.readOnly || e.dirty ? (O(), F("span", Yae, K(e.readOnly ? "只读" : "有未保存修改"), 1)) : ge("", !0),
        e.status ? (O(), F("span", Xae, K(e.status), 1)) : ge("", !0),
        !e.hasSelectedFile && e.loadedPath ? (O(), F("span", Qae, "原档案暂不可用")) : ge("", !0),
        e.hasSelectedFile ? (O(), F("span", Zae, K(e.fileMeta), 1)) : ge("", !0)
      ])
    ]));
  }
}), eie = jae, tie = { class: "tavern-workspace-panel" }, nie = { class: "tavern-workspace-tabs" }, rie = {
  key: 0,
  class: "tavern-state-panel"
}, oie = {
  class: "tavern-state-inline-switcher",
  role: "tablist",
  "aria-label": "地图视图"
}, sie = ["aria-selected"], aie = ["aria-selected"], iie = {
  key: 2,
  class: "tavern-map-current-empty"
}, lie = {
  key: 1,
  class: "tavern-map-info-body"
}, uie = { class: "tavern-map-info-head" }, cie = { class: "tavern-map-info-grid" }, die = {
  key: 0,
  class: "tavern-map-info-line"
}, fie = {
  key: 1,
  class: "tavern-map-info-digest"
}, hie = {
  key: 2,
  class: "tavern-map-info-empty"
}, pie = { class: "tavern-memory-directory" }, mie = { class: "tavern-memory-directory-head" }, gie = {
  key: 0,
  class: "memory-search tavern-memory-search"
}, vie = {
  key: 1,
  class: "memory-directory-list xb-files"
}, yie = { class: "memory-file-group-title" }, _ie = { class: "memory-file-tree" }, bie = ["onClick"], wie = { class: "memory-file-main" }, Sie = ["onClick"], Tie = { class: "memory-file-main" }, Eie = {
  key: 0,
  class: "muted compact"
}, Aie = {
  key: 2,
  class: "muted compact"
}, Cie = ["disabled"], kie = /* @__PURE__ */ un({
  __name: "TavernWorkspacePanel",
  props: { mobileMemoryDirectoryOpen: { type: Boolean } },
  emits: [
    "close",
    "close-memory-directory",
    "toggle-memory-directory"
  ],
  setup(e, { emit: t }) {
    const n = t, r = s5(), o = a5(), { chatWorkspacePanel: s } = o, { activeMemoryFiles: a, discardMemoryDraft: i, enterMemoryEditMode: l, expandMemoryFileGroup: u, formatMemoryFileMeta: d, MEMORY_FILE_BATCH_SIZE: f, MEMORY_TURN_BATCH_SIZE: m, memoryDirectoryGroups: v, memoryEditorDirty: y, memoryEditorDocumentAvailable: T, memoryEditorDraft: C, memoryEditorLoadedPath: A, memoryEditorMode: I, memoryEditorReadOnly: R, memoryEditorStatus: $, memoryFileDisplayName: p, memoryFiles: b, memoryFileSearchText: w, markdownSignature: E, previewMemoryDraft: x, renderChatMarkdown: M, saveSelectedMemoryFile: N, selectedMemoryFileEntry: q, selectedMemoryFilePath: z, selectMemoryFile: J } = r, { activeMapDocId: G, atlasActiveLocationKey: oe, atlasStateDocument: B, atlasStatePatches: X, currentAssistantFloor: de, mapStateDocuments: fe, mapStateDocument: ve, mapStatePatches: Ce, sessionContract: Ue, tavernTasks: le } = o, me = /* @__PURE__ */ ie("scene"), We = /* @__PURE__ */ ie(""), H = /* @__PURE__ */ ie(!1), W = re(() => {
      const Be = B.value?.data, Ee = Be && typeof Be == "object" && !Array.isArray(Be) ? Be : {};
      return {
        version: 1,
        activeLocationKey: String(Ee.activeLocationKey || oe.value || "").trim() || void 0,
        locations: Array.isArray(Ee.locations) ? Ee.locations : [],
        links: Array.isArray(Ee.links) ? Ee.links : [],
        actors: Array.isArray(Ee.actors) ? Ee.actors : []
      };
    }), se = re(() => {
      const Be = W.value.activeLocationKey || oe.value || "";
      return W.value.locations.find((Ee) => Ee.key === Be) || null;
    }), ke = re(() => String(se.value?.mapDocId || "").trim()), Pe = re({
      get() {
        return H.value ? String(We.value || "").trim() : W.value.activeLocationKey ? ke.value : String(G.value || "main").trim();
      },
      set(Be) {
        We.value = String(Be || "").trim(), H.value = !!We.value;
      }
    }), Le = re(() => {
      const Be = Array.isArray(fe.value) ? fe.value : [], Ee = String(Pe.value || "").trim();
      return Ee && Be.find((Ye) => Ye.docId === Ee) || null;
    }), Y = re(() => {
      const Be = Le.value?.data;
      if (!Be || typeof Be != "object" || Array.isArray(Be)) return null;
      const Ee = Be, Ye = yu();
      return {
        meta: Ee.meta && typeof Ee.meta == "object" && !Array.isArray(Ee.meta) ? {
          ...Ye.meta,
          ...Ee.meta
        } : Ye.meta,
        elements: Array.isArray(Ee.elements) ? Ee.elements.filter((je) => !!je && typeof je == "object" && !Array.isArray(je) && typeof je.id == "string") : []
      };
    }), te = re(() => Y.value?.elements || []), he = re(() => String(Y.value?.meta?.name || Le.value?.title || Le.value?.docId || "地图")), we = re(() => String(Le.value?.docId || "") === String(G.value || "main")), Te = re(() => we.value ? Ce.value : []), Ie = re(() => !H.value && !!W.value.activeLocationKey && !ke.value), Ne = re(() => String(Le.value?.digest || "").split(`
`).map((Be) => Be.trim()).filter(Boolean).slice(0, 3)), Oe = re(() => te.value.filter((Be) => Be.cat === "actor").map((Be) => String(Be.text || Be.id || "").trim()).filter(Boolean).slice(0, 6)), L = re(() => {
      const Be = te.value, Ee = (Ye) => Be.filter((je) => je.cat === Ye).length;
      return [
        {
          label: "元素",
          value: String(Be.length)
        },
        {
          label: "人物",
          value: String(Ee("actor"))
        },
        {
          label: "出入口",
          value: String(Ee("door"))
        },
        {
          label: "标注",
          value: String(Ee("label") + Ee("marker"))
        }
      ];
    });
    gt([ke, fe], () => {
      const Be = Array.isArray(fe.value) ? fe.value : [], Ee = String(We.value || "").trim();
      Ee && Be.some((Ye) => Ye.docId === Ee) || H.value && (H.value = !1, We.value = "");
    }, { immediate: !0 });
    function P() {
      n("close");
    }
    const { dragging: Q, handleSheetHandlePointerCancel: V, handleSheetHandlePointerDown: ae, handleSheetHandlePointerMove: Se, handleSheetHandlePointerUp: Me } = i5(P);
    function Ke() {
      n("toggle-memory-directory");
    }
    function qe(Be) {
      J(Be), n("close-memory-directory");
    }
    return (Be, Ee) => (O(), F("aside", tie, [
      g("button", {
        type: "button",
        class: ze(["chat-mobile-sheet-handle", { "is-dragging": S(Q) }]),
        title: "收起记忆面板",
        "aria-label": "收起记忆面板",
        onClick: P,
        onPointercancel: Ee[0] || (Ee[0] = (...Ye) => S(V) && S(V)(...Ye)),
        onPointerdown: Ee[1] || (Ee[1] = (...Ye) => S(ae) && S(ae)(...Ye)),
        onPointermove: Ee[2] || (Ee[2] = (...Ye) => S(Se) && S(Se)(...Ye)),
        onPointerup: Ee[3] || (Ee[3] = (...Ye) => S(Me) && S(Me)(...Ye))
      }, null, 34),
      g("div", nie, [
        g("button", {
          type: "button",
          class: ze({ active: S(s) === "state" }),
          onClick: Ee[4] || (Ee[4] = (Ye) => s.value = "state")
        }, " 地图 ", 2),
        g("button", {
          type: "button",
          class: ze({ active: S(s) === "memory" }),
          onClick: Ee[5] || (Ee[5] = (Ye) => s.value = "memory")
        }, " 记忆 ", 2),
        g("button", {
          type: "button",
          class: ze({ active: S(s) === "event" }),
          onClick: Ee[6] || (Ee[6] = (Ye) => s.value = "event")
        }, " 事件 ", 2)
      ]),
      S(s) === "state" ? (O(), F("section", rie, [g("div", { class: ze(["tavern-state-viewport", `is-${me.value}`]) }, [g("div", oie, [g("button", {
        type: "button",
        role: "tab",
        "aria-selected": me.value === "scene",
        class: ze({ active: me.value === "scene" }),
        onClick: Ee[7] || (Ee[7] = (Ye) => me.value = "scene")
      }, " 场景图 ", 10, sie), g("button", {
        type: "button",
        role: "tab",
        "aria-selected": me.value === "world",
        class: ze({ active: me.value === "world" }),
        onClick: Ee[8] || (Ee[8] = (Ye) => me.value = "world")
      }, " 世界图 ", 10, aie)]), me.value === "scene" && Le.value ? (O(), gn(Lae, {
        key: 0,
        "selected-doc-id": Pe.value,
        "onUpdate:selectedDocId": Ee[9] || (Ee[9] = (Ye) => Pe.value = Ye),
        compact: "",
        documents: S(fe),
        "active-doc-id": S(G),
        document: Le.value,
        patches: Te.value
      }, null, 8, [
        "selected-doc-id",
        "documents",
        "active-doc-id",
        "document",
        "patches"
      ])) : me.value === "world" ? (O(), gn(qC, {
        key: 1,
        "display-mode": "graph",
        document: S(B),
        patches: S(X),
        "active-location-key": S(oe),
        "active-map-doc-id": S(G),
        "preview-map-doc-id": Pe.value,
        "map-documents": S(fe)
      }, null, 8, [
        "document",
        "patches",
        "active-location-key",
        "active-map-doc-id",
        "preview-map-doc-id",
        "map-documents"
      ])) : (O(), F("div", iie, [g("strong", null, K(se.value?.name || "当前地点"), 1), g("p", null, K(Ie.value ? "当前地点暂无详细地图。" : "暂无地图信息。"), 1)]))], 2), g("article", { class: ze(["tavern-map-info", { "is-world": me.value === "world" }]) }, [me.value === "world" ? (O(), gn(qC, {
        key: 0,
        "display-mode": "detail",
        document: S(B),
        patches: S(X),
        "active-location-key": S(oe),
        "active-map-doc-id": S(G),
        "preview-map-doc-id": Pe.value,
        "map-documents": S(fe)
      }, null, 8, [
        "document",
        "patches",
        "active-location-key",
        "active-map-doc-id",
        "preview-map-doc-id",
        "map-documents"
      ])) : Le.value ? (O(), F("div", lie, [
        g("header", uie, [g("div", null, [g("strong", null, K(he.value), 1), g("span", null, K(Le.value.docId), 1)]), g("em", null, K(we.value ? "当前场景" : "地图档案"), 1)]),
        g("dl", cie, [(O(!0), F(Je, null, lt(L.value, (Ye) => (O(), F("div", { key: Ye.label }, [g("dt", null, K(Ye.label), 1), g("dd", null, K(Ye.value), 1)]))), 128))]),
        Oe.value.length ? (O(), F("div", die, [Ee[12] || (Ee[12] = g("span", null, "人物", -1)), g("strong", null, K(Oe.value.join("、")), 1)])) : ge("", !0),
        Ne.value.length ? (O(), F("div", fie, [(O(!0), F(Je, null, lt(Ne.value, (Ye) => (O(), F("p", { key: Ye }, K(Ye), 1))), 128))])) : ge("", !0)
      ])) : (O(), F("div", hie, " 暂无地图信息。 "))], 2)])) : S(s) === "memory" ? (O(), F("section", {
        key: 1,
        class: ze(["tavern-memory-workspace", { "is-memory-directory-open": e.mobileMemoryDirectoryOpen }])
      }, [
        g("aside", pie, [
          g("header", mie, [Ee[13] || (Ee[13] = g("strong", null, "记忆文档", -1)), g("span", null, K(S(a).length), 1)]),
          S(b).length ? (O(), F("label", gie, [Ln(g("input", {
            "onUpdate:modelValue": Ee[10] || (Ee[10] = (Ye) => /* @__PURE__ */ pn(w) ? w.value = Ye : null),
            type: "search",
            placeholder: "检索档案"
          }, null, 512), [[Ro, S(w)]])])) : ge("", !0),
          S(b).length ? (O(), F("div", vie, [(O(!0), F(Je, null, lt(S(v), (Ye) => (O(), F("div", {
            key: Ye.key,
            class: "memory-file-group"
          }, [g("div", yie, [g("span", null, K(Ye.title), 1), g("em", null, K(Ye.totalCount), 1)]), g("div", _ie, [(O(!0), F(Je, null, lt(Ye.files, (je) => (O(), F("button", {
            key: je.path,
            type: "button",
            class: ze(["memory-file", {
              active: S(z) === je.path,
              stale: je.status === "stale"
            }]),
            onClick: (It) => qe(je.path)
          }, [g("span", wie, K(S(p)(je)), 1)], 10, bie))), 128)), Ye.hiddenCount ? (O(), F("button", {
            key: 0,
            type: "button",
            class: "memory-file memory-file-more",
            onClick: (je) => S(u)(Ye.key)
          }, [g("span", Tie, "再显示 " + K(Math.min(Ye.hiddenCount, Ye.key === "turns" ? S(m) : S(f))) + " 个", 1)], 8, Sie)) : ge("", !0)])]))), 128)), S(w) && !S(v).length ? (O(), F("p", Eie, " 没有匹配的记忆档案。 ")) : ge("", !0)])) : (O(), F("p", Aie, " 还没有记忆档案。 "))
        ]),
        g("button", {
          type: "button",
          class: ze(["tavern-mobile-memory-picker", { "is-open": e.mobileMemoryDirectoryOpen }]),
          disabled: !S(b).length,
          title: "选择记忆文档",
          "aria-label": "选择记忆文档",
          onClick: Ke
        }, [g("span", null, K(S(q) ? S(p)(S(q)) : "选择记忆文档"), 1), Ee[14] || (Ee[14] = g("em", { "aria-hidden": "true" }, "⌄", -1))], 10, Cie),
        En(eie, {
          draft: S(C),
          "onUpdate:draft": Ee[11] || (Ee[11] = (Ye) => /* @__PURE__ */ pn(C) ? C.value = Ye : null),
          "document-available": S(T),
          "read-only": S(R),
          dirty: S(y),
          mode: S(I),
          "preview-html": S(M)(S(C)),
          "preview-signature": S(E)(S(C)),
          status: S($),
          "has-selected-file": !!S(q),
          "loaded-path": S(A),
          "file-meta": S(q) ? S(d)(S(q)) : "",
          onEnterEdit: S(l),
          onPreview: S(x),
          onDiscard: S(i),
          onSave: S(N)
        }, null, 8, [
          "draft",
          "document-available",
          "read-only",
          "dirty",
          "mode",
          "preview-html",
          "preview-signature",
          "status",
          "has-selected-file",
          "loaded-path",
          "file-meta",
          "onEnterEdit",
          "onPreview",
          "onDiscard",
          "onSave"
        ])
      ], 2)) : (O(), gn(zse, {
        key: 2,
        tasks: S(le),
        enabled: S(Ue).questOrchestration,
        "assistant-floor": S(de)
      }, null, 8, [
        "tasks",
        "enabled",
        "assistant-floor"
      ]))
    ]));
  }
}), Iie = kie, xie = { class: "panel step-panel native-workspace" }, Mie = { class: "panel-head preset-page-head" }, Pie = { class: "panel-pills panel-head-actions" }, Rie = { class: "pill" }, Nie = {
  key: 0,
  class: "preset-status-line"
}, $ie = { class: "worldbook-section worldbook-global-enable" }, Die = { class: "worldbook-global-bar" }, Lie = ["aria-expanded", "disabled"], Oie = { class: "worldbook-global-current" }, Fie = { key: 0 }, Bie = { key: 1 }, Uie = {
  key: 0,
  class: "worldbook-global-list"
}, qie = [
  "checked",
  "disabled",
  "onChange"
], Hie = {
  key: 0,
  class: "worldbook-global-empty"
}, zie = { class: "worldbook-section worldbook-main-section" }, Wie = { class: "preset-command-bar worldbook-command-bar" }, Gie = { class: "preset-source-select worldbook-source-select" }, Vie = ["value"], Kie = {
  key: 0,
  value: "",
  disabled: ""
}, Jie = ["value"], Yie = { class: "native-settings-studio worldbook-overview-grid" }, Xie = { class: "native-detail-panel worldbook-overview-detail worldbook-gateway-panel" }, Qie = {
  key: 0,
  class: "worldbook-preview-surface"
}, Zie = {
  key: 0,
  class: "worldbook-preview-status"
}, jie = { class: "worldbook-metric-grid" }, ele = {
  key: 0,
  class: "worldbook-entry-preview-list"
}, tle = ["open", "onToggle"], nle = { class: "worldbook-entry-title" }, rle = ["title", "aria-label"], ole = { class: "worldbook-entry-position" }, sle = {
  key: 0,
  class: "worldbook-entry-body"
}, ale = { class: "worldbook-entry-editor-head" }, ile = { key: 0 }, lle = { key: 1 }, ule = { class: "worldbook-entry-editor-actions" }, cle = ["disabled"], dle = ["disabled"], fle = { class: "worldbook-entry-core-grid" }, hle = { class: "worldbook-entry-title-row" }, ple = { class: "worldbook-entry-title-field" }, mle = ["value"], gle = { class: "worldbook-entry-active-toggle" }, vle = ["checked"], yle = { class: "worldbook-entry-state-field" }, _le = ["value"], ble = { class: "worldbook-entry-position-field" }, wle = ["value"], Sle = ["value", "title"], Tle = { class: "worldbook-entry-depth-field" }, Ele = ["value", "disabled"], Ale = { class: "worldbook-entry-order-field" }, Cle = ["value"], kle = { class: "worldbook-entry-probability-field" }, Ile = ["value", "disabled"], xle = { class: "worldbook-entry-scan-depth-field" }, Mle = ["value"], Ple = { class: "worldbook-entry-key-grid" }, Rle = { class: "worldbook-entry-editor-lines worldbook-entry-keywords-field" }, Nle = ["value"], $le = { class: "worldbook-entry-filter-controls" }, Dle = { class: "worldbook-entry-logic-field" }, Lle = ["value"], Ole = ["value"], Fle = { class: "worldbook-entry-editor-lines worldbook-entry-keywords-field" }, Ble = ["value"], Ule = { class: "worldbook-entry-editor-lines worldbook-entry-content-field" }, qle = ["value"], Hle = ["aria-expanded"], zle = { class: "worldbook-entry-advanced-grid" }, Wle = ["value"], Gle = ["value"], Vle = ["value"], Kle = ["value"], Jle = ["value"], Yle = ["value"], Xle = ["value"], Qle = ["value"], Zle = ["value"], jle = ["value"], eue = { class: "worldbook-entry-advanced-checks" }, tue = ["checked"], nue = ["checked"], rue = ["checked"], oue = ["checked"], sue = ["checked"], aue = ["checked"], iue = ["value", "disabled"], lue = { class: "worldbook-entry-editor-lines worldbook-entry-triggers-field" }, uue = ["value"], cue = { class: "worldbook-entry-match-source-list" }, due = ["checked", "onChange"], fue = { class: "worldbook-entry-actions" }, hue = ["onClick"], pue = { key: 0 }, mue = {
  key: 0,
  class: "worldbook-entry-keys"
}, gue = {
  key: 1,
  class: "worldbook-entry-content"
}, vue = {
  key: 1,
  class: "empty-note"
}, yue = {
  key: 1,
  class: "empty-note"
}, _ue = /* @__PURE__ */ un({
  __name: "TavernWorldbooksSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, cancelWorldbookEntryEdit: n, globalWorldbookOptions: r, globalWorldbookSelected: o, globalWorldbookSaving: s, globalWorldbookStatus: a, hiddenWorldbookPreviewEntryCount: i, isEditingWorldbookEntry: l, saveWorldbookEntryDraft: u, selectedWorldbook: d, selectedWorldbookName: f, showMoreWorldbookPreviewEntries: m, startWorldbookEntryEdit: v, toggleGlobalWorldbook: y, updateWorldbookEntryDraftPatch: T, WORLDBOOK_PREVIEW_BATCH_SIZE: C, worldbookEntryDirty: A, worldbookEntryDraft: I, worldbookEntrySaving: R, worldbookEntryStatus: $, worldbookOptions: p, worldbookPreview: b, worldbookPreviewStatus: w, worldbookStatus: E } = Va(), x = Og(), M = /* @__PURE__ */ ie(!1), N = [
      {
        value: "0:",
        label: "↑Char",
        title: "角色定义前"
      },
      {
        value: "1:",
        label: "↓Char",
        title: "角色定义后"
      },
      {
        value: "5:",
        label: "↑EM",
        title: "示例消息前"
      },
      {
        value: "6:",
        label: "↓EM",
        title: "示例消息后"
      },
      {
        value: "2:",
        label: "↑AN",
        title: "作者注释前"
      },
      {
        value: "3:",
        label: "↓AN",
        title: "作者注释后"
      },
      {
        value: "4:0",
        label: "@D ⚙️",
        title: "按深度注入：系统"
      },
      {
        value: "4:1",
        label: "@D 👤",
        title: "按深度注入：用户"
      },
      {
        value: "4:2",
        label: "@D 🤖",
        title: "按深度注入：助手"
      },
      {
        value: "7:",
        label: "Outlet",
        title: "Outlet"
      }
    ], q = [
      {
        value: 0,
        label: "满足任一"
      },
      {
        value: 3,
        label: "全部满足"
      },
      {
        value: 1,
        label: "不全满足"
      },
      {
        value: 2,
        label: "全部不满足"
      }
    ], z = [
      {
        key: "matchPersonaDescription",
        label: "用户设定"
      },
      {
        key: "matchCharacterDescription",
        label: "角色描述"
      },
      {
        key: "matchCharacterPersonality",
        label: "角色人格"
      },
      {
        key: "matchCharacterDepthPrompt",
        label: "角色深度提示"
      },
      {
        key: "matchScenario",
        label: "场景"
      },
      {
        key: "matchCreatorNotes",
        label: "作者备注"
      }
    ], J = re(() => {
      const L = o.value.length;
      return L ? L === 1 ? o.value[0] || "1 本" : `${L} 本` : "未启用";
    }), G = re(() => o.value.length ? o.value.join(" / ") : "没有全局生效的世界书");
    function oe(L) {
      const P = L.uid !== void 0 && L.uid !== null && String(L.uid).trim() ? String(L.uid).trim() : L.order !== void 0 && L.order !== null && String(L.order).trim() ? String(L.order).trim() : L.name;
      return `worldbook:${f.value}:${P}`;
    }
    function B(L) {
      return `worldbook-advanced:${f.value}:${L.uid}`;
    }
    function X(L, P) {
      T({ [L]: P });
    }
    function de(L) {
      return Array.isArray(L) ? L.map((P) => String(P || "").trim()).filter(Boolean).join(", ") : "";
    }
    function fe(L = "") {
      return String(L || "").split(",").map((P) => P.trim()).filter(Boolean);
    }
    function ve(L) {
      return L == null ? "" : String(L);
    }
    function Ce(L) {
      const P = L.target.value;
      if (P === "") return null;
      const Q = Number(P);
      return Number.isFinite(Q) ? Q : null;
    }
    function Ue(L) {
      return L === !0 ? "true" : L === !1 ? "false" : "null";
    }
    function le(L) {
      const P = L.target.value;
      return P === "true" ? !0 : P === "false" ? !1 : null;
    }
    function me(L) {
      return L.constant ? "constant" : L.vectorized ? "vectorized" : "normal";
    }
    function We(L) {
      const P = L.target.value;
      T({
        constant: P === "constant",
        vectorized: P === "vectorized"
      });
    }
    function H(L) {
      return L.selective ? String(Number(L.selectiveLogic) || 0) : "off";
    }
    function W(L) {
      const P = L.target.value;
      if (P === "off") {
        T({ selective: !1 });
        return;
      }
      const Q = Number(P);
      T({
        selective: !0,
        selectiveLogic: Number.isFinite(Q) ? Q : 0
      });
    }
    function se(L) {
      return L.enabled === !1 ? "×" : L.constant ? "🔵" : L.vectorized ? "🔗" : "🟢";
    }
    function ke(L) {
      return L.enabled === !1 ? "关闭" : L.constant ? "常驻" : L.vectorized ? "向量化" : "普通";
    }
    function Pe(L) {
      return `${Number(L.position) || 0}:${Number(L.position) === 4 ? Number(L.role) || 0 : ""}`;
    }
    function Le(L) {
      const [P, Q = ""] = L.target.value.split(":"), V = Number(P), ae = Number(Q), Se = Number.isFinite(V) ? V : 0;
      T({
        position: Se,
        role: Number.isFinite(ae) ? ae : 0,
        depth: Se === 4 ? I.value?.depth ?? 4 : null
      });
    }
    function Y(L) {
      const P = Number(L.position ?? 0), Q = N.find((V) => V.value === `${P}:${P === 4 ? Number(L.role || 0) : ""}`);
      return P === 4 ? `${Q?.label || "@D"} ${ve(L.depth) || 4}` : Q?.label || "↑Char";
    }
    function te(L) {
      return Number(L.position) === 4;
    }
    function he(L) {
      const P = Ce(L);
      T({
        probability: P,
        useProbability: P !== null
      });
    }
    function we(L) {
      const P = L.target.checked;
      T({
        useProbability: P,
        probability: P ? I.value?.probability ?? 100 : null
      });
    }
    function Te(L) {
      return L !== !1 && L !== 0;
    }
    function Ie(L) {
      return typeof L == "number" ? String(L) : "";
    }
    function Ne(L) {
      const P = L.target.checked;
      T({ delayUntilRecursion: !!P });
    }
    function Oe(L) {
      const P = L.target.value;
      if (!P) {
        T({ delayUntilRecursion: !0 });
        return;
      }
      const Q = Number(P);
      T({ delayUntilRecursion: Number.isFinite(Q) && Q > 0 ? Math.floor(Q) : !1 });
    }
    return gt([
      t,
      f,
      () => b.value?.name
    ], () => x.reset()), (L, P) => Ln((O(), F("div", xie, [
      g("div", Mie, [P[29] || (P[29] = g("div", null, [g("h2", null, "世界书")], -1)), g("div", Pie, [g("span", Rie, K(S(p).length) + " 本", 1)])]),
      S(E) ? (O(), F("div", Nie, [g("span", null, K(S(E)), 1)])) : ge("", !0),
      g("section", $ie, [
        P[30] || (P[30] = g("div", { class: "worldbook-section-head worldbook-global-enable-head" }, [g("h3", null, "已启用的全局世界书")], -1)),
        g("div", Die, [g("button", {
          type: "button",
          class: "worldbook-global-toggle",
          "aria-expanded": M.value,
          disabled: S(s),
          onClick: P[0] || (P[0] = (Q) => M.value = !M.value)
        }, K(J.value), 9, Lie), g("div", Oie, [S(a) ? (O(), F("span", Fie, K(S(a)), 1)) : (O(), F("span", Bie, K(G.value), 1))])]),
        M.value ? (O(), F("div", Uie, [(O(!0), F(Je, null, lt(S(r), (Q) => (O(), F("label", {
          key: Q,
          class: "worldbook-global-option"
        }, [g("input", {
          type: "checkbox",
          checked: S(o).includes(Q),
          disabled: S(s),
          onChange: (V) => S(y)(Q, V.target.checked)
        }, null, 40, qie), g("span", null, K(Q), 1)]))), 128)), S(r).length ? ge("", !0) : (O(), F("span", Hie, "没有世界书"))])) : ge("", !0)
      ]),
      g("section", zie, [
        P[73] || (P[73] = g("div", { class: "worldbook-section-head worldbook-main-head" }, [g("h3", null, "世界书操作")], -1)),
        g("div", Wie, [g("label", Gie, [g("select", {
          "aria-label": "选择世界书",
          value: S(f),
          onChange: P[1] || (P[1] = (Q) => f.value = Q.target.value)
        }, [
          P[31] || (P[31] = g("option", { value: "" }, " 未选择 ", -1)),
          S(p).length ? ge("", !0) : (O(), F("option", Kie, " 没有世界书 ")),
          (O(!0), F(Je, null, lt(S(p), (Q) => (O(), F("option", {
            key: Q.name,
            value: Q.name
          }, K(Q.name), 9, Jie))), 128))
        ], 40, Vie)])]),
        g("div", Yie, [g("div", Xie, [S(d) ? (O(), F("div", Qie, [S(w) ? (O(), F("div", Zie, K(S(w)), 1)) : S(b) && S(b).name === S(d).name ? (O(), F(Je, { key: 1 }, [g("div", jie, [
          g("span", null, [g("strong", null, K(S(b).entryCount), 1), P[32] || (P[32] = g("small", null, "条目", -1))]),
          g("span", null, [g("strong", null, K(S(b).enabledCount), 1), P[33] || (P[33] = g("small", null, "启用", -1))]),
          g("span", null, [g("strong", null, K(S(b).constantCount), 1), P[34] || (P[34] = g("small", null, "常驻", -1))]),
          g("span", null, [g("strong", null, K(S(b).keywordCount), 1), P[35] || (P[35] = g("small", null, "关键词", -1))]),
          g("span", null, [g("strong", null, K(S(b).totalChars), 1), P[36] || (P[36] = g("small", null, "正文字符", -1))])
        ]), S(b).entries.length ? (O(), F("div", ele, [(O(!0), F(Je, null, lt(S(b).entries, (Q) => (O(), F("details", {
          key: oe(Q),
          class: ze(["worldbook-entry-preview", { disabled: !Q.enabled }]),
          open: S(x).isOpen(oe(Q)),
          onToggle: (V) => S(x).setOpenFromEvent(oe(Q), V)
        }, [g("summary", null, [
          g("span", nle, [g("strong", null, K(Q.name), 1)]),
          g("span", {
            class: "worldbook-entry-state",
            title: ke(Q),
            "aria-label": ke(Q)
          }, K(se(Q)), 9, rle),
          g("span", ole, K(Y(Q)), 1)
        ]), S(x).isOpen(oe(Q)) ? (O(), F("div", sle, [S(l)(Q) && S(I) ? (O(), F("form", {
          key: 0,
          class: "worldbook-entry-editor",
          onSubmit: P[27] || (P[27] = sn((...V) => S(u) && S(u)(...V), ["prevent"]))
        }, [
          g("div", ale, [S($) ? (O(), F("span", ile, K(S($)), 1)) : (O(), F("span", lle, "编辑条目")), g("div", ule, [g("button", {
            type: "button",
            class: "worldbook-row-open",
            disabled: S(R),
            onClick: P[2] || (P[2] = (...V) => S(n) && S(n)(...V))
          }, " 取消 ", 8, cle), g("button", {
            type: "submit",
            class: "primary-action",
            disabled: !S(A) || S(R)
          }, " 保存 ", 8, dle)])]),
          g("div", fle, [
            g("div", hle, [g("label", ple, [P[37] || (P[37] = g("span", null, "条目名", -1)), g("input", {
              value: S(I).comment,
              type: "text",
              onInput: P[3] || (P[3] = (V) => S(T)({ comment: V.target.value }))
            }, null, 40, mle)]), g("label", gle, [g("input", {
              type: "checkbox",
              checked: !S(I).disable,
              onChange: P[4] || (P[4] = (V) => S(T)({
                disable: !V.target.checked,
                enabled: V.target.checked
              }))
            }, null, 40, vle), P[38] || (P[38] = g("span", null, "启用", -1))])]),
            g("label", yle, [P[40] || (P[40] = g("span", null, "状态", -1)), g("select", {
              value: me(S(I)),
              onChange: We
            }, [...P[39] || (P[39] = [
              g("option", { value: "constant" }, " 🔵 常驻 ", -1),
              g("option", { value: "normal" }, " 🟢 普通 ", -1),
              g("option", { value: "vectorized" }, " 🔗 向量化 ", -1)
            ])], 40, _le)]),
            g("label", ble, [P[41] || (P[41] = g("span", null, "注入位置", -1)), g("select", {
              value: Pe(S(I)),
              onChange: Le
            }, [(O(), F(Je, null, lt(N, (V) => g("option", {
              key: V.value,
              value: V.value,
              title: V.title
            }, K(V.label), 9, Sle)), 64))], 40, wle)]),
            g("label", Tle, [P[42] || (P[42] = g("span", null, "深度", -1)), g("input", {
              value: ve(S(I).depth),
              type: "number",
              min: "0",
              max: "9999",
              step: "1",
              disabled: !te(S(I)),
              onInput: P[5] || (P[5] = (V) => X("depth", Ce(V)))
            }, null, 40, Ele)]),
            g("label", Ale, [P[43] || (P[43] = g("span", null, "排序", -1)), g("input", {
              value: S(I).order,
              type: "number",
              min: "0",
              max: "9999",
              step: "1",
              onInput: P[6] || (P[6] = (V) => X("order", Number(V.target.value)))
            }, null, 40, Cle)]),
            g("label", kle, [P[44] || (P[44] = g("span", null, "触发概率", -1)), g("input", {
              value: ve(S(I).probability),
              type: "number",
              min: "0",
              max: "100",
              step: "1",
              disabled: !S(I).useProbability,
              onInput: he
            }, null, 40, Ile)]),
            g("label", xle, [P[45] || (P[45] = g("span", null, "扫描深度", -1)), g("input", {
              value: ve(S(I).scanDepth),
              type: "number",
              min: "0",
              max: "1000",
              onInput: P[7] || (P[7] = (V) => X("scanDepth", Ce(V)))
            }, null, 40, Mle)])
          ]),
          g("div", Ple, [
            g("label", Rle, [P[46] || (P[46] = g("span", null, "关键词", -1)), g("input", {
              value: de(S(I).key),
              type: "text",
              onInput: P[8] || (P[8] = (V) => S(T)({ key: fe(V.target.value) }))
            }, null, 40, Nle)]),
            g("div", $le, [g("label", Dle, [P[48] || (P[48] = g("span", null, "过滤逻辑", -1)), g("select", {
              value: H(S(I)),
              onChange: W
            }, [P[47] || (P[47] = g("option", { value: "off" }, " 关闭 ", -1)), (O(), F(Je, null, lt(q, (V) => g("option", {
              key: V.value,
              value: V.value
            }, K(V.label), 9, Ole)), 64))], 40, Lle)])]),
            g("label", Fle, [P[49] || (P[49] = g("span", null, "可选过滤", -1)), g("input", {
              value: de(S(I).keysecondary.length ? S(I).keysecondary : S(I).secondary_keys),
              type: "text",
              onInput: P[9] || (P[9] = (V) => S(T)({ keysecondary: fe(V.target.value) }))
            }, null, 40, Ble)])
          ]),
          g("label", Ule, [P[50] || (P[50] = g("span", null, "内容", -1)), g("textarea", {
            value: S(I).content,
            rows: "9",
            onInput: P[10] || (P[10] = (V) => S(T)({ content: V.target.value }))
          }, null, 40, qle)]),
          g("div", { class: ze(["worldbook-entry-advanced", { open: S(x).isOpen(B(S(I))) }]) }, [g("button", {
            type: "button",
            class: "worldbook-entry-advanced-toggle",
            "aria-expanded": S(x).isOpen(B(S(I))),
            onClick: P[11] || (P[11] = (V) => S(x).setOpen(B(S(I)), !S(x).isOpen(B(S(I)))))
          }, " 高级设置 ", 8, Hle), S(x).isOpen(B(S(I))) ? (O(), F(Je, { key: 0 }, [
            g("div", zle, [
              g("label", null, [P[51] || (P[51] = g("span", null, "注入出口", -1)), g("input", {
                value: S(I).outletName,
                type: "text",
                onInput: P[12] || (P[12] = (V) => X("outletName", V.target.value))
              }, null, 40, Wle)]),
              g("label", null, [P[53] || (P[53] = g("span", null, "区分大小写", -1)), g("select", {
                value: Ue(S(I).caseSensitive),
                onChange: P[13] || (P[13] = (V) => X("caseSensitive", le(V)))
              }, [...P[52] || (P[52] = [
                g("option", { value: "null" }, "跟随全局", -1),
                g("option", { value: "true" }, "是", -1),
                g("option", { value: "false" }, "否", -1)
              ])], 40, Gle)]),
              g("label", null, [P[55] || (P[55] = g("span", null, "全词匹配", -1)), g("select", {
                value: Ue(S(I).matchWholeWords),
                onChange: P[14] || (P[14] = (V) => X("matchWholeWords", le(V)))
              }, [...P[54] || (P[54] = [
                g("option", { value: "null" }, "跟随全局", -1),
                g("option", { value: "true" }, "是", -1),
                g("option", { value: "false" }, "否", -1)
              ])], 40, Vle)]),
              g("label", null, [P[57] || (P[57] = g("span", null, "分组评分", -1)), g("select", {
                value: Ue(S(I).useGroupScoring),
                onChange: P[15] || (P[15] = (V) => X("useGroupScoring", le(V)))
              }, [...P[56] || (P[56] = [
                g("option", { value: "null" }, "跟随全局", -1),
                g("option", { value: "true" }, "是", -1),
                g("option", { value: "false" }, "否", -1)
              ])], 40, Kle)]),
              g("label", null, [P[58] || (P[58] = g("span", null, "自动化 ID", -1)), g("input", {
                value: S(I).automationId,
                type: "text",
                onInput: P[16] || (P[16] = (V) => X("automationId", V.target.value))
              }, null, 40, Jle)]),
              g("label", null, [P[59] || (P[59] = g("span", null, "包含组", -1)), g("input", {
                value: S(I).group,
                type: "text",
                onInput: P[17] || (P[17] = (V) => X("group", V.target.value))
              }, null, 40, Yle)]),
              g("label", null, [P[60] || (P[60] = g("span", null, "分组权重", -1)), g("input", {
                value: ve(S(I).groupWeight),
                type: "number",
                min: "1",
                onInput: P[18] || (P[18] = (V) => X("groupWeight", Ce(V)))
              }, null, 40, Xle)]),
              g("label", null, [P[61] || (P[61] = g("span", null, "黏着轮数", -1)), g("input", {
                value: ve(S(I).sticky),
                type: "number",
                min: "0",
                onInput: P[19] || (P[19] = (V) => X("sticky", Ce(V)))
              }, null, 40, Qle)]),
              g("label", null, [P[62] || (P[62] = g("span", null, "冷却轮数", -1)), g("input", {
                value: ve(S(I).cooldown),
                type: "number",
                min: "0",
                onInput: P[20] || (P[20] = (V) => X("cooldown", Ce(V)))
              }, null, 40, Zle)]),
              g("label", null, [P[63] || (P[63] = g("span", null, "延迟轮数", -1)), g("input", {
                value: ve(S(I).delay),
                type: "number",
                min: "0",
                onInput: P[21] || (P[21] = (V) => X("delay", Ce(V)))
              }, null, 40, jle)])
            ]),
            g("div", eue, [
              g("label", null, [g("input", {
                type: "checkbox",
                checked: S(I).useProbability,
                onChange: we
              }, null, 40, tue), P[64] || (P[64] = g("span", null, "启用概率", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: S(I).ignoreBudget,
                onChange: P[22] || (P[22] = (V) => X("ignoreBudget", V.target.checked))
              }, null, 40, nue), P[65] || (P[65] = g("span", null, "忽略预算", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: S(I).excludeRecursion,
                onChange: P[23] || (P[23] = (V) => X("excludeRecursion", V.target.checked))
              }, null, 40, rue), P[66] || (P[66] = g("span", null, "排除递归", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: S(I).preventRecursion,
                onChange: P[24] || (P[24] = (V) => X("preventRecursion", V.target.checked))
              }, null, 40, oue), P[67] || (P[67] = g("span", null, "阻止递归", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: S(I).groupOverride,
                onChange: P[25] || (P[25] = (V) => X("groupOverride", V.target.checked))
              }, null, 40, sue), P[68] || (P[68] = g("span", null, "分组优先", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: Te(S(I).delayUntilRecursion),
                onChange: Ne
              }, null, 40, aue), P[69] || (P[69] = g("span", null, "递归后延迟", -1))]),
              g("label", null, [P[70] || (P[70] = g("span", null, "递归层级", -1)), g("input", {
                value: Ie(S(I).delayUntilRecursion),
                type: "number",
                min: "1",
                disabled: !Te(S(I).delayUntilRecursion),
                onInput: Oe
              }, null, 40, iue)])
            ]),
            g("label", lue, [P[71] || (P[71] = g("span", null, "生成触发", -1)), g("input", {
              value: de(S(I).triggers),
              type: "text",
              onInput: P[26] || (P[26] = (V) => X("triggers", fe(V.target.value)))
            }, null, 40, uue)]),
            g("div", cue, [P[72] || (P[72] = g("span", null, "额外匹配源", -1)), (O(), F(Je, null, lt(z, (V) => g("label", { key: V.key }, [g("input", {
              type: "checkbox",
              checked: !!S(I)[V.key],
              onChange: (ae) => X(V.key, ae.target.checked)
            }, null, 40, due), g("span", null, K(V.label), 1)])), 64))])
          ], 64)) : ge("", !0)], 2)
        ], 32)) : (O(), F(Je, { key: 1 }, [
          g("div", fue, [g("button", {
            type: "button",
            class: "worldbook-row-open",
            onClick: (V) => S(v)(Q)
          }, " 编辑 ", 8, hue), S($) && S(l)(Q) ? (O(), F("span", pue, K(S($)), 1)) : ge("", !0)]),
          Q.keys.length || Q.secondaryKeys.length ? (O(), F("p", mue, K([...Q.keys, ...Q.secondaryKeys].join(" / ")), 1)) : ge("", !0),
          Q.contentPreview ? (O(), F("p", gue, K(Q.contentPreview), 1)) : ge("", !0)
        ], 64))])) : ge("", !0)], 42, tle))), 128)), S(i) ? (O(), F("button", {
          key: 0,
          type: "button",
          class: "native-add-row worldbook-preview-more",
          onClick: P[28] || (P[28] = (...Q) => S(m) && S(m)(...Q))
        }, " 再显示 " + K(Math.min(S(i), S(C))) + " 条 ", 1)) : ge("", !0)])) : (O(), F("div", vue, " 这本世界书暂无条目。 "))], 64)) : ge("", !0)])) : (O(), F("div", yue, " 选择一本世界书后查看预览。 "))])])
      ])
    ], 512)), [[Iu, S(t) === "worldbooks"]]);
  }
}), p5 = _ue, bue = {
  key: 0,
  class: "chat-mobile-topbar"
}, wue = { class: "chat-mobile-primary-row" }, Sue = {
  class: "chat-mobile-segment",
  role: "tablist",
  "aria-label": "聊天视图"
}, Tue = ["aria-selected"], Eue = ["aria-selected"], Aue = { class: "chat-mobile-action-group" }, Cue = ["title", "aria-label"], kue = {
  key: 0,
  class: "chat-mobile-svg",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.8",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, Iue = {
  key: 1,
  class: "chat-mobile-svg",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.8",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, xue = { class: "chat-mobile-context-row" }, Mue = { class: "chat-flip-card" }, Pue = { class: "compose-author-note-body" }, Rue = { class: "compose-author-note-field" }, Nue = { class: "compose-author-note-field" }, $ue = { class: "compose-author-note-segments" }, Due = ["onClick"], Lue = { class: "compose-author-note-grid" }, Oue = { class: "compose-author-note-field" }, Fue = { class: "compose-author-note-field" }, Bue = { class: "compose-author-note-field" }, Uue = { class: "compose-author-note-segments" }, que = ["onClick"], Hue = { class: "compose-author-note-check" }, zue = { class: "compose-author-note-actions" }, Wue = ["disabled"], Gue = {
  key: 6,
  class: "chat-quick-settings-overlay",
  role: "presentation"
}, Vue = {
  class: "chat-quick-settings-dialog",
  role: "dialog",
  "aria-modal": "true",
  "aria-labelledby": "chat-quick-settings-title"
}, Kue = { class: "chat-quick-settings-head" }, Jue = { id: "chat-quick-settings-title" }, Yue = { class: "xb-main" }, Xue = /* @__PURE__ */ un({
  __name: "TavernChatPage",
  setup(e) {
    const t = Af(), n = Fg(), r = o5(), o = Va(), s = a5(), { activeView: a, chatFocus: i, homeThemeDark: l } = t, { chatAutoScroll: u, chatLayout: d, chatMessageWindow: f, chatScrollRef: m, currentAuthorNote: v, saveCurrentAuthorNote: y, chatSidePanel: T, messageKey: C, updateChatScrollButtons: A, visibleChatMessages: I } = n, { managerAutoScroll: R, managerMessageWindow: $, managerScrollRef: p, updateManagerScrollButtons: b, visibleManagerChatItems: w } = r, { chatWorkspacePanel: E, saveSessionContract: x, selectedSessionId: M, sessionContract: N } = s, { activeSettingsWorkspace: q, apiSettingsRootRef: z, syncChatPresetFromHost: J, syncGlobalWorldbooksFromHost: G, syncWorldbooksForCurrentCharacter: oe } = o;
    let B = null, X = null, de = !0, fe = !0;
    const ve = /* @__PURE__ */ ie(!1), Ce = /* @__PURE__ */ ie(null), Ue = /* @__PURE__ */ ie(!1), le = /* @__PURE__ */ ie(""), me = /* @__PURE__ */ ie(mo(N.value)), We = /* @__PURE__ */ ie(!1), H = /* @__PURE__ */ ie(Ta(f_)), W = /* @__PURE__ */ ie(!1), se = /* @__PURE__ */ ie(""), ke = /* @__PURE__ */ ie("none"), Pe = /* @__PURE__ */ ie(!1), Le = Bg("(max-width: 760px)"), Y = re(() => JSON.stringify(me.value) !== JSON.stringify(N.value)), te = re(() => !Le.value || ke.value === "directory"), he = re(() => !Le.value || ke.value === "workspace"), we = re(() => a.value === "chat" && i.value === "chat"), Te = re(() => a.value === "chat" && i.value === "manager"), Ie = re(() => [
      f.value.startIndex,
      f.value.visibleCount,
      ...I.value.map((et) => `${C(et)}:${et.error ? 1 : 0}:${String(et.content || "").length}`)
    ].join("|")), Ne = re(() => [
      $.value.startIndex,
      $.value.visibleCount,
      ...w.value.map((et) => `${et.kind}:${et.key}`)
    ].join("|")), Oe = [
      {
        value: jh.AFTER_MAIN,
        label: "主提示词后"
      },
      {
        value: jh.BEFORE_MAIN,
        label: "主提示词前"
      },
      {
        value: jh.IN_CHAT,
        label: "聊天内 @ Depth"
      }
    ], L = [
      {
        value: Zh.SYSTEM,
        label: "System"
      },
      {
        value: Zh.USER,
        label: "User"
      },
      {
        value: Zh.ASSISTANT,
        label: "Assistant"
      }
    ];
    function P() {
      ke.value = "none", Pe.value = !1;
    }
    function Q() {
      T.value = "sessions", ke.value = "directory", Pe.value = !1;
    }
    function V(et) {
      const xe = ke.value === "workspace" && E.value === et;
      E.value = et, ke.value = xe ? "none" : "workspace", Pe.value = !1;
    }
    function ae(et) {
      const xe = et.target instanceof HTMLElement ? et.target : null;
      if (xe?.closest?.(".chat-mobile-sheet-handle")) {
        P();
        return;
      }
      xe?.closest?.(".session-open, .memory-file:not(.memory-file-more)") && P();
    }
    function Se() {
      Pe.value = !Pe.value;
    }
    function Me() {
      le.value = "", me.value = mo(N.value), ve.value = !0;
    }
    function Ke() {
      le.value = "", ve.value = !1;
    }
    const qe = re(() => Ce.value === "worldbooks" ? "世界书" : Ce.value === "chatPreset" ? "聊天预设" : "API 配置"), Be = re(() => Ce.value ? `is-${Ce.value}-workspace` : "");
    function Ee(et) {
      P(), q.value = et, Ce.value = et, et === "chatPreset" && J(), et === "worldbooks" && (oe(), G());
    }
    function Ye() {
      Ce.value = null;
    }
    function je() {
      P(), Ce.value = null, H.value = Ta(v.value), se.value = "", We.value = !0;
    }
    function It() {
      se.value = "", We.value = !1;
    }
    function Ht(et) {
      H.value = Ta({
        ...H.value,
        ...et
      });
    }
    async function At() {
      if (!W.value) {
        W.value = !0, se.value = "";
        try {
          const et = Ta(H.value);
          await y(et), H.value = et, se.value = "已保存";
        } catch (et) {
          se.value = et instanceof Error ? et.message : String(et || "保存失败");
        } finally {
          W.value = !1;
        }
      }
    }
    function Mt(et) {
      z.value = et instanceof HTMLElement ? et : null;
    }
    function Ut(et) {
      me.value = {
        ...me.value,
        [et]: !me.value[et]
      };
    }
    async function yt() {
      if (!M.value) {
        le.value = "", ve.value = !1;
        return;
      }
      le.value = "", Ue.value = !0;
      try {
        if (!await x(me.value)) throw new Error("contract_save_failed");
        ve.value = !1;
      } catch {
        le.value = "契约保存失败，请重试。";
      } finally {
        Ue.value = !1;
      }
    }
    return gt(() => N.value, (et) => {
      ve.value || (me.value = mo(et));
    }, { deep: !0 }), gt(() => M.value, () => {
      le.value = "", ve.value = !1, me.value = mo(N.value);
    }), gt(() => a.value, (et) => {
      et !== "chat" && (Ce.value = null, We.value = !1);
    }), gt(() => M.value, () => {
      se.value = "", We.value = !1, H.value = Ta(v.value);
    }), gt(Ie, () => {
      de = !0;
    }, { flush: "sync" }), gt(Ne, () => {
      fe = !0;
    }, { flush: "sync" }), Ok(() => {
      if (B = null, X = null, we.value && de) {
        B = OC(m.value, {
          itemSelector: ".chat-bubble[data-chat-anchor-key], .chat-history-gate[data-chat-anchor-key]",
          datasetKey: "chatAnchorKey"
        });
        return;
      }
      Te.value && fe && (X = OC(p.value, {
        itemSelector: "[data-manager-anchor-key]",
        datasetKey: "managerAnchorKey"
      }));
    }), Fk(() => {
      const et = we.value && u.value !== !1, xe = Te.value && R.value !== !1;
      B && (FC(m.value, B, {
        itemSelector: ".chat-bubble[data-chat-anchor-key], .chat-history-gate[data-chat-anchor-key]",
        datasetKey: "chatAnchorKey"
      }, {
        forceBottom: et,
        defaultToBottom: et,
        preserveScrollTop: !et
      }), de = !1, A()), X && (FC(p.value, X, {
        itemSelector: "[data-manager-anchor-key]",
        datasetKey: "managerAnchorKey"
      }, {
        forceBottom: xe,
        defaultToBottom: xe,
        preserveScrollTop: !xe
      }), fe = !1, b()), B = null, X = null;
    }), (et, xe) => S(a) === "chat" ? (O(), F("section", {
      key: 0,
      class: ze(["tavern-chat xb-page", [
        `chat-focus-${S(i)}`,
        `chat-layout-${S(d)}`,
        {
          "is-mobile-directory-open": ke.value === "directory",
          "is-mobile-workspace-open": ke.value === "workspace"
        }
      ]])
    }, [
      En(Ef, {
        "include-api": "",
        "include-chat-preset": "",
        "include-home": "",
        "include-worldbooks": "",
        "home-last": "",
        dark: S(l),
        onApi: xe[0] || (xe[0] = (Ze) => Ee("api")),
        onChatPreset: xe[1] || (xe[1] = (Ze) => Ee("chatPreset")),
        onHome: xe[2] || (xe[2] = (Ze) => a.value = "home"),
        onToggleTheme: xe[3] || (xe[3] = (Ze) => l.value = !S(l)),
        onWorldbooks: xe[4] || (xe[4] = (Ze) => Ee("worldbooks"))
      }, null, 8, ["dark"]),
      S(Le) ? (O(), F("header", bue, [g("div", wue, [g("div", Sue, [
        xe[21] || (xe[21] = g("span", { "aria-hidden": "true" }, null, -1)),
        g("button", {
          type: "button",
          role: "tab",
          "aria-selected": S(i) === "chat",
          class: ze({ active: S(i) === "chat" }),
          onClick: xe[5] || (xe[5] = (Ze) => {
            i.value = "chat", P();
          })
        }, " 角色 ", 10, Tue),
        g("button", {
          type: "button",
          role: "tab",
          "aria-selected": S(i) === "manager",
          class: ze({ active: S(i) === "manager" }),
          onClick: xe[6] || (xe[6] = (Ze) => {
            i.value = "manager", P();
          })
        }, " 助手 ", 10, Eue)
      ]), g("div", Aue, [
        g("button", {
          type: "button",
          class: "chat-mobile-icon-button chat-mobile-utility-button",
          title: "聊天预设",
          "aria-label": "聊天预设",
          onClick: xe[7] || (xe[7] = (Ze) => Ee("chatPreset"))
        }, [...xe[22] || (xe[22] = [g("svg", {
          class: "chat-mobile-svg",
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          "stroke-linecap": "round",
          "stroke-linejoin": "round",
          "aria-hidden": "true"
        }, [g("path", {
          "stroke-width": "1.6",
          d: "M5 5h14v10H9l-4 4V5ZM8 9h8M8 12h5"
        })], -1)])]),
        g("button", {
          type: "button",
          class: "chat-mobile-icon-button chat-mobile-utility-button",
          title: "API 配置",
          "aria-label": "API 配置",
          onClick: xe[8] || (xe[8] = (Ze) => Ee("api"))
        }, [...xe[23] || (xe[23] = [g("svg", {
          class: "chat-mobile-svg",
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          "stroke-linecap": "round",
          "stroke-linejoin": "round",
          "aria-hidden": "true"
        }, [g("path", {
          "stroke-width": "1.6",
          d: "M9 7V3M15 7V3M7 11h10M8 7h8v5a4 4 0 0 1-8 0V7ZM12 16v5"
        })], -1)])]),
        g("button", {
          type: "button",
          class: "chat-mobile-icon-button chat-mobile-utility-button",
          title: "世界书",
          "aria-label": "世界书",
          onClick: xe[9] || (xe[9] = (Ze) => Ee("worldbooks"))
        }, [...xe[24] || (xe[24] = [g("svg", {
          class: "chat-mobile-svg",
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          "stroke-linecap": "round",
          "stroke-linejoin": "round",
          "aria-hidden": "true"
        }, [g("path", {
          "stroke-width": "1.6",
          d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
        })], -1)])]),
        g("button", {
          type: "button",
          class: "chat-mobile-icon-button chat-mobile-utility-button",
          title: S(l) ? "切换到白天" : "切换到夜间",
          "aria-label": S(l) ? "切换到白天" : "切换到夜间",
          onClick: xe[10] || (xe[10] = (Ze) => l.value = !S(l))
        }, [S(l) ? (O(), F("svg", kue, [...xe[25] || (xe[25] = [ku('<circle cx="12" cy="12" r="4"></circle><path d="M12 2v2"></path><path d="M12 20v2"></path><path d="m4.93 4.93 1.41 1.41"></path><path d="m17.66 17.66 1.41 1.41"></path><path d="M2 12h2"></path><path d="M20 12h2"></path><path d="m6.34 17.66-1.41 1.41"></path><path d="m19.07 4.93-1.41 1.41"></path>', 9)])])) : (O(), F("svg", Iue, [...xe[26] || (xe[26] = [g("path", { d: "M20.2 14.5A7.3 7.3 0 0 1 9.5 3.8 8.7 8.7 0 1 0 20.2 14.5Z" }, null, -1)])]))], 8, Cue),
        g("button", {
          type: "button",
          class: "chat-mobile-icon-button chat-mobile-utility-button",
          title: "首页",
          "aria-label": "首页",
          onClick: xe[11] || (xe[11] = (Ze) => {
            a.value = "home", P();
          })
        }, [...xe[27] || (xe[27] = [g("svg", {
          class: "chat-mobile-svg",
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          "stroke-width": "1.8",
          "stroke-linecap": "round",
          "stroke-linejoin": "round",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M3 11.5 12 4l9 7.5" }),
          g("path", { d: "M5.5 10.5V20h13v-9.5" }),
          g("path", { d: "M9.5 20v-5.5h5V20" })
        ], -1)])])
      ])]), g("div", xue, [
        g("button", {
          type: "button",
          class: ze(["chat-mobile-context-button", { "is-active": ke.value === "workspace" && S(E) === "state" }]),
          title: "地图",
          "aria-label": "地图",
          onClick: xe[12] || (xe[12] = (Ze) => V("state"))
        }, " 地图 ", 2),
        g("button", {
          type: "button",
          class: ze(["chat-mobile-context-button", { "is-active": ke.value === "workspace" && S(E) === "memory" }]),
          title: "记忆",
          "aria-label": "记忆",
          onClick: xe[13] || (xe[13] = (Ze) => V("memory"))
        }, " 记忆 ", 2),
        g("button", {
          type: "button",
          class: ze(["chat-mobile-context-button", { "is-active": ke.value === "workspace" && S(E) === "event" }]),
          title: "事件",
          "aria-label": "事件",
          onClick: xe[14] || (xe[14] = (Ze) => V("event"))
        }, " 事件 ", 2),
        g("button", {
          type: "button",
          class: "chat-mobile-context-button",
          title: "契约",
          "aria-label": "契约",
          onClick: xe[15] || (xe[15] = (Ze) => {
            P(), Me();
          })
        }, " 契约 ")
      ])])) : ge("", !0),
      S(Le) && ke.value !== "none" ? (O(), F("button", {
        key: 1,
        type: "button",
        class: "chat-mobile-sheet-scrim",
        title: "收起面板",
        "aria-label": "收起面板",
        onClick: P
      })) : ge("", !0),
      te.value ? (O(), gn(Ote, {
        key: 2,
        onClick: ae,
        onClose: P
      })) : ge("", !0),
      g("section", { class: ze(["chat-workbench", { "is-manager": S(i) === "manager" }]) }, [g("div", Mue, [En(Gre, {
        onOpenContract: Me,
        onOpenAuthorNote: je,
        onOpenSessionArchive: Q
      }), En(ose, { onOpenContract: Me })])], 2),
      he.value ? (O(), gn(Iie, {
        key: 3,
        "mobile-memory-directory-open": Pe.value,
        onClose: P,
        onCloseMemoryDirectory: xe[16] || (xe[16] = (Ze) => Pe.value = !1),
        onToggleMemoryDirectory: Se
      }, null, 8, ["mobile-memory-directory-open"])) : ge("", !0),
      ve.value ? (O(), gn(lte, {
        key: 4,
        "draft-contract": me.value,
        dirty: Y.value,
        saving: Ue.value,
        "can-save": !!S(M),
        "error-text": le.value,
        onClose: Ke,
        onToggle: Ut,
        onSave: yt
      }, null, 8, [
        "draft-contract",
        "dirty",
        "saving",
        "can-save",
        "error-text"
      ])) : ge("", !0),
      We.value ? (O(), F("div", {
        key: 5,
        class: "compose-author-note-overlay",
        role: "dialog",
        "aria-modal": "true",
        "aria-labelledby": "compose-author-note-title",
        onKeydown: Vs(It, ["esc"])
      }, [g("form", {
        class: "compose-author-note-dialog compose-author-note-panel",
        onSubmit: sn(At, ["prevent"])
      }, [
        g("header", { class: "compose-author-note-head" }, [xe[28] || (xe[28] = g("div", null, [g("strong", { id: "compose-author-note-title" }, "玩家便签"), g("span", null, "只对当前会话生效。")], -1)), g("button", {
          type: "button",
          class: "compose-author-note-back",
          "aria-label": "关闭玩家便签",
          onClick: It
        })]),
        g("div", Pue, [
          g("label", Rue, [xe[29] || (xe[29] = g("span", null, "便签内容", -1)), Ln(g("textarea", {
            "onUpdate:modelValue": xe[17] || (xe[17] = (Ze) => H.value.prompt = Ze),
            rows: "6"
          }, null, 512), [[Ro, H.value.prompt]])]),
          g("div", Nue, [xe[30] || (xe[30] = g("span", null, "插入位置", -1)), g("div", $ue, [(O(), F(Je, null, lt(Oe, (Ze) => g("button", {
            key: Ze.value,
            type: "button",
            class: ze({ selected: Number(H.value.position) === Ze.value }),
            onClick: (zt) => Ht({ position: Ze.value })
          }, K(Ze.label), 11, Due)), 64))])]),
          g("div", Lue, [g("label", Oue, [xe[31] || (xe[31] = g("span", null, "Depth", -1)), Ln(g("input", {
            "onUpdate:modelValue": xe[18] || (xe[18] = (Ze) => H.value.depth = Ze),
            type: "number",
            min: "0",
            max: "9999"
          }, null, 512), [[
            Ro,
            H.value.depth,
            void 0,
            { number: !0 }
          ]])]), g("label", Fue, [xe[32] || (xe[32] = g("span", null, "插入频率", -1)), Ln(g("input", {
            "onUpdate:modelValue": xe[19] || (xe[19] = (Ze) => H.value.interval = Ze),
            type: "number",
            min: "0",
            max: "9999"
          }, null, 512), [[
            Ro,
            H.value.interval,
            void 0,
            { number: !0 }
          ]])])]),
          g("div", Bue, [xe[33] || (xe[33] = g("span", null, "Role", -1)), g("div", Uue, [(O(), F(Je, null, lt(L, (Ze) => g("button", {
            key: Ze.value,
            type: "button",
            class: ze({ selected: Number(H.value.role) === Ze.value }),
            onClick: (zt) => Ht({ role: Ze.value })
          }, K(Ze.label), 11, que)), 64))])]),
          g("label", Hue, [Ln(g("input", {
            "onUpdate:modelValue": xe[20] || (xe[20] = (Ze) => H.value.scan = Ze),
            type: "checkbox"
          }, null, 512), [[f$, H.value.scan]]), xe[34] || (xe[34] = g("span", null, "参与世界书扫描", -1))])
        ]),
        g("footer", zue, [g("span", null, K(se.value || "0 禁用，1 每次，N 每 N 次用户输入"), 1), g("button", {
          type: "submit",
          disabled: W.value
        }, K(W.value ? "保存中..." : "保存"), 9, Wue)])
      ], 32)], 32)) : ge("", !0),
      Ce.value ? (O(), F("div", Gue, [g("section", Vue, [g("header", Kue, [g("h2", Jue, K(qe.value), 1), g("button", {
        type: "button",
        class: "chat-quick-settings-close",
        title: "关闭",
        "aria-label": "关闭",
        onClick: Ye
      }, " × ")]), g("div", { class: ze(["chat-quick-settings-body", Be.value]) }, [Ce.value === "api" ? (O(), F("div", {
        key: 0,
        ref: Mt,
        class: "tavern-api-settings chat-quick-api-root"
      })) : (O(), F("div", {
        key: 1,
        class: ze(["settings-layout chat-quick-settings-layout", Be.value])
      }, [g("section", Yue, [Ce.value === "chatPreset" ? (O(), gn(l5, { key: 0 })) : Ce.value === "worldbooks" ? (O(), gn(p5, { key: 1 })) : ge("", !0)])], 2))], 2)])])) : ge("", !0)
    ], 2)) : ge("", !0);
  }
}), Que = Xue;
function Zue(e) {
  return (Array.isArray(e) ? e : []).map((t, n) => {
    const r = t && typeof t == "object" ? t : {};
    return {
      label: String(r.label || `思考 ${n + 1}`).trim() || `思考 ${n + 1}`,
      text: String(r.text || "").trim()
    };
  }).filter((t) => t.text);
}
function jue(e) {
  const t = /* @__PURE__ */ ie(Date.now()), n = re(() => e.managerRuns.value[0] || null), r = re(() => e.managerRuns.value.find((E) => d(E.status)) || n.value), o = re(() => {
    const E = String(r.value?.id || "");
    return e.managerRuns.value.filter((x) => String(x.id || "") !== E).slice(0, e.visibleRunLimit);
  }), s = re(() => {
    const E = r.value ? 1 : 0;
    return Math.max(0, e.managerRuns.value.length - E - o.value.length);
  }), a = re(() => e.managerRuns.value.some((E) => ["queued", "running"].includes(E.status)));
  function i(E = 0) {
    const x = Math.max(0, t.value - Number(E || 0));
    return !E || x < 3e3 ? "刚刚" : l(x);
  }
  function l(E = 0) {
    const x = Math.floor(E / 1e3);
    if (x < 60) return `${x} 秒前`;
    const M = Math.floor(x / 60);
    return M < 60 ? `${M} 分钟前` : `${Math.floor(M / 60)} 小时前`;
  }
  function u(E = 0) {
    const x = Math.max(0, t.value - Number(E || 0));
    return !E || x < 3e3 ? "刚开始" : l(x).replace(/前$/, "");
  }
  function d(E = "") {
    return ["queued", "running"].includes(String(E || ""));
  }
  function f(E = "") {
    return {
      queued: "排队",
      running: "运行中",
      completed: "完成",
      failed: "失败",
      cancelled: "已取消",
      superseded: "已作废",
      rolled_back: "未采用"
    }[E] || E || "未知";
  }
  function m(E = "") {
    const x = typeof E == "string" ? E : String(E?.status || "");
    if (typeof E != "string" && x === "running") {
      const M = Number(E.updatedAt) || Number(E.createdAt) || 0, N = Math.max(0, t.value - M);
      if (N > 3e4) return "danger";
      if (N > 9e3) return "warn";
    }
    return ["failed", "rolled_back"].includes(x) ? "danger" : ["cancelled", "superseded"].includes(x) ? "muted" : ["queued", "running"].includes(x) ? "active" : x === "completed" ? "done" : "neutral";
  }
  function v(E) {
    return E.status === "queued" ? "等待后台模型" : E.status === "running" ? "后台模型运行中" : [String(E.provider || "").trim(), String(E.model || "").trim()].filter(Boolean).join(" / ") || "未记录模型信息";
  }
  function y(E) {
    const x = String(E.status || ""), M = Number(E.updatedAt) || Number(E.createdAt) || 0;
    if (x === "queued") return `等待开始 · 建立于 ${i(E.createdAt)}`;
    if (x === "running") {
      const N = Math.max(0, t.value - M), q = u(E.createdAt);
      return N <= 9e3 ? `还活着 · 已运行 ${q} · 正在等 API/工具返回` : N <= 3e4 ? `等待中 · 已运行 ${q} · 上次心跳 ${i(M)}` : `可能卡住 · 已运行 ${q} · ${i(M)}没有心跳`;
    }
    return [
      "completed",
      "failed",
      "cancelled",
      "superseded",
      "rolled_back"
    ].includes(x) ? `已结束 · ${i(M)}` : M ? `最后更新 ${i(M)}` : "";
  }
  function T(E) {
    const x = String(E.error || "").trim(), M = {
      manager_memory_tool_failed: "记忆工具返回失败，系统没有采用这次结果。",
      manager_memory_tool_required: "本轮没有完成必要的记忆维护，系统没有采用这次结果。",
      manager_aborted: "本次后台工作已停止，系统没有采用这次结果。",
      manager_source_messages_changed: "原文消息已经变化，系统没有采用这次结果。",
      manager_epoch_expired: "后台工作已过期，系统没有采用这次结果。"
    };
    return /工具轮次达到上限/.test(x) ? `原因：${x} 系统没有采用这次结果。` : x && M[x] ? `原因：${M[x]}` : E.status === "rolled_back" ? "原因：本次结果已撤回，当前记忆、地图和事件保持上一版。" : x ? `原因：${x}` : "";
  }
  function C(E) {
    const x = Number(E.assistantOrder), M = Number(E.userOrder);
    return [
      Number.isInteger(x) && x >= 0 ? `第 ${x + 1} 楼` : `第 ${Math.max(0, Number(E.turn) || 0)} 次维护`,
      Number.isInteger(M) && M >= 0 && Number.isInteger(x) && x >= 0 ? `原文 ${M + 1}-${x + 1} 楼` : "",
      E.trigger === "after_turn" ? "自动记忆" : E.trigger === "manager_chat" ? "助手聊天" : String(E.trigger || "")
    ].filter(Boolean).join(" · ");
  }
  function A(E) {
    const x = Array.isArray(E.changedFiles) ? E.changedFiles : [];
    return E.status === "queued" ? "记忆：等待开始" : E.status === "running" ? "记忆：正在整理" : E.status === "failed" ? x.length ? `记忆：已写入 ${x.length} 份档案，但本轮失败` : "记忆：未完成" : ["cancelled", "superseded"].includes(E.status) ? "记忆：已停止，未采用本轮结果" : E.status === "rolled_back" ? "记忆：未采用，已撤回本轮写入" : x.length ? `记忆：已更新 ${x.length} 份档案` : "记忆：没有写入文件";
  }
  function I(E) {
    const x = Array.isArray(E.changedStates) ? E.changedStates : [];
    return E.status === "queued" ? "地图：等待开始" : E.status === "running" ? "地图：正在判断本轮有没有空间变化" : E.status === "failed" ? x.length ? `地图：已写入 ${x.length} 份状态，但本轮失败` : "地图：未完成" : ["cancelled", "superseded"].includes(E.status) ? "地图：已停止，未采用本轮结果" : E.status === "rolled_back" ? "地图：未采用，已撤回本轮更新" : x.length ? `地图：已更新 ${x.length} 份状态` : "地图：本轮没有明确空间变化，未更新";
  }
  function R(E) {
    const x = Array.isArray(E.changedTasks) ? E.changedTasks : [];
    return E.status === "queued" ? "事件：等待开始" : E.status === "running" ? "事件：正在判断线索池" : E.status === "failed" ? x.length ? `事件：已写入 ${x.length} 条线索，但本轮失败` : "事件：未完成" : ["cancelled", "superseded"].includes(E.status) ? "事件：已停止，未采用本轮结果" : E.status === "rolled_back" ? "事件：未采用，已撤回本轮更新" : x.length ? `事件：已更新 ${x.length} 条线索` : "事件：本轮没有新线索";
  }
  function $(E) {
    if (!E) return "";
    if (Array.isArray(E)) {
      const x = E.filter((N) => N && typeof N == "object" && N.ok === !1).length, M = E.filter((N) => N && typeof N == "object" && String(N.status || "") === "running").length;
      return M ? `工具调用 ${E.length} 次 · ${M} 个运行中` : x ? `工具调用 ${E.length} 次 · ${x} 次失败` : `工具调用 ${E.length} 次 · 全部成功`;
    }
    if (typeof E == "object") {
      const x = E, M = [
        "calls",
        "toolCalls",
        "steps",
        "trace"
      ].map((q) => Array.isArray(x[q]) ? x[q].length : 0).filter((q) => q > 0);
      if (M.length) return `工具调用 ${Math.max(...M)} 次`;
      const N = Object.keys(x).length;
      return N ? `工具记录 ${N} 项` : "";
    }
    return "有工具记录";
  }
  function p(E) {
    if (!Array.isArray(E)) return [];
    const x = /* @__PURE__ */ new Set(), M = /* @__PURE__ */ new Set();
    return E.map((N, q) => {
      const z = N && typeof N == "object" ? N : {}, J = String(z.name || "工具").trim() || "工具", G = String(z.status || "").trim(), oe = z.ok !== !1, B = Math.max(0, Number(z.elapsedMs) || (Number(z.startedAt) && Number(z.finishedAt) ? Number(z.finishedAt) - Number(z.startedAt) : 0)), X = Math.max(1, Number(z.round) || 1), de = String(z.preface || "").trim(), fe = `${X}
${de}`, ve = de && !x.has(fe) ? de : "";
      de && x.add(fe);
      const Ce = Zue(z.thoughts), Ue = `${X}
${JSON.stringify(Ce)}`, le = Ce.length && !M.has(Ue) ? Ce : [];
      return Ce.length && M.add(Ue), {
        id: String(z.id || `${J}-${q}`),
        round: X,
        name: J,
        status: G,
        ok: oe,
        args: String(z.args || "").trim(),
        path: String(z.path || "").trim(),
        summary: String(z.summary || z.error || "").trim(),
        error: String(z.error || "").trim(),
        preface: ve,
        thoughts: le,
        elapsedLabel: B ? `${(B / 1e3).toFixed(1)}s` : ""
      };
    });
  }
  function b(E) {
    return E.status === "running" ? "运行中" : E.ok === !1 ? "失败" : "已返回";
  }
  function w(E) {
    return E.status === "running" ? "is-running" : E.ok === !1 ? "is-error" : "is-resolved";
  }
  return {
    archivedManagerRuns: o,
    currentManagerWorkRun: r,
    formatRunActivityLine: y,
    formatRunIssueLine: T,
    formatRunInputLine: C,
    formatRunMapLine: I,
    formatRunMemoryLine: A,
    formatRunModelLine: v,
    formatRunTaskLine: R,
    hiddenManagerRunCount: s,
    isManagerRunLive: d,
    managerBusy: a,
    managerRunTone: m,
    managerStatusClock: t,
    managerStatusLabel: f,
    managerToolStatusLabel: b,
    managerToolTone: w,
    managerToolTraceItems: p,
    toolTraceSummary: $
  };
}
function ece(e) {
  let t = 0;
  function n(v = "") {
    return v === "stale" ? "过期" : "可用";
  }
  function r(v) {
    return v ? "contentLength" in v && Number.isFinite(Number(v.contentLength)) ? Math.max(0, Number(v.contentLength) || 0) : "content" in v ? Math.max(0, String(v.content || "").length) : 0 : 0;
  }
  function o(v) {
    return `${n(v.status)} · ${r(v)} 字`;
  }
  function s(v) {
    const y = String(v?.content || "");
    e.memoryEditorLoadedPath.value = String(v?.path || ""), e.memoryEditorBaseContent.value = y, e.memoryEditorDraft.value = y, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "";
  }
  function a(v = !0) {
    t += 1, v && (e.selectedMemoryFileRecord.value = null);
  }
  async function i(v = "") {
    const y = String(e.selectedSessionId.value || "").trim(), T = String(v || "").trim(), C = ++t;
    if (!y || !T)
      return a(), null;
    const A = await Ma(y, T);
    return C !== t ? null : (e.selectedMemoryFileRecord.value = A, A);
  }
  function l(v = "") {
    const y = String(v || "").trim();
    !y || y === e.selectedMemoryFilePath.value || e.memoryEditorDirty.value && !window.confirm("当前记忆档案有未保存修改，切换后会放弃这份草稿。继续切换？") || (e.selectedMemoryFilePath.value = y);
  }
  async function u() {
    const v = e.selectedMemoryFileEntry.value;
    if (!(!e.selectedSessionId.value || !v)) {
      e.memoryEditorStatus.value = "保存中";
      try {
        await t1(e.selectedSessionId.value, v.path, e.memoryEditorDraft.value, { source: "user" }), await e.refreshRecords(e.selectedSessionId.value), e.memoryEditorLoadedPath.value = v.path, e.memoryEditorBaseContent.value = e.memoryEditorDraft.value, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "";
      } catch (y) {
        e.memoryEditorStatus.value = y instanceof Error ? y.message : String(y || "保存失败");
      }
    }
  }
  function d() {
    e.memoryEditorDocumentAvailable.value && (e.memoryEditorMode.value = "edit", Qt(() => {
      document.querySelector('[data-memory-editor-textarea="true"]')?.focus();
    }));
  }
  function f() {
    e.memoryEditorDocumentAvailable.value && (e.memoryEditorMode.value = "preview");
  }
  function m() {
    e.memoryEditorDocumentAvailable.value && (e.memoryEditorDraft.value = e.memoryEditorBaseContent.value, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "");
  }
  return {
    discardMemoryDraft: m,
    enterMemoryEditMode: d,
    formatMemoryFileMeta: o,
    invalidateMemoryFileRecordLoad: a,
    loadMemoryFileIntoEditor: s,
    loadSelectedMemoryFileRecord: i,
    memoryFileStatusLabel: n,
    previewMemoryDraft: f,
    saveSelectedMemoryFile: u,
    selectMemoryFile: l
  };
}
var tce = {
  class: "prompt-inspector-modal",
  tabindex: "-1"
}, nce = { class: "prompt-inspector-head" }, rce = {
  class: "prompt-inspector-tabs",
  "aria-label": "API 请求视图"
}, oce = { class: "prompt-inspector-body" }, sce = {
  key: 0,
  class: "prompt-inspector-view"
}, ace = { class: "prompt-inspector-summary" }, ice = {
  key: 0,
  class: "prompt-request-json"
}, lce = {
  key: 1,
  class: "prompt-empty-state"
}, uce = {
  key: 1,
  class: "prompt-inspector-view"
}, cce = { class: "prompt-simulate-panel" }, dce = ["value"], fce = {
  key: 0,
  class: "muted compact"
}, hce = {
  key: 1,
  class: "error"
}, pce = {
  key: 2,
  class: "prompt-request-json"
}, mce = /* @__PURE__ */ un({
  __name: "TavernRequestLogModal",
  props: {
    tab: {},
    lastRequestRawJson: {},
    lastRequestSnapshot: {},
    simulateInput: {},
    simulateStatus: {},
    simulateError: {},
    simulateJson: {}
  },
  emits: [
    "close",
    "update:tab",
    "update:simulateInput",
    "simulate"
  ],
  setup(e, { emit: t }) {
    const n = t;
    function r(o) {
      n("update:simulateInput", o.target.value);
    }
    return (o, s) => (O(), F("div", {
      class: "prompt-inspector-overlay",
      role: "dialog",
      "aria-modal": "true",
      onClick: s[4] || (s[4] = sn((a) => o.$emit("close"), ["self"])),
      onKeydown: s[5] || (s[5] = Vs((a) => o.$emit("close"), ["esc"]))
    }, [g("section", tce, [
      g("header", nce, [s[6] || (s[6] = g("div", { class: "prompt-inspector-heading" }, [g("h2", null, "请求日志")], -1)), g("button", {
        type: "button",
        class: "prompt-inspector-close",
        "aria-label": "关闭日志",
        onClick: s[0] || (s[0] = (a) => o.$emit("close"))
      }, " 关闭 ")]),
      g("div", rce, [g("button", {
        type: "button",
        class: ze({ active: e.tab === "history" }),
        onClick: s[1] || (s[1] = (a) => o.$emit("update:tab", "history"))
      }, " 上次调用 ", 2), g("button", {
        type: "button",
        class: ze({ active: e.tab === "simulate" }),
        onClick: s[2] || (s[2] = (a) => o.$emit("update:tab", "simulate"))
      }, " 模拟发送 ", 2)]),
      g("div", oce, [e.tab === "history" ? (O(), F("section", sce, [g("div", ace, [
        g("span", null, K(e.lastRequestRawJson ? "有记录" : "暂无记录"), 1),
        g("span", null, K(e.lastRequestSnapshot?.requestKind || "history"), 1),
        g("span", null, K(e.lastRequestSnapshot?.providerLabel || e.lastRequestSnapshot?.provider || "未调用"), 1),
        g("span", null, K(e.lastRequestSnapshot?.model || "未选择模型"), 1)
      ]), e.lastRequestRawJson ? (O(), F("pre", ice, K(e.lastRequestRawJson), 1)) : (O(), F("p", lce, " 暂无请求历史。 "))])) : e.tab === "simulate" ? (O(), F("section", uce, [
        g("div", cce, [g("div", null, [s[7] || (s[7] = g("label", { for: "request-simulate-input" }, "模拟本轮发言", -1)), g("textarea", {
          id: "request-simulate-input",
          value: e.simulateInput,
          rows: "5",
          placeholder: "写一句要模拟发送的话",
          onInput: r
        }, null, 40, dce)]), g("button", {
          type: "button",
          onClick: s[3] || (s[3] = (a) => o.$emit("simulate"))
        }, " 生成请求 ")]),
        e.simulateStatus ? (O(), F("p", fce, K(e.simulateStatus), 1)) : ge("", !0),
        e.simulateError ? (O(), F("p", hce, K(e.simulateError), 1)) : ge("", !0),
        e.simulateJson ? (O(), F("pre", pce, K(e.simulateJson), 1)) : ge("", !0)
      ])) : ge("", !0)])
    ])], 32));
  }
}), gce = mce, vce = { class: "xb-sidebar settings-sidebar" }, yce = { class: "panel guide-card" }, _ce = { class: "guide-steps" }, bce = ["onClick"], wce = { class: "guide-label-full" }, Sce = { class: "guide-label-mobile" }, Tce = { key: 0 }, Ece = /* @__PURE__ */ un({
  __name: "TavernSettingsSidebar",
  props: {
    active: {},
    items: {}
  },
  emits: ["select"],
  setup(e, { emit: t }) {
    const n = t;
    return (r, o) => (O(), F("aside", vce, [g("div", yce, [o[0] || (o[0] = g("h2", null, "设置", -1)), g("div", _ce, [(O(!0), F(Je, null, lt(e.items, (s) => (O(), F("button", {
      key: s.key,
      type: "button",
      class: ze(["guide-step", { active: e.active === s.key }]),
      onClick: (a) => n("select", s.key)
    }, [g("strong", null, [g("span", wce, K(s.label), 1), g("span", Sce, K(s.mobileLabel || s.label), 1)]), s.badge ? (O(), F("em", Tce, K(s.badge), 1)) : ge("", !0)], 10, bce))), 128))])])]));
  }
}), Ace = Ece, Cce = { class: "panel step-panel api-workspace" }, kce = /* @__PURE__ */ un({
  __name: "TavernApiSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, apiSettingsRootRef: n } = Va();
    function r(o) {
      n.value = o instanceof HTMLElement ? o : null;
    }
    return (o, s) => Ln((O(), F("div", Cce, [s[0] || (s[0] = g("div", { class: "panel-head" }, [g("div", null, [g("h2", null, "API 配置")])], -1)), g("div", { class: "api-scroll-body" }, [g("div", {
      ref: r,
      class: "tavern-api-settings"
    })])], 512)), [[Iu, S(t) === "api"]]);
  }
}), Ice = kce, xce = { class: "panel step-panel base-settings-workspace" }, Mce = {
  key: 0,
  class: "preset-status-line"
}, Pce = { class: "base-settings-scroll" }, Rce = { class: "base-settings-section" }, Nce = ["value", "disabled"], $ce = {
  key: 0,
  value: ""
}, Dce = ["value"], Lce = { class: "base-user-detail" }, Oce = { class: "base-user-avatar" }, Fce = ["src", "alt"], Bce = { key: 1 }, Uce = { class: "base-user-copy" }, qce = { class: "base-settings-section" }, Hce = { class: "base-setting-row" }, zce = ["value", "disabled"], Wce = { class: "base-setting-row" }, Gce = ["value", "disabled"], Vce = { class: "base-settings-section" }, Kce = { class: "base-setting-row base-setting-row--controller" }, Jce = {
  class: "xb-workspace-controller chat-font-size-controller",
  "aria-label": "聊天字体"
}, Yce = ["disabled", "onClick"], Xce = /* @__PURE__ */ un({
  __name: "TavernBaseSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, baseSettingsLoading: n, baseSettingsSaving: r, baseSettingsStatus: o, currentTavernUser: s, displaySettings: a, shortText: i, switchingTavernUserId: l, switchTavernUser: u, tavernUsers: d, updateDisplaySettingsPatch: f } = Va(), m = re(() => s.value?.name || "未选择 USER"), v = re(() => s.value?.description || "聊天上下文会沿用这位 USER 的身份与头像。"), y = re(() => s.value ? C(s.value) : "U"), T = re(() => s.value?.id || d.value.find((b) => b.active)?.id || "");
    function C(b) {
      return (String(b.name || b.id || "U").trim().slice(0, 1) || "U").toUpperCase();
    }
    function A(b) {
      const w = String(b || "").trim();
      !w || w === T.value || u(w);
    }
    function I(b) {
      const w = Number(b);
      Number.isFinite(w) && f({ hiddenOutsideCount: w });
    }
    function R(b) {
      const w = Number(b);
      Number.isFinite(w) && f({ loadBatchSize: w });
    }
    const $ = {
      small: "小",
      medium: "中",
      large: "大"
    };
    function p(b) {
      a.value.chatFontSize !== b && f({ chatFontSize: b });
    }
    return (b, w) => Ln((O(), F("div", xce, [
      w[10] || (w[10] = g("div", { class: "panel-head preset-page-head" }, [g("div", null, [g("h2", null, "基础设定")])], -1)),
      S(o) ? (O(), F("div", Mce, [g("span", null, K(S(o)), 1)])) : ge("", !0),
      g("div", Pce, [
        g("section", Rce, [
          w[3] || (w[3] = g("h3", null, "USER身份切换", -1)),
          g("select", {
            class: "base-user-select",
            value: T.value,
            disabled: S(n) || !!S(l) || !S(d).length,
            onChange: w[0] || (w[0] = (E) => A(E.target.value))
          }, [S(d).length ? ge("", !0) : (O(), F("option", $ce, K(S(n) ? "正在读取 USER 列表" : "没有可切换的 USER"), 1)), (O(!0), F(Je, null, lt(S(d), (E) => (O(), F("option", {
            key: E.id,
            value: E.id
          }, K(E.name), 9, Dce))), 128))], 40, Nce),
          g("div", Lce, [g("div", Oce, [S(s)?.avatarUrl ? (O(), F("img", {
            key: 0,
            src: S(s).avatarUrl,
            alt: m.value
          }, null, 8, Fce)) : (O(), F("span", Bce, K(y.value), 1))]), g("div", Uce, [g("strong", null, K(m.value), 1), g("p", null, K(S(i)(v.value, 120)), 1)])])
        ]),
        g("section", qce, [
          w[6] || (w[6] = g("h3", null, "楼层隐藏", -1)),
          g("div", Hce, [w[4] || (w[4] = g("div", null, [g("strong", null, "显示楼层"), g("span", null, "数字外楼层将隐藏")], -1)), g("input", {
            value: S(a).hiddenOutsideCount,
            type: "number",
            min: "1",
            max: "20",
            step: "1",
            disabled: S(r),
            onChange: w[1] || (w[1] = (E) => I(E.target.value))
          }, null, 40, zce)]),
          g("div", Wce, [w[5] || (w[5] = g("div", null, [g("strong", null, "翻旧楼"), g("span", null, "每次加载楼数")], -1)), g("input", {
            value: S(a).loadBatchSize,
            type: "number",
            min: "5",
            max: "50",
            step: "5",
            disabled: S(r),
            onChange: w[2] || (w[2] = (E) => R(E.target.value))
          }, null, 40, Gce)])
        ]),
        g("section", Vce, [w[8] || (w[8] = g("h3", null, "字体", -1)), g("div", Kce, [w[7] || (w[7] = g("div", null, [g("strong", null, "聊天字体"), g("span", null, "只影响聊天正文与输入框")], -1)), g("div", Jce, [(O(!0), F(Je, null, lt(S(A$), (E) => (O(), F("button", {
          key: E,
          type: "button",
          class: ze(["xb-layout-button", { "is-active": S(a).chatFontSize === E }]),
          disabled: S(r),
          onClick: (x) => p(E)
        }, K($[E]), 11, Yce))), 128))])])]),
        w[9] || (w[9] = g("section", { class: "base-settings-section" }, [g("h3", null, "上下文管理"), g("div", { class: "base-context-block" }, [g("strong", null, "当前暂不支持调节"), g("p", null, "超过 20 楼时触发压缩并保留最近 10 楼；删楼后窗口内不足 5 楼时，回退窗口补到 10 楼；窗口外记忆由契约中的记忆系统处理。")])], -1))
      ])
    ], 512)), [[Iu, S(t) === "base"]]);
  }
}), Qce = Xce, Zce = { class: "panel-head preset-page-head" }, jce = { class: "panel-pills" }, ede = {
  key: 0,
  class: "pill warning"
}, tde = { class: "pill" }, nde = { class: "preset-command-bar regex-command-bar" }, rde = { class: "preset-source-select regex-group-select" }, ode = ["value"], sde = { class: "archive-search native-search regex-search-field" }, ade = { class: "settings-toolstrip" }, ide = ["aria-expanded"], lde = {
  key: 0,
  class: "regex-create-popover"
}, ude = ["onClick"], cde = {
  key: 0,
  class: "preset-status-line"
}, dde = { class: "native-settings-studio regex-studio" }, fde = { class: "native-list-panel regex-group-panel regex-library-panel" }, hde = { class: "assistant-preset-nav-head" }, pde = ["onClick", "onKeydown"], mde = { class: "regex-row-state" }, gde = { class: "regex-row-copy" }, vde = { class: "regex-row-actions" }, yde = ["onClick"], _de = ["onClick"], bde = ["onClick"], wde = {
  key: 0,
  class: "inline-empty-note"
}, Sde = {
  key: 0,
  class: "native-detail-panel regex-detail-panel"
}, Tde = { class: "preset-preview-head" }, Ede = { class: "regex-editor-actions" }, Ade = ["disabled"], Cde = { class: "regex-editor-grid" }, kde = { class: "regex-editor-section regex-pattern-section" }, Ide = { class: "native-form-grid regex-editor-meta" }, xde = ["value"], Mde = ["value"], Pde = { class: "preset-text-field regex-main-field" }, Rde = ["value"], Nde = { class: "preset-text-field regex-support-field" }, $de = ["value"], Dde = { class: "regex-editor-section regex-placement-section" }, Lde = { class: "native-check-row placement-row regex-editor-switches" }, Ode = ["checked", "onChange"], Fde = { class: "regex-editor-section regex-ephemeral-section" }, Bde = { class: "native-check-row regex-editor-switches" }, Ude = { class: "inline-check" }, qde = ["checked"], Hde = { class: "inline-check" }, zde = ["checked"], Wde = { class: "regex-editor-section regex-options-section" }, Gde = { class: "native-check-row regex-editor-switches" }, Vde = { class: "inline-check" }, Kde = ["checked"], Jde = { class: "inline-check" }, Yde = ["checked"], Xde = { class: "native-form-grid regex-advanced-grid" }, Qde = ["value"], Zde = ["value"], jde = ["value"], efe = {
  key: 1,
  class: "empty-note"
}, tfe = /* @__PURE__ */ un({
  __name: "TavernRegexSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, applyActiveRegexScript: n, createRegexScript: r, deleteRegexScript: o, expandRegexGroup: s, linesFromList: a, listFromLines: i, REGEX_GROUP_BATCH_SIZE: l, regexDirty: u, regexDraft: d, regexDraftTypeLabel: f, regexGroups: m, regexGroupsForDisplay: v, regexPlacementLabel: y, regexScriptRows: T, regexSearchText: C, regexStatus: A, saveCurrentRegexScript: I, selectedRegexKey: R, selectedRegexRow: $, selectRegexScript: p, toggleRegexPlacement: b, updateRegexPatch: w } = Va(), E = /* @__PURE__ */ ie(!1), x = Bg("(max-width: 560px)"), M = /* @__PURE__ */ ie(""), N = /* @__PURE__ */ ie(!1), q = re(() => {
      const Ue = M.value;
      return Ue ? v.value.filter((le) => le.key === Ue) : v.value;
    }), z = re(() => m.value.filter((Ue) => Number.isFinite(Ue.scriptType))), J = re(() => t.value === "regex" && (!x.value || E.value));
    function G(Ue) {
      p(Ue), E.value = !0;
    }
    function oe(Ue) {
      r(Ue), N.value = !1, E.value = !0;
    }
    function B() {
      E.value = !1;
    }
    function X() {
      n($.value), B();
    }
    async function de() {
      await I(), u.value || B();
    }
    function fe() {
      N.value = !N.value;
    }
    function ve(Ue) {
      return Ue.key === "scoped" ? "保存到酒馆本体当前角色" : Ue.key === "preset" ? "保存到当前酒馆预设" : "保存到全局正则";
    }
    function Ce(Ue, le) {
      Ue.key !== "Enter" && Ue.key !== " " || (Ue.preventDefault(), G(le));
    }
    return gt(t, (Ue) => {
      Ue !== "regex" && (E.value = !1, N.value = !1);
    }), (Ue, le) => Ln((O(), F("div", { class: ze(["panel step-panel native-workspace", { "is-mobile-editor-open": E.value }]) }, [
      g("div", Zce, [le[13] || (le[13] = g("div", null, [g("h2", null, "正则")], -1)), g("div", jce, [S(u) ? (O(), F("span", ede, "未保存")) : ge("", !0), g("span", tde, K(S(T).length) + " 条", 1)])]),
      g("div", nde, [
        g("label", rde, [Ln(g("select", { "onUpdate:modelValue": le[0] || (le[0] = (me) => M.value = me) }, [le[14] || (le[14] = g("option", { value: "" }, " 全部正则 ", -1)), (O(!0), F(Je, null, lt(S(v), (me) => (O(), F("option", {
          key: me.key,
          value: me.key
        }, K(me.label), 9, ode))), 128))], 512), [[h$, M.value]])]),
        g("label", sde, [Ln(g("input", {
          "onUpdate:modelValue": le[1] || (le[1] = (me) => /* @__PURE__ */ pn(C) ? C.value = me : null),
          type: "search",
          "aria-label": "筛选正则",
          placeholder: "名称、匹配式或替换文本"
        }, null, 512), [[Ro, S(C)]])]),
        g("div", ade, [g("div", { class: ze(["regex-create-menu", { open: N.value }]) }, [g("button", {
          type: "button",
          class: "settings-icon-tool regex-create-trigger",
          title: "新增正则",
          "aria-label": "新增正则",
          "aria-expanded": N.value ? "true" : "false",
          onClick: fe
        }, [...le[15] || (le[15] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [g("path", { d: "M12 5v14" }), g("path", { d: "M5 12h14" })], -1)])], 8, ide), N.value ? (O(), F("div", lde, [(O(!0), F(Je, null, lt(z.value, (me) => (O(), F("button", {
          key: me.key,
          type: "button",
          onClick: (We) => oe(me)
        }, [g("strong", null, K(me.label), 1), g("span", null, K(ve(me)), 1)], 8, ude))), 128))])) : ge("", !0)], 2)])
      ]),
      S(A) ? (O(), F("div", cde, [g("span", null, K(S(A)), 1)])) : ge("", !0),
      g("div", dde, [g("aside", fde, [(O(!0), F(Je, null, lt(q.value, (me) => (O(), F("div", {
        key: me.key,
        class: "regex-group-block"
      }, [
        g("div", hde, [g("strong", null, K(me.label), 1), g("span", null, K(me.allowed === !1 ? "未允许" : `${me.visibleRows.length} / ${me.filteredCount} / ${me.totalCount} 条`), 1)]),
        (O(!0), F(Je, null, lt(me.visibleRows, (We) => (O(), F("div", {
          key: We.key,
          class: ze(["native-list-row regex-list-row", {
            selected: S(R) === We.key,
            disabled: We.script.disabled
          }]),
          role: "button",
          tabindex: "0",
          onClick: (H) => G(We),
          onKeydown: (H) => Ce(H, We)
        }, [
          g("span", mde, K(We.script.disabled ? "停" : "用"), 1),
          g("span", gde, [g("strong", null, K(We.script.scriptName || "未命名正则"), 1), g("small", null, K(We.script.findRegex || "空匹配式"), 1)]),
          g("span", vde, [g("button", {
            type: "button",
            class: "regex-row-icon regex-row-edit-icon",
            title: "编辑",
            "aria-label": "编辑",
            onClick: sn((H) => G(We), ["stop"])
          }, [...le[16] || (le[16] = [g("svg", {
            viewBox: "0 0 24 24",
            "aria-hidden": "true"
          }, [g("path", { d: "M12 20h9" }), g("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])], 8, yde), g("button", {
            type: "button",
            class: "regex-row-icon regex-row-delete-icon",
            title: "删除",
            "aria-label": "删除",
            onClick: sn((H) => S(o)(We), ["stop"])
          }, [...le[17] || (le[17] = [ku('<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M19 6l-1 14a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1L5 6"></path><path d="M10 11v6"></path><path d="M14 11v6"></path></svg>', 1)])], 8, _de)])
        ], 42, pde))), 128)),
        me.hiddenCount ? (O(), F("button", {
          key: 0,
          type: "button",
          class: "native-add-row",
          onClick: (We) => S(s)(me.key)
        }, " 再显示 " + K(Math.min(me.hiddenCount, S(l))) + " 条 ", 9, bde)) : ge("", !0)
      ]))), 128)), S(C) && !q.value.length ? (O(), F("div", wde, " 没有匹配的正则。 ")) : ge("", !0)]), J.value ? (O(), F("section", Sde, [S(R) || S(d).scriptName ? (O(), F(Je, { key: 0 }, [g("div", Tde, [
        g("strong", null, K(S(d).scriptName || "新正则"), 1),
        g("span", null, K(S(f)()), 1),
        g("div", Ede, [g("button", {
          type: "button",
          class: "regex-editor-secondary",
          onClick: X
        }, " 取消 "), g("button", {
          type: "button",
          class: "regex-editor-primary",
          disabled: !S(d).scriptName || !S(u),
          onClick: de
        }, " 保存 ", 8, Ade)])
      ]), g("div", Cde, [
        g("section", kde, [
          le[22] || (le[22] = g("div", { class: "regex-section-title" }, " 查找与替换 ", -1)),
          g("div", Ide, [g("label", null, [le[18] || (le[18] = g("span", null, "名称", -1)), g("input", {
            value: S(d).scriptName || "",
            onInput: le[2] || (le[2] = (me) => S(w)({ scriptName: me.target.value }))
          }, null, 40, xde)]), g("label", null, [le[19] || (le[19] = g("span", null, "匹配", -1)), g("input", {
            value: S(d).findRegex || "",
            spellcheck: "false",
            onInput: le[3] || (le[3] = (me) => S(w)({ findRegex: me.target.value }))
          }, null, 40, Mde)])]),
          g("label", Pde, [le[20] || (le[20] = g("span", null, "替换为", -1)), g("textarea", {
            value: S(d).replaceString || "",
            rows: "5",
            spellcheck: "false",
            onInput: le[4] || (le[4] = (me) => S(w)({ replaceString: me.target.value }))
          }, null, 40, Rde)]),
          g("label", Nde, [le[21] || (le[21] = g("span", null, "裁剪字符串", -1)), g("textarea", {
            value: S(a)(S(d).trimStrings),
            rows: "3",
            spellcheck: "false",
            onInput: le[5] || (le[5] = (me) => S(w)({ trimStrings: S(i)(me.target.value) }))
          }, null, 40, $de)])
        ]),
        g("section", Dde, [le[23] || (le[23] = g("div", { class: "regex-section-title" }, " 作用范围 ", -1)), g("div", Lde, [(O(), F(Je, null, lt([
          1,
          2,
          3,
          5,
          6
        ], (me) => g("label", {
          key: me,
          class: "inline-check"
        }, [g("input", {
          type: "checkbox",
          checked: (S(d).placement || []).includes(me),
          onChange: (We) => S(b)(me, We.target.checked)
        }, null, 40, Ode), g("span", null, K(S(y)(me)), 1)])), 64))])]),
        g("section", Fde, [le[26] || (le[26] = g("div", { class: "regex-section-title" }, " 表层替换 ", -1)), g("div", Bde, [g("label", Ude, [g("input", {
          type: "checkbox",
          checked: S(d).markdownOnly === !0,
          onChange: le[6] || (le[6] = (me) => S(w)({ markdownOnly: me.target.checked }))
        }, null, 40, qde), le[24] || (le[24] = g("span", null, "只改显示", -1))]), g("label", Hde, [g("input", {
          type: "checkbox",
          checked: S(d).promptOnly === !0,
          onChange: le[7] || (le[7] = (me) => S(w)({ promptOnly: me.target.checked }))
        }, null, 40, zde), le[25] || (le[25] = g("span", null, "只改提示词", -1))])])]),
        g("section", Wde, [
          le[33] || (le[33] = g("div", { class: "regex-section-title" }, " 其他选项 ", -1)),
          g("div", Gde, [g("label", Vde, [g("input", {
            type: "checkbox",
            checked: S(d).disabled === !0,
            onChange: le[8] || (le[8] = (me) => S(w)({ disabled: me.target.checked }))
          }, null, 40, Kde), le[27] || (le[27] = g("span", null, "停用", -1))]), g("label", Jde, [g("input", {
            type: "checkbox",
            checked: S(d).runOnEdit === !0,
            onChange: le[9] || (le[9] = (me) => S(w)({ runOnEdit: me.target.checked }))
          }, null, 40, Yde), le[28] || (le[28] = g("span", null, "编辑时执行", -1))])]),
          g("div", Xde, [
            g("label", null, [le[30] || (le[30] = g("span", null, "查找宏", -1)), g("select", {
              value: S(d).substituteRegex ?? 0,
              onChange: le[10] || (le[10] = (me) => S(w)({ substituteRegex: Number(me.target.value) }))
            }, [...le[29] || (le[29] = [
              g("option", { value: 0 }, "不替换", -1),
              g("option", { value: 1 }, "原样替换", -1),
              g("option", { value: 2 }, "转义替换", -1)
            ])], 40, Qde)]),
            g("label", null, [le[31] || (le[31] = g("span", null, "最小深度", -1)), g("input", {
              type: "number",
              value: S(d).minDepth ?? "",
              onInput: le[11] || (le[11] = (me) => S(w)({ minDepth: me.target.value === "" ? null : Number(me.target.value) }))
            }, null, 40, Zde)]),
            g("label", null, [le[32] || (le[32] = g("span", null, "最大深度", -1)), g("input", {
              type: "number",
              value: S(d).maxDepth ?? "",
              onInput: le[12] || (le[12] = (me) => S(w)({ maxDepth: me.target.value === "" ? null : Number(me.target.value) }))
            }, null, 40, jde)])
          ])
        ])
      ])], 64)) : (O(), F("div", efe, " 选择一条正则，或在左侧新建。 "))])) : ge("", !0)])
    ], 2)), [[Iu, S(t) === "regex"]]);
  }
}), nfe = tfe, rfe = { class: "panel step-panel preset-workspace" }, ofe = { class: "panel-head preset-page-head" }, sfe = { class: "panel-pills" }, afe = {
  key: 0,
  class: "pill warning"
}, ife = { class: "preset-command-bar assistant-preset-command-bar" }, lfe = { class: "assistant-preset-picker" }, ufe = ["value"], cfe = {
  key: 0,
  value: ""
}, dfe = ["value"], ffe = { class: "assistant-preset-toolstrip" }, hfe = ["disabled"], pfe = ["disabled"], mfe = ["disabled"], gfe = {
  key: 0,
  class: "preset-status-line"
}, vfe = { class: "assistant-preset-studio" }, yfe = { class: "assistant-preset-item-list archive-item-list" }, _fe = ["onClick"], bfe = { class: "assistant-preset-detail-panel archive-editor-panel" }, wfe = { class: "assistant-preset-line-head" }, Sfe = {
  key: 0,
  class: "assistant-preset-detail-fields"
}, Tfe = ["value"], Efe = {
  key: 1,
  type: "button",
  class: "assistant-preset-empty-add",
  disabled: ""
}, Afe = /* @__PURE__ */ un({
  __name: "TavernAssistantPresetSettingsPanel",
  setup(e) {
    const { activeAssistantPresetId: t, activeSettingsWorkspace: n, assistantPreset: r, assistantPresetDirty: o, assistantPresetItems: s, assistantPresets: a, assistantPresetStatus: i, createAssistantPreset: l, deleteCurrentAssistantPreset: u, importAssistantPreset: d, saveCurrentAssistantPreset: f, selectAssistantPreset: m, selectAssistantPresetItem: v, selectedAssistantPresetItem: y, shortText: T, updateAssistantPresetPatch: C, updateSelectedAssistantPresetItem: A } = Va(), I = re(() => String(t.value || r.value.id || "").trim()), R = re(() => a.value.find((x) => x.id === I.value) || null), $ = /* @__PURE__ */ ie(null);
    function p() {
      const x = String(r.value.name || "").trim() || "助手预设", M = window.prompt("输入预设名称：", x);
      if (M === null) return;
      const N = String(M || "").trim();
      !N || N === x || C({ name: N });
    }
    function b() {
      $.value?.click();
    }
    async function w(x) {
      const M = x.target, N = M?.files?.[0];
      if (N)
        try {
          const q = await N.text();
          await d(JSON.parse(q));
        } catch (q) {
          const z = q instanceof Error ? q.message : String(q || "导入失败");
          window.alert(z || "导入失败");
        } finally {
          M && (M.value = "");
        }
    }
    function E() {
      const x = String(r.value.name || "assistant-preset").trim() || "assistant-preset", M = {
        kind: "littlewhitebox-assistant-preset",
        version: 1,
        exportedAt: (/* @__PURE__ */ new Date()).toISOString(),
        preset: r.value
      }, N = new Blob([JSON.stringify(M, null, 2)], { type: "application/json;charset=utf-8" }), q = URL.createObjectURL(N), z = document.createElement("a");
      z.href = q, z.download = `${x.replace(/[\\/:*?"<>|]+/g, "-").replace(/\s+/g, " ").trim() || "assistant-preset"}.json`, document.body.appendChild(z), z.click(), z.remove(), URL.revokeObjectURL(q);
    }
    return (x, M) => Ln((O(), F("div", rfe, [
      g("div", ofe, [M[5] || (M[5] = g("div", null, [g("h2", null, "助手预设")], -1)), g("div", sfe, [S(o) ? (O(), F("span", afe, "未保存")) : ge("", !0)])]),
      g("div", ife, [g("label", lfe, [g("select", {
        value: I.value,
        onChange: M[0] || (M[0] = (N) => S(m)(N.target.value))
      }, [S(a).length ? ge("", !0) : (O(), F("option", cfe, " 没有可用助手预设 ")), (O(!0), F(Je, null, lt(S(a), (N) => (O(), F("option", {
        key: N.id,
        value: N.id
      }, K(N.name) + K(N.isBuiltIn ? " · 内置" : ""), 9, dfe))), 128))], 40, ufe)]), g("div", ffe, [
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "改名",
          "aria-label": "改名",
          onClick: p
        }, [...M[6] || (M[6] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [g("path", { d: "M12 20h9" }), g("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])]),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "保存",
          "aria-label": "保存",
          disabled: !S(o),
          onClick: M[1] || (M[1] = (...N) => S(f) && S(f)(...N))
        }, [...M[7] || (M[7] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M5 21h14a1 1 0 0 0 1-1V7.5L16.5 4H5a1 1 0 0 0-1 1v15a1 1 0 0 0 1 1Z" }),
          g("path", { d: "M8 21v-7h8v7" }),
          g("path", { d: "M8 4v5h7" })
        ], -1)])], 8, hfe),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "删除",
          "aria-label": "删除",
          disabled: !R.value || R.value.id === S("littlewhitebox-assistant-default"),
          onClick: M[2] || (M[2] = (...N) => S(u) && S(u)(...N))
        }, [...M[8] || (M[8] = [ku('<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M19 6l-1 14a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1L5 6"></path><path d="M10 11v6"></path><path d="M14 11v6"></path></svg>', 1)])], 8, pfe),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "新增",
          "aria-label": "新增",
          onClick: M[3] || (M[3] = (...N) => S(l) && S(l)(...N))
        }, [...M[9] || (M[9] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [g("path", { d: "M12 5v14" }), g("path", { d: "M5 12h14" })], -1)])]),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "导入",
          "aria-label": "导入",
          onClick: b
        }, [...M[10] || (M[10] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M12 4v11" }),
          g("path", { d: "m7 11 5 5 5-5" }),
          g("path", { d: "M4 20h16" })
        ], -1)])]),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "导出",
          "aria-label": "导出",
          disabled: !I.value,
          onClick: E
        }, [...M[11] || (M[11] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M12 20V9" }),
          g("path", { d: "m17 13-5-5-5 5" }),
          g("path", { d: "M4 20h16" })
        ], -1)])], 8, mfe),
        g("input", {
          ref_key: "importInputRef",
          ref: $,
          class: "assistant-preset-import-input",
          type: "file",
          accept: "application/json,.json",
          onChange: w
        }, null, 544)
      ])]),
      S(i) ? (O(), F("div", gfe, [g("span", null, K(S(i)), 1)])) : ge("", !0),
      g("div", vfe, [g("aside", yfe, [M[12] || (M[12] = g("div", { class: "assistant-preset-nav-head" }, [g("strong", null, "维护规则")], -1)), (O(!0), F(Je, null, lt(S(s), (N) => (O(), F("button", {
        key: N.id,
        type: "button",
        class: ze(["assistant-preset-nav-row", { selected: S(y)?.id === N.id }]),
        onClick: (q) => S(v)(N.id)
      }, [g("strong", null, K(N.label), 1), g("small", null, K(S(T)(N.content || "未填写职责。", 54)), 1)], 10, _fe))), 128))]), g("section", bfe, [g("div", wfe, [g("div", null, [g("strong", null, K(S(y)?.label || "维护规则"), 1)])]), S(y) ? (O(), F("div", Sfe, [g("label", null, [M[13] || (M[13] = g("span", null, "职责说明", -1)), g("textarea", {
        value: S(y).content,
        rows: "12",
        placeholder: "写这条维护规则应该要求什么、保留什么、避免什么。",
        onInput: M[4] || (M[4] = (N) => S(A)(N.target.value))
      }, null, 40, Tfe)])])) : (O(), F("button", Efe, " 没有可编辑规则 "))])])
    ], 512)), [[Iu, S(n) === "assistantPreset"]]);
  }
}), Cfe = Afe, kfe = { class: "xb-main" }, Ife = /* @__PURE__ */ un({
  __name: "TavernSettingsPage",
  setup(e) {
    const t = Va(), n = Af(), { activeSettingsWorkspace: r, selectSettingsWorkspace: o, settingsNavItems: s } = t, { activeView: a, homeThemeDark: i } = n;
    return (l, u) => S(a) === "settings" ? (O(), F("section", {
      key: 0,
      class: ze(["xb-layout xb-page settings-layout", `is-${S(r)}-workspace`])
    }, [
      En(Ef, {
        "include-home": "",
        "home-last": "",
        dark: S(i),
        onHome: u[0] || (u[0] = (d) => a.value = "home"),
        onToggleTheme: u[1] || (u[1] = (d) => i.value = !S(i))
      }, null, 8, ["dark"]),
      En(Ace, {
        active: S(r),
        items: S(s),
        onSelect: S(o)
      }, null, 8, [
        "active",
        "items",
        "onSelect"
      ]),
      g("section", kfe, [
        S(r) === "api" ? (O(), gn(Ice, { key: 0 })) : ge("", !0),
        S(r) === "chatPreset" ? (O(), gn(l5, { key: 1 })) : ge("", !0),
        S(r) === "worldbooks" ? (O(), gn(p5, { key: 2 })) : ge("", !0),
        S(r) === "regex" ? (O(), gn(nfe, { key: 3 })) : ge("", !0),
        S(r) === "assistantPreset" ? (O(), gn(Cfe, { key: 4 })) : ge("", !0),
        S(r) === "base" ? (O(), gn(Qce, { key: 5 })) : ge("", !0)
      ])
    ], 2)) : ge("", !0);
  }
}), xfe = Ife, Mfe = { chat: { exclude: [
  "embedding",
  "embed",
  "rerank",
  "reranker",
  "tts",
  "speech",
  "audio",
  "whisper",
  "transcription",
  "stt",
  "image",
  "sdxl",
  "flux",
  "moderation"
] } }, Pfe = Object.freeze([
  "claude-opus-4-7",
  "claude-opus-4-6",
  "claude-opus-4-5",
  "claude-opus-4-5-20251101",
  "claude-sonnet-4-6",
  "claude-sonnet-4-5",
  "claude-sonnet-4-5-20250929",
  "claude-opus-4-1",
  "claude-opus-4-1-20250805",
  "claude-opus-4-0",
  "claude-opus-4-20250514",
  "claude-sonnet-4-0",
  "claude-sonnet-4-20250514"
]);
function Qo(e, t, n = "") {
  if (e.replaceChildren(), n) {
    const r = document.createElement("option");
    r.value = "", r.textContent = n, e.appendChild(r);
  }
  t.forEach((r) => {
    const o = document.createElement("option");
    o.value = r.value, o.textContent = r.label, e.appendChild(o);
  });
}
function Xd(e = []) {
  const t = [...new Set(e.filter(Boolean).map((o) => String(o).trim()).filter(Boolean))], n = Mfe.chat, r = t.filter((o) => {
    const s = o.toLowerCase();
    return !n.exclude.some((a) => s.includes(a));
  });
  return r.length ? r : t;
}
function Gh(e = "") {
  return e === "delegate" ? "delegate" : "main";
}
function Eu(e) {
  return String(e || "").trim().replace(/\/+$/, "");
}
function Rfe(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function Ml(e = "") {
  return e === "openai-compatible" || e === "sillytavern-openai-compatible";
}
function u0(e = "") {
  return e === "anthropic" || e === "sillytavern-claude";
}
function Nfe(e = "") {
  return e === "sillytavern-claude" ? R0 : e === "sillytavern-google" ? N0 : xu;
}
function Qd(e = []) {
  return [...new Set(e.filter(Boolean).map((t) => String(t).trim()).filter(Boolean))];
}
function $fe(e) {
  const t = Eu(e);
  if (!t) return [];
  if (t.endsWith("/v1")) {
    const n = t.slice(0, -3);
    return Qd([
      `${t}/models`,
      `${n}/v1/models`,
      `${n}/models`
    ]);
  }
  return Qd([`${t}/v1/models`, `${t}/models`]);
}
function m5(e) {
  const t = Eu(e);
  if (!t) return [];
  if (t.endsWith("/v1")) {
    const n = t.slice(0, -3);
    return Qd([
      `${t}/models`,
      `${n}/v1/models`,
      `${n}/models`
    ]);
  }
  return Qd([`${t}/v1/models`, `${t}/models`]);
}
function Dfe(e, t) {
  const n = Eu(e);
  if (!n) return [];
  const r = n.endsWith("/v1beta") ? n.slice(0, -7) : n;
  return Qd([
    `${n}/models?key=${encodeURIComponent(t)}`,
    `${n}/models`,
    `${r}/v1beta/models?key=${encodeURIComponent(t)}`,
    `${r}/v1beta/models`,
    `${r}/models?key=${encodeURIComponent(t)}`,
    `${r}/models`
  ]);
}
function Lfe(e, t) {
  const n = [
    e?.error?.message,
    e?.message,
    e?.detail,
    e?.details,
    e?.error
  ].find((r) => typeof r == "string" && r.trim());
  return n ? n.trim() : String(t || "").trim().slice(0, 160);
}
async function Ofe(e, t = {}) {
  const n = await fetch(e, t), r = await n.text();
  let o = null, s = null;
  try {
    o = r ? JSON.parse(r) : {};
  } catch (a) {
    s = a;
  }
  return {
    ok: n.ok,
    status: n.status,
    url: e,
    data: o,
    rawText: r,
    parseError: s,
    errorSnippet: Lfe(o, r)
  };
}
function Ffe(e) {
  return Xd((e?.data || []).map((t) => String(t?.id || "").trim()).filter(Boolean));
}
function g5(e) {
  return Xd((e?.data || []).map((t) => String(t?.id || "").trim()).filter(Boolean));
}
function Bfe(e) {
  return Xd((e?.models || e?.data || []).map((t) => String(t?.id || t?.name || "")).map((t) => t.split("/").pop() || "").filter(Boolean));
}
async function kp({ urls: e, requestOptionsList: t, extractModels: n, providerLabel: r }) {
  let o = null;
  for (const s of e) for (const a of t) {
    const i = await Ofe(s, a);
    if (!i.ok) {
      o = i;
      continue;
    }
    if (i.parseError) {
      o = {
        ...i,
        errorSnippet: "返回的不是 JSON"
      };
      continue;
    }
    const l = n(i.data);
    if (l.length) return l;
    o = {
      ...i,
      errorSnippet: "返回成功，但模型列表为空"
    };
  }
  if (o) {
    const s = o.url ? ` (${o.url})` : "", a = o.errorSnippet ? `：${o.errorSnippet}` : "";
    throw new Error(`${r} 拉取模型失败：${o.status || "unknown"}${a}${s}`);
  }
  throw new Error(`${r} 拉取模型失败：未获取到模型列表。`);
}
async function Ufe(e) {
  const t = String(e.apiKey || "").trim(), n = Eu(e.baseUrl || ""), r = Eu(n || mI.claude);
  if (t && r) try {
    return await kp({
      urls: m5(r),
      requestOptionsList: [{ headers: {
        "x-api-key": t,
        "anthropic-version": "2023-06-01",
        Accept: "application/json"
      } }],
      extractModels: g5,
      providerLabel: "Anthropic"
    });
  } catch (o) {
    if (n) throw o;
  }
  return [...Pfe];
}
async function WC(e) {
  const t = e.provider, n = Eu(e.baseUrl || ""), r = String(e.apiKey || "").trim();
  if (t === "sillytavern-claude") return Xd(await Ufe(e));
  if (Rfe(t)) return Xd(await N9(e, Nfe(t)));
  if (!r) throw new Error("请先填写 API Key。");
  if (!n) throw new Error("请先填写 Base URL。");
  return t === "google" ? await kp({
    urls: Dfe(n, r),
    requestOptionsList: [
      { headers: {
        Accept: "application/json",
        "x-goog-api-key": r
      } },
      { headers: {
        Accept: "application/json",
        Authorization: `Bearer ${r}`
      } },
      { headers: { Accept: "application/json" } }
    ],
    extractModels: Bfe,
    providerLabel: "Google AI"
  }) : u0(t) ? await kp({
    urls: m5(n),
    requestOptionsList: [{ headers: {
      "x-api-key": r,
      "anthropic-version": "2023-06-01",
      Accept: "application/json"
    } }],
    extractModels: g5,
    providerLabel: "Anthropic"
  }) : await kp({
    urls: $fe(n),
    requestOptionsList: [{ headers: {
      Authorization: `Bearer ${r}`,
      Accept: "application/json"
    } }],
    extractModels: Ffe,
    providerLabel: t === "openai-responses" ? "OpenAI Responses" : "OpenAI-Compatible"
  });
}
function qfe(e) {
  return e instanceof Error ? e.message : String(e || "unknown_error");
}
function Hfe(e = {}) {
  const { state: t, render: n, showToast: r, createRequestId: o = (H = "req") => `${H}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`, saveConfig: s, describeError: a = qfe, getRuntimeSummaryText: i } = e;
  function l() {
    t.configFormSyncPending = !0;
  }
  function u(H, W = "main") {
    const se = String(H || "").trim() || "openai-compatible";
    return W === "delegate" ? `delegate:${se}` : se;
  }
  function d(H, W = "main") {
    return t.pullStateByProvider?.[u(H, W)] || {
      status: "idle",
      message: ""
    };
  }
  function f(H, W, se = "main") {
    t.pullStateByProvider = {
      ...t.pullStateByProvider || {},
      [u(H, se)]: W
    };
  }
  function m(H, W, se = "main") {
    t.modelOptionsByProvider = {
      ...t.modelOptionsByProvider || {},
      [u(H, se)]: Array.isArray(W) ? W : []
    };
  }
  function v(H, W = "main") {
    const se = u(H, W);
    return Array.isArray(t.modelOptionsByProvider?.[se]) ? t.modelOptionsByProvider[se] : [];
  }
  function y(H, W) {
    const se = t.config?.presets || {}, ke = kn(H || W || "默认");
    return se[ke] ? ke : W && se[W] ? W : Object.keys(se)[0] || "默认";
  }
  function T(H, W) {
    const se = y(H, vw), ke = W && typeof W == "object" ? W : Yr(), Pe = ke.provider || "openai-compatible", Le = Kr(ke.modelConfigs || {}), Y = Le[Pe] || {};
    return {
      delegatePresetName: se,
      delegateProvider: Pe,
      delegateModelConfigs: Le,
      delegateBaseUrl: String(Y.baseUrl || ""),
      delegateModel: String(Y.model || ""),
      delegateApiKey: String(Y.apiKey || ""),
      delegateTemperature: Vr(Y.temperature, 0.2),
      delegateSendTemperature: iu(Y),
      delegateReasoningEnabled: !!Y.reasoningEnabled,
      delegateReasoningEffort: Gr(Y.reasoningEffort),
      delegateToolMode: Y.toolMode || "native"
    };
  }
  function C(H = "openai-compatible", W = {}) {
    const se = Kr(W || {})[H] || {};
    return {
      baseUrl: String(se.baseUrl || ""),
      model: String(se.model || ""),
      apiKey: String(se.apiKey || ""),
      temperature: Vr(se.temperature, 0.2),
      sendTemperature: iu(se),
      reasoningEnabled: !!se.reasoningEnabled,
      reasoningEffort: Gr(se.reasoningEffort),
      toolMode: se.toolMode || "native"
    };
  }
  function A(H = "openai-compatible", W = {}) {
    const se = Kr(W || {})[H] || {};
    return {
      delegateBaseUrl: String(se.baseUrl || ""),
      delegateModel: String(se.model || ""),
      delegateApiKey: String(se.apiKey || ""),
      delegateTemperature: Vr(se.temperature, 0.2),
      delegateSendTemperature: iu(se),
      delegateReasoningEnabled: !!se.reasoningEnabled,
      delegateReasoningEffort: Gr(se.reasoningEffort),
      delegateToolMode: se.toolMode || "native"
    };
  }
  function I(H, W, se = t.config) {
    const ke = kn(H || "默认"), Pe = W && typeof W == "object" ? W : Yr(), Le = Pe.provider || "openai-compatible", Y = Kr(Pe.modelConfigs || {}), te = C(Le, Y), he = y(se?.delegatePresetName, ke), we = T(he, se?.delegateConfig && typeof se.delegateConfig == "object" ? se.delegateConfig : (se?.presets || {})[he] || Pe);
    return {
      currentPresetName: ke,
      presetDraftName: ke,
      provider: Le,
      modelConfigs: Y,
      ...te,
      tavilyApiKey: String(se?.tavilyApiKey || ""),
      tavilyBaseUrl: ho(se?.tavilyBaseUrl || "https://api.tavily.com"),
      permissionMode: Yl(Pe.permissionMode),
      jsApiPermission: va(se?.jsApiPermission),
      ...we
    };
  }
  function R() {
    if (t.configDraft) return t.configDraft;
    const H = kn(t.config?.currentPresetName || "默认");
    return t.configDraft = I(H, (t.config?.presets || {})[H] || Yr()), t.configDraft;
  }
  function $(H) {
    const W = R(), se = H.querySelector("#xb-assistant-provider")?.value || W.provider || "openai-compatible", ke = H.querySelector("#xb-assistant-delegate-provider")?.value || W.delegateProvider || "openai-compatible", Pe = {
      baseUrl: H.querySelector("#xb-assistant-base-url")?.value.trim() || "",
      model: H.querySelector("#xb-assistant-model")?.value.trim() || "",
      apiKey: H.querySelector("#xb-assistant-api-key")?.value.trim() || "",
      temperature: Vr(H.querySelector("#xb-assistant-temperature")?.value, W.temperature ?? 0.2),
      sendTemperature: H.querySelector("#xb-assistant-send-temperature")?.checked ?? !!(W.sendTemperature ?? !0),
      reasoningEnabled: H.querySelector("#xb-assistant-reasoning-enabled")?.checked || !1,
      reasoningEffort: Gr(H.querySelector("#xb-assistant-reasoning-effort")?.value),
      toolMode: Ml(se) ? H.querySelector("#xb-assistant-tool-mode")?.value || W.toolMode || "native" : void 0
    }, Le = {
      baseUrl: H.querySelector("#xb-assistant-delegate-base-url")?.value.trim() ?? W.delegateBaseUrl ?? "",
      model: H.querySelector("#xb-assistant-delegate-model")?.value.trim() ?? W.delegateModel ?? "",
      apiKey: H.querySelector("#xb-assistant-delegate-api-key")?.value.trim() ?? W.delegateApiKey ?? "",
      temperature: Vr(H.querySelector("#xb-assistant-delegate-temperature")?.value, W.delegateTemperature ?? 0.2),
      sendTemperature: H.querySelector("#xb-assistant-delegate-send-temperature")?.checked ?? !!(W.delegateSendTemperature ?? !0),
      reasoningEnabled: H.querySelector("#xb-assistant-delegate-reasoning-enabled")?.checked ?? !!W.delegateReasoningEnabled,
      reasoningEffort: Gr(H.querySelector("#xb-assistant-delegate-reasoning-effort")?.value || W.delegateReasoningEffort),
      toolMode: Ml(ke) ? H.querySelector("#xb-assistant-delegate-tool-mode")?.value || W.delegateToolMode || "native" : void 0
    }, Y = {
      ...Kr(W.modelConfigs || {}),
      [se]: {
        ...Kr(W.modelConfigs || {})[se] || {},
        ...Pe
      }
    }, te = {
      ...Kr(W.delegateModelConfigs || {}),
      [ke]: {
        ...Kr(W.delegateModelConfigs || {})[ke] || {},
        ...Le
      }
    };
    return {
      ...W,
      currentPresetName: W.currentPresetName,
      presetDraftName: kn(H.querySelector("#xb-assistant-preset-name")?.value),
      provider: se,
      modelConfigs: Y,
      baseUrl: Pe.baseUrl,
      model: Pe.model,
      apiKey: Pe.apiKey,
      temperature: Pe.temperature,
      sendTemperature: Pe.sendTemperature,
      reasoningEnabled: Pe.reasoningEnabled,
      reasoningEffort: Pe.reasoningEffort,
      toolMode: Pe.toolMode || W.toolMode || "native",
      tavilyApiKey: H.querySelector("#xb-assistant-tavily-api-key")?.value.trim() || "",
      tavilyBaseUrl: ho(W.tavilyBaseUrl || "https://api.tavily.com"),
      permissionMode: Yl(H.querySelector("#xb-assistant-permission-mode")?.value || W.permissionMode),
      jsApiPermission: va(H.querySelector("#xb-assistant-jsapi-permission")?.value || W.jsApiPermission),
      delegatePresetName: y(H.querySelector("#xb-assistant-delegate-preset-select")?.value || W.delegatePresetName, W.currentPresetName),
      delegateProvider: ke,
      delegateModelConfigs: te,
      delegateBaseUrl: Le.baseUrl,
      delegateModel: Le.model,
      delegateApiKey: Le.apiKey,
      delegateTemperature: Le.temperature,
      delegateSendTemperature: Le.sendTemperature,
      delegateReasoningEnabled: Le.reasoningEnabled,
      delegateReasoningEffort: Le.reasoningEffort,
      delegateToolMode: Le.toolMode || W.delegateToolMode || "native"
    };
  }
  function p(H) {
    return t.configDraft = $(H), t.configDraft;
  }
  function b(H = R()) {
    return u0(H.provider) ? 32e3 : null;
  }
  function w(H = R()) {
    return {
      baseUrl: String(H.baseUrl || ""),
      model: String(H.model || ""),
      apiKey: String(H.apiKey || ""),
      temperature: Vr(H.temperature, 0.2),
      sendTemperature: !!(H.sendTemperature ?? !0),
      reasoningEnabled: !!H.reasoningEnabled,
      reasoningEffort: Gr(H.reasoningEffort),
      toolMode: Ml(H.provider) ? H.toolMode || "native" : void 0
    };
  }
  function E(H = R()) {
    return {
      baseUrl: String(H.delegateBaseUrl || ""),
      model: String(H.delegateModel || ""),
      apiKey: String(H.delegateApiKey || ""),
      temperature: Vr(H.delegateTemperature, 0.2),
      sendTemperature: !!(H.delegateSendTemperature ?? !0),
      reasoningEnabled: !!H.delegateReasoningEnabled,
      reasoningEffort: Gr(H.delegateReasoningEffort),
      toolMode: Ml(H.delegateProvider) ? H.delegateToolMode || "native" : void 0
    };
  }
  function x(H = R()) {
    const W = H.delegateProvider || "openai-compatible", se = Kr(H.delegateModelConfigs || {});
    return {
      provider: W,
      modelConfigs: {
        ...se,
        [W]: {
          ...se[W] || {},
          ...E(H)
        }
      }
    };
  }
  function M(H = R()) {
    return {
      provider: H.provider || "openai-compatible",
      baseUrl: H.baseUrl || "",
      model: H.model || "",
      apiKey: H.apiKey || "",
      tavilyApiKey: H.tavilyApiKey || "",
      tavilyBaseUrl: ho(H.tavilyBaseUrl || "https://api.tavily.com"),
      temperature: H.sendTemperature === !1 ? void 0 : Vr(H.temperature, 0.2),
      sendTemperature: !!(H.sendTemperature ?? !0),
      maxTokens: b(H),
      timeoutMs: vC,
      toolMode: H.toolMode || "native",
      reasoningEnabled: !!H.reasoningEnabled,
      reasoningEffort: Gr(H.reasoningEffort)
    };
  }
  function N(H = R()) {
    return {
      provider: H.delegateProvider || "openai-compatible",
      baseUrl: H.delegateBaseUrl || "",
      model: H.delegateModel || "",
      apiKey: H.delegateApiKey || "",
      tavilyApiKey: H.tavilyApiKey || "",
      tavilyBaseUrl: ho(H.tavilyBaseUrl || "https://api.tavily.com"),
      temperature: H.delegateSendTemperature === !1 ? void 0 : Vr(H.delegateTemperature, 0.2),
      sendTemperature: !!(H.delegateSendTemperature ?? !0),
      maxTokens: u0(H.delegateProvider) ? 32e3 : null,
      timeoutMs: vC,
      toolMode: H.delegateToolMode || "native",
      reasoningEnabled: !!H.delegateReasoningEnabled,
      reasoningEffort: Gr(H.delegateReasoningEffort)
    };
  }
  function q(H = {}) {
    const W = (H.role === "delegate", R());
    return H.role === "delegate" ? N(W) : M(W);
  }
  function z(H) {
    R(), t.configDraft = {
      ...t.configDraft,
      presetDraftName: kn(H.querySelector("#xb-assistant-preset-name")?.value)
    };
  }
  function J(H = R(), W = H.provider || "openai-compatible", se = "main") {
    const ke = d(W, se);
    return typeof i == "function" ? i({
      state: t,
      draft: H,
      provider: W,
      pullState: ke,
      providerLabel: wC(W)
    }) : `预设「${H.currentPresetName || "默认"}」 · ${wC(W)}`;
  }
  function G(H, W, se) {
    const ke = H?.querySelector?.(W);
    if (!ke) return;
    const Pe = String(se?.status || "idle"), Le = String(se?.message || "").trim();
    ke.textContent = Le, ke.hidden = !Le, ke.classList.toggle("is-loading", Pe === "loading"), ke.classList.toggle("is-success", Pe === "success"), ke.classList.toggle("is-error", Pe === "error");
  }
  function oe(H) {
    if (!H) return;
    const W = Gh(t.configPage);
    t.configPage = W, H.querySelectorAll("[data-config-page]").forEach((se) => {
      const ke = Gh(se?.dataset?.configPage) === W;
      se.classList.toggle("is-active", ke), se.setAttribute("aria-selected", ke ? "true" : "false");
    }), H.querySelectorAll("[data-config-page-panel]").forEach((se) => {
      const ke = Gh(se?.dataset?.configPagePanel) === W;
      se.toggleAttribute("hidden", !ke);
    }), H.querySelector("#xb-assistant-delete-preset")?.toggleAttribute("hidden", W === "delegate");
  }
  function B(H) {
    if (!t.config) return;
    oe(H);
    const W = R(), se = W.provider || "openai-compatible", ke = v(se), Pe = W.delegateProvider || "openai-compatible", Le = v(Pe, "delegate"), Y = H.querySelector("#xb-assistant-tool-mode-wrap"), te = H.querySelector("#xb-assistant-tool-mode"), he = H.querySelector("#xb-assistant-reasoning-enabled"), we = H.querySelector("#xb-assistant-reasoning-effort-wrap"), Te = H.querySelector("#xb-assistant-reasoning-effort"), Ie = H.querySelector("#xb-assistant-permission-mode"), Ne = H.querySelector("#xb-assistant-jsapi-permission"), Oe = H.querySelector("#xb-assistant-model-pulled"), L = H.querySelector("#xb-assistant-preset-select"), P = H.querySelector("#xb-assistant-preset-name"), Q = H.querySelector("#xb-assistant-delegate-preset-select"), V = H.querySelector("#xb-assistant-delegate-provider"), ae = H.querySelector("#xb-assistant-delegate-base-url"), Se = H.querySelector("#xb-assistant-delegate-model"), Me = H.querySelector("#xb-assistant-delegate-api-key"), Ke = H.querySelector("#xb-assistant-tavily-api-key"), qe = H.querySelector("#xb-assistant-delegate-model-pulled"), Be = H.querySelector("#xb-assistant-delegate-tool-mode-wrap"), Ee = H.querySelector("#xb-assistant-delegate-tool-mode"), Ye = H.querySelector("#xb-assistant-delegate-reasoning-enabled"), je = H.querySelector("#xb-assistant-delegate-reasoning-effort-wrap"), It = H.querySelector("#xb-assistant-delegate-reasoning-effort");
    if (!L || !P) return;
    const Ht = (t.config.presetNames || []).map((yt) => ({
      value: yt,
      label: yt
    }));
    Qo(L, Ht), L.value = W.currentPresetName || t.config.currentPresetName || "默认", Q && (Qo(Q, Ht), Q.value = y(W.delegatePresetName, W.currentPresetName)), P.value = W.presetDraftName || W.currentPresetName || "默认", H.querySelector("#xb-assistant-provider").value = se, H.querySelector("#xb-assistant-base-url").value = W.baseUrl || "", H.querySelector("#xb-assistant-model").value = W.model || "", H.querySelector("#xb-assistant-api-key").value = W.apiKey || "", H.querySelector("#xb-assistant-temperature").value = String(Vr(W.temperature, 0.2)), H.querySelector("#xb-assistant-send-temperature").checked = !!(W.sendTemperature ?? !0), Ke && (Ke.value = W.tavilyApiKey || ""), Y.style.display = Ml(se) ? "" : "none", Qo(te, yC), te.value = W.toolMode || "native", Ie && (Qo(Ie, pQ), Ie.value = Yl(W.permissionMode)), Ne && (Qo(Ne, mQ), Ne.value = va(W.jsApiPermission)), Qo(Te, t0), he.checked = !!W.reasoningEnabled, Te.value = Gr(W.reasoningEffort), we.style.display = he.checked ? "" : "none", Qo(Oe, ke.map((yt) => ({
      value: yt,
      label: yt
    })), "手动填写"), Oe.value = ke.includes(W.model) ? W.model : "", V && (V.value = Pe), ae && (ae.value = W.delegateBaseUrl || ""), Se && (Se.value = W.delegateModel || ""), Me && (Me.value = W.delegateApiKey || "");
    const At = H.querySelector("#xb-assistant-delegate-temperature"), Mt = H.querySelector("#xb-assistant-delegate-send-temperature");
    At && (At.value = String(Vr(W.delegateTemperature, 0.2))), Mt && (Mt.checked = !!(W.delegateSendTemperature ?? !0)), Be && (Be.style.display = Ml(Pe) ? "" : "none"), Ee && (Qo(Ee, yC), Ee.value = W.delegateToolMode || "native"), It && (Qo(It, t0), It.value = Gr(W.delegateReasoningEffort)), Ye && (Ye.checked = !!W.delegateReasoningEnabled), je && (je.style.display = W.delegateReasoningEnabled ? "" : "none"), qe && (Qo(qe, Le.map((yt) => ({
      value: yt,
      label: yt
    })), "手动填写"), qe.value = Le.includes(W.delegateModel) ? W.delegateModel : ""), G(H, "#xb-assistant-model-pull-status", d(se)), G(H, "#xb-assistant-delegate-model-pull-status", d(Pe, "delegate"));
    const Ut = H.querySelector("#xb-assistant-runtime");
    if (Ut) {
      const yt = t.configPage === "delegate";
      Ut.textContent = J(yt ? {
        ...W,
        currentPresetName: "分身",
        provider: Pe
      } : W, yt ? Pe : se, yt ? "delegate" : "main");
    }
  }
  function X(H) {
    if (typeof s != "function") return;
    const W = s(H);
    W && typeof W.catch == "function" && W.catch((se) => {
      r?.(a(se));
    });
  }
  function de(H, W, se) {
    H.querySelector(W)?.addEventListener("click", () => {
      const ke = H.querySelector(se);
      ke && (ke.type = ke.type === "password" ? "text" : "password");
    });
  }
  function fe(H) {
    return {
      workspaceFileName: H?.workspaceFileName || "",
      jsApiPermission: va(H?.jsApiPermission),
      tavilyApiKey: String(H?.tavilyApiKey || ""),
      tavilyBaseUrl: ho(H?.tavilyBaseUrl || "https://api.tavily.com"),
      currentPresetName: H?.currentPresetName || "默认",
      delegatePresetName: H?.delegatePresetName || H?.currentPresetName || "默认",
      delegateConfig: H?.delegateConfig || {},
      presets: H?.presets || {}
    };
  }
  function ve(H, W = {}) {
    const se = p(H), ke = kn(W.presetName || se.presetDraftName), Pe = kn(se.currentPresetName || t.config?.currentPresetName || "默认"), Le = (t.config?.presets || {})[Pe] || Yr(), Y = Kr(se.modelConfigs || Le.modelConfigs || {}), te = {
      ...Le,
      provider: se.provider,
      permissionMode: Yl(se.permissionMode),
      modelConfigs: {
        ...Y,
        [se.provider]: {
          ...Y[se.provider] || {},
          ...w(se)
        }
      }
    }, he = { ...t.config?.presets || {} };
    W.renameCurrentPreset && ke !== Pe && delete he[Pe], he[ke] = te, t.config = wd({
      ...t.config,
      jsApiPermission: va(se.jsApiPermission),
      tavilyApiKey: String(se.tavilyApiKey || ""),
      tavilyBaseUrl: ho(se.tavilyBaseUrl || "https://api.tavily.com"),
      currentPresetName: ke,
      delegatePresetName: y(se.delegatePresetName, ke),
      delegateConfig: x(se),
      presets: he
    }), t.configDraft = I(ke, te, t.config), l(), X({
      requestId: o(W.requestPrefix || "save-config"),
      config: t.config,
      payload: fe(t.config)
    });
  }
  function Ce(H, W = "") {
    const se = kn(W || "默认"), ke = typeof window < "u" && typeof window.prompt == "function" ? window.prompt(H, se) : se;
    return ke === null ? "" : kn(ke);
  }
  function Ue(H) {
    const W = Ce("输入新预设名称：", `${p(H).currentPresetName || "默认"} 副本`);
    if (!W) {
      r?.("预设名称不能为空");
      return;
    }
    H.querySelector("#xb-assistant-preset-name").value = W, ve(H, {
      presetName: W,
      requestPrefix: "create-preset"
    });
  }
  function le(H) {
    const W = p(H), se = kn(W.currentPresetName || t.config?.currentPresetName || "默认"), ke = Ce("输入预设名称：", W.presetDraftName || se);
    if (!ke) {
      r?.("预设名称不能为空");
      return;
    }
    ke !== se && (H.querySelector("#xb-assistant-preset-name").value = ke, ve(H, {
      presetName: ke,
      renameCurrentPreset: !0,
      requestPrefix: "rename-preset"
    }));
  }
  function me(H) {
    if (Object.keys(t.config?.presets || {}).length <= 1) {
      r?.("至少要保留一套预设");
      return;
    }
    const W = p(H), se = kn(t.configDraft?.currentPresetName || t.config?.currentPresetName || "默认"), ke = { ...t.config?.presets || {} };
    delete ke[se];
    const Pe = Object.keys(ke)[0] || "默认", Le = ke[Pe] || Yr();
    t.config = wd({
      ...t.config,
      jsApiPermission: va(W.jsApiPermission),
      tavilyApiKey: String(W.tavilyApiKey || t.config?.tavilyApiKey || ""),
      tavilyBaseUrl: ho(W.tavilyBaseUrl || t.config?.tavilyBaseUrl || "https://api.tavily.com"),
      currentPresetName: Pe,
      delegatePresetName: y(W.delegatePresetName, Pe),
      delegateConfig: x(W),
      presets: ke
    }), t.configDraft = I(Pe, Le, t.config), l(), X({
      requestId: o("delete-preset"),
      config: t.config,
      payload: fe(t.config)
    }), n?.();
  }
  function We(H) {
    H?.querySelector?.("#xb-assistant-provider") && (H.querySelector("#xb-assistant-provider").addEventListener("change", (W) => {
      const se = W.currentTarget.value, ke = p(H);
      t.configDraft = {
        ...ke,
        provider: se,
        ...C(se, ke.modelConfigs)
      }, l(), n?.();
    }), H.querySelector("#xb-assistant-preset-select").addEventListener("change", (W) => {
      const se = kn(W.currentTarget.value), ke = (t.config?.presets || {})[se] || Yr(), Pe = p(H);
      t.config = wd({
        ...t.config,
        jsApiPermission: va(Pe.jsApiPermission),
        currentPresetName: se,
        delegatePresetName: y(Pe.delegatePresetName, se),
        delegateConfig: x(Pe)
      }), t.configDraft = I(se, ke, t.config), l(), n?.();
    }), H.querySelector("#xb-assistant-preset-name")?.addEventListener("input", () => {
      z(H);
    }), H.querySelector("#xb-assistant-base-url").addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-model").addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-api-key").addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-temperature")?.addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-send-temperature")?.addEventListener("change", () => {
      p(H);
    }), H.querySelector("#xb-assistant-tavily-api-key")?.addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-model-pulled").addEventListener("change", (W) => {
      const se = W.currentTarget.value;
      se && (H.querySelector("#xb-assistant-model").value = se, p(H));
    }), de(H, "#xb-assistant-toggle-key", "#xb-assistant-api-key"), de(H, "#xb-assistant-toggle-tavily-key", "#xb-assistant-tavily-api-key"), H.querySelector("#xb-assistant-delegate-provider")?.addEventListener("change", (W) => {
      const se = p(H), ke = W.currentTarget.value;
      t.configDraft = {
        ...se,
        delegateProvider: ke,
        ...A(ke, se.delegateModelConfigs)
      }, l(), n?.();
    }), H.querySelector("#xb-assistant-delegate-base-url")?.addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-delegate-model")?.addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-delegate-api-key")?.addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-delegate-temperature")?.addEventListener("input", () => {
      p(H);
    }), H.querySelector("#xb-assistant-delegate-send-temperature")?.addEventListener("change", () => {
      p(H);
    }), H.querySelector("#xb-assistant-delegate-model-pulled")?.addEventListener("change", (W) => {
      const se = W.currentTarget.value;
      if (!se) return;
      const ke = H.querySelector("#xb-assistant-delegate-model");
      ke && (ke.value = se), p(H);
    }), de(H, "#xb-assistant-delegate-toggle-key", "#xb-assistant-delegate-api-key"), H.querySelector("#xb-assistant-reasoning-enabled").addEventListener("change", () => {
      p(H), l(), n?.();
    }), H.querySelector("#xb-assistant-reasoning-effort").addEventListener("change", () => {
      p(H);
    }), H.querySelector("#xb-assistant-tool-mode").addEventListener("change", () => {
      p(H);
    }), H.querySelector("#xb-assistant-delegate-reasoning-enabled")?.addEventListener("change", () => {
      p(H), l(), n?.();
    }), H.querySelector("#xb-assistant-delegate-reasoning-effort")?.addEventListener("change", () => {
      p(H);
    }), H.querySelector("#xb-assistant-delegate-tool-mode")?.addEventListener("change", () => {
      p(H);
    }), H.querySelector("#xb-assistant-permission-mode")?.addEventListener("change", () => {
      p(H);
    }), H.querySelector("#xb-assistant-jsapi-permission")?.addEventListener("change", () => {
      p(H);
    }), H.querySelector("#xb-assistant-delegate-preset-select")?.addEventListener("change", (W) => {
      const se = y(W.currentTarget?.value, t.configDraft?.currentPresetName || t.config?.currentPresetName || "默认"), ke = (t.config?.presets || {})[se] || Yr();
      t.configDraft = {
        ...p(H),
        ...T(se, ke)
      }, l(), n?.();
    }), H.querySelectorAll("[data-config-page]").forEach((W) => {
      W.addEventListener("click", (se) => {
        p(H), t.configPage = Gh(se.currentTarget?.dataset?.configPage), oe(H), B(H);
      });
    }), H.querySelector("#xb-assistant-pull-models").addEventListener("click", async () => {
      p(H), l();
      const W = q();
      f(W.provider, {
        status: "loading",
        message: "正在拉取模型列表…"
      }), n?.();
      try {
        const se = await WC(W);
        m(W.provider, se), f(W.provider, {
          status: "success",
          message: `已拉取 ${se.length} 个模型`
        });
      } catch (se) {
        m(W.provider, []), f(W.provider, {
          status: "error",
          message: a(se)
        });
      }
      l(), n?.();
    }), H.querySelector("#xb-assistant-delegate-pull-models")?.addEventListener("click", async () => {
      p(H), l();
      const W = q({ role: "delegate" });
      f(W.provider, {
        status: "loading",
        message: "正在拉取模型列表…"
      }, "delegate"), n?.();
      try {
        const se = await WC(W);
        m(W.provider, se, "delegate"), f(W.provider, {
          status: "success",
          message: `已拉取 ${se.length} 个模型`
        }, "delegate");
      } catch (se) {
        m(W.provider, [], "delegate"), f(W.provider, {
          status: "error",
          message: a(se)
        }, "delegate");
      }
      l(), n?.();
    }), H.querySelector("#xb-assistant-new-preset")?.addEventListener("click", () => {
      Ue(H);
    }), H.querySelector("#xb-assistant-rename-preset")?.addEventListener("click", () => {
      le(H);
    }), H.querySelector("#xb-assistant-save").addEventListener("click", () => {
      ve(H);
    }), H.querySelector("#xb-assistant-delegate-save")?.addEventListener("click", () => {
      ve(H, { requestPrefix: "save-delegate-config" });
    }), H.querySelector("#xb-assistant-delete-preset").addEventListener("click", () => {
      me(H);
    }));
  }
  return {
    getActiveProviderConfig: q,
    syncConfigToForm: B,
    bindSettingsPanelEvents: We
  };
}
function Ip(e = "") {
  return String(e || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function Rc(e) {
  return `<svg viewBox="0 0 24 24" aria-hidden="true">${{
    add: '<path d="M12 5v14" /><path d="M5 12h14" />',
    rename: '<path d="M12 20h9" /><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" />',
    save: '<path d="M5 21h14a1 1 0 0 0 1-1V7.5L16.5 4H5a1 1 0 0 0-1 1v15a1 1 0 0 0 1 1Z" /><path d="M8 21v-7h8v7" /><path d="M8 4v5h7" />',
    saving: '<path class="xb-assistant-save-spinner" d="M12 3a9 9 0 1 1-8.2 5.3" />',
    success: '<path d="M20 6 9 17l-5-5" />',
    error: '<path d="M18 6 6 18" /><path d="M6 6l12 12" />',
    delete: '<path d="M3 6h18" /><path d="M8 6V4h8v2" /><path d="M19 6l-1 14a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1L5 6" /><path d="M10 11v6" /><path d="M14 11v6" />'
  }[e] || ""}</svg>`;
}
function zfe(e = {}) {
  const t = String(e?.status || "idle");
  return t === "saving" ? "saving" : t === "success" ? "success" : t === "error" ? "error" : "save";
}
function Wfe(e = {}) {
  const t = String(e?.status || "idle");
  return t === "saving" ? {
    className: "xb-assistant-save-button is-saving",
    title: "正在保存配置"
  } : t === "success" ? {
    className: "xb-assistant-save-button is-success",
    title: "配置已保存"
  } : t === "error" ? {
    className: "xb-assistant-save-button is-error",
    title: Ip(e?.error || "保存失败")
  } : {
    className: "xb-assistant-save-button",
    title: "保存配置"
  };
}
function Gfe(e = {}) {
  const { configSave: t = {}, runtimeText: n = "", inlineToastText: r = "", showInlineToast: o = !0, showAssistantPermissions: s = !0, showDelegateSettings: a = !0, activePage: i = "main", delegatePresetHint: l = "DelegateRun 分身会使用这里的独立 API 配置；可以和主助手使用不同 Provider、Base URL、模型和 Tool 调用格式。", isBusy: u = !1, canDeletePreset: d = !0 } = e, f = Wfe(t), m = zfe(t), v = u || String(t?.status || "") === "saving" ? "disabled" : "", y = u || !d ? "disabled" : "", T = i === "delegate" ? "delegate" : "main", C = T === "main", A = T === "delegate", I = s ? `
            <label>
                <span>斜杠命令权限</span>
                <select id="xb-assistant-permission-mode"></select>
            </label>
            <label>
                <span>JavaScript API 权限</span>
                <select id="xb-assistant-jsapi-permission"></select>
            </label>` : "", R = a ? `
            <div class="xb-assistant-config-tabs" role="tablist" aria-label="API 配置分页">
                <button id="xb-assistant-config-tab-main" type="button" class="xb-assistant-config-tab ${C ? "is-active" : ""}" data-config-page="main" role="tab" aria-selected="${C ? "true" : "false"}">主助手 API</button>
                <button id="xb-assistant-config-tab-delegate" type="button" class="xb-assistant-config-tab ${A ? "is-active" : ""}" data-config-page="delegate" role="tab" aria-selected="${A ? "true" : "false"}">分身 API</button>
            </div>` : "", $ = a ? `
            <div class="xb-assistant-config-page" data-config-page-panel="delegate" ${A ? "" : "hidden"}>
                <p class="xb-assistant-config-note">${Ip(l)}</p>
                <div class="xb-assistant-preset-row">
                    <select id="xb-assistant-delegate-preset-select" class="xb-assistant-preset-field" aria-label="已存预设"></select>
                    <div class="xb-assistant-preset-tools is-single" aria-label="分身 API 预设操作">
                        <button id="xb-assistant-delegate-save" type="button" class="xb-assistant-icon-button ${f.className}" title="${f.title}" aria-label="${f.title}" ${v}>${Rc(m)}</button>
                    </div>
                </div>
                <label>
                    <span>Provider</span>
                    <select id="xb-assistant-delegate-provider">
                        <option value="openai-responses">OpenAI Responses</option>
                        <option value="openai-compatible">OpenAI 兼容</option>
                        <option value="sillytavern-openai-compatible">酒馆 OpenAI 兼容</option>
                        <option value="sillytavern-claude">酒馆 Claude</option>
                        <option value="sillytavern-google">酒馆 Google AI</option>
                        <option value="anthropic">Anthropic</option>
                        <option value="google">Google AI</option>
                    </select>
                </label>
                <label>
                    <span>Base URL</span>
                    <input id="xb-assistant-delegate-base-url" type="text" />
                </label>
                <label>
                    <span>API Key</span>
                    <div class="xb-assistant-inline-input">
                        <input id="xb-assistant-delegate-api-key" type="password" />
                        <button id="xb-assistant-delegate-toggle-key" type="button" class="secondary ghost">显示</button>
                    </div>
                </label>
                <label>
                    <span>Model</span>
                    <input id="xb-assistant-delegate-model" type="text" />
                </label>
                <div class="xb-assistant-inline-input xb-assistant-model-row">
                    <label class="xb-assistant-grow">
                        <span>已拉取模型</span>
                        <select id="xb-assistant-delegate-model-pulled">
                            <option value="">手动填写</option>
                        </select>
                    </label>
                    <button id="xb-assistant-delegate-pull-models" type="button" class="secondary" ${u ? "disabled" : ""}>拉取模型</button>
                </div>
                <div class="xb-assistant-inline-status" id="xb-assistant-delegate-model-pull-status" aria-live="polite" hidden></div>
                <div class="xb-assistant-temperature-row">
                    <label>
                        <span>温度</span>
                        <input id="xb-assistant-delegate-temperature" type="number" min="0" max="2" step="0.05" />
                    </label>
                    <label class="xb-assistant-checkbox-row">
                        <span>允许传参</span>
                        <span class="xb-assistant-checkbox-control">
                            <input id="xb-assistant-delegate-send-temperature" type="checkbox" />
                        </span>
                    </label>
                </div>
                <label id="xb-assistant-delegate-tool-mode-wrap">
                    <span>Tool 调用格式</span>
                    <select id="xb-assistant-delegate-tool-mode"></select>
                </label>
                <label class="xb-assistant-checkbox-row">
                    <span>
                        Reasoning参数
                        <small>需 API 支持，否则报错</small>
                    </span>
                    <span class="xb-assistant-checkbox-control">
                        <input id="xb-assistant-delegate-reasoning-enabled" type="checkbox" />
                        <span>开启</span>
                    </span>
                </label>
                <label id="xb-assistant-delegate-reasoning-effort-wrap">
                    <span>思考强度</span>
                    <select id="xb-assistant-delegate-reasoning-effort"></select>
                </label>
            </div>` : "";
  return `
        <section class="xb-assistant-config">
            ${R}
            <div class="xb-assistant-config-page" data-config-page-panel="main" ${C ? "" : "hidden"}>
            <div class="xb-assistant-preset-row">
                <select id="xb-assistant-preset-select" class="xb-assistant-preset-field" aria-label="已存预设"></select>
                <input id="xb-assistant-preset-name" type="hidden" />
                <div class="xb-assistant-preset-tools" aria-label="API 预设操作">
                    <button id="xb-assistant-new-preset" type="button" class="xb-assistant-icon-button" title="新增预设" aria-label="新增预设" ${u ? "disabled" : ""}>${Rc("add")}</button>
                    <button id="xb-assistant-rename-preset" type="button" class="xb-assistant-icon-button" title="重命名预设" aria-label="重命名预设" ${u ? "disabled" : ""}>${Rc("rename")}</button>
                    <button id="xb-assistant-save" type="button" class="xb-assistant-icon-button ${f.className}" title="${f.title}" aria-label="${f.title}" ${v}>${Rc(m)}</button>
                    <button id="xb-assistant-delete-preset" type="button" class="xb-assistant-icon-button" title="删除预设" aria-label="删除预设" ${y}>${Rc("delete")}</button>
                </div>
            </div>
            <label>
                <span>Provider</span>
                <select id="xb-assistant-provider">
                    <option value="openai-responses">OpenAI Responses</option>
                    <option value="openai-compatible">OpenAI 兼容</option>
                    <option value="sillytavern-openai-compatible">酒馆 OpenAI 兼容</option>
                    <option value="sillytavern-claude">酒馆 Claude</option>
                    <option value="sillytavern-google">酒馆 Google AI</option>
                    <option value="anthropic">Anthropic</option>
                    <option value="google">Google AI</option>
                </select>
            </label>
            <label>
                <span>Base URL</span>
                <input id="xb-assistant-base-url" type="text" />
            </label>
            <label>
                <span>API Key</span>
                <div class="xb-assistant-inline-input">
                    <input id="xb-assistant-api-key" type="password" />
                    <button id="xb-assistant-toggle-key" type="button" class="secondary ghost">显示</button>
                </div>
            </label>
            <label>
                <span>Model</span>
                <input id="xb-assistant-model" type="text" />
            </label>
            <div class="xb-assistant-inline-input xb-assistant-model-row">
                <label class="xb-assistant-grow">
                    <span>已拉取模型</span>
                    <select id="xb-assistant-model-pulled">
                        <option value="">手动填写</option>
                    </select>
                </label>
                <button id="xb-assistant-pull-models" type="button" class="secondary" ${u ? "disabled" : ""}>拉取模型</button>
            </div>
            <div class="xb-assistant-inline-status" id="xb-assistant-model-pull-status" aria-live="polite" hidden></div>
            <div class="xb-assistant-temperature-row">
                <label>
                    <span>温度</span>
                    <input id="xb-assistant-temperature" type="number" min="0" max="2" step="0.05" />
                </label>
                <label class="xb-assistant-checkbox-row">
                    <span>允许传参</span>
                    <span class="xb-assistant-checkbox-control">
                        <input id="xb-assistant-send-temperature" type="checkbox" />
                    </span>
                </label>
            </div>
            <label>
                <span>Tavily API Key（全局）</span>
                <div class="xb-assistant-inline-input">
                    <input id="xb-assistant-tavily-api-key" type="password" />
                    <button id="xb-assistant-toggle-tavily-key" type="button" class="secondary ghost">显示</button>
                </div>
            </label>
            <label id="xb-assistant-tool-mode-wrap">
                <span>Tool 调用格式</span>
                <select id="xb-assistant-tool-mode"></select>
            </label>
            ${I}
            <label class="xb-assistant-checkbox-row">
                <span>
                    Reasoning参数
                    <small>需 API 支持，否则报错</small>
                </span>
                <span class="xb-assistant-checkbox-control">
                    <input id="xb-assistant-reasoning-enabled" type="checkbox" />
                    <span>开启</span>
                </span>
            </label>
            <label id="xb-assistant-reasoning-effort-wrap">
                <span>思考强度</span>
                <select id="xb-assistant-reasoning-effort"></select>
            </label>
            </div>
            ${$}
            <div class="xb-assistant-runtime" id="xb-assistant-runtime">${Ip(n)}</div>
            ${o ? `<div class="xb-assistant-toast xb-assistant-toast-inline" id="xb-assistant-toast" aria-live="polite">${Ip(r)}</div>` : ""}
        </section>
    `;
}
var zy = 48, Wy = 48, Gy = 80, Vh = 24, Vy = 60, Vfe = [{
  key: "statePrompt",
  label: "全局记忆",
  summary: "全局状态输出规则"
}, {
  key: "characterPrompt",
  label: "人物记忆",
  summary: "人物长期状态输出规则"
}], Kfe = {
  top: "最前面",
  beforeCharacter: "角色卡前",
  afterCharacter: "角色卡后",
  beforeHistory: "历史前",
  afterHistory: "历史后",
  assistantPrefill: "回复开头"
};
function qn(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function Pl(e) {
  try {
    return JSON.parse(JSON.stringify(e));
  } catch {
    return e;
  }
}
function ya(e = "") {
  return String(e || "").trim().toLocaleLowerCase();
}
function Jfe(e, t) {
  return t ? ya(e).includes(t) : !0;
}
function Ky(e, t = 1600) {
  return e.map((n) => String(n ?? "").slice(0, t)).filter(Boolean).join(`
`);
}
function Rl(e) {
  return JSON.stringify(e || {});
}
function Kh(e) {
  return Array.isArray(e) ? e.map((t) => String(t || "").trim()).filter(Boolean) : [];
}
function $s(e) {
  if (e === "" || e === null || e === void 0) return null;
  const t = Number(e);
  return Number.isFinite(t) ? t : null;
}
function Jy(e) {
  return e === !0 || e === "true" ? !0 : e === !1 || e === "false" ? !1 : null;
}
function Yfe(e) {
  if (e === !0 || e === "true") return !0;
  if (e === !1 || e === "false" || e === "" || e === null || e === void 0) return !1;
  const t = Number(e);
  return Number.isFinite(t) && t > 0 ? Math.floor(t) : !1;
}
function Xfe(e) {
  const t = qn(e), n = Array.isArray(t.entries) ? t.entries.map((r) => {
    const o = qn(r);
    return {
      uid: String(o.uid || ""),
      name: String(o.name || "未命名条目"),
      keys: Array.isArray(o.keys) ? o.keys.map((s) => String(s || "")).filter(Boolean) : [],
      secondaryKeys: Array.isArray(o.secondaryKeys) ? o.secondaryKeys.map((s) => String(s || "")).filter(Boolean) : [],
      contentPreview: String(o.contentPreview || ""),
      enabled: o.enabled !== !1,
      constant: o.constant === !0,
      vectorized: o.vectorized === !0,
      order: Number.isFinite(Number(o.order)) ? Number(o.order) : 100,
      position: Number.isFinite(Number(o.position)) ? Number(o.position) : 0,
      role: Number.isFinite(Number(o.role)) ? Number(o.role) : 0,
      depth: $s(o.depth),
      probability: $s(o.probability)
    };
  }) : [];
  return {
    name: String(t.name || ""),
    entryCount: Number.isFinite(Number(t.entryCount)) ? Number(t.entryCount) : n.length,
    enabledCount: Number.isFinite(Number(t.enabledCount)) ? Number(t.enabledCount) : n.filter((r) => r.enabled).length,
    constantCount: Number.isFinite(Number(t.constantCount)) ? Number(t.constantCount) : n.filter((r) => r.constant).length,
    disabledCount: Number.isFinite(Number(t.disabledCount)) ? Number(t.disabledCount) : n.filter((r) => !r.enabled).length,
    keywordCount: Number.isFinite(Number(t.keywordCount)) ? Number(t.keywordCount) : n.reduce((r, o) => r + o.keys.length + o.secondaryKeys.length, 0),
    totalChars: Number.isFinite(Number(t.totalChars)) ? Number(t.totalChars) : n.reduce((r, o) => r + o.contentPreview.length, 0),
    entries: n
  };
}
function GC(e) {
  const t = qn(e), n = Kh(t.keysecondary), r = Kh(t.secondary_keys), o = n.length ? n : r, s = o, a = t.disable === !0 || t.enabled === !1, i = t.constant === !0, l = !i && t.vectorized === !0;
  return {
    worldbookName: String(t.worldbookName || t.name || "").trim(),
    uid: t.uid === null || t.uid === void 0 ? "" : String(t.uid).trim(),
    comment: String(t.comment ?? ""),
    key: Kh(t.key),
    keysecondary: o,
    secondary_keys: s,
    content: String(t.content ?? ""),
    disable: a,
    enabled: !a,
    constant: i,
    vectorized: l,
    order: Number.isFinite(Number(t.order)) ? Number(t.order) : 100,
    position: Number.isFinite(Number(t.position)) ? Number(t.position) : 0,
    role: Number.isFinite(Number(t.role)) ? Number(t.role) : 0,
    depth: Number(t.position) === 4 ? $s(t.depth) ?? 4 : null,
    probability: $s(t.probability) ?? 100,
    useProbability: t.useProbability !== !1,
    selective: t.selective === !0,
    selectiveLogic: Number.isFinite(Number(t.selectiveLogic)) ? Number(t.selectiveLogic) : 0,
    scanDepth: $s(t.scanDepth),
    caseSensitive: Jy(t.caseSensitive),
    matchWholeWords: Jy(t.matchWholeWords),
    useGroupScoring: Jy(t.useGroupScoring),
    outletName: String(t.outletName ?? ""),
    automationId: String(t.automationId ?? ""),
    ignoreBudget: t.ignoreBudget === !0,
    excludeRecursion: t.excludeRecursion === !0,
    preventRecursion: t.preventRecursion === !0,
    delayUntilRecursion: Yfe(t.delayUntilRecursion),
    group: String(t.group ?? ""),
    groupOverride: t.groupOverride === !0,
    groupWeight: $s(t.groupWeight),
    sticky: $s(t.sticky),
    cooldown: $s(t.cooldown),
    delay: $s(t.delay),
    triggers: Kh(t.triggers),
    matchPersonaDescription: t.matchPersonaDescription === !0,
    matchCharacterDescription: t.matchCharacterDescription === !0,
    matchCharacterPersonality: t.matchCharacterPersonality === !0,
    matchCharacterDepthPrompt: t.matchCharacterDepthPrompt === !0,
    matchScenario: t.matchScenario === !0,
    matchCreatorNotes: t.matchCreatorNotes === !0,
    entryHash: String(t.entryHash || ""),
    revision: String(t.revision || t.entryHash || "")
  };
}
function VC(e) {
  const t = qn(e), n = Array.isArray(t.options) ? t.options.map((r) => String(r || "").trim()).filter(Boolean) : [];
  return {
    options: n,
    selected: Array.isArray(t.selected) ? t.selected.map((r) => String(r || "").trim()).filter((r) => n.includes(r)) : []
  };
}
function Yy(e = {}) {
  const t = qn(e);
  return {
    ...t,
    id: String(t.id || ""),
    scriptName: String(t.scriptName || ""),
    findRegex: String(t.findRegex || ""),
    replaceString: String(t.replaceString || ""),
    trimStrings: Array.isArray(t.trimStrings) ? t.trimStrings.map((n) => String(n || "")).filter(Boolean) : [],
    placement: Array.isArray(t.placement) ? t.placement.map((n) => Number(n)).filter((n) => Number.isFinite(n)) : [],
    disabled: t.disabled === !0,
    markdownOnly: t.markdownOnly === !0,
    promptOnly: t.promptOnly === !0,
    runOnEdit: t.runOnEdit === !0,
    substituteRegex: Number.isFinite(Number(t.substituteRegex)) ? Number(t.substituteRegex) : 0,
    minDepth: t.minDepth === null || t.minDepth === "" || t.minDepth === void 0 ? null : Number(t.minDepth),
    maxDepth: t.maxDepth === null || t.maxDepth === "" || t.maxDepth === void 0 ? null : Number(t.maxDepth)
  };
}
function Qfe(e) {
  const t = qn(e), n = String(t.id || "").trim();
  return n ? {
    id: n,
    name: String(t.name || n).trim() || n,
    avatarUrl: String(t.avatarUrl || "").trim(),
    description: String(t.description || "").trim(),
    active: t.active === !0
  } : null;
}
function Zfe(e) {
  const t = qn(e), n = String(t.currentUserId || "").trim() || null, r = Array.isArray(t.users) ? t.users.map((o) => Qfe(o)).filter((o) => !!o) : [];
  return n && !r.some((o) => o.id === n) && r.unshift({
    id: n,
    name: n,
    avatarUrl: "",
    description: "",
    active: !0
  }), {
    users: r.map((o) => ({
      ...o,
      active: n ? o.id === n : o.active
    })),
    currentUserId: n
  };
}
var jfe = 5e3;
function ehe() {
  const e = String(window.location.hash || "").replace(/^#\/?/, "").split("/")[1];
  return e === "api" || e === "chatPreset" || e === "worldbooks" || e === "regex" || e === "assistantPreset" || e === "base" ? e : "api";
}
function the(e) {
  const t = fx(), n = Fd(), r = /* @__PURE__ */ ie(null), o = /* @__PURE__ */ ie({
    status: "idle",
    requestId: "",
    error: ""
  }), s = /* @__PURE__ */ ie("");
  let a = null, i = null;
  const l = /* @__PURE__ */ ie(t), u = /* @__PURE__ */ ie(t), d = /* @__PURE__ */ ie({}), f = /* @__PURE__ */ ie(""), m = /* @__PURE__ */ ie(JSON.stringify(t)), v = /* @__PURE__ */ ie(""), y = /* @__PURE__ */ ie(n), T = /* @__PURE__ */ ie(n), C = /* @__PURE__ */ ie([]), A = /* @__PURE__ */ ie(""), I = /* @__PURE__ */ ie(""), R = /* @__PURE__ */ ie(JSON.stringify(n)), $ = /* @__PURE__ */ ie(""), p = /* @__PURE__ */ ie("statePrompt"), b = /* @__PURE__ */ ie({}), w = /* @__PURE__ */ ie(""), E = /* @__PURE__ */ ie(""), x = /* @__PURE__ */ ie([]), M = /* @__PURE__ */ ie([]), N = /* @__PURE__ */ ie(""), q = /* @__PURE__ */ ie(!1), z = /* @__PURE__ */ ie(null), J = /* @__PURE__ */ ie(""), G = /* @__PURE__ */ ie(null), oe = /* @__PURE__ */ ie(""), B = /* @__PURE__ */ ie(""), X = /* @__PURE__ */ ie(""), de = /* @__PURE__ */ ie(""), fe = /* @__PURE__ */ ie(zy), ve = /* @__PURE__ */ ie(""), Ce = /* @__PURE__ */ ie(Wy), Ue = /* @__PURE__ */ ie(""), le = /* @__PURE__ */ ie(Gy), me = /* @__PURE__ */ ie(Vh), We = /* @__PURE__ */ ie(""), H = /* @__PURE__ */ ie({}), W = /* @__PURE__ */ ie({}), se = /* @__PURE__ */ ie(""), ke = /* @__PURE__ */ ie({}), Pe = /* @__PURE__ */ ie(Rl(ke.value)), Le = /* @__PURE__ */ ie(""), Y = /* @__PURE__ */ ie([]), te = /* @__PURE__ */ ie(null), he = /* @__PURE__ */ ie(""), we = /* @__PURE__ */ ie(!1), Te = /* @__PURE__ */ ie(!1), Ie = /* @__PURE__ */ ie(""), Ne = /* @__PURE__ */ ie(zl(e.tavernDisplaySettings.value)), Oe = /* @__PURE__ */ ie(Pl(Ne.value));
  let L = 0, P = 0, Q = "", V = 0, ae = 0, Se = 0;
  const Me = {
    config: {},
    configDraft: null,
    configFormSyncPending: !0,
    configPage: "main",
    configSave: o.value,
    pullStateByProvider: {},
    modelOptionsByProvider: {}
  };
  let Ke = null;
  const qe = (U = l.value) => JSON.stringify(U || {}), Be = (U = y.value) => JSON.stringify(U || {}), Ee = re(() => qe(l.value) !== m.value), Ye = re(() => dh(l.value)), je = re(() => Be(y.value) !== R.value), It = re(() => Tw(e.agentConfig.value)), Ht = re(() => It.value.readiness.ok), At = re(() => It.value.readiness.message), Mt = re(() => {
    const U = It.value;
    return `预设「${U.currentPresetName || "默认"}」 · ${U.providerLabel} / ${U.model || "未选择模型"}`;
  }), Ut = re(() => {
    const U = qn(d.value.components), ee = Array.isArray(U.promptManager) ? U.promptManager : [], _e = String(l.value.promptManager?.name || l.value.name || "").trim(), $e = /* @__PURE__ */ new Set();
    return [_e, ...ee].map((ht) => {
      if (typeof ht == "string") return ht.trim();
      const bt = qn(ht);
      return String(bt.name || bt.label || bt.id || "").trim();
    }).filter((ht) => !ht || $e.has(ht) ? !1 : ($e.add(ht), !0)).map((ht) => ({
      name: ht,
      label: ht
    }));
  }), yt = re(() => {
    const U = ya(de.value);
    return U ? Ut.value.filter((ee) => Jfe(ee.label || ee.name, U)) : Ut.value;
  }), et = re(() => {
    const U = yt.value.slice(0, fe.value), ee = String($.value || l.value.promptManager?.name || l.value.name || "").trim();
    if (!ee || U.some(($e) => $e.name === ee)) return U;
    const _e = Ut.value.find(($e) => $e.name === ee);
    return _e ? [_e, ...U] : U;
  }), xe = re(() => Math.max(0, yt.value.length - Math.min(yt.value.length, fe.value))), Ze = re(() => (Array.isArray(b.value.books) ? b.value.books : []).map((U) => {
    const ee = qn(U);
    return {
      name: String(ee.name || "").trim(),
      globalActive: ee.globalActive === !0
    };
  }).filter((U) => U.name)), zt = re(() => Ze.value.find((U) => U.name === w.value) || null), jt = re(() => {
    const U = z.value;
    return !U || U.name !== w.value ? 0 : Math.max(0, U.entryCount - U.entries.length);
  }), Cn = re(() => G.value ? Rl(G.value) !== oe.value : !1), Gn = re(() => X.value === "正在保存"), er = re(() => (Array.isArray(W.value.groups) ? W.value.groups : []).map((U) => {
    const ee = qn(U), _e = Array.isArray(ee.scripts) ? ee.scripts.map(($e) => qn($e)) : [];
    return {
      key: String(ee.key || ""),
      label: String(ee.label || ee.key || ""),
      scriptType: Number(ee.scriptType),
      scripts: _e,
      allowed: ee.allowed === !0
    };
  }).filter((U) => U.key && Number.isFinite(U.scriptType))), wt = re(() => er.value.flatMap((U) => U.scripts.map((ee, _e) => {
    const $e = {
      key: `${U.scriptType}:${String(ee.id || _e)}`,
      groupKey: U.key,
      groupLabel: U.label,
      scriptType: U.scriptType,
      script: ee
    };
    return $e.searchCorpus = ya(Ky([
      $e.groupLabel,
      $e.script.scriptName,
      $e.script.findRegex,
      $e.script.replaceString,
      $e.script.trimStrings,
      $e.script.placement,
      $e.key
    ], 1200)), $e;
  })));
  function nn(U = "") {
    const ee = Number(H.value[U]);
    return Number.isFinite(ee) && ee > 0 ? Math.floor(ee) : Vy;
  }
  function Ft(U = "") {
    const ee = nn(U);
    H.value = {
      ...H.value,
      [U]: ee + Vy
    };
  }
  const yn = re(() => {
    const U = ya(We.value), ee = String(se.value || "").trim();
    return er.value.map((_e) => {
      const $e = wt.value.filter((Gt) => Gt.groupKey === _e.key), ht = U ? $e.filter((Gt) => String(Gt.searchCorpus || "").includes(U)) : $e, bt = nn(_e.key), Kt = ht.slice(0, bt);
      if (ee && !Kt.some((Gt) => Gt.key === ee)) {
        const Gt = $e.find((Ts) => Ts.key === ee);
        Gt && Kt.unshift(Gt);
      }
      return {
        ..._e,
        visibleRows: Kt,
        totalCount: $e.length,
        filteredCount: ht.length,
        hiddenCount: Math.max(0, ht.length - Math.min(ht.length, bt))
      };
    }).filter((_e) => _e.filteredCount || !U);
  }), ot = re(() => wt.value.find((U) => U.key === se.value) || null), ye = re(() => Rl(ke.value) !== Pe.value), dt = re(() => [
    {
      key: "worldbooks",
      label: "世界书",
      mobileLabel: "世界"
    },
    {
      key: "chatPreset",
      label: "聊天预设",
      mobileLabel: "聊天预设",
      badge: Ee.value ? "未保存" : ""
    },
    {
      key: "assistantPreset",
      label: "助手预设",
      mobileLabel: "助手",
      badge: je.value ? "未保存" : ""
    },
    {
      key: "regex",
      label: "正则",
      badge: ye.value ? "未保存" : ""
    },
    {
      key: "api",
      label: "API 配置",
      mobileLabel: "API"
    },
    {
      key: "base",
      label: "基础设定",
      mobileLabel: "基础"
    }
  ]), dn = re(() => Y.value.find((U) => U.id === te.value) || null), Pt = re(() => Vfe.map((U) => ({
    id: U.key,
    key: U.key,
    label: U.label,
    summary: U.summary,
    content: String(y.value[U.key] || "")
  }))), j = re(() => {
    const U = ya(ve.value);
    return U ? C.value.filter((ee) => ya(Ky([
      ee.name,
      ee.description,
      ee.id,
      ee.preset?.name,
      ee.preset?.description
    ], 1200)).includes(U)) : C.value;
  }), ce = re(() => {
    const U = j.value.slice(0, Ce.value), ee = String(A.value || y.value.id || "").trim();
    if (!ee || U.some(($e) => $e.id === ee)) return U;
    const _e = C.value.find(($e) => $e.id === ee);
    return _e ? [_e, ...U] : U;
  }), ne = re(() => Math.max(0, j.value.length - Math.min(j.value.length, Ce.value))), it = re(() => C.value.find((U) => U.id === A.value) || null), Xe = re(() => Pt.value.find((U) => U.id === p.value) || Pt.value[0] || null), Lt = re(() => !!kf()), qt = re(() => {
    const U = rl(), ee = new Map(U.map((bt, Kt) => [String(bt.identifier || bt.id || `prompt-${Kt + 1}`).trim(), bt])), _e = Ug(), $e = /* @__PURE__ */ new Set(), ht = [];
    return _e.forEach((bt) => {
      const Kt = String(bt.identifier || "").trim();
      if (!Kt || $e.has(Kt)) return;
      const Gt = ee.get(Kt) || {
        identifier: Kt,
        name: Kt
      };
      $e.add(Kt), ht.push({
        identifier: Kt,
        name: String(Gt.name || Gt.label || Kt),
        role: String(Gt.role || "system"),
        content: String(Gt.content || ""),
        enabled: bt.enabled !== !1,
        marker: Gt.marker === !0,
        systemPrompt: Gt.system_prompt === !0,
        injectionPosition: Number(Gt.injection_position ?? 0),
        injectionDepth: Number.isFinite(Number(Gt.injection_depth)) ? Number(Gt.injection_depth) : "",
        source: Gt.extension ? "扩展" : Gt.system_prompt ? "系统" : "预设",
        orderEntry: bt,
        prompt: Gt,
        listed: !0,
        searchCorpus: ya(Ky([
          Gt.name || Gt.label || Kt,
          Gt.role || "system",
          Gt.extension ? "扩展" : Gt.system_prompt ? "系统" : "预设",
          Kt,
          Gt.content || ""
        ]))
      });
    }), ht;
  }), Ve = re(() => {
    const U = ya(Ue.value);
    return U ? qt.value.filter((ee) => String(ee.searchCorpus || "").includes(U)) : qt.value;
  }), He = re(() => {
    const U = Ve.value.slice(0, le.value), ee = String(v.value || "").trim();
    if (!ee || U.some(($e) => $e.identifier === ee)) return U;
    const _e = qt.value.find(($e) => $e.identifier === ee);
    return _e ? [_e, ...U] : U;
  }), Re = re(() => Math.max(0, Ve.value.length - Math.min(Ve.value.length, le.value))), _t = re(() => new Map(qt.value.map((U, ee) => [U.identifier, ee]))), wn = re(() => qt.value.find((U) => U.identifier === v.value) || qt.value[0] || null), so = re(() => kf() ? "当前角色顺序" : "未读取到当前角色顺序"), Pr = re(() => (l.value.sections || []).map((U, ee) => ({
    ...U,
    previewId: U.id || `chat-preset-section-${ee}`,
    previewLabel: U.label || U.source || `提示词 ${ee + 1}`,
    previewPlacement: U.source === "promptManager" ? U.marker ? "酒馆标记" : "酒馆顺序" : Kfe[U.placement || "beforeHistory"] || U.placement || "历史前",
    sectionIndex: ee,
    chars: String(U.content || "").length
  })).filter((U) => (U.content || U.marker) && U.enabled !== !1)), Cf = re(() => Pr.value.reduce((U, ee) => U + ee.chars, 0));
  function Ka(U) {
    if (!U) return "";
    const ee = U.uid !== void 0 && U.uid !== null && String(U.uid).trim() ? String(U.uid).trim() : U.order !== void 0 && U.order !== null && String(U.order).trim() ? String(U.order).trim() : "name" in U ? String(U.name || "").trim() : "";
    return `${String(w.value || "").trim()}:${ee}`;
  }
  function Pn(U) {
    return !!B.value && B.value === Ka(U);
  }
  function Rn(U) {
    return _t.value.get(U) ?? -1;
  }
  function ys() {
    return qn(l.value.promptManager);
  }
  function ra() {
    return qn(ys().rawPreset);
  }
  function rl() {
    const U = ra(), ee = ys();
    return (Array.isArray(U.prompts) ? U.prompts : Array.isArray(ee.prompts) ? ee.prompts : []).map((_e) => qn(_e)).filter((_e) => String(_e.identifier || _e.id || "").trim());
  }
  function ol() {
    const U = ra(), ee = ys();
    return (Array.isArray(U.prompt_order) ? U.prompt_order : Array.isArray(ee.promptOrder) ? ee.promptOrder : []).map((_e) => qn(_e));
  }
  function Ug() {
    const U = ys(), ee = Array.isArray(U.activeOrder) ? U.activeOrder.map((ht) => qn(ht)) : [];
    if (ee.length) return ee;
    const _e = String(U.activeCharacterId ?? "").trim();
    if (!_e) return [];
    const $e = ol().find((ht) => String(ht.character_id ?? "") === _e);
    return Array.isArray($e?.order) ? $e.order.map((ht) => qn(ht)) : [];
  }
  function kf() {
    return String(ys().activeCharacterId ?? "").trim();
  }
  function sl(U = {}, ee = {}) {
    const _e = dh(U);
    u.value = _e, m.value = qe(_e), $.value = String(_e.promptManager?.name || _e.name || "").trim(), ee.replaceDraft !== !1 && (l.value = _e);
  }
  function Ja(U) {
    if (!U) {
      ke.value = {}, Pe.value = Rl({}), se.value = "";
      return;
    }
    const ee = Yy(U.script);
    ke.value = ee, Pe.value = Rl(ee), se.value = U.key;
  }
  function _s(U = {}, ee = {}) {
    const _e = cb(U);
    T.value = _e, R.value = Be(_e), ee.replaceDraft !== !1 && (y.value = _e);
  }
  function Rr(U, ee = "继续？") {
    return window.confirm(`当前${U}有未保存修改，${ee}会放弃这些草稿。继续？`);
  }
  function al(U) {
    const ee = GC(U);
    G.value = ee, oe.value = Rl(ee), B.value = Ka(ee);
  }
  async function If() {
    if (je.value && !Rr("助手预设", "刷新")) {
      I.value = "";
      return;
    }
    const [U, ee, _e] = await Promise.all([
      Tl(),
      im(),
      uy()
    ]);
    C.value = U, A.value = ee || _e.id, _s(_e, { replaceDraft: !je.value });
  }
  async function wo() {
    if (Ee.value) {
      f.value = "";
      return;
    }
    f.value = "正在同步";
    try {
      const U = await e.requestHost("xb-tavern:list-chat-presets"), ee = U.result || U;
      d.value = ee, sl(ee.active), f.value = "";
    } catch (U) {
      f.value = U instanceof Error ? U.message : String(U || "读取失败");
    }
  }
  async function Ya(U = $.value) {
    const ee = String(U || "").trim(), _e = String(l.value.promptManager?.name || l.value.name || "").trim();
    if (!ee) {
      $.value = _e;
      return;
    }
    if (ee === _e) {
      $.value = _e;
      return;
    }
    if (Ee.value && !Rr("聊天预设", "切换")) {
      $.value = _e;
      return;
    }
    f.value = "正在切换";
    try {
      const $e = await e.requestHost("xb-tavern:select-chat-preset", { payload: { promptManagerName: ee } });
      sl($e.result || $e), f.value = "", e.postToHost("xb-tavern:refresh-context", {});
    } catch ($e) {
      $.value = _e, f.value = $e instanceof Error ? $e.message : String($e || "切换失败");
    }
  }
  async function il() {
    if (!Lt.value) {
      f.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    if (!Ee.value) return;
    f.value = "正在保存";
    const U = await e.requestHost("xb-tavern:save-chat-preset", { payload: l.value });
    if (U.ok === !1) {
      f.value = String(U.error || "保存失败");
      return;
    }
    sl(U.result), f.value = "", e.postToHost("xb-tavern:refresh-context", {});
  }
  async function ao(U = {}) {
    const ee = U.requestSerial || ++V;
    if (ee === V) {
      E.value = "正在同步";
      try {
        const _e = await e.requestHost("xb-tavern:list-worldbook-sources", { payload: { context: e.effectiveContext.value } });
        if (ee !== V) return;
        b.value = _e.result || _e;
        const $e = String(w.value || "").trim(), ht = !!$e && Ze.value.some((Ts) => Ts.name === $e), bt = String(U.preferredName || "").trim(), Kt = !!bt && Ze.value.some((Ts) => Ts.name === bt), Gt = U.selectFirst && (Ze.value.find((Ts) => Ts.globalActive)?.name || Ze.value[0]?.name) || "";
        w.value = Kt ? bt : U.keepSelection && ht ? w.value : Gt, E.value = "", qo(w.value);
      } catch (_e) {
        if (ee !== V) return;
        E.value = _e instanceof Error ? _e.message : String(_e || "读取失败");
      }
    }
  }
  async function oa() {
    const U = ++ae;
    N.value = "正在同步";
    try {
      const ee = await e.requestHost("xb-tavern:get-global-worldbooks");
      if (U !== ae) return;
      const _e = VC(ee.result || ee);
      Se && Se <= U && (Se = 0, q.value = !1), x.value = _e.options, M.value = _e.selected, N.value = "";
    } catch (ee) {
      if (U !== ae) return;
      Se && Se <= U && (Se = 0, q.value = !1), N.value = ee instanceof Error ? ee.message : String(ee || "读取失败");
    }
  }
  async function Xa(U = M.value) {
    const ee = Array.from(new Set(U.map(($e) => String($e || "").trim()).filter(Boolean))), _e = ++ae;
    Se = _e, M.value = ee, q.value = !0, N.value = "正在保存";
    try {
      const $e = await e.requestHost("xb-tavern:set-global-worldbooks", { payload: { selected: ee } });
      if (_e !== ae) return;
      const ht = VC($e.result || $e);
      Se === _e && (Se = 0, q.value = !1), x.value = ht.options, M.value = ht.selected, N.value = "", e.postToHost("xb-tavern:refresh-context", {}), ao({ keepSelection: !0 });
    } catch ($e) {
      if (_e !== ae) return;
      Se === _e && (Se = 0, q.value = !1), N.value = $e instanceof Error ? $e.message : String($e || "保存失败"), oa();
    }
  }
  function sa(U, ee) {
    const _e = String(U || "").trim();
    if (!_e || q.value) return;
    const $e = new Set(M.value);
    ee ? $e.add(_e) : $e.delete(_e), Xa([...$e]);
  }
  async function qo(U = w.value) {
    const ee = String(U || "").trim();
    if (!ee) {
      z.value = null, J.value = "";
      return;
    }
    const _e = ee;
    J.value = "正在读取预览";
    try {
      const $e = await e.requestHost("xb-tavern:get-worldbook-preview", { payload: {
        name: _e,
        limit: me.value
      } });
      if (String(w.value || "").trim() !== _e) return;
      z.value = Xfe($e.result || $e), J.value = "";
    } catch ($e) {
      if (String(w.value || "").trim() !== _e) return;
      z.value = null, J.value = $e instanceof Error ? $e.message : String($e || "预览读取失败");
    }
  }
  function io() {
    w.value && (me.value += Vh, qo(w.value));
  }
  async function Ho(U) {
    const ee = String(w.value || "").trim(), _e = U?.uid === void 0 || U.uid === null ? "" : String(U.uid).trim();
    if (!ee || !_e) return;
    const $e = Ka(U);
    if (Cn.value && B.value !== $e && !Rr("世界书条目", "切换")) return;
    B.value = $e, P += 1;
    const ht = `${$e}:${P}`;
    Q = ht, X.value = "正在读取条目";
    try {
      const bt = await e.requestHost("xb-tavern:get-worldbook-entry", { payload: {
        name: ee,
        uid: _e
      } });
      if (String(w.value || "").trim() !== ee || B.value !== $e || Q !== ht) return;
      al(bt.result || bt), X.value = "";
    } catch (bt) {
      if (B.value !== $e || Q !== ht) return;
      X.value = bt instanceof Error ? bt.message : String(bt || "条目读取失败");
    }
  }
  function zo() {
    G.value = null, oe.value = "", B.value = "", Q = "", X.value = "";
  }
  function Qa(U) {
    G.value && (G.value = GC({
      ...G.value,
      ...U
    }), X.value && X.value !== "正在保存" && (X.value = ""));
  }
  async function aa() {
    const U = G.value, ee = String(w.value || U?.worldbookName || "").trim();
    if (!(!U || !ee || !U.uid) && Cn.value) {
      X.value = "正在保存";
      try {
        const _e = await e.requestHost("xb-tavern:save-worldbook-entry", { payload: {
          name: ee,
          uid: U.uid,
          entryHash: U.entryHash,
          draft: U
        } });
        al(_e.result || _e), X.value = "已保存", await qo(ee), e.postToHost("xb-tavern:refresh-context", {});
      } catch (_e) {
        X.value = _e instanceof Error ? _e.message : String(_e || "保存失败");
      }
    }
  }
  async function Hu() {
    if (ye.value && !Rr("正则", "刷新")) {
      Le.value = "";
      return;
    }
    Le.value = "正在读取";
    try {
      const U = await e.requestHost("xb-tavern:list-regex-scripts");
      W.value = U.result || U, Ja(wt.value.find((ee) => ee.key === se.value) || wt.value[0] || null), Le.value = "";
    } catch (U) {
      Le.value = U instanceof Error ? U.message : String(U || "读取失败");
    }
  }
  function qg(U) {
    ye.value && !Rr("正则", "切换") || Ja(U);
  }
  function Hg(U) {
    ye.value && !Rr("正则", "新建") || (ke.value = Yy({
      scriptName: "新正则",
      findRegex: "",
      replaceString: "",
      trimStrings: [],
      placement: [],
      disabled: !1,
      markdownOnly: !1,
      promptOnly: !1,
      runOnEdit: !1,
      substituteRegex: 0,
      minDepth: null,
      maxDepth: null
    }), Pe.value = "", se.value = `${U.scriptType}:new`);
  }
  function Za(U) {
    ke.value = Yy({
      ...ke.value,
      ...U
    });
  }
  function xf(U, ee) {
    const _e = new Set((ke.value.placement || []).map(($e) => Number($e)));
    ee ? _e.add(U) : _e.delete(U), Za({ placement: [..._e] });
  }
  async function Vn() {
    const U = ot.value?.scriptType || Number(se.value.split(":")[0]);
    if (Number.isFinite(U) && ye.value) {
      Le.value = "正在保存";
      try {
        const ee = await e.requestHost("xb-tavern:save-regex-script", { payload: {
          scriptType: U,
          script: ke.value
        } }), _e = ee.result || ee;
        W.value = _e;
        const $e = String(_e.savedScriptId || ke.value.id || ""), ht = Number(_e.savedScriptType ?? U);
        Ja(wt.value.find((bt) => bt.scriptType === ht && $e && bt.script.id === $e) || wt.value.find((bt) => bt.script.scriptName === ke.value.scriptName) || wt.value[0] || null), Le.value = "";
      } catch (ee) {
        Le.value = ee instanceof Error ? ee.message : String(ee || "保存失败");
      }
    }
  }
  async function Mf() {
    const U = ot.value;
    if (U) {
      await ll(U);
      return;
    }
    const ee = String(ke.value.id || ""), _e = Number(se.value.split(":")[0]);
    !ee || !Number.isFinite(_e) || await ll({
      key: `${_e}:${ee}`,
      groupKey: "",
      groupLabel: "",
      scriptType: _e,
      script: ke.value
    });
  }
  async function ll(U) {
    if (ye.value && se.value !== U.key && !Rr("正则", "删除")) return;
    const ee = String(U?.script.id || ""), _e = U?.scriptType;
    if (!(!ee || !Number.isFinite(_e)) && window.confirm("删除这个正则脚本？")) {
      Le.value = "正在删除";
      try {
        const $e = await e.requestHost("xb-tavern:delete-regex-script", { payload: {
          scriptType: _e,
          id: ee
        } });
        W.value = $e.result || $e, Ja(wt.value[0] || null), Le.value = "";
      } catch ($e) {
        Le.value = $e instanceof Error ? $e.message : String($e || "删除失败");
      }
    }
  }
  function zu() {
    return zl(e.tavernDisplaySettings.value);
  }
  function Pf(U = {}) {
    const ee = zu();
    Ne.value = ee, U.updateCommitted !== !1 && (Oe.value = Pl(ee));
  }
  function ja(U) {
    Ne.value = U, e.tavernDisplaySettings.value = zl(U);
  }
  async function Wu(U) {
    const ee = ++L;
    we.value = !0, he.value = "正在保存显示设置", ja(U);
    try {
      if (await e.requestHost("xb-tavern:save-display-settings", { payload: U }), ee !== L) return;
      Oe.value = Pl(zu()), he.value = "显示设置已保存";
    } catch (_e) {
      if (ee !== L) return;
      ja(Pl(Oe.value)), he.value = _e instanceof Error ? _e.message : String(_e || "保存失败");
    } finally {
      ee === L && (we.value = !1);
    }
  }
  function bs(U) {
    Wu(zl({
      ...Ne.value,
      ...U
    }));
  }
  function ei(U) {
    const ee = Number(Ne.value.hiddenOutsideCount) || 5, _e = Math.min(20, Math.max(1, ee + U));
    _e !== ee && bs({ hiddenOutsideCount: _e });
  }
  function Nr(U) {
    const ee = Number(Ne.value.loadBatchSize) || 20, _e = Math.min(50, Math.max(5, ee + U * 5));
    _e !== ee && bs({ loadBatchSize: _e });
  }
  function ti(U) {
    const ee = Zfe(U);
    Y.value = ee.users, te.value = ee.currentUserId;
  }
  async function ul() {
    Te.value = !0, Ie.value || (he.value = "正在读取 USER 列表");
    try {
      const U = await e.requestHost("xb-tavern:list-users");
      ti(U.result || U), Ie.value || (he.value = "");
    } catch (U) {
      he.value = U instanceof Error ? U.message : String(U || "USER 列表读取失败");
    } finally {
      Te.value = !1;
    }
  }
  async function Rf(U) {
    const ee = String(U || "").trim();
    if (!(!ee || ee === te.value || Ie.value)) {
      Ie.value = ee, he.value = "正在切换 USER";
      try {
        const _e = await e.requestHost("xb-tavern:switch-user", { payload: { userId: ee } });
        ti(_e.result || _e), he.value = dn.value ? `已切换到 ${dn.value.name}` : "USER 已切换", e.postToHost("xb-tavern:refresh-context", {});
      } catch (_e) {
        he.value = _e instanceof Error ? _e.message : String(_e || "切换失败");
      } finally {
        Ie.value = "";
      }
    }
  }
  async function Nf() {
    Ee.value && (l.value = dh(u.value), m.value = qe(u.value), f.value = "");
  }
  function $f(U, ee) {
    l.value = dh({
      ...l.value,
      [U]: {
        ...l.value[U] || {},
        ...ee
      }
    });
  }
  function Wo(U, ee = {}) {
    $f("promptManager", {
      rawPreset: U,
      prompts: Array.isArray(U.prompts) ? U.prompts : [],
      promptOrder: Array.isArray(U.prompt_order) ? U.prompt_order : [],
      ...ee
    });
  }
  function ia(U, ee) {
    const _e = String(U || "").trim();
    if (!_e) return;
    const $e = Pl(ra()), ht = rl().map((Kt) => ({ ...Kt })), bt = ht.findIndex((Kt) => String(Kt.identifier || Kt.id || "").trim() === _e);
    bt >= 0 ? ht[bt] = {
      ...ht[bt],
      ...ee,
      identifier: _e
    } : ht.push({
      identifier: _e,
      name: _e,
      role: "system",
      content: "",
      ...ee
    }), $e.prompts = ht, Wo($e);
  }
  function ni(U) {
    const ee = Pl(ra()), _e = ys(), $e = ol().map((Kt) => ({ ...Kt })), ht = String(_e.activeCharacterId ?? "").trim();
    if (!ht) {
      f.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    let bt = $e.findIndex((Kt) => String(Kt.character_id ?? "") === ht);
    bt < 0 && (bt = $e.length, $e.push({
      character_id: ht,
      order: []
    })), $e[bt] = {
      ...$e[bt],
      order: U
    }, ee.prompt_order = $e, Wo(ee, { activeOrder: U });
  }
  function ws(U = qt.value) {
    return U.filter((ee) => ee.listed).map((ee) => ({
      ...ee.orderEntry,
      identifier: ee.identifier,
      enabled: ee.enabled
    }));
  }
  function la(U, ee) {
    const _e = String(U || "").trim();
    if (!Lt.value) {
      f.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    if (!_e) return;
    const $e = qt.value;
    ni(ws($e).map((ht) => String(ht.identifier || "").trim() === _e ? {
      ...ht,
      ...ee,
      identifier: _e
    } : ht));
  }
  function ri(U, ee) {
    if (!Lt.value) {
      f.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    const _e = qt.value, $e = _e.findIndex((Gt) => Gt.identifier === U), ht = $e + ee;
    if ($e < 0 || ht < 0 || ht >= _e.length) return;
    const bt = _e.slice(), [Kt] = bt.splice($e, 1);
    Kt && (bt.splice(ht, 0, Kt), ni(ws(bt)), v.value = U);
  }
  function cl(U, ee) {
    la(U, { enabled: ee });
  }
  function zg(U = "") {
    const ee = {
      system: "系统",
      user: "用户",
      assistant: "助手"
    }, _e = String(U || "system").trim();
    return ee[_e] || _e || "系统";
  }
  function Wg(U) {
    return Array.isArray(U) ? U.map((ee) => String(ee || "").trim()).filter(Boolean).join(`
`) : String(U || "");
  }
  function Gg(U = "") {
    return String(U || "").split(/\r?\n/).map((ee) => ee.trim()).filter(Boolean);
  }
  function Vg(U) {
    return {
      1: "用户输入",
      2: "AI 回复",
      3: "斜杠命令",
      5: "世界书",
      6: "推理内容"
    }[U] || String(U);
  }
  function Df(U) {
    return er.value.find((ee) => ee.scriptType === U);
  }
  function Kg() {
    return Df(ot.value?.scriptType || Number(se.value.split(":")[0]))?.label || "正则";
  }
  function Lf(U) {
    y.value = {
      ...y.value,
      ...U
    };
  }
  function Gu(U) {
    p.value = U;
  }
  function Sr(U = "") {
    const ee = Xe.value;
    ee && Lf({ [ee.key]: String(U || "") });
  }
  async function Pw(U) {
    const ee = String(U || "").trim();
    !ee || ee === A.value || je.value && !Rr("助手预设", "切换") || (await Ll(ee), A.value = ee, _s(await uy()), I.value = "");
  }
  async function Jg() {
    if (!je.value) return;
    const U = await Fc(String(it.value?.id || "") === "littlewhitebox-assistant-default" ? {
      ...y.value,
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: `${y.value.name || "助手预设"} 自定义`
    } : y.value);
    await Ll(U.id), A.value = U.id, _s(U.preset), C.value = await Tl(), I.value = "";
  }
  async function Vu() {
    const U = await Fc({
      ...y.value,
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: `${y.value.name || "助手预设"} 副本`
    });
    await Ll(U.id), A.value = U.id, _s(U.preset), C.value = await Tl(), I.value = "";
  }
  async function Yg() {
    if (je.value && !Rr("助手预设", "新建")) return;
    const U = await Fc({
      ...Fd(),
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: "新助手预设",
      description: ""
    });
    await Ll(U.id), A.value = U.id, _s(U.preset), C.value = await Tl(), I.value = "";
  }
  async function Xg(U) {
    if (je.value && !Rr("助手预设", "导入")) return !1;
    const ee = U && typeof U == "object" ? U : {}, _e = ee.preset && typeof ee.preset == "object" ? ee.preset : ee, $e = await Fc({
      ..._e,
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: String(_e.name || ee.name || "导入助手预设").trim() || "导入助手预设",
      description: String(_e.description || ee.description || "").trim()
    });
    return await Ll($e.id), A.value = $e.id, _s($e.preset), C.value = await Tl(), I.value = "", !0;
  }
  async function Ss() {
    const U = String(A.value || y.value.id || "").trim(), ee = C.value.find((_e) => _e.id === U) || null;
    !ee || ee.id === "littlewhitebox-assistant-default" || je.value && !Rr("助手预设", "删除") || window.confirm(`删除「${ee.name || "当前助手预设"}」？`) && (await AF(ee.id), C.value = await Tl(), A.value = await im(), _s(await uy()), I.value = "");
  }
  async function Qg() {
    je.value && (y.value = { ...T.value }, R.value = Be(T.value), I.value = "");
  }
  function Ku() {
    Me.config = wd(e.agentConfig.value || {}), Me.configDraft = null, Me.configFormSyncPending = !0;
  }
  function Zg(U = "") {
    a !== null && (window.clearTimeout(a), a = null), i !== null && (window.clearTimeout(i), i = null), o.value = {
      status: "saving",
      requestId: U,
      error: ""
    }, Me.configSave = o.value, s.value = "正在保存共享 API 配置...", a = window.setTimeout(() => {
      a = null, !(o.value.requestId !== U || o.value.status !== "saving") && (o.value = {
        status: "error",
        requestId: U,
        error: "保存超时，请重试"
      }, Me.configSave = o.value, s.value = "保存失败：保存超时，请重试", i = window.setTimeout(() => {
        o.value.requestId !== U || o.value.status !== "error" || (o.value = {
          status: "idle",
          requestId: "",
          error: ""
        }, Me.configSave = o.value, s.value = "", Qt(Go));
      }, 1800), Qt(Go));
    }, jfe), Qt(Go);
  }
  function Ju(U = "", ee = {}) {
    if (U && o.value.requestId && U !== o.value.requestId) return;
    a !== null && (window.clearTimeout(a), a = null), i !== null && (window.clearTimeout(i), i = null), o.value = {
      status: ee.ok ? "success" : "error",
      requestId: U,
      error: ee.error || ""
    }, Me.configSave = o.value, s.value = ee.ok ? "" : `保存失败：${ee.error || "unknown_error"}`;
    const _e = o.value.status;
    i = window.setTimeout(() => {
      o.value.requestId !== U || o.value.status !== _e || (o.value = {
        status: "idle",
        requestId: "",
        error: ""
      }, Me.configSave = o.value, s.value = "", Qt(Go));
    }, 1800), Qt(Go);
  }
  function jg(U) {
    const ee = String(U.requestId || `save-config-${Date.now()}`);
    Zg(ee), e.postToHost("xb-tavern:save-config", {
      requestId: ee,
      payload: U.payload || {}
    });
  }
  function Go() {
    const U = r.value;
    U && (Ke || (Ke = Hfe({
      state: Me,
      render: Go,
      describeError: e.describeError,
      showToast: (ee) => {
        s.value = String(ee || "");
      },
      saveConfig: jg,
      getRuntimeSummaryText: () => Mt.value
    })), Me.configSave = o.value, U.innerHTML = Gfe({
      configSave: o.value,
      runtimeText: Mt.value,
      inlineToastText: s.value,
      showAssistantPermissions: !1,
      showDelegateSettings: !0,
      activePage: String(Me.configPage || "main"),
      delegatePresetHint: "记忆整理会复用这里的分身 API；当前聊天仍使用主 API。",
      isBusy: e.isRunning.value,
      canDeletePreset: Object.keys(Me.config?.presets || {}).length > 1
    }), Ke.syncConfigToForm(U), Ke.bindSettingsPanelEvents(U));
  }
  function ev(U) {
    if (U.ok === !0) {
      e.agentConfig.value = U.config || e.agentConfig.value, Ku(), Ju(String(U.requestId || ""), { ok: !0 });
      return;
    }
    Ku(), Ju(String(U.requestId || ""), {
      ok: !1,
      error: String(U.error || "保存失败")
    });
  }
  function dl(U) {
    "chatPreset" in U && sl(U.chatPreset, { replaceDraft: !Ee.value }), "chatPresetList" in U && (d.value = U.chatPresetList || {});
  }
  function fl(U) {
    e.activeSettingsWorkspace.value = U, e.activeView.value = "settings";
  }
  function tv(U = "") {
    w.value = String(U || "").trim(), fl("worldbooks");
  }
  async function Of() {
    const U = ++V, ee = String(e.currentWorldbookCharacterId.value || "").trim();
    let _e = "";
    if (w.value = "", !ee) {
      await ao({ requestSerial: U });
      return;
    }
    try {
      const $e = await e.requestHost("xb-tavern:get-character-worldbook-state", { payload: { characterId: ee } });
      if (U !== V) return;
      const ht = $e.result || $e, bt = String(ht.boundWorldbookName || "").trim();
      if (bt && ht.boundExists === !0) {
        await ao({
          preferredName: bt,
          requestSerial: U
        });
        return;
      }
    } catch ($e) {
      if (U !== V) return;
      _e = $e instanceof Error ? $e.message : String($e || "绑定状态读取失败");
    }
    await ao({ requestSerial: U }), _e && U === V && (E.value = _e);
  }
  function Yu(U) {
    const ee = U;
    (ee === "api" || ee === "chatPreset" || ee === "worldbooks" || ee === "regex" || ee === "assistantPreset" || ee === "base") && fl(ee);
  }
  return gt(qt, (U) => {
    if (!U.length) {
      v.value = "";
      return;
    }
    U.some((ee) => ee.identifier === v.value) || (v.value = U[0]?.identifier || "");
  }, { immediate: !0 }), gt(de, () => {
    fe.value = zy;
  }), gt(ve, () => {
    Ce.value = Wy;
  }), gt(Ue, () => {
    le.value = Gy;
  }), gt(We, () => {
    H.value = {};
  }), gt(() => e.tavernDisplaySettings.value, () => {
    Pf({ updateCommitted: !we.value });
  }, { immediate: !0 }), gt(Pt, (U) => {
    if (!U.length) {
      p.value = "";
      return;
    }
    U.some((ee) => ee.id === p.value) || (p.value = U[0]?.id || "");
  }, { immediate: !0 }), gt([
    () => e.activeSettingsWorkspace.value,
    () => e.activeView.value,
    () => o.value.status,
    () => e.agentConfig.value,
    () => r.value
  ], () => {
    r.value && Qt(Go), e.activeView.value === "settings" && e.activeSettingsWorkspace.value === "regex" && !er.value.length && Hu();
  }), gt([e.activeView, e.activeSettingsWorkspace], ([U, ee], [_e, $e]) => {
    U === "settings" && ee === "chatPreset" && (_e !== U || $e !== ee) && wo(), U === "settings" && ee === "worldbooks" && (_e !== U || $e !== ee) && (ao({ keepSelection: !0 }), oa()), U === "settings" && ee === "assistantPreset" && (_e !== U || $e !== ee) && If(), U === "settings" && ee === "base" && (_e !== U || $e !== ee) && ul();
  }), gt(w, (U) => {
    e.activeSettingsWorkspace.value === "worldbooks" && (zo(), me.value = Vh, qo(U));
  }), {
    activeAssistantPreset: T,
    activeChatPreset: u,
    apiReady: Ht,
    apiReadyDetail: At,
    apiRuntimeLine: Mt,
    apiSettingsRootRef: r,
    applyHostChatPreset: dl,
    handleApiConfigSaved: ev,
    openSettingsWorkspace: fl,
    openWorldbookWorkspace: tv,
    loadTavernUsers: ul,
    refreshPresets: If,
    refreshRegexFromHost: Hu,
    renderApiSettingsPanel: Go,
    runtimeChatPreset: Ye,
    selectSettingsWorkspace: Yu,
    settingsContext: {
      activeAssistantPresetId: A,
      activePromptOrderLabel: so,
      activeSettingsWorkspace: e.activeSettingsWorkspace,
      activeView: e.activeView,
      apiReady: Ht,
      apiReadyDetail: At,
      apiRuntimeLine: Mt,
      apiSettingsRootRef: r,
      applyActiveRegexScript: Ja,
      ASSISTANT_PRESET_BATCH_SIZE: Wy,
      assistantPreset: y,
      assistantPresetDirty: je,
      assistantPresetItems: Pt,
      assistantPresets: C,
      assistantPresetSearchText: ve,
      assistantPresetStatus: I,
      assistantPresetVisibleLimit: Ce,
      canEditPromptOrder: Lt,
      cancelWorldbookEntryEdit: zo,
      chatPresetOptions: Ut,
      chatPresetSourceSearchText: de,
      chatPresetSourceVisibleLimit: fe,
      CHAT_PRESET_SOURCE_BATCH_SIZE: zy,
      createAssistantPreset: Yg,
      createRegexScript: Hg,
      deleteCurrentAssistantPreset: Ss,
      deleteCurrentRegexScript: Mf,
      deleteRegexScript: ll,
      deriveAssistantPreset: Vu,
      discardAssistantPresetChanges: Qg,
      discardPresetChanges: Nf,
      expandRegexGroup: Ft,
      filteredPromptEditorRows: Ve,
      globalWorldbookOptions: x,
      globalWorldbookSelected: M,
      globalWorldbookSaving: q,
      globalWorldbookStatus: N,
      hiddenAssistantPresetCount: ne,
      hiddenChatPresetOptionCount: xe,
      hiddenPromptCount: Re,
      hiddenWorldbookPreviewEntryCount: jt,
      homeThemeDark: e.homeThemeDark,
      importAssistantPreset: Xg,
      isEditingWorldbookEntry: Pn,
      linesFromList: Wg,
      listFromLines: Gg,
      movePromptRow: ri,
      postToHost: e.postToHost,
      preset: l,
      presetDirty: Ee,
      presetRows: Pr,
      presetStatus: f,
      presetTotalChars: Cf,
      PROMPT_EDITOR_BATCH_SIZE: Gy,
      promptEditorRows: qt,
      promptRoleDisplay: zg,
      promptRowIndex: Rn,
      promptSearchText: Ue,
      promptVisibleLimit: le,
      refreshRegexFromHost: Hu,
      currentTavernUser: dn,
      currentTavernUserId: te,
      displaySettings: Ne,
      REGEX_GROUP_BATCH_SIZE: Vy,
      regexDirty: ye,
      regexDraft: ke,
      regexDraftTypeLabel: Kg,
      regexGroups: er,
      regexGroupsForDisplay: yn,
      regexPlacementLabel: Vg,
      regexScriptRows: wt,
      regexSearchText: We,
      regexStatus: Le,
      saveCurrentAssistantPreset: Jg,
      saveCurrentPreset: il,
      saveCurrentRegexScript: Vn,
      saveGlobalWorldbooksToHost: Xa,
      saveWorldbookEntryDraft: aa,
      selectAssistantPreset: Pw,
      selectAssistantPresetItem: Gu,
      selectChatPresetFromHost: Ya,
      selectedAssistantPresetItem: Xe,
      selectedPresetSourceId: $,
      selectedPromptIdentifier: v,
      selectedPromptRow: wn,
      selectedRegexKey: se,
      selectedRegexRow: ot,
      selectedWorldbook: zt,
      selectedWorldbookName: w,
      selectRegexScript: qg,
      selectSettingsWorkspace: Yu,
      settingsNavItems: dt,
      shortText: e.shortText,
      showMoreWorldbookPreviewEntries: io,
      stepHiddenOutsideCount: ei,
      stepLoadBatchSize: Nr,
      startWorldbookEntryEdit: Ho,
      switchingTavernUserId: Ie,
      syncChatPresetFromHost: wo,
      syncGlobalWorldbooksFromHost: oa,
      syncWorldbooksForCurrentCharacter: Of,
      syncWorldbooksFromHost: ao,
      switchTavernUser: Rf,
      tavernUsers: Y,
      togglePromptRow: cl,
      toggleGlobalWorldbook: sa,
      toggleRegexPlacement: xf,
      updateAssistantPresetPatch: Lf,
      updateDisplaySettingsPatch: bs,
      updatePromptByIdentifier: ia,
      updateRegexPatch: Za,
      updateSelectedAssistantPresetItem: Sr,
      updateWorldbookEntryDraftPatch: Qa,
      baseSettingsLoading: Te,
      baseSettingsSaving: we,
      baseSettingsStatus: he,
      visibleAssistantPresetRecords: ce,
      visibleChatPresetOptions: et,
      visiblePromptEditorRows: He,
      WORLDBOOK_PREVIEW_BATCH_SIZE: Vh,
      worldbookEntryDirty: Cn,
      worldbookEntryDraft: G,
      worldbookEntryEditingKey: B,
      worldbookEntrySaving: Gn,
      worldbookEntryStatus: X,
      worldbookOptions: Ze,
      worldbookPreview: z,
      worldbookPreviewStatus: J,
      worldbookPreviewVisibleLimit: me,
      worldbookStatus: E
    },
    syncApiSettingsConfigFromAgentConfig: Ku,
    syncChatPresetFromHost: wo,
    syncWorldbooksFromHost: ao
  };
}
var nhe = ["data-chat-font-size"], rhe = { class: "xb-os-stage" }, ohe = { class: "character-worldbook-picker" }, she = {
  key: 0,
  class: "worldbook-picker-status"
}, ahe = { class: "worldbook-picker-list" }, ihe = ["disabled", "onClick"], lhe = "xb-tavern-app", uhe = "xb-tavern-host", Jh = 48, KC = 6, che = 12, dhe = 12, fhe = 36, JC = 48, Xy = 24, YC = 480, hhe = 200, XC = 1e9, QC = "LittleWhiteBox_Tavern_theme", phe = "配图已生成", mhe = 100, ghe = /* @__PURE__ */ un({
  __name: "App",
  setup(e) {
    const t = /* @__PURE__ */ ie({}), n = /* @__PURE__ */ ie({}), r = /* @__PURE__ */ ie({}), o = /* @__PURE__ */ ie(zl({})), s = /* @__PURE__ */ ie(!0), a = re(() => Sa(o.value.hiddenOutsideCount)), i = re(() => I0(o.value.loadBatchSize)), l = /* @__PURE__ */ ie({}), u = /* @__PURE__ */ ie("15px"), d = /* @__PURE__ */ ie("23px"), f = /* @__PURE__ */ ie([]), m = /* @__PURE__ */ ie(""), v = /* @__PURE__ */ ie(""), y = /* @__PURE__ */ ie(0), T = /* @__PURE__ */ ie(""), C = /* @__PURE__ */ ie(""), A = /* @__PURE__ */ ie(null), I = /* @__PURE__ */ ie(!1), R = /* @__PURE__ */ ie(""), $ = /* @__PURE__ */ ie(!1), p = /* @__PURE__ */ ie([]), b = /* @__PURE__ */ ie(0), w = /* @__PURE__ */ ie(""), E = /* @__PURE__ */ ie("等待读取角色与会话"), x = /* @__PURE__ */ ie(""), M = /* @__PURE__ */ ie("raw"), N = /* @__PURE__ */ ie(""), q = /* @__PURE__ */ ie([]), z = /* @__PURE__ */ ie(""), J = /* @__PURE__ */ ie(""), G = /* @__PURE__ */ ie(""), oe = /* @__PURE__ */ ie(""), B = /* @__PURE__ */ ie(!1), X = /* @__PURE__ */ ie(""), de = /* @__PURE__ */ ie(!1), fe = /* @__PURE__ */ ie(!1), ve = /* @__PURE__ */ ie({
      provider: "disabled",
      enabled: !1,
      ready: !1
    }), Ce = /* @__PURE__ */ ie(""), Ue = /* @__PURE__ */ ie(""), le = /* @__PURE__ */ ie("idle"), me = /* @__PURE__ */ ie("");
    let We = null;
    const H = /* @__PURE__ */ ie([]), W = /* @__PURE__ */ ie(""), se = /* @__PURE__ */ ie([]), ke = /* @__PURE__ */ ie(0), Pe = /* @__PURE__ */ ie(null), Le = /* @__PURE__ */ ie(null), Y = /* @__PURE__ */ ie(-1), te = /* @__PURE__ */ ie({}), he = /* @__PURE__ */ ie([]), we = /* @__PURE__ */ ie([]), Te = /* @__PURE__ */ ie([]), Ie = /* @__PURE__ */ ie(null), Ne = /* @__PURE__ */ ie(""), Oe = /* @__PURE__ */ ie(null), L = /* @__PURE__ */ ie(null), P = /* @__PURE__ */ ie(""), Q = /* @__PURE__ */ ie(""), V = /* @__PURE__ */ ie(""), ae = /* @__PURE__ */ ie("preview"), Se = /* @__PURE__ */ ie(""), Me = /* @__PURE__ */ ie("state"), Ke = /* @__PURE__ */ ie([]), qe = /* @__PURE__ */ ie("main"), Be = /* @__PURE__ */ ie(null), Ee = /* @__PURE__ */ ie([]), Ye = /* @__PURE__ */ ie(null), je = /* @__PURE__ */ ie([]), It = /* @__PURE__ */ ie(""), Ht = /* @__PURE__ */ ie(""), At = /* @__PURE__ */ ie(""), Mt = /* @__PURE__ */ ie(""), Ut = /* @__PURE__ */ ie(""), yt = /* @__PURE__ */ ie([]), et = /* @__PURE__ */ ie(!1), xe = /* @__PURE__ */ ie(!1), Ze = /* @__PURE__ */ ie(null), zt = /* @__PURE__ */ ie([]), jt = /* @__PURE__ */ ie(null), Cn = /* @__PURE__ */ ie(""), Gn = /* @__PURE__ */ ie(Jh), er = /* @__PURE__ */ ie(""), wt = /* @__PURE__ */ ie({}), nn = /* @__PURE__ */ ie(KC), Ft = /* @__PURE__ */ ie({});
    function yn() {
      const [_] = String(window.location.hash || "").replace(/^#\/?/, "").split("/");
      return _ === "chat" || _ === "characters" || _ === "settings" || _ === "about" ? _ : "home";
    }
    function ot() {
      try {
        const _ = globalThis.localStorage?.getItem(QC);
        if (_ === "dark") return !0;
        if (_ === "light") return !1;
      } catch {
      }
      return !1;
    }
    const ye = /* @__PURE__ */ new Map();
    function dt(_ = "") {
      return String(_ || "").trim().toLocaleLowerCase();
    }
    function dn(_, k) {
      const D = Number.parseFloat(String(_ || ""));
      return Number.isFinite(D) && D > 0 ? `${Math.round(D * 100) / 100}px` : k;
    }
    function Pt(_ = u.value) {
      const k = Number.parseFloat(String(_ || ""));
      return Number.isFinite(k) && k > 0 ? `${Math.round((k + 8) * 100) / 100}px` : "23px";
    }
    function j(_, k = 1600) {
      return _.map((D) => String(D ?? "").slice(0, k)).filter(Boolean).join(`
`);
    }
    function ce(_) {
      return Array.isArray(_) ? _.map((k) => String(k || "").trim()).filter(Boolean) : [];
    }
    const ne = re(() => H.value.find((_) => _.id === W.value) || null), it = re(() => Ta(ne.value?.contextSnapshot?.authorNote)), Xe = re(() => sr(ne.value?.state || {})), Lt = re(() => mo(Xe.value.contract)), qt = re(() => !!(ne.value && String(ne.value.characterId || "").trim())), Ve = /* @__PURE__ */ ie(yn()), He = /* @__PURE__ */ ie(ehe()), Re = /* @__PURE__ */ ie(ot()), _t = /* @__PURE__ */ ie("chat"), wn = /* @__PURE__ */ ie("balanced"), so = /* @__PURE__ */ ie("memory"), Pr = /* @__PURE__ */ ie(null), Cf = /* @__PURE__ */ ie(null), Ka = /* @__PURE__ */ ie(null), Pn = T2({
      totalItems: () => ke.value,
      defaultLimit: a,
      loadBatchSize: i
    }), Rn = T2({
      totalItems: () => uv.value.length,
      defaultLimit: a,
      loadBatchSize: i
    }), ys = Pn.scrollRef, ra = Rn.scrollRef, rl = Pn.autoScroll, ol = Rn.autoScroll, Ug = Pn.showScrollTop, kf = Pn.showScrollBottom, sl = Pn.scrollControlsActive, Ja = Rn.showScrollTop, _s = Rn.showScrollBottom, Rr = Rn.scrollControlsActive, al = Pn.messageWindowLimit, If = Rn.messageWindowLimit, wo = /* @__PURE__ */ ie(""), Ya = /* @__PURE__ */ ie(""), il = /* @__PURE__ */ ie(!1), ao = /* @__PURE__ */ ie("history"), oa = /* @__PURE__ */ ie(""), Xa = /* @__PURE__ */ ie(""), sa = /* @__PURE__ */ ie(""), qo = /* @__PURE__ */ ie(""), io = /* @__PURE__ */ ie({}), Ho = /* @__PURE__ */ ie({}), zo = /* @__PURE__ */ ie(null), Qa = /* @__PURE__ */ ie(null), aa = /* @__PURE__ */ ie(null), Hu = re(() => ({
      "--xb-host-main-font-size": u.value,
      "--xb-host-prose-line-height": d.value
    })), { clearMarkdownCache: qg, disposeMarkdownTools: Hg, enhanceChatMarkdown: Za, enhanceManagerMarkdown: xf, markdownSignature: Vn, renderChatMarkdown: Mf, stripTavernImageMarkers: ll } = y9({
      chatScrollRef: ys,
      managerScrollRef: ra,
      htmlRenderEnabled: s,
      requestHost: ur
    }), zu = /* @__PURE__ */ new Map(), Pf = /* @__PURE__ */ new WeakMap();
    let ja = 0, Wu = 0, bs = 0, ei = null, Nr = null, ti = null, ul = !1, Rf = 0, Nf = !1, $f = !1;
    const Wo = /* @__PURE__ */ new Set(), ia = /* @__PURE__ */ new Map(), ni = /* @__PURE__ */ new Map(), ws = /* @__PURE__ */ new Map();
    let la = 0;
    const ri = re(() => ({
      ...t.value,
      history: W.value ? Tf(se.value) : t.value.history
    })), { activeAssistantPreset: cl, applyHostChatPreset: zg, handleApiConfigSaved: Wg, loadTavernUsers: Gg, openSettingsWorkspace: Vg, openWorldbookWorkspace: Df, refreshPresets: Kg, refreshRegexFromHost: Lf, renderApiSettingsPanel: Gu, runtimeChatPreset: Sr, selectSettingsWorkspace: Pw, settingsContext: Jg, syncApiSettingsConfigFromAgentConfig: Vu, syncChatPresetFromHost: Yg, syncWorldbooksFromHost: Xg } = the({
      activeView: Ve,
      activeSettingsWorkspace: He,
      agentConfig: r,
      tavernDisplaySettings: o,
      effectiveContext: ri,
      currentWorldbookCharacterId: re(() => String(ne.value?.characterId || ri.value.character?.id || m.value || "").trim()),
      homeThemeDark: Re,
      isRunning: de,
      describeError: oi,
      postToHost: Ko,
      requestHost: ur,
      shortText: Ev
    }), Ss = re(() => ri.value.character || {}), Qg = re(() => Vo(Ss.value.name || "", "未选择角色")), Ku = re(() => {
      const _ = String(Ss.value.avatar || "").trim(), k = String(Ss.value.id || m.value || "").trim(), D = String(Ss.value.name || "").trim(), Z = f.value.find((pe) => String(pe.id || "").trim() === k) || f.value.find((pe) => String(pe.name || "").trim() === D), ue = [_, String(Z?.avatar || "").trim()].filter((pe, De, ut) => pe && ut.indexOf(pe) === De);
      return ue.find((pe) => !Ft.value[pe]) || ue[0] || "";
    }), Zg = re(() => {
      const _ = Ku.value;
      return _ && !Ft.value[_] ? _ : "";
    }), Ju = re(() => ri.value.user || {}), jg = re(() => Vo(Ju.value.name || "", "User")), Go = re(() => String(Ju.value.avatar || "").trim()), ev = re(() => {
      const _ = Go.value;
      return _ && !Ft.value[_] ? _ : "";
    }), dl = re(() => t.value.character || {}), fl = re(() => String(dl.value.id || m.value || "").trim());
    function tv(_ = "") {
      const k = String(_ || "").trim();
      return !!k && !Ft.value[k];
    }
    function Of(_ = "") {
      const k = String(_ || "").trim();
      !k || Ft.value[k] || (Ft.value = {
        ...Ft.value,
        [k]: !0
      });
    }
    function Yu(_, k = "") {
      const D = String(_.id || k || "").trim();
      if (!D) return null;
      const Z = String(_.name || "").trim() || `角色 ${D}`, ue = String(_.avatar || "").trim(), pe = _.shallow === !0, De = String(_.description || "").trim(), ut = String(_.personality || "").trim(), Qe = String(_.scenario || "").trim(), tt = String(_.firstMessage || _.first_mes || "").trim(), Vt = ce(_.alternateGreetings || _.alternate_greetings), So = String(_.mesExample || _.mes_example || "").trim(), gl = String(_.creatorNotes || _.creator_notes || "").trim(), Bt = String(_.characterDepthPrompt || _.character_depth_prompt || "").trim(), li = JSON.stringify([
        D,
        Z,
        ue,
        pe,
        De,
        ut,
        Qe,
        tt,
        Vt,
        So,
        gl,
        Bt
      ]), xS = zu.get(D);
      if (xS?.signature === li) return xS.option;
      const Lr = {
        id: D,
        name: Z,
        avatar: ue,
        shallow: pe,
        description: De,
        personality: ut,
        scenario: Qe,
        firstMessage: tt,
        alternateGreetings: Vt,
        mesExample: So,
        creatorNotes: gl,
        characterDepthPrompt: Bt
      };
      return Lr.searchCorpus = dt(j([
        Lr.id,
        Lr.name,
        Lr.description,
        Lr.personality,
        Lr.scenario,
        Lr.firstMessage,
        ...Lr.alternateGreetings || [],
        Lr.mesExample,
        Lr.creatorNotes,
        Lr.characterDepthPrompt
      ], 900)), zu.set(D, {
        signature: li,
        option: Lr
      }), Lr;
    }
    const U = re(() => {
      const _ = /* @__PURE__ */ new Map();
      f.value.forEach((D) => {
        const Z = Yu(D);
        Z && _.set(Z.id, Z);
      });
      const k = String(dl.value.id || "").trim();
      if (k && dl.value.name && !_.has(k)) {
        const D = Yu(dl.value, k);
        D && _.set(k, D);
      }
      return [..._.values()].sort((D, Z) => D.name.localeCompare(Z.name, "zh-Hans-CN"));
    }), ee = re(() => {
      const _ = dt(Cn.value);
      return _ ? U.value.filter((k) => String(k.searchCorpus || "").includes(_)) : U.value;
    }), _e = re(() => {
      const _ = ee.value.slice(0, Gn.value), k = String(v.value || "").trim();
      if (!k || _.some((Z) => Z.id === k)) return _;
      const D = U.value.find((Z) => Z.id === k);
      return D ? [D, ..._] : _;
    }), $e = re(() => {
      const _ = String(v.value || "").trim();
      if (_) {
        const k = U.value.find((D) => D.id === _);
        if (k) return k;
      }
      return null;
    }), ht = re(() => {
      const _ = String($e.value?.id || "").trim();
      return _ ? H.value.filter((k) => String(k.characterId || "").trim() === _).slice().sort((k, D) => (Number(D.updatedAt) || Number(D.createdAt) || 0) - (Number(k.updatedAt) || Number(k.createdAt) || 0)) : [];
    }), bt = re(() => {
      const _ = $e.value;
      return _ ? [String(_.firstMessage || "").trim(), ..._.alternateGreetings || []].filter(Boolean) : [];
    }), Kt = re(() => Math.max(0, ee.value.length - Math.min(ee.value.length, Gn.value)));
    function Gt(_ = "") {
      return String(_ || "").replace(/\s*[·-]\s*小白酒馆\s*$/g, "").replace(/\s*[·-]\s*会话\s*$/g, "").replace(/^小白酒馆会话$/g, "").replace(/\s*·\s*第\s*\d+\s*轮\s*·\s*\d+\s*条可用消息\s*$/g, "").trim();
    }
    function Ts(_ = "") {
      return /^(sillytavern\s+system|system)\b/i.test(String(_ || "").trim());
    }
    function Vo(_ = "", k = "") {
      const D = Gt(_);
      return D && !Ts(D) ? D : k;
    }
    function nv(_) {
      if (!_) return "";
      const k = Vo(_.characterName || "");
      if (k) return k;
      const D = Vo(_.title || "");
      return D || "";
    }
    const v5 = re(() => nv(ne.value) || Vo(Ss.value.name || "") || "未选择角色"), y5 = re(() => W.value ? v5.value : Vo(Qg.value) || "未选择角色"), rv = re(() => ne.value?.state?.lastRequestSnapshot), _5 = re(() => String(rv.value?.rawRequestJson || rv.value?.rawMessagesJson || "")), b5 = re(() => se.value);
    function w5(_) {
      let k = null;
      return _.forEach((D) => {
        (!k || D.order > k.order) && (k = D);
      }), k;
    }
    const ov = re(() => w5(se.value)), S5 = re(() => Y.value), Rw = re(() => {
      const _ = Math.max(0, Math.floor(Number(ke.value) || 0)), k = se.value.length, D = Math.max(0, _ - k);
      return {
        startIndex: D,
        hiddenBefore: D,
        visibleCount: k,
        limit: al.value,
        total: _
      };
    }), sv = re(() => se.value), T5 = re(() => J.value), E5 = re(() => {
      const _ = ov.value;
      return _?.error ? `${_.sessionId}:${_.order}:${_.content || ""}` : "";
    }), { archivedManagerRuns: A5, currentManagerWorkRun: C5, formatRunActivityLine: k5, formatRunIssueLine: I5, formatRunInputLine: x5, formatRunMapLine: M5, formatRunMemoryLine: P5, formatRunModelLine: R5, formatRunTaskLine: N5, hiddenManagerRunCount: $5, isManagerRunLive: D5, managerBusy: Nw, managerRunTone: L5, managerStatusClock: O5, managerStatusLabel: F5, managerToolStatusLabel: B5, managerToolTone: U5, managerToolTraceItems: q5, toolTraceSummary: H5 } = jue({
      managerRuns: he,
      visibleRunLimit: dhe
    }), $w = re(() => {
      const _ = String(ne.value?.characterId || ri.value.character?.id || "").trim();
      return _ ? H.value.filter((k) => String(k.characterId || "").trim() === _) : H.value;
    }), Ff = re(() => {
      const _ = $w.value, k = _.slice(0, nn.value);
      if (W.value && !k.some((D) => D.id === W.value)) {
        const D = _.find((Z) => Z.id === W.value);
        D && k.unshift(D);
      }
      return k;
    }), av = re(() => $w.value.length), z5 = re(() => Math.max(0, av.value - Ff.value.length));
    function Dw(_ = "", k = 0) {
      const D = String(_ || "").trim();
      if (!D) return;
      const Z = Math.max(0, Math.floor(Number(k) || 0));
      te.value[D] !== Z && (te.value = {
        ...te.value,
        [D]: Z
      });
    }
    function W5(_ = "") {
      const k = String(_ || "").trim();
      if (!k || !(k in te.value)) return;
      const D = { ...te.value };
      delete D[k], te.value = D;
    }
    function G5(_) {
      const k = String(_?.id || "").trim();
      return k ? k === W.value ? ke.value : Math.max(0, Number(te.value[k]) || 0) : 0;
    }
    function iv(_) {
      const k = String(_?.id || "").trim();
      return k && k !== W.value && !(k in te.value) ? "统计中" : `第 ${G5(_)} 楼`;
    }
    async function Lw(_ = []) {
      const k = _.map((pe) => String(pe.id || "").trim()).filter(Boolean), D = [...new Set(k)].filter((pe) => pe !== W.value && !(pe in te.value));
      if (!D.length) return;
      const Z = await Promise.all(D.map(async (pe) => [pe, await yF(pe)])), ue = { ...te.value };
      Z.forEach(([pe, De]) => {
        pe === W.value || pe in te.value || (ue[pe] = Math.max(0, Math.floor(Number(De) || 0)));
      }), te.value = ue;
    }
    async function V5() {
      await Lw(Ff.value);
    }
    const Bf = re(() => Te.value.filter((_) => _.status !== "stale")), Xu = re(() => Te.value.find((_) => _.path === Ne.value) || Te.value[0] || null), Ow = re(() => Oe.value?.sessionId === W.value && Oe.value?.path === Xu.value?.path ? Oe.value : null);
    function lv(_) {
      const k = typeof _ == "string" ? _ : String(_?.path || "");
      return k === "memory/state.md" ? "会话记忆" : k.startsWith("memory/characters/") && k.endsWith(".md") ? k.replace(/^memory\/characters\//, "").replace(/\.md$/i, "") || "人物记忆" : k.replace(/^memory\//, "").replace(/\.md$/i, "") || "记忆档案";
    }
    function Fw(_) {
      const k = typeof _ == "string" ? _ : String(_?.path || "");
      return k === "memory/state.md" ? "当前有效记忆状态" : k.startsWith("memory/characters/") && k.endsWith(".md") ? "人物长期记忆" : "记忆档案";
    }
    function Bw(_ = "") {
      return _ === "memory/state.md" ? 0 : _.startsWith("memory/characters/") ? 10 : 30;
    }
    function K5(_) {
      const k = Pf.get(_);
      if (k) return k;
      const D = dt([
        _.path,
        lv(_),
        Fw(_),
        _.status,
        _.searchText || _.preview || ""
      ].filter(Boolean).join(`
`));
      return Pf.set(_, D), D;
    }
    function Uw(_ = "") {
      const k = _ === "turns" ? fhe : Xy, D = Number(wt.value[_]);
      return Number.isFinite(D) && D > 0 ? Math.floor(D) : k;
    }
    function J5(_ = "") {
      const k = Uw(_);
      wt.value = {
        ...wt.value,
        [_]: k + (_ === "turns" ? JC : Xy)
      };
    }
    const Y5 = re(() => {
      const _ = [{
        key: "core",
        title: "会话记忆",
        files: []
      }, {
        key: "characters",
        title: "人物记忆",
        files: []
      }], k = [];
      [...Te.value].sort((pe, De) => Bw(pe.path) - Bw(De.path) || String(pe.path || "").localeCompare(String(De.path || ""))).forEach((pe) => {
        pe.path === "memory/state.md" ? _[0].files.push(pe) : pe.path.startsWith("memory/characters/") ? _[1].files.push(pe) : k.push(pe);
      });
      const D = _.filter((pe) => pe.files.length);
      k.length && D.push({
        key: "other",
        title: "其他档案",
        files: k
      });
      const Z = dt(er.value), ue = String(Ne.value || "").trim();
      return D.map((pe) => {
        const De = Z ? pe.files.filter((tt) => K5(tt).includes(Z)) : pe.files, ut = Uw(pe.key), Qe = De.slice(0, ut);
        if (ue && !Qe.some((tt) => tt.path === ue)) {
          const tt = De.find((Vt) => Vt.path === ue);
          tt && Qe.unshift(tt);
        }
        return {
          ...pe,
          files: Qe,
          totalCount: pe.files.length,
          filteredCount: De.length,
          hiddenCount: Math.max(0, De.length - Qe.length)
        };
      }).filter((pe) => pe.filteredCount || !Z);
    }), qw = re(() => !!Xu.value || !!Q.value), X5 = re(() => !1), Qu = re(() => !!Q.value && P.value !== V.value), Q5 = re(() => {
      const _ = Ie.value;
      return _ ? _.status === "ready" ? "记忆可检索" : _.status === "failed" ? `记忆整理失败：${_.error || "memory_index_failed"}` : "记忆正在整理" : "还没有可检索记忆";
    }), Z5 = re(() => sv.value.map((_) => `${_.sessionId}:${_.order}:${_.error ? 1 : 0}:${Vn(_.content)}`).join("|")), j5 = re(() => {
      const _ = se.value.filter((D) => pv(D)), k = _.length;
      return _.reduce((D, Z, ue) => (D[cr(Z)] = Math.max(0, k - ue - 1), D), {});
    }), e6 = re(() => q.value.map((_, k) => `${k}:${Vn(String(_.label || ""))}:${Vn(String(_.text || ""))}`).join("|")), t6 = re(() => zt.value.map((_, k) => [
      k,
      _.toolCallId || "",
      _.stat,
      _.action,
      _.roll,
      _.difficulty,
      _.insertAfterChars,
      _.success ? 1 : 0
    ].join(":")).join("|")), uv = re(() => LC(yt.value)), n6 = re(() => {
      const _ = Ze.value;
      return !_ || _.sessionId !== W.value ? [] : _.draft ? [..._.messages, _.draft] : _.messages;
    }), Hw = re(() => LC(n6.value)), cv = re(() => uI({ uiMessageWindowLimit: If.value }, uv.value.length, { defaultLimit: a.value })), dv = re(() => uv.value.slice(cv.value.startIndex)), r6 = re(() => dv.value.filter((_) => _.kind === "message").map((_) => _.message)), o6 = re(() => dv.value.map((_) => _.kind === "message" ? `${_.message.sessionId}:${_.message.order}:${Vn(_.message.content)}` : `${_.key}:${Vn(_.assistantMessage.content)}:${_.calls.map((k) => `${k.id}:${Vn(k.resultText)}`).join(",")}`).concat(s.value ? "html-render:on" : "html-render:off").join("|")), s6 = re(() => Hw.value.map((_) => _.kind === "message" ? `${_.message.sessionId}:${_.message.order}:${Vn(_.message.content)}` : `${_.key}:${Vn(_.assistantMessage.content)}:${_.calls.map((k) => `${k.id}:${Vn(k.resultText)}`).join(",")}`).concat(s.value ? "html-render:on" : "html-render:off").join("|")), zw = re(() => W.value ? iv(ne.value) : "写一句话后会自动创建独立会话。"), a6 = re(() => !fe.value && (de.value || !!x.value.trim())), i6 = re(() => !xe.value && (et.value || !!W.value && !!Mt.value.trim()));
    function l6(_) {
      const k = /* @__PURE__ */ new WeakSet();
      try {
        return JSON.parse(JSON.stringify(_, (D, Z) => {
          if (typeof Z == "bigint") return String(Z);
          if (!(typeof Z == "function" || typeof Z == "symbol")) {
            if (!Z || typeof Z != "object") return Z;
            if (!k.has(Z))
              return k.add(Z), Z;
          }
        }));
      } catch {
        return {};
      }
    }
    gt(Cn, () => {
      Gn.value = Jh;
    }), gt(ee, (_) => {
      if (!_.length) {
        v.value = "";
        return;
      }
      const k = String(v.value || "").trim();
      k && _.some((D) => D.id === k) || (v.value = "");
    }, { immediate: !0 }), gt(v, () => {
      y.value = 0, A.value = null, R.value = "", $.value = !1, p.value = [], Ve.value === "characters" && ec();
    }), gt([
      Ve,
      v,
      fl
    ], ([_, k]) => {
      if (_ !== "characters") return;
      const D = String(k || "").trim();
      D && Wf(D);
    }), gt(bt, (_) => {
      if (!_.length) {
        y.value = 0;
        return;
      }
      const k = _.length - 1;
      y.value > k && (y.value = k);
    }), gt(er, () => {
      wt.value = {};
    }), gt([() => W.value, () => ke.value], ([_, k]) => {
      Dw(String(_ || ""), Number(k) || 0);
    }), gt(() => Ff.value.map((_) => _.id).join("|"), () => {
      V5();
    }, { immediate: !0 }), gt(() => ht.value.map((_) => _.id).join("|"), () => {
      Lw(ht.value);
    }, { immediate: !0 });
    function oi(_) {
      return _ instanceof Error ? _.message : String(_ || "unknown_error");
    }
    function fv() {
      Nr && (window.clearTimeout(Nr), Nr = null), J.value = "";
    }
    function Zu(_ = "", k = 4200) {
      const D = String(_ || "").trim();
      if (!D) {
        fv();
        return;
      }
      Nr && (window.clearTimeout(Nr), Nr = null), J.value = D, Nr = window.setTimeout(() => {
        Nr = null, J.value === D && (J.value = "");
      }, k);
    }
    function Ko(_, k = {}) {
      const D = l6(k);
      window.parent?.postMessage({
        source: lhe,
        type: _,
        payload: D
      }, window.location.origin);
    }
    function u6(_ = "host") {
      return `${_}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
    }
    function Ww() {
      try {
        return new DOMException("request_aborted", "AbortError");
      } catch {
        return /* @__PURE__ */ new Error("request_aborted");
      }
    }
    function ur(_, k = {}, D = {}) {
      const Z = u6();
      return D.signal?.aborted ? Promise.reject(Ww()) : (Ko(_, {
        ...k,
        requestId: Z
      }), new Promise((ue, pe) => {
        const De = () => {
          const Qe = ye.get(Z);
          Qe?.abort && D.signal && D.signal.removeEventListener("abort", Qe.abort), ye.delete(Z);
        }, ut = () => {
          De(), Ko("xb-tavern:cancel-request", { requestId: Z }), pe(Ww());
        };
        D.signal && D.signal.addEventListener("abort", ut, { once: !0 }), ye.set(Z, {
          resolve: ue,
          reject: pe,
          type: _,
          abort: D.signal ? ut : void 0,
          signal: D.signal
        });
      }));
    }
    function c6(_ = "", k = "") {
      const D = String(_ || ""), Z = String(k || "").trim();
      if (!D || !Z) return -1;
      const ue = D.indexOf(Z);
      if (ue >= 0) return ue + Z.length;
      const pe = D.replace(/\s+/g, ""), De = Z.replace(/\s+/g, "");
      if (!De) return -1;
      const ut = pe.indexOf(De);
      if (ut < 0) return -1;
      let Qe = 0;
      for (let tt = 0; tt < D.length; tt += 1)
        if (!/\s/.test(D[tt]) && (Qe += 1, Qe >= ut + De.length))
          return tt + 1;
      return -1;
    }
    function d6(_ = "", k = -1) {
      const D = String(_ || "");
      if (k < 0 || !D) return k;
      if (k >= D.length) return D.length;
      const Z = 80, ue = Math.min(D.length, k + Z), pe = /* @__PURE__ */ new Set([
        "。",
        "！",
        "？",
        "!",
        "?",
        "…"
      ]), De = /* @__PURE__ */ new Set([
        "”",
        "“",
        "’",
        "‘",
        "」",
        "』",
        "】",
        "）",
        ")",
        '"',
        "'",
        "*",
        "~",
        "～",
        "]"
      ]), ut = (Qe) => {
        let tt = Qe;
        for (; tt < D.length && De.has(D[tt]); ) tt += 1;
        return tt;
      };
      if (k > 0 && pe.has(D[k - 1])) return ut(k);
      for (let Qe = 0; Qe < Z && k + Qe < ue; Qe += 1) {
        const tt = k + Qe, Vt = D[tt];
        if (Vt === `
`) return tt + 1;
        if (pe.has(Vt)) return ut(tt + 1);
        if (Vt === "." && D.slice(tt, tt + 3) === "...") return ut(tt + 3);
      }
      return k;
    }
    function f6(_ = "", k = {}) {
      const D = String(k.slotId || "").trim();
      if (!D) return {
        content: _,
        inserted: !1,
        appended: !1
      };
      const Z = `[tavern-image:${D}]`, ue = String(_ || "");
      if (ue.includes(Z)) return {
        content: ue,
        inserted: !1,
        appended: !1
      };
      let pe = c6(ue, String(k.anchor || ""));
      if (pe >= 0 && (pe = d6(ue, pe)), pe >= 0) {
        const De = ue.slice(0, pe), ut = ue.slice(pe);
        let Qe = Z;
        return De.length > 0 && !De.endsWith(`
`) && (Qe = `
${Qe}`), ut.length > 0 && !ut.startsWith(`
`) && (Qe = `${Qe}
`), {
          content: `${De}${Qe}${ut}`,
          inserted: !0,
          appended: !1
        };
      }
      return {
        content: `${ue}${ue.length > 0 && !ue.endsWith(`
`) ? `
` : ""}${Z}`,
        inserted: !0,
        appended: !0
      };
    }
    function h6(_ = "", k = []) {
      let D = String(_ || ""), Z = 0, ue = 0;
      return (Array.isArray(k) ? k : []).forEach((pe) => {
        const De = pe && typeof pe == "object" ? pe : {};
        if (!De.slotId || De.success === !1) return;
        const ut = f6(D, De);
        D = ut.content, ut.inserted && (Z += 1), ut.appended && (ue += 1);
      }), {
        content: D,
        inserted: Z,
        appended: ue
      };
    }
    function Gw(_ = "", k = {}) {
      const D = Number(k.current) || 0, Z = Number(k.total) || 0, ue = Z ? ` ${D}/${Z}` : "";
      switch (_) {
        case "llm":
          return "正在分析画面";
        case "gen":
          return Z ? `准备生成 ${Z} 张图` : "准备生成图片";
        case "queued":
          return Number(k.ahead) > 0 ? `画图排队中，前方 ${Number(k.ahead)} 个任务` : `画图排队中${ue}`;
        case "progress":
          return `正在生成图片${ue}`;
        case "cooldown": {
          const pe = Number.isFinite(Number(k.remainingMs)) ? Number(k.remainingMs) : Number(k.duration);
          return !Number.isFinite(pe) || pe <= 0 ? "等待画图冷却" : `等待下一张图片${Z ? ` ${k.nextIndex || D}/${Z}` : ""}，剩余 ${(pe / 1e3).toFixed(1)}s`;
        }
        case "success": {
          const pe = Number(k.success) || 0, De = Z || pe;
          return De > 0 && pe === 0 ? `画图结束，${De} 张都失败` : `画图完成 ${pe}/${De}`;
        }
        default:
          return "正在处理画图";
      }
    }
    function hl() {
      We && (window.clearInterval(We), We = null);
    }
    function p6(_, k = {}) {
      hl();
      const D = Math.max(0, Number(k.duration) || 0), Z = Date.now() + D, ue = () => {
        if (Ue.value !== _) {
          hl();
          return;
        }
        const pe = Math.max(0, Z - Date.now());
        le.value = "running", me.value = Gw("cooldown", {
          ...k,
          remainingMs: pe
        }), pe <= 0 && hl();
      };
      ue(), D > 0 && (We = window.setInterval(ue, mhe));
    }
    async function m6() {
      try {
        const _ = await ur("xb-tavern:draw-status", {});
        ve.value = {
          provider: String(_.provider || "disabled"),
          enabled: _.enabled === !0,
          ready: _.ready === !0
        };
      } catch {
        ve.value = {
          provider: "disabled",
          enabled: !1,
          ready: !1
        };
      }
      return ve.value;
    }
    async function ju(_) {
      if (!_.length) return {
        items: [],
        changedCount: 0
      };
      const k = await ur("xb-tavern:apply-regex", { payload: { items: _ } }), D = k.result || k;
      return {
        items: Array.isArray(D.items) ? D.items : [],
        changedCount: Number(D.changedCount) || 0
      };
    }
    function Vw() {
      ia.forEach((_) => window.clearTimeout(_.timer)), ia.clear(), ni.clear(), ws.clear();
    }
    function Kw() {
      la += 1, Wo.clear(), Vw(), Ho.value = {};
    }
    function Jw(_, k) {
      const D = {
        ...Ho.value,
        [_]: k
      }, Z = Object.keys(D);
      Z.length > YC && Z.slice(0, Z.length - YC).forEach((ue) => {
        delete D[ue];
      }), Ho.value = D, Ve.value === "chat" && _t.value === "chat" && Qt(() => {
        Za(), Nv();
      });
    }
    function Uf(_, k) {
      const D = hI(_, k.actionCheckEvents || [], k.actionCheckBoundaries || []);
      return {
        text: D.text,
        actionCheckEvents: D.events
      };
    }
    function Yw(_, k, D, Z) {
      Jw(k, D), ws.set(_, Uf(D, Z));
    }
    function hv(_) {
      return _.role === "user" ? "userInput" : _.role === "assistant" ? "aiOutput" : null;
    }
    function pv(_) {
      return ["user", "assistant"].includes(_.role) && !_.error && !!String(_.content || "").trim();
    }
    function mv(_) {
      const k = Number(j5.value[cr(_)]);
      return Number.isFinite(k) && k >= 0 ? k : 0;
    }
    function gv(_) {
      return String(_.name || Kf(_.role) || "").trim();
    }
    function vv(_, k, D) {
      return [
        _,
        k.sessionId,
        String(k.order),
        k.role,
        String(k.name || ""),
        String(D.index ?? ""),
        D.placement,
        String(D.depth),
        D.characterOverride || "",
        D.actionCheckSignature || "",
        Vn(String(D.label || "")),
        Vn(D.text)
      ].join("");
    }
    function Xw(_, k) {
      const D = ov.value?.order ?? -1;
      return [
        "runtime",
        _,
        W.value,
        String(D),
        "assistant",
        String(k.index ?? ""),
        k.placement,
        String(k.depth),
        k.characterOverride || "",
        k.actionCheckSignature || "",
        Vn(String(k.label || "")),
        Vn(k.text)
      ].join("");
    }
    function Qw(_ = []) {
      return Na(_).map((k, D) => [
        D,
        k.toolCallId || "",
        k.createdAt || "",
        k.stat,
        k.action,
        k.roll,
        k.difficulty,
        k.insertAfterChars,
        k.success ? 1 : 0,
        k.summary || "",
        k.stakes || ""
      ].join(":")).join("|");
    }
    async function yv(_) {
      if (Wo.has(_.key)) return;
      const k = la;
      Wo.add(_.key);
      try {
        const D = await ju([{
          id: _.key,
          text: _.text,
          placement: _.placement,
          options: _.options
        }]);
        if (k !== la) return;
        const Z = D.items.find((ue) => ue.id === _.key) || D.items[0];
        Jw(_.key, Z?.text ?? _.text);
      } catch (D) {
        console.warn("[小白酒馆] 显示正则应用失败", D);
      } finally {
        Wo.delete(_.key);
      }
    }
    async function g6(_, k) {
      if (Wo.has(k.key)) return;
      const D = la;
      Wo.add(k.key);
      try {
        const Z = await ju([{
          id: k.key,
          text: k.text,
          placement: k.placement,
          options: k.options
        }]);
        if (D !== la || ni.get(_) !== k.key) return;
        const ue = Z.items.find((pe) => pe.id === k.key) || Z.items[0];
        Yw(_, k.key, ue?.text ?? k.text, k);
      } catch (Z) {
        console.warn("[小白酒馆] 生成中显示正则应用失败", Z), D === la && ni.get(_) === k.key && Yw(_, k.key, k.text, k);
      } finally {
        Wo.delete(k.key);
      }
    }
    function Zw(_, k) {
      ni.set(_, k.key);
      const D = ia.get(_);
      if (D) {
        D.key = k.key, D.input = k;
        return;
      }
      const Z = window.setTimeout(() => {
        const ue = ia.get(_);
        ue && (ia.delete(_), g6(_, ue.input));
      }, hhe);
      ia.set(_, {
        timer: Z,
        key: k.key,
        input: k
      });
    }
    function jw(_) {
      const k = String(_.content || "");
      if (!k) return "";
      if (!pv(_)) return k;
      const D = hv(_);
      if (!D) return k;
      const Z = mv(_), ue = gv(_), pe = vv("message", _, {
        placement: D,
        text: k,
        depth: Z,
        characterOverride: ue
      }), De = Ho.value[pe];
      return De !== void 0 ? De : (yv({
        key: pe,
        text: k,
        placement: D,
        options: {
          isMarkdown: !0,
          depth: Z,
          characterOverride: ue
        }
      }), k);
    }
    function v6(_) {
      const k = String(_.content || "");
      if (!k) return {
        text: "",
        actionCheckEvents: []
      };
      const D = _.role === "assistant" ? Na(_.runtimeEvents) : [];
      if (!D.length) return {
        text: jw(_),
        actionCheckEvents: []
      };
      if (!pv(_)) return {
        text: k,
        actionCheckEvents: D
      };
      const Z = hv(_);
      if (!Z) return {
        text: k,
        actionCheckEvents: D
      };
      const ue = c_(k, D), pe = mv(_), De = gv(_), ut = vv("message", _, {
        placement: Z,
        text: ue.text,
        depth: pe,
        characterOverride: De,
        actionCheckSignature: Qw(D)
      }), Qe = {
        key: ut,
        text: ue.text,
        placement: Z,
        options: {
          isMarkdown: !0,
          depth: pe,
          characterOverride: De
        },
        actionCheckEvents: D,
        actionCheckBoundaries: ue.boundaries
      }, tt = Ho.value[ut];
      return tt !== void 0 ? Uf(tt, Qe) : (yv(Qe), Uf(ue.text, Qe));
    }
    function y6(_) {
      const k = mv(_);
      return si(_).map((D, Z) => {
        const ue = String(D.text || "");
        if (!ue) return D;
        const pe = vv("reasoning", _, {
          placement: "reasoning",
          text: ue,
          depth: k,
          index: Z,
          label: D.label
        }), De = Ho.value[pe];
        return De !== void 0 ? {
          ...D,
          text: De
        } : (yv({
          key: pe,
          text: ue,
          placement: "reasoning",
          options: {
            isMarkdown: !0,
            depth: k
          }
        }), D);
      }).filter((D) => String(D.text || "").trim());
    }
    function eS(_ = "", k = []) {
      const D = String(_ || ""), Z = Na(k);
      if (!D && !Z.length) return {
        text: "",
        actionCheckEvents: []
      };
      if (!D) return {
        text: "",
        actionCheckEvents: Z
      };
      const ue = c_(D, Z), pe = 0, De = String(Kf("assistant") || "").trim(), ut = Xw("message", {
        placement: "aiOutput",
        text: ue.text,
        depth: pe,
        characterOverride: De,
        actionCheckSignature: Qw(Z)
      }), Qe = {
        key: ut,
        text: ue.text,
        placement: "aiOutput",
        options: {
          isMarkdown: !0,
          depth: pe,
          characterOverride: De
        },
        actionCheckEvents: Z,
        actionCheckBoundaries: ue.boundaries
      }, tt = Ho.value[ut];
      if (tt !== void 0) {
        const Vt = Uf(tt, Qe);
        return ws.set("runtime:message", Vt), Vt;
      }
      return Zw("runtime:message", Qe), ws.get("runtime:message") ?? {
        text: "",
        actionCheckEvents: []
      };
    }
    function _6(_ = "") {
      return eS(_).text;
    }
    function b6(_ = []) {
      return si(_).map((D, Z) => {
        const ue = String(D.text || "");
        if (!ue) return D;
        const pe = Xw("reasoning", {
          placement: "reasoning",
          text: ue,
          depth: 0,
          index: Z,
          label: D.label
        }), De = Ho.value[pe];
        if (De !== void 0)
          return ws.set(`runtime:reasoning:${Z}`, {
            text: De,
            actionCheckEvents: []
          }), {
            ...D,
            text: De
          };
        const ut = {
          key: pe,
          text: ue,
          placement: "reasoning",
          options: {
            isMarkdown: !0,
            depth: 0
          }
        }, Qe = `runtime:reasoning:${Z}`;
        return Zw(Qe, ut), {
          ...D,
          text: ws.get(Qe)?.text ?? ""
        };
      }).filter((D) => String(D.text || "").trim());
    }
    async function qf(_) {
      if (!_.length) return {
        items: [],
        changedCount: 0
      };
      const k = await ur("xb-tavern:substitute-params", { payload: { items: _ } }), D = k.result || k;
      return {
        items: Array.isArray(D.items) ? D.items : [],
        changedCount: Number(D.changedCount) || 0
      };
    }
    async function _v(_) {
      const k = await ur("xb-tavern:get-worldbook-runtime", { payload: {
        context: _.context,
        currentUserMessage: _.currentUserMessage,
        trigger: _.trigger,
        timedState: _.timedState
      } });
      return k.result || k;
    }
    const tS = async (_) => {
      const k = await ur("xb-tavern:build-native-chat-prompt", { payload: _ }, { signal: _.signal });
      return k.result || k;
    };
    async function nS(_ = {}, k = {}) {
      const D = await ur("xb-tavern:get-context", { payload: {
        characterId: _.characterId,
        includeHistory: _.includeHistory,
        includeWorldbooks: _.includeWorldbooks
      } }, k);
      return D.result || D;
    }
    function rS() {
      return String(t.value.character?.id || m.value || "").trim();
    }
    function w6() {
      return !!Vo(t.value.character?.name || "") && !!rS();
    }
    function S6(_) {
      return !!_ && typeof _ == "object" && !Array.isArray(_) && Object.keys(_).length > 0;
    }
    function T6(_ = {}, k) {
      const D = k?.contextSnapshot?.authorNote;
      return S6(D) ? {
        ..._,
        authorNote: { ...D }
      } : _;
    }
    async function E6(_) {
      const k = String(W.value || "").trim(), D = ne.value;
      if (!k || !D) throw new Error("当前没有可保存的会话。");
      const Z = Ta(_), ue = {
        ...W.value === k ? t.value || D.contextSnapshot || {} : D.contextSnapshot || t.value || {},
        authorNote: Z
      }, pe = await L_(k, {
        contextSnapshot: ue,
        characterId: String(ue.character?.id || D.characterId || ""),
        characterName: String(ue.character?.name || D.characterName || "")
      });
      pe && (H.value = H.value.map((De) => De.id === pe.id ? pe : De)), W.value === k && (t.value = ue);
    }
    async function Hf(_ = {}) {
      const k = String(_.sessionId || W.value || "").trim(), D = H.value.find((Qe) => Qe.id === k) || (k === W.value ? ne.value : null);
      if (!D) return t.value;
      const Z = String(D.characterId || "").trim();
      if (!Z || !_.force && w6() && rS() === Z) return t.value;
      const ue = ++Rf, pe = await nS({
        characterId: Z,
        includeHistory: !1
      });
      if (ue !== Rf || k !== String(W.value || "").trim()) return t.value;
      const De = T6(pe.context || t.value, D);
      bv({
        ...pe,
        context: De
      });
      const ut = await L_(k, {
        contextSnapshot: De,
        characterId: String(De.character?.id || D.characterId || ""),
        characterName: String(De.character?.name || D.characterName || "")
      });
      return ut && (H.value = H.value.map((Qe) => Qe.id === ut.id ? ut : Qe)), De;
    }
    async function zf(_ = W.value) {
      const k = String(_ || "").trim();
      return k ? await Hf({
        sessionId: k,
        force: !0
      }) : t.value;
    }
    function A6(_ = {}) {
      const k = String(_.requestId || ""), D = ye.get(k);
      if (D) {
        if (D.abort && D.signal && D.signal.removeEventListener("abort", D.abort), ye.delete(k), _.ok === !1) {
          const Z = String(_.error || "host_request_failed"), ue = /* @__PURE__ */ new Error(`${D.type}: ${Z}`), pe = String(_.errorStack || "");
          pe && (ue.stack = `${ue.message}
${pe}`), console.error("[小白酒馆] host request failed", {
            type: D.type,
            error: Z,
            errorName: String(_.errorName || ""),
            errorStack: pe
          }), D.reject(ue);
          return;
        }
        D.resolve(_), [
          "xb-tavern:list-regex-scripts",
          "xb-tavern:save-regex-script",
          "xb-tavern:delete-regex-script"
        ].includes(D.type) && Kw();
      }
    }
    function C6(_ = {}) {
      l.value = (_.hostRequestHeaders && typeof _.hostRequestHeaders == "object" ? _.hostRequestHeaders : l.value) || {}, E2(async () => {
        try {
          const k = await ur("xb-tavern:get-host-request-headers");
          return k.hostRequestHeaders && typeof k.hostRequestHeaders == "object" ? k.hostRequestHeaders : l.value;
        } catch {
          return l.value;
        }
      });
    }
    function bv(_) {
      C6(_), "context" in _ && (t.value = _.context || {}), "diagnostics" in _ && (n.value = _.diagnostics || {});
      const k = "hostMainFontSizePx" in _ ? dn(_.hostMainFontSizePx, u.value) : u.value;
      u.value = k, d.value = "hostProseLineHeightPx" in _ ? dn(_.hostProseLineHeightPx, Pt(k)) : Pt(k), "agentConfig" in _ && (r.value = _.agentConfig || r.value, Vu()), "tavernDisplaySettings" in _ && (o.value = zl(_.tavernDisplaySettings)), "htmlRenderEnabled" in _ && (s.value = _.htmlRenderEnabled !== !1), zg(_), f.value = _.availableCharacters || f.value, m.value = String(_.selectedCharacterId || t.value.character?.id || m.value || ""), E.value = n.value.message || "", W6().catch((D) => {
        w.value = D instanceof Error ? D.message : String(D || "create_session_failed"), Vf();
      }), Qt(Gu);
    }
    function k6() {
      !Nf || $f || ($f = !0, Q8());
    }
    function I6(_) {
      const k = _.availableCharacters;
      Array.isArray(k) && (f.value = k);
    }
    function x6(_) {
      return !!_ && !!(_.description || _.personality || _.scenario || _.firstMessage || _.mesExample || _.creatorNotes || _.characterDepthPrompt || _.alternateGreetings?.length);
    }
    function oS(_) {
      if (_.origin !== window.location.origin) return;
      const k = _.data || {};
      if (k.source === uhe) {
        if (k.type === "xb-tavern:host-result") {
          A6(k.payload || {});
          return;
        }
        if (k.type === "xb-tavern:draw-progress") {
          const D = k.payload || {};
          Ce.value && (Ue.value = Ce.value, le.value = D.state === "success" ? "success" : "running", D.state === "cooldown" ? p6(Ce.value, D.data || {}) : (hl(), me.value = Gw(String(D.state || ""), D.data || {})));
          return;
        }
        if (k.type === "xb-tavern:inline-image-progress") {
          window.dispatchEvent(new CustomEvent(d_, { detail: k.payload && typeof k.payload == "object" ? k.payload : {} }));
          return;
        }
        if (k.type === "xb-tavern:config") {
          bv(k.payload || {}), Nf = !0, k6();
          return;
        }
        if (k.type === "xb-tavern:context") {
          bv(k.payload || {});
          return;
        }
        k.type === "xb-tavern:config-saved" && Wg(k.payload || {});
      }
    }
    function sS() {
      Ve.value = "characters", w.value = "", v.value = "", aS();
    }
    function aS() {
      E.value = "正在读取角色列表", w.value = "", Ko("xb-tavern:refresh-context", {});
    }
    async function M6(_) {
      const k = String(_ || "").trim();
      if (!k || C.value) return;
      v.value = k, Wf(k);
      const D = U.value.find((ue) => ue.id === k);
      if (D && D.shallow !== !0 && x6(D)) return;
      const Z = ++ja;
      T.value = k, w.value = "";
      try {
        const ue = await nS({
          characterId: k,
          includeHistory: !1,
          includeWorldbooks: !1
        });
        if (Z !== ja || v.value !== k) return;
        I6(ue);
      } catch (ue) {
        if (Z !== ja || v.value !== k) return;
        w.value = ue instanceof Error ? ue.message : String(ue || "character_preview_failed");
      } finally {
        Z === ja && T.value === k && (T.value = "");
      }
    }
    async function Wf(_ = v.value) {
      const k = String(_ || "").trim();
      if (!k) {
        A.value = null, $.value = !1, p.value = [];
        return;
      }
      const D = ++Wu;
      try {
        const Z = await ur("xb-tavern:get-character-worldbook-state", { payload: { characterId: k } });
        if (D !== Wu || String(v.value || "").trim() !== k) return;
        A.value = Z.result || Z, R.value = "";
      } catch (Z) {
        if (D !== Wu || String(v.value || "").trim() !== k) return;
        R.value = oi(Z);
      }
    }
    async function P6() {
      const _ = String(v.value || "").trim();
      if (!(!_ || I.value)) {
        I.value = !0, R.value = "";
        try {
          let k = await ur("xb-tavern:activate-character-worldbook", { payload: { characterId: _ } }), D = k.result || k;
          if (D.action === "needs_import_confirmation") {
            const ue = String(D.name || "").trim(), pe = D.state;
            if (pe && (A.value = pe), !ue || !window.confirm(`世界书「${ue}」已存在，导入角色内嵌世界书会覆盖它。继续？`)) return;
            k = await ur("xb-tavern:activate-character-worldbook", { payload: {
              characterId: _,
              confirmed: !0
            } }), D = k.result || k;
          }
          const Z = String(D.action || "");
          if (Z === "selected" || Z === "imported") {
            const ue = D.state;
            ue && (A.value = ue), Z === "imported" && Ko("xb-tavern:refresh-context", {}), Df(String(D.name || ""));
            return;
          }
          if (Z === "needs_selection") {
            p.value = ce(D.worldbookOptions), $.value = !0;
            const ue = D.state;
            ue && (A.value = ue);
            return;
          }
          await Wf(_);
        } catch (k) {
          R.value = oi(k);
        } finally {
          I.value = !1;
        }
      }
    }
    function iS() {
      $.value = !1;
    }
    async function R6(_) {
      const k = String(v.value || "").trim(), D = String(_ || "").trim();
      if (!(!k || !D || I.value)) {
        I.value = !0, R.value = "";
        try {
          const Z = await ur("xb-tavern:bind-character-worldbook", { payload: {
            characterId: k,
            name: D
          } });
          A.value = Z.result || Z, $.value = !1, Ko("xb-tavern:refresh-context", {}), Df(D);
        } catch (Z) {
          R.value = oi(Z);
        } finally {
          I.value = !1;
        }
      }
    }
    function N6(_) {
      const k = bt.value;
      if (!k.length) {
        y.value = 0;
        return;
      }
      y.value = Math.max(0, Math.min(k.length - 1, Number(_) || 0));
    }
    function ec() {
      Qt(() => {
        Ka.value?.scrollSelectedIntoView();
      });
    }
    function lS(_) {
      const k = _e.value;
      if (!k.length || C.value) return;
      const D = String($e.value?.id || v.value || "").trim();
      if (!D) {
        v.value = k[_ < 0 ? k.length - 1 : 0]?.id || "", ec();
        return;
      }
      const Z = Math.max(0, k.findIndex((ue) => ue.id === D));
      v.value = k[Math.min(k.length - 1, Math.max(0, Z + _))]?.id || "", ec();
    }
    function $6(_) {
      _.key === "ArrowDown" ? (_.preventDefault(), lS(1)) : _.key === "ArrowUp" ? (_.preventDefault(), lS(-1)) : _.key === "Home" ? (_.preventDefault(), v.value = _e.value[0]?.id || "", ec()) : _.key === "End" ? (_.preventDefault(), v.value = _e.value.at(-1)?.id || "", ec()) : _.key === "Enter" && (_.preventDefault(), Ka.value?.openSessionArchive());
    }
    async function D6() {
      const _ = String($e.value?.id || v.value || "").trim();
      !_ || C.value || await G6(_);
    }
    let wv = 0;
    function Sv() {
      se.value = [], ke.value = 0, Pe.value = null, Le.value = null, Y.value = -1;
    }
    async function pl(_ = {}) {
      const k = String(_.sessionId || W.value || "").trim(), D = wv + 1;
      if (wv = D, !k) {
        Sv();
        return;
      }
      _.reset && Gf();
      const Z = Math.max(1, Math.floor(Number(al.value) || a.value || 1)), [ue, pe] = await Promise.all([mF(k, Z), hb(k)]);
      D !== wv || k !== W.value || (se.value = ue.messages, ke.value = ue.total, Pe.value = ue.loadedStartOrder, Le.value = ue.loadedEndOrder, Y.value = pe ?? -1, Dw(k, ue.total));
    }
    async function Es() {
      H.value = await dF();
      const _ = String(await fF() || "").trim();
      W.value = H.value.some((k) => k.id === _) ? _ : "", _ && !W.value && await Sl(""), await pl(), await $r(W.value), W.value && Hf({ sessionId: W.value });
    }
    async function L6(_ = {}) {
      const k = String(W.value || "").trim();
      if (!k) return null;
      const D = await Sx(k, { contract: mo({
        ...Lt.value,
        ..._
      }) });
      return D ? (H.value = H.value.map((Z) => Z.id === D.id ? D : Z), D) : null;
    }
    async function $r(_ = W.value) {
      const k = String(_ || "").trim();
      if (!k) {
        yt.value = [], he.value = [], we.value = [], Te.value = [], Ie.value = null, Av(), L.value = null, Ke.value = [], qe.value = "main", Be.value = null, Ee.value = [], Ye.value = null, je.value = [], It.value = "", Ne.value = "";
        return;
      }
      const [D, Z, ue, pe, De, ut, Qe] = await Promise.all([
        fo(k),
        SF(k, { limit: 18 }),
        No(k, { includeCompleted: !0 }),
        vM(k),
        cB(k),
        dB(k),
        Ma(k, "memory/state.md")
      ]), tt = pe && pe.status === "ready" && Array.isArray(pe.files) ? pe : await Xi(k);
      if (k === W.value && (yt.value = D, he.value = Z, we.value = ue, Te.value = Array.isArray(tt.files) ? tt.files.map((Vt) => ({
        path: String(Vt.path || ""),
        status: Vt.status === "stale" ? "stale" : "active",
        createdAt: Number(Vt.createdAt) || Number(Vt.updatedAt) || 0,
        updatedAt: Number(Vt.updatedAt) || 0,
        source: String(Vt.source || ""),
        staleFromOrder: Number.isFinite(Number(Vt.staleFromOrder)) ? Number(Vt.staleFromOrder) : void 0,
        contentLength: Math.max(0, Number(Vt.contentLength) || 0),
        title: String(Vt.title || ""),
        preview: String(Vt.preview || ""),
        searchText: String(Vt.searchText || "")
      })) : [], Ie.value = tt, L.value = Qe, Ke.value = De.documents, qe.value = De.activeDocId, Be.value = De.activeDocument, Ee.value = De.activePatches, Ye.value = ut.document, je.value = ut.patches, It.value = ut.activeLocationKey, !Te.value.some((Vt) => Vt.path === Ne.value))) {
        if (Qu.value && Ne.value) {
          Se.value = "当前档案已变化，草稿仍保留";
          return;
        }
        Ne.value = Te.value[0]?.path || "";
      }
    }
    async function O6() {
      if (O5.value = Date.now(), !ul && W.value && !(!he.value.some((_) => D5(_.status)) && !de.value && !et.value)) {
        ul = !0;
        try {
          await $r(W.value);
        } finally {
          ul = !1;
        }
      }
    }
    async function uS() {
      if (!W.value) return;
      const _ = sr(ne.value?.state || {}), k = await Cx(W.value), D = await t5({
        messages: k,
        contextSnapshot: await zf(W.value),
        chatPreset: Sr.value,
        historyMode: M.value,
        diagnostics: n.value,
        applySubstituteParams: qf,
        getNativeWorldInfoRuntime: _v
      });
      await Tx(W.value, {
        ...D,
        contract: _.contract,
        contextWindowStartOrder: Hl({
          messages: k,
          contextWindowStartOrder: _.contextWindowStartOrder
        }).contextWindowStartOrder
      }), await Es();
    }
    function tc() {
      bs += 1, x.value = "", N.value = "", z.value = "", oa.value = "", Xa.value = "", sa.value = "", qo.value = "", il.value = !1, ao.value = "history", jt.value = null, Gf(), Tv(), qg();
    }
    const Gf = Pn.resetWindowState, Tv = Rn.resetWindowState, F6 = Pn.revealOlderMessages, B6 = Rn.revealOlderMessages;
    function U6(_ = {}) {
      return [String(_.firstMessage || _.first_mes || "").trim(), ...ce(_.alternateGreetings || _.alternate_greetings)].filter(Boolean);
    }
    async function q6(_, k, D = 0) {
      const Z = U6(k.character || {}), ue = Math.max(0, Math.min(Z.length - 1, Number(D) || 0)), pe = String(Z[ue] || "").trim();
      pe && await Oc(_, {
        role: "assistant",
        name: String(k.character?.name || ""),
        content: pe,
        contextSnapshot: k,
        chatPresetId: String(Sr.value.id || ""),
        chatPresetName: String(Sr.value.name || ""),
        presetId: String(Sr.value.id || ""),
        presetName: String(Sr.value.name || "")
      });
    }
    async function cS(_ = {}) {
      const k = _.contextSnapshot || t.value, D = PI({
        context: k,
        chatPreset: Sr.value,
        currentUserMessage: "",
        historyMode: M.value,
        turn: 0,
        entryStates: {},
        diagnostics: n.value
      }), Z = await wx({
        title: String(k.character?.name || "未选择角色"),
        characterId: String(k.character?.id || ""),
        characterName: String(k.character?.name || "未选择角色"),
        contextSnapshot: k,
        buildSnapshot: D.buildSnapshot,
        chatPresetId: String(Sr.value.id || ""),
        chatPresetName: String(Sr.value.name || ""),
        presetId: String(Sr.value.id || ""),
        presetName: String(Sr.value.name || ""),
        state: {
          turn: 0,
          worldEntryStates: {},
          nativeWorldInfoTimedState: {
            sticky: {},
            cooldown: {}
          }
        }
      });
      return _.includeFirstMessage !== !1 && await q6(Z.id, k, _.greetingIndex), W.value = Z.id, await Es(), Z;
    }
    async function dS(_ = {}) {
      await cS(_), Ve.value = "chat", _t.value = "chat", tr(!0);
    }
    async function H6() {
      if (de.value || fe.value) return;
      const _ = W.value ? await zf(W.value) : t.value;
      tc(), await dS({ contextSnapshot: _ });
    }
    async function z6() {
      if (qt.value) {
        K6();
        return;
      }
      sS();
    }
    function Vf() {
      C.value = "", b.value = 0;
    }
    async function W6() {
      const _ = C.value;
      if (!_ || String(t.value.character?.id || m.value || "").trim() !== _) return;
      if (!Vo(t.value.character?.name || "")) {
        Vf(), w.value = "没有读到这张角色卡。";
        return;
      }
      const k = b.value;
      Vf(), w.value = "", tc(), await dS({ greetingIndex: k });
    }
    async function G6(_) {
      const k = String(_ || "").trim();
      if (!k || C.value) return;
      const D = k === String(v.value || "").trim() ? y.value : 0;
      v.value = k, y.value = D, W.value = "", Sv(), await Sl(""), await $r(""), tc(), m.value = k, w.value = "", E.value = "正在读取角色卡", C.value = k, b.value = D, Ko("xb-tavern:refresh-context", {
        characterId: k,
        includeHistory: !1
      });
    }
    async function fS(_) {
      tc(), Av(), W.value = _, await Sl(_), await pl({
        reset: !0,
        sessionId: _
      }), await $r(_), Hf({
        sessionId: _,
        force: !0
      }), Ve.value = "chat", _t.value = "chat", tr(!0);
    }
    async function V6(_, k) {
      k?.stopPropagation();
      const D = String(_ || "").trim();
      if (!D) return;
      const Z = H.value.find((Qe) => Qe.id === D), ue = String(Z?.characterId || "").trim(), pe = D === W.value, De = ue ? H.value.filter((Qe) => Qe.id !== D && String(Qe.characterId || "").trim() === ue).slice().sort((Qe, tt) => (Number(tt.updatedAt) || Number(tt.createdAt) || 0) - (Number(Qe.updatedAt) || Number(Qe.createdAt) || 0))[0] : null, ut = nv(Z) || "这个会话";
      if (window.confirm(`删除「${ut}」？`) && (pe && de.value && zo.value?.abort(), !!await hF(D))) {
        if (W5(D), !pe) {
          await Es(), Ve.value = W.value ? "chat" : "home", W.value && (_t.value = "chat", tr(!0));
          return;
        }
        if (tc(), De?.id) {
          W.value = De.id, await Sl(De.id), await Es(), Ve.value = "chat", _t.value = "chat", tr(!0);
          return;
        }
        W.value = "", Sv(), await Sl(""), await Es(), ue && (v.value = ue, Wf(ue)), Ve.value = ue ? "characters" : "home";
      }
    }
    function K6() {
      Ve.value = "chat", _t.value = "chat", tr(!0);
    }
    function J6(_ = "history") {
      ao.value = _, il.value = !0;
    }
    function Y6() {
      il.value = !1;
    }
    async function X6() {
      const _ = oa.value.trim();
      if (qo.value = "", sa.value = "", Xa.value = "", !_) {
        qo.value = "先写一句话。";
        return;
      }
      const k = bs + 1;
      bs = k;
      const D = W.value;
      sa.value = "模拟中";
      try {
        const Z = await zf(D), ue = await aj({
          sessionId: D,
          agentConfig: r.value,
          contextSnapshot: Z,
          chatPreset: Sr.value,
          currentUserMessage: _,
          runtimeState: sr(ne.value?.state || {}),
          diagnostics: n.value,
          historyMode: M.value,
          applyRegex: ju,
          applySubstituteParams: qf,
          getNativeWorldInfoRuntime: _v,
          buildNativeChatPrompt: tS
        });
        if (k !== bs || D !== W.value) return;
        Xa.value = ue.requestSnapshot.rawRequestJson || ue.requestSnapshot.rawMessagesJson || "", sa.value = `模拟完成 · ${ue.requestSnapshot.providerLabel || ue.provider} / ${ue.model || "未选择模型"}`;
      } catch (Z) {
        if (k !== bs || D !== W.value) return;
        console.error("[小白酒馆] simulate request failed", Z), sa.value = "", qo.value = Z instanceof Error ? Z.message : String(Z || "simulate_failed");
      }
    }
    function Ev(_ = "", k = 180) {
      const D = String(_ || "").trim();
      return D.length > k ? `${D.slice(0, k)}...` : D;
    }
    const { discardMemoryDraft: Q6, enterMemoryEditMode: Z6, formatMemoryFileMeta: j6, invalidateMemoryFileRecordLoad: Av, loadMemoryFileIntoEditor: hS, loadSelectedMemoryFileRecord: e8, memoryFileStatusLabel: t8, previewMemoryDraft: n8, saveSelectedMemoryFile: r8, selectMemoryFile: o8 } = ece({
      memoryEditorBaseContent: V,
      memoryEditorDocumentAvailable: qw,
      memoryEditorDraft: P,
      memoryEditorDirty: Qu,
      memoryEditorLoadedPath: Q,
      memoryEditorMode: ae,
      memoryEditorStatus: Se,
      selectedMemoryFileEntry: Xu,
      selectedMemoryFilePath: Ne,
      selectedMemoryFileRecord: Oe,
      selectedSessionId: W,
      refreshRecords: $r
    });
    async function s8(_) {
      const k = String(_.id || "");
      if (!(!k || !W.value || Nw.value || At.value)) {
        At.value = k, Ht.value = "记忆正在重试。";
        try {
          const [D, Z] = await Promise.all([_u(W.value, _.userOrder), _u(W.value, _.assistantOrder)]), ue = D?.role === "user" ? D : null, pe = Z?.role === "assistant" && !Z.error ? Z : null;
          if (!ue || !pe) {
            Ht.value = "原文楼层不存在，无法重试。", await $r();
            return;
          }
          const De = await BR({
            sessionId: W.value,
            agentConfig: r.value,
            userMessage: ue,
            assistantMessage: pe,
            turn: _.turn,
            trigger: "manual_retry",
            assistantPreset: cl.value,
            sessionContract: Lt.value
          });
          Ht.value = De.ok ? "" : `失败：${De.error || "manager_retry_failed"}`;
        } catch (D) {
          Ht.value = D instanceof Error ? D.message : String(D || "manager_retry_failed");
        } finally {
          try {
            await $r();
          } finally {
            At.value = "";
          }
        }
      }
    }
    function a8(_) {
      return !!_ && !!At.value && String(_.id || "") === At.value;
    }
    function Kf(_ = "") {
      return _ === "assistant" ? Vo(ne.value?.characterName || Ss.value.name || "", "角色") : _ === "user" ? Vo(jg.value || "", "User") : {
        system: "规则",
        tool: "工具结果"
      }[_] || _ || "未知";
    }
    function si(_) {
      return (Array.isArray(_) ? _ : Array.isArray(_?.thoughts) ? _.thoughts : []).map((k, D) => ({
        label: String(k?.label || `思考 ${D + 1}`).trim() || `思考 ${D + 1}`,
        text: String(k?.text || "").trim()
      })).filter((k) => k.text);
    }
    function i8(_, k = !1) {
      const D = si(_);
      if (!D.length) return "";
      const Z = k ? "正在思考" : "展开思考块";
      return D.length > 1 ? `${Z}（${D.length} 段）` : Z;
    }
    function l8(_) {
      const k = Number(_) || 0;
      if (!k) return "";
      try {
        return new Intl.DateTimeFormat("zh-CN", {
          hour: "2-digit",
          minute: "2-digit"
        }).format(new Date(k));
      } catch {
        return "";
      }
    }
    function cr(_) {
      return `${_.sessionId}:${_.order}`;
    }
    function nc(_) {
      return `manager:${_.sessionId}:${_.order}`;
    }
    function Cv(_) {
      return !de.value && !_.error && ["user", "assistant"].includes(_.role);
    }
    function kv(_) {
      return !et.value && !_.error && ["user", "assistant"].includes(_.role);
    }
    function pS(_) {
      if (de.value) return !1;
      const k = ov.value;
      return !k || k.sessionId !== _.sessionId || k.order !== _.order ? !1 : _.role === "user" ? !0 : _.role === "assistant";
    }
    function mS(_) {
      if (et.value) return !1;
      if (_.role === "user") return !0;
      const k = [...yt.value].sort((Z, ue) => Z.order - ue.order), D = k.findIndex((Z) => Z.order === _.order);
      return D < 0 ? !1 : k.slice(0, D + 1).some((Z) => Z.role === "user");
    }
    function gS(_) {
      return wo.value === cr(_);
    }
    function vS(_) {
      return wo.value === nc(_);
    }
    function yS(_) {
      return vS(_) && Ya.value.trim() !== String(_.content || "").trim();
    }
    function Dr(_, k, D) {
      const Z = `${cr(_)}:${k}`;
      io.value = {
        ...io.value,
        [Z]: D ? "success" : "error"
      }, window.setTimeout(() => {
        const ue = { ...io.value };
        delete ue[Z], io.value = ue;
      }, 1100);
    }
    function ai(_, k, D) {
      const Z = `${nc(_)}:${k}`;
      io.value = {
        ...io.value,
        [Z]: D ? "success" : "error"
      }, window.setTimeout(() => {
        const ue = { ...io.value };
        delete ue[Z], io.value = ue;
      }, 1100);
    }
    function u8(_, k) {
      return io.value[`${cr(_)}:${k}`] || "";
    }
    function c8(_, k) {
      return io.value[`${nc(_)}:${k}`] || "";
    }
    function Jf(_) {
      return Ce.value === cr(_);
    }
    function ii(_, k = "", D = "running", Z = 0) {
      if (hl(), Ue.value = cr(_), le.value = D, me.value = k, Z > 0) {
        const ue = cr(_);
        window.setTimeout(() => {
          Ue.value !== ue || Ce.value === ue || (Ue.value = "", le.value = "idle", me.value = "");
        }, Z);
      }
    }
    function _S(_) {
      return Ue.value === cr(_) ? me.value : "";
    }
    function d8(_) {
      return Ue.value === cr(_) ? `is-${le.value}` : "";
    }
    function Iv(_) {
      return de.value || gS(_) || _.error || !["user", "assistant"].includes(_.role) || Ce.value && !Jf(_) ? !1 : !!ll(_.content || "");
    }
    function f8(_) {
      return Jf(_) ? _S(_) || "停止画图" : Iv(_) ? (ve.value.enabled && ve.value.ready, "为这条消息画图") : Ce.value ? "已有画图任务正在运行" : "这条消息暂不能画图";
    }
    async function bS(_ = "") {
      const k = String(_ || "");
      if (!k) return !1;
      try {
        if (navigator.clipboard?.writeText)
          return await navigator.clipboard.writeText(k), !0;
      } catch {
      }
      try {
        const D = document.createElement("textarea");
        D.value = k, D.setAttribute("readonly", "readonly"), D.style.position = "fixed", D.style.opacity = "0", D.style.pointerEvents = "none", document.body.appendChild(D), D.select(), D.setSelectionRange(0, D.value.length);
        const Z = document.execCommand("copy");
        return D.remove(), Z;
      } catch {
        return !1;
      }
    }
    async function h8(_) {
      Dr(_, "copy", await bS(_.content || ""));
    }
    async function p8(_) {
      ai(_, "copy", await bS(_.content || ""));
    }
    async function m8(_) {
      if (Jf(_)) {
        aa.value?.abort(), ii(_, "正在停止画图", "running");
        return;
      }
      if (!Iv(_)) {
        Dr(_, "draw", !1);
        return;
      }
      const k = await m6();
      if (!k.enabled || !k.ready) {
        ii(_, "请开启小白X画图模块", "error", 3200), Dr(_, "draw", !1);
        return;
      }
      const D = new AbortController();
      aa.value = D, Ce.value = cr(_), ii(_, "正在准备画图", "running");
      try {
        const Z = await ur("xb-tavern:draw-generate", { payload: {
          source: "tavern",
          text: ll(_.content || ""),
          title: Kf(_.role),
          sessionId: _.sessionId,
          messageOrder: _.order,
          role: _.role,
          characterName: ne.value?.characterName || Ss.value.name || ""
        } }, { signal: D.signal });
        if (D.signal.aborted) {
          Dr(_, "draw", !1);
          return;
        }
        const ue = Z.result || Z, pe = h6(_.content || "", Array.isArray(ue.images) ? ue.images : []);
        if (!pe.inserted) {
          const ut = Number(ue.success) || 0, Qe = Number(ue.total) || (Array.isArray(ue.images) ? ue.images.length : 0);
          ii(_, Qe > 0 && ut === 0 ? `画图任务结束：${Qe} 张都失败了，未插入图片。` : ut > 0 ? `画图完成 ${ut}/${Qe || ut}，但没有返回可用图片占位。` : "画图任务结束，但没有返回可用图片。", "error", 4200), Dr(_, "draw", !1);
          return;
        }
        const De = await fd(_.sessionId, _.order, { content: pe.content });
        De && W.value === _.sessionId && await pl({ sessionId: _.sessionId }), ii(_, `${phe}${pe.appended ? `，${pe.appended} 张追加到末尾` : ""}`, "success", 2600), Dr(De || _, "draw", !!De), Qt(Za);
      } catch (Z) {
        const ue = oi(Z);
        /abort|已取消|request_aborted/i.test(ue) ? ii(_, "配图已取消", "error", 1800) : ii(_, `配图失败：${ue}`, "error", 4200), Dr(_, "draw", !1);
      } finally {
        aa.value === D && (aa.value = null), Ce.value === cr(_) && window.setTimeout(() => {
          !aa.value && Ce.value === cr(_) && (Ce.value = "");
        }, 1200);
      }
    }
    function g8(_) {
      Cv(_) && (wo.value = cr(_));
    }
    function v8(_) {
      kv(_) && (wo.value = nc(_), Ya.value = _.content || "", Qt(() => {
        const k = ra.value?.querySelector(`[data-manager-message-editor="${nc(_)}"]`);
        k && (xv(k), k.focus(), k.setSelectionRange(k.value.length, k.value.length));
      }));
    }
    function Jo() {
      wo.value = "", Ya.value = "", Qt(() => {
        Za(), xf();
      });
    }
    function xv(_, k = {}) {
      if (!_) return;
      const D = Number(k.minHeight ?? 144), Z = Number(k.maxHeight ?? 420);
      _.style.height = "auto", _.style.height = `${Math.min(Math.max(_.scrollHeight, D), Z)}px`;
    }
    function ml(_) {
      _ && (_.style.height = "");
    }
    function y8(_, k) {
      if (_.key === "Escape") {
        _.preventDefault(), Jo();
        return;
      }
      _.key === "Enter" && (_.ctrlKey || _.metaKey) && (_.preventDefault(), SS(k));
    }
    function _8(_) {
      xv(_.target);
    }
    function b8(_) {
      const k = _.target;
      xv(k, k.closest(".manager-compose") ? {
        minHeight: 48,
        maxHeight: 180
      } : {
        minHeight: 36,
        maxHeight: 76
      });
    }
    async function wS(_ = "", k = 0) {
      const D = String(_ || "").trim(), Z = Number(k);
      !D || !Number.isFinite(Z) || (await gM(D, Z - 1), await rM(D, Z - 1), await mM(D, Z), await nM(D, Z), await Xi(D));
    }
    function Mv(_) {
      const k = Math.max(0, _ - 1);
      return `会话记忆、人物记忆和事件线索会回滚到${k > 0 ? `第 ${k} 楼后的状态` : "开局前状态"}。`;
    }
    async function w8(_, k = {}) {
      if (!Cv(_)) return;
      const D = ("content" in k ? String(k.content || "") : String(_.content || "")).trim();
      if (!D) {
        Dr(_, "edit", !1);
        return;
      }
      if (!k.rerun && D === String(_.content || "").trim()) {
        Jo();
        return;
      }
      if (k.rerun && _.role !== "user") {
        Jo(), await Pv(_);
        return;
      }
      const Z = Math.max(1, Number(_.order) + 1);
      if (!window.confirm(`保存第 ${Z} 楼的编辑？

${Mv(Z)}`)) return;
      const ue = await z8(_, await H8(_, D)), pe = await fd(_.sessionId, _.order, {
        content: ue,
        ..._.role === "user" ? { runtimeEvents: [] } : {}
      });
      pe && (await s0(_.sessionId, _.order), await wS(_.sessionId, _.order)), pe && W.value && (await pl({ sessionId: W.value }), await $r(W.value)), Jo(), Dr(pe || _, "edit", !!pe), pe && k.rerun ? pe.role === "user" ? await Qf({
        messageText: pe.content,
        reuseUserMessageOrder: pe.order,
        rerollRuntimeEvents: !0
      }) : await Pv(pe) : pe && await uS();
    }
    async function SS(_, k = {}) {
      if (!kv(_)) return;
      const D = Ya.value.trim();
      if (!D) {
        ai(_, "edit", !1);
        return;
      }
      if (!k.rerun && !yS(_)) {
        Jo();
        return;
      }
      if (k.rerun && _.role !== "user") {
        Jo(), await Rv(_);
        return;
      }
      const Z = await bF(_.sessionId, _.order, {
        content: D,
        clearProtocolPayload: _.role === "assistant"
      });
      Z && W.value === _.sessionId && (yt.value = await fo(_.sessionId)), Jo(), ai(Z || _, "edit", !!Z), Z && k.rerun && await Rv(Z);
    }
    async function S8(_) {
      const k = await pb(_.sessionId, _.order);
      return k.length ? k : [_.order];
    }
    async function T8(_) {
      if (de.value) return;
      const k = await S8(_), D = Math.max(1, Number(_.order) + 1), Z = k.length > 1 ? `从第 ${D} 楼开始删除后续剧情？将移除 ${k.length} 楼。

${Mv(D)}` : `删除第 ${D} 楼？

${Mv(D)}`;
      if (!window.confirm(Z)) return;
      const ue = Math.min(...k);
      await s0(_.sessionId, ue);
      const pe = await Ax(_.sessionId, k);
      pe > 0 && await wS(_.sessionId, ue), W.value && (await pl({ sessionId: W.value }), await $r(W.value)), pe > 0 && await uS(), wo.value.startsWith(`${_.sessionId}:`) && Jo(), Dr(_, "delete", pe > 0);
    }
    async function Pv(_) {
      if (!pS(_)) {
        Dr(_, "rerun", !1);
        return;
      }
      const k = _.role === "user" ? _ : await vF(_.sessionId, _.order);
      if (!k) {
        Dr(_, "rerun", !1);
        return;
      }
      Dr(_, "rerun", !0), await Qf({
        messageText: k.content,
        reuseUserMessageOrder: k.order
      });
    }
    function E8(_) {
      const k = [...yt.value].sort((ue, pe) => ue.order - pe.order), D = k.findIndex((ue) => ue.order === _.order);
      if (D < 0) return [_.order];
      if (_.role !== "user") {
        let ue = D;
        for (let De = D; De >= 0; De -= 1) if (k[De]?.role === "user") {
          ue = De;
          break;
        }
        const pe = [];
        for (let De = ue + 1; De < k.length; De += 1) {
          const ut = k[De];
          if (ut.role === "user") break;
          pe.push(ut.order);
        }
        return pe.length ? pe : [_.order];
      }
      const Z = [];
      for (let ue = D; ue < k.length; ue += 1) {
        const pe = k[ue];
        if (ue > D && pe.role === "user") break;
        Z.push(pe.order);
      }
      return Z;
    }
    async function A8(_) {
      if (et.value) return;
      const k = E8(_), D = _.role === "user" ? `删除这轮助手对话？将移除 ${k.length} 条记录。` : "删除这条助手回复？";
      if (!window.confirm(D)) return;
      const Z = await O_(_.sessionId, k);
      W.value === _.sessionId && (yt.value = await fo(_.sessionId)), wo.value.startsWith(`manager:${_.sessionId}:`) && Jo(), ai(_, "delete", Z > 0);
    }
    async function Rv(_) {
      if (!mS(_)) {
        ai(_, "rerun", !1);
        return;
      }
      const k = [...yt.value].sort((De, ut) => De.order - ut.order), D = k.findIndex((De) => De.order === _.order), Z = _.role === "user" ? _ : [...k.slice(0, Math.max(0, D + 1))].reverse().find((De) => De.role === "user");
      if (!Z || !W.value) {
        ai(_, "rerun", !1);
        return;
      }
      const ue = k.filter((De) => De.order >= Z.order).map((De) => De.order);
      if (ue.length > 2 && !window.confirm(`从这里重新询问助手会移除后面的 ${ue.length} 条记录。继续？`))
        return;
      const pe = k.filter((De) => De.order < Z.order);
      ai(_, "rerun", !0), await kS(Z.sessionId, Z.content, {
        historyBeforeTurn: pe,
        replaceOrdersAfterAppend: ue
      });
    }
    const Nv = Pn.updateScrollButtons, TS = Rn.updateScrollButtons, tr = Pn.scrollToBottom, C8 = Pn.scrollToTop, $v = Rn.scrollToBottom, k8 = Rn.scrollToTop, I8 = Pn.handleScroll, x8 = Rn.handleScroll, M8 = Pn.handleWheel, P8 = Rn.handleWheel, R8 = Pn.handleTouchStart, N8 = Rn.handleTouchStart, $8 = Pn.handleTouchMove, D8 = Rn.handleTouchMove;
    function L8(_) {
      _.key === "Enter" && (_.isComposing || _.shiftKey || _.altKey || (_.ctrlKey || _.metaKey || window.innerWidth >= 760) && (_.preventDefault(), Qf()));
    }
    function O8(_) {
      _.key === "Enter" && (_.isComposing || _.shiftKey || _.altKey || (_.ctrlKey || _.metaKey || window.innerWidth >= 760) && (_.preventDefault(), IS()));
    }
    function ES() {
      ei && (window.clearTimeout(ei), ei = null);
    }
    function Yf(_) {
      const k = jt.value || null, D = String(_.id || k?.id || `manager-compaction-${Date.now()}`), Z = Number(_.visibleSince) || (k?.id === D ? Number(k.visibleSince) : 0) || Date.now();
      jt.value = {
        id: D,
        active: !0,
        resolved: !1,
        currentTokens: 0,
        yieldTokens: 0,
        triggerTokens: 0,
        status: "正在释放较早管理员对话...",
        ...k,
        ..._,
        visibleSince: Z
      };
    }
    function AS(_ = 3e3) {
      const k = jt.value?.id || "", D = Number(jt.value?.visibleSince) || Date.now(), Z = Math.max(0, Date.now() - D), ue = Math.max(0, _ - Z);
      ES(), ei = window.setTimeout(() => {
        ei = null, !(!k || jt.value?.id !== k) && (jt.value = null);
      }, ue);
    }
    function Dv(_, k) {
      const D = Ze.value?.sessionId === _ ? Ze.value : {
        sessionId: _,
        messages: [],
        draft: null,
        nextOrder: XC
      }, Z = D.nextOrder;
      return D.nextOrder += 1, Ze.value = D, {
        sessionId: _,
        order: Z,
        role: k.role,
        content: k.content,
        name: k.name,
        error: k.error === !0,
        createdAt: Date.now(),
        updatedAt: Date.now(),
        provider: k.provider,
        model: k.model,
        finishReason: k.finishReason,
        thoughts: k.thoughts,
        providerPayload: k.providerPayload,
        toolCalls: k.toolCalls,
        toolCallId: k.toolCallId,
        toolName: k.toolName,
        toolDisplay: k.toolDisplay
      };
    }
    function F8(_, k = {}) {
      return !Number.isFinite(Number(k.reuseUserMessageOrder)) && _.trim().startsWith("/");
    }
    function B8(_) {
      if (_ == null) return "";
      if (typeof _ == "string") return _.trim();
      if (typeof _ == "number" || typeof _ == "boolean" || typeof _ == "bigint") return String(_);
      try {
        return JSON.stringify(_, null, 2);
      } catch {
        return String(_);
      }
    }
    async function U8(_, k = {}) {
      if (!F8(_, k)) return _;
      const D = await ur("xb-tavern:run-slash-command", { payload: { command: _ } }), Z = D.result || D, ue = Z.execution && typeof Z.execution == "object" ? Z.execution : {};
      if (Z.ok === !1 || ue.isError === !0 || ue.isAborted === !0) throw new Error(String(Z.error || ue.errorMessage || ue.abortReason || "slash_command_failed"));
      const pe = B8(Z.pipe);
      return pe || (x.value = "", Qt(() => ml(Pr.value)), Zu("斜杠命令已执行，没有输出。", 2600), "");
    }
    function q8(_ = {}) {
      const k = {}, D = String(_.user?.name || "").trim(), Z = String(_.character?.name || "").trim();
      return D && (k.name1Override = D), Z && (k.name2Override = Z), k;
    }
    async function H8(_, k) {
      const D = (H.value.find((Z) => Z.id === _.sessionId) || (W.value === _.sessionId ? ne.value : null))?.contextSnapshot || t.value || {};
      return (await qf([{
        id: `edit:${_.sessionId}:${_.order}`,
        text: k,
        options: q8(D)
      }])).items[0]?.text ?? k;
    }
    async function z8(_, k) {
      const D = hv(_);
      return D ? (await ju([{
        id: `edit:${_.sessionId}:${_.order}`,
        text: k,
        placement: D,
        options: {
          isEdit: !0,
          characterOverride: gv(_)
        }
      }])).items[0]?.text ?? k : k;
    }
    function Xf(_) {
      const k = String(_ || "").trim();
      return k ? ((!Ze.value || Ze.value.sessionId !== k) && (Ze.value = {
        sessionId: k,
        messages: [],
        draft: Dv(k, {
          role: "assistant",
          content: "正在思考..."
        }),
        nextOrder: XC + 1
      }), Ze.value) : null;
    }
    function CS(_ = "") {
      const k = String(_ || "").trim();
      (!k || Ze.value?.sessionId === k) && (Ze.value = null);
    }
    function W8(_, k) {
      const D = Xf(_);
      D && (D.draft = {
        ...D.draft || Dv(_, {
          role: "assistant",
          content: k.content
        }),
        ...k,
        role: "assistant",
        content: k.content,
        updatedAt: Date.now()
      });
    }
    function G8(_) {
      return Array.isArray(_.toolCalls) && _.toolCalls.length ? _.toolCalls.map((k) => ({
        id: typeof k?.id == "string" ? k.id : "",
        name: typeof k?.name == "string" ? k.name : "",
        arguments: typeof k?.arguments == "string" ? k.arguments : "{}"
      })) : Array.isArray(_.tool_calls) ? _.tool_calls.map((k) => ({
        id: typeof k?.id == "string" ? k.id : "",
        name: typeof k?.function?.name == "string" ? k.function.name : "",
        arguments: typeof k?.function?.arguments == "string" ? k.function.arguments : "{}"
      })) : [];
    }
    function V8(_, k) {
      const D = Xf(_);
      D && (D.messages = [...D.messages, Dv(_, {
        role: k.role,
        content: String(k.content || ""),
        name: k.name,
        thoughts: Array.isArray(k.thoughts) ? si(k.thoughts) : void 0,
        providerPayload: k.providerPayload,
        toolCalls: k.role === "assistant" ? G8(k) : void 0,
        toolCallId: k.role === "tool" ? String(k.toolCallId || k.tool_call_id || "") : void 0,
        toolName: k.role === "tool" ? String(k.toolName || "") : void 0,
        toolDisplay: k.role === "tool" ? k.toolDisplay : void 0
      })]);
    }
    function K8(_, k) {
      const D = String(_ || "").trim();
      if (D) {
        if (k.type === "clear_stream_draft") {
          const Z = Xf(D);
          Z && (Z.draft = null);
          return;
        }
        V8(D, k.message);
      }
    }
    function rc() {
      Vw(), N.value = "", q.value = [], zt.value = [], B.value = !1, X.value = "";
    }
    async function J8(_, k, D, Z) {
      const ue = (Array.isArray(k) ? k : []).filter((Qe) => Qe && ["assistant", "tool"].includes(Qe.role)), pe = ue.length ? ue : [{
        role: "assistant",
        content: D
      }], De = [...pe].map((Qe, tt) => ({
        message: Qe,
        index: tt
      })).reverse().find(({ message: Qe }) => {
        const tt = Array.isArray(Qe.toolCalls) && Qe.toolCalls.length || Array.isArray(Qe.tool_calls) && Qe.tool_calls.length;
        return Qe.role === "assistant" && !tt;
      })?.index ?? -1;
      De < 0 ? pe.push({
        role: "assistant",
        content: D
      }) : String(pe[De]?.content || "").trim() || (pe[De] = {
        ...pe[De],
        content: D
      });
      const ut = [];
      for (let Qe = 0; Qe < pe.length; Qe += 1) {
        const tt = pe[Qe], Vt = Array.isArray(tt.toolCalls) && tt.toolCalls.length || Array.isArray(tt.tool_calls) && tt.tool_calls.length, So = tt.role === "assistant" && !Vt && Qe === pe.length - 1, gl = await fh(_, {
          role: tt.role,
          content: So ? String(tt.content || "").trim() || D : String(tt.content || ""),
          name: tt.name,
          thoughts: tt.thoughts,
          providerPayload: tt.providerPayload,
          toolCalls: tt.toolCalls,
          tool_calls: tt.tool_calls,
          toolCallId: tt.toolCallId || tt.tool_call_id,
          toolName: tt.toolName,
          toolDisplay: tt.toolDisplay,
          provider: tt.role === "assistant" ? Z.provider : void 0,
          model: tt.role === "assistant" ? Z.model : void 0,
          finishReason: tt.role === "assistant" && !Vt ? Z.finishReason : void 0,
          error: tt.role === "assistant" && !Vt ? Z.error : !1
        });
        ut.push(gl);
      }
      return ut;
    }
    async function kS(_, k, D = {}) {
      const Z = String(k || "").trim();
      if (!_ || !Z) return;
      const ue = Number(Xe.value.turn || 0), pe = new AbortController();
      Qa.value = pe, et.value = !0, xe.value = !1, Ut.value = "运行中", ol.value = !0, Tv();
      const De = wj();
      try {
        const ut = await MZ({
          sessionId: _,
          agentConfig: r.value,
          assistantPreset: cl.value,
          question: Z,
          history: D.historyBeforeTurn,
          signal: pe.signal,
          onCompactionStart: (Bt) => {
            Yf({
              id: `manager-compaction-${Date.now()}`,
              active: !0,
              resolved: !1,
              currentTokens: Bt.currentTokens,
              yieldTokens: Bt.yieldTokens || 0,
              triggerTokens: Bt.triggerTokens,
              status: Bt.status
            });
          },
          onCompactionProgress: (Bt) => {
            Yf({
              currentTokens: Bt.currentTokens,
              yieldTokens: Bt.yieldTokens || Bt.currentTokens,
              triggerTokens: Bt.triggerTokens,
              status: Bt.status
            });
          },
          onCompactionComplete: (Bt) => {
            Yf({
              resolved: !0,
              currentTokens: Bt.currentTokens,
              yieldTokens: Bt.yieldTokens || Bt.currentTokens,
              triggerTokens: Bt.triggerTokens,
              status: Bt.status
            }), AS(), $r(_);
          },
          onCompactionUnable: (Bt) => {
            Yf({
              resolved: !0,
              currentTokens: Bt.currentTokens,
              yieldTokens: Bt.yieldTokens || Bt.currentTokens,
              triggerTokens: Bt.triggerTokens,
              status: Bt.status
            }), AS();
          }
        }), Qe = Array.isArray(D.historyBeforeTurn) ? ut.history : await fo(_), tt = await fh(_, {
          role: "user",
          content: Z
        });
        W.value === _ && (yt.value = [...yt.value, tt].sort((Bt, li) => Bt.order - li.order)), Xf(_);
        const Vt = [...new Set((D.replaceOrdersAfterAppend || []).map((Bt) => Number(Bt)).filter((Bt) => Number.isInteger(Bt) && Bt >= 0))];
        Vt.length && (await O_(_, Vt), W.value === _ && (yt.value = await fo(_)));
        const So = await PZ({
          sessionId: _,
          agentConfig: r.value,
          assistantPreset: cl.value,
          question: Z,
          history: Qe,
          turn: ue,
          signal: pe.signal,
          onProtocolEvent: (Bt) => {
            De.reset(), K8(_, Bt);
          },
          onStreamProgress: (Bt) => {
            const li = De.update(Bt);
            W8(_, {
              content: li.content,
              thoughts: Array.isArray(Bt.thoughts) ? si(Bt.thoughts) : void 0,
              toolCalls: li.toolCalls
            });
          }
        }), gl = String(So.text || "").trim() || "没有返回内容。";
        await J8(_, So.protocolMessages, gl, {
          provider: So.provider,
          model: So.model,
          finishReason: So.ok ? "stop" : "error",
          error: !So.ok
        }), CS(_), W.value === _ && (yt.value = await fo(_)), await $r(_), Ut.value = "";
      } catch (ut) {
        CS(_), pe.signal.aborted ? (await fh(_, {
          role: "assistant",
          content: "已停止。",
          finishReason: "aborted"
        }), W.value === _ && (yt.value = await fo(_)), Ut.value = "") : (await fh(_, {
          role: "assistant",
          content: ut instanceof Error ? ut.message : String(ut || "assistant_failed"),
          error: !0,
          finishReason: "error"
        }), W.value === _ && (yt.value = await fo(_)), Ut.value = "失败");
      } finally {
        De.reset(), Qa.value === pe && (Qa.value = null), xe.value = !1, et.value = !1;
      }
    }
    async function IS() {
      if (et.value) {
        xe.value || (xe.value = !0, Ut.value = "正在停止..."), Qa.value?.abort();
        return;
      }
      const _ = Mt.value.trim();
      if (!_ || !W.value) return;
      const k = W.value;
      if (Mt.value = "", Qt(() => ml(Cf.value)), et.value = !0, xe.value = !1, Ut.value = "准备中", xe.value) {
        et.value = !1, xe.value = !1, Mt.value = _, Ut.value = "";
        return;
      }
      et.value = !1, await kS(k, _);
    }
    function Y8() {
      !de.value || !zo.value || (fe.value || (fe.value = !0, N.value = N.value || "正在停止..."), zo.value.abort());
    }
    function X8() {
      Qf();
    }
    async function Qf(_ = {}) {
      if (de.value) {
        Y8();
        return;
      }
      fv();
      let k = String(_.messageText ?? x.value ?? "").trim();
      if (!k) {
        z.value = "先写一句话。", Zu("先写一句话。");
        return;
      }
      try {
        k = await U8(k, _);
      } catch (Z) {
        const ue = oi(Z);
        z.value = ue, Zu(ue);
        return;
      }
      if (!k) return;
      const D = new AbortController();
      zo.value = D, de.value = !0, fe.value = !1, z.value = "", fv(), N.value = "", q.value = [], zt.value = [], B.value = !1, X.value = "", G.value = "", oe.value = "", rl.value = !0, Gf(), Number.isFinite(Number(_.reuseUserMessageOrder)) || (X.value = k, x.value = "", Qt(() => ml(Pr.value)), tr(!0));
      try {
        if (D.signal.aborted) {
          const pe = X.value;
          rc(), pe && !x.value.trim() && (x.value = pe, Qt(() => ml(Pr.value)));
          return;
        }
        W.value || await cS();
        const Z = await zf(W.value);
        if (D.signal.aborted) {
          rc();
          return;
        }
        const ue = await dj({
          sessionId: W.value,
          agentConfig: r.value,
          contextSnapshot: Z,
          chatPreset: Sr.value,
          assistantPreset: cl.value,
          currentUserMessage: k,
          runtimeState: sr(ne.value?.state || {}),
          diagnostics: n.value,
          historyMode: M.value,
          signal: D.signal,
          reuseUserMessageOrder: _.reuseUserMessageOrder,
          rerollRuntimeEvents: _.rerollRuntimeEvents,
          runManager: !0,
          applyRegex: ju,
          applySubstituteParams: qf,
          getNativeWorldInfoRuntime: _v,
          buildNativeChatPrompt: tS,
          onStreamProgress: (pe) => {
            typeof pe.text == "string" && (N.value = pe.text), Array.isArray(pe.thoughts) && (q.value = si(pe.thoughts)), Array.isArray(pe.liveActionCheckEvents) && (zt.value = pe.liveActionCheckEvents.map((De) => ({ ...De })));
          },
          onUserMessageSaved: async (pe, De) => {
            W.value = pe;
            const ut = se.value.findIndex((Qe) => Qe.sessionId === De.sessionId && Qe.order === De.order);
            se.value = ut >= 0 ? se.value.map((Qe, tt) => tt === ut ? De : Qe) : [...se.value, De].sort((Qe, tt) => Qe.order - tt.order), B.value = !0, X.value = "", x.value = "", Qt(() => ml(Pr.value)), tr(!0), await Sl(pe), await Es(), tr(!0);
          },
          onAssistantMessageSaved: async (pe, De) => {
            W.value = pe;
            const ut = se.value.findIndex((Qe) => Qe.sessionId === De.sessionId && Qe.order === De.order);
            se.value = ut >= 0 ? se.value.map((Qe, tt) => tt === ut ? De : Qe) : [...se.value, De].sort((Qe, tt) => Qe.order - tt.order), rc(), await Es(), tr();
          },
          onManagerRunSaved: async (pe) => {
            await $r(pe);
          }
        });
        W.value = ue.sessionId, rc(), z.value = ue.error || "", G.value = ue.provider || "", oe.value = ue.model || "", await Es(), await $r(ue.sessionId), tr();
      } catch (Z) {
        console.error("[小白酒馆] turn failed", Z);
        const ue = X.value;
        rc(), ue && !x.value.trim() && (x.value = ue, Qt(() => ml(Pr.value))), z.value = Z instanceof Error ? Z.message : String(Z || "run_failed");
      } finally {
        zo.value === D && (zo.value = null), fe.value = !1, de.value = !1, tr();
      }
    }
    gt([
      () => sv.value.length,
      () => Rw.value.startIndex,
      () => Z5.value,
      () => N.value,
      () => X.value,
      () => e6.value,
      () => t6.value,
      () => s.value,
      () => Ve.value,
      () => _t.value
    ], () => {
      Ve.value === "chat" && _t.value === "chat" && (tr(), Qt(() => {
        Za(), Nv();
      }));
    }), gt([
      () => r6.value.length,
      () => cv.value.startIndex,
      () => o6.value,
      () => s6.value,
      () => et.value,
      () => Ve.value,
      () => _t.value
    ], () => {
      Ve.value === "chat" && _t.value === "manager" && ($v(), Qt(() => {
        xf(), TS();
      }));
    }), gt([() => Ve.value, () => _t.value], () => {
      Ve.value === "chat" && _t.value === "chat" && tr(!0);
    }), gt(() => al.value, () => {
      W.value && pl();
    }), gt(() => W.value, () => {
      Gf(), Tv(), rl.value = !0, ol.value = !0, Pn.resetPositionState(), Rn.resetPositionState(), er.value = "", wt.value = {}, nn.value = KC, Qt(() => {
        tr(!0), $v(!0);
      });
    }), gt([() => Bf.value.length, () => av.value], ([_, k]) => {
      so.value === "sessions" && _ && !k && (so.value = "memory");
    }), gt(z, (_) => {
      _ && Zu(_);
    }), gt(E5, (_) => {
      if (!_) return;
      const k = _.split(":").slice(2).join(":").trim();
      k && Zu(k);
    }), gt(Xu, async (_) => {
      const k = String(_?.path || "");
      if (!k) {
        if (Qu.value) {
          Se.value = "当前档案已变化，草稿仍保留";
          return;
        }
        Av(), hS(null);
        return;
      }
      if (Q.value === k && Qu.value) {
        Oe.value?.sessionId !== W.value && (Oe.value = null), Se.value = "档案已刷新，当前草稿仍保留";
        return;
      }
      const D = await e8(k);
      String(Ne.value || "") === k && hS(D);
    }, { immediate: !0 }), gt(Re, (_) => {
      try {
        globalThis.localStorage?.setItem(QC, _ ? "dark" : "light");
      } catch {
      }
    }), Mk(r5, {
      shell: {
        activeView: Ve,
        chatFocus: _t,
        homeThemeDark: Re,
        openPromptInspector: J6,
        postToHost: Ko,
        rememberBrokenAvatar: Of,
        shortText: Ev
      },
      chat: {
        actionFeedback: u8,
        cancelEditMessage: Jo,
        canDrawMessage: Iv,
        canEditMessage: Cv,
        canRerunMessage: pS,
        canSendMessage: a6,
        createNewChatSession: H6,
        currentAuthorNote: it,
        CHAT_SIDEBAR_BATCH_SIZE: che,
        chatAutoScroll: rl,
        chatFocus: _t,
        chatLayout: wn,
        chatMessages: b5,
        chatMessageWindow: Rw,
        chatComposeTextareaRef: Pr,
        chatScrollControlsActive: sl,
        chatScrollRef: ys,
        chatSidebarSessionLimit: nn,
        chatSidebarSessions: Ff,
        chatSidePanel: so,
        chatSubtitle: zw,
        copyMessage: h8,
        currentUserMessage: x,
        deleteMessageTurn: T8,
        displayMessageContent: jw,
        displayMessageRenderProjection: v6,
        displayMessageThoughtBlocks: y6,
        displayRuntimeContent: _6,
        displayRuntimeRenderProjection: eS,
        displayRuntimeThoughtBlocks: b6,
        displayCharacterName: y5,
        drawMessage: m8,
        drawMessageStatusClass: d8,
        drawMessageStatusText: _S,
        drawMessageTitle: f8,
        drawProgressText: me,
        filteredChatSidebarSessionCount: av,
        formatMessageTime: l8,
        handleChatScroll: I8,
        handleChatSubmit: X8,
        handleChatTouchMove: $8,
        handleChatTouchStart: R8,
        handleChatWheel: M8,
        handleComposeInput: b8,
        handleComposeKeydown: L8,
        hiddenChatSidebarSessionCount: z5,
        isDrawingMessage: Jf,
        isEditingMessage: gS,
        isCancellingRun: fe,
        isRunning: de,
        latestErrorMessage: T5,
        markdownSignature: Vn,
        htmlRenderEnabled: s,
        messageKey: cr,
        normalizeTavernSessionState: sr,
        removeSession: V6,
        renderChatMarkdown: Mf,
        rerunFromMessage: Pv,
        revealOlderChatMessages: F6,
        roleLabel: Kf,
        runtimeText: N,
        runtimeThoughts: q,
        runtimeActionCheckEvents: zt,
        runtimeUserMessageVisible: B,
        runtimePendingUserMessage: X,
        saveEditMessage: w8,
        scrollChatToBottom: tr,
        scrollChatToTop: C8,
        selectedSessionId: W,
        selectSession: fS,
        saveCurrentAuthorNote: E6,
        sessionDisplayTitle: nv,
        sessionFloorLabel: iv,
        sessions: H,
        showChatScrollBottom: kf,
        showChatScrollTop: Ug,
        startEditMessage: g8,
        thoughtBlocks: si,
        thoughtSummaryLabel: i8,
        updateChatScrollButtons: Nv,
        visibleCharacterAvatar: Zg,
        visibleChatMessages: sv,
        visibleUserAvatar: ev
      },
      manager: {
        activeMemoryFiles: Bf,
        archivedManagerRuns: A5,
        canEditManagerMessage: kv,
        canRerunManagerMessage: mS,
        canSendManagerMessage: i6,
        copyManagerMessage: p8,
        currentManagerWorkRun: C5,
        deleteManagerMessageTurn: A8,
        editingMessageDraft: Ya,
        formatRunActivityLine: k5,
        formatRunIssueLine: I5,
        formatRunInputLine: x5,
        formatRunMapLine: M5,
        formatRunMemoryLine: P5,
        formatRunModelLine: R5,
        formatRunTaskLine: N5,
        handleEditInput: _8,
        handleManagerComposeKeydown: O8,
        handleManagerEditKeydown: y8,
        handleManagerScroll: x8,
        handleManagerSubmit: IS,
        handleManagerTouchMove: D8,
        handleManagerTouchStart: N8,
        handleManagerWheel: P8,
        hiddenManagerRunCount: $5,
        isEditingManagerMessage: vS,
        isEditingManagerMessageDirty: yS,
        isManagerRunRetrying: a8,
        isManagerAssistantCancelling: xe,
        isManagerAssistantRunning: et,
        liveManagerChatDisplayItems: Hw,
        managerActionFeedback: c8,
        managerAutoScroll: ol,
        managerBusy: Nw,
        managerCompactionOverlay: jt,
        managerComposeTextareaRef: Cf,
        managerInputDraft: Mt,
        managerInputStatus: Ut,
        managerMessageWindow: cv,
        managerRuns: he,
        managerRunTone: L5,
        managerScrollControlsActive: Rr,
        managerScrollRef: ra,
        managerStatusLabel: F5,
        managerToolStatusLabel: B5,
        managerToolTone: U5,
        managerToolTraceItems: q5,
        managerToolTurnPreview: bj,
        managerToolTurnSummary: _j,
        memoryFileDisplayName: lv,
        memoryFiles: Te,
        memoryIndexStatusLine: Q5,
        retryManagerRun: s8,
        revealOlderManagerMessages: B6,
        rerunFromManagerMessage: Rv,
        saveEditManagerMessage: SS,
        scrollManagerToBottom: $v,
        scrollManagerToTop: k8,
        selectedMemoryFile: Ow,
        showManagerScrollBottom: _s,
        showManagerScrollTop: Ja,
        startEditManagerMessage: v8,
        toolTraceSummary: H5,
        updateManagerScrollButtons: TS,
        visibleManagerChatItems: dv
      },
      memory: {
        activeMemoryFiles: Bf,
        discardMemoryDraft: Q6,
        enterMemoryEditMode: Z6,
        expandMemoryFileGroup: J5,
        formatMemoryFileMeta: j6,
        markdownSignature: Vn,
        MEMORY_FILE_BATCH_SIZE: Xy,
        MEMORY_TURN_BATCH_SIZE: JC,
        memoryDirectoryGroups: Y5,
        memoryEditorDirty: Qu,
        memoryEditorDocumentAvailable: qw,
        memoryEditorDraft: P,
        memoryEditorLoadedPath: Q,
        memoryEditorMode: ae,
        memoryEditorReadOnly: X5,
        memoryEditorStatus: Se,
        memoryFileDisplayName: lv,
        memoryFileKindLabel: Fw,
        memoryFiles: Te,
        memoryFileSearchText: er,
        memoryFileStatusLabel: t8,
        previewMemoryDraft: n8,
        renderChatMarkdown: Mf,
        saveSelectedMemoryFile: r8,
        selectedMemoryFileEntry: Xu,
        selectedMemoryFile: Ow,
        selectedMemoryFilePath: Ne,
        selectMemoryFile: o8,
        stateMemoryFile: L
      },
      workspace: {
        activeMemoryFiles: Bf,
        activeMapDocId: qe,
        atlasActiveLocationKey: It,
        atlasStateDocument: Ye,
        atlasStatePatches: je,
        chatWorkspacePanel: Me,
        currentAssistantFloor: S5,
        mapStateDocuments: Ke,
        mapStateDocument: Be,
        mapStatePatches: Ee,
        saveSessionContract: L6,
        selectedSessionId: W,
        sessionContract: Lt,
        stateMemoryFile: L,
        tavernTasks: we
      },
      settings: Jg
    });
    async function Q8() {
      const _ = (await Promise.allSettled([Kg(), Es()])).find((k) => k.status === "rejected");
      _?.status === "rejected" && (E.value = oi(_.reason)), Vu(), Ve.value === "settings" && He.value === "api" && await Qt(Gu), Ve.value === "settings" && He.value === "chatPreset" && Yg(), Ve.value === "settings" && He.value === "worldbooks" && Xg(), Ve.value === "settings" && He.value === "regex" && Lf(), Ve.value === "settings" && He.value === "base" && Gg(), W.value && Hf({
        sessionId: W.value,
        force: !0
      }), Ve.value === "chat" && _t.value === "chat" && tr(!0);
    }
    return Lk(async () => {
      window.addEventListener("message", oS), ti = window.setInterval(() => {
        O6();
      }, 2e3), Ve.value === "settings" && He.value === "api" && Qt(Gu), Vu(), await Qt(), Ko("xb-tavern:frame-ready");
    }), T0(() => {
      window.removeEventListener("message", oS), Hg(), hl(), E2(null), ye.forEach((_) => {
        _.abort && _.signal && _.signal.removeEventListener("abort", _.abort), _.reject(/* @__PURE__ */ new Error("tavern_unmounted"));
      }), ye.clear(), zo.value?.abort(), Qa.value?.abort(), aa.value?.abort(), Pn.cleanup(), Rn.cleanup(), Nr && (window.clearTimeout(Nr), Nr = null), ti && (window.clearInterval(ti), ti = null), Kw(), ES(), Vf();
    }), (_, k) => (O(), F("main", {
      class: ze(["xb-tavern xb-os-shell", {
        "is-home-view": Ve.value === "home" || Ve.value === "about",
        "theme-dark": Re.value,
        "theme-light": !Re.value
      }]),
      "data-chat-font-size": o.value.chatFontSize,
      style: Jr(Hu.value)
    }, [
      g("section", rhe, [
        Ve.value === "home" ? (O(), gn(Yee, {
          key: 0,
          dark: Re.value,
          "has-session": qt.value,
          subtitle: zw.value,
          "character-count": U.value.length,
          onToggleTheme: k[0] || (k[0] = (D) => Re.value = !Re.value),
          onExit: k[1] || (k[1] = (D) => Ko("xb-tavern:close")),
          onEnter: z6,
          onOpenCharacters: sS,
          onOpenSettings: S(Vg),
          onOpenAbout: k[2] || (k[2] = (D) => Ve.value = "about")
        }, null, 8, [
          "dark",
          "has-session",
          "subtitle",
          "character-count",
          "onOpenSettings"
        ])) : ge("", !0),
        Ve.value === "about" ? (O(), gn(Ij, {
          key: 1,
          dark: Re.value,
          onToggleTheme: k[3] || (k[3] = (D) => Re.value = !Re.value),
          onBack: k[4] || (k[4] = (D) => Ve.value = "home")
        }, null, 8, ["dark"])) : ge("", !0),
        Ve.value === "characters" ? (O(), gn(Fee, {
          key: 2,
          ref_key: "characterArchivePageRef",
          ref: Ka,
          "search-text": Cn.value,
          "onUpdate:searchText": k[5] || (k[5] = (D) => Cn.value = D),
          dark: Re.value,
          "pending-error": w.value,
          characters: U.value,
          "visible-characters": _e.value,
          "filtered-count": ee.value.length,
          "live-character-id": fl.value,
          "selected-character": $e.value,
          "selected-character-sessions": ht.value,
          "selected-greeting-index": y.value,
          "pending-preview-character-id": T.value,
          "pending-character-session-id": C.value,
          "character-worldbook-state": A.value,
          "character-worldbook-busy": I.value,
          "hidden-count": Kt.value,
          "batch-size": Jh,
          "avatar-available": tv,
          "session-floor-label": iv,
          "short-text": Ev,
          onToggleTheme: k[6] || (k[6] = (D) => Re.value = !Re.value),
          onBack: k[7] || (k[7] = (D) => Ve.value = "home"),
          onRefresh: aS,
          onSelect: M6,
          onSelectGreeting: N6,
          onEnterSelected: D6,
          onOpenCharacterWorldbook: P6,
          onOpenSession: fS,
          onLoadMore: k[8] || (k[8] = (D) => Gn.value += Jh),
          onKeydown: $6,
          onAvatarError: Of
        }, null, 8, [
          "search-text",
          "dark",
          "pending-error",
          "characters",
          "visible-characters",
          "filtered-count",
          "live-character-id",
          "selected-character",
          "selected-character-sessions",
          "selected-greeting-index",
          "pending-preview-character-id",
          "pending-character-session-id",
          "character-worldbook-state",
          "character-worldbook-busy",
          "hidden-count"
        ])) : ge("", !0),
        Ve.value === "chat" ? (O(), gn(Que, { key: 3 })) : ge("", !0),
        Ve.value === "settings" ? (O(), gn(xfe, { key: 4 })) : ge("", !0)
      ]),
      il.value ? (O(), gn(gce, {
        key: 0,
        tab: ao.value,
        "onUpdate:tab": k[9] || (k[9] = (D) => ao.value = D),
        "simulate-input": oa.value,
        "onUpdate:simulateInput": k[10] || (k[10] = (D) => oa.value = D),
        "last-request-raw-json": _5.value,
        "last-request-snapshot": rv.value,
        "simulate-status": sa.value,
        "simulate-error": qo.value,
        "simulate-json": Xa.value,
        onClose: Y6,
        onSimulate: X6
      }, null, 8, [
        "tab",
        "simulate-input",
        "last-request-raw-json",
        "last-request-snapshot",
        "simulate-status",
        "simulate-error",
        "simulate-json"
      ])) : ge("", !0),
      $.value ? (O(), F("div", {
        key: 1,
        class: "character-worldbook-picker-overlay",
        onClick: sn(iS, ["self"])
      }, [g("section", ohe, [
        g("header", null, [k[11] || (k[11] = g("strong", null, "选择角色世界书", -1)), g("button", {
          type: "button",
          class: "worldbook-picker-close",
          "aria-label": "关闭",
          onClick: iS
        })]),
        R.value ? (O(), F("div", she, K(R.value), 1)) : ge("", !0),
        g("div", ahe, [(O(!0), F(Je, null, lt(p.value, (D) => (O(), F("button", {
          key: D,
          type: "button",
          disabled: I.value,
          onClick: (Z) => R6(D)
        }, K(D), 9, ihe))), 128))])
      ])])) : ge("", !0)
    ], 14, nhe));
  }
}), vhe = ghe;
_$(vhe).mount("#app");
