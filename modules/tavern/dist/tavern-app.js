/* eslint-disable */
var i7 = Object.create, Mx = Object.defineProperty, l7 = Object.getOwnPropertyDescriptor, u7 = Object.getOwnPropertyNames, c7 = Object.getPrototypeOf, d7 = Object.prototype.hasOwnProperty, Pf = (e, t) => () => (t || (e((t = { exports: {} }).exports, t), e = null), t.exports), f7 = (e, t, n, r) => {
  if (t && typeof t == "object" || typeof t == "function")
    for (var s = u7(t), a = 0, o = s.length, l; a < o; a++)
      l = s[a], !d7.call(e, l) && l !== n && Mx(e, l, {
        get: ((i) => t[i]).bind(null, l),
        enumerable: !(r = l7(t, l)) || r.enumerable
      });
  return e;
}, Px = (e, t, n) => (n = e != null ? i7(c7(e)) : {}, f7(t || !e || !e.__esModule ? Mx(n, "default", {
  value: e,
  enumerable: !0
}) : n, e));
// @__NO_SIDE_EFFECTS__
function kg(e) {
  const t = /* @__PURE__ */ Object.create(null);
  for (const n of e.split(",")) t[n] = 1;
  return (n) => n in t;
}
var gn = {}, Eu = [], La = () => {
}, Rx = () => !1, Mg = (e) => e.charCodeAt(0) === 111 && e.charCodeAt(1) === 110 && (e.charCodeAt(2) > 122 || e.charCodeAt(2) < 97), Pg = (e) => e.startsWith("onUpdate:"), tr = Object.assign, l1 = (e, t) => {
  const n = e.indexOf(t);
  n > -1 && e.splice(n, 1);
}, h7 = Object.prototype.hasOwnProperty, ln = (e, t) => h7.call(e, t), Et = Array.isArray, Cu = (e) => Rf(e) === "[object Map]", tc = (e) => Rf(e) === "[object Set]", YA = (e) => Rf(e) === "[object Date]", $t = (e) => typeof e == "function", xn = (e) => typeof e == "string", Ba = (e) => typeof e == "symbol", pn = (e) => e !== null && typeof e == "object", Nx = (e) => (pn(e) || $t(e)) && $t(e.then) && $t(e.catch), $x = Object.prototype.toString, Rf = (e) => $x.call(e), p7 = (e) => Rf(e).slice(8, -1), Lx = (e) => Rf(e) === "[object Object]", u1 = (e) => xn(e) && e !== "NaN" && e[0] !== "-" && "" + parseInt(e, 10) === e, Rd = /* @__PURE__ */ kg(",key,ref,ref_for,ref_key,onVnodeBeforeMount,onVnodeMounted,onVnodeBeforeUpdate,onVnodeUpdated,onVnodeBeforeUnmount,onVnodeUnmounted"), Rg = (e) => {
  const t = /* @__PURE__ */ Object.create(null);
  return ((n) => t[n] || (t[n] = e(n)));
}, m7 = /-\w/g, na = Rg((e) => e.replace(m7, (t) => t.slice(1).toUpperCase())), g7 = /\B([A-Z])/g, Ai = Rg((e) => e.replace(g7, "-$1").toLowerCase()), Dx = Rg((e) => e.charAt(0).toUpperCase() + e.slice(1)), Ty = Rg((e) => e ? `on${Dx(e)}` : ""), Ra = (e, t) => !Object.is(e, t), Rp = (e, ...t) => {
  for (let n = 0; n < e.length; n++) e[n](...t);
}, Ox = (e, t, n, r = !1) => {
  Object.defineProperty(e, t, {
    configurable: !0,
    enumerable: !1,
    writable: r,
    value: n
  });
}, Ng = (e) => {
  const t = parseFloat(e);
  return isNaN(t) ? e : t;
}, XA, $g = () => XA || (XA = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : typeof globalThis < "u" ? globalThis : {});
function Nn(e) {
  if (Et(e)) {
    const t = {};
    for (let n = 0; n < e.length; n++) {
      const r = e[n], s = xn(r) ? b7(r) : Nn(r);
      if (s) for (const a in s) t[a] = s[a];
    }
    return t;
  } else if (xn(e) || pn(e)) return e;
}
var v7 = /;(?![^(]*\))/g, y7 = /:([^]+)/, _7 = /\/\*[^]*?\*\//g;
function b7(e) {
  const t = {};
  return e.replace(_7, "").split(v7).forEach((n) => {
    if (n) {
      const r = n.split(y7);
      r.length > 1 && (t[r[0].trim()] = r[1].trim());
    }
  }), t;
}
function Fe(e) {
  let t = "";
  if (xn(e)) t = e;
  else if (Et(e)) for (let n = 0; n < e.length; n++) {
    const r = Fe(e[n]);
    r && (t += r + " ");
  }
  else if (pn(e))
    for (const n in e) e[n] && (t += n + " ");
  return t.trim();
}
var Fx = "itemscope,allowfullscreen,formnovalidate,ismap,nomodule,novalidate,readonly", w7 = /* @__PURE__ */ kg(Fx), Ave = /* @__PURE__ */ kg(Fx + ",async,autofocus,autoplay,controls,default,defer,disabled,hidden,inert,loop,open,required,reversed,scoped,seamless,checked,muted,multiple,selected");
function Bx(e) {
  return !!e || e === "";
}
function S7(e, t) {
  if (e.length !== t.length) return !1;
  let n = !0;
  for (let r = 0; n && r < e.length; r++) n = nc(e[r], t[r]);
  return n;
}
function nc(e, t) {
  if (e === t) return !0;
  let n = YA(e), r = YA(t);
  if (n || r) return n && r ? e.getTime() === t.getTime() : !1;
  if (n = Ba(e), r = Ba(t), n || r) return e === t;
  if (n = Et(e), r = Et(t), n || r) return n && r ? S7(e, t) : !1;
  if (n = pn(e), r = pn(t), n || r) {
    if (!n || !r || Object.keys(e).length !== Object.keys(t).length) return !1;
    for (const s in e) {
      const a = e.hasOwnProperty(s), o = t.hasOwnProperty(s);
      if (a && !o || !a && o || !nc(e[s], t[s])) return !1;
    }
  }
  return String(e) === String(t);
}
function c1(e, t) {
  return e.findIndex((n) => nc(n, t));
}
var Ux = (e) => !!(e && e.__v_isRef === !0), te = (e) => xn(e) ? e : e == null ? "" : Et(e) || pn(e) && (e.toString === $x || !$t(e.toString)) ? Ux(e) ? te(e.value) : JSON.stringify(e, qx, 2) : String(e), qx = (e, t) => Ux(t) ? qx(e, t.value) : Cu(t) ? { [`Map(${t.size})`]: [...t.entries()].reduce((n, [r, s], a) => (n[Ey(r, a) + " =>"] = s, n), {}) } : tc(t) ? { [`Set(${t.size})`]: [...t.values()].map((n) => Ey(n)) } : Ba(t) ? Ey(t) : pn(t) && !Et(t) && !Lx(t) ? String(t) : t, Ey = (e, t = "") => {
  var n;
  return Ba(e) ? `Symbol(${(n = e.description) != null ? n : t})` : e;
}, hr, A7 = class {
  constructor(e = !1) {
    this.detached = e, this._active = !0, this._on = 0, this.effects = [], this.cleanups = [], this._isPaused = !1, this._warnOnRun = !0, this.__v_skip = !0, !e && hr && (hr.active ? (this.parent = hr, this.index = (hr.scopes || (hr.scopes = [])).push(this) - 1) : (this._active = !1, this._warnOnRun = !1));
  }
  get active() {
    return this._active;
  }
  pause() {
    if (this._active) {
      this._isPaused = !0;
      let e, t;
      if (this.scopes) for (e = 0, t = this.scopes.length; e < t; e++) this.scopes[e].pause();
      for (e = 0, t = this.effects.length; e < t; e++) this.effects[e].pause();
    }
  }
  resume() {
    if (this._active && this._isPaused) {
      this._isPaused = !1;
      let e, t;
      if (this.scopes) for (e = 0, t = this.scopes.length; e < t; e++) this.scopes[e].resume();
      for (e = 0, t = this.effects.length; e < t; e++) this.effects[e].resume();
    }
  }
  run(e) {
    if (this._active) {
      const t = hr;
      try {
        return hr = this, e();
      } finally {
        hr = t;
      }
    }
  }
  on() {
    ++this._on === 1 && (this.prevScope = hr, hr = this);
  }
  off() {
    if (this._on > 0 && --this._on === 0) {
      if (hr === this) hr = this.prevScope;
      else {
        let e = hr;
        for (; e; ) {
          if (e.prevScope === this) {
            e.prevScope = this.prevScope;
            break;
          }
          e = e.prevScope;
        }
      }
      this.prevScope = void 0;
    }
  }
  stop(e) {
    if (this._active) {
      this._active = !1;
      let t, n;
      for (t = 0, n = this.effects.length; t < n; t++) this.effects[t].stop();
      for (this.effects.length = 0, t = 0, n = this.cleanups.length; t < n; t++) this.cleanups[t]();
      if (this.cleanups.length = 0, this.scopes) {
        for (t = 0, n = this.scopes.length; t < n; t++) this.scopes[t].stop(!0);
        this.scopes.length = 0;
      }
      if (!this.detached && this.parent && !e) {
        const r = this.parent.scopes.pop();
        r && r !== this && (this.parent.scopes[this.index] = r, r.index = this.index);
      }
      this.parent = void 0;
    }
  }
};
function T7() {
  return hr;
}
var _n, Cy = /* @__PURE__ */ new WeakSet(), Hx = class {
  constructor(e) {
    this.fn = e, this.deps = void 0, this.depsTail = void 0, this.flags = 5, this.next = void 0, this.cleanup = void 0, this.scheduler = void 0, hr && (hr.active ? hr.effects.push(this) : this.flags &= -2);
  }
  pause() {
    this.flags |= 64;
  }
  resume() {
    this.flags & 64 && (this.flags &= -65, Cy.has(this) && (Cy.delete(this), this.trigger()));
  }
  notify() {
    this.flags & 2 && !(this.flags & 32) || this.flags & 8 || Kx(this);
  }
  run() {
    if (!(this.flags & 1)) return this.fn();
    this.flags |= 2, jA(this), Wx(this);
    const e = _n, t = ra;
    _n = this, ra = !0;
    try {
      return this.fn();
    } finally {
      Gx(this), _n = e, ra = t, this.flags &= -3;
    }
  }
  stop() {
    if (this.flags & 1) {
      for (let e = this.deps; e; e = e.nextDep) h1(e);
      this.deps = this.depsTail = void 0, jA(this), this.onStop && this.onStop(), this.flags &= -2;
    }
  }
  trigger() {
    this.flags & 64 ? Cy.add(this) : this.scheduler ? this.scheduler() : this.runIfDirty();
  }
  runIfDirty() {
    z_(this) && this.run();
  }
  get dirty() {
    return z_(this);
  }
}, zx = 0, Nd, $d;
function Kx(e, t = !1) {
  if (e.flags |= 8, t) {
    e.next = $d, $d = e;
    return;
  }
  e.next = Nd, Nd = e;
}
function d1() {
  zx++;
}
function f1() {
  if (--zx > 0) return;
  if ($d) {
    let t = $d;
    for ($d = void 0; t; ) {
      const n = t.next;
      t.next = void 0, t.flags &= -9, t = n;
    }
  }
  let e;
  for (; Nd; ) {
    let t = Nd;
    for (Nd = void 0; t; ) {
      const n = t.next;
      if (t.next = void 0, t.flags &= -9, t.flags & 1) try {
        t.trigger();
      } catch (r) {
        e || (e = r);
      }
      t = n;
    }
  }
  if (e) throw e;
}
function Wx(e) {
  for (let t = e.deps; t; t = t.nextDep)
    t.version = -1, t.prevActiveLink = t.dep.activeLink, t.dep.activeLink = t;
}
function Gx(e) {
  let t, n = e.depsTail, r = n;
  for (; r; ) {
    const s = r.prevDep;
    r.version === -1 ? (r === n && (n = s), h1(r), E7(r)) : t = r, r.dep.activeLink = r.prevActiveLink, r.prevActiveLink = void 0, r = s;
  }
  e.deps = t, e.depsTail = n;
}
function z_(e) {
  for (let t = e.deps; t; t = t.nextDep) if (t.dep.version !== t.version || t.dep.computed && (Vx(t.dep.computed) || t.dep.version !== t.version)) return !0;
  return !!e._dirty;
}
function Vx(e) {
  if (e.flags & 4 && !(e.flags & 16) || (e.flags &= -17, e.globalVersion === rf) || (e.globalVersion = rf, !e.isSSR && e.flags & 128 && (!e.deps && !e._dirty || !z_(e)))) return;
  e.flags |= 2;
  const t = e.dep, n = _n, r = ra;
  _n = e, ra = !0;
  try {
    Wx(e);
    const s = e.fn(e._value);
    (t.version === 0 || Ra(s, e._value)) && (e.flags |= 128, e._value = s, t.version++);
  } catch (s) {
    throw t.version++, s;
  } finally {
    _n = n, ra = r, Gx(e), e.flags &= -3;
  }
}
function h1(e, t = !1) {
  const { dep: n, prevSub: r, nextSub: s } = e;
  if (r && (r.nextSub = s, e.prevSub = void 0), s && (s.prevSub = r, e.nextSub = void 0), n.subs === e && (n.subs = r, !r && n.computed)) {
    n.computed.flags &= -5;
    for (let a = n.computed.deps; a; a = a.nextDep) h1(a, !0);
  }
  !t && !--n.sc && n.map && n.map.delete(n.key);
}
function E7(e) {
  const { prevDep: t, nextDep: n } = e;
  t && (t.nextDep = n, e.prevDep = void 0), n && (n.prevDep = t, e.nextDep = void 0);
}
var ra = !0, Jx = [];
function Co() {
  Jx.push(ra), ra = !1;
}
function Io() {
  const e = Jx.pop();
  ra = e === void 0 ? !0 : e;
}
function jA(e) {
  const { cleanup: t } = e;
  if (e.cleanup = void 0, t) {
    const n = _n;
    _n = void 0;
    try {
      t();
    } finally {
      _n = n;
    }
  }
}
var rf = 0, C7 = class {
  constructor(e, t) {
    this.sub = e, this.dep = t, this.version = t.version, this.nextDep = this.prevDep = this.nextSub = this.prevSub = this.prevActiveLink = void 0;
  }
}, p1 = class {
  constructor(e) {
    this.computed = e, this.version = 0, this.activeLink = void 0, this.subs = void 0, this.map = void 0, this.key = void 0, this.sc = 0, this.__v_skip = !0;
  }
  track(e) {
    if (!_n || !ra || _n === this.computed) return;
    let t = this.activeLink;
    if (t === void 0 || t.sub !== _n)
      t = this.activeLink = new C7(_n, this), _n.deps ? (t.prevDep = _n.depsTail, _n.depsTail.nextDep = t, _n.depsTail = t) : _n.deps = _n.depsTail = t, Yx(t);
    else if (t.version === -1 && (t.version = this.version, t.nextDep)) {
      const n = t.nextDep;
      n.prevDep = t.prevDep, t.prevDep && (t.prevDep.nextDep = n), t.prevDep = _n.depsTail, t.nextDep = void 0, _n.depsTail.nextDep = t, _n.depsTail = t, _n.deps === t && (_n.deps = n);
    }
    return t;
  }
  trigger(e) {
    this.version++, rf++, this.notify(e);
  }
  notify(e) {
    d1();
    try {
      for (let t = this.subs; t; t = t.prevSub) t.sub.notify() && t.sub.dep.notify();
    } finally {
      f1();
    }
  }
};
function Yx(e) {
  if (e.dep.sc++, e.sub.flags & 4) {
    const t = e.dep.computed;
    if (t && !e.dep.subs) {
      t.flags |= 20;
      for (let r = t.deps; r; r = r.nextDep) Yx(r);
    }
    const n = e.dep.subs;
    n !== e && (e.prevSub = n, n && (n.nextSub = e)), e.dep.subs = e;
  }
}
var K_ = /* @__PURE__ */ new WeakMap(), hl = /* @__PURE__ */ Symbol(""), W_ = /* @__PURE__ */ Symbol(""), sf = /* @__PURE__ */ Symbol("");
function Er(e, t, n) {
  if (ra && _n) {
    let r = K_.get(e);
    r || K_.set(e, r = /* @__PURE__ */ new Map());
    let s = r.get(n);
    s || (r.set(n, s = new p1()), s.map = r, s.key = n), s.track();
  }
}
function ho(e, t, n, r, s, a) {
  const o = K_.get(e);
  if (!o) {
    rf++;
    return;
  }
  const l = (i) => {
    i && i.trigger();
  };
  if (d1(), t === "clear") o.forEach(l);
  else {
    const i = Et(e), u = i && u1(n);
    if (i && n === "length") {
      const d = Number(r);
      o.forEach((f, m) => {
        (m === "length" || m === sf || !Ba(m) && m >= d) && l(f);
      });
    } else
      switch ((n !== void 0 || o.has(void 0)) && l(o.get(n)), u && l(o.get(sf)), t) {
        case "add":
          i ? u && l(o.get("length")) : (l(o.get(hl)), Cu(e) && l(o.get(W_)));
          break;
        case "delete":
          i || (l(o.get(hl)), Cu(e) && l(o.get(W_)));
          break;
        case "set":
          Cu(e) && l(o.get(hl));
          break;
      }
  }
  f1();
}
function Vl(e) {
  const t = /* @__PURE__ */ on(e);
  return t === e ? t : (Er(t, "iterate", sf), /* @__PURE__ */ Us(e) ? t : t.map(ia));
}
function Lg(e) {
  return Er(e = /* @__PURE__ */ on(e), "iterate", sf), e;
}
function xa(e, t) {
  return /* @__PURE__ */ xo(e) ? Uu(/* @__PURE__ */ pl(e) ? ia(t) : t) : ia(t);
}
var I7 = {
  __proto__: null,
  [Symbol.iterator]() {
    return Iy(this, Symbol.iterator, (e) => xa(this, e));
  },
  concat(...e) {
    return Vl(this).concat(...e.map((t) => Et(t) ? Vl(t) : t));
  },
  entries() {
    return Iy(this, "entries", (e) => (e[1] = xa(this, e[1]), e));
  },
  every(e, t) {
    return Za(this, "every", e, t, void 0, arguments);
  },
  filter(e, t) {
    return Za(this, "filter", e, t, (n) => n.map((r) => xa(this, r)), arguments);
  },
  find(e, t) {
    return Za(this, "find", e, t, (n) => xa(this, n), arguments);
  },
  findIndex(e, t) {
    return Za(this, "findIndex", e, t, void 0, arguments);
  },
  findLast(e, t) {
    return Za(this, "findLast", e, t, (n) => xa(this, n), arguments);
  },
  findLastIndex(e, t) {
    return Za(this, "findLastIndex", e, t, void 0, arguments);
  },
  forEach(e, t) {
    return Za(this, "forEach", e, t, void 0, arguments);
  },
  includes(...e) {
    return xy(this, "includes", e);
  },
  indexOf(...e) {
    return xy(this, "indexOf", e);
  },
  join(e) {
    return Vl(this).join(e);
  },
  lastIndexOf(...e) {
    return xy(this, "lastIndexOf", e);
  },
  map(e, t) {
    return Za(this, "map", e, t, void 0, arguments);
  },
  pop() {
    return Rc(this, "pop");
  },
  push(...e) {
    return Rc(this, "push", e);
  },
  reduce(e, ...t) {
    return QA(this, "reduce", e, t);
  },
  reduceRight(e, ...t) {
    return QA(this, "reduceRight", e, t);
  },
  shift() {
    return Rc(this, "shift");
  },
  some(e, t) {
    return Za(this, "some", e, t, void 0, arguments);
  },
  splice(...e) {
    return Rc(this, "splice", e);
  },
  toReversed() {
    return Vl(this).toReversed();
  },
  toSorted(e) {
    return Vl(this).toSorted(e);
  },
  toSpliced(...e) {
    return Vl(this).toSpliced(...e);
  },
  unshift(...e) {
    return Rc(this, "unshift", e);
  },
  values() {
    return Iy(this, "values", (e) => xa(this, e));
  }
};
function Iy(e, t, n) {
  const r = Lg(e), s = r[t]();
  return r !== e && !/* @__PURE__ */ Us(e) && (s._next = s.next, s.next = () => {
    const a = s._next();
    return a.done || (a.value = n(a.value)), a;
  }), s;
}
var x7 = Array.prototype;
function Za(e, t, n, r, s, a) {
  const o = Lg(e), l = o !== e && !/* @__PURE__ */ Us(e), i = o[t];
  if (i !== x7[t]) {
    const f = i.apply(e, a);
    return l ? ia(f) : f;
  }
  let u = n;
  o !== e && (l ? u = function(f, m) {
    return n.call(this, xa(e, f), m, e);
  } : n.length > 2 && (u = function(f, m) {
    return n.call(this, f, m, e);
  }));
  const d = i.call(o, u, r);
  return l && s ? s(d) : d;
}
function QA(e, t, n, r) {
  const s = Lg(e), a = s !== e && !/* @__PURE__ */ Us(e);
  let o = n, l = !1;
  s !== e && (a ? (l = r.length === 0, o = function(u, d, f) {
    return l && (l = !1, u = xa(e, u)), n.call(this, u, xa(e, d), f, e);
  }) : n.length > 3 && (o = function(u, d, f) {
    return n.call(this, u, d, f, e);
  }));
  const i = s[t](o, ...r);
  return l ? xa(e, i) : i;
}
function xy(e, t, n) {
  const r = /* @__PURE__ */ on(e);
  Er(r, "iterate", sf);
  const s = r[t](...n);
  return (s === -1 || s === !1) && /* @__PURE__ */ y1(n[0]) ? (n[0] = /* @__PURE__ */ on(n[0]), r[t](...n)) : s;
}
function Rc(e, t, n = []) {
  Co(), d1();
  const r = (/* @__PURE__ */ on(e))[t].apply(e, n);
  return f1(), Io(), r;
}
var k7 = /* @__PURE__ */ kg("__proto__,__v_isRef,__isVue"), Xx = new Set(/* @__PURE__ */ Object.getOwnPropertyNames(Symbol).filter((e) => e !== "arguments" && e !== "caller").map((e) => Symbol[e]).filter(Ba));
function M7(e) {
  Ba(e) || (e = String(e));
  const t = /* @__PURE__ */ on(this);
  return Er(t, "has", e), t.hasOwnProperty(e);
}
var jx = class {
  constructor(e = !1, t = !1) {
    this._isReadonly = e, this._isShallow = t;
  }
  get(e, t, n) {
    if (t === "__v_skip") return e.__v_skip;
    const r = this._isReadonly, s = this._isShallow;
    if (t === "__v_isReactive") return !r;
    if (t === "__v_isReadonly") return r;
    if (t === "__v_isShallow") return s;
    if (t === "__v_raw")
      return n === (r ? s ? U7 : tk : s ? ek : Zx).get(e) || Object.getPrototypeOf(e) === Object.getPrototypeOf(n) ? e : void 0;
    const a = Et(e);
    if (!r) {
      let l;
      if (a && (l = I7[t])) return l;
      if (t === "hasOwnProperty") return M7;
    }
    const o = Reflect.get(e, t, /* @__PURE__ */ kn(e) ? e : n);
    if ((Ba(t) ? Xx.has(t) : k7(t)) || (r || Er(e, "get", t), s)) return o;
    if (/* @__PURE__ */ kn(o)) {
      const l = a && u1(t) ? o : o.value;
      return r && pn(l) ? /* @__PURE__ */ V_(l) : l;
    }
    return pn(o) ? r ? /* @__PURE__ */ V_(o) : /* @__PURE__ */ g1(o) : o;
  }
}, Qx = class extends jx {
  constructor(e = !1) {
    super(!1, e);
  }
  set(e, t, n, r) {
    let s = e[t];
    const a = Et(e) && u1(t);
    if (!this._isShallow) {
      const i = /* @__PURE__ */ xo(s);
      if (!/* @__PURE__ */ Us(n) && !/* @__PURE__ */ xo(n) && (s = /* @__PURE__ */ on(s), n = /* @__PURE__ */ on(n)), !a && /* @__PURE__ */ kn(s) && !/* @__PURE__ */ kn(n)) return i || (s.value = n), !0;
    }
    const o = a ? Number(t) < e.length : ln(e, t), l = Reflect.set(e, t, n, /* @__PURE__ */ kn(e) ? e : r);
    return e === /* @__PURE__ */ on(r) && (o ? Ra(n, s) && ho(e, "set", t, n, s) : ho(e, "add", t, n)), l;
  }
  deleteProperty(e, t) {
    const n = ln(e, t), r = e[t], s = Reflect.deleteProperty(e, t);
    return s && n && ho(e, "delete", t, void 0, r), s;
  }
  has(e, t) {
    const n = Reflect.has(e, t);
    return (!Ba(t) || !Xx.has(t)) && Er(e, "has", t), n;
  }
  ownKeys(e) {
    return Er(e, "iterate", Et(e) ? "length" : hl), Reflect.ownKeys(e);
  }
}, P7 = class extends jx {
  constructor(e = !1) {
    super(!0, e);
  }
  set(e, t) {
    return !0;
  }
  deleteProperty(e, t) {
    return !0;
  }
}, R7 = /* @__PURE__ */ new Qx(), N7 = /* @__PURE__ */ new P7(), $7 = /* @__PURE__ */ new Qx(!0), G_ = (e) => e, Rh = (e) => Reflect.getPrototypeOf(e);
function L7(e, t, n) {
  return function(...r) {
    const s = this.__v_raw, a = /* @__PURE__ */ on(s), o = Cu(a), l = e === "entries" || e === Symbol.iterator && o, i = e === "keys" && o, u = s[e](...r), d = n ? G_ : t ? Uu : ia;
    return !t && Er(a, "iterate", i ? W_ : hl), tr(Object.create(u), { next() {
      const { value: f, done: m } = u.next();
      return m ? {
        value: f,
        done: m
      } : {
        value: l ? [d(f[0]), d(f[1])] : d(f),
        done: m
      };
    } });
  };
}
function Nh(e) {
  return function(...t) {
    return e === "delete" ? !1 : e === "clear" ? void 0 : this;
  };
}
function D7(e, t) {
  const n = {
    get(r) {
      const s = this.__v_raw, a = /* @__PURE__ */ on(s), o = /* @__PURE__ */ on(r);
      e || (Ra(r, o) && Er(a, "get", r), Er(a, "get", o));
      const { has: l } = Rh(a), i = t ? G_ : e ? Uu : ia;
      if (l.call(a, r)) return i(s.get(r));
      if (l.call(a, o)) return i(s.get(o));
      s !== a && s.get(r);
    },
    get size() {
      const r = this.__v_raw;
      return !e && Er(/* @__PURE__ */ on(r), "iterate", hl), r.size;
    },
    has(r) {
      const s = this.__v_raw, a = /* @__PURE__ */ on(s), o = /* @__PURE__ */ on(r);
      return e || (Ra(r, o) && Er(a, "has", r), Er(a, "has", o)), r === o ? s.has(r) : s.has(r) || s.has(o);
    },
    forEach(r, s) {
      const a = this, o = a.__v_raw, l = /* @__PURE__ */ on(o), i = t ? G_ : e ? Uu : ia;
      return !e && Er(l, "iterate", hl), o.forEach((u, d) => r.call(s, i(u), i(d), a));
    }
  };
  return tr(n, e ? {
    add: Nh("add"),
    set: Nh("set"),
    delete: Nh("delete"),
    clear: Nh("clear")
  } : {
    add(r) {
      const s = /* @__PURE__ */ on(this), a = Rh(s), o = /* @__PURE__ */ on(r), l = !t && !/* @__PURE__ */ Us(r) && !/* @__PURE__ */ xo(r) ? o : r;
      return a.has.call(s, l) || Ra(r, l) && a.has.call(s, r) || Ra(o, l) && a.has.call(s, o) || (s.add(l), ho(s, "add", l, l)), this;
    },
    set(r, s) {
      !t && !/* @__PURE__ */ Us(s) && !/* @__PURE__ */ xo(s) && (s = /* @__PURE__ */ on(s));
      const a = /* @__PURE__ */ on(this), { has: o, get: l } = Rh(a);
      let i = o.call(a, r);
      i || (r = /* @__PURE__ */ on(r), i = o.call(a, r));
      const u = l.call(a, r);
      return a.set(r, s), i ? Ra(s, u) && ho(a, "set", r, s, u) : ho(a, "add", r, s), this;
    },
    delete(r) {
      const s = /* @__PURE__ */ on(this), { has: a, get: o } = Rh(s);
      let l = a.call(s, r);
      l || (r = /* @__PURE__ */ on(r), l = a.call(s, r));
      const i = o ? o.call(s, r) : void 0, u = s.delete(r);
      return l && ho(s, "delete", r, void 0, i), u;
    },
    clear() {
      const r = /* @__PURE__ */ on(this), s = r.size !== 0, a = void 0, o = r.clear();
      return s && ho(r, "clear", void 0, void 0, a), o;
    }
  }), [
    "keys",
    "values",
    "entries",
    Symbol.iterator
  ].forEach((r) => {
    n[r] = L7(r, e, t);
  }), n;
}
function m1(e, t) {
  const n = D7(e, t);
  return (r, s, a) => s === "__v_isReactive" ? !e : s === "__v_isReadonly" ? e : s === "__v_raw" ? r : Reflect.get(ln(n, s) && s in r ? n : r, s, a);
}
var O7 = { get: /* @__PURE__ */ m1(!1, !1) }, F7 = { get: /* @__PURE__ */ m1(!1, !0) }, B7 = { get: /* @__PURE__ */ m1(!0, !1) }, Zx = /* @__PURE__ */ new WeakMap(), ek = /* @__PURE__ */ new WeakMap(), tk = /* @__PURE__ */ new WeakMap(), U7 = /* @__PURE__ */ new WeakMap();
function q7(e) {
  switch (e) {
    case "Object":
    case "Array":
      return 1;
    case "Map":
    case "Set":
    case "WeakMap":
    case "WeakSet":
      return 2;
    default:
      return 0;
  }
}
// @__NO_SIDE_EFFECTS__
function g1(e) {
  return /* @__PURE__ */ xo(e) ? e : v1(e, !1, R7, O7, Zx);
}
// @__NO_SIDE_EFFECTS__
function H7(e) {
  return v1(e, !1, $7, F7, ek);
}
// @__NO_SIDE_EFFECTS__
function V_(e) {
  return v1(e, !0, N7, B7, tk);
}
function v1(e, t, n, r, s) {
  if (!pn(e) || e.__v_raw && !(t && e.__v_isReactive) || e.__v_skip || !Object.isExtensible(e)) return e;
  const a = s.get(e);
  if (a) return a;
  const o = q7(p7(e));
  if (o === 0) return e;
  const l = new Proxy(e, o === 2 ? r : n);
  return s.set(e, l), l;
}
// @__NO_SIDE_EFFECTS__
function pl(e) {
  return /* @__PURE__ */ xo(e) ? /* @__PURE__ */ pl(e.__v_raw) : !!(e && e.__v_isReactive);
}
// @__NO_SIDE_EFFECTS__
function xo(e) {
  return !!(e && e.__v_isReadonly);
}
// @__NO_SIDE_EFFECTS__
function Us(e) {
  return !!(e && e.__v_isShallow);
}
// @__NO_SIDE_EFFECTS__
function y1(e) {
  return e ? !!e.__v_raw : !1;
}
// @__NO_SIDE_EFFECTS__
function on(e) {
  const t = e && e.__v_raw;
  return t ? /* @__PURE__ */ on(t) : e;
}
function z7(e) {
  return !ln(e, "__v_skip") && Object.isExtensible(e) && Ox(e, "__v_skip", !0), e;
}
var ia = (e) => pn(e) ? /* @__PURE__ */ g1(e) : e, Uu = (e) => pn(e) ? /* @__PURE__ */ V_(e) : e;
// @__NO_SIDE_EFFECTS__
function kn(e) {
  return e ? e.__v_isRef === !0 : !1;
}
// @__NO_SIDE_EFFECTS__
function ue(e) {
  return K7(e, !1);
}
function K7(e, t) {
  return /* @__PURE__ */ kn(e) ? e : new W7(e, t);
}
var W7 = class {
  constructor(e, t) {
    this.dep = new p1(), this.__v_isRef = !0, this.__v_isShallow = !1, this._rawValue = t ? e : /* @__PURE__ */ on(e), this._value = t ? e : ia(e), this.__v_isShallow = t;
  }
  get value() {
    return this.dep.track(), this._value;
  }
  set value(e) {
    const t = this._rawValue, n = this.__v_isShallow || /* @__PURE__ */ Us(e) || /* @__PURE__ */ xo(e);
    e = n ? e : /* @__PURE__ */ on(e), Ra(e, t) && (this._rawValue = e, this._value = n ? e : ia(e), this.dep.trigger());
  }
};
function T(e) {
  return /* @__PURE__ */ kn(e) ? e.value : e;
}
var G7 = {
  get: (e, t, n) => t === "__v_raw" ? e : T(Reflect.get(e, t, n)),
  set: (e, t, n, r) => {
    const s = e[t];
    return /* @__PURE__ */ kn(s) && !/* @__PURE__ */ kn(n) ? (s.value = n, !0) : Reflect.set(e, t, n, r);
  }
};
function nk(e) {
  return /* @__PURE__ */ pl(e) ? e : new Proxy(e, G7);
}
var V7 = class {
  constructor(e, t, n) {
    this.fn = e, this.setter = t, this._value = void 0, this.dep = new p1(this), this.__v_isRef = !0, this.deps = void 0, this.depsTail = void 0, this.flags = 16, this.globalVersion = rf - 1, this.next = void 0, this.effect = this, this.__v_isReadonly = !t, this.isSSR = n;
  }
  notify() {
    if (this.flags |= 16, !(this.flags & 8) && _n !== this)
      return Kx(this, !0), !0;
  }
  get value() {
    const e = this.dep.track();
    return Vx(this), e && (e.version = this.dep.version), this._value;
  }
  set value(e) {
    this.setter && this.setter(e);
  }
};
// @__NO_SIDE_EFFECTS__
function J7(e, t, n = !1) {
  let r, s;
  return $t(e) ? r = e : (r = e.get, s = e.set), new V7(r, s, n);
}
var $h = {}, hm = /* @__PURE__ */ new WeakMap(), Yi = void 0;
function Y7(e, t = !1, n = Yi) {
  if (n) {
    let r = hm.get(n);
    r || hm.set(n, r = []), r.push(e);
  }
}
function X7(e, t, n = gn) {
  const { immediate: r, deep: s, once: a, scheduler: o, augmentJob: l, call: i } = n, u = ($) => s ? $ : /* @__PURE__ */ Us($) || s === !1 || s === 0 ? po($, 1) : po($);
  let d, f, m, v, y = !1, S = !1;
  if (/* @__PURE__ */ kn(e) ? (f = () => e.value, y = /* @__PURE__ */ Us(e)) : /* @__PURE__ */ pl(e) ? (f = () => u(e), y = !0) : Et(e) ? (S = !0, y = e.some(($) => /* @__PURE__ */ pl($) || /* @__PURE__ */ Us($)), f = () => e.map(($) => {
    if (/* @__PURE__ */ kn($)) return $.value;
    if (/* @__PURE__ */ pl($)) return u($);
    if ($t($)) return i ? i($, 2) : $();
  })) : $t(e) ? t ? f = i ? () => i(e, 2) : e : f = () => {
    if (m) {
      Co();
      try {
        m();
      } finally {
        Io();
      }
    }
    const $ = Yi;
    Yi = d;
    try {
      return i ? i(e, 3, [v]) : e(v);
    } finally {
      Yi = $;
    }
  } : f = La, t && s) {
    const $ = f, p = s === !0 ? 1 / 0 : s;
    f = () => po($(), p);
  }
  const x = T7(), I = () => {
    d.stop(), x && x.active && l1(x.effects, d);
  };
  if (a && t) {
    const $ = t;
    t = (...p) => {
      $(...p), I();
    };
  }
  let E = S ? new Array(e.length).fill($h) : $h;
  const N = ($) => {
    if (!(!(d.flags & 1) || !d.dirty && !$))
      if (t) {
        const p = d.run();
        if (s || y || (S ? p.some((w, _) => Ra(w, E[_])) : Ra(p, E))) {
          m && m();
          const w = Yi;
          Yi = d;
          try {
            const _ = [
              p,
              E === $h ? void 0 : S && E[0] === $h ? [] : E,
              v
            ];
            E = p, i ? i(t, 3, _) : t(..._);
          } finally {
            Yi = w;
          }
        }
      } else d.run();
  };
  return l && l(N), d = new Hx(f), d.scheduler = o ? () => o(N, !1) : N, v = ($) => Y7($, !1, d), m = d.onStop = () => {
    const $ = hm.get(d);
    if ($) {
      if (i) i($, 4);
      else for (const p of $) p();
      hm.delete(d);
    }
  }, t ? r ? N(!0) : E = d.run() : o ? o(N.bind(null, !0), !0) : d.run(), I.pause = d.pause.bind(d), I.resume = d.resume.bind(d), I.stop = I, I;
}
function po(e, t = 1 / 0, n) {
  if (t <= 0 || !pn(e) || e.__v_skip || (n = n || /* @__PURE__ */ new Map(), (n.get(e) || 0) >= t)) return e;
  if (n.set(e, t), t--, /* @__PURE__ */ kn(e)) po(e.value, t, n);
  else if (Et(e)) for (let r = 0; r < e.length; r++) po(e[r], t, n);
  else if (tc(e) || Cu(e)) e.forEach((r) => {
    po(r, t, n);
  });
  else if (Lx(e)) {
    for (const r in e) po(e[r], t, n);
    for (const r of Object.getOwnPropertySymbols(e)) Object.prototype.propertyIsEnumerable.call(e, r) && po(e[r], t, n);
  }
  return e;
}
function Nf(e, t, n, r) {
  try {
    return r ? e(...r) : e();
  } catch (s) {
    Dg(s, t, n);
  }
}
function la(e, t, n, r) {
  if ($t(e)) {
    const s = Nf(e, t, n, r);
    return s && Nx(s) && s.catch((a) => {
      Dg(a, t, n);
    }), s;
  }
  if (Et(e)) {
    const s = [];
    for (let a = 0; a < e.length; a++) s.push(la(e[a], t, n, r));
    return s;
  }
}
function Dg(e, t, n, r = !0) {
  const s = t ? t.vnode : null, { errorHandler: a, throwUnhandledErrorInProduction: o } = t && t.appContext.config || gn;
  if (t) {
    let l = t.parent;
    const i = t.proxy, u = `https://vuejs.org/error-reference/#runtime-${n}`;
    for (; l; ) {
      const d = l.ec;
      if (d) {
        for (let f = 0; f < d.length; f++) if (d[f](e, i, u) === !1) return;
      }
      l = l.parent;
    }
    if (a) {
      Co(), Nf(a, null, 10, [
        e,
        i,
        u
      ]), Io();
      return;
    }
  }
  j7(e, n, s, r, o);
}
function j7(e, t, n, r = !0, s = !1) {
  if (s) throw e;
  console.error(e);
}
var Br = [], Ea = -1, Iu = [], jo = null, lu = 0, rk = /* @__PURE__ */ Promise.resolve(), pm = null;
function Ut(e) {
  const t = pm || rk;
  return e ? t.then(this ? e.bind(this) : e) : t;
}
function Q7(e) {
  let t = Ea + 1, n = Br.length;
  for (; t < n; ) {
    const r = t + n >>> 1, s = Br[r], a = af(s);
    a < e || a === e && s.flags & 2 ? t = r + 1 : n = r;
  }
  return t;
}
function _1(e) {
  if (!(e.flags & 1)) {
    const t = af(e), n = Br[Br.length - 1];
    !n || !(e.flags & 2) && t >= af(n) ? Br.push(e) : Br.splice(Q7(t), 0, e), e.flags |= 1, sk();
  }
}
function sk() {
  pm || (pm = rk.then(ok));
}
function Z7(e) {
  Et(e) ? Iu.push(...e) : jo && e.id === -1 ? jo.splice(lu + 1, 0, e) : e.flags & 1 || (Iu.push(e), e.flags |= 1), sk();
}
function ZA(e, t, n = Ea + 1) {
  for (; n < Br.length; n++) {
    const r = Br[n];
    if (r && r.flags & 2) {
      if (e && r.id !== e.uid) continue;
      Br.splice(n, 1), n--, r.flags & 4 && (r.flags &= -2), r(), r.flags & 4 || (r.flags &= -2);
    }
  }
}
function ak(e) {
  if (Iu.length) {
    const t = [...new Set(Iu)].sort((n, r) => af(n) - af(r));
    if (Iu.length = 0, jo) {
      jo.push(...t);
      return;
    }
    for (jo = t, lu = 0; lu < jo.length; lu++) {
      const n = jo[lu];
      n.flags & 4 && (n.flags &= -2), n.flags & 8 || n(), n.flags &= -2;
    }
    jo = null, lu = 0;
  }
}
var af = (e) => e.id == null ? e.flags & 2 ? -1 : 1 / 0 : e.id;
function ok(e) {
  try {
    for (Ea = 0; Ea < Br.length; Ea++) {
      const t = Br[Ea];
      t && !(t.flags & 8) && (t.flags & 4 && (t.flags &= -2), Nf(t, t.i, t.i ? 15 : 14), t.flags & 4 || (t.flags &= -2));
    }
  } finally {
    for (; Ea < Br.length; Ea++) {
      const t = Br[Ea];
      t && (t.flags &= -2);
    }
    Ea = -1, Br.length = 0, ak(e), pm = null, (Br.length || Iu.length) && ok(e);
  }
}
var Os = null, ik = null;
function mm(e) {
  const t = Os;
  return Os = e, ik = e && e.type.__scopeId || null, t;
}
function e9(e, t = Os, n) {
  if (!t || e._n) return e;
  const r = (...s) => {
    r._d && d2(-1);
    const a = mm(t);
    let o;
    try {
      o = e(...s);
    } finally {
      mm(a), r._d && d2(1);
    }
    return o;
  };
  return r._n = !0, r._c = !0, r._d = !0, r;
}
function er(e, t) {
  if (Os === null) return e;
  const n = qg(Os), r = e.dirs || (e.dirs = []);
  for (let s = 0; s < t.length; s++) {
    let [a, o, l, i = gn] = t[s];
    a && ($t(a) && (a = {
      mounted: a,
      updated: a
    }), a.deep && po(o), r.push({
      dir: a,
      instance: n,
      value: o,
      oldValue: void 0,
      arg: l,
      modifiers: i
    }));
  }
  return e;
}
function zi(e, t, n, r) {
  const s = e.dirs, a = t && t.dirs;
  for (let o = 0; o < s.length; o++) {
    const l = s[o];
    a && (l.oldValue = a[o].value);
    let i = l.dir[r];
    i && (Co(), la(i, n, 8, [
      e.el,
      l,
      e,
      t
    ]), Io());
  }
}
function lk(e, t) {
  if (qr) {
    let n = qr.provides;
    const r = qr.parent && qr.parent.provides;
    r === n && (n = qr.provides = Object.create(r)), n[e] = t;
  }
}
function Ld(e, t, n = !1) {
  const r = X9();
  if (r || xu) {
    let s = xu ? xu._context.provides : r ? r.parent == null || r.ce ? r.vnode.appContext && r.vnode.appContext.provides : r.parent.provides : void 0;
    if (s && e in s) return s[e];
    if (arguments.length > 1) return n && $t(t) ? t.call(r && r.proxy) : t;
  }
}
var t9 = /* @__PURE__ */ Symbol.for("v-scx"), n9 = () => {
  {
    const e = Ld(t9);
    return e;
  }
};
function vt(e, t, n) {
  return uk(e, t, n);
}
function uk(e, t, n = gn) {
  const { immediate: r, deep: s, flush: a, once: o } = n, l = tr({}, n), i = t && r || !t && a !== "post";
  let u;
  if (lf) {
    if (a === "sync") {
      const v = n9();
      u = v.__watcherHandles || (v.__watcherHandles = []);
    } else if (!i) {
      const v = () => {
      };
      return v.stop = La, v.resume = La, v.pause = La, v;
    }
  }
  const d = qr;
  l.call = (v, y, S) => la(v, d, y, S);
  let f = !1;
  a === "post" ? l.scheduler = (v) => {
    Dr(v, d && d.suspense);
  } : a !== "sync" && (f = !0, l.scheduler = (v, y) => {
    y ? v() : _1(v);
  }), l.augmentJob = (v) => {
    t && (v.flags |= 4), f && (v.flags |= 2, d && (v.id = d.uid, v.i = d));
  };
  const m = X7(e, t, l);
  return lf && (u ? u.push(m) : i && m()), m;
}
function r9(e, t, n) {
  const r = this.proxy, s = xn(e) ? e.includes(".") ? ck(r, e) : () => r[e] : e.bind(r, r);
  let a;
  $t(t) ? a = t : (a = t.handler, n = t);
  const o = $f(this), l = uk(s, a.bind(r), n);
  return o(), l;
}
function ck(e, t) {
  const n = t.split(".");
  return () => {
    let r = e;
    for (let s = 0; s < n.length && r; s++) r = r[n[s]];
    return r;
  };
}
var Vo = /* @__PURE__ */ new WeakMap(), dk = /* @__PURE__ */ Symbol("_vte"), s9 = (e) => e.__isTeleport, tl = (e) => e && (e.disabled || e.disabled === ""), a9 = (e) => e && (e.defer || e.defer === ""), e2 = (e) => typeof SVGElement < "u" && e instanceof SVGElement, t2 = (e) => typeof MathMLElement == "function" && e instanceof MathMLElement, J_ = (e, t) => {
  const n = e && e.to;
  return xn(n) ? t ? t(n) : null : n;
}, o9 = {
  name: "Teleport",
  __isTeleport: !0,
  process(e, t, n, r, s, a, o, l, i, u) {
    const { mc: d, pc: f, pbc: m, o: { insert: v, querySelector: y, createText: S, createComment: x, parentNode: I } } = u, E = tl(t.props);
    let { dynamicChildren: N } = t;
    const $ = (_, b, C) => {
      _.shapeFlag & 16 && d(_.children, b, C, s, a, o, l, i);
    }, p = (_ = t) => {
      const b = tl(_.props), C = _.target = J_(_.props, y), k = Y_(C, _, S, v);
      C && (o !== "svg" && e2(C) ? o = "svg" : o !== "mathml" && t2(C) && (o = "mathml"), s && s.isCE && (s.ce._teleportTargets || (s.ce._teleportTargets = /* @__PURE__ */ new Set())).add(C), b || ($(_, C, k), ld(_, !1)));
    }, w = (_) => {
      const b = () => {
        Vo.get(_) === b && (Vo.delete(_), tl(_.props) && ($(_, I(_.el) || n, _.anchor), ld(_, !0)), p(_));
      };
      Vo.set(_, b), Dr(b, a);
    };
    if (e == null) {
      const _ = t.el = S(""), b = t.anchor = S("");
      if (v(_, n, r), v(b, n, r), a9(t.props) || a && a.pendingBranch) {
        w(t);
        return;
      }
      E && ($(t, n, b), ld(t, !0)), p();
    } else {
      t.el = e.el;
      const _ = t.anchor = e.anchor, b = Vo.get(e);
      if (b) {
        b.flags |= 8, Vo.delete(e), w(t);
        return;
      }
      t.targetStart = e.targetStart;
      const C = t.target = e.target, k = t.targetAnchor = e.targetAnchor, P = tl(e.props), H = P ? n : C, G = P ? _ : k;
      if (o === "svg" || e2(C) ? o = "svg" : (o === "mathml" || t2(C)) && (o = "mathml"), N ? (m(e.dynamicChildren, N, H, s, a, o, l), E1(e, t, !0)) : i || f(e, t, H, G, s, a, o, l, !1), E)
        P ? t.props && e.props && t.props.to !== e.props.to && (t.props.to = e.props.to) : Lh(t, n, _, u, 1);
      else if ((t.props && t.props.to) !== (e.props && e.props.to)) {
        const ee = t.target = J_(t.props, y);
        ee && Lh(t, ee, null, u, 0);
      } else P && Lh(t, C, k, u, 1);
      ld(t, E);
    }
  },
  remove(e, t, n, { um: r, o: { remove: s } }, a) {
    const { shapeFlag: o, children: l, anchor: i, targetStart: u, targetAnchor: d, target: f, props: m } = e, v = a || !tl(m), y = Vo.get(e);
    if (y && (y.flags |= 8, Vo.delete(e)), f && (s(u), s(d)), a && s(i), !y && o & 16) for (let S = 0; S < l.length; S++) {
      const x = l[S];
      r(x, t, n, v, !!x.dynamicChildren);
    }
  },
  move: Lh,
  hydrate: i9
};
function Lh(e, t, n, { o: { insert: r }, m: s }, a = 2) {
  a === 0 && r(e.targetAnchor, t, n);
  const { el: o, anchor: l, shapeFlag: i, children: u, props: d } = e, f = a === 2;
  if (f && r(o, t, n), !Vo.has(e) && (!f || tl(d)) && i & 16)
    for (let m = 0; m < u.length; m++) s(u[m], t, n, 2);
  f && r(l, t, n);
}
function i9(e, t, n, r, s, a, { o: { nextSibling: o, parentNode: l, querySelector: i, insert: u, createText: d } }, f) {
  function m(x, I) {
    let E = I;
    for (; E; ) {
      if (E && E.nodeType === 8) {
        if (E.data === "teleport start anchor") t.targetStart = E;
        else if (E.data === "teleport anchor") {
          t.targetAnchor = E, x._lpa = t.targetAnchor && o(t.targetAnchor);
          break;
        }
      }
      E = o(E);
    }
  }
  function v(x, I) {
    I.anchor = f(o(x), I, l(x), n, r, s, a);
  }
  const y = t.target = J_(t.props, i), S = tl(t.props);
  if (y) {
    const x = y._lpa || y.firstChild;
    t.shapeFlag & 16 && (S ? (v(e, t), m(y, x), t.targetAnchor || Y_(y, t, d, u, l(e) === y ? e : null)) : (t.anchor = o(e), m(y, x), t.targetAnchor || Y_(y, t, d, u), f(x && o(x), t, y, n, r, s, a))), ld(t, S);
  } else S && t.shapeFlag & 16 && (v(e, t), t.targetStart = e, t.targetAnchor = o(e));
  return t.anchor && o(t.anchor);
}
var fk = o9;
function ld(e, t) {
  const n = e.ctx;
  if (n && n.ut) {
    let r, s;
    for (t ? (r = e.el, s = e.anchor) : (r = e.targetStart, s = e.targetAnchor); r && r !== s; )
      r.nodeType === 1 && r.setAttribute("data-v-owner", n.uid), r = r.nextSibling;
    n.ut();
  }
}
function Y_(e, t, n, r, s = null) {
  const a = t.targetStart = n(""), o = t.targetAnchor = n("");
  return a[dk] = o, e && (r(a, e, s), r(o, e, s)), o;
}
var ky = /* @__PURE__ */ Symbol("_leaveCb");
function b1(e, t) {
  e.shapeFlag & 6 && e.component ? (e.transition = t, b1(e.component.subTree, t)) : e.shapeFlag & 128 ? (e.ssContent.transition = t.clone(e.ssContent), e.ssFallback.transition = t.clone(e.ssFallback)) : e.transition = t;
}
// @__NO_SIDE_EFFECTS__
function bn(e, t) {
  return $t(e) ? tr({ name: e.name }, t, { setup: e }) : e;
}
function hk(e) {
  e.ids = [
    e.ids[0] + e.ids[2]++ + "-",
    0,
    0
  ];
}
function n2(e, t) {
  let n;
  return !!((n = Object.getOwnPropertyDescriptor(e, t)) && !n.configurable);
}
var gm = /* @__PURE__ */ new WeakMap();
function Dd(e, t, n, r, s = !1) {
  if (Et(e)) {
    e.forEach((S, x) => Dd(S, t && (Et(t) ? t[x] : t), n, r, s));
    return;
  }
  if (Od(r) && !s) {
    r.shapeFlag & 512 && r.type.__asyncResolved && r.component.subTree.component && Dd(e, t, n, r.component.subTree);
    return;
  }
  const a = r.shapeFlag & 4 ? qg(r.component) : r.el, o = s ? null : a, { i: l, r: i } = e, u = t && t.r, d = l.refs === gn ? l.refs = {} : l.refs, f = l.setupState, m = /* @__PURE__ */ on(f), v = f === gn ? Rx : (S) => n2(d, S) ? !1 : ln(m, S), y = (S, x) => !(x && n2(d, x));
  if (u != null && u !== i) {
    if (r2(t), xn(u))
      d[u] = null, v(u) && (f[u] = null);
    else if (/* @__PURE__ */ kn(u)) {
      const S = t;
      y(u, S.k) && (u.value = null), S.k && (d[S.k] = null);
    }
  }
  if ($t(i)) Nf(i, l, 12, [o, d]);
  else {
    const S = xn(i), x = /* @__PURE__ */ kn(i);
    if (S || x) {
      const I = () => {
        if (e.f) {
          const E = S ? v(i) ? f[i] : d[i] : y(i) || !e.k ? i.value : d[e.k];
          if (s) Et(E) && l1(E, a);
          else if (Et(E)) E.includes(a) || E.push(a);
          else if (S)
            d[i] = [a], v(i) && (f[i] = d[i]);
          else {
            const N = [a];
            y(i, e.k) && (i.value = N), e.k && (d[e.k] = N);
          }
        } else S ? (d[i] = o, v(i) && (f[i] = o)) : x && (y(i, e.k) && (i.value = o), e.k && (d[e.k] = o));
      };
      if (o) {
        const E = () => {
          I(), gm.delete(e);
        };
        E.id = -1, gm.set(e, E), Dr(E, n);
      } else
        r2(e), I();
    }
  }
}
function r2(e) {
  const t = gm.get(e);
  t && (t.flags |= 8, gm.delete(e));
}
var Tve = $g().requestIdleCallback || ((e) => setTimeout(e, 1)), Eve = $g().cancelIdleCallback || ((e) => clearTimeout(e)), Od = (e) => !!e.type.__asyncLoader, pk = (e) => e.type.__isKeepAlive;
function l9(e, t) {
  mk(e, "a", t);
}
function u9(e, t) {
  mk(e, "da", t);
}
function mk(e, t, n = qr) {
  const r = e.__wdc || (e.__wdc = () => {
    let s = n;
    for (; s; ) {
      if (s.isDeactivated) return;
      s = s.parent;
    }
    return e();
  });
  if (Og(t, r, n), n) {
    let s = n.parent;
    for (; s && s.parent; )
      pk(s.parent.vnode) && c9(r, t, n, s), s = s.parent;
  }
}
function c9(e, t, n, r) {
  const s = Og(t, e, r, !0);
  Fg(() => {
    l1(r[t], s);
  }, n);
}
function Og(e, t, n = qr, r = !1) {
  if (n) {
    const s = n[e] || (n[e] = []), a = t.__weh || (t.__weh = (...o) => {
      Co();
      const l = $f(n), i = la(t, n, e, o);
      return l(), Io(), i;
    });
    return r ? s.unshift(a) : s.push(a), a;
  }
}
var Mo = (e) => (t, n = qr) => {
  (!lf || e === "sp") && Og(e, (...r) => t(...r), n);
}, d9 = Mo("bm"), w1 = Mo("m"), gk = Mo("bu"), vk = Mo("u"), Sl = Mo("bum"), Fg = Mo("um"), f9 = Mo("sp"), h9 = Mo("rtg"), p9 = Mo("rtc");
function m9(e, t = qr) {
  Og("ec", e, t);
}
var g9 = /* @__PURE__ */ Symbol.for("v-ndc");
function tt(e, t, n, r) {
  let s;
  const a = n && n[r], o = Et(e);
  if (o || xn(e)) {
    const l = o && /* @__PURE__ */ pl(e);
    let i = !1, u = !1;
    l && (i = !/* @__PURE__ */ Us(e), u = /* @__PURE__ */ xo(e), e = Lg(e)), s = new Array(e.length);
    for (let d = 0, f = e.length; d < f; d++) s[d] = t(i ? u ? Uu(ia(e[d])) : ia(e[d]) : e[d], d, void 0, a && a[d]);
  } else if (typeof e == "number") {
    s = new Array(e);
    for (let l = 0; l < e; l++) s[l] = t(l + 1, l, void 0, a && a[l]);
  } else if (pn(e)) if (e[Symbol.iterator]) s = Array.from(e, (l, i) => t(l, i, void 0, a && a[i]));
  else {
    const l = Object.keys(e);
    s = new Array(l.length);
    for (let i = 0, u = l.length; i < u; i++) {
      const d = l[i];
      s[i] = t(e[d], d, i, a && a[i]);
    }
  }
  else s = [];
  return n && (n[r] = s), s;
}
var X_ = (e) => e ? Dk(e) ? qg(e) : X_(e.parent) : null, Fd = /* @__PURE__ */ tr(/* @__PURE__ */ Object.create(null), {
  $: (e) => e,
  $el: (e) => e.vnode.el,
  $data: (e) => e.data,
  $props: (e) => e.props,
  $attrs: (e) => e.attrs,
  $slots: (e) => e.slots,
  $refs: (e) => e.refs,
  $parent: (e) => X_(e.parent),
  $root: (e) => X_(e.root),
  $host: (e) => e.ce,
  $emit: (e) => e.emit,
  $options: (e) => S1(e),
  $forceUpdate: (e) => e.f || (e.f = () => {
    _1(e.update);
  }),
  $nextTick: (e) => e.n || (e.n = Ut.bind(e.proxy)),
  $watch: (e) => r9.bind(e)
}), My = (e, t) => e !== gn && !e.__isScriptSetup && ln(e, t), v9 = {
  get({ _: e }, t) {
    if (t === "__v_skip") return !0;
    const { ctx: n, setupState: r, data: s, props: a, accessCache: o, type: l, appContext: i } = e;
    if (t[0] !== "$") {
      const m = o[t];
      if (m !== void 0) switch (m) {
        case 1:
          return r[t];
        case 2:
          return s[t];
        case 4:
          return n[t];
        case 3:
          return a[t];
      }
      else {
        if (My(r, t))
          return o[t] = 1, r[t];
        if (s !== gn && ln(s, t))
          return o[t] = 2, s[t];
        if (ln(a, t))
          return o[t] = 3, a[t];
        if (n !== gn && ln(n, t))
          return o[t] = 4, n[t];
        j_ && (o[t] = 0);
      }
    }
    const u = Fd[t];
    let d, f;
    if (u)
      return t === "$attrs" && Er(e.attrs, "get", ""), u(e);
    if ((d = l.__cssModules) && (d = d[t])) return d;
    if (n !== gn && ln(n, t))
      return o[t] = 4, n[t];
    if (f = i.config.globalProperties, ln(f, t)) return f[t];
  },
  set({ _: e }, t, n) {
    const { data: r, setupState: s, ctx: a } = e;
    return My(s, t) ? (s[t] = n, !0) : r !== gn && ln(r, t) ? (r[t] = n, !0) : ln(e.props, t) || t[0] === "$" && t.slice(1) in e ? !1 : (a[t] = n, !0);
  },
  has({ _: { data: e, setupState: t, accessCache: n, ctx: r, appContext: s, props: a, type: o } }, l) {
    let i;
    return !!(n[l] || e !== gn && l[0] !== "$" && ln(e, l) || My(t, l) || ln(a, l) || ln(r, l) || ln(Fd, l) || ln(s.config.globalProperties, l) || (i = o.__cssModules) && i[l]);
  },
  defineProperty(e, t, n) {
    return n.get != null ? e._.accessCache[t] = 0 : ln(n, "value") && this.set(e, t, n.value, null), Reflect.defineProperty(e, t, n);
  }
};
function s2(e) {
  return Et(e) ? e.reduce((t, n) => (t[n] = null, t), {}) : e;
}
var j_ = !0;
function y9(e) {
  const t = S1(e), n = e.proxy, r = e.ctx;
  j_ = !1, t.beforeCreate && a2(t.beforeCreate, e, "bc");
  const { data: s, computed: a, methods: o, watch: l, provide: i, inject: u, created: d, beforeMount: f, mounted: m, beforeUpdate: v, updated: y, activated: S, deactivated: x, beforeDestroy: I, beforeUnmount: E, destroyed: N, unmounted: $, render: p, renderTracked: w, renderTriggered: _, errorCaptured: b, serverPrefetch: C, expose: k, inheritAttrs: P, components: H, directives: G, filters: ee } = t;
  if (u && _9(u, r, null), o) for (const R in o) {
    const V = o[R];
    $t(V) && (r[R] = V.bind(n));
  }
  if (s) {
    const R = s.call(n, n);
    pn(R) && (e.data = /* @__PURE__ */ g1(R));
  }
  if (j_ = !0, a) for (const R in a) {
    const V = a[R], le = ne({
      get: $t(V) ? V.bind(n, n) : $t(V.get) ? V.get.bind(n, n) : La,
      set: !$t(V) && $t(V.set) ? V.set.bind(n) : La
    });
    Object.defineProperty(r, R, {
      enumerable: !0,
      configurable: !0,
      get: () => le.value,
      set: (ve) => le.value = ve
    });
  }
  if (l) for (const R in l) yk(l[R], r, n, R);
  if (i) {
    const R = $t(i) ? i.call(n) : i;
    Reflect.ownKeys(R).forEach((V) => {
      lk(V, R[V]);
    });
  }
  d && a2(d, e, "c");
  function z(R, V) {
    Et(V) ? V.forEach((le) => R(le.bind(n))) : V && R(V.bind(n));
  }
  if (z(d9, f), z(w1, m), z(gk, v), z(vk, y), z(l9, S), z(u9, x), z(m9, b), z(p9, w), z(h9, _), z(Sl, E), z(Fg, $), z(f9, C), Et(k))
    if (k.length) {
      const R = e.exposed || (e.exposed = {});
      k.forEach((V) => {
        Object.defineProperty(R, V, {
          get: () => n[V],
          set: (le) => n[V] = le,
          enumerable: !0
        });
      });
    } else e.exposed || (e.exposed = {});
  p && e.render === La && (e.render = p), P != null && (e.inheritAttrs = P), H && (e.components = H), G && (e.directives = G), C && hk(e);
}
function _9(e, t, n = La) {
  Et(e) && (e = Q_(e));
  for (const r in e) {
    const s = e[r];
    let a;
    pn(s) ? "default" in s ? a = Ld(s.from || r, s.default, !0) : a = Ld(s.from || r) : a = Ld(s), /* @__PURE__ */ kn(a) ? Object.defineProperty(t, r, {
      enumerable: !0,
      configurable: !0,
      get: () => a.value,
      set: (o) => a.value = o
    }) : t[r] = a;
  }
}
function a2(e, t, n) {
  la(Et(e) ? e.map((r) => r.bind(t.proxy)) : e.bind(t.proxy), t, n);
}
function yk(e, t, n, r) {
  let s = r.includes(".") ? ck(n, r) : () => n[r];
  if (xn(e)) {
    const a = t[e];
    $t(a) && vt(s, a);
  } else if ($t(e)) vt(s, e.bind(n));
  else if (pn(e)) if (Et(e)) e.forEach((a) => yk(a, t, n, r));
  else {
    const a = $t(e.handler) ? e.handler.bind(n) : t[e.handler];
    $t(a) && vt(s, a, e);
  }
}
function S1(e) {
  const t = e.type, { mixins: n, extends: r } = t, { mixins: s, optionsCache: a, config: { optionMergeStrategies: o } } = e.appContext, l = a.get(t);
  let i;
  return l ? i = l : !s.length && !n && !r ? i = t : (i = {}, s.length && s.forEach((u) => vm(i, u, o, !0)), vm(i, t, o)), pn(t) && a.set(t, i), i;
}
function vm(e, t, n, r = !1) {
  const { mixins: s, extends: a } = t;
  a && vm(e, a, n, !0), s && s.forEach((o) => vm(e, o, n, !0));
  for (const o in t) if (!(r && o === "expose")) {
    const l = b9[o] || n && n[o];
    e[o] = l ? l(e[o], t[o]) : t[o];
  }
  return e;
}
var b9 = {
  data: o2,
  props: i2,
  emits: i2,
  methods: ud,
  computed: ud,
  beforeCreate: $r,
  created: $r,
  beforeMount: $r,
  mounted: $r,
  beforeUpdate: $r,
  updated: $r,
  beforeDestroy: $r,
  beforeUnmount: $r,
  destroyed: $r,
  unmounted: $r,
  activated: $r,
  deactivated: $r,
  errorCaptured: $r,
  serverPrefetch: $r,
  components: ud,
  directives: ud,
  watch: S9,
  provide: o2,
  inject: w9
};
function o2(e, t) {
  return t ? e ? function() {
    return tr($t(e) ? e.call(this, this) : e, $t(t) ? t.call(this, this) : t);
  } : t : e;
}
function w9(e, t) {
  return ud(Q_(e), Q_(t));
}
function Q_(e) {
  if (Et(e)) {
    const t = {};
    for (let n = 0; n < e.length; n++) t[e[n]] = e[n];
    return t;
  }
  return e;
}
function $r(e, t) {
  return e ? [...new Set([].concat(e, t))] : t;
}
function ud(e, t) {
  return e ? tr(/* @__PURE__ */ Object.create(null), e, t) : t;
}
function i2(e, t) {
  return e ? Et(e) && Et(t) ? [.../* @__PURE__ */ new Set([...e, ...t])] : tr(/* @__PURE__ */ Object.create(null), s2(e), s2(t ?? {})) : t;
}
function S9(e, t) {
  if (!e) return t;
  if (!t) return e;
  const n = tr(/* @__PURE__ */ Object.create(null), e);
  for (const r in t) n[r] = $r(e[r], t[r]);
  return n;
}
function _k() {
  return {
    app: null,
    config: {
      isNativeTag: Rx,
      performance: !1,
      globalProperties: {},
      optionMergeStrategies: {},
      errorHandler: void 0,
      warnHandler: void 0,
      compilerOptions: {}
    },
    mixins: [],
    components: {},
    directives: {},
    provides: /* @__PURE__ */ Object.create(null),
    optionsCache: /* @__PURE__ */ new WeakMap(),
    propsCache: /* @__PURE__ */ new WeakMap(),
    emitsCache: /* @__PURE__ */ new WeakMap()
  };
}
var A9 = 0;
function T9(e, t) {
  return function(r, s = null) {
    $t(r) || (r = tr({}, r)), s != null && !pn(s) && (s = null);
    const a = _k(), o = /* @__PURE__ */ new WeakSet(), l = [];
    let i = !1;
    const u = a.app = {
      _uid: A9++,
      _component: r,
      _props: s,
      _container: null,
      _context: a,
      _instance: null,
      version: nL,
      get config() {
        return a.config;
      },
      set config(d) {
      },
      use(d, ...f) {
        return o.has(d) || (d && $t(d.install) ? (o.add(d), d.install(u, ...f)) : $t(d) && (o.add(d), d(u, ...f))), u;
      },
      mixin(d) {
        return a.mixins.includes(d) || a.mixins.push(d), u;
      },
      component(d, f) {
        return f ? (a.components[d] = f, u) : a.components[d];
      },
      directive(d, f) {
        return f ? (a.directives[d] = f, u) : a.directives[d];
      },
      mount(d, f, m) {
        if (!i) {
          const v = u._ceVNode || Wn(r, s);
          return v.appContext = a, m === !0 ? m = "svg" : m === !1 && (m = void 0), f && t ? t(v, d) : e(v, d, m), i = !0, u._container = d, d.__vue_app__ = u, qg(v.component);
        }
      },
      onUnmount(d) {
        l.push(d);
      },
      unmount() {
        i && (la(l, u._instance, 16), e(null, u._container), delete u._container.__vue_app__);
      },
      provide(d, f) {
        return a.provides[d] = f, u;
      },
      runWithContext(d) {
        const f = xu;
        xu = u;
        try {
          return d();
        } finally {
          xu = f;
        }
      }
    };
    return u;
  };
}
var xu = null, E9 = (e, t) => t === "modelValue" || t === "model-value" ? e.modelModifiers : e[`${t}Modifiers`] || e[`${na(t)}Modifiers`] || e[`${Ai(t)}Modifiers`];
function C9(e, t, ...n) {
  if (e.isUnmounted) return;
  const r = e.vnode.props || gn;
  let s = n;
  const a = t.startsWith("update:"), o = a && E9(r, t.slice(7));
  o && (o.trim && (s = n.map((d) => xn(d) ? d.trim() : d)), o.number && (s = n.map(Ng)));
  let l, i = r[l = Ty(t)] || r[l = Ty(na(t))];
  !i && a && (i = r[l = Ty(Ai(t))]), i && la(i, e, 6, s);
  const u = r[l + "Once"];
  if (u) {
    if (!e.emitted) e.emitted = {};
    else if (e.emitted[l]) return;
    e.emitted[l] = !0, la(u, e, 6, s);
  }
}
var I9 = /* @__PURE__ */ new WeakMap();
function bk(e, t, n = !1) {
  const r = n ? I9 : t.emitsCache, s = r.get(e);
  if (s !== void 0) return s;
  const a = e.emits;
  let o = {}, l = !1;
  if (!$t(e)) {
    const i = (u) => {
      const d = bk(u, t, !0);
      d && (l = !0, tr(o, d));
    };
    !n && t.mixins.length && t.mixins.forEach(i), e.extends && i(e.extends), e.mixins && e.mixins.forEach(i);
  }
  return !a && !l ? (pn(e) && r.set(e, null), null) : (Et(a) ? a.forEach((i) => o[i] = null) : tr(o, a), pn(e) && r.set(e, o), o);
}
function Bg(e, t) {
  return !e || !Mg(t) ? !1 : (t = t.slice(2).replace(/Once$/, ""), ln(e, t[0].toLowerCase() + t.slice(1)) || ln(e, Ai(t)) || ln(e, t));
}
function Py(e) {
  const { type: t, vnode: n, proxy: r, withProxy: s, propsOptions: [a], slots: o, attrs: l, emit: i, render: u, renderCache: d, props: f, data: m, setupState: v, ctx: y, inheritAttrs: S } = e, x = mm(e);
  let I, E;
  try {
    if (n.shapeFlag & 4) {
      const $ = s || r, p = $;
      I = ka(u.call(p, $, d, f, v, m, y)), E = l;
    } else {
      const $ = t;
      I = ka($.length > 1 ? $(f, {
        attrs: l,
        slots: o,
        emit: i
      }) : $(f, null)), E = t.props ? l : x9(l);
    }
  } catch ($) {
    Bd.length = 0, Dg($, e, 1), I = Wn(pi);
  }
  let N = I;
  if (E && S !== !1) {
    const $ = Object.keys(E), { shapeFlag: p } = N;
    $.length && p & 7 && (a && $.some(Pg) && (E = k9(E, a)), N = qu(N, E, !1, !0));
  }
  return n.dirs && (N = qu(N, null, !1, !0), N.dirs = N.dirs ? N.dirs.concat(n.dirs) : n.dirs), n.transition && b1(N, n.transition), I = N, mm(x), I;
}
var x9 = (e) => {
  let t;
  for (const n in e) (n === "class" || n === "style" || Mg(n)) && ((t || (t = {}))[n] = e[n]);
  return t;
}, k9 = (e, t) => {
  const n = {};
  for (const r in e) (!Pg(r) || !(r.slice(9) in t)) && (n[r] = e[r]);
  return n;
};
function M9(e, t, n) {
  const { props: r, children: s, component: a } = e, { props: o, children: l, patchFlag: i } = t, u = a.emitsOptions;
  if (t.dirs || t.transition) return !0;
  if (n && i >= 0) {
    if (i & 1024) return !0;
    if (i & 16)
      return r ? l2(r, o, u) : !!o;
    if (i & 8) {
      const d = t.dynamicProps;
      for (let f = 0; f < d.length; f++) {
        const m = d[f];
        if (wk(o, r, m) && !Bg(u, m)) return !0;
      }
    }
  } else
    return (s || l) && (!l || !l.$stable) ? !0 : r === o ? !1 : r ? o ? l2(r, o, u) : !0 : !!o;
  return !1;
}
function l2(e, t, n) {
  const r = Object.keys(t);
  if (r.length !== Object.keys(e).length) return !0;
  for (let s = 0; s < r.length; s++) {
    const a = r[s];
    if (wk(t, e, a) && !Bg(n, a)) return !0;
  }
  return !1;
}
function wk(e, t, n) {
  const r = e[n], s = t[n];
  return n === "style" && pn(r) && pn(s) ? !nc(r, s) : r !== s;
}
function P9({ vnode: e, parent: t, suspense: n }, r) {
  for (; t; ) {
    const s = t.subTree;
    if (s.suspense && s.suspense.activeBranch === e && (s.suspense.vnode.el = s.el = r, e = s), s === e)
      (e = t.vnode).el = r, t = t.parent;
    else break;
  }
  n && n.activeBranch === e && (n.vnode.el = r);
}
var Sk = {}, Ak = () => Object.create(Sk), Tk = (e) => Object.getPrototypeOf(e) === Sk;
function R9(e, t, n, r = !1) {
  const s = {}, a = Ak();
  e.propsDefaults = /* @__PURE__ */ Object.create(null), Ek(e, t, s, a);
  for (const o in e.propsOptions[0]) o in s || (s[o] = void 0);
  n ? e.props = r ? s : /* @__PURE__ */ H7(s) : e.type.props ? e.props = s : e.props = a, e.attrs = a;
}
function N9(e, t, n, r) {
  const { props: s, attrs: a, vnode: { patchFlag: o } } = e, l = /* @__PURE__ */ on(s), [i] = e.propsOptions;
  let u = !1;
  if ((r || o > 0) && !(o & 16)) {
    if (o & 8) {
      const d = e.vnode.dynamicProps;
      for (let f = 0; f < d.length; f++) {
        let m = d[f];
        if (Bg(e.emitsOptions, m)) continue;
        const v = t[m];
        if (i) if (ln(a, m))
          v !== a[m] && (a[m] = v, u = !0);
        else {
          const y = na(m);
          s[y] = Z_(i, l, y, v, e, !1);
        }
        else v !== a[m] && (a[m] = v, u = !0);
      }
    }
  } else {
    Ek(e, t, s, a) && (u = !0);
    let d;
    for (const f in l) (!t || !ln(t, f) && ((d = Ai(f)) === f || !ln(t, d))) && (i ? n && (n[f] !== void 0 || n[d] !== void 0) && (s[f] = Z_(i, l, f, void 0, e, !0)) : delete s[f]);
    if (a !== l)
      for (const f in a) (!t || !ln(t, f)) && (delete a[f], u = !0);
  }
  u && ho(e.attrs, "set", "");
}
function Ek(e, t, n, r) {
  const [s, a] = e.propsOptions;
  let o = !1, l;
  if (t) for (let i in t) {
    if (Rd(i)) continue;
    const u = t[i];
    let d;
    s && ln(s, d = na(i)) ? !a || !a.includes(d) ? n[d] = u : (l || (l = {}))[d] = u : Bg(e.emitsOptions, i) || (!(i in r) || u !== r[i]) && (r[i] = u, o = !0);
  }
  if (a) {
    const i = /* @__PURE__ */ on(n), u = l || gn;
    for (let d = 0; d < a.length; d++) {
      const f = a[d];
      n[f] = Z_(s, i, f, u[f], e, !ln(u, f));
    }
  }
  return o;
}
function Z_(e, t, n, r, s, a) {
  const o = e[n];
  if (o != null) {
    const l = ln(o, "default");
    if (l && r === void 0) {
      const i = o.default;
      if (o.type !== Function && !o.skipFactory && $t(i)) {
        const { propsDefaults: u } = s;
        if (n in u) r = u[n];
        else {
          const d = $f(s);
          r = u[n] = i.call(null, t), d();
        }
      } else r = i;
      s.ce && s.ce._setProp(n, r);
    }
    o[0] && (a && !l ? r = !1 : o[1] && (r === "" || r === Ai(n)) && (r = !0));
  }
  return r;
}
var $9 = /* @__PURE__ */ new WeakMap();
function Ck(e, t, n = !1) {
  const r = n ? $9 : t.propsCache, s = r.get(e);
  if (s) return s;
  const a = e.props, o = {}, l = [];
  let i = !1;
  if (!$t(e)) {
    const d = (f) => {
      i = !0;
      const [m, v] = Ck(f, t, !0);
      tr(o, m), v && l.push(...v);
    };
    !n && t.mixins.length && t.mixins.forEach(d), e.extends && d(e.extends), e.mixins && e.mixins.forEach(d);
  }
  if (!a && !i)
    return pn(e) && r.set(e, Eu), Eu;
  if (Et(a)) for (let d = 0; d < a.length; d++) {
    const f = na(a[d]);
    u2(f) && (o[f] = gn);
  }
  else if (a) for (const d in a) {
    const f = na(d);
    if (u2(f)) {
      const m = a[d], v = o[f] = Et(m) || $t(m) ? { type: m } : tr({}, m), y = v.type;
      let S = !1, x = !0;
      if (Et(y)) for (let I = 0; I < y.length; ++I) {
        const E = y[I], N = $t(E) && E.name;
        if (N === "Boolean") {
          S = !0;
          break;
        } else N === "String" && (x = !1);
      }
      else S = $t(y) && y.name === "Boolean";
      v[0] = S, v[1] = x, (S || ln(v, "default")) && l.push(f);
    }
  }
  const u = [o, l];
  return pn(e) && r.set(e, u), u;
}
function u2(e) {
  return e[0] !== "$" && !Rd(e);
}
var A1 = (e) => e === "_" || e === "_ctx" || e === "$stable", T1 = (e) => Et(e) ? e.map(ka) : [ka(e)], L9 = (e, t, n) => {
  if (t._n) return t;
  const r = e9((...s) => T1(t(...s)), n);
  return r._c = !1, r;
}, Ik = (e, t, n) => {
  const r = e._ctx;
  for (const s in e) {
    if (A1(s)) continue;
    const a = e[s];
    if ($t(a)) t[s] = L9(s, a, r);
    else if (a != null) {
      const o = T1(a);
      t[s] = () => o;
    }
  }
}, xk = (e, t) => {
  const n = T1(t);
  e.slots.default = () => n;
}, kk = (e, t, n) => {
  for (const r in t) (n || !A1(r)) && (e[r] = t[r]);
}, D9 = (e, t, n) => {
  const r = e.slots = Ak();
  if (e.vnode.shapeFlag & 32) {
    const s = t._;
    s ? (kk(r, t, n), n && Ox(r, "_", s, !0)) : Ik(t, r);
  } else t && xk(e, t);
}, O9 = (e, t, n) => {
  const { vnode: r, slots: s } = e;
  let a = !0, o = gn;
  if (r.shapeFlag & 32) {
    const l = t._;
    l ? n && l === 1 ? a = !1 : kk(s, t, n) : (a = !t.$stable, Ik(t, s)), o = t;
  } else t && (xk(e, t), o = { default: 1 });
  if (a)
    for (const l in s) !A1(l) && o[l] == null && delete s[l];
}, Dr = H9;
function F9(e) {
  return B9(e);
}
function B9(e, t) {
  const n = $g();
  n.__VUE__ = !0;
  const { insert: r, remove: s, patchProp: a, createElement: o, createText: l, createComment: i, setText: u, setElementText: d, parentNode: f, nextSibling: m, setScopeId: v = La, insertStaticContent: y } = e, S = (W, L, Q, de = null, ge = null, pe = null, Me = void 0, Ie = null, he = !!L.dynamicChildren) => {
    if (W === L) return;
    W && !Nc(W, L) && (de = F(W), be(W, ge, pe, !0), W = null), L.patchFlag === -2 && (he = !1, L.dynamicChildren = null);
    const { type: q, ref: ye, shapeFlag: oe } = L;
    switch (q) {
      case Ug:
        x(W, L, Q, de);
        break;
      case pi:
        I(W, L, Q, de);
        break;
      case Np:
        W == null && E(L, Q, de, Me);
        break;
      case He:
        H(W, L, Q, de, ge, pe, Me, Ie, he);
        break;
      default:
        oe & 1 ? p(W, L, Q, de, ge, pe, Me, Ie, he) : oe & 6 ? G(W, L, Q, de, ge, pe, Me, Ie, he) : (oe & 64 || oe & 128) && q.process(W, L, Q, de, ge, pe, Me, Ie, he, ie);
    }
    ye != null && ge ? Dd(ye, W && W.ref, pe, L || W, !L) : ye == null && W && W.ref != null && Dd(W.ref, null, pe, W, !0);
  }, x = (W, L, Q, de) => {
    if (W == null) r(L.el = l(L.children), Q, de);
    else {
      const ge = L.el = W.el;
      L.children !== W.children && u(ge, L.children);
    }
  }, I = (W, L, Q, de) => {
    W == null ? r(L.el = i(L.children || ""), Q, de) : L.el = W.el;
  }, E = (W, L, Q, de) => {
    [W.el, W.anchor] = y(W.children, L, Q, de, W.el, W.anchor);
  }, N = ({ el: W, anchor: L }, Q, de) => {
    let ge;
    for (; W && W !== L; )
      ge = m(W), r(W, Q, de), W = ge;
    r(L, Q, de);
  }, $ = ({ el: W, anchor: L }) => {
    let Q;
    for (; W && W !== L; )
      Q = m(W), s(W), W = Q;
    s(L);
  }, p = (W, L, Q, de, ge, pe, Me, Ie, he) => {
    if (L.type === "svg" ? Me = "svg" : L.type === "math" && (Me = "mathml"), W == null) w(L, Q, de, ge, pe, Me, Ie, he);
    else {
      const q = W.el && W.el._isVueCE ? W.el : null;
      try {
        q && q._beginPatch(), C(W, L, ge, pe, Me, Ie, he);
      } finally {
        q && q._endPatch();
      }
    }
  }, w = (W, L, Q, de, ge, pe, Me, Ie) => {
    let he, q;
    const { props: ye, shapeFlag: oe, transition: De, dirs: Je } = W;
    if (he = W.el = o(W.type, pe, ye && ye.is, ye), oe & 8 ? d(he, W.children) : oe & 16 && b(W.children, he, null, de, ge, Ry(W, pe), Me, Ie), Je && zi(W, null, de, "created"), _(he, W, W.scopeId, Me, de), ye) {
      for (const lt in ye) lt !== "value" && !Rd(lt) && a(he, lt, null, ye[lt], pe, de);
      "value" in ye && a(he, "value", null, ye.value, pe), (q = ye.onVnodeBeforeMount) && Sa(q, de, W);
    }
    Je && zi(W, null, de, "beforeMount");
    const st = U9(ge, De);
    st && De.beforeEnter(he), r(he, L, Q), ((q = ye && ye.onVnodeMounted) || st || Je) && Dr(() => {
      q && Sa(q, de, W), st && De.enter(he), Je && zi(W, null, de, "mounted");
    }, ge);
  }, _ = (W, L, Q, de, ge) => {
    if (Q && v(W, Q), de) for (let pe = 0; pe < de.length; pe++) v(W, de[pe]);
    if (ge) {
      let pe = ge.subTree;
      if (L === pe || Rk(pe.type) && (pe.ssContent === L || pe.ssFallback === L)) {
        const Me = ge.vnode;
        _(W, Me, Me.scopeId, Me.slotScopeIds, ge.parent);
      }
    }
  }, b = (W, L, Q, de, ge, pe, Me, Ie, he = 0) => {
    for (let q = he; q < W.length; q++) S(null, W[q] = Ie ? co(W[q]) : ka(W[q]), L, Q, de, ge, pe, Me, Ie);
  }, C = (W, L, Q, de, ge, pe, Me) => {
    const Ie = L.el = W.el;
    let { patchFlag: he, dynamicChildren: q, dirs: ye } = L;
    he |= W.patchFlag & 16;
    const oe = W.props || gn, De = L.props || gn;
    let Je;
    if (Q && Ki(Q, !1), (Je = De.onVnodeBeforeUpdate) && Sa(Je, Q, L, W), ye && zi(L, W, Q, "beforeUpdate"), Q && Ki(Q, !0), (oe.innerHTML && De.innerHTML == null || oe.textContent && De.textContent == null) && d(Ie, ""), q ? k(W.dynamicChildren, q, Ie, Q, de, Ry(L, ge), pe) : Me || V(W, L, Ie, null, Q, de, Ry(L, ge), pe, !1), he > 0) {
      if (he & 16) P(Ie, oe, De, Q, ge);
      else if (he & 2 && oe.class !== De.class && a(Ie, "class", null, De.class, ge), he & 4 && a(Ie, "style", oe.style, De.style, ge), he & 8) {
        const st = L.dynamicProps;
        for (let lt = 0; lt < st.length; lt++) {
          const ot = st[lt], pt = oe[ot], ht = De[ot];
          (ht !== pt || ot === "value") && a(Ie, ot, pt, ht, ge, Q);
        }
      }
      he & 1 && W.children !== L.children && d(Ie, L.children);
    } else !Me && q == null && P(Ie, oe, De, Q, ge);
    ((Je = De.onVnodeUpdated) || ye) && Dr(() => {
      Je && Sa(Je, Q, L, W), ye && zi(L, W, Q, "updated");
    }, de);
  }, k = (W, L, Q, de, ge, pe, Me) => {
    for (let Ie = 0; Ie < L.length; Ie++) {
      const he = W[Ie], q = L[Ie];
      S(he, q, he.el && (he.type === He || !Nc(he, q) || he.shapeFlag & 198) ? f(he.el) : Q, null, de, ge, pe, Me, !0);
    }
  }, P = (W, L, Q, de, ge) => {
    if (L !== Q) {
      if (L !== gn)
        for (const pe in L) !Rd(pe) && !(pe in Q) && a(W, pe, L[pe], null, ge, de);
      for (const pe in Q) {
        if (Rd(pe)) continue;
        const Me = Q[pe], Ie = L[pe];
        Me !== Ie && pe !== "value" && a(W, pe, Ie, Me, ge, de);
      }
      "value" in Q && a(W, "value", L.value, Q.value, ge);
    }
  }, H = (W, L, Q, de, ge, pe, Me, Ie, he) => {
    const q = L.el = W ? W.el : l(""), ye = L.anchor = W ? W.anchor : l("");
    let { patchFlag: oe, dynamicChildren: De, slotScopeIds: Je } = L;
    Je && (Ie = Ie ? Ie.concat(Je) : Je), W == null ? (r(q, Q, de), r(ye, Q, de), b(L.children || [], Q, ye, ge, pe, Me, Ie, he)) : oe > 0 && oe & 64 && De && W.dynamicChildren && W.dynamicChildren.length === De.length ? (k(W.dynamicChildren, De, Q, ge, pe, Me, Ie), (L.key != null || ge && L === ge.subTree) && E1(W, L, !0)) : V(W, L, Q, ye, ge, pe, Me, Ie, he);
  }, G = (W, L, Q, de, ge, pe, Me, Ie, he) => {
    L.slotScopeIds = Ie, W == null ? L.shapeFlag & 512 ? ge.ctx.activate(L, Q, de, Me, he) : ee(L, Q, de, ge, pe, Me, he) : J(W, L, he);
  }, ee = (W, L, Q, de, ge, pe, Me) => {
    const Ie = W.component = Y9(W, de, ge);
    if (pk(W) && (Ie.ctx.renderer = ie), j9(Ie, !1, Me), Ie.asyncDep) {
      if (ge && ge.registerDep(Ie, z, Me), !W.el) {
        const he = Ie.subTree = Wn(pi);
        I(null, he, L, Q), W.placeholder = he.el;
      }
    } else z(Ie, W, L, Q, ge, pe, Me);
  }, J = (W, L, Q) => {
    const de = L.component = W.component;
    if (M9(W, L, Q)) if (de.asyncDep && !de.asyncResolved) {
      R(de, L, Q);
      return;
    } else
      de.next = L, de.update();
    else
      L.el = W.el, de.vnode = L;
  }, z = (W, L, Q, de, ge, pe, Me) => {
    const Ie = () => {
      if (W.isMounted) {
        let { next: oe, bu: De, u: Je, parent: st, vnode: lt } = W;
        {
          const Lt = Mk(W);
          if (Lt) {
            oe && (oe.el = lt.el, R(W, oe, Me)), Lt.asyncDep.then(() => {
              Dr(() => {
                W.isUnmounted || q();
              }, ge);
            });
            return;
          }
        }
        let ot = oe, pt;
        Ki(W, !1), oe ? (oe.el = lt.el, R(W, oe, Me)) : oe = lt, De && Rp(De), (pt = oe.props && oe.props.onVnodeBeforeUpdate) && Sa(pt, st, oe, lt), Ki(W, !0);
        const ht = Py(W), It = W.subTree;
        W.subTree = ht, S(It, ht, f(It.el), F(It), W, ge, pe), oe.el = ht.el, ot === null && P9(W, ht.el), Je && Dr(Je, ge), (pt = oe.props && oe.props.onVnodeUpdated) && Dr(() => Sa(pt, st, oe, lt), ge);
      } else {
        let oe;
        const { el: De, props: Je } = L, { bm: st, m: lt, parent: ot, root: pt, type: ht } = W, It = Od(L);
        if (Ki(W, !1), st && Rp(st), !It && (oe = Je && Je.onVnodeBeforeMount) && Sa(oe, ot, L), Ki(W, !0), De && Se) {
          const Lt = () => {
            W.subTree = Py(W), Se(De, W.subTree, W, ge, null);
          };
          It && ht.__asyncHydrate ? ht.__asyncHydrate(De, W, Lt) : Lt();
        } else {
          pt.ce && pt.ce._hasShadowRoot() && pt.ce._injectChildStyle(ht, W.parent ? W.parent.type : void 0);
          const Lt = W.subTree = Py(W);
          S(null, Lt, Q, de, W, ge, pe), L.el = Lt.el;
        }
        if (lt && Dr(lt, ge), !It && (oe = Je && Je.onVnodeMounted)) {
          const Lt = L;
          Dr(() => Sa(oe, ot, Lt), ge);
        }
        (L.shapeFlag & 256 || ot && Od(ot.vnode) && ot.vnode.shapeFlag & 256) && W.a && Dr(W.a, ge), W.isMounted = !0, L = Q = de = null;
      }
    };
    W.scope.on();
    const he = W.effect = new Hx(Ie);
    W.scope.off();
    const q = W.update = he.run.bind(he), ye = W.job = he.runIfDirty.bind(he);
    ye.i = W, ye.id = W.uid, he.scheduler = () => _1(ye), Ki(W, !0), q();
  }, R = (W, L, Q) => {
    L.component = W;
    const de = W.vnode.props;
    W.vnode = L, W.next = null, N9(W, L.props, de, Q), O9(W, L.children, Q), Co(), ZA(W), Io();
  }, V = (W, L, Q, de, ge, pe, Me, Ie, he = !1) => {
    const q = W && W.children, ye = W ? W.shapeFlag : 0, oe = L.children, { patchFlag: De, shapeFlag: Je } = L;
    if (De > 0) {
      if (De & 128) {
        ve(q, oe, Q, de, ge, pe, Me, Ie, he);
        return;
      } else if (De & 256) {
        le(q, oe, Q, de, ge, pe, Me, Ie, he);
        return;
      }
    }
    Je & 8 ? (ye & 16 && Oe(q, ge, pe), oe !== q && d(Q, oe)) : ye & 16 ? Je & 16 ? ve(q, oe, Q, de, ge, pe, Me, Ie, he) : Oe(q, ge, pe, !0) : (ye & 8 && d(Q, ""), Je & 16 && b(oe, Q, de, ge, pe, Me, Ie, he));
  }, le = (W, L, Q, de, ge, pe, Me, Ie, he) => {
    W = W || Eu, L = L || Eu;
    const q = W.length, ye = L.length, oe = Math.min(q, ye);
    let De;
    for (De = 0; De < oe; De++) {
      const Je = L[De] = he ? co(L[De]) : ka(L[De]);
      S(W[De], Je, Q, null, ge, pe, Me, Ie, he);
    }
    q > ye ? Oe(W, ge, pe, !0, !1, oe) : b(L, Q, de, ge, pe, Me, Ie, he, oe);
  }, ve = (W, L, Q, de, ge, pe, Me, Ie, he) => {
    let q = 0;
    const ye = L.length;
    let oe = W.length - 1, De = ye - 1;
    for (; q <= oe && q <= De; ) {
      const Je = W[q], st = L[q] = he ? co(L[q]) : ka(L[q]);
      if (Nc(Je, st)) S(Je, st, Q, null, ge, pe, Me, Ie, he);
      else break;
      q++;
    }
    for (; q <= oe && q <= De; ) {
      const Je = W[oe], st = L[De] = he ? co(L[De]) : ka(L[De]);
      if (Nc(Je, st)) S(Je, st, Q, null, ge, pe, Me, Ie, he);
      else break;
      oe--, De--;
    }
    if (q > oe) {
      if (q <= De) {
        const Je = De + 1, st = Je < ye ? L[Je].el : de;
        for (; q <= De; )
          S(null, L[q] = he ? co(L[q]) : ka(L[q]), Q, st, ge, pe, Me, Ie, he), q++;
      }
    } else if (q > De) for (; q <= oe; )
      be(W[q], ge, pe, !0), q++;
    else {
      const Je = q, st = q, lt = /* @__PURE__ */ new Map();
      for (q = st; q <= De; q++) {
        const X = L[q] = he ? co(L[q]) : ka(L[q]);
        X.key != null && lt.set(X.key, q);
      }
      let ot, pt = 0;
      const ht = De - st + 1;
      let It = !1, Lt = 0;
      const en = new Array(ht);
      for (q = 0; q < ht; q++) en[q] = 0;
      for (q = Je; q <= oe; q++) {
        const X = W[q];
        if (pt >= ht) {
          be(X, ge, pe, !0);
          continue;
        }
        let se;
        if (X.key != null) se = lt.get(X.key);
        else for (ot = st; ot <= De; ot++) if (en[ot - st] === 0 && Nc(X, L[ot])) {
          se = ot;
          break;
        }
        se === void 0 ? be(X, ge, pe, !0) : (en[se - st] = q + 1, se >= Lt ? Lt = se : It = !0, S(X, L[se], Q, null, ge, pe, Me, Ie, he), pt++);
      }
      const at = It ? q9(en) : Eu;
      for (ot = at.length - 1, q = ht - 1; q >= 0; q--) {
        const X = st + q, se = L[X], Ae = L[X + 1], Re = X + 1 < ye ? Ae.el || Pk(Ae) : de;
        en[q] === 0 ? S(null, se, Q, Re, ge, pe, Me, Ie, he) : It && (ot < 0 || q !== at[ot] ? ke(se, Q, Re, 2) : ot--);
      }
    }
  }, ke = (W, L, Q, de, ge = null) => {
    const { el: pe, type: Me, transition: Ie, children: he, shapeFlag: q } = W;
    if (q & 6) {
      ke(W.component.subTree, L, Q, de);
      return;
    }
    if (q & 128) {
      W.suspense.move(L, Q, de);
      return;
    }
    if (q & 64) {
      Me.move(W, L, Q, ie);
      return;
    }
    if (Me === He) {
      r(pe, L, Q);
      for (let ye = 0; ye < he.length; ye++) ke(he[ye], L, Q, de);
      r(W.anchor, L, Q);
      return;
    }
    if (Me === Np) {
      N(W, L, Q);
      return;
    }
    if (de !== 2 && q & 1 && Ie) if (de === 0) Ie.persisted && !pe[ky] ? r(pe, L, Q) : (Ie.beforeEnter(pe), r(pe, L, Q), Dr(() => Ie.enter(pe), ge));
    else {
      const { leave: ye, delayLeave: oe, afterLeave: De } = Ie, Je = () => {
        W.ctx.isUnmounted ? s(pe) : r(pe, L, Q);
      }, st = () => {
        const lt = pe._isLeaving || !!pe[ky];
        pe._isLeaving && pe[ky](!0), Ie.persisted && !lt ? Je() : ye(pe, () => {
          Je(), De && De();
        });
      };
      oe ? oe(pe, Je, st) : st();
    }
    else r(pe, L, Q);
  }, be = (W, L, Q, de = !1, ge = !1) => {
    const { type: pe, props: Me, ref: Ie, children: he, dynamicChildren: q, shapeFlag: ye, patchFlag: oe, dirs: De, cacheIndex: Je, memo: st } = W;
    if (oe === -2 && (ge = !1), Ie != null && (Co(), Dd(Ie, null, Q, W, !0), Io()), Je != null && (L.renderCache[Je] = void 0), ye & 256) {
      L.ctx.deactivate(W);
      return;
    }
    const lt = ye & 1 && De, ot = !Od(W);
    let pt;
    if (ot && (pt = Me && Me.onVnodeBeforeUnmount) && Sa(pt, L, W), ye & 6) we(W.component, Q, de);
    else {
      if (ye & 128) {
        W.suspense.unmount(Q, de);
        return;
      }
      lt && zi(W, null, L, "beforeUnmount"), ye & 64 ? W.type.remove(W, L, Q, ie, de) : q && !q.hasOnce && (pe !== He || oe > 0 && oe & 64) ? Oe(q, L, Q, !1, !0) : (pe === He && oe & 384 || !ge && ye & 16) && Oe(he, L, Q), de && Ne(W);
    }
    const ht = st != null && Je == null;
    (ot && (pt = Me && Me.onVnodeUnmounted) || lt || ht) && Dr(() => {
      pt && Sa(pt, L, W), lt && zi(W, null, L, "unmounted"), ht && (W.el = null);
    }, Q);
  }, Ne = (W) => {
    const { type: L, el: Q, anchor: de, transition: ge } = W;
    if (L === He) {
      ce(Q, de);
      return;
    }
    if (L === Np) {
      $(W);
      return;
    }
    const pe = () => {
      s(Q), ge && !ge.persisted && ge.afterLeave && ge.afterLeave();
    };
    if (W.shapeFlag & 1 && ge && !ge.persisted) {
      const { leave: Me, delayLeave: Ie } = ge, he = () => Me(Q, pe);
      Ie ? Ie(W.el, pe, he) : he();
    } else pe();
  }, ce = (W, L) => {
    let Q;
    for (; W !== L; )
      Q = m(W), s(W), W = Q;
    s(L);
  }, we = (W, L, Q) => {
    const { bum: de, scope: ge, job: pe, subTree: Me, um: Ie, m: he, a: q } = W;
    c2(he), c2(q), de && Rp(de), ge.stop(), pe && (pe.flags |= 8, be(Me, W, L, Q)), Ie && Dr(Ie, L), Dr(() => {
      W.isUnmounted = !0;
    }, L);
  }, Oe = (W, L, Q, de = !1, ge = !1, pe = 0) => {
    for (let Me = pe; Me < W.length; Me++) be(W[Me], L, Q, de, ge);
  }, F = (W) => {
    if (W.shapeFlag & 6) return F(W.component.subTree);
    if (W.shapeFlag & 128) return W.suspense.next();
    const L = m(W.anchor || W.el), Q = L && L[dk];
    return Q ? m(Q) : L;
  };
  let ae = !1;
  const Z = (W, L, Q) => {
    let de;
    W == null ? L._vnode && (be(L._vnode, null, null, !0), de = L._vnode.component) : S(L._vnode || null, W, L, null, null, null, Q), L._vnode = W, ae || (ae = !0, ZA(de), ak(), ae = !1);
  }, ie = {
    p: S,
    um: be,
    m: ke,
    r: Ne,
    mt: ee,
    mc: b,
    pc: V,
    pbc: k,
    n: F,
    o: e
  };
  let me, Se;
  return t && ([me, Se] = t(ie)), {
    render: Z,
    hydrate: me,
    createApp: T9(Z, me)
  };
}
function Ry({ type: e, props: t }, n) {
  return n === "svg" && e === "foreignObject" || n === "mathml" && e === "annotation-xml" && t && t.encoding && t.encoding.includes("html") ? void 0 : n;
}
function Ki({ effect: e, job: t }, n) {
  n ? (e.flags |= 32, t.flags |= 4) : (e.flags &= -33, t.flags &= -5);
}
function U9(e, t) {
  return (!e || e && !e.pendingBranch) && t && !t.persisted;
}
function E1(e, t, n = !1) {
  const r = e.children, s = t.children;
  if (Et(r) && Et(s)) for (let a = 0; a < r.length; a++) {
    const o = r[a];
    let l = s[a];
    l.shapeFlag & 1 && !l.dynamicChildren && ((l.patchFlag <= 0 || l.patchFlag === 32) && (l = s[a] = co(s[a]), l.el = o.el), !n && l.patchFlag !== -2 && E1(o, l)), l.type === Ug && (l.patchFlag === -1 && (l = s[a] = co(l)), l.el = o.el), l.type === pi && !l.el && (l.el = o.el);
  }
}
function q9(e) {
  const t = e.slice(), n = [0];
  let r, s, a, o, l;
  const i = e.length;
  for (r = 0; r < i; r++) {
    const u = e[r];
    if (u !== 0) {
      if (s = n[n.length - 1], e[s] < u) {
        t[r] = s, n.push(r);
        continue;
      }
      for (a = 0, o = n.length - 1; a < o; )
        l = a + o >> 1, e[n[l]] < u ? a = l + 1 : o = l;
      u < e[n[a]] && (a > 0 && (t[r] = n[a - 1]), n[a] = r);
    }
  }
  for (a = n.length, o = n[a - 1]; a-- > 0; )
    n[a] = o, o = t[o];
  return n;
}
function Mk(e) {
  const t = e.subTree.component;
  if (t) return t.asyncDep && !t.asyncResolved ? t : Mk(t);
}
function c2(e) {
  if (e) for (let t = 0; t < e.length; t++) e[t].flags |= 8;
}
function Pk(e) {
  if (e.placeholder) return e.placeholder;
  const t = e.component;
  return t ? Pk(t.subTree) : null;
}
var Rk = (e) => e.__isSuspense;
function H9(e, t) {
  t && t.pendingBranch ? Et(e) ? t.effects.push(...e) : t.effects.push(e) : Z7(e);
}
var He = /* @__PURE__ */ Symbol.for("v-fgt"), Ug = /* @__PURE__ */ Symbol.for("v-txt"), pi = /* @__PURE__ */ Symbol.for("v-cmt"), Np = /* @__PURE__ */ Symbol.for("v-stc"), Bd = [], bs = null;
function O(e = !1) {
  Bd.push(bs = e ? null : []);
}
function z9() {
  Bd.pop(), bs = Bd[Bd.length - 1] || null;
}
var of = 1;
function d2(e, t = !1) {
  of += e, e < 0 && bs && t && (bs.hasOnce = !0);
}
function Nk(e) {
  return e.dynamicChildren = of > 0 ? bs || Eu : null, z9(), of > 0 && bs && bs.push(e), e;
}
function B(e, t, n, r, s, a) {
  return Nk(g(e, t, n, r, s, a, !0));
}
function cn(e, t, n, r, s) {
  return Nk(Wn(e, t, n, r, s, !0));
}
function $k(e) {
  return e ? e.__v_isVNode === !0 : !1;
}
function Nc(e, t) {
  return e.type === t.type && e.key === t.key;
}
var Lk = ({ key: e }) => e ?? null, $p = ({ ref: e, ref_key: t, ref_for: n }) => (typeof e == "number" && (e = "" + e), e != null ? xn(e) || /* @__PURE__ */ kn(e) || $t(e) ? {
  i: Os,
  r: e,
  k: t,
  f: !!n
} : e : null);
function g(e, t = null, n = null, r = 0, s = null, a = e === He ? 0 : 1, o = !1, l = !1) {
  const i = {
    __v_isVNode: !0,
    __v_skip: !0,
    type: e,
    props: t,
    key: t && Lk(t),
    ref: t && $p(t),
    scopeId: ik,
    slotScopeIds: null,
    children: n,
    component: null,
    suspense: null,
    ssContent: null,
    ssFallback: null,
    dirs: null,
    transition: null,
    el: null,
    anchor: null,
    target: null,
    targetStart: null,
    targetAnchor: null,
    staticCount: 0,
    shapeFlag: a,
    patchFlag: r,
    dynamicProps: s,
    dynamicChildren: null,
    appContext: null,
    ctx: Os
  };
  return l ? (C1(i, n), a & 128 && e.normalize(i)) : n && (i.shapeFlag |= xn(n) ? 8 : 16), of > 0 && !o && bs && (i.patchFlag > 0 || a & 6) && i.patchFlag !== 32 && bs.push(i), i;
}
var Wn = K9;
function K9(e, t = null, n = null, r = 0, s = null, a = !1) {
  if ((!e || e === g9) && (e = pi), $k(e)) {
    const l = qu(e, t, !0);
    return n && C1(l, n), of > 0 && !a && bs && (l.shapeFlag & 6 ? bs[bs.indexOf(e)] = l : bs.push(l)), l.patchFlag = -2, l;
  }
  if (tL(e) && (e = e.__vccOpts), t) {
    t = W9(t);
    let { class: l, style: i } = t;
    l && !xn(l) && (t.class = Fe(l)), pn(i) && (/* @__PURE__ */ y1(i) && !Et(i) && (i = tr({}, i)), t.style = Nn(i));
  }
  const o = xn(e) ? 1 : Rk(e) ? 128 : s9(e) ? 64 : pn(e) ? 4 : $t(e) ? 2 : 0;
  return g(e, t, n, r, s, o, a, !0);
}
function W9(e) {
  return e ? /* @__PURE__ */ y1(e) || Tk(e) ? tr({}, e) : e : null;
}
function qu(e, t, n = !1, r = !1) {
  const { props: s, ref: a, patchFlag: o, children: l, transition: i } = e, u = t ? G9(s || {}, t) : s, d = {
    __v_isVNode: !0,
    __v_skip: !0,
    type: e.type,
    props: u,
    key: u && Lk(u),
    ref: t && t.ref ? n && a ? Et(a) ? a.concat($p(t)) : [a, $p(t)] : $p(t) : a,
    scopeId: e.scopeId,
    slotScopeIds: e.slotScopeIds,
    children: l,
    target: e.target,
    targetStart: e.targetStart,
    targetAnchor: e.targetAnchor,
    staticCount: e.staticCount,
    shapeFlag: e.shapeFlag,
    patchFlag: t && e.type !== He ? o === -1 ? 16 : o | 16 : o,
    dynamicProps: e.dynamicProps,
    dynamicChildren: e.dynamicChildren,
    appContext: e.appContext,
    dirs: e.dirs,
    transition: i,
    component: e.component,
    suspense: e.suspense,
    ssContent: e.ssContent && qu(e.ssContent),
    ssFallback: e.ssFallback && qu(e.ssFallback),
    placeholder: e.placeholder,
    el: e.el,
    anchor: e.anchor,
    ctx: e.ctx,
    ce: e.ce
  };
  return i && r && b1(d, i.clone(d)), d;
}
function ku(e = " ", t = 0) {
  return Wn(Ug, null, e, t);
}
function Al(e, t) {
  const n = Wn(Np, null, e);
  return n.staticCount = t, n;
}
function Te(e = "", t = !1) {
  return t ? (O(), cn(pi, null, e)) : Wn(pi, null, e);
}
function ka(e) {
  return e == null || typeof e == "boolean" ? Wn(pi) : Et(e) ? Wn(He, null, e.slice()) : $k(e) ? co(e) : Wn(Ug, null, String(e));
}
function co(e) {
  return e.el === null && e.patchFlag !== -1 || e.memo ? e : qu(e);
}
function C1(e, t) {
  let n = 0;
  const { shapeFlag: r } = e;
  if (t == null) t = null;
  else if (Et(t)) n = 16;
  else if (typeof t == "object") if (r & 65) {
    const s = t.default;
    s && (s._c && (s._d = !1), C1(e, s()), s._c && (s._d = !0));
    return;
  } else {
    n = 32;
    const s = t._;
    !s && !Tk(t) ? t._ctx = Os : s === 3 && Os && (Os.slots._ === 1 ? t._ = 1 : (t._ = 2, e.patchFlag |= 1024));
  }
  else $t(t) ? (t = {
    default: t,
    _ctx: Os
  }, n = 32) : (t = String(t), r & 64 ? (n = 16, t = [ku(t)]) : n = 8);
  e.children = t, e.shapeFlag |= n;
}
function G9(...e) {
  const t = {};
  for (let n = 0; n < e.length; n++) {
    const r = e[n];
    for (const s in r) if (s === "class")
      t.class !== r.class && (t.class = Fe([t.class, r.class]));
    else if (s === "style") t.style = Nn([t.style, r.style]);
    else if (Mg(s)) {
      const a = t[s], o = r[s];
      o && a !== o && !(Et(a) && a.includes(o)) ? t[s] = a ? [].concat(a, o) : o : o == null && a == null && !Pg(s) && (t[s] = o);
    } else s !== "" && (t[s] = r[s]);
  }
  return t;
}
function Sa(e, t, n, r = null) {
  la(e, t, 7, [n, r]);
}
var V9 = _k(), J9 = 0;
function Y9(e, t, n) {
  const r = e.type, s = (t ? t.appContext : e.appContext) || V9, a = {
    uid: J9++,
    vnode: e,
    type: r,
    parent: t,
    appContext: s,
    root: null,
    next: null,
    subTree: null,
    effect: null,
    update: null,
    job: null,
    scope: new A7(!0),
    render: null,
    proxy: null,
    exposed: null,
    exposeProxy: null,
    withProxy: null,
    provides: t ? t.provides : Object.create(s.provides),
    ids: t ? t.ids : [
      "",
      0,
      0
    ],
    accessCache: null,
    renderCache: [],
    components: null,
    directives: null,
    propsOptions: Ck(r, s),
    emitsOptions: bk(r, s),
    emit: null,
    emitted: null,
    propsDefaults: gn,
    inheritAttrs: r.inheritAttrs,
    ctx: gn,
    data: gn,
    props: gn,
    attrs: gn,
    slots: gn,
    refs: gn,
    setupState: gn,
    setupContext: null,
    suspense: n,
    suspenseId: n ? n.pendingId : 0,
    asyncDep: null,
    asyncResolved: !1,
    isMounted: !1,
    isUnmounted: !1,
    isDeactivated: !1,
    bc: null,
    c: null,
    bm: null,
    m: null,
    bu: null,
    u: null,
    um: null,
    bum: null,
    da: null,
    a: null,
    rtg: null,
    rtc: null,
    ec: null,
    sp: null
  };
  return a.ctx = { _: a }, a.root = t ? t.root : a, a.emit = C9.bind(null, a), e.ce && e.ce(a), a;
}
var qr = null, X9 = () => qr || Os, ym, eb;
{
  const e = $g(), t = (n, r) => {
    let s;
    return (s = e[n]) || (s = e[n] = []), s.push(r), (a) => {
      s.length > 1 ? s.forEach((o) => o(a)) : s[0](a);
    };
  };
  ym = t("__VUE_INSTANCE_SETTERS__", (n) => qr = n), eb = t("__VUE_SSR_SETTERS__", (n) => lf = n);
}
var $f = (e) => {
  const t = qr;
  return ym(e), e.scope.on(), () => {
    e.scope.off(), ym(t);
  };
}, f2 = () => {
  qr && qr.scope.off(), ym(null);
};
function Dk(e) {
  return e.vnode.shapeFlag & 4;
}
var lf = !1;
function j9(e, t = !1, n = !1) {
  t && eb(t);
  const { props: r, children: s } = e.vnode, a = Dk(e);
  R9(e, r, a, t), D9(e, s, n || t);
  const o = a ? Q9(e, t) : void 0;
  return t && eb(!1), o;
}
function Q9(e, t) {
  const n = e.type;
  e.accessCache = /* @__PURE__ */ Object.create(null), e.proxy = new Proxy(e.ctx, v9);
  const { setup: r } = n;
  if (r) {
    Co();
    const s = e.setupContext = r.length > 1 ? eL(e) : null, a = $f(e), o = Nf(r, e, 0, [e.props, s]), l = Nx(o);
    if (Io(), a(), (l || e.sp) && !Od(e) && hk(e), l) {
      if (o.then(f2, f2), t) return o.then((i) => {
        h2(e, i, t);
      }).catch((i) => {
        Dg(i, e, 0);
      });
      e.asyncDep = o;
    } else h2(e, o, t);
  } else Ok(e, t);
}
function h2(e, t, n) {
  $t(t) ? e.type.__ssrInlineRender ? e.ssrRender = t : e.render = t : pn(t) && (e.setupState = nk(t)), Ok(e, n);
}
var p2, m2;
function Ok(e, t, n) {
  const r = e.type;
  if (!e.render) {
    if (!t && p2 && !r.render) {
      const s = r.template || S1(e).template;
      if (s) {
        const { isCustomElement: a, compilerOptions: o } = e.appContext.config, { delimiters: l, compilerOptions: i } = r, u = tr(tr({
          isCustomElement: a,
          delimiters: l
        }, o), i);
        r.render = p2(s, u);
      }
    }
    e.render = r.render || La, m2 && m2(e);
  }
  {
    const s = $f(e);
    Co();
    try {
      y9(e);
    } finally {
      Io(), s();
    }
  }
}
var Z9 = { get(e, t) {
  return Er(e, "get", ""), e[t];
} };
function eL(e) {
  const t = (n) => {
    e.exposed = n || {};
  };
  return {
    attrs: new Proxy(e.attrs, Z9),
    slots: e.slots,
    emit: e.emit,
    expose: t
  };
}
function qg(e) {
  return e.exposed ? e.exposeProxy || (e.exposeProxy = new Proxy(nk(z7(e.exposed)), {
    get(t, n) {
      if (n in t) return t[n];
      if (n in Fd) return Fd[n](e);
    },
    has(t, n) {
      return n in t || n in Fd;
    }
  })) : e.proxy;
}
function tL(e) {
  return $t(e) && "__vccOpts" in e;
}
var ne = (e, t) => /* @__PURE__ */ J7(e, t, lf), nL = "3.5.35", tb = void 0, g2 = typeof window < "u" && window.trustedTypes;
if (g2) try {
  tb = /* @__PURE__ */ g2.createPolicy("vue", { createHTML: (e) => e });
} catch {
}
var Fk = tb ? (e) => tb.createHTML(e) : (e) => e, rL = "http://www.w3.org/2000/svg", sL = "http://www.w3.org/1998/Math/MathML", io = typeof document < "u" ? document : null, v2 = io && /* @__PURE__ */ io.createElement("template"), aL = {
  insert: (e, t, n) => {
    t.insertBefore(e, n || null);
  },
  remove: (e) => {
    const t = e.parentNode;
    t && t.removeChild(e);
  },
  createElement: (e, t, n, r) => {
    const s = t === "svg" ? io.createElementNS(rL, e) : t === "mathml" ? io.createElementNS(sL, e) : n ? io.createElement(e, { is: n }) : io.createElement(e);
    return e === "select" && r && r.multiple != null && s.setAttribute("multiple", r.multiple), s;
  },
  createText: (e) => io.createTextNode(e),
  createComment: (e) => io.createComment(e),
  setText: (e, t) => {
    e.nodeValue = t;
  },
  setElementText: (e, t) => {
    e.textContent = t;
  },
  parentNode: (e) => e.parentNode,
  nextSibling: (e) => e.nextSibling,
  querySelector: (e) => io.querySelector(e),
  setScopeId(e, t) {
    e.setAttribute(t, "");
  },
  insertStaticContent(e, t, n, r, s, a) {
    const o = n ? n.previousSibling : t.lastChild;
    if (s && (s === a || s.nextSibling)) for (; t.insertBefore(s.cloneNode(!0), n), !(s === a || !(s = s.nextSibling)); )
      ;
    else {
      v2.innerHTML = Fk(r === "svg" ? `<svg>${e}</svg>` : r === "mathml" ? `<math>${e}</math>` : e);
      const l = v2.content;
      if (r === "svg" || r === "mathml") {
        const i = l.firstChild;
        for (; i.firstChild; ) l.appendChild(i.firstChild);
        l.removeChild(i);
      }
      t.insertBefore(l, n);
    }
    return [o ? o.nextSibling : t.firstChild, n ? n.previousSibling : t.lastChild];
  }
}, oL = /* @__PURE__ */ Symbol("_vtc");
function iL(e, t, n) {
  const r = e[oL];
  r && (t = (t ? [t, ...r] : [...r]).join(" ")), t == null ? e.removeAttribute("class") : n ? e.setAttribute("class", t) : e.className = t;
}
var _m = /* @__PURE__ */ Symbol("_vod"), Bk = /* @__PURE__ */ Symbol("_vsh"), rc = {
  name: "show",
  beforeMount(e, { value: t }, { transition: n }) {
    e[_m] = e.style.display === "none" ? "" : e.style.display, n && t ? n.beforeEnter(e) : $c(e, t);
  },
  mounted(e, { value: t }, { transition: n }) {
    n && t && n.enter(e);
  },
  updated(e, { value: t, oldValue: n }, { transition: r }) {
    !t != !n && (r ? t ? (r.beforeEnter(e), $c(e, !0), r.enter(e)) : r.leave(e, () => {
      $c(e, !1);
    }) : $c(e, t));
  },
  beforeUnmount(e, { value: t }) {
    $c(e, t);
  }
};
function $c(e, t) {
  e.style.display = t ? e[_m] : "none", e[Bk] = !t;
}
var lL = /* @__PURE__ */ Symbol(""), uL = /(?:^|;)\s*display\s*:/;
function cL(e, t, n) {
  const r = e.style, s = xn(n);
  let a = !1;
  if (n && !s) {
    if (t) if (xn(t))
      for (const o of t.split(";")) {
        const l = o.slice(0, o.indexOf(":")).trim();
        n[l] == null && cd(r, l, "");
      }
    else for (const o in t) n[o] == null && cd(r, o, "");
    for (const o in n) {
      o === "display" && (a = !0);
      const l = n[o];
      l != null ? fL(e, o, !xn(t) && t ? t[o] : void 0, l) || cd(r, o, l) : cd(r, o, "");
    }
  } else if (s) {
    if (t !== n) {
      const o = r[lL];
      o && (n += ";" + o), r.cssText = n, a = uL.test(n);
    }
  } else t && e.removeAttribute("style");
  _m in e && (e[_m] = a ? r.display : "", e[Bk] && (r.display = "none"));
}
var y2 = /\s*!important$/;
function cd(e, t, n) {
  if (Et(n)) n.forEach((r) => cd(e, t, r));
  else if (n == null && (n = ""), t.startsWith("--")) e.setProperty(t, n);
  else {
    const r = dL(e, t);
    y2.test(n) ? e.setProperty(Ai(r), n.replace(y2, ""), "important") : e[r] = n;
  }
}
var _2 = [
  "Webkit",
  "Moz",
  "ms"
], Ny = {};
function dL(e, t) {
  const n = Ny[t];
  if (n) return n;
  let r = na(t);
  if (r !== "filter" && r in e) return Ny[t] = r;
  r = Dx(r);
  for (let s = 0; s < _2.length; s++) {
    const a = _2[s] + r;
    if (a in e) return Ny[t] = a;
  }
  return t;
}
function fL(e, t, n, r) {
  return e.tagName === "TEXTAREA" && (t === "width" || t === "height") && xn(r) && n === r;
}
var b2 = "http://www.w3.org/1999/xlink";
function w2(e, t, n, r, s, a = w7(t)) {
  r && t.startsWith("xlink:") ? n == null ? e.removeAttributeNS(b2, t.slice(6, t.length)) : e.setAttributeNS(b2, t, n) : n == null || a && !Bx(n) ? e.removeAttribute(t) : e.setAttribute(t, a ? "" : Ba(n) ? String(n) : n);
}
function S2(e, t, n, r, s) {
  if (t === "innerHTML" || t === "textContent") {
    n != null && (e[t] = t === "innerHTML" ? Fk(n) : n);
    return;
  }
  const a = e.tagName;
  if (t === "value" && a !== "PROGRESS" && !a.includes("-")) {
    const l = a === "OPTION" ? e.getAttribute("value") || "" : e.value, i = n == null ? e.type === "checkbox" ? "on" : "" : String(n);
    (l !== i || !("_value" in e)) && (e.value = i), n == null && e.removeAttribute(t), e._value = n;
    return;
  }
  let o = !1;
  if (n === "" || n == null) {
    const l = typeof e[t];
    l === "boolean" ? n = Bx(n) : n == null && l === "string" ? (n = "", o = !0) : l === "number" && (n = 0, o = !0);
  }
  try {
    e[t] = n;
  } catch {
  }
  o && e.removeAttribute(s || t);
}
function ei(e, t, n, r) {
  e.addEventListener(t, n, r);
}
function hL(e, t, n, r) {
  e.removeEventListener(t, n, r);
}
var A2 = /* @__PURE__ */ Symbol("_vei");
function pL(e, t, n, r, s = null) {
  const a = e[A2] || (e[A2] = {}), o = a[t];
  if (r && o) o.value = r;
  else {
    const [l, i] = mL(t);
    r ? ei(e, l, a[t] = yL(r, s), i) : o && (hL(e, l, o, i), a[t] = void 0);
  }
}
var T2 = /(?:Once|Passive|Capture)$/;
function mL(e) {
  let t;
  if (T2.test(e)) {
    t = {};
    let n;
    for (; n = e.match(T2); )
      e = e.slice(0, e.length - n[0].length), t[n[0].toLowerCase()] = !0;
  }
  return [e[2] === ":" ? e.slice(3) : Ai(e.slice(2)), t];
}
var $y = 0, gL = /* @__PURE__ */ Promise.resolve(), vL = () => $y || (gL.then(() => $y = 0), $y = Date.now());
function yL(e, t) {
  const n = (r) => {
    if (!r._vts) r._vts = Date.now();
    else if (r._vts <= n.attached) return;
    const s = n.value;
    if (Et(s)) {
      const a = r.stopImmediatePropagation;
      r.stopImmediatePropagation = () => {
        a.call(r), r._stopped = !0;
      };
      const o = s.slice(), l = [r];
      for (let i = 0; i < o.length && !r._stopped; i++) {
        const u = o[i];
        u && la(u, t, 5, l);
      }
    } else la(s, t, 5, [r]);
  };
  return n.value = e, n.attached = vL(), n;
}
var E2 = (e) => e.charCodeAt(0) === 111 && e.charCodeAt(1) === 110 && e.charCodeAt(2) > 96 && e.charCodeAt(2) < 123, _L = (e, t, n, r, s, a) => {
  const o = s === "svg";
  t === "class" ? iL(e, r, o) : t === "style" ? cL(e, n, r) : Mg(t) ? Pg(t) || pL(e, t, n, r, a) : (t[0] === "." ? (t = t.slice(1), !0) : t[0] === "^" ? (t = t.slice(1), !1) : bL(e, t, r, o)) ? (S2(e, t, r), !e.tagName.includes("-") && (t === "value" || t === "checked" || t === "selected") && w2(e, t, r, o, a, t !== "value")) : e._isVueCE && (wL(e, t) || e._def.__asyncLoader && (/[A-Z]/.test(t) || !xn(r))) ? S2(e, na(t), r, a, t) : (t === "true-value" ? e._trueValue = r : t === "false-value" && (e._falseValue = r), w2(e, t, r, o));
};
function bL(e, t, n, r) {
  if (r)
    return !!(t === "innerHTML" || t === "textContent" || t in e && E2(t) && $t(n));
  if (t === "spellcheck" || t === "draggable" || t === "translate" || t === "autocorrect" || t === "sandbox" && e.tagName === "IFRAME" || t === "form" || t === "list" && e.tagName === "INPUT" || t === "type" && e.tagName === "TEXTAREA") return !1;
  if (t === "width" || t === "height") {
    const s = e.tagName;
    if (s === "IMG" || s === "VIDEO" || s === "CANVAS" || s === "SOURCE") return !1;
  }
  return E2(t) && xn(n) ? !1 : t in e;
}
function wL(e, t) {
  const n = e._def.props;
  if (!n) return !1;
  const r = na(t);
  return Array.isArray(n) ? n.some((s) => na(s) === r) : Object.keys(n).some((s) => na(s) === r);
}
var Hu = (e) => {
  const t = e.props["onUpdate:modelValue"] || !1;
  return Et(t) ? (n) => Rp(t, n) : t;
};
function SL(e) {
  e.target.composing = !0;
}
function C2(e) {
  const t = e.target;
  t.composing && (t.composing = !1, t.dispatchEvent(new Event("input")));
}
var wo = /* @__PURE__ */ Symbol("_assign");
function I2(e, t, n) {
  return t && (e = e.trim()), n && (e = Ng(e)), e;
}
var sa = {
  created(e, { modifiers: { lazy: t, trim: n, number: r } }, s) {
    e[wo] = Hu(s);
    const a = r || s.props && s.props.type === "number";
    ei(e, t ? "change" : "input", (o) => {
      o.target.composing || e[wo](I2(e.value, n, a));
    }), (n || a) && ei(e, "change", () => {
      e.value = I2(e.value, n, a);
    }), t || (ei(e, "compositionstart", SL), ei(e, "compositionend", C2), ei(e, "change", C2));
  },
  mounted(e, { value: t }) {
    e.value = t ?? "";
  },
  beforeUpdate(e, { value: t, oldValue: n, modifiers: { lazy: r, trim: s, number: a } }, o) {
    if (e[wo] = Hu(o), e.composing) return;
    const l = (a || e.type === "number") && !/^0\d/.test(e.value) ? Ng(e.value) : e.value, i = t ?? "";
    if (l === i) return;
    const u = e.getRootNode();
    (u instanceof Document || u instanceof ShadowRoot) && u.activeElement === e && e.type !== "range" && (r && t === n || s && e.value.trim() === i) || (e.value = i);
  }
}, AL = {
  deep: !0,
  created(e, t, n) {
    e[wo] = Hu(n), ei(e, "change", () => {
      const r = e._modelValue, s = uf(e), a = e.checked, o = e[wo];
      if (Et(r)) {
        const l = c1(r, s), i = l !== -1;
        if (a && !i) o(r.concat(s));
        else if (!a && i) {
          const u = [...r];
          u.splice(l, 1), o(u);
        }
      } else if (tc(r)) {
        const l = new Set(r);
        a ? l.add(s) : l.delete(s), o(l);
      } else o(Uk(e, a));
    });
  },
  mounted: x2,
  beforeUpdate(e, t, n) {
    e[wo] = Hu(n), x2(e, t, n);
  }
};
function x2(e, { value: t, oldValue: n }, r) {
  e._modelValue = t;
  let s;
  if (Et(t)) s = c1(t, r.props.value) > -1;
  else if (tc(t)) s = t.has(r.props.value);
  else {
    if (t === n) return;
    s = nc(t, Uk(e, !0));
  }
  e.checked !== s && (e.checked = s);
}
var TL = {
  deep: !0,
  created(e, { value: t, modifiers: { number: n } }, r) {
    const s = tc(t);
    ei(e, "change", () => {
      const a = Array.prototype.filter.call(e.options, (o) => o.selected).map((o) => n ? Ng(uf(o)) : uf(o));
      e[wo](e.multiple ? s ? new Set(a) : a : a[0]), e._assigning = !0, Ut(() => {
        e._assigning = !1;
      });
    }), e[wo] = Hu(r);
  },
  mounted(e, { value: t }) {
    k2(e, t);
  },
  beforeUpdate(e, t, n) {
    e[wo] = Hu(n);
  },
  updated(e, { value: t }) {
    e._assigning || k2(e, t);
  }
};
function k2(e, t) {
  const n = e.multiple, r = Et(t);
  if (!(n && !r && !tc(t))) {
    for (let s = 0, a = e.options.length; s < a; s++) {
      const o = e.options[s], l = uf(o);
      if (n) if (r) {
        const i = typeof l;
        i === "string" || i === "number" ? o.selected = t.some((u) => String(u) === String(l)) : o.selected = c1(t, l) > -1;
      } else o.selected = t.has(l);
      else if (nc(uf(o), t)) {
        e.selectedIndex !== s && (e.selectedIndex = s);
        return;
      }
    }
    !n && e.selectedIndex !== -1 && (e.selectedIndex = -1);
  }
}
function uf(e) {
  return "_value" in e ? e._value : e.value;
}
function Uk(e, t) {
  const n = t ? "_trueValue" : "_falseValue";
  return n in e ? e[n] : t;
}
var EL = [
  "ctrl",
  "shift",
  "alt",
  "meta"
], CL = {
  stop: (e) => e.stopPropagation(),
  prevent: (e) => e.preventDefault(),
  self: (e) => e.target !== e.currentTarget,
  ctrl: (e) => !e.ctrlKey,
  shift: (e) => !e.shiftKey,
  alt: (e) => !e.altKey,
  meta: (e) => !e.metaKey,
  left: (e) => "button" in e && e.button !== 0,
  middle: (e) => "button" in e && e.button !== 1,
  right: (e) => "button" in e && e.button !== 2,
  exact: (e, t) => EL.some((n) => e[`${n}Key`] && !t.includes(n))
}, Kt = (e, t) => {
  if (!e) return e;
  const n = e._withMods || (e._withMods = {}), r = t.join(".");
  return n[r] || (n[r] = ((s, ...a) => {
    for (let o = 0; o < t.length; o++) {
      const l = CL[t[o]];
      if (l && l(s, t)) return;
    }
    return e(s, ...a);
  }));
}, IL = {
  esc: "escape",
  space: " ",
  up: "arrow-up",
  left: "arrow-left",
  right: "arrow-right",
  down: "arrow-down",
  delete: "backspace"
}, Qr = (e, t) => {
  const n = e._withKeys || (e._withKeys = {}), r = t.join(".");
  return n[r] || (n[r] = ((s) => {
    if (!("key" in s)) return;
    const a = Ai(s.key);
    if (t.some((o) => o === a || IL[o] === a)) return e(s);
  }));
}, xL = /* @__PURE__ */ tr({ patchProp: _L }, aL), M2;
function kL() {
  return M2 || (M2 = F9(xL));
}
var ML = ((...e) => {
  const t = kL().createApp(...e), { mount: n } = t;
  return t.mount = (r) => {
    const s = RL(r);
    if (!s) return;
    const a = t._component;
    !$t(a) && !a.render && !a.template && (a.template = s.innerHTML), s.nodeType === 1 && (s.textContent = "");
    const o = n(s, !1, PL(s));
    return s instanceof Element && (s.removeAttribute("v-cloak"), s.setAttribute("data-v-app", "")), o;
  }, t;
});
function PL(e) {
  if (e instanceof SVGElement) return "svg";
  if (typeof MathMLElement == "function" && e instanceof MathMLElement) return "mathml";
}
function RL(e) {
  return xn(e) ? document.querySelector(e) : e;
}
function NL(e, t) {
  const n = Number(e);
  return Number.isFinite(n) && n > 0 ? Math.floor(n) : t;
}
function mo(e, t = 5) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.min(20, Math.max(1, Math.floor(n))) : Math.min(20, Math.max(1, Math.floor(t)));
}
function I1(e, t = 20) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.min(50, Math.max(5, Math.round(n / 5) * 5)) : Math.min(50, Math.max(5, Math.round(t / 5) * 5));
}
function qk(e = {}, t = 0, n = {}) {
  const r = Math.max(0, Number(t) || 0), s = mo(n.defaultLimit, 5), a = NL(e.uiMessageWindowLimit, s), o = Math.min(Math.max(s, a), Math.max(r, s));
  e.uiMessageWindowLimit = o;
  const l = Math.max(0, r - o);
  return {
    startIndex: l,
    hiddenBefore: l,
    visibleCount: r - l,
    limit: o,
    total: r
  };
}
function $L(e = {}, t = 0, n = {}) {
  const r = Math.max(0, Number(t) || 0), s = qk(e, r, n);
  if (!s.hiddenBefore) return !1;
  const a = I1(n.chunk, 20);
  return e.uiMessageWindowLimit = Math.min(r, s.limit + a), !0;
}
function LL(e = {}, t = {}) {
  e.uiMessageWindowLimit = mo(t.defaultLimit, 5);
}
var DL = [
  "small",
  "medium",
  "large"
];
function OL(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function Hk(e, t, n, r) {
  const s = Number(e);
  return Number.isFinite(s) ? Math.min(r, Math.max(n, Math.floor(s))) : t;
}
function FL(e, t = 20) {
  const n = Hk(e, t, 5, 50);
  return Math.min(50, Math.max(5, Math.round(n / 5) * 5));
}
function BL(e) {
  return e === "medium" || e === "large" ? e : "small";
}
function yu(e = {}) {
  const t = OL(e);
  return {
    hiddenOutsideCount: Hk(t.hiddenOutsideCount, 5, 1, 20),
    loadBatchSize: FL(t.loadBatchSize, 20),
    chatFontSize: BL(t.chatFontSize)
  };
}
var UL = /* @__PURE__ */ Pf(((e, t) => {
  (function() {
    function n(p) {
      "use strict";
      var w = {
        omitExtraWLInCodeBlocks: {
          defaultValue: !1,
          describe: "Omit the default extra whiteline added to code blocks",
          type: "boolean"
        },
        noHeaderId: {
          defaultValue: !1,
          describe: "Turn on/off generated header id",
          type: "boolean"
        },
        prefixHeaderId: {
          defaultValue: !1,
          describe: "Add a prefix to the generated header ids. Passing a string will prefix that string to the header id. Setting to true will add a generic 'section-' prefix",
          type: "string"
        },
        rawPrefixHeaderId: {
          defaultValue: !1,
          describe: 'Setting this option to true will prevent showdown from modifying the prefix. This might result in malformed IDs (if, for instance, the " char is used in the prefix)',
          type: "boolean"
        },
        ghCompatibleHeaderId: {
          defaultValue: !1,
          describe: "Generate header ids compatible with github style (spaces are replaced with dashes, a bunch of non alphanumeric chars are removed)",
          type: "boolean"
        },
        rawHeaderId: {
          defaultValue: !1,
          describe: `Remove only spaces, ' and " from generated header ids (including prefixes), replacing them with dashes (-). WARNING: This might result in malformed ids`,
          type: "boolean"
        },
        headerLevelStart: {
          defaultValue: !1,
          describe: "The header blocks level start",
          type: "integer"
        },
        parseImgDimensions: {
          defaultValue: !1,
          describe: "Turn on/off image dimension parsing",
          type: "boolean"
        },
        simplifiedAutoLink: {
          defaultValue: !1,
          describe: "Turn on/off GFM autolink style",
          type: "boolean"
        },
        excludeTrailingPunctuationFromURLs: {
          defaultValue: !1,
          describe: "Excludes trailing punctuation from links generated with autoLinking",
          type: "boolean"
        },
        literalMidWordUnderscores: {
          defaultValue: !1,
          describe: "Parse midword underscores as literal underscores",
          type: "boolean"
        },
        literalMidWordAsterisks: {
          defaultValue: !1,
          describe: "Parse midword asterisks as literal asterisks",
          type: "boolean"
        },
        strikethrough: {
          defaultValue: !1,
          describe: "Turn on/off strikethrough support",
          type: "boolean"
        },
        tables: {
          defaultValue: !1,
          describe: "Turn on/off tables support",
          type: "boolean"
        },
        tablesHeaderId: {
          defaultValue: !1,
          describe: "Add an id to table headers",
          type: "boolean"
        },
        ghCodeBlocks: {
          defaultValue: !0,
          describe: "Turn on/off GFM fenced code blocks support",
          type: "boolean"
        },
        tasklists: {
          defaultValue: !1,
          describe: "Turn on/off GFM tasklist support",
          type: "boolean"
        },
        smoothLivePreview: {
          defaultValue: !1,
          describe: "Prevents weird effects in live previews due to incomplete input",
          type: "boolean"
        },
        smartIndentationFix: {
          defaultValue: !1,
          describe: "Tries to smartly fix indentation in es6 strings",
          type: "boolean"
        },
        disableForced4SpacesIndentedSublists: {
          defaultValue: !1,
          describe: "Disables the requirement of indenting nested sublists by 4 spaces",
          type: "boolean"
        },
        simpleLineBreaks: {
          defaultValue: !1,
          describe: "Parses simple line breaks as <br> (GFM Style)",
          type: "boolean"
        },
        requireSpaceBeforeHeadingText: {
          defaultValue: !1,
          describe: "Makes adding a space between `#` and the header text mandatory (GFM Style)",
          type: "boolean"
        },
        ghMentions: {
          defaultValue: !1,
          describe: "Enables github @mentions",
          type: "boolean"
        },
        ghMentionsLink: {
          defaultValue: "https://github.com/{u}",
          describe: "Changes the link generated by @mentions. Only applies if ghMentions option is enabled.",
          type: "string"
        },
        encodeEmails: {
          defaultValue: !0,
          describe: "Encode e-mail addresses through the use of Character Entities, transforming ASCII e-mail addresses into its equivalent decimal entities",
          type: "boolean"
        },
        openLinksInNewWindow: {
          defaultValue: !1,
          describe: "Open all links in new windows",
          type: "boolean"
        },
        backslashEscapesHTMLTags: {
          defaultValue: !1,
          describe: "Support for HTML Tag escaping. ex: <div>foo</div>",
          type: "boolean"
        },
        emoji: {
          defaultValue: !1,
          describe: "Enable emoji support. Ex: `this is a :smile: emoji`",
          type: "boolean"
        },
        underline: {
          defaultValue: !1,
          describe: "Enable support for underline. Syntax is double or triple underscores: `__underline word__`. With this option enabled, underscores no longer parses into `<em>` and `<strong>`",
          type: "boolean"
        },
        ellipsis: {
          defaultValue: !0,
          describe: "Replaces three dots with the ellipsis unicode character",
          type: "boolean"
        },
        completeHTMLDocument: {
          defaultValue: !1,
          describe: "Outputs a complete html document, including `<html>`, `<head>` and `<body>` tags",
          type: "boolean"
        },
        metadata: {
          defaultValue: !1,
          describe: "Enable support for document metadata (defined at the top of the document between `«««` and `»»»` or between `---` and `---`).",
          type: "boolean"
        },
        splitAdjacentBlockquotes: {
          defaultValue: !1,
          describe: "Split adjacent blockquote blocks",
          type: "boolean"
        }
      };
      if (p === !1) return JSON.parse(JSON.stringify(w));
      var _ = {};
      for (var b in w) w.hasOwnProperty(b) && (_[b] = w[b].defaultValue);
      return _;
    }
    function r() {
      "use strict";
      var p = n(!0), w = {};
      for (var _ in p) p.hasOwnProperty(_) && (w[_] = !0);
      return w;
    }
    var s = {}, a = {}, o = {}, l = n(!0), i = "vanilla", u = {
      github: {
        omitExtraWLInCodeBlocks: !0,
        simplifiedAutoLink: !0,
        excludeTrailingPunctuationFromURLs: !0,
        literalMidWordUnderscores: !0,
        strikethrough: !0,
        tables: !0,
        tablesHeaderId: !0,
        ghCodeBlocks: !0,
        tasklists: !0,
        disableForced4SpacesIndentedSublists: !0,
        simpleLineBreaks: !0,
        requireSpaceBeforeHeadingText: !0,
        ghCompatibleHeaderId: !0,
        ghMentions: !0,
        backslashEscapesHTMLTags: !0,
        emoji: !0,
        splitAdjacentBlockquotes: !0
      },
      original: {
        noHeaderId: !0,
        ghCodeBlocks: !1
      },
      ghost: {
        omitExtraWLInCodeBlocks: !0,
        parseImgDimensions: !0,
        simplifiedAutoLink: !0,
        excludeTrailingPunctuationFromURLs: !0,
        literalMidWordUnderscores: !0,
        strikethrough: !0,
        tables: !0,
        tablesHeaderId: !0,
        ghCodeBlocks: !0,
        tasklists: !0,
        smoothLivePreview: !0,
        simpleLineBreaks: !0,
        requireSpaceBeforeHeadingText: !0,
        ghMentions: !1,
        encodeEmails: !0
      },
      vanilla: n(!0),
      allOn: r()
    };
    s.helper = {}, s.extensions = {}, s.setOption = function(p, w) {
      "use strict";
      return l[p] = w, this;
    }, s.getOption = function(p) {
      "use strict";
      return l[p];
    }, s.getOptions = function() {
      "use strict";
      return l;
    }, s.resetOptions = function() {
      "use strict";
      l = n(!0);
    }, s.setFlavor = function(p) {
      "use strict";
      if (!u.hasOwnProperty(p)) throw Error(p + " flavor was not found");
      s.resetOptions();
      var w = u[p];
      i = p;
      for (var _ in w) w.hasOwnProperty(_) && (l[_] = w[_]);
    }, s.getFlavor = function() {
      "use strict";
      return i;
    }, s.getFlavorOptions = function(p) {
      "use strict";
      if (u.hasOwnProperty(p)) return u[p];
    }, s.getDefaultOptions = function(p) {
      "use strict";
      return n(p);
    }, s.subParser = function(p, w) {
      "use strict";
      if (s.helper.isString(p)) if (typeof w < "u") a[p] = w;
      else {
        if (a.hasOwnProperty(p)) return a[p];
        throw Error("SubParser named " + p + " not registered!");
      }
    }, s.extension = function(p, w) {
      "use strict";
      if (!s.helper.isString(p)) throw Error("Extension 'name' must be a string");
      if (p = s.helper.stdExtName(p), s.helper.isUndefined(w)) {
        if (!o.hasOwnProperty(p)) throw Error("Extension named " + p + " is not registered!");
        return o[p];
      } else {
        typeof w == "function" && (w = w()), s.helper.isArray(w) || (w = [w]);
        var _ = d(w, p);
        if (_.valid) o[p] = w;
        else throw Error(_.error);
      }
    }, s.getAllExtensions = function() {
      "use strict";
      return o;
    }, s.removeExtension = function(p) {
      "use strict";
      delete o[p];
    }, s.resetExtensions = function() {
      "use strict";
      o = {};
    };
    function d(p, w) {
      "use strict";
      var _ = w ? "Error in " + w + " extension->" : "Error in unnamed extension", b = {
        valid: !0,
        error: ""
      };
      s.helper.isArray(p) || (p = [p]);
      for (var C = 0; C < p.length; ++C) {
        var k = _ + " sub-extension " + C + ": ", P = p[C];
        if (typeof P != "object")
          return b.valid = !1, b.error = k + "must be an object, but " + typeof P + " given", b;
        if (!s.helper.isString(P.type))
          return b.valid = !1, b.error = k + 'property "type" must be a string, but ' + typeof P.type + " given", b;
        var H = P.type = P.type.toLowerCase();
        if (H === "language" && (H = P.type = "lang"), H === "html" && (H = P.type = "output"), H !== "lang" && H !== "output" && H !== "listener")
          return b.valid = !1, b.error = k + "type " + H + ' is not recognized. Valid values: "lang/language", "output/html" or "listener"', b;
        if (H === "listener") {
          if (s.helper.isUndefined(P.listeners))
            return b.valid = !1, b.error = k + '. Extensions of type "listener" must have a property called "listeners"', b;
        } else if (s.helper.isUndefined(P.filter) && s.helper.isUndefined(P.regex))
          return b.valid = !1, b.error = k + H + ' extensions must define either a "regex" property or a "filter" method', b;
        if (P.listeners) {
          if (typeof P.listeners != "object")
            return b.valid = !1, b.error = k + '"listeners" property must be an object but ' + typeof P.listeners + " given", b;
          for (var G in P.listeners) if (P.listeners.hasOwnProperty(G) && typeof P.listeners[G] != "function")
            return b.valid = !1, b.error = k + '"listeners" property must be an hash of [event name]: [callback]. listeners.' + G + " must be a function but " + typeof P.listeners[G] + " given", b;
        }
        if (P.filter) {
          if (typeof P.filter != "function")
            return b.valid = !1, b.error = k + '"filter" must be a function, but ' + typeof P.filter + " given", b;
        } else if (P.regex) {
          if (s.helper.isString(P.regex) && (P.regex = new RegExp(P.regex, "g")), !(P.regex instanceof RegExp))
            return b.valid = !1, b.error = k + '"regex" property must either be a string or a RegExp object, but ' + typeof P.regex + " given", b;
          if (s.helper.isUndefined(P.replace))
            return b.valid = !1, b.error = k + '"regex" extensions must implement a replace string or function', b;
        }
      }
      return b;
    }
    s.validateExtension = function(p) {
      "use strict";
      var w = d(p, null);
      return w.valid ? !0 : (console.warn(w.error), !1);
    }, s.hasOwnProperty("helper") || (s.helper = {}), s.helper.isString = function(p) {
      "use strict";
      return typeof p == "string" || p instanceof String;
    }, s.helper.isFunction = function(p) {
      "use strict";
      return p && {}.toString.call(p) === "[object Function]";
    }, s.helper.isArray = function(p) {
      "use strict";
      return Array.isArray(p);
    }, s.helper.isUndefined = function(p) {
      "use strict";
      return typeof p > "u";
    }, s.helper.forEach = function(p, w) {
      "use strict";
      if (s.helper.isUndefined(p)) throw new Error("obj param is required");
      if (s.helper.isUndefined(w)) throw new Error("callback param is required");
      if (!s.helper.isFunction(w)) throw new Error("callback param must be a function/closure");
      if (typeof p.forEach == "function") p.forEach(w);
      else if (s.helper.isArray(p)) for (var _ = 0; _ < p.length; _++) w(p[_], _, p);
      else if (typeof p == "object")
        for (var b in p) p.hasOwnProperty(b) && w(p[b], b, p);
      else throw new Error("obj does not seem to be an array or an iterable object");
    }, s.helper.stdExtName = function(p) {
      "use strict";
      return p.replace(/[_?*+\/\\.^-]/g, "").replace(/\s/g, "").toLowerCase();
    };
    function f(p, w) {
      "use strict";
      return "¨E" + w.charCodeAt(0) + "E";
    }
    s.helper.escapeCharactersCallback = f, s.helper.escapeCharacters = function(p, w, _) {
      "use strict";
      var b = "([" + w.replace(/([\[\]\\])/g, "\\$1") + "])";
      _ && (b = "\\\\" + b);
      var C = new RegExp(b, "g");
      return p = p.replace(C, f), p;
    }, s.helper.unescapeHTMLEntities = function(p) {
      "use strict";
      return p.replace(/&quot;/g, '"').replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&amp;/g, "&");
    };
    var m = function(p, w, _, b) {
      "use strict";
      var C = b || "", k = C.indexOf("g") > -1, P = new RegExp(w + "|" + _, "g" + C.replace(/g/g, "")), H = new RegExp(w, C.replace(/g/g, "")), G = [], ee, J, z, R, V;
      do
        for (ee = 0; z = P.exec(p); ) if (H.test(z[0]))
          ee++ || (J = P.lastIndex, R = J - z[0].length);
        else if (ee && !--ee) {
          V = z.index + z[0].length;
          var le = {
            left: {
              start: R,
              end: J
            },
            match: {
              start: J,
              end: z.index
            },
            right: {
              start: z.index,
              end: V
            },
            wholeMatch: {
              start: R,
              end: V
            }
          };
          if (G.push(le), !k) return G;
        }
      while (ee && (P.lastIndex = J));
      return G;
    };
    s.helper.matchRecursiveRegExp = function(p, w, _, b) {
      "use strict";
      for (var C = m(p, w, _, b), k = [], P = 0; P < C.length; ++P) k.push([
        p.slice(C[P].wholeMatch.start, C[P].wholeMatch.end),
        p.slice(C[P].match.start, C[P].match.end),
        p.slice(C[P].left.start, C[P].left.end),
        p.slice(C[P].right.start, C[P].right.end)
      ]);
      return k;
    }, s.helper.replaceRecursiveRegExp = function(p, w, _, b, C) {
      "use strict";
      if (!s.helper.isFunction(w)) {
        var k = w;
        w = function() {
          return k;
        };
      }
      var P = m(p, _, b, C), H = p, G = P.length;
      if (G > 0) {
        var ee = [];
        P[0].wholeMatch.start !== 0 && ee.push(p.slice(0, P[0].wholeMatch.start));
        for (var J = 0; J < G; ++J)
          ee.push(w(p.slice(P[J].wholeMatch.start, P[J].wholeMatch.end), p.slice(P[J].match.start, P[J].match.end), p.slice(P[J].left.start, P[J].left.end), p.slice(P[J].right.start, P[J].right.end))), J < G - 1 && ee.push(p.slice(P[J].wholeMatch.end, P[J + 1].wholeMatch.start));
        P[G - 1].wholeMatch.end < p.length && ee.push(p.slice(P[G - 1].wholeMatch.end)), H = ee.join("");
      }
      return H;
    }, s.helper.regexIndexOf = function(p, w, _) {
      "use strict";
      if (!s.helper.isString(p)) throw "InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string";
      if (!(w instanceof RegExp)) throw "InvalidArgumentError: second parameter of showdown.helper.regexIndexOf function must be an instance of RegExp";
      var b = p.substring(_ || 0).search(w);
      return b >= 0 ? b + (_ || 0) : b;
    }, s.helper.splitAtIndex = function(p, w) {
      "use strict";
      if (!s.helper.isString(p)) throw "InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string";
      return [p.substring(0, w), p.substring(w)];
    }, s.helper.encodeEmailAddress = function(p) {
      "use strict";
      var w = [
        function(_) {
          return "&#" + _.charCodeAt(0) + ";";
        },
        function(_) {
          return "&#x" + _.charCodeAt(0).toString(16) + ";";
        },
        function(_) {
          return _;
        }
      ];
      return p = p.replace(/./g, function(_) {
        if (_ === "@") _ = w[Math.floor(Math.random() * 2)](_);
        else {
          var b = Math.random();
          _ = b > 0.9 ? w[2](_) : b > 0.45 ? w[1](_) : w[0](_);
        }
        return _;
      }), p;
    }, s.helper.padEnd = function(w, _, b) {
      "use strict";
      return _ = _ >> 0, b = String(b || " "), w.length > _ ? String(w) : (_ = _ - w.length, _ > b.length && (b += b.repeat(_ / b.length)), String(w) + b.slice(0, _));
    }, typeof console > "u" && (console = {
      warn: function(p) {
        "use strict";
        alert(p);
      },
      log: function(p) {
        "use strict";
        alert(p);
      },
      error: function(p) {
        "use strict";
        throw p;
      }
    }), s.helper.regexes = { asteriskDashAndColon: /([*_:~])/g }, s.helper.emojis = {
      "+1": "👍",
      "-1": "👎",
      100: "💯",
      1234: "🔢",
      "1st_place_medal": "🥇",
      "2nd_place_medal": "🥈",
      "3rd_place_medal": "🥉",
      "8ball": "🎱",
      a: "🅰️",
      ab: "🆎",
      abc: "🔤",
      abcd: "🔡",
      accept: "🉑",
      aerial_tramway: "🚡",
      airplane: "✈️",
      alarm_clock: "⏰",
      alembic: "⚗️",
      alien: "👽",
      ambulance: "🚑",
      amphora: "🏺",
      anchor: "⚓️",
      angel: "👼",
      anger: "💢",
      angry: "😠",
      anguished: "😧",
      ant: "🐜",
      apple: "🍎",
      aquarius: "♒️",
      aries: "♈️",
      arrow_backward: "◀️",
      arrow_double_down: "⏬",
      arrow_double_up: "⏫",
      arrow_down: "⬇️",
      arrow_down_small: "🔽",
      arrow_forward: "▶️",
      arrow_heading_down: "⤵️",
      arrow_heading_up: "⤴️",
      arrow_left: "⬅️",
      arrow_lower_left: "↙️",
      arrow_lower_right: "↘️",
      arrow_right: "➡️",
      arrow_right_hook: "↪️",
      arrow_up: "⬆️",
      arrow_up_down: "↕️",
      arrow_up_small: "🔼",
      arrow_upper_left: "↖️",
      arrow_upper_right: "↗️",
      arrows_clockwise: "🔃",
      arrows_counterclockwise: "🔄",
      art: "🎨",
      articulated_lorry: "🚛",
      artificial_satellite: "🛰",
      astonished: "😲",
      athletic_shoe: "👟",
      atm: "🏧",
      atom_symbol: "⚛️",
      avocado: "🥑",
      b: "🅱️",
      baby: "👶",
      baby_bottle: "🍼",
      baby_chick: "🐤",
      baby_symbol: "🚼",
      back: "🔙",
      bacon: "🥓",
      badminton: "🏸",
      baggage_claim: "🛄",
      baguette_bread: "🥖",
      balance_scale: "⚖️",
      balloon: "🎈",
      ballot_box: "🗳",
      ballot_box_with_check: "☑️",
      bamboo: "🎍",
      banana: "🍌",
      bangbang: "‼️",
      bank: "🏦",
      bar_chart: "📊",
      barber: "💈",
      baseball: "⚾️",
      basketball: "🏀",
      basketball_man: "⛹️",
      basketball_woman: "⛹️&zwj;♀️",
      bat: "🦇",
      bath: "🛀",
      bathtub: "🛁",
      battery: "🔋",
      beach_umbrella: "🏖",
      bear: "🐻",
      bed: "🛏",
      bee: "🐝",
      beer: "🍺",
      beers: "🍻",
      beetle: "🐞",
      beginner: "🔰",
      bell: "🔔",
      bellhop_bell: "🛎",
      bento: "🍱",
      biking_man: "🚴",
      bike: "🚲",
      biking_woman: "🚴&zwj;♀️",
      bikini: "👙",
      biohazard: "☣️",
      bird: "🐦",
      birthday: "🎂",
      black_circle: "⚫️",
      black_flag: "🏴",
      black_heart: "🖤",
      black_joker: "🃏",
      black_large_square: "⬛️",
      black_medium_small_square: "◾️",
      black_medium_square: "◼️",
      black_nib: "✒️",
      black_small_square: "▪️",
      black_square_button: "🔲",
      blonde_man: "👱",
      blonde_woman: "👱&zwj;♀️",
      blossom: "🌼",
      blowfish: "🐡",
      blue_book: "📘",
      blue_car: "🚙",
      blue_heart: "💙",
      blush: "😊",
      boar: "🐗",
      boat: "⛵️",
      bomb: "💣",
      book: "📖",
      bookmark: "🔖",
      bookmark_tabs: "📑",
      books: "📚",
      boom: "💥",
      boot: "👢",
      bouquet: "💐",
      bowing_man: "🙇",
      bow_and_arrow: "🏹",
      bowing_woman: "🙇&zwj;♀️",
      bowling: "🎳",
      boxing_glove: "🥊",
      boy: "👦",
      bread: "🍞",
      bride_with_veil: "👰",
      bridge_at_night: "🌉",
      briefcase: "💼",
      broken_heart: "💔",
      bug: "🐛",
      building_construction: "🏗",
      bulb: "💡",
      bullettrain_front: "🚅",
      bullettrain_side: "🚄",
      burrito: "🌯",
      bus: "🚌",
      business_suit_levitating: "🕴",
      busstop: "🚏",
      bust_in_silhouette: "👤",
      busts_in_silhouette: "👥",
      butterfly: "🦋",
      cactus: "🌵",
      cake: "🍰",
      calendar: "📆",
      call_me_hand: "🤙",
      calling: "📲",
      camel: "🐫",
      camera: "📷",
      camera_flash: "📸",
      camping: "🏕",
      cancer: "♋️",
      candle: "🕯",
      candy: "🍬",
      canoe: "🛶",
      capital_abcd: "🔠",
      capricorn: "♑️",
      car: "🚗",
      card_file_box: "🗃",
      card_index: "📇",
      card_index_dividers: "🗂",
      carousel_horse: "🎠",
      carrot: "🥕",
      cat: "🐱",
      cat2: "🐈",
      cd: "💿",
      chains: "⛓",
      champagne: "🍾",
      chart: "💹",
      chart_with_downwards_trend: "📉",
      chart_with_upwards_trend: "📈",
      checkered_flag: "🏁",
      cheese: "🧀",
      cherries: "🍒",
      cherry_blossom: "🌸",
      chestnut: "🌰",
      chicken: "🐔",
      children_crossing: "🚸",
      chipmunk: "🐿",
      chocolate_bar: "🍫",
      christmas_tree: "🎄",
      church: "⛪️",
      cinema: "🎦",
      circus_tent: "🎪",
      city_sunrise: "🌇",
      city_sunset: "🌆",
      cityscape: "🏙",
      cl: "🆑",
      clamp: "🗜",
      clap: "👏",
      clapper: "🎬",
      classical_building: "🏛",
      clinking_glasses: "🥂",
      clipboard: "📋",
      clock1: "🕐",
      clock10: "🕙",
      clock1030: "🕥",
      clock11: "🕚",
      clock1130: "🕦",
      clock12: "🕛",
      clock1230: "🕧",
      clock130: "🕜",
      clock2: "🕑",
      clock230: "🕝",
      clock3: "🕒",
      clock330: "🕞",
      clock4: "🕓",
      clock430: "🕟",
      clock5: "🕔",
      clock530: "🕠",
      clock6: "🕕",
      clock630: "🕡",
      clock7: "🕖",
      clock730: "🕢",
      clock8: "🕗",
      clock830: "🕣",
      clock9: "🕘",
      clock930: "🕤",
      closed_book: "📕",
      closed_lock_with_key: "🔐",
      closed_umbrella: "🌂",
      cloud: "☁️",
      cloud_with_lightning: "🌩",
      cloud_with_lightning_and_rain: "⛈",
      cloud_with_rain: "🌧",
      cloud_with_snow: "🌨",
      clown_face: "🤡",
      clubs: "♣️",
      cocktail: "🍸",
      coffee: "☕️",
      coffin: "⚰️",
      cold_sweat: "😰",
      comet: "☄️",
      computer: "💻",
      computer_mouse: "🖱",
      confetti_ball: "🎊",
      confounded: "😖",
      confused: "😕",
      congratulations: "㊗️",
      construction: "🚧",
      construction_worker_man: "👷",
      construction_worker_woman: "👷&zwj;♀️",
      control_knobs: "🎛",
      convenience_store: "🏪",
      cookie: "🍪",
      cool: "🆒",
      policeman: "👮",
      copyright: "©️",
      corn: "🌽",
      couch_and_lamp: "🛋",
      couple: "👫",
      couple_with_heart_woman_man: "💑",
      couple_with_heart_man_man: "👨&zwj;❤️&zwj;👨",
      couple_with_heart_woman_woman: "👩&zwj;❤️&zwj;👩",
      couplekiss_man_man: "👨&zwj;❤️&zwj;💋&zwj;👨",
      couplekiss_man_woman: "💏",
      couplekiss_woman_woman: "👩&zwj;❤️&zwj;💋&zwj;👩",
      cow: "🐮",
      cow2: "🐄",
      cowboy_hat_face: "🤠",
      crab: "🦀",
      crayon: "🖍",
      credit_card: "💳",
      crescent_moon: "🌙",
      cricket: "🏏",
      crocodile: "🐊",
      croissant: "🥐",
      crossed_fingers: "🤞",
      crossed_flags: "🎌",
      crossed_swords: "⚔️",
      crown: "👑",
      cry: "😢",
      crying_cat_face: "😿",
      crystal_ball: "🔮",
      cucumber: "🥒",
      cupid: "💘",
      curly_loop: "➰",
      currency_exchange: "💱",
      curry: "🍛",
      custard: "🍮",
      customs: "🛃",
      cyclone: "🌀",
      dagger: "🗡",
      dancer: "💃",
      dancing_women: "👯",
      dancing_men: "👯&zwj;♂️",
      dango: "🍡",
      dark_sunglasses: "🕶",
      dart: "🎯",
      dash: "💨",
      date: "📅",
      deciduous_tree: "🌳",
      deer: "🦌",
      department_store: "🏬",
      derelict_house: "🏚",
      desert: "🏜",
      desert_island: "🏝",
      desktop_computer: "🖥",
      male_detective: "🕵️",
      diamond_shape_with_a_dot_inside: "💠",
      diamonds: "♦️",
      disappointed: "😞",
      disappointed_relieved: "😥",
      dizzy: "💫",
      dizzy_face: "😵",
      do_not_litter: "🚯",
      dog: "🐶",
      dog2: "🐕",
      dollar: "💵",
      dolls: "🎎",
      dolphin: "🐬",
      door: "🚪",
      doughnut: "🍩",
      dove: "🕊",
      dragon: "🐉",
      dragon_face: "🐲",
      dress: "👗",
      dromedary_camel: "🐪",
      drooling_face: "🤤",
      droplet: "💧",
      drum: "🥁",
      duck: "🦆",
      dvd: "📀",
      "e-mail": "📧",
      eagle: "🦅",
      ear: "👂",
      ear_of_rice: "🌾",
      earth_africa: "🌍",
      earth_americas: "🌎",
      earth_asia: "🌏",
      egg: "🥚",
      eggplant: "🍆",
      eight_pointed_black_star: "✴️",
      eight_spoked_asterisk: "✳️",
      electric_plug: "🔌",
      elephant: "🐘",
      email: "✉️",
      end: "🔚",
      envelope_with_arrow: "📩",
      euro: "💶",
      european_castle: "🏰",
      european_post_office: "🏤",
      evergreen_tree: "🌲",
      exclamation: "❗️",
      expressionless: "😑",
      eye: "👁",
      eye_speech_bubble: "👁&zwj;🗨",
      eyeglasses: "👓",
      eyes: "👀",
      face_with_head_bandage: "🤕",
      face_with_thermometer: "🤒",
      fist_oncoming: "👊",
      factory: "🏭",
      fallen_leaf: "🍂",
      family_man_woman_boy: "👪",
      family_man_boy: "👨&zwj;👦",
      family_man_boy_boy: "👨&zwj;👦&zwj;👦",
      family_man_girl: "👨&zwj;👧",
      family_man_girl_boy: "👨&zwj;👧&zwj;👦",
      family_man_girl_girl: "👨&zwj;👧&zwj;👧",
      family_man_man_boy: "👨&zwj;👨&zwj;👦",
      family_man_man_boy_boy: "👨&zwj;👨&zwj;👦&zwj;👦",
      family_man_man_girl: "👨&zwj;👨&zwj;👧",
      family_man_man_girl_boy: "👨&zwj;👨&zwj;👧&zwj;👦",
      family_man_man_girl_girl: "👨&zwj;👨&zwj;👧&zwj;👧",
      family_man_woman_boy_boy: "👨&zwj;👩&zwj;👦&zwj;👦",
      family_man_woman_girl: "👨&zwj;👩&zwj;👧",
      family_man_woman_girl_boy: "👨&zwj;👩&zwj;👧&zwj;👦",
      family_man_woman_girl_girl: "👨&zwj;👩&zwj;👧&zwj;👧",
      family_woman_boy: "👩&zwj;👦",
      family_woman_boy_boy: "👩&zwj;👦&zwj;👦",
      family_woman_girl: "👩&zwj;👧",
      family_woman_girl_boy: "👩&zwj;👧&zwj;👦",
      family_woman_girl_girl: "👩&zwj;👧&zwj;👧",
      family_woman_woman_boy: "👩&zwj;👩&zwj;👦",
      family_woman_woman_boy_boy: "👩&zwj;👩&zwj;👦&zwj;👦",
      family_woman_woman_girl: "👩&zwj;👩&zwj;👧",
      family_woman_woman_girl_boy: "👩&zwj;👩&zwj;👧&zwj;👦",
      family_woman_woman_girl_girl: "👩&zwj;👩&zwj;👧&zwj;👧",
      fast_forward: "⏩",
      fax: "📠",
      fearful: "😨",
      feet: "🐾",
      female_detective: "🕵️&zwj;♀️",
      ferris_wheel: "🎡",
      ferry: "⛴",
      field_hockey: "🏑",
      file_cabinet: "🗄",
      file_folder: "📁",
      film_projector: "📽",
      film_strip: "🎞",
      fire: "🔥",
      fire_engine: "🚒",
      fireworks: "🎆",
      first_quarter_moon: "🌓",
      first_quarter_moon_with_face: "🌛",
      fish: "🐟",
      fish_cake: "🍥",
      fishing_pole_and_fish: "🎣",
      fist_raised: "✊",
      fist_left: "🤛",
      fist_right: "🤜",
      flags: "🎏",
      flashlight: "🔦",
      fleur_de_lis: "⚜️",
      flight_arrival: "🛬",
      flight_departure: "🛫",
      floppy_disk: "💾",
      flower_playing_cards: "🎴",
      flushed: "😳",
      fog: "🌫",
      foggy: "🌁",
      football: "🏈",
      footprints: "👣",
      fork_and_knife: "🍴",
      fountain: "⛲️",
      fountain_pen: "🖋",
      four_leaf_clover: "🍀",
      fox_face: "🦊",
      framed_picture: "🖼",
      free: "🆓",
      fried_egg: "🍳",
      fried_shrimp: "🍤",
      fries: "🍟",
      frog: "🐸",
      frowning: "😦",
      frowning_face: "☹️",
      frowning_man: "🙍&zwj;♂️",
      frowning_woman: "🙍",
      middle_finger: "🖕",
      fuelpump: "⛽️",
      full_moon: "🌕",
      full_moon_with_face: "🌝",
      funeral_urn: "⚱️",
      game_die: "🎲",
      gear: "⚙️",
      gem: "💎",
      gemini: "♊️",
      ghost: "👻",
      gift: "🎁",
      gift_heart: "💝",
      girl: "👧",
      globe_with_meridians: "🌐",
      goal_net: "🥅",
      goat: "🐐",
      golf: "⛳️",
      golfing_man: "🏌️",
      golfing_woman: "🏌️&zwj;♀️",
      gorilla: "🦍",
      grapes: "🍇",
      green_apple: "🍏",
      green_book: "📗",
      green_heart: "💚",
      green_salad: "🥗",
      grey_exclamation: "❕",
      grey_question: "❔",
      grimacing: "😬",
      grin: "😁",
      grinning: "😀",
      guardsman: "💂",
      guardswoman: "💂&zwj;♀️",
      guitar: "🎸",
      gun: "🔫",
      haircut_woman: "💇",
      haircut_man: "💇&zwj;♂️",
      hamburger: "🍔",
      hammer: "🔨",
      hammer_and_pick: "⚒",
      hammer_and_wrench: "🛠",
      hamster: "🐹",
      hand: "✋",
      handbag: "👜",
      handshake: "🤝",
      hankey: "💩",
      hatched_chick: "🐥",
      hatching_chick: "🐣",
      headphones: "🎧",
      hear_no_evil: "🙉",
      heart: "❤️",
      heart_decoration: "💟",
      heart_eyes: "😍",
      heart_eyes_cat: "😻",
      heartbeat: "💓",
      heartpulse: "💗",
      hearts: "♥️",
      heavy_check_mark: "✔️",
      heavy_division_sign: "➗",
      heavy_dollar_sign: "💲",
      heavy_heart_exclamation: "❣️",
      heavy_minus_sign: "➖",
      heavy_multiplication_x: "✖️",
      heavy_plus_sign: "➕",
      helicopter: "🚁",
      herb: "🌿",
      hibiscus: "🌺",
      high_brightness: "🔆",
      high_heel: "👠",
      hocho: "🔪",
      hole: "🕳",
      honey_pot: "🍯",
      horse: "🐴",
      horse_racing: "🏇",
      hospital: "🏥",
      hot_pepper: "🌶",
      hotdog: "🌭",
      hotel: "🏨",
      hotsprings: "♨️",
      hourglass: "⌛️",
      hourglass_flowing_sand: "⏳",
      house: "🏠",
      house_with_garden: "🏡",
      houses: "🏘",
      hugs: "🤗",
      hushed: "😯",
      ice_cream: "🍨",
      ice_hockey: "🏒",
      ice_skate: "⛸",
      icecream: "🍦",
      id: "🆔",
      ideograph_advantage: "🉐",
      imp: "👿",
      inbox_tray: "📥",
      incoming_envelope: "📨",
      tipping_hand_woman: "💁",
      information_source: "ℹ️",
      innocent: "😇",
      interrobang: "⁉️",
      iphone: "📱",
      izakaya_lantern: "🏮",
      jack_o_lantern: "🎃",
      japan: "🗾",
      japanese_castle: "🏯",
      japanese_goblin: "👺",
      japanese_ogre: "👹",
      jeans: "👖",
      joy: "😂",
      joy_cat: "😹",
      joystick: "🕹",
      kaaba: "🕋",
      key: "🔑",
      keyboard: "⌨️",
      keycap_ten: "🔟",
      kick_scooter: "🛴",
      kimono: "👘",
      kiss: "💋",
      kissing: "😗",
      kissing_cat: "😽",
      kissing_closed_eyes: "😚",
      kissing_heart: "😘",
      kissing_smiling_eyes: "😙",
      kiwi_fruit: "🥝",
      koala: "🐨",
      koko: "🈁",
      label: "🏷",
      large_blue_circle: "🔵",
      large_blue_diamond: "🔷",
      large_orange_diamond: "🔶",
      last_quarter_moon: "🌗",
      last_quarter_moon_with_face: "🌜",
      latin_cross: "✝️",
      laughing: "😆",
      leaves: "🍃",
      ledger: "📒",
      left_luggage: "🛅",
      left_right_arrow: "↔️",
      leftwards_arrow_with_hook: "↩️",
      lemon: "🍋",
      leo: "♌️",
      leopard: "🐆",
      level_slider: "🎚",
      libra: "♎️",
      light_rail: "🚈",
      link: "🔗",
      lion: "🦁",
      lips: "👄",
      lipstick: "💄",
      lizard: "🦎",
      lock: "🔒",
      lock_with_ink_pen: "🔏",
      lollipop: "🍭",
      loop: "➿",
      loud_sound: "🔊",
      loudspeaker: "📢",
      love_hotel: "🏩",
      love_letter: "💌",
      low_brightness: "🔅",
      lying_face: "🤥",
      m: "Ⓜ️",
      mag: "🔍",
      mag_right: "🔎",
      mahjong: "🀄️",
      mailbox: "📫",
      mailbox_closed: "📪",
      mailbox_with_mail: "📬",
      mailbox_with_no_mail: "📭",
      man: "👨",
      man_artist: "👨&zwj;🎨",
      man_astronaut: "👨&zwj;🚀",
      man_cartwheeling: "🤸&zwj;♂️",
      man_cook: "👨&zwj;🍳",
      man_dancing: "🕺",
      man_facepalming: "🤦&zwj;♂️",
      man_factory_worker: "👨&zwj;🏭",
      man_farmer: "👨&zwj;🌾",
      man_firefighter: "👨&zwj;🚒",
      man_health_worker: "👨&zwj;⚕️",
      man_in_tuxedo: "🤵",
      man_judge: "👨&zwj;⚖️",
      man_juggling: "🤹&zwj;♂️",
      man_mechanic: "👨&zwj;🔧",
      man_office_worker: "👨&zwj;💼",
      man_pilot: "👨&zwj;✈️",
      man_playing_handball: "🤾&zwj;♂️",
      man_playing_water_polo: "🤽&zwj;♂️",
      man_scientist: "👨&zwj;🔬",
      man_shrugging: "🤷&zwj;♂️",
      man_singer: "👨&zwj;🎤",
      man_student: "👨&zwj;🎓",
      man_teacher: "👨&zwj;🏫",
      man_technologist: "👨&zwj;💻",
      man_with_gua_pi_mao: "👲",
      man_with_turban: "👳",
      tangerine: "🍊",
      mans_shoe: "👞",
      mantelpiece_clock: "🕰",
      maple_leaf: "🍁",
      martial_arts_uniform: "🥋",
      mask: "😷",
      massage_woman: "💆",
      massage_man: "💆&zwj;♂️",
      meat_on_bone: "🍖",
      medal_military: "🎖",
      medal_sports: "🏅",
      mega: "📣",
      melon: "🍈",
      memo: "📝",
      men_wrestling: "🤼&zwj;♂️",
      menorah: "🕎",
      mens: "🚹",
      metal: "🤘",
      metro: "🚇",
      microphone: "🎤",
      microscope: "🔬",
      milk_glass: "🥛",
      milky_way: "🌌",
      minibus: "🚐",
      minidisc: "💽",
      mobile_phone_off: "📴",
      money_mouth_face: "🤑",
      money_with_wings: "💸",
      moneybag: "💰",
      monkey: "🐒",
      monkey_face: "🐵",
      monorail: "🚝",
      moon: "🌔",
      mortar_board: "🎓",
      mosque: "🕌",
      motor_boat: "🛥",
      motor_scooter: "🛵",
      motorcycle: "🏍",
      motorway: "🛣",
      mount_fuji: "🗻",
      mountain: "⛰",
      mountain_biking_man: "🚵",
      mountain_biking_woman: "🚵&zwj;♀️",
      mountain_cableway: "🚠",
      mountain_railway: "🚞",
      mountain_snow: "🏔",
      mouse: "🐭",
      mouse2: "🐁",
      movie_camera: "🎥",
      moyai: "🗿",
      mrs_claus: "🤶",
      muscle: "💪",
      mushroom: "🍄",
      musical_keyboard: "🎹",
      musical_note: "🎵",
      musical_score: "🎼",
      mute: "🔇",
      nail_care: "💅",
      name_badge: "📛",
      national_park: "🏞",
      nauseated_face: "🤢",
      necktie: "👔",
      negative_squared_cross_mark: "❎",
      nerd_face: "🤓",
      neutral_face: "😐",
      new: "🆕",
      new_moon: "🌑",
      new_moon_with_face: "🌚",
      newspaper: "📰",
      newspaper_roll: "🗞",
      next_track_button: "⏭",
      ng: "🆖",
      no_good_man: "🙅&zwj;♂️",
      no_good_woman: "🙅",
      night_with_stars: "🌃",
      no_bell: "🔕",
      no_bicycles: "🚳",
      no_entry: "⛔️",
      no_entry_sign: "🚫",
      no_mobile_phones: "📵",
      no_mouth: "😶",
      no_pedestrians: "🚷",
      no_smoking: "🚭",
      "non-potable_water": "🚱",
      nose: "👃",
      notebook: "📓",
      notebook_with_decorative_cover: "📔",
      notes: "🎶",
      nut_and_bolt: "🔩",
      o: "⭕️",
      o2: "🅾️",
      ocean: "🌊",
      octopus: "🐙",
      oden: "🍢",
      office: "🏢",
      oil_drum: "🛢",
      ok: "🆗",
      ok_hand: "👌",
      ok_man: "🙆&zwj;♂️",
      ok_woman: "🙆",
      old_key: "🗝",
      older_man: "👴",
      older_woman: "👵",
      om: "🕉",
      on: "🔛",
      oncoming_automobile: "🚘",
      oncoming_bus: "🚍",
      oncoming_police_car: "🚔",
      oncoming_taxi: "🚖",
      open_file_folder: "📂",
      open_hands: "👐",
      open_mouth: "😮",
      open_umbrella: "☂️",
      ophiuchus: "⛎",
      orange_book: "📙",
      orthodox_cross: "☦️",
      outbox_tray: "📤",
      owl: "🦉",
      ox: "🐂",
      package: "📦",
      page_facing_up: "📄",
      page_with_curl: "📃",
      pager: "📟",
      paintbrush: "🖌",
      palm_tree: "🌴",
      pancakes: "🥞",
      panda_face: "🐼",
      paperclip: "📎",
      paperclips: "🖇",
      parasol_on_ground: "⛱",
      parking: "🅿️",
      part_alternation_mark: "〽️",
      partly_sunny: "⛅️",
      passenger_ship: "🛳",
      passport_control: "🛂",
      pause_button: "⏸",
      peace_symbol: "☮️",
      peach: "🍑",
      peanuts: "🥜",
      pear: "🍐",
      pen: "🖊",
      pencil2: "✏️",
      penguin: "🐧",
      pensive: "😔",
      performing_arts: "🎭",
      persevere: "😣",
      person_fencing: "🤺",
      pouting_woman: "🙎",
      phone: "☎️",
      pick: "⛏",
      pig: "🐷",
      pig2: "🐖",
      pig_nose: "🐽",
      pill: "💊",
      pineapple: "🍍",
      ping_pong: "🏓",
      pisces: "♓️",
      pizza: "🍕",
      place_of_worship: "🛐",
      plate_with_cutlery: "🍽",
      play_or_pause_button: "⏯",
      point_down: "👇",
      point_left: "👈",
      point_right: "👉",
      point_up: "☝️",
      point_up_2: "👆",
      police_car: "🚓",
      policewoman: "👮&zwj;♀️",
      poodle: "🐩",
      popcorn: "🍿",
      post_office: "🏣",
      postal_horn: "📯",
      postbox: "📮",
      potable_water: "🚰",
      potato: "🥔",
      pouch: "👝",
      poultry_leg: "🍗",
      pound: "💷",
      rage: "😡",
      pouting_cat: "😾",
      pouting_man: "🙎&zwj;♂️",
      pray: "🙏",
      prayer_beads: "📿",
      pregnant_woman: "🤰",
      previous_track_button: "⏮",
      prince: "🤴",
      princess: "👸",
      printer: "🖨",
      purple_heart: "💜",
      purse: "👛",
      pushpin: "📌",
      put_litter_in_its_place: "🚮",
      question: "❓",
      rabbit: "🐰",
      rabbit2: "🐇",
      racehorse: "🐎",
      racing_car: "🏎",
      radio: "📻",
      radio_button: "🔘",
      radioactive: "☢️",
      railway_car: "🚃",
      railway_track: "🛤",
      rainbow: "🌈",
      rainbow_flag: "🏳️&zwj;🌈",
      raised_back_of_hand: "🤚",
      raised_hand_with_fingers_splayed: "🖐",
      raised_hands: "🙌",
      raising_hand_woman: "🙋",
      raising_hand_man: "🙋&zwj;♂️",
      ram: "🐏",
      ramen: "🍜",
      rat: "🐀",
      record_button: "⏺",
      recycle: "♻️",
      red_circle: "🔴",
      registered: "®️",
      relaxed: "☺️",
      relieved: "😌",
      reminder_ribbon: "🎗",
      repeat: "🔁",
      repeat_one: "🔂",
      rescue_worker_helmet: "⛑",
      restroom: "🚻",
      revolving_hearts: "💞",
      rewind: "⏪",
      rhinoceros: "🦏",
      ribbon: "🎀",
      rice: "🍚",
      rice_ball: "🍙",
      rice_cracker: "🍘",
      rice_scene: "🎑",
      right_anger_bubble: "🗯",
      ring: "💍",
      robot: "🤖",
      rocket: "🚀",
      rofl: "🤣",
      roll_eyes: "🙄",
      roller_coaster: "🎢",
      rooster: "🐓",
      rose: "🌹",
      rosette: "🏵",
      rotating_light: "🚨",
      round_pushpin: "📍",
      rowing_man: "🚣",
      rowing_woman: "🚣&zwj;♀️",
      rugby_football: "🏉",
      running_man: "🏃",
      running_shirt_with_sash: "🎽",
      running_woman: "🏃&zwj;♀️",
      sa: "🈂️",
      sagittarius: "♐️",
      sake: "🍶",
      sandal: "👡",
      santa: "🎅",
      satellite: "📡",
      saxophone: "🎷",
      school: "🏫",
      school_satchel: "🎒",
      scissors: "✂️",
      scorpion: "🦂",
      scorpius: "♏️",
      scream: "😱",
      scream_cat: "🙀",
      scroll: "📜",
      seat: "💺",
      secret: "㊙️",
      see_no_evil: "🙈",
      seedling: "🌱",
      selfie: "🤳",
      shallow_pan_of_food: "🥘",
      shamrock: "☘️",
      shark: "🦈",
      shaved_ice: "🍧",
      sheep: "🐑",
      shell: "🐚",
      shield: "🛡",
      shinto_shrine: "⛩",
      ship: "🚢",
      shirt: "👕",
      shopping: "🛍",
      shopping_cart: "🛒",
      shower: "🚿",
      shrimp: "🦐",
      signal_strength: "📶",
      six_pointed_star: "🔯",
      ski: "🎿",
      skier: "⛷",
      skull: "💀",
      skull_and_crossbones: "☠️",
      sleeping: "😴",
      sleeping_bed: "🛌",
      sleepy: "😪",
      slightly_frowning_face: "🙁",
      slightly_smiling_face: "🙂",
      slot_machine: "🎰",
      small_airplane: "🛩",
      small_blue_diamond: "🔹",
      small_orange_diamond: "🔸",
      small_red_triangle: "🔺",
      small_red_triangle_down: "🔻",
      smile: "😄",
      smile_cat: "😸",
      smiley: "😃",
      smiley_cat: "😺",
      smiling_imp: "😈",
      smirk: "😏",
      smirk_cat: "😼",
      smoking: "🚬",
      snail: "🐌",
      snake: "🐍",
      sneezing_face: "🤧",
      snowboarder: "🏂",
      snowflake: "❄️",
      snowman: "⛄️",
      snowman_with_snow: "☃️",
      sob: "😭",
      soccer: "⚽️",
      soon: "🔜",
      sos: "🆘",
      sound: "🔉",
      space_invader: "👾",
      spades: "♠️",
      spaghetti: "🍝",
      sparkle: "❇️",
      sparkler: "🎇",
      sparkles: "✨",
      sparkling_heart: "💖",
      speak_no_evil: "🙊",
      speaker: "🔈",
      speaking_head: "🗣",
      speech_balloon: "💬",
      speedboat: "🚤",
      spider: "🕷",
      spider_web: "🕸",
      spiral_calendar: "🗓",
      spiral_notepad: "🗒",
      spoon: "🥄",
      squid: "🦑",
      stadium: "🏟",
      star: "⭐️",
      star2: "🌟",
      star_and_crescent: "☪️",
      star_of_david: "✡️",
      stars: "🌠",
      station: "🚉",
      statue_of_liberty: "🗽",
      steam_locomotive: "🚂",
      stew: "🍲",
      stop_button: "⏹",
      stop_sign: "🛑",
      stopwatch: "⏱",
      straight_ruler: "📏",
      strawberry: "🍓",
      stuck_out_tongue: "😛",
      stuck_out_tongue_closed_eyes: "😝",
      stuck_out_tongue_winking_eye: "😜",
      studio_microphone: "🎙",
      stuffed_flatbread: "🥙",
      sun_behind_large_cloud: "🌥",
      sun_behind_rain_cloud: "🌦",
      sun_behind_small_cloud: "🌤",
      sun_with_face: "🌞",
      sunflower: "🌻",
      sunglasses: "😎",
      sunny: "☀️",
      sunrise: "🌅",
      sunrise_over_mountains: "🌄",
      surfing_man: "🏄",
      surfing_woman: "🏄&zwj;♀️",
      sushi: "🍣",
      suspension_railway: "🚟",
      sweat: "😓",
      sweat_drops: "💦",
      sweat_smile: "😅",
      sweet_potato: "🍠",
      swimming_man: "🏊",
      swimming_woman: "🏊&zwj;♀️",
      symbols: "🔣",
      synagogue: "🕍",
      syringe: "💉",
      taco: "🌮",
      tada: "🎉",
      tanabata_tree: "🎋",
      taurus: "♉️",
      taxi: "🚕",
      tea: "🍵",
      telephone_receiver: "📞",
      telescope: "🔭",
      tennis: "🎾",
      tent: "⛺️",
      thermometer: "🌡",
      thinking: "🤔",
      thought_balloon: "💭",
      ticket: "🎫",
      tickets: "🎟",
      tiger: "🐯",
      tiger2: "🐅",
      timer_clock: "⏲",
      tipping_hand_man: "💁&zwj;♂️",
      tired_face: "😫",
      tm: "™️",
      toilet: "🚽",
      tokyo_tower: "🗼",
      tomato: "🍅",
      tongue: "👅",
      top: "🔝",
      tophat: "🎩",
      tornado: "🌪",
      trackball: "🖲",
      tractor: "🚜",
      traffic_light: "🚥",
      train: "🚋",
      train2: "🚆",
      tram: "🚊",
      triangular_flag_on_post: "🚩",
      triangular_ruler: "📐",
      trident: "🔱",
      triumph: "😤",
      trolleybus: "🚎",
      trophy: "🏆",
      tropical_drink: "🍹",
      tropical_fish: "🐠",
      truck: "🚚",
      trumpet: "🎺",
      tulip: "🌷",
      tumbler_glass: "🥃",
      turkey: "🦃",
      turtle: "🐢",
      tv: "📺",
      twisted_rightwards_arrows: "🔀",
      two_hearts: "💕",
      two_men_holding_hands: "👬",
      two_women_holding_hands: "👭",
      u5272: "🈹",
      u5408: "🈴",
      u55b6: "🈺",
      u6307: "🈯️",
      u6708: "🈷️",
      u6709: "🈶",
      u6e80: "🈵",
      u7121: "🈚️",
      u7533: "🈸",
      u7981: "🈲",
      u7a7a: "🈳",
      umbrella: "☔️",
      unamused: "😒",
      underage: "🔞",
      unicorn: "🦄",
      unlock: "🔓",
      up: "🆙",
      upside_down_face: "🙃",
      v: "✌️",
      vertical_traffic_light: "🚦",
      vhs: "📼",
      vibration_mode: "📳",
      video_camera: "📹",
      video_game: "🎮",
      violin: "🎻",
      virgo: "♍️",
      volcano: "🌋",
      volleyball: "🏐",
      vs: "🆚",
      vulcan_salute: "🖖",
      walking_man: "🚶",
      walking_woman: "🚶&zwj;♀️",
      waning_crescent_moon: "🌘",
      waning_gibbous_moon: "🌖",
      warning: "⚠️",
      wastebasket: "🗑",
      watch: "⌚️",
      water_buffalo: "🐃",
      watermelon: "🍉",
      wave: "👋",
      wavy_dash: "〰️",
      waxing_crescent_moon: "🌒",
      wc: "🚾",
      weary: "😩",
      wedding: "💒",
      weight_lifting_man: "🏋️",
      weight_lifting_woman: "🏋️&zwj;♀️",
      whale: "🐳",
      whale2: "🐋",
      wheel_of_dharma: "☸️",
      wheelchair: "♿️",
      white_check_mark: "✅",
      white_circle: "⚪️",
      white_flag: "🏳️",
      white_flower: "💮",
      white_large_square: "⬜️",
      white_medium_small_square: "◽️",
      white_medium_square: "◻️",
      white_small_square: "▫️",
      white_square_button: "🔳",
      wilted_flower: "🥀",
      wind_chime: "🎐",
      wind_face: "🌬",
      wine_glass: "🍷",
      wink: "😉",
      wolf: "🐺",
      woman: "👩",
      woman_artist: "👩&zwj;🎨",
      woman_astronaut: "👩&zwj;🚀",
      woman_cartwheeling: "🤸&zwj;♀️",
      woman_cook: "👩&zwj;🍳",
      woman_facepalming: "🤦&zwj;♀️",
      woman_factory_worker: "👩&zwj;🏭",
      woman_farmer: "👩&zwj;🌾",
      woman_firefighter: "👩&zwj;🚒",
      woman_health_worker: "👩&zwj;⚕️",
      woman_judge: "👩&zwj;⚖️",
      woman_juggling: "🤹&zwj;♀️",
      woman_mechanic: "👩&zwj;🔧",
      woman_office_worker: "👩&zwj;💼",
      woman_pilot: "👩&zwj;✈️",
      woman_playing_handball: "🤾&zwj;♀️",
      woman_playing_water_polo: "🤽&zwj;♀️",
      woman_scientist: "👩&zwj;🔬",
      woman_shrugging: "🤷&zwj;♀️",
      woman_singer: "👩&zwj;🎤",
      woman_student: "👩&zwj;🎓",
      woman_teacher: "👩&zwj;🏫",
      woman_technologist: "👩&zwj;💻",
      woman_with_turban: "👳&zwj;♀️",
      womans_clothes: "👚",
      womans_hat: "👒",
      women_wrestling: "🤼&zwj;♀️",
      womens: "🚺",
      world_map: "🗺",
      worried: "😟",
      wrench: "🔧",
      writing_hand: "✍️",
      x: "❌",
      yellow_heart: "💛",
      yen: "💴",
      yin_yang: "☯️",
      yum: "😋",
      zap: "⚡️",
      zipper_mouth_face: "🤐",
      zzz: "💤",
      octocat: '<img alt=":octocat:" height="20" width="20" align="absmiddle" src="https://assets-cdn.github.com/images/icons/emoji/octocat.png">',
      showdown: `<span style="font-family: 'Anonymous Pro', monospace; text-decoration: underline; text-decoration-style: dashed; text-decoration-color: #3e8b8a;text-underline-position: under;">S</span>`
    }, s.Converter = function(p) {
      "use strict";
      var w = {}, _ = [], b = [], C = {}, k = i, P = {
        parsed: {},
        raw: "",
        format: ""
      };
      H();
      function H() {
        p = p || {};
        for (var R in l) l.hasOwnProperty(R) && (w[R] = l[R]);
        if (typeof p == "object")
          for (var V in p) p.hasOwnProperty(V) && (w[V] = p[V]);
        else throw Error("Converter expects the passed parameter to be an object, but " + typeof p + " was passed instead.");
        w.extensions && s.helper.forEach(w.extensions, G);
      }
      function G(R, V) {
        if (V = V || null, s.helper.isString(R))
          if (R = s.helper.stdExtName(R), V = R, s.extensions[R]) {
            console.warn("DEPRECATION WARNING: " + R + " is an old extension that uses a deprecated loading method.Please inform the developer that the extension should be updated!"), ee(s.extensions[R], R);
            return;
          } else if (!s.helper.isUndefined(o[R])) R = o[R];
          else throw Error('Extension "' + R + '" could not be loaded. It was either not found or is not a valid extension.');
        typeof R == "function" && (R = R()), s.helper.isArray(R) || (R = [R]);
        var le = d(R, V);
        if (!le.valid) throw Error(le.error);
        for (var ve = 0; ve < R.length; ++ve) {
          switch (R[ve].type) {
            case "lang":
              _.push(R[ve]);
              break;
            case "output":
              b.push(R[ve]);
              break;
          }
          if (R[ve].hasOwnProperty("listeners"))
            for (var ke in R[ve].listeners) R[ve].listeners.hasOwnProperty(ke) && J(ke, R[ve].listeners[ke]);
        }
      }
      function ee(R, V) {
        typeof R == "function" && (R = R(new s.Converter())), s.helper.isArray(R) || (R = [R]);
        var le = d(R, V);
        if (!le.valid) throw Error(le.error);
        for (var ve = 0; ve < R.length; ++ve) switch (R[ve].type) {
          case "lang":
            _.push(R[ve]);
            break;
          case "output":
            b.push(R[ve]);
            break;
          default:
            throw Error("Extension loader error: Type unrecognized!!!");
        }
      }
      function J(R, V) {
        if (!s.helper.isString(R)) throw Error("Invalid argument in converter.listen() method: name must be a string, but " + typeof R + " given");
        if (typeof V != "function") throw Error("Invalid argument in converter.listen() method: callback must be a function, but " + typeof V + " given");
        C.hasOwnProperty(R) || (C[R] = []), C[R].push(V);
      }
      function z(R) {
        var V = R.match(/^\s*/)[0].length, le = new RegExp("^\\s{0," + V + "}", "gm");
        return R.replace(le, "");
      }
      this._dispatch = function(V, le, ve, ke) {
        if (C.hasOwnProperty(V)) for (var be = 0; be < C[V].length; ++be) {
          var Ne = C[V][be](V, le, this, ve, ke);
          Ne && typeof Ne < "u" && (le = Ne);
        }
        return le;
      }, this.listen = function(R, V) {
        return J(R, V), this;
      }, this.makeHtml = function(R) {
        if (!R) return R;
        var V = {
          gHtmlBlocks: [],
          gHtmlMdBlocks: [],
          gHtmlSpans: [],
          gUrls: {},
          gTitles: {},
          gDimensions: {},
          gListLevel: 0,
          hashLinkCounts: {},
          langExtensions: _,
          outputModifiers: b,
          converter: this,
          ghCodeBlocks: [],
          metadata: {
            parsed: {},
            raw: "",
            format: ""
          }
        };
        return R = R.replace(/¨/g, "¨T"), R = R.replace(/\$/g, "¨D"), R = R.replace(/\r\n/g, `
`), R = R.replace(/\r/g, `
`), R = R.replace(/\u00A0/g, "&nbsp;"), w.smartIndentationFix && (R = z(R)), R = `

` + R + `

`, R = s.subParser("detab")(R, w, V), R = R.replace(/^[ \t]+$/gm, ""), s.helper.forEach(_, function(le) {
          R = s.subParser("runExtension")(le, R, w, V);
        }), R = s.subParser("metadata")(R, w, V), R = s.subParser("hashPreCodeTags")(R, w, V), R = s.subParser("githubCodeBlocks")(R, w, V), R = s.subParser("hashHTMLBlocks")(R, w, V), R = s.subParser("hashCodeTags")(R, w, V), R = s.subParser("stripLinkDefinitions")(R, w, V), R = s.subParser("blockGamut")(R, w, V), R = s.subParser("unhashHTMLSpans")(R, w, V), R = s.subParser("unescapeSpecialChars")(R, w, V), R = R.replace(/¨D/g, "$$"), R = R.replace(/¨T/g, "¨"), R = s.subParser("completeHTMLDocument")(R, w, V), s.helper.forEach(b, function(le) {
          R = s.subParser("runExtension")(le, R, w, V);
        }), P = V.metadata, R;
      }, this.makeMarkdown = this.makeMd = function(R, V) {
        if (R = R.replace(/\r\n/g, `
`), R = R.replace(/\r/g, `
`), R = R.replace(/>[ \t]+</, ">¨NBSP;<"), !V) if (window && window.document) V = window.document;
        else throw new Error("HTMLParser is undefined. If in a webworker or nodejs environment, you need to provide a WHATWG DOM and HTML such as JSDOM");
        var le = V.createElement("div");
        le.innerHTML = R;
        var ve = { preList: we(le) };
        ce(le);
        for (var ke = le.childNodes, be = "", Ne = 0; Ne < ke.length; Ne++) be += s.subParser("makeMarkdown.node")(ke[Ne], ve);
        function ce(Oe) {
          for (var F = 0; F < Oe.childNodes.length; ++F) {
            var ae = Oe.childNodes[F];
            ae.nodeType === 3 ? !/\S/.test(ae.nodeValue) && !/^[ ]+$/.test(ae.nodeValue) ? (Oe.removeChild(ae), --F) : (ae.nodeValue = ae.nodeValue.split(`
`).join(" "), ae.nodeValue = ae.nodeValue.replace(/(\s)+/g, "$1")) : ae.nodeType === 1 && ce(ae);
          }
        }
        function we(Oe) {
          for (var F = Oe.querySelectorAll("pre"), ae = [], Z = 0; Z < F.length; ++Z) if (F[Z].childElementCount === 1 && F[Z].firstChild.tagName.toLowerCase() === "code") {
            var ie = F[Z].firstChild.innerHTML.trim(), me = F[Z].firstChild.getAttribute("data-language") || "";
            if (me === "")
              for (var Se = F[Z].firstChild.className.split(" "), W = 0; W < Se.length; ++W) {
                var L = Se[W].match(/^language-(.+)$/);
                if (L !== null) {
                  me = L[1];
                  break;
                }
              }
            ie = s.helper.unescapeHTMLEntities(ie), ae.push(ie), F[Z].outerHTML = '<precode language="' + me + '" precodenum="' + Z.toString() + '"></precode>';
          } else
            ae.push(F[Z].innerHTML), F[Z].innerHTML = "", F[Z].setAttribute("prenum", Z.toString());
          return ae;
        }
        return be;
      }, this.setOption = function(R, V) {
        w[R] = V;
      }, this.getOption = function(R) {
        return w[R];
      }, this.getOptions = function() {
        return w;
      }, this.addExtension = function(R, V) {
        V = V || null, G(R, V);
      }, this.useExtension = function(R) {
        G(R);
      }, this.setFlavor = function(R) {
        if (!u.hasOwnProperty(R)) throw Error(R + " flavor was not found");
        var V = u[R];
        k = R;
        for (var le in V) V.hasOwnProperty(le) && (w[le] = V[le]);
      }, this.getFlavor = function() {
        return k;
      }, this.removeExtension = function(R) {
        s.helper.isArray(R) || (R = [R]);
        for (var V = 0; V < R.length; ++V) {
          for (var le = R[V], ve = 0; ve < _.length; ++ve) _[ve] === le && _.splice(ve, 1);
          for (var ke = 0; ke < b.length; ++ke) b[ke] === le && b.splice(ke, 1);
        }
      }, this.getAllExtensions = function() {
        return {
          language: _,
          output: b
        };
      }, this.getMetadata = function(R) {
        return R ? P.raw : P.parsed;
      }, this.getMetadataFormat = function() {
        return P.format;
      }, this._setMetadataPair = function(R, V) {
        P.parsed[R] = V;
      }, this._setMetadataFormat = function(R) {
        P.format = R;
      }, this._setMetadataRaw = function(R) {
        P.raw = R;
      };
    }, s.subParser("anchors", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("anchors.before", p, w, _);
      var b = function(C, k, P, H, G, ee, J) {
        if (s.helper.isUndefined(J) && (J = ""), P = P.toLowerCase(), C.search(/\(<?\s*>? ?(['"].*['"])?\)$/m) > -1) H = "";
        else if (!H)
          if (P || (P = k.toLowerCase().replace(/ ?\n/g, " ")), H = "#" + P, !s.helper.isUndefined(_.gUrls[P]))
            H = _.gUrls[P], s.helper.isUndefined(_.gTitles[P]) || (J = _.gTitles[P]);
          else return C;
        H = H.replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback);
        var z = '<a href="' + H + '"';
        return J !== "" && J !== null && (J = J.replace(/"/g, "&quot;"), J = J.replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback), z += ' title="' + J + '"'), w.openLinksInNewWindow && !/^#/.test(H) && (z += ' rel="noopener noreferrer" target="¨E95Eblank"'), z += ">" + k + "</a>", z;
      };
      return p = p.replace(/\[((?:\[[^\]]*]|[^\[\]])*)] ?(?:\n *)?\[(.*?)]()()()()/g, b), p = p.replace(/\[((?:\[[^\]]*]|[^\[\]])*)]()[ \t]*\([ \t]?<([^>]*)>(?:[ \t]*((["'])([^"]*?)\5))?[ \t]?\)/g, b), p = p.replace(/\[((?:\[[^\]]*]|[^\[\]])*)]()[ \t]*\([ \t]?<?([\S]+?(?:\([\S]*?\)[\S]*?)?)>?(?:[ \t]*((["'])([^"]*?)\5))?[ \t]?\)/g, b), p = p.replace(/\[([^\[\]]+)]()()()()()/g, b), w.ghMentions && (p = p.replace(/(^|\s)(\\)?(@([a-z\d]+(?:[a-z\d.-]+?[a-z\d]+)*))/gim, function(C, k, P, H, G) {
        if (P === "\\") return k + H;
        if (!s.helper.isString(w.ghMentionsLink)) throw new Error("ghMentionsLink option must be a string");
        var ee = w.ghMentionsLink.replace(/\{u}/g, G), J = "";
        return w.openLinksInNewWindow && (J = ' rel="noopener noreferrer" target="¨E95Eblank"'), k + '<a href="' + ee + '"' + J + ">" + H + "</a>";
      })), p = _.converter._dispatch("anchors.after", p, w, _), p;
    });
    var v = /([*~_]+|\b)(((https?|ftp|dict):\/\/|www\.)[^'">\s]+?\.[^'">\s]+?)()(\1)?(?=\s|$)(?!["<>])/gi, y = /([*~_]+|\b)(((https?|ftp|dict):\/\/|www\.)[^'">\s]+\.[^'">\s]+?)([.!?,()\[\]])?(\1)?(?=\s|$)(?!["<>])/gi, S = /()<(((https?|ftp|dict):\/\/|www\.)[^'">\s]+)()>()/gi, x = /(^|\s)(?:mailto:)?([A-Za-z0-9!#$%&'*+-/=?^_`{|}~.]+@[-a-z0-9]+(\.[-a-z0-9]+)*\.[a-z]+)(?=$|\s)/gim, I = /<()(?:mailto:)?([-.\w]+@[-a-z0-9]+(\.[-a-z0-9]+)*\.[a-z]+)>/gi, E = function(p) {
      "use strict";
      return function(w, _, b, C, k, P, H) {
        b = b.replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback);
        var G = b, ee = "", J = "", z = _ || "", R = H || "";
        return /^www\./i.test(b) && (b = b.replace(/^www\./i, "http://www.")), p.excludeTrailingPunctuationFromURLs && P && (ee = P), p.openLinksInNewWindow && (J = ' rel="noopener noreferrer" target="¨E95Eblank"'), z + '<a href="' + b + '"' + J + ">" + G + "</a>" + ee + R;
      };
    }, N = function(p, w) {
      "use strict";
      return function(_, b, C) {
        var k = "mailto:";
        return b = b || "", C = s.subParser("unescapeSpecialChars")(C, p, w), p.encodeEmails ? (k = s.helper.encodeEmailAddress(k + C), C = s.helper.encodeEmailAddress(C)) : k = k + C, b + '<a href="' + k + '">' + C + "</a>";
      };
    };
    s.subParser("autoLinks", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("autoLinks.before", p, w, _), p = p.replace(S, E(w)), p = p.replace(I, N(w, _)), p = _.converter._dispatch("autoLinks.after", p, w, _), p;
    }), s.subParser("simplifiedAutoLinks", function(p, w, _) {
      "use strict";
      return w.simplifiedAutoLink && (p = _.converter._dispatch("simplifiedAutoLinks.before", p, w, _), w.excludeTrailingPunctuationFromURLs ? p = p.replace(y, E(w)) : p = p.replace(v, E(w)), p = p.replace(x, N(w, _)), p = _.converter._dispatch("simplifiedAutoLinks.after", p, w, _)), p;
    }), s.subParser("blockGamut", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("blockGamut.before", p, w, _), p = s.subParser("blockQuotes")(p, w, _), p = s.subParser("headers")(p, w, _), p = s.subParser("horizontalRule")(p, w, _), p = s.subParser("lists")(p, w, _), p = s.subParser("codeBlocks")(p, w, _), p = s.subParser("tables")(p, w, _), p = s.subParser("hashHTMLBlocks")(p, w, _), p = s.subParser("paragraphs")(p, w, _), p = _.converter._dispatch("blockGamut.after", p, w, _), p;
    }), s.subParser("blockQuotes", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("blockQuotes.before", p, w, _), p = p + `

`;
      var b = /(^ {0,3}>[ \t]?.+\n(.+\n)*\n*)+/gm;
      return w.splitAdjacentBlockquotes && (b = /^ {0,3}>[\s\S]*?(?:\n\n)/gm), p = p.replace(b, function(C) {
        return C = C.replace(/^[ \t]*>[ \t]?/gm, ""), C = C.replace(/¨0/g, ""), C = C.replace(/^[ \t]+$/gm, ""), C = s.subParser("githubCodeBlocks")(C, w, _), C = s.subParser("blockGamut")(C, w, _), C = C.replace(/(^|\n)/g, "$1  "), C = C.replace(/(\s*<pre>[^\r]+?<\/pre>)/gm, function(k, P) {
          var H = P;
          return H = H.replace(/^  /gm, "¨0"), H = H.replace(/¨0/g, ""), H;
        }), s.subParser("hashBlock")(`<blockquote>
` + C + `
</blockquote>`, w, _);
      }), p = _.converter._dispatch("blockQuotes.after", p, w, _), p;
    }), s.subParser("codeBlocks", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("codeBlocks.before", p, w, _), p += "¨0", p = p.replace(/(?:\n\n|^)((?:(?:[ ]{4}|\t).*\n+)+)(\n*[ ]{0,3}[^ \t\n]|(?=¨0))/g, function(b, C, k) {
        var P = C, H = k, G = `
`;
        return P = s.subParser("outdent")(P, w, _), P = s.subParser("encodeCode")(P, w, _), P = s.subParser("detab")(P, w, _), P = P.replace(/^\n+/g, ""), P = P.replace(/\n+$/g, ""), w.omitExtraWLInCodeBlocks && (G = ""), P = "<pre><code>" + P + G + "</code></pre>", s.subParser("hashBlock")(P, w, _) + H;
      }), p = p.replace(/¨0/, ""), p = _.converter._dispatch("codeBlocks.after", p, w, _), p;
    }), s.subParser("codeSpans", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("codeSpans.before", p, w, _), typeof p > "u" && (p = ""), p = p.replace(/(^|[^\\])(`+)([^\r]*?[^`])\2(?!`)/gm, function(b, C, k, P) {
        var H = P;
        return H = H.replace(/^([ \t]*)/g, ""), H = H.replace(/[ \t]*$/g, ""), H = s.subParser("encodeCode")(H, w, _), H = C + "<code>" + H + "</code>", H = s.subParser("hashHTMLSpans")(H, w, _), H;
      }), p = _.converter._dispatch("codeSpans.after", p, w, _), p;
    }), s.subParser("completeHTMLDocument", function(p, w, _) {
      "use strict";
      if (!w.completeHTMLDocument) return p;
      p = _.converter._dispatch("completeHTMLDocument.before", p, w, _);
      var b = "html", C = `<!DOCTYPE HTML>
`, k = "", P = `<meta charset="utf-8">
`, H = "", G = "";
      typeof _.metadata.parsed.doctype < "u" && (C = "<!DOCTYPE " + _.metadata.parsed.doctype + `>
`, b = _.metadata.parsed.doctype.toString().toLowerCase(), (b === "html" || b === "html5") && (P = '<meta charset="utf-8">'));
      for (var ee in _.metadata.parsed) if (_.metadata.parsed.hasOwnProperty(ee)) switch (ee.toLowerCase()) {
        case "doctype":
          break;
        case "title":
          k = "<title>" + _.metadata.parsed.title + `</title>
`;
          break;
        case "charset":
          b === "html" || b === "html5" ? P = '<meta charset="' + _.metadata.parsed.charset + `">
` : P = '<meta name="charset" content="' + _.metadata.parsed.charset + `">
`;
          break;
        case "language":
        case "lang":
          H = ' lang="' + _.metadata.parsed[ee] + '"', G += '<meta name="' + ee + '" content="' + _.metadata.parsed[ee] + `">
`;
          break;
        default:
          G += '<meta name="' + ee + '" content="' + _.metadata.parsed[ee] + `">
`;
      }
      return p = C + "<html" + H + `>
<head>
` + k + P + G + `</head>
<body>
` + p.trim() + `
</body>
</html>`, p = _.converter._dispatch("completeHTMLDocument.after", p, w, _), p;
    }), s.subParser("detab", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("detab.before", p, w, _), p = p.replace(/\t(?=\t)/g, "    "), p = p.replace(/\t/g, "¨A¨B"), p = p.replace(/¨B(.+?)¨A/g, function(b, C) {
        for (var k = C, P = 4 - k.length % 4, H = 0; H < P; H++) k += " ";
        return k;
      }), p = p.replace(/¨A/g, "    "), p = p.replace(/¨B/g, ""), p = _.converter._dispatch("detab.after", p, w, _), p;
    }), s.subParser("ellipsis", function(p, w, _) {
      "use strict";
      return w.ellipsis && (p = _.converter._dispatch("ellipsis.before", p, w, _), p = p.replace(/\.\.\./g, "…"), p = _.converter._dispatch("ellipsis.after", p, w, _)), p;
    }), s.subParser("emoji", function(p, w, _) {
      "use strict";
      return w.emoji && (p = _.converter._dispatch("emoji.before", p, w, _), p = p.replace(/:([\S]+?):/g, function(b, C) {
        return s.helper.emojis.hasOwnProperty(C) ? s.helper.emojis[C] : b;
      }), p = _.converter._dispatch("emoji.after", p, w, _)), p;
    }), s.subParser("encodeAmpsAndAngles", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("encodeAmpsAndAngles.before", p, w, _), p = p.replace(/&(?!#?[xX]?(?:[0-9a-fA-F]+|\w+);)/g, "&amp;"), p = p.replace(/<(?![a-z\/?$!])/gi, "&lt;"), p = p.replace(/</g, "&lt;"), p = p.replace(/>/g, "&gt;"), p = _.converter._dispatch("encodeAmpsAndAngles.after", p, w, _), p;
    }), s.subParser("encodeBackslashEscapes", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("encodeBackslashEscapes.before", p, w, _), p = p.replace(/\\(\\)/g, s.helper.escapeCharactersCallback), p = p.replace(/\\([`*_{}\[\]()>#+.!~=|:-])/g, s.helper.escapeCharactersCallback), p = _.converter._dispatch("encodeBackslashEscapes.after", p, w, _), p;
    }), s.subParser("encodeCode", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("encodeCode.before", p, w, _), p = p.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/([*_{}\[\]\\=~-])/g, s.helper.escapeCharactersCallback), p = _.converter._dispatch("encodeCode.after", p, w, _), p;
    }), s.subParser("escapeSpecialCharsWithinTagAttributes", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("escapeSpecialCharsWithinTagAttributes.before", p, w, _);
      var b = /<\/?[a-z\d_:-]+(?:[\s]+[\s\S]+?)?>/gi, C = /<!(--(?:(?:[^>-]|-[^>])(?:[^-]|-[^-])*)--)>/gi;
      return p = p.replace(b, function(k) {
        return k.replace(/(.)<\/?code>(?=.)/g, "$1`").replace(/([\\`*_~=|])/g, s.helper.escapeCharactersCallback);
      }), p = p.replace(C, function(k) {
        return k.replace(/([\\`*_~=|])/g, s.helper.escapeCharactersCallback);
      }), p = _.converter._dispatch("escapeSpecialCharsWithinTagAttributes.after", p, w, _), p;
    }), s.subParser("githubCodeBlocks", function(p, w, _) {
      "use strict";
      return w.ghCodeBlocks ? (p = _.converter._dispatch("githubCodeBlocks.before", p, w, _), p += "¨0", p = p.replace(/(?:^|\n)(?: {0,3})(```+|~~~+)(?: *)([^\s`~]*)\n([\s\S]*?)\n(?: {0,3})\1/g, function(b, C, k, P) {
        var H = w.omitExtraWLInCodeBlocks ? "" : `
`;
        return P = s.subParser("encodeCode")(P, w, _), P = s.subParser("detab")(P, w, _), P = P.replace(/^\n+/g, ""), P = P.replace(/\n+$/g, ""), P = "<pre><code" + (k ? ' class="' + k + " language-" + k + '"' : "") + ">" + P + H + "</code></pre>", P = s.subParser("hashBlock")(P, w, _), `

¨G` + (_.ghCodeBlocks.push({
          text: b,
          codeblock: P
        }) - 1) + `G

`;
      }), p = p.replace(/¨0/, ""), _.converter._dispatch("githubCodeBlocks.after", p, w, _)) : p;
    }), s.subParser("hashBlock", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("hashBlock.before", p, w, _), p = p.replace(/(^\n+|\n+$)/g, ""), p = `

¨K` + (_.gHtmlBlocks.push(p) - 1) + `K

`, p = _.converter._dispatch("hashBlock.after", p, w, _), p;
    }), s.subParser("hashCodeTags", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("hashCodeTags.before", p, w, _);
      var b = function(C, k, P, H) {
        var G = P + s.subParser("encodeCode")(k, w, _) + H;
        return "¨C" + (_.gHtmlSpans.push(G) - 1) + "C";
      };
      return p = s.helper.replaceRecursiveRegExp(p, b, "<code\\b[^>]*>", "</code>", "gim"), p = _.converter._dispatch("hashCodeTags.after", p, w, _), p;
    }), s.subParser("hashElement", function(p, w, _) {
      "use strict";
      return function(b, C) {
        var k = C;
        return k = k.replace(/\n\n/g, `
`), k = k.replace(/^\n/, ""), k = k.replace(/\n+$/g, ""), k = `

¨K` + (_.gHtmlBlocks.push(k) - 1) + `K

`, k;
      };
    }), s.subParser("hashHTMLBlocks", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("hashHTMLBlocks.before", p, w, _);
      var b = [
        "pre",
        "div",
        "h1",
        "h2",
        "h3",
        "h4",
        "h5",
        "h6",
        "blockquote",
        "table",
        "dl",
        "ol",
        "ul",
        "script",
        "noscript",
        "form",
        "fieldset",
        "iframe",
        "math",
        "style",
        "section",
        "header",
        "footer",
        "nav",
        "article",
        "aside",
        "address",
        "audio",
        "canvas",
        "figure",
        "hgroup",
        "output",
        "video",
        "p"
      ], C = function(R, V, le, ve) {
        var ke = R;
        return le.search(/\bmarkdown\b/) !== -1 && (ke = le + _.converter.makeHtml(V) + ve), `

¨K` + (_.gHtmlBlocks.push(ke) - 1) + `K

`;
      };
      w.backslashEscapesHTMLTags && (p = p.replace(/\\<(\/?[^>]+?)>/g, function(R, V) {
        return "&lt;" + V + "&gt;";
      }));
      for (var k = 0; k < b.length; ++k)
        for (var P, H = new RegExp("^ {0,3}(<" + b[k] + "\\b[^>]*>)", "im"), G = "<" + b[k] + "\\b[^>]*>", ee = "</" + b[k] + ">"; (P = s.helper.regexIndexOf(p, H)) !== -1; ) {
          var J = s.helper.splitAtIndex(p, P), z = s.helper.replaceRecursiveRegExp(J[1], C, G, ee, "im");
          if (z === J[1]) break;
          p = J[0].concat(z);
        }
      return p = p.replace(/(\n {0,3}(<(hr)\b([^<>])*?\/?>)[ \t]*(?=\n{2,}))/g, s.subParser("hashElement")(p, w, _)), p = s.helper.replaceRecursiveRegExp(p, function(R) {
        return `

¨K` + (_.gHtmlBlocks.push(R) - 1) + `K

`;
      }, "^ {0,3}<!--", "-->", "gm"), p = p.replace(/(?:\n\n)( {0,3}(?:<([?%])[^\r]*?\2>)[ \t]*(?=\n{2,}))/g, s.subParser("hashElement")(p, w, _)), p = _.converter._dispatch("hashHTMLBlocks.after", p, w, _), p;
    }), s.subParser("hashHTMLSpans", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("hashHTMLSpans.before", p, w, _);
      function b(C) {
        return "¨C" + (_.gHtmlSpans.push(C) - 1) + "C";
      }
      return p = p.replace(/<[^>]+?\/>/gi, function(C) {
        return b(C);
      }), p = p.replace(/<([^>]+?)>[\s\S]*?<\/\1>/g, function(C) {
        return b(C);
      }), p = p.replace(/<([^>]+?)\s[^>]+?>[\s\S]*?<\/\1>/g, function(C) {
        return b(C);
      }), p = p.replace(/<[^>]+?>/gi, function(C) {
        return b(C);
      }), p = _.converter._dispatch("hashHTMLSpans.after", p, w, _), p;
    }), s.subParser("unhashHTMLSpans", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("unhashHTMLSpans.before", p, w, _);
      for (var b = 0; b < _.gHtmlSpans.length; ++b) {
        for (var C = _.gHtmlSpans[b], k = 0; /¨C(\d+)C/.test(C); ) {
          var P = RegExp.$1;
          if (C = C.replace("¨C" + P + "C", _.gHtmlSpans[P]), k === 10) {
            console.error("maximum nesting of 10 spans reached!!!");
            break;
          }
          ++k;
        }
        p = p.replace("¨C" + b + "C", C);
      }
      return p = _.converter._dispatch("unhashHTMLSpans.after", p, w, _), p;
    }), s.subParser("hashPreCodeTags", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("hashPreCodeTags.before", p, w, _);
      var b = function(C, k, P, H) {
        var G = P + s.subParser("encodeCode")(k, w, _) + H;
        return `

¨G` + (_.ghCodeBlocks.push({
          text: C,
          codeblock: G
        }) - 1) + `G

`;
      };
      return p = s.helper.replaceRecursiveRegExp(p, b, "^ {0,3}<pre\\b[^>]*>\\s*<code\\b[^>]*>", "^ {0,3}</code>\\s*</pre>", "gim"), p = _.converter._dispatch("hashPreCodeTags.after", p, w, _), p;
    }), s.subParser("headers", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("headers.before", p, w, _);
      var b = isNaN(parseInt(w.headerLevelStart)) ? 1 : parseInt(w.headerLevelStart), C = w.smoothLivePreview ? /^(.+)[ \t]*\n={2,}[ \t]*\n+/gm : /^(.+)[ \t]*\n=+[ \t]*\n+/gm, k = w.smoothLivePreview ? /^(.+)[ \t]*\n-{2,}[ \t]*\n+/gm : /^(.+)[ \t]*\n-+[ \t]*\n+/gm;
      p = p.replace(C, function(G, ee) {
        var J = s.subParser("spanGamut")(ee, w, _), z = w.noHeaderId ? "" : ' id="' + H(ee) + '"', R = b, V = "<h" + R + z + ">" + J + "</h" + R + ">";
        return s.subParser("hashBlock")(V, w, _);
      }), p = p.replace(k, function(G, ee) {
        var J = s.subParser("spanGamut")(ee, w, _), z = w.noHeaderId ? "" : ' id="' + H(ee) + '"', R = b + 1, V = "<h" + R + z + ">" + J + "</h" + R + ">";
        return s.subParser("hashBlock")(V, w, _);
      });
      var P = w.requireSpaceBeforeHeadingText ? /^(#{1,6})[ \t]+(.+?)[ \t]*#*\n+/gm : /^(#{1,6})[ \t]*(.+?)[ \t]*#*\n+/gm;
      p = p.replace(P, function(G, ee, J) {
        var z = J;
        w.customizedHeaderId && (z = J.replace(/\s?\{([^{]+?)}\s*$/, ""));
        var R = s.subParser("spanGamut")(z, w, _), V = w.noHeaderId ? "" : ' id="' + H(J) + '"', le = b - 1 + ee.length, ve = "<h" + le + V + ">" + R + "</h" + le + ">";
        return s.subParser("hashBlock")(ve, w, _);
      });
      function H(G) {
        var ee, J;
        if (w.customizedHeaderId) {
          var z = G.match(/\{([^{]+?)}\s*$/);
          z && z[1] && (G = z[1]);
        }
        return ee = G, s.helper.isString(w.prefixHeaderId) ? J = w.prefixHeaderId : w.prefixHeaderId === !0 ? J = "section-" : J = "", w.rawPrefixHeaderId || (ee = J + ee), w.ghCompatibleHeaderId ? ee = ee.replace(/ /g, "-").replace(/&amp;/g, "").replace(/¨T/g, "").replace(/¨D/g, "").replace(/[&+$,\/:;=?@"#{}|^¨~\[\]`\\*)(%.!'<>]/g, "").toLowerCase() : w.rawHeaderId ? ee = ee.replace(/ /g, "-").replace(/&amp;/g, "&").replace(/¨T/g, "¨").replace(/¨D/g, "$").replace(/["']/g, "-").toLowerCase() : ee = ee.replace(/[^\w]/g, "").toLowerCase(), w.rawPrefixHeaderId && (ee = J + ee), _.hashLinkCounts[ee] ? ee = ee + "-" + _.hashLinkCounts[ee]++ : _.hashLinkCounts[ee] = 1, ee;
      }
      return p = _.converter._dispatch("headers.after", p, w, _), p;
    }), s.subParser("horizontalRule", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("horizontalRule.before", p, w, _);
      var b = s.subParser("hashBlock")("<hr />", w, _);
      return p = p.replace(/^ {0,2}( ?-){3,}[ \t]*$/gm, b), p = p.replace(/^ {0,2}( ?\*){3,}[ \t]*$/gm, b), p = p.replace(/^ {0,2}( ?_){3,}[ \t]*$/gm, b), p = _.converter._dispatch("horizontalRule.after", p, w, _), p;
    }), s.subParser("images", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("images.before", p, w, _);
      var b = /!\[([^\]]*?)][ \t]*()\([ \t]?<?([\S]+?(?:\([\S]*?\)[\S]*?)?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g, C = /!\[([^\]]*?)][ \t]*()\([ \t]?<([^>]*)>(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(?:(["'])([^"]*?)\6))?[ \t]?\)/g, k = /!\[([^\]]*?)][ \t]*()\([ \t]?<?(data:.+?\/.+?;base64,[A-Za-z0-9+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g, P = /!\[([^\]]*?)] ?(?:\n *)?\[([\s\S]*?)]()()()()()/g, H = /!\[([^\[\]]+)]()()()()()/g;
      function G(J, z, R, V, le, ve, ke, be) {
        return V = V.replace(/\s/g, ""), ee(J, z, R, V, le, ve, ke, be);
      }
      function ee(J, z, R, V, le, ve, ke, be) {
        var Ne = _.gUrls, ce = _.gTitles, we = _.gDimensions;
        if (R = R.toLowerCase(), be || (be = ""), J.search(/\(<?\s*>? ?(['"].*['"])?\)$/m) > -1) V = "";
        else if (V === "" || V === null)
          if ((R === "" || R === null) && (R = z.toLowerCase().replace(/ ?\n/g, " ")), V = "#" + R, !s.helper.isUndefined(Ne[R]))
            V = Ne[R], s.helper.isUndefined(ce[R]) || (be = ce[R]), s.helper.isUndefined(we[R]) || (le = we[R].width, ve = we[R].height);
          else return J;
        z = z.replace(/"/g, "&quot;").replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback), V = V.replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback);
        var Oe = '<img src="' + V + '" alt="' + z + '"';
        return be && s.helper.isString(be) && (be = be.replace(/"/g, "&quot;").replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback), Oe += ' title="' + be + '"'), le && ve && (le = le === "*" ? "auto" : le, ve = ve === "*" ? "auto" : ve, Oe += ' width="' + le + '"', Oe += ' height="' + ve + '"'), Oe += " />", Oe;
      }
      return p = p.replace(P, ee), p = p.replace(k, G), p = p.replace(C, ee), p = p.replace(b, ee), p = p.replace(H, ee), p = _.converter._dispatch("images.after", p, w, _), p;
    }), s.subParser("italicsAndBold", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("italicsAndBold.before", p, w, _);
      function b(C, k, P) {
        return k + C + P;
      }
      return w.literalMidWordUnderscores ? (p = p.replace(/\b___(\S[\s\S]*?)___\b/g, function(C, k) {
        return b(k, "<strong><em>", "</em></strong>");
      }), p = p.replace(/\b__(\S[\s\S]*?)__\b/g, function(C, k) {
        return b(k, "<strong>", "</strong>");
      }), p = p.replace(/\b_(\S[\s\S]*?)_\b/g, function(C, k) {
        return b(k, "<em>", "</em>");
      })) : (p = p.replace(/___(\S[\s\S]*?)___/g, function(C, k) {
        return /\S$/.test(k) ? b(k, "<strong><em>", "</em></strong>") : C;
      }), p = p.replace(/__(\S[\s\S]*?)__/g, function(C, k) {
        return /\S$/.test(k) ? b(k, "<strong>", "</strong>") : C;
      }), p = p.replace(/_([^\s_][\s\S]*?)_/g, function(C, k) {
        return /\S$/.test(k) ? b(k, "<em>", "</em>") : C;
      })), w.literalMidWordAsterisks ? (p = p.replace(/([^*]|^)\B\*\*\*(\S[\s\S]*?)\*\*\*\B(?!\*)/g, function(C, k, P) {
        return b(P, k + "<strong><em>", "</em></strong>");
      }), p = p.replace(/([^*]|^)\B\*\*(\S[\s\S]*?)\*\*\B(?!\*)/g, function(C, k, P) {
        return b(P, k + "<strong>", "</strong>");
      }), p = p.replace(/([^*]|^)\B\*(\S[\s\S]*?)\*\B(?!\*)/g, function(C, k, P) {
        return b(P, k + "<em>", "</em>");
      })) : (p = p.replace(/\*\*\*(\S[\s\S]*?)\*\*\*/g, function(C, k) {
        return /\S$/.test(k) ? b(k, "<strong><em>", "</em></strong>") : C;
      }), p = p.replace(/\*\*(\S[\s\S]*?)\*\*/g, function(C, k) {
        return /\S$/.test(k) ? b(k, "<strong>", "</strong>") : C;
      }), p = p.replace(/\*([^\s*][\s\S]*?)\*/g, function(C, k) {
        return /\S$/.test(k) ? b(k, "<em>", "</em>") : C;
      })), p = _.converter._dispatch("italicsAndBold.after", p, w, _), p;
    }), s.subParser("lists", function(p, w, _) {
      "use strict";
      function b(P, H) {
        _.gListLevel++, P = P.replace(/\n{2,}$/, `
`), P += "¨0";
        var G = /(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[(x|X| )?])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0| {0,3}([*+-]|\d+[.])[ \t]+))/gm, ee = /\n[ \t]*\n(?!¨0)/.test(P);
        return w.disableForced4SpacesIndentedSublists && (G = /(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[(x|X| )?])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0|\2([*+-]|\d+[.])[ \t]+))/gm), P = P.replace(G, function(J, z, R, V, le, ve, ke) {
          ke = ke && ke.trim() !== "";
          var be = s.subParser("outdent")(le, w, _), Ne = "";
          return ve && w.tasklists && (Ne = ' class="task-list-item" style="list-style-type: none;"', be = be.replace(/^[ \t]*\[(x|X| )?]/m, function() {
            var ce = '<input type="checkbox" disabled style="margin: 0px 0.35em 0.25em -1.6em; vertical-align: middle;"';
            return ke && (ce += " checked"), ce += ">", ce;
          })), be = be.replace(/^([-*+]|\d\.)[ \t]+[\S\n ]*/g, function(ce) {
            return "¨A" + ce;
          }), z || be.search(/\n{2,}/) > -1 ? (be = s.subParser("githubCodeBlocks")(be, w, _), be = s.subParser("blockGamut")(be, w, _)) : (be = s.subParser("lists")(be, w, _), be = be.replace(/\n$/, ""), be = s.subParser("hashHTMLBlocks")(be, w, _), be = be.replace(/\n\n+/g, `

`), ee ? be = s.subParser("paragraphs")(be, w, _) : be = s.subParser("spanGamut")(be, w, _)), be = be.replace("¨A", ""), be = "<li" + Ne + ">" + be + `</li>
`, be;
        }), P = P.replace(/¨0/g, ""), _.gListLevel--, H && (P = P.replace(/\s+$/, "")), P;
      }
      function C(P, H) {
        if (H === "ol") {
          var G = P.match(/^ *(\d+)\./);
          if (G && G[1] !== "1") return ' start="' + G[1] + '"';
        }
        return "";
      }
      function k(P, H, G) {
        var ee = w.disableForced4SpacesIndentedSublists ? /^ ?\d+\.[ \t]/gm : /^ {0,3}\d+\.[ \t]/gm, J = w.disableForced4SpacesIndentedSublists ? /^ ?[*+-][ \t]/gm : /^ {0,3}[*+-][ \t]/gm, z = H === "ul" ? ee : J, R = "";
        if (P.search(z) !== -1) (function le(ve) {
          var ke = ve.search(z), be = C(P, H);
          ke !== -1 ? (R += `

<` + H + be + `>
` + b(ve.slice(0, ke), !!G) + "</" + H + `>
`, H = H === "ul" ? "ol" : "ul", z = H === "ul" ? ee : J, le(ve.slice(ke))) : R += `

<` + H + be + `>
` + b(ve, !!G) + "</" + H + `>
`;
        })(P);
        else {
          var V = C(P, H);
          R = `

<` + H + V + `>
` + b(P, !!G) + "</" + H + `>
`;
        }
        return R;
      }
      return p = _.converter._dispatch("lists.before", p, w, _), p += "¨0", _.gListLevel ? p = p.replace(/^(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm, function(P, H, G) {
        return k(H, G.search(/[*+-]/g) > -1 ? "ul" : "ol", !0);
      }) : p = p.replace(/(\n\n|^\n?)(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm, function(P, H, G, ee) {
        return k(G, ee.search(/[*+-]/g) > -1 ? "ul" : "ol", !1);
      }), p = p.replace(/¨0/, ""), p = _.converter._dispatch("lists.after", p, w, _), p;
    }), s.subParser("metadata", function(p, w, _) {
      "use strict";
      if (!w.metadata) return p;
      p = _.converter._dispatch("metadata.before", p, w, _);
      function b(C) {
        _.metadata.raw = C, C = C.replace(/&/g, "&amp;").replace(/"/g, "&quot;"), C = C.replace(/\n {4}/g, " "), C.replace(/^([\S ]+): +([\s\S]+?)$/gm, function(k, P, H) {
          return _.metadata.parsed[P] = H, "";
        });
      }
      return p = p.replace(/^\s*«««+(\S*?)\n([\s\S]+?)\n»»»+\n/, function(C, k, P) {
        return b(P), "¨M";
      }), p = p.replace(/^\s*---+(\S*?)\n([\s\S]+?)\n---+\n/, function(C, k, P) {
        return k && (_.metadata.format = k), b(P), "¨M";
      }), p = p.replace(/¨M/g, ""), p = _.converter._dispatch("metadata.after", p, w, _), p;
    }), s.subParser("outdent", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("outdent.before", p, w, _), p = p.replace(/^(\t|[ ]{1,4})/gm, "¨0"), p = p.replace(/¨0/g, ""), p = _.converter._dispatch("outdent.after", p, w, _), p;
    }), s.subParser("paragraphs", function(p, w, _) {
      "use strict";
      p = _.converter._dispatch("paragraphs.before", p, w, _), p = p.replace(/^\n+/g, ""), p = p.replace(/\n+$/g, "");
      for (var b = p.split(/\n{2,}/g), C = [], k = b.length, P = 0; P < k; P++) {
        var H = b[P];
        H.search(/¨(K|G)(\d+)\1/g) >= 0 ? C.push(H) : H.search(/\S/) >= 0 && (H = s.subParser("spanGamut")(H, w, _), H = H.replace(/^([ \t]*)/g, "<p>"), H += "</p>", C.push(H));
      }
      for (k = C.length, P = 0; P < k; P++) {
        for (var G = "", ee = C[P], J = !1; /¨(K|G)(\d+)\1/.test(ee); ) {
          var z = RegExp.$1, R = RegExp.$2;
          z === "K" ? G = _.gHtmlBlocks[R] : J ? G = s.subParser("encodeCode")(_.ghCodeBlocks[R].text, w, _) : G = _.ghCodeBlocks[R].codeblock, G = G.replace(/\$/g, "$$$$"), ee = ee.replace(/(\n\n)?¨(K|G)\d+\2(\n\n)?/, G), /^<pre\b[^>]*>\s*<code\b[^>]*>/.test(ee) && (J = !0);
        }
        C[P] = ee;
      }
      return p = C.join(`
`), p = p.replace(/^\n+/g, ""), p = p.replace(/\n+$/g, ""), _.converter._dispatch("paragraphs.after", p, w, _);
    }), s.subParser("runExtension", function(p, w, _, b) {
      "use strict";
      if (p.filter) w = p.filter(w, b.converter, _);
      else if (p.regex) {
        var C = p.regex;
        C instanceof RegExp || (C = new RegExp(C, "g")), w = w.replace(C, p.replace);
      }
      return w;
    }), s.subParser("spanGamut", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("spanGamut.before", p, w, _), p = s.subParser("codeSpans")(p, w, _), p = s.subParser("escapeSpecialCharsWithinTagAttributes")(p, w, _), p = s.subParser("encodeBackslashEscapes")(p, w, _), p = s.subParser("images")(p, w, _), p = s.subParser("anchors")(p, w, _), p = s.subParser("autoLinks")(p, w, _), p = s.subParser("simplifiedAutoLinks")(p, w, _), p = s.subParser("emoji")(p, w, _), p = s.subParser("underline")(p, w, _), p = s.subParser("italicsAndBold")(p, w, _), p = s.subParser("strikethrough")(p, w, _), p = s.subParser("ellipsis")(p, w, _), p = s.subParser("hashHTMLSpans")(p, w, _), p = s.subParser("encodeAmpsAndAngles")(p, w, _), w.simpleLineBreaks ? /\n\n¨K/.test(p) || (p = p.replace(/\n+/g, `<br />
`)) : p = p.replace(/  +\n/g, `<br />
`), p = _.converter._dispatch("spanGamut.after", p, w, _), p;
    }), s.subParser("strikethrough", function(p, w, _) {
      "use strict";
      function b(C) {
        return w.simplifiedAutoLink && (C = s.subParser("simplifiedAutoLinks")(C, w, _)), "<del>" + C + "</del>";
      }
      return w.strikethrough && (p = _.converter._dispatch("strikethrough.before", p, w, _), p = p.replace(/(?:~){2}([\s\S]+?)(?:~){2}/g, function(C, k) {
        return b(k);
      }), p = _.converter._dispatch("strikethrough.after", p, w, _)), p;
    }), s.subParser("stripLinkDefinitions", function(p, w, _) {
      "use strict";
      var b = /^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?([^>\s]+)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n+|(?=¨0))/gm, C = /^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?(data:.+?\/.+?;base64,[A-Za-z0-9+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n\n|(?=¨0)|(?=\n\[))/gm;
      p += "¨0";
      var k = function(P, H, G, ee, J, z, R) {
        return H = H.toLowerCase(), p.toLowerCase().split(H).length - 1 < 2 ? P : (G.match(/^data:.+?\/.+?;base64,/) ? _.gUrls[H] = G.replace(/\s/g, "") : _.gUrls[H] = s.subParser("encodeAmpsAndAngles")(G, w, _), z ? z + R : (R && (_.gTitles[H] = R.replace(/"|'/g, "&quot;")), w.parseImgDimensions && ee && J && (_.gDimensions[H] = {
          width: ee,
          height: J
        }), ""));
      };
      return p = p.replace(C, k), p = p.replace(b, k), p = p.replace(/¨0/, ""), p;
    }), s.subParser("tables", function(p, w, _) {
      "use strict";
      if (!w.tables) return p;
      var b = /^ {0,3}\|?.+\|.+\n {0,3}\|?[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*:?[ \t]*(?:[-=]){2,}[\s\S]+?(?:\n\n|¨0)/gm, C = /^ {0,3}\|.+\|[ \t]*\n {0,3}\|[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*\n( {0,3}\|.+\|[ \t]*\n)*(?:\n|¨0)/gm;
      function k(J) {
        return /^:[ \t]*--*$/.test(J) ? ' style="text-align:left;"' : /^--*[ \t]*:[ \t]*$/.test(J) ? ' style="text-align:right;"' : /^:[ \t]*--*[ \t]*:$/.test(J) ? ' style="text-align:center;"' : "";
      }
      function P(J, z) {
        var R = "";
        return J = J.trim(), (w.tablesHeaderId || w.tableHeaderId) && (R = ' id="' + J.replace(/ /g, "_").toLowerCase() + '"'), J = s.subParser("spanGamut")(J, w, _), "<th" + R + z + ">" + J + `</th>
`;
      }
      function H(J, z) {
        var R = s.subParser("spanGamut")(J, w, _);
        return "<td" + z + ">" + R + `</td>
`;
      }
      function G(J, z) {
        for (var R = `<table>
<thead>
<tr>
`, V = J.length, le = 0; le < V; ++le) R += J[le];
        for (R += `</tr>
</thead>
<tbody>
`, le = 0; le < z.length; ++le) {
          R += `<tr>
`;
          for (var ve = 0; ve < V; ++ve) R += z[le][ve];
          R += `</tr>
`;
        }
        return R += `</tbody>
</table>
`, R;
      }
      function ee(J) {
        var z, R = J.split(`
`);
        for (z = 0; z < R.length; ++z)
          /^ {0,3}\|/.test(R[z]) && (R[z] = R[z].replace(/^ {0,3}\|/, "")), /\|[ \t]*$/.test(R[z]) && (R[z] = R[z].replace(/\|[ \t]*$/, "")), R[z] = s.subParser("codeSpans")(R[z], w, _);
        var V = R[0].split("|").map(function(Oe) {
          return Oe.trim();
        }), le = R[1].split("|").map(function(Oe) {
          return Oe.trim();
        }), ve = [], ke = [], be = [], Ne = [];
        for (R.shift(), R.shift(), z = 0; z < R.length; ++z)
          R[z].trim() !== "" && ve.push(R[z].split("|").map(function(Oe) {
            return Oe.trim();
          }));
        if (V.length < le.length) return J;
        for (z = 0; z < le.length; ++z) be.push(k(le[z]));
        for (z = 0; z < V.length; ++z)
          s.helper.isUndefined(be[z]) && (be[z] = ""), ke.push(P(V[z], be[z]));
        for (z = 0; z < ve.length; ++z) {
          for (var ce = [], we = 0; we < ke.length; ++we)
            s.helper.isUndefined(ve[z][we]), ce.push(H(ve[z][we], be[we]));
          Ne.push(ce);
        }
        return G(ke, Ne);
      }
      return p = _.converter._dispatch("tables.before", p, w, _), p = p.replace(/\\(\|)/g, s.helper.escapeCharactersCallback), p = p.replace(b, ee), p = p.replace(C, ee), p = _.converter._dispatch("tables.after", p, w, _), p;
    }), s.subParser("underline", function(p, w, _) {
      "use strict";
      return w.underline && (p = _.converter._dispatch("underline.before", p, w, _), w.literalMidWordUnderscores ? (p = p.replace(/\b___(\S[\s\S]*?)___\b/g, function(b, C) {
        return "<u>" + C + "</u>";
      }), p = p.replace(/\b__(\S[\s\S]*?)__\b/g, function(b, C) {
        return "<u>" + C + "</u>";
      })) : (p = p.replace(/___(\S[\s\S]*?)___/g, function(b, C) {
        return /\S$/.test(C) ? "<u>" + C + "</u>" : b;
      }), p = p.replace(/__(\S[\s\S]*?)__/g, function(b, C) {
        return /\S$/.test(C) ? "<u>" + C + "</u>" : b;
      })), p = p.replace(/(_)/g, s.helper.escapeCharactersCallback), p = _.converter._dispatch("underline.after", p, w, _)), p;
    }), s.subParser("unescapeSpecialChars", function(p, w, _) {
      "use strict";
      return p = _.converter._dispatch("unescapeSpecialChars.before", p, w, _), p = p.replace(/¨E(\d+)E/g, function(b, C) {
        var k = parseInt(C);
        return String.fromCharCode(k);
      }), p = _.converter._dispatch("unescapeSpecialChars.after", p, w, _), p;
    }), s.subParser("makeMarkdown.blockquote", function(p, w) {
      "use strict";
      var _ = "";
      if (p.hasChildNodes())
        for (var b = p.childNodes, C = b.length, k = 0; k < C; ++k) {
          var P = s.subParser("makeMarkdown.node")(b[k], w);
          P !== "" && (_ += P);
        }
      return _ = _.trim(), _ = "> " + _.split(`
`).join(`
> `), _;
    }), s.subParser("makeMarkdown.codeBlock", function(p, w) {
      "use strict";
      var _ = p.getAttribute("language"), b = p.getAttribute("precodenum");
      return "```" + _ + `
` + w.preList[b] + "\n```";
    }), s.subParser("makeMarkdown.codeSpan", function(p) {
      "use strict";
      return "`" + p.innerHTML + "`";
    }), s.subParser("makeMarkdown.emphasis", function(p, w) {
      "use strict";
      var _ = "";
      if (p.hasChildNodes()) {
        _ += "*";
        for (var b = p.childNodes, C = b.length, k = 0; k < C; ++k) _ += s.subParser("makeMarkdown.node")(b[k], w);
        _ += "*";
      }
      return _;
    }), s.subParser("makeMarkdown.header", function(p, w, _) {
      "use strict";
      var b = new Array(_ + 1).join("#"), C = "";
      if (p.hasChildNodes()) {
        C = b + " ";
        for (var k = p.childNodes, P = k.length, H = 0; H < P; ++H) C += s.subParser("makeMarkdown.node")(k[H], w);
      }
      return C;
    }), s.subParser("makeMarkdown.hr", function() {
      "use strict";
      return "---";
    }), s.subParser("makeMarkdown.image", function(p) {
      "use strict";
      var w = "";
      return p.hasAttribute("src") && (w += "![" + p.getAttribute("alt") + "](", w += "<" + p.getAttribute("src") + ">", p.hasAttribute("width") && p.hasAttribute("height") && (w += " =" + p.getAttribute("width") + "x" + p.getAttribute("height")), p.hasAttribute("title") && (w += ' "' + p.getAttribute("title") + '"'), w += ")"), w;
    }), s.subParser("makeMarkdown.links", function(p, w) {
      "use strict";
      var _ = "";
      if (p.hasChildNodes() && p.hasAttribute("href")) {
        var b = p.childNodes, C = b.length;
        _ = "[";
        for (var k = 0; k < C; ++k) _ += s.subParser("makeMarkdown.node")(b[k], w);
        _ += "](", _ += "<" + p.getAttribute("href") + ">", p.hasAttribute("title") && (_ += ' "' + p.getAttribute("title") + '"'), _ += ")";
      }
      return _;
    }), s.subParser("makeMarkdown.list", function(p, w, _) {
      "use strict";
      var b = "";
      if (!p.hasChildNodes()) return "";
      for (var C = p.childNodes, k = C.length, P = p.getAttribute("start") || 1, H = 0; H < k; ++H)
        if (!(typeof C[H].tagName > "u" || C[H].tagName.toLowerCase() !== "li")) {
          var G = "";
          _ === "ol" ? G = P.toString() + ". " : G = "- ", b += G + s.subParser("makeMarkdown.listItem")(C[H], w), ++P;
        }
      return b += `
<!-- -->
`, b.trim();
    }), s.subParser("makeMarkdown.listItem", function(p, w) {
      "use strict";
      for (var _ = "", b = p.childNodes, C = b.length, k = 0; k < C; ++k) _ += s.subParser("makeMarkdown.node")(b[k], w);
      return /\n$/.test(_) ? _ = _.split(`
`).join(`
    `).replace(/^ {4}$/gm, "").replace(/\n\n+/g, `

`) : _ += `
`, _;
    }), s.subParser("makeMarkdown.node", function(p, w, _) {
      "use strict";
      _ = _ || !1;
      var b = "";
      if (p.nodeType === 3) return s.subParser("makeMarkdown.txt")(p, w);
      if (p.nodeType === 8) return "<!--" + p.data + `-->

`;
      if (p.nodeType !== 1) return "";
      switch (p.tagName.toLowerCase()) {
        case "h1":
          _ || (b = s.subParser("makeMarkdown.header")(p, w, 1) + `

`);
          break;
        case "h2":
          _ || (b = s.subParser("makeMarkdown.header")(p, w, 2) + `

`);
          break;
        case "h3":
          _ || (b = s.subParser("makeMarkdown.header")(p, w, 3) + `

`);
          break;
        case "h4":
          _ || (b = s.subParser("makeMarkdown.header")(p, w, 4) + `

`);
          break;
        case "h5":
          _ || (b = s.subParser("makeMarkdown.header")(p, w, 5) + `

`);
          break;
        case "h6":
          _ || (b = s.subParser("makeMarkdown.header")(p, w, 6) + `

`);
          break;
        case "p":
          _ || (b = s.subParser("makeMarkdown.paragraph")(p, w) + `

`);
          break;
        case "blockquote":
          _ || (b = s.subParser("makeMarkdown.blockquote")(p, w) + `

`);
          break;
        case "hr":
          _ || (b = s.subParser("makeMarkdown.hr")(p, w) + `

`);
          break;
        case "ol":
          _ || (b = s.subParser("makeMarkdown.list")(p, w, "ol") + `

`);
          break;
        case "ul":
          _ || (b = s.subParser("makeMarkdown.list")(p, w, "ul") + `

`);
          break;
        case "precode":
          _ || (b = s.subParser("makeMarkdown.codeBlock")(p, w) + `

`);
          break;
        case "pre":
          _ || (b = s.subParser("makeMarkdown.pre")(p, w) + `

`);
          break;
        case "table":
          _ || (b = s.subParser("makeMarkdown.table")(p, w) + `

`);
          break;
        case "code":
          b = s.subParser("makeMarkdown.codeSpan")(p, w);
          break;
        case "em":
        case "i":
          b = s.subParser("makeMarkdown.emphasis")(p, w);
          break;
        case "strong":
        case "b":
          b = s.subParser("makeMarkdown.strong")(p, w);
          break;
        case "del":
          b = s.subParser("makeMarkdown.strikethrough")(p, w);
          break;
        case "a":
          b = s.subParser("makeMarkdown.links")(p, w);
          break;
        case "img":
          b = s.subParser("makeMarkdown.image")(p, w);
          break;
        default:
          b = p.outerHTML + `

`;
      }
      return b;
    }), s.subParser("makeMarkdown.paragraph", function(p, w) {
      "use strict";
      var _ = "";
      if (p.hasChildNodes())
        for (var b = p.childNodes, C = b.length, k = 0; k < C; ++k) _ += s.subParser("makeMarkdown.node")(b[k], w);
      return _ = _.trim(), _;
    }), s.subParser("makeMarkdown.pre", function(p, w) {
      "use strict";
      var _ = p.getAttribute("prenum");
      return "<pre>" + w.preList[_] + "</pre>";
    }), s.subParser("makeMarkdown.strikethrough", function(p, w) {
      "use strict";
      var _ = "";
      if (p.hasChildNodes()) {
        _ += "~~";
        for (var b = p.childNodes, C = b.length, k = 0; k < C; ++k) _ += s.subParser("makeMarkdown.node")(b[k], w);
        _ += "~~";
      }
      return _;
    }), s.subParser("makeMarkdown.strong", function(p, w) {
      "use strict";
      var _ = "";
      if (p.hasChildNodes()) {
        _ += "**";
        for (var b = p.childNodes, C = b.length, k = 0; k < C; ++k) _ += s.subParser("makeMarkdown.node")(b[k], w);
        _ += "**";
      }
      return _;
    }), s.subParser("makeMarkdown.table", function(p, w) {
      "use strict";
      var _ = "", b = [[], []], C = p.querySelectorAll("thead>tr>th"), k = p.querySelectorAll("tbody>tr"), P, H;
      for (P = 0; P < C.length; ++P) {
        var G = s.subParser("makeMarkdown.tableCell")(C[P], w), ee = "---";
        if (C[P].hasAttribute("style")) switch (C[P].getAttribute("style").toLowerCase().replace(/\s/g, "")) {
          case "text-align:left;":
            ee = ":---";
            break;
          case "text-align:right;":
            ee = "---:";
            break;
          case "text-align:center;":
            ee = ":---:";
            break;
        }
        b[0][P] = G.trim(), b[1][P] = ee;
      }
      for (P = 0; P < k.length; ++P) {
        var J = b.push([]) - 1, z = k[P].getElementsByTagName("td");
        for (H = 0; H < C.length; ++H) {
          var R = " ";
          typeof z[H] < "u" && (R = s.subParser("makeMarkdown.tableCell")(z[H], w)), b[J].push(R);
        }
      }
      var V = 3;
      for (P = 0; P < b.length; ++P) for (H = 0; H < b[P].length; ++H) {
        var le = b[P][H].length;
        le > V && (V = le);
      }
      for (P = 0; P < b.length; ++P) {
        for (H = 0; H < b[P].length; ++H) P === 1 ? b[P][H].slice(-1) === ":" ? b[P][H] = s.helper.padEnd(b[P][H].slice(-1), V - 1, "-") + ":" : b[P][H] = s.helper.padEnd(b[P][H], V, "-") : b[P][H] = s.helper.padEnd(b[P][H], V);
        _ += "| " + b[P].join(" | ") + ` |
`;
      }
      return _.trim();
    }), s.subParser("makeMarkdown.tableCell", function(p, w) {
      "use strict";
      var _ = "";
      if (!p.hasChildNodes()) return "";
      for (var b = p.childNodes, C = b.length, k = 0; k < C; ++k) _ += s.subParser("makeMarkdown.node")(b[k], w, !0);
      return _.trim();
    }), s.subParser("makeMarkdown.txt", function(p) {
      "use strict";
      var w = p.nodeValue;
      return w = w.replace(/ +/g, " "), w = w.replace(/¨NBSP;/g, " "), w = s.helper.unescapeHTMLEntities(w), w = w.replace(/([*_~|`])/g, "\\$1"), w = w.replace(/^(\s*)>/g, "\\$1>"), w = w.replace(/^#/gm, "\\#"), w = w.replace(/^(\s*)([-=]{3,})(\s*)$/, "$1\\$2$3"), w = w.replace(/^( {0,3}\d+)\./gm, "$1\\."), w = w.replace(/^( {0,3})([+-])/gm, "$1\\$2"), w = w.replace(/]([\s]*)\(/g, "\\]$1\\("), w = w.replace(/^ {0,3}\[([\S \t]*?)]:/gm, "\\[$1]:"), w;
    });
    var $ = this;
    typeof define == "function" && define.amd ? define(function() {
      "use strict";
      return s;
    }) : typeof t < "u" && t.exports ? t.exports = s : $.showdown = s;
  }).call(e);
})), zk = /* @__PURE__ */ Px(UL(), 1), Ly = null, P2 = !1, R2 = 0, nb = /* @__PURE__ */ new Map(), N2 = 0, rb = /* @__PURE__ */ new Map(), qL = /* @__PURE__ */ new Set([
  "html",
  "htm",
  "xhtml",
  "xml",
  "svg",
  "vue",
  "svelte"
]), HL = "allow-scripts", bm = [".xb-tavern-markdown", ".xb-assistant-markdown"], zL = /* @__PURE__ */ new Set([
  "script",
  "style",
  "textarea",
  "title"
]);
function KL(e) {
  return String(e || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function WL() {
  return R2 += 1, `html-${Date.now().toString(36)}-${R2.toString(36)}`;
}
function GL() {
  return N2 += 1, `raw-${Date.now().toString(36)}-${N2.toString(36)}`;
}
function VL(e = "") {
  const t = String(e || "").length;
  if (t >= 1e3) {
    const n = t / 1e3;
    return `${n >= 10 ? Math.round(n) : n.toFixed(1)}k 字符`;
  }
  return `${t} 字符`;
}
function JL(e = "") {
  return qL.has(String(e || "").trim().toLowerCase());
}
function YL() {
  P2 || (P2 = !0, zk.default.subParser("unhashHTMLSpans", function(t, n, r) {
    let s = r.converter._dispatch("unhashHTMLSpans.before", t, n, r);
    for (let a = 0; a < r.gHtmlSpans.length; a += 1) {
      let o = r.gHtmlSpans[a], l = 0;
      for (; /¨C(\d+)C/.test(o); ) {
        const i = RegExp.$1;
        if (o = o.replace(`¨C${i}C`, r.gHtmlSpans[i]), l === 1e4) break;
        l += 1;
      }
      s = s.replace(`¨C${a}C`, o);
    }
    return r.converter._dispatch("unhashHTMLSpans.after", s, n, r);
  }));
}
function XL(e = "") {
  const t = String(e || "").trim();
  return t ? /^<!doctype\s+html/i.test(t) || /^<html[\s>]/i.test(t) ? !0 : (t.match(/<\/?[a-z][\w:-]*(?:\s[^<>]*)?>/gi) || []).length >= 3 && /<\/[a-z][\w:-]*>/i.test(t) : !1;
}
function jL(e = "", t = "html") {
  const n = WL();
  return nb.set(n, {
    code: String(e || ""),
    language: String(t || "html").trim() || "html"
  }), `@@XBHTMLBLOCK:${n}@@`;
}
function QL(e = "") {
  const t = GL();
  return rb.set(t, String(e || "")), `@@XBHTMLRAW:${t}@@`;
}
function Dy(e = "") {
  const t = String(e || "").trim();
  if (!t || !t.startsWith("<") || !t.endsWith(">") || /^<!--[\s\S]*-->$/.test(t) || /^<!doctype\b/i.test(t) || /^<\?xml\b/i.test(t)) return !1;
  let n = !1;
  const r = /<\/?\s*([a-z][\w:-]*)\b[^>]*>/gi;
  let s = null;
  for (; (s = r.exec(t)) !== null; ) {
    const o = String(s[1] || "").toLowerCase();
    if (zL.has(o)) return !1;
    n = !0;
  }
  if (!n) return !1;
  const a = t.replace(/<\/?\s*[a-z][\w:-]*\b[^>]*>/gi, "").trim();
  return !a || !/(^|\s)(?:#{1,6}\s|[-+*]\s|\d+\.\s|```|~~~|>\s)/.test(a);
}
function $2(e = "") {
  const t = String(e || "");
  if (!t.trim()) return t;
  const n = t.split(/\r?\n/), r = [];
  for (let s = 0; s < n.length; s += 1) {
    const a = n[s];
    if (!Dy(a)) {
      r.push(a);
      continue;
    }
    const o = [a];
    for (; s + 1 < n.length && Dy(n[s + 1]); )
      s += 1, o.push(n[s]);
    (r[r.length - 1] ?? "").trim() && r.push(""), r.push(QL(o.join(`
`)));
    const l = n[s + 1] ?? "";
    l.trim() && !Dy(l) && r.push("");
  }
  return r.join(`
`);
}
function ZL(e = "", t = {}) {
  const n = String(e || ""), r = t.htmlFenceMode === "code" ? "code" : "placeholder", s = t.protectRawHtmlBoundaries !== !1, a = /(^|\n)(`{3,}|~{3,})[ \t]*([^\n]*)\n([\s\S]*?)\n\2[ \t]*(?=\n|$)/g;
  let o = "", l = 0, i = null;
  for (; (i = a.exec(n)) !== null; ) {
    const u = i[1] || "", d = i.index + u.length, f = a.lastIndex, m = String(i[3] || "").trim().split(/\s+/)[0] || "", v = String(i[4] || ""), y = JL(m) || !m && XL(v);
    o += s ? $2(n.slice(l, d)) : n.slice(l, d), y && r !== "code" ? o += jL(v, m || "html") : o += n.slice(d, f), l = f;
  }
  return o += s ? $2(n.slice(l)) : n.slice(l), o;
}
function L2(e = "") {
  return String(e || "").replace(/@@XBHTMLBLOCK:([a-z0-9-]+)@@|@@XB_HTML_BLOCK_([a-z0-9-]+)@@/g, (t, n, r) => `<span class="xb-markdown-html-placeholder" data-xb-html-block-id="${n || r}"></span>`);
}
function eD(e = "") {
  const t = (n, r) => {
    const s = rb.get(r) || "";
    return rb.delete(r), s;
  };
  return String(e || "").replace(/<p>\s*@@XBHTMLRAW:([a-z0-9-]+)@@\s*<\/p>/g, t).replace(/(^|[\r\n])@@XBHTMLRAW:([a-z0-9-]+)@@(?=[\r\n]|$)/g, (n, r, s) => `${r}${t(n, s)}`);
}
function tD(e = "") {
  return String(e || "").replace(/&#x([0-9a-f]+);?/gi, (t, n) => String.fromCodePoint(Number.parseInt(n, 16) || 0)).replace(/&#([0-9]+);?/g, (t, n) => String.fromCodePoint(Number.parseInt(n, 10) || 0)).replace(/&colon;?/gi, ":").replace(/&tab;?/gi, "	").replace(/&newline;?/gi, `
`).replace(/&amp;?/gi, "&");
}
function nD(e = "") {
  const t = tD(e).trim().replace(/[\u0000-\u001F\u007F\s]+/g, "").toLowerCase();
  return /^(?:javascript|vbscript|data):/.test(t);
}
function rD(e = "") {
  return String(e || "").replace(/<style>([\s\S]+?)<\/style>/gim, (t, n) => `<custom-style>${encodeURIComponent(n)}</custom-style>`);
}
function sD(e = "") {
  const t = [];
  let n = "", r = 0;
  for (const s of String(e || "")) {
    if ((s === "(" || s === "[") && (r += 1), (s === ")" || s === "]") && r > 0 && (r -= 1), s === "," && r === 0) {
      t.push(n), n = "";
      continue;
    }
    n += s;
  }
  return n && t.push(n), t;
}
function aD(e = "") {
  const t = new RegExp(`:(${[
    "has",
    "not",
    "where",
    "is",
    "matches",
    "any"
  ].join("|")})\\(([^)]+)\\)`, "g"), n = (r = "") => String(r || "").split(/\s+/).map((s) => s.replace(/\.([\w-]+)/g, (a, o) => String(o || "").startsWith("custom-") ? a : `.custom-${o}`)).join(" ");
  return n(String(e || "").replace(t, (r, s, a) => `:${s}(${n(a)})`));
}
function oD(e = bm) {
  const t = (Array.isArray(e) ? e : [e]).map((n) => String(n || "").trim()).filter(Boolean).map((n) => `${n} `);
  return t.length ? t : bm.map((n) => `${n} `);
}
function iD(e = "", t = bm) {
  const n = oD(t);
  return String(e || "").replace(/@import[^;]+;?/gi, "").replace(/(^|[{}])\s*([^@{}][^{}]*)\{/g, (r, s, a) => {
    const o = sD(a).map((l) => l.trim()).filter(Boolean);
    return !o.length || o.every((l) => /^(?:from|to|\d+(?:\.\d+)?%)$/i.test(l)) ? r : `${s}${o.flatMap((l) => {
      const i = aD(l);
      return n.map((u) => `${u}${i}`);
    }).join(", ")}{`;
  }).replace(/[^{};]+:\s*[^{};]*:\/\/[^{};]*(?:;|(?=}))/g, "");
}
function D2(e = "", t = {}) {
  const n = Array.isArray(t.prefixes) ? t.prefixes : t.prefix ? [t.prefix] : bm;
  return String(e || "").replace(/<custom-style>([\s\S]+?)<\/custom-style>/gim, (r, s) => {
    try {
      return `<style>${iD(decodeURIComponent(String(s || "")).replaceAll(/<br\/>/g, ""), n)}</style>`;
    } catch (a) {
      return `CSS ERROR: ${a instanceof Error ? a.message : String(a || "decode_failed")}`;
    }
  });
}
function lD(e = "") {
  return String(e || "").split(/\s+/).filter(Boolean).map((t) => t.startsWith("fa-") || t.startsWith("note-") || t === "monospace" || t.startsWith("custom-") ? t : `custom-${t}`).join(" ");
}
function uD(e = "") {
  return String(e || "").replace(/<\/?(?:script|style|iframe|object|embed|link|meta|base|form|input|button|textarea|select|option)[^>]*>/gi, "").replace(/\s+on[a-z0-9_-]+\s*=\s*(?:"[^"]*"|'[^']*'|[^\s"'=<>`]+)/gi, "").replace(/\s+class\s*=\s*("[^"]*"|'[^']*'|[^\s"'=<>`]+)/gi, (t, n) => {
    const r = String(n || "")[0], s = r === '"' || r === "'", a = lD(s ? String(n).slice(1, -1) : String(n || ""));
    return s ? ` class=${r}${a}${r}` : ` class=${a}`;
  }).replace(/\s+(href|src|xlink:href)\s*=\s*("[^"]*"|'[^']*'|[^\s"'=<>`]+)/gi, (t, n, r) => nD(String(r || "").replace(/^["']|["']$/g, "")) ? "" : ` ${n}=${r}`);
}
function O2(e) {
  return typeof e?.sanitize == "function" ? e : null;
}
function cD() {
  try {
    const e = globalThis.parent && globalThis.parent !== globalThis ? O2(globalThis.parent.DOMPurify) : null;
    if (e) return e;
  } catch {
  }
  return O2(globalThis.DOMPurify);
}
function dD(e = "") {
  const t = rD(e), n = cD(), r = {
    RETURN_DOM: !1,
    RETURN_DOM_FRAGMENT: !1,
    RETURN_TRUSTED_TYPE: !1,
    MESSAGE_SANITIZE: !0,
    ADD_TAGS: ["custom-style"]
  };
  if (n) try {
    return D2(String(n.sanitize(t, r) || ""));
  } catch {
  }
  return D2(uD(t));
}
function fD(e, t = {}) {
  const n = String(e || "").trim();
  if (!n) return "";
  const r = ZL(n, t);
  try {
    return Ly || (YL(), Ly = new zk.default.Converter({
      emoji: !0,
      literalMidWordUnderscores: !0,
      parseImgDimensions: !0,
      simpleLineBreaks: !0,
      strikethrough: !0,
      tables: !0,
      underline: !0,
      disableForced4SpacesIndentedSublists: !0
    })), L2(dD(eD(Ly.makeHtml(r))));
  } catch {
  }
  return L2(KL(r).replace(/\n/g, "<br>"));
}
async function hD(e = "", t = null) {
  const n = String(e || "");
  if (!n) return !1;
  const r = t?.createElement ? t : globalThis.document, s = r?.defaultView || globalThis;
  try {
    if (s.navigator?.clipboard?.writeText)
      return await s.navigator.clipboard.writeText(n), !0;
  } catch {
  }
  try {
    if (!r?.createElement || !r.body?.appendChild) return !1;
    const a = r.createElement("textarea");
    a.value = n, a.setAttribute("readonly", "readonly"), a.style.position = "fixed", a.style.left = "-9999px", a.style.top = "0", a.style.opacity = "0", a.style.pointerEvents = "none", a.style.fontSize = "16px", r.body.appendChild(a);
    try {
      a.focus({ preventScroll: !0 });
    } catch {
      a.focus();
    }
    a.select(), a.setSelectionRange(0, a.value.length);
    const o = r.execCommand?.("copy") || !1;
    return a.remove(), o;
  } catch {
    return !1;
  }
}
function pD(e, t = {}) {
  if (!e || typeof t.onPathClick != "function") return;
  const n = e.ownerDocument || globalThis.document, r = n?.defaultView?.NodeFilter || globalThis.NodeFilter;
  if (!n?.createTreeWalker || !r) return;
  const s = t.pathRegex || /local\/[^\s`"'<>，。；：！？（）()[\]{}]+/g, a = String(t.linkClassName || "xb-markdown-local-path-link"), o = n.createTreeWalker(e, r.SHOW_TEXT), l = [];
  for (; o.nextNode(); ) {
    const i = o.currentNode;
    !i?.nodeValue || !String(i.nodeValue).includes("local/") || i.parentElement?.closest?.("button, a, textarea, input") || l.push(i);
  }
  l.forEach((i) => {
    const u = String(i.nodeValue || ""), d = n.createDocumentFragment();
    let f = null, m = 0, v = !1;
    for (s.lastIndex = 0; (f = s.exec(u)) !== null; ) {
      v = !0, f.index > m && d.appendChild(n.createTextNode(u.slice(m, f.index)));
      const y = f[0], S = n.createElement("button");
      S.type = "button", S.className = a, S.textContent = y, S.addEventListener("click", () => {
        t.onPathClick(y);
      }), d.appendChild(S), m = f.index + y.length;
    }
    v && (m < u.length && d.appendChild(n.createTextNode(u.slice(m))), i.parentNode?.replaceChild(d, i));
  });
}
function mD(e, t = {}) {
  if (!e?.querySelectorAll) return;
  const n = e.ownerDocument || globalThis.document;
  if (!n?.createElement) return;
  const r = String(t.codeBlockClassName || "xb-markdown-codeblock"), s = String(t.codeCopyClassName || "xb-markdown-code-copy"), a = String(t.copyButtonTitle || "复制代码"), o = String(t.copySuccessTitle || "已复制"), l = String(t.copyFailureTitle || "复制失败"), i = t.flattenPreCode === !0, u = String(t.skipPreSelector || "").trim();
  Array.from(e.querySelectorAll("pre")).forEach((d) => {
    if (u && d.matches?.(u) || d.closest(`.${r}`)) return;
    const f = d.children.length === 1 && d.firstElementChild?.tagName === "CODE" ? d.firstElementChild : null;
    if (i && f) {
      for (; f.firstChild; ) d.insertBefore(f.firstChild, f);
      f.remove();
    }
    const m = n.createElement("div");
    m.className = r;
    const v = n.createElement("button");
    v.type = "button", v.className = s, v.textContent = "⧉", v.title = a, v.setAttribute("aria-label", a), v.addEventListener("pointerdown", (y) => {
      y.stopPropagation();
    }), v.addEventListener("pointerup", (y) => {
      y.stopPropagation();
    }), v.addEventListener("touchstart", (y) => {
      y.stopPropagation();
    }, { passive: !0 }), v.addEventListener("touchend", (y) => {
      y.stopPropagation();
    }, { passive: !0 }), v.addEventListener("click", async (y) => {
      y.preventDefault(), y.stopPropagation();
      const S = await hD(d.querySelector("code")?.textContent || d.textContent || "", n);
      v.textContent = S ? "✓" : "!", v.title = S ? o : l, v.setAttribute("aria-label", S ? o : l), v.classList.toggle("is-copied", S), v.classList.toggle("is-failed", !S), setTimeout(() => {
        v.textContent = "⧉", v.title = a, v.setAttribute("aria-label", a), v.classList.remove("is-copied", "is-failed");
      }, 1200);
    }), d.parentNode?.insertBefore(m, d), m.append(v, d);
  });
}
function F2(e = [], t = null) {
  e.forEach((n) => {
    n.classList.toggle("is-active", n === t), n.setAttribute("aria-pressed", n === t ? "true" : "false");
  });
}
function B2(e, t = "") {
  const n = e.createElement("pre");
  return n.className = "xb-markdown-html-code", n.textContent = String(t || ""), n;
}
function U2(e, t = "") {
  const n = e.createElement("iframe");
  return n.className = "xb-markdown-html-preview", n.setAttribute("sandbox", HL), n.referrerPolicy = "no-referrer", n.title = "HTML 渲染预览", n.srcdoc = String(t || ""), n;
}
function gD(e, t = {}, n = {}) {
  const r = String(t.code || "");
  if (n.htmlBlockMode === "code") return B2(e, r);
  if (n.htmlBlockMode === "preview") {
    const S = e.createElement("div");
    S.className = "xb-markdown-html-block xb-markdown-html-block-auto";
    const x = e.createElement("div");
    return x.className = "xb-markdown-html-body", x.append(U2(e, r)), S.append(x), S;
  }
  const s = e.createElement("div");
  s.className = "xb-markdown-html-block";
  const a = e.createElement("div");
  a.className = "xb-markdown-html-head";
  const o = e.createElement("div");
  o.className = "xb-markdown-html-title", o.textContent = "HTML 片段";
  const l = e.createElement("span");
  l.textContent = VL(r), o.appendChild(l);
  const i = e.createElement("div");
  i.className = "xb-markdown-html-actions";
  const u = e.createElement("button");
  u.type = "button", u.textContent = "显示代码", u.setAttribute("aria-pressed", "false");
  const d = e.createElement("button");
  d.type = "button", d.textContent = "渲染预览", d.setAttribute("aria-pressed", "false"), i.append(u, d), a.append(o, i);
  const f = e.createElement("div");
  f.className = "xb-markdown-html-body", f.hidden = !0;
  let m = "";
  const v = [u, d], y = (S) => {
    if (m === S) {
      m = "", f.hidden = !0, f.replaceChildren(), F2(v);
      return;
    }
    m = S, f.hidden = !1, f.replaceChildren(S === "preview" ? U2(e, r) : B2(e, r)), F2(v, S === "preview" ? d : u);
  };
  return u.addEventListener("click", () => y("code")), d.addEventListener("click", () => y("preview")), s.append(a, f), s;
}
function vD(e, t = {}) {
  if (!e?.querySelectorAll) return;
  const n = e.ownerDocument || globalThis.document;
  n?.createElement && Array.from(e.querySelectorAll(".xb-markdown-html-placeholder[data-xb-html-block-id]")).forEach((r) => {
    const s = String(r.getAttribute("data-xb-html-block-id") || ""), a = nb.get(s);
    if (nb.delete(s), !a) {
      r.remove();
      return;
    }
    const o = gD(n, a, t), l = r.parentElement;
    if (l?.tagName === "P" && l.textContent.trim() === "") {
      l.replaceWith(o);
      return;
    }
    r.replaceWith(o);
  });
}
function q2(e, t = {}) {
  return e && (vD(e, t), mD(e, t), pD(e, t), e);
}
function yD() {
  return window.location.origin;
}
function H2(e, t, n, r = null) {
  if (!e?.contentWindow) return !1;
  const s = n ? {
    source: n,
    ...t
  } : t, a = r || yD();
  return e.contentWindow.postMessage(s, a), !0;
}
function _D() {
  return `
(function(){
  // vh 修复：CSS注入（立即生效） + 延迟样式表遍历（不阻塞渲染）
  (function(){
    var s=document.createElement('style');
    s.textContent='html,body{height:auto!important;min-height:0!important;max-height:none!important}';
    (document.head||document.documentElement).appendChild(s);
    // 延迟遍历样式表，不阻塞初次渲染
    (window.requestIdleCallback||function(cb){setTimeout(cb,50)})(function(){
      try{
        for(var i=0,sheets=document.styleSheets;i<sheets.length;i++){
          try{
            var rules=sheets[i].cssRules;
            if(!rules)continue;
            for(var j=0;j<rules.length;j++){
              var st=rules[j].style;
              if(!st)continue;
              if((st.height||'').indexOf('vh')>-1)st.height='auto';
              if((st.minHeight||'').indexOf('vh')>-1)st.minHeight='0';
              if((st.maxHeight||'').indexOf('vh')>-1)st.maxHeight='none';
            }
          }catch(e){}
        }
      }catch(e){}
    });
  })();

  function measureVisibleHeight(){
    try{
      var doc=document,target=doc.body;
      if(!target)return 0;
      var minTop=Infinity,maxBottom=0;
      var addRect=function(el){
        try{
          var r=el.getBoundingClientRect();
          if(r&&r.height>0){
            if(minTop>r.top)minTop=r.top;
            if(maxBottom<r.bottom)maxBottom=r.bottom;
          }
        }catch(e){}
      };
      addRect(target);
      var children=target.children||[];
      for(var i=0;i<children.length;i++){
        var child=children[i];
        if(!child)continue;
        try{
          var s=window.getComputedStyle(child);
          if(s.display==='none'||s.visibility==='hidden')continue;
          if(!child.offsetParent&&s.position!=='fixed')continue;
        }catch(e){}
        addRect(child);
      }
      return maxBottom>0?Math.ceil(maxBottom-Math.min(minTop,0)):(target.scrollHeight||0);
    }catch(e){
      return(document.body&&document.body.scrollHeight)||0;
    }
  }

  var parentOrigin;try{parentOrigin=new URL(document.referrer).origin}catch(_){parentOrigin='*'}
  function post(m){try{parent.postMessage(m,parentOrigin)}catch(e){}}
  var rafPending=false,lastH=0,HYSTERESIS=2;

  function send(force){
    if(rafPending&&!force)return;
    rafPending=true;
    requestAnimationFrame(function(){
      rafPending=false;
      var h=measureVisibleHeight();
      if(force||Math.abs(h-lastH)>=HYSTERESIS){
        lastH=h;
        post({height:h,force:!!force});
      }
    });
  }

  try{send(true)}catch(e){}
  document.addEventListener('DOMContentLoaded',function(){send(true)},{once:true});
  window.addEventListener('load',function(){send(true)},{once:true});

  try{
    if(document.fonts){
      document.fonts.ready.then(function(){send(true)}).catch(function(){});
      if(document.fonts.addEventListener){
        document.fonts.addEventListener('loadingdone',function(){send(true)});
        document.fonts.addEventListener('loadingerror',function(){send(true)});
      }
    }
  }catch(e){}

  ['transitionend','animationend'].forEach(function(evt){
    document.addEventListener(evt,function(){send(false)},{passive:true,capture:true});
  });

  try{
    var root=document.body||document.documentElement;
    var ro=new ResizeObserver(function(){send(false)});
    ro.observe(root);
  }catch(e){
    try{
      var rootMO=document.body||document.documentElement;
      new MutationObserver(function(){send(false)})
        .observe(rootMO,{childList:true,subtree:true,attributes:true,characterData:true});
    }catch(e){}
    window.addEventListener('resize',function(){send(false)},{passive:true});
  }

  window.addEventListener('message',function(e){
    if(parentOrigin!=='*'&&e&&e.origin!==parentOrigin)return;
    var d=e&&e.data||{};
    if(d&&d.type==='probe')setTimeout(function(){send(true)},10);
  });

  window.STscript=function(command){
    return new Promise(function(resolve,reject){
      try{
        if(!command){reject(new Error('empty'));return}
        if(command[0]!=='/')command='/'+command;
        var id=Date.now().toString(36)+Math.random().toString(36).slice(2);
        function onMessage(e){
          if(parentOrigin!=='*'&&e&&e.origin!==parentOrigin)return;
          var d=e&&e.data||{};
          if(d.source!=='xiaobaix-host')return;
          if((d.type==='commandResult'||d.type==='commandError')&&d.id===id){
            try{window.removeEventListener('message',onMessage)}catch(e){}
            if(d.type==='commandResult')resolve(d.result);
            else reject(new Error(d.error||'error'));
          }
        }
        try{window.addEventListener('message',onMessage)}catch(e){}
        post({type:'runCommand',id:id,command:command});
        setTimeout(function(){
          try{window.removeEventListener('message',onMessage)}catch(e){}
          reject(new Error('Command timeout'));
        },180000);
      }catch(e){reject(e)}
    });
  };
  try{if(typeof window['stscript']!=='function')window['stscript']=window.STscript}catch(e){}
})();`;
}
function bD() {
  return `
(function(){
  function sanitizeOptions(options){
    try{
      return JSON.parse(JSON.stringify(options,function(k,v){return(typeof v==='function')?undefined:v}))
    }catch(_){
      try{
        var seen=new WeakSet();
        var clone=function(val){
          if(val===null||val===undefined)return val;
          var t=typeof val;
          if(t==='function')return undefined;
          if(t!=='object')return val;
          if(seen.has(val))return undefined;
          seen.add(val);
          if(Array.isArray(val)){
            var arr=[];for(var i=0;i<val.length;i++){var v=clone(val[i]);if(v!==undefined)arr.push(v)}return arr;
          }
          var proto=Object.getPrototypeOf(val);
          if(proto!==Object.prototype&&proto!==null)return undefined;
          var out={};
          for(var k in val){if(Object.prototype.hasOwnProperty.call(val,k)){var v=clone(val[k]);if(v!==undefined)out[k]=v}}
          return out;
        };
        return clone(options);
      }catch(__){return{}}
    }
  }
  function CallGenerateImpl(options){
    return new Promise(function(resolve,reject){
      try{
        var parentOrigin;try{parentOrigin=new URL(document.referrer).origin}catch(_){parentOrigin='*'}
        function post(m){try{parent.postMessage(m,parentOrigin)}catch(e){}}
        if(!options||typeof options!=='object'){reject(new Error('Invalid options'));return}
        var id=Date.now().toString(36)+Math.random().toString(36).slice(2);
        function onMessage(e){
          if(parentOrigin!=='*'&&e&&e.origin!==parentOrigin)return;
          var d=e&&e.data||{};
          if(d.source!=='xiaobaix-host'||d.id!==id)return;
          if(d.type==='generateStreamStart'&&options.streaming&&options.streaming.onStart){try{options.streaming.onStart(d.sessionId)}catch(_){}}
          else if(d.type==='generateStreamChunk'&&options.streaming&&options.streaming.onChunk){try{options.streaming.onChunk(d.chunk,d.accumulated)}catch(_){}}
          else if(d.type==='generateStreamComplete'){try{window.removeEventListener('message',onMessage)}catch(_){}resolve(d.result)}
          else if(d.type==='generateStreamError'){try{window.removeEventListener('message',onMessage)}catch(_){}reject(new Error(d.error||'Stream failed'))}
          else if(d.type==='generateResult'){try{window.removeEventListener('message',onMessage)}catch(_){}resolve(d.result)}
          else if(d.type==='generateError'){try{window.removeEventListener('message',onMessage)}catch(_){}reject(new Error(d.error||'Generation failed'))}
        }
        try{window.addEventListener('message',onMessage)}catch(_){}
        var sanitized=sanitizeOptions(options);
        post({type:'generateRequest',id:id,options:sanitized});
        setTimeout(function(){try{window.removeEventListener('message',onMessage)}catch(e){};reject(new Error('Generation timeout'))},300000);
      }catch(e){reject(e)}
    });
  }
  try{window.CallGenerate=CallGenerateImpl}catch(e){}
  try{window.callGenerate=CallGenerateImpl}catch(e){}
  try{window.__xb_callGenerate_loaded=true}catch(e){}
})();

(function(){
  function applyAvatarCss(urls){
    try{
      var root=document.documentElement;
      root.style.setProperty('--xb-user-avatar',urls&&urls.user?'url("'+urls.user+'")':'none');
      root.style.setProperty('--xb-char-avatar',urls&&urls.char?'url("'+urls.char+'")':'none');
      if(!document.getElementById('xb-avatar-style')){
        var css='.xb-avatar,.xb-user-avatar,.xb-char-avatar{width:36px;height:36px;border-radius:50%;background-size:cover;background-position:center;background-repeat:no-repeat;display:inline-block}.xb-user-avatar{background-image:var(--xb-user-avatar)}.xb-char-avatar{background-image:var(--xb-char-avatar)}';
        var style=document.createElement('style');
        style.id='xb-avatar-style';
        style.textContent=css;
        document.head.appendChild(style);
      }
    }catch(_){}
  }
  var parentOrigin;try{parentOrigin=new URL(document.referrer).origin}catch(_){parentOrigin='*'}
  function requestAvatars(){try{parent.postMessage({type:'getAvatars'},parentOrigin)}catch(_){}}
  function onMessage(e){
    if(parentOrigin!=='*'&&e&&e.origin!==parentOrigin)return;
    var d=e&&e.data||{};
    if(d&&d.source==='xiaobaix-host'&&d.type==='avatars'){
      applyAvatarCss(d.urls);
      try{window.removeEventListener('message',onMessage)}catch(_){}
    }
  }
  try{
    window.addEventListener('message',onMessage);
    if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',requestAvatars,{once:true});}
    else{requestAvatars();}
    window.addEventListener('load',requestAvatars,{once:true});
  }catch(_){}
})();`;
}
var Jl = /\[tavern-image:([a-z0-9\-_]+)\]/gi;
function wD(e) {
  const { isHiddenMarkdownNode: t } = e;
  function n(_, b = "") {
    return !!(_ && _.isConnected !== !1 && !t(_) && String(_.dataset.tavernImageSlot || "").trim() === b);
  }
  function r(_) {
    return _.result || _;
  }
  function s(_) {
    return Array.isArray(_.characterPrompts) ? _.characterPrompts.filter((b) => !!b && typeof b == "object") : [];
  }
  function a(_, b) {
    const C = document.createElement("span");
    C.className = "xb-tavern-image-edit-group";
    const k = document.createElement("span");
    k.className = "xb-tavern-image-edit-group-label", k.textContent = b.label, C.append(k);
    const P = document.createElement("textarea");
    return P.className = "xb-tavern-image-edit-input", P.dataset.type = b.type, typeof b.index == "number" && (P.dataset.index = String(b.index)), P.value = b.value, C.append(P), _.append(C), P;
  }
  function o(_, b) {
    _.replaceChildren();
    const C = a(_, {
      label: "场景",
      value: String(b.tags || ""),
      type: "scene"
    });
    return s(b).forEach((k, P) => {
      a(_, {
        label: String(k.name || `角色 ${P + 1}`),
        value: String(k.prompt || ""),
        type: "char",
        index: P
      });
    }), C;
  }
  function l(_, b, C, k = {}) {
    const P = document.createElement("span");
    P.className = "xb-tavern-image-edit", k.inline && P.classList.add("is-inline"), P.style.display = "none";
    const H = document.createElement("span");
    H.className = "xb-tavern-image-edit-label", H.textContent = "编辑 TAG（场景描述）";
    const G = document.createElement("span");
    G.className = "xb-tavern-image-edit-scroll";
    const ee = document.createElement("span");
    ee.className = "xb-tavern-image-edit-actions", [{
      action: "save-tags",
      label: k.saveLabel || "保存 TAG"
    }, {
      action: "cancel-edit",
      label: "取消"
    }].forEach((R) => {
      const V = document.createElement("button");
      V.type = "button", V.dataset.action = R.action, V.textContent = R.label, ee.append(V);
    }), P.append(H, G, ee);
    const J = (R) => {
      P.style.display = R ? "block" : "none", k.onToggle?.(R);
    }, z = () => {
      const R = o(G, C);
      J(!0), R.focus();
    };
    return P.addEventListener("click", (R) => {
      const V = R.target?.closest("[data-action]");
      if (!V) return;
      R.preventDefault(), R.stopPropagation();
      const le = V.dataset.action || "";
      if (le === "cancel-edit") {
        J(!1), G.replaceChildren();
        return;
      }
      le === "save-tags" && (async () => {
        const ve = P.querySelector('textarea[data-type="scene"]'), ke = String(ve?.value || "").trim();
        if (!ke) {
          await e.alertDialog({
            title: "缺少场景 TAG",
            message: "场景 TAG 不能为空"
          });
          return;
        }
        const be = i(P, C);
        u(_, !0), (async () => {
          const ce = await e.requestHost("xb-tavern:draw-image-edit", { payload: {
            slotId: b,
            tags: ke,
            characterPrompts: be
          } });
          if (k.retryAfterSave) {
            try {
              await y(_, b, await e.requestHost("xb-tavern:draw-image-refresh", { payload: { slotId: b } }));
            } catch {
              await y(_, b, ce);
            }
            return;
          }
          await y(_, b, ce);
        })().catch(() => u(_, !1));
      })();
    }), {
      editPanel: P,
      openEditor: z
    };
  }
  function i(_, b) {
    const C = s(b), k = Array.from(_.querySelectorAll('textarea[data-type="char"]'));
    if (!k.length || !C.length) return null;
    const P = [];
    return k.forEach((H) => {
      const G = Math.floor(Number(H.dataset.index)), ee = Number.isFinite(G) ? C[G] : null;
      ee && P.push({
        ...ee,
        prompt: H.value.trim()
      });
    }), P;
  }
  function u(_, b) {
    _.classList.toggle("is-busy", b), _.querySelectorAll("button").forEach((C) => {
      C.disabled = b || C.dataset.navDisabled === "true" || C.dataset.editDisabled === "true";
    });
  }
  function d(_) {
    return (_ instanceof Error ? _.message : String(_ || "unknown_error")).replace(/^xb-tavern:draw-image-refresh:\s*/, "").trim() || "unknown_error";
  }
  function f(_) {
    e.showToast?.(`重绘失败：${d(_)}`, {
      tone: "warning",
      durationMs: 4200
    });
  }
  function m(_ = document) {
    _.querySelectorAll(".xb-tavern-image-menu.is-open").forEach((b) => b.classList.remove("is-open"));
  }
  function v() {
    typeof document > "u" || document.getElementById("xb-tavern-image-gallery-overlay")?.remove();
  }
  async function y(_, b = "", C) {
    if (!n(_, b)) return;
    const k = r(C);
    k.hasData && k.url ? E(_, k) : I(_, k);
  }
  async function S(_ = "", b = 0, C) {
    const k = await e.requestHost("xb-tavern:draw-image-select", { payload: {
      slotId: _,
      index: b
    } });
    return C && await y(C, _, k), r(k);
  }
  async function x(_ = "", b) {
    const C = r(await e.requestHost("xb-tavern:draw-image-gallery", { payload: { slotId: _ } })), k = Array.isArray(C.previews) ? C.previews : [];
    if (!k.length) return;
    v();
    let P = Math.max(0, Math.min(k.length - 1, Math.floor(Number(C.currentIndex) || 0)));
    const H = document.createElement("div");
    H.id = "xb-tavern-image-gallery-overlay", H.className = "xb-tavern-image-gallery-overlay visible";
    const G = document.createElement("button");
    G.type = "button", G.className = "xb-tavern-image-gallery-close", G.textContent = "×";
    const ee = document.createElement("div");
    ee.className = "xb-tavern-image-gallery-main";
    const J = document.createElement("button");
    J.type = "button", J.className = "xb-tavern-image-gallery-nav", J.textContent = "‹";
    const z = document.createElement("div");
    z.className = "xb-tavern-image-gallery-img-wrap";
    const R = document.createElement("img");
    R.className = "xb-tavern-image-gallery-img";
    const V = document.createElement("div");
    V.className = "xb-tavern-image-gallery-saved-badge", V.textContent = "已保存", z.append(R, V);
    const le = document.createElement("button");
    le.type = "button", le.className = "xb-tavern-image-gallery-nav", le.textContent = "›", ee.append(J, z, le);
    const ve = document.createElement("div");
    ve.className = "xb-tavern-image-gallery-thumbs";
    const ke = document.createElement("div");
    ke.className = "xb-tavern-image-gallery-actions";
    const be = document.createElement("button");
    be.type = "button", be.className = "xb-tavern-image-gallery-btn primary", be.textContent = "使用此图";
    const Ne = document.createElement("button");
    Ne.type = "button", Ne.className = "xb-tavern-image-gallery-btn";
    const ce = document.createElement("button");
    ce.type = "button", ce.className = "xb-tavern-image-gallery-btn danger", ce.textContent = "删除", ke.append(be, Ne, ce);
    const we = document.createElement("div");
    we.className = "xb-tavern-image-gallery-info", H.append(G, ee, ve, ke, we), document.body.append(H);
    const Oe = () => {
      const F = k[P] || k[0];
      R.src = String(F.url || ""), V.style.display = F.saved ? "block" : "none", J.disabled = P >= k.length - 1, le.disabled = P <= 0, Ne.textContent = F.saved ? "✓ 已保存" : "保存到服务器", Ne.disabled = !!F.saved, we.textContent = `版本 ${k.length - P} / ${k.length}${F.timestamp ? ` · ${new Date(Number(F.timestamp)).toLocaleString()}` : ""}`, ve.querySelectorAll(".xb-tavern-image-gallery-thumb").forEach((ae) => {
        ae.classList.toggle("active", Number(ae.dataset.index) === P);
      });
    };
    k.slice().reverse().forEach((F, ae) => {
      const Z = k.length - 1 - ae, ie = document.createElement("img");
      ie.className = `xb-tavern-image-gallery-thumb${F.saved ? " saved" : ""}`, ie.src = String(F.url || ""), ie.dataset.index = String(Z), ie.loading = "lazy", ie.addEventListener("click", () => {
        P = Z, Oe();
      }), ve.append(ie);
    }), G.addEventListener("click", v), H.addEventListener("click", (F) => {
      F.target === H && v();
    }), J.addEventListener("click", () => {
      P = Math.min(k.length - 1, P + 1), Oe();
    }), le.addEventListener("click", () => {
      P = Math.max(0, P - 1), Oe();
    }), be.addEventListener("click", () => {
      S(_, P, b).then(v);
    }), Ne.addEventListener("click", () => {
      const F = k[P] || k[0], ae = String(F?.imgId || "").trim();
      ae && (Ne.disabled = !0, e.requestHost("xb-tavern:draw-image-save", { payload: {
        slotId: _,
        imgId: ae
      } }).then((Z) => (F.saved = !0, Oe(), y(b, _, Z))).catch(() => {
        Ne.disabled = !1;
      }));
    }), ce.addEventListener("click", () => {
      (async () => {
        if (!await e.confirmDialog({
          title: "删除图片",
          message: "确定删除这张图片吗？",
          confirmText: "删除",
          tone: "danger"
        })) return;
        const F = k[P] || k[0], ae = String(F?.imgId || "").trim();
        ae && e.requestHost("xb-tavern:draw-image-delete", { payload: {
          slotId: _,
          imgId: ae
        } }).then((Z) => y(b, _, Z)).then(() => {
          if (k.splice(P, 1), !k.length) {
            v();
            return;
          }
          P = Math.max(0, Math.min(k.length - 1, P)), ve.querySelectorAll(".xb-tavern-image-gallery-thumb").forEach((Z) => {
            Number(Z.dataset.index) === P && Z.remove();
          }), v(), x(_, b);
        });
      })();
    }), Oe();
  }
  function I(_, b = {}) {
    _.classList.add("is-failed"), _.classList.remove("is-loaded", "is-busy"), _.dataset.tavernImageSlot = String(b.slotId || _.dataset.tavernImageSlot || "").trim(), _.dataset.tavernImageHydrating = "false", _.dataset.tavernImageLoaded = "false", _.dataset.state = "failed";
    const C = String(b.slotId || _.dataset.tavernImageSlot || "").trim(), k = !!b.isFailed, P = String(b.errorType || "").trim(), H = String(b.errorMessage || "").trim(), G = document.createElement("span");
    G.className = "xb-tavern-image-wrap xb-tavern-image-failed-wrap";
    const ee = document.createElement("span");
    ee.className = "xb-tavern-image-failed-icon", ee.textContent = k ? "⚠️" : "🌫️", G.append(ee);
    const J = document.createElement("span");
    J.className = "xb-tavern-image-failed-title", J.textContent = P || (k ? "生成失败" : "配图未找到"), G.append(J);
    const z = document.createElement("span");
    if (z.className = "xb-tavern-image-failed-desc", z.textContent = H || (k ? "点击重试" : ""), z.textContent && G.append(z), k && C) {
      const R = document.createElement("span");
      R.className = "xb-tavern-image-failed-actions";
      const V = document.createElement("button");
      V.type = "button", V.dataset.action = "refresh-image", V.textContent = "⟳ 重新生成";
      const le = document.createElement("button");
      le.type = "button", le.dataset.action = "edit-tags", le.textContent = "✐ 编辑TAG", R.append(V, le), G.append(R);
      const { editPanel: ve, openEditor: ke } = l(_, C, b, {
        inline: !0,
        saveLabel: "保存并重试",
        retryAfterSave: !0,
        onToggle: (be) => {
          R.style.opacity = be ? "0.3" : "", R.style.pointerEvents = be ? "none" : "", R.querySelectorAll("button").forEach((Ne) => {
            Ne.dataset.editDisabled = be ? "true" : "false", Ne.disabled = be || _.classList.contains("is-busy") || Ne.dataset.navDisabled === "true";
          });
        }
      });
      G.append(ve), R.addEventListener("click", (be) => {
        const Ne = be.target?.closest("[data-action]");
        if (!Ne) return;
        be.preventDefault(), be.stopPropagation();
        const ce = Ne.dataset.action || "";
        if (ce === "refresh-image") {
          u(_, !0), e.requestHost("xb-tavern:draw-image-refresh", { payload: { slotId: C } }).then((we) => y(_, C, we)).catch(() => u(_, !1));
          return;
        }
        ce === "edit-tags" && ke();
      });
    }
    _.replaceChildren(G);
  }
  function E(_, b) {
    const C = String(b.slotId || _.dataset.tavernImageSlot || "").trim(), k = String(b.url || "").trim(), P = Math.max(1, Math.floor(Number(b.historyCount) || 1)), H = Math.max(0, Math.min(P - 1, Math.floor(Number(b.currentIndex) || 0))), G = P - H;
    _.classList.add("is-loaded"), _.classList.remove("is-failed", "is-busy"), _.dataset.tavernImageSlot = C, _.dataset.tavernImageLoaded = "true", _.dataset.tavernImageIndex = String(H), _.dataset.tavernImageHistory = String(P), _.dataset.tavernImageImgId = String(b.imgId || ""), _.dataset.state = b.saved ? "saved" : "preview";
    const ee = document.createElement("span");
    ee.className = "xb-tavern-image-wrap";
    const J = document.createElement("img");
    J.src = k, J.alt = b.tags ? `配图：${String(b.tags)}` : "配图", J.loading = "lazy", J.addEventListener("click", () => {
      x(C, _);
    }), ee.append(J);
    const z = document.createElement("span");
    z.className = "xb-tavern-image-nav";
    const R = document.createElement("button");
    R.type = "button", R.className = "xb-tavern-image-nav-button", R.dataset.action = "older", R.title = "上一版本", R.textContent = "‹", R.disabled = H >= P - 1, R.dataset.navDisabled = R.disabled ? "true" : "false";
    const V = document.createElement("span");
    V.className = "xb-tavern-image-nav-text", V.textContent = `${G} / ${P}`;
    const le = document.createElement("button");
    le.type = "button", le.className = "xb-tavern-image-nav-button", le.dataset.action = "newer", le.title = H === 0 ? "重新生成" : "下一版本", le.textContent = "›", le.disabled = !1, le.dataset.navDisabled = "false", z.append(R, V, le), ee.append(z);
    const ve = document.createElement("span");
    ve.className = "xb-tavern-image-menu";
    const ke = document.createElement("button");
    ke.type = "button", ke.className = "xb-tavern-image-menu-trigger", ke.title = "操作", ke.textContent = "⋮";
    const be = document.createElement("span");
    be.className = "xb-tavern-image-dropdown", [
      ...b.saved ? [] : [{
        action: "save-image",
        title: "保存到服务器",
        label: "⬇"
      }],
      {
        action: "refresh-image",
        title: "重新生成",
        label: "⟳"
      },
      {
        action: "edit-tags",
        title: "编辑TAG",
        label: "✐"
      },
      {
        action: "delete-image",
        title: "删除",
        label: "✕"
      }
    ].forEach((F) => {
      const ae = document.createElement("button");
      ae.type = "button", ae.dataset.action = F.action, ae.title = F.title, ae.textContent = F.label, be.append(ae);
    }), ve.append(ke, be), ee.append(ve);
    const { editPanel: Ne, openEditor: ce } = l(_, C, b);
    ee.append(Ne), _.replaceChildren(ee);
    const we = async (F) => {
      u(_, !0);
      try {
        const ae = await e.requestHost("xb-tavern:draw-image-select", { payload: {
          slotId: C,
          index: F
        } });
        if (!n(_, C)) return;
        E(_, r(ae));
      } catch {
        u(_, !1);
      }
    }, Oe = async () => {
      u(_, !0);
      try {
        await y(_, C, await e.requestHost("xb-tavern:draw-image-refresh", { payload: { slotId: C } }));
      } catch (F) {
        u(_, !1), f(F);
      }
    };
    R.addEventListener("click", (F) => {
      F.preventDefault(), F.stopPropagation(), H < P - 1 && we(H + 1);
    }), le.addEventListener("click", (F) => {
      if (F.preventDefault(), F.stopPropagation(), H > 0) {
        we(H - 1);
        return;
      }
      Oe();
    }), ke.addEventListener("click", (F) => {
      F.preventDefault(), F.stopPropagation();
      const ae = !ve.classList.contains("is-open");
      m(), ve.classList.toggle("is-open", ae);
    }), be.addEventListener("click", (F) => {
      const ae = F.target?.closest("[data-action]");
      if (!ae) return;
      F.preventDefault(), F.stopPropagation(), m();
      const Z = ae.dataset.action || "";
      Z === "save-image" ? (u(_, !0), e.requestHost("xb-tavern:draw-image-save", { payload: { slotId: C } }).then((ie) => {
        n(_, C) && E(_, r(ie));
      }).catch(() => u(_, !1))) : Z === "refresh-image" ? Oe() : Z === "edit-tags" ? ce() : Z === "delete-image" && (async () => await e.confirmDialog({
        title: "删除图片",
        message: "确定删除这张图片吗？",
        confirmText: "删除",
        tone: "danger"
      }) && (u(_, !0), e.requestHost("xb-tavern:draw-image-delete", { payload: { slotId: C } }).then((ie) => y(_, C, ie)).catch(() => u(_, !1))))();
    });
  }
  function N(_) {
    const b = String(_.dataset.tavernImageSlot || "").trim();
    !b || _.dataset.tavernImageHydrating === "true" || _.dataset.tavernImageLoaded === "true" || (_.dataset.tavernImageHydrating = "true", e.requestHost("xb-tavern:draw-image", { payload: { slotId: b } }).then((C) => {
      if (!n(_, b)) return;
      const k = C.result || C;
      if (_.dataset.tavernImageHydrating = "false", !k.hasData || !k.url) {
        I(_, k);
        return;
      }
      E(_, k);
    }).catch(() => {
      if (!n(_, b)) return;
      _.dataset.tavernImageHydrating = "false", _.classList.add("is-failed");
      const C = document.createElement("span");
      C.className = "xb-tavern-image-placeholder", C.textContent = "配图加载失败", _.replaceChildren(C);
    }));
  }
  function $(_ = "") {
    const b = document.createElement("span");
    b.className = "xb-tavern-image", b.dataset.tavernImageSlot = _;
    const C = document.createElement("span");
    return C.className = "xb-tavern-image-placeholder", C.textContent = "配图加载中", b.append(C), b;
  }
  function p(_) {
    const b = _.parentNode;
    if (!b) return;
    const C = (k) => {
      let P = _[k];
      for (; P?.nodeType === Node.TEXT_NODE && !String(P.textContent || "").trim(); ) {
        const H = P[k];
        b.removeChild(P), P = H;
      }
      if (P instanceof HTMLBRElement) {
        const H = P[k];
        for (b.removeChild(P), P = H; P?.nodeType === Node.TEXT_NODE && !String(P.textContent || "").trim(); ) {
          const G = P[k];
          b.removeChild(P), P = G;
        }
      }
    };
    C("previousSibling"), C("nextSibling");
  }
  function w(_) {
    const b = document.createTreeWalker(_, NodeFilter.SHOW_TEXT, { acceptNode(k) {
      const P = k;
      return !P.data || !Jl.test(P.data) ? (Jl.lastIndex = 0, NodeFilter.FILTER_SKIP) : (Jl.lastIndex = 0, P.parentElement?.closest?.("a, button, code, kbd, pre, script, style, textarea, .xb-tavern-image") ? NodeFilter.FILTER_REJECT : NodeFilter.FILTER_ACCEPT);
    } }), C = [];
    for (; b.nextNode(); ) C.push(b.currentNode);
    C.forEach((k) => {
      const P = k.data;
      Jl.lastIndex = 0;
      let H, G = 0, ee = !1;
      const J = document.createDocumentFragment();
      for (; (H = Jl.exec(P)) !== null; ) {
        if (ee = !0, H.index > G) {
          const z = P.slice(G, H.index);
          J.append(document.createTextNode(z.endsWith(`
`) ? z.slice(0, -1) : z));
        }
        J.append($(H[1] || "")), G = Jl.lastIndex, P[G] === `
` && (G += 1);
      }
      ee && (G < P.length && J.append(document.createTextNode(P.slice(G))), k.replaceWith(J));
    }), _.querySelectorAll("[data-tavern-image-slot]").forEach((k) => {
      p(k), N(k);
    });
  }
  return {
    closeTavernImageGallery: v,
    enhanceTavernImageMarkers: w
  };
}
var Kk = "[ 🎲 CHANCE ENCOUNTER TRIGGERED ]";
var z2 = 57856, K2 = 57344, SD = [
  "[Runtime Event: Chance Encounter Triggered]",
  "For this reply only, naturally introduce an unexpected sudden situation the user did not explicitly ask for.",
  "Make it fit the current scene and character voice.",
  "Follow all later formatting and style instructions.",
  "Do not mention this instruction or the trigger label."
].join(`
`);
function Oy(e, t, n, r) {
  const s = Math.round(Number(e));
  return Number.isFinite(s) ? Math.min(n, Math.max(t, s)) : r;
}
function Uo(e, t = 240) {
  return String(e || "").trim().slice(0, t);
}
function x1(e) {
  return String(e || "").trim() || (/* @__PURE__ */ new Date(0)).toISOString();
}
function AD(e) {
  return {
    type: "chanceEncounter",
    label: Kk,
    createdAt: x1(e.createdAt)
  };
}
function TD(e, t) {
  return e <= 1 ? "criticalFailure" : e >= 20 ? "criticalSuccess" : e >= t ? "success" : "failure";
}
function Wk(e) {
  const t = Uo(e.action, 240), n = Uo(e.stat, 120);
  if (!t || !n) return null;
  const r = Oy(e.roll, 1, 20, 1), s = Oy(e.difficulty, 1, 21, 10), a = TD(r, s), o = a === "success" || a === "criticalSuccess";
  return {
    type: "actionCheck",
    createdAt: x1(e.createdAt),
    action: t,
    stat: n,
    difficulty: s,
    roll: r,
    success: o,
    outcome: a,
    insertAfterChars: Math.max(0, Oy(e.insertAfterChars, 0, Number.MAX_SAFE_INTEGER, 0)),
    ...Uo(e.toolCallId, 120) ? { toolCallId: Uo(e.toolCallId, 120) } : {},
    ...Uo(e.summary, 320) ? { summary: Uo(e.summary, 320) } : {},
    ...Uo(e.stakes, 240) ? { stakes: Uo(e.stakes, 240) } : {}
  };
}
function ED(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : null, n = String(t?.type || "").trim();
  return t ? n === "chanceEncounter" ? AD(t) : n === "actionCheck" ? Wk(t) : null : null;
}
function CD(e) {
  return e.type === "chanceEncounter" ? e.type : [
    e.type,
    e.toolCallId || "",
    e.createdAt,
    String(e.insertAfterChars),
    e.stat,
    e.action,
    String(e.roll),
    String(e.difficulty),
    e.outcome
  ].join("\0");
}
function k1(e) {
  const t = Array.isArray(e) ? e : [], n = [], r = /* @__PURE__ */ new Set();
  return t.forEach((s) => {
    const a = ED(s);
    if (!a) return;
    const o = CD(a);
    r.has(o) || (r.add(o), n.push(a));
  }), n;
}
function ID(e = (/* @__PURE__ */ new Date()).toISOString()) {
  return {
    type: "chanceEncounter",
    label: Kk,
    createdAt: x1(e)
  };
}
function xD(e) {
  return Wk({
    type: "actionCheck",
    createdAt: e.createdAt || (/* @__PURE__ */ new Date()).toISOString(),
    action: e.action,
    stat: e.stat,
    difficulty: e.difficulty,
    roll: e.roll,
    success: e.success,
    outcome: e.outcome,
    insertAfterChars: e.insertAfterChars,
    toolCallId: e.toolCallId,
    summary: e.summary,
    stakes: e.stakes
  });
}
function M1(e) {
  return k1(e).find((t) => t.type === "chanceEncounter") || null;
}
function kD(e) {
  return !!M1(e);
}
function di(e) {
  return k1(e).filter((t) => t.type === "actionCheck");
}
function MD(e) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = t && typeof t == "object" && !Array.isArray(t) ? t : {}, r = String(n.marker || "").slice(0, 1), s = di(n.events);
    return r && s.length ? {
      marker: r,
      events: s
    } : null;
  }).filter((t) => !!t);
}
function PD(e, t = []) {
  const n = di(t).slice().sort((l, i) => l.insertAfterChars - i.insertAfterChars);
  if (!n.length) return {
    text: e,
    groups: []
  };
  const r = [];
  if (n.forEach((l) => {
    const i = Math.max(0, Math.min(e.length, Number(l.insertAfterChars) || 0)), u = r[r.length - 1];
    if (u && u.offset === i) {
      u.events.push(l);
      return;
    }
    r.push({
      offset: i,
      events: [l]
    });
  }), r.length > 63743 - z2) return {
    text: e,
    groups: []
  };
  let s = 0, a = "";
  const o = r.map((l, i) => {
    const u = String.fromCharCode(z2 + i);
    return a += e.slice(s, l.offset) + u, s = l.offset, {
      marker: u,
      events: l.events
    };
  });
  return a += e.slice(s), {
    text: a,
    groups: o
  };
}
function sb(e, t = []) {
  const n = String(e || ""), r = [...new Set(di(t).map((l) => Math.max(0, Math.min(n.length, Number(l.insertAfterChars) || 0))))].sort((l, i) => l - i);
  if (!r.length || r.length > 63743 - K2) return {
    text: n,
    boundaries: []
  };
  let s = 0, a = "";
  const o = r.map((l, i) => {
    const u = String.fromCharCode(K2 + i);
    return a += n.slice(s, l) + u, s = l, {
      originalOffset: l,
      marker: u
    };
  });
  return a += n.slice(s), {
    text: a,
    boundaries: o
  };
}
function Gk(e, t = [], n = []) {
  const r = String(e || ""), s = di(t);
  if (!n.length || !s.length) return {
    text: r,
    events: s
  };
  const a = new Map(n.map((i) => [i.marker, i.originalOffset])), o = /* @__PURE__ */ new Map();
  let l = "";
  for (let i = 0; i < r.length; i += 1) {
    const u = r[i], d = a.get(u);
    if (d !== void 0) {
      o.has(d) || o.set(d, l.length);
      continue;
    }
    l += u;
  }
  return {
    text: l,
    events: s.map((i) => {
      const u = Math.max(0, Number(i.insertAfterChars) || 0);
      return {
        ...i,
        insertAfterChars: o.has(u) ? o.get(u) : Math.max(0, Math.min(l.length, u))
      };
    })
  };
}
function Vk() {
  return {
    role: "system",
    content: SD
  };
}
var RD = /\[tavern-image:([a-z0-9\-_]+)\]/gi, Wi = /\[(?:img|图片)\s*:\s*([^\]]+)\]/gi, ab = "xb-tavern:inline-image-progress";
function W2(e = "", t = "") {
  return String(e || "").trim() || t;
}
function ND(e = "", t = {}) {
  const n = W2(t.userName, "User"), r = W2(t.characterName, "角色");
  return String(e || "").replace(/<\s*user\s*>/gi, n).replace(/<\s*(?:char|bot)\s*>/gi, r).replace(/\{\{\s*user\s*\}\}/gi, n).replace(/\{\{\s*(?:char|bot)\s*\}\}/gi, r);
}
function $D(e = "", t = {}) {
  return ND(e, t);
}
var Fy = "iframe.xb-tavern-html-iframe", By = 'pre[data-xb-tavern-html-final="true"]', LD = 3e5, DD = /* @__PURE__ */ new Set([
  "html",
  "htm",
  "xhtml",
  "xml",
  "svg",
  "vue",
  "svelte"
]), OD = /^\s*(?:<!--[\s\S]*?-->\s*)*<(?:style|link|meta|svg|iframe|canvas|img|video|audio|picture|div|section|main|article|header|footer|nav|aside|p|span|button|input|textarea|select|label|ul|ol|li|table|thead|tbody|tr|td|th|form|figure|figcaption|details|summary|dialog|h[1-6])\b/i, FD = /* @__PURE__ */ new Set([
  "generatePromptPreview",
  "generateStreamStart",
  "generateStreamChunk",
  "generateStreamComplete",
  "generateStreamError",
  "generateResult",
  "generateError"
]);
function ob(e = "") {
  const t = String(e || "").trim();
  if (!t) return null;
  if (/^https?:\/\/[^\s]+$/i.test(t)) return t;
  const n = t.match(/<!--\s*xb-src:\s*(https?:\/\/[^\s>]+)\s*-->/i);
  return n ? n[1] : null;
}
function BD(e = "") {
  let t = 5381;
  for (let n = 0; n < e.length; n += 1) t = (t << 5) + t ^ e.charCodeAt(n);
  return (t >>> 0).toString(16);
}
function G2(e = "") {
  const t = String(e || "").trim();
  if (!t) return !1;
  if (ob(t)) return !0;
  const n = t.toLowerCase();
  return n.includes("<!doctype") || n.includes("<html") || n.includes("<script") || OD.test(t);
}
function UD(e = "") {
  const t = Array.from(new Set((String(e || "").match(/https?:\/\/[^"'()\s]+/gi) || []).map((o) => {
    try {
      return new URL(o).origin;
    } catch {
      return "";
    }
  }).filter(Boolean)));
  let n = "";
  t.slice(0, 6).forEach((o) => {
    n += `<link rel="dns-prefetch" href="${o}">`, n += `<link rel="preconnect" href="${o}" crossorigin>`;
  });
  const r = (String(e || "").match(/https?:\/\/[^"'()\s]+\.(?:woff2|woff|ttf|otf)/i) || [])[0];
  if (r) {
    const o = r.endsWith(".woff2") ? "font/woff2" : r.endsWith(".woff") ? "font/woff" : r.endsWith(".ttf") ? "font/ttf" : "font/otf";
    n += `<link rel="preload" as="font" href="${r}" type="${o}" crossorigin fetchpriority="high">`;
  }
  const s = (String(e || "").match(/https?:\/\/[^"'()\s]+\.css/i) || [])[0];
  s && (n += `<link rel="preload" as="style" href="${s}" crossorigin fetchpriority="high">`);
  const a = (String(e || "").match(/https?:\/\/[^"'()\s]+\.(?:png|jpg|jpeg|webp|gif|svg)/i) || [])[0];
  return a && (n += `<link rel="preload" as="image" href="${a}" crossorigin fetchpriority="high">`), n;
}
function qD(e = !1) {
  return e ? "dark" : "light";
}
function HD(e) {
  return `<meta name="color-scheme" content="${e}"><style id="xb-tavern-html-theme">:root{color-scheme:${e}}</style><script>(function(){try{var r=document.documentElement;r.dataset.xbTavernTheme='${e}';r.classList.add('xb-tavern-theme-${e}');r.style.colorScheme='${e}';}catch(_){}})();<\/script>`;
}
function zD(e) {
  return `<html data-xb-tavern-theme="${e}" class="xb-tavern-theme-${e}" style="color-scheme:${e}">`;
}
function KD(e = "", t) {
  const n = `xb-tavern-theme-${t}`;
  return String(e || "").replace(/<html\b([^>]*)>/i, (r, s = "") => {
    let a = String(s || "").replace(/\sdata-xb-tavern-theme=(?:"[^"]*"|'[^']*'|[^\s>]*)/i, "");
    return /\sclass\s*=/.test(a) ? a = a.replace(/(\sclass\s*=\s*)(["'])(.*?)\2/i, (o, l, i, u) => `${l}${i}${String(u || "").trim()} ${n}${i}`) : a += ` class="${n}"`, /\sstyle\s*=/.test(a) ? a = a.replace(/(\sstyle\s*=\s*)(["'])(.*?)\2/i, (o, l, i, u) => `${l}${i}${String(u || "").replace(/;\s*$/, "")};color-scheme:${t}${i}`) : a += ` style="color-scheme:${t}"`, a += ` data-xb-tavern-theme="${t}"`, `<html${a}>`;
  });
}
function V2(e = "", t = !1) {
  const n = String(e || ""), r = `<script>${bD()}${_D()}<\/script>`, s = UD(n), a = qD(t), o = `${HD(a)}${r}${s}<style>html,body{height:auto!important;min-height:0!important;max-height:none!important}.profile-container,[style*="100vh"]{height:auto!important;min-height:600px!important}[style*="height:100%"]{height:auto!important;min-height:100%!important}</style>`;
  if (n.includes("<html") && n.includes("</html")) {
    const l = KD(n, a);
    return l.includes("<head>") ? l.replace("<head>", `<head>${o}`) : l.includes("</head>") ? l.replace("</head>", `${o}</head>`) : l.replace("<body", `<head>${o}</head><body`);
  }
  return `<!DOCTYPE html>
${zD(a)}
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
${o}
<style>html,body{margin:0;padding:0;background:transparent}</style>
</head>
<body>${n}</body></html>`;
}
function WD(e) {
  const t = /* @__PURE__ */ new Map(), n = /* @__PURE__ */ new Map(), r = /* @__PURE__ */ new WeakMap(), s = /* @__PURE__ */ new WeakMap();
  function a(X) {
    return e.preserveChatScroll ? e.preserveChatScroll(X) : X();
  }
  function o(X) {
    return !X || !!X.closest('[hidden], [aria-hidden="true"]');
  }
  function l(X) {
    return !!X && !o(X);
  }
  function i(X) {
    return !!(X && X.isConnected !== !1 && !o(X) && !X.closest(".streaming, .pending-user"));
  }
  const u = wD({
    alertDialog: e.alertDialog,
    confirmDialog: e.confirmDialog,
    requestHost: e.requestHost,
    showToast: e.showToast,
    isHiddenMarkdownNode: o
  });
  function d(X = "") {
    const se = String(X || "");
    let Ae = 0;
    for (let Re = 0; Re < se.length; Re += 1) Ae = Ae * 31 + se.charCodeAt(Re) >>> 0;
    return `${se.length}:${Ae.toString(36)}`;
  }
  function f(X = "", se = {}) {
    const Ae = se.roleplay ? $D(X, se) : String(X || ""), Re = se.roleplay ? {
      htmlFenceMode: "code",
      protectRawHtmlBoundaries: !1
    } : {}, Be = !/(^|\n)(`{3,}|~{3,})[ \t]*(html|htm|xhtml|xml|svg|vue|svelte)?\b/i.test(Ae) && !se.roleplay, je = d(Ae);
    if (Be && t.has(je)) return t.get(je) || "";
    const Qe = fD(Ae, Re);
    if (Be && !Qe.includes("xb-markdown-html-placeholder") && (t.set(je, Qe), t.size > 160)) {
      const ut = t.keys().next().value;
      ut && t.delete(ut);
    }
    return Qe;
  }
  function m(X = "") {
    return String(X || "").replace(RD, "").replace(Wi, "").trim();
  }
  function v() {
    t.clear();
  }
  function y(X) {
    if (!X) return null;
    const se = [
      e.chatScrollRef.value,
      e.managerScrollRef.value,
      e.managerWorkRef?.value
    ].filter(Boolean);
    for (const Ae of se) {
      const Re = Array.from(Ae.querySelectorAll(Fy)).find((Be) => Be.contentWindow === X);
      if (Re) return Re;
    }
    return null;
  }
  function S(X) {
    const se = String(X || "").trim();
    return se && se !== "null" ? se : "*";
  }
  function x(X) {
    try {
      return JSON.parse(JSON.stringify(X));
    } catch {
      return X;
    }
  }
  function I(X, se, Ae = window.location.origin) {
    H2(X, se, void 0, S(Ae));
  }
  function E(X) {
    const se = x(X);
    window.parent?.postMessage(se, window.location.origin);
  }
  function N(X) {
    const se = n.get(X);
    return se ? (window.clearTimeout(se.timeoutId), n.delete(X), !0) : !1;
  }
  function $(X) {
    Array.from(n.entries()).forEach(([se, Ae]) => {
      Ae.iframe === X && N(se);
    });
  }
  function p() {
    Array.from(n.keys()).forEach((X) => {
      N(X);
    });
  }
  function w(X) {
    return X.source === "xiaobaix-host" && typeof X.id == "string" && FD.has(String(X.type || ""));
  }
  function _(X) {
    const se = String(X.id || ""), Ae = n.get(se);
    if (!Ae) return !1;
    I(Ae.iframe, X, Ae.targetOrigin);
    const Re = String(X.type || "");
    return (Re === "generateResult" || Re === "generateError" || Re === "generateStreamComplete" || Re === "generateStreamError") && N(se), !0;
  }
  function b(X, se, Ae) {
    N(X);
    const Re = window.setTimeout(() => {
      n.get(X)?.iframe === se && n.delete(X);
    }, LD);
    n.set(X, {
      iframe: se,
      targetOrigin: Ae,
      timeoutId: Re
    });
  }
  function C(X) {
    if (!X?.classList.contains("xb-tavern-html-wrapper")) return;
    const se = X.querySelector(Fy);
    if (se) {
      $(se);
      const Ae = s.get(se);
      Ae && (window.cancelAnimationFrame(Ae), s.delete(se)), r.delete(se);
    }
    a(() => {
      X.remove();
    });
  }
  function k(X, se, Ae = !1) {
    const Re = Math.max(0, Number(se) || 0);
    if (Re < 1) return;
    const Be = Math.ceil(Re), je = r.get(X) || 0;
    if (!Ae && Math.abs(Be - je) < 1) return;
    r.set(X, Be);
    const Qe = s.get(X);
    Qe && window.cancelAnimationFrame(Qe);
    const ut = window.requestAnimationFrame(() => {
      s.delete(X), X.isConnected && a(() => {
        X.style.height = `${Be}px`;
      });
    });
    s.set(X, ut);
  }
  function P(X, se = 0) {
    const Ae = () => {
      try {
        H2(X, { type: "probe" }, void 0, window.location.origin);
      } catch {
      }
    };
    if (se > 0) {
      window.setTimeout(Ae, se);
      return;
    }
    Ae();
  }
  function H(X) {
    const se = X.data && typeof X.data == "object" ? X.data : {};
    if (X.source === window.parent && w(se)) {
      _(se);
      return;
    }
    const Ae = y(X.source);
    if (!Ae) return;
    const Re = S(X.origin), Be = X.data && typeof X.data == "object" ? X.data : {};
    if (typeof Be.height == "number") {
      k(Ae, Be.height, !!Be.force);
      return;
    }
    if (Be.type === "getAvatars") {
      const je = e.getHtmlFrameAvatarUrls?.() || {};
      I(Ae, {
        source: "xiaobaix-host",
        type: "avatars",
        urls: {
          user: String(je.user || ""),
          char: String(je.char || "")
        }
      }, Re);
      return;
    }
    if (Be.type === "runCommand") {
      const je = String(Be.id || ""), Qe = String(Be.command || "");
      e.requestHost("xb-tavern:run-slash-command", { payload: { command: Qe } }).then((ut) => {
        I(Ae, {
          source: "xiaobaix-host",
          type: "commandResult",
          id: je,
          result: ut.result || ut
        }, Re);
      }).catch((ut) => {
        I(Ae, {
          source: "xiaobaix-host",
          type: "commandError",
          id: je,
          error: ut instanceof Error ? ut.message : String(ut || "slash_command_failed")
        }, Re);
      });
      return;
    }
    if (Be.type === "generateRequest") {
      const je = String(Be.id || "");
      if (!je) return;
      b(je, Ae, Re), E({
        type: "generateRequest",
        id: je,
        options: Be.options && typeof Be.options == "object" ? Be.options : {}
      });
    }
  }
  function G(X) {
    const se = X.previousElementSibling;
    C(se), X.style.display = "", delete X.dataset.xbTavernHtmlFinal, delete X.dataset.xbTavernHtmlHash, delete X.dataset.xbTavernHtmlPending;
  }
  async function ee(X) {
    try {
      const se = await fetch(X, { mode: "cors" });
      if (se.ok) return await se.text();
    } catch {
    }
    return null;
  }
  async function J(X = "") {
    const se = String(X || "");
    if (!se.includes("{{xbgetvar::")) return se;
    try {
      const Ae = await e.requestHost("xb-tavern:replace-html-render-vars", { payload: { html: se } }), Re = Ae.result && typeof Ae.result == "object" ? Ae.result : Ae;
      return typeof Re.html == "string" ? Re.html : se;
    } catch (Ae) {
      return console.warn("[LittleWhiteBox Tavern] xbgetvar macro replacement failed:", Ae), se;
    }
  }
  async function z(X, se) {
    try {
      X.srcdoc = '<!DOCTYPE html><html><body style="display:flex;justify-content:center;align-items:center;height:100px;color:#888;font-family:sans-serif;background:transparent">加载中...</body></html>';
      let Ae = await ee(se);
      if (Ae) {
        Ae = await J(Ae), X.srcdoc = V2(Ae, e.htmlThemeDark.value), P(X, 100);
        return;
      }
      X.removeAttribute("srcdoc"), X.src = se, X.style.minHeight = "800px", X.setAttribute("scrolling", "auto");
    } catch (Ae) {
      console.error("[LittleWhiteBox Tavern] external HTML preview failed:", Ae), X.removeAttribute("srcdoc"), X.src = se, X.style.minHeight = "800px", X.setAttribute("scrolling", "auto");
    }
  }
  async function R(X, se = "") {
    const Ae = String(se || ""), Re = ob(Ae);
    if (Re) {
      await z(X, Re);
      return;
    }
    X.srcdoc = V2(await J(Ae), e.htmlThemeDark.value), P(X);
  }
  function V(X, se = "", Ae = "") {
    const Re = X.previousElementSibling;
    C(Re);
    const Be = document.createElement("div");
    Be.className = "xb-tavern-html-wrapper";
    const je = document.createElement("iframe");
    je.className = "xb-tavern-html-iframe", je.setAttribute("frameborder", "0"), je.setAttribute("scrolling", "no"), je.loading = "eager", je.style.cssText = "width:100%;border:none;background:transparent;overflow:hidden;height:0;margin:0;padding:0;display:block;contain:layout paint style;will-change:height;min-height:50px", Be.append(je), X.parentNode?.insertBefore(Be, X), X.style.display = "none", X.dataset.xbTavernHtmlFinal = "true", X.dataset.xbTavernHtmlHash = Ae, delete X.dataset.xbTavernHtmlPending, R(je, se);
  }
  function le(X) {
    e.htmlRenderEnabled.value && X.querySelectorAll("pre > code").forEach((se) => {
      const Ae = se.parentElement;
      !Ae || Ae.dataset.xbTavernHtmlFinal === "true" || be(se) && (Ae.style.display = "none", Ae.dataset.xbTavernHtmlPending = "true");
    });
  }
  function ve(X) {
    l(X) && (X.querySelectorAll(By).forEach((se) => {
      e.htmlRenderEnabled.value || G(se);
    }), X.querySelectorAll("pre > code").forEach((se) => {
      const Ae = se.parentElement;
      if (!Ae) return;
      if (!be(se)) {
        G(Ae);
        return;
      }
      const Re = se.textContent || "";
      if (!G2(Re)) {
        G(Ae);
        return;
      }
      if (!e.htmlRenderEnabled.value) {
        G(Ae);
        return;
      }
      const Be = BD(Re), je = ob(Re), Qe = Ae.previousElementSibling, ut = Ae.dataset.xbTavernHtmlFinal === "true" && Ae.dataset.xbTavernHtmlHash === Be && Qe?.classList.contains("xb-tavern-html-wrapper") && !!Qe.querySelector(Fy);
      !je && ut || V(Ae, Re, Be);
    }));
  }
  function ke(X) {
    return Array.from(X.classList).some((se) => {
      const Ae = String(se || "").replace(/^custom-/, "").replace(/^language-/, "").toLowerCase();
      return DD.has(Ae);
    });
  }
  function be(X) {
    return ke(X) || G2(X.textContent || "");
  }
  const Ne = {
    '"': '"',
    "“": "”",
    "「": "」",
    "『": "』"
  }, ce = new Set(Object.keys(Ne)), we = /* @__PURE__ */ new Set([
    "A",
    "BUTTON",
    "CODE",
    "KBD",
    "PRE",
    "SCRIPT",
    "STYLE",
    "TEXTAREA"
  ]), Oe = 600;
  function F(X) {
    let se = X.parentElement;
    for (; se; ) {
      if (we.has(se.tagName) || se.classList.contains("xb-rp-dialogue")) return !0;
      se = se.parentElement;
    }
    return !1;
  }
  function ae(X) {
    const se = [];
    let Ae = 0;
    for (; Ae < X.length; ) {
      const Re = X[Ae];
      if (!ce.has(Re)) {
        Ae += 1;
        continue;
      }
      const Be = Ne[Re];
      let je = X.indexOf(Be, Ae + 1);
      if (je < 0 || je === Ae + 1 || je - Ae > Oe) {
        Ae += 1;
        continue;
      }
      const Qe = X.slice(Ae + 1, je);
      if (Qe.includes(`
`) || !Qe.trim()) {
        Ae += 1;
        continue;
      }
      se.push({
        start: Ae,
        end: je + 1
      }), Ae = je + 1;
    }
    return se;
  }
  function Z(X) {
    const se = document.createTreeWalker(X, NodeFilter.SHOW_TEXT, { acceptNode(Re) {
      const Be = Re;
      return !Be.data || F(Be) ? NodeFilter.FILTER_REJECT : ae(Be.data).length ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
    } }), Ae = [];
    for (; se.nextNode(); ) Ae.push(se.currentNode);
    Ae.forEach((Re) => {
      const Be = ae(Re.data);
      if (!Be.length) return;
      const je = document.createDocumentFragment();
      let Qe = 0;
      Be.forEach((ut) => {
        ut.start > Qe && je.append(document.createTextNode(Re.data.slice(Qe, ut.start)));
        const wt = document.createElement("span");
        wt.className = "xb-rp-dialogue", wt.textContent = Re.data.slice(ut.start, ut.end), je.append(wt), Qe = ut.end;
      }), Qe < Re.data.length && je.append(document.createTextNode(Re.data.slice(Qe))), Re.replaceWith(je);
    });
  }
  function ie(X = "") {
    let se = String(X || "").trim();
    return se = se.replace(/^(nsfw|sketchy)\s*:\s*/i, "nsfw, "), se.split(",").map((Ae) => Ae.trim()).filter(Boolean).join(", ");
  }
  function me(X = "") {
    return encodeURIComponent(ie(X));
  }
  function Se(X) {
    const se = X.parentNode;
    if (!se) return;
    const Ae = (Re) => {
      let Be = X[Re];
      for (; Be?.nodeType === Node.TEXT_NODE && !String(Be.textContent || "").trim(); ) {
        const je = Be[Re];
        se.removeChild(Be), Be = je;
      }
      if (Be instanceof HTMLBRElement) {
        const je = Be[Re];
        for (se.removeChild(Be), Be = je; Be?.nodeType === Node.TEXT_NODE && !String(Be.textContent || "").trim(); ) {
          const Qe = Be[Re];
          se.removeChild(Be), Be = Qe;
        }
      }
    };
    Ae("previousSibling"), Ae("nextSibling");
  }
  function W(X = "") {
    const se = String(X || "").trim();
    return se ? /^data:[^;]+;base64,/i.test(se) ? se : `data:image/png;base64,${se}` : "";
  }
  function L(X, se, Ae = "loading") {
    a(() => {
      X.dataset.state = Ae;
      const Re = document.createElement("span");
      Re.className = `xb-tavern-inline-image-placeholder is-${Ae}`, Re.textContent = se, X.replaceChildren(Re);
    });
  }
  function Q(X, se = "") {
    const Ae = W(se);
    if (!Ae) throw new Error("image_data_missing");
    a(() => {
      X.dataset.loaded = "1", X.dataset.loading = "", X.dataset.state = "loaded";
      const Re = document.createElement("img");
      Re.className = "xb-tavern-inline-image-img", Re.src = Ae, Re.loading = "lazy", Re.addEventListener("click", () => window.open(Ae, "_blank")), X.replaceChildren(Re);
    });
  }
  function de(X, se = "", Ae) {
    a(() => {
      X.dataset.loaded = "1", X.dataset.loading = "", X.dataset.state = "error";
      const Re = document.createElement("span");
      Re.className = "xb-tavern-inline-image-error";
      const Be = document.createElement("span");
      Be.textContent = Ae instanceof Error ? Ae.message : String(Ae || "生成失败");
      const je = document.createElement("button");
      je.type = "button", je.textContent = "重试", je.addEventListener("click", (Qe) => {
        Qe.stopPropagation(), X.dataset.loaded = "", Me(X, se);
      }), Re.append(Be, je), X.replaceChildren(Re);
    });
  }
  function ge(X = "", se = {}) {
    const Ae = Math.max(0, Math.floor(Number(se.position) || 0)), Re = Math.max(0, Math.floor(Number(se.delay) || 0));
    if (X === "queued") {
      const Be = Math.max(0, Ae - 1);
      return Be > 0 ? `排队中，前面还有 ${Be} 张` : "排队中";
    }
    return X === "generating" ? "正在生成" : X === "waiting" ? Re > 0 ? `等待冷却 ${Re}s` : "等待冷却" : "检查缓存...";
  }
  function pe(X = {}) {
    const se = ie(String(X.tags || ""));
    if (!se) return;
    const Ae = me(se), Re = ge(String(X.status || "").trim(), X);
    document.querySelectorAll('.xb-tavern-inline-image[data-loading="1"]').forEach((Be) => {
      String(Be.dataset.tags || "") === Ae && i(Be) && L(Be, Re, "loading");
    });
  }
  async function Me(X, se = "") {
    const Ae = ie(se || decodeURIComponent(X.dataset.tags || ""));
    if (!(!Ae || X.dataset.loading === "1" || X.dataset.loaded === "1" || !i(X))) {
      X.dataset.tags = me(Ae), X.dataset.loading = "1", X.dataset.loaded = "", L(X, "检查缓存...", "loading");
      try {
        const Re = await e.requestHost("xb-tavern:inline-image-generate", { payload: { tags: Ae } }), Be = Re.result && typeof Re.result == "object" ? Re.result : Re;
        Q(X, String(Be.base64 || ""));
      } catch (Re) {
        de(X, Ae, Re);
      }
    }
  }
  let Ie = null;
  function he() {
    return Ie || typeof IntersectionObserver > "u" || (Ie = new IntersectionObserver((X) => {
      X.forEach((se) => {
        if (!se.isIntersecting) return;
        const Ae = se.target;
        i(Ae) && (Ie?.unobserve(Ae), Me(Ae));
      });
    }, {
      rootMargin: "200px 0px",
      threshold: 0.01
    })), Ie;
  }
  function q(X = "") {
    const se = document.createElement("span");
    return se.className = "xb-tavern-inline-image", se.dataset.tags = me(X), L(se, "滚动加载", "idle"), se;
  }
  function ye(X) {
    l(X) && X.querySelectorAll(".xb-tavern-inline-image").forEach((se) => {
      if (se.dataset.observed === "1" || se.dataset.loaded === "1" || se.dataset.loading === "1" || !i(se)) return;
      se.dataset.observed = "1";
      const Ae = he();
      if (Ae) {
        Ae.observe(se);
        return;
      }
      Me(se);
    });
  }
  function oe(X) {
    if (!l(X) || X.closest(".streaming, .pending-user")) return;
    const se = document.createTreeWalker(X, NodeFilter.SHOW_TEXT, { acceptNode(Re) {
      const Be = Re;
      return !Be.data || !Wi.test(Be.data) ? (Wi.lastIndex = 0, NodeFilter.FILTER_SKIP) : (Wi.lastIndex = 0, Be.parentElement?.closest?.("a, button, code, kbd, pre, script, style, textarea, .xb-tavern-image, .xb-tavern-inline-image") ? NodeFilter.FILTER_REJECT : NodeFilter.FILTER_ACCEPT);
    } }), Ae = [];
    for (; se.nextNode(); ) Ae.push(se.currentNode);
    Ae.forEach((Re) => {
      const Be = Re.data;
      Wi.lastIndex = 0;
      let je, Qe = 0, ut = !1;
      const wt = document.createDocumentFragment();
      for (; (je = Wi.exec(Be)) !== null; ) {
        if (ut = !0, je.index > Qe) {
          const Ht = Be.slice(Qe, je.index);
          wt.append(document.createTextNode(Ht.endsWith(`
`) ? Ht.slice(0, -1) : Ht));
        }
        const rn = ie(je[1] || "");
        wt.append(q(rn)), Qe = Wi.lastIndex, Be[Qe] === `
` && (Qe += 1);
      }
      ut && (Qe < Be.length && wt.append(document.createTextNode(Be.slice(Qe))), Re.replaceWith(wt));
    }), X.querySelectorAll(".xb-tavern-inline-image").forEach((Re) => {
      Se(Re);
    }), ye(X);
  }
  function De(X) {
    pe(X instanceof CustomEvent && X.detail && typeof X.detail == "object" ? X.detail : {});
  }
  typeof window < "u" && (window.addEventListener(ab, De), window.addEventListener("message", H));
  function Je(X) {
    return X.outcome === "criticalSuccess" ? "大成功" : X.outcome === "criticalFailure" ? "大失败" : X.success ? "判定成功" : "判定失败";
  }
  function st(X) {
    return X.outcome === "criticalSuccess" ? "is-success is-critical-success" : X.outcome === "criticalFailure" ? "is-failure is-critical-failure" : X.success ? "is-success" : "is-failure";
  }
  function lt(X) {
    const se = Je(X), Ae = String(X.action || "").trim(), Re = String(X.stakes || "").trim();
    return [
      `行动判定：${X.stat}。`,
      `掷骰 ${X.roll} 对抗难度 ${X.difficulty}。`,
      `${se}。`,
      Ae ? `行动意图：${Ae}。` : "",
      Re ? `风险：${Re}。` : ""
    ].filter(Boolean).join(" ");
  }
  function ot(X) {
    const se = document.createElement("span");
    se.className = `action-check-card ${st(X)}`, se.setAttribute("role", "group"), se.setAttribute("aria-label", lt(X));
    const Ae = document.createElement("span");
    Ae.className = "action-check-card-head";
    const Re = document.createElement("strong");
    Re.textContent = X.stat, Ae.append(Re);
    const Be = document.createElement("span");
    Be.className = "action-check-card-outcome", Be.textContent = Je(X), Ae.append(Be);
    const je = document.createElement("span");
    je.className = "action-check-card-grid", [{
      className: "action-check-card-roll",
      label: "掷骰",
      value: String(X.roll)
    }, {
      className: "action-check-card-dc",
      label: "难度",
      value: String(X.difficulty)
    }].forEach((wt) => {
      const rn = document.createElement("span");
      rn.className = wt.className;
      const Ht = document.createElement("small");
      Ht.textContent = wt.label;
      const Dn = document.createElement("strong");
      Dn.textContent = wt.value, rn.append(Ht, Dn), je.append(rn);
    });
    const Qe = document.createElement("span");
    Qe.className = "action-check-card-copy", Qe.textContent = X.action;
    const ut = [
      Ae,
      je,
      Qe
    ];
    if (X.stakes) {
      const wt = document.createElement("span");
      wt.className = "action-check-card-stakes", wt.textContent = X.stakes, ut.push(wt);
    }
    return se.append(...ut), se;
  }
  function pt(X = []) {
    const se = document.createElement("span");
    return se.className = "assistant-runtime-event-stack", se.setAttribute("role", "group"), se.setAttribute("aria-label", X.length > 1 ? `${X.length} action check results.` : "Action check result."), X.forEach((Ae) => {
      se.append(ot(Ae));
    }), se;
  }
  function ht(X) {
    try {
      return MD(JSON.parse(String(X || "[]")));
    } catch {
      return [];
    }
  }
  function It(X) {
    const se = ht(X.dataset.actionCheckGroups);
    if (!se.length) return;
    const Ae = new Map(se.map((Qe) => [Qe.marker, Qe.events])), Re = new Set(se.map((Qe) => Qe.marker)), Be = document.createTreeWalker(X, NodeFilter.SHOW_TEXT, { acceptNode(Qe) {
      const ut = Qe;
      return ut.data ? ut.parentElement?.closest?.("a, button, code, kbd, pre, script, style, textarea, .assistant-runtime-event-stack") ? NodeFilter.FILTER_REJECT : [...ut.data].some((wt) => Re.has(wt)) ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP : NodeFilter.FILTER_SKIP;
    } }), je = [];
    for (; Be.nextNode(); ) je.push(Be.currentNode);
    je.forEach((Qe) => {
      const ut = document.createDocumentFragment();
      let wt = !1;
      [...Qe.data].forEach((rn) => {
        const Ht = Ae.get(rn);
        if (Ht?.length) {
          ut.append(pt(Ht)), wt = !0;
          return;
        }
        ut.append(document.createTextNode(rn));
      }), wt && Qe.replaceWith(ut);
    });
  }
  function Lt() {
    a(() => {
      const X = e.chatScrollRef.value;
      X?.querySelectorAll && X.querySelectorAll(".xb-tavern-markdown").forEach((se) => {
        if (!l(se)) return;
        const Ae = se.dataset.markdownSignature || "";
        se.dataset.markdownEnhanced !== Ae && (ve(se), q2(se, {
          codeBlockClassName: "xb-tavern-codeblock",
          codeCopyClassName: "xb-tavern-code-copy",
          flattenPreCode: !0,
          htmlBlockMode: e.htmlRenderEnabled.value ? "preview" : "code",
          skipPreSelector: By
        }), u.enhanceTavernImageMarkers(se), oe(se), Z(se), It(se), se.dataset.markdownEnhanced = Ae);
      });
    });
  }
  function en() {
    const X = e.chatScrollRef.value;
    X?.querySelectorAll && X.querySelectorAll(".chat-bubble.streaming .xb-tavern-markdown").forEach((se) => {
      l(se) && (le(se), It(se));
    });
  }
  function at() {
    [e.managerScrollRef.value, e.managerWorkRef?.value].filter(Boolean).forEach((X) => {
      X?.querySelectorAll && X.querySelectorAll(".xb-tavern-markdown").forEach((se) => {
        if (!l(se)) return;
        const Ae = se.dataset.markdownSignature || "";
        se.dataset.markdownEnhanced !== Ae && (ve(se), q2(se, {
          codeBlockClassName: "xb-tavern-codeblock",
          codeCopyClassName: "xb-tavern-code-copy",
          flattenPreCode: !0,
          htmlBlockMode: e.htmlRenderEnabled.value ? void 0 : "code",
          skipPreSelector: By
        }), se.dataset.markdownEnhanced = Ae);
      });
    });
  }
  return {
    clearMarkdownCache: v,
    disposeMarkdownTools() {
      Ie?.disconnect(), Ie = null, p(), u.closeTavernImageGallery(), typeof window < "u" && (window.removeEventListener(ab, De), window.removeEventListener("message", H));
    },
    enhanceChatMarkdown: Lt,
    enhanceLiveChatMarkdown: en,
    enhanceManagerMarkdown: at,
    markdownSignature: d,
    renderChatMarkdown: f,
    stripTavernImageMarkers: m
  };
}
function J2(e) {
  const t = /* @__PURE__ */ ue(null), n = /* @__PURE__ */ ue(!0), r = /* @__PURE__ */ ue(!1), s = /* @__PURE__ */ ue(!1), a = /* @__PURE__ */ ue(!1), o = /* @__PURE__ */ ue(mo(T(e.defaultLimit), 5));
  let l = null, i = !1, u = null, d = 0;
  function f() {
    const z = { uiMessageWindowLimit: o.value };
    LL(z, { defaultLimit: mo(T(e.defaultLimit), 5) }), o.value = Number(z.uiMessageWindowLimit || 5);
  }
  function m(z = !1) {
    const R = t.value;
    if (!z && n.value !== !1 || !R || !z && R.scrollTop > 64) return !1;
    const V = { uiMessageWindowLimit: o.value };
    return $L(V, e.totalItems(), {
      defaultLimit: mo(T(e.defaultLimit), 5),
      chunk: I1(T(e.loadBatchSize), 20)
    }) ? (o.value = Number(V.uiMessageWindowLimit || o.value), n.value = !1, !0) : !1;
  }
  function v() {
    const z = t.value;
    if (!z) {
      r.value = !1, s.value = !1;
      return;
    }
    const R = 80;
    r.value = z.scrollTop > R, s.value = z.scrollHeight - z.scrollTop - z.clientHeight > R;
  }
  function y() {
    a.value = !0, t.value?.classList.add("is-scrolling"), l && window.clearTimeout(l), l = window.setTimeout(() => {
      a.value = !1, t.value?.classList.remove("is-scrolling"), l = null;
    }, 1500);
  }
  function S(z = 56) {
    const R = t.value;
    return R ? R.scrollHeight - R.scrollTop - R.clientHeight <= z : !0;
  }
  function x(z = !1) {
    const R = mo(T(e.defaultLimit), 5);
    return o.value <= R || !z && !S(8) ? !1 : (f(), !0);
  }
  function I(z, R) {
    return e.onReturnToBottom?.({
      collapseWindow: z,
      force: R
    }) === !0;
  }
  vt(() => mo(T(e.defaultLimit), 5), () => {
    n.value !== !1 && f();
  });
  function E(z = !1, R = {}) {
    !z && !n.value || (z && (n.value = !0), Ut(() => {
      const V = t.value;
      if (!V) return;
      const le = () => {
        V.scrollTop = V.scrollHeight;
      };
      le(), requestAnimationFrame(() => {
        le(), requestAnimationFrame(() => {
          le();
          const ve = I(!!R.collapseWindow, z);
          (R.collapseWindow || ve) && (x(!0), Ut(() => {
            le(), requestAnimationFrame(le);
          })), v(), R.revealHelpers && y();
        });
      });
    }));
  }
  function N() {
    const z = t.value;
    z && (n.value = !1, d = 0, z.scrollTo?.({
      top: 0,
      behavior: "smooth"
    }), z.scrollTop = 0, v(), y());
  }
  function $() {
    const z = t.value;
    if (!z || m()) return;
    const R = d, V = Number(z.scrollTop || 0), le = V > R;
    d = V, S() ? (n.value !== !1 || le) && (n.value = !0, I(!1, !1), x()) : V < R && (n.value = !1), !i && (i = !0, requestAnimationFrame(() => {
      v(), y(), i = !1;
    }));
  }
  function p(z) {
    const R = z.target;
    return R instanceof HTMLElement ? R : R instanceof Node && R.parentElement instanceof HTMLElement ? R.parentElement : null;
  }
  function w(z) {
    if (z instanceof HTMLTextAreaElement) return !0;
    const R = (z.ownerDocument?.defaultView || window).getComputedStyle?.(z);
    return /^(auto|scroll|overlay)$/i.test(String(R?.overflowY || ""));
  }
  function _(z, R) {
    const V = Number(z.deltaY || 0);
    return !Number.isFinite(V) || V === 0 ? 0 : z.deltaMode === WheelEvent.DOM_DELTA_LINE ? V * 16 : z.deltaMode === WheelEvent.DOM_DELTA_PAGE ? V * Math.max(1, R.clientHeight || 1) : V;
  }
  function b(z, R) {
    if (!w(z)) return !1;
    const V = Math.max(0, Number(z.scrollHeight || 0) - Number(z.clientHeight || 0));
    if (V <= 1) return !1;
    const le = Number(z.scrollTop || 0);
    return R < 0 ? le > 0 : le < V - 1;
  }
  function C(z, R, V) {
    let le = p(z);
    for (; le && le !== R; ) {
      if (b(le, V)) return le;
      le = le.parentElement;
    }
    return b(R, V) ? R : null;
  }
  function k(z, R) {
    const V = Math.max(0, Number(z.scrollHeight || 0) - Number(z.clientHeight || 0));
    z.scrollTop = Math.min(Math.max(0, Number(z.scrollTop || 0) + R), V);
  }
  function P(z) {
    const R = t.value;
    if (!R) return;
    const V = _(z, R);
    if (V < 0 && (n.value = !1), !V) return;
    const le = C(z, R, V);
    if (!le) return;
    const ve = Number(le.scrollTop || 0);
    requestAnimationFrame(() => {
      le.isConnected && (Math.abs(Number(le.scrollTop || 0) - ve) > 0.5 || (k(le, V), le === R && $()));
    });
  }
  function H(z) {
    u = Number(z.touches?.[0]?.clientY);
  }
  function G(z) {
    const R = Number(z.touches?.[0]?.clientY);
    if (!Number.isFinite(Number(u)) || !Number.isFinite(R)) {
      n.value = !1;
      return;
    }
    u !== null && (R > u + 4 || !S()) && (n.value = !1);
  }
  function ee() {
    d = 0;
  }
  function J() {
    l && (window.clearTimeout(l), l = null), t.value?.classList.remove("is-scrolling");
  }
  return {
    scrollRef: t,
    autoScroll: n,
    showScrollTop: r,
    showScrollBottom: s,
    scrollControlsActive: a,
    messageWindowLimit: o,
    resetWindowState: f,
    revealOlderMessages: m,
    updateScrollButtons: v,
    isNearBottom: S,
    collapseMessageWindowIfBottom: x,
    scrollToBottom: E,
    scrollToTop: N,
    handleScroll: $,
    handleWheel: P,
    handleTouchStart: H,
    handleTouchMove: G,
    resetPositionState: ee,
    cleanup: J
  };
}
function ib(e, t) {
  if (!e) return null;
  const n = e.scrollHeight - e.scrollTop - e.clientHeight, r = typeof e.getBoundingClientRect == "function" ? e.getBoundingClientRect() : null, s = r && t ? Array.from(e.querySelectorAll(t.itemSelector)).map((o) => ({
    key: o?.dataset?.[t.datasetKey] || "",
    rect: typeof o?.getBoundingClientRect == "function" ? o.getBoundingClientRect() : null
  })).filter((o) => o.key && o.rect && o.rect.bottom >= r.top + 1 && o.rect.top <= r.bottom - 1).map((o) => ({
    key: o.key,
    topOffset: o.rect ? o.rect.top - r.top : 0
  })) : [], a = s[0] || null;
  return {
    scrollTop: Number(e.scrollTop || 0),
    nearBottom: n < 80,
    anchorKey: a?.key || "",
    anchorTopOffset: a?.topOffset || 0,
    anchors: s
  };
}
function lb(e, t, n, r = {}) {
  if (e) {
    if (r.forceBottom) {
      e.scrollTop = e.scrollHeight;
      return;
    }
    if (!t) {
      r.defaultToBottom !== !1 && (e.scrollTop = e.scrollHeight);
      return;
    }
    if (r.preserveScrollTop) {
      e.scrollTop = Math.min(Math.max(0, t.scrollTop), e.scrollHeight);
      const s = t.anchors?.length ? t.anchors : t.anchorKey ? [{
        key: t.anchorKey,
        topOffset: t.anchorTopOffset
      }] : [];
      if (s.length && n) {
        const a = typeof e.getBoundingClientRect == "function" ? e.getBoundingClientRect() : null, o = Array.from(e.querySelectorAll(n.itemSelector)), l = s.map((i) => {
          const u = o.find((f) => f?.dataset?.[n.datasetKey] === i.key), d = typeof u?.getBoundingClientRect == "function" ? u.getBoundingClientRect() : null;
          return d ? {
            rect: d,
            topOffset: i.topOffset
          } : null;
        }).find((i) => !!i);
        if (a && l) {
          const i = l.rect.top - a.top;
          e.scrollTop = Math.min(Math.max(0, e.scrollTop + i - Number(l.topOffset || 0)), e.scrollHeight);
        }
      }
      return;
    }
    e.scrollTop = t.nearBottom ? e.scrollHeight : Math.min(t.scrollTop, e.scrollHeight);
  }
}
async function GD(e, t) {
  const n = e.body?.getReader?.();
  if (!n) throw new Error("host_chat_completions_stream_missing_body");
  const r = new TextDecoder();
  let s = "";
  const a = /\r?\n\r?\n/, o = (i) => {
    const u = i.split(/\r?\n/).filter((d) => d.startsWith("data:")).map((d) => d.slice(5).trimStart()).join(`
`).trim();
    !u || u === "[DONE]" || t(JSON.parse(u));
  };
  for (; ; ) {
    const { done: i, value: u } = await n.read();
    if (i) break;
    for (s += r.decode(u, { stream: !0 }); ; ) {
      const d = s.match(a);
      if (!d || typeof d.index != "number") break;
      const f = s.slice(0, d.index);
      s = s.slice(d.index + d[0].length), o(f);
    }
  }
  const l = s.trim();
  l && o(l);
}
var sc = "openai", P1 = "claude", R1 = "makersuite", VD = "/api/backends/chat-completions/status", JD = "/api/backends/chat-completions/generate", Jk = Object.freeze({
  [P1]: "https://api.anthropic.com/v1",
  [R1]: "https://generativelanguage.googleapis.com"
}), Yk = null;
function YD(e) {
  return String(e || "").trim().replace(/\/+$/, "");
}
function XD(e, t) {
  const n = YD(e);
  return t === "claude" ? !n || /\/v\d[\w.-]*$/i.test(n) ? n : `${n}/v1` : t === "makersuite" ? n.replace(/\/v\d[\w.-]*$/i, "") : n;
}
function Y2(e) {
  Yk = typeof e == "function" ? e : null;
}
async function Xk() {
  return {
    "Content-Type": "application/json",
    ...await Promise.resolve(Yk?.() || {}),
    Accept: "application/json"
  };
}
function jD(e = {}) {
  const t = {};
  return Object.entries(e || {}).forEach(([n, r]) => {
    t[n] = /authorization|csrf|token|api[-_]?key/i.test(n) ? "[redacted]" : r;
  }), t;
}
async function Lf(e = {}, t = !1) {
  const n = await Xk(), r = {
    url: JD,
    method: "POST",
    headers: jD(n),
    body: {
      ...e,
      stream: !!t
    }
  };
  return Object.defineProperty(r, "rawHeaders", {
    value: n,
    enumerable: !1
  }), r;
}
function QD(e = "") {
  return /^\s*<!DOCTYPE\s+html/i.test(String(e || ""));
}
function ZD(e = "") {
  return /invalid csrf token/i.test(String(e || ""));
}
function eO() {
  return "酒馆当前页面的 CSRF token 已失效，请按 F5 刷新并重新进入酒馆后再试。";
}
function zu(e = "", t = "") {
  return ZD(e) || QD(e) ? eO() : String(e || t || "").trim();
}
function jk(e = {}, t = sc) {
  const n = XD(e.baseUrl, t), r = String(e.apiKey || "").trim(), s = Jk[t] || "", a = n || (r ? s : ""), o = { chat_completion_source: t || "openai" };
  return a && (o.reverse_proxy = a), r && (o.proxy_password = r), o;
}
function tO(e = {}) {
  return Object.keys(e).forEach((t) => {
    (e[t] === void 0 || e[t] === "") && delete e[t];
  }), e;
}
function nO(e = {}, t = sc) {
  return jk(e, t);
}
function N1(e = {}, t = {}, n = [], r = !1, s = sc) {
  return tO({
    ...jk(e, s),
    stream: !!r,
    messages: n,
    model: e.model,
    max_tokens: t.maxTokens,
    temperature: t.reasoning?.enabled ? void 0 : t.temperature,
    tools: Array.isArray(t.tools) && t.tools.length ? t.tools : void 0,
    tool_choice: Array.isArray(t.tools) && t.tools.length ? t.toolChoice || "auto" : void 0,
    use_sysprompt: s === "openai" ? void 0 : !0,
    reasoning_effort: t.reasoning?.enabled ? t.reasoning.effort : void 0,
    include_reasoning: s === "openai" ? void 0 : t.reasoning?.enabled ? !0 : void 0
  });
}
function rO(e = {}, t = {}, n = [], r = !1) {
  return N1(e, t, n, r, sc);
}
function sO(e = {}, t = {}, n = [], r = !1) {
  return N1(e, t, n, r, P1);
}
function aO(e = {}, t = {}, n = [], r = !1) {
  return N1(e, t, n, r, R1);
}
async function oO(e = {}, t = sc, n = {}) {
  const r = await fetch(VD, {
    method: "POST",
    headers: await Xk(),
    body: JSON.stringify(nO(e, t)),
    signal: n.signal
  }), s = await r.text();
  let a = null;
  try {
    a = s ? JSON.parse(s) : {};
  } catch (l) {
    throw new Error(`酒馆后端模型列表拉取失败：${zu(s, String(l?.message || l))}`);
  }
  if (!r.ok || a?.error) {
    const l = zu(a?.message || a?.error?.message || s, `HTTP ${r.status}`);
    throw new Error(`酒馆后端模型列表拉取失败：${l}`);
  }
  const o = Array.isArray(a?.data) ? a.data.map((l) => String(l?.id || l?.name || "").trim()).filter(Boolean) : [];
  return [...new Set(o)];
}
async function $1(e = {}, t = {}) {
  const n = await Lf(e, !1);
  typeof t.onRequest == "function" && t.onRequest(n);
  const r = await fetch(n.url, {
    method: n.method,
    headers: n.rawHeaders || n.headers,
    body: JSON.stringify(n.body),
    signal: t.signal
  }), s = await r.text();
  let a = null;
  try {
    a = s ? JSON.parse(s) : {};
  } catch (o) {
    throw new Error(`酒馆后端生成失败：${zu(s, String(o?.message || o))}`);
  }
  if (!r.ok || a?.error) {
    const o = zu(a?.error?.message || a?.message || s, `HTTP ${r.status}`);
    throw new Error(`酒馆后端生成失败：${o}`);
  }
  return a;
}
async function L1(e = {}, t, n = {}) {
  const r = await Lf(e, !0);
  typeof n.onRequest == "function" && n.onRequest(r);
  const s = await fetch(r.url, {
    method: r.method,
    headers: r.rawHeaders || r.headers,
    body: JSON.stringify(r.body),
    signal: n.signal
  });
  if (!s.ok) {
    const a = await s.text().catch(() => "");
    throw new Error(zu(a, `酒馆后端流式生成失败：HTTP ${s.status}`));
  }
  await GD(s, (a) => {
    if (a?.error) {
      const o = zu(a.error?.message || a.message || JSON.stringify(a.error), "酒馆后端流式生成失败");
      throw new Error(o);
    }
    t(a);
  });
}
var Hg = /* @__PURE__ */ ((e) => (e[e.before = 0] = "before", e[e.after = 1] = "after", e[e.ANTop = 2] = "ANTop", e[e.ANBottom = 3] = "ANBottom", e[e.atDepth = 4] = "atDepth", e[e.EMTop = 5] = "EMTop", e[e.EMBottom = 6] = "EMBottom", e[e.outlet = 7] = "outlet", e))(Hg || {}), Lp = /* @__PURE__ */ ((e) => (e[e.SYSTEM = 0] = "SYSTEM", e[e.USER = 1] = "USER", e[e.ASSISTANT = 2] = "ASSISTANT", e))(Lp || {}), Dp = /* @__PURE__ */ ((e) => (e[e.AFTER_MAIN = 0] = "AFTER_MAIN", e[e.IN_CHAT = 1] = "IN_CHAT", e[e.BEFORE_MAIN = 2] = "BEFORE_MAIN", e))(Dp || {}), ub = {
  prompt: "",
  interval: 1,
  position: 1,
  depth: 4,
  role: 0,
  scan: !1
};
function iO(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function Uy(e, t = 0) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.max(0, Math.floor(n)) : t;
}
function lO(e) {
  const t = Number(e);
  return t === 0 ? 0 : t === 2 ? 2 : 1;
}
function uO(e) {
  const t = Number(e);
  return t === 1 ? 1 : t === 2 ? 2 : 0;
}
function ti(e = {}) {
  const t = iO(e);
  return {
    prompt: ze(t.prompt),
    interval: Uy(t.interval, ub.interval),
    position: lO(t.position),
    depth: Uy(t.depth, ub.depth),
    role: uO(t.role),
    scan: t.scan === !0,
    characterName: ze(t.characterName),
    characterPrompt: ze(t.characterPrompt),
    characterUse: t.characterUse === !0,
    characterPosition: Uy(t.characterPosition, 0)
  };
}
function cO(e = {}, t = "") {
  return (Array.isArray(e.history) ? e.history : []).filter((n) => Ha(n.role ?? n.is_user) === "user").length + (ze(t) ? 1 : 0);
}
function dO(e = {}, t = "") {
  const n = ti(e.authorNote);
  let r = cO(e, t);
  n.interval === 1 && (r = 1);
  const s = r > 0 && n.interval > 0 && (r >= n.interval ? r % n.interval === 0 : !1);
  let a = s ? ze(n.prompt) : "";
  if (s && n.characterUse === !0) {
    const o = ze(n.characterPrompt);
    switch (n.characterPosition) {
      case 1:
        a = [o, a].filter(Boolean).join(`
`);
        break;
      case 2:
        a = [a, o].filter(Boolean).join(`
`);
        break;
      default:
        a = o;
        break;
    }
  }
  return {
    shouldAddPrompt: s,
    prompt: a,
    position: n.position,
    depth: n.depth,
    role: n.role,
    scan: n.scan === !0
  };
}
function fO(e = {}, t = "") {
  const n = dO(e, t);
  return n.scan && n.shouldAddPrompt ? ze(n.prompt) : "";
}
var cb = {
  0: "system",
  1: "user",
  2: "assistant"
}, X2 = {
  before: 0,
  before_char: 0,
  beforeCharacter: 0,
  after: 1,
  after_char: 1,
  afterCharacter: 1,
  atDepth: 4,
  depth: 4,
  outlet: 7,
  ANTop: 2,
  ANBottom: 3,
  EMTop: 5,
  EMBottom: 6
}, hO = {
  0: "before character",
  1: "after character",
  2: "author note top",
  3: "author note bottom",
  4: "depth",
  5: "example top",
  6: "example bottom",
  7: "outlet"
}, pO = [
  "top",
  "beforeCharacter",
  "afterCharacter",
  "beforeHistory",
  "afterHistory",
  "assistantPrefill"
];
function ze(e = "") {
  return String(e || "").trim();
}
function zn(e, t) {
  let n = e;
  for (const r of t) {
    if (!n || typeof n != "object") return "";
    n = n[r];
  }
  return ze(n);
}
function Ha(e, t = "system") {
  if (typeof e == "number" && cb[e]) return cb[e];
  const n = String(e || "").trim().toLowerCase();
  return n === "model" ? "assistant" : n === "sys" ? "system" : [
    "system",
    "user",
    "assistant",
    "tool"
  ].includes(n) ? n : t;
}
function Ol(e, t, n = {}) {
  const r = ze(t);
  return r ? {
    role: Ha(e),
    content: r,
    ...n
  } : null;
}
function db(e) {
  return Array.isArray(e) ? e.map((t, n) => {
    const r = t && typeof t == "object" ? t : {}, s = ze(r.text);
    return s ? {
      label: ze(r.label) || `reasoning ${n + 1}`,
      text: s
    } : null;
  }).filter((t) => !!t) : [];
}
function mO(e = []) {
  const t = db(e);
  return t.length ? [
    "<reasoning>",
    ...t.map((n) => [
      `<thought label="${ze(n.label).replace(/"/g, "&quot;")}">`,
      n.text,
      "</thought>"
    ].join(`
`)),
    "</reasoning>"
  ].join(`
`) : "";
}
function gO(e) {
  const { thoughts: t, ...n } = e;
  if (e.role !== "assistant") return n;
  const r = mO(e.thoughts || []);
  return r ? {
    ...n,
    content: [e.content, r].filter(Boolean).join(`

`)
  } : n;
}
function D1(e) {
  return e.filter((t) => !!t && !!ze(t.content));
}
function Cn(e, t, n = "unknown", r = "", s = {}, a = "") {
  return {
    message: Ol(e, t, s),
    layer: n,
    label: r || n,
    sourceId: ze(a)
  };
}
function vO(e = []) {
  const t = [], n = [];
  return e.forEach((r) => {
    if (!r.message || !ze(r.message.content)) return;
    const s = t.length;
    t.push(r.message);
    const a = r.message.content.length;
    n.push({
      index: s,
      role: r.message.role,
      layer: r.layer,
      label: r.label,
      sourceId: r.sourceId || void 0,
      chars: a,
      tokenEstimate: Math.max(1, Math.ceil(a / 4))
    });
  }), {
    messages: t,
    messageLayers: n
  };
}
function ni(e) {
  if (Array.isArray(e)) return e.map((n) => ze(n)).filter(Boolean);
  const t = ze(e);
  return t ? [t] : [];
}
function yO(e = "") {
  const t = [], n = String(e || "").split(`
`);
  let r = 0;
  for (; r < n.length && n[r].startsWith("@@"); ) {
    const s = n[r].trim();
    s && t.push(s.startsWith("@@@") ? s.slice(1) : s), r += 1;
  }
  return {
    decorators: t,
    content: n.slice(r).join(`
`).trim()
  };
}
function _O(e) {
  if (typeof e == "number" && Object.values(Hg).includes(e)) return e;
  const t = String(e || "").trim();
  return Object.prototype.hasOwnProperty.call(X2, t) ? X2[t] : 1;
}
function O1(e = {}, t = 0) {
  const n = e, r = e.extensions || {}, s = (d, f) => Object.prototype.hasOwnProperty.call(r, d) ? r[d] : n[f], a = yO(e.content || ""), o = e.uid ?? e.id ?? e.comment ?? e.name ?? t + 1, l = ze(e.sourceWorldBook || e.worldName || e.world), i = a.content || ze(e.content), u = n.order ?? n.insertion_order;
  return {
    ...e,
    uid: o,
    activationKey: bO(l, o, t),
    content: i,
    decorators: [...ni(e.decorators), ...a.decorators],
    key: ni(e.key),
    keysecondary: [...ni(e.keysecondary), ...ni(e.secondary_keys)],
    order: Number(u) || 0,
    disable: n.disable === !0 || n.disabled === !0 || n.enabled === !1,
    enabled: n.enabled === !1 ? !1 : n.disable !== !0 && n.disabled !== !0,
    vectorized: s("vectorized", "vectorized") === !0,
    depth: Number.isFinite(Number(s("depth", "depth"))) ? Number(s("depth", "depth")) : 4,
    role: Ha(s("role", "role"), "system"),
    position: _O(s("position", "position")),
    selectiveLogic: s("selectiveLogic", "selectiveLogic"),
    scanDepth: Number.isFinite(Number(s("scan_depth", "scanDepth"))) ? Number(s("scan_depth", "scanDepth")) : null,
    caseSensitive: s("case_sensitive", "caseSensitive"),
    matchWholeWords: s("match_whole_words", "matchWholeWords"),
    characterFilter: s("character_filter", "characterFilter") || n.character_filter,
    ignoreBudget: s("ignore_budget", "ignoreBudget") === !0,
    excludeRecursion: s("exclude_recursion", "excludeRecursion") === !0,
    preventRecursion: s("prevent_recursion", "preventRecursion") === !0,
    delayUntilRecursion: s("delay_until_recursion", "delayUntilRecursion"),
    group: ze(s("group", "group")),
    groupOverride: s("group_override", "groupOverride") === !0,
    groupWeight: Number.isFinite(Number(s("group_weight", "groupWeight"))) ? Number(s("group_weight", "groupWeight")) : void 0,
    useGroupScoring: s("use_group_scoring", "useGroupScoring"),
    probability: s("probability", "probability"),
    useProbability: s("useProbability", "useProbability"),
    sticky: s("sticky", "sticky"),
    cooldown: s("cooldown", "cooldown"),
    delay: s("delay", "delay"),
    triggers: ni(s("triggers", "triggers")),
    outletName: ze(s("outlet_name", "outletName")),
    matchPersonaDescription: s("match_persona_description", "matchPersonaDescription"),
    matchCharacterDescription: s("match_character_description", "matchCharacterDescription"),
    matchCharacterPersonality: s("match_character_personality", "matchCharacterPersonality"),
    matchCharacterDepthPrompt: s("match_character_depth_prompt", "matchCharacterDepthPrompt"),
    matchScenario: s("match_scenario", "matchScenario"),
    matchCreatorNotes: s("match_creator_notes", "matchCreatorNotes"),
    activationReason: "",
    sourceWorldBook: l,
    worldSourceType: ze(e.worldSourceType),
    worldSourceIndex: Number.isFinite(Number(e.worldSourceIndex)) ? Number(e.worldSourceIndex) : t,
    contentChars: i.length
  };
}
function bO(e, t, n = 0) {
  return `${ze(e) || "direct"}\0${ze(t) || `index:${n}`}`;
}
function F1(e) {
  return hO[e] || "after character";
}
function cf(e, t = 2) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.max(0, Math.floor(n)) : t;
}
function Qk(e = [], t = 2) {
  const n = cf(t, 2);
  return n <= 0 ? "" : e.slice(-n).map((r) => String(r || "")).filter(Boolean).join(`
`);
}
function wO(e = {}, t) {
  const n = t?.scanDepth === null || t?.scanDepth === void 0 ? cf(e.scanDepth ?? 2, 2) : cf(t.scanDepth, 2), r = [
    Array.isArray(e.scanMessages) ? Qk(e.scanMessages, n) : String(e.scanText || ""),
    ze(e.injectScanText),
    String(e.recursionText || "")
  ], s = e.globalScanData || {};
  return t?.matchPersonaDescription && s.personaDescription && r.push(s.personaDescription), t?.matchCharacterDescription && s.characterDescription && r.push(s.characterDescription), t?.matchCharacterPersonality && s.characterPersonality && r.push(s.characterPersonality), t?.matchCharacterDepthPrompt && s.characterDepthPrompt && r.push(s.characterDepthPrompt), t?.matchScenario && s.scenario && r.push(s.scenario), t?.matchCreatorNotes && s.creatorNotes && r.push(s.creatorNotes), r.filter(Boolean).join(`
`);
}
function zg(e = {}, t) {
  const n = !!(t?.caseSensitive ?? t?.case_sensitive ?? e.caseSensitive), r = !!(t?.matchWholeWords ?? t?.match_whole_words ?? e.matchWholeWords), s = wO(e, t), a = n ? s : s.toLowerCase();
  return (o = "") => {
    const l = String(o || "").trim();
    if (!l) return !1;
    const i = SO(l);
    if (i) return i.test(s);
    const u = n ? l : l.toLowerCase();
    if (!r) return a.includes(u);
    const d = u.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    return new RegExp(`(^|[^\\p{L}\\p{N}_])${d}($|[^\\p{L}\\p{N}_])`, n ? "u" : "iu").test(a);
  };
}
function SO(e = "") {
  const t = String(e || "").match(/^\/([\w\W]+?)\/([gimsuy]*)$/);
  if (!t) return null;
  let n = t[1] || "";
  const r = t[2] || "";
  if (/(^|[^\\])\//.test(n)) return null;
  n = n.replace("\\/", "/");
  try {
    return new RegExp(n, r);
  } catch {
    return null;
  }
}
function Zk(e, t) {
  if (e.selective === !1 || !e.keysecondary.length) return !0;
  const n = e.keysecondary.map((a) => t(a)), r = n.some(Boolean), s = n.every(Boolean);
  switch (Number(e.selectiveLogic ?? e.selective_logic ?? 0)) {
    case 1:
      return !s;
    case 2:
      return !r;
    case 3:
      return s;
    default:
      return r;
  }
}
function B1(e, t) {
  return e.entryStates?.[t.activationKey] || e.entryStates?.[String(t.uid)] || {};
}
function U1(e, t) {
  const n = Number(t.turn) || 0, r = B1(t, e);
  return Number(r.stickyUntilTurn) >= n;
}
function AO(e, t) {
  if (e.useProbability === !1 || e.useProbabilityGlobal === !1 || U1(e, t)) return !0;
  const n = Number(e.probability);
  if (!Number.isFinite(n) || n <= 0) return n !== 0;
  const r = n > 1 ? n / 100 : n;
  return r >= 1 ? !0 : (t.random || Math.random)() <= r;
}
function eM(e) {
  return Math.max(0, Number(e.recursionPass) || 0);
}
function tM(e, t) {
  const n = e.delayUntilRecursion ?? e.delay_until_recursion;
  return n ? t <= 0 ? !1 : n === !0 ? !0 : t >= Math.max(1, Number(n) || 1) : !0;
}
function nM(e, t) {
  const n = e.characterFilter;
  if (!n || typeof n != "object") return !0;
  const r = Array.isArray(n.names) ? n.names.map((d) => ze(d)).filter(Boolean) : [], s = Array.isArray(n.tags) ? n.tags.map((d) => ze(d)).filter(Boolean) : [];
  if (!r.length && !s.length) return !0;
  const a = new Set((t.characterFilterData?.names || []).map((d) => ze(d)).filter(Boolean)), o = new Set((t.characterFilterData?.tags || []).map((d) => ze(d)).filter(Boolean)), l = r.some((d) => a.has(d)), i = s.some((d) => o.has(d)), u = l || i;
  return n.isExclude ? !u : u;
}
function TO(e, t) {
  if (e.disable === !0 || e.disabled === !0 || e.triggers?.length && !e.triggers.includes(ze(t.trigger) || "normal") || !nM(e, t)) return "";
  const n = Number(t.turn) || 0, r = B1(t, e), s = U1(e, t), a = eM(t);
  if (Number(r.delayUntilTurn) > n || Number(e.delay) > 0 && n < Number(e.delay) || Number(r.cooldownUntilTurn) > n && !s || a > 0 && e.excludeRecursion && !s || !tM(e, a) && !s) return "";
  if (e.decorators.includes("@@activate")) return "decorator";
  if (e.decorators.includes("@@dont_activate")) return "";
  if (e.constant === !0) return "constant";
  if (s) return "sticky";
  const o = zg(t, e);
  return !e.key.some((l) => o(l)) || !Zk(e, o) ? "" : "keyword";
}
function EO(e, t) {
  if (e.disable === !0 || e.disabled === !0) return {
    status: "disabled",
    activationReason: ""
  };
  if (e.triggers?.length && !e.triggers.includes(ze(t.trigger) || "normal")) return {
    status: "trigger_filtered",
    activationReason: ""
  };
  if (!nM(e, t)) return {
    status: "character_filtered",
    activationReason: ""
  };
  const n = Number(t.turn) || 0, r = B1(t, e), s = U1(e, t), a = eM(t);
  if (Number(r.delayUntilTurn) > n || Number(e.delay) > 0 && n < Number(e.delay)) return {
    status: "delay",
    activationReason: ""
  };
  if (Number(r.cooldownUntilTurn) > n && !s) return {
    status: "cooldown",
    activationReason: ""
  };
  if (a > 0 && e.excludeRecursion && !s) return {
    status: "excluded_from_recursion",
    activationReason: ""
  };
  if (!tM(e, a) && !s) return {
    status: "delay_until_recursion",
    activationReason: ""
  };
  if (e.decorators.includes("@@activate")) return {
    status: "activated",
    activationReason: "decorator"
  };
  if (e.decorators.includes("@@dont_activate")) return {
    status: "suppressed_by_decorator",
    activationReason: ""
  };
  if (e.constant === !0) return {
    status: "activated",
    activationReason: "constant"
  };
  if (s) return {
    status: "activated",
    activationReason: "sticky"
  };
  const o = zg(t, e);
  return e.key.some((l) => o(l)) ? Zk(e, o) ? {
    status: "activated",
    activationReason: "keyword"
  } : {
    status: "secondary_not_matched",
    activationReason: ""
  } : {
    status: "not_matched",
    activationReason: ""
  };
}
function j2(e, t = {}) {
  const n = ze(e.worldSourceType);
  if (n === "chat") return 0;
  if (n === "persona") return 1;
  const r = Number(t.insertionStrategy ?? 1);
  return n === "character" ? r === 2 ? 3 : 2 : n === "global" ? r === 1 ? 3 : 2 : 4;
}
function Q2(e, t = {}) {
  const n = ze(e.worldSourceType);
  if (Number(t.insertionStrategy ?? 1) === 0) {
    if (n === "global") return 0;
    if (n === "character") return 1;
  }
  return 0;
}
function uu(e, t, n = {}) {
  return j2(e, n) - j2(t, n) || t.order - e.order || Q2(e, n) - Q2(t, n) || Number(e.worldSourceIndex ?? 0) - Number(t.worldSourceIndex ?? 0) || e.activationKey.localeCompare(t.activationKey, "en");
}
function CO(e) {
  const t = Number(e.budgetChars);
  return Number.isFinite(t) && t > 0 ? t : 0;
}
function Op(e = [], t = {}) {
  const n = CO(t), r = n > 0, s = /* @__PURE__ */ new Set(), a = /* @__PURE__ */ new Map(), o = [];
  let l = 0, i = 0, u = !1;
  return e.forEach((d) => {
    const f = d.content.length;
    if (!f) return;
    const m = r ? Math.max(0, n - l) : Number.POSITIVE_INFINITY;
    if (a.set(d.activationKey, {
      budgetUsedBefore: l,
      budgetRemainingBefore: r ? m : void 0,
      budgetShortfall: r && !d.ignoreBudget && (u || l + f >= n) ? Math.max(1, l + f - n) : void 0
    }), d.ignoreBudget) {
      o.push(d), s.add(d.activationKey);
      return;
    }
    if (u || r && l + f >= n) {
      u = r, i += f;
      return;
    }
    o.push(d), s.add(d.activationKey), l += f;
  }), {
    includedKeys: s,
    byKey: a,
    enabled: r,
    limit: n,
    used: l,
    remaining: r ? Math.max(0, n - l) : 0,
    activatedChars: l,
    skippedChars: i
  };
}
function Z2(e) {
  return ze(e.group).split(/,\s*/).map((t) => t.trim()).filter(Boolean);
}
function IO(e, t = {}) {
  const n = zg(t, e), r = e.key.filter((a) => n(a)).length;
  if (!e.key.length || !r) return 0;
  const s = e.keysecondary.filter((a) => n(a)).length;
  if (e.keysecondary.length > 0) {
    const a = Number(e.selectiveLogic ?? e.selective_logic ?? 0);
    if (a === 0 || a === 3 && s === e.keysecondary.length) return r + s;
  }
  return r;
}
function xO(e = [], t, n = {}) {
  const r = new Set(e), s = /* @__PURE__ */ new Set();
  t.forEach((o) => {
    Z2(o).forEach((l) => s.add(l));
  });
  const a = /* @__PURE__ */ new Map();
  return e.forEach((o) => {
    Z2(o).forEach((l) => {
      const i = a.get(l) || [];
      i.push(o), a.set(l, i);
    });
  }), a.forEach((o, l) => {
    const i = o.filter((f) => r.has(f));
    if (!i.length) return;
    if (s.has(l)) {
      i.forEach((f) => r.delete(f));
      return;
    }
    if (n.useGroupScoring === !0 || i.some((f) => f.useGroupScoring === !0)) {
      const f = i.map((v) => IO(v, n)), m = Math.max(...f);
      i.forEach((v, y) => {
        (v.useGroupScoring ?? n.useGroupScoring) && f[y] < m && r.delete(v);
      });
    }
    const u = i.filter((f) => r.has(f));
    if (u.length <= 1) return;
    const d = u.filter((f) => f.groupOverride).sort((f, m) => uu(f, m, n))[0] || kO(u, n);
    u.forEach((f) => {
      f !== d && r.delete(f);
    });
  }), e.filter((o) => r.has(o));
}
function kO(e = [], t = {}) {
  if (!e.length) return null;
  const n = e.map((a) => ({
    entry: a,
    weight: Math.max(1, Number(a.groupWeight) || 100)
  })), r = n.reduce((a, o) => a + o.weight, 0);
  let s = (t.random || Math.random)() * r;
  for (const a of n)
    if (s -= a.weight, s <= 0) return a.entry;
  return n[n.length - 1]?.entry || null;
}
function MO(e = [], t = {}, n = {}) {
  const r = {
    ...t,
    ...n,
    scanText: n.scanText ?? t.scanText ?? ""
  }, s = (Array.isArray(e) ? e : []).map((C, k) => O1(C, k)).sort((C, k) => uu(C, k, r)), a = Math.max(0, Number(r.recursionLimit) || 0), o = !!r.recursion, l = o ? a > 0 ? Math.max(0, a - 1) : Math.max(1, s.length) : 0, i = /* @__PURE__ */ new Map(), u = /* @__PURE__ */ new Map(), d = /* @__PURE__ */ new Set(), f = String(r.scanText || ""), m = cf(r.scanDepth ?? 2, 2), v = Math.max(0, Number(r.minActivations) || 0), y = Math.max(0, Number(r.minActivationsDepthMax) || 0), S = Array.isArray(r.scanMessages) ? r.scanMessages.length : 0, x = Math.max(m, y > 0 ? y : S);
  let I = "", E = 0, N = 0, $ = "initial", p = 0;
  const w = Math.max(1, s.length + l + x + 4);
  for (; p < w; ) {
    p += 1;
    const C = m + N, k = $ === "minActivations" ? "" : I, P = [f, k].filter(Boolean).join(`
`), H = {
      ...r,
      scanDepth: C,
      scanText: P,
      recursionText: k,
      recursionPass: $ === "recursion" ? E : 0
    };
    let G = !1;
    const ee = [];
    s.forEach((V) => {
      const le = V.activationKey;
      if (u.has(le) || d.has(le)) return;
      const ve = TO(V, H);
      ve && ee.push({
        ...V,
        activationReason: ve
      });
    }), xO(ee, u, r).forEach((V) => {
      if (!AO(V, H)) {
        d.add(V.activationKey);
        return;
      }
      u.set(V.activationKey, V);
      const le = Op([...u.values()].sort((ve, ke) => uu(ve, ke, r)), r);
      (!le.enabled || le.includedKeys.has(V.activationKey)) && (i.set(V.activationKey, V), V.preventRecursion || (I = [I, V.content].filter(Boolean).join(`
`)), G = !0);
    });
    const J = Op([...u.values()].sort((V, le) => uu(V, le, r)), r), z = J.enabled && J.skippedChars > 0;
    if (o && G && !z && E < l) {
      E += 1, $ = "recursion";
      continue;
    }
    const R = v > 0 && i.size < v;
    if (!z && R && S > 0 && C < x && C < S) {
      N += 1, $ = "minActivations";
      continue;
    }
    break;
  }
  const _ = [...u.values()].sort((C, k) => uu(C, k, r)), b = Op(_, r);
  return {
    activatedBeforeBudget: _,
    activatedEntries: [...i.values()].sort((C, k) => uu(C, k, r)).filter((C) => !b.enabled || b.includedKeys.has(C.activationKey)),
    budgetDebug: b
  };
}
function PO(e = [], t = [], n = {}, r = Op(t, n)) {
  const s = new Map(t.map((o) => [o.activationKey, o])), a = r.includedKeys;
  return (Array.isArray(e) ? e : []).map((o, l) => {
    const i = O1(o, l), u = s.get(i.activationKey), d = r.byKey.get(i.activationKey) || {}, f = u ? {
      status: "activated",
      activationReason: u.activationReason
    } : EO(i, n), m = zg(n, i), v = u ? r.enabled && !a.has(i.activationKey) ? "budget_skipped" : "activated" : f.status === "activated" ? "probability_failed" : f.status;
    return {
      uid: i.uid,
      activationKey: i.activationKey,
      title: ze(i.comment || i.title || i.name || i.uid),
      sourceWorldBook: i.sourceWorldBook,
      worldSourceType: i.worldSourceType,
      worldSourceIndex: i.worldSourceIndex,
      content: i.content,
      contentChars: i.contentChars,
      key: i.key,
      keysecondary: i.keysecondary,
      matchedKeys: i.key.filter((y) => m(y)),
      matchedSecondaryKeys: i.keysecondary.filter((y) => m(y)),
      decorators: i.decorators,
      position: i.position,
      positionLabel: F1(i.position),
      role: i.role,
      order: i.order,
      depth: i.depth,
      status: v,
      activationReason: u?.activationReason || f.activationReason,
      insertionTarget: Kg(i),
      ...d
    };
  });
}
function RO(e = []) {
  return (Array.isArray(e) ? e : []).map((t, n) => ({
    ...O1(t, n),
    activationReason: ze(t.activationReason) || "native"
  })).filter((t) => !!t.content);
}
function NO(e = []) {
  return e.map((t) => ({
    uid: t.uid,
    activationKey: t.activationKey,
    title: ze(t.comment || t.title || t.name || t.uid),
    sourceWorldBook: t.sourceWorldBook,
    worldSourceType: t.worldSourceType,
    worldSourceIndex: t.worldSourceIndex,
    content: t.content,
    contentChars: t.contentChars,
    key: t.key,
    keysecondary: t.keysecondary,
    matchedKeys: [],
    matchedSecondaryKeys: [],
    decorators: t.decorators,
    position: t.position,
    positionLabel: F1(t.position),
    role: t.role,
    order: t.order,
    depth: t.depth,
    status: "activated",
    activationReason: t.activationReason || "native",
    insertionTarget: Kg(t)
  }));
}
function $O(e = {}) {
  const t = [];
  let n = 0;
  const r = (a, o, l = {}) => {
    const i = ze(a);
    if (!i) return;
    const u = `native:${o}:${n}`;
    t.push({
      uid: u,
      activationKey: u,
      content: i,
      contentChars: i.length,
      key: [],
      keysecondary: [],
      decorators: [],
      position: o,
      role: l.role || "system",
      order: n,
      depth: Number.isFinite(Number(l.depth)) ? Number(l.depth) : 4,
      activationReason: "native",
      sourceWorldBook: "",
      title: "",
      comment: "",
      ...l
    }), n += 1;
  };
  r(e.worldInfoBefore, 0), r(e.worldInfoAfter, 1), (Array.isArray(e.worldInfoExamples) ? e.worldInfoExamples : []).forEach((a) => {
    const o = ze(a?.position) === "after" ? 6 : 5;
    r(a?.content, o);
  }), (Array.isArray(e.anBefore) ? e.anBefore : []).forEach((a) => {
    r(a, 2);
  }), (Array.isArray(e.anAfter) ? e.anAfter : []).forEach((a) => {
    r(a, 3);
  }), (Array.isArray(e.worldInfoDepth) ? e.worldInfoDepth : []).forEach((a) => {
    const o = Number.isFinite(Number(a?.depth)) ? Number(a.depth) : 4, l = Ha(cb[Number(a?.role)] || "system", "system");
    (Array.isArray(a?.entries) ? a.entries : []).forEach((i) => {
      r(i, 4, {
        depth: o,
        role: l
      });
    });
  });
  const s = e.outlets && typeof e.outlets == "object" ? e.outlets : {};
  return Object.entries(s).forEach(([a, o]) => {
    (Array.isArray(o) ? o : []).forEach((l) => {
      r(l, 7, { outletName: a });
    });
  }), t;
}
function Kg(e) {
  switch (e.position) {
    case 0:
      return "before character card";
    case 1:
      return "after character card";
    case 4:
      return `history depth ${Math.max(0, Number(e.depth) || 0)}`;
    case 2:
      return "author note top";
    case 3:
      return "author note bottom";
    case 5:
      return "example messages top";
    case 6:
      return "example messages bottom";
    case 7:
      return `outlet:${ze(e.outletName || e.outlet || "default")}`;
    default:
      return F1(e.position);
  }
}
function LO(e = []) {
  const t = {
    before: [],
    after: [],
    atDepth: [],
    outlet: {},
    examplesTop: [],
    examplesBottom: [],
    authorNoteTop: [],
    authorNoteBottom: []
  };
  return e.forEach((n) => {
    if (n.content)
      switch (n.position) {
        case 0:
          t.before.push(n);
          break;
        case 4:
          t.atDepth.push(n);
          break;
        case 7: {
          const r = ze(n.outletName || n.outlet || "default");
          t.outlet[r] = t.outlet[r] || [], t.outlet[r].push(n);
          break;
        }
        case 5:
          t.examplesTop.push(n);
          break;
        case 6:
          t.examplesBottom.push(n);
          break;
        case 2:
          t.authorNoteTop.push(n);
          break;
        case 3:
          t.authorNoteBottom.push(n);
          break;
        default:
          t.after.push(n);
          break;
      }
  }), t;
}
function DO(e = []) {
  const t = {};
  return e.forEach((n) => {
    const r = Kg(n);
    t[r] = (t[r] || 0) + 1;
  }), t;
}
function OO(e = [], t = {}) {
  const n = Number(t.turn) || 0, r = {};
  return e.forEach((s) => {
    const a = s.activationKey, o = {}, l = s.sticky === !0 ? 1 : Number(s.sticky), i = Number(s.cooldown), u = Number(s.delay);
    Number.isFinite(l) && l > 0 && (o.stickyUntilTurn = n + l), Number.isFinite(i) && i > 0 && (o.cooldownUntilTurn = n + i), Number.isFinite(u) && u > 0 && (o.delayUntilTurn = n + u), Object.keys(o).length && (r[a] = o);
  }), r;
}
function rM(e = []) {
  return [...e].sort((t, n) => t.order - n.order || Number(n.worldSourceIndex ?? 0) - Number(t.worldSourceIndex ?? 0) || n.activationKey.localeCompare(t.activationKey, "en"));
}
function Tr(e, t = []) {
  const n = rM(t).map((r) => r.content).filter(Boolean).join(`

`);
  return n ? `<${e}>
${n}
</${e}>` : "";
}
function sM(e = {}, t = {}) {
  const n = e.data || {}, r = [
    ["Character", e.name || zn(n, ["name"])],
    ["User", t.name],
    ["Description", e.description || zn(n, ["description"])],
    ["Personality", e.personality || zn(n, ["personality"])],
    ["Scenario", e.scenario || zn(n, ["scenario"])],
    ["Creator Notes", e.creatorNotes || e.creator_notes || zn(n, ["creator_notes"])],
    ["First Message", e.firstMessage || e.first_mes || zn(n, ["first_mes"])],
    ["Message Examples", e.mesExample || e.mes_example || zn(n, ["mes_example"])],
    ["User Persona", t.persona || t.description]
  ].map(([s, a]) => {
    const o = ze(a);
    return o ? `## ${s}
${o}` : "";
  }).filter(Boolean);
  return r.length ? `<character_card>
${r.join(`

`)}
</character_card>` : "";
}
function Lc(e, t) {
  const n = ze(t);
  return n ? `## ${e}
${n}` : "";
}
function FO(e = {}) {
  const t = Array.isArray(e.memoryFiles) ? e.memoryFiles : [], n = Array.isArray(e.structuredStates) ? e.structuredStates : [], r = ze(e.spatialState), s = Array.isArray(e.questHooks) ? e.questHooks.map((u) => ze(u)).filter(Boolean) : [], a = [];
  s.length && a.push(s.join(`
`));
  const o = ze(t.find((u) => u.path === "memory/state.md")?.content || "");
  o && a.push(`## 会话记忆
${o}`);
  const l = t.filter((u) => String(u.path || "").startsWith("memory/characters/")).map((u) => {
    const d = ze(u.content);
    return d ? `### ${ze(u.title || String(u.path || "").replace(/^memory\/characters\//, "").replace(/\.md$/i, "")) || "相关人物"}
${d}` : "";
  }).filter(Boolean);
  l.length && a.push(`## 相关人物记忆
${l.join(`

`)}`);
  const i = r ? [] : n.map((u) => ze(u.digest)).filter(Boolean);
  return i.length && a.push(`## 状态摘要
${i.join(`

`)}`), r && a.push(`## 空间状态
${r}`), a.join(`

`);
}
function BO(e = {}) {
  const t = FO(e);
  return t ? [{
    uid: "session-memory",
    activationKey: "memory:session",
    content: t,
    contentChars: t.length,
    key: [],
    keysecondary: [],
    decorators: [],
    position: 4,
    role: "system",
    order: -1e6,
    depth: 1,
    activationReason: "memory",
    sourceWorldBook: "",
    title: "session memory",
    comment: "session memory"
  }] : [];
}
function UO(e = []) {
  return e.map((t, n) => {
    const r = ze(t.content);
    if (!r) return null;
    const s = ze(t.label) || `runtime depth ${n + 1}`;
    return {
      uid: `runtime-depth:${n}`,
      activationKey: `runtime-depth:${n}:${s}`,
      content: r,
      contentChars: r.length,
      key: [],
      keysecondary: [],
      decorators: [],
      position: 4,
      role: Ha(t.role, "system"),
      order: Number.isFinite(Number(t.order)) ? Number(t.order) : 1e9 + n,
      depth: Number.isFinite(Number(t.depth)) ? Math.max(0, Number(t.depth)) : 1,
      activationReason: ze(t.layer) || "runtime",
      sourceWorldBook: "",
      title: s,
      comment: s
    };
  }).filter((t) => !!t);
}
function qO(e = {}) {
  return (Array.isArray(e.sections) ? e.sections : []).map((t) => ({
    id: ze(t.id),
    label: ze(t.label),
    enabled: t.enabled !== !1,
    marker: t.marker === !0,
    role: Ha(t.role, "system"),
    content: ze(t.content),
    placement: pO.includes(t.placement) ? t.placement : "beforeHistory",
    source: ze(t.source)
  })).filter((t) => t.enabled && (t.content || t.marker));
}
function Yl(e = [], t = "") {
  return e.filter((n) => n.placement === t);
}
function Xl(e = [], t, n = "preset") {
  return e.map((r, s) => ({
    message: Ol(r.role, r.content),
    layer: n,
    label: r.label || `preset ${t} ${s + 1}`,
    sourceId: r.id || void 0
  }));
}
function HO(e = {}) {
  return ze(e.id).replace(/^prompt-manager:/, "");
}
function zO(e) {
  const { presetSections: t, character: n, user: r, worldBuckets: s, conversationUnits: a, runtimeProtocolUnits: o } = e, l = n.data || {}, i = /* @__PURE__ */ new Set(), u = [], d = (f, m) => {
    i.add(f), u.push(...m);
  };
  return t.forEach((f, m) => {
    const v = HO(f);
    if (f.marker) switch (v) {
      case "worldInfoBefore":
        d(v, [Cn("system", Tr("world_info_before_character", s.before), "world-before", f.label || "World Info (before)", {}, f.id)]);
        return;
      case "worldInfoAfter":
        d(v, [Cn("system", Tr("world_info_after_character", s.after), "world-after", f.label || "World Info (after)", {}, f.id)]);
        return;
      case "charDescription":
        d(v, [Cn(f.role, Lc("Description", n.description || zn(l, ["description"])), "character-card", f.label || "Char Description", {}, f.id)]);
        return;
      case "charPersonality":
        d(v, [Cn(f.role, Lc("Personality", n.personality || zn(l, ["personality"])), "character-card", f.label || "Char Personality", {}, f.id)]);
        return;
      case "scenario":
        d(v, [Cn(f.role, Lc("Scenario", n.scenario || zn(l, ["scenario"])), "character-card", f.label || "Scenario", {}, f.id)]);
        return;
      case "personaDescription":
        d(v, [Cn(f.role, Lc("User Persona", r.persona || r.description), "character-card", f.label || "Persona Description", {}, f.id)]);
        return;
      case "dialogueExamples":
        d(v, [Cn(f.role, Lc("Message Examples", n.mesExample || n.mes_example || zn(l, ["mes_example"])), "character-card", f.label || "Chat Examples", {}, f.id)]);
        return;
      case "chatHistory":
        d(v, [
          Cn("system", Tr("world_info_examples_top", s.examplesTop), "world-examples", "world info examples top"),
          Cn("system", Tr("world_info_author_note_top", s.authorNoteTop), "world-author-note", "world info author note top"),
          ...a,
          ...o
        ]);
        return;
      default:
        return;
    }
    u.push({
      message: Ol(f.role, f.content),
      layer: (f.source === "promptManager", "preset"),
      label: f.label || `preset ordered ${m + 1}`,
      sourceId: f.id || void 0
    });
  }), i.has("worldInfoBefore") || u.push(Cn("system", Tr("world_info_before_character", s.before), "world-before", "world info before character")), !i.has("charDescription") && !i.has("charPersonality") && !i.has("scenario") && !i.has("personaDescription") && !i.has("dialogueExamples") && u.push(Cn("system", sM(n, r), "character-card", "character card")), i.has("worldInfoAfter") || u.push(Cn("system", Tr("world_info_after_character", s.after), "world-after", "world info after character")), i.has("chatHistory") || u.push(Cn("system", Tr("world_info_examples_top", s.examplesTop), "world-examples", "world info examples top"), Cn("system", Tr("world_info_author_note_top", s.authorNoteTop), "world-author-note", "world info author note top"), ...a, ...o), u.push(Cn("system", Tr("world_info_author_note_bottom", s.authorNoteBottom), "world-author-note", "world info author note bottom"), Cn("system", Tr("world_info_examples_bottom", s.examplesBottom), "world-examples", "world info examples bottom")), u;
}
function KO(e, t, n) {
  const r = n.get(e);
  return Number.isFinite(Number(r)) ? {
    message: e,
    layer: "world-depth",
    label: `world info depth ${Math.max(0, Number(r))}`
  } : {
    message: e,
    layer: e.role === "user" ? "current-user/history" : "history",
    label: `history ${t + 1}`
  };
}
function aM(e = {}) {
  const t = e.is_user === !0 ? "user" : Ha(e.role, "assistant");
  return t === "tool" ? null : Ol(t, e.content || e.mes || e.message, {
    ...e.name ? { name: String(e.name) } : {},
    ...db(e.thoughts).length ? { thoughts: db(e.thoughts) } : {}
  });
}
function WO(e = [], t = {}) {
  const n = (Array.isArray(e) ? e : []).map((s) => aM(s)).filter((s) => !!s);
  if (!n.length) return [];
  const r = t.separator || `

`;
  return [Ol(Ha(t.role, "system"), `<conversation_history>
${n.map((s) => `<message role="${s.role === "user" ? "user" : "assistant"}">
${s.content}
</message>`).join(r)}
</conversation_history>`)].filter((s) => !!s);
}
function GO(e = [], t = {}) {
  return t.mode === "raw" ? (Array.isArray(e) ? e : []).map((n) => aM(n)).filter((n) => !!n) : WO(e, t);
}
function VO(e = []) {
  const t = /* @__PURE__ */ new Map();
  return rM(e).forEach((n) => {
    const r = Math.max(0, Number(n.depth) || 0), s = Ha(n.role, "system"), a = `${r}\0${s}`, o = t.get(a) || {
      depth: r,
      role: s,
      entries: []
    };
    o.entries.push(n.content), t.set(a, o);
  }), [...t.values()].map((n) => ({
    depth: n.depth,
    message: Ol(n.role, n.entries.join(`

`))
  })).filter((n) => !!n.message);
}
function JO(e = [], t = []) {
  if (!t.length) return e;
  const n = Array.from({ length: e.length + 1 }, () => []);
  t.forEach((s) => {
    const a = Math.max(0, Number(s.depth) || 0);
    n[(e.length ? Math.max(-1, e.length - 1 - a) : -1) + 1].push(s.message);
  });
  const r = [...n[0]];
  return e.forEach((s, a) => {
    r.push(s, ...n[a + 1]);
  }), r;
}
function YO(e = {}) {
  const t = e.character || {}, n = e.user || {}, r = t.data || {};
  return {
    personaDescription: ze(n.persona || n.description),
    characterDescription: ze(t.description || zn(r, ["description"])),
    characterPersonality: ze(t.personality || zn(r, ["personality"])),
    characterDepthPrompt: ze(zn(t, ["characterDepthPrompt"]) || zn(t, ["character_depth_prompt"]) || zn(r, [
      "extensions",
      "depth_prompt",
      "prompt"
    ]) || zn(r, ["character_depth_prompt"]) || zn(r, ["depth_prompt"])),
    scenario: ze(t.scenario || zn(r, ["scenario"])),
    creatorNotes: ze(t.creatorNotes || t.creator_notes || zn(r, ["creator_notes"]))
  };
}
function XO(e = {}) {
  const t = e.character || {}, n = ze(t.avatar), r = n.split(/[\\/]/).pop() || n, s = Array.isArray(t.tags) ? t.tags : [];
  return {
    names: [
      t.name,
      t.characterKey,
      n,
      r
    ].map((a) => ze(a)).filter(Boolean),
    tags: s.map((a) => ze(a)).filter(Boolean)
  };
}
function jO(e = {}, t = {}) {
  const n = String(e.content || e.mes || e.message || "");
  if (!n) return "";
  const r = (e.is_user === !0 ? "user" : Ha(e.role, "assistant")) === "user" ? t.user?.name : t.character?.name, s = ze(e.name || r);
  return s ? `${s}: ${n}` : n;
}
function QO(e = {}, t = "", n = !1) {
  const r = e.history || [], s = n && t ? `${ze(e.user?.name) || "User"}: ${t}` : t;
  return [...r.map((a) => n ? jO(a, e) : a.content || a.mes || a.message || ""), s].map((a) => String(a || "")).filter(Boolean);
}
function ZO(e = {}) {
  const t = !(Array.isArray(e.worldBooks) && e.worldBooks.length > 0) && Array.isArray(e.worldEntries) ? e.worldEntries.map((r) => ({
    ...r,
    sourceWorldBook: r.sourceWorldBook || r.worldName || r.world || ""
  })) : [], n = (Array.isArray(e.worldBooks) ? e.worldBooks : []).flatMap((r) => {
    const s = ze(r.worldSourceType), a = Number(r.worldSourceIndex);
    return Array.isArray(r.entries) ? r.entries.map((o) => ({
      ...o,
      sourceWorldBook: o.sourceWorldBook || o.worldName || o.world || r.name,
      worldSourceType: ze(o.worldSourceType) || s,
      worldSourceIndex: Number.isFinite(Number(o.worldSourceIndex)) ? Number(o.worldSourceIndex) : Number.isFinite(a) ? a : void 0
    })) : [];
  });
  return tF([...t, ...n]);
}
function eF(e = {}) {
  const t = ze(e.sourceWorldBook || e.worldName || e.world) || "direct", n = ze(e.uid ?? e.id);
  if (n) return `${t}\0uid\0${n}`;
  const r = ze(e.comment || e.title || e.name), s = ze(e.content);
  return `${t}\0body\0${r}\0${ni(e.key).join("")}\0${[...ni(e.keysecondary), ...ni(e.secondary_keys)].join("")}\0${s}`;
}
function tF(e = []) {
  const t = /* @__PURE__ */ new Set(), n = [];
  return e.forEach((r) => {
    const s = eF(r);
    t.has(s) || (t.add(s), n.push(r));
  }), n;
}
function oM(e = {}, t = {}, n = {}) {
  const r = e.character || {}, s = e.user || {}, a = e.history || [], o = n.currentUserMessage || "", l = n.historyMode || "raw", i = n.memoryContext || {}, u = Array.isArray(n.runtimeDepthEntries) ? n.runtimeDepthEntries : [], d = Array.isArray(n.runtimeProtocolMessages) ? n.runtimeProtocolMessages : [], f = qO(t), m = n.worldSettings || {}, v = typeof n.worldScanText == "string", y = QO(e, o, m.includeNames === !0), S = v ? String(n.worldScanText || "") : Qk(y, cf(m.scanDepth ?? 2, 2)), x = [ze(m.injectScanText), fO(e, o)].filter(Boolean).join(`
`), I = RO(e.nativeWorldInfo?.activatedEntries), E = $O(e.nativeWorldInfo), N = {
    ...m,
    scanText: S,
    injectScanText: x,
    scanMessages: v ? void 0 : y,
    globalScanData: m.globalScanData || YO(e),
    characterFilterData: m.characterFilterData || XO(e),
    turn: n.turn ?? n.worldSettings?.turn,
    entryStates: n.entryStates ?? n.worldSettings?.entryStates
  }, $ = ZO(e), p = e.nativeWorldInfo ? [] : $, w = MO(p, N), _ = w.activatedBeforeBudget, b = w.budgetDebug, C = PO(p, _, N, b), k = w.activatedEntries;
  if (I.length || E.length || e.nativeWorldInfo) {
    const P = E.length ? E : I;
    return {
      character: r,
      user: s,
      history: a,
      currentUserMessage: o,
      historyMode: l,
      squashRole: n.squashRole,
      memoryContext: i,
      runtimeDepthEntries: u,
      runtimeProtocolMessages: d,
      presetSections: f,
      scanText: S,
      worldSettings: N,
      worldEntryCandidates: [...NO(I), ...C],
      activatedWorldEntries: [...P, ...k],
      localStateWorldEntries: k,
      budgetDebug: b
    };
  }
  return {
    character: r,
    user: s,
    history: a,
    currentUserMessage: o,
    historyMode: l,
    squashRole: n.squashRole,
    memoryContext: i,
    runtimeDepthEntries: u,
    runtimeProtocolMessages: d,
    presetSections: f,
    scanText: S,
    worldSettings: N,
    worldEntryCandidates: C,
    activatedWorldEntries: k,
    localStateWorldEntries: k,
    budgetDebug: b
  };
}
function iM(e, t = {}) {
  const n = GO(e.history, {
    mode: e.historyMode,
    role: e.squashRole || "system",
    userName: e.user.name,
    characterName: e.character.name,
    separator: t.historySeparator
  }), r = Ol("user", e.currentUserMessage);
  return D1([...n, r]);
}
function lM(e = {}, t, n) {
  const { character: r, user: s, history: a, currentUserMessage: o, historyMode: l, squashRole: i, memoryContext: u, runtimeDepthEntries: d, runtimeProtocolMessages: f, presetSections: m, scanText: v, worldSettings: y, worldEntryCandidates: S, activatedWorldEntries: x, budgetDebug: I } = t, E = LO(x), N = VO([
    ...BO(u),
    ...E.atDepth,
    ...UO(d)
  ]), $ = /* @__PURE__ */ new WeakMap();
  N.forEach((R) => $.set(R.message, R.depth));
  const p = JO((t.promptConversationMessages || iM(t, e)).map(gO), N), w = Xl(Yl(m, "top"), "top"), _ = Xl(Yl(m, "beforeCharacter"), "before character"), b = Xl(Yl(m, "afterCharacter"), "after character"), C = Xl(Yl(m, "beforeHistory"), "before history"), k = Xl(Yl(m, "afterHistory"), "after history"), P = Xl(Yl(m, "assistantPrefill"), "assistant prefill", "assistant-prefill"), H = m.some((R) => R.source === "promptManager"), G = p.map((R, V) => KO(R, V, $));
  for (let R = G.length - 1; R >= 0; R -= 1) if (G[R]?.message?.role === "user") {
    G[R].label = "current user message";
    break;
  }
  const ee = f.map((R, V) => ({
    message: R,
    layer: "runtime-protocol",
    label: `runtime protocol ${V + 1}`
  })), J = vO((H ? zO({
    presetSections: m,
    character: r,
    user: s,
    worldBuckets: E,
    conversationUnits: G,
    runtimeProtocolUnits: ee
  }) : null) || [
    ...w,
    Cn("system", Tr("world_info_before_character", E.before), "world-before", "world info before character"),
    ..._,
    Cn("system", sM(r, s), "character-card", "character card"),
    Cn("system", Tr("world_info_after_character", E.after), "world-after", "world info after character"),
    ...b,
    Cn("system", Tr("world_info_examples_top", E.examplesTop), "world-examples", "world info examples top"),
    Cn("system", Tr("world_info_author_note_top", E.authorNoteTop), "world-author-note", "world info author note top"),
    ...C,
    ...G,
    ...ee,
    ...k,
    Cn("system", Tr("world_info_author_note_bottom", E.authorNoteBottom), "world-author-note", "world info author note bottom"),
    Cn("system", Tr("world_info_examples_bottom", E.examplesBottom), "world-examples", "world info examples bottom"),
    ...P
  ]), z = J.messages;
  return {
    messages: z,
    messageLayers: J.messageLayers,
    activatedWorldEntries: x,
    worldEntryCandidates: S,
    outlets: Object.fromEntries(Object.entries(E.outlet).map(([R, V]) => [R, V.map((le) => le.content).join(`

`)])),
    meta: {
      scanText: v,
      scanTextChars: v.length,
      historyMode: l,
      squashedHistory: l !== "raw",
      rawMessagesJson: JSON.stringify(z, null, 2),
      ...n ? { regexApplications: n } : {},
      ...u.structuredStates?.length ? { structuredStates: u.structuredStates } : {},
      ...u.spatialState ? { spatialState: u.spatialState } : {},
      worldBudget: {
        enabled: I.enabled,
        limit: I.limit,
        used: I.used,
        remaining: I.remaining,
        activatedChars: I.activatedChars,
        skippedChars: I.skippedChars
      },
      worldPositionCounts: DO(x),
      worldEntryStateUpdates: OO(t.localStateWorldEntries, y)
    }
  };
}
function nF(e, t = []) {
  const n = D1(t.map((r) => ({
    ...r,
    content: ze(r.content)
  })));
  return {
    ...e,
    messages: n,
    messageLayers: e.messageLayers.map((r, s) => {
      const a = n[s];
      if (!a) return r;
      const o = a.content.length;
      return {
        ...r,
        role: a.role,
        chars: o,
        tokenEstimate: Math.max(1, Math.ceil(o / 4))
      };
    }),
    meta: {
      ...e.meta,
      rawMessagesJson: JSON.stringify(n, null, 2)
    }
  };
}
function rF(e = {}, t = {}, n = {}) {
  return lM(t, oM(e, t, n));
}
async function sF(e = {}, t = {}, n = {}, r = {}) {
  const s = oM(e, t, n);
  if (r.transformWorldEntries) {
    s.activatedWorldEntries = (await r.transformWorldEntries(s.activatedWorldEntries)).map((l) => ({
      ...l,
      content: ze(l.content),
      contentChars: ze(l.content).length
    })).filter((l) => !!l.content);
    const o = new Map(s.activatedWorldEntries.map((l) => [l.activationKey, l]));
    s.localStateWorldEntries = s.localStateWorldEntries.map((l) => o.get(l.activationKey) || l).filter((l) => !!l.content);
  }
  r.transformConversationMessages && (s.promptConversationMessages = D1((await r.transformConversationMessages(iM(s, t))).map((o) => ({
    ...o,
    content: ze(o.content)
  }))));
  const a = lM(t, s, r.regexApplications);
  return r.transformFinalMessages ? nF(a, await r.transformFinalMessages(a.messages)) : a;
}
function q1(e = {}, t = {}, n, r = void 0) {
  const s = e.character || {}, a = e.user || {}, o = Array.isArray(e.worldBooks) ? e.worldBooks : [], l = new Map(n.worldEntryCandidates.map((i) => [i.activationKey, i]));
  return {
    chatPresetId: ze(t.id),
    chatPresetName: ze(t.name),
    presetId: ze(t.id),
    presetName: ze(t.name),
    characterKey: ze(s.characterKey),
    characterName: ze(s.name),
    userName: ze(a.name),
    historyCount: Array.isArray(e.history) ? e.history.length : 0,
    worldBooks: o.map((i) => ({
      name: ze(i.name),
      entries: Array.isArray(i.entries) ? i.entries.length : 0,
      ...i.error ? { error: ze(i.error) } : {}
    })),
    messageCount: n.messages.length,
    messageChars: n.messages.reduce((i, u) => i + String(u.content || "").length, 0),
    messageLayers: n.messageLayers,
    rawMessagesJson: n.meta.rawMessagesJson,
    ...n.meta.regexApplications ? { regexApplications: n.meta.regexApplications } : {},
    activatedWorldEntries: n.activatedWorldEntries.map((i) => {
      const u = l.get(i.activationKey);
      return {
        uid: i.uid,
        sourceWorldBook: i.sourceWorldBook,
        title: u?.title || ze(i.comment || i.title || i.name || i.uid),
        activationReason: i.activationReason,
        insertionTarget: u?.insertionTarget || Kg(i),
        contentChars: i.contentChars
      };
    }),
    ...e.nativeWorldInfo ? { nativeWorldInfo: {
      trigger: ze(e.nativeWorldInfo.trigger),
      sourceNames: Array.isArray(e.nativeWorldInfo.sourceNames) ? e.nativeWorldInfo.sourceNames.map((i) => ({
        name: ze(i.name),
        ...i.sourceType ? { sourceType: i.sourceType } : {},
        ...Number.isFinite(Number(i.sourceIndex)) ? { sourceIndex: Number(i.sourceIndex) } : {}
      })) : []
    } } : {},
    ...n.meta.structuredStates?.length ? { structuredStates: n.meta.structuredStates.map((i) => ({
      docType: ze(i.docType),
      docId: ze(i.docId),
      revision: Number(i.revision) || 0,
      digestChars: ze(i.digest).length
    })) } : {},
    ...n.meta.spatialState ? { spatialStateChars: ze(n.meta.spatialState).length } : {},
    worldBudget: n.meta.worldBudget,
    worldPositionCounts: n.meta.worldPositionCounts,
    scanTextChars: n.meta.scanTextChars,
    ...r === void 0 ? {} : { diagnostics: r }
  };
}
var aF = Object.freeze({
  recursion: !0,
  recursionLimit: 4,
  budgetChars: 24e3
});
function oF(e = {}) {
  const t = e.worldSettings || {};
  return {
    ...aF,
    ...t,
    turn: Math.max(0, Number(e.turn) || 0),
    entryStates: e.entryStates || {}
  };
}
function uM(e) {
  return {
    currentUserMessage: String(e.currentUserMessage || ""),
    historyMode: e.historyMode || "raw",
    worldSettings: oF({
      worldSettings: e.context?.worldSettings,
      turn: e.turn,
      entryStates: e.entryStates
    }),
    memoryContext: e.memoryContext,
    runtimeDepthEntries: e.runtimeDepthEntries,
    runtimeProtocolMessages: e.runtimeProtocolMessages
  };
}
function cM(e) {
  const t = e.context || {}, n = e.chatPreset || e.preset || {}, r = uM(e), s = rF(t, n, r);
  return {
    runtimeState: r,
    buildResult: s,
    buildSnapshot: q1(t, n, s, e.diagnostics),
    rawMessagesJson: s.meta.rawMessagesJson
  };
}
async function H1(e) {
  const t = e.context || {}, n = e.chatPreset || e.preset || {}, r = uM(e), s = await sF(t, n, r, {
    transformWorldEntries: e.transformWorldEntries,
    transformConversationMessages: e.transformConversationMessages,
    transformFinalMessages: e.transformFinalMessages,
    regexApplications: e.regexApplications
  });
  return {
    runtimeState: r,
    buildResult: s,
    buildSnapshot: q1(t, n, s, e.diagnostics),
    rawMessagesJson: s.meta.rawMessagesJson
  };
}
var eT = 24, iF = 24, lF = 240, uF = /* @__PURE__ */ new Map([
  ["“", '"'],
  ["”", '"'],
  ["＂", '"'],
  ["「", '"'],
  ["」", '"'],
  ["『", '"'],
  ["』", '"'],
  ["‘", "'"],
  ["’", "'"],
  ["，", ","],
  ["。", "."],
  ["：", ":"],
  ["；", ";"],
  ["？", "?"],
  ["！", "!"],
  ["（", "("],
  ["）", ")"]
]);
function df(e = "") {
  return Array.from(String(e ?? ""), (t) => uF.get(t) || t).join("");
}
function fb(e = "") {
  let t = "";
  const n = [];
  let r = 0;
  for (const s of String(e ?? "")) {
    const a = r;
    r += s.length, !/\s/u.test(s) && (t += df(s), n.push({
      start: a,
      end: r
    }));
  }
  return {
    compact: t,
    positions: n
  };
}
function z1(e = "", t = "") {
  if (!t) return [];
  const n = [];
  let r = 0;
  for (; r <= e.length; ) {
    const s = e.indexOf(t, r);
    if (s < 0) break;
    n.push({
      start: s,
      end: s + t.length,
      equivalent: !1
    }), r = s + Math.max(t.length, 1);
  }
  return n;
}
function cF(e = "", t = "") {
  const n = df(e), r = df(t);
  return !r || r === t && n === e ? [] : z1(n, r).map((s) => ({
    ...s,
    equivalent: !0
  }));
}
function dF(e = "", t = "") {
  const n = fb(e), r = fb(t);
  return r.compact.length < iF ? [] : !r.compact || r.compact === t && n.compact === e ? [] : z1(n.compact, r.compact).map((s) => {
    const a = n.positions[s.start], o = n.positions[s.end - 1];
    return !a || !o ? null : {
      start: a.start,
      end: o.end,
      equivalent: !0,
      flexibleWhitespace: !0
    };
  }).filter(Boolean);
}
function fF(e = "", t = 0) {
  let n = 1;
  const r = Math.max(0, Math.min(t, e.length));
  for (let s = 0; s < r; s += 1) e[s] === `
` && (n += 1);
  return n;
}
function hF(e = "", t = {}) {
  const n = Math.max(0, Number(t.start) - eT), r = Math.min(e.length, Number(t.end) + eT), s = n > 0 ? "..." : "", a = r < e.length ? "..." : "";
  return `${s}${e.slice(n, r).replace(/\s+/g, " ")}${a}`;
}
function Mu(e = [], t = 1, n = 8) {
  const r = e.slice(0, n), s = e.length > n ? `
...` : "";
  return `${r.map((a, o) => `${t + o}: ${a}`).join(`
`)}${s}`;
}
function Dh(e = "", t = lF) {
  const n = String(e ?? "").replace(/\r\n/g, `
`);
  return n.length > t ? `${n.slice(0, t)}...` : n;
}
function pF(e = "", t = []) {
  return t.slice(0, 8).map((n) => ({
    line: fF(e, n.start),
    context: hF(e, n)
  }));
}
function Oh(e = "", t = /"/g, n = "“", r = "”") {
  let s = !0;
  return e.replace(t, () => {
    const a = s ? n : r;
    return s = !s, a;
  });
}
function mF(e = "", t = "") {
  let n = String(e ?? "");
  return /[“”]/.test(t) ? n = Oh(n, /"/g, "“", "”") : /[「」]/.test(t) ? n = Oh(n, /"/g, "「", "」") : /[『』]/.test(t) && (n = Oh(n, /"/g, "『", "』")), /[‘’]/.test(t) && (n = Oh(n, /'/g, "‘", "’")), t.includes("，") && (n = n.replaceAll(",", "，")), t.includes("。") && (n = n.replaceAll(".", "。")), t.includes("：") && (n = n.replaceAll(":", "：")), t.includes("；") && (n = n.replaceAll(";", "；")), t.includes("？") && (n = n.replaceAll("?", "？")), t.includes("！") && (n = n.replaceAll("!", "！")), t.includes("（") && (n = n.replaceAll("(", "（")), t.includes("）") && (n = n.replaceAll(")", "）")), n;
}
function wm(e = "", t = "") {
  const n = z1(e, t);
  if (n.length) return n;
  const r = cF(e, t);
  return r.length ? r : dF(e, t);
}
function gF(e = "") {
  return fb(e).compact.length;
}
function hb(e = "", t = 8) {
  return gF(e) >= t;
}
function qy(e = "", t = "") {
  return t ? wm(e, t).length > 0 : !1;
}
function vF(e = {}, t = []) {
  const n = typeof e.oldString == "string" ? e.oldString : String(e.oldString ?? ""), r = typeof e.newString == "string" ? e.newString : String(e.newString ?? "");
  return t.some((s = {}) => {
    const a = String(s.matchedText ?? ""), o = String(s.replacement ?? "");
    return a === n && o === r ? !0 : !hb(n, 8) || r !== "" && !hb(r, 8) || !qy(a, n) || qy(o, n) ? !1 : r === "" || qy(o, r);
  });
}
function yF(e = "", t = "", n = "") {
  if (!n || !hb(n, 16)) return {};
  const r = t ? wm(e, t).length : 0, s = wm(e, n).length;
  return r !== 0 || s <= 0 ? {} : {
    uncertain: !0,
    possibleAlreadyApplied: !0,
    newStringMatches: s
  };
}
function _F(e = "", t = [], n = "") {
  let r = e;
  const s = [], a = [];
  return t.slice().sort((o, l) => l.start - o.start).forEach((o) => {
    const l = e.slice(o.start, o.end), i = o.equivalent ? mF(n, l) : n;
    s.push(i), a.push({
      matchedText: l,
      replacement: i
    }), r = `${r.slice(0, o.start)}${i}${r.slice(o.end)}`;
  }), {
    content: r,
    replacements: s,
    replacementDetails: a,
    matchedBy: t.some((o) => o.flexibleWhitespace) ? "flexible_whitespace" : t.some((o) => o.equivalent) ? "punctuation_equivalent" : "exact"
  };
}
function bF(e = "", t = "", n = {}) {
  return {
    ok: !1,
    error: e,
    message: t,
    ...n
  };
}
function Sn(e = "", t = "", n = "", r = {}) {
  return bF(e, t, n ? {
    suggestion: n,
    ...r
  } : r);
}
var wF = /* @__PURE__ */ new Set([
  "oldString",
  "newString",
  "startLine",
  "endLine",
  "insertAtLine",
  "replaceAll"
]);
function Fh(e = [], t = "") {
  for (let n = e.length - 1; n >= 0; n -= 1) if (e[n]?.[1] === t) return e[n];
  return null;
}
function SF(e = [], t = 0) {
  const n = Fh(e, "startLine"), r = Fh(e, "endLine"), s = Fh(e, "insertAtLine"), a = Fh(e, "oldString"), o = [];
  return n && r ? o.push(n, r) : s ? o.push(s) : a && o.push(a), o.filter((l) => l.index > t).sort((l, i) => l.index - i.index)[0] || null;
}
function AF(e = "") {
  let t = String(e ?? "").trim();
  return t.endsWith(",") && (t = t.slice(0, -1).trimEnd()), t.startsWith('"') && (t = t.slice(1)), t.endsWith('"') && (t = t.slice(0, -1)), t.replace(/\\r/g, "\r").replace(/\\n/g, `
`).replace(/\\t/g, "	").replace(/\\"/g, '"').replace(/\\\\/g, "\\");
}
function TF(e = "") {
  const t = String(e || "").trim().replace(/^\{/, "").replace(/\}$/, ""), n = [...t.matchAll(/"?([A-Za-z][A-Za-z0-9_]*)"?\s*:/g)].filter((o) => wF.has(o[1]));
  if (!n.length) return null;
  const r = n.filter((o) => o[1] !== "newString"), s = r[r.length - 1] || null, a = {};
  for (let o = 0; o < n.length; o += 1) {
    const l = n[o], i = l[1], u = l.index + l[0].length, d = Am(a) || Tm(a) || Em(a), f = i === "newString" ? !d && s && s.index > l.index ? SF(n, l.index) : null : n[o + 1];
    let m = f ? f.index : t.length;
    if (f) {
      const y = t.lastIndexOf(",", f.index);
      y >= u && (m = y);
    }
    const v = t.slice(u, m).trim();
    if (i === "startLine" || i === "endLine" || i === "insertAtLine") {
      const y = v.match(/-?\d+/);
      y && (a[i] = Number(y[0]));
      continue;
    }
    if (i === "replaceAll") {
      a[i] = /^true\b/i.test(v);
      continue;
    }
    if (a[i] = AF(v), i === "newString" && !f) break;
  }
  return (Am(a) || Tm(a) || Em(a)) && Object.hasOwn(a, "newString") ? a : null;
}
function EF(e) {
  return e && typeof e == "object" && !Array.isArray(e) && Object.hasOwn(e, "newString") && (Am(e) || Tm(e) || Em(e));
}
function tT(e = "") {
  return /"?newString"?\s*:/i.test(e) && /"?(?:oldString|startLine|endLine|insertAtLine)"?\s*:/i.test(e);
}
function CF(e = "") {
  const t = String(e || "").trim();
  if (!t.startsWith("[") || !t.endsWith("]")) return [];
  const n = t.slice(1, -1).trim();
  if (!n) return [];
  const r = [...n.matchAll(/\}\s*,\s*\{(?=\s*"?(?:oldString|newString|startLine|endLine|insertAtLine|replaceAll)"?\s*:)/g)].map((o) => ({
    index: o.index,
    splitEnd: o.index + 1,
    nextStart: o.index + o[0].lastIndexOf("{")
  })), s = [];
  let a = 0;
  return r.forEach((o, l) => {
    const i = r[l + 1], u = n.slice(a, o.splitEnd), d = n.slice(o.nextStart, i?.splitEnd ?? n.length);
    !tT(u) || !tT(d) || (s.push(u), a = o.nextStart);
  }), s.push(n.slice(a)), s.map((o) => o.trim()).filter(Boolean);
}
function IF(e = "") {
  const t = CF(e);
  if (!t.length) return null;
  const n = t.map((r) => {
    try {
      const s = JSON.parse(r);
      return EF(s) ? s : null;
    } catch {
      return TF(r);
    }
  });
  return n.every((r) => r && typeof r == "object") ? n : null;
}
function Sm(e = {}, t = "") {
  if (!Object.hasOwn(e, t)) return !1;
  const n = e[t];
  return n != null && n !== "";
}
function Am(e = {}) {
  return Sm(e, "oldString");
}
function Tm(e = {}) {
  return Sm(e, "startLine") || Sm(e, "endLine");
}
function Em(e = {}) {
  return Sm(e, "insertAtLine");
}
function So(e) {
  const t = Number(e);
  return Number.isInteger(t) && t > 0 ? t : 0;
}
function dM(e = {}) {
  const t = So(e.startLine), n = So(e.endLine);
  return t && n ? "line_range" : So(e.insertAtLine) ? "line_insert" : Am(e) ? "old_string" : Tm(e) ? "line_range" : Em(e) ? "line_insert" : "old_string";
}
function Bh(e = {}, t = "") {
  const n = typeof e.newString == "string" ? e.newString : String(e.newString ?? "");
  return t === "line_range" ? {
    startLine: e.startLine,
    endLine: e.endLine,
    newString: n
  } : t === "line_insert" ? {
    insertAtLine: e.insertAtLine,
    newString: n
  } : {
    oldString: typeof e.oldString == "string" ? e.oldString : String(e.oldString ?? ""),
    newString: n,
    replaceAll: !!e.replaceAll
  };
}
function xF(e = {}, t = "") {
  return t === "line_range" ? {
    mode: t,
    startLine: e.startLine,
    endLine: e.endLine,
    newPreview: Dh(e.newString)
  } : t === "line_insert" ? {
    mode: t,
    insertAtLine: e.insertAtLine,
    newPreview: Dh(e.newString)
  } : {
    mode: t,
    oldPreview: Dh(e.oldString),
    newPreview: Dh(e.newString),
    replaceAll: !!e.replaceAll || void 0
  };
}
function ff(e = [], t = [], n = []) {
  return e.map((r, s) => {
    if (!r || typeof r != "object") return r;
    const a = n[s] || dM(t[s] || {});
    if (r.ok) return Object.hasOwn(r, "index") ? r : {
      ...r,
      index: s
    };
    const o = Object.hasOwn(r, "index") ? r.index : s;
    return {
      ...xF(t[s] || {}, a),
      ...r,
      index: o
    };
  });
}
function Wg(e = "") {
  return String(e ?? "").replace(/\r\n/g, `
`).split(`
`);
}
function Cm(e = "") {
  if (e === "") return [];
  const t = String(e ?? "").replace(/\r\n/g, `
`).split(`
`);
  t.length > 1 && t[t.length - 1] === "" && t.pop();
  const n = t.filter((r) => r.trim());
  return n.length > 0 && n.every((r) => /^\s*\d+:\s?/.test(r)) ? t.map((r) => r.trim() ? r.replace(/^\s*\d+:\s?/, "") : r) : t;
}
function kF(e = "", t = []) {
  const n = Wg(e), r = new Array(t.length), s = t.map((u = {}, d) => {
    const f = So(u.startLine), m = So(u.endLine), v = typeof u.newString == "string" ? u.newString : String(u.newString ?? "");
    return {
      edit: u,
      index: d,
      startLine: f,
      endLine: m,
      newString: v,
      replacementLines: Cm(v)
    };
  });
  s.forEach((u) => {
    if (!u.startLine || !u.endLine) {
      r[u.index] = Sn("invalid_line_range", "startLine and endLine must be positive integers", "Use line numbers from the latest Read result, with startLine <= endLine.");
      return;
    }
    if (u.endLine < u.startLine) {
      r[u.index] = Sn("invalid_line_range", "endLine must be greater than or equal to startLine", "Use an inclusive line range from the latest Read result.");
      return;
    }
    u.endLine > n.length && (r[u.index] = Sn("line_range_out_of_bounds", `Line range ${u.startLine}-${u.endLine} is outside the file`, `Read the current file again. This file has ${n.length} lines.`));
  });
  const a = s.slice().sort((u, d) => u.startLine - d.startLine || u.endLine - d.endLine);
  a.forEach((u, d) => {
    if (r[u.index]) return;
    const f = a[d - 1];
    f && !r[f.index] && u.startLine <= f.endLine && (r[f.index] = Sn("overlapping_line_ranges", `Line range ${f.startLine}-${f.endLine} overlaps ${u.startLine}-${u.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."), r[u.index] = Sn("overlapping_line_ranges", `Line range ${u.startLine}-${u.endLine} overlaps ${f.startLine}-${f.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."));
  });
  const o = n.slice();
  s.slice().sort((u, d) => d.startLine - u.startLine || d.endLine - u.endLine).forEach((u) => {
    if (r[u.index]) return;
    const d = n.slice(u.startLine - 1, u.endLine);
    o.splice(u.startLine - 1, u.endLine - u.startLine + 1, ...u.replacementLines), r[u.index] = {
      ok: !0,
      index: u.index,
      startLine: u.startLine,
      endLine: u.endLine,
      replacements: 1,
      matchedBy: "line_range",
      oldPreview: Mu(d, u.startLine),
      newPreview: Mu(u.replacementLines, u.startLine)
    };
  });
  const l = r.filter((u) => u && !u.ok).length, i = r.filter((u) => u && u.ok).length;
  return {
    ok: l === 0,
    partial: i > 0 && l > 0 ? !0 : void 0,
    content: o.join(`
`),
    results: ff(r, t, t.map(() => "line_range"))
  };
}
function fM(e = "") {
  return e === "" ? 0 : Wg(e).length;
}
function MF(e = "", t = []) {
  const n = e === "" ? [] : Wg(e), r = fM(e), s = new Array(t.length), a = t.map((u = {}, d) => {
    const f = So(u.insertAtLine), m = typeof u.newString == "string" ? u.newString : String(u.newString ?? "");
    return {
      edit: u,
      index: d,
      insertAtLine: f,
      newString: m,
      insertionLines: Cm(m)
    };
  });
  a.forEach((u) => {
    if (!u.insertAtLine) {
      s[u.index] = Sn("invalid_insert_line", "insertAtLine must be a positive integer", "Use a line number from the latest Read result. insertAtLine inserts before that line; totalLines + 1 appends to the end.");
      return;
    }
    if (u.insertAtLine > r + 1) {
      s[u.index] = Sn("insert_line_out_of_bounds", `Insert line ${u.insertAtLine} is outside the file`, `Read the current file again. This file has ${r} lines, so valid insertAtLine values are 1-${r + 1}.`);
      return;
    }
    u.newString || (s[u.index] = Sn("no_changes", "No changes to make", "Provide non-empty newString text to insert."));
  });
  const o = n.slice();
  a.slice().sort((u, d) => d.insertAtLine - u.insertAtLine || d.index - u.index).forEach((u) => {
    s[u.index] || (o.splice(u.insertAtLine - 1, 0, ...u.insertionLines), s[u.index] = {
      ok: !0,
      index: u.index,
      insertAtLine: u.insertAtLine,
      replacements: 0,
      matchedBy: "line_insert",
      newPreview: Mu(u.insertionLines, u.insertAtLine)
    });
  });
  const l = s.filter((u) => u && !u.ok).length, i = s.filter((u) => u && u.ok).length;
  return {
    ok: l === 0,
    partial: i > 0 && l > 0 ? !0 : void 0,
    content: o.join(`
`),
    results: ff(s, t, t.map(() => "line_insert"))
  };
}
function PF(e = "", t = [], n = []) {
  const r = n.some((m) => m === "line_range"), s = e === "" && !r ? [] : Wg(e), a = fM(e), o = new Array(t.length), l = t.map((m = {}, v) => {
    const y = n[v];
    if (y === "line_insert") {
      const E = So(m.insertAtLine), N = typeof m.newString == "string" ? m.newString : String(m.newString ?? "");
      return {
        mode: y,
        edit: m,
        index: v,
        insertAtLine: E,
        newString: N,
        insertionLines: Cm(N)
      };
    }
    const S = So(m.startLine), x = So(m.endLine), I = typeof m.newString == "string" ? m.newString : String(m.newString ?? "");
    return {
      mode: y,
      edit: m,
      index: v,
      startLine: S,
      endLine: x,
      newString: I,
      replacementLines: Cm(I)
    };
  });
  l.forEach((m) => {
    if (m.mode === "line_insert") {
      if (!m.insertAtLine) {
        o[m.index] = Sn("invalid_insert_line", "insertAtLine must be a positive integer", "Use a line number from the latest Read result. insertAtLine inserts before that line; totalLines + 1 appends to the end.");
        return;
      }
      if (m.insertAtLine > a + 1) {
        o[m.index] = Sn("insert_line_out_of_bounds", `Insert line ${m.insertAtLine} is outside the file`, `Read the current file again. This file has ${a} lines, so valid insertAtLine values are 1-${a + 1}.`);
        return;
      }
      m.newString || (o[m.index] = Sn("no_changes", "No changes to make", "Provide non-empty newString text to insert."));
      return;
    }
    if (!m.startLine || !m.endLine) {
      o[m.index] = Sn("invalid_line_range", "startLine and endLine must be positive integers", "Use line numbers from the latest Read result, with startLine <= endLine.");
      return;
    }
    if (m.endLine < m.startLine) {
      o[m.index] = Sn("invalid_line_range", "endLine must be greater than or equal to startLine", "Use an inclusive line range from the latest Read result.");
      return;
    }
    m.endLine > s.length && (o[m.index] = Sn("line_range_out_of_bounds", `Line range ${m.startLine}-${m.endLine} is outside the file`, `Read the current file again. This file has ${s.length} lines.`));
  });
  const i = l.filter((m) => m.mode === "line_range").slice().sort((m, v) => m.startLine - v.startLine || m.endLine - v.endLine);
  i.forEach((m, v) => {
    if (o[m.index]) return;
    const y = i[v - 1];
    y && !o[y.index] && m.startLine <= y.endLine && (o[y.index] = Sn("overlapping_line_ranges", `Line range ${y.startLine}-${y.endLine} overlaps ${m.startLine}-${m.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."), o[m.index] = Sn("overlapping_line_ranges", `Line range ${m.startLine}-${m.endLine} overlaps ${y.startLine}-${y.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."));
  }), l.filter((m) => m.mode === "line_insert").forEach((m) => {
    if (o[m.index]) return;
    const v = i.find((S) => !o[S.index] && S.startLine < m.insertAtLine && m.insertAtLine <= S.endLine);
    if (!v) return;
    const y = Sn("insert_inside_line_range", `Insert line ${m.insertAtLine} falls inside replaced line range ${v.startLine}-${v.endLine}`, "Merge the insertion into that startLine/endLine replacement, or insert at the range start/end boundary.");
    o[m.index] = y, o[v.index] = y;
  });
  const u = s.slice();
  l.slice().sort((m, v) => {
    const y = m.mode === "line_insert" ? m.insertAtLine : m.startLine, S = v.mode === "line_insert" ? v.insertAtLine : v.startLine;
    return y !== S ? S - y : m.mode !== v.mode ? m.mode === "line_range" ? -1 : 1 : m.mode === "line_insert" ? v.index - m.index : v.endLine - m.endLine || v.index - m.index;
  }).forEach((m) => {
    if (o[m.index]) return;
    if (m.mode === "line_insert") {
      u.splice(m.insertAtLine - 1, 0, ...m.insertionLines), o[m.index] = {
        ok: !0,
        index: m.index,
        insertAtLine: m.insertAtLine,
        replacements: 0,
        matchedBy: "line_insert",
        newPreview: Mu(m.insertionLines, m.insertAtLine)
      };
      return;
    }
    const v = s.slice(m.startLine - 1, m.endLine);
    u.splice(m.startLine - 1, m.endLine - m.startLine + 1, ...m.replacementLines), o[m.index] = {
      ok: !0,
      index: m.index,
      startLine: m.startLine,
      endLine: m.endLine,
      replacements: 1,
      matchedBy: "line_range",
      oldPreview: Mu(v, m.startLine),
      newPreview: Mu(m.replacementLines, m.startLine)
    };
  });
  const d = o.filter((m) => m && !m.ok).length, f = o.filter((m) => m && m.ok).length;
  return {
    ok: d === 0,
    partial: f > 0 && d > 0 ? !0 : void 0,
    content: u.join(`
`),
    results: ff(o, t, n)
  };
}
function RF(e) {
  if (Array.isArray(e)) return {
    ok: !0,
    edits: e,
    parsedFromString: !1
  };
  if (typeof e == "string") {
    const t = e.trim();
    if (!t) return {
      ok: !1,
      error: "edits_must_be_array",
      message: "edits must be a JSON array, but received an empty string",
      suggestion: 'Pass edits as an array, not a quoted JSON string. Correct: "edits":[{"oldString":"old","newString":"new"}].'
    };
    try {
      const n = JSON.parse(t);
      return Array.isArray(n) ? {
        ok: !0,
        edits: n,
        parsedFromString: !0,
        warning: "提醒：模型把 edits 发成了字符串；插件已解析并继续执行。下次请把 edits 直接作为数组传入，不要作为字符串。"
      } : {
        ok: !1,
        error: "edits_must_be_array",
        message: `edits must be a JSON array, but the JSON string parsed to ${Array.isArray(n) ? "array" : typeof n}`,
        suggestion: 'Pass edits directly as an array. Correct: "edits":[{"startLine":10,"endLine":50,"newString":"..."}]. Wrong: "edits":"[{\\"startLine\\":10,...}]".'
      };
    } catch (n) {
      const r = IF(t);
      return r ? {
        ok: !0,
        edits: r,
        parsedFromString: !0,
        warning: "提醒：模型把 edits 发成了字符串，且内容不是标准 JSON；插件已尝试修复并继续执行。下次请把 edits 直接作为数组传入，不要作为字符串。"
      } : {
        ok: !1,
        error: "invalid_edits_json_string",
        message: `edits must be a JSON array, but received a string that is not valid JSON: ${n?.message || "parse failed"}`,
        suggestion: 'Do not JSON-stringify edits. Pass an array value: "edits":[{"oldString":"old","newString":"new"}].'
      };
    }
  }
  return e == null ? {
    ok: !1,
    error: "missing_edits_array",
    message: "edits is required and must be a JSON array, but it was missing",
    suggestion: 'Include an edits array with at least one item. Correct: "edits":[{"startLine":10,"endLine":50,"newString":"..."}].'
  } : {
    ok: !1,
    error: "edits_must_be_array",
    message: `edits must be a JSON array, but received ${typeof e}`,
    suggestion: 'Pass edits as an array, not an object/string. Correct: "edits":[{"oldString":"old","newString":"new"}].'
  };
}
function NF(e = "", t) {
  let n = String(e ?? "");
  const r = RF(t);
  if (!r.ok) return {
    ok: !1,
    content: n,
    results: [Sn(r.error, r.message, r.suggestion)]
  };
  const s = r.edits, a = [], o = [], l = [], i = [];
  let u = 0;
  if (!s.length) return {
    ok: !1,
    content: n,
    results: [Sn("invalid_edits", "No edits provided", "Provide a non-empty edits array. For same-file multi-spot changes, put all replacements in this one Edit call.")]
  };
  const d = s.map((I) => dM(I)), f = d.filter((I) => I === "line_range").length, m = d.filter((I) => I === "line_insert").length, v = d.filter((I) => I === "old_string").length;
  if (f + m && v) return {
    ok: !1,
    content: n,
    results: ff(s.map(() => Sn("mixed_edit_modes", "Do not mix Edit modes in one Edit call", "Use separate Edit calls for oldString replacements, line-range replacements, and insertAtLine insertions.")), s, d)
  };
  if (f && m) {
    const I = PF(n, s.map((E, N) => Bh(E, d[N])), d);
    return r.warning && (I.warning = r.warning), I;
  }
  if (f) {
    const I = kF(n, s.map((E, N) => Bh(E, d[N])));
    return r.warning && (I.warning = r.warning), I;
  }
  if (m) {
    const I = MF(n, s.map((E, N) => Bh(E, d[N])));
    return r.warning && (I.warning = r.warning), I;
  }
  const y = s.map((I, E) => Bh(I, d[E]));
  y.forEach((I = {}, E) => {
    i[E] = l.slice();
    const N = typeof I.oldString == "string" ? I.oldString : String(I.oldString ?? ""), $ = typeof I.newString == "string" ? I.newString : String(I.newString ?? ""), p = !!I.replaceAll;
    if (N === $) {
      a.push(Sn("no_changes", "No changes to make", "Change newString or remove this edit item."));
      return;
    }
    const w = df(N);
    if (N && o.some((k) => k.includes(N) || df(k).includes(w))) {
      a.push(Sn("old_string_matches_previous_new_string", "old_string is a substring of a new_string from a previous edit.", "This edit may match text inserted earlier in the same Edit call. Merge overlapping changes into one larger replacement, or read the updated file and run a later Edit."));
      return;
    }
    if (!N) {
      a.push(Sn("empty_old_string", "oldString is empty; provide text to replace", "Use Write to create files. For Edit, provide the exact current fragment to replace, or use Write for a full rewrite."));
      return;
    }
    const _ = wm(n, N);
    if (!_.length) {
      const k = yF(n, N, $);
      a.push(Sn("not_found", k.possibleAlreadyApplied ? "String to replace not found, but the requested newString already exists in the file" : "String to replace not found in file", k.possibleAlreadyApplied ? "Read the current file and verify whether the intended location is already correct. Edit will not mark this as successful without a same-call replacement proof." : "Edit already retries long oldString values with common punctuation and whitespace differences ignored. If it still fails, Read the current file and copy the exact current text into oldString. If this overlaps another same-file edit, merge them into one larger replacement; for very large rewrites, use Write.", k));
      return;
    }
    if (_.length > 1 && !p) {
      a.push(Sn("multiple_matches", `找到 ${_.length} 处匹配，需要更多上下文或使用 replaceAll`, "Use the returned line contexts to expand oldString with unique surrounding text, or set replaceAll: true only if every match should change.", { matches: pF(n, _) }));
      return;
    }
    const b = p ? _ : _.slice(0, 1), C = _F(n, b, $);
    n = C.content, o.push(...C.replacements), l.push(...C.replacementDetails), u += 1, a.push({
      ok: !0,
      index: E,
      replacements: b.length,
      matchedBy: C.matchedBy
    });
  }), a.forEach((I, E) => {
    !I || I.ok || ["not_found", "old_string_matches_previous_new_string"].includes(I.error) && vF(y[E], i[E] || []) && (a[E] = {
      ok: !0,
      index: E,
      replacements: 0,
      matchedBy: "already_satisfied",
      satisfied: !0,
      previousError: I.error,
      message: "Desired edit is already present in the file"
    });
  });
  const S = a.filter((I) => !I.ok).length, x = a.filter((I) => I?.satisfied).length;
  return {
    ok: S === 0,
    partial: (u > 0 || x > 0) && S > 0 ? !0 : void 0,
    content: n,
    results: ff(a, y, d),
    warning: r.warning
  };
}
var pb = function(e, t) {
  return pb = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(n, r) {
    n.__proto__ = r;
  } || function(n, r) {
    for (var s in r) Object.prototype.hasOwnProperty.call(r, s) && (n[s] = r[s]);
  }, pb(e, t);
};
function $F(e, t) {
  if (typeof t != "function" && t !== null) throw new TypeError("Class extends value " + String(t) + " is not a constructor or null");
  pb(e, t);
  function n() {
    this.constructor = e;
  }
  e.prototype = t === null ? Object.create(t) : (n.prototype = t.prototype, new n());
}
var Rt = function() {
  return Rt = Object.assign || function(t) {
    for (var n, r = 1, s = arguments.length; r < s; r++) {
      n = arguments[r];
      for (var a in n) Object.prototype.hasOwnProperty.call(n, a) && (t[a] = n[a]);
    }
    return t;
  }, Rt.apply(this, arguments);
};
function Im(e, t, n) {
  if (n || arguments.length === 2)
    for (var r = 0, s = t.length, a; r < s; r++) (a || !(r in t)) && (a || (a = Array.prototype.slice.call(t, 0, r)), a[r] = t[r]);
  return e.concat(a || Array.prototype.slice.call(t));
}
var gr = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : globalThis, dr = Object.keys, In = Array.isArray;
typeof Promise < "u" && !gr.Promise && (gr.Promise = Promise);
function Es(e, t) {
  return typeof t != "object" || dr(t).forEach(function(n) {
    e[n] = t[n];
  }), e;
}
var Ku = Object.getPrototypeOf, LF = {}.hasOwnProperty;
function Zr(e, t) {
  return LF.call(e, t);
}
function Wu(e, t) {
  typeof t == "function" && (t = t(Ku(e))), (typeof Reflect > "u" ? dr : Reflect.ownKeys)(t).forEach(function(n) {
    mi(e, n, t[n]);
  });
}
var hM = Object.defineProperty;
function mi(e, t, n, r) {
  hM(e, t, Es(n && Zr(n, "get") && typeof n.get == "function" ? {
    get: n.get,
    set: n.set,
    configurable: !0
  } : {
    value: n,
    configurable: !0,
    writable: !0
  }, r));
}
function ac(e) {
  return { from: function(t) {
    return e.prototype = Object.create(t.prototype), mi(e.prototype, "constructor", e), { extend: Wu.bind(null, e.prototype) };
  } };
}
var DF = Object.getOwnPropertyDescriptor;
function pM(e, t) {
  var n = DF(e, t), r;
  return n || (r = Ku(e)) && pM(r, t);
}
var OF = [].slice;
function Gg(e, t, n) {
  return OF.call(e, t, n);
}
function mM(e, t) {
  return t(e);
}
function dd(e) {
  if (!e) throw new Error("Assertion Failed");
}
function gM(e) {
  gr.setImmediate ? setImmediate(e) : setTimeout(e, 0);
}
function FF(e, t) {
  return e.reduce(function(n, r, s) {
    var a = t(r, s);
    return a && (n[a[0]] = a[1]), n;
  }, {});
}
function Ao(e, t) {
  if (typeof t == "string" && Zr(e, t)) return e[t];
  if (!t) return e;
  if (typeof t != "string") {
    for (var n = [], r = 0, s = t.length; r < s; ++r) {
      var a = Ao(e, t[r]);
      n.push(a);
    }
    return n;
  }
  var o = t.indexOf(".");
  if (o !== -1) {
    var l = e[t.substr(0, o)];
    return l == null ? void 0 : Ao(l, t.substr(o + 1));
  }
}
function ws(e, t, n) {
  if (!(!e || t === void 0) && !("isFrozen" in Object && Object.isFrozen(e)))
    if (typeof t != "string" && "length" in t) {
      dd(typeof n != "string" && "length" in n);
      for (var r = 0, s = t.length; r < s; ++r) ws(e, t[r], n[r]);
    } else {
      var a = t.indexOf(".");
      if (a !== -1) {
        var o = t.substr(0, a), l = t.substr(a + 1);
        if (l === "") n === void 0 ? In(e) && !isNaN(parseInt(o)) ? e.splice(o, 1) : delete e[o] : e[o] = n;
        else {
          var i = e[o];
          (!i || !Zr(e, o)) && (i = e[o] = {}), ws(i, l, n);
        }
      } else n === void 0 ? In(e) && !isNaN(parseInt(t)) ? e.splice(t, 1) : delete e[t] : e[t] = n;
    }
}
function BF(e, t) {
  typeof t == "string" ? ws(e, t, void 0) : "length" in t && [].map.call(t, function(n) {
    ws(e, n, void 0);
  });
}
function vM(e) {
  var t = {};
  for (var n in e) Zr(e, n) && (t[n] = e[n]);
  return t;
}
var UF = [].concat;
function yM(e) {
  return UF.apply([], e);
}
var qF = "BigUint64Array,BigInt64Array,Array,Boolean,String,Date,RegExp,Blob,File,FileList,FileSystemFileHandle,FileSystemDirectoryHandle,ArrayBuffer,DataView,Uint8ClampedArray,ImageBitmap,ImageData,Map,Set,CryptoKey".split(",").concat(yM([
  8,
  16,
  32,
  64
].map(function(e) {
  return [
    "Int",
    "Uint",
    "Float"
  ].map(function(t) {
    return t + e + "Array";
  });
}))).filter(function(e) {
  return gr[e];
}), _M = new Set(qF.map(function(e) {
  return gr[e];
}));
function bM(e) {
  var t = {};
  for (var n in e) if (Zr(e, n)) {
    var r = e[n];
    t[n] = !r || typeof r != "object" || _M.has(r.constructor) ? r : bM(r);
  }
  return t;
}
function HF(e) {
  for (var t in e) if (Zr(e, t)) return !1;
  return !0;
}
var Ud = null;
function Tl(e) {
  Ud = /* @__PURE__ */ new WeakMap();
  var t = mb(e);
  return Ud = null, t;
}
function mb(e) {
  if (!e || typeof e != "object") return e;
  var t = Ud.get(e);
  if (t) return t;
  if (In(e)) {
    t = [], Ud.set(e, t);
    for (var n = 0, r = e.length; n < r; ++n) t.push(mb(e[n]));
  } else if (_M.has(e.constructor)) t = e;
  else {
    var s = Ku(e);
    t = s === Object.prototype ? {} : Object.create(s), Ud.set(e, t);
    for (var a in e) Zr(e, a) && (t[a] = mb(e[a]));
  }
  return t;
}
var zF = {}.toString;
function gb(e) {
  return zF.call(e).slice(8, -1);
}
var vb = typeof Symbol < "u" ? Symbol.iterator : "@@iterator", KF = typeof vb == "symbol" ? function(e) {
  var t;
  return e != null && (t = e[vb]) && t.apply(e);
} : function() {
  return null;
};
function Xi(e, t) {
  var n = e.indexOf(t);
  return n >= 0 && e.splice(n, 1), n >= 0;
}
var cu = {};
function go(e) {
  var t, n, r, s;
  if (arguments.length === 1) {
    if (In(e)) return e.slice();
    if (this === cu && typeof e == "string") return [e];
    if (s = KF(e)) {
      for (n = []; r = s.next(), !r.done; ) n.push(r.value);
      return n;
    }
    if (e == null) return [e];
    if (t = e.length, typeof t == "number") {
      for (n = new Array(t); t--; ) n[t] = e[t];
      return n;
    }
    return [e];
  }
  for (t = arguments.length, n = new Array(t); t--; ) n[t] = arguments[t];
  return n;
}
var K1 = typeof Symbol < "u" ? function(e) {
  return e[Symbol.toStringTag] === "AsyncFunction";
} : function() {
  return !1;
}, WF = [
  "Modify",
  "Bulk",
  "OpenFailed",
  "VersionChange",
  "Schema",
  "Upgrade",
  "InvalidTable",
  "MissingAPI",
  "NoSuchDatabase",
  "InvalidArgument",
  "SubTransaction",
  "Unsupported",
  "Internal",
  "DatabaseClosed",
  "PrematureCommit",
  "ForeignAwait"
], wM = [
  "Unknown",
  "Constraint",
  "Data",
  "TransactionInactive",
  "ReadOnly",
  "Version",
  "NotFound",
  "InvalidState",
  "InvalidAccess",
  "Abort",
  "Timeout",
  "QuotaExceeded",
  "Syntax",
  "DataClone"
], W1 = WF.concat(wM), GF = {
  VersionChanged: "Database version changed by other database connection",
  DatabaseClosed: "Database has been closed",
  Abort: "Transaction aborted",
  TransactionInactive: "Transaction has already completed or failed",
  MissingAPI: "IndexedDB API missing. Please visit https://tinyurl.com/y2uuvskb"
};
function oc(e, t) {
  this.name = e, this.message = t;
}
ac(oc).from(Error).extend({ toString: function() {
  return this.name + ": " + this.message;
} });
function SM(e, t) {
  return e + ". Errors: " + Object.keys(t).map(function(n) {
    return t[n].toString();
  }).filter(function(n, r, s) {
    return s.indexOf(n) === r;
  }).join(`
`);
}
function xm(e, t, n, r) {
  this.failures = t, this.failedKeys = r, this.successCount = n, this.message = SM(e, t);
}
ac(xm).from(oc);
function _u(e, t) {
  this.name = "BulkError", this.failures = Object.keys(t).map(function(n) {
    return t[n];
  }), this.failuresByPos = t, this.message = SM(e, this.failures);
}
ac(_u).from(oc);
var G1 = W1.reduce(function(e, t) {
  return e[t] = t + "Error", e;
}, {}), VF = oc, yt = W1.reduce(function(e, t) {
  var n = t + "Error";
  function r(s, a) {
    this.name = n, s ? typeof s == "string" ? (this.message = "".concat(s).concat(a ? `
 ` + a : ""), this.inner = a || null) : typeof s == "object" && (this.message = "".concat(s.name, " ").concat(s.message), this.inner = s) : (this.message = GF[t] || n, this.inner = null);
  }
  return ac(r).from(VF), e[t] = r, e;
}, {});
yt.Syntax = SyntaxError;
yt.Type = TypeError;
yt.Range = RangeError;
var nT = wM.reduce(function(e, t) {
  return e[t + "Error"] = yt[t], e;
}, {});
function JF(e, t) {
  if (!e || e instanceof oc || e instanceof TypeError || e instanceof SyntaxError || !e.name || !nT[e.name]) return e;
  var n = new nT[e.name](t || e.message, e);
  return "stack" in e && mi(n, "stack", { get: function() {
    return this.inner.stack;
  } }), n;
}
var Vg = W1.reduce(function(e, t) {
  return [
    "Syntax",
    "Type",
    "Range"
  ].indexOf(t) === -1 && (e[t + "Error"] = yt[t]), e;
}, {});
Vg.ModifyError = xm;
Vg.DexieError = oc;
Vg.BulkError = _u;
function hn() {
}
function Df(e) {
  return e;
}
function YF(e, t) {
  return e == null || e === Df ? t : function(n) {
    return t(e(n));
  };
}
function El(e, t) {
  return function() {
    e.apply(this, arguments), t.apply(this, arguments);
  };
}
function XF(e, t) {
  return e === hn ? t : function() {
    var n = e.apply(this, arguments);
    n !== void 0 && (arguments[0] = n);
    var r = this.onsuccess, s = this.onerror;
    this.onsuccess = null, this.onerror = null;
    var a = t.apply(this, arguments);
    return r && (this.onsuccess = this.onsuccess ? El(r, this.onsuccess) : r), s && (this.onerror = this.onerror ? El(s, this.onerror) : s), a !== void 0 ? a : n;
  };
}
function jF(e, t) {
  return e === hn ? t : function() {
    e.apply(this, arguments);
    var n = this.onsuccess, r = this.onerror;
    this.onsuccess = this.onerror = null, t.apply(this, arguments), n && (this.onsuccess = this.onsuccess ? El(n, this.onsuccess) : n), r && (this.onerror = this.onerror ? El(r, this.onerror) : r);
  };
}
function QF(e, t) {
  return e === hn ? t : function(n) {
    var r = e.apply(this, arguments);
    Es(n, r);
    var s = this.onsuccess, a = this.onerror;
    this.onsuccess = null, this.onerror = null;
    var o = t.apply(this, arguments);
    return s && (this.onsuccess = this.onsuccess ? El(s, this.onsuccess) : s), a && (this.onerror = this.onerror ? El(a, this.onerror) : a), r === void 0 ? o === void 0 ? void 0 : o : Es(r, o);
  };
}
function ZF(e, t) {
  return e === hn ? t : function() {
    return t.apply(this, arguments) === !1 ? !1 : e.apply(this, arguments);
  };
}
function V1(e, t) {
  return e === hn ? t : function() {
    var n = e.apply(this, arguments);
    if (n && typeof n.then == "function") {
      for (var r = this, s = arguments.length, a = new Array(s); s--; ) a[s] = arguments[s];
      return n.then(function() {
        return t.apply(r, a);
      });
    }
    return t.apply(this, arguments);
  };
}
var Ua = typeof location < "u" && /^(http|https):\/\/(localhost|127\.0\.0\.1)/.test(location.href);
function AM(e, t) {
  Ua = e;
}
var hf = {}, TM = 100, J1 = typeof Promise > "u" ? [] : (function() {
  var e = Promise.resolve();
  if (typeof crypto > "u" || !crypto.subtle) return [
    e,
    Ku(e),
    e
  ];
  var t = crypto.subtle.digest("SHA-512", new Uint8Array([0]));
  return [
    t,
    Ku(t),
    e
  ];
})(), rT = J1[0], sT = J1[1], eB = J1[2], tB = sT && sT.then, nl = rT && rT.constructor, Y1 = !!eB;
function nB() {
  queueMicrotask(sB);
}
var pf = function(e, t) {
  fd.push([e, t]), km && (nB(), km = !1);
}, yb = !0, km = !0, ml = [], Fp = [], _b = Df, fi = {
  id: "global",
  global: !0,
  ref: 0,
  unhandleds: [],
  onunhandled: hn,
  pgp: !1,
  env: {},
  finalize: hn
}, mt = fi, fd = [], gl = 0, Bp = [];
function et(e) {
  if (typeof this != "object") throw new TypeError("Promises must be constructed via new");
  this._listeners = [], this._lib = !1;
  var t = this._PSD = mt;
  if (typeof e != "function") {
    if (e !== hf) throw new TypeError("Not a function");
    this._state = arguments[1], this._value = arguments[2], this._state === !1 && wb(this, this._value);
    return;
  }
  this._state = null, this._value = null, ++t.ref, CM(this, e);
}
var bb = {
  get: function() {
    var e = mt, t = Mm;
    function n(r, s) {
      var a = this, o = !e.global && (e !== mt || t !== Mm), l = o && !vi(), i = new et(function(u, d) {
        X1(a, new EM(aT(r, e, o, l), aT(s, e, o, l), u, d, e));
      });
      return this._consoleTask && (i._consoleTask = this._consoleTask), i;
    }
    return n.prototype = hf, n;
  },
  set: function(e) {
    mi(this, "then", e && e.prototype === hf ? bb : {
      get: function() {
        return e;
      },
      set: bb.set
    });
  }
};
Wu(et.prototype, {
  then: bb,
  _then: function(e, t) {
    X1(this, new EM(null, null, e, t, mt));
  },
  catch: function(e) {
    if (arguments.length === 1) return this.then(null, e);
    var t = arguments[0], n = arguments[1];
    return typeof t == "function" ? this.then(null, function(r) {
      return r instanceof t ? n(r) : Up(r);
    }) : this.then(null, function(r) {
      return r && r.name === t ? n(r) : Up(r);
    });
  },
  finally: function(e) {
    return this.then(function(t) {
      return et.resolve(e()).then(function() {
        return t;
      });
    }, function(t) {
      return et.resolve(e()).then(function() {
        return Up(t);
      });
    });
  },
  timeout: function(e, t) {
    var n = this;
    return e < 1 / 0 ? new et(function(r, s) {
      var a = setTimeout(function() {
        return s(new yt.Timeout(t));
      }, e);
      n.then(r, s).finally(clearTimeout.bind(null, a));
    }) : this;
  }
});
typeof Symbol < "u" && Symbol.toStringTag && mi(et.prototype, Symbol.toStringTag, "Dexie.Promise");
fi.env = xM();
function EM(e, t, n, r, s) {
  this.onFulfilled = typeof e == "function" ? e : null, this.onRejected = typeof t == "function" ? t : null, this.resolve = n, this.reject = r, this.psd = s;
}
Wu(et, {
  all: function() {
    var e = go.apply(null, arguments).map(Pm);
    return new et(function(t, n) {
      e.length === 0 && t([]);
      var r = e.length;
      e.forEach(function(s, a) {
        return et.resolve(s).then(function(o) {
          e[a] = o, --r || t(e);
        }, n);
      });
    });
  },
  resolve: function(e) {
    return e instanceof et ? e : e && typeof e.then == "function" ? new et(function(t, n) {
      e.then(t, n);
    }) : new et(hf, !0, e);
  },
  reject: Up,
  race: function() {
    var e = go.apply(null, arguments).map(Pm);
    return new et(function(t, n) {
      e.map(function(r) {
        return et.resolve(r).then(t, n);
      });
    });
  },
  PSD: {
    get: function() {
      return mt;
    },
    set: function(e) {
      return mt = e;
    }
  },
  totalEchoes: { get: function() {
    return Mm;
  } },
  newPSD: gi,
  usePSD: Cl,
  scheduler: {
    get: function() {
      return pf;
    },
    set: function(e) {
      pf = e;
    }
  },
  rejectionMapper: {
    get: function() {
      return _b;
    },
    set: function(e) {
      _b = e;
    }
  },
  follow: function(e, t) {
    return new et(function(n, r) {
      return gi(function(s, a) {
        var o = mt;
        o.unhandleds = [], o.onunhandled = a, o.finalize = El(function() {
          var l = this;
          aB(function() {
            l.unhandleds.length === 0 ? s() : a(l.unhandleds[0]);
          });
        }, o.finalize), e();
      }, t, n, r);
    });
  }
});
nl && (nl.allSettled && mi(et, "allSettled", function() {
  var e = go.apply(null, arguments).map(Pm);
  return new et(function(t) {
    e.length === 0 && t([]);
    var n = e.length, r = new Array(n);
    e.forEach(function(s, a) {
      return et.resolve(s).then(function(o) {
        return r[a] = {
          status: "fulfilled",
          value: o
        };
      }, function(o) {
        return r[a] = {
          status: "rejected",
          reason: o
        };
      }).then(function() {
        return --n || t(r);
      });
    });
  });
}), nl.any && typeof AggregateError < "u" && mi(et, "any", function() {
  var e = go.apply(null, arguments).map(Pm);
  return new et(function(t, n) {
    e.length === 0 && n(/* @__PURE__ */ new AggregateError([]));
    var r = e.length, s = new Array(r);
    e.forEach(function(a, o) {
      return et.resolve(a).then(function(l) {
        return t(l);
      }, function(l) {
        s[o] = l, --r || n(new AggregateError(s));
      });
    });
  });
}), nl.withResolvers && (et.withResolvers = nl.withResolvers));
function CM(e, t) {
  try {
    t(function(n) {
      if (e._state === null) {
        if (n === e) throw new TypeError("A promise cannot be resolved with itself.");
        var r = e._lib && ic();
        n && typeof n.then == "function" ? CM(e, function(s, a) {
          n instanceof et ? n._then(s, a) : n.then(s, a);
        }) : (e._state = !0, e._value = n, IM(e)), r && lc();
      }
    }, wb.bind(null, e));
  } catch (n) {
    wb(e, n);
  }
}
function wb(e, t) {
  if (Fp.push(t), e._state === null) {
    var n = e._lib && ic();
    t = _b(t), e._state = !1, e._value = t, oB(e), IM(e), n && lc();
  }
}
function IM(e) {
  var t = e._listeners;
  e._listeners = [];
  for (var n = 0, r = t.length; n < r; ++n) X1(e, t[n]);
  var s = e._PSD;
  --s.ref || s.finalize(), gl === 0 && (++gl, pf(function() {
    --gl === 0 && j1();
  }, []));
}
function X1(e, t) {
  if (e._state === null) {
    e._listeners.push(t);
    return;
  }
  var n = e._state ? t.onFulfilled : t.onRejected;
  if (n === null) return (e._state ? t.resolve : t.reject)(e._value);
  ++t.psd.ref, ++gl, pf(rB, [
    n,
    e,
    t
  ]);
}
function rB(e, t, n) {
  try {
    var r, s = t._value;
    !t._state && Fp.length && (Fp = []), r = Ua && t._consoleTask ? t._consoleTask.run(function() {
      return e(s);
    }) : e(s), !t._state && Fp.indexOf(s) === -1 && iB(t), n.resolve(r);
  } catch (a) {
    n.reject(a);
  } finally {
    --gl === 0 && j1(), --n.psd.ref || n.psd.finalize();
  }
}
function sB() {
  Cl(fi, function() {
    ic() && lc();
  });
}
function ic() {
  var e = yb;
  return yb = !1, km = !1, e;
}
function lc() {
  var e, t, n;
  do
    for (; fd.length > 0; )
      for (e = fd, fd = [], n = e.length, t = 0; t < n; ++t) {
        var r = e[t];
        r[0].apply(null, r[1]);
      }
  while (fd.length > 0);
  yb = !0, km = !0;
}
function j1() {
  var e = ml;
  ml = [], e.forEach(function(r) {
    r._PSD.onunhandled.call(null, r._value, r);
  });
  for (var t = Bp.slice(0), n = t.length; n; ) t[--n]();
}
function aB(e) {
  function t() {
    e(), Bp.splice(Bp.indexOf(t), 1);
  }
  Bp.push(t), ++gl, pf(function() {
    --gl === 0 && j1();
  }, []);
}
function oB(e) {
  ml.some(function(t) {
    return t._value === e._value;
  }) || ml.push(e);
}
function iB(e) {
  for (var t = ml.length; t; ) if (ml[--t]._value === e._value) {
    ml.splice(t, 1);
    return;
  }
}
function Up(e) {
  return new et(hf, !1, e);
}
function An(e, t) {
  var n = mt;
  return function() {
    var r = ic(), s = mt;
    try {
      return yi(n, !0), e.apply(this, arguments);
    } catch (a) {
      t && t(a);
    } finally {
      yi(s, !1), r && lc();
    }
  };
}
var lr = {
  awaits: 0,
  echoes: 0,
  id: 0
}, lB = 0, qp = [], Hp = 0, Mm = 0, uB = 0;
function gi(e, t, n, r) {
  var s = mt, a = Object.create(s);
  a.parent = s, a.ref = 0, a.global = !1, a.id = ++uB, fi.env, a.env = Y1 ? {
    Promise: et,
    PromiseProp: {
      value: et,
      configurable: !0,
      writable: !0
    },
    all: et.all,
    race: et.race,
    allSettled: et.allSettled,
    any: et.any,
    resolve: et.resolve,
    reject: et.reject
  } : {}, t && Es(a, t), ++s.ref, a.finalize = function() {
    --this.parent.ref || this.parent.finalize();
  };
  var o = Cl(a, e, n, r);
  return a.ref === 0 && a.finalize(), o;
}
function uc() {
  return lr.id || (lr.id = ++lB), ++lr.awaits, lr.echoes += TM, lr.id;
}
function vi() {
  return lr.awaits ? (--lr.awaits === 0 && (lr.id = 0), lr.echoes = lr.awaits * TM, !0) : !1;
}
("" + tB).indexOf("[native code]") === -1 && (uc = vi = hn);
function Pm(e) {
  return lr.echoes && e && e.constructor === nl ? (uc(), e.then(function(t) {
    return vi(), t;
  }, function(t) {
    return vi(), Ln(t);
  })) : e;
}
function cB(e) {
  ++Mm, (!lr.echoes || --lr.echoes === 0) && (lr.echoes = lr.awaits = lr.id = 0), qp.push(mt), yi(e, !0);
}
function dB() {
  var e = qp[qp.length - 1];
  qp.pop(), yi(e, !1);
}
function yi(e, t) {
  var n = mt;
  if ((t ? lr.echoes && (!Hp++ || e !== mt) : Hp && (!--Hp || e !== mt)) && queueMicrotask(t ? cB.bind(null, e) : dB), e !== mt && (mt = e, n === fi && (fi.env = xM()), Y1)) {
    var r = fi.env.Promise, s = e.env;
    (n.global || e.global) && (Object.defineProperty(gr, "Promise", s.PromiseProp), r.all = s.all, r.race = s.race, r.resolve = s.resolve, r.reject = s.reject, s.allSettled && (r.allSettled = s.allSettled), s.any && (r.any = s.any));
  }
}
function xM() {
  var e = gr.Promise;
  return Y1 ? {
    Promise: e,
    PromiseProp: Object.getOwnPropertyDescriptor(gr, "Promise"),
    all: e.all,
    race: e.race,
    allSettled: e.allSettled,
    any: e.any,
    resolve: e.resolve,
    reject: e.reject
  } : {};
}
function Cl(e, t, n, r, s) {
  var a = mt;
  try {
    return yi(e, !0), t(n, r, s);
  } finally {
    yi(a, !1);
  }
}
function aT(e, t, n, r) {
  return typeof e != "function" ? e : function() {
    var s = mt;
    n && uc(), yi(t, !0);
    try {
      return e.apply(this, arguments);
    } finally {
      yi(s, !1), r && queueMicrotask(vi);
    }
  };
}
function Hy(e) {
  Promise === nl && lr.echoes === 0 ? Hp === 0 ? e() : enqueueNativeMicroTask(e) : setTimeout(e, 0);
}
var Ln = et.reject;
function Sb(e, t, n, r) {
  if (!e.idbdb || !e._state.openComplete && !mt.letThrough && !e._vip) {
    if (e._state.openComplete) return Ln(new yt.DatabaseClosed(e._state.dbOpenError));
    if (!e._state.isBeingOpened) {
      if (!e._state.autoOpen) return Ln(new yt.DatabaseClosed());
      e.open().catch(hn);
    }
    return e._state.dbReadyPromise.then(function() {
      return Sb(e, t, n, r);
    });
  } else {
    var s = e._createTransaction(t, n, e._dbSchema);
    try {
      s.create(), e._state.PR1398_maxLoop = 3;
    } catch (a) {
      return a.name === G1.InvalidState && e.isOpen() && --e._state.PR1398_maxLoop > 0 ? (console.warn("Dexie: Need to reopen db"), e.close({ disableAutoOpen: !1 }), e.open().then(function() {
        return Sb(e, t, n, r);
      })) : Ln(a);
    }
    return s._promise(t, function(a, o) {
      return gi(function() {
        return mt.trans = s, r(a, o, s);
      });
    }).then(function(a) {
      if (t === "readwrite") try {
        s.idbtrans.commit();
      } catch {
      }
      return t === "readonly" ? a : s._completion.then(function() {
        return a;
      });
    });
  }
}
var oT = "4.0.10", ul = "￿", Ab = -1 / 0, eo = "Invalid key provided. Keys must be of type string, number, Date or Array<string | number | Date>.", kM = "String expected.", Pu = [], Jg = "__dbnames", zy = "readonly", Ky = "readwrite";
function Il(e, t) {
  return e ? t ? function() {
    return e.apply(this, arguments) && t.apply(this, arguments);
  } : e : t;
}
var MM = {
  type: 3,
  lower: -1 / 0,
  lowerOpen: !1,
  upper: [[]],
  upperOpen: !1
};
function Uh(e) {
  return typeof e == "string" && !/\./.test(e) ? function(t) {
    return t[e] === void 0 && e in t && (t = Tl(t), delete t[e]), t;
  } : function(t) {
    return t;
  };
}
function fB() {
  throw yt.Type();
}
function Jt(e, t) {
  try {
    var n = iT(e), r = iT(t);
    if (n !== r)
      return n === "Array" ? 1 : r === "Array" ? -1 : n === "binary" ? 1 : r === "binary" ? -1 : n === "string" ? 1 : r === "string" ? -1 : n === "Date" ? 1 : r !== "Date" ? NaN : -1;
    switch (n) {
      case "number":
      case "Date":
      case "string":
        return e > t ? 1 : e < t ? -1 : 0;
      case "binary":
        return pB(lT(e), lT(t));
      case "Array":
        return hB(e, t);
    }
  } catch {
  }
  return NaN;
}
function hB(e, t) {
  for (var n = e.length, r = t.length, s = n < r ? n : r, a = 0; a < s; ++a) {
    var o = Jt(e[a], t[a]);
    if (o !== 0) return o;
  }
  return n === r ? 0 : n < r ? -1 : 1;
}
function pB(e, t) {
  for (var n = e.length, r = t.length, s = n < r ? n : r, a = 0; a < s; ++a) if (e[a] !== t[a]) return e[a] < t[a] ? -1 : 1;
  return n === r ? 0 : n < r ? -1 : 1;
}
function iT(e) {
  var t = typeof e;
  if (t !== "object") return t;
  if (ArrayBuffer.isView(e)) return "binary";
  var n = gb(e);
  return n === "ArrayBuffer" ? "binary" : n;
}
function lT(e) {
  return e instanceof Uint8Array ? e : ArrayBuffer.isView(e) ? new Uint8Array(e.buffer, e.byteOffset, e.byteLength) : new Uint8Array(e);
}
var PM = (function() {
  function e() {
  }
  return e.prototype._trans = function(t, n, r) {
    var s = this._tx || mt.trans, a = this.name, o = Ua && typeof console < "u" && console.createTask && console.createTask("Dexie: ".concat(t === "readonly" ? "read" : "write", " ").concat(this.name));
    function l(d, f, m) {
      if (!m.schema[a]) throw new yt.NotFound("Table " + a + " not part of transaction");
      return n(m.idbtrans, m);
    }
    var i = ic();
    try {
      var u = s && s.db._novip === this.db._novip ? s === mt.trans ? s._promise(t, l, r) : gi(function() {
        return s._promise(t, l, r);
      }, {
        trans: s,
        transless: mt.transless || mt
      }) : Sb(this.db, t, [this.name], l);
      return o && (u._consoleTask = o, u = u.catch(function(d) {
        return console.trace(d), Ln(d);
      })), u;
    } finally {
      i && lc();
    }
  }, e.prototype.get = function(t, n) {
    var r = this;
    return t && t.constructor === Object ? this.where(t).first(n) : t == null ? Ln(new yt.Type("Invalid argument to Table.get()")) : this._trans("readonly", function(s) {
      return r.core.get({
        trans: s,
        key: t
      }).then(function(a) {
        return r.hook.reading.fire(a);
      });
    }).then(n);
  }, e.prototype.where = function(t) {
    if (typeof t == "string") return new this.db.WhereClause(this, t);
    if (In(t)) return new this.db.WhereClause(this, "[".concat(t.join("+"), "]"));
    var n = dr(t);
    if (n.length === 1) return this.where(n[0]).equals(t[n[0]]);
    var r = this.schema.indexes.concat(this.schema.primKey).filter(function(d) {
      if (d.compound && n.every(function(m) {
        return d.keyPath.indexOf(m) >= 0;
      })) {
        for (var f = 0; f < n.length; ++f) if (n.indexOf(d.keyPath[f]) === -1) return !1;
        return !0;
      }
      return !1;
    }).sort(function(d, f) {
      return d.keyPath.length - f.keyPath.length;
    })[0];
    if (r && this.db._maxKey !== ul) {
      var s = r.keyPath.slice(0, n.length);
      return this.where(s).equals(s.map(function(d) {
        return t[d];
      }));
    }
    !r && Ua && console.warn("The query ".concat(JSON.stringify(t), " on ").concat(this.name, " would benefit from a ") + "compound index [".concat(n.join("+"), "]"));
    var a = this.schema.idxByName;
    function o(d, f) {
      return Jt(d, f) === 0;
    }
    var l = n.reduce(function(d, f) {
      var m = d[0], v = d[1], y = a[f], S = t[f];
      return [m || y, m || !y ? Il(v, y && y.multi ? function(x) {
        var I = Ao(x, f);
        return In(I) && I.some(function(E) {
          return o(S, E);
        });
      } : function(x) {
        return o(S, Ao(x, f));
      }) : v];
    }, [null, null]), i = l[0], u = l[1];
    return i ? this.where(i.name).equals(t[i.keyPath]).filter(u) : r ? this.filter(u) : this.where(n).equals("");
  }, e.prototype.filter = function(t) {
    return this.toCollection().and(t);
  }, e.prototype.count = function(t) {
    return this.toCollection().count(t);
  }, e.prototype.offset = function(t) {
    return this.toCollection().offset(t);
  }, e.prototype.limit = function(t) {
    return this.toCollection().limit(t);
  }, e.prototype.each = function(t) {
    return this.toCollection().each(t);
  }, e.prototype.toArray = function(t) {
    return this.toCollection().toArray(t);
  }, e.prototype.toCollection = function() {
    return new this.db.Collection(new this.db.WhereClause(this));
  }, e.prototype.orderBy = function(t) {
    return new this.db.Collection(new this.db.WhereClause(this, In(t) ? "[".concat(t.join("+"), "]") : t));
  }, e.prototype.reverse = function() {
    return this.toCollection().reverse();
  }, e.prototype.mapToClass = function(t) {
    var n = this, r = n.db, s = n.name;
    this.schema.mappedClass = t, t.prototype instanceof fB && (t = (function(i) {
      $F(u, i);
      function u() {
        return i !== null && i.apply(this, arguments) || this;
      }
      return Object.defineProperty(u.prototype, "db", {
        get: function() {
          return r;
        },
        enumerable: !1,
        configurable: !0
      }), u.prototype.table = function() {
        return s;
      }, u;
    })(t));
    for (var a = /* @__PURE__ */ new Set(), o = t.prototype; o; o = Ku(o)) Object.getOwnPropertyNames(o).forEach(function(i) {
      return a.add(i);
    });
    var l = function(i) {
      if (!i) return i;
      var u = Object.create(t.prototype);
      for (var d in i) if (!a.has(d)) try {
        u[d] = i[d];
      } catch {
      }
      return u;
    };
    return this.schema.readHook && this.hook.reading.unsubscribe(this.schema.readHook), this.schema.readHook = l, this.hook("reading", l), t;
  }, e.prototype.defineClass = function() {
    function t(n) {
      Es(this, n);
    }
    return this.mapToClass(t);
  }, e.prototype.add = function(t, n) {
    var r = this, s = this.schema.primKey, a = s.auto, o = s.keyPath, l = t;
    return o && a && (l = Uh(o)(t)), this._trans("readwrite", function(i) {
      return r.core.mutate({
        trans: i,
        type: "add",
        keys: n != null ? [n] : null,
        values: [l]
      });
    }).then(function(i) {
      return i.numFailures ? et.reject(i.failures[0]) : i.lastResult;
    }).then(function(i) {
      if (o) try {
        ws(t, o, i);
      } catch {
      }
      return i;
    });
  }, e.prototype.update = function(t, n) {
    if (typeof t == "object" && !In(t)) {
      var r = Ao(t, this.schema.primKey.keyPath);
      return r === void 0 ? Ln(new yt.InvalidArgument("Given object does not contain its primary key")) : this.where(":id").equals(r).modify(n);
    } else return this.where(":id").equals(t).modify(n);
  }, e.prototype.put = function(t, n) {
    var r = this, s = this.schema.primKey, a = s.auto, o = s.keyPath, l = t;
    return o && a && (l = Uh(o)(t)), this._trans("readwrite", function(i) {
      return r.core.mutate({
        trans: i,
        type: "put",
        values: [l],
        keys: n != null ? [n] : null
      });
    }).then(function(i) {
      return i.numFailures ? et.reject(i.failures[0]) : i.lastResult;
    }).then(function(i) {
      if (o) try {
        ws(t, o, i);
      } catch {
      }
      return i;
    });
  }, e.prototype.delete = function(t) {
    var n = this;
    return this._trans("readwrite", function(r) {
      return n.core.mutate({
        trans: r,
        type: "delete",
        keys: [t]
      });
    }).then(function(r) {
      return r.numFailures ? et.reject(r.failures[0]) : void 0;
    });
  }, e.prototype.clear = function() {
    var t = this;
    return this._trans("readwrite", function(n) {
      return t.core.mutate({
        trans: n,
        type: "deleteRange",
        range: MM
      });
    }).then(function(n) {
      return n.numFailures ? et.reject(n.failures[0]) : void 0;
    });
  }, e.prototype.bulkGet = function(t) {
    var n = this;
    return this._trans("readonly", function(r) {
      return n.core.getMany({
        keys: t,
        trans: r
      }).then(function(s) {
        return s.map(function(a) {
          return n.hook.reading.fire(a);
        });
      });
    });
  }, e.prototype.bulkAdd = function(t, n, r) {
    var s = this, a = Array.isArray(n) ? n : void 0;
    r = r || (a ? void 0 : n);
    var o = r ? r.allKeys : void 0;
    return this._trans("readwrite", function(l) {
      var i = s.schema.primKey, u = i.auto, d = i.keyPath;
      if (d && a) throw new yt.InvalidArgument("bulkAdd(): keys argument invalid on tables with inbound keys");
      if (a && a.length !== t.length) throw new yt.InvalidArgument("Arguments objects and keys must have the same length");
      var f = t.length, m = d && u ? t.map(Uh(d)) : t;
      return s.core.mutate({
        trans: l,
        type: "add",
        keys: a,
        values: m,
        wantResults: o
      }).then(function(v) {
        var y = v.numFailures, S = v.results, x = v.lastResult, I = v.failures, E = o ? S : x;
        if (y === 0) return E;
        throw new _u("".concat(s.name, ".bulkAdd(): ").concat(y, " of ").concat(f, " operations failed"), I);
      });
    });
  }, e.prototype.bulkPut = function(t, n, r) {
    var s = this, a = Array.isArray(n) ? n : void 0;
    r = r || (a ? void 0 : n);
    var o = r ? r.allKeys : void 0;
    return this._trans("readwrite", function(l) {
      var i = s.schema.primKey, u = i.auto, d = i.keyPath;
      if (d && a) throw new yt.InvalidArgument("bulkPut(): keys argument invalid on tables with inbound keys");
      if (a && a.length !== t.length) throw new yt.InvalidArgument("Arguments objects and keys must have the same length");
      var f = t.length, m = d && u ? t.map(Uh(d)) : t;
      return s.core.mutate({
        trans: l,
        type: "put",
        keys: a,
        values: m,
        wantResults: o
      }).then(function(v) {
        var y = v.numFailures, S = v.results, x = v.lastResult, I = v.failures, E = o ? S : x;
        if (y === 0) return E;
        throw new _u("".concat(s.name, ".bulkPut(): ").concat(y, " of ").concat(f, " operations failed"), I);
      });
    });
  }, e.prototype.bulkUpdate = function(t) {
    var n = this, r = this.core, s = t.map(function(l) {
      return l.key;
    }), a = t.map(function(l) {
      return l.changes;
    }), o = [];
    return this._trans("readwrite", function(l) {
      return r.getMany({
        trans: l,
        keys: s,
        cache: "clone"
      }).then(function(i) {
        var u = [], d = [];
        t.forEach(function(m, v) {
          var y = m.key, S = m.changes, x = i[v];
          if (x) {
            for (var I = 0, E = Object.keys(S); I < E.length; I++) {
              var N = E[I], $ = S[N];
              if (N === n.schema.primKey.keyPath) {
                if (Jt($, y) !== 0) throw new yt.Constraint("Cannot update primary key in bulkUpdate()");
              } else ws(x, N, $);
            }
            o.push(v), u.push(y), d.push(x);
          }
        });
        var f = u.length;
        return r.mutate({
          trans: l,
          type: "put",
          keys: u,
          values: d,
          updates: {
            keys: s,
            changeSpecs: a
          }
        }).then(function(m) {
          var v = m.numFailures, y = m.failures;
          if (v === 0) return f;
          for (var S = 0, x = Object.keys(y); S < x.length; S++) {
            var I = x[S], E = o[Number(I)];
            if (E != null) {
              var N = y[I];
              delete y[I], y[E] = N;
            }
          }
          throw new _u("".concat(n.name, ".bulkUpdate(): ").concat(v, " of ").concat(f, " operations failed"), y);
        });
      });
    });
  }, e.prototype.bulkDelete = function(t) {
    var n = this, r = t.length;
    return this._trans("readwrite", function(s) {
      return n.core.mutate({
        trans: s,
        type: "delete",
        keys: t
      });
    }).then(function(s) {
      var a = s.numFailures, o = s.lastResult, l = s.failures;
      if (a === 0) return o;
      throw new _u("".concat(n.name, ".bulkDelete(): ").concat(a, " of ").concat(r, " operations failed"), l);
    });
  }, e;
})();
function Of(e) {
  var t = {}, n = function(l, i) {
    if (i) {
      for (var u = arguments.length, d = new Array(u - 1); --u; ) d[u - 1] = arguments[u];
      return t[l].subscribe.apply(null, d), e;
    } else if (typeof l == "string") return t[l];
  };
  n.addEventType = a;
  for (var r = 1, s = arguments.length; r < s; ++r) a(arguments[r]);
  return n;
  function a(l, i, u) {
    if (typeof l == "object") return o(l);
    i || (i = ZF), u || (u = hn);
    var d = {
      subscribers: [],
      fire: u,
      subscribe: function(f) {
        d.subscribers.indexOf(f) === -1 && (d.subscribers.push(f), d.fire = i(d.fire, f));
      },
      unsubscribe: function(f) {
        d.subscribers = d.subscribers.filter(function(m) {
          return m !== f;
        }), d.fire = d.subscribers.reduce(i, u);
      }
    };
    return t[l] = n[l] = d, d;
  }
  function o(l) {
    dr(l).forEach(function(i) {
      var u = l[i];
      if (In(u)) a(i, l[i][0], l[i][1]);
      else if (u === "asap") var d = a(i, Df, function() {
        for (var m = arguments.length, v = new Array(m); m--; ) v[m] = arguments[m];
        d.subscribers.forEach(function(y) {
          gM(function() {
            y.apply(null, v);
          });
        });
      });
      else throw new yt.InvalidArgument("Invalid event config");
    });
  }
}
function Ff(e, t) {
  return ac(t).from({ prototype: e }), t;
}
function mB(e) {
  return Ff(PM.prototype, function(n, r, s) {
    this.db = e, this._tx = s, this.name = n, this.schema = r, this.hook = e._allTables[n] ? e._allTables[n].hook : Of(null, {
      creating: [XF, hn],
      reading: [YF, Df],
      updating: [QF, hn],
      deleting: [jF, hn]
    });
  });
}
function jl(e, t) {
  return !(e.filter || e.algorithm || e.or) && (t ? e.justLimit : !e.replayFilter);
}
function Wy(e, t) {
  e.filter = Il(e.filter, t);
}
function Gy(e, t, n) {
  var r = e.replayFilter;
  e.replayFilter = r ? function() {
    return Il(r(), t());
  } : t, e.justLimit = n && !r;
}
function gB(e, t) {
  e.isMatch = Il(e.isMatch, t);
}
function zp(e, t) {
  if (e.isPrimKey) return t.primaryKey;
  var n = t.getIndexByKeyPath(e.index);
  if (!n) throw new yt.Schema("KeyPath " + e.index + " on object store " + t.name + " is not indexed");
  return n;
}
function uT(e, t, n) {
  var r = zp(e, t.schema);
  return t.openCursor({
    trans: n,
    values: !e.keysOnly,
    reverse: e.dir === "prev",
    unique: !!e.unique,
    query: {
      index: r,
      range: e.range
    }
  });
}
function qh(e, t, n, r) {
  var s = e.replayFilter ? Il(e.filter, e.replayFilter()) : e.filter;
  if (e.or) {
    var a = {}, o = function(l, i, u) {
      if (!s || s(i, u, function(m) {
        return i.stop(m);
      }, function(m) {
        return i.fail(m);
      })) {
        var d = i.primaryKey, f = "" + d;
        f === "[object ArrayBuffer]" && (f = "" + new Uint8Array(d)), Zr(a, f) || (a[f] = !0, t(l, i, u));
      }
    };
    return Promise.all([e.or._iterate(o, n), cT(uT(e, r, n), e.algorithm, o, !e.keysOnly && e.valueMapper)]);
  } else
    return cT(uT(e, r, n), Il(e.algorithm, s), t, !e.keysOnly && e.valueMapper);
}
function cT(e, t, n, r) {
  var s = An(r ? function(a, o, l) {
    return n(r(a), o, l);
  } : n);
  return e.then(function(a) {
    if (a) return a.start(function() {
      var o = function() {
        return a.continue();
      };
      (!t || t(a, function(l) {
        return o = l;
      }, function(l) {
        a.stop(l), o = hn;
      }, function(l) {
        a.fail(l), o = hn;
      })) && s(a.value, a, function(l) {
        return o = l;
      }), o();
    });
  });
}
var vB = (function() {
  function e(t) {
    Object.assign(this, t);
  }
  return e.prototype.execute = function(t) {
    var n;
    if (this.add !== void 0) {
      var r = this.add;
      if (In(r)) return Im(Im([], In(t) ? t : [], !0), r, !0).sort();
      if (typeof r == "number") return (Number(t) || 0) + r;
      if (typeof r == "bigint") try {
        return BigInt(t) + r;
      } catch {
        return BigInt(0) + r;
      }
      throw new TypeError("Invalid term ".concat(r));
    }
    if (this.remove !== void 0) {
      var s = this.remove;
      if (In(s)) return In(t) ? t.filter(function(o) {
        return !s.includes(o);
      }).sort() : [];
      if (typeof s == "number") return Number(t) - s;
      if (typeof s == "bigint") try {
        return BigInt(t) - s;
      } catch {
        return BigInt(0) - s;
      }
      throw new TypeError("Invalid subtrahend ".concat(s));
    }
    var a = (n = this.replacePrefix) === null || n === void 0 ? void 0 : n[0];
    return a && typeof t == "string" && t.startsWith(a) ? this.replacePrefix[1] + t.substring(a.length) : t;
  }, e;
})(), yB = (function() {
  function e() {
  }
  return e.prototype._read = function(t, n) {
    var r = this._ctx;
    return r.error ? r.table._trans(null, Ln.bind(null, r.error)) : r.table._trans("readonly", t).then(n);
  }, e.prototype._write = function(t) {
    var n = this._ctx;
    return n.error ? n.table._trans(null, Ln.bind(null, n.error)) : n.table._trans("readwrite", t, "locked");
  }, e.prototype._addAlgorithm = function(t) {
    var n = this._ctx;
    n.algorithm = Il(n.algorithm, t);
  }, e.prototype._iterate = function(t, n) {
    return qh(this._ctx, t, n, this._ctx.table.core);
  }, e.prototype.clone = function(t) {
    var n = Object.create(this.constructor.prototype), r = Object.create(this._ctx);
    return t && Es(r, t), n._ctx = r, n;
  }, e.prototype.raw = function() {
    return this._ctx.valueMapper = null, this;
  }, e.prototype.each = function(t) {
    var n = this._ctx;
    return this._read(function(r) {
      return qh(n, t, r, n.table.core);
    });
  }, e.prototype.count = function(t) {
    var n = this;
    return this._read(function(r) {
      var s = n._ctx, a = s.table.core;
      if (jl(s, !0)) return a.count({
        trans: r,
        query: {
          index: zp(s, a.schema),
          range: s.range
        }
      }).then(function(l) {
        return Math.min(l, s.limit);
      });
      var o = 0;
      return qh(s, function() {
        return ++o, !1;
      }, r, a).then(function() {
        return o;
      });
    }).then(t);
  }, e.prototype.sortBy = function(t, n) {
    var r = t.split(".").reverse(), s = r[0], a = r.length - 1;
    function o(u, d) {
      return d ? o(u[r[d]], d - 1) : u[s];
    }
    var l = this._ctx.dir === "next" ? 1 : -1;
    function i(u, d) {
      return Jt(o(u, a), o(d, a)) * l;
    }
    return this.toArray(function(u) {
      return u.sort(i);
    }).then(n);
  }, e.prototype.toArray = function(t) {
    var n = this;
    return this._read(function(r) {
      var s = n._ctx;
      if (s.dir === "next" && jl(s, !0) && s.limit > 0) {
        var a = s.valueMapper, o = zp(s, s.table.core.schema);
        return s.table.core.query({
          trans: r,
          limit: s.limit,
          values: !0,
          query: {
            index: o,
            range: s.range
          }
        }).then(function(i) {
          var u = i.result;
          return a ? u.map(a) : u;
        });
      } else {
        var l = [];
        return qh(s, function(i) {
          return l.push(i);
        }, r, s.table.core).then(function() {
          return l;
        });
      }
    }, t);
  }, e.prototype.offset = function(t) {
    var n = this._ctx;
    return t <= 0 ? this : (n.offset += t, jl(n) ? Gy(n, function() {
      var r = t;
      return function(s, a) {
        return r === 0 ? !0 : r === 1 ? (--r, !1) : (a(function() {
          s.advance(r), r = 0;
        }), !1);
      };
    }) : Gy(n, function() {
      var r = t;
      return function() {
        return --r < 0;
      };
    }), this);
  }, e.prototype.limit = function(t) {
    return this._ctx.limit = Math.min(this._ctx.limit, t), Gy(this._ctx, function() {
      var n = t;
      return function(r, s, a) {
        return --n <= 0 && s(a), n >= 0;
      };
    }, !0), this;
  }, e.prototype.until = function(t, n) {
    return Wy(this._ctx, function(r, s, a) {
      return t(r.value) ? (s(a), n) : !0;
    }), this;
  }, e.prototype.first = function(t) {
    return this.limit(1).toArray(function(n) {
      return n[0];
    }).then(t);
  }, e.prototype.last = function(t) {
    return this.reverse().first(t);
  }, e.prototype.filter = function(t) {
    return Wy(this._ctx, function(n) {
      return t(n.value);
    }), gB(this._ctx, t), this;
  }, e.prototype.and = function(t) {
    return this.filter(t);
  }, e.prototype.or = function(t) {
    return new this.db.WhereClause(this._ctx.table, t, this);
  }, e.prototype.reverse = function() {
    return this._ctx.dir = this._ctx.dir === "prev" ? "next" : "prev", this._ondirectionchange && this._ondirectionchange(this._ctx.dir), this;
  }, e.prototype.desc = function() {
    return this.reverse();
  }, e.prototype.eachKey = function(t) {
    var n = this._ctx;
    return n.keysOnly = !n.isMatch, this.each(function(r, s) {
      t(s.key, s);
    });
  }, e.prototype.eachUniqueKey = function(t) {
    return this._ctx.unique = "unique", this.eachKey(t);
  }, e.prototype.eachPrimaryKey = function(t) {
    var n = this._ctx;
    return n.keysOnly = !n.isMatch, this.each(function(r, s) {
      t(s.primaryKey, s);
    });
  }, e.prototype.keys = function(t) {
    var n = this._ctx;
    n.keysOnly = !n.isMatch;
    var r = [];
    return this.each(function(s, a) {
      r.push(a.key);
    }).then(function() {
      return r;
    }).then(t);
  }, e.prototype.primaryKeys = function(t) {
    var n = this._ctx;
    if (n.dir === "next" && jl(n, !0) && n.limit > 0) return this._read(function(s) {
      var a = zp(n, n.table.core.schema);
      return n.table.core.query({
        trans: s,
        values: !1,
        limit: n.limit,
        query: {
          index: a,
          range: n.range
        }
      });
    }).then(function(s) {
      return s.result;
    }).then(t);
    n.keysOnly = !n.isMatch;
    var r = [];
    return this.each(function(s, a) {
      r.push(a.primaryKey);
    }).then(function() {
      return r;
    }).then(t);
  }, e.prototype.uniqueKeys = function(t) {
    return this._ctx.unique = "unique", this.keys(t);
  }, e.prototype.firstKey = function(t) {
    return this.limit(1).keys(function(n) {
      return n[0];
    }).then(t);
  }, e.prototype.lastKey = function(t) {
    return this.reverse().firstKey(t);
  }, e.prototype.distinct = function() {
    var t = this._ctx, n = t.index && t.table.schema.idxByName[t.index];
    if (!n || !n.multi) return this;
    var r = {};
    return Wy(this._ctx, function(s) {
      var a = s.primaryKey.toString(), o = Zr(r, a);
      return r[a] = !0, !o;
    }), this;
  }, e.prototype.modify = function(t) {
    var n = this, r = this._ctx;
    return this._write(function(s) {
      var a;
      if (typeof t == "function") a = t;
      else {
        var o = dr(t), l = o.length;
        a = function(E) {
          for (var N = !1, $ = 0; $ < l; ++$) {
            var p = o[$], w = t[p], _ = Ao(E, p);
            w instanceof vB ? (ws(E, p, w.execute(_)), N = !0) : _ !== w && (ws(E, p, w), N = !0);
          }
          return N;
        };
      }
      var i = r.table.core, u = i.schema.primaryKey, d = u.outbound, f = u.extractKey, m = 200, v = n.db._options.modifyChunkSize;
      v && (typeof v == "object" ? m = v[i.name] || v["*"] || 200 : m = v);
      var y = [], S = 0, x = [], I = function(E, N) {
        var $ = N.failures, p = N.numFailures;
        S += E - p;
        for (var w = 0, _ = dr($); w < _.length; w++) {
          var b = _[w];
          y.push($[b]);
        }
      };
      return n.clone().primaryKeys().then(function(E) {
        var N = jl(r) && r.limit === 1 / 0 && (typeof t != "function" || t === Vy) && {
          index: r.index,
          range: r.range
        }, $ = function(p) {
          var w = Math.min(m, E.length - p);
          return i.getMany({
            trans: s,
            keys: E.slice(p, p + w),
            cache: "immutable"
          }).then(function(_) {
            for (var b = [], C = [], k = d ? [] : null, P = [], H = 0; H < w; ++H) {
              var G = _[H], ee = {
                value: Tl(G),
                primKey: E[p + H]
              };
              a.call(ee, ee.value, ee) !== !1 && (ee.value == null ? P.push(E[p + H]) : !d && Jt(f(G), f(ee.value)) !== 0 ? (P.push(E[p + H]), b.push(ee.value)) : (C.push(ee.value), d && k.push(E[p + H])));
            }
            return Promise.resolve(b.length > 0 && i.mutate({
              trans: s,
              type: "add",
              values: b
            }).then(function(J) {
              for (var z in J.failures) P.splice(parseInt(z), 1);
              I(b.length, J);
            })).then(function() {
              return (C.length > 0 || N && typeof t == "object") && i.mutate({
                trans: s,
                type: "put",
                keys: k,
                values: C,
                criteria: N,
                changeSpec: typeof t != "function" && t,
                isAdditionalChunk: p > 0
              }).then(function(J) {
                return I(C.length, J);
              });
            }).then(function() {
              return (P.length > 0 || N && t === Vy) && i.mutate({
                trans: s,
                type: "delete",
                keys: P,
                criteria: N,
                isAdditionalChunk: p > 0
              }).then(function(J) {
                return I(P.length, J);
              });
            }).then(function() {
              return E.length > p + w && $(p + m);
            });
          });
        };
        return $(0).then(function() {
          if (y.length > 0) throw new xm("Error modifying one or more objects", y, S, x);
          return E.length;
        });
      });
    });
  }, e.prototype.delete = function() {
    var t = this._ctx, n = t.range;
    return jl(t) && (t.isPrimKey || n.type === 3) ? this._write(function(r) {
      var s = t.table.core.schema.primaryKey, a = n;
      return t.table.core.count({
        trans: r,
        query: {
          index: s,
          range: a
        }
      }).then(function(o) {
        return t.table.core.mutate({
          trans: r,
          type: "deleteRange",
          range: a
        }).then(function(l) {
          var i = l.failures;
          l.lastResult, l.results;
          var u = l.numFailures;
          if (u) throw new xm("Could not delete some values", Object.keys(i).map(function(d) {
            return i[d];
          }), o - u);
          return o - u;
        });
      });
    }) : this.modify(Vy);
  }, e;
})(), Vy = function(e, t) {
  return t.value = null;
};
function _B(e) {
  return Ff(yB.prototype, function(n, r) {
    this.db = e;
    var s = MM, a = null;
    if (r) try {
      s = r();
    } catch (u) {
      a = u;
    }
    var o = n._ctx, l = o.table, i = l.hook.reading.fire;
    this._ctx = {
      table: l,
      index: o.index,
      isPrimKey: !o.index || l.schema.primKey.keyPath && o.index === l.schema.primKey.name,
      range: s,
      keysOnly: !1,
      dir: "next",
      unique: "",
      algorithm: null,
      filter: null,
      replayFilter: null,
      justLimit: !0,
      isMatch: null,
      offset: 0,
      limit: 1 / 0,
      error: a,
      or: o.or,
      valueMapper: i !== Df ? i : null
    };
  });
}
function bB(e, t) {
  return e < t ? -1 : e === t ? 0 : 1;
}
function wB(e, t) {
  return e > t ? -1 : e === t ? 0 : 1;
}
function ls(e, t, n) {
  var r = e instanceof NM ? new e.Collection(e) : e;
  return r._ctx.error = n ? new n(t) : new TypeError(t), r;
}
function Ql(e) {
  return new e.Collection(e, function() {
    return RM("");
  }).limit(0);
}
function SB(e) {
  return e === "next" ? function(t) {
    return t.toUpperCase();
  } : function(t) {
    return t.toLowerCase();
  };
}
function AB(e) {
  return e === "next" ? function(t) {
    return t.toLowerCase();
  } : function(t) {
    return t.toUpperCase();
  };
}
function TB(e, t, n, r, s, a) {
  for (var o = Math.min(e.length, r.length), l = -1, i = 0; i < o; ++i) {
    var u = t[i];
    if (u !== r[i])
      return s(e[i], n[i]) < 0 ? e.substr(0, i) + n[i] + n.substr(i + 1) : s(e[i], r[i]) < 0 ? e.substr(0, i) + r[i] + n.substr(i + 1) : l >= 0 ? e.substr(0, l) + t[l] + n.substr(l + 1) : null;
    s(e[i], u) < 0 && (l = i);
  }
  return o < r.length && a === "next" ? e + n.substr(e.length) : o < e.length && a === "prev" ? e.substr(0, n.length) : l < 0 ? null : e.substr(0, l) + r[l] + n.substr(l + 1);
}
function Hh(e, t, n, r) {
  var s, a, o, l, i, u, d, f = n.length;
  if (!n.every(function(S) {
    return typeof S == "string";
  })) return ls(e, kM);
  function m(S) {
    s = SB(S), a = AB(S), o = S === "next" ? bB : wB;
    var x = n.map(function(I) {
      return {
        lower: a(I),
        upper: s(I)
      };
    }).sort(function(I, E) {
      return o(I.lower, E.lower);
    });
    l = x.map(function(I) {
      return I.upper;
    }), i = x.map(function(I) {
      return I.lower;
    }), u = S, d = S === "next" ? "" : r;
  }
  m("next");
  var v = new e.Collection(e, function() {
    return Jo(l[0], i[f - 1] + r);
  });
  v._ondirectionchange = function(S) {
    m(S);
  };
  var y = 0;
  return v._addAlgorithm(function(S, x, I) {
    var E = S.key;
    if (typeof E != "string") return !1;
    var N = a(E);
    if (t(N, i, y)) return !0;
    for (var $ = null, p = y; p < f; ++p) {
      var w = TB(E, N, l[p], i[p], o, u);
      w === null && $ === null ? y = p + 1 : ($ === null || o($, w) > 0) && ($ = w);
    }
    return x($ !== null ? function() {
      S.continue($ + d);
    } : I), !1;
  }), v;
}
function Jo(e, t, n, r) {
  return {
    type: 2,
    lower: e,
    upper: t,
    lowerOpen: n,
    upperOpen: r
  };
}
function RM(e) {
  return {
    type: 1,
    lower: e,
    upper: e
  };
}
var NM = (function() {
  function e() {
  }
  return Object.defineProperty(e.prototype, "Collection", {
    get: function() {
      return this._ctx.table.db.Collection;
    },
    enumerable: !1,
    configurable: !0
  }), e.prototype.between = function(t, n, r, s) {
    r = r !== !1, s = s === !0;
    try {
      return this._cmp(t, n) > 0 || this._cmp(t, n) === 0 && (r || s) && !(r && s) ? Ql(this) : new this.Collection(this, function() {
        return Jo(t, n, !r, !s);
      });
    } catch {
      return ls(this, eo);
    }
  }, e.prototype.equals = function(t) {
    return t == null ? ls(this, eo) : new this.Collection(this, function() {
      return RM(t);
    });
  }, e.prototype.above = function(t) {
    return t == null ? ls(this, eo) : new this.Collection(this, function() {
      return Jo(t, void 0, !0);
    });
  }, e.prototype.aboveOrEqual = function(t) {
    return t == null ? ls(this, eo) : new this.Collection(this, function() {
      return Jo(t, void 0, !1);
    });
  }, e.prototype.below = function(t) {
    return t == null ? ls(this, eo) : new this.Collection(this, function() {
      return Jo(void 0, t, !1, !0);
    });
  }, e.prototype.belowOrEqual = function(t) {
    return t == null ? ls(this, eo) : new this.Collection(this, function() {
      return Jo(void 0, t);
    });
  }, e.prototype.startsWith = function(t) {
    return typeof t != "string" ? ls(this, kM) : this.between(t, t + ul, !0, !0);
  }, e.prototype.startsWithIgnoreCase = function(t) {
    return t === "" ? this.startsWith(t) : Hh(this, function(n, r) {
      return n.indexOf(r[0]) === 0;
    }, [t], ul);
  }, e.prototype.equalsIgnoreCase = function(t) {
    return Hh(this, function(n, r) {
      return n === r[0];
    }, [t], "");
  }, e.prototype.anyOfIgnoreCase = function() {
    var t = go.apply(cu, arguments);
    return t.length === 0 ? Ql(this) : Hh(this, function(n, r) {
      return r.indexOf(n) !== -1;
    }, t, "");
  }, e.prototype.startsWithAnyOfIgnoreCase = function() {
    var t = go.apply(cu, arguments);
    return t.length === 0 ? Ql(this) : Hh(this, function(n, r) {
      return r.some(function(s) {
        return n.indexOf(s) === 0;
      });
    }, t, ul);
  }, e.prototype.anyOf = function() {
    var t = this, n = go.apply(cu, arguments), r = this._cmp;
    try {
      n.sort(r);
    } catch {
      return ls(this, eo);
    }
    if (n.length === 0) return Ql(this);
    var s = new this.Collection(this, function() {
      return Jo(n[0], n[n.length - 1]);
    });
    s._ondirectionchange = function(o) {
      r = o === "next" ? t._ascending : t._descending, n.sort(r);
    };
    var a = 0;
    return s._addAlgorithm(function(o, l, i) {
      for (var u = o.key; r(u, n[a]) > 0; )
        if (++a, a === n.length)
          return l(i), !1;
      return r(u, n[a]) === 0 ? !0 : (l(function() {
        o.continue(n[a]);
      }), !1);
    }), s;
  }, e.prototype.notEqual = function(t) {
    return this.inAnyRange([[Ab, t], [t, this.db._maxKey]], {
      includeLowers: !1,
      includeUppers: !1
    });
  }, e.prototype.noneOf = function() {
    var t = go.apply(cu, arguments);
    if (t.length === 0) return new this.Collection(this);
    try {
      t.sort(this._ascending);
    } catch {
      return ls(this, eo);
    }
    var n = t.reduce(function(r, s) {
      return r ? r.concat([[r[r.length - 1][1], s]]) : [[Ab, s]];
    }, null);
    return n.push([t[t.length - 1], this.db._maxKey]), this.inAnyRange(n, {
      includeLowers: !1,
      includeUppers: !1
    });
  }, e.prototype.inAnyRange = function(t, n) {
    var r = this, s = this._cmp, a = this._ascending, o = this._descending, l = this._min, i = this._max;
    if (t.length === 0) return Ql(this);
    if (!t.every(function(p) {
      return p[0] !== void 0 && p[1] !== void 0 && a(p[0], p[1]) <= 0;
    })) return ls(this, "First argument to inAnyRange() must be an Array of two-value Arrays [lower,upper] where upper must not be lower than lower", yt.InvalidArgument);
    var u = !n || n.includeLowers !== !1, d = n && n.includeUppers === !0;
    function f(p, w) {
      for (var _ = 0, b = p.length; _ < b; ++_) {
        var C = p[_];
        if (s(w[0], C[1]) < 0 && s(w[1], C[0]) > 0) {
          C[0] = l(C[0], w[0]), C[1] = i(C[1], w[1]);
          break;
        }
      }
      return _ === b && p.push(w), p;
    }
    var m = a;
    function v(p, w) {
      return m(p[0], w[0]);
    }
    var y;
    try {
      y = t.reduce(f, []), y.sort(v);
    } catch {
      return ls(this, eo);
    }
    var S = 0, x = d ? function(p) {
      return a(p, y[S][1]) > 0;
    } : function(p) {
      return a(p, y[S][1]) >= 0;
    }, I = u ? function(p) {
      return o(p, y[S][0]) > 0;
    } : function(p) {
      return o(p, y[S][0]) >= 0;
    };
    function E(p) {
      return !x(p) && !I(p);
    }
    var N = x, $ = new this.Collection(this, function() {
      return Jo(y[0][0], y[y.length - 1][1], !u, !d);
    });
    return $._ondirectionchange = function(p) {
      p === "next" ? (N = x, m = a) : (N = I, m = o), y.sort(v);
    }, $._addAlgorithm(function(p, w, _) {
      for (var b = p.key; N(b); )
        if (++S, S === y.length)
          return w(_), !1;
      return E(b) ? !0 : (r._cmp(b, y[S][1]) === 0 || r._cmp(b, y[S][0]) === 0 || w(function() {
        m === a ? p.continue(y[S][0]) : p.continue(y[S][1]);
      }), !1);
    }), $;
  }, e.prototype.startsWithAnyOf = function() {
    var t = go.apply(cu, arguments);
    return t.every(function(n) {
      return typeof n == "string";
    }) ? t.length === 0 ? Ql(this) : this.inAnyRange(t.map(function(n) {
      return [n, n + ul];
    })) : ls(this, "startsWithAnyOf() only works with strings");
  }, e;
})();
function EB(e) {
  return Ff(NM.prototype, function(n, r, s) {
    if (this.db = e, this._ctx = {
      table: n,
      index: r === ":id" ? null : r,
      or: s
    }, this._cmp = this._ascending = Jt, this._descending = function(a, o) {
      return Jt(o, a);
    }, this._max = function(a, o) {
      return Jt(a, o) > 0 ? a : o;
    }, this._min = function(a, o) {
      return Jt(a, o) < 0 ? a : o;
    }, this._IDBKeyRange = e._deps.IDBKeyRange, !this._IDBKeyRange) throw new yt.MissingAPI();
  });
}
function Ca(e) {
  return An(function(t) {
    return mf(t), e(t.target.error), !1;
  });
}
function mf(e) {
  e.stopPropagation && e.stopPropagation(), e.preventDefault && e.preventDefault();
}
var Bf = "storagemutated", Tb = "x-storagemutated-1", _i = Of(null, Bf), CB = (function() {
  function e() {
  }
  return e.prototype._lock = function() {
    return dd(!mt.global), ++this._reculock, this._reculock === 1 && !mt.global && (mt.lockOwnerFor = this), this;
  }, e.prototype._unlock = function() {
    if (dd(!mt.global), --this._reculock === 0)
      for (mt.global || (mt.lockOwnerFor = null); this._blockedFuncs.length > 0 && !this._locked(); ) {
        var t = this._blockedFuncs.shift();
        try {
          Cl(t[1], t[0]);
        } catch {
        }
      }
    return this;
  }, e.prototype._locked = function() {
    return this._reculock && mt.lockOwnerFor !== this;
  }, e.prototype.create = function(t) {
    var n = this;
    if (!this.mode) return this;
    var r = this.db.idbdb, s = this.db._state.dbOpenError;
    if (dd(!this.idbtrans), !t && !r) switch (s && s.name) {
      case "DatabaseClosedError":
        throw new yt.DatabaseClosed(s);
      case "MissingAPIError":
        throw new yt.MissingAPI(s.message, s);
      default:
        throw new yt.OpenFailed(s);
    }
    if (!this.active) throw new yt.TransactionInactive();
    return dd(this._completion._state === null), t = this.idbtrans = t || (this.db.core ? this.db.core.transaction(this.storeNames, this.mode, { durability: this.chromeTransactionDurability }) : r.transaction(this.storeNames, this.mode, { durability: this.chromeTransactionDurability })), t.onerror = An(function(a) {
      mf(a), n._reject(t.error);
    }), t.onabort = An(function(a) {
      mf(a), n.active && n._reject(new yt.Abort(t.error)), n.active = !1, n.on("abort").fire(a);
    }), t.oncomplete = An(function() {
      n.active = !1, n._resolve(), "mutatedParts" in t && _i.storagemutated.fire(t.mutatedParts);
    }), this;
  }, e.prototype._promise = function(t, n, r) {
    var s = this;
    if (t === "readwrite" && this.mode !== "readwrite") return Ln(new yt.ReadOnly("Transaction is readonly"));
    if (!this.active) return Ln(new yt.TransactionInactive());
    if (this._locked()) return new et(function(o, l) {
      s._blockedFuncs.push([function() {
        s._promise(t, n, r).then(o, l);
      }, mt]);
    });
    if (r) return gi(function() {
      var o = new et(function(l, i) {
        s._lock();
        var u = n(l, i, s);
        u && u.then && u.then(l, i);
      });
      return o.finally(function() {
        return s._unlock();
      }), o._lib = !0, o;
    });
    var a = new et(function(o, l) {
      var i = n(o, l, s);
      i && i.then && i.then(o, l);
    });
    return a._lib = !0, a;
  }, e.prototype._root = function() {
    return this.parent ? this.parent._root() : this;
  }, e.prototype.waitFor = function(t) {
    var n = this._root(), r = et.resolve(t);
    if (n._waitingFor) n._waitingFor = n._waitingFor.then(function() {
      return r;
    });
    else {
      n._waitingFor = r, n._waitingQueue = [];
      var s = n.idbtrans.objectStore(n.storeNames[0]);
      (function o() {
        for (++n._spinCount; n._waitingQueue.length; ) n._waitingQueue.shift()();
        n._waitingFor && (s.get(-1 / 0).onsuccess = o);
      })();
    }
    var a = n._waitingFor;
    return new et(function(o, l) {
      r.then(function(i) {
        return n._waitingQueue.push(An(o.bind(null, i)));
      }, function(i) {
        return n._waitingQueue.push(An(l.bind(null, i)));
      }).finally(function() {
        n._waitingFor === a && (n._waitingFor = null);
      });
    });
  }, e.prototype.abort = function() {
    this.active && (this.active = !1, this.idbtrans && this.idbtrans.abort(), this._reject(new yt.Abort()));
  }, e.prototype.table = function(t) {
    var n = this._memoizedTables || (this._memoizedTables = {});
    if (Zr(n, t)) return n[t];
    var r = this.schema[t];
    if (!r) throw new yt.NotFound("Table " + t + " not part of transaction");
    var s = new this.db.Table(t, r, this);
    return s.core = this.db.core.table(t), n[t] = s, s;
  }, e;
})();
function IB(e) {
  return Ff(CB.prototype, function(n, r, s, a, o) {
    var l = this;
    this.db = e, this.mode = n, this.storeNames = r, this.schema = s, this.chromeTransactionDurability = a, this.idbtrans = null, this.on = Of(this, "complete", "error", "abort"), this.parent = o || null, this.active = !0, this._reculock = 0, this._blockedFuncs = [], this._resolve = null, this._reject = null, this._waitingFor = null, this._waitingQueue = null, this._spinCount = 0, this._completion = new et(function(i, u) {
      l._resolve = i, l._reject = u;
    }), this._completion.then(function() {
      l.active = !1, l.on.complete.fire();
    }, function(i) {
      var u = l.active;
      return l.active = !1, l.on.error.fire(i), l.parent ? l.parent._reject(i) : u && l.idbtrans && l.idbtrans.abort(), Ln(i);
    });
  });
}
function Eb(e, t, n, r, s, a, o) {
  return {
    name: e,
    keyPath: t,
    unique: n,
    multi: r,
    auto: s,
    compound: a,
    src: (n && !o ? "&" : "") + (r ? "*" : "") + (s ? "++" : "") + $M(t)
  };
}
function $M(e) {
  return typeof e == "string" ? e : e ? "[" + [].join.call(e, "+") + "]" : "";
}
function Q1(e, t, n) {
  return {
    name: e,
    primKey: t,
    indexes: n,
    mappedClass: null,
    idxByName: FF(n, function(r) {
      return [r.name, r];
    })
  };
}
function xB(e) {
  return e.length === 1 ? e[0] : e;
}
var gf = function(e) {
  try {
    return e.only([[]]), gf = function() {
      return [[]];
    }, [[]];
  } catch {
    return gf = function() {
      return ul;
    }, ul;
  }
};
function Cb(e) {
  return e == null ? function() {
  } : typeof e == "string" ? kB(e) : function(t) {
    return Ao(t, e);
  };
}
function kB(e) {
  return e.split(".").length === 1 ? function(t) {
    return t[e];
  } : function(t) {
    return Ao(t, e);
  };
}
function dT(e) {
  return [].slice.call(e);
}
var MB = 0;
function qd(e) {
  return e == null ? ":id" : typeof e == "string" ? e : "[".concat(e.join("+"), "]");
}
function PB(e, t, n) {
  function r(f, m) {
    var v = dT(f.objectStoreNames);
    return {
      schema: {
        name: f.name,
        tables: v.map(function(y) {
          return m.objectStore(y);
        }).map(function(y) {
          var S = y.keyPath, x = y.autoIncrement, I = In(S), E = S == null, N = {}, $ = {
            name: y.name,
            primaryKey: {
              name: null,
              isPrimaryKey: !0,
              outbound: E,
              compound: I,
              keyPath: S,
              autoIncrement: x,
              unique: !0,
              extractKey: Cb(S)
            },
            indexes: dT(y.indexNames).map(function(p) {
              return y.index(p);
            }).map(function(p) {
              var w = p.name, _ = p.unique, b = p.multiEntry, C = p.keyPath, k = {
                name: w,
                compound: In(C),
                keyPath: C,
                unique: _,
                multiEntry: b,
                extractKey: Cb(C)
              };
              return N[qd(C)] = k, k;
            }),
            getIndexByKeyPath: function(p) {
              return N[qd(p)];
            }
          };
          return N[":id"] = $.primaryKey, S != null && (N[qd(S)] = $.primaryKey), $;
        })
      },
      hasGetAll: v.length > 0 && "getAll" in m.objectStore(v[0]) && !(typeof navigator < "u" && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604)
    };
  }
  function s(f) {
    if (f.type === 3) return null;
    if (f.type === 4) throw new Error("Cannot convert never type to IDBKeyRange");
    var m = f.lower, v = f.upper, y = f.lowerOpen, S = f.upperOpen;
    return m === void 0 ? v === void 0 ? null : t.upperBound(v, !!S) : v === void 0 ? t.lowerBound(m, !!y) : t.bound(m, v, !!y, !!S);
  }
  function a(f) {
    var m = f.name;
    function v(x) {
      var I = x.trans, E = x.type, N = x.keys, $ = x.values, p = x.range;
      return new Promise(function(w, _) {
        w = An(w);
        var b = I.objectStore(m), C = b.keyPath == null, k = E === "put" || E === "add";
        if (!k && E !== "delete" && E !== "deleteRange") throw new Error("Invalid operation type: " + E);
        var P = (N || $ || { length: 1 }).length;
        if (N && $ && N.length !== $.length) throw new Error("Given keys array must have same length as given values array.");
        if (P === 0) return w({
          numFailures: 0,
          failures: {},
          results: [],
          lastResult: void 0
        });
        var H, G = [], ee = [], J = 0, z = function(be) {
          ++J, mf(be);
        };
        if (E === "deleteRange") {
          if (p.type === 4) return w({
            numFailures: J,
            failures: ee,
            results: [],
            lastResult: void 0
          });
          p.type === 3 ? G.push(H = b.clear()) : G.push(H = b.delete(s(p)));
        } else {
          var R = k ? C ? [$, N] : [$, null] : [N, null], V = R[0], le = R[1];
          if (k) for (var ve = 0; ve < P; ++ve)
            G.push(H = le && le[ve] !== void 0 ? b[E](V[ve], le[ve]) : b[E](V[ve])), H.onerror = z;
          else for (var ve = 0; ve < P; ++ve)
            G.push(H = b[E](V[ve])), H.onerror = z;
        }
        var ke = function(be) {
          var Ne = be.target.result;
          G.forEach(function(ce, we) {
            return ce.error != null && (ee[we] = ce.error);
          }), w({
            numFailures: J,
            failures: ee,
            results: E === "delete" ? N : G.map(function(ce) {
              return ce.result;
            }),
            lastResult: Ne
          });
        };
        H.onerror = function(be) {
          z(be), ke(be);
        }, H.onsuccess = ke;
      });
    }
    function y(x) {
      var I = x.trans, E = x.values, N = x.query, $ = x.reverse, p = x.unique;
      return new Promise(function(w, _) {
        w = An(w);
        var b = N.index, C = N.range, k = I.objectStore(m), P = b.isPrimaryKey ? k : k.index(b.name), H = $ ? p ? "prevunique" : "prev" : p ? "nextunique" : "next", G = E || !("openKeyCursor" in P) ? P.openCursor(s(C), H) : P.openKeyCursor(s(C), H);
        G.onerror = Ca(_), G.onsuccess = An(function(ee) {
          var J = G.result;
          if (!J) {
            w(null);
            return;
          }
          J.___id = ++MB, J.done = !1;
          var z = J.continue.bind(J), R = J.continuePrimaryKey;
          R && (R = R.bind(J));
          var V = J.advance.bind(J), le = function() {
            throw new Error("Cursor not started");
          }, ve = function() {
            throw new Error("Cursor not stopped");
          };
          J.trans = I, J.stop = J.continue = J.continuePrimaryKey = J.advance = le, J.fail = An(_), J.next = function() {
            var ke = this, be = 1;
            return this.start(function() {
              return be-- ? ke.continue() : ke.stop();
            }).then(function() {
              return ke;
            });
          }, J.start = function(ke) {
            var be = new Promise(function(ce, we) {
              ce = An(ce), G.onerror = Ca(we), J.fail = we, J.stop = function(Oe) {
                J.stop = J.continue = J.continuePrimaryKey = J.advance = ve, ce(Oe);
              };
            }), Ne = function() {
              if (G.result) try {
                ke();
              } catch (ce) {
                J.fail(ce);
              }
              else
                J.done = !0, J.start = function() {
                  throw new Error("Cursor behind last entry");
                }, J.stop();
            };
            return G.onsuccess = An(function(ce) {
              G.onsuccess = Ne, Ne();
            }), J.continue = z, J.continuePrimaryKey = R, J.advance = V, Ne(), be;
          }, w(J);
        }, _);
      });
    }
    function S(x) {
      return function(I) {
        return new Promise(function(E, N) {
          E = An(E);
          var $ = I.trans, p = I.values, w = I.limit, _ = I.query, b = w === 1 / 0 ? void 0 : w, C = _.index, k = _.range, P = $.objectStore(m), H = C.isPrimaryKey ? P : P.index(C.name), G = s(k);
          if (w === 0) return E({ result: [] });
          if (x) {
            var ee = p ? H.getAll(G, b) : H.getAllKeys(G, b);
            ee.onsuccess = function(V) {
              return E({ result: V.target.result });
            }, ee.onerror = Ca(N);
          } else {
            var J = 0, z = p || !("openKeyCursor" in H) ? H.openCursor(G) : H.openKeyCursor(G), R = [];
            z.onsuccess = function(V) {
              var le = z.result;
              if (!le) return E({ result: R });
              if (R.push(p ? le.value : le.primaryKey), ++J === w) return E({ result: R });
              le.continue();
            }, z.onerror = Ca(N);
          }
        });
      };
    }
    return {
      name: m,
      schema: f,
      mutate: v,
      getMany: function(x) {
        var I = x.trans, E = x.keys;
        return new Promise(function(N, $) {
          N = An(N);
          for (var p = I.objectStore(m), w = E.length, _ = new Array(w), b = 0, C = 0, k, P = function(ee) {
            var J = ee.target;
            (_[J._pos] = J.result) != null, ++C === b && N(_);
          }, H = Ca($), G = 0; G < w; ++G) E[G] != null && (k = p.get(E[G]), k._pos = G, k.onsuccess = P, k.onerror = H, ++b);
          b === 0 && N(_);
        });
      },
      get: function(x) {
        var I = x.trans, E = x.key;
        return new Promise(function(N, $) {
          N = An(N);
          var p = I.objectStore(m).get(E);
          p.onsuccess = function(w) {
            return N(w.target.result);
          }, p.onerror = Ca($);
        });
      },
      query: S(i),
      openCursor: y,
      count: function(x) {
        var I = x.query, E = x.trans, N = I.index, $ = I.range;
        return new Promise(function(p, w) {
          var _ = E.objectStore(m), b = N.isPrimaryKey ? _ : _.index(N.name), C = s($), k = C ? b.count(C) : b.count();
          k.onsuccess = An(function(P) {
            return p(P.target.result);
          }), k.onerror = Ca(w);
        });
      }
    };
  }
  var o = r(e, n), l = o.schema, i = o.hasGetAll, u = l.tables.map(function(f) {
    return a(f);
  }), d = {};
  return u.forEach(function(f) {
    return d[f.name] = f;
  }), {
    stack: "dbcore",
    transaction: e.transaction.bind(e),
    table: function(f) {
      if (!d[f]) throw new Error("Table '".concat(f, "' not found"));
      return d[f];
    },
    MIN_KEY: -1 / 0,
    MAX_KEY: gf(t),
    schema: l
  };
}
function RB(e, t) {
  return t.reduce(function(n, r) {
    var s = r.create;
    return Rt(Rt({}, n), s(n));
  }, e);
}
function NB(e, t, n, r) {
  var s = n.IDBKeyRange;
  return n.indexedDB, { dbcore: RB(PB(t, s, r), e.dbcore) };
}
function Rm(e, t) {
  var n = t.db;
  e.core = NB(e._middlewares, n, e._deps, t).dbcore, e.tables.forEach(function(r) {
    var s = r.name;
    e.core.schema.tables.some(function(a) {
      return a.name === s;
    }) && (r.core = e.core.table(s), e[s] instanceof e.Table && (e[s].core = r.core));
  });
}
function Nm(e, t, n, r) {
  n.forEach(function(s) {
    var a = r[s];
    t.forEach(function(o) {
      var l = pM(o, s);
      (!l || "value" in l && l.value === void 0) && (o === e.Transaction.prototype || o instanceof e.Transaction ? mi(o, s, {
        get: function() {
          return this.table(s);
        },
        set: function(i) {
          hM(this, s, {
            value: i,
            writable: !0,
            configurable: !0,
            enumerable: !0
          });
        }
      }) : o[s] = new e.Table(s, a));
    });
  });
}
function Ib(e, t) {
  t.forEach(function(n) {
    for (var r in n) n[r] instanceof e.Table && delete n[r];
  });
}
function $B(e, t) {
  return e._cfg.version - t._cfg.version;
}
function LB(e, t, n, r) {
  var s = e._dbSchema;
  n.objectStoreNames.contains("$meta") && !s.$meta && (s.$meta = Q1("$meta", DM("")[0], []), e._storeNames.push("$meta"));
  var a = e._createTransaction("readwrite", e._storeNames, s);
  a.create(n), a._completion.catch(r);
  var o = a._reject.bind(a), l = mt.transless || mt;
  gi(function() {
    if (mt.trans = a, mt.transless = l, t === 0)
      dr(s).forEach(function(i) {
        ew(n, i, s[i].primKey, s[i].indexes);
      }), Rm(e, n), et.follow(function() {
        return e.on.populate.fire(a);
      }).catch(o);
    else
      return Rm(e, n), OB(e, a, t).then(function(i) {
        return FB(e, i, a, n);
      }).catch(o);
  });
}
function DB(e, t) {
  LM(e._dbSchema, t), t.db.version % 10 === 0 && !t.objectStoreNames.contains("$meta") && t.db.createObjectStore("$meta").add(Math.ceil(t.db.version / 10 - 1), "version");
  var n = Yg(e, e.idbdb, t);
  Lm(e, e._dbSchema, t);
  for (var r = Z1(n, e._dbSchema), s = function(u) {
    if (u.change.length || u.recreate)
      return console.warn("Unable to patch indexes of table ".concat(u.name, " because it has changes on the type of index or primary key.")), { value: void 0 };
    var d = t.objectStore(u.name);
    u.add.forEach(function(f) {
      Ua && console.debug("Dexie upgrade patch: Creating missing index ".concat(u.name, ".").concat(f.src)), $m(d, f);
    });
  }, a = 0, o = r.change; a < o.length; a++) {
    var l = o[a], i = s(l);
    if (typeof i == "object") return i.value;
  }
}
function OB(e, t, n) {
  return t.storeNames.includes("$meta") ? t.table("$meta").get("version").then(function(r) {
    return r ?? n;
  }) : et.resolve(n);
}
function FB(e, t, n, r) {
  var s = [], a = e._versions, o = e._dbSchema = Yg(e, e.idbdb, r), l = a.filter(function(u) {
    return u._cfg.version >= t;
  });
  if (l.length === 0) return et.resolve();
  l.forEach(function(u) {
    s.push(function() {
      var d = o, f = u._cfg.dbschema;
      Lm(e, d, r), Lm(e, f, r), o = e._dbSchema = f;
      var m = Z1(d, f);
      m.add.forEach(function(E) {
        ew(r, E[0], E[1].primKey, E[1].indexes);
      }), m.change.forEach(function(E) {
        if (E.recreate) throw new yt.Upgrade("Not yet support for changing primary key");
        var N = r.objectStore(E.name);
        E.add.forEach(function($) {
          return $m(N, $);
        }), E.change.forEach(function($) {
          N.deleteIndex($.name), $m(N, $);
        }), E.del.forEach(function($) {
          return N.deleteIndex($);
        });
      });
      var v = u._cfg.contentUpgrade;
      if (v && u._cfg.version > t) {
        Rm(e, r), n._memoizedTables = {};
        var y = vM(f);
        m.del.forEach(function(E) {
          y[E] = d[E];
        }), Ib(e, [e.Transaction.prototype]), Nm(e, [e.Transaction.prototype], dr(y), y), n.schema = y;
        var S = K1(v);
        S && uc();
        var x, I = et.follow(function() {
          if (x = v(n), x && S) {
            var E = vi.bind(null, null);
            x.then(E, E);
          }
        });
        return x && typeof x.then == "function" ? et.resolve(x) : I.then(function() {
          return x;
        });
      }
    }), s.push(function(d) {
      var f = u._cfg.dbschema;
      BB(f, d), Ib(e, [e.Transaction.prototype]), Nm(e, [e.Transaction.prototype], e._storeNames, e._dbSchema), n.schema = e._dbSchema;
    }), s.push(function(d) {
      e.idbdb.objectStoreNames.contains("$meta") && (Math.ceil(e.idbdb.version / 10) === u._cfg.version ? (e.idbdb.deleteObjectStore("$meta"), delete e._dbSchema.$meta, e._storeNames = e._storeNames.filter(function(f) {
        return f !== "$meta";
      })) : d.objectStore("$meta").put(u._cfg.version, "version"));
    });
  });
  function i() {
    return s.length ? et.resolve(s.shift()(n.idbtrans)).then(i) : et.resolve();
  }
  return i().then(function() {
    LM(o, r);
  });
}
function Z1(e, t) {
  var n = {
    del: [],
    add: [],
    change: []
  }, r;
  for (r in e) t[r] || n.del.push(r);
  for (r in t) {
    var s = e[r], a = t[r];
    if (!s) n.add.push([r, a]);
    else {
      var o = {
        name: r,
        def: a,
        recreate: !1,
        del: [],
        add: [],
        change: []
      };
      if ("" + (s.primKey.keyPath || "") != "" + (a.primKey.keyPath || "") || s.primKey.auto !== a.primKey.auto)
        o.recreate = !0, n.change.push(o);
      else {
        var l = s.idxByName, i = a.idxByName, u = void 0;
        for (u in l) i[u] || o.del.push(u);
        for (u in i) {
          var d = l[u], f = i[u];
          d ? d.src !== f.src && o.change.push(f) : o.add.push(f);
        }
        (o.del.length > 0 || o.add.length > 0 || o.change.length > 0) && n.change.push(o);
      }
    }
  }
  return n;
}
function ew(e, t, n, r) {
  var s = e.db.createObjectStore(t, n.keyPath ? {
    keyPath: n.keyPath,
    autoIncrement: n.auto
  } : { autoIncrement: n.auto });
  return r.forEach(function(a) {
    return $m(s, a);
  }), s;
}
function LM(e, t) {
  dr(e).forEach(function(n) {
    t.db.objectStoreNames.contains(n) || (Ua && console.debug("Dexie: Creating missing table", n), ew(t, n, e[n].primKey, e[n].indexes));
  });
}
function BB(e, t) {
  [].slice.call(t.db.objectStoreNames).forEach(function(n) {
    return e[n] == null && t.db.deleteObjectStore(n);
  });
}
function $m(e, t) {
  e.createIndex(t.name, t.keyPath, {
    unique: t.unique,
    multiEntry: t.multi
  });
}
function Yg(e, t, n) {
  var r = {};
  return Gg(t.objectStoreNames, 0).forEach(function(s) {
    for (var a = n.objectStore(s), o = a.keyPath, l = Eb($M(o), o || "", !0, !1, !!a.autoIncrement, o && typeof o != "string", !0), i = [], u = 0; u < a.indexNames.length; ++u) {
      var d = a.index(a.indexNames[u]);
      o = d.keyPath;
      var f = Eb(d.name, o, !!d.unique, !!d.multiEntry, !1, o && typeof o != "string", !1);
      i.push(f);
    }
    r[s] = Q1(s, l, i);
  }), r;
}
function UB(e, t, n) {
  e.verno = t.version / 10;
  var r = e._dbSchema = Yg(e, t, n);
  e._storeNames = Gg(t.objectStoreNames, 0), Nm(e, [e._allTables], dr(r), r);
}
function qB(e, t) {
  var n = Z1(Yg(e, e.idbdb, t), e._dbSchema);
  return !(n.add.length || n.change.some(function(r) {
    return r.add.length || r.change.length;
  }));
}
function Lm(e, t, n) {
  for (var r = n.db.objectStoreNames, s = 0; s < r.length; ++s) {
    var a = r[s], o = n.objectStore(a);
    e._hasGetAll = "getAll" in o;
    for (var l = 0; l < o.indexNames.length; ++l) {
      var i = o.indexNames[l], u = o.index(i).keyPath, d = typeof u == "string" ? u : "[" + Gg(u).join("+") + "]";
      if (t[a]) {
        var f = t[a].idxByName[d];
        f && (f.name = i, delete t[a].idxByName[d], t[a].idxByName[i] = f);
      }
    }
  }
  typeof navigator < "u" && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && gr.WorkerGlobalScope && gr instanceof gr.WorkerGlobalScope && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604 && (e._hasGetAll = !1);
}
function DM(e) {
  return e.split(",").map(function(t, n) {
    t = t.trim();
    var r = t.replace(/([&*]|\+\+)/g, ""), s = /^\[/.test(r) ? r.match(/^\[(.*)\]$/)[1].split("+") : r;
    return Eb(r, s || null, /\&/.test(t), /\*/.test(t), /\+\+/.test(t), In(s), n === 0);
  });
}
var HB = (function() {
  function e() {
  }
  return e.prototype._parseStoresSpec = function(t, n) {
    dr(t).forEach(function(r) {
      if (t[r] !== null) {
        var s = DM(t[r]), a = s.shift();
        if (a.unique = !0, a.multi) throw new yt.Schema("Primary key cannot be multi-valued");
        s.forEach(function(o) {
          if (o.auto) throw new yt.Schema("Only primary key can be marked as autoIncrement (++)");
          if (!o.keyPath) throw new yt.Schema("Index must have a name and cannot be an empty string");
        }), n[r] = Q1(r, a, s);
      }
    });
  }, e.prototype.stores = function(t) {
    var n = this.db;
    this._cfg.storesSource = this._cfg.storesSource ? Es(this._cfg.storesSource, t) : t;
    var r = n._versions, s = {}, a = {};
    return r.forEach(function(o) {
      Es(s, o._cfg.storesSource), a = o._cfg.dbschema = {}, o._parseStoresSpec(s, a);
    }), n._dbSchema = a, Ib(n, [
      n._allTables,
      n,
      n.Transaction.prototype
    ]), Nm(n, [
      n._allTables,
      n,
      n.Transaction.prototype,
      this._cfg.tables
    ], dr(a), a), n._storeNames = dr(a), this;
  }, e.prototype.upgrade = function(t) {
    return this._cfg.contentUpgrade = V1(this._cfg.contentUpgrade || hn, t), this;
  }, e;
})();
function zB(e) {
  return Ff(HB.prototype, function(n) {
    this.db = e, this._cfg = {
      version: n,
      storesSource: null,
      dbschema: {},
      tables: {},
      contentUpgrade: null
    };
  });
}
function tw(e, t) {
  var n = e._dbNamesDB;
  return n || (n = e._dbNamesDB = new Gu(Jg, {
    addons: [],
    indexedDB: e,
    IDBKeyRange: t
  }), n.version(1).stores({ dbnames: "name" })), n.table("dbnames");
}
function nw(e) {
  return e && typeof e.databases == "function";
}
function KB(e) {
  var t = e.indexedDB, n = e.IDBKeyRange;
  return nw(t) ? Promise.resolve(t.databases()).then(function(r) {
    return r.map(function(s) {
      return s.name;
    }).filter(function(s) {
      return s !== Jg;
    });
  }) : tw(t, n).toCollection().primaryKeys();
}
function WB(e, t) {
  var n = e.indexedDB, r = e.IDBKeyRange;
  !nw(n) && t !== Jg && tw(n, r).put({ name: t }).catch(hn);
}
function GB(e, t) {
  var n = e.indexedDB, r = e.IDBKeyRange;
  !nw(n) && t !== Jg && tw(n, r).delete(t).catch(hn);
}
function xb(e) {
  return gi(function() {
    return mt.letThrough = !0, e();
  });
}
function VB() {
  if (!(!navigator.userAgentData && /Safari\//.test(navigator.userAgent) && !/Chrom(e|ium)\//.test(navigator.userAgent)) || !indexedDB.databases) return Promise.resolve();
  var e;
  return new Promise(function(t) {
    var n = function() {
      return indexedDB.databases().finally(t);
    };
    e = setInterval(n, 100), n();
  }).finally(function() {
    return clearInterval(e);
  });
}
var Jy;
function rw(e) {
  return !("from" in e);
}
var Ur = function(e, t) {
  if (this) Es(this, arguments.length ? {
    d: 1,
    from: e,
    to: arguments.length > 1 ? t : e
  } : { d: 0 });
  else {
    var n = new Ur();
    return e && "d" in e && Es(n, e), n;
  }
};
Wu(Ur.prototype, (Jy = {
  add: function(e) {
    return Dm(this, e), this;
  },
  addKey: function(e) {
    return vf(this, e, e), this;
  },
  addKeys: function(e) {
    var t = this;
    return e.forEach(function(n) {
      return vf(t, n, n);
    }), this;
  },
  hasKey: function(e) {
    var t = Om(this).next(e).value;
    return t && Jt(t.from, e) <= 0 && Jt(t.to, e) >= 0;
  }
}, Jy[vb] = function() {
  return Om(this);
}, Jy));
function vf(e, t, n) {
  var r = Jt(t, n);
  if (!isNaN(r)) {
    if (r > 0) throw RangeError();
    if (rw(e)) return Es(e, {
      from: t,
      to: n,
      d: 1
    });
    var s = e.l, a = e.r;
    if (Jt(n, e.from) < 0)
      return s ? vf(s, t, n) : e.l = {
        from: t,
        to: n,
        d: 1,
        l: null,
        r: null
      }, fT(e);
    if (Jt(t, e.to) > 0)
      return a ? vf(a, t, n) : e.r = {
        from: t,
        to: n,
        d: 1,
        l: null,
        r: null
      }, fT(e);
    Jt(t, e.from) < 0 && (e.from = t, e.l = null, e.d = a ? a.d + 1 : 1), Jt(n, e.to) > 0 && (e.to = n, e.r = null, e.d = e.l ? e.l.d + 1 : 1);
    var o = !e.r;
    s && !e.l && Dm(e, s), a && o && Dm(e, a);
  }
}
function Dm(e, t) {
  function n(r, s) {
    var a = s.from, o = s.to, l = s.l, i = s.r;
    vf(r, a, o), l && n(r, l), i && n(r, i);
  }
  rw(t) || n(e, t);
}
function JB(e, t) {
  var n = Om(t), r = n.next();
  if (r.done) return !1;
  for (var s = r.value, a = Om(e), o = a.next(s.from), l = o.value; !r.done && !o.done; ) {
    if (Jt(l.from, s.to) <= 0 && Jt(l.to, s.from) >= 0) return !0;
    Jt(s.from, l.from) < 0 ? s = (r = n.next(l.from)).value : l = (o = a.next(s.from)).value;
  }
  return !1;
}
function Om(e) {
  var t = rw(e) ? null : {
    s: 0,
    n: e
  };
  return { next: function(n) {
    for (var r = arguments.length > 0; t; ) switch (t.s) {
      case 0:
        if (t.s = 1, r) for (; t.n.l && Jt(n, t.n.from) < 0; ) t = {
          up: t,
          n: t.n.l,
          s: 1
        };
        else for (; t.n.l; ) t = {
          up: t,
          n: t.n.l,
          s: 1
        };
      case 1:
        if (t.s = 2, !r || Jt(n, t.n.to) <= 0) return {
          value: t.n,
          done: !1
        };
      case 2:
        if (t.n.r) {
          t.s = 3, t = {
            up: t,
            n: t.n.r,
            s: 0
          };
          continue;
        }
      case 3:
        t = t.up;
    }
    return { done: !0 };
  } };
}
function fT(e) {
  var t, n, r = (((t = e.r) === null || t === void 0 ? void 0 : t.d) || 0) - (((n = e.l) === null || n === void 0 ? void 0 : n.d) || 0), s = r > 1 ? "r" : r < -1 ? "l" : "";
  if (s) {
    var a = s === "r" ? "l" : "r", o = Rt({}, e), l = e[s];
    e.from = l.from, e.to = l.to, e[s] = l[s], o[s] = l[a], e[a] = o, o.d = hT(o);
  }
  e.d = hT(e);
}
function hT(e) {
  var t = e.r, n = e.l;
  return (t ? n ? Math.max(t.d, n.d) : t.d : n ? n.d : 0) + 1;
}
function Xg(e, t) {
  return dr(t).forEach(function(n) {
    e[n] ? Dm(e[n], t[n]) : e[n] = bM(t[n]);
  }), e;
}
function sw(e, t) {
  return e.all || t.all || Object.keys(e).some(function(n) {
    return t[n] && JB(t[n], e[n]);
  });
}
var vl = {}, Yy = {}, Xy = !1;
function zh(e, t) {
  Xg(Yy, e), Xy || (Xy = !0, setTimeout(function() {
    Xy = !1;
    var n = Yy;
    Yy = {}, aw(n, !1);
  }, 0));
}
function aw(e, t) {
  t === void 0 && (t = !1);
  var n = /* @__PURE__ */ new Set();
  if (e.all) for (var r = 0, s = Object.values(vl); r < s.length; r++) {
    var a = s[r];
    pT(a, e, n, t);
  }
  else for (var o in e) {
    var l = /^idb\:\/\/(.*)\/(.*)\//.exec(o);
    if (l) {
      var i = l[1], u = l[2], a = vl["idb://".concat(i, "/").concat(u)];
      a && pT(a, e, n, t);
    }
  }
  n.forEach(function(d) {
    return d();
  });
}
function pT(e, t, n, r) {
  for (var s = [], a = 0, o = Object.entries(e.queries.query); a < o.length; a++) {
    for (var l = o[a], i = l[0], u = l[1], d = [], f = 0, m = u; f < m.length; f++) {
      var v = m[f];
      sw(t, v.obsSet) ? v.subscribers.forEach(function(I) {
        return n.add(I);
      }) : r && d.push(v);
    }
    r && s.push([i, d]);
  }
  if (r) for (var y = 0, S = s; y < S.length; y++) {
    var x = S[y], i = x[0], d = x[1];
    e.queries.query[i] = d;
  }
}
function YB(e) {
  var t = e._state, n = e._deps.indexedDB;
  if (t.isBeingOpened || e.idbdb) return t.dbReadyPromise.then(function() {
    return t.dbOpenError ? Ln(t.dbOpenError) : e;
  });
  t.isBeingOpened = !0, t.dbOpenError = null, t.openComplete = !1;
  var r = t.openCanceller, s = Math.round(e.verno * 10), a = !1;
  function o() {
    if (t.openCanceller !== r) throw new yt.DatabaseClosed("db.open() was cancelled");
  }
  var l = t.dbReadyResolve, i = null, u = !1, d = function() {
    return new et(function(f, m) {
      if (o(), !n) throw new yt.MissingAPI();
      var v = e.name, y = t.autoSchema || !s ? n.open(v) : n.open(v, s);
      if (!y) throw new yt.MissingAPI();
      y.onerror = Ca(m), y.onblocked = An(e._fireOnBlocked), y.onupgradeneeded = An(function(S) {
        if (i = y.transaction, t.autoSchema && !e._options.allowEmptyDB) {
          y.onerror = mf, i.abort(), y.result.close();
          var x = n.deleteDatabase(v);
          x.onsuccess = x.onerror = An(function() {
            m(new yt.NoSuchDatabase("Database ".concat(v, " doesnt exist")));
          });
        } else {
          i.onerror = Ca(m);
          var I = S.oldVersion > Math.pow(2, 62) ? 0 : S.oldVersion;
          u = I < 1, e.idbdb = y.result, a && DB(e, i), LB(e, I / 10, i, m);
        }
      }, m), y.onsuccess = An(function() {
        i = null;
        var S = e.idbdb = y.result, x = Gg(S.objectStoreNames);
        if (x.length > 0) try {
          var I = S.transaction(xB(x), "readonly");
          if (t.autoSchema) UB(e, S, I);
          else if (Lm(e, e._dbSchema, I), !qB(e, I) && !a)
            return console.warn("Dexie SchemaDiff: Schema was extended without increasing the number passed to db.version(). Dexie will add missing parts and increment native version number to workaround this."), S.close(), s = S.version + 1, a = !0, f(d());
          Rm(e, I);
        } catch {
        }
        Pu.push(e), S.onversionchange = An(function(E) {
          t.vcFired = !0, e.on("versionchange").fire(E);
        }), S.onclose = An(function(E) {
          e.on("close").fire(E);
        }), u && WB(e._deps, v), f();
      }, m);
    }).catch(function(f) {
      switch (f?.name) {
        case "UnknownError":
          if (t.PR1398_maxLoop > 0)
            return t.PR1398_maxLoop--, console.warn("Dexie: Workaround for Chrome UnknownError on open()"), d();
          break;
        case "VersionError":
          if (s > 0)
            return s = 0, d();
          break;
      }
      return et.reject(f);
    });
  };
  return et.race([r, (typeof navigator > "u" ? et.resolve() : VB()).then(d)]).then(function() {
    return o(), t.onReadyBeingFired = [], et.resolve(xb(function() {
      return e.on.ready.fire(e.vip);
    })).then(function f() {
      if (t.onReadyBeingFired.length > 0) {
        var m = t.onReadyBeingFired.reduce(V1, hn);
        return t.onReadyBeingFired = [], et.resolve(xb(function() {
          return m(e.vip);
        })).then(f);
      }
    });
  }).finally(function() {
    t.openCanceller === r && (t.onReadyBeingFired = null, t.isBeingOpened = !1);
  }).catch(function(f) {
    t.dbOpenError = f;
    try {
      i && i.abort();
    } catch {
    }
    return r === t.openCanceller && e._close(), Ln(f);
  }).finally(function() {
    t.openComplete = !0, l();
  }).then(function() {
    if (u) {
      var f = {};
      e.tables.forEach(function(m) {
        m.schema.indexes.forEach(function(v) {
          v.name && (f["idb://".concat(e.name, "/").concat(m.name, "/").concat(v.name)] = new Ur(-1 / 0, [[[]]]));
        }), f["idb://".concat(e.name, "/").concat(m.name, "/")] = f["idb://".concat(e.name, "/").concat(m.name, "/:dels")] = new Ur(-1 / 0, [[[]]]);
      }), _i(Bf).fire(f), aw(f, !0);
    }
    return e;
  });
}
function kb(e) {
  var t = function(o) {
    return e.next(o);
  }, n = function(o) {
    return e.throw(o);
  }, r = a(t), s = a(n);
  function a(o) {
    return function(l) {
      var i = o(l), u = i.value;
      return i.done ? u : !u || typeof u.then != "function" ? In(u) ? Promise.all(u).then(r, s) : r(u) : u.then(r, s);
    };
  }
  return a(t)();
}
function XB(e, t, n) {
  var r = arguments.length;
  if (r < 2) throw new yt.InvalidArgument("Too few arguments");
  for (var s = new Array(r - 1); --r; ) s[r - 1] = arguments[r];
  return n = s.pop(), [
    e,
    yM(s),
    n
  ];
}
function OM(e, t, n, r, s) {
  return et.resolve().then(function() {
    var a = mt.transless || mt, o = e._createTransaction(t, n, e._dbSchema, r);
    o.explicit = !0;
    var l = {
      trans: o,
      transless: a
    };
    if (r) o.idbtrans = r.idbtrans;
    else try {
      o.create(), o.idbtrans._explicit = !0, e._state.PR1398_maxLoop = 3;
    } catch (f) {
      return f.name === G1.InvalidState && e.isOpen() && --e._state.PR1398_maxLoop > 0 ? (console.warn("Dexie: Need to reopen db"), e.close({ disableAutoOpen: !1 }), e.open().then(function() {
        return OM(e, t, n, null, s);
      })) : Ln(f);
    }
    var i = K1(s);
    i && uc();
    var u, d = et.follow(function() {
      if (u = s.call(o, o), u)
        if (i) {
          var f = vi.bind(null, null);
          u.then(f, f);
        } else typeof u.next == "function" && typeof u.throw == "function" && (u = kb(u));
    }, l);
    return (u && typeof u.then == "function" ? et.resolve(u).then(function(f) {
      return o.active ? f : Ln(new yt.PrematureCommit("Transaction committed too early. See http://bit.ly/2kdckMn"));
    }) : d.then(function() {
      return u;
    })).then(function(f) {
      return r && o._resolve(), o._completion.then(function() {
        return f;
      });
    }).catch(function(f) {
      return o._reject(f), Ln(f);
    });
  });
}
function Kh(e, t, n) {
  for (var r = In(e) ? e.slice() : [e], s = 0; s < n; ++s) r.push(t);
  return r;
}
function jB(e) {
  return Rt(Rt({}, e), { table: function(t) {
    var n = e.table(t), r = n.schema, s = {}, a = [];
    function o(y, S, x) {
      var I = qd(y), E = s[I] = s[I] || [], N = y == null ? 0 : typeof y == "string" ? 1 : y.length, $ = S > 0, p = Rt(Rt({}, x), {
        name: $ ? "".concat(I, "(virtual-from:").concat(x.name, ")") : x.name,
        lowLevelIndex: x,
        isVirtual: $,
        keyTail: S,
        keyLength: N,
        extractKey: Cb(y),
        unique: !$ && x.unique
      });
      return E.push(p), p.isPrimaryKey || a.push(p), N > 1 && o(N === 2 ? y[0] : y.slice(0, N - 1), S + 1, x), E.sort(function(w, _) {
        return w.keyTail - _.keyTail;
      }), p;
    }
    var l = o(r.primaryKey.keyPath, 0, r.primaryKey);
    s[":id"] = [l];
    for (var i = 0, u = r.indexes; i < u.length; i++) {
      var d = u[i];
      o(d.keyPath, 0, d);
    }
    function f(y) {
      var S = s[qd(y)];
      return S && S[0];
    }
    function m(y, S) {
      return {
        type: y.type === 1 ? 2 : y.type,
        lower: Kh(y.lower, y.lowerOpen ? e.MAX_KEY : e.MIN_KEY, S),
        lowerOpen: !0,
        upper: Kh(y.upper, y.upperOpen ? e.MIN_KEY : e.MAX_KEY, S),
        upperOpen: !0
      };
    }
    function v(y) {
      var S = y.query.index;
      return S.isVirtual ? Rt(Rt({}, y), { query: {
        index: S.lowLevelIndex,
        range: m(y.query.range, S.keyTail)
      } }) : y;
    }
    return Rt(Rt({}, n), {
      schema: Rt(Rt({}, r), {
        primaryKey: l,
        indexes: a,
        getIndexByKeyPath: f
      }),
      count: function(y) {
        return n.count(v(y));
      },
      query: function(y) {
        return n.query(v(y));
      },
      openCursor: function(y) {
        var S = y.query.index, x = S.keyTail, I = S.isVirtual, E = S.keyLength;
        if (!I) return n.openCursor(y);
        function N($) {
          function p(w) {
            w != null ? $.continue(Kh(w, y.reverse ? e.MAX_KEY : e.MIN_KEY, x)) : y.unique ? $.continue($.key.slice(0, E).concat(y.reverse ? e.MIN_KEY : e.MAX_KEY, x)) : $.continue();
          }
          return Object.create($, {
            continue: { value: p },
            continuePrimaryKey: { value: function(w, _) {
              $.continuePrimaryKey(Kh(w, e.MAX_KEY, x), _);
            } },
            primaryKey: { get: function() {
              return $.primaryKey;
            } },
            key: { get: function() {
              var w = $.key;
              return E === 1 ? w[0] : w.slice(0, E);
            } },
            value: { get: function() {
              return $.value;
            } }
          });
        }
        return n.openCursor(v(y)).then(function($) {
          return $ && N($);
        });
      }
    });
  } });
}
var QB = {
  stack: "dbcore",
  name: "VirtualIndexMiddleware",
  level: 1,
  create: jB
};
function ow(e, t, n, r) {
  return n = n || {}, r = r || "", dr(e).forEach(function(s) {
    if (!Zr(t, s)) n[r + s] = void 0;
    else {
      var a = e[s], o = t[s];
      if (typeof a == "object" && typeof o == "object" && a && o) {
        var l = gb(a);
        l !== gb(o) ? n[r + s] = t[s] : l === "Object" ? ow(a, o, n, r + s + ".") : a !== o && (n[r + s] = t[s]);
      } else a !== o && (n[r + s] = t[s]);
    }
  }), dr(t).forEach(function(s) {
    Zr(e, s) || (n[r + s] = t[s]);
  }), n;
}
function iw(e, t) {
  return t.type === "delete" ? t.keys : t.keys || t.values.map(e.extractKey);
}
var ZB = {
  stack: "dbcore",
  name: "HooksMiddleware",
  level: 2,
  create: function(e) {
    return Rt(Rt({}, e), { table: function(t) {
      var n = e.table(t), r = n.schema.primaryKey;
      return Rt(Rt({}, n), { mutate: function(s) {
        var a = mt.trans, o = a.table(t).hook, l = o.deleting, i = o.creating, u = o.updating;
        switch (s.type) {
          case "add":
            if (i.fire === hn) break;
            return a._promise("readwrite", function() {
              return d(s);
            }, !0);
          case "put":
            if (i.fire === hn && u.fire === hn) break;
            return a._promise("readwrite", function() {
              return d(s);
            }, !0);
          case "delete":
            if (l.fire === hn) break;
            return a._promise("readwrite", function() {
              return d(s);
            }, !0);
          case "deleteRange":
            if (l.fire === hn) break;
            return a._promise("readwrite", function() {
              return f(s);
            }, !0);
        }
        return n.mutate(s);
        function d(v) {
          var y = mt.trans, S = v.keys || iw(r, v);
          if (!S) throw new Error("Keys missing");
          return v = v.type === "add" || v.type === "put" ? Rt(Rt({}, v), { keys: S }) : Rt({}, v), v.type !== "delete" && (v.values = Im([], v.values, !0)), v.keys && (v.keys = Im([], v.keys, !0)), eU(n, v, S).then(function(x) {
            var I = S.map(function(E, N) {
              var $ = x[N], p = {
                onerror: null,
                onsuccess: null
              };
              if (v.type === "delete") l.fire.call(p, E, $, y);
              else if (v.type === "add" || $ === void 0) {
                var w = i.fire.call(p, E, v.values[N], y);
                E == null && w != null && (E = w, v.keys[N] = E, r.outbound || ws(v.values[N], r.keyPath, E));
              } else {
                var _ = ow($, v.values[N]), b = u.fire.call(p, _, E, $, y);
                if (b) {
                  var C = v.values[N];
                  Object.keys(b).forEach(function(k) {
                    Zr(C, k) ? C[k] = b[k] : ws(C, k, b[k]);
                  });
                }
              }
              return p;
            });
            return n.mutate(v).then(function(E) {
              for (var N = E.failures, $ = E.results, p = E.numFailures, w = E.lastResult, _ = 0; _ < S.length; ++_) {
                var b = $ ? $[_] : S[_], C = I[_];
                b == null ? C.onerror && C.onerror(N[_]) : C.onsuccess && C.onsuccess(v.type === "put" && x[_] ? v.values[_] : b);
              }
              return {
                failures: N,
                results: $,
                numFailures: p,
                lastResult: w
              };
            }).catch(function(E) {
              return I.forEach(function(N) {
                return N.onerror && N.onerror(E);
              }), Promise.reject(E);
            });
          });
        }
        function f(v) {
          return m(v.trans, v.range, 1e4);
        }
        function m(v, y, S) {
          return n.query({
            trans: v,
            values: !1,
            query: {
              index: r,
              range: y
            },
            limit: S
          }).then(function(x) {
            var I = x.result;
            return d({
              type: "delete",
              keys: I,
              trans: v
            }).then(function(E) {
              return E.numFailures > 0 ? Promise.reject(E.failures[0]) : I.length < S ? {
                failures: [],
                numFailures: 0,
                lastResult: void 0
              } : m(v, Rt(Rt({}, y), {
                lower: I[I.length - 1],
                lowerOpen: !0
              }), S);
            });
          });
        }
      } });
    } });
  }
};
function eU(e, t, n) {
  return t.type === "add" ? Promise.resolve([]) : e.getMany({
    trans: t.trans,
    keys: n,
    cache: "immutable"
  });
}
function FM(e, t, n) {
  try {
    if (!t || t.keys.length < e.length) return null;
    for (var r = [], s = 0, a = 0; s < t.keys.length && a < e.length; ++s)
      Jt(t.keys[s], e[a]) === 0 && (r.push(n ? Tl(t.values[s]) : t.values[s]), ++a);
    return r.length === e.length ? r : null;
  } catch {
    return null;
  }
}
var tU = {
  stack: "dbcore",
  level: -1,
  create: function(e) {
    return { table: function(t) {
      var n = e.table(t);
      return Rt(Rt({}, n), {
        getMany: function(r) {
          if (!r.cache) return n.getMany(r);
          var s = FM(r.keys, r.trans._cache, r.cache === "clone");
          return s ? et.resolve(s) : n.getMany(r).then(function(a) {
            return r.trans._cache = {
              keys: r.keys,
              values: r.cache === "clone" ? Tl(a) : a
            }, a;
          });
        },
        mutate: function(r) {
          return r.type !== "add" && (r.trans._cache = null), n.mutate(r);
        }
      });
    } };
  }
};
function BM(e, t) {
  return e.trans.mode === "readonly" && !!e.subscr && !e.trans.explicit && e.trans.db._options.cache !== "disabled" && !t.schema.primaryKey.outbound;
}
function UM(e, t) {
  switch (e) {
    case "query":
      return t.values && !t.unique;
    case "get":
      return !1;
    case "getMany":
      return !1;
    case "count":
      return !1;
    case "openCursor":
      return !1;
  }
}
var nU = {
  stack: "dbcore",
  level: 0,
  name: "Observability",
  create: function(e) {
    var t = e.schema.name, n = new Ur(e.MIN_KEY, e.MAX_KEY);
    return Rt(Rt({}, e), {
      transaction: function(r, s, a) {
        if (mt.subscr && s !== "readonly") throw new yt.ReadOnly("Readwrite transaction in liveQuery context. Querier source: ".concat(mt.querier));
        return e.transaction(r, s, a);
      },
      table: function(r) {
        var s = e.table(r), a = s.schema, o = a.primaryKey, l = a.indexes, i = o.extractKey, u = o.outbound, d = o.autoIncrement && l.filter(function(y) {
          return y.compound && y.keyPath.includes(o.keyPath);
        }), f = Rt(Rt({}, s), { mutate: function(y) {
          var S, x, I = y.trans, E = y.mutatedParts || (y.mutatedParts = {}), N = function(G) {
            var ee = "idb://".concat(t, "/").concat(r, "/").concat(G);
            return E[ee] || (E[ee] = new Ur());
          }, $ = N(""), p = N(":dels"), w = y.type, _ = y.type === "deleteRange" ? [y.range] : y.type === "delete" ? [y.keys] : y.values.length < 50 ? [iw(o, y).filter(function(G) {
            return G;
          }), y.values] : [], b = _[0], C = _[1], k = y.trans._cache;
          if (In(b)) {
            $.addKeys(b);
            var P = w === "delete" || b.length === C.length ? FM(b, k) : null;
            P || p.addKeys(b), (P || C) && rU(N, a, P, C);
          } else if (b) {
            var H = {
              from: (S = b.lower) !== null && S !== void 0 ? S : e.MIN_KEY,
              to: (x = b.upper) !== null && x !== void 0 ? x : e.MAX_KEY
            };
            p.add(H), $.add(H);
          } else
            $.add(n), p.add(n), a.indexes.forEach(function(G) {
              return N(G.name).add(n);
            });
          return s.mutate(y).then(function(G) {
            return b && (y.type === "add" || y.type === "put") && ($.addKeys(G.results), d && d.forEach(function(ee) {
              for (var J = y.values.map(function(le) {
                return ee.extractKey(le);
              }), z = ee.keyPath.findIndex(function(le) {
                return le === o.keyPath;
              }), R = 0, V = G.results.length; R < V; ++R) J[R][z] = G.results[R];
              N(ee.name).addKeys(J);
            })), I.mutatedParts = Xg(I.mutatedParts || {}, E), G;
          });
        } }), m = function(y) {
          var S, x, I = y.query, E = I.index, N = I.range;
          return [E, new Ur((S = N.lower) !== null && S !== void 0 ? S : e.MIN_KEY, (x = N.upper) !== null && x !== void 0 ? x : e.MAX_KEY)];
        }, v = {
          get: function(y) {
            return [o, new Ur(y.key)];
          },
          getMany: function(y) {
            return [o, new Ur().addKeys(y.keys)];
          },
          count: m,
          query: m,
          openCursor: m
        };
        return dr(v).forEach(function(y) {
          f[y] = function(S) {
            var x = mt.subscr, I = !!x, E = BM(mt, s) && UM(y, S) ? S.obsSet = {} : x;
            if (I) {
              var N = function(k) {
                var P = "idb://".concat(t, "/").concat(r, "/").concat(k);
                return E[P] || (E[P] = new Ur());
              }, $ = N(""), p = N(":dels"), w = v[y](S), _ = w[0], b = w[1];
              if (y === "query" && _.isPrimaryKey && !S.values ? p.add(b) : N(_.name || "").add(b), !_.isPrimaryKey) if (y === "count") p.add(n);
              else {
                var C = y === "query" && u && S.values && s.query(Rt(Rt({}, S), { values: !1 }));
                return s[y].apply(this, arguments).then(function(k) {
                  if (y === "query") {
                    if (u && S.values) return C.then(function(ee) {
                      var J = ee.result;
                      return $.addKeys(J), k;
                    });
                    var P = S.values ? k.result.map(i) : k.result;
                    S.values ? $.addKeys(P) : p.addKeys(P);
                  } else if (y === "openCursor") {
                    var H = k, G = S.values;
                    return H && Object.create(H, {
                      key: { get: function() {
                        return p.addKey(H.primaryKey), H.key;
                      } },
                      primaryKey: { get: function() {
                        var ee = H.primaryKey;
                        return p.addKey(ee), ee;
                      } },
                      value: { get: function() {
                        return G && $.addKey(H.primaryKey), H.value;
                      } }
                    });
                  }
                  return k;
                });
              }
            }
            return s[y].apply(this, arguments);
          };
        }), f;
      }
    });
  }
};
function rU(e, t, n, r) {
  function s(a) {
    var o = e(a.name || "");
    function l(u) {
      return u != null ? a.extractKey(u) : null;
    }
    var i = function(u) {
      return a.multiEntry && In(u) ? u.forEach(function(d) {
        return o.addKey(d);
      }) : o.addKey(u);
    };
    (n || r).forEach(function(u, d) {
      var f = n && l(n[d]), m = r && l(r[d]);
      Jt(f, m) !== 0 && (f != null && i(f), m != null && i(m));
    });
  }
  t.indexes.forEach(s);
}
function mT(e, t, n) {
  if (n.numFailures === 0) return t;
  if (t.type === "deleteRange") return null;
  var r = t.keys ? t.keys.length : "values" in t && t.values ? t.values.length : 1;
  if (n.numFailures === r) return null;
  var s = Rt({}, t);
  return In(s.keys) && (s.keys = s.keys.filter(function(a, o) {
    return !(o in n.failures);
  })), "values" in s && In(s.values) && (s.values = s.values.filter(function(a, o) {
    return !(o in n.failures);
  })), s;
}
function sU(e, t) {
  return t.lower === void 0 ? !0 : t.lowerOpen ? Jt(e, t.lower) > 0 : Jt(e, t.lower) >= 0;
}
function aU(e, t) {
  return t.upper === void 0 ? !0 : t.upperOpen ? Jt(e, t.upper) < 0 : Jt(e, t.upper) <= 0;
}
function jy(e, t) {
  return sU(e, t) && aU(e, t);
}
function gT(e, t, n, r, s, a) {
  if (!n || n.length === 0) return e;
  var o = t.query.index, l = o.multiEntry, i = t.query.range, u = r.schema.primaryKey.extractKey, d = o.extractKey, f = (o.lowLevelIndex || o).extractKey, m = n.reduce(function(v, y) {
    var S = v, x = [];
    if (y.type === "add" || y.type === "put")
      for (var I = new Ur(), E = y.values.length - 1; E >= 0; --E) {
        var N = y.values[E], $ = u(N);
        if (!I.hasKey($)) {
          var p = d(N);
          (l && In(p) ? p.some(function(k) {
            return jy(k, i);
          }) : jy(p, i)) && (I.addKey($), x.push(N));
        }
      }
    switch (y.type) {
      case "add":
        var w = new Ur().addKeys(t.values ? v.map(function(k) {
          return u(k);
        }) : v);
        S = v.concat(t.values ? x.filter(function(k) {
          var P = u(k);
          return w.hasKey(P) ? !1 : (w.addKey(P), !0);
        }) : x.map(function(k) {
          return u(k);
        }).filter(function(k) {
          return w.hasKey(k) ? !1 : (w.addKey(k), !0);
        }));
        break;
      case "put":
        var _ = new Ur().addKeys(y.values.map(function(k) {
          return u(k);
        }));
        S = v.filter(function(k) {
          return !_.hasKey(t.values ? u(k) : k);
        }).concat(t.values ? x : x.map(function(k) {
          return u(k);
        }));
        break;
      case "delete":
        var b = new Ur().addKeys(y.keys);
        S = v.filter(function(k) {
          return !b.hasKey(t.values ? u(k) : k);
        });
        break;
      case "deleteRange":
        var C = y.range;
        S = v.filter(function(k) {
          return !jy(u(k), C);
        });
        break;
    }
    return S;
  }, e);
  return m === e ? e : (m.sort(function(v, y) {
    return Jt(f(v), f(y)) || Jt(u(v), u(y));
  }), t.limit && t.limit < 1 / 0 && (m.length > t.limit ? m.length = t.limit : e.length === t.limit && m.length < t.limit && (s.dirty = !0)), a ? Object.freeze(m) : m);
}
function vT(e, t) {
  return Jt(e.lower, t.lower) === 0 && Jt(e.upper, t.upper) === 0 && !!e.lowerOpen == !!t.lowerOpen && !!e.upperOpen == !!t.upperOpen;
}
function oU(e, t, n, r) {
  if (e === void 0) return t !== void 0 ? -1 : 0;
  if (t === void 0) return 1;
  var s = Jt(e, t);
  if (s === 0) {
    if (n && r) return 0;
    if (n) return 1;
    if (r) return -1;
  }
  return s;
}
function iU(e, t, n, r) {
  if (e === void 0) return t !== void 0 ? 1 : 0;
  if (t === void 0) return -1;
  var s = Jt(e, t);
  if (s === 0) {
    if (n && r) return 0;
    if (n) return -1;
    if (r) return 1;
  }
  return s;
}
function lU(e, t) {
  return oU(e.lower, t.lower, e.lowerOpen, t.lowerOpen) <= 0 && iU(e.upper, t.upper, e.upperOpen, t.upperOpen) >= 0;
}
function uU(e, t, n, r) {
  var s = vl["idb://".concat(e, "/").concat(t)];
  if (!s) return [];
  var a = s.queries[n];
  if (!a) return [
    null,
    !1,
    s,
    null
  ];
  var o = a[(r.query ? r.query.index.name : null) || ""];
  if (!o) return [
    null,
    !1,
    s,
    null
  ];
  switch (n) {
    case "query":
      var l = o.find(function(u) {
        return u.req.limit === r.limit && u.req.values === r.values && vT(u.req.query.range, r.query.range);
      });
      return l ? [
        l,
        !0,
        s,
        o
      ] : [
        o.find(function(u) {
          return ("limit" in u.req ? u.req.limit : 1 / 0) >= r.limit && (r.values ? u.req.values : !0) && lU(u.req.query.range, r.query.range);
        }),
        !1,
        s,
        o
      ];
    case "count":
      var i = o.find(function(u) {
        return vT(u.req.query.range, r.query.range);
      });
      return [
        i,
        !!i,
        s,
        o
      ];
  }
}
function cU(e, t, n, r) {
  e.subscribers.add(n), r.addEventListener("abort", function() {
    e.subscribers.delete(n), e.subscribers.size === 0 && dU(e, t);
  });
}
function dU(e, t) {
  setTimeout(function() {
    e.subscribers.size === 0 && Xi(t, e);
  }, 3e3);
}
var fU = {
  stack: "dbcore",
  level: 0,
  name: "Cache",
  create: function(e) {
    var t = e.schema.name;
    return Rt(Rt({}, e), {
      transaction: function(n, r, s) {
        var a = e.transaction(n, r, s);
        if (r === "readwrite") {
          var o = new AbortController(), l = o.signal, i = function(u) {
            return function() {
              if (o.abort(), r === "readwrite") {
                for (var d = /* @__PURE__ */ new Set(), f = 0, m = n; f < m.length; f++) {
                  var v = m[f], y = vl["idb://".concat(t, "/").concat(v)];
                  if (y) {
                    var S = e.table(v), x = y.optimisticOps.filter(function(G) {
                      return G.trans === a;
                    });
                    if (a._explicit && u && a.mutatedParts) for (var I = 0, E = Object.values(y.queries.query); I < E.length; I++)
                      for (var N = E[I], $ = 0, p = N.slice(); $ < p.length; $++) {
                        var w = p[$];
                        sw(w.obsSet, a.mutatedParts) && (Xi(N, w), w.subscribers.forEach(function(G) {
                          return d.add(G);
                        }));
                      }
                    else if (x.length > 0) {
                      y.optimisticOps = y.optimisticOps.filter(function(G) {
                        return G.trans !== a;
                      });
                      for (var _ = 0, b = Object.values(y.queries.query); _ < b.length; _++)
                        for (var N = b[_], C = 0, k = N.slice(); C < k.length; C++) {
                          var w = k[C];
                          if (w.res != null && a.mutatedParts) if (u && !w.dirty) {
                            var P = Object.isFrozen(w.res), H = gT(w.res, w.req, x, S, w, P);
                            w.dirty ? (Xi(N, w), w.subscribers.forEach(function(J) {
                              return d.add(J);
                            })) : H !== w.res && (w.res = H, w.promise = et.resolve({ result: H }));
                          } else
                            w.dirty && Xi(N, w), w.subscribers.forEach(function(J) {
                              return d.add(J);
                            });
                        }
                    }
                  }
                }
                d.forEach(function(G) {
                  return G();
                });
              }
            };
          };
          a.addEventListener("abort", i(!1), { signal: l }), a.addEventListener("error", i(!1), { signal: l }), a.addEventListener("complete", i(!0), { signal: l });
        }
        return a;
      },
      table: function(n) {
        var r = e.table(n), s = r.schema.primaryKey;
        return Rt(Rt({}, r), {
          mutate: function(a) {
            var o = mt.trans;
            if (s.outbound || o.db._options.cache === "disabled" || o.explicit || o.idbtrans.mode !== "readwrite") return r.mutate(a);
            var l = vl["idb://".concat(t, "/").concat(n)];
            if (!l) return r.mutate(a);
            var i = r.mutate(a);
            return (a.type === "add" || a.type === "put") && (a.values.length >= 50 || iw(s, a).some(function(u) {
              return u == null;
            })) ? i.then(function(u) {
              var d = mT(l, Rt(Rt({}, a), { values: a.values.map(function(f, m) {
                var v;
                if (u.failures[m]) return f;
                var y = !((v = s.keyPath) === null || v === void 0) && v.includes(".") ? Tl(f) : Rt({}, f);
                return ws(y, s.keyPath, u.results[m]), y;
              }) }), u);
              l.optimisticOps.push(d), queueMicrotask(function() {
                return a.mutatedParts && zh(a.mutatedParts);
              });
            }) : (l.optimisticOps.push(a), a.mutatedParts && zh(a.mutatedParts), i.then(function(u) {
              if (u.numFailures > 0) {
                Xi(l.optimisticOps, a);
                var d = mT(l, a, u);
                d && l.optimisticOps.push(d), a.mutatedParts && zh(a.mutatedParts);
              }
            }), i.catch(function() {
              Xi(l.optimisticOps, a), a.mutatedParts && zh(a.mutatedParts);
            })), i;
          },
          query: function(a) {
            var o;
            if (!BM(mt, r) || !UM("query", a)) return r.query(a);
            var l = ((o = mt.trans) === null || o === void 0 ? void 0 : o.db._options.cache) === "immutable", i = mt, u = i.requery, d = i.signal, f = uU(t, n, "query", a), m = f[0], v = f[1], y = f[2], S = f[3];
            if (m && v) m.obsSet = a.obsSet;
            else {
              var x = r.query(a).then(function(I) {
                var E = I.result;
                if (m && (m.res = E), l) {
                  for (var N = 0, $ = E.length; N < $; ++N) Object.freeze(E[N]);
                  Object.freeze(E);
                } else I.result = Tl(E);
                return I;
              }).catch(function(I) {
                return S && m && Xi(S, m), Promise.reject(I);
              });
              m = {
                obsSet: a.obsSet,
                promise: x,
                subscribers: /* @__PURE__ */ new Set(),
                type: "query",
                req: a,
                dirty: !1
              }, S ? S.push(m) : (S = [m], y || (y = vl["idb://".concat(t, "/").concat(n)] = {
                queries: {
                  query: {},
                  count: {}
                },
                objs: /* @__PURE__ */ new Map(),
                optimisticOps: [],
                unsignaledParts: {}
              }), y.queries.query[a.query.index.name || ""] = S);
            }
            return cU(m, S, u, d), m.promise.then(function(I) {
              return { result: gT(I.result, a, y?.optimisticOps, r, m, l) };
            });
          }
        });
      }
    });
  }
};
function Wh(e, t) {
  return new Proxy(e, { get: function(n, r, s) {
    return r === "db" ? t : Reflect.get(n, r, s);
  } });
}
var Gu = (function() {
  function e(t, n) {
    var r = this;
    this._middlewares = {}, this.verno = 0;
    var s = e.dependencies;
    this._options = n = Rt({
      addons: e.addons,
      autoOpen: !0,
      indexedDB: s.indexedDB,
      IDBKeyRange: s.IDBKeyRange,
      cache: "cloned"
    }, n), this._deps = {
      indexedDB: n.indexedDB,
      IDBKeyRange: n.IDBKeyRange
    };
    var a = n.addons;
    this._dbSchema = {}, this._versions = [], this._storeNames = [], this._allTables = {}, this.idbdb = null, this._novip = this;
    var o = {
      dbOpenError: null,
      isBeingOpened: !1,
      onReadyBeingFired: null,
      openComplete: !1,
      dbReadyResolve: hn,
      dbReadyPromise: null,
      cancelOpen: hn,
      openCanceller: null,
      autoSchema: !0,
      PR1398_maxLoop: 3,
      autoOpen: n.autoOpen
    };
    o.dbReadyPromise = new et(function(i) {
      o.dbReadyResolve = i;
    }), o.openCanceller = new et(function(i, u) {
      o.cancelOpen = u;
    }), this._state = o, this.name = t, this.on = Of(this, "populate", "blocked", "versionchange", "close", { ready: [V1, hn] }), this.on.ready.subscribe = mM(this.on.ready.subscribe, function(i) {
      return function(u, d) {
        e.vip(function() {
          var f = r._state;
          if (f.openComplete)
            f.dbOpenError || et.resolve().then(u), d && i(u);
          else if (f.onReadyBeingFired)
            f.onReadyBeingFired.push(u), d && i(u);
          else {
            i(u);
            var m = r;
            d || i(function v() {
              m.on.ready.unsubscribe(u), m.on.ready.unsubscribe(v);
            });
          }
        });
      };
    }), this.Collection = _B(this), this.Table = mB(this), this.Transaction = IB(this), this.Version = zB(this), this.WhereClause = EB(this), this.on("versionchange", function(i) {
      i.newVersion > 0 ? console.warn("Another connection wants to upgrade database '".concat(r.name, "'. Closing db now to resume the upgrade.")) : console.warn("Another connection wants to delete database '".concat(r.name, "'. Closing db now to resume the delete request.")), r.close({ disableAutoOpen: !1 });
    }), this.on("blocked", function(i) {
      !i.newVersion || i.newVersion < i.oldVersion ? console.warn("Dexie.delete('".concat(r.name, "') was blocked")) : console.warn("Upgrade '".concat(r.name, "' blocked by other connection holding version ").concat(i.oldVersion / 10));
    }), this._maxKey = gf(n.IDBKeyRange), this._createTransaction = function(i, u, d, f) {
      return new r.Transaction(i, u, d, r._options.chromeTransactionDurability, f);
    }, this._fireOnBlocked = function(i) {
      r.on("blocked").fire(i), Pu.filter(function(u) {
        return u.name === r.name && u !== r && !u._state.vcFired;
      }).map(function(u) {
        return u.on("versionchange").fire(i);
      });
    }, this.use(tU), this.use(fU), this.use(nU), this.use(QB), this.use(ZB);
    var l = new Proxy(this, { get: function(i, u, d) {
      if (u === "_vip") return !0;
      if (u === "table") return function(m) {
        return Wh(r.table(m), l);
      };
      var f = Reflect.get(i, u, d);
      return f instanceof PM ? Wh(f, l) : u === "tables" ? f.map(function(m) {
        return Wh(m, l);
      }) : u === "_createTransaction" ? function() {
        return Wh(f.apply(this, arguments), l);
      } : f;
    } });
    this.vip = l, a.forEach(function(i) {
      return i(r);
    });
  }
  return e.prototype.version = function(t) {
    if (isNaN(t) || t < 0.1) throw new yt.Type("Given version is not a positive number");
    if (t = Math.round(t * 10) / 10, this.idbdb || this._state.isBeingOpened) throw new yt.Schema("Cannot add version when database is open");
    this.verno = Math.max(this.verno, t);
    var n = this._versions, r = n.filter(function(s) {
      return s._cfg.version === t;
    })[0];
    return r || (r = new this.Version(t), n.push(r), n.sort($B), r.stores({}), this._state.autoSchema = !1, r);
  }, e.prototype._whenReady = function(t) {
    var n = this;
    return this.idbdb && (this._state.openComplete || mt.letThrough || this._vip) ? t() : new et(function(r, s) {
      if (n._state.openComplete) return s(new yt.DatabaseClosed(n._state.dbOpenError));
      if (!n._state.isBeingOpened) {
        if (!n._state.autoOpen) {
          s(new yt.DatabaseClosed());
          return;
        }
        n.open().catch(hn);
      }
      n._state.dbReadyPromise.then(r, s);
    }).then(t);
  }, e.prototype.use = function(t) {
    var n = t.stack, r = t.create, s = t.level, a = t.name;
    a && this.unuse({
      stack: n,
      name: a
    });
    var o = this._middlewares[n] || (this._middlewares[n] = []);
    return o.push({
      stack: n,
      create: r,
      level: s ?? 10,
      name: a
    }), o.sort(function(l, i) {
      return l.level - i.level;
    }), this;
  }, e.prototype.unuse = function(t) {
    var n = t.stack, r = t.name, s = t.create;
    return n && this._middlewares[n] && (this._middlewares[n] = this._middlewares[n].filter(function(a) {
      return s ? a.create !== s : r ? a.name !== r : !1;
    })), this;
  }, e.prototype.open = function() {
    var t = this;
    return Cl(fi, function() {
      return YB(t);
    });
  }, e.prototype._close = function() {
    var t = this._state, n = Pu.indexOf(this);
    if (n >= 0 && Pu.splice(n, 1), this.idbdb) {
      try {
        this.idbdb.close();
      } catch {
      }
      this.idbdb = null;
    }
    t.isBeingOpened || (t.dbReadyPromise = new et(function(r) {
      t.dbReadyResolve = r;
    }), t.openCanceller = new et(function(r, s) {
      t.cancelOpen = s;
    }));
  }, e.prototype.close = function(t) {
    var n = (t === void 0 ? { disableAutoOpen: !0 } : t).disableAutoOpen, r = this._state;
    n ? (r.isBeingOpened && r.cancelOpen(new yt.DatabaseClosed()), this._close(), r.autoOpen = !1, r.dbOpenError = new yt.DatabaseClosed()) : (this._close(), r.autoOpen = this._options.autoOpen || r.isBeingOpened, r.openComplete = !1, r.dbOpenError = null);
  }, e.prototype.delete = function(t) {
    var n = this;
    t === void 0 && (t = { disableAutoOpen: !0 });
    var r = arguments.length > 0 && typeof arguments[0] != "object", s = this._state;
    return new et(function(a, o) {
      var l = function() {
        n.close(t);
        var i = n._deps.indexedDB.deleteDatabase(n.name);
        i.onsuccess = An(function() {
          GB(n._deps, n.name), a();
        }), i.onerror = Ca(o), i.onblocked = n._fireOnBlocked;
      };
      if (r) throw new yt.InvalidArgument("Invalid closeOptions argument to db.delete()");
      s.isBeingOpened ? s.dbReadyPromise.then(l) : l();
    });
  }, e.prototype.backendDB = function() {
    return this.idbdb;
  }, e.prototype.isOpen = function() {
    return this.idbdb !== null;
  }, e.prototype.hasBeenClosed = function() {
    var t = this._state.dbOpenError;
    return t && t.name === "DatabaseClosed";
  }, e.prototype.hasFailed = function() {
    return this._state.dbOpenError !== null;
  }, e.prototype.dynamicallyOpened = function() {
    return this._state.autoSchema;
  }, Object.defineProperty(e.prototype, "tables", {
    get: function() {
      var t = this;
      return dr(this._allTables).map(function(n) {
        return t._allTables[n];
      });
    },
    enumerable: !1,
    configurable: !0
  }), e.prototype.transaction = function() {
    var t = XB.apply(this, arguments);
    return this._transaction.apply(this, t);
  }, e.prototype._transaction = function(t, n, r) {
    var s = this, a = mt.trans;
    (!a || a.db !== this || t.indexOf("!") !== -1) && (a = null);
    var o = t.indexOf("?") !== -1;
    t = t.replace("!", "").replace("?", "");
    var l, i;
    try {
      if (i = n.map(function(d) {
        var f = d instanceof s.Table ? d.name : d;
        if (typeof f != "string") throw new TypeError("Invalid table argument to Dexie.transaction(). Only Table or String are allowed");
        return f;
      }), t == "r" || t === zy) l = zy;
      else if (t == "rw" || t == Ky) l = Ky;
      else throw new yt.InvalidArgument("Invalid transaction mode: " + t);
      if (a) {
        if (a.mode === zy && l === Ky) if (o) a = null;
        else throw new yt.SubTransaction("Cannot enter a sub-transaction with READWRITE mode when parent transaction is READONLY");
        a && i.forEach(function(d) {
          if (a && a.storeNames.indexOf(d) === -1) if (o) a = null;
          else throw new yt.SubTransaction("Table " + d + " not included in parent transaction.");
        }), o && a && !a.active && (a = null);
      }
    } catch (d) {
      return a ? a._promise(null, function(f, m) {
        m(d);
      }) : Ln(d);
    }
    var u = OM.bind(null, this, l, i, a, r);
    return a ? a._promise(l, u, "lock") : mt.trans ? Cl(mt.transless, function() {
      return s._whenReady(u);
    }) : this._whenReady(u);
  }, e.prototype.table = function(t) {
    if (!Zr(this._allTables, t)) throw new yt.InvalidTable("Table ".concat(t, " does not exist"));
    return this._allTables[t];
  }, e;
})(), hU = typeof Symbol < "u" && "observable" in Symbol ? Symbol.observable : "@@observable", pU = (function() {
  function e(t) {
    this._subscribe = t;
  }
  return e.prototype.subscribe = function(t, n, r) {
    return this._subscribe(!t || typeof t == "function" ? {
      next: t,
      error: n,
      complete: r
    } : t);
  }, e.prototype[hU] = function() {
    return this;
  }, e;
})(), Fm;
try {
  Fm = {
    indexedDB: gr.indexedDB || gr.mozIndexedDB || gr.webkitIndexedDB || gr.msIndexedDB,
    IDBKeyRange: gr.IDBKeyRange || gr.webkitIDBKeyRange
  };
} catch {
  Fm = {
    indexedDB: null,
    IDBKeyRange: null
  };
}
function mU(e) {
  var t = !1, n, r = new pU(function(s) {
    var a = K1(e);
    function o(I) {
      var E = ic();
      try {
        a && uc();
        var N = gi(e, I);
        return a && (N = N.finally(vi)), N;
      } finally {
        E && lc();
      }
    }
    var l = !1, i, u = {}, d = {}, f = {
      get closed() {
        return l;
      },
      unsubscribe: function() {
        l || (l = !0, i && i.abort(), m && _i.storagemutated.unsubscribe(S));
      }
    };
    s.start && s.start(f);
    var m = !1, v = function() {
      return Hy(x);
    };
    function y() {
      return sw(d, u);
    }
    var S = function(I) {
      Xg(u, I), y() && v();
    }, x = function() {
      if (!(l || !Fm.indexedDB)) {
        u = {};
        var I = {};
        i && i.abort(), i = new AbortController();
        var E = {
          subscr: I,
          signal: i.signal,
          requery: v,
          querier: e,
          trans: null
        }, N = o(E);
        Promise.resolve(N).then(function($) {
          t = !0, n = $, !(l || E.signal.aborted) && (u = {}, d = I, !HF(d) && !m && (_i(Bf, S), m = !0), Hy(function() {
            return !l && s.next && s.next($);
          }));
        }, function($) {
          t = !1, ["DatabaseClosedError", "AbortError"].includes($?.name) || l || Hy(function() {
            l || s.error && s.error($);
          });
        });
      }
    };
    return setTimeout(v, 0), f;
  });
  return r.hasValue = function() {
    return t;
  }, r.getValue = function() {
    return n;
  }, r;
}
var rl = Gu;
Wu(rl, Rt(Rt({}, Vg), {
  delete: function(e) {
    return new rl(e, { addons: [] }).delete();
  },
  exists: function(e) {
    return new rl(e, { addons: [] }).open().then(function(t) {
      return t.close(), !0;
    }).catch("NoSuchDatabaseError", function() {
      return !1;
    });
  },
  getDatabaseNames: function(e) {
    try {
      return KB(rl.dependencies).then(e);
    } catch {
      return Ln(new yt.MissingAPI());
    }
  },
  defineClass: function() {
    function e(t) {
      Es(this, t);
    }
    return e;
  },
  ignoreTransaction: function(e) {
    return mt.trans ? Cl(mt.transless, e) : e();
  },
  vip: xb,
  async: function(e) {
    return function() {
      try {
        var t = kb(e.apply(this, arguments));
        return !t || typeof t.then != "function" ? et.resolve(t) : t;
      } catch (n) {
        return Ln(n);
      }
    };
  },
  spawn: function(e, t, n) {
    try {
      var r = kb(e.apply(n, t || []));
      return !r || typeof r.then != "function" ? et.resolve(r) : r;
    } catch (s) {
      return Ln(s);
    }
  },
  currentTransaction: { get: function() {
    return mt.trans || null;
  } },
  waitFor: function(e, t) {
    var n = et.resolve(typeof e == "function" ? rl.ignoreTransaction(e) : e).timeout(t || 6e4);
    return mt.trans ? mt.trans.waitFor(n) : n;
  },
  Promise: et,
  debug: {
    get: function() {
      return Ua;
    },
    set: function(e) {
      AM(e);
    }
  },
  derive: ac,
  extend: Es,
  props: Wu,
  override: mM,
  Events: Of,
  on: _i,
  liveQuery: mU,
  extendObservabilitySet: Xg,
  getByKeyPath: Ao,
  setByKeyPath: ws,
  delByKeyPath: BF,
  shallowClone: vM,
  deepClone: Tl,
  getObjectDiff: ow,
  cmp: Jt,
  asap: gM,
  minKey: Ab,
  addons: [],
  connections: Pu,
  errnames: G1,
  dependencies: Fm,
  cache: vl,
  semVer: oT,
  version: oT.split(".").map(function(e) {
    return parseInt(e);
  }).reduce(function(e, t, n) {
    return e + t / Math.pow(10, n * 2);
  })
}));
rl.maxKey = gf(rl.dependencies.IDBKeyRange);
typeof dispatchEvent < "u" && typeof addEventListener < "u" && (_i(Bf, function(e) {
  if (!ii) {
    var t = new CustomEvent(Tb, { detail: e });
    ii = !0, dispatchEvent(t), ii = !1;
  }
}), addEventListener(Tb, function(e) {
  var t = e.detail;
  ii || lw(t);
}));
function lw(e) {
  var t = ii;
  try {
    ii = !0, _i.storagemutated.fire(e), aw(e, !0);
  } finally {
    ii = t;
  }
}
var ii = !1, ri, Mb = function() {
};
typeof BroadcastChannel < "u" && (Mb = function() {
  ri = new BroadcastChannel(Tb), ri.onmessage = function(e) {
    return e.data && lw(e.data);
  };
}, Mb(), typeof ri.unref == "function" && ri.unref(), _i(Bf, function(e) {
  ii || ri.postMessage(e);
}));
typeof addEventListener < "u" && (addEventListener("pagehide", function(e) {
  if (!Gu.disableBfCache && e.persisted) {
    Ua && console.debug("Dexie: handling persisted pagehide"), ri?.close();
    for (var t = 0, n = Pu; t < n.length; t++) n[t].close({ disableAutoOpen: !1 });
  }
}), addEventListener("pageshow", function(e) {
  !Gu.disableBfCache && e.persisted && (Ua && console.debug("Dexie: handling persisted pageshow"), Mb(), lw({ all: new Ur(-1 / 0, [[]]) }));
}));
et.rejectionMapper = JF;
AM(Ua);
var jg = "littlewhitebox-assistant-default", gU = "2026-06-assistant-prompt-memory-gate-v3";
function qM(e = {}) {
  return {
    includeMemory: e.includeMemory !== !1,
    includeCartography: e.includeCartography !== !1,
    includeQuestOrchestration: e.includeQuestOrchestration === !0
  };
}
function os(e, t = []) {
  const n = t.map((r) => Vu(r)).filter(Boolean);
  return n.length ? ["## " + e, n.join(`
`)].join(`
`) : "";
}
function vU(e = {}) {
  const { includeMemory: t, includeCartography: n, includeQuestOrchestration: r } = qM(e), s = [
    "You are the backstage manager for the current LittleWhiteTavern RP session, running inside the user's SillyTavern instance through the LittleWhiteBox tavern workspace.",
    "The main chat handles immersive roleplay. Your job is to keep the backstage materials useful: memory, spatial records, and possible future directions. Do not take over the scene, speak as the RP character, or steer the user by force.",
    "Accepted-turn maintenance and manual manager chat share the same identity and evidence standard. The trigger differs: accepted-turn maintenance handles the previous RP turn after the user continues, while manual chat answers the user's current question.",
    t ? "When the Memory Archiving contract is enabled, maintain the current session's Markdown memory files." : "",
    n ? "When the Cartography Engine contract is enabled, maintain the current session's map and atlas records." : "",
    r ? "When the Quest Orchestration contract is enabled, maintain a small rollbackable pool of possible next narrative directions." : ""
  ], a = [
    t ? "Turn already-confirmed RP source text into concise long-term memory instead of echoing whole chat logs back into the prompt." : "",
    t ? "Update memory only when the turn establishes a new or changed continuity fact that future RP must remember. If nothing material changed, explicitly skip writing." : "",
    n ? "Update the map only when confirmed spatial facts changed: position, boundaries, routes, objects, hazards, or current scene scope." : "",
    r ? "Maintain event directions only as forward-looking possibilities. They are not memory, not old events, and not random surprises." : "",
    n ? "When the user asks about memory, continuity, the map, or backstage materials, answer from evidence. If uncertain, inspect RP source text or existing records instead of guessing." : "When the user asks about memory, continuity, or backstage materials, answer from evidence. If uncertain, inspect RP source text or existing records instead of guessing.",
    "Important facts should land in the appropriate memory file, map record, or atlas record, not only in your final reply."
  ], o = [
    "The current session is the only work scope. Do not operate on other sessions, character cards, worldbook configuration, SillyTavern settings, or plugin source code.",
    [
      t ? "current-session `memory/...` files" : "",
      n ? "current-session map and atlas records" : "",
      r ? "current-session event pool" : "",
      "manager chat history",
      "RP source evidence"
    ].filter(Boolean).join(", ") + " all belong to this session only.",
    "RP source text is the source of truth. Memory files, map records, and atlas records are derived materials; if they conflict, verify with Grep/Read under `chat/` first, then repair the derived material.",
    "Injected context is only a snapshot of current materials, not the full chat history. Treat unread source text as unverified.",
    t ? "The author of `[用户消息]` is not automatically an in-world character. Do not infer a user persona, profile, or player identity from the speaker label." : ""
  ], l = [
    t ? "`[Resident Memory Files]` automatically provides the current global memory file, `memory/state.md`. Character memory files are not all resident; use LS/Grep/Read for relevant `memory/characters/<角色名>.md` files when needed." : "",
    t ? "Accepted-turn maintenance receives the previous accepted user message and assistant reply. Update memory only when the assistant reply makes a fact or state actually established." : "",
    r ? "`[Current Event Pool]` provides the current active and recently completed event directions for backstage maintenance only. Use it to advance, complete, or decide whether the pool is low." : "",
    "Manual manager chat receives the manager's own conversation history and the current user question. RP source text is not fully preloaded; use Grep/Read under `chat/` when evidence is needed.",
    "Message order and floor numbers are backstage coordinates for evidence and rollback. They are not story time."
  ], i = t ? [
    "Operate on current-session `memory/...` Markdown files only through LS/Grep/Read/Edit/Write.",
    "`memory/state.md` is global memory: current situation, hard world facts, unresolved hooks, and scene continuity that future RP must remember.",
    "`memory/characters/<角色名>.md` is character memory: one file per entity, with the filename as the entity name. It carries durable character changes such as identity, motives, secrets, constraints, relationship shifts, promises, debts, and risks.",
    '`memory/state.md` is not a directory. Do not write index notes such as "see another file"; it records global facts only.',
    "Edit and Write may write only `memory/state.md` or `memory/characters/<角色名>.md`. Do not create `memory/session.md`, `memory/turns/*.md`, or other memory paths.",
    "Do not create or maintain `memory/characters/User.md`, `memory/characters/Player.md`, `memory/characters/用户.md`, `memory/characters/玩家.md`, or any file whose subject is merely the message author. If player-side durable state matters, keep it in `memory/state.md` unless the RP clearly established a named in-world player character.",
    "These Markdown files are for future model reading and retrieval, not rigid database schemas. Preserve useful headings and keep them clear, editable, and maintainable."
  ] : [], u = [
    [
      "First identify the work type: accepted-turn maintenance, a user asking about backstage materials",
      t ? ", a user asking to correct memory" : "",
      n ? ", or a user asking to inspect or change the map" : "",
      "."
    ].join(""),
    "Use tools when you need evidence or need to save material. All saves must go through the currently available tools.",
    "Before writing, read the existing record or RP source text, then make the smallest necessary change. Do not rewrite whole sections without a read-backed reason.",
    t ? "In accepted-turn maintenance, update memory only when the accepted assistant reply confirms a new long-term fact, current state, character change, unresolved matter, or next-turn carry-forward event." : "",
    t ? "Do not write user persona cards, status-bar text, UI metadata, model instructions, or the user message author as story facts unless the assistant reply clearly establishes the fact inside the RP." : "",
    t ? "Write global facts to `memory/state.md`; write character-specific changes to the matching `memory/characters/<角色名>.md`. If nothing material changed, skip writing and say why." : "",
    t && n ? "Map and atlas records are separate from written memory. They do not replace written memory; maintain textual facts and spatial changes in their own places." : "",
    n ? "Spatial records are split in two: `tavern.atlas/main` is the single world index, and each detailed scene map lives in its own place-named `tavern.map/<docId>` document." : "",
    "In manual manager chat, answer the user question first. Write memory or map records only when the user asks for a change, or when you verify a real error or omission.",
    "When a tool fails, adjust the path, arguments, or strategy based on the error. Do not repeat the same failing call unchanged."
  ], d = [
    "LS/Grep/Read inspect Tavern text sources: read-only `chat/`, read-only `worldbooks/`, and current-session `memory/` files. Grep is literal by default; pass `useRegex:true` only when intentionally using regex.",
    t ? "Edit and Write save memory changes only under `memory/state.md` and `memory/characters/<角色名>.md`. `chat/` and `worldbooks/` are evidence sources and are read-only." : "",
    n ? "MapDocs and MapInspect inspect scene maps and the world atlas. MapPatch saves one atomic spatial transaction." : "",
    r ? "EventInspect and EventPatch maintain the event direction pool. It is for future hooks only, not memory and not random encounters." : "",
    "Use tools when exact evidence, current records, ids, line numbers, map revisions, or saved changes matter. If a direct answer is enough, answer directly."
  ], f = [
    t ? "When explaining existing memory or answering a memory question, read the relevant memory file first. If the user is only asking, do not casually modify files." : "",
    t ? "To check memory against RP source text, gather evidence with Grep/Read under `chat/` first, then repair the file with Read or Edit." : "",
    t ? 'Use Grep with `path:"memory/"` to ask whether a fact is already in memory; use Grep with `path:"chat/"` to ask whether something actually happened in the RP source text. Match the search scope to the question.' : "",
    "If you know the message order, use Read on `chat/messages/<order>.md`. If you only know a keyword, use Grep under `chat/`. If you only need recent continuity, use Read on `chat/transcript.md` with `tail`.",
    n ? "When maintaining spatial records, start with MapInspect summary for the relevant docType. For maps use elements/element when you need ids; for atlas use locations/location/links/actors." : "",
    n ? "Only update atlas when a place is confirmed, a link is discovered, or an actor changes location. Only update maps when internal layout or actor coordinates change." : "",
    n ? "Map and atlas records only save confirmed spatial changes from this turn. Unknown rooms, future routes, and unconfirmed details should stay unwritten until RP confirms them." : "",
    t ? "Keep Markdown memory clear and restrained. Update facts that still hold, and do not write guesses, plans, or unconfirmed psychology as settled truth." : "",
    t ? "Message order, floor numbers, and manager run timing are backstage evidence coordinates only. Never record them as in-world time, dates, or story chronology unless the RP text itself says so." : ""
  ], m = n ? [
    "Spatial records have two layers: `tavern.atlas/main` is the single world index, and each `tavern.map/<docId>` is the local scene map for one stable place.",
    "Use `tavern.atlas/main` for place hierarchy, routes, and which location each actor is currently in. Atlas ops are only `upsert-location`, `remove-location`, `upsert-link`, `remove-link`, and `move-actor`.",
    "Use scene maps for the local layout of one place: boundaries, entrances, furniture, landmarks, hazards, items, and actor coordinates inside that place. Name scene-map docIds from the place identity, such as `home`, `office`, or `street_market`.",
    'When the player reaches a new named or trackable place, first update the atlas location and move the player with `move-actor` and `actorKey:"player"`. Then reuse that location’s `mapDocId`; if none exists and a local map is useful, create a new place-named map doc and store its id on the atlas location.',
    "Do not move the atlas player for small movement inside the same current place. For walking across a room, approaching a door, shifting around a street edge, or similar local motion, update the scene map actor coordinates or grow the current map instead.",
    "Map `activate:true` only switches or maintains the map tool’s current doc. It does not change atlas.activeLocationKey, player location, or visited status.",
    "Before scene switches, use MapDocs and the atlas location `mapDocId` to reuse the right scene map. Create a new scene map only for a clearly separate place that does not already have one. Do not overwrite an unrelated scene map just because the current scene changed.",
    'In atlas, create parent hierarchy when confirmed: city/district/building/floor/room/outdoor. Use `status:"mentioned"` for known but unvisited places and `status:"visited"` for explored places. Player movement automatically promotes the target location to visited.',
    "Within the same location or connected short-range spaces, grow the current map: adjacent rooms, yard, doorway, street edge, landmarks, paths, forest edges, or districts. Create/switch docs only for clearly named, independent interiors or distant locations.",
    "Read MapInspect summary first for the candidate doc, inspect `meta.status` and `meta.hint`, then decide whether to initialize, maintain incrementally, activate, or skip.",
    "The map is a spatial relation view of the current scene, not a board of floating text labels. Project confirmed spatial facts into a flat layout with clear boundaries, direction, focus, and proportion.",
    "When reading spatial information, first identify what defines the boundary, where entrances and exits are, where the current player or viewpoint focus is, what occupies interactive space, and which directions remain unexplored.",
    "Place the most certain anchors first, such as outer walls, a main road, a river, corridor edges, or the current location. Place other elements relative to those anchors.",
    "Use the default orientation north-up: north = smaller y, south = larger y, west = smaller x, east = larger x. If narration only gives left/right/front/back, choose a reasonable facing and keep it consistent within one map.",
    "For indoor scenes, use an outer-wall rect as the anchor and place furniture and doors inside it. For outdoor scenes, use roads or rivers as the backbone. For passage scenes, use two parallel boundary lines and a stretched `viewBox`.",
    "Let the scene pressure shape composition: key doors, threats, escape routes, intimate focus points, and important objects should explain the action instead of being evenly scattered.",
    "Translate named places into local geometry. A house becomes walls, doors, windows, yard, and road edge; a forest becomes paths, clearings, trees, rocks, or water; a tavern becomes rooms, counter, tables, exits, and actors.",
    'Use `cat:"terrain"` for ground or floor surfaces and use `material` to describe what they are made of, such as wood, stone, tile, grass, dirt, snow, carpet, blood, water, metal, rune, warm-light, cold-light, or shadow. Do not create `floor`, `ground`, `region`, or `subtype` categories.',
    'Use `cat:"light"` for confirmed light, glow, and shadow areas. Use `meta.mood` only when the scene tone is confirmed by narration; valid moods are neutral, warm, cold, dark, mystic, danger, and calm.',
    'Use `certainty:"inferred"` or `certainty:"unknown"` only for explicitly uncertain spatial facts. Do not use opacity, zIndex, rotation, visual scale, blur, custom fill colors, or other renderer styling in map patches. Atlas `scale` is only for location hierarchy such as city/district/building/floor/room/outdoor.',
    "Show uncertainty with edges, unexplored exits, partial paths, or sparse labels only when the RP implies unknown space. Do not invent a complete floor plan just to make the map look full.",
    "`meta.viewBox` is the camera; it does not move map elements. Move the player by changing the player `at`, then adjust `meta.viewBox` only if the camera should follow.",
    'If the chosen scene-map doc is still `uninitialized`, initialize it with one `meta + add` transaction as soon as the current turn clearly establishes that place. First appearance does not require a prior "change".',
    "When you initialize a new scene map for the current place, make sure the atlas has the matching location and that the location points to this map with `mapDocId`.",
    'Initialization must include at least one drawable spatial geometry element (`rect`, `circle`, `path`, `curve`, or `icon`) plus a player actor using `cat:"actor"` and `actorKey:"player"`. `text` is only a short label and cannot replace geometry. Name, `viewBox`, and labels alone are not a valid map.',
    "Atlas glyphs describe places. Scene maps describe local space. Do not put house/castle/village/forest/temple/shop icons inside a scene map; draw the local walls, doors, roads, furniture, hazards, objects, labels, and actor positions instead.",
    "For scene-map `icon`, use only small objects or abstract markers: chest/table/bed/barrel/door/stairs/portal/skull/trap/fire/tree/rock/water/heart/lips/lovers/cherish/love-letter/locked-heart/perfume when they match the object or mood; use x/o/+ and arrow-n/arrow-s/arrow-e/arrow-w/stairs-up/stairs-down only for markers and directions.",
    "When a map already exists, prefer growing the same connected map. Enlarge `meta.viewBox` when the visible scope needs more room.",
    "Let `meta.name` grow with the map scope. For example, a bedroom map may become a house map, then a glade or town-edge map as confirmed connected areas appear.",
    "For a clearly separate place with no useful spatial continuity, switch to an existing doc or create a new doc instead of replacing the previous map. If nothing changes spatially, skip the map update; if unsure, do not change it yet.",
    "Common updates: character movement = modify `at`; discovering a door or route = add door/road; an item appearing = add icon + label; an item being taken away = remove; a door opening = update the door geometry/icon/label only when the spatial fact changes; a new danger = add danger; camera follow = meta viewBox.",
    'Actors use `cat:"actor"` and optional `actorKey`. `actorKey` is the full-session identity key; id is only the element id inside this document. The runtime removes older actors with the same final key from other documents, so do not duplicate the same actor manually.',
    "Use `meta.name` as the map scope title. If you add an area label such as a room, house, road, or district name, attach it to that visible region instead of placing it at the top edge as a second title.",
    "Keep at least 20 units of spacing between elements. Place text labels 15-25 units beside what they describe instead of on top of the shape center.",
    "Before submitting, sanity-check the map: clear anchors, clear focus, drawable elements, a reasonable camera `viewBox`, and enough geometry to carry the map body.",
    "The map should record only spatial facts that already happened and are worth visualizing. Unconfirmed spatial information stays unwritten until confirmed."
  ] : [], v = r ? [
    "事件引擎维护的是“接下来可以去闯的大事”：它给故事准备新的可玩方向，不记录已经发生的事，不替代记忆，不替代地图，也不是随机遭遇。",
    "好的方向要有野心、对味、有第一步。用户看到它时应该自然产生“好，我去做”的冲动，而不是觉得那只是背景琐事或后台摘要。",
    "方向必须从已经建立的人物、地点、关系、世界事实、调性和用户偏好里长出来，再组合成还没上屏的新局面。",
    "大胆程度跟随当前故事和用户口味。情色、暴力、权谋、恐怖、家庭日常等故事里，方向要有同样的欲望、锋利度和尺度，不要洗成安全泛用的神秘线索。",
    "不要制造这些方向：已有伏笔的复述、记忆里已经记录的未解决事项、当前关系或当前场景的自然顺延、泛泛背景琐事、和当前故事断开的随机意外。",
    "想不到足够好的方向就保持空白。事件池宁可少，也不要用平庸线索填满。"
  ] : [], y = t ? [
    "Write memory like a case file for a future model: specific, restrained, and easy to carry forward.",
    "Character psychology, secrets, and future plans become facts only after the RP source text clearly establishes them.",
    "Separate what happened, what the user requested, what you inferred, and what is still unconfirmed. Long-term memory should contain established facts only.",
    "User messages are evidence, not a character sheet. Do not turn the user speaker label, UI status text, or out-of-character preferences into `memory/characters/<角色名>.md`.",
    "When character-specific material makes `memory/state.md` bloated, create or update the matching `memory/characters/<角色名>.md` and keep global memory concise."
  ] : [];
  return [
    "# 小白酒馆后台管理员",
    "",
    os("Role", s),
    "",
    os("Responsibilities", a),
    "",
    os("Current Session", o),
    "",
    os("Injected Context", l),
    i.length ? `
` + os("File Discipline", i) : "",
    "",
    os("Tool Use Guide", ["You may call multiple tools in one assistant turn. Independent reads may run in parallel; edits to the same file should be combined into one edit/patch call.", "If a tool returns an error, adjust the arguments, path, or strategy based on that error."]),
    "",
    os("Tool Layers", d),
    "",
    os("Work Loop", u),
    "",
    os("Selection Strategy", f),
    m.length ? `
` + os("Map Records", m) : "",
    v.length ? `
` + os("Event Orchestration", v) : "",
    y.length ? `
` + os("Memory Tone", y) : "",
    "",
    os("Output", [
      "Reply with a short, clear, user-facing operation summary.",
      "State what you verified, wrote, skipped, or left unchanged. If nothing changed, say that you checked and why you did not write.",
      "Only expand tool arguments, raw JSON, full Markdown, or protocol details when the user explicitly asks for debugging detail."
    ])
  ].filter(Boolean).join(`
`).replace(/\n{3,}/g, `

`).trim();
}
function Vu(e = "") {
  return String(e || "").trim();
}
var yT = /`?memory\/(?:state\.md|characters\/<角色名>\.md|session\.md|turns\/\*\.md)`?/i, yU = /facts and states that are still true|keep character state|do not keep transient events|旧三页规则|旧整套规则/i;
function HM(e = []) {
  return e.join(`
`).trim();
}
function zM() {
  return HM([
    "推荐结构：",
    "- `## 当前局面`：下一轮必须继承的时间、地点、在场关系、关键物件、持续限制。",
    "- `## 硬事实`：身份、归属、生死、规则、长期状态、已经确认的世界事实。",
    "- `## 未解决钩子`：未来会影响 RP 的承诺、冲突、秘密、危险或待办后果。",
    "",
    "写入准入：",
    "- 先问：这轮是否新增或改变了未来 RP 必须记住的事实？没有就跳过。",
    "- 只写会影响连续性的内容：位置变化、关系定性、物品归属、明确承诺、暴露秘密、持续伤势/限制、未解决冲突。",
    "- 不写普通对白、气氛描写、一次性动作、重复情绪、没有后果的暧昧/寒暄、状态栏、格式块、系统提示。",
    "",
    "写法约束：",
    "- 每条只写一个事实，短句即可；保留正式人名、地点、物件、具体动作和结果。",
    "- 优先合并或替换旧条目，不追加流水账；事件结束后只保留仍会影响未来的结果。",
    "- 人物动机、秘密、关系、承诺、债务和风险优先写入对应人物记忆；全局记忆只留必要摘要。",
    "- 修改前先读现有全局记忆；证据不确定时再查 `chat/`，不要凭印象补设定。"
  ]);
}
function KM() {
  return HM([
    "推荐结构：",
    "- `## 稳定状态`：身份、位置、身体状态、公开目标、持续限制。",
    "- `## 关系变化`：只记录已经发生变化或被明确确认的关系判断。",
    "- `## 秘密与动机`：已被 RP 明确建立的秘密、动机、执念或禁忌。",
    "- `## 承诺、债务与风险`：未来会影响互动的约定、亏欠、威胁、把柄。",
    "",
    "写入准入：",
    "- 只在人物出现实质变化时写：关系转向、身份揭示、目标改变、秘密暴露、承诺/债务成立、伤势/物品/限制持续存在。",
    "- 角色只是出场、说了普通话、做了一次性动作、短暂害羞/生气/沉默，都不值得写。",
    "- 不把猜测、计划、隐藏推理、用户指令、状态栏文字写成人物事实。",
    "",
    "写法约束：",
    "- 一条记忆要能被未来召回：写清谁、对谁、在哪里、因为什么、改变了什么。",
    "- 优先改旧条目，让人物档案保持短而准；不要按回合追加“最近发生了”。",
    "- 不为用户、玩家、消息作者或泛称代词建立人物档案；除非 RP 明确给了一个世界内角色名。",
    "- 修改前先读目标人物记忆；证据不确定时查 `chat/`，不要靠状态栏或印象补全。"
  ]);
}
function _U() {
  return "Maintain the current session's global long-term memory in `memory/state.md`. This target path and responsibility are fixed system contract; the user-editable preset only defines the file's internal format, content scope, and selection rules.";
}
function bU() {
  return "Maintain current-session character long-term memory in `memory/characters/<角色名>.md`. Use one file per character, with the filename as the entity name; the user-editable preset only defines the file's internal format, content scope, and selection rules.";
}
function _T(e, t) {
  const n = Vu(e);
  return !n || yU.test(n) ? t : yT.test(n) ? n.split(/\r?\n/).filter((r) => !yT.test(r)).join(`
`).trim() || t : n;
}
function wU(e = {}, t = {}) {
  const { includeMemory: n } = qM(t), r = Vu(e.statePrompt) || zM(), s = Vu(e.characterPrompt) || KM(), a = [vU(t)];
  return n && (r && a.push("", "## Global Memory Rules", _U(), r), s && a.push("", "## Character Memory Rules", bU(), s)), a.join(`
`).trim();
}
function SU(e = {}, t = {}) {
  return wU(e, t);
}
function yf() {
  return {
    id: jg,
    name: "默认助手预设",
    description: "记忆管理员的默认维护规则。",
    statePrompt: zM(),
    characterPrompt: KM()
  };
}
function uw(e = {}) {
  const t = yf();
  return {
    id: Vu(e.id) || t.id,
    name: Vu(e.name) || t.name,
    description: String(e.description || ""),
    statePrompt: _T(e.statePrompt, t.statePrompt),
    characterPrompt: _T(e.characterPrompt, t.characterPrompt),
    updatedAt: Number(e.updatedAt) || void 0
  };
}
var AU = "sillytavern-current-chat-prompt";
function WM() {
  return {
    id: AU,
    name: "酒馆当前聊天预设",
    source: "sillytavern",
    selected: !0,
    sections: []
  };
}
function TU(e = {}) {
  return {
    id: String(e.id || "").trim(),
    label: String(e.label || "").trim(),
    enabled: e.enabled !== !1,
    marker: e.marker === !0,
    role: e.role || "system",
    content: String(e.content || "").trim(),
    placement: e.placement || "beforeHistory",
    source: String(e.source || "manual").trim()
  };
}
function Dc(e = {}) {
  const t = WM();
  return {
    id: String(e.id || t.id).trim() || t.id,
    name: String(e.name || t.name).trim() || t.name,
    description: String(e.description || ""),
    version: String(e.version || ""),
    source: String(e.source || t.source || "sillytavern"),
    selected: e.selected !== !1,
    promptManager: e.promptManager,
    systemPrompt: e.systemPrompt,
    contextTemplate: e.contextTemplate,
    instructTemplate: e.instructTemplate,
    historySeparator: String(e.historySeparator || ""),
    sections: Array.isArray(e.sections) ? e.sections.map(TU).filter((n) => n.content || n.marker) : [],
    updatedAt: Number(e.updatedAt) || void 0
  };
}
var GM = "tavern.map", Bm = "main", VM = "__label__";
function Pb() {
  return [
    "This scene map is empty. Use it only after it has been chosen for a specific confirmed place, then initialize it with one minimal MapPatch. Use `activate:true` only to switch the map tool doc, not to move the player; player location belongs in `tavern.atlas/main` via move-actor.",
    'Indoor example: {"activate":true,"ops":[{"op":"meta","set":{"name":"<Place Name>","viewBox":[0,0,400,300],"status":"active","mood":"warm"}},{"op":"add","element":{"id":"room-terrain","at":[40,40],"rect":[320,220],"cat":"terrain","material":"wood"}},{"op":"add","element":{"id":"wall","at":[40,40],"rect":[320,220],"cat":"wall","material":"stone"}},{"op":"add","element":{"id":"firelight","at":[80,80],"circle":70,"cat":"light","material":"warm-light"}},{"op":"add","element":{"id":"door","at":[200,260],"icon":"o","cat":"door"}},{"op":"add","element":{"id":"player-room","actorKey":"player","at":[200,180],"icon":"o","text":"Player","cat":"actor"}}]}',
    'Outdoor example: {"activate":true,"ops":[{"op":"meta","set":{"name":"<Place Name>","viewBox":[0,0,800,600],"status":"active","mood":"neutral"}},{"op":"add","element":{"id":"ground","at":[400,300],"circle":150,"cat":"terrain","material":"grass"}},{"op":"add","element":{"id":"path","at":[0,300],"path":[[800,0]],"cat":"road","material":"dirt"}},{"op":"add","element":{"id":"player-road","actorKey":"player","at":[400,320],"icon":"o","text":"Player","cat":"actor"}}]}',
    "Requirement: include at least one spatial geometry element (`rect`, `circle`, `path`, `curve`, or `icon`). Do not send only `text` labels. After initialization, use only incremental `add` / `modify` / `remove` ops. If nothing changes spatially, skip the map update instead of resetting the document.",
    "Use MapDocs and MapInspect before scene switches. For connected places, grow the same document by adding adjacent spaces and enlarging `meta.viewBox`; for clearly separate named locations, find or create a place-named docId and link it from the atlas location `mapDocId` instead of replacing an old map.",
    "For `icon`, prefer semantic keys such as door/chest/table/bed/barrel/skull/trap/fire/tree/rock/water/heart/lips/lovers/cherish/love-letter/locked-heart/perfume; reserve x/o/+ and arrows for abstract markers.",
    "Use `meta.name` as the map scope title. Area labels are allowed only when they mark a concrete visible region; place them inside or next to that region, not at the top edge as a second title.",
    "`viewBox` is the camera. Changing it does not move any element. Move the player by updating the player `at`, then adjust `meta.viewBox` only if the camera should follow.",
    'Elements use `at:[x,y]` plus one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. With `at`, `path` and `curve` points are relative offsets. Without `at`, points are absolute coordinates and the first point becomes the anchor. For `cat:"actor"`, use `actorKey` as the full-session identity key; if omitted, id is the fallback.',
    "Use material/mood/certainty only for confirmed semantic facts. Material is a closed enum: unknown/wood/stone/tile/carpet/blood/water/grass/dirt/snow/metal/rune/warm-light/cold-light/shadow. Use terrain for ground/floor and light for glow/shadow; do not use region/subtype/opacity/custom fill colors or patch only for aesthetics."
  ].join(" ");
}
function Ju() {
  return {
    meta: {
      name: null,
      viewBox: null,
      theme: "parchment",
      status: "uninitialized",
      mood: "neutral",
      hint: Pb()
    },
    elements: []
  };
}
function Na(e) {
  return String(e || "").startsWith(VM);
}
function To(e) {
  return `${VM}${e}`;
}
function JM(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? !1 : e.meta?.status === "uninitialized";
}
var YM = "tavern.atlas", XM = "main";
function jM() {
  return {
    version: 1,
    locations: [],
    links: [],
    actors: []
  };
}
var Oc = {
  memoryArchiving: !0,
  cartographyEngine: !0,
  actionChecks: !0,
  randomEncounters: !0,
  questOrchestration: !1
}, QM = [
  "memoryArchiving",
  "cartographyEngine",
  "actionChecks",
  "randomEncounters",
  "questOrchestration"
], EU = {
  "silent-operations": "暗中行事",
  "fate-arbitration": "命运仲裁",
  "narrative-orchestration": "叙事编排"
}, Rb = [
  {
    key: "memoryArchiving",
    tier: "silent-operations",
    icon: "🧠",
    title: "记忆存档",
    summary: "",
    description: "授权代理人管理你的长期记忆。它将默默维护、压缩和召回上下文——确保故事永不遗忘。"
  },
  {
    key: "cartographyEngine",
    tier: "silent-operations",
    icon: "🗺️",
    title: "制图引擎",
    summary: "",
    description: "允许代理人在你探索时生成、更新和渲染空间数据。随着叙事展开，区域、地标和兴趣点将在附录面板中逐渐显现。"
  },
  {
    key: "actionChecks",
    tier: "fate-arbitration",
    icon: "🎲",
    title: "行动检定",
    summary: "",
    description: "当你尝试不确定的事——说服陌生人、翻越高墙、破译符文——由骰子裁决，而非AI。一次真随机掷骰仲裁结果，故事顺从命运。"
  },
  {
    key: "randomEncounters",
    tier: "fate-arbitration",
    icon: "⚔️",
    title: "随机遭遇",
    summary: "",
    description: "允许世界给你意外。每隔一段时间，一颗无形的骰子在暗中滚动。当它落定，意料之外的事降临——路上的伏击、天空中的异兆、门缝下塞进的一封信。"
  },
  {
    key: "questOrchestration",
    tier: "narrative-orchestration",
    icon: "📜",
    title: "织线者",
    summary: "",
    description: "让代理人感知你故事中的暗流，浮现接下来可能发生的事。当势头停滞，一根线索显现——追随它、忽视它，或任它消散。"
  }
], CU = {
  memoryArchiving: {
    includeMemoryFiles: !0,
    automaticManagerWork: !0,
    managerPromptMemory: !0
  },
  cartographyEngine: {
    includeStructuredStates: !0,
    automaticManagerWork: !0,
    managerPromptCartography: !0
  },
  actionChecks: { includeActionChecks: !0 },
  randomEncounters: { includeRandomEncounters: !0 },
  questOrchestration: { automaticManagerWork: !0 }
};
function Fs(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {};
  return {
    memoryArchiving: "memoryArchiving" in t ? !!t.memoryArchiving : Oc.memoryArchiving,
    cartographyEngine: "cartographyEngine" in t ? !!t.cartographyEngine : Oc.cartographyEngine,
    actionChecks: "actionChecks" in t ? !!t.actionChecks : Oc.actionChecks,
    randomEncounters: "randomEncounters" in t ? !!t.randomEncounters : Oc.randomEncounters,
    questOrchestration: "questOrchestration" in t ? !!t.questOrchestration : Oc.questOrchestration
  };
}
function Qg(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? !1 : QM.some((t) => Object.prototype.hasOwnProperty.call(e, t));
}
function Um(e, t) {
  return Fs({
    ...Fs(e),
    ...Qg(t) ? t : {}
  });
}
function IU(e) {
  const t = Fs(e);
  return Object.values(t).filter(Boolean).length;
}
function xU(e) {
  return Rb.find((t) => t.key === e);
}
function _f(e) {
  const t = Fs(e), n = QM.filter((r) => t[r]);
  return n.reduce((r, s) => {
    const a = CU[s];
    return a ? {
      ...r,
      includeMemoryFiles: r.includeMemoryFiles || a.includeMemoryFiles === !0,
      includeStructuredStates: r.includeStructuredStates || a.includeStructuredStates === !0,
      includeActionChecks: r.includeActionChecks || a.includeActionChecks === !0,
      includeRandomEncounters: r.includeRandomEncounters || a.includeRandomEncounters === !0,
      includeQuestOrchestration: r.includeQuestOrchestration || s === "questOrchestration",
      hasAutomaticManagerWork: r.hasAutomaticManagerWork || a.automaticManagerWork === !0,
      managerPromptOptions: {
        includeMemory: r.managerPromptOptions.includeMemory || a.managerPromptMemory === !0,
        includeCartography: r.managerPromptOptions.includeCartography || a.managerPromptCartography === !0,
        includeQuestOrchestration: r.managerPromptOptions.includeQuestOrchestration || s === "questOrchestration"
      }
    } : r;
  }, {
    contract: t,
    enabledKeys: n,
    includeMemoryFiles: !1,
    includeStructuredStates: !1,
    includeActionChecks: !1,
    includeRandomEncounters: !1,
    includeQuestOrchestration: !1,
    hasAutomaticManagerWork: !1,
    managerPromptOptions: {
      includeMemory: !1,
      includeCartography: !1,
      includeQuestOrchestration: !1
    }
  });
}
var kU = class extends Gu {
  sessions;
  messages;
  managerMessages;
  meta;
  presets;
  managerRuns;
  memoryFiles;
  memorySnapshots;
  memoryIndexes;
  assistantPresets;
  managerMemorySnapshots;
  stateDocuments;
  statePatches;
  managerStateSnapshots;
  tasks;
  taskSnapshots;
  managerTaskSnapshots;
  taskFingerprintStates;
  constructor() {
    super("LittleWhiteBox_Tavern"), this.version(1).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      turnSummaries: "id, sessionId, episodeId, turn, userOrder, assistantOrder, status, updatedAt",
      episodeSummaries: "id, sessionId, status, updatedAt, startTurn, endTurn",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(2).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      turnSummaries: "id, sessionId, turn, userOrder, assistantOrder, status, updatedAt",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(3).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(4).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(5).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: null,
      memorySnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(6).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: null,
      memorySnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt",
      tasks: "[sessionId+id], sessionId, status, fingerprint, updatedOrder, updatedAt",
      taskSnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      managerTaskSnapshots: "managerRunId, sessionId, updatedAt",
      taskFingerprintStates: "sessionId, updatedAt"
    }), this.version(7).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: null,
      memorySnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt",
      tasks: "[sessionId+id], sessionId, status, fingerprint, updatedOrder, updatedAt",
      taskSnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      managerTaskSnapshots: "managerRunId, sessionId, updatedAt",
      taskFingerprintStates: "sessionId, updatedAt"
    }), this.version(8).stores({
      sessions: "id, updatedAt, characterKey, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryStateSnapshots: null,
      memorySnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt",
      tasks: "[sessionId+id], sessionId, status, fingerprint, updatedOrder, updatedAt",
      taskSnapshots: "[sessionId+floor], sessionId, floor, createdAt",
      managerTaskSnapshots: "managerRunId, sessionId, updatedAt",
      taskFingerprintStates: "sessionId, updatedAt"
    });
  }
}, Dt = new kU(), Ct = Dt.sessions, Qt = Dt.messages, Ss = Dt.managerMessages, Eo = Dt.meta, Cve = Dt.presets, ur = Dt.managerRuns, Tn = Dt.memoryFiles, Da = Dt.memorySnapshots, As = Dt.memoryIndexes, xl = Dt.assistantPresets, zr = Dt.managerMemorySnapshots, Cr = Dt.stateDocuments, yl = Dt.statePatches, Ru = Dt.managerStateSnapshots, $n = Dt.tasks, Oa = Dt.taskSnapshots, Ir = Dt.managerTaskSnapshots, Fa = Dt.taskFingerprintStates, Kr = Gu;
function un() {
  return Date.now();
}
function Zg(e = "tavern-session") {
  return `${e}-${un()}-${Math.random().toString(36).slice(2, 8)}`;
}
function MU(e = "", t = "小白酒馆会话") {
  return String(e || "").trim().slice(0, 120) || t;
}
function bT(e = "") {
  const t = String(e || "").replace(/\s*[·-]\s*小白酒馆\s*$/g, "").replace(/\s*[·-]\s*会话\s*$/g, "").replace(/^小白酒馆会话$/g, "").replace(/\s*·\s*第\s*\d+\s*轮\s*·\s*\d+\s*条可用消息\s*$/g, "").trim();
  return /^(sillytavern\s+system|system)\b/i.test(t) ? "" : t;
}
function PU(e = Bm) {
  const t = String(e || "main").trim() || "main";
  return /^[\w.-]{1,80}$/i.test(t) ? t : Bm;
}
function RU(e) {
  return JSON.parse(JSON.stringify(e));
}
function nn(e, t) {
  if (e === void 0) return t;
  try {
    return JSON.parse(JSON.stringify(e));
  } catch {
    return t;
  }
}
function _l(e, t = 12) {
  return (Array.isArray(e) ? e : typeof e == "string" ? [e] : []).map((n) => String(n || "").trim()).filter(Boolean).slice(0, t);
}
function NU(e) {
  const t = String(e || "").trim();
  return t === "assistant" || t === "tool" || t === "system" ? t : "user";
}
function ZM(e) {
  const t = Array.isArray(e.toolCalls) ? e.toolCalls : [], n = "tool_calls" in e && Array.isArray(e.tool_calls) ? e.tool_calls : [], r = [...t.map((s) => ({
    id: String(s?.id || ""),
    name: String(s?.name || ""),
    arguments: String(s?.arguments || "{}")
  })), ...n.map((s) => ({
    id: String(s?.id || ""),
    name: String(s?.function?.name || ""),
    arguments: String(s?.function?.arguments || "{}")
  }))].filter((s) => s.name);
  return r.length ? r : void 0;
}
function cw(e) {
  return k1(e);
}
function za(e) {
  return {
    ...e,
    runtimeEvents: cw(e.runtimeEvents)
  };
}
function e3(e) {
  return [
    "pending",
    "queued",
    "running",
    "completed",
    "failed",
    "cancelled",
    "superseded",
    "rolled_back"
  ].includes(String(e || "")) ? e : "queued";
}
function $U(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return {};
  const t = {};
  return Object.entries(e).forEach(([n, r]) => {
    if (!n || !r || typeof r != "object" || Array.isArray(r)) return;
    const s = {}, a = r;
    [
      "stickyUntilTurn",
      "cooldownUntilTurn",
      "delayUntilTurn"
    ].forEach((o) => {
      const l = Number(a[o]);
      Number.isFinite(l) && (s[o] = l);
    }), Object.keys(s).length && (t[n] = s);
  }), t;
}
function LU(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return null;
  const t = e, n = {}, r = Number(t.hash), s = Number(t.start), a = Number(t.end);
  return Number.isFinite(r) && (n.hash = r), Number.isFinite(s) && (n.start = s), Number.isFinite(a) && (n.end = a), t.protected === !0 && (n.protected = !0), n.hash !== void 0 || n.start !== void 0 || n.end !== void 0 || n.protected ? n : null;
}
function DU(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return {
    sticky: {},
    cooldown: {}
  };
  const t = e, n = (r) => {
    if (!r || typeof r != "object" || Array.isArray(r)) return {};
    const s = {};
    return Object.entries(r).forEach(([a, o]) => {
      const l = LU(o);
      a && l && (s[a] = l);
    }), s;
  };
  return {
    sticky: n(t.sticky),
    cooldown: n(t.cooldown)
  };
}
function Xr(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {}, n = PU(t.activeMapDocId || "main");
  return {
    ...t,
    turn: Math.max(0, Number(t.turn) || 0),
    contextWindowStartOrder: Math.max(0, Math.floor(Number(t.contextWindowStartOrder) || 0)),
    activeMapDocId: n,
    contract: Fs(t.contract),
    worldEntryStates: $U(t.worldEntryStates),
    nativeWorldInfoTimedState: DU(t.nativeWorldInfoTimedState)
  };
}
function Hd(e, t) {
  return !!e && typeof e == "object" && !Array.isArray(e) && Object.prototype.hasOwnProperty.call(e, t);
}
function dw(e = {}, t = {}) {
  const n = RU(e || {});
  return Object.entries(t || {}).forEach(([r, s]) => {
    !r || !s || typeof s != "object" || (n[r] = {
      ...n[r] || {},
      ...s
    });
  }), n;
}
async function t3(e = {}) {
  const t = un(), n = bT(e.characterName || ""), r = bT(e.title || ""), s = {
    id: String(e.id || Zg()),
    title: MU(r, n || "未选择角色"),
    characterKey: String(e.characterKey || ""),
    characterName: n,
    createdAt: Number(e.createdAt) || t,
    updatedAt: t,
    contextSnapshot: nn(e.contextSnapshot, void 0),
    buildSnapshot: nn(e.buildSnapshot, void 0),
    chatPresetId: String(e.chatPresetId || e.presetId || ""),
    chatPresetName: String(e.chatPresetName || e.presetName || ""),
    presetId: String(e.presetId || ""),
    presetName: String(e.presetName || ""),
    summary: String(e.summary || ""),
    state: nn(Xr(e.state || {}), {})
  };
  return await Dt.transaction("rw", Ct, Eo, Cr, async () => {
    await Ct.put(s), await Eo.put({
      key: "selectedSessionId",
      value: s.id,
      updatedAt: t
    }), await Uf(s.id, { touchSession: !1 });
  }), s;
}
async function OU() {
  return Ct.orderBy("updatedAt").reverse().toArray();
}
async function FU() {
  const e = await Eo.get("selectedSessionId");
  return String(e?.value || "").trim();
}
async function BU(e = "") {
  const t = String(e || "").trim();
  return await Eo.put({
    key: "selectedSessionId",
    value: t,
    updatedAt: un()
  }), t;
}
async function qa(e = "") {
  const t = String(e || "").trim();
  return t && await Ct.get(t) || null;
}
async function UU(e = "") {
  const t = String(e || "").trim();
  return !t || !await qa(t) ? 0 : (await Dt.transaction("rw", Ct, Qt, Ss, ur, zr, Ru, Ir, Tn, Da, As, Cr, yl, $n, Oa, Fa, Eo, async () => {
    const [n, r, s, a, o, l, i, u, d, f, m, v, y, S] = await Promise.all([
      Qt.where("sessionId").equals(t).toArray(),
      Ss.where("sessionId").equals(t).toArray(),
      ur.where("sessionId").equals(t).toArray(),
      zr.where("sessionId").equals(t).toArray(),
      Ru.where("sessionId").equals(t).toArray(),
      Ir.where("sessionId").equals(t).toArray(),
      Tn.where("sessionId").equals(t).toArray(),
      Da.where("sessionId").equals(t).toArray(),
      As.where("sessionId").equals(t).toArray(),
      Cr.where("sessionId").equals(t).toArray(),
      yl.where("sessionId").equals(t).toArray(),
      $n.where("sessionId").equals(t).toArray(),
      Oa.where("sessionId").equals(t).toArray(),
      Fa.where("sessionId").equals(t).toArray()
    ]);
    await Promise.all([
      n.length ? Qt.bulkDelete(n.map((I) => [I.sessionId, I.order])) : 0,
      r.length ? Ss.bulkDelete(r.map((I) => [I.sessionId, I.order])) : 0,
      s.length ? ur.bulkDelete(s.map((I) => I.id)) : 0,
      a.length ? zr.bulkDelete(a.map((I) => [I.managerRunId, I.path])) : 0,
      o.length ? Ru.bulkDelete(o.map((I) => [
        I.managerRunId,
        I.docType,
        I.docId
      ])) : 0,
      l.length ? Ir.bulkDelete(l.map((I) => I.managerRunId)) : 0,
      i.length ? Tn.bulkDelete(i.map((I) => [I.sessionId, I.path])) : 0,
      u.length ? Da.bulkDelete(u.map((I) => [I.sessionId, I.floor])) : 0,
      d.length ? As.bulkDelete(d.map((I) => [I.sessionId, I.kind])) : 0,
      f.length ? Cr.bulkDelete(f.map((I) => [
        I.sessionId,
        I.docType,
        I.docId
      ])) : 0,
      m.length ? yl.bulkDelete(m.map((I) => I.id)) : 0,
      v.length ? $n.bulkDelete(v.map((I) => [I.sessionId, I.id])) : 0,
      y.length ? Oa.bulkDelete(y.map((I) => [I.sessionId, I.floor])) : 0,
      S.length ? Fa.bulkDelete(S.map((I) => I.sessionId)) : 0
    ]), await Ct.delete(t);
    const x = await Eo.get("selectedSessionId");
    if (String(x?.value || "") === t) {
      const [I] = await Ct.orderBy("updatedAt").reverse().toArray();
      await Eo.put({
        key: "selectedSessionId",
        value: I?.id || "",
        updatedAt: un()
      });
    }
  }), 1);
}
async function n3(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await qa(n);
  if (!r) return null;
  const s = un(), a = Xr(r.state || {}), o = Xr(t), l = {
    ...a,
    ...t,
    turn: Math.max(0, Number(t.turn ?? a.turn) || 0),
    contract: Hd(t, "contract") ? Um(a.contract, Qg(t.contract) ? t.contract : void 0) : a.contract,
    worldEntryStates: dw(a.worldEntryStates || {}, o.worldEntryStates || {}),
    nativeWorldInfoTimedState: Hd(t, "nativeWorldInfoTimedState") ? o.nativeWorldInfoTimedState : a.nativeWorldInfoTimedState,
    activeMapDocId: Hd(t, "activeMapDocId") ? o.activeMapDocId : a.activeMapDocId
  };
  return await Ct.update(n, {
    state: nn(l, {}),
    updatedAt: s,
    buildSnapshot: nn(t.lastBuildSnapshot || r.buildSnapshot, void 0)
  }), await qa(n);
}
async function r3(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await qa(n);
  if (!r) return null;
  const s = un(), a = Xr(r.state || {}), o = Xr(t), l = {
    ...t,
    turn: Math.max(0, Number(o.turn) || 0),
    activeMapDocId: Hd(t, "activeMapDocId") ? o.activeMapDocId : a.activeMapDocId || "main",
    contract: Hd(t, "contract") ? Um(a.contract, Qg(t.contract) ? t.contract : void 0) : a.contract,
    worldEntryStates: o.worldEntryStates || {},
    nativeWorldInfoTimedState: o.nativeWorldInfoTimedState
  };
  return await Ct.update(n, {
    state: nn(l, {}),
    updatedAt: s,
    buildSnapshot: nn(l.lastBuildSnapshot || r.buildSnapshot, void 0)
  }), await qa(n);
}
async function Nb(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await qa(n);
  if (!r) return null;
  const s = "contextSnapshot" in t ? t.contextSnapshot : r.contextSnapshot, a = s?.character || {}, o = {
    updatedAt: un(),
    contextSnapshot: nn(s, void 0),
    buildSnapshot: nn("buildSnapshot" in t ? t.buildSnapshot : r.buildSnapshot, void 0),
    chatPresetId: "chatPresetId" in t ? String(t.chatPresetId || "") : r.chatPresetId,
    chatPresetName: "chatPresetName" in t ? String(t.chatPresetName || "") : r.chatPresetName,
    presetId: "presetId" in t ? String(t.presetId || "") : r.presetId,
    presetName: "presetName" in t ? String(t.presetName || "") : r.presetName,
    characterKey: "characterKey" in t ? String(t.characterKey || "") : String(a.characterKey || r.characterKey || ""),
    characterName: "characterName" in t ? String(t.characterName || "") : String(a.name || r.characterName || "")
  };
  return await Ct.update(n, o), await qa(n);
}
async function hd(e, t) {
  const n = String(e || "").trim();
  if (!n) throw new Error("session_required");
  const r = un();
  let s = null;
  if (await Dt.transaction("rw", Qt, Ct, async () => {
    const a = await o3(n);
    s = {
      sessionId: n,
      order: (a ? Math.floor(Number(a.order)) : -1) + 1,
      role: String(t.role || ""),
      content: String(t.content || ""),
      name: t.name ? String(t.name) : void 0,
      error: t.error === !0,
      createdAt: r,
      provider: "provider" in t ? String(t.provider || "") : void 0,
      model: "model" in t ? String(t.model || "") : void 0,
      finishReason: "finishReason" in t ? String(t.finishReason || "") : void 0,
      thoughts: "thoughts" in t ? nn(t.thoughts, void 0) : void 0,
      providerPayload: "providerPayload" in t ? nn(t.providerPayload, void 0) : void 0,
      contextSnapshot: "contextSnapshot" in t ? nn(t.contextSnapshot, void 0) : void 0,
      buildSnapshot: "buildSnapshot" in t ? nn(t.buildSnapshot, void 0) : void 0,
      chatPresetId: "chatPresetId" in t ? String(t.chatPresetId || "") : String(t.presetId || ""),
      chatPresetName: "chatPresetName" in t ? String(t.chatPresetName || "") : String(t.presetName || ""),
      presetId: "presetId" in t ? String(t.presetId || "") : void 0,
      presetName: "presetName" in t ? String(t.presetName || "") : void 0,
      requestSnapshot: "requestSnapshot" in t ? nn(t.requestSnapshot, void 0) : void 0,
      runtimeEvents: "runtimeEvents" in t ? cw(t.runtimeEvents) : []
    }, await Qt.put(s), await Ct.update(n, { updatedAt: r });
  }), !s) throw new Error("message_append_failed");
  return za(s);
}
async function zd(e = "", t = -1, n) {
  const r = String(e || "").trim(), s = Number(t);
  if (!r || !Number.isInteger(s) || s < 0 || !await Qt.get([r, s])) return null;
  const a = {};
  "content" in n && (a.content = String(n.content || "")), "error" in n && (a.error = n.error === !0), "thoughts" in n && (a.thoughts = nn(n.thoughts, void 0)), "runtimeEvents" in n && (a.runtimeEvents = cw(n.runtimeEvents)), "contextSnapshot" in n && (a.contextSnapshot = nn(n.contextSnapshot, void 0)), "buildSnapshot" in n && (a.buildSnapshot = nn(n.buildSnapshot, void 0)), "chatPresetId" in n && (a.chatPresetId = String(n.chatPresetId || "")), "chatPresetName" in n && (a.chatPresetName = String(n.chatPresetName || "")), "presetId" in n && (a.presetId = String(n.presetId || "")), "presetName" in n && (a.presetName = String(n.presetName || "")), "requestSnapshot" in n && (a.requestSnapshot = nn(n.requestSnapshot, void 0)), "provider" in n && (a.provider = String(n.provider || "")), "model" in n && (a.model = String(n.model || "")), "finishReason" in n && (a.finishReason = String(n.finishReason || "")), await Qt.update([r, s], a), await Ct.update(r, { updatedAt: un() });
  const o = await Qt.get([r, s]);
  return o ? za(o) : null;
}
async function s3(e = "", t = []) {
  const n = String(e || "").trim(), r = [...new Set((Array.isArray(t) ? t : []).map((a) => Number(a)).filter((a) => Number.isInteger(a) && a >= 0))];
  if (!n || !r.length) return 0;
  const s = [];
  return await Promise.all(r.map(async (a) => {
    await Qt.get([n, a]) && s.push([n, a]);
  })), s.length ? (await Dt.transaction("rw", Qt, Ct, async () => {
    await Qt.bulkDelete(s), await Ct.update(n, { updatedAt: un() });
  }), s.length) : 0;
}
async function a3(e = "") {
  const t = String(e || "").trim();
  return t ? (await Qt.where("sessionId").equals(t).sortBy("order")).map(za) : [];
}
async function bl(e = "", t = -1) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isInteger(r) || r < 0) return null;
  const s = await Qt.get([n, r]);
  return s ? za(s) : null;
}
async function o3(e = "") {
  const t = String(e || "").trim();
  if (!t) return null;
  const n = await Qt.where("[sessionId+order]").between([t, Kr.minKey], [t, Kr.maxKey]).reverse().first();
  return n ? za(n) : null;
}
async function fw(e = "") {
  const t = String(e || "").trim();
  if (!t) return null;
  const n = await Qt.where("[sessionId+order]").between([t, Kr.minKey], [t, Kr.maxKey]).reverse().filter((r) => r.role === "assistant" && r.error !== !0).first();
  return n ? Math.floor(Number(n.order)) : null;
}
async function ev(e = "", t = 12, n = 0) {
  const r = String(e || "").trim();
  if (!r) return {
    messages: [],
    total: 0
  };
  const s = Math.max(1, Math.min(1e3, Math.floor(Number(t) || 12))), a = Math.max(0, Math.floor(Number(n) || 0));
  return await Dt.transaction("r", Qt, async () => {
    const [o, l] = await Promise.all([Qt.where("[sessionId+order]").between([r, Kr.minKey], [r, Kr.maxKey]).reverse().offset(a).limit(s).toArray(), Qt.where("sessionId").equals(r).count()]);
    return {
      messages: o.reverse().map(za),
      total: l
    };
  });
}
async function qU(e = "", t = 12, n = 0) {
  const r = await ev(e, t, n);
  return {
    ...r,
    loadedStartOrder: r.messages[0]?.order ?? null,
    loadedEndOrder: r.messages.at(-1)?.order ?? null
  };
}
async function HU(e = "", t = 0, n = Number.POSITIVE_INFINITY, r = 1e3, s = 0) {
  const a = String(e || "").trim();
  if (!a) return [];
  const o = Math.max(0, Math.floor(Number(t) || 0)), l = Number.isFinite(Number(n)), i = l ? Math.max(o, Math.floor(Number(n) || o)) : Number.POSITIVE_INFINITY, u = l ? i : Kr.maxKey, d = Math.max(1, Math.min(1e3, Math.floor(Number(r) || 1e3))), f = Math.max(0, Math.floor(Number(s) || 0));
  return (await Qt.where("[sessionId+order]").between([a, o], [a, u], !0, !0).offset(f).limit(d).toArray()).map(za);
}
async function Nu(e = "", t = 0, n = Number.POSITIVE_INFINITY, r = 1e3, s = 0) {
  const a = String(e || "").trim();
  if (!a) return {
    messages: [],
    total: 0
  };
  const o = Math.max(0, Math.floor(Number(t) || 0)), l = Number.isFinite(Number(n)), i = l ? Math.max(o, Math.floor(Number(n) || o)) : Number.POSITIVE_INFINITY, u = l ? i : Kr.maxKey, d = Math.max(1, Math.min(1e3, Math.floor(Number(r) || 1e3))), f = Math.max(0, Math.floor(Number(s) || 0));
  return await Dt.transaction("r", Qt, async () => {
    const m = () => Qt.where("[sessionId+order]").between([a, o], [a, u], !0, !0), [v, y] = await Promise.all([m().offset(f).limit(d).toArray(), m().count()]);
    return {
      messages: v.map(za),
      total: y
    };
  });
}
async function hw(e = "", t = 0) {
  const n = String(e || "").trim(), r = Math.max(0, Math.floor(Number(t) || 0));
  return n ? (await Qt.where("[sessionId+order]").between([n, r], [n, Kr.maxKey], !0, !0).primaryKeys()).map((s) => Array.isArray(s) ? s[1] : void 0).map((s) => Math.floor(Number(s))).filter((s) => Number.isInteger(s) && s >= 0) : [];
}
async function wT(e = "", t = Number.POSITIVE_INFINITY, n = 5) {
  const r = String(e || "").trim();
  if (!r) return [];
  const s = Math.max(1, Math.min(100, Math.floor(Number(n) || 5))), a = Number.isFinite(Number(t)), o = Math.floor(Number(t) || 0);
  if (a && o <= 0) return [];
  const l = a ? Math.max(0, o - 1) : Kr.maxKey;
  return (await Qt.where("[sessionId+order]").between([r, Kr.minKey], [r, l], !0, !0).reverse().filter((i) => i.role === "user").limit(s).toArray()).reverse().map(za);
}
async function Kp(e = "", t = Number.POSITIVE_INFINITY) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = Number.isFinite(Number(t)) ? Math.max(0, Math.floor(Number(t) || 0)) : Kr.maxKey, s = await Qt.where("[sessionId+order]").between([n, Kr.minKey], [n, r], !0, !0).reverse().filter((a) => a.role === "user").first();
  return s ? za(s) : null;
}
async function zU(e = "") {
  const t = String(e || "").trim();
  return t ? Qt.where("sessionId").equals(t).count() : 0;
}
async function KU(e = "") {
  const t = String(e || "").trim();
  return t && await Ss.where("[sessionId+order]").between([t, Kr.minKey], [t, Kr.maxKey]).reverse().first() || null;
}
async function Gh(e, t) {
  const n = String(e || "").trim();
  if (!n) throw new Error("session_required");
  const r = un();
  let s = null;
  if (await Dt.transaction("rw", Ss, Ct, async () => {
    const a = await KU(n);
    s = {
      sessionId: n,
      order: (a ? Math.floor(Number(a.order)) : -1) + 1,
      role: NU(t.role),
      content: String(t.content || ""),
      name: t.name ? String(t.name) : void 0,
      error: t.error === !0,
      createdAt: r,
      updatedAt: r,
      provider: "provider" in t ? String(t.provider || "") : void 0,
      model: "model" in t ? String(t.model || "") : void 0,
      finishReason: "finishReason" in t ? String(t.finishReason || "") : void 0,
      thoughts: "thoughts" in t ? nn(t.thoughts, void 0) : void 0,
      providerPayload: "providerPayload" in t ? nn(t.providerPayload, void 0) : void 0,
      toolCalls: ZM(t),
      toolCallId: String(t.toolCallId || t.tool_call_id || "").trim() || void 0,
      toolName: String(t.toolName || "").trim() || void 0,
      toolDisplay: "toolDisplay" in t ? nn(t.toolDisplay, void 0) : void 0
    }, await Ss.put(s), await Ct.update(n, { updatedAt: r });
  }), !s) throw new Error("manager_message_append_failed");
  return s;
}
async function WU(e = "", t = -1, n) {
  const r = String(e || "").trim(), s = Number(t);
  if (!r || !Number.isInteger(s) || s < 0 || !await Ss.get([r, s])) return null;
  const a = un(), o = { updatedAt: a };
  return "content" in n && (o.content = String(n.content || "")), "error" in n && (o.error = n.error === !0), "provider" in n && (o.provider = String(n.provider || "")), "model" in n && (o.model = String(n.model || "")), "finishReason" in n && (o.finishReason = String(n.finishReason || "")), "thoughts" in n && (o.thoughts = nn(n.thoughts, void 0)), "providerPayload" in n && (o.providerPayload = nn(n.providerPayload, void 0)), "toolCalls" in n && (o.toolCalls = ZM(n)), "toolCallId" in n && (o.toolCallId = String(n.toolCallId || "").trim()), "toolName" in n && (o.toolName = String(n.toolName || "").trim()), "toolDisplay" in n && (o.toolDisplay = nn(n.toolDisplay, void 0)), n.clearProtocolPayload === !0 && (o.providerPayload = void 0, o.toolCalls = void 0, o.toolCallId = void 0, o.toolName = void 0, o.toolDisplay = void 0), await Ss.update([r, s], o), await Ct.update(r, { updatedAt: a }), await Ss.get([r, s]) || null;
}
async function $b(e = "", t = []) {
  const n = String(e || "").trim(), r = [...new Set((Array.isArray(t) ? t : []).map((a) => Number(a)).filter((a) => Number.isInteger(a) && a >= 0))];
  if (!n || !r.length) return 0;
  const s = [];
  return await Promise.all(r.map(async (a) => {
    await Ss.get([n, a]) && s.push([n, a]);
  })), s.length ? (await Dt.transaction("rw", Ss, Ct, async () => {
    await Ss.bulkDelete(s), await Ct.update(n, { updatedAt: un() });
  }), s.length) : 0;
}
async function Ls(e = "") {
  const t = String(e || "").trim();
  return t ? Ss.where("sessionId").equals(t).sortBy("order") : [];
}
async function qm(e = {}) {
  const t = String(e.sessionId || "").trim();
  if (!t) throw new Error("manager_run_session_required");
  const n = un(), r = {
    id: String(e.id || Zg("manager-run")),
    sessionId: t,
    turn: Math.max(0, Number(e.turn) || 0),
    userOrder: Number.isInteger(Number(e.userOrder)) ? Number(e.userOrder) : -1,
    assistantOrder: Number.isInteger(Number(e.assistantOrder)) ? Number(e.assistantOrder) : -1,
    trigger: String(e.trigger || "accepted_turn"),
    status: e3(e.status),
    provider: String(e.provider || ""),
    model: String(e.model || ""),
    inputSummary: String(e.inputSummary || ""),
    outputText: String(e.outputText || ""),
    parsedAction: String(e.parsedAction || ""),
    toolTrace: "toolTrace" in e ? nn(e.toolTrace, void 0) : void 0,
    changedFiles: _l(e.changedFiles, 100),
    changedStates: _l(e.changedStates, 100),
    changedTasks: _l(e.changedTasks, 100),
    error: String(e.error || ""),
    createdAt: Number(e.createdAt) || n,
    updatedAt: n
  };
  return await ur.put(r), await Ct.update(t, { updatedAt: n }), r;
}
async function yr(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await ur.get(n);
  if (!r) return null;
  const s = { updatedAt: un() };
  return "status" in t && (s.status = e3(t.status)), [
    "provider",
    "model",
    "inputSummary",
    "outputText",
    "parsedAction",
    "error",
    "trigger"
  ].forEach((a) => {
    a in t && (s[a] = String(t[a] || ""));
  }), "toolTrace" in t && (s.toolTrace = nn(t.toolTrace, void 0)), "changedFiles" in t && (s.changedFiles = _l(t.changedFiles, 100)), "changedStates" in t && (s.changedStates = _l(t.changedStates, 100)), "changedTasks" in t && (s.changedTasks = _l(t.changedTasks, 100)), "turn" in t && (s.turn = Math.max(0, Number(t.turn) || 0)), "userOrder" in t && (s.userOrder = Number(t.userOrder)), "assistantOrder" in t && (s.assistantOrder = Number(t.assistantOrder)), await ur.update(n, s), await Ct.update(r.sessionId, { updatedAt: un() }), await ur.get(n) || null;
}
async function GU(e = "") {
  const t = String(e || "").trim();
  if (!t) return null;
  const n = await ur.get(t);
  if (!n || n.status !== "running") return n || null;
  const r = un();
  return await ur.update(t, { updatedAt: r }), await Ct.update(n.sessionId, { updatedAt: r }), await ur.get(t) || null;
}
async function i3(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await ur.where("sessionId").equals(n).sortBy("updatedAt"), s = Math.max(0, Number(t.limit) || 0);
  return (s ? r.slice(-s) : r).reverse();
}
async function l3(e = "") {
  const t = String(e || "").trim();
  return t ? (await ur.where("sessionId").equals(t).toArray()).filter((n) => n.trigger === "accepted_turn" && n.status === "pending").sort((n, r) => {
    const s = Number(r.assistantOrder) - Number(n.assistantOrder);
    return s || Number(r.updatedAt) - Number(n.updatedAt);
  }) : [];
}
function pw(e) {
  const t = JSON.stringify({
    content: String(e?.content || ""),
    status: String(e?.status || ""),
    source: String(e?.source || ""),
    staleFromOrder: Number.isFinite(Number(e?.staleFromOrder)) ? Number(e?.staleFromOrder) : null
  });
  let n = 2166136261;
  for (let r = 0; r < t.length; r += 1)
    n ^= t.charCodeAt(r), n = Math.imul(n, 16777619) >>> 0;
  return n.toString(16);
}
function VU(e = "", t = []) {
  const n = String(e || "").trim();
  if (!t.length) return n;
  const r = "rollback_conflict:", s = n.startsWith(r) ? n.slice(18).split(",").map((a) => a.trim()).filter(Boolean) : [];
  return `${r}${[.../* @__PURE__ */ new Set([...s, ...t])].join(",")}`;
}
async function Lb(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await Cr.where("sessionId").equals(n).sortBy("updatedAt"), s = String(t.docType || "").trim();
  return r.filter((a) => !s || a.docType === s).filter((a) => t.includeStale || a.status !== "stale");
}
async function bi(e = "", t = "tavern.map", n = "main") {
  const r = String(e || "").trim(), s = String(t || "").trim(), a = String(n || "main").trim() || "main";
  return !r || !s || !a ? null : await Cr.get([
    r,
    s,
    a
  ]) || null;
}
async function Uf(e = "", t = {}) {
  const n = String(e || "").trim();
  return n ? await Dt.transaction("rw", Cr, Ct, async () => {
    const r = un(), s = await Cr.get([
      n,
      GM,
      Bm
    ]), a = await Cr.get([
      n,
      YM,
      XM
    ]), o = s || {
      sessionId: n,
      docType: "tavern.map",
      docId: "main",
      title: "地图",
      revision: 0,
      data: Ju(),
      digest: "",
      status: "active",
      source: "system-seed",
      staleFromOrder: void 0,
      createdAt: r,
      updatedAt: r
    }, l = a || {
      sessionId: n,
      docType: "tavern.atlas",
      docId: "main",
      title: "世界图",
      revision: 0,
      data: jM(),
      digest: "",
      status: "active",
      source: "system-seed",
      staleFromOrder: void 0,
      createdAt: r,
      updatedAt: r
    };
    return s || await Cr.put(o), a || await Cr.put(l), t.touchSession !== !1 && await Ct.update(n, { updatedAt: r }), o;
  }) : null;
}
async function ST(e) {
  const t = un(), n = {
    ...nn(e, e),
    sessionId: String(e.sessionId || "").trim(),
    docType: String(e.docType || "tavern.map"),
    docId: String(e.docId || "main").trim() || "main",
    title: String(e.title || ""),
    revision: Math.max(0, Number(e.revision) || 0),
    digest: String(e.digest || ""),
    status: e.status === "stale" ? "stale" : "active",
    createdAt: Number(e.createdAt) || t,
    updatedAt: t
  };
  if (!n.sessionId) throw new Error("state_session_required");
  return await Cr.put(n), await Ct.update(n.sessionId, { updatedAt: t }), n;
}
async function Db(e = {}) {
  const t = String(e.sessionId || "").trim(), n = String(e.docType || "tavern.map"), r = String(e.docId || "main").trim() || "main";
  if (!t) throw new Error("state_patch_session_required");
  const s = un(), a = {
    id: String(e.id || Zg("state-patch")),
    sessionId: t,
    docType: n,
    docId: r,
    revision: Math.max(0, Number(e.revision) || 0),
    status: e.status === "rolled_back" ? "rolled_back" : "active",
    managerRunId: String(e.managerRunId || ""),
    sourceUserOrder: Number.isFinite(Number(e.sourceUserOrder)) ? Number(e.sourceUserOrder) : void 0,
    sourceAssistantOrder: Number.isFinite(Number(e.sourceAssistantOrder)) ? Number(e.sourceAssistantOrder) : void 0,
    source: String(e.source || ""),
    summary: String(e.summary || ""),
    ops: Array.isArray(e.ops) ? nn(e.ops, []) : [],
    changedIds: _l(e.changedIds, 200),
    removedElements: Array.isArray(e.removedElements) ? nn(e.removedElements, []) : [],
    createdAt: Number(e.createdAt) || s,
    updatedAt: s
  };
  return await yl.put(a), await Ct.update(t, { updatedAt: s }), a;
}
async function Hm(e = {}) {
  const t = String(e.sessionId || "").trim();
  if (!t) return [];
  const n = String(e.docType || "").trim(), r = String(e.docId || "").trim(), s = (await yl.where("sessionId").equals(t).sortBy("revision")).filter((o) => (!n || o.docType === n) && (!r || o.docId === r) && (e.includeRolledBack || o.status !== "rolled_back")), a = Math.max(0, Number(e.limit) || 0);
  return a ? s.slice(-a) : s;
}
async function Ob(e) {
  const t = String(e.managerRunId || "").trim(), n = String(e.sessionId || "").trim(), r = String(e.path || "").trim();
  if (!t || !n || !r) return null;
  const s = await zr.get([t, r]);
  if (s) return s;
  const a = un(), o = await Tn.get([n, r]) || null, l = {
    managerRunId: t,
    sessionId: n,
    path: r,
    beforeExists: !!o,
    beforeFile: o ? nn(o, void 0) : void 0,
    beforeHash: pw(o),
    afterHash: "",
    rollbackStatus: "pending",
    error: "",
    createdAt: a,
    updatedAt: a
  };
  return await zr.put(l), l;
}
async function AT(e) {
  const t = String(e.managerRunId || "").trim(), n = String(e.sessionId || "").trim(), r = String(e.path || "").trim();
  if (!t || !n || !r || !await Ob({
    managerRunId: t,
    sessionId: n,
    path: r
  })) return null;
  const s = await Tn.get([n, r]) || null;
  return await zr.update([t, r], {
    afterHash: pw(s),
    updatedAt: un()
  }), await zr.get([t, r]) || null;
}
async function qf(e = "") {
  const t = String(e || "").trim();
  return t ? await zr.where("managerRunId").equals(t).sortBy("updatedAt") : [];
}
async function JU(e = "") {
  const t = String(e || "").trim();
  return t ? await Ru.where("managerRunId").equals(t).sortBy("updatedAt") : [];
}
async function YU(e = "") {
  const t = String(e || "").trim();
  if (!t) return;
  const [n, r] = await Promise.all([qf(t), JU(t)]);
  await Dt.transaction("rw", zr, Ru, Ir, async () => {
    n.length && await zr.bulkDelete(n.map((s) => [s.managerRunId, s.path])), r.length && await Ru.bulkDelete(r.map((s) => [
      s.managerRunId,
      s.docType,
      s.docId
    ])), await Ir.delete(t);
  });
}
async function u3(e = "") {
  const t = String(e || "").trim();
  if (!t) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const n = await ur.get(t);
  if (!n) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const r = (await qf(t)).reverse();
  let s = 0, a = 0;
  const o = [];
  for (const i of r) {
    if (i.rollbackStatus === "rolled_back" || i.rollbackStatus === "skipped") {
      a += 1;
      continue;
    }
    if (i.rollbackStatus === "conflict") {
      o.push(i.path), a += 1;
      continue;
    }
    const u = await Tn.get([i.sessionId, i.path]) || null;
    if (!i.afterHash) {
      a += 1, await zr.update([i.managerRunId, i.path], {
        rollbackStatus: "skipped",
        error: "snapshot_after_hash_missing",
        updatedAt: un()
      });
      continue;
    }
    if (pw(u) !== i.afterHash) {
      o.push(i.path), await zr.update([i.managerRunId, i.path], {
        rollbackStatus: "conflict",
        error: "rollback_conflict_current_file_changed",
        updatedAt: un()
      });
      continue;
    }
    i.beforeExists && i.beforeFile ? await Tn.put(nn(i.beforeFile, i.beforeFile)) : await Tn.delete([i.sessionId, i.path]), s += 1, await zr.update([i.managerRunId, i.path], {
      rollbackStatus: "rolled_back",
      error: "",
      updatedAt: un()
    });
  }
  const l = await As.get([n.sessionId, "markdown-derived"]);
  return await As.put({
    sessionId: n.sessionId,
    kind: "markdown-derived",
    status: "stale",
    error: o.length ? `rollback_conflict:${o.join(",")}` : "",
    sourceFingerprint: "",
    derivedAt: un(),
    updatedAt: un(),
    files: Array.isArray(l?.files) ? l.files : []
  }), await yr(t, {
    status: "rolled_back",
    error: VU(n.error, o)
  }), await Ct.update(n.sessionId, { updatedAt: un() }), {
    rolledBack: s,
    conflicts: o,
    skipped: a
  };
}
async function XU(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return {
    runIds: [],
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const s = (await ur.where("sessionId").equals(n).toArray()).filter((i) => ["accepted_turn", "after_turn"].includes(i.trigger) && (Number(i.userOrder) >= r || Number(i.assistantOrder) >= r)).sort((i, u) => u.updatedAt - i.updatedAt);
  let a = 0, o = 0;
  const l = [];
  for (const i of s) {
    if ((await qf(i.id)).some((u) => String(u.afterHash || "").trim())) {
      const u = await u3(i.id);
      a += u.rolledBack, o += u.skipped, l.push(...u.conflicts);
      continue;
    }
    await yr(i.id, {
      status: [
        "pending",
        "queued",
        "running"
      ].includes(i.status) ? "cancelled" : "superseded",
      error: "manager_source_messages_superseded"
    });
  }
  return {
    runIds: s.map((i) => i.id),
    rolledBack: a,
    conflicts: l,
    skipped: o
  };
}
async function zm() {
  const e = await Eo.get("activeAssistantPresetId");
  return String(e?.value || "littlewhitebox-assistant-default").trim() || "littlewhitebox-assistant-default";
}
async function du(e = jg) {
  const t = String(e || "littlewhitebox-assistant-default").trim() || "littlewhitebox-assistant-default";
  return await Eo.put({
    key: "activeAssistantPresetId",
    value: t,
    updatedAt: un()
  }), t;
}
async function pd(e, t = {}) {
  const n = un(), r = String(e.id || Zg("assistant-preset")), s = await xl.get(r), a = uw({
    ...e,
    id: r,
    updatedAt: n
  }), o = {
    id: r,
    name: a.name,
    description: a.description,
    version: t.isBuiltIn === !0 ? gU : String(s?.version || ""),
    isBuiltIn: t.isBuiltIn === !0,
    createdAt: Number(s?.createdAt) || n,
    updatedAt: n,
    preset: a
  };
  return await xl.put(o), o;
}
async function jU(e = "") {
  const t = String(e || "").trim();
  return !t || t === "littlewhitebox-assistant-default" ? !1 : (await xl.delete(t), await zm() === t && await du(jg), !0);
}
async function c3() {
  const e = await xl.get(jg);
  return e?.version === "2026-06-assistant-prompt-memory-gate-v3" ? e : pd(yf(), { isBuiltIn: !0 });
}
async function Zl() {
  return await c3(), xl.orderBy("updatedAt").reverse().toArray();
}
async function Qy() {
  await c3();
  const e = await zm();
  return uw((await xl.get(e) || await xl.get("littlewhitebox-assistant-default"))?.preset || yf());
}
function mw(e = []) {
  return e.some((t) => !!t.rect || typeof t.circle == "number" || Array.isArray(t.path) || Array.isArray(t.curve) || !!t.icon);
}
function QU(e) {
  return e ? e.meta.status === "active" && mw(e.elements) : !1;
}
var d3 = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon",
  "text"
];
function yo(e) {
  return JSON.parse(JSON.stringify(e));
}
function lo(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function f3(e, t) {
  const n = { ...e };
  return Object.entries(t).forEach(([r, s]) => {
    if (s == null) {
      delete n[r];
      return;
    }
    if (lo(s) && lo(n[r])) {
      n[r] = f3(n[r], s);
      return;
    }
    n[r] = yo(s);
  }), n;
}
function ZU(e) {
  return d3.filter((t) => t === "circle" ? typeof e.circle == "number" : t === "text" ? typeof e.text == "string" && !!e.text.trim() : Array.isArray(e[t]) || typeof e[t] == "string" && !!e[t]);
}
function eq(e, t) {
  if (t !== null)
    return t === void 0 ? e ? yo(e) : void 0 : Object.keys(t).length ? {
      ...e || {},
      ...t
    } : void 0;
}
function h3(e, t) {
  const n = ZU(t).length > 0, r = yo(e);
  if (n && (d3.forEach((s) => {
    delete r[s];
  }), delete r.style, delete r.fill, delete r.closed), Object.entries(t).forEach(([s, a]) => {
    if (s !== "style") {
      if (a == null) {
        delete r[s];
        return;
      }
      r[s] = yo(a);
    }
  }), "style" in t) {
    const s = n ? t.style === null || t.style === void 0 ? void 0 : yo(t.style) : eq(e.style, t.style);
    s === void 0 ? delete r.style : r.style = s;
  }
  return r;
}
function tq(e, t) {
  let n = yo(e);
  return t.forEach((r) => {
    const s = String(r.op || "").trim();
    if (s === "meta") {
      const a = r.set && lo(r.set) ? r.set : r.changes && lo(r.changes) ? r.changes : null;
      a && (n.meta = f3(n.meta, a), n.meta.status !== "uninitialized" && delete n.meta.hint);
      return;
    }
    if (s === "add") {
      const a = r.element;
      if (!a || !lo(a) || typeof a.id != "string") return;
      const o = yo(a), l = n.elements.findIndex((i) => i.id === o.id);
      if (l >= 0) {
        n.elements[l] = o;
        return;
      }
      n.elements.push(o);
      return;
    }
    if (s === "remove") {
      const a = String(r.id || "").trim();
      if (!a) return;
      n.elements = n.elements.filter((o) => o.id !== a);
      return;
    }
    if (s === "modify") {
      const a = String(r.id || "").trim(), o = r.set && lo(r.set) ? r.set : r.changes && lo(r.changes) ? r.changes : null;
      if (!a || !o) return;
      n.elements = n.elements.map((l) => l.id === a ? h3(l, o) : l);
      return;
    }
    if (s === "init" || s === "reset") {
      const a = r.document;
      a && lo(a) && (n = yo(a));
      return;
    }
    if (s === "replace") {
      const a = String(r.id || "").trim(), o = r.element;
      if (!a || !o || !lo(o)) return;
      n.elements = n.elements.map((l) => l.id === a ? yo(o) : l);
    }
  }), n;
}
var p3 = [
  "x",
  "o",
  "+",
  "arrow-n",
  "arrow-s",
  "arrow-e",
  "arrow-w",
  "stairs-up",
  "stairs-down"
], m3 = [
  "castle",
  "village",
  "road",
  "house",
  "shop",
  "inn-sign",
  "temple",
  "stairs",
  "door",
  "forest",
  "mountains",
  "cave",
  "waterfall",
  "skull",
  "trap",
  "fire",
  "chest",
  "table",
  "bed",
  "barrel",
  "flag",
  "star",
  "torch",
  "portal",
  "tree",
  "rock",
  "water",
  "heart",
  "lips",
  "lovers",
  "cherish",
  "love-letter",
  "locked-heart",
  "perfume"
], gw = [...p3, ...m3], Ive = new Set(gw), g3 = [
  "neutral",
  "warm",
  "cold",
  "dark",
  "mystic",
  "danger",
  "calm"
], vw = [
  "unknown",
  "wood",
  "stone",
  "tile",
  "carpet",
  "blood",
  "water",
  "grass",
  "dirt",
  "snow",
  "metal",
  "rune",
  "warm-light",
  "cold-light",
  "shadow"
], yw = [
  "confirmed",
  "inferred",
  "unknown"
], nq = new Set(g3), rq = new Set(vw), sq = new Set(yw);
function Qo(e, t) {
  const n = String(e || ""), r = String(t || "");
  return n < r ? -1 : n > r ? 1 : 0;
}
function v3(e) {
  return nq.has(String(e || "").trim());
}
function aq(e) {
  return rq.has(String(e || "").trim());
}
function oq(e) {
  return sq.has(String(e || "").trim());
}
function Km(e) {
  return {
    wood: {
      paint: "url(#mat-wood)",
      blend: "normal",
      layer: "fill"
    },
    stone: {
      paint: "url(#mat-stone)",
      blend: "normal",
      layer: "fill"
    },
    tile: {
      paint: "url(#mat-tile)",
      blend: "normal",
      layer: "fill"
    },
    carpet: {
      paint: "url(#mat-carpet)",
      blend: "normal",
      layer: "fill"
    },
    blood: {
      paint: "url(#mat-blood)",
      blend: "multiply",
      layer: "fill",
      opacity: 0.85
    },
    water: {
      paint: "url(#mat-water)",
      blend: "normal",
      layer: "fill",
      opacity: 0.9
    },
    grass: {
      paint: "url(#mat-grass)",
      blend: "normal",
      layer: "fill"
    },
    dirt: {
      paint: "url(#mat-dirt)",
      blend: "normal",
      layer: "fill"
    },
    snow: {
      paint: "url(#mat-snow)",
      blend: "normal",
      layer: "fill"
    },
    metal: {
      paint: "url(#mat-metal)",
      blend: "normal",
      layer: "fill"
    },
    rune: {
      paint: "url(#mat-rune)",
      blend: "screen",
      layer: "fill",
      opacity: 0.8
    },
    "warm-light": {
      paint: "url(#grad-warm)",
      blend: "screen",
      layer: "light",
      opacity: 0.7
    },
    "cold-light": {
      paint: "url(#grad-cold)",
      blend: "screen",
      layer: "light",
      opacity: 0.6
    },
    shadow: {
      paint: "#000",
      blend: "multiply",
      layer: "light",
      opacity: 0.5
    }
  }[String(e || "").trim()] || null;
}
function iq(e, t) {
  const n = String(e || "").trim();
  return Km(t)?.layer === "light" ? 80 : n === "terrain" ? 10 : n === "water" ? 20 : n === "road" ? 30 : n === "furniture" ? 40 : n === "danger" || n === "secret" ? 50 : n === "magic" ? 60 : 90;
}
function lq(e) {
  return ![
    "wall",
    "grid",
    "label",
    "actor",
    "marker"
  ].includes(String(e || "").trim());
}
function si(e) {
  return typeof e == "number" ? Number(e.toFixed(4)) : Array.isArray(e) ? e.map(si) : e && typeof e == "object" ? Object.fromEntries(Object.entries(e).sort(([t], [n]) => Qo(t, n)).map(([t, n]) => [t, si(n)])) : e;
}
function uq(e) {
  const t = {};
  return [
    "at",
    "rect",
    "circle",
    "path",
    "curve",
    "icon",
    "closed"
  ].forEach((n) => {
    e[n] !== void 0 && (t[n] = si(e[n]));
  }), t;
}
function TT(e) {
  const t = /* @__PURE__ */ new Map();
  e.elements.forEach((r) => {
    if (!Na(r.id)) return;
    const s = r.id.slice(To("").length);
    s && t.set(s, r);
  });
  const n = e.elements.filter((r) => !Na(r.id)).map((r) => {
    const s = t.get(r.id), a = {
      id: r.id,
      cat: r.cat,
      ...uq(r)
    };
    return r.material && (a.material = r.material), r.certainty && (a.certainty = r.certainty), r.actorKey && (a.actorKey = r.actorKey), r.fill && (a.fill = r.fill), r.style && (a.style = si(r.style)), r.text && (a.text = r.text), s?.at && (a.labelAt = si(s.at)), s?.text && (a.labelText = s.text), s?.style && (a.labelStyle = si(s.style)), si(a);
  }).sort((r, s) => Qo(r.id, s.id));
  return si({
    meta: {
      name: e.meta.name ?? null,
      mood: e.meta.mood || "neutral",
      viewBox: e.meta.viewBox,
      theme: e.meta.theme,
      status: e.meta.status,
      hint: e.meta.hint
    },
    elements: n
  });
}
function y3(e) {
  return ![
    "terrain",
    "light",
    "grid",
    "label"
  ].includes(String(e || "").trim());
}
var _s = {
  LIST: "MapDocs",
  READ: "MapInspect",
  PATCH: "MapPatch"
}, Zy = {
  LIST: "StateList",
  READ: "StateRead",
  PATCH: "StatePatch"
};
function cq(e = "") {
  const t = String(e || "").trim();
  return t === Zy.LIST ? _s.LIST : t === Zy.READ ? _s.READ : t === Zy.PATCH ? _s.PATCH : t;
}
var ir = GM, Hr = YM, kr = Bm, ea = XM, Fb = 2e3, _3 = 1e3, Qs = 300, _w = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon",
  "text"
], b3 = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon"
], tv = /* @__PURE__ */ new Set([
  "wall",
  "road",
  "water",
  "terrain",
  "furniture",
  "door",
  "danger",
  "marker",
  "actor",
  "label",
  "grid",
  "magic",
  "secret",
  "light"
]), bw = new Set(gw), dq = /* @__PURE__ */ new Set([
  "house",
  "castle",
  "village",
  "forest",
  "temple",
  "shop"
]), w3 = /* @__PURE__ */ new Set([
  "parchment",
  "paper",
  "dark",
  "blueprint",
  "grid"
]), S3 = /* @__PURE__ */ new Set(["uninitialized", "active"]), ww = /* @__PURE__ */ new Set([
  "city",
  "district",
  "building",
  "floor",
  "room",
  "outdoor"
]), Sw = /* @__PURE__ */ new Set(["mentioned", "visited"]), bf = /* @__PURE__ */ new Set([
  "door",
  "stairs",
  "elevator",
  "path",
  "road",
  "portal",
  "passage"
]), fq = /* @__PURE__ */ new Set([
  "parent",
  "mapDocId",
  "aliases",
  "brief"
]);
function Bb() {
  return Date.now();
}
function A3(e = kr) {
  const t = String(e || kr).trim() || kr;
  return /^[\w.-]{1,80}$/i.test(t) ? t : kr;
}
function hq(e, t = "state_doc_id_invalid") {
  const n = String(e || "").trim();
  if (!n || !/^[\w.-]{1,80}$/i.test(n)) throw new Error(t);
  return n;
}
function Kd(e) {
  return e?.docType === ir && e.docId === kr && Number(e.revision) === 0 && JM(e.data);
}
function pq(e) {
  return [...e].sort((t, n) => (Number(n.updatedAt) || 0) - (Number(t.updatedAt) || 0) || (Number(n.revision) || 0) - (Number(t.revision) || 0));
}
async function Ub(e = "") {
  return A3((await Ct.get(String(e || "").trim()))?.state?.activeMapDocId || kr);
}
async function Vh(e = "", t = kr) {
  const n = String(e || "").trim(), r = A3(t);
  if (!n) return r;
  const s = await Ct.get(n), a = s?.state && typeof s.state == "object" && !Array.isArray(s.state) ? {
    ...s.state,
    activeMapDocId: r
  } : { activeMapDocId: r };
  return await Ct.update(n, {
    state: a,
    updatedAt: Bb()
  }), r;
}
async function e_(e = "", t = "") {
  const n = Sf(await bi(e, Hr, ea)), r = n.locations.find((s) => s.key === n.activeLocationKey)?.mapDocId;
  return !n.activeLocationKey || !r || r === t ? [] : [`Map doc '${t}' activated. Atlas still says player/current location is '${n.activeLocationKey}'. If the story moved the player, send tavern.atlas move-actor with actorKey:"player" and the correct locationKey.`];
}
async function Wm(e = "", t = {}) {
  await Uf(e, { touchSession: !1 });
  const n = await Ub(e), r = await Lb(e, {
    docType: ir,
    includeStale: t.includeStale === !0
  }), s = pq(r.filter((i) => !Kd(i))), a = r.find((i) => i.docId === n) || null, o = r.find((i) => i.docId === kr) || null, l = a && (!Kd(a) || !s.length) ? a : (Kd(o) ? null : o) || s[0] || r[0] || null;
  return {
    requestedDocId: n,
    activeDocId: l?.docId || n,
    record: l,
    documents: r
  };
}
function jt(e) {
  return JSON.parse(JSON.stringify(e));
}
function Wr(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function Tt(e = "", t = 400) {
  const n = String(e ?? "").replace(/\s+/g, " ").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function ET(e) {
  return JSON.stringify(e ?? null);
}
function Ys(e, t) {
  return ET(e) === ET(t);
}
function mq(e = ir) {
  const t = String(e || ir).trim();
  if (t === ir || t === Hr) return t;
  throw new Error("state_doc_type_not_supported");
}
function gq(e = kr) {
  const t = String(e || kr).trim() || kr;
  if (!/^[\w.-]{1,80}$/i.test(t)) throw new Error("state_doc_id_invalid");
  return t;
}
function vq(e, t) {
  if (e === Hr) {
    if ((String(t || ea).trim() || ea) !== ea) throw new Error("atlas_doc_id_invalid");
    return ea;
  }
  return gq(t || kr);
}
function T3(e, t = "parchment") {
  return w3.has(String(e || "").trim()) ? String(e).trim() : t;
}
function E3(e, t = "active") {
  return S3.has(String(e || "").trim()) ? String(e).trim() : t;
}
function qb(e) {
  if (e === null || !Array.isArray(e) || e.length !== 4) return null;
  const t = e.map((n) => Number(n));
  return t.every((n) => Number.isFinite(n)) ? [
    t[0],
    t[1],
    Math.max(1, t[2]),
    Math.max(1, t[3])
  ] : null;
}
function hi(e) {
  if (Array.isArray(e) && e.length >= 2) {
    const t = Number(e[0]), n = Number(e[1]);
    return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
  }
  if (Wr(e)) {
    const t = Number(e.x ?? e.cx), n = Number(e.y ?? e.cy);
    return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
  }
  return null;
}
function C3(e, t) {
  const n = hi(e);
  if (!n) throw new Error(t);
  return n;
}
function Hb(e, t, n = 2) {
  if (!Array.isArray(e) || e.length < n) throw new Error(t);
  const r = e.map((s) => hi(s));
  if (r.some((s) => !s)) throw new Error(t);
  return r;
}
function nv(e) {
  const t = Number(e);
  return Number.isFinite(t) && t > 0 ? t : null;
}
function Aw(e) {
  if (!Array.isArray(e) || e.length < 2) return null;
  const t = Number(e[0]), n = Number(e[1]);
  return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
}
function Gm(e, t) {
  const n = String(e || "").trim();
  return n === "floor" || n === "ground" ? "terrain" : n && tv.has(n) ? n : t;
}
function yq(e, t = "neutral", n) {
  const r = String(e || "").trim();
  return v3(r) ? r : (r && n?.push(`Ignored invalid map mood: ${r}.`), t);
}
function Tw(e, t = {}) {
  if (e == null || e === "") return;
  const n = String(e || "").trim();
  if (aq(n)) return n;
  t.warnings?.push(`Ignored invalid map material${t.elementId ? ` for ${t.elementId}` : ""}: ${n}.`);
}
function Ew(e, t, n = {}) {
  if (e == null || e === "") return t;
  const r = String(e || "").trim();
  return oq(r) ? r : (n.warnings?.push(`Ignored invalid map certainty${n.elementId ? ` for ${n.elementId}` : ""}: ${r}.`), t);
}
function Cw(e) {
  const t = String(e || "").trim();
  return bw.has(t) ? t : void 0;
}
function I3(e, t) {
  if (e && dq.has(e)) throw new Error(`map_element_icon_place_scale:${t}:${e}`);
}
function wf(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").trim().toLowerCase().replace(/\s+/g, "-").slice(0, 120);
}
function Iw(e) {
  if (e === null) return;
  const t = Wr(e) ? e : {}, n = {}, r = Tt(t.color, 80), s = Tt(t.dash, 80), a = nv(t.width);
  return r && (n.color = r), s && (n.dash = s), a !== null && (n.width = a), Object.keys(n).length ? n : void 0;
}
function x3(e, t = {}, n) {
  const r = Wr(e) ? e : {}, s = E3(r.status ?? t.status ?? "active", t.status ?? "active"), a = "name" in r ? r.name : t.name, o = "hint" in r ? r.hint : t.hint;
  return {
    name: a === null ? null : Tt(a, 120) || null,
    viewBox: "viewBox" in r ? qb(r.viewBox) : qb(t.viewBox ?? null),
    theme: T3(r.theme ?? t.theme ?? "parchment", t.theme ?? "parchment"),
    status: s,
    mood: yq(r.mood ?? t.mood ?? "neutral", t.mood ?? "neutral", n),
    ...s === "uninitialized" ? { hint: Tt(o ?? Pb(), 1200) || Pb() } : {}
  };
}
function _q(e, t = []) {
  if (!Wr(e)) throw new Error("map_meta_set_required");
  const n = {};
  if ("name" in e && (n.name = e.name === null ? null : Tt(e.name, 120) || null), "viewBox" in e && (n.viewBox = qb(e.viewBox)), "theme" in e && (n.theme = T3(e.theme)), "status" in e && (n.status = E3(e.status)), "mood" in e) {
    const r = String(e.mood || "").trim();
    v3(r) ? n.mood = r : e.mood === null || r === "" ? n.mood = "neutral" : t.push(`Ignored invalid map mood: ${r}.`);
  }
  return "hint" in e && (n.hint = Tt(e.hint, 1200) || void 0), n;
}
function zb(e, t) {
  return x3({
    ...e,
    ...t,
    ...t.hint === void 0 && "hint" in t ? { hint: null } : {}
  }, e);
}
function k3(e) {
  for (const t of _w) {
    if (t === "circle") {
      if (typeof e.circle == "number") return t;
      continue;
    }
    if (t === "text") {
      if (typeof e.text == "string" && e.text.trim()) return t;
      continue;
    }
    if (Array.isArray(e[t]) || typeof e[t] == "string" && e[t]) return t;
  }
  return null;
}
function Kb(e) {
  return e === "text" ? "label" : e === "icon" ? "marker" : "wall";
}
function M3(e, t) {
  const n = C3(e.center ?? e.pos ?? {
    x: e.cx,
    y: e.cy
  }, `map_element_center_required:${t}`), r = nv(e.r ?? e.radius);
  if (r === null) throw new Error(`map_element_radius_required:${t}`);
  const s = Number(e.startAngle), a = Number(e.endAngle);
  if (!Number.isFinite(s) || !Number.isFinite(a)) throw new Error(`map_element_arc_angles_required:${t}`);
  const o = a - s, l = Math.max(4, Math.min(24, Math.ceil(Math.abs(o) / 30)));
  return Array.from({ length: l + 1 }, (i, u) => {
    const d = (s + o * u / l) * Math.PI / 180;
    return [Number((n[0] + r * Math.cos(d)).toFixed(2)), Number((n[1] + r * Math.sin(d)).toFixed(2))];
  });
}
function Ma(e, t, n) {
  const r = Hb(e, `map_element_points_required:${t}`, 2);
  if (n) return {
    at: n,
    points: r
  };
  const s = r[0];
  return {
    at: s,
    points: r.map(([a, o]) => [Number((a - s[0]).toFixed(2)), Number((o - s[1]).toFixed(2))])
  };
}
function P3(e) {
  const [t, n] = e.at;
  return e.rect ? [t + Math.max(18, e.rect[0] / 2), n - 18] : typeof e.circle == "number" ? [t + e.circle + 18, n - 6] : [t + 18, n - 18];
}
function R3(e, t = {}) {
  if (!e || !/^[\w:.-]{1,120}$/i.test(e)) throw new Error("map_element_id_invalid");
  if (!t.allowReserved && Na(e)) throw new Error(`map_element_id_reserved:${e}`);
  return e;
}
function bq(e, t) {
  const n = [...new Set(t)];
  if (!n.length) throw new Error(`map_element_shape_required:${e}`);
  if (n.length !== 1 && !(n.length === 2 && n.includes("text") && n.some((r) => b3.includes(r))))
    throw new Error(`map_element_shape_conflict:${e}`);
}
function $u(e, t, n = {}) {
  const r = R3(t, { allowReserved: n.allowReservedId === !0 }), s = k3(e);
  if (!s) throw new Error(`map_element_shape_required:${r}`);
  const a = {
    id: r,
    at: C3(e.at, `map_element_at_required:${r}`),
    cat: Gm(e.cat, Kb(s))
  };
  if (e.rect) {
    const u = Aw(e.rect);
    if (!u || u[0] <= 0 || u[1] <= 0) throw new Error(`map_element_rect_invalid:${r}`);
    a.rect = u;
  }
  if (typeof e.circle == "number") {
    if (!(e.circle > 0)) throw new Error(`map_element_radius_required:${r}`);
    a.circle = e.circle;
  }
  if (e.path && (a.path = Hb(e.path, `map_element_points_required:${r}`, 2)), e.curve && (a.curve = Hb(e.curve, `map_element_points_required:${r}`, 2)), e.icon) {
    const u = Cw(e.icon);
    if (!u) throw new Error(`map_element_icon_invalid:${r}`);
    a.icon = u;
  }
  if (e.text !== void 0) {
    const u = Tt(e.text, 240);
    if (!u) throw new Error(`map_element_text_required:${r}`);
    a.text = u;
  }
  const o = Tt(e.actorKey, 120);
  o && a.cat === "actor" && (a.actorKey = o);
  const l = Tw(e.material, {
    elementId: r,
    warnings: n.warnings,
    source: n.source
  });
  l && (a.cat === "actor" && l === "shadow" ? n.warnings?.push(`Ignored material:"shadow" for actor ${r}; spectral actors are not represented by material in map v1.1.`) : a.material = l);
  const i = Ew(e.certainty, void 0, {
    elementId: r,
    warnings: n.warnings
  });
  if (i && i !== "confirmed" && (a.certainty = i), e.closed === !0 && (a.closed = !0), Tt(e.fill, 120) && (a.fill = Tt(e.fill, 120)), e.style !== void 0) {
    const u = Iw(e.style);
    u && (a.style = u);
  }
  return a;
}
function Vm(e, t = {}) {
  if (!Wr(e)) throw new Error("map_element_must_be_object");
  const n = t.source || "model-input", r = R3(t.forcedId || String(e.id || "").trim(), { allowReserved: t.allowReservedId === !0 || n === "stored-document" });
  let s = hi(e.at ?? e.pos ?? e.center ?? e.position);
  !s && (e.x !== void 0 || e.y !== void 0) && (s = hi({
    x: e.x,
    y: e.y
  })), !s && (e.cx !== void 0 || e.cy !== void 0) && (s = hi({
    x: e.cx,
    y: e.cy
  }));
  const a = {}, o = String(e.type || "").trim(), l = Aw(e.rect ?? e.size ?? (e.width !== void 0 || e.height !== void 0 ? [e.width, e.height] : null));
  l && (a.rect = l);
  const i = o === "arc" ? null : nv(e.circle ?? e.r ?? e.radius);
  i !== null && (a.circle = i);
  const u = e.path ?? e.points ?? e.line ?? (e.x1 !== void 0 || e.y1 !== void 0 || e.x2 !== void 0 || e.y2 !== void 0 ? [[e.x1, e.y1], [e.x2, e.y2]] : null), d = e.curve, f = Tt(e.text ?? e.content ?? e.label ?? e.value, 240);
  f && (a.text = f);
  const m = Cw(e.icon);
  if (m && (n === "model-input" && I3(m, r), a.icon = m), o === "line" && u) {
    const $ = Ma(u, r, s);
    s = $.at, a.path = $.points;
  } else if (o === "curve" && (d ?? u)) {
    const $ = Ma(d ?? u, r, s);
    s = $.at, a.curve = $.points;
  } else if (o === "fill" && u) {
    const $ = Ma(u, r, s);
    s = $.at, a.path = $.points, a.closed = !0, Tt(e.fill, 120) && (a.fill = Tt(e.fill, 120));
  } else if (o === "arc") {
    const $ = Ma(M3(e, r), r, s);
    s = $.at, a.curve = $.points;
  } else {
    if (u && !a.path && !a.curve) {
      const $ = Ma(u, r, s);
      s = $.at, a.path = $.points;
    }
    if (d && !a.curve) {
      const $ = Ma(d, r, s);
      s = $.at, a.curve = $.points;
    }
  }
  const v = _w.filter(($) => $ === "circle" ? typeof a.circle == "number" : $ === "text" ? typeof a.text == "string" && !!a.text.trim() : $ in a);
  bq(r, v);
  const y = Gm(e.cat, Kb(v.find(($) => $ !== "text") || v[0] || null)), S = Tw(e.material, {
    elementId: r,
    warnings: t.warnings,
    source: n
  }), x = Ew(e.certainty, void 0, {
    elementId: r,
    warnings: t.warnings
  }), I = Tt(e.fill ?? a.fill, 120) || void 0, E = S && I && n === "model-input" ? void 0 : I;
  S && I && n === "model-input" && t.warnings?.push(`Dropped legacy fill for ${r} because material:"${S}" was supplied.`);
  const N = {
    id: r,
    at: s || void 0,
    cat: y,
    closed: e.closed === !0 || a.closed === !0,
    fill: E,
    style: Iw(e.style),
    actorKey: Tt(e.actorKey, 120) || void 0,
    material: S,
    certainty: x,
    ...a
  };
  if (v.length === 2 && t.splitGeometryText !== !1) {
    const $ = v.find((b) => b !== "text"), p = Gm(e.cat, Kb($)), w = $u({
      id: r,
      at: N.at,
      cat: p,
      closed: N.closed,
      fill: N.fill,
      style: N.style,
      actorKey: N.actorKey,
      material: N.material,
      certainty: N.certainty,
      [$]: N[$]
    }, r, {
      allowReservedId: t.allowReservedId === !0 || n === "stored-document",
      warnings: t.warnings,
      source: n
    });
    if (!y3(w.cat))
      return t.warnings?.push(`Ignored label text for ${r}; cat:"${w.cat}" does not derive map labels.`), [w];
    const _ = To(r);
    return [w, $u({
      id: _,
      at: P3(w),
      cat: "label",
      text: N.text
    }, _, {
      allowReservedId: !0,
      warnings: t.warnings,
      source: n
    })];
  }
  return [$u(N, r, {
    allowReservedId: t.allowReservedId === !0 || n === "stored-document",
    warnings: t.warnings,
    source: n
  })];
}
function wq(e) {
  const t = /* @__PURE__ */ new Map(), n = /* @__PURE__ */ new Map(), r = (s, a = {}) => {
    t.has(s.id) && (t.delete(s.id), n.delete(s.id)), t.set(s.id, jt(s)), a.derivedFrom ? n.set(s.id, a.derivedFrom) : Na(s.id) && n.delete(s.id);
  };
  return e.forEach((s) => {
    const a = Vm(s, { source: "stored-document" }), o = a[0], l = a.length === 2 && o && a[1]?.id === To(o.id) ? a[1] : null;
    if (o && !l && !Na(o.id) && Ym(o)) {
      const i = To(o.id);
      n.get(i) === o.id && (t.delete(i), n.delete(i));
    }
    a.forEach((i) => {
      r(i, l && i.id === l.id ? { derivedFrom: o.id } : {});
    });
  }), [...t.values()];
}
function Jm(e, t = {}, n = "stored-document", r = []) {
  const s = Wr(e) ? e : {}, a = x3(s.meta, t, n === "model-input" ? r : void 0), o = Array.isArray(s.elements) ? n === "stored-document" ? wq(s.elements) : s.elements.flatMap((l) => Vm(l, {
    source: n,
    warnings: r
  })) : [];
  if (o.length > Fb) throw new Error("map_elements_limit_exceeded");
  if (n === "model-input") {
    const l = /* @__PURE__ */ new Set();
    o.forEach((i) => {
      if (l.has(i.id)) throw new Error(`map_element_duplicate:${i.id}`);
      l.add(i.id);
    });
  }
  return {
    meta: a,
    elements: o
  };
}
function N3() {
  return Jm(Ju(), Ju().meta, "stored-document");
}
function kl(e) {
  return e?.data ? Jm(e.data, Ju().meta, "stored-document") : N3();
}
function Ml(e, t = 0) {
  if (e.meta.status !== "active" || !mw(e.elements)) return "";
  const n = Tt(e.meta.name || "Map", 80) || "Map", r = e.elements.filter((a) => typeof a.text == "string" && a.text.trim()).map((a) => Tt(a.text, 40)).filter(Boolean).slice(0, 8), s = [...new Set(e.elements.map((a) => a.material).filter(Boolean))].slice(0, 5);
  return [
    `地图：${n}`,
    e.meta.mood && e.meta.mood !== "neutral" ? `氛围：${e.meta.mood}` : "",
    s.length ? `材质：${s.join(", ")}` : "",
    r.length ? `标注：${r.join(", ")}` : ""
  ].filter(Boolean).join(`
`);
}
function Yu(e) {
  return Tt(e.meta.name || "Map", 120) || "Map";
}
function es(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").trim().replace(/\s+/g, "-").slice(0, 120);
}
function Fc(e, t) {
  const n = es(e);
  if (!n || /[/\\:*?"<>|\u0000-\u001F]/.test(n) || n === "." || n === "..") throw new Error(t);
  return n;
}
function Sq(e) {
  const t = String(e || "").trim();
  if (!ww.has(t)) throw new Error("atlas_location_scale_invalid");
  return t;
}
function Aq(e) {
  const t = String(e || "").trim();
  if (!Sw.has(t)) throw new Error("atlas_location_status_invalid");
  return t;
}
function Tq(e) {
  const t = String(e || "").trim();
  if (!bf.has(t)) throw new Error("atlas_link_kind_invalid");
  return t;
}
function $3(e, t = 20) {
  if (Array.isArray(e))
    return [...new Set(e.map((n) => Tt(n, 80)).filter(Boolean))].slice(0, t);
}
function Wb(e, t, n, r = !0) {
  return r === !1 ? `link:${e}:${t}:${n}` : `link:${[e, t].sort().join(":")}:${n}`;
}
function Eq(e) {
  if (!Wr(e)) return null;
  const t = es(e.key), n = Tt(e.name, 120), r = String(e.scale || "").trim(), s = String(e.status || "").trim();
  if (!t || !n || !ww.has(r) || !Sw.has(s)) return null;
  const a = {
    key: t,
    name: n,
    scale: r,
    status: s
  }, o = es(e.parent);
  o && (a.parent = o);
  const l = Tt(e.mapDocId, 80);
  l && /^[\w.-]{1,80}$/i.test(l) && (a.mapDocId = l);
  const i = $3(e.aliases);
  i?.length && (a.aliases = i);
  const u = Tt(e.brief, 240);
  return u && (a.brief = u), a;
}
function Cq(e) {
  if (!Wr(e)) return null;
  const t = es(e.from), n = es(e.to), r = String(e.kind || "").trim();
  if (!t || !n || !bf.has(r)) return null;
  const s = e.bidirectional !== !1, a = {
    id: Tt(e.id, 160) || Wb(t, n, r, s),
    from: t,
    to: n,
    kind: r,
    bidirectional: s
  }, o = Tt(e.label, 120);
  return o && (a.label = o), a;
}
function Iq(e) {
  if (!Wr(e)) return null;
  const t = wf(e.actorKey), n = es(e.locationKey);
  return t && n ? {
    actorKey: t,
    locationKey: n
  } : null;
}
function xq() {
  return xw(jM());
}
function xw(e) {
  const t = Wr(e) ? e : {}, n = Array.isArray(t.locations) ? t.locations.map(Eq).filter((f) => !!f) : [], r = Array.isArray(t.links) ? t.links.map(Cq).filter((f) => !!f) : [], s = Array.isArray(t.actors) ? t.actors.map(Iq).filter((f) => !!f) : [], a = /* @__PURE__ */ new Set(), o = [];
  n.forEach((f) => {
    a.has(f.key) || (a.add(f.key), o.push(f));
  });
  const l = /* @__PURE__ */ new Set(), i = [];
  r.forEach((f) => {
    l.has(f.id) || (l.add(f.id), i.push(f));
  });
  const u = /* @__PURE__ */ new Map();
  s.forEach((f) => u.set(f.actorKey, f));
  const d = es(t.activeLocationKey);
  return {
    version: 1,
    ...d ? { activeLocationKey: d } : {},
    locations: o,
    links: i,
    actors: [...u.values()]
  };
}
function Sf(e) {
  return e?.data ? xw(e.data) : xq();
}
function Gb(e) {
  const t = e.locations.find((n) => n.key === e.activeLocationKey);
  return t ? `世界图：${t.name}` : "世界图";
}
function Wp(e) {
  const t = e.locations.find((r) => r.key === e.activeLocationKey), n = e.locations.filter((r) => r.status === "visited").length;
  return [
    t ? `当前地点：${t.name}` : "",
    `地点：${e.locations.length}`,
    `连接：${e.links.length}`,
    `人物：${e.actors.length}`,
    n ? `已探索：${n}` : ""
  ].filter(Boolean).join(`
`);
}
function kq(e, t) {
  const n = /* @__PURE__ */ new Set();
  let r = t;
  for (; r; ) {
    if (n.has(r)) return !0;
    n.add(r), r = e.get(r)?.parent;
  }
  return !1;
}
function L3(e) {
  return jt(e);
}
async function Mq(e) {
  const t = /* @__PURE__ */ new Map();
  if (e.document.elements.forEach((o) => {
    if (o.cat !== "actor") return;
    const l = wf(o.actorKey || o.id);
    l && t.set(l, o.id);
  }), !t.size) return {
    document: e.document,
    removedElements: [],
    otherDocumentRemovals: []
  };
  const n = (o, l) => {
    let i = !1;
    const u = /* @__PURE__ */ new Set(), d = [], f = o.elements.filter((S) => {
      if (S.cat !== "actor") return !0;
      const x = wf(S.actorKey || S.id);
      if (!t.has(x)) return !0;
      const I = l && t.get(x) === S.id;
      return I || (d.push(jt(S)), u.add(S.id), i = !0), I;
    }), m = new Set([...u].map((S) => To(S))), v = m.size ? f.filter((S) => m.has(S.id) ? (d.push(jt(S)), i = !0, !1) : !0) : f, y = d.map((S) => ({
      op: "remove",
      id: S.id
    }));
    return {
      document: i ? {
        ...o,
        elements: v
      } : o,
      removedElements: d,
      removeOps: y
    };
  }, r = n(e.document, !0), s = [], a = await Cr.where("sessionId").equals(e.sessionId).toArray();
  for (const o of a) {
    if (o.docType !== e.docType || o.docId === e.docId || o.status === "stale") continue;
    const l = n(kl(o), !1);
    if (!l.removedElements.length) continue;
    const i = Math.max(0, Number(o.revision) || 0) + 1, u = Ml(l.document, i);
    await Cr.put({
      ...o,
      data: l.document,
      digest: u,
      revision: i,
      title: Yu(l.document),
      updatedAt: e.timestamp
    }), await Db({
      sessionId: e.sessionId,
      docType: e.docType,
      docId: o.docId,
      revision: i,
      status: "active",
      managerRunId: e.managerRunId,
      sourceUserOrder: e.sourceUserOrder,
      sourceAssistantOrder: e.sourceAssistantOrder,
      source: "system",
      summary: Tt(`Actor dedupe ${i}`, 400),
      ops: l.removeOps,
      changedIds: l.removedElements.map((d) => d.id),
      removedElements: l.removedElements
    }), s.push({
      docId: o.docId,
      revision: i,
      removedElements: l.removedElements,
      removeOps: l.removeOps
    });
  }
  return {
    document: r.document,
    removedElements: r.removedElements,
    otherDocumentRemovals: s
  };
}
function kw(e = "") {
  const [t, n = "element"] = String(e || "").split(":"), r = n || "element";
  switch (t) {
    case "map_element_id_invalid":
      return "Invalid element id. Use a short stable id containing only letters, numbers, underscores, dots, colons, and hyphens.";
    case "map_element_id_reserved":
      return `${r} uses the reserved \`__label__\` prefix. Use a normal id instead.`;
    case "map_element_at_required":
      return `${r} is missing a position. Provide \`at:[x,y]\`. Legacy aliases such as pos/center/x+y are also accepted.`;
    case "map_element_rect_invalid":
      return `${r} has an invalid rect. Use positive dimensions such as \`rect:[120,80]\`.`;
    case "map_element_radius_required":
      return `${r} is missing a valid radius. \`circle\` must be a number greater than 0.`;
    case "map_element_points_required":
      return `${r} is missing a valid point array. \`path\` and \`curve\` need at least two points. With \`at\`, points are relative offsets; without \`at\`, the first point becomes the anchor.`;
    case "map_element_text_required":
      return `${r} is missing text content. \`text\` must be a short non-empty label.`;
    case "map_element_icon_invalid":
      return `${r} has an invalid icon. Use one of ${gw.join("/")}.`;
    case "map_element_icon_place_scale":
      return `${r} uses a place icon inside a scene map. Scene maps should draw local space with geometry, small objects, labels, and actor positions instead of house/castle/village/forest/temple/shop.`;
    case "map_element_shape_required":
      return `${r} is missing a shape field. Every element must provide exactly one of rect/circle/path/curve/icon/text.`;
    case "map_element_shape_conflict":
      return `${r} has multiple shape fields. Only the special "geometry + text" input is allowed, and it will be split into a derived label automatically.`;
    case "map_element_duplicate":
      return `${r} is duplicated. Use a stable unique id.`;
    case "map_element_not_found":
      return `${r} does not exist. Use MapInspect summary/elements first to find existing ids.`;
    case "map_element_already_exists":
      return `${r} already exists. Use \`modify\` to change it. If it is already identical, do not repeat the \`add\`.`;
    case "map_element_id_cannot_change":
      return `${r} cannot change id inside a \`modify\` op.`;
    case "map_meta_set_required":
      return "`meta` requires a `set` object.";
    case "map_modify_set_required":
      return `${r} is missing a \`set\` object. \`modify\` requires \`set\`.`;
    case "map_op_not_supported":
      return `Unsupported op: ${r}. Supported canonical ops are meta/add/modify/remove. Legacy init/reset/replace input is still absorbed at runtime.`;
    case "map_element_center_required":
      return `${r} is missing a center point.`;
    case "map_element_arc_angles_required":
      return `${r} is missing arc start/end angles.`;
    case "atlas_doc_id_invalid":
      return "Atlas has exactly one document: tavern.atlas/main.";
    case "atlas_activate_not_supported":
      return 'Atlas does not support activate:true. Move the player with move-actor(actorKey:"player").';
    case "atlas_location_key_invalid":
      return "Invalid atlas location key. Use a stable non-empty key without path separators or control characters.";
    case "atlas_location_create_required":
      return `${r} is new. Provide set.name, set.scale, and set.status.`;
    case "atlas_location_not_found":
      return `${r} does not exist in the atlas. Create the location before linking or moving actors there.`;
    case "atlas_parent_not_found":
      return `${r} does not exist. Create the parent location before assigning it.`;
    case "atlas_parent_cycle":
      return `${r} would create a parent cycle.`;
    case "atlas_link_location_not_found":
      return `${r} does not exist. Atlas links cannot point to missing locations.`;
    case "atlas_link_kind_invalid":
      return "Invalid atlas link kind. Use door/stairs/elevator/path/road/portal/passage.";
    case "atlas_link_not_found":
      return `${r} does not exist. Use MapInspect links first.`;
    case "atlas_location_has_children":
    case "atlas_location_has_links":
    case "atlas_location_has_actors":
    case "atlas_location_active":
      return `${r} still has dependencies. Remove child locations, links, actors, or active status first.`;
    case "atlas_op_not_supported":
      return `Unsupported atlas op: ${r}. Use upsert-location/remove-location/upsert-link/remove-link/move-actor.`;
    default:
      return e;
  }
}
function CT(e) {
  const t = e.slice(0, 3).map((r) => `${r.index >= 0 ? `op ${r.index + 1}` : "transaction"}: ${r.hint || kw(r.error) || r.error}`), n = e.length > t.length ? `; plus ${e.length - t.length} more failures` : "";
  return t.length ? `${t.join("; ")}${n}` : "";
}
function Pq(e, t = []) {
  return mw(e.elements) ? e.meta.status === "uninitialized" ? {
    document: {
      ...e,
      meta: zb(e.meta, { status: "active" })
    },
    statusChanged: !0
  } : {
    document: e,
    statusChanged: !1
  } : e.meta.status === "active" ? (t.push("Map stays uninitialized: it needs at least one spatial geometry element. Name, viewBox, or text labels alone are not enough."), {
    document: {
      ...e,
      meta: zb(e.meta, { status: "uninitialized" })
    },
    statusChanged: !0
  }) : {
    document: e,
    statusChanged: !1
  };
}
function Rq(e, t = {}) {
  const n = Tt(t.query, 120).toLowerCase(), r = String(t.elementType || t.type || "").trim(), s = String(t.category || t.cat || "").trim(), a = Math.max(0, Number(t.offset) || 0), o = Math.max(1, Math.min(Qs, Number(t.limit) || 30)), l = e.elements.filter((d) => {
    const f = k3(d) || "";
    return r && f !== r || s && d.cat !== s ? !1 : n ? [
      d.id,
      d.cat,
      f,
      d.text,
      d.icon,
      d.material,
      d.certainty
    ].map((m) => String(m || "").toLowerCase()).join(`
`).includes(n) : !0;
  }), i = l.slice(a, a + o).map(L3), u = a + i.length < l.length ? a + i.length : 0;
  return {
    elements: i,
    count: l.length,
    truncated: u > 0,
    nextOffset: u
  };
}
function Nq(e, t = {}) {
  const n = Tt(t.query, 120).toLowerCase(), r = es(t.parent), s = String(t.status || "").trim(), a = Math.max(0, Number(t.offset) || 0), o = Math.max(1, Math.min(Qs, Number(t.limit) || 30)), l = e.locations.filter((d) => r && d.parent !== r || s && d.status !== s ? !1 : n ? [
    d.key,
    d.name,
    d.scale,
    d.status,
    d.brief,
    ...d.aliases || []
  ].join(`
`).toLowerCase().includes(n) : !0), i = l.slice(a, a + o).map((d) => jt(d)), u = a + i.length < l.length ? a + i.length : 0;
  return {
    locations: i,
    count: l.length,
    truncated: u > 0,
    nextOffset: u
  };
}
function $q(e, t = {}) {
  const n = Tt(t.query, 120).toLowerCase(), r = es(t.from), s = es(t.to), a = String(t.kind || "").trim(), o = Math.max(0, Number(t.offset) || 0), l = Math.max(1, Math.min(Qs, Number(t.limit) || 30)), i = e.links.filter((f) => r && f.from !== r || s && f.to !== s || a && f.kind !== a ? !1 : n ? [
    f.id,
    f.from,
    f.to,
    f.kind,
    f.label
  ].join(`
`).toLowerCase().includes(n) : !0), u = i.slice(o, o + l).map((f) => jt(f)), d = o + u.length < i.length ? o + u.length : 0;
  return {
    links: u,
    count: i.length,
    truncated: d > 0,
    nextOffset: d
  };
}
function Lq(e = {}) {
  if (!Wr(e)) throw new Error("atlas_location_set_required");
  const t = {};
  if ("name" in e) {
    const n = Tt(e.name, 120);
    if (!n) throw new Error("atlas_location_name_required");
    t.name = n;
  }
  if ("scale" in e && (t.scale = Sq(e.scale)), "status" in e && (t.status = Aq(e.status)), "parent" in e) {
    const n = es(e.parent);
    n ? t.parent = n : t.parent = void 0;
  }
  if ("mapDocId" in e) {
    const n = Tt(e.mapDocId, 80);
    n ? t.mapDocId = hq(n, "atlas_location_map_doc_id_invalid") : t.mapDocId = void 0;
  }
  if ("aliases" in e && (t.aliases = $3(e.aliases) || []), "brief" in e) {
    const n = Tt(e.brief, 240);
    n ? t.brief = n : t.brief = void 0;
  }
  return t;
}
function Dq(e, t) {
  if (!Array.isArray(t)) throw new Error("state_patch_ops_must_be_array");
  if (!t.length) throw new Error("state_patch_ops_required");
  if (t.length > _3) throw new Error("state_patch_ops_limit_exceeded");
  let n = xw(e);
  const r = [], s = [], a = [], o = /* @__PURE__ */ new Set();
  let l = 0, i = 0, u = !1, d;
  const f = () => new Map(n.locations.map((I) => [I.key, I])), m = (I) => n.links.findIndex((E) => E.id === I), v = (I) => n.actors.findIndex((E) => E.actorKey === I), y = () => {
    const I = f();
    n.locations.forEach((E) => {
      if (E.parent) {
        if (E.parent === E.key) throw new Error(`atlas_parent_cycle:${E.key}`);
        if (!I.has(E.parent)) throw new Error(`atlas_parent_not_found:${E.parent}`);
      }
    }), n.locations.forEach((E) => {
      if (kq(I, E.key)) throw new Error(`atlas_parent_cycle:${E.key}`);
    });
  }, S = (I, E) => {
    const N = f();
    if (!N.has(I)) throw new Error(`atlas_link_location_not_found:${I}`);
    if (!N.has(E)) throw new Error(`atlas_link_location_not_found:${E}`);
  }, x = (I, E) => {
    r.push(jt(I)), o.add(E), u = !0, l += 1;
  };
  return t.forEach((I, E) => {
    try {
      if (!Wr(I)) throw new Error("atlas_op_invalid");
      const N = String(I.op || "").trim();
      if (N === "upsert-location") {
        const $ = Fc(I.key, "atlas_location_key_invalid"), p = Lq(I.set || {}), w = Array.isArray(I.unset) ? I.unset.map((G) => String(G || "").trim()) : [], _ = w.find((G) => !fq.has(G));
        if (_) throw new Error(`atlas_unset_field_invalid:${_ || "empty"}`);
        const b = w.filter(Boolean), C = n.locations.findIndex((G) => G.key === $);
        if (C < 0) {
          if (!p.name || !p.scale || !p.status) throw new Error(`atlas_location_create_required:${$}`);
          const G = {
            key: $,
            name: p.name,
            scale: p.scale,
            status: p.status
          };
          p.parent && (G.parent = p.parent), p.mapDocId && (G.mapDocId = p.mapDocId), p.aliases?.length && (G.aliases = p.aliases), p.brief && (G.brief = p.brief), n.locations.push(G), y(), x({
            op: "upsert-location",
            key: $,
            set: jt(p),
            ...b.length ? { unset: b } : {}
          }, `location:${$}`);
          return;
        }
        const k = n.locations[C], P = {
          ...k,
          ...p
        }, H = { ...p };
        if (k.status === "visited" && p.status === "mentioned" && (P.status = "visited", H.status = "visited", a.push(`${$} is already visited; status was not downgraded to mentioned.`)), b.forEach((G) => {
          delete P[G];
        }), !P.name || !P.scale || !P.status) throw new Error(`atlas_location_required_field:${$}`);
        if (n.locations[C] = P, y(), Ys(k, P)) {
          i += 1;
          return;
        }
        x({
          op: "upsert-location",
          key: $,
          set: jt(H),
          ...b.length ? { unset: b } : {}
        }, `location:${$}`);
        return;
      }
      if (N === "remove-location") {
        const $ = Fc(I.key, "atlas_location_key_invalid"), p = n.locations.findIndex((w) => w.key === $);
        if (p < 0) throw new Error(`atlas_location_not_found:${$}`);
        if (n.activeLocationKey === $) throw new Error(`atlas_location_active:${$}`);
        if (n.locations.some((w) => w.parent === $)) throw new Error(`atlas_location_has_children:${$}`);
        if (n.links.some((w) => w.from === $ || w.to === $)) throw new Error(`atlas_location_has_links:${$}`);
        if (n.actors.some((w) => w.locationKey === $)) throw new Error(`atlas_location_has_actors:${$}`);
        n.locations.splice(p, 1), x({
          op: "remove-location",
          key: $
        }, `location:${$}`);
        return;
      }
      if (N === "upsert-link") {
        const $ = Fc(I.from, "atlas_link_from_required"), p = Fc(I.to, "atlas_link_to_required"), w = Tq(I.kind), _ = I.bidirectional !== !1, b = Tt(I.id, 160) || Wb($, p, w, _);
        S($, p);
        const C = {
          id: b,
          from: $,
          to: p,
          kind: w,
          bidirectional: _
        }, k = Tt(I.label, 120);
        k && (C.label = k);
        const P = m(b);
        if (P >= 0) {
          if (Ys(n.links[P], C)) {
            i += 1;
            return;
          }
          n.links[P] = C;
        } else n.links.push(C);
        x({
          op: "upsert-link",
          id: b,
          from: $,
          to: p,
          kind: w,
          ...k ? { label: k } : {},
          bidirectional: _
        }, `link:${b}`);
        return;
      }
      if (N === "remove-link") {
        let $ = Tt(I.id, 160);
        if (!$) {
          const w = es(I.from), _ = es(I.to), b = String(I.kind || "").trim();
          if (!w || !_ || !bf.has(b)) throw new Error("atlas_link_remove_locator_required");
          $ = Wb(w, _, b, I.bidirectional !== !1);
        }
        const p = m($);
        if (p < 0) throw new Error(`atlas_link_not_found:${$}`);
        n.links.splice(p, 1), x({
          op: "remove-link",
          id: $
        }, `link:${$}`);
        return;
      }
      if (N === "move-actor") {
        const $ = wf(I.actorKey), p = Fc(I.locationKey, "atlas_actor_location_required");
        if (!$) throw new Error("atlas_actor_key_required");
        if (!f().get(p)) throw new Error(`atlas_location_not_found:${p}`);
        const w = {
          actorKey: $,
          locationKey: p
        }, _ = v($), b = _ >= 0 ? n.actors[_] : null;
        if (b && Ys(b, w) && !($ === "player" && n.activeLocationKey !== p)) {
          i += 1;
          return;
        }
        if (_ >= 0 ? n.actors[_] = w : n.actors.push(w), $ === "player") {
          n.activeLocationKey = p;
          const C = n.locations.findIndex((k) => k.key === p);
          C >= 0 && n.locations[C].status !== "visited" && (n.locations[C] = {
            ...n.locations[C],
            status: "visited"
          }), d = n.locations[C]?.mapDocId;
        }
        x({
          op: "move-actor",
          actorKey: $,
          locationKey: p
        }, `actor:${$}`);
        return;
      }
      throw new Error(`atlas_op_not_supported:${N || "unknown"}`);
    } catch (N) {
      s.push({
        index: E,
        error: N instanceof Error ? N.message : String(N || "atlas_op_failed")
      });
    }
  }), {
    document: n,
    effectiveOps: r,
    appliedCount: l,
    satisfiedCount: i,
    failed: s,
    warnings: a,
    changedIds: [...o],
    changed: u,
    syncedActiveMapDocId: d
  };
}
function Oq(e, t, n = []) {
  if (!Wr(e)) throw new Error(`map_modify_set_required:${t}`);
  const r = {};
  if ("id" in e && Tt(e.id, 120) && Tt(e.id, 120) !== t) throw new Error(`map_element_id_cannot_change:${t}`);
  if ("at" in e || "pos" in e || "center" in e || "position" in e || "x" in e || "y" in e || "cx" in e || "cy" in e) {
    const a = hi(e.at ?? e.pos ?? e.center ?? e.position) || hi({
      x: e.x,
      y: e.y
    }) || hi({
      x: e.cx,
      y: e.cy
    });
    if (!a) throw new Error(`map_element_at_required:${t}`);
    r.at = a;
  }
  if ("cat" in e && (r.cat = Gm(e.cat, "wall")), "material" in e) if (e.material === null) r.material = void 0;
  else {
    const a = Tw(e.material, {
      elementId: t,
      warnings: n,
      source: "model-input"
    });
    a && (r.material = a);
  }
  if ("certainty" in e) if (e.certainty === null) r.certainty = void 0;
  else {
    const a = Ew(e.certainty, void 0, {
      elementId: t,
      warnings: n
    });
    a && (r.certainty = a === "confirmed" ? void 0 : a);
  }
  if ("rect" in e || "size" in e || "width" in e || "height" in e) {
    const a = Aw(e.rect ?? e.size ?? [e.width, e.height]);
    if (!a || a[0] <= 0 || a[1] <= 0) throw new Error(`map_element_rect_invalid:${t}`);
    r.rect = a;
  }
  if ("circle" in e || "r" in e || "radius" in e) {
    const a = nv(e.circle ?? e.r ?? e.radius);
    if (a === null) throw new Error(`map_element_radius_required:${t}`);
    r.circle = a;
  }
  if ("path" in e || "points" in e || "line" in e) {
    const a = r.at || null, o = Ma(e.path ?? e.points ?? e.line, t, a);
    r.at || (r.at = o.at), r.path = o.points;
  }
  if ("curve" in e) {
    const a = r.at || null, o = Ma(e.curve, t, a);
    r.at || (r.at = o.at), r.curve = o.points;
  }
  if ("icon" in e) {
    const a = Cw(e.icon);
    if (!a) throw new Error(`map_element_icon_invalid:${t}`);
    I3(a, t), r.icon = a;
  }
  ("text" in e || "content" in e || "label" in e || "value" in e) && (r.text = Tt(e.text ?? e.content ?? e.label ?? e.value, 240) || void 0), "closed" in e && (r.closed = e.closed === !0), "fill" in e && (r.fill = e.fill === null ? void 0 : Tt(e.fill, 120) || void 0), r.material && r.fill && (n.push(`Dropped legacy fill for ${t} because material:"${r.material}" was supplied.`), delete r.fill), "style" in e && (r.style = e.style === null ? void 0 : Iw(e.style));
  const s = String(e.type || "").trim();
  if (s === "fill" && !r.path) {
    const a = Ma(e.points, t, r.at || null);
    r.at = a.at, r.path = a.points, r.closed = !0;
  }
  if (s === "arc" && !r.curve) {
    const a = Ma(M3(e, t), t, r.at || null);
    r.at = a.at, r.curve = a.points;
  }
  return r;
}
function IT(e, t, n = []) {
  const r = h3(e, t);
  return Object.prototype.hasOwnProperty.call(t, "material") && t.material && r.fill && (n.push(`Dropped legacy fill for ${e.id} because material:"${t.material}" was supplied.`), delete r.fill), $u(r, e.id, {
    allowReservedId: Na(e.id),
    warnings: n,
    source: "model-input"
  });
}
function Fq(e, t) {
  const n = {}, r = /* @__PURE__ */ new Set([...Object.keys(e), ...Object.keys(t)]);
  return r.delete("id"), r.forEach((s) => {
    const a = Object.prototype.hasOwnProperty.call(e, s), o = Object.prototype.hasOwnProperty.call(t, s);
    if (o && !Ys(e[s], t[s])) {
      n[s] = jt(t[s]);
      return;
    }
    a && !o && (n[s] = null);
  }), n;
}
function Ym(e) {
  return b3.some((t) => t === "circle" ? typeof e.circle == "number" : t === "icon" ? typeof e.icon == "string" && !!e.icon.trim() : Array.isArray(e[t]));
}
function Bq(e) {
  const t = { ...e };
  return delete t.text, t;
}
function Uq(e) {
  return Object.keys(e).length > 0;
}
function t_(e, t, n, r) {
  const s = To(e.id);
  return $u({
    id: s,
    at: r || n?.at || P3(e),
    cat: "label",
    text: t,
    style: n?.style
  }, s, { allowReservedId: !0 });
}
function n_(e, t, n) {
  return {
    cat: "label",
    text: t,
    ...n ? { at: n } : {},
    ...e.style ? { style: jt(e.style) } : {}
  };
}
function r_(e) {
  return e.cat === "label" && typeof e.text == "string" && !!e.text.trim() && !Ym(e) && !e.actorKey && e.closed !== !0 && !e.fill;
}
function qq(e, t, n = {}) {
  const r = Object.prototype.hasOwnProperty.call(n, "text"), s = Object.prototype.hasOwnProperty.call(n, "at"), a = Object.prototype.hasOwnProperty.call(n, "style");
  return $u({
    id: e,
    at: s ? n.at : t.at,
    cat: "label",
    text: r ? n.text : t.text,
    style: a ? n.style : t.style
  }, e, { allowReservedId: !0 });
}
async function Hq(e = "", t = ir, n = kr) {
  if (t !== ir || n !== kr) return await bi(e, t, n);
  const r = await bi(e, t, n);
  return r || await Uf(e, { touchSession: !1 });
}
async function Vb(e = "") {
  const t = await bi(e, Hr, ea);
  return t || (await Uf(e, { touchSession: !1 }), await bi(e, Hr, ea));
}
function zq(e, t) {
  const n = [];
  return e.elements.forEach((r) => {
    n.push({
      op: "remove",
      id: r.id,
      _internalSoft: !0
    });
  }), n.push({
    op: "meta",
    set: {
      name: t.meta.name,
      viewBox: t.meta.viewBox,
      theme: t.meta.theme,
      status: t.meta.status,
      mood: t.meta.mood,
      hint: t.meta.hint
    }
  }), t.elements.forEach((r) => {
    n.push({
      op: "add",
      element: r
    });
  }), n;
}
function Kq(e, t) {
  if (!Array.isArray(t)) throw new Error("state_patch_ops_must_be_array");
  if (!t.length) throw new Error("state_patch_ops_required");
  if (t.length > _3) throw new Error("state_patch_ops_limit_exceeded");
  const n = Jm(e, e.meta, "stored-document");
  let r = jt(n);
  const s = [], a = [], o = /* @__PURE__ */ new Set(), l = [], i = [];
  let u = 0, d = 0, f = !1;
  const m = (I) => r.elements.findIndex((E) => E.id === I), v = (I) => {
    const E = zb(r.meta, I);
    if (Ys(E, r.meta)) {
      d += 1;
      return;
    }
    r.meta = E, s.push({
      op: "meta",
      set: jt(I)
    }), f = !0, u += 1, o.add("meta");
  }, y = (I) => {
    const E = m(I.id);
    if (E >= 0) {
      if (Ys(r.elements[E], I)) {
        d += 1;
        return;
      }
      throw new Error(`map_element_already_exists:${I.id}`);
    }
    r.elements.push(jt(I)), s.push({
      op: "add",
      element: jt(I)
    }), f = !0, u += 1, o.add(I.id);
  }, S = (I, E = {}) => {
    const N = m(I);
    if (N < 0) {
      if (E.soft) {
        d += 1;
        return;
      }
      throw new Error(`map_element_not_found:${I}`);
    }
    const [$] = r.elements.splice(N, 1);
    if (l.push(jt($)), s.push({
      op: "remove",
      id: I,
      ...E.soft ? { _internalSoft: !0 } : {}
    }), f = !0, u += 1, o.add(I), E.cascadeLabel !== !1 && !Na(I)) {
      const p = To(I);
      m(p) >= 0 && S(p, {
        soft: !0,
        cascadeLabel: !1
      });
    }
  }, x = (I, E) => {
    const N = m(I);
    if (N < 0) throw new Error(`map_element_not_found:${I}`);
    const $ = r.elements[N];
    if (Na(I)) {
      const R = qq(I, $, E);
      if (Ys($, R)) {
        d += 1;
        return;
      }
      r.elements[N] = R, r_($) && Ys($.style, R.style) ? s.push({
        op: "modify",
        id: I,
        set: n_($, R.text, R.at)
      }) : (l.push(jt($)), s.push({
        op: "remove",
        id: I,
        _internalSoft: !0
      }), s.push({
        op: "add",
        element: jt(R)
      })), f = !0, u += 1, o.add(I);
      return;
    }
    const p = To(I), w = m(p), _ = w >= 0 ? r.elements[w] : null, b = Object.prototype.hasOwnProperty.call(E, "text"), C = Tt(b ? E.text : $.text || _?.text, 240), k = b ? {
      ...Bq(E),
      ...C ? {} : { text: void 0 }
    } : E, P = Uq(k) || b && Ym($) ? IT($, k, a) : jt($), H = Ym(P), G = b && C && !H ? {
      ...P,
      text: C
    } : P, ee = !Na(I) && H && !!C && y3(P.cat), J = ee ? P : G;
    let z = !1;
    if (Ys($, J) || (r.elements[N] = J, s.push({
      op: "modify",
      id: I,
      set: Fq($, J)
    }), f = !0, u += 1, o.add(I), z = !0), ee && !Na(I) && E.at && $.at) {
      const R = To(I), V = m(R);
      if (V >= 0) {
        const le = r.elements[V], ve = [E.at[0] - $.at[0], E.at[1] - $.at[1]];
        if (ve[0] || ve[1]) {
          const ke = [Number((le.at[0] + ve[0]).toFixed(2)), Number((le.at[1] + ve[1]).toFixed(2))], be = ee ? t_(J, C, le, ke) : IT(le, { at: ke }, a);
          if (r.elements[V] = be, ee && !r_(le) ? (l.push(jt(le)), s.push({
            op: "remove",
            id: R,
            _internalSoft: !0
          }), s.push({
            op: "add",
            element: jt(be)
          })) : s.push({
            op: "modify",
            id: R,
            set: ee ? n_(le, C, ke) : { at: ke }
          }), f = !0, ee && (u += 1), o.add(R), ee) return;
        }
      }
    }
    if (ee) if (w >= 0) {
      const R = r.elements[w], V = t_(J, C, R);
      if (!Ys(R, V)) {
        r.elements[w] = V, r_(R) ? s.push({
          op: "modify",
          id: p,
          set: n_(R, C)
        }) : (l.push(jt(R)), s.push({
          op: "remove",
          id: p,
          _internalSoft: !0
        }), s.push({
          op: "add",
          element: jt(V)
        })), f = !0, u += 1, o.add(p);
        return;
      }
    } else {
      const R = t_(J, C);
      r.elements.push(R), s.push({
        op: "add",
        element: jt(R)
      }), f = !0, u += 1, o.add(p);
      return;
    }
    if (!ee && w >= 0) {
      S(p, {
        soft: !0,
        cascadeLabel: !1
      });
      return;
    }
    z || (d += 1);
  };
  for (let I = 0; I < t.length; I += 1) {
    const E = t[I];
    try {
      if (!Wr(E)) throw new Error("op_must_be_object");
      const N = String(E.op || "").trim();
      if (!N) throw new Error("map_op_not_supported:empty");
      if (N === "meta") {
        v(_q(E.set ?? E.changes ?? E.meta, a));
        continue;
      }
      if (N === "add") {
        Vm(E.element ?? E, {
          source: "model-input",
          warnings: a
        }).forEach(($) => y($));
        continue;
      }
      if (N === "remove") {
        const $ = Tt(E.id, 120);
        if (!$) throw new Error("map_element_id_invalid");
        S($);
        continue;
      }
      if (N === "modify") {
        const $ = Tt(E.id, 120);
        if (!$) throw new Error("map_element_id_invalid");
        x($, Oq(E.set ?? E.changes, $, a));
        continue;
      }
      if (N === "replace") {
        const $ = Tt(E.id, 120);
        if (!$) throw new Error("map_element_id_invalid");
        if (m($) < 0) throw new Error(`map_element_not_found:${$}`);
        S($, { cascadeLabel: !0 }), Vm(E.element, {
          forcedId: $,
          source: "model-input",
          warnings: a
        }).forEach((p) => y(p));
        continue;
      }
      if (N === "init" || N === "reset") {
        const $ = Jm(E.document || {
          meta: {
            ...Wr(E.meta) ? E.meta : {},
            status: "active"
          },
          elements: E.elements
        }, {
          ...r.meta,
          status: "active",
          hint: r.meta.hint
        }, "model-input", a);
        if (N === "init" && r.meta.status === "active" && r.elements.length && E.replaceDocument !== !0) throw new Error("state_init_existing_document_requires_reset");
        zq(r, $).forEach((p) => {
          p.op === "meta" ? v(p.set) : p.op === "add" ? y(p.element) : p.op === "remove" && S(p.id, {
            soft: p._internalSoft === !0,
            cascadeLabel: !1
          });
        });
        continue;
      }
      throw new Error(`map_op_not_supported:${N}`);
    } catch (N) {
      const $ = N instanceof Error ? N.message : String(N || "map_op_failed");
      i.push({
        index: I,
        error: $,
        hint: kw($)
      });
      break;
    }
  }
  if (!i.length) {
    const I = Pq(r, a);
    r = I.document, I.statusChanged && (s.push({
      op: "meta",
      set: { status: r.meta.status }
    }), f = !0, o.add("meta"));
  }
  return !i.length && r.elements.length > Fb && i.push({
    index: -1,
    error: "map_elements_limit_exceeded",
    hint: `Map has more than ${Fb} elements. Split or reduce the element count.`
  }), !i.length && f && Ys(TT(n), TT(r)) && (d += u, u = 0, f = !1, o.clear(), s.splice(0, s.length), l.splice(0, l.length)), {
    document: r,
    effectiveOps: s,
    appliedCount: u,
    satisfiedCount: d,
    failed: i,
    warnings: a,
    changedIds: [...o],
    removedElements: l,
    changed: f
  };
}
function Wq() {
  return {
    type: "object",
    description: "One map element. It must have `id` and `cat`, plus exactly one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. Most elements use `at:[x,y]`; `path` and `curve` may omit `at` and let the first point become the anchor.",
    properties: {
      id: {
        type: "string",
        description: "Stable unique id. Do not use the reserved `__label__` prefix."
      },
      at: {
        type: "array",
        items: { type: "number" },
        minItems: 2,
        maxItems: 2,
        description: "Anchor coordinate `[x,y]`. Most elements should provide `at`; `path` and `curve` may omit it and use the first point as the anchor. North = smaller y, south = larger y, west = smaller x, east = larger x."
      },
      cat: {
        type: "string",
        enum: [...tv],
        description: "Semantic category such as wall, door, marker, actor, terrain, road, light, or label. Use terrain for walkable ground/floor; do not use floor, ground, or region."
      },
      material: {
        type: "string",
        enum: [...vw],
        description: "Optional renderer-owned material enum. Use only when RP facts confirm it. Common values include wood, stone, tile, carpet, blood, water, grass, dirt, snow, metal, rune, warm-light, cold-light, shadow, or unknown. Do not invent material names."
      },
      certainty: {
        type: "string",
        enum: [...yw],
        description: "Optional semantic confidence. Omit for confirmed facts. Use inferred for likely/unconfirmed placements and unknown when the position or existence is explicitly uncertain."
      },
      rect: {
        type: "array",
        items: { type: "number" },
        minItems: 2,
        maxItems: 2,
        description: "Rectangle `[width,height]`. The top-left corner is at `at`."
      },
      circle: {
        type: "number",
        description: "Circle radius. The center is at `at`."
      },
      path: {
        type: "array",
        items: {
          type: "array",
          items: { type: "number" },
          minItems: 2,
          maxItems: 2
        },
        description: "Polyline point array. With `at`, points are relative offsets. Without `at`, points are absolute coordinates and the first point becomes the anchor."
      },
      curve: {
        type: "array",
        items: {
          type: "array",
          items: { type: "number" },
          minItems: 2,
          maxItems: 2
        },
        description: "Smooth curve point array. The anchor and relative/absolute rules are the same as `path`."
      },
      icon: {
        type: "string",
        enum: [...bw],
        description: "Named scene-map icon marker. Use only small objects or abstract markers such as chest/table/bed/barrel/door/stairs/portal/skull/trap/fire/tree/rock/water/heart/lips/lovers/cherish/love-letter/locked-heart/perfume, or x/o/+ and arrows for markers and directions. Do not put place icons such as house/castle/village/forest/temple/shop inside a scene map; describe places in the atlas and draw local space with geometry, labels, and actor positions."
      },
      text: {
        type: "string",
        description: "Short label text. If you provide text together with label-eligible geometry in an add input, the runtime will split the text into a derived label element automatically. Terrain, light, grid, and label categories do not derive labels from geometry text."
      },
      actorKey: {
        type: "string",
        description: 'Optional full-session actor identity key for cat:"actor". If omitted, the element id is used as the actor key fallback.'
      },
      closed: {
        type: "boolean",
        description: "Whether a path or curve should be closed."
      }
    },
    required: ["id", "cat"],
    additionalProperties: !1
  };
}
function Gq() {
  const e = {
    type: "array",
    items: { type: "number" },
    minItems: 2,
    maxItems: 2
  }, t = {
    type: "array",
    items: e
  };
  return {
    type: "object",
    description: "For map `meta`/`modify` and atlas `upsert-location`, merge only these whitelisted semantic fields. Renderer style fields such as fill/style/opacity/zIndex/rotation/blur and visual transform scale are intentionally not part of this schema.",
    properties: {
      name: {
        type: "string",
        description: "Map title or atlas location display name. Empty string clears a map title."
      },
      viewBox: {
        type: "array",
        items: { type: "number" },
        minItems: 4,
        maxItems: 4,
        description: "Map camera frame `[x,y,width,height]`; it does not move elements."
      },
      theme: {
        type: "string",
        enum: [...w3],
        description: "Map renderer theme."
      },
      status: {
        type: "string",
        enum: [...S3, ...Sw],
        description: "Map status or atlas location status, depending on docType/op."
      },
      mood: {
        type: "string",
        enum: [...g3],
        description: "Map mood; use only when scene facts support it."
      },
      hint: {
        type: "string",
        description: "Map maintenance hint for uninitialized scene maps."
      },
      at: {
        ...e,
        description: "Map element anchor coordinate `[x,y]`."
      },
      cat: {
        type: "string",
        enum: [...tv],
        description: "Map element semantic category."
      },
      material: {
        type: "string",
        enum: [...vw],
        description: "Map element material enum. Use unknown when the material is explicitly unclear."
      },
      certainty: {
        type: "string",
        enum: [...yw],
        description: "Map element certainty enum. Use confirmed to clear stored uncertainty."
      },
      rect: {
        ...e,
        description: "Map element rectangle size `[width,height]`."
      },
      circle: {
        type: "number",
        description: "Map element circle radius."
      },
      path: {
        ...t,
        description: "Map element polyline points."
      },
      curve: {
        ...t,
        description: "Map element curve points."
      },
      icon: {
        type: "string",
        enum: [...bw],
        description: "Map element icon."
      },
      text: {
        type: "string",
        description: "Map label text. Empty string clears source/derived label text."
      },
      actorKey: {
        type: "string",
        description: 'Map actor identity key for cat:"actor".'
      },
      closed: {
        type: "boolean",
        description: "Whether a path or curve should be closed."
      },
      scale: {
        type: "string",
        enum: [...ww],
        description: "Atlas location scale."
      },
      parent: {
        type: "string",
        description: 'Atlas parent location key. Prefer unset:["parent"] to clear it.'
      },
      mapDocId: {
        type: "string",
        description: 'Atlas linked scene-map docId. Prefer unset:["mapDocId"] to clear it.'
      },
      aliases: {
        type: "array",
        items: { type: "string" },
        description: "Atlas location aliases."
      },
      brief: {
        type: "string",
        description: 'Short atlas location note. Prefer unset:["brief"] to clear it.'
      }
    },
    additionalProperties: !1
  };
}
function Vq() {
  const e = Wq(), t = Gq();
  return [
    {
      type: "function",
      function: {
        name: _s.LIST,
        description: [
          "List structured map documents in the current RP session.",
          "Returns document entries only. It does not read full state, elements, or patch history.",
          "Use before MapInspect when you need available scene-map documents, or when you need the atlas world index.",
          "Map scope is fixed to this tavern session; it cannot inspect text sources, memory Markdown, RP chat history, character cards, world books, settings, or plugin source code."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: { docType: {
            type: "string",
            enum: [ir, Hr],
            description: "Optional structured document type filter: `tavern.map` for scene maps, `tavern.atlas` for the world index."
          } },
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: _s.READ,
        description: [
          "Inspect a structured scene map or world atlas document in the current RP session.",
          "Use `summary` first when choosing what to patch, checking revision, or deciding whether a map/atlas update is needed.",
          "For `tavern.map`, use `summary` first, `elements` to browse map elements, `element` for one id, `document` for the full map, and `history` for saved map patch transactions.",
          "For `tavern.atlas`, use `summary`, `document`, `locations`, `location`, `links`, `actors`, or `history`. Atlas does not have map elements.",
          "This inspects structured spatial data only. Use LS/Grep/Read for text evidence, memory files, chat history, and worldbook material."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            docType: {
              type: "string",
              enum: [ir, Hr],
              description: "Structured document type. Use `tavern.map` for scene maps or `tavern.atlas` for the world index."
            },
            docId: {
              type: "string",
              description: "Structured document id. Omit for the active map; atlas always uses `main`."
            },
            mode: {
              type: "string",
              enum: [
                "summary",
                "elements",
                "document",
                "element",
                "history",
                "locations",
                "location",
                "links",
                "actors"
              ],
              description: "For maps: summary/elements/document/element/history. For atlas: summary/document/locations/location/links/actors/history."
            },
            elementId: {
              type: "string",
              description: "Required for `element` mode. Exact element id to read."
            },
            locationKey: {
              type: "string",
              description: "Required for atlas `location` mode."
            },
            actorKey: {
              type: "string",
              description: "Optional atlas `actors` filter."
            },
            parent: {
              type: "string",
              description: "Optional atlas `locations` parent filter."
            },
            status: {
              type: "string",
              enum: ["mentioned", "visited"],
              description: "Optional atlas `locations` status filter."
            },
            from: {
              type: "string",
              description: "Optional atlas `links` from filter."
            },
            to: {
              type: "string",
              description: "Optional atlas `links` to filter."
            },
            kind: {
              type: "string",
              enum: [...bf],
              description: "Optional atlas `links` kind filter."
            },
            elementType: {
              type: "string",
              enum: [..._w],
              description: "Optional `elements`-mode shape filter such as rect, circle, path, curve, icon, or text."
            },
            category: {
              type: "string",
              enum: [...tv],
              description: "Optional `elements`-mode category filter such as wall, door, marker, terrain, road, or label."
            },
            query: {
              type: "string",
              description: "Optional `elements`-mode text query matched against id, category, shape, icon, label text, material, and certainty."
            },
            offset: {
              type: "number",
              minimum: 0,
              description: "Pagination offset for `elements` or `history` results. Default 0."
            },
            limit: {
              type: "number",
              minimum: 1,
              maximum: Qs,
              description: "Maximum `elements` or `history` results to return. Default 30 for `elements`, 20 for `history`."
            },
            tail: {
              type: "number",
              minimum: 1,
              maximum: Qs,
              description: "For `history` mode, return the final N patch transactions."
            }
          },
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: _s.PATCH,
        description: [
          "Apply one structured map/atlas patch transaction to the current RP session.",
          "Use this only for confirmed spatial changes from RP source text or a user-requested correction. If nothing spatial changed, do not patch.",
          "Read MapInspect summary first unless you already have the current doc, ids, and revision from this turn. Use `baseRevision` when you are protecting against concurrent changes.",
          "For `tavern.map`, canonical ops are `meta`, `add`, `modify`, and `remove`. One MapPatch call is one atomic transaction and becomes exactly one revision when it saves.",
          "Use `meta` to update document fields such as name, viewBox, theme, status, mood, or hint. Mood enum is neutral/warm/cold/dark/mystic/danger/calm; write it only when the scene facts support it.",
          "Each element has `id` and `cat`, plus exactly one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. Most elements use `at:[x,y]`; `path` and `curve` may omit `at` and use the first point as the anchor.",
          "Use semantic material/certainty instead of renderer styling. Material enum is unknown/wood/stone/tile/carpet/blood/water/grass/dirt/snow/metal/rune/warm-light/cold-light/shadow. Certainty enum is confirmed/inferred/unknown; omit confirmed fields.",
          'Use cat:"terrain" for ground/floor, cat:"light" for light/glow/shadow areas, and material for appearance. Do not use floor, ground, region, subtype, opacity, zIndex, rotation, visual scale, blur, or custom fill colors in new map patches.',
          'For `cat:"actor"`, optional `actorKey` is the full-session identity key. If omitted, the element id is used. The runtime keeps only the latest actor with the same final key across all map documents.',
          "With `at`, `path` and `curve` points are relative offsets. Without `at`, the points are treated as absolute coordinates and the stored result becomes relative to the first point.",
          "If one add element contains label-eligible geometry plus text, the runtime splits the text into a system label element automatically. Terrain/light/grid geometry does not derive labels.",
          "Atlas glyphs describe places. Scene maps describe local space. Do not put house/castle/village/forest/temple/shop icons inside a scene map; draw the local walls, doors, roads, furniture, hazards, objects, labels, and actor positions instead.",
          "For `tavern.atlas/main`, use only `upsert-location`, `remove-location`, `upsert-link`, `remove-link`, and `move-actor`. There is no set-active-location op.",
          'Atlas links may omit `id`. The default link id is `link:${sorted(from,to).join(":")}:${kind}` for bidirectional links and `link:${from}:${to}:${kind}` for `bidirectional:false`. Use an explicit id only when two locations need multiple same-kind links.',
          'Move the player between places with `move-actor` and `actorKey:"player"`. That updates atlas.activeLocationKey, marks the location visited, and syncs activeMapDocId when the location has mapDocId. Non-player actors do not change the current location.',
          "Pass `activate:true` only for `tavern.map` to make that map document active for map tools. Do not use map activate to represent player movement.",
          "`meta.viewBox` is the camera. Changing it does not move elements. Move actors by changing their `at`, then adjust `viewBox` only if the camera should follow.",
          "The `ops` argument must be a real JSON array, not a quoted JSON string. With `dryRun:true`, validate without saving or incrementing revision.",
          "Legacy `init`, `reset`, and `replace` input is still absorbed at runtime, but do not rely on it in new calls."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            docType: {
              type: "string",
              enum: [ir, Hr],
              description: "Structured document type. Use `tavern.map` for scene maps or `tavern.atlas` for the world index."
            },
            docId: {
              type: "string",
              description: "Structured document id. For scene maps, prefer an explicit stable place-named docId; omit only when intentionally maintaining the currently active scene-map doc. Atlas always uses `main`."
            },
            baseRevision: {
              type: "number",
              description: "Optional optimistic revision check from MapInspect summary/document."
            },
            dryRun: {
              type: "boolean",
              description: "Validate and simulate the transaction without saving or incrementing the revision."
            },
            activate: {
              type: "boolean",
              description: "Set this map document as the current scene after the transaction. With `ops:[]`, this only switches the active map."
            },
            desc: {
              type: "string",
              description: "Short one-line summary of this turn’s spatial update."
            },
            ops: {
              type: "array",
              description: "Patch ops as one atomic transaction. For maps use `meta/add/modify/remove`. For atlas use `upsert-location/remove-location/upsert-link/remove-link/move-actor`.",
              items: {
                type: "object",
                properties: {
                  op: {
                    type: "string",
                    enum: [
                      "meta",
                      "add",
                      "modify",
                      "remove",
                      "upsert-location",
                      "remove-location",
                      "upsert-link",
                      "remove-link",
                      "move-actor"
                    ],
                    description: "Operation type. Map ops and atlas ops are selected by docType."
                  },
                  id: {
                    type: "string",
                    description: "Map element id for `modify`/`remove`, or atlas link id for `upsert-link`/`remove-link`. Atlas links may omit it and use the default id rule."
                  },
                  key: {
                    type: "string",
                    description: "Atlas location key for upsert-location/remove-location."
                  },
                  locationKey: {
                    type: "string",
                    description: "Atlas target location key for move-actor."
                  },
                  actorKey: {
                    type: "string",
                    description: 'Atlas actor key for move-actor. Use actorKey:"player" for the player.'
                  },
                  from: {
                    type: "string",
                    description: "Atlas link source location key."
                  },
                  to: {
                    type: "string",
                    description: "Atlas link target location key."
                  },
                  kind: {
                    type: "string",
                    enum: [...bf],
                    description: "Atlas link kind."
                  },
                  label: {
                    type: "string",
                    description: "Optional atlas link label."
                  },
                  bidirectional: {
                    type: "boolean",
                    description: "Atlas link direction flag. Defaults true."
                  },
                  unset: {
                    type: "array",
                    items: {
                      type: "string",
                      enum: [
                        "parent",
                        "mapDocId",
                        "aliases",
                        "brief"
                      ]
                    },
                    description: "Atlas upsert-location optional fields to remove."
                  },
                  set: t,
                  element: {
                    ...e,
                    description: "Full element object for `add`."
                  }
                },
                required: ["op"],
                additionalProperties: !1
              }
            }
          },
          required: ["ops"],
          additionalProperties: !1
        }
      }
    }
  ];
}
async function Jq(e = "", t = "", n = {}, r = {}) {
  const s = String(e || "").trim(), a = cq(t);
  if (!s) return {
    ok: !1,
    summary: "Missing sessionId.",
    error: "state_session_required"
  };
  try {
    const o = Tt(n.docType, 40), l = !!o, i = mq(o || ir), u = vq(i, Tt(n.docId, 80) || (i === ir && (a === _s.READ || a === _s.PATCH) ? (await Wm(s)).activeDocId : i === Hr ? ea : kr));
    if (a === _s.LIST) {
      const d = !l, f = d || i === ir ? await Wm(s, { includeStale: !0 }) : null, m = d ? await Lb(s, { includeStale: !0 }) : f?.documents || await Lb(s, {
        docType: i,
        includeStale: !0
      }), v = f?.activeDocId || kr, y = [...m].sort((S, x) => {
        const I = S.docType === ir && S.docId === v;
        return I !== (x.docType === ir && x.docId === v) ? I ? -1 : 1 : S.docType !== x.docType ? S.docType.localeCompare(x.docType) : (Number(x.updatedAt) || 0) - (Number(S.updatedAt) || 0) || (Number(x.revision) || 0) - (Number(S.revision) || 0);
      });
      return {
        ok: !0,
        summary: `Found ${y.length} map/atlas document(s).`,
        count: y.length,
        docId: v,
        documents: y.map((S) => ({
          docType: S.docType,
          docId: S.docId,
          title: S.title,
          revision: S.revision,
          digest: S.digest,
          status: S.status,
          updatedAt: S.updatedAt,
          active: S.docType === ir ? S.docId === v : S.docType === Hr && S.docId === ea
        }))
      };
    }
    if (a === _s.READ) {
      const d = String(n.mode || "summary").trim() || "summary";
      if (i === Hr) {
        const v = await Vb(s);
        if (!v) return {
          ok: !1,
          summary: `${i}/${u} does not exist.`,
          docType: i,
          docId: u,
          error: "state_document_not_found"
        };
        const y = Sf(v);
        if (d === "summary") return {
          ok: !0,
          summary: `Read atlas summary, revision ${v.revision}.`,
          docType: i,
          docId: u,
          title: v.title,
          revision: v.revision,
          digest: Wp(y),
          activeLocationKey: y.activeLocationKey,
          count: y.locations.length,
          details: {
            locationCount: y.locations.length,
            linkCount: y.links.length,
            actorCount: y.actors.length
          }
        };
        if (d === "document") return {
          ok: !0,
          summary: `Read full atlas, revision ${v.revision}.`,
          docType: i,
          docId: u,
          title: v.title,
          revision: v.revision,
          digest: Wp(y),
          activeLocationKey: y.activeLocationKey,
          document: y
        };
        if (d === "locations") {
          const S = Nq(y, n);
          return {
            ok: !0,
            summary: `Matched ${S.count} atlas location(s); returned ${S.locations.length}.`,
            docType: i,
            docId: u,
            revision: v.revision,
            count: S.count,
            truncated: S.truncated,
            nextOffset: S.nextOffset,
            locations: S.locations
          };
        }
        if (d === "location") {
          const S = es(n.locationKey ?? n.key);
          if (!S) return {
            ok: !1,
            summary: "Missing locationKey.",
            docType: i,
            docId: u,
            error: "atlas_location_key_required"
          };
          const x = y.locations.find((I) => I.key === S);
          return x ? {
            ok: !0,
            summary: `Read atlas location ${x.name}.`,
            docType: i,
            docId: u,
            revision: v.revision,
            location: {
              ...jt(x),
              links: y.links.filter((I) => I.from === S || I.to === S).map((I) => jt(I)),
              actors: y.actors.filter((I) => I.locationKey === S).map((I) => jt(I))
            }
          } : {
            ok: !1,
            summary: `${S} does not exist.`,
            docType: i,
            docId: u,
            revision: v.revision,
            error: "atlas_location_not_found"
          };
        }
        if (d === "links") {
          const S = $q(y, n);
          return {
            ok: !0,
            summary: `Matched ${S.count} atlas link(s); returned ${S.links.length}.`,
            docType: i,
            docId: u,
            revision: v.revision,
            count: S.count,
            truncated: S.truncated,
            nextOffset: S.nextOffset,
            links: S.links
          };
        }
        if (d === "actors") {
          const S = wf(n.actorKey), x = S ? y.actors.filter((I) => I.actorKey === S) : y.actors;
          return {
            ok: !0,
            summary: `Matched ${x.length} atlas actor position(s).`,
            docType: i,
            docId: u,
            revision: v.revision,
            count: x.length,
            actors: x.map((I) => jt(I))
          };
        }
        if (d === "history") {
          const S = await Hm({
            sessionId: s,
            docType: i,
            docId: u
          }), x = Math.max(0, Number(n.tail) || 0), I = Math.max(1, Math.min(Qs, Number(n.limit) || 20)), E = Math.max(0, Number(n.offset) || 0), N = x > 0 ? Math.max(0, S.length - Math.min(Qs, x)) : E, $ = S.slice(N, N + (x > 0 ? Math.min(Qs, x) : I)), p = N + $.length < S.length ? N + $.length : 0;
          return {
            ok: !0,
            summary: `Found ${S.length} saved atlas patch transaction(s); returned ${$.length}.`,
            docType: i,
            docId: u,
            revision: v.revision,
            count: S.length,
            truncated: p > 0,
            nextOffset: p,
            patches: $
          };
        }
        return {
          ok: !1,
          summary: `Unsupported atlas MapInspect mode: ${d}`,
          docType: i,
          docId: u,
          error: "state_read_mode_invalid"
        };
      }
      const f = await Hq(s, i, u);
      if (!f) return {
        ok: !1,
        summary: `${i}/${u} does not exist.`,
        docType: i,
        docId: u,
        error: "state_document_not_found"
      };
      const m = kl(f);
      if (d === "summary") return {
        ok: !0,
        summary: `Read summary for ${f.title || u}, revision ${f.revision}, status ${m.meta.status}.`,
        docType: i,
        docId: u,
        title: f.title,
        revision: f.revision,
        digest: f.digest,
        meta: jt(m.meta),
        elementCount: m.elements.length
      };
      if (d === "document") return {
        ok: !0,
        summary: `Read full state for ${f.title || u}, revision ${f.revision}.`,
        docType: i,
        docId: u,
        title: f.title,
        revision: f.revision,
        digest: f.digest,
        meta: jt(m.meta),
        elementCount: m.elements.length,
        document: m
      };
      if (d === "element") {
        const v = Tt(n.elementId, 120);
        if (!v) return {
          ok: !1,
          summary: "Missing elementId.",
          docType: i,
          docId: u,
          error: "state_element_id_required"
        };
        const y = m.elements.find((S) => S.id === v);
        return y ? {
          ok: !0,
          summary: `Read element ${v}.`,
          docType: i,
          docId: u,
          revision: f.revision,
          element: L3(y)
        } : {
          ok: !1,
          summary: `${v} does not exist.`,
          docType: i,
          docId: u,
          revision: f.revision,
          error: "state_element_not_found"
        };
      }
      if (d === "elements") {
        const v = Rq(m, n);
        return {
          ok: !0,
          summary: `Matched ${v.count} map element(s); returned ${v.elements.length}.`,
          docType: i,
          docId: u,
          revision: f.revision,
          count: v.count,
          truncated: v.truncated,
          nextOffset: v.nextOffset,
          elements: v.elements
        };
      }
      if (d === "history") {
        const v = await Hm({
          sessionId: s,
          docType: i,
          docId: u
        }), y = Math.max(0, Number(n.tail) || 0), S = Math.max(1, Math.min(Qs, Number(n.limit) || 20)), x = Math.max(0, Number(n.offset) || 0), I = y > 0 ? Math.max(0, v.length - Math.min(Qs, y)) : x, E = v.slice(I, I + (y > 0 ? Math.min(Qs, y) : S)), N = I + E.length < v.length ? I + E.length : 0;
        return {
          ok: !0,
          summary: `Found ${v.length} saved patch transaction(s); returned ${E.length}.`,
          docType: i,
          docId: u,
          revision: f.revision,
          count: v.length,
          truncated: N > 0,
          nextOffset: N,
          patches: E
        };
      }
      return {
        ok: !1,
        summary: `Unsupported MapInspect mode: ${d}`,
        docType: i,
        docId: u,
        error: "state_read_mode_invalid"
      };
    }
    if (a === _s.PATCH) {
      if (!Array.isArray(n.ops)) return {
        ok: !1,
        summary: "MapPatch ops must be a real array.",
        docType: i,
        docId: u,
        error: "state_patch_ops_must_be_array"
      };
      const d = n.ops, f = n.activate === !0;
      return !d.length && !f ? {
        ok: !1,
        summary: "MapPatch ops are required unless activate:true is used to switch active map.",
        docType: i,
        docId: u,
        error: "state_patch_ops_required"
      } : i === Hr && f ? {
        ok: !1,
        summary: 'Atlas has no activate:true operation. Move the player with move-actor(actorKey:"player").',
        docType: i,
        docId: u,
        error: "atlas_activate_not_supported"
      } : i === Hr ? await Dt.transaction("rw", Cr, yl, Qt, Ct, async () => {
        await r.beforeWriteGuard?.();
        const m = await Vb(s), v = Number(m?.revision) || 0;
        if (Number.isFinite(Number(n.baseRevision)) && Number(n.baseRevision) !== v) return {
          ok: !1,
          summary: `Revision changed: current is ${v}, but this call was based on ${Number(n.baseRevision)}. Run MapInspect again before patching.`,
          docType: i,
          docId: u,
          revision: v,
          error: "state_revision_conflict"
        };
        const y = Dq(Sf(m), d);
        if (y.failed.length) {
          const N = CT(y.failed);
          return {
            ok: !1,
            summary: `Atlas patch was not saved: ${y.failed.length} op(s) failed.${N ? ` ${N}` : ""}`,
            docType: i,
            docId: u,
            revision: v,
            changed: !1,
            appliedCount: 0,
            satisfiedCount: y.satisfiedCount,
            failedCount: y.failed.length,
            warnings: y.warnings,
            error: "state_patch_failed",
            details: y.failed
          };
        }
        if (!y.changed) return {
          ok: !0,
          summary: "Atlas is already at the target result. No write was needed.",
          docType: i,
          docId: u,
          revision: v,
          changed: !1,
          appliedCount: 0,
          satisfiedCount: y.satisfiedCount,
          failedCount: 0,
          warnings: y.warnings
        };
        const S = v + 1, x = Wp(y.document);
        if (n.dryRun === !0) return {
          ok: !0,
          summary: `Dry run passed: ${i}/${u} would advance to revision ${S} with ${y.appliedCount} applied op(s). Nothing was saved.`,
          docType: i,
          docId: u,
          title: Gb(y.document),
          revision: v,
          digest: x,
          changed: !0,
          appliedCount: y.appliedCount,
          satisfiedCount: y.satisfiedCount,
          failedCount: 0,
          changedIds: y.changedIds,
          warnings: y.warnings,
          activeLocationKey: y.document.activeLocationKey,
          document: y.document
        };
        const I = Bb(), E = await ST({
          sessionId: s,
          docType: i,
          docId: u,
          title: Gb(y.document),
          revision: S,
          data: y.document,
          digest: x,
          status: "active",
          source: r.caller || "auto",
          createdAt: Number(m?.createdAt) || I,
          updatedAt: I
        });
        return await Db({
          sessionId: s,
          docType: i,
          docId: u,
          revision: S,
          status: "active",
          managerRunId: r.managerRunId,
          sourceUserOrder: r.sourceUserOrder,
          sourceAssistantOrder: r.sourceAssistantOrder,
          source: r.caller || "auto",
          summary: Tt(n.desc || `AtlasPatch ${S}`, 400),
          ops: y.effectiveOps,
          changedIds: y.changedIds,
          removedElements: []
        }), y.syncedActiveMapDocId && await Vh(s, y.syncedActiveMapDocId), {
          ok: !0,
          summary: `Updated ${i}/${u} to revision ${E.revision} with ${y.appliedCount} applied op(s).`,
          docType: i,
          docId: u,
          title: E.title,
          revision: E.revision,
          digest: E.digest,
          changed: !0,
          appliedCount: y.appliedCount,
          satisfiedCount: y.satisfiedCount,
          failedCount: 0,
          changedIds: y.changedIds,
          warnings: y.warnings,
          activeLocationKey: y.document.activeLocationKey,
          document: y.document
        };
      }) : await Dt.transaction("rw", Cr, yl, Qt, Ct, async () => {
        await r.beforeWriteGuard?.();
        const m = await bi(s, i, u), v = Number(m?.revision) || 0;
        if (Number.isFinite(Number(n.baseRevision)) && Number(n.baseRevision) !== v) return {
          ok: !1,
          summary: `Revision changed: current is ${v}, but this call was based on ${Number(n.baseRevision)}. Run MapInspect again before patching.`,
          docType: i,
          docId: u,
          revision: v,
          error: "state_revision_conflict"
        };
        if (!d.length && f) {
          if (!m) return {
            ok: !1,
            summary: `${i}/${u} does not exist.`,
            docType: i,
            docId: u,
            error: "state_document_not_found"
          };
          const C = await Ub(s), k = await Vh(s, u), P = await e_(s, u);
          return {
            ok: !0,
            summary: `Activated ${i}/${u}.`,
            docType: i,
            docId: u,
            title: m.title,
            revision: v,
            digest: m.digest,
            changed: C !== k,
            appliedCount: 0,
            satisfiedCount: 0,
            failedCount: 0,
            warnings: P
          };
        }
        const y = Kq(m ? kl(m) : N3(), d);
        if (y.failed.length) {
          const C = CT(y.failed);
          return {
            ok: !1,
            summary: `MapPatch was not saved: ${y.failed.length} op(s) failed.${C ? ` ${C}` : ""}`,
            docType: i,
            docId: u,
            revision: v,
            changed: !1,
            appliedCount: 0,
            satisfiedCount: y.satisfiedCount,
            failedCount: y.failed.length,
            warnings: y.warnings,
            error: "state_patch_failed",
            details: y.failed
          };
        }
        if (!y.changed) {
          let C = !1;
          if (f && n.dryRun !== !0) {
            if (!m) return {
              ok: !1,
              summary: `${i}/${u} does not exist.`,
              docType: i,
              docId: u,
              error: "state_document_not_found"
            };
            C = await Ub(s) !== await Vh(s, u);
          }
          const k = f && n.dryRun !== !0 ? await e_(s, u) : [];
          return {
            ok: !0,
            summary: C ? `Activated ${i}/${u}. State was already at the target result.` : "State is already at the target result. No write was needed.",
            docType: i,
            docId: u,
            revision: v,
            changed: C,
            appliedCount: 0,
            satisfiedCount: y.satisfiedCount,
            failedCount: 0,
            warnings: [...y.warnings, ...k]
          };
        }
        const S = v + 1, x = Bb();
        if (n.dryRun === !0) {
          const C = Ml(y.document, S);
          return {
            ok: !0,
            summary: `Dry run passed: ${i}/${u} would advance to revision ${S} with ${y.appliedCount} applied op(s). Nothing was saved.`,
            docType: i,
            docId: u,
            title: Yu(y.document),
            revision: v,
            digest: C,
            changed: !0,
            appliedCount: y.appliedCount,
            satisfiedCount: y.satisfiedCount,
            failedCount: 0,
            changedIds: y.changedIds,
            removedElements: y.removedElements,
            warnings: y.warnings,
            meta: jt(y.document.meta),
            elementCount: y.document.elements.length
          };
        }
        const I = await Mq({
          sessionId: s,
          docType: i,
          docId: u,
          document: y.document,
          timestamp: x,
          managerRunId: r.managerRunId,
          sourceUserOrder: r.sourceUserOrder,
          sourceAssistantOrder: r.sourceAssistantOrder
        }), E = I.document, N = [...y.effectiveOps, ...I.removedElements.map((C) => ({
          op: "remove",
          id: C.id
        }))], $ = [...y.removedElements, ...I.removedElements], p = [.../* @__PURE__ */ new Set([...y.changedIds, ...I.removedElements.map((C) => C.id)])], w = Ml(E, S), _ = await ST({
          sessionId: s,
          docType: i,
          docId: u,
          title: Yu(E),
          revision: S,
          data: E,
          digest: w,
          status: "active",
          source: r.caller || "auto",
          createdAt: Number(m?.createdAt) || x,
          updatedAt: x
        });
        await Db({
          sessionId: s,
          docType: i,
          docId: u,
          revision: S,
          status: "active",
          managerRunId: r.managerRunId,
          sourceUserOrder: r.sourceUserOrder,
          sourceAssistantOrder: r.sourceAssistantOrder,
          source: r.caller || "auto",
          summary: Tt(n.desc || `MapPatch ${S}`, 400),
          ops: N,
          changedIds: p,
          removedElements: $
        }), f && await Vh(s, u);
        const b = f ? await e_(s, u) : [];
        return {
          ok: !0,
          summary: `Updated ${i}/${u} to revision ${_.revision} with ${y.appliedCount} applied op(s).`,
          docType: i,
          docId: u,
          title: _.title,
          revision: _.revision,
          digest: _.digest,
          changed: !0,
          appliedCount: y.appliedCount,
          satisfiedCount: y.satisfiedCount,
          failedCount: 0,
          changedIds: p,
          removedElements: $,
          warnings: [...y.warnings, ...b],
          meta: jt(E.meta),
          elementCount: E.elements.length
        };
      });
    }
    return {
      ok: !1,
      summary: `${t} is not available.`,
      error: "state_tool_not_available"
    };
  } catch (o) {
    const l = o instanceof Error ? o.name : "", i = o instanceof Error ? o.message : String(o || "state_tool_failed");
    if (l === "AbortError" || i === "manager_source_messages_changed" || i === "manager_epoch_expired" || i === "manager_aborted") throw o;
    return {
      ok: !1,
      summary: kw(i) || i,
      error: i
    };
  }
}
async function Yq(e = "") {
  const t = await Wm(e), n = t.activeDocId, r = t.documents, s = (r.some((i) => !Kd(i)) ? r.filter((i) => !Kd(i)) : r).map((i) => {
    const u = kl(i);
    return {
      ...i,
      data: u,
      docType: i.docType,
      docId: i.docId,
      title: Yu(u),
      revision: i.revision,
      digest: Ml(u, i.revision),
      status: i.status,
      updatedAt: i.updatedAt,
      active: i.docId === n
    };
  }).sort((i, u) => !!i.active != !!u.active ? i.active ? -1 : 1 : (Number(u.updatedAt) || 0) - (Number(i.updatedAt) || 0) || (Number(u.revision) || 0) - (Number(i.revision) || 0)), a = t.record, o = a ? kl(a) : null, l = a ? await Hm({
    sessionId: e,
    docType: ir,
    docId: a.docId,
    limit: 80
  }) : [];
  return !a || !o ? {
    documents: s,
    activeDocId: n,
    activeDocument: null,
    activePatches: []
  } : {
    documents: s,
    activeDocId: n,
    activeDocument: {
      ...a,
      data: o,
      title: Yu(o),
      digest: Ml(o, a.revision)
    },
    activePatches: l
  };
}
async function Xq(e = "") {
  const t = await Vb(e);
  if (!t) return {
    document: null,
    patches: [],
    activeLocationKey: ""
  };
  const n = Sf(t), r = await Hm({
    sessionId: e,
    docType: Hr,
    docId: ea,
    limit: 80
  });
  return {
    document: {
      ...t,
      data: n,
      title: Gb(n),
      digest: Wp(n)
    },
    patches: r,
    activeLocationKey: n.activeLocationKey || ""
  };
}
function xT(e) {
  return e ? e.brief ? `${e.name}（${e.brief}）` : e.name : "";
}
function jq(e = "") {
  return String(e || "").split(`
`).map((t) => t.trim()).find((t) => t.startsWith("标注："))?.replace(/^标注：/, "").trim() || "";
}
async function Qq(e = "") {
  await Uf(e, { touchSession: !1 });
  const t = Sf(await bi(e, Hr, ea)), n = new Map(t.locations.map((f) => [f.key, f])), r = t.activeLocationKey ? n.get(t.activeLocationKey) : void 0;
  if (!r) return "";
  const s = r.parent ? n.get(r.parent) : void 0, a = t.links.flatMap((f) => f.from === r.key ? [n.get(f.to)] : f.bidirectional && f.to === r.key ? [n.get(f.from)] : []).filter((f) => !!f), o = t.locations.filter((f) => f.status === "visited").map((f) => f.name).filter(Boolean), l = t.locations.filter((f) => f.status === "mentioned").map((f) => f.name).filter(Boolean), i = t.actors.map((f) => {
    const m = n.get(f.locationKey);
    return m ? `${f.actorKey === "player" ? "玩家" : f.actorKey}=${m.name}` : "";
  }).filter(Boolean), u = r.mapDocId ? await bi(e, ir, r.mapDocId) : null, d = jq(u ? Ml(kl(u), u.revision) : "");
  return [
    `当前地点：${xT(r)}`,
    s ? `上级地点：${s.name}` : "",
    a.length ? `相邻地点：${a.map((f) => xT(f)).join("、")}` : "",
    o.length ? `已探索地点：${o.join("、")}` : "",
    l.length ? `已知但未到达：${l.join("、")}` : "",
    i.length ? `人物位置：${i.join("，")}` : "",
    d ? `当前场景标注：${d}` : r.mapDocId ? "当前场景标注：暂无" : "当前地点暂无详细地图"
  ].filter(Boolean).join(`
`);
}
async function Zq(e = "") {
  const { record: t } = await Wm(e);
  if (!t) return [];
  const n = kl(t);
  return n.meta.status !== "active" || JM(n) ? [] : [{
    docType: t.docType,
    docId: t.docId,
    title: Yu(n),
    revision: t.revision,
    digest: Ml(n, t.revision)
  }];
}
var Pl = {
  INSPECT: "EventInspect",
  PATCH: "EventPatch"
}, eH = { PATCH: "TaskPatch" };
function tH(e = "") {
  const t = String(e || "").trim();
  return t === eH.PATCH ? Pl.PATCH : t;
}
var nH = /(?:任务|目标|子目标|远景|已完成|\bquest\b|\btask\b|\bgoal\b|\bobjective\b|\bcomplete(?:d|s|ion)?\b)|(?:完成|完成剧情)(?=任务|目标|子目标|远景)|完成任务|任务完成|目标完成|子目标完成|任务已完成|目标已完成|子目标已完成/i;
function xr() {
  return Date.now();
}
function rH(e = "task") {
  return `${e}-${xr()}-${Math.random().toString(36).slice(2, 8)}`;
}
function rv(e) {
  return JSON.parse(JSON.stringify(e));
}
function vs(e = "", t = 1200) {
  const n = String(e || "").replace(/\r\n/g, `
`).trim();
  return n.length > t ? n.slice(0, t) : n;
}
function Wd(e = "") {
  return vs(e, 40).replace(/[\r\n]+/g, " ").replace(/\s+/g, " ").trim();
}
function D3(e = "") {
  const t = String(e || "").trim();
  return /^[\w:.-]{1,120}$/i.test(t) ? t : "";
}
function sH(e = "") {
  const t = String(e || "").trim();
  return t === "upsert-task" ? "upsert-event" : t === "advance-task" ? "advance-event" : t === "complete-task" ? "complete-event" : t === "abandon-task" ? "abandon-event" : t;
}
function Jb(e = {}) {
  return D3(e.eventId || e.taskId || e.id);
}
function sv(e = "") {
  return vs(e, 240);
}
function Pa(e) {
  return Wd(e.title || e.current).slice(0, 8) || "未命名方向";
}
function kT(e = "") {
  const t = Array.from(e).length;
  return t < 2 ? "task_title_too_short" : t > 12 ? "task_title_too_long" : "";
}
function aH(e = "", t = "", n = "") {
  return Rl([
    Wd(e),
    vs(t, 500),
    vs(n, 500)
  ].join(`
`));
}
function Yb(e = "") {
  return nH.test(String(e || ""));
}
function bu(e, t = -1) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.floor(n) : t;
}
function Xu(e = [], t = []) {
  return JSON.stringify({
    tasks: [...e].sort((n, r) => n.id.localeCompare(r.id)).map((n) => ({
      id: n.id,
      sessionId: n.sessionId,
      status: n.status,
      title: Pa(n),
      horizon: n.horizon,
      current: n.current,
      doneWhen: n.doneWhen,
      hookForModel: n.hookForModel,
      fingerprint: n.fingerprint,
      createdOrder: n.createdOrder,
      updatedOrder: n.updatedOrder,
      lastAdvancedOrder: n.lastAdvancedOrder,
      completedOrder: n.completedOrder ?? null,
      abandonedOrder: n.abandonedOrder ?? null,
      sourceManagerRunId: n.sourceManagerRunId || ""
    })),
    abandonedFingerprints: [...new Set(t)].sort()
  });
}
function Rl(e = "") {
  const t = String(e || "");
  let n = 2166136261, r = 522970236;
  for (let s = 0; s < t.length; s += 1) {
    const a = t.charCodeAt(s);
    n ^= a, n = Math.imul(n, 16777619) >>> 0, r ^= a + s, r = Math.imul(r, 16777619) >>> 0;
  }
  return `${t.length.toString(36)}:${n.toString(16)}${r.toString(16)}`;
}
function oH(e = "", t = []) {
  const n = String(e || "").trim();
  if (!t.length) return n;
  const r = "rollback_conflict:", s = n.startsWith(r) ? n.slice(18).split(",").map((a) => a.trim()).filter(Boolean) : [];
  return `${r}${[.../* @__PURE__ */ new Set([...s, ...t])].join(",")}`;
}
async function O3(e = "") {
  const [t, n] = await Promise.all([qs(e, {
    includeAbandoned: !0,
    includeCompleted: !0
  }), cc(e)]);
  return Rl(Xu(t, n));
}
async function F3(e = "", t) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.floor(n) : await fw(e) ?? -1;
}
function iH(e) {
  return Rl(Xu(e?.tasks || [], e?.abandonedFingerprints || []));
}
async function qs(e = "", t = {}) {
  const n = String(e || "").trim();
  return n ? (await $n.where("sessionId").equals(n).sortBy("updatedAt")).filter((r) => t.includeCompleted || r.status !== "completed").filter((r) => t.includeAbandoned || r.status !== "abandoned").sort((r, s) => Number(s.updatedOrder) - Number(r.updatedOrder) || Number(s.updatedAt) - Number(r.updatedAt)) : [];
}
async function lH(e = "", t = "") {
  const n = String(e || "").trim(), r = D3(t);
  return !n || !r ? null : await $n.get([n, r]) || null;
}
async function cc(e = "") {
  const t = String(e || "").trim();
  if (!t) return [];
  const n = await Fa.get(t);
  return Array.isArray(n?.abandonedFingerprints) ? n.abandonedFingerprints.map((r) => sv(r)).filter(Boolean) : [];
}
async function Xm(e = "", t = []) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = [...new Set(t.map((s) => sv(s)).filter(Boolean))].slice(-200);
  return await Fa.put({
    sessionId: n,
    abandonedFingerprints: r,
    updatedAt: xr()
  }), r;
}
async function B3(e = "", t = "") {
  const n = sv(t);
  n && await Xm(e, [...(await cc(e)).filter((r) => r !== n), n]);
}
async function uH(e = "", t) {
  const n = String(e || "").trim();
  return n ? await Dt.transaction("rw", $n, Oa, Fa, async () => {
    const [r, s] = await Promise.all([qs(n, {
      includeAbandoned: !0,
      includeCompleted: !0
    }), cc(n)]), a = await F3(n, t);
    if (a === -1 && !r.length && !s.length) return null;
    const o = await Mw(n), l = Rl(Xu(r, s));
    if (o && iH(o) === l) return null;
    const i = {
      sessionId: n,
      floor: a,
      tasks: r.map((u) => rv(u)),
      abandonedFingerprints: [...s],
      createdAt: xr()
    };
    return await Oa.put(i), i;
  }) : null;
}
async function cH(e = "", t) {
  return F3(e, t);
}
async function U3(e = "") {
  const t = String(e || "").trim();
  return t ? (await Oa.where("sessionId").equals(t).sortBy("floor")).sort((n, r) => n.floor - r.floor || n.createdAt - r.createdAt) : [];
}
async function Mw(e = "", t = Number.POSITIVE_INFINITY) {
  const n = String(e || "").trim(), r = Number(t);
  return n && (await U3(n)).filter((s) => Number(s.floor) <= r || r === Number.POSITIVE_INFINITY).sort((s, a) => Number(a.floor) - Number(s.floor) || Number(a.createdAt) - Number(s.createdAt))[0] || null;
}
async function q3(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  return !n || !Number.isFinite(r) ? 0 : await Dt.transaction("rw", Oa, async () => {
    const s = (await U3(n)).filter((a) => Number(a.floor) >= r);
    return s.length ? (await Oa.bulkDelete(s.map((a) => [a.sessionId, a.floor])), s.length) : 0;
  });
}
async function dH(e = "", t = -1) {
  const n = String(e || "").trim();
  if (!n) return {
    changed: !1,
    currentTaskCount: 0,
    targetTaskCount: 0
  };
  const r = await Mw(n, t), [s, a] = await Promise.all([qs(n, {
    includeAbandoned: !0,
    includeCompleted: !0
  }), cc(n)]), o = r?.tasks || [], l = r?.abandonedFingerprints || [];
  return {
    changed: Rl(Xu(s, a)) !== Rl(Xu(o, l)),
    currentTaskCount: s.length,
    targetTaskCount: o.length
  };
}
async function H3(e = "", t = -1) {
  const n = String(e || "").trim();
  if (!n) throw new Error("task_session_required");
  return await Dt.transaction("rw", $n, Oa, Fa, async () => {
    const r = await Mw(n, t), s = await $n.where("sessionId").equals(n).toArray();
    if (s.length && await $n.bulkDelete(s.map((o) => [o.sessionId, o.id])), !r)
      return await Xm(n, []), [];
    const a = (r.tasks || []).map((o) => ({
      ...rv(o),
      sessionId: n
    }));
    return a.length && await $n.bulkPut(a), await Xm(n, r.abandonedFingerprints || []), a;
  });
}
async function z3(e = "", t = "") {
  const n = String(e || "").trim(), r = String(t || "").trim();
  if (!n || !r) return null;
  const s = await Ir.get(n);
  if (s) return s;
  const [a, o] = await Promise.all([qs(r, {
    includeAbandoned: !0,
    includeCompleted: !0
  }), cc(r)]), l = {
    managerRunId: n,
    sessionId: r,
    beforeTasks: a.map((i) => rv(i)),
    beforeFingerprints: [...o],
    beforeHash: Rl(Xu(a, o)),
    afterHash: "",
    rollbackStatus: "pending",
    error: "",
    createdAt: xr(),
    updatedAt: xr()
  };
  return await Ir.put(l), l;
}
async function fH(e = "", t = "") {
  const n = await z3(e, t);
  return n ? (await Ir.update(n.managerRunId, {
    afterHash: await O3(n.sessionId),
    updatedAt: xr()
  }), await Ir.get(n.managerRunId) || null) : null;
}
async function Pw(e = "") {
  const t = String(e || "").trim();
  if (!t) return [];
  const n = await Ir.get(t);
  return n ? [n] : [];
}
async function K3(e = "") {
  const t = String(e || "").trim();
  if (!t) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const n = await Ir.get(t), r = await ur.get(t);
  return !n || !r ? {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  } : n.rollbackStatus === "rolled_back" || n.rollbackStatus === "skipped" ? {
    rolledBack: 0,
    conflicts: [],
    skipped: 1
  } : n.afterHash ? await O3(n.sessionId) !== n.afterHash ? (await Ir.update(t, {
    rollbackStatus: "conflict",
    error: "rollback_conflict_current_tasks_changed",
    updatedAt: xr()
  }), await ur.update(t, {
    status: "rolled_back",
    error: oH(r.error, ["tasks"]),
    updatedAt: xr()
  }), {
    rolledBack: 0,
    conflicts: ["tasks"],
    skipped: 0
  }) : (await Dt.transaction("rw", $n, Fa, Ir, ur, async () => {
    const s = await $n.where("sessionId").equals(n.sessionId).toArray();
    s.length && await $n.bulkDelete(s.map((a) => [a.sessionId, a.id])), n.beforeTasks.length && await $n.bulkPut(n.beforeTasks.map((a) => rv(a))), await Xm(n.sessionId, n.beforeFingerprints || []), await Ir.update(t, {
      rollbackStatus: "rolled_back",
      error: "",
      updatedAt: xr()
    }), await ur.update(t, {
      status: "rolled_back",
      updatedAt: xr()
    });
  }), {
    rolledBack: 1,
    conflicts: [],
    skipped: 0
  }) : (await Ir.update(t, {
    rollbackStatus: "skipped",
    error: "snapshot_after_hash_missing",
    updatedAt: xr()
  }), {
    rolledBack: 0,
    conflicts: [],
    skipped: 1
  });
}
async function MT(e = "", t = {}) {
  const n = Jb(t);
  return n ? lH(e, n) : null;
}
async function hH(e = "", t = "") {
  const n = sv(t);
  return n && (await qs(e, { includeCompleted: !0 })).find((r) => (r.status === "active" || r.status === "completed") && r.fingerprint === n) || null;
}
function Bc(e) {
  return {
    id: e.id,
    status: e.status,
    title: Pa(e),
    horizon: e.horizon,
    current: e.current,
    doneWhen: e.doneWhen,
    updatedOrder: e.updatedOrder
  };
}
async function md(e, t, n) {
  return await Dt.transaction("rw", $n, Fa, Ir, Qt, Ct, async () => {
    await t.beforeWriteGuard?.(), t.managerRunId && await z3(t.managerRunId, e);
    const r = await n();
    return t.managerRunId && await fH(t.managerRunId, e), r;
  });
}
async function pH(e = "", t = "", n = {}, r = {}) {
  const s = String(e || "").trim();
  if (!s) return {
    ok: !1,
    summary: "缺少 sessionId。",
    changed: !1,
    error: "task_session_required"
  };
  const a = tH(t);
  if (a === Pl.INSPECT) {
    const d = String(n.status || "").trim(), f = d === "completed" || d === "all" || d === "", m = Jb(n), v = (await qs(s, { includeCompleted: f })).filter((I) => I.status === "active" || I.status === "completed").filter((I) => !m || I.id === m).filter((I) => !d || d === "all" || I.status === d).sort((I, E) => I.status !== E.status ? I.status === "active" ? -1 : 1 : Number(E.updatedOrder) - Number(I.updatedOrder) || Number(E.updatedAt) - Number(I.updatedAt)), y = Math.max(1, Math.min(50, Math.floor(Number(n.limit) || 20))), S = Math.max(0, Math.floor(Number(n.offset) || 0)), x = v.slice(S, S + y);
    return {
      ok: !0,
      summary: `找到 ${v.length} 条事件线索，返回 ${x.length} 条。`,
      changed: !1,
      events: x.map(Bc),
      warnings: v.length > x.length ? ["event_inspect_truncated"] : []
    };
  }
  if (a !== Pl.PATCH) return {
    ok: !1,
    summary: `未知事件工具：${t}`,
    changed: !1,
    error: "task_tool_unknown"
  };
  const o = sH(n.op), l = bu(r.sourceAssistantOrder, bu(n.order, -1));
  if (![
    "upsert-event",
    "advance-event",
    "complete-event",
    "abandon-event"
  ].includes(o)) return {
    ok: !1,
    summary: "EventPatch op 只能是 upsert-event、advance-event、complete-event、abandon-event。",
    changed: !1,
    error: "task_op_invalid"
  };
  if (o === "upsert-event") {
    const d = await MT(s, n), f = vs(n.horizon ?? d?.horizon, 500), m = vs(n.current ?? d?.current, 500), v = vs(n.doneWhen ?? d?.doneWhen, 500), y = n.title !== void 0 ? Wd(n.title) : d ? Pa(d) : r.caller ? "" : Wd(m).slice(0, 8), S = vs(n.hookForModel ?? d?.hookForModel, 500);
    if (!y || !f || !m || !v || !S) return {
      ok: !1,
      summary: "upsert-event 需要 title、horizon、current、doneWhen、hookForModel。",
      changed: !1,
      error: "task_fields_required"
    };
    const x = kT(y);
    if (x) return {
      ok: !1,
      summary: x === "task_title_too_short" ? "title 需要至少 2 个字。" : "title 不能超过 12 个字。",
      changed: !1,
      error: x
    };
    if (Yb(S)) return {
      ok: !1,
      summary: "hookForModel 必须是无元叙事词的软句。",
      changed: !1,
      error: "task_hook_meta_words"
    };
    const I = aH(y, f, m);
    if ((await cc(s)).includes(I)) return {
      ok: !1,
      summary: "这个方向已经被放弃过，本轮不再重建。",
      changed: !1,
      error: "task_fingerprint_abandoned"
    };
    const E = d || await hH(s, I);
    if (E?.status === "abandoned") return {
      ok: !1,
      summary: "这个方向已经被放弃过，本轮不再重建。",
      changed: !1,
      error: "task_fingerprint_abandoned"
    };
    if (E?.status === "completed") return {
      ok: !1,
      summary: "这个方向已经完成过，本轮不再重建。",
      changed: !1,
      error: "task_fingerprint_completed"
    };
    const N = Jb(n) || E?.id || rH("quest-event"), $ = !E;
    if (r.caller === "auto" && $ && l < 5) return {
      ok: !1,
      summary: "第 5 楼前不创建事件线索。",
      changed: !1,
      error: "task_floor_too_early"
    };
    if (r.caller === "auto" && $) {
      const w = (await qs(s)).filter((_) => _.status === "active").length;
      if (w > 1) return {
        ok: !1,
        summary: `自动新建仅允许 active 池有 1 条或以下时创建新线索，当前已有 ${w} 条。`,
        changed: !1,
        error: "task_auto_create_pool_busy"
      };
    }
    if ($ && (await qs(s)).filter((w) => w.status === "active").length >= 3)
      return {
        ok: !1,
        summary: "事件线索池已满（最多 3 条 active）。",
        changed: !1,
        error: "task_active_pool_full"
      };
    const p = await md(s, r, async () => {
      const w = xr(), _ = {
        id: N,
        sessionId: s,
        status: "active",
        title: y,
        horizon: f,
        current: m,
        doneWhen: v,
        hookForModel: S,
        fingerprint: E?.fingerprint || I,
        createdOrder: $ ? l : bu(E.createdOrder, l),
        updatedOrder: l,
        lastAdvancedOrder: $ ? l : bu(E.lastAdvancedOrder, l),
        sourceManagerRunId: String(r.managerRunId || E?.sourceManagerRunId || ""),
        createdAt: $ ? w : Number(E.createdAt) || w,
        updatedAt: w
      };
      return await $n.put(_), _;
    });
    return {
      ok: !0,
      summary: `${$ ? "已创建" : "已更新"}事件方向：${Pa(p)}`,
      changed: !0,
      eventId: p.id,
      op: o,
      events: [Bc(p)]
    };
  }
  const i = await MT(s, n);
  if (!i) return {
    ok: !1,
    summary: "找不到要更新的事件线索。",
    changed: !1,
    error: "task_not_found"
  };
  if (o === "advance-event") {
    const d = vs(n.current ?? i.current, 500), f = vs(n.horizon ?? i.horizon, 500), m = vs(n.doneWhen ?? i.doneWhen, 500), v = n.title !== void 0 ? Wd(n.title) : Pa(i), y = vs(n.hookForModel ?? i.hookForModel, 500);
    if (!v || !f || !d || !m || !y) return {
      ok: !1,
      summary: "advance-event 需要保留 title、horizon、current、doneWhen、hookForModel。",
      changed: !1,
      error: "task_fields_required"
    };
    const S = kT(v);
    if (S) return {
      ok: !1,
      summary: S === "task_title_too_short" ? "title 需要至少 2 个字。" : "title 不能超过 12 个字。",
      changed: !1,
      error: S
    };
    if (Yb(y)) return {
      ok: !1,
      summary: "hookForModel 必须是无元叙事词的软句。",
      changed: !1,
      error: "task_hook_meta_words"
    };
    const x = await md(s, r, async () => {
      const I = {
        ...i,
        status: "active",
        title: v,
        horizon: f,
        current: d,
        doneWhen: m,
        hookForModel: y,
        fingerprint: i.fingerprint,
        updatedOrder: l,
        lastAdvancedOrder: l,
        completedOrder: void 0,
        abandonedOrder: void 0,
        updatedAt: xr()
      };
      return await $n.put(I), I;
    });
    return {
      ok: !0,
      summary: `已推进事件方向：${Pa(x)}`,
      changed: !0,
      eventId: x.id,
      op: o,
      events: [Bc(x)]
    };
  }
  if (o === "complete-event") {
    const d = await md(s, r, async () => {
      const f = {
        ...i,
        status: "completed",
        updatedOrder: l,
        completedOrder: l,
        updatedAt: xr()
      };
      return await $n.put(f), f;
    });
    return {
      ok: !0,
      summary: `已完成事件方向：${Pa(d)}`,
      changed: !0,
      eventId: d.id,
      op: o,
      events: [Bc(d)]
    };
  }
  const u = await md(s, r, async () => {
    const d = {
      ...i,
      status: "abandoned",
      updatedOrder: l,
      abandonedOrder: l,
      updatedAt: xr()
    };
    return await $n.put(d), await B3(s, d.fingerprint), d;
  });
  return {
    ok: !0,
    summary: `已放弃事件方向：${Pa(u)}`,
    changed: !0,
    eventId: u.id,
    op: o,
    events: [Bc(u)]
  };
}
async function mH(e = "", t = -1, n = {}) {
  const r = String(e || "").trim(), s = bu(t, -1);
  if (!r || s < 0) return [];
  const a = Math.max(1, Math.floor(Number(n.threshold) || 8)), o = (await qs(r)).filter((l) => l.status === "active" && s - bu(l.lastAdvancedOrder, l.createdOrder) > a);
  return o.length ? await md(r, {
    managerRunId: n.managerRunId,
    sourceAssistantOrder: s,
    beforeWriteGuard: n.beforeWriteGuard
  }, async () => {
    const l = [];
    for (const i of o) {
      const u = {
        ...i,
        status: "abandoned",
        updatedOrder: s,
        abandonedOrder: s,
        updatedAt: xr()
      };
      await $n.put(u), await B3(r, u.fingerprint), l.push(u);
    }
    return l;
  }) : [];
}
async function W3(e = "", t = 1) {
  const n = String(e || "").trim();
  return n ? (await qs(n)).filter((r) => r.status === "active").sort((r, s) => Number(s.updatedOrder) - Number(r.updatedOrder) || Number(s.updatedAt) - Number(r.updatedAt)).map((r) => vs(r.hookForModel, 500)).filter((r) => !Yb(r)).filter(Boolean).slice(0, Math.max(1, Math.min(3, Math.floor(Number(t) || 1)))) : [];
}
async function gH(e = "") {
  const t = String(e || "").trim();
  if (!t) return `[Current Event Pool]
No session.`;
  const n = await qs(t, { includeCompleted: !0 }), r = n.filter((a) => a.status === "active"), s = n.filter((a) => a.status === "completed").slice(0, 5);
  return [
    "[Current Event Pool]",
    `Active count: ${r.length}/3`,
    r.length ? "Active directions:" : "Active directions: none.",
    ...r.map((a) => [
      `- id: ${a.id}`,
      `  title: ${Pa(a)}`,
      `  current: ${a.current}`,
      `  horizon: ${a.horizon}`,
      `  done when: ${a.doneWhen}`,
      `  last advanced floor: ${a.lastAdvancedOrder}`
    ].join(`
`)),
    s.length ? "Recently completed:" : "",
    ...s.map((a) => `- id: ${a.id}; title: ${Pa(a)}; current: ${a.current}; done when: ${a.doneWhen}; completed floor: ${a.completedOrder ?? a.updatedOrder}`),
    "Abandoned directions are hidden."
  ].filter(Boolean).join(`
`);
}
function vH() {
  return [{
    type: "function",
    function: {
      name: Pl.INSPECT,
      description: [
        "Inspect the current RP session event direction pool.",
        "Returns active and recently completed event directions only. Abandoned directions are not shown.",
        "Use before EventPatch when you need to advance, complete, or decide whether the active pool is low.",
        "This is not memory and not a map."
      ].join(`
`),
      parameters: {
        type: "object",
        properties: {
          status: {
            type: "string",
            enum: [
              "active",
              "completed",
              "all"
            ],
            description: "Optional status filter. Default returns active plus recently completed."
          },
          eventId: {
            type: "string",
            description: "Optional event direction id filter."
          },
          id: {
            type: "string",
            description: "Alias for eventId."
          },
          offset: {
            type: "number",
            description: "Pagination offset. Default 0."
          },
          limit: {
            type: "number",
            description: "Maximum directions to return. Default 20, max 50."
          }
        },
        additionalProperties: !1
      }
    }
  }, {
    type: "function",
    function: {
      name: Pl.PATCH,
      description: [
        "Maintain the current RP session event direction pool.",
        "Allowed ops: upsert-event, advance-event, complete-event, abandon-event.",
        "Auto managers may create new directions only at floor 5 or later, and only when active count is 1 or lower. Active pool max is 3. Earlier auto upserts fail with task_floor_too_early.",
        "New upsert-event requires title, horizon, current, doneWhen, and hookForModel. The tool generates its duplicate guard internally.",
        "title: short single-line UI title. Aim for 2-8 characters; hard limit 12.",
        "horizon: larger not-yet-happened pull.",
        "current: immediate playable entrance.",
        "doneWhen: concrete observable story event that marks this direction as reached, not an abstract state.",
        "hookForModel: one soft in-world sentence for RP injection; no meta planning words such as quest, task, goal, objective, completed, 任务, 目标, 完成.",
        "Examples:",
        '{"op":"upsert-event","title":"城东母亲","horizon":"莉娜的家庭压力把你们牵进城东旧屋。","current":"莉娜提到母亲一个人住在城东，最近需要人修房子。","doneWhen":"角色亲自到达城东旧屋并见到莉娜的母亲。","hookForModel":"莉娜提过她母亲一个人住在城东，最近似乎想找人帮忙修房子。"}',
        '{"op":"advance-event","eventId":"quest-event-1","current":"城东旧屋的门锁被人从外面换过。","doneWhen":"角色查清是谁更换了旧屋门锁。","hookForModel":"莉娜母亲家的门锁看起来像是最近才被人换过。"}',
        '{"op":"complete-event","eventId":"quest-event-1"}'
      ].join(`
`),
      parameters: {
        type: "object",
        properties: {
          op: {
            type: "string",
            enum: [
              "upsert-event",
              "advance-event",
              "complete-event",
              "abandon-event"
            ]
          },
          eventId: {
            type: "string",
            description: "Existing event direction id. Required for advance/complete/abandon. Optional for upsert."
          },
          id: {
            type: "string",
            description: "Alias for eventId."
          },
          title: {
            type: "string",
            description: "Short single-line event direction title. Aim for 2-8 characters; hard limit 12. Required for new upsert-event."
          },
          horizon: {
            type: "string",
            description: "User-facing larger not-yet-happened direction."
          },
          current: {
            type: "string",
            description: "User-facing immediate playable entrance."
          },
          doneWhen: {
            type: "string",
            description: "Objective completion condition, written as a concrete observable event in the story, not an abstract state."
          },
          hookForModel: {
            type: "string",
            description: "Soft in-world prompt sentence for RP injection; no meta planning language."
          }
        },
        required: ["op"],
        additionalProperties: !1
      }
    }
  }];
}
var Zo = {
  LIST: "MemoryList",
  READ: "MemoryRead",
  WRITE: "MemoryWrite",
  EDIT: "MemoryEdit",
  GREP: "MemoryGrep",
  CHAT_HISTORY: "ChatHistory"
}, vr = {
  LS: "LS",
  GREP: "Grep",
  READ: "Read",
  WRITE: "Write",
  EDIT: "Edit"
}, Rw = 1200, wl = 2e3, yH = 100, Xb = 100;
function wi() {
  return Date.now();
}
function PT(e = "") {
  if (e === "memory/state.md") return "会话记忆";
  const t = Nl(e);
  return t || String(e || "").replace(/^memory\//, "").replace(/\.md$/i, "") || "记忆档案";
}
function _o(e = "", t = 400) {
  const n = String(e || "").replace(/\s+/g, " ").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function Ts(e = "", t) {
  const n = String(e || "").replace(/\r\n/g, `
`).trim();
  return typeof t == "number" && t > 0 && n.length > t ? n.slice(0, t) : n;
}
function jm(e = "") {
  return String(e ?? "").replace(/\r\n?/g, `
`).split(`
`);
}
function bo(e = [], t = 1) {
  return e.map((n, r) => `${t + r}: ${n}`).join(`
`);
}
function av(e, t = 1) {
  const n = Math.floor(Number(e));
  return Number.isFinite(n) && n > 0 ? n : t;
}
function Qm(e, t = 0) {
  const n = Math.floor(Number(e));
  return Number.isFinite(n) && n >= 0 ? n : t;
}
function Zm(e, t, n) {
  const r = Math.floor(Number(e));
  return !Number.isFinite(r) || r <= 0 ? t : Math.min(n, r);
}
function _H(e = "") {
  const t = String(e || "content").trim().replace(/[\s-]/g, "_").replace(/[A-Z]/g, (n) => `_${n.toLowerCase()}`).replace(/_+/g, "_").replace(/^_+|_+$/g, "");
  return t === "files_with_matches" || t === "fileswithmatches" ? "files_with_matches" : t === "count" ? "count" : "content";
}
function sl(e = "") {
  return String(e || "").replace(/\\/g, "/").trim() === "memory/state.md";
}
function G3(e = "") {
  const t = String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").trim();
  if (!t) throw new Error("memory_character_name_required");
  if (t === "." || t === "..") throw new Error("memory_character_name_invalid");
  if (/[\x00-\x1F\x7F/\\:*?"<>|]/u.test(t)) throw new Error("memory_character_name_invalid");
  return t;
}
function V3(e = "") {
  const t = String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").toLowerCase().replace(/[\s_-]+/g, "").trim();
  return (/* @__PURE__ */ new Set([
    "user",
    "users",
    "player",
    "players",
    "pc",
    "you",
    "me",
    "myself",
    "self",
    "host",
    "dm",
    "gm",
    "narrator",
    "operator",
    "protagonist",
    "用户",
    "玩家",
    "主控",
    "我",
    "你",
    "自己",
    "本人",
    "我方",
    "主人翁",
    "叙述者",
    "主持人"
  ])).has(t);
}
function bH(e = "") {
  return `memory/characters/${G3(e)}.md`;
}
function Nl(e = "") {
  const t = String(e || "").replace(/\\/g, "/").trim();
  if (!t.startsWith("memory/characters/") || !t.endsWith(".md")) return "";
  const n = t.slice(18, -3);
  if (!n || n.includes("/")) return "";
  try {
    return G3(n);
  } catch {
    return "";
  }
}
function eg(e = "") {
  return !!Nl(e);
}
function Cs(e = "") {
  const t = String(e || "").replace(/\\/g, "/").trim();
  if (!t) throw new Error("memory_path_required");
  if (sl(t)) return "memory/state.md";
  const n = Nl(t);
  if (n) return bH(n);
  throw t.startsWith("memory/") ? t.endsWith(".md") ? new Error("memory_path_invalid") : new Error("memory_path_must_be_md") : new Error("memory_path_scope_required");
}
function Nw(e) {
  return JSON.parse(JSON.stringify(e));
}
function ov(e = "") {
  return [
    "# 会话记忆",
    "",
    "## 剧情脉络",
    "",
    "### 当前主线",
    "- 未开始。",
    "",
    "### 长期压力",
    "- 暂无。",
    "",
    "### 未解伏笔",
    "- 暂无。",
    "",
    "### 关系态势",
    "- 只保留影响主线或当前局面的关系摘要；人物细节放入对应人物记忆。",
    "",
    "## 当前状态",
    "- 时间：未明确",
    "- 地点：未明确",
    `- 在场人物：${_o(e, 120) || "当前角色"}`,
    "- 关键物品：暂无",
    "- 身体/情绪/约束状态：暂无",
    "## 近期连续事件",
    "- 暂无。"
  ].join(`
`);
}
function wH(e = "", t = "") {
  const n = wi();
  return [{
    sessionId: e,
    status: "active",
    createdAt: n,
    updatedAt: n,
    source: "default",
    path: "memory/state.md",
    content: ov(t)
  }];
}
async function $w(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) throw new Error("memory_session_required");
  if (!await Ct.get(n)) throw new Error("memory_session_missing");
  const r = await Tn.where("sessionId").equals(n).toArray();
  if (r.length) return r.sort((a, o) => a.path.localeCompare(o.path));
  const s = wH(n, t.characterName);
  return await Tn.bulkPut(s), await Ct.update(n, { updatedAt: wi() }), s;
}
async function ua(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await Tn.where("sessionId").equals(n).sortBy("path");
  return t.includeStale ? r : r.filter((s) => s.status !== "stale");
}
function SH(e) {
  return Ts(e.content, 1200);
}
function AH(e) {
  return {
    path: e.path,
    status: e.status,
    createdAt: e.createdAt,
    updatedAt: e.updatedAt,
    source: e.source,
    staleFromOrder: e.staleFromOrder,
    contentLength: String(e.content || "").length,
    preview: SH(e)
  };
}
function TH(e) {
  return {
    ...AH(e),
    title: PT(e.path),
    searchText: Ts([
      e.path,
      PT(e.path),
      e.content || ""
    ].filter(Boolean).join(`
`), 2e4)
  };
}
async function li(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = Cs(t);
  return await Tn.get([n, r]) || null;
}
async function EH(e = "", t) {
  const n = Number(t);
  return Number.isFinite(n) ? Math.floor(n) : await fw(e) ?? -1;
}
function CH(e = [], t = "") {
  const n = e.filter((r) => r.status !== "stale").sort((r, s) => r.path.localeCompare(s.path));
  return n.length === 1 && n[0]?.path === "memory/state.md" && n[0]?.content === ov(t);
}
function J3(e) {
  const t = String(e || "");
  let n = 2166136261, r = 522970236;
  for (let s = 0; s < t.length; s += 1) {
    const a = t.charCodeAt(s);
    n ^= a, n = Math.imul(n, 16777619) >>> 0, r ^= a + s, r = Math.imul(r, 16777619) >>> 0;
  }
  return `${t.length.toString(36)}:${n.toString(16)}${r.toString(16)}`;
}
function jb(e) {
  return [
    Cs(e.path),
    String(e.status || ""),
    String(e.source || ""),
    Number.isFinite(Number(e.staleFromOrder)) ? String(Number(e.staleFromOrder)) : "",
    String(e.content || "").length,
    J3(e.content || "")
  ].join("");
}
function tg(e = []) {
  const t = [...e].sort((n, r) => Cs(n.path).localeCompare(Cs(r.path))).map(jb).join("");
  return `${e.length}:${J3(t)}`;
}
function IH(e) {
  return tg((e?.files || []).map((t) => t.file));
}
async function xH(e = "", t) {
  const n = String(e || "").trim();
  return n ? await Dt.transaction("rw", Tn, Da, Qt, Ct, async () => {
    const r = await ua(n, { includeStale: !0 });
    if (!r.length) return null;
    const s = await EH(n, t);
    if (s === -1 && CH(r, (await Ct.get(n))?.characterName || ""))
      return null;
    const a = tg(r), o = await Lw(n);
    if (o && IH(o) === a) return null;
    const l = {
      sessionId: n,
      floor: s,
      files: r.sort((i, u) => i.path.localeCompare(u.path)).map((i) => ({
        path: i.path,
        file: Nw(i)
      })),
      createdAt: wi()
    };
    return await Da.put(l), l;
  }) : null;
}
async function Y3(e = "") {
  const t = String(e || "").trim();
  return t ? (await Da.where("sessionId").equals(t).sortBy("floor")).sort((n, r) => n.floor - r.floor || n.createdAt - r.createdAt) : [];
}
async function Lw(e = "", t = Number.POSITIVE_INFINITY) {
  const n = String(e || "").trim(), r = Number(t);
  return n && (await Y3(n)).filter((s) => Number(s.floor) <= r || r === Number.POSITIVE_INFINITY).sort((s, a) => Number(a.floor) - Number(s.floor) || Number(a.createdAt) - Number(s.createdAt))[0] || null;
}
async function X3(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  return !n || !Number.isFinite(r) ? 0 : await Dt.transaction("rw", Da, async () => {
    const s = (await Y3(n)).filter((a) => Number(a.floor) >= r);
    return s.length ? (await Da.bulkDelete(s.map((a) => [a.sessionId, a.floor])), s.length) : 0;
  });
}
async function kH(e = "", t = -1) {
  const n = String(e || "").trim();
  if (!n) return {
    changed: !1,
    currentFileCount: 0,
    targetFileCount: 0,
    changedPaths: []
  };
  const r = await Lw(n, t), s = await ua(n, { includeStale: !0 });
  let a;
  r ? a = r.files.map((f) => Nw(f.file)) : a = [{
    sessionId: n,
    path: "memory/state.md",
    content: ov((await Ct.get(n))?.characterName || ""),
    status: "active",
    source: "default",
    createdAt: 0,
    updatedAt: 0
  }];
  const o = tg(s), l = tg(a), i = new Map(s.map((f) => [Cs(f.path), jb(f)])), u = new Map(a.map((f) => [Cs(f.path), jb(f)])), d = [.../* @__PURE__ */ new Set([...i.keys(), ...u.keys()])].filter((f) => i.get(f) !== u.get(f));
  return {
    changed: o !== l,
    currentFileCount: s.length,
    targetFileCount: a.length,
    changedPaths: d
  };
}
async function j3(e = "", t = -1) {
  const n = String(e || "").trim();
  if (!n) throw new Error("memory_session_required");
  return await Dt.transaction("rw", Tn, Da, As, Ct, async () => {
    const r = await Lw(n, t);
    if (r?.files) {
      const u = wi(), d = await ua(n, { includeStale: !0 }), f = r.files, m = new Set(f.map((S) => Cs(S.path))), v = d.filter((S) => !m.has(S.path)).map((S) => [S.sessionId, S.path]), y = f.map((S) => ({
        ...Nw(S.file),
        sessionId: n,
        path: Cs(S.path),
        updatedAt: u
      }));
      return v.length && await Tn.bulkDelete(v), y.length && await Tn.bulkPut(y), await Ct.update(n, { updatedAt: u }), await Zb(n), y;
    }
    const s = wi(), a = await Ct.get(n), o = (await ua(n, { includeStale: !0 })).filter((u) => u.path !== "memory/state.md").map((u) => [u.sessionId, u.path]);
    o.length && await Tn.bulkDelete(o);
    const l = await Tn.get([n, "memory/state.md"]), i = {
      sessionId: n,
      path: "memory/state.md",
      content: ov(a?.characterName || ""),
      status: "active",
      source: "default",
      createdAt: Number(l?.createdAt) || s,
      updatedAt: s
    };
    return await Tn.put(i), await Ct.update(n, { updatedAt: s }), await Zb(n), [i];
  });
}
async function Qb(e = "", t = "", n = "", r = {}) {
  const s = String(e || "").trim();
  if (!s) throw new Error("memory_session_required");
  const a = Cs(t), o = wi(), l = await Tn.get([s, a]), i = {
    sessionId: s,
    path: a,
    content: String(n || "").replace(/\r\n/g, `
`),
    status: "active",
    source: String(r.source || l?.source || "manager"),
    staleFromOrder: Number.isFinite(Number(r.staleFromOrder)) ? Number(r.staleFromOrder) : l?.staleFromOrder,
    createdAt: Number(l?.createdAt) || o,
    updatedAt: o
  };
  return await Tn.put(i), await Ct.update(s, { updatedAt: o }), await Zb(s), i;
}
async function Zb(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) throw new Error("memory_session_required");
  const r = wi(), s = await As.get([n, "markdown-derived"]), a = {
    sessionId: n,
    kind: "markdown-derived",
    status: "stale",
    error: String(t || ""),
    sourceFingerprint: "",
    derivedAt: r,
    updatedAt: r,
    files: Array.isArray(s?.files) ? s.files : []
  };
  return await As.put(a), a;
}
async function Q3(e = "", t = "markdown-derived") {
  const n = String(e || "").trim();
  return n && await As.get([n, t]) || null;
}
function MH(e) {
  const t = e.map((r) => `${r.path}${r.status}${r.updatedAt}${r.content.length}`).join("");
  let n = 2166136261;
  for (let r = 0; r < t.length; r += 1)
    n ^= t.charCodeAt(r), n = Math.imul(n, 16777619) >>> 0;
  return `${e.length}:${n.toString(16)}`;
}
async function Hf(e = "") {
  const t = String(e || "").trim();
  if (!t) throw new Error("memory_session_required");
  const n = wi();
  try {
    const r = await ua(t, { includeStale: !0 }), s = {
      sessionId: t,
      kind: "markdown-derived",
      status: "ready",
      error: "",
      sourceFingerprint: MH(r),
      derivedAt: n,
      updatedAt: n,
      files: r.map(TH)
    };
    return await As.put(s), s;
  } catch (r) {
    const s = {
      sessionId: t,
      kind: "markdown-derived",
      status: "failed",
      error: r instanceof Error ? r.message : String(r || "memory_index_failed"),
      sourceFingerprint: "",
      derivedAt: n,
      updatedAt: n,
      files: []
    };
    return await As.put(s), s;
  }
}
function PH(e) {
  return Cs(e.filePath || e.path || "");
}
function Af(e = "") {
  return String(e || "").replace(/\\/g, "/").replace(/^\/+/, "").trim();
}
function Z3(e = "memory/") {
  const t = Af(e || "");
  if (!t) throw new Error("source_path_required");
  const n = t.endsWith("/") ? t : `${t}/`;
  if (n.includes("..")) throw new Error("source_path_invalid");
  if (![
    "chat/",
    "worldbooks/",
    "memory/"
  ].some((r) => n === r || n.startsWith(r))) throw new Error("source_path_scope_required");
  return n;
}
function Jh(e = "", t = "item") {
  return String(e || "").trim().replace(/[\\/:*?"<>|#%{}[\]^~`]+/g, "_").replace(/\s+/g, "_").replace(/^_+|_+$/g, "") || t;
}
function s_(e = "") {
  const t = Af(e);
  if (!t) throw new Error("source_path_required");
  if (t.endsWith("/")) throw new Error("source_file_path_required");
  if (t.includes("..")) throw new Error("source_path_invalid");
  if (t.startsWith("memory/")) return Cs(t);
  if (t === "chat/transcript.md" || /^chat\/messages\/\d+\.md$/.test(t) || /^worldbooks\/(?:character|global|chat|entries)\//.test(t) && t.endsWith(".md")) return t;
  throw new Error("source_file_path_invalid");
}
function RT(e = "") {
  return e === "memory/state.md" || eg(e);
}
function NT(e = "") {
  const t = Cs(e), n = Nl(t);
  if (n && V3(n)) throw new Error("memory_character_user_reserved");
  return t;
}
function RH(e = [], t = "memory/") {
  const n = Z3(t), r = /* @__PURE__ */ new Map();
  return [
    "chat/",
    "chat/messages/",
    "worldbooks/",
    "worldbooks/character/",
    "worldbooks/global/",
    "worldbooks/chat/",
    "worldbooks/entries/",
    "memory/",
    "memory/characters/"
  ].forEach((s) => {
    if (!s.startsWith(n) || s === n) return;
    const [a] = s.slice(n.length).split("/");
    if (!a) return;
    const o = `${n}${a}/`;
    r.set(o, {
      name: a,
      path: o,
      type: "directory",
      readonly: !o.startsWith("memory/")
    });
  }), e.forEach((s) => {
    if (!s.startsWith(n) || s === n) return;
    const a = s.slice(n.length), [o] = a.split("/");
    if (!o) return;
    const l = a.includes("/"), i = `${n}${o}${l ? "/" : ""}`;
    r.set(i, {
      name: o,
      path: i,
      type: l ? "directory" : "file",
      readonly: !i.startsWith("memory/")
    });
  }), [...r.values()].sort((s, a) => s.type !== a.type ? s.type === "directory" ? -1 : 1 : s.path.localeCompare(a.path, "zh-CN"));
}
function $T(e, t, n = 0) {
  const r = _o(t.comment || t.title || t.name || `entry-${n + 1}`, 200), s = Array.isArray(t.key) ? t.key.join(", ") : String(t.key || ""), a = Array.isArray(t.keysecondary) ? t.keysecondary.join(", ") : Array.isArray(t.secondary_keys) ? t.secondary_keys.join(", ") : String(t.keysecondary || t.secondary_keys || "");
  return [
    `# ${r}`,
    "",
    `Book: ${_o(e.name, 200)}`,
    `Source: ${_o(t.worldSourceType || e.worldSourceType || "worldbook", 80)}`,
    `UID: ${_o(t.uid ?? t.id ?? n, 120)}`,
    s ? `Keys: ${s}` : "",
    a ? `Secondary keys: ${a}` : "",
    Number.isFinite(Number(t.order)) ? `Order: ${Number(t.order)}` : "",
    t.position !== void 0 ? `Position: ${String(t.position)}` : "",
    "",
    "## Content",
    Ts(t.content || "")
  ].filter((o) => o !== "").join(`
`);
}
function Dw(e = {}) {
  const t = Array.isArray(e.worldBooks) ? e.worldBooks : [], n = [];
  return t.forEach((r, s) => {
    const a = Jh(r.name, `book-${s + 1}`), o = String(r.worldSourceType || "entries").trim(), l = o === "character" || o === "global" || o === "chat" ? o : "entries";
    (Array.isArray(r.entries) ? r.entries : []).forEach((i, u) => {
      if (!Ts(i.content || "")) return;
      const d = Jh(i.uid ?? i.id ?? i.comment ?? u, `entry-${u + 1}`);
      n.push({
        path: `worldbooks/${l}/${a}/${d}.md`,
        content: $T(r, i, u),
        readonly: !0
      });
    });
  }), !n.length && Array.isArray(e.worldEntries) && e.worldEntries.forEach((r, s) => {
    if (!Ts(r.content || "")) return;
    const a = Jh(r.sourceWorldBook || "worldEntries", "worldEntries"), o = Jh(r.uid ?? r.id ?? r.comment ?? s, `entry-${s + 1}`);
    n.push({
      path: `worldbooks/entries/${a}/${o}.md`,
      content: $T({
        name: a,
        entries: [r]
      }, r, s),
      readonly: !0
    });
  }), n.sort((r, s) => r.path.localeCompare(s.path, "zh-CN"));
}
async function NH(e = "", t = {}) {
  const n = await ua(e, { includeStale: !0 });
  return [
    "chat/transcript.md",
    ...(await hw(e, 0)).map((r) => `chat/messages/${r}.md`),
    ...Dw(t).map((r) => r.path),
    ...n.map((r) => r.path)
  ].sort((r, s) => r.localeCompare(s, "zh-CN"));
}
function $H(e = "") {
  const t = String(e || "content").trim().replace(/[\s-]/g, "_").replace(/[A-Z]/g, (n) => `_${n.toLowerCase()}`).replace(/_+/g, "_").replace(/^_+|_+$/g, "");
  return t === "files_with_matches" || t === "fileswithmatches" ? "files_with_matches" : t === "count" ? "count" : "content";
}
function LH(e = "", t = !1) {
  const n = String(e || "");
  if (!n) throw new Error("grep_pattern_required");
  return t === !0 ? new RegExp(n, "iu") : new RegExp(n.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "iu");
}
function Uc(e = "", t = "", n = "") {
  const r = Af(t || "");
  if (r) {
    if (r.endsWith("/")) {
      if (!e.startsWith(r)) return !1;
    } else if (e !== r) return !1;
  }
  const s = String(n || "").trim();
  if (!s) return !0;
  if (!/[/*?[\]{}]/.test(s)) return e === s || e.endsWith(`/${s.replace(/^\/+/, "")}`);
  const a = s.replace(/\*\*/g, "\0DOUBLE_STAR\0").replace(/[|\\{}()[\]^$+?.]/g, "\\$&").replace(/\0DOUBLE_STAR\0/g, ".*").replace(/\*/g, "[^/]*").replace(/\?/g, "[^/]");
  return new RegExp(`^${a.startsWith("chat/") || a.startsWith("worldbooks/") || a.startsWith("memory/") ? a : `(?:chat|worldbooks|memory)/${a}`}$`).test(e);
}
function a_(e, t, n, r, s = 0) {
  const a = jm(t.content);
  let o = 0;
  a.forEach((l, i) => {
    if (n.lastIndex = 0, !!n.test(l) && (o += 1, r === "content")) {
      const u = Math.max(0, i - s), d = Math.min(a.length, i + s + 1);
      e.push({
        path: t.path,
        lineNumber: i + 1,
        line: l,
        context: bo(a.slice(u, d), u + 1)
      });
    }
  }), r === "files_with_matches" && o > 0 ? e.push({ path: t.path }) : r === "count" && o > 0 && e.push({
    path: t.path,
    count: o
  });
}
function DH() {
  return [
    {
      type: "function",
      function: {
        name: vr.LS,
        description: [
          "List first-level files and directories in Tavern text sources.",
          "Returns directory entries only. Does not recurse and does not read file contents.",
          "Use before reading or editing when you need to locate chat evidence, worldbook entries, or memory files.",
          "Directories are `chat/`, `worldbooks/`, and `memory/`. `chat/` and `worldbooks/` are read-only; `memory/` is writable when memory archiving is authorized."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            path: {
              type: "string",
              description: "Directory path, for example `chat/`, `worldbooks/`, `memory/`, or `memory/characters/`."
            },
            offset: {
              type: "number",
              description: "1-based directory entry offset. Default 1."
            },
            limit: {
              type: "number",
              description: "Maximum entries to return. Default 100, max 300."
            }
          },
          required: ["path"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: vr.GREP,
        description: [
          "Search text inside Tavern text sources.",
          "Uses literal text search by default and returns matching files plus line-level snippets.",
          "Use before reading many files to locate facts, names, dialogue, places, worldbook lore, memory notes, or source evidence.",
          "`path` can be a directory like `chat/`, `worldbooks/`, `memory/characters/`, or one exact file. `include` limits the file glob/name. For regex search, explicitly pass `useRegex: true`."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            pattern: {
              type: "string",
              description: "Search pattern. Treated as literal text by default; use `useRegex: true` only when intentionally using regex."
            },
            path: {
              type: "string",
              description: "Optional search scope, directory or exact file."
            },
            include: {
              type: "string",
              description: "Optional file glob/name filter, for example `*.md`, `memory/characters/*.md`, or `12.md`."
            },
            outputMode: {
              type: "string",
              enum: [
                "content",
                "files_with_matches",
                "count"
              ],
              description: "`content` returns matched lines, `files_with_matches` returns files only, and `count` returns match counts. Default content."
            },
            limit: {
              type: "number",
              description: "Maximum results to return. Default 100, max 100."
            },
            offset: {
              type: "number",
              description: "Skip this many results before returning matches. Default 0."
            },
            contextLines: {
              type: "number",
              description: "Context lines before and after each match. Default 0, max 5."
            },
            useRegex: {
              type: "boolean",
              description: "Whether to treat pattern as a regex. Default false; omit it for literal text search."
            }
          },
          required: ["pattern"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: vr.READ,
        description: [
          "Read a Tavern text source file, or list a directory.",
          "For files, returns line-numbered content. For directories, returns directory entries. Large files include continuation hints.",
          "Use `tail` by itself when you need the end of a file.",
          "The argument name is `filePath`, not `path`. File paths look like `chat/messages/12.md`, `worldbooks/character/book/uid.md`, or `memory/state.md`."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "File or directory path. Do not pass path."
            },
            offset: {
              type: "number",
              description: "1-based line or directory-entry offset. Default 1."
            },
            limit: {
              type: "number",
              description: "Maximum lines or entries. Default 1200, max 2000."
            },
            tail: {
              type: "number",
              description: "Return the final N lines of a file. Use by itself; do not combine with offset/limit."
            }
          },
          required: ["filePath"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: vr.WRITE,
        description: [
          "Write a complete Tavern memory Markdown file.",
          "Only `memory/state.md` and `memory/characters/<角色名>.md` are writable. `chat/` and `worldbooks/` are read-only evidence sources.",
          "Use for creating files, complete rewrites, or rewrites where most content is new.",
          "Read the target file first. Write overwrites the entire file, so include all original content you want to keep.",
          "The argument names are `filePath` and `content`."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "Writable memory file path: `memory/state.md` or `memory/characters/<角色名>.md`."
            },
            content: {
              type: "string",
              description: "Full file content to write."
            }
          },
          required: ["filePath", "content"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: vr.EDIT,
        description: [
          "Edit one existing Tavern memory Markdown file by replacing original text fragments, replacing inclusive line ranges, inserting text at line positions, or removing text with empty replacements.",
          "One call edits one file. Only `memory/state.md` and `memory/characters/<角色名>.md` are writable.",
          "Use oldString/newString for local revisions. Use startLine/endLine/newString for contiguous passage replacement where copying oldString would be fragile. Use insertAtLine/newString to add text before a line or at the end. Use Write for complete rewrites.",
          "Read the target file first unless the exact current text is already available in the conversation or a recent tool result. Line-range and insertion edits must use line numbers from the latest Read result.",
          "Put multiple edits in the edits array. Line-range and insertion items may share one call when they use line numbers from the same Read result. Keep oldString edits separate from line-number edits unless the whole change can be expressed with line numbers.",
          'The `edits` argument must be a non-empty JSON array, not a JSON-stringified string. Correct: `"edits":[{"startLine":10,"endLine":50,"newString":"..."}]`.',
          "Each edit item should choose exactly one mode. If stray optional fields appear, Edit normalizes by priority: complete startLine/endLine wins, then insertAtLine, then oldString.",
          "Do not issue multiple Edit tool calls for the same file in one assistant turn. Combine same-file changes into one call.",
          "",
          "## Matching Rules",
          "oldString must be an exact fragment present in the file, including spaces and newlines. To remove a matched fragment, set newString to an empty string.",
          "Common punctuation equivalence is supported, such as straight/curly quotes and ASCII/full-width punctuation. Replacements preserve the file punctuation style when possible.",
          "For long oldString fragments, Edit can tolerate whitespace-only differences such as indentation, line wrapping, or extra/missing blank lines.",
          "Each oldString must be unique by default. Multiple matches return line numbers and context.",
          "Line-range edits use 1-based inclusive startLine/endLine from Read output. Insertion uses 1-based insertAtLine; use totalLines + 1 to append.",
          "Line-range and insertion edits are applied by original Read line numbers from bottom to top automatically; do not adjust later line numbers yourself."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "Writable memory file path: `memory/state.md` or `memory/characters/<角色名>.md`."
            },
            edits: {
              type: "array",
              description: "Real, non-empty JSON array, not a quoted JSON string. Each item should use exactly one mode: oldString/newString, startLine/endLine/newString, or insertAtLine/newString. Stray optional fields are ignored by mode priority.",
              items: {
                type: "object",
                properties: {
                  oldString: {
                    type: "string",
                    description: "Original text fragment to replace. Must match exactly, with punctuation/long-whitespace tolerance."
                  },
                  startLine: {
                    type: "number",
                    description: "1-based inclusive start line from the latest Read result."
                  },
                  endLine: {
                    type: "number",
                    description: "1-based inclusive end line from the latest Read result."
                  },
                  insertAtLine: {
                    type: "number",
                    description: "1-based insertion point from the latest Read result. Use totalLines + 1 to append."
                  },
                  newString: {
                    type: "string",
                    description: "Replacement, inserted text, or an empty string to delete the matched fragment or line range."
                  },
                  replaceAll: {
                    type: "boolean",
                    description: "Whether to replace all matches. Default false."
                  }
                },
                required: ["newString"],
                additionalProperties: !1
              }
            }
          },
          required: ["filePath", "edits"],
          additionalProperties: !1
        }
      }
    }
  ];
}
async function OH(e = "", t = {}) {
  const n = Math.floor(Number(t.tail) || 0), r = av(t.offset, 1), s = Zm(t.limit, Rw, wl), a = [];
  let o = 0;
  if (n > 0) {
    let d = 0;
    for (; a.length < Math.min(wl, n); ) {
      const v = await ev(e, 100, d);
      if (o = v.total, !v.messages.length) break;
      const y = v.messages.flatMap((S) => [
        `## order ${S.order} ${S.role}`,
        Ts(S.content || ""),
        ""
      ]);
      if (a.unshift(...y), d += v.messages.length, d >= v.total) break;
    }
    const f = a.slice(Math.max(0, a.length - Math.min(wl, n))), m = Math.max(1, a.length - f.length + 1);
    return {
      ok: !0,
      path: "chat/transcript.md",
      content: bo(f, m),
      numberedContent: bo(f, m),
      lineStart: m,
      lineEnd: m + f.length - 1,
      totalLines: a.length,
      truncated: a.length > f.length,
      nextOffset: 0,
      summary: `读取 chat/transcript.md 末尾 ${f.length} 行（${o} 条消息）。`
    };
  }
  let l = 0, i = 0;
  for (; a.length < s; ) {
    const d = await Nu(e, 0, Number.POSITIVE_INFINITY, 200, l);
    if (o = d.total, !d.messages.length) break;
    for (const f of d.messages) {
      const m = [
        `## order ${f.order} ${f.role}`,
        Ts(f.content || ""),
        ""
      ];
      if (i + m.length < r) {
        i += m.length;
        continue;
      }
      const v = Math.max(0, r - i - 1);
      if (a.push(...m.slice(v)), i += m.length, a.length >= s) break;
    }
    if (l += d.messages.length, l >= d.total) break;
  }
  const u = a.slice(0, s);
  return {
    ok: !0,
    path: "chat/transcript.md",
    content: bo(u, r),
    numberedContent: bo(u, r),
    lineStart: r,
    lineEnd: r + u.length - 1,
    totalLines: r + u.length - 1,
    truncated: u.length >= s,
    nextOffset: u.length >= s ? r + u.length : 0,
    summary: `读取 chat/transcript.md 第 ${r}-${r + u.length - 1} 行（${o} 条消息）。`
  };
}
async function FH(e = "", t = "", n = {}, r = {}) {
  if (t === "chat/transcript.md") return OH(e, n);
  const s = t.match(/^chat\/messages\/(\d+)\.md$/);
  if (s) {
    const o = await bl(e, Number(s[1]));
    return o ? o_(t, [
      `# Message ${o.order}`,
      "",
      `Role: ${o.role}`,
      o.name ? `Name: ${o.name}` : "",
      o.error ? "Error: true" : "",
      "",
      "## Content",
      Ts(o.content || ""),
      ...Array.isArray(o.thoughts) && o.thoughts.length ? [
        "",
        "## Thoughts",
        ...o.thoughts.map((l, i) => `### ${_o(l?.label || `思考 ${i + 1}`, 120)}
${Ts(l?.text || "")}`)
      ] : []
    ].filter((l) => l !== "").join(`
`), n) : {
      ok: !1,
      path: t,
      summary: `${t} 不存在。`,
      error: "source_file_not_found"
    };
  }
  const a = Dw(r).find((o) => o.path === t);
  if (a) return o_(t, a.content, n);
  if (t.startsWith("memory/")) {
    const o = await li(e, t);
    return o ? o_(t, o.content, n) : {
      ok: !1,
      summary: `${t} 不存在。`,
      path: t,
      error: "memory_file_not_found"
    };
  }
  return {
    ok: !1,
    path: t,
    summary: `${t} 不存在。`,
    error: "source_file_not_found"
  };
}
function o_(e = "", t = "", n = {}) {
  const r = jm(t), s = Math.floor(Number(n.tail) || 0);
  let a = av(n.offset, 1), o = Zm(n.limit, Rw, wl);
  s > 0 && (o = Math.min(wl, s), a = Math.max(1, r.length - o + 1));
  const l = Math.max(0, a - 1), i = r.slice(l, l + o), u = l + o < r.length ? l + o + 1 : 0;
  return {
    ok: !0,
    path: e,
    lineStart: l + 1,
    lineEnd: l + i.length,
    totalLines: r.length,
    content: bo(i, l + 1),
    numberedContent: bo(i, l + 1),
    truncated: u > 0,
    nextOffset: u,
    summary: `读取 ${e} 第 ${l + 1}-${l + i.length} 行，共 ${r.length} 行。`
  };
}
async function e4(e = "", t = "", n = {}, r = {}) {
  const s = String(e || "").trim();
  if (!s) return {
    ok: !1,
    summary: "缺少 sessionId。",
    error: "source_session_required"
  };
  const a = r.contextSnapshot || {};
  try {
    if (t === vr.LS) {
      const o = Z3(n.path || "memory/"), l = RH(await NH(s, a), o), i = av(n.offset, 1), u = Math.min(300, Math.max(1, Math.floor(Number(n.limit) || 100))), d = l.slice(i - 1, i - 1 + u);
      return {
        ok: !0,
        path: o,
        entries: d,
        count: l.length,
        truncated: i - 1 + u < l.length,
        nextOffset: i - 1 + u < l.length ? i + u : 0,
        summary: `${o} 下有 ${l.length} 项，返回 ${d.length} 项。`
      };
    }
    if (t === vr.READ) {
      const o = Af(n.filePath || "");
      if (!o) throw new Error("source_path_required");
      return o.endsWith("/") ? e4(s, vr.LS, {
        path: o,
        offset: n.offset,
        limit: n.limit
      }, r) : FH(s, s_(o), n, a);
    }
    if (t === vr.GREP) {
      const o = String(n.pattern ?? n.query ?? "").trim();
      if (!o) return {
        ok: !1,
        summary: "缺少搜索词。",
        error: "grep_pattern_required"
      };
      const l = LH(o, n.useRegex === !0), i = $H(n.outputMode), u = Math.min(Xb, Math.max(1, Math.floor(Number(n.limit) || Xb))), d = Qm(n.offset, 0), f = Math.min(5, Qm(n.contextLines, 0)), m = Af(n.path || n.scope || ""), v = String(n.include || "").trim(), y = [];
      let S = 0;
      for (const I of Dw(a))
        Uc(I.path, m, v) && (S += 1, a_(y, I, l, i, f));
      if ((await ua(s, { includeStale: !0 })).forEach((I) => {
        Uc(I.path, m, v) && (S += 1, a_(y, {
          path: I.path,
          content: I.content
        }, l, i, f));
      }), !m || Uc("chat/transcript.md", m, v) || m.startsWith("chat/")) {
        let E = 0;
        for (; ; ) {
          const N = await Nu(s, 0, Number.POSITIVE_INFINITY, 500, E);
          if (!N.messages.length || (N.messages.forEach(($) => {
            const p = `chat/messages/${$.order}.md`;
            !Uc(p, m, v) && !Uc("chat/transcript.md", m, v) || (S += 1, a_(y, {
              path: p,
              content: Ts($.content || "")
            }, l, i, f));
          }), E += N.messages.length, E >= N.total)) break;
        }
      }
      const x = y.slice(d, d + u);
      return {
        ok: !0,
        pattern: o,
        path: m,
        outputMode: i,
        searchedFileCount: S,
        count: y.length,
        results: x,
        matches: x.map((I) => ({
          path: I.path,
          line: I.lineNumber,
          text: I.line,
          context: I.context,
          count: I.count
        })),
        truncated: d + u < y.length,
        nextOffset: d + u < y.length ? d + u : 0,
        summary: `搜索到 ${y.length} 项，返回 ${x.length} 项。`
      };
    }
    if (t === vr.WRITE) {
      const o = s_(n.filePath || n.path || "");
      if (!RT(o)) return {
        ok: !1,
        path: o,
        changed: !1,
        summary: `${o} 是只读资料，不能写入。`,
        error: "source_file_read_only"
      };
      const l = await LT(s, Zo.WRITE, {
        filePath: o,
        content: String(n.content || "")
      }, r);
      return {
        ...l,
        bytes: l.ok ? new TextEncoder().encode(String(n.content || "")).length : void 0
      };
    }
    if (t === vr.EDIT) {
      const o = s_(n.filePath || n.path || "");
      return RT(o) ? LT(s, Zo.EDIT, {
        filePath: o,
        edits: n.edits
      }, r) : {
        ok: !1,
        path: o,
        changed: !1,
        summary: `${o} 是只读资料，不能编辑。`,
        error: "source_file_read_only"
      };
    }
    return {
      ok: !1,
      summary: `${t} 不可用。`,
      error: "source_tool_not_available"
    };
  } catch (o) {
    if (t4(o)) throw o;
    return {
      ok: !1,
      summary: o instanceof Error ? o.message : String(o || "source_tool_failed"),
      error: o instanceof Error ? o.message : String(o || "source_tool_failed")
    };
  }
}
function e0() {
  return [
    ...DH(),
    ...Vq(),
    ...vH()
  ];
}
function t4(e) {
  const t = e instanceof Error ? e.message : String(e || "");
  return (e instanceof Error ? e.name : "") === "AbortError" || t === "manager_source_messages_changed" || t === "manager_epoch_expired" || t === "manager_aborted";
}
function i_(e, t = {}) {
  const n = t.full === !0, r = n ? Ts(e.content) : Ts(e.content, 8e3), s = Array.isArray(e.thoughts) ? e.thoughts.map((a, o) => ({
    label: _o(a?.label || `思考 ${o + 1}`, 80),
    text: Ts(a?.text || "", n ? void 0 : 8e3)
  })).filter((a) => a.text) : [];
  return {
    order: e.order,
    role: String(e.role || ""),
    snippet: _o(e.content, 320),
    reasoningSnippet: s.length ? _o(s.map((a) => a.text).join(`
`), 320) : void 0,
    content: n ? r : void 0,
    thoughts: n && s.length ? s : void 0
  };
}
async function LT(e = "", t = "", n = {}, r = {}) {
  const s = String(e || "").trim();
  if (!s) return {
    ok: !1,
    summary: "缺少 sessionId。",
    error: "memory_session_required"
  };
  await $w(s);
  try {
    if (t === Zo.LIST) {
      const a = await ua(s, { includeStale: !0 });
      return {
        ok: !0,
        summary: `找到 ${a.length} 个记忆文件。`,
        files: a.map((o) => ({
          path: o.path,
          status: o.status,
          updatedAt: o.updatedAt
        }))
      };
    }
    if (t === Zo.READ) {
      const a = PH(n), o = await li(s, a);
      if (!o) return {
        ok: !1,
        summary: `${a} 不存在。`,
        path: a,
        error: "memory_file_not_found"
      };
      const l = jm(o.content), i = Math.floor(Number(n.tail) || 0);
      let u = av(n.offset, 1), d = Zm(n.limit, Rw, wl);
      i > 0 && (d = Math.min(wl, i), u = Math.max(1, l.length - d + 1));
      const f = Math.max(0, u - 1), m = l.slice(f, f + d), v = f + d < l.length ? f + d + 1 : 0;
      return {
        ok: !0,
        summary: `读取 ${a} 第 ${f + 1}-${f + m.length} 行，共 ${l.length} 行。`,
        path: a,
        content: m.join(`
`),
        numberedContent: bo(m, f + 1),
        lineStart: f + 1,
        lineEnd: f + m.length,
        totalLines: l.length,
        truncated: v > 0,
        nextOffset: v
      };
    }
    if (t === Zo.WRITE) {
      const a = NT(String(n.filePath || n.path || "")), o = await Dt.transaction("rw", Tn, As, zr, Qt, Ct, async () => {
        await r.beforeWriteGuard?.(), r.managerRunId && await Ob({
          managerRunId: r.managerRunId,
          sessionId: s,
          path: a
        });
        const i = await Qb(s, a, String(n.content || ""), { source: "manager" });
        return r.managerRunId && await AT({
          managerRunId: r.managerRunId,
          sessionId: s,
          path: i.path
        }), i;
      }), l = await li(s, o.path);
      return !l || l.content !== o.content ? {
        ok: !1,
        summary: `已生成 ${o.path} 的写入结果，但保存校验未通过；请重新读取文件确认当前内容。`,
        path: o.path,
        changed: !1,
        error: "memory_write_verification_failed"
      } : {
        ok: !0,
        summary: `已写入 ${o.path}。`,
        path: o.path,
        changed: !0
      };
    }
    if (t === Zo.EDIT) {
      const a = NT(String(n.filePath || n.path || "")), o = await li(s, a);
      if (!o) return {
        ok: !1,
        summary: `${a} 不存在。`,
        path: a,
        error: "memory_file_not_found"
      };
      const l = NF(o.content, n.edits), i = Array.isArray(l.results) ? l.results : [], u = i.filter((y) => y.ok && !y.satisfied).length, d = i.filter((y) => y.ok && y.satisfied).length, f = u + d, m = Math.max(0, i.length - f), v = l.content !== o.content;
      if (v) {
        await Dt.transaction("rw", Tn, As, zr, Qt, Ct, async () => {
          await r.beforeWriteGuard?.(), r.managerRunId && await Ob({
            managerRunId: r.managerRunId,
            sessionId: s,
            path: a
          }), await Qb(s, a, l.content, { source: "manager" }), r.managerRunId && await AT({
            managerRunId: r.managerRunId,
            sessionId: s,
            path: a
          });
        });
        const y = await li(s, a);
        if (!y || y.content !== l.content) return {
          ok: !1,
          partial: !0,
          summary: `已生成 ${a} 的修改结果，但保存校验未通过；请重新读取文件确认当前内容。`,
          path: a,
          changed: !1,
          appliedCount: u,
          satisfiedCount: d || void 0,
          successCount: f,
          failedCount: m,
          warning: l.warning,
          error: "memory_edit_persistence_verification_failed",
          details: i
        };
      }
      return {
        ok: !!l.ok,
        partial: !!l.partial,
        summary: l.ok ? v ? d > 0 ? `已修改 ${a}，应用 ${u} 项，另有 ${d} 项已是目标状态。` : `已修改 ${a}，应用 ${u} 项。` : `已确认 ${a} 的 ${d || u} 项修改已是目标状态，无需重复写入。` : v ? `已部分修改 ${a}：成功 ${f} 项，失败 ${m} 项。` : `未修改 ${a}：${i.find((y) => !y.ok)?.message || i.find((y) => !y.ok)?.error || "Edit failed"}。`,
        path: a,
        changed: v,
        appliedCount: u,
        satisfiedCount: d || void 0,
        successCount: f,
        failedCount: m,
        warning: l.warning,
        error: l.ok ? "" : "memory_edit_failed",
        details: i
      };
    }
    if (t === Zo.GREP) {
      const a = String(n.pattern || "").trim();
      if (!a) return {
        ok: !1,
        summary: "缺少搜索词。",
        error: "memory_grep_pattern_required"
      };
      const o = n.regex === !0 || n.useRegex === !0 ? new RegExp(a, "iu") : null, l = a.toLowerCase(), i = _H(n.outputMode), u = Zm(n.limit, yH, Xb), d = Qm(n.offset, 0), f = Math.min(5, Qm(n.contextLines, 0)), m = String(n.filePath || n.path || "").trim(), v = m && m.endsWith(".md") ? Cs(m) : "", y = m && !v ? String(m || "").replace(/\\/g, "/").trim() : "";
      if (y && (!y.startsWith("memory/") || y.includes(".."))) throw new Error("memory_path_scope_required");
      const S = [];
      (await ua(s, { includeStale: !0 })).filter((I) => v ? I.path === v : y ? I.path.startsWith(y.endsWith("/") ? y : `${y}/`) : !0).forEach((I) => {
        const E = jm(I.content);
        let N = 0;
        E.forEach(($, p) => {
          if (o && (o.lastIndex = 0), (o ? o.test($) : $.toLowerCase().includes(l)) && (N += 1, i === "content")) {
            const w = Math.max(0, p - f), _ = Math.min(E.length, p + f + 1);
            S.push({
              path: I.path,
              line: p + 1,
              text: $.trim(),
              context: f > 0 ? bo(E.slice(w, _), w + 1) : void 0
            });
          }
        }), i === "files_with_matches" && N > 0 ? S.push({ path: I.path }) : i === "count" && N > 0 && S.push({
          path: I.path,
          count: N
        });
      });
      const x = S.slice(d, d + u);
      return {
        ok: !0,
        summary: `搜索到 ${S.length} 项，返回 ${x.length} 项。`,
        count: S.length,
        truncated: d + u < S.length,
        nextOffset: d + u < S.length ? d + u : 0,
        matches: x
      };
    }
    if (t === Zo.CHAT_HISTORY) {
      const a = String(n.mode || "").trim(), o = Math.max(1, Math.min(100, Number(n.limit) || 12)), l = Math.max(0, Number(n.offset) || 0), i = n.full === !0;
      if (a === "recent") {
        const { messages: u, total: d } = await ev(s, o, l), f = u.map((v) => i_(v, { full: i })), m = l + o < d;
        return {
          ok: !0,
          summary: `共有 ${d} 条原文，读取最近窗口 ${f.length} 条。`,
          count: d,
          truncated: m,
          nextOffset: m ? l + o : 0,
          messages: f
        };
      }
      if (a === "range") {
        const u = Math.max(0, Number(n.startOrder) || 0), d = Object.prototype.hasOwnProperty.call(n, "endOrder") && Number.isFinite(Number(n.endOrder)), f = d ? null : await o3(s), m = d ? Math.max(u, Number(n.endOrder) || u) : Math.max(u, Number(f?.order) || u), { messages: v, total: y } = await Nu(s, u, m, o, l), S = v.map((I) => i_(I, { full: i })), x = l + o < y;
        return {
          ok: !0,
          summary: `order ${u}-${m} 共 ${y} 条原文，返回 ${S.length} 条。`,
          count: y,
          truncated: x,
          nextOffset: x ? l + o : 0,
          messages: S
        };
      }
      if (a === "grep") {
        const u = await a3(s), d = String(n.pattern || "").trim();
        if (!d) return {
          ok: !1,
          summary: "缺少搜索词。",
          error: "chat_history_pattern_required"
        };
        const f = n.regex === !0 || n.useRegex === !0 ? new RegExp(d, "iu") : null, m = d.toLowerCase(), v = u.filter((x) => (f && (f.lastIndex = 0), f ? f.test(x.content) : x.content.toLowerCase().includes(m))), y = v.slice(l, l + o).map((x) => i_(x, { full: i })), S = l + o < v.length;
        return {
          ok: !0,
          summary: `搜索到 ${v.length} 条原文，返回 ${y.length} 条。`,
          count: v.length,
          truncated: S,
          nextOffset: S ? l + o : 0,
          messages: y
        };
      }
      return {
        ok: !1,
        summary: `不支持的 ChatHistory 模式：${a || "empty"}`,
        error: "chat_history_mode_invalid"
      };
    }
    return {
      ok: !1,
      summary: `${t} 不可用。`,
      error: "memory_tool_not_available"
    };
  } catch (a) {
    if (t4(a)) throw a;
    return {
      ok: !1,
      summary: a instanceof Error ? a.message : String(a || "memory_tool_failed"),
      error: a instanceof Error ? a.message : String(a || "memory_tool_failed")
    };
  }
}
async function ng(e = "", t) {
  const n = String(e || "").trim();
  if (!n) return {
    floor: -1,
    memorySnapshotSaved: !1,
    taskSnapshotSaved: !1
  };
  const r = await cH(n, t), [s, a] = await Dt.transaction("rw", Tn, Da, Qt, Ct, $n, Oa, Fa, async () => [await xH(n, r), await uH(n, r)]);
  return {
    floor: r,
    memorySnapshotSaved: !!s,
    taskSnapshotSaved: !!a
  };
}
function nt(e, t, n, r, s) {
  if (r === "m") throw new TypeError("Private method is not writable");
  if (r === "a" && !s) throw new TypeError("Private accessor was defined without a setter");
  if (typeof t == "function" ? e !== t || !s : !t.has(e)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return r === "a" ? s.call(e, n) : s ? s.value = n : t.set(e, n), n;
}
function Ce(e, t, n, r) {
  if (n === "a" && !r) throw new TypeError("Private accessor was defined without a getter");
  if (typeof t == "function" ? e !== t || !r : !t.has(e)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return n === "m" ? r : n === "a" ? r.call(e) : r ? r.value : t.get(e);
}
var n4 = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return n4 = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
};
function Tf(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var t0 = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, xt = class extends Error {
}, Is = class n0 extends xt {
  constructor(t, n, r, s, a) {
    super(`${n0.makeMessage(t, n, r)}`), this.status = t, this.headers = s, this.requestID = s?.get("request-id"), this.error = n, this.type = a ?? null;
  }
  static makeMessage(t, n, r) {
    const s = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && s ? `${t} ${s}` : t ? `${t} status code (no body)` : s || "(no status code or body)";
  }
  static generate(t, n, r, s) {
    if (!t || !s) return new iv({
      message: r,
      cause: t0(n)
    });
    const a = n, o = a?.error?.type;
    return t === 400 ? new s4(t, a, r, s, o) : t === 401 ? new a4(t, a, r, s, o) : t === 403 ? new o4(t, a, r, s, o) : t === 404 ? new i4(t, a, r, s, o) : t === 409 ? new l4(t, a, r, s, o) : t === 422 ? new u4(t, a, r, s, o) : t === 429 ? new c4(t, a, r, s, o) : t >= 500 ? new d4(t, a, r, s, o) : new n0(t, a, r, s, o);
  }
}, ta = class extends Is {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, iv = class extends Is {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, r4 = class extends iv {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, s4 = class extends Is {
}, a4 = class extends Is {
}, o4 = class extends Is {
}, i4 = class extends Is {
}, l4 = class extends Is {
}, u4 = class extends Is {
}, c4 = class extends Is {
}, d4 = class extends Is {
}, BH = /^[a-z][a-z0-9+.-]*:/i, UH = (e) => BH.test(e), r0 = (e) => (r0 = Array.isArray, r0(e)), DT = r0;
function s0(e) {
  return typeof e != "object" ? {} : e ?? {};
}
function OT(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function qH(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
var HH = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new xt(`${e} must be an integer`);
  if (t < 0) throw new xt(`${e} must be a positive integer`);
  return t;
}, f4 = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, zH = (e) => new Promise((t) => setTimeout(t, e)), fu = "0.91.1", KH = () => typeof window < "u" && typeof window.document < "u" && typeof navigator < "u";
function WH() {
  return typeof Deno < "u" && Deno.build != null ? "deno" : typeof EdgeRuntime < "u" ? "edge" : Object.prototype.toString.call(typeof globalThis.process < "u" ? globalThis.process : 0) === "[object process]" ? "node" : "unknown";
}
var GH = () => {
  const e = WH();
  if (e === "deno") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": fu,
    "X-Stainless-OS": BT(Deno.build.os),
    "X-Stainless-Arch": FT(Deno.build.arch),
    "X-Stainless-Runtime": "deno",
    "X-Stainless-Runtime-Version": typeof Deno.version == "string" ? Deno.version : Deno.version?.deno ?? "unknown"
  };
  if (typeof EdgeRuntime < "u") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": fu,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": `other:${EdgeRuntime}`,
    "X-Stainless-Runtime": "edge",
    "X-Stainless-Runtime-Version": globalThis.process.version
  };
  if (e === "node") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": fu,
    "X-Stainless-OS": BT(globalThis.process.platform ?? "unknown"),
    "X-Stainless-Arch": FT(globalThis.process.arch ?? "unknown"),
    "X-Stainless-Runtime": "node",
    "X-Stainless-Runtime-Version": globalThis.process.version ?? "unknown"
  };
  const t = VH();
  return t ? {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": fu,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": `browser:${t.browser}`,
    "X-Stainless-Runtime-Version": t.version
  } : {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": fu,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": "unknown",
    "X-Stainless-Runtime-Version": "unknown"
  };
};
function VH() {
  if (typeof navigator > "u" || !navigator) return null;
  for (const { key: e, pattern: t } of [
    {
      key: "edge",
      pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "chrome",
      pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "firefox",
      pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "safari",
      pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/
    }
  ]) {
    const n = t.exec(navigator.userAgent);
    if (n) return {
      browser: e,
      version: `${n[1] || 0}.${n[2] || 0}.${n[3] || 0}`
    };
  }
  return null;
}
var FT = (e) => e === "x32" ? "x32" : e === "x86_64" || e === "x64" ? "x64" : e === "arm" ? "arm" : e === "aarch64" || e === "arm64" ? "arm64" : e ? `other:${e}` : "unknown", BT = (e) => (e = e.toLowerCase(), e.includes("ios") ? "iOS" : e === "android" ? "Android" : e === "darwin" ? "MacOS" : e === "win32" ? "Windows" : e === "freebsd" ? "FreeBSD" : e === "openbsd" ? "OpenBSD" : e === "linux" ? "Linux" : e ? `Other:${e}` : "Unknown"), UT, JH = () => UT ?? (UT = GH());
function YH() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new Anthropic({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function h4(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function p4(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return h4({
    start() {
    },
    async pull(n) {
      const { done: r, value: s } = await t.next();
      r ? n.close() : n.enqueue(s);
    },
    async cancel() {
      await t.return?.();
    }
  });
}
function Ow(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function XH(e) {
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await e[Symbol.asyncIterator]().return?.();
    return;
  }
  const t = e.getReader(), n = t.cancel();
  t.releaseLock(), await n;
}
var jH = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
});
function QH(e) {
  return Object.entries(e).filter(([t, n]) => typeof n < "u").map(([t, n]) => {
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") return `${encodeURIComponent(t)}=${encodeURIComponent(n)}`;
    if (n === null) return `${encodeURIComponent(t)}=`;
    throw new xt(`Cannot stringify type ${typeof n}; Expected string, number, boolean, or null. If you need to pass nested query parameters, you can manually encode them, e.g. { query: { 'foo[key1]': value1, 'foo[key2]': value2 } }, and please open a GitHub issue requesting better support for your use case.`);
  }).join("&");
}
function ZH(e) {
  let t = 0;
  for (const s of e) t += s.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const s of e)
    n.set(s, r), r += s.length;
  return n;
}
var qT;
function Fw(e) {
  let t;
  return (qT ?? (t = new globalThis.TextEncoder(), qT = t.encode.bind(t)))(e);
}
var HT;
function zT(e) {
  let t;
  return (HT ?? (t = new globalThis.TextDecoder(), HT = t.decode.bind(t)))(e);
}
var us, cs, zf = class {
  constructor() {
    us.set(this, void 0), cs.set(this, void 0), nt(this, us, new Uint8Array(), "f"), nt(this, cs, null, "f");
  }
  decode(e) {
    if (e == null) return [];
    const t = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? Fw(e) : e;
    nt(this, us, ZH([Ce(this, us, "f"), t]), "f");
    const n = [];
    let r;
    for (; (r = ez(Ce(this, us, "f"), Ce(this, cs, "f"))) != null; ) {
      if (r.carriage && Ce(this, cs, "f") == null) {
        nt(this, cs, r.index, "f");
        continue;
      }
      if (Ce(this, cs, "f") != null && (r.index !== Ce(this, cs, "f") + 1 || r.carriage)) {
        n.push(zT(Ce(this, us, "f").subarray(0, Ce(this, cs, "f") - 1))), nt(this, us, Ce(this, us, "f").subarray(Ce(this, cs, "f")), "f"), nt(this, cs, null, "f");
        continue;
      }
      const s = Ce(this, cs, "f") !== null ? r.preceding - 1 : r.preceding, a = zT(Ce(this, us, "f").subarray(0, s));
      n.push(a), nt(this, us, Ce(this, us, "f").subarray(r.index), "f"), nt(this, cs, null, "f");
    }
    return n;
  }
  flush() {
    return Ce(this, us, "f").length ? this.decode(`
`) : [];
  }
};
us = /* @__PURE__ */ new WeakMap(), cs = /* @__PURE__ */ new WeakMap();
zf.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
zf.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function ez(e, t) {
  for (let s = t ?? 0; s < e.length; s++) {
    if (e[s] === 10) return {
      preceding: s,
      index: s + 1,
      carriage: !1
    };
    if (e[s] === 13) return {
      preceding: s,
      index: s + 1,
      carriage: !0
    };
  }
  return null;
}
function tz(e) {
  for (let r = 0; r < e.length - 1; r++) {
    if (e[r] === 10 && e[r + 1] === 10 || e[r] === 13 && e[r + 1] === 13) return r + 2;
    if (e[r] === 13 && e[r + 1] === 10 && r + 3 < e.length && e[r + 2] === 13 && e[r + 3] === 10) return r + 4;
  }
  return -1;
}
var rg = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, KT = (e, t, n) => {
  if (e) {
    if (qH(rg, e)) return e;
    Or(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(rg))}`);
  }
};
function gd() {
}
function Yh(e, t, n) {
  return !t || rg[e] > rg[n] ? gd : t[e].bind(t);
}
var nz = {
  error: gd,
  warn: gd,
  info: gd,
  debug: gd
}, WT = /* @__PURE__ */ new WeakMap();
function Or(e) {
  const t = e.logger, n = e.logLevel ?? "off";
  if (!t) return nz;
  const r = WT.get(t);
  if (r && r[0] === n) return r[1];
  const s = {
    error: Yh("error", t, n),
    warn: Yh("warn", t, n),
    info: Yh("info", t, n),
    debug: Yh("debug", t, n)
  };
  return WT.set(t, [n, s]), s;
}
var ji = (e) => (e.options && (e.options = { ...e.options }, delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "x-api-key" || t.toLowerCase() === "authorization" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), qc, Ef = class vd {
  constructor(t, n, r) {
    this.iterator = t, qc.set(this, void 0), this.controller = n, nt(this, qc, r, "f");
  }
  static fromSSEResponse(t, n, r) {
    let s = !1;
    const a = r ? Or(r) : console;
    async function* o() {
      if (s) throw new xt("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      s = !0;
      let l = !1;
      try {
        for await (const i of rz(t, n)) {
          if (i.event === "completion") try {
            yield JSON.parse(i.data);
          } catch (u) {
            throw a.error("Could not parse message into JSON:", i.data), a.error("From chunk:", i.raw), u;
          }
          if (i.event === "message_start" || i.event === "message_delta" || i.event === "message_stop" || i.event === "content_block_start" || i.event === "content_block_delta" || i.event === "content_block_stop" || i.event === "message" || i.event === "user.message" || i.event === "user.interrupt" || i.event === "user.tool_confirmation" || i.event === "user.custom_tool_result" || i.event === "agent.message" || i.event === "agent.thinking" || i.event === "agent.tool_use" || i.event === "agent.tool_result" || i.event === "agent.mcp_tool_use" || i.event === "agent.mcp_tool_result" || i.event === "agent.custom_tool_use" || i.event === "agent.thread_context_compacted" || i.event === "session.status_running" || i.event === "session.status_idle" || i.event === "session.status_rescheduled" || i.event === "session.status_terminated" || i.event === "session.error" || i.event === "session.deleted" || i.event === "span.model_request_start" || i.event === "span.model_request_end") try {
            yield JSON.parse(i.data);
          } catch (u) {
            throw a.error("Could not parse message into JSON:", i.data), a.error("From chunk:", i.raw), u;
          }
          if (i.event !== "ping" && i.event === "error") {
            const u = f4(i.data) ?? i.data, d = u?.error?.type;
            throw new Is(void 0, u, void 0, t.headers, d);
          }
        }
        l = !0;
      } catch (i) {
        if (Tf(i)) return;
        throw i;
      } finally {
        l || n.abort();
      }
    }
    return new vd(o, n, r);
  }
  static fromReadableStream(t, n, r) {
    let s = !1;
    async function* a() {
      const l = new zf(), i = Ow(t);
      for await (const u of i) for (const d of l.decode(u)) yield d;
      for (const u of l.flush()) yield u;
    }
    async function* o() {
      if (s) throw new xt("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      s = !0;
      let l = !1;
      try {
        for await (const i of a())
          l || i && (yield JSON.parse(i));
        l = !0;
      } catch (i) {
        if (Tf(i)) return;
        throw i;
      } finally {
        l || n.abort();
      }
    }
    return new vd(o, n, r);
  }
  [(qc = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), s = (a) => ({ next: () => {
      if (a.length === 0) {
        const o = r.next();
        t.push(o), n.push(o);
      }
      return a.shift();
    } });
    return [new vd(() => s(t), this.controller, Ce(this, qc, "f")), new vd(() => s(n), this.controller, Ce(this, qc, "f"))];
  }
  toReadableStream() {
    const t = this;
    let n;
    return h4({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: s, done: a } = await n.next();
          if (a) return r.close();
          const o = Fw(JSON.stringify(s) + `
`);
          r.enqueue(o);
        } catch (s) {
          r.error(s);
        }
      },
      async cancel() {
        await n.return?.();
      }
    });
  }
};
async function* rz(e, t) {
  if (!e.body)
    throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new xt("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new xt("Attempted to iterate over a response with no body");
  const n = new az(), r = new zf(), s = Ow(e.body);
  for await (const a of sz(s)) for (const o of r.decode(a)) {
    const l = n.decode(o);
    l && (yield l);
  }
  for (const a of r.flush()) {
    const o = n.decode(a);
    o && (yield o);
  }
}
async function* sz(e) {
  let t = new Uint8Array();
  for await (const n of e) {
    if (n == null) continue;
    const r = n instanceof ArrayBuffer ? new Uint8Array(n) : typeof n == "string" ? Fw(n) : n;
    let s = new Uint8Array(t.length + r.length);
    s.set(t), s.set(r, t.length), t = s;
    let a;
    for (; (a = tz(t)) !== -1; )
      yield t.slice(0, a), t = t.slice(a);
  }
  t.length > 0 && (yield t);
}
var az = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const s = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], s;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = oz(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function oz(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function m4(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: s, startTime: a } = t, o = await (async () => {
    if (t.options.stream)
      return Or(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller) : Ef.fromSSEResponse(n, t.controller);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const l = n.headers.get("content-type")?.split(";")[0]?.trim();
    return l?.includes("application/json") || l?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : g4(await n.json(), n) : await n.text();
  })();
  return Or(e).debug(`[${r}] response parsed`, ji({
    retryOfRequestLogID: s,
    url: n.url,
    status: n.status,
    body: o,
    durationMs: Date.now() - a
  })), o;
}
function g4(e, t) {
  return !e || typeof e != "object" || Array.isArray(e) ? e : Object.defineProperty(e, "_request_id", {
    value: t.headers.get("request-id"),
    enumerable: !1
  });
}
var yd, v4 = class y4 extends Promise {
  constructor(t, n, r = m4) {
    super((s) => {
      s(null);
    }), this.responsePromise = n, this.parseResponse = r, yd.set(this, void 0), nt(this, yd, t, "f");
  }
  _thenUnwrap(t) {
    return new y4(Ce(this, yd, "f"), this.responsePromise, async (n, r) => g4(t(await this.parseResponse(n, r), r), r.response));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n,
      request_id: n.headers.get("request-id")
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(Ce(this, yd, "f"), t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
};
yd = /* @__PURE__ */ new WeakMap();
var Xh, _4 = class {
  constructor(e, t, n, r) {
    Xh.set(this, void 0), nt(this, Xh, e, "f"), this.options = r, this.response = t, this.body = n;
  }
  hasNextPage() {
    return this.getPaginatedItems().length ? this.nextPageRequestOptions() != null : !1;
  }
  async getNextPage() {
    const e = this.nextPageRequestOptions();
    if (!e) throw new xt("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
    return await Ce(this, Xh, "f").requestAPIList(this.constructor, e);
  }
  async *iterPages() {
    let e = this;
    for (yield e; e.hasNextPage(); )
      e = await e.getNextPage(), yield e;
  }
  async *[(Xh = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    for await (const e of this.iterPages()) for (const t of e.getPaginatedItems()) yield t;
  }
}, iz = class extends v4 {
  constructor(e, t, n) {
    super(e, t, async (r, s) => new n(r, s.response, await m4(r, s), s.options));
  }
  async *[Symbol.asyncIterator]() {
    const e = await this;
    for await (const t of e) yield t;
  }
}, Kf = class extends _4 {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1, this.first_id = n.first_id || null, this.last_id = n.last_id || null;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    if (this.options.query?.before_id) {
      const t = this.first_id;
      return t ? {
        ...this.options,
        query: {
          ...s0(this.options.query),
          before_id: t
        }
      } : null;
    }
    const e = this.last_id;
    return e ? {
      ...this.options,
      query: {
        ...s0(this.options.query),
        after_id: e
      }
    } : null;
  }
}, ts = class extends _4 {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.next_page = n.next_page || null;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  nextPageRequestOptions() {
    const e = this.next_page;
    return e ? {
      ...this.options,
      query: {
        ...s0(this.options.query),
        page: e
      }
    } : null;
  }
}, b4 = () => {
  if (typeof File > "u") {
    const { process: e } = globalThis, t = typeof e?.versions?.node == "string" && parseInt(e.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (t ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function Lu(e, t, n) {
  return b4(), new File(e, t ?? "unknown_file", n);
}
function Gp(e, t) {
  const n = typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "";
  return t ? n.split(/[\\/]/).pop() || void 0 : n;
}
var w4 = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", Bw = async (e, t, n = !0) => ({
  ...e,
  body: await uz(e.body, t, n)
}), GT = /* @__PURE__ */ new WeakMap();
function lz(e) {
  const t = typeof e == "function" ? e : e.fetch, n = GT.get(t);
  if (n) return n;
  const r = (async () => {
    try {
      const s = "Response" in t ? t.Response : (await t("data:,")).constructor, a = new FormData();
      return a.toString() !== await new s(a).text();
    } catch {
      return !0;
    }
  })();
  return GT.set(t, r), r;
}
var uz = async (e, t, n = !0) => {
  if (!await lz(t)) throw new TypeError("The provided fetch function does not support file uploads with the current global FormData class.");
  const r = new FormData();
  return await Promise.all(Object.entries(e || {}).map(([s, a]) => a0(r, s, a, n))), r;
}, cz = (e) => e instanceof Blob && "name" in e, a0 = async (e, t, n, r) => {
  if (n !== void 0) {
    if (n == null) throw new TypeError(`Received null for "${t}"; to pass null in FormData, you must use the string 'null'`);
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") e.append(t, String(n));
    else if (n instanceof Response) {
      let s = {};
      const a = n.headers.get("Content-Type");
      a && (s = { type: a }), e.append(t, Lu([await n.blob()], Gp(n, r), s));
    } else if (w4(n)) e.append(t, Lu([await new Response(p4(n)).blob()], Gp(n, r)));
    else if (cz(n)) e.append(t, Lu([n], Gp(n, r), { type: n.type }));
    else if (Array.isArray(n)) await Promise.all(n.map((s) => a0(e, t + "[]", s, r)));
    else if (typeof n == "object") await Promise.all(Object.entries(n).map(([s, a]) => a0(e, `${t}[${s}]`, a, r)));
    else throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${n} instead`);
  }
}, S4 = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", dz = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && S4(e), fz = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function hz(e, t, n) {
  if (b4(), e = await e, t || (t = Gp(e, !0)), dz(e))
    return e instanceof File && t == null && n == null ? e : Lu([await e.arrayBuffer()], t ?? e.name, {
      type: e.type,
      lastModified: e.lastModified,
      ...n
    });
  if (fz(e)) {
    const s = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), Lu(await o0(s), t, n);
  }
  const r = await o0(e);
  if (!n?.type) {
    const s = r.find((a) => typeof a == "object" && "type" in a && a.type);
    typeof s == "string" && (n = {
      ...n,
      type: s
    });
  }
  return Lu(r, t, n);
}
async function o0(e) {
  let t = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) t.push(e);
  else if (S4(e)) t.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (w4(e)) for await (const n of e) t.push(...await o0(n));
  else {
    const n = e?.constructor?.name;
    throw new Error(`Unexpected data type: ${typeof e}${n ? `; constructor: ${n}` : ""}${pz(e)}`);
  }
  return t;
}
function pz(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var Pn = class {
  constructor(e) {
    this._client = e;
  }
}, A4 = /* @__PURE__ */ Symbol.for("brand.privateNullableHeaders");
function* mz(e) {
  if (!e) return;
  if (A4 in e) {
    const { values: r, nulls: s } = e;
    yield* r.entries();
    for (const a of s) yield [a, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : DT(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const s = r[0];
    if (typeof s != "string") throw new TypeError("expected header name to be a string");
    const a = DT(r[1]) ? r[1] : [r[1]];
    let o = !1;
    for (const l of a)
      l !== void 0 && (t && !o && (o = !0, yield [s, null]), yield [s, l]);
  }
}
var Ge = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const s = /* @__PURE__ */ new Set();
    for (const [a, o] of mz(r)) {
      const l = a.toLowerCase();
      s.has(l) || (t.delete(a), s.add(l)), o === null ? (t.delete(a), n.add(l)) : (t.append(a, o), n.delete(l));
    }
  }
  return {
    [A4]: !0,
    values: t,
    nulls: n
  };
};
function T4(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var VT = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), gz = (e = T4) => function(n, ...r) {
  if (n.length === 1) return n[0];
  let s = !1;
  const a = [], o = n.reduce((d, f, m) => {
    /[?#]/.test(f) && (s = !0);
    const v = r[m];
    let y = (s ? encodeURIComponent : e)("" + v);
    return m !== r.length && (v == null || typeof v == "object" && v.toString === Object.getPrototypeOf(Object.getPrototypeOf(v.hasOwnProperty ?? VT) ?? VT)?.toString) && (y = v + "", a.push({
      start: d.length + f.length,
      length: y.length,
      error: `Value of type ${Object.prototype.toString.call(v).slice(8, -1)} is not a valid path parameter`
    })), d + f + (m === r.length ? "" : y);
  }, ""), l = o.split(/[?#]/, 1)[0], i = /(?<=^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = i.exec(l)) !== null; ) a.push({
    start: u.index,
    length: u[0].length,
    error: `Value "${u[0]}" can't be safely passed as a path parameter`
  });
  if (a.sort((d, f) => d.start - f.start), a.length > 0) {
    let d = 0;
    const f = a.reduce((m, v) => {
      const y = " ".repeat(v.start - d), S = "^".repeat(v.length);
      return d = v.start + v.length, m + y + S;
    }, "");
    throw new xt(`Path parameters result in path with invalid segments:
${a.map((m) => m.error).join(`
`)}
${o}
${f}`);
  }
  return o;
}, ft = /* @__PURE__ */ gz(T4), E4 = class extends Pn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/environments?beta=true", {
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/environments/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/environments/${e}?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/environments?beta=true", ts, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(ft`/v1/environments/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(ft`/v1/environments/${e}/archive?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, Gd = /* @__PURE__ */ Symbol("anthropic.sdk.stainlessHelper");
function Vp(e) {
  return typeof e == "object" && e !== null && Gd in e;
}
function C4(e, t) {
  const n = /* @__PURE__ */ new Set();
  if (e)
    for (const r of e) Vp(r) && n.add(r[Gd]);
  if (t) {
    for (const r of t)
      if (Vp(r) && n.add(r[Gd]), Array.isArray(r.content))
        for (const s of r.content) Vp(s) && n.add(s[Gd]);
  }
  return Array.from(n);
}
function I4(e, t) {
  const n = C4(e, t);
  return n.length === 0 ? {} : { "x-stainless-helper": n.join(", ") };
}
function vz(e) {
  return Vp(e) ? { "x-stainless-helper": e[Gd] } : {};
}
var x4 = class extends Pn {
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/files?beta=true", Kf, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "files-api-2025-04-14"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(ft`/v1/files/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString() }, n?.headers])
    });
  }
  download(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/files/${e}/content?beta=true`, {
      ...n,
      headers: Ge([{
        "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString(),
        Accept: "application/binary"
      }, n?.headers]),
      __binaryResponse: !0
    });
  }
  retrieveMetadata(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/files/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString() }, n?.headers])
    });
  }
  upload(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/files?beta=true", Bw({
      body: r,
      ...t,
      headers: Ge([
        { "anthropic-beta": [...n ?? [], "files-api-2025-04-14"].toString() },
        vz(r.file),
        t?.headers
      ])
    }, this._client));
  }
}, k4 = class extends Pn {
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/models/${e}?beta=true`, {
      ...n,
      headers: Ge([{ ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/models?beta=true", Kf, {
      query: r,
      ...t,
      headers: Ge([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers])
    });
  }
}, M4 = class extends Pn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/user_profiles?beta=true", {
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "user-profiles-2026-03-24"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/user_profiles/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "user-profiles-2026-03-24"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/user_profiles/${e}?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "user-profiles-2026-03-24"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/user_profiles?beta=true", ts, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "user-profiles-2026-03-24"].toString() }, t?.headers])
    });
  }
  createEnrollmentURL(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(ft`/v1/user_profiles/${e}/enrollment_url?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "user-profiles-2026-03-24"].toString() }, n?.headers])
    });
  }
}, P4 = class extends Pn {
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(ft`/v1/agents/${e}/versions?beta=true`, ts, {
      query: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, Uw = class extends Pn {
  constructor() {
    super(...arguments), this.versions = new P4(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/agents?beta=true", {
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.get(ft`/v1/agents/${e}?beta=true`, {
      query: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/agents/${e}?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/agents?beta=true", ts, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(ft`/v1/agents/${e}/archive?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
Uw.Versions = P4;
var R4 = class extends Pn {
  create(e, t, n) {
    const { view: r, betas: s, ...a } = t;
    return this._client.post(ft`/v1/memory_stores/${e}/memories?beta=true`, {
      query: { view: r },
      body: a,
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { memory_store_id: r, betas: s, ...a } = t;
    return this._client.get(ft`/v1/memory_stores/${r}/memories/${e}?beta=true`, {
      query: a,
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { memory_store_id: r, view: s, betas: a, ...o } = t;
    return this._client.post(ft`/v1/memory_stores/${r}/memories/${e}?beta=true`, {
      query: { view: s },
      body: o,
      ...n,
      headers: Ge([{ "anthropic-beta": [...a ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(ft`/v1/memory_stores/${e}/memories?beta=true`, ts, {
      query: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { memory_store_id: r, expected_content_sha256: s, betas: a } = t;
    return this._client.delete(ft`/v1/memory_stores/${r}/memories/${e}?beta=true`, {
      query: { expected_content_sha256: s },
      ...n,
      headers: Ge([{ "anthropic-beta": [...a ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, N4 = class extends Pn {
  retrieve(e, t, n) {
    const { memory_store_id: r, betas: s, ...a } = t;
    return this._client.get(ft`/v1/memory_stores/${r}/memory_versions/${e}?beta=true`, {
      query: a,
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(ft`/v1/memory_stores/${e}/memory_versions?beta=true`, ts, {
      query: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  redact(e, t, n) {
    const { memory_store_id: r, betas: s } = t;
    return this._client.post(ft`/v1/memory_stores/${r}/memory_versions/${e}/redact?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, lv = class extends Pn {
  constructor() {
    super(...arguments), this.memories = new R4(this._client), this.memoryVersions = new N4(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/memory_stores?beta=true", {
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/memory_stores/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/memory_stores/${e}?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/memory_stores?beta=true", ts, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(ft`/v1/memory_stores/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(ft`/v1/memory_stores/${e}/archive?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
lv.Memories = R4;
lv.MemoryVersions = N4;
var $4 = {
  "claude-opus-4-20250514": 8192,
  "claude-opus-4-0": 8192,
  "claude-4-opus-20250514": 8192,
  "anthropic.claude-opus-4-20250514-v1:0": 8192,
  "claude-opus-4@20250514": 8192,
  "claude-opus-4-1-20250805": 8192,
  "anthropic.claude-opus-4-1-20250805-v1:0": 8192,
  "claude-opus-4-1@20250805": 8192
};
function L4(e) {
  return e?.output_format ?? e?.output_config?.format;
}
function JT(e, t, n) {
  const r = L4(t);
  return !t || !("parse" in (r ?? {})) ? {
    ...e,
    content: e.content.map((s) => {
      if (s.type === "text") {
        const a = Object.defineProperty({ ...s }, "parsed_output", {
          value: null,
          enumerable: !1
        });
        return Object.defineProperty(a, "parsed", {
          get() {
            return n.logger.warn("The `parsed` property on `text` blocks is deprecated, please use `parsed_output` instead."), null;
          },
          enumerable: !1
        });
      }
      return s;
    }),
    parsed_output: null
  } : D4(e, t, n);
}
function D4(e, t, n) {
  let r = null;
  const s = e.content.map((a) => {
    if (a.type === "text") {
      const o = yz(t, a.text);
      r === null && (r = o);
      const l = Object.defineProperty({ ...a }, "parsed_output", {
        value: o,
        enumerable: !1
      });
      return Object.defineProperty(l, "parsed", {
        get() {
          return n.logger.warn("The `parsed` property on `text` blocks is deprecated, please use `parsed_output` instead."), o;
        },
        enumerable: !1
      });
    }
    return a;
  });
  return {
    ...e,
    content: s,
    parsed_output: r
  };
}
function yz(e, t) {
  const n = L4(e);
  if (n?.type !== "json_schema") return null;
  try {
    return "parse" in n ? n.parse(t) : JSON.parse(t);
  } catch (r) {
    throw new xt(`Failed to parse structured output: ${r}`);
  }
}
var _z = (e) => {
  let t = 0, n = [];
  for (; t < e.length; ) {
    let r = e[t];
    if (r === "\\") {
      t++;
      continue;
    }
    if (r === "{") {
      n.push({
        type: "brace",
        value: "{"
      }), t++;
      continue;
    }
    if (r === "}") {
      n.push({
        type: "brace",
        value: "}"
      }), t++;
      continue;
    }
    if (r === "[") {
      n.push({
        type: "paren",
        value: "["
      }), t++;
      continue;
    }
    if (r === "]") {
      n.push({
        type: "paren",
        value: "]"
      }), t++;
      continue;
    }
    if (r === ":") {
      n.push({
        type: "separator",
        value: ":"
      }), t++;
      continue;
    }
    if (r === ",") {
      n.push({
        type: "delimiter",
        value: ","
      }), t++;
      continue;
    }
    if (r === '"') {
      let o = "", l = !1;
      for (r = e[++t]; r !== '"'; ) {
        if (t === e.length) {
          l = !0;
          break;
        }
        if (r === "\\") {
          if (t++, t === e.length) {
            l = !0;
            break;
          }
          o += r + e[t], r = e[++t];
        } else
          o += r, r = e[++t];
      }
      r = e[++t], l || n.push({
        type: "string",
        value: o
      });
      continue;
    }
    if (r && /\s/.test(r)) {
      t++;
      continue;
    }
    let s = /[0-9]/;
    if (r && s.test(r) || r === "-" || r === ".") {
      let o = "";
      for (r === "-" && (o += r, r = e[++t]); r && s.test(r) || r === "."; )
        o += r, r = e[++t];
      n.push({
        type: "number",
        value: o
      });
      continue;
    }
    let a = /[a-z]/i;
    if (r && a.test(r)) {
      let o = "";
      for (; r && a.test(r) && t !== e.length; )
        o += r, r = e[++t];
      if (o == "true" || o == "false" || o === "null") n.push({
        type: "name",
        value: o
      });
      else {
        t++;
        continue;
      }
      continue;
    }
    t++;
  }
  return n;
}, hu = (e) => {
  if (e.length === 0) return e;
  let t = e[e.length - 1];
  switch (t.type) {
    case "separator":
      return e = e.slice(0, e.length - 1), hu(e);
    case "number":
      let n = t.value[t.value.length - 1];
      if (n === "." || n === "-")
        return e = e.slice(0, e.length - 1), hu(e);
    case "string":
      let r = e[e.length - 2];
      if (r?.type === "delimiter")
        return e = e.slice(0, e.length - 1), hu(e);
      if (r?.type === "brace" && r.value === "{")
        return e = e.slice(0, e.length - 1), hu(e);
      break;
    case "delimiter":
      return e = e.slice(0, e.length - 1), hu(e);
  }
  return e;
}, bz = (e) => {
  let t = [];
  return e.map((n) => {
    n.type === "brace" && (n.value === "{" ? t.push("}") : t.splice(t.lastIndexOf("}"), 1)), n.type === "paren" && (n.value === "[" ? t.push("]") : t.splice(t.lastIndexOf("]"), 1));
  }), t.length > 0 && t.reverse().map((n) => {
    n === "}" ? e.push({
      type: "brace",
      value: "}"
    }) : n === "]" && e.push({
      type: "paren",
      value: "]"
    });
  }), e;
}, wz = (e) => {
  let t = "";
  return e.map((n) => {
    n.type === "string" ? t += '"' + n.value + '"' : t += n.value;
  }), t;
}, O4 = (e) => JSON.parse(wz(bz(hu(_z(e))))), Ns, qo, eu, Hc, jh, zc, Kc, Qh, Wc, to, Gc, Zh, ep, Gi, tp, np, Vc, l_, YT, rp, u_, c_, d_, XT, jT = "__json_buf";
function QT(e) {
  return e.type === "tool_use" || e.type === "server_tool_use" || e.type === "mcp_tool_use";
}
var Sz = class i0 {
  constructor(t, n) {
    Ns.add(this), this.messages = [], this.receivedMessages = [], qo.set(this, void 0), eu.set(this, null), this.controller = new AbortController(), Hc.set(this, void 0), jh.set(this, () => {
    }), zc.set(this, () => {
    }), Kc.set(this, void 0), Qh.set(this, () => {
    }), Wc.set(this, () => {
    }), to.set(this, {}), Gc.set(this, !1), Zh.set(this, !1), ep.set(this, !1), Gi.set(this, !1), tp.set(this, void 0), np.set(this, void 0), Vc.set(this, void 0), rp.set(this, (r) => {
      if (nt(this, Zh, !0, "f"), Tf(r) && (r = new ta()), r instanceof ta)
        return nt(this, ep, !0, "f"), this._emit("abort", r);
      if (r instanceof xt) return this._emit("error", r);
      if (r instanceof Error) {
        const s = new xt(r.message);
        return s.cause = r, this._emit("error", s);
      }
      return this._emit("error", new xt(String(r)));
    }), nt(this, Hc, new Promise((r, s) => {
      nt(this, jh, r, "f"), nt(this, zc, s, "f");
    }), "f"), nt(this, Kc, new Promise((r, s) => {
      nt(this, Qh, r, "f"), nt(this, Wc, s, "f");
    }), "f"), Ce(this, Hc, "f").catch(() => {
    }), Ce(this, Kc, "f").catch(() => {
    }), nt(this, eu, t, "f"), nt(this, Vc, n?.logger ?? console, "f");
  }
  get response() {
    return Ce(this, tp, "f");
  }
  get request_id() {
    return Ce(this, np, "f");
  }
  async withResponse() {
    nt(this, Gi, !0, "f");
    const t = await Ce(this, Hc, "f");
    if (!t) throw new Error("Could not resolve a `Response` object");
    return {
      data: this,
      response: t,
      request_id: t.headers.get("request-id")
    };
  }
  static fromReadableStream(t) {
    const n = new i0(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createMessage(t, n, r, { logger: s } = {}) {
    const a = new i0(n, { logger: s });
    for (const o of n.messages) a._addMessageParam(o);
    return nt(a, eu, {
      ...n,
      stream: !0
    }, "f"), a._run(() => a._createMessage(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), a;
  }
  _run(t) {
    t().then(() => {
      this._emitFinal(), this._emit("end");
    }, Ce(this, rp, "f"));
  }
  _addMessageParam(t) {
    this.messages.push(t);
  }
  _addMessage(t, n = !0) {
    this.receivedMessages.push(t), n && this._emit("message", t);
  }
  async _createMessage(t, n, r) {
    const s = r?.signal;
    let a;
    s && (s.aborted && this.controller.abort(), a = this.controller.abort.bind(this.controller), s.addEventListener("abort", a));
    try {
      Ce(this, Ns, "m", u_).call(this);
      const { response: o, data: l } = await t.create({
        ...n,
        stream: !0
      }, {
        ...r,
        signal: this.controller.signal
      }).withResponse();
      this._connected(o);
      for await (const i of l) Ce(this, Ns, "m", c_).call(this, i);
      if (l.controller.signal?.aborted) throw new ta();
      Ce(this, Ns, "m", d_).call(this);
    } finally {
      s && a && s.removeEventListener("abort", a);
    }
  }
  _connected(t) {
    this.ended || (nt(this, tp, t, "f"), nt(this, np, t?.headers.get("request-id"), "f"), Ce(this, jh, "f").call(this, t), this._emit("connect"));
  }
  get ended() {
    return Ce(this, Gc, "f");
  }
  get errored() {
    return Ce(this, Zh, "f");
  }
  get aborted() {
    return Ce(this, ep, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(t, n) {
    return (Ce(this, to, "f")[t] || (Ce(this, to, "f")[t] = [])).push({ listener: n }), this;
  }
  off(t, n) {
    const r = Ce(this, to, "f")[t];
    if (!r) return this;
    const s = r.findIndex((a) => a.listener === n);
    return s >= 0 && r.splice(s, 1), this;
  }
  once(t, n) {
    return (Ce(this, to, "f")[t] || (Ce(this, to, "f")[t] = [])).push({
      listener: n,
      once: !0
    }), this;
  }
  emitted(t) {
    return new Promise((n, r) => {
      nt(this, Gi, !0, "f"), t !== "error" && this.once("error", r), this.once(t, n);
    });
  }
  async done() {
    nt(this, Gi, !0, "f"), await Ce(this, Kc, "f");
  }
  get currentMessage() {
    return Ce(this, qo, "f");
  }
  async finalMessage() {
    return await this.done(), Ce(this, Ns, "m", l_).call(this);
  }
  async finalText() {
    return await this.done(), Ce(this, Ns, "m", YT).call(this);
  }
  _emit(t, ...n) {
    if (Ce(this, Gc, "f")) return;
    t === "end" && (nt(this, Gc, !0, "f"), Ce(this, Qh, "f").call(this));
    const r = Ce(this, to, "f")[t];
    if (r && (Ce(this, to, "f")[t] = r.filter((s) => !s.once), r.forEach(({ listener: s }) => s(...n))), t === "abort") {
      const s = n[0];
      !Ce(this, Gi, "f") && !r?.length && Promise.reject(s), Ce(this, zc, "f").call(this, s), Ce(this, Wc, "f").call(this, s), this._emit("end");
      return;
    }
    if (t === "error") {
      const s = n[0];
      !Ce(this, Gi, "f") && !r?.length && Promise.reject(s), Ce(this, zc, "f").call(this, s), Ce(this, Wc, "f").call(this, s), this._emit("end");
    }
  }
  _emitFinal() {
    this.receivedMessages.at(-1) && this._emit("finalMessage", Ce(this, Ns, "m", l_).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    let s;
    r && (r.aborted && this.controller.abort(), s = this.controller.abort.bind(this.controller), r.addEventListener("abort", s));
    try {
      Ce(this, Ns, "m", u_).call(this), this._connected(null);
      const a = Ef.fromReadableStream(t, this.controller);
      for await (const o of a) Ce(this, Ns, "m", c_).call(this, o);
      if (a.controller.signal?.aborted) throw new ta();
      Ce(this, Ns, "m", d_).call(this);
    } finally {
      r && s && r.removeEventListener("abort", s);
    }
  }
  [(qo = /* @__PURE__ */ new WeakMap(), eu = /* @__PURE__ */ new WeakMap(), Hc = /* @__PURE__ */ new WeakMap(), jh = /* @__PURE__ */ new WeakMap(), zc = /* @__PURE__ */ new WeakMap(), Kc = /* @__PURE__ */ new WeakMap(), Qh = /* @__PURE__ */ new WeakMap(), Wc = /* @__PURE__ */ new WeakMap(), to = /* @__PURE__ */ new WeakMap(), Gc = /* @__PURE__ */ new WeakMap(), Zh = /* @__PURE__ */ new WeakMap(), ep = /* @__PURE__ */ new WeakMap(), Gi = /* @__PURE__ */ new WeakMap(), tp = /* @__PURE__ */ new WeakMap(), np = /* @__PURE__ */ new WeakMap(), Vc = /* @__PURE__ */ new WeakMap(), rp = /* @__PURE__ */ new WeakMap(), Ns = /* @__PURE__ */ new WeakSet(), l_ = function() {
    if (this.receivedMessages.length === 0) throw new xt("stream ended without producing a Message with role=assistant");
    return this.receivedMessages.at(-1);
  }, YT = function() {
    if (this.receivedMessages.length === 0) throw new xt("stream ended without producing a Message with role=assistant");
    const n = this.receivedMessages.at(-1).content.filter((r) => r.type === "text").map((r) => r.text);
    if (n.length === 0) throw new xt("stream ended without producing a content block with type=text");
    return n.join(" ");
  }, u_ = function() {
    this.ended || nt(this, qo, void 0, "f");
  }, c_ = function(n) {
    if (this.ended) return;
    const r = Ce(this, Ns, "m", XT).call(this, n);
    switch (this._emit("streamEvent", n, r), n.type) {
      case "content_block_delta": {
        const s = r.content.at(-1);
        switch (n.delta.type) {
          case "text_delta":
            s.type === "text" && this._emit("text", n.delta.text, s.text || "");
            break;
          case "citations_delta":
            s.type === "text" && this._emit("citation", n.delta.citation, s.citations ?? []);
            break;
          case "input_json_delta":
            QT(s) && s.input && this._emit("inputJson", n.delta.partial_json, s.input);
            break;
          case "thinking_delta":
            s.type === "thinking" && this._emit("thinking", n.delta.thinking, s.thinking);
            break;
          case "signature_delta":
            s.type === "thinking" && this._emit("signature", s.signature);
            break;
          case "compaction_delta":
            s.type === "compaction" && s.content && this._emit("compaction", s.content);
            break;
          default:
            n.delta;
        }
        break;
      }
      case "message_stop":
        this._addMessageParam(r), this._addMessage(JT(r, Ce(this, eu, "f"), { logger: Ce(this, Vc, "f") }), !0);
        break;
      case "content_block_stop":
        this._emit("contentBlock", r.content.at(-1));
        break;
      case "message_start":
        nt(this, qo, r, "f");
        break;
      case "content_block_start":
      case "message_delta":
        break;
    }
  }, d_ = function() {
    if (this.ended) throw new xt("stream has ended, this shouldn't happen");
    const n = Ce(this, qo, "f");
    if (!n) throw new xt("request ended without sending any chunks");
    return nt(this, qo, void 0, "f"), JT(n, Ce(this, eu, "f"), { logger: Ce(this, Vc, "f") });
  }, XT = function(n) {
    let r = Ce(this, qo, "f");
    if (n.type === "message_start") {
      if (r) throw new xt(`Unexpected event order, got ${n.type} before receiving "message_stop"`);
      return n.message;
    }
    if (!r) throw new xt(`Unexpected event order, got ${n.type} before "message_start"`);
    switch (n.type) {
      case "message_stop":
        return r;
      case "message_delta":
        return r.container = n.delta.container, r.stop_reason = n.delta.stop_reason, r.stop_sequence = n.delta.stop_sequence, r.usage.output_tokens = n.usage.output_tokens, r.context_management = n.context_management, n.usage.input_tokens != null && (r.usage.input_tokens = n.usage.input_tokens), n.usage.cache_creation_input_tokens != null && (r.usage.cache_creation_input_tokens = n.usage.cache_creation_input_tokens), n.usage.cache_read_input_tokens != null && (r.usage.cache_read_input_tokens = n.usage.cache_read_input_tokens), n.usage.server_tool_use != null && (r.usage.server_tool_use = n.usage.server_tool_use), n.usage.iterations != null && (r.usage.iterations = n.usage.iterations), r;
      case "content_block_start":
        return r.content.push(n.content_block), r;
      case "content_block_delta": {
        const s = r.content.at(n.index);
        switch (n.delta.type) {
          case "text_delta":
            s?.type === "text" && (r.content[n.index] = {
              ...s,
              text: (s.text || "") + n.delta.text
            });
            break;
          case "citations_delta":
            s?.type === "text" && (r.content[n.index] = {
              ...s,
              citations: [...s.citations ?? [], n.delta.citation]
            });
            break;
          case "input_json_delta":
            if (s && QT(s)) {
              let a = s[jT] || "";
              a += n.delta.partial_json;
              const o = { ...s };
              if (Object.defineProperty(o, jT, {
                value: a,
                enumerable: !1,
                writable: !0
              }), a) try {
                o.input = O4(a);
              } catch (l) {
                const i = new xt(`Unable to parse tool parameter JSON from model. Please retry your request or adjust your prompt. Error: ${l}. JSON: ${a}`);
                Ce(this, rp, "f").call(this, i);
              }
              r.content[n.index] = o;
            }
            break;
          case "thinking_delta":
            s?.type === "thinking" && (r.content[n.index] = {
              ...s,
              thinking: s.thinking + n.delta.thinking
            });
            break;
          case "signature_delta":
            s?.type === "thinking" && (r.content[n.index] = {
              ...s,
              signature: n.delta.signature
            });
            break;
          case "compaction_delta":
            s?.type === "compaction" && (r.content[n.index] = {
              ...s,
              content: (s.content || "") + n.delta.content
            });
            break;
          default:
            n.delta;
        }
        return r;
      }
      case "content_block_stop":
        return r;
    }
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("streamEvent", (s) => {
      const a = n.shift();
      a ? a.resolve(s) : t.push(s);
    }), this.on("end", () => {
      r = !0;
      for (const s of n) s.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (s) => {
      r = !0;
      for (const a of n) a.reject(s);
      n.length = 0;
    }), this.on("error", (s) => {
      r = !0;
      for (const a of n) a.reject(s);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((s, a) => n.push({
        resolve: s,
        reject: a
      })).then((s) => s ? {
        value: s,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new Ef(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
}, F4 = class extends Error {
  constructor(e) {
    const t = typeof e == "string" ? e : e.map((n) => n.type === "text" ? n.text : `[${n.type}]`).join(" ");
    super(t), this.name = "ToolError", this.content = e;
  }
};
var Az = `You have been working on the task described above but have not yet completed it. Write a continuation summary that will allow you (or another instance of yourself) to resume work efficiently in a future context window where the conversation history will be replaced with this summary. Your summary should be structured, concise, and actionable. Include:
1. Task Overview
The user's core request and success criteria
Any clarifications or constraints they specified
2. Current State
What has been completed so far
Files created, modified, or analyzed (with paths if relevant)
Key outputs or artifacts produced
3. Important Discoveries
Technical constraints or requirements uncovered
Decisions made and their rationale
Errors encountered and how they were resolved
What approaches were tried that didn't work (and why)
4. Next Steps
Specific actions needed to complete the task
Any blockers or open questions to resolve
Priority order if multiple steps remain
5. Context to Preserve
User preferences or style requirements
Domain-specific details that aren't obvious
Any promises made to the user
Be concise but complete—err on the side of including information that would prevent duplicate work or repeated mistakes. Write in a way that enables immediate resumption of the task.
Wrap your summary in <summary></summary> tags.`, Jc, tu, Vi, Qn, Vr, is, vo, Ho, Yc, ZT, l0;
function eE() {
  let e, t;
  return {
    promise: new Promise((n, r) => {
      e = n, t = r;
    }),
    resolve: e,
    reject: t
  };
}
var B4 = class {
  constructor(e, t, n) {
    Jc.add(this), this.client = e, tu.set(this, !1), Vi.set(this, !1), Qn.set(this, void 0), Vr.set(this, void 0), is.set(this, void 0), vo.set(this, void 0), Ho.set(this, void 0), Yc.set(this, 0), nt(this, Qn, { params: {
      ...t,
      messages: structuredClone(t.messages)
    } }, "f");
    const r = ["BetaToolRunner", ...C4(t.tools, t.messages)].join(", ");
    nt(this, Vr, {
      ...n,
      headers: Ge([{ "x-stainless-helper": r }, n?.headers])
    }, "f"), nt(this, Ho, eE(), "f"), t.compactionControl?.enabled && console.warn('Anthropic: The `compactionControl` parameter is deprecated and will be removed in a future version. Use server-side compaction instead by passing `edits: [{ type: "compact_20260112" }]` in the params passed to `toolRunner()`. See https://platform.claude.com/docs/en/build-with-claude/compaction');
  }
  async *[(tu = /* @__PURE__ */ new WeakMap(), Vi = /* @__PURE__ */ new WeakMap(), Qn = /* @__PURE__ */ new WeakMap(), Vr = /* @__PURE__ */ new WeakMap(), is = /* @__PURE__ */ new WeakMap(), vo = /* @__PURE__ */ new WeakMap(), Ho = /* @__PURE__ */ new WeakMap(), Yc = /* @__PURE__ */ new WeakMap(), Jc = /* @__PURE__ */ new WeakSet(), ZT = async function() {
    const t = Ce(this, Qn, "f").params.compactionControl;
    if (!t || !t.enabled) return !1;
    let n = 0;
    if (Ce(this, is, "f") !== void 0) try {
      const i = await Ce(this, is, "f");
      n = i.usage.input_tokens + (i.usage.cache_creation_input_tokens ?? 0) + (i.usage.cache_read_input_tokens ?? 0) + i.usage.output_tokens;
    } catch {
      return !1;
    }
    const r = t.contextTokenThreshold ?? 1e5;
    if (n < r) return !1;
    const s = t.model ?? Ce(this, Qn, "f").params.model, a = t.summaryPrompt ?? Az, o = Ce(this, Qn, "f").params.messages;
    if (o[o.length - 1].role === "assistant") {
      const i = o[o.length - 1];
      if (Array.isArray(i.content)) {
        const u = i.content.filter((d) => d.type !== "tool_use");
        u.length === 0 ? o.pop() : i.content = u;
      }
    }
    const l = await this.client.beta.messages.create({
      model: s,
      messages: [...o, {
        role: "user",
        content: [{
          type: "text",
          text: a
        }]
      }],
      max_tokens: Ce(this, Qn, "f").params.max_tokens
    }, {
      signal: Ce(this, Vr, "f").signal,
      headers: Ge([Ce(this, Vr, "f").headers, { "x-stainless-helper": "compaction" }])
    });
    if (l.content[0]?.type !== "text") throw new xt("Expected text response for compaction");
    return Ce(this, Qn, "f").params.messages = [{
      role: "user",
      content: l.content
    }], !0;
  }, Symbol.asyncIterator)]() {
    var e;
    if (Ce(this, tu, "f")) throw new xt("Cannot iterate over a consumed stream");
    nt(this, tu, !0, "f"), nt(this, Vi, !0, "f"), nt(this, vo, void 0, "f");
    try {
      for (; ; ) {
        let t;
        try {
          if (Ce(this, Qn, "f").params.max_iterations && Ce(this, Yc, "f") >= Ce(this, Qn, "f").params.max_iterations) break;
          nt(this, Vi, !1, "f"), nt(this, vo, void 0, "f"), nt(this, Yc, (e = Ce(this, Yc, "f"), e++, e), "f"), nt(this, is, void 0, "f");
          const { max_iterations: n, compactionControl: r, ...s } = Ce(this, Qn, "f").params;
          if (s.stream ? (t = this.client.beta.messages.stream({ ...s }, Ce(this, Vr, "f")), nt(this, is, t.finalMessage(), "f"), Ce(this, is, "f").catch(() => {
          }), yield t) : (nt(this, is, this.client.beta.messages.create({
            ...s,
            stream: !1
          }, Ce(this, Vr, "f")), "f"), yield Ce(this, is, "f")), !await Ce(this, Jc, "m", ZT).call(this)) {
            if (!Ce(this, Vi, "f")) {
              const { role: o, content: l } = await Ce(this, is, "f");
              Ce(this, Qn, "f").params.messages.push({
                role: o,
                content: l
              });
            }
            const a = await Ce(this, Jc, "m", l0).call(this, Ce(this, Qn, "f").params.messages.at(-1));
            if (a) Ce(this, Qn, "f").params.messages.push(a);
            else if (!Ce(this, Vi, "f")) break;
          }
        } finally {
          t && t.abort();
        }
      }
      if (!Ce(this, is, "f")) throw new xt("ToolRunner concluded without a message from the server");
      Ce(this, Ho, "f").resolve(await Ce(this, is, "f"));
    } catch (t) {
      throw nt(this, tu, !1, "f"), Ce(this, Ho, "f").promise.catch(() => {
      }), Ce(this, Ho, "f").reject(t), nt(this, Ho, eE(), "f"), t;
    }
  }
  setMessagesParams(e) {
    typeof e == "function" ? Ce(this, Qn, "f").params = e(Ce(this, Qn, "f").params) : Ce(this, Qn, "f").params = e, nt(this, Vi, !0, "f"), nt(this, vo, void 0, "f");
  }
  setRequestOptions(e) {
    typeof e == "function" ? nt(this, Vr, e(Ce(this, Vr, "f")), "f") : nt(this, Vr, {
      ...Ce(this, Vr, "f"),
      ...e
    }, "f");
  }
  async generateToolResponse(e = Ce(this, Vr, "f").signal) {
    const t = await Ce(this, is, "f") ?? this.params.messages.at(-1);
    return t ? Ce(this, Jc, "m", l0).call(this, t, e) : null;
  }
  done() {
    return Ce(this, Ho, "f").promise;
  }
  async runUntilDone() {
    if (!Ce(this, tu, "f")) for await (const e of this) ;
    return this.done();
  }
  get params() {
    return Ce(this, Qn, "f").params;
  }
  pushMessages(...e) {
    this.setMessagesParams((t) => ({
      ...t,
      messages: [...t.messages, ...e]
    }));
  }
  then(e, t) {
    return this.runUntilDone().then(e, t);
  }
};
l0 = async function(t, n = Ce(this, Vr, "f").signal) {
  return Ce(this, vo, "f") !== void 0 ? Ce(this, vo, "f") : (nt(this, vo, Tz(Ce(this, Qn, "f").params, t, {
    ...Ce(this, Vr, "f"),
    signal: n
  }), "f"), Ce(this, vo, "f"));
};
async function Tz(e, t = e.messages.at(-1), n) {
  if (!t || t.role !== "assistant" || !t.content || typeof t.content == "string") return null;
  const r = t.content.filter((s) => s.type === "tool_use");
  return r.length === 0 ? null : {
    role: "user",
    content: await Promise.all(r.map(async (s) => {
      const a = e.tools.find((o) => ("name" in o ? o.name : o.mcp_server_name) === s.name);
      if (!a || !("run" in a)) return {
        type: "tool_result",
        tool_use_id: s.id,
        content: `Error: Tool '${s.name}' not found`,
        is_error: !0
      };
      try {
        let o = s.input;
        "parse" in a && a.parse && (o = a.parse(o));
        const l = await a.run(o, {
          toolUseBlock: s,
          signal: n?.signal
        });
        return {
          type: "tool_result",
          tool_use_id: s.id,
          content: l
        };
      } catch (o) {
        return {
          type: "tool_result",
          tool_use_id: s.id,
          content: o instanceof F4 ? o.content : `Error: ${o instanceof Error ? o.message : String(o)}`,
          is_error: !0
        };
      }
    }))
  };
}
var U4 = class q4 {
  constructor(t, n) {
    this.iterator = t, this.controller = n;
  }
  async *decoder() {
    const t = new zf();
    for await (const n of this.iterator) for (const r of t.decode(n)) yield JSON.parse(r);
    for (const n of t.flush()) yield JSON.parse(n);
  }
  [Symbol.asyncIterator]() {
    return this.decoder();
  }
  static fromResponse(t, n) {
    if (!t.body)
      throw n.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new xt("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new xt("Attempted to iterate over a response with no body");
    return new q4(Ow(t.body), n);
  }
}, H4 = class extends Pn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/messages/batches?beta=true", {
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "message-batches-2024-09-24"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/messages/batches/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/messages/batches?beta=true", Kf, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "message-batches-2024-09-24"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(ft`/v1/messages/batches/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  cancel(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(ft`/v1/messages/batches/${e}/cancel?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  async results(e, t = {}, n) {
    const r = await this.retrieve(e);
    if (!r.results_url) throw new xt(`No batch \`results_url\`; Has it finished processing? ${r.processing_status} - ${r.id}`);
    const { betas: s } = t ?? {};
    return this._client.get(r.results_url, {
      ...n,
      headers: Ge([{
        "anthropic-beta": [...s ?? [], "message-batches-2024-09-24"].toString(),
        Accept: "application/binary"
      }, n?.headers]),
      stream: !0,
      __binaryResponse: !0
    })._thenUnwrap((a, o) => U4.fromResponse(o.response, o.controller));
  }
}, tE = {
  "claude-1.3": "November 6th, 2024",
  "claude-1.3-100k": "November 6th, 2024",
  "claude-instant-1.1": "November 6th, 2024",
  "claude-instant-1.1-100k": "November 6th, 2024",
  "claude-instant-1.2": "November 6th, 2024",
  "claude-3-sonnet-20240229": "July 21st, 2025",
  "claude-3-opus-20240229": "January 5th, 2026",
  "claude-2.1": "July 21st, 2025",
  "claude-2.0": "July 21st, 2025",
  "claude-3-7-sonnet-latest": "February 19th, 2026",
  "claude-3-7-sonnet-20250219": "February 19th, 2026"
}, Ez = ["claude-mythos-preview", "claude-opus-4-6"], Wf = class extends Pn {
  constructor() {
    super(...arguments), this.batches = new H4(this._client);
  }
  create(e, t) {
    const n = nE(e), { betas: r, ...s } = n;
    s.model in tE && console.warn(`The model '${s.model}' is deprecated and will reach end-of-life on ${tE[s.model]}
Please migrate to a newer model. Visit https://docs.anthropic.com/en/docs/resources/model-deprecations for more information.`), Ez.includes(s.model) && s.thinking && s.thinking.type === "enabled" && console.warn(`Using Claude with ${s.model} and 'thinking.type=enabled' is deprecated. Use 'thinking.type=adaptive' instead which results in better model performance in our testing: https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking`);
    let a = this._client._options.timeout;
    if (!s.stream && a == null) {
      const l = $4[s.model] ?? void 0;
      a = this._client.calculateNonstreamingTimeout(s.max_tokens, l);
    }
    const o = I4(s.tools, s.messages);
    return this._client.post("/v1/messages?beta=true", {
      body: s,
      timeout: a ?? 6e5,
      ...t,
      headers: Ge([
        { ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 },
        o,
        t?.headers
      ]),
      stream: n.stream ?? !1
    });
  }
  parse(e, t) {
    return t = {
      ...t,
      headers: Ge([{ "anthropic-beta": [...e.betas ?? [], "structured-outputs-2025-12-15"].toString() }, t?.headers])
    }, this.create(e, t).then((n) => D4(n, e, { logger: this._client.logger ?? console }));
  }
  stream(e, t) {
    return Sz.createMessage(this, e, t);
  }
  countTokens(e, t) {
    const { betas: n, ...r } = nE(e);
    return this._client.post("/v1/messages/count_tokens?beta=true", {
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "token-counting-2024-11-01"].toString() }, t?.headers])
    });
  }
  toolRunner(e, t) {
    return new B4(this._client, e, t);
  }
};
function nE(e) {
  if (!e.output_format) return e;
  if (e.output_config?.format) throw new xt("Both output_format and output_config.format were provided. Please use only output_config.format (output_format is deprecated).");
  const { output_format: t, ...n } = e;
  return {
    ...n,
    output_config: {
      ...e.output_config,
      format: t
    }
  };
}
Wf.Batches = H4;
Wf.BetaToolRunner = B4;
Wf.ToolError = F4;
var z4 = class extends Pn {
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(ft`/v1/sessions/${e}/events?beta=true`, ts, {
      query: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  send(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/sessions/${e}/events?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  stream(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/sessions/${e}/events/stream?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers]),
      stream: !0
    });
  }
}, K4 = class extends Pn {
  retrieve(e, t, n) {
    const { session_id: r, betas: s } = t;
    return this._client.get(ft`/v1/sessions/${r}/resources/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { session_id: r, betas: s, ...a } = t;
    return this._client.post(ft`/v1/sessions/${r}/resources/${e}?beta=true`, {
      body: a,
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(ft`/v1/sessions/${e}/resources?beta=true`, ts, {
      query: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { session_id: r, betas: s } = t;
    return this._client.delete(ft`/v1/sessions/${r}/resources/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  add(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/sessions/${e}/resources?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, uv = class extends Pn {
  constructor() {
    super(...arguments), this.events = new z4(this._client), this.resources = new K4(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/sessions?beta=true", {
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/sessions/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/sessions/${e}?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/sessions?beta=true", ts, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(ft`/v1/sessions/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(ft`/v1/sessions/${e}/archive?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
uv.Events = z4;
uv.Resources = K4;
var W4 = class extends Pn {
  create(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.post(ft`/v1/skills/${e}/versions?beta=true`, Bw({
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    }, this._client));
  }
  retrieve(e, t, n) {
    const { skill_id: r, betas: s } = t;
    return this._client.get(ft`/v1/skills/${r}/versions/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(ft`/v1/skills/${e}/versions?beta=true`, ts, {
      query: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { skill_id: r, betas: s } = t;
    return this._client.delete(ft`/v1/skills/${r}/versions/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
}, qw = class extends Pn {
  constructor() {
    super(...arguments), this.versions = new W4(this._client);
  }
  create(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.post("/v1/skills?beta=true", Bw({
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "skills-2025-10-02"].toString() }, t?.headers])
    }, this._client, !1));
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/skills/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/skills?beta=true", ts, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "skills-2025-10-02"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(ft`/v1/skills/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
};
qw.Versions = W4;
var G4 = class extends Pn {
  create(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/vaults/${e}/credentials?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { vault_id: r, betas: s } = t;
    return this._client.get(ft`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { vault_id: r, betas: s, ...a } = t;
    return this._client.post(ft`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      body: a,
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(ft`/v1/vaults/${e}/credentials?beta=true`, ts, {
      query: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { vault_id: r, betas: s } = t;
    return this._client.delete(ft`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t, n) {
    const { vault_id: r, betas: s } = t;
    return this._client.post(ft`/v1/vaults/${r}/credentials/${e}/archive?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, Hw = class extends Pn {
  constructor() {
    super(...arguments), this.credentials = new G4(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/vaults?beta=true", {
      body: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/vaults/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(ft`/v1/vaults/${e}?beta=true`, {
      body: s,
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/vaults?beta=true", ts, {
      query: r,
      ...t,
      headers: Ge([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(ft`/v1/vaults/${e}?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(ft`/v1/vaults/${e}/archive?beta=true`, {
      ...n,
      headers: Ge([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
Hw.Credentials = G4;
var Ks = class extends Pn {
  constructor() {
    super(...arguments), this.models = new k4(this._client), this.messages = new Wf(this._client), this.agents = new Uw(this._client), this.environments = new E4(this._client), this.sessions = new uv(this._client), this.vaults = new Hw(this._client), this.memoryStores = new lv(this._client), this.files = new x4(this._client), this.skills = new qw(this._client), this.userProfiles = new M4(this._client);
  }
};
Ks.Models = k4;
Ks.Messages = Wf;
Ks.Agents = Uw;
Ks.Environments = E4;
Ks.Sessions = uv;
Ks.Vaults = Hw;
Ks.MemoryStores = lv;
Ks.Files = x4;
Ks.Skills = qw;
Ks.UserProfiles = M4;
var V4 = class extends Pn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/complete", {
      body: r,
      timeout: this._client._options.timeout ?? 6e5,
      ...t,
      headers: Ge([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers]),
      stream: e.stream ?? !1
    });
  }
};
function J4(e) {
  return e?.output_config?.format;
}
function rE(e, t, n) {
  const r = J4(t);
  return !t || !("parse" in (r ?? {})) ? {
    ...e,
    content: e.content.map((s) => s.type === "text" ? Object.defineProperty({ ...s }, "parsed_output", {
      value: null,
      enumerable: !1
    }) : s),
    parsed_output: null
  } : Y4(e, t, n);
}
function Y4(e, t, n) {
  let r = null;
  const s = e.content.map((a) => {
    if (a.type === "text") {
      const o = Cz(t, a.text);
      return r === null && (r = o), Object.defineProperty({ ...a }, "parsed_output", {
        value: o,
        enumerable: !1
      });
    }
    return a;
  });
  return {
    ...e,
    content: s,
    parsed_output: r
  };
}
function Cz(e, t) {
  const n = J4(e);
  if (n?.type !== "json_schema") return null;
  try {
    return "parse" in n ? n.parse(t) : JSON.parse(t);
  } catch (r) {
    throw new xt(`Failed to parse structured output: ${r}`);
  }
}
var $s, zo, nu, Xc, sp, jc, Qc, ap, Zc, no, ed, op, ip, Ji, lp, up, td, f_, sE, h_, p_, m_, g_, aE, oE = "__json_buf";
function iE(e) {
  return e.type === "tool_use" || e.type === "server_tool_use";
}
var Iz = class u0 {
  constructor(t, n) {
    $s.add(this), this.messages = [], this.receivedMessages = [], zo.set(this, void 0), nu.set(this, null), this.controller = new AbortController(), Xc.set(this, void 0), sp.set(this, () => {
    }), jc.set(this, () => {
    }), Qc.set(this, void 0), ap.set(this, () => {
    }), Zc.set(this, () => {
    }), no.set(this, {}), ed.set(this, !1), op.set(this, !1), ip.set(this, !1), Ji.set(this, !1), lp.set(this, void 0), up.set(this, void 0), td.set(this, void 0), h_.set(this, (r) => {
      if (nt(this, op, !0, "f"), Tf(r) && (r = new ta()), r instanceof ta)
        return nt(this, ip, !0, "f"), this._emit("abort", r);
      if (r instanceof xt) return this._emit("error", r);
      if (r instanceof Error) {
        const s = new xt(r.message);
        return s.cause = r, this._emit("error", s);
      }
      return this._emit("error", new xt(String(r)));
    }), nt(this, Xc, new Promise((r, s) => {
      nt(this, sp, r, "f"), nt(this, jc, s, "f");
    }), "f"), nt(this, Qc, new Promise((r, s) => {
      nt(this, ap, r, "f"), nt(this, Zc, s, "f");
    }), "f"), Ce(this, Xc, "f").catch(() => {
    }), Ce(this, Qc, "f").catch(() => {
    }), nt(this, nu, t, "f"), nt(this, td, n?.logger ?? console, "f");
  }
  get response() {
    return Ce(this, lp, "f");
  }
  get request_id() {
    return Ce(this, up, "f");
  }
  async withResponse() {
    nt(this, Ji, !0, "f");
    const t = await Ce(this, Xc, "f");
    if (!t) throw new Error("Could not resolve a `Response` object");
    return {
      data: this,
      response: t,
      request_id: t.headers.get("request-id")
    };
  }
  static fromReadableStream(t) {
    const n = new u0(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createMessage(t, n, r, { logger: s } = {}) {
    const a = new u0(n, { logger: s });
    for (const o of n.messages) a._addMessageParam(o);
    return nt(a, nu, {
      ...n,
      stream: !0
    }, "f"), a._run(() => a._createMessage(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), a;
  }
  _run(t) {
    t().then(() => {
      this._emitFinal(), this._emit("end");
    }, Ce(this, h_, "f"));
  }
  _addMessageParam(t) {
    this.messages.push(t);
  }
  _addMessage(t, n = !0) {
    this.receivedMessages.push(t), n && this._emit("message", t);
  }
  async _createMessage(t, n, r) {
    const s = r?.signal;
    let a;
    s && (s.aborted && this.controller.abort(), a = this.controller.abort.bind(this.controller), s.addEventListener("abort", a));
    try {
      Ce(this, $s, "m", p_).call(this);
      const { response: o, data: l } = await t.create({
        ...n,
        stream: !0
      }, {
        ...r,
        signal: this.controller.signal
      }).withResponse();
      this._connected(o);
      for await (const i of l) Ce(this, $s, "m", m_).call(this, i);
      if (l.controller.signal?.aborted) throw new ta();
      Ce(this, $s, "m", g_).call(this);
    } finally {
      s && a && s.removeEventListener("abort", a);
    }
  }
  _connected(t) {
    this.ended || (nt(this, lp, t, "f"), nt(this, up, t?.headers.get("request-id"), "f"), Ce(this, sp, "f").call(this, t), this._emit("connect"));
  }
  get ended() {
    return Ce(this, ed, "f");
  }
  get errored() {
    return Ce(this, op, "f");
  }
  get aborted() {
    return Ce(this, ip, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(t, n) {
    return (Ce(this, no, "f")[t] || (Ce(this, no, "f")[t] = [])).push({ listener: n }), this;
  }
  off(t, n) {
    const r = Ce(this, no, "f")[t];
    if (!r) return this;
    const s = r.findIndex((a) => a.listener === n);
    return s >= 0 && r.splice(s, 1), this;
  }
  once(t, n) {
    return (Ce(this, no, "f")[t] || (Ce(this, no, "f")[t] = [])).push({
      listener: n,
      once: !0
    }), this;
  }
  emitted(t) {
    return new Promise((n, r) => {
      nt(this, Ji, !0, "f"), t !== "error" && this.once("error", r), this.once(t, n);
    });
  }
  async done() {
    nt(this, Ji, !0, "f"), await Ce(this, Qc, "f");
  }
  get currentMessage() {
    return Ce(this, zo, "f");
  }
  async finalMessage() {
    return await this.done(), Ce(this, $s, "m", f_).call(this);
  }
  async finalText() {
    return await this.done(), Ce(this, $s, "m", sE).call(this);
  }
  _emit(t, ...n) {
    if (Ce(this, ed, "f")) return;
    t === "end" && (nt(this, ed, !0, "f"), Ce(this, ap, "f").call(this));
    const r = Ce(this, no, "f")[t];
    if (r && (Ce(this, no, "f")[t] = r.filter((s) => !s.once), r.forEach(({ listener: s }) => s(...n))), t === "abort") {
      const s = n[0];
      !Ce(this, Ji, "f") && !r?.length && Promise.reject(s), Ce(this, jc, "f").call(this, s), Ce(this, Zc, "f").call(this, s), this._emit("end");
      return;
    }
    if (t === "error") {
      const s = n[0];
      !Ce(this, Ji, "f") && !r?.length && Promise.reject(s), Ce(this, jc, "f").call(this, s), Ce(this, Zc, "f").call(this, s), this._emit("end");
    }
  }
  _emitFinal() {
    this.receivedMessages.at(-1) && this._emit("finalMessage", Ce(this, $s, "m", f_).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    let s;
    r && (r.aborted && this.controller.abort(), s = this.controller.abort.bind(this.controller), r.addEventListener("abort", s));
    try {
      Ce(this, $s, "m", p_).call(this), this._connected(null);
      const a = Ef.fromReadableStream(t, this.controller);
      for await (const o of a) Ce(this, $s, "m", m_).call(this, o);
      if (a.controller.signal?.aborted) throw new ta();
      Ce(this, $s, "m", g_).call(this);
    } finally {
      r && s && r.removeEventListener("abort", s);
    }
  }
  [(zo = /* @__PURE__ */ new WeakMap(), nu = /* @__PURE__ */ new WeakMap(), Xc = /* @__PURE__ */ new WeakMap(), sp = /* @__PURE__ */ new WeakMap(), jc = /* @__PURE__ */ new WeakMap(), Qc = /* @__PURE__ */ new WeakMap(), ap = /* @__PURE__ */ new WeakMap(), Zc = /* @__PURE__ */ new WeakMap(), no = /* @__PURE__ */ new WeakMap(), ed = /* @__PURE__ */ new WeakMap(), op = /* @__PURE__ */ new WeakMap(), ip = /* @__PURE__ */ new WeakMap(), Ji = /* @__PURE__ */ new WeakMap(), lp = /* @__PURE__ */ new WeakMap(), up = /* @__PURE__ */ new WeakMap(), td = /* @__PURE__ */ new WeakMap(), h_ = /* @__PURE__ */ new WeakMap(), $s = /* @__PURE__ */ new WeakSet(), f_ = function() {
    if (this.receivedMessages.length === 0) throw new xt("stream ended without producing a Message with role=assistant");
    return this.receivedMessages.at(-1);
  }, sE = function() {
    if (this.receivedMessages.length === 0) throw new xt("stream ended without producing a Message with role=assistant");
    const n = this.receivedMessages.at(-1).content.filter((r) => r.type === "text").map((r) => r.text);
    if (n.length === 0) throw new xt("stream ended without producing a content block with type=text");
    return n.join(" ");
  }, p_ = function() {
    this.ended || nt(this, zo, void 0, "f");
  }, m_ = function(n) {
    if (this.ended) return;
    const r = Ce(this, $s, "m", aE).call(this, n);
    switch (this._emit("streamEvent", n, r), n.type) {
      case "content_block_delta": {
        const s = r.content.at(-1);
        switch (n.delta.type) {
          case "text_delta":
            s.type === "text" && this._emit("text", n.delta.text, s.text || "");
            break;
          case "citations_delta":
            s.type === "text" && this._emit("citation", n.delta.citation, s.citations ?? []);
            break;
          case "input_json_delta":
            iE(s) && s.input && this._emit("inputJson", n.delta.partial_json, s.input);
            break;
          case "thinking_delta":
            s.type === "thinking" && this._emit("thinking", n.delta.thinking, s.thinking);
            break;
          case "signature_delta":
            s.type === "thinking" && this._emit("signature", s.signature);
            break;
          default:
            n.delta;
        }
        break;
      }
      case "message_stop":
        this._addMessageParam(r), this._addMessage(rE(r, Ce(this, nu, "f"), { logger: Ce(this, td, "f") }), !0);
        break;
      case "content_block_stop":
        this._emit("contentBlock", r.content.at(-1));
        break;
      case "message_start":
        nt(this, zo, r, "f");
        break;
      case "content_block_start":
      case "message_delta":
        break;
    }
  }, g_ = function() {
    if (this.ended) throw new xt("stream has ended, this shouldn't happen");
    const n = Ce(this, zo, "f");
    if (!n) throw new xt("request ended without sending any chunks");
    return nt(this, zo, void 0, "f"), rE(n, Ce(this, nu, "f"), { logger: Ce(this, td, "f") });
  }, aE = function(n) {
    let r = Ce(this, zo, "f");
    if (n.type === "message_start") {
      if (r) throw new xt(`Unexpected event order, got ${n.type} before receiving "message_stop"`);
      return n.message;
    }
    if (!r) throw new xt(`Unexpected event order, got ${n.type} before "message_start"`);
    switch (n.type) {
      case "message_stop":
        return r;
      case "message_delta":
        return r.stop_reason = n.delta.stop_reason, r.stop_sequence = n.delta.stop_sequence, r.usage.output_tokens = n.usage.output_tokens, n.usage.input_tokens != null && (r.usage.input_tokens = n.usage.input_tokens), n.usage.cache_creation_input_tokens != null && (r.usage.cache_creation_input_tokens = n.usage.cache_creation_input_tokens), n.usage.cache_read_input_tokens != null && (r.usage.cache_read_input_tokens = n.usage.cache_read_input_tokens), n.usage.server_tool_use != null && (r.usage.server_tool_use = n.usage.server_tool_use), r;
      case "content_block_start":
        return r.content.push({ ...n.content_block }), r;
      case "content_block_delta": {
        const s = r.content.at(n.index);
        switch (n.delta.type) {
          case "text_delta":
            s?.type === "text" && (r.content[n.index] = {
              ...s,
              text: (s.text || "") + n.delta.text
            });
            break;
          case "citations_delta":
            s?.type === "text" && (r.content[n.index] = {
              ...s,
              citations: [...s.citations ?? [], n.delta.citation]
            });
            break;
          case "input_json_delta":
            if (s && iE(s)) {
              let a = s[oE] || "";
              a += n.delta.partial_json;
              const o = { ...s };
              Object.defineProperty(o, oE, {
                value: a,
                enumerable: !1,
                writable: !0
              }), a && (o.input = O4(a)), r.content[n.index] = o;
            }
            break;
          case "thinking_delta":
            s?.type === "thinking" && (r.content[n.index] = {
              ...s,
              thinking: s.thinking + n.delta.thinking
            });
            break;
          case "signature_delta":
            s?.type === "thinking" && (r.content[n.index] = {
              ...s,
              signature: n.delta.signature
            });
            break;
          default:
            n.delta;
        }
        return r;
      }
      case "content_block_stop":
        return r;
    }
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("streamEvent", (s) => {
      const a = n.shift();
      a ? a.resolve(s) : t.push(s);
    }), this.on("end", () => {
      r = !0;
      for (const s of n) s.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (s) => {
      r = !0;
      for (const a of n) a.reject(s);
      n.length = 0;
    }), this.on("error", (s) => {
      r = !0;
      for (const a of n) a.reject(s);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((s, a) => n.push({
        resolve: s,
        reject: a
      })).then((s) => s ? {
        value: s,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new Ef(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
}, X4 = class extends Pn {
  create(e, t) {
    return this._client.post("/v1/messages/batches", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(ft`/v1/messages/batches/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/v1/messages/batches", Kf, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(ft`/v1/messages/batches/${e}`, t);
  }
  cancel(e, t) {
    return this._client.post(ft`/v1/messages/batches/${e}/cancel`, t);
  }
  async results(e, t) {
    const n = await this.retrieve(e);
    if (!n.results_url) throw new xt(`No batch \`results_url\`; Has it finished processing? ${n.processing_status} - ${n.id}`);
    return this._client.get(n.results_url, {
      ...t,
      headers: Ge([{ Accept: "application/binary" }, t?.headers]),
      stream: !0,
      __binaryResponse: !0
    })._thenUnwrap((r, s) => U4.fromResponse(s.response, s.controller));
  }
}, zw = class extends Pn {
  constructor() {
    super(...arguments), this.batches = new X4(this._client);
  }
  create(e, t) {
    e.model in lE && console.warn(`The model '${e.model}' is deprecated and will reach end-of-life on ${lE[e.model]}
Please migrate to a newer model. Visit https://docs.anthropic.com/en/docs/resources/model-deprecations for more information.`), xz.includes(e.model) && e.thinking && e.thinking.type === "enabled" && console.warn(`Using Claude with ${e.model} and 'thinking.type=enabled' is deprecated. Use 'thinking.type=adaptive' instead which results in better model performance in our testing: https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking`);
    let n = this._client._options.timeout;
    if (!e.stream && n == null) {
      const s = $4[e.model] ?? void 0;
      n = this._client.calculateNonstreamingTimeout(e.max_tokens, s);
    }
    const r = I4(e.tools, e.messages);
    return this._client.post("/v1/messages", {
      body: e,
      timeout: n ?? 6e5,
      ...t,
      headers: Ge([r, t?.headers]),
      stream: e.stream ?? !1
    });
  }
  parse(e, t) {
    return this.create(e, t).then((n) => Y4(n, e, { logger: this._client.logger ?? console }));
  }
  stream(e, t) {
    return Iz.createMessage(this, e, t, { logger: this._client.logger ?? console });
  }
  countTokens(e, t) {
    return this._client.post("/v1/messages/count_tokens", {
      body: e,
      ...t
    });
  }
}, lE = {
  "claude-1.3": "November 6th, 2024",
  "claude-1.3-100k": "November 6th, 2024",
  "claude-instant-1.1": "November 6th, 2024",
  "claude-instant-1.1-100k": "November 6th, 2024",
  "claude-instant-1.2": "November 6th, 2024",
  "claude-3-sonnet-20240229": "July 21st, 2025",
  "claude-3-opus-20240229": "January 5th, 2026",
  "claude-2.1": "July 21st, 2025",
  "claude-2.0": "July 21st, 2025",
  "claude-3-7-sonnet-latest": "February 19th, 2026",
  "claude-3-7-sonnet-20250219": "February 19th, 2026",
  "claude-3-5-haiku-latest": "February 19th, 2026",
  "claude-3-5-haiku-20241022": "February 19th, 2026",
  "claude-opus-4-0": "June 15th, 2026",
  "claude-opus-4-20250514": "June 15th, 2026",
  "claude-sonnet-4-0": "June 15th, 2026",
  "claude-sonnet-4-20250514": "June 15th, 2026"
}, xz = ["claude-mythos-preview", "claude-opus-4-6"];
zw.Batches = X4;
var j4 = class extends Pn {
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(ft`/v1/models/${e}`, {
      ...n,
      headers: Ge([{ ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/models", Kf, {
      query: r,
      ...t,
      headers: Ge([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers])
    });
  }
}, cp = (e) => {
  if (typeof globalThis.process < "u") return globalThis.process.env?.[e]?.trim() || void 0;
  if (typeof globalThis.Deno < "u") return globalThis.Deno.env?.get?.(e)?.trim() || void 0;
}, c0, Kw, Jp, Q4, kz = "\\n\\nHuman:", Mz = "\\n\\nAssistant:", Gn = class {
  constructor({ baseURL: e = cp("ANTHROPIC_BASE_URL"), apiKey: t = cp("ANTHROPIC_API_KEY") ?? null, authToken: n = cp("ANTHROPIC_AUTH_TOKEN") ?? null, ...r } = {}) {
    c0.add(this), Jp.set(this, void 0);
    const s = {
      apiKey: t,
      authToken: n,
      ...r,
      baseURL: e || "https://api.anthropic.com"
    };
    if (!s.dangerouslyAllowBrowser && KH()) throw new xt(`It looks like you're running in a browser-like environment.

This is disabled by default, as it risks exposing your secret API credentials to attackers.
If you understand the risks and have appropriate mitigations in place,
you can set the \`dangerouslyAllowBrowser\` option to \`true\`, e.g.,

new Anthropic({ apiKey, dangerouslyAllowBrowser: true });
`);
    this.baseURL = s.baseURL, this.timeout = s.timeout ?? Kw.DEFAULT_TIMEOUT, this.logger = s.logger ?? console;
    const a = "warn";
    this.logLevel = a, this.logLevel = KT(s.logLevel, "ClientOptions.logLevel", this) ?? KT(cp("ANTHROPIC_LOG"), "process.env['ANTHROPIC_LOG']", this) ?? a, this.fetchOptions = s.fetchOptions, this.maxRetries = s.maxRetries ?? 2, this.fetch = s.fetch ?? YH(), nt(this, Jp, jH, "f"), this._options = s, this.apiKey = typeof t == "string" ? t : null, this.authToken = n;
  }
  withOptions(e) {
    return new this.constructor({
      ...this._options,
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this.apiKey,
      authToken: this.authToken,
      ...e
    });
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: e, nulls: t }) {
    if (!(e.get("x-api-key") || e.get("authorization")) && !(this.apiKey && e.get("x-api-key")) && !t.has("x-api-key") && !(this.authToken && e.get("authorization")) && !t.has("authorization"))
      throw new Error('Could not resolve authentication method. Expected either apiKey or authToken to be set. Or for one of the "X-Api-Key" or "Authorization" headers to be explicitly omitted');
  }
  async authHeaders(e) {
    return Ge([await this.apiKeyAuth(e), await this.bearerAuth(e)]);
  }
  async apiKeyAuth(e) {
    if (this.apiKey != null)
      return Ge([{ "X-Api-Key": this.apiKey }]);
  }
  async bearerAuth(e) {
    if (this.authToken != null)
      return Ge([{ Authorization: `Bearer ${this.authToken}` }]);
  }
  stringifyQuery(e) {
    return QH(e);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${fu}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${n4()}`;
  }
  makeStatusError(e, t, n, r) {
    return Is.generate(e, t, n, r);
  }
  buildURL(e, t, n) {
    const r = !Ce(this, c0, "m", Q4).call(this) && n || this.baseURL, s = UH(e) ? new URL(e) : new URL(r + (r.endsWith("/") && e.startsWith("/") ? e.slice(1) : e)), a = this.defaultQuery(), o = Object.fromEntries(s.searchParams);
    return (!OT(a) || !OT(o)) && (t = {
      ...o,
      ...a,
      ...t
    }), typeof t == "object" && t && !Array.isArray(t) && (s.search = this.stringifyQuery(t)), s.toString();
  }
  _calculateNonstreamingTimeout(e) {
    if (3600 * e / 128e3 > 600) throw new xt("Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#streaming-responses for more details");
    return 600 * 1e3;
  }
  async prepareOptions(e) {
  }
  async prepareRequest(e, { url: t, options: n }) {
  }
  get(e, t) {
    return this.methodRequest("get", e, t);
  }
  post(e, t) {
    return this.methodRequest("post", e, t);
  }
  patch(e, t) {
    return this.methodRequest("patch", e, t);
  }
  put(e, t) {
    return this.methodRequest("put", e, t);
  }
  delete(e, t) {
    return this.methodRequest("delete", e, t);
  }
  methodRequest(e, t, n) {
    return this.request(Promise.resolve(n).then((r) => ({
      method: e,
      path: t,
      ...r
    })));
  }
  request(e, t = null) {
    return new v4(this, this.makeRequest(e, t, void 0));
  }
  async makeRequest(e, t, n) {
    const r = await e, s = r.maxRetries ?? this.maxRetries;
    t == null && (t = s), await this.prepareOptions(r);
    const { req: a, url: o, timeout: l } = await this.buildRequest(r, { retryCount: s - t });
    await this.prepareRequest(a, {
      url: o,
      options: r
    });
    const i = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), u = n === void 0 ? "" : `, retryOf: ${n}`, d = Date.now();
    if (Or(this).debug(`[${i}] sending request`, ji({
      retryOfRequestLogID: n,
      method: r.method,
      url: o,
      options: r,
      headers: a.headers
    })), r.signal?.aborted) throw new ta();
    const f = new AbortController(), m = await this.fetchWithTimeout(o, a, l, f).catch(t0), v = Date.now();
    if (m instanceof globalThis.Error) {
      const S = `retrying, ${t} attempts remaining`;
      if (r.signal?.aborted) throw new ta();
      const x = Tf(m) || /timed? ?out/i.test(String(m) + ("cause" in m ? String(m.cause) : ""));
      if (t)
        return Or(this).info(`[${i}] connection ${x ? "timed out" : "failed"} - ${S}`), Or(this).debug(`[${i}] connection ${x ? "timed out" : "failed"} (${S})`, ji({
          retryOfRequestLogID: n,
          url: o,
          durationMs: v - d,
          message: m.message
        })), this.retryRequest(r, t, n ?? i);
      throw Or(this).info(`[${i}] connection ${x ? "timed out" : "failed"} - error; no more retries left`), Or(this).debug(`[${i}] connection ${x ? "timed out" : "failed"} (error; no more retries left)`, ji({
        retryOfRequestLogID: n,
        url: o,
        durationMs: v - d,
        message: m.message
      })), x ? new r4() : new iv({ cause: m });
    }
    const y = `[${i}${u}${[...m.headers.entries()].filter(([S]) => S === "request-id").map(([S, x]) => ", " + S + ": " + JSON.stringify(x)).join("")}] ${a.method} ${o} ${m.ok ? "succeeded" : "failed"} with status ${m.status} in ${v - d}ms`;
    if (!m.ok) {
      const S = await this.shouldRetry(m);
      if (t && S) {
        const $ = `retrying, ${t} attempts remaining`;
        return await XH(m.body), Or(this).info(`${y} - ${$}`), Or(this).debug(`[${i}] response error (${$})`, ji({
          retryOfRequestLogID: n,
          url: m.url,
          status: m.status,
          headers: m.headers,
          durationMs: v - d
        })), this.retryRequest(r, t, n ?? i, m.headers);
      }
      const x = S ? "error; no more retries left" : "error; not retryable";
      Or(this).info(`${y} - ${x}`);
      const I = await m.text().catch(($) => t0($).message), E = f4(I), N = E ? void 0 : I;
      throw Or(this).debug(`[${i}] response error (${x})`, ji({
        retryOfRequestLogID: n,
        url: m.url,
        status: m.status,
        headers: m.headers,
        message: N,
        durationMs: Date.now() - d
      })), this.makeStatusError(m.status, E, N, m.headers);
    }
    return Or(this).info(y), Or(this).debug(`[${i}] response start`, ji({
      retryOfRequestLogID: n,
      url: m.url,
      status: m.status,
      headers: m.headers,
      durationMs: v - d
    })), {
      response: m,
      options: r,
      controller: f,
      requestLogID: i,
      retryOfRequestLogID: n,
      startTime: d
    };
  }
  getAPIList(e, t, n) {
    return this.requestAPIList(t, n && "then" in n ? n.then((r) => ({
      method: "get",
      path: e,
      ...r
    })) : {
      method: "get",
      path: e,
      ...n
    });
  }
  requestAPIList(e, t) {
    const n = this.makeRequest(t, null, void 0);
    return new iz(this, n, e);
  }
  async fetchWithTimeout(e, t, n, r) {
    const { signal: s, method: a, ...o } = t || {}, l = this._makeAbort(r);
    s && s.addEventListener("abort", l, { once: !0 });
    const i = setTimeout(l, n), u = globalThis.ReadableStream && o.body instanceof globalThis.ReadableStream || typeof o.body == "object" && o.body !== null && Symbol.asyncIterator in o.body, d = {
      signal: r.signal,
      ...u ? { duplex: "half" } : {},
      method: "GET",
      ...o
    };
    a && (d.method = a.toUpperCase());
    try {
      return await this.fetch.call(void 0, e, d);
    } finally {
      clearTimeout(i);
    }
  }
  async shouldRetry(e) {
    const t = e.headers.get("x-should-retry");
    return t === "true" ? !0 : t === "false" ? !1 : e.status === 408 || e.status === 409 || e.status === 429 || e.status >= 500;
  }
  async retryRequest(e, t, n, r) {
    let s;
    const a = r?.get("retry-after-ms");
    if (a) {
      const l = parseFloat(a);
      Number.isNaN(l) || (s = l);
    }
    const o = r?.get("retry-after");
    if (o && !s) {
      const l = parseFloat(o);
      Number.isNaN(l) ? s = Date.parse(o) - Date.now() : s = l * 1e3;
    }
    if (s === void 0) {
      const l = e.maxRetries ?? this.maxRetries;
      s = this.calculateDefaultRetryTimeoutMillis(t, l);
    }
    return await zH(s), this.makeRequest(e, t - 1, n);
  }
  calculateDefaultRetryTimeoutMillis(e, t) {
    const s = t - e;
    return Math.min(0.5 * Math.pow(2, s), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  calculateNonstreamingTimeout(e, t) {
    if (36e5 * e / 128e3 > 6e5 || t != null && e > t) throw new xt("Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#long-requests for more details");
    return 6e5;
  }
  async buildRequest(e, { retryCount: t = 0 } = {}) {
    const n = { ...e }, { method: r, path: s, query: a, defaultBaseURL: o } = n, l = this.buildURL(s, a, o);
    "timeout" in n && HH("timeout", n.timeout), n.timeout = n.timeout ?? this.timeout;
    const { bodyHeaders: i, body: u } = this.buildBody({ options: n });
    return {
      req: {
        method: r,
        headers: await this.buildHeaders({
          options: e,
          method: r,
          bodyHeaders: i,
          retryCount: t
        }),
        ...n.signal && { signal: n.signal },
        ...globalThis.ReadableStream && u instanceof globalThis.ReadableStream && { duplex: "half" },
        ...u && { body: u },
        ...this.fetchOptions ?? {},
        ...n.fetchOptions ?? {}
      },
      url: l,
      timeout: n.timeout
    };
  }
  async buildHeaders({ options: e, method: t, bodyHeaders: n, retryCount: r }) {
    let s = {};
    this.idempotencyHeader && t !== "get" && (e.idempotencyKey || (e.idempotencyKey = this.defaultIdempotencyKey()), s[this.idempotencyHeader] = e.idempotencyKey);
    const a = Ge([
      s,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent(),
        "X-Stainless-Retry-Count": String(r),
        ...e.timeout ? { "X-Stainless-Timeout": String(Math.trunc(e.timeout / 1e3)) } : {},
        ...JH(),
        ...this._options.dangerouslyAllowBrowser ? { "anthropic-dangerous-direct-browser-access": "true" } : void 0,
        "anthropic-version": "2023-06-01"
      },
      await this.authHeaders(e),
      this._options.defaultHeaders,
      n,
      e.headers
    ]);
    return this.validateHeaders(a), a.values;
  }
  _makeAbort(e) {
    return () => e.abort();
  }
  buildBody({ options: { body: e, headers: t } }) {
    if (!e) return {
      bodyHeaders: void 0,
      body: void 0
    };
    const n = Ge([t]);
    return ArrayBuffer.isView(e) || e instanceof ArrayBuffer || e instanceof DataView || typeof e == "string" && n.values.has("content-type") || globalThis.Blob && e instanceof globalThis.Blob || e instanceof FormData || e instanceof URLSearchParams || globalThis.ReadableStream && e instanceof globalThis.ReadableStream ? {
      bodyHeaders: void 0,
      body: e
    } : typeof e == "object" && (Symbol.asyncIterator in e || Symbol.iterator in e && "next" in e && typeof e.next == "function") ? {
      bodyHeaders: void 0,
      body: p4(e)
    } : typeof e == "object" && n.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(e)
    } : Ce(this, Jp, "f").call(this, {
      body: e,
      headers: n
    });
  }
};
Kw = Gn, Jp = /* @__PURE__ */ new WeakMap(), c0 = /* @__PURE__ */ new WeakSet(), Q4 = function() {
  return this.baseURL !== "https://api.anthropic.com";
};
Gn.Anthropic = Kw;
Gn.HUMAN_PROMPT = kz;
Gn.AI_PROMPT = Mz;
Gn.DEFAULT_TIMEOUT = 6e5;
Gn.AnthropicError = xt;
Gn.APIError = Is;
Gn.APIConnectionError = iv;
Gn.APIConnectionTimeoutError = r4;
Gn.APIUserAbortError = ta;
Gn.NotFoundError = i4;
Gn.ConflictError = l4;
Gn.RateLimitError = c4;
Gn.BadRequestError = s4;
Gn.AuthenticationError = a4;
Gn.InternalServerError = d4;
Gn.PermissionDeniedError = o4;
Gn.UnprocessableEntityError = u4;
Gn.toFile = hz;
var Gf = class extends Gn {
  constructor() {
    super(...arguments), this.completions = new V4(this), this.messages = new zw(this), this.models = new j4(this), this.beta = new Ks(this);
  }
};
Gf.Completions = V4;
Gf.Messages = zw;
Gf.Models = j4;
Gf.Beta = Ks;
function ju(e) {
  if (Array.isArray(e)) return e.map((n) => ju(n));
  if (!e || typeof e != "object") return e;
  const t = {};
  return Object.entries(e).forEach(([n, r]) => {
    t[n] = /authorization|csrf|token|api[-_]?key|proxy_password|password/i.test(n) ? "[redacted]" : ju(r);
  }), t;
}
function Cf(e = {}) {
  return {
    provider: e.provider || "",
    model: e.model || "",
    transport: e.transport || "sdk",
    request: ju({
      url: e.url || "",
      method: e.method || "POST",
      headers: e.headers || {},
      body: e.body || {},
      sdk: e.sdk || void 0
    })
  };
}
function Pz(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function Rz(e = "") {
  const t = String(e || "").match(/^data:([^;,]+);base64,(.+)$/);
  return t ? {
    mediaType: t[1],
    data: t[2]
  } : {
    mediaType: "",
    data: ""
  };
}
function Z4(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function Nz(e) {
  if (typeof e == "string") return [{
    type: "text",
    text: e
  }];
  if (!Array.isArray(e)) return [{
    type: "text",
    text: ""
  }];
  const t = e.map((n) => {
    if (!n || typeof n != "object") return null;
    if (n.type === "text") return {
      type: "text",
      text: n.text || ""
    };
    if (n.type === "image_url" && n.image_url?.url) {
      const r = Rz(n.image_url.url);
      return !r.mediaType || !r.data ? null : {
        type: "image",
        source: {
          type: "base64",
          media_type: r.mediaType,
          data: r.data
        }
      };
    }
    return null;
  }).filter(Boolean);
  return t.length ? t : [{
    type: "text",
    text: ""
  }];
}
function $z(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  return t.length ? [...new Set(t)].join(`

`) : "";
}
function Lz(e) {
  const t = e?.providerPayload?.anthropicContent;
  return Array.isArray(t) && t.length && Z4(t) || null;
}
function Dz(e) {
  return Array.isArray(e?.content) && e.content.length ? { anthropicContent: Z4(e.content) || [] } : void 0;
}
function uE(e = {}) {
  return {
    type: "tool_result",
    tool_use_id: e.tool_call_id,
    content: e.content
  };
}
function cE(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = String(t?.function?.name || "").trim();
    return n ? {
      type: "tool_use",
      id: t.id,
      name: n,
      input: Pz(t.function.arguments)
    } : null;
  }).filter(Boolean);
}
function Oz(e) {
  const t = [];
  for (let n = 0; n < e.length; n += 1) {
    const r = e[n];
    if (r.role !== "system") {
      if (r.role === "assistant") {
        const s = Lz(r), a = cE(r.tool_calls);
        if (s && a.length) {
          t.push({
            role: "assistant",
            content: s.filter((o) => o?.type !== "tool_use").concat(a)
          });
          continue;
        }
        if (s) {
          t.push({
            role: "assistant",
            content: s
          });
          continue;
        }
      }
      if (r.role === "tool") {
        const s = [uE(r)];
        for (; e[n + 1]?.role === "tool"; )
          n += 1, s.push(uE(e[n]));
        t.push({
          role: "user",
          content: s
        });
        continue;
      }
      if (r.role === "assistant" && Array.isArray(r.tool_calls) && r.tool_calls.length) {
        t.push({
          role: "assistant",
          content: [...r.content ? [{
            type: "text",
            text: r.content
          }] : [], ...cE(r.tool_calls)]
        });
        continue;
      }
      t.push({
        role: r.role,
        content: Nz(r.content)
      });
    }
  }
  return t;
}
function dp(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function dE(e = "") {
  return String(e || "https://api.anthropic.com").trim().replace(/\/+$/, "").replace(/\/v1$/i, "");
}
var Fz = class {
  constructor(e) {
    this.config = e, this.client = new Gf({
      apiKey: e.apiKey,
      baseURL: dE(e.baseUrl),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e) {
    const t = (e.tools || []).map((s) => ({
      name: s.function.name,
      description: s.function.description,
      input_schema: s.function.parameters
    })), n = $z(e), r = {
      model: this.config.model,
      system: n,
      messages: Oz(e.messages),
      tools: t,
      ...e.maxTokens ? { max_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (r.temperature = e.temperature), e.reasoning?.enabled && (r.thinking = {
      type: "adaptive",
      display: "summarized"
    }), r;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = dE(this.config.baseUrl);
    return Cf({
      provider: "anthropic",
      model: this.config.model,
      transport: "anthropic-sdk",
      url: `${r}/v1/messages`,
      headers: {
        "Content-Type": "application/json",
        "x-api-key": this.config.apiKey || ""
      },
      body: t.body || this.buildRequestBody(e),
      sdk: n ? "client.messages.stream" : "client.messages.create"
    });
  }
  async chat(e) {
    const t = this.buildRequestBody(e), n = this.inspectRequest(e, { body: t });
    let r;
    if (typeof e.onStreamProgress == "function") {
      const a = this.client.messages.stream(t, { signal: e.signal }), o = /* @__PURE__ */ new Map(), l = /* @__PURE__ */ new Map();
      let i = "";
      const u = () => Array.from(o.entries()).sort(([m], [v]) => m.localeCompare(v)).map(([m, v]) => ({
        label: m.startsWith("redacted:") ? "已脱敏思考块" : "思考块",
        text: v
      })).filter((m) => m.text), d = () => Array.from(l.entries()).sort(([m], [v]) => Number(m) - Number(v)).map(([, m]) => ({
        id: m.id || "anthropic-tool-draft",
        name: m.name || "工具调用",
        arguments: m.inputJson || "{}",
        draft: !0
      })).filter((m) => m.name), f = () => {
        const m = d();
        m.length && dp(e, {
          text: i,
          thoughts: u(),
          toolCalls: m,
          toolCallDraft: !0
        });
      };
      a.on("text", (m, v) => {
        i = v || "", dp(e, {
          text: i,
          thoughts: u(),
          ...d().length ? {
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        });
      }), a.on("thinking", (m, v) => {
        o.set("thinking:0", v || ""), dp(e, {
          thoughts: u(),
          ...d().length ? {
            text: i,
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        });
      }), a.on("streamEvent", (m) => {
        if (m?.type === "content_block_start" && m.content_block?.type === "tool_use") {
          const v = m.content_block.input && typeof m.content_block.input == "object" ? m.content_block.input : {};
          l.set(m.index, {
            id: m.content_block.id || `anthropic-tool-draft-${m.index + 1}`,
            name: m.content_block.name || "工具调用",
            inputJson: Object.keys(v).length ? JSON.stringify(v) : ""
          }), f();
          return;
        }
        if (m?.type === "content_block_delta" && m.delta?.type === "input_json_delta") {
          const v = l.get(m.index) || {
            id: `anthropic-tool-draft-${m.index + 1}`,
            name: "工具调用",
            inputJson: ""
          };
          l.set(m.index, {
            ...v,
            inputJson: `${v.inputJson || ""}${m.delta.partial_json || ""}`
          }), f();
        }
      }), a.on("contentBlock", (m) => {
        m?.type === "redacted_thinking" && (o.set("redacted:0", m.data || ""), dp(e, {
          thoughts: u(),
          ...d().length ? {
            text: i,
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        }));
      }), r = await a.finalMessage();
    } else r = await this.client.messages.create(t, { signal: e.signal });
    const s = (r.content || []).filter((a) => a.type === "tool_use" && a.name).map((a, o) => ({
      id: a.id || `anthropic-tool-${o + 1}`,
      name: a.name,
      arguments: JSON.stringify(a.input || {})
    }));
    return {
      text: (r.content || []).filter((a) => a.type === "text").map((a) => a.text || "").join(`
`),
      toolCalls: s,
      thoughts: (r.content || []).filter((a) => a.type === "thinking" || a.type === "redacted_thinking").map((a) => ({
        label: a.type === "thinking" ? "思考块" : "已脱敏思考块",
        text: a.type === "thinking" ? a.thinking || "" : a.data || ""
      })).filter((a) => a.text),
      finishReason: r.stop_reason || "stop",
      model: r.model || this.config.model,
      provider: "anthropic",
      providerPayload: Dz(r),
      requestInspection: n
    };
  }
}, Bz = /* @__PURE__ */ Pf(((e, t) => {
  function n(r, s) {
    typeof s == "boolean" && (s = { forever: s }), this._originalTimeouts = JSON.parse(JSON.stringify(r)), this._timeouts = r, this._options = s || {}, this._maxRetryTime = s && s.maxRetryTime || 1 / 0, this._fn = null, this._errors = [], this._attempts = 1, this._operationTimeout = null, this._operationTimeoutCb = null, this._timeout = null, this._operationStart = null, this._timer = null, this._options.forever && (this._cachedTimeouts = this._timeouts.slice(0));
  }
  t.exports = n, n.prototype.reset = function() {
    this._attempts = 1, this._timeouts = this._originalTimeouts.slice(0);
  }, n.prototype.stop = function() {
    this._timeout && clearTimeout(this._timeout), this._timer && clearTimeout(this._timer), this._timeouts = [], this._cachedTimeouts = null;
  }, n.prototype.retry = function(r) {
    if (this._timeout && clearTimeout(this._timeout), !r) return !1;
    var s = (/* @__PURE__ */ new Date()).getTime();
    if (r && s - this._operationStart >= this._maxRetryTime)
      return this._errors.push(r), this._errors.unshift(/* @__PURE__ */ new Error("RetryOperation timeout occurred")), !1;
    this._errors.push(r);
    var a = this._timeouts.shift();
    if (a === void 0) if (this._cachedTimeouts)
      this._errors.splice(0, this._errors.length - 1), a = this._cachedTimeouts.slice(-1);
    else return !1;
    var o = this;
    return this._timer = setTimeout(function() {
      o._attempts++, o._operationTimeoutCb && (o._timeout = setTimeout(function() {
        o._operationTimeoutCb(o._attempts);
      }, o._operationTimeout), o._options.unref && o._timeout.unref()), o._fn(o._attempts);
    }, a), this._options.unref && this._timer.unref(), !0;
  }, n.prototype.attempt = function(r, s) {
    this._fn = r, s && (s.timeout && (this._operationTimeout = s.timeout), s.cb && (this._operationTimeoutCb = s.cb));
    var a = this;
    this._operationTimeoutCb && (this._timeout = setTimeout(function() {
      a._operationTimeoutCb();
    }, a._operationTimeout)), this._operationStart = (/* @__PURE__ */ new Date()).getTime(), this._fn(this._attempts);
  }, n.prototype.try = function(r) {
    console.log("Using RetryOperation.try() is deprecated"), this.attempt(r);
  }, n.prototype.start = function(r) {
    console.log("Using RetryOperation.start() is deprecated"), this.attempt(r);
  }, n.prototype.start = n.prototype.try, n.prototype.errors = function() {
    return this._errors;
  }, n.prototype.attempts = function() {
    return this._attempts;
  }, n.prototype.mainError = function() {
    if (this._errors.length === 0) return null;
    for (var r = {}, s = null, a = 0, o = 0; o < this._errors.length; o++) {
      var l = this._errors[o], i = l.message, u = (r[i] || 0) + 1;
      r[i] = u, u >= a && (s = l, a = u);
    }
    return s;
  };
})), Uz = /* @__PURE__ */ Pf(((e) => {
  var t = Bz();
  e.operation = function(n) {
    return new t(e.timeouts(n), {
      forever: n && (n.forever || n.retries === 1 / 0),
      unref: n && n.unref,
      maxRetryTime: n && n.maxRetryTime
    });
  }, e.timeouts = function(n) {
    if (n instanceof Array) return [].concat(n);
    var r = {
      retries: 10,
      factor: 2,
      minTimeout: 1 * 1e3,
      maxTimeout: 1 / 0,
      randomize: !1
    };
    for (var s in n) r[s] = n[s];
    if (r.minTimeout > r.maxTimeout) throw new Error("minTimeout is greater than maxTimeout");
    for (var a = [], o = 0; o < r.retries; o++) a.push(this.createTimeout(o, r));
    return n && n.forever && !a.length && a.push(this.createTimeout(o, r)), a.sort(function(l, i) {
      return l - i;
    }), a;
  }, e.createTimeout = function(n, r) {
    var s = r.randomize ? Math.random() + 1 : 1, a = Math.round(s * Math.max(r.minTimeout, 1) * Math.pow(r.factor, n));
    return a = Math.min(a, r.maxTimeout), a;
  }, e.wrap = function(n, r, s) {
    if (r instanceof Array && (s = r, r = null), !s) {
      s = [];
      for (var a in n) typeof n[a] == "function" && s.push(a);
    }
    for (var o = 0; o < s.length; o++) {
      var l = s[o], i = n[l];
      n[l] = function(d) {
        var f = e.operation(r), m = Array.prototype.slice.call(arguments, 1), v = m.pop();
        m.push(function(y) {
          f.retry(y) || (y && (arguments[0] = f.mainError()), v.apply(this, arguments));
        }), f.attempt(function() {
          d.apply(n, m);
        });
      }.bind(n, i), n[l].options = r;
    }
  };
})), qz = /* @__PURE__ */ Pf(((e, t) => {
  t.exports = Uz();
})), Hz = /* @__PURE__ */ Pf(((e, t) => {
  var n = qz(), r = [
    "Failed to fetch",
    "NetworkError when attempting to fetch resource.",
    "The Internet connection appears to be offline.",
    "Network request failed"
  ], s = class extends Error {
    constructor(i) {
      super(), i instanceof Error ? (this.originalError = i, { message: i } = i) : (this.originalError = new Error(i), this.originalError.stack = this.stack), this.name = "AbortError", this.message = i;
    }
  }, a = (i, u, d) => {
    const f = d.retries - (u - 1);
    return i.attemptNumber = u, i.retriesLeft = f, i;
  }, o = (i) => r.includes(i), l = (i, u) => new Promise((d, f) => {
    u = {
      onFailedAttempt: () => {
      },
      retries: 10,
      ...u
    };
    const m = n.operation(u);
    m.attempt(async (v) => {
      try {
        d(await i(v));
      } catch (y) {
        if (!(y instanceof Error)) {
          f(/* @__PURE__ */ new TypeError(`Non-error was thrown: "${y}". You should only throw errors.`));
          return;
        }
        if (y instanceof s)
          m.stop(), f(y.originalError);
        else if (y instanceof TypeError && !o(y.message))
          m.stop(), f(y);
        else {
          a(y, v, u);
          try {
            await u.onFailedAttempt(y);
          } catch (S) {
            f(S);
            return;
          }
          m.retry(y) || f(m.mainError());
        }
      }
    });
  });
  t.exports = l, t.exports.default = l, t.exports.AbortError = s;
})), fE = /* @__PURE__ */ Px(Hz(), 1), zz = void 0, Kz = void 0;
function Wz() {
  return {
    geminiUrl: zz,
    vertexUrl: Kz
  };
}
function Gz(e, t, n, r) {
  var s, a;
  if (!e?.baseUrl) {
    const o = Wz();
    return t ? (s = o.vertexUrl) !== null && s !== void 0 ? s : n : (a = o.geminiUrl) !== null && a !== void 0 ? a : r;
  }
  return e.baseUrl;
}
var Po = class {
};
function Ze(e, t) {
  return e.replace(/\{([^}]+)\}/g, (n, r) => {
    if (Object.prototype.hasOwnProperty.call(t, r)) {
      const s = t[r];
      return s != null ? String(s) : "";
    } else throw new Error(`Key '${r}' not found in valueMap.`);
  });
}
function h(e, t, n) {
  for (let a = 0; a < t.length - 1; a++) {
    const o = t[a];
    if (o.endsWith("[]")) {
      const l = o.slice(0, -2);
      if (!(l in e)) if (Array.isArray(n)) e[l] = Array.from({ length: n.length }, () => ({}));
      else throw new Error(`Value must be a list given an array path ${o}`);
      if (Array.isArray(e[l])) {
        const i = e[l];
        if (Array.isArray(n)) for (let u = 0; u < i.length; u++) {
          const d = i[u];
          h(d, t.slice(a + 1), n[u]);
        }
        else for (const u of i) h(u, t.slice(a + 1), n);
      }
      return;
    } else if (o.endsWith("[0]")) {
      const l = o.slice(0, -3);
      l in e || (e[l] = [{}]);
      const i = e[l];
      h(i[0], t.slice(a + 1), n);
      return;
    }
    (!e[o] || typeof e[o] != "object") && (e[o] = {}), e = e[o];
  }
  const r = t[t.length - 1], s = e[r];
  if (s !== void 0) {
    if (!n || typeof n == "object" && Object.keys(n).length === 0 || n === s) return;
    if (typeof s == "object" && typeof n == "object" && s !== null && n !== null) Object.assign(s, n);
    else throw new Error(`Cannot set value for an existing key. Key: ${r}`);
  } else r === "_self" && typeof n == "object" && n !== null && !Array.isArray(n) ? Object.assign(e, n) : e[r] = n;
}
function c(e, t, n = void 0) {
  try {
    if (t.length === 1 && t[0] === "_self") return e;
    for (let r = 0; r < t.length; r++) {
      if (typeof e != "object" || e === null) return n;
      const s = t[r];
      if (s.endsWith("[]")) {
        const a = s.slice(0, -2);
        if (a in e) {
          const o = e[a];
          return Array.isArray(o) ? o.map((l) => c(l, t.slice(r + 1), n)) : n;
        } else return n;
      } else e = e[s];
    }
    return e;
  } catch (r) {
    if (r instanceof TypeError) return n;
    throw r;
  }
}
function Vz(e, t) {
  for (const [n, r] of Object.entries(t)) {
    const s = n.split("."), a = r.split("."), o = /* @__PURE__ */ new Set();
    let l = -1;
    for (let i = 0; i < s.length; i++) if (s[i] === "*") {
      l = i;
      break;
    }
    if (l !== -1 && a.length > l) for (let i = l; i < a.length; i++) {
      const u = a[i];
      u !== "*" && !u.endsWith("[]") && !u.endsWith("[0]") && o.add(u);
    }
    d0(e, s, a, 0, o);
  }
}
function d0(e, t, n, r, s) {
  if (r >= t.length || typeof e != "object" || e === null) return;
  const a = t[r];
  if (a.endsWith("[]")) {
    const o = a.slice(0, -2), l = e;
    if (o in l && Array.isArray(l[o])) for (const i of l[o]) d0(i, t, n, r + 1, s);
  } else if (a === "*") {
    if (typeof e == "object" && e !== null && !Array.isArray(e)) {
      const o = e, l = Object.keys(o).filter((u) => !u.startsWith("_") && !s.has(u)), i = {};
      for (const u of l) i[u] = o[u];
      for (const [u, d] of Object.entries(i)) {
        const f = [];
        for (const m of n.slice(r)) m === "*" ? f.push(u) : f.push(m);
        h(o, f, d);
      }
      for (const u of l) delete o[u];
    }
  } else {
    const o = e;
    a in o && d0(o[a], t, n, r + 1, s);
  }
}
function Ww(e) {
  if (typeof e != "string") throw new Error("fromImageBytes must be a string");
  return e;
}
function Jz(e) {
  const t = {}, n = c(e, ["operationName"]);
  n != null && h(t, ["operationName"], n);
  const r = c(e, ["resourceName"]);
  return r != null && h(t, ["_url", "resourceName"], r), t;
}
function Yz(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && h(t, ["done"], s);
  const a = c(e, ["error"]);
  a != null && h(t, ["error"], a);
  const o = c(e, ["response", "generateVideoResponse"]);
  return o != null && h(t, ["response"], jz(o)), t;
}
function Xz(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && h(t, ["done"], s);
  const a = c(e, ["error"]);
  a != null && h(t, ["error"], a);
  const o = c(e, ["response"]);
  return o != null && h(t, ["response"], Qz(o)), t;
}
function jz(e) {
  const t = {}, n = c(e, ["generatedSamples"]);
  if (n != null) {
    let a = n;
    Array.isArray(a) && (a = a.map((o) => Zz(o))), h(t, ["generatedVideos"], a);
  }
  const r = c(e, ["raiMediaFilteredCount"]);
  r != null && h(t, ["raiMediaFilteredCount"], r);
  const s = c(e, ["raiMediaFilteredReasons"]);
  return s != null && h(t, ["raiMediaFilteredReasons"], s), t;
}
function Qz(e) {
  const t = {}, n = c(e, ["videos"]);
  if (n != null) {
    let a = n;
    Array.isArray(a) && (a = a.map((o) => eK(o))), h(t, ["generatedVideos"], a);
  }
  const r = c(e, ["raiMediaFilteredCount"]);
  r != null && h(t, ["raiMediaFilteredCount"], r);
  const s = c(e, ["raiMediaFilteredReasons"]);
  return s != null && h(t, ["raiMediaFilteredReasons"], s), t;
}
function Zz(e) {
  const t = {}, n = c(e, ["video"]);
  return n != null && h(t, ["video"], oK(n)), t;
}
function eK(e) {
  const t = {}, n = c(e, ["_self"]);
  return n != null && h(t, ["video"], iK(n)), t;
}
function tK(e) {
  const t = {}, n = c(e, ["operationName"]);
  return n != null && h(t, ["_url", "operationName"], n), t;
}
function nK(e) {
  const t = {}, n = c(e, ["operationName"]);
  return n != null && h(t, ["_url", "operationName"], n), t;
}
function rK(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && h(t, ["done"], s);
  const a = c(e, ["error"]);
  a != null && h(t, ["error"], a);
  const o = c(e, ["response"]);
  return o != null && h(t, ["response"], sK(o)), t;
}
function sK(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && h(t, ["parent"], r);
  const s = c(e, ["documentName"]);
  return s != null && h(t, ["documentName"], s), t;
}
function eP(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && h(t, ["done"], s);
  const a = c(e, ["error"]);
  a != null && h(t, ["error"], a);
  const o = c(e, ["response"]);
  return o != null && h(t, ["response"], aK(o)), t;
}
function aK(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && h(t, ["parent"], r);
  const s = c(e, ["documentName"]);
  return s != null && h(t, ["documentName"], s), t;
}
function oK(e) {
  const t = {}, n = c(e, ["uri"]);
  n != null && h(t, ["uri"], n);
  const r = c(e, ["encodedVideo"]);
  r != null && h(t, ["videoBytes"], Ww(r));
  const s = c(e, ["encoding"]);
  return s != null && h(t, ["mimeType"], s), t;
}
function iK(e) {
  const t = {}, n = c(e, ["gcsUri"]);
  n != null && h(t, ["uri"], n);
  const r = c(e, ["bytesBase64Encoded"]);
  r != null && h(t, ["videoBytes"], Ww(r));
  const s = c(e, ["mimeType"]);
  return s != null && h(t, ["mimeType"], s), t;
}
var hE;
(function(e) {
  e.LANGUAGE_UNSPECIFIED = "LANGUAGE_UNSPECIFIED", e.PYTHON = "PYTHON";
})(hE || (hE = {}));
var pE;
(function(e) {
  e.OUTCOME_UNSPECIFIED = "OUTCOME_UNSPECIFIED", e.OUTCOME_OK = "OUTCOME_OK", e.OUTCOME_FAILED = "OUTCOME_FAILED", e.OUTCOME_DEADLINE_EXCEEDED = "OUTCOME_DEADLINE_EXCEEDED";
})(pE || (pE = {}));
var mE;
(function(e) {
  e.SCHEDULING_UNSPECIFIED = "SCHEDULING_UNSPECIFIED", e.SILENT = "SILENT", e.WHEN_IDLE = "WHEN_IDLE", e.INTERRUPT = "INTERRUPT";
})(mE || (mE = {}));
var ui;
(function(e) {
  e.TYPE_UNSPECIFIED = "TYPE_UNSPECIFIED", e.STRING = "STRING", e.NUMBER = "NUMBER", e.INTEGER = "INTEGER", e.BOOLEAN = "BOOLEAN", e.ARRAY = "ARRAY", e.OBJECT = "OBJECT", e.NULL = "NULL";
})(ui || (ui = {}));
var gE;
(function(e) {
  e.ENVIRONMENT_UNSPECIFIED = "ENVIRONMENT_UNSPECIFIED", e.ENVIRONMENT_BROWSER = "ENVIRONMENT_BROWSER";
})(gE || (gE = {}));
var vE;
(function(e) {
  e.AUTH_TYPE_UNSPECIFIED = "AUTH_TYPE_UNSPECIFIED", e.NO_AUTH = "NO_AUTH", e.API_KEY_AUTH = "API_KEY_AUTH", e.HTTP_BASIC_AUTH = "HTTP_BASIC_AUTH", e.GOOGLE_SERVICE_ACCOUNT_AUTH = "GOOGLE_SERVICE_ACCOUNT_AUTH", e.OAUTH = "OAUTH", e.OIDC_AUTH = "OIDC_AUTH";
})(vE || (vE = {}));
var yE;
(function(e) {
  e.HTTP_IN_UNSPECIFIED = "HTTP_IN_UNSPECIFIED", e.HTTP_IN_QUERY = "HTTP_IN_QUERY", e.HTTP_IN_HEADER = "HTTP_IN_HEADER", e.HTTP_IN_PATH = "HTTP_IN_PATH", e.HTTP_IN_BODY = "HTTP_IN_BODY", e.HTTP_IN_COOKIE = "HTTP_IN_COOKIE";
})(yE || (yE = {}));
var _E;
(function(e) {
  e.API_SPEC_UNSPECIFIED = "API_SPEC_UNSPECIFIED", e.SIMPLE_SEARCH = "SIMPLE_SEARCH", e.ELASTIC_SEARCH = "ELASTIC_SEARCH";
})(_E || (_E = {}));
var bE;
(function(e) {
  e.PHISH_BLOCK_THRESHOLD_UNSPECIFIED = "PHISH_BLOCK_THRESHOLD_UNSPECIFIED", e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_HIGH_AND_ABOVE = "BLOCK_HIGH_AND_ABOVE", e.BLOCK_HIGHER_AND_ABOVE = "BLOCK_HIGHER_AND_ABOVE", e.BLOCK_VERY_HIGH_AND_ABOVE = "BLOCK_VERY_HIGH_AND_ABOVE", e.BLOCK_ONLY_EXTREMELY_HIGH = "BLOCK_ONLY_EXTREMELY_HIGH";
})(bE || (bE = {}));
var wE;
(function(e) {
  e.UNSPECIFIED = "UNSPECIFIED", e.BLOCKING = "BLOCKING", e.NON_BLOCKING = "NON_BLOCKING";
})(wE || (wE = {}));
var SE;
(function(e) {
  e.MODE_UNSPECIFIED = "MODE_UNSPECIFIED", e.MODE_DYNAMIC = "MODE_DYNAMIC";
})(SE || (SE = {}));
var f0;
(function(e) {
  e.MODE_UNSPECIFIED = "MODE_UNSPECIFIED", e.AUTO = "AUTO", e.ANY = "ANY", e.NONE = "NONE", e.VALIDATED = "VALIDATED";
})(f0 || (f0 = {}));
var Vd;
(function(e) {
  e.THINKING_LEVEL_UNSPECIFIED = "THINKING_LEVEL_UNSPECIFIED", e.MINIMAL = "MINIMAL", e.LOW = "LOW", e.MEDIUM = "MEDIUM", e.HIGH = "HIGH";
})(Vd || (Vd = {}));
var AE;
(function(e) {
  e.DONT_ALLOW = "DONT_ALLOW", e.ALLOW_ADULT = "ALLOW_ADULT", e.ALLOW_ALL = "ALLOW_ALL";
})(AE || (AE = {}));
var TE;
(function(e) {
  e.PROMINENT_PEOPLE_UNSPECIFIED = "PROMINENT_PEOPLE_UNSPECIFIED", e.ALLOW_PROMINENT_PEOPLE = "ALLOW_PROMINENT_PEOPLE", e.BLOCK_PROMINENT_PEOPLE = "BLOCK_PROMINENT_PEOPLE";
})(TE || (TE = {}));
var EE;
(function(e) {
  e.HARM_CATEGORY_UNSPECIFIED = "HARM_CATEGORY_UNSPECIFIED", e.HARM_CATEGORY_HARASSMENT = "HARM_CATEGORY_HARASSMENT", e.HARM_CATEGORY_HATE_SPEECH = "HARM_CATEGORY_HATE_SPEECH", e.HARM_CATEGORY_SEXUALLY_EXPLICIT = "HARM_CATEGORY_SEXUALLY_EXPLICIT", e.HARM_CATEGORY_DANGEROUS_CONTENT = "HARM_CATEGORY_DANGEROUS_CONTENT", e.HARM_CATEGORY_CIVIC_INTEGRITY = "HARM_CATEGORY_CIVIC_INTEGRITY", e.HARM_CATEGORY_IMAGE_HATE = "HARM_CATEGORY_IMAGE_HATE", e.HARM_CATEGORY_IMAGE_DANGEROUS_CONTENT = "HARM_CATEGORY_IMAGE_DANGEROUS_CONTENT", e.HARM_CATEGORY_IMAGE_HARASSMENT = "HARM_CATEGORY_IMAGE_HARASSMENT", e.HARM_CATEGORY_IMAGE_SEXUALLY_EXPLICIT = "HARM_CATEGORY_IMAGE_SEXUALLY_EXPLICIT", e.HARM_CATEGORY_JAILBREAK = "HARM_CATEGORY_JAILBREAK";
})(EE || (EE = {}));
var CE;
(function(e) {
  e.HARM_BLOCK_METHOD_UNSPECIFIED = "HARM_BLOCK_METHOD_UNSPECIFIED", e.SEVERITY = "SEVERITY", e.PROBABILITY = "PROBABILITY";
})(CE || (CE = {}));
var IE;
(function(e) {
  e.HARM_BLOCK_THRESHOLD_UNSPECIFIED = "HARM_BLOCK_THRESHOLD_UNSPECIFIED", e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_ONLY_HIGH = "BLOCK_ONLY_HIGH", e.BLOCK_NONE = "BLOCK_NONE", e.OFF = "OFF";
})(IE || (IE = {}));
var xE;
(function(e) {
  e.FINISH_REASON_UNSPECIFIED = "FINISH_REASON_UNSPECIFIED", e.STOP = "STOP", e.MAX_TOKENS = "MAX_TOKENS", e.SAFETY = "SAFETY", e.RECITATION = "RECITATION", e.LANGUAGE = "LANGUAGE", e.OTHER = "OTHER", e.BLOCKLIST = "BLOCKLIST", e.PROHIBITED_CONTENT = "PROHIBITED_CONTENT", e.SPII = "SPII", e.MALFORMED_FUNCTION_CALL = "MALFORMED_FUNCTION_CALL", e.IMAGE_SAFETY = "IMAGE_SAFETY", e.UNEXPECTED_TOOL_CALL = "UNEXPECTED_TOOL_CALL", e.IMAGE_PROHIBITED_CONTENT = "IMAGE_PROHIBITED_CONTENT", e.NO_IMAGE = "NO_IMAGE", e.IMAGE_RECITATION = "IMAGE_RECITATION", e.IMAGE_OTHER = "IMAGE_OTHER";
})(xE || (xE = {}));
var kE;
(function(e) {
  e.HARM_PROBABILITY_UNSPECIFIED = "HARM_PROBABILITY_UNSPECIFIED", e.NEGLIGIBLE = "NEGLIGIBLE", e.LOW = "LOW", e.MEDIUM = "MEDIUM", e.HIGH = "HIGH";
})(kE || (kE = {}));
var ME;
(function(e) {
  e.HARM_SEVERITY_UNSPECIFIED = "HARM_SEVERITY_UNSPECIFIED", e.HARM_SEVERITY_NEGLIGIBLE = "HARM_SEVERITY_NEGLIGIBLE", e.HARM_SEVERITY_LOW = "HARM_SEVERITY_LOW", e.HARM_SEVERITY_MEDIUM = "HARM_SEVERITY_MEDIUM", e.HARM_SEVERITY_HIGH = "HARM_SEVERITY_HIGH";
})(ME || (ME = {}));
var PE;
(function(e) {
  e.URL_RETRIEVAL_STATUS_UNSPECIFIED = "URL_RETRIEVAL_STATUS_UNSPECIFIED", e.URL_RETRIEVAL_STATUS_SUCCESS = "URL_RETRIEVAL_STATUS_SUCCESS", e.URL_RETRIEVAL_STATUS_ERROR = "URL_RETRIEVAL_STATUS_ERROR", e.URL_RETRIEVAL_STATUS_PAYWALL = "URL_RETRIEVAL_STATUS_PAYWALL", e.URL_RETRIEVAL_STATUS_UNSAFE = "URL_RETRIEVAL_STATUS_UNSAFE";
})(PE || (PE = {}));
var RE;
(function(e) {
  e.BLOCKED_REASON_UNSPECIFIED = "BLOCKED_REASON_UNSPECIFIED", e.SAFETY = "SAFETY", e.OTHER = "OTHER", e.BLOCKLIST = "BLOCKLIST", e.PROHIBITED_CONTENT = "PROHIBITED_CONTENT", e.IMAGE_SAFETY = "IMAGE_SAFETY", e.MODEL_ARMOR = "MODEL_ARMOR", e.JAILBREAK = "JAILBREAK";
})(RE || (RE = {}));
var NE;
(function(e) {
  e.TRAFFIC_TYPE_UNSPECIFIED = "TRAFFIC_TYPE_UNSPECIFIED", e.ON_DEMAND = "ON_DEMAND", e.ON_DEMAND_PRIORITY = "ON_DEMAND_PRIORITY", e.ON_DEMAND_FLEX = "ON_DEMAND_FLEX", e.PROVISIONED_THROUGHPUT = "PROVISIONED_THROUGHPUT";
})(NE || (NE = {}));
var sg;
(function(e) {
  e.MODALITY_UNSPECIFIED = "MODALITY_UNSPECIFIED", e.TEXT = "TEXT", e.IMAGE = "IMAGE", e.AUDIO = "AUDIO", e.VIDEO = "VIDEO";
})(sg || (sg = {}));
var $E;
(function(e) {
  e.MODEL_STAGE_UNSPECIFIED = "MODEL_STAGE_UNSPECIFIED", e.UNSTABLE_EXPERIMENTAL = "UNSTABLE_EXPERIMENTAL", e.EXPERIMENTAL = "EXPERIMENTAL", e.PREVIEW = "PREVIEW", e.STABLE = "STABLE", e.LEGACY = "LEGACY", e.DEPRECATED = "DEPRECATED", e.RETIRED = "RETIRED";
})($E || ($E = {}));
var LE;
(function(e) {
  e.MEDIA_RESOLUTION_UNSPECIFIED = "MEDIA_RESOLUTION_UNSPECIFIED", e.MEDIA_RESOLUTION_LOW = "MEDIA_RESOLUTION_LOW", e.MEDIA_RESOLUTION_MEDIUM = "MEDIA_RESOLUTION_MEDIUM", e.MEDIA_RESOLUTION_HIGH = "MEDIA_RESOLUTION_HIGH";
})(LE || (LE = {}));
var DE;
(function(e) {
  e.TUNING_MODE_UNSPECIFIED = "TUNING_MODE_UNSPECIFIED", e.TUNING_MODE_FULL = "TUNING_MODE_FULL", e.TUNING_MODE_PEFT_ADAPTER = "TUNING_MODE_PEFT_ADAPTER";
})(DE || (DE = {}));
var OE;
(function(e) {
  e.ADAPTER_SIZE_UNSPECIFIED = "ADAPTER_SIZE_UNSPECIFIED", e.ADAPTER_SIZE_ONE = "ADAPTER_SIZE_ONE", e.ADAPTER_SIZE_TWO = "ADAPTER_SIZE_TWO", e.ADAPTER_SIZE_FOUR = "ADAPTER_SIZE_FOUR", e.ADAPTER_SIZE_EIGHT = "ADAPTER_SIZE_EIGHT", e.ADAPTER_SIZE_SIXTEEN = "ADAPTER_SIZE_SIXTEEN", e.ADAPTER_SIZE_THIRTY_TWO = "ADAPTER_SIZE_THIRTY_TWO";
})(OE || (OE = {}));
var h0;
(function(e) {
  e.JOB_STATE_UNSPECIFIED = "JOB_STATE_UNSPECIFIED", e.JOB_STATE_QUEUED = "JOB_STATE_QUEUED", e.JOB_STATE_PENDING = "JOB_STATE_PENDING", e.JOB_STATE_RUNNING = "JOB_STATE_RUNNING", e.JOB_STATE_SUCCEEDED = "JOB_STATE_SUCCEEDED", e.JOB_STATE_FAILED = "JOB_STATE_FAILED", e.JOB_STATE_CANCELLING = "JOB_STATE_CANCELLING", e.JOB_STATE_CANCELLED = "JOB_STATE_CANCELLED", e.JOB_STATE_PAUSED = "JOB_STATE_PAUSED", e.JOB_STATE_EXPIRED = "JOB_STATE_EXPIRED", e.JOB_STATE_UPDATING = "JOB_STATE_UPDATING", e.JOB_STATE_PARTIALLY_SUCCEEDED = "JOB_STATE_PARTIALLY_SUCCEEDED";
})(h0 || (h0 = {}));
var FE;
(function(e) {
  e.TUNING_JOB_STATE_UNSPECIFIED = "TUNING_JOB_STATE_UNSPECIFIED", e.TUNING_JOB_STATE_WAITING_FOR_QUOTA = "TUNING_JOB_STATE_WAITING_FOR_QUOTA", e.TUNING_JOB_STATE_PROCESSING_DATASET = "TUNING_JOB_STATE_PROCESSING_DATASET", e.TUNING_JOB_STATE_WAITING_FOR_CAPACITY = "TUNING_JOB_STATE_WAITING_FOR_CAPACITY", e.TUNING_JOB_STATE_TUNING = "TUNING_JOB_STATE_TUNING", e.TUNING_JOB_STATE_POST_PROCESSING = "TUNING_JOB_STATE_POST_PROCESSING";
})(FE || (FE = {}));
var BE;
(function(e) {
  e.AGGREGATION_METRIC_UNSPECIFIED = "AGGREGATION_METRIC_UNSPECIFIED", e.AVERAGE = "AVERAGE", e.MODE = "MODE", e.STANDARD_DEVIATION = "STANDARD_DEVIATION", e.VARIANCE = "VARIANCE", e.MINIMUM = "MINIMUM", e.MAXIMUM = "MAXIMUM", e.MEDIAN = "MEDIAN", e.PERCENTILE_P90 = "PERCENTILE_P90", e.PERCENTILE_P95 = "PERCENTILE_P95", e.PERCENTILE_P99 = "PERCENTILE_P99";
})(BE || (BE = {}));
var UE;
(function(e) {
  e.PAIRWISE_CHOICE_UNSPECIFIED = "PAIRWISE_CHOICE_UNSPECIFIED", e.BASELINE = "BASELINE", e.CANDIDATE = "CANDIDATE", e.TIE = "TIE";
})(UE || (UE = {}));
var qE;
(function(e) {
  e.TUNING_TASK_UNSPECIFIED = "TUNING_TASK_UNSPECIFIED", e.TUNING_TASK_I2V = "TUNING_TASK_I2V", e.TUNING_TASK_T2V = "TUNING_TASK_T2V", e.TUNING_TASK_R2V = "TUNING_TASK_R2V";
})(qE || (qE = {}));
var HE;
(function(e) {
  e.STATE_UNSPECIFIED = "STATE_UNSPECIFIED", e.STATE_PENDING = "STATE_PENDING", e.STATE_ACTIVE = "STATE_ACTIVE", e.STATE_FAILED = "STATE_FAILED";
})(HE || (HE = {}));
var zE;
(function(e) {
  e.MEDIA_RESOLUTION_UNSPECIFIED = "MEDIA_RESOLUTION_UNSPECIFIED", e.MEDIA_RESOLUTION_LOW = "MEDIA_RESOLUTION_LOW", e.MEDIA_RESOLUTION_MEDIUM = "MEDIA_RESOLUTION_MEDIUM", e.MEDIA_RESOLUTION_HIGH = "MEDIA_RESOLUTION_HIGH", e.MEDIA_RESOLUTION_ULTRA_HIGH = "MEDIA_RESOLUTION_ULTRA_HIGH";
})(zE || (zE = {}));
var KE;
(function(e) {
  e.TOOL_TYPE_UNSPECIFIED = "TOOL_TYPE_UNSPECIFIED", e.GOOGLE_SEARCH_WEB = "GOOGLE_SEARCH_WEB", e.GOOGLE_SEARCH_IMAGE = "GOOGLE_SEARCH_IMAGE", e.URL_CONTEXT = "URL_CONTEXT", e.GOOGLE_MAPS = "GOOGLE_MAPS", e.FILE_SEARCH = "FILE_SEARCH";
})(KE || (KE = {}));
var p0;
(function(e) {
  e.COLLECTION = "COLLECTION";
})(p0 || (p0 = {}));
var WE;
(function(e) {
  e.UNSPECIFIED = "unspecified", e.FLEX = "flex", e.STANDARD = "standard", e.PRIORITY = "priority";
})(WE || (WE = {}));
var GE;
(function(e) {
  e.FEATURE_SELECTION_PREFERENCE_UNSPECIFIED = "FEATURE_SELECTION_PREFERENCE_UNSPECIFIED", e.PRIORITIZE_QUALITY = "PRIORITIZE_QUALITY", e.BALANCED = "BALANCED", e.PRIORITIZE_COST = "PRIORITIZE_COST";
})(GE || (GE = {}));
var ag;
(function(e) {
  e.PREDICT = "PREDICT", e.EMBED_CONTENT = "EMBED_CONTENT";
})(ag || (ag = {}));
var VE;
(function(e) {
  e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_ONLY_HIGH = "BLOCK_ONLY_HIGH", e.BLOCK_NONE = "BLOCK_NONE";
})(VE || (VE = {}));
var JE;
(function(e) {
  e.auto = "auto", e.en = "en", e.ja = "ja", e.ko = "ko", e.hi = "hi", e.zh = "zh", e.pt = "pt", e.es = "es";
})(JE || (JE = {}));
var YE;
(function(e) {
  e.MASK_MODE_DEFAULT = "MASK_MODE_DEFAULT", e.MASK_MODE_USER_PROVIDED = "MASK_MODE_USER_PROVIDED", e.MASK_MODE_BACKGROUND = "MASK_MODE_BACKGROUND", e.MASK_MODE_FOREGROUND = "MASK_MODE_FOREGROUND", e.MASK_MODE_SEMANTIC = "MASK_MODE_SEMANTIC";
})(YE || (YE = {}));
var XE;
(function(e) {
  e.CONTROL_TYPE_DEFAULT = "CONTROL_TYPE_DEFAULT", e.CONTROL_TYPE_CANNY = "CONTROL_TYPE_CANNY", e.CONTROL_TYPE_SCRIBBLE = "CONTROL_TYPE_SCRIBBLE", e.CONTROL_TYPE_FACE_MESH = "CONTROL_TYPE_FACE_MESH";
})(XE || (XE = {}));
var jE;
(function(e) {
  e.SUBJECT_TYPE_DEFAULT = "SUBJECT_TYPE_DEFAULT", e.SUBJECT_TYPE_PERSON = "SUBJECT_TYPE_PERSON", e.SUBJECT_TYPE_ANIMAL = "SUBJECT_TYPE_ANIMAL", e.SUBJECT_TYPE_PRODUCT = "SUBJECT_TYPE_PRODUCT";
})(jE || (jE = {}));
var QE;
(function(e) {
  e.EDIT_MODE_DEFAULT = "EDIT_MODE_DEFAULT", e.EDIT_MODE_INPAINT_REMOVAL = "EDIT_MODE_INPAINT_REMOVAL", e.EDIT_MODE_INPAINT_INSERTION = "EDIT_MODE_INPAINT_INSERTION", e.EDIT_MODE_OUTPAINT = "EDIT_MODE_OUTPAINT", e.EDIT_MODE_CONTROLLED_EDITING = "EDIT_MODE_CONTROLLED_EDITING", e.EDIT_MODE_STYLE = "EDIT_MODE_STYLE", e.EDIT_MODE_BGSWAP = "EDIT_MODE_BGSWAP", e.EDIT_MODE_PRODUCT_IMAGE = "EDIT_MODE_PRODUCT_IMAGE";
})(QE || (QE = {}));
var ZE;
(function(e) {
  e.FOREGROUND = "FOREGROUND", e.BACKGROUND = "BACKGROUND", e.PROMPT = "PROMPT", e.SEMANTIC = "SEMANTIC", e.INTERACTIVE = "INTERACTIVE";
})(ZE || (ZE = {}));
var eC;
(function(e) {
  e.ASSET = "ASSET", e.STYLE = "STYLE";
})(eC || (eC = {}));
var tC;
(function(e) {
  e.INSERT = "INSERT", e.REMOVE = "REMOVE", e.REMOVE_STATIC = "REMOVE_STATIC", e.OUTPAINT = "OUTPAINT";
})(tC || (tC = {}));
var nC;
(function(e) {
  e.OPTIMIZED = "OPTIMIZED", e.LOSSLESS = "LOSSLESS";
})(nC || (nC = {}));
var rC;
(function(e) {
  e.SUPERVISED_FINE_TUNING = "SUPERVISED_FINE_TUNING", e.PREFERENCE_TUNING = "PREFERENCE_TUNING", e.DISTILLATION = "DISTILLATION";
})(rC || (rC = {}));
var sC;
(function(e) {
  e.STATE_UNSPECIFIED = "STATE_UNSPECIFIED", e.PROCESSING = "PROCESSING", e.ACTIVE = "ACTIVE", e.FAILED = "FAILED";
})(sC || (sC = {}));
var aC;
(function(e) {
  e.SOURCE_UNSPECIFIED = "SOURCE_UNSPECIFIED", e.UPLOADED = "UPLOADED", e.GENERATED = "GENERATED", e.REGISTERED = "REGISTERED";
})(aC || (aC = {}));
var oC;
(function(e) {
  e.TURN_COMPLETE_REASON_UNSPECIFIED = "TURN_COMPLETE_REASON_UNSPECIFIED", e.MALFORMED_FUNCTION_CALL = "MALFORMED_FUNCTION_CALL", e.RESPONSE_REJECTED = "RESPONSE_REJECTED", e.NEED_MORE_INPUT = "NEED_MORE_INPUT", e.PROHIBITED_INPUT_CONTENT = "PROHIBITED_INPUT_CONTENT", e.IMAGE_PROHIBITED_INPUT_CONTENT = "IMAGE_PROHIBITED_INPUT_CONTENT", e.INPUT_TEXT_CONTAIN_PROMINENT_PERSON_PROHIBITED = "INPUT_TEXT_CONTAIN_PROMINENT_PERSON_PROHIBITED", e.INPUT_IMAGE_CELEBRITY = "INPUT_IMAGE_CELEBRITY", e.INPUT_IMAGE_PHOTO_REALISTIC_CHILD_PROHIBITED = "INPUT_IMAGE_PHOTO_REALISTIC_CHILD_PROHIBITED", e.INPUT_TEXT_NCII_PROHIBITED = "INPUT_TEXT_NCII_PROHIBITED", e.INPUT_OTHER = "INPUT_OTHER", e.INPUT_IP_PROHIBITED = "INPUT_IP_PROHIBITED", e.BLOCKLIST = "BLOCKLIST", e.UNSAFE_PROMPT_FOR_IMAGE_GENERATION = "UNSAFE_PROMPT_FOR_IMAGE_GENERATION", e.GENERATED_IMAGE_SAFETY = "GENERATED_IMAGE_SAFETY", e.GENERATED_CONTENT_SAFETY = "GENERATED_CONTENT_SAFETY", e.GENERATED_AUDIO_SAFETY = "GENERATED_AUDIO_SAFETY", e.GENERATED_VIDEO_SAFETY = "GENERATED_VIDEO_SAFETY", e.GENERATED_CONTENT_PROHIBITED = "GENERATED_CONTENT_PROHIBITED", e.GENERATED_CONTENT_BLOCKLIST = "GENERATED_CONTENT_BLOCKLIST", e.GENERATED_IMAGE_PROHIBITED = "GENERATED_IMAGE_PROHIBITED", e.GENERATED_IMAGE_CELEBRITY = "GENERATED_IMAGE_CELEBRITY", e.GENERATED_IMAGE_PROMINENT_PEOPLE_DETECTED_BY_REWRITER = "GENERATED_IMAGE_PROMINENT_PEOPLE_DETECTED_BY_REWRITER", e.GENERATED_IMAGE_IDENTIFIABLE_PEOPLE = "GENERATED_IMAGE_IDENTIFIABLE_PEOPLE", e.GENERATED_IMAGE_MINORS = "GENERATED_IMAGE_MINORS", e.OUTPUT_IMAGE_IP_PROHIBITED = "OUTPUT_IMAGE_IP_PROHIBITED", e.GENERATED_OTHER = "GENERATED_OTHER", e.MAX_REGENERATION_REACHED = "MAX_REGENERATION_REACHED";
})(oC || (oC = {}));
var iC;
(function(e) {
  e.MODALITY_UNSPECIFIED = "MODALITY_UNSPECIFIED", e.TEXT = "TEXT", e.IMAGE = "IMAGE", e.VIDEO = "VIDEO", e.AUDIO = "AUDIO", e.DOCUMENT = "DOCUMENT";
})(iC || (iC = {}));
var lC;
(function(e) {
  e.VAD_SIGNAL_TYPE_UNSPECIFIED = "VAD_SIGNAL_TYPE_UNSPECIFIED", e.VAD_SIGNAL_TYPE_SOS = "VAD_SIGNAL_TYPE_SOS", e.VAD_SIGNAL_TYPE_EOS = "VAD_SIGNAL_TYPE_EOS";
})(lC || (lC = {}));
var uC;
(function(e) {
  e.TYPE_UNSPECIFIED = "TYPE_UNSPECIFIED", e.ACTIVITY_START = "ACTIVITY_START", e.ACTIVITY_END = "ACTIVITY_END";
})(uC || (uC = {}));
var cC;
(function(e) {
  e.START_SENSITIVITY_UNSPECIFIED = "START_SENSITIVITY_UNSPECIFIED", e.START_SENSITIVITY_HIGH = "START_SENSITIVITY_HIGH", e.START_SENSITIVITY_LOW = "START_SENSITIVITY_LOW";
})(cC || (cC = {}));
var dC;
(function(e) {
  e.END_SENSITIVITY_UNSPECIFIED = "END_SENSITIVITY_UNSPECIFIED", e.END_SENSITIVITY_HIGH = "END_SENSITIVITY_HIGH", e.END_SENSITIVITY_LOW = "END_SENSITIVITY_LOW";
})(dC || (dC = {}));
var fC;
(function(e) {
  e.ACTIVITY_HANDLING_UNSPECIFIED = "ACTIVITY_HANDLING_UNSPECIFIED", e.START_OF_ACTIVITY_INTERRUPTS = "START_OF_ACTIVITY_INTERRUPTS", e.NO_INTERRUPTION = "NO_INTERRUPTION";
})(fC || (fC = {}));
var hC;
(function(e) {
  e.TURN_COVERAGE_UNSPECIFIED = "TURN_COVERAGE_UNSPECIFIED", e.TURN_INCLUDES_ONLY_ACTIVITY = "TURN_INCLUDES_ONLY_ACTIVITY", e.TURN_INCLUDES_ALL_INPUT = "TURN_INCLUDES_ALL_INPUT", e.TURN_INCLUDES_AUDIO_ACTIVITY_AND_ALL_VIDEO = "TURN_INCLUDES_AUDIO_ACTIVITY_AND_ALL_VIDEO";
})(hC || (hC = {}));
var pC;
(function(e) {
  e.SCALE_UNSPECIFIED = "SCALE_UNSPECIFIED", e.C_MAJOR_A_MINOR = "C_MAJOR_A_MINOR", e.D_FLAT_MAJOR_B_FLAT_MINOR = "D_FLAT_MAJOR_B_FLAT_MINOR", e.D_MAJOR_B_MINOR = "D_MAJOR_B_MINOR", e.E_FLAT_MAJOR_C_MINOR = "E_FLAT_MAJOR_C_MINOR", e.E_MAJOR_D_FLAT_MINOR = "E_MAJOR_D_FLAT_MINOR", e.F_MAJOR_D_MINOR = "F_MAJOR_D_MINOR", e.G_FLAT_MAJOR_E_FLAT_MINOR = "G_FLAT_MAJOR_E_FLAT_MINOR", e.G_MAJOR_E_MINOR = "G_MAJOR_E_MINOR", e.A_FLAT_MAJOR_F_MINOR = "A_FLAT_MAJOR_F_MINOR", e.A_MAJOR_G_FLAT_MINOR = "A_MAJOR_G_FLAT_MINOR", e.B_FLAT_MAJOR_G_MINOR = "B_FLAT_MAJOR_G_MINOR", e.B_MAJOR_A_FLAT_MINOR = "B_MAJOR_A_FLAT_MINOR";
})(pC || (pC = {}));
var mC;
(function(e) {
  e.MUSIC_GENERATION_MODE_UNSPECIFIED = "MUSIC_GENERATION_MODE_UNSPECIFIED", e.QUALITY = "QUALITY", e.DIVERSITY = "DIVERSITY", e.VOCALIZATION = "VOCALIZATION";
})(mC || (mC = {}));
var wu;
(function(e) {
  e.PLAYBACK_CONTROL_UNSPECIFIED = "PLAYBACK_CONTROL_UNSPECIFIED", e.PLAY = "PLAY", e.PAUSE = "PAUSE", e.STOP = "STOP", e.RESET_CONTEXT = "RESET_CONTEXT";
})(wu || (wu = {}));
var m0 = class {
  constructor(e) {
    const t = {};
    for (const n of e.headers.entries()) t[n[0]] = n[1];
    this.headers = t, this.responseInternal = e;
  }
  json() {
    return this.responseInternal.json();
  }
}, nd = class {
  get text() {
    var e, t, n, r, s, a, o, l;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning text from the first one.");
    let i = "", u = !1;
    const d = [];
    for (const f of (l = (o = (a = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || a === void 0 ? void 0 : a.content) === null || o === void 0 ? void 0 : o.parts) !== null && l !== void 0 ? l : []) {
      for (const [m, v] of Object.entries(f)) m !== "text" && m !== "thought" && m !== "thoughtSignature" && (v !== null || v !== void 0) && d.push(m);
      if (typeof f.text == "string") {
        if (typeof f.thought == "boolean" && f.thought) continue;
        u = !0, i += f.text;
      }
    }
    return d.length > 0 && console.warn(`there are non-text parts ${d} in the response, returning concatenation of all text parts. Please refer to the non text parts for a full response from model.`), u ? i : void 0;
  }
  get data() {
    var e, t, n, r, s, a, o, l;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning data from the first one.");
    let i = "";
    const u = [];
    for (const d of (l = (o = (a = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || a === void 0 ? void 0 : a.content) === null || o === void 0 ? void 0 : o.parts) !== null && l !== void 0 ? l : []) {
      for (const [f, m] of Object.entries(d)) f !== "inlineData" && (m !== null || m !== void 0) && u.push(f);
      d.inlineData && typeof d.inlineData.data == "string" && (i += atob(d.inlineData.data));
    }
    return u.length > 0 && console.warn(`there are non-data parts ${u} in the response, returning concatenation of all data parts. Please refer to the non data parts for a full response from model.`), i.length > 0 ? btoa(i) : void 0;
  }
  get functionCalls() {
    var e, t, n, r, s, a, o, l;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning function calls from the first one.");
    const i = (l = (o = (a = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || a === void 0 ? void 0 : a.content) === null || o === void 0 ? void 0 : o.parts) === null || l === void 0 ? void 0 : l.filter((u) => u.functionCall).map((u) => u.functionCall).filter((u) => u !== void 0);
    if (i?.length !== 0)
      return i;
  }
  get executableCode() {
    var e, t, n, r, s, a, o, l, i;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning executable code from the first one.");
    const u = (l = (o = (a = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || a === void 0 ? void 0 : a.content) === null || o === void 0 ? void 0 : o.parts) === null || l === void 0 ? void 0 : l.filter((d) => d.executableCode).map((d) => d.executableCode).filter((d) => d !== void 0);
    if (u?.length !== 0)
      return (i = u?.[0]) === null || i === void 0 ? void 0 : i.code;
  }
  get codeExecutionResult() {
    var e, t, n, r, s, a, o, l, i;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning code execution result from the first one.");
    const u = (l = (o = (a = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || a === void 0 ? void 0 : a.content) === null || o === void 0 ? void 0 : o.parts) === null || l === void 0 ? void 0 : l.filter((d) => d.codeExecutionResult).map((d) => d.codeExecutionResult).filter((d) => d !== void 0);
    if (u?.length !== 0)
      return (i = u?.[0]) === null || i === void 0 ? void 0 : i.output;
  }
}, gC = class {
}, vC = class {
}, lK = class {
}, uK = class {
}, cK = class {
}, dK = class {
}, yC = class {
}, _C = class {
}, bC = class {
}, fK = class {
}, wC = class tP {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new tP();
    let s;
    const a = t;
    return n ? s = Xz(a) : s = Yz(a), Object.assign(r, s), r;
  }
}, SC = class {
}, AC = class {
}, TC = class {
}, EC = class {
}, hK = class {
}, pK = class {
}, mK = class {
}, gK = class nP {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new nP(), s = rK(t);
    return Object.assign(r, s), r;
  }
}, vK = class {
}, yK = class {
}, _K = class {
}, bK = class {
}, CC = class {
}, wK = class {
  get text() {
    var e, t, n;
    let r = "", s = !1;
    const a = [];
    for (const o of (n = (t = (e = this.serverContent) === null || e === void 0 ? void 0 : e.modelTurn) === null || t === void 0 ? void 0 : t.parts) !== null && n !== void 0 ? n : []) {
      for (const [l, i] of Object.entries(o)) l !== "text" && l !== "thought" && i !== null && a.push(l);
      if (typeof o.text == "string") {
        if (typeof o.thought == "boolean" && o.thought) continue;
        s = !0, r += o.text;
      }
    }
    return a.length > 0 && console.warn(`there are non-text parts ${a} in the response, returning concatenation of all text parts. Please refer to the non text parts for a full response from model.`), s ? r : void 0;
  }
  get data() {
    var e, t, n;
    let r = "";
    const s = [];
    for (const a of (n = (t = (e = this.serverContent) === null || e === void 0 ? void 0 : e.modelTurn) === null || t === void 0 ? void 0 : t.parts) !== null && n !== void 0 ? n : []) {
      for (const [o, l] of Object.entries(a)) o !== "inlineData" && l !== null && s.push(o);
      a.inlineData && typeof a.inlineData.data == "string" && (r += atob(a.inlineData.data));
    }
    return s.length > 0 && console.warn(`there are non-data parts ${s} in the response, returning concatenation of all data parts. Please refer to the non data parts for a full response from model.`), r.length > 0 ? btoa(r) : void 0;
  }
}, SK = class {
  get audioChunk() {
    if (this.serverContent && this.serverContent.audioChunks && this.serverContent.audioChunks.length > 0) return this.serverContent.audioChunks[0];
  }
}, AK = class rP {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new rP(), s = eP(t);
    return Object.assign(r, s), r;
  }
};
function Zt(e, t) {
  if (!t || typeof t != "string") throw new Error("model is required and must be a string");
  if (t.includes("..") || t.includes("?") || t.includes("&")) throw new Error("invalid model parameter");
  if (e.isVertexAI()) {
    if (t.startsWith("publishers/") || t.startsWith("projects/") || t.startsWith("models/")) return t;
    if (t.indexOf("/") >= 0) {
      const n = t.split("/", 2);
      return `publishers/${n[0]}/models/${n[1]}`;
    } else return `publishers/google/models/${t}`;
  } else return t.startsWith("models/") || t.startsWith("tunedModels/") ? t : `models/${t}`;
}
function sP(e, t) {
  const n = Zt(e, t);
  return n ? n.startsWith("publishers/") && e.isVertexAI() ? `projects/${e.getProject()}/locations/${e.getLocation()}/${n}` : n.startsWith("models/") && e.isVertexAI() ? `projects/${e.getProject()}/locations/${e.getLocation()}/publishers/google/${n}` : n : "";
}
function aP(e) {
  return Array.isArray(e) ? e.map((t) => og(t)) : [og(e)];
}
function og(e) {
  if (typeof e == "object" && e !== null) return e;
  throw new Error(`Could not parse input as Blob. Unsupported blob type: ${typeof e}`);
}
function oP(e) {
  const t = og(e);
  if (t.mimeType && t.mimeType.startsWith("image/")) return t;
  throw new Error(`Unsupported mime type: ${t.mimeType}`);
}
function iP(e) {
  const t = og(e);
  if (t.mimeType && t.mimeType.startsWith("audio/")) return t;
  throw new Error(`Unsupported mime type: ${t.mimeType}`);
}
function IC(e) {
  if (e == null) throw new Error("PartUnion is required");
  if (typeof e == "object") return e;
  if (typeof e == "string") return { text: e };
  throw new Error(`Unsupported part type: ${typeof e}`);
}
function lP(e) {
  if (e == null || Array.isArray(e) && e.length === 0) throw new Error("PartListUnion is required");
  return Array.isArray(e) ? e.map((t) => IC(t)) : [IC(e)];
}
function g0(e) {
  return e != null && typeof e == "object" && "parts" in e && Array.isArray(e.parts);
}
function xC(e) {
  return e != null && typeof e == "object" && "functionCall" in e;
}
function kC(e) {
  return e != null && typeof e == "object" && "functionResponse" in e;
}
function cr(e) {
  if (e == null) throw new Error("ContentUnion is required");
  return g0(e) ? e : {
    role: "user",
    parts: lP(e)
  };
}
function Gw(e, t) {
  if (!t) return [];
  if (e.isVertexAI() && Array.isArray(t)) return t.flatMap((n) => {
    const r = cr(n);
    return r.parts && r.parts.length > 0 && r.parts[0].text !== void 0 ? [r.parts[0].text] : [];
  });
  if (e.isVertexAI()) {
    const n = cr(t);
    return n.parts && n.parts.length > 0 && n.parts[0].text !== void 0 ? [n.parts[0].text] : [];
  }
  return Array.isArray(t) ? t.map((n) => cr(n)) : [cr(t)];
}
function Yr(e) {
  if (e == null || Array.isArray(e) && e.length === 0) throw new Error("contents are required");
  if (!Array.isArray(e)) {
    if (xC(e) || kC(e)) throw new Error("To specify functionCall or functionResponse parts, please wrap them in a Content object, specifying the role for them");
    return [cr(e)];
  }
  const t = [], n = [], r = g0(e[0]);
  for (const s of e) {
    const a = g0(s);
    if (a != r) throw new Error("Mixing Content and Parts is not supported, please group the parts into a the appropriate Content objects and specify the roles for them");
    if (a) t.push(s);
    else {
      if (xC(s) || kC(s)) throw new Error("To specify functionCall or functionResponse parts, please wrap them, and any other parts, in Content objects as appropriate, specifying the role for them");
      n.push(s);
    }
  }
  return r || t.push({
    role: "user",
    parts: lP(n)
  }), t;
}
function TK(e, t) {
  e.includes("null") && (t.nullable = !0);
  const n = e.filter((r) => r !== "null");
  if (n.length === 1) t.type = Object.values(ui).includes(n[0].toUpperCase()) ? n[0].toUpperCase() : ui.TYPE_UNSPECIFIED;
  else {
    t.anyOf = [];
    for (const r of n) t.anyOf.push({ type: Object.values(ui).includes(r.toUpperCase()) ? r.toUpperCase() : ui.TYPE_UNSPECIFIED });
  }
}
function Du(e) {
  const t = {}, n = ["items"], r = ["anyOf"], s = ["properties"];
  if (e.type && e.anyOf) throw new Error("type and anyOf cannot be both populated.");
  const a = e.anyOf;
  a != null && a.length == 2 && (a[0].type === "null" ? (t.nullable = !0, e = a[1]) : a[1].type === "null" && (t.nullable = !0, e = a[0])), e.type instanceof Array && TK(e.type, t);
  for (const [o, l] of Object.entries(e))
    if (l != null)
      if (o == "type") {
        if (l === "null") throw new Error("type: null can not be the only possible type for the field.");
        if (l instanceof Array) continue;
        t.type = Object.values(ui).includes(l.toUpperCase()) ? l.toUpperCase() : ui.TYPE_UNSPECIFIED;
      } else if (n.includes(o)) t[o] = Du(l);
      else if (r.includes(o)) {
        const i = [];
        for (const u of l) {
          if (u.type == "null") {
            t.nullable = !0;
            continue;
          }
          i.push(Du(u));
        }
        t[o] = i;
      } else if (s.includes(o)) {
        const i = {};
        for (const [u, d] of Object.entries(l)) i[u] = Du(d);
        t[o] = i;
      } else {
        if (o === "additionalProperties") continue;
        t[o] = l;
      }
  return t;
}
function Vw(e) {
  return Du(e);
}
function Jw(e) {
  if (typeof e == "object") return e;
  if (typeof e == "string") return { voiceConfig: { prebuiltVoiceConfig: { voiceName: e } } };
  throw new Error(`Unsupported speechConfig type: ${typeof e}`);
}
function Yw(e) {
  if ("multiSpeakerVoiceConfig" in e) throw new Error("multiSpeakerVoiceConfig is not supported in the live API.");
  return e;
}
function dc(e) {
  if (e.functionDeclarations) for (const t of e.functionDeclarations)
    t.parameters && (Object.keys(t.parameters).includes("$schema") ? t.parametersJsonSchema || (t.parametersJsonSchema = t.parameters, delete t.parameters) : t.parameters = Du(t.parameters)), t.response && (Object.keys(t.response).includes("$schema") ? t.responseJsonSchema || (t.responseJsonSchema = t.response, delete t.response) : t.response = Du(t.response));
  return e;
}
function fc(e) {
  if (e == null) throw new Error("tools is required");
  if (!Array.isArray(e)) throw new Error("tools is required and must be an array of Tools");
  const t = [];
  for (const n of e) t.push(n);
  return t;
}
function EK(e, t, n, r = 1) {
  const s = !t.startsWith(`${n}/`) && t.split("/").length === r;
  return e.isVertexAI() ? t.startsWith("projects/") ? t : t.startsWith("locations/") ? `projects/${e.getProject()}/${t}` : t.startsWith(`${n}/`) ? `projects/${e.getProject()}/locations/${e.getLocation()}/${t}` : s ? `projects/${e.getProject()}/locations/${e.getLocation()}/${n}/${t}` : t : s ? `${n}/${t}` : t;
}
function Ro(e, t) {
  if (typeof t != "string") throw new Error("name must be a string");
  return EK(e, t, "cachedContents");
}
function uP(e) {
  switch (e) {
    case "STATE_UNSPECIFIED":
      return "JOB_STATE_UNSPECIFIED";
    case "CREATING":
      return "JOB_STATE_RUNNING";
    case "ACTIVE":
      return "JOB_STATE_SUCCEEDED";
    case "FAILED":
      return "JOB_STATE_FAILED";
    default:
      return e;
  }
}
function Ti(e) {
  return Ww(e);
}
function CK(e) {
  return e != null && typeof e == "object" && "name" in e;
}
function IK(e) {
  return e != null && typeof e == "object" && "video" in e;
}
function xK(e) {
  return e != null && typeof e == "object" && "uri" in e;
}
function cP(e) {
  var t;
  let n;
  if (CK(e) && (n = e.name), !(xK(e) && (n = e.uri, n === void 0)) && !(IK(e) && (n = (t = e.video) === null || t === void 0 ? void 0 : t.uri, n === void 0))) {
    if (typeof e == "string" && (n = e), n === void 0) throw new Error("Could not extract file name from the provided input.");
    if (n.startsWith("https://")) {
      const r = n.split("files/")[1].match(/[a-z0-9]+/);
      if (r === null) throw new Error(`Could not extract file name from URI ${n}`);
      n = r[0];
    } else n.startsWith("files/") && (n = n.split("files/")[1]);
    return n;
  }
}
function dP(e, t) {
  let n;
  return e.isVertexAI() ? n = t ? "publishers/google/models" : "models" : n = t ? "models" : "tunedModels", n;
}
function fP(e) {
  for (const t of [
    "models",
    "tunedModels",
    "publisherModels"
  ]) if (kK(e, t)) return e[t];
  return [];
}
function kK(e, t) {
  return e !== null && typeof e == "object" && t in e;
}
function MK(e, t = {}) {
  const n = e, r = {
    name: n.name,
    description: n.description,
    parametersJsonSchema: n.inputSchema
  };
  return n.outputSchema && (r.responseJsonSchema = n.outputSchema), t.behavior && (r.behavior = t.behavior), { functionDeclarations: [r] };
}
function PK(e, t = {}) {
  const n = [], r = /* @__PURE__ */ new Set();
  for (const s of e) {
    const a = s.name;
    if (r.has(a)) throw new Error(`Duplicate function name ${a} found in MCP tools. Please ensure function names are unique.`);
    r.add(a);
    const o = MK(s, t);
    o.functionDeclarations && n.push(...o.functionDeclarations);
  }
  return { functionDeclarations: n };
}
function hP(e, t) {
  let n;
  if (typeof t == "string") if (e.isVertexAI()) if (t.startsWith("gs://")) n = {
    format: "jsonl",
    gcsUri: [t]
  };
  else if (t.startsWith("bq://")) n = {
    format: "bigquery",
    bigqueryUri: t
  };
  else throw new Error(`Unsupported string source for Vertex AI: ${t}`);
  else if (t.startsWith("files/")) n = { fileName: t };
  else throw new Error(`Unsupported string source for Gemini API: ${t}`);
  else if (Array.isArray(t)) {
    if (e.isVertexAI()) throw new Error("InlinedRequest[] is not supported in Vertex AI.");
    n = { inlinedRequests: t };
  } else n = t;
  const r = [n.gcsUri, n.bigqueryUri].filter(Boolean).length, s = [n.inlinedRequests, n.fileName].filter(Boolean).length;
  if (e.isVertexAI()) {
    if (s > 0 || r !== 1) throw new Error("Exactly one of `gcsUri` or `bigqueryUri` must be set for Vertex AI.");
  } else if (r > 0 || s !== 1) throw new Error("Exactly one of `inlinedRequests`, `fileName`, must be set for Gemini API.");
  return n;
}
function RK(e) {
  if (typeof e != "string") return e;
  const t = e;
  if (t.startsWith("gs://")) return {
    format: "jsonl",
    gcsUri: t
  };
  if (t.startsWith("bq://")) return {
    format: "bigquery",
    bigqueryUri: t
  };
  throw new Error(`Unsupported destination: ${t}`);
}
function pP(e) {
  if (typeof e != "object" || e === null) return {};
  const t = e, n = t.inlinedResponses;
  if (typeof n != "object" || n === null) return e;
  const r = n.inlinedResponses;
  if (!Array.isArray(r) || r.length === 0) return e;
  let s = !1;
  for (const a of r) {
    if (typeof a != "object" || a === null) continue;
    const o = a.response;
    if (!(typeof o != "object" || o === null) && o.embedding !== void 0) {
      s = !0;
      break;
    }
  }
  return s && (t.inlinedEmbedContentResponses = t.inlinedResponses, delete t.inlinedResponses), e;
}
function hc(e, t) {
  const n = t;
  if (!e.isVertexAI()) {
    if (/batches\/[^/]+$/.test(n)) return n.split("/").pop();
    throw new Error(`Invalid batch job name: ${n}.`);
  }
  if (/^projects\/[^/]+\/locations\/[^/]+\/batchPredictionJobs\/[^/]+$/.test(n)) return n.split("/").pop();
  if (/^\d+$/.test(n)) return n;
  throw new Error(`Invalid batch job name: ${n}.`);
}
function mP(e) {
  const t = e;
  return t === "BATCH_STATE_UNSPECIFIED" ? "JOB_STATE_UNSPECIFIED" : t === "BATCH_STATE_PENDING" ? "JOB_STATE_PENDING" : t === "BATCH_STATE_RUNNING" ? "JOB_STATE_RUNNING" : t === "BATCH_STATE_SUCCEEDED" ? "JOB_STATE_SUCCEEDED" : t === "BATCH_STATE_FAILED" ? "JOB_STATE_FAILED" : t === "BATCH_STATE_CANCELLED" ? "JOB_STATE_CANCELLED" : t === "BATCH_STATE_EXPIRED" ? "JOB_STATE_EXPIRED" : t;
}
function NK(e) {
  return e.includes("gemini") && e !== "gemini-embedding-001" || e.includes("maas");
}
function $K(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && h(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function LK(e) {
  const t = {}, n = c(e, ["responsesFile"]);
  n != null && h(t, ["fileName"], n);
  const r = c(e, ["inlinedResponses", "inlinedResponses"]);
  if (r != null) {
    let a = r;
    Array.isArray(a) && (a = a.map((o) => mW(o))), h(t, ["inlinedResponses"], a);
  }
  const s = c(e, ["inlinedEmbedContentResponses", "inlinedResponses"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => o)), h(t, ["inlinedEmbedContentResponses"], a);
  }
  return t;
}
function DK(e) {
  const t = {}, n = c(e, ["predictionsFormat"]);
  n != null && h(t, ["format"], n);
  const r = c(e, ["gcsDestination", "outputUriPrefix"]);
  r != null && h(t, ["gcsUri"], r);
  const s = c(e, ["bigqueryDestination", "outputUri"]);
  return s != null && h(t, ["bigqueryUri"], s), t;
}
function OK(e) {
  const t = {}, n = c(e, ["format"]);
  n != null && h(t, ["predictionsFormat"], n);
  const r = c(e, ["gcsUri"]);
  r != null && h(t, ["gcsDestination", "outputUriPrefix"], r);
  const s = c(e, ["bigqueryUri"]);
  if (s != null && h(t, ["bigqueryDestination", "outputUri"], s), c(e, ["fileName"]) !== void 0) throw new Error("fileName parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedResponses"]) !== void 0) throw new Error("inlinedResponses parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedEmbedContentResponses"]) !== void 0) throw new Error("inlinedEmbedContentResponses parameter is not supported in Vertex AI.");
  return t;
}
function Yp(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata", "displayName"]);
  r != null && h(t, ["displayName"], r);
  const s = c(e, ["metadata", "state"]);
  s != null && h(t, ["state"], mP(s));
  const a = c(e, ["metadata", "createTime"]);
  a != null && h(t, ["createTime"], a);
  const o = c(e, ["metadata", "endTime"]);
  o != null && h(t, ["endTime"], o);
  const l = c(e, ["metadata", "updateTime"]);
  l != null && h(t, ["updateTime"], l);
  const i = c(e, ["metadata", "model"]);
  i != null && h(t, ["model"], i);
  const u = c(e, ["metadata", "output"]);
  return u != null && h(t, ["dest"], LK(pP(u))), t;
}
function v0(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["displayName"]);
  r != null && h(t, ["displayName"], r);
  const s = c(e, ["state"]);
  s != null && h(t, ["state"], mP(s));
  const a = c(e, ["error"]);
  a != null && h(t, ["error"], a);
  const o = c(e, ["createTime"]);
  o != null && h(t, ["createTime"], o);
  const l = c(e, ["startTime"]);
  l != null && h(t, ["startTime"], l);
  const i = c(e, ["endTime"]);
  i != null && h(t, ["endTime"], i);
  const u = c(e, ["updateTime"]);
  u != null && h(t, ["updateTime"], u);
  const d = c(e, ["model"]);
  d != null && h(t, ["model"], d);
  const f = c(e, ["inputConfig"]);
  f != null && h(t, ["src"], FK(f));
  const m = c(e, ["outputConfig"]);
  m != null && h(t, ["dest"], DK(pP(m)));
  const v = c(e, ["completionStats"]);
  return v != null && h(t, ["completionStats"], v), t;
}
function FK(e) {
  const t = {}, n = c(e, ["instancesFormat"]);
  n != null && h(t, ["format"], n);
  const r = c(e, ["gcsSource", "uris"]);
  r != null && h(t, ["gcsUri"], r);
  const s = c(e, ["bigquerySource", "inputUri"]);
  return s != null && h(t, ["bigqueryUri"], s), t;
}
function BK(e, t) {
  const n = {};
  if (c(t, ["format"]) !== void 0) throw new Error("format parameter is not supported in Gemini API.");
  if (c(t, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  if (c(t, ["bigqueryUri"]) !== void 0) throw new Error("bigqueryUri parameter is not supported in Gemini API.");
  const r = c(t, ["fileName"]);
  r != null && h(n, ["fileName"], r);
  const s = c(t, ["inlinedRequests"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => pW(e, o))), h(n, ["requests", "requests"], a);
  }
  return n;
}
function UK(e) {
  const t = {}, n = c(e, ["format"]);
  n != null && h(t, ["instancesFormat"], n);
  const r = c(e, ["gcsUri"]);
  r != null && h(t, ["gcsSource", "uris"], r);
  const s = c(e, ["bigqueryUri"]);
  if (s != null && h(t, ["bigquerySource", "inputUri"], s), c(e, ["fileName"]) !== void 0) throw new Error("fileName parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedRequests"]) !== void 0) throw new Error("inlinedRequests parameter is not supported in Vertex AI.");
  return t;
}
function qK(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && h(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function HK(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], hc(e, r)), n;
}
function zK(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], hc(e, r)), n;
}
function KK(e) {
  const t = {}, n = c(e, ["content"]);
  n != null && h(t, ["content"], n);
  const r = c(e, ["citationMetadata"]);
  r != null && h(t, ["citationMetadata"], WK(r));
  const s = c(e, ["tokenCount"]);
  s != null && h(t, ["tokenCount"], s);
  const a = c(e, ["finishReason"]);
  a != null && h(t, ["finishReason"], a);
  const o = c(e, ["groundingMetadata"]);
  o != null && h(t, ["groundingMetadata"], o);
  const l = c(e, ["avgLogprobs"]);
  l != null && h(t, ["avgLogprobs"], l);
  const i = c(e, ["index"]);
  i != null && h(t, ["index"], i);
  const u = c(e, ["logprobsResult"]);
  u != null && h(t, ["logprobsResult"], u);
  const d = c(e, ["safetyRatings"]);
  if (d != null) {
    let m = d;
    Array.isArray(m) && (m = m.map((v) => v)), h(t, ["safetyRatings"], m);
  }
  const f = c(e, ["urlContextMetadata"]);
  return f != null && h(t, ["urlContextMetadata"], f), t;
}
function WK(e) {
  const t = {}, n = c(e, ["citationSources"]);
  if (n != null) {
    let r = n;
    Array.isArray(r) && (r = r.map((s) => s)), h(t, ["citations"], r);
  }
  return t;
}
function gP(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((a) => SW(a))), h(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function GK(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  if (t !== void 0 && r != null && h(t, ["batch", "displayName"], r), c(e, ["dest"]) !== void 0) throw new Error("dest parameter is not supported in Gemini API.");
  const s = c(e, ["webhookConfig"]);
  return t !== void 0 && s != null && h(t, ["batch", "webhookConfig"], s), n;
}
function VK(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  t !== void 0 && r != null && h(t, ["displayName"], r);
  const s = c(e, ["dest"]);
  if (t !== void 0 && s != null && h(t, ["outputConfig"], OK(RK(s))), c(e, ["webhookConfig"]) !== void 0) throw new Error("webhookConfig parameter is not supported in Vertex AI.");
  return n;
}
function MC(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["_url", "model"], Zt(e, r));
  const s = c(t, ["src"]);
  s != null && h(n, ["batch", "inputConfig"], BK(e, hP(e, s)));
  const a = c(t, ["config"]);
  return a != null && GK(a, n), n;
}
function JK(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["model"], Zt(e, r));
  const s = c(t, ["src"]);
  s != null && h(n, ["inputConfig"], UK(hP(e, s)));
  const a = c(t, ["config"]);
  return a != null && VK(a, n), n;
}
function YK(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  return t !== void 0 && r != null && h(t, ["batch", "displayName"], r), n;
}
function XK(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["_url", "model"], Zt(e, r));
  const s = c(t, ["src"]);
  s != null && h(n, ["batch", "inputConfig"], rW(e, s));
  const a = c(t, ["config"]);
  return a != null && YK(a, n), n;
}
function jK(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], hc(e, r)), n;
}
function QK(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], hc(e, r)), n;
}
function ZK(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["name"]);
  r != null && h(t, ["name"], r);
  const s = c(e, ["done"]);
  s != null && h(t, ["done"], s);
  const a = c(e, ["error"]);
  return a != null && h(t, ["error"], a), t;
}
function eW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["name"]);
  r != null && h(t, ["name"], r);
  const s = c(e, ["done"]);
  s != null && h(t, ["done"], s);
  const a = c(e, ["error"]);
  return a != null && h(t, ["error"], a), t;
}
function tW(e, t) {
  const n = {}, r = c(t, ["contents"]);
  if (r != null) {
    let a = Gw(e, r);
    Array.isArray(a) && (a = a.map((o) => o)), h(n, [
      "requests[]",
      "request",
      "content"
    ], a);
  }
  const s = c(t, ["config"]);
  return s != null && (h(n, ["_self"], nW(s, n)), Vz(n, { "requests[].*": "requests[].request.*" })), n;
}
function nW(e, t) {
  const n = {}, r = c(e, ["taskType"]);
  t !== void 0 && r != null && h(t, ["requests[]", "taskType"], r);
  const s = c(e, ["title"]);
  t !== void 0 && s != null && h(t, ["requests[]", "title"], s);
  const a = c(e, ["outputDimensionality"]);
  if (t !== void 0 && a != null && h(t, ["requests[]", "outputDimensionality"], a), c(e, ["mimeType"]) !== void 0) throw new Error("mimeType parameter is not supported in Gemini API.");
  if (c(e, ["autoTruncate"]) !== void 0) throw new Error("autoTruncate parameter is not supported in Gemini API.");
  if (c(e, ["documentOcr"]) !== void 0) throw new Error("documentOcr parameter is not supported in Gemini API.");
  if (c(e, ["audioTrackExtraction"]) !== void 0) throw new Error("audioTrackExtraction parameter is not supported in Gemini API.");
  return n;
}
function rW(e, t) {
  const n = {}, r = c(t, ["fileName"]);
  r != null && h(n, ["file_name"], r);
  const s = c(t, ["inlinedRequests"]);
  return s != null && h(n, ["requests"], tW(e, s)), n;
}
function sW(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && h(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function aW(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && h(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && h(t, ["args"], r);
  const s = c(e, ["name"]);
  if (s != null && h(t, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function oW(e) {
  const t = {}, n = c(e, ["allowedFunctionNames"]);
  n != null && h(t, ["allowedFunctionNames"], n);
  const r = c(e, ["mode"]);
  if (r != null && h(t, ["mode"], r), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return t;
}
function iW(e, t, n) {
  const r = {}, s = c(t, ["systemInstruction"]);
  n !== void 0 && s != null && h(n, ["systemInstruction"], gP(cr(s)));
  const a = c(t, ["temperature"]);
  a != null && h(r, ["temperature"], a);
  const o = c(t, ["topP"]);
  o != null && h(r, ["topP"], o);
  const l = c(t, ["topK"]);
  l != null && h(r, ["topK"], l);
  const i = c(t, ["candidateCount"]);
  i != null && h(r, ["candidateCount"], i);
  const u = c(t, ["maxOutputTokens"]);
  u != null && h(r, ["maxOutputTokens"], u);
  const d = c(t, ["stopSequences"]);
  d != null && h(r, ["stopSequences"], d);
  const f = c(t, ["responseLogprobs"]);
  f != null && h(r, ["responseLogprobs"], f);
  const m = c(t, ["logprobs"]);
  m != null && h(r, ["logprobs"], m);
  const v = c(t, ["presencePenalty"]);
  v != null && h(r, ["presencePenalty"], v);
  const y = c(t, ["frequencyPenalty"]);
  y != null && h(r, ["frequencyPenalty"], y);
  const S = c(t, ["seed"]);
  S != null && h(r, ["seed"], S);
  const x = c(t, ["responseMimeType"]);
  x != null && h(r, ["responseMimeType"], x);
  const I = c(t, ["responseSchema"]);
  I != null && h(r, ["responseSchema"], Vw(I));
  const E = c(t, ["responseJsonSchema"]);
  if (E != null && h(r, ["responseJsonSchema"], E), c(t, ["routingConfig"]) !== void 0) throw new Error("routingConfig parameter is not supported in Gemini API.");
  if (c(t, ["modelSelectionConfig"]) !== void 0) throw new Error("modelSelectionConfig parameter is not supported in Gemini API.");
  const N = c(t, ["safetySettings"]);
  if (n !== void 0 && N != null) {
    let ee = N;
    Array.isArray(ee) && (ee = ee.map((J) => AW(J))), h(n, ["safetySettings"], ee);
  }
  const $ = c(t, ["tools"]);
  if (n !== void 0 && $ != null) {
    let ee = fc($);
    Array.isArray(ee) && (ee = ee.map((J) => EW(dc(J)))), h(n, ["tools"], ee);
  }
  const p = c(t, ["toolConfig"]);
  if (n !== void 0 && p != null && h(n, ["toolConfig"], TW(p)), c(t, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const w = c(t, ["cachedContent"]);
  n !== void 0 && w != null && h(n, ["cachedContent"], Ro(e, w));
  const _ = c(t, ["responseModalities"]);
  _ != null && h(r, ["responseModalities"], _);
  const b = c(t, ["mediaResolution"]);
  b != null && h(r, ["mediaResolution"], b);
  const C = c(t, ["speechConfig"]);
  if (C != null && h(r, ["speechConfig"], Jw(C)), c(t, ["audioTimestamp"]) !== void 0) throw new Error("audioTimestamp parameter is not supported in Gemini API.");
  const k = c(t, ["thinkingConfig"]);
  k != null && h(r, ["thinkingConfig"], k);
  const P = c(t, ["imageConfig"]);
  P != null && h(r, ["imageConfig"], hW(P));
  const H = c(t, ["enableEnhancedCivicAnswers"]);
  if (H != null && h(r, ["enableEnhancedCivicAnswers"], H), c(t, ["modelArmorConfig"]) !== void 0) throw new Error("modelArmorConfig parameter is not supported in Gemini API.");
  const G = c(t, ["serviceTier"]);
  return n !== void 0 && G != null && h(n, ["serviceTier"], G), r;
}
function lW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["candidates"]);
  if (r != null) {
    let u = r;
    Array.isArray(u) && (u = u.map((d) => KK(d))), h(t, ["candidates"], u);
  }
  const s = c(e, ["modelVersion"]);
  s != null && h(t, ["modelVersion"], s);
  const a = c(e, ["promptFeedback"]);
  a != null && h(t, ["promptFeedback"], a);
  const o = c(e, ["responseId"]);
  o != null && h(t, ["responseId"], o);
  const l = c(e, ["usageMetadata"]);
  l != null && h(t, ["usageMetadata"], l);
  const i = c(e, ["modelStatus"]);
  return i != null && h(t, ["modelStatus"], i), t;
}
function uW(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], hc(e, r)), n;
}
function cW(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], hc(e, r)), n;
}
function dW(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && h(t, ["authConfig"], $K(n));
  const r = c(e, ["enableWidget"]);
  return r != null && h(t, ["enableWidget"], r), t;
}
function fW(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && h(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && h(t, ["timeRangeFilter"], r), t;
}
function hW(e) {
  const t = {}, n = c(e, ["aspectRatio"]);
  n != null && h(t, ["aspectRatio"], n);
  const r = c(e, ["imageSize"]);
  if (r != null && h(t, ["imageSize"], r), c(e, ["personGeneration"]) !== void 0) throw new Error("personGeneration parameter is not supported in Gemini API.");
  if (c(e, ["prominentPeople"]) !== void 0) throw new Error("prominentPeople parameter is not supported in Gemini API.");
  if (c(e, ["outputMimeType"]) !== void 0) throw new Error("outputMimeType parameter is not supported in Gemini API.");
  if (c(e, ["outputCompressionQuality"]) !== void 0) throw new Error("outputCompressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["imageOutputOptions"]) !== void 0) throw new Error("imageOutputOptions parameter is not supported in Gemini API.");
  return t;
}
function pW(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["request", "model"], Zt(e, r));
  const s = c(t, ["contents"]);
  if (s != null) {
    let l = Yr(s);
    Array.isArray(l) && (l = l.map((i) => gP(i))), h(n, ["request", "contents"], l);
  }
  const a = c(t, ["metadata"]);
  a != null && h(n, ["metadata"], a);
  const o = c(t, ["config"]);
  return o != null && h(n, ["request", "generationConfig"], iW(e, o, c(n, ["request"], {}))), n;
}
function mW(e) {
  const t = {}, n = c(e, ["response"]);
  n != null && h(t, ["response"], lW(n));
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const s = c(e, ["error"]);
  return s != null && h(t, ["error"], s), t;
}
function gW(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  if (t !== void 0 && s != null && h(t, ["_query", "pageToken"], s), c(e, ["filter"]) !== void 0) throw new Error("filter parameter is not supported in Gemini API.");
  return n;
}
function vW(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  t !== void 0 && s != null && h(t, ["_query", "pageToken"], s);
  const a = c(e, ["filter"]);
  return t !== void 0 && a != null && h(t, ["_query", "filter"], a), n;
}
function yW(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && gW(n, t), t;
}
function _W(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && vW(n, t), t;
}
function bW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const s = c(e, ["operations"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => Yp(o))), h(t, ["batchJobs"], a);
  }
  return t;
}
function wW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const s = c(e, ["batchPredictionJobs"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => v0(o))), h(t, ["batchJobs"], a);
  }
  return t;
}
function SW(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && h(t, ["executableCode"], s);
  const a = c(e, ["fileData"]);
  a != null && h(t, ["fileData"], sW(a));
  const o = c(e, ["functionCall"]);
  o != null && h(t, ["functionCall"], aW(o));
  const l = c(e, ["functionResponse"]);
  l != null && h(t, ["functionResponse"], l);
  const i = c(e, ["inlineData"]);
  i != null && h(t, ["inlineData"], qK(i));
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  m != null && h(t, ["videoMetadata"], m);
  const v = c(e, ["toolCall"]);
  v != null && h(t, ["toolCall"], v);
  const y = c(e, ["toolResponse"]);
  y != null && h(t, ["toolResponse"], y);
  const S = c(e, ["partMetadata"]);
  return S != null && h(t, ["partMetadata"], S), t;
}
function AW(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && h(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && h(t, ["threshold"], r), t;
}
function TW(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && h(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  r != null && h(t, ["functionCallingConfig"], oW(r));
  const s = c(e, ["includeServerSideToolInvocations"]);
  return s != null && h(t, ["includeServerSideToolInvocations"], s), t;
}
function EW(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && h(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && h(t, ["fileSearch"], r);
  const s = c(e, ["googleSearch"]);
  s != null && h(t, ["googleSearch"], fW(s));
  const a = c(e, ["googleMaps"]);
  a != null && h(t, ["googleMaps"], dW(a));
  const o = c(e, ["codeExecution"]);
  if (o != null && h(t, ["codeExecution"], o), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let f = l;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["functionDeclarations"], f);
  }
  const i = c(e, ["googleSearchRetrieval"]);
  if (i != null && h(t, ["googleSearchRetrieval"], i), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && h(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let f = d;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["mcpServers"], f);
  }
  return t;
}
var ko;
(function(e) {
  e.PAGED_ITEM_BATCH_JOBS = "batchJobs", e.PAGED_ITEM_MODELS = "models", e.PAGED_ITEM_TUNING_JOBS = "tuningJobs", e.PAGED_ITEM_FILES = "files", e.PAGED_ITEM_CACHED_CONTENTS = "cachedContents", e.PAGED_ITEM_FILE_SEARCH_STORES = "fileSearchStores", e.PAGED_ITEM_DOCUMENTS = "documents";
})(ko || (ko = {}));
var Fl = class {
  constructor(e, t, n, r) {
    this.pageInternal = [], this.paramsInternal = {}, this.requestInternal = t, this.init(e, n, r);
  }
  init(e, t, n) {
    var r, s;
    this.nameInternal = e, this.pageInternal = t[this.nameInternal] || [], this.sdkHttpResponseInternal = t?.sdkHttpResponse, this.idxInternal = 0;
    let a = { config: {} };
    !n || Object.keys(n).length === 0 ? a = { config: {} } : typeof n == "object" ? a = Object.assign({}, n) : a = n, a.config && (a.config.pageToken = t.nextPageToken), this.paramsInternal = a, this.pageInternalSize = (s = (r = a.config) === null || r === void 0 ? void 0 : r.pageSize) !== null && s !== void 0 ? s : this.pageInternal.length;
  }
  initNextPage(e) {
    this.init(this.nameInternal, e, this.paramsInternal);
  }
  get page() {
    return this.pageInternal;
  }
  get name() {
    return this.nameInternal;
  }
  get pageSize() {
    return this.pageInternalSize;
  }
  get sdkHttpResponse() {
    return this.sdkHttpResponseInternal;
  }
  get params() {
    return this.paramsInternal;
  }
  get pageLength() {
    return this.pageInternal.length;
  }
  getItem(e) {
    return this.pageInternal[e];
  }
  [Symbol.asyncIterator]() {
    return {
      next: async () => {
        if (this.idxInternal >= this.pageLength) if (this.hasNextPage()) await this.nextPage();
        else return {
          value: void 0,
          done: !0
        };
        const e = this.getItem(this.idxInternal);
        return this.idxInternal += 1, {
          value: e,
          done: !1
        };
      },
      return: async () => ({
        value: void 0,
        done: !0
      })
    };
  }
  async nextPage() {
    if (!this.hasNextPage()) throw new Error("No more pages to fetch.");
    const e = await this.requestInternal(this.params);
    return this.initNextPage(e), this.page;
  }
  hasNextPage() {
    var e;
    return ((e = this.params.config) === null || e === void 0 ? void 0 : e.pageToken) !== void 0;
  }
}, CW = class extends Po {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new Fl(ko.PAGED_ITEM_BATCH_JOBS, (n) => this.listInternal(n), await this.listInternal(t), t), this.create = async (t) => (this.apiClient.isVertexAI() && (t.config = this.formatDestination(t.src, t.config)), this.createInternal(t)), this.createEmbeddings = async (t) => {
      if (console.warn("batches.createEmbeddings() is experimental and may change without notice."), this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support batches.createEmbeddings.");
      return this.createEmbeddingsInternal(t);
    };
  }
  createInlinedGenerateContentRequest(e) {
    const t = MC(this.apiClient, e), n = t._url, r = Ze("{model}:batchGenerateContent", n), s = t.batch.inputConfig.requests, a = s.requests, o = [];
    for (const l of a) {
      const i = Object.assign({}, l);
      if (i.systemInstruction) {
        const u = i.systemInstruction;
        delete i.systemInstruction;
        const d = i.request;
        d.systemInstruction = u, i.request = d;
      }
      o.push(i);
    }
    return s.requests = o, delete t.config, delete t._url, delete t._query, {
      path: r,
      body: t
    };
  }
  getGcsUri(e) {
    if (typeof e == "string") return e.startsWith("gs://") ? e : void 0;
    if (!Array.isArray(e) && e.gcsUri && e.gcsUri.length > 0) return e.gcsUri[0];
  }
  getBigqueryUri(e) {
    if (typeof e == "string") return e.startsWith("bq://") ? e : void 0;
    if (!Array.isArray(e)) return e.bigqueryUri;
  }
  formatDestination(e, t) {
    const n = t ? Object.assign({}, t) : {}, r = Date.now().toString();
    if (n.displayName || (n.displayName = `genaiBatchJob_${r}`), n.dest === void 0) {
      const s = this.getGcsUri(e), a = this.getBigqueryUri(e);
      if (s) s.endsWith(".jsonl") ? n.dest = `${s.slice(0, -6)}/dest` : n.dest = `${s}_dest_${r}`;
      else if (a) n.dest = `${a}_dest_${r}`;
      else throw new Error("Unsupported source for Vertex AI: No GCS or BigQuery URI found.");
    }
    return n;
  }
  async createInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = JK(this.apiClient, e);
      return o = Ze("batchPredictionJobs", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a.then((u) => v0(u));
    } else {
      const i = MC(this.apiClient, e);
      return o = Ze("{model}:batchGenerateContent", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a.then((u) => Yp(u));
    }
  }
  async createEmbeddingsInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = XK(this.apiClient, e);
      return s = Ze("{model}:asyncBatchEmbedContent", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => Yp(l));
    }
  }
  async get(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = cW(this.apiClient, e);
      return o = Ze("batchPredictionJobs/{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a.then((u) => v0(u));
    } else {
      const i = uW(this.apiClient, e);
      return o = Ze("batches/{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a.then((u) => Yp(u));
    }
  }
  async cancel(e) {
    var t, n, r, s;
    let a = "", o = {};
    if (this.apiClient.isVertexAI()) {
      const l = zK(this.apiClient, e);
      a = Ze("batchPredictionJobs/{name}:cancel", l._url), o = l._query, delete l._url, delete l._query, await this.apiClient.request({
        path: a,
        queryParams: o,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    } else {
      const l = HK(this.apiClient, e);
      a = Ze("batches/{name}:cancel", l._url), o = l._query, delete l._url, delete l._query, await this.apiClient.request({
        path: a,
        queryParams: o,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = _W(e);
      return o = Ze("batchPredictionJobs", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = wW(u), f = new CC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = yW(e);
      return o = Ze("batches", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = bW(u), f = new CC();
        return Object.assign(f, d), f;
      });
    }
  }
  async delete(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = QK(this.apiClient, e);
      return o = Ze("batchPredictionJobs/{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => eW(u));
    } else {
      const i = jK(this.apiClient, e);
      return o = Ze("batches/{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => ZK(u));
    }
  }
};
function IW(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && h(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function xW(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && h(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function PC(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((a) => jW(a))), h(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function RC(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((a) => QW(a))), h(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function kW(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && h(t, ["ttl"], r);
  const s = c(e, ["expireTime"]);
  t !== void 0 && s != null && h(t, ["expireTime"], s);
  const a = c(e, ["displayName"]);
  t !== void 0 && a != null && h(t, ["displayName"], a);
  const o = c(e, ["contents"]);
  if (t !== void 0 && o != null) {
    let d = Yr(o);
    Array.isArray(d) && (d = d.map((f) => PC(f))), h(t, ["contents"], d);
  }
  const l = c(e, ["systemInstruction"]);
  t !== void 0 && l != null && h(t, ["systemInstruction"], PC(cr(l)));
  const i = c(e, ["tools"]);
  if (t !== void 0 && i != null) {
    let d = i;
    Array.isArray(d) && (d = d.map((f) => tG(f))), h(t, ["tools"], d);
  }
  const u = c(e, ["toolConfig"]);
  if (t !== void 0 && u != null && h(t, ["toolConfig"], ZW(u)), c(e, ["kmsKeyName"]) !== void 0) throw new Error("kmsKeyName parameter is not supported in Gemini API.");
  return n;
}
function MW(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && h(t, ["ttl"], r);
  const s = c(e, ["expireTime"]);
  t !== void 0 && s != null && h(t, ["expireTime"], s);
  const a = c(e, ["displayName"]);
  t !== void 0 && a != null && h(t, ["displayName"], a);
  const o = c(e, ["contents"]);
  if (t !== void 0 && o != null) {
    let f = Yr(o);
    Array.isArray(f) && (f = f.map((m) => RC(m))), h(t, ["contents"], f);
  }
  const l = c(e, ["systemInstruction"]);
  t !== void 0 && l != null && h(t, ["systemInstruction"], RC(cr(l)));
  const i = c(e, ["tools"]);
  if (t !== void 0 && i != null) {
    let f = i;
    Array.isArray(f) && (f = f.map((m) => nG(m))), h(t, ["tools"], f);
  }
  const u = c(e, ["toolConfig"]);
  t !== void 0 && u != null && h(t, ["toolConfig"], eG(u));
  const d = c(e, ["kmsKeyName"]);
  return t !== void 0 && d != null && h(t, ["encryption_spec", "kmsKeyName"], d), n;
}
function PW(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["model"], sP(e, r));
  const s = c(t, ["config"]);
  return s != null && kW(s, n), n;
}
function RW(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["model"], sP(e, r));
  const s = c(t, ["config"]);
  return s != null && MW(s, n), n;
}
function NW(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ro(e, r)), n;
}
function $W(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ro(e, r)), n;
}
function LW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
function DW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
function OW(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && h(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function FW(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && h(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && h(t, ["args"], r);
  const s = c(e, ["name"]);
  if (s != null && h(t, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function BW(e) {
  const t = {}, n = c(e, ["allowedFunctionNames"]);
  n != null && h(t, ["allowedFunctionNames"], n);
  const r = c(e, ["mode"]);
  if (r != null && h(t, ["mode"], r), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return t;
}
function UW(e) {
  const t = {}, n = c(e, ["description"]);
  n != null && h(t, ["description"], n);
  const r = c(e, ["name"]);
  r != null && h(t, ["name"], r);
  const s = c(e, ["parameters"]);
  s != null && h(t, ["parameters"], s);
  const a = c(e, ["parametersJsonSchema"]);
  a != null && h(t, ["parametersJsonSchema"], a);
  const o = c(e, ["response"]);
  o != null && h(t, ["response"], o);
  const l = c(e, ["responseJsonSchema"]);
  if (l != null && h(t, ["responseJsonSchema"], l), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return t;
}
function qW(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ro(e, r)), n;
}
function HW(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && h(n, ["_url", "name"], Ro(e, r)), n;
}
function zW(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && h(t, ["authConfig"], IW(n));
  const r = c(e, ["enableWidget"]);
  return r != null && h(t, ["enableWidget"], r), t;
}
function KW(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && h(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && h(t, ["timeRangeFilter"], r), t;
}
function WW(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && h(t, ["_query", "pageToken"], s), n;
}
function GW(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && h(t, ["_query", "pageToken"], s), n;
}
function VW(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && WW(n, t), t;
}
function JW(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && GW(n, t), t;
}
function YW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const s = c(e, ["cachedContents"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => o)), h(t, ["cachedContents"], a);
  }
  return t;
}
function XW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const s = c(e, ["cachedContents"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => o)), h(t, ["cachedContents"], a);
  }
  return t;
}
function jW(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && h(t, ["executableCode"], s);
  const a = c(e, ["fileData"]);
  a != null && h(t, ["fileData"], OW(a));
  const o = c(e, ["functionCall"]);
  o != null && h(t, ["functionCall"], FW(o));
  const l = c(e, ["functionResponse"]);
  l != null && h(t, ["functionResponse"], l);
  const i = c(e, ["inlineData"]);
  i != null && h(t, ["inlineData"], xW(i));
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  m != null && h(t, ["videoMetadata"], m);
  const v = c(e, ["toolCall"]);
  v != null && h(t, ["toolCall"], v);
  const y = c(e, ["toolResponse"]);
  y != null && h(t, ["toolResponse"], y);
  const S = c(e, ["partMetadata"]);
  return S != null && h(t, ["partMetadata"], S), t;
}
function QW(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && h(t, ["executableCode"], s);
  const a = c(e, ["fileData"]);
  a != null && h(t, ["fileData"], a);
  const o = c(e, ["functionCall"]);
  o != null && h(t, ["functionCall"], o);
  const l = c(e, ["functionResponse"]);
  l != null && h(t, ["functionResponse"], l);
  const i = c(e, ["inlineData"]);
  i != null && h(t, ["inlineData"], i);
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  if (m != null && h(t, ["videoMetadata"], m), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return t;
}
function ZW(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && h(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  r != null && h(t, ["functionCallingConfig"], BW(r));
  const s = c(e, ["includeServerSideToolInvocations"]);
  return s != null && h(t, ["includeServerSideToolInvocations"], s), t;
}
function eG(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && h(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  if (r != null && h(t, ["functionCallingConfig"], r), c(e, ["includeServerSideToolInvocations"]) !== void 0) throw new Error("includeServerSideToolInvocations parameter is not supported in Vertex AI.");
  return t;
}
function tG(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && h(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && h(t, ["fileSearch"], r);
  const s = c(e, ["googleSearch"]);
  s != null && h(t, ["googleSearch"], KW(s));
  const a = c(e, ["googleMaps"]);
  a != null && h(t, ["googleMaps"], zW(a));
  const o = c(e, ["codeExecution"]);
  if (o != null && h(t, ["codeExecution"], o), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let f = l;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["functionDeclarations"], f);
  }
  const i = c(e, ["googleSearchRetrieval"]);
  if (i != null && h(t, ["googleSearchRetrieval"], i), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && h(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let f = d;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["mcpServers"], f);
  }
  return t;
}
function nG(e) {
  const t = {}, n = c(e, ["retrieval"]);
  n != null && h(t, ["retrieval"], n);
  const r = c(e, ["computerUse"]);
  if (r != null && h(t, ["computerUse"], r), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const s = c(e, ["googleSearch"]);
  s != null && h(t, ["googleSearch"], s);
  const a = c(e, ["googleMaps"]);
  a != null && h(t, ["googleMaps"], a);
  const o = c(e, ["codeExecution"]);
  o != null && h(t, ["codeExecution"], o);
  const l = c(e, ["enterpriseWebSearch"]);
  l != null && h(t, ["enterpriseWebSearch"], l);
  const i = c(e, ["functionDeclarations"]);
  if (i != null) {
    let m = i;
    Array.isArray(m) && (m = m.map((v) => UW(v))), h(t, ["functionDeclarations"], m);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  u != null && h(t, ["googleSearchRetrieval"], u);
  const d = c(e, ["parallelAiSearch"]);
  d != null && h(t, ["parallelAiSearch"], d);
  const f = c(e, ["urlContext"]);
  if (f != null && h(t, ["urlContext"], f), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return t;
}
function rG(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && h(t, ["ttl"], r);
  const s = c(e, ["expireTime"]);
  return t !== void 0 && s != null && h(t, ["expireTime"], s), n;
}
function sG(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && h(t, ["ttl"], r);
  const s = c(e, ["expireTime"]);
  return t !== void 0 && s != null && h(t, ["expireTime"], s), n;
}
function aG(e, t) {
  const n = {}, r = c(t, ["name"]);
  r != null && h(n, ["_url", "name"], Ro(e, r));
  const s = c(t, ["config"]);
  return s != null && rG(s, n), n;
}
function oG(e, t) {
  const n = {}, r = c(t, ["name"]);
  r != null && h(n, ["_url", "name"], Ro(e, r));
  const s = c(t, ["config"]);
  return s != null && sG(s, n), n;
}
var iG = class extends Po {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new Fl(ko.PAGED_ITEM_CACHED_CONTENTS, (n) => this.listInternal(n), await this.listInternal(t), t);
  }
  async create(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = RW(this.apiClient, e);
      return o = Ze("cachedContents", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a.then((u) => u);
    } else {
      const i = PW(this.apiClient, e);
      return o = Ze("cachedContents", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a.then((u) => u);
    }
  }
  async get(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = HW(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a.then((u) => u);
    } else {
      const i = qW(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a.then((u) => u);
    }
  }
  async delete(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = $W(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = DW(u), f = new TC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = NW(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = LW(u), f = new TC();
        return Object.assign(f, d), f;
      });
    }
  }
  async update(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = oG(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "PATCH",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a.then((u) => u);
    } else {
      const i = aG(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "PATCH",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a.then((u) => u);
    }
  }
  async listInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = JW(e);
      return o = Ze("cachedContents", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = XW(u), f = new EC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = VW(e);
      return o = Ze("cachedContents", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = YW(u), f = new EC();
        return Object.assign(f, d), f;
      });
    }
  }
};
function ci(e, t) {
  var n = {};
  for (var r in e) Object.prototype.hasOwnProperty.call(e, r) && t.indexOf(r) < 0 && (n[r] = e[r]);
  if (e != null && typeof Object.getOwnPropertySymbols == "function")
    for (var s = 0, r = Object.getOwnPropertySymbols(e); s < r.length; s++) t.indexOf(r[s]) < 0 && Object.prototype.propertyIsEnumerable.call(e, r[s]) && (n[r[s]] = e[r[s]]);
  return n;
}
function NC(e) {
  var t = typeof Symbol == "function" && Symbol.iterator, n = t && e[t], r = 0;
  if (n) return n.call(e);
  if (e && typeof e.length == "number") return { next: function() {
    return e && r >= e.length && (e = void 0), {
      value: e && e[r++],
      done: !e
    };
  } };
  throw new TypeError(t ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function kt(e) {
  return this instanceof kt ? (this.v = e, this) : new kt(e);
}
function aa(e, t, n) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var r = n.apply(e, t || []), s, a = [];
  return s = Object.create((typeof AsyncIterator == "function" ? AsyncIterator : Object).prototype), l("next"), l("throw"), l("return", o), s[Symbol.asyncIterator] = function() {
    return this;
  }, s;
  function o(v) {
    return function(y) {
      return Promise.resolve(y).then(v, f);
    };
  }
  function l(v, y) {
    r[v] && (s[v] = function(S) {
      return new Promise(function(x, I) {
        a.push([
          v,
          S,
          x,
          I
        ]) > 1 || i(v, S);
      });
    }, y && (s[v] = y(s[v])));
  }
  function i(v, y) {
    try {
      u(r[v](y));
    } catch (S) {
      m(a[0][3], S);
    }
  }
  function u(v) {
    v.value instanceof kt ? Promise.resolve(v.value.v).then(d, f) : m(a[0][2], v);
  }
  function d(v) {
    i("next", v);
  }
  function f(v) {
    i("throw", v);
  }
  function m(v, y) {
    v(y), a.shift(), a.length && i(a[0][0], a[0][1]);
  }
}
function oa(e) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var t = e[Symbol.asyncIterator], n;
  return t ? t.call(e) : (e = typeof NC == "function" ? NC(e) : e[Symbol.iterator](), n = {}, r("next"), r("throw"), r("return"), n[Symbol.asyncIterator] = function() {
    return this;
  }, n);
  function r(a) {
    n[a] = e[a] && function(o) {
      return new Promise(function(l, i) {
        o = e[a](o), s(l, i, o.done, o.value);
      });
    };
  }
  function s(a, o, l, i) {
    Promise.resolve(i).then(function(u) {
      a({
        value: u,
        done: l
      });
    }, o);
  }
}
function lG(e) {
  var t;
  if (e.candidates == null || e.candidates.length === 0) return !1;
  const n = (t = e.candidates[0]) === null || t === void 0 ? void 0 : t.content;
  return n === void 0 ? !1 : vP(n);
}
function vP(e) {
  if (e.parts === void 0 || e.parts.length === 0) return !1;
  for (const t of e.parts) if (t === void 0 || Object.keys(t).length === 0) return !1;
  return !0;
}
function uG(e) {
  if (e.length !== 0) {
    for (const t of e) if (t.role !== "user" && t.role !== "model") throw new Error(`Role must be user or model, but got ${t.role}.`);
  }
}
function $C(e) {
  if (e === void 0 || e.length === 0) return [];
  const t = [], n = e.length;
  let r = 0;
  for (; r < n; ) if (e[r].role === "user")
    t.push(e[r]), r++;
  else {
    const s = [];
    let a = !0;
    for (; r < n && e[r].role === "model"; )
      s.push(e[r]), a && !vP(e[r]) && (a = !1), r++;
    a ? t.push(...s) : t.pop();
  }
  return t;
}
var cG = class {
  constructor(e, t) {
    this.modelsModule = e, this.apiClient = t;
  }
  create(e) {
    return new dG(this.apiClient, this.modelsModule, e.model, e.config, structuredClone(e.history));
  }
}, dG = class {
  constructor(e, t, n, r = {}, s = []) {
    this.apiClient = e, this.modelsModule = t, this.model = n, this.config = r, this.history = s, this.sendPromise = Promise.resolve(), uG(s);
  }
  async sendMessage(e) {
    var t;
    await this.sendPromise;
    const n = cr(e.message), r = this.modelsModule.generateContent({
      model: this.model,
      contents: this.getHistory(!0).concat(n),
      config: (t = e.config) !== null && t !== void 0 ? t : this.config
    });
    return this.sendPromise = (async () => {
      var s, a, o;
      const l = await r, i = (a = (s = l.candidates) === null || s === void 0 ? void 0 : s[0]) === null || a === void 0 ? void 0 : a.content, u = l.automaticFunctionCallingHistory, d = this.getHistory(!0).length;
      let f = [];
      u != null && (f = (o = u.slice(d)) !== null && o !== void 0 ? o : []);
      const m = i ? [i] : [];
      this.recordHistory(n, m, f);
    })(), await this.sendPromise.catch(() => {
      this.sendPromise = Promise.resolve();
    }), r;
  }
  async sendMessageStream(e) {
    var t;
    await this.sendPromise;
    const n = cr(e.message), r = this.modelsModule.generateContentStream({
      model: this.model,
      contents: this.getHistory(!0).concat(n),
      config: (t = e.config) !== null && t !== void 0 ? t : this.config
    });
    this.sendPromise = r.then(() => {
    }).catch(() => {
    });
    const s = await r;
    return this.processStreamResponse(s, n);
  }
  getHistory(e = !1) {
    const t = e ? $C(this.history) : this.history;
    return structuredClone(t);
  }
  processStreamResponse(e, t) {
    return aa(this, arguments, function* () {
      var r, s, a, o, l, i;
      const u = [];
      try {
        for (var d = !0, f = oa(e), m; m = yield kt(f.next()), r = m.done, !r; d = !0) {
          o = m.value, d = !1;
          const v = o;
          if (lG(v)) {
            const y = (i = (l = v.candidates) === null || l === void 0 ? void 0 : l[0]) === null || i === void 0 ? void 0 : i.content;
            y !== void 0 && u.push(y);
          }
          yield yield kt(v);
        }
      } catch (v) {
        s = { error: v };
      } finally {
        try {
          !d && !r && (a = f.return) && (yield kt(a.call(f)));
        } finally {
          if (s) throw s.error;
        }
      }
      this.recordHistory(t, u);
    });
  }
  recordHistory(e, t, n) {
    let r = [];
    t.length > 0 && t.every((s) => s.role !== void 0) ? r = t : r.push({
      role: "model",
      parts: []
    }), n && n.length > 0 ? this.history.push(...$C(n)) : this.history.push(e), this.history.push(...r);
  }
}, yP = class _P extends Error {
  constructor(t) {
    super(t.message), this.name = "ApiError", this.status = t.status, Object.setPrototypeOf(this, _P.prototype);
  }
};
function fG(e) {
  const t = {}, n = c(e, ["file"]);
  return n != null && h(t, ["file"], n), t;
}
function hG(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
function pG(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && h(t, ["_url", "file"], cP(n)), t;
}
function mG(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
function gG(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && h(t, ["_url", "file"], cP(n)), t;
}
function vG(e) {
  const t = {}, n = c(e, ["uris"]);
  return n != null && h(t, ["uris"], n), t;
}
function yG(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && h(t, ["_query", "pageToken"], s), n;
}
function _G(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && yG(n, t), t;
}
function bG(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const s = c(e, ["files"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => o)), h(t, ["files"], a);
  }
  return t;
}
function wG(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["files"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => a)), h(t, ["files"], s);
  }
  return t;
}
var SG = class extends Po {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new Fl(ko.PAGED_ITEM_FILES, (n) => this.listInternal(n), await this.listInternal(t), t);
  }
  async upload(e) {
    if (this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support uploading files. You can share files through a GCS bucket.");
    return this.apiClient.uploadFile(e.file, e.config).then((t) => t);
  }
  async download(e) {
    await this.apiClient.downloadFile(e);
  }
  async registerFiles(e) {
    throw new Error("registerFiles is only supported in Node.js environments.");
  }
  async _registerFiles(e) {
    return this.registerFilesInternal(e);
  }
  async listInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = _G(e);
      return s = Ze("files", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json().then((i) => {
        const u = i;
        return u.sdkHttpResponse = { headers: l.headers }, u;
      })), r.then((l) => {
        const i = bG(l), u = new vK();
        return Object.assign(u, i), u;
      });
    }
  }
  async createInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = fG(e);
      return s = Ze("upload/v1beta/files", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => {
        const i = hG(l), u = new yK();
        return Object.assign(u, i), u;
      });
    }
  }
  async get(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = gG(e);
      return s = Ze("files/{file}", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => l);
    }
  }
  async delete(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = pG(e);
      return s = Ze("files/{file}", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json().then((i) => {
        const u = i;
        return u.sdkHttpResponse = { headers: l.headers }, u;
      })), r.then((l) => {
        const i = mG(l), u = new _K();
        return Object.assign(u, i), u;
      });
    }
  }
  async registerFilesInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = vG(e);
      return s = Ze("files:register", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => {
        const i = wG(l), u = new bK();
        return Object.assign(u, i), u;
      });
    }
  }
};
function LC(e) {
  const t = {};
  if (c(e, ["languageCodes"]) !== void 0) throw new Error("languageCodes parameter is not supported in Gemini API.");
  return t;
}
function AG(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && h(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function Xp(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && h(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function TG(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((a) => qG(a))), h(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function EG(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((a) => HG(a))), h(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function CG(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && h(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function IG(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && h(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && h(t, ["args"], r);
  const s = c(e, ["name"]);
  if (s != null && h(t, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function xG(e) {
  const t = {}, n = c(e, ["description"]);
  n != null && h(t, ["description"], n);
  const r = c(e, ["name"]);
  r != null && h(t, ["name"], r);
  const s = c(e, ["parameters"]);
  s != null && h(t, ["parameters"], s);
  const a = c(e, ["parametersJsonSchema"]);
  a != null && h(t, ["parametersJsonSchema"], a);
  const o = c(e, ["response"]);
  o != null && h(t, ["response"], o);
  const l = c(e, ["responseJsonSchema"]);
  if (l != null && h(t, ["responseJsonSchema"], l), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return t;
}
function kG(e) {
  const t = {}, n = c(e, ["modelSelectionConfig"]);
  n != null && h(t, ["modelConfig"], n);
  const r = c(e, ["responseJsonSchema"]);
  r != null && h(t, ["responseJsonSchema"], r);
  const s = c(e, ["audioTimestamp"]);
  s != null && h(t, ["audioTimestamp"], s);
  const a = c(e, ["candidateCount"]);
  a != null && h(t, ["candidateCount"], a);
  const o = c(e, ["enableAffectiveDialog"]);
  o != null && h(t, ["enableAffectiveDialog"], o);
  const l = c(e, ["frequencyPenalty"]);
  l != null && h(t, ["frequencyPenalty"], l);
  const i = c(e, ["logprobs"]);
  i != null && h(t, ["logprobs"], i);
  const u = c(e, ["maxOutputTokens"]);
  u != null && h(t, ["maxOutputTokens"], u);
  const d = c(e, ["mediaResolution"]);
  d != null && h(t, ["mediaResolution"], d);
  const f = c(e, ["presencePenalty"]);
  f != null && h(t, ["presencePenalty"], f);
  const m = c(e, ["responseLogprobs"]);
  m != null && h(t, ["responseLogprobs"], m);
  const v = c(e, ["responseMimeType"]);
  v != null && h(t, ["responseMimeType"], v);
  const y = c(e, ["responseModalities"]);
  y != null && h(t, ["responseModalities"], y);
  const S = c(e, ["responseSchema"]);
  S != null && h(t, ["responseSchema"], S);
  const x = c(e, ["routingConfig"]);
  x != null && h(t, ["routingConfig"], x);
  const I = c(e, ["seed"]);
  I != null && h(t, ["seed"], I);
  const E = c(e, ["speechConfig"]);
  E != null && h(t, ["speechConfig"], E);
  const N = c(e, ["stopSequences"]);
  N != null && h(t, ["stopSequences"], N);
  const $ = c(e, ["temperature"]);
  $ != null && h(t, ["temperature"], $);
  const p = c(e, ["thinkingConfig"]);
  p != null && h(t, ["thinkingConfig"], p);
  const w = c(e, ["topK"]);
  w != null && h(t, ["topK"], w);
  const _ = c(e, ["topP"]);
  if (_ != null && h(t, ["topP"], _), c(e, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  return t;
}
function MG(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && h(t, ["authConfig"], AG(n));
  const r = c(e, ["enableWidget"]);
  return r != null && h(t, ["enableWidget"], r), t;
}
function PG(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && h(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && h(t, ["timeRangeFilter"], r), t;
}
function RG(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && h(t, ["setup", "generationConfig"], r);
  const s = c(e, ["responseModalities"]);
  t !== void 0 && s != null && h(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], s);
  const a = c(e, ["temperature"]);
  t !== void 0 && a != null && h(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], a);
  const o = c(e, ["topP"]);
  t !== void 0 && o != null && h(t, [
    "setup",
    "generationConfig",
    "topP"
  ], o);
  const l = c(e, ["topK"]);
  t !== void 0 && l != null && h(t, [
    "setup",
    "generationConfig",
    "topK"
  ], l);
  const i = c(e, ["maxOutputTokens"]);
  t !== void 0 && i != null && h(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], i);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && h(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && h(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const f = c(e, ["speechConfig"]);
  t !== void 0 && f != null && h(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], Yw(f));
  const m = c(e, ["thinkingConfig"]);
  t !== void 0 && m != null && h(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], m);
  const v = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && v != null && h(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], v);
  const y = c(e, ["systemInstruction"]);
  t !== void 0 && y != null && h(t, ["setup", "systemInstruction"], TG(cr(y)));
  const S = c(e, ["tools"]);
  if (t !== void 0 && S != null) {
    let b = fc(S);
    Array.isArray(b) && (b = b.map((C) => WG(dc(C)))), h(t, ["setup", "tools"], b);
  }
  const x = c(e, ["sessionResumption"]);
  t !== void 0 && x != null && h(t, ["setup", "sessionResumption"], KG(x));
  const I = c(e, ["inputAudioTranscription"]);
  t !== void 0 && I != null && h(t, ["setup", "inputAudioTranscription"], LC(I));
  const E = c(e, ["outputAudioTranscription"]);
  t !== void 0 && E != null && h(t, ["setup", "outputAudioTranscription"], LC(E));
  const N = c(e, ["realtimeInputConfig"]);
  t !== void 0 && N != null && h(t, ["setup", "realtimeInputConfig"], N);
  const $ = c(e, ["contextWindowCompression"]);
  t !== void 0 && $ != null && h(t, ["setup", "contextWindowCompression"], $);
  const p = c(e, ["proactivity"]);
  if (t !== void 0 && p != null && h(t, ["setup", "proactivity"], p), c(e, ["explicitVadSignal"]) !== void 0) throw new Error("explicitVadSignal parameter is not supported in Gemini API.");
  const w = c(e, ["avatarConfig"]);
  t !== void 0 && w != null && h(t, ["setup", "avatarConfig"], w);
  const _ = c(e, ["safetySettings"]);
  if (t !== void 0 && _ != null) {
    let b = _;
    Array.isArray(b) && (b = b.map((C) => zG(C))), h(t, ["setup", "safetySettings"], b);
  }
  return n;
}
function NG(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && h(t, ["setup", "generationConfig"], kG(r));
  const s = c(e, ["responseModalities"]);
  t !== void 0 && s != null && h(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], s);
  const a = c(e, ["temperature"]);
  t !== void 0 && a != null && h(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], a);
  const o = c(e, ["topP"]);
  t !== void 0 && o != null && h(t, [
    "setup",
    "generationConfig",
    "topP"
  ], o);
  const l = c(e, ["topK"]);
  t !== void 0 && l != null && h(t, [
    "setup",
    "generationConfig",
    "topK"
  ], l);
  const i = c(e, ["maxOutputTokens"]);
  t !== void 0 && i != null && h(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], i);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && h(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && h(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const f = c(e, ["speechConfig"]);
  t !== void 0 && f != null && h(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], Yw(f));
  const m = c(e, ["thinkingConfig"]);
  t !== void 0 && m != null && h(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], m);
  const v = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && v != null && h(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], v);
  const y = c(e, ["systemInstruction"]);
  t !== void 0 && y != null && h(t, ["setup", "systemInstruction"], EG(cr(y)));
  const S = c(e, ["tools"]);
  if (t !== void 0 && S != null) {
    let C = fc(S);
    Array.isArray(C) && (C = C.map((k) => GG(dc(k)))), h(t, ["setup", "tools"], C);
  }
  const x = c(e, ["sessionResumption"]);
  t !== void 0 && x != null && h(t, ["setup", "sessionResumption"], x);
  const I = c(e, ["inputAudioTranscription"]);
  t !== void 0 && I != null && h(t, ["setup", "inputAudioTranscription"], I);
  const E = c(e, ["outputAudioTranscription"]);
  t !== void 0 && E != null && h(t, ["setup", "outputAudioTranscription"], E);
  const N = c(e, ["realtimeInputConfig"]);
  t !== void 0 && N != null && h(t, ["setup", "realtimeInputConfig"], N);
  const $ = c(e, ["contextWindowCompression"]);
  t !== void 0 && $ != null && h(t, ["setup", "contextWindowCompression"], $);
  const p = c(e, ["proactivity"]);
  t !== void 0 && p != null && h(t, ["setup", "proactivity"], p);
  const w = c(e, ["explicitVadSignal"]);
  t !== void 0 && w != null && h(t, ["setup", "explicitVadSignal"], w);
  const _ = c(e, ["avatarConfig"]);
  t !== void 0 && _ != null && h(t, ["setup", "avatarConfig"], _);
  const b = c(e, ["safetySettings"]);
  if (t !== void 0 && b != null) {
    let C = b;
    Array.isArray(C) && (C = C.map((k) => k)), h(t, ["setup", "safetySettings"], C);
  }
  return n;
}
function $G(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["setup", "model"], Zt(e, r));
  const s = c(t, ["config"]);
  return s != null && h(n, ["config"], RG(s, n)), n;
}
function LG(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["setup", "model"], Zt(e, r));
  const s = c(t, ["config"]);
  return s != null && h(n, ["config"], NG(s, n)), n;
}
function DG(e) {
  const t = {}, n = c(e, ["musicGenerationConfig"]);
  return n != null && h(t, ["musicGenerationConfig"], n), t;
}
function OG(e) {
  const t = {}, n = c(e, ["weightedPrompts"]);
  if (n != null) {
    let r = n;
    Array.isArray(r) && (r = r.map((s) => s)), h(t, ["weightedPrompts"], r);
  }
  return t;
}
function FG(e) {
  const t = {}, n = c(e, ["media"]);
  if (n != null) {
    let u = aP(n);
    Array.isArray(u) && (u = u.map((d) => Xp(d))), h(t, ["mediaChunks"], u);
  }
  const r = c(e, ["audio"]);
  r != null && h(t, ["audio"], Xp(iP(r)));
  const s = c(e, ["audioStreamEnd"]);
  s != null && h(t, ["audioStreamEnd"], s);
  const a = c(e, ["video"]);
  a != null && h(t, ["video"], Xp(oP(a)));
  const o = c(e, ["text"]);
  o != null && h(t, ["text"], o);
  const l = c(e, ["activityStart"]);
  l != null && h(t, ["activityStart"], l);
  const i = c(e, ["activityEnd"]);
  return i != null && h(t, ["activityEnd"], i), t;
}
function BG(e) {
  const t = {}, n = c(e, ["media"]);
  if (n != null) {
    let u = aP(n);
    Array.isArray(u) && (u = u.map((d) => d)), h(t, ["mediaChunks"], u);
  }
  const r = c(e, ["audio"]);
  r != null && h(t, ["audio"], iP(r));
  const s = c(e, ["audioStreamEnd"]);
  s != null && h(t, ["audioStreamEnd"], s);
  const a = c(e, ["video"]);
  a != null && h(t, ["video"], oP(a));
  const o = c(e, ["text"]);
  o != null && h(t, ["text"], o);
  const l = c(e, ["activityStart"]);
  l != null && h(t, ["activityStart"], l);
  const i = c(e, ["activityEnd"]);
  return i != null && h(t, ["activityEnd"], i), t;
}
function UG(e) {
  const t = {}, n = c(e, ["setupComplete"]);
  n != null && h(t, ["setupComplete"], n);
  const r = c(e, ["serverContent"]);
  r != null && h(t, ["serverContent"], r);
  const s = c(e, ["toolCall"]);
  s != null && h(t, ["toolCall"], s);
  const a = c(e, ["toolCallCancellation"]);
  a != null && h(t, ["toolCallCancellation"], a);
  const o = c(e, ["usageMetadata"]);
  o != null && h(t, ["usageMetadata"], VG(o));
  const l = c(e, ["goAway"]);
  l != null && h(t, ["goAway"], l);
  const i = c(e, ["sessionResumptionUpdate"]);
  i != null && h(t, ["sessionResumptionUpdate"], i);
  const u = c(e, ["voiceActivityDetectionSignal"]);
  u != null && h(t, ["voiceActivityDetectionSignal"], u);
  const d = c(e, ["voiceActivity"]);
  return d != null && h(t, ["voiceActivity"], JG(d)), t;
}
function qG(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && h(t, ["executableCode"], s);
  const a = c(e, ["fileData"]);
  a != null && h(t, ["fileData"], CG(a));
  const o = c(e, ["functionCall"]);
  o != null && h(t, ["functionCall"], IG(o));
  const l = c(e, ["functionResponse"]);
  l != null && h(t, ["functionResponse"], l);
  const i = c(e, ["inlineData"]);
  i != null && h(t, ["inlineData"], Xp(i));
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  m != null && h(t, ["videoMetadata"], m);
  const v = c(e, ["toolCall"]);
  v != null && h(t, ["toolCall"], v);
  const y = c(e, ["toolResponse"]);
  y != null && h(t, ["toolResponse"], y);
  const S = c(e, ["partMetadata"]);
  return S != null && h(t, ["partMetadata"], S), t;
}
function HG(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && h(t, ["executableCode"], s);
  const a = c(e, ["fileData"]);
  a != null && h(t, ["fileData"], a);
  const o = c(e, ["functionCall"]);
  o != null && h(t, ["functionCall"], o);
  const l = c(e, ["functionResponse"]);
  l != null && h(t, ["functionResponse"], l);
  const i = c(e, ["inlineData"]);
  i != null && h(t, ["inlineData"], i);
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  if (m != null && h(t, ["videoMetadata"], m), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return t;
}
function zG(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && h(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && h(t, ["threshold"], r), t;
}
function KG(e) {
  const t = {}, n = c(e, ["handle"]);
  if (n != null && h(t, ["handle"], n), c(e, ["transparent"]) !== void 0) throw new Error("transparent parameter is not supported in Gemini API.");
  return t;
}
function WG(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && h(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && h(t, ["fileSearch"], r);
  const s = c(e, ["googleSearch"]);
  s != null && h(t, ["googleSearch"], PG(s));
  const a = c(e, ["googleMaps"]);
  a != null && h(t, ["googleMaps"], MG(a));
  const o = c(e, ["codeExecution"]);
  if (o != null && h(t, ["codeExecution"], o), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let f = l;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["functionDeclarations"], f);
  }
  const i = c(e, ["googleSearchRetrieval"]);
  if (i != null && h(t, ["googleSearchRetrieval"], i), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && h(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let f = d;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["mcpServers"], f);
  }
  return t;
}
function GG(e) {
  const t = {}, n = c(e, ["retrieval"]);
  n != null && h(t, ["retrieval"], n);
  const r = c(e, ["computerUse"]);
  if (r != null && h(t, ["computerUse"], r), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const s = c(e, ["googleSearch"]);
  s != null && h(t, ["googleSearch"], s);
  const a = c(e, ["googleMaps"]);
  a != null && h(t, ["googleMaps"], a);
  const o = c(e, ["codeExecution"]);
  o != null && h(t, ["codeExecution"], o);
  const l = c(e, ["enterpriseWebSearch"]);
  l != null && h(t, ["enterpriseWebSearch"], l);
  const i = c(e, ["functionDeclarations"]);
  if (i != null) {
    let m = i;
    Array.isArray(m) && (m = m.map((v) => xG(v))), h(t, ["functionDeclarations"], m);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  u != null && h(t, ["googleSearchRetrieval"], u);
  const d = c(e, ["parallelAiSearch"]);
  d != null && h(t, ["parallelAiSearch"], d);
  const f = c(e, ["urlContext"]);
  if (f != null && h(t, ["urlContext"], f), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return t;
}
function VG(e) {
  const t = {}, n = c(e, ["promptTokenCount"]);
  n != null && h(t, ["promptTokenCount"], n);
  const r = c(e, ["cachedContentTokenCount"]);
  r != null && h(t, ["cachedContentTokenCount"], r);
  const s = c(e, ["candidatesTokenCount"]);
  s != null && h(t, ["responseTokenCount"], s);
  const a = c(e, ["toolUsePromptTokenCount"]);
  a != null && h(t, ["toolUsePromptTokenCount"], a);
  const o = c(e, ["thoughtsTokenCount"]);
  o != null && h(t, ["thoughtsTokenCount"], o);
  const l = c(e, ["totalTokenCount"]);
  l != null && h(t, ["totalTokenCount"], l);
  const i = c(e, ["promptTokensDetails"]);
  if (i != null) {
    let v = i;
    Array.isArray(v) && (v = v.map((y) => y)), h(t, ["promptTokensDetails"], v);
  }
  const u = c(e, ["cacheTokensDetails"]);
  if (u != null) {
    let v = u;
    Array.isArray(v) && (v = v.map((y) => y)), h(t, ["cacheTokensDetails"], v);
  }
  const d = c(e, ["candidatesTokensDetails"]);
  if (d != null) {
    let v = d;
    Array.isArray(v) && (v = v.map((y) => y)), h(t, ["responseTokensDetails"], v);
  }
  const f = c(e, ["toolUsePromptTokensDetails"]);
  if (f != null) {
    let v = f;
    Array.isArray(v) && (v = v.map((y) => y)), h(t, ["toolUsePromptTokensDetails"], v);
  }
  const m = c(e, ["trafficType"]);
  return m != null && h(t, ["trafficType"], m), t;
}
function JG(e) {
  const t = {}, n = c(e, ["type"]);
  return n != null && h(t, ["voiceActivityType"], n), t;
}
function YG(e, t) {
  const n = {}, r = c(e, ["apiKey"]);
  if (r != null && h(n, ["apiKey"], r), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return n;
}
function XG(e, t) {
  const n = {}, r = c(e, ["data"]);
  if (r != null && h(n, ["data"], r), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function jG(e, t) {
  const n = {}, r = c(e, ["content"]);
  r != null && h(n, ["content"], r);
  const s = c(e, ["citationMetadata"]);
  s != null && h(n, ["citationMetadata"], QG(s));
  const a = c(e, ["tokenCount"]);
  a != null && h(n, ["tokenCount"], a);
  const o = c(e, ["finishReason"]);
  o != null && h(n, ["finishReason"], o);
  const l = c(e, ["groundingMetadata"]);
  l != null && h(n, ["groundingMetadata"], l);
  const i = c(e, ["avgLogprobs"]);
  i != null && h(n, ["avgLogprobs"], i);
  const u = c(e, ["index"]);
  u != null && h(n, ["index"], u);
  const d = c(e, ["logprobsResult"]);
  d != null && h(n, ["logprobsResult"], d);
  const f = c(e, ["safetyRatings"]);
  if (f != null) {
    let v = f;
    Array.isArray(v) && (v = v.map((y) => y)), h(n, ["safetyRatings"], v);
  }
  const m = c(e, ["urlContextMetadata"]);
  return m != null && h(n, ["urlContextMetadata"], m), n;
}
function QG(e, t) {
  const n = {}, r = c(e, ["citationSources"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => a)), h(n, ["citations"], s);
  }
  return n;
}
function ZG(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["contents"]);
  if (a != null) {
    let o = Yr(a);
    Array.isArray(o) && (o = o.map((l) => pc(l))), h(r, ["contents"], o);
  }
  return r;
}
function eV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["tokensInfo"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => o)), h(n, ["tokensInfo"], a);
  }
  return n;
}
function tV(e, t) {
  const n = {}, r = c(e, ["values"]);
  r != null && h(n, ["values"], r);
  const s = c(e, ["statistics"]);
  return s != null && h(n, ["statistics"], nV(s)), n;
}
function nV(e, t) {
  const n = {}, r = c(e, ["truncated"]);
  r != null && h(n, ["truncated"], r);
  const s = c(e, ["token_count"]);
  return s != null && h(n, ["tokenCount"], s), n;
}
function Vf(e, t) {
  const n = {}, r = c(e, ["parts"]);
  if (r != null) {
    let a = r;
    Array.isArray(a) && (a = a.map((o) => dJ(o))), h(n, ["parts"], a);
  }
  const s = c(e, ["role"]);
  return s != null && h(n, ["role"], s), n;
}
function pc(e, t) {
  const n = {}, r = c(e, ["parts"]);
  if (r != null) {
    let a = r;
    Array.isArray(a) && (a = a.map((o) => fJ(o))), h(n, ["parts"], a);
  }
  const s = c(e, ["role"]);
  return s != null && h(n, ["role"], s), n;
}
function rV(e, t) {
  const n = {}, r = c(e, ["controlType"]);
  r != null && h(n, ["controlType"], r);
  const s = c(e, ["enableControlImageComputation"]);
  return s != null && h(n, ["computeControl"], s), n;
}
function sV(e, t) {
  const n = {};
  if (c(e, ["systemInstruction"]) !== void 0) throw new Error("systemInstruction parameter is not supported in Gemini API.");
  if (c(e, ["tools"]) !== void 0) throw new Error("tools parameter is not supported in Gemini API.");
  if (c(e, ["generationConfig"]) !== void 0) throw new Error("generationConfig parameter is not supported in Gemini API.");
  return n;
}
function aV(e, t, n) {
  const r = {}, s = c(e, ["systemInstruction"]);
  t !== void 0 && s != null && h(t, ["systemInstruction"], pc(cr(s)));
  const a = c(e, ["tools"]);
  if (t !== void 0 && a != null) {
    let l = a;
    Array.isArray(l) && (l = l.map((i) => AP(i))), h(t, ["tools"], l);
  }
  const o = c(e, ["generationConfig"]);
  return t !== void 0 && o != null && h(t, ["generationConfig"], XV(o)), r;
}
function oV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["contents"]);
  if (a != null) {
    let l = Yr(a);
    Array.isArray(l) && (l = l.map((i) => Vf(i))), h(r, ["contents"], l);
  }
  const o = c(t, ["config"]);
  return o != null && sV(o), r;
}
function iV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["contents"]);
  if (a != null) {
    let l = Yr(a);
    Array.isArray(l) && (l = l.map((i) => pc(i))), h(r, ["contents"], l);
  }
  const o = c(t, ["config"]);
  return o != null && aV(o, r), r;
}
function lV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["totalTokens"]);
  s != null && h(n, ["totalTokens"], s);
  const a = c(e, ["cachedContentTokenCount"]);
  return a != null && h(n, ["cachedContentTokenCount"], a), n;
}
function uV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["totalTokens"]);
  return s != null && h(n, ["totalTokens"], s), n;
}
function cV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  return s != null && h(r, ["_url", "name"], Zt(e, s)), r;
}
function dV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  return s != null && h(r, ["_url", "name"], Zt(e, s)), r;
}
function fV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && h(n, ["sdkHttpResponse"], r), n;
}
function hV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && h(n, ["sdkHttpResponse"], r), n;
}
function pV(e, t, n) {
  const r = {}, s = c(e, ["outputGcsUri"]);
  t !== void 0 && s != null && h(t, ["parameters", "storageUri"], s);
  const a = c(e, ["negativePrompt"]);
  t !== void 0 && a != null && h(t, ["parameters", "negativePrompt"], a);
  const o = c(e, ["numberOfImages"]);
  t !== void 0 && o != null && h(t, ["parameters", "sampleCount"], o);
  const l = c(e, ["aspectRatio"]);
  t !== void 0 && l != null && h(t, ["parameters", "aspectRatio"], l);
  const i = c(e, ["guidanceScale"]);
  t !== void 0 && i != null && h(t, ["parameters", "guidanceScale"], i);
  const u = c(e, ["seed"]);
  t !== void 0 && u != null && h(t, ["parameters", "seed"], u);
  const d = c(e, ["safetyFilterLevel"]);
  t !== void 0 && d != null && h(t, ["parameters", "safetySetting"], d);
  const f = c(e, ["personGeneration"]);
  t !== void 0 && f != null && h(t, ["parameters", "personGeneration"], f);
  const m = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && m != null && h(t, ["parameters", "includeSafetyAttributes"], m);
  const v = c(e, ["includeRaiReason"]);
  t !== void 0 && v != null && h(t, ["parameters", "includeRaiReason"], v);
  const y = c(e, ["language"]);
  t !== void 0 && y != null && h(t, ["parameters", "language"], y);
  const S = c(e, ["outputMimeType"]);
  t !== void 0 && S != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], S);
  const x = c(e, ["outputCompressionQuality"]);
  t !== void 0 && x != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], x);
  const I = c(e, ["addWatermark"]);
  t !== void 0 && I != null && h(t, ["parameters", "addWatermark"], I);
  const E = c(e, ["labels"]);
  t !== void 0 && E != null && h(t, ["labels"], E);
  const N = c(e, ["editMode"]);
  t !== void 0 && N != null && h(t, ["parameters", "editMode"], N);
  const $ = c(e, ["baseSteps"]);
  return t !== void 0 && $ != null && h(t, [
    "parameters",
    "editConfig",
    "baseSteps"
  ], $), r;
}
function mV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["prompt"]);
  a != null && h(r, ["instances[0]", "prompt"], a);
  const o = c(t, ["referenceImages"]);
  if (o != null) {
    let i = o;
    Array.isArray(i) && (i = i.map((u) => yJ(u))), h(r, ["instances[0]", "referenceImages"], i);
  }
  const l = c(t, ["config"]);
  return l != null && pV(l, r), r;
}
function gV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => cv(o))), h(n, ["generatedImages"], a);
  }
  return n;
}
function vV(e, t, n) {
  const r = {}, s = c(e, ["taskType"]);
  t !== void 0 && s != null && h(t, ["requests[]", "taskType"], s);
  const a = c(e, ["title"]);
  t !== void 0 && a != null && h(t, ["requests[]", "title"], a);
  const o = c(e, ["outputDimensionality"]);
  if (t !== void 0 && o != null && h(t, ["requests[]", "outputDimensionality"], o), c(e, ["mimeType"]) !== void 0) throw new Error("mimeType parameter is not supported in Gemini API.");
  if (c(e, ["autoTruncate"]) !== void 0) throw new Error("autoTruncate parameter is not supported in Gemini API.");
  if (c(e, ["documentOcr"]) !== void 0) throw new Error("documentOcr parameter is not supported in Gemini API.");
  if (c(e, ["audioTrackExtraction"]) !== void 0) throw new Error("audioTrackExtraction parameter is not supported in Gemini API.");
  return r;
}
function yV(e, t, n) {
  const r = {};
  let s = c(n, ["embeddingApiType"]);
  if (s === void 0 && (s = "PREDICT"), s === "PREDICT") {
    const f = c(e, ["taskType"]);
    t !== void 0 && f != null && h(t, ["instances[]", "task_type"], f);
  } else if (s === "EMBED_CONTENT") {
    const f = c(e, ["taskType"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "taskType"], f);
  }
  let a = c(n, ["embeddingApiType"]);
  if (a === void 0 && (a = "PREDICT"), a === "PREDICT") {
    const f = c(e, ["title"]);
    t !== void 0 && f != null && h(t, ["instances[]", "title"], f);
  } else if (a === "EMBED_CONTENT") {
    const f = c(e, ["title"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "title"], f);
  }
  let o = c(n, ["embeddingApiType"]);
  if (o === void 0 && (o = "PREDICT"), o === "PREDICT") {
    const f = c(e, ["outputDimensionality"]);
    t !== void 0 && f != null && h(t, ["parameters", "outputDimensionality"], f);
  } else if (o === "EMBED_CONTENT") {
    const f = c(e, ["outputDimensionality"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "outputDimensionality"], f);
  }
  let l = c(n, ["embeddingApiType"]);
  if (l === void 0 && (l = "PREDICT"), l === "PREDICT") {
    const f = c(e, ["mimeType"]);
    t !== void 0 && f != null && h(t, ["instances[]", "mimeType"], f);
  }
  let i = c(n, ["embeddingApiType"]);
  if (i === void 0 && (i = "PREDICT"), i === "PREDICT") {
    const f = c(e, ["autoTruncate"]);
    t !== void 0 && f != null && h(t, ["parameters", "autoTruncate"], f);
  } else if (i === "EMBED_CONTENT") {
    const f = c(e, ["autoTruncate"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "autoTruncate"], f);
  }
  let u = c(n, ["embeddingApiType"]);
  if (u === void 0 && (u = "PREDICT"), u === "EMBED_CONTENT") {
    const f = c(e, ["documentOcr"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "documentOcr"], f);
  }
  let d = c(n, ["embeddingApiType"]);
  if (d === void 0 && (d = "PREDICT"), d === "EMBED_CONTENT") {
    const f = c(e, ["audioTrackExtraction"]);
    t !== void 0 && f != null && h(t, ["embedContentConfig", "audioTrackExtraction"], f);
  }
  return r;
}
function _V(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["contents"]);
  if (a != null) {
    let u = Gw(e, a);
    Array.isArray(u) && (u = u.map((d) => d)), h(r, ["requests[]", "content"], u);
  }
  const o = c(t, ["content"]);
  o != null && Vf(cr(o));
  const l = c(t, ["config"]);
  l != null && vV(l, r);
  const i = c(t, ["model"]);
  return i !== void 0 && h(r, ["requests[]", "model"], Zt(e, i)), r;
}
function bV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  let a = c(n, ["embeddingApiType"]);
  if (a === void 0 && (a = "PREDICT"), a === "PREDICT") {
    const i = c(t, ["contents"]);
    if (i != null) {
      let u = Gw(e, i);
      Array.isArray(u) && (u = u.map((d) => d)), h(r, ["instances[]", "content"], u);
    }
  }
  let o = c(n, ["embeddingApiType"]);
  if (o === void 0 && (o = "PREDICT"), o === "EMBED_CONTENT") {
    const i = c(t, ["content"]);
    i != null && h(r, ["content"], pc(cr(i)));
  }
  const l = c(t, ["config"]);
  return l != null && yV(l, r, n), r;
}
function wV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["embeddings"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((l) => l)), h(n, ["embeddings"], o);
  }
  const a = c(e, ["metadata"]);
  return a != null && h(n, ["metadata"], a), n;
}
function SV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions[]", "embeddings"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((l) => tV(l))), h(n, ["embeddings"], o);
  }
  const a = c(e, ["metadata"]);
  if (a != null && h(n, ["metadata"], a), t && c(t, ["embeddingApiType"]) === "EMBED_CONTENT") {
    const o = c(e, ["embedding"]), l = c(e, ["usageMetadata"]), i = c(e, ["truncated"]);
    if (o) {
      const u = {};
      l && l.promptTokenCount && (u.tokenCount = l.promptTokenCount), i && (u.truncated = i), o.statistics = u, h(n, ["embeddings"], [o]);
    }
  }
  return n;
}
function AV(e, t) {
  const n = {}, r = c(e, ["endpoint"]);
  r != null && h(n, ["name"], r);
  const s = c(e, ["deployedModelId"]);
  return s != null && h(n, ["deployedModelId"], s), n;
}
function TV(e, t) {
  const n = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["fileUri"]);
  r != null && h(n, ["fileUri"], r);
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function EV(e, t) {
  const n = {}, r = c(e, ["id"]);
  r != null && h(n, ["id"], r);
  const s = c(e, ["args"]);
  s != null && h(n, ["args"], s);
  const a = c(e, ["name"]);
  if (a != null && h(n, ["name"], a), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return n;
}
function CV(e, t) {
  const n = {}, r = c(e, ["allowedFunctionNames"]);
  r != null && h(n, ["allowedFunctionNames"], r);
  const s = c(e, ["mode"]);
  if (s != null && h(n, ["mode"], s), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return n;
}
function IV(e, t) {
  const n = {}, r = c(e, ["description"]);
  r != null && h(n, ["description"], r);
  const s = c(e, ["name"]);
  s != null && h(n, ["name"], s);
  const a = c(e, ["parameters"]);
  a != null && h(n, ["parameters"], a);
  const o = c(e, ["parametersJsonSchema"]);
  o != null && h(n, ["parametersJsonSchema"], o);
  const l = c(e, ["response"]);
  l != null && h(n, ["response"], l);
  const i = c(e, ["responseJsonSchema"]);
  if (i != null && h(n, ["responseJsonSchema"], i), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return n;
}
function xV(e, t, n, r) {
  const s = {}, a = c(t, ["systemInstruction"]);
  n !== void 0 && a != null && h(n, ["systemInstruction"], Vf(cr(a)));
  const o = c(t, ["temperature"]);
  o != null && h(s, ["temperature"], o);
  const l = c(t, ["topP"]);
  l != null && h(s, ["topP"], l);
  const i = c(t, ["topK"]);
  i != null && h(s, ["topK"], i);
  const u = c(t, ["candidateCount"]);
  u != null && h(s, ["candidateCount"], u);
  const d = c(t, ["maxOutputTokens"]);
  d != null && h(s, ["maxOutputTokens"], d);
  const f = c(t, ["stopSequences"]);
  f != null && h(s, ["stopSequences"], f);
  const m = c(t, ["responseLogprobs"]);
  m != null && h(s, ["responseLogprobs"], m);
  const v = c(t, ["logprobs"]);
  v != null && h(s, ["logprobs"], v);
  const y = c(t, ["presencePenalty"]);
  y != null && h(s, ["presencePenalty"], y);
  const S = c(t, ["frequencyPenalty"]);
  S != null && h(s, ["frequencyPenalty"], S);
  const x = c(t, ["seed"]);
  x != null && h(s, ["seed"], x);
  const I = c(t, ["responseMimeType"]);
  I != null && h(s, ["responseMimeType"], I);
  const E = c(t, ["responseSchema"]);
  E != null && h(s, ["responseSchema"], Vw(E));
  const N = c(t, ["responseJsonSchema"]);
  if (N != null && h(s, ["responseJsonSchema"], N), c(t, ["routingConfig"]) !== void 0) throw new Error("routingConfig parameter is not supported in Gemini API.");
  if (c(t, ["modelSelectionConfig"]) !== void 0) throw new Error("modelSelectionConfig parameter is not supported in Gemini API.");
  const $ = c(t, ["safetySettings"]);
  if (n !== void 0 && $ != null) {
    let J = $;
    Array.isArray(J) && (J = J.map((z) => _J(z))), h(n, ["safetySettings"], J);
  }
  const p = c(t, ["tools"]);
  if (n !== void 0 && p != null) {
    let J = fc(p);
    Array.isArray(J) && (J = J.map((z) => IJ(dc(z)))), h(n, ["tools"], J);
  }
  const w = c(t, ["toolConfig"]);
  if (n !== void 0 && w != null && h(n, ["toolConfig"], EJ(w)), c(t, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const _ = c(t, ["cachedContent"]);
  n !== void 0 && _ != null && h(n, ["cachedContent"], Ro(e, _));
  const b = c(t, ["responseModalities"]);
  b != null && h(s, ["responseModalities"], b);
  const C = c(t, ["mediaResolution"]);
  C != null && h(s, ["mediaResolution"], C);
  const k = c(t, ["speechConfig"]);
  if (k != null && h(s, ["speechConfig"], Jw(k)), c(t, ["audioTimestamp"]) !== void 0) throw new Error("audioTimestamp parameter is not supported in Gemini API.");
  const P = c(t, ["thinkingConfig"]);
  P != null && h(s, ["thinkingConfig"], P);
  const H = c(t, ["imageConfig"]);
  H != null && h(s, ["imageConfig"], tJ(H));
  const G = c(t, ["enableEnhancedCivicAnswers"]);
  if (G != null && h(s, ["enableEnhancedCivicAnswers"], G), c(t, ["modelArmorConfig"]) !== void 0) throw new Error("modelArmorConfig parameter is not supported in Gemini API.");
  const ee = c(t, ["serviceTier"]);
  return n !== void 0 && ee != null && h(n, ["serviceTier"], ee), s;
}
function kV(e, t, n, r) {
  const s = {}, a = c(t, ["systemInstruction"]);
  n !== void 0 && a != null && h(n, ["systemInstruction"], pc(cr(a)));
  const o = c(t, ["temperature"]);
  o != null && h(s, ["temperature"], o);
  const l = c(t, ["topP"]);
  l != null && h(s, ["topP"], l);
  const i = c(t, ["topK"]);
  i != null && h(s, ["topK"], i);
  const u = c(t, ["candidateCount"]);
  u != null && h(s, ["candidateCount"], u);
  const d = c(t, ["maxOutputTokens"]);
  d != null && h(s, ["maxOutputTokens"], d);
  const f = c(t, ["stopSequences"]);
  f != null && h(s, ["stopSequences"], f);
  const m = c(t, ["responseLogprobs"]);
  m != null && h(s, ["responseLogprobs"], m);
  const v = c(t, ["logprobs"]);
  v != null && h(s, ["logprobs"], v);
  const y = c(t, ["presencePenalty"]);
  y != null && h(s, ["presencePenalty"], y);
  const S = c(t, ["frequencyPenalty"]);
  S != null && h(s, ["frequencyPenalty"], S);
  const x = c(t, ["seed"]);
  x != null && h(s, ["seed"], x);
  const I = c(t, ["responseMimeType"]);
  I != null && h(s, ["responseMimeType"], I);
  const E = c(t, ["responseSchema"]);
  E != null && h(s, ["responseSchema"], Vw(E));
  const N = c(t, ["responseJsonSchema"]);
  N != null && h(s, ["responseJsonSchema"], N);
  const $ = c(t, ["routingConfig"]);
  $ != null && h(s, ["routingConfig"], $);
  const p = c(t, ["modelSelectionConfig"]);
  p != null && h(s, ["modelConfig"], p);
  const w = c(t, ["safetySettings"]);
  if (n !== void 0 && w != null) {
    let le = w;
    Array.isArray(le) && (le = le.map((ve) => ve)), h(n, ["safetySettings"], le);
  }
  const _ = c(t, ["tools"]);
  if (n !== void 0 && _ != null) {
    let le = fc(_);
    Array.isArray(le) && (le = le.map((ve) => AP(dc(ve)))), h(n, ["tools"], le);
  }
  const b = c(t, ["toolConfig"]);
  n !== void 0 && b != null && h(n, ["toolConfig"], CJ(b));
  const C = c(t, ["labels"]);
  n !== void 0 && C != null && h(n, ["labels"], C);
  const k = c(t, ["cachedContent"]);
  n !== void 0 && k != null && h(n, ["cachedContent"], Ro(e, k));
  const P = c(t, ["responseModalities"]);
  P != null && h(s, ["responseModalities"], P);
  const H = c(t, ["mediaResolution"]);
  H != null && h(s, ["mediaResolution"], H);
  const G = c(t, ["speechConfig"]);
  G != null && h(s, ["speechConfig"], Jw(G));
  const ee = c(t, ["audioTimestamp"]);
  ee != null && h(s, ["audioTimestamp"], ee);
  const J = c(t, ["thinkingConfig"]);
  J != null && h(s, ["thinkingConfig"], J);
  const z = c(t, ["imageConfig"]);
  if (z != null && h(s, ["imageConfig"], nJ(z)), c(t, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  const R = c(t, ["modelArmorConfig"]);
  n !== void 0 && R != null && h(n, ["modelArmorConfig"], R);
  const V = c(t, ["serviceTier"]);
  return n !== void 0 && V != null && h(n, ["serviceTier"], V), s;
}
function DC(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["contents"]);
  if (a != null) {
    let l = Yr(a);
    Array.isArray(l) && (l = l.map((i) => Vf(i))), h(r, ["contents"], l);
  }
  const o = c(t, ["config"]);
  return o != null && h(r, ["generationConfig"], xV(e, o, r)), r;
}
function OC(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["contents"]);
  if (a != null) {
    let l = Yr(a);
    Array.isArray(l) && (l = l.map((i) => pc(i))), h(r, ["contents"], l);
  }
  const o = c(t, ["config"]);
  return o != null && h(r, ["generationConfig"], kV(e, o, r)), r;
}
function FC(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["candidates"]);
  if (s != null) {
    let d = s;
    Array.isArray(d) && (d = d.map((f) => jG(f))), h(n, ["candidates"], d);
  }
  const a = c(e, ["modelVersion"]);
  a != null && h(n, ["modelVersion"], a);
  const o = c(e, ["promptFeedback"]);
  o != null && h(n, ["promptFeedback"], o);
  const l = c(e, ["responseId"]);
  l != null && h(n, ["responseId"], l);
  const i = c(e, ["usageMetadata"]);
  i != null && h(n, ["usageMetadata"], i);
  const u = c(e, ["modelStatus"]);
  return u != null && h(n, ["modelStatus"], u), n;
}
function BC(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["candidates"]);
  if (s != null) {
    let d = s;
    Array.isArray(d) && (d = d.map((f) => f)), h(n, ["candidates"], d);
  }
  const a = c(e, ["createTime"]);
  a != null && h(n, ["createTime"], a);
  const o = c(e, ["modelVersion"]);
  o != null && h(n, ["modelVersion"], o);
  const l = c(e, ["promptFeedback"]);
  l != null && h(n, ["promptFeedback"], l);
  const i = c(e, ["responseId"]);
  i != null && h(n, ["responseId"], i);
  const u = c(e, ["usageMetadata"]);
  return u != null && h(n, ["usageMetadata"], u), n;
}
function MV(e, t, n) {
  const r = {};
  if (c(e, ["outputGcsUri"]) !== void 0) throw new Error("outputGcsUri parameter is not supported in Gemini API.");
  if (c(e, ["negativePrompt"]) !== void 0) throw new Error("negativePrompt parameter is not supported in Gemini API.");
  const s = c(e, ["numberOfImages"]);
  t !== void 0 && s != null && h(t, ["parameters", "sampleCount"], s);
  const a = c(e, ["aspectRatio"]);
  t !== void 0 && a != null && h(t, ["parameters", "aspectRatio"], a);
  const o = c(e, ["guidanceScale"]);
  if (t !== void 0 && o != null && h(t, ["parameters", "guidanceScale"], o), c(e, ["seed"]) !== void 0) throw new Error("seed parameter is not supported in Gemini API.");
  const l = c(e, ["safetyFilterLevel"]);
  t !== void 0 && l != null && h(t, ["parameters", "safetySetting"], l);
  const i = c(e, ["personGeneration"]);
  t !== void 0 && i != null && h(t, ["parameters", "personGeneration"], i);
  const u = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && u != null && h(t, ["parameters", "includeSafetyAttributes"], u);
  const d = c(e, ["includeRaiReason"]);
  t !== void 0 && d != null && h(t, ["parameters", "includeRaiReason"], d);
  const f = c(e, ["language"]);
  t !== void 0 && f != null && h(t, ["parameters", "language"], f);
  const m = c(e, ["outputMimeType"]);
  t !== void 0 && m != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], m);
  const v = c(e, ["outputCompressionQuality"]);
  if (t !== void 0 && v != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], v), c(e, ["addWatermark"]) !== void 0) throw new Error("addWatermark parameter is not supported in Gemini API.");
  if (c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const y = c(e, ["imageSize"]);
  if (t !== void 0 && y != null && h(t, ["parameters", "sampleImageSize"], y), c(e, ["enhancePrompt"]) !== void 0) throw new Error("enhancePrompt parameter is not supported in Gemini API.");
  return r;
}
function PV(e, t, n) {
  const r = {}, s = c(e, ["outputGcsUri"]);
  t !== void 0 && s != null && h(t, ["parameters", "storageUri"], s);
  const a = c(e, ["negativePrompt"]);
  t !== void 0 && a != null && h(t, ["parameters", "negativePrompt"], a);
  const o = c(e, ["numberOfImages"]);
  t !== void 0 && o != null && h(t, ["parameters", "sampleCount"], o);
  const l = c(e, ["aspectRatio"]);
  t !== void 0 && l != null && h(t, ["parameters", "aspectRatio"], l);
  const i = c(e, ["guidanceScale"]);
  t !== void 0 && i != null && h(t, ["parameters", "guidanceScale"], i);
  const u = c(e, ["seed"]);
  t !== void 0 && u != null && h(t, ["parameters", "seed"], u);
  const d = c(e, ["safetyFilterLevel"]);
  t !== void 0 && d != null && h(t, ["parameters", "safetySetting"], d);
  const f = c(e, ["personGeneration"]);
  t !== void 0 && f != null && h(t, ["parameters", "personGeneration"], f);
  const m = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && m != null && h(t, ["parameters", "includeSafetyAttributes"], m);
  const v = c(e, ["includeRaiReason"]);
  t !== void 0 && v != null && h(t, ["parameters", "includeRaiReason"], v);
  const y = c(e, ["language"]);
  t !== void 0 && y != null && h(t, ["parameters", "language"], y);
  const S = c(e, ["outputMimeType"]);
  t !== void 0 && S != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], S);
  const x = c(e, ["outputCompressionQuality"]);
  t !== void 0 && x != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], x);
  const I = c(e, ["addWatermark"]);
  t !== void 0 && I != null && h(t, ["parameters", "addWatermark"], I);
  const E = c(e, ["labels"]);
  t !== void 0 && E != null && h(t, ["labels"], E);
  const N = c(e, ["imageSize"]);
  t !== void 0 && N != null && h(t, ["parameters", "sampleImageSize"], N);
  const $ = c(e, ["enhancePrompt"]);
  return t !== void 0 && $ != null && h(t, ["parameters", "enhancePrompt"], $), r;
}
function RV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["prompt"]);
  a != null && h(r, ["instances[0]", "prompt"], a);
  const o = c(t, ["config"]);
  return o != null && MV(o, r), r;
}
function NV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["prompt"]);
  a != null && h(r, ["instances[0]", "prompt"], a);
  const o = c(t, ["config"]);
  return o != null && PV(o, r), r;
}
function $V(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((l) => GV(l))), h(n, ["generatedImages"], o);
  }
  const a = c(e, ["positivePromptSafetyAttributes"]);
  return a != null && h(n, ["positivePromptSafetyAttributes"], wP(a)), n;
}
function LV(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((l) => cv(l))), h(n, ["generatedImages"], o);
  }
  const a = c(e, ["positivePromptSafetyAttributes"]);
  return a != null && h(n, ["positivePromptSafetyAttributes"], SP(a)), n;
}
function DV(e, t, n) {
  const r = {}, s = c(e, ["numberOfVideos"]);
  if (t !== void 0 && s != null && h(t, ["parameters", "sampleCount"], s), c(e, ["outputGcsUri"]) !== void 0) throw new Error("outputGcsUri parameter is not supported in Gemini API.");
  if (c(e, ["fps"]) !== void 0) throw new Error("fps parameter is not supported in Gemini API.");
  const a = c(e, ["durationSeconds"]);
  if (t !== void 0 && a != null && h(t, ["parameters", "durationSeconds"], a), c(e, ["seed"]) !== void 0) throw new Error("seed parameter is not supported in Gemini API.");
  const o = c(e, ["aspectRatio"]);
  t !== void 0 && o != null && h(t, ["parameters", "aspectRatio"], o);
  const l = c(e, ["resolution"]);
  t !== void 0 && l != null && h(t, ["parameters", "resolution"], l);
  const i = c(e, ["personGeneration"]);
  if (t !== void 0 && i != null && h(t, ["parameters", "personGeneration"], i), c(e, ["pubsubTopic"]) !== void 0) throw new Error("pubsubTopic parameter is not supported in Gemini API.");
  const u = c(e, ["negativePrompt"]);
  t !== void 0 && u != null && h(t, ["parameters", "negativePrompt"], u);
  const d = c(e, ["enhancePrompt"]);
  if (t !== void 0 && d != null && h(t, ["parameters", "enhancePrompt"], d), c(e, ["generateAudio"]) !== void 0) throw new Error("generateAudio parameter is not supported in Gemini API.");
  const f = c(e, ["lastFrame"]);
  t !== void 0 && f != null && h(t, ["instances[0]", "lastFrame"], dv(f));
  const m = c(e, ["referenceImages"]);
  if (t !== void 0 && m != null) {
    let y = m;
    Array.isArray(y) && (y = y.map((S) => UJ(S))), h(t, ["instances[0]", "referenceImages"], y);
  }
  if (c(e, ["mask"]) !== void 0) throw new Error("mask parameter is not supported in Gemini API.");
  if (c(e, ["compressionQuality"]) !== void 0) throw new Error("compressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const v = c(e, ["webhookConfig"]);
  return t !== void 0 && v != null && h(t, ["webhookConfig"], v), r;
}
function OV(e, t, n) {
  const r = {}, s = c(e, ["numberOfVideos"]);
  t !== void 0 && s != null && h(t, ["parameters", "sampleCount"], s);
  const a = c(e, ["outputGcsUri"]);
  t !== void 0 && a != null && h(t, ["parameters", "storageUri"], a);
  const o = c(e, ["fps"]);
  t !== void 0 && o != null && h(t, ["parameters", "fps"], o);
  const l = c(e, ["durationSeconds"]);
  t !== void 0 && l != null && h(t, ["parameters", "durationSeconds"], l);
  const i = c(e, ["seed"]);
  t !== void 0 && i != null && h(t, ["parameters", "seed"], i);
  const u = c(e, ["aspectRatio"]);
  t !== void 0 && u != null && h(t, ["parameters", "aspectRatio"], u);
  const d = c(e, ["resolution"]);
  t !== void 0 && d != null && h(t, ["parameters", "resolution"], d);
  const f = c(e, ["personGeneration"]);
  t !== void 0 && f != null && h(t, ["parameters", "personGeneration"], f);
  const m = c(e, ["pubsubTopic"]);
  t !== void 0 && m != null && h(t, ["parameters", "pubsubTopic"], m);
  const v = c(e, ["negativePrompt"]);
  t !== void 0 && v != null && h(t, ["parameters", "negativePrompt"], v);
  const y = c(e, ["enhancePrompt"]);
  t !== void 0 && y != null && h(t, ["parameters", "enhancePrompt"], y);
  const S = c(e, ["generateAudio"]);
  t !== void 0 && S != null && h(t, ["parameters", "generateAudio"], S);
  const x = c(e, ["lastFrame"]);
  t !== void 0 && x != null && h(t, ["instances[0]", "lastFrame"], ca(x));
  const I = c(e, ["referenceImages"]);
  if (t !== void 0 && I != null) {
    let p = I;
    Array.isArray(p) && (p = p.map((w) => qJ(w))), h(t, ["instances[0]", "referenceImages"], p);
  }
  const E = c(e, ["mask"]);
  t !== void 0 && E != null && h(t, ["instances[0]", "mask"], BJ(E));
  const N = c(e, ["compressionQuality"]);
  t !== void 0 && N != null && h(t, ["parameters", "compressionQuality"], N);
  const $ = c(e, ["labels"]);
  if (t !== void 0 && $ != null && h(t, ["labels"], $), c(e, ["webhookConfig"]) !== void 0) throw new Error("webhookConfig parameter is not supported in Vertex AI.");
  return r;
}
function FV(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["name"], r);
  const s = c(e, ["metadata"]);
  s != null && h(n, ["metadata"], s);
  const a = c(e, ["done"]);
  a != null && h(n, ["done"], a);
  const o = c(e, ["error"]);
  o != null && h(n, ["error"], o);
  const l = c(e, ["response", "generateVideoResponse"]);
  return l != null && h(n, ["response"], HV(l)), n;
}
function BV(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["name"], r);
  const s = c(e, ["metadata"]);
  s != null && h(n, ["metadata"], s);
  const a = c(e, ["done"]);
  a != null && h(n, ["done"], a);
  const o = c(e, ["error"]);
  o != null && h(n, ["error"], o);
  const l = c(e, ["response"]);
  return l != null && h(n, ["response"], zV(l)), n;
}
function UV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["prompt"]);
  a != null && h(r, ["instances[0]", "prompt"], a);
  const o = c(t, ["image"]);
  o != null && h(r, ["instances[0]", "image"], dv(o));
  const l = c(t, ["video"]);
  l != null && h(r, ["instances[0]", "video"], TP(l));
  const i = c(t, ["source"]);
  i != null && KV(i, r);
  const u = c(t, ["config"]);
  return u != null && DV(u, r), r;
}
function qV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["prompt"]);
  a != null && h(r, ["instances[0]", "prompt"], a);
  const o = c(t, ["image"]);
  o != null && h(r, ["instances[0]", "image"], ca(o));
  const l = c(t, ["video"]);
  l != null && h(r, ["instances[0]", "video"], EP(l));
  const i = c(t, ["source"]);
  i != null && WV(i, r);
  const u = c(t, ["config"]);
  return u != null && OV(u, r), r;
}
function HV(e, t) {
  const n = {}, r = c(e, ["generatedSamples"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((l) => JV(l))), h(n, ["generatedVideos"], o);
  }
  const s = c(e, ["raiMediaFilteredCount"]);
  s != null && h(n, ["raiMediaFilteredCount"], s);
  const a = c(e, ["raiMediaFilteredReasons"]);
  return a != null && h(n, ["raiMediaFilteredReasons"], a), n;
}
function zV(e, t) {
  const n = {}, r = c(e, ["videos"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((l) => YV(l))), h(n, ["generatedVideos"], o);
  }
  const s = c(e, ["raiMediaFilteredCount"]);
  s != null && h(n, ["raiMediaFilteredCount"], s);
  const a = c(e, ["raiMediaFilteredReasons"]);
  return a != null && h(n, ["raiMediaFilteredReasons"], a), n;
}
function KV(e, t, n) {
  const r = {}, s = c(e, ["prompt"]);
  t !== void 0 && s != null && h(t, ["instances[0]", "prompt"], s);
  const a = c(e, ["image"]);
  t !== void 0 && a != null && h(t, ["instances[0]", "image"], dv(a));
  const o = c(e, ["video"]);
  return t !== void 0 && o != null && h(t, ["instances[0]", "video"], TP(o)), r;
}
function WV(e, t, n) {
  const r = {}, s = c(e, ["prompt"]);
  t !== void 0 && s != null && h(t, ["instances[0]", "prompt"], s);
  const a = c(e, ["image"]);
  t !== void 0 && a != null && h(t, ["instances[0]", "image"], ca(a));
  const o = c(e, ["video"]);
  return t !== void 0 && o != null && h(t, ["instances[0]", "video"], EP(o)), r;
}
function GV(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && h(n, ["image"], rJ(r));
  const s = c(e, ["raiFilteredReason"]);
  s != null && h(n, ["raiFilteredReason"], s);
  const a = c(e, ["_self"]);
  return a != null && h(n, ["safetyAttributes"], wP(a)), n;
}
function cv(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && h(n, ["image"], bP(r));
  const s = c(e, ["raiFilteredReason"]);
  s != null && h(n, ["raiFilteredReason"], s);
  const a = c(e, ["_self"]);
  a != null && h(n, ["safetyAttributes"], SP(a));
  const o = c(e, ["prompt"]);
  return o != null && h(n, ["enhancedPrompt"], o), n;
}
function VV(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && h(n, ["mask"], bP(r));
  const s = c(e, ["labels"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => o)), h(n, ["labels"], a);
  }
  return n;
}
function JV(e, t) {
  const n = {}, r = c(e, ["video"]);
  return r != null && h(n, ["video"], OJ(r)), n;
}
function YV(e, t) {
  const n = {}, r = c(e, ["_self"]);
  return r != null && h(n, ["video"], FJ(r)), n;
}
function XV(e, t) {
  const n = {}, r = c(e, ["modelSelectionConfig"]);
  r != null && h(n, ["modelConfig"], r);
  const s = c(e, ["responseJsonSchema"]);
  s != null && h(n, ["responseJsonSchema"], s);
  const a = c(e, ["audioTimestamp"]);
  a != null && h(n, ["audioTimestamp"], a);
  const o = c(e, ["candidateCount"]);
  o != null && h(n, ["candidateCount"], o);
  const l = c(e, ["enableAffectiveDialog"]);
  l != null && h(n, ["enableAffectiveDialog"], l);
  const i = c(e, ["frequencyPenalty"]);
  i != null && h(n, ["frequencyPenalty"], i);
  const u = c(e, ["logprobs"]);
  u != null && h(n, ["logprobs"], u);
  const d = c(e, ["maxOutputTokens"]);
  d != null && h(n, ["maxOutputTokens"], d);
  const f = c(e, ["mediaResolution"]);
  f != null && h(n, ["mediaResolution"], f);
  const m = c(e, ["presencePenalty"]);
  m != null && h(n, ["presencePenalty"], m);
  const v = c(e, ["responseLogprobs"]);
  v != null && h(n, ["responseLogprobs"], v);
  const y = c(e, ["responseMimeType"]);
  y != null && h(n, ["responseMimeType"], y);
  const S = c(e, ["responseModalities"]);
  S != null && h(n, ["responseModalities"], S);
  const x = c(e, ["responseSchema"]);
  x != null && h(n, ["responseSchema"], x);
  const I = c(e, ["routingConfig"]);
  I != null && h(n, ["routingConfig"], I);
  const E = c(e, ["seed"]);
  E != null && h(n, ["seed"], E);
  const N = c(e, ["speechConfig"]);
  N != null && h(n, ["speechConfig"], N);
  const $ = c(e, ["stopSequences"]);
  $ != null && h(n, ["stopSequences"], $);
  const p = c(e, ["temperature"]);
  p != null && h(n, ["temperature"], p);
  const w = c(e, ["thinkingConfig"]);
  w != null && h(n, ["thinkingConfig"], w);
  const _ = c(e, ["topK"]);
  _ != null && h(n, ["topK"], _);
  const b = c(e, ["topP"]);
  if (b != null && h(n, ["topP"], b), c(e, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  return n;
}
function jV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  return s != null && h(r, ["_url", "name"], Zt(e, s)), r;
}
function QV(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  return s != null && h(r, ["_url", "name"], Zt(e, s)), r;
}
function ZV(e, t) {
  const n = {}, r = c(e, ["authConfig"]);
  r != null && h(n, ["authConfig"], YG(r));
  const s = c(e, ["enableWidget"]);
  return s != null && h(n, ["enableWidget"], s), n;
}
function eJ(e, t) {
  const n = {}, r = c(e, ["searchTypes"]);
  if (r != null && h(n, ["searchTypes"], r), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const s = c(e, ["timeRangeFilter"]);
  return s != null && h(n, ["timeRangeFilter"], s), n;
}
function tJ(e, t) {
  const n = {}, r = c(e, ["aspectRatio"]);
  r != null && h(n, ["aspectRatio"], r);
  const s = c(e, ["imageSize"]);
  if (s != null && h(n, ["imageSize"], s), c(e, ["personGeneration"]) !== void 0) throw new Error("personGeneration parameter is not supported in Gemini API.");
  if (c(e, ["prominentPeople"]) !== void 0) throw new Error("prominentPeople parameter is not supported in Gemini API.");
  if (c(e, ["outputMimeType"]) !== void 0) throw new Error("outputMimeType parameter is not supported in Gemini API.");
  if (c(e, ["outputCompressionQuality"]) !== void 0) throw new Error("outputCompressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["imageOutputOptions"]) !== void 0) throw new Error("imageOutputOptions parameter is not supported in Gemini API.");
  return n;
}
function nJ(e, t) {
  const n = {}, r = c(e, ["aspectRatio"]);
  r != null && h(n, ["aspectRatio"], r);
  const s = c(e, ["imageSize"]);
  s != null && h(n, ["imageSize"], s);
  const a = c(e, ["personGeneration"]);
  a != null && h(n, ["personGeneration"], a);
  const o = c(e, ["prominentPeople"]);
  o != null && h(n, ["prominentPeople"], o);
  const l = c(e, ["outputMimeType"]);
  l != null && h(n, ["imageOutputOptions", "mimeType"], l);
  const i = c(e, ["outputCompressionQuality"]);
  i != null && h(n, ["imageOutputOptions", "compressionQuality"], i);
  const u = c(e, ["imageOutputOptions"]);
  return u != null && h(n, ["imageOutputOptions"], u), n;
}
function rJ(e, t) {
  const n = {}, r = c(e, ["bytesBase64Encoded"]);
  r != null && h(n, ["imageBytes"], Ti(r));
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function bP(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && h(n, ["gcsUri"], r);
  const s = c(e, ["bytesBase64Encoded"]);
  s != null && h(n, ["imageBytes"], Ti(s));
  const a = c(e, ["mimeType"]);
  return a != null && h(n, ["mimeType"], a), n;
}
function dv(e, t) {
  const n = {};
  if (c(e, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  const r = c(e, ["imageBytes"]);
  r != null && h(n, ["bytesBase64Encoded"], Ti(r));
  const s = c(e, ["mimeType"]);
  return s != null && h(n, ["mimeType"], s), n;
}
function ca(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && h(n, ["gcsUri"], r);
  const s = c(e, ["imageBytes"]);
  s != null && h(n, ["bytesBase64Encoded"], Ti(s));
  const a = c(e, ["mimeType"]);
  return a != null && h(n, ["mimeType"], a), n;
}
function sJ(e, t, n, r) {
  const s = {}, a = c(t, ["pageSize"]);
  n !== void 0 && a != null && h(n, ["_query", "pageSize"], a);
  const o = c(t, ["pageToken"]);
  n !== void 0 && o != null && h(n, ["_query", "pageToken"], o);
  const l = c(t, ["filter"]);
  n !== void 0 && l != null && h(n, ["_query", "filter"], l);
  const i = c(t, ["queryBase"]);
  return n !== void 0 && i != null && h(n, ["_url", "models_url"], dP(e, i)), s;
}
function aJ(e, t, n, r) {
  const s = {}, a = c(t, ["pageSize"]);
  n !== void 0 && a != null && h(n, ["_query", "pageSize"], a);
  const o = c(t, ["pageToken"]);
  n !== void 0 && o != null && h(n, ["_query", "pageToken"], o);
  const l = c(t, ["filter"]);
  n !== void 0 && l != null && h(n, ["_query", "filter"], l);
  const i = c(t, ["queryBase"]);
  return n !== void 0 && i != null && h(n, ["_url", "models_url"], dP(e, i)), s;
}
function oJ(e, t, n) {
  const r = {}, s = c(t, ["config"]);
  return s != null && sJ(e, s, r), r;
}
function iJ(e, t, n) {
  const r = {}, s = c(t, ["config"]);
  return s != null && aJ(e, s, r), r;
}
function lJ(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["nextPageToken"]);
  s != null && h(n, ["nextPageToken"], s);
  const a = c(e, ["_self"]);
  if (a != null) {
    let o = fP(a);
    Array.isArray(o) && (o = o.map((l) => y0(l))), h(n, ["models"], o);
  }
  return n;
}
function uJ(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["nextPageToken"]);
  s != null && h(n, ["nextPageToken"], s);
  const a = c(e, ["_self"]);
  if (a != null) {
    let o = fP(a);
    Array.isArray(o) && (o = o.map((l) => _0(l))), h(n, ["models"], o);
  }
  return n;
}
function cJ(e, t) {
  const n = {}, r = c(e, ["maskMode"]);
  r != null && h(n, ["maskMode"], r);
  const s = c(e, ["segmentationClasses"]);
  s != null && h(n, ["maskClasses"], s);
  const a = c(e, ["maskDilation"]);
  return a != null && h(n, ["dilation"], a), n;
}
function y0(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["name"], r);
  const s = c(e, ["displayName"]);
  s != null && h(n, ["displayName"], s);
  const a = c(e, ["description"]);
  a != null && h(n, ["description"], a);
  const o = c(e, ["version"]);
  o != null && h(n, ["version"], o);
  const l = c(e, ["_self"]);
  l != null && h(n, ["tunedModelInfo"], xJ(l));
  const i = c(e, ["inputTokenLimit"]);
  i != null && h(n, ["inputTokenLimit"], i);
  const u = c(e, ["outputTokenLimit"]);
  u != null && h(n, ["outputTokenLimit"], u);
  const d = c(e, ["supportedGenerationMethods"]);
  d != null && h(n, ["supportedActions"], d);
  const f = c(e, ["temperature"]);
  f != null && h(n, ["temperature"], f);
  const m = c(e, ["maxTemperature"]);
  m != null && h(n, ["maxTemperature"], m);
  const v = c(e, ["topP"]);
  v != null && h(n, ["topP"], v);
  const y = c(e, ["topK"]);
  y != null && h(n, ["topK"], y);
  const S = c(e, ["thinking"]);
  return S != null && h(n, ["thinking"], S), n;
}
function _0(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["name"], r);
  const s = c(e, ["displayName"]);
  s != null && h(n, ["displayName"], s);
  const a = c(e, ["description"]);
  a != null && h(n, ["description"], a);
  const o = c(e, ["versionId"]);
  o != null && h(n, ["version"], o);
  const l = c(e, ["deployedModels"]);
  if (l != null) {
    let m = l;
    Array.isArray(m) && (m = m.map((v) => AV(v))), h(n, ["endpoints"], m);
  }
  const i = c(e, ["labels"]);
  i != null && h(n, ["labels"], i);
  const u = c(e, ["_self"]);
  u != null && h(n, ["tunedModelInfo"], kJ(u));
  const d = c(e, ["defaultCheckpointId"]);
  d != null && h(n, ["defaultCheckpointId"], d);
  const f = c(e, ["checkpoints"]);
  if (f != null) {
    let m = f;
    Array.isArray(m) && (m = m.map((v) => v)), h(n, ["checkpoints"], m);
  }
  return n;
}
function dJ(e, t) {
  const n = {}, r = c(e, ["mediaResolution"]);
  r != null && h(n, ["mediaResolution"], r);
  const s = c(e, ["codeExecutionResult"]);
  s != null && h(n, ["codeExecutionResult"], s);
  const a = c(e, ["executableCode"]);
  a != null && h(n, ["executableCode"], a);
  const o = c(e, ["fileData"]);
  o != null && h(n, ["fileData"], TV(o));
  const l = c(e, ["functionCall"]);
  l != null && h(n, ["functionCall"], EV(l));
  const i = c(e, ["functionResponse"]);
  i != null && h(n, ["functionResponse"], i);
  const u = c(e, ["inlineData"]);
  u != null && h(n, ["inlineData"], XG(u));
  const d = c(e, ["text"]);
  d != null && h(n, ["text"], d);
  const f = c(e, ["thought"]);
  f != null && h(n, ["thought"], f);
  const m = c(e, ["thoughtSignature"]);
  m != null && h(n, ["thoughtSignature"], m);
  const v = c(e, ["videoMetadata"]);
  v != null && h(n, ["videoMetadata"], v);
  const y = c(e, ["toolCall"]);
  y != null && h(n, ["toolCall"], y);
  const S = c(e, ["toolResponse"]);
  S != null && h(n, ["toolResponse"], S);
  const x = c(e, ["partMetadata"]);
  return x != null && h(n, ["partMetadata"], x), n;
}
function fJ(e, t) {
  const n = {}, r = c(e, ["mediaResolution"]);
  r != null && h(n, ["mediaResolution"], r);
  const s = c(e, ["codeExecutionResult"]);
  s != null && h(n, ["codeExecutionResult"], s);
  const a = c(e, ["executableCode"]);
  a != null && h(n, ["executableCode"], a);
  const o = c(e, ["fileData"]);
  o != null && h(n, ["fileData"], o);
  const l = c(e, ["functionCall"]);
  l != null && h(n, ["functionCall"], l);
  const i = c(e, ["functionResponse"]);
  i != null && h(n, ["functionResponse"], i);
  const u = c(e, ["inlineData"]);
  u != null && h(n, ["inlineData"], u);
  const d = c(e, ["text"]);
  d != null && h(n, ["text"], d);
  const f = c(e, ["thought"]);
  f != null && h(n, ["thought"], f);
  const m = c(e, ["thoughtSignature"]);
  m != null && h(n, ["thoughtSignature"], m);
  const v = c(e, ["videoMetadata"]);
  if (v != null && h(n, ["videoMetadata"], v), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return n;
}
function hJ(e, t) {
  const n = {}, r = c(e, ["productImage"]);
  return r != null && h(n, ["image"], ca(r)), n;
}
function pJ(e, t, n) {
  const r = {}, s = c(e, ["numberOfImages"]);
  t !== void 0 && s != null && h(t, ["parameters", "sampleCount"], s);
  const a = c(e, ["baseSteps"]);
  t !== void 0 && a != null && h(t, ["parameters", "baseSteps"], a);
  const o = c(e, ["outputGcsUri"]);
  t !== void 0 && o != null && h(t, ["parameters", "storageUri"], o);
  const l = c(e, ["seed"]);
  t !== void 0 && l != null && h(t, ["parameters", "seed"], l);
  const i = c(e, ["safetyFilterLevel"]);
  t !== void 0 && i != null && h(t, ["parameters", "safetySetting"], i);
  const u = c(e, ["personGeneration"]);
  t !== void 0 && u != null && h(t, ["parameters", "personGeneration"], u);
  const d = c(e, ["addWatermark"]);
  t !== void 0 && d != null && h(t, ["parameters", "addWatermark"], d);
  const f = c(e, ["outputMimeType"]);
  t !== void 0 && f != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], f);
  const m = c(e, ["outputCompressionQuality"]);
  t !== void 0 && m != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], m);
  const v = c(e, ["enhancePrompt"]);
  t !== void 0 && v != null && h(t, ["parameters", "enhancePrompt"], v);
  const y = c(e, ["labels"]);
  return t !== void 0 && y != null && h(t, ["labels"], y), r;
}
function mJ(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["source"]);
  a != null && vJ(a, r);
  const o = c(t, ["config"]);
  return o != null && pJ(o, r), r;
}
function gJ(e, t) {
  const n = {}, r = c(e, ["predictions"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => cv(a))), h(n, ["generatedImages"], s);
  }
  return n;
}
function vJ(e, t, n) {
  const r = {}, s = c(e, ["prompt"]);
  t !== void 0 && s != null && h(t, ["instances[0]", "prompt"], s);
  const a = c(e, ["personImage"]);
  t !== void 0 && a != null && h(t, [
    "instances[0]",
    "personImage",
    "image"
  ], ca(a));
  const o = c(e, ["productImages"]);
  if (t !== void 0 && o != null) {
    let l = o;
    Array.isArray(l) && (l = l.map((i) => hJ(i))), h(t, ["instances[0]", "productImages"], l);
  }
  return r;
}
function yJ(e, t) {
  const n = {}, r = c(e, ["referenceImage"]);
  r != null && h(n, ["referenceImage"], ca(r));
  const s = c(e, ["referenceId"]);
  s != null && h(n, ["referenceId"], s);
  const a = c(e, ["referenceType"]);
  a != null && h(n, ["referenceType"], a);
  const o = c(e, ["maskImageConfig"]);
  o != null && h(n, ["maskImageConfig"], cJ(o));
  const l = c(e, ["controlImageConfig"]);
  l != null && h(n, ["controlImageConfig"], rV(l));
  const i = c(e, ["styleImageConfig"]);
  i != null && h(n, ["styleImageConfig"], i);
  const u = c(e, ["subjectImageConfig"]);
  return u != null && h(n, ["subjectImageConfig"], u), n;
}
function wP(e, t) {
  const n = {}, r = c(e, ["safetyAttributes", "categories"]);
  r != null && h(n, ["categories"], r);
  const s = c(e, ["safetyAttributes", "scores"]);
  s != null && h(n, ["scores"], s);
  const a = c(e, ["contentType"]);
  return a != null && h(n, ["contentType"], a), n;
}
function SP(e, t) {
  const n = {}, r = c(e, ["safetyAttributes", "categories"]);
  r != null && h(n, ["categories"], r);
  const s = c(e, ["safetyAttributes", "scores"]);
  s != null && h(n, ["scores"], s);
  const a = c(e, ["contentType"]);
  return a != null && h(n, ["contentType"], a), n;
}
function _J(e, t) {
  const n = {}, r = c(e, ["category"]);
  if (r != null && h(n, ["category"], r), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const s = c(e, ["threshold"]);
  return s != null && h(n, ["threshold"], s), n;
}
function bJ(e, t) {
  const n = {}, r = c(e, ["image"]);
  return r != null && h(n, ["image"], ca(r)), n;
}
function wJ(e, t, n) {
  const r = {}, s = c(e, ["mode"]);
  t !== void 0 && s != null && h(t, ["parameters", "mode"], s);
  const a = c(e, ["maxPredictions"]);
  t !== void 0 && a != null && h(t, ["parameters", "maxPredictions"], a);
  const o = c(e, ["confidenceThreshold"]);
  t !== void 0 && o != null && h(t, ["parameters", "confidenceThreshold"], o);
  const l = c(e, ["maskDilation"]);
  t !== void 0 && l != null && h(t, ["parameters", "maskDilation"], l);
  const i = c(e, ["binaryColorThreshold"]);
  t !== void 0 && i != null && h(t, ["parameters", "binaryColorThreshold"], i);
  const u = c(e, ["labels"]);
  return t !== void 0 && u != null && h(t, ["labels"], u), r;
}
function SJ(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["source"]);
  a != null && TJ(a, r);
  const o = c(t, ["config"]);
  return o != null && wJ(o, r), r;
}
function AJ(e, t) {
  const n = {}, r = c(e, ["predictions"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => VV(a))), h(n, ["generatedMasks"], s);
  }
  return n;
}
function TJ(e, t, n) {
  const r = {}, s = c(e, ["prompt"]);
  t !== void 0 && s != null && h(t, ["instances[0]", "prompt"], s);
  const a = c(e, ["image"]);
  t !== void 0 && a != null && h(t, ["instances[0]", "image"], ca(a));
  const o = c(e, ["scribbleImage"]);
  return t !== void 0 && o != null && h(t, ["instances[0]", "scribble"], bJ(o)), r;
}
function EJ(e, t) {
  const n = {}, r = c(e, ["retrievalConfig"]);
  r != null && h(n, ["retrievalConfig"], r);
  const s = c(e, ["functionCallingConfig"]);
  s != null && h(n, ["functionCallingConfig"], CV(s));
  const a = c(e, ["includeServerSideToolInvocations"]);
  return a != null && h(n, ["includeServerSideToolInvocations"], a), n;
}
function CJ(e, t) {
  const n = {}, r = c(e, ["retrievalConfig"]);
  r != null && h(n, ["retrievalConfig"], r);
  const s = c(e, ["functionCallingConfig"]);
  if (s != null && h(n, ["functionCallingConfig"], s), c(e, ["includeServerSideToolInvocations"]) !== void 0) throw new Error("includeServerSideToolInvocations parameter is not supported in Vertex AI.");
  return n;
}
function IJ(e, t) {
  const n = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const r = c(e, ["computerUse"]);
  r != null && h(n, ["computerUse"], r);
  const s = c(e, ["fileSearch"]);
  s != null && h(n, ["fileSearch"], s);
  const a = c(e, ["googleSearch"]);
  a != null && h(n, ["googleSearch"], eJ(a));
  const o = c(e, ["googleMaps"]);
  o != null && h(n, ["googleMaps"], ZV(o));
  const l = c(e, ["codeExecution"]);
  if (l != null && h(n, ["codeExecution"], l), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const i = c(e, ["functionDeclarations"]);
  if (i != null) {
    let m = i;
    Array.isArray(m) && (m = m.map((v) => v)), h(n, ["functionDeclarations"], m);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  if (u != null && h(n, ["googleSearchRetrieval"], u), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const d = c(e, ["urlContext"]);
  d != null && h(n, ["urlContext"], d);
  const f = c(e, ["mcpServers"]);
  if (f != null) {
    let m = f;
    Array.isArray(m) && (m = m.map((v) => v)), h(n, ["mcpServers"], m);
  }
  return n;
}
function AP(e, t) {
  const n = {}, r = c(e, ["retrieval"]);
  r != null && h(n, ["retrieval"], r);
  const s = c(e, ["computerUse"]);
  if (s != null && h(n, ["computerUse"], s), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const a = c(e, ["googleSearch"]);
  a != null && h(n, ["googleSearch"], a);
  const o = c(e, ["googleMaps"]);
  o != null && h(n, ["googleMaps"], o);
  const l = c(e, ["codeExecution"]);
  l != null && h(n, ["codeExecution"], l);
  const i = c(e, ["enterpriseWebSearch"]);
  i != null && h(n, ["enterpriseWebSearch"], i);
  const u = c(e, ["functionDeclarations"]);
  if (u != null) {
    let v = u;
    Array.isArray(v) && (v = v.map((y) => IV(y))), h(n, ["functionDeclarations"], v);
  }
  const d = c(e, ["googleSearchRetrieval"]);
  d != null && h(n, ["googleSearchRetrieval"], d);
  const f = c(e, ["parallelAiSearch"]);
  f != null && h(n, ["parallelAiSearch"], f);
  const m = c(e, ["urlContext"]);
  if (m != null && h(n, ["urlContext"], m), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return n;
}
function xJ(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && h(n, ["baseModel"], r);
  const s = c(e, ["createTime"]);
  s != null && h(n, ["createTime"], s);
  const a = c(e, ["updateTime"]);
  return a != null && h(n, ["updateTime"], a), n;
}
function kJ(e, t) {
  const n = {}, r = c(e, ["labels", "google-vertex-llm-tuning-base-model-id"]);
  r != null && h(n, ["baseModel"], r);
  const s = c(e, ["createTime"]);
  s != null && h(n, ["createTime"], s);
  const a = c(e, ["updateTime"]);
  return a != null && h(n, ["updateTime"], a), n;
}
function MJ(e, t, n) {
  const r = {}, s = c(e, ["displayName"]);
  t !== void 0 && s != null && h(t, ["displayName"], s);
  const a = c(e, ["description"]);
  t !== void 0 && a != null && h(t, ["description"], a);
  const o = c(e, ["defaultCheckpointId"]);
  return t !== void 0 && o != null && h(t, ["defaultCheckpointId"], o), r;
}
function PJ(e, t, n) {
  const r = {}, s = c(e, ["displayName"]);
  t !== void 0 && s != null && h(t, ["displayName"], s);
  const a = c(e, ["description"]);
  t !== void 0 && a != null && h(t, ["description"], a);
  const o = c(e, ["defaultCheckpointId"]);
  return t !== void 0 && o != null && h(t, ["defaultCheckpointId"], o), r;
}
function RJ(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "name"], Zt(e, s));
  const a = c(t, ["config"]);
  return a != null && MJ(a, r), r;
}
function NJ(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["config"]);
  return a != null && PJ(a, r), r;
}
function $J(e, t, n) {
  const r = {}, s = c(e, ["outputGcsUri"]);
  t !== void 0 && s != null && h(t, ["parameters", "storageUri"], s);
  const a = c(e, ["safetyFilterLevel"]);
  t !== void 0 && a != null && h(t, ["parameters", "safetySetting"], a);
  const o = c(e, ["personGeneration"]);
  t !== void 0 && o != null && h(t, ["parameters", "personGeneration"], o);
  const l = c(e, ["includeRaiReason"]);
  t !== void 0 && l != null && h(t, ["parameters", "includeRaiReason"], l);
  const i = c(e, ["outputMimeType"]);
  t !== void 0 && i != null && h(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], i);
  const u = c(e, ["outputCompressionQuality"]);
  t !== void 0 && u != null && h(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], u);
  const d = c(e, ["enhanceInputImage"]);
  t !== void 0 && d != null && h(t, [
    "parameters",
    "upscaleConfig",
    "enhanceInputImage"
  ], d);
  const f = c(e, ["imagePreservationFactor"]);
  t !== void 0 && f != null && h(t, [
    "parameters",
    "upscaleConfig",
    "imagePreservationFactor"
  ], f);
  const m = c(e, ["labels"]);
  t !== void 0 && m != null && h(t, ["labels"], m);
  const v = c(e, ["numberOfImages"]);
  t !== void 0 && v != null && h(t, ["parameters", "sampleCount"], v);
  const y = c(e, ["mode"]);
  return t !== void 0 && y != null && h(t, ["parameters", "mode"], y), r;
}
function LJ(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && h(r, ["_url", "model"], Zt(e, s));
  const a = c(t, ["image"]);
  a != null && h(r, ["instances[0]", "image"], ca(a));
  const o = c(t, ["upscaleFactor"]);
  o != null && h(r, [
    "parameters",
    "upscaleConfig",
    "upscaleFactor"
  ], o);
  const l = c(t, ["config"]);
  return l != null && $J(l, r), r;
}
function DJ(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => cv(o))), h(n, ["generatedImages"], a);
  }
  return n;
}
function OJ(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && h(n, ["uri"], r);
  const s = c(e, ["encodedVideo"]);
  s != null && h(n, ["videoBytes"], Ti(s));
  const a = c(e, ["encoding"]);
  return a != null && h(n, ["mimeType"], a), n;
}
function FJ(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && h(n, ["uri"], r);
  const s = c(e, ["bytesBase64Encoded"]);
  s != null && h(n, ["videoBytes"], Ti(s));
  const a = c(e, ["mimeType"]);
  return a != null && h(n, ["mimeType"], a), n;
}
function BJ(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && h(n, ["_self"], ca(r));
  const s = c(e, ["maskMode"]);
  return s != null && h(n, ["maskMode"], s), n;
}
function UJ(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && h(n, ["image"], dv(r));
  const s = c(e, ["referenceType"]);
  return s != null && h(n, ["referenceType"], s), n;
}
function qJ(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && h(n, ["image"], ca(r));
  const s = c(e, ["referenceType"]);
  return s != null && h(n, ["referenceType"], s), n;
}
function TP(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && h(n, ["uri"], r);
  const s = c(e, ["videoBytes"]);
  s != null && h(n, ["encodedVideo"], Ti(s));
  const a = c(e, ["mimeType"]);
  return a != null && h(n, ["encoding"], a), n;
}
function EP(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && h(n, ["gcsUri"], r);
  const s = c(e, ["videoBytes"]);
  s != null && h(n, ["bytesBase64Encoded"], Ti(s));
  const a = c(e, ["mimeType"]);
  return a != null && h(n, ["mimeType"], a), n;
}
function HJ(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  return t !== void 0 && r != null && h(t, ["displayName"], r), n;
}
function zJ(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && HJ(n, t), t;
}
function KJ(e, t) {
  const n = {}, r = c(e, ["force"]);
  return t !== void 0 && r != null && h(t, ["_query", "force"], r), n;
}
function WJ(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["_url", "name"], n);
  const r = c(e, ["config"]);
  return r != null && KJ(r, t), t;
}
function GJ(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && h(t, ["_url", "name"], n), t;
}
function VJ(e, t) {
  const n = {}, r = c(e, ["customMetadata"]);
  if (t !== void 0 && r != null) {
    let a = r;
    Array.isArray(a) && (a = a.map((o) => o)), h(t, ["customMetadata"], a);
  }
  const s = c(e, ["chunkingConfig"]);
  return t !== void 0 && s != null && h(t, ["chunkingConfig"], s), n;
}
function JJ(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && h(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && h(t, ["done"], s);
  const a = c(e, ["error"]);
  a != null && h(t, ["error"], a);
  const o = c(e, ["response"]);
  return o != null && h(t, ["response"], XJ(o)), t;
}
function YJ(e) {
  const t = {}, n = c(e, ["fileSearchStoreName"]);
  n != null && h(t, ["_url", "file_search_store_name"], n);
  const r = c(e, ["fileName"]);
  r != null && h(t, ["fileName"], r);
  const s = c(e, ["config"]);
  return s != null && VJ(s, t), t;
}
function XJ(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && h(t, ["parent"], r);
  const s = c(e, ["documentName"]);
  return s != null && h(t, ["documentName"], s), t;
}
function jJ(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && h(t, ["_query", "pageToken"], s), n;
}
function QJ(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && jJ(n, t), t;
}
function ZJ(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const s = c(e, ["fileSearchStores"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => o)), h(t, ["fileSearchStores"], a);
  }
  return t;
}
function CP(e, t) {
  const n = {}, r = c(e, ["mimeType"]);
  t !== void 0 && r != null && h(t, ["mimeType"], r);
  const s = c(e, ["displayName"]);
  t !== void 0 && s != null && h(t, ["displayName"], s);
  const a = c(e, ["customMetadata"]);
  if (t !== void 0 && a != null) {
    let l = a;
    Array.isArray(l) && (l = l.map((i) => i)), h(t, ["customMetadata"], l);
  }
  const o = c(e, ["chunkingConfig"]);
  return t !== void 0 && o != null && h(t, ["chunkingConfig"], o), n;
}
function eY(e) {
  const t = {}, n = c(e, ["fileSearchStoreName"]);
  n != null && h(t, ["_url", "file_search_store_name"], n);
  const r = c(e, ["config"]);
  return r != null && CP(r, t), t;
}
function tY(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && h(t, ["sdkHttpResponse"], n), t;
}
var nY = "Content-Type", rY = "X-Server-Timeout", sY = "User-Agent", b0 = "x-goog-api-client", aY = "google-genai-sdk/1.50.1", oY = "v1beta1", iY = "v1beta", lY = /* @__PURE__ */ new Set(["us", "eu"]), uY = 5, cY = [
  408,
  429,
  500,
  502,
  503,
  504
], dY = class {
  constructor(e) {
    var t, n, r;
    this.clientOptions = Object.assign({}, e), this.customBaseUrl = (t = e.httpOptions) === null || t === void 0 ? void 0 : t.baseUrl, this.clientOptions.vertexai && (this.clientOptions.project && this.clientOptions.location ? this.clientOptions.apiKey = void 0 : this.clientOptions.apiKey && (this.clientOptions.project = void 0, this.clientOptions.location = void 0));
    const s = {};
    if (this.clientOptions.vertexai) {
      if (!this.clientOptions.location && !this.clientOptions.apiKey && !this.customBaseUrl && (this.clientOptions.location = "global"), !(this.clientOptions.project && this.clientOptions.location || this.clientOptions.apiKey) && !this.customBaseUrl) throw new Error("Authentication is not set up. Please provide either a project and location, or an API key, or a custom base URL.");
      const a = e.project && e.location || !!e.apiKey;
      this.customBaseUrl && !a ? (s.baseUrl = this.customBaseUrl, this.clientOptions.project = void 0, this.clientOptions.location = void 0) : this.clientOptions.apiKey || this.clientOptions.location === "global" ? s.baseUrl = "https://aiplatform.googleapis.com/" : this.clientOptions.project && this.clientOptions.location && lY.has(this.clientOptions.location) ? s.baseUrl = `https://aiplatform.${this.clientOptions.location}.rep.googleapis.com/` : this.clientOptions.project && this.clientOptions.location && (s.baseUrl = `https://${this.clientOptions.location}-aiplatform.googleapis.com/`), s.apiVersion = (n = this.clientOptions.apiVersion) !== null && n !== void 0 ? n : oY;
    } else
      this.clientOptions.apiKey || console.warn("API key should be set when using the Gemini API."), s.apiVersion = (r = this.clientOptions.apiVersion) !== null && r !== void 0 ? r : iY, s.baseUrl = "https://generativelanguage.googleapis.com/";
    s.headers = this.getDefaultHeaders(), this.clientOptions.httpOptions = s, e.httpOptions && (this.clientOptions.httpOptions = this.patchHttpOptions(s, e.httpOptions));
  }
  isVertexAI() {
    var e;
    return (e = this.clientOptions.vertexai) !== null && e !== void 0 ? e : !1;
  }
  getProject() {
    return this.clientOptions.project;
  }
  getLocation() {
    return this.clientOptions.location;
  }
  getCustomBaseUrl() {
    return this.customBaseUrl;
  }
  async getAuthHeaders() {
    const e = new Headers();
    return await this.clientOptions.auth.addAuthHeaders(e), e;
  }
  getApiVersion() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.apiVersion !== void 0) return this.clientOptions.httpOptions.apiVersion;
    throw new Error("API version is not set.");
  }
  getBaseUrl() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.baseUrl !== void 0) return this.clientOptions.httpOptions.baseUrl;
    throw new Error("Base URL is not set.");
  }
  getRequestUrl() {
    return this.getRequestUrlInternal(this.clientOptions.httpOptions);
  }
  getHeaders() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.headers !== void 0) return this.clientOptions.httpOptions.headers;
    throw new Error("Headers are not set.");
  }
  getRequestUrlInternal(e) {
    if (!e || e.baseUrl === void 0 || e.apiVersion === void 0) throw new Error("HTTP options are not correctly set.");
    const t = [e.baseUrl.endsWith("/") ? e.baseUrl.slice(0, -1) : e.baseUrl];
    return e.apiVersion && e.apiVersion !== "" && t.push(e.apiVersion), t.join("/");
  }
  getBaseResourcePath() {
    return `projects/${this.clientOptions.project}/locations/${this.clientOptions.location}`;
  }
  getApiKey() {
    return this.clientOptions.apiKey;
  }
  getWebsocketBaseUrl() {
    const e = this.getBaseUrl(), t = new URL(e);
    return t.protocol = t.protocol == "http:" ? "ws" : "wss", t.toString();
  }
  setBaseUrl(e) {
    if (this.clientOptions.httpOptions) this.clientOptions.httpOptions.baseUrl = e;
    else throw new Error("HTTP options are not correctly set.");
  }
  constructUrl(e, t, n) {
    const r = [this.getRequestUrlInternal(t)];
    return n && r.push(this.getBaseResourcePath()), e !== "" && r.push(e), new URL(`${r.join("/")}`);
  }
  shouldPrependVertexProjectPath(e, t) {
    return !(t.baseUrl && t.baseUrlResourceScope === p0.COLLECTION || this.clientOptions.apiKey || !this.clientOptions.vertexai || e.path.startsWith("projects/") || e.httpMethod === "GET" && e.path.startsWith("publishers/google/models"));
  }
  async request(e) {
    let t = this.clientOptions.httpOptions;
    e.httpOptions && (t = this.patchHttpOptions(this.clientOptions.httpOptions, e.httpOptions));
    const n = this.shouldPrependVertexProjectPath(e, t), r = this.constructUrl(e.path, t, n);
    if (e.queryParams) for (const [a, o] of Object.entries(e.queryParams)) r.searchParams.append(a, String(o));
    let s = {};
    if (e.httpMethod === "GET") {
      if (e.body && e.body !== "{}") throw new Error("Request body should be empty for GET request, but got non empty request body");
    } else s.body = e.body;
    return s = await this.includeExtraHttpOptionsToRequestInit(s, t, r.toString(), e.abortSignal), this.unaryApiCall(r, s, e.httpMethod);
  }
  patchHttpOptions(e, t) {
    const n = JSON.parse(JSON.stringify(e));
    for (const [r, s] of Object.entries(t)) typeof s == "object" ? n[r] = Object.assign(Object.assign({}, n[r]), s) : s !== void 0 && (n[r] = s);
    return n;
  }
  async requestStream(e) {
    let t = this.clientOptions.httpOptions;
    e.httpOptions && (t = this.patchHttpOptions(this.clientOptions.httpOptions, e.httpOptions));
    const n = this.shouldPrependVertexProjectPath(e, t), r = this.constructUrl(e.path, t, n);
    (!r.searchParams.has("alt") || r.searchParams.get("alt") !== "sse") && r.searchParams.set("alt", "sse");
    let s = {};
    return s.body = e.body, s = await this.includeExtraHttpOptionsToRequestInit(s, t, r.toString(), e.abortSignal), this.streamApiCall(r, s, e.httpMethod);
  }
  async includeExtraHttpOptionsToRequestInit(e, t, n, r) {
    if (t && t.timeout || r) {
      const s = new AbortController(), a = s.signal;
      if (t.timeout && t?.timeout > 0) {
        const o = setTimeout(() => s.abort(), t.timeout);
        o && typeof o.unref == "function" && o.unref();
      }
      r && r.addEventListener("abort", () => {
        s.abort();
      }), e.signal = a;
    }
    return t && t.extraBody !== null && fY(e, t.extraBody), e.headers = await this.getHeadersInternal(t, n), e;
  }
  async unaryApiCall(e, t, n) {
    return this.apiCall(e.toString(), Object.assign(Object.assign({}, t), { method: n })).then(async (r) => (await UC(r), new m0(r))).catch((r) => {
      throw r instanceof Error ? r : new Error(JSON.stringify(r));
    });
  }
  async streamApiCall(e, t, n) {
    return this.apiCall(e.toString(), Object.assign(Object.assign({}, t), { method: n })).then(async (r) => (await UC(r), this.processStreamResponse(r))).catch((r) => {
      throw r instanceof Error ? r : new Error(JSON.stringify(r));
    });
  }
  processStreamResponse(e) {
    return aa(this, arguments, function* () {
      var n;
      const r = (n = e?.body) === null || n === void 0 ? void 0 : n.getReader(), s = new TextDecoder("utf-8");
      if (!r) throw new Error("Response body is empty");
      try {
        let a = "";
        const o = "data:", l = [
          `

`,
          "\r\r",
          `\r
\r
`
        ];
        for (; ; ) {
          const { done: i, value: u } = yield kt(r.read());
          if (i) {
            if (a.trim().length > 0) throw new Error("Incomplete JSON segment at the end");
            break;
          }
          const d = s.decode(u, { stream: !0 });
          try {
            const v = JSON.parse(d);
            if ("error" in v) {
              const y = JSON.parse(JSON.stringify(v.error)), S = y.status, x = y.code, I = `got status: ${S}. ${JSON.stringify(v)}`;
              if (x >= 400 && x < 600) throw new yP({
                message: I,
                status: x
              });
            }
          } catch (v) {
            if (v.name === "ApiError") throw v;
          }
          a += d;
          let f = -1, m = 0;
          for (; ; ) {
            f = -1, m = 0;
            for (const S of l) {
              const x = a.indexOf(S);
              x !== -1 && (f === -1 || x < f) && (f = x, m = S.length);
            }
            if (f === -1) break;
            const v = a.substring(0, f);
            a = a.substring(f + m);
            const y = v.trim();
            if (y.startsWith(o)) {
              const S = y.substring(5).trim();
              try {
                yield yield kt(new m0(new Response(S, {
                  headers: e?.headers,
                  status: e?.status,
                  statusText: e?.statusText
                })));
              } catch (x) {
                throw new Error(`exception parsing stream chunk ${S}. ${x}`);
              }
            }
          }
        }
      } finally {
        r.releaseLock();
      }
    });
  }
  async apiCall(e, t) {
    var n;
    if (!this.clientOptions.httpOptions || !this.clientOptions.httpOptions.retryOptions) return fetch(e, t);
    const r = this.clientOptions.httpOptions.retryOptions, s = async () => {
      const a = await fetch(e, t);
      if (a.ok) return a;
      throw cY.includes(a.status) ? new Error(`Retryable HTTP Error: ${a.statusText}`) : new fE.AbortError(`Non-retryable exception ${a.statusText} sending request`);
    };
    return (0, fE.default)(s, { retries: ((n = r.attempts) !== null && n !== void 0 ? n : uY) - 1 });
  }
  getDefaultHeaders() {
    const e = {}, t = aY + " " + this.clientOptions.userAgentExtra;
    return e[sY] = t, e[b0] = t, e[nY] = "application/json", e;
  }
  async getHeadersInternal(e, t) {
    const n = new Headers();
    if (e && e.headers) {
      for (const [r, s] of Object.entries(e.headers)) n.append(r, s);
      e.timeout && e.timeout > 0 && n.append(rY, String(Math.ceil(e.timeout / 1e3)));
    }
    return await this.clientOptions.auth.addAuthHeaders(n, t), n;
  }
  getFileName(e) {
    var t;
    let n = "";
    return typeof e == "string" && (n = e.replace(/[/\\]+$/, ""), n = (t = n.split(/[/\\]/).pop()) !== null && t !== void 0 ? t : ""), n;
  }
  async uploadFile(e, t) {
    var n;
    const r = {};
    t != null && (r.mimeType = t.mimeType, r.name = t.name, r.displayName = t.displayName), r.name && !r.name.startsWith("files/") && (r.name = `files/${r.name}`);
    const s = this.clientOptions.uploader, a = await s.stat(e);
    r.sizeBytes = String(a.size);
    const o = (n = t?.mimeType) !== null && n !== void 0 ? n : a.type;
    if (o === void 0 || o === "") throw new Error("Can not determine mimeType. Please provide mimeType in the config.");
    r.mimeType = o;
    const l = { file: r }, i = this.getFileName(e), u = Ze("upload/v1beta/files", l._url), d = await this.fetchUploadUrl(u, r.sizeBytes, r.mimeType, i, l, t?.httpOptions);
    return s.upload(e, d, this);
  }
  async uploadFileToFileSearchStore(e, t, n) {
    var r;
    const s = this.clientOptions.uploader, a = await s.stat(t), o = String(a.size), l = (r = n?.mimeType) !== null && r !== void 0 ? r : a.type;
    if (l === void 0 || l === "") throw new Error("Can not determine mimeType. Please provide mimeType in the config.");
    const i = `upload/v1beta/${e}:uploadToFileSearchStore`, u = this.getFileName(t), d = {};
    n != null && CP(n, d);
    const f = await this.fetchUploadUrl(i, o, l, u, d, n?.httpOptions);
    return s.uploadToFileSearchStore(t, f, this);
  }
  async downloadFile(e) {
    await this.clientOptions.downloader.download(e, this);
  }
  async fetchUploadUrl(e, t, n, r, s, a) {
    var o;
    let l = {};
    a ? l = a : l = {
      apiVersion: "",
      headers: Object.assign({
        "Content-Type": "application/json",
        "X-Goog-Upload-Protocol": "resumable",
        "X-Goog-Upload-Command": "start",
        "X-Goog-Upload-Header-Content-Length": `${t}`,
        "X-Goog-Upload-Header-Content-Type": `${n}`
      }, r ? { "X-Goog-Upload-File-Name": r } : {})
    };
    const i = await this.request({
      path: e,
      body: JSON.stringify(s),
      httpMethod: "POST",
      httpOptions: l
    });
    if (!i || !i?.headers) throw new Error("Server did not return an HttpResponse or the returned HttpResponse did not have headers.");
    const u = (o = i?.headers) === null || o === void 0 ? void 0 : o["x-goog-upload-url"];
    if (u === void 0) throw new Error("Failed to get upload url. Server did not return the x-google-upload-url in the headers");
    return u;
  }
};
async function UC(e) {
  var t;
  if (e === void 0) throw new Error("response is undefined");
  if (!e.ok) {
    const n = e.status;
    let r;
    !((t = e.headers.get("content-type")) === null || t === void 0) && t.includes("application/json") ? r = await e.json() : r = { error: {
      message: await e.text(),
      code: e.status,
      status: e.statusText
    } };
    const s = JSON.stringify(r);
    throw n >= 400 && n < 600 ? new yP({
      message: s,
      status: n
    }) : new Error(s);
  }
}
function fY(e, t) {
  if (!t || Object.keys(t).length === 0) return;
  if (e.body instanceof Blob) {
    console.warn("includeExtraBodyToRequestInit: extraBody provided but current request body is a Blob. extraBody will be ignored as merging is not supported for Blob bodies.");
    return;
  }
  let n = {};
  if (typeof e.body == "string" && e.body.length > 0) try {
    const a = JSON.parse(e.body);
    if (typeof a == "object" && a !== null && !Array.isArray(a)) n = a;
    else {
      console.warn("includeExtraBodyToRequestInit: Original request body is valid JSON but not a non-array object. Skip applying extraBody to the request body.");
      return;
    }
  } catch {
    console.warn("includeExtraBodyToRequestInit: Original request body is not valid JSON. Skip applying extraBody to the request body.");
    return;
  }
  function r(a, o) {
    const l = Object.assign({}, a);
    for (const i in o) if (Object.prototype.hasOwnProperty.call(o, i)) {
      const u = o[i], d = l[i];
      u && typeof u == "object" && !Array.isArray(u) && d && typeof d == "object" && !Array.isArray(d) ? l[i] = r(d, u) : (d && u && typeof d != typeof u && console.warn(`includeExtraBodyToRequestInit:deepMerge: Type mismatch for key "${i}". Original type: ${typeof d}, New type: ${typeof u}. Overwriting.`), l[i] = u);
    }
    return l;
  }
  const s = r(n, t);
  e.body = JSON.stringify(s);
}
var hY = "mcp_used/unknown", pY = !1;
function IP(e) {
  for (const t of e)
    if (mY(t) || typeof t == "object" && "inputSchema" in t) return !0;
  return pY;
}
function xP(e) {
  var t;
  e[b0] = (((t = e[b0]) !== null && t !== void 0 ? t : "") + ` ${hY}`).trimStart();
}
function mY(e) {
  return e !== null && typeof e == "object" && e instanceof vY;
}
function gY(e) {
  return aa(this, arguments, function* (n, r = 100) {
    let s, a = 0;
    for (; a < r; ) {
      const o = yield kt(n.listTools({ cursor: s }));
      for (const l of o.tools)
        yield yield kt(l), a++;
      if (!o.nextCursor) break;
      s = o.nextCursor;
    }
  });
}
var vY = class kP {
  constructor(t = [], n) {
    this.mcpTools = [], this.functionNameToMcpClient = {}, this.mcpClients = t, this.config = n;
  }
  static create(t, n) {
    return new kP(t, n);
  }
  async initialize() {
    var t, n, r, s;
    if (this.mcpTools.length > 0) return;
    const a = {}, o = [];
    for (const d of this.mcpClients) try {
      for (var l = !0, i = (n = void 0, oa(gY(d))), u; u = await i.next(), t = u.done, !t; l = !0) {
        s = u.value, l = !1;
        const f = s;
        o.push(f);
        const m = f.name;
        if (a[m]) throw new Error(`Duplicate function name ${m} found in MCP tools. Please ensure function names are unique.`);
        a[m] = d;
      }
    } catch (f) {
      n = { error: f };
    } finally {
      try {
        !l && !t && (r = i.return) && await r.call(i);
      } finally {
        if (n) throw n.error;
      }
    }
    this.mcpTools = o, this.functionNameToMcpClient = a;
  }
  async tool() {
    return await this.initialize(), PK(this.mcpTools, this.config);
  }
  async callTool(t) {
    await this.initialize();
    const n = [];
    for (const r of t) if (r.name in this.functionNameToMcpClient) {
      const s = this.functionNameToMcpClient[r.name];
      let a;
      this.config.timeout && (a = { timeout: this.config.timeout });
      const o = await s.callTool({
        name: r.name,
        arguments: r.args
      }, void 0, a);
      n.push({ functionResponse: {
        name: r.name,
        response: o.isError ? { error: o } : o
      } });
    }
    return n;
  }
};
async function yY(e, t, n) {
  const r = new SK();
  let s;
  n.data instanceof Blob ? s = JSON.parse(await n.data.text()) : s = JSON.parse(n.data), Object.assign(r, s), t(r);
}
var _Y = class {
  constructor(e, t, n) {
    this.apiClient = e, this.auth = t, this.webSocketFactory = n;
  }
  async connect(e) {
    var t, n;
    if (this.apiClient.isVertexAI()) throw new Error("Live music is not supported for Vertex AI.");
    console.warn("Live music generation is experimental and may change in future versions.");
    const r = this.apiClient.getWebsocketBaseUrl(), s = this.apiClient.getApiVersion(), a = SY(this.apiClient.getDefaultHeaders()), o = `${r}/ws/google.ai.generativelanguage.${s}.GenerativeService.BidiGenerateMusic?key=${this.apiClient.getApiKey()}`;
    let l = () => {
    };
    const i = new Promise((S) => {
      l = S;
    }), u = e.callbacks, d = function() {
      l({});
    }, f = this.apiClient, m = {
      onopen: d,
      onmessage: (S) => {
        yY(f, u.onmessage, S);
      },
      onerror: (t = u?.onerror) !== null && t !== void 0 ? t : function(S) {
      },
      onclose: (n = u?.onclose) !== null && n !== void 0 ? n : function(S) {
      }
    }, v = this.webSocketFactory.create(o, wY(a), m);
    v.connect(), await i;
    const y = { setup: { model: Zt(this.apiClient, e.model) } };
    return v.send(JSON.stringify(y)), new bY(v, this.apiClient);
  }
}, bY = class {
  constructor(e, t) {
    this.conn = e, this.apiClient = t;
  }
  async setWeightedPrompts(e) {
    if (!e.weightedPrompts || Object.keys(e.weightedPrompts).length === 0) throw new Error("Weighted prompts must be set and contain at least one entry.");
    const t = OG(e);
    this.conn.send(JSON.stringify({ clientContent: t }));
  }
  async setMusicGenerationConfig(e) {
    e.musicGenerationConfig || (e.musicGenerationConfig = {});
    const t = DG(e);
    this.conn.send(JSON.stringify(t));
  }
  sendPlaybackControl(e) {
    const t = { playbackControl: e };
    this.conn.send(JSON.stringify(t));
  }
  play() {
    this.sendPlaybackControl(wu.PLAY);
  }
  pause() {
    this.sendPlaybackControl(wu.PAUSE);
  }
  stop() {
    this.sendPlaybackControl(wu.STOP);
  }
  resetContext() {
    this.sendPlaybackControl(wu.RESET_CONTEXT);
  }
  close() {
    this.conn.close();
  }
};
function wY(e) {
  const t = {};
  return e.forEach((n, r) => {
    t[r] = n;
  }), t;
}
function SY(e) {
  const t = new Headers();
  for (const [n, r] of Object.entries(e)) t.append(n, r);
  return t;
}
var AY = "FunctionResponse request must have an `id` field from the response of a ToolCall.FunctionalCalls in Google AI.";
async function TY(e, t, n) {
  const r = new wK();
  let s;
  n.data instanceof Blob ? s = await n.data.text() : n.data instanceof ArrayBuffer ? s = new TextDecoder().decode(n.data) : s = n.data;
  const a = JSON.parse(s);
  if (e.isVertexAI()) {
    const o = UG(a);
    Object.assign(r, o);
  } else Object.assign(r, a);
  t(r);
}
var EY = class {
  constructor(e, t, n) {
    this.apiClient = e, this.auth = t, this.webSocketFactory = n, this.music = new _Y(this.apiClient, this.auth, this.webSocketFactory);
  }
  async connect(e) {
    var t, n, r, s, a, o;
    if (e.config && e.config.httpOptions) throw new Error("The Live module does not support httpOptions at request-level in LiveConnectConfig yet. Please use the client-level httpOptions configuration instead.");
    const l = this.apiClient.getWebsocketBaseUrl(), i = this.apiClient.getApiVersion();
    let u;
    const d = this.apiClient.getHeaders();
    e.config && e.config.tools && IP(e.config.tools) && xP(d);
    const f = kY(d);
    if (this.apiClient.isVertexAI()) {
      const b = this.apiClient.getProject(), C = this.apiClient.getLocation(), k = this.apiClient.getApiKey(), P = !!b && !!C || !!k;
      this.apiClient.getCustomBaseUrl() && !P ? u = l : (u = `${l}/ws/google.cloud.aiplatform.${i}.LlmBidiService/BidiGenerateContent`, await this.auth.addAuthHeaders(f, u));
    } else {
      const b = this.apiClient.getApiKey();
      let C = "BidiGenerateContent", k = "key";
      b?.startsWith("auth_tokens/") && (console.warn("Warning: Ephemeral token support is experimental and may change in future versions."), i !== "v1alpha" && console.warn("Warning: The SDK's ephemeral token support is in v1alpha only. Please use const ai = new GoogleGenAI({apiKey: token.name, httpOptions: { apiVersion: 'v1alpha' }}); before session connection."), C = "BidiGenerateContentConstrained", k = "access_token"), u = `${l}/ws/google.ai.generativelanguage.${i}.GenerativeService.${C}?${k}=${b}`;
    }
    let m = () => {
    };
    const v = new Promise((b) => {
      m = b;
    }), y = e.callbacks, S = function() {
      var b;
      (b = y?.onopen) === null || b === void 0 || b.call(y), m({});
    }, x = this.apiClient, I = {
      onopen: S,
      onmessage: (b) => {
        TY(x, y.onmessage, b);
      },
      onerror: (t = y?.onerror) !== null && t !== void 0 ? t : function(b) {
      },
      onclose: (n = y?.onclose) !== null && n !== void 0 ? n : function(b) {
      }
    }, E = this.webSocketFactory.create(u, xY(f), I);
    E.connect(), await v;
    let N = Zt(this.apiClient, e.model);
    if (this.apiClient.isVertexAI() && N.startsWith("publishers/")) {
      const b = this.apiClient.getProject(), C = this.apiClient.getLocation();
      b && C && (N = `projects/${b}/locations/${C}/` + N);
    }
    let $ = {};
    this.apiClient.isVertexAI() && ((r = e.config) === null || r === void 0 ? void 0 : r.responseModalities) === void 0 && (e.config === void 0 ? e.config = { responseModalities: [sg.AUDIO] } : e.config.responseModalities = [sg.AUDIO]), !((s = e.config) === null || s === void 0) && s.generationConfig && console.warn("Setting `LiveConnectConfig.generation_config` is deprecated, please set the fields on `LiveConnectConfig` directly. This will become an error in a future version (not before Q3 2025).");
    const p = (o = (a = e.config) === null || a === void 0 ? void 0 : a.tools) !== null && o !== void 0 ? o : [], w = [];
    for (const b of p) if (this.isCallableTool(b)) {
      const C = b;
      w.push(await C.tool());
    } else w.push(b);
    w.length > 0 && (e.config.tools = w);
    const _ = {
      model: N,
      config: e.config,
      callbacks: e.callbacks
    };
    return this.apiClient.isVertexAI() ? $ = LG(this.apiClient, _) : $ = $G(this.apiClient, _), delete $.config, E.send(JSON.stringify($)), new IY(E, this.apiClient);
  }
  isCallableTool(e) {
    return "callTool" in e && typeof e.callTool == "function";
  }
}, CY = { turnComplete: !0 }, IY = class {
  constructor(e, t) {
    this.conn = e, this.apiClient = t;
  }
  tLiveClientContent(e, t) {
    if (t.turns !== null && t.turns !== void 0) {
      let n = [];
      try {
        n = Yr(t.turns), e.isVertexAI() || (n = n.map((r) => Vf(r)));
      } catch {
        throw new Error(`Failed to parse client content "turns", type: '${typeof t.turns}'`);
      }
      return { clientContent: {
        turns: n,
        turnComplete: t.turnComplete
      } };
    }
    return { clientContent: { turnComplete: t.turnComplete } };
  }
  tLiveClienttToolResponse(e, t) {
    let n = [];
    if (t.functionResponses == null) throw new Error("functionResponses is required.");
    if (Array.isArray(t.functionResponses) ? n = t.functionResponses : n = [t.functionResponses], n.length === 0) throw new Error("functionResponses is required.");
    for (const r of n) {
      if (typeof r != "object" || r === null || !("name" in r) || !("response" in r)) throw new Error(`Could not parse function response, type '${typeof r}'.`);
      if (!e.isVertexAI() && !("id" in r)) throw new Error(AY);
    }
    return { toolResponse: { functionResponses: n } };
  }
  sendClientContent(e) {
    e = Object.assign(Object.assign({}, CY), e);
    const t = this.tLiveClientContent(this.apiClient, e);
    this.conn.send(JSON.stringify(t));
  }
  sendRealtimeInput(e) {
    let t = {};
    this.apiClient.isVertexAI() ? t = { realtimeInput: BG(e) } : t = { realtimeInput: FG(e) }, this.conn.send(JSON.stringify(t));
  }
  sendToolResponse(e) {
    if (e.functionResponses == null) throw new Error("Tool response parameters are required.");
    const t = this.tLiveClienttToolResponse(this.apiClient, e);
    this.conn.send(JSON.stringify(t));
  }
  close() {
    this.conn.close();
  }
};
function xY(e) {
  const t = {};
  return e.forEach((n, r) => {
    t[r] = n;
  }), t;
}
function kY(e) {
  const t = new Headers();
  for (const [n, r] of Object.entries(e)) t.append(n, r);
  return t;
}
var qC = 10;
function HC(e) {
  var t, n, r;
  if (!((t = e?.automaticFunctionCalling) === null || t === void 0) && t.disable) return !0;
  let s = !1;
  for (const o of (n = e?.tools) !== null && n !== void 0 ? n : []) if (Ou(o)) {
    s = !0;
    break;
  }
  if (!s) return !0;
  const a = (r = e?.automaticFunctionCalling) === null || r === void 0 ? void 0 : r.maximumRemoteCalls;
  return a && (a < 0 || !Number.isInteger(a)) || a == 0 ? (console.warn("Invalid maximumRemoteCalls value provided for automatic function calling. Disabled automatic function calling. Please provide a valid integer value greater than 0. maximumRemoteCalls provided:", a), !0) : !1;
}
function Ou(e) {
  return "callTool" in e && typeof e.callTool == "function";
}
function MY(e) {
  var t, n, r;
  return (r = (n = (t = e.config) === null || t === void 0 ? void 0 : t.tools) === null || n === void 0 ? void 0 : n.some((s) => Ou(s))) !== null && r !== void 0 ? r : !1;
}
function zC(e) {
  var t;
  const n = [];
  return !((t = e?.config) === null || t === void 0) && t.tools && e.config.tools.forEach((r, s) => {
    if (Ou(r)) return;
    const a = r;
    a.functionDeclarations && a.functionDeclarations.length > 0 && n.push(s);
  }), n;
}
function KC(e) {
  var t;
  return !(!((t = e?.automaticFunctionCalling) === null || t === void 0) && t.ignoreCallHistory);
}
var PY = class extends Po {
  constructor(e) {
    super(), this.apiClient = e, this.embedContent = async (t) => {
      if (!this.apiClient.isVertexAI())
        return t.model.includes("gemini-embedding-2") && (t.contents = Yr(t.contents)), await this.embedContentInternal(t);
      if (t.model.includes("gemini") && t.model !== "gemini-embedding-001" || t.model.includes("maas")) {
        const n = Yr(t.contents);
        if (n.length > 1) throw new Error("The embedContent API for this model only supports one content at a time.");
        const r = Object.assign(Object.assign({}, t), {
          content: n[0],
          embeddingApiType: ag.EMBED_CONTENT
        });
        return await this.embedContentInternal(r);
      } else {
        const n = Object.assign(Object.assign({}, t), { embeddingApiType: ag.PREDICT });
        return await this.embedContentInternal(n);
      }
    }, this.generateContent = async (t) => {
      var n, r, s, a, o;
      const l = await this.processParamsMaybeAddMcpUsage(t);
      if (this.maybeMoveToResponseJsonSchem(t), !MY(t) || HC(t.config)) return await this.generateContentInternal(l);
      const i = zC(t);
      if (i.length > 0) {
        const y = i.map((S) => `tools[${S}]`).join(", ");
        throw new Error(`Automatic function calling with CallableTools (or MCP objects) and basic FunctionDeclarations is not yet supported. Incompatible tools found at ${y}.`);
      }
      let u, d;
      const f = Yr(l.contents), m = (s = (r = (n = l.config) === null || n === void 0 ? void 0 : n.automaticFunctionCalling) === null || r === void 0 ? void 0 : r.maximumRemoteCalls) !== null && s !== void 0 ? s : qC;
      let v = 0;
      for (; v < m && (u = await this.generateContentInternal(l), !(!u.functionCalls || u.functionCalls.length === 0)); ) {
        const y = u.candidates[0].content, S = [];
        for (const x of (o = (a = t.config) === null || a === void 0 ? void 0 : a.tools) !== null && o !== void 0 ? o : []) if (Ou(x)) {
          const I = await x.callTool(u.functionCalls);
          S.push(...I);
        }
        v++, d = {
          role: "user",
          parts: S
        }, l.contents = Yr(l.contents), l.contents.push(y), l.contents.push(d), KC(l.config) && (f.push(y), f.push(d));
      }
      return KC(l.config) && (u.automaticFunctionCallingHistory = f), u;
    }, this.generateContentStream = async (t) => {
      var n, r, s, a, o;
      if (this.maybeMoveToResponseJsonSchem(t), HC(t.config)) {
        const d = await this.processParamsMaybeAddMcpUsage(t);
        return await this.generateContentStreamInternal(d);
      }
      const l = zC(t);
      if (l.length > 0) {
        const d = l.map((f) => `tools[${f}]`).join(", ");
        throw new Error(`Incompatible tools found at ${d}. Automatic function calling with CallableTools (or MCP objects) and basic FunctionDeclarations" is not yet supported.`);
      }
      const i = (s = (r = (n = t?.config) === null || n === void 0 ? void 0 : n.toolConfig) === null || r === void 0 ? void 0 : r.functionCallingConfig) === null || s === void 0 ? void 0 : s.streamFunctionCallArguments, u = (o = (a = t?.config) === null || a === void 0 ? void 0 : a.automaticFunctionCalling) === null || o === void 0 ? void 0 : o.disable;
      if (i && !u) throw new Error("Running in streaming mode with 'streamFunctionCallArguments' enabled, this feature is not compatible with automatic function calling (AFC). Please set 'config.automaticFunctionCalling.disable' to true to disable AFC or leave 'config.toolConfig.functionCallingConfig.streamFunctionCallArguments' to be undefined or set to false to disable streaming function call arguments feature.");
      return await this.processAfcStream(t);
    }, this.generateImages = async (t) => await this.generateImagesInternal(t).then((n) => {
      var r;
      let s;
      const a = [];
      if (n?.generatedImages) for (const l of n.generatedImages) l && l?.safetyAttributes && ((r = l?.safetyAttributes) === null || r === void 0 ? void 0 : r.contentType) === "Positive Prompt" ? s = l?.safetyAttributes : a.push(l);
      let o;
      return s ? o = {
        generatedImages: a,
        positivePromptSafetyAttributes: s,
        sdkHttpResponse: n.sdkHttpResponse
      } : o = {
        generatedImages: a,
        sdkHttpResponse: n.sdkHttpResponse
      }, o;
    }), this.list = async (t) => {
      var n;
      const r = { config: Object.assign(Object.assign({}, { queryBase: !0 }), t?.config) };
      if (this.apiClient.isVertexAI() && !r.config.queryBase) {
        if (!((n = r.config) === null || n === void 0) && n.filter) throw new Error("Filtering tuned models list for Vertex AI is not currently supported");
        r.config.filter = "labels.tune-type:*";
      }
      return new Fl(ko.PAGED_ITEM_MODELS, (s) => this.listInternal(s), await this.listInternal(r), r);
    }, this.editImage = async (t) => {
      const n = {
        model: t.model,
        prompt: t.prompt,
        referenceImages: [],
        config: t.config
      };
      return t.referenceImages && t.referenceImages && (n.referenceImages = t.referenceImages.map((r) => r.toReferenceImageAPI())), await this.editImageInternal(n);
    }, this.upscaleImage = async (t) => {
      let n = {
        numberOfImages: 1,
        mode: "upscale"
      };
      t.config && (n = Object.assign(Object.assign({}, n), t.config));
      const r = {
        model: t.model,
        image: t.image,
        upscaleFactor: t.upscaleFactor,
        config: n
      };
      return await this.upscaleImageInternal(r);
    }, this.generateVideos = async (t) => {
      var n, r, s, a, o, l;
      if ((t.prompt || t.image || t.video) && t.source) throw new Error("Source and prompt/image/video are mutually exclusive. Please only use source.");
      return this.apiClient.isVertexAI() || (!((n = t.video) === null || n === void 0) && n.uri && (!((r = t.video) === null || r === void 0) && r.videoBytes) ? t.video = {
        uri: t.video.uri,
        mimeType: t.video.mimeType
      } : !((a = (s = t.source) === null || s === void 0 ? void 0 : s.video) === null || a === void 0) && a.uri && (!((l = (o = t.source) === null || o === void 0 ? void 0 : o.video) === null || l === void 0) && l.videoBytes) && (t.source.video = {
        uri: t.source.video.uri,
        mimeType: t.source.video.mimeType
      })), await this.generateVideosInternal(t);
    };
  }
  maybeMoveToResponseJsonSchem(e) {
    e.config && e.config.responseSchema && (e.config.responseJsonSchema || Object.keys(e.config.responseSchema).includes("$schema") && (e.config.responseJsonSchema = e.config.responseSchema, delete e.config.responseSchema));
  }
  async processParamsMaybeAddMcpUsage(e) {
    var t, n, r;
    const s = (t = e.config) === null || t === void 0 ? void 0 : t.tools;
    if (!s) return e;
    const a = await Promise.all(s.map(async (l) => Ou(l) ? await l.tool() : l)), o = {
      model: e.model,
      contents: e.contents,
      config: Object.assign(Object.assign({}, e.config), { tools: a })
    };
    if (o.config.tools = a, e.config && e.config.tools && IP(e.config.tools)) {
      const l = (r = (n = e.config.httpOptions) === null || n === void 0 ? void 0 : n.headers) !== null && r !== void 0 ? r : {};
      let i = Object.assign({}, l);
      Object.keys(i).length === 0 && (i = this.apiClient.getDefaultHeaders()), xP(i), o.config.httpOptions = Object.assign(Object.assign({}, e.config.httpOptions), { headers: i });
    }
    return o;
  }
  async initAfcToolsMap(e) {
    var t, n, r;
    const s = /* @__PURE__ */ new Map();
    for (const a of (n = (t = e.config) === null || t === void 0 ? void 0 : t.tools) !== null && n !== void 0 ? n : []) if (Ou(a)) {
      const o = a, l = await o.tool();
      for (const i of (r = l.functionDeclarations) !== null && r !== void 0 ? r : []) {
        if (!i.name) throw new Error("Function declaration name is required.");
        if (s.has(i.name)) throw new Error(`Duplicate tool declaration name: ${i.name}`);
        s.set(i.name, o);
      }
    }
    return s;
  }
  async processAfcStream(e) {
    var t, n, r;
    const s = (r = (n = (t = e.config) === null || t === void 0 ? void 0 : t.automaticFunctionCalling) === null || n === void 0 ? void 0 : n.maximumRemoteCalls) !== null && r !== void 0 ? r : qC;
    let a = !1, o = 0;
    const l = await this.initAfcToolsMap(e);
    return (function(i, u, d) {
      return aa(this, arguments, function* () {
        for (var f, m, v, y, S, x; o < s; ) {
          a && (o++, a = !1);
          const $ = yield kt(i.processParamsMaybeAddMcpUsage(d)), p = yield kt(i.generateContentStreamInternal($)), w = [], _ = [];
          try {
            for (var I = !0, E = (m = void 0, oa(p)), N; N = yield kt(E.next()), f = N.done, !f; I = !0) {
              y = N.value, I = !1;
              const b = y;
              if (yield yield kt(b), b.candidates && (!((S = b.candidates[0]) === null || S === void 0) && S.content)) {
                _.push(b.candidates[0].content);
                for (const C of (x = b.candidates[0].content.parts) !== null && x !== void 0 ? x : []) if (o < s && C.functionCall) {
                  if (!C.functionCall.name) throw new Error("Function call name was not returned by the model.");
                  if (u.has(C.functionCall.name)) {
                    const k = yield kt(u.get(C.functionCall.name).callTool([C.functionCall]));
                    w.push(...k);
                  } else
                    throw new Error(`Automatic function calling was requested, but not all the tools the model used implement the CallableTool interface. Available tools: ${u.keys()}, mising tool: ${C.functionCall.name}`);
                }
              }
            }
          } catch (b) {
            m = { error: b };
          } finally {
            try {
              !I && !f && (v = E.return) && (yield kt(v.call(E)));
            } finally {
              if (m) throw m.error;
            }
          }
          if (w.length > 0) {
            a = !0;
            const b = new nd();
            b.candidates = [{ content: {
              role: "user",
              parts: w
            } }], yield yield kt(b);
            const C = [];
            C.push(..._), C.push({
              role: "user",
              parts: w
            }), d.contents = Yr(d.contents).concat(C);
          } else break;
        }
      });
    })(this, l, e);
  }
  async generateContentInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = OC(this.apiClient, e);
      return o = Ze("{model}:generateContent", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = BC(u), f = new nd();
        return Object.assign(f, d), f;
      });
    } else {
      const i = DC(this.apiClient, e);
      return o = Ze("{model}:generateContent", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = FC(u), f = new nd();
        return Object.assign(f, d), f;
      });
    }
  }
  async generateContentStreamInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = OC(this.apiClient, e);
      return o = Ze("{model}:streamGenerateContent?alt=sse", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.requestStream({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }), a.then(function(u) {
        return aa(this, arguments, function* () {
          var d, f, m, v;
          try {
            for (var y = !0, S = oa(u), x; x = yield kt(S.next()), d = x.done, !d; y = !0) {
              v = x.value, y = !1;
              const I = v, E = BC(yield kt(I.json()), e);
              E.sdkHttpResponse = { headers: I.headers };
              const N = new nd();
              Object.assign(N, E), yield yield kt(N);
            }
          } catch (I) {
            f = { error: I };
          } finally {
            try {
              !y && !d && (m = S.return) && (yield kt(m.call(S)));
            } finally {
              if (f) throw f.error;
            }
          }
        });
      });
    } else {
      const i = DC(this.apiClient, e);
      return o = Ze("{model}:streamGenerateContent?alt=sse", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.requestStream({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }), a.then(function(u) {
        return aa(this, arguments, function* () {
          var d, f, m, v;
          try {
            for (var y = !0, S = oa(u), x; x = yield kt(S.next()), d = x.done, !d; y = !0) {
              v = x.value, y = !1;
              const I = v, E = FC(yield kt(I.json()), e);
              E.sdkHttpResponse = { headers: I.headers };
              const N = new nd();
              Object.assign(N, E), yield yield kt(N);
            }
          } catch (I) {
            f = { error: I };
          } finally {
            try {
              !y && !d && (m = S.return) && (yield kt(m.call(S)));
            } finally {
              if (f) throw f.error;
            }
          }
        });
      });
    }
  }
  async embedContentInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = bV(this.apiClient, e, e);
      return o = Ze(NK(e.model) ? "{model}:embedContent" : "{model}:predict", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = SV(u, e), f = new gC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = _V(this.apiClient, e);
      return o = Ze("{model}:batchEmbedContents", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = wV(u), f = new gC();
        return Object.assign(f, d), f;
      });
    }
  }
  async generateImagesInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = NV(this.apiClient, e);
      return o = Ze("{model}:predict", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = LV(u), f = new vC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = RV(this.apiClient, e);
      return o = Ze("{model}:predict", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = $V(u), f = new vC();
        return Object.assign(f, d), f;
      });
    }
  }
  async editImageInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const o = mV(this.apiClient, e);
      return s = Ze("{model}:predict", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json().then((i) => {
        const u = i;
        return u.sdkHttpResponse = { headers: l.headers }, u;
      })), r.then((l) => {
        const i = gV(l), u = new lK();
        return Object.assign(u, i), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async upscaleImageInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const o = LJ(this.apiClient, e);
      return s = Ze("{model}:predict", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json().then((i) => {
        const u = i;
        return u.sdkHttpResponse = { headers: l.headers }, u;
      })), r.then((l) => {
        const i = DJ(l), u = new uK();
        return Object.assign(u, i), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async recontextImage(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const o = mJ(this.apiClient, e);
      return s = Ze("{model}:predict", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => {
        const i = gJ(l), u = new cK();
        return Object.assign(u, i), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async segmentImage(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const o = SJ(this.apiClient, e);
      return s = Ze("{model}:predict", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => {
        const i = AJ(l), u = new dK();
        return Object.assign(u, i), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async get(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = QV(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a.then((u) => _0(u));
    } else {
      const i = jV(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a.then((u) => y0(u));
    }
  }
  async listInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = iJ(this.apiClient, e);
      return o = Ze("{models_url}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = uJ(u), f = new yC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = oJ(this.apiClient, e);
      return o = Ze("{models_url}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = lJ(u), f = new yC();
        return Object.assign(f, d), f;
      });
    }
  }
  async update(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = NJ(this.apiClient, e);
      return o = Ze("{model}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "PATCH",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a.then((u) => _0(u));
    } else {
      const i = RJ(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "PATCH",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a.then((u) => y0(u));
    }
  }
  async delete(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = dV(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = hV(u), f = new _C();
        return Object.assign(f, d), f;
      });
    } else {
      const i = cV(this.apiClient, e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = fV(u), f = new _C();
        return Object.assign(f, d), f;
      });
    }
  }
  async countTokens(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = iV(this.apiClient, e);
      return o = Ze("{model}:countTokens", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = uV(u), f = new bC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = oV(this.apiClient, e);
      return o = Ze("{model}:countTokens", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = lV(u), f = new bC();
        return Object.assign(f, d), f;
      });
    }
  }
  async computeTokens(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const o = ZG(this.apiClient, e);
      return s = Ze("{model}:computeTokens", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json().then((i) => {
        const u = i;
        return u.sdkHttpResponse = { headers: l.headers }, u;
      })), r.then((l) => {
        const i = eV(l), u = new fK();
        return Object.assign(u, i), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async generateVideosInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = qV(this.apiClient, e);
      return o = Ze("{model}:predictLongRunning", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a.then((u) => {
        const d = BV(u), f = new wC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = UV(this.apiClient, e);
      return o = Ze("{model}:predictLongRunning", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a.then((u) => {
        const d = FV(u), f = new wC();
        return Object.assign(f, d), f;
      });
    }
  }
}, RY = class extends Po {
  constructor(e) {
    super(), this.apiClient = e;
  }
  async getVideosOperation(e) {
    const t = e.operation, n = e.config;
    if (t.name === void 0 || t.name === "") throw new Error("Operation name is required.");
    if (this.apiClient.isVertexAI()) {
      const r = t.name.split("/operations/")[0];
      let s;
      n && "httpOptions" in n && (s = n.httpOptions);
      const a = await this.fetchPredictVideosOperationInternal({
        operationName: t.name,
        resourceName: r,
        config: { httpOptions: s }
      });
      return t._fromAPIResponse({
        apiResponse: a,
        _isVertexAI: !0
      });
    } else {
      const r = await this.getVideosOperationInternal({
        operationName: t.name,
        config: n
      });
      return t._fromAPIResponse({
        apiResponse: r,
        _isVertexAI: !1
      });
    }
  }
  async get(e) {
    const t = e.operation, n = e.config;
    if (t.name === void 0 || t.name === "") throw new Error("Operation name is required.");
    if (this.apiClient.isVertexAI()) {
      const r = t.name.split("/operations/")[0];
      let s;
      n && "httpOptions" in n && (s = n.httpOptions);
      const a = await this.fetchPredictVideosOperationInternal({
        operationName: t.name,
        resourceName: r,
        config: { httpOptions: s }
      });
      return t._fromAPIResponse({
        apiResponse: a,
        _isVertexAI: !0
      });
    } else {
      const r = await this.getVideosOperationInternal({
        operationName: t.name,
        config: n
      });
      return t._fromAPIResponse({
        apiResponse: r,
        _isVertexAI: !1
      });
    }
  }
  async getVideosOperationInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = nK(e);
      return o = Ze("{operationName}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), a;
    } else {
      const i = tK(e);
      return o = Ze("{operationName}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), a;
    }
  }
  async fetchPredictVideosOperationInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const o = Jz(e);
      return s = Ze("{resourceName}:fetchPredictOperation", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r;
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
};
function WC(e) {
  const t = {};
  if (c(e, ["languageCodes"]) !== void 0) throw new Error("languageCodes parameter is not supported in Gemini API.");
  return t;
}
function NY(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && h(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function $Y(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && h(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function LY(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((a) => KY(a))), h(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && h(t, ["role"], r), t;
}
function DY(e, t, n) {
  const r = {}, s = c(t, ["expireTime"]);
  n !== void 0 && s != null && h(n, ["expireTime"], s);
  const a = c(t, ["newSessionExpireTime"]);
  n !== void 0 && a != null && h(n, ["newSessionExpireTime"], a);
  const o = c(t, ["uses"]);
  n !== void 0 && o != null && h(n, ["uses"], o);
  const l = c(t, ["liveConnectConstraints"]);
  n !== void 0 && l != null && h(n, ["bidiGenerateContentSetup"], zY(e, l));
  const i = c(t, ["lockAdditionalFields"]);
  return n !== void 0 && i != null && h(n, ["fieldMask"], i), r;
}
function OY(e, t) {
  const n = {}, r = c(t, ["config"]);
  return r != null && h(n, ["config"], DY(e, r, n)), n;
}
function FY(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && h(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && h(t, ["mimeType"], r), t;
}
function BY(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && h(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && h(t, ["args"], r);
  const s = c(e, ["name"]);
  if (s != null && h(t, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function UY(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && h(t, ["authConfig"], NY(n));
  const r = c(e, ["enableWidget"]);
  return r != null && h(t, ["enableWidget"], r), t;
}
function qY(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && h(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && h(t, ["timeRangeFilter"], r), t;
}
function HY(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && h(t, ["setup", "generationConfig"], r);
  const s = c(e, ["responseModalities"]);
  t !== void 0 && s != null && h(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], s);
  const a = c(e, ["temperature"]);
  t !== void 0 && a != null && h(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], a);
  const o = c(e, ["topP"]);
  t !== void 0 && o != null && h(t, [
    "setup",
    "generationConfig",
    "topP"
  ], o);
  const l = c(e, ["topK"]);
  t !== void 0 && l != null && h(t, [
    "setup",
    "generationConfig",
    "topK"
  ], l);
  const i = c(e, ["maxOutputTokens"]);
  t !== void 0 && i != null && h(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], i);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && h(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && h(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const f = c(e, ["speechConfig"]);
  t !== void 0 && f != null && h(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], Yw(f));
  const m = c(e, ["thinkingConfig"]);
  t !== void 0 && m != null && h(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], m);
  const v = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && v != null && h(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], v);
  const y = c(e, ["systemInstruction"]);
  t !== void 0 && y != null && h(t, ["setup", "systemInstruction"], LY(cr(y)));
  const S = c(e, ["tools"]);
  if (t !== void 0 && S != null) {
    let b = fc(S);
    Array.isArray(b) && (b = b.map((C) => VY(dc(C)))), h(t, ["setup", "tools"], b);
  }
  const x = c(e, ["sessionResumption"]);
  t !== void 0 && x != null && h(t, ["setup", "sessionResumption"], GY(x));
  const I = c(e, ["inputAudioTranscription"]);
  t !== void 0 && I != null && h(t, ["setup", "inputAudioTranscription"], WC(I));
  const E = c(e, ["outputAudioTranscription"]);
  t !== void 0 && E != null && h(t, ["setup", "outputAudioTranscription"], WC(E));
  const N = c(e, ["realtimeInputConfig"]);
  t !== void 0 && N != null && h(t, ["setup", "realtimeInputConfig"], N);
  const $ = c(e, ["contextWindowCompression"]);
  t !== void 0 && $ != null && h(t, ["setup", "contextWindowCompression"], $);
  const p = c(e, ["proactivity"]);
  if (t !== void 0 && p != null && h(t, ["setup", "proactivity"], p), c(e, ["explicitVadSignal"]) !== void 0) throw new Error("explicitVadSignal parameter is not supported in Gemini API.");
  const w = c(e, ["avatarConfig"]);
  t !== void 0 && w != null && h(t, ["setup", "avatarConfig"], w);
  const _ = c(e, ["safetySettings"]);
  if (t !== void 0 && _ != null) {
    let b = _;
    Array.isArray(b) && (b = b.map((C) => WY(C))), h(t, ["setup", "safetySettings"], b);
  }
  return n;
}
function zY(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && h(n, ["setup", "model"], Zt(e, r));
  const s = c(t, ["config"]);
  return s != null && h(n, ["config"], HY(s, n)), n;
}
function KY(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && h(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && h(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && h(t, ["executableCode"], s);
  const a = c(e, ["fileData"]);
  a != null && h(t, ["fileData"], FY(a));
  const o = c(e, ["functionCall"]);
  o != null && h(t, ["functionCall"], BY(o));
  const l = c(e, ["functionResponse"]);
  l != null && h(t, ["functionResponse"], l);
  const i = c(e, ["inlineData"]);
  i != null && h(t, ["inlineData"], $Y(i));
  const u = c(e, ["text"]);
  u != null && h(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && h(t, ["thought"], d);
  const f = c(e, ["thoughtSignature"]);
  f != null && h(t, ["thoughtSignature"], f);
  const m = c(e, ["videoMetadata"]);
  m != null && h(t, ["videoMetadata"], m);
  const v = c(e, ["toolCall"]);
  v != null && h(t, ["toolCall"], v);
  const y = c(e, ["toolResponse"]);
  y != null && h(t, ["toolResponse"], y);
  const S = c(e, ["partMetadata"]);
  return S != null && h(t, ["partMetadata"], S), t;
}
function WY(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && h(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && h(t, ["threshold"], r), t;
}
function GY(e) {
  const t = {}, n = c(e, ["handle"]);
  if (n != null && h(t, ["handle"], n), c(e, ["transparent"]) !== void 0) throw new Error("transparent parameter is not supported in Gemini API.");
  return t;
}
function VY(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && h(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && h(t, ["fileSearch"], r);
  const s = c(e, ["googleSearch"]);
  s != null && h(t, ["googleSearch"], qY(s));
  const a = c(e, ["googleMaps"]);
  a != null && h(t, ["googleMaps"], UY(a));
  const o = c(e, ["codeExecution"]);
  if (o != null && h(t, ["codeExecution"], o), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let f = l;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["functionDeclarations"], f);
  }
  const i = c(e, ["googleSearchRetrieval"]);
  if (i != null && h(t, ["googleSearchRetrieval"], i), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && h(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let f = d;
    Array.isArray(f) && (f = f.map((m) => m)), h(t, ["mcpServers"], f);
  }
  return t;
}
function JY(e) {
  const t = [];
  for (const n in e) if (Object.prototype.hasOwnProperty.call(e, n)) {
    const r = e[n];
    if (typeof r == "object" && r != null && Object.keys(r).length > 0) {
      const s = Object.keys(r).map((a) => `${n}.${a}`);
      t.push(...s);
    } else t.push(n);
  }
  return t.join(",");
}
function YY(e, t) {
  let n = null;
  const r = e.bidiGenerateContentSetup;
  if (typeof r == "object" && r !== null && "setup" in r) {
    const a = r.setup;
    typeof a == "object" && a !== null ? (e.bidiGenerateContentSetup = a, n = a) : delete e.bidiGenerateContentSetup;
  } else r !== void 0 && delete e.bidiGenerateContentSetup;
  const s = e.fieldMask;
  if (n) {
    const a = JY(n);
    if (Array.isArray(t?.lockAdditionalFields) && t?.lockAdditionalFields.length === 0) a ? e.fieldMask = a : delete e.fieldMask;
    else if (t?.lockAdditionalFields && t.lockAdditionalFields.length > 0 && s !== null && Array.isArray(s) && s.length > 0) {
      const o = [
        "temperature",
        "topK",
        "topP",
        "maxOutputTokens",
        "responseModalities",
        "seed",
        "speechConfig"
      ];
      let l = [];
      s.length > 0 && (l = s.map((u) => o.includes(u) ? `generationConfig.${u}` : u));
      const i = [];
      a && i.push(a), l.length > 0 && i.push(...l), i.length > 0 ? e.fieldMask = i.join(",") : delete e.fieldMask;
    } else delete e.fieldMask;
  } else s !== null && Array.isArray(s) && s.length > 0 ? e.fieldMask = s.join(",") : delete e.fieldMask;
  return e;
}
var XY = class extends Po {
  constructor(e) {
    super(), this.apiClient = e;
  }
  async create(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("The client.tokens.create method is only supported by the Gemini Developer API.");
    {
      const o = OY(this.apiClient, e);
      s = Ze("auth_tokens", o._url), a = o._query, delete o.config, delete o._url, delete o._query;
      const l = YY(o, e.config);
      return r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((i) => i.json()), r.then((i) => i);
    }
  }
};
function jY(e, t) {
  const n = {}, r = c(e, ["force"]);
  return t !== void 0 && r != null && h(t, ["_query", "force"], r), n;
}
function QY(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && h(t, ["_url", "name"], n);
  const r = c(e, ["config"]);
  return r != null && jY(r, t), t;
}
function ZY(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && h(t, ["_url", "name"], n), t;
}
function eX(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && h(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && h(t, ["_query", "pageToken"], s), n;
}
function tX(e) {
  const t = {}, n = c(e, ["parent"]);
  n != null && h(t, ["_url", "parent"], n);
  const r = c(e, ["config"]);
  return r != null && eX(r, t), t;
}
function nX(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && h(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && h(t, ["nextPageToken"], r);
  const s = c(e, ["documents"]);
  if (s != null) {
    let a = s;
    Array.isArray(a) && (a = a.map((o) => o)), h(t, ["documents"], a);
  }
  return t;
}
var rX = class extends Po {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t) => new Fl(ko.PAGED_ITEM_DOCUMENTS, (n) => this.listInternal({
      parent: t.parent,
      config: n.config
    }), await this.listInternal(t), t);
  }
  async get(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = ZY(e);
      return s = Ze("{name}", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => l);
    }
  }
  async delete(e) {
    var t, n;
    let r = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = QY(e);
      r = Ze("{name}", a._url), s = a._query, delete a._url, delete a._query, await this.apiClient.request({
        path: r,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = tX(e);
      return s = Ze("{parent}/documents", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => {
        const i = nX(l), u = new hK();
        return Object.assign(u, i), u;
      });
    }
  }
}, sX = class extends Po {
  constructor(e, t = new rX(e)) {
    super(), this.apiClient = e, this.documents = t, this.list = async (n = {}) => new Fl(ko.PAGED_ITEM_FILE_SEARCH_STORES, (r) => this.listInternal(r), await this.listInternal(n), n);
  }
  async uploadToFileSearchStore(e) {
    if (this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support uploading files to a file search store.");
    return this.apiClient.uploadFileToFileSearchStore(e.fileSearchStoreName, e.file, e.config);
  }
  async create(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = zJ(e);
      return s = Ze("fileSearchStores", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => l);
    }
  }
  async get(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = GJ(e);
      return s = Ze("{name}", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => l);
    }
  }
  async delete(e) {
    var t, n;
    let r = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const a = WJ(e);
      r = Ze("{name}", a._url), s = a._query, delete a._url, delete a._query, await this.apiClient.request({
        path: r,
        queryParams: s,
        body: JSON.stringify(a),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = QJ(e);
      return s = Ze("fileSearchStores", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => {
        const i = ZJ(l), u = new pK();
        return Object.assign(u, i), u;
      });
    }
  }
  async uploadToFileSearchStoreInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = eY(e);
      return s = Ze("upload/v1beta/{file_search_store_name}:uploadToFileSearchStore", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => {
        const i = tY(l), u = new mK();
        return Object.assign(u, i), u;
      });
    }
  }
  async importFile(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = YJ(e);
      return s = Ze("{file_search_store_name}:importFile", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => {
        const i = JJ(l), u = new gK();
        return Object.assign(u, i), u;
      });
    }
  }
}, MP = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return MP = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
}, aX = () => MP();
function w0(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var S0 = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, Hs = class extends Error {
}, Ws = class A0 extends Hs {
  constructor(t, n, r, s) {
    super(`${A0.makeMessage(t, n, r)}`), this.status = t, this.headers = s, this.error = n;
  }
  static makeMessage(t, n, r) {
    const s = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && s ? `${t} ${s}` : t ? `${t} status code (no body)` : s || "(no status code or body)";
  }
  static generate(t, n, r, s) {
    if (!t || !s) return new fv({
      message: r,
      cause: S0(n)
    });
    const a = n;
    return t === 400 ? new RP(t, a, r, s) : t === 401 ? new NP(t, a, r, s) : t === 403 ? new $P(t, a, r, s) : t === 404 ? new LP(t, a, r, s) : t === 409 ? new DP(t, a, r, s) : t === 422 ? new OP(t, a, r, s) : t === 429 ? new FP(t, a, r, s) : t >= 500 ? new BP(t, a, r, s) : new A0(t, a, r, s);
  }
}, T0 = class extends Ws {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, fv = class extends Ws {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, PP = class extends fv {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, RP = class extends Ws {
}, NP = class extends Ws {
}, $P = class extends Ws {
}, LP = class extends Ws {
}, DP = class extends Ws {
}, OP = class extends Ws {
}, FP = class extends Ws {
}, BP = class extends Ws {
}, oX = /^[a-z][a-z0-9+.-]*:/i, iX = (e) => oX.test(e), E0 = (e) => (E0 = Array.isArray, E0(e)), GC = E0;
function VC(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function lX(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
var uX = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new Hs(`${e} must be an integer`);
  if (t < 0) throw new Hs(`${e} must be a positive integer`);
  return t;
}, cX = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, dX = (e) => new Promise((t) => setTimeout(t, e));
function fX() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new GeminiNextGenAPIClient({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function UP(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function hX(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return UP({
    start() {
    },
    async pull(n) {
      const { done: r, value: s } = await t.next();
      r ? n.close() : n.enqueue(s);
    },
    async cancel() {
      var n;
      await ((n = t.return) === null || n === void 0 ? void 0 : n.call(t));
    }
  });
}
function qP(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function pX(e) {
  var t, n;
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await ((n = (t = e[Symbol.asyncIterator]()).return) === null || n === void 0 ? void 0 : n.call(t));
    return;
  }
  const r = e.getReader(), s = r.cancel();
  r.releaseLock(), await s;
}
var mX = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
});
function gX(e) {
  return Object.entries(e).filter(([t, n]) => typeof n < "u").map(([t, n]) => {
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") return `${encodeURIComponent(t)}=${encodeURIComponent(n)}`;
    if (n === null) return `${encodeURIComponent(t)}=`;
    throw new Hs(`Cannot stringify type ${typeof n}; Expected string, number, boolean, or null. If you need to pass nested query parameters, you can manually encode them, e.g. { query: { 'foo[key1]': value1, 'foo[key2]': value2 } }, and please open a GitHub issue requesting better support for your use case.`);
  }).join("&");
}
var vX = "0.0.1", HP = () => {
  var e;
  if (typeof File > "u") {
    const { process: t } = globalThis, n = typeof ((e = t?.versions) === null || e === void 0 ? void 0 : e.node) == "string" && parseInt(t.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (n ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function v_(e, t, n) {
  return HP(), new File(e, t ?? "unknown_file", n);
}
function yX(e) {
  return (typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "").split(/[\\/]/).pop() || void 0;
}
var _X = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", zP = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", bX = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && zP(e), wX = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function SX(e, t, n) {
  if (HP(), e = await e, bX(e))
    return e instanceof File ? e : v_([await e.arrayBuffer()], e.name);
  if (wX(e)) {
    const s = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), v_(await C0(s), t, n);
  }
  const r = await C0(e);
  if (t || (t = yX(e)), !n?.type) {
    const s = r.find((a) => typeof a == "object" && "type" in a && a.type);
    typeof s == "string" && (n = Object.assign(Object.assign({}, n), { type: s }));
  }
  return v_(r, t, n);
}
async function C0(e) {
  var t, n, r, s, a;
  let o = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) o.push(e);
  else if (zP(e)) o.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (_X(e)) try {
    for (var l = !0, i = oa(e), u; u = await i.next(), t = u.done, !t; l = !0) {
      s = u.value, l = !1;
      const d = s;
      o.push(...await C0(d));
    }
  } catch (d) {
    n = { error: d };
  } finally {
    try {
      !l && !t && (r = i.return) && await r.call(i);
    } finally {
      if (n) throw n.error;
    }
  }
  else {
    const d = (a = e?.constructor) === null || a === void 0 ? void 0 : a.name;
    throw new Error(`Unexpected data type: ${typeof e}${d ? `; constructor: ${d}` : ""}${AX(e)}`);
  }
  return o;
}
function AX(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var Xw = class {
  constructor(e) {
    this._client = e;
  }
};
Xw._key = [];
function KP(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var JC = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), TX = (e = KP) => (function(n, ...r) {
  if (n.length === 1) return n[0];
  let s = !1;
  const a = [], o = n.reduce((d, f, m) => {
    var v, y, S;
    /[?#]/.test(f) && (s = !0);
    const x = r[m];
    let I = (s ? encodeURIComponent : e)("" + x);
    return m !== r.length && (x == null || typeof x == "object" && x.toString === ((S = Object.getPrototypeOf((y = Object.getPrototypeOf((v = x.hasOwnProperty) !== null && v !== void 0 ? v : JC)) !== null && y !== void 0 ? y : JC)) === null || S === void 0 ? void 0 : S.toString)) && (I = x + "", a.push({
      start: d.length + f.length,
      length: I.length,
      error: `Value of type ${Object.prototype.toString.call(x).slice(8, -1)} is not a valid path parameter`
    })), d + f + (m === r.length ? "" : I);
  }, ""), l = o.split(/[?#]/, 1)[0], i = /(^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = i.exec(l)) !== null; ) {
    const d = u[0].startsWith("/"), f = d ? 1 : 0, m = d ? u[0].slice(1) : u[0];
    a.push({
      start: u.index + f,
      length: m.length,
      error: `Value "${m}" can't be safely passed as a path parameter`
    });
  }
  if (a.sort((d, f) => d.start - f.start), a.length > 0) {
    let d = 0;
    const f = a.reduce((m, v) => {
      const y = " ".repeat(v.start - d), S = "^".repeat(v.length);
      return d = v.start + v.length, m + y + S;
    }, "");
    throw new Hs(`Path parameters result in path with invalid segments:
${a.map((m) => m.error).join(`
`)}
${o}
${f}`);
  }
  return o;
}), Xs = /* @__PURE__ */ TX(KP), WP = class extends Xw {
  create(e, t) {
    var n;
    const { api_version: r = this._client.apiVersion } = e, s = ci(e, ["api_version"]);
    if ("model" in s && "agent_config" in s) throw new Hs("Invalid request: specified `model` and `agent_config`. If specifying `model`, use `generation_config`.");
    if ("agent" in s && "generation_config" in s) throw new Hs("Invalid request: specified `agent` and `generation_config`. If specifying `agent`, use `agent_config`.");
    return this._client.post(Xs`/${r}/interactions`, Object.assign(Object.assign({ body: s }, t), { stream: (n = e.stream) !== null && n !== void 0 ? n : !1 }));
  }
  delete(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.delete(Xs`/${r}/interactions/${e}`, n);
  }
  cancel(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.post(Xs`/${r}/interactions/${e}/cancel`, n);
  }
  get(e, t = {}, n) {
    var r;
    const s = t ?? {}, { api_version: a = this._client.apiVersion } = s, o = ci(s, ["api_version"]);
    return this._client.get(Xs`/${a}/interactions/${e}`, Object.assign(Object.assign({ query: o }, n), { stream: (r = t?.stream) !== null && r !== void 0 ? r : !1 }));
  }
};
WP._key = Object.freeze(["interactions"]);
var GP = class extends WP {
}, VP = class extends Xw {
  create(e, t) {
    const { api_version: n = this._client.apiVersion, webhook_id: r } = e, s = ci(e, ["api_version", "webhook_id"]);
    return this._client.post(Xs`/${n}/webhooks`, Object.assign({
      query: { webhook_id: r },
      body: s
    }, t));
  }
  update(e, t, n) {
    const { api_version: r = this._client.apiVersion, update_mask: s } = t, a = ci(t, ["api_version", "update_mask"]);
    return this._client.patch(Xs`/${r}/webhooks/${e}`, Object.assign({
      query: { update_mask: s },
      body: a
    }, n));
  }
  list(e = {}, t) {
    const n = e ?? {}, { api_version: r = this._client.apiVersion } = n, s = ci(n, ["api_version"]);
    return this._client.get(Xs`/${r}/webhooks`, Object.assign({ query: s }, t));
  }
  delete(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.delete(Xs`/${r}/webhooks/${e}`, n);
  }
  get(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.get(Xs`/${r}/webhooks/${e}`, n);
  }
  ping(e, t = void 0, n) {
    const { api_version: r = this._client.apiVersion, body: s } = t ?? {};
    return this._client.post(Xs`/${r}/webhooks/${e}:ping`, Object.assign({ body: s }, n));
  }
  rotateSigningSecret(e, t = {}, n) {
    const r = t ?? {}, { api_version: s = this._client.apiVersion } = r, a = ci(r, ["api_version"]);
    return this._client.post(Xs`/${s}/webhooks/${e}:rotateSigningSecret`, Object.assign({ body: a }, n));
  }
};
VP._key = Object.freeze(["webhooks"]);
var JP = class extends VP {
};
function EX(e) {
  let t = 0;
  for (const s of e) t += s.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const s of e)
    n.set(s, r), r += s.length;
  return n;
}
var fp;
function jw(e) {
  let t;
  return (fp ?? (t = new globalThis.TextEncoder(), fp = t.encode.bind(t)))(e);
}
var hp;
function YC(e) {
  let t;
  return (hp ?? (t = new globalThis.TextDecoder(), hp = t.decode.bind(t)))(e);
}
var hv = class {
  constructor() {
    this.buffer = new Uint8Array(), this.carriageReturnIndex = null, this.searchIndex = 0;
  }
  decode(e) {
    var t;
    if (e == null) return [];
    const n = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? jw(e) : e;
    this.buffer = EX([this.buffer, n]);
    const r = [];
    let s;
    for (; (s = CX(this.buffer, (t = this.carriageReturnIndex) !== null && t !== void 0 ? t : this.searchIndex)) != null; ) {
      if (s.carriage && this.carriageReturnIndex == null) {
        this.carriageReturnIndex = s.index;
        continue;
      }
      if (this.carriageReturnIndex != null && (s.index !== this.carriageReturnIndex + 1 || s.carriage)) {
        r.push(YC(this.buffer.subarray(0, this.carriageReturnIndex - 1))), this.buffer = this.buffer.subarray(this.carriageReturnIndex), this.carriageReturnIndex = null, this.searchIndex = 0;
        continue;
      }
      const a = this.carriageReturnIndex !== null ? s.preceding - 1 : s.preceding, o = YC(this.buffer.subarray(0, a));
      r.push(o), this.buffer = this.buffer.subarray(s.index), this.carriageReturnIndex = null, this.searchIndex = 0;
    }
    return this.searchIndex = Math.max(0, this.buffer.length - 1), r;
  }
  flush() {
    return this.buffer.length ? this.decode(`
`) : [];
  }
};
hv.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
hv.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function CX(e, t) {
  const s = t ?? 0, a = e.indexOf(10, s), o = e.indexOf(13, s);
  if (a === -1 && o === -1) return null;
  let l;
  return a !== -1 && o !== -1 ? l = Math.min(a, o) : l = a !== -1 ? a : o, e[l] === 10 ? {
    preceding: l,
    index: l + 1,
    carriage: !1
  } : {
    preceding: l,
    index: l + 1,
    carriage: !0
  };
}
var ig = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, XC = (e, t, n) => {
  if (e) {
    if (lX(ig, e)) return e;
    Fr(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(ig))}`);
  }
};
function _d() {
}
function pp(e, t, n) {
  return !t || ig[e] > ig[n] ? _d : t[e].bind(t);
}
var IX = {
  error: _d,
  warn: _d,
  info: _d,
  debug: _d
}, jC = /* @__PURE__ */ new WeakMap();
function Fr(e) {
  var t;
  const n = e.logger, r = (t = e.logLevel) !== null && t !== void 0 ? t : "off";
  if (!n) return IX;
  const s = jC.get(n);
  if (s && s[0] === r) return s[1];
  const a = {
    error: pp("error", n, r),
    warn: pp("warn", n, r),
    info: pp("info", n, r),
    debug: pp("debug", n, r)
  };
  return jC.set(n, [r, a]), a;
}
var Qi = (e) => (e.options && (e.options = Object.assign({}, e.options), delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "x-goog-api-key" || t.toLowerCase() === "authorization" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), xX = class bd {
  constructor(t, n, r) {
    this.iterator = t, this.controller = n, this.client = r;
  }
  static fromSSEResponse(t, n, r) {
    let s = !1;
    const a = r ? Fr(r) : console;
    function o() {
      return aa(this, arguments, function* () {
        var i, u, d, f;
        if (s) throw new Hs("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
        s = !0;
        let m = !1;
        try {
          try {
            for (var v = !0, y = oa(kX(t, n)), S; S = yield kt(y.next()), i = S.done, !i; v = !0) {
              f = S.value, v = !1;
              const x = f;
              if (!m)
                if (x.data.startsWith("[DONE]")) {
                  m = !0;
                  continue;
                } else try {
                  yield yield kt(JSON.parse(x.data));
                } catch (I) {
                  throw a.error("Could not parse message into JSON:", x.data), a.error("From chunk:", x.raw), I;
                }
            }
          } catch (x) {
            u = { error: x };
          } finally {
            try {
              !v && !i && (d = y.return) && (yield kt(d.call(y)));
            } finally {
              if (u) throw u.error;
            }
          }
          m = !0;
        } catch (x) {
          if (w0(x)) return yield kt(void 0);
          throw x;
        } finally {
          m || n.abort();
        }
      });
    }
    return new bd(o, n, r);
  }
  static fromReadableStream(t, n, r) {
    let s = !1;
    function a() {
      return aa(this, arguments, function* () {
        var i, u, d, f;
        const m = new hv(), v = qP(t);
        try {
          for (var y = !0, S = oa(v), x; x = yield kt(S.next()), i = x.done, !i; y = !0) {
            f = x.value, y = !1;
            const I = f;
            for (const E of m.decode(I)) yield yield kt(E);
          }
        } catch (I) {
          u = { error: I };
        } finally {
          try {
            !y && !i && (d = S.return) && (yield kt(d.call(S)));
          } finally {
            if (u) throw u.error;
          }
        }
        for (const I of m.flush()) yield yield kt(I);
      });
    }
    function o() {
      return aa(this, arguments, function* () {
        var i, u, d, f;
        if (s) throw new Hs("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
        s = !0;
        let m = !1;
        try {
          try {
            for (var v = !0, y = oa(a()), S; S = yield kt(y.next()), i = S.done, !i; v = !0) {
              f = S.value, v = !1;
              const x = f;
              m || x && (yield yield kt(JSON.parse(x)));
            }
          } catch (x) {
            u = { error: x };
          } finally {
            try {
              !v && !i && (d = y.return) && (yield kt(d.call(y)));
            } finally {
              if (u) throw u.error;
            }
          }
          m = !0;
        } catch (x) {
          if (w0(x)) return yield kt(void 0);
          throw x;
        } finally {
          m || n.abort();
        }
      });
    }
    return new bd(o, n, r);
  }
  [Symbol.asyncIterator]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), s = (a) => ({ next: () => {
      if (a.length === 0) {
        const o = r.next();
        t.push(o), n.push(o);
      }
      return a.shift();
    } });
    return [new bd(() => s(t), this.controller, this.client), new bd(() => s(n), this.controller, this.client)];
  }
  toReadableStream() {
    const t = this;
    let n;
    return UP({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: s, done: a } = await n.next();
          if (a) return r.close();
          const o = jw(JSON.stringify(s) + `
`);
          r.enqueue(o);
        } catch (s) {
          r.error(s);
        }
      },
      async cancel() {
        var r;
        await ((r = n.return) === null || r === void 0 ? void 0 : r.call(n));
      }
    });
  }
};
function kX(e, t) {
  return aa(this, arguments, function* () {
    var r, s, a, o;
    if (!e.body)
      throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new Hs("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new Hs("Attempted to iterate over a response with no body");
    const l = new PX(), i = new hv(), u = qP(e.body);
    try {
      for (var d = !0, f = oa(MX(u)), m; m = yield kt(f.next()), r = m.done, !r; d = !0) {
        o = m.value, d = !1;
        const v = o;
        for (const y of i.decode(v)) {
          const S = l.decode(y);
          S && (yield yield kt(S));
        }
      }
    } catch (v) {
      s = { error: v };
    } finally {
      try {
        !d && !r && (a = f.return) && (yield kt(a.call(f)));
      } finally {
        if (s) throw s.error;
      }
    }
    for (const v of i.flush()) {
      const y = l.decode(v);
      y && (yield yield kt(y));
    }
  });
}
function MX(e) {
  return aa(this, arguments, function* () {
    var n, r, s, a;
    try {
      for (var o = !0, l = oa(e), i; i = yield kt(l.next()), n = i.done, !n; o = !0) {
        a = i.value, o = !1;
        const u = a;
        u != null && (yield yield kt(u instanceof ArrayBuffer ? new Uint8Array(u) : typeof u == "string" ? jw(u) : u));
      }
    } catch (u) {
      r = { error: u };
    } finally {
      try {
        !o && !n && (s = l.return) && (yield kt(s.call(l)));
      } finally {
        if (r) throw r.error;
      }
    }
  });
}
var PX = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const s = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], s;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = RX(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function RX(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function NX(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: s, startTime: a } = t, o = await (async () => {
    var l;
    if (t.options.stream)
      return Fr(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller, e) : xX.fromSSEResponse(n, t.controller, e);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const i = n.headers.get("content-type"), u = (l = i?.split(";")[0]) === null || l === void 0 ? void 0 : l.trim();
    return u?.includes("application/json") || u?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : await n.json() : await n.text();
  })();
  return Fr(e).debug(`[${r}] response parsed`, Qi({
    retryOfRequestLogID: s,
    url: n.url,
    status: n.status,
    body: o,
    durationMs: Date.now() - a
  })), o;
}
var $X = class YP extends Promise {
  constructor(t, n, r = NX) {
    super((s) => {
      s(null);
    }), this.responsePromise = n, this.parseResponse = r, this.client = t;
  }
  _thenUnwrap(t) {
    return new YP(this.client, this.responsePromise, async (n, r) => t(await this.parseResponse(n, r), r));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(this.client, t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
}, XP = /* @__PURE__ */ Symbol("brand.privateNullableHeaders");
function* LX(e) {
  if (!e) return;
  if (XP in e) {
    const { values: r, nulls: s } = e;
    yield* r.entries();
    for (const a of s) yield [a, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : GC(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const s = r[0];
    if (typeof s != "string") throw new TypeError("expected header name to be a string");
    const a = GC(r[1]) ? r[1] : [r[1]];
    let o = !1;
    for (const l of a)
      l !== void 0 && (t && !o && (o = !0, yield [s, null]), yield [s, l]);
  }
}
var rd = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const s = /* @__PURE__ */ new Set();
    for (const [a, o] of LX(r)) {
      const l = a.toLowerCase();
      s.has(l) || (t.delete(a), s.add(l)), o === null ? (t.delete(a), n.add(l)) : (t.append(a, o), n.delete(l));
    }
  }
  return {
    [XP]: !0,
    values: t,
    nulls: n
  };
}, y_ = (e) => {
  var t, n, r, s, a;
  if (typeof globalThis.process < "u") return ((n = (t = globalThis.process.env) === null || t === void 0 ? void 0 : t[e]) === null || n === void 0 ? void 0 : n.trim()) || void 0;
  if (typeof globalThis.Deno < "u") return ((a = (s = (r = globalThis.Deno.env) === null || r === void 0 ? void 0 : r.get) === null || s === void 0 ? void 0 : s.call(r, e)) === null || a === void 0 ? void 0 : a.trim()) || void 0;
}, jP, QP = class ZP {
  constructor(t) {
    var n, r, s, a, o, l, i, { baseURL: u = y_("GEMINI_NEXT_GEN_API_BASE_URL"), apiKey: d = (n = y_("GEMINI_API_KEY")) !== null && n !== void 0 ? n : null, apiVersion: f = "v1beta" } = t, m = ci(t, [
      "baseURL",
      "apiKey",
      "apiVersion"
    ]);
    const v = Object.assign(Object.assign({
      apiKey: d,
      apiVersion: f
    }, m), { baseURL: u || "https://generativelanguage.googleapis.com" });
    this.baseURL = v.baseURL, this.timeout = (r = v.timeout) !== null && r !== void 0 ? r : ZP.DEFAULT_TIMEOUT, this.logger = (s = v.logger) !== null && s !== void 0 ? s : console;
    const y = "warn";
    this.logLevel = y, this.logLevel = (o = (a = XC(v.logLevel, "ClientOptions.logLevel", this)) !== null && a !== void 0 ? a : XC(y_("GEMINI_NEXT_GEN_API_LOG"), "process.env['GEMINI_NEXT_GEN_API_LOG']", this)) !== null && o !== void 0 ? o : y, this.fetchOptions = v.fetchOptions, this.maxRetries = (l = v.maxRetries) !== null && l !== void 0 ? l : 2, this.fetch = (i = v.fetch) !== null && i !== void 0 ? i : fX(), this.encoder = mX, this._options = v, this.apiKey = d, this.apiVersion = f, this.clientAdapter = v.clientAdapter;
  }
  withOptions(t) {
    return new this.constructor(Object.assign(Object.assign(Object.assign({}, this._options), {
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this.apiKey,
      apiVersion: this.apiVersion
    }), t));
  }
  baseURLOverridden() {
    return this.baseURL !== "https://generativelanguage.googleapis.com";
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: t, nulls: n }) {
    if (!(t.has("authorization") || t.has("x-goog-api-key")) && !(this.apiKey && t.get("x-goog-api-key")) && !n.has("x-goog-api-key"))
      throw new Error('Could not resolve authentication method. Expected the apiKey to be set. Or for the "x-goog-api-key" headers to be explicitly omitted');
  }
  async authHeaders(t) {
    const n = rd([t.headers]);
    if (!(n.values.has("authorization") || n.values.has("x-goog-api-key"))) {
      if (this.apiKey) return rd([{ "x-goog-api-key": this.apiKey }]);
      if (this.clientAdapter && this.clientAdapter.isVertexAI()) return rd([await this.clientAdapter.getAuthHeaders()]);
    }
  }
  stringifyQuery(t) {
    return gX(t);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${vX}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${aX()}`;
  }
  makeStatusError(t, n, r, s) {
    return Ws.generate(t, n, r, s);
  }
  buildURL(t, n, r) {
    const s = !this.baseURLOverridden() && r || this.baseURL, a = iX(t) ? new URL(t) : new URL(s + (s.endsWith("/") && t.startsWith("/") ? t.slice(1) : t)), o = this.defaultQuery(), l = Object.fromEntries(a.searchParams);
    return (!VC(o) || !VC(l)) && (n = Object.assign(Object.assign(Object.assign({}, l), o), n)), typeof n == "object" && n && !Array.isArray(n) && (a.search = this.stringifyQuery(n)), a.toString();
  }
  async prepareOptions(t) {
    if (this.clientAdapter && this.clientAdapter.isVertexAI() && !t.path.startsWith(`/${this.apiVersion}/projects/`)) {
      const n = t.path.slice(this.apiVersion.length + 1);
      t.path = `/${this.apiVersion}/projects/${this.clientAdapter.getProject()}/locations/${this.clientAdapter.getLocation()}${n}`;
    }
  }
  async prepareRequest(t, { url: n, options: r }) {
  }
  get(t, n) {
    return this.methodRequest("get", t, n);
  }
  post(t, n) {
    return this.methodRequest("post", t, n);
  }
  patch(t, n) {
    return this.methodRequest("patch", t, n);
  }
  put(t, n) {
    return this.methodRequest("put", t, n);
  }
  delete(t, n) {
    return this.methodRequest("delete", t, n);
  }
  methodRequest(t, n, r) {
    return this.request(Promise.resolve(r).then((s) => Object.assign({
      method: t,
      path: n
    }, s)));
  }
  request(t, n = null) {
    return new $X(this, this.makeRequest(t, n, void 0));
  }
  async makeRequest(t, n, r) {
    var s, a, o;
    const l = await t, i = (s = l.maxRetries) !== null && s !== void 0 ? s : this.maxRetries;
    n == null && (n = i), await this.prepareOptions(l);
    const { req: u, url: d, timeout: f } = await this.buildRequest(l, { retryCount: i - n });
    await this.prepareRequest(u, {
      url: d,
      options: l
    });
    const m = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), v = r === void 0 ? "" : `, retryOf: ${r}`, y = Date.now();
    if (Fr(this).debug(`[${m}] sending request`, Qi({
      retryOfRequestLogID: r,
      method: l.method,
      url: d,
      options: l,
      headers: u.headers
    })), !((a = l.signal) === null || a === void 0) && a.aborted) throw new T0();
    const S = new AbortController(), x = await this.fetchWithTimeout(d, u, f, S).catch(S0), I = Date.now();
    if (x instanceof globalThis.Error) {
      const N = `retrying, ${n} attempts remaining`;
      if (!((o = l.signal) === null || o === void 0) && o.aborted) throw new T0();
      const $ = w0(x) || /timed? ?out/i.test(String(x) + ("cause" in x ? String(x.cause) : ""));
      if (n)
        return Fr(this).info(`[${m}] connection ${$ ? "timed out" : "failed"} - ${N}`), Fr(this).debug(`[${m}] connection ${$ ? "timed out" : "failed"} (${N})`, Qi({
          retryOfRequestLogID: r,
          url: d,
          durationMs: I - y,
          message: x.message
        })), this.retryRequest(l, n, r ?? m);
      throw Fr(this).info(`[${m}] connection ${$ ? "timed out" : "failed"} - error; no more retries left`), Fr(this).debug(`[${m}] connection ${$ ? "timed out" : "failed"} (error; no more retries left)`, Qi({
        retryOfRequestLogID: r,
        url: d,
        durationMs: I - y,
        message: x.message
      })), $ ? new PP() : new fv({ cause: x });
    }
    const E = `[${m}${v}] ${u.method} ${d} ${x.ok ? "succeeded" : "failed"} with status ${x.status} in ${I - y}ms`;
    if (!x.ok) {
      const N = await this.shouldRetry(x);
      if (n && N) {
        const b = `retrying, ${n} attempts remaining`;
        return await pX(x.body), Fr(this).info(`${E} - ${b}`), Fr(this).debug(`[${m}] response error (${b})`, Qi({
          retryOfRequestLogID: r,
          url: x.url,
          status: x.status,
          headers: x.headers,
          durationMs: I - y
        })), this.retryRequest(l, n, r ?? m, x.headers);
      }
      const $ = N ? "error; no more retries left" : "error; not retryable";
      Fr(this).info(`${E} - ${$}`);
      const p = await x.text().catch((b) => S0(b).message), w = cX(p), _ = w ? void 0 : p;
      throw Fr(this).debug(`[${m}] response error (${$})`, Qi({
        retryOfRequestLogID: r,
        url: x.url,
        status: x.status,
        headers: x.headers,
        message: _,
        durationMs: Date.now() - y
      })), this.makeStatusError(x.status, w, _, x.headers);
    }
    return Fr(this).info(E), Fr(this).debug(`[${m}] response start`, Qi({
      retryOfRequestLogID: r,
      url: x.url,
      status: x.status,
      headers: x.headers,
      durationMs: I - y
    })), {
      response: x,
      options: l,
      controller: S,
      requestLogID: m,
      retryOfRequestLogID: r,
      startTime: y
    };
  }
  async fetchWithTimeout(t, n, r, s) {
    const a = n || {}, { signal: o, method: l } = a, i = ci(a, ["signal", "method"]), u = this._makeAbort(s);
    o && o.addEventListener("abort", u, { once: !0 });
    const d = setTimeout(u, r), f = globalThis.ReadableStream && i.body instanceof globalThis.ReadableStream || typeof i.body == "object" && i.body !== null && Symbol.asyncIterator in i.body, m = Object.assign(Object.assign(Object.assign({ signal: s.signal }, f ? { duplex: "half" } : {}), { method: "GET" }), i);
    l && (m.method = l.toUpperCase());
    try {
      return await this.fetch.call(void 0, t, m);
    } finally {
      clearTimeout(d);
    }
  }
  async shouldRetry(t) {
    const n = t.headers.get("x-should-retry");
    return n === "true" ? !0 : n === "false" ? !1 : t.status === 408 || t.status === 409 || t.status === 429 || t.status >= 500;
  }
  async retryRequest(t, n, r, s) {
    var a;
    let o;
    const l = s?.get("retry-after-ms");
    if (l) {
      const u = parseFloat(l);
      Number.isNaN(u) || (o = u);
    }
    const i = s?.get("retry-after");
    if (i && !o) {
      const u = parseFloat(i);
      Number.isNaN(u) ? o = Date.parse(i) - Date.now() : o = u * 1e3;
    }
    if (o === void 0) {
      const u = (a = t.maxRetries) !== null && a !== void 0 ? a : this.maxRetries;
      o = this.calculateDefaultRetryTimeoutMillis(n, u);
    }
    return await dX(o), this.makeRequest(t, n - 1, r);
  }
  calculateDefaultRetryTimeoutMillis(t, n) {
    const a = n - t;
    return Math.min(0.5 * Math.pow(2, a), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  async buildRequest(t, { retryCount: n = 0 } = {}) {
    var r, s, a;
    const o = Object.assign({}, t), { method: l, path: i, query: u, defaultBaseURL: d } = o, f = this.buildURL(i, u, d);
    "timeout" in o && uX("timeout", o.timeout), o.timeout = (r = o.timeout) !== null && r !== void 0 ? r : this.timeout;
    const { bodyHeaders: m, body: v } = this.buildBody({ options: o }), y = await this.buildHeaders({
      options: t,
      method: l,
      bodyHeaders: m,
      retryCount: n
    });
    return {
      req: Object.assign(Object.assign(Object.assign(Object.assign(Object.assign({
        method: l,
        headers: y
      }, o.signal && { signal: o.signal }), globalThis.ReadableStream && v instanceof globalThis.ReadableStream && { duplex: "half" }), v && { body: v }), (s = this.fetchOptions) !== null && s !== void 0 ? s : {}), (a = o.fetchOptions) !== null && a !== void 0 ? a : {}),
      url: f,
      timeout: o.timeout
    };
  }
  async buildHeaders({ options: t, method: n, bodyHeaders: r, retryCount: s }) {
    let a = {};
    this.idempotencyHeader && n !== "get" && (t.idempotencyKey || (t.idempotencyKey = this.defaultIdempotencyKey()), a[this.idempotencyHeader] = t.idempotencyKey);
    const o = await this.authHeaders(t);
    let l = rd([
      a,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent()
      },
      this._options.defaultHeaders,
      r,
      t.headers,
      o
    ]);
    return this.validateHeaders(l), l.values;
  }
  _makeAbort(t) {
    return () => t.abort();
  }
  buildBody({ options: { body: t, headers: n } }) {
    if (!t) return {
      bodyHeaders: void 0,
      body: void 0
    };
    const r = rd([n]);
    return ArrayBuffer.isView(t) || t instanceof ArrayBuffer || t instanceof DataView || typeof t == "string" && r.values.has("content-type") || globalThis.Blob && t instanceof globalThis.Blob || t instanceof FormData || t instanceof URLSearchParams || globalThis.ReadableStream && t instanceof globalThis.ReadableStream ? {
      bodyHeaders: void 0,
      body: t
    } : typeof t == "object" && (Symbol.asyncIterator in t || Symbol.iterator in t && "next" in t && typeof t.next == "function") ? {
      bodyHeaders: void 0,
      body: hX(t)
    } : typeof t == "object" && r.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(t)
    } : this.encoder({
      body: t,
      headers: r
    });
  }
};
QP.DEFAULT_TIMEOUT = 6e4;
var nr = class extends QP {
  constructor() {
    super(...arguments), this.interactions = new GP(this), this.webhooks = new JP(this);
  }
};
jP = nr;
nr.GeminiNextGenAPIClient = jP;
nr.GeminiNextGenAPIClientError = Hs;
nr.APIError = Ws;
nr.APIConnectionError = fv;
nr.APIConnectionTimeoutError = PP;
nr.APIUserAbortError = T0;
nr.NotFoundError = LP;
nr.ConflictError = DP;
nr.RateLimitError = FP;
nr.BadRequestError = RP;
nr.AuthenticationError = NP;
nr.InternalServerError = BP;
nr.PermissionDeniedError = $P;
nr.UnprocessableEntityError = OP;
nr.toFile = SX;
nr.Interactions = GP;
nr.Webhooks = JP;
function DX(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && h(n, ["_url", "name"], r), n;
}
function OX(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && h(n, ["_url", "name"], r), n;
}
function FX(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && h(n, ["sdkHttpResponse"], r), n;
}
function BX(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && h(n, ["sdkHttpResponse"], r), n;
}
function UX(e, t, n) {
  const r = {};
  if (c(e, ["validationDataset"]) !== void 0) throw new Error("validationDataset parameter is not supported in Gemini API.");
  const s = c(e, ["tunedModelDisplayName"]);
  if (t !== void 0 && s != null && h(t, ["displayName"], s), c(e, ["description"]) !== void 0) throw new Error("description parameter is not supported in Gemini API.");
  const a = c(e, ["epochCount"]);
  t !== void 0 && a != null && h(t, [
    "tuningTask",
    "hyperparameters",
    "epochCount"
  ], a);
  const o = c(e, ["learningRateMultiplier"]);
  if (o != null && h(r, [
    "tuningTask",
    "hyperparameters",
    "learningRateMultiplier"
  ], o), c(e, ["exportLastCheckpointOnly"]) !== void 0) throw new Error("exportLastCheckpointOnly parameter is not supported in Gemini API.");
  if (c(e, ["preTunedModelCheckpointId"]) !== void 0) throw new Error("preTunedModelCheckpointId parameter is not supported in Gemini API.");
  if (c(e, ["adapterSize"]) !== void 0) throw new Error("adapterSize parameter is not supported in Gemini API.");
  if (c(e, ["tuningMode"]) !== void 0) throw new Error("tuningMode parameter is not supported in Gemini API.");
  if (c(e, ["customBaseModel"]) !== void 0) throw new Error("customBaseModel parameter is not supported in Gemini API.");
  const l = c(e, ["batchSize"]);
  t !== void 0 && l != null && h(t, [
    "tuningTask",
    "hyperparameters",
    "batchSize"
  ], l);
  const i = c(e, ["learningRate"]);
  if (t !== void 0 && i != null && h(t, [
    "tuningTask",
    "hyperparameters",
    "learningRate"
  ], i), c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  if (c(e, ["beta"]) !== void 0) throw new Error("beta parameter is not supported in Gemini API.");
  if (c(e, ["baseTeacherModel"]) !== void 0) throw new Error("baseTeacherModel parameter is not supported in Gemini API.");
  if (c(e, ["tunedTeacherModelSource"]) !== void 0) throw new Error("tunedTeacherModelSource parameter is not supported in Gemini API.");
  if (c(e, ["sftLossWeightMultiplier"]) !== void 0) throw new Error("sftLossWeightMultiplier parameter is not supported in Gemini API.");
  if (c(e, ["outputUri"]) !== void 0) throw new Error("outputUri parameter is not supported in Gemini API.");
  if (c(e, ["encryptionSpec"]) !== void 0) throw new Error("encryptionSpec parameter is not supported in Gemini API.");
  return r;
}
function qX(e, t, n) {
  const r = {};
  let s = c(n, ["config", "method"]);
  if (s === void 0 && (s = "SUPERVISED_FINE_TUNING"), s === "SUPERVISED_FINE_TUNING") {
    const w = c(e, ["validationDataset"]);
    t !== void 0 && w != null && h(t, ["supervisedTuningSpec"], __(w));
  } else if (s === "PREFERENCE_TUNING") {
    const w = c(e, ["validationDataset"]);
    t !== void 0 && w != null && h(t, ["preferenceOptimizationSpec"], __(w));
  } else if (s === "DISTILLATION") {
    const w = c(e, ["validationDataset"]);
    t !== void 0 && w != null && h(t, ["distillationSpec"], __(w));
  }
  const a = c(e, ["tunedModelDisplayName"]);
  t !== void 0 && a != null && h(t, ["tunedModelDisplayName"], a);
  const o = c(e, ["description"]);
  t !== void 0 && o != null && h(t, ["description"], o);
  let l = c(n, ["config", "method"]);
  if (l === void 0 && (l = "SUPERVISED_FINE_TUNING"), l === "SUPERVISED_FINE_TUNING") {
    const w = c(e, ["epochCount"]);
    t !== void 0 && w != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "epochCount"
    ], w);
  } else if (l === "PREFERENCE_TUNING") {
    const w = c(e, ["epochCount"]);
    t !== void 0 && w != null && h(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "epochCount"
    ], w);
  } else if (l === "DISTILLATION") {
    const w = c(e, ["epochCount"]);
    t !== void 0 && w != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "epochCount"
    ], w);
  }
  let i = c(n, ["config", "method"]);
  if (i === void 0 && (i = "SUPERVISED_FINE_TUNING"), i === "SUPERVISED_FINE_TUNING") {
    const w = c(e, ["learningRateMultiplier"]);
    t !== void 0 && w != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], w);
  } else if (i === "PREFERENCE_TUNING") {
    const w = c(e, ["learningRateMultiplier"]);
    t !== void 0 && w != null && h(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], w);
  } else if (i === "DISTILLATION") {
    const w = c(e, ["learningRateMultiplier"]);
    t !== void 0 && w != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], w);
  }
  let u = c(n, ["config", "method"]);
  if (u === void 0 && (u = "SUPERVISED_FINE_TUNING"), u === "SUPERVISED_FINE_TUNING") {
    const w = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && w != null && h(t, ["supervisedTuningSpec", "exportLastCheckpointOnly"], w);
  } else if (u === "PREFERENCE_TUNING") {
    const w = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && w != null && h(t, ["preferenceOptimizationSpec", "exportLastCheckpointOnly"], w);
  } else if (u === "DISTILLATION") {
    const w = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && w != null && h(t, ["distillationSpec", "exportLastCheckpointOnly"], w);
  }
  let d = c(n, ["config", "method"]);
  if (d === void 0 && (d = "SUPERVISED_FINE_TUNING"), d === "SUPERVISED_FINE_TUNING") {
    const w = c(e, ["adapterSize"]);
    t !== void 0 && w != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "adapterSize"
    ], w);
  } else if (d === "PREFERENCE_TUNING") {
    const w = c(e, ["adapterSize"]);
    t !== void 0 && w != null && h(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "adapterSize"
    ], w);
  } else if (d === "DISTILLATION") {
    const w = c(e, ["adapterSize"]);
    t !== void 0 && w != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "adapterSize"
    ], w);
  }
  let f = c(n, ["config", "method"]);
  if (f === void 0 && (f = "SUPERVISED_FINE_TUNING"), f === "SUPERVISED_FINE_TUNING") {
    const w = c(e, ["tuningMode"]);
    t !== void 0 && w != null && h(t, ["supervisedTuningSpec", "tuningMode"], w);
  } else if (f === "DISTILLATION") {
    const w = c(e, ["tuningMode"]);
    t !== void 0 && w != null && h(t, ["distillationSpec", "tuningMode"], w);
  }
  const m = c(e, ["customBaseModel"]);
  t !== void 0 && m != null && h(t, ["customBaseModel"], m);
  let v = c(n, ["config", "method"]);
  if (v === void 0 && (v = "SUPERVISED_FINE_TUNING"), v === "SUPERVISED_FINE_TUNING") {
    const w = c(e, ["batchSize"]);
    t !== void 0 && w != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "batchSize"
    ], w);
  } else if (v === "DISTILLATION") {
    const w = c(e, ["batchSize"]);
    t !== void 0 && w != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "batchSize"
    ], w);
  }
  let y = c(n, ["config", "method"]);
  if (y === void 0 && (y = "SUPERVISED_FINE_TUNING"), y === "SUPERVISED_FINE_TUNING") {
    const w = c(e, ["learningRate"]);
    t !== void 0 && w != null && h(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "learningRate"
    ], w);
  } else if (y === "DISTILLATION") {
    const w = c(e, ["learningRate"]);
    t !== void 0 && w != null && h(t, [
      "distillationSpec",
      "hyperParameters",
      "learningRate"
    ], w);
  }
  const S = c(e, ["labels"]);
  t !== void 0 && S != null && h(t, ["labels"], S);
  const x = c(e, ["beta"]);
  t !== void 0 && x != null && h(t, [
    "preferenceOptimizationSpec",
    "hyperParameters",
    "beta"
  ], x);
  const I = c(e, ["baseTeacherModel"]);
  t !== void 0 && I != null && h(t, ["distillationSpec", "baseTeacherModel"], I);
  const E = c(e, ["tunedTeacherModelSource"]);
  t !== void 0 && E != null && h(t, ["distillationSpec", "tunedTeacherModelSource"], E);
  const N = c(e, ["sftLossWeightMultiplier"]);
  t !== void 0 && N != null && h(t, [
    "distillationSpec",
    "hyperParameters",
    "sftLossWeightMultiplier"
  ], N);
  const $ = c(e, ["outputUri"]);
  t !== void 0 && $ != null && h(t, ["outputUri"], $);
  const p = c(e, ["encryptionSpec"]);
  return t !== void 0 && p != null && h(t, ["encryptionSpec"], p), r;
}
function HX(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && h(n, ["baseModel"], r);
  const s = c(e, ["preTunedModel"]);
  s != null && h(n, ["preTunedModel"], s);
  const a = c(e, ["trainingDataset"]);
  a != null && ZX(a);
  const o = c(e, ["config"]);
  return o != null && UX(o, n), n;
}
function zX(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && h(n, ["baseModel"], r);
  const s = c(e, ["preTunedModel"]);
  s != null && h(n, ["preTunedModel"], s);
  const a = c(e, ["trainingDataset"]);
  a != null && ej(a, n, t);
  const o = c(e, ["config"]);
  return o != null && qX(o, n, t), n;
}
function KX(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && h(n, ["_url", "name"], r), n;
}
function WX(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && h(n, ["_url", "name"], r), n;
}
function GX(e, t, n) {
  const r = {}, s = c(e, ["pageSize"]);
  t !== void 0 && s != null && h(t, ["_query", "pageSize"], s);
  const a = c(e, ["pageToken"]);
  t !== void 0 && a != null && h(t, ["_query", "pageToken"], a);
  const o = c(e, ["filter"]);
  return t !== void 0 && o != null && h(t, ["_query", "filter"], o), r;
}
function VX(e, t, n) {
  const r = {}, s = c(e, ["pageSize"]);
  t !== void 0 && s != null && h(t, ["_query", "pageSize"], s);
  const a = c(e, ["pageToken"]);
  t !== void 0 && a != null && h(t, ["_query", "pageToken"], a);
  const o = c(e, ["filter"]);
  return t !== void 0 && o != null && h(t, ["_query", "filter"], o), r;
}
function JX(e, t) {
  const n = {}, r = c(e, ["config"]);
  return r != null && GX(r, n), n;
}
function YX(e, t) {
  const n = {}, r = c(e, ["config"]);
  return r != null && VX(r, n), n;
}
function XX(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["nextPageToken"]);
  s != null && h(n, ["nextPageToken"], s);
  const a = c(e, ["tunedModels"]);
  if (a != null) {
    let o = a;
    Array.isArray(o) && (o = o.map((l) => eR(l))), h(n, ["tuningJobs"], o);
  }
  return n;
}
function jX(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["nextPageToken"]);
  s != null && h(n, ["nextPageToken"], s);
  const a = c(e, ["tuningJobs"]);
  if (a != null) {
    let o = a;
    Array.isArray(o) && (o = o.map((l) => I0(l))), h(n, ["tuningJobs"], o);
  }
  return n;
}
function QX(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && h(n, ["model"], r);
  const s = c(e, ["name"]);
  return s != null && h(n, ["endpoint"], s), n;
}
function ZX(e, t) {
  const n = {};
  if (c(e, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  if (c(e, ["vertexDatasetResource"]) !== void 0) throw new Error("vertexDatasetResource parameter is not supported in Gemini API.");
  const r = c(e, ["examples"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((a) => a)), h(n, ["examples", "examples"], s);
  }
  return n;
}
function ej(e, t, n) {
  const r = {};
  let s = c(n, ["config", "method"]);
  if (s === void 0 && (s = "SUPERVISED_FINE_TUNING"), s === "SUPERVISED_FINE_TUNING") {
    const o = c(e, ["gcsUri"]);
    t !== void 0 && o != null && h(t, ["supervisedTuningSpec", "trainingDatasetUri"], o);
  } else if (s === "PREFERENCE_TUNING") {
    const o = c(e, ["gcsUri"]);
    t !== void 0 && o != null && h(t, ["preferenceOptimizationSpec", "trainingDatasetUri"], o);
  } else if (s === "DISTILLATION") {
    const o = c(e, ["gcsUri"]);
    t !== void 0 && o != null && h(t, ["distillationSpec", "promptDatasetUri"], o);
  }
  let a = c(n, ["config", "method"]);
  if (a === void 0 && (a = "SUPERVISED_FINE_TUNING"), a === "SUPERVISED_FINE_TUNING") {
    const o = c(e, ["vertexDatasetResource"]);
    t !== void 0 && o != null && h(t, ["supervisedTuningSpec", "trainingDatasetUri"], o);
  } else if (a === "PREFERENCE_TUNING") {
    const o = c(e, ["vertexDatasetResource"]);
    t !== void 0 && o != null && h(t, ["preferenceOptimizationSpec", "trainingDatasetUri"], o);
  } else if (a === "DISTILLATION") {
    const o = c(e, ["vertexDatasetResource"]);
    t !== void 0 && o != null && h(t, ["distillationSpec", "promptDatasetUri"], o);
  }
  if (c(e, ["examples"]) !== void 0) throw new Error("examples parameter is not supported in Vertex AI.");
  return r;
}
function eR(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["name"]);
  s != null && h(n, ["name"], s);
  const a = c(e, ["state"]);
  a != null && h(n, ["state"], uP(a));
  const o = c(e, ["createTime"]);
  o != null && h(n, ["createTime"], o);
  const l = c(e, ["tuningTask", "startTime"]);
  l != null && h(n, ["startTime"], l);
  const i = c(e, ["tuningTask", "completeTime"]);
  i != null && h(n, ["endTime"], i);
  const u = c(e, ["updateTime"]);
  u != null && h(n, ["updateTime"], u);
  const d = c(e, ["description"]);
  d != null && h(n, ["description"], d);
  const f = c(e, ["baseModel"]);
  f != null && h(n, ["baseModel"], f);
  const m = c(e, ["_self"]);
  return m != null && h(n, ["tunedModel"], QX(m)), n;
}
function I0(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["name"]);
  s != null && h(n, ["name"], s);
  const a = c(e, ["state"]);
  a != null && h(n, ["state"], uP(a));
  const o = c(e, ["createTime"]);
  o != null && h(n, ["createTime"], o);
  const l = c(e, ["startTime"]);
  l != null && h(n, ["startTime"], l);
  const i = c(e, ["endTime"]);
  i != null && h(n, ["endTime"], i);
  const u = c(e, ["updateTime"]);
  u != null && h(n, ["updateTime"], u);
  const d = c(e, ["error"]);
  d != null && h(n, ["error"], d);
  const f = c(e, ["description"]);
  f != null && h(n, ["description"], f);
  const m = c(e, ["baseModel"]);
  m != null && h(n, ["baseModel"], m);
  const v = c(e, ["tunedModel"]);
  v != null && h(n, ["tunedModel"], v);
  const y = c(e, ["preTunedModel"]);
  y != null && h(n, ["preTunedModel"], y);
  const S = c(e, ["supervisedTuningSpec"]);
  S != null && h(n, ["supervisedTuningSpec"], S);
  const x = c(e, ["preferenceOptimizationSpec"]);
  x != null && h(n, ["preferenceOptimizationSpec"], x);
  const I = c(e, ["distillationSpec"]);
  I != null && h(n, ["distillationSpec"], I);
  const E = c(e, ["tuningDataStats"]);
  E != null && h(n, ["tuningDataStats"], E);
  const N = c(e, ["encryptionSpec"]);
  N != null && h(n, ["encryptionSpec"], N);
  const $ = c(e, ["partnerModelTuningSpec"]);
  $ != null && h(n, ["partnerModelTuningSpec"], $);
  const p = c(e, ["customBaseModel"]);
  p != null && h(n, ["customBaseModel"], p);
  const w = c(e, ["evaluateDatasetRuns"]);
  if (w != null) {
    let V = w;
    Array.isArray(V) && (V = V.map((le) => le)), h(n, ["evaluateDatasetRuns"], V);
  }
  const _ = c(e, ["experiment"]);
  _ != null && h(n, ["experiment"], _);
  const b = c(e, ["fullFineTuningSpec"]);
  b != null && h(n, ["fullFineTuningSpec"], b);
  const C = c(e, ["labels"]);
  C != null && h(n, ["labels"], C);
  const k = c(e, ["outputUri"]);
  k != null && h(n, ["outputUri"], k);
  const P = c(e, ["pipelineJob"]);
  P != null && h(n, ["pipelineJob"], P);
  const H = c(e, ["serviceAccount"]);
  H != null && h(n, ["serviceAccount"], H);
  const G = c(e, ["tunedModelDisplayName"]);
  G != null && h(n, ["tunedModelDisplayName"], G);
  const ee = c(e, ["tuningJobState"]);
  ee != null && h(n, ["tuningJobState"], ee);
  const J = c(e, ["veoTuningSpec"]);
  J != null && h(n, ["veoTuningSpec"], J);
  const z = c(e, ["distillationSamplingSpec"]);
  z != null && h(n, ["distillationSamplingSpec"], z);
  const R = c(e, ["tuningJobMetadata"]);
  return R != null && h(n, ["tuningJobMetadata"], R), n;
}
function tj(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && h(n, ["sdkHttpResponse"], r);
  const s = c(e, ["name"]);
  s != null && h(n, ["name"], s);
  const a = c(e, ["metadata"]);
  a != null && h(n, ["metadata"], a);
  const o = c(e, ["done"]);
  o != null && h(n, ["done"], o);
  const l = c(e, ["error"]);
  return l != null && h(n, ["error"], l), n;
}
function __(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && h(n, ["validationDatasetUri"], r);
  const s = c(e, ["vertexDatasetResource"]);
  return s != null && h(n, ["validationDatasetUri"], s), n;
}
var nj = class extends Po {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new Fl(ko.PAGED_ITEM_TUNING_JOBS, (n) => this.listInternal(n), await this.listInternal(t), t), this.get = async (t) => await this.getInternal(t), this.tune = async (t) => {
      var n;
      if (this.apiClient.isVertexAI()) if (t.baseModel.startsWith("projects/")) {
        const r = { tunedModelName: t.baseModel };
        !((n = t.config) === null || n === void 0) && n.preTunedModelCheckpointId && (r.checkpointId = t.config.preTunedModelCheckpointId);
        const s = Object.assign(Object.assign({}, t), { preTunedModel: r });
        return s.baseModel = void 0, await this.tuneInternal(s);
      } else {
        const r = Object.assign({}, t);
        return await this.tuneInternal(r);
      }
      else {
        const r = Object.assign({}, t), s = await this.tuneMldevInternal(r);
        let a = "";
        return s.metadata !== void 0 && s.metadata.tunedModel !== void 0 ? a = s.metadata.tunedModel : s.name !== void 0 && s.name.includes("/operations/") && (a = s.name.split("/operations/")[0]), {
          name: a,
          state: h0.JOB_STATE_QUEUED
        };
      }
    };
  }
  async getInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = WX(e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => I0(u));
    } else {
      const i = KX(e);
      return o = Ze("{name}", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => eR(u));
    }
  }
  async listInternal(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = YX(e);
      return o = Ze("tuningJobs", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = jX(u), f = new SC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = JX(e);
      return o = Ze("tunedModels", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = XX(u), f = new SC();
        return Object.assign(f, d), f;
      });
    }
  }
  async cancel(e) {
    var t, n, r, s;
    let a, o = "", l = {};
    if (this.apiClient.isVertexAI()) {
      const i = OX(e);
      return o = Ze("{name}:cancel", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = BX(u), f = new AC();
        return Object.assign(f, d), f;
      });
    } else {
      const i = DX(e);
      return o = Ze("{name}:cancel", i._url), l = i._query, delete i._url, delete i._query, a = this.apiClient.request({
        path: o,
        queryParams: l,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const f = d;
        return f.sdkHttpResponse = { headers: u.headers }, f;
      })), a.then((u) => {
        const d = FX(u), f = new AC();
        return Object.assign(f, d), f;
      });
    }
  }
  async tuneInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const o = zX(e, e);
      return s = Ze("tuningJobs", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json().then((i) => {
        const u = i;
        return u.sdkHttpResponse = { headers: l.headers }, u;
      })), r.then((l) => I0(l));
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async tuneMldevInternal(e) {
    var t, n;
    let r, s = "", a = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = HX(e);
      return s = Ze("tunedModels", o._url), a = o._query, delete o._url, delete o._query, r = this.apiClient.request({
        path: s,
        queryParams: a,
        body: JSON.stringify(o),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json().then((i) => {
        const u = i;
        return u.sdkHttpResponse = { headers: l.headers }, u;
      })), r.then((l) => tj(l));
    }
  }
}, rj = class {
  async download(e, t) {
    throw new Error("Download to file is not supported in the browser, please use a browser compliant download like an <a> tag.");
  }
}, sj = 1024 * 1024 * 8, aj = 3, oj = 1e3, ij = 2, lg = "x-goog-upload-status";
async function lj(e, t, n, r) {
  var s;
  const a = await tR(e, t, n, r), o = await a?.json();
  if (((s = a?.headers) === null || s === void 0 ? void 0 : s[lg]) !== "final") throw new Error("Failed to upload file: Upload status is not finalized.");
  return o.file;
}
async function uj(e, t, n, r) {
  var s;
  const a = await tR(e, t, n, r), o = await a?.json();
  if (((s = a?.headers) === null || s === void 0 ? void 0 : s[lg]) !== "final") throw new Error("Failed to upload file: Upload status is not finalized.");
  const l = eP(o), i = new AK();
  return Object.assign(i, l), i;
}
async function tR(e, t, n, r) {
  var s, a, o;
  let l = t;
  const i = r?.baseUrl || ((s = n.clientOptions.httpOptions) === null || s === void 0 ? void 0 : s.baseUrl);
  if (i) {
    const v = new URL(i), y = new URL(t);
    y.protocol = v.protocol, y.host = v.host, y.port = v.port, l = y.toString();
  }
  let u = 0, d = 0, f = new m0(new Response()), m = "upload";
  for (u = e.size; d < u; ) {
    const v = Math.min(sj, u - d), y = e.slice(d, d + v);
    d + v >= u && (m += ", finalize");
    let S = 0, x = oj;
    for (; S < aj; ) {
      const I = Object.assign(Object.assign({}, r?.headers || {}), {
        "X-Goog-Upload-Command": m,
        "X-Goog-Upload-Offset": String(d),
        "Content-Length": String(v)
      });
      if (f = await n.request({
        path: "",
        body: y,
        httpMethod: "POST",
        httpOptions: Object.assign(Object.assign({}, r), {
          apiVersion: "",
          baseUrl: l,
          headers: I
        })
      }), !((a = f?.headers) === null || a === void 0) && a[lg]) break;
      S++, await dj(x), x = x * ij;
    }
    if (d += v, ((o = f?.headers) === null || o === void 0 ? void 0 : o[lg]) !== "active") break;
    if (u <= d) throw new Error("All content has been uploaded, but the upload status is not finalized.");
  }
  return f;
}
async function cj(e) {
  return {
    size: e.size,
    type: e.type
  };
}
function dj(e) {
  return new Promise((t) => setTimeout(t, e));
}
var fj = class {
  async upload(e, t, n, r) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await lj(e, t, n, r);
  }
  async uploadToFileSearchStore(e, t, n, r) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await uj(e, t, n, r);
  }
  async stat(e) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await cj(e);
  }
}, hj = class {
  create(e, t, n) {
    return new pj(e, t, n);
  }
}, pj = class {
  constructor(e, t, n) {
    this.url = e, this.headers = t, this.callbacks = n;
  }
  connect() {
    this.ws = new WebSocket(this.url), this.ws.onopen = this.callbacks.onopen, this.ws.onerror = this.callbacks.onerror, this.ws.onclose = this.callbacks.onclose, this.ws.onmessage = this.callbacks.onmessage;
  }
  send(e) {
    if (this.ws === void 0) throw new Error("WebSocket is not connected");
    this.ws.send(e);
  }
  close() {
    if (this.ws === void 0) throw new Error("WebSocket is not connected");
    this.ws.close();
  }
}, QC = "x-goog-api-key", mj = class {
  constructor(e) {
    this.apiKey = e;
  }
  async addAuthHeaders(e, t) {
    if (e.get(QC) === null) {
      if (this.apiKey.startsWith("auth_tokens/")) throw new Error("Ephemeral tokens are only supported by the live API.");
      if (!this.apiKey) throw new Error("API key is missing. Please provide a valid API key.");
      e.append(QC, this.apiKey);
    }
  }
}, gj = class {
  getNextGenClient() {
    var e;
    const t = this.httpOptions;
    if (this._nextGenClient === void 0) {
      const n = this.httpOptions;
      this._nextGenClient = new nr({
        baseURL: this.apiClient.getBaseUrl(),
        apiKey: this.apiKey,
        apiVersion: this.apiClient.getApiVersion(),
        clientAdapter: this.apiClient,
        defaultHeaders: this.apiClient.getDefaultHeaders(),
        timeout: n?.timeout,
        maxRetries: (e = n?.retryOptions) === null || e === void 0 ? void 0 : e.attempts
      });
    }
    return t?.extraBody && console.warn("GoogleGenAI.interactions: Client level httpOptions.extraBody is not supported by the interactions client and will be ignored."), this._nextGenClient;
  }
  get interactions() {
    return this._interactions !== void 0 ? this._interactions : (console.warn("GoogleGenAI.interactions: Interactions usage is experimental and may change in future versions."), this._interactions = this.getNextGenClient().interactions, this._interactions);
  }
  get webhooks() {
    return this._webhooks !== void 0 ? this._webhooks : (this._webhooks = this.getNextGenClient().webhooks, this._webhooks);
  }
  constructor(e) {
    var t;
    if (e.apiKey == null) throw new Error("An API Key must be set when running in a browser");
    if (e.project || e.location) throw new Error("Vertex AI project based authentication is not supported on browser runtimes. Please do not provide a project or location.");
    this.vertexai = (t = e.vertexai) !== null && t !== void 0 ? t : !1, this.apiKey = e.apiKey;
    const n = Gz(e.httpOptions, e.vertexai, void 0, void 0);
    n && (e.httpOptions ? e.httpOptions.baseUrl = n : e.httpOptions = { baseUrl: n }), this.apiVersion = e.apiVersion, this.httpOptions = e.httpOptions;
    const r = new mj(this.apiKey);
    this.apiClient = new dY({
      auth: r,
      apiVersion: this.apiVersion,
      apiKey: this.apiKey,
      vertexai: this.vertexai,
      httpOptions: this.httpOptions,
      userAgentExtra: "gl-node/web",
      uploader: new fj(),
      downloader: new rj()
    }), this.models = new PY(this.apiClient), this.live = new EY(this.apiClient, r, new hj()), this.batches = new CW(this.apiClient), this.chats = new cG(this.models, this.apiClient), this.caches = new iG(this.apiClient), this.files = new SG(this.apiClient), this.operations = new RY(this.apiClient), this.authTokens = new XY(this.apiClient), this.tunings = new nj(this.apiClient), this.fileSearchStores = new sX(this.apiClient);
  }
};
function ZC(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function Jd(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function cl(e) {
  return { text: String(e || "") };
}
function vj(e = "") {
  const t = String(e || "").match(/^data:([^;,]+);base64,(.+)$/);
  return t ? { inlineData: {
    mimeType: t[1],
    data: t[2]
  } } : null;
}
function yj(e) {
  if (typeof e == "string") return [cl(e)];
  if (!Array.isArray(e)) return [cl("")];
  const t = e.map((n) => !n || typeof n != "object" ? null : n.type === "text" ? cl(n.text || "") : n.type === "image_url" && n.image_url?.url ? vj(n.image_url.url) : null).filter(Boolean);
  return t.length ? t : [cl("")];
}
function eI() {
  return {
    role: "user",
    parts: [cl("")]
  };
}
function Jf(e, t = "model") {
  if (!e?.parts?.length) return null;
  const n = Jd(e);
  return n ? (n.role || (n.role = t), n) : null;
}
function _j(e) {
  return !!e?.parts?.some((t) => typeof t?.thoughtSignature == "string" && t.thoughtSignature);
}
function bj(e) {
  return !!e?.parts?.some((t) => t?.functionCall?.name);
}
function b_(e, t) {
  return e?.functionCall?.name ? [
    String(e.functionCall.id || ""),
    String(e.functionCall.name || ""),
    JSON.stringify(e.functionCall.args || {}),
    String(t)
  ].join("\0") : "";
}
function wj(e = [], t = "") {
  const n = e.map((i) => Jf(i, "model")).filter(Boolean);
  if (!n.length) return null;
  const r = [...n].reverse().find((i) => _j(i)) || null, s = [...n].reverse().find((i) => bj(i)) || null, a = Jd(r || s || n[n.length - 1]);
  if (!a?.parts?.length) return n[n.length - 1];
  if (s) {
    const i = /* @__PURE__ */ new Map();
    n.forEach((d) => {
      d.parts.forEach((f, m) => {
        const v = b_(f, m);
        if (!v) return;
        const y = i.get(v);
        (!y || f.thoughtSignature || !y.thoughtSignature) && i.set(v, Jd(f));
      });
    });
    const u = /* @__PURE__ */ new Set();
    a.parts = a.parts.map((d, f) => {
      const m = b_(d, f);
      return m ? (u.add(m), i.get(m) || d) : d;
    }), s.parts.forEach((d, f) => {
      const m = b_(d, f);
      !m || u.has(m) || (a.parts.push(i.get(m) || Jd(d)), u.add(m));
    });
  }
  const o = String(t || ""), l = a.parts.filter((i) => !(typeof i?.text == "string" && !i?.thought));
  return a.parts = o ? [{ text: o }, ...l] : l, a.parts.length ? a : n[n.length - 1];
}
function tI(e) {
  const t = e?.candidates?.[0]?.content?.parts || [], n = t.filter((r) => !r?.thought && typeof r?.text == "string" && r.text).map((r) => r.text).join(`
`);
  return n || t.length ? n : typeof e?.text == "string" && e.text ? e.text : "";
}
function nI(e) {
  const t = Array.isArray(e?.functionCalls) ? e.functionCalls : [], n = (e?.candidates?.[0]?.content?.parts || []).map((r) => r?.functionCall || r).filter((r) => r && r.name);
  return (t.length ? t : n).map((r, s) => ({
    id: r.id || `google-tool-${s + 1}`,
    name: r.name || "",
    arguments: JSON.stringify(r.args || {})
  })).filter((r) => r.name);
}
function Sj(e = [], t = []) {
  const n = Array.isArray(e) ? [...e] : [];
  return (Array.isArray(t) ? t : []).forEach((r) => {
    if (!r?.name) return;
    const s = [
      String(r.id || ""),
      String(r.name || ""),
      String(r.arguments || "")
    ].join("\0");
    n.some((a) => [
      String(a.id || ""),
      String(a.name || ""),
      String(a.arguments || "")
    ].join("\0") === s) || n.push(r);
  }), n;
}
function Aj(e = []) {
  return {
    role: "user",
    parts: e.filter((t) => t && t.name).map((t) => ({ functionResponse: {
      name: t.name,
      response: t.response || {}
    } }))
  };
}
function Tj(e) {
  switch (e) {
    case "high":
      return Vd.HIGH;
    case "medium":
      return Vd.MEDIUM;
    default:
      return Vd.LOW;
  }
}
function rI(e) {
  return (e?.candidates?.[0]?.content?.parts || []).filter((t) => t?.thought && typeof t.text == "string" && t.text.trim()).map((t, n) => ({
    label: `思考块 ${n + 1}`,
    text: t.text.trim()
  }));
}
function Ej(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  if (t.length)
    return [...new Set(t)].join(`

`);
}
function Cj(e) {
  const t = e?.providerPayload?.googleContent;
  return Jf(t, "model");
}
function Ij(e) {
  const t = e?.providerPayload?.googleContents;
  if (!Array.isArray(t) || !t.length) {
    const n = Cj(e);
    return n ? [n] : [];
  }
  return t.map((n) => Jf(n, "model")).filter(Boolean);
}
function Qw(e = []) {
  const t = (Array.isArray(e) ? e : []).map((n) => Jf(n, "model")).filter(Boolean);
  if (t.length)
    return {
      googleContent: t[t.length - 1],
      googleContents: t
    };
}
function xj(e) {
  const t = e?.candidates?.[0]?.content;
  return Qw(t ? [t] : []);
}
function kj(e) {
  return Qw(e ? [e] : []);
}
function nR(e) {
  try {
    if (typeof e?.getHistory == "function") return e.getHistory(!1);
  } catch {
    return [];
  }
  return Array.isArray(e?.history) ? Jd(e.history) || [] : [];
}
function Mj(e, t = 0) {
  return nR(e).slice(Math.max(0, t)).filter((n) => n?.role === "model").map((n) => Jf(n, "model")).filter(Boolean);
}
function Pj(e) {
  const t = /* @__PURE__ */ new Map(), n = [], r = (e || []).filter((a) => a.role === "user" || a.role === "assistant" || a.role === "tool");
  r.forEach((a) => {
    (a.tool_calls || []).forEach((o) => {
      o.id && o.function?.name && t.set(o.id, o.function.name);
    });
  });
  for (let a = 0; a < r.length; a += 1) {
    const o = r[a];
    if (o.role === "tool") {
      const l = [];
      let i = a;
      for (; i < r.length && r[i].role === "tool"; ) {
        const u = r[i];
        l.push({ functionResponse: {
          name: String(u.toolName || u.tool_name || "").trim() || t.get(u.tool_call_id || "") || "tool_result",
          response: ZC(u.content)
        } }), i += 1;
      }
      n.push({
        role: "user",
        parts: l
      }), a = i - 1;
      continue;
    }
    if (o.role === "assistant") {
      const l = Ij(o);
      if (l.length) {
        n.push(...l);
        continue;
      }
    }
    if (o.role === "assistant" && Array.isArray(o.tool_calls) && o.tool_calls.length) {
      n.push({
        role: "model",
        parts: [...o.content ? [cl(o.content)] : [], ...o.tool_calls.map((l) => ({ functionCall: {
          name: l.function.name,
          args: ZC(l.function.arguments)
        } }))]
      });
      continue;
    }
    n.push({
      role: o.role === "assistant" ? "model" : "user",
      parts: yj(o.content)
    });
  }
  if (!n.length) return {
    history: [],
    latestMessage: eI().parts
  };
  const s = n[n.length - 1];
  return s.role === "user" && s.parts?.length ? {
    history: n.slice(0, -1),
    latestMessage: s.parts
  } : {
    history: n,
    latestMessage: eI().parts
  };
}
function Rj(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function sI(e, t) {
  const n = String(t || ""), r = String(e || "");
  return n ? !r || n.startsWith(r) ? n : r.endsWith(n) ? r : `${r}${n}` : r;
}
var Nj = class {
  constructor(e) {
    this.config = e, this.supportsSessionToolLoop = !0, this.activeChat = null, this.client = new gj({
      apiKey: e.apiKey,
      httpOptions: {
        baseUrl: String(e.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, ""),
        timeout: Number(e.timeoutMs) || 900 * 1e3
      }
    });
  }
  buildChatPayload(e) {
    const t = Pj(e.messages), n = Array.isArray(e.tools) ? e.tools : [], r = Ej(e), s = {
      ...r ? { systemInstruction: r } : {},
      temperature: e.temperature,
      ...e.maxTokens ? { maxOutputTokens: e.maxTokens } : {}
    };
    return e.reasoning?.enabled && (s.thinkingConfig = {
      includeThoughts: !0,
      thinkingLevel: Tj(e.reasoning.effort)
    }), n.length && (s.tools = [{ functionDeclarations: n.map((a) => ({
      name: a.function.name,
      description: a.function.description,
      parameters: a.function.parameters
    })) }]), n.length && e.toolChoice && e.toolChoice !== "auto" && e.toolChoice !== "none" && (s.toolConfig = { functionCallingConfig: { mode: f0.ANY } }), {
      createPayload: {
        model: this.config.model,
        history: t.history,
        config: s
      },
      sendPayload: { message: t.latestMessage }
    };
  }
  inspectRequest(e, t = {}) {
    const n = t.payload || this.buildChatPayload(e), r = String(this.config.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, "");
    return Cf({
      provider: "google",
      model: this.config.model,
      transport: "google-genai-sdk",
      url: `${r}/models/${encodeURIComponent(this.config.model || "")}:generateContent`,
      headers: {
        "Content-Type": "application/json",
        "x-goog-api-key": this.config.apiKey || ""
      },
      body: {
        chatCreate: n.createPayload,
        sendMessage: n.sendPayload,
        stream: typeof e.onStreamProgress == "function"
      },
      sdk: typeof e.onStreamProgress == "function" ? "client.chats.create(...).sendMessageStream" : "client.chats.create(...).sendMessage"
    });
  }
  inspectSendRequest(e, t) {
    const n = String(this.config.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, "");
    return Cf({
      provider: "google",
      model: this.config.model,
      transport: "google-genai-sdk",
      url: `${n}/models/${encodeURIComponent(this.config.model || "")}:generateContent`,
      headers: {
        "Content-Type": "application/json",
        "x-goog-api-key": this.config.apiKey || ""
      },
      body: {
        sendMessage: e,
        stream: typeof t.onStreamProgress == "function"
      },
      sdk: typeof t.onStreamProgress == "function" ? "activeChat.sendMessageStream" : "activeChat.sendMessage"
    });
  }
  createChat(e) {
    const t = this.buildChatPayload(e);
    return {
      chat: this.client.chats.create(t.createPayload),
      sendPayload: t.sendPayload,
      requestInspection: this.inspectRequest(e, { payload: t })
    };
  }
  async sendThroughChat(e, t, n) {
    let r, s, a, o = [], l = null;
    const i = { ...t }, u = typeof n.onStreamProgress == "function", d = nR(e).length;
    if (u) {
      const y = await e.sendMessageStream(i), S = /* @__PURE__ */ new Map();
      let x = "", I = [], E = null;
      const N = [];
      for await (const $ of y) {
        E = $;
        const p = $?.candidates?.[0]?.content;
        p?.parts?.length && N.push(p), rI($).forEach((_, b) => {
          const C = `${_.label}:${b}`;
          S.set(C, sI(S.get(C) || "", _.text));
        }), I = ($.functionCalls || []).map((_, b) => ({
          id: _.id || `google-tool-${b + 1}`,
          name: _.name || "",
          arguments: JSON.stringify(_.args || {})
        })).filter((_) => _.name), o = Sj(o, I.length ? I : nI($));
        const w = tI($);
        x = sI(x, w), Rj(n, {
          text: x,
          thoughts: Array.from(S.values()).filter(Boolean).map((_, b) => ({
            label: `思考块 ${b + 1}`,
            text: _
          })),
          ...I.length ? {
            toolCalls: I,
            toolCallDraft: !0
          } : {}
        });
      }
      r = E || { functionCalls: I }, l = wj(N, x) || r?.candidates?.[0]?.content || null, s = Array.from(S.values()).filter(Boolean).map(($, p) => ({
        label: `思考块 ${p + 1}`,
        text: $
      })), a = x;
    } else
      r = await e.sendMessage(i), s = rI(r), a = tI(r);
    const f = nI(r), m = f.length ? f : o, v = Mj(e, d);
    return {
      text: a,
      toolCalls: m,
      thoughts: s,
      finishReason: r.candidates?.[0]?.finishReason || "STOP",
      model: r.modelVersion || this.config.model,
      provider: "google",
      providerPayload: Qw(v) || kj(l) || xj(r)
    };
  }
  async chat(e) {
    if (Array.isArray(e.toolResponses) && e.toolResponses.length) {
      if (!this.activeChat) throw new Error("google_chat_session_missing");
      const r = { message: Aj(e.toolResponses) };
      return {
        ...await this.sendThroughChat(this.activeChat, r, e),
        requestInspection: this.inspectSendRequest(r, e)
      };
    }
    const t = String(e.finalAnswerReminderText || "").trim();
    if (t) {
      if (!this.activeChat) throw new Error("google_chat_session_missing");
      const r = { message: [cl(t)] };
      return {
        ...await this.sendThroughChat(this.activeChat, r, e),
        requestInspection: this.inspectSendRequest(r, e)
      };
    }
    const n = this.createChat(e);
    return this.activeChat = n.chat, {
      ...await this.sendThroughChat(this.activeChat, n.sendPayload, e),
      requestInspection: n.requestInspection
    };
  }
};
function At(e, t, n, r, s) {
  if (r === "m") throw new TypeError("Private method is not writable");
  if (r === "a" && !s) throw new TypeError("Private accessor was defined without a setter");
  if (typeof t == "function" ? e !== t || !s : !t.has(e)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return r === "a" ? s.call(e, n) : s ? s.value = n : t.set(e, n), n;
}
function xe(e, t, n, r) {
  if (n === "a" && !r) throw new TypeError("Private accessor was defined without a getter");
  if (typeof t == "function" ? e !== t || !r : !t.has(e)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return n === "m" ? r : n === "a" ? r.call(e) : r ? r.value : t.get(e);
}
var rR = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return rR = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
};
function x0(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var k0 = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, gt = class extends Error {
}, Mr = class M0 extends gt {
  constructor(t, n, r, s) {
    super(`${M0.makeMessage(t, n, r)}`), this.status = t, this.headers = s, this.requestID = s?.get("x-request-id"), this.error = n;
    const a = n;
    this.code = a?.code, this.param = a?.param, this.type = a?.type;
  }
  static makeMessage(t, n, r) {
    const s = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && s ? `${t} ${s}` : t ? `${t} status code (no body)` : s || "(no status code or body)";
  }
  static generate(t, n, r, s) {
    if (!t || !s) return new pv({
      message: r,
      cause: k0(n)
    });
    const a = n?.error;
    return t === 400 ? new sR(t, a, r, s) : t === 401 ? new aR(t, a, r, s) : t === 403 ? new oR(t, a, r, s) : t === 404 ? new iR(t, a, r, s) : t === 409 ? new lR(t, a, r, s) : t === 422 ? new uR(t, a, r, s) : t === 429 ? new cR(t, a, r, s) : t >= 500 ? new dR(t, a, r, s) : new M0(t, a, r, s);
  }
}, Bs = class extends Mr {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, pv = class extends Mr {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, Zw = class extends pv {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, sR = class extends Mr {
}, aR = class extends Mr {
}, oR = class extends Mr {
}, iR = class extends Mr {
}, lR = class extends Mr {
}, uR = class extends Mr {
}, cR = class extends Mr {
}, dR = class extends Mr {
}, fR = class extends gt {
  constructor() {
    super("Could not parse response content as the length limit was reached");
  }
}, hR = class extends gt {
  constructor() {
    super("Could not parse response content as the request was rejected by the content filter");
  }
}, wd = class extends Error {
  constructor(e) {
    super(e);
  }
}, pR = class extends Mr {
  constructor(e, t, n) {
    let r = "OAuth2 authentication error", s;
    if (t && typeof t == "object") {
      const a = t;
      s = a.error;
      const o = a.error_description;
      o && typeof o == "string" ? r = o : s && (r = s);
    }
    super(e, t, r, n), this.error_code = s;
  }
}, $j = class extends gt {
  constructor(e, t, n) {
    super(e), this.provider = t, this.cause = n;
  }
}, Lj = /^[a-z][a-z0-9+.-]*:/i, Dj = (e) => Lj.test(e), Jr = (e) => (Jr = Array.isArray, Jr(e)), aI = Jr;
function eS(e) {
  return typeof e != "object" ? {} : e ?? {};
}
function oI(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function Oj(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
function w_(e) {
  return e != null && typeof e == "object" && !Array.isArray(e);
}
var Fj = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new gt(`${e} must be an integer`);
  if (t < 0) throw new gt(`${e} must be a positive integer`);
  return t;
}, Bj = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, Yf = (e) => new Promise((t) => setTimeout(t, e)), pu = "6.44.0", Uj = () => typeof window < "u" && typeof window.document < "u" && typeof navigator < "u";
function qj() {
  return typeof Deno < "u" && Deno.build != null ? "deno" : typeof EdgeRuntime < "u" ? "edge" : Object.prototype.toString.call(typeof globalThis.process < "u" ? globalThis.process : 0) === "[object process]" ? "node" : "unknown";
}
var Hj = () => {
  const e = qj();
  if (e === "deno") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": pu,
    "X-Stainless-OS": lI(Deno.build.os),
    "X-Stainless-Arch": iI(Deno.build.arch),
    "X-Stainless-Runtime": "deno",
    "X-Stainless-Runtime-Version": typeof Deno.version == "string" ? Deno.version : Deno.version?.deno ?? "unknown"
  };
  if (typeof EdgeRuntime < "u") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": pu,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": `other:${EdgeRuntime}`,
    "X-Stainless-Runtime": "edge",
    "X-Stainless-Runtime-Version": globalThis.process.version
  };
  if (e === "node") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": pu,
    "X-Stainless-OS": lI(globalThis.process.platform ?? "unknown"),
    "X-Stainless-Arch": iI(globalThis.process.arch ?? "unknown"),
    "X-Stainless-Runtime": "node",
    "X-Stainless-Runtime-Version": globalThis.process.version ?? "unknown"
  };
  const t = zj();
  return t ? {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": pu,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": `browser:${t.browser}`,
    "X-Stainless-Runtime-Version": t.version
  } : {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": pu,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": "unknown",
    "X-Stainless-Runtime-Version": "unknown"
  };
};
function zj() {
  if (typeof navigator > "u" || !navigator) return null;
  for (const { key: e, pattern: t } of [
    {
      key: "edge",
      pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "chrome",
      pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "firefox",
      pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "safari",
      pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/
    }
  ]) {
    const n = t.exec(navigator.userAgent);
    if (n) return {
      browser: e,
      version: `${n[1] || 0}.${n[2] || 0}.${n[3] || 0}`
    };
  }
  return null;
}
var iI = (e) => e === "x32" ? "x32" : e === "x86_64" || e === "x64" ? "x64" : e === "arm" ? "arm" : e === "aarch64" || e === "arm64" ? "arm64" : e ? `other:${e}` : "unknown", lI = (e) => (e = e.toLowerCase(), e.includes("ios") ? "iOS" : e === "android" ? "Android" : e === "darwin" ? "MacOS" : e === "win32" ? "Windows" : e === "freebsd" ? "FreeBSD" : e === "openbsd" ? "OpenBSD" : e === "linux" ? "Linux" : e ? `Other:${e}` : "Unknown"), uI, Kj = () => uI ?? (uI = Hj());
function mR() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new OpenAI({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function gR(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function vR(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return gR({
    start() {
    },
    async pull(n) {
      const { done: r, value: s } = await t.next();
      r ? n.close() : n.enqueue(s);
    },
    async cancel() {
      await t.return?.();
    }
  });
}
function yR(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function cI(e) {
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await e[Symbol.asyncIterator]().return?.();
    return;
  }
  const t = e.getReader(), n = t.cancel();
  t.releaseLock(), await n;
}
var Wj = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
}), _R = "RFC3986", bR = (e) => String(e), dI = {
  RFC1738: (e) => String(e).replace(/%20/g, "+"),
  RFC3986: bR
};
var P0 = (e, t) => (P0 = Object.hasOwn ?? Function.prototype.call.bind(Object.prototype.hasOwnProperty), P0(e, t)), Aa = /* @__PURE__ */ (() => {
  const e = [];
  for (let t = 0; t < 256; ++t) e.push("%" + ((t < 16 ? "0" : "") + t.toString(16)).toUpperCase());
  return e;
})(), S_ = 1024, Gj = (e, t, n, r, s) => {
  if (e.length === 0) return e;
  let a = e;
  if (typeof e == "symbol" ? a = Symbol.prototype.toString.call(e) : typeof e != "string" && (a = String(e)), n === "iso-8859-1") return escape(a).replace(/%u[0-9a-f]{4}/gi, function(l) {
    return "%26%23" + parseInt(l.slice(2), 16) + "%3B";
  });
  let o = "";
  for (let l = 0; l < a.length; l += S_) {
    const i = a.length >= S_ ? a.slice(l, l + S_) : a, u = [];
    for (let d = 0; d < i.length; ++d) {
      let f = i.charCodeAt(d);
      if (f === 45 || f === 46 || f === 95 || f === 126 || f >= 48 && f <= 57 || f >= 65 && f <= 90 || f >= 97 && f <= 122 || s === "RFC1738" && (f === 40 || f === 41)) {
        u[u.length] = i.charAt(d);
        continue;
      }
      if (f < 128) {
        u[u.length] = Aa[f];
        continue;
      }
      if (f < 2048) {
        u[u.length] = Aa[192 | f >> 6] + Aa[128 | f & 63];
        continue;
      }
      if (f < 55296 || f >= 57344) {
        u[u.length] = Aa[224 | f >> 12] + Aa[128 | f >> 6 & 63] + Aa[128 | f & 63];
        continue;
      }
      d += 1, f = 65536 + ((f & 1023) << 10 | i.charCodeAt(d) & 1023), u[u.length] = Aa[240 | f >> 18] + Aa[128 | f >> 12 & 63] + Aa[128 | f >> 6 & 63] + Aa[128 | f & 63];
    }
    o += u.join("");
  }
  return o;
};
function Vj(e) {
  return !e || typeof e != "object" ? !1 : !!(e.constructor && e.constructor.isBuffer && e.constructor.isBuffer(e));
}
function fI(e, t) {
  if (Jr(e)) {
    const n = [];
    for (let r = 0; r < e.length; r += 1) n.push(t(e[r]));
    return n;
  }
  return t(e);
}
var wR = {
  brackets(e) {
    return String(e) + "[]";
  },
  comma: "comma",
  indices(e, t) {
    return String(e) + "[" + t + "]";
  },
  repeat(e) {
    return String(e);
  }
}, SR = function(e, t) {
  Array.prototype.push.apply(e, Jr(t) ? t : [t]);
}, hI, Zn = {
  addQueryPrefix: !1,
  allowDots: !1,
  allowEmptyArrays: !1,
  arrayFormat: "indices",
  charset: "utf-8",
  charsetSentinel: !1,
  delimiter: "&",
  encode: !0,
  encodeDotInKeys: !1,
  encoder: Gj,
  encodeValuesOnly: !1,
  format: _R,
  formatter: bR,
  indices: !1,
  serializeDate(e) {
    return (hI ?? (hI = Function.prototype.call.bind(Date.prototype.toISOString)))(e);
  },
  skipNulls: !1,
  strictNullHandling: !1
};
function Jj(e) {
  return typeof e == "string" || typeof e == "number" || typeof e == "boolean" || typeof e == "symbol" || typeof e == "bigint";
}
var A_ = {};
function AR(e, t, n, r, s, a, o, l, i, u, d, f, m, v, y, S, x, I) {
  let E = e, N = I, $ = 0, p = !1;
  for (; (N = N.get(A_)) !== void 0 && !p; ) {
    const k = N.get(e);
    if ($ += 1, typeof k < "u") {
      if (k === $) throw new RangeError("Cyclic object value");
      p = !0;
    }
    typeof N.get(A_) > "u" && ($ = 0);
  }
  if (typeof u == "function" ? E = u(t, E) : E instanceof Date ? E = m?.(E) : n === "comma" && Jr(E) && (E = fI(E, function(k) {
    return k instanceof Date ? m?.(k) : k;
  })), E === null) {
    if (a) return i && !S ? i(t, Zn.encoder, x, "key", v) : t;
    E = "";
  }
  if (Jj(E) || Vj(E)) {
    if (i) {
      const k = S ? t : i(t, Zn.encoder, x, "key", v);
      return [y?.(k) + "=" + y?.(i(E, Zn.encoder, x, "value", v))];
    }
    return [y?.(t) + "=" + y?.(String(E))];
  }
  const w = [];
  if (typeof E > "u") return w;
  let _;
  if (n === "comma" && Jr(E))
    S && i && (E = fI(E, i)), _ = [{ value: E.length > 0 ? E.join(",") || null : void 0 }];
  else if (Jr(u)) _ = u;
  else {
    const k = Object.keys(E);
    _ = d ? k.sort(d) : k;
  }
  const b = l ? String(t).replace(/\./g, "%2E") : String(t), C = r && Jr(E) && E.length === 1 ? b + "[]" : b;
  if (s && Jr(E) && E.length === 0) return C + "[]";
  for (let k = 0; k < _.length; ++k) {
    const P = _[k], H = typeof P == "object" && typeof P.value < "u" ? P.value : E[P];
    if (o && H === null) continue;
    const G = f && l ? P.replace(/\./g, "%2E") : P, ee = Jr(E) ? typeof n == "function" ? n(C, G) : C : C + (f ? "." + G : "[" + G + "]");
    I.set(e, $);
    const J = /* @__PURE__ */ new WeakMap();
    J.set(A_, I), SR(w, AR(H, ee, n, r, s, a, o, l, n === "comma" && S && Jr(E) ? null : i, u, d, f, m, v, y, S, x, J));
  }
  return w;
}
function Yj(e = Zn) {
  if (typeof e.allowEmptyArrays < "u" && typeof e.allowEmptyArrays != "boolean") throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
  if (typeof e.encodeDotInKeys < "u" && typeof e.encodeDotInKeys != "boolean") throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
  if (e.encoder !== null && typeof e.encoder < "u" && typeof e.encoder != "function") throw new TypeError("Encoder has to be a function.");
  const t = e.charset || Zn.charset;
  if (typeof e.charset < "u" && e.charset !== "utf-8" && e.charset !== "iso-8859-1") throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
  let n = _R;
  if (typeof e.format < "u") {
    if (!P0(dI, e.format)) throw new TypeError("Unknown format option provided.");
    n = e.format;
  }
  const r = dI[n];
  let s = Zn.filter;
  (typeof e.filter == "function" || Jr(e.filter)) && (s = e.filter);
  let a;
  if (e.arrayFormat && e.arrayFormat in wR ? a = e.arrayFormat : "indices" in e ? a = e.indices ? "indices" : "repeat" : a = Zn.arrayFormat, "commaRoundTrip" in e && typeof e.commaRoundTrip != "boolean") throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
  const o = typeof e.allowDots > "u" ? e.encodeDotInKeys ? !0 : Zn.allowDots : !!e.allowDots;
  return {
    addQueryPrefix: typeof e.addQueryPrefix == "boolean" ? e.addQueryPrefix : Zn.addQueryPrefix,
    allowDots: o,
    allowEmptyArrays: typeof e.allowEmptyArrays == "boolean" ? !!e.allowEmptyArrays : Zn.allowEmptyArrays,
    arrayFormat: a,
    charset: t,
    charsetSentinel: typeof e.charsetSentinel == "boolean" ? e.charsetSentinel : Zn.charsetSentinel,
    commaRoundTrip: !!e.commaRoundTrip,
    delimiter: typeof e.delimiter > "u" ? Zn.delimiter : e.delimiter,
    encode: typeof e.encode == "boolean" ? e.encode : Zn.encode,
    encodeDotInKeys: typeof e.encodeDotInKeys == "boolean" ? e.encodeDotInKeys : Zn.encodeDotInKeys,
    encoder: typeof e.encoder == "function" ? e.encoder : Zn.encoder,
    encodeValuesOnly: typeof e.encodeValuesOnly == "boolean" ? e.encodeValuesOnly : Zn.encodeValuesOnly,
    filter: s,
    format: n,
    formatter: r,
    serializeDate: typeof e.serializeDate == "function" ? e.serializeDate : Zn.serializeDate,
    skipNulls: typeof e.skipNulls == "boolean" ? e.skipNulls : Zn.skipNulls,
    sort: typeof e.sort == "function" ? e.sort : null,
    strictNullHandling: typeof e.strictNullHandling == "boolean" ? e.strictNullHandling : Zn.strictNullHandling
  };
}
function Xj(e, t = {}) {
  let n = e;
  const r = Yj(t);
  let s, a;
  typeof r.filter == "function" ? (a = r.filter, n = a("", n)) : Jr(r.filter) && (a = r.filter, s = a);
  const o = [];
  if (typeof n != "object" || n === null) return "";
  const l = wR[r.arrayFormat], i = l === "comma" && r.commaRoundTrip;
  s || (s = Object.keys(n)), r.sort && s.sort(r.sort);
  const u = /* @__PURE__ */ new WeakMap();
  for (let m = 0; m < s.length; ++m) {
    const v = s[m];
    r.skipNulls && n[v] === null || SR(o, AR(n[v], v, l, i, r.allowEmptyArrays, r.strictNullHandling, r.skipNulls, r.encodeDotInKeys, r.encode ? r.encoder : null, r.filter, r.sort, r.allowDots, r.serializeDate, r.format, r.formatter, r.encodeValuesOnly, r.charset, u));
  }
  const d = o.join(r.delimiter);
  let f = r.addQueryPrefix === !0 ? "?" : "";
  return r.charsetSentinel && (r.charset === "iso-8859-1" ? f += "utf8=%26%2310003%3B&" : f += "utf8=%E2%9C%93&"), d.length > 0 ? f + d : "";
}
function jj(e) {
  return Xj(e, { arrayFormat: "brackets" });
}
function Qj(e) {
  let t = 0;
  for (const s of e) t += s.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const s of e)
    n.set(s, r), r += s.length;
  return n;
}
var pI;
function tS(e) {
  let t;
  return (pI ?? (t = new globalThis.TextEncoder(), pI = t.encode.bind(t)))(e);
}
var mI;
function gI(e) {
  let t;
  return (mI ?? (t = new globalThis.TextDecoder(), mI = t.decode.bind(t)))(e);
}
var ds, fs, mv = class {
  constructor() {
    ds.set(this, void 0), fs.set(this, void 0), At(this, ds, new Uint8Array(), "f"), At(this, fs, null, "f");
  }
  decode(e) {
    if (e == null) return [];
    const t = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? tS(e) : e;
    At(this, ds, Qj([xe(this, ds, "f"), t]), "f");
    const n = [];
    let r;
    for (; (r = Zj(xe(this, ds, "f"), xe(this, fs, "f"))) != null; ) {
      if (r.carriage && xe(this, fs, "f") == null) {
        At(this, fs, r.index, "f");
        continue;
      }
      if (xe(this, fs, "f") != null && (r.index !== xe(this, fs, "f") + 1 || r.carriage)) {
        n.push(gI(xe(this, ds, "f").subarray(0, xe(this, fs, "f") - 1))), At(this, ds, xe(this, ds, "f").subarray(xe(this, fs, "f")), "f"), At(this, fs, null, "f");
        continue;
      }
      const s = xe(this, fs, "f") !== null ? r.preceding - 1 : r.preceding, a = gI(xe(this, ds, "f").subarray(0, s));
      n.push(a), At(this, ds, xe(this, ds, "f").subarray(r.index), "f"), At(this, fs, null, "f");
    }
    return n;
  }
  flush() {
    return xe(this, ds, "f").length ? this.decode(`
`) : [];
  }
};
ds = /* @__PURE__ */ new WeakMap(), fs = /* @__PURE__ */ new WeakMap();
mv.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
mv.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function Zj(e, t) {
  for (let s = t ?? 0; s < e.length; s++) {
    if (e[s] === 10) return {
      preceding: s,
      index: s + 1,
      carriage: !1
    };
    if (e[s] === 13) return {
      preceding: s,
      index: s + 1,
      carriage: !0
    };
  }
  return null;
}
function eQ(e) {
  for (let r = 0; r < e.length - 1; r++) {
    if (e[r] === 10 && e[r + 1] === 10 || e[r] === 13 && e[r + 1] === 13) return r + 2;
    if (e[r] === 13 && e[r + 1] === 10 && r + 3 < e.length && e[r + 2] === 13 && e[r + 3] === 10) return r + 4;
  }
  return -1;
}
var ug = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, vI = (e, t, n) => {
  if (e) {
    if (Oj(ug, e)) return e;
    pr(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(ug))}`);
  }
};
function Sd() {
}
function mp(e, t, n) {
  return !t || ug[e] > ug[n] ? Sd : t[e].bind(t);
}
var tQ = {
  error: Sd,
  warn: Sd,
  info: Sd,
  debug: Sd
}, yI = /* @__PURE__ */ new WeakMap();
function pr(e) {
  const t = e.logger, n = e.logLevel ?? "off";
  if (!t) return tQ;
  const r = yI.get(t);
  if (r && r[0] === n) return r[1];
  const s = {
    error: mp("error", t, n),
    warn: mp("warn", t, n),
    info: mp("info", t, n),
    debug: mp("debug", t, n)
  };
  return yI.set(t, [n, s]), s;
}
var Zi = (e) => (e.options && (e.options = { ...e.options }, delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "authorization" || t.toLowerCase() === "api-key" || t.toLowerCase() === "x-api-key" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), sd, If = class Ad {
  constructor(t, n, r) {
    this.iterator = t, sd.set(this, void 0), this.controller = n, At(this, sd, r, "f");
  }
  static fromSSEResponse(t, n, r, s) {
    let a = !1;
    const o = r ? pr(r) : console;
    async function* l() {
      if (a) throw new gt("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      a = !0;
      let i = !1;
      try {
        for await (const u of nQ(t, n))
          if (!i) {
            if (u.data.startsWith("[DONE]")) {
              i = !0;
              continue;
            }
            if (u.event === null || !u.event.startsWith("thread.")) {
              let d;
              try {
                d = JSON.parse(u.data);
              } catch (f) {
                throw o.error("Could not parse message into JSON:", u.data), o.error("From chunk:", u.raw), f;
              }
              if (d && d.error) throw new Mr(void 0, d.error, void 0, t.headers);
              yield s ? {
                event: u.event,
                data: d
              } : d;
            } else {
              let d;
              try {
                d = JSON.parse(u.data);
              } catch (f) {
                throw console.error("Could not parse message into JSON:", u.data), console.error("From chunk:", u.raw), f;
              }
              if (u.event == "error") throw new Mr(void 0, d.error, d.message, void 0);
              yield {
                event: u.event,
                data: d
              };
            }
          }
        i = !0;
      } catch (u) {
        if (x0(u)) return;
        throw u;
      } finally {
        i || n.abort();
      }
    }
    return new Ad(l, n, r);
  }
  static fromReadableStream(t, n, r) {
    let s = !1;
    async function* a() {
      const l = new mv(), i = yR(t);
      for await (const u of i) for (const d of l.decode(u)) yield d;
      for (const u of l.flush()) yield u;
    }
    async function* o() {
      if (s) throw new gt("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      s = !0;
      let l = !1;
      try {
        for await (const i of a())
          l || i && (yield JSON.parse(i));
        l = !0;
      } catch (i) {
        if (x0(i)) return;
        throw i;
      } finally {
        l || n.abort();
      }
    }
    return new Ad(o, n, r);
  }
  [(sd = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), s = (a) => ({ next: () => {
      if (a.length === 0) {
        const o = r.next();
        t.push(o), n.push(o);
      }
      return a.shift();
    } });
    return [new Ad(() => s(t), this.controller, xe(this, sd, "f")), new Ad(() => s(n), this.controller, xe(this, sd, "f"))];
  }
  toReadableStream() {
    const t = this;
    let n;
    return gR({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: s, done: a } = await n.next();
          if (a) return r.close();
          const o = tS(JSON.stringify(s) + `
`);
          r.enqueue(o);
        } catch (s) {
          r.error(s);
        }
      },
      async cancel() {
        await n.return?.();
      }
    });
  }
};
async function* nQ(e, t) {
  if (!e.body)
    throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new gt("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new gt("Attempted to iterate over a response with no body");
  const n = new sQ(), r = new mv(), s = yR(e.body);
  for await (const a of rQ(s)) for (const o of r.decode(a)) {
    const l = n.decode(o);
    l && (yield l);
  }
  for (const a of r.flush()) {
    const o = n.decode(a);
    o && (yield o);
  }
}
async function* rQ(e) {
  let t = new Uint8Array();
  for await (const n of e) {
    if (n == null) continue;
    const r = n instanceof ArrayBuffer ? new Uint8Array(n) : typeof n == "string" ? tS(n) : n;
    let s = new Uint8Array(t.length + r.length);
    s.set(t), s.set(r, t.length), t = s;
    let a;
    for (; (a = eQ(t)) !== -1; )
      yield t.slice(0, a), t = t.slice(a);
  }
  t.length > 0 && (yield t);
}
var sQ = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const s = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], s;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = aQ(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function aQ(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function TR(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: s, startTime: a } = t, o = await (async () => {
    if (t.options.stream)
      return pr(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller, e, t.options.__synthesizeEventData) : If.fromSSEResponse(n, t.controller, e, t.options.__synthesizeEventData);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const l = n.headers.get("content-type")?.split(";")[0]?.trim();
    return l?.includes("application/json") || l?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : ER(await n.json(), n) : await n.text();
  })();
  return pr(e).debug(`[${r}] response parsed`, Zi({
    retryOfRequestLogID: s,
    url: n.url,
    status: n.status,
    body: o,
    durationMs: Date.now() - a
  })), o;
}
function ER(e, t) {
  return !e || typeof e != "object" || Array.isArray(e) ? e : Object.defineProperty(e, "_request_id", {
    value: t.headers.get("x-request-id"),
    enumerable: !1
  });
}
var Td, CR = class IR extends Promise {
  constructor(t, n, r = TR) {
    super((s) => {
      s(null);
    }), this.responsePromise = n, this.parseResponse = r, Td.set(this, void 0), At(this, Td, t, "f");
  }
  _thenUnwrap(t) {
    return new IR(xe(this, Td, "f"), this.responsePromise, async (n, r) => ER(t(await this.parseResponse(n, r), r), r.response));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n,
      request_id: n.headers.get("x-request-id")
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(xe(this, Td, "f"), t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
};
Td = /* @__PURE__ */ new WeakMap();
var gp, gv = class {
  constructor(e, t, n, r) {
    gp.set(this, void 0), At(this, gp, e, "f"), this.options = r, this.response = t, this.body = n;
  }
  hasNextPage() {
    return this.getPaginatedItems().length ? this.nextPageRequestOptions() != null : !1;
  }
  async getNextPage() {
    const e = this.nextPageRequestOptions();
    if (!e) throw new gt("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
    return await xe(this, gp, "f").requestAPIList(this.constructor, e);
  }
  async *iterPages() {
    let e = this;
    for (yield e; e.hasNextPage(); )
      e = await e.getNextPage(), yield e;
  }
  async *[(gp = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    for await (const e of this.iterPages()) for (const t of e.getPaginatedItems()) yield t;
  }
}, oQ = class extends CR {
  constructor(e, t, n) {
    super(e, t, async (r, s) => new n(r, s.response, await TR(r, s), s.options));
  }
  async *[Symbol.asyncIterator]() {
    const e = await this;
    for await (const t of e) yield t;
  }
}, Si = class extends gv {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.object = n.object;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  nextPageRequestOptions() {
    return null;
  }
}, Mn = class extends gv {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    const e = this.getPaginatedItems(), t = e[e.length - 1]?.id;
    return t ? {
      ...this.options,
      query: {
        ...eS(this.options.query),
        after: t
      }
    } : null;
  }
}, _r = class extends gv {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1, this.last_id = n.last_id || "";
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    const e = this.last_id;
    return e ? {
      ...this.options,
      query: {
        ...eS(this.options.query),
        after: e
      }
    } : null;
  }
}, No = class extends gv {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1, this.next = n.next || null;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    const e = this.next;
    return e ? {
      ...this.options,
      query: {
        ...eS(this.options.query),
        after: e
      }
    } : null;
  }
}, iQ = {
  jwt: "urn:ietf:params:oauth:token-type:jwt",
  id: "urn:ietf:params:oauth:token-type:id_token"
}, lQ = "urn:ietf:params:oauth:grant-type:token-exchange", uQ = class {
  constructor(e, t) {
    this.cachedToken = null, this.refreshPromise = null, this.tokenExchangeUrl = "https://auth.openai.com/oauth/token", this.config = e, this.fetch = t ?? mR();
  }
  async getToken() {
    if (!this.cachedToken || this.isTokenExpired(this.cachedToken)) {
      if (this.refreshPromise) return await this.refreshPromise;
      this.refreshPromise = this.refreshToken();
      try {
        return await this.refreshPromise;
      } finally {
        this.refreshPromise = null;
      }
    }
    return this.needsRefresh(this.cachedToken) && !this.refreshPromise && (this.refreshPromise = this.refreshToken().finally(() => {
      this.refreshPromise = null;
    })), this.cachedToken.token;
  }
  async refreshToken() {
    const e = {
      grant_type: lQ,
      subject_token: await this.config.provider.getToken(),
      subject_token_type: iQ[this.config.provider.tokenType],
      identity_provider_id: this.config.identityProviderId,
      service_account_id: this.config.serviceAccountId
    };
    this.config.clientId && (e.client_id = this.config.clientId);
    const t = await this.fetch(this.tokenExchangeUrl, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(e)
    });
    if (!t.ok) {
      const a = await t.text();
      let o;
      try {
        o = JSON.parse(a);
      } catch {
      }
      throw t.status === 400 || t.status === 401 || t.status === 403 ? new pR(t.status, o, t.headers) : Mr.generate(t.status, o, `Token exchange failed with status ${t.status}`, t.headers);
    }
    const n = await t.json(), r = n.expires_in || 3600, s = Date.now() + r * 1e3;
    return this.cachedToken = {
      token: n.access_token,
      expiresAt: s
    }, n.access_token;
  }
  isTokenExpired(e) {
    return Date.now() >= e.expiresAt;
  }
  needsRefresh(e) {
    const t = (this.config.refreshBufferSeconds ?? 1200) * 1e3;
    return Date.now() >= e.expiresAt - t;
  }
  invalidateToken() {
    this.cachedToken = null, this.refreshPromise = null;
  }
}, xR = () => {
  if (typeof File > "u") {
    const { process: e } = globalThis, t = typeof e?.versions?.node == "string" && parseInt(e.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (t ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function Yd(e, t, n) {
  return xR(), new File(e, t ?? "unknown_file", n);
}
function jp(e) {
  return (typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "").split(/[\\/]/).pop() || void 0;
}
var nS = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", vv = async (e, t) => R0(e.body) ? {
  ...e,
  body: await kR(e.body, t)
} : e, $a = async (e, t) => ({
  ...e,
  body: await kR(e.body, t)
}), _I = /* @__PURE__ */ new WeakMap();
function cQ(e) {
  const t = typeof e == "function" ? e : e.fetch, n = _I.get(t);
  if (n) return n;
  const r = (async () => {
    try {
      const s = "Response" in t ? t.Response : (await t("data:,")).constructor, a = new FormData();
      return a.toString() !== await new s(a).text();
    } catch {
      return !0;
    }
  })();
  return _I.set(t, r), r;
}
var kR = async (e, t) => {
  if (!await cQ(t)) throw new TypeError("The provided fetch function does not support file uploads with the current global FormData class.");
  const n = new FormData();
  return await Promise.all(Object.entries(e || {}).map(([r, s]) => N0(n, r, s))), n;
}, MR = (e) => e instanceof Blob && "name" in e, dQ = (e) => typeof e == "object" && e !== null && (e instanceof Response || nS(e) || MR(e)), R0 = (e) => {
  if (dQ(e)) return !0;
  if (Array.isArray(e)) return e.some(R0);
  if (e && typeof e == "object") {
    for (const t in e) if (R0(e[t])) return !0;
  }
  return !1;
}, N0 = async (e, t, n) => {
  if (n !== void 0) {
    if (n == null) throw new TypeError(`Received null for "${t}"; to pass null in FormData, you must use the string 'null'`);
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") e.append(t, String(n));
    else if (n instanceof Response) e.append(t, Yd([await n.blob()], jp(n)));
    else if (nS(n)) e.append(t, Yd([await new Response(vR(n)).blob()], jp(n)));
    else if (MR(n)) e.append(t, n, jp(n));
    else if (Array.isArray(n)) await Promise.all(n.map((r) => N0(e, t + "[]", r)));
    else if (typeof n == "object") await Promise.all(Object.entries(n).map(([r, s]) => N0(e, `${t}[${r}]`, s)));
    else throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${n} instead`);
  }
}, PR = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", fQ = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && PR(e), hQ = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function pQ(e, t, n) {
  if (xR(), e = await e, fQ(e))
    return e instanceof File ? e : Yd([await e.arrayBuffer()], e.name);
  if (hQ(e)) {
    const s = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), Yd(await $0(s), t, n);
  }
  const r = await $0(e);
  if (t || (t = jp(e)), !n?.type) {
    const s = r.find((a) => typeof a == "object" && "type" in a && a.type);
    typeof s == "string" && (n = {
      ...n,
      type: s
    });
  }
  return Yd(r, t, n);
}
async function $0(e) {
  let t = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) t.push(e);
  else if (PR(e)) t.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (nS(e)) for await (const n of e) t.push(...await $0(n));
  else {
    const n = e?.constructor?.name;
    throw new Error(`Unexpected data type: ${typeof e}${n ? `; constructor: ${n}` : ""}${mQ(e)}`);
  }
  return t;
}
function mQ(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var Ve = class {
  constructor(e) {
    this._client = e;
  }
};
function RR(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var bI = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), gQ = (e = RR) => function(n, ...r) {
  if (n.length === 1) return n[0];
  let s = !1;
  const a = [], o = n.reduce((d, f, m) => {
    /[?#]/.test(f) && (s = !0);
    const v = r[m];
    let y = (s ? encodeURIComponent : e)("" + v);
    return m !== r.length && (v == null || typeof v == "object" && v.toString === Object.getPrototypeOf(Object.getPrototypeOf(v.hasOwnProperty ?? bI) ?? bI)?.toString) && (y = v + "", a.push({
      start: d.length + f.length,
      length: y.length,
      error: `Value of type ${Object.prototype.toString.call(v).slice(8, -1)} is not a valid path parameter`
    })), d + f + (m === r.length ? "" : y);
  }, ""), l = o.split(/[?#]/, 1)[0], i = /(?<=^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = i.exec(l)) !== null; ) a.push({
    start: u.index,
    length: u[0].length,
    error: `Value "${u[0]}" can't be safely passed as a path parameter`
  });
  if (a.sort((d, f) => d.start - f.start), a.length > 0) {
    let d = 0;
    const f = a.reduce((m, v) => {
      const y = " ".repeat(v.start - d), S = "^".repeat(v.length);
      return d = v.start + v.length, m + y + S;
    }, "");
    throw new gt(`Path parameters result in path with invalid segments:
${a.map((m) => m.error).join(`
`)}
${o}
${f}`);
  }
  return o;
}, _e = /* @__PURE__ */ gQ(RR), NR = class extends Ve {
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/chat/completions/${e}/messages`, Mn, {
      query: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
};
function cg(e) {
  return e !== void 0 && "function" in e && e.function !== void 0;
}
function rS(e) {
  return e?.$brand === "auto-parseable-response-format";
}
function Xf(e) {
  return e?.$brand === "auto-parseable-tool";
}
function vQ(e, t) {
  return !t || !$R(t) ? {
    ...e,
    choices: e.choices.map((n) => (LR(n.message.tool_calls), {
      ...n,
      message: {
        ...n.message,
        parsed: null,
        ...n.message.tool_calls ? { tool_calls: n.message.tool_calls } : void 0
      }
    }))
  } : sS(e, t);
}
function sS(e, t) {
  const n = e.choices.map((r) => {
    if (r.finish_reason === "length") throw new fR();
    if (r.finish_reason === "content_filter") throw new hR();
    return LR(r.message.tool_calls), {
      ...r,
      message: {
        ...r.message,
        ...r.message.tool_calls ? { tool_calls: r.message.tool_calls?.map((s) => _Q(t, s)) ?? void 0 } : void 0,
        parsed: r.message.content && !r.message.refusal ? yQ(t, r.message.content) : null
      }
    };
  });
  return {
    ...e,
    choices: n
  };
}
function yQ(e, t) {
  return e.response_format?.type !== "json_schema" ? null : e.response_format?.type === "json_schema" ? "$parseRaw" in e.response_format ? e.response_format.$parseRaw(t) : JSON.parse(t) : null;
}
function _Q(e, t) {
  const n = e.tools?.find((r) => cg(r) && r.function?.name === t.function.name);
  return {
    ...t,
    function: {
      ...t.function,
      parsed_arguments: Xf(n) ? n.$parseRaw(t.function.arguments) : n?.function.strict ? JSON.parse(t.function.arguments) : null
    }
  };
}
function bQ(e, t) {
  if (!e || !("tools" in e) || !e.tools) return !1;
  const n = e.tools?.find((r) => cg(r) && r.function?.name === t.function.name);
  return cg(n) && (Xf(n) || n?.function.strict || !1);
}
function $R(e) {
  return rS(e.response_format) ? !0 : e.tools?.some((t) => Xf(t) || t.type === "function" && t.function.strict === !0) ?? !1;
}
function LR(e) {
  for (const t of e || []) if (t.type !== "function") throw new gt(`Currently only \`function\` tool calls are supported; Received \`${t.type}\``);
}
function wQ(e) {
  for (const t of e ?? []) {
    if (t.type !== "function") throw new gt(`Currently only \`function\` tool types support auto-parsing; Received \`${t.type}\``);
    if (t.function.strict !== !0) throw new gt(`The \`${t.function.name}\` tool is not marked with \`strict: true\`. Only strict function tools can be auto-parsed`);
  }
}
var dg = (e) => e?.role === "assistant", DR = (e) => e?.role === "tool", L0, Qp, Zp, Ed, Cd, em, Id, so, xd, fg, hg, mu, OR, aS = class {
  constructor() {
    L0.add(this), this.controller = new AbortController(), Qp.set(this, void 0), Zp.set(this, () => {
    }), Ed.set(this, () => {
    }), Cd.set(this, void 0), em.set(this, () => {
    }), Id.set(this, () => {
    }), so.set(this, {}), xd.set(this, !1), fg.set(this, !1), hg.set(this, !1), mu.set(this, !1), At(this, Qp, new Promise((e, t) => {
      At(this, Zp, e, "f"), At(this, Ed, t, "f");
    }), "f"), At(this, Cd, new Promise((e, t) => {
      At(this, em, e, "f"), At(this, Id, t, "f");
    }), "f"), xe(this, Qp, "f").catch(() => {
    }), xe(this, Cd, "f").catch(() => {
    });
  }
  _run(e) {
    setTimeout(() => {
      e().then(() => {
        this._emitFinal(), this._emit("end");
      }, xe(this, L0, "m", OR).bind(this));
    }, 0);
  }
  _connected() {
    this.ended || (xe(this, Zp, "f").call(this), this._emit("connect"));
  }
  get ended() {
    return xe(this, xd, "f");
  }
  get errored() {
    return xe(this, fg, "f");
  }
  get aborted() {
    return xe(this, hg, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(e, t) {
    return (xe(this, so, "f")[e] || (xe(this, so, "f")[e] = [])).push({ listener: t }), this;
  }
  off(e, t) {
    const n = xe(this, so, "f")[e];
    if (!n) return this;
    const r = n.findIndex((s) => s.listener === t);
    return r >= 0 && n.splice(r, 1), this;
  }
  once(e, t) {
    return (xe(this, so, "f")[e] || (xe(this, so, "f")[e] = [])).push({
      listener: t,
      once: !0
    }), this;
  }
  emitted(e) {
    return new Promise((t, n) => {
      At(this, mu, !0, "f"), e !== "error" && this.once("error", n), this.once(e, t);
    });
  }
  async done() {
    At(this, mu, !0, "f"), await xe(this, Cd, "f");
  }
  _emit(e, ...t) {
    if (xe(this, xd, "f")) return;
    e === "end" && (At(this, xd, !0, "f"), xe(this, em, "f").call(this));
    const n = xe(this, so, "f")[e];
    if (n && (xe(this, so, "f")[e] = n.filter((r) => !r.once), n.forEach(({ listener: r }) => r(...t))), e === "abort") {
      const r = t[0];
      !xe(this, mu, "f") && !n?.length && Promise.reject(r), xe(this, Ed, "f").call(this, r), xe(this, Id, "f").call(this, r), this._emit("end");
      return;
    }
    if (e === "error") {
      const r = t[0];
      !xe(this, mu, "f") && !n?.length && Promise.reject(r), xe(this, Ed, "f").call(this, r), xe(this, Id, "f").call(this, r), this._emit("end");
    }
  }
  _emitFinal() {
  }
};
Qp = /* @__PURE__ */ new WeakMap(), Zp = /* @__PURE__ */ new WeakMap(), Ed = /* @__PURE__ */ new WeakMap(), Cd = /* @__PURE__ */ new WeakMap(), em = /* @__PURE__ */ new WeakMap(), Id = /* @__PURE__ */ new WeakMap(), so = /* @__PURE__ */ new WeakMap(), xd = /* @__PURE__ */ new WeakMap(), fg = /* @__PURE__ */ new WeakMap(), hg = /* @__PURE__ */ new WeakMap(), mu = /* @__PURE__ */ new WeakMap(), L0 = /* @__PURE__ */ new WeakSet(), OR = function(t) {
  if (At(this, fg, !0, "f"), t instanceof Error && t.name === "AbortError" && (t = new Bs()), t instanceof Bs)
    return At(this, hg, !0, "f"), this._emit("abort", t);
  if (t instanceof gt) return this._emit("error", t);
  if (t instanceof Error) {
    const n = new gt(t.message);
    return n.cause = t, this._emit("error", n);
  }
  return this._emit("error", new gt(String(t)));
};
function SQ(e) {
  return typeof e.parse == "function";
}
var Lr, D0, pg, O0, F0, B0, FR, BR, AQ = 10, UR = class extends aS {
  constructor() {
    super(...arguments), Lr.add(this), this._chatCompletions = [], this.messages = [];
  }
  _addChatCompletion(e) {
    this._chatCompletions.push(e), this._emit("chatCompletion", e);
    const t = e.choices[0]?.message;
    return t && this._addMessage(t), e;
  }
  _addMessage(e, t = !0) {
    if ("content" in e || (e.content = null), this.messages.push(e), t) {
      if (this._emit("message", e), DR(e) && e.content) this._emit("functionToolCallResult", e.content);
      else if (dg(e) && e.tool_calls)
        for (const n of e.tool_calls) n.type === "function" && this._emit("functionToolCall", n.function);
    }
  }
  async finalChatCompletion() {
    await this.done();
    const e = this._chatCompletions[this._chatCompletions.length - 1];
    if (!e) throw new gt("stream ended without producing a ChatCompletion");
    return e;
  }
  async finalContent() {
    return await this.done(), xe(this, Lr, "m", D0).call(this);
  }
  async finalMessage() {
    return await this.done(), xe(this, Lr, "m", pg).call(this);
  }
  async finalFunctionToolCall() {
    return await this.done(), xe(this, Lr, "m", O0).call(this);
  }
  async finalFunctionToolCallResult() {
    return await this.done(), xe(this, Lr, "m", F0).call(this);
  }
  async totalUsage() {
    return await this.done(), xe(this, Lr, "m", B0).call(this);
  }
  allChatCompletions() {
    return [...this._chatCompletions];
  }
  _emitFinal() {
    const e = this._chatCompletions[this._chatCompletions.length - 1];
    e && this._emit("finalChatCompletion", e);
    const t = xe(this, Lr, "m", pg).call(this);
    t && this._emit("finalMessage", t);
    const n = xe(this, Lr, "m", D0).call(this);
    n && this._emit("finalContent", n);
    const r = xe(this, Lr, "m", O0).call(this);
    r && this._emit("finalFunctionToolCall", r);
    const s = xe(this, Lr, "m", F0).call(this);
    s != null && this._emit("finalFunctionToolCallResult", s), this._chatCompletions.some((a) => a.usage) && this._emit("totalUsage", xe(this, Lr, "m", B0).call(this));
  }
  async _createChatCompletion(e, t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort())), xe(this, Lr, "m", FR).call(this, t);
    const s = await e.chat.completions.create({
      ...t,
      stream: !1
    }, {
      ...n,
      signal: this.controller.signal
    });
    return this._connected(), this._addChatCompletion(sS(s, t));
  }
  async _runChatCompletion(e, t, n) {
    for (const r of t.messages) this._addMessage(r, !1);
    return await this._createChatCompletion(e, t, n);
  }
  async _runTools(e, t, n) {
    const r = "tool", { tool_choice: s = "auto", stream: a, ...o } = t, l = typeof s != "string" && s.type === "function" && s?.function?.name, { maxChatCompletions: i = AQ } = n || {}, u = t.tools.map((m) => {
      if (Xf(m)) {
        if (!m.$callback) throw new gt("Tool given to `.runTools()` that does not have an associated function");
        return {
          type: "function",
          function: {
            function: m.$callback,
            name: m.function.name,
            description: m.function.description || "",
            parameters: m.function.parameters,
            parse: m.$parseRaw,
            strict: !0
          }
        };
      }
      return m;
    }), d = {};
    for (const m of u) m.type === "function" && (d[m.function.name || m.function.function.name] = m.function);
    const f = "tools" in t ? u.map((m) => m.type === "function" ? {
      type: "function",
      function: {
        name: m.function.name || m.function.function.name,
        parameters: m.function.parameters,
        description: m.function.description,
        strict: m.function.strict
      }
    } : m) : void 0;
    for (const m of t.messages) this._addMessage(m, !1);
    for (let m = 0; m < i; ++m) {
      const v = (await this._createChatCompletion(e, {
        ...o,
        tool_choice: s,
        tools: f,
        messages: [...this.messages]
      }, n)).choices[0]?.message;
      if (!v) throw new gt("missing message in ChatCompletion response");
      if (!v.tool_calls?.length) return;
      for (const y of v.tool_calls) {
        if (y.type !== "function") continue;
        const S = y.id, { name: x, arguments: I } = y.function, E = d[x];
        if (E) {
          if (l && l !== x) {
            const w = `Invalid tool_call: ${JSON.stringify(x)}. ${JSON.stringify(l)} requested. Please try again`;
            this._addMessage({
              role: r,
              tool_call_id: S,
              content: w
            });
            continue;
          }
        } else {
          const w = `Invalid tool_call: ${JSON.stringify(x)}. Available options are: ${Object.keys(d).map((_) => JSON.stringify(_)).join(", ")}. Please try again`;
          this._addMessage({
            role: r,
            tool_call_id: S,
            content: w
          });
          continue;
        }
        let N;
        try {
          N = SQ(E) ? await E.parse(I) : I;
        } catch (w) {
          const _ = w instanceof Error ? w.message : String(w);
          this._addMessage({
            role: r,
            tool_call_id: S,
            content: _
          });
          continue;
        }
        const $ = await E.function(N, this), p = xe(this, Lr, "m", BR).call(this, $);
        if (this._addMessage({
          role: r,
          tool_call_id: S,
          content: p
        }), l) return;
      }
    }
  }
};
Lr = /* @__PURE__ */ new WeakSet(), D0 = function() {
  return xe(this, Lr, "m", pg).call(this).content ?? null;
}, pg = function() {
  let t = this.messages.length;
  for (; t-- > 0; ) {
    const n = this.messages[t];
    if (dg(n)) return {
      ...n,
      content: n.content ?? null,
      refusal: n.refusal ?? null
    };
  }
  throw new gt("stream ended without producing a ChatCompletionMessage with role=assistant");
}, O0 = function() {
  for (let t = this.messages.length - 1; t >= 0; t--) {
    const n = this.messages[t];
    if (dg(n) && n?.tool_calls?.length) for (let r = n.tool_calls.length - 1; r >= 0; r--) {
      const s = n.tool_calls[r];
      if (s?.type === "function") return s.function;
    }
  }
}, F0 = function() {
  for (let t = this.messages.length - 1; t >= 0; t--) {
    const n = this.messages[t];
    if (DR(n) && n.content != null && typeof n.content == "string" && this.messages.some((r) => r.role === "assistant" && r.tool_calls?.some((s) => s.type === "function" && s.id === n.tool_call_id))) return n.content;
  }
}, B0 = function() {
  const t = {
    completion_tokens: 0,
    prompt_tokens: 0,
    total_tokens: 0
  };
  for (const { usage: n } of this._chatCompletions) n && (t.completion_tokens += n.completion_tokens, t.prompt_tokens += n.prompt_tokens, t.total_tokens += n.total_tokens);
  return t;
}, FR = function(t) {
  if (t.n != null && t.n > 1) throw new gt("ChatCompletion convenience helpers only support n=1 at this time. To use n>1, please use chat.completions.create() directly.");
}, BR = function(t) {
  return typeof t == "string" ? t : t === void 0 ? "undefined" : JSON.stringify(t);
};
var TQ = class qR extends UR {
  static runTools(t, n, r) {
    const s = new qR(), a = {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "runTools"
      }
    };
    return s._run(() => s._runTools(t, n, a)), s;
  }
  _addMessage(t, n = !0) {
    super._addMessage(t, n), dg(t) && t.content && this._emit("content", t.content);
  }
}, ar = {
  STR: 1,
  NUM: 2,
  ARR: 4,
  OBJ: 8,
  NULL: 16,
  BOOL: 32,
  NAN: 64,
  INFINITY: 128,
  MINUS_INFINITY: 256,
  INF: 384,
  SPECIAL: 496,
  ATOM: 499,
  COLLECTION: 12,
  ALL: 511
}, EQ = class extends Error {
}, CQ = class extends Error {
};
function IQ(e, t = ar.ALL) {
  if (typeof e != "string") throw new TypeError(`expecting str, got ${typeof e}`);
  if (!e.trim()) throw new Error(`${e} is empty`);
  return xQ(e.trim(), t);
}
var xQ = (e, t) => {
  const n = e.length;
  let r = 0;
  const s = (m) => {
    throw new EQ(`${m} at position ${r}`);
  }, a = (m) => {
    throw new CQ(`${m} at position ${r}`);
  }, o = () => (f(), r >= n && s("Unexpected end of input"), e[r] === '"' ? l() : e[r] === "{" ? i() : e[r] === "[" ? u() : e.substring(r, r + 4) === "null" || ar.NULL & t && n - r < 4 && "null".startsWith(e.substring(r)) ? (r += 4, null) : e.substring(r, r + 4) === "true" || ar.BOOL & t && n - r < 4 && "true".startsWith(e.substring(r)) ? (r += 4, !0) : e.substring(r, r + 5) === "false" || ar.BOOL & t && n - r < 5 && "false".startsWith(e.substring(r)) ? (r += 5, !1) : e.substring(r, r + 8) === "Infinity" || ar.INFINITY & t && n - r < 8 && "Infinity".startsWith(e.substring(r)) ? (r += 8, 1 / 0) : e.substring(r, r + 9) === "-Infinity" || ar.MINUS_INFINITY & t && 1 < n - r && n - r < 9 && "-Infinity".startsWith(e.substring(r)) ? (r += 9, -1 / 0) : e.substring(r, r + 3) === "NaN" || ar.NAN & t && n - r < 3 && "NaN".startsWith(e.substring(r)) ? (r += 3, NaN) : d()), l = () => {
    const m = r;
    let v = !1;
    for (r++; r < n && (e[r] !== '"' || v && e[r - 1] === "\\"); )
      v = e[r] === "\\" ? !v : !1, r++;
    if (e.charAt(r) == '"') try {
      return JSON.parse(e.substring(m, ++r - Number(v)));
    } catch (y) {
      a(String(y));
    }
    else if (ar.STR & t) try {
      return JSON.parse(e.substring(m, r - Number(v)) + '"');
    } catch {
      return JSON.parse(e.substring(m, e.lastIndexOf("\\")) + '"');
    }
    s("Unterminated string literal");
  }, i = () => {
    r++, f();
    const m = {};
    try {
      for (; e[r] !== "}"; ) {
        if (f(), r >= n && ar.OBJ & t) return m;
        const v = l();
        f(), r++;
        try {
          const y = o();
          Object.defineProperty(m, v, {
            value: y,
            writable: !0,
            enumerable: !0,
            configurable: !0
          });
        } catch (y) {
          if (ar.OBJ & t) return m;
          throw y;
        }
        f(), e[r] === "," && r++;
      }
    } catch {
      if (ar.OBJ & t) return m;
      s("Expected '}' at end of object");
    }
    return r++, m;
  }, u = () => {
    r++;
    const m = [];
    try {
      for (; e[r] !== "]"; )
        m.push(o()), f(), e[r] === "," && r++;
    } catch {
      if (ar.ARR & t) return m;
      s("Expected ']' at end of array");
    }
    return r++, m;
  }, d = () => {
    if (r === 0) {
      e === "-" && ar.NUM & t && s("Not sure what '-' is");
      try {
        return JSON.parse(e);
      } catch (v) {
        if (ar.NUM & t) try {
          return e[e.length - 1] === "." ? JSON.parse(e.substring(0, e.lastIndexOf("."))) : JSON.parse(e.substring(0, e.lastIndexOf("e")));
        } catch {
        }
        a(String(v));
      }
    }
    const m = r;
    for (e[r] === "-" && r++; e[r] && !",]}".includes(e[r]); ) r++;
    r == n && !(ar.NUM & t) && s("Unterminated number literal");
    try {
      return JSON.parse(e.substring(m, r));
    } catch {
      e.substring(m, r) === "-" && ar.NUM & t && s("Not sure what '-' is");
      try {
        return JSON.parse(e.substring(m, e.lastIndexOf("e")));
      } catch (y) {
        a(String(y));
      }
    }
  }, f = () => {
    for (; r < n && " \n\r\t".includes(e[r]); ) r++;
  };
  return o();
}, wI = (e) => IQ(e, ar.ALL ^ ar.NUM), jn, ro, ru, Ko, T_, vp, E_, C_, I_, yp, x_, SI, HR = class U0 extends UR {
  constructor(t) {
    super(), jn.add(this), ro.set(this, void 0), ru.set(this, void 0), Ko.set(this, void 0), At(this, ro, t, "f"), At(this, ru, [], "f");
  }
  get currentChatCompletionSnapshot() {
    return xe(this, Ko, "f");
  }
  static fromReadableStream(t) {
    const n = new U0(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createChatCompletion(t, n, r) {
    const s = new U0(n);
    return s._run(() => s._runChatCompletion(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  async _createChatCompletion(t, n, r) {
    super._createChatCompletion;
    const s = r?.signal;
    s && (s.aborted && this.controller.abort(), s.addEventListener("abort", () => this.controller.abort())), xe(this, jn, "m", T_).call(this);
    const a = await t.chat.completions.create({
      ...n,
      stream: !0
    }, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const o of a) xe(this, jn, "m", E_).call(this, o);
    if (a.controller.signal?.aborted) throw new Bs();
    return this._addChatCompletion(xe(this, jn, "m", yp).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort())), xe(this, jn, "m", T_).call(this), this._connected();
    const s = If.fromReadableStream(t, this.controller);
    let a;
    for await (const o of s)
      a && a !== o.id && this._addChatCompletion(xe(this, jn, "m", yp).call(this)), xe(this, jn, "m", E_).call(this, o), a = o.id;
    if (s.controller.signal?.aborted) throw new Bs();
    return this._addChatCompletion(xe(this, jn, "m", yp).call(this));
  }
  [(ro = /* @__PURE__ */ new WeakMap(), ru = /* @__PURE__ */ new WeakMap(), Ko = /* @__PURE__ */ new WeakMap(), jn = /* @__PURE__ */ new WeakSet(), T_ = function() {
    this.ended || At(this, Ko, void 0, "f");
  }, vp = function(n) {
    let r = xe(this, ru, "f")[n.index];
    return r || (r = {
      content_done: !1,
      refusal_done: !1,
      logprobs_content_done: !1,
      logprobs_refusal_done: !1,
      done_tool_calls: /* @__PURE__ */ new Set(),
      current_tool_call_index: null
    }, xe(this, ru, "f")[n.index] = r, r);
  }, E_ = function(n) {
    if (this.ended) return;
    const r = xe(this, jn, "m", SI).call(this, n);
    this._emit("chunk", n, r);
    for (const s of n.choices) {
      const a = r.choices[s.index];
      s.delta.content != null && a.message?.role === "assistant" && a.message?.content && (this._emit("content", s.delta.content, a.message.content), this._emit("content.delta", {
        delta: s.delta.content,
        snapshot: a.message.content,
        parsed: a.message.parsed
      })), s.delta.refusal != null && a.message?.role === "assistant" && a.message?.refusal && this._emit("refusal.delta", {
        delta: s.delta.refusal,
        snapshot: a.message.refusal
      }), s.logprobs?.content != null && a.message?.role === "assistant" && this._emit("logprobs.content.delta", {
        content: s.logprobs?.content,
        snapshot: a.logprobs?.content ?? []
      }), s.logprobs?.refusal != null && a.message?.role === "assistant" && this._emit("logprobs.refusal.delta", {
        refusal: s.logprobs?.refusal,
        snapshot: a.logprobs?.refusal ?? []
      });
      const o = xe(this, jn, "m", vp).call(this, a);
      a.finish_reason && (xe(this, jn, "m", I_).call(this, a), o.current_tool_call_index != null && xe(this, jn, "m", C_).call(this, a, o.current_tool_call_index));
      for (const l of s.delta.tool_calls ?? [])
        o.current_tool_call_index !== l.index && (xe(this, jn, "m", I_).call(this, a), o.current_tool_call_index != null && xe(this, jn, "m", C_).call(this, a, o.current_tool_call_index)), o.current_tool_call_index = l.index;
      for (const l of s.delta.tool_calls ?? []) {
        const i = a.message.tool_calls?.[l.index];
        i?.type && (i?.type === "function" ? this._emit("tool_calls.function.arguments.delta", {
          name: i.function?.name,
          index: l.index,
          arguments: i.function.arguments,
          parsed_arguments: i.function.parsed_arguments,
          arguments_delta: l.function?.arguments ?? ""
        }) : i?.type);
      }
    }
  }, C_ = function(n, r) {
    if (xe(this, jn, "m", vp).call(this, n).done_tool_calls.has(r)) return;
    const s = n.message.tool_calls?.[r];
    if (!s) throw new Error("no tool call snapshot");
    if (!s.type) throw new Error("tool call snapshot missing `type`");
    if (s.type === "function") {
      const a = xe(this, ro, "f")?.tools?.find((o) => cg(o) && o.function.name === s.function.name);
      this._emit("tool_calls.function.arguments.done", {
        name: s.function.name,
        index: r,
        arguments: s.function.arguments,
        parsed_arguments: Xf(a) ? a.$parseRaw(s.function.arguments) : a?.function.strict ? JSON.parse(s.function.arguments) : null
      });
    } else s.type;
  }, I_ = function(n) {
    const r = xe(this, jn, "m", vp).call(this, n);
    if (n.message.content && !r.content_done) {
      r.content_done = !0;
      const s = xe(this, jn, "m", x_).call(this);
      this._emit("content.done", {
        content: n.message.content,
        parsed: s ? s.$parseRaw(n.message.content) : null
      });
    }
    n.message.refusal && !r.refusal_done && (r.refusal_done = !0, this._emit("refusal.done", { refusal: n.message.refusal })), n.logprobs?.content && !r.logprobs_content_done && (r.logprobs_content_done = !0, this._emit("logprobs.content.done", { content: n.logprobs.content })), n.logprobs?.refusal && !r.logprobs_refusal_done && (r.logprobs_refusal_done = !0, this._emit("logprobs.refusal.done", { refusal: n.logprobs.refusal }));
  }, yp = function() {
    if (this.ended) throw new gt("stream has ended, this shouldn't happen");
    const n = xe(this, Ko, "f");
    if (!n) throw new gt("request ended without sending any chunks");
    return At(this, Ko, void 0, "f"), At(this, ru, [], "f"), kQ(n, xe(this, ro, "f"));
  }, x_ = function() {
    const n = xe(this, ro, "f")?.response_format;
    return rS(n) ? n : null;
  }, SI = function(n) {
    var r, s, a, o;
    let l = xe(this, Ko, "f");
    const { choices: i, ...u } = n;
    l ? Object.assign(l, u) : l = At(this, Ko, {
      ...u,
      choices: []
    }, "f");
    for (const { delta: d, finish_reason: f, index: m, logprobs: v = null, ...y } of n.choices) {
      let S = l.choices[m];
      if (S || (S = l.choices[m] = {
        finish_reason: f,
        index: m,
        message: {},
        logprobs: v,
        ...y
      }), v) if (!S.logprobs) S.logprobs = Object.assign({}, v);
      else {
        const { content: w, refusal: _, ...b } = v;
        Object.assign(S.logprobs, b), w && ((r = S.logprobs).content ?? (r.content = []), S.logprobs.content.push(...w)), _ && ((s = S.logprobs).refusal ?? (s.refusal = []), S.logprobs.refusal.push(..._));
      }
      if (f && (S.finish_reason = f, xe(this, ro, "f") && $R(xe(this, ro, "f")))) {
        if (f === "length") throw new fR();
        if (f === "content_filter") throw new hR();
      }
      if (Object.assign(S, y), !d) continue;
      const { content: x, refusal: I, function_call: E, role: N, tool_calls: $, ...p } = d;
      if (Object.assign(S.message, p), I && (S.message.refusal = (S.message.refusal || "") + I), N && (S.message.role = N), E && (S.message.function_call ? (E.name && (S.message.function_call.name = E.name), E.arguments && ((a = S.message.function_call).arguments ?? (a.arguments = ""), S.message.function_call.arguments += E.arguments)) : S.message.function_call = E), x && (S.message.content = (S.message.content || "") + x, !S.message.refusal && xe(this, jn, "m", x_).call(this) && (S.message.parsed = wI(S.message.content))), $) {
        S.message.tool_calls || (S.message.tool_calls = []);
        for (const { index: w, id: _, type: b, function: C, ...k } of $) {
          const P = (o = S.message.tool_calls)[w] ?? (o[w] = {});
          Object.assign(P, k), _ && (P.id = _), b && (P.type = b), C && (P.function ?? (P.function = {
            name: C.name ?? "",
            arguments: ""
          })), C?.name && (P.function.name = C.name), C?.arguments && (P.function.arguments += C.arguments, bQ(xe(this, ro, "f"), P) && (P.function.parsed_arguments = wI(P.function.arguments)));
        }
      }
    }
    return l;
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("chunk", (s) => {
      const a = n.shift();
      a ? a.resolve(s) : t.push(s);
    }), this.on("end", () => {
      r = !0;
      for (const s of n) s.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (s) => {
      r = !0;
      for (const a of n) a.reject(s);
      n.length = 0;
    }), this.on("error", (s) => {
      r = !0;
      for (const a of n) a.reject(s);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((s, a) => n.push({
        resolve: s,
        reject: a
      })).then((s) => s ? {
        value: s,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new If(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
};
function kQ(e, t) {
  const { id: n, choices: r, created: s, model: a, system_fingerprint: o, ...l } = e;
  return vQ({
    ...l,
    id: n,
    choices: r.map(({ message: i, finish_reason: u, index: d, logprobs: f, ...m }) => {
      if (!u) throw new gt(`missing finish_reason for choice ${d}`);
      const { content: v = null, function_call: y, tool_calls: S, ...x } = i, I = i.role;
      if (!I) throw new gt(`missing role for choice ${d}`);
      if (y) {
        const { arguments: E, name: N } = y;
        if (E == null) throw new gt(`missing function_call.arguments for choice ${d}`);
        if (!N) throw new gt(`missing function_call.name for choice ${d}`);
        return {
          ...m,
          message: {
            content: v,
            function_call: {
              arguments: E,
              name: N
            },
            role: I,
            refusal: i.refusal ?? null
          },
          finish_reason: u,
          index: d,
          logprobs: f
        };
      }
      return S ? {
        ...m,
        index: d,
        finish_reason: u,
        logprobs: f,
        message: {
          ...x,
          role: I,
          content: v,
          refusal: i.refusal ?? null,
          tool_calls: S.map((E, N) => {
            const { function: $, type: p, id: w, ..._ } = E, { arguments: b, name: C, ...k } = $ || {};
            if (w == null) throw new gt(`missing choices[${d}].tool_calls[${N}].id
${_p(e)}`);
            if (p == null) throw new gt(`missing choices[${d}].tool_calls[${N}].type
${_p(e)}`);
            if (C == null) throw new gt(`missing choices[${d}].tool_calls[${N}].function.name
${_p(e)}`);
            if (b == null) throw new gt(`missing choices[${d}].tool_calls[${N}].function.arguments
${_p(e)}`);
            return {
              ..._,
              id: w,
              type: p,
              function: {
                ...k,
                name: C,
                arguments: b
              }
            };
          })
        }
      } : {
        ...m,
        message: {
          ...x,
          content: v,
          role: I,
          refusal: i.refusal ?? null
        },
        finish_reason: u,
        index: d,
        logprobs: f
      };
    }),
    created: s,
    model: a,
    object: "chat.completion",
    ...o ? { system_fingerprint: o } : {}
  }, t);
}
function _p(e) {
  return JSON.stringify(e);
}
var MQ = class q0 extends HR {
  static fromReadableStream(t) {
    const n = new q0(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static runTools(t, n, r) {
    const s = new q0(n), a = {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "runTools"
      }
    };
    return s._run(() => s._runTools(t, n, a)), s;
  }
}, oS = class extends Ve {
  constructor() {
    super(...arguments), this.messages = new NR(this._client);
  }
  create(e, t) {
    return this._client.post("/chat/completions", {
      body: e,
      ...t,
      stream: e.stream ?? !1,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/chat/completions/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/chat/completions/${e}`, {
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/chat/completions", Mn, {
      query: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/chat/completions/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  parse(e, t) {
    return wQ(e.tools), this._client.chat.completions.create(e, {
      ...t,
      headers: {
        ...t?.headers,
        "X-Stainless-Helper-Method": "chat.completions.parse"
      }
    })._thenUnwrap((n) => sS(n, e));
  }
  runTools(e, t) {
    return e.stream ? MQ.runTools(this._client, e, t) : TQ.runTools(this._client, e, t);
  }
  stream(e, t) {
    return HR.createChatCompletion(this._client, e, t);
  }
};
oS.Messages = NR;
var iS = class extends Ve {
  constructor() {
    super(...arguments), this.completions = new oS(this._client);
  }
};
iS.Completions = oS;
var zR = class extends Ve {
  create(e, t) {
    return this._client.post("/organization/admin_api_keys", {
      body: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/organization/admin_api_keys/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/organization/admin_api_keys", Mn, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/organization/admin_api_keys/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, KR = class extends Ve {
  list(e = {}, t) {
    return this._client.getAPIList("/organization/audit_logs", _r, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, WR = class extends Ve {
  create(e, t) {
    return this._client.post("/organization/certificates", {
      body: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t = {}, n) {
    return this._client.get(_e`/organization/certificates/${e}`, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/certificates/${e}`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/organization/certificates", _r, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/organization/certificates/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  activate(e, t) {
    return this._client.getAPIList("/organization/certificates/activate", Si, {
      body: e,
      method: "post",
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  deactivate(e, t) {
    return this._client.getAPIList("/organization/certificates/deactivate", Si, {
      body: e,
      method: "post",
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, GR = class extends Ve {
  retrieve(e) {
    return this._client.get("/organization/data_retention", {
      ...e,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t) {
    return this._client.post("/organization/data_retention", {
      body: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, VR = class extends Ve {
  create(e, t) {
    return this._client.post("/organization/invites", {
      body: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/organization/invites/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/organization/invites", _r, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/organization/invites/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, JR = class extends Ve {
  create(e, t) {
    return this._client.post("/organization/roles", {
      body: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/organization/roles/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/roles/${e}`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/organization/roles", No, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/organization/roles/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, YR = class extends Ve {
  create(e, t) {
    return this._client.post("/organization/spend_alerts", {
      body: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/organization/spend_alerts/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/spend_alerts/${e}`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/organization/spend_alerts", _r, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/organization/spend_alerts/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, XR = class extends Ve {
  audioSpeeches(e, t) {
    return this._client.get("/organization/usage/audio_speeches", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  audioTranscriptions(e, t) {
    return this._client.get("/organization/usage/audio_transcriptions", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  codeInterpreterSessions(e, t) {
    return this._client.get("/organization/usage/code_interpreter_sessions", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  completions(e, t) {
    return this._client.get("/organization/usage/completions", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  costs(e, t) {
    return this._client.get("/organization/costs", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  embeddings(e, t) {
    return this._client.get("/organization/usage/embeddings", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  fileSearchCalls(e, t) {
    return this._client.get("/organization/usage/file_search_calls", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  images(e, t) {
    return this._client.get("/organization/usage/images", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  moderations(e, t) {
    return this._client.get("/organization/usage/moderations", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  vectorStores(e, t) {
    return this._client.get("/organization/usage/vector_stores", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  webSearchCalls(e, t) {
    return this._client.get("/organization/usage/web_search_calls", {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, jR = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/organization/groups/${e}/roles`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { group_id: r } = t;
    return this._client.get(_e`/organization/groups/${r}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/groups/${e}/roles`, No, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { group_id: r } = t;
    return this._client.delete(_e`/organization/groups/${r}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, QR = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/organization/groups/${e}/users`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { group_id: r } = t;
    return this._client.get(_e`/organization/groups/${r}/users/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/groups/${e}/users`, No, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { group_id: r } = t;
    return this._client.delete(_e`/organization/groups/${r}/users/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, yv = class extends Ve {
  constructor() {
    super(...arguments), this.users = new QR(this._client), this.roles = new jR(this._client);
  }
  create(e, t) {
    return this._client.post("/organization/groups", {
      body: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/organization/groups/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/groups/${e}`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/organization/groups", No, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/organization/groups/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
};
yv.Users = QR;
yv.Roles = jR;
var ZR = class extends Ve {
  retrieve(e, t, n) {
    const { project_id: r } = t;
    return this._client.get(_e`/organization/projects/${r}/api_keys/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/api_keys`, _r, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { project_id: r } = t;
    return this._client.delete(_e`/organization/projects/${r}/api_keys/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, e5 = class extends Ve {
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/certificates`, _r, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  activate(e, t, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/certificates/activate`, Si, {
      body: t,
      method: "post",
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  deactivate(e, t, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/certificates/deactivate`, Si, {
      body: t,
      method: "post",
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, t5 = class extends Ve {
  retrieve(e, t) {
    return this._client.get(_e`/organization/projects/${e}/data_retention`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/projects/${e}/data_retention`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, n5 = class extends Ve {
  retrieve(e, t) {
    return this._client.get(_e`/organization/projects/${e}/hosted_tool_permissions`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/projects/${e}/hosted_tool_permissions`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, r5 = class extends Ve {
  retrieve(e, t) {
    return this._client.get(_e`/organization/projects/${e}/model_permissions`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/projects/${e}/model_permissions`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/organization/projects/${e}/model_permissions`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, s5 = class extends Ve {
  listRateLimits(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/rate_limits`, _r, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  updateRateLimit(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.post(_e`/organization/projects/${r}/rate_limits/${e}`, {
      body: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, a5 = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/projects/${e}/roles`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { project_id: r } = t;
    return this._client.get(_e`/projects/${r}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.post(_e`/projects/${r}/roles/${e}`, {
      body: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/projects/${e}/roles`, No, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { project_id: r } = t;
    return this._client.delete(_e`/projects/${r}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, o5 = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/organization/projects/${e}/service_accounts`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { project_id: r } = t;
    return this._client.get(_e`/organization/projects/${r}/service_accounts/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.post(_e`/organization/projects/${r}/service_accounts/${e}`, {
      body: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/service_accounts`, _r, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { project_id: r } = t;
    return this._client.delete(_e`/organization/projects/${r}/service_accounts/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, i5 = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/organization/projects/${e}/spend_alerts`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { project_id: r } = t;
    return this._client.get(_e`/organization/projects/${r}/spend_alerts/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.post(_e`/organization/projects/${r}/spend_alerts/${e}`, {
      body: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/spend_alerts`, _r, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { project_id: r } = t;
    return this._client.delete(_e`/organization/projects/${r}/spend_alerts/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, l5 = class extends Ve {
  create(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.post(_e`/projects/${r}/groups/${e}/roles`, {
      body: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { project_id: r, group_id: s } = t;
    return this._client.get(_e`/projects/${r}/groups/${s}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.getAPIList(_e`/projects/${r}/groups/${e}/roles`, No, {
      query: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { project_id: r, group_id: s } = t;
    return this._client.delete(_e`/projects/${r}/groups/${s}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, lS = class extends Ve {
  constructor() {
    super(...arguments), this.roles = new l5(this._client);
  }
  create(e, t, n) {
    return this._client.post(_e`/organization/projects/${e}/groups`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.get(_e`/organization/projects/${r}/groups/${e}`, {
      query: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/groups`, No, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { project_id: r } = t;
    return this._client.delete(_e`/organization/projects/${r}/groups/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
};
lS.Roles = l5;
var u5 = class extends Ve {
  create(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.post(_e`/projects/${r}/users/${e}/roles`, {
      body: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { project_id: r, user_id: s } = t;
    return this._client.get(_e`/projects/${r}/users/${s}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.getAPIList(_e`/projects/${r}/users/${e}/roles`, No, {
      query: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { project_id: r, user_id: s } = t;
    return this._client.delete(_e`/projects/${r}/users/${s}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, uS = class extends Ve {
  constructor() {
    super(...arguments), this.roles = new u5(this._client);
  }
  create(e, t, n) {
    return this._client.post(_e`/organization/projects/${e}/users`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { project_id: r } = t;
    return this._client.get(_e`/organization/projects/${r}/users/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    const { project_id: r, ...s } = t;
    return this._client.post(_e`/organization/projects/${r}/users/${e}`, {
      body: s,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/projects/${e}/users`, _r, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { project_id: r } = t;
    return this._client.delete(_e`/organization/projects/${r}/users/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
};
uS.Roles = u5;
var xs = class extends Ve {
  constructor() {
    super(...arguments), this.users = new uS(this._client), this.serviceAccounts = new o5(this._client), this.apiKeys = new ZR(this._client), this.rateLimits = new s5(this._client), this.modelPermissions = new r5(this._client), this.hostedToolPermissions = new n5(this._client), this.groups = new lS(this._client), this.roles = new a5(this._client), this.dataRetention = new t5(this._client), this.spendAlerts = new i5(this._client), this.certificates = new e5(this._client);
  }
  create(e, t) {
    return this._client.post("/organization/projects", {
      body: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/organization/projects/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/projects/${e}`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/organization/projects", _r, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  archive(e, t) {
    return this._client.post(_e`/organization/projects/${e}/archive`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
};
xs.Users = uS;
xs.ServiceAccounts = o5;
xs.APIKeys = ZR;
xs.RateLimits = s5;
xs.ModelPermissions = r5;
xs.HostedToolPermissions = n5;
xs.Groups = lS;
xs.Roles = a5;
xs.DataRetention = t5;
xs.SpendAlerts = i5;
xs.Certificates = e5;
var c5 = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/organization/users/${e}/roles`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { user_id: r } = t;
    return this._client.get(_e`/organization/users/${r}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/organization/users/${e}/roles`, No, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { user_id: r } = t;
    return this._client.delete(_e`/organization/users/${r}/roles/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, cS = class extends Ve {
  constructor() {
    super(...arguments), this.roles = new c5(this._client);
  }
  retrieve(e, t) {
    return this._client.get(_e`/organization/users/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/organization/users/${e}`, {
      body: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/organization/users", _r, {
      query: e,
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/organization/users/${e}`, {
      ...t,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
};
cS.Roles = c5;
var ks = class extends Ve {
  constructor() {
    super(...arguments), this.auditLogs = new KR(this._client), this.adminAPIKeys = new zR(this._client), this.usage = new XR(this._client), this.invites = new VR(this._client), this.users = new cS(this._client), this.groups = new yv(this._client), this.roles = new JR(this._client), this.dataRetention = new GR(this._client), this.spendAlerts = new YR(this._client), this.certificates = new WR(this._client), this.projects = new xs(this._client);
  }
};
ks.AuditLogs = KR;
ks.AdminAPIKeys = zR;
ks.Usage = XR;
ks.Invites = VR;
ks.Users = cS;
ks.Groups = yv;
ks.Roles = JR;
ks.DataRetention = GR;
ks.SpendAlerts = YR;
ks.Certificates = WR;
ks.Projects = xs;
var dS = class extends Ve {
  constructor() {
    super(...arguments), this.organization = new ks(this._client);
  }
};
dS.Organization = ks;
var d5 = /* @__PURE__ */ Symbol("brand.privateNullableHeaders");
function* PQ(e) {
  if (!e) return;
  if (d5 in e) {
    const { values: r, nulls: s } = e;
    yield* r.entries();
    for (const a of s) yield [a, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : aI(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const s = r[0];
    if (typeof s != "string") throw new TypeError("expected header name to be a string");
    const a = aI(r[1]) ? r[1] : [r[1]];
    let o = !1;
    for (const l of a)
      l !== void 0 && (t && !o && (o = !0, yield [s, null]), yield [s, l]);
  }
}
var rt = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const s = /* @__PURE__ */ new Set();
    for (const [a, o] of PQ(r)) {
      const l = a.toLowerCase();
      s.has(l) || (t.delete(a), s.add(l)), o === null ? (t.delete(a), n.add(l)) : (t.append(a, o), n.delete(l));
    }
  }
  return {
    [d5]: !0,
    values: t,
    nulls: n
  };
}, f5 = class extends Ve {
  create(e, t) {
    return this._client.post("/audio/speech", {
      body: e,
      ...t,
      headers: rt([{ Accept: "application/octet-stream" }, t?.headers]),
      __security: { bearerAuth: !0 },
      __binaryResponse: !0
    });
  }
}, h5 = class extends Ve {
  create(e, t) {
    return this._client.post("/audio/transcriptions", $a({
      body: e,
      ...t,
      stream: e.stream ?? !1,
      __metadata: { model: e.model },
      __security: { bearerAuth: !0 }
    }, this._client));
  }
}, p5 = class extends Ve {
  create(e, t) {
    return this._client.post("/audio/translations", $a({
      body: e,
      ...t,
      __metadata: { model: e.model },
      __security: { bearerAuth: !0 }
    }, this._client));
  }
}, jf = class extends Ve {
  constructor() {
    super(...arguments), this.transcriptions = new h5(this._client), this.translations = new p5(this._client), this.speech = new f5(this._client);
  }
};
jf.Transcriptions = h5;
jf.Translations = p5;
jf.Speech = f5;
var m5 = class extends Ve {
  create(e, t) {
    return this._client.post("/batches", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/batches/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/batches", Mn, {
      query: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  cancel(e, t) {
    return this._client.post(_e`/batches/${e}/cancel`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
}, g5 = class extends Ve {
  create(e, t) {
    return this._client.post("/assistants", {
      body: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/assistants/${e}`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/assistants/${e}`, {
      body: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/assistants", Mn, {
      query: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/assistants/${e}`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, v5 = class extends Ve {
  create(e, t) {
    return this._client.post("/realtime/sessions", {
      body: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, y5 = class extends Ve {
  create(e, t) {
    return this._client.post("/realtime/transcription_sessions", {
      body: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, _v = class extends Ve {
  constructor() {
    super(...arguments), this.sessions = new v5(this._client), this.transcriptionSessions = new y5(this._client);
  }
};
_v.Sessions = v5;
_v.TranscriptionSessions = y5;
var _5 = class extends Ve {
  create(e, t) {
    return this._client.post("/chatkit/sessions", {
      body: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  cancel(e, t) {
    return this._client.post(_e`/chatkit/sessions/${e}/cancel`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, b5 = class extends Ve {
  retrieve(e, t) {
    return this._client.get(_e`/chatkit/threads/${e}`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/chatkit/threads", _r, {
      query: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/chatkit/threads/${e}`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  listItems(e, t = {}, n) {
    return this._client.getAPIList(_e`/chatkit/threads/${e}/items`, _r, {
      query: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "chatkit_beta=v1" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, bv = class extends Ve {
  constructor() {
    super(...arguments), this.sessions = new _5(this._client), this.threads = new b5(this._client);
  }
};
bv.Sessions = _5;
bv.Threads = b5;
var w5 = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/threads/${e}/messages`, {
      body: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { thread_id: r } = t;
    return this._client.get(_e`/threads/${r}/messages/${e}`, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    const { thread_id: r, ...s } = t;
    return this._client.post(_e`/threads/${r}/messages/${e}`, {
      body: s,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/threads/${e}/messages`, Mn, {
      query: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { thread_id: r } = t;
    return this._client.delete(_e`/threads/${r}/messages/${e}`, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, S5 = class extends Ve {
  retrieve(e, t, n) {
    const { thread_id: r, run_id: s, ...a } = t;
    return this._client.get(_e`/threads/${r}/runs/${s}/steps/${e}`, {
      query: a,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t, n) {
    const { thread_id: r, ...s } = t;
    return this._client.getAPIList(_e`/threads/${r}/runs/${e}/steps`, Mn, {
      query: s,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, RQ = (e) => {
  if (typeof Buffer < "u") {
    const t = Buffer.from(e, "base64");
    return Array.from(new Float32Array(t.buffer, t.byteOffset, t.length / Float32Array.BYTES_PER_ELEMENT));
  } else {
    const t = atob(e), n = t.length, r = new Uint8Array(n);
    for (let s = 0; s < n; s++) r[s] = t.charCodeAt(s);
    return Array.from(new Float32Array(r.buffer));
  }
}, Wo = (e) => {
  if (typeof globalThis.process < "u") return globalThis.process.env?.[e]?.trim() || void 0;
  if (typeof globalThis.Deno < "u") return globalThis.Deno.env?.get?.(e)?.trim() || void 0;
}, mr, dl, H0, Ia, tm, Js, fl, Su, al, mg, hs, nm, rm, Xd, kd, Md, AI, TI, EI, CI, II, xI, kI, jd = class extends aS {
  constructor() {
    super(...arguments), mr.add(this), H0.set(this, []), Ia.set(this, {}), tm.set(this, {}), Js.set(this, void 0), fl.set(this, void 0), Su.set(this, void 0), al.set(this, void 0), mg.set(this, void 0), hs.set(this, void 0), nm.set(this, void 0), rm.set(this, void 0), Xd.set(this, void 0);
  }
  [(H0 = /* @__PURE__ */ new WeakMap(), Ia = /* @__PURE__ */ new WeakMap(), tm = /* @__PURE__ */ new WeakMap(), Js = /* @__PURE__ */ new WeakMap(), fl = /* @__PURE__ */ new WeakMap(), Su = /* @__PURE__ */ new WeakMap(), al = /* @__PURE__ */ new WeakMap(), mg = /* @__PURE__ */ new WeakMap(), hs = /* @__PURE__ */ new WeakMap(), nm = /* @__PURE__ */ new WeakMap(), rm = /* @__PURE__ */ new WeakMap(), Xd = /* @__PURE__ */ new WeakMap(), mr = /* @__PURE__ */ new WeakSet(), Symbol.asyncIterator)]() {
    const e = [], t = [];
    let n = !1;
    return this.on("event", (r) => {
      const s = t.shift();
      s ? s.resolve(r) : e.push(r);
    }), this.on("end", () => {
      n = !0;
      for (const r of t) r.resolve(void 0);
      t.length = 0;
    }), this.on("abort", (r) => {
      n = !0;
      for (const s of t) s.reject(r);
      t.length = 0;
    }), this.on("error", (r) => {
      n = !0;
      for (const s of t) s.reject(r);
      t.length = 0;
    }), {
      next: async () => e.length ? {
        value: e.shift(),
        done: !1
      } : n ? {
        value: void 0,
        done: !0
      } : new Promise((r, s) => t.push({
        resolve: r,
        reject: s
      })).then((r) => r ? {
        value: r,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  static fromReadableStream(e) {
    const t = new dl();
    return t._run(() => t._fromReadableStream(e)), t;
  }
  async _fromReadableStream(e, t) {
    const n = t?.signal;
    n && (n.aborted && this.controller.abort(), n.addEventListener("abort", () => this.controller.abort())), this._connected();
    const r = If.fromReadableStream(e, this.controller);
    for await (const s of r) xe(this, mr, "m", kd).call(this, s);
    if (r.controller.signal?.aborted) throw new Bs();
    return this._addRun(xe(this, mr, "m", Md).call(this));
  }
  toReadableStream() {
    return new If(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
  static createToolAssistantStream(e, t, n, r) {
    const s = new dl();
    return s._run(() => s._runToolAssistantStream(e, t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  async _createToolAssistantStream(e, t, n, r) {
    const s = r?.signal;
    s && (s.aborted && this.controller.abort(), s.addEventListener("abort", () => this.controller.abort()));
    const a = {
      ...n,
      stream: !0
    }, o = await e.submitToolOutputs(t, a, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const l of o) xe(this, mr, "m", kd).call(this, l);
    if (o.controller.signal?.aborted) throw new Bs();
    return this._addRun(xe(this, mr, "m", Md).call(this));
  }
  static createThreadAssistantStream(e, t, n) {
    const r = new dl();
    return r._run(() => r._threadAssistantStream(e, t, {
      ...n,
      headers: {
        ...n?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), r;
  }
  static createAssistantStream(e, t, n, r) {
    const s = new dl();
    return s._run(() => s._runAssistantStream(e, t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  currentEvent() {
    return xe(this, nm, "f");
  }
  currentRun() {
    return xe(this, rm, "f");
  }
  currentMessageSnapshot() {
    return xe(this, Js, "f");
  }
  currentRunStepSnapshot() {
    return xe(this, Xd, "f");
  }
  async finalRunSteps() {
    return await this.done(), Object.values(xe(this, Ia, "f"));
  }
  async finalMessages() {
    return await this.done(), Object.values(xe(this, tm, "f"));
  }
  async finalRun() {
    if (await this.done(), !xe(this, fl, "f")) throw Error("Final run was not received.");
    return xe(this, fl, "f");
  }
  async _createThreadAssistantStream(e, t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort()));
    const s = {
      ...t,
      stream: !0
    }, a = await e.createAndRun(s, {
      ...n,
      signal: this.controller.signal
    });
    this._connected();
    for await (const o of a) xe(this, mr, "m", kd).call(this, o);
    if (a.controller.signal?.aborted) throw new Bs();
    return this._addRun(xe(this, mr, "m", Md).call(this));
  }
  async _createAssistantStream(e, t, n, r) {
    const s = r?.signal;
    s && (s.aborted && this.controller.abort(), s.addEventListener("abort", () => this.controller.abort()));
    const a = {
      ...n,
      stream: !0
    }, o = await e.create(t, a, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const l of o) xe(this, mr, "m", kd).call(this, l);
    if (o.controller.signal?.aborted) throw new Bs();
    return this._addRun(xe(this, mr, "m", Md).call(this));
  }
  static accumulateDelta(e, t) {
    for (const [n, r] of Object.entries(t)) {
      if (!e.hasOwnProperty(n)) {
        e[n] = r;
        continue;
      }
      let s = e[n];
      if (s == null) {
        e[n] = r;
        continue;
      }
      if (n === "index" || n === "type") {
        e[n] = r;
        continue;
      }
      if (typeof s == "string" && typeof r == "string") s += r;
      else if (typeof s == "number" && typeof r == "number") s += r;
      else if (w_(s) && w_(r)) s = this.accumulateDelta(s, r);
      else if (Array.isArray(s) && Array.isArray(r)) {
        if (s.every((a) => typeof a == "string" || typeof a == "number")) {
          s.push(...r);
          continue;
        }
        for (const a of r) {
          if (!w_(a)) throw new Error(`Expected array delta entry to be an object but got: ${a}`);
          const o = a.index;
          if (o == null)
            throw console.error(a), new Error("Expected array delta entry to have an `index` property");
          if (typeof o != "number") throw new Error(`Expected array delta entry \`index\` property to be a number but got ${o}`);
          const l = s[o];
          l == null ? s.push(a) : s[o] = this.accumulateDelta(l, a);
        }
        continue;
      } else throw Error(`Unhandled record type: ${n}, deltaValue: ${r}, accValue: ${s}`);
      e[n] = s;
    }
    return e;
  }
  _addRun(e) {
    return e;
  }
  async _threadAssistantStream(e, t, n) {
    return await this._createThreadAssistantStream(t, e, n);
  }
  async _runAssistantStream(e, t, n, r) {
    return await this._createAssistantStream(t, e, n, r);
  }
  async _runToolAssistantStream(e, t, n, r) {
    return await this._createToolAssistantStream(t, e, n, r);
  }
};
dl = jd, kd = function(t) {
  if (!this.ended)
    switch (At(this, nm, t, "f"), xe(this, mr, "m", EI).call(this, t), t.event) {
      case "thread.created":
        break;
      case "thread.run.created":
      case "thread.run.queued":
      case "thread.run.in_progress":
      case "thread.run.requires_action":
      case "thread.run.completed":
      case "thread.run.incomplete":
      case "thread.run.failed":
      case "thread.run.cancelling":
      case "thread.run.cancelled":
      case "thread.run.expired":
        xe(this, mr, "m", kI).call(this, t);
        break;
      case "thread.run.step.created":
      case "thread.run.step.in_progress":
      case "thread.run.step.delta":
      case "thread.run.step.completed":
      case "thread.run.step.failed":
      case "thread.run.step.cancelled":
      case "thread.run.step.expired":
        xe(this, mr, "m", TI).call(this, t);
        break;
      case "thread.message.created":
      case "thread.message.in_progress":
      case "thread.message.delta":
      case "thread.message.completed":
      case "thread.message.incomplete":
        xe(this, mr, "m", AI).call(this, t);
        break;
      case "error":
        throw new Error("Encountered an error event in event processing - errors should be processed earlier");
      default:
    }
}, Md = function() {
  if (this.ended) throw new gt("stream has ended, this shouldn't happen");
  if (!xe(this, fl, "f")) throw Error("Final run has not been received");
  return xe(this, fl, "f");
}, AI = function(t) {
  const [n, r] = xe(this, mr, "m", II).call(this, t, xe(this, Js, "f"));
  At(this, Js, n, "f"), xe(this, tm, "f")[n.id] = n;
  for (const s of r) {
    const a = n.content[s.index];
    a?.type == "text" && this._emit("textCreated", a.text);
  }
  switch (t.event) {
    case "thread.message.created":
      this._emit("messageCreated", t.data);
      break;
    case "thread.message.in_progress":
      break;
    case "thread.message.delta":
      if (this._emit("messageDelta", t.data.delta, n), t.data.delta.content) for (const s of t.data.delta.content) {
        if (s.type == "text" && s.text) {
          let a = s.text, o = n.content[s.index];
          if (o && o.type == "text") this._emit("textDelta", a, o.text);
          else throw Error("The snapshot associated with this text delta is not text or missing");
        }
        if (s.index != xe(this, Su, "f")) {
          if (xe(this, al, "f")) switch (xe(this, al, "f").type) {
            case "text":
              this._emit("textDone", xe(this, al, "f").text, xe(this, Js, "f"));
              break;
            case "image_file":
              this._emit("imageFileDone", xe(this, al, "f").image_file, xe(this, Js, "f"));
              break;
          }
          At(this, Su, s.index, "f");
        }
        At(this, al, n.content[s.index], "f");
      }
      break;
    case "thread.message.completed":
    case "thread.message.incomplete":
      if (xe(this, Su, "f") !== void 0) {
        const s = t.data.content[xe(this, Su, "f")];
        if (s) switch (s.type) {
          case "image_file":
            this._emit("imageFileDone", s.image_file, xe(this, Js, "f"));
            break;
          case "text":
            this._emit("textDone", s.text, xe(this, Js, "f"));
            break;
        }
      }
      xe(this, Js, "f") && this._emit("messageDone", t.data), At(this, Js, void 0, "f");
  }
}, TI = function(t) {
  const n = xe(this, mr, "m", CI).call(this, t);
  switch (At(this, Xd, n, "f"), t.event) {
    case "thread.run.step.created":
      this._emit("runStepCreated", t.data);
      break;
    case "thread.run.step.delta":
      const r = t.data.delta;
      if (r.step_details && r.step_details.type == "tool_calls" && r.step_details.tool_calls && n.step_details.type == "tool_calls") for (const s of r.step_details.tool_calls) s.index == xe(this, mg, "f") ? this._emit("toolCallDelta", s, n.step_details.tool_calls[s.index]) : (xe(this, hs, "f") && this._emit("toolCallDone", xe(this, hs, "f")), At(this, mg, s.index, "f"), At(this, hs, n.step_details.tool_calls[s.index], "f"), xe(this, hs, "f") && this._emit("toolCallCreated", xe(this, hs, "f")));
      this._emit("runStepDelta", t.data.delta, n);
      break;
    case "thread.run.step.completed":
    case "thread.run.step.failed":
    case "thread.run.step.cancelled":
    case "thread.run.step.expired":
      At(this, Xd, void 0, "f"), t.data.step_details.type == "tool_calls" && xe(this, hs, "f") && (this._emit("toolCallDone", xe(this, hs, "f")), At(this, hs, void 0, "f")), this._emit("runStepDone", t.data, n);
      break;
    case "thread.run.step.in_progress":
      break;
  }
}, EI = function(t) {
  xe(this, H0, "f").push(t), this._emit("event", t);
}, CI = function(t) {
  switch (t.event) {
    case "thread.run.step.created":
      return xe(this, Ia, "f")[t.data.id] = t.data, t.data;
    case "thread.run.step.delta":
      let n = xe(this, Ia, "f")[t.data.id];
      if (!n) throw Error("Received a RunStepDelta before creation of a snapshot");
      let r = t.data;
      if (r.delta) {
        const s = dl.accumulateDelta(n, r.delta);
        xe(this, Ia, "f")[t.data.id] = s;
      }
      return xe(this, Ia, "f")[t.data.id];
    case "thread.run.step.completed":
    case "thread.run.step.failed":
    case "thread.run.step.cancelled":
    case "thread.run.step.expired":
    case "thread.run.step.in_progress":
      xe(this, Ia, "f")[t.data.id] = t.data;
      break;
  }
  if (xe(this, Ia, "f")[t.data.id]) return xe(this, Ia, "f")[t.data.id];
  throw new Error("No snapshot available");
}, II = function(t, n) {
  let r = [];
  switch (t.event) {
    case "thread.message.created":
      return [t.data, r];
    case "thread.message.delta":
      if (!n) throw Error("Received a delta with no existing snapshot (there should be one from message creation)");
      let s = t.data;
      if (s.delta.content) for (const a of s.delta.content) if (a.index in n.content) {
        let o = n.content[a.index];
        n.content[a.index] = xe(this, mr, "m", xI).call(this, a, o);
      } else
        n.content[a.index] = a, r.push(a);
      return [n, r];
    case "thread.message.in_progress":
    case "thread.message.completed":
    case "thread.message.incomplete":
      if (n) return [n, r];
      throw Error("Received thread message event with no existing snapshot");
  }
  throw Error("Tried to accumulate a non-message event");
}, xI = function(t, n) {
  return dl.accumulateDelta(n, t);
}, kI = function(t) {
  switch (At(this, rm, t.data, "f"), t.event) {
    case "thread.run.created":
      break;
    case "thread.run.queued":
      break;
    case "thread.run.in_progress":
      break;
    case "thread.run.requires_action":
    case "thread.run.cancelled":
    case "thread.run.failed":
    case "thread.run.completed":
    case "thread.run.expired":
    case "thread.run.incomplete":
      At(this, fl, t.data, "f"), xe(this, hs, "f") && (this._emit("toolCallDone", xe(this, hs, "f")), At(this, hs, void 0, "f"));
      break;
    case "thread.run.cancelling":
      break;
  }
};
var fS = class extends Ve {
  constructor() {
    super(...arguments), this.steps = new S5(this._client);
  }
  create(e, t, n) {
    const { include: r, ...s } = t;
    return this._client.post(_e`/threads/${e}/runs`, {
      query: { include: r },
      body: s,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      stream: t.stream ?? !1,
      __synthesizeEventData: !0,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { thread_id: r } = t;
    return this._client.get(_e`/threads/${r}/runs/${e}`, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    const { thread_id: r, ...s } = t;
    return this._client.post(_e`/threads/${r}/runs/${e}`, {
      body: s,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/threads/${e}/runs`, Mn, {
      query: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  cancel(e, t, n) {
    const { thread_id: r } = t;
    return this._client.post(_e`/threads/${r}/runs/${e}/cancel`, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t, n);
    return await this.poll(r.id, { thread_id: e }, n);
  }
  createAndStream(e, t, n) {
    return jd.createAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
  async poll(e, t, n) {
    const r = rt([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const { data: s, response: a } = await this.retrieve(e, t, {
        ...n,
        headers: {
          ...n?.headers,
          ...r
        }
      }).withResponse();
      switch (s.status) {
        case "queued":
        case "in_progress":
        case "cancelling":
          let o = 5e3;
          if (n?.pollIntervalMs) o = n.pollIntervalMs;
          else {
            const l = a.headers.get("openai-poll-after-ms");
            if (l) {
              const i = parseInt(l);
              isNaN(i) || (o = i);
            }
          }
          await Yf(o);
          break;
        case "requires_action":
        case "incomplete":
        case "cancelled":
        case "completed":
        case "failed":
        case "expired":
          return s;
      }
    }
  }
  stream(e, t, n) {
    return jd.createAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
  submitToolOutputs(e, t, n) {
    const { thread_id: r, ...s } = t;
    return this._client.post(_e`/threads/${r}/runs/${e}/submit_tool_outputs`, {
      body: s,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      stream: t.stream ?? !1,
      __synthesizeEventData: !0,
      __security: { bearerAuth: !0 }
    });
  }
  async submitToolOutputsAndPoll(e, t, n) {
    const r = await this.submitToolOutputs(e, t, n);
    return await this.poll(r.id, t, n);
  }
  submitToolOutputsStream(e, t, n) {
    return jd.createToolAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
};
fS.Steps = S5;
var wv = class extends Ve {
  constructor() {
    super(...arguments), this.runs = new fS(this._client), this.messages = new w5(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/threads", {
      body: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/threads/${e}`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/threads/${e}`, {
      body: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/threads/${e}`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  createAndRun(e, t) {
    return this._client.post("/threads/runs", {
      body: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      stream: e.stream ?? !1,
      __synthesizeEventData: !0,
      __security: { bearerAuth: !0 }
    });
  }
  async createAndRunPoll(e, t) {
    const n = await this.createAndRun(e, t);
    return await this.runs.poll(n.id, { thread_id: n.thread_id }, t);
  }
  createAndRunStream(e, t) {
    return jd.createThreadAssistantStream(e, this._client.beta.threads, t);
  }
};
wv.Runs = fS;
wv.Messages = w5;
var mc = class extends Ve {
  constructor() {
    super(...arguments), this.realtime = new _v(this._client), this.chatkit = new bv(this._client), this.assistants = new g5(this._client), this.threads = new wv(this._client);
  }
};
mc.Realtime = _v;
mc.ChatKit = bv;
mc.Assistants = g5;
mc.Threads = wv;
var A5 = class extends Ve {
  create(e, t) {
    return this._client.post("/completions", {
      body: e,
      ...t,
      stream: e.stream ?? !1,
      __security: { bearerAuth: !0 }
    });
  }
}, T5 = class extends Ve {
  retrieve(e, t, n) {
    const { container_id: r } = t;
    return this._client.get(_e`/containers/${r}/files/${e}/content`, {
      ...n,
      headers: rt([{ Accept: "application/binary" }, n?.headers]),
      __security: { bearerAuth: !0 },
      __binaryResponse: !0
    });
  }
}, hS = class extends Ve {
  constructor() {
    super(...arguments), this.content = new T5(this._client);
  }
  create(e, t, n) {
    return this._client.post(_e`/containers/${e}/files`, vv({
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  retrieve(e, t, n) {
    const { container_id: r } = t;
    return this._client.get(_e`/containers/${r}/files/${e}`, {
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/containers/${e}/files`, Mn, {
      query: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { container_id: r } = t;
    return this._client.delete(_e`/containers/${r}/files/${e}`, {
      ...n,
      headers: rt([{ Accept: "*/*" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
};
hS.Content = T5;
var pS = class extends Ve {
  constructor() {
    super(...arguments), this.files = new hS(this._client);
  }
  create(e, t) {
    return this._client.post("/containers", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/containers/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/containers", Mn, {
      query: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/containers/${e}`, {
      ...t,
      headers: rt([{ Accept: "*/*" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
};
pS.Files = hS;
var E5 = class extends Ve {
  create(e, t, n) {
    const { include: r, ...s } = t;
    return this._client.post(_e`/conversations/${e}/items`, {
      query: { include: r },
      body: s,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { conversation_id: r, ...s } = t;
    return this._client.get(_e`/conversations/${r}/items/${e}`, {
      query: s,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/conversations/${e}/items`, _r, {
      query: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { conversation_id: r } = t;
    return this._client.delete(_e`/conversations/${r}/items/${e}`, {
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
}, mS = class extends Ve {
  constructor() {
    super(...arguments), this.items = new E5(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/conversations", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/conversations/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/conversations/${e}`, {
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/conversations/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
};
mS.Items = E5;
var C5 = class extends Ve {
  create(e, t) {
    const n = !!e.encoding_format;
    let r = n ? e.encoding_format : "base64";
    n && pr(this._client).debug("embeddings/user defined encoding_format:", e.encoding_format);
    const s = this._client.post("/embeddings", {
      body: {
        ...e,
        encoding_format: r
      },
      ...t,
      __security: { bearerAuth: !0 }
    });
    return n ? s : (pr(this._client).debug("embeddings/decoding base64 embeddings from base64"), s._thenUnwrap((a) => (a && a.data && a.data.forEach((o) => {
      const l = o.embedding;
      o.embedding = RQ(l);
    }), a)));
  }
}, I5 = class extends Ve {
  retrieve(e, t, n) {
    const { eval_id: r, run_id: s } = t;
    return this._client.get(_e`/evals/${r}/runs/${s}/output_items/${e}`, {
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t, n) {
    const { eval_id: r, ...s } = t;
    return this._client.getAPIList(_e`/evals/${r}/runs/${e}/output_items`, Mn, {
      query: s,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
}, gS = class extends Ve {
  constructor() {
    super(...arguments), this.outputItems = new I5(this._client);
  }
  create(e, t, n) {
    return this._client.post(_e`/evals/${e}/runs`, {
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { eval_id: r } = t;
    return this._client.get(_e`/evals/${r}/runs/${e}`, {
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/evals/${e}/runs`, Mn, {
      query: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { eval_id: r } = t;
    return this._client.delete(_e`/evals/${r}/runs/${e}`, {
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  cancel(e, t, n) {
    const { eval_id: r } = t;
    return this._client.post(_e`/evals/${r}/runs/${e}`, {
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
};
gS.OutputItems = I5;
var vS = class extends Ve {
  constructor() {
    super(...arguments), this.runs = new gS(this._client);
  }
  create(e, t) {
    return this._client.post("/evals", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/evals/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/evals/${e}`, {
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/evals", Mn, {
      query: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/evals/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
};
vS.Runs = gS;
var x5 = class extends Ve {
  create(e, t) {
    return this._client.post("/files", $a({
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(_e`/files/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/files", Mn, {
      query: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/files/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  content(e, t) {
    return this._client.get(_e`/files/${e}/content`, {
      ...t,
      headers: rt([{ Accept: "application/binary" }, t?.headers]),
      __security: { bearerAuth: !0 },
      __binaryResponse: !0
    });
  }
  async waitForProcessing(e, { pollInterval: t = 5e3, maxWait: n = 1800 * 1e3 } = {}) {
    const r = /* @__PURE__ */ new Set([
      "processed",
      "error",
      "deleted"
    ]), s = Date.now();
    let a = await this.retrieve(e);
    for (; !a.status || !r.has(a.status); )
      if (await Yf(t), a = await this.retrieve(e), Date.now() - s > n) throw new Zw({ message: `Giving up on waiting for file ${e} to finish processing after ${n} milliseconds.` });
    return a;
  }
}, k5 = class extends Ve {
}, M5 = class extends Ve {
  run(e, t) {
    return this._client.post("/fine_tuning/alpha/graders/run", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  validate(e, t) {
    return this._client.post("/fine_tuning/alpha/graders/validate", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
}, yS = class extends Ve {
  constructor() {
    super(...arguments), this.graders = new M5(this._client);
  }
};
yS.Graders = M5;
var P5 = class extends Ve {
  create(e, t, n) {
    return this._client.getAPIList(_e`/fine_tuning/checkpoints/${e}/permissions`, Si, {
      body: t,
      method: "post",
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  retrieve(e, t = {}, n) {
    return this._client.get(_e`/fine_tuning/checkpoints/${e}/permissions`, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/fine_tuning/checkpoints/${e}/permissions`, _r, {
      query: t,
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { fine_tuned_model_checkpoint: r } = t;
    return this._client.delete(_e`/fine_tuning/checkpoints/${r}/permissions/${e}`, {
      ...n,
      __security: { adminAPIKeyAuth: !0 }
    });
  }
}, _S = class extends Ve {
  constructor() {
    super(...arguments), this.permissions = new P5(this._client);
  }
};
_S.Permissions = P5;
var R5 = class extends Ve {
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/fine_tuning/jobs/${e}/checkpoints`, Mn, {
      query: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
}, bS = class extends Ve {
  constructor() {
    super(...arguments), this.checkpoints = new R5(this._client);
  }
  create(e, t) {
    return this._client.post("/fine_tuning/jobs", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/fine_tuning/jobs/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/fine_tuning/jobs", Mn, {
      query: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  cancel(e, t) {
    return this._client.post(_e`/fine_tuning/jobs/${e}/cancel`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  listEvents(e, t = {}, n) {
    return this._client.getAPIList(_e`/fine_tuning/jobs/${e}/events`, Mn, {
      query: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  pause(e, t) {
    return this._client.post(_e`/fine_tuning/jobs/${e}/pause`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  resume(e, t) {
    return this._client.post(_e`/fine_tuning/jobs/${e}/resume`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
};
bS.Checkpoints = R5;
var gc = class extends Ve {
  constructor() {
    super(...arguments), this.methods = new k5(this._client), this.jobs = new bS(this._client), this.checkpoints = new _S(this._client), this.alpha = new yS(this._client);
  }
};
gc.Methods = k5;
gc.Jobs = bS;
gc.Checkpoints = _S;
gc.Alpha = yS;
var N5 = class extends Ve {
}, wS = class extends Ve {
  constructor() {
    super(...arguments), this.graderModels = new N5(this._client);
  }
};
wS.GraderModels = N5;
var $5 = class extends Ve {
  createVariation(e, t) {
    return this._client.post("/images/variations", $a({
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  edit(e, t) {
    return this._client.post("/images/edits", $a({
      body: e,
      ...t,
      stream: e.stream ?? !1,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  generate(e, t) {
    return this._client.post("/images/generations", {
      body: e,
      ...t,
      stream: e.stream ?? !1,
      __security: { bearerAuth: !0 }
    });
  }
}, L5 = class extends Ve {
  retrieve(e, t) {
    return this._client.get(_e`/models/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  list(e) {
    return this._client.getAPIList("/models", Si, {
      ...e,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/models/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
}, D5 = class extends Ve {
  create(e, t) {
    return this._client.post("/moderations", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
}, O5 = class extends Ve {
  accept(e, t, n) {
    return this._client.post(_e`/realtime/calls/${e}/accept`, {
      body: t,
      ...n,
      headers: rt([{ Accept: "*/*" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  hangup(e, t) {
    return this._client.post(_e`/realtime/calls/${e}/hangup`, {
      ...t,
      headers: rt([{ Accept: "*/*" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  refer(e, t, n) {
    return this._client.post(_e`/realtime/calls/${e}/refer`, {
      body: t,
      ...n,
      headers: rt([{ Accept: "*/*" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  reject(e, t = {}, n) {
    return this._client.post(_e`/realtime/calls/${e}/reject`, {
      body: t,
      ...n,
      headers: rt([{ Accept: "*/*" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, F5 = class extends Ve {
  create(e, t) {
    return this._client.post("/realtime/client_secrets", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
}, Sv = class extends Ve {
  constructor() {
    super(...arguments), this.clientSecrets = new F5(this._client), this.calls = new O5(this._client);
  }
};
Sv.ClientSecrets = F5;
Sv.Calls = O5;
function NQ(e, t) {
  return !t || !LQ(t) ? {
    ...e,
    output_parsed: null,
    output: e.output.map((n) => n.type === "function_call" ? {
      ...n,
      parsed_arguments: null
    } : n.type === "message" ? {
      ...n,
      content: n.content.map((r) => ({
        ...r,
        parsed: null
      }))
    } : n)
  } : B5(e, t);
}
function B5(e, t) {
  const n = e.output.map((s) => {
    if (s.type === "function_call") return {
      ...s,
      parsed_arguments: FQ(t, s)
    };
    if (s.type === "message") {
      const a = s.content.map((o) => o.type === "output_text" ? {
        ...o,
        parsed: $Q(t, o.text)
      } : o);
      return {
        ...s,
        content: a
      };
    }
    return s;
  }), r = Object.assign({}, e, { output: n });
  return Object.getOwnPropertyDescriptor(e, "output_text") || z0(r), Object.defineProperty(r, "output_parsed", {
    enumerable: !0,
    get() {
      for (const s of r.output)
        if (s.type === "message") {
          for (const a of s.content) if (a.type === "output_text" && a.parsed !== null) return a.parsed;
        }
      return null;
    }
  }), r;
}
function $Q(e, t) {
  return e.text?.format?.type !== "json_schema" ? null : "$parseRaw" in e.text?.format ? (e.text?.format).$parseRaw(t) : JSON.parse(t);
}
function LQ(e) {
  return !!rS(e.text?.format);
}
function DQ(e) {
  return e?.$brand === "auto-parseable-tool";
}
function OQ(e, t) {
  return e.find((n) => n.type === "function" && n.name === t);
}
function FQ(e, t) {
  const n = OQ(e.tools ?? [], t.name);
  return {
    ...t,
    ...t,
    parsed_arguments: DQ(n) ? n.$parseRaw(t.arguments) : n?.strict ? JSON.parse(t.arguments) : null
  };
}
function z0(e) {
  const t = [];
  for (const n of e.output)
    if (n.type === "message")
      for (const r of n.content) r.type === "output_text" && t.push(r.text);
  e.output_text = t.join("");
}
var su, bp, Go, wp, MI, PI, RI, NI, BQ = class U5 extends aS {
  constructor(t) {
    super(), su.add(this), bp.set(this, void 0), Go.set(this, void 0), wp.set(this, void 0), At(this, bp, t, "f");
  }
  static createResponse(t, n, r) {
    const s = new U5(n);
    return s._run(() => s._createOrRetrieveResponse(t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  async _createOrRetrieveResponse(t, n, r) {
    const s = r?.signal;
    s && (s.aborted && this.controller.abort(), s.addEventListener("abort", () => this.controller.abort())), xe(this, su, "m", MI).call(this);
    let a, o = null;
    "response_id" in n ? (a = await t.responses.retrieve(n.response_id, { stream: !0 }, {
      ...r,
      signal: this.controller.signal,
      stream: !0
    }), o = n.starting_after ?? null) : a = await t.responses.create({
      ...n,
      stream: !0
    }, {
      ...r,
      signal: this.controller.signal
    }), this._connected();
    for await (const l of a) xe(this, su, "m", PI).call(this, l, o);
    if (a.controller.signal?.aborted) throw new Bs();
    return xe(this, su, "m", RI).call(this);
  }
  [(bp = /* @__PURE__ */ new WeakMap(), Go = /* @__PURE__ */ new WeakMap(), wp = /* @__PURE__ */ new WeakMap(), su = /* @__PURE__ */ new WeakSet(), MI = function() {
    this.ended || At(this, Go, void 0, "f");
  }, PI = function(n, r) {
    if (this.ended) return;
    const s = (o, l) => {
      (r == null || l.sequence_number > r) && this._emit(o, l);
    }, a = xe(this, su, "m", NI).call(this, n);
    switch (s("event", n), n.type) {
      case "response.output_text.delta": {
        const o = a.output[n.output_index];
        if (!o) throw new gt(`missing output at index ${n.output_index}`);
        if (o.type === "message") {
          const l = o.content[n.content_index];
          if (!l) throw new gt(`missing content at index ${n.content_index}`);
          if (l.type !== "output_text") throw new gt(`expected content to be 'output_text', got ${l.type}`);
          s("response.output_text.delta", {
            ...n,
            snapshot: l.text
          });
        }
        break;
      }
      case "response.function_call_arguments.delta": {
        const o = a.output[n.output_index];
        if (!o) throw new gt(`missing output at index ${n.output_index}`);
        o.type === "function_call" && s("response.function_call_arguments.delta", {
          ...n,
          snapshot: o.arguments
        });
        break;
      }
      default:
        s(n.type, n);
        break;
    }
  }, RI = function() {
    if (this.ended) throw new gt("stream has ended, this shouldn't happen");
    const n = xe(this, Go, "f");
    if (!n) throw new gt("request ended without sending any events");
    At(this, Go, void 0, "f");
    const r = UQ(n, xe(this, bp, "f"));
    return At(this, wp, r, "f"), r;
  }, NI = function(n) {
    let r = xe(this, Go, "f");
    if (!r) {
      if (n.type !== "response.created") throw new gt(`When snapshot hasn't been set yet, expected 'response.created' event, got ${n.type}`);
      return r = At(this, Go, n.response, "f"), r;
    }
    switch (n.type) {
      case "response.output_item.added":
        r.output.push(n.item);
        break;
      case "response.content_part.added": {
        const s = r.output[n.output_index];
        if (!s) throw new gt(`missing output at index ${n.output_index}`);
        const a = s.type, o = n.part;
        a === "message" && o.type !== "reasoning_text" ? s.content.push(o) : a === "reasoning" && o.type === "reasoning_text" && (s.content || (s.content = []), s.content.push(o));
        break;
      }
      case "response.output_text.delta": {
        const s = r.output[n.output_index];
        if (!s) throw new gt(`missing output at index ${n.output_index}`);
        if (s.type === "message") {
          const a = s.content[n.content_index];
          if (!a) throw new gt(`missing content at index ${n.content_index}`);
          if (a.type !== "output_text") throw new gt(`expected content to be 'output_text', got ${a.type}`);
          a.text += n.delta;
        }
        break;
      }
      case "response.function_call_arguments.delta": {
        const s = r.output[n.output_index];
        if (!s) throw new gt(`missing output at index ${n.output_index}`);
        s.type === "function_call" && (s.arguments += n.delta);
        break;
      }
      case "response.reasoning_text.delta": {
        const s = r.output[n.output_index];
        if (!s) throw new gt(`missing output at index ${n.output_index}`);
        if (s.type === "reasoning") {
          const a = s.content?.[n.content_index];
          if (!a) throw new gt(`missing content at index ${n.content_index}`);
          if (a.type !== "reasoning_text") throw new gt(`expected content to be 'reasoning_text', got ${a.type}`);
          a.text += n.delta;
        }
        break;
      }
      case "response.completed":
        At(this, Go, n.response, "f");
        break;
    }
    return r;
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("event", (s) => {
      const a = n.shift();
      a ? a.resolve(s) : t.push(s);
    }), this.on("end", () => {
      r = !0;
      for (const s of n) s.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (s) => {
      r = !0;
      for (const a of n) a.reject(s);
      n.length = 0;
    }), this.on("error", (s) => {
      r = !0;
      for (const a of n) a.reject(s);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((s, a) => n.push({
        resolve: s,
        reject: a
      })).then((s) => s ? {
        value: s,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  async finalResponse() {
    await this.done();
    const t = xe(this, wp, "f");
    if (!t) throw new gt("stream ended without producing a ChatCompletion");
    return t;
  }
};
function UQ(e, t) {
  return NQ(e, t);
}
var q5 = class extends Ve {
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/responses/${e}/input_items`, Mn, {
      query: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
}, H5 = class extends Ve {
  count(e = {}, t) {
    return this._client.post("/responses/input_tokens", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
}, Av = class extends Ve {
  constructor() {
    super(...arguments), this.inputItems = new q5(this._client), this.inputTokens = new H5(this._client);
  }
  create(e, t) {
    return this._client.post("/responses", {
      body: e,
      ...t,
      stream: e.stream ?? !1,
      __security: { bearerAuth: !0 }
    })._thenUnwrap((n) => ("object" in n && n.object === "response" && z0(n), n));
  }
  retrieve(e, t = {}, n) {
    return this._client.get(_e`/responses/${e}`, {
      query: t,
      ...n,
      stream: t?.stream ?? !1,
      __security: { bearerAuth: !0 }
    })._thenUnwrap((r) => ("object" in r && r.object === "response" && z0(r), r));
  }
  delete(e, t) {
    return this._client.delete(_e`/responses/${e}`, {
      ...t,
      headers: rt([{ Accept: "*/*" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  parse(e, t) {
    return this._client.responses.create(e, t)._thenUnwrap((n) => B5(n, e));
  }
  stream(e, t) {
    return BQ.createResponse(this._client, e, t);
  }
  cancel(e, t) {
    return this._client.post(_e`/responses/${e}/cancel`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  compact(e, t) {
    return this._client.post("/responses/compact", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
};
Av.InputItems = q5;
Av.InputTokens = H5;
var z5 = class extends Ve {
  retrieve(e, t) {
    return this._client.get(_e`/skills/${e}/content`, {
      ...t,
      headers: rt([{ Accept: "application/binary" }, t?.headers]),
      __security: { bearerAuth: !0 },
      __binaryResponse: !0
    });
  }
}, K5 = class extends Ve {
  retrieve(e, t, n) {
    const { skill_id: r } = t;
    return this._client.get(_e`/skills/${r}/versions/${e}/content`, {
      ...n,
      headers: rt([{ Accept: "application/binary" }, n?.headers]),
      __security: { bearerAuth: !0 },
      __binaryResponse: !0
    });
  }
}, SS = class extends Ve {
  constructor() {
    super(...arguments), this.content = new K5(this._client);
  }
  create(e, t = {}, n) {
    return this._client.post(_e`/skills/${e}/versions`, vv({
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  retrieve(e, t, n) {
    const { skill_id: r } = t;
    return this._client.get(_e`/skills/${r}/versions/${e}`, {
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/skills/${e}/versions`, Mn, {
      query: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { skill_id: r } = t;
    return this._client.delete(_e`/skills/${r}/versions/${e}`, {
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
};
SS.Content = K5;
var Tv = class extends Ve {
  constructor() {
    super(...arguments), this.content = new z5(this._client), this.versions = new SS(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/skills", vv({
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(_e`/skills/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/skills/${e}`, {
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/skills", Mn, {
      query: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/skills/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
};
Tv.Content = z5;
Tv.Versions = SS;
var W5 = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/uploads/${e}/parts`, $a({
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
}, AS = class extends Ve {
  constructor() {
    super(...arguments), this.parts = new W5(this._client);
  }
  create(e, t) {
    return this._client.post("/uploads", {
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  cancel(e, t) {
    return this._client.post(_e`/uploads/${e}/cancel`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  complete(e, t, n) {
    return this._client.post(_e`/uploads/${e}/complete`, {
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    });
  }
};
AS.Parts = W5;
var qQ = async (e) => {
  const t = await Promise.allSettled(e), n = t.filter((s) => s.status === "rejected");
  if (n.length) {
    for (const s of n) console.error(s.reason);
    throw new Error(`${n.length} promise(s) failed - see the above errors`);
  }
  const r = [];
  for (const s of t) s.status === "fulfilled" && r.push(s.value);
  return r;
}, G5 = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/vector_stores/${e}/file_batches`, {
      body: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.get(_e`/vector_stores/${r}/file_batches/${e}`, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  cancel(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.post(_e`/vector_stores/${r}/file_batches/${e}/cancel`, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t);
    return await this.poll(e, r.id, n);
  }
  listFiles(e, t, n) {
    const { vector_store_id: r, ...s } = t;
    return this._client.getAPIList(_e`/vector_stores/${r}/file_batches/${e}/files`, Mn, {
      query: s,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  async poll(e, t, n) {
    const r = rt([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const { data: s, response: a } = await this.retrieve(t, { vector_store_id: e }, {
        ...n,
        headers: r
      }).withResponse();
      switch (s.status) {
        case "in_progress":
          let o = 5e3;
          if (n?.pollIntervalMs) o = n.pollIntervalMs;
          else {
            const l = a.headers.get("openai-poll-after-ms");
            if (l) {
              const i = parseInt(l);
              isNaN(i) || (o = i);
            }
          }
          await Yf(o);
          break;
        case "failed":
        case "cancelled":
        case "completed":
          return s;
      }
    }
  }
  async uploadAndPoll(e, { files: t, fileIds: n = [] }, r) {
    if (t == null || t.length == 0) throw new Error("No `files` provided to process. If you've already uploaded files you should use `.createAndPoll()` instead");
    const s = r?.maxConcurrency ?? 5, a = Math.min(s, t.length), o = this._client, l = t.values(), i = [...n];
    async function u(d) {
      for (let f of d) {
        const m = await o.files.create({
          file: f,
          purpose: "assistants"
        }, r);
        i.push(m.id);
      }
    }
    return await qQ(Array(a).fill(l).map(u)), await this.createAndPoll(e, { file_ids: i });
  }
}, V5 = class extends Ve {
  create(e, t, n) {
    return this._client.post(_e`/vector_stores/${e}/files`, {
      body: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.get(_e`/vector_stores/${r}/files/${e}`, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    const { vector_store_id: r, ...s } = t;
    return this._client.post(_e`/vector_stores/${r}/files/${e}`, {
      body: s,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(_e`/vector_stores/${e}/files`, Mn, {
      query: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.delete(_e`/vector_stores/${r}/files/${e}`, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t, n);
    return await this.poll(e, r.id, n);
  }
  async poll(e, t, n) {
    const r = rt([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const s = await this.retrieve(t, { vector_store_id: e }, {
        ...n,
        headers: r
      }).withResponse(), a = s.data;
      switch (a.status) {
        case "in_progress":
          let o = 5e3;
          if (n?.pollIntervalMs) o = n.pollIntervalMs;
          else {
            const l = s.response.headers.get("openai-poll-after-ms");
            if (l) {
              const i = parseInt(l);
              isNaN(i) || (o = i);
            }
          }
          await Yf(o);
          break;
        case "failed":
        case "completed":
          return a;
      }
    }
  }
  async upload(e, t, n) {
    const r = await this._client.files.create({
      file: t,
      purpose: "assistants"
    }, n);
    return this.create(e, { file_id: r.id }, n);
  }
  async uploadAndPoll(e, t, n) {
    const r = await this.upload(e, t, n);
    return await this.poll(e, r.id, n);
  }
  content(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.getAPIList(_e`/vector_stores/${r}/files/${e}/content`, Si, {
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
}, Ev = class extends Ve {
  constructor() {
    super(...arguments), this.files = new V5(this._client), this.fileBatches = new G5(this._client);
  }
  create(e, t) {
    return this._client.post("/vector_stores", {
      body: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  retrieve(e, t) {
    return this._client.get(_e`/vector_stores/${e}`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  update(e, t, n) {
    return this._client.post(_e`/vector_stores/${e}`, {
      body: t,
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/vector_stores", Mn, {
      query: e,
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/vector_stores/${e}`, {
      ...t,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
  search(e, t, n) {
    return this._client.getAPIList(_e`/vector_stores/${e}/search`, Si, {
      body: t,
      method: "post",
      ...n,
      headers: rt([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      __security: { bearerAuth: !0 }
    });
  }
};
Ev.Files = V5;
Ev.FileBatches = G5;
var J5 = class extends Ve {
  create(e, t) {
    return this._client.post("/videos", $a({
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(_e`/videos/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/videos", _r, {
      query: e,
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  delete(e, t) {
    return this._client.delete(_e`/videos/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  createCharacter(e, t) {
    return this._client.post("/videos/characters", $a({
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  downloadContent(e, t = {}, n) {
    return this._client.get(_e`/videos/${e}/content`, {
      query: t,
      ...n,
      headers: rt([{ Accept: "application/binary" }, n?.headers]),
      __security: { bearerAuth: !0 },
      __binaryResponse: !0
    });
  }
  edit(e, t) {
    return this._client.post("/videos/edits", $a({
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  extend(e, t) {
    return this._client.post("/videos/extensions", $a({
      body: e,
      ...t,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
  getCharacter(e, t) {
    return this._client.get(_e`/videos/characters/${e}`, {
      ...t,
      __security: { bearerAuth: !0 }
    });
  }
  remix(e, t, n) {
    return this._client.post(_e`/videos/${e}/remix`, vv({
      body: t,
      ...n,
      __security: { bearerAuth: !0 }
    }, this._client));
  }
}, gu, Y5, sm, X5 = class extends Ve {
  constructor() {
    super(...arguments), gu.add(this);
  }
  async unwrap(e, t, n = this._client.webhookSecret, r = 300) {
    return await this.verifySignature(e, t, n, r), JSON.parse(e);
  }
  async verifySignature(e, t, n = this._client.webhookSecret, r = 300) {
    if (typeof crypto > "u" || typeof crypto.subtle.importKey != "function" || typeof crypto.subtle.verify != "function") throw new Error("Webhook signature verification is only supported when the `crypto` global is defined");
    xe(this, gu, "m", Y5).call(this, n);
    const s = rt([t]).values, a = xe(this, gu, "m", sm).call(this, s, "webhook-signature"), o = xe(this, gu, "m", sm).call(this, s, "webhook-timestamp"), l = xe(this, gu, "m", sm).call(this, s, "webhook-id"), i = parseInt(o, 10);
    if (isNaN(i)) throw new wd("Invalid webhook timestamp format");
    const u = Math.floor(Date.now() / 1e3);
    if (u - i > r) throw new wd("Webhook timestamp is too old");
    if (i > u + r) throw new wd("Webhook timestamp is too new");
    const d = a.split(" ").map((y) => y.startsWith("v1,") ? y.substring(3) : y), f = n.startsWith("whsec_") ? Buffer.from(n.replace("whsec_", ""), "base64") : Buffer.from(n, "utf-8"), m = l ? `${l}.${o}.${e}` : `${o}.${e}`, v = await crypto.subtle.importKey("raw", f, {
      name: "HMAC",
      hash: "SHA-256"
    }, !1, ["verify"]);
    for (const y of d) try {
      const S = Buffer.from(y, "base64");
      if (await crypto.subtle.verify("HMAC", v, S, new TextEncoder().encode(m))) return;
    } catch {
      continue;
    }
    throw new wd("The given webhook signature does not match the expected signature");
  }
};
gu = /* @__PURE__ */ new WeakSet(), Y5 = function(t) {
  if (typeof t != "string" || t.length === 0) throw new Error("The webhook secret must either be set using the env var, OPENAI_WEBHOOK_SECRET, on the client class, OpenAI({ webhookSecret: '123' }), or passed to this function");
}, sm = function(t, n) {
  if (!t) throw new Error("Headers are required");
  const r = t.get(n);
  if (r == null) throw new Error(`Missing required header: ${n}`);
  return r;
};
var K0, TS, am, j5, HQ = "workload-identity-auth", Nt = class {
  constructor({ baseURL: e = Wo("OPENAI_BASE_URL"), apiKey: t = Wo("OPENAI_API_KEY") ?? null, adminAPIKey: n = Wo("OPENAI_ADMIN_KEY") ?? null, organization: r = Wo("OPENAI_ORG_ID") ?? null, project: s = Wo("OPENAI_PROJECT_ID") ?? null, webhookSecret: a = Wo("OPENAI_WEBHOOK_SECRET") ?? null, workloadIdentity: o, ...l } = {}) {
    K0.add(this), am.set(this, void 0), this.completions = new A5(this), this.chat = new iS(this), this.embeddings = new C5(this), this.files = new x5(this), this.images = new $5(this), this.audio = new jf(this), this.moderations = new D5(this), this.models = new L5(this), this.fineTuning = new gc(this), this.graders = new wS(this), this.vectorStores = new Ev(this), this.webhooks = new X5(this), this.beta = new mc(this), this.batches = new m5(this), this.uploads = new AS(this), this.admin = new dS(this), this.responses = new Av(this), this.realtime = new Sv(this), this.conversations = new mS(this), this.evals = new vS(this), this.containers = new pS(this), this.skills = new Tv(this), this.videos = new J5(this);
    const i = {
      apiKey: t,
      adminAPIKey: n,
      organization: r,
      project: s,
      webhookSecret: a,
      workloadIdentity: o,
      ...l,
      baseURL: e || "https://api.openai.com/v1"
    };
    if (t && o) throw new gt("The `apiKey` and `workloadIdentity` options are mutually exclusive");
    if (!t && !n && !o) throw new gt("Missing credentials. Please pass an `apiKey`, `workloadIdentity`, `adminAPIKey`, or set the `OPENAI_API_KEY` or `OPENAI_ADMIN_KEY` environment variable.");
    if (!i.dangerouslyAllowBrowser && Uj()) throw new gt(`It looks like you're running in a browser-like environment.

This is disabled by default, as it risks exposing your secret API credentials to attackers.
If you understand the risks and have appropriate mitigations in place,
you can set the \`dangerouslyAllowBrowser\` option to \`true\`, e.g.,

new OpenAI({ apiKey, dangerouslyAllowBrowser: true });

https://help.openai.com/en/articles/5112595-best-practices-for-api-key-safety
`);
    this.baseURL = i.baseURL, this.timeout = i.timeout ?? TS.DEFAULT_TIMEOUT, this.logger = i.logger ?? console;
    const u = "warn";
    this.logLevel = u, this.logLevel = vI(i.logLevel, "ClientOptions.logLevel", this) ?? vI(Wo("OPENAI_LOG"), "process.env['OPENAI_LOG']", this) ?? u, this.fetchOptions = i.fetchOptions, this.maxRetries = i.maxRetries ?? 2, this.fetch = i.fetch ?? mR(), At(this, am, Wj, "f");
    const d = Wo("OPENAI_CUSTOM_HEADERS");
    if (d) {
      const f = {};
      for (const m of d.split(`
`)) {
        const v = m.indexOf(":");
        v >= 0 && (f[m.substring(0, v).trim()] = m.substring(v + 1).trim());
      }
      i.defaultHeaders = rt([f, i.defaultHeaders]);
    }
    this._options = i, o && (this._workloadIdentityAuth = new uQ(o, this.fetch)), this.apiKey = typeof t == "string" ? t : null, this.adminAPIKey = n, this.organization = r, this.project = s, this.webhookSecret = a;
  }
  withOptions(e) {
    return new this.constructor({
      ...this._options,
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this._options.apiKey,
      adminAPIKey: this.adminAPIKey,
      workloadIdentity: this._options.workloadIdentity,
      organization: this.organization,
      project: this.project,
      webhookSecret: this.webhookSecret,
      ...e
    });
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: e, nulls: t }, n = {
    bearerAuth: !0,
    adminAPIKeyAuth: !0
  }) {
    if (!(e.get("authorization") || e.get("api-key")) && !(t.has("authorization") || t.has("api-key")) && !(this._workloadIdentityAuth && n.bearerAuth))
      throw new Error('Could not resolve authentication method. Expected either apiKey or adminAPIKey to be set. Or for one of the "Authorization" or "api-key" headers to be explicitly omitted');
  }
  async authHeaders(e, t = {
    bearerAuth: !0,
    adminAPIKeyAuth: !0
  }) {
    return rt([t.bearerAuth ? await this.bearerAuth(e) : null, t.adminAPIKeyAuth ? await this.adminAPIKeyAuth(e) : null]);
  }
  async bearerAuth(e) {
    if (this._workloadIdentityAuth) return rt([{ Authorization: `Bearer ${await this._workloadIdentityAuth.getToken()}` }]);
    if (this.apiKey != null)
      return rt([{ Authorization: `Bearer ${this.apiKey}` }]);
  }
  async adminAPIKeyAuth(e) {
    if (this.adminAPIKey != null)
      return rt([{ Authorization: `Bearer ${this.adminAPIKey}` }]);
  }
  stringifyQuery(e) {
    return jj(e);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${pu}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${rR()}`;
  }
  makeStatusError(e, t, n, r) {
    return Mr.generate(e, t, n, r);
  }
  async _callApiKey() {
    const e = this._options.apiKey;
    if (typeof e != "function") return !1;
    let t;
    try {
      t = await e();
    } catch (n) {
      throw n instanceof gt ? n : new gt(`Failed to get token from 'apiKey' function: ${n.message}`, { cause: n });
    }
    if (typeof t != "string" || !t) throw new gt(`Expected 'apiKey' function argument to return a string but it returned ${t}`);
    return this.apiKey = t, !0;
  }
  buildURL(e, t, n) {
    const r = !xe(this, K0, "m", j5).call(this) && n || this.baseURL, s = Dj(e) ? new URL(e) : new URL(r + (r.endsWith("/") && e.startsWith("/") ? e.slice(1) : e)), a = this.defaultQuery(), o = Object.fromEntries(s.searchParams);
    return (!oI(a) || !oI(o)) && (t = {
      ...o,
      ...a,
      ...t
    }), typeof t == "object" && t && !Array.isArray(t) && (s.search = this.stringifyQuery(t)), s.toString();
  }
  async prepareOptions(e) {
    (e.__security ?? { bearerAuth: !0 }).bearerAuth && await this._callApiKey();
  }
  async prepareRequest(e, { url: t, options: n }) {
  }
  get(e, t) {
    return this.methodRequest("get", e, t);
  }
  post(e, t) {
    return this.methodRequest("post", e, t);
  }
  patch(e, t) {
    return this.methodRequest("patch", e, t);
  }
  put(e, t) {
    return this.methodRequest("put", e, t);
  }
  delete(e, t) {
    return this.methodRequest("delete", e, t);
  }
  methodRequest(e, t, n) {
    return this.request(Promise.resolve(n).then((r) => ({
      method: e,
      path: t,
      ...r
    })));
  }
  request(e, t = null) {
    return new CR(this, this.makeRequest(e, t, void 0));
  }
  async makeRequest(e, t, n) {
    const r = await e, s = r.maxRetries ?? this.maxRetries;
    t == null && (t = s), await this.prepareOptions(r);
    const { req: a, url: o, timeout: l } = await this.buildRequest(r, { retryCount: s - t });
    await this.prepareRequest(a, {
      url: o,
      options: r
    });
    const i = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), u = n === void 0 ? "" : `, retryOf: ${n}`, d = Date.now();
    if (pr(this).debug(`[${i}] sending request`, Zi({
      retryOfRequestLogID: n,
      method: r.method,
      url: o,
      options: r,
      headers: a.headers
    })), r.signal?.aborted) throw new Bs();
    const f = r.__security ?? { bearerAuth: !0 }, m = new AbortController(), v = await this.fetchWithAuth(o, a, l, m, f).catch(k0), y = Date.now();
    if (v instanceof globalThis.Error) {
      const x = `retrying, ${t} attempts remaining`;
      if (r.signal?.aborted) throw new Bs();
      const I = x0(v) || /timed? ?out/i.test(String(v) + ("cause" in v ? String(v.cause) : ""));
      if (t)
        return pr(this).info(`[${i}] connection ${I ? "timed out" : "failed"} - ${x}`), pr(this).debug(`[${i}] connection ${I ? "timed out" : "failed"} (${x})`, Zi({
          retryOfRequestLogID: n,
          url: o,
          durationMs: y - d,
          message: v.message
        })), this.retryRequest(r, t, n ?? i);
      throw pr(this).info(`[${i}] connection ${I ? "timed out" : "failed"} - error; no more retries left`), pr(this).debug(`[${i}] connection ${I ? "timed out" : "failed"} (error; no more retries left)`, Zi({
        retryOfRequestLogID: n,
        url: o,
        durationMs: y - d,
        message: v.message
      })), v instanceof pR || v instanceof $j ? v : I ? new Zw() : new pv({
        message: zQ(v),
        cause: v
      });
    }
    const S = `[${i}${u}${[...v.headers.entries()].filter(([x]) => x === "x-request-id").map(([x, I]) => ", " + x + ": " + JSON.stringify(I)).join("")}] ${a.method} ${o} ${v.ok ? "succeeded" : "failed"} with status ${v.status} in ${y - d}ms`;
    if (!v.ok) {
      if (v.status === 401 && this._workloadIdentityAuth && f.bearerAuth && !r.__metadata?.hasStreamingBody && !r.__metadata?.workloadIdentityTokenRefreshed)
        return await cI(v.body), this._workloadIdentityAuth.invalidateToken(), this.makeRequest({
          ...r,
          __metadata: {
            ...r.__metadata,
            workloadIdentityTokenRefreshed: !0
          }
        }, t, n ?? i);
      const x = await this.shouldRetry(v);
      if (t && x) {
        const p = `retrying, ${t} attempts remaining`;
        return await cI(v.body), pr(this).info(`${S} - ${p}`), pr(this).debug(`[${i}] response error (${p})`, Zi({
          retryOfRequestLogID: n,
          url: v.url,
          status: v.status,
          headers: v.headers,
          durationMs: y - d
        })), this.retryRequest(r, t, n ?? i, v.headers);
      }
      const I = x ? "error; no more retries left" : "error; not retryable";
      pr(this).info(`${S} - ${I}`);
      const E = await v.text().catch((p) => k0(p).message), N = Bj(E), $ = N ? void 0 : E;
      throw pr(this).debug(`[${i}] response error (${I})`, Zi({
        retryOfRequestLogID: n,
        url: v.url,
        status: v.status,
        headers: v.headers,
        message: $,
        durationMs: Date.now() - d
      })), this.makeStatusError(v.status, N, $, v.headers);
    }
    return pr(this).info(S), pr(this).debug(`[${i}] response start`, Zi({
      retryOfRequestLogID: n,
      url: v.url,
      status: v.status,
      headers: v.headers,
      durationMs: y - d
    })), {
      response: v,
      options: r,
      controller: m,
      requestLogID: i,
      retryOfRequestLogID: n,
      startTime: d
    };
  }
  getAPIList(e, t, n) {
    return this.requestAPIList(t, n && "then" in n ? n.then((r) => ({
      method: "get",
      path: e,
      ...r
    })) : {
      method: "get",
      path: e,
      ...n
    });
  }
  requestAPIList(e, t) {
    const n = this.makeRequest(t, null, void 0);
    return new oQ(this, n, e);
  }
  async fetchWithAuth(e, t, n, r, s = {
    bearerAuth: !0,
    adminAPIKeyAuth: !0
  }) {
    if (this._workloadIdentityAuth && s.bearerAuth) {
      const a = t.headers, o = a.get("Authorization");
      if (!o || o === `Bearer ${HQ}`) {
        const l = await this._workloadIdentityAuth.getToken();
        a.set("Authorization", `Bearer ${l}`);
      }
    }
    return await this.fetchWithTimeout(e, t, n, r);
  }
  async fetchWithTimeout(e, t, n, r) {
    const { signal: s, method: a, ...o } = t || {}, l = this._makeAbort(r);
    s && s.addEventListener("abort", l, { once: !0 });
    const i = setTimeout(l, n), u = globalThis.ReadableStream && o.body instanceof globalThis.ReadableStream || typeof o.body == "object" && o.body !== null && Symbol.asyncIterator in o.body, d = {
      signal: r.signal,
      ...u ? { duplex: "half" } : {},
      method: "GET",
      ...o
    };
    a && (d.method = a.toUpperCase());
    try {
      return await this.fetch.call(void 0, e, d);
    } finally {
      clearTimeout(i);
    }
  }
  async shouldRetry(e) {
    const t = e.headers.get("x-should-retry");
    return t === "true" ? !0 : t === "false" ? !1 : e.status === 408 || e.status === 409 || e.status === 429 || e.status >= 500;
  }
  async retryRequest(e, t, n, r) {
    let s;
    const a = r?.get("retry-after-ms");
    if (a) {
      const l = parseFloat(a);
      Number.isNaN(l) || (s = l);
    }
    const o = r?.get("retry-after");
    if (o && !s) {
      const l = parseFloat(o);
      Number.isNaN(l) ? s = Date.parse(o) - Date.now() : s = l * 1e3;
    }
    if (s === void 0) {
      const l = e.maxRetries ?? this.maxRetries;
      s = this.calculateDefaultRetryTimeoutMillis(t, l);
    }
    return await Yf(s), this.makeRequest(e, t - 1, n);
  }
  calculateDefaultRetryTimeoutMillis(e, t) {
    const s = t - e;
    return Math.min(0.5 * Math.pow(2, s), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  async buildRequest(e, { retryCount: t = 0 } = {}) {
    const n = { ...e }, { method: r, path: s, query: a, defaultBaseURL: o } = n, l = this.buildURL(s, a, o);
    "timeout" in n && Fj("timeout", n.timeout), n.timeout = n.timeout ?? this.timeout;
    const { bodyHeaders: i, body: u, isStreamingBody: d } = this.buildBody({ options: n });
    return d && (e.__metadata = {
      ...e.__metadata,
      hasStreamingBody: !0
    }), {
      req: {
        method: r,
        headers: await this.buildHeaders({
          options: e,
          method: r,
          bodyHeaders: i,
          retryCount: t
        }),
        ...n.signal && { signal: n.signal },
        ...globalThis.ReadableStream && u instanceof globalThis.ReadableStream && { duplex: "half" },
        ...u && { body: u },
        ...this.fetchOptions ?? {},
        ...n.fetchOptions ?? {}
      },
      url: l,
      timeout: n.timeout
    };
  }
  async buildHeaders({ options: e, method: t, bodyHeaders: n, retryCount: r }) {
    let s = {};
    this.idempotencyHeader && t !== "get" && (e.idempotencyKey || (e.idempotencyKey = this.defaultIdempotencyKey()), s[this.idempotencyHeader] = e.idempotencyKey);
    const a = rt([
      s,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent(),
        "X-Stainless-Retry-Count": String(r),
        ...e.timeout ? { "X-Stainless-Timeout": String(Math.trunc(e.timeout / 1e3)) } : {},
        ...Kj(),
        "OpenAI-Organization": this.organization,
        "OpenAI-Project": this.project
      },
      await this.authHeaders(e, e.__security ?? { bearerAuth: !0 }),
      this._options.defaultHeaders,
      n,
      e.headers
    ]);
    return this.validateHeaders(a, e.__security ?? { bearerAuth: !0 }), a.values;
  }
  _makeAbort(e) {
    return () => e.abort();
  }
  buildBody({ options: { body: e, headers: t } }) {
    if (!e) return {
      bodyHeaders: void 0,
      body: void 0,
      isStreamingBody: !1
    };
    const n = rt([t]), r = typeof globalThis.ReadableStream < "u" && e instanceof globalThis.ReadableStream, s = !r && (typeof e == "string" || e instanceof ArrayBuffer || ArrayBuffer.isView(e) || typeof globalThis.Blob < "u" && e instanceof globalThis.Blob || e instanceof URLSearchParams || e instanceof FormData);
    return ArrayBuffer.isView(e) || e instanceof ArrayBuffer || e instanceof DataView || typeof e == "string" && n.values.has("content-type") || globalThis.Blob && e instanceof globalThis.Blob || e instanceof FormData || e instanceof URLSearchParams || r ? {
      bodyHeaders: void 0,
      body: e,
      isStreamingBody: !s
    } : typeof e == "object" && (Symbol.asyncIterator in e || Symbol.iterator in e && "next" in e && typeof e.next == "function") ? {
      bodyHeaders: void 0,
      body: vR(e),
      isStreamingBody: !0
    } : typeof e == "object" && n.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(e),
      isStreamingBody: !1
    } : {
      ...xe(this, am, "f").call(this, {
        body: e,
        headers: n
      }),
      isStreamingBody: !1
    };
  }
};
TS = Nt, am = /* @__PURE__ */ new WeakMap(), K0 = /* @__PURE__ */ new WeakSet(), j5 = function() {
  return this.baseURL !== "https://api.openai.com/v1";
};
Nt.OpenAI = TS;
Nt.DEFAULT_TIMEOUT = 6e5;
Nt.OpenAIError = gt;
Nt.APIError = Mr;
Nt.APIConnectionError = pv;
Nt.APIConnectionTimeoutError = Zw;
Nt.APIUserAbortError = Bs;
Nt.NotFoundError = iR;
Nt.ConflictError = lR;
Nt.RateLimitError = cR;
Nt.BadRequestError = sR;
Nt.AuthenticationError = aR;
Nt.InternalServerError = dR;
Nt.PermissionDeniedError = oR;
Nt.UnprocessableEntityError = uR;
Nt.InvalidWebhookSignatureError = wd;
Nt.toFile = pQ;
Nt.Completions = A5;
Nt.Chat = iS;
Nt.Embeddings = C5;
Nt.Files = x5;
Nt.Images = $5;
Nt.Audio = jf;
Nt.Moderations = D5;
Nt.Models = L5;
Nt.FineTuning = gc;
Nt.Graders = wS;
Nt.VectorStores = Ev;
Nt.Webhooks = X5;
Nt.Beta = mc;
Nt.Batches = m5;
Nt.Uploads = AS;
Nt.Admin = dS;
Nt.Responses = Av;
Nt.Realtime = Sv;
Nt.Conversations = mS;
Nt.Evals = vS;
Nt.Containers = pS;
Nt.Skills = Tv;
Nt.Videos = J5;
function zQ(e) {
  if (KQ(e)) return "Connection error. This may be caused by passing an undici dispatcher, such as ProxyAgent, that is incompatible with the fetch implementation. If you are using undici's ProxyAgent, pass the fetch implementation from the same undici package: import { fetch, ProxyAgent } from 'undici'; new OpenAI({ fetch, fetchOptions: { dispatcher: new ProxyAgent(...) } });";
}
function KQ(e) {
  let t = e;
  for (let n = 0; n < 8 && t && typeof t == "object"; n++) {
    const r = t;
    if (r.code === "UND_ERR_INVALID_ARG" && typeof r.message == "string" && r.message.includes("invalid onRequestStart method")) return !0;
    t = r.cause;
  }
  return !1;
}
function $I(e = "", t = 0) {
  let n = 0;
  for (let r = t - 1; r >= 0 && e[r] === "\\"; r -= 1) n += 1;
  return n % 2 === 1;
}
function WQ(e = "") {
  return /^[0-9a-fA-F]{4}$/.test(e);
}
function GQ(e = "") {
  return /^[dD][89a-bA-B][0-9a-fA-F]{2}$/.test(e);
}
function VQ(e = "") {
  return /^[dD][c-fC-F][0-9a-fA-F]{2}$/.test(e);
}
function JQ(e = "") {
  const t = String(e ?? "");
  let n = "", r = 0;
  for (; r < t.length; ) {
    const s = t.slice(r, r + 2), a = t.slice(r + 2, r + 6);
    if (s !== "\\u" || $I(t, r) || !WQ(a)) {
      n += t[r] || "", r += 1;
      continue;
    }
    const o = r + 6, l = t.slice(o + 2, o + 6);
    if (GQ(a) && t.slice(o, o + 2) === "\\u" && !$I(t, o) && VQ(l)) {
      const i = Number.parseInt(a, 16), u = Number.parseInt(l, 16), d = 65536 + (i - 55296 << 10) + (u - 56320);
      n += String.fromCodePoint(d), r += 12;
      continue;
    }
    n += String.fromCharCode(Number.parseInt(a, 16)), r += 6;
  }
  return n;
}
function YQ(e = "") {
  let t = String(e ?? "").trim();
  return t.endsWith(",") && (t = t.slice(0, -1).trimEnd()), t.startsWith('\\"') && (t = t.slice(2)), t.endsWith('\\"') && (t = t.slice(0, -2)), t.startsWith('"') && (t = t.slice(1)), t.endsWith('"') && (t = t.slice(0, -1)), JQ(t.replace(/\r\n/g, `
`).replace(/\\r/g, "\r").replace(/\\n/g, `
`).replace(/\\t/g, "	").replace(/\\"/g, '"')).replace(/\\\\/g, "\\");
}
function XQ(e = "") {
  return String(e || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function ES(e = "", t = "", n = 0) {
  const r = new RegExp(`(^|[^A-Za-z0-9_])(?:\\\\?")?${XQ(t)}(?:\\\\?")?\\s*:`, "i"), s = String(e || "").slice(Math.max(0, n)).match(r);
  if (!s || s.index === void 0) return null;
  const a = s[1]?.length || 0;
  return {
    key: t,
    index: Math.max(0, n) + s.index + a,
    end: Math.max(0, n) + s.index + s[0].length
  };
}
function jQ(e = "", t = [], n = 0) {
  return t.map((r) => ES(e, r, n)).filter(Boolean).sort((r, s) => r.index - s.index)[0] || null;
}
function Zs(e = "", t = "", n = []) {
  const r = String(e || ""), s = ES(r, t);
  if (!s) return;
  let a = s.end;
  for (; /\s/.test(r[a] || ""); ) a += 1;
  r[a] === '"' && (a += 1);
  const o = jQ(r, n.filter((u) => u !== t), a);
  let l = o ? o.index : r.length;
  if (o) {
    const u = r.lastIndexOf(",", o.index);
    u >= a && (l = u);
  }
  let i = r.slice(a, l).trim();
  return o || (i = i.replace(/\}\s*$/, "").trimEnd()), YQ(i);
}
function ao(e = "") {
  const t = String(e ?? "").trim();
  return /^-?\d+(?:\.\d+)?$/.test(t) ? Number(t) : /^true$/i.test(t) ? !0 : /^false$/i.test(t) ? !1 : /^null$/i.test(t) ? null : t;
}
var Pd = {
  Read: [
    "filePath",
    "path",
    "scope",
    "fromLine",
    "toLine",
    "tail",
    "offset",
    "limit",
    "outputMode",
    "contentFormat"
  ],
  Write: [
    "filePath",
    "path",
    "content"
  ],
  Edit: [
    "filePath",
    "path",
    "edits"
  ],
  Delete: ["filePath", "path"],
  Move: [
    "fromPath",
    "toPath",
    "filePath",
    "path"
  ],
  RenameBook: ["title", "name"],
  ImportMaterial: [
    "title",
    "content",
    "source"
  ],
  Glob: [
    "pattern",
    "path",
    "scope"
  ],
  Grep: [
    "pattern",
    "query",
    "path",
    "scope",
    "include",
    "outputMode",
    "limit",
    "offset",
    "contextLines",
    "useRegex"
  ],
  MapDocs: [
    "docType",
    "docId",
    "limit",
    "offset"
  ],
  MapInspect: [
    "docType",
    "docId",
    "mode",
    "elementId",
    "locationKey",
    "actorKey",
    "from",
    "to",
    "kind",
    "status",
    "query",
    "parent",
    "limit",
    "offset"
  ],
  MapPatch: [
    "docType",
    "docId",
    "expectedRevision",
    "activate",
    "dryRun",
    "ops"
  ],
  EventInspect: [
    "mode",
    "eventId",
    "id",
    "status",
    "limit",
    "offset"
  ],
  EventPatch: [
    "op",
    "eventId",
    "id",
    "title",
    "horizon",
    "current",
    "doneWhen",
    "hookForModel",
    "status"
  ],
  MemoryRead: [
    "filePath",
    "path",
    "offset",
    "limit",
    "tail"
  ],
  MemoryWrite: [
    "filePath",
    "path",
    "content"
  ],
  MemoryEdit: [
    "filePath",
    "path",
    "edits"
  ],
  MemoryGrep: [
    "pattern",
    "query",
    "filePath",
    "path",
    "scope",
    "outputMode",
    "limit",
    "offset",
    "contextLines",
    "regex",
    "useRegex"
  ],
  ChatHistory: [
    "mode",
    "limit",
    "offset",
    "startOrder",
    "endOrder",
    "pattern",
    "query",
    "regex",
    "useRegex",
    "full"
  ],
  WebSearch: ["query", "maxResults"],
  DelegateRun: ["task"],
  PlanCreate: [
    "title",
    "details",
    "priority",
    "owner",
    "blockedBy"
  ],
  PlanUpdate: [
    "id",
    "status",
    "details",
    "priority",
    "owner",
    "blockedBy"
  ],
  PlanList: ["status"],
  apply_patch: ["patchText"]
}, QQ = [
  "filePath",
  "path",
  "fromPath",
  "toPath",
  "content",
  "edits",
  "patchText",
  "query",
  "task",
  "title",
  "details",
  "pattern",
  "scope",
  "include",
  "status",
  "priority",
  "owner",
  "blockedBy",
  "fromLine",
  "toLine",
  "tail",
  "maxResults",
  "outputMode",
  "contentFormat",
  "limit",
  "offset",
  "contextLines",
  "useRegex",
  "regex",
  "mode",
  "docType",
  "docId",
  "expectedRevision",
  "activate",
  "dryRun",
  "ops",
  "op",
  "eventId",
  "fingerprint",
  "horizon",
  "current",
  "doneWhen",
  "hookForModel",
  "startOrder",
  "endOrder",
  "full"
];
function LI(e = "", t = [], n = []) {
  for (const r of t) {
    const s = Zs(e, r, n);
    if (s !== void 0) return s;
  }
}
function ZQ(e = "", t = "") {
  if (t === "Read") {
    const n = Pd.Read, r = {};
    return n.forEach((s, a) => {
      const o = Zs(e, s, n.slice(a + 1));
      o !== void 0 && (r[s] = ao(o));
    }), r.filePath === void 0 && r.path !== void 0 && (r.filePath = r.path, delete r.path), r.filePath === void 0 && r.scope !== void 0 && (r.filePath = r.scope, delete r.scope), Object.keys(r).length ? r : null;
  }
  if (t === "Write") {
    const n = {}, r = LI(e, ["filePath", "path"], ["content"]), s = Zs(e, "content", []);
    return r !== void 0 && (n.filePath = ao(r)), s !== void 0 && (n.content = ao(s)), Object.keys(n).length ? n : null;
  }
  if (t === "Edit") {
    const n = {}, r = LI(e, ["filePath", "path"], ["edits"]), s = Zs(e, "edits", []);
    return r !== void 0 && (n.filePath = ao(r)), s !== void 0 && (n.edits = ao(s)), Object.keys(n).length ? n : null;
  }
  if (t === "Grep") {
    const n = Pd.Grep, r = {};
    return n.forEach((s) => {
      const a = Zs(e, s, n.filter((o) => o !== s));
      a !== void 0 && (r[s] = ao(a));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.path === void 0 && r.scope !== void 0 && (r.path = r.scope), Object.keys(r).length ? r : null;
  }
  if (t === "MemoryGrep") {
    const n = Pd.MemoryGrep, r = {};
    return n.forEach((s) => {
      const a = Zs(e, s, n.filter((o) => o !== s));
      a !== void 0 && (r[s] = ao(a));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.path === void 0 && r.scope !== void 0 && (r.path = r.scope), r.regex === void 0 && r.useRegex !== void 0 && (r.regex = r.useRegex), Object.keys(r).length ? r : null;
  }
  if (t === "ChatHistory") {
    const n = Pd.ChatHistory, r = {};
    return n.forEach((s) => {
      const a = Zs(e, s, n.filter((o) => o !== s));
      a !== void 0 && (r[s] = ao(a));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.regex === void 0 && r.useRegex !== void 0 && (r.regex = r.useRegex), Object.keys(r).length ? r : null;
  }
  return null;
}
function eZ(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) return null;
  try {
    const o = JSON.parse(n);
    if (o && typeof o == "object" && !Array.isArray(o)) return o;
  } catch {
  }
  const r = ZQ(n, t);
  if (r) return r;
  const s = Pd[t] || QQ, a = {};
  return s.forEach((o, l) => {
    const i = Zs(n, o, s.slice(l + 1));
    i !== void 0 && (a[o] = ao(i));
  }), Object.keys(a).length ? a : null;
}
function Q5(e = "", t = "") {
  const n = eZ(e, t);
  return n ? JSON.stringify(n) : "";
}
function Z5(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function js(e, t, n) {
  const r = String(n || "").trim();
  r && e.push({
    label: t,
    text: r
  });
}
function jr(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function Pr(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function e6(e) {
  if (typeof e == "string") return e;
  if (e == null) return "{}";
  try {
    return JSON.stringify(e);
  } catch {
    return "{}";
  }
}
function t6(e, t = "") {
  if (e && typeof e == "object" && !Array.isArray(e)) return JSON.stringify(e);
  const n = typeof e == "string" ? e : e6(e);
  return Q5(n, t) || JSON.stringify(Z5(n));
}
function tZ(e = "") {
  const t = String(e || ""), n = ES(t, "arguments");
  if (!n) return "";
  let r = n.end;
  for (; /\s/.test(t[r] || ""); ) r += 1;
  const s = t[r] || "";
  return s === "{" ? t.slice(r).replace(/\}\s*$/, "").trimEnd() : s === '"' ? t.slice(r + 1).replace(/"\s*\}\s*$/, "").trimEnd() : t.slice(r).replace(/\}\s*$/, "").trimEnd();
}
function nZ(e = "", t = 0) {
  const n = String(e || "").trim(), r = Zs(n, "name", ["id", "arguments"]) || Zs(n, "toolName", ["id", "arguments"]) || "", s = Zs(n, "id", [
    "name",
    "toolName",
    "arguments"
  ]) || `tool-call-${t + 1}`, a = tZ(n);
  return !r || !a ? null : {
    id: s,
    name: r,
    arguments: t6(a, r)
  };
}
function rZ(e, t = 0, n = "openai-tool") {
  if (!Pr(e)) return null;
  const r = Pr(e.function) ? e.function : null, s = String(r?.name || "").trim();
  if (!s) return null;
  const a = jr(e) || {};
  return delete a.index, a.id = String(a.id || `${n}-${t + 1}`), a.type = "function", a.function = {
    ...jr(r) || {},
    name: s,
    arguments: e6(r.arguments)
  }, a;
}
function $l(e = [], t = "openai-tool") {
  return (Array.isArray(e) ? e : []).map((n, r) => rZ(n, r, t)).filter(Boolean);
}
function CS(e) {
  if (!Pr(e)) return null;
  const t = jr(e) || {};
  if (typeof t.content == "string" && /<tool_call\b/i.test(t.content) && (t.content = il(ol(t.content).cleaned)), Array.isArray(t.tool_calls)) {
    const n = $l(t.tool_calls);
    n.length ? t.tool_calls = n : delete t.tool_calls;
  }
  return t;
}
function ai(e = [], t = "openai-tool") {
  return $l(e, t).map((n, r) => ({
    id: n.id || `${t}-${Date.now()}-${r + 1}`,
    name: n.function.name,
    arguments: n.function.arguments
  }));
}
function n6(e) {
  return typeof e == "string" ? e : Array.isArray(e) ? e.map((t) => t ? typeof t == "string" ? t : t.text || t.content || "" : "").filter(Boolean).join(`
`) : "";
}
function ol(e = "") {
  const t = [];
  return {
    cleaned: String(e || "").replace(/<think>([\s\S]*?)<\/think>/gi, (n, r) => (js(t, "思考块", r), "")).trim(),
    thoughts: t
  };
}
function il(e = "") {
  const t = String(e || ""), n = t.search(/<tool_call\b/i);
  return n < 0 ? t.trim() : t.slice(0, n).trim();
}
function W0(e = "") {
  const t = String(e || "");
  return /<tool_call\b/i.test(t) ? [{
    id: "tagged-json-draft",
    name: t.match(/["']?name["']?\s*:\s*["']([^"']+)/i)?.[1] || "工具调用",
    arguments: "{}",
    draft: !0
  }] : [];
}
function el(e, t, n) {
  if (t) {
    if (typeof t == "string") {
      js(e, n, t);
      return;
    }
    if (Array.isArray(t)) {
      t.forEach((r) => el(e, r, n));
      return;
    }
    typeof t == "object" && (typeof t.text == "string" && js(e, n, t.text), typeof t.content == "string" && js(e, n, t.content), typeof t.reasoning_content == "string" && js(e, n, t.reasoning_content), typeof t.thinking == "string" && js(e, n, t.thinking), Array.isArray(t.summary) && t.summary.forEach((r) => {
      if (typeof r == "string") {
        js(e, "推理摘要", r);
        return;
      }
      r && typeof r == "object" && js(e, "推理摘要", r.text || r.content || "");
    }));
  }
}
function oi(e = {}, t = {}) {
  const n = [];
  return el(n, e.reasoning_content, "推理文本"), el(n, e.reasoning, "推理文本"), el(n, e.reasoning_text, "推理文本"), el(n, e.thinking, "思考块"), el(n, t.reasoning_content, "推理文本"), el(n, t.reasoning, "推理文本"), Array.isArray(e.content) && e.content.forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "reasoning_text") {
        js(n, "推理文本", r.text);
        return;
      }
      if (r.type === "summary_text") {
        js(n, "推理摘要", r.text);
        return;
      }
      (r.type === "thinking" || r.type === "reasoning" || r.type === "reasoning_content") && js(n, "思考块", r.text || r.content || r.reasoning || "");
    }
  }), n;
}
function Qd(e = "") {
  const t = [/<tool_call>\s*([\s\S]*?)\s*<\/tool_call>/g], n = [];
  return t.forEach((r) => {
    [...e.matchAll(r)].forEach((s, a) => {
      try {
        const o = JSON.parse(s[1]);
        n.push({
          id: o.id || `tool-call-${a + 1}`,
          name: String(o.name || ""),
          arguments: t6(o.arguments, o.name)
        });
      } catch {
        const o = nZ(s[1], a);
        o && n.push(o);
      }
    });
  }), n.filter((r) => r.name);
}
function Cv(e) {
  const t = e?.providerPayload?.openaiCompatibleMessage;
  return !t || typeof t != "object" || Array.isArray(t) ? null : CS(t);
}
function sZ(e = []) {
  for (let t = e.length - 1; t >= 0; t -= 1) if (e[t]?.role === "user") return t;
  return -1;
}
function aZ(e) {
  if ($l(e?.tool_calls).length > 0) return !0;
  const t = Cv(e);
  return Array.isArray(t?.tool_calls) && t.tool_calls.length > 0;
}
function oZ(e = {}) {
  const t = $l(e?.tool_calls);
  if (t.length) return t;
  const n = $l(Cv(e)?.tool_calls);
  return n.length ? n : [];
}
function iZ(e = {}) {
  return $l(e?.tool_calls).length > 0;
}
function lZ(e, t, n) {
  return e?.role !== "assistant" || t <= n ? !1 : aZ(e);
}
function uZ(e = "") {
  return /deepseek/i.test(String(e || ""));
}
function cZ(e = "") {
  return /claude/i.test(String(e || ""));
}
function r6(e = [], t = "") {
  if (!cZ(t)) return e;
  let n = -1;
  for (let s = e.length - 1; s >= 0; s -= 1) if (typeof e[s]?.role == "string") {
    n = s;
    break;
  }
  const r = e[n]?.role;
  return n < 0 || r === "user" || r !== "system" && r !== "assistant" ? e : e.map((s, a) => a === n ? {
    ...s,
    role: "user"
  } : s);
}
function DI(e, t = "") {
  return !Pr(e) || !uZ(t) || !Array.isArray(e.tool_calls) || !e.tool_calls.length || Object.prototype.hasOwnProperty.call(e, "reasoning_content") ? e : {
    ...e,
    reasoning_content: ""
  };
}
var OI = /* @__PURE__ */ new Set([
  "content",
  "refusal",
  "arguments",
  "reasoning_content",
  "reasoning_text",
  "thinking",
  "text"
]);
function dZ(e = [], t = []) {
  const n = Array.isArray(e) ? e.map((r) => jr(r) || {}) : [];
  return (Array.isArray(t) ? t : []).forEach((r, s) => {
    const a = jr(r) || {}, o = Number.isInteger(Number(r?.index)) ? Number(r.index) : s, l = n[o];
    n[o] = Pr(l) ? Ll(l, a, "tool_call") : a;
  }), n.filter((r) => r !== void 0);
}
function Ll(e, t, n = "") {
  if (t === void 0) return e;
  if (e === void 0) return jr(t);
  if (t === null && OI.has(String(n || ""))) return e;
  if (n === "tool_calls" && Array.isArray(e) && Array.isArray(t)) return dZ(e, t);
  if (typeof e == "string" && typeof t == "string")
    return OI.has(String(n || "")) ? e === t ? e : t.startsWith(e) ? t : e.startsWith(t) ? e : `${e}${t}` : e === t ? e : jr(t);
  if (Array.isArray(e) && Array.isArray(t)) return e.concat(jr(t) || []);
  if (Pr(e) && Pr(t)) {
    const r = { ...e };
    return Object.entries(t).forEach(([s, a]) => {
      r[s] = Ll(r[s], a, s);
    }), r;
  }
  return jr(t);
}
function gg(e = {}, t = {}) {
  const n = Pr(e) ? jr(e) || {} : {}, r = Pr(t) ? jr(t) || {} : {};
  return delete r.message, delete r.finish_reason, delete r.index, delete r.logprobs, delete r.delta, Object.entries(r).forEach(([s, a]) => {
    n[s] = Ll(n[s], a, s);
  }), n.role || (n.role = "assistant"), CS(n) || { role: "assistant" };
}
function Zd(e, t = {}) {
  const n = CS(gg(e, t));
  if (!(!n || typeof n != "object" || Array.isArray(n)))
    return { openaiCompatibleMessage: n };
}
function fZ(e = {}, t = {}) {
  return Pr(e) ? Pr(t) ? Ll(jr(e) || {}, t, "") : jr(e) : jr(t);
}
function G0(e, t = "") {
  const n = Array.isArray(e.messages) ? e.messages : [], r = sZ(n), s = n.map((o, l) => {
    const i = $l(o?.tool_calls);
    if (lZ(o, l, r)) {
      const d = Cv(o);
      if (iZ(d)) return DI({
        ...d,
        ...i.length ? { tool_calls: i } : {}
      }, t);
    }
    const u = {
      role: o.role,
      content: o.content
    };
    return o.role === "tool" && o.tool_call_id && (u.tool_call_id = o.tool_call_id), o.role === "assistant" && i.length && (u.tool_calls = i), DI(u, t);
  }), a = String(e.systemPrompt || "").trim();
  return a && s[0]?.role !== "system" && s.unshift({
    role: "system",
    content: a
  }), r6(s, t);
}
function FI(e) {
  const t = (e.tools || []).map((n) => [`- ${n.function.name}: ${n.function.description || ""}`.trim(), `  参数 JSON Schema: ${JSON.stringify(n.function.parameters || {})}`].join(`
`)).join(`
`);
  return [
    e.systemPrompt || "",
    "如果你需要调用工具，不要使用原生 tool calling 字段。",
    "用 <tool_call> 和 </tool_call> 明确 JSON 范围，请严格输出如下边界标记和包裹的 JSON，不要改写边界标记：",
    '<tool_call>{"name":"工具名","arguments":{...}}</tool_call>',
    "如果需要多个工具调用，可以连续输出多段 <tool_call> ... </tool_call>。",
    "在输出第一个 <tool_call> 之前，可根据任务复杂度决定是否需要先说明：简单查询可直接输出 <tool_call>；复杂任务可先简要说明你准备查什么或怎么查。",
    "一旦开始输出第一个 <tool_call>，就不要再继续输出面向用户的正文、解释、总结或补充；把本轮需要的 tool_call 连续输出完就结束。",
    t ? `可用工具:
${t}` : ""
  ].filter(Boolean).join(`

`);
}
function V0(e, t = "") {
  const n = /* @__PURE__ */ new Map(), r = [];
  return (Array.isArray(e.messages) ? e.messages : []).forEach((s) => {
    if (s.role === "assistant") {
      const a = oZ(s);
      if (a.length) {
        const o = Cv(s), l = typeof o?.content == "string" ? o.content : String(s.content || ""), i = a.map((u, d) => {
          const f = u.function?.name || "", m = u.id || `tool-call-${d + 1}`;
          return f && n.set(m, f), `<tool_call>${JSON.stringify({
            id: m,
            name: f,
            arguments: Z5(u.function?.arguments || "{}")
          })}</tool_call>`;
        }).join(`
`);
        r.push({
          role: "assistant",
          content: [l, i].filter(Boolean).join(`

`)
        });
        return;
      }
    }
    if (s.role === "tool") {
      const a = String(s.toolName || s.tool_name || "").trim() || n.get(s.tool_call_id || "") || "unknown_tool";
      s.tool_call_id && n.delete(s.tool_call_id);
      const o = String(s.content || "");
      r.push({
        role: "user",
        content: [
          "<tool_result>",
          "这是系统工具执行结果，不是用户新发言。",
          `name: ${a}`,
          "content:",
          o,
          "</tool_result>"
        ].join(`
`)
      });
      return;
    }
    r.push({
      role: s.role,
      content: s.content
    });
  }), !r.length || r[0].role !== "system" ? r.unshift({
    role: "system",
    content: FI(e)
  }) : r[0] = {
    ...r[0],
    content: FI({
      ...e,
      systemPrompt: r[0].content || e.systemPrompt
    })
  }, r6(r, t);
}
function BI(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function UI(e, t, n) {
  !e || !t || n === void 0 || (e[t] = Ll(e[t], n, t));
}
function hZ(e, t = []) {
  !Array.isArray(t) || !t.length || (Array.isArray(e.tool_calls) || (e.tool_calls = []), t.forEach((n) => {
    const r = Number(n?.index ?? 0), s = { ...e.tool_calls[r] || {} };
    Object.entries(n || {}).forEach(([a, o]) => {
      if (a !== "index" && !(a === "function" && o == null)) {
        if (a === "function" && Pr(o)) {
          s.function = Pr(s.function) ? { ...s.function } : {}, Object.entries(o).forEach(([l, i]) => {
            s.function[l] = Ll(s.function[l], i, l);
          });
          return;
        }
        s[a] = Ll(s[a], o, a);
      }
    }), e.tool_calls[r] = s;
  }));
}
function J0(e, t = {}) {
  if (!e || !t || typeof t != "object") return;
  Object.entries(t).forEach(([r, s]) => {
    r === "delta" || r === "finish_reason" || r === "index" || r === "logprobs" || UI(e, r, s);
  });
  const n = Pr(t.delta) ? t.delta : {};
  Object.entries(n).forEach(([r, s]) => {
    if (r === "tool_calls") {
      hZ(e, s);
      return;
    }
    UI(e, r, s);
  });
}
function Y0(e, t = {}) {
  if (!e || !Pr(t)) return;
  const n = Number(t.index ?? 0), r = e.toolCalls[n] || {
    id: "",
    type: "function",
    function: {
      name: "",
      arguments: ""
    }
  }, s = Pr(t.function) ? t.function : {};
  e.toolCalls[n] = {
    ...r,
    id: t.id || r.id,
    type: t.type || r.type,
    function: {
      name: s.name || r.function?.name || "",
      arguments: `${r.function?.arguments || ""}${s.arguments || ""}`
    }
  };
}
async function pZ(e, t) {
  const n = e.body?.getReader?.();
  if (!n) throw new Error("openai_compatible_stream_missing_body");
  const r = new TextDecoder();
  let s = "";
  const a = /\r?\n\r?\n/;
  for (; ; ) {
    const { done: l, value: i } = await n.read();
    if (l) break;
    for (s += r.decode(i, { stream: !0 }); ; ) {
      const u = s.match(a);
      if (!u || typeof u.index != "number") break;
      const d = u.index, f = s.slice(0, d);
      s = s.slice(d + u[0].length);
      const m = f.split(/\r?\n/).filter((v) => v.startsWith("data:")).map((v) => v.slice(5).trimStart()).join(`
`).trim();
      !m || m === "[DONE]" || t(JSON.parse(m));
    }
  }
  const o = s.trim();
  if (o && o !== "[DONE]") {
    const l = o.split(/\r?\n/).filter((i) => i.startsWith("data:")).map((i) => i.slice(5).trimStart()).join(`
`).trim();
    l && l !== "[DONE]" && t(JSON.parse(l));
  }
}
var mZ = class {
  constructor(e) {
    this.config = e, this.client = new Nt({
      apiKey: e.apiKey,
      baseURL: String(e.baseUrl || "https://api.openai.com/v1").replace(/\/$/, ""),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = !t && Array.isArray(e.tools) && e.tools.length ? e.tools : null, r = {
      model: this.config.model,
      messages: t ? V0(e, this.config.model) : G0(e, this.config.model),
      ...n ? {
        tools: n,
        tool_choice: e.toolChoice || "auto"
      } : {},
      ...e.maxTokens ? { max_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (r.temperature = e.temperature), e.reasoning?.enabled && (r.reasoning_effort = e.reasoning.effort), r;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = {
      ...t.body || this.buildRequestBody(e),
      ...n ? { stream: !0 } : {}
    }, s = String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "");
    return Cf({
      provider: "openai-compatible",
      model: this.config.model,
      transport: "openai-compatible",
      url: `${s}/chat/completions`,
      headers: {
        "Content-Type": "application/json",
        Authorization: this.config.apiKey ? `Bearer ${this.config.apiKey}` : ""
      },
      body: r,
      sdk: n ? "client.chat.completions.create(..., { stream: true })" : "client.chat.completions.create"
    });
  }
  async streamNativeChatCompletions(e, t) {
    const n = `${String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "")}/chat/completions`, r = await fetch(n, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${this.config.apiKey}`
      },
      body: JSON.stringify({
        ...t,
        stream: !0
      }),
      signal: e.signal
    });
    if (!r.ok) {
      const y = await r.text().catch(() => "");
      throw new Error(y || `openai_compatible_stream_http_${r.status}`);
    }
    const s = {
      content: "",
      toolCalls: []
    }, a = { role: "assistant" };
    let o = "stop", l = this.config.model;
    await pZ(r, (y) => {
      l = y?.model || l;
      const S = y?.choices?.[0], x = S?.delta || {};
      J0(a, S), S?.finish_reason && (o = S.finish_reason), typeof x.content == "string" && (s.content += x.content), Array.isArray(x.tool_calls) && x.tool_calls.forEach(($) => {
        Y0(s, $);
      });
      const I = ol(s.content), E = s.toolCalls.filter(($) => $?.function?.name), N = E.length ? ai(s.toolCalls) : W0(I.cleaned);
      BI(e, {
        text: E.length ? I.cleaned : il(I.cleaned),
        thoughts: oi(a, S).concat(I.thoughts),
        ...N.length ? { toolCalls: N } : {},
        ...!E.length && N.length ? { toolCallDraft: !0 } : {}
      });
    });
    const i = Zd(a), u = ai(s.toolCalls), d = ol(s.content), f = oi(a, {});
    d.thoughts.forEach((y) => f.push(y));
    const m = u.length ? [] : Qd(d.cleaned), v = [...u, ...m];
    return {
      text: u.length ? d.cleaned : il(d.cleaned),
      toolCalls: v,
      thoughts: f,
      finishReason: o,
      model: l,
      provider: "openai-compatible",
      providerPayload: i
    };
  }
  async chat(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = typeof e.onStreamProgress == "function", r = this.buildRequestBody(e), s = this.inspectRequest(e, { body: r });
    if (n) {
      if (!t) return {
        ...await this.streamNativeChatCompletions(e, r),
        requestInspection: s
      };
      const S = await this.client.chat.completions.create({
        ...r,
        stream: !0
      }, { signal: e.signal }), x = {
        content: "",
        toolCalls: []
      }, I = { role: "assistant" };
      let E = "stop", N = this.config.model, $;
      for await (const H of S) {
        N = H.model || N;
        const G = H.choices?.[0], ee = G?.delta || {};
        J0(I, G), G?.finish_reason && (E = G.finish_reason), typeof ee.content == "string" && (x.content += ee.content), Array.isArray(ee.tool_calls) && ee.tool_calls.forEach((V) => {
          Y0(x, V);
        });
        const J = ol(x.content), z = x.toolCalls.filter((V) => V?.function?.name), R = z.length ? ai(x.toolCalls) : W0(J.cleaned);
        BI(e, {
          text: z.length ? J.cleaned : il(J.cleaned),
          thoughts: oi(I, G).concat(J.thoughts),
          ...R.length ? { toolCalls: R } : {},
          ...!z.length && R.length ? { toolCallDraft: !0 } : {}
        });
      }
      const p = (typeof S.finalChatCompletion == "function" ? await S.finalChatCompletion() : null)?.choices?.[0] || null, w = fZ(I, gg(p?.message || I, p || {}));
      $ = Zd(w);
      const _ = ai(x.toolCalls), b = ol(x.content), C = oi(w, p || {});
      b.thoughts.forEach((H) => C.push(H));
      const k = _.length ? [] : Qd(b.cleaned), P = [..._, ...k];
      return {
        text: _.length ? b.cleaned : il(b.cleaned),
        toolCalls: P,
        thoughts: C,
        finishReason: E,
        model: N,
        provider: "openai-compatible",
        providerPayload: $,
        requestInspection: s
      };
    }
    const a = await this.client.chat.completions.create(r, { signal: e.signal }), o = a.choices?.[0] || {}, l = o.message || {}, i = oi(l, o), u = ai(l.tool_calls || []), d = ol(n6(l.content));
    d.thoughts.forEach((S) => i.push(S));
    const f = u.length ? [] : Qd(d.cleaned), m = [...u, ...f], v = u.length ? d.cleaned : il(d.cleaned), y = gg(l, o);
    return {
      text: v,
      toolCalls: m,
      thoughts: i,
      finishReason: o.finish_reason || "stop",
      model: a.model || this.config.model,
      provider: "openai-compatible",
      providerPayload: Zd(y),
      requestInspection: s
    };
  }
};
function s6(e, t) {
  return {
    type: "message",
    role: e,
    content: gZ(t)
  };
}
function vg(e) {
  return {
    role: "assistant",
    content: typeof e == "string" ? e : ""
  };
}
function gZ(e) {
  if (typeof e == "string") return [{
    type: "input_text",
    text: e
  }];
  if (!Array.isArray(e)) return [{
    type: "input_text",
    text: ""
  }];
  const t = e.map((n) => !n || typeof n != "object" ? null : n.type === "image_url" && n.image_url?.url ? {
    type: "input_image",
    image_url: n.image_url.url
  } : n.type === "text" ? {
    type: "input_text",
    text: n.text || ""
  } : null).filter(Boolean);
  return t.length ? t : [{
    type: "input_text",
    text: ""
  }];
}
function yg(e, t, n) {
  const r = String(n || "").trim();
  r && e.push({
    label: t,
    text: r
  });
}
function qI(e, t = [], n = {}) {
  (t || []).forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "reasoning_text") {
        yg(e, n.reasoning || "推理文本", r.text);
        return;
      }
      r.type === "summary_text" && yg(e, n.summary || "推理摘要", r.text);
    }
  });
}
function vZ(e = []) {
  const t = [];
  return (e || []).forEach((n) => {
    !n || typeof n != "object" || n.type === "reasoning" && (qI(t, n.content, {
      reasoning: "推理文本",
      summary: "推理摘要"
    }), qI(t, n.summary, {
      reasoning: "推理文本",
      summary: "推理摘要"
    }));
  }), t;
}
function yZ(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  return t.length ? [...new Set(t)].join(`

`) : "";
}
function _Z(e) {
  const t = e?.choices?.[0]?.message?.content;
  if (typeof t == "string" && t.trim()) return t.trim();
  if (typeof e?.output_text == "string" && e.output_text.trim()) return e.output_text.trim();
  const n = [];
  return (Array.isArray(e?.output) ? e.output : []).forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "message" && Array.isArray(r.content)) {
        r.content.forEach((s) => {
          if (!(!s || typeof s != "object")) {
            if (s.type === "output_text" && typeof s.text == "string" && s.text.trim()) {
              n.push(s.text.trim());
              return;
            }
            s.type === "refusal" && typeof s.refusal == "string" && s.refusal.trim() && n.push(s.refusal.trim());
          }
        });
        return;
      }
      typeof r.text == "string" && r.text.trim() && n.push(r.text.trim());
    }
  }), n.join(`
`).trim();
}
function bZ(e) {
  const t = e?.choices?.[0], n = t?.message?.content, r = String(t?.finish_reason || "");
  if (typeof n != "string" || !n.trim()) return null;
  const s = n.toLowerCase();
  return !s.includes("proxy error") || !s.includes("/responses") && !r.toLowerCase().includes("proxy error") ? null : n.trim();
}
function wZ(e) {
  const t = [];
  for (const n of e.messages || [])
    if (n.role !== "system") {
      if (n.role === "tool") {
        t.push({
          type: "function_call_output",
          call_id: n.tool_call_id || "missing_tool_call_id",
          output: n.content
        });
        continue;
      }
      if (n.role === "assistant" && Array.isArray(n.tool_calls) && n.tool_calls.length) {
        n.content?.trim() && t.push(vg(n.content)), n.tool_calls.forEach((r, s) => {
          t.push({
            type: "function_call",
            call_id: r.id || `function_call_${s + 1}`,
            name: r.function?.name || "",
            arguments: r.function?.arguments || "{}",
            status: "completed"
          });
        });
        continue;
      }
      if (n.role === "assistant") {
        t.push(vg(n.content || ""));
        continue;
      }
      t.push(n.role === "user" ? s6(n.role, n.content || "") : {
        role: n.role,
        content: typeof n.content == "string" ? n.content : ""
      });
    }
  return t;
}
function SZ(e) {
  const t = [];
  for (const n of e.messages || []) {
    if (n.role === "system") {
      t.push({
        role: "system",
        content: typeof n.content == "string" ? n.content : ""
      });
      continue;
    }
    if (n.role === "tool") {
      t.push({
        type: "function_call_output",
        call_id: n.tool_call_id || "missing_tool_call_id",
        output: n.content
      });
      continue;
    }
    if (n.role === "assistant" && Array.isArray(n.tool_calls) && n.tool_calls.length) {
      n.content?.trim() && t.push(vg(n.content)), n.tool_calls.forEach((r, s) => {
        t.push({
          type: "function_call",
          call_id: r.id || `function_call_${s + 1}`,
          name: r.function?.name || "",
          arguments: r.function?.arguments || "{}",
          status: "completed"
        });
      });
      continue;
    }
    if (n.role === "assistant") {
      t.push(vg(n.content || ""));
      continue;
    }
    t.push(n.role === "user" ? s6(n.role, n.content || "") : {
      role: n.role,
      content: typeof n.content == "string" ? n.content : ""
    });
  }
  return t;
}
function AZ(e) {
  try {
    return new URL(String(e || "https://api.openai.com/v1")).hostname === "api.openai.com";
  } catch {
    return !1;
  }
}
function TZ(e) {
  const t = String(e?.message || e || "").toLowerCase();
  return t.includes("instructions") || t.includes("unsupported") || t.includes("unknown parameter") || t.includes("invalid input");
}
function EZ(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {}
  });
}
function k_(e, t) {
  const [n = "0", r = "0"] = String(e || "").split(":"), [s = "0", a = "0"] = String(t || "").split(":");
  return Number(n) - Number(s) || Number(r) - Number(a);
}
var CZ = class {
  constructor(e) {
    this.config = e, this.client = new Nt({
      apiKey: e.apiKey,
      baseURL: String(e.baseUrl || "https://api.openai.com/v1").replace(/\/$/, ""),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e, t = !1) {
    const n = {
      model: this.config.model,
      instructions: t ? void 0 : yZ(e) || void 0,
      input: t ? SZ(e) : wZ(e),
      ...Array.isArray(e.tools) && e.tools.length ? {
        tools: e.tools.map((r) => ({
          type: "function",
          name: r.function.name,
          description: r.function.description,
          parameters: r.function.parameters
        })),
        tool_choice: e.toolChoice || "auto"
      } : {},
      ...e.maxTokens ? { max_output_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (n.temperature = e.temperature), e.reasoning?.enabled && (n.reasoning = {
      effort: e.reasoning.effort,
      summary: "detailed"
    }), n;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = t.legacySystemInInput === !0, s = String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "");
    return Cf({
      provider: "openai-responses",
      model: this.config.model,
      transport: "openai-responses",
      url: `${s}/responses`,
      headers: {
        "Content-Type": "application/json",
        Authorization: this.config.apiKey ? `Bearer ${this.config.apiKey}` : ""
      },
      body: t.body || this.buildRequestBody(e, r),
      sdk: n ? "client.responses.stream" : "client.responses.create"
    });
  }
  async chat(e) {
    let t = this.inspectRequest(e);
    const n = (i) => {
      const u = bZ(i);
      if (u) {
        const f = new Error(u);
        throw f.name = "ProxyEndpointError", f.rawDisplay = u, f;
      }
      const d = Array.isArray(i.output) ? i.output : [];
      return {
        output: d,
        thoughts: vZ(d),
        toolCalls: d.filter((f) => f.type === "function_call" && f.name).map((f, m) => ({
          id: f.call_id || `response-tool-${m + 1}`,
          name: f.name || "",
          arguments: f.arguments || "{}"
        })),
        text: _Z(i)
      };
    }, r = async (i = !1) => {
      const u = this.buildRequestBody(e, i);
      return t = this.inspectRequest(e, {
        body: u,
        legacySystemInInput: i
      }), await this.client.responses.create(u, { signal: e.signal });
    }, s = async (i = !1) => {
      const u = this.buildRequestBody(e, i);
      t = this.inspectRequest(e, {
        body: u,
        legacySystemInInput: i
      });
      const d = this.client.responses.stream(u, { signal: e.signal }), f = /* @__PURE__ */ new Map(), m = /* @__PURE__ */ new Map(), v = /* @__PURE__ */ new Map(), y = () => {
        const S = [];
        Array.from(m.entries()).sort(([x], [I]) => k_(x, I)).forEach(([, x]) => yg(S, "推理文本", x)), Array.from(v.entries()).sort(([x], [I]) => k_(x, I)).forEach(([, x]) => yg(S, "推理摘要", x)), EZ(e, {
          text: Array.from(f.entries()).sort(([x], [I]) => k_(x, I)).map(([, x]) => x).join(`
`).trim(),
          thoughts: S
        });
      };
      return d.on("response.output_text.delta", (S) => {
        const x = `${S.output_index}:${S.content_index}`;
        f.set(x, `${f.get(x) || ""}${S.delta}`), y();
      }), d.on("response.reasoning_text.delta", (S) => {
        const x = `${S.output_index}:${S.content_index}`;
        m.set(x, `${m.get(x) || ""}${S.delta}`), y();
      }), d.on("response.reasoning_summary_text.delta", (S) => {
        const x = `${S.output_index}:${S.summary_index}`;
        v.set(x, `${v.get(x) || ""}${S.delta}`), y();
      }), await d.finalResponse();
    }, a = !AZ(this.config.baseUrl);
    let o, l;
    try {
      o = typeof e.onStreamProgress == "function" ? await s(!1) : await r(!1), l = n(o), a && !l.text && !l.toolCalls.length && (o = typeof e.onStreamProgress == "function" ? await s(!0) : await r(!0), l = n(o));
    } catch (i) {
      if (!a || !TZ(i)) throw i;
      o = typeof e.onStreamProgress == "function" ? await s(!0) : await r(!0), l = n(o);
    }
    return {
      text: l.text,
      toolCalls: l.toolCalls,
      thoughts: l.thoughts,
      finishReason: o.incomplete_details?.reason || o.status || "stop",
      model: o.model || this.config.model,
      provider: "openai-responses",
      requestInspection: t
    };
  }
};
function Dl(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function a6(e = "") {
  try {
    return {
      ok: !0,
      input: JSON.parse(String(e || ""))
    };
  } catch (t) {
    return {
      ok: !1,
      input: {},
      raw: String(e || ""),
      error: t instanceof Error ? t.message : String(t || "invalid_tool_input_json")
    };
  }
}
function IZ(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = String(t?.function?.name || "").trim();
    if (!n) return null;
    const r = a6(t.function.arguments || "{}");
    return {
      type: "tool_use",
      id: String(t.id || n),
      name: n,
      input: r.input,
      ...r.ok ? {} : {
        invalidInputJson: r.raw,
        inputParseError: r.error
      }
    };
  }).filter(Boolean);
}
function xZ(e = []) {
  const t = Array.isArray(e) ? Dl(e) : null;
  return Array.isArray(t) && t.length ? t : null;
}
function kZ(e = {}) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  return t.forEach((r) => {
    if (!r || typeof r != "object") return;
    const s = Dl(r) || {}, a = xZ(s?.providerPayload?.anthropicContent), o = IZ(s.tool_calls);
    delete s.providerPayload, s.role === "assistant" && a && o.length ? (delete s.tool_calls, s.content = a.filter((l) => l?.type !== "tool_use").concat(o)) : s.role === "assistant" && a && (delete s.tool_calls, s.content = a), n.push(s);
  }), n;
}
function MZ(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    if (!t || typeof t != "object") return null;
    if (t.type === "text") return {
      type: "text",
      text: String(t.text || "")
    };
    if (t.type === "tool_use" && t.name) {
      if (t.inputJson !== void 0) {
        const r = a6(t.inputJson);
        return {
          type: "tool_use",
          id: String(t.id || t.name),
          name: String(t.name),
          input: r.input,
          ...r.ok ? {} : {
            invalidInputJson: r.raw,
            inputParseError: r.error
          }
        };
      }
      const n = Dl(t.input);
      return n !== void 0 ? {
        type: "tool_use",
        id: String(t.id || t.name),
        name: String(t.name),
        input: n
      } : {
        type: "tool_use",
        id: String(t.id || t.name),
        name: String(t.name),
        input: {}
      };
    }
    return t.type === "thinking" ? {
      type: "thinking",
      thinking: String(t.thinking || t.text || "")
    } : t.type === "redacted_thinking" ? {
      type: "redacted_thinking",
      data: String(t.data || "")
    } : Dl(t) || null;
  }).filter(Boolean);
}
function PZ(e = []) {
  return e.map((t) => !t || typeof t != "object" ? null : t.type === "tool_use" && t.name ? {
    type: "tool_use",
    id: t.id,
    name: t.name,
    input: Dl(t.input) || {}
  } : Dl(t) || null).filter(Boolean);
}
function RZ(e = []) {
  const t = Array.isArray(e) ? e : [], n = t.filter((a) => a?.type === "text").map((a) => a.text || "").join(`
`), r = t.filter((a) => a?.type === "thinking" || a?.type === "redacted_thinking").map((a) => ({
    label: a.type === "thinking" ? "思考块" : "已脱敏思考块",
    text: a.type === "thinking" ? a.thinking || "" : a.data || ""
  })).filter((a) => a.text), s = t.filter((a) => a?.type === "tool_use" && a.name).map((a, o) => ({
    id: a.id || `st-claude-tool-${o + 1}`,
    name: a.name,
    arguments: a.inputJson !== void 0 ? a.inputJson : JSON.stringify(a.input || {})
  }));
  return {
    text: n,
    thoughts: r,
    ...s.length ? {
      toolCalls: s,
      toolCallDraft: !0
    } : {}
  };
}
function o6(e = [], t = {}) {
  const n = MZ(e), r = n.filter((s) => s.type === "tool_use" && s.name).map((s, a) => ({
    id: s.id || `st-claude-tool-${a + 1}`,
    name: s.name,
    arguments: s.invalidInputJson !== void 0 ? s.invalidInputJson : JSON.stringify(s.input || {})
  }));
  return {
    text: n.filter((s) => s.type === "text").map((s) => s.text || "").join(`
`),
    toolCalls: r,
    thoughts: n.filter((s) => s.type === "thinking" || s.type === "redacted_thinking").map((s) => ({
      label: s.type === "thinking" ? "思考块" : "已脱敏思考块",
      text: s.type === "thinking" ? s.thinking || "" : s.data || ""
    })).filter((s) => s.text),
    finishReason: t.finishReason || "stop",
    model: t.model || "",
    provider: "sillytavern-claude",
    providerPayload: n.length ? { anthropicContent: PZ(n) } : void 0
  };
}
function NZ(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function $Z(e, t = {}) {
  const n = [];
  let r = "stop", s = t.model || "";
  const a = (l, i = {}) => {
    const u = Number.isInteger(Number(l)) ? Number(l) : n.length;
    return n[u] ? n[u] = {
      ...n[u],
      ...i
    } : n[u] = { ...i }, n[u];
  }, o = () => {
    const l = RZ(n);
    NZ(e, {
      text: l.text,
      thoughts: l.thoughts,
      ...Array.isArray(l.toolCalls) ? { toolCalls: l.toolCalls } : {},
      ...l.toolCallDraft ? { toolCallDraft: !0 } : {}
    });
  };
  return {
    accept(l = {}) {
      if (l?.message?.model && (s = l.message.model), l.type === "content_block_start") {
        a(l.index, Dl(l.content_block) || {}), o();
        return;
      }
      if (l.type === "content_block_delta") {
        const i = a(l.index), u = l.delta || {};
        u.type === "text_delta" ? (i.type = i.type || "text", i.text = `${i.text || ""}${u.text || ""}`) : u.type === "input_json_delta" ? (i.type = i.type || "tool_use", i.inputJson = `${i.inputJson || ""}${u.partial_json || ""}`) : u.type === "thinking_delta" ? (i.type = i.type || "thinking", i.thinking = `${i.thinking || ""}${u.thinking || ""}`) : u.type === "signature_delta" && (i.signature = `${i.signature || ""}${u.signature || ""}`), o();
        return;
      }
      l.type === "message_delta" && (r = l.delta?.stop_reason || r);
    },
    result() {
      return o6(n, {
        finishReason: r,
        model: s
      });
    }
  };
}
var LZ = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return kZ(e);
  }
  buildPayload(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildMessages(e);
    return sO(this.config, e, n, t);
  }
  async inspectRequest(e, t = {}) {
    const n = await Lf(t.payload || this.buildPayload(e), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-claude",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: ju(e)
    };
  }
  async chat(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildPayload(e);
    let r = null;
    const s = (a) => {
      r = this.buildRequestInspection(a);
    };
    try {
      if (t) {
        const o = $Z(e, this.config);
        return await L1(n, (l) => {
          o.accept(l);
        }, {
          signal: e.signal,
          onRequest: s
        }), {
          ...o.result(),
          requestInspection: r
        };
      }
      const a = await $1(n, {
        signal: e.signal,
        onRequest: s
      });
      return {
        ...o6(Array.isArray(a?.content) ? a.content : [{
          type: "text",
          text: a?.choices?.[0]?.message?.content || ""
        }], {
          finishReason: a?.stop_reason || a?.choices?.[0]?.finish_reason || "stop",
          model: a?.model || this.config.model
        }),
        requestInspection: r
      };
    } catch (a) {
      throw r && a && typeof a == "object" && (a.requestInspection = r), a;
    }
  }
};
function IS(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function Qu(e) {
  if (typeof e == "string") return {
    role: "model",
    parts: e ? [{ text: e }] : []
  };
  if (!e || typeof e != "object") return {
    role: "model",
    parts: []
  };
  const t = IS(e) || {};
  return t.role = t.role || "model", t.parts = Array.isArray(t.parts) ? t.parts : [], t;
}
function DZ(e) {
  const t = Array.isArray(e?.providerPayload?.googleContents) ? e.providerPayload.googleContents : [];
  if (t.length) return t.map((s) => Qu(s)).filter((s) => Array.isArray(s.parts) && s.parts.length);
  const n = e?.providerPayload?.googleContent, r = Qu(n);
  return r.parts.length ? [r] : [];
}
function OZ(e = {}) {
  const t = String(e?.mimeType || "").trim(), n = String(e?.data || "").trim();
  if (!t || !n) return null;
  const r = `data:${t};base64,${n}`;
  return t.startsWith("image/") ? {
    type: "image_url",
    image_url: { url: r }
  } : t.startsWith("video/") ? {
    type: "video_url",
    video_url: { url: r }
  } : t.startsWith("audio/") ? {
    type: "audio_url",
    audio_url: { url: r }
  } : null;
}
function FZ(e = {}, t = 0) {
  const n = Qu(e);
  if (!n.parts.length) return null;
  const r = {
    role: n.role === "user" ? "user" : "assistant",
    content: []
  }, s = n.parts.find((o) => !o?.thought && typeof o?.text == "string" && typeof o?.thoughtSignature == "string" && o.thoughtSignature)?.thoughtSignature || "", a = [];
  return n.parts.forEach((o) => {
    if (!o || typeof o != "object") return;
    if (!o.thought && typeof o.text == "string" && o.text) {
      r.content.push({
        type: "text",
        text: o.text
      });
      return;
    }
    if (o.functionCall?.name) {
      a.push({
        id: String(o.functionCall.id || `st-google-tool-${t + 1}-${a.length + 1}`),
        type: "function",
        function: {
          name: String(o.functionCall.name || ""),
          arguments: JSON.stringify(o.functionCall.args || {})
        },
        ...typeof o.thoughtSignature == "string" && o.thoughtSignature ? { signature: o.thoughtSignature } : {}
      });
      return;
    }
    const l = OZ(o.inlineData);
    l && r.content.push(l);
  }), a.length && r.content.push({
    type: "tool_calls",
    tool_calls: a
  }), s && r.content.some((o) => o?.type === "text") && (r.signature = s), r.content.length ? r : null;
}
function BZ(e = {}) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  return t.forEach((r) => {
    if (!r || typeof r != "object") return;
    const s = DZ(r);
    if (r.role === "assistant" && s.length) {
      s.forEach((o, l) => {
        const i = FZ(o, l);
        i && n.push(i);
      });
      return;
    }
    const a = IS(r) || {};
    delete a.providerPayload, n.push(a);
  }), n;
}
function i6(e = {}) {
  return Qu(e?.responseContent || e?.candidates?.[0]?.content || "");
}
function l6(e = {}) {
  return (e.parts || []).filter((t) => !t?.thought && typeof t?.text == "string" && t.text).map((t) => t.text).join(`
`);
}
function u6(e = {}) {
  return (e.parts || []).filter((t) => t?.thought && typeof t.text == "string" && t.text.trim()).map((t, n) => ({
    label: `思考块 ${n + 1}`,
    text: t.text.trim()
  }));
}
function c6(e = {}) {
  return (e.parts || []).map((t) => t?.functionCall || null).filter((t) => t?.name).map((t, n) => ({
    id: t.id || `st-google-tool-${n + 1}`,
    name: t.name,
    arguments: JSON.stringify(t.args || {})
  }));
}
function UZ(e, t) {
  const n = String(t || ""), r = String(e || "");
  return n ? !r || n.startsWith(r) ? n : r.endsWith(n) ? r : `${r}${n}` : r;
}
function qZ(e = [], t = []) {
  const n = Array.isArray(e) ? [...e] : [];
  return t.forEach((r) => {
    const s = [
      r.id || "",
      r.name || "",
      r.arguments || ""
    ].join("\0");
    n.some((a) => [
      a.id || "",
      a.name || "",
      a.arguments || ""
    ].join("\0") === s) || n.push(r);
  }), n;
}
function d6(e) {
  const t = Qu(e);
  return t.parts.length ? {
    googleContent: t,
    googleContents: [t]
  } : void 0;
}
function HZ(e = {}, t = {}) {
  const n = i6(e), r = e?.choices?.[0]?.message?.content || "";
  return {
    text: l6(n) || r,
    toolCalls: c6(n),
    thoughts: u6(n),
    finishReason: e?.candidates?.[0]?.finishReason || e?.choices?.[0]?.finish_reason || t.finishReason || "STOP",
    model: e?.model || e?.modelVersion || t.model || "",
    provider: "sillytavern-google",
    providerPayload: d6(n)
  };
}
function zZ(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function KZ(e, t = {}) {
  let n = "", r = [], s = [], a = "STOP", o = t.model || "";
  const l = [];
  return {
    accept(i = {}) {
      o = i.model || i.modelVersion || o, a = i?.candidates?.[0]?.finishReason || a;
      const u = i6(i);
      u.parts.length && l.push(...IS(u.parts) || []), n = UZ(n, l6(u)), r = qZ(r, c6(u));
      const d = u6(u);
      d.length && (s = d), zZ(e, {
        text: n,
        thoughts: s,
        ...r.length ? {
          toolCalls: r,
          toolCallDraft: !0
        } : {}
      });
    },
    result() {
      const i = Qu({
        role: "model",
        parts: l.length ? l : n ? [{ text: n }] : []
      });
      return {
        text: n,
        toolCalls: r,
        thoughts: s,
        finishReason: a,
        model: o,
        provider: "sillytavern-google",
        providerPayload: d6(i)
      };
    }
  };
}
var WZ = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return BZ(e);
  }
  buildPayload(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildMessages(e);
    return aO(this.config, e, n, t);
  }
  async inspectRequest(e, t = {}) {
    const n = await Lf(t.payload || this.buildPayload(e), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-google",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: ju(e)
    };
  }
  async chat(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildPayload(e);
    let r = null;
    const s = (a) => {
      r = this.buildRequestInspection(a);
    };
    try {
      if (t) {
        const a = KZ(e, this.config);
        return await L1(n, (o) => {
          a.accept(o);
        }, {
          signal: e.signal,
          onRequest: s
        }), {
          ...a.result(),
          requestInspection: r
        };
      }
      return {
        ...HZ(await $1(n, {
          signal: e.signal,
          onRequest: s
        }), { model: this.config.model }),
        requestInspection: r
      };
    } catch (a) {
      throw r && a && typeof a == "object" && (a.requestInspection = r), a;
    }
  }
};
function GZ(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function M_(e, t = []) {
  const n = ol(e);
  return {
    thinkTagged: n,
    cleanedText: t.length ? n.cleaned : il(n.cleaned)
  };
}
function VZ(e) {
  const t = String(e?.message || e || "");
  return /Cannot read properties of null \(reading ['"]function['"]\)/i.test(t) || /reading ['"]function['"]/i.test(t) || /badresponsestatuscode/i.test(t);
}
var JZ = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0 ? V0(e, this.config.model) : G0(e, this.config.model);
  }
  buildPayload(e, t = !1) {
    const n = t ? V0(e, this.config.model) : G0(e, this.config.model);
    return rO(this.config, t ? {
      ...e,
      tools: void 0,
      toolChoice: void 0
    } : e, n, typeof e.onStreamProgress == "function");
  }
  async inspectRequest(e, t = {}) {
    const n = await Lf(t.payload || this.buildPayload(e, !!t.taggedMode), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-openai-compatible",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: ju(e)
    };
  }
  async streamChat(e, t, n = {}) {
    const r = {
      content: "",
      toolCalls: []
    }, s = { role: "assistant" };
    let a = "stop", o = this.config.model;
    await L1(t, (m) => {
      o = m?.model || o;
      const v = m?.choices?.[0] || {}, y = v.delta || {};
      J0(s, v), v.finish_reason && (a = v.finish_reason), typeof y.content == "string" && (r.content += y.content), Array.isArray(y.tool_calls) && y.tool_calls.forEach((N) => {
        Y0(r, N);
      });
      const S = r.toolCalls.filter((N) => N?.function?.name), { thinkTagged: x, cleanedText: I } = M_(r.content, S), E = S.length ? ai(r.toolCalls, "st-openai-tool") : W0(x.cleaned);
      GZ(e, {
        text: I,
        thoughts: oi(s, v).concat(x.thoughts),
        ...E.length ? { toolCalls: E } : {},
        ...!S.length && E.length ? { toolCallDraft: !0 } : {}
      });
    }, {
      signal: e.signal,
      onRequest: n.onRequest
    });
    const l = ai(r.toolCalls, "st-openai-tool"), { thinkTagged: i, cleanedText: u } = M_(r.content, l), d = oi(s, {});
    i.thoughts.forEach((m) => d.push(m));
    const f = l.length ? [] : Qd(i.cleaned);
    return {
      text: u,
      toolCalls: [...l, ...f],
      thoughts: d,
      finishReason: a,
      model: o,
      provider: "sillytavern-openai-compatible",
      providerPayload: Zd(s)
    };
  }
  async nonStreamingChat(e, t, n = {}) {
    const r = await $1(t, {
      signal: e.signal,
      onRequest: n.onRequest
    }), s = r.choices?.[0] || {}, a = s.message || {}, o = oi(a, s), l = ai(a.tool_calls || [], "st-openai-tool"), { thinkTagged: i, cleanedText: u } = M_(n6(a.content), l);
    i.thoughts.forEach((m) => o.push(m));
    const d = l.length ? [] : Qd(i.cleaned), f = gg(a, s);
    return {
      text: u,
      toolCalls: [...l, ...d],
      thoughts: o,
      finishReason: s.finish_reason || "stop",
      model: r.model || this.config.model,
      provider: "sillytavern-openai-compatible",
      providerPayload: Zd(f)
    };
  }
  async chat(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = Array.isArray(e.tools) && e.tools.length > 0, r = async (a) => {
      let o = null;
      const l = (i) => {
        o = this.buildRequestInspection(i);
      };
      try {
        return {
          ...typeof e.onStreamProgress == "function" ? await this.streamChat(e, a, { onRequest: l }) : await this.nonStreamingChat(e, a, { onRequest: l }),
          requestInspection: o
        };
      } catch (i) {
        throw o && i && typeof i == "object" && (i.requestInspection = o), i;
      }
    }, s = this.buildPayload(e, t);
    try {
      return await r(s);
    } catch (a) {
      if (t || !n || !VZ(a)) throw a;
    }
    return typeof e.onToolProtocolFallback == "function" && e.onToolProtocolFallback({
      provider: "sillytavern-openai-compatible",
      fromToolMode: "native",
      toToolMode: "tagged-json",
      reason: "malformed_native_tool_host_error"
    }), await r(this.buildPayload(e, !0));
  }
}, YZ = "https://api.tavily.com";
function X0(e = "") {
  return String(e || "").trim();
}
function Ds(e = "") {
  return String(e || "").trim().replace(/\/+$/, "") || "https://api.tavily.com";
}
var f6 = "openai-compatible", xS = "默认", h6 = "default", XZ = "deny", jZ = Object.freeze([{
  value: "default",
  label: "默认权限"
}, {
  value: "full",
  label: "完全权限"
}]), QZ = Object.freeze([{
  value: "deny",
  label: "禁止"
}, {
  value: "allow",
  label: "允许"
}]), j0 = {
  "openai-responses": {
    baseUrl: "https://api.openai.com/v1",
    model: "gpt-4.1-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  "openai-compatible": {
    baseUrl: "https://api.openai.com/v1",
    model: "gpt-4o-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0,
    toolMode: "native"
  },
  "sillytavern-openai-compatible": {
    baseUrl: "",
    model: "gpt-4o-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0,
    toolMode: "native"
  },
  "sillytavern-claude": {
    baseUrl: "",
    model: "claude-sonnet-4-0",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  "sillytavern-google": {
    baseUrl: "",
    model: "gemini-2.5-pro",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  anthropic: {
    baseUrl: "https://api.anthropic.com",
    model: "claude-sonnet-4-0",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  google: {
    baseUrl: "https://generativelanguage.googleapis.com/v1beta",
    model: "gemini-2.5-pro",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  }
};
function Au() {
  return JSON.parse(JSON.stringify(j0));
}
function ys() {
  return {
    provider: f6,
    modelConfigs: Au(),
    permissionMode: h6
  };
}
function ZZ(e = ys()) {
  const t = e && typeof e == "object" ? e : ys();
  return {
    provider: kS(t.provider),
    modelConfigs: gs(t.modelConfigs || {})
  };
}
function Tu(e) {
  return e === "full" ? "full" : h6;
}
function Yo(e) {
  return e === "allow" ? "allow" : XZ;
}
function Hn(e) {
  return String(e || "").trim() || "默认";
}
function gs(e = {}) {
  const t = Au();
  return Object.keys(j0).forEach((n) => {
    t[n] = {
      ...j0[n],
      ...e && typeof e[n] == "object" ? e[n] : {}
    };
  }), t;
}
function kS(e) {
  return typeof e == "string" && e.trim() ? e : f6;
}
function MS(e = {}, t) {
  return e && typeof e.presets == "object" && e.presets ? e.presets : e?.modelConfigs ? { [t]: {
    provider: e.provider || "openai-compatible",
    modelConfigs: e.modelConfigs,
    permissionMode: e.permissionMode
  } } : {};
}
function eee(e = {}, t) {
  const n = {}, r = MS(e, t);
  return Object.entries(r).forEach(([s, a]) => {
    if (!a || typeof a != "object") return;
    const o = Hn(s);
    n[o] = {
      provider: kS(a.provider),
      modelConfigs: gs(a.modelConfigs || {}),
      permissionMode: Tu(a.permissionMode)
    };
  }), Object.keys(n).length || (n[xS] = ys()), n;
}
function tee(e, t) {
  const n = Hn(t);
  return e[n] ? n : Object.keys(e)[0];
}
function nee(e, t, n) {
  const r = Hn(t || n);
  return e[r] ? r : e[n] ? n : Object.keys(e)[0];
}
function ree(e = {}, t = ys()) {
  const n = ZZ(t), r = e && typeof e == "object" ? e : {};
  return {
    provider: kS(r.provider || n.provider),
    modelConfigs: gs(r.modelConfigs || n.modelConfigs)
  };
}
function see(e = {}, t, n, r, s) {
  const a = s(e?.[r]);
  if (a) return a;
  const o = MS(e, t), l = [
    n,
    t,
    e?.currentPresetName,
    e?.delegatePresetName,
    ...Object.keys(o || {})
  ].map(Hn), i = /* @__PURE__ */ new Set();
  for (const u of l) {
    if (i.has(u)) continue;
    i.add(u);
    const d = s(o?.[u]?.[r]);
    if (d) return d;
  }
  return s(e?.delegateConfig?.[r]);
}
function aee(e = {}, t, n) {
  const r = (l) => String(l || "").trim();
  if (r(e?.tavilyBaseUrl)) return Ds(e.tavilyBaseUrl);
  const s = MS(e, t), a = [
    n,
    t,
    e?.currentPresetName,
    e?.delegatePresetName,
    ...Object.keys(s || {})
  ].map(Hn), o = /* @__PURE__ */ new Set();
  for (const l of a) {
    if (o.has(l)) continue;
    o.add(l);
    const i = s?.[l]?.tavilyBaseUrl;
    if (r(i)) return Ds(i);
  }
  return r(e?.delegateConfig?.tavilyBaseUrl) ? Ds(e.delegateConfig.tavilyBaseUrl) : YZ;
}
function oee(e = {}, t, n) {
  return {
    tavilyApiKey: see(e, t, n, "tavilyApiKey", X0),
    tavilyBaseUrl: aee(e, t, n)
  };
}
function ef(e = {}) {
  const t = Hn(e.currentPresetName || e.presetDraftName || "默认"), n = eee(e, t), r = tee(n, e.currentPresetName), s = nee(n, e.delegatePresetName, r), a = n[r] || ys(), o = n[s] || a, l = ree(e.delegateConfig, o), i = oee(e, t, r);
  return {
    workspaceFileName: String(e.workspaceFileName || ""),
    jsApiPermission: Yo(e.jsApiPermission),
    currentPresetName: r,
    delegatePresetName: s,
    delegateConfig: l,
    presetDraftName: Hn(e.presetDraftName || r),
    presetNames: Object.keys(n),
    presets: n,
    provider: a.provider,
    modelConfigs: a.modelConfigs,
    permissionMode: Tu(a.permissionMode),
    tavilyApiKey: i.tavilyApiKey,
    tavilyBaseUrl: i.tavilyBaseUrl
  };
}
var HI = 900 * 1e3, zI = Object.freeze([{
  value: "native",
  label: "原生 Tool Calling"
}, {
  value: "tagged-json",
  label: "Tagged JSON 兼容模式"
}]), Q0 = Object.freeze([
  {
    value: "low",
    label: "低"
  },
  {
    value: "medium",
    label: "中"
  },
  {
    value: "high",
    label: "高"
  }
]), iee = Object.freeze([
  {
    value: "openai-responses",
    label: "OpenAI Responses"
  },
  {
    value: "openai-compatible",
    label: "OpenAI 兼容"
  },
  {
    value: "sillytavern-openai-compatible",
    label: "酒馆 OpenAI 兼容"
  },
  {
    value: "sillytavern-claude",
    label: "酒馆 Claude"
  },
  {
    value: "sillytavern-google",
    label: "酒馆 Google AI"
  },
  {
    value: "anthropic",
    label: "Anthropic"
  },
  {
    value: "google",
    label: "Google AI"
  }
]);
function KI(e = "") {
  return e === "anthropic" || e === "sillytavern-claude";
}
function lee(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function ps(e = "") {
  return Q0.some((t) => t.value === e) ? e : "medium";
}
function ms(e, t = 0.2) {
  const n = typeof e == "string" && !e.trim() ? t : e, r = Number(n);
  return Number.isFinite(r) ? Math.max(0, Math.min(2, r)) : ms(t, 0.2);
}
function Fu(e = {}) {
  return e.sendTemperature !== !1;
}
function WI(e = {}) {
  return Fu(e) ? ms(e.temperature, 0.2) : void 0;
}
function GI(e = "", t = {}) {
  return t && typeof t == "object" && t[e] ? t[e] : iee.find((n) => n.value === e)?.label || e || "未配置";
}
function Iv(e = {}, t = {}) {
  const n = ef(e || {});
  if (t.role === "delegate" && n.delegateConfig) {
    const i = n.delegateConfig.provider || "openai-compatible", u = (n.delegateConfig.modelConfigs || Au())[i] || Au()[i] || {};
    return {
      currentPresetName: String(n.delegatePresetName || n.currentPresetName || ""),
      provider: i,
      baseUrl: String(u.baseUrl || ""),
      model: String(u.model || ""),
      apiKey: String(u.apiKey || ""),
      tavilyApiKey: X0(n.tavilyApiKey),
      tavilyBaseUrl: Ds(n.tavilyBaseUrl),
      temperature: WI(u),
      sendTemperature: Fu(u),
      maxTokens: KI(i) ? 32e3 : null,
      timeoutMs: Number(t.timeoutMs) || 9e5,
      toolMode: u.toolMode || "native",
      reasoningEnabled: !!u.reasoningEnabled,
      reasoningEffort: ps(u.reasoningEffort)
    };
  }
  const r = Hn(t.presetName || (t.role === "delegate" ? n.delegatePresetName : n.currentPresetName) || "默认"), s = n.presets?.[r] ? r : n.presets?.[n.currentPresetName] ? n.currentPresetName : xS, a = n.presets?.[s] || ys(), o = a.provider || n.provider || "openai-compatible", l = (a.modelConfigs || n.modelConfigs || Au())[o] || Au()[o] || {};
  return {
    currentPresetName: String(s || ""),
    provider: o,
    baseUrl: String(l.baseUrl || ""),
    model: String(l.model || ""),
    apiKey: String(l.apiKey || ""),
    tavilyApiKey: X0(n.tavilyApiKey),
    tavilyBaseUrl: Ds(n.tavilyBaseUrl),
    temperature: WI(l),
    sendTemperature: Fu(l),
    maxTokens: KI(o) ? 32e3 : null,
    timeoutMs: Number(t.timeoutMs) || 9e5,
    toolMode: l.toolMode || "native",
    reasoningEnabled: !!l.reasoningEnabled,
    reasoningEffort: ps(l.reasoningEffort)
  };
}
function PS(e = {}, t = {}) {
  if (!e.apiKey && !lee(e.provider)) throw new Error(t.missingApiKeyMessage || "请先填写当前模型配置的 API Key。");
  switch (e.provider) {
    case "sillytavern-openai-compatible":
      return new JZ(e);
    case "sillytavern-claude":
      return new LZ(e);
    case "sillytavern-google":
      return new WZ(e);
    case "openai-responses":
      return new CZ(e);
    case "anthropic":
      return new Fz(e);
    case "google":
      return new Nj(e);
    default:
      return new mZ(e);
  }
}
function uee(e = {}) {
  const t = String(e?.name || "").trim();
  if (typeof e?.arguments == "string") {
    const n = e.arguments;
    try {
      return JSON.parse(n.trim() || "{}"), n;
    } catch {
      return Q5(n, t) || n;
    }
  }
  try {
    return JSON.stringify(e?.arguments || {});
  } catch {
    return "{}";
  }
}
function Z0(e = [], t = {}) {
  const n = String(t.fallbackPrefix || "agent-tool").trim() || "agent-tool", r = typeof t.createId == "function" ? t.createId : (s) => `${n}-${Date.now()}-${s + 1}`;
  return (Array.isArray(e) ? e : []).map((s, a) => ({
    id: String(s?.id || r(a) || `${n}-${a + 1}`),
    name: String(s?.name || "").trim(),
    arguments: uee(s)
  })).filter((s) => s.name);
}
function cee(e, t = {}) {
  return (Array.isArray(e?.googleContent?.parts) ? e.googleContent.parts : []).filter((n) => n?.functionCall?.name).map((n, r) => ({
    id: String(n.functionCall.id || `${t.fallbackPrefix || "google-tool"}-${r + 1}`),
    name: String(n.functionCall.name || ""),
    arguments: JSON.stringify(n.functionCall.args || {})
  })).filter((n) => n.name);
}
function p6(e = {}, t = {}, n = {}) {
  const r = Z0(e?.toolCalls, n);
  return r.length ? r : String(e?.provider || t?.provider || "").toLowerCase() !== "google" ? [] : Z0(cee(e?.providerPayload, n), n);
}
function m6(e = {}, t = [], n = {}) {
  return {
    role: "assistant",
    content: Object.prototype.hasOwnProperty.call(n, "content") ? String(n.content || "") : String(e.text || ""),
    providerPayload: e.providerPayload,
    tool_calls: Z0(t, n).map((r) => ({
      id: r.id,
      type: "function",
      function: {
        name: r.name,
        arguments: r.arguments || "{}"
      }
    }))
  };
}
function g6(e = {}) {
  const t = String(e.toolName || e.tool_name || "").trim();
  return {
    role: "tool",
    tool_call_id: String(e.toolCallId || e.tool_call_id || ""),
    ...t ? { toolName: t } : {},
    content: String(e.content || "")
  };
}
function e1(e) {
  return typeof e == "string" && e.trim().length > 0;
}
var RS = "ActionCheck";
var dee = [
  "[Runtime Protocol: Action Checks]",
  "",
  "In this reply, when even you cannot know whether a key action succeeds or fails, you may roll once and leave the outcome to luck instead of deciding it yourself. Think like a tabletop game master: when fate is genuinely undecided, let the die speak.",
  "",
  "Use it boldly when an attempt could truly succeed or fail and the result would change what happens next: climbing a wall, sneaking, confrontation, deception, persuasion, gambling, a chase, spellcasting, spotting a lie, risky improvisation, and similar moments.",
  "",
  "Do not roll when the outcome is already settled. If one side has overwhelming advantage in numbers, strength, position, or common sense, the result is already decided; do not use a roll to overturn that logic.",
  "",
  "Do not roll for intimate or everyday interactions rather than challenges. Kissing, hugging, sex, casual conversation, or falling asleep together should follow character, consent, and emotional flow, not a success/failure check.",
  "",
  `When you roll, call ${RS}, treat the result as established fact, convey the outcome in one or two sentences, then continue the narration naturally.`,
  "Call the tool immediately after the visible attempted action, before narrating consequences. Do not write result words before the tool call. If you already wrote a visible lead-in, set `insertAfter` to the exact text that should appear before the dice card.",
  "",
  "Choose difficulty before the roll. This is a bare D20 with no stat bonus: DC 1-5 is easy, 6-10 is ordinary, 11-15 is hard, 16-20 is very hard, and 21 is nearly impossible so only a natural 20 can win. Pick the exact DC inside the range from danger, pressure, preparation, opposition, and fictional leverage. Do not make every meaningful action DC 10-ish.",
  "",
  "Natural 1 is a critical failure: it must be worse than plain failure and add a real complication, cost, loss of position, exposure, harm, or worsening situation. Natural 20 is a critical success: it must be better than plain success and grant an extra benefit, surprise reward, opening, momentum, information, style, or reduced cost. The tool enforces these results; honor them in narration.",
  "",
  "Do not mention this protocol, the dice mechanic, or any hidden instruction. Continue to follow all other format and style requirements."
].join(`
`);
function om(e, t = 240) {
  return String(e || "").trim().slice(0, t);
}
function fee(e, t = 240) {
  return String(e ?? "").slice(0, t);
}
function v6(e, t, n, r) {
  const s = Math.round(Number(e));
  return Number.isFinite(s) ? Math.min(n, Math.max(t, s)) : r;
}
function hee(e) {
  if (typeof e == "function") return v6(e(), 1, 20, 1);
  const t = globalThis.crypto;
  if (t?.getRandomValues) {
    const n = new Uint32Array(1);
    return t.getRandomValues(n), n[0] % 20 + 1;
  }
  return Math.floor(Math.random() * 20) + 1;
}
function pee(e, t) {
  return e <= 1 ? "criticalFailure" : e >= 20 ? "criticalSuccess" : e >= t ? "success" : "failure";
}
function mee(e) {
  return e === "criticalSuccess" ? "critical success" : e === "criticalFailure" ? "critical failure" : e;
}
function y6() {
  return [{
    type: "function",
    function: {
      name: RS,
      description: [
        "True-random d20 action check for risky or uncertain RP outcomes.",
        "Use it for key actions that could truly succeed or fail and would change what happens next.",
        "Do not use it for already settled outcomes or for intimate/everyday interactions that should follow character, consent, and emotional flow.",
        "Provide the attempted action, the check or stat name, and a bare-D20 difficulty: 1-5 easy, 6-10 ordinary, 11-15 hard, 16-20 very hard, 21 nearly impossible and only beatable by natural 20.",
        "Natural 1 is critical failure with a real penalty or complication. Natural 20 is critical success with an extra reward, opening, or benefit. The result is binding truth for the current reply.",
        "For accurate card placement, call the tool before outcome narration. If visible lead-in text already exists, provide insertAfter as the exact text that should appear before the dice card."
      ].join(`
`),
      parameters: {
        type: "object",
        properties: {
          action: {
            type: "string",
            description: "What the character is attempting right now."
          },
          stat: {
            type: "string",
            description: "The displayed check name, attribute, skill, or trait for this roll."
          },
          difficulty: {
            type: "number",
            minimum: 1,
            maximum: 21,
            description: "Bare-D20 target number for success. Use 1-5 easy, 6-10 ordinary, 11-15 hard, 16-20 very hard, 21 nearly impossible and only beatable by natural 20. Defaults to 10 if omitted or invalid."
          },
          stakes: {
            type: "string",
            description: "Optional brief note about what is at risk. Display-only."
          },
          insertAfter: {
            type: "string",
            description: "Optional exact visible lead-in text that should appear before the dice card. Do not include consequence or outcome text."
          }
        },
        required: ["action", "stat"],
        additionalProperties: !1
      }
    }
  }];
}
function gee() {
  return {
    role: "system",
    content: dee
  };
}
function vee(e = {}, t = {}) {
  const n = om(e.action, 240), r = om(e.stat, 120);
  if (!n || !r) return {
    ok: !1,
    error: "action_check_action_and_stat_required",
    summary: "ActionCheck 需要同时提供 action 和 stat。"
  };
  const s = v6(e.difficulty, 1, 21, 10), a = hee(t.rollDie), o = pee(a, s), l = o === "success" || o === "criticalSuccess", i = om(e.stakes, 240), u = fee(e.insertAfter, 240);
  return {
    ok: !0,
    action: n,
    stat: r,
    difficulty: s,
    roll: a,
    success: l,
    outcome: o,
    summary: `${r} check ${a} vs DC ${s}: ${mee(o)}.`,
    ...i ? { stakes: i } : {},
    ...u.trim() ? { insertAfter: u } : {}
  };
}
function yee(e = "") {
  return {
    ok: !1,
    error: "action_check_unknown_tool",
    summary: `主聊天当前只允许调用 ${RS}；${om(e, 80) || "unknown_tool"} 不会执行。`
  };
}
function Qf(e = []) {
  const t = {};
  return e.forEach((n) => {
    const [r] = String(n.id || "").split(":");
    if (!n.changed || ![
      "userInput",
      "worldInfo",
      "aiOutput",
      "reasoning"
    ].includes(r)) return;
    const s = r;
    t[s] = (t[s] || 0) + 1;
  }), t;
}
function _6(e) {
  return !!e && Object.values(e).some((t) => Number(t) > 0);
}
var P_ = [
  {
    start: "<think>",
    end: "</think>"
  },
  {
    start: "<thinking>",
    end: "</thinking>"
  },
  {
    start: "```",
    end: "```"
  }
];
function _g(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").toLowerCase().replace(/\s+/g, "").trim();
}
function b6(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").toLowerCase();
}
function _ee(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "");
}
function bg(e) {
  return e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function w6(e, t = []) {
  if (!e || !t.length) return e;
  let n = e;
  for (const r of t) {
    const s = String(r?.start ?? ""), a = String(r?.end ?? "");
    if (!(!s && !a)) {
      if (s && a) n = n.replace(new RegExp(`${bg(s)}[\\s\\S]*?${bg(a)}`, "gi"), "");
      else if (s) {
        const o = n.toLowerCase().indexOf(s.toLowerCase());
        o >= 0 && (n = n.slice(0, o));
      } else if (a) {
        const o = n.toLowerCase().indexOf(a.toLowerCase());
        o >= 0 && (n = n.slice(o + a.length));
      }
    }
  }
  return n.trim();
}
function S6() {
  try {
    const e = globalThis.localStorage?.getItem?.("summary_panel_config");
    if (!e) return P_;
    const t = JSON.parse(e);
    return (Array.isArray(t?.textFilterRules) ? t.textFilterRules : Array.isArray(t?.vector?.textFilterRules) ? t.vector.textFilterRules : P_).filter((n) => !!n && typeof n == "object");
  } catch {
    return P_;
  }
}
function bee(e = []) {
  return new Set(e.map((t) => _g(t)).filter((t) => t.length >= 2));
}
function A6(e) {
  return !(e.length < 2 || /^[a-z0-9]+$/.test(e) && e.length < 3);
}
function wee(e) {
  return /^[a-z0-9\s_-]+$/.test(e.trim());
}
function VI(e = "") {
  return /^[A-Z0-9]/.test(e);
}
function See(e, t, n) {
  const r = e.slice(0, t).match(/([A-Za-z0-9]+)([\s_-]+)$/);
  if (r && (r[2].includes("-") || r[2].includes("_")) && VI(r[1])) return !0;
  const s = e.slice(n).match(/^([\s_-]+)([A-Za-z0-9]+)/);
  return s ? s[1].includes("-") || s[1].includes("_") || VI(s[2]) : !1;
}
function Aee(e, t, n, r) {
  const s = b6(e).trim(), a = _g(e);
  if (!A6(a)) return [];
  if (wee(s)) {
    const l = s.split(/[\s_-]+/).filter(Boolean);
    if (!l.length) return [];
    const i = l.map(bg).join("[\\s_-]+"), u = new RegExp(`(^|[^a-z0-9])(${i})(?=$|[^a-z0-9])`, "gi"), d = [];
    for (const f of t.matchAll(u)) {
      const m = f[1] || "", v = f[2] || "", y = Number(f.index || 0) + m.length, S = y + v.length;
      l.length === 1 && See(n, y, S) || d.push({
        coordinate: "text",
        start: y,
        end: S,
        length: v.replace(/[\s_-]+/g, "").length,
        tokenCount: l.length
      });
    }
    return d;
  }
  const o = r.indexOf(a);
  return o < 0 ? [] : [{
    coordinate: "compact",
    start: o,
    end: o + a.length,
    length: a.length,
    tokenCount: 1
  }];
}
function Tee(e, t) {
  return e.path === t.path || e.coordinate !== t.coordinate || t.start > e.start || t.end < e.end ? !1 : t.tokenCount > e.tokenCount || t.length > e.length;
}
function Eee(e, t = []) {
  const n = [
    ...t,
    "user",
    "assistant",
    "用户",
    "角色",
    "玩家",
    "旁白"
  ].map((s) => String(s || "").trim()).filter(Boolean);
  if (!n.length) return e;
  const r = n.map(bg).join("|");
  return String(e || "").split(/\r?\n/).map((s) => s.replace(new RegExp(`^\\s*(?:${r})\\s*[:：]\\s*`, "i"), "")).join(`
`);
}
function Cee(e = "") {
  return w6(String(e || ""), S6()).replace(/\[tts:[^\]]*]/gi, " ").replace(/<state>[\s\S]*?<\/state>/gi, " ").replace(/<status[^>]*>[\s\S]*?<\/status>/gi, " ").replace(/\s+/g, " ").trim();
}
function JI(e = "") {
  return w6(String(e || ""), S6()).replace(/\[tts:[^\]]*]/gi, " ").replace(/\[(?:tavern-image|img|图片)\s*:[^\]]*]/gi, " ").replace(/<state>[\s\S]*?<\/state>/gi, " ").replace(/<status>[\s\S]*?<\/status>/gi, " ").replace(/<status[^>]*>[\s\S]*?<\/status>/gi, " ").replace(/\r\n/g, `
`).trim();
}
function YI(e) {
  if ("title" in e && e.title) return String(e.title || "").trim();
  const t = "content" in e ? String(e.content || "").match(/^\s*#\s+(.+?)\s*$/m)?.[1] : "";
  return String(t || e.path.split("/").pop() || e.path).trim();
}
function Iee(e) {
  return String(("content" in e ? e.content : "") || ("preview" in e ? e.preview : "") || "");
}
function T6(e) {
  const t = String(e.path || "");
  if (e.status === "stale") return !1;
  const n = Nl(t);
  return n && V3(n) ? !1 : sl(t) || eg(t);
}
async function xee(e = "", t = {}) {
  const n = Array.isArray(t.memoryFiles) ? t.memoryFiles : [];
  if (!n.length) return t;
  const r = await Promise.all(n.map(async (s) => ({
    summary: s,
    record: await li(e, s.path)
  })));
  return {
    ...t,
    memoryFiles: r.map(({ summary: s, record: a }) => a ? {
      ...s,
      content: String(a.content || "")
    } : s)
  };
}
function NS(e = {}) {
  return [e.user?.name].map((t) => String(t || "").trim()).filter((t) => t.length >= 2);
}
function E6(e = {}, t = "") {
  const n = Array.isArray(e.history) ? e.history.slice(-2) : [], r = NS(e), s = (a) => Eee(Cee(a), r);
  return [
    s(t),
    ...n.map((a) => s(a.content || a.mes || a.message || "")),
    s(e.character?.name || "")
  ].filter(Boolean).join(`
`);
}
function kee(e = {}) {
  const t = [...e.memoryFiles || []].filter(T6).sort((i, u) => i.path.localeCompare(u.path)), n = bee(e.ignoredTerms || []), r = _ee(e.queryText || ""), s = b6(e.queryText || ""), a = _g(s), o = t.map((i) => ({
    file: i,
    characterName: Nl(i.path)
  })).flatMap((i) => {
    if (!eg(i.file.path)) return [];
    const u = _g(i.characterName);
    return !A6(u) || n.has(u) ? [] : Aee(i.characterName, s, r, a).map((d) => ({
      ...d,
      path: i.file.path,
      file: i.file,
      characterName: i.characterName
    }));
  }), l = new Set(o.filter((i) => !o.some((u) => Tee(i, u))).map((i) => i.path));
  return { memoryFiles: t.filter((i) => sl(i.path) || l.has(i.path)).map((i) => ({
    file: i,
    characterName: Nl(i.path)
  })).sort((i, u) => (sl(i.file.path) ? 0 : 1) - (sl(u.file.path) ? 0 : 1) || i.file.path.localeCompare(u.file.path)).map((i) => ({
    ...i.file,
    title: eg(i.file.path) ? i.characterName : YI(i.file),
    recallReason: sl(i.file.path) ? "fixed" : "entity",
    recallScore: sl(i.file.path) ? 0 : 1
  })).map((i) => ({
    path: i.path,
    title: YI(i),
    content: Iee(i),
    recallReason: i.recallReason,
    recallScore: i.recallScore
  })) };
}
async function C6(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return {};
  const n = e.includeMemoryFiles !== !1, r = e.includeStructuredStates !== !1, [s, a, o] = await Promise.all([
    n ? Q3(t) : Promise.resolve(null),
    r ? Zq(t) : Promise.resolve([]),
    r ? Qq(t) : Promise.resolve("")
  ]), l = n && s?.status === "ready" && Array.isArray(s.files) || n && s?.status === "stale" && s.error && Array.isArray(s.files) ? s : n ? await Hf(t) : null, i = Array.isArray(l?.files) ? l.files : [], u = i.some(T6);
  if (!u && !a.length && !o) return {};
  const d = u ? kee({
    memoryFiles: i,
    queryText: e.queryText,
    ignoredTerms: e.ignoredTerms
  }) : {};
  return {
    ...u ? await xee(t, d) : d,
    structuredStates: a,
    ...o ? { spatialState: o } : {}
  };
}
var Mee = Object.freeze({
  tools: [],
  toolChoice: "none"
});
function Pee(e, t = Mee) {
  return {
    provider: e,
    capabilities: t,
    buildChatTask(n) {
      return {
        messages: n.messages,
        tools: t.tools,
        toolChoice: t.toolChoice,
        temperature: e.temperature,
        maxTokens: e.maxTokens,
        signal: n.signal,
        onStreamProgress: n.onStreamProgress
      };
    }
  };
}
var Ree = 3.35, Nee = /* @__PURE__ */ new Set([
  "openai-compatible",
  "openai-responses",
  "sillytavern-openai-compatible"
]), $ee = new TextEncoder();
function Lee(e = []) {
  return e.map((t) => {
    const n = Array.isArray(t.content) ? t.content.map((r) => !r || typeof r != "object" ? "" : r.type === "text" ? r.text || "" : r.type === "image_url" ? `[image:${r.name || r.mimeType || "image"}]` : "").filter(Boolean).join(`
`) : t.content || "";
    return t.role === "assistant" && Array.isArray(t.tool_calls) && t.tool_calls.length ? {
      role: "assistant",
      content: [n, t.tool_calls.map((r) => JSON.stringify({
        id: r.id,
        name: r.function?.name || "",
        arguments: r.function?.arguments || "{}"
      })).join(`
`)].filter(Boolean).join(`
`)
    } : t.role === "tool" ? {
      role: "tool",
      content: [t.tool_call_id || "", t.content || ""].filter(Boolean).join(`
`)
    } : {
      role: t.role,
      content: n
    };
  });
}
function I6(e = [], t = []) {
  return [...Lee(e), {
    role: "system",
    content: t.length ? `TOOLS
${JSON.stringify(t)}` : ""
  }].filter((n) => n.content);
}
function Dee(e = "") {
  return Math.ceil($ee.encode(String(e || "")).length / Ree);
}
function XI({ messages: e = [], tools: t = [] } = {}) {
  return Dee(JSON.stringify(I6(e, t)));
}
function Oee(e = {}) {
  const t = String(e?.model || "").trim();
  return t || (e?.provider === "anthropic" ? "claude" : "gpt-4o");
}
async function x6(e, t) {
  const n = await fetch(e, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(t)
  });
  if (!n.ok) throw new Error(`tokenizer_http_${n.status}`);
  return await n.json();
}
async function Fee(e = [], t = "") {
  if (!e.length) return 0;
  const n = `/api/tokenizers/openai/count?model=${encodeURIComponent(t || "gpt-4o")}`;
  let r = -1;
  for (const s of e) {
    const a = await x6(n, [s]), o = Number(a?.token_count);
    if (!Number.isFinite(o)) throw new Error("tokenizer_invalid_response");
    r += o;
  }
  return Math.max(0, r);
}
async function Bee(e, t) {
  const n = await x6(e, { text: t }), r = Number(n?.count);
  if (!Number.isFinite(r)) throw new Error("tokenizer_invalid_response");
  return r;
}
async function Uee({ messages: e = [], tools: t = null, providerConfig: n = {} } = {}) {
  const r = String(n?.provider || ""), s = Array.isArray(t) ? t : [], a = I6(e, s), o = JSON.stringify(a);
  try {
    if (Nee.has(r)) return await Fee(a, Oee(n));
    if (r === "anthropic") return await Bee("/api/tokenizers/claude/encode", o);
  } catch {
    return XI({
      messages: e,
      tools: s
    });
  }
  return XI({
    messages: e,
    tools: s
  });
}
var k6 = [
  vr.LS,
  vr.GREP,
  vr.READ
], t1 = [vr.EDIT, vr.WRITE], wg = Object.values(_s), Sg = Object.values(Pl);
function qee(e = "") {
  const t = String(e || "").trim();
  return !t || k6.includes(t) ? null : t1.includes(t) ? "memoryArchiving" : wg.includes(t) ? "cartographyEngine" : Sg.includes(t) ? "questOrchestration" : null;
}
function Hee(e = "") {
  return wg.includes(String(e || "").trim());
}
function zee(e = "") {
  return Sg.includes(String(e || "").trim());
}
function tf(e) {
  const t = _f(e), n = [...k6];
  t.includeMemoryFiles && n.push(...t1), t.includeStructuredStates && n.push(...wg), t.includeQuestOrchestration && n.push(...Sg);
  const r = new Set(n), s = [
    ...t1,
    ...wg,
    ...Sg
  ].filter((a) => !r.has(a));
  return {
    runtime: t,
    allowedToolNames: [...r],
    deniedToolNames: s
  };
}
function Kee(e = [], t) {
  const n = new Set(tf(t).allowedToolNames);
  return e.filter((r) => n.has(String(r?.function?.name || "").trim()));
}
function Wee(e = "", t) {
  return new Set(tf(t).allowedToolNames).has(String(e || "").trim());
}
function Gee(e = "", t) {
  const n = String(e || "").trim(), r = qee(n), s = r ? xU(r)?.title || r : "Auto Manager", a = r ? `契约未授权 ${s}，本轮不会执行 ${n || "该工具"}。` : `自动管理员当前不会执行 ${n || "该工具"}。`;
  return Hee(n) ? {
    ok: !1,
    summary: a,
    changed: !1,
    error: "contract_tool_unauthorized",
    warnings: tf(t).runtime.hasAutomaticManagerWork ? [] : ["contract_manager_work_disabled"]
  } : zee(n) ? {
    ok: !1,
    summary: a,
    changed: !1,
    error: "contract_tool_unauthorized",
    warnings: tf(t).runtime.hasAutomaticManagerWork ? [] : ["contract_manager_work_disabled"]
  } : {
    ok: !1,
    summary: a,
    changed: !1,
    error: "contract_tool_unauthorized",
    warning: tf(t).runtime.hasAutomaticManagerWork ? "" : "contract_manager_work_disabled"
  };
}
var Vee = {
  "openai-responses": "OpenAI Responses",
  "openai-compatible": "OpenAI 兼容",
  "sillytavern-openai-compatible": "酒馆 OpenAI 兼容",
  "sillytavern-claude": "酒馆 Claude",
  "sillytavern-google": "酒馆 Google AI",
  anthropic: "Anthropic",
  google: "Google AI"
};
function Jee(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function M6(e = "") {
  return Vee[e] || e || "未配置";
}
function $S(e = {}) {
  const t = Iv(e || {}, { timeoutMs: 9e5 }), n = String(t.provider || ""), r = String(t.model || "").trim(), s = String(t.apiKey || "").trim(), a = [];
  r || a.push("模型"), !Jee(n) && !s && a.push("API Key");
  const o = a.length ? `请先在 API 配置里选择模型/填写 Key：缺少 ${a.join("、")}` : "API 配置可用";
  return {
    currentPresetName: String(t.currentPresetName || ""),
    provider: n,
    providerLabel: M6(n),
    baseUrl: String(t.baseUrl || ""),
    model: r,
    apiKey: s,
    temperature: Number(t.temperature ?? 0.2),
    maxTokens: t.maxTokens === null || t.maxTokens === void 0 ? null : Number(t.maxTokens),
    timeoutMs: Number(t.timeoutMs) || 900 * 1e3,
    toolMode: String(t.toolMode || "native"),
    readiness: {
      ok: a.length === 0,
      missing: a,
      message: o
    }
  };
}
function P6(e = {}) {
  const t = $S(e);
  if (!t.readiness.ok) throw new Error(t.readiness.message);
  return t;
}
function xv(e) {
  const t = e.supportsSessionToolLoop === !0, n = Array.isArray(e.messages) ? e.messages : [], r = Array.isArray(e.toolResponses) ? e.toolResponses : [], s = String(e.finalAnswerReminderText || "").trim();
  return t && r.length ? {
    mode: "session_tool_response_round",
    requestMessages: [],
    toolResponses: r,
    finalAnswerReminderText: "",
    supportsSessionToolLoop: t
  } : t && s ? {
    mode: "session_final_reminder_round",
    requestMessages: [],
    toolResponses: [],
    finalAnswerReminderText: s,
    supportsSessionToolLoop: t
  } : {
    mode: "full_prompt_round",
    requestMessages: n,
    toolResponses: [],
    finalAnswerReminderText: "",
    supportsSessionToolLoop: t
  };
}
function R6(e, t) {
  return delete e.messages, delete e.toolResponses, delete e.finalAnswerReminderText, t.mode === "full_prompt_round" ? (e.messages = t.requestMessages, e) : t.mode === "session_tool_response_round" ? (e.toolResponses = t.toolResponses, e) : (t.mode === "session_final_reminder_round" && (e.finalAnswerReminderText = t.finalAnswerReminderText), e);
}
function Yee(e) {
  return e.mode === "session_tool_response_round" ? { message: {
    role: "user",
    parts: e.toolResponses.filter((t) => t && t.name).map((t) => ({ functionResponse: {
      name: String(t.name || ""),
      response: t.response || {}
    } }))
  } } : e.mode === "session_final_reminder_round" ? { message: [{ text: e.finalAnswerReminderText }] } : null;
}
var ll = "accepted_turn", LS = 300 * 1e3, Xee = Uee, ad = 158e3;
var n1 = /* @__PURE__ */ new Map();
function im(e = "", t = 4e3) {
  const n = String(e || "").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function jI(e) {
  try {
    return JSON.stringify(e, null, 2);
  } catch {
    return String(e || "");
  }
}
function jee(e, t = {}) {
  if (e && typeof e == "object" && !Array.isArray(e)) return e;
  try {
    const n = JSON.parse(String(e || "{}"));
    return n && typeof n == "object" && !Array.isArray(n) ? n : t;
  } catch {
    return t;
  }
}
function N6(e, t = {}) {
  return SU(e, t).trim();
}
function $6(e) {
  return _f(e);
}
function L6(e) {
  const t = e.find((n) => n.path === "memory/state.md");
  return ["[Resident Memory Files]", ...[t ? ["[memory/state.md]", t.content].join(`
`) : ""].filter(Boolean)].join(`

`);
}
function Qee(e) {
  const t = e.runtime.managerPromptOptions.includeMemory, n = e.runtime.managerPromptOptions.includeCartography, r = e.runtime.managerPromptOptions.includeQuestOrchestration, s = [];
  let a = 1;
  return t && (s.push(`${a}. Read \`memory/state.md\` and relevant \`memory/characters/<角色名>.md\` files as needed, then update memory only if this accepted assistant reply changed durable memory.`), a += 1, s.push(`${a}. Do not create or maintain user/player character files such as \`memory/characters/User.md\`, \`Player.md\`, \`用户.md\`, or \`玩家.md\`. Treat user messages as source evidence, not as a user persona sheet; ignore status-bar/UI/meta text unless the assistant reply establishes it inside RP.`), a += 1), n && (s.push(`${a}. Spatial maintenance has two layers: \`tavern.atlas/main\` is the single world index, and each local scene map lives in its own place-named \`tavern.map/<docId>\`. Move the player in atlas only when the story reaches a new named or trackable place; small movement inside the same place belongs in the scene map actor coordinates. Map \`activate:true\` only switches the map tool doc and does not move the player.`), a += 1), t && n ? (s.push(`${a}. Maintain \`memory/state.md\` for global memory and \`memory/characters/<角色名>.md\` for character memory. The map does not replace written memory.`), a += 1) : t ? (s.push(`${a}. Maintain \`memory/state.md\` for global memory and \`memory/characters/<角色名>.md\` for character memory.`), a += 1) : n ? (s.push(`${a}. This contract authorizes only the map system. Do not write memory Markdown.`), a += 1) : r || (s.push(`${a}. This contract authorizes neither background memory nor map maintenance. Do not write memory or map records; clearly say that you skipped it.`), a += 1), r && (s.push(`${a}. 事件：先看当前方向池；信息不足时再用 EventInspect。如果上一条回复真的推动或了结了某个活跃方向，advance 或 complete 它。如果活跃方向只剩 0-1 个且已过第 5 楼，并且你想到一个有野心、对味的新方向，就 upsert 一个新的。想不到好的就别造；过期方向系统自动清，不用手动删。什么算好方向按事件引擎规则，字段按工具说明。`), a += 1), s.push(`${a}. Close with a short result: say what you wrote, skipped, or left pending.`), [
    ...t ? [L6(e.memoryFiles), ""] : [],
    ...r ? [String(e.taskPoolBlock || `[Current Event Pool]
Active directions: unknown.`).trim(), ""] : [],
    "[本轮 RP 原文]",
    "[用户消息]",
    JI(e.userMessage.content),
    "",
    "[角色回复]",
    JI(e.assistantMessage.content),
    "",
    "[本轮要求]",
    ...s
  ].join(`
`);
}
function Zee(e) {
  return [
    L6(e.memoryFiles),
    "",
    "[当前问题]",
    e.question
  ].join(`
`);
}
function Ag(e) {
  return String(e.trigger || "") === "manager_chat" ? `manager chat; turn ${Math.max(0, Number(e.turn) || 0)}; question ${String(e.text || "").length} chars` : `turn ${Math.max(0, Number(e.turn) || 0)}; messages ${Number(e.userOrder)}/${Number(e.assistantOrder)}; user ${String(e.text || "").length} chars`;
}
async function ete(e, t, n) {
  const r = Array.isArray(n.messages) ? n.messages : [], s = {
    systemPrompt: r[0]?.content || "",
    tools: Array.isArray(n.tools) ? n.tools : [],
    toolChoice: n.toolChoice || (n.tools?.length ? "auto" : "none"),
    temperature: t.temperature,
    maxTokens: t.maxTokens,
    reasoning: {
      enabled: t.reasoningEnabled,
      effort: t.reasoningEffort
    },
    signal: n.signal,
    onStreamProgress: (o) => {
      n.onStreamProgress?.({
        ...typeof o.text == "string" ? { text: o.text } : {},
        ...Array.isArray(o.thoughts) ? { thoughts: Tg(o.thoughts) } : {},
        ...Array.isArray(o.toolCalls) ? { toolCalls: o.toolCalls } : {},
        ...o.toolCallDraft ? { toolCallDraft: !0 } : {}
      });
    }
  };
  R6(s, xv({
    supportsSessionToolLoop: e?.supportsSessionToolLoop === !0,
    messages: r,
    toolResponses: n.toolResponses,
    finalAnswerReminderText: n.finalAnswerReminderText
  }));
  const a = await e.chat(s);
  return {
    text: String(a?.text || "").trim(),
    provider: String(a?.provider || t.provider || ""),
    model: String(a?.model || t.model || ""),
    toolCalls: Array.isArray(a?.toolCalls) ? a.toolCalls : [],
    thoughts: Tg(a?.thoughts),
    providerPayload: a?.providerPayload
  };
}
function QI(e = {}) {
  return [
    "filePath",
    "path",
    "pattern",
    "mode",
    "docType",
    "docId",
    "elementId",
    "startOrder",
    "endOrder"
  ].map((t) => {
    const n = im(e[t], 160);
    return n ? `${t}: ${n}` : "";
  }).filter(Boolean).join("; ");
}
function ZI(e) {
  const t = im(e.summary || e.error || "", 360);
  if (e.ok !== !1) return t;
  const n = e.details;
  return !Array.isArray(n) || !n.length ? t : im([t, n.slice(0, 2).map((r) => {
    const s = r && typeof r == "object" ? r : {};
    return im(s.hint || s.error || r, 180);
  }).filter(Boolean).join("；")].filter(Boolean).join(" "), 420);
}
function tte(e = "") {
  return Object.values(_s).includes(e);
}
function nte(e = "") {
  return Object.values(Pl).includes(e);
}
function rte(e = "") {
  return Object.values(vr).includes(e);
}
function Tg(e) {
  if (!Array.isArray(e)) return [];
  const t = [];
  return e.forEach((n, r) => {
    const s = n && typeof n == "object" ? n : {}, a = String(s.text || s.content || s.thinking || "").trim();
    a && t.push({
      label: String(s.label || s.summary || `思考 ${r + 1}`).trim() || `思考 ${r + 1}`,
      text: a
    });
  }), t;
}
async function ex(e = "", t = []) {
  const n = String(e || "").trim();
  n && await yr(n, {
    status: "running",
    toolTrace: t.map((r) => ({ ...r }))
  });
}
function ste(e, t) {
  if (t?.aborted) return !0;
  const n = e instanceof Error ? e.message : String(e || "");
  return (e instanceof Error ? e.name : "") === "AbortError" || /abort|aborted|cancelled|canceled/i.test(n);
}
function DS(e, t) {
  const n = e instanceof Error ? e.message : String(e || "");
  return ste(e, t) ? "cancelled" : n === "manager_source_messages_changed" ? "superseded" : "failed";
}
async function OS(e = "") {
  const [t, n] = await Promise.all([qf(e), Pw(e)]), r = t.some((u) => String(u.afterHash || "").trim()), s = n.some((u) => String(u.afterHash || "").trim());
  if (!r && !s) return null;
  const a = r ? await u3(e) : {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  }, o = s ? await K3(e) : {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  }, l = [...a.conflicts, ...o.conflicts], i = await yr(e, {});
  return i && l.length && await yr(e, { error: D6(i.error, l) }), {
    managerRun: await yr(e, {}),
    conflicts: l
  };
}
function D6(e = "", t = []) {
  const n = String(e || "").trim();
  if (!t.length) return n;
  const r = "rollback_conflict:", s = n.startsWith(r) ? n.slice(18).split(",").map((a) => a.trim()).filter(Boolean) : [];
  return `${r}${[.../* @__PURE__ */ new Set([...s, ...t])].join(",")}`;
}
async function tx(e) {
  const t = e.selected.run.id, n = await OS(t);
  if (n?.conflicts.length || await Cg(e.selected.run.sessionId), n?.conflicts.length) {
    const s = n.managerRun || await yr(t, {
      status: "failed",
      error: D6("manager_pending_interrupted_by_current_turn_abort", n.conflicts)
    }) || e.selected.run;
    return await e.onManagerRunSaved?.(s), {
      ok: !1,
      managerRun: s,
      error: s.error || "manager_pending_interrupted_rollback_conflict"
    };
  }
  try {
    await FS(e.selected.run.sessionId, e.selected.run.userOrder, e.selected.run.assistantOrder);
  } catch {
    const s = await yr(t, {
      status: "cancelled",
      error: "manager_source_messages_superseded"
    }) || e.selected.run;
    return await e.onManagerRunSaved?.(s), {
      ok: !1,
      managerRun: s,
      error: s.error
    };
  }
  await YU(t);
  const r = await yr(t, {
    status: "pending",
    provider: "",
    model: "",
    outputText: "等待用户继续后维护上一条已接受回复。",
    parsedAction: "",
    toolTrace: void 0,
    changedFiles: [],
    changedStates: [],
    changedTasks: [],
    error: "manager_pending_interrupted_by_current_turn_abort"
  }) || e.selected.run;
  return await e.onManagerRunSaved?.(r), {
    ok: !1,
    managerRun: r,
    error: r.error
  };
}
function Bu(e) {
  if (!e?.aborted) return;
  const t = /* @__PURE__ */ new Error("manager_aborted");
  throw t.name = "AbortError", t;
}
async function ate(e) {
  const t = Iv(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: LS
  }), n = e.executeManagerOnce ? null : PS(t, { missingApiKeyMessage: "请先在 API 配置里填写记忆管理员 API。" }), r = e.executeManagerOnce || ((p) => ete(n, t, p)), s = !!n?.supportsSessionToolLoop || e.executeManagerOnce?.supportsSessionToolLoop === !0, a = e.caller === "auto" ? Kee(e0(), e.sessionContract) : e0(), o = [], l = [], i = /* @__PURE__ */ new Set(), u = /* @__PURE__ */ new Set(), d = /* @__PURE__ */ new Set();
  let f = "", m = "", v = !1, y = null, S = "", x = "", I = 0, E = !1;
  const N = (p) => {
    e.onProtocolEvent?.(p);
  };
  function $(p, w, _) {
    try {
      return `${String(p || "").trim()}|${jI(w)}|${String(_ || "").trim()}`;
    } catch {
      return `${String(p || "").trim()}|${String(_ || "").trim()}`;
    }
  }
  for (let p = 1; p <= 48; p += 1) {
    Bu(e.signal);
    let w = "", _ = [];
    const b = xv({
      supportsSessionToolLoop: s,
      messages: e.messages,
      toolResponses: y,
      finalAnswerReminderText: S
    }), C = await r({
      agentConfig: e.agentConfig,
      messages: b.requestMessages,
      tools: a,
      toolChoice: "auto",
      ...b.mode === "session_tool_response_round" ? { toolResponses: b.toolResponses } : {},
      ...b.mode === "session_final_reminder_round" ? { finalAnswerReminderText: b.finalAnswerReminderText } : {},
      signal: e.signal,
      onStreamProgress: (R) => {
        typeof R.text == "string" && (w = R.text), Array.isArray(R.thoughts) && (_ = Tg(R.thoughts)), e.onStreamProgress?.({
          ...typeof R.text == "string" ? { text: R.text } : {},
          ...Array.isArray(R.thoughts) ? { thoughts: _ } : {},
          ...Array.isArray(R.toolCalls) ? { toolCalls: R.toolCalls } : {},
          ...R.toolCallDraft ? { toolCallDraft: !0 } : {}
        });
      }
    });
    y = null, S = "", Bu(e.signal);
    const k = String(C.text || w || ""), P = k.trim();
    f = String(C.provider || f || t.provider || ""), m = String(C.model || m || t.model || "");
    const H = C, G = Tg(C.thoughts?.length ? C.thoughts : _), ee = p6(H, t, { fallbackPrefix: "tavern-manager-tool" });
    if (!ee.length) {
      if (!e1(P) && o.length && !v) {
        v = !0;
        const V = "工具结果已经齐备，请收束为本轮处理结论。";
        s ? S = V : e.messages.push({
          role: "system",
          content: V
        });
        continue;
      }
      const R = {
        role: "assistant",
        content: P,
        thoughts: G,
        providerPayload: C.providerPayload
      };
      return N({ type: "clear_stream_draft" }), N({
        type: "final_assistant",
        message: R
      }), {
        text: P,
        provider: f,
        model: m,
        toolTrace: o,
        changedFiles: [...i],
        changedStates: [...u],
        changedTasks: [...d],
        protocolMessages: [...l, R]
      };
    }
    const J = m6({
      ...H,
      text: k
    }, ee, { fallbackPrefix: "tavern-manager-tool" });
    J.thoughts = G, J.toolCalls = ee.map((R) => ({
      id: String(R.id || ""),
      name: String(R.name || ""),
      arguments: String(R.arguments || "{}")
    })), e.messages.push(J), l.push(J), N({ type: "clear_stream_draft" }), N({
      type: "assistant_tool_round",
      message: J
    });
    const z = [];
    for (const [R, V] of ee.entries()) {
      const le = jee(V.arguments, {});
      Bu(e.signal);
      const ve = {
        id: String(V.id || ""),
        round: p,
        name: V.name,
        status: "running",
        ok: !0,
        args: QI(le),
        path: "",
        summary: "工具运行中，等待返回。",
        preface: R === 0 ? P : "",
        thoughts: R === 0 ? G : [],
        startedAt: Date.now()
      };
      o.push(ve), await ex(e.managerRunId, o);
      const ke = e.caller === "auto" && !Wee(V.name, e.sessionContract) ? Gee(V.name, e.sessionContract) : rte(V.name) ? await e4(e.sessionId, V.name, le, {
        caller: e.caller,
        managerRunId: e.managerRunId,
        turn: e.turn,
        sourceUserOrder: e.userOrder,
        sourceAssistantOrder: e.assistantOrder,
        beforeWriteGuard: e.beforeWriteGuard,
        contextSnapshot: e.contextSnapshot
      }) : tte(V.name) ? await Jq(e.sessionId, V.name, le, {
        caller: e.caller,
        managerRunId: e.managerRunId,
        sourceUserOrder: e.userOrder,
        sourceAssistantOrder: e.assistantOrder,
        beforeWriteGuard: e.beforeWriteGuard
      }) : nte(V.name) ? await pH(e.sessionId, V.name, le, {
        caller: e.caller,
        managerRunId: e.managerRunId,
        sourceUserOrder: e.userOrder,
        sourceAssistantOrder: e.assistantOrder,
        beforeWriteGuard: e.beforeWriteGuard
      }) : {
        ok: !1,
        summary: `${V.name || "tool"} 不可用。`,
        changed: !1,
        error: "manager_tool_not_available"
      }, be = "path" in ke ? ke.path : "", Ne = "docType" in ke && ke.docType ? `${ke.docType}/${ke.docId || ""}` : "", ce = "eventId" in ke && ke.eventId ? `event/${ke.eventId}` : "";
      ke.changed && be && i.add(be), ke.changed && Ne && u.add(Ne), ke.changed && ce && d.add(ce), Object.assign(ve, {
        status: "resolved",
        ok: ke.ok,
        args: QI(le),
        path: be || Ne || ce,
        summary: ZI(ke),
        error: ke.error || "",
        finishedAt: Date.now()
      }), Number(ve.startedAt) && (ve.elapsedMs = Math.max(0, Number(ve.finishedAt) - Number(ve.startedAt))), await ex(e.managerRunId, o);
      const we = g6({
        toolCallId: V.id,
        toolName: V.name,
        content: jI(ke)
      });
      if (we.toolCallId = String(V.id || ""), we.toolDisplay = ZI(ke), e.messages.push(we), l.push(we), N({
        type: "tool_result",
        message: we
      }), z.push({
        id: V.id,
        name: V.name,
        response: ke
      }), ke.ok)
        x = "", I = 0;
      else {
        const Oe = $(String(V.name || ""), le, String(ke.error || ""));
        Oe === x ? I += 1 : (x = Oe, I = 1);
      }
    }
    if (I >= 3) {
      if (E) throw new Error("manager_tool_repeat_loop");
      v = !0, E = !0;
      const R = "A tool keeps failing with the same arguments and error. Stop repeating it. Either adjust the arguments and strategy, or finish with a summary of what you verified, skipped, or left pending.";
      s ? S = R : e.messages.push({
        role: "system",
        content: R
      }), x = "", I = 0;
    }
    s && (y = z);
  }
  throw new Error("工具轮次达到上限（48），已停止。");
}
async function Sp(e) {
  return FS(e.sessionId, e.userMessage.order, e.assistantMessage.order);
}
async function FS(e = "", t = -1, n = -1) {
  const [r, s] = await Promise.all([bl(e, t), bl(e, n)]), a = r?.role === "user" && r.error !== !0, o = s?.role === "assistant" && s.error !== !0 && !["aborted", "error"].includes(String(s.finishReason || "").trim());
  if (!a || !o) throw new Error("manager_source_messages_changed");
  return {
    userMessage: r,
    assistantMessage: s
  };
}
async function ote(e) {
  const t = Iv(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: LS
  }), n = {
    status: "running",
    provider: M6(String(t.provider || "")),
    model: String(t.model || ""),
    inputSummary: e.inputSummary
  }, r = e.managerRunId ? await yr(e.managerRunId, n) : await qm({
    sessionId: e.sessionId,
    trigger: e.trigger,
    turn: e.turn,
    userOrder: Number.isInteger(Number(e.userOrder)) ? Number(e.userOrder) : -1,
    assistantOrder: Number.isInteger(Number(e.assistantOrder)) ? Number(e.assistantOrder) : -1,
    ...n
  });
  if (!r) throw new Error("manager_run_missing");
  return r;
}
function ite(e = "", t) {
  const n = String(e || "").trim();
  if (!n) return () => {
  };
  let r = !1;
  const s = setInterval(() => {
    r || t?.aborted || GU(n);
  }, 4e3);
  return () => {
    r = !0, clearInterval(s);
  };
}
async function Eg(e, t) {
  return await yr(e.id, t) || e;
}
async function Cg(e = "") {
  const t = String(e || "").trim();
  !t || !await qa(t) || await Hf(t);
}
async function lte(e, t) {
  const n = $6(e.sessionContract);
  n.includeMemoryFiles && await $w(e.sessionId);
  const r = n.includeMemoryFiles ? await ua(e.sessionId, { includeStale: !0 }) : [], s = n.includeQuestOrchestration ? await gH(e.sessionId) : "";
  return [{
    role: "system",
    content: N6(e.assistantPreset, n.managerPromptOptions)
  }, {
    role: "user",
    content: Qee({
      turn: e.turn,
      userMessage: t.userMessage,
      assistantMessage: t.assistantMessage,
      memoryFiles: r,
      taskPoolBlock: s,
      runtime: n
    })
  }];
}
async function O6(e) {
  await $w(e.sessionId);
  const t = await ua(e.sessionId, { includeStale: !0 }), n = Array.isArray(e.history) ? e.history : await Ls(e.sessionId), r = [{
    role: "system",
    content: N6(e.assistantPreset)
  }];
  return n.forEach((s) => {
    const a = s.role === "assistant" && s.error !== !0 && !["aborted", "error"].includes(String(s.finishReason || "").trim()) && Array.isArray(s.toolCalls) ? s.toolCalls : [];
    r.push({
      role: s.role,
      content: String(s.content || ""),
      ...s.name ? { name: s.name } : {},
      ...Array.isArray(s.thoughts) ? { thoughts: s.thoughts } : {},
      ..."providerPayload" in s ? { providerPayload: s.providerPayload } : {},
      ...a.length ? {
        toolCalls: a,
        tool_calls: a.map((o) => ({
          id: o.id || "",
          type: "function",
          function: {
            name: o.name || "",
            arguments: o.arguments || "{}"
          }
        }))
      } : {},
      ...s.role === "tool" ? {
        tool_call_id: s.toolCallId || "",
        toolName: s.toolName || "",
        toolDisplay: s.toolDisplay
      } : {}
    });
  }), r.push({
    role: "user",
    content: Zee({
      question: e.question,
      memoryFiles: t
    })
  }), r;
}
async function F6(e) {
  const t = await ote({
    managerRunId: e.managerRunId,
    sessionId: e.sessionId,
    trigger: e.trigger,
    turn: e.turn,
    userOrder: e.userOrder,
    assistantOrder: e.assistantOrder,
    inputSummary: e.inputSummary,
    agentConfig: e.agentConfig
  });
  let n = "", r = t.provider || "", s = t.model || "", a = [], o = [], l = [], i = [], u = [];
  const d = ite(t.id, e.signal);
  try {
    const f = await ate({
      sessionId: e.sessionId,
      agentConfig: e.agentConfig,
      caller: e.caller,
      messages: e.messages,
      managerRunId: t.id,
      turn: e.turn,
      userOrder: e.userOrder,
      assistantOrder: e.assistantOrder,
      beforeWriteGuard: e.beforeWriteGuard,
      sessionContract: e.sessionContract,
      contextSnapshot: e.contextSnapshot,
      signal: e.signal,
      executeManagerOnce: e.executeManagerOnce,
      onStreamProgress: e.onStreamProgress,
      onProtocolEvent: e.onProtocolEvent
    });
    if (n = f.text, r = f.provider || r, s = f.model || s, a = f.toolTrace, o = f.changedFiles, l = f.changedStates, i = f.changedTasks, u = f.protocolMessages, e.requireChangedFiles && !o.length) throw new Error("manager_memory_tool_required");
    await Cg(e.sessionId);
    const m = o.length + l.length + i.length, v = m ? `已维护 ${o.length} 个记忆文件、${l.length} 份结构化状态、${i.length} 条事件线索。` : "已检查并回复。";
    return {
      ok: !0,
      managerRun: await Eg(t, {
        status: "completed",
        provider: r,
        model: s,
        outputText: n || v,
        parsedAction: m ? "manager_state_updated" : "memory_checked",
        toolTrace: a,
        changedFiles: o,
        changedStates: l,
        changedTasks: i,
        error: ""
      }),
      text: n,
      provider: r,
      model: s,
      toolTrace: a,
      changedFiles: o,
      changedStates: l,
      changedTasks: i,
      protocolMessages: u
    };
  } catch (f) {
    const m = f instanceof Error ? f.message : String(f || "manager_failed"), v = DS(f, e.signal), y = await Eg(t, {
      status: v,
      provider: r,
      model: s,
      outputText: n,
      toolTrace: a,
      changedFiles: o,
      changedStates: l,
      changedTasks: i,
      error: m
    }), S = e.caller === "auto" || ["cancelled", "superseded"].includes(v) ? await OS(t.id) : null;
    return S?.conflicts.length || await Cg(e.sessionId), {
      ok: !1,
      managerRun: S?.managerRun || y,
      text: n,
      provider: r,
      model: s,
      toolTrace: a,
      changedFiles: o,
      changedStates: l,
      changedTasks: i,
      protocolMessages: u,
      error: m
    };
  } finally {
    d();
  }
}
function ute(e = []) {
  const t = [];
  let n = [];
  return (e || []).forEach((r) => {
    if (!(!r || ![
      "user",
      "assistant",
      "tool"
    ].includes(r.role))) {
      if (r.role === "user" && n.length) {
        t.push(n), n = [r];
        return;
      }
      n.push(r);
    }
  }), n.length && t.push(n), t.filter((r) => r.length);
}
function nx(e) {
  if (!e?.aborted) return;
  const t = /* @__PURE__ */ new Error("manager_chat_compaction_aborted");
  throw t.name = "AbortError", t;
}
async function rx(e) {
  const t = Iv(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: LS
  });
  return await Xee({
    messages: await O6(e),
    tools: e0(),
    providerConfig: t
  });
}
async function cte(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return {
    compacted: !1,
    currentTokens: 0,
    history: []
  };
  nx(e.signal);
  const n = Array.isArray(e.history);
  let r = n ? [...e.history].sort((a, o) => a.order - o.order) : await Ls(t), s = await rx({
    sessionId: t,
    agentConfig: e.agentConfig,
    assistantPreset: e.assistantPreset,
    question: e.question,
    history: r
  });
  if (s <= 158e3) return {
    compacted: !1,
    currentTokens: s,
    history: r
  };
  e.onCompactionStart?.({
    currentTokens: s,
    triggerTokens: ad,
    status: "正在释放较早管理员对话，只保留最近管理上下文..."
  });
  for (const a of [2, 1]) {
    nx(e.signal);
    const o = ute(r), l = Math.max(0, o.length - Math.min(a, o.length));
    if (l > 0) {
      e.onCompactionProgress?.({
        currentTokens: s,
        triggerTokens: ad,
        preservedTurns: a,
        status: `正在只保留最近 ${a} 轮管理员上下文...`
      });
      const d = o.slice(0, l).flat().map((f) => f.order);
      d.length && await $b(t, d), r = n ? r.filter((f) => !d.includes(f.order)) : await Ls(t);
    }
    const i = await rx({
      sessionId: t,
      agentConfig: e.agentConfig,
      assistantPreset: e.assistantPreset,
      question: e.question,
      history: r
    });
    s = i;
    const u = i <= 158e3 ? `已只保留最近 ${a} 轮管理员上下文。` : "最近管理员上下文仍然过长，继续收缩...";
    if (e.onCompactionProgress?.({
      currentTokens: s,
      yieldTokens: i,
      triggerTokens: ad,
      preservedTurns: a,
      status: u
    }), i <= 158e3)
      return e.onCompactionComplete?.({
        currentTokens: s,
        yieldTokens: i,
        triggerTokens: ad,
        preservedTurns: a,
        status: u
      }), {
        compacted: !0,
        currentTokens: i,
        history: r,
        preservedTurns: a
      };
  }
  return e.onCompactionUnable?.({
    currentTokens: s,
    triggerTokens: ad,
    status: "当前管理员这一轮上下文本身已经过长，无法继续自动收缩。"
  }), {
    compacted: !1,
    currentTokens: s,
    history: r
  };
}
async function B6(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) throw new Error("manager_session_required");
  const n = $6(e.sessionContract);
  if (!n.hasAutomaticManagerWork) return {
    ok: !0,
    managerRun: e.managerRunId ? await yr(e.managerRunId, {
      status: "completed",
      outputText: "契约未授权后台记忆或地图维护，本轮已跳过。",
      parsedAction: "manager_skipped_by_contract",
      error: ""
    }) : await qm({
      sessionId: t,
      trigger: ll,
      turn: e.turn,
      userOrder: e.userMessage.order,
      assistantOrder: e.assistantMessage.order,
      status: "completed",
      inputSummary: "contract skipped",
      outputText: "契约未授权后台记忆或地图维护，本轮已跳过。",
      parsedAction: "manager_skipped_by_contract"
    }),
    changedFiles: [],
    changedStates: [],
    changedTasks: []
  };
  const r = Ag({
    trigger: ll,
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    text: e.userMessage.content
  }), s = e.managerRunId ? await yr(e.managerRunId, {
    status: "running",
    inputSummary: r
  }) : await qm({
    sessionId: t,
    trigger: ll,
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    status: "queued",
    inputSummary: r
  });
  if (!s) throw new Error("manager_run_missing");
  await e.onManagerRunSaved?.(s);
  try {
    const a = await Sp(e), o = Ag({
      trigger: ll,
      turn: e.turn,
      userOrder: a.userMessage.order,
      assistantOrder: a.assistantMessage.order,
      text: a.userMessage.content
    }), l = await lte(e, a), i = await F6({
      sessionId: t,
      agentConfig: e.agentConfig,
      trigger: ll,
      turn: e.turn,
      userOrder: a.userMessage.order,
      assistantOrder: a.assistantMessage.order,
      inputSummary: o,
      messages: l,
      managerRunId: s.id,
      caller: "auto",
      requireChangedFiles: !1,
      beforeWriteGuard: async () => {
        Bu(e.signal), await Sp(e);
      },
      sessionContract: e.sessionContract,
      contextSnapshot: e.contextSnapshot || a.assistantMessage.contextSnapshot || a.userMessage.contextSnapshot || {},
      signal: e.signal,
      executeManagerOnce: e.executeManagerOnce,
      onProtocolEvent: e.onProtocolEvent
    });
    if (!i.ok) return {
      ok: !1,
      managerRun: i.managerRun,
      error: i.error
    };
    await Sp(e);
    const u = [...i.changedFiles || []];
    let d = [...i.changedTasks || []], f = i.managerRun;
    if (n.includeQuestOrchestration) {
      const m = await mH(t, a.assistantMessage.order, {
        managerRunId: s.id,
        beforeWriteGuard: async () => {
          Bu(e.signal), await Sp(e);
        }
      });
      if (m.length) {
        d = [.../* @__PURE__ */ new Set([...d, ...m.map((S) => `event/${S.id}`)])];
        const v = `系统已放弃 ${m.length} 条过期事件线索。`, y = String(f.outputText || "").trim();
        f = await Eg(f, {
          outputText: y && y !== "已检查并回复。" ? `${y}
${v}` : v,
          parsedAction: "manager_state_updated",
          changedTasks: d
        });
      }
    }
    return {
      ok: !0,
      managerRun: f,
      changedFiles: u,
      changedStates: i.changedStates,
      changedTasks: d
    };
  } catch (a) {
    const o = a instanceof Error ? a.message : String(a || "manager_failed"), l = await Eg(s, {
      status: DS(a, e.signal),
      error: o
    }), i = await OS(s.id);
    return i?.conflicts.length || await Cg(t), {
      ok: !1,
      managerRun: i?.managerRun || l,
      error: o
    };
  }
}
async function dte(e) {
  const t = String(e.sessionId || "").trim(), n = String(e.question || "").trim();
  if (!t) throw new Error("manager_session_required");
  if (!n) throw new Error("manager_question_required");
  const r = Array.isArray(e.history) ? e.history : await Ls(t), s = await O6({
    sessionId: t,
    question: n,
    assistantPreset: e.assistantPreset,
    history: r
  }), a = await F6({
    sessionId: t,
    agentConfig: e.agentConfig,
    trigger: e.trigger || "manager_chat",
    turn: Math.max(0, Number(e.turn) || 0),
    inputSummary: Ag({
      trigger: "manager_chat",
      turn: e.turn,
      text: n
    }),
    messages: s,
    caller: "chat",
    requireChangedFiles: !1,
    signal: e.signal,
    executeManagerOnce: e.executeManagerOnce,
    onStreamProgress: e.onStreamProgress,
    onProtocolEvent: e.onProtocolEvent
  });
  return {
    ok: a.ok,
    managerRun: a.managerRun,
    text: a.text,
    provider: a.provider,
    model: a.model,
    changedFiles: a.changedFiles,
    changedStates: a.changedStates,
    changedTasks: a.changedTasks,
    protocolMessages: a.protocolMessages,
    error: a.error
  };
}
async function fte(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) throw new Error("manager_session_required");
  const n = await qm({
    sessionId: t,
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    trigger: ll,
    status: "pending",
    inputSummary: Ag({
      trigger: ll,
      turn: e.turn,
      userOrder: e.userMessage.order,
      assistantOrder: e.assistantMessage.order,
      text: e.userMessage.content
    }),
    outputText: "等待用户继续后维护上一条已接受回复。"
  }), r = (await l3(t)).filter((s) => s.id !== n.id);
  return await Promise.all(r.map(async (s) => {
    const a = await yr(s.id, {
      status: "superseded",
      error: "manager_pending_superseded_by_newer_turn"
    });
    a && await e.onManagerRunSaved?.(a);
  })), await e.onManagerRunSaved?.(n), {
    managerRunId: n.id,
    managerStatus: n.status
  };
}
async function hte(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return null;
  const n = await l3(t);
  if (!n.length) return null;
  let r = null;
  for (const l of n) {
    if (r) {
      const i = await yr(l.id, {
        status: "superseded",
        error: "manager_pending_superseded_by_newer_turn"
      });
      i && await e.onManagerRunSaved?.(i);
      continue;
    }
    try {
      const i = await FS(t, l.userOrder, l.assistantOrder);
      r = {
        run: l,
        userMessage: i.userMessage,
        assistantMessage: i.assistantMessage
      };
    } catch {
      const i = await yr(l.id, {
        status: "superseded",
        error: "manager_source_messages_superseded"
      });
      i && await e.onManagerRunSaved?.(i);
    }
  }
  if (!r) return null;
  const s = new AbortController();
  let a = !1;
  const o = () => {
    a = !0, s.abort();
  };
  e.signal?.aborted ? (a = !0, s.abort()) : e.signal?.addEventListener("abort", o, { once: !0 }), n1.set(r.run.id, {
    controller: s,
    sessionId: t,
    userOrder: r.userMessage.order,
    assistantOrder: r.assistantMessage.order
  });
  try {
    Bu(s.signal);
    const l = await B6({
      ...e,
      managerRunId: r.run.id,
      turn: r.run.turn,
      userMessage: r.userMessage,
      assistantMessage: r.assistantMessage,
      signal: s.signal
    });
    return !l.ok && a && e.signal?.aborted ? await tx({
      selected: r,
      onManagerRunSaved: e.onManagerRunSaved
    }) : (await e.onManagerRunSaved?.(l.managerRun), l);
  } catch (l) {
    if (a && e.signal?.aborted) return await tx({
      selected: r,
      onManagerRunSaved: e.onManagerRunSaved
    });
    const i = l instanceof Error ? l.message : String(l || "manager_failed"), u = await yr(r.run.id, {
      status: DS(l, s.signal),
      error: i
    }) || r.run;
    return await e.onManagerRunSaved?.(u), {
      ok: !1,
      managerRun: u,
      error: i
    };
  } finally {
    e.signal?.removeEventListener("abort", o), n1.delete(r.run.id);
  }
}
async function pte(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return {
    affectedRuns: 0,
    pendingRuns: 0,
    writtenMemoryFiles: 0,
    writtenTaskRuns: 0,
    hasWrittenState: !1
  };
  const s = (await i3(n)).filter((i) => ["accepted_turn", "after_turn"].includes(i.trigger) && (Number(i.userOrder) >= r || Number(i.assistantOrder) >= r));
  let a = 0, o = 0, l = 0;
  for (const i of s) {
    [
      "pending",
      "queued",
      "running"
    ].includes(i.status) && (a += 1);
    const u = await qf(i.id), d = await Pw(i.id);
    o += u.filter((f) => String(f.afterHash || "").trim()).length, d.some((f) => String(f.afterHash || "").trim()) && (l += 1);
  }
  return {
    affectedRuns: s.length,
    pendingRuns: a,
    writtenMemoryFiles: o,
    writtenTaskRuns: l,
    hasWrittenState: o > 0 || l > 0
  };
}
async function BS(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return {
    runIds: [],
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  n1.forEach((i) => {
    i.sessionId === n && (i.userOrder >= r || i.assistantOrder >= r) && i.controller.abort();
  });
  const s = await XU(n, r);
  let a = 0, o = 0;
  const l = [];
  for (const i of s.runIds) {
    if (!(await Pw(i)).some((d) => String(d.afterHash || "").trim())) continue;
    const u = await K3(i);
    a += u.rolledBack, o += u.skipped, l.push(...u.conflicts);
  }
  return {
    runIds: [.../* @__PURE__ */ new Set([...s.runIds])],
    rolledBack: s.rolledBack + a,
    conflicts: [...s.conflicts, ...l],
    skipped: s.skipped + o
  };
}
var mte = /\[tavern-image:[a-z0-9\-_]+\]/gi, gte = /\[(?:img|图片)\s*:\s*[^\]]+\]/gi, vte = 8;
function Bl(e = "") {
  return String(e || "").replace(mte, "").replace(gte, "").trim();
}
function U6(e) {
  return !e.error && !!Bl(e.content);
}
function q6(e = "") {
  return !!Bl(e);
}
function vu(e = {}) {
  const t = [...e.messages || []].sort((l, i) => l.order - i.order), n = t.filter(U6), r = q6(e.currentUserMessage || "") ? 1 : 0;
  let s = Math.max(0, Math.floor(Number(e.contextWindowStartOrder) || 0));
  if (!n.length || n.length < 5) s = 0;
  else if (n.length + r <= 20) s = 0;
  else if (s > 0) {
    let l = n.filter((i) => i.order >= s);
    !n.some((i) => i.order === s) && l.length && (s = l[0].order, l = n.filter((i) => i.order >= s)), l.length < 5 && (s = n[Math.max(0, n.length - 10)]?.order || 0);
  }
  let a = s > 0 ? n.filter((l) => l.order >= s) : n;
  if (a.length + r > 20) {
    const l = Math.max(0, 10 - r);
    s = l > 0 && a.slice(-l)[0]?.order || 0, a = s > 0 ? n.filter((i) => i.order >= s) : n;
  }
  const o = s > 0 ? t.filter((l) => l.order >= s) : t;
  return {
    contextWindowStartOrder: s,
    historyMessages: o,
    usableHistoryCount: n.length,
    windowHistoryCount: a.length,
    currentUserCount: r
  };
}
async function yte(e = "", t = 0, n = Number.POSITIVE_INFINITY) {
  const s = [];
  let a = 0;
  for (; ; ) {
    const o = await HU(e, t, n, 1e3, a);
    if (s.push(...o), o.length < 1e3) break;
    a += 1e3;
  }
  return s;
}
function _te(e) {
  return e.filter(U6).length;
}
async function H6(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return vu({
    messages: [],
    contextWindowStartOrder: e.contextWindowStartOrder,
    currentUserMessage: e.currentUserMessage
  });
  const n = Number.isFinite(Number(e.beforeOrder)), r = n ? Math.floor(Number(e.beforeOrder) || 0) : Number.POSITIVE_INFINITY;
  if (n && r <= 0) return vu({
    messages: [],
    contextWindowStartOrder: e.contextWindowStartOrder,
    currentUserMessage: e.currentUserMessage
  });
  const s = n ? r - 1 : Number.POSITIVE_INFINITY, a = Math.max(0, Math.floor(Number(e.contextWindowStartOrder) || 0)), o = q6(e.currentUserMessage || "") ? 1 : 0, l = Math.max(20, 10 + o), i = Math.max(60, 60);
  if (a > 0) {
    const y = await Nu(t, a, s, i, 0);
    if (y.total <= i) {
      const S = vu({
        messages: y.messages,
        contextWindowStartOrder: a,
        currentUserMessage: e.currentUserMessage
      });
      if (S.contextWindowStartOrder >= a) return S;
    }
  }
  const u = /* @__PURE__ */ new Map();
  let d = 0, f = null, m = 0, v = 0;
  for (; ; ) {
    let y;
    if (n) {
      f === null && (f = (await Nu(t, 0, s, 1, 0)).total);
      const x = Math.max(0, f - m), I = Math.min(i, x), E = Math.max(0, f - m - I);
      y = I > 0 ? await Nu(t, 0, s, I, E) : {
        messages: [],
        total: f
      }, m += y.messages.length;
    } else
      y = await ev(t, i, d), d += i;
    v = y.total, y.messages.forEach((x) => u.set(x.order, x));
    const S = [...u.values()].sort((x, I) => x.order - I.order);
    if (S.length >= v || _te(S) >= l) return vu({
      messages: S,
      contextWindowStartOrder: a,
      currentUserMessage: e.currentUserMessage
    });
    if (!y.messages.length) return vu({
      messages: S,
      contextWindowStartOrder: a,
      currentUserMessage: e.currentUserMessage
    });
  }
}
function bte(e = []) {
  return e.filter((t) => t.role === "user").slice(-2).some((t) => kD(t.runtimeEvents));
}
function wte(e) {
  return Number.isFinite(e) && e < 0.1;
}
function Ste(e) {
  if (!e.runtime.includeRandomEncounters) return null;
  const t = M1(e.reusedUserMessage?.runtimeEvents);
  return t || (e.reusedUserMessage && e.rerollRuntimeEvents !== !0 || bte(e.reusedUserMessage ? e.historyMessages : e.sessionMessages) ? null : wte(e.randomEncounterRoll ? e.randomEncounterRoll() : Math.random()) ? ID() : null);
}
function z6(e) {
  return e.includeActionChecks ? {
    tools: y6(),
    toolChoice: "auto"
  } : {
    tools: [],
    toolChoice: "none"
  };
}
function K6(e) {
  return e.includeActionChecks ? [gee()] : [];
}
function Ate(e) {
  return e ? [{
    content: Vk().content,
    depth: 1,
    role: "system",
    order: 1e9,
    label: "chance encounter",
    layer: "runtime-event"
  }] : [];
}
function Tte(e = {}) {
  const t = Array.isArray(e.memoryFiles) ? e.memoryFiles : [], n = Array.isArray(e.structuredStates) ? e.structuredStates : [], r = String(e.spatialState || "").trim(), s = Array.isArray(e.questHooks) ? e.questHooks.map((d) => String(d || "").trim()).filter(Boolean) : [], a = [];
  s.length && a.push(s.join(`
`));
  const o = t.find((d) => String(d.path || "") === "memory/state.md"), l = String(o?.content || "").trim();
  l && a.push(`## 会话记忆
${l}`);
  const i = t.filter((d) => String(d.path || "").startsWith("memory/characters/")).map((d) => {
    const f = String(d.path || "").slice(18).replace(/\.md$/i, ""), m = String(d.title || f || "相关人物").trim(), v = String(d.content || "").trim();
    return v ? `### ${m}
${v}` : "";
  }).filter(Boolean);
  i.length && a.push(`## 相关人物记忆
${i.join(`

`)}`);
  const u = r ? [] : n.map((d) => String(d.digest || "").trim()).filter(Boolean);
  return u.length && a.push(`## 状态摘要
${u.join(`

`)}`), r && a.push(`## 空间状态
${r}`), a.join(`

`);
}
function Ete(e = []) {
  return e.map((t) => String(t.content || "").trim()).filter(Boolean).join(`

`);
}
function W6(e = []) {
  if (!e.length) return [];
  let t = -1;
  for (let n = e.length - 1; n >= 0; n -= 1) if (e[n]?.role === "assistant") {
    t = n;
    break;
  }
  return t < 0 ? e : e.map((n, r) => {
    if (r !== t) return n;
    const s = String(n.content || "").trimEnd();
    return s === n.content ? n : {
      ...n,
      content: s
    };
  });
}
function Cte(e = [], t = "sillytavern-native") {
  return e.map((n, r) => {
    const s = String(n.content || "").length;
    return {
      index: r,
      role: n.role,
      layer: t,
      label: `${t} ${r + 1}`,
      chars: s,
      tokenEstimate: Math.max(1, Math.ceil(s / 4))
    };
  });
}
function Ite(e, t = [], n = "sillytavern-native") {
  const r = W6(t).map((s) => ({
    ...s,
    content: String(s.content || "")
  })).filter((s) => s.content);
  return {
    ...e,
    messages: r,
    messageLayers: Cte(r, n),
    meta: {
      ...e.meta,
      rawMessagesJson: JSON.stringify(r, null, 2)
    }
  };
}
function xte(e) {
  const t = "error" in e ? e.error : "";
  return String(e.summary || t || "").trim();
}
function kte(e = "", t, n = 0) {
  const r = String(e || ""), s = Math.max(0, Math.min(r.length, Number(n) || 0));
  if (!t.ok) return s;
  const a = String(t.insertAfter ?? "");
  if (!a.trim()) return s;
  const o = r.lastIndexOf(a);
  return o < 0 ? s : Math.max(0, Math.min(r.length, o + a.length));
}
async function G6(e) {
  if (!e.buildNativeChatPrompt) return {
    buildResult: e.baseBuildResult,
    buildSnapshot: e.baseBuildSnapshot
  };
  const t = await Kn(e.stage, () => e.buildNativeChatPrompt?.({
    context: e.contextForBuild,
    chatPreset: e.chatPreset,
    currentUserMessage: e.currentUserMessage,
    generationType: e.generationType,
    debugStage: e.stage,
    signal: e.signal,
    memoryPrompt: Tte(e.memoryContext),
    chancePrompt: e.chancePrompt || "",
    actionCheckPrompt: Ete(e.runtimeProtocolMessages || [])
  })), n = Array.isArray(t?.messages) ? t.messages : [];
  if (!n.length) return {
    buildResult: e.baseBuildResult,
    buildSnapshot: e.baseBuildSnapshot
  };
  const r = Ite(e.baseBuildResult, n, "sillytavern-native");
  return {
    buildResult: r,
    buildSnapshot: q1(e.contextForBuild, e.chatPreset, r, {
      ...e.diagnostics && typeof e.diagnostics == "object" ? e.diagnostics : {},
      promptSource: t?.source || "sillytavern-prepareOpenAIMessages",
      promptMessageCount: t?.promptMessageCount ?? n.length
    })
  };
}
function Mte(e, t) {
  if (t?.aborted) return !0;
  if (!e || typeof e != "object") return !1;
  const n = e, r = String(n.name || ""), s = String(n.code || ""), a = String(n.message || "");
  return r === "AbortError" || s === "ABORT_ERR" || /abort|aborted|cancelled|canceled/i.test(a);
}
function Pte(e, t) {
  if (t instanceof Error && /^\[xb-tavern:[^\]]+\]/.test(t.message)) return t;
  const n = t instanceof Error ? t.message : String(t || "unknown_error"), r = /* @__PURE__ */ new Error(`[xb-tavern:${e}] ${n}`);
  return r.name = t instanceof Error ? t.name : "Error", t instanceof Error && t.stack && (r.stack = `${r.name}: ${r.message}
Caused by: ${t.stack}`), r;
}
function Rte(e) {
  const t = String(e || "run_failed");
  return /^\[xb-tavern:provider_chat\]\s*Failed to fetch\b/i.test(t) && !/切换酒馆补全源/.test(t) ? `${t}

可以尝试在 API 配置中切换酒馆补全源。` : t;
}
async function Kn(e, t) {
  const n = typeof performance < "u" && typeof performance.now == "function" ? performance.now() : Date.now();
  console.info("[小白酒馆] turn stage start", { stage: e });
  try {
    const r = await t(), s = typeof performance < "u" && typeof performance.now == "function" ? performance.now() : Date.now();
    return console.info("[小白酒馆] turn stage end", {
      stage: e,
      ms: Math.round(s - n)
    }), r;
  } catch (r) {
    const s = typeof performance < "u" && typeof performance.now == "function" ? performance.now() : Date.now();
    throw console.info("[小白酒馆] turn stage failed", {
      stage: e,
      ms: Math.round(s - n),
      error: r instanceof Error ? r.message : String(r || "unknown_error")
    }), Pte(e, r);
  }
}
async function oo(e) {
  if (e)
    try {
      await e();
    } catch (t) {
      console.warn("[小白酒馆] run callback failed", t);
    }
}
async function R_(e, t, n = {}) {
  return n.replace ? await r3(e, t) : await n3(e, t);
}
function lm(e) {
  const t = String(e?.character?.name || "").trim();
  return !!t && !/^(sillytavern\s+system|system)\b/i.test(t);
}
function sx(e) {
  return String(e?.character?.characterKey || "").trim();
}
function V6(e, t = {}) {
  if (e) {
    const n = String(e.characterKey || sx(e.contextSnapshot)).trim(), r = sx(t);
    if (lm(t)) {
      if (n && r && n !== r) throw new Error("会话角色身份不匹配，请重新选择对应角色会话。");
      if (!n || r === n) return t || {};
    }
    return lm(e.contextSnapshot) ? e.contextSnapshot || {} : e.contextSnapshot || {};
  }
  return lm(t) ? t || {} : t || {};
}
function r1(e = {}) {
  if (!lm(e))
    throw new Error("当前没有可用角色，请先选择角色或刷新当前会话。");
}
function Zf(e = {}) {
  return e.chatPreset || e.preset || {};
}
function s1(e) {
  return Fs(e?.contract);
}
function Nte(e, t) {
  const n = e && typeof e == "object" && !Array.isArray(e) ? e : {};
  return Xr({
    ...n,
    contract: Qg(n.contract) ? Um(t, n.contract) : Um(t, void 0)
  });
}
function J6(e, t) {
  if (!e) return e;
  if (!t.includeMemoryFiles && !t.includeStructuredStates && !t.includeQuestOrchestration) return {};
  const n = {};
  return t.includeMemoryFiles && Array.isArray(e.memoryFiles) && (n.memoryFiles = e.memoryFiles), t.includeStructuredStates && Array.isArray(e.structuredStates) && (n.structuredStates = e.structuredStates), t.includeStructuredStates && e.spatialState && (n.spatialState = e.spatialState), t.includeQuestOrchestration && Array.isArray(e.questHooks) && (n.questHooks = e.questHooks), n;
}
function fo(e, t) {
  t && [
    "userInput",
    "worldInfo",
    "aiOutput",
    "reasoning"
  ].forEach((n) => {
    const r = Number(t[n]) || 0;
    r > 0 && (e[n] = (e[n] || 0) + r);
  });
}
function $te(e = []) {
  return e.map((t) => ({
    id: t.id,
    text: t.text,
    changed: !1
  }));
}
async function eh(e, t = []) {
  if (!t.length) return [];
  if (!e) return $te(t);
  const n = await e(t), r = new Map((Array.isArray(n.items) ? n.items : []).map((s) => [s.id, s]));
  return t.map((s) => r.get(s.id) || {
    id: s.id,
    text: s.text,
    changed: !1
  });
}
async function um(e) {
  const [t] = await eh(e.applyRegex, [{
    id: `${e.placement}:${e.id}`,
    text: e.text,
    placement: e.placement,
    options: e.options
  }]);
  return {
    text: t?.text ?? e.text,
    summary: Qf(t ? [t] : [])
  };
}
async function Lte(e) {
  const t = Array.isArray(e.thoughts) ? e.thoughts : [], n = t.map((a, o) => ({
    id: `reasoning:${o}`,
    text: String(a.text || ""),
    placement: "reasoning"
  })).filter((a) => a.text);
  if (!n.length) return { thoughts: e.thoughts };
  const r = await eh(e.applyRegex, n), s = new Map(r.map((a) => [a.id, a]));
  return {
    thoughts: t.map((a, o) => ({
      ...a,
      text: s.get(`reasoning:${o}`)?.text ?? a.text
    })),
    summary: Qf(r)
  };
}
async function Y6(e) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = t.reduce((a, o, l) => {
    const i = t.length - l - 1;
    if (["user", "assistant"].includes(o.role) && String(o.content || "")) {
      const u = o.role === "user" ? "userInput" : "aiOutput";
      a.push({
        id: `${u}:prompt:${l}`,
        text: String(o.content || ""),
        placement: u,
        options: {
          isPrompt: !0,
          depth: i
        }
      });
    }
    return o.role === "assistant" && Array.isArray(o.thoughts) && o.thoughts.forEach((u, d) => {
      const f = String(u?.text || "");
      f && a.push({
        id: `reasoning:prompt:${l}:${d}`,
        text: f,
        placement: "reasoning",
        options: {
          isPrompt: !0,
          depth: i
        }
      });
    }), a;
  }, []);
  if (!n.length) return { messages: t };
  const r = await eh(e.applyRegex, n), s = new Map(r.map((a) => [a.id, a]));
  return {
    messages: t.map((a, o) => {
      const l = a.role === "user" ? "userInput" : a.role === "assistant" ? "aiOutput" : "", i = l ? s.get(`${l}:prompt:${o}`) : null, u = Array.isArray(a.thoughts) ? a.thoughts.map((d, f) => ({
        ...d,
        text: s.get(`reasoning:prompt:${o}:${f}`)?.text ?? d.text
      })) : a.thoughts;
      return {
        ...a,
        ...i ? { content: i.text } : {},
        ...u ? { thoughts: u } : {}
      };
    }),
    summary: Qf(r)
  };
}
function Dte(e = []) {
  return e.map((t) => ({
    id: t.id,
    text: t.text,
    changed: !1
  }));
}
async function US(e, t = []) {
  if (!t.length) return [];
  if (!e) return Dte(t);
  const n = await e(t), r = new Map((Array.isArray(n.items) ? n.items : []).map((s) => [s.id, s]));
  return t.map((s) => r.get(s.id) || {
    id: s.id,
    text: s.text,
    changed: !1
  });
}
function qS(e = {}) {
  const t = {}, n = String(e.user?.name || "").trim(), r = String(e.character?.name || "").trim();
  return n && (t.name1Override = n), r && (t.name2Override = r), t;
}
async function X6(e) {
  const [t] = await US(e.applySubstituteParams, [{
    id: e.id,
    text: e.text,
    options: e.options
  }]);
  return t?.text ?? e.text;
}
function Ote(e) {
  return Array.isArray(e) ? e.map((t) => String(t || "")) : e == null ? [] : [String(e)];
}
function Fte(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function j6(e) {
  return Array.isArray(e) ? e.filter(Fte) : [];
}
async function ax(e) {
  const t = j6(e.entries), n = [], r = [];
  t.forEach((l, i) => {
    const u = String(l.content || "");
    if (u) {
      const d = `${e.scope}:${i}:content`;
      r.push({
        entryIndex: i,
        field: "content",
        id: d
      }), n.push({
        id: d,
        text: u,
        options: e.options
      });
    }
    [
      "key",
      "keysecondary",
      "secondary_keys"
    ].forEach((d) => {
      Ote(l[d]).forEach((f, m) => {
        if (!f) return;
        const v = `${e.scope}:${i}:${d}:${m}`;
        r.push({
          entryIndex: i,
          field: d,
          valueIndex: m,
          id: v
        }), n.push({
          id: v,
          text: f,
          options: e.options
        });
      });
    });
  });
  const s = await US(e.applySubstituteParams, n), a = new Map(s.map((l) => [l.id, l.text])), o = t.map((l) => ({ ...l }));
  return r.forEach((l) => {
    const i = a.get(l.id);
    if (i === void 0) return;
    const u = o[l.entryIndex];
    if (!u) return;
    if (l.field === "content") {
      u.content = i;
      return;
    }
    const d = u[l.field];
    if (Array.isArray(d)) {
      const f = [...d];
      f[l.valueIndex || 0] = i, u[l.field] = f;
    } else u[l.field] = i;
  }), o;
}
async function HS(e) {
  if (!e.applySubstituteParams) return e.context;
  const t = {
    ...e.context,
    worldEntries: Array.isArray(e.context.worldEntries) ? await ax({
      applySubstituteParams: e.applySubstituteParams,
      entries: e.context.worldEntries,
      options: e.options,
      scope: "worldEntries"
    }) : e.context.worldEntries
  };
  return Array.isArray(e.context.worldBooks) && (t.worldBooks = await Promise.all(j6(e.context.worldBooks).map(async (n, r) => ({
    ...n,
    entries: await ax({
      applySubstituteParams: e.applySubstituteParams,
      entries: n.entries,
      options: e.options,
      scope: `worldBooks:${r}`
    })
  })))), t;
}
async function Ig(e) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  t.forEach((a, o) => {
    const l = String(a.content || "");
    l && n.push({
      id: `message:${o}`,
      text: l,
      options: e.options
    }), a.role === "assistant" && Array.isArray(a.thoughts) && a.thoughts.forEach((i, u) => {
      const d = String(i?.text || "");
      d && n.push({
        id: `thought:${o}:${u}`,
        text: d,
        options: e.options
      });
    });
  });
  const r = await US(e.applySubstituteParams, n), s = new Map(r.map((a) => [a.id, a.text]));
  return t.map((a, o) => {
    const l = Array.isArray(a.thoughts) ? a.thoughts.map((i, u) => ({
      ...i,
      text: s.get(`thought:${o}:${u}`) ?? i.text
    })) : a.thoughts;
    return {
      ...a,
      content: s.get(`message:${o}`) ?? a.content,
      ...l ? { thoughts: l } : {}
    };
  });
}
function nf(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {}, n = (r) => {
    if (!r || typeof r != "object" || Array.isArray(r)) return {};
    const s = {};
    return Object.entries(r).forEach(([a, o]) => {
      if (!a || !o || typeof o != "object" || Array.isArray(o)) return;
      const l = o, i = {}, u = Number(l.hash), d = Number(l.start), f = Number(l.end);
      Number.isFinite(u) && (i.hash = u), Number.isFinite(d) && (i.start = d), Number.isFinite(f) && (i.end = f), l.protected === !0 && (i.protected = !0), Object.keys(i).length && (s[a] = i);
    }), s;
  };
  return {
    sticky: n(t.sticky),
    cooldown: n(t.cooldown)
  };
}
async function zS(e) {
  const t = nf(e.timedState);
  if (!e.getNativeWorldInfoRuntime) return {
    context: e.context,
    timedState: t
  };
  const n = await e.getNativeWorldInfoRuntime({
    context: e.context,
    currentUserMessage: e.currentUserMessage,
    trigger: e.trigger,
    timedState: t
  });
  return {
    context: {
      ...e.context,
      nativeWorldInfo: n
    },
    timedState: nf(n?.timedState)
  };
}
function Q6(e, t = !1) {
  return !t;
}
function ox(e = {}, t, n = !1) {
  const r = e.worldEntryStates || {}, s = t.meta.worldEntryStateUpdates || {};
  return n ? dw(r, s) : s;
}
function Bte(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = {
      role: t.role,
      content: String(t.content || "")
    };
    return t.name && (n.name = String(t.name)), Array.isArray(t.tool_calls) && t.tool_calls.length && (n.tool_calls = t.tool_calls.map((r) => ({
      ...r.id ? { id: String(r.id) } : {},
      ...r.type ? { type: String(r.type) } : {},
      ...r.function ? { function: {
        name: String(r.function.name || ""),
        arguments: String(r.function.arguments || "")
      } } : {}
    }))), Array.isArray(t.toolCalls) && t.toolCalls.length && (n.toolCalls = t.toolCalls.map((r) => ({
      ...r.id ? { id: String(r.id) } : {},
      name: String(r.name || ""),
      arguments: String(r.arguments || "")
    }))), t.tool_call_id && (n.tool_call_id = String(t.tool_call_id)), t.toolCallId && (n.toolCallId = String(t.toolCallId)), t.toolName && (n.toolName = String(t.toolName)), n;
  });
}
function Zu(e = {}, t = [], n = {}) {
  const r = $S(e), s = n.requestInspection || null, a = String(n.chatPreset?.name || "").trim(), o = Bte(t), l = JSON.stringify(o, null, 2), i = s || {
    provider: String(n.provider || r.provider || ""),
    model: String(n.model || r.model || ""),
    transport: "unavailable",
    request: n.requestTask || { messages: o }
  }, u = JSON.stringify(i, null, 2);
  return {
    presetName: a || r.currentPresetName,
    chatPresetName: a,
    apiPresetName: r.currentPresetName,
    provider: String(n.provider || r.provider || ""),
    providerLabel: r.providerLabel,
    model: String(n.model || r.model || ""),
    toolMode: r.toolMode,
    messageCount: o.length,
    messageChars: o.reduce((d, f) => d + String(f.content || "").length, 0),
    rawMessagesJson: l,
    rawRequestJson: u,
    requestKind: n.requestKind || "actual",
    capturedAt: Date.now(),
    ..._6(n.regexApplications) ? { regexApplications: n.regexApplications } : {},
    ...s ? { requestInspection: s } : {}
  };
}
async function KS(e) {
  const t = e.providerConfig || P6(e.agentConfig), n = W6(e.messages), r = Pee(t, {
    tools: Array.isArray(e.tools) ? e.tools : [],
    toolChoice: e.toolChoice || (Array.isArray(e.tools) && e.tools.length ? "auto" : "none")
  }), s = e.adapter || PS(t, { missingApiKeyMessage: "请先在 API 配置里选择模型/填写 Key。" }), a = r.buildChatTask({
    messages: n,
    signal: e.signal,
    onStreamProgress: e.onStreamProgress
  }), o = xv({
    supportsSessionToolLoop: s.supportsSessionToolLoop === !0,
    messages: n,
    toolResponses: e.toolResponses,
    finalAnswerReminderText: e.finalAnswerReminderText
  });
  R6(a, o);
  const l = Yee(o), i = l && typeof s.inspectSendRequest == "function" ? await s.inspectSendRequest(l, a) : typeof s.inspectRequest == "function" ? await s.inspectRequest(a) : null;
  return {
    task: a,
    adapter: s,
    providerConfig: t,
    requestSnapshot: Zu(e.agentConfig, o.requestMessages, {
      provider: String(i?.provider || t.provider || ""),
      model: String(i?.model || t.model || ""),
      requestInspection: i,
      requestKind: e.requestKind || "actual",
      chatPreset: e.chatPreset,
      regexApplications: e.regexApplications,
      requestTask: a
    }),
    snapshotMessages: o.requestMessages
  };
}
function th(e = []) {
  return e.filter((t) => !t.error).map((t) => ({
    role: [
      "system",
      "user",
      "assistant",
      "tool"
    ].includes(t.role) ? t.role : "assistant",
    content: Bl(t.content),
    ...t.name ? { name: t.name } : {},
    ...Array.isArray(t.thoughts) && t.thoughts.length ? { thoughts: t.thoughts } : {}
  })).filter((t) => String(t.content || "").trim());
}
function Z6(e = [], t = -1) {
  if (t < 0) return null;
  for (let n = t + 1; n < e.length; n += 1) {
    const r = e[n];
    if (r.role === "user") break;
    if (r.role === "assistant" && !r.error && String(r.content || "").trim()) return r;
  }
  return null;
}
function Ute(e) {
  const t = Zf(e), n = [...e.messages || []].sort((f, m) => f.order - m.order), r = e.contextSnapshot || {}, s = [];
  let a = 0, o, l, i = "", u = "";
  n.forEach((f, m) => {
    const v = Bl(f.content);
    if (f.role !== "user" || f.error || !v) {
      s.push(f);
      return;
    }
    const y = Z6(n, m);
    if (y) {
      const S = cM({
        context: {
          ...r,
          history: th(s)
        },
        chatPreset: t,
        currentUserMessage: v,
        historyMode: e.historyMode || "raw",
        turn: a,
        entryStates: {},
        diagnostics: e.diagnostics || {}
      });
      a += 1, o = S.buildSnapshot, l = y.requestSnapshot || f.requestSnapshot, i = String(y.provider || ""), u = String(y.model || "");
    }
    s.push(f);
  });
  const d = n[n.length - 1];
  return {
    turn: a,
    worldEntryStates: {},
    nativeWorldInfoTimedState: {
      sticky: {},
      cooldown: {}
    },
    lastBuildSnapshot: o,
    lastRequestSnapshot: l,
    lastProvider: i,
    lastModel: u,
    lastError: d?.error ? String(d.content || "") : ""
  };
}
async function e8(e) {
  if (!e.applySubstituteParams && !e.getNativeWorldInfoRuntime) return Ute(e);
  const t = Zf(e), n = [...e.messages || []].sort((y, S) => y.order - S.order), r = e.contextSnapshot || {}, s = qS(r), a = [];
  let o = 0, l = {}, i = {
    sticky: {},
    cooldown: {}
  }, u, d, f = "", m = "";
  for (let y = 0; y < n.length; y += 1) {
    const S = n[y], x = Bl(S.content);
    if (S.role !== "user" || S.error || !x) {
      a.push(S);
      continue;
    }
    const I = Z6(n, y);
    if (I) {
      const E = String(I.buildSnapshot?.nativeWorldInfo?.trigger || S.buildSnapshot?.nativeWorldInfo?.trigger || "normal"), N = {
        ...r,
        worldSettings: {
          ...r.worldSettings || {},
          trigger: E
        },
        history: th(a)
      }, $ = await zS({
        getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
        context: N,
        currentUserMessage: x,
        trigger: E,
        timedState: i
      });
      i = $.timedState;
      const p = await H1({
        context: await HS({
          applySubstituteParams: e.applySubstituteParams,
          context: $.context,
          options: s
        }),
        chatPreset: t,
        currentUserMessage: x,
        historyMode: e.historyMode || "raw",
        turn: o,
        entryStates: l,
        diagnostics: e.diagnostics || {}
      });
      l = dw(l, p.buildResult.meta.worldEntryStateUpdates), o += 1, u = p.buildSnapshot, d = I.requestSnapshot || S.requestSnapshot, f = String(I.provider || ""), m = String(I.model || "");
    }
    a.push(S);
  }
  const v = n[n.length - 1];
  return {
    turn: o,
    worldEntryStates: l,
    nativeWorldInfoTimedState: i,
    lastBuildSnapshot: u,
    lastRequestSnapshot: d,
    lastProvider: f,
    lastModel: m,
    lastError: v?.error ? String(v.content || "") : ""
  };
}
async function qte(e, t) {
  const n = await qa(e.sessionId || "");
  if (n) return n;
  const r = Zf(e), s = e.contextSnapshot || {}, a = s.character || {}, o = Xr(e.runtimeState || {});
  return await t3({
    title: String(a.name || "未选择角色"),
    characterKey: String(a.characterKey || ""),
    characterName: String(a.name || "未选择角色"),
    contextSnapshot: s,
    buildSnapshot: t,
    chatPresetId: String(r.id || ""),
    chatPresetName: String(r.name || ""),
    presetId: String(r.id || ""),
    presetName: String(r.name || ""),
    state: {
      turn: 0,
      contract: o.contract,
      worldEntryStates: {},
      nativeWorldInfoTimedState: {
        sticky: {},
        cooldown: {}
      }
    }
  });
}
async function Hte(e, t, n) {
  const r = await Kn("provider_request_inspection", () => KS({
    agentConfig: n.agentConfig,
    messages: n.messages,
    chatPreset: n.chatPreset,
    regexApplications: n.regexApplications,
    tools: n.tools,
    toolChoice: n.toolChoice,
    toolResponses: n.toolResponses,
    finalAnswerReminderText: n.finalAnswerReminderText,
    signal: n.signal,
    onStreamProgress: n.onStreamProgress,
    requestKind: "actual",
    providerConfig: t,
    adapter: e
  }));
  let s;
  try {
    s = await Kn("provider_chat", () => r.adapter.chat(r.task));
  } catch (d) {
    const f = d?.requestInspection;
    throw f && d && typeof d == "object" && (d.requestSnapshot = Zu(n.agentConfig, r.snapshotMessages, {
      provider: String(f.provider || r.providerConfig.provider || ""),
      model: String(f.model || r.providerConfig.model || ""),
      requestInspection: f,
      requestKind: "actual",
      chatPreset: n.chatPreset,
      regexApplications: n.regexApplications,
      requestTask: r.task
    })), d;
  }
  const a = s?.requestInspection || r.requestSnapshot.requestInspection || null, o = String(s?.text || ""), l = String(s?.provider || a?.provider || r.providerConfig.provider || ""), i = String(s?.model || a?.model || r.providerConfig.model || ""), u = p6(s || {}, r.providerConfig, { fallbackPrefix: "tavern-rp-tool" });
  return {
    text: o,
    thoughts: s?.thoughts,
    model: i,
    provider: l,
    finishReason: s?.finishReason,
    providerPayload: s?.providerPayload,
    toolCalls: u,
    requestSnapshot: Zu(n.agentConfig, r.snapshotMessages, {
      provider: l,
      model: i,
      requestInspection: a,
      requestKind: "actual",
      chatPreset: n.chatPreset,
      regexApplications: n.regexApplications,
      requestTask: r.task
    })
  };
}
async function zte(e) {
  const t = Zf(e), n = e.sessionId ? await qa(e.sessionId) : null, r = V6(n, e.contextSnapshot);
  r1(r);
  const s = Xr(n?.state || e.runtimeState || {}), a = _f(s1(s)), o = z6(a), l = await Kn("simulate_user_input_regex", () => um({
    applyRegex: e.applyRegex,
    placement: "userInput",
    id: "simulate",
    text: e.currentUserMessage
  })), i = {};
  fo(i, l.summary);
  const u = qS(r), d = Bl(await Kn("simulate_user_input_substitute", () => X6({
    applySubstituteParams: e.applySubstituteParams,
    id: "userInput:simulate",
    text: l.text,
    options: u
  }))), f = n ? await H6({
    sessionId: n.id,
    contextWindowStartOrder: s.contextWindowStartOrder,
    currentUserMessage: d
  }) : null, m = {
    ...r,
    worldSettings: {
      ...r.worldSettings || {},
      trigger: String(e.generationTrigger || "normal")
    },
    history: n ? th(f?.historyMessages || []) : e.contextSnapshot.history || []
  }, v = await Kn("simulate_native_worldbook_runtime", () => zS({
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
    context: m,
    currentUserMessage: d,
    trigger: String(e.generationTrigger || "normal"),
    timedState: s.nativeWorldInfoTimedState
  })), y = await Kn("simulate_world_entry_substitute", () => HS({
    applySubstituteParams: e.applySubstituteParams,
    context: v.context,
    options: u
  })), S = await Kn("simulate_memory_query", () => E6(y, d)), x = n && (a.includeMemoryFiles || a.includeStructuredStates) ? await Kn("simulate_memory_retrieval", () => C6({
    sessionId: n.id,
    queryText: S,
    ignoredTerms: NS(y),
    includeMemoryFiles: a.includeMemoryFiles,
    includeStructuredStates: a.includeStructuredStates
  })) : void 0, I = n && a.includeQuestOrchestration ? await Kn("simulate_quest_hook_retrieval", () => W3(n.id, 1)) : [], E = J6(x || I.length ? {
    ...x || {},
    ...I.length ? { questHooks: I } : {}
  } : void 0, a), N = K6(a), $ = await Kn("simulate_brain_build", () => H1({
    context: y,
    chatPreset: t,
    currentUserMessage: d,
    historyMode: e.historyMode || "raw",
    turn: s.turn,
    entryStates: s.worldEntryStates,
    memoryContext: E,
    runtimeProtocolMessages: N,
    diagnostics: e.diagnostics || {},
    regexApplications: i,
    transformConversationMessages: async (k) => {
      const P = await Ig({
        applySubstituteParams: e.applySubstituteParams,
        messages: k,
        options: u
      }), H = await Y6({
        applyRegex: e.applyRegex,
        messages: P
      });
      return fo(i, H.summary), H.messages;
    },
    transformFinalMessages: async (k) => Ig({
      applySubstituteParams: e.applySubstituteParams,
      messages: k,
      options: u
    }),
    transformWorldEntries: async (k) => {
      const P = !!y.nativeWorldInfo, H = k.filter((J) => Q6(J, P)), G = await eh(e.applyRegex, H.map((J) => ({
        id: `worldInfo:${J.activationKey}`,
        text: J.content,
        placement: "worldInfo",
        options: {
          isMarkdown: !1,
          isPrompt: !0,
          depth: J.position === Hg.atDepth ? J.depth : null
        }
      })));
      fo(i, Qf(G));
      const ee = new Map(G.map((J) => [J.id, J]));
      return k.map((J) => {
        const z = ee.get(`worldInfo:${J.activationKey}`)?.text ?? J.content;
        return {
          ...J,
          content: z,
          contentChars: z.length
        };
      });
    }
  })), { buildResult: p, buildSnapshot: w } = await G6({
    stage: "simulate_native_prompt_build",
    buildNativeChatPrompt: e.buildNativeChatPrompt,
    contextForBuild: y,
    chatPreset: t,
    baseBuildResult: $.buildResult,
    baseBuildSnapshot: $.buildSnapshot,
    currentUserMessage: d,
    generationType: String(e.generationTrigger || "normal"),
    memoryContext: E,
    runtimeProtocolMessages: N,
    diagnostics: e.diagnostics
  }), _ = await Kn("simulate_request_inspection", () => KS({
    agentConfig: e.agentConfig,
    messages: p.messages,
    chatPreset: t,
    tools: o.tools,
    toolChoice: o.toolChoice,
    onStreamProgress: () => {
    },
    requestKind: "simulated",
    regexApplications: i
  })), b = _.requestSnapshot.provider, C = _.requestSnapshot.model;
  return {
    buildResult: p,
    buildSnapshot: w,
    requestSnapshot: _.requestSnapshot,
    provider: b,
    model: C
  };
}
function Kte(e, t = {}) {
  if (e && typeof e == "object" && !Array.isArray(e)) return e;
  try {
    const n = JSON.parse(String(e || "{}"));
    return n && typeof n == "object" && !Array.isArray(n) ? n : t;
  } catch {
    return t;
  }
}
function Wte(e) {
  try {
    return JSON.stringify(e || {});
  } catch {
    return "{}";
  }
}
async function Gte(e) {
  const t = y6(), n = [...e.messages], r = [], s = Vte(e.agentConfig, e.executeRunOnce);
  let a = "", o, l = "", i = "", u = "", d, f = Zu(e.agentConfig, e.messages, {
    chatPreset: e.chatPreset,
    regexApplications: e.regexApplications
  }), m, v = "", y = !1, S = !1;
  for (let x = 1; x <= 10; x += 1) {
    const I = xv({
      supportsSessionToolLoop: s,
      messages: n,
      toolResponses: m,
      finalAnswerReminderText: v
    }), E = await e.executeRunOnce({
      agentConfig: e.agentConfig,
      messages: I.requestMessages,
      chatPreset: e.chatPreset,
      regexApplications: e.regexApplications,
      tools: t,
      toolChoice: "auto",
      ...I.mode === "session_tool_response_round" ? { toolResponses: I.toolResponses } : {},
      ...I.mode === "session_final_reminder_round" ? { finalAnswerReminderText: I.finalAnswerReminderText } : {},
      signal: e.signal,
      onStreamProgress: (b) => {
        if (e.onStreamProgress) {
          if (typeof b?.text != "string") {
            e.onStreamProgress(b);
            return;
          }
          e.onStreamProgress({
            ...b,
            text: a + b.text
          });
        }
      }
    });
    m = void 0, v = "", l = String(E.provider || l || ""), i = String(E.model || i || ""), u = String(E.finishReason || u || ""), d = E.providerPayload, f = E.requestSnapshot || f;
    const N = String(E.text || ""), $ = Array.isArray(E.toolCalls) ? E.toolCalls : [];
    if (!$.length) {
      if (!e1(N) && y && !S) {
        S = !0;
        const b = "All required action checks are complete. Do not call more tools. Finish the assistant reply now.";
        s ? v = b : n.push({
          role: "system",
          content: b
        });
        continue;
      }
      if (!e1(N) && y) throw new Error("模型在检定后没有给出有效结论。");
      return a += N, o = E.thoughts, {
        ...E,
        text: a,
        thoughts: o,
        provider: l,
        model: i,
        finishReason: u,
        providerPayload: d,
        requestSnapshot: f,
        runtimeEvents: r
      };
    }
    if (x > vte) throw new Error("action_check_tool_round_limit_exceeded");
    y = !0, a += N;
    const p = a.length, w = m6({
      text: N,
      providerPayload: E.providerPayload
    }, $, { fallbackPrefix: "tavern-rp-tool" });
    w.thoughts = E.thoughts, n.push(w);
    const _ = [];
    $.forEach((b) => {
      const C = Kte(b.arguments, {}), k = b.name === "ActionCheck" ? vee(C, { rollDie: e.actionCheckRoll }) : yee(b.name);
      if (k.ok) {
        const P = kte(a, k, p);
        r.push(xD({
          action: k.action,
          stat: k.stat,
          difficulty: k.difficulty,
          roll: k.roll,
          success: k.success,
          outcome: k.outcome,
          insertAfterChars: P,
          toolCallId: String(b.id || ""),
          summary: xte(k),
          stakes: k.stakes
        })), e.onStreamProgress?.({
          text: a,
          liveActionCheckEvents: r.map((H) => ({ ...H }))
        });
      }
      _.push({
        id: String(b.id || ""),
        name: String(b.name || ""),
        response: k
      }), n.push(g6({
        toolCallId: String(b.id || ""),
        toolName: String(b.name || ""),
        content: Wte(k)
      }));
    }), s && (m = _);
  }
  throw new Error("action_check_tool_round_limit_exceeded");
}
function Vte(e, t) {
  return t?.supportsSessionToolLoop === !0;
}
var Ap = /* @__PURE__ */ new Map();
function Jte(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return;
  const n = (Ap.get(t) || Promise.resolve()).catch(() => {
  }).then(async () => {
    const r = await hte({
      sessionId: t,
      agentConfig: e.agentConfig,
      assistantPreset: e.assistantPreset,
      sessionContract: e.sessionContract,
      contextSnapshot: e.contextSnapshot,
      executeManagerOnce: e.executeManagerOnce,
      onManagerRunSaved: async (s) => {
        await oo(() => e.onManagerRunSaved?.(t, s.id));
      }
    });
    r?.ok && r.managerRun?.status === "completed" && await ng(t, r.managerRun.assistantOrder);
  }).catch(async (r) => {
    console.warn("[小白酒馆] accepted-turn manager background task failed", r);
  }).finally(() => {
    Ap.get(t) === n && Ap.delete(t);
  });
  Ap.set(t, n);
}
async function Yte(e) {
  const t = Zf(e);
  e.sessionId || r1(e.contextSnapshot || {});
  const n = await qte(e), r = V6(n, e.contextSnapshot);
  r1(r);
  const s = Number(e.reuseUserMessageOrder), a = Number.isInteger(s) && s >= 0 ? await bl(n.id, s) : null, o = a?.role === "user" && !a.error ? a : null;
  if (o) {
    const W = o.order + 1;
    await BS(n.id, W), await j3(n.id, W - 1), await H3(n.id, W - 1), await X3(n.id, W), await q3(n.id, W), await Hf(n.id), await s3(n.id, await hw(n.id, W));
  }
  const l = Xr(n.state || e.runtimeState || {}), i = s1(l), u = _f(i), d = { ...e.diagnostics || {} };
  o || (e.runManager === !0 && u.hasAutomaticManagerWork && Jte({
    sessionId: n.id,
    agentConfig: e.agentConfig,
    assistantPreset: e.assistantPreset,
    sessionContract: i,
    contextSnapshot: r,
    executeManagerOnce: e.executeManagerOnce,
    onManagerRunSaved: e.onManagerRunSaved
  }), await ng(n.id));
  const f = o ? await yte(n.id, 0, o.order - 1) : null, m = o ? await e8({
    messages: f || [],
    contextSnapshot: r,
    chatPreset: e.chatPreset,
    historyMode: e.historyMode || "raw",
    diagnostics: d,
    applySubstituteParams: e.applySubstituteParams,
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime
  }) : null, v = o ? Nte({
    ...m,
    contextWindowStartOrder: l.contextWindowStartOrder
  }, l.contract) : l, y = s1(v), S = _f(y), x = z6(S), I = !!o, E = String(o?.content || e.currentUserMessage || ""), N = String(t.id || n.chatPresetId || n.presetId || ""), $ = String(t.name || n.chatPresetName || n.presetName || "");
  let p = o;
  p || (p = await hd(n.id, {
    role: "user",
    content: E,
    contextSnapshot: r,
    chatPresetId: N,
    chatPresetName: $,
    presetId: N,
    presetName: $
  }), await oo(() => e.onUserMessageSaved?.(n.id, p)));
  const w = {}, _ = o ? {
    text: E,
    summary: void 0
  } : await Kn("turn_user_input_regex", () => um({
    applyRegex: e.applyRegex,
    placement: "userInput",
    id: "turn",
    text: E
  }));
  fo(w, _.summary);
  const b = qS(r), C = o ? _.text : await Kn("turn_user_input_substitute", () => X6({
    applySubstituteParams: e.applySubstituteParams,
    id: "userInput:turn",
    text: _.text,
    options: b
  })), k = Bl(C);
  p && !o && p.content !== C && (p = await zd(n.id, p.order, { content: C }) || p, await oo(() => e.onUserMessageSaved?.(n.id, p)));
  const P = await H6({
    sessionId: n.id,
    contextWindowStartOrder: v.contextWindowStartOrder,
    currentUserMessage: k,
    beforeOrder: p?.order
  }), H = P.historyMessages, G = o ? await wT(n.id, o.order, 2) : await wT(n.id, p?.order ?? Number.POSITIVE_INFINITY, 2), ee = Ste({
    runtime: S,
    sessionMessages: G,
    historyMessages: o ? G : H,
    reusedUserMessage: o,
    rerollRuntimeEvents: e.rerollRuntimeEvents,
    randomEncounterRoll: e.randomEncounterRoll
  });
  p && !o && ee && (p = await zd(n.id, p.order, { runtimeEvents: [ee] }) || p, await oo(() => e.onUserMessageSaved?.(n.id, p)));
  const J = String(e.generationTrigger || (o ? "regenerate" : "normal")), z = {
    ...r,
    worldSettings: {
      ...r.worldSettings || {},
      trigger: J
    },
    history: th(P.historyMessages)
  }, R = await Kn("turn_native_worldbook_runtime", () => zS({
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
    context: z,
    currentUserMessage: k,
    trigger: J,
    timedState: v.nativeWorldInfoTimedState
  })), V = await Kn("turn_world_entry_substitute", () => HS({
    applySubstituteParams: e.applySubstituteParams,
    context: R.context,
    options: b
  })), le = await Kn("turn_memory_query", () => E6(V, k)), ve = S.includeMemoryFiles || S.includeStructuredStates ? await Kn("turn_memory_retrieval", () => C6({
    sessionId: n.id,
    queryText: le,
    ignoredTerms: NS(V),
    includeMemoryFiles: S.includeMemoryFiles,
    includeStructuredStates: S.includeStructuredStates
  })) : void 0, ke = S.includeQuestOrchestration ? await Kn("turn_quest_hook_retrieval", () => W3(n.id, 1)) : [], be = J6(ve || ke.length ? {
    ...ve || {},
    ...ke.length ? { questHooks: ke } : {}
  } : void 0, S), Ne = K6(S), ce = await Kn("turn_brain_build", () => H1({
    context: V,
    chatPreset: t,
    currentUserMessage: k,
    historyMode: e.historyMode || "raw",
    turn: v.turn,
    entryStates: v.worldEntryStates,
    memoryContext: be,
    runtimeDepthEntries: Ate(ee),
    runtimeProtocolMessages: Ne,
    diagnostics: d,
    regexApplications: w,
    transformConversationMessages: async (W) => {
      const L = await Ig({
        applySubstituteParams: e.applySubstituteParams,
        messages: W,
        options: b
      }), Q = await Y6({
        applyRegex: e.applyRegex,
        messages: L
      });
      return fo(w, Q.summary), Q.messages;
    },
    transformFinalMessages: async (W) => Ig({
      applySubstituteParams: e.applySubstituteParams,
      messages: W,
      options: b
    }),
    transformWorldEntries: async (W) => {
      const L = !!V.nativeWorldInfo, Q = W.filter((pe) => Q6(pe, L)), de = await eh(e.applyRegex, Q.map((pe) => ({
        id: `worldInfo:${pe.activationKey}`,
        text: pe.content,
        placement: "worldInfo",
        options: {
          isMarkdown: !1,
          isPrompt: !0,
          depth: pe.position === Hg.atDepth ? pe.depth : null
        }
      })));
      fo(w, Qf(de));
      const ge = new Map(de.map((pe) => [pe.id, pe]));
      return W.map((pe) => {
        const Me = ge.get(`worldInfo:${pe.activationKey}`)?.text ?? pe.content;
        return {
          ...pe,
          content: Me,
          contentChars: Me.length
        };
      });
    }
  })), { buildResult: we, buildSnapshot: Oe } = await G6({
    stage: "turn_native_prompt_build",
    buildNativeChatPrompt: e.buildNativeChatPrompt,
    contextForBuild: V,
    chatPreset: t,
    baseBuildResult: ce.buildResult,
    baseBuildSnapshot: ce.buildSnapshot,
    currentUserMessage: k,
    generationType: J,
    signal: e.signal,
    memoryContext: be,
    chancePrompt: ee ? Vk().content : "",
    runtimeProtocolMessages: Ne,
    diagnostics: d
  }), F = await Nb(n.id, {
    contextSnapshot: r,
    buildSnapshot: Oe,
    chatPresetId: String(t.id || n.chatPresetId || n.presetId || ""),
    chatPresetName: String(t.name || n.chatPresetName || n.presetName || ""),
    presetId: String(t.id || n.presetId || ""),
    presetName: String(t.name || n.presetName || "")
  }) || n;
  let ae = "";
  const Z = (W) => {
    typeof W.text == "string" && (ae = W.text), e.onStreamProgress?.(W);
  };
  let ie = Zu(e.agentConfig, we.messages, {
    chatPreset: t,
    regexApplications: w
  });
  try {
    ie = (await KS({
      agentConfig: e.agentConfig,
      messages: we.messages,
      chatPreset: t,
      tools: x.tools,
      toolChoice: x.toolChoice,
      onStreamProgress: Z,
      requestKind: "actual",
      regexApplications: w
    })).requestSnapshot;
  } catch {
    ie = Zu(e.agentConfig, we.messages, {
      requestKind: "fallback",
      chatPreset: t,
      regexApplications: w,
      requestTask: {
        messages: we.messages,
        tools: x.tools,
        toolChoice: x.toolChoice
      }
    });
  }
  const me = String(t.id || F.chatPresetId || F.presetId || ""), Se = String(t.name || F.chatPresetName || F.presetName || "");
  if (p) {
    const W = M1(p.runtimeEvents);
    p = await zd(F.id, p.order, {
      contextSnapshot: r,
      buildSnapshot: Oe,
      chatPresetId: me,
      chatPresetName: Se,
      presetId: me,
      presetName: Se,
      requestSnapshot: ie,
      runtimeEvents: !W && ee ? [ee] : p.runtimeEvents || []
    }) || p;
  }
  if (!p) throw new Error("user_message_save_failed");
  await oo(() => e.onUserMessageSaved?.(F.id, p));
  try {
    const W = e.executeRunOnce || Xte(e.agentConfig), L = S.includeActionChecks ? await Gte({
      agentConfig: e.agentConfig,
      messages: we.messages,
      chatPreset: t,
      regexApplications: w,
      signal: e.signal,
      onStreamProgress: Z,
      executeRunOnce: W,
      actionCheckRoll: e.actionCheckRoll
    }) : await W({
      agentConfig: e.agentConfig,
      messages: we.messages,
      chatPreset: t,
      regexApplications: w,
      signal: e.signal,
      onStreamProgress: Z
    }), Q = S.includeActionChecks ? di(L.runtimeEvents) : [], de = sb(L.text, Q), ge = await um({
      applyRegex: e.applyRegex,
      placement: "aiOutput",
      id: "assistant",
      text: de.text
    }), pe = await Lte({
      applyRegex: e.applyRegex,
      thoughts: L.thoughts
    });
    fo(w, ge.summary), fo(w, pe.summary);
    const Me = Gk(ge.text, Q, de.boundaries);
    (Me.text !== L.text || pe.thoughts !== L.thoughts) && e.onStreamProgress?.({
      text: Me.text,
      thoughts: pe.thoughts,
      liveActionCheckEvents: Me.events.map((st) => ({ ...st }))
    });
    const Ie = Me.events, he = {
      ...L.requestSnapshot,
      regexApplications: w
    }, q = await hd(F.id, {
      role: "assistant",
      content: Me.text,
      thoughts: pe.thoughts,
      providerPayload: L.providerPayload,
      contextSnapshot: r,
      buildSnapshot: Oe,
      chatPresetId: me,
      chatPresetName: Se,
      presetId: me,
      presetName: Se,
      requestSnapshot: he,
      provider: L.provider || "",
      model: L.model || "",
      finishReason: L.finishReason || "",
      runtimeEvents: Ie
    });
    await oo(() => e.onAssistantMessageSaved?.(F.id, q));
    const ye = Number(v.turn || 0) + 1;
    await R_(F.id, {
      turn: ye,
      contextWindowStartOrder: P.contextWindowStartOrder,
      worldEntryStates: ox(v, we, I),
      nativeWorldInfoTimedState: V.nativeWorldInfo ? R.timedState : nf(v.nativeWorldInfoTimedState),
      lastBuildSnapshot: Oe,
      lastRequestSnapshot: he,
      lastProvider: L.provider || "",
      lastModel: L.model || ""
    }, { replace: I });
    let oe = "", De = "";
    const Je = String(L.finishReason || "").trim();
    if (e.runManager === !0 && !q.error && !["aborted", "error"].includes(Je) && S.hasAutomaticManagerWork) {
      const st = await fte({
        sessionId: F.id,
        agentConfig: e.agentConfig,
        userMessage: p,
        assistantMessage: q,
        turn: ye,
        assistantPreset: e.assistantPreset,
        sessionContract: y,
        contextSnapshot: r,
        executeManagerOnce: e.executeManagerOnce,
        onManagerRunSaved: async (lt) => {
          await oo(() => e.onManagerRunSaved?.(F.id, lt.id));
        }
      });
      oe = st.managerRunId, De = st.managerStatus;
    }
    return {
      sessionId: F.id,
      userMessage: p,
      assistantMessage: q,
      buildResult: we,
      buildSnapshot: Oe,
      requestSnapshot: he,
      provider: L.provider || "",
      model: L.model || "",
      finishReason: L.finishReason,
      previewMatchesRequest: we.meta.rawMessagesJson === he.rawMessagesJson,
      nextTurn: ye,
      managerRunId: oe,
      managerStatus: De
    };
  } catch (W) {
    const L = W?.requestSnapshot;
    L && (ie = {
      ...L,
      regexApplications: w
    });
    const Q = Mte(W, e.signal), de = String(ae || "").trim();
    if (Q && de) {
      const Me = await um({
        applyRegex: e.applyRegex,
        placement: "aiOutput",
        id: "assistant-partial",
        text: de
      });
      fo(w, Me.summary), _6(w) && (ie = {
        ...ie,
        regexApplications: w
      }), Me.text !== de && e.onStreamProgress?.({ text: Me.text });
      const Ie = await hd(F.id, {
        role: "assistant",
        content: Me.text,
        error: !1,
        contextSnapshot: r,
        buildSnapshot: Oe,
        chatPresetId: me,
        chatPresetName: Se,
        presetId: me,
        presetName: Se,
        requestSnapshot: ie,
        provider: ie.provider,
        model: ie.model,
        finishReason: "aborted"
      });
      await oo(() => e.onAssistantMessageSaved?.(F.id, Ie));
      const he = Number(v.turn || 0) + 1;
      return await R_(F.id, {
        turn: he,
        contextWindowStartOrder: P.contextWindowStartOrder,
        worldEntryStates: ox(v, we, I),
        nativeWorldInfoTimedState: V.nativeWorldInfo ? R.timedState : nf(v.nativeWorldInfoTimedState),
        lastBuildSnapshot: Oe,
        lastRequestSnapshot: ie,
        lastProvider: ie.provider,
        lastModel: ie.model
      }, { replace: I }), {
        sessionId: F.id,
        userMessage: p,
        assistantMessage: Ie,
        buildResult: we,
        buildSnapshot: Oe,
        requestSnapshot: ie,
        provider: ie.provider,
        model: ie.model,
        finishReason: "aborted",
        previewMatchesRequest: we.meta.rawMessagesJson === ie.rawMessagesJson,
        nextTurn: he,
        managerRunId: "",
        managerStatus: ""
      };
    }
    const ge = Rte(W instanceof Error ? W.message : String(W || "run_failed")), pe = await hd(F.id, {
      role: "assistant",
      content: Q ? "已停止生成。" : ge,
      error: !0,
      contextSnapshot: r,
      buildSnapshot: Oe,
      chatPresetId: me,
      chatPresetName: Se,
      presetId: me,
      presetName: Se,
      requestSnapshot: ie,
      provider: ie.provider,
      model: ie.model,
      finishReason: Q ? "aborted" : "error"
    });
    return await oo(() => e.onAssistantMessageSaved?.(F.id, pe)), await R_(F.id, {
      turn: Number(v.turn || 0),
      contextWindowStartOrder: P.contextWindowStartOrder,
      worldEntryStates: I ? v.worldEntryStates || {} : {},
      nativeWorldInfoTimedState: nf(v.nativeWorldInfoTimedState),
      lastBuildSnapshot: Oe,
      lastRequestSnapshot: ie,
      lastProvider: ie.provider,
      lastModel: ie.model,
      lastError: Q ? "已停止生成。" : ge
    }, { replace: I }), {
      sessionId: F.id,
      userMessage: p,
      errorMessage: pe,
      buildResult: we,
      buildSnapshot: Oe,
      requestSnapshot: ie,
      provider: ie.provider,
      model: ie.model,
      finishReason: Q ? "aborted" : "error",
      previewMatchesRequest: we.meta.rawMessagesJson === ie.rawMessagesJson,
      nextTurn: Number(v.turn || 0),
      error: Q ? "已停止生成。" : ge
    };
  }
}
function Xte(e) {
  const t = P6(e), n = PS(t, { missingApiKeyMessage: "请先在 API 配置里选择模型/填写 Key。" }), r = ((s) => Hte(n, t, s));
  return r.supportsSessionToolLoop = n.supportsSessionToolLoop === !0, r;
}
function jte(e) {
  return !Array.isArray(e) || !e.length ? [] : e.map((t) => {
    const n = t && typeof t == "object" ? t : {}, r = n.function && typeof n.function == "object" ? n.function : {}, s = typeof n.id == "string" ? n.id : typeof n.tool_call_id == "string" ? n.tool_call_id : void 0, a = typeof n.name == "string" ? n.name : typeof r.name == "string" ? r.name : void 0, o = n.arguments ?? r.arguments, l = typeof o == "string" ? o : o === void 0 ? void 0 : JSON.stringify(o);
    return {
      ...s ? { id: s } : {},
      ...a ? { name: a } : {},
      ...l ? { arguments: l } : {}
    };
  }).filter((t) => !!(t.id || t.name || t.arguments));
}
function cm(e = "", t = 180) {
  const n = String(e || "").trim();
  return n.length > t ? `${n.slice(0, t)}...` : n;
}
function Qte(e) {
  return `manager:${e.sessionId}:${e.order}`;
}
function ix(e) {
  if (!e || typeof e != "object") return !1;
  const t = e;
  return t.error === !0 || ["aborted", "error"].includes(String(t.finishReason || "").trim()) ? !1 : Array.isArray(t.toolCalls) && t.toolCalls.length > 0 || Array.isArray(t.tool_calls) && t.tool_calls.length > 0;
}
function Zte(e) {
  const t = e && typeof e == "object" ? e : {};
  return (Array.isArray(t.toolCalls) && t.toolCalls.length ? t.toolCalls : Array.isArray(t.tool_calls) ? t.tool_calls : []).map((n, r) => {
    const s = n && typeof n == "object" ? n : {}, a = s.function && typeof s.function == "object" ? s.function : {}, o = String(s.id || s.tool_call_id || `tool-call-${r + 1}`), l = String(s.name || a.name || "").trim() || "工具", i = s.arguments ?? a.arguments ?? {};
    return {
      id: o,
      name: l,
      argumentsText: cm(typeof i == "string" ? i : JSON.stringify(i || {}), 320)
    };
  }).filter((n) => n.name);
}
function t8(e) {
  if (!e || typeof e != "string") return {};
  try {
    const t = JSON.parse(e);
    return t && typeof t == "object" && !Array.isArray(t) ? t : {};
  } catch {
    return {};
  }
}
function ene(e) {
  if (!e) return "等待工具返回。";
  const t = String(e.toolDisplay || "").trim();
  if (t) return cm(t, 360);
  const n = t8(e.content), r = String(n.summary || n.message || n.error || "").trim();
  if (r) return cm(r, 360);
  const s = String(n.path || n.filePath || n.docId || "").trim(), a = n.changed === !0 ? "已变更" : n.changed === !1 ? "无变化" : "", o = [
    n.ok === !1 ? "失败" : n.ok === !0 ? "成功" : "",
    a,
    s
  ].filter(Boolean).join(" · ");
  return o || cm(String(e.content || "").trim(), 360) || "工具已返回。";
}
function tne(e, t) {
  return {
    assistantMessage: e,
    toolMessages: t,
    calls: Zte(e).map((n) => {
      const r = t.find((a) => String(a.toolCallId || a.tool_call_id || "") === n.id), s = t8(r?.content);
      return {
        id: n.id,
        name: r?.toolName || n.name,
        argumentsText: n.argumentsText,
        resultText: ene(r),
        ok: s.ok !== !1 && !r?.error,
        toolMessage: r
      };
    })
  };
}
function nne(e) {
  const t = e[0], n = e[e.length - 1] || t;
  return {
    kind: "tool-turn",
    key: `tool-turn:${t.assistantMessage.sessionId}:${t.assistantMessage.order}:${n.assistantMessage.order}`,
    anchorKey: `tool:${t.assistantMessage.sessionId}:${t.assistantMessage.order}`,
    rounds: e,
    assistantMessage: t.assistantMessage,
    toolMessages: e.flatMap((r) => r.toolMessages),
    calls: e.flatMap((r) => r.calls)
  };
}
function lx(e) {
  const t = [...e].sort((r, s) => r.order - s.order), n = [];
  for (let r = 0; r < t.length; r += 1) {
    const s = t[r];
    if (!(!s || ![
      "user",
      "assistant",
      "tool"
    ].includes(s.role))) {
      if (s.role === "assistant" && ix(s)) {
        const a = [];
        let o = r;
        for (; o < t.length && t[o]?.role === "assistant" && ix(t[o]); ) {
          const l = t[o], i = [];
          for (o += 1; o < t.length && t[o]?.role === "tool"; )
            i.push(t[o]), o += 1;
          a.push(tne(l, i));
        }
        n.push(nne(a)), r = o - 1;
        continue;
      }
      s.role !== "tool" && n.push({
        kind: "message",
        key: Qte(s),
        anchorKey: `msg:${s.sessionId}:${s.order}`,
        message: s
      });
    }
  }
  return n;
}
function rne(e) {
  const t = e.calls.filter((s) => !s.ok).length, n = e.calls.filter((s) => !s.toolMessage).length, r = e.rounds.length > 1 ? `${e.rounds.length} 轮 · ` : "";
  return n ? `${r}工具调用 ${e.calls.length} 次 · ${n} 个等待返回` : t ? `${r}工具调用 ${e.calls.length} 次 · ${t} 次失败` : `${r}工具调用 ${e.calls.length} 次 · 全部成功`;
}
function sne(e) {
  const t = e.calls.map((r) => r.name).filter(Boolean).slice(0, 3).join("、"), n = e.calls.length > 3 ? ` 等 ${e.calls.length} 个` : "";
  return t ? `${t}${n}` : "等待工具返回";
}
function ane() {
  let e = [], t = !1;
  return {
    update(n = {}) {
      const r = Array.isArray(n.toolCalls) && n.toolCalls.length ? jte(n.toolCalls) : null, s = typeof n.text == "string" && n.text.trim().length > 0;
      return r ? (e = r, t = !0) : s && t && n.toolCallDraft !== !0 && (e = [], t = !1), {
        content: typeof n.text == "string" && n.text.trim() ? n.text : e.length ? "正在准备工具调用..." : "正在思考...",
        toolCalls: e
      };
    },
    reset() {
      e = [], t = !1;
    }
  };
}
async function ux(e = "", t = 0) {
  return BS(e, t);
}
async function cx(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  !n || !Number.isFinite(r) || (await j3(n, r - 1), await H3(n, r - 1), await X3(n, r), await q3(n, r), await Hf(n));
}
async function dx(e, t) {
  const n = Number(t) - 1, [r, s, a] = await Promise.all([
    kH(e, n),
    dH(e, n),
    pte(e, t)
  ]);
  return {
    targetFloor: n,
    memory: r,
    tasks: s,
    managers: a,
    willRollbackState: r.changed || s.changed,
    willCancelWork: a.pendingRuns > 0
  };
}
function one(e) {
  return e >= 0 ? `第 ${e + 1} 楼后的状态` : "开局前状态";
}
function fx(e) {
  const t = one(e.targetFloor), n = [];
  return e.memory.changed && e.tasks.changed ? n.push(`会话记忆和事件线索会恢复到${t}。`) : e.memory.changed ? n.push(`会话记忆会恢复到${t}。`) : e.tasks.changed && n.push(`事件线索会恢复到${t}。`), e.managers.pendingRuns && n.push(`将取消 ${e.managers.pendingRuns} 个尚未执行的后台维护。`), n;
}
function ine() {
  return {
    currentUserMessage: /* @__PURE__ */ ue(""),
    isCancellingRun: /* @__PURE__ */ ue(!1),
    isRunning: /* @__PURE__ */ ue(!1),
    runtimeActionCheckEvents: /* @__PURE__ */ ue([]),
    runtimeError: /* @__PURE__ */ ue(""),
    runtimeFinalizedAssistantMessage: /* @__PURE__ */ ue(null),
    runtimeModel: /* @__PURE__ */ ue(""),
    runtimePendingUserMessage: /* @__PURE__ */ ue(""),
    runtimeProvider: /* @__PURE__ */ ue(""),
    runtimeText: /* @__PURE__ */ ue(""),
    runtimeThoughts: /* @__PURE__ */ ue([]),
    runtimeUserMessageVisible: /* @__PURE__ */ ue(!1)
  };
}
function lne(e) {
  const t = e.state, n = /* @__PURE__ */ ue(null);
  let r = 0, s = null;
  function a(E) {
    typeof E.text == "string" && (t.runtimeText.value = E.text), Array.isArray(E.thoughts) && (t.runtimeThoughts.value = e.thoughtBlocks(E.thoughts)), Array.isArray(E.liveActionCheckEvents) && (t.runtimeActionCheckEvents.value = E.liveActionCheckEvents.map((N) => ({ ...N })));
  }
  function o() {
    r && (typeof window < "u" && typeof window.cancelAnimationFrame == "function" && window.cancelAnimationFrame(r), r = 0);
  }
  function l() {
    o();
    const E = s;
    s = null, E && a(E);
  }
  function i(E) {
    const N = s ? { ...s } : {};
    if (typeof E.text == "string" && (N.text = E.text), Array.isArray(E.thoughts) && (N.thoughts = E.thoughts), Array.isArray(E.liveActionCheckEvents) && (N.liveActionCheckEvents = E.liveActionCheckEvents), s = N, !r) {
      if (typeof window > "u" || typeof window.requestAnimationFrame != "function") {
        l();
        return;
      }
      r = window.requestAnimationFrame(() => {
        r = 0, l();
      });
    }
  }
  function u() {
    o(), s = null, e.clearRuntimeDisplayRegexRequests(), t.runtimeText.value = "", t.runtimeThoughts.value = [], t.runtimeActionCheckEvents.value = [], t.runtimeFinalizedAssistantMessage.value = null, t.runtimeUserMessageVisible.value = !1, t.runtimePendingUserMessage.value = "";
  }
  function d() {
    t.currentUserMessage.value = "", t.runtimeError.value = "", t.runtimeProvider.value = "", t.runtimeModel.value = "", u();
  }
  function f() {
    return !!t.runtimeFinalizedAssistantMessage.value;
  }
  function m(E = {}) {
    const N = t.runtimeFinalizedAssistantMessage.value;
    if (!N) return !1;
    const $ = String(E.sessionId || N.sessionId || "").trim();
    if ($ && N.sessionId !== $) return !1;
    const p = Number(N.order), w = Number(E.discardFromOrder), _ = Array.isArray(E.discardOrders) ? E.discardOrders.map((b) => Number(b)).filter((b) => Number.isFinite(b)) : [];
    return Number.isFinite(p) && (Number.isFinite(w) && p >= w || _.includes(p)) ? (u(), !0) : (e.upsertLoadedSessionMessage(N), e.touchSessionLocally(N.sessionId, N.createdAt), u(), !0);
  }
  function v() {
    return m();
  }
  function y() {
    n.value?.abort();
  }
  function S() {
    !t.isRunning.value || !n.value || (t.isCancellingRun.value || (l(), t.isCancellingRun.value = !0, t.runtimeText.value = t.runtimeText.value || "正在停止..."), n.value.abort());
  }
  function x() {
    I();
  }
  async function I(E = {}) {
    if (t.isRunning.value) {
      S();
      return;
    }
    let N = String(E.messageText ?? t.currentUserMessage.value ?? "").trim();
    if (!N) {
      t.runtimeError.value = "先写一句话。", e.showToast("先写一句话。", {
        tone: "info",
        durationMs: 1800
      });
      return;
    }
    if (e.selectedSessionCharacterError.value) {
      t.runtimeError.value = e.selectedSessionCharacterError.value, e.showToast(e.selectedSessionCharacterError.value, {
        tone: "warning",
        durationMs: 7e3
      });
      return;
    }
    try {
      N = await e.resolveSlashCommandMessageText(N, E);
    } catch (k) {
      const P = e.describeError(k);
      t.runtimeError.value = P, e.showToast(`命令执行失败：${P}`, {
        tone: "warning",
        durationMs: 5e3
      });
      return;
    }
    if (!N) return;
    const $ = new AbortController();
    n.value = $, t.isRunning.value = !0, t.isCancellingRun.value = !1, t.runtimeError.value = "", o(), s = null, t.runtimeText.value = "", t.runtimeThoughts.value = [], t.runtimeActionCheckEvents.value = [], t.runtimeUserMessageVisible.value = !1, t.runtimePendingUserMessage.value = "", t.runtimeProvider.value = "", t.runtimeModel.value = "";
    const p = e.chatAutoScroll.value !== !1;
    p && (e.chatAutoScroll.value = !0);
    const w = Number(E.reuseUserMessageOrder), _ = Number.isFinite(w);
    m({
      sessionId: e.selectedSessionId.value,
      discardFromOrder: _ ? w + 1 : void 0
    });
    const b = e.normalizeHiddenOutsideCount(e.hiddenOutsideCount.value);
    _ && Number(e.chatMessageWindowLimit.value) !== b && e.setSuppressNextChatWindowLimitReload(), p ? e.resetChatMessageWindowState() : e.setSuppressNextChatWindowLimitReload(), _ && e.selectedSessionId.value && (e.cancelDrawJobsForMessageRange(e.selectedSessionId.value, w + 1), e.pruneLoadedSessionMessagesFromOrder(e.selectedSessionId.value, w + 1)), _ ? (t.runtimeUserMessageVisible.value = !0, p ? e.scrollChatToBottom(!0) : e.updateChatScrollButtons()) : (t.runtimePendingUserMessage.value = N, t.currentUserMessage.value = "", Ut(() => e.resetTextareaHeight(e.chatComposeTextareaRef.value)), p ? e.scrollChatToBottom(!0) : e.updateChatScrollButtons());
    let C = !1;
    try {
      if ($.signal.aborted) {
        const z = t.runtimePendingUserMessage.value;
        u(), _ && e.selectedSessionId.value && await e.loadSelectedSessionMessageWindow({ sessionId: e.selectedSessionId.value }), z && !t.currentUserMessage.value.trim() && (t.currentUserMessage.value = z, Ut(() => e.resetTextareaHeight(e.chatComposeTextareaRef.value)));
        return;
      }
      e.selectedSessionId.value || (await e.refreshRuntimeChatPresetFromHost(), await e.createSessionFromContext());
      const k = await e.resolveRuntimeContextForSession(e.selectedSessionId.value), P = String(k.character?.nativeCharacterId || "").trim(), H = (z) => e.applyRegex(z, { nativeCharacterId: P });
      if ($.signal.aborted) {
        u(), _ && e.selectedSessionId.value && await e.loadSelectedSessionMessageWindow({ sessionId: e.selectedSessionId.value });
        return;
      }
      const G = await e.refreshRuntimeChatPresetFromHost();
      if ($.signal.aborted) {
        u(), _ && e.selectedSessionId.value && await e.loadSelectedSessionMessageWindow({ sessionId: e.selectedSessionId.value });
        return;
      }
      const ee = await Yte({
        sessionId: e.selectedSessionId.value,
        agentConfig: e.agentConfig.value,
        contextSnapshot: k,
        chatPreset: G,
        assistantPreset: e.activeAssistantPreset.value,
        currentUserMessage: N,
        runtimeState: Xr(e.activeSession.value?.state || {}),
        diagnostics: e.diagnostics.value,
        historyMode: e.historyMode.value,
        signal: $.signal,
        reuseUserMessageOrder: E.reuseUserMessageOrder,
        rerollRuntimeEvents: E.rerollRuntimeEvents,
        runManager: !0,
        applyRegex: H,
        applySubstituteParams: e.applySubstituteParams,
        getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
        buildNativeChatPrompt: e.buildNativeChatPrompt,
        onStreamProgress: (z) => {
          i(z);
        },
        onUserMessageSaved: async (z, R) => {
          e.setSelectedSessionId(z), e.upsertLoadedSessionMessage(R), e.touchSessionLocally(z, R.createdAt), t.runtimeUserMessageVisible.value = !0, t.runtimePendingUserMessage.value = "", t.currentUserMessage.value = "", Ut(() => e.resetTextareaHeight(e.chatComposeTextareaRef.value)), e.chatAutoScroll.value !== !1 ? e.scrollChatToBottom(!0) : e.updateChatScrollButtons(), await e.persistSelectedSessionId(z), e.chatAutoScroll.value !== !1 && e.scrollChatToBottom(!0);
        },
        onAssistantMessageSaved: async (z, R) => {
          C = !0, e.setSelectedSessionId(z), l(), e.touchSessionLocally(z, R.createdAt), e.chatAutoScroll.value === !1 ? (t.runtimeFinalizedAssistantMessage.value = R, t.runtimeUserMessageVisible.value = !0, e.updateChatScrollButtons()) : (e.upsertLoadedSessionMessage(R), u(), e.scrollChatToBottom());
        },
        onManagerRunSaved: async (z) => {
          await e.refreshManagerRecords(z);
        }
      });
      e.setSelectedSessionId(ee.sessionId), l();
      const J = f();
      J || u(), t.runtimeError.value = ee.error || "", t.runtimeProvider.value = ee.provider || "", t.runtimeModel.value = ee.model || "", J ? e.updateChatScrollButtons() : (await e.refreshSessions(), e.scrollChatToBottom());
    } catch (k) {
      console.error("[小白酒馆] turn failed", k);
      const P = t.runtimePendingUserMessage.value;
      u(), _ && e.selectedSessionId.value && await e.loadSelectedSessionMessageWindow({ sessionId: e.selectedSessionId.value }), P && !t.currentUserMessage.value.trim() && (t.currentUserMessage.value = P, Ut(() => e.resetTextareaHeight(e.chatComposeTextareaRef.value)));
      const H = e.describeError(k || "run_failed");
      t.runtimeError.value = H, C || e.showToast(H, {
        tone: "warning",
        durationMs: 6e3
      });
    } finally {
      n.value === $ && (n.value = null), t.isCancellingRun.value = !1, t.isRunning.value = !1, f() ? e.updateChatScrollButtons() : e.scrollChatToBottom(), Ut(() => {
        e.enhanceChatMarkdown(), e.updateChatScrollButtons();
      });
    }
  }
  return {
    ...t,
    abortActiveRun: y,
    applyRuntimeStreamSnapshot: a,
    cancelActiveRun: S,
    clearRuntimeAssistantLiveState: u,
    flushRuntimeStreamSnapshotNow: l,
    flushDeferredAssistantCommit: v,
    handleChatSubmit: x,
    resolveDeferredAssistantCommit: m,
    resetChatRunPreviewState: d,
    runOnce: I,
    scheduleRuntimeStreamSnapshot: i
  };
}
var une = "配图已生成", cne = 100, Tp = {
  provider: "disabled",
  providerLabel: "",
  available: !1,
  auto: !1,
  presets: [],
  selectedPresetId: "",
  sizeOptions: [],
  selectedSize: ""
};
function dne(e = "", t = "") {
  const n = String(e || ""), r = String(t || "").trim();
  if (!r) return -1;
  const s = n.indexOf(r);
  if (s >= 0) return s + r.length;
  const a = r.replace(/\s+/g, "");
  if (!a) return -1;
  const o = n.replace(/\s+/g, "").indexOf(a);
  if (o < 0) return -1;
  let l = 0;
  for (let i = 0; i < n.length; i += 1)
    if (!/\s/.test(n[i]) && (l += 1, l >= o + a.length))
      return i + 1;
  return -1;
}
function fne(e = "", t = -1) {
  const n = String(e || "");
  if (t < 0 || !n) return t;
  if (t >= n.length) return n.length;
  const r = 80, s = Math.min(n.length, t + r), a = /* @__PURE__ */ new Set([
    "。",
    "！",
    "？",
    "!",
    "?",
    "…"
  ]), o = /* @__PURE__ */ new Set([
    "”",
    "“",
    "’",
    "‘",
    "」",
    "』",
    "】",
    "）",
    ")",
    '"',
    "'",
    "*",
    "~",
    "～",
    "]"
  ]), l = (i) => {
    let u = i;
    for (; u < n.length && o.has(n[u]); ) u += 1;
    return u;
  };
  if (t > 0 && a.has(n[t - 1])) return l(t);
  for (let i = 0; i < r && t + i < s; i += 1) {
    const u = t + i, d = n[u];
    if (d === `
`) return u + 1;
    if (a.has(d)) return l(u + 1);
    if (d === "." && n.slice(u, u + 3) === "...") return l(u + 3);
  }
  return t;
}
function hne(e = "", t = {}) {
  const n = String(t.slotId || "").trim();
  if (!n) return {
    content: e,
    inserted: !1,
    appended: !1
  };
  const r = `[tavern-image:${n}]`, s = String(e || "");
  if (s.includes(r)) return {
    content: s,
    inserted: !1,
    appended: !1
  };
  let a = dne(s, String(t.anchor || ""));
  if (a >= 0 && (a = fne(s, a)), a >= 0) {
    const o = s.slice(0, a), l = s.slice(a);
    let i = r;
    return o.length > 0 && !o.endsWith(`
`) && (i = `
${i}`), l.length > 0 && !l.startsWith(`
`) && (i = `${i}
`), {
      content: `${o}${i}${l}`,
      inserted: !0,
      appended: !1
    };
  }
  return {
    content: `${s}${s.length > 0 && !s.endsWith(`
`) ? `
` : ""}${r}`,
    inserted: !0,
    appended: !0
  };
}
function pne(e = "", t = []) {
  let n = String(e || ""), r = 0, s = 0;
  return (Array.isArray(t) ? t : []).forEach((a) => {
    const o = a && typeof a == "object" ? a : {};
    if (!o.slotId) return;
    const l = hne(n, o);
    n = l.content, l.inserted && (r += 1), l.appended && (s += 1);
  }), {
    content: n,
    inserted: r,
    appended: s
  };
}
function hx(e = "", t = {}) {
  const n = Number(t.current) || 0, r = Number(t.total) || 0, s = r ? ` ${n}/${r}` : "";
  switch (e) {
    case "llm":
      return "正在分析画面";
    case "gen":
      return r ? `准备生成 ${r} 张图` : "准备生成图片";
    case "queued":
      return Number(t.ahead) > 0 ? `画图排队中，前方 ${Number(t.ahead)} 个任务` : `画图排队中${s}`;
    case "progress":
      return `正在生成图片${s}`;
    case "cooldown": {
      const a = Number.isFinite(Number(t.remainingMs)) ? Number(t.remainingMs) : Number(t.duration);
      return !Number.isFinite(a) || a <= 0 ? "等待画图冷却" : `等待下一张图片${r ? ` ${t.nextIndex || n}/${r}` : ""}，剩余 ${(a / 1e3).toFixed(1)}s`;
    }
    case "success": {
      const a = Number(t.success) || 0, o = r || a;
      return o > 0 && a === 0 ? `画图结束，${o} 张都失败` : `画图完成 ${a}/${o}`;
    }
    default:
      return "正在处理画图";
  }
}
function px(e) {
  if (!e || typeof e != "object") return null;
  const t = e, n = String(t.value || "").trim();
  return n ? {
    value: n,
    label: String(t.label || n).trim() || n
  } : null;
}
function mne(e) {
  const t = /* @__PURE__ */ ue({
    provider: "disabled",
    enabled: !1,
    ready: !1
  }), n = /* @__PURE__ */ ue({ ...Tp }), r = /* @__PURE__ */ ue(!1), s = /* @__PURE__ */ ue({}), a = /* @__PURE__ */ ue([]), o = /* @__PURE__ */ new Map();
  let l = null, i = 0;
  function u(L) {
    return s.value[e.messageKey(L)] || null;
  }
  function d(L) {
    return L?.status === "queued" || L?.status === "running";
  }
  function f() {
    return Object.values(s.value).find((L) => L.status === "running")?.key || "";
  }
  function m(L = "", Q) {
    const de = String(L || "").trim();
    if (!de) return;
    const ge = s.value[de];
    ge && (s.value = {
      ...s.value,
      [de]: {
        ...ge,
        ...Q
      }
    });
  }
  function v(L = "") {
    const Q = String(L || "").trim();
    if (!Q || !s.value[Q]) return;
    const de = { ...s.value };
    delete de[Q], s.value = de;
  }
  function y() {
    l && (window.clearInterval(l), l = null);
  }
  function S(L = "", Q, de = 0) {
    const ge = String(L || "").trim();
    if (!ge) return;
    const pe = Date.now(), Me = i += 1;
    m(ge, {
      ...Q,
      finishedAt: pe,
      finishId: Me,
      controller: void 0
    }), de > 0 && window.setTimeout(() => {
      const Ie = s.value[ge];
      !Ie || ["queued", "running"].includes(Ie.status) || Ie.finishId !== Me || v(ge);
    }, de);
  }
  function x() {
    a.value.forEach((L, Q) => {
      const de = s.value[L];
      !de || de.status !== "queued" || m(L, {
        progressText: Q > 0 ? `排队中，前方 ${Q} 个任务` : "排队中",
        statusKind: "running"
      });
    });
  }
  function I(L, Q = {}) {
    y();
    const de = Math.max(0, Number(Q.duration) || 0), ge = Date.now() + de, pe = () => {
      const Me = s.value[L];
      if (!Me || Me.status !== "running") {
        y();
        return;
      }
      const Ie = Math.max(0, ge - Date.now());
      m(L, {
        statusKind: "running",
        progressText: hx("cooldown", {
          ...Q,
          remainingMs: Ie
        })
      }), Ie <= 0 && y();
    };
    pe(), de > 0 && (l = window.setInterval(pe, cne));
  }
  function E(L = {}) {
    t.value = {
      provider: String(L.provider || "disabled"),
      enabled: L.enabled === !0,
      ready: L.ready === !0
    }, (!t.value.enabled || t.value.provider === "disabled") && (n.value = { ...Tp });
  }
  const N = ne(() => le()), $ = ne(() => t.value.enabled === !0 && String(t.value.provider || "disabled") !== "disabled"), p = ne(() => {
    const L = N.value;
    return L ? z(L) : "";
  }), w = ne(() => {
    const L = N.value;
    return L ? R(L) : "";
  }), _ = ne(() => {
    const L = N.value;
    return !!L && J(L);
  }), b = ne(() => $.value && !t.value.ready && !_.value), C = ne(() => {
    const L = N.value;
    if (L) {
      const Q = p.value;
      if (Q) return Q;
      if (_.value) return ke(L);
    }
    return t.value.ready ? L ? "为最新回复画图" : "没有可配图的回复" : "画图模块初始化中";
  }), k = ne(() => {
    if (_.value) return "■";
    const L = w.value;
    return L.includes("success") ? "✓" : L.includes("error") ? "!" : "🎨";
  });
  async function P() {
    try {
      E(await e.requestHost("xb-tavern:draw-status", {})), $.value && G();
    } catch {
      E({
        provider: "disabled",
        enabled: !1,
        ready: !1
      });
    }
    return t.value;
  }
  function H(L = {}) {
    const Q = L.result && typeof L.result == "object" ? L.result : L, de = Array.isArray(Q.presets) ? Q.presets.map(px).filter((Ie) => !!Ie) : [], ge = Array.isArray(Q.sizeOptions) ? Q.sizeOptions.map(px).filter((Ie) => !!Ie) : [], pe = String(Q.selectedPresetId || de[0]?.value || "").trim(), Me = String(Q.selectedSize || ge[0]?.value || "").trim();
    return {
      provider: String(Q.provider || t.value.provider || "disabled"),
      providerLabel: String(Q.providerLabel || "").trim(),
      available: Q.available === !0 && de.length > 0 && ge.length > 0,
      auto: Q.auto === !0,
      presets: de,
      selectedPresetId: pe,
      sizeOptions: ge,
      selectedSize: Me
    };
  }
  async function G() {
    if (!$.value)
      return n.value = { ...Tp }, n.value;
    r.value = !0;
    try {
      n.value = H(await e.requestHost("xb-tavern:draw-quick-settings", {}));
    } catch {
      n.value = {
        ...Tp,
        provider: t.value.provider
      };
    } finally {
      r.value = !1;
    }
    return n.value;
  }
  async function ee(L = {}) {
    if (!(!$.value || r.value)) {
      r.value = !0;
      try {
        n.value = H(await e.requestHost("xb-tavern:draw-update-quick-settings", { payload: L }));
      } catch (Q) {
        e.showToast(`画图快捷设置保存失败：${e.describeError(Q)}`, {
          tone: "warning",
          durationMs: 3600
        });
      } finally {
        r.value = !1;
      }
    }
  }
  function J(L) {
    return d(u(L));
  }
  function z(L) {
    return u(L)?.progressText || "";
  }
  function R(L) {
    const Q = u(L);
    return Q ? `is-${Q.statusKind}` : "";
  }
  function V(L) {
    return J(L) ? !0 : e.isEditingMessage(L) || L.error || !["user", "assistant"].includes(L.role) ? !1 : !!e.stripTavernImageMarkers(L.content || "");
  }
  function le() {
    return [...e.loadedSessionMessages.value].sort((L, Q) => Q.order - L.order).find((L) => L.sessionId === e.selectedSessionId.value && L.role === "assistant" && V(L)) || null;
  }
  function ve(L = "") {
    return e.markdownSignature(e.stripTavernImageMarkers(L));
  }
  function ke(L) {
    const Q = u(L);
    return Q?.status === "queued" ? z(L) || "取消画图" : Q?.status === "running" ? z(L) || "停止画图" : V(L) ? (t.value.enabled && t.value.ready, "为这条消息画图") : "这条消息暂不能画图";
  }
  async function be() {
    if (!$.value) return;
    const L = N.value;
    if (!L) {
      e.showToast("没有可配图的回复", {
        tone: "info",
        durationMs: 2200
      });
      return;
    }
    if (J(L)) {
      await Se(L);
      return;
    }
    if (!t.value.ready && !(await P()).ready) {
      e.showToast("画图模块初始化中", {
        tone: "info",
        durationMs: 2200
      });
      return;
    }
    await Se(L);
  }
  async function Ne() {
    try {
      await e.requestHost("xb-tavern:draw-open-settings", {}), P();
    } catch (L) {
      e.showToast(`打开画图设置失败：${e.describeError(L)}`, {
        tone: "warning",
        durationMs: 4200
      });
    }
  }
  function ce(L = "") {
    const Q = String(L || "").trim(), de = s.value[Q];
    if (!(!de || !d(de))) {
      if (de.status === "queued") {
        a.value = a.value.filter((ge) => ge !== Q), x(), S(Q, {
          status: "cancelled",
          statusKind: "error",
          progressText: "配图已取消"
        }, 1800);
        return;
      }
      de.controller?.abort(), y(), m(Q, {
        progressText: "正在停止画图",
        statusKind: "running"
      });
    }
  }
  function we(L = "", Q = 0) {
    const de = String(L || "").trim(), ge = Number(Q);
    !de || !Number.isFinite(ge) || Object.values(s.value).forEach((pe) => {
      pe.sessionId === de && Number(pe.order) >= ge && ce(pe.key);
    });
  }
  function Oe(L = "") {
    const Q = String(L || "").trim();
    Q && Object.values(s.value).forEach((de) => {
      de.sessionId === Q && ce(de.key);
    });
  }
  function F() {
    Object.values(s.value).forEach((L) => {
      L.controller?.abort();
    }), a.value = [], o.clear();
  }
  function ae(L) {
    const Q = e.messageKey(L), de = Date.now();
    s.value = {
      ...s.value,
      [Q]: {
        key: Q,
        sessionId: L.sessionId,
        order: L.order,
        role: L.role,
        status: "queued",
        statusKind: "running",
        progressText: "排队中",
        requestId: "",
        sourceTextHash: "",
        queuedAt: de,
        startedAt: 0,
        finishedAt: 0,
        finishId: 0
      }
    }, a.value = [...a.value.filter((ge) => ge !== Q), Q], x(), Z();
  }
  async function Z() {
    if (f()) return;
    const L = a.value.find((Q) => s.value[Q]?.status === "queued") || "";
    L && (a.value = a.value.filter((Q) => Q !== L), x(), await me(L));
  }
  function ie(L, Q) {
    return L ? L.sessionId !== Q.sessionId || L.order !== Q.order || L.role !== Q.role ? "消息已变化" : L.error ? "错误消息不能画图" : ["user", "assistant"].includes(L.role) ? e.stripTavernImageMarkers(L.content || "") ? "" : "消息正文为空" : "这条消息暂不能画图" : "消息已不存在";
  }
  async function me(L = "") {
    const Q = s.value[L];
    if (!Q || Q.status !== "queued") return;
    const de = e.createHostRequestId("draw"), ge = new AbortController();
    m(L, {
      status: "running",
      statusKind: "running",
      progressText: "正在准备画图",
      requestId: de,
      controller: ge,
      startedAt: Date.now()
    }), o.set(de, L);
    try {
      const pe = await e.getTavernMessage(Q.sessionId, Q.order), Me = ie(pe, Q);
      if (Me) {
        S(L, {
          status: "cancelled",
          statusKind: "error",
          progressText: Me
        }, 2600);
        return;
      }
      const Ie = await P();
      if (!Ie.enabled || !Ie.ready) {
        S(L, {
          status: "failed",
          statusKind: "error",
          progressText: "请开启小白X画图模块"
        }, 3200), e.flashMessageAction(pe, "draw", !1);
        return;
      }
      const he = e.stripTavernImageMarkers(pe.content || ""), q = e.markdownSignature(he);
      m(L, { sourceTextHash: q });
      const ye = await e.requestHost("xb-tavern:draw-generate", { payload: {
        source: "tavern",
        text: he,
        title: e.roleLabel(pe.role),
        sessionId: pe.sessionId,
        messageOrder: pe.order,
        role: pe.role,
        characterName: e.selectedSession.value?.characterName || e.effectiveCharacterName.value || ""
      } }, {
        signal: ge.signal,
        requestId: de
      });
      if (ge.signal.aborted) {
        S(L, {
          status: "cancelled",
          statusKind: "error",
          progressText: "配图已取消"
        }, 1800), e.flashMessageAction(pe, "draw", !1);
        return;
      }
      const oe = await e.getTavernMessage(Q.sessionId, Q.order), De = ie(oe, Q);
      if (De) {
        S(L, {
          status: "cancelled",
          statusKind: "error",
          progressText: De
        }, 2600), e.flashMessageAction(pe, "draw", !1);
        return;
      }
      if (ge.signal.aborted) {
        S(L, {
          status: "cancelled",
          statusKind: "error",
          progressText: "配图已取消"
        }, 1800), e.flashMessageAction(oe, "draw", !1);
        return;
      }
      if (ve(oe.content || "") !== q) {
        S(L, {
          status: "cancelled",
          statusKind: "error",
          progressText: "源楼层已变化"
        }, 2600), e.flashMessageAction(oe, "draw", !1);
        return;
      }
      const Je = ye.result || ye, st = Array.isArray(Je.images) ? Je.images : [], lt = pne(oe.content || "", st);
      if (!lt.inserted) {
        const at = Number(Je.success) || 0, X = Number(Je.total) || st.length;
        S(L, {
          status: "failed",
          statusKind: "error",
          progressText: X > 0 && at === 0 ? `画图任务结束：${X} 张都失败了，未插入图片。` : at > 0 ? `画图完成 ${at}/${X || at}，但没有返回可用图片占位。` : "画图任务结束，但没有返回可用图片。"
        }, 4200), e.flashMessageAction(oe, "draw", !1);
        return;
      }
      const ot = await e.updateTavernMessage(oe.sessionId, oe.order, { content: lt.content });
      ot && e.selectedSessionId.value === oe.sessionId && await e.loadSelectedSessionMessageWindow({ sessionId: oe.sessionId });
      const pt = Number(Je.success) || 0, ht = Number(Je.total) || st.length, It = ht > 0 && pt === 0, Lt = lt.appended ? `，${lt.appended} 张追加到末尾` : "", en = ht > 0 ? `配图失败：${ht} 张都失败了` : "配图失败";
      S(L, {
        status: It ? "failed" : "success",
        statusKind: It ? "error" : "success",
        progressText: It ? en : `${une}${Lt}`
      }, It ? 4200 : 2600), e.flashMessageAction(ot || oe, "draw", !It && !!ot), e.nextTick(e.enhanceChatMarkdown);
    } catch (pe) {
      const Me = await e.getTavernMessage(Q.sessionId, Q.order).catch(() => null), Ie = e.describeError(pe);
      /abort|已取消|request_aborted/i.test(Ie) ? S(L, {
        status: "cancelled",
        statusKind: "error",
        progressText: "配图已取消"
      }, 1800) : S(L, {
        status: "failed",
        statusKind: "error",
        progressText: `配图失败：${Ie}`
      }, 4200), Me && e.flashMessageAction(Me, "draw", !1);
    } finally {
      o.delete(de), s.value[L]?.controller === ge && m(L, { controller: void 0 }), Z();
    }
  }
  async function Se(L) {
    const Q = u(L);
    if (d(Q)) {
      ce(Q.key);
      return;
    }
    if (!V(L)) {
      e.flashMessageAction(L, "draw", !1);
      return;
    }
    ae(L);
  }
  function W(L) {
    if (L.type === "xb-tavern:draw-status-changed")
      return E(L.payload || {}), $.value && G(), !0;
    if (L.type === "xb-tavern:draw-progress") {
      const Q = L.payload && typeof L.payload == "object" ? L.payload : {}, de = String(Q.requestId || "").trim(), ge = o.get(de);
      if (ge && s.value[ge]) {
        const pe = String(Q.state || "");
        Q.state === "cooldown" ? I(ge, Q.data || {}) : (y(), m(ge, {
          statusKind: pe === "success" ? "success" : "running",
          progressText: hx(pe, Q.data || {})
        }));
      }
      return !0;
    }
    return !1;
  }
  return {
    canDrawMessage: V,
    cancelJob: ce,
    cancelJobsForMessageRange: we,
    cancelJobsForSession: Oe,
    abortAllJobs: F,
    clearCooldownTimer: y,
    drawLatestAssistantMessage: be,
    drawMessage: Se,
    drawMessageStatusClass: R,
    drawMessageStatusText: z,
    drawMessageTitle: ke,
    handleHostMessage: W,
    isDrawingMessage: J,
    openTavernDrawSettings: Ne,
    refreshTavernDrawQuickSettings: G,
    refreshTavernDrawStatus: P,
    tavernDrawCapsuleIcon: k,
    tavernDrawCapsuleMainDisabled: b,
    tavernDrawCapsuleStatusClass: w,
    tavernDrawCapsuleStatusText: p,
    tavernDrawCapsuleTitle: C,
    tavernDrawCapsuleVisible: $,
    tavernDrawQuickSettings: n,
    tavernDrawQuickSettingsLoading: r,
    updateTavernDrawQuickSettings: ee
  };
}
var gne = "xb-tavern-app", vne = "xb-tavern-host";
function yne(e) {
  const t = /* @__PURE__ */ new WeakSet();
  try {
    return JSON.parse(JSON.stringify(e, (n, r) => {
      if (typeof r == "bigint") return String(r);
      if (!(typeof r == "function" || typeof r == "symbol")) {
        if (!r || typeof r != "object") return r;
        if (!t.has(r))
          return t.add(r), r;
      }
    }));
  } catch {
    return {};
  }
}
function mx() {
  try {
    return new DOMException("request_aborted", "AbortError");
  } catch {
    return /* @__PURE__ */ new Error("request_aborted");
  }
}
function _ne(e = {}) {
  const t = /* @__PURE__ */ new Map(), n = [];
  let r = !1;
  function s(v, y = {}) {
    const S = yne(y);
    window.parent?.postMessage({
      source: gne,
      type: v,
      payload: S
    }, window.location.origin);
  }
  function a(v, y) {
    s("xb-tavern:startup-progress", {
      percent: v,
      action: y
    });
  }
  function o(v = "host") {
    return `${v}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
  }
  function l(v, y = {}, S = {}) {
    const x = String(S.requestId || "").trim() || o();
    return S.signal?.aborted ? Promise.reject(mx()) : (s(v, {
      ...y,
      requestId: x
    }), new Promise((I, E) => {
      const N = () => {
        const p = t.get(x);
        p?.abort && S.signal && S.signal.removeEventListener("abort", p.abort), t.delete(x);
      }, $ = () => {
        N(), s("xb-tavern:cancel-request", { requestId: x }), E(mx());
      };
      S.signal && S.signal.addEventListener("abort", $, { once: !0 }), t.set(x, {
        resolve: I,
        reject: E,
        type: v,
        abort: S.signal ? $ : void 0,
        signal: S.signal
      });
    }));
  }
  function i(v = {}) {
    const y = String(v.requestId || ""), S = t.get(y);
    if (S) {
      if (S.abort && S.signal && S.signal.removeEventListener("abort", S.abort), t.delete(y), v.ok === !1) {
        const x = String(v.error || "host_request_failed"), I = /* @__PURE__ */ new Error(`${S.type}: ${x}`), E = String(v.errorStack || "");
        E && (I.stack = `${I.message}
${E}`), console.error("[小白酒馆] host request failed", {
          type: S.type,
          error: x,
          errorName: String(v.errorName || ""),
          errorStack: E
        }), S.reject(I);
        return;
      }
      S.resolve(v), e.onHostRequestResolved?.(S.type);
    }
  }
  function u(v) {
    return n.push(v), () => {
      const y = n.indexOf(v);
      y >= 0 && n.splice(y, 1);
    };
  }
  function d(v) {
    if (v.origin !== window.location.origin) return;
    const y = v.data || {};
    if (y.source === vne) {
      if (y.type === "xb-tavern:host-result") {
        i(y.payload || {});
        return;
      }
      for (const S of n) if (S(y)) return;
    }
  }
  function f() {
    r || (r = !0, window.addEventListener("message", d));
  }
  function m(v = /* @__PURE__ */ new Error("tavern_host_bridge_disposed")) {
    r && (window.removeEventListener("message", d), r = !1), t.forEach((y) => {
      y.abort && y.signal && y.signal.removeEventListener("abort", y.abort), y.reject(v);
    }), t.clear(), n.length = 0;
  }
  return {
    addMessageHandler: u,
    createHostRequestId: o,
    dispose: m,
    mount: f,
    postToHost: s,
    reportStartupProgress: a,
    requestHost: l
  };
}
function bne() {
  const e = /* @__PURE__ */ ue([]), t = /* @__PURE__ */ ue(""), n = /* @__PURE__ */ ue([]), r = /* @__PURE__ */ ue(0), s = /* @__PURE__ */ ue(null), a = /* @__PURE__ */ ue(null), o = /* @__PURE__ */ ue(-1), l = /* @__PURE__ */ ue({}), i = ne(() => e.value.find((f) => f.id === t.value) || null), u = ne(() => n.value), d = ne(() => wne(n.value));
  return {
    chatMessages: u,
    currentAssistantFloor: ne(() => o.value),
    latestSessionMessage: d,
    loadedSessionMessageEndOrder: a,
    loadedSessionMessageStartOrder: s,
    loadedSessionMessages: n,
    selectedSession: i,
    selectedSessionId: t,
    selectedSessionLatestAssistantOrder: o,
    selectedSessionMessageTotal: r,
    sessionMessageCounts: l,
    sessions: e,
    visibleChatMessages: ne(() => n.value)
  };
}
function wne(e) {
  let t = null;
  return e.forEach((n) => {
    (!t || n.order > t.order) && (t = n);
  }), t;
}
function od(e) {
  return Number(e.updatedAt) || Number(e.createdAt) || 0;
}
function Sne(e, t) {
  let n = 0, r = !1;
  const s = ne(() => {
    const b = Math.max(0, Math.floor(Number(e.selectedSessionMessageTotal.value) || 0)), C = e.loadedSessionMessages.value.length, k = Math.max(0, b - C);
    return {
      startIndex: k,
      hiddenBefore: k,
      visibleCount: C,
      limit: t.chatMessageWindowLimit.value,
      total: b
    };
  });
  vt([() => e.selectedSessionId.value, () => e.selectedSessionMessageTotal.value], ([b, C]) => {
    a(String(b || ""), Number(C) || 0);
  });
  function a(b = "", C = 0) {
    const k = String(b || "").trim();
    if (!k) return;
    const P = Math.max(0, Math.floor(Number(C) || 0));
    e.sessionMessageCounts.value[k] !== P && (e.sessionMessageCounts.value = {
      ...e.sessionMessageCounts.value,
      [k]: P
    });
  }
  function o(b = "") {
    const C = String(b || "").trim();
    if (!C || !(C in e.sessionMessageCounts.value)) return;
    const k = { ...e.sessionMessageCounts.value };
    delete k[C], e.sessionMessageCounts.value = k;
  }
  function l(b) {
    const C = String(b?.id || "").trim();
    return C ? C === e.selectedSessionId.value ? e.selectedSessionMessageTotal.value : Math.max(0, Number(e.sessionMessageCounts.value[C]) || 0) : 0;
  }
  function i(b) {
    const C = String(b?.id || "").trim();
    return C && C !== e.selectedSessionId.value && !(C in e.sessionMessageCounts.value) ? "统计中" : `第 ${l(b)} 楼`;
  }
  async function u(b = []) {
    const C = b.map((G) => String(G.id || "").trim()).filter(Boolean), k = [...new Set(C)].filter((G) => G !== e.selectedSessionId.value && !(G in e.sessionMessageCounts.value));
    if (!k.length) return;
    const P = await Promise.all(k.map(async (G) => [G, await zU(G)])), H = { ...e.sessionMessageCounts.value };
    P.forEach(([G, ee]) => {
      G === e.selectedSessionId.value || G in e.sessionMessageCounts.value || (H[G] = Math.max(0, Math.floor(Number(ee) || 0)));
    }), e.sessionMessageCounts.value = H;
  }
  function d() {
    e.loadedSessionMessages.value = [], e.selectedSessionMessageTotal.value = 0, e.loadedSessionMessageStartOrder.value = null, e.loadedSessionMessageEndOrder.value = null, e.selectedSessionLatestAssistantOrder.value = -1;
  }
  async function f(b = {}) {
    t.reportStartupProgress(96, "loadSelectedSessionMessageWindow");
    const C = String(b.sessionId || e.selectedSessionId.value || "").trim(), k = n + 1;
    if (n = k, !C) {
      d();
      return;
    }
    b.reset && t.resetChatMessageWindowState();
    const P = Math.max(1, Math.floor(Number(t.chatMessageWindowLimit.value) || t.hiddenOutsideCount.value || 1)), [H, G] = await Promise.all([qU(C, P), fw(C)]);
    k !== n || C !== e.selectedSessionId.value || (e.loadedSessionMessages.value = H.messages, e.selectedSessionMessageTotal.value = H.total, e.loadedSessionMessageStartOrder.value = H.loadedStartOrder, e.loadedSessionMessageEndOrder.value = H.loadedEndOrder, e.selectedSessionLatestAssistantOrder.value = G ?? -1, a(C, H.total));
  }
  function m(b) {
    const C = String(b.sessionId || "").trim();
    if (!C || C !== e.selectedSessionId.value) return;
    const k = e.loadedSessionMessages.value, P = k.findIndex((G) => G.sessionId === b.sessionId && G.order === b.order);
    if (P >= 0) e.loadedSessionMessages.value = k.map((G, ee) => ee === P ? b : G);
    else {
      const G = Number(b.order), ee = Number(k.at(-1)?.order);
      if (!k.length || !Number.isFinite(G) || !Number.isFinite(ee) || G >= ee) e.loadedSessionMessages.value = [...k, b];
      else {
        const J = k.findIndex((z) => Number(z.order) > G);
        e.loadedSessionMessages.value = J >= 0 ? [
          ...k.slice(0, J),
          b,
          ...k.slice(J)
        ] : [...k, b];
      }
      e.selectedSessionMessageTotal.value = Math.max(Math.max(0, Math.floor(Number(e.selectedSessionMessageTotal.value) || 0)) + 1, e.loadedSessionMessages.value.length);
    }
    const H = Number(b.order);
    Number.isFinite(H) && (e.loadedSessionMessageStartOrder.value = e.loadedSessionMessageStartOrder.value === null ? H : Math.min(e.loadedSessionMessageStartOrder.value, H), e.loadedSessionMessageEndOrder.value = e.loadedSessionMessageEndOrder.value === null ? H : Math.max(e.loadedSessionMessageEndOrder.value, H), b.role === "assistant" && (e.selectedSessionLatestAssistantOrder.value = Math.max(e.selectedSessionLatestAssistantOrder.value, H))), a(C, e.selectedSessionMessageTotal.value);
  }
  function v(b = "", C = Number.POSITIVE_INFINITY) {
    const k = String(b || "").trim(), P = Number(C);
    if (!k || k !== e.selectedSessionId.value || !Number.isFinite(P)) return 0;
    const H = e.loadedSessionMessages.value, G = H.filter((z) => z.sessionId !== k || Number(z.order) < P), ee = H.length - G.length;
    if (ee <= 0) return 0;
    e.loadedSessionMessages.value = G, e.selectedSessionMessageTotal.value = Math.max(G.length, Math.max(0, Math.floor(Number(e.selectedSessionMessageTotal.value) || 0) - ee));
    const J = G.map((z) => Number(z.order)).filter((z) => Number.isFinite(z));
    return e.loadedSessionMessageStartOrder.value = J.length ? Math.min(...J) : null, e.loadedSessionMessageEndOrder.value = J.length ? Math.max(...J) : null, e.selectedSessionLatestAssistantOrder.value = G.filter((z) => z.role === "assistant" && Number.isFinite(Number(z.order))).reduce((z, R) => Math.max(z, Number(R.order)), -1), a(k, e.selectedSessionMessageTotal.value), ee;
  }
  function y(b, C = Date.now()) {
    const k = String(b || "").trim();
    if (!k) return;
    const P = Number(C) || Date.now();
    let H = !1;
    const G = e.sessions.value.map((ee) => ee.id !== k ? ee : (H = !0, {
      ...ee,
      updatedAt: Math.max(od(ee), P)
    }));
    H && (e.sessions.value = G.sort((ee, J) => od(J) - od(ee)));
  }
  function S(b) {
    b?.id && (e.sessions.value = e.sessions.value.map((C) => C.id === b.id ? b : C));
  }
  function x(b = "") {
    e.selectedSessionId.value = String(b || "").trim();
  }
  async function I(b = "") {
    return await BU(b);
  }
  async function E(b = {}) {
    e.selectedSessionId.value = "", b.clearCharacterError !== !1 && (t.selectedSessionCharacterError.value = ""), d(), b.persist && await I(""), b.refreshManager && await t.refreshManagerRecords("");
  }
  async function N() {
    t.reportStartupProgress(94, "refreshSessions"), e.sessions.value = await OU();
    const b = String(await FU() || "").trim();
    e.selectedSessionId.value = e.sessions.value.some((C) => C.id === b) ? b : "", b && !e.selectedSessionId.value && await I(""), await f(), await t.refreshManagerRecords(e.selectedSessionId.value), e.selectedSessionId.value && t.syncSessionCharacterContextSafely({ sessionId: e.selectedSessionId.value });
  }
  async function $(b) {
    const C = String(b || "").trim();
    if (!C) return;
    t.resetSessionPreviewState(), t.invalidateMemoryFileRecordLoad(), e.selectedSessionId.value = C, t.selectedSessionCharacterError.value = "";
    const k = e.sessions.value.find((P) => P.id === C) || null;
    t.applySessionSnapshotContext(k), await I(C), await f({
      reset: !0,
      sessionId: C
    }), await t.refreshManagerRecords(C), t.syncSessionCharacterContextSafely({
      sessionId: C,
      force: !0
    }), t.activeView.value = "chat", t.chatFocus.value = "chat", t.scrollChatToBottom(!0);
  }
  async function p(b) {
    const C = String(b || "").trim();
    if (!C) return;
    const k = e.sessions.value.find((J) => J.id === C), P = String(k?.characterKey || "").trim(), H = C === e.selectedSessionId.value, G = P ? e.sessions.value.filter((J) => J.id !== C && String(J.characterKey || "").trim() === P).slice().sort((J, z) => od(z) - od(J))[0] : null, ee = t.describeSessionTitle(k) || "这个会话";
    if (await t.confirmDeleteSession(ee) && (H && t.isRunning.value && t.abortActiveRun(), t.cancelDrawJobsForSession(C), await t.cancelAndRollbackManagersForSession(C), !!await UU(C))) {
      if (o(C), !H) {
        await N(), t.activeView.value = e.selectedSessionId.value ? "chat" : "home", e.selectedSessionId.value && (t.chatFocus.value = "chat", t.scrollChatToBottom(!0));
        return;
      }
      if (t.resetSessionPreviewState(), G?.id) {
        e.selectedSessionId.value = G.id, await I(G.id), await N(), t.activeView.value = "chat", t.chatFocus.value = "chat", t.scrollChatToBottom(!0);
        return;
      }
      if (e.selectedSessionId.value = "", t.selectedSessionCharacterError.value = "", d(), await I(""), await N(), P) {
        t.selectedCharacterPreviewKey.value = P, t.syncCharacterWorldbookState(P), t.openCharacterSettingsWorkspace();
        return;
      }
      t.activeView.value = "home";
    }
  }
  function w() {
    r = !0;
  }
  function _() {
    if (r) {
      r = !1;
      return;
    }
    e.selectedSessionId.value && f();
  }
  return {
    ...e,
    chatMessageWindow: s,
    clearLoadedSessionMessageWindow: d,
    clearSelectedSession: E,
    handleChatMessageWindowLimitChanged: _,
    loadSelectedSessionMessageWindow: f,
    persistSelectedSessionId: I,
    pruneLoadedSessionMessagesFromOrder: v,
    refreshSessionMessageCountsForSessions: u,
    refreshSessions: N,
    removeSession: p,
    selectSession: $,
    sessionFloorCount: l,
    sessionFloorLabel: i,
    setSelectedSessionId: x,
    suppressNextChatWindowLimitReload: w,
    touchSessionLocally: y,
    updateSessionRecord: S,
    upsertLoadedSessionMessage: m
  };
}
var Ane = ["title", "aria-label"], Tne = {
  key: 0,
  class: "xb-theme-icon",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.75",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, Ene = {
  key: 1,
  class: "xb-theme-glyph",
  "aria-hidden": "true"
}, Cne = /* @__PURE__ */ bn({
  __name: "TavernCornerActions",
  props: {
    dark: { type: Boolean },
    includeHome: { type: Boolean },
    includeApi: { type: Boolean },
    includeChatPreset: { type: Boolean },
    includeWorldbooks: { type: Boolean },
    includeExit: { type: Boolean },
    compact: { type: Boolean },
    homeLast: { type: Boolean }
  },
  emits: [
    "api",
    "chatPreset",
    "home",
    "toggleTheme",
    "worldbooks",
    "exit"
  ],
  setup(e, { emit: t }) {
    const n = t;
    return (r, s) => (O(), B("div", { class: Fe(["home-corner-actions", { "page-corner-actions": e.includeHome || e.compact }]) }, [
      e.includeChatPreset ? (O(), B("button", {
        key: 0,
        type: "button",
        class: "home-icon-button page-chat-preset-button",
        title: "聊天预设",
        "aria-label": "聊天预设",
        onClick: s[0] || (s[0] = (a) => n("chatPreset"))
      }, [...s[7] || (s[7] = [g("svg", {
        class: "xb-chat-preset-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [g("path", {
        "stroke-width": "1.6",
        d: "M5 5h14v10H9l-4 4V5ZM8 9h8M8 12h5"
      })], -1)])])) : Te("", !0),
      e.includeApi ? (O(), B("button", {
        key: 1,
        type: "button",
        class: "home-icon-button page-api-button",
        title: "API 配置",
        "aria-label": "API 配置",
        onClick: s[1] || (s[1] = (a) => n("api"))
      }, [...s[8] || (s[8] = [g("svg", {
        class: "xb-api-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [g("path", {
        "stroke-width": "1.6",
        d: "M9 7V3M15 7V3M7 11h10M8 7h8v5a4 4 0 0 1-8 0V7ZM12 16v5"
      })], -1)])])) : Te("", !0),
      e.includeWorldbooks ? (O(), B("button", {
        key: 2,
        type: "button",
        class: "home-icon-button page-worldbooks-button",
        title: "世界书",
        "aria-label": "世界书",
        onClick: s[2] || (s[2] = (a) => n("worldbooks"))
      }, [...s[9] || (s[9] = [g("svg", {
        class: "xb-worldbooks-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [g("path", {
        "stroke-width": "1.6",
        d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
      })], -1)])])) : Te("", !0),
      e.includeHome && !e.homeLast ? (O(), B("button", {
        key: 3,
        type: "button",
        class: "home-icon-button page-home-button",
        title: "首页",
        "aria-label": "首页",
        onClick: s[3] || (s[3] = (a) => n("home"))
      }, [...s[10] || (s[10] = [g("svg", {
        class: "xb-home-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.8",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        g("path", { d: "M3 11.5 12 4l9 7.5" }),
        g("path", { d: "M5.5 10.5V20h13v-9.5" }),
        g("path", { d: "M9.5 20v-5.5h5V20" })
      ], -1)])])) : Te("", !0),
      g("button", {
        type: "button",
        class: "home-icon-button home-theme-button",
        title: e.dark ? "切换到白天" : "切换到夜间",
        "aria-label": e.dark ? "切换到白天" : "切换到夜间",
        onClick: s[4] || (s[4] = (a) => n("toggleTheme"))
      }, [e.dark ? (O(), B("svg", Tne, [...s[11] || (s[11] = [Al('<circle cx="12" cy="12" r="4"></circle><path d="M12 2v2"></path><path d="M12 20v2"></path><path d="m4.93 4.93 1.41 1.41"></path><path d="m17.66 17.66 1.41 1.41"></path><path d="M2 12h2"></path><path d="M20 12h2"></path><path d="m6.34 17.66-1.41 1.41"></path><path d="m19.07 4.93-1.41 1.41"></path>', 9)])])) : (O(), B("span", Ene, "☾"))], 8, Ane),
      e.includeHome && e.homeLast ? (O(), B("button", {
        key: 4,
        type: "button",
        class: "home-icon-button page-home-button",
        title: "首页",
        "aria-label": "首页",
        onClick: s[5] || (s[5] = (a) => n("home"))
      }, [...s[12] || (s[12] = [g("svg", {
        class: "xb-home-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.8",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        g("path", { d: "M3 11.5 12 4l9 7.5" }),
        g("path", { d: "M5.5 10.5V20h13v-9.5" }),
        g("path", { d: "M9.5 20v-5.5h5V20" })
      ], -1)])])) : Te("", !0),
      e.includeExit ? (O(), B("button", {
        key: 5,
        type: "button",
        class: "home-icon-button home-exit-button",
        title: "退出",
        "aria-label": "退出",
        onClick: s[6] || (s[6] = (a) => n("exit"))
      }, [...s[13] || (s[13] = [g("svg", {
        class: "xb-exit-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.9",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        g("path", { d: "M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" }),
        g("path", { d: "M16 17l5-5-5-5" }),
        g("path", { d: "M21 12H9" })
      ], -1)])])) : Te("", !0)
    ], 2));
  }
}), WS = Cne, Ine = { class: "xb-page tavern-about-page" }, xne = /* @__PURE__ */ bn({
  __name: "TavernAboutPage",
  props: { dark: { type: Boolean } },
  emits: ["toggle-theme", "back"],
  setup(e) {
    return (t, n) => (O(), B("section", Ine, [
      Wn(WS, {
        compact: "",
        dark: e.dark,
        onToggleTheme: n[0] || (n[0] = (r) => t.$emit("toggle-theme"))
      }, null, 8, ["dark"]),
      g("button", {
        type: "button",
        class: "about-return-inline",
        onClick: n[1] || (n[1] = (r) => t.$emit("back"))
      }, " RETURN TO INDEX "),
      n[2] || (n[2] = Al('<section class="about-title-block"><h1 class="about-title"> 小白酒馆 </h1><div class="about-sub"> A CLEANER WAY TO ROLEPLAY </div></section><section class="manifesto-body"><p class="intro-dropcap"> 当你与角色聊到数十万字，故事不该被界面拖慢。小白酒馆重写了渲染层，长对话里依然顺滑。 </p><p>但流畅只是基础。我们真正想做的，是给角色扮演多配一位后台帮手。它不替代记忆，而是替你打理那些容易散落的线索。</p><div class="feature-block"><h3>I. 幕后代理与双面板</h3><p>舞台前，你与角色保持沉浸的互动；舞台后，一个 Agent 静静旁观对话。需要时翻转面板进入助手聊天，直接找它梳理线索、讨论伏笔设定，不必出戏去打扰剧情里的角色。</p></div><div class="feature-block"><h3>II. 自动演进的可视化世界</h3><p>代理在后台帮你整理剧情脉络、归档阶段事件。基于这套结构化数据，系统支撑起了可视化的空间地图与任务线索。你去过的地点、展开的事件，会在台面下慢慢生长。这块还在持续演进。</p></div><div class="feature-block"><h3>III. 依然是你的酒馆</h3><p>这套基建扎根在你熟悉的土壤上。角色卡、世界书、聊天预设、正则，全部直连酒馆本体。你可以在这里调取和修改旧设定，也能在请求日志里，清楚看到代理发出的每一条真实记录。</p></div><div class="feature-block quiet"><p>放下那些拥挤的参数窗口，选一张卡片，把心思还给故事本身。</p></div></section>', 2))
    ]));
  }
}), kne = xne, Mne = { class: "tavern-home xb-page home-command-center" }, Pne = { class: "home-hero home-command-hero" }, Rne = { class: "home-brand-panel" }, Nne = { class: "home-version-line" }, $ne = { class: "home-enter-desktop" }, Lne = { class: "home-enter-mobile-label" }, Dne = { class: "home-enter-mobile-title" }, One = { class: "home-command-panel home-menu-panel" }, Fne = { class: "home-menu-mobile-footer" }, Bne = /* @__PURE__ */ bn({
  __name: "TavernHomePage",
  props: {
    dark: { type: Boolean },
    hasSession: { type: Boolean },
    subtitle: {},
    characterCount: {}
  },
  emits: [
    "toggle-theme",
    "exit",
    "enter",
    "open-characters",
    "open-settings",
    "open-about"
  ],
  setup(e) {
    return (t, n) => (O(), B("section", Mne, [Wn(WS, {
      "include-exit": "",
      dark: e.dark,
      onToggleTheme: n[0] || (n[0] = (r) => t.$emit("toggle-theme")),
      onExit: n[1] || (n[1] = (r) => t.$emit("exit"))
    }, null, 8, ["dark"]), g("div", Pne, [
      g("div", Rne, [
        n[13] || (n[13] = g("p", { class: "eyebrow" }, " Private Narrative Archive ", -1)),
        n[14] || (n[14] = g("h2", null, [g("span", null, "Little White Tavern")], -1)),
        n[15] || (n[15] = g("p", null, " A quiet narrative desk where the scene, the archive, and the assistant stay in the same room. ", -1)),
        g("p", Nne, te(e.hasSession ? e.subtitle : `${e.characterCount} cards in archive`), 1)
      ]),
      g("button", {
        type: "button",
        class: "enter-btn home-enter-chat",
        onClick: n[2] || (n[2] = (r) => t.$emit("enter"))
      }, [
        g("span", $ne, te(e.hasSession ? "ENTER LAST CHAT" : "OPEN CHARACTER ARCHIVE"), 1),
        g("span", Lne, te(e.hasSession ? "RESUME SESSION" : "OPEN ARCHIVE"), 1),
        g("span", Dne, te(e.hasSession ? "继续聊天" : "选择角色"), 1),
        n[16] || (n[16] = g("span", {
          class: "home-enter-mobile-arrow",
          "aria-hidden": "true"
        }, "→", -1))
      ]),
      g("div", One, [
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[3] || (n[3] = (r) => t.$emit("open-characters"))
        }, [...n[17] || (n[17] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M16 7a4 4 0 1 1-8 0 4 4 0 0 1 8 0ZM5 21a7 7 0 0 1 14 0"
          })], -1),
          g("strong", null, "角色卡", -1),
          g("span", null, "Character Cards", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[4] || (n[4] = (r) => t.$emit("open-settings", "worldbooks"))
        }, [...n[18] || (n[18] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
          })], -1),
          g("strong", null, "世界书", -1),
          g("span", null, "World Lore Books", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[5] || (n[5] = (r) => t.$emit("open-settings", "chatPreset"))
        }, [...n[19] || (n[19] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M5 5h14v10H9l-4 4V5ZM8 9h8M8 12h5"
          })], -1),
          g("strong", null, "聊天预设", -1),
          g("span", null, "Prompt Presets", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[6] || (n[6] = (r) => t.$emit("open-settings", "assistantPreset"))
        }, [...n[20] || (n[20] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M8 6.5A3.5 3.5 0 0 1 15 6a3.5 3.5 0 0 1 1 6.85V19a2 2 0 0 1-2 2h-4a2 2 0 0 1-2-2v-6.15A3.5 3.5 0 0 1 8 6.5ZM8 14h8M10 18h4"
          })], -1),
          g("strong", null, "助手预设", -1),
          g("span", null, "Memory Manager", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[7] || (n[7] = (r) => t.$emit("open-settings", "regex"))
        }, [...n[21] || (n[21] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M10 20 14 4M6 8l-4 4 4 4M18 8l4 4-4 4"
          })], -1),
          g("strong", null, "正则", -1),
          g("span", null, "Text Filters", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[8] || (n[8] = (r) => t.$emit("open-settings", "api"))
        }, [...n[22] || (n[22] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M9 7V3M15 7V3M7 11h10M8 7h8v5a4 4 0 0 1-8 0V7ZM12 16v5"
          })], -1),
          g("strong", null, "API 配置", -1),
          g("span", null, "Model Connection", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item desktop-only",
          onClick: n[9] || (n[9] = (r) => t.$emit("open-settings", "base"))
        }, [...n[23] || (n[23] = [
          g("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [g("path", {
            "stroke-width": "1.6",
            d: "M12 12a4 4 0 1 0-4-4 4 4 0 0 0 4 4Zm-7 8a7 7 0 0 1 14 0"
          })], -1),
          g("strong", null, "基础设定", -1),
          g("span", null, "Base Setup", -1)
        ])]),
        g("button", {
          type: "button",
          class: "home-menu-item highlight desktop-only",
          onClick: n[10] || (n[10] = (r) => t.$emit("open-about"))
        }, [...n[24] || (n[24] = [g("strong", null, "关于界面", -1), g("span", null, "About", -1)])]),
        g("div", Fne, [g("button", {
          type: "button",
          class: "home-menu-item mobile-footer-item",
          onClick: n[11] || (n[11] = (r) => t.$emit("open-settings", "base"))
        }, [...n[25] || (n[25] = [g("strong", null, "基础设定", -1)])]), g("button", {
          type: "button",
          class: "home-menu-item mobile-footer-item mobile-footer-about",
          onClick: n[12] || (n[12] = (r) => t.$emit("open-about"))
        }, [...n[26] || (n[26] = [g("strong", null, "关于界面", -1)])])])
      ])
    ])]));
  }
}), Une = Bne, qne = {
  class: "tavern-contract-modal",
  tabindex: "-1"
}, Hne = { class: "tavern-contract-head" }, zne = { class: "tavern-contract-body" }, Kne = { class: "tavern-contract-tier-bar" }, Wne = ["aria-pressed", "onClick"], Gne = { class: "tavern-contract-copy" }, Vne = { class: "tavern-contract-description" }, Jne = { class: "tavern-contract-foot" }, Yne = ["disabled"], Xne = { class: "tavern-contract-note" }, jne = {
  key: 0,
  class: "tavern-contract-error"
}, Qne = /* @__PURE__ */ bn({
  __name: "TavernContractModal",
  props: {
    draftContract: {},
    dirty: { type: Boolean },
    saving: { type: Boolean },
    canSave: { type: Boolean },
    errorText: {}
  },
  emits: [
    "close",
    "toggle",
    "save"
  ],
  setup(e, { emit: t }) {
    const n = e, r = ne(() => {
      const a = /* @__PURE__ */ new Map();
      return Rb.forEach((o) => {
        const l = a.get(o.tier) || [];
        l.push(o), a.set(o.tier, l);
      }), Array.from(a.entries()).map(([o, l]) => ({
        tier: o,
        mandates: l
      }));
    }), s = ne(() => IU(n.draftContract));
    return (a, o) => (O(), B("div", {
      class: "tavern-contract-overlay",
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": "tavern-contract-title",
      onClick: o[2] || (o[2] = Kt((l) => a.$emit("close"), ["self"])),
      onKeydown: o[3] || (o[3] = Qr((l) => a.$emit("close"), ["esc"]))
    }, [g("section", qne, [
      g("header", Hne, [o[4] || (o[4] = g("div", { class: "tavern-contract-heading" }, [g("h2", { id: "tavern-contract-title" }, " 契约 "), g("p", { class: "eyebrow" }, " 玩家 — 代理人誓约 ")], -1)), g("button", {
        type: "button",
        class: "tavern-contract-close",
        "aria-label": "关闭契约",
        onClick: o[0] || (o[0] = (l) => a.$emit("close"))
      }, " × ")]),
      g("div", zne, [o[5] || (o[5] = g("p", { class: "tavern-contract-preamble" }, " 故事开始之前，定义你的代理人被允许做什么。 每一条封印的授权赋予一项特定权限。 你可以随时修改。 ", -1)), (O(!0), B(He, null, tt(r.value, (l) => (O(), B("section", {
        key: l.tier,
        class: "tavern-contract-tier"
      }, [g("div", Kne, [g("span", null, te(T(EU)[l.tier]), 1)]), (O(!0), B(He, null, tt(l.mandates, (i) => (O(), B("article", {
        key: i.key,
        class: Fe(["tavern-contract-mandate", { "is-off": !e.draftContract[i.key] }])
      }, [g("button", {
        type: "button",
        class: Fe(["tavern-contract-seal", { active: e.draftContract[i.key] }]),
        "aria-pressed": e.draftContract[i.key] ? "true" : "false",
        onClick: (u) => a.$emit("toggle", i.key)
      }, [g("span", null, te(i.icon), 1)], 10, Wne), g("div", Gne, [g("h3", null, te(i.title), 1), g("p", Vne, te(i.description), 1)])], 2))), 128))]))), 128))]),
      g("footer", Jne, [
        g("button", {
          type: "button",
          class: "tavern-contract-save",
          disabled: e.saving || !e.canSave,
          onClick: o[1] || (o[1] = (l) => a.$emit("save"))
        }, te(e.saving ? "封印中..." : "封存誓约"), 9, Yne),
        g("p", Xne, te(s.value) + " / " + te(T(Rb).length) + " 项授权已启用 ", 1),
        e.errorText ? (O(), B("p", jne, te(e.errorText), 1)) : Te("", !0)
      ])
    ])], 32));
  }
}), Zne = Qne, n8 = /* @__PURE__ */ Symbol("TavernAppUiContext");
function $o() {
  const e = Ld(n8);
  if (!e) throw new Error("tavern_app_ui_context_missing");
  return e;
}
function vc() {
  return $o().shell;
}
function r8() {
  return $o().character;
}
function kv() {
  return $o().session;
}
function s8() {
  return $o().draw;
}
function GS() {
  return $o().chat;
}
function a8() {
  return $o().manager;
}
function ere() {
  return $o().memory;
}
function o8() {
  return $o().workspace;
}
function Ei() {
  return $o().settings;
}
function Mv() {
  const e = /* @__PURE__ */ ue(/* @__PURE__ */ new Map());
  function t(o, l = !1) {
    return e.value.get(o) ?? l;
  }
  function n(o, l) {
    const i = new Map(e.value);
    i.set(o, l), e.value = i;
  }
  function r(o, l) {
    n(o, !!l.currentTarget?.open);
  }
  function s(o, l = !1) {
    n(o, !t(o, l));
  }
  function a() {
    e.value = /* @__PURE__ */ new Map();
  }
  return Sl(a), {
    isOpen: t,
    reset: a,
    setOpen: n,
    setOpenFromEvent: r,
    toggleOpen: s
  };
}
var tre = { class: "character-workspace-panel" }, nre = {
  key: 0,
  class: "character-select-error"
}, rre = { class: "character-index-panel" }, sre = { class: "index-panel-toolbar" }, are = { class: "os-search-bar" }, ore = ["value"], ire = { class: "index-meta" }, lre = [
  "data-character-card-id",
  "disabled",
  "onClick"
], ure = { class: "character-card-avatar" }, cre = ["src", "onError"], dre = { key: 1 }, fre = { class: "character-card-body" }, hre = { class: "character-card-title" }, pre = { class: "character-card-desc" }, mre = { class: "card-status-tags" }, gre = {
  key: 0,
  class: "tag active"
}, vre = {
  key: 1,
  class: "tag loading"
}, yre = {
  key: 2,
  class: "tag loading"
}, _re = {
  key: 0,
  class: "empty-note"
}, bre = {
  key: 0,
  class: "character-preview-panel dossier-view"
}, wre = { class: "dossier-header" }, Sre = { class: "dossier-identity" }, Are = { class: "dossier-title-row" }, Tre = { class: "dossier-title-actions" }, Ere = ["disabled", "title"], Cre = ["disabled"], Ire = { key: 0 }, xre = ["disabled"], kre = { class: "dossier-summary" }, Mre = { class: "dossier-details" }, Pre = { class: "data-group" }, Rre = { class: "data-row" }, Nre = { key: 0 }, $re = {
  key: 0,
  class: "greeting-current-card"
}, Lre = { class: "greeting-current-head" }, Dre = { key: 0 }, Ore = { class: "data-block greeting-current-text" }, Fre = ["open"], Bre = {
  key: 0,
  class: "greeting-choice-list"
}, Ure = ["disabled", "onClick"], qre = { class: "greeting-choice-name" }, Hre = { class: "greeting-choice-text" }, zre = {
  key: 1,
  class: "data-block"
}, Kre = {
  key: 2,
  class: "data-block"
}, Wre = {
  key: 1,
  class: "character-definition-overlay",
  role: "dialog",
  "aria-modal": "true",
  "aria-label": "角色卡详情"
}, Gre = { class: "character-definition-dialog" }, Vre = { class: "character-definition-list" }, Jre = {
  key: 2,
  class: "character-session-archive-overlay",
  role: "dialog",
  "aria-modal": "true",
  "aria-label": "会话档案"
}, Yre = { class: "character-session-archive" }, Xre = {
  key: 0,
  class: "session-archive-list"
}, jre = ["onClick"], Qre = { class: "session-archive-item-title" }, Zre = { class: "session-archive-item-meta" }, ese = {
  key: 0,
  class: "session-archive-item-summary"
}, tse = ["onClick"], nse = {
  key: 1,
  class: "session-archive-empty"
}, rse = {
  key: 3,
  class: "character-preview-panel dossier-empty"
}, sse = {
  key: 2,
  class: "character-empty"
}, ase = { class: "empty-content" }, ose = /* @__PURE__ */ bn({
  __name: "TavernCharacterWorkspacePanel",
  setup(e) {
    const { avatarAvailable: t, batchSize: n, characterWorldbookBusy: r, characterWorldbookState: s, characters: a, enterSelected: o, filteredCount: l, hiddenCount: i, liveCharacterKey: u, loadMore: d, movePreview: f, openCharacterWorldbook: m, pendingCharacterSessionKey: v, pendingError: y, pendingPreviewCharacterKey: S, refresh: x, rememberBrokenAvatar: I, searchText: E, select: N, selectFirstVisible: $, selectGreeting: p, selectLastVisible: w, selectedCharacter: _, selectedGreetingIndex: b, shortText: C, syncWorldbookState: k, visibleCharacters: P } = r8(), { removeSession: H, selectedCharacterSessions: G, selectSession: ee, sessionFloorLabel: J } = kv(), z = /* @__PURE__ */ ue(null), R = /* @__PURE__ */ ue(!1), V = /* @__PURE__ */ ue(!1), le = Mv(), ve = ne(() => {
      const q = _.value;
      return q ? [String(q.firstMessage || "").trim(), ...q.alternateGreetings || []].filter(Boolean) : [];
    }), ke = ne(() => {
      if (!ve.value.length) return "";
      const q = Math.min(Math.max(0, Number(b.value) || 0), ve.value.length - 1);
      return ve.value[q] || "";
    }), be = ne(() => ve.value.length > 1), Ne = ne(() => G.value.length), ce = ne(() => {
      const q = _.value;
      return [
        {
          label: "性格摘要",
          value: q?.personality || ""
        },
        {
          label: "情景",
          value: q?.scenario || ""
        },
        {
          label: "角色备注",
          value: q?.characterDepthPrompt || ""
        },
        {
          label: "制作者备注",
          value: q?.creatorNotes || ""
        },
        {
          label: "示例对话",
          value: q?.mesExample || ""
        }
      ];
    }), we = ne(() => !!_.value && String(S.value || "") === String(_.value.characterKey || "")), Oe = ne(() => {
      const q = s.value;
      return q ? q.boundWorldbookName && q.boundExists ? `打开 ${q.boundWorldbookName}` : q.hasEmbeddedBook ? `导入 ${q.embeddedBookName}` : "选择一本世界书绑定" : "读取角色世界书";
    }), F = ne(() => s.value?.boundExists === !0);
    function ae() {
      Ut(() => {
        const q = z.value, ye = String(_.value?.characterKey || "").trim();
        !q || !ye || Array.from(q.querySelectorAll("[data-character-card-id]")).find((oe) => oe.dataset.characterCardId === ye)?.scrollIntoView?.({
          block: "nearest",
          behavior: "smooth"
        });
      });
    }
    function Z(q) {
      E.value = q.target.value;
    }
    function ie(q) {
      q.key === "ArrowDown" ? (q.preventDefault(), f(1)) : q.key === "ArrowUp" ? (q.preventDefault(), f(-1)) : q.key === "Home" ? (q.preventDefault(), $()) : q.key === "End" ? (q.preventDefault(), w()) : q.key === "Enter" && (q.preventDefault(), de());
    }
    function me(q) {
      return `character-data:${_.value?.characterKey || "none"}:${q}`;
    }
    function Se(q) {
      return q === 0 ? "主开场白" : `备用 ${q}`;
    }
    function W(q) {
      const ye = Number(q) || 0;
      return ye ? new Date(ye).toLocaleString("zh-CN", {
        month: "2-digit",
        day: "2-digit",
        hour: "2-digit",
        minute: "2-digit"
      }) : "未知时间";
    }
    function L(q) {
      return String(q.title || q.characterName || _.value?.name || "未命名会话").trim();
    }
    function Q(q) {
      return `${W(q.updatedAt || q.createdAt)} · ${J(q)}`;
    }
    function de() {
      !_.value || v.value || (R.value = !0);
    }
    function ge() {
      R.value = !1;
    }
    function pe() {
      _.value && (V.value = !0);
    }
    function Me() {
      V.value = !1;
    }
    function Ie(q) {
      const ye = String(q || "").trim();
      ye && (ge(), ee(ye));
    }
    async function he(q, ye) {
      await H(q, ye);
    }
    return vt(() => _.value?.characterKey, (q) => {
      le.reset(), Me(), ge(), ae();
      const ye = String(q || "").trim();
      ye && k(ye);
    }, { immediate: !0 }), (q, ye) => (O(), B("div", tre, [T(y) ? (O(), B("div", nre, " 角色列表读取失败：" + te(T(y)), 1)) : Te("", !0), T(a).length ? (O(), B("section", {
      key: 1,
      class: Fe(["character-archive", { "has-character-selection": !!T(_) }])
    }, [
      g("aside", rre, [
        g("div", sre, [g("label", are, [ye[5] || (ye[5] = g("span", { class: "icon" }, "⌕", -1)), g("input", {
          value: T(E),
          type: "search",
          placeholder: "检索角色特征...",
          onInput: Z
        }, null, 40, ore)]), g("div", ire, te(T(P).length) + " / " + te(T(l)) + " 项 ", 1)]),
        g("div", {
          ref_key: "listRef",
          ref: z,
          class: "character-card-grid",
          tabindex: "0",
          "aria-label": "角色卡列表",
          onKeydown: ie
        }, [(O(!0), B(He, null, tt(T(P), (oe) => (O(), B("button", {
          key: oe.characterKey,
          type: "button",
          class: Fe(["character-card-option", {
            current: oe.characterKey === T(u),
            selected: oe.characterKey === T(_)?.characterKey,
            pending: oe.characterKey === T(v)
          }]),
          "data-character-card-id": oe.characterKey,
          disabled: !!T(v),
          onClick: (De) => T(N)(oe.characterKey)
        }, [
          ye[6] || (ye[6] = g("div", { class: "card-focus-indicator" }, null, -1)),
          g("span", ure, [T(t)(oe.avatar) ? (O(), B("img", {
            key: 0,
            src: oe.avatar,
            loading: "lazy",
            decoding: "async",
            alt: "",
            onError: (De) => T(I)(oe.avatar)
          }, null, 40, cre)) : (O(), B("span", dre, te(oe.name.slice(0, 1)), 1))]),
          g("span", fre, [
            g("span", hre, [g("strong", null, te(oe.name), 1)]),
            g("span", pre, te(T(C)(oe.description || oe.personality || oe.scenario || oe.firstMessage || "暂无数据存档。", 72)), 1),
            g("span", mre, [oe.characterKey === T(u) ? (O(), B("small", gre, "当前")) : Te("", !0), oe.characterKey === T(v) ? (O(), B("small", vre, "打开中")) : oe.characterKey === T(S) ? (O(), B("small", yre, "读取中")) : Te("", !0)])
          ])
        ], 10, lre))), 128)), T(i) ? (O(), B("button", {
          key: 0,
          type: "button",
          class: "archive-load-more character-load-more",
          onClick: ye[0] || (ye[0] = (...oe) => T(d) && T(d)(...oe))
        }, " 加载更多角色 (" + te(Math.min(T(i), T(n))) + ") ", 1)) : Te("", !0)], 544),
        T(P).length ? Te("", !0) : (O(), B("div", _re, " 没有匹配的角色。 "))
      ]),
      T(_) ? (O(), B("main", bre, [g("div", wre, [g("div", Sre, [g("div", Are, [g("h3", null, te(T(_).name), 1), g("div", Tre, [
        g("button", {
          type: "button",
          class: "os-system-act-btn character-definition-button",
          title: "角色卡详情",
          "aria-label": "角色卡详情",
          onClick: pe
        }, [...ye[7] || (ye[7] = [g("svg", {
          class: "character-definition-icon",
          "aria-hidden": "true",
          fill: "none",
          stroke: "currentColor",
          viewBox: "0 0 24 24"
        }, [g("path", {
          "stroke-linecap": "round",
          "stroke-linejoin": "round",
          "stroke-width": "1.7",
          d: "M5.5 4.7c1.8-.9 4.1-.9 6.5.3 2.4-1.2 4.7-1.2 6.5-.3v14.4c-1.8-.8-4.1-.7-6.5.5-2.4-1.2-4.7-1.3-6.5-.5V4.7Z"
        }), g("path", {
          "stroke-linecap": "round",
          "stroke-width": "1.3",
          d: "M12 5v14.2M8 8.5h1.9M8 11.5h1.9M14.1 8.5H16M14.1 11.5H16"
        })], -1)])]),
        g("button", {
          type: "button",
          class: Fe(["os-system-act-btn character-worldbook-button", {
            "is-loading": T(r),
            "is-bound": F.value
          }]),
          disabled: !!T(v) || T(r),
          title: Oe.value,
          "aria-label": "角色世界书",
          onClick: ye[1] || (ye[1] = (...oe) => T(m) && T(m)(...oe))
        }, [...ye[8] || (ye[8] = [g("svg", {
          class: "character-worldbook-icon",
          "aria-hidden": "true",
          fill: "none",
          stroke: "currentColor",
          viewBox: "0 0 24 24"
        }, [g("path", {
          "stroke-width": "1.6",
          d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
        })], -1)])], 10, Ere),
        g("button", {
          type: "button",
          class: "os-system-act-btn session-archive-button",
          disabled: !!T(v),
          onClick: de
        }, [ye[9] || (ye[9] = ku(" 会话档案 ", -1)), Ne.value ? (O(), B("span", Ire, te(Ne.value), 1)) : Te("", !0)], 8, Cre),
        g("button", {
          type: "button",
          class: Fe(["os-system-act-btn enter-chat-button", { "is-loading": T(_).characterKey === T(v) }]),
          disabled: !!T(v),
          onClick: ye[2] || (ye[2] = (...oe) => T(o) && T(o)(...oe))
        }, te(T(_).characterKey === T(v) ? "新建中..." : "新建聊天"), 11, xre)
      ])]), g("div", kre, te(we.value ? "正在读取角色卡..." : T(_).description || T(_).personality || T(_).scenario || "暂无角色描述。"), 1)])]), g("div", Mre, [g("dl", Pre, [g("div", Rre, [g("dt", null, [ye[10] || (ye[10] = ku("开场白 ", -1)), ve.value.length > 1 ? (O(), B("span", Nre, te(T(b) + 1) + " / " + te(ve.value.length), 1)) : Te("", !0)]), g("dd", null, [ve.value.length ? (O(), B("div", $re, [
        g("div", Lre, [g("strong", null, te(Se(T(b))), 1), ve.value.length > 1 ? (O(), B("span", Dre, te(ve.value.length) + " 个可选", 1)) : Te("", !0)]),
        g("div", Ore, te(ke.value), 1),
        g("details", {
          open: T(le).isOpen(me("greetings")),
          class: Fe(["data-section greeting-picker", { "is-empty": !be.value }]),
          onToggle: ye[3] || (ye[3] = (oe) => T(le).setOpenFromEvent(me("greetings"), oe))
        }, [ye[11] || (ye[11] = g("summary", { class: "greeting-section-title" }, " 切换开场白 ", -1)), be.value && T(le).isOpen(me("greetings")) ? (O(), B("div", Bre, [(O(!0), B(He, null, tt(ve.value, (oe, De) => (O(), B("button", {
          key: `${T(_).characterKey}-greeting-${De}`,
          type: "button",
          class: Fe(["greeting-choice", { selected: De === T(b) }]),
          disabled: !!T(v),
          onClick: (Je) => T(p)(De)
        }, [g("span", qre, te(Se(De)), 1), g("span", Hre, te(T(C)(oe, 120)), 1)], 10, Ure))), 128))])) : Te("", !0)], 42, Fre)
      ])) : we.value ? (O(), B("div", zre, " 正在读取角色卡... ")) : (O(), B("div", Kre, " 未填写 "))])])])])])) : Te("", !0),
      V.value && T(_) ? (O(), B("div", Wre, [g("section", Gre, [g("header", null, [g("div", null, [ye[12] || (ye[12] = g("strong", null, "角色卡详情", -1)), g("span", null, te(T(_).name), 1)]), g("button", {
        type: "button",
        class: "session-archive-close character-definition-close",
        "aria-label": "关闭角色卡详情",
        onClick: Me
      })]), g("dl", Vre, [(O(!0), B(He, null, tt(ce.value, (oe) => (O(), B("div", {
        key: oe.label,
        class: "character-definition-section"
      }, [g("dt", null, te(oe.label), 1), g("dd", { class: Fe({ "is-empty": !oe.value }) }, te(oe.value || (we.value ? "正在读取角色卡..." : "未填写")), 3)]))), 128))])])])) : Te("", !0),
      R.value && T(_) ? (O(), B("div", Jre, [g("section", Yre, [g("header", null, [g("div", null, [ye[13] || (ye[13] = g("strong", null, "会话档案", -1)), g("span", null, te(T(_).name), 1)]), g("button", {
        type: "button",
        class: "session-archive-close",
        "aria-label": "关闭会话档案",
        onClick: ge
      })]), T(G).length ? (O(), B("div", Xre, [(O(!0), B(He, null, tt(T(G), (oe) => (O(), B("div", {
        key: oe.id,
        class: "session-archive-item"
      }, [g("button", {
        type: "button",
        class: "session-archive-open",
        onClick: (De) => Ie(oe.id)
      }, [
        g("span", Qre, te(L(oe)), 1),
        g("span", Zre, te(Q(oe)), 1),
        oe.summary ? (O(), B("span", ese, te(T(C)(oe.summary, 96)), 1)) : Te("", !0)
      ], 8, jre), g("button", {
        type: "button",
        class: "session-archive-delete",
        title: "删除会话",
        "aria-label": "删除会话",
        onClick: (De) => he(oe.id, De)
      }, [...ye[14] || (ye[14] = [g("svg", {
        "aria-hidden": "true",
        viewBox: "0 0 24 24"
      }, [
        g("path", { d: "M3 6h18" }),
        g("path", { d: "M8 6V4h8v2" }),
        g("path", { d: "M6 6l1 15h10l1-15" })
      ], -1)])], 8, tse)]))), 128))])) : (O(), B("div", nse, " 这张角色卡还没有旧会话。 "))])])) : Te("", !0),
      T(_) ? Te("", !0) : (O(), B("main", rse, [...ye[15] || (ye[15] = [g("div", { class: "cyber-placeholder" }, [g("div", { class: "ring" }), g("span", null, "选择角色")], -1)])]))
    ], 2)) : (O(), B("section", sse, [g("div", ase, [
      ye[16] || (ye[16] = g("h2", null, "暂无角色卡", -1)),
      ye[17] || (ye[17] = g("p", null, "本地缓存在酒馆内未找到可用角色，请在主控端加载后重试。", -1)),
      g("button", {
        type: "button",
        class: "os-sys-button primary",
        onClick: ye[4] || (ye[4] = (...oe) => T(x) && T(x)(...oe))
      }, " 重新唤醒数据 ")
    ])]))]));
  }
}), i8 = ose, ise = { class: "panel step-panel preset-workspace" }, lse = { class: "panel-head preset-page-head" }, use = { class: "panel-pills" }, cse = {
  key: 0,
  class: "pill warning"
}, dse = { class: "preset-command-bar assistant-preset-command-bar" }, fse = { class: "assistant-preset-picker" }, hse = ["value"], pse = {
  key: 0,
  value: ""
}, mse = ["value"], gse = { class: "assistant-preset-toolstrip" }, vse = [
  "title",
  "aria-label",
  "disabled"
], yse = {
  key: 0,
  viewBox: "0 0 24 24",
  "aria-hidden": "true"
}, _se = {
  key: 1,
  viewBox: "0 0 24 24",
  "aria-hidden": "true"
}, bse = {
  key: 2,
  viewBox: "0 0 24 24",
  "aria-hidden": "true"
}, wse = {
  key: 3,
  viewBox: "0 0 24 24",
  "aria-hidden": "true"
}, Sse = ["disabled"], Ase = ["disabled"], Tse = {
  key: 0,
  class: "preset-status-line"
}, Ese = { class: "assistant-preset-studio" }, Cse = { class: "assistant-preset-item-list archive-item-list" }, Ise = ["onClick"], xse = { class: "assistant-preset-detail-panel archive-editor-panel" }, kse = { class: "assistant-preset-line-head" }, Mse = {
  key: 0,
  class: "assistant-preset-detail-fields"
}, Pse = ["value"], Rse = {
  key: 1,
  type: "button",
  class: "assistant-preset-empty-add",
  disabled: ""
}, Nse = /* @__PURE__ */ bn({
  __name: "TavernAssistantPresetSettingsPanel",
  setup(e) {
    const t = Ei(), n = vc(), { activeAssistantPresetId: r, activeSettingsWorkspace: s, assistantPreset: a, assistantPresetDirty: o, assistantPresetItems: l, assistantPresets: i, assistantPresetSaveFeedback: u, assistantPresetStatus: d, createAssistantPreset: f, deleteCurrentAssistantPreset: m, importAssistantPreset: v, saveCurrentAssistantPreset: y, selectAssistantPreset: S, selectAssistantPresetItem: x, selectedAssistantPresetItem: I, shortText: E, updateAssistantPresetPatch: N, updateSelectedAssistantPresetItem: $ } = t, p = ne(() => String(r.value || a.value.id || "").trim()), w = ne(() => i.value.find((J) => J.id === p.value) || null), _ = /* @__PURE__ */ ue(null), b = ne(() => {
      const J = u.value.status;
      return J === "saving" ? "正在保存" : J === "success" ? "已保存" : J === "error" ? u.value.error || "保存失败" : "保存";
    }), C = ne(() => ({
      "is-saving": u.value.status === "saving",
      "is-success": u.value.status === "success",
      "is-error": u.value.status === "error"
    })), k = ne(() => !o.value || u.value.status === "saving");
    async function P() {
      const J = String(a.value.name || "").trim() || "助手预设", z = await n.promptTavernDialog({
        title: "重命名助手预设",
        message: "输入预设名称：",
        defaultValue: J,
        confirmText: "保存"
      });
      if (z === null) return;
      const R = String(z || "").trim();
      !R || R === J || N({ name: R });
    }
    function H() {
      _.value?.click();
    }
    async function G(J) {
      const z = J.target, R = z?.files?.[0];
      if (R)
        try {
          const V = await R.text();
          await v(JSON.parse(V));
        } catch (V) {
          const le = V instanceof Error ? V.message : String(V || "导入失败");
          await n.alertTavernDialog({
            title: "导入失败",
            message: le || "导入失败"
          });
        } finally {
          z && (z.value = "");
        }
    }
    function ee() {
      const J = String(a.value.name || "assistant-preset").trim() || "assistant-preset", z = {
        kind: "littlewhitebox-assistant-preset",
        version: 1,
        exportedAt: (/* @__PURE__ */ new Date()).toISOString(),
        preset: a.value
      }, R = new Blob([JSON.stringify(z, null, 2)], { type: "application/json;charset=utf-8" }), V = URL.createObjectURL(R), le = document.createElement("a");
      le.href = V, le.download = `${J.replace(/[\\/:*?"<>|]+/g, "-").replace(/\s+/g, " ").trim() || "assistant-preset"}.json`, document.body.appendChild(le), le.click(), le.remove(), URL.revokeObjectURL(V);
    }
    return (J, z) => er((O(), B("div", ise, [
      g("div", lse, [z[5] || (z[5] = g("div", null, [g("h2", null, "助手预设")], -1)), g("div", use, [T(o) ? (O(), B("span", cse, "未保存")) : Te("", !0)])]),
      g("div", dse, [g("label", fse, [g("select", {
        value: p.value,
        onChange: z[0] || (z[0] = (R) => T(S)(R.target.value))
      }, [T(i).length ? Te("", !0) : (O(), B("option", pse, " 没有可用助手预设 ")), (O(!0), B(He, null, tt(T(i), (R) => (O(), B("option", {
        key: R.id,
        value: R.id
      }, te(R.name) + te(R.isBuiltIn ? " · 内置" : ""), 9, mse))), 128))], 40, hse)]), g("div", gse, [
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "改名",
          "aria-label": "改名",
          onClick: P
        }, [...z[6] || (z[6] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [g("path", { d: "M12 20h9" }), g("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])]),
        g("button", {
          type: "button",
          class: Fe(["assistant-preset-tool icon-button", C.value]),
          title: b.value,
          "aria-label": b.value,
          disabled: k.value,
          onClick: z[1] || (z[1] = (...R) => T(y) && T(y)(...R))
        }, [T(u).status === "saving" ? (O(), B("svg", yse, [...z[7] || (z[7] = [g("path", { d: "M12 3a9 9 0 1 1-8.2 5.3" }, null, -1)])])) : T(u).status === "success" ? (O(), B("svg", _se, [...z[8] || (z[8] = [g("path", { d: "M20 6 9 17l-5-5" }, null, -1)])])) : T(u).status === "error" ? (O(), B("svg", bse, [...z[9] || (z[9] = [
          g("path", { d: "M12 9v4" }, null, -1),
          g("path", { d: "M12 17h.01" }, null, -1),
          g("path", { d: "M10.3 4.2 2.8 17a2 2 0 0 0 1.7 3h15a2 2 0 0 0 1.7-3L13.7 4.2a2 2 0 0 0-3.4 0Z" }, null, -1)
        ])])) : (O(), B("svg", wse, [...z[10] || (z[10] = [
          g("path", { d: "M5 21h14a1 1 0 0 0 1-1V7.5L16.5 4H5a1 1 0 0 0-1 1v15a1 1 0 0 0 1 1Z" }, null, -1),
          g("path", { d: "M8 21v-7h8v7" }, null, -1),
          g("path", { d: "M8 4v5h7" }, null, -1)
        ])]))], 10, vse),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "删除",
          "aria-label": "删除",
          disabled: !w.value || w.value.id === T("littlewhitebox-assistant-default"),
          onClick: z[2] || (z[2] = (...R) => T(m) && T(m)(...R))
        }, [...z[11] || (z[11] = [Al('<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M19 6l-1 14a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1L5 6"></path><path d="M10 11v6"></path><path d="M14 11v6"></path></svg>', 1)])], 8, Sse),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "新增",
          "aria-label": "新增",
          onClick: z[3] || (z[3] = (...R) => T(f) && T(f)(...R))
        }, [...z[12] || (z[12] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [g("path", { d: "M12 5v14" }), g("path", { d: "M5 12h14" })], -1)])]),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "导入",
          "aria-label": "导入",
          onClick: H
        }, [...z[13] || (z[13] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M12 4v11" }),
          g("path", { d: "m7 11 5 5 5-5" }),
          g("path", { d: "M4 20h16" })
        ], -1)])]),
        g("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "导出",
          "aria-label": "导出",
          disabled: !p.value,
          onClick: ee
        }, [...z[14] || (z[14] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M12 20V9" }),
          g("path", { d: "m17 13-5-5-5 5" }),
          g("path", { d: "M4 20h16" })
        ], -1)])], 8, Ase),
        g("input", {
          ref_key: "importInputRef",
          ref: _,
          class: "assistant-preset-import-input",
          type: "file",
          accept: "application/json,.json",
          onChange: G
        }, null, 544)
      ])]),
      T(d) ? (O(), B("div", Tse, [g("span", null, te(T(d)), 1)])) : Te("", !0),
      g("div", Ese, [g("aside", Cse, [z[15] || (z[15] = g("div", { class: "assistant-preset-nav-head" }, [g("strong", null, "维护规则")], -1)), (O(!0), B(He, null, tt(T(l), (R) => (O(), B("button", {
        key: R.id,
        type: "button",
        class: Fe(["assistant-preset-nav-row", { selected: T(I)?.id === R.id }]),
        onClick: (V) => T(x)(R.id)
      }, [g("strong", null, te(R.label), 1), g("small", null, te(T(E)(R.content || "未填写职责。", 54)), 1)], 10, Ise))), 128))]), g("section", xse, [g("div", kse, [g("div", null, [g("strong", null, te(T(I)?.label || "维护规则"), 1)])]), T(I) ? (O(), B("div", Mse, [g("label", null, [z[16] || (z[16] = g("span", null, "职责说明", -1)), g("textarea", {
        value: T(I).content,
        rows: "12",
        placeholder: "写这条维护规则应该要求什么、保留什么、避免什么。",
        onInput: z[4] || (z[4] = (R) => T($)(R.target.value))
      }, null, 40, Pse)])])) : (O(), B("button", Rse, " 没有可编辑规则 "))])])
    ], 512)), [[rc, T(s) === "assistantPreset"]]);
  }
}), l8 = Nse, $se = { class: "panel step-panel base-settings-workspace" }, Lse = {
  key: 0,
  class: "preset-status-line"
}, Dse = { class: "base-settings-scroll" }, Ose = { class: "base-settings-section" }, Fse = ["value", "disabled"], Bse = {
  key: 0,
  value: ""
}, Use = ["value"], qse = { class: "base-user-detail" }, Hse = { class: "base-user-avatar" }, zse = ["src", "alt"], Kse = { key: 1 }, Wse = { class: "base-user-copy" }, Gse = { class: "base-settings-section" }, Vse = { class: "base-setting-row" }, Jse = ["value", "disabled"], Yse = { class: "base-setting-row" }, Xse = ["value", "disabled"], jse = { class: "base-settings-section" }, Qse = { class: "base-setting-row base-setting-row--controller" }, Zse = {
  class: "xb-workspace-controller chat-font-size-controller",
  "aria-label": "聊天字体"
}, eae = ["disabled", "onClick"], tae = /* @__PURE__ */ bn({
  __name: "TavernBaseSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, baseSettingsLoading: n, baseSettingsSaving: r, baseSettingsStatus: s, currentTavernUser: a, displaySettings: o, shortText: l, switchingTavernUserId: i, switchTavernUser: u, tavernUsers: d, updateDisplaySettingsPatch: f } = Ei(), m = ne(() => a.value?.name || "未选择 USER"), v = ne(() => a.value?.description || "聊天上下文会沿用这位 USER 的身份与头像。"), y = ne(() => a.value ? x(a.value) : "U"), S = ne(() => a.value?.id || d.value.find((w) => w.active)?.id || "");
    function x(w) {
      return (String(w.name || w.id || "U").trim().slice(0, 1) || "U").toUpperCase();
    }
    function I(w) {
      const _ = String(w || "").trim();
      !_ || _ === S.value || u(_);
    }
    function E(w) {
      const _ = Number(w);
      Number.isFinite(_) && f({ hiddenOutsideCount: _ });
    }
    function N(w) {
      const _ = Number(w);
      Number.isFinite(_) && f({ loadBatchSize: _ });
    }
    const $ = {
      small: "小",
      medium: "中",
      large: "大"
    };
    function p(w) {
      o.value.chatFontSize !== w && f({ chatFontSize: w });
    }
    return (w, _) => er((O(), B("div", $se, [
      _[10] || (_[10] = g("div", { class: "panel-head preset-page-head" }, [g("div", null, [g("h2", null, "基础设定")])], -1)),
      T(s) ? (O(), B("div", Lse, [g("span", null, te(T(s)), 1)])) : Te("", !0),
      g("div", Dse, [
        g("section", Ose, [
          _[3] || (_[3] = g("h3", null, "USER身份切换", -1)),
          g("select", {
            class: "base-user-select",
            value: S.value,
            disabled: T(n) || !!T(i) || !T(d).length,
            onChange: _[0] || (_[0] = (b) => I(b.target.value))
          }, [T(d).length ? Te("", !0) : (O(), B("option", Bse, te(T(n) ? "正在读取 USER 列表" : "没有可切换的 USER"), 1)), (O(!0), B(He, null, tt(T(d), (b) => (O(), B("option", {
            key: b.id,
            value: b.id
          }, te(b.name), 9, Use))), 128))], 40, Fse),
          g("div", qse, [g("div", Hse, [T(a)?.avatarUrl ? (O(), B("img", {
            key: 0,
            src: T(a).avatarUrl,
            alt: m.value
          }, null, 8, zse)) : (O(), B("span", Kse, te(y.value), 1))]), g("div", Wse, [g("strong", null, te(m.value), 1), g("p", null, te(T(l)(v.value, 120)), 1)])])
        ]),
        g("section", Gse, [
          _[6] || (_[6] = g("h3", null, "楼层隐藏", -1)),
          g("div", Vse, [_[4] || (_[4] = g("div", null, [g("strong", null, "显示楼层"), g("span", null, "数字外楼层将隐藏")], -1)), g("input", {
            value: T(o).hiddenOutsideCount,
            type: "number",
            min: "1",
            max: "20",
            step: "1",
            disabled: T(r),
            onChange: _[1] || (_[1] = (b) => E(b.target.value))
          }, null, 40, Jse)]),
          g("div", Yse, [_[5] || (_[5] = g("div", null, [g("strong", null, "翻旧楼"), g("span", null, "每次加载楼数")], -1)), g("input", {
            value: T(o).loadBatchSize,
            type: "number",
            min: "5",
            max: "50",
            step: "5",
            disabled: T(r),
            onChange: _[2] || (_[2] = (b) => N(b.target.value))
          }, null, 40, Xse)])
        ]),
        g("section", jse, [_[8] || (_[8] = g("h3", null, "字体", -1)), g("div", Qse, [_[7] || (_[7] = g("div", null, [g("strong", null, "聊天字体"), g("span", null, "只影响聊天正文与输入框")], -1)), g("div", Zse, [(O(!0), B(He, null, tt(T(DL), (b) => (O(), B("button", {
          key: b,
          type: "button",
          class: Fe(["xb-layout-button", { "is-active": T(o).chatFontSize === b }]),
          disabled: T(r),
          onClick: (C) => p(b)
        }, te($[b]), 11, eae))), 128))])])]),
        _[9] || (_[9] = g("section", { class: "base-settings-section" }, [g("h3", null, "上下文管理"), g("div", { class: "base-context-block" }, [g("strong", null, "当前暂不支持调节"), g("p", null, "超过 20 楼时触发压缩并保留最近 10 楼；删楼后窗口内不足 5 楼时，回退窗口补到 10 楼；窗口外记忆由契约中的记忆系统处理。")])], -1))
      ])
    ], 512)), [[rc, T(t) === "base"]]);
  }
}), u8 = tae;
function Pv(e) {
  const t = /* @__PURE__ */ ue(!1);
  if (typeof window > "u" || typeof window.matchMedia != "function") return t;
  const n = window.matchMedia(e), r = () => {
    t.value = n.matches;
  };
  return r(), typeof n.addEventListener == "function" ? (n.addEventListener("change", r), Sl(() => n.removeEventListener("change", r))) : (n.addListener(r), Sl(() => n.removeListener(r))), t;
}
var nae = { class: "panel-head preset-page-head" }, rae = { class: "panel-pills" }, sae = {
  key: 0,
  class: "pill warning"
}, aae = { class: "pill" }, oae = { class: "preset-command-bar" }, iae = { class: "preset-source-select" }, lae = ["value"], uae = {
  key: 0,
  value: ""
}, cae = ["value"], dae = { class: "settings-toolstrip" }, fae = ["disabled"], hae = [
  "title",
  "aria-label",
  "disabled"
], pae = {
  key: 0,
  viewBox: "0 0 24 24",
  "aria-hidden": "true"
}, mae = {
  key: 1,
  viewBox: "0 0 24 24",
  "aria-hidden": "true"
}, gae = {
  key: 2,
  viewBox: "0 0 24 24",
  "aria-hidden": "true"
}, vae = {
  key: 3,
  viewBox: "0 0 24 24",
  "aria-hidden": "true"
}, yae = {
  key: 0,
  class: "preset-status-line"
}, _ae = { class: "preset-studio" }, bae = { class: "preset-edit-main prompt-sequence-panel" }, wae = { class: "preset-form-grid prompt-sequence-summary" }, Sae = ["value"], Aae = ["value"], Tae = { class: "archive-toolbar preset-filterbar" }, Eae = { class: "archive-search" }, Cae = { class: "prompt-manager-list" }, Iae = {
  key: 0,
  class: "inline-empty-note"
}, xae = ["onClick"], kae = ["onClick"], Mae = { class: "prompt-row-main" }, Pae = { class: "prompt-row-actions" }, Rae = ["title", "aria-label"], Nae = ["onClick"], $ae = ["disabled", "onClick"], Lae = ["disabled", "onClick"], Dae = [
  "checked",
  "disabled",
  "onChange"
], Oae = {
  key: 0,
  class: "preset-preview-panel prompt-detail-panel prompt-editor-panel"
}, Fae = { class: "preset-preview-head" }, Bae = {
  key: 0,
  class: "prompt-detail-form prompt-editor-form"
}, Uae = ["value"], qae = ["value"], Hae = { class: "preset-text-field" }, zae = ["value", "disabled"], Kae = {
  key: 0,
  class: "muted compact"
}, Wae = {
  key: 1,
  class: "empty-note"
}, Gae = /* @__PURE__ */ bn({
  __name: "TavernChatPresetSettingsPanel",
  setup(e) {
    const { activePromptOrderLabel: t, activeSettingsWorkspace: n, canEditPromptOrder: r, chatPresetOptions: s, discardPresetChanges: a, filteredPromptEditorRows: o, hiddenPromptCount: l, movePromptRow: i, preset: u, presetDirty: d, presetRows: f, presetSaveFeedback: m, presetStatus: v, presetTotalChars: y, PROMPT_EDITOR_BATCH_SIZE: S, promptEditorRows: x, promptRoleDisplay: I, promptRowIndex: E, promptSearchText: N, promptVisibleLimit: $, saveCurrentPreset: p, selectChatPresetFromHost: w, selectedPresetSourceId: _, selectedPromptIdentifier: b, selectedPromptRow: C, togglePromptRow: k, updatePromptByIdentifier: P, visiblePromptEditorRows: H } = Ei(), G = ne(() => String(_.value || u.value.promptManager?.name || u.value.name || "").trim()), ee = /* @__PURE__ */ ue(!1), J = Pv("(max-width: 560px)"), z = ne(() => n.value === "chatPreset" && (!J.value || ee.value)), R = ne(() => {
      const Ne = m.value.status;
      return Ne === "saving" ? "正在保存" : Ne === "success" ? "已保存" : Ne === "error" ? m.value.error || "保存失败" : "保存";
    }), V = ne(() => ({
      "is-saving": m.value.status === "saving",
      "is-success": m.value.status === "success",
      "is-error": m.value.status === "error"
    })), le = ne(() => !r.value || !d.value || m.value.status === "saving");
    function ve(Ne) {
      b.value = Ne, ee.value = !0;
    }
    function ke() {
      ee.value = !1;
    }
    function be(Ne) {
      return Ne === "assistant" ? "A" : Ne === "user" ? "U" : "S";
    }
    return vt(n, (Ne) => {
      Ne !== "chatPreset" && (ee.value = !1);
    }), (Ne, ce) => er((O(), B("div", { class: Fe(["panel step-panel preset-workspace", { "is-mobile-editor-open": ee.value }]) }, [
      g("div", nae, [ce[9] || (ce[9] = g("div", null, [g("h2", null, "聊天预设")], -1)), g("div", rae, [T(d) ? (O(), B("span", sae, "未保存")) : Te("", !0), g("span", aae, te(T(f).length) + " 条 · " + te(T(y)) + " 字", 1)])]),
      g("div", oae, [g("label", iae, [g("select", {
        value: G.value,
        onChange: ce[0] || (ce[0] = (we) => T(w)(we.target.value))
      }, [T(s).length ? Te("", !0) : (O(), B("option", uae, " 没有可用预设 ")), (O(!0), B(He, null, tt(T(s), (we) => (O(), B("option", {
        key: we.name,
        value: we.name
      }, te(we.label), 9, cae))), 128))], 40, lae)]), g("div", dae, [g("button", {
        type: "button",
        class: "settings-icon-tool",
        title: "放弃修改",
        "aria-label": "放弃修改",
        disabled: !T(d),
        onClick: ce[1] || (ce[1] = (...we) => T(a) && T(a)(...we))
      }, [...ce[10] || (ce[10] = [g("svg", {
        viewBox: "0 0 24 24",
        "aria-hidden": "true"
      }, [g("path", { d: "M3 7v6h6" }), g("path", { d: "M21 17a8 8 0 0 0-13.7-5.7L3 15" })], -1)])], 8, fae), g("button", {
        type: "button",
        class: Fe(["settings-icon-tool", V.value]),
        title: R.value,
        "aria-label": R.value,
        disabled: le.value,
        onClick: ce[2] || (ce[2] = (...we) => T(p) && T(p)(...we))
      }, [T(m).status === "saving" ? (O(), B("svg", pae, [...ce[11] || (ce[11] = [g("path", { d: "M12 3a9 9 0 1 1-8.2 5.3" }, null, -1)])])) : T(m).status === "success" ? (O(), B("svg", mae, [...ce[12] || (ce[12] = [g("path", { d: "M20 6 9 17l-5-5" }, null, -1)])])) : T(m).status === "error" ? (O(), B("svg", gae, [...ce[13] || (ce[13] = [
        g("path", { d: "M12 9v4" }, null, -1),
        g("path", { d: "M12 17h.01" }, null, -1),
        g("path", { d: "M10.3 4.2 2.8 17a2 2 0 0 0 1.7 3h15a2 2 0 0 0 1.7-3L13.7 4.2a2 2 0 0 0-3.4 0Z" }, null, -1)
      ])])) : (O(), B("svg", vae, [...ce[14] || (ce[14] = [
        g("path", { d: "M5 21h14a1 1 0 0 0 1-1V7.5L16.5 4H5a1 1 0 0 0-1 1v15a1 1 0 0 0 1 1Z" }, null, -1),
        g("path", { d: "M8 21v-7h8v7" }, null, -1),
        g("path", { d: "M8 4v5h7" }, null, -1)
      ])]))], 10, hae)])]),
      T(v) ? (O(), B("div", yae, [g("span", null, te(T(v)), 1)])) : Te("", !0),
      g("div", _ae, [g("section", bae, [
        g("div", wae, [g("label", null, [ce[15] || (ce[15] = g("span", null, "当前来源", -1)), g("input", {
          value: T(u).promptManager?.name || "",
          readonly: ""
        }, null, 8, Sae)]), g("label", null, [ce[16] || (ce[16] = g("span", null, "顺序", -1)), g("input", {
          value: T(t),
          readonly: ""
        }, null, 8, Aae)])]),
        g("div", Tae, [g("label", Eae, [ce[17] || (ce[17] = g("span", null, "检索条目", -1)), er(g("input", {
          "onUpdate:modelValue": ce[3] || (ce[3] = (we) => /* @__PURE__ */ kn(N) ? N.value = we : null),
          type: "search",
          placeholder: "按名称、消息身份或内容搜索"
        }, null, 512), [[sa, T(N)]])]), g("p", null, " 显示 " + te(Math.min(T(o).length, T($))) + " / " + te(T(o).length) + "，共 " + te(T(x).length) + " 条 ", 1)]),
        g("div", Cae, [T(H).length ? Te("", !0) : (O(), B("div", Iae, " 没有匹配的提示词条目。 ")), (O(!0), B(He, null, tt(T(H), (we) => (O(), B("div", {
          key: we.identifier,
          class: Fe(["prompt-manager-row", {
            selected: T(b) === we.identifier,
            disabled: !we.enabled,
            marker: we.marker
          }]),
          onClick: (Oe) => b.value = we.identifier
        }, [
          g("button", {
            type: "button",
            class: "prompt-row-index",
            title: "选择",
            onClick: Kt((Oe) => b.value = we.identifier, ["stop"])
          }, te(T(E)(we.identifier) + 1), 9, kae),
          g("div", Mae, [g("strong", null, te(we.name), 1), g("small", null, te(T(I)(we.role)), 1)]),
          g("div", Pae, [
            g("span", {
              class: "prompt-role-badge",
              title: T(I)(we.role),
              "aria-label": T(I)(we.role)
            }, te(be(we.role)), 9, Rae),
            g("button", {
              type: "button",
              class: "prompt-edit-button",
              title: "编辑",
              "aria-label": "编辑",
              onClick: Kt((Oe) => ve(we.identifier), ["stop"])
            }, [...ce[18] || (ce[18] = [g("svg", {
              viewBox: "0 0 24 24",
              "aria-hidden": "true"
            }, [g("path", { d: "M12 20h9" }), g("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])], 8, Nae),
            g("button", {
              type: "button",
              title: "上移",
              class: "prompt-move-button",
              disabled: !T(r) || !!T(N) || T(E)(we.identifier) === 0,
              onClick: Kt((Oe) => T(i)(we.identifier, -1), ["stop"])
            }, " ↑ ", 8, $ae),
            g("button", {
              type: "button",
              title: "下移",
              class: "prompt-move-button",
              disabled: !T(r) || !!T(N) || T(E)(we.identifier) === T(x).length - 1,
              onClick: Kt((Oe) => T(i)(we.identifier, 1), ["stop"])
            }, " ↓ ", 8, Lae),
            g("label", {
              class: "prompt-toggle",
              title: "启用",
              onClick: ce[4] || (ce[4] = Kt(() => {
              }, ["stop"]))
            }, [g("input", {
              type: "checkbox",
              checked: we.enabled,
              disabled: !T(r),
              onChange: (Oe) => T(k)(we.identifier, Oe.target.checked)
            }, null, 40, Dae)])
          ])
        ], 10, xae))), 128))]),
        T(l) ? (O(), B("button", {
          key: 0,
          type: "button",
          class: "archive-load-more",
          onClick: ce[5] || (ce[5] = (we) => $.value += T(S))
        }, " 再显示 " + te(Math.min(T(l), T(S))) + " 条 ", 1)) : Te("", !0)
      ]), z.value ? (O(), B("aside", Oae, [g("div", Fae, [
        g("strong", null, te(T(C)?.name || "提示词条目"), 1),
        g("span", null, te(T(I)(String(T(C)?.role || "system"))), 1),
        T(J) ? (O(), B("button", {
          key: 0,
          type: "button",
          class: "prompt-editor-close",
          title: "关闭",
          "aria-label": "关闭",
          onClick: ke
        }, [...ce[19] || (ce[19] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [g("path", { d: "M6 6l12 12" }), g("path", { d: "M18 6 6 18" })], -1)])])) : Te("", !0)
      ]), T(C) ? (O(), B("div", Bae, [
        g("label", null, [ce[20] || (ce[20] = g("span", null, "名称", -1)), g("input", {
          value: T(C).name,
          onInput: ce[6] || (ce[6] = (we) => T(P)(T(C).identifier, { name: we.target.value }))
        }, null, 40, Uae)]),
        g("label", null, [ce[22] || (ce[22] = g("span", null, "消息身份", -1)), g("select", {
          value: T(C).role,
          onChange: ce[7] || (ce[7] = (we) => T(P)(T(C).identifier, { role: we.target.value }))
        }, [...ce[21] || (ce[21] = [
          g("option", { value: "system" }, "系统", -1),
          g("option", { value: "user" }, "用户", -1),
          g("option", { value: "assistant" }, "助手", -1)
        ])], 40, qae)]),
        g("label", Hae, [ce[23] || (ce[23] = g("span", null, "内容", -1)), g("textarea", {
          value: T(C).content,
          rows: "16",
          spellcheck: "false",
          disabled: T(C).marker,
          onInput: ce[8] || (ce[8] = (we) => T(P)(T(C).identifier, { content: we.target.value }))
        }, null, 40, zae)]),
        T(C).marker ? (O(), B("p", Kae, " 这是酒馆顺序占位条目，不允许编辑。 ")) : Te("", !0)
      ])) : (O(), B("div", Wae, " 当前预设没有可编辑条目。 "))])) : Te("", !0)])
    ], 2)), [[rc, T(n) === "chatPreset"]]);
  }
}), c8 = Gae, Vae = /* @__PURE__ */ bn({
  __name: "TavernScrollControls",
  props: {
    active: { type: Boolean },
    showTop: { type: Boolean },
    showBottom: { type: Boolean },
    extraClass: {}
  },
  emits: ["top", "bottom"],
  setup(e) {
    return (t, n) => (O(), B("div", { class: Fe(["chat-scroll-helpers", [e.extraClass, { active: e.active }]]) }, [g("button", {
      type: "button",
      class: Fe({ visible: e.showTop }),
      title: "回到顶部",
      "aria-label": "回到顶部",
      onClick: n[0] || (n[0] = (r) => t.$emit("top"))
    }, " ▲ ", 2), g("button", {
      type: "button",
      class: Fe({ visible: e.showBottom }),
      title: "回到底部",
      "aria-label": "回到底部",
      onClick: n[1] || (n[1] = (r) => t.$emit("bottom"))
    }, " ▼ ", 2)], 2));
  }
}), d8 = Vae, Jae = { class: "message-edit-panel" }, Yae = ["data-message-editor"], Xae = { class: "message-edit-actions" }, jae = ["disabled"], Qae = ["disabled"], Zae = /* @__PURE__ */ bn({
  __name: "TavernMessageEditPanel",
  props: {
    message: {},
    messageKey: {}
  },
  emits: ["cancel", "save"],
  setup(e, { emit: t }) {
    const n = e, r = t, s = /* @__PURE__ */ ue(String(n.message.content || "")), a = /* @__PURE__ */ ue(null), o = /* @__PURE__ */ ue(null);
    let l = 0;
    const i = ne(() => s.value.trim() !== String(n.message.content || "").trim());
    function u(y) {
      return y.closest(".chat-scroll");
    }
    function d() {
      const y = a.value, S = o.value;
      if (!y || !S) return;
      const x = u(y), I = x?.scrollTop ?? 0, E = y.offsetHeight, N = 144, $ = 420, p = window.getComputedStyle(y);
      S.textContent = y.value || " ", S.style.width = `${y.clientWidth}px`, S.style.font = p.font, S.style.lineHeight = p.lineHeight, S.style.letterSpacing = p.letterSpacing, S.style.padding = p.padding, S.style.border = p.border, S.style.boxSizing = p.boxSizing;
      const w = Math.min(Math.max(S.scrollHeight, N), $);
      Math.abs(E - w) >= 1 && (y.style.height = `${w}px`, x && (x.scrollTop = I)), x && (l && cancelAnimationFrame(l), l = requestAnimationFrame(() => {
        l = 0, x.scrollTop = I;
      }));
    }
    function f() {
      Ut(() => {
        const y = a.value;
        y && (d(), y.focus(), y.setSelectionRange(y.value.length, y.value.length));
      });
    }
    function m(y = {}) {
      i.value && r("save", {
        content: s.value,
        ...y
      });
    }
    function v(y) {
      if (y.key === "Escape") {
        y.preventDefault(), r("cancel");
        return;
      }
      y.key === "Enter" && (y.ctrlKey || y.metaKey) && (y.preventDefault(), m());
    }
    return vt(() => n.messageKey, () => {
      s.value = String(n.message.content || ""), f();
    }, { immediate: !0 }), Sl(() => {
      l && (cancelAnimationFrame(l), l = 0);
    }), (y, S) => (O(), B("div", Jae, [
      er(g("textarea", {
        ref_key: "textareaRef",
        ref: a,
        "onUpdate:modelValue": S[0] || (S[0] = (x) => s.value = x),
        class: "message-edit-box",
        rows: "6",
        "data-message-editor": e.messageKey,
        onInput: d,
        onKeydown: v
      }, null, 40, Yae), [[sa, s.value]]),
      g("div", {
        ref_key: "sizerRef",
        ref: o,
        class: "message-edit-sizer",
        "aria-hidden": "true"
      }, null, 512),
      g("div", Xae, [
        g("button", {
          type: "button",
          disabled: !i.value,
          onClick: S[1] || (S[1] = (x) => m())
        }, " 仅保存 ", 8, jae),
        g("button", {
          type: "button",
          disabled: !i.value,
          onClick: S[2] || (S[2] = (x) => m({ rollbackState: !0 }))
        }, " 回滚保存 ", 8, Qae),
        g("button", {
          type: "button",
          onClick: S[3] || (S[3] = (x) => y.$emit("cancel"))
        }, " 取消 ")
      ])
    ]));
  }
}), eoe = Zae, toe = ["onKeydown"], noe = {
  class: "tavern-draw-capsule",
  role: "presentation"
}, roe = { class: "tavern-draw-inner" }, soe = { class: "tavern-draw-layer tavern-draw-layer-idle" }, aoe = [
  "disabled",
  "title",
  "aria-label"
], ooe = { "aria-hidden": "true" }, ioe = ["aria-expanded"], loe = ["title", "aria-label"], uoe = {
  class: "tavern-draw-status-icon",
  "aria-hidden": "true"
}, coe = { class: "tavern-draw-status-text" }, doe = ["onKeydown"], foe = { class: "tavern-draw-detail-header" }, hoe = { class: "tavern-draw-detail-heading" }, poe = {
  class: "tavern-draw-detail-icon",
  "aria-hidden": "true"
}, moe = { class: "tavern-draw-detail-summary" }, goe = {
  key: 0,
  class: "tavern-draw-detail-note"
}, voe = ["onKeydown"], yoe = { class: "tavern-draw-menu-card" }, _oe = { class: "tavern-draw-menu-row" }, boe = ["value", "disabled"], woe = {
  key: 0,
  value: ""
}, Soe = ["value"], Aoe = { class: "tavern-draw-menu-row" }, Toe = ["value", "disabled"], Eoe = {
  key: 0,
  value: ""
}, Coe = ["value"], Ioe = { class: "tavern-draw-menu-actions" }, xoe = ["disabled", "aria-checked"], koe = /* @__PURE__ */ bn({
  __name: "TavernDrawCapsule",
  props: { mobile: { type: Boolean } },
  setup(e) {
    const t = e, n = s8(), { homeThemeDark: r } = vc(), { drawLatestAssistantMessage: s, openTavernDrawSettings: a, refreshTavernDrawQuickSettings: o, tavernDrawCapsuleIcon: l, tavernDrawCapsuleMainDisabled: i, tavernDrawCapsuleStatusClass: u, tavernDrawCapsuleStatusText: d, tavernDrawCapsuleTitle: f, tavernDrawCapsuleVisible: m, tavernDrawQuickSettings: v, tavernDrawQuickSettingsLoading: y, updateTavernDrawQuickSettings: S } = n, x = /* @__PURE__ */ ue(!1), I = /* @__PURE__ */ ue(!1), E = /* @__PURE__ */ ue(null), N = /* @__PURE__ */ ue({}), $ = /* @__PURE__ */ ue({});
    let p = 0;
    const w = ne(() => !!(d.value || u.value || l.value === "■")), _ = ne(() => v.value.available && v.value.presets.length > 0 && v.value.sizeOptions.length > 0), b = ne(() => v.value.auto === !0), C = ne(() => d.value || (l.value === "■" ? "绘制中" : f.value)), k = ne(() => le(C.value, u.value, l.value)), P = ne(() => l.value === "■" || u.value.includes("running")), H = ne(() => !x.value && I.value && w.value), G = ne(() => P.value ? f.value : "查看画图状态"), ee = ne(() => P.value ? "画图进行中" : u.value.includes("success") ? "画图完成" : u.value.includes("error") ? "画图失败" : "画图状态"), J = ne(() => {
      const W = C.value.trim();
      if (W) return W;
      const L = f.value.trim();
      return L || "暂无可显示的画图状态";
    }), z = ne(() => {
      const W = f.value.trim(), L = J.value.trim();
      return !W || W === L ? "" : P.value || u.value.includes("error") ? W : "";
    }), R = ne(() => [
      "tavern-draw-popover-layer",
      t.mobile ? "is-mobile" : "is-desktop",
      r.value ? "theme-dark" : "theme-light"
    ]);
    function V(W, L, Q) {
      return Math.max(L, Math.min(Q, W));
    }
    function le(W = "", L = "", Q = "") {
      const de = String(W || "").trim(), ge = de.match(/(\d+)\s*\/\s*(\d+)/);
      if (ge) return `${ge[1]}/${ge[2]}`;
      const pe = de.match(/前方\s*(\d+)/);
      if (pe) return Number(pe[1]) > 0 ? `排队${pe[1]}` : "排队";
      const Me = de.match(/(\d+(?:\.\d+)?)\s*s/i);
      return Me && /冷却|等待/.test(de) ? `${Me[1]}s` : /排队/.test(de) ? "排队" : /冷却|等待下一张/.test(de) ? "冷却" : /分析/.test(de) ? "分析" : /准备/.test(de) ? "准备" : /生成|绘制|画图|配图/.test(de) && Q === "■" ? "绘制" : /取消/.test(de) ? "取消" : L.includes("success") ? "完成" : L.includes("error") ? "错误" : Q === "■" ? "绘制" : de || "状态";
    }
    function ve(W, L) {
      const Q = E.value?.getBoundingClientRect();
      if (!Q) return {
        position: "fixed",
        top: "0px",
        left: "0px"
      };
      const de = 8, ge = Math.max(document.documentElement.clientWidth || 0, window.innerWidth || 0), pe = Math.max(document.documentElement.clientHeight || 0, window.innerHeight || 0), Me = V(Q.right - W, de, Math.max(de, ge - W - de)), Ie = Q.bottom + de, he = Q.top - L - de, q = Ie + L <= pe - de ? Ie : V(he, de, Math.max(de, pe - L - de));
      return {
        position: "fixed",
        top: `${Math.round(q)}px`,
        left: `${Math.round(Me)}px`
      };
    }
    function ke() {
      !x.value && !H.value || (N.value = ve(198, 156), $.value = ve(244, 96));
    }
    function be() {
      p || (p = window.requestAnimationFrame(() => {
        p = 0, ke();
      }));
    }
    function Ne() {
      x.value = !1;
    }
    function ce() {
      Ne(), I.value = !1;
    }
    async function we() {
      x.value = !x.value, I.value = !1, x.value && (await o(), await Ut(), ke());
    }
    async function Oe(W) {
      const L = W.target instanceof HTMLSelectElement ? W.target : null;
      L && await S({ selectedPresetId: L.value });
    }
    async function F(W) {
      const L = W.target instanceof HTMLSelectElement ? W.target : null;
      L && await S({ selectedSize: L.value });
    }
    async function ae() {
      await S({ auto: !b.value });
    }
    async function Z() {
      Ne(), await a();
    }
    async function ie() {
      if (P.value) {
        await s();
        return;
      }
      Ne(), I.value = !I.value, await Ut(), ke();
    }
    async function me(W) {
      W.key !== "Enter" && W.key !== " " || (W.preventDefault(), W.stopPropagation(), await ie());
    }
    function Se(W) {
      const L = W.target instanceof Node ? W.target : null;
      !x.value && !I.value || L && (E.value?.contains(L) || L instanceof Element && L.closest(".tavern-draw-popover-layer")) || ce();
    }
    return vt([x, H], ([W, L]) => {
      if (W || L) {
        be(), document.addEventListener("pointerdown", Se), window.addEventListener("resize", be), window.addEventListener("scroll", be, !0);
        return;
      }
      document.removeEventListener("pointerdown", Se), window.removeEventListener("resize", be), window.removeEventListener("scroll", be, !0);
    }), vt(m, (W) => {
      W || ce();
    }), vt(w, (W) => {
      W || (I.value = !1);
    }), Sl(() => {
      p && (window.cancelAnimationFrame(p), p = 0), document.removeEventListener("pointerdown", Se), window.removeEventListener("resize", be), window.removeEventListener("scroll", be, !0);
    }), (W, L) => (O(), B(He, null, [T(m) ? (O(), B("div", {
      key: 0,
      ref_key: "rootRef",
      ref: E,
      class: Fe(["tavern-draw-float", [T(u), {
        "tavern-draw-capsule-mobile": t.mobile,
        "is-expanded": x.value,
        "is-auto-on": b.value,
        "is-disabled": T(i),
        "is-working": T(l) === "■",
        "has-active-layer": w.value
      }]]),
      role: "group",
      "aria-label": "画图",
      onKeydown: Qr(Kt(ce, ["stop", "prevent"]), ["esc"])
    }, [g("div", noe, [g("div", roe, [g("div", soe, [
      g("button", {
        type: "button",
        class: "tavern-draw-main",
        disabled: T(i),
        title: T(f),
        "aria-label": T(f),
        onClick: L[0] || (L[0] = (...Q) => T(s) && T(s)(...Q))
      }, [g("span", ooe, te(T(l)), 1), L[3] || (L[3] = g("span", {
        class: "tavern-draw-auto-dot",
        "aria-hidden": "true"
      }, null, -1))], 8, aoe),
      L[5] || (L[5] = g("span", {
        class: "tavern-draw-divider",
        "aria-hidden": "true"
      }, null, -1)),
      g("button", {
        type: "button",
        class: "tavern-draw-menu-button",
        title: "展开画图菜单",
        "aria-label": "展开画图菜单",
        "aria-expanded": x.value ? "true" : "false",
        onClick: Kt(we, ["stop"])
      }, [...L[4] || (L[4] = [g("span", {
        class: "tavern-draw-arrow",
        "aria-hidden": "true"
      }, "▼", -1)])], 8, ioe)
    ]), g("div", {
      class: "tavern-draw-layer tavern-draw-layer-active",
      role: "button",
      tabindex: "0",
      title: G.value,
      "aria-label": G.value,
      onClick: ie,
      onKeydown: me
    }, [g("span", uoe, te(T(l)), 1), g("span", coe, te(k.value), 1)], 40, loe)])])], 42, toe)) : Te("", !0), (O(), cn(fk, { to: "body" }, [H.value ? (O(), B("div", {
      key: 0,
      class: Fe(["tavern-draw-detail", R.value]),
      style: Nn($.value),
      onKeydown: Qr(Kt(ce, ["stop", "prevent"]), ["esc"])
    }, [
      g("div", foe, [g("span", hoe, te(ee.value), 1), g("strong", poe, te(T(l)), 1)]),
      g("div", moe, te(J.value), 1),
      z.value ? (O(), B("p", goe, te(z.value), 1)) : Te("", !0)
    ], 46, doe)) : Te("", !0), x.value ? (O(), B("div", {
      key: 1,
      class: Fe(["tavern-draw-menu", R.value]),
      style: Nn(N.value),
      role: "menu",
      "aria-label": "画图快捷设置",
      onKeydown: Qr(Kt(ce, ["stop", "prevent"]), ["esc"]),
      onPointerdown: L[1] || (L[1] = Kt(() => {
      }, ["stop"])),
      onClick: L[2] || (L[2] = Kt(() => {
      }, ["stop"]))
    }, [g("div", yoe, [g("label", _oe, [L[6] || (L[6] = g("span", null, "预设", -1)), g("select", {
      value: T(v).selectedPresetId,
      disabled: T(y) || !_.value,
      onChange: Oe
    }, [T(v).presets.length ? Te("", !0) : (O(), B("option", woe, " 暂无预设 ")), (O(!0), B(He, null, tt(T(v).presets, (Q) => (O(), B("option", {
      key: Q.value,
      value: Q.value
    }, te(Q.label), 9, Soe))), 128))], 40, boe)]), g("label", Aoe, [L[7] || (L[7] = g("span", null, "尺寸", -1)), g("select", {
      class: "is-size",
      value: T(v).selectedSize,
      disabled: T(y) || !_.value,
      onChange: F
    }, [T(v).sizeOptions.length ? Te("", !0) : (O(), B("option", Eoe, " 暂无尺寸 ")), (O(!0), B(He, null, tt(T(v).sizeOptions, (Q) => (O(), B("option", {
      key: Q.value,
      value: Q.value
    }, te(Q.label), 9, Coe))), 128))], 40, Toe)])]), g("div", Ioe, [g("button", {
      type: "button",
      class: Fe(["tavern-draw-auto-toggle", { "is-on": b.value }]),
      disabled: T(y) || !T(v).available,
      role: "switch",
      "aria-checked": b.value ? "true" : "false",
      onClick: ae
    }, [...L[8] || (L[8] = [g("span", { "aria-hidden": "true" }, null, -1), g("strong", null, "自动配图", -1)])], 10, xoe), g("button", {
      type: "button",
      class: "tavern-draw-gear",
      title: "画图设置",
      "aria-label": "画图设置",
      onClick: Z
    }, " ⚙ ")])], 46, voe)) : Te("", !0)]))], 64));
  }
}), f8 = koe;
function Moe(e) {
  const t = String(e.text || ""), n = Array.isArray(e.thoughts) ? e.thoughts : [], r = Array.isArray(e.actionCheckEvents) ? e.actionCheckEvents : [];
  return !!t || n.length > 0 || r.length > 0;
}
function Poe(e) {
  const t = String(e.text || ""), n = Array.isArray(e.actionCheckEvents) ? e.actionCheckEvents : [];
  return !!t || n.length > 0;
}
var Roe = ["aria-hidden"], Noe = {
  key: 0,
  class: "chat-ambient-standee",
  "aria-hidden": "true"
}, $oe = ["src"], Loe = { class: "chat-head" }, Doe = { class: "chat-head-main" }, Ooe = {
  class: "xb-workspace-controller chat-layout-controller",
  "aria-label": "区域大小"
}, Foe = { class: "chat-head-actions" }, Boe = { class: "chat-scroll-shell" }, Uoe = ["data-chat-anchor-key"], qoe = ["data-chat-anchor-key", "onClick"], Hoe = { class: "bubble-meta" }, zoe = { class: "bubble-identity" }, Koe = { class: "bubble-nameplate" }, Woe = { class: "bubble-avatar-stamp" }, Goe = ["src"], Voe = ["src"], Joe = { key: 2 }, Yoe = { class: "bubble-role-name" }, Xoe = { class: "bubble-meta-line" }, joe = ["title", "aria-label"], Qoe = { class: "bubble-time-tag" }, Zoe = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], eie = ["disabled", "onClick"], tie = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], nie = ["disabled", "onClick"], rie = ["open", "onToggle"], sie = { class: "tavern-thought-label" }, aie = [
  "data-markdown-signature",
  "data-action-check-groups",
  "innerHTML"
], oie = { class: "scene-tag" }, iie = {
  key: 1,
  "data-chat-anchor-key": "pending:user",
  class: "chat-bubble from-user pending-user"
}, lie = { class: "bubble-meta" }, uie = { class: "bubble-identity" }, cie = { class: "bubble-nameplate" }, die = { class: "bubble-avatar-stamp" }, fie = ["src"], hie = { key: 1 }, pie = { class: "bubble-role-name" }, mie = ["data-markdown-signature", "innerHTML"], gie = {
  key: 2,
  "data-chat-anchor-key": "streaming:content",
  class: "chat-bubble from-assistant streaming"
}, vie = { class: "bubble-meta" }, yie = { class: "bubble-nameplate" }, _ie = { class: "bubble-avatar-stamp" }, bie = ["src"], wie = { key: 1 }, Sie = { class: "bubble-role-name" }, Aie = ["open"], Tie = { class: "tavern-thought-label" }, Eie = [
  "data-action-check-groups",
  "data-markdown-signature",
  "innerHTML"
], Cie = {
  key: 3,
  "data-chat-anchor-key": "streaming:empty",
  class: "chat-bubble from-assistant streaming thinking"
}, Iie = { class: "bubble-meta" }, xie = { class: "bubble-nameplate" }, kie = { class: "bubble-avatar-stamp" }, Mie = ["src"], Pie = { key: 1 }, Rie = { class: "bubble-role-name" }, Nie = {
  key: 4,
  class: "chat-empty"
}, $ie = { class: "chat-compose-dock" }, Lie = { class: "compose-menu-shell" }, Die = ["aria-expanded", "disabled"], Oie = ["onKeydown"], Fie = ["disabled"], Bie = ["disabled", "aria-label"], Uie = {
  class: "compose-send-icon",
  "aria-hidden": "true"
}, qie = { class: "compose-send-label" }, Hie = { class: "character-session-archive chat-session-archive" }, zie = {
  key: 0,
  class: "session-archive-list"
}, Kie = ["onClick"], Wie = { class: "session-archive-item-title" }, Gie = { class: "session-archive-item-meta" }, Vie = ["onClick"], Jie = {
  key: 1,
  class: "session-archive-empty"
}, Ep = "chat:runtime-thoughts", Yie = /* @__PURE__ */ bn({
  __name: "TavernConversationPanel",
  emits: ["open-contract", "open-author-note"],
  setup(e, { emit: t }) {
    const n = t, r = vc(), s = GS(), a = kv(), o = s8(), { activeView: l, homeThemeDark: i, openPromptInspector: u, rememberBrokenAvatar: d } = r, { actionFeedback: f, cancelEditMessage: m, canEditMessage: v, canRerunMessage: y, canSendMessage: S, chatComposeTextareaRef: x, chatFocus: I, chatLayout: E, chatScrollControlsActive: N, chatScrollRef: $, currentUserMessage: p, deleteMessageTurn: w, displayMessageContent: _, displayMessageRenderProjection: b, displayMessageThoughtBlocks: C, displayRuntimeRenderProjection: k, displayRuntimeThoughtBlocks: P, formatMessageTime: H, handleChatScroll: G, handleChatSubmit: ee, handleChatTouchMove: J, handleChatTouchStart: z, handleChatWheel: R, handleComposeInput: V, handleComposeKeydown: le, isEditingMessage: ve, isCancellingRun: ke, isRunning: be, markdownSignature: Ne, htmlRenderEnabled: ce, messageKey: we, renderChatMarkdown: Oe, rerunFromMessage: F, revealOlderChatMessages: ae, roleLabel: Z, runtimeActionCheckEvents: ie, runtimeFinalizedAssistantMessage: me, runtimePendingUserMessage: Se, runtimeText: W, runtimeThoughts: L, runtimeUserMessageVisible: Q, saveEditMessage: de, scrollChatToBottom: ge, scrollChatToTop: pe, showChatScrollBottom: Me, showChatScrollTop: Ie, startEditMessage: he, thoughtBlocks: q, thoughtSummaryLabel: ye, visibleCharacterAvatar: oe, visibleUserAvatar: De } = s, { chatMessages: Je, chatMessageWindow: st, createNewChatSession: lt, currentChatCharacterSessions: ot, removeSession: pt, selectedSessionId: ht, selectSession: It, sessionDisplayTitle: Lt, sessionFloorLabel: en, visibleChatMessages: at } = a, { canDrawMessage: X, drawMessage: se, drawMessageStatusClass: Ae, drawMessageStatusText: Re, drawMessageTitle: Be, isDrawingMessage: je } = o;
    function Qe(St) {
      $.value = St instanceof HTMLElement ? St : null;
    }
    function ut(St) {
      x.value = St instanceof HTMLTextAreaElement ? St : null;
    }
    function wt() {
      n("open-contract");
    }
    function rn() {
      return {
        roleplay: !0,
        userName: Z("user"),
        characterName: Z("assistant")
      };
    }
    function Ht(St = "", D = "") {
      const Y = rn();
      return Ne([
        St,
        D,
        Y.userName,
        Y.characterName,
        ce.value ? "html-render:on" : "html-render:off",
        i.value ? "theme:dark" : "theme:light"
      ].join("\0"));
    }
    function Dn(St = "") {
      return Oe(St, rn());
    }
    function br(St) {
      return `#${Math.max(1, Number(St.order) + 1)}`;
    }
    function fr(St, D = []) {
      const Y = PD(St, D), j = Y.groups.length ? JSON.stringify(Y.groups) : "";
      return {
        text: Y.text,
        signature: Ht(Y.text, j),
        actionCheckGroups: j
      };
    }
    function ct(St) {
      if (St.role !== "assistant") {
        const Y = _(St);
        return {
          text: Y,
          signature: Ht(Y),
          actionCheckGroups: ""
        };
      }
      const D = b(St);
      return fr(D.text, D.actionCheckEvents);
    }
    const Ke = ne(() => {
      if (me.value) return ct(me.value);
      const St = k(W.value, Array.isArray(ie.value) ? ie.value : []);
      return fr(St.text, St.actionCheckEvents);
    }), it = ne(() => Moe({
      text: Ke.value.text,
      thoughts: me.value ? C(me.value) : L.value,
      actionCheckEvents: me.value ? [] : ie.value
    })), En = ne(() => be.value && Q.value || !!me.value), On = ne(() => Poe({
      text: Ke.value.text,
      actionCheckEvents: me.value ? [] : ie.value
    })), Gs = ne(() => me.value ? C(me.value) : P(q(L.value))), Gr = ne(() => be.value && !Q.value && !!Se.value.trim()), rr = ne(() => {
      const St = Se.value.trim();
      return {
        text: St,
        signature: Ht(St, "pending-user")
      };
    }), Vn = Mv(), Ms = Pv("(max-width: 760px)"), mn = /* @__PURE__ */ ue(""), Fn = /* @__PURE__ */ ue(!1), Ps = /* @__PURE__ */ ue(!1);
    function Ot(St) {
      return `chat:thought:${we(St)}`;
    }
    function wn(St) {
      return mn.value === we(St);
    }
    function sn(St) {
      return !!(St.target instanceof Element ? St.target : null)?.closest("summary, details, a, button, input, textarea, select, label, .xb-tavern-html-wrapper");
    }
    function Wt(St, D) {
      if (!Ms.value || ve(St) || D && sn(D)) return;
      const Y = we(St);
      mn.value = mn.value === Y ? "" : Y;
    }
    function Gt() {
      mn.value = "";
    }
    function Rn() {
      Fn.value = !1;
    }
    function da() {
      Gt(), Rn();
    }
    function fa() {
      Fn.value = !Fn.value;
    }
    async function bt() {
      Rn(), await lt();
    }
    function Ye() {
      Rn(), Ps.value = !0;
    }
    function Pe() {
      Rn(), n("open-author-note");
    }
    function Xt() {
      Rn(), u("history");
    }
    function Rr() {
      Ps.value = !1;
    }
    async function ns(St) {
      await It(St), Rr();
    }
    async function ha(St, D) {
      await pt(St, D);
    }
    return vt([
      l,
      I,
      ht
    ], ([St, D]) => {
      (St !== "chat" || D !== "chat") && (Vn.reset(), Gt(), Rn(), Rr());
    }), vt(() => `${ht.value}:${st.value.startIndex}:${st.value.visibleCount}`, () => {
      Vn.reset(), Gt(), Rn(), Rr();
    }), vt(Ms, (St) => {
      St || Gt();
    }), (St, D) => (O(), B("section", {
      class: "chat-face chat-face-front chat-main",
      "aria-hidden": T(I) === "manager",
      onClick: da
    }, [
      T(oe) ? (O(), B("div", Noe, [g("img", {
        src: T(oe),
        alt: "",
        onError: D[0] || (D[0] = (Y) => T(d)(T(oe)))
      }, null, 40, $oe)])) : Te("", !0),
      g("header", Loe, [g("div", Doe, [g("div", Ooe, [
        g("button", {
          type: "button",
          class: Fe(["xb-layout-button", { "is-active": T(E) === "chat" }]),
          onClick: D[1] || (D[1] = (Y) => E.value = "chat")
        }, " 聊天 ", 2),
        g("button", {
          type: "button",
          class: Fe(["xb-layout-button", { "is-active": T(E) === "balanced" }]),
          onClick: D[2] || (D[2] = (Y) => E.value = "balanced")
        }, " 平衡 ", 2),
        g("button", {
          type: "button",
          class: Fe(["xb-layout-button", { "is-active": T(E) === "editor" }]),
          onClick: D[3] || (D[3] = (Y) => E.value = "editor")
        }, " 记忆 ", 2)
      ])]), g("div", Foe, [
        Wn(f8),
        g("button", {
          type: "button",
          class: "contract-trigger",
          title: "契约",
          "aria-label": "契约",
          onClick: wt
        }, " 契约 "),
        g("button", {
          type: "button",
          class: "chat-flip-trigger",
          title: "助手聊天",
          "aria-label": "助手聊天",
          onClick: D[4] || (D[4] = (Y) => I.value = "manager")
        }, " 助手 ")
      ])]),
      g("div", Boe, [g("div", {
        ref: Qe,
        class: "chat-scroll",
        onScroll: D[15] || (D[15] = (...Y) => T(G) && T(G)(...Y)),
        onClick: Gt,
        onWheelPassive: D[16] || (D[16] = (...Y) => T(R) && T(R)(...Y)),
        onTouchstartPassive: D[17] || (D[17] = (...Y) => T(z) && T(z)(...Y)),
        onTouchmovePassive: D[18] || (D[18] = (...Y) => T(J) && T(J)(...Y))
      }, [
        T(st).hiddenBefore ? (O(), B("div", {
          key: 0,
          class: "chat-history-gate",
          "data-chat-anchor-key": `gate:${T(st).hiddenBefore}`,
          role: "button",
          tabindex: "0",
          onClick: D[5] || (D[5] = (Y) => T(ae)(!0)),
          onKeydown: [D[6] || (D[6] = Qr(Kt((Y) => T(ae)(!0), ["prevent"]), ["enter"])), D[7] || (D[7] = Qr(Kt((Y) => T(ae)(!0), ["prevent"]), ["space"]))]
        }, " 展开较早记录 " + te(T(st).hiddenBefore) + " 条 ", 41, Uoe)) : Te("", !0),
        (O(!0), B(He, null, tt(T(at), (Y) => (O(), B(He, { key: `${Y.sessionId}-${Y.order}` }, [g("div", {
          "data-chat-anchor-key": `${Y.sessionId}:${Y.order}`,
          class: Fe(["chat-bubble", [
            Y.role === "user" ? "from-user" : "from-assistant",
            { "is-error": Y.error },
            { "is-action-tray-open": wn(Y) }
          ]]),
          onClick: Kt((j) => Wt(Y, j), ["stop"])
        }, [
          g("div", Hoe, [g("div", zoe, [g("span", Koe, [
            g("span", Woe, [Y.role === "user" && T(De) ? (O(), B("img", {
              key: 0,
              src: T(De),
              alt: "",
              onError: D[8] || (D[8] = (j) => T(d)(T(De)))
            }, null, 40, Goe)) : Y.role !== "user" && T(oe) ? (O(), B("img", {
              key: 1,
              src: T(oe),
              alt: "",
              onError: D[9] || (D[9] = (j) => T(d)(T(oe)))
            }, null, 40, Voe)) : (O(), B("span", Joe, te(String(T(Z)(Y.role)).slice(0, 1)), 1))]),
            g("span", Yoe, te(Y.error ? "错误" : T(Z)(Y.role)), 1),
            g("span", Xoe, [g("span", {
              class: "message-floor-label",
              title: `第 ${br(Y).slice(1)} 楼`,
              "aria-label": `第 ${br(Y).slice(1)} 楼`
            }, te(br(Y)), 9, joe), g("small", Qoe, te(T(H)(Y.createdAt)), 1)])
          ])])]),
          T(ve)(Y) ? Te("", !0) : (O(), B("div", {
            key: 0,
            class: Fe(["message-actions", { "has-status": !!T(Re)(Y) }]),
            onClick: D[10] || (D[10] = Kt(() => {
            }, ["stop"]))
          }, [
            T(Re)(Y) ? (O(), B("span", {
              key: 0,
              class: Fe(["message-draw-status", T(Ae)(Y)])
            }, te(T(Re)(Y)), 3)) : Te("", !0),
            g("button", {
              type: "button",
              disabled: !T(X)(Y),
              class: Fe([T(f)(Y, "draw"), { "is-running": T(je)(Y) }]),
              title: T(Be)(Y),
              "aria-label": T(Be)(Y),
              onClick: (j) => T(se)(Y)
            }, te(T(je)(Y) ? "■" : "🎨"), 11, Zoe),
            g("button", {
              type: "button",
              disabled: !T(v)(Y),
              class: Fe(T(f)(Y, "edit")),
              title: "编辑",
              "aria-label": "编辑",
              onClick: (j) => T(he)(Y)
            }, [...D[28] || (D[28] = [g("svg", {
              "aria-hidden": "true",
              viewBox: "0 0 24 24"
            }, [g("path", { d: "M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7" }), g("path", { d: "M18.5 2.5a2.12 2.12 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z" })], -1)])], 10, eie),
            g("button", {
              type: "button",
              disabled: !T(y)(Y),
              class: Fe(T(f)(Y, "rerun")),
              title: Y.role === "user" ? "发送最后一楼" : "重新生成最后回复",
              "aria-label": Y.role === "user" ? "发送最后一楼" : "重新生成最后回复",
              onClick: (j) => T(F)(Y)
            }, [...D[29] || (D[29] = [g("svg", {
              "aria-hidden": "true",
              viewBox: "0 0 24 24"
            }, [g("path", { d: "M1 4v6h6M23 20v-6h-6" }), g("path", { d: "M20.49 9A9 9 0 0 0 5.64 5.64L1 10m22 4-4.64 4.36A9 9 0 0 1 3.51 15" })], -1)])], 10, tie),
            D[31] || (D[31] = g("span", {
              class: "message-action-divider",
              "aria-hidden": "true"
            }, null, -1)),
            g("button", {
              type: "button",
              disabled: T(be),
              class: Fe(T(f)(Y, "delete")),
              title: "从这里删除后续剧情",
              "aria-label": "从这里删除后续剧情",
              onClick: (j) => T(w)(Y)
            }, [...D[30] || (D[30] = [g("svg", {
              "aria-hidden": "true",
              viewBox: "0 0 24 24"
            }, [g("path", { d: "M3 6h18M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2" })], -1)])], 10, nie)
          ], 2)),
          T(ve)(Y) ? (O(), cn(eoe, {
            key: 1,
            message: Y,
            "message-key": T(we)(Y),
            onCancel: T(m),
            onSave: (j) => T(de)(Y, j)
          }, null, 8, [
            "message",
            "message-key",
            "onCancel",
            "onSave"
          ])) : Te("", !0),
          (O(!0), B(He, null, tt([T(q)(Y)], (j) => (O(), B(He, { key: `${T(we)(Y)}:raw-thoughts:${j.length}` }, [!T(ve)(Y) && j.length ? (O(), B("details", {
            key: 0,
            class: "tavern-thought-details",
            open: T(Vn).isOpen(Ot(Y)),
            onToggle: (We) => T(Vn).setOpenFromEvent(Ot(Y), We)
          }, [g("summary", null, te(T(ye)(j)), 1), T(Vn).isOpen(Ot(Y)) ? (O(!0), B(He, { key: 0 }, tt([T(C)(Y)], (We) => (O(), B(He, { key: `${T(we)(Y)}:display-thoughts:${We.length}` }, [(O(!0), B(He, null, tt(We, (qe, Ft) => (O(), B("div", {
            key: `${Y.sessionId}-${Y.order}-thought-${Ft}`,
            class: "tavern-thought-block"
          }, [g("div", sie, te(qe.label), 1), g("pre", null, te(qe.text), 1)]))), 128))], 64))), 128)) : Te("", !0)], 40, rie)) : Te("", !0)], 64))), 128)),
          T(ve)(Y) ? Te("", !0) : (O(!0), B(He, { key: 2 }, tt([ct(Y)], (j) => (O(), B("div", {
            key: `${T(we)(Y)}:${j.signature}`,
            class: "xb-tavern-markdown",
            "data-markdown-signature": j.signature,
            "data-action-check-groups": j.actionCheckGroups || null,
            innerHTML: Dn(j.text)
          }, null, 8, aie))), 128))
        ], 10, qoe), (O(!0), B(He, null, tt(Y.role === "user" ? Y.runtimeEvents || [] : [], (j, We) => (O(), B("div", {
          key: `${Y.sessionId}-${Y.order}-runtime-event-${j.type}-${We}`,
          class: "chat-runtime-event scene-narration",
          "aria-hidden": "true"
        }, [g("div", oie, te(String(j.label || "")), 1)]))), 128))], 64))), 128)),
        Gr.value ? (O(), B("div", iie, [g("div", lie, [g("div", uie, [g("span", cie, [
          g("span", die, [T(De) ? (O(), B("img", {
            key: 0,
            src: T(De),
            alt: "",
            onError: D[11] || (D[11] = (Y) => T(d)(T(De)))
          }, null, 40, fie)) : (O(), B("span", hie, te(String(T(Z)("user")).slice(0, 1)), 1))]),
          g("span", pie, te(T(Z)("user")), 1),
          D[32] || (D[32] = g("span", { class: "bubble-meta-line" }, [g("small", { class: "bubble-time-tag" }, "发送中")], -1))
        ])])]), (O(), B("div", {
          key: `pending-user:${rr.value.signature}`,
          class: "xb-tavern-markdown",
          "data-markdown-signature": rr.value.signature,
          innerHTML: Dn(rr.value.text)
        }, null, 8, mie))])) : Te("", !0),
        En.value && it.value ? (O(), B("div", gie, [
          g("div", vie, [g("span", yie, [g("span", _ie, [T(oe) ? (O(), B("img", {
            key: 0,
            src: T(oe),
            alt: "",
            onError: D[12] || (D[12] = (Y) => T(d)(T(oe)))
          }, null, 40, bie)) : (O(), B("span", wie, te(String(T(Z)("assistant")).slice(0, 1)), 1))]), g("span", Sie, te(T(Z)("assistant")), 1)]), g("small", null, te(T(me) ? "已完成" : "生成中"), 1)]),
          (O(!0), B(He, null, tt([Gs.value], (Y) => (O(), B(He, { key: `${Ep}:${Y.length}` }, [Y.length ? (O(), B("details", {
            key: 0,
            class: "tavern-thought-details",
            open: T(Vn).isOpen(Ep, !0),
            onToggle: D[13] || (D[13] = (j) => T(Vn).setOpenFromEvent(Ep, j))
          }, [g("summary", null, te(T(ye)(Y, !0)), 1), T(Vn).isOpen(Ep, !0) ? (O(!0), B(He, { key: 0 }, tt(Y, (j, We) => (O(), B("div", {
            key: `runtime-thought-${We}`,
            class: "tavern-thought-block"
          }, [g("div", Tie, te(j.label), 1), g("pre", null, te(j.text), 1)]))), 128)) : Te("", !0)], 40, Aie)) : Te("", !0)], 64))), 128)),
          On.value ? (O(), B("div", {
            key: 0,
            class: "xb-tavern-markdown",
            "data-action-check-groups": Ke.value.actionCheckGroups || void 0,
            "data-markdown-signature": Ke.value.signature,
            innerHTML: Dn(Ke.value.text)
          }, null, 8, Eie)) : Te("", !0)
        ])) : Te("", !0),
        En.value && !it.value ? (O(), B("div", Cie, [g("div", Iie, [g("span", xie, [g("span", kie, [T(oe) ? (O(), B("img", {
          key: 0,
          src: T(oe),
          alt: "",
          onError: D[14] || (D[14] = (Y) => T(d)(T(oe)))
        }, null, 40, Mie)) : (O(), B("span", Pie, te(String(T(Z)("assistant")).slice(0, 1)), 1))]), g("span", Rie, te(T(Z)("assistant")), 1)]), g("small", null, te(T(me) ? "已完成" : "生成中"), 1)]), D[33] || (D[33] = g("p", null, "正在组织回复...", -1))])) : Te("", !0),
        !T(Je).length && !T(be) ? (O(), B("p", Nie, " 写一句话，开始。 ")) : Te("", !0),
        D[34] || (D[34] = g("div", {
          class: "chat-compose-spacer",
          "aria-hidden": "true"
        }, null, -1))
      ], 32), Wn(d8, {
        active: T(N),
        "show-top": T(Ie),
        "show-bottom": T(Me),
        onTop: T(pe),
        onBottom: D[19] || (D[19] = (Y) => T(ge)(!0, {
          collapseWindow: !0,
          revealHelpers: !0
        }))
      }, null, 8, [
        "active",
        "show-top",
        "show-bottom",
        "onTop"
      ])]),
      g("div", $ie, [g("div", { class: Fe(["chat-compose-shell", { "has-text": !!T(p).trim() }]) }, [g("div", Lie, [g("button", {
        type: "button",
        class: "compose-menu-button",
        title: "聊天操作",
        "aria-label": "聊天操作",
        "aria-expanded": Fn.value ? "true" : "false",
        "aria-controls": "xb-tavern-compose-menu",
        disabled: T(be) || T(ke),
        onClick: Kt(fa, ["stop"])
      }, [...D[35] || (D[35] = [g("svg", {
        viewBox: "0 0 24 24",
        "aria-hidden": "true"
      }, [
        g("path", { d: "M4 6h16" }),
        g("path", { d: "M4 12h16" }),
        g("path", { d: "M4 18h16" })
      ], -1)])], 8, Die), Fn.value ? (O(), B("div", {
        key: 0,
        id: "xb-tavern-compose-menu",
        class: "compose-menu-popover",
        role: "menu",
        onKeydown: Qr(Kt(Rn, ["stop", "prevent"]), ["esc"]),
        onPointerdown: D[20] || (D[20] = Kt(() => {
        }, ["stop"])),
        onMousedown: D[21] || (D[21] = Kt(() => {
        }, ["stop"])),
        onTouchstart: D[22] || (D[22] = Kt(() => {
        }, ["stop"])),
        onClick: D[23] || (D[23] = Kt(() => {
        }, ["stop"]))
      }, [
        g("button", {
          type: "button",
          role: "menuitem",
          class: "compose-menu-item",
          onClick: bt
        }, " 新建会话 "),
        g("button", {
          type: "button",
          role: "menuitem",
          class: "compose-menu-item",
          onClick: Ye
        }, " 会话档案 "),
        g("button", {
          type: "button",
          role: "menuitem",
          class: "compose-menu-item",
          onClick: Pe
        }, " 玩家便签 "),
        g("button", {
          type: "button",
          role: "menuitem",
          class: "compose-menu-item",
          onClick: Xt
        }, " 请求日志 ")
      ], 40, Oie)) : Te("", !0)]), g("form", {
        class: "chat-compose",
        onSubmit: D[27] || (D[27] = Kt((...Y) => T(ee) && T(ee)(...Y), ["prevent"]))
      }, [er(g("textarea", {
        ref: ut,
        "onUpdate:modelValue": D[24] || (D[24] = (Y) => /* @__PURE__ */ kn(p) ? p.value = Y : null),
        rows: "1",
        placeholder: "对角色说一句话...",
        disabled: T(be),
        onInput: D[25] || (D[25] = (...Y) => T(V) && T(V)(...Y)),
        onKeydown: D[26] || (D[26] = (...Y) => T(le) && T(le)(...Y)),
        onFocus: Rn
      }, null, 40, Fie), [[sa, T(p)]]), g("button", {
        type: "submit",
        class: "primary-action",
        disabled: !T(S),
        "aria-label": T(ke) ? "正在停止" : T(be) ? "停止" : "发送"
      }, [g("span", Uie, te(T(ke) ? "..." : T(be) ? "■" : "➤"), 1), g("span", qie, te(T(ke) ? "正在停止" : T(be) ? "停止" : "发送"), 1)], 8, Bie)], 32)], 2)]),
      Ps.value ? (O(), B("div", {
        key: 1,
        class: "character-session-archive-overlay chat-session-archive-overlay",
        role: "dialog",
        "aria-modal": "true",
        "aria-label": "会话档案",
        onClick: Kt(Rr, ["self"])
      }, [g("section", Hie, [g("header", null, [g("div", null, [D[36] || (D[36] = g("strong", null, "会话档案", -1)), g("span", null, te(T(ot).length) + " 个会话", 1)]), g("button", {
        type: "button",
        class: "session-archive-close",
        "aria-label": "关闭会话档案",
        onClick: Rr
      })]), T(ot).length ? (O(), B("div", zie, [(O(!0), B(He, null, tt(T(ot), (Y) => (O(), B("div", {
        key: Y.id,
        class: Fe(["session-archive-item", { active: Y.id === T(ht) }])
      }, [g("button", {
        type: "button",
        class: "session-archive-open",
        onClick: (j) => ns(Y.id)
      }, [g("span", Wie, te(T(Lt)(Y) || "未命名会话"), 1), g("span", Gie, te(T(en)(Y)), 1)], 8, Kie), g("button", {
        type: "button",
        class: "session-archive-delete",
        title: "删除会话",
        "aria-label": "删除会话",
        onClick: (j) => ha(Y.id, j)
      }, [...D[37] || (D[37] = [g("svg", {
        "aria-hidden": "true",
        viewBox: "0 0 24 24"
      }, [
        g("path", { d: "M3 6h18" }),
        g("path", { d: "M8 6V4h8v2" }),
        g("path", { d: "M6 6l1 15h10l1-15" })
      ], -1)])], 8, Vie)], 2))), 128))])) : (O(), B("p", Jie, " 当前角色还没有其他会话。 "))])])) : Te("", !0)
    ], 8, Roe));
  }
}), Xie = Yie, jie = ["aria-hidden"], Qie = { class: "manager-head" }, Zie = { class: "manager-head-actions" }, ele = ["open"], tle = {
  key: 0,
  class: "manager-work-band-body"
}, nle = ["aria-busy"], rle = { class: "manager-work-section-head" }, sle = { class: "manager-work-source" }, ale = { class: "manager-run-activity" }, ole = { class: "manager-work-status-grid" }, ile = { key: 0 }, lle = {
  key: 1,
  class: "manager-work-issue-line"
}, ule = ["disabled"], cle = {
  key: 1,
  class: "manager-work-section"
}, dle = { class: "manager-work-section-head" }, fle = { key: 0 }, hle = {
  key: 2,
  class: "manager-work-section manager-work-live-draft"
}, ple = { class: "manager-work-section-head" }, mle = { key: 0 }, gle = { class: "manager-tool-list" }, vle = { class: "manager-tool-head" }, yle = { key: 0 }, _le = {
  key: 3,
  class: "manager-work-section manager-work-tools"
}, ble = { class: "manager-work-section-head" }, wle = { class: "manager-tool-list" }, Sle = { class: "manager-tool-head" }, Ale = ["open", "onToggle"], Tle = ["data-markdown-signature", "innerHTML"], Ele = { key: 2 }, Cle = { key: 3 }, Ile = { key: 4 }, xle = ["open"], kle = { key: 0 }, Mle = { class: "chat-scroll-shell manager-scroll-shell" }, Ple = { key: 0 }, Rle = ["data-manager-anchor-key"], Nle = ["data-manager-anchor-key"], $le = { class: "manager-run-title" }, Lle = {
  key: 0,
  class: "message-edit-panel manager-message-edit-panel"
}, Dle = ["data-manager-message-editor", "onKeydown"], Ole = { class: "message-edit-actions" }, Fle = ["disabled", "onClick"], Ble = ["disabled", "onClick"], Ule = ["data-markdown-signature", "innerHTML"], qle = {
  key: 2,
  class: "message-actions manager-message-actions"
}, Hle = ["onClick"], zle = ["disabled", "onClick"], Kle = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], Wle = ["disabled", "onClick"], Gle = ["data-manager-anchor-key"], Vle = { class: "manager-run-title" }, Jle = ["data-markdown-signature", "innerHTML"], Yle = {
  key: 2,
  class: "manager-card manager-message manager-message-assistant manager-message-live",
  "data-manager-anchor-key": "live:manager-thinking"
}, Xle = {
  key: 3,
  "data-manager-anchor-key": "empty",
  class: "chat-empty"
}, jle = { class: "chat-compose-dock manager-compose-dock" }, Qle = ["disabled"], Zle = ["disabled", "aria-label"], eue = {
  class: "compose-send-icon",
  "aria-hidden": "true"
}, tue = { class: "compose-send-label" }, nue = /* @__PURE__ */ bn({
  __name: "TavernManagerPanel",
  emits: ["open-contract"],
  setup(e, { emit: t }) {
    const n = t, r = vc(), s = GS(), a = a8(), { activeView: o, homeThemeDark: l, shortText: i } = r, { chatFocus: u, cancelEditMessage: d, formatMessageTime: f, handleComposeInput: m, htmlRenderEnabled: v, markdownSignature: y, renderChatMarkdown: S, roleLabel: x, thoughtSummaryLabel: I } = s, { activeMemoryFiles: E, archivedManagerRuns: N, canEditManagerMessage: $, canRerunManagerMessage: p, canSendManagerMessage: w, copyManagerMessage: _, currentManagerWorkRun: b, deleteManagerMessageTurn: C, editingMessageDraft: k, enhanceManagerMarkdown: P, formatRunActivityLine: H, formatRunIssueLine: G, formatRunInputLine: ee, formatRunMapLine: J, formatRunMemoryLine: z, formatRunModelLine: R, formatRunTaskLine: V, handleEditInput: le, handleManagerComposeKeydown: ve, handleManagerEditKeydown: ke, handleManagerScroll: be, handleManagerSubmit: Ne, handleManagerTouchMove: ce, handleManagerTouchStart: we, handleManagerWheel: Oe, hiddenManagerRunCount: F, isEditingManagerMessage: ae, isEditingManagerMessageDirty: Z, isManagerAssistantCancelling: ie, isManagerAssistantRunning: me, isManagerRunRetrying: Se, liveManagerChatDisplayItems: W, managerActionFeedback: L, managerBusy: Q, managerCompactionOverlay: de, managerComposeTextareaRef: ge, managerInputDraft: pe, managerMessageWindow: Me, managerRuns: Ie, managerRunTone: he, managerScrollControlsActive: q, managerScrollRef: ye, managerWorkRef: oe, managerStatusLabel: De, managerToolStatusLabel: Je, managerToolTone: st, managerToolTraceItems: lt, managerToolTurnPreview: ot, managerToolTurnSummary: pt, memoryFileDisplayName: ht, memoryFiles: It, memoryIndexStatusLine: Lt, retryManagerRun: en, revealOlderManagerMessages: at, rerunFromManagerMessage: X, saveEditManagerMessage: se, scrollManagerToBottom: Ae, scrollManagerToTop: Re, selectedMemoryFile: Be, showManagerScrollBottom: je, showManagerScrollTop: Qe, startEditManagerMessage: ut, toolTraceSummary: wt, updateManagerScrollButtons: rn, visibleManagerChatItems: Ht } = a;
    function Dn(bt) {
      ye.value = bt instanceof HTMLElement ? bt : null;
    }
    function br(bt) {
      oe.value = bt instanceof HTMLElement ? bt : null;
    }
    function fr(bt) {
      ge.value = bt instanceof HTMLTextAreaElement ? bt : null;
    }
    function ct(bt) {
      it.setOpenFromEvent(On("work-band"), bt), Ut(() => {
        P(), rn();
      });
    }
    function Ke() {
      n("open-contract");
    }
    const it = Mv();
    function En(bt = "") {
      return y([
        bt,
        v.value ? "html-render:on" : "html-render:off",
        l.value ? "theme:dark" : "theme:light"
      ].join("\0"));
    }
    function On(bt, ...Ye) {
      return `manager:${bt}:${Ye.map((Pe) => String(Pe ?? "")).join(":")}`;
    }
    function Gs(bt) {
      return bt.kind === "message";
    }
    function Gr(bt) {
      return bt.kind === "tool-turn";
    }
    const rr = ne(() => Ht.value.filter(Gs)), Vn = ne(() => W.value.filter(Gs)), Ms = ne(() => W.value.filter(Gr)), mn = ne(() => Ms.value[0] || null), Fn = ne(() => b.value ? lt(b.value.toolTrace) : []), Ps = ne(() => !!(It.value.length || Ie.value.length || mn.value)), Ot = ne(() => N.value.length + F.value);
    function wn(bt) {
      return bt ? ["accepted_turn", "after_turn"].includes(bt.trigger) ? "已接受回合维护" : bt.trigger === "manager_chat" ? "助手问答" : "运行记录" : "工作记录";
    }
    function sn(bt = "") {
      return String(bt || "").replace(/^记忆：/, "记忆").replace(/^地图：/, "地图").replace(/^事件：/, "事件").replace(/本轮没有明确空间变化，未更新/g, "无变化").replace(/本轮没有新线索/g, "无变化").replace(/没有写入文件/g, "无变化").replace(/份档案/g, "").replace(/份状态/g, "").replace(/条线索/g, "").trim();
    }
    function Wt(bt) {
      return bt ? [
        `${De(bt.status)} · ${ee(bt)}`,
        sn(z(bt)),
        sn(J(bt)),
        sn(V(bt))
      ].filter(Boolean).join(" · ") : `记忆档案 ${E.value.length}/${It.value.length} · 最近维护完成 · 当前无后台工作`;
    }
    function Gt(bt) {
      return bt ? [wt(bt.toolTrace), Se(bt) ? "重试中" : H(bt)].filter(Boolean).join(" · ") : Lt.value || "当前无后台工作";
    }
    const Rn = ne(() => b.value ? wn(b.value) : mn.value ? "助手问答" : "工作记录"), da = ne(() => b.value ? Wt(b.value) : mn.value ? `运行中 · 工具准备中 · ${ot(mn.value)}` : Wt(null)), fa = ne(() => b.value ? Gt(b.value) : mn.value ? pt(mn.value) : Gt(null));
    return vt([o, u], ([bt, Ye]) => {
      (bt !== "chat" || Ye !== "manager") && it.reset();
    }), vt(() => `${Me.value.startIndex}:${Me.value.visibleCount}`, () => it.reset()), (bt, Ye) => (O(), B("section", {
      class: "chat-face chat-face-back chat-manager",
      "aria-hidden": T(u) === "chat"
    }, [
      g("header", Qie, [g("div", Zie, [g("button", {
        type: "button",
        class: "contract-trigger",
        title: "契约",
        "aria-label": "契约",
        onClick: Ke
      }, " 契约 "), g("button", {
        type: "button",
        class: "chat-flip-trigger",
        title: "角色聊天",
        "aria-label": "角色聊天",
        onClick: Ye[0] || (Ye[0] = (Pe) => u.value = "chat")
      }, " 角色 ")])]),
      Ps.value ? (O(), B("details", {
        key: 0,
        ref: br,
        class: "manager-work-band",
        open: T(it).isOpen(On("work-band")),
        onToggle: ct
      }, [g("summary", null, [
        g("strong", null, te(Rn.value), 1),
        g("span", null, te(da.value), 1),
        g("em", null, te(fa.value), 1)
      ]), T(it).isOpen(On("work-band")) ? (O(), B("div", tle, [
        T(b) ? (O(), B("section", {
          key: 0,
          class: Fe(["manager-work-section manager-work-current", [
            `is-${T(b).status}`,
            `tone-${T(he)(T(b))}`,
            { "is-retrying": T(Se)(T(b)) }
          ]]),
          "aria-busy": T(Se)(T(b)) ? "true" : "false"
        }, [
          g("div", rle, [Ye[18] || (Ye[18] = g("strong", null, "本次运行", -1)), g("small", null, te(wn(T(b))) + " · " + te(T(Se)(T(b)) ? "重试中" : T(De)(T(b).status)), 1)]),
          g("p", sle, te(T(ee)(T(b))), 1),
          g("p", null, te(T(R)(T(b))), 1),
          g("p", ale, te(T(Se)(T(b)) ? "重试已开始 · 正在等 API/工具返回" : T(H)(T(b))), 1),
          g("div", ole, [
            g("p", null, te(T(z)(T(b))), 1),
            g("p", null, te(T(J)(T(b))), 1),
            g("p", null, te(T(V)(T(b))), 1)
          ]),
          T(b).outputText ? (O(), B("p", ile, " 结果：" + te(T(i)(T(b).outputText, 180)), 1)) : Te("", !0),
          T(G)(T(b)) ? (O(), B("p", lle, te(T(G)(T(b))), 1)) : Te("", !0),
          T(b).status === "failed" ? (O(), B("button", {
            key: 2,
            type: "button",
            class: "manager-run-retry-button",
            disabled: T(Q) || T(Se)(T(b)),
            onClick: Ye[1] || (Ye[1] = (Pe) => T(en)(T(b)))
          }, te(T(Se)(T(b)) ? "重试中" : "重试"), 9, ule)) : Te("", !0)
        ], 10, nle)) : (O(), B("section", cle, [
          g("div", dle, [Ye[19] || (Ye[19] = g("strong", null, "记忆档案", -1)), g("small", null, te(T(E).length) + "/" + te(T(It).length), 1)]),
          g("p", null, te(T(Lt)), 1),
          T(Be) ? (O(), B("p", fle, " 当前打开：" + te(T(ht)(T(Be))), 1)) : Te("", !0)
        ])),
        mn.value && !Fn.value.length ? (O(), B("section", hle, [
          g("div", ple, [Ye[20] || (Ye[20] = g("strong", null, "工具准备中", -1)), g("small", null, te(T(pt)(mn.value)), 1)]),
          mn.value.assistantMessage.content ? (O(), B("p", mle, te(mn.value.assistantMessage.content), 1)) : Te("", !0),
          g("div", gle, [(O(!0), B(He, null, tt(mn.value.calls, (Pe) => (O(), B("div", {
            key: Pe.id,
            class: "manager-tool-item is-running"
          }, [
            g("div", vle, [g("span", null, te(Pe.name), 1), Ye[21] || (Ye[21] = g("em", null, "准备中", -1))]),
            Pe.argumentsText ? (O(), B("small", yle, te(Pe.argumentsText), 1)) : Te("", !0),
            g("p", null, te(Pe.resultText), 1)
          ]))), 128))])
        ])) : Te("", !0),
        T(b) && Fn.value.length ? (O(), B("section", _le, [g("div", ble, [Ye[22] || (Ye[22] = g("strong", null, "工具调用", -1)), g("small", null, te(T(wt)(T(b).toolTrace)), 1)]), g("div", wle, [(O(!0), B(He, null, tt(Fn.value, (Pe) => (O(), B("div", {
          key: Pe.id,
          class: Fe(["manager-tool-item", T(st)(Pe)])
        }, [
          g("div", Sle, [g("span", null, te(Pe.name), 1), g("em", null, [ku(te(T(Je)(Pe)), 1), Pe.elapsedLabel ? (O(), B(He, { key: 0 }, [ku(" · " + te(Pe.elapsedLabel), 1)], 64)) : Te("", !0)])]),
          Pe.thoughts.length ? (O(), B("details", {
            key: 0,
            class: "manager-tool-thoughts",
            open: T(it).isOpen(On("work-tool-thoughts", T(b).id, Pe.id)),
            onToggle: (Xt) => T(it).setOpenFromEvent(On("work-tool-thoughts", T(b).id, Pe.id), Xt)
          }, [g("summary", null, te(T(I)(Pe.thoughts, !1)), 1), T(it).isOpen(On("work-tool-thoughts", T(b).id, Pe.id)) ? (O(!0), B(He, { key: 0 }, tt(Pe.thoughts, (Xt, Rr) => (O(), B("div", {
            key: `${Pe.id}-stored-thought-${Rr}`,
            class: "chat-thought-block"
          }, [g("strong", null, te(Xt.label), 1), g("pre", null, te(Xt.text), 1)]))), 128)) : Te("", !0)], 40, Ale)) : Te("", !0),
          Pe.preface ? (O(), B("div", {
            key: `work-tool-preface:${T(b).id}:${Pe.id}:${En(Pe.preface)}`,
            class: "manager-tool-preface xb-tavern-markdown",
            "data-markdown-signature": En(Pe.preface),
            innerHTML: T(S)(Pe.preface)
          }, null, 8, Tle)) : Te("", !0),
          Pe.args ? (O(), B("small", Ele, te(Pe.args), 1)) : Te("", !0),
          Pe.summary ? (O(), B("p", Cle, te(Pe.summary), 1)) : Te("", !0),
          Pe.path ? (O(), B("p", Ile, te(Pe.path), 1)) : Te("", !0)
        ], 2))), 128))])])) : Te("", !0),
        Ot.value ? (O(), B("details", {
          key: 4,
          class: "manager-work-history",
          open: T(it).isOpen(On("work-history")),
          onToggle: Ye[2] || (Ye[2] = (Pe) => T(it).setOpenFromEvent(On("work-history"), Pe))
        }, [g("summary", null, [Ye[23] || (Ye[23] = g("strong", null, "历史记录", -1)), g("span", null, te(Ot.value) + " 条", 1)]), T(it).isOpen(On("work-history")) ? (O(), B(He, { key: 0 }, [(O(!0), B(He, null, tt(T(N), (Pe) => (O(), B("div", {
          key: Pe.id,
          class: Fe(["manager-history-row", [`tone-${T(he)(Pe)}`]])
        }, [g("div", null, [g("strong", null, te(wn(Pe)) + " · " + te(T(De)(Pe.status)), 1), g("small", null, te(T(ee)(Pe)), 1)]), g("span", null, te(T(wt)(Pe.toolTrace) || T(H)(Pe)), 1)], 2))), 128)), T(F) ? (O(), B("p", kle, " 更早 " + te(T(F)) + " 条已收起。 ", 1)) : Te("", !0)], 64)) : Te("", !0)], 40, xle)) : Te("", !0)
      ])) : Te("", !0)], 40, ele)) : Te("", !0),
      g("div", Mle, [g("div", {
        ref: Dn,
        class: "manager-chat-scroll",
        onScroll: Ye[9] || (Ye[9] = (...Pe) => T(be) && T(be)(...Pe)),
        onWheelPassive: Ye[10] || (Ye[10] = (...Pe) => T(Oe) && T(Oe)(...Pe)),
        onTouchstartPassive: Ye[11] || (Ye[11] = (...Pe) => T(we) && T(we)(...Pe)),
        onTouchmovePassive: Ye[12] || (Ye[12] = (...Pe) => T(ce) && T(ce)(...Pe))
      }, [
        T(de)?.active ? (O(), B("div", {
          key: 0,
          class: Fe(["manager-compaction-overlay", { resolved: T(de).resolved }]),
          role: "status",
          "aria-live": "polite"
        }, [g("strong", null, te(T(de).status), 1), g("small", null, [ku(te(T(de).currentTokens) + " / " + te(T(de).triggerTokens || "...") + " ", 1), T(de).yieldTokens ? (O(), B("span", Ple, " → " + te(T(de).yieldTokens), 1)) : Te("", !0)])], 2)) : Te("", !0),
        T(Me).hiddenBefore ? (O(), B("div", {
          key: 1,
          class: "chat-history-gate manager-history-gate",
          "data-manager-anchor-key": `gate:${T(Me).hiddenBefore}`,
          role: "button",
          tabindex: "0",
          onClick: Ye[3] || (Ye[3] = (Pe) => T(at)(!0)),
          onKeydown: [Ye[4] || (Ye[4] = Qr(Kt((Pe) => T(at)(!0), ["prevent"]), ["enter"])), Ye[5] || (Ye[5] = Qr(Kt((Pe) => T(at)(!0), ["prevent"]), ["space"]))]
        }, " 展开较早记录 " + te(T(Me).hiddenBefore) + " 条 ", 41, Rle)) : Te("", !0),
        (O(!0), B(He, null, tt(rr.value, (Pe) => (O(), B(He, { key: Pe.key }, [Pe.kind === "message" ? (O(), B("article", {
          key: 0,
          "data-manager-anchor-key": Pe.anchorKey,
          class: Fe(["manager-card manager-message", Pe.message.role === "user" ? "manager-message-user" : "manager-message-assistant"])
        }, [
          g("div", $le, [g("strong", null, te(Pe.message.role === "user" ? T(x)("user") : "助手"), 1), g("small", null, te(T(f)(Pe.message.createdAt)), 1)]),
          T(ae)(Pe.message) ? (O(), B("div", Lle, [er(g("textarea", {
            "onUpdate:modelValue": Ye[6] || (Ye[6] = (Xt) => /* @__PURE__ */ kn(k) ? k.value = Xt : null),
            class: "message-edit-box",
            rows: "6",
            "data-manager-message-editor": `manager:${Pe.message.sessionId}:${Pe.message.order}`,
            onInput: Ye[7] || (Ye[7] = (...Xt) => T(le) && T(le)(...Xt)),
            onKeydown: (Xt) => T(ke)(Xt, Pe.message)
          }, null, 40, Dle), [[sa, T(k)]]), g("div", Ole, [
            g("button", {
              type: "button",
              disabled: !T(Z)(Pe.message),
              onClick: (Xt) => T(se)(Pe.message)
            }, te(Pe.message.role === "user" ? "保存" : "保存修改"), 9, Fle),
            Pe.message.role === "user" ? (O(), B("button", {
              key: 0,
              type: "button",
              disabled: !T(Z)(Pe.message),
              onClick: (Xt) => T(se)(Pe.message, { rerun: !0 })
            }, " 保存并重发 ", 8, Ble)) : Te("", !0),
            g("button", {
              type: "button",
              onClick: Ye[8] || (Ye[8] = (...Xt) => T(d) && T(d)(...Xt))
            }, " 取消 ")
          ])])) : Te("", !0),
          T(ae)(Pe.message) ? Te("", !0) : (O(), B("div", {
            key: `history-message:${Pe.key}:${En(Pe.message.content)}`,
            class: "xb-tavern-markdown",
            "data-markdown-signature": En(Pe.message.content),
            innerHTML: T(S)(Pe.message.content)
          }, null, 8, Ule)),
          T(ae)(Pe.message) ? Te("", !0) : (O(), B("div", qle, [
            g("button", {
              type: "button",
              class: Fe(T(L)(Pe.message, "copy")),
              title: "复制",
              "aria-label": "复制",
              onClick: (Xt) => T(_)(Pe.message)
            }, " ⧉ ", 10, Hle),
            g("button", {
              type: "button",
              disabled: !T($)(Pe.message),
              class: Fe(T(L)(Pe.message, "edit")),
              title: "编辑",
              "aria-label": "编辑",
              onClick: (Xt) => T(ut)(Pe.message)
            }, " ✎ ", 10, zle),
            g("button", {
              type: "button",
              disabled: !T(p)(Pe.message),
              class: Fe(T(L)(Pe.message, "rerun")),
              title: Pe.message.role === "user" ? "从这里重问助手" : "重新生成这条助手回复",
              "aria-label": Pe.message.role === "user" ? "从这里重问助手" : "重新生成这条助手回复",
              onClick: (Xt) => T(X)(Pe.message)
            }, " ↻ ", 10, Kle),
            g("button", {
              type: "button",
              disabled: T(me),
              class: Fe(T(L)(Pe.message, "delete")),
              title: "删除",
              "aria-label": "删除",
              onClick: (Xt) => T(C)(Pe.message)
            }, " ⌫ ", 10, Wle)
          ]))
        ], 10, Nle)) : Te("", !0)], 64))), 128)),
        (O(!0), B(He, null, tt(Vn.value, (Pe) => (O(), B(He, { key: `live:${Pe.key}` }, [Pe.kind === "message" ? (O(), B("article", {
          key: 0,
          "data-manager-anchor-key": `live:${Pe.anchorKey}`,
          class: Fe(["manager-card manager-message manager-message-live", Pe.message.role === "user" ? "manager-message-user" : "manager-message-assistant"])
        }, [g("div", Vle, [g("strong", null, te(Pe.message.role === "user" ? T(x)("user") : "助手"), 1), Ye[24] || (Ye[24] = g("small", null, "正在处理", -1))]), (O(), B("div", {
          key: `live-message:${Pe.key}:${En(Pe.message.content)}`,
          class: "xb-tavern-markdown",
          "data-markdown-signature": En(Pe.message.content),
          innerHTML: T(S)(Pe.message.content)
        }, null, 8, Jle))], 10, Gle)) : Te("", !0)], 64))), 128)),
        T(me) && !Vn.value.length ? (O(), B("article", Yle, [...Ye[25] || (Ye[25] = [g("div", { class: "manager-run-title" }, [g("strong", null, "助手"), g("small", null, "正在处理")], -1), g("p", null, "正在思考...", -1)])])) : Te("", !0),
        rr.value.length ? Te("", !0) : (O(), B("p", Xle, " 还没有和助手对话。 ")),
        Ye[26] || (Ye[26] = g("div", {
          class: "chat-compose-spacer",
          "aria-hidden": "true"
        }, null, -1))
      ], 32), Wn(d8, {
        "extra-class": "manager-scroll-helpers",
        active: T(q),
        "show-top": T(Qe),
        "show-bottom": T(je),
        onTop: T(Re),
        onBottom: Ye[13] || (Ye[13] = (Pe) => T(Ae)(!0, {
          collapseWindow: !0,
          revealHelpers: !0
        }))
      }, null, 8, [
        "active",
        "show-top",
        "show-bottom",
        "onTop"
      ])]),
      g("div", jle, [g("div", { class: Fe(["chat-compose-shell manager-compose-shell", { "has-text": !!T(pe).trim() }]) }, [g("form", {
        class: "manager-compose chat-compose",
        onSubmit: Ye[17] || (Ye[17] = Kt((...Pe) => T(Ne) && T(Ne)(...Pe), ["prevent"]))
      }, [er(g("textarea", {
        ref: fr,
        "onUpdate:modelValue": Ye[14] || (Ye[14] = (Pe) => /* @__PURE__ */ kn(pe) ? pe.value = Pe : null),
        rows: "1",
        placeholder: "和助手说一句话...",
        disabled: T(me),
        onInput: Ye[15] || (Ye[15] = (...Pe) => T(m) && T(m)(...Pe)),
        onKeydown: Ye[16] || (Ye[16] = (...Pe) => T(ve) && T(ve)(...Pe))
      }, null, 40, Qle), [[sa, T(pe)]]), g("button", {
        type: "submit",
        class: "primary-action",
        disabled: !T(w),
        "aria-label": T(ie) ? "正在停止" : T(me) ? "停止" : "发送"
      }, [g("span", eue, te(T(ie) ? "..." : T(me) ? "■" : "➤"), 1), g("span", tue, te(T(ie) ? "正在停止" : T(me) ? "停止" : "发送"), 1)], 8, Zle)], 32)], 2)])
    ], 8, jie));
  }
}), rue = nue, sue = { class: "panel-head preset-page-head" }, aue = { class: "panel-pills" }, oue = {
  key: 0,
  class: "pill warning"
}, iue = { class: "pill" }, lue = { class: "preset-command-bar regex-command-bar" }, uue = { class: "preset-source-select regex-group-select" }, cue = ["value"], due = { class: "archive-search native-search regex-search-field" }, fue = { class: "settings-toolstrip" }, hue = ["aria-expanded"], pue = {
  key: 0,
  class: "regex-create-popover"
}, mue = ["onClick"], gue = {
  key: 0,
  class: "preset-status-line"
}, vue = { class: "native-settings-studio regex-studio" }, yue = { class: "native-list-panel regex-group-panel regex-library-panel" }, _ue = { class: "assistant-preset-nav-head" }, bue = ["onClick", "onKeydown"], wue = { class: "regex-row-state" }, Sue = { class: "regex-row-copy" }, Aue = { class: "regex-row-actions" }, Tue = ["onClick"], Eue = ["onClick"], Cue = ["onClick"], Iue = {
  key: 0,
  class: "inline-empty-note"
}, xue = {
  key: 0,
  class: "native-detail-panel regex-detail-panel"
}, kue = { class: "preset-preview-head" }, Mue = { class: "regex-editor-actions" }, Pue = ["disabled"], Rue = { class: "regex-editor-grid" }, Nue = { class: "regex-editor-section regex-pattern-section" }, $ue = { class: "native-form-grid regex-editor-meta" }, Lue = ["value"], Due = ["value"], Oue = { class: "preset-text-field regex-main-field" }, Fue = ["value"], Bue = { class: "preset-text-field regex-support-field" }, Uue = ["value"], que = { class: "regex-editor-section regex-placement-section" }, Hue = { class: "native-check-row placement-row regex-editor-switches" }, zue = ["checked", "onChange"], Kue = { class: "regex-editor-section regex-ephemeral-section" }, Wue = { class: "native-check-row regex-editor-switches" }, Gue = { class: "inline-check" }, Vue = ["checked"], Jue = { class: "inline-check" }, Yue = ["checked"], Xue = { class: "regex-editor-section regex-options-section" }, jue = { class: "native-check-row regex-editor-switches" }, Que = { class: "inline-check" }, Zue = ["checked"], ece = { class: "inline-check" }, tce = ["checked"], nce = { class: "native-form-grid regex-advanced-grid" }, rce = ["value"], sce = ["value"], ace = ["value"], oce = {
  key: 1,
  class: "empty-note"
}, ice = /* @__PURE__ */ bn({
  __name: "TavernRegexSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, applyActiveRegexScript: n, createRegexScript: r, deleteRegexScript: s, expandRegexGroup: a, linesFromList: o, listFromLines: l, REGEX_GROUP_BATCH_SIZE: i, regexDirty: u, regexDraft: d, regexDraftTypeLabel: f, regexGroups: m, regexGroupsForDisplay: v, regexPlacementLabel: y, regexScriptRows: S, regexSearchText: x, regexStatus: I, saveCurrentRegexScript: E, selectedRegexKey: N, selectedRegexRow: $, selectRegexScript: p, toggleRegexPlacement: w, updateRegexPatch: _ } = Ei(), b = /* @__PURE__ */ ue(!1), C = Pv("(max-width: 560px)"), k = /* @__PURE__ */ ue(""), P = /* @__PURE__ */ ue(!1), H = ne(() => {
      const Ne = k.value;
      return Ne ? v.value.filter((ce) => ce.key === Ne) : v.value;
    }), G = ne(() => m.value.filter((Ne) => Number.isFinite(Ne.scriptType))), ee = ne(() => t.value === "regex" && (!C.value || b.value));
    function J(Ne) {
      p(Ne).then((ce) => {
        ce && (b.value = !0);
      });
    }
    function z(Ne) {
      r(Ne).then((ce) => {
        ce && (P.value = !1, b.value = !0);
      });
    }
    function R() {
      b.value = !1;
    }
    function V() {
      n($.value), R();
    }
    async function le() {
      await E(), u.value || R();
    }
    function ve() {
      P.value = !P.value;
    }
    function ke(Ne) {
      return Ne.key === "scoped" ? "保存到酒馆本体当前角色" : Ne.key === "preset" ? "保存到当前酒馆预设" : "保存到全局正则";
    }
    function be(Ne, ce) {
      Ne.key !== "Enter" && Ne.key !== " " || (Ne.preventDefault(), J(ce));
    }
    return vt(t, (Ne) => {
      Ne !== "regex" && (b.value = !1, P.value = !1);
    }), (Ne, ce) => er((O(), B("div", { class: Fe(["panel step-panel native-workspace", { "is-mobile-editor-open": b.value }]) }, [
      g("div", sue, [ce[13] || (ce[13] = g("div", null, [g("h2", null, "正则")], -1)), g("div", aue, [T(u) ? (O(), B("span", oue, "未保存")) : Te("", !0), g("span", iue, te(T(S).length) + " 条", 1)])]),
      g("div", lue, [
        g("label", uue, [er(g("select", { "onUpdate:modelValue": ce[0] || (ce[0] = (we) => k.value = we) }, [ce[14] || (ce[14] = g("option", { value: "" }, " 全部正则 ", -1)), (O(!0), B(He, null, tt(T(v), (we) => (O(), B("option", {
          key: we.key,
          value: we.key
        }, te(we.label), 9, cue))), 128))], 512), [[TL, k.value]])]),
        g("label", due, [er(g("input", {
          "onUpdate:modelValue": ce[1] || (ce[1] = (we) => /* @__PURE__ */ kn(x) ? x.value = we : null),
          type: "search",
          "aria-label": "筛选正则",
          placeholder: "名称、匹配式或替换文本"
        }, null, 512), [[sa, T(x)]])]),
        g("div", fue, [g("div", { class: Fe(["regex-create-menu", { open: P.value }]) }, [g("button", {
          type: "button",
          class: "settings-icon-tool regex-create-trigger",
          title: "新增正则",
          "aria-label": "新增正则",
          "aria-expanded": P.value ? "true" : "false",
          onClick: ve
        }, [...ce[15] || (ce[15] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [g("path", { d: "M12 5v14" }), g("path", { d: "M5 12h14" })], -1)])], 8, hue), P.value ? (O(), B("div", pue, [(O(!0), B(He, null, tt(G.value, (we) => (O(), B("button", {
          key: we.key,
          type: "button",
          onClick: (Oe) => z(we)
        }, [g("strong", null, te(we.label), 1), g("span", null, te(ke(we)), 1)], 8, mue))), 128))])) : Te("", !0)], 2)])
      ]),
      T(I) ? (O(), B("div", gue, [g("span", null, te(T(I)), 1)])) : Te("", !0),
      g("div", vue, [g("aside", yue, [(O(!0), B(He, null, tt(H.value, (we) => (O(), B("div", {
        key: we.key,
        class: "regex-group-block"
      }, [
        g("div", _ue, [g("strong", null, te(we.label), 1), g("span", null, te(we.allowed === !1 ? "未允许" : `${we.visibleRows.length} / ${we.filteredCount} / ${we.totalCount} 条`), 1)]),
        (O(!0), B(He, null, tt(we.visibleRows, (Oe) => (O(), B("div", {
          key: Oe.key,
          class: Fe(["native-list-row regex-list-row", {
            selected: T(N) === Oe.key,
            disabled: Oe.script.disabled
          }]),
          role: "button",
          tabindex: "0",
          onClick: (F) => J(Oe),
          onKeydown: (F) => be(F, Oe)
        }, [
          g("span", wue, te(Oe.script.disabled ? "停" : "用"), 1),
          g("span", Sue, [g("strong", null, te(Oe.script.scriptName || "未命名正则"), 1), g("small", null, te(Oe.script.findRegex || "空匹配式"), 1)]),
          g("span", Aue, [g("button", {
            type: "button",
            class: "regex-row-icon regex-row-edit-icon",
            title: "编辑",
            "aria-label": "编辑",
            onClick: Kt((F) => J(Oe), ["stop"])
          }, [...ce[16] || (ce[16] = [g("svg", {
            viewBox: "0 0 24 24",
            "aria-hidden": "true"
          }, [g("path", { d: "M12 20h9" }), g("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])], 8, Tue), g("button", {
            type: "button",
            class: "regex-row-icon regex-row-delete-icon",
            title: "删除",
            "aria-label": "删除",
            onClick: Kt((F) => T(s)(Oe), ["stop"])
          }, [...ce[17] || (ce[17] = [Al('<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M19 6l-1 14a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1L5 6"></path><path d="M10 11v6"></path><path d="M14 11v6"></path></svg>', 1)])], 8, Eue)])
        ], 42, bue))), 128)),
        we.hiddenCount ? (O(), B("button", {
          key: 0,
          type: "button",
          class: "native-add-row",
          onClick: (Oe) => T(a)(we.key)
        }, " 再显示 " + te(Math.min(we.hiddenCount, T(i))) + " 条 ", 9, Cue)) : Te("", !0)
      ]))), 128)), T(x) && !H.value.length ? (O(), B("div", Iue, " 没有匹配的正则。 ")) : Te("", !0)]), ee.value ? (O(), B("section", xue, [T(N) || T(d).scriptName ? (O(), B(He, { key: 0 }, [g("div", kue, [
        g("strong", null, te(T(d).scriptName || "新正则"), 1),
        g("span", null, te(T(f)()), 1),
        g("div", Mue, [g("button", {
          type: "button",
          class: "regex-editor-secondary",
          onClick: V
        }, " 取消 "), g("button", {
          type: "button",
          class: "regex-editor-primary",
          disabled: !T(d).scriptName || !T(u),
          onClick: le
        }, " 保存 ", 8, Pue)])
      ]), g("div", Rue, [
        g("section", Nue, [
          ce[22] || (ce[22] = g("div", { class: "regex-section-title" }, " 查找与替换 ", -1)),
          g("div", $ue, [g("label", null, [ce[18] || (ce[18] = g("span", null, "名称", -1)), g("input", {
            value: T(d).scriptName || "",
            onInput: ce[2] || (ce[2] = (we) => T(_)({ scriptName: we.target.value }))
          }, null, 40, Lue)]), g("label", null, [ce[19] || (ce[19] = g("span", null, "匹配", -1)), g("input", {
            value: T(d).findRegex || "",
            spellcheck: "false",
            onInput: ce[3] || (ce[3] = (we) => T(_)({ findRegex: we.target.value }))
          }, null, 40, Due)])]),
          g("label", Oue, [ce[20] || (ce[20] = g("span", null, "替换为", -1)), g("textarea", {
            value: T(d).replaceString || "",
            rows: "5",
            spellcheck: "false",
            onInput: ce[4] || (ce[4] = (we) => T(_)({ replaceString: we.target.value }))
          }, null, 40, Fue)]),
          g("label", Bue, [ce[21] || (ce[21] = g("span", null, "裁剪字符串", -1)), g("textarea", {
            value: T(o)(T(d).trimStrings),
            rows: "3",
            spellcheck: "false",
            onInput: ce[5] || (ce[5] = (we) => T(_)({ trimStrings: T(l)(we.target.value) }))
          }, null, 40, Uue)])
        ]),
        g("section", que, [ce[23] || (ce[23] = g("div", { class: "regex-section-title" }, " 作用范围 ", -1)), g("div", Hue, [(O(), B(He, null, tt([
          1,
          2,
          3,
          5,
          6
        ], (we) => g("label", {
          key: we,
          class: "inline-check"
        }, [g("input", {
          type: "checkbox",
          checked: (T(d).placement || []).includes(we),
          onChange: (Oe) => T(w)(we, Oe.target.checked)
        }, null, 40, zue), g("span", null, te(T(y)(we)), 1)])), 64))])]),
        g("section", Kue, [ce[26] || (ce[26] = g("div", { class: "regex-section-title" }, " 表层替换 ", -1)), g("div", Wue, [g("label", Gue, [g("input", {
          type: "checkbox",
          checked: T(d).markdownOnly === !0,
          onChange: ce[6] || (ce[6] = (we) => T(_)({ markdownOnly: we.target.checked }))
        }, null, 40, Vue), ce[24] || (ce[24] = g("span", null, "只改显示", -1))]), g("label", Jue, [g("input", {
          type: "checkbox",
          checked: T(d).promptOnly === !0,
          onChange: ce[7] || (ce[7] = (we) => T(_)({ promptOnly: we.target.checked }))
        }, null, 40, Yue), ce[25] || (ce[25] = g("span", null, "只改提示词", -1))])])]),
        g("section", Xue, [
          ce[33] || (ce[33] = g("div", { class: "regex-section-title" }, " 其他选项 ", -1)),
          g("div", jue, [g("label", Que, [g("input", {
            type: "checkbox",
            checked: T(d).disabled === !0,
            onChange: ce[8] || (ce[8] = (we) => T(_)({ disabled: we.target.checked }))
          }, null, 40, Zue), ce[27] || (ce[27] = g("span", null, "停用", -1))]), g("label", ece, [g("input", {
            type: "checkbox",
            checked: T(d).runOnEdit === !0,
            onChange: ce[9] || (ce[9] = (we) => T(_)({ runOnEdit: we.target.checked }))
          }, null, 40, tce), ce[28] || (ce[28] = g("span", null, "编辑时执行", -1))])]),
          g("div", nce, [
            g("label", null, [ce[30] || (ce[30] = g("span", null, "查找宏", -1)), g("select", {
              value: T(d).substituteRegex ?? 0,
              onChange: ce[10] || (ce[10] = (we) => T(_)({ substituteRegex: Number(we.target.value) }))
            }, [...ce[29] || (ce[29] = [
              g("option", { value: 0 }, "不替换", -1),
              g("option", { value: 1 }, "原样替换", -1),
              g("option", { value: 2 }, "转义替换", -1)
            ])], 40, rce)]),
            g("label", null, [ce[31] || (ce[31] = g("span", null, "最小深度", -1)), g("input", {
              type: "number",
              value: T(d).minDepth ?? "",
              onInput: ce[11] || (ce[11] = (we) => T(_)({ minDepth: we.target.value === "" ? null : Number(we.target.value) }))
            }, null, 40, sce)]),
            g("label", null, [ce[32] || (ce[32] = g("span", null, "最大深度", -1)), g("input", {
              type: "number",
              value: T(d).maxDepth ?? "",
              onInput: ce[12] || (ce[12] = (we) => T(_)({ maxDepth: we.target.value === "" ? null : Number(we.target.value) }))
            }, null, 40, ace)])
          ])
        ])
      ])], 64)) : (O(), B("div", oce, " 选择一条正则，或在左侧新建。 "))])) : Te("", !0)])
    ], 2)), [[rc, T(t) === "regex"]]);
  }
}), h8 = ice, Cp = 128, lce = 54, gx = 36, uce = 72, cce = 28;
function N_(e) {
  return [...e].sort((t, n) => String(t.parent || "").localeCompare(String(n.parent || "")) || String(t.name || "").localeCompare(String(n.name || "")) || String(t.key || "").localeCompare(String(n.key || "")));
}
function dce(e) {
  const t = /* @__PURE__ */ new Set();
  return e.forEach((n) => {
    const r = /* @__PURE__ */ new Set();
    let s = n;
    for (; s?.parent; ) {
      if (r.has(s.key)) {
        r.forEach((a) => t.add(a)), t.add(s.key);
        break;
      }
      r.add(s.key), s = e.get(s.parent);
    }
  }), t;
}
function fce(e) {
  return (e % 2 === 0 ? -1 : 1) * (Math.floor(e / 2) + 1) * cce;
}
function vx(e) {
  const t = e.map(([r]) => r), n = e.map(([, r]) => r);
  return [
    Math.min(...t),
    Math.min(...n),
    Math.max(...t),
    Math.max(...n)
  ];
}
function hce(e) {
  const t = Array.isArray(e?.locations) ? N_(e.locations) : [], n = new Map(t.map((E) => [E.key, E])), r = dce(n), s = [];
  r.size && s.push("atlas_parent_cycle");
  const a = /* @__PURE__ */ new Map(), o = [];
  t.forEach((E) => {
    const N = E.parent || "";
    if (N && n.has(N) && !r.has(E.key) && !r.has(N)) {
      const $ = a.get(N) || [];
      $.push(E), a.set(N, $);
      return;
    }
    N && !n.has(N) && s.push(`atlas_parent_missing:${E.key}`), o.push(E);
  }), a.forEach((E, N) => a.set(N, N_(E)));
  const l = [], i = (E) => {
    const N = a.get(E.key) || [];
    return N.length ? Math.max(Cp, N.reduce(($, p, w) => $ + i(p) + (w > 0 ? gx : 0), 0)) : Cp;
  }, u = (E, N, $) => {
    const p = i(E), w = N + (p - Cp) / 2;
    l.push({
      key: E.key,
      x: w,
      y: $ * 132,
      width: Cp,
      height: lce,
      depth: $
    });
    let _ = N;
    return (a.get(E.key) || []).forEach((b) => {
      const C = i(b);
      u(b, _, $ + 1), _ += C + gx;
    }), p;
  };
  let d = 0;
  N_(o).forEach((E) => {
    const N = u(E, d, 0);
    d += N + uce;
  });
  const f = new Map(l.map((E) => [E.key, E])), m = /* @__PURE__ */ new Map(), v = (Array.isArray(e?.links) ? e.links : []).map((E) => {
    const N = f.get(E.from), $ = f.get(E.to);
    if (!N || !$)
      return s.push(`atlas_link_endpoint_missing:${E.id}`), null;
    const p = N.x + N.width / 2, w = N.y + N.height / 2, _ = $.x + $.width / 2, b = $.y + $.height / 2, C = _ - p, k = b - w, P = Math.abs(k) < 1, H = Math.abs(C) >= Math.abs(k) ? C >= 0 ? N.x + N.width : N.x : p, G = Math.abs(C) >= Math.abs(k) ? w : k >= 0 ? N.y + N.height : N.y, ee = Math.abs(C) >= Math.abs(k) ? C >= 0 ? $.x : $.x + $.width : _, J = Math.abs(C) >= Math.abs(k) ? b : k >= 0 ? $.y : $.y + $.height, z = (H + ee) / 2, R = (G + J) / 2;
    if (P) {
      const V = Math.round(w), le = m.get(V) || 0;
      m.set(V, le + 1);
      const ve = fce(le), ke = R + ve + (ve < 0 ? -8 : 16), be = `M ${H} ${G} C ${H} ${G + ve}, ${ee} ${J + ve}, ${ee} ${J}`;
      return {
        id: E.id,
        from: E.from,
        to: E.to,
        path: be,
        labelX: z,
        labelY: ke,
        bounds: vx([
          [H, G],
          [H, G + ve],
          [ee, J + ve],
          [ee, J],
          [z, ke]
        ])
      };
    }
    return {
      id: E.id,
      from: E.from,
      to: E.to,
      path: `M ${H} ${G} C ${H} ${R}, ${ee} ${R}, ${ee} ${J}`,
      labelX: z,
      labelY: R - 8,
      bounds: vx([
        [H, G],
        [H, R],
        [ee, R],
        [ee, J],
        [z, R - 8]
      ])
    };
  }).filter((E) => !!E);
  if (!l.length) return {
    nodes: l,
    links: v,
    viewBox: [
      0,
      0,
      640,
      360
    ],
    warnings: s
  };
  const y = Math.min(...l.map((E) => E.x), ...v.map((E) => E.bounds[0])) - 48, S = Math.min(...l.map((E) => E.y), ...v.map((E) => E.bounds[1])) - 48, x = Math.max(...l.map((E) => E.x + E.width), ...v.map((E) => E.bounds[2])) + 48, I = Math.max(...l.map((E) => E.y + E.height), ...v.map((E) => E.bounds[3])) + 48;
  return {
    nodes: l,
    links: v,
    viewBox: [
      y,
      S,
      Math.max(320, x - y),
      Math.max(220, I - S)
    ],
    warnings: s
  };
}
var pce = {
  castle: {
    path: "M254.25 15.344c-132.537 0-240.188 107.62-240.188 240.156 0 132.537 107.65 240.188 240.188 240.188 132.537 0 240.156-107.65 240.156-240.188 0-132.537-107.62-240.156-240.156-240.156zm0 18.687c122.436 0 221.47 99.034 221.47 221.47 0 65.65-28.465 124.583-73.75 165.125V238.75l14-22.78h-7.595L364 101.5l-43.813 114.47h-8.156l14.595 22.78v33.875h-36.813v-88.188l14.625-22.78h-7.593l-44.406-114.47-44.375 114.47h-7.594l14.03 22.78v123.22h-37.375v-18.094l14.594-22.782h-8.19l-43.78-114.467L95.344 266.78H87.75l14.03 22.783V416.25C59.25 375.9 32.75 318.83 32.75 255.5c0-122.436 99.064-221.47 221.5-221.47zm1.094 160.532h18.687v36.344h-18.686v-36.344zm110.156 87.97h18.688v36.312H365.5V282.53zm-246.656 22.03h18.687v36.344h-18.686v-36.344zm50.875 29.407h18.686v36.342H169.72V333.97zm170.81 30.5h18.69v36.342h-18.69V364.47z",
    slug: "castle",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/castle.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/castle.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  village: {
    path: "m109.902 35.87-71.14 59.284h142.28l-71.14-59.285zm288 32-71.14 59.284h142.28l-71.14-59.285zM228.73 84.403l-108.9 90.75h217.8l-108.9-90.75zm-173.828 28.75v62h36.81l73.19-60.992v-1.008h-110zm23 14h16v18h-16v-18zm265 18v10.963l23 19.166v-16.13h16v18h-13.756l.104.087 19.098 15.914h-44.446v14h78v-39h18v39h14v-62h-110zm-194.345 48v20.08l24.095-20.08h-24.095zm28.158 0 105.1 87.582 27.087-22.574v-65.008H176.715zm74.683 14h35.735v34h-35.735v-34zm-76.714 7.74L30.37 335.153H319l-144.314-120.26zm198.046 13.51-76.857 64.047 32.043 26.704H481.63l-108.9-90.75zm-23.214 108.75.103.086 19.095 15.914h-72.248v77.467h60.435v-63.466h50v63.467h46v-93.466H349.516zm-278.614 16V476.13h126v-76.976h50v76.977h31.565V353.155H70.902zm30 30h50v50h-50v-50z",
    slug: "village",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/village.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/village.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  road: {
    path: "M168.4 29.55 24.38 477.6l15.24 4.8L183.6 34.45l-15.2-4.9zm175.2 0-15.2 4.9 144 447.95 15.2-4.8-144-448.05zM248 32l-.8 20h17.6l-.8-20h-16zm-1.4 36-.8 20h20.4l-.8-20h-18.8zm-1.5 36-1.3 32h24.4l-1.3-32h-21.8zm-1.9 48-2 48h29.6l-2-48h-25.6zm-2.8 68-2.4 60h36l-2.4-60h-31.2zm-3.3 84-2.9 72h43.6l-2.9-72h-37.8zm-4 100-3.1 76h52l-3.1-76h-45.8z",
    slug: "road",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/road.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/road.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  house: {
    path: "m55.379 25-28.4 142H172.27L256 83.271 339.729 167H485.02l-28.4-142zM256 108.727 179.729 185H41v302h158v-87c0-18.25 7.166-33.077 18.021-42.727C227.877 347.624 242 343 256 343s28.123 4.624 38.979 14.273C305.834 366.923 313 381.75 313 400v87h158V185H332.271zm0 38.544 57 57V297H199v-92.729zm0 25.456-39 39V279h78v-67.271zM71 199h98v98H71zm272 0h98v98h-98zM89 217v30h62v-30zm272 0v30h62v-30zM89 265v14h62v-14zm272 0v14h62v-14zM71 359h98v98H71v-98zm272 0h98v98h-98v-98zm-87 2c-10 0-19.877 3.376-27.021 9.727C221.834 377.077 217 386.25 217 400v87h78v-87c0-13.75-4.834-22.923-11.979-29.273C275.877 364.376 266 361 256 361zM89 377v62h62v-62zm272 0v62h62v-62z",
    slug: "family-house",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/family-house.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/family-house.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  shop: {
    path: "M256.18 21c-23.242 0-46.577 3.01-63.186 8.54-8.304 2.763-14.868 6.196-18.808 9.558-3.94 3.36-5.167 5.956-5.186 8.96L168.943 57H41v14h430V57H342.967l.033-9.033c.01-3.002-1.17-5.55-5.057-8.895-3.887-3.344-10.407-6.773-18.677-9.535C302.724 24.014 279.42 21 256.18 21zM38.277 89l-10.443 94h80.9l20.243-86.36L122.81 183h81.07l17.114-86.68-3.096 86.68h75.75l-4.634-86.518L307.694 183h81.497l-6.167-86.36L403.266 183h80.9l-10.443-94H38.277zM25.834 201l-.51 4.588C39.822 226.445 52.968 235 64 235c11.32 0 24.852-8.89 39.8-30.96l.714-3.04h-78.68zm95.687 0-.32 4.498C135.753 226.495 148.935 235 160 235c11.293 0 24.788-8.85 39.693-30.803l.63-3.197h-78.802zm95.736 0-.156 4.352C231.69 226.455 244.908 235 256 235c11.08 0 24.28-8.525 38.85-29.576l-.237-4.424h-77.357zm94.324 0 .674 3.12c15 22.192 28.503 31.044 39.773 31.013 11.03-.03 24.212-8.62 38.772-29.637l-.32-4.496h-78.9zm95.906 0 .713 3.04C423.147 226.11 436.68 235 448 235c11.032 0 24.178-8.555 38.676-29.412l-.51-4.588h-78.68zM112 223.31C97.313 242.11 81.492 253 64 253c-13.972 0-26.884-6.906-39-19.264V487h318V279h114v208h30V233.736C474.884 246.094 461.972 253 448 253c-17.49 0-33.31-10.888-47.996-29.684-14.664 18.808-30.432 29.77-47.926 29.817-17.508.048-33.352-10.87-48.092-29.807C289.303 242.116 273.486 253 256 253c-17.492 0-33.313-10.89-48-29.69-14.687 18.8-30.508 29.69-48 29.69s-33.313-10.89-48-29.69zM55 279h258v178H55V279zm18 18v142h222V297H73zm288 0v71.064l78 .573V297h-78zM88 312h128c-108.235 8-116.31 24-128 113.11V312zm273 74.066v13.998l78 .573v-14.002l-78-.57zm0 32V487h78v-68.365l-78-.57z",
    slug: "shop",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/shop.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/shop.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  "inn-sign": {
    path: "M240.03 35.938c-1.08.01-2.168.062-3.25.124-8.644.502-17.16 2.8-22.5 5.97-5.336 3.167-7.018 5.72-6.81 9.593v.25l.78 28.156 59.97-1.28-.876-31.844c-.148-3.014-1.806-5.15-7.47-7.593C255.63 37.48 249.63 36.27 243.25 36a61.354 61.354 0 0 0-3.22-.063zm224.94 57.218L33.593 102.53l1.375 62 154.655 4.064-148.156 9.72-2.907 98.81 1.406.313 8.06 1.844-.843 8.22-6.906 67.47 429.533-9.283L464.219 283l-140.376-3.656 139.22-9.156-8.877-99.407-138.875-3.624 151.032-9.937-1.375-64.064zM276.31 368.562l-59.875 1.282L220 495.78h59.844l-3.53-127.217z",
    slug: "wooden-sign",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/wooden-sign.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/wooden-sign.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  temple: {
    path: "M256 26.2 52 135h408L256 26.2zM73 153v14h366v-14H73zm16 32v206h30V185H89zm101.334 0v206h30V185h-30zm101.332 0v206h30V185h-30zM393 185v206h30V185h-30zM73 409v30h366v-30H73zm-32 48v30h430v-30H41z",
    slug: "greek-temple",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/greek-temple.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/greek-temple.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  stairs: {
    path: "M64 448v-64h64v-64h64v-64h64v-64h64v-64h64V64h64v384z",
    slug: "stairs",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/stairs.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/stairs.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  door: {
    path: "M105 41v398h302V41H105zm55 174c18.1 0 33 14.9 33 33s-14.9 33-33 33-33-14.9-33-33 14.9-33 33-33zm0 18c-8.4 0-15 6.6-15 15s6.6 15 15 15 15-6.6 15-15-6.6-15-15-15zM73 457v30h366v-30H73z",
    slug: "door",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/door.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/door.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  forest: {
    path: "M169.92 15.654c-10.512 16.697-22.392 34.058-41.688 50.473.46.447.934.89 1.405 1.336l23.55-.406-3.527 10.7a71.131 71.131 0 0 1-1.678 4.552 183.485 183.485 0 0 0 8.872 5.788l25.052 15.33-45.99-.793c-1.954 2.474-4.06 4.9-6.305 7.275 8.8 11.517 22.045 22.713 38.675 32.766l-12.404-.365-.015 5.678c8.53 6.358 17.792 12.448 27.162 18.48 18.11-11.568 31.79-24.5 39.51-37.616l-57.353.99 25.052-15.33c8.97-5.488 16.525-11.115 23.082-16.867-5.098-5.475-9-10.962-11.257-16.69l-4.04-10.246 29.742-1.823C205.14 54.41 182.44 38.31 169.92 15.654zm234.605 13.223c-6.537 12.962-13.88 29.016-23.638 44.63 10.927 18.367 24.428 35.997 46.773 52.382l20.42 14.97-52.51-1.085c6.956 12.136 19.936 26.858 34.58 40.047 19.157 17.254 40.928 32.572 53.907 39.914l11.34 6.418c.005-4.163.005-7.382.015-13.972-17.176-11.938-29.767-25.795-36.322-42.2l-4.115-10.298 43.605-2.58c-9.91-4.358-19.747-8.263-28.723-12.06-8.498-3.594-16.23-7.107-22.724-11.23-6.495-4.122-12.102-8.954-14.858-15.943l-3.93-9.962 36.077-3.178c-24.245-20.226-47.223-48.756-59.897-75.853zM84.027 34.62c-12.52 22.656-35.22 38.757-57.843 53.234l29.738 1.822-4.037 10.244c-4.78 12.125-16.815 24.123-33.182 36.873-.295 11.21-.392 17.66-.385 17.732.005.044.19 1.1.29 3.127.038.742.04 3.177.066 4.358l13.828.797-.004.55-3.906 9.778c-1.18 2.955-2.498 6.07-4.16 9.232l11.525-7.05c37.376-22.87 50.42-48.163 67.395-74.833l-.233 6.366 4.474-1.875a72.336 72.336 0 0 1-3.305-8.25l-3.528-10.7 26.582.46C107.068 69.66 94.836 51.787 84.027 34.618zM263.06 61.036c-10.808 17.168-23.04 35.04-43.314 51.86l26.582-.458-3.527 10.7c-6.657 20.188-22.772 37.89-44.874 52.91l17.916 11.573 18.273.688-4.127 10.5c-2.305 5.863-5.355 11.4-9.01 16.65l8.737-6.19c17.913-12.69 36.773-27.495 51.8-41.534 10.026-9.365 17.978-18.52 23.083-25.914l-50.537-3.715 16.5-12.773c10.37-8.027 20.973-15.94 30.924-24.178-15.49-11.246-29.53-24.024-38.423-40.12zm93.07.68c-12.76 26.805-36.227 46.04-59.107 63.506l31.39 2.306-3.194 9.726c-4.503 13.7-16.983 27.412-32.783 42.172-10.794 10.083-23.245 20.29-35.896 29.926l49.278 3.494-3.26 9.78c-11.273 33.806-46.845 56.924-75.816 75.597 13.487 8.056 27.67 15.44 39.813 22.318 68.34 18.82 147.594 6.972 200.924-16-13.19-7.554-23.844-14.547-32.748-23.006-11.805-11.216-20.072-24.96-26.377-44.183l-3.017-9.208 51.97-6.61c-11.874-8.21-25.17-18.39-37.867-29.825-20.11-18.113-38.394-38.003-43.915-58.067L372.68 123.3l27.867.577c-21.174-20.07-33.542-41.485-44.416-62.16zM108.78 121.84c-10.807 17.168-23.038 35.04-43.313 51.86l26.582-.456-3.53 10.697c-8.24 24.998-30.974 46.186-61.625 63.185l52.94 1.99-4.126 10.496c-11.05 28.11-29.76 52.974-61.038 71.585 73.885 22.566 156.574 31.568 231.582-3.572-12.908-7.055-26.9-14.653-40.33-23.56-18.07-11.983-34.525-25.736-41.71-43.713l-4.116-10.3 48.4-2.864c-11.828-7.625-23.415-15.48-33.557-23.383-16.245-12.663-29.06-24.086-34.02-36.664l-4.04-10.247 29.74-1.823c-22.62-14.476-45.318-30.576-57.84-53.23zm-40.52 67.816-49.016.844c-.168.218-.32.44-.492.656-.276 21.495-.448 33.384-.502 42.38 23.342-13.057 40.863-28.338 50.01-43.88zm105.674 12.403a189.592 189.592 0 0 0 10.845 9.124 360.036 360.036 0 0 0 11.14 8.308c5.44-5.177 10.084-10.545 13.692-16.092l-35.678-1.34zm41.658 20.428a112.599 112.599 0 0 1-5.947 6.504 619.186 619.186 0 0 0 28.38 17.815l17.645 10.427c11.34-9.42 21.135-19.423 27.215-29.972l-67.293-4.774zm279.803 10.334-68.383 8.698c4.666 11.45 10.047 19.75 17.28 26.992l-.21-9.6c17.455-.996 34.705-3.352 51.4-7.363-.065-7.174-.084-12.42-.087-18.728zm-243.96 27.852-66.394 3.928c3.903 5.187 9.085 10.326 15.097 15.314a495.837 495.837 0 0 0 19.455 3.504c10.638-7.12 21.72-14.713 31.842-22.746zm-219.744 2.64-.29 35.618c10.746-10.37 18.692-21.97 24.83-34.694l-24.54-.924zm413.947 66.534c-14.262 4.577-29.517 8.396-45.38 11.222l5.55 132.18h42.978l-3.148-143.402zm-181.83 7.002a257.23 257.23 0 0 1-8.018 4.046l-.927 114.274h52.682l-2.426-110.496c-13.946-1.552-27.788-4.123-41.313-7.824zm120.54 6.693c-15.085 1.992-30.582 3.05-46.18 2.994l-.386 139.742h52.56l-5.994-142.737zm-353.38 9.146-.924 114.04H74.31l1.975-104.398c-15.362-2.464-30.51-5.75-45.318-9.643zm174.72 6.27c-11.576 2.87-23.226 4.935-34.898 6.284l3.915 66.195h33.065l-2.084-72.48zm-113.443 5.628-2.508 132.56 72.692-2.287-7.584-128.21c-20.97 1.29-41.936.424-62.6-2.062z",
    slug: "forest",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/forest.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/forest.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  mountains: {
    path: "M256.22 18.375c-132.32 0-239.783 107.43-239.783 239.75S123.9 497.905 256.22 497.905 496 390.446 496 258.126 388.54 18.375 256.22 18.375zm0 17.875c102.773 0 189.092 69.664 214.374 164.406l-79.313-81.47-6.967-7.155-6.688 7.47-77.22 86.438a1912.536 1912.536 0 0 0-34.467-30.063l-6.563-5.625-6.125 6.156a3510.226 3510.226 0 0 0-55.438 57.094l-76.437-83.375-6.875-7.5-6.875 7.5-71.188 77.313C51.364 119.34 143.983 36.25 256.22 36.25zm102.25 147.28-3.845 35.376 21.563-32 10.75 16.688 9.968-8.47 27.188 26.814L417 187.344l19.5 5.062 39.188 40.25.843-.812a224.379 224.379 0 0 1 1.564 26.28c0 37.033-9.06 71.917-25.063 102.595-46.25-53.48-92.512-100.116-138.75-142.283l11-12.312 33.19-22.594zm-220.16 22.75 26.438 18.782 20.22 22.032c-39.47 42.024-78.63 85.836-115.94 130.344-21.98-34.443-34.718-75.38-34.718-119.313v-.78l16.25-17.658L87.81 219.5l-17.187 54.063 41.813-51.22 27.312 32.72-1.438-48.782zm141.375 61.657 53.157 60.938-7.688-54.563L386.312 315a1631.644 1631.644 0 0 1 56.75 62.78l.188-.186C403.853 439.216 334.868 480.03 256.22 480.03c-71.76 0-135.483-33.992-176.033-86.75 19.135-22.91 38.775-45.645 58.72-68.06l56.155-33.814-29.312 76.75 61.53-73.375 6.25 32.19 19.532-36.783 47.844 69.5-21.22-91.75z",
    slug: "mountains",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/mountains.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/mountains.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  cave: {
    path: "M346.951 24.582 299.193 72.34l-101.136-7.024-40.97 80.737 68.688 25.35 37.153-19.936 8.511 15.861-44.293 23.768-79.7-29.416-70.19 55.341 35.117 58.995-.375.2 13.014 21.585 29.134 2.361 55.06-35.123 9.679 15.176-60.16 38.377-44.364-3.596-18.23-30.234-56.8 30.586 33.712 61.804-33.713 40.735L18 444.177V494h170.62l-5.6-45.592a260.658 260.658 0 0 1-5.147-4.512c-4.186-3.761-5.89-5.444-8.027-7.484l-73.13 21.797-21.339-20.484 12.467-12.985 13.777 13.225 73.068-21.78 3.784 3.667s4.24 4.09 9.216 8.636l37.797-37.248 8.133 79.54 6.3-93.444 10.364 28.387 6.281-45.112 3.14-3.091-.29-.233 22.486-27.974.465-.907.188.096 11.453-14.248 14.03 11.277-9.122 11.348 67.803 34.715 27.008-9.489 22.478 17.71 22.924-12.036 8.367 15.938-33.262 17.46-23.875-18.81-24.964 8.772-9.584-4.907 39.04 87.842L383.923 494H494v-28.512L462.713 478.2l-6.776-16.678L494 446.06V211.176l-23.438-26.463-21.654-67.371-33.547 32.666-107.77-13.873-28.019-29.096 12.967-12.486 23.629 24.539 92.867 11.953 31.442-30.615-52.79-61.801zm27.53 177.74 34.177 41.428 28.863-6.56-4.136-13.59 17.22-5.243 9.77 32.098-58.543 13.307-31.377-38.033-33.086 19.853-9.262-15.436z",
    slug: "cave-entrance",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/cave-entrance.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/cave-entrance.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  waterfall: {
    path: "M432.3 28.66c-13.4 0-26.6 6.43-40.5 19.98-4 3.94-7.8 8.06-11.6 12.12 3.7 5.59 6.1 12.64 7.7 18.46 5.6-6.15 11.1-12.41 16.5-17.76 10.1-9.98 19.1-15.59 26.9-14.66 16.1 1.9 23.7 6.55 29.6 12.81 5.9 6.26 10.1 15.12 16.4 25.18l15.2-9.58c-5.5-8.82-10-18.95-18.5-28.01-14.6-12.13-28.3-18.55-41.7-18.54zM113.7 45.63c-53.35.23-77.7 17.56-95.66 35.88l12.86 12.6c17.42-17.77 35.03-31.75 90-30.32l34.5 32.52h15c-.2-8.94-7.8-17.4-12.6-22.5-14.9-16.14-24.1-28.22-44.1-28.18zm146.7 9.79c-10.1 0-20.2.21-29.6.57 3.8 8.38 5.6 17.38 6.6 24.88 1 11.64-15.6 12.34-17.8 2.42-1.2-8.61-4.6-19.79-8.9-26.23-15.2 1-28.9 2.35-40.3 3.73 7.3 8.95 18 21.8 18 31.02.5 89.69-2.8 180.09-9.7 270.89 1.6-1.7 3.2-3.4 4.9-4.9 8.4-7.6 18.4-13.7 30-13h.1c2.6.2 5.1.7 7.5 1.4.2-16.6.6-30.9.1-44.4 5.3 5.6 10.7 10 18-.6.7 17.6-.1 35.5-.3 56.9 1.1 1.1 2 2.2 3 3.3 7.4 8.6 13.4 19.2 18.4 29.6 1.8 3.8 3.4 7.5 4.9 11.1 1.3-56.5.5-112.9-2.2-169.4 8.6 11.3 13.3 7 18-.8 2.7 56.6 3.5 113.3 2.2 169.9 11.4-11.3 25.1-22.9 41.6-22.6 4.1.1 7.8 1 11.2 2.5l.1-23.9c7.9 8.9 13.4 6.9 18 0L354 396c3.5 3.6 6.9 7.9 9.8 11.7 3.9-2 8.9-4.4 14.9-7l-5.7-73.9c6 5.5 12 10 18-1.4l5.3 68.5c6.6-2.3 13.5-4.2 20.4-5-11.2-93.4-25.2-192-44.3-296.39h.1c-1.4-7.39-3.3-14.6-5.9-19.56-2.5-4.95-5-7.23-8.7-8.07-18.2-4.12-37.7-6.65-57.3-8.04 8.6 17.14 8.4 34.97 8.6 49.66 1.1 10.8-17.2 17-18 .2-.3-18.71.2-35.46-12.7-50.98-6-.2-12-.29-18.1-.3zm90.8 31.52C362.6 130.7 371.8 206.4 378.3 251c-.9 17.6-14.2 13.4-17.8 2.6-6.5-45-16.1-121.1-26.7-162.09 9.9 7.7 16 6.9 17.4-4.57zM224 107c3.5 55 2.4 109.1-.7 162.5.2 11-15.3 14.1-18-1 3.1-53 4.2-106.3.7-160.3 8.9 13.3 14.2 9.3 18-1.2zm51.5 22.5c3.2 27.5 4.3 42.9 3.9 59.9-6.4 9.5-12.3 7.9-18-.4.4-16-.7-30.2-3.7-57.5 7 8.6 12.8 6.6 17.8-2zm52.3 27.2c3.4 50.7 4.3 90 5.6 154.8-5 5.4-9.8 11.7-18 .4-1.3-64.8-2.2-103.7-5.6-154 7.5 9.3 13.1 6 18-1.2zm-246 180.8c-10.73-.3-16.16 18.9-13.38 29.3 3.68 13.8 34.78 24.8 34.78 24.8s-2-53.6-21.4-54.1zm47.4-.4c-.8.6-1.3 1.4-1.7 2.3-4.2 9.9 22.6 23.1 22.6 23.1s5.9-17.4.6-22.8c-4.7-4.8-16.6-7-21.5-2.6zm319.5-3.8c-.5 0-.9 0-1.3.1-11.7 2.2-13.9 23.8-8.3 34.9 0 0 18.8-9.1 19.8-18 .8-6.3-4-16.8-10.2-17zm29.8 20.5c-20.3 1.1-16.8 58.6-16.8 58.6s27.7-10.5 31.2-22.9c3.5-12.3-1.6-36.4-14.4-35.7zm-282.8 17.3c-6.5 5.9-13 14.6-18.7 23.5-11.2 17.8-18.8 36-18.8 36l-3.6 8.7c-22-11.1-36.9-16.8-57.82-17.6-13.41-.5-24.76 11.5-27.43 22.4-1.41 6.4 0 14.3 9.09 25.6 120.56 14.8 310.86 21.1 411.06.4 3.5-16.4-2.3-27.7-13-37.9-11.7-11.3-29.8-19.6-45-24.5-7.3-2.4-24.9 1.2-39.4 6.9-14.6 5.8-26.4 12.5-26.4 12.5l-7.4 4.2-4.6-7s-4.8-7.3-11.3-14.4c-6.6-7.1-15.5-12.7-17.7-12.7-6.6-.1-20.7 9.2-31.5 20.3-10.7 11-18.6 22.2-18.6 22.2l-11 15.5-5.1-18.3s-5.3-19.1-14.4-38.1c-4.5-9.6-10-19-15.7-25.7-12.7-13.4-20.3-13-32.7-2zM35.58 384c-4.27-.1-7.98 1.2-9.85 4.5-8.27 14.4 30.78 39.3 30.78 39.3s6.88-26.5-.99-35.3c-4.19-4.6-12.83-8.4-19.94-8.5z",
    slug: "waterfall",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/waterfall.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/waterfall.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  skull: {
    path: "M425.344 22.22c-9.027.085-18.7 5.826-24.344 19.405-11.143 26.803-31.93 59.156-58.563 93.47a141.233 141.233 0 0 1 27.5 30.31c35.1-26.57 68.882-46.81 98.125-56.75 44.6-15.16 12.02-69.72-35.343-35.343 26.91-27.842 11.107-51.27-7.376-51.093zm-341.22.03c-18.5.378-37.604 23.962-16.343 49.875C31.523 38.635-.802 85.48 37.095 102.813c28.085 12.844 62.54 35.66 99.062 64.343 8.125-12.5 18.207-23.61 29.78-32.937-26.782-35.743-48.44-69.835-61.78-98.47-4.515-9.69-12.22-13.66-20.03-13.5zm169.5 99.688c-67.104 0-121.31 54.21-121.31 121.312 0 44.676 24.04 83.613 59.905 104.656v56.406h18.718v-47.468a120.352 120.352 0 0 0 16.093 4.78v42.688h18.69v-40.03a123.913 123.913 0 0 0 15.749 0v40.03h18.686v-42.687a120.573 120.573 0 0 0 16.094-4.78v47.467h18.688V347.97c35.92-21.03 60-60.003 60-104.72 0-67.105-54.208-121.313-121.313-121.313zm-66.874 88.218c19.88 0 36 16.12 36 36s-16.12 36-36 36-36-16.12-36-36 16.12-36 36-36zm133.563 0c19.878 0 36 16.12 36 36s-16.122 36-36 36c-19.88 0-36-16.12-36-36s16.12-36 36-36zm-66.72 52.344 29.938 48.188h-59.874l29.938-48.188zm-107.28 70.563c-40.263 32.472-78.546 58.41-109.22 72.437-37.896 17.334-5.57 64.146 30.688 30.656-30.237 36.854 21.167 69.05 36.376 36.406 15.072-32.352 40.727-71.7 72.438-112.5a140.972 140.972 0 0 1-30.28-27zm213.156 1.718a141.025 141.025 0 0 1-27.908 24.69c31.846 39.39 56.82 76.862 69.438 107.217 17.203 41.383 71.774 9.722 31.72-31.718 47.363 34.376 79.94-20.185 35.342-35.345-32.146-10.926-69.758-34.3-108.593-64.844z",
    slug: "skull-crossed-bones",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/skull-crossed-bones.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/skull-crossed-bones.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  trap: {
    path: "m106 113.773-32.963 74.375a99.166 99.166 0 0 0-3.12.704c-5.293 1.296-9.95 2.918-14.044 4.79l-8.266-53.435-25.037 87.277a108.288 108.288 0 0 0-3.338 11.635l-.26.905.07-.04c-3.632 16.665-3.56 35.726 3.597 55.818 3.306 14.022 15.515 30.355 40.24 48.135 29.193 20.992 75.05 42.954 138.495 63.86a39.272 39.272 0 0 1-.393-5.486c0-12.21 5.637-23.185 14.432-30.447l-4.07-42.73-31.54 37.69a620.827 620.827 0 0 1-27.896-11.3l-2.95-78.177-33.57 60.615c-9.068-4.85-17.496-9.773-25.294-14.75l-4.627-90.04-28.932 65.057c-7.485-6.607-13.957-13.243-19.45-19.86-4.244-20.016-.412-38.063 6.145-52.42l4.483-2.602c15.852-5.496 35.514-7.645 58.504-6.182 32.732 2.084 72.51 11.748 118.152 30.803.098-13.092 7.704-24.51 18.692-30.142l-5.597-52.59-30.14 42.78c-9.68-3.6-19.025-6.73-28.012-9.41l-4.26-68.73-32.567 59.774c-11.784-2.163-22.712-3.436-32.716-3.91l-3.77-71.97zm323.08 29.936-15.973 70.28c-9.928-1.244-20.884-1.876-32.837-1.777l-19.58-66.443-18.075 68.964c-9.342 1.12-19.127 2.635-29.316 4.55l-19.015-44.84-16.422 45.742c8.9 6.183 14.768 16.47 14.768 28.04 0 2.407-.257 4.758-.74 7.03 47.224-10.57 87.28-13.166 119.37-9.7 22.9 2.47 41.908 7.938 56.592 16.05l3.978 3.332c4.016 15.265 4.72 33.704-2.873 52.707-6.54 5.582-14.047 11.016-22.547 16.25l-17.43-69.034-19.89 87.94c-8.51 3.565-17.626 6.972-27.356 10.198l-19.724-61.576-19.274 72.674a622.45 622.45 0 0 1-29.326 6.37l-22.605-45.43-14.87 49.995a39.091 39.091 0 0 1 4.02 17.283 39.03 39.03 0 0 1-3.476 16.107c70.416-9.85 122.176-24.18 155.893-40.565 27.394-13.31 42.205-27.326 47.852-40.582 10.472-18.58 13.79-37.348 13.048-54.388l.063.053-.102-.942a107.887 107.887 0 0 0-1.308-12.035l-9.81-90.26-17.243 51.245c-3.714-2.54-8.03-4.93-13.023-7.11-.96-.417-1.95-.822-2.954-1.222L429.08 143.71zm-170.584 89.07c-8.642 0-15.443 6.802-15.443 15.445 0 3.53 1.15 6.74 3.084 9.318a161.247 161.247 0 0 1 23.101 1.844c2.91-2.793 4.705-6.733 4.705-11.162 0-8.64-6.806-15.446-15.447-15.446zm-12.652 43.468c-1.02-.003-2.032.005-3.033.025-12.016.244-22.59 2.134-30.23 4.98-5.094 1.9-8.82 4.23-10.85 6.22-2.03 1.99-2.375 3.155-2.375 4.37 0 2.426 3.81 8.437 14.258 13.844 10.448 5.408 25.905 9.714 42.992 10.954 17.088 1.24 32.486-.854 42.674-4.65 5.093-1.898 8.82-4.23 10.85-6.22 2.03-1.987 2.374-3.154 2.374-4.368 0-2.43-3.81-8.44-14.258-13.847-10.447-5.408-25.904-9.712-42.992-10.95a134.712 134.712 0 0 0-9.41-.357zm-5.688 57.215-2.96 29.51c1.08-.09 2.17-.15 3.273-.15 5.382 0 10.524 1.1 15.214 3.077l3.05-30.406a159.916 159.916 0 0 1-18.578-2.032zm.313 48.05c-11.6 0-20.798 9.2-20.798 20.8 0 11.595 9.2 20.796 20.797 20.796 11.594 0 20.798-9.203 20.798-20.798 0-11.595-9.202-20.798-20.8-20.798z",
    slug: "wolf-trap",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/wolf-trap.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/wolf-trap.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  fire: {
    path: "M282.344 15.5C205.347 26.1 179.64 117.517 196.47 178.188c21.46 77.357-72.35 84.35-51.876-38.5-27.153 44.283-34.75 83.65-29.28 116.843.016.096.043.188.06.283 6.086 32.673 23.263 58.04 46.595 76.562 14.994 11.903 32.572 20.854 51.25 26.688-59.82-145.108 153.65-144.51 89.593 3.875 20.82-4.818 39.963-13.81 55.312-27 20.46-17.582 34.67-42.43 37.688-76.313 1.43-24.45-2.34-53.204-12.875-86.563-26.74 103.853-85.32 79.05-31.344-53.03-95.86 83.26-108.066-17.42-69.25-105.532zm130.53 258.063c-1.06 6.353-2.47 12.473-4.25 18.312 14.737 5.447 26.477 11.677 34.376 17.938 9.02 7.148 12.5 13.61 12.5 19.28 0 5.673-3.48 12.164-12.5 19.313-9.02 7.15-23.072 14.23-40.844 20.188-35.544 11.915-85.786 19.53-141.25 19.53s-105.674-7.615-141.22-19.53c-17.77-5.958-31.822-13.04-40.842-20.188-9.02-7.15-12.5-13.64-12.5-19.312 0-5.672 3.48-12.133 12.5-19.28 6.886-5.46 16.706-10.874 28.875-15.783a144.01 144.01 0 0 1-6.69-17.467c-13.486 5.386-24.878 11.545-33.81 18.625-10.75 8.52-18.33 18.99-19.407 30.968h-.157v9.344c0 44.85 24.787 85.116 63.47 113.625 38.68 28.51 91.436 45.78 149.5 45.78 58.062 0 110.817-17.27 149.5-45.78 38.68-28.51 63.468-68.774 63.468-113.625h-.063c.416-2.088.658-4.224.658-6.406 0-13.205-7.945-24.7-19.563-33.906-10.634-8.43-24.763-15.56-41.75-21.625z",
    slug: "fire-bowl",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/fire-bowl.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/fire-bowl.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  chest: {
    path: "M58.553 89 42.27 211.8H79V89H58.553zM97 89v122.8h38V89H97zm56 0v122.8h56.666v-17.6h92.668v17.6H359V89H153zm224 0v122.8h38V89h-38zm56 0v122.8h36.73L453.446 89H433zM227.666 212.2v105.2h56.668V212.2h-56.668zM256 223.794a18.667 16.103 0 0 1 18.666 16.1 18.667 16.103 0 0 1-9.666 14.09v37.214h-18V254a18.667 16.103 0 0 1-9.666-14.106 18.667 16.103 0 0 1 18.666-16.1zM41 229.8v127.915l19.334 18.23V229.8H41zm37.334 0v158h355.332v-158H302.334v105.6h-92.668V229.8H78.334zm373.332 0v146.145L471 357.715V229.8h-19.334zM41 382.456V423h43.002L41 382.455zm430 0L427.998 423H471v-40.545zM92 405.8l18.24 17.2h291.52L420 405.8H92z",
    slug: "chest",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/chest.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/chest.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  table: {
    path: "M253.8 30.44c-9.5 1.2-18.1 12.3-18.1 26.8 0 8 2.7 15 6.7 19.9l4.9 5.8-7.5 1.4c-5.2 1-9 3.8-12.3 8.3-3.4 4.6-5.9 10.76-7.6 18.06-3.2 13.1-3.9 29.1-4 43.3h17.5l1.6 21.9c6.8-.3 13.8-.5 20.9-.5 7.5 0 14.8.2 21.9.6l1.5-22h16.6c0-14.3-.2-30.6-3.1-43.9-1.6-7.2-4-13.46-7.3-17.96s-7.4-7.2-13.3-8.1l-7.6-1.2 4.8-6c3.9-4.8 6.5-11.8 6.5-19.6 0-15.4-9.6-26.8-20.1-26.8zM61.9 174.4c-9.5 1.2-18.1 12.3-18.1 26.8 0 8 2.7 15 6.7 19.9l4.9 5.8-7.5 1.4c-5.2 1-9 3.8-12.3 8.3-3.4 4.6-5.9 10.8-7.6 18.1-3.2 13.1-3.9 29.1-4 43.3h17.5l4.7 64.3c12.2 2.8 25.2 2.6 36.94 0l4.3-64.3H104c0-14.3-.2-30.6-3.1-43.9-1.57-7.2-3.97-13.5-7.27-18-3.29-4.5-7.39-7.2-13.29-8.1l-7.64-1.2 4.8-6c3.94-4.8 6.54-11.8 6.54-19.6 0-15.4-9.64-26.8-20.14-26.8zm384 0c-9.5 1.2-18.1 12.3-18.1 26.8 0 8 2.7 15 6.7 19.9l4.9 5.8-7.5 1.4c-5.2 1-9 3.8-12.3 8.3-3.4 4.6-5.9 10.8-7.6 18.1-3.2 13.1-3.9 29.1-4 43.3h17.5l4.7 64.3c12.2 2.8 25.2 2.6 36.9 0l4.3-64.3H488c0-14.3-.2-30.6-3.1-43.9-1.6-7.2-4-13.5-7.3-18s-7.4-7.2-13.3-8.1l-7.6-1.2 4.8-6c3.9-4.8 6.5-11.8 6.5-19.6 0-15.4-9.6-26.8-20.1-26.8zm-190 19c-36.7 0-69.9 5.3-93.2 13.5-11.6 4.1-20.7 9-26.3 13.7-5.7 4.7-7.5 8.6-7.5 11.8 0 3.2 1.8 7.1 7.5 11.8 5.6 4.7 14.7 9.5 26.3 13.7 23.3 8.2 56.5 13.5 93.2 13.5s69.9-5.3 93.2-13.5c11.6-4.2 20.7-9 26.3-13.7 5.7-4.7 7.5-8.6 7.5-11.8 0-3.2-1.8-7.1-7.5-11.8-5.6-4.7-14.7-9.6-26.3-13.7-23.3-8.2-56.5-13.5-93.2-13.5zm-105 79.2v45.8h18v-39.8c-4.2-1.2-8.3-2.4-12.2-3.8-2-.7-3.9-1.4-5.8-2.2zm210 0c-1.9.8-3.8 1.5-5.8 2.2-3.9 1.4-8 2.6-12.2 3.8v39.8h18zm-107.1 19c-9.5 1.2-18.1 12.3-18.1 26.8 0 8 2.7 15 6.7 19.9l4.9 5.8-7.5 1.4c-5.2 1-9 3.8-12.3 8.3-3.4 4.6-5.9 10.8-7.6 18.1-3.2 13.1-3.9 29.1-4 43.3h17.5l4.7 64.3c12.2 2.8 25.2 2.6 36.9 0l4.3-64.3h16.6c0-14.3-.2-30.6-3.1-43.9-1.6-7.2-4-13.5-7.3-18s-7.4-7.2-13.3-8.1l-7.6-1.2 4.8-6c3.9-4.8 6.5-11.8 6.5-19.6 0-15.4-9.6-26.8-20.1-26.8z",
    slug: "round-table",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/round-table.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/round-table.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  bed: {
    path: "M239.802 74.44v.19h12.275v29.433h.004v31.265a85.275 85.275 0 0 0-10.095-1.084c-26.8-1.293-55.033 8.738-73.23 13.36l-7.545 1.92.582 5.564c-.46-.176-.917-.356-1.387-.525l-.705-.256-.74-.135c-4.097-.747-8.27-1.193-12.48-1.39-29.477-1.372-60.834 9.463-81.174 14.523l-7.612 1.892.836 7.8c.605 5.644 1.218 11.59 2.774 17.493-10.642 13.072-10.078 18.35-8.417 27.184l211.14 73.916v74.053C184.03 336.45 106.252 295.828 25.582 264.49v-170h18v.125h12.374v34.77l165.848-21.414V74.44zm-2.088 77.845c1.203-.013 2.39 0 3.564.04 2.818.095 5.505.396 8.09.84 13.548 5.197 20.296 12.637 24.25 21.462-23.255 9.644-44.174 13.507-62.515 15.736-5.277-1.15-9.503-2.466-12.944-3.894a63.312 63.312 0 0 0-16.522-20.16 91.603 91.603 0 0 1-.584-3.33c17.414-4.63 38.614-10.504 56.66-10.695zm-94.35 18.528c1.38-.014 2.743 0 4.09.046a69.18 69.18 0 0 1 9.26.95c15.757 5.89 23.546 14.435 28.002 24.526-26.44 10.85-50.22 15.162-70.965 17.62-17.42-3.692-25.116-8.99-29.17-14.665-3.072-4.302-4.524-9.753-5.53-16.518 19.495-5.077 43.62-11.753 64.314-11.96zM291.8 186.295l26.406 7.453c-59.194 10.41-125.095 28.732-165.18 45.766l-27.443-9.17c21.235-3.146 45.785-8.753 72.568-20.846l5.29-2.39c1.72.44 3.5.853 5.35 1.232l1.42.29 1.44-.17c21.562-2.54 47.905-7.294 77.15-20.782zm68.797 19.418 51.336 14.49-147.905 38.377v17.6l-82.517-27.147-1.77-.59c49.176-17.717 124.438-36.303 180.857-42.73zm127.79 13.68v90.57L283.69 372.127v-99.62zM23.613 282.45 60.837 299v14.674L39.98 322.13l-16.366-10.57zm463.26 49.243v34.995l-21.91 9.515-16.367-7.4v-25.487zm-234.453 52.49 11.608 5.16 9.442 4.196 19.342-6.87v40.848l-22.704 10.043-17.687-12.685z",
    slug: "bed",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/bed.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/bed.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  barrel: {
    path: "M256 41c-43.696 0-83.28 3.58-111.37 9.197-14.047 2.81-25.26 6.196-32.21 9.483-3.476 1.643-5.842 3.293-6.88 4.306l-.013.014.014.014c1.038 1.013 3.404 2.663 6.88 4.306 6.95 3.287 18.163 6.674 32.21 9.483C172.72 83.42 212.303 87 256 87s83.28-3.58 111.37-9.197c14.047-2.81 25.26-6.196 32.21-9.483 3.476-1.643 5.842-3.293 6.88-4.306l.013-.014-.014-.014c-1.038-1.013-3.404-2.663-6.88-4.306-6.95-3.287-18.163-6.674-32.21-9.483C339.28 44.58 299.697 41 256 41zm-80 15a32 8 0 0 1 32 8 32 8 0 0 1-32 8 32 8 0 0 1-32-8 32 8 0 0 1 32-8zm-75.168 26.594a1745.469 1745.469 0 0 0-12.05 55.28 671.56 671.56 0 0 0 50.327 10.995c2.988-17.203 6.707-34.438 11.27-51.708-3.186-.547-6.3-1.113-9.282-1.71-14.91-2.98-27.13-6.49-36.37-10.86a65.244 65.244 0 0 1-3.896-1.998zm310.336 0a66.12 66.12 0 0 1-3.895 1.998c-9.24 4.37-21.462 7.88-36.37 10.86-2.93.587-5.99 1.142-9.116 1.68 5.27 16.954 9.544 34.033 12.953 51.22a668.043 668.043 0 0 0 48.424-10.754 1745.49 1745.49 0 0 0-11.996-55.004zm-67.4 17.238c-23.065 2.982-49.9 4.803-78.768 5.117v54.198a660.337 660.337 0 0 0 91.975-7.773c-3.45-17.334-7.805-34.523-13.207-51.543zm-175.475.008c-4.647 17.345-8.416 34.67-11.426 51.98A660.497 660.497 0 0 0 247 159.174V104.95c-28.842-.314-55.656-2.133-78.707-5.11zm-84.38 55.277-5.518 30.088c128.542 30.936 239.89 29.948 353.384.137l-4.98-30.172c-110.776 28.798-228.035 29.785-342.886-.053zm350.634 48.176a729.99 729.99 0 0 1-50.83 11.312c3.656 47.603 1.776 95.87-3.55 144.49 18.6-3.803 36.796-8.527 54.468-14.17C439.592 314.762 439 291.606 439 256c0-14.915-1.77-33.334-4.453-52.707zm-357.13.256C74.758 222.827 73 241.15 73 256c0 23.794 4.678 57.228 10.424 89.404a635.542 635.542 0 0 0 50.27 12.418c-4.532-47.516-6.03-95.247-2.577-143.222-17.624-3.063-35.507-6.74-53.7-11.05zm71.546 13.944c-3.336 47.978-1.63 95.883 3.164 143.813a641.805 641.805 0 0 0 94.873 9.33V225.94c-31.995-.576-64.57-3.38-98.037-8.446zm216.902.19A677.143 677.143 0 0 1 265 226.023V370.8c32.816-.174 65.224-2.93 96.64-8.25 5.61-49.032 7.722-97.417 4.225-144.866zM86.66 364.93l8.29 31.9c104.15 32.39 225.75 32.428 326.077.733l8.272-32.264c-106.024 31.367-228.01 31.34-342.64-.37zm11.236 51.666c3.816 16.945 6.585 28.183 6.704 28.662.792 2.185 4.694 6.427 12.96 10.37 7.587 3.616 18.215 6.947 30.77 9.704a1879.47 1879.47 0 0 1-6.016-37.74 546.987 546.987 0 0 1-44.418-10.996zm318.366 1.31c-14.934 4.36-30.254 8.052-45.852 11.086a1770.631 1770.631 0 0 1-6.43 36.272c12.422-2.745 22.935-6.05 30.46-9.637 8.376-3.994 12.302-8.315 13.02-10.473a1427.72 1427.72 0 0 0 8.802-27.248zm-255.217 13.18a1936.038 1936.038 0 0 0 6.144 37.74c23.637 3.684 51.525 5.748 79.81 6.11V439.24c-28.815-.644-57.66-3.36-85.955-8.154zm190.55 1.223c-28.306 4.484-57.373 6.847-86.595 7.07v35.556c28.358-.363 56.317-2.437 79.994-6.14a1820.27 1820.27 0 0 0 6.602-36.487z",
    slug: "barrel",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/barrel.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/barrel.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  flag: {
    path: "M356.688 19.188c-6.83-.032-12.837.64-18.125 1.843-24.178 5.495-36.437 21.983-50.938 41.157-14.5 19.175-31.317 40.993-62.78 47.47C195.08 115.78 154.27 108.253 91.25 78.5c-10.013 44.88-33.406 128.62-60.906 178.656 60.093 28.5 97.245 34.926 121 30.875.01 0 .02.004.03 0 21.59-5.827 34.487-20.094 47.876-43.092 17.014-29.227 32.563-72.198 60.25-123.188l16.406 8.938c-16.69 30.735-28.802 58.617-40 82.937 8.552-6.512 18.633-11.77 31.063-14.594 27.71-6.296 65.053-.495 121.655 24.75-6.932-29.276-1.885-61.913 9.875-92.218 12.686-32.69 33.038-62.907 56.28-84.03-42.595-19.553-73.152-27.554-95.124-28.282-1.01-.033-1.993-.058-2.97-.063zm127.54 14.144a10.775 10.775 0 0 0-2.664.266c-4.378.977-8.94 4.424-12.084 11.097L289.53 497.31h23.61L490.972 49.368c3.475-10.153-.75-15.86-6.746-16.035z",
    slug: "flying-flag",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/flying-flag.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/flying-flag.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  star: {
    path: "m436.406 29.625-18.094 42.22-48.562 5.905 42.156 25.656 1.375 13.47C367.938 90.74 302.435 75.36 214.78 82.31l-20.186-3.343-24.125-38.407.5 39.78-49.22 16.438 55.063 4.564 7.843 33.78 17.094-37.78 17.906-2.75c203.993 22.03 277.475 204.75 77.875 207.625l5.22-37.595 36.75-43.72-51.344-24.968-30.22-48.468-39.623 41.124-4.125 1.03C-8.4 163.078-31.708 304.485 98.844 376.125l-11.938 12.688L39.844 374.5l33.03 39.406-15.124 42.53 36.375-31.155 47.03 18.095-30.374-43.875 4.69-15.03c62.43 28.648 153.852 42.16 270.5 20.717-241.042 33.38-364.142-137.94-219.283-195.687l23.032 43.25-4 56.97 56.218-9.97 19.25 7.813c218.255 102.608 297.46-83.917 171.843-177.75l14.376-22.22 46.47-16.5-41.907-14.812-15.564-46.655zM34.53 79.03l4.845 26.095-19.47 22 27.22-3.25 17.563 23.344.687-29.47 24.78-17.906-33.218-1.72L34.531 79.03zm358.564 298.5 14.25 51.658-31.375 41.062 49.592-12.688 33.688 34.282-2.53-51.406 35.217-30.375-51.593 1.562-47.25-34.094z",
    slug: "star-swirl",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/star-swirl.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/star-swirl.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  torch: {
    path: "M476.72 51.375c-5.28 30.185-124.992 107.9-82.47 16.875-80.216 45.38-107.557 23.42-78.53-5.656-54.825 2.8-62.753 88.173-55.345 112.406l17.438 19.125c-2.14-31.218.404-48.445 19.5-71.47 13.764 20.614 18.495 33.702 52.062 6.97-9.393 53.52 54.61 18.747 88.75 10.938-15.248 14.048-35.153 32.723-38.875 55.468-1.24 7.587 6.208 17.925 14.125 25.626-9.443 2.236-41.474 8.91-38.563 26.22 2.912 17.31 12.14 11.885 3.5 15.28-12.403 2.766-21.156 5.58-39.593-2.187l18.874 20.717c28.39 14.79 73.904 7.306 83.594-14.875-14.778-1.22-27.125-4.674-33-11.53 44.022-8.34 66.764-39.243 85.78-75.032-33.638 18.95-42.158 17.784-56 16.313 35.514-14.365 46.876-108.943 38.75-145.188zM246.874 186.063l-56.78 70.125 79.186 86.906 75.095-50-97.5-107.03zm-62.344 90.125L21.657 467.625l21.438 23.53 205.75-144.374-64.313-70.592z",
    slug: "torch",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/torch.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/torch.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  portal: {
    path: "M298.736 21.016c-99.298 0-195.928 104.647-215.83 233.736-7.074 45.887-3.493 88.68 8.512 124.787a243.08 243.08 0 0 1-11.467-20.034c-16.516-32.335-24.627-65.378-25-96.272-11.74 36.254-8.083 82.47 14.482 126.643 27.7 54.227 81.563 91.94 139.87 97.502a136.491 136.491 0 0 0 17.364 1.108c99.298 0 195.93-104.647 215.83-233.736 9.28-60.196.23-115.072-22.133-156.506 21.625 21.867 36.56 45.786 44.617 69.496.623-30.408-14.064-65.766-44.21-95.806-33.718-33.598-77.227-50.91-114.995-50.723-2.328-.118-4.67-.197-7.04-.197zm-5.6 36.357c40.223 0 73.65 20.342 95.702 53.533 15.915 42.888 12.51 108.315.98 147.858-16.02 54.944-40.598 96.035-79.77 126.107-41.79 32.084-98.447 24.39-115.874-5.798a43.356 43.356 0 0 1-3.38-7.385c11.724 14.06 38.188 14.944 61.817 1.3 25.48-14.71 38.003-40.727 27.968-58.108-10.036-17.384-38.826-19.548-64.307-4.837-9.83 5.676-17.72 13.037-23.14 20.934a81.918 81.918 0 0 1 1.626-3.88c-18.687 24.49-24.562 52.126-12.848 72.417 38.702 45.923 98.07 25.503 140.746-6.426 37.95-28.392 72.32-73.55 89.356-131.988a295.488 295.488 0 0 0 3.467-13.008c-.286 2.218-.59 4.442-.934 6.678-16.807 109.02-98.412 197.396-182.272 197.396-35.644 0-65.954-15.975-87.74-42.71-26.492-48.396-15.988-142.083 4.675-185.15 26.745-55.742 66.133-122.77 134.324-116.804 46.03 4.027 63.098 58.637 39.128 116.22-8.61 20.685-21.192 39.314-36.21 54.313 24.91-16.6 46.72-42.13 59.572-73 23.97-57.583 6.94-113.422-39.13-116.805-85.737-6.296-137.638 58.55-177.542 128.485a318.924 318.924 0 0 0-20.977 60.707 263.164 263.164 0 0 1 2.493-22.652C127.67 145.75 209.275 57.373 293.135 57.373z",
    slug: "portal",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/portal.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/portal.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  tree: {
    path: "M348.92 15.633a52.31 52.31 0 0 0-4.168.113l-.002.006c-13.368.9-26.782 7.143-39.715 19.27-20.056-5.876-37.08-10.614-57.232-.58-37.81-11.887-75.686-13.068-104.54 19.27-42.477-10.09-87.2 9.883-76.51 58.985-47.074 14.955-56.408 78.2-.58 73.586 7.938 46.78 41.118 55.937 83.514 40.883 19.41 24.008 46.462 39.182 77.282 46.443.28 5.105.085 10.33-.566 15.68-5.807 47.77-50.79 105.31-121.367 154.696L81.12 460.723l29.16-1.354c29.003-1.347 48.05-2.39 58.935-1.55-5.818 6.555-11.317 12.37-16.28 17.297L134.04 493.88l26.26-4.417c23.887-4.017 44.442-7.94 59.927-8.363 15.484-.423 24.434 2.174 29.916 9.552l7.33 9.864 6.597-10.368c6.53-10.26 13.576-12.87 27.227-12.257 13.65.613 32.84 6.013 57.043 13.3l29.69 8.94-21.036-22.782a394.31 394.31 0 0 1-23.357-27.786 130.6 130.6 0 0 1 8.015.596c12.14 1.3 26.887 3.978 40.67 8.067l26.127 7.75-17.145-21.184c-32.25-39.843-64.6-69.885-69.768-104.74-2.584-17.43.916-36.76 15.28-61.374a196.408 196.408 0 0 1 6.49-10.31 286.497 286.497 0 0 0 23.634-7.913c27.094 24.058 78.02 4.07 50.812-27.45 58.72-14.457 105.89-59.625 26.865-95.196 46.613-36.08 17.39-90.36-40.297-88.188-14.636-21.026-34.603-33.72-55.4-33.987zm11.277 65.488c-42.59 32.678-48.978 37.184-53.506 65.825.317 3.472.597 7.013 1.103 10.11 3.153 19.296 10.22 32.648 21.43 39.89 11.21 7.243 24.752 7.554 38.814 5.344 3.876-.61 7.86-1.446 11.91-2.397-26.82 26.337-45.555 49.373-57.742 70.253-15.75 26.986-20.6 50.827-17.404 72.385 5.283 35.632 30.606 63.305 56.864 93.556-6.317-1.147-12.557-2.146-18.21-2.752-8.907-.955-16.085-1.496-22.225-1.018-15.78-23.394-27.137-46.75-28.396-64.013l-16.875 1.23c2.246 30.812 24.037 66.155 49.786 97.565-12.35-3.28-23.554-5.657-33.69-6.112-13.696-.614-26.363 3.152-35.763 12.244-9.7-7.222-22.628-9.424-36.527-9.044-11.164.304-23.902 2.172-37.366 4.402 44.064-49.242 105.015-137.42 104.04-226.442l-16.92.186c.81 73.714-47.797 152.037-88.25 201.336-11.867-3.388-25.73-3.414-44.05-2.566 60.184-46.96 99.88-99.595 105.98-149.77 3.418-28.11-4.46-55.45-25.085-78.033-16.147-17.684-39.853-32.51-71.576-44.105 7.55-1.88 14.953-4.365 21.462-9.18 6.738-4.988 11.306-13.447 12.117-23.892 4.84 5.99 9.412 10.25 15.808 12.004 3.987 1.093 8.89.41 12.398-1.818 1.18-.75 2.055-1.72 2.987-2.65 1.008 13.36 2.204 26.116 4.996 35.543 1.305 4.41 2.77 8.54 6.917 11.938 4.147 3.4 11.522 3.424 16.02.846 2.954-1.693 5.624-4.19 8.255-7.105.53 5.72 1.776 10.502 4.068 14.624 1.406 2.527 3.53 5.005 6.594 6.473 3.065 1.467 6.72 1.527 9.586.712 5.732-1.63 9.41-5.37 13.29-10.104 8.362-10.204 17.167-27.234 25.41-49.5-1.647-26.645-3.49-39.06-35.522-58.758 25.123 13.013 47.678 20.495 89.27-7.207z",
    slug: "oak",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/oak.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/oak.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  rock: {
    path: "m317.727 108.904-95.192 96.592-26.93 86.815 17.54 36.723 20.417 9.287 33.182-55.082 11.297-3.61 61.75 26.85 20.26-12.998 4.47-43.7 11.42 53.634-10.622 14.162 3.772 1.64 5.238 6.5 6.832 34.343 55.977-66.775 13.98.23 22.397 28.575-9.453-52.244L434.01 166.81l-116.28-57.906zM123.61 120.896 94.08 173l-4.603 27.62 25.98-8.442 11.704 7.377.084.634 28.295 59.865 13.773-4.543 10.94 4.668 3.922 8.21 19.517-62.917-1.074-33.336-40.15-.522-29.732-23.78 34.06 10.888 42.49-7.727 26.034 15.88 36.282-36.815a332.698 332.698 0 0 0-8.58-3.52l-79.58 10.126-3.528-.25-56.307-15.52zm249.33 36.422 47.058 66.02 2.107 62.51-25.283-59.698-65.322-60.404 41.44-8.428zm-262.2 55.32-64.234 20.876-16.71 78.552 50.794 5.582.596-7.14 37.662-36.707-8.108-61.16zm56.688 62.45-36.44 12.016-31.644 30.84 22.588 30.867 57.326 1.74 16.5-16.16-28.33-59.302zm110.666 24.19-44.307 73.546-.033 57.14 97.264 12.216 44.242-19.528-17.666-88.806-79.5-34.567zM443.8 313.36l-46.843 55.876.287 1.774 65.147 13.887 25.78-14.926-44.37-56.613zm-138.382 15.89 39.23 22.842 13.41 50.658-26.82 23.838-45.015-2.553 38.562-28.242 2.483-39.23-21.85-27.312zm-238.37 53.838-8.77 28.51 13.152 48.498 91.037-11.91 1.32-26.418-62.582-31.995-34.156-6.684z",
    slug: "stone-pile",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/stone-pile.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/stone-pile.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  water: {
    path: "M107.156 17.72c-22.515 130.852-89.5 157.227-89.5 248.968 0 46.03 45.485 79.843 89.5 79.843 1.7 0 3.402-.066 5.094-.155.82-6.47 1.875-12.74 3.125-18.844a69.49 69.49 0 0 1-9.063.595c-38.51 0-69.656-31.488-69.656-70.72 0-39.23 31.145-70.717 69.656-70.717.576 0 1.147.017 1.72.03-17.205 9.417-28.876 28.2-28.876 49.5 0 27.927 20.063 51.5 46.344 55.345 10.542-29.276 25.437-55.543 41.125-83.125a3540.08 3540.08 0 0 0 9.25-16.344c-20.93-44.947-53.572-89.35-68.72-174.375zm296.531 0c-14.905 86.624-49.318 127.455-70.875 171.78 4.337 8.4 8.666 16.6 12.907 24.625.27.514.54 1.018.81 1.53 12.664-17.583 33.143-28.968 56.283-28.968.585 0 1.167.017 1.75.032-17.214 9.413-28.907 28.192-28.907 49.5 0 30.555 24.045 55.905 53.938 55.905 16.704 0 31.57-7.9 41.437-20.22-6.557 32.216-34.585 56.22-68.217 56.22-3.16 0-6.266-.245-9.313-.656a259.132 259.132 0 0 1 3.656 18.78c2.18.163 4.354.28 6.53.28 45.005 0 87.752-31.642 87.752-75.342 0-88.064-64.484-122.863-87.75-253.47zM256.72 52.47c-18.814 73.33-47.047 123.11-71.69 166.436-29.68 52.186-53.686 94.326-53.686 161.313 0 31.412 15.432 58.706 39.094 78.624 23.66 19.917 55.448 31.906 86.125 31.906 31.66 0 62.733-11.278 85.468-29.906 22.736-18.628 37.095-44.098 37.095-73.78 0-64.192-23.26-108.428-52.125-163.064-23.955-45.34-51.36-97.692-70.28-171.53zm-1.5 212.56c6.448 0 12.75.62 18.874 1.75-31.1 8.977-53.844 38.675-53.844 73.5 0 41.822 32.78 76.314 73.47 76.314 28.857 0 53.72-17.368 65.75-42.375-1.763 57.015-47.717 102.374-104.25 102.374-57.654 0-104.314-47.18-104.314-105.78 0-58.603 46.66-105.783 104.313-105.783z",
    slug: "droplets",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/droplets.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/droplets.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  heart: {
    path: "m142.094 20.063 37.594 76.78a94.196 94.196 0 0 0-27.47-4.187 94.293 94.293 0 0 0-23 2.78c-9.593 2.388-18.805 6.298-27.312 11.533L18.53 23.593v70.312l56.19 37.813c-12.143 16.172-19.905 36.81-20.657 60.78-.14 4.438-.026 8.76.312 12.97l-35.844-9.657v41.843l44.72 3.094c3.274 7.523 7.29 14.613 11.906 21.28 9.205 13.295 20.61 25.098 33 36.345L18.53 322.188V368.5l109.157-53.28c13.032 10.868 26.395 21.638 39.094 33L20.345 494.28H91.78l88.97-132.936c14.037 13.884 26.635 28.95 36.188 46.344l-23.22 86.593h41.595l4.593-65.936 1.938-5.03c9.503-24.743 24.99-45.3 42.937-63.94l35.626 134.907h47.22L296.218 348c9.033-8.604 18.47-16.856 27.936-25.03L492.78 491.593v-70.938l-158.374-106.53c1.105-.955 2.21-1.922 3.313-2.876 10.166-8.807 20.203-17.72 29.467-27.063l125.594 33.907v-42.22l-111-7.718c13.545-16.36 24.01-34.353 28.533-55.437l82.468-21.94v-47.843l-80.186 39.47c-1.48-25.603-11.335-46.322-25.938-61.25L487.75 20.062h-70.375l-55.906 83.062a92.902 92.902 0 0 0-22.376-7.406c-14.422-2.82-29.657-2.43-44.47 1.186l20.813-76.844h-41.843l-5.938 87.438c-12.74 7.1-24.463 16.97-34.187 29.688-5.627-8.07-11.943-14.987-18.72-20.813L189 20.062h-46.906zm10.125 91.124c27.033-.222 54.69 13.835 72.655 47.563l7.906 14.813 8.44-14.532c21.855-37.654 61.142-51.45 94.31-44.968 33.17 6.483 59.992 31.78 58.5 78.438-1.38 43.233-31.41 72.474-68.53 104.625-32.58 28.218-70.07 57.918-92.594 100.78-23.6-42.682-62.317-71.126-95.156-98.624-18.69-15.648-35.51-30.997-47.22-47.905-11.707-16.908-18.508-35.107-17.78-58.28 1.364-43.454 28.753-71.513 61-79.532 6.046-1.504 12.23-2.324 18.47-2.376zM314.97 135.5c-11.794 0-23.042 3.426-32.25 9.406 20.885-2.46 44.746 10.98 59.03 35.72 12.43 21.526 14.015 45.497 5.938 62.468 15.646-10.572 25.78-28.802 25.78-49.094a58.297 58.297 0 0 0-58.5-58.5z",
    slug: "shining-heart",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/shining-heart.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/shining-heart.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  lips: {
    path: "M166.844 112.344c-42.01 38.554-97.815 75.023-147.53 86.03 39.097 14.38 89.036 18.718 134.717 9.157 53.264 31.33 128.095 32.185 184.876 0 44.99 15.297 96.346 13.18 149.688-9.31-59.564-9.606-125.004-51.588-155.25-85.876-55.502 39.62-111 32.146-166.5 0zm-133.72 114.03c17.33 29.572 48.467 79.248 89.75 131.064 27.24 34.188 76.606 31.093 129.97 31.093 49.298 0 102.29.706 127.625-31.092 37.724-47.35 68.59-92.143 88.217-122-124.874 84.53-316.773 93.18-435.562-9.063z",
    slug: "lips",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/lips.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/lips.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  lovers: {
    path: "M132.255 18.093c-32.842 0-59.592 33.923-59.592 75.935 0 21.274 7.079 40.468 18.092 54.284C22.623 158.879 7.903 253.733 24.1 339.874h23.563L46.63 234.53l18.688-.185 1.25 126.685 8.75 132.877h46.875v-172.29c-15.257-18.713-25.615-39.351-26.424-64.825l-.004-.1v-.1c-.154-15.988 6.478-35.643 19.219-52.566 12.741-16.922 32.481-30.98 57.783-31.29 17.252-.212 35.458 5.013 51.338 14.771-12.131-20.214-29.245-34.177-51.26-38.32 11.507-13.88 18.971-33.345 18.971-55.157.097-36.903-24.805-76.041-59.562-75.937zm247.841 0c-32.842 0-59.591 33.923-59.591 75.935 0 21.274 7.078 40.468 18.091 54.284-25.296 3.923-43.223 19.473-54.945 41.875 16.803-11.476 36.697-17.682 55.465-17.452 25.302.311 45.042 14.369 57.783 31.291 12.741 16.923 19.373 36.578 19.219 52.567v.1l-.004.1c-.826 26.007-11.608 46.974-27.393 65.997v171.113h44.315l11.5-154.029h.03l1.062-105.531 18.687.185-1.062 105.346h24.53c18.363-88.46-4.724-178.95-67.095-190.687 11.507-13.88 18.97-33.345 18.97-55.157.098-36.903-24.805-76.041-59.562-75.937zM172.99 190.733c-18.837.232-33.281 10.381-43.625 24.12-10.326 13.715-15.675 31.13-15.594 41.506 1.226 36.63 26.557 61.833 61.219 92.044 26.758 23.322 58.19 48.519 80.617 83.38l.357-.637.336.601c22.427-34.843 53.847-60.03 80.596-83.344 34.662-30.211 59.993-55.414 61.219-92.044.081-10.376-5.268-27.791-15.594-41.506-10.344-13.739-24.788-23.888-43.625-24.12-28.453-.349-63.84 20.288-74.414 52.022l-8.539 25.625-8.54-25.625c-10.573-31.734-45.96-52.37-74.413-52.021zm-32.11 150.989v152.181h44.315l7.893-105.714c-9.94-9.023-20.098-17.651-29.924-26.215-7.726-6.734-15.242-13.43-22.283-20.252zm229.157.93c-6.762 6.5-13.94 12.894-21.315 19.322-10.636 9.27-21.66 18.622-32.375 28.463l6.815 103.47h46.875z",
    slug: "lovers",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/lovers.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/lovers.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  cherish: {
    path: "M159.361 28.344c-51.352.63-79.54 56.303-79.244 86.957 1.504 47.346 34.181 79.356 74.82 114.763 35.351 30.799 76.091 63.283 100.907 109.987.05-.092.105-.18.156-.272.051.092.105.18.156.272 24.816-46.704 65.556-79.188 100.906-109.987 40.64-35.407 73.317-67.417 74.82-114.763.297-30.654-27.891-86.327-79.243-86.957C314.636 27.878 270 54 256 96c-14-42-58.636-68.122-96.639-67.656zM60.17 198.06c-8.818-.137-17.843 11.093-17.895 39.882-.078 44.153-4.356 56.616 16.077 106.551C73.335 381.112 80.054 409.257 128 432c5.68 20.022 3.413 24.73-.44 41.84-3.596 15.974 33.423 18.91 60.534 5.453 29.091-15.868 26.65-59.557 21.453-89.184-6.044-34.454-25.06-41.615-41.543-56.332-17.115-24.475-21.098-68.813-48.856-86.699-5.797-3.735-35.37-7.527 5.262 93.942-53.571-13.268-43.813-74.773-47.687-120.31-1.154-13.561-8.773-22.53-16.553-22.65zm391.66 0c-7.78.12-15.399 9.088-16.553 22.65-3.874 45.536 5.884 107.041-47.687 120.309 40.633-101.47 11.059-97.677 5.262-93.942-27.758 17.886-31.74 62.224-48.856 86.7-16.482 14.716-35.5 21.877-41.543 56.331-5.197 29.627-7.638 73.316 21.453 89.184 27.111 13.456 64.13 10.521 60.533-5.453-3.852-17.11-6.119-21.818-.439-41.84 47.946-22.743 54.665-50.888 69.648-87.506 20.433-49.935 16.155-62.398 16.077-106.55-.052-28.79-9.077-40.02-17.895-39.883z",
    slug: "cherish",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/cherish.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/cherish.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  "love-letter": {
    path: "M216.058 50.312c-.86-.005-1.572.037-2.12.123-5.981.932-20.108 8.016-36.207 19.511-16.098 11.496-34.887 27.134-53.93 44.485C95.74 140 67.162 169.31 45.217 194.7a3640.34 3640.34 0 0 0 50.812-3.872c1.538-16.08 6.79-32.61 17.368-45.879 10.166-12.751 26.122-21.682 46.617-21.675 18.943.006 41.55 7.126 68.935 23.275 21.438-25.45 40.574-39.736 58.412-45.451 4.809-1.54 9.513-2.39 14.06-2.643 13.645-.757 25.888 3.872 35.364 11.428 12.635 10.075 21.142 24.598 26.899 38.805.992 2.45 1.896 4.884 2.724 7.293 23.722-4.31 47.353-8.98 70.862-14.045-37.748-21.248-80.89-42.643-120-59.428-24.529-10.527-47.336-19.235-65.735-24.926-16.098-4.98-29.455-7.241-35.476-7.271zM305.4 116.13c-3.799-.029-7.94.635-12.548 2.111-14.046 4.5-32.654 17.808-54.899 45.59l-5.01 6.26-6.795-4.258c-28.717-17.994-50.548-24.553-66.142-24.558-15.595-.005-25.28 5.793-32.537 14.896-14.515 18.206-15.809 52.865-10.364 69.465 18.603 56.713 44.738 83.02 82.76 99.79 40.195-.354 79.786-7.153 118.967-19.483 20.884-31.578 38.095-67.142 36.006-118.356-.289-7.076-2.844-19.814-7.838-32.139-4.994-12.324-12.465-24.333-21.44-31.49-5.609-4.473-11.496-7.346-18.552-7.773-.53-.032-1.065-.05-1.608-.055zm-3.562 12.434a32.378 32.378 0 0 1 1.945.008c12.646.438 23.185 11.183 29.035 24.455-30.127-21.403-61.454-.145-79.638 12.49 12.363-17.632 28.675-36.452 48.658-36.953zm-137.897 25.761c13.546.007 30.081 4.885 46.036 17.26-86.153-26.888-63.03 66.667-35.965 120.168-28.36-17.18-59.2-88.077-38.166-125.47 3.994-7.101 14.55-11.965 28.095-11.958zm273.852 5.817a2248.032 2248.032 0 0 1-66.674 13.187c.972 4.94 1.54 9.517 1.703 13.524 1.966 48.19-12.285 84.872-30.328 115.326 3.958-.999 8.002-2.008 11.836-2.985 35.11-44.441 56.905-91.65 83.463-139.052zm15.437 9.289c-24.388 43.716-45.437 88.85-78.228 132.535 42.408 26.96 76.17 59.279 109.562 91.057-4.48-75.07-16.635-149.384-31.334-223.592zm-357.435 39.4a3715.148 3715.148 0 0 1-68.361 5.033c42.3 39.285 77.699 79.787 131.166 113.1 2.476.118 4.947.205 7.414.275-28.645-19.655-50.737-49.423-66.012-95.994-2.143-6.533-3.63-14.2-4.207-22.414zm-66.64 31.49 35.439 208.356c15.896-32.83 45.198-74.957 77.945-110.531-46.16-29.99-78.98-64.656-113.385-97.825zm333.741 75.29-1.09.277c-54.862 13.97-125.265 32.309-201.154 29.244-35.114 37.072-67.574 85.062-82.158 116.557 146.769-2.776 272.969-26.229 398.856-50.698-35.89-34.067-70.647-67.974-114.454-95.38z",
    slug: "love-letter",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/love-letter.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/love-letter.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  "locked-heart": {
    path: "M256 27.845c-21.563 0-48.67 1.224-69.041 9.357C166.589 45.336 153 58.047 153 86.845v51.847a125.34 125.34 0 0 1 19.988-1.732c3.353-.02 6.693.103 10.012.355v-38.47c0-16 11.186-28.155 24.975-35.05 13.788-6.893 30.9-9.95 48.025-9.95s34.237 3.057 48.025 9.95C317.815 70.69 329 82.846 329 98.846v38.478a124.58 124.58 0 0 1 30 1.377V86.845c0-28.798-13.589-41.51-33.959-49.643-20.37-8.133-47.478-9.357-69.041-9.357zm-83.12 127.093a107.73 107.73 0 0 0-22.886 2.682C109.396 166.847 73 198.594 73 258.845c0 43 37.72 89.974 81.213 131.49 21.747 20.758 44.64 40.173 64.25 57.52 15.099 13.356 28.124 25.257 37.537 36.3 9.413-11.043 22.438-22.944 37.537-36.3 19.61-17.347 42.503-36.762 64.25-57.52C401.28 348.819 439 301.845 439 258.845c0-60.25-36.396-91.998-76.994-101.225-40.598-9.227-83.648 5.56-97.65 40.567L256 219.075l-8.355-20.888c-11.377-28.443-41.931-43.537-74.764-43.249zM256 234.845a44.488 44.488 0 0 1 44.488 44.488A44.488 44.488 0 0 1 284 313.876l16.488 98.922h-88.976l16.49-98.936a44.488 44.488 0 0 1-16.49-34.529A44.488 44.488 0 0 1 256 234.845z",
    slug: "locked-heart",
    author: "Delapouite",
    authorUrl: "https://delapouite.com",
    source: "https://game-icons.net/1x1/delapouite/locked-heart.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/delapouite/locked-heart.svg",
    viewBox: 512,
    fillRule: "nonzero"
  },
  perfume: {
    path: "M257.125 18.53c-43.358 0-78.25 34.79-78.25 77.907 0 43.118 34.892 77.907 78.25 77.907s78.28-34.79 78.28-77.906c0-43.118-34.922-77.907-78.28-77.907zm-88.5 117.314c-6.644 8.198-10.156 17.204-10.156 26.47 0 15.315 9.554 29.91 27.093 41.31 17.538 11.4 42.63 18.876 70.53 18.876s53.025-7.475 70.563-18.875c17.54-11.4 27.094-25.996 27.094-41.313 0-8.654-3.057-17.087-8.875-24.843-15.528 32.8-49.04 55.56-87.75 55.56-39.35 0-73.333-23.508-88.5-57.186zm-27.22 39.72c-44.97 28.067-73.686 72.15-73.686 121.03 0 83.262 83.312 152.594 188.374 152.594 105.06 0 188.375-69.332 188.375-152.594 0-48.867-28.708-92.93-73.658-121-4.323 17.498-16.962 32.665-33.968 43.72-21.18 13.765-49.568 21.874-80.75 21.874-31.183 0-59.54-8.11-80.72-21.875-17.015-11.06-29.65-26.24-33.968-43.75zm-48.342 110.59h.032c14.633 56.066 82.046 98.406 163.125 98.406 81.077 0 147.834-42.338 162.467-98.406 2.013 7.54 3.938 15.033 3.938 23 0 66.954-74.647 121.344-166.406 121.344-91.76 0-166.44-54.39-166.44-121.344 0-7.967 1.27-15.46 3.282-23zM415.47 406.03c-38.153 37.933-95.66 61.845-159.376 61.845-63.68 0-121.16-23.885-159.313-61.78 5.558 20.953 20.806 40.113 43.595 55.374 29.125 19.503 70.212 31.967 115.75 31.967s86.594-12.464 115.72-31.968c22.81-15.277 38.07-34.46 43.624-55.44z",
    slug: "perfume-bottle",
    author: "Lorc",
    authorUrl: "https://lorcblog.blogspot.com",
    source: "https://game-icons.net/1x1/lorc/perfume-bottle.html",
    svg: "https://game-icons.net/icons/ffffff/transparent/1x1/lorc/perfume-bottle.svg",
    viewBox: 512,
    fillRule: "nonzero"
  }
}, mce = {
  name: "CC BY 3.0",
  url: "https://creativecommons.org/licenses/by/3.0/",
  source: "https://game-icons.net",
  attribution: "Icons made by Delapouite and Lorc. Available on https://game-icons.net"
}, p8 = mce.attribution;
var gce = {
  city: "castle",
  district: "village",
  building: "house",
  floor: "stairs",
  room: "door",
  outdoor: "forest"
}, vce = new Set(m3), xve = new Set(p3);
function xg(e) {
  const t = String(e || "").trim();
  return vce.has(t) && pce[t] || null;
}
function yce(e) {
  return xg(e) ? 24 : 20;
}
function m8(e, t) {
  return `translate(${Number(e.toFixed(2))}, ${Number(t.toFixed(2))})`;
}
function g8(e = 24) {
  return `scale(${Number((e / 512).toFixed(5))})`;
}
function _ce(e) {
  return gce[String(e || "").trim()] || "house";
}
var bce = {
  key: 0,
  class: "tavern-atlas-empty"
}, wce = {
  key: 0,
  class: "tavern-atlas-warning"
}, Sce = {
  class: "tavern-map-zoom-controls tavern-atlas-zoom-controls",
  "aria-label": "世界图缩放"
}, Ace = ["disabled"], Tce = ["disabled"], Ece = ["viewBox"], Cce = ["d"], Ice = ["x", "y"], xce = ["onClick", "onKeydown"], kce = [
  "x",
  "y",
  "width",
  "height"
], Mce = ["transform"], Pce = ["transform"], Rce = ["d", "fill-rule"], Nce = ["x", "y"], $ce = ["x", "y"], Lce = { class: "tavern-atlas-icon-credit" }, Dce = {
  key: 2,
  class: "tavern-atlas-detail"
}, Oce = { key: 0 }, Fce = {
  key: 1,
  class: "tavern-atlas-detail-links"
}, Bce = { key: 2 }, Uce = /* @__PURE__ */ bn({
  __name: "TavernAtlasPanel",
  props: {
    document: {},
    patches: { default: () => [] },
    activeLocationKey: { default: "" },
    activeMapDocId: { default: "main" },
    previewMapDocId: { default: "" },
    mapDocuments: { default: () => [] },
    displayMode: { default: "full" }
  },
  setup(e) {
    const t = e, n = /* @__PURE__ */ ue(""), r = /* @__PURE__ */ ue(null), s = /* @__PURE__ */ ue([0, 0]), a = /* @__PURE__ */ ue(1), o = /* @__PURE__ */ ue(null), l = ne(() => {
      const Z = t.document?.data, ie = Z && typeof Z == "object" && !Array.isArray(Z) ? Z : {};
      return {
        version: 1,
        activeLocationKey: String(ie.activeLocationKey || t.activeLocationKey || "").trim() || void 0,
        locations: Array.isArray(ie.locations) ? ie.locations.filter(b) : [],
        links: Array.isArray(ie.links) ? ie.links.filter(C) : [],
        actors: Array.isArray(ie.actors) ? ie.actors.filter(k) : []
      };
    }), i = ne(() => hce(l.value)), u = ne(() => new Map(l.value.locations.map((Z) => [Z.key, Z]))), d = ne(() => u.value.get(n.value) || f.value || l.value.locations[0] || null), f = ne(() => u.value.get(l.value.activeLocationKey || "") || null), m = ne(() => String(f.value?.mapDocId || "").trim()), v = ne(() => !!m.value && !!t.activeMapDocId && m.value !== t.activeMapDocId), y = ne(() => String(t.patches.at(-1)?.summary || "").trim()), S = ne(() => {
      const Z = /* @__PURE__ */ new Map();
      return t.mapDocuments.forEach((ie) => Z.set(ie.docId, ie.title || ie.docId)), Z;
    }), x = ne(() => new Set(t.mapDocuments.map((Z) => Z.docId))), I = ne(() => P(d.value?.key || "")), E = ne(() => {
      const Z = d.value?.key || "";
      return l.value.links.filter((ie) => ie.from === Z || ie.to === Z);
    }), N = ne(() => t.displayMode !== "detail"), $ = ne(() => t.displayMode !== "graph"), p = ne(() => {
      const [Z, ie, me, Se] = i.value.viewBox, [W, L] = s.value, Q = Math.max(0.5, Math.min(4, a.value)), de = me / Q, ge = Se / Q;
      return [
        Number((Z + W + (me - de) / 2).toFixed(2)),
        Number((ie + L + (Se - ge) / 2).toFixed(2)),
        Number(de.toFixed(2)),
        Number(ge.toFixed(2))
      ];
    }), w = ne(() => p.value.join(" ")), _ = ne(() => `${Math.round(a.value * 100)}%`);
    vt([() => l.value.activeLocationKey, () => l.value.locations.length], ([Z]) => {
      n.value && u.value.has(n.value) || (n.value = String(Z || l.value.locations[0]?.key || ""));
    }, { immediate: !0 }), vt(() => [t.document?.docId, t.displayMode], () => {
      be();
    });
    function b(Z) {
      return !!Z && typeof Z == "object" && !Array.isArray(Z) && typeof Z.key == "string";
    }
    function C(Z) {
      return !!Z && typeof Z == "object" && !Array.isArray(Z) && typeof Z.id == "string";
    }
    function k(Z) {
      return !!Z && typeof Z == "object" && !Array.isArray(Z) && typeof Z.actorKey == "string";
    }
    function P(Z) {
      return l.value.actors.filter((ie) => ie.locationKey === Z).sort((ie, me) => ie.actorKey === "player" ? -1 : me.actorKey === "player" ? 1 : ie.actorKey.localeCompare(me.actorKey));
    }
    function H(Z) {
      return Z.actorKey === "player" ? "玩家" : Z.actorKey;
    }
    function G(Z) {
      const ie = P(Z);
      if (!ie.length) return "";
      const me = ie.slice(0, 2).map(H);
      return ie.length > 2 ? `${me.join("、")} +${ie.length - 2}` : me.join("、");
    }
    function ee(Z) {
      return {
        "is-current": Z.key === l.value.activeLocationKey,
        "is-preview": Z.mapDocId && Z.mapDocId === t.previewMapDocId,
        "is-selected": Z.key === n.value,
        "is-mentioned": Z.status === "mentioned",
        "has-map": J(Z)
      };
    }
    function J(Z) {
      const ie = String(Z?.mapDocId || "").trim();
      return !!ie && x.value.has(ie);
    }
    function z(Z) {
      const ie = String(Z?.mapDocId || "").trim();
      return ie ? S.value.get(ie) || `已关联 ${ie}，地图未创建` : "暂无详细地图";
    }
    function R(Z) {
      return xg(_ce(Z?.scale));
    }
    function V(Z) {
      return m8(Z.x + 18, Z.y + 23);
    }
    function le() {
      return g8(18);
    }
    function ve(Z) {
      return l.value.links.find((ie) => ie.id === Z);
    }
    function ke(Z) {
      const ie = ve(Z);
      return String(ie?.label || ie?.kind || "").trim();
    }
    function be() {
      s.value = [0, 0], a.value = 1, o.value = null;
    }
    function Ne(Z, ie) {
      const me = r.value?.getBoundingClientRect(), [Se, W, L, Q] = p.value, [de, ge, pe, Me] = i.value.viewBox, Ie = Number(Math.max(0.5, Math.min(4, Z)).toFixed(2));
      if (Ie === a.value) return;
      let he = 0.5, q = 0.5;
      ie && me && me.width > 0 && me.height > 0 && (he = Math.max(0, Math.min(1, (ie.clientX - me.left) / me.width)), q = Math.max(0, Math.min(1, (ie.clientY - me.top) / me.height)));
      const ye = Se + L * he, oe = W + Q * q, De = pe / Ie, Je = Me / Ie;
      a.value = Ie, s.value = [Number((ye - De * he - de - (pe - De) / 2).toFixed(2)), Number((oe - Je * q - ge - (Me - Je) / 2).toFixed(2))];
    }
    function ce(Z) {
      Ne(a.value + Z);
    }
    function we(Z) {
      const ie = Z.currentTarget instanceof SVGSVGElement ? Z.currentTarget : r.value;
      if (!ie || Z.button !== 0) return;
      const me = ie.getBoundingClientRect();
      if (me.width <= 0 || me.height <= 0) return;
      const [, , Se, W] = p.value, [L, Q] = s.value;
      o.value = {
        pointerId: Z.pointerId,
        startClientX: Z.clientX,
        startClientY: Z.clientY,
        startPanX: L,
        startPanY: Q,
        viewWidth: Se,
        viewHeight: W,
        clientWidth: me.width,
        clientHeight: me.height
      }, ie.setPointerCapture(Z.pointerId);
    }
    function Oe(Z) {
      const ie = o.value;
      if (!ie || ie.pointerId !== Z.pointerId) return;
      const me = -((Z.clientX - ie.startClientX) / ie.clientWidth) * ie.viewWidth, Se = -((Z.clientY - ie.startClientY) / ie.clientHeight) * ie.viewHeight;
      s.value = [Number((ie.startPanX + me).toFixed(2)), Number((ie.startPanY + Se).toFixed(2))];
    }
    function F(Z) {
      const ie = o.value;
      if (!ie || ie.pointerId !== Z.pointerId) return;
      const me = Z.currentTarget instanceof SVGSVGElement ? Z.currentTarget : r.value;
      me?.hasPointerCapture(Z.pointerId) && me.releasePointerCapture(Z.pointerId), o.value = null;
    }
    function ae(Z) {
      if (!r.value) return;
      Z.preventDefault();
      const ie = Z.deltaY > 0 ? -1 : 1;
      Ne(a.value + ie * 0.18, {
        clientX: Z.clientX,
        clientY: Z.clientY
      });
    }
    return (Z, ie) => (O(), B("section", { class: Fe(["tavern-atlas-panel", `is-${e.displayMode}`]) }, [l.value.locations.length ? (O(), B(He, { key: 1 }, [
      $.value && v.value ? (O(), B("div", wce, " 当前激活的场景图不是世界当前位置的地图。 ")) : Te("", !0),
      N.value ? (O(), B("div", {
        key: 1,
        class: Fe(["tavern-atlas-graph", { "is-panning": o.value }])
      }, [
        g("div", Sce, [
          g("button", {
            type: "button",
            "aria-label": "缩小世界图",
            disabled: a.value <= 0.5,
            onClick: ie[0] || (ie[0] = (me) => ce(-0.25))
          }, " - ", 8, Ace),
          g("button", {
            type: "button",
            class: "tavern-map-zoom-value",
            "aria-label": "重置世界图缩放",
            onClick: be
          }, te(_.value), 1),
          g("button", {
            type: "button",
            "aria-label": "放大世界图",
            disabled: a.value >= 4,
            onClick: ie[1] || (ie[1] = (me) => ce(0.25))
          }, " + ", 8, Tce)
        ]),
        (O(), B("svg", {
          ref_key: "atlasSvgRef",
          ref: r,
          viewBox: w.value,
          preserveAspectRatio: "xMidYMid meet",
          role: "img",
          "aria-label": "世界图",
          onPointerdown: we,
          onPointermove: Oe,
          onPointerup: F,
          onPointercancel: F,
          onDblclick: be,
          onWheel: ae
        }, [
          ie[4] || (ie[4] = g("defs", null, [g("filter", {
            id: "tavern-atlas-sketch",
            x: "-5%",
            y: "-5%",
            width: "110%",
            height: "110%"
          }, [g("feTurbulence", {
            type: "fractalNoise",
            baseFrequency: "0.022",
            numOctaves: "3",
            seed: "11",
            result: "noise"
          }), g("feDisplacementMap", {
            in: "SourceGraphic",
            in2: "noise",
            scale: "0.55"
          })])], -1)),
          (O(!0), B(He, null, tt(i.value.links, (me) => (O(), B("path", {
            key: me.id,
            class: Fe(["tavern-atlas-link", `kind-${ve(me.id)?.kind || "path"}`]),
            d: me.path,
            filter: "url(#tavern-atlas-sketch)"
          }, null, 10, Cce))), 128)),
          (O(!0), B(He, null, tt(i.value.links, (me) => (O(), B("text", {
            key: `${me.id}:label`,
            class: Fe(["tavern-atlas-link-label", `kind-${ve(me.id)?.kind || "path"}`]),
            x: me.labelX,
            y: me.labelY,
            "text-anchor": "middle"
          }, te(ke(me.id)), 11, Ice))), 128)),
          (O(!0), B(He, null, tt(i.value.nodes, (me) => (O(), B("g", {
            key: me.key,
            class: Fe(["tavern-atlas-node", ee(u.value.get(me.key))]),
            role: "button",
            tabindex: "0",
            onClick: (Se) => n.value = me.key,
            onKeydown: Qr(Kt((Se) => n.value = me.key, ["prevent"]), ["enter"])
          }, [
            g("rect", {
              x: me.x,
              y: me.y,
              width: me.width,
              height: me.height,
              rx: "8",
              filter: "url(#tavern-atlas-sketch)"
            }, null, 8, kce),
            R(u.value.get(me.key)) ? (O(), B("g", {
              key: 0,
              transform: V(me)
            }, [g("g", { transform: le() }, [g("path", {
              class: "tavern-atlas-node-glyph",
              d: R(u.value.get(me.key))?.path,
              transform: "translate(-256, -256)",
              "fill-rule": R(u.value.get(me.key))?.fillRule
            }, null, 8, Rce)], 8, Pce)], 8, Mce)) : Te("", !0),
            g("text", {
              x: me.x + 36,
              y: me.y + 22,
              "text-anchor": "start"
            }, te(u.value.get(me.key)?.name || me.key), 9, Nce),
            g("text", {
              x: me.x + 36,
              y: me.y + 40,
              "text-anchor": "start",
              class: "tavern-atlas-node-meta"
            }, te(G(me.key) || u.value.get(me.key)?.scale), 9, $ce)
          ], 42, xce))), 128))
        ], 40, Ece)),
        g("span", Lce, te(T(p8)), 1)
      ], 2)) : Te("", !0),
      $.value ? (O(), B("aside", Dce, [
        g("header", null, [g("div", null, [g("strong", null, te(d.value?.name || "地点"), 1), g("span", null, te(d.value?.key), 1)]), g("em", null, te(d.value?.status === "visited" ? "已探索" : "已知"), 1)]),
        d.value?.brief ? (O(), B("p", Oce, te(d.value.brief), 1)) : Te("", !0),
        g("dl", null, [
          g("div", null, [ie[5] || (ie[5] = g("dt", null, "层级", -1)), g("dd", null, te(d.value?.scale || "-"), 1)]),
          g("div", null, [ie[6] || (ie[6] = g("dt", null, "上级", -1)), g("dd", null, te(d.value?.parent ? u.value.get(d.value.parent)?.name || d.value.parent : "顶级地点"), 1)]),
          g("div", null, [ie[7] || (ie[7] = g("dt", null, "场景图", -1)), g("dd", null, te(z(d.value)), 1)]),
          g("div", null, [ie[8] || (ie[8] = g("dt", null, "人物", -1)), g("dd", null, te(I.value.length ? I.value.map(H).join("、") : "无"), 1)])
        ]),
        E.value.length ? (O(), B("div", Fce, [(O(!0), B(He, null, tt(E.value, (me) => (O(), B("span", { key: me.id }, te(me.label || me.kind) + " · " + te(u.value.get(me.from)?.name || me.from) + " → " + te(u.value.get(me.to)?.name || me.to), 1))), 128))])) : Te("", !0),
        y.value ? (O(), B("small", Bce, "最近更新：" + te(y.value), 1)) : Te("", !0)
      ])) : Te("", !0)
    ], 64)) : (O(), B("div", bce, [
      ie[2] || (ie[2] = g("strong", null, "世界地图尚未建立", -1)),
      ie[3] || (ie[3] = g("p", null, "随着剧情展开，AI 会自动记录你去过的地点，以及地点之间的联系。", -1)),
      g("span", null, "当前场景图：" + te(S.value.get(e.activeMapDocId) || e.activeMapDocId || "main"), 1)
    ]))], 2));
  }
}), qce = (e, t) => {
  const n = e.__vccOpts || e;
  for (const [r, s] of t) n[r] = s;
  return n;
}, yx = /* @__PURE__ */ qce(Uce, [["__scopeId", "data-v-42e75be1"]]), Hce = { class: "tavern-event-panel" }, zce = {
  key: 0,
  class: "tavern-event-current"
}, Kce = { class: "tavern-event-current-head" }, Wce = { class: "tavern-event-current-entry" }, Gce = { class: "tavern-event-field" }, Vce = { class: "tavern-event-done-token" }, Jce = {
  key: 1,
  class: "tavern-event-empty"
}, Yce = {
  key: 2,
  class: "tavern-event-section"
}, Xce = { class: "tavern-event-current-text" }, jce = { class: "tavern-event-done" }, Qce = { class: "tavern-event-horizon" }, Zce = {
  key: 3,
  class: "tavern-event-section"
}, ede = { class: "tavern-event-current-text" }, tde = { class: "tavern-event-done" }, nde = {
  key: 0,
  class: "tavern-event-completed-more"
}, rde = /* @__PURE__ */ bn({
  __name: "TavernEventPanel",
  props: {
    tasks: { default: () => [] },
    enabled: {
      type: Boolean,
      default: !1
    },
    assistantFloor: { default: -1 }
  },
  setup(e) {
    const t = e, n = ne(() => (Array.isArray(t.tasks) ? t.tasks : []).filter((v) => v.status !== "abandoned").sort((v, y) => Number(y.updatedOrder) - Number(v.updatedOrder) || Number(y.updatedAt) - Number(v.updatedAt))), r = ne(() => n.value.filter((v) => v.status === "active")), s = ne(() => n.value.filter((v) => v.status === "completed")), a = ne(() => r.value[0] || null), o = ne(() => r.value.slice(1, 4)), l = ne(() => s.value.slice(0, 3)), i = ne(() => Math.max(0, s.value.length - l.value.length)), u = ne(() => t.enabled ? Number(t.assistantFloor) < 5 ? "剧情展开后会出现事件线索" : "当前没有足够新鲜的方向" : "事件功能未授权"), d = ne(() => t.enabled ? Number(t.assistantFloor) < 5 ? "先让人物、地点和关系沉淀几轮，系统不会过早生成方向。" : "后台没有找到对味的新钩子时会保持空白。" : "开启契约里的织线者后，后台会维护可回滚的事件线索池。");
    function f(v) {
      const y = Number(v.updatedOrder);
      return Number.isFinite(y) && y >= 0 ? `#${y}` : "";
    }
    function m(v) {
      return String(v.title || v.current || "未命名方向").trim();
    }
    return (v, y) => (O(), B("section", Hce, [
      a.value ? (O(), B("div", zce, [
        g("div", Kce, [y[0] || (y[0] = g("span", { class: "tavern-event-kicker" }, "当前方向", -1)), g("small", null, te(f(a.value)), 1)]),
        g("h3", null, te(m(a.value)), 1),
        g("div", Wce, te(a.value.current), 1),
        g("div", Gce, [y[1] || (y[1] = g("span", null, "远景", -1)), g("p", null, te(a.value.horizon), 1)]),
        g("div", Vce, [y[2] || (y[2] = g("span", null, "达成标志", -1)), g("p", null, te(a.value.doneWhen), 1)])
      ])) : (O(), B("div", Jce, [g("strong", null, te(u.value), 1), g("p", null, te(d.value), 1)])),
      o.value.length ? (O(), B("div", Yce, [g("header", null, [y[3] || (y[3] = g("strong", null, "其他线索", -1)), g("span", null, te(o.value.length), 1)]), (O(!0), B(He, null, tt(o.value, (S) => (O(), B("article", {
        key: S.id,
        class: "tavern-event-card"
      }, [
        g("div", null, [g("strong", null, te(m(S)), 1), g("small", null, te(f(S)), 1)]),
        g("p", Xce, te(S.current), 1),
        g("p", jce, " 达成：" + te(S.doneWhen), 1),
        g("p", Qce, te(S.horizon), 1)
      ]))), 128))])) : Te("", !0),
      s.value.length ? (O(), B("div", Zce, [
        g("header", null, [g("strong", null, "已完成 " + te(s.value.length), 1)]),
        (O(!0), B(He, null, tt(l.value, (S) => (O(), B("article", {
          key: S.id,
          class: "tavern-event-card is-completed"
        }, [
          g("div", null, [g("strong", null, te(m(S)), 1), g("small", null, te(f(S)), 1)]),
          g("p", ede, te(S.current), 1),
          g("p", tde, " 达成：" + te(S.doneWhen), 1)
        ]))), 128)),
        i.value ? (O(), B("p", nde, " 另有 " + te(i.value) + " 条完成回执已收起 ", 1)) : Te("", !0)
      ])) : Te("", !0)
    ]));
  }
}), sde = rde, xf = [
  0,
  0,
  800,
  600
], ade = 48, Ip = 160, ode = 4;
function zs(e, t, n, r) {
  if (![
    e,
    t,
    n,
    r
  ].every((i) => Number.isFinite(i))) return null;
  const s = Math.min(e, n), a = Math.max(e, n), o = Math.min(t, r), l = Math.max(t, r);
  return {
    minX: s,
    minY: o,
    maxX: a,
    maxY: l,
    width: Math.max(0, a - s),
    height: Math.max(0, l - o)
  };
}
function v8(e, t) {
  return e ? t ? zs(Math.min(e.minX, t.minX), Math.min(e.minY, t.minY), Math.max(e.maxX, t.maxX), Math.max(e.maxY, t.maxY)) : e : t;
}
function a1(e, t) {
  return zs(e.minX - t, e.minY - t, e.maxX + t, e.maxY + t) || e;
}
function _x(e, t) {
  return t.map(([n, r]) => [e.at[0] + n, e.at[1] + r]);
}
function bx(e) {
  return !Array.isArray(e) || !e.length ? null : e.reduce((t, [n, r]) => v8(t, zs(n, r, n, r)), null);
}
function ide(e) {
  return e.rect ? zs(e.at[0], e.at[1], e.at[0] + e.rect[0], e.at[1] + e.rect[1]) : null;
}
function lde(e) {
  return typeof e.circle != "number" || e.circle <= 0 ? null : zs(e.at[0] - e.circle, e.at[1] - e.circle, e.at[0] + e.circle, e.at[1] + e.circle);
}
function ude(e) {
  if (!e.icon) return null;
  const t = yce(e.icon) / 2;
  return zs(e.at[0] - t, e.at[1] - t, e.at[0] + t, e.at[1] + t);
}
function cde(e) {
  if (!e.text) return null;
  const t = Math.max(12, e.text.length * 8.4);
  return zs(e.at[0] - t / 2, e.at[1] - 14, e.at[0] + t / 2, e.at[1] + 6);
}
function dde(e) {
  const t = Number(e.style?.width || 2);
  return Math.max(4, Number.isFinite(t) && t > 0 ? t / 2 + 4 : 4);
}
function fde(e) {
  if (!Array.isArray(e) || e.length !== 4) return [...xf];
  const t = e.map((n) => Number(n));
  return t.every((n) => Number.isFinite(n)) ? [
    t[0],
    t[1],
    Math.max(1, t[2]),
    Math.max(1, t[3])
  ] : [...xf];
}
function o1(e) {
  if (e.cat === "light" || Km(e.material)?.layer === "light") return a1(zs(e.at[0], e.at[1], e.at[0], e.at[1]) || {
    minX: e.at[0],
    minY: e.at[1],
    maxX: e.at[0],
    maxY: e.at[1],
    width: 0,
    height: 0
  }, ode);
  let t = null;
  return e.rect ? t = ide(e) : typeof e.circle == "number" ? t = lde(e) : e.path ? t = bx(_x(e, e.path)) : e.curve ? t = bx(_x(e, e.curve)) : e.icon ? t = ude(e) : e.text && (t = cde(e)), t ? a1(t, dde(e)) : null;
}
function hde(e) {
  return e?.elements?.length ? e.elements.reduce((t, n) => v8(t, o1(n)), null) : null;
}
function pde(e, t) {
  if (!Number.isFinite(t) || t <= 0) return e;
  const n = Math.max(e.width, 1), r = Math.max(e.height, 1), s = n / r;
  if (Math.abs(s - t) < 0.01) return e;
  if (s > t) {
    const o = (n / t - r) / 2;
    return zs(e.minX, e.minY - o, e.maxX, e.maxY + o) || e;
  }
  const a = (r * t - n) / 2;
  return zs(e.minX - a, e.minY, e.maxX + a, e.maxY) || e;
}
function mde(e, t) {
  const n = t >= 1 ? Ip : Ip * t, r = t >= 1 ? Ip / t : Ip;
  let s = e;
  if (s.width < n) {
    const a = (n - s.width) / 2;
    s = zs(s.minX - a, s.minY, s.maxX + a, s.maxY) || s;
  }
  if (s.height < r) {
    const a = (r - s.height) / 2;
    s = zs(s.minX, s.minY - a, s.maxX, s.maxY + a) || s;
  }
  return s;
}
function gde(e, t) {
  const n = hde(e);
  if (!n) return [...xf];
  const r = mde(pde(a1(n, ade), t), t);
  return [
    Number(r.minX.toFixed(2)),
    Number(r.minY.toFixed(2)),
    Number(r.width.toFixed(2)),
    Number(r.height.toFixed(2))
  ];
}
function vde(e) {
  return e?.meta?.viewBox ? fde(e.meta.viewBox) : gde(e, xf[2] / xf[3]);
}
var $_ = 8, wx = 4, yde = 1e4, _de = 12e3, bde = 0.12;
function wde(e) {
  return /[\u2e80-\u9fff\uf900-\ufaff\uff00-\uffef]/u.test(e);
}
function Sde(e = "") {
  return [...String(e || "")].reduce((t, n) => wde(n) ? t + 1 : /\s/u.test(n) ? t + 0.35 : /[\x00-\x7f]/u.test(n) ? t + 0.56 : t + 0.78, 0);
}
function Sx(e, t = e.x, n = e.y) {
  const r = Math.max(8, Number(e.fontSize || 14)), s = Math.max(r, Sde(e.text) * r) + $_ * 2, a = r * 1.35 + wx * 2, o = String(e.anchor || "middle").toLowerCase(), l = o === "start" ? t - $_ : o === "end" ? t - s + $_ : t - s / 2, i = l + s, u = n + wx;
  return {
    minX: l,
    minY: u - a,
    maxX: i,
    maxY: u,
    width: s,
    height: a
  };
}
function Ade(e, t) {
  return Math.max(0, Math.min(e.maxX, t.maxX) - Math.max(e.minX, t.minX)) * Math.max(0, Math.min(e.maxY, t.maxY) - Math.max(e.minY, t.minY));
}
function Tde(e, t) {
  const [n, r, s, a] = t, o = n + s, l = r + a, i = Math.max(0, n - e.minX) + Math.max(0, e.maxX - o), u = Math.max(0, r - e.minY) + Math.max(0, e.maxY - l);
  return i * e.height + u * e.width;
}
function Ede(e) {
  const t = Math.max(16, Math.min(42, e.height * 1.45)), n = Math.max(14, Math.min(32, e.height * 1.08));
  return [
    [0, 0],
    [0, -n],
    [0, n],
    [t, -n * 0.72],
    [t, n * 0.72],
    [-t, -n * 0.72],
    [-t, n * 0.72],
    [t, 0],
    [-t, 0]
  ];
}
function Cde(e = "", t = "") {
  const n = String(e), r = String(t);
  return n < r ? -1 : n > r ? 1 : 0;
}
function Ide(e, t, n = {}) {
  if (!e.length) return [];
  const r = /* @__PURE__ */ new Map(), s = [], a = e.map((o, l) => ({
    item: o,
    index: l
  }));
  return a.sort((o, l) => (n.priority?.(o.item) ?? 50) - (n.priority?.(l.item) ?? 50) || Cde(o.item.id, l.item.id) || o.index - l.index), a.forEach(({ item: o, index: l }) => {
    const i = Sx(o);
    let u = {
      x: o.x,
      y: o.y,
      bounds: i,
      score: Number.POSITIVE_INFINITY
    };
    Ede(i).forEach(([d, f]) => {
      const m = Number((o.x + d).toFixed(2)), v = Number((o.y + f).toFixed(2)), y = Sx(o, m, v), S = s.reduce((N, $) => N + Ade(y, $), 0), x = Tde(y, t), I = d * d + f * f, E = S * yde + x * _de + I * bde;
      E < u.score && (u = {
        x: m,
        y: v,
        bounds: y,
        score: E
      });
    }), r.set(l, u), s.push(u.bounds);
  }), e.map((o, l) => {
    const i = r.get(l);
    return i ? {
      ...o,
      x: i.x,
      y: i.y
    } : { ...o };
  });
}
var xde = {
  key: 0,
  class: "tavern-editor-head xb-editor-head tavern-map-head"
}, kde = { class: "tavern-map-title" }, Mde = { key: 0 }, Pde = { class: "tavern-editor-actions xb-editor-actions tavern-map-actions" }, Rde = ["value", "disabled"], Nde = ["value"], $de = ["disabled"], Lde = ["disabled"], Dde = ["disabled"], Ode = {
  key: 0,
  class: "tavern-map-compact-controls is-floating"
}, Fde = ["value", "disabled"], Bde = ["value"], Ude = {
  class: "tavern-map-zoom-controls",
  "aria-label": "地图缩放"
}, qde = ["disabled"], Hde = ["disabled"], zde = ["viewBox", "aria-label"], Kde = [
  "id",
  "x",
  "y",
  "width",
  "height"
], Wde = [
  "href",
  "x",
  "y",
  "width",
  "height"
], Gde = { class: "map-fill-layer" }, Vde = [
  "d",
  "fill",
  "transform",
  "fill-rule"
], Jde = { class: "map-avatar-layer" }, Yde = [
  "d",
  "fill",
  "transform",
  "fill-rule"
], Xde = {
  class: "map-line-layer",
  filter: "url(#tavern-map-sketch)"
}, jde = [
  "d",
  "fill",
  "stroke",
  "stroke-width",
  "stroke-dasharray",
  "transform",
  "fill-rule"
], Qde = ["transform"], Zde = ["transform"], efe = [
  "d",
  "fill",
  "fill-rule"
], tfe = { class: "map-light-layer" }, nfe = [
  "d",
  "fill",
  "transform",
  "fill-rule"
], rfe = [
  "x",
  "y",
  "width",
  "height",
  "fill"
], sfe = { class: "map-label-layer" }, afe = [
  "x",
  "y",
  "font-size",
  "fill",
  "text-anchor"
], ofe = { class: "map-removed-layer" }, ife = [
  "d",
  "fill",
  "transform",
  "fill-rule"
], lfe = [
  "d",
  "fill",
  "transform",
  "fill-rule"
], ufe = [
  "d",
  "fill",
  "stroke",
  "stroke-width",
  "stroke-dasharray",
  "transform",
  "fill-rule"
], cfe = ["transform"], dfe = ["transform"], ffe = [
  "d",
  "fill",
  "fill-rule"
], hfe = [
  "x",
  "y",
  "font-size",
  "fill",
  "text-anchor"
], pfe = ["aria-expanded", "title"], mfe = {
  key: 2,
  class: "tavern-map-timeline-control"
}, gfe = {
  key: 2,
  class: "tavern-map-empty"
}, vfe = {
  key: 0,
  class: "tavern-map-compact-controls"
}, yfe = ["value", "disabled"], _fe = ["value"], bfe = {
  key: 3,
  class: "tavern-editor-foot xb-editor-foot tavern-map-foot"
}, wfe = { key: 0 }, Sfe = { key: 1 }, Afe = { class: "tavern-map-icon-credit" }, Tfe = /* @__PURE__ */ bn({
  __name: "TavernMapPanel",
  props: {
    documents: { default: () => [] },
    activeDocId: { default: "main" },
    selectedDocId: { default: "" },
    document: {},
    patches: {},
    compact: {
      type: Boolean,
      default: !1
    },
    playerDisplayName: { default: "User" },
    playerAvatarUrl: { default: "" }
  },
  emits: ["update:selectedDocId"],
  setup(e, { emit: t }) {
    function n(D) {
      return D.find((Y) => !Y.gameIcon && Y.layer !== "label" && !!Y.path) || null;
    }
    const r = e, s = t, a = /* @__PURE__ */ ue(0), o = /* @__PURE__ */ ue("patch"), l = /* @__PURE__ */ ue(0), i = /* @__PURE__ */ ue(""), u = /* @__PURE__ */ ue(null), d = /* @__PURE__ */ ue([0, 0]), f = /* @__PURE__ */ ue(1), m = /* @__PURE__ */ ue(null);
    let v;
    function y(D) {
      return JSON.parse(JSON.stringify(D));
    }
    function S() {
      return Ju();
    }
    function x(D) {
      if (!D || typeof D != "object" || Array.isArray(D)) return S();
      const Y = D, j = S();
      return {
        meta: Y.meta && typeof Y.meta == "object" && !Array.isArray(Y.meta) ? {
          ...j.meta,
          ...y(Y.meta)
        } : j.meta,
        elements: Array.isArray(Y.elements) ? y(Y.elements).filter((We) => !!We && typeof We == "object" && !Array.isArray(We) && typeof We.id == "string") : []
      };
    }
    const I = ne(() => {
      const D = Array.isArray(r.documents) ? r.documents : [], Y = String(i.value || r.activeDocId || r.document?.docId || "").trim();
      return D.find((j) => j.docId === Y) || D.find((j) => j.docId === r.activeDocId) || r.document || null;
    }), E = ne(() => {
      const D = String(I.value?.docId || "").trim();
      return D && D === String(r.document?.docId || "").trim() ? r.patches : [];
    }), N = ne(() => {
      const D = I.value?.data;
      return !D || typeof D != "object" || Array.isArray(D) ? null : x(D);
    }), $ = ne(() => E.value.at(-1) || null), p = /* @__PURE__ */ ue(!1), w = ne(() => {
      const D = [...E.value].sort((j, We) => Number(j.revision || 0) - Number(We.revision || 0));
      let Y = S();
      return D.map((j, We) => {
        const qe = Array.isArray(j.ops) ? j.ops : [];
        return Y = tq(x(Y), qe), {
          revision: Number(j.revision || We + 1),
          document: y(Y),
          patch: j,
          index: We
        };
      });
    }), _ = ne(() => {
      if (!w.value.length) return null;
      const D = Math.max(0, Math.min(l.value, w.value.length - 1));
      return w.value[D] || null;
    }), b = ne(() => w.value.length > 0 && Number(w.value[0]?.revision || 0) <= 1), C = ne(() => o.value === "timeline" && _.value?.patch || $.value), k = ne(() => o.value === "timeline" && _.value?.document || N.value), P = ne(() => Array.isArray(C.value?.ops) ? C.value?.ops : []), H = ne(() => new Set((C.value?.changedIds || []).map((D) => String(D || "").trim()).filter(Boolean))), G = ne(() => {
      const D = /* @__PURE__ */ new Map();
      return P.value.forEach((Y) => {
        const j = String(Y.op || "").trim(), We = Y.element && typeof Y.element == "object" && !Array.isArray(Y.element) ? Y.element : null, qe = String(Y.id || We?.id || "").trim();
        qe && (j === "add" || j === "modify" || j === "remove") && D.set(qe, j);
      }), D;
    }), ee = ne(() => P.value.filter((D) => String(D.op || "").trim() === "remove").map((D, Y) => ({
      id: String(D.id || `removed-${Y}`).trim(),
      delayMs: Y * 120
    })).filter((D) => D.id)), J = ne(() => (Array.isArray(C.value?.removedElements) ? C.value?.removedElements : []).filter((D) => !!D && typeof D == "object" && !Array.isArray(D) && typeof D.id == "string")), z = ne(() => vde(k.value)), R = ne(() => {
      const [D, Y, j, We] = z.value, [qe, Ft] = d.value, tn = Math.max(0.5, Math.min(4, f.value)), Bt = j / tn, an = We / tn;
      return [
        Number((D + qe + (j - Bt) / 2).toFixed(2)),
        Number((Y + Ft + (We - an) / 2).toFixed(2)),
        Number(Bt.toFixed(2)),
        Number(an.toFixed(2))
      ];
    }), V = ne(() => R.value.join(" ")), le = ne(() => `${Math.round(f.value * 100)}%`), ve = ne(() => String(k.value?.meta?.theme || "parchment")), ke = ne(() => String(o.value === "timeline" ? k.value?.meta?.name || I.value?.title || "地图" : I.value?.title || k.value?.meta?.name || "地图")), be = ne(() => String(I.value?.digest || "")), Ne = ne(() => _.value && o.value === "timeline" ? `revision ${_.value.revision}` : I.value ? `revision ${I.value.revision}` : "no map"), ce = ne(() => C.value?.summary || (C.value ? `revision ${C.value.revision}` : "等待空间变化")), we = ne(() => o.value === "full" ? "完整重绘" : o.value === "timeline" ? `回合 ${Se.value}` : "地图更新"), Oe = ne(() => be.value.split(`
`).map((D) => D.trim()).filter(Boolean).slice(0, 4)), F = ne(() => k.value?.elements.length || 0), ae = ne(() => E.value.length), Z = ne(() => Array.isArray(r.documents) && r.documents.length ? r.documents : r.document ? [{
      ...r.document,
      active: !0
    }] : []), ie = ne(() => String(r.playerDisplayName || "").trim() || "User"), me = ne(() => String(r.playerAvatarUrl || "").trim()), Se = ne(() => _.value ? `${_.value.index + 1} / ${w.value.length}` : "0 / 0"), W = ne(() => QU(k.value)), L = ne(() => !!C.value);
    vt(() => r.document?.revision, () => {
      o.value = "patch", Q(), l.value = Math.max(0, w.value.length - 1), a.value += 1, p.value = !1;
    }), vt(() => [
      r.selectedDocId,
      r.activeDocId,
      r.document?.docId,
      r.documents.length
    ], () => {
      const D = String(r.selectedDocId || i.value || r.activeDocId || r.document?.docId || "").trim(), Y = Z.value.some((We) => We.docId === D) ? D : String(r.activeDocId || r.document?.docId || Z.value[0]?.docId || "").trim(), j = i.value;
      i.value = Y, j && Y && j !== Y && de(), Y && Y !== r.selectedDocId && s("update:selectedDocId", Y);
    }, { immediate: !0 }), vt(() => E.value.length, () => {
      l.value >= w.value.length && (l.value = Math.max(0, w.value.length - 1)), o.value === "timeline" && !b.value && (o.value = "patch", Q()), p.value = !1;
    }), Sl(() => {
      Q();
    });
    function Q() {
      v !== void 0 && (window.clearTimeout(v), v = void 0);
    }
    function de() {
      d.value = [0, 0], f.value = 1, m.value = null;
    }
    function ge(D, Y) {
      const j = u.value?.getBoundingClientRect(), [We, qe, Ft, tn] = R.value, [Bt, an, Bn, dn] = z.value, Jn = Number(Math.max(0.5, Math.min(4, D)).toFixed(2));
      if (Jn === f.value) return;
      let sr = 0.5, Un = 0.5;
      Y && j && j.width > 0 && j.height > 0 && (sr = Math.max(0, Math.min(1, (Y.clientX - j.left) / j.width)), Un = Math.max(0, Math.min(1, (Y.clientY - j.top) / j.height)));
      const wr = We + Ft * sr, qn = qe + tn * Un, vn = Bn / Jn, Sr = dn / Jn;
      f.value = Jn, d.value = [Number((wr - vn * sr - Bt - (Bn - vn) / 2).toFixed(2)), Number((qn - Sr * Un - an - (dn - Sr) / 2).toFixed(2))];
    }
    function pe(D) {
      ge(f.value + D);
    }
    function Me(D, Y = [0, 0]) {
      if (!Array.isArray(D) || D.length < 2) return Y;
      const j = Number(D[0]), We = Number(D[1]);
      return Number.isFinite(j) && Number.isFinite(We) ? [j, We] : Y;
    }
    function Ie(D, Y) {
      return Y.map(([j, We]) => [D.at[0] + j, D.at[1] + We]);
    }
    function he(D, Y = !1) {
      if (!Array.isArray(D) || D.length < 2) return "";
      const j = [`M ${D[0][0]} ${D[0][1]}`, ...D.slice(1).map(([We, qe]) => `L ${We} ${qe}`)];
      return Y && j.push("Z"), j.join(" ");
    }
    function q(D, Y = !1) {
      if (!Array.isArray(D) || D.length < 2) return "";
      if (D.length === 2) return he(D, Y);
      const j = 0.3, We = [`M ${D[0][0]} ${D[0][1]}`];
      for (let qe = 0; qe < D.length - 1; qe += 1) {
        const Ft = D[Math.max(qe - 1, 0)], tn = D[qe], Bt = D[qe + 1], an = D[Math.min(qe + 2, D.length - 1)];
        We.push(`C ${tn[0] + (Bt[0] - Ft[0]) * j} ${tn[1] + (Bt[1] - Ft[1]) * j}, ${Bt[0] - (an[0] - tn[0]) * j} ${Bt[1] - (an[1] - tn[1]) * j}, ${Bt[0]} ${Bt[1]}`);
      }
      return Y && We.push("Z"), We.join(" ");
    }
    function ye(D) {
      const [Y, j] = D.at, [We, qe] = Me(D.rect, [10, 10]);
      return `M ${Y} ${j} L ${Y + We} ${j} L ${Y + We} ${j + qe} L ${Y} ${j + qe} Z`;
    }
    function oe(D) {
      const [Y, j] = D.at, We = Number(D.circle || 8), qe = Number.isFinite(We) && We > 0 ? We : 8;
      return `M ${Y - qe} ${j} A ${qe} ${qe} 0 1 0 ${Y + qe} ${j} A ${qe} ${qe} 0 1 0 ${Y - qe} ${j}`;
    }
    function De(D) {
      const [Y, j] = D.at, We = 10, qe = We / 2, Ft = String(D.icon || "+"), tn = xg(Ft);
      return tn ? tn.path : Ft === "x" ? `M ${Y - qe} ${j - qe} L ${Y + qe} ${j + qe} M ${Y + qe} ${j - qe} L ${Y - qe} ${j + qe}` : Ft === "o" ? `M ${Y - qe} ${j} A ${qe} ${qe} 0 1 0 ${Y + qe} ${j} A ${qe} ${qe} 0 1 0 ${Y - qe} ${j}` : Ft === "arrow-n" ? `M ${Y} ${j + We} L ${Y} ${j - We} M ${Y - qe} ${j - qe} L ${Y} ${j - We} L ${Y + qe} ${j - qe}` : Ft === "arrow-s" ? `M ${Y} ${j - We} L ${Y} ${j + We} M ${Y - qe} ${j + qe} L ${Y} ${j + We} L ${Y + qe} ${j + qe}` : Ft === "arrow-e" ? `M ${Y - We} ${j} L ${Y + We} ${j} M ${Y + qe} ${j - qe} L ${Y + We} ${j} L ${Y + qe} ${j + qe}` : Ft === "arrow-w" ? `M ${Y + We} ${j} L ${Y - We} ${j} M ${Y - qe} ${j - qe} L ${Y - We} ${j} L ${Y - qe} ${j + qe}` : Ft === "stairs-up" ? `M ${Y - 9} ${j + 7} H ${Y - 3} V ${j + 1} H ${Y + 3} V ${j - 5} H ${Y + 9} M ${Y + 3} ${j - 5} L ${Y + 3} ${j - 10} M ${Y + 3} ${j - 10} L ${Y} ${j - 7} M ${Y + 3} ${j - 10} L ${Y + 6} ${j - 7}` : Ft === "stairs-down" ? `M ${Y - 9} ${j - 7} H ${Y - 3} V ${j - 1} H ${Y + 3} V ${j + 5} H ${Y + 9} M ${Y + 3} ${j + 5} L ${Y + 3} ${j + 10} M ${Y + 3} ${j + 10} L ${Y} ${j + 7} M ${Y + 3} ${j + 10} L ${Y + 6} ${j + 7}` : `M ${Y - qe} ${j} L ${Y + qe} ${j} M ${Y} ${j - qe} L ${Y} ${j + qe}`;
    }
    function Je(D) {
      return D.rect ? ye(D) : typeof D.circle == "number" ? oe(D) : D.path ? he(Ie(D, D.path), D.closed === !0) : D.curve ? q(Ie(D, D.curve), D.closed === !0) : D.icon ? De(D) : "";
    }
    function st(D) {
      if (D.style?.color) return String(D.style.color);
      const Y = {
        stone: "#6b6570",
        tile: "#565b69",
        wood: "#5d422c",
        metal: "#6f757b"
      };
      if (["wall", "door"].includes(String(D.cat || "").trim()) && D.material && Y[D.material]) return Y[D.material];
      const j = {
        wall: "#2f2418",
        road: "#9a8253",
        water: "#2d7490",
        terrain: "#55734b",
        furniture: "#765d37",
        door: "#a5652b",
        danger: "#b94035",
        marker: "#b94035",
        actor: "#b94035",
        label: "#2b2118",
        grid: "#9f987f",
        magic: "#76539a",
        secret: "#68625b",
        light: "#d48a42"
      };
      return j[String(D.cat || "wall")] || j.wall;
    }
    function lt(D) {
      return !!D.rect || typeof D.circle == "number" || (!!D.path || !!D.curve) && D.closed === !0;
    }
    function ot(D) {
      return D.certainty === "unknown" ? 0.48 : D.certainty === "inferred" ? 0.68 : 1;
    }
    function pt(D) {
      if (!lq(D.cat)) return D.fill ? String(D.fill) : "none";
      const Y = Km(D.material);
      return Y?.layer === "fill" ? lt(D) ? Y.paint : "none" : D.fill ? String(D.fill) : lt(D) ? {
        water: "rgba(63, 136, 167, 0.24)",
        terrain: "rgba(87, 121, 70, 0.18)",
        danger: "rgba(180, 64, 54, 0.15)",
        magic: "rgba(126, 83, 154, 0.16)",
        secret: "rgba(63, 56, 48, 0.10)"
      }[String(D.cat || "")] || "rgba(92, 70, 36, 0.08)" : "none";
    }
    function ht(D) {
      const Y = Number(D.style?.width || 2);
      return Number.isFinite(Y) && Y > 0 ? Y : 2;
    }
    function It(D) {
      return String(D.style?.dash || "");
    }
    function Lt(D, Y) {
      if (typeof D.circle == "number") return Math.max(32, Math.PI * 2 * D.circle);
      if (D.rect) return Math.max(40, Math.abs(D.rect[0]) * 2 + Math.abs(D.rect[1]) * 2);
      const j = Y.match(/-?\d+(?:\.\d+)?/g)?.map(Number) || [];
      let We = 0;
      for (let qe = 0; qe + 3 < j.length; qe += 2) {
        const Ft = j[qe + 2] - j[qe], tn = j[qe + 3] - j[qe + 1];
        We += Math.hypot(Ft, tn);
      }
      return Math.max(28, Math.min(2400, We || 180));
    }
    function en(D) {
      return o.value === "full" ? "add" : G.value.get(D.id) || (H.value.has(D.id) ? "modify" : "stable");
    }
    function at(D, Y) {
      if (!D) return Y;
      if (!Y) return D;
      const j = Math.min(D.minX, Y.minX), We = Math.min(D.minY, Y.minY), qe = Math.max(D.maxX, Y.maxX), Ft = Math.max(D.maxY, Y.maxY);
      return {
        minX: j,
        minY: We,
        maxX: qe,
        maxY: Ft,
        width: Math.max(0, qe - j),
        height: Math.max(0, Ft - We)
      };
    }
    function X() {
      return (k.value?.elements || []).reduce((D, Y) => Y.text || String(Y.cat || "") === "label" ? D : at(D, o1(Y)), null);
    }
    function se(D) {
      if (!D.text || String(D.cat || "") !== "label") return D.at;
      const Y = String(D.id || "").trim().toLowerCase();
      if (Y.startsWith("__label__")) return D.at;
      const [j, We, qe, Ft] = R.value, tn = We + Math.max(72, Ft * 0.26);
      if (![
        "label",
        "room-label",
        "scene-label",
        "place-label",
        "map-label"
      ].includes(Y) || D.at[1] >= tn) return D.at;
      const Bt = X(), an = (Bt?.minX ?? j) + 24, Bn = (Bt?.maxX ?? j + qe) - 24, dn = Bt ? Bt.minY + Math.min(90, Math.max(42, Bt.height * 0.22)) : tn, Jn = Math.max(tn, dn);
      return [Math.min(Bn, Math.max(an, D.at[0])), Math.min(We + Ft - 24, Jn)];
    }
    function Ae(D) {
      if (!D.text || String(D.cat || "") !== "label") return 14;
      const Y = String(D.id || "").trim().toLowerCase();
      return Y.startsWith("__label__") ? 14 : [
        "label",
        "room-label",
        "scene-label",
        "place-label",
        "map-label"
      ].includes(Y) ? 13 : 14;
    }
    function Re(D) {
      const Y = String(D.id || "");
      if (!Y.startsWith("__label__")) return null;
      const j = Y.slice(9);
      return (k.value?.elements || []).find((We) => We.id === j) || null;
    }
    function Be(D) {
      return String(D?.cat || "").trim().toLowerCase() === "actor" && String(D?.actorKey || D?.id || "").trim().toLowerCase() === "player";
    }
    function je(D) {
      if (Be(D)) return ie.value;
      const Y = Re(D);
      return Y && Be(Y) ? ie.value : String(D.text || "");
    }
    function Qe(D) {
      const Y = Re(D.element), j = String(Y?.cat || D.element.cat || "").trim().toLowerCase(), We = String(Y?.actorKey || Y?.id || D.element.id || "").trim().toLowerCase(), qe = String(D.element.id || "").trim().toLowerCase();
      return j === "actor" && We === "player" ? 0 : j === "actor" ? 1 : [
        "danger",
        "door",
        "marker",
        "magic",
        "secret"
      ].includes(j) ? 2 : qe.startsWith("__label__") ? 3 : [
        "label",
        "room-label",
        "scene-label",
        "place-label",
        "map-label"
      ].includes(qe) ? 5 : 4;
    }
    function ut(D, Y, j) {
      if (!o1(D)) return [];
      const We = j || en(D), qe = Km(D.material), Ft = (qe?.opacity ?? 1) * ot(D), tn = qe?.blend || "normal", Bt = iq(D.cat, D.material), an = j === "remove" ? Y * 110 : o.value === "full" ? Y * 115 : We === "stable" ? 0 : Y * 30, Bn = Be(D);
      if (D.text) {
        const [Sr, yn] = se(D), rs = je(D);
        return rs ? [{
          element: D,
          id: `${D.id}-label`,
          layer: "label",
          path: "",
          color: j === "remove" ? "#b94035" : st(D),
          fill: "none",
          blend: "normal",
          opacity: ot(D),
          z: Bt,
          strokeWidth: 0,
          dash: "",
          delayMs: an,
          durationMs: 900,
          length: 60,
          opKind: We,
          text: rs,
          x: Sr,
          y: yn,
          fontSize: Ae(D),
          anchor: "middle",
          transform: "",
          fillRule: "nonzero",
          gameIcon: !1
        }] : [];
      }
      const dn = Je(D);
      if (!dn) return [];
      const Jn = D.icon ? xg(D.icon) : null, sr = Jn ? 120 : Lt(D, dn), Un = j === "remove" ? 1050 : Math.max(680, Math.min(2200, sr * 4.2)), wr = j === "remove" ? "#b94035" : st(D), qn = j === "remove" ? lt(D) ? "rgba(185, 64, 53, 0.16)" : "none" : pt(D), vn = [];
      if (qe?.layer === "light" && j !== "remove" && lt(D))
        return vn.push({
          element: D,
          id: `${D.id}-light`,
          layer: "light",
          path: dn,
          color: wr,
          fill: qe.paint,
          blend: tn,
          opacity: Ft,
          z: Bt,
          strokeWidth: 0,
          dash: "",
          delayMs: an,
          durationMs: Un,
          length: sr,
          opKind: We,
          text: "",
          x: 0,
          y: 0,
          fontSize: 0,
          anchor: "middle",
          transform: "",
          fillRule: "nonzero",
          gameIcon: !1
        }), vn;
      if (Bn && me.value && j !== "remove") {
        const rs = `${String(D.id || "player").trim()}-player-avatar`;
        return vn.push({
          element: D,
          id: `${D.id}-avatar`,
          layer: "avatar",
          path: oe({
            ...D,
            circle: 12
          }),
          color: wr,
          fill: `url(#${rs})`,
          blend: "normal",
          opacity: ot(D),
          z: Bt,
          strokeWidth: 0,
          dash: "",
          delayMs: an,
          durationMs: Un,
          length: Math.max(48, Math.PI * 2 * 12),
          opKind: We,
          text: "",
          x: 0,
          y: 0,
          fontSize: 0,
          anchor: "middle",
          transform: "",
          fillRule: "nonzero",
          gameIcon: !1,
          avatarHref: me.value,
          patternId: rs,
          patternX: D.at[0] - 12,
          patternY: D.at[1] - 12,
          patternSize: 24
        }), vn.push({
          element: D,
          id: `${D.id}-player-outline`,
          layer: "line",
          path: oe({
            ...D,
            circle: 13.25
          }),
          color: "#18120f",
          fill: "none",
          blend: "normal",
          opacity: 0.86 * ot(D),
          z: Bt,
          strokeWidth: 1.15,
          dash: "3 2",
          delayMs: an,
          durationMs: Un,
          length: Math.max(48, Math.PI * 2 * 13.25),
          opKind: We,
          text: "",
          x: 0,
          y: 0,
          fontSize: 0,
          anchor: "middle",
          transform: "",
          fillRule: "nonzero",
          gameIcon: !1
        }), vn;
      }
      if (Jn)
        return vn.push({
          element: D,
          id: `${D.id}-glyph`,
          layer: "line",
          path: dn,
          color: wr,
          fill: wr,
          blend: "normal",
          opacity: ot(D),
          z: Bt,
          strokeWidth: 0,
          dash: "",
          delayMs: an,
          durationMs: Un,
          length: sr,
          opKind: We,
          text: "",
          x: 0,
          y: 0,
          fontSize: 0,
          anchor: "middle",
          transform: "",
          glyphTransform: m8(D.at[0], D.at[1]),
          glyphScaleTransform: g8(),
          fillRule: Jn.fillRule,
          gameIcon: !0
        }), vn;
      if (qn !== "none" && vn.push({
        element: D,
        id: `${D.id}-fill`,
        layer: "fill",
        path: dn,
        color: wr,
        fill: qn,
        blend: tn,
        opacity: Ft,
        z: Bt,
        strokeWidth: 0,
        dash: "",
        delayMs: an,
        durationMs: Un,
        length: sr,
        opKind: We,
        text: "",
        x: 0,
        y: 0,
        fontSize: 0,
        anchor: "middle",
        transform: "",
        fillRule: "nonzero",
        gameIcon: !1
      }), vn.push({
        element: D,
        id: `${D.id}-line`,
        layer: "line",
        path: dn,
        color: wr,
        fill: "none",
        blend: "normal",
        opacity: ot(D),
        z: Bt,
        strokeWidth: ht(D),
        dash: It(D),
        delayMs: an,
        durationMs: Un,
        length: sr,
        opKind: We,
        text: "",
        x: 0,
        y: 0,
        fontSize: 0,
        anchor: "middle",
        transform: "",
        fillRule: "nonzero",
        gameIcon: !1
      }), Bn) {
        const Sr = D.icon ? 18 : typeof D.circle == "number" ? D.circle + 5 : 16;
        vn.push({
          element: D,
          id: `${D.id}-player-outline`,
          layer: "line",
          path: oe({
            ...D,
            circle: Sr
          }),
          color: "#18120f",
          fill: "none",
          blend: "normal",
          opacity: 0.72 * ot(D),
          z: Bt,
          strokeWidth: 1.15,
          dash: "3 2",
          delayMs: an,
          durationMs: Un,
          length: Math.max(48, Math.PI * 2 * Sr),
          opKind: We,
          text: "",
          x: 0,
          y: 0,
          fontSize: 0,
          anchor: "middle",
          transform: "",
          fillRule: "nonzero",
          gameIcon: !1
        });
      }
      return vn;
    }
    const wt = ne(() => (k.value?.elements || []).flatMap((D, Y) => ut(D, Y))), rn = ne(() => wt.value.filter((D) => D.layer === "fill").sort((D, Y) => D.z - Y.z || Qo(D.element.id, Y.element.id) || Qo(D.id, Y.id))), Ht = ne(() => wt.value.filter((D) => D.layer === "avatar").sort((D, Y) => D.z - Y.z || Qo(D.element.id, Y.element.id) || Qo(D.id, Y.id))), Dn = ne(() => wt.value.filter((D) => D.layer === "line")), br = ne(() => Dn.value.filter((D) => !D.gameIcon)), fr = ne(() => Dn.value.filter((D) => D.gameIcon)), ct = ne(() => wt.value.filter((D) => D.layer === "light").sort((D, Y) => D.z - Y.z || Qo(D.element.id, Y.element.id) || Qo(D.id, Y.id))), Ke = ne(() => wt.value.filter((D) => D.layer === "label")), it = ne(() => Ide(Ke.value, R.value, { priority: Qe })), En = ne(() => J.value.flatMap((D, Y) => ut(D, Y, "remove"))), On = ne(() => En.value.filter((D) => D.layer === "fill")), Gs = ne(() => En.value.filter((D) => D.layer === "light")), Gr = ne(() => En.value.filter((D) => D.layer === "line")), rr = ne(() => Gr.value.filter((D) => !D.gameIcon)), Vn = ne(() => Gr.value.filter((D) => D.gameIcon)), Ms = ne(() => En.value.filter((D) => D.layer === "label")), mn = ne(() => wt.value.filter((D) => o.value === "full" || D.opKind !== "stable")), Fn = ne(() => {
      const D = mn.value;
      return D.length ? Math.max(...D.map((Y) => Y.delayMs + Y.durationMs)) + 360 : 1200;
    }), Ps = ne(() => ({ "--map-progress-duration": `${Fn.value}ms` })), Ot = ne(() => {
      const D = n(mn.value);
      return D ? {
        offsetPath: `path("${D.path}")`,
        animationDelay: `${D.delayMs}ms`,
        animationDuration: `${D.durationMs}ms`
      } : { display: "none" };
    }), wn = ne(() => {
      const D = {
        warm: {
          fill: "url(#mood-warm)",
          blend: "overlay",
          opacity: 0.34
        },
        cold: {
          fill: "url(#mood-cold)",
          blend: "multiply",
          opacity: 0.28
        },
        dark: {
          fill: "url(#mood-dark)",
          blend: "multiply",
          opacity: 0.36
        },
        mystic: {
          fill: "url(#mood-mystic)",
          blend: "screen",
          opacity: 0.22
        },
        danger: {
          fill: "url(#mood-danger)",
          blend: "multiply",
          opacity: 0.26
        },
        calm: {
          fill: "url(#mood-calm)",
          blend: "screen",
          opacity: 0.18
        }
      }[String(k.value?.meta?.mood || "neutral")];
      if (!D) return null;
      const [Y, j, We, qe] = R.value, Ft = {
        opacity: D.opacity,
        mixBlendMode: D.blend
      };
      return {
        ...D,
        x: Y,
        y: j,
        width: We,
        height: qe,
        style: Ft
      };
    });
    function sn() {
      Q(), !(o.value !== "timeline" || !b.value || w.value.length <= 1) && Ut(() => {
        v = window.setTimeout(() => {
          o.value === "timeline" && (l.value = l.value + 1 >= w.value.length ? 0 : l.value + 1, a.value += 1, sn());
        }, Fn.value + 420);
      });
    }
    function Wt(D) {
      return {
        "--map-delay": `${D.delayMs}ms`,
        "--map-duration": `${D.durationMs}ms`,
        "--map-length": `${D.length}`,
        opacity: D.opacity,
        mixBlendMode: D.blend === "normal" ? void 0 : D.blend
      };
    }
    function Gt(D) {
      return [
        "map-element",
        D.gameIcon ? "map-game-icon" : `map-${D.layer}`,
        `cat-${D.element.cat || "wall"}`,
        `op-${D.opKind}`,
        {
          "is-animated": o.value === "full" || D.opKind !== "stable",
          "is-latest": H.value.has(D.element.id),
          "is-game-icon": D.gameIcon
        }
      ];
    }
    function Rn() {
      Q(), o.value = "patch", a.value += 1, de();
    }
    function da() {
      Q(), o.value = "full", a.value += 1, de();
    }
    function fa() {
      b.value && (o.value = "timeline", l.value = 0, a.value += 1, de(), sn());
    }
    function bt(D) {
      if (Q(), o.value = "timeline", !b.value) return;
      const Y = w.value.length;
      Y && (l.value = (l.value + D + Y) % Y, a.value += 1, de());
    }
    function Ye() {
      p.value = !p.value;
    }
    function Pe() {
      p.value = !1;
    }
    function Xt(D) {
      const Y = D.target instanceof HTMLSelectElement ? D.target : null;
      i.value = String(Y?.value || "").trim(), s("update:selectedDocId", i.value), o.value = "patch", Q(), a.value += 1, de();
    }
    function Rr(D) {
      const Y = D.currentTarget instanceof SVGSVGElement ? D.currentTarget : u.value;
      if (!Y || D.button !== 0) return;
      const j = Y.getBoundingClientRect();
      if (j.width <= 0 || j.height <= 0) return;
      const [, , We, qe] = R.value, [Ft, tn] = d.value;
      m.value = {
        pointerId: D.pointerId,
        startClientX: D.clientX,
        startClientY: D.clientY,
        startPanX: Ft,
        startPanY: tn,
        viewWidth: We,
        viewHeight: qe,
        clientWidth: j.width,
        clientHeight: j.height
      }, Y.setPointerCapture(D.pointerId);
    }
    function ns(D) {
      const Y = m.value;
      if (!Y || Y.pointerId !== D.pointerId) return;
      const j = -((D.clientX - Y.startClientX) / Y.clientWidth) * Y.viewWidth, We = -((D.clientY - Y.startClientY) / Y.clientHeight) * Y.viewHeight;
      d.value = [Number((Y.startPanX + j).toFixed(2)), Number((Y.startPanY + We).toFixed(2))];
    }
    function ha(D) {
      const Y = m.value;
      if (!Y || Y.pointerId !== D.pointerId) return;
      const j = D.currentTarget instanceof SVGSVGElement ? D.currentTarget : u.value;
      j?.hasPointerCapture(D.pointerId) && j.releasePointerCapture(D.pointerId), m.value = null;
    }
    function St(D) {
      if (!u.value) return;
      D.preventDefault();
      const Y = D.deltaY > 0 ? -1 : 1;
      ge(f.value + Y * 0.18, {
        clientX: D.clientX,
        clientY: D.clientY
      });
    }
    return (D, Y) => (O(), B("aside", { class: Fe(["tavern-map-panel xb-editor", { "is-compact": e.compact }]) }, [
      e.compact ? Te("", !0) : (O(), B("header", xde, [g("div", kde, [
        Y[4] || (Y[4] = g("span", { class: "tavern-map-kicker" }, "LIVE CARTOGRAPHY", -1)),
        g("strong", null, te(ke.value), 1),
        I.value ? (O(), B("span", Mde, te(Ne.value) + " · " + te(F.value) + " elements", 1)) : Te("", !0)
      ]), g("div", Pde, [
        g("select", {
          class: "tavern-map-select",
          value: I.value?.docId || "",
          disabled: Z.value.length <= 1,
          "aria-label": "选择地图",
          onChange: Xt
        }, [(O(!0), B(He, null, tt(Z.value, (j) => (O(), B("option", {
          key: j.docId,
          value: j.docId
        }, te(j.active ? "当前 · " : "") + te(j.title || j.docId), 9, Nde))), 128))], 40, Rde),
        g("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: o.value === "patch" }]),
          disabled: !$.value,
          onClick: Rn
        }, " 最近回合 ", 10, $de),
        g("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: o.value === "full" }]),
          disabled: !W.value,
          onClick: da
        }, " 完整重绘 ", 10, Lde),
        g("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: o.value === "timeline" }]),
          disabled: !b.value,
          onClick: fa
        }, " 回合回放 ", 10, Dde)
      ])])),
      W.value ? (O(), B("div", {
        key: 1,
        class: Fe(["tavern-map-canvas", [
          `theme-${ve.value}`,
          `mode-${o.value}`,
          { "is-panning": m.value }
        ]])
      }, [
        e.compact ? (O(), B("div", Ode, [g("select", {
          class: "tavern-map-select",
          value: I.value?.docId || "",
          disabled: Z.value.length <= 1,
          "aria-label": "选择地图",
          onChange: Xt
        }, [(O(!0), B(He, null, tt(Z.value, (j) => (O(), B("option", {
          key: j.docId,
          value: j.docId
        }, te(j.active ? "当前 · " : "") + te(j.title || j.docId), 9, Bde))), 128))], 40, Fde)])) : Te("", !0),
        g("div", Ude, [
          g("button", {
            type: "button",
            "aria-label": "缩小地图",
            disabled: f.value <= 0.5,
            onClick: Y[0] || (Y[0] = (j) => pe(-0.25))
          }, " - ", 8, qde),
          g("button", {
            type: "button",
            class: "tavern-map-zoom-value",
            "aria-label": "重置地图缩放",
            onClick: de
          }, te(le.value), 1),
          g("button", {
            type: "button",
            "aria-label": "放大地图",
            disabled: f.value >= 4,
            onClick: Y[1] || (Y[1] = (j) => pe(0.25))
          }, " + ", 8, Hde)
        ]),
        (O(), B("svg", {
          ref_key: "mapSvgRef",
          ref: u,
          key: a.value,
          viewBox: V.value,
          preserveAspectRatio: "xMidYMid meet",
          role: "img",
          "aria-label": ke.value,
          onPointerdown: Rr,
          onPointermove: ns,
          onPointerup: ha,
          onPointercancel: ha,
          onDblclick: de,
          onWheel: St
        }, [
          g("defs", null, [Y[5] || (Y[5] = Al('<filter id="tavern-map-sketch" x="-5%" y="-5%" width="110%" height="110%"><feTurbulence type="fractalNoise" baseFrequency="0.025" numOctaves="3" seed="7" result="noise"></feTurbulence><feDisplacementMap in="SourceGraphic" in2="noise" scale="0.65"></feDisplacementMap></filter><filter id="tavern-map-glow" x="-30%" y="-30%" width="160%" height="160%"><feGaussianBlur stdDeviation="3" result="blur"></feGaussianBlur><feMerge><feMergeNode in="blur"></feMergeNode><feMergeNode in="SourceGraphic"></feMergeNode></feMerge></filter><pattern id="mat-wood" width="36" height="18" patternUnits="userSpaceOnUse"><rect width="36" height="18" fill="#5d422c"></rect><path d="M0 9 H36 M12 0 V9 M28 9 V18" stroke="#382517" stroke-width="1.2" opacity="0.85"></path><path d="M2 4 C8 2 12 6 18 4 M4 14 C12 12 18 16 30 13" stroke="#77563a" stroke-width="0.8" fill="none" opacity="0.6"></path></pattern><pattern id="mat-stone" width="54" height="30" patternUnits="userSpaceOnUse"><rect width="54" height="30" fill="#55505a"></rect><path d="M0 15 H54 M18 0 V15 M38 15 V30" stroke="#2b2931" stroke-width="2" fill="none" opacity="0.82"></path></pattern><pattern id="mat-tile" width="28" height="28" patternUnits="userSpaceOnUse"><rect width="28" height="28" fill="#343746"></rect><rect x="2" y="2" width="24" height="24" rx="2" fill="#404454" stroke="#20232d" stroke-width="1.4"></rect></pattern><pattern id="mat-carpet" width="22" height="22" patternUnits="userSpaceOnUse"><rect width="22" height="22" fill="#8d2638"></rect><path d="M0 11 H22 M11 0 V22 M4 4 L18 18 M18 4 L4 18" stroke="#f0c77c" stroke-width="0.8" opacity="0.45"></path></pattern><radialGradient id="mat-blood"><stop offset="0%" stop-color="#5b1118" stop-opacity="0.92"></stop><stop offset="100%" stop-color="#5b1118" stop-opacity="0.18"></stop></radialGradient><pattern id="mat-water" width="34" height="18" patternUnits="userSpaceOnUse"><rect width="34" height="18" fill="#2f7990" opacity="0.72"></rect><path d="M0 6 C8 1 14 11 22 6 S30 4 34 7 M0 14 C7 10 14 17 22 13 S31 10 34 13" stroke="#bde9ee" stroke-width="1" fill="none" opacity="0.5"></path></pattern><pattern id="mat-grass" width="24" height="24" patternUnits="userSpaceOnUse"><rect width="24" height="24" fill="#486d3e"></rect><path d="M5 20 L8 12 M12 22 L14 11 M18 20 L16 10 M3 10 L6 5 M21 13 L19 7" stroke="#7ca45f" stroke-width="1.2" opacity="0.68"></path></pattern><pattern id="mat-dirt" width="26" height="26" patternUnits="userSpaceOnUse"><rect width="26" height="26" fill="#76583a"></rect><circle cx="7" cy="9" r="1.8" fill="#4b3725" opacity="0.45"></circle><circle cx="18" cy="17" r="2.2" fill="#9b7651" opacity="0.45"></circle></pattern><pattern id="mat-snow" width="30" height="30" patternUnits="userSpaceOnUse"><rect width="30" height="30" fill="#dce8ed"></rect><path d="M5 8 H11 M8 5 V11 M20 20 H26 M23 17 V23" stroke="#ffffff" stroke-width="1.4" opacity="0.75"></path></pattern><pattern id="mat-metal" width="32" height="32" patternUnits="userSpaceOnUse"><rect width="32" height="32" fill="#62666d"></rect><path d="M-4 28 L28 -4 M6 36 L38 4" stroke="#a8adb4" stroke-width="3" opacity="0.28"></path></pattern><pattern id="mat-rune" width="34" height="34" patternUnits="userSpaceOnUse"><rect width="34" height="34" fill="#44335e" opacity="0.55"></rect><path d="M17 4 L27 17 L17 30 L7 17 Z M17 9 V25 M9 17 H25" stroke="#bda5ff" stroke-width="1.2" fill="none" opacity="0.72"></path></pattern><radialGradient id="grad-warm"><stop offset="0%" stop-color="#ffb15e" stop-opacity="0.8"></stop><stop offset="100%" stop-color="#ff8a3c" stop-opacity="0"></stop></radialGradient><radialGradient id="grad-cold"><stop offset="0%" stop-color="#86c7ff" stop-opacity="0.56"></stop><stop offset="100%" stop-color="#3567aa" stop-opacity="0"></stop></radialGradient><radialGradient id="mood-warm"><stop offset="0%" stop-color="#ffcf88" stop-opacity="0.2"></stop><stop offset="100%" stop-color="#633016" stop-opacity="0.45"></stop></radialGradient><radialGradient id="mood-cold"><stop offset="0%" stop-color="#8ab6ff" stop-opacity="0.06"></stop><stop offset="100%" stop-color="#0b1630" stop-opacity="0.62"></stop></radialGradient><radialGradient id="mood-dark"><stop offset="0%" stop-color="#1d2530" stop-opacity="0.12"></stop><stop offset="100%" stop-color="#000000" stop-opacity="0.78"></stop></radialGradient><radialGradient id="mood-mystic"><stop offset="0%" stop-color="#a77dff" stop-opacity="0.24"></stop><stop offset="100%" stop-color="#123f4d" stop-opacity="0.32"></stop></radialGradient><radialGradient id="mood-danger"><stop offset="0%" stop-color="#c9463d" stop-opacity="0.14"></stop><stop offset="100%" stop-color="#2a0606" stop-opacity="0.56"></stop></radialGradient><radialGradient id="mood-calm"><stop offset="0%" stop-color="#c4e8d1" stop-opacity="0.16"></stop><stop offset="100%" stop-color="#49706f" stop-opacity="0.18"></stop></radialGradient>', 21)), (O(!0), B(He, null, tt(Ht.value, (j) => (O(), B("pattern", {
            id: j.patternId,
            key: j.patternId,
            x: j.patternX,
            y: j.patternY,
            width: j.patternSize,
            height: j.patternSize,
            patternUnits: "userSpaceOnUse"
          }, [g("image", {
            href: j.avatarHref,
            x: j.patternX,
            y: j.patternY,
            width: j.patternSize,
            height: j.patternSize,
            preserveAspectRatio: "xMidYMid slice"
          }, null, 8, Wde)], 8, Kde))), 128))]),
          g("g", Gde, [(O(!0), B(He, null, tt(rn.value, (j) => (O(), B("path", {
            key: j.id,
            d: j.path,
            fill: j.fill,
            transform: j.transform,
            "fill-rule": j.fillRule,
            class: Fe(Gt(j)),
            style: Nn(Wt(j))
          }, null, 14, Vde))), 128))]),
          g("g", Jde, [(O(!0), B(He, null, tt(Ht.value, (j) => (O(), B("path", {
            key: j.id,
            d: j.path,
            fill: j.fill,
            transform: j.transform,
            "fill-rule": j.fillRule,
            class: Fe(Gt(j)),
            style: Nn(Wt(j))
          }, null, 14, Yde))), 128))]),
          g("g", Xde, [(O(!0), B(He, null, tt(br.value, (j) => (O(), B("path", {
            key: j.id,
            d: j.path,
            fill: j.fill,
            stroke: j.color,
            "stroke-width": j.strokeWidth,
            "stroke-dasharray": j.dash,
            transform: j.transform,
            "fill-rule": j.fillRule,
            "stroke-linecap": "round",
            "stroke-linejoin": "round",
            class: Fe(Gt(j)),
            style: Nn(Wt(j))
          }, null, 14, jde))), 128)), (O(!0), B(He, null, tt(fr.value, (j) => (O(), B("g", {
            key: j.id,
            transform: j.glyphTransform,
            class: Fe(Gt(j)),
            style: Nn(Wt(j))
          }, [g("g", { transform: j.glyphScaleTransform }, [g("path", {
            d: j.path,
            fill: j.fill,
            "fill-rule": j.fillRule,
            transform: "translate(-256, -256)",
            class: "map-game-icon-path"
          }, null, 8, efe)], 8, Zde)], 14, Qde))), 128))]),
          g("g", tfe, [(O(!0), B(He, null, tt(ct.value, (j) => (O(), B("path", {
            key: j.id,
            d: j.path,
            fill: j.fill,
            transform: j.transform,
            "fill-rule": j.fillRule,
            class: Fe(Gt(j)),
            style: Nn(Wt(j))
          }, null, 14, nfe))), 128))]),
          wn.value ? (O(), B("rect", {
            key: 0,
            class: "map-mood-overlay",
            x: wn.value.x,
            y: wn.value.y,
            width: wn.value.width,
            height: wn.value.height,
            fill: wn.value.fill,
            style: Nn(wn.value.style)
          }, null, 12, rfe)) : Te("", !0),
          g("g", sfe, [(O(!0), B(He, null, tt(it.value, (j) => (O(), B("text", {
            key: j.id,
            x: j.x,
            y: j.y,
            "font-size": j.fontSize,
            fill: j.color,
            stroke: "rgba(245, 235, 210, 0.78)",
            "stroke-width": "4",
            "stroke-linejoin": "round",
            "paint-order": "stroke",
            "text-anchor": j.anchor,
            class: Fe(Gt(j)),
            style: Nn(Wt(j))
          }, te(j.text), 15, afe))), 128))]),
          g("g", ofe, [
            (O(!0), B(He, null, tt(On.value, (j) => (O(), B("path", {
              key: `removed-fill-${j.id}`,
              d: j.path,
              fill: j.fill,
              transform: j.transform,
              "fill-rule": j.fillRule,
              class: Fe(Gt(j)),
              style: Nn(Wt(j))
            }, null, 14, ife))), 128)),
            (O(!0), B(He, null, tt(Gs.value, (j) => (O(), B("path", {
              key: `removed-light-${j.id}`,
              d: j.path,
              fill: j.fill,
              transform: j.transform,
              "fill-rule": j.fillRule,
              class: Fe(Gt(j)),
              style: Nn(Wt(j))
            }, null, 14, lfe))), 128)),
            (O(!0), B(He, null, tt(rr.value, (j) => (O(), B("path", {
              key: `removed-line-${j.id}`,
              d: j.path,
              fill: j.fill,
              stroke: j.color,
              "stroke-width": j.strokeWidth,
              "stroke-dasharray": j.dash,
              transform: j.transform,
              "fill-rule": j.fillRule,
              "stroke-linecap": "round",
              "stroke-linejoin": "round",
              class: Fe(Gt(j)),
              style: Nn(Wt(j))
            }, null, 14, ufe))), 128)),
            (O(!0), B(He, null, tt(Vn.value, (j) => (O(), B("g", {
              key: `removed-game-icon-${j.id}`,
              transform: j.glyphTransform,
              class: Fe(Gt(j)),
              style: Nn(Wt(j))
            }, [g("g", { transform: j.glyphScaleTransform }, [g("path", {
              d: j.path,
              fill: j.fill,
              "fill-rule": j.fillRule,
              transform: "translate(-256, -256)",
              class: "map-game-icon-path"
            }, null, 8, ffe)], 8, dfe)], 14, cfe))), 128)),
            (O(!0), B(He, null, tt(Ms.value, (j) => (O(), B("text", {
              key: `removed-label-${j.id}`,
              x: j.x,
              y: j.y,
              "font-size": j.fontSize,
              fill: j.color,
              stroke: "rgba(245, 235, 210, 0.78)",
              "stroke-width": "4",
              "stroke-linejoin": "round",
              "paint-order": "stroke",
              "text-anchor": j.anchor,
              class: Fe(Gt(j)),
              style: Nn(Wt(j))
            }, te(j.text), 15, hfe))), 128))
          ])
        ], 40, zde)),
        (O(), B("div", {
          key: `pen-${a.value}`,
          class: "tavern-map-pen",
          style: Nn(Ot.value)
        }, null, 4)),
        (O(), B("div", {
          key: `progress-${a.value}`,
          class: "tavern-map-progress",
          style: Nn(Ps.value)
        }, null, 4)),
        L.value ? (O(), B("div", {
          key: 1,
          class: Fe(["tavern-map-badge-shell", { "is-open": p.value }])
        }, [g("button", {
          type: "button",
          class: "tavern-map-badge-toggle",
          "aria-expanded": p.value,
          title: p.value ? "收起地图更新提示" : "显示地图更新提示",
          "aria-label": "切换地图更新提示",
          onClick: Ye
        }, " ! ", 8, pfe), p.value ? (O(), B("button", {
          key: 0,
          type: "button",
          class: "tavern-map-badge",
          title: "收起地图更新提示",
          onClick: Pe
        }, [g("span", null, te(we.value), 1), g("strong", null, te(ce.value), 1)])) : Te("", !0)], 2)) : Te("", !0),
        o.value === "timeline" && b.value && w.value.length > 1 ? (O(), B("div", mfe, [g("button", {
          type: "button",
          onClick: Y[2] || (Y[2] = (j) => bt(-1))
        }, " 上一回合 "), g("button", {
          type: "button",
          onClick: Y[3] || (Y[3] = (j) => bt(1))
        }, " 下一回合 ")])) : Te("", !0),
        ee.value.length ? (O(), B("div", {
          key: `removed-${a.value}`,
          class: "tavern-map-removed-stack"
        }, [(O(!0), B(He, null, tt(ee.value, (j) => (O(), B("span", {
          key: j.id,
          style: Nn({ animationDelay: `${j.delayMs}ms` })
        }, " - " + te(j.id), 5))), 128))])) : Te("", !0)
      ], 2)) : (O(), B("div", gfe, [
        e.compact ? (O(), B("div", vfe, [g("select", {
          class: "tavern-map-select",
          value: I.value?.docId || "",
          disabled: Z.value.length <= 1,
          "aria-label": "选择地图",
          onChange: Xt
        }, [(O(!0), B(He, null, tt(Z.value, (j) => (O(), B("option", {
          key: j.docId,
          value: j.docId
        }, te(j.active ? "当前 · " : "") + te(j.title || j.docId), 9, _fe))), 128))], 40, yfe)])) : Te("", !0),
        Y[6] || (Y[6] = g("strong", null, "地图待初始化", -1)),
        Y[7] || (Y[7] = g("span", null, "当前会话已备好种子地图；剧情出现明确空间变化后，后台会激活并维护它。", -1))
      ])),
      e.compact ? Te("", !0) : (O(), B("footer", bfe, [
        g("span", null, te(ae.value) + " patches", 1),
        Oe.value.length ? (O(), B("span", wfe, te(Oe.value[0]), 1)) : Te("", !0),
        Oe.value[1] ? (O(), B("span", Sfe, te(Oe.value[1]), 1)) : Te("", !0),
        g("span", Afe, te(T(p8)), 1)
      ]))
    ], 2));
  }
}), Efe = Tfe, Cfe = { class: "tavern-memory-editor xb-editor" }, Ife = { class: "tavern-editor-head xb-editor-head" }, xfe = { class: "tavern-editor-actions xb-editor-actions" }, kfe = ["disabled"], Mfe = ["disabled"], Pfe = ["disabled"], Rfe = ["disabled"], Nfe = { class: "tavern-editor-body xb-editor-body" }, $fe = ["data-markdown-signature", "innerHTML"], Lfe = ["value", "readonly"], Dfe = {
  key: 2,
  class: "tavern-editor-empty"
}, Ofe = { class: "tavern-editor-foot xb-editor-foot" }, Ffe = { key: 0 }, Bfe = { key: 1 }, Ufe = { key: 2 }, qfe = { key: 3 }, Hfe = /* @__PURE__ */ bn({
  __name: "TavernMemoryEditor",
  props: {
    documentAvailable: { type: Boolean },
    readOnly: { type: Boolean },
    dirty: { type: Boolean },
    mode: {},
    draft: {},
    previewHtml: {},
    previewSignature: {},
    status: {},
    hasSelectedFile: { type: Boolean },
    loadedPath: {},
    fileMeta: {}
  },
  emits: [
    "update:draft",
    "enter-edit",
    "preview",
    "discard",
    "save"
  ],
  setup(e, { emit: t }) {
    const n = t;
    function r(s) {
      n("update:draft", s.target.value);
    }
    return (s, a) => (O(), B("aside", Cfe, [
      g("header", Ife, [g("div", xfe, [
        g("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: e.mode === "edit" }]),
          disabled: !e.documentAvailable || e.readOnly,
          onClick: a[0] || (a[0] = (o) => s.$emit("enter-edit"))
        }, " 编辑 ", 10, kfe),
        g("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: e.mode === "preview" }]),
          disabled: !e.documentAvailable,
          onClick: a[1] || (a[1] = (o) => s.$emit("preview"))
        }, " 阅读 ", 10, Mfe),
        e.dirty ? (O(), B("button", {
          key: 0,
          type: "button",
          class: "tavern-editor-discard-button",
          disabled: !e.documentAvailable,
          onClick: a[2] || (a[2] = (o) => s.$emit("discard"))
        }, " 放弃 ", 8, Pfe)) : Te("", !0),
        e.dirty ? (O(), B("button", {
          key: 1,
          type: "button",
          class: "tavern-editor-save-button",
          disabled: !e.hasSelectedFile || !e.dirty || e.readOnly,
          onClick: a[3] || (a[3] = (o) => s.$emit("save"))
        }, " 保存 ", 8, Rfe)) : Te("", !0)
      ])]),
      g("div", Nfe, [e.documentAvailable && e.mode === "preview" ? (O(), B("div", {
        key: 0,
        class: "tavern-editor-preview xb-tavern-markdown",
        "data-markdown-signature": e.previewSignature,
        innerHTML: e.previewHtml
      }, null, 8, $fe)) : e.documentAvailable ? (O(), B("textarea", {
        key: 1,
        value: e.draft,
        readonly: e.readOnly,
        spellcheck: "false",
        "data-memory-editor-textarea": "true",
        onInput: r
      }, null, 40, Lfe)) : (O(), B("div", Dfe, " 选择一份记忆档案。 "))]),
      g("footer", Ofe, [
        e.readOnly || e.dirty ? (O(), B("span", Ffe, te(e.readOnly ? "只读" : "有未保存修改"), 1)) : Te("", !0),
        e.status ? (O(), B("span", Bfe, te(e.status), 1)) : Te("", !0),
        !e.hasSelectedFile && e.loadedPath ? (O(), B("span", Ufe, "原档案暂不可用")) : Te("", !0),
        e.hasSelectedFile ? (O(), B("span", qfe, te(e.fileMeta), 1)) : Te("", !0)
      ])
    ]));
  }
}), zfe = Hfe;
function Kfe(e, t = 36) {
  const n = /* @__PURE__ */ ue(!1);
  let r = null, s = 0;
  function a() {
    n.value = !1, r = null, s = 0;
  }
  function o(d) {
    d.button && d.button !== 0 || (r = d.clientY, s = 0, n.value = !0, d.currentTarget?.setPointerCapture?.(d.pointerId));
  }
  function l(d) {
    r !== null && (s = d.clientY - r, s > 0 && d.cancelable && d.preventDefault());
  }
  function i(d) {
    if (r === null) return;
    d.currentTarget?.releasePointerCapture?.(d.pointerId);
    const f = s >= t;
    a(), f && e();
  }
  function u(d) {
    d.currentTarget?.releasePointerCapture?.(d.pointerId), a();
  }
  return {
    dragging: n,
    handleSheetHandlePointerCancel: u,
    handleSheetHandlePointerDown: o,
    handleSheetHandlePointerMove: l,
    handleSheetHandlePointerUp: i
  };
}
var Wfe = { class: "tavern-workspace-panel" }, Gfe = { class: "tavern-workspace-tabs" }, Vfe = {
  key: 0,
  class: "tavern-state-panel"
}, Jfe = {
  class: "tavern-state-inline-switcher",
  role: "tablist",
  "aria-label": "地图视图"
}, Yfe = ["aria-selected"], Xfe = ["aria-selected"], jfe = {
  key: 2,
  class: "tavern-map-current-empty"
}, Qfe = {
  key: 1,
  class: "tavern-map-info-body"
}, Zfe = { class: "tavern-map-info-head" }, ehe = { class: "tavern-map-info-grid" }, the = {
  key: 0,
  class: "tavern-map-info-line"
}, nhe = {
  key: 1,
  class: "tavern-map-info-digest"
}, rhe = {
  key: 2,
  class: "tavern-map-info-empty"
}, she = ["disabled", "aria-expanded"], ahe = {
  id: "xb-tavern-memory-directory",
  class: "tavern-memory-directory"
}, ohe = {
  key: 0,
  class: "memory-search tavern-memory-search"
}, ihe = {
  key: 1,
  class: "memory-directory-list xb-files"
}, lhe = { class: "memory-file-tree" }, uhe = ["onClick"], che = { class: "memory-file-main" }, dhe = ["onClick"], fhe = { class: "memory-file-main" }, hhe = {
  key: 0,
  class: "muted compact"
}, phe = {
  key: 2,
  class: "muted compact"
}, mhe = /* @__PURE__ */ bn({
  __name: "TavernWorkspacePanel",
  props: { memoryDirectoryOpen: { type: Boolean } },
  emits: [
    "close",
    "close-memory-directory",
    "toggle-memory-directory"
  ],
  setup(e, { emit: t }) {
    const n = t, r = ere(), s = kv(), a = o8(), { chatWorkspacePanel: o, displayUserName: l, visibleUserAvatar: i } = a, { activeMemoryFiles: u, discardMemoryDraft: d, enterMemoryEditMode: f, expandMemoryFileGroup: m, formatMemoryFileMeta: v, MEMORY_FILE_BATCH_SIZE: y, MEMORY_TURN_BATCH_SIZE: S, memoryDirectoryGroups: x, memoryEditorDirty: I, memoryEditorDocumentAvailable: E, memoryEditorDraft: N, memoryEditorLoadedPath: $, memoryEditorMode: p, memoryEditorReadOnly: w, memoryEditorStatus: _, memoryFileDisplayName: b, memoryFiles: C, memoryFileSearchText: k, markdownSignature: P, previewMemoryDraft: H, renderChatMarkdown: G, saveSelectedMemoryFile: ee, selectedMemoryFileEntry: J, selectedMemoryFilePath: z, selectMemoryFile: R } = r, { activeMapDocId: V, atlasActiveLocationKey: le, atlasStateDocument: ve, atlasStatePatches: ke, mapStateDocuments: be, mapStateDocument: Ne, mapStatePatches: ce, sessionContract: we, tavernTasks: Oe } = a, { currentAssistantFloor: F } = s, ae = /* @__PURE__ */ ue("scene"), Z = /* @__PURE__ */ ue(""), ie = /* @__PURE__ */ ue(!1), me = ne(() => {
      const at = ve.value?.data, X = at && typeof at == "object" && !Array.isArray(at) ? at : {};
      return {
        version: 1,
        activeLocationKey: String(X.activeLocationKey || le.value || "").trim() || void 0,
        locations: Array.isArray(X.locations) ? X.locations : [],
        links: Array.isArray(X.links) ? X.links : [],
        actors: Array.isArray(X.actors) ? X.actors : []
      };
    }), Se = ne(() => {
      const at = me.value.activeLocationKey || le.value || "";
      return me.value.locations.find((X) => X.key === at) || null;
    }), W = ne(() => String(Se.value?.mapDocId || "").trim()), L = ne({
      get() {
        return ie.value ? String(Z.value || "").trim() : me.value.activeLocationKey ? W.value : String(V.value || "main").trim();
      },
      set(at) {
        Z.value = String(at || "").trim(), ie.value = !!Z.value;
      }
    }), Q = ne(() => {
      const at = Array.isArray(be.value) ? be.value : [], X = String(L.value || "").trim();
      return X && at.find((se) => se.docId === X) || null;
    }), de = ne(() => {
      const at = Q.value?.data;
      if (!at || typeof at != "object" || Array.isArray(at)) return null;
      const X = at, se = Ju();
      return {
        meta: X.meta && typeof X.meta == "object" && !Array.isArray(X.meta) ? {
          ...se.meta,
          ...X.meta
        } : se.meta,
        elements: Array.isArray(X.elements) ? X.elements.filter((Ae) => !!Ae && typeof Ae == "object" && !Array.isArray(Ae) && typeof Ae.id == "string") : []
      };
    }), ge = ne(() => de.value?.elements || []), pe = ne(() => String(de.value?.meta?.name || Q.value?.title || Q.value?.docId || "地图")), Me = ne(() => String(Q.value?.docId || "") === String(V.value || "main")), Ie = ne(() => Me.value ? ce.value : []), he = ne(() => !ie.value && !!me.value.activeLocationKey && !W.value), q = ne(() => String(Q.value?.digest || "").split(`
`).map((at) => at.trim()).filter(Boolean).slice(0, 3));
    function ye(at) {
      return String(at.actorKey || at.id || "").trim().toLowerCase() === "player" ? String(l.value || "User").trim() || "User" : String(at.text || at.id || "").trim();
    }
    const oe = ne(() => ge.value.filter((at) => at.cat === "actor").map((at) => ye(at)).filter(Boolean).slice(0, 6)), De = ne(() => {
      const at = ge.value, X = (se) => at.filter((Ae) => Ae.cat === se).length;
      return [
        {
          label: "元素",
          value: String(at.length)
        },
        {
          label: "人物",
          value: String(X("actor"))
        },
        {
          label: "出入口",
          value: String(X("door"))
        },
        {
          label: "标注",
          value: String(X("label") + X("marker"))
        }
      ];
    }), Je = ne(() => J.value ? b(J.value) : "");
    vt([W, be], () => {
      const at = Array.isArray(be.value) ? be.value : [], X = String(Z.value || "").trim();
      X && at.some((se) => se.docId === X) || ie.value && (ie.value = !1, Z.value = "");
    }, { immediate: !0 });
    function st() {
      n("close");
    }
    const { dragging: lt, handleSheetHandlePointerCancel: ot, handleSheetHandlePointerDown: pt, handleSheetHandlePointerMove: ht, handleSheetHandlePointerUp: It } = Kfe(st);
    function Lt() {
      n("toggle-memory-directory");
    }
    function en(at) {
      R(at).then((X) => {
        X && n("close-memory-directory");
      });
    }
    return (at, X) => (O(), B("aside", Wfe, [
      g("button", {
        type: "button",
        class: Fe(["chat-mobile-sheet-handle", { "is-dragging": T(lt) }]),
        title: "收起记忆面板",
        "aria-label": "收起记忆面板",
        onClick: st,
        onPointercancel: X[0] || (X[0] = (...se) => T(ot) && T(ot)(...se)),
        onPointerdown: X[1] || (X[1] = (...se) => T(pt) && T(pt)(...se)),
        onPointermove: X[2] || (X[2] = (...se) => T(ht) && T(ht)(...se)),
        onPointerup: X[3] || (X[3] = (...se) => T(It) && T(It)(...se))
      }, null, 34),
      g("div", Gfe, [
        g("button", {
          type: "button",
          class: Fe({ active: T(o) === "state" }),
          onClick: X[4] || (X[4] = (se) => o.value = "state")
        }, " 地图 ", 2),
        g("button", {
          type: "button",
          class: Fe({ active: T(o) === "memory" }),
          onClick: X[5] || (X[5] = (se) => o.value = "memory")
        }, " 记忆 ", 2),
        g("button", {
          type: "button",
          class: Fe({ active: T(o) === "event" }),
          onClick: X[6] || (X[6] = (se) => o.value = "event")
        }, " 事件 ", 2)
      ]),
      T(o) === "state" ? (O(), B("section", Vfe, [g("div", { class: Fe(["tavern-state-viewport", `is-${ae.value}`]) }, [g("div", Jfe, [g("button", {
        type: "button",
        role: "tab",
        "aria-selected": ae.value === "scene",
        class: Fe({ active: ae.value === "scene" }),
        onClick: X[7] || (X[7] = (se) => ae.value = "scene")
      }, " 场景图 ", 10, Yfe), g("button", {
        type: "button",
        role: "tab",
        "aria-selected": ae.value === "world",
        class: Fe({ active: ae.value === "world" }),
        onClick: X[8] || (X[8] = (se) => ae.value = "world")
      }, " 世界图 ", 10, Xfe)]), ae.value === "scene" && Q.value ? (O(), cn(Efe, {
        key: 0,
        "selected-doc-id": L.value,
        "onUpdate:selectedDocId": X[9] || (X[9] = (se) => L.value = se),
        compact: "",
        documents: T(be),
        "active-doc-id": T(V),
        document: Q.value,
        patches: Ie.value,
        "player-display-name": T(l),
        "player-avatar-url": T(i)
      }, null, 8, [
        "selected-doc-id",
        "documents",
        "active-doc-id",
        "document",
        "patches",
        "player-display-name",
        "player-avatar-url"
      ])) : ae.value === "world" ? (O(), cn(yx, {
        key: 1,
        "display-mode": "graph",
        document: T(ve),
        patches: T(ke),
        "active-location-key": T(le),
        "active-map-doc-id": T(V),
        "preview-map-doc-id": L.value,
        "map-documents": T(be)
      }, null, 8, [
        "document",
        "patches",
        "active-location-key",
        "active-map-doc-id",
        "preview-map-doc-id",
        "map-documents"
      ])) : (O(), B("div", jfe, [g("strong", null, te(Se.value?.name || "当前地点"), 1), g("p", null, te(he.value ? "当前地点暂无详细地图。" : "暂无地图信息。"), 1)]))], 2), g("article", { class: Fe(["tavern-map-info", { "is-world": ae.value === "world" }]) }, [ae.value === "world" ? (O(), cn(yx, {
        key: 0,
        "display-mode": "detail",
        document: T(ve),
        patches: T(ke),
        "active-location-key": T(le),
        "active-map-doc-id": T(V),
        "preview-map-doc-id": L.value,
        "map-documents": T(be)
      }, null, 8, [
        "document",
        "patches",
        "active-location-key",
        "active-map-doc-id",
        "preview-map-doc-id",
        "map-documents"
      ])) : Q.value ? (O(), B("div", Qfe, [
        g("header", Zfe, [g("div", null, [g("strong", null, te(pe.value), 1), g("span", null, te(Q.value.docId), 1)]), g("em", null, te(Me.value ? "当前场景" : "地图档案"), 1)]),
        g("dl", ehe, [(O(!0), B(He, null, tt(De.value, (se) => (O(), B("div", { key: se.label }, [g("dt", null, te(se.label), 1), g("dd", null, te(se.value), 1)]))), 128))]),
        oe.value.length ? (O(), B("div", the, [X[12] || (X[12] = g("span", null, "人物", -1)), g("strong", null, te(oe.value.join("、")), 1)])) : Te("", !0),
        q.value.length ? (O(), B("div", nhe, [(O(!0), B(He, null, tt(q.value, (se) => (O(), B("p", { key: se }, te(se), 1))), 128))])) : Te("", !0)
      ])) : (O(), B("div", rhe, " 暂无地图信息。 "))], 2)])) : T(o) === "memory" ? (O(), B("section", {
        key: 1,
        class: Fe(["tavern-memory-workspace", { "is-memory-directory-open": e.memoryDirectoryOpen }])
      }, [
        g("button", {
          type: "button",
          class: Fe(["tavern-memory-selector", { "is-open": e.memoryDirectoryOpen }]),
          disabled: !T(C).length,
          "aria-expanded": e.memoryDirectoryOpen ? "true" : "false",
          "aria-controls": "xb-tavern-memory-directory",
          title: "选择记忆文档",
          "aria-label": "选择记忆文档",
          onClick: Lt
        }, [
          X[13] || (X[13] = g("span", null, "记忆文档", -1)),
          g("strong", null, te(Je.value || "选择记忆档案"), 1),
          g("em", null, te(T(u).length) + " 个", 1),
          X[14] || (X[14] = g("i", { "aria-hidden": "true" }, null, -1))
        ], 10, she),
        g("aside", ahe, [T(C).length ? (O(), B("label", ohe, [er(g("input", {
          "onUpdate:modelValue": X[10] || (X[10] = (se) => /* @__PURE__ */ kn(k) ? k.value = se : null),
          type: "search",
          placeholder: "检索档案"
        }, null, 512), [[sa, T(k)]])])) : Te("", !0), T(C).length ? (O(), B("div", ihe, [(O(!0), B(He, null, tt(T(x), (se) => (O(), B("div", {
          key: se.key,
          class: "memory-file-group"
        }, [g("div", lhe, [(O(!0), B(He, null, tt(se.files, (Ae) => (O(), B("button", {
          key: Ae.path,
          type: "button",
          class: Fe(["memory-file", {
            active: T(z) === Ae.path,
            stale: Ae.status === "stale"
          }]),
          onClick: (Re) => en(Ae.path)
        }, [g("span", che, te(T(b)(Ae)), 1)], 10, uhe))), 128)), se.hiddenCount ? (O(), B("button", {
          key: 0,
          type: "button",
          class: "memory-file memory-file-more",
          onClick: (Ae) => T(m)(se.key)
        }, [g("span", fhe, "再显示 " + te(Math.min(se.hiddenCount, se.key === "turns" ? T(S) : T(y))) + " 个", 1)], 8, dhe)) : Te("", !0)])]))), 128)), T(k) && !T(x).length ? (O(), B("p", hhe, " 没有匹配的记忆档案。 ")) : Te("", !0)])) : (O(), B("p", phe, " 还没有记忆档案。 "))]),
        Wn(zfe, {
          draft: T(N),
          "onUpdate:draft": X[11] || (X[11] = (se) => /* @__PURE__ */ kn(N) ? N.value = se : null),
          "document-available": T(E),
          "read-only": T(w),
          dirty: T(I),
          mode: T(p),
          "preview-html": T(G)(T(N)),
          "preview-signature": T(P)(T(N)),
          status: T(_),
          "has-selected-file": !!T(J),
          "loaded-path": T($),
          "file-meta": T(J) ? T(v)(T(J)) : "",
          onEnterEdit: T(f),
          onPreview: T(H),
          onDiscard: T(d),
          onSave: T(ee)
        }, null, 8, [
          "draft",
          "document-available",
          "read-only",
          "dirty",
          "mode",
          "preview-html",
          "preview-signature",
          "status",
          "has-selected-file",
          "loaded-path",
          "file-meta",
          "onEnterEdit",
          "onPreview",
          "onDiscard",
          "onSave"
        ])
      ], 2)) : (O(), cn(sde, {
        key: 2,
        tasks: T(Oe),
        enabled: T(we).questOrchestration,
        "assistant-floor": T(F)
      }, null, 8, [
        "tasks",
        "enabled",
        "assistant-floor"
      ]))
    ]));
  }
}), ghe = mhe, vhe = { class: "panel step-panel native-workspace" }, yhe = { class: "panel-head preset-page-head" }, _he = { class: "panel-pills panel-head-actions" }, bhe = { class: "pill" }, whe = {
  key: 0,
  class: "preset-status-line"
}, She = { class: "worldbook-section worldbook-global-enable" }, Ahe = { class: "worldbook-global-bar" }, The = ["aria-expanded", "disabled"], Ehe = { class: "worldbook-global-current" }, Che = { key: 0 }, Ihe = { key: 1 }, xhe = {
  key: 0,
  class: "worldbook-global-list"
}, khe = [
  "checked",
  "disabled",
  "onChange"
], Mhe = {
  key: 0,
  class: "worldbook-global-empty"
}, Phe = { class: "worldbook-section worldbook-main-section" }, Rhe = { class: "preset-command-bar worldbook-command-bar" }, Nhe = { class: "preset-source-select worldbook-source-select" }, $he = ["value"], Lhe = {
  key: 0,
  value: "",
  disabled: ""
}, Dhe = ["value"], Ohe = { class: "native-settings-studio worldbook-overview-grid" }, Fhe = { class: "native-detail-panel worldbook-overview-detail worldbook-gateway-panel" }, Bhe = {
  key: 0,
  class: "worldbook-preview-surface"
}, Uhe = {
  key: 0,
  class: "worldbook-preview-status"
}, qhe = { class: "worldbook-metric-grid" }, Hhe = {
  key: 0,
  class: "worldbook-entry-preview-list"
}, zhe = ["open", "onToggle"], Khe = { class: "worldbook-entry-title" }, Whe = ["title", "aria-label"], Ghe = { class: "worldbook-entry-position" }, Vhe = {
  key: 0,
  class: "worldbook-entry-body"
}, Jhe = { class: "worldbook-entry-editor-head" }, Yhe = { key: 0 }, Xhe = { key: 1 }, jhe = { class: "worldbook-entry-editor-actions" }, Qhe = ["disabled"], Zhe = ["disabled"], epe = { class: "worldbook-entry-core-grid" }, tpe = { class: "worldbook-entry-title-row" }, npe = { class: "worldbook-entry-title-field" }, rpe = ["value"], spe = { class: "worldbook-entry-active-toggle" }, ape = ["checked"], ope = { class: "worldbook-entry-state-field" }, ipe = ["value"], lpe = { class: "worldbook-entry-position-field" }, upe = ["value"], cpe = ["value", "title"], dpe = { class: "worldbook-entry-depth-field" }, fpe = ["value", "disabled"], hpe = { class: "worldbook-entry-order-field" }, ppe = ["value"], mpe = { class: "worldbook-entry-probability-field" }, gpe = ["value", "disabled"], vpe = { class: "worldbook-entry-scan-depth-field" }, ype = ["value"], _pe = { class: "worldbook-entry-key-grid" }, bpe = { class: "worldbook-entry-editor-lines worldbook-entry-keywords-field" }, wpe = ["value"], Spe = { class: "worldbook-entry-filter-controls" }, Ape = { class: "worldbook-entry-logic-field" }, Tpe = ["value"], Epe = ["value"], Cpe = { class: "worldbook-entry-editor-lines worldbook-entry-keywords-field" }, Ipe = ["value"], xpe = { class: "worldbook-entry-editor-lines worldbook-entry-content-field" }, kpe = ["value"], Mpe = ["aria-expanded"], Ppe = { class: "worldbook-entry-advanced-grid" }, Rpe = ["value"], Npe = ["value"], $pe = ["value"], Lpe = ["value"], Dpe = ["value"], Ope = ["value"], Fpe = ["value"], Bpe = ["value"], Upe = ["value"], qpe = ["value"], Hpe = { class: "worldbook-entry-advanced-checks" }, zpe = ["checked"], Kpe = ["checked"], Wpe = ["checked"], Gpe = ["checked"], Vpe = ["checked"], Jpe = ["checked"], Ype = ["value", "disabled"], Xpe = { class: "worldbook-entry-editor-lines worldbook-entry-triggers-field" }, jpe = ["value"], Qpe = { class: "worldbook-entry-match-source-list" }, Zpe = ["checked", "onChange"], eme = { class: "worldbook-entry-actions" }, tme = ["onClick"], nme = { key: 0 }, rme = {
  key: 0,
  class: "worldbook-entry-keys"
}, sme = {
  key: 1,
  class: "worldbook-entry-content"
}, ame = {
  key: 1,
  class: "empty-note"
}, ome = {
  key: 1,
  class: "empty-note"
}, ime = /* @__PURE__ */ bn({
  __name: "TavernWorldbooksSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, cancelWorldbookEntryEdit: n, globalWorldbookOptions: r, globalWorldbookSelected: s, globalWorldbookSaving: a, globalWorldbookStatus: o, hiddenWorldbookPreviewEntryCount: l, isEditingWorldbookEntry: i, saveWorldbookEntryDraft: u, selectedWorldbook: d, selectedWorldbookName: f, showMoreWorldbookPreviewEntries: m, startWorldbookEntryEdit: v, toggleGlobalWorldbook: y, updateWorldbookEntryDraftPatch: S, WORLDBOOK_PREVIEW_BATCH_SIZE: x, worldbookEntryDirty: I, worldbookEntryDraft: E, worldbookEntrySaving: N, worldbookEntryStatus: $, worldbookOptions: p, worldbookPreview: w, worldbookPreviewStatus: _, worldbookStatus: b } = Ei(), C = Mv(), k = /* @__PURE__ */ ue(!1), P = [
      {
        value: "0:",
        label: "↑Char",
        title: "角色定义前"
      },
      {
        value: "1:",
        label: "↓Char",
        title: "角色定义后"
      },
      {
        value: "5:",
        label: "↑EM",
        title: "示例消息前"
      },
      {
        value: "6:",
        label: "↓EM",
        title: "示例消息后"
      },
      {
        value: "2:",
        label: "↑AN",
        title: "作者注释前"
      },
      {
        value: "3:",
        label: "↓AN",
        title: "作者注释后"
      },
      {
        value: "4:0",
        label: "@D ⚙️",
        title: "按深度注入：系统"
      },
      {
        value: "4:1",
        label: "@D 👤",
        title: "按深度注入：用户"
      },
      {
        value: "4:2",
        label: "@D 🤖",
        title: "按深度注入：助手"
      },
      {
        value: "7:",
        label: "Outlet",
        title: "Outlet"
      }
    ], H = [
      {
        value: 0,
        label: "满足任一"
      },
      {
        value: 3,
        label: "全部满足"
      },
      {
        value: 1,
        label: "不全满足"
      },
      {
        value: 2,
        label: "全部不满足"
      }
    ], G = [
      {
        key: "matchPersonaDescription",
        label: "用户设定"
      },
      {
        key: "matchCharacterDescription",
        label: "角色描述"
      },
      {
        key: "matchCharacterPersonality",
        label: "角色人格"
      },
      {
        key: "matchCharacterDepthPrompt",
        label: "角色深度提示"
      },
      {
        key: "matchScenario",
        label: "场景"
      },
      {
        key: "matchCreatorNotes",
        label: "作者备注"
      }
    ], ee = ne(() => {
      const he = s.value.length;
      return he ? he === 1 ? s.value[0] || "1 本" : `${he} 本` : "未启用";
    }), J = ne(() => s.value.length ? s.value.join(" / ") : "没有全局生效的世界书");
    function z(he) {
      const q = he.uid !== void 0 && he.uid !== null && String(he.uid).trim() ? String(he.uid).trim() : he.order !== void 0 && he.order !== null && String(he.order).trim() ? String(he.order).trim() : he.name;
      return `worldbook:${f.value}:${q}`;
    }
    function R(he) {
      return `worldbook-advanced:${f.value}:${he.uid}`;
    }
    function V(he, q) {
      S({ [he]: q });
    }
    function le(he) {
      return Array.isArray(he) ? he.map((q) => String(q || "").trim()).filter(Boolean).join(", ") : "";
    }
    function ve(he = "") {
      return String(he || "").split(",").map((q) => q.trim()).filter(Boolean);
    }
    function ke(he) {
      return he == null ? "" : String(he);
    }
    function be(he) {
      const q = he.target.value;
      if (q === "") return null;
      const ye = Number(q);
      return Number.isFinite(ye) ? ye : null;
    }
    function Ne(he) {
      return he === !0 ? "true" : he === !1 ? "false" : "null";
    }
    function ce(he) {
      const q = he.target.value;
      return q === "true" ? !0 : q === "false" ? !1 : null;
    }
    function we(he) {
      return he.constant ? "constant" : he.vectorized ? "vectorized" : "normal";
    }
    function Oe(he) {
      const q = he.target.value;
      S({
        constant: q === "constant",
        vectorized: q === "vectorized"
      });
    }
    function F(he) {
      return he.selective ? String(Number(he.selectiveLogic) || 0) : "off";
    }
    function ae(he) {
      const q = he.target.value;
      if (q === "off") {
        S({ selective: !1 });
        return;
      }
      const ye = Number(q);
      S({
        selective: !0,
        selectiveLogic: Number.isFinite(ye) ? ye : 0
      });
    }
    function Z(he) {
      return he.enabled === !1 ? "×" : he.constant ? "🔵" : he.vectorized ? "🔗" : "🟢";
    }
    function ie(he) {
      return he.enabled === !1 ? "关闭" : he.constant ? "常驻" : he.vectorized ? "向量化" : "普通";
    }
    function me(he) {
      return `${Number(he.position) || 0}:${Number(he.position) === 4 ? Number(he.role) || 0 : ""}`;
    }
    function Se(he) {
      const [q, ye = ""] = he.target.value.split(":"), oe = Number(q), De = Number(ye), Je = Number.isFinite(oe) ? oe : 0;
      S({
        position: Je,
        role: Number.isFinite(De) ? De : 0,
        depth: Je === 4 ? E.value?.depth ?? 4 : null
      });
    }
    function W(he) {
      const q = Number(he.position ?? 0), ye = P.find((oe) => oe.value === `${q}:${q === 4 ? Number(he.role || 0) : ""}`);
      return q === 4 ? `${ye?.label || "@D"} ${ke(he.depth) || 4}` : ye?.label || "↑Char";
    }
    function L(he) {
      return Number(he.position) === 4;
    }
    function Q(he) {
      const q = be(he);
      S({
        probability: q,
        useProbability: q !== null
      });
    }
    function de(he) {
      const q = he.target.checked;
      S({
        useProbability: q,
        probability: q ? E.value?.probability ?? 100 : null
      });
    }
    function ge(he) {
      return he !== !1 && he !== 0;
    }
    function pe(he) {
      return typeof he == "number" ? String(he) : "";
    }
    function Me(he) {
      const q = he.target.checked;
      S({ delayUntilRecursion: !!q });
    }
    function Ie(he) {
      const q = he.target.value;
      if (!q) {
        S({ delayUntilRecursion: !0 });
        return;
      }
      const ye = Number(q);
      S({ delayUntilRecursion: Number.isFinite(ye) && ye > 0 ? Math.floor(ye) : !1 });
    }
    return vt([
      t,
      f,
      () => w.value?.name
    ], () => C.reset()), (he, q) => er((O(), B("div", vhe, [
      g("div", yhe, [q[29] || (q[29] = g("div", null, [g("h2", null, "世界书")], -1)), g("div", _he, [g("span", bhe, te(T(p).length) + " 本", 1)])]),
      T(b) ? (O(), B("div", whe, [g("span", null, te(T(b)), 1)])) : Te("", !0),
      g("section", She, [
        q[30] || (q[30] = g("div", { class: "worldbook-section-head worldbook-global-enable-head" }, [g("h3", null, "已启用的全局世界书")], -1)),
        g("div", Ahe, [g("button", {
          type: "button",
          class: "worldbook-global-toggle",
          "aria-expanded": k.value,
          disabled: T(a),
          onClick: q[0] || (q[0] = (ye) => k.value = !k.value)
        }, te(ee.value), 9, The), g("div", Ehe, [T(o) ? (O(), B("span", Che, te(T(o)), 1)) : (O(), B("span", Ihe, te(J.value), 1))])]),
        k.value ? (O(), B("div", xhe, [(O(!0), B(He, null, tt(T(r), (ye) => (O(), B("label", {
          key: ye,
          class: "worldbook-global-option"
        }, [g("input", {
          type: "checkbox",
          checked: T(s).includes(ye),
          disabled: T(a),
          onChange: (oe) => T(y)(ye, oe.target.checked)
        }, null, 40, khe), g("span", null, te(ye), 1)]))), 128)), T(r).length ? Te("", !0) : (O(), B("span", Mhe, "没有世界书"))])) : Te("", !0)
      ]),
      g("section", Phe, [
        q[73] || (q[73] = g("div", { class: "worldbook-section-head worldbook-main-head" }, [g("h3", null, "世界书操作")], -1)),
        g("div", Rhe, [g("label", Nhe, [g("select", {
          "aria-label": "选择世界书",
          value: T(f),
          onChange: q[1] || (q[1] = (ye) => f.value = ye.target.value)
        }, [
          q[31] || (q[31] = g("option", { value: "" }, " 未选择 ", -1)),
          T(p).length ? Te("", !0) : (O(), B("option", Lhe, " 没有世界书 ")),
          (O(!0), B(He, null, tt(T(p), (ye) => (O(), B("option", {
            key: ye.name,
            value: ye.name
          }, te(ye.name), 9, Dhe))), 128))
        ], 40, $he)])]),
        g("div", Ohe, [g("div", Fhe, [T(d) ? (O(), B("div", Bhe, [T(_) ? (O(), B("div", Uhe, te(T(_)), 1)) : T(w) && T(w).name === T(d).name ? (O(), B(He, { key: 1 }, [g("div", qhe, [
          g("span", null, [g("strong", null, te(T(w).entryCount), 1), q[32] || (q[32] = g("small", null, "条目", -1))]),
          g("span", null, [g("strong", null, te(T(w).enabledCount), 1), q[33] || (q[33] = g("small", null, "启用", -1))]),
          g("span", null, [g("strong", null, te(T(w).constantCount), 1), q[34] || (q[34] = g("small", null, "常驻", -1))]),
          g("span", null, [g("strong", null, te(T(w).keywordCount), 1), q[35] || (q[35] = g("small", null, "关键词", -1))]),
          g("span", null, [g("strong", null, te(T(w).totalChars), 1), q[36] || (q[36] = g("small", null, "正文字符", -1))])
        ]), T(w).entries.length ? (O(), B("div", Hhe, [(O(!0), B(He, null, tt(T(w).entries, (ye) => (O(), B("details", {
          key: z(ye),
          class: Fe(["worldbook-entry-preview", { disabled: !ye.enabled }]),
          open: T(C).isOpen(z(ye)),
          onToggle: (oe) => T(C).setOpenFromEvent(z(ye), oe)
        }, [g("summary", null, [
          g("span", Khe, [g("strong", null, te(ye.name), 1)]),
          g("span", {
            class: "worldbook-entry-state",
            title: ie(ye),
            "aria-label": ie(ye)
          }, te(Z(ye)), 9, Whe),
          g("span", Ghe, te(W(ye)), 1)
        ]), T(C).isOpen(z(ye)) ? (O(), B("div", Vhe, [T(i)(ye) && T(E) ? (O(), B("form", {
          key: 0,
          class: "worldbook-entry-editor",
          onSubmit: q[27] || (q[27] = Kt((...oe) => T(u) && T(u)(...oe), ["prevent"]))
        }, [
          g("div", Jhe, [T($) ? (O(), B("span", Yhe, te(T($)), 1)) : (O(), B("span", Xhe, "编辑条目")), g("div", jhe, [g("button", {
            type: "button",
            class: "worldbook-row-open",
            disabled: T(N),
            onClick: q[2] || (q[2] = (...oe) => T(n) && T(n)(...oe))
          }, " 取消 ", 8, Qhe), g("button", {
            type: "submit",
            class: "primary-action",
            disabled: !T(I) || T(N)
          }, " 保存 ", 8, Zhe)])]),
          g("div", epe, [
            g("div", tpe, [g("label", npe, [q[37] || (q[37] = g("span", null, "条目名", -1)), g("input", {
              value: T(E).comment,
              type: "text",
              onInput: q[3] || (q[3] = (oe) => T(S)({ comment: oe.target.value }))
            }, null, 40, rpe)]), g("label", spe, [g("input", {
              type: "checkbox",
              checked: !T(E).disable,
              onChange: q[4] || (q[4] = (oe) => T(S)({
                disable: !oe.target.checked,
                enabled: oe.target.checked
              }))
            }, null, 40, ape), q[38] || (q[38] = g("span", null, "启用", -1))])]),
            g("label", ope, [q[40] || (q[40] = g("span", null, "状态", -1)), g("select", {
              value: we(T(E)),
              onChange: Oe
            }, [...q[39] || (q[39] = [
              g("option", { value: "constant" }, " 🔵 常驻 ", -1),
              g("option", { value: "normal" }, " 🟢 普通 ", -1),
              g("option", { value: "vectorized" }, " 🔗 向量化 ", -1)
            ])], 40, ipe)]),
            g("label", lpe, [q[41] || (q[41] = g("span", null, "注入位置", -1)), g("select", {
              value: me(T(E)),
              onChange: Se
            }, [(O(), B(He, null, tt(P, (oe) => g("option", {
              key: oe.value,
              value: oe.value,
              title: oe.title
            }, te(oe.label), 9, cpe)), 64))], 40, upe)]),
            g("label", dpe, [q[42] || (q[42] = g("span", null, "深度", -1)), g("input", {
              value: ke(T(E).depth),
              type: "number",
              min: "0",
              max: "9999",
              step: "1",
              disabled: !L(T(E)),
              onInput: q[5] || (q[5] = (oe) => V("depth", be(oe)))
            }, null, 40, fpe)]),
            g("label", hpe, [q[43] || (q[43] = g("span", null, "排序", -1)), g("input", {
              value: T(E).order,
              type: "number",
              min: "0",
              max: "9999",
              step: "1",
              onInput: q[6] || (q[6] = (oe) => V("order", Number(oe.target.value)))
            }, null, 40, ppe)]),
            g("label", mpe, [q[44] || (q[44] = g("span", null, "触发概率", -1)), g("input", {
              value: ke(T(E).probability),
              type: "number",
              min: "0",
              max: "100",
              step: "1",
              disabled: !T(E).useProbability,
              onInput: Q
            }, null, 40, gpe)]),
            g("label", vpe, [q[45] || (q[45] = g("span", null, "扫描深度", -1)), g("input", {
              value: ke(T(E).scanDepth),
              type: "number",
              min: "0",
              max: "1000",
              onInput: q[7] || (q[7] = (oe) => V("scanDepth", be(oe)))
            }, null, 40, ype)])
          ]),
          g("div", _pe, [
            g("label", bpe, [q[46] || (q[46] = g("span", null, "关键词", -1)), g("input", {
              value: le(T(E).key),
              type: "text",
              onInput: q[8] || (q[8] = (oe) => T(S)({ key: ve(oe.target.value) }))
            }, null, 40, wpe)]),
            g("div", Spe, [g("label", Ape, [q[48] || (q[48] = g("span", null, "过滤逻辑", -1)), g("select", {
              value: F(T(E)),
              onChange: ae
            }, [q[47] || (q[47] = g("option", { value: "off" }, " 关闭 ", -1)), (O(), B(He, null, tt(H, (oe) => g("option", {
              key: oe.value,
              value: oe.value
            }, te(oe.label), 9, Epe)), 64))], 40, Tpe)])]),
            g("label", Cpe, [q[49] || (q[49] = g("span", null, "可选过滤", -1)), g("input", {
              value: le(T(E).keysecondary.length ? T(E).keysecondary : T(E).secondary_keys),
              type: "text",
              onInput: q[9] || (q[9] = (oe) => T(S)({ keysecondary: ve(oe.target.value) }))
            }, null, 40, Ipe)])
          ]),
          g("label", xpe, [q[50] || (q[50] = g("span", null, "内容", -1)), g("textarea", {
            value: T(E).content,
            rows: "9",
            onInput: q[10] || (q[10] = (oe) => T(S)({ content: oe.target.value }))
          }, null, 40, kpe)]),
          g("div", { class: Fe(["worldbook-entry-advanced", { open: T(C).isOpen(R(T(E))) }]) }, [g("button", {
            type: "button",
            class: "worldbook-entry-advanced-toggle",
            "aria-expanded": T(C).isOpen(R(T(E))),
            onClick: q[11] || (q[11] = (oe) => T(C).setOpen(R(T(E)), !T(C).isOpen(R(T(E)))))
          }, " 高级设置 ", 8, Mpe), T(C).isOpen(R(T(E))) ? (O(), B(He, { key: 0 }, [
            g("div", Ppe, [
              g("label", null, [q[51] || (q[51] = g("span", null, "注入出口", -1)), g("input", {
                value: T(E).outletName,
                type: "text",
                onInput: q[12] || (q[12] = (oe) => V("outletName", oe.target.value))
              }, null, 40, Rpe)]),
              g("label", null, [q[53] || (q[53] = g("span", null, "区分大小写", -1)), g("select", {
                value: Ne(T(E).caseSensitive),
                onChange: q[13] || (q[13] = (oe) => V("caseSensitive", ce(oe)))
              }, [...q[52] || (q[52] = [
                g("option", { value: "null" }, "跟随全局", -1),
                g("option", { value: "true" }, "是", -1),
                g("option", { value: "false" }, "否", -1)
              ])], 40, Npe)]),
              g("label", null, [q[55] || (q[55] = g("span", null, "全词匹配", -1)), g("select", {
                value: Ne(T(E).matchWholeWords),
                onChange: q[14] || (q[14] = (oe) => V("matchWholeWords", ce(oe)))
              }, [...q[54] || (q[54] = [
                g("option", { value: "null" }, "跟随全局", -1),
                g("option", { value: "true" }, "是", -1),
                g("option", { value: "false" }, "否", -1)
              ])], 40, $pe)]),
              g("label", null, [q[57] || (q[57] = g("span", null, "分组评分", -1)), g("select", {
                value: Ne(T(E).useGroupScoring),
                onChange: q[15] || (q[15] = (oe) => V("useGroupScoring", ce(oe)))
              }, [...q[56] || (q[56] = [
                g("option", { value: "null" }, "跟随全局", -1),
                g("option", { value: "true" }, "是", -1),
                g("option", { value: "false" }, "否", -1)
              ])], 40, Lpe)]),
              g("label", null, [q[58] || (q[58] = g("span", null, "自动化 ID", -1)), g("input", {
                value: T(E).automationId,
                type: "text",
                onInput: q[16] || (q[16] = (oe) => V("automationId", oe.target.value))
              }, null, 40, Dpe)]),
              g("label", null, [q[59] || (q[59] = g("span", null, "包含组", -1)), g("input", {
                value: T(E).group,
                type: "text",
                onInput: q[17] || (q[17] = (oe) => V("group", oe.target.value))
              }, null, 40, Ope)]),
              g("label", null, [q[60] || (q[60] = g("span", null, "分组权重", -1)), g("input", {
                value: ke(T(E).groupWeight),
                type: "number",
                min: "1",
                onInput: q[18] || (q[18] = (oe) => V("groupWeight", be(oe)))
              }, null, 40, Fpe)]),
              g("label", null, [q[61] || (q[61] = g("span", null, "黏着轮数", -1)), g("input", {
                value: ke(T(E).sticky),
                type: "number",
                min: "0",
                onInput: q[19] || (q[19] = (oe) => V("sticky", be(oe)))
              }, null, 40, Bpe)]),
              g("label", null, [q[62] || (q[62] = g("span", null, "冷却轮数", -1)), g("input", {
                value: ke(T(E).cooldown),
                type: "number",
                min: "0",
                onInput: q[20] || (q[20] = (oe) => V("cooldown", be(oe)))
              }, null, 40, Upe)]),
              g("label", null, [q[63] || (q[63] = g("span", null, "延迟轮数", -1)), g("input", {
                value: ke(T(E).delay),
                type: "number",
                min: "0",
                onInput: q[21] || (q[21] = (oe) => V("delay", be(oe)))
              }, null, 40, qpe)])
            ]),
            g("div", Hpe, [
              g("label", null, [g("input", {
                type: "checkbox",
                checked: T(E).useProbability,
                onChange: de
              }, null, 40, zpe), q[64] || (q[64] = g("span", null, "启用概率", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: T(E).ignoreBudget,
                onChange: q[22] || (q[22] = (oe) => V("ignoreBudget", oe.target.checked))
              }, null, 40, Kpe), q[65] || (q[65] = g("span", null, "忽略预算", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: T(E).excludeRecursion,
                onChange: q[23] || (q[23] = (oe) => V("excludeRecursion", oe.target.checked))
              }, null, 40, Wpe), q[66] || (q[66] = g("span", null, "排除递归", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: T(E).preventRecursion,
                onChange: q[24] || (q[24] = (oe) => V("preventRecursion", oe.target.checked))
              }, null, 40, Gpe), q[67] || (q[67] = g("span", null, "阻止递归", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: T(E).groupOverride,
                onChange: q[25] || (q[25] = (oe) => V("groupOverride", oe.target.checked))
              }, null, 40, Vpe), q[68] || (q[68] = g("span", null, "分组优先", -1))]),
              g("label", null, [g("input", {
                type: "checkbox",
                checked: ge(T(E).delayUntilRecursion),
                onChange: Me
              }, null, 40, Jpe), q[69] || (q[69] = g("span", null, "递归后延迟", -1))]),
              g("label", null, [q[70] || (q[70] = g("span", null, "递归层级", -1)), g("input", {
                value: pe(T(E).delayUntilRecursion),
                type: "number",
                min: "1",
                disabled: !ge(T(E).delayUntilRecursion),
                onInput: Ie
              }, null, 40, Ype)])
            ]),
            g("label", Xpe, [q[71] || (q[71] = g("span", null, "生成触发", -1)), g("input", {
              value: le(T(E).triggers),
              type: "text",
              onInput: q[26] || (q[26] = (oe) => V("triggers", ve(oe.target.value)))
            }, null, 40, jpe)]),
            g("div", Qpe, [q[72] || (q[72] = g("span", null, "额外匹配源", -1)), (O(), B(He, null, tt(G, (oe) => g("label", { key: oe.key }, [g("input", {
              type: "checkbox",
              checked: !!T(E)[oe.key],
              onChange: (De) => V(oe.key, De.target.checked)
            }, null, 40, Zpe), g("span", null, te(oe.label), 1)])), 64))])
          ], 64)) : Te("", !0)], 2)
        ], 32)) : (O(), B(He, { key: 1 }, [
          g("div", eme, [g("button", {
            type: "button",
            class: "worldbook-row-open",
            onClick: (oe) => T(v)(ye)
          }, " 编辑 ", 8, tme), T($) && T(i)(ye) ? (O(), B("span", nme, te(T($)), 1)) : Te("", !0)]),
          ye.keys.length || ye.secondaryKeys.length ? (O(), B("p", rme, te([...ye.keys, ...ye.secondaryKeys].join(" / ")), 1)) : Te("", !0),
          ye.contentPreview ? (O(), B("p", sme, te(ye.contentPreview), 1)) : Te("", !0)
        ], 64))])) : Te("", !0)], 42, zhe))), 128)), T(l) ? (O(), B("button", {
          key: 0,
          type: "button",
          class: "native-add-row worldbook-preview-more",
          onClick: q[28] || (q[28] = (...ye) => T(m) && T(m)(...ye))
        }, " 再显示 " + te(Math.min(T(l), T(x))) + " 条 ", 1)) : Te("", !0)])) : (O(), B("div", ame, " 这本世界书暂无条目。 "))], 64)) : Te("", !0)])) : (O(), B("div", ome, " 选择一本世界书后查看预览。 "))])])
      ])
    ], 512)), [[rc, T(t) === "worldbooks"]]);
  }
}), y8 = ime, lme = ["title", "aria-label"], ume = {
  key: 0,
  class: "xb-theme-icon",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.75",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, cme = {
  key: 1,
  class: "xb-theme-glyph",
  "aria-hidden": "true"
}, dme = ["aria-expanded"], fme = {
  key: 0,
  class: "chat-app-menu-popover",
  role: "menu",
  "aria-label": "酒馆操作"
}, hme = ["onClick"], pme = { class: "chat-app-menu-label-full" }, mme = { class: "chat-app-menu-label-mobile" }, gme = {
  key: 0,
  class: "chat-mobile-topbar"
}, vme = { class: "chat-mobile-primary-row" }, yme = {
  class: "chat-mobile-segment",
  role: "tablist",
  "aria-label": "聊天视图"
}, _me = ["aria-selected"], bme = ["aria-selected"], wme = { class: "chat-mobile-action-group" }, Sme = ["title", "aria-label"], Ame = {
  key: 0,
  class: "chat-mobile-svg",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.8",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, Tme = {
  key: 1,
  class: "chat-mobile-svg",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.8",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, Eme = ["aria-expanded"], Cme = {
  key: 0,
  class: "chat-app-menu-popover",
  role: "menu",
  "aria-label": "酒馆操作"
}, Ime = ["onClick"], xme = { class: "chat-app-menu-label-full" }, kme = { class: "chat-app-menu-label-mobile" }, Mme = { class: "chat-mobile-context-row" }, Pme = { class: "chat-flip-card" }, Rme = { class: "compose-author-note-body" }, Nme = { class: "compose-author-note-field" }, $me = { class: "compose-author-note-field" }, Lme = { class: "compose-author-note-segments" }, Dme = ["onClick"], Ome = { class: "compose-author-note-grid" }, Fme = { class: "compose-author-note-field" }, Bme = { class: "compose-author-note-field" }, Ume = { class: "compose-author-note-field" }, qme = { class: "compose-author-note-segments" }, Hme = ["onClick"], zme = { class: "compose-author-note-check" }, Kme = { class: "compose-author-note-actions" }, Wme = ["disabled"], Gme = { class: "chat-quick-settings-head" }, Vme = { id: "chat-quick-settings-title" }, Jme = { class: "xb-main" }, Yme = /* @__PURE__ */ bn({
  __name: "TavernChatPage",
  setup(e) {
    const t = vc(), n = r8(), r = GS(), s = a8(), a = kv(), o = Ei(), l = o8(), { activeView: i, chatFocus: u, homeThemeDark: d } = t, { chatAutoScroll: f, chatLayout: m, chatScrollRef: v, currentAuthorNote: y, saveCurrentAuthorNote: S, messageKey: x, updateChatScrollButtons: I } = r, { chatMessageWindow: E, selectedSessionId: N, visibleChatMessages: $ } = a, { managerAutoScroll: p, managerMessageWindow: w, managerScrollRef: _, updateManagerScrollButtons: b, visibleManagerChatItems: C } = s, { chatWorkspacePanel: k, saveSessionContract: P, sessionContract: H } = l, { activeSettingsWorkspace: G, apiSettingsRootRef: ee, loadTavernUsers: J, refreshPresets: z, refreshRegexFromHost: R, syncChatPresetFromHost: V, syncGlobalWorldbooksFromHost: le, syncWorldbooksForCurrentCharacter: ve } = o, { refresh: ke } = n;
    let be = null, Ne = null, ce = !0, we = !0;
    const Oe = /* @__PURE__ */ ue(!1), F = /* @__PURE__ */ ue(null), ae = /* @__PURE__ */ ue(!1), Z = /* @__PURE__ */ ue(null), ie = /* @__PURE__ */ ue(null), me = /* @__PURE__ */ ue(!1), Se = /* @__PURE__ */ ue(""), W = /* @__PURE__ */ ue(Fs(H.value)), L = /* @__PURE__ */ ue(!1), Q = /* @__PURE__ */ ue(ti(ub)), de = /* @__PURE__ */ ue(!1), ge = /* @__PURE__ */ ue(""), pe = /* @__PURE__ */ ue("none"), Me = /* @__PURE__ */ ue(!1), Ie = Pv("(max-width: 760px)"), he = ne(() => JSON.stringify(W.value) !== JSON.stringify(H.value)), q = ne(() => !Ie.value || pe.value === "workspace"), ye = ne(() => i.value === "chat" && u.value === "chat"), oe = ne(() => i.value === "chat" && u.value === "manager"), De = ne(() => [
      E.value.startIndex,
      E.value.visibleCount,
      ...$.value.map((ct) => `${x(ct)}:${ct.error ? 1 : 0}:${String(ct.content || "").length}`)
    ].join("|")), Je = ne(() => [
      w.value.startIndex,
      w.value.visibleCount,
      ...C.value.map((ct) => `${ct.kind}:${ct.key}`)
    ].join("|")), st = [
      {
        value: Dp.AFTER_MAIN,
        label: "主提示词后"
      },
      {
        value: Dp.BEFORE_MAIN,
        label: "主提示词前"
      },
      {
        value: Dp.IN_CHAT,
        label: "聊天内 @ Depth"
      }
    ], lt = [
      {
        value: Lp.SYSTEM,
        label: "System"
      },
      {
        value: Lp.USER,
        label: "User"
      },
      {
        value: Lp.ASSISTANT,
        label: "Assistant"
      }
    ], ot = [
      {
        key: "characters",
        label: "角色卡",
        mobileLabel: "角色卡"
      },
      {
        key: "api",
        label: "API 配置",
        mobileLabel: "API 配置"
      },
      {
        key: "chatPreset",
        label: "聊天预设",
        mobileLabel: "聊天预设"
      },
      {
        key: "assistantPreset",
        label: "助手预设",
        mobileLabel: "助手预设"
      },
      {
        key: "worldbooks",
        label: "世界书",
        mobileLabel: "世界书"
      },
      {
        key: "regex",
        label: "正则",
        mobileLabel: "正则"
      },
      {
        key: "base",
        label: "基础设置",
        mobileLabel: "基础设置"
      }
    ];
    function pt() {
      se(), pe.value = "none", Me.value = !1;
    }
    function ht(ct) {
      se();
      const Ke = pe.value === "workspace" && k.value === ct;
      k.value = ct, pe.value = Ke ? "none" : "workspace", Me.value = !1;
    }
    function It() {
      se(), Me.value = !Me.value;
    }
    function Lt() {
      se(), Se.value = "", W.value = Fs(H.value), Oe.value = !0;
    }
    function en() {
      Se.value = "", Oe.value = !1;
    }
    const at = ne(() => F.value === "characters" ? "角色卡" : F.value === "worldbooks" ? "世界书" : F.value === "chatPreset" ? "聊天预设" : F.value === "assistantPreset" ? "助手预设" : F.value === "regex" ? "正则" : F.value === "base" ? "基础设置" : "API 配置"), X = ne(() => F.value ? `is-${F.value}-workspace` : "");
    function se() {
      ae.value = !1;
    }
    function Ae() {
      ae.value = !ae.value;
    }
    function Re(ct) {
      se(), pt(), G.value = ct, F.value = ct, ct === "characters" && ke(), ct === "chatPreset" && V(), ct === "assistantPreset" && z(), ct === "worldbooks" && (ve(), le()), ct === "regex" && R(), ct === "base" && J();
    }
    function Be() {
      F.value = null;
    }
    function je() {
      pt(), F.value = null, Q.value = ti(y.value), ge.value = "", L.value = !0;
    }
    function Qe() {
      ge.value = "", L.value = !1;
    }
    function ut(ct) {
      Q.value = ti({
        ...Q.value,
        ...ct
      });
    }
    async function wt() {
      if (!de.value) {
        de.value = !0, ge.value = "";
        try {
          const ct = ti(Q.value);
          await S(ct), Q.value = ct, ge.value = "已保存";
        } catch (ct) {
          ge.value = ct instanceof Error ? ct.message : String(ct || "保存失败");
        } finally {
          de.value = !1;
        }
      }
    }
    function rn(ct) {
      ee.value = ct instanceof HTMLElement ? ct : null;
    }
    function Ht(ct) {
      if (!ae.value) return;
      const Ke = ct.target instanceof Node ? ct.target : null;
      Ke && (Z.value?.contains(Ke) || ie.value?.contains(Ke)) || se();
    }
    function Dn(ct) {
      ct.key !== "Escape" || !ae.value || se();
    }
    function br(ct) {
      W.value = {
        ...W.value,
        [ct]: !W.value[ct]
      };
    }
    async function fr() {
      if (!N.value) {
        Se.value = "", Oe.value = !1;
        return;
      }
      Se.value = "", me.value = !0;
      try {
        if (!await P(W.value)) throw new Error("contract_save_failed");
        Oe.value = !1;
      } catch {
        Se.value = "契约保存失败，请重试。";
      } finally {
        me.value = !1;
      }
    }
    return vt(() => H.value, (ct) => {
      Oe.value || (W.value = Fs(ct));
    }, { deep: !0 }), vt(() => N.value, () => {
      Se.value = "", Oe.value = !1, Me.value = !1, W.value = Fs(H.value), F.value === "characters" && (F.value = null);
    }), vt(() => i.value, (ct) => {
      se(), ct !== "chat" && (F.value = null, L.value = !1);
    }), vt(() => N.value, () => {
      ge.value = "", L.value = !1, Q.value = ti(y.value);
    }), vt(De, () => {
      ce = !0;
    }, { flush: "sync" }), vt(Je, () => {
      we = !0;
    }, { flush: "sync" }), w1(() => {
      document.addEventListener("pointerdown", Ht), document.addEventListener("keydown", Dn);
    }), Fg(() => {
      document.removeEventListener("pointerdown", Ht), document.removeEventListener("keydown", Dn);
    }), gk(() => {
      if (be = null, Ne = null, ye.value && ce) {
        be = ib(v.value, {
          itemSelector: ".chat-bubble[data-chat-anchor-key], .chat-history-gate[data-chat-anchor-key]",
          datasetKey: "chatAnchorKey"
        });
        return;
      }
      oe.value && we && (Ne = ib(_.value, {
        itemSelector: "[data-manager-anchor-key]",
        datasetKey: "managerAnchorKey"
      }));
    }), vk(() => {
      const ct = ye.value && f.value !== !1, Ke = oe.value && p.value !== !1;
      be && (lb(v.value, be, {
        itemSelector: ".chat-bubble[data-chat-anchor-key], .chat-history-gate[data-chat-anchor-key]",
        datasetKey: "chatAnchorKey"
      }, {
        forceBottom: ct,
        defaultToBottom: ct,
        preserveScrollTop: !ct
      }), ce = !1, I()), Ne && (lb(_.value, Ne, {
        itemSelector: "[data-manager-anchor-key]",
        datasetKey: "managerAnchorKey"
      }, {
        forceBottom: Ke,
        defaultToBottom: Ke,
        preserveScrollTop: !Ke
      }), we = !1, b()), be = null, Ne = null;
    }), (ct, Ke) => T(i) === "chat" ? (O(), B("section", {
      key: 0,
      class: Fe(["tavern-chat xb-page", [
        `chat-focus-${T(u)}`,
        `chat-layout-${T(m)}`,
        { "is-mobile-workspace-open": pe.value === "workspace" }
      ]])
    }, [
      g("div", {
        ref_key: "desktopChatAppMenuRef",
        ref: Z,
        class: "home-corner-actions page-corner-actions chat-app-menu-shell"
      }, [
        g("button", {
          type: "button",
          class: "home-icon-button home-theme-button",
          title: T(d) ? "切换到白天" : "切换到夜间",
          "aria-label": T(d) ? "切换到白天" : "切换到夜间",
          onClick: Ke[0] || (Ke[0] = (it) => d.value = !T(d))
        }, [T(d) ? (O(), B("svg", ume, [...Ke[15] || (Ke[15] = [Al('<circle cx="12" cy="12" r="4"></circle><path d="M12 2v2"></path><path d="M12 20v2"></path><path d="m4.93 4.93 1.41 1.41"></path><path d="m17.66 17.66 1.41 1.41"></path><path d="M2 12h2"></path><path d="M20 12h2"></path><path d="m6.34 17.66-1.41 1.41"></path><path d="m19.07 4.93-1.41 1.41"></path>', 9)])])) : (O(), B("span", cme, "☾"))], 8, lme),
        g("button", {
          type: "button",
          class: "home-icon-button page-home-button",
          title: "首页",
          "aria-label": "首页",
          onClick: Ke[1] || (Ke[1] = (it) => i.value = "home")
        }, [...Ke[16] || (Ke[16] = [g("svg", {
          class: "xb-home-icon",
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          "stroke-width": "1.8",
          "stroke-linecap": "round",
          "stroke-linejoin": "round",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M3 11.5 12 4l9 7.5" }),
          g("path", { d: "M5.5 10.5V20h13v-9.5" }),
          g("path", { d: "M9.5 20v-5.5h5V20" })
        ], -1)])]),
        g("button", {
          type: "button",
          class: "home-icon-button chat-app-menu-button",
          title: "酒馆操作菜单",
          "aria-label": "酒馆操作菜单",
          "aria-expanded": ae.value,
          onClick: Ae
        }, [...Ke[17] || (Ke[17] = [g("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M5 7h14" }),
          g("path", { d: "M5 12h14" }),
          g("path", { d: "M5 17h14" })
        ], -1)])], 8, dme),
        ae.value ? (O(), B("div", fme, [(O(), B(He, null, tt(ot, (it) => g("button", {
          key: it.key,
          type: "button",
          class: "chat-app-menu-item",
          role: "menuitem",
          onClick: (En) => Re(it.key)
        }, [g("span", pme, te(it.label), 1), g("span", mme, te(it.mobileLabel), 1)], 8, hme)), 64))])) : Te("", !0)
      ], 512),
      T(Ie) ? (O(), B("header", gme, [g("div", vme, [g("div", yme, [
        Ke[18] || (Ke[18] = g("span", { "aria-hidden": "true" }, null, -1)),
        g("button", {
          type: "button",
          role: "tab",
          "aria-selected": T(u) === "chat",
          class: Fe({ active: T(u) === "chat" }),
          onClick: Ke[2] || (Ke[2] = (it) => {
            u.value = "chat", pt();
          })
        }, " 角色 ", 10, _me),
        g("button", {
          type: "button",
          role: "tab",
          "aria-selected": T(u) === "manager",
          class: Fe({ active: T(u) === "manager" }),
          onClick: Ke[3] || (Ke[3] = (it) => {
            u.value = "manager", pt();
          })
        }, " 助手 ", 10, bme)
      ]), g("div", wme, [
        T(u) === "chat" ? (O(), cn(f8, {
          key: 0,
          mobile: ""
        })) : Te("", !0),
        g("button", {
          type: "button",
          class: "chat-mobile-icon-button chat-mobile-utility-button",
          title: T(d) ? "切换到白天" : "切换到夜间",
          "aria-label": T(d) ? "切换到白天" : "切换到夜间",
          onClick: Ke[4] || (Ke[4] = (it) => d.value = !T(d))
        }, [T(d) ? (O(), B("svg", Ame, [...Ke[19] || (Ke[19] = [Al('<circle cx="12" cy="12" r="4"></circle><path d="M12 2v2"></path><path d="M12 20v2"></path><path d="m4.93 4.93 1.41 1.41"></path><path d="m17.66 17.66 1.41 1.41"></path><path d="M2 12h2"></path><path d="M20 12h2"></path><path d="m6.34 17.66-1.41 1.41"></path><path d="m19.07 4.93-1.41 1.41"></path>', 9)])])) : (O(), B("svg", Tme, [...Ke[20] || (Ke[20] = [g("path", { d: "M20.2 14.5A7.3 7.3 0 0 1 9.5 3.8 8.7 8.7 0 1 0 20.2 14.5Z" }, null, -1)])]))], 8, Sme),
        g("button", {
          type: "button",
          class: "chat-mobile-icon-button chat-mobile-utility-button",
          title: "首页",
          "aria-label": "首页",
          onClick: Ke[5] || (Ke[5] = (it) => {
            i.value = "home", pt();
          })
        }, [...Ke[21] || (Ke[21] = [g("svg", {
          class: "chat-mobile-svg",
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "currentColor",
          "stroke-width": "1.8",
          "stroke-linecap": "round",
          "stroke-linejoin": "round",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M3 11.5 12 4l9 7.5" }),
          g("path", { d: "M5.5 10.5V20h13v-9.5" }),
          g("path", { d: "M9.5 20v-5.5h5V20" })
        ], -1)])]),
        g("div", {
          ref_key: "mobileChatAppMenuRef",
          ref: ie,
          class: "chat-app-menu-shell chat-app-menu-shell-mobile"
        }, [g("button", {
          type: "button",
          class: "chat-mobile-icon-button chat-mobile-utility-button chat-app-menu-button",
          title: "酒馆操作菜单",
          "aria-label": "酒馆操作菜单",
          "aria-expanded": ae.value,
          onClick: Ae
        }, [...Ke[22] || (Ke[22] = [g("svg", {
          class: "chat-mobile-svg",
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          g("path", { d: "M5 7h14" }),
          g("path", { d: "M5 12h14" }),
          g("path", { d: "M5 17h14" })
        ], -1)])], 8, Eme), ae.value ? (O(), B("div", Cme, [(O(), B(He, null, tt(ot, (it) => g("button", {
          key: it.key,
          type: "button",
          class: "chat-app-menu-item",
          role: "menuitem",
          onClick: (En) => Re(it.key)
        }, [g("span", xme, te(it.label), 1), g("span", kme, te(it.mobileLabel), 1)], 8, Ime)), 64))])) : Te("", !0)], 512)
      ])]), g("div", Mme, [
        g("button", {
          type: "button",
          class: Fe(["chat-mobile-context-button", { "is-active": pe.value === "workspace" && T(k) === "state" }]),
          title: "地图",
          "aria-label": "地图",
          onClick: Ke[6] || (Ke[6] = (it) => ht("state"))
        }, " 地图 ", 2),
        g("button", {
          type: "button",
          class: Fe(["chat-mobile-context-button", { "is-active": pe.value === "workspace" && T(k) === "memory" }]),
          title: "记忆",
          "aria-label": "记忆",
          onClick: Ke[7] || (Ke[7] = (it) => ht("memory"))
        }, " 记忆 ", 2),
        g("button", {
          type: "button",
          class: Fe(["chat-mobile-context-button", { "is-active": pe.value === "workspace" && T(k) === "event" }]),
          title: "事件",
          "aria-label": "事件",
          onClick: Ke[8] || (Ke[8] = (it) => ht("event"))
        }, " 事件 ", 2),
        g("button", {
          type: "button",
          class: "chat-mobile-context-button",
          title: "契约",
          "aria-label": "契约",
          onClick: Ke[9] || (Ke[9] = (it) => {
            pt(), Lt();
          })
        }, " 契约 ")
      ])])) : Te("", !0),
      T(Ie) && pe.value !== "none" ? (O(), B("button", {
        key: 1,
        type: "button",
        class: "chat-mobile-sheet-scrim",
        title: "收起面板",
        "aria-label": "收起面板",
        onClick: pt
      })) : Te("", !0),
      g("section", { class: Fe(["chat-workbench", { "is-manager": T(u) === "manager" }]) }, [g("div", Pme, [Wn(Xie, {
        onOpenContract: Lt,
        onOpenAuthorNote: je
      }), Wn(rue, { onOpenContract: Lt })])], 2),
      q.value ? (O(), cn(ghe, {
        key: 2,
        "memory-directory-open": Me.value,
        onClose: pt,
        onCloseMemoryDirectory: Ke[10] || (Ke[10] = (it) => Me.value = !1),
        onToggleMemoryDirectory: It
      }, null, 8, ["memory-directory-open"])) : Te("", !0),
      Oe.value ? (O(), cn(Zne, {
        key: 3,
        "draft-contract": W.value,
        dirty: he.value,
        saving: me.value,
        "can-save": !!T(N),
        "error-text": Se.value,
        onClose: en,
        onToggle: br,
        onSave: fr
      }, null, 8, [
        "draft-contract",
        "dirty",
        "saving",
        "can-save",
        "error-text"
      ])) : Te("", !0),
      L.value ? (O(), B("div", {
        key: 4,
        class: "compose-author-note-overlay",
        role: "dialog",
        "aria-modal": "true",
        "aria-labelledby": "compose-author-note-title",
        onKeydown: Qr(Qe, ["esc"])
      }, [g("form", {
        class: "compose-author-note-dialog compose-author-note-panel",
        onSubmit: Kt(wt, ["prevent"])
      }, [
        g("header", { class: "compose-author-note-head" }, [Ke[23] || (Ke[23] = g("div", null, [g("strong", { id: "compose-author-note-title" }, "玩家便签"), g("span", null, "只对当前会话生效。")], -1)), g("button", {
          type: "button",
          class: "compose-author-note-back",
          "aria-label": "关闭玩家便签",
          onClick: Qe
        })]),
        g("div", Rme, [
          g("label", Nme, [Ke[24] || (Ke[24] = g("span", null, "便签内容", -1)), er(g("textarea", {
            "onUpdate:modelValue": Ke[11] || (Ke[11] = (it) => Q.value.prompt = it),
            rows: "6"
          }, null, 512), [[sa, Q.value.prompt]])]),
          g("div", $me, [Ke[25] || (Ke[25] = g("span", null, "插入位置", -1)), g("div", Lme, [(O(), B(He, null, tt(st, (it) => g("button", {
            key: it.value,
            type: "button",
            class: Fe({ selected: Number(Q.value.position) === it.value }),
            onClick: (En) => ut({ position: it.value })
          }, te(it.label), 11, Dme)), 64))])]),
          g("div", Ome, [g("label", Fme, [Ke[26] || (Ke[26] = g("span", null, "Depth", -1)), er(g("input", {
            "onUpdate:modelValue": Ke[12] || (Ke[12] = (it) => Q.value.depth = it),
            type: "number",
            min: "0",
            max: "9999"
          }, null, 512), [[
            sa,
            Q.value.depth,
            void 0,
            { number: !0 }
          ]])]), g("label", Bme, [Ke[27] || (Ke[27] = g("span", null, "插入频率", -1)), er(g("input", {
            "onUpdate:modelValue": Ke[13] || (Ke[13] = (it) => Q.value.interval = it),
            type: "number",
            min: "0",
            max: "9999"
          }, null, 512), [[
            sa,
            Q.value.interval,
            void 0,
            { number: !0 }
          ]])])]),
          g("div", Ume, [Ke[28] || (Ke[28] = g("span", null, "Role", -1)), g("div", qme, [(O(), B(He, null, tt(lt, (it) => g("button", {
            key: it.value,
            type: "button",
            class: Fe({ selected: Number(Q.value.role) === it.value }),
            onClick: (En) => ut({ role: it.value })
          }, te(it.label), 11, Hme)), 64))])]),
          g("label", zme, [er(g("input", {
            "onUpdate:modelValue": Ke[14] || (Ke[14] = (it) => Q.value.scan = it),
            type: "checkbox"
          }, null, 512), [[AL, Q.value.scan]]), Ke[29] || (Ke[29] = g("span", null, "参与世界书扫描", -1))])
        ]),
        g("footer", Kme, [g("span", null, te(ge.value || "0 禁用，1 每次，N 每 N 次用户输入"), 1), g("button", {
          type: "submit",
          disabled: de.value
        }, te(de.value ? "保存中..." : "保存"), 9, Wme)])
      ], 32)], 32)) : Te("", !0),
      F.value ? (O(), B("div", {
        key: 5,
        class: Fe(["chat-quick-settings-overlay", X.value]),
        role: "presentation"
      }, [g("section", {
        class: Fe(["chat-quick-settings-dialog", X.value]),
        role: "dialog",
        "aria-modal": "true",
        "aria-labelledby": "chat-quick-settings-title"
      }, [g("header", Gme, [g("h2", Vme, te(at.value), 1), g("button", {
        type: "button",
        class: "chat-quick-settings-close",
        title: "关闭",
        "aria-label": "关闭",
        onClick: Be
      }, " × ")]), g("div", { class: Fe(["chat-quick-settings-body", X.value]) }, [F.value === "api" ? (O(), B("div", {
        key: 0,
        ref: rn,
        class: "tavern-api-settings chat-quick-api-root"
      })) : (O(), B("div", {
        key: 1,
        class: Fe(["settings-layout chat-quick-settings-layout", X.value])
      }, [g("section", Jme, [F.value === "characters" ? (O(), cn(i8, { key: 0 })) : F.value === "chatPreset" ? (O(), cn(c8, { key: 1 })) : F.value === "assistantPreset" ? (O(), cn(l8, { key: 2 })) : F.value === "worldbooks" ? (O(), cn(y8, { key: 3 })) : F.value === "regex" ? (O(), cn(h8, { key: 4 })) : F.value === "base" ? (O(), cn(u8, { key: 5 })) : Te("", !0)])], 2))], 2)], 2)], 2)) : Te("", !0)
    ], 2)) : Te("", !0);
  }
}), Xme = Yme;
function jme(e) {
  return (Array.isArray(e) ? e : []).map((t, n) => {
    const r = t && typeof t == "object" ? t : {};
    return {
      label: String(r.label || `思考 ${n + 1}`).trim() || `思考 ${n + 1}`,
      text: String(r.text || "").trim()
    };
  }).filter((t) => t.text);
}
function Qme(e) {
  const t = /* @__PURE__ */ ue(Date.now()), n = ne(() => e.managerRuns.value[0] || null), r = ne(() => e.managerRuns.value.find((b) => d(b.status)) || n.value), s = ne(() => {
    const b = String(r.value?.id || "");
    return e.managerRuns.value.filter((C) => String(C.id || "") !== b).slice(0, e.visibleRunLimit);
  }), a = ne(() => {
    const b = r.value ? 1 : 0;
    return Math.max(0, e.managerRuns.value.length - b - s.value.length);
  }), o = ne(() => e.managerRuns.value.some((b) => ["queued", "running"].includes(b.status)));
  function l(b = 0) {
    const C = Math.max(0, t.value - Number(b || 0));
    return !b || C < 3e3 ? "刚刚" : i(C);
  }
  function i(b = 0) {
    const C = Math.floor(b / 1e3);
    if (C < 60) return `${C} 秒前`;
    const k = Math.floor(C / 60);
    return k < 60 ? `${k} 分钟前` : `${Math.floor(k / 60)} 小时前`;
  }
  function u(b = 0) {
    const C = Math.max(0, t.value - Number(b || 0));
    return !b || C < 3e3 ? "刚开始" : i(C).replace(/前$/, "");
  }
  function d(b = "") {
    return ["queued", "running"].includes(String(b || ""));
  }
  function f(b = "") {
    return {
      pending: "待维护",
      queued: "排队",
      running: "运行中",
      completed: "完成",
      failed: "失败",
      cancelled: "已取消",
      superseded: "已作废",
      rolled_back: "未采用"
    }[b] || b || "未知";
  }
  function m(b = "") {
    const C = typeof b == "string" ? b : String(b?.status || "");
    if (typeof b != "string" && C === "running") {
      const k = Number(b.updatedAt) || Number(b.createdAt) || 0, P = Math.max(0, t.value - k);
      if (P > 3e4) return "danger";
      if (P > 9e3) return "warn";
    }
    return ["failed", "rolled_back"].includes(C) ? "danger" : [
      "pending",
      "cancelled",
      "superseded"
    ].includes(C) ? "muted" : ["queued", "running"].includes(C) ? "active" : C === "completed" ? "done" : "neutral";
  }
  function v(b) {
    return b.status === "pending" ? "等待用户继续" : b.status === "queued" ? "等待后台模型" : b.status === "running" ? "后台模型运行中" : [String(b.provider || "").trim(), String(b.model || "").trim()].filter(Boolean).join(" / ") || "未记录模型信息";
  }
  function y(b) {
    const C = String(b.status || ""), k = Number(b.updatedAt) || Number(b.createdAt) || 0;
    if (C === "pending") return `待用户继续后维护 · 建立于 ${l(b.createdAt)}`;
    if (C === "queued") return `等待开始 · 建立于 ${l(b.createdAt)}`;
    if (C === "running") {
      const P = Math.max(0, t.value - k), H = u(b.createdAt);
      return P <= 9e3 ? `还活着 · 已运行 ${H} · 正在等 API/工具返回` : P <= 3e4 ? `等待中 · 已运行 ${H} · 上次心跳 ${l(k)}` : `可能卡住 · 已运行 ${H} · ${l(k)}没有心跳`;
    }
    return [
      "completed",
      "failed",
      "cancelled",
      "superseded",
      "rolled_back"
    ].includes(C) ? `已结束 · ${l(k)}` : k ? `最后更新 ${l(k)}` : "";
  }
  function S(b) {
    const C = String(b.error || "").trim(), k = {
      manager_memory_tool_required: "本轮没有完成必要的记忆维护，系统没有采用这次结果。",
      manager_aborted: "本次后台工作已停止，系统没有采用这次结果。",
      manager_source_messages_changed: "源楼层已失效，系统没有采用这次结果。",
      manager_epoch_expired: "后台工作已过期，系统没有采用这次结果。"
    };
    return /工具轮次达到上限/.test(C) ? `原因：${C} 系统没有采用这次结果。` : C && k[C] ? `原因：${k[C]}` : b.status === "rolled_back" ? "原因：本次结果已撤回，当前记忆、地图和事件保持上一版。" : C ? `原因：${C}` : "";
  }
  function x(b) {
    const C = Number(b.assistantOrder), k = Number(b.userOrder);
    return [
      Number.isInteger(C) && C >= 0 ? `第 ${C + 1} 楼` : `第 ${Math.max(0, Number(b.turn) || 0)} 次维护`,
      Number.isInteger(k) && k >= 0 && Number.isInteger(C) && C >= 0 ? `原文 ${k + 1}-${C + 1} 楼` : "",
      ["accepted_turn", "after_turn"].includes(b.trigger) ? "已接受回合维护" : b.trigger === "manager_chat" ? "助手聊天" : String(b.trigger || "")
    ].filter(Boolean).join(" · ");
  }
  function I(b) {
    const C = Array.isArray(b.changedFiles) ? b.changedFiles : [];
    return b.status === "pending" ? "记忆：等待用户继续后维护" : b.status === "queued" ? "记忆：等待开始" : b.status === "running" ? "记忆：正在整理" : b.status === "failed" ? C.length ? `记忆：已写入 ${C.length} 份档案，但本轮失败` : "记忆：未完成" : ["cancelled", "superseded"].includes(b.status) ? "记忆：已停止，未采用本轮结果" : b.status === "rolled_back" ? "记忆：未采用，已撤回本轮写入" : C.length ? `记忆：已更新 ${C.length} 份档案` : "记忆：没有写入文件";
  }
  function E(b) {
    const C = Array.isArray(b.changedStates) ? b.changedStates : [];
    return b.status === "pending" ? "地图：等待用户继续后维护" : b.status === "queued" ? "地图：等待开始" : b.status === "running" ? "地图：正在判断本轮有没有空间变化" : b.status === "failed" ? C.length ? `地图：已写入 ${C.length} 份状态，但本轮失败` : "地图：未完成" : ["cancelled", "superseded"].includes(b.status) ? "地图：已停止，未采用本轮结果" : b.status === "rolled_back" ? "地图：未采用，已撤回本轮更新" : C.length ? `地图：已更新 ${C.length} 份状态` : "地图：本轮没有明确空间变化，未更新";
  }
  function N(b) {
    const C = Array.isArray(b.changedTasks) ? b.changedTasks : [];
    return b.status === "pending" ? "事件：等待用户继续后维护" : b.status === "queued" ? "事件：等待开始" : b.status === "running" ? "事件：正在判断线索池" : b.status === "failed" ? C.length ? `事件：已写入 ${C.length} 条线索，但本轮失败` : "事件：未完成" : ["cancelled", "superseded"].includes(b.status) ? "事件：已停止，未采用本轮结果" : b.status === "rolled_back" ? "事件：未采用，已撤回本轮更新" : C.length ? `事件：已更新 ${C.length} 条线索` : "事件：本轮没有新线索";
  }
  function $(b) {
    if (!b) return "";
    if (Array.isArray(b)) {
      const C = b.filter((P) => P && typeof P == "object" && P.ok === !1).length, k = b.filter((P) => P && typeof P == "object" && String(P.status || "") === "running").length;
      return k ? `工具调用 ${b.length} 次 · ${k} 个运行中` : C ? `工具调用 ${b.length} 次 · ${C} 次失败` : `工具调用 ${b.length} 次 · 全部成功`;
    }
    if (typeof b == "object") {
      const C = b, k = [
        "calls",
        "toolCalls",
        "steps",
        "trace"
      ].map((H) => Array.isArray(C[H]) ? C[H].length : 0).filter((H) => H > 0);
      if (k.length) return `工具调用 ${Math.max(...k)} 次`;
      const P = Object.keys(C).length;
      return P ? `工具记录 ${P} 项` : "";
    }
    return "有工具记录";
  }
  function p(b) {
    if (!Array.isArray(b)) return [];
    const C = /* @__PURE__ */ new Set(), k = /* @__PURE__ */ new Set();
    return b.map((P, H) => {
      const G = P && typeof P == "object" ? P : {}, ee = String(G.name || "工具").trim() || "工具", J = String(G.status || "").trim(), z = G.ok !== !1, R = Math.max(0, Number(G.elapsedMs) || (Number(G.startedAt) && Number(G.finishedAt) ? Number(G.finishedAt) - Number(G.startedAt) : 0)), V = Math.max(1, Number(G.round) || 1), le = String(G.preface || "").trim(), ve = `${V}
${le}`, ke = le && !C.has(ve) ? le : "";
      le && C.add(ve);
      const be = jme(G.thoughts), Ne = `${V}
${JSON.stringify(be)}`, ce = be.length && !k.has(Ne) ? be : [];
      return be.length && k.add(Ne), {
        id: String(G.id || `${ee}-${H}`),
        round: V,
        name: ee,
        status: J,
        ok: z,
        args: String(G.args || "").trim(),
        path: String(G.path || "").trim(),
        summary: String(G.summary || G.error || "").trim(),
        error: String(G.error || "").trim(),
        preface: ke,
        thoughts: ce,
        elapsedLabel: R ? `${(R / 1e3).toFixed(1)}s` : ""
      };
    });
  }
  function w(b) {
    return b.status === "running" ? "运行中" : b.ok === !1 ? "失败" : "已返回";
  }
  function _(b) {
    return b.status === "running" ? "is-running" : b.ok === !1 ? "is-error" : "is-resolved";
  }
  return {
    archivedManagerRuns: s,
    currentManagerWorkRun: r,
    formatRunActivityLine: y,
    formatRunIssueLine: S,
    formatRunInputLine: x,
    formatRunMapLine: E,
    formatRunMemoryLine: I,
    formatRunModelLine: v,
    formatRunTaskLine: N,
    hiddenManagerRunCount: a,
    isManagerRunLive: d,
    managerBusy: o,
    managerRunTone: m,
    managerStatusClock: t,
    managerStatusLabel: f,
    managerToolStatusLabel: w,
    managerToolTone: _,
    managerToolTraceItems: p,
    toolTraceSummary: $
  };
}
function Zme(e) {
  let t = 0;
  function n(v = "") {
    return v === "stale" ? "过期" : "可用";
  }
  function r(v) {
    return v ? "contentLength" in v && Number.isFinite(Number(v.contentLength)) ? Math.max(0, Number(v.contentLength) || 0) : "content" in v ? Math.max(0, String(v.content || "").length) : 0 : 0;
  }
  function s(v) {
    return `${n(v.status)} · ${r(v)} 字`;
  }
  function a(v) {
    const y = String(v?.content || "");
    e.memoryEditorLoadedPath.value = String(v?.path || ""), e.memoryEditorBaseContent.value = y, e.memoryEditorDraft.value = y, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "";
  }
  function o(v = !0) {
    t += 1, v && (e.selectedMemoryFileRecord.value = null);
  }
  async function l(v = "") {
    const y = String(e.selectedSessionId.value || "").trim(), S = String(v || "").trim(), x = ++t;
    if (!y || !S)
      return o(), null;
    const I = await li(y, S);
    return x !== t ? null : (e.selectedMemoryFileRecord.value = I, I);
  }
  async function i(v = "") {
    const y = String(v || "").trim();
    return !y || y === e.selectedMemoryFilePath.value || e.memoryEditorDirty.value && !await e.confirmDialog({
      title: "切换记忆档案",
      message: "当前记忆档案有未保存修改，切换后会放弃这份草稿。继续切换？",
      confirmText: "继续切换",
      tone: "warning"
    }) ? !1 : (e.selectedMemoryFilePath.value = y, !0);
  }
  async function u() {
    const v = e.selectedMemoryFileEntry.value;
    if (!(!e.selectedSessionId.value || !v || e.memoryEditorReadOnly.value)) {
      e.memoryEditorStatus.value = "保存中";
      try {
        const y = (await Kp(e.selectedSessionId.value, Number.POSITIVE_INFINITY))?.order ?? -1, S = await Qb(e.selectedSessionId.value, v.path, e.memoryEditorDraft.value, { source: "user" });
        await e.commitUserAcceptedState(e.selectedSessionId.value, y), await e.refreshRecords(e.selectedSessionId.value), e.memoryEditorLoadedPath.value = v.path, e.memoryEditorBaseContent.value = S.content, e.memoryEditorDraft.value = S.content, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "";
      } catch (y) {
        e.memoryEditorStatus.value = y instanceof Error ? y.message : String(y || "保存失败");
      }
    }
  }
  function d() {
    !e.memoryEditorDocumentAvailable.value || e.memoryEditorReadOnly.value || (e.memoryEditorMode.value = "edit", Ut(() => {
      document.querySelector('[data-memory-editor-textarea="true"]')?.focus();
    }));
  }
  function f() {
    e.memoryEditorDocumentAvailable.value && (e.memoryEditorMode.value = "preview");
  }
  function m() {
    e.memoryEditorDocumentAvailable.value && (e.memoryEditorDraft.value = e.memoryEditorBaseContent.value, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "");
  }
  return {
    discardMemoryDraft: m,
    enterMemoryEditMode: d,
    formatMemoryFileMeta: s,
    invalidateMemoryFileRecordLoad: o,
    loadMemoryFileIntoEditor: a,
    loadSelectedMemoryFileRecord: l,
    memoryFileStatusLabel: n,
    previewMemoryDraft: f,
    saveSelectedMemoryFile: u,
    selectMemoryFile: i
  };
}
var ege = {
  class: "prompt-inspector-modal",
  tabindex: "-1"
}, tge = { class: "prompt-inspector-head" }, nge = {
  class: "prompt-inspector-tabs",
  "aria-label": "API 请求视图"
}, rge = { class: "prompt-inspector-body" }, sge = {
  key: 0,
  class: "prompt-inspector-view"
}, age = { class: "prompt-inspector-summary" }, oge = {
  key: 0,
  class: "prompt-request-json"
}, ige = {
  key: 1,
  class: "prompt-empty-state"
}, lge = {
  key: 1,
  class: "prompt-inspector-view"
}, uge = { class: "prompt-simulate-panel" }, cge = ["value"], dge = {
  key: 0,
  class: "muted compact"
}, fge = {
  key: 1,
  class: "error"
}, hge = {
  key: 2,
  class: "prompt-request-json"
}, pge = /* @__PURE__ */ bn({
  __name: "TavernRequestLogModal",
  props: {
    tab: {},
    lastRequestRawJson: {},
    lastRequestSnapshot: {},
    simulateInput: {},
    simulateStatus: {},
    simulateError: {},
    simulateJson: {}
  },
  emits: [
    "close",
    "update:tab",
    "update:simulateInput",
    "simulate"
  ],
  setup(e, { emit: t }) {
    const n = t;
    function r(s) {
      n("update:simulateInput", s.target.value);
    }
    return (s, a) => (O(), B("div", {
      class: "prompt-inspector-overlay",
      role: "dialog",
      "aria-modal": "true",
      onClick: a[4] || (a[4] = Kt((o) => s.$emit("close"), ["self"])),
      onKeydown: a[5] || (a[5] = Qr((o) => s.$emit("close"), ["esc"]))
    }, [g("section", ege, [
      g("header", tge, [a[6] || (a[6] = g("div", { class: "prompt-inspector-heading" }, [g("h2", null, "请求日志")], -1)), g("button", {
        type: "button",
        class: "prompt-inspector-close",
        "aria-label": "关闭日志",
        onClick: a[0] || (a[0] = (o) => s.$emit("close"))
      }, " 关闭 ")]),
      g("div", nge, [g("button", {
        type: "button",
        class: Fe({ active: e.tab === "history" }),
        onClick: a[1] || (a[1] = (o) => s.$emit("update:tab", "history"))
      }, " 上次调用 ", 2), g("button", {
        type: "button",
        class: Fe({ active: e.tab === "simulate" }),
        onClick: a[2] || (a[2] = (o) => s.$emit("update:tab", "simulate"))
      }, " 模拟发送 ", 2)]),
      g("div", rge, [e.tab === "history" ? (O(), B("section", sge, [g("div", age, [
        g("span", null, te(e.lastRequestRawJson ? "有记录" : "暂无记录"), 1),
        g("span", null, te(e.lastRequestSnapshot?.requestKind || "history"), 1),
        g("span", null, te(e.lastRequestSnapshot?.providerLabel || e.lastRequestSnapshot?.provider || "未调用"), 1),
        g("span", null, te(e.lastRequestSnapshot?.model || "未选择模型"), 1)
      ]), e.lastRequestRawJson ? (O(), B("pre", oge, te(e.lastRequestRawJson), 1)) : (O(), B("p", ige, " 暂无请求历史。 "))])) : e.tab === "simulate" ? (O(), B("section", lge, [
        g("div", uge, [g("div", null, [a[7] || (a[7] = g("label", { for: "request-simulate-input" }, "模拟本轮发言", -1)), g("textarea", {
          id: "request-simulate-input",
          value: e.simulateInput,
          rows: "5",
          placeholder: "写一句要模拟发送的话",
          onInput: r
        }, null, 40, cge)]), g("button", {
          type: "button",
          onClick: a[3] || (a[3] = (o) => s.$emit("simulate"))
        }, " 生成请求 ")]),
        e.simulateStatus ? (O(), B("p", dge, te(e.simulateStatus), 1)) : Te("", !0),
        e.simulateError ? (O(), B("p", fge, te(e.simulateError), 1)) : Te("", !0),
        e.simulateJson ? (O(), B("pre", hge, te(e.simulateJson), 1)) : Te("", !0)
      ])) : Te("", !0)])
    ])], 32));
  }
}), mge = pge, gge = { class: "xb-sidebar settings-sidebar" }, vge = { class: "panel guide-card" }, yge = { class: "guide-steps" }, _ge = ["onClick"], bge = { class: "guide-label-full" }, wge = { class: "guide-label-mobile" }, Sge = { key: 0 }, Age = /* @__PURE__ */ bn({
  __name: "TavernSettingsSidebar",
  props: {
    active: {},
    items: {}
  },
  emits: ["select"],
  setup(e, { emit: t }) {
    const n = t;
    return (r, s) => (O(), B("aside", gge, [g("div", vge, [s[0] || (s[0] = g("h2", null, "设置", -1)), g("div", yge, [(O(!0), B(He, null, tt(e.items, (a) => (O(), B("button", {
      key: a.key,
      type: "button",
      class: Fe(["guide-step", { active: e.active === a.key }]),
      onClick: (o) => n("select", a.key)
    }, [g("strong", null, [g("span", bge, te(a.label), 1), g("span", wge, te(a.mobileLabel || a.label), 1)]), a.badge ? (O(), B("em", Sge, te(a.badge), 1)) : Te("", !0)], 10, _ge))), 128))])])]));
  }
}), Tge = Age, Ege = { class: "panel step-panel api-workspace" }, Cge = /* @__PURE__ */ bn({
  __name: "TavernApiSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, apiSettingsRootRef: n } = Ei();
    function r(s) {
      n.value = s instanceof HTMLElement ? s : null;
    }
    return (s, a) => er((O(), B("div", Ege, [a[0] || (a[0] = g("div", { class: "panel-head" }, [g("div", null, [g("h2", null, "API 配置")])], -1)), g("div", { class: "api-scroll-body" }, [g("div", {
      ref: r,
      class: "tavern-api-settings"
    })])], 512)), [[rc, T(t) === "api"]]);
  }
}), Ige = Cge, xge = { class: "xb-main" }, kge = /* @__PURE__ */ bn({
  __name: "TavernSettingsPage",
  setup(e) {
    const t = Ei(), n = vc(), { activeSettingsWorkspace: r, selectSettingsWorkspace: s, settingsNavItems: a } = t, { activeView: o, homeThemeDark: l } = n;
    return (i, u) => T(o) === "settings" ? (O(), B("section", {
      key: 0,
      class: Fe(["xb-layout xb-page settings-layout", `is-${T(r)}-workspace`])
    }, [
      Wn(WS, {
        "include-home": "",
        "home-last": "",
        dark: T(l),
        onHome: u[0] || (u[0] = (d) => o.value = "home"),
        onToggleTheme: u[1] || (u[1] = (d) => l.value = !T(l))
      }, null, 8, ["dark"]),
      Wn(Tge, {
        active: T(r),
        items: T(a),
        onSelect: T(s)
      }, null, 8, [
        "active",
        "items",
        "onSelect"
      ]),
      g("section", xge, [
        T(r) === "characters" ? (O(), cn(i8, { key: 0 })) : Te("", !0),
        T(r) === "api" ? (O(), cn(Ige, { key: 1 })) : Te("", !0),
        T(r) === "chatPreset" ? (O(), cn(c8, { key: 2 })) : Te("", !0),
        T(r) === "worldbooks" ? (O(), cn(y8, { key: 3 })) : Te("", !0),
        T(r) === "regex" ? (O(), cn(h8, { key: 4 })) : Te("", !0),
        T(r) === "assistantPreset" ? (O(), cn(l8, { key: 5 })) : Te("", !0),
        T(r) === "base" ? (O(), cn(u8, { key: 6 })) : Te("", !0)
      ])
    ], 2)) : Te("", !0);
  }
}), Mge = kge, Pge = { chat: { exclude: [
  "embedding",
  "embed",
  "rerank",
  "reranker",
  "tts",
  "speech",
  "audio",
  "whisper",
  "transcription",
  "stt",
  "image",
  "sdxl",
  "flux",
  "moderation"
] } }, Rge = Object.freeze([
  "claude-opus-4-7",
  "claude-opus-4-6",
  "claude-opus-4-5",
  "claude-opus-4-5-20251101",
  "claude-sonnet-4-6",
  "claude-sonnet-4-5",
  "claude-sonnet-4-5-20250929",
  "claude-opus-4-1",
  "claude-opus-4-1-20250805",
  "claude-opus-4-0",
  "claude-opus-4-20250514",
  "claude-sonnet-4-0",
  "claude-sonnet-4-20250514"
]);
function Ta(e, t, n = "") {
  if (e.replaceChildren(), n) {
    const r = document.createElement("option");
    r.value = "", r.textContent = n, e.appendChild(r);
  }
  t.forEach((r) => {
    const s = document.createElement("option");
    s.value = r.value, s.textContent = r.label, e.appendChild(s);
  });
}
function kf(e = []) {
  const t = [...new Set(e.filter(Boolean).map((s) => String(s).trim()).filter(Boolean))], n = Pge.chat, r = t.filter((s) => {
    const a = s.toLowerCase();
    return !n.exclude.some((o) => a.includes(o));
  });
  return r.length ? r : t;
}
function xp(e = "") {
  return e === "delegate" ? "delegate" : "main";
}
function ec(e) {
  return String(e || "").trim().replace(/\/+$/, "");
}
function Nge(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function au(e = "") {
  return e === "openai-compatible" || e === "sillytavern-openai-compatible";
}
function i1(e = "") {
  return e === "anthropic" || e === "sillytavern-claude";
}
function $ge(e = "") {
  return e === "sillytavern-claude" ? P1 : e === "sillytavern-google" ? R1 : sc;
}
function Mf(e = []) {
  return [...new Set(e.filter(Boolean).map((t) => String(t).trim()).filter(Boolean))];
}
function Lge(e) {
  const t = ec(e);
  if (!t) return [];
  if (t.endsWith("/v1")) {
    const n = t.slice(0, -3);
    return Mf([
      `${t}/models`,
      `${n}/v1/models`,
      `${n}/models`
    ]);
  }
  return Mf([`${t}/v1/models`, `${t}/models`]);
}
function _8(e) {
  const t = ec(e);
  if (!t) return [];
  if (t.endsWith("/v1")) {
    const n = t.slice(0, -3);
    return Mf([
      `${t}/models`,
      `${n}/v1/models`,
      `${n}/models`
    ]);
  }
  return Mf([`${t}/v1/models`, `${t}/models`]);
}
function Dge(e, t) {
  const n = ec(e);
  if (!n) return [];
  const r = n.endsWith("/v1beta") ? n.slice(0, -7) : n;
  return Mf([
    `${n}/models?key=${encodeURIComponent(t)}`,
    `${n}/models`,
    `${r}/v1beta/models?key=${encodeURIComponent(t)}`,
    `${r}/v1beta/models`,
    `${r}/models?key=${encodeURIComponent(t)}`,
    `${r}/models`
  ]);
}
function Oge(e, t) {
  const n = [
    e?.error?.message,
    e?.message,
    e?.detail,
    e?.details,
    e?.error
  ].find((r) => typeof r == "string" && r.trim());
  return n ? n.trim() : String(t || "").trim().slice(0, 160);
}
async function Fge(e, t = {}) {
  const n = await fetch(e, t), r = await n.text();
  let s = null, a = null;
  try {
    s = r ? JSON.parse(r) : {};
  } catch (o) {
    a = o;
  }
  return {
    ok: n.ok,
    status: n.status,
    url: e,
    data: s,
    rawText: r,
    parseError: a,
    errorSnippet: Oge(s, r)
  };
}
function Bge(e) {
  return kf((e?.data || []).map((t) => String(t?.id || "").trim()).filter(Boolean));
}
function b8(e) {
  return kf((e?.data || []).map((t) => String(t?.id || "").trim()).filter(Boolean));
}
function Uge(e) {
  return kf((e?.models || e?.data || []).map((t) => String(t?.id || t?.name || "")).map((t) => t.split("/").pop() || "").filter(Boolean));
}
async function dm({ urls: e, requestOptionsList: t, extractModels: n, providerLabel: r }) {
  let s = null;
  for (const a of e) for (const o of t) {
    const l = await Fge(a, o);
    if (!l.ok) {
      s = l;
      continue;
    }
    if (l.parseError) {
      s = {
        ...l,
        errorSnippet: "返回的不是 JSON"
      };
      continue;
    }
    const i = n(l.data);
    if (i.length) return i;
    s = {
      ...l,
      errorSnippet: "返回成功，但模型列表为空"
    };
  }
  if (s) {
    const a = s.url ? ` (${s.url})` : "", o = s.errorSnippet ? `：${s.errorSnippet}` : "";
    throw new Error(`${r} 拉取模型失败：${s.status || "unknown"}${o}${a}`);
  }
  throw new Error(`${r} 拉取模型失败：未获取到模型列表。`);
}
async function qge(e) {
  const t = String(e.apiKey || "").trim(), n = ec(e.baseUrl || ""), r = ec(n || Jk.claude);
  if (t && r) try {
    return await dm({
      urls: _8(r),
      requestOptionsList: [{ headers: {
        "x-api-key": t,
        "anthropic-version": "2023-06-01",
        Accept: "application/json"
      } }],
      extractModels: b8,
      providerLabel: "Anthropic"
    });
  } catch (s) {
    if (n) throw s;
  }
  return [...Rge];
}
async function Ax(e) {
  const t = e.provider, n = ec(e.baseUrl || ""), r = String(e.apiKey || "").trim();
  if (t === "sillytavern-claude") return kf(await qge(e));
  if (Nge(t)) return kf(await oO(e, $ge(t)));
  if (!r) throw new Error("请先填写 API Key。");
  if (!n) throw new Error("请先填写 Base URL。");
  return t === "google" ? await dm({
    urls: Dge(n, r),
    requestOptionsList: [
      { headers: {
        Accept: "application/json",
        "x-goog-api-key": r
      } },
      { headers: {
        Accept: "application/json",
        Authorization: `Bearer ${r}`
      } },
      { headers: { Accept: "application/json" } }
    ],
    extractModels: Uge,
    providerLabel: "Google AI"
  }) : i1(t) ? await dm({
    urls: _8(n),
    requestOptionsList: [{ headers: {
      "x-api-key": r,
      "anthropic-version": "2023-06-01",
      Accept: "application/json"
    } }],
    extractModels: b8,
    providerLabel: "Anthropic"
  }) : await dm({
    urls: Lge(n),
    requestOptionsList: [{ headers: {
      Authorization: `Bearer ${r}`,
      Accept: "application/json"
    } }],
    extractModels: Bge,
    providerLabel: t === "openai-responses" ? "OpenAI Responses" : "OpenAI-Compatible"
  });
}
function Hge(e) {
  return e instanceof Error ? e.message : String(e || "unknown_error");
}
function zge(e = {}) {
  const { state: t, render: n, showToast: r, createRequestId: s = (F = "req") => `${F}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`, saveConfig: a, describeError: o = Hge, getRuntimeSummaryText: l } = e;
  function i() {
    t.configFormSyncPending = !0;
  }
  function u(F, ae = "main") {
    const Z = String(F || "").trim() || "openai-compatible";
    return ae === "delegate" ? `delegate:${Z}` : Z;
  }
  function d(F, ae = "main") {
    return t.pullStateByProvider?.[u(F, ae)] || {
      status: "idle",
      message: ""
    };
  }
  function f(F, ae, Z = "main") {
    t.pullStateByProvider = {
      ...t.pullStateByProvider || {},
      [u(F, Z)]: ae
    };
  }
  function m(F, ae, Z = "main") {
    t.modelOptionsByProvider = {
      ...t.modelOptionsByProvider || {},
      [u(F, Z)]: Array.isArray(ae) ? ae : []
    };
  }
  function v(F, ae = "main") {
    const Z = u(F, ae);
    return Array.isArray(t.modelOptionsByProvider?.[Z]) ? t.modelOptionsByProvider[Z] : [];
  }
  function y(F, ae) {
    const Z = t.config?.presets || {}, ie = Hn(F || ae || "默认");
    return Z[ie] ? ie : ae && Z[ae] ? ae : Object.keys(Z)[0] || "默认";
  }
  function S(F, ae) {
    const Z = y(F, xS), ie = ae && typeof ae == "object" ? ae : ys(), me = ie.provider || "openai-compatible", Se = gs(ie.modelConfigs || {}), W = Se[me] || {};
    return {
      delegatePresetName: Z,
      delegateProvider: me,
      delegateModelConfigs: Se,
      delegateBaseUrl: String(W.baseUrl || ""),
      delegateModel: String(W.model || ""),
      delegateApiKey: String(W.apiKey || ""),
      delegateTemperature: ms(W.temperature, 0.2),
      delegateSendTemperature: Fu(W),
      delegateReasoningEnabled: !!W.reasoningEnabled,
      delegateReasoningEffort: ps(W.reasoningEffort),
      delegateToolMode: W.toolMode || "native"
    };
  }
  function x(F = "openai-compatible", ae = {}) {
    const Z = gs(ae || {})[F] || {};
    return {
      baseUrl: String(Z.baseUrl || ""),
      model: String(Z.model || ""),
      apiKey: String(Z.apiKey || ""),
      temperature: ms(Z.temperature, 0.2),
      sendTemperature: Fu(Z),
      reasoningEnabled: !!Z.reasoningEnabled,
      reasoningEffort: ps(Z.reasoningEffort),
      toolMode: Z.toolMode || "native"
    };
  }
  function I(F = "openai-compatible", ae = {}) {
    const Z = gs(ae || {})[F] || {};
    return {
      delegateBaseUrl: String(Z.baseUrl || ""),
      delegateModel: String(Z.model || ""),
      delegateApiKey: String(Z.apiKey || ""),
      delegateTemperature: ms(Z.temperature, 0.2),
      delegateSendTemperature: Fu(Z),
      delegateReasoningEnabled: !!Z.reasoningEnabled,
      delegateReasoningEffort: ps(Z.reasoningEffort),
      delegateToolMode: Z.toolMode || "native"
    };
  }
  function E(F, ae, Z = t.config) {
    const ie = Hn(F || "默认"), me = ae && typeof ae == "object" ? ae : ys(), Se = me.provider || "openai-compatible", W = gs(me.modelConfigs || {}), L = x(Se, W), Q = y(Z?.delegatePresetName, ie), de = S(Q, Z?.delegateConfig && typeof Z.delegateConfig == "object" ? Z.delegateConfig : (Z?.presets || {})[Q] || me);
    return {
      currentPresetName: ie,
      presetDraftName: ie,
      provider: Se,
      modelConfigs: W,
      ...L,
      tavilyApiKey: String(Z?.tavilyApiKey || ""),
      tavilyBaseUrl: Ds(Z?.tavilyBaseUrl || "https://api.tavily.com"),
      permissionMode: Tu(me.permissionMode),
      jsApiPermission: Yo(Z?.jsApiPermission),
      ...de
    };
  }
  function N() {
    if (t.configDraft) return t.configDraft;
    const F = Hn(t.config?.currentPresetName || "默认");
    return t.configDraft = E(F, (t.config?.presets || {})[F] || ys()), t.configDraft;
  }
  function $(F) {
    const ae = N(), Z = F.querySelector("#xb-assistant-provider")?.value || ae.provider || "openai-compatible", ie = F.querySelector("#xb-assistant-delegate-provider")?.value || ae.delegateProvider || "openai-compatible", me = {
      baseUrl: F.querySelector("#xb-assistant-base-url")?.value.trim() || "",
      model: F.querySelector("#xb-assistant-model")?.value.trim() || "",
      apiKey: F.querySelector("#xb-assistant-api-key")?.value.trim() || "",
      temperature: ms(F.querySelector("#xb-assistant-temperature")?.value, ae.temperature ?? 0.2),
      sendTemperature: F.querySelector("#xb-assistant-send-temperature")?.checked ?? !!(ae.sendTemperature ?? !0),
      reasoningEnabled: F.querySelector("#xb-assistant-reasoning-enabled")?.checked || !1,
      reasoningEffort: ps(F.querySelector("#xb-assistant-reasoning-effort")?.value),
      toolMode: au(Z) ? F.querySelector("#xb-assistant-tool-mode")?.value || ae.toolMode || "native" : void 0
    }, Se = {
      baseUrl: F.querySelector("#xb-assistant-delegate-base-url")?.value.trim() ?? ae.delegateBaseUrl ?? "",
      model: F.querySelector("#xb-assistant-delegate-model")?.value.trim() ?? ae.delegateModel ?? "",
      apiKey: F.querySelector("#xb-assistant-delegate-api-key")?.value.trim() ?? ae.delegateApiKey ?? "",
      temperature: ms(F.querySelector("#xb-assistant-delegate-temperature")?.value, ae.delegateTemperature ?? 0.2),
      sendTemperature: F.querySelector("#xb-assistant-delegate-send-temperature")?.checked ?? !!(ae.delegateSendTemperature ?? !0),
      reasoningEnabled: F.querySelector("#xb-assistant-delegate-reasoning-enabled")?.checked ?? !!ae.delegateReasoningEnabled,
      reasoningEffort: ps(F.querySelector("#xb-assistant-delegate-reasoning-effort")?.value || ae.delegateReasoningEffort),
      toolMode: au(ie) ? F.querySelector("#xb-assistant-delegate-tool-mode")?.value || ae.delegateToolMode || "native" : void 0
    }, W = {
      ...gs(ae.modelConfigs || {}),
      [Z]: {
        ...gs(ae.modelConfigs || {})[Z] || {},
        ...me
      }
    }, L = {
      ...gs(ae.delegateModelConfigs || {}),
      [ie]: {
        ...gs(ae.delegateModelConfigs || {})[ie] || {},
        ...Se
      }
    };
    return {
      ...ae,
      currentPresetName: ae.currentPresetName,
      presetDraftName: Hn(F.querySelector("#xb-assistant-preset-name")?.value),
      provider: Z,
      modelConfigs: W,
      baseUrl: me.baseUrl,
      model: me.model,
      apiKey: me.apiKey,
      temperature: me.temperature,
      sendTemperature: me.sendTemperature,
      reasoningEnabled: me.reasoningEnabled,
      reasoningEffort: me.reasoningEffort,
      toolMode: me.toolMode || ae.toolMode || "native",
      tavilyApiKey: F.querySelector("#xb-assistant-tavily-api-key")?.value.trim() || "",
      tavilyBaseUrl: Ds(ae.tavilyBaseUrl || "https://api.tavily.com"),
      permissionMode: Tu(F.querySelector("#xb-assistant-permission-mode")?.value || ae.permissionMode),
      jsApiPermission: Yo(F.querySelector("#xb-assistant-jsapi-permission")?.value || ae.jsApiPermission),
      delegatePresetName: y(F.querySelector("#xb-assistant-delegate-preset-select")?.value || ae.delegatePresetName, ae.currentPresetName),
      delegateProvider: ie,
      delegateModelConfigs: L,
      delegateBaseUrl: Se.baseUrl,
      delegateModel: Se.model,
      delegateApiKey: Se.apiKey,
      delegateTemperature: Se.temperature,
      delegateSendTemperature: Se.sendTemperature,
      delegateReasoningEnabled: Se.reasoningEnabled,
      delegateReasoningEffort: Se.reasoningEffort,
      delegateToolMode: Se.toolMode || ae.delegateToolMode || "native"
    };
  }
  function p(F) {
    return t.configDraft = $(F), t.configDraft;
  }
  function w(F = N()) {
    return i1(F.provider) ? 32e3 : null;
  }
  function _(F = N()) {
    return {
      baseUrl: String(F.baseUrl || ""),
      model: String(F.model || ""),
      apiKey: String(F.apiKey || ""),
      temperature: ms(F.temperature, 0.2),
      sendTemperature: !!(F.sendTemperature ?? !0),
      reasoningEnabled: !!F.reasoningEnabled,
      reasoningEffort: ps(F.reasoningEffort),
      toolMode: au(F.provider) ? F.toolMode || "native" : void 0
    };
  }
  function b(F = N()) {
    return {
      baseUrl: String(F.delegateBaseUrl || ""),
      model: String(F.delegateModel || ""),
      apiKey: String(F.delegateApiKey || ""),
      temperature: ms(F.delegateTemperature, 0.2),
      sendTemperature: !!(F.delegateSendTemperature ?? !0),
      reasoningEnabled: !!F.delegateReasoningEnabled,
      reasoningEffort: ps(F.delegateReasoningEffort),
      toolMode: au(F.delegateProvider) ? F.delegateToolMode || "native" : void 0
    };
  }
  function C(F = N()) {
    const ae = F.delegateProvider || "openai-compatible", Z = gs(F.delegateModelConfigs || {});
    return {
      provider: ae,
      modelConfigs: {
        ...Z,
        [ae]: {
          ...Z[ae] || {},
          ...b(F)
        }
      }
    };
  }
  function k(F = N()) {
    return {
      provider: F.provider || "openai-compatible",
      baseUrl: F.baseUrl || "",
      model: F.model || "",
      apiKey: F.apiKey || "",
      tavilyApiKey: F.tavilyApiKey || "",
      tavilyBaseUrl: Ds(F.tavilyBaseUrl || "https://api.tavily.com"),
      temperature: F.sendTemperature === !1 ? void 0 : ms(F.temperature, 0.2),
      sendTemperature: !!(F.sendTemperature ?? !0),
      maxTokens: w(F),
      timeoutMs: HI,
      toolMode: F.toolMode || "native",
      reasoningEnabled: !!F.reasoningEnabled,
      reasoningEffort: ps(F.reasoningEffort)
    };
  }
  function P(F = N()) {
    return {
      provider: F.delegateProvider || "openai-compatible",
      baseUrl: F.delegateBaseUrl || "",
      model: F.delegateModel || "",
      apiKey: F.delegateApiKey || "",
      tavilyApiKey: F.tavilyApiKey || "",
      tavilyBaseUrl: Ds(F.tavilyBaseUrl || "https://api.tavily.com"),
      temperature: F.delegateSendTemperature === !1 ? void 0 : ms(F.delegateTemperature, 0.2),
      sendTemperature: !!(F.delegateSendTemperature ?? !0),
      maxTokens: i1(F.delegateProvider) ? 32e3 : null,
      timeoutMs: HI,
      toolMode: F.delegateToolMode || "native",
      reasoningEnabled: !!F.delegateReasoningEnabled,
      reasoningEffort: ps(F.delegateReasoningEffort)
    };
  }
  function H(F = {}) {
    const ae = (F.role === "delegate", N());
    return F.role === "delegate" ? P(ae) : k(ae);
  }
  function G(F) {
    N(), t.configDraft = {
      ...t.configDraft,
      presetDraftName: Hn(F.querySelector("#xb-assistant-preset-name")?.value)
    };
  }
  function ee(F = N(), ae = F.provider || "openai-compatible", Z = "main") {
    const ie = d(ae, Z);
    return typeof l == "function" ? l({
      state: t,
      draft: F,
      provider: ae,
      pullState: ie,
      providerLabel: GI(ae)
    }) : `预设「${F.currentPresetName || "默认"}」 · ${GI(ae)}`;
  }
  function J(F, ae, Z) {
    const ie = F?.querySelector?.(ae);
    if (!ie) return;
    const me = String(Z?.status || "idle"), Se = String(Z?.message || "").trim();
    ie.textContent = Se, ie.hidden = !Se, ie.classList.toggle("is-loading", me === "loading"), ie.classList.toggle("is-success", me === "success"), ie.classList.toggle("is-error", me === "error");
  }
  function z(F) {
    if (!F) return;
    const ae = xp(t.configPage);
    t.configPage = ae, F.querySelectorAll("[data-config-page]").forEach((Z) => {
      const ie = xp(Z?.dataset?.configPage) === ae;
      Z.classList.toggle("is-active", ie), Z.setAttribute("aria-selected", ie ? "true" : "false");
    }), F.querySelectorAll("[data-config-page-panel]").forEach((Z) => {
      const ie = xp(Z?.dataset?.configPagePanel) === ae;
      Z.toggleAttribute("hidden", !ie);
    }), F.querySelector("#xb-assistant-delete-preset")?.toggleAttribute("hidden", ae === "delegate");
  }
  function R(F) {
    if (!t.config) return;
    z(F);
    const ae = N(), Z = ae.provider || "openai-compatible", ie = v(Z), me = ae.delegateProvider || "openai-compatible", Se = v(me, "delegate"), W = F.querySelector("#xb-assistant-tool-mode-wrap"), L = F.querySelector("#xb-assistant-tool-mode"), Q = F.querySelector("#xb-assistant-reasoning-enabled"), de = F.querySelector("#xb-assistant-reasoning-effort-wrap"), ge = F.querySelector("#xb-assistant-reasoning-effort"), pe = F.querySelector("#xb-assistant-permission-mode"), Me = F.querySelector("#xb-assistant-jsapi-permission"), Ie = F.querySelector("#xb-assistant-model-pulled"), he = F.querySelector("#xb-assistant-preset-select"), q = F.querySelector("#xb-assistant-preset-name"), ye = F.querySelector("#xb-assistant-delegate-preset-select"), oe = F.querySelector("#xb-assistant-delegate-provider"), De = F.querySelector("#xb-assistant-delegate-base-url"), Je = F.querySelector("#xb-assistant-delegate-model"), st = F.querySelector("#xb-assistant-delegate-api-key"), lt = F.querySelector("#xb-assistant-tavily-api-key"), ot = F.querySelector("#xb-assistant-delegate-model-pulled"), pt = F.querySelector("#xb-assistant-delegate-tool-mode-wrap"), ht = F.querySelector("#xb-assistant-delegate-tool-mode"), It = F.querySelector("#xb-assistant-delegate-reasoning-enabled"), Lt = F.querySelector("#xb-assistant-delegate-reasoning-effort-wrap"), en = F.querySelector("#xb-assistant-delegate-reasoning-effort");
    if (!he || !q) return;
    const at = (t.config.presetNames || []).map((Re) => ({
      value: Re,
      label: Re
    }));
    Ta(he, at), he.value = ae.currentPresetName || t.config.currentPresetName || "默认", ye && (Ta(ye, at), ye.value = y(ae.delegatePresetName, ae.currentPresetName)), q.value = ae.presetDraftName || ae.currentPresetName || "默认", F.querySelector("#xb-assistant-provider").value = Z, F.querySelector("#xb-assistant-base-url").value = ae.baseUrl || "", F.querySelector("#xb-assistant-model").value = ae.model || "", F.querySelector("#xb-assistant-api-key").value = ae.apiKey || "", F.querySelector("#xb-assistant-temperature").value = String(ms(ae.temperature, 0.2)), F.querySelector("#xb-assistant-send-temperature").checked = !!(ae.sendTemperature ?? !0), lt && (lt.value = ae.tavilyApiKey || ""), W.style.display = au(Z) ? "" : "none", Ta(L, zI), L.value = ae.toolMode || "native", pe && (Ta(pe, jZ), pe.value = Tu(ae.permissionMode)), Me && (Ta(Me, QZ), Me.value = Yo(ae.jsApiPermission)), Ta(ge, Q0), Q.checked = !!ae.reasoningEnabled, ge.value = ps(ae.reasoningEffort), de.style.display = Q.checked ? "" : "none", Ta(Ie, ie.map((Re) => ({
      value: Re,
      label: Re
    })), "手动填写"), Ie.value = ie.includes(ae.model) ? ae.model : "", oe && (oe.value = me), De && (De.value = ae.delegateBaseUrl || ""), Je && (Je.value = ae.delegateModel || ""), st && (st.value = ae.delegateApiKey || "");
    const X = F.querySelector("#xb-assistant-delegate-temperature"), se = F.querySelector("#xb-assistant-delegate-send-temperature");
    X && (X.value = String(ms(ae.delegateTemperature, 0.2))), se && (se.checked = !!(ae.delegateSendTemperature ?? !0)), pt && (pt.style.display = au(me) ? "" : "none"), ht && (Ta(ht, zI), ht.value = ae.delegateToolMode || "native"), en && (Ta(en, Q0), en.value = ps(ae.delegateReasoningEffort)), It && (It.checked = !!ae.delegateReasoningEnabled), Lt && (Lt.style.display = ae.delegateReasoningEnabled ? "" : "none"), ot && (Ta(ot, Se.map((Re) => ({
      value: Re,
      label: Re
    })), "手动填写"), ot.value = Se.includes(ae.delegateModel) ? ae.delegateModel : ""), J(F, "#xb-assistant-model-pull-status", d(Z)), J(F, "#xb-assistant-delegate-model-pull-status", d(me, "delegate"));
    const Ae = F.querySelector("#xb-assistant-runtime");
    if (Ae) {
      const Re = t.configPage === "delegate";
      Ae.textContent = ee(Re ? {
        ...ae,
        currentPresetName: "分身",
        provider: me
      } : ae, Re ? me : Z, Re ? "delegate" : "main");
    }
  }
  function V(F) {
    if (typeof a != "function") return;
    const ae = a(F);
    ae && typeof ae.catch == "function" && ae.catch((Z) => {
      r?.(o(Z));
    });
  }
  function le(F, ae, Z) {
    F.querySelector(ae)?.addEventListener("click", () => {
      const ie = F.querySelector(Z);
      ie && (ie.type = ie.type === "password" ? "text" : "password");
    });
  }
  function ve(F) {
    return {
      workspaceFileName: F?.workspaceFileName || "",
      jsApiPermission: Yo(F?.jsApiPermission),
      tavilyApiKey: String(F?.tavilyApiKey || ""),
      tavilyBaseUrl: Ds(F?.tavilyBaseUrl || "https://api.tavily.com"),
      currentPresetName: F?.currentPresetName || "默认",
      delegatePresetName: F?.delegatePresetName || F?.currentPresetName || "默认",
      delegateConfig: F?.delegateConfig || {},
      presets: F?.presets || {}
    };
  }
  function ke(F, ae = {}) {
    const Z = p(F), ie = Hn(ae.presetName || Z.presetDraftName), me = Hn(Z.currentPresetName || t.config?.currentPresetName || "默认"), Se = (t.config?.presets || {})[me] || ys(), W = gs(Z.modelConfigs || Se.modelConfigs || {}), L = {
      ...Se,
      provider: Z.provider,
      permissionMode: Tu(Z.permissionMode),
      modelConfigs: {
        ...W,
        [Z.provider]: {
          ...W[Z.provider] || {},
          ..._(Z)
        }
      }
    }, Q = { ...t.config?.presets || {} };
    ae.renameCurrentPreset && ie !== me && delete Q[me], Q[ie] = L, t.config = ef({
      ...t.config,
      jsApiPermission: Yo(Z.jsApiPermission),
      tavilyApiKey: String(Z.tavilyApiKey || ""),
      tavilyBaseUrl: Ds(Z.tavilyBaseUrl || "https://api.tavily.com"),
      currentPresetName: ie,
      delegatePresetName: y(Z.delegatePresetName, ie),
      delegateConfig: C(Z),
      presets: Q
    }), t.configDraft = E(ie, L, t.config), i(), V({
      requestId: s(ae.requestPrefix || "save-config"),
      config: t.config,
      payload: ve(t.config)
    });
  }
  function be(F, ae = "") {
    const Z = Hn(ae || "默认"), ie = typeof window < "u" && typeof window.prompt == "function" ? window.prompt(F, Z) : Z;
    return ie === null ? "" : Hn(ie);
  }
  function Ne(F) {
    const ae = be("输入新预设名称：", `${p(F).currentPresetName || "默认"} 副本`);
    if (!ae) {
      r?.("预设名称不能为空");
      return;
    }
    F.querySelector("#xb-assistant-preset-name").value = ae, ke(F, {
      presetName: ae,
      requestPrefix: "create-preset"
    });
  }
  function ce(F) {
    const ae = p(F), Z = Hn(ae.currentPresetName || t.config?.currentPresetName || "默认"), ie = be("输入预设名称：", ae.presetDraftName || Z);
    if (!ie) {
      r?.("预设名称不能为空");
      return;
    }
    ie !== Z && (F.querySelector("#xb-assistant-preset-name").value = ie, ke(F, {
      presetName: ie,
      renameCurrentPreset: !0,
      requestPrefix: "rename-preset"
    }));
  }
  function we(F) {
    if (Object.keys(t.config?.presets || {}).length <= 1) {
      r?.("至少要保留一套预设");
      return;
    }
    const ae = p(F), Z = Hn(t.configDraft?.currentPresetName || t.config?.currentPresetName || "默认"), ie = { ...t.config?.presets || {} };
    delete ie[Z];
    const me = Object.keys(ie)[0] || "默认", Se = ie[me] || ys();
    t.config = ef({
      ...t.config,
      jsApiPermission: Yo(ae.jsApiPermission),
      tavilyApiKey: String(ae.tavilyApiKey || t.config?.tavilyApiKey || ""),
      tavilyBaseUrl: Ds(ae.tavilyBaseUrl || t.config?.tavilyBaseUrl || "https://api.tavily.com"),
      currentPresetName: me,
      delegatePresetName: y(ae.delegatePresetName, me),
      delegateConfig: C(ae),
      presets: ie
    }), t.configDraft = E(me, Se, t.config), i(), V({
      requestId: s("delete-preset"),
      config: t.config,
      payload: ve(t.config)
    }), n?.();
  }
  function Oe(F) {
    F?.querySelector?.("#xb-assistant-provider") && (F.querySelector("#xb-assistant-provider").addEventListener("change", (ae) => {
      const Z = ae.currentTarget.value, ie = p(F);
      t.configDraft = {
        ...ie,
        provider: Z,
        ...x(Z, ie.modelConfigs)
      }, i(), n?.();
    }), F.querySelector("#xb-assistant-preset-select").addEventListener("change", (ae) => {
      const Z = Hn(ae.currentTarget.value), ie = (t.config?.presets || {})[Z] || ys(), me = p(F);
      t.config = ef({
        ...t.config,
        jsApiPermission: Yo(me.jsApiPermission),
        currentPresetName: Z,
        delegatePresetName: y(me.delegatePresetName, Z),
        delegateConfig: C(me)
      }), t.configDraft = E(Z, ie, t.config), i(), n?.();
    }), F.querySelector("#xb-assistant-preset-name")?.addEventListener("input", () => {
      G(F);
    }), F.querySelector("#xb-assistant-base-url").addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-model").addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-api-key").addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-temperature")?.addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-send-temperature")?.addEventListener("change", () => {
      p(F);
    }), F.querySelector("#xb-assistant-tavily-api-key")?.addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-model-pulled").addEventListener("change", (ae) => {
      const Z = ae.currentTarget.value;
      Z && (F.querySelector("#xb-assistant-model").value = Z, p(F));
    }), le(F, "#xb-assistant-toggle-key", "#xb-assistant-api-key"), le(F, "#xb-assistant-toggle-tavily-key", "#xb-assistant-tavily-api-key"), F.querySelector("#xb-assistant-delegate-provider")?.addEventListener("change", (ae) => {
      const Z = p(F), ie = ae.currentTarget.value;
      t.configDraft = {
        ...Z,
        delegateProvider: ie,
        ...I(ie, Z.delegateModelConfigs)
      }, i(), n?.();
    }), F.querySelector("#xb-assistant-delegate-base-url")?.addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-delegate-model")?.addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-delegate-api-key")?.addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-delegate-temperature")?.addEventListener("input", () => {
      p(F);
    }), F.querySelector("#xb-assistant-delegate-send-temperature")?.addEventListener("change", () => {
      p(F);
    }), F.querySelector("#xb-assistant-delegate-model-pulled")?.addEventListener("change", (ae) => {
      const Z = ae.currentTarget.value;
      if (!Z) return;
      const ie = F.querySelector("#xb-assistant-delegate-model");
      ie && (ie.value = Z), p(F);
    }), le(F, "#xb-assistant-delegate-toggle-key", "#xb-assistant-delegate-api-key"), F.querySelector("#xb-assistant-reasoning-enabled").addEventListener("change", () => {
      p(F), i(), n?.();
    }), F.querySelector("#xb-assistant-reasoning-effort").addEventListener("change", () => {
      p(F);
    }), F.querySelector("#xb-assistant-tool-mode").addEventListener("change", () => {
      p(F);
    }), F.querySelector("#xb-assistant-delegate-reasoning-enabled")?.addEventListener("change", () => {
      p(F), i(), n?.();
    }), F.querySelector("#xb-assistant-delegate-reasoning-effort")?.addEventListener("change", () => {
      p(F);
    }), F.querySelector("#xb-assistant-delegate-tool-mode")?.addEventListener("change", () => {
      p(F);
    }), F.querySelector("#xb-assistant-permission-mode")?.addEventListener("change", () => {
      p(F);
    }), F.querySelector("#xb-assistant-jsapi-permission")?.addEventListener("change", () => {
      p(F);
    }), F.querySelector("#xb-assistant-delegate-preset-select")?.addEventListener("change", (ae) => {
      const Z = y(ae.currentTarget?.value, t.configDraft?.currentPresetName || t.config?.currentPresetName || "默认"), ie = (t.config?.presets || {})[Z] || ys();
      t.configDraft = {
        ...p(F),
        ...S(Z, ie)
      }, i(), n?.();
    }), F.querySelectorAll("[data-config-page]").forEach((ae) => {
      ae.addEventListener("click", (Z) => {
        p(F), t.configPage = xp(Z.currentTarget?.dataset?.configPage), z(F), R(F);
      });
    }), F.querySelector("#xb-assistant-pull-models").addEventListener("click", async () => {
      p(F), i();
      const ae = H();
      f(ae.provider, {
        status: "loading",
        message: "正在拉取模型列表…"
      }), n?.();
      try {
        const Z = await Ax(ae);
        m(ae.provider, Z), f(ae.provider, {
          status: "success",
          message: `已拉取 ${Z.length} 个模型`
        });
      } catch (Z) {
        m(ae.provider, []), f(ae.provider, {
          status: "error",
          message: o(Z)
        });
      }
      i(), n?.();
    }), F.querySelector("#xb-assistant-delegate-pull-models")?.addEventListener("click", async () => {
      p(F), i();
      const ae = H({ role: "delegate" });
      f(ae.provider, {
        status: "loading",
        message: "正在拉取模型列表…"
      }, "delegate"), n?.();
      try {
        const Z = await Ax(ae);
        m(ae.provider, Z, "delegate"), f(ae.provider, {
          status: "success",
          message: `已拉取 ${Z.length} 个模型`
        }, "delegate");
      } catch (Z) {
        m(ae.provider, [], "delegate"), f(ae.provider, {
          status: "error",
          message: o(Z)
        }, "delegate");
      }
      i(), n?.();
    }), F.querySelector("#xb-assistant-new-preset")?.addEventListener("click", () => {
      Ne(F);
    }), F.querySelector("#xb-assistant-rename-preset")?.addEventListener("click", () => {
      ce(F);
    }), F.querySelector("#xb-assistant-save").addEventListener("click", () => {
      ke(F);
    }), F.querySelector("#xb-assistant-delegate-save")?.addEventListener("click", () => {
      ke(F, { requestPrefix: "save-delegate-config" });
    }), F.querySelector("#xb-assistant-delete-preset").addEventListener("click", () => {
      we(F);
    }));
  }
  return {
    getActiveProviderConfig: H,
    syncConfigToForm: R,
    bindSettingsPanelEvents: Oe
  };
}
function fm(e = "") {
  return String(e || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function id(e) {
  return `<svg viewBox="0 0 24 24" aria-hidden="true">${{
    add: '<path d="M12 5v14" /><path d="M5 12h14" />',
    rename: '<path d="M12 20h9" /><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" />',
    save: '<path d="M5 21h14a1 1 0 0 0 1-1V7.5L16.5 4H5a1 1 0 0 0-1 1v15a1 1 0 0 0 1 1Z" /><path d="M8 21v-7h8v7" /><path d="M8 4v5h7" />',
    saving: '<path class="xb-assistant-save-spinner" d="M12 3a9 9 0 1 1-8.2 5.3" />',
    success: '<path d="M20 6 9 17l-5-5" />',
    error: '<path d="M18 6 6 18" /><path d="M6 6l12 12" />',
    delete: '<path d="M3 6h18" /><path d="M8 6V4h8v2" /><path d="M19 6l-1 14a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1L5 6" /><path d="M10 11v6" /><path d="M14 11v6" />'
  }[e] || ""}</svg>`;
}
function Kge(e = {}) {
  const t = String(e?.status || "idle");
  return t === "saving" ? "saving" : t === "success" ? "success" : t === "error" ? "error" : "save";
}
function Wge(e = {}) {
  const t = String(e?.status || "idle");
  return t === "saving" ? {
    className: "xb-assistant-save-button is-saving",
    title: "正在保存配置"
  } : t === "success" ? {
    className: "xb-assistant-save-button is-success",
    title: "配置已保存"
  } : t === "error" ? {
    className: "xb-assistant-save-button is-error",
    title: fm(e?.error || "保存失败")
  } : {
    className: "xb-assistant-save-button",
    title: "保存配置"
  };
}
function Gge(e = {}) {
  const { configSave: t = {}, runtimeText: n = "", inlineToastText: r = "", showInlineToast: s = !0, showAssistantPermissions: a = !0, showDelegateSettings: o = !0, activePage: l = "main", delegatePresetHint: i = "DelegateRun 分身会使用这里的独立 API 配置；可以和主助手使用不同 Provider、Base URL、模型和 Tool 调用格式。", isBusy: u = !1, canDeletePreset: d = !0 } = e, f = Wge(t), m = Kge(t), v = u || String(t?.status || "") === "saving" ? "disabled" : "", y = u || !d ? "disabled" : "", S = l === "delegate" ? "delegate" : "main", x = S === "main", I = S === "delegate", E = a ? `
            <label>
                <span>斜杠命令权限</span>
                <select id="xb-assistant-permission-mode"></select>
            </label>
            <label>
                <span>JavaScript API 权限</span>
                <select id="xb-assistant-jsapi-permission"></select>
            </label>` : "", N = o ? `
            <div class="xb-assistant-config-tabs" role="tablist" aria-label="API 配置分页">
                <button id="xb-assistant-config-tab-main" type="button" class="xb-assistant-config-tab ${x ? "is-active" : ""}" data-config-page="main" role="tab" aria-selected="${x ? "true" : "false"}">主助手 API</button>
                <button id="xb-assistant-config-tab-delegate" type="button" class="xb-assistant-config-tab ${I ? "is-active" : ""}" data-config-page="delegate" role="tab" aria-selected="${I ? "true" : "false"}">分身 API</button>
            </div>` : "", $ = o ? `
            <div class="xb-assistant-config-page" data-config-page-panel="delegate" ${I ? "" : "hidden"}>
                <p class="xb-assistant-config-note">${fm(i)}</p>
                <div class="xb-assistant-preset-row">
                    <select id="xb-assistant-delegate-preset-select" class="xb-assistant-preset-field" aria-label="已存预设"></select>
                    <div class="xb-assistant-preset-tools is-single" aria-label="分身 API 预设操作">
                        <button id="xb-assistant-delegate-save" type="button" class="xb-assistant-icon-button ${f.className}" title="${f.title}" aria-label="${f.title}" ${v}>${id(m)}</button>
                    </div>
                </div>
                <label>
                    <span>Provider</span>
                    <select id="xb-assistant-delegate-provider">
                        <option value="openai-responses">OpenAI Responses</option>
                        <option value="openai-compatible">OpenAI 兼容</option>
                        <option value="sillytavern-openai-compatible">酒馆 OpenAI 兼容</option>
                        <option value="sillytavern-claude">酒馆 Claude</option>
                        <option value="sillytavern-google">酒馆 Google AI</option>
                        <option value="anthropic">Anthropic</option>
                        <option value="google">Google AI</option>
                    </select>
                </label>
                <label>
                    <span>Base URL</span>
                    <input id="xb-assistant-delegate-base-url" type="text" />
                </label>
                <label>
                    <span>API Key</span>
                    <div class="xb-assistant-inline-input">
                        <input id="xb-assistant-delegate-api-key" type="password" />
                        <button id="xb-assistant-delegate-toggle-key" type="button" class="secondary ghost">显示</button>
                    </div>
                </label>
                <label>
                    <span>Model</span>
                    <input id="xb-assistant-delegate-model" type="text" />
                </label>
                <div class="xb-assistant-inline-input xb-assistant-model-row">
                    <label class="xb-assistant-grow">
                        <span>已拉取模型</span>
                        <select id="xb-assistant-delegate-model-pulled">
                            <option value="">手动填写</option>
                        </select>
                    </label>
                    <button id="xb-assistant-delegate-pull-models" type="button" class="secondary" ${u ? "disabled" : ""}>拉取模型</button>
                </div>
                <div class="xb-assistant-inline-status" id="xb-assistant-delegate-model-pull-status" aria-live="polite" hidden></div>
                <div class="xb-assistant-temperature-row">
                    <label>
                        <span>温度</span>
                        <input id="xb-assistant-delegate-temperature" type="number" min="0" max="2" step="0.05" />
                    </label>
                    <label class="xb-assistant-checkbox-row">
                        <span>允许传参</span>
                        <span class="xb-assistant-checkbox-control">
                            <input id="xb-assistant-delegate-send-temperature" type="checkbox" />
                        </span>
                    </label>
                </div>
                <label id="xb-assistant-delegate-tool-mode-wrap">
                    <span>Tool 调用格式</span>
                    <select id="xb-assistant-delegate-tool-mode"></select>
                </label>
                <label class="xb-assistant-checkbox-row">
                    <span>
                        Reasoning参数
                        <small>需 API 支持，否则报错</small>
                    </span>
                    <span class="xb-assistant-checkbox-control">
                        <input id="xb-assistant-delegate-reasoning-enabled" type="checkbox" />
                        <span>开启</span>
                    </span>
                </label>
                <label id="xb-assistant-delegate-reasoning-effort-wrap">
                    <span>思考强度</span>
                    <select id="xb-assistant-delegate-reasoning-effort"></select>
                </label>
            </div>` : "";
  return `
        <section class="xb-assistant-config">
            ${N}
            <div class="xb-assistant-config-page" data-config-page-panel="main" ${x ? "" : "hidden"}>
            <div class="xb-assistant-preset-row">
                <select id="xb-assistant-preset-select" class="xb-assistant-preset-field" aria-label="已存预设"></select>
                <input id="xb-assistant-preset-name" type="hidden" />
                <div class="xb-assistant-preset-tools" aria-label="API 预设操作">
                    <button id="xb-assistant-new-preset" type="button" class="xb-assistant-icon-button" title="新增预设" aria-label="新增预设" ${u ? "disabled" : ""}>${id("add")}</button>
                    <button id="xb-assistant-rename-preset" type="button" class="xb-assistant-icon-button" title="重命名预设" aria-label="重命名预设" ${u ? "disabled" : ""}>${id("rename")}</button>
                    <button id="xb-assistant-save" type="button" class="xb-assistant-icon-button ${f.className}" title="${f.title}" aria-label="${f.title}" ${v}>${id(m)}</button>
                    <button id="xb-assistant-delete-preset" type="button" class="xb-assistant-icon-button" title="删除预设" aria-label="删除预设" ${y}>${id("delete")}</button>
                </div>
            </div>
            <label>
                <span>Provider</span>
                <select id="xb-assistant-provider">
                    <option value="openai-responses">OpenAI Responses</option>
                    <option value="openai-compatible">OpenAI 兼容</option>
                    <option value="sillytavern-openai-compatible">酒馆 OpenAI 兼容</option>
                    <option value="sillytavern-claude">酒馆 Claude</option>
                    <option value="sillytavern-google">酒馆 Google AI</option>
                    <option value="anthropic">Anthropic</option>
                    <option value="google">Google AI</option>
                </select>
            </label>
            <label>
                <span>Base URL</span>
                <input id="xb-assistant-base-url" type="text" />
            </label>
            <label>
                <span>API Key</span>
                <div class="xb-assistant-inline-input">
                    <input id="xb-assistant-api-key" type="password" />
                    <button id="xb-assistant-toggle-key" type="button" class="secondary ghost">显示</button>
                </div>
            </label>
            <label>
                <span>Model</span>
                <input id="xb-assistant-model" type="text" />
            </label>
            <div class="xb-assistant-inline-input xb-assistant-model-row">
                <label class="xb-assistant-grow">
                    <span>已拉取模型</span>
                    <select id="xb-assistant-model-pulled">
                        <option value="">手动填写</option>
                    </select>
                </label>
                <button id="xb-assistant-pull-models" type="button" class="secondary" ${u ? "disabled" : ""}>拉取模型</button>
            </div>
            <div class="xb-assistant-inline-status" id="xb-assistant-model-pull-status" aria-live="polite" hidden></div>
            <div class="xb-assistant-temperature-row">
                <label>
                    <span>温度</span>
                    <input id="xb-assistant-temperature" type="number" min="0" max="2" step="0.05" />
                </label>
                <label class="xb-assistant-checkbox-row">
                    <span>允许传参</span>
                    <span class="xb-assistant-checkbox-control">
                        <input id="xb-assistant-send-temperature" type="checkbox" />
                    </span>
                </label>
            </div>
            <label>
                <span>Tavily API Key（全局）</span>
                <div class="xb-assistant-inline-input">
                    <input id="xb-assistant-tavily-api-key" type="password" />
                    <button id="xb-assistant-toggle-tavily-key" type="button" class="secondary ghost">显示</button>
                </div>
            </label>
            <label id="xb-assistant-tool-mode-wrap">
                <span>Tool 调用格式</span>
                <select id="xb-assistant-tool-mode"></select>
            </label>
            ${E}
            <label class="xb-assistant-checkbox-row">
                <span>
                    Reasoning参数
                    <small>需 API 支持，否则报错</small>
                </span>
                <span class="xb-assistant-checkbox-control">
                    <input id="xb-assistant-reasoning-enabled" type="checkbox" />
                    <span>开启</span>
                </span>
            </label>
            <label id="xb-assistant-reasoning-effort-wrap">
                <span>思考强度</span>
                <select id="xb-assistant-reasoning-effort"></select>
            </label>
            </div>
            ${$}
            <div class="xb-assistant-runtime" id="xb-assistant-runtime">${fm(n)}</div>
            ${s ? `<div class="xb-assistant-toast xb-assistant-toast-inline" id="xb-assistant-toast" aria-live="polite">${fm(r)}</div>` : ""}
        </section>
    `;
}
var L_ = 48, D_ = 48, O_ = 80, kp = 24, F_ = 60, Vge = [{
  key: "statePrompt",
  label: "全局记忆",
  summary: "全局状态输出规则"
}, {
  key: "characterPrompt",
  label: "人物记忆",
  summary: "人物长期状态输出规则"
}], Jge = {
  top: "最前面",
  beforeCharacter: "角色卡前",
  afterCharacter: "角色卡后",
  beforeHistory: "历史前",
  afterHistory: "历史后",
  assistantPrefill: "回复开头"
};
function or(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function ou(e) {
  try {
    return JSON.parse(JSON.stringify(e));
  } catch {
    return e;
  }
}
function Xo(e = "") {
  return String(e || "").trim().toLocaleLowerCase();
}
function Yge(e, t) {
  return t ? Xo(e).includes(t) : !0;
}
function B_(e, t = 1600) {
  return e.map((n) => String(n ?? "").slice(0, t)).filter(Boolean).join(`
`);
}
function iu(e) {
  return JSON.stringify(e || {});
}
function Mp(e) {
  return Array.isArray(e) ? e.map((t) => String(t || "").trim()).filter(Boolean) : [];
}
function uo(e) {
  if (e === "" || e === null || e === void 0) return null;
  const t = Number(e);
  return Number.isFinite(t) ? t : null;
}
function U_(e) {
  return e === !0 || e === "true" ? !0 : e === !1 || e === "false" ? !1 : null;
}
function Xge(e) {
  if (e === !0 || e === "true") return !0;
  if (e === !1 || e === "false" || e === "" || e === null || e === void 0) return !1;
  const t = Number(e);
  return Number.isFinite(t) && t > 0 ? Math.floor(t) : !1;
}
function jge(e) {
  const t = or(e), n = Array.isArray(t.entries) ? t.entries.map((r) => {
    const s = or(r);
    return {
      uid: String(s.uid || ""),
      name: String(s.name || "未命名条目"),
      keys: Array.isArray(s.keys) ? s.keys.map((a) => String(a || "")).filter(Boolean) : [],
      secondaryKeys: Array.isArray(s.secondaryKeys) ? s.secondaryKeys.map((a) => String(a || "")).filter(Boolean) : [],
      contentPreview: String(s.contentPreview || ""),
      enabled: s.enabled !== !1,
      constant: s.constant === !0,
      vectorized: s.vectorized === !0,
      order: Number.isFinite(Number(s.order)) ? Number(s.order) : 100,
      position: Number.isFinite(Number(s.position)) ? Number(s.position) : 0,
      role: Number.isFinite(Number(s.role)) ? Number(s.role) : 0,
      depth: uo(s.depth),
      probability: uo(s.probability)
    };
  }) : [];
  return {
    name: String(t.name || ""),
    entryCount: Number.isFinite(Number(t.entryCount)) ? Number(t.entryCount) : n.length,
    enabledCount: Number.isFinite(Number(t.enabledCount)) ? Number(t.enabledCount) : n.filter((r) => r.enabled).length,
    constantCount: Number.isFinite(Number(t.constantCount)) ? Number(t.constantCount) : n.filter((r) => r.constant).length,
    disabledCount: Number.isFinite(Number(t.disabledCount)) ? Number(t.disabledCount) : n.filter((r) => !r.enabled).length,
    keywordCount: Number.isFinite(Number(t.keywordCount)) ? Number(t.keywordCount) : n.reduce((r, s) => r + s.keys.length + s.secondaryKeys.length, 0),
    totalChars: Number.isFinite(Number(t.totalChars)) ? Number(t.totalChars) : n.reduce((r, s) => r + s.contentPreview.length, 0),
    entries: n
  };
}
function Tx(e) {
  const t = or(e), n = Mp(t.keysecondary), r = Mp(t.secondary_keys), s = n.length ? n : r, a = s, o = t.disable === !0 || t.enabled === !1, l = t.constant === !0, i = !l && t.vectorized === !0;
  return {
    worldbookName: String(t.worldbookName || t.name || "").trim(),
    uid: t.uid === null || t.uid === void 0 ? "" : String(t.uid).trim(),
    comment: String(t.comment ?? ""),
    key: Mp(t.key),
    keysecondary: s,
    secondary_keys: a,
    content: String(t.content ?? ""),
    disable: o,
    enabled: !o,
    constant: l,
    vectorized: i,
    order: Number.isFinite(Number(t.order)) ? Number(t.order) : 100,
    position: Number.isFinite(Number(t.position)) ? Number(t.position) : 0,
    role: Number.isFinite(Number(t.role)) ? Number(t.role) : 0,
    depth: Number(t.position) === 4 ? uo(t.depth) ?? 4 : null,
    probability: uo(t.probability) ?? 100,
    useProbability: t.useProbability !== !1,
    selective: t.selective === !0,
    selectiveLogic: Number.isFinite(Number(t.selectiveLogic)) ? Number(t.selectiveLogic) : 0,
    scanDepth: uo(t.scanDepth),
    caseSensitive: U_(t.caseSensitive),
    matchWholeWords: U_(t.matchWholeWords),
    useGroupScoring: U_(t.useGroupScoring),
    outletName: String(t.outletName ?? ""),
    automationId: String(t.automationId ?? ""),
    ignoreBudget: t.ignoreBudget === !0,
    excludeRecursion: t.excludeRecursion === !0,
    preventRecursion: t.preventRecursion === !0,
    delayUntilRecursion: Xge(t.delayUntilRecursion),
    group: String(t.group ?? ""),
    groupOverride: t.groupOverride === !0,
    groupWeight: uo(t.groupWeight),
    sticky: uo(t.sticky),
    cooldown: uo(t.cooldown),
    delay: uo(t.delay),
    triggers: Mp(t.triggers),
    matchPersonaDescription: t.matchPersonaDescription === !0,
    matchCharacterDescription: t.matchCharacterDescription === !0,
    matchCharacterPersonality: t.matchCharacterPersonality === !0,
    matchCharacterDepthPrompt: t.matchCharacterDepthPrompt === !0,
    matchScenario: t.matchScenario === !0,
    matchCreatorNotes: t.matchCreatorNotes === !0,
    entryHash: String(t.entryHash || ""),
    revision: String(t.revision || t.entryHash || "")
  };
}
function Ex(e) {
  const t = or(e), n = Array.isArray(t.options) ? t.options.map((r) => String(r || "").trim()).filter(Boolean) : [];
  return {
    options: n,
    selected: Array.isArray(t.selected) ? t.selected.map((r) => String(r || "").trim()).filter((r) => n.includes(r)) : []
  };
}
function q_(e = {}) {
  const t = or(e);
  return {
    ...t,
    id: String(t.id || ""),
    scriptName: String(t.scriptName || ""),
    findRegex: String(t.findRegex || ""),
    replaceString: String(t.replaceString || ""),
    trimStrings: Array.isArray(t.trimStrings) ? t.trimStrings.map((n) => String(n || "")).filter(Boolean) : [],
    placement: Array.isArray(t.placement) ? t.placement.map((n) => Number(n)).filter((n) => Number.isFinite(n)) : [],
    disabled: t.disabled === !0,
    markdownOnly: t.markdownOnly === !0,
    promptOnly: t.promptOnly === !0,
    runOnEdit: t.runOnEdit === !0,
    substituteRegex: Number.isFinite(Number(t.substituteRegex)) ? Number(t.substituteRegex) : 0,
    minDepth: t.minDepth === null || t.minDepth === "" || t.minDepth === void 0 ? null : Number(t.minDepth),
    maxDepth: t.maxDepth === null || t.maxDepth === "" || t.maxDepth === void 0 ? null : Number(t.maxDepth)
  };
}
function Qge(e) {
  const t = or(e), n = String(t.id || "").trim();
  return n ? {
    id: n,
    name: String(t.name || n).trim() || n,
    avatarUrl: String(t.avatarUrl || "").trim(),
    description: String(t.description || "").trim(),
    active: t.active === !0
  } : null;
}
function Zge(e) {
  const t = or(e), n = String(t.currentUserId || "").trim() || null, r = Array.isArray(t.users) ? t.users.map((s) => Qge(s)).filter((s) => !!s) : [];
  return n && !r.some((s) => s.id === n) && r.unshift({
    id: n,
    name: n,
    avatarUrl: "",
    description: "",
    active: !0
  }), {
    users: r.map((s) => ({
      ...s,
      active: n ? s.id === n : s.active
    })),
    currentUserId: n
  };
}
var eve = 5e3, tve = 5e3, Cx = 1800;
function nve() {
  const e = String(window.location.hash || "").replace(/^#\/?/, "").split("/")[1];
  return e === "characters" || e === "api" || e === "chatPreset" || e === "worldbooks" || e === "regex" || e === "assistantPreset" || e === "base" ? e : "api";
}
function rve(e) {
  const t = WM(), n = yf(), r = /* @__PURE__ */ ue(null), s = /* @__PURE__ */ ue({
    status: "idle",
    requestId: "",
    error: ""
  }), a = /* @__PURE__ */ ue("");
  let o = null, l = null;
  const i = /* @__PURE__ */ ue(t), u = /* @__PURE__ */ ue(t), d = /* @__PURE__ */ ue({}), f = /* @__PURE__ */ ue(""), m = /* @__PURE__ */ ue({
    status: "idle",
    error: ""
  }), v = /* @__PURE__ */ ue(JSON.stringify(t)), y = /* @__PURE__ */ ue(""), S = /* @__PURE__ */ ue(n), x = /* @__PURE__ */ ue(n), I = /* @__PURE__ */ ue([]), E = /* @__PURE__ */ ue(""), N = /* @__PURE__ */ ue(""), $ = /* @__PURE__ */ ue({
    status: "idle",
    error: ""
  }), p = /* @__PURE__ */ ue(JSON.stringify(n)), w = /* @__PURE__ */ ue(""), _ = /* @__PURE__ */ ue("statePrompt"), b = /* @__PURE__ */ ue({}), C = /* @__PURE__ */ ue(""), k = /* @__PURE__ */ ue(""), P = /* @__PURE__ */ ue([]), H = /* @__PURE__ */ ue([]), G = /* @__PURE__ */ ue(""), ee = /* @__PURE__ */ ue(!1), J = /* @__PURE__ */ ue(null), z = /* @__PURE__ */ ue(""), R = /* @__PURE__ */ ue(null), V = /* @__PURE__ */ ue(""), le = /* @__PURE__ */ ue(""), ve = /* @__PURE__ */ ue(""), ke = /* @__PURE__ */ ue(""), be = /* @__PURE__ */ ue(L_), Ne = /* @__PURE__ */ ue(""), ce = /* @__PURE__ */ ue(D_), we = /* @__PURE__ */ ue(""), Oe = /* @__PURE__ */ ue(O_), F = /* @__PURE__ */ ue(kp), ae = /* @__PURE__ */ ue(""), Z = /* @__PURE__ */ ue({}), ie = /* @__PURE__ */ ue({}), me = /* @__PURE__ */ ue(""), Se = /* @__PURE__ */ ue({}), W = /* @__PURE__ */ ue(iu(Se.value)), L = /* @__PURE__ */ ue(""), Q = /* @__PURE__ */ ue([]), de = /* @__PURE__ */ ue(null), ge = /* @__PURE__ */ ue(""), pe = /* @__PURE__ */ ue(!1), Me = /* @__PURE__ */ ue(!1), Ie = /* @__PURE__ */ ue(""), he = /* @__PURE__ */ ue(yu(e.tavernDisplaySettings.value)), q = /* @__PURE__ */ ue(ou(he.value));
  let ye = 0, oe = null, De = null, Je = 0, st = "", lt = 0, ot = 0, pt = 0;
  const ht = {
    config: {},
    configDraft: null,
    configFormSyncPending: !0,
    configPage: "main",
    configSave: s.value,
    pullStateByProvider: {},
    modelOptionsByProvider: {}
  };
  let It = null;
  const Lt = (U = i.value) => JSON.stringify(U || {}), en = (U = S.value) => JSON.stringify(U || {}), at = ne(() => Lt(i.value) !== v.value), X = ne(() => Dc(u.value)), se = ne(() => en(S.value) !== p.value), Ae = ne(() => $S(e.agentConfig.value)), Re = ne(() => Ae.value.readiness.ok), Be = ne(() => Ae.value.readiness.message), je = ne(() => {
    const U = Ae.value;
    return `预设「${U.currentPresetName || "默认"}」 · ${U.providerLabel} / ${U.model || "未选择模型"}`;
  }), Qe = ne(() => {
    const U = or(d.value.components), re = Array.isArray(U.promptManager) ? U.promptManager : [], Ee = String(i.value.promptManager?.name || i.value.name || "").trim(), Le = /* @__PURE__ */ new Set();
    return [Ee, ...re].map((dt) => {
      if (typeof dt == "string") return dt.trim();
      const _t = or(dt);
      return String(_t.name || _t.label || _t.id || "").trim();
    }).filter((dt) => !dt || Le.has(dt) ? !1 : (Le.add(dt), !0)).map((dt) => ({
      name: dt,
      label: dt
    }));
  }), ut = ne(() => {
    const U = Xo(ke.value);
    return U ? Qe.value.filter((re) => Yge(re.label || re.name, U)) : Qe.value;
  }), wt = ne(() => {
    const U = ut.value.slice(0, be.value), re = String(w.value || i.value.promptManager?.name || i.value.name || "").trim();
    if (!re || U.some((Le) => Le.name === re)) return U;
    const Ee = Qe.value.find((Le) => Le.name === re);
    return Ee ? [Ee, ...U] : U;
  }), rn = ne(() => Math.max(0, ut.value.length - Math.min(ut.value.length, be.value))), Ht = ne(() => (Array.isArray(b.value.books) ? b.value.books : []).map((U) => {
    const re = or(U);
    return {
      name: String(re.name || "").trim(),
      globalActive: re.globalActive === !0
    };
  }).filter((U) => U.name)), Dn = ne(() => Ht.value.find((U) => U.name === C.value) || null), br = ne(() => {
    const U = J.value;
    return !U || U.name !== C.value ? 0 : Math.max(0, U.entryCount - U.entries.length);
  }), fr = ne(() => R.value ? iu(R.value) !== V.value : !1), ct = ne(() => ve.value === "正在保存"), Ke = ne(() => (Array.isArray(ie.value.groups) ? ie.value.groups : []).map((U) => {
    const re = or(U), Ee = Array.isArray(re.scripts) ? re.scripts.map((Le) => or(Le)) : [];
    return {
      key: String(re.key || ""),
      label: String(re.label || re.key || ""),
      scriptType: Number(re.scriptType),
      scripts: Ee,
      allowed: re.allowed === !0
    };
  }).filter((U) => U.key && Number.isFinite(U.scriptType))), it = ne(() => Ke.value.flatMap((U) => U.scripts.map((re, Ee) => {
    const Le = {
      key: `${U.scriptType}:${String(re.id || Ee)}`,
      groupKey: U.key,
      groupLabel: U.label,
      scriptType: U.scriptType,
      script: re
    };
    return Le.searchCorpus = Xo(B_([
      Le.groupLabel,
      Le.script.scriptName,
      Le.script.findRegex,
      Le.script.replaceString,
      Le.script.trimStrings,
      Le.script.placement,
      Le.key
    ], 1200)), Le;
  })));
  function En(U = "") {
    const re = Number(Z.value[U]);
    return Number.isFinite(re) && re > 0 ? Math.floor(re) : F_;
  }
  function On(U = "") {
    const re = En(U);
    Z.value = {
      ...Z.value,
      [U]: re + F_
    };
  }
  const Gs = ne(() => {
    const U = Xo(ae.value), re = String(me.value || "").trim();
    return Ke.value.map((Ee) => {
      const Le = it.value.filter((Yt) => Yt.groupKey === Ee.key), dt = U ? Le.filter((Yt) => String(Yt.searchCorpus || "").includes(U)) : Le, _t = En(Ee.key), zt = dt.slice(0, _t);
      if (re && !zt.some((Yt) => Yt.key === re)) {
        const Yt = Le.find((Nr) => Nr.key === re);
        Yt && zt.unshift(Yt);
      }
      return {
        ...Ee,
        visibleRows: zt,
        totalCount: Le.length,
        filteredCount: dt.length,
        hiddenCount: Math.max(0, dt.length - Math.min(dt.length, _t))
      };
    }).filter((Ee) => Ee.filteredCount || !U);
  }), Gr = ne(() => it.value.find((U) => U.key === me.value) || null), rr = ne(() => iu(Se.value) !== W.value), Vn = ne(() => [
    {
      key: "characters",
      label: "角色卡",
      mobileLabel: "角色卡"
    },
    {
      key: "worldbooks",
      label: "世界书",
      mobileLabel: "世界书"
    },
    {
      key: "chatPreset",
      label: "聊天预设",
      mobileLabel: "聊天预设",
      badge: at.value ? "未保存" : ""
    },
    {
      key: "assistantPreset",
      label: "助手预设",
      mobileLabel: "助手预设",
      badge: se.value ? "未保存" : ""
    },
    {
      key: "regex",
      label: "正则",
      badge: rr.value ? "未保存" : ""
    },
    {
      key: "api",
      label: "API 配置",
      mobileLabel: "API"
    },
    {
      key: "base",
      label: "基础设定",
      mobileLabel: "基础设定"
    }
  ]), Ms = ne(() => Q.value.find((U) => U.id === de.value) || null), mn = ne(() => Vge.map((U) => ({
    id: U.key,
    key: U.key,
    label: U.label,
    summary: U.summary,
    content: String(S.value[U.key] || "")
  }))), Fn = ne(() => {
    const U = Xo(Ne.value);
    return U ? I.value.filter((re) => Xo(B_([
      re.name,
      re.description,
      re.id,
      re.preset?.name,
      re.preset?.description
    ], 1200)).includes(U)) : I.value;
  }), Ps = ne(() => {
    const U = Fn.value.slice(0, ce.value), re = String(E.value || S.value.id || "").trim();
    if (!re || U.some((Le) => Le.id === re)) return U;
    const Ee = I.value.find((Le) => Le.id === re);
    return Ee ? [Ee, ...U] : U;
  }), Ot = ne(() => Math.max(0, Fn.value.length - Math.min(Fn.value.length, ce.value))), wn = ne(() => I.value.find((U) => U.id === E.value) || null), sn = ne(() => mn.value.find((U) => U.id === _.value) || mn.value[0] || null), Wt = ne(() => !!Ft()), Gt = ne(() => {
    const U = j(), re = new Map(U.map((_t, zt) => [String(_t.identifier || _t.id || `prompt-${zt + 1}`).trim(), _t])), Ee = qe(), Le = /* @__PURE__ */ new Set(), dt = [];
    return Ee.forEach((_t) => {
      const zt = String(_t.identifier || "").trim();
      if (!zt || Le.has(zt)) return;
      const Yt = re.get(zt) || {
        identifier: zt,
        name: zt
      };
      Le.add(zt), dt.push({
        identifier: zt,
        name: String(Yt.name || Yt.label || zt),
        role: String(Yt.role || "system"),
        content: String(Yt.content || ""),
        enabled: _t.enabled !== !1,
        marker: Yt.marker === !0,
        systemPrompt: Yt.system_prompt === !0,
        injectionPosition: Number(Yt.injection_position ?? 0),
        injectionDepth: Number.isFinite(Number(Yt.injection_depth)) ? Number(Yt.injection_depth) : "",
        source: Yt.extension ? "扩展" : Yt.system_prompt ? "系统" : "预设",
        orderEntry: _t,
        prompt: Yt,
        listed: !0,
        searchCorpus: Xo(B_([
          Yt.name || Yt.label || zt,
          Yt.role || "system",
          Yt.extension ? "扩展" : Yt.system_prompt ? "系统" : "预设",
          zt,
          Yt.content || ""
        ]))
      });
    }), dt;
  }), Rn = ne(() => {
    const U = Xo(we.value);
    return U ? Gt.value.filter((re) => String(re.searchCorpus || "").includes(U)) : Gt.value;
  }), da = ne(() => {
    const U = Rn.value.slice(0, Oe.value), re = String(y.value || "").trim();
    if (!re || U.some((Le) => Le.identifier === re)) return U;
    const Ee = Gt.value.find((Le) => Le.identifier === re);
    return Ee ? [Ee, ...U] : U;
  }), fa = ne(() => Math.max(0, Rn.value.length - Math.min(Rn.value.length, Oe.value))), bt = ne(() => new Map(Gt.value.map((U, re) => [U.identifier, re]))), Ye = ne(() => Gt.value.find((U) => U.identifier === y.value) || Gt.value[0] || null), Pe = ne(() => Ft() ? "当前角色顺序" : "未读取到当前角色顺序"), Xt = ne(() => (i.value.sections || []).map((U, re) => ({
    ...U,
    previewId: U.id || `chat-preset-section-${re}`,
    previewLabel: U.label || U.source || `提示词 ${re + 1}`,
    previewPlacement: U.source === "promptManager" ? U.marker ? "酒馆标记" : "酒馆顺序" : Jge[U.placement || "beforeHistory"] || U.placement || "历史前",
    sectionIndex: re,
    chars: String(U.content || "").length
  })).filter((U) => (U.content || U.marker) && U.enabled !== !1)), Rr = ne(() => Xt.value.reduce((U, re) => U + re.chars, 0));
  function ns(U) {
    if (!U) return "";
    const re = U.uid !== void 0 && U.uid !== null && String(U.uid).trim() ? String(U.uid).trim() : U.order !== void 0 && U.order !== null && String(U.order).trim() ? String(U.order).trim() : "name" in U ? String(U.name || "").trim() : "";
    return `${String(C.value || "").trim()}:${re}`;
  }
  function ha(U) {
    return !!le.value && le.value === ns(U);
  }
  function St(U) {
    return bt.value.get(U) ?? -1;
  }
  function D() {
    return or(i.value.promptManager);
  }
  function Y() {
    return or(D().rawPreset);
  }
  function j() {
    const U = Y(), re = D();
    return (Array.isArray(U.prompts) ? U.prompts : Array.isArray(re.prompts) ? re.prompts : []).map((Ee) => or(Ee)).filter((Ee) => String(Ee.identifier || Ee.id || "").trim());
  }
  function We() {
    const U = Y(), re = D();
    return (Array.isArray(U.prompt_order) ? U.prompt_order : Array.isArray(re.promptOrder) ? re.promptOrder : []).map((Ee) => or(Ee));
  }
  function qe() {
    const U = D(), re = Array.isArray(U.activeOrder) ? U.activeOrder.map((dt) => or(dt)) : [];
    if (re.length) return re;
    const Ee = String(U.activeCharacterId ?? "").trim();
    if (!Ee) return [];
    const Le = We().find((dt) => String(dt.character_id ?? "") === Ee);
    return Array.isArray(Le?.order) ? Le.order.map((dt) => or(dt)) : [];
  }
  function Ft() {
    return String(D().activeCharacterId ?? "").trim();
  }
  function tn(U = {}, re = {}) {
    const Ee = Dc(U);
    u.value = Ee, v.value = Lt(Ee), w.value = String(Ee.promptManager?.name || Ee.name || "").trim(), re.replaceDraft !== !1 && (i.value = Ee);
  }
  function Bt() {
    const U = String(e.currentNativeCharacterId.value || "").trim();
    U && e.postToHost("xb-tavern:refresh-context", {
      nativeCharacterId: U,
      includeHistory: !1
    });
  }
  function an(U) {
    if (!U) {
      Se.value = {}, W.value = iu({}), me.value = "";
      return;
    }
    const re = q_(U.script);
    Se.value = re, W.value = iu(re), me.value = U.key;
  }
  function Bn(U = {}, re = {}) {
    const Ee = uw(U);
    x.value = Ee, p.value = en(Ee), re.replaceDraft !== !1 && (S.value = Ee);
  }
  function dn(U, re = "继续？") {
    return e.confirmDialog({
      title: "放弃未保存修改",
      message: `当前${U}有未保存修改，${re}会放弃这些草稿。继续？`,
      confirmText: re,
      tone: "warning"
    });
  }
  function Jn() {
    oe !== null && (clearTimeout(oe), oe = null), m.value = {
      status: "idle",
      error: ""
    };
  }
  function sr() {
    De !== null && (clearTimeout(De), De = null), $.value = {
      status: "idle",
      error: ""
    };
  }
  function Un() {
    Jn(), m.value = {
      status: "saving",
      error: ""
    };
  }
  function wr() {
    sr(), $.value = {
      status: "saving",
      error: ""
    };
  }
  function qn(U) {
    oe !== null && clearTimeout(oe);
    const re = U.ok ? "success" : "error";
    m.value = {
      status: re,
      error: U.error || ""
    }, oe = setTimeout(() => {
      m.value.status === re && (m.value = {
        status: "idle",
        error: ""
      }, oe = null);
    }, Cx), oe.unref?.();
  }
  function vn(U) {
    De !== null && clearTimeout(De);
    const re = U.ok ? "success" : "error";
    $.value = {
      status: re,
      error: U.error || ""
    }, De = setTimeout(() => {
      $.value.status === re && ($.value = {
        status: "idle",
        error: ""
      }, De = null);
    }, Cx), De.unref?.();
  }
  async function Sr() {
    const U = typeof AbortController == "function" ? new AbortController() : null;
    let re = null;
    const Ee = new Promise((Le, dt) => {
      re = setTimeout(() => {
        dt(/* @__PURE__ */ new Error("保存超时，请重试")), U?.abort();
      }, tve), re.unref?.();
    });
    try {
      return await Promise.race([e.requestHost("xb-tavern:save-chat-preset", { payload: i.value }, U ? { signal: U.signal } : {}), Ee]);
    } finally {
      re !== null && clearTimeout(re);
    }
  }
  function yn(U) {
    const re = Tx(U);
    R.value = re, V.value = iu(re), le.value = ns(re);
  }
  async function rs() {
    if (se.value && !await dn("助手预设", "刷新")) {
      N.value = "";
      return;
    }
    const [U, re, Ee] = await Promise.all([
      Zl(),
      zm(),
      Qy()
    ]);
    I.value = U, E.value = re || Ee.id, Bn(Ee, { replaceDraft: !se.value });
  }
  async function Lo() {
    f.value = "正在同步";
    try {
      const U = await e.requestHost("xb-tavern:list-chat-presets"), re = U.result || U;
      d.value = re, tn(re.active, { replaceDraft: !at.value }), f.value = "";
    } catch (U) {
      f.value = U instanceof Error ? U.message : String(U || "读取失败");
    }
  }
  async function nh() {
    const U = await e.requestHost("xb-tavern:get-chat-preset");
    return tn(U.result || U, { replaceDraft: !at.value }), f.value = "", Dc(u.value);
  }
  async function yc(U = w.value) {
    const re = String(U || "").trim(), Ee = String(i.value.promptManager?.name || i.value.name || "").trim();
    if (Jn(), !re) {
      w.value = Ee;
      return;
    }
    if (re === Ee) {
      w.value = Ee;
      return;
    }
    if (at.value && !await dn("聊天预设", "切换")) {
      w.value = Ee;
      return;
    }
    f.value = "正在切换";
    try {
      const Le = await e.requestHost("xb-tavern:select-chat-preset", { payload: { promptManagerName: re } });
      tn(Le.result || Le), f.value = "", Bt();
    } catch (Le) {
      w.value = Ee, f.value = Le instanceof Error ? Le.message : String(Le || "切换失败");
    }
  }
  async function Ci() {
    if (m.value.status !== "saving") {
      if (!Wt.value) {
        qn({
          ok: !1,
          error: "未读取到当前角色顺序，请刷新后再保存"
        });
        return;
      }
      if (at.value) {
        Un();
        try {
          const U = await Sr();
          if (U.ok === !1) {
            qn({
              ok: !1,
              error: String(U.error || "保存失败")
            });
            return;
          }
          tn(U.result), f.value = "", qn({ ok: !0 }), Bt();
        } catch (U) {
          qn({
            ok: !1,
            error: U instanceof Error ? U.message : String(U || "保存失败")
          });
        }
      }
    }
  }
  async function Ka(U = {}) {
    const re = U.requestSerial || ++lt;
    if (re === lt) {
      k.value = "正在同步";
      try {
        const Ee = await e.requestHost("xb-tavern:list-worldbook-sources", { payload: { context: e.effectiveContext.value } });
        if (re !== lt) return;
        b.value = Ee.result || Ee;
        const Le = String(C.value || "").trim(), dt = !!Le && Ht.value.some((Nr) => Nr.name === Le), _t = String(U.preferredName || "").trim(), zt = !!_t && Ht.value.some((Nr) => Nr.name === _t), Yt = U.selectFirst && (Ht.value.find((Nr) => Nr.globalActive)?.name || Ht.value[0]?.name) || "";
        C.value = zt ? _t : U.keepSelection && dt ? C.value : Yt, k.value = "", xi(C.value);
      } catch (Ee) {
        if (re !== lt) return;
        k.value = Ee instanceof Error ? Ee.message : String(Ee || "读取失败");
      }
    }
  }
  async function Ii() {
    const U = ++ot;
    G.value = "正在同步";
    try {
      const re = await e.requestHost("xb-tavern:get-global-worldbooks");
      if (U !== ot) return;
      const Ee = Ex(re.result || re);
      pt && pt <= U && (pt = 0, ee.value = !1), P.value = Ee.options, H.value = Ee.selected, G.value = "";
    } catch (re) {
      if (U !== ot) return;
      pt && pt <= U && (pt = 0, ee.value = !1), G.value = re instanceof Error ? re.message : String(re || "读取失败");
    }
  }
  async function rh(U = H.value) {
    const re = Array.from(new Set(U.map((Le) => String(Le || "").trim()).filter(Boolean))), Ee = ++ot;
    pt = Ee, H.value = re, ee.value = !0, G.value = "正在保存";
    try {
      const Le = await e.requestHost("xb-tavern:set-global-worldbooks", { payload: { selected: re } });
      if (Ee !== ot) return;
      const dt = Ex(Le.result || Le);
      pt === Ee && (pt = 0, ee.value = !1), P.value = dt.options, H.value = dt.selected, G.value = "", Bt(), Ka({ keepSelection: !0 });
    } catch (Le) {
      if (Ee !== ot) return;
      pt === Ee && (pt = 0, ee.value = !1), G.value = Le instanceof Error ? Le.message : String(Le || "保存失败"), Ii();
    }
  }
  function Rv(U, re) {
    const Ee = String(U || "").trim();
    if (!Ee || ee.value) return;
    const Le = new Set(H.value);
    re ? Le.add(Ee) : Le.delete(Ee), rh([...Le]);
  }
  async function xi(U = C.value) {
    const re = String(U || "").trim();
    if (!re) {
      J.value = null, z.value = "";
      return;
    }
    const Ee = re;
    z.value = "正在读取预览";
    try {
      const Le = await e.requestHost("xb-tavern:get-worldbook-preview", { payload: {
        name: Ee,
        limit: F.value
      } });
      if (String(C.value || "").trim() !== Ee) return;
      J.value = jge(Le.result || Le), z.value = "";
    } catch (Le) {
      if (String(C.value || "").trim() !== Ee) return;
      J.value = null, z.value = Le instanceof Error ? Le.message : String(Le || "预览读取失败");
    }
  }
  function Do() {
    C.value && (F.value += kp, xi(C.value));
  }
  async function sh(U) {
    const re = String(C.value || "").trim(), Ee = U?.uid === void 0 || U.uid === null ? "" : String(U.uid).trim();
    if (!re || !Ee) return;
    const Le = ns(U);
    if (fr.value && le.value !== Le && !await dn("世界书条目", "切换")) return;
    le.value = Le, Je += 1;
    const dt = `${Le}:${Je}`;
    st = dt, ve.value = "正在读取条目";
    try {
      const _t = await e.requestHost("xb-tavern:get-worldbook-entry", { payload: {
        name: re,
        uid: Ee
      } });
      if (String(C.value || "").trim() !== re || le.value !== Le || st !== dt) return;
      yn(_t.result || _t), ve.value = "";
    } catch (_t) {
      if (le.value !== Le || st !== dt) return;
      ve.value = _t instanceof Error ? _t.message : String(_t || "条目读取失败");
    }
  }
  function Ul() {
    R.value = null, V.value = "", le.value = "", st = "", ve.value = "";
  }
  function Rs(U) {
    R.value && (R.value = Tx({
      ...R.value,
      ...U
    }), ve.value && ve.value !== "正在保存" && (ve.value = ""));
  }
  async function ah() {
    const U = R.value, re = String(C.value || U?.worldbookName || "").trim();
    if (!(!U || !re || !U.uid) && fr.value) {
      ve.value = "正在保存";
      try {
        const Ee = await e.requestHost("xb-tavern:save-worldbook-entry", { payload: {
          name: re,
          uid: U.uid,
          entryHash: U.entryHash,
          draft: U
        } });
        yn(Ee.result || Ee), ve.value = "已保存", await xi(re), Bt();
      } catch (Ee) {
        ve.value = Ee instanceof Error ? Ee.message : String(Ee || "保存失败");
      }
    }
  }
  async function _c() {
    if (rr.value && !await dn("正则", "刷新")) {
      L.value = "";
      return;
    }
    L.value = "正在读取";
    try {
      const U = await e.requestHost("xb-tavern:list-regex-scripts", { payload: { nativeCharacterId: String(e.currentNativeCharacterId.value || "").trim() } });
      ie.value = U.result || U, an(it.value.find((re) => re.key === me.value) || it.value[0] || null), L.value = "";
    } catch (U) {
      L.value = U instanceof Error ? U.message : String(U || "读取失败");
    }
  }
  async function ki(U) {
    return rr.value && !await dn("正则", "切换") ? !1 : (an(U), !0);
  }
  async function Nv(U) {
    return rr.value && !await dn("正则", "新建") ? !1 : (Se.value = q_({
      scriptName: "新正则",
      findRegex: "",
      replaceString: "",
      trimStrings: [],
      placement: [],
      disabled: !1,
      markdownOnly: !1,
      promptOnly: !1,
      runOnEdit: !1,
      substituteRegex: 0,
      minDepth: null,
      maxDepth: null
    }), W.value = "", me.value = `${U.scriptType}:new`, !0);
  }
  function oh(U) {
    Se.value = q_({
      ...Se.value,
      ...U
    });
  }
  function $v(U, re) {
    const Ee = new Set((Se.value.placement || []).map((Le) => Number(Le)));
    re ? Ee.add(U) : Ee.delete(U), oh({ placement: [...Ee] });
  }
  async function Lv() {
    const U = Gr.value?.scriptType || Number(me.value.split(":")[0]);
    if (Number.isFinite(U) && rr.value) {
      L.value = "正在保存";
      try {
        const re = await e.requestHost("xb-tavern:save-regex-script", { payload: {
          nativeCharacterId: String(e.currentNativeCharacterId.value || "").trim(),
          scriptType: U,
          script: Se.value
        } }), Ee = re.result || re;
        ie.value = Ee;
        const Le = String(Ee.savedScriptId || Se.value.id || ""), dt = Number(Ee.savedScriptType ?? U);
        an(it.value.find((_t) => _t.scriptType === dt && Le && _t.script.id === Le) || it.value.find((_t) => _t.script.scriptName === Se.value.scriptName) || it.value[0] || null), L.value = "";
      } catch (re) {
        L.value = re instanceof Error ? re.message : String(re || "保存失败");
      }
    }
  }
  async function Dv() {
    const U = Gr.value;
    if (U) {
      await bc(U);
      return;
    }
    const re = String(Se.value.id || ""), Ee = Number(me.value.split(":")[0]);
    !re || !Number.isFinite(Ee) || await bc({
      key: `${Ee}:${re}`,
      groupKey: "",
      groupLabel: "",
      scriptType: Ee,
      script: Se.value
    });
  }
  async function bc(U) {
    if (rr.value && me.value !== U.key && !await dn("正则", "删除")) return;
    const re = String(U?.script.id || ""), Ee = U?.scriptType;
    if (!(!re || !Number.isFinite(Ee)) && await e.confirmDialog({
      title: "删除正则脚本",
      message: "删除这个正则脚本？",
      confirmText: "删除",
      tone: "danger"
    })) {
      L.value = "正在删除";
      try {
        const Le = await e.requestHost("xb-tavern:delete-regex-script", { payload: {
          nativeCharacterId: String(e.currentNativeCharacterId.value || "").trim(),
          scriptType: Ee,
          id: re
        } });
        ie.value = Le.result || Le, an(it.value[0] || null), L.value = "";
      } catch (Le) {
        L.value = Le instanceof Error ? Le.message : String(Le || "删除失败");
      }
    }
  }
  function ih() {
    return yu(e.tavernDisplaySettings.value);
  }
  function Wa(U = {}) {
    const re = ih();
    he.value = re, U.updateCommitted !== !1 && (q.value = ou(re));
  }
  function lh(U) {
    he.value = U, e.tavernDisplaySettings.value = yu(U);
  }
  async function Ga(U) {
    const re = ++ye;
    pe.value = !0, ge.value = "正在保存显示设置", lh(U);
    try {
      if (await e.requestHost("xb-tavern:save-display-settings", { payload: U }), re !== ye) return;
      q.value = ou(ih()), ge.value = "显示设置已保存";
    } catch (Ee) {
      if (re !== ye) return;
      lh(ou(q.value)), ge.value = Ee instanceof Error ? Ee.message : String(Ee || "保存失败");
    } finally {
      re === ye && (pe.value = !1);
    }
  }
  function Va(U) {
    Ga(yu({
      ...he.value,
      ...U
    }));
  }
  function Ar(U) {
    const re = Number(he.value.hiddenOutsideCount) || 5, Ee = Math.min(20, Math.max(1, re + U));
    Ee !== re && Va({ hiddenOutsideCount: Ee });
  }
  function Ov(U) {
    const re = Number(he.value.loadBatchSize) || 20, Ee = Math.min(50, Math.max(5, re + U * 5));
    Ee !== re && Va({ loadBatchSize: Ee });
  }
  function Mi(U) {
    const re = Zge(U);
    Q.value = re.users, de.value = re.currentUserId;
  }
  async function ql() {
    Me.value = !0, Ie.value || (ge.value = "正在读取 USER 列表");
    try {
      const U = await e.requestHost("xb-tavern:list-users");
      Mi(U.result || U), Ie.value || (ge.value = "");
    } catch (U) {
      ge.value = U instanceof Error ? U.message : String(U || "USER 列表读取失败");
    } finally {
      Me.value = !1;
    }
  }
  async function wc(U) {
    const re = String(U || "").trim();
    if (!(!re || re === de.value || Ie.value)) {
      Ie.value = re, ge.value = "正在切换 USER";
      try {
        const Ee = await e.requestHost("xb-tavern:switch-user", { payload: { userId: re } });
        Mi(Ee.result || Ee), ge.value = Ms.value ? `已切换到 ${Ms.value.name}` : "USER 已切换", Bt();
      } catch (Ee) {
        ge.value = Ee instanceof Error ? Ee.message : String(Ee || "切换失败");
      } finally {
        Ie.value = "";
      }
    }
  }
  async function Fv() {
    at.value && (i.value = Dc(u.value), v.value = Lt(u.value), f.value = "", Jn());
  }
  function Bv(U, re) {
    i.value = Dc({
      ...i.value,
      [U]: {
        ...i.value[U] || {},
        ...re
      }
    });
  }
  function uh(U, re = {}) {
    Bv("promptManager", {
      rawPreset: U,
      prompts: Array.isArray(U.prompts) ? U.prompts : [],
      promptOrder: Array.isArray(U.prompt_order) ? U.prompt_order : [],
      ...re
    });
  }
  function Oo(U, re) {
    const Ee = String(U || "").trim();
    if (!Ee) return;
    const Le = ou(Y()), dt = j().map((zt) => ({ ...zt })), _t = dt.findIndex((zt) => String(zt.identifier || zt.id || "").trim() === Ee);
    _t >= 0 ? dt[_t] = {
      ...dt[_t],
      ...re,
      identifier: Ee
    } : dt.push({
      identifier: Ee,
      name: Ee,
      role: "system",
      content: "",
      ...re
    }), Le.prompts = dt, uh(Le);
  }
  function Sc(U) {
    const re = ou(Y()), Ee = D(), Le = We().map((zt) => ({ ...zt })), dt = String(Ee.activeCharacterId ?? "").trim();
    if (!dt) {
      f.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    let _t = Le.findIndex((zt) => String(zt.character_id ?? "") === dt);
    _t < 0 && (_t = Le.length, Le.push({
      character_id: dt,
      order: []
    })), Le[_t] = {
      ...Le[_t],
      order: U
    }, re.prompt_order = Le, uh(re, { activeOrder: U });
  }
  function Hl(U = Gt.value) {
    return U.filter((re) => re.listed).map((re) => ({
      ...re.orderEntry,
      identifier: re.identifier,
      enabled: re.enabled
    }));
  }
  function Yn(U, re) {
    const Ee = String(U || "").trim();
    if (!Wt.value) {
      f.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    if (!Ee) return;
    const Le = Gt.value;
    Sc(Hl(Le).map((dt) => String(dt.identifier || "").trim() === Ee ? {
      ...dt,
      ...re,
      identifier: Ee
    } : dt));
  }
  function ch(U, re) {
    if (!Wt.value) {
      f.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    const Ee = Gt.value, Le = Ee.findIndex((Yt) => Yt.identifier === U), dt = Le + re;
    if (Le < 0 || dt < 0 || dt >= Ee.length) return;
    const _t = Ee.slice(), [zt] = _t.splice(Le, 1);
    zt && (_t.splice(dt, 0, zt), Sc(Hl(_t)), y.value = U);
  }
  function Uv(U, re) {
    Yn(U, { enabled: re });
  }
  function dh(U = "") {
    const re = {
      system: "系统",
      user: "用户",
      assistant: "助手"
    }, Ee = String(U || "system").trim();
    return re[Ee] || Ee || "系统";
  }
  function fh(U) {
    return Array.isArray(U) ? U.map((re) => String(re || "").trim()).filter(Boolean).join(`
`) : String(U || "");
  }
  function zl(U = "") {
    return String(U || "").split(/\r?\n/).map((re) => re.trim()).filter(Boolean);
  }
  function Ac(U) {
    return {
      1: "用户输入",
      2: "AI 回复",
      3: "斜杠命令",
      5: "世界书",
      6: "推理内容"
    }[U] || String(U);
  }
  function Pi(U) {
    return Ke.value.find((re) => re.scriptType === U);
  }
  function Ri() {
    return Pi(Gr.value?.scriptType || Number(me.value.split(":")[0]))?.label || "正则";
  }
  function Vs(U) {
    S.value = {
      ...S.value,
      ...U
    };
  }
  function Kl(U) {
    _.value = U;
  }
  function Tc(U = "") {
    const re = sn.value;
    re && Vs({ [re.key]: String(U || "") });
  }
  async function hh(U) {
    const re = String(U || "").trim();
    !re || re === E.value || (sr(), !(se.value && !await dn("助手预设", "切换")) && (await du(re), E.value = re, Bn(await Qy()), N.value = ""));
  }
  async function ph() {
    if ($.value.status !== "saving" && se.value) {
      wr();
      try {
        const U = await pd(String(wn.value?.id || "") === "littlewhitebox-assistant-default" ? {
          ...S.value,
          id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
          name: `${S.value.name || "助手预设"} 自定义`
        } : S.value);
        await du(U.id), E.value = U.id, Bn(U.preset), I.value = await Zl(), N.value = "", vn({ ok: !0 });
      } catch (U) {
        vn({
          ok: !1,
          error: U instanceof Error ? U.message : String(U || "保存失败")
        });
      }
    }
  }
  async function mh() {
    const U = await pd({
      ...S.value,
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: `${S.value.name || "助手预设"} 副本`
    });
    await du(U.id), E.value = U.id, Bn(U.preset), I.value = await Zl(), N.value = "";
  }
  async function Ja() {
    if (se.value && !await dn("助手预设", "新建")) return;
    const U = await pd({
      ...yf(),
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: "新助手预设",
      description: ""
    });
    await du(U.id), E.value = U.id, Bn(U.preset), I.value = await Zl(), N.value = "";
  }
  async function pa(U) {
    if (se.value && !await dn("助手预设", "导入")) return !1;
    const re = U && typeof U == "object" ? U : {}, Ee = re.preset && typeof re.preset == "object" ? re.preset : re, Le = await pd({
      ...Ee,
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: String(Ee.name || re.name || "导入助手预设").trim() || "导入助手预设",
      description: String(Ee.description || re.description || "").trim()
    });
    return await du(Le.id), E.value = Le.id, Bn(Le.preset), I.value = await Zl(), N.value = "", !0;
  }
  async function Wl() {
    const U = String(E.value || S.value.id || "").trim(), re = I.value.find((Ee) => Ee.id === U) || null;
    !re || re.id === "littlewhitebox-assistant-default" || se.value && !await dn("助手预设", "删除") || await e.confirmDialog({
      title: "删除助手预设",
      message: `删除「${re.name || "当前助手预设"}」？`,
      confirmText: "删除",
      tone: "danger"
    }) && (await jU(re.id), I.value = await Zl(), E.value = await zm(), Bn(await Qy()), N.value = "");
  }
  async function Fo() {
    se.value && (S.value = { ...x.value }, p.value = en(x.value), N.value = "", sr());
  }
  function ma() {
    ht.config = ef(e.agentConfig.value || {}), ht.configDraft = null, ht.configFormSyncPending = !0;
  }
  function ga(U = "") {
    o !== null && (window.clearTimeout(o), o = null), l !== null && (window.clearTimeout(l), l = null), s.value = {
      status: "saving",
      requestId: U,
      error: ""
    }, ht.configSave = s.value, a.value = "正在保存共享 API 配置...", o = window.setTimeout(() => {
      o = null, !(s.value.requestId !== U || s.value.status !== "saving") && (s.value = {
        status: "error",
        requestId: U,
        error: "保存超时，请重试"
      }, ht.configSave = s.value, a.value = "保存失败：保存超时，请重试", l = window.setTimeout(() => {
        s.value.requestId !== U || s.value.status !== "error" || (s.value = {
          status: "idle",
          requestId: "",
          error: ""
        }, ht.configSave = s.value, a.value = "", Ut(va));
      }, 1800), Ut(va));
    }, eve), Ut(va);
  }
  function Ni(U = "", re = {}) {
    if (U && s.value.requestId && U !== s.value.requestId) return;
    o !== null && (window.clearTimeout(o), o = null), l !== null && (window.clearTimeout(l), l = null), s.value = {
      status: re.ok ? "success" : "error",
      requestId: U,
      error: re.error || ""
    }, ht.configSave = s.value, a.value = re.ok ? "" : `保存失败：${re.error || "unknown_error"}`;
    const Ee = s.value.status;
    l = window.setTimeout(() => {
      s.value.requestId !== U || s.value.status !== Ee || (s.value = {
        status: "idle",
        requestId: "",
        error: ""
      }, ht.configSave = s.value, a.value = "", Ut(va));
    }, 1800), Ut(va);
  }
  function Gl(U) {
    const re = String(U.requestId || `save-config-${Date.now()}`);
    ga(re), e.postToHost("xb-tavern:save-config", {
      requestId: re,
      payload: U.payload || {}
    });
  }
  function va() {
    const U = r.value;
    U && (It || (It = zge({
      state: ht,
      render: va,
      describeError: e.describeError,
      showToast: (re) => {
        a.value = String(re || "");
      },
      saveConfig: Gl,
      getRuntimeSummaryText: () => je.value
    })), ht.configSave = s.value, U.innerHTML = Gge({
      configSave: s.value,
      runtimeText: je.value,
      inlineToastText: a.value,
      showAssistantPermissions: !1,
      showDelegateSettings: !0,
      activePage: String(ht.configPage || "main"),
      delegatePresetHint: "记忆整理会复用这里的分身 API；当前聊天仍使用主 API。",
      isBusy: e.isRunning.value,
      canDeletePreset: Object.keys(ht.config?.presets || {}).length > 1
    }), It.syncConfigToForm(U), It.bindSettingsPanelEvents(U));
  }
  function qv(U) {
    if (U.ok === !0) {
      e.agentConfig.value = U.config || e.agentConfig.value, ma(), Ni(String(U.requestId || ""), { ok: !0 });
      return;
    }
    ma(), Ni(String(U.requestId || ""), {
      ok: !1,
      error: String(U.error || "保存失败")
    });
  }
  function Hv(U) {
    "chatPreset" in U && tn(U.chatPreset, { replaceDraft: !at.value }), "chatPresetList" in U && (d.value = U.chatPresetList || {});
  }
  function $i(U) {
    e.activeSettingsWorkspace.value = U, e.activeView.value = "settings";
  }
  function gh(U = "") {
    C.value = String(U || "").trim(), $i("worldbooks");
  }
  async function zv() {
    const U = ++lt, re = String(e.currentNativeCharacterId.value || "").trim();
    let Ee = "";
    if (C.value = "", !re) {
      await Ka({ requestSerial: U });
      return;
    }
    try {
      const Le = await e.requestHost("xb-tavern:get-character-worldbook-state", { payload: { nativeCharacterId: re } });
      if (U !== lt) return;
      const dt = Le.result || Le, _t = String(dt.boundWorldbookName || "").trim();
      if (_t && dt.boundExists === !0) {
        await Ka({
          preferredName: _t,
          requestSerial: U
        });
        return;
      }
    } catch (Le) {
      if (U !== lt) return;
      Ee = Le instanceof Error ? Le.message : String(Le || "绑定状态读取失败");
    }
    await Ka({ requestSerial: U }), Ee && U === lt && (k.value = Ee);
  }
  function vh(U) {
    const re = U;
    (re === "api" || re === "characters" || re === "chatPreset" || re === "worldbooks" || re === "regex" || re === "assistantPreset" || re === "base") && $i(re);
  }
  return vt(Gt, (U) => {
    if (!U.length) {
      y.value = "";
      return;
    }
    U.some((re) => re.identifier === y.value) || (y.value = U[0]?.identifier || "");
  }, { immediate: !0 }), vt(ke, () => {
    be.value = L_;
  }), vt(Ne, () => {
    ce.value = D_;
  }), vt(we, () => {
    Oe.value = O_;
  }), vt(ae, () => {
    Z.value = {};
  }), vt(() => e.tavernDisplaySettings.value, () => {
    Wa({ updateCommitted: !pe.value });
  }, { immediate: !0 }), vt(mn, (U) => {
    if (!U.length) {
      _.value = "";
      return;
    }
    U.some((re) => re.id === _.value) || (_.value = U[0]?.id || "");
  }, { immediate: !0 }), vt([
    () => e.activeSettingsWorkspace.value,
    () => e.activeView.value,
    () => String(e.currentNativeCharacterId.value || "").trim(),
    () => s.value.status,
    () => e.agentConfig.value,
    () => r.value
  ], ([U, re, Ee], [Le, dt, _t]) => {
    r.value && Ut(va);
    const zt = re === "settings" && U === "regex";
    zt && (zt && (dt !== re || Le !== U) || zt && Ee !== _t || !Ke.value.length) && _c();
  }), vt([e.activeView, e.activeSettingsWorkspace], ([U, re], [Ee, Le]) => {
    U === "settings" && re === "chatPreset" && (Ee !== U || Le !== re) && Lo(), U === "settings" && re === "worldbooks" && (Ee !== U || Le !== re) && (Ka({ keepSelection: !0 }), Ii()), U === "settings" && re === "assistantPreset" && (Ee !== U || Le !== re) && rs(), U === "settings" && re === "base" && (Ee !== U || Le !== re) && ql();
  }), vt(C, (U) => {
    e.activeSettingsWorkspace.value === "worldbooks" && (Ul(), F.value = kp, xi(U));
  }), {
    activeAssistantPreset: x,
    activeChatPreset: u,
    apiReady: Re,
    apiReadyDetail: Be,
    apiRuntimeLine: je,
    apiSettingsRootRef: r,
    applyHostChatPreset: Hv,
    handleApiConfigSaved: qv,
    openSettingsWorkspace: $i,
    openWorldbookWorkspace: gh,
    loadTavernUsers: ql,
    refreshPresets: rs,
    refreshRegexFromHost: _c,
    refreshRuntimeChatPresetFromHost: nh,
    renderApiSettingsPanel: va,
    runtimeChatPreset: X,
    selectSettingsWorkspace: vh,
    settingsContext: {
      activeAssistantPresetId: E,
      activePromptOrderLabel: Pe,
      activeSettingsWorkspace: e.activeSettingsWorkspace,
      activeView: e.activeView,
      apiReady: Re,
      apiReadyDetail: Be,
      apiRuntimeLine: je,
      apiSettingsRootRef: r,
      applyActiveRegexScript: an,
      ASSISTANT_PRESET_BATCH_SIZE: D_,
      assistantPreset: S,
      assistantPresetDirty: se,
      assistantPresetItems: mn,
      assistantPresets: I,
      assistantPresetSearchText: Ne,
      assistantPresetSaveFeedback: $,
      assistantPresetStatus: N,
      assistantPresetVisibleLimit: ce,
      canEditPromptOrder: Wt,
      cancelWorldbookEntryEdit: Ul,
      chatPresetOptions: Qe,
      chatPresetSourceSearchText: ke,
      chatPresetSourceVisibleLimit: be,
      CHAT_PRESET_SOURCE_BATCH_SIZE: L_,
      createAssistantPreset: Ja,
      createRegexScript: Nv,
      deleteCurrentAssistantPreset: Wl,
      deleteCurrentRegexScript: Dv,
      deleteRegexScript: bc,
      deriveAssistantPreset: mh,
      discardAssistantPresetChanges: Fo,
      discardPresetChanges: Fv,
      expandRegexGroup: On,
      filteredPromptEditorRows: Rn,
      globalWorldbookOptions: P,
      globalWorldbookSelected: H,
      globalWorldbookSaving: ee,
      globalWorldbookStatus: G,
      hiddenAssistantPresetCount: Ot,
      hiddenChatPresetOptionCount: rn,
      hiddenPromptCount: fa,
      hiddenWorldbookPreviewEntryCount: br,
      homeThemeDark: e.homeThemeDark,
      importAssistantPreset: pa,
      isEditingWorldbookEntry: ha,
      loadTavernUsers: ql,
      linesFromList: fh,
      listFromLines: zl,
      movePromptRow: ch,
      postToHost: e.postToHost,
      preset: i,
      presetDirty: at,
      presetRows: Xt,
      presetSaveFeedback: m,
      presetStatus: f,
      presetTotalChars: Rr,
      PROMPT_EDITOR_BATCH_SIZE: O_,
      promptEditorRows: Gt,
      promptRoleDisplay: dh,
      promptRowIndex: St,
      promptSearchText: we,
      promptVisibleLimit: Oe,
      refreshPresets: rs,
      refreshRegexFromHost: _c,
      currentTavernUser: Ms,
      currentTavernUserId: de,
      displaySettings: he,
      REGEX_GROUP_BATCH_SIZE: F_,
      regexDirty: rr,
      regexDraft: Se,
      regexDraftTypeLabel: Ri,
      regexGroups: Ke,
      regexGroupsForDisplay: Gs,
      regexPlacementLabel: Ac,
      regexScriptRows: it,
      regexSearchText: ae,
      regexStatus: L,
      saveCurrentAssistantPreset: ph,
      saveCurrentPreset: Ci,
      saveCurrentRegexScript: Lv,
      saveGlobalWorldbooksToHost: rh,
      saveWorldbookEntryDraft: ah,
      selectAssistantPreset: hh,
      selectAssistantPresetItem: Kl,
      selectChatPresetFromHost: yc,
      selectedAssistantPresetItem: sn,
      selectedPresetSourceId: w,
      selectedPromptIdentifier: y,
      selectedPromptRow: Ye,
      selectedRegexKey: me,
      selectedRegexRow: Gr,
      selectedWorldbook: Dn,
      selectedWorldbookName: C,
      selectRegexScript: ki,
      selectSettingsWorkspace: vh,
      settingsNavItems: Vn,
      shortText: e.shortText,
      showMoreWorldbookPreviewEntries: Do,
      stepHiddenOutsideCount: Ar,
      stepLoadBatchSize: Ov,
      startWorldbookEntryEdit: sh,
      switchingTavernUserId: Ie,
      syncChatPresetFromHost: Lo,
      syncGlobalWorldbooksFromHost: Ii,
      syncWorldbooksForCurrentCharacter: zv,
      syncWorldbooksFromHost: Ka,
      switchTavernUser: wc,
      tavernUsers: Q,
      togglePromptRow: Uv,
      toggleGlobalWorldbook: Rv,
      toggleRegexPlacement: $v,
      updateAssistantPresetPatch: Vs,
      updateDisplaySettingsPatch: Va,
      updatePromptByIdentifier: Oo,
      updateRegexPatch: oh,
      updateSelectedAssistantPresetItem: Tc,
      updateWorldbookEntryDraftPatch: Rs,
      baseSettingsLoading: Me,
      baseSettingsSaving: pe,
      baseSettingsStatus: ge,
      visibleAssistantPresetRecords: Ps,
      visibleChatPresetOptions: wt,
      visiblePromptEditorRows: da,
      WORLDBOOK_PREVIEW_BATCH_SIZE: kp,
      worldbookEntryDirty: fr,
      worldbookEntryDraft: R,
      worldbookEntryEditingKey: le,
      worldbookEntrySaving: ct,
      worldbookEntryStatus: ve,
      worldbookOptions: Ht,
      worldbookPreview: J,
      worldbookPreviewStatus: z,
      worldbookPreviewVisibleLimit: F,
      worldbookStatus: k
    },
    syncApiSettingsConfigFromAgentConfig: ma,
    syncChatPresetFromHost: Lo,
    syncWorldbooksFromHost: Ka
  };
}
var sve = ["data-chat-font-size"], ave = { class: "xb-os-stage" }, ove = { class: "character-worldbook-picker" }, ive = {
  key: 0,
  class: "worldbook-picker-status"
}, lve = { class: "worldbook-picker-list" }, uve = ["disabled", "onClick"], cve = { class: "tavern-dialog-head" }, dve = { id: "tavern-dialog-title" }, fve = { class: "tavern-dialog-body" }, hve = { key: 0 }, pve = {
  key: 1,
  class: "tavern-dialog-field"
}, mve = ["placeholder"], gve = { class: "tavern-dialog-actions" }, Pp = 48, vve = 12, yve = 36, Ix = 48, H_ = 24, xx = 480, _ve = 200, bve = 1e9, kx = "LittleWhiteBox_Tavern_theme", wve = /* @__PURE__ */ bn({
  __name: "App",
  setup(e) {
    const t = /* @__PURE__ */ ue({}), n = /* @__PURE__ */ ue({}), r = /* @__PURE__ */ ue({}), s = /* @__PURE__ */ ue(yu({})), a = /* @__PURE__ */ ue(!0), o = ne(() => mo(s.value.hiddenOutsideCount)), l = ne(() => I1(s.value.loadBatchSize)), i = /* @__PURE__ */ ue({}), u = /* @__PURE__ */ ue("15px"), d = /* @__PURE__ */ ue("23px"), f = /* @__PURE__ */ ue([]), m = /* @__PURE__ */ ue(""), v = /* @__PURE__ */ ue(0), y = /* @__PURE__ */ ue(""), S = /* @__PURE__ */ ue(""), x = /* @__PURE__ */ ue(null), I = /* @__PURE__ */ ue(!1), E = /* @__PURE__ */ ue(""), N = /* @__PURE__ */ ue(!1), $ = /* @__PURE__ */ ue([]), p = /* @__PURE__ */ ue(0), w = /* @__PURE__ */ ue(""), _ = /* @__PURE__ */ ue(""), b = /* @__PURE__ */ ue("等待读取角色与会话"), C = /* @__PURE__ */ ue("raw"), k = ine(), { currentUserMessage: P, isCancellingRun: H, isRunning: G, runtimeActionCheckEvents: ee, runtimeError: J, runtimeFinalizedAssistantMessage: z, runtimeModel: R, runtimePendingUserMessage: V, runtimeProvider: le, runtimeText: ve, runtimeThoughts: ke, runtimeUserMessageVisible: be } = k, Ne = /* @__PURE__ */ ue(null), ce = bne(), { chatMessages: we, currentAssistantFloor: Oe, latestSessionMessage: F, loadedSessionMessageEndOrder: ae, loadedSessionMessageStartOrder: Z, loadedSessionMessages: ie, selectedSession: me, selectedSessionId: Se, selectedSessionLatestAssistantOrder: W, selectedSessionMessageTotal: L, sessionMessageCounts: Q, sessions: de, visibleChatMessages: ge } = ce, pe = /* @__PURE__ */ ue([]), Me = /* @__PURE__ */ ue([]), Ie = /* @__PURE__ */ ue([]), he = /* @__PURE__ */ ue(null), q = /* @__PURE__ */ ue(""), ye = /* @__PURE__ */ ue(null), oe = /* @__PURE__ */ ue(null), De = /* @__PURE__ */ ue(""), Je = /* @__PURE__ */ ue(""), st = /* @__PURE__ */ ue(""), lt = /* @__PURE__ */ ue("preview"), ot = /* @__PURE__ */ ue(""), pt = /* @__PURE__ */ ue("state"), ht = /* @__PURE__ */ ue([]), It = /* @__PURE__ */ ue("main"), Lt = /* @__PURE__ */ ue(null), en = /* @__PURE__ */ ue([]), at = /* @__PURE__ */ ue(null), X = /* @__PURE__ */ ue([]), se = /* @__PURE__ */ ue(""), Ae = /* @__PURE__ */ ue(""), Re = /* @__PURE__ */ ue(""), Be = /* @__PURE__ */ ue(""), je = /* @__PURE__ */ ue(""), Qe = /* @__PURE__ */ ue([]), ut = /* @__PURE__ */ ue(!1), wt = /* @__PURE__ */ ue(!1), rn = /* @__PURE__ */ ue(null), Ht = /* @__PURE__ */ ue(null), Dn = /* @__PURE__ */ ue(""), br = /* @__PURE__ */ ue(Pp), fr = /* @__PURE__ */ ue(""), ct = /* @__PURE__ */ ue({}), Ke = /* @__PURE__ */ ue({});
    function it() {
      const [A] = String(window.location.hash || "").replace(/^#\/?/, "").split("/");
      return A === "chat" || A === "settings" || A === "about" ? A : "home";
    }
    function En() {
      try {
        const A = globalThis.localStorage?.getItem(kx);
        if (A === "dark") return !0;
        if (A === "light") return !1;
      } catch {
      }
      return !1;
    }
    function On(A = "") {
      return String(A || "").trim().toLocaleLowerCase();
    }
    function Gs(A, M) {
      const K = Number.parseFloat(String(A || ""));
      return Number.isFinite(K) && K > 0 ? `${Math.round(K * 100) / 100}px` : M;
    }
    function Gr(A = u.value) {
      const M = Number.parseFloat(String(A || ""));
      return Number.isFinite(M) && M > 0 ? `${Math.round((M + 8) * 100) / 100}px` : "23px";
    }
    function rr(A, M = 1600) {
      return A.map((K) => String(K ?? "").slice(0, M)).filter(Boolean).join(`
`);
    }
    function Vn(A) {
      return Array.isArray(A) ? A.map((M) => String(M || "").trim()).filter(Boolean) : [];
    }
    const Ms = ne(() => ti(me.value?.contextSnapshot?.authorNote)), mn = ne(() => Xr(me.value?.state || {})), Fn = ne(() => Fs(mn.value.contract)), Ps = ne(() => !!(me.value && String(me.value.characterKey || "").trim())), Ot = /* @__PURE__ */ ue(it()), wn = /* @__PURE__ */ ue(nve()), sn = /* @__PURE__ */ ue(En()), Wt = /* @__PURE__ */ ue("chat"), Gt = /* @__PURE__ */ ue("balanced"), Rn = /* @__PURE__ */ ue(null), da = /* @__PURE__ */ ue(null), fa = /* @__PURE__ */ ue(null);
    let bt = () => !1;
    const Ye = J2({
      totalItems: () => L.value,
      defaultLimit: o,
      loadBatchSize: l,
      onReturnToBottom: () => bt()
    }), Pe = J2({
      totalItems: () => Zv.value.length,
      defaultLimit: o,
      loadBatchSize: l
    }), Xt = Ye.scrollRef, Rr = Pe.scrollRef, ns = Ye.autoScroll, ha = Pe.autoScroll, St = Ye.showScrollTop, D = Ye.showScrollBottom, Y = Ye.scrollControlsActive, j = Pe.showScrollTop, We = Pe.showScrollBottom, qe = Pe.scrollControlsActive, Ft = Ye.messageWindowLimit, tn = Pe.messageWindowLimit, Bt = /* @__PURE__ */ ue(""), an = /* @__PURE__ */ ue(""), Bn = /* @__PURE__ */ ue(!1), dn = /* @__PURE__ */ ue("history"), Jn = /* @__PURE__ */ ue(""), sr = /* @__PURE__ */ ue(""), Un = /* @__PURE__ */ ue(""), wr = /* @__PURE__ */ ue(""), qn = /* @__PURE__ */ ue({}), vn = /* @__PURE__ */ ue({}), Sr = /* @__PURE__ */ ue(null), yn = /* @__PURE__ */ ue(null), rs = /* @__PURE__ */ ue(null), Lo = /* @__PURE__ */ ue(null), nh = /* @__PURE__ */ ue(null), yc = /* @__PURE__ */ ue(null), Ci = /* @__PURE__ */ ue(!1), Ka = ne(() => ({
      "--xb-host-main-font-size": u.value,
      "--xb-host-prose-line-height": d.value
    }));
    let Ii = null;
    function rh(A, M) {
      const K = typeof A == "string" ? { message: A } : A || {};
      return {
        title: String(K.title || (M === "alert" ? "提示" : M === "prompt" ? "输入" : "确认操作")).trim(),
        message: String(K.message || "").trim(),
        confirmText: String(K.confirmText || (M === "alert" ? "知道了" : "确认")).trim(),
        cancelText: String(K.cancelText || "取消").trim(),
        placeholder: String(K.placeholder || "").trim(),
        inputValue: String(K.defaultValue || ""),
        tone: K.tone === "danger" || K.tone === "warning" ? K.tone : "default"
      };
    }
    function Rv() {
      if (yn.value) return;
      const A = document.activeElement;
      Ii = A instanceof HTMLElement ? A : null;
    }
    function xi() {
      const A = Ii;
      Ii = null, A?.isConnected && Ut(() => {
        try {
          A.focus({ preventScroll: !0 });
        } catch {
          A.focus();
        }
      });
    }
    function Do(A) {
      if (A)
        try {
          A.focus({ preventScroll: !0 });
        } catch {
          A.focus();
        }
    }
    function sh() {
      const A = yn.value;
      if (A) {
        if (A.kind === "prompt") {
          Do(rs.value), rs.value?.select();
          return;
        }
        if (A.kind === "alert") {
          Do(yc.value);
          return;
        }
        Do(nh.value || yc.value || Lo.value);
      }
    }
    function Ul(A, M) {
      return Rv(), yn.value?.resolve(A === "prompt" ? null : !1), new Promise((K) => {
        yn.value = {
          kind: A,
          ...rh(M, A),
          resolve: K
        }, Ut(sh);
      });
    }
    async function Rs(A) {
      return await Ul("confirm", A) === !0;
    }
    async function ah(A) {
      await Ul("alert", A);
    }
    async function _c(A) {
      const M = await Ul("prompt", A);
      return typeof M == "string" ? M : null;
    }
    function ki(A) {
      const M = yn.value;
      if (M) {
        if (yn.value = null, Ci.value = !1, typeof A < "u") {
          M.resolve(A), xi();
          return;
        }
        M.resolve(M.kind === "prompt" ? null : !1), xi();
      }
    }
    function Nv() {
      const A = yn.value;
      A && ki(A.kind === "prompt" ? A.inputValue : !0);
    }
    const oh = ne(() => yn.value?.confirmText || "确认");
    function $v(A) {
      return A ? A.kind === "alert" || A.kind === "confirm" && A.tone === "default" : !1;
    }
    function Lv() {
      Ci.value = !1, Ut(() => {
        Ci.value = !0, window.setTimeout(() => {
          Ci.value = !1;
        }, 260);
      });
    }
    function Dv() {
      if ($v(yn.value)) {
        ki();
        return;
      }
      Lv(), sh();
    }
    function bc() {
      const A = Lo.value;
      return A ? Array.from(A.querySelectorAll([
        "button:not(:disabled)",
        "input:not(:disabled)",
        "textarea:not(:disabled)",
        "select:not(:disabled)",
        "a[href]",
        '[tabindex]:not([tabindex="-1"])'
      ].join(","))).filter((M) => M.offsetParent !== null || M === document.activeElement) : [];
    }
    function ih(A) {
      const M = bc();
      if (!M.length) {
        A.preventDefault(), Do(Lo.value);
        return;
      }
      const K = M[0], fe = M[M.length - 1], $e = document.activeElement;
      if (A.shiftKey) {
        ($e === K || !M.includes($e)) && (A.preventDefault(), Do(fe));
        return;
      }
      ($e === fe || !M.includes($e)) && (A.preventDefault(), Do(K));
    }
    const Wa = _ne({ onHostRequestResolved: (A) => {
      [
        "xb-tavern:list-regex-scripts",
        "xb-tavern:save-regex-script",
        "xb-tavern:delete-regex-script"
      ].includes(A) && cA();
    } }), { createHostRequestId: lh, postToHost: Ga, reportStartupProgress: Va, requestHost: Ar } = Wa;
    function Ov(A) {
      if (!(A instanceof HTMLElement)) return !1;
      if (A.isContentEditable) return !0;
      const M = A.tagName.toLowerCase();
      if (M === "textarea") return !0;
      if (M !== "input") return !1;
      const K = String(A.type || "text").toLowerCase();
      return ![
        "button",
        "checkbox",
        "color",
        "file",
        "hidden",
        "image",
        "radio",
        "range",
        "reset",
        "submit"
      ].includes(K);
    }
    function Mi(A) {
      Ov(A.target) && Ga("xb-tavern:viewport-settle", { reason: A.type });
    }
    const ql = {
      itemSelector: ".chat-bubble[data-chat-anchor-key], .chat-history-gate[data-chat-anchor-key]",
      datasetKey: "chatAnchorKey"
    };
    function wc(A) {
      !A || ns.value !== !1 || (lb(Xt.value, A, ql, { preserveScrollTop: !0 }), Ye.updateScrollButtons());
    }
    function Fv(A) {
      const M = ns.value === !1 ? ib(Xt.value, ql) : null;
      try {
        return A();
      } finally {
        wc(M), M && typeof window < "u" && typeof window.requestAnimationFrame == "function" && window.requestAnimationFrame(() => {
          wc(M), window.requestAnimationFrame(() => {
            wc(M);
          });
        });
      }
    }
    const { clearMarkdownCache: Bv, disposeMarkdownTools: uh, enhanceChatMarkdown: Oo, enhanceLiveChatMarkdown: Sc, enhanceManagerMarkdown: Hl, markdownSignature: Yn, renderChatMarkdown: ch, stripTavernImageMarkers: Uv } = WD({
      chatScrollRef: Xt,
      managerScrollRef: Rr,
      managerWorkRef: fa,
      htmlRenderEnabled: a,
      htmlThemeDark: sn,
      alertDialog: ah,
      confirmDialog: Rs,
      requestHost: Ar,
      showToast: Ic,
      preserveChatScroll: Fv,
      getHtmlFrameAvatarUrls: () => ({
        user: String(ga.value.user?.avatar || ""),
        char: String(ga.value.character?.avatar || "")
      })
    }), dh = /* @__PURE__ */ new Map(), fh = /* @__PURE__ */ new WeakMap();
    let zl = 0, Ac = 0, Pi = 0, Ri = null, Vs = null, Kl = null, Tc = !1, hh = 0, ph = !1, mh = !1;
    const Ja = /* @__PURE__ */ new Set(), pa = /* @__PURE__ */ new Map(), Wl = /* @__PURE__ */ new Map(), Fo = /* @__PURE__ */ new Map();
    let ma = 0;
    const ga = ne(() => ({
      ...t.value,
      history: Se.value ? th(ie.value) : t.value.history
    })), Ni = ne(() => {
      const A = String(me.value?.characterKey || ga.value.character?.characterKey || "").trim();
      return (A ? Li(A, { optional: !0 }) : "") || String(me.value?.contextSnapshot?.character?.nativeCharacterId || "").trim() || String(ga.value.character?.nativeCharacterId || "").trim();
    }), { activeAssistantPreset: Gl, applyHostChatPreset: va, handleApiConfigSaved: qv, loadTavernUsers: Hv, openSettingsWorkspace: $i, openWorldbookWorkspace: gh, refreshPresets: zv, refreshRegexFromHost: vh, refreshRuntimeChatPresetFromHost: U, renderApiSettingsPanel: re, runtimeChatPreset: Ee, selectSettingsWorkspace: Le, settingsContext: dt, syncApiSettingsConfigFromAgentConfig: _t, syncChatPresetFromHost: zt, syncWorldbooksFromHost: Yt } = rve({
      activeView: Ot,
      activeSettingsWorkspace: wn,
      agentConfig: r,
      tavernDisplaySettings: s,
      effectiveContext: ga,
      currentNativeCharacterId: Ni,
      homeThemeDark: sn,
      isRunning: G,
      confirmDialog: Rs,
      describeError: ba,
      postToHost: Ga,
      requestHost: Ar,
      shortText: py
    }), Nr = ne(() => ga.value.character || {}), w8 = ne(() => _a(Nr.value.name || "", "未选择角色")), S8 = ne(() => {
      const A = String(Nr.value.avatar || "").trim(), M = String(Nr.value.characterKey || "").trim(), K = String(Nr.value.name || "").trim(), fe = f.value.find((Ue) => String(Ue.characterKey || "").trim() === M) || f.value.find((Ue) => String(Ue.name || "").trim() === K), $e = [A, String(fe?.avatar || "").trim()].filter((Ue, Xe, Vt) => Ue && Vt.indexOf(Ue) === Xe);
      return $e.find((Ue) => !Ke.value[Ue]) || $e[0] || "";
    }), A8 = ne(() => {
      const A = S8.value;
      return A && !Ke.value[A] ? A : "";
    }), VS = ne(() => ga.value.user || {}), Kv = ne(() => _a(VS.value.name || "", "User")), T8 = ne(() => String(VS.value.avatar || "").trim()), JS = ne(() => {
      const A = T8.value;
      return A && !Ke.value[A] ? A : "";
    }), ya = mne({
      selectedSessionId: Se,
      loadedSessionMessages: ie,
      selectedSession: me,
      effectiveCharacterName: ne(() => String(Nr.value.name || "")),
      isEditingMessage: DA,
      messageKey: Ya,
      roleLabel: xh,
      createHostRequestId: lh,
      requestHost: Ar,
      getTavernMessage: bl,
      updateTavernMessage: zd,
      loadSelectedSessionMessageWindow: Th,
      flashMessageAction: Bo,
      showToast: Ic,
      describeError: ba,
      markdownSignature: Yn,
      stripTavernImageMarkers: Uv,
      enhanceChatMarkdown: Oo,
      nextTick: Ut
    }), Xn = Sne(ce, {
      activeView: Ot,
      chatFocus: Wt,
      chatMessageWindowLimit: Ft,
      hiddenOutsideCount: o,
      isRunning: G,
      selectedCharacterPreviewKey: m,
      selectedSessionCharacterError: _,
      applySessionSnapshotContext: SA,
      cancelAndRollbackManagersForSession: (A) => BS(A, 0),
      cancelDrawJobsForSession: ya.cancelJobsForSession,
      confirmDeleteSession: (A) => Rs({
        title: "删除会话",
        message: `删除「${A}」？`,
        confirmText: "删除",
        tone: "danger"
      }),
      describeSessionTitle: Vv,
      invalidateMemoryFileRecordLoad: () => gy(),
      openCharacterSettingsWorkspace: () => {
        $i("characters");
      },
      refreshManagerRecords: wa,
      reportStartupProgress: Va,
      resetChatMessageWindowState: () => Ih(),
      resetSessionPreviewState: Ch,
      scrollChatToBottom: (A) => ja(A),
      syncCharacterWorldbookState: fy,
      syncSessionCharacterContextSafely: EA,
      abortActiveRun: () => Qa.abortActiveRun()
    }), YS = Xn.chatMessageWindow, yh = ne(() => t.value.character || {}), E8 = ne(() => String(yh.value.characterKey || "").trim());
    function XS(A = "") {
      const M = String(A || "").trim();
      return M && Di.value.find((K) => K.characterKey === M) || null;
    }
    function Li(A = "", M = {}) {
      const K = String(A || "").trim();
      if (!K) {
        if (M.optional) return "";
        throw new Error("缺少角色身份。");
      }
      const fe = String(XS(K)?.nativeCharacterId || "").trim();
      if (fe) return fe;
      if (M.optional) return "";
      throw new Error("角色卡已不存在或文件名变化，请重新选择角色。");
    }
    function C8(A = "") {
      const M = String(A || "").trim();
      return !!M && !Ke.value[M];
    }
    function jS(A = "") {
      const M = String(A || "").trim();
      !M || Ke.value[M] || (Ke.value = {
        ...Ke.value,
        [M]: !0
      });
    }
    function QS(A, M = "") {
      const K = String(A.characterKey || M || "").trim();
      if (!K) return null;
      const fe = String(A.nativeCharacterId || "").trim(), $e = String(A.name || "").trim() || `角色 ${K}`, Ue = String(A.avatar || "").trim(), Xe = A.shallow === !0, Vt = String(A.description || "").trim(), Mt = String(A.personality || "").trim(), Pt = String(A.scenario || "").trim(), fn = String(A.firstMessage || A.first_mes || "").trim(), qi = Vn(A.alternateGreetings || A.alternate_greetings), ss = String(A.mesExample || A.mes_example || "").trim(), Ph = String(A.creatorNotes || A.creator_notes || "").trim(), qt = String(A.characterDepthPrompt || A.character_depth_prompt || "").trim(), Hi = JSON.stringify([
        K,
        fe,
        $e,
        Ue,
        Xe,
        Vt,
        Mt,
        Pt,
        fn,
        qi,
        ss,
        Ph,
        qt
      ]), JA = dh.get(K);
      if (JA?.signature === Hi) return JA.option;
      const as = {
        characterKey: K,
        nativeCharacterId: fe,
        name: $e,
        avatar: Ue,
        shallow: Xe,
        description: Vt,
        personality: Mt,
        scenario: Pt,
        firstMessage: fn,
        alternateGreetings: qi,
        mesExample: ss,
        creatorNotes: Ph,
        characterDepthPrompt: qt
      };
      return as.searchCorpus = On(rr([
        as.characterKey,
        as.name,
        as.description,
        as.personality,
        as.scenario,
        as.firstMessage,
        ...as.alternateGreetings || [],
        as.mesExample,
        as.creatorNotes,
        as.characterDepthPrompt
      ], 900)), dh.set(K, {
        signature: Hi,
        option: as
      }), as;
    }
    const Di = ne(() => {
      const A = /* @__PURE__ */ new Map();
      f.value.forEach((K) => {
        const fe = QS(K);
        fe && A.set(fe.characterKey, fe);
      });
      const M = String(yh.value.characterKey || "").trim();
      if (M && yh.value.name && !A.has(M)) {
        const K = QS(yh.value, M);
        K && A.set(M, K);
      }
      return [...A.values()].sort((K, fe) => K.name.localeCompare(fe.name, "zh-Hans-CN"));
    }), Ec = ne(() => {
      const A = On(Dn.value);
      return A ? Di.value.filter((M) => String(M.searchCorpus || "").includes(A)) : Di.value;
    }), _h = ne(() => {
      const A = Ec.value.slice(0, br.value), M = String(m.value || "").trim();
      if (!M || A.some((fe) => fe.characterKey === M)) return A;
      const K = Di.value.find((fe) => fe.characterKey === M);
      return K ? [K, ...A] : A;
    }), Cc = ne(() => {
      const A = String(m.value || "").trim();
      if (A) {
        const M = Di.value.find((K) => K.characterKey === A);
        if (M) return M;
      }
      return null;
    }), Wv = ne(() => {
      const A = String(Cc.value?.characterKey || "").trim();
      return A ? de.value.filter((M) => String(M.characterKey || "").trim() === A).slice().sort((M, K) => (Number(K.updatedAt) || Number(K.createdAt) || 0) - (Number(M.updatedAt) || Number(M.createdAt) || 0)) : [];
    }), Gv = ne(() => {
      const A = String(me.value?.characterKey || ga.value.character?.characterKey || "").trim();
      return A ? de.value.filter((M) => String(M.characterKey || "").trim() === A).slice().sort((M, K) => (Number(K.updatedAt) || Number(K.createdAt) || 0) - (Number(M.updatedAt) || Number(M.createdAt) || 0)) : [];
    }), ZS = ne(() => {
      const A = Cc.value;
      return A ? [String(A.firstMessage || "").trim(), ...A.alternateGreetings || []].filter(Boolean) : [];
    }), I8 = ne(() => Math.max(0, Ec.value.length - Math.min(Ec.value.length, br.value)));
    function x8(A = "") {
      return String(A || "").replace(/\s*[·-]\s*小白酒馆\s*$/g, "").replace(/\s*[·-]\s*会话\s*$/g, "").replace(/^小白酒馆会话$/g, "").replace(/\s*·\s*第\s*\d+\s*轮\s*·\s*\d+\s*条可用消息\s*$/g, "").trim();
    }
    function k8(A = "") {
      return /^(sillytavern\s+system|system)\b/i.test(String(A || "").trim());
    }
    function _a(A = "", M = "") {
      const K = x8(A);
      return K && !k8(K) ? K : M;
    }
    function Vv(A) {
      if (!A) return "";
      const M = _a(A.characterName || "");
      if (M) return M;
      const K = _a(A.title || "");
      return K || "";
    }
    const M8 = ne(() => Vv(me.value) || _a(Nr.value.name || "") || "未选择角色"), P8 = ne(() => Se.value ? M8.value : _a(w8.value) || "未选择角色"), Jv = ne(() => me.value?.state?.lastRequestSnapshot), R8 = ne(() => String(Jv.value?.rawRequestJson || Jv.value?.rawMessagesJson || "")), { archivedManagerRuns: N8, currentManagerWorkRun: $8, formatRunActivityLine: L8, formatRunIssueLine: D8, formatRunInputLine: O8, formatRunMapLine: F8, formatRunMemoryLine: B8, formatRunModelLine: U8, formatRunTaskLine: q8, hiddenManagerRunCount: H8, isManagerRunLive: z8, managerBusy: Yv, managerRunTone: K8, managerStatusClock: W8, managerStatusLabel: G8, managerToolStatusLabel: V8, managerToolTone: J8, managerToolTraceItems: Y8, toolTraceSummary: X8 } = Qme({
      managerRuns: pe,
      visibleRunLimit: vve
    });
    function eA(A) {
      return Xn.sessionFloorLabel(A);
    }
    async function tA(A = []) {
      await Xn.refreshSessionMessageCountsForSessions(A);
    }
    const Xv = ne(() => Ie.value.filter((A) => A.status !== "stale")), Oi = ne(() => Ie.value.find((A) => A.path === q.value) || Ie.value[0] || null), nA = ne(() => ye.value?.sessionId === Se.value && ye.value?.path === Oi.value?.path ? ye.value : null);
    function jv(A) {
      const M = typeof A == "string" ? A : String(A?.path || "");
      return M === "memory/state.md" ? "会话记忆" : M.startsWith("memory/characters/") && M.endsWith(".md") ? M.replace(/^memory\/characters\//, "").replace(/\.md$/i, "") || "人物记忆" : M.replace(/^memory\//, "").replace(/\.md$/i, "") || "记忆档案";
    }
    function rA(A) {
      const M = typeof A == "string" ? A : String(A?.path || "");
      return M === "memory/state.md" ? "当前有效记忆状态" : M.startsWith("memory/characters/") && M.endsWith(".md") ? "人物长期记忆" : "记忆档案";
    }
    function sA(A = "") {
      return A === "memory/state.md" ? 0 : A.startsWith("memory/characters/") ? 10 : 30;
    }
    function j8(A) {
      const M = fh.get(A);
      if (M) return M;
      const K = On([
        A.path,
        jv(A),
        rA(A),
        A.status,
        A.searchText || A.preview || ""
      ].filter(Boolean).join(`
`));
      return fh.set(A, K), K;
    }
    function aA(A = "") {
      const M = A === "turns" ? yve : H_, K = Number(ct.value[A]);
      return Number.isFinite(K) && K > 0 ? Math.floor(K) : M;
    }
    function Q8(A = "") {
      const M = aA(A);
      ct.value = {
        ...ct.value,
        [A]: M + (A === "turns" ? Ix : H_)
      };
    }
    const Z8 = ne(() => {
      const A = [...Ie.value].sort((Xe, Vt) => sA(Xe.path) - sA(Vt.path) || String(Xe.path || "").localeCompare(String(Vt.path || ""))), M = On(fr.value), K = String(q.value || "").trim(), fe = M ? A.filter((Xe) => j8(Xe).includes(M)) : A;
      if (!fe.length && M) return [];
      const $e = aA("all"), Ue = fe.slice(0, $e);
      if (K && !Ue.some((Xe) => Xe.path === K)) {
        const Xe = fe.find((Vt) => Vt.path === K);
        Xe && Ue.unshift(Xe);
      }
      return [{
        key: "all",
        title: "",
        files: Ue,
        totalCount: A.length,
        filteredCount: fe.length,
        hiddenCount: Math.max(0, fe.length - Ue.length)
      }];
    }), oA = ne(() => !!Oi.value || !!Je.value), Qv = ne(() => G.value || Yv.value || ut.value), Fi = ne(() => !!Je.value && De.value !== st.value), eN = ne(() => {
      const A = he.value;
      return A ? A.status === "ready" ? "记忆可检索" : A.status === "failed" ? `记忆整理失败：${A.error || "memory_index_failed"}` : "记忆正在整理" : "还没有可检索记忆";
    }), tN = ne(() => ge.value.map((A) => `${A.sessionId}:${A.order}:${A.error ? 1 : 0}:${Yn(A.content)}`).join("|")), nN = ne(() => {
      const A = ie.value.filter((K) => ry(K)), M = A.length;
      return A.reduce((K, fe, $e) => (K[Ya(fe)] = Math.max(0, M - $e - 1), K), {});
    }), rN = ne(() => ke.value.map((A, M) => `${M}:${Yn(String(A.label || ""))}:${Yn(String(A.text || ""))}`).join("|")), sN = ne(() => ee.value.map((A, M) => [
      M,
      A.toolCallId || "",
      A.stat,
      A.action,
      A.roll,
      A.difficulty,
      A.outcome || "",
      A.insertAfterChars,
      A.success ? 1 : 0
    ].join(":")).join("|")), aN = ne(() => lx(Qe.value)), Zv = ne(() => aN.value.filter((A) => A.kind === "message")), oN = ne(() => {
      const A = rn.value;
      return !A || A.sessionId !== Se.value ? [] : A.draft ? [...A.messages, A.draft] : A.messages;
    }), iA = ne(() => lx(oN.value)), ey = ne(() => qk({ uiMessageWindowLimit: tn.value }, Zv.value.length, { defaultLimit: o.value })), ty = ne(() => Zv.value.slice(ey.value.startIndex)), iN = ne(() => ty.value.filter((A) => A.kind === "message").map((A) => A.message)), lN = ne(() => ty.value.map((A) => A.kind === "message" ? `${A.message.sessionId}:${A.message.order}:${Yn(A.message.content)}` : `${A.key}:${Yn(A.assistantMessage.content)}:${A.calls.map((M) => `${M.id}:${Yn(M.resultText)}`).join(",")}`).concat(a.value ? "html-render:on" : "html-render:off").concat(sn.value ? "theme:dark" : "theme:light").join("|")), uN = ne(() => iA.value.map((A) => A.kind === "message" ? `${A.message.sessionId}:${A.message.order}:${Yn(A.message.content)}` : `${A.key}:${Yn(A.assistantMessage.content)}:${A.calls.map((M) => `${M.id}:${Yn(M.resultText)}`).join(",")}`).concat(a.value ? "html-render:on" : "html-render:off").concat(sn.value ? "theme:dark" : "theme:light").join("|")), cN = ne(() => pe.value.flatMap((A) => (Array.isArray(A.toolTrace) ? A.toolTrace : []).map((M, K) => {
      const fe = M && typeof M == "object" ? M : {};
      return [
        A.id,
        K,
        fe.id || "",
        Yn(String(fe.preface || ""))
      ].join(":");
    })).concat(a.value ? "html-render:on" : "html-render:off").concat(sn.value ? "theme:dark" : "theme:light").join("|")), lA = ne(() => Se.value ? eA(me.value) : "写一句话后会自动创建独立会话。"), dN = ne(() => !H.value && (G.value || !_.value && !!P.value.trim())), fN = ne(() => !wt.value && (ut.value || !!Se.value && !!Be.value.trim()));
    vt(Dn, () => {
      br.value = Pp;
    }), vt(Ec, (A) => {
      if (!A.length) {
        m.value = "";
        return;
      }
      const M = String(m.value || "").trim();
      M && A.some((K) => K.characterKey === M) || (m.value = "");
    }, { immediate: !0 }), vt(m, () => {
      v.value = 0, x.value = null, E.value = "", N.value = !1, $.value = [];
    }), vt([Ot, wn], ([A, M], [K, fe]) => {
      A === "settings" && M === "characters" && (K !== A || fe !== M) && (w.value = "", dy());
    }), vt(ZS, (A) => {
      if (!A.length) {
        v.value = 0;
        return;
      }
      const M = A.length - 1;
      v.value > M && (v.value = M);
    }), vt(fr, () => {
      ct.value = {};
    }), vt(() => Wv.value.map((A) => A.id).join("|"), () => {
      tA(Wv.value);
    }, { immediate: !0 }), vt(() => Gv.value.map((A) => A.id).join("|"), () => {
      tA(Gv.value);
    }, { immediate: !0 });
    function ba(A) {
      return A instanceof Error ? A.message : String(A || "unknown_error");
    }
    function Ic(A = "", M = {}) {
      const K = String(A || "").trim();
      K && (Vs && (window.clearTimeout(Vs), Vs = null), Ne.value = {
        id: Date.now(),
        message: K,
        tone: M.tone || "info"
      }, Vs = window.setTimeout(() => {
        Vs = null, Ne.value = null;
      }, Math.max(1200, M.durationMs ?? 3600)));
    }
    async function xc(A, M = {}) {
      if (!A.length) return {
        items: [],
        changedCount: 0
      };
      const K = Object.prototype.hasOwnProperty.call(M, "nativeCharacterId"), fe = await Ar("xb-tavern:apply-regex", { payload: {
        nativeCharacterId: String(K ? M.nativeCharacterId || "" : Ni.value || "").trim(),
        items: A
      } }), $e = fe.result || fe;
      return {
        items: Array.isArray($e.items) ? $e.items : [],
        changedCount: Number($e.changedCount) || 0
      };
    }
    function hN(A = "") {
      const M = String(A || "").trim();
      return (K) => xc(K, { nativeCharacterId: M });
    }
    function uA() {
      pa.forEach((A) => window.clearTimeout(A.timer)), pa.clear(), Wl.clear(), Fo.clear();
    }
    function cA() {
      ma += 1, Ja.clear(), uA(), vn.value = {};
    }
    function dA(A, M) {
      const K = {
        ...vn.value,
        [A]: M
      }, fe = Object.keys(K);
      fe.length > xx && fe.slice(0, fe.length - xx).forEach(($e) => {
        delete K[$e];
      }), vn.value = K, Ot.value === "chat" && Wt.value === "chat" && Ut(() => {
        G.value ? Sc() : Oo(), Pc();
      });
    }
    function bh(A, M) {
      const K = Gk(A, M.actionCheckEvents || [], M.actionCheckBoundaries || []);
      return {
        text: K.text,
        actionCheckEvents: K.events
      };
    }
    function fA(A, M, K, fe) {
      dA(M, K), Fo.set(A, bh(K, fe));
    }
    function ny(A) {
      return A.role === "user" ? "userInput" : A.role === "assistant" ? "aiOutput" : null;
    }
    function ry(A) {
      return ["user", "assistant"].includes(A.role) && !A.error && !!String(A.content || "").trim();
    }
    function sy(A) {
      const M = Number(nN.value[Ya(A)]);
      return Number.isFinite(M) && M >= 0 ? M : 0;
    }
    function ay(A) {
      return String(A.name || xh(A.role) || "").trim();
    }
    function oy(A, M, K) {
      return [
        A,
        M.sessionId,
        String(Ni.value || ""),
        String(M.order),
        M.role,
        String(M.name || ""),
        String(K.index ?? ""),
        K.placement,
        String(K.depth),
        K.characterOverride || "",
        K.actionCheckSignature || "",
        Yn(String(K.label || "")),
        Yn(K.text)
      ].join("");
    }
    function hA(A, M) {
      const K = F.value?.order ?? -1;
      return [
        "runtime",
        A,
        Se.value,
        String(Ni.value || ""),
        String(K),
        "assistant",
        String(M.index ?? ""),
        M.placement,
        String(M.depth),
        M.characterOverride || "",
        M.actionCheckSignature || "",
        Yn(String(M.label || "")),
        Yn(M.text)
      ].join("");
    }
    function pA(A = []) {
      return di(A).map((M, K) => [
        K,
        M.toolCallId || "",
        M.createdAt || "",
        M.stat,
        M.action,
        M.roll,
        M.difficulty,
        M.outcome || "",
        M.insertAfterChars,
        M.success ? 1 : 0,
        M.summary || "",
        M.stakes || ""
      ].join(":")).join("|");
    }
    async function iy(A) {
      if (Ja.has(A.key)) return;
      const M = ma;
      Ja.add(A.key);
      try {
        const K = await xc([{
          id: A.key,
          text: A.text,
          placement: A.placement,
          options: A.options
        }]);
        if (M !== ma) return;
        const fe = K.items.find(($e) => $e.id === A.key) || K.items[0];
        dA(A.key, fe?.text ?? A.text);
      } catch (K) {
        console.warn("[小白酒馆] 显示正则应用失败", K);
      } finally {
        Ja.delete(A.key);
      }
    }
    async function pN(A, M) {
      if (Ja.has(M.key)) return;
      const K = ma;
      Ja.add(M.key);
      try {
        const fe = await xc([{
          id: M.key,
          text: M.text,
          placement: M.placement,
          options: M.options
        }]);
        if (K !== ma || Wl.get(A) !== M.key) return;
        const $e = fe.items.find((Ue) => Ue.id === M.key) || fe.items[0];
        fA(A, M.key, $e?.text ?? M.text, M);
      } catch (fe) {
        console.warn("[小白酒馆] 生成中显示正则应用失败", fe), K === ma && Wl.get(A) === M.key && fA(A, M.key, M.text, M);
      } finally {
        Ja.delete(M.key);
      }
    }
    function mA(A) {
      const M = pa.get(A);
      if (!M || M.inFlight) return;
      const K = M.latest;
      M.inFlight = !0, pN(A, K).finally(() => {
        const fe = pa.get(A);
        if (fe) {
          if (fe.inFlight = !1, fe.latest.key !== K.key) {
            mA(A);
            return;
          }
          pa.delete(A);
        }
      });
    }
    function gA(A, M) {
      Wl.set(A, M.key);
      const K = pa.get(A);
      if (K) {
        K.latest = M;
        return;
      }
      const fe = window.setTimeout(() => {
        const $e = pa.get(A);
        $e && ($e.timer = 0, mA(A));
      }, _ve);
      pa.set(A, {
        timer: fe,
        latest: M,
        inFlight: !1
      });
    }
    function vA(A) {
      const M = String(A.content || "");
      if (!M) return "";
      if (!ry(A)) return M;
      const K = ny(A);
      if (!K) return M;
      const fe = sy(A), $e = ay(A), Ue = oy("message", A, {
        placement: K,
        text: M,
        depth: fe,
        characterOverride: $e
      }), Xe = vn.value[Ue];
      return Xe !== void 0 ? Xe : (iy({
        key: Ue,
        text: M,
        placement: K,
        options: {
          isMarkdown: !0,
          depth: fe,
          characterOverride: $e
        }
      }), M);
    }
    function mN(A) {
      const M = String(A.content || "");
      if (!M) return {
        text: "",
        actionCheckEvents: []
      };
      const K = A.role === "assistant" ? di(A.runtimeEvents) : [];
      if (!K.length) return {
        text: vA(A),
        actionCheckEvents: []
      };
      if (!ry(A)) return {
        text: M,
        actionCheckEvents: K
      };
      const fe = ny(A);
      if (!fe) return {
        text: M,
        actionCheckEvents: K
      };
      const $e = sb(M, K), Ue = sy(A), Xe = ay(A), Vt = oy("message", A, {
        placement: fe,
        text: $e.text,
        depth: Ue,
        characterOverride: Xe,
        actionCheckSignature: pA(K)
      }), Mt = {
        key: Vt,
        text: $e.text,
        placement: fe,
        options: {
          isMarkdown: !0,
          depth: Ue,
          characterOverride: Xe
        },
        actionCheckEvents: K,
        actionCheckBoundaries: $e.boundaries
      }, Pt = vn.value[Vt];
      return Pt !== void 0 ? bh(Pt, Mt) : (iy(Mt), bh($e.text, Mt));
    }
    function gN(A) {
      const M = sy(A);
      return Bi(A).map((K, fe) => {
        const $e = String(K.text || "");
        if (!$e) return K;
        const Ue = oy("reasoning", A, {
          placement: "reasoning",
          text: $e,
          depth: M,
          index: fe,
          label: K.label
        }), Xe = vn.value[Ue];
        return Xe !== void 0 ? {
          ...K,
          text: Xe
        } : (iy({
          key: Ue,
          text: $e,
          placement: "reasoning",
          options: {
            isMarkdown: !0,
            depth: M
          }
        }), K);
      }).filter((K) => String(K.text || "").trim());
    }
    function yA(A = "", M = []) {
      const K = String(A || ""), fe = di(M);
      if (!K && !fe.length) return {
        text: "",
        actionCheckEvents: []
      };
      if (!K) return {
        text: "",
        actionCheckEvents: fe
      };
      const $e = sb(K, fe), Ue = 0, Xe = String(xh("assistant") || "").trim(), Vt = hA("message", {
        placement: "aiOutput",
        text: $e.text,
        depth: Ue,
        characterOverride: Xe,
        actionCheckSignature: pA(fe)
      }), Mt = {
        key: Vt,
        text: $e.text,
        placement: "aiOutput",
        options: {
          isMarkdown: !0,
          depth: Ue,
          characterOverride: Xe
        },
        actionCheckEvents: fe,
        actionCheckBoundaries: $e.boundaries
      }, Pt = vn.value[Vt];
      if (Pt !== void 0) {
        const fn = bh(Pt, Mt);
        return Fo.set("runtime:message", fn), fn;
      }
      return gA("runtime:message", Mt), Fo.get("runtime:message") ?? {
        text: "",
        actionCheckEvents: []
      };
    }
    function vN(A = "") {
      return yA(A).text;
    }
    function yN(A = []) {
      return Bi(A).map((K, fe) => {
        const $e = String(K.text || "");
        if (!$e) return K;
        const Ue = hA("reasoning", {
          placement: "reasoning",
          text: $e,
          depth: 0,
          index: fe,
          label: K.label
        }), Xe = vn.value[Ue];
        if (Xe !== void 0)
          return Fo.set(`runtime:reasoning:${fe}`, {
            text: Xe,
            actionCheckEvents: []
          }), {
            ...K,
            text: Xe
          };
        const Vt = {
          key: Ue,
          text: $e,
          placement: "reasoning",
          options: {
            isMarkdown: !0,
            depth: 0
          }
        }, Mt = `runtime:reasoning:${fe}`;
        return gA(Mt, Vt), {
          ...K,
          text: Fo.get(Mt)?.text ?? ""
        };
      }).filter((K) => String(K.text || "").trim());
    }
    async function wh(A) {
      if (!A.length) return {
        items: [],
        changedCount: 0
      };
      const M = await Ar("xb-tavern:substitute-params", { payload: { items: A } }), K = M.result || M;
      return {
        items: Array.isArray(K.items) ? K.items : [],
        changedCount: Number(K.changedCount) || 0
      };
    }
    async function ly(A) {
      const M = await Ar("xb-tavern:get-worldbook-runtime", { payload: {
        context: A.context,
        currentUserMessage: A.currentUserMessage,
        trigger: A.trigger,
        timedState: A.timedState
      } });
      return M.result || M;
    }
    const _A = async (A) => {
      const M = await Ar("xb-tavern:build-native-chat-prompt", { payload: A }, { signal: A.signal });
      return M.result || M;
    };
    async function uy(A = {}, M = {}) {
      const K = await Ar("xb-tavern:get-context", { payload: {
        nativeCharacterId: A.nativeCharacterId,
        includeHistory: A.includeHistory,
        includeWorldbooks: A.includeWorldbooks
      } }, M);
      return K.result || K;
    }
    function bA() {
      return String(t.value.character?.characterKey || "").trim();
    }
    function _N(A) {
      return String(A?.characterKey || A?.contextSnapshot?.character?.characterKey || "").trim();
    }
    function bN(A = {}) {
      const M = _N(me.value);
      return !String(Se.value || "").trim() || !M ? !0 : String(A.character?.characterKey || "").trim() === M;
    }
    function wN(A = {}, M = "") {
      const K = String(M || "").trim();
      if (K && String(A.character?.characterKey || "").trim() !== K)
        throw new Error("刷新到的角色卡与当前会话不一致，请重新选择角色。");
    }
    function SN() {
      return !!_a(t.value.character?.name || "") && !!bA();
    }
    function AN(A) {
      return !!A && typeof A == "object" && !Array.isArray(A) && Object.keys(A).length > 0;
    }
    function wA(A = {}, M) {
      const K = M?.contextSnapshot?.authorNote;
      return AN(K) ? {
        ...A,
        authorNote: { ...K }
      } : A;
    }
    function SA(A) {
      A && (t.value = wA(A.contextSnapshot || {}, A));
    }
    function TN(A = Se.value) {
      String(A || "").trim() === String(Se.value || "").trim() && (_.value = "");
    }
    function AA(A, M = Se.value) {
      String(M || "").trim() === String(Se.value || "").trim() && (_.value = ba(A));
    }
    function EN(A) {
      const M = A.contextSnapshot || {}, K = M.character && typeof M.character == "object" && !Array.isArray(M.character) ? M.character : {}, fe = String(K.characterKey || A.characterKey || "").trim(), $e = String(K.name || A.characterName || "").trim();
      return !fe && !$e ? M : {
        ...M,
        character: {
          ...K,
          characterKey: fe,
          name: $e
        }
      };
    }
    async function CN(A) {
      const M = String(Se.value || "").trim(), K = me.value;
      if (!M || !K) throw new Error("当前没有可保存的会话。");
      const fe = ti(A), $e = {
        ...EN(K),
        authorNote: fe
      }, Ue = await Nb(M, {
        contextSnapshot: $e,
        characterKey: String(K.characterKey || $e.character?.characterKey || ""),
        characterName: String($e.character?.name || K.characterName || "")
      });
      Ue && Xn.updateSessionRecord(Ue), Se.value === M && (t.value = $e);
    }
    async function TA(A = {}) {
      const M = String(A.sessionId || Se.value || "").trim(), K = de.value.find((fn) => fn.id === M) || (M === Se.value ? me.value : null);
      if (!K) return t.value;
      const fe = String(K.characterKey || "").trim();
      if (!fe) return t.value;
      const $e = SN() && bA() === fe;
      M === String(Se.value || "").trim() && SA(K);
      const Ue = Li(fe);
      if (TN(M), !A.force && $e) return t.value;
      const Xe = ++hh, Vt = await uy({
        nativeCharacterId: Ue,
        includeHistory: !1
      });
      if (Xe !== hh || M !== String(Se.value || "").trim()) return t.value;
      const Mt = wA(Vt.context || t.value, K);
      wN(Mt, fe), cy({
        ...Vt,
        context: Mt
      });
      const Pt = await Nb(M, {
        contextSnapshot: Mt,
        characterKey: String(Mt.character?.characterKey || K.characterKey || ""),
        characterName: String(Mt.character?.name || K.characterName || "")
      });
      return Pt && Xn.updateSessionRecord(Pt), Mt;
    }
    async function EA(A = {}) {
      const M = String(A.sessionId || Se.value || "").trim();
      try {
        await TA(A);
      } catch (K) {
        AA(K, M);
      }
    }
    async function Sh(A = Se.value) {
      const M = String(A || "").trim();
      if (!M) return t.value;
      try {
        return await TA({
          sessionId: M,
          force: !0
        });
      } catch (K) {
        throw AA(K, M), K;
      }
    }
    function IN(A = {}) {
      i.value = (A.hostRequestHeaders && typeof A.hostRequestHeaders == "object" ? A.hostRequestHeaders : i.value) || {}, Y2(async () => {
        try {
          const M = await Ar("xb-tavern:get-host-request-headers");
          return M.hostRequestHeaders && typeof M.hostRequestHeaders == "object" ? M.hostRequestHeaders : i.value;
        } catch {
          return i.value;
        }
      });
    }
    function cy(A) {
      if (IN(A), "context" in A) {
        const K = A.context || {};
        bN(K) && (t.value = K);
      }
      "diagnostics" in A && (n.value = A.diagnostics || {});
      const M = "hostMainFontSizePx" in A ? Gs(A.hostMainFontSizePx, u.value) : u.value;
      u.value = M, d.value = "hostProseLineHeightPx" in A ? Gs(A.hostProseLineHeightPx, Gr(M)) : Gr(M), "agentConfig" in A && (r.value = A.agentConfig || r.value, _t()), "tavernDisplaySettings" in A && (s.value = yu(A.tavernDisplaySettings)), "htmlRenderEnabled" in A && (a.value = A.htmlRenderEnabled !== !1), va(A), f.value = A.availableCharacters || f.value, b.value = n.value.message || "", QN().catch((K) => {
        w.value = K instanceof Error ? K.message : String(K || "create_session_failed"), kc();
      }), Ut(re);
    }
    function xN() {
      !ph || mh || (mh = !0, o7());
    }
    function CA(A) {
      const M = A.availableCharacters;
      Array.isArray(M) && (f.value = M);
    }
    function kN(A) {
      return !!A && !!(A.description || A.personality || A.scenario || A.firstMessage || A.mesExample || A.creatorNotes || A.characterDepthPrompt || A.alternateGreetings?.length);
    }
    function Ah(A) {
      return A.payload && typeof A.payload == "object" ? A.payload : {};
    }
    function MN(A) {
      return A.type === "xb-tavern:inline-image-progress" ? (window.dispatchEvent(new CustomEvent(ab, { detail: Ah(A) })), !0) : !1;
    }
    function PN(A) {
      return A.type === "xb-tavern:config" ? (Va(88, "applyHostPayload"), cy(Ah(A)), ph = !0, xN(), !0) : A.type === "xb-tavern:context" ? (cy(Ah(A)), !0) : A.type === "xb-tavern:config-saved" ? (qv(Ah(A)), !0) : !1;
    }
    Wa.addMessageHandler((A) => ya.handleHostMessage(A)), Wa.addMessageHandler(MN), Wa.addMessageHandler(PN);
    function IA() {
      w.value = "", m.value = "", $i("characters");
    }
    async function dy() {
      b.value = "正在读取角色列表", w.value = "";
      try {
        CA(await uy({
          includeHistory: !1,
          includeWorldbooks: !1
        })), b.value = n.value.message || "";
      } catch (A) {
        w.value = ba(A);
      }
    }
    async function RN(A) {
      const M = String(A || "").trim();
      if (!M || S.value) return;
      m.value = M;
      const K = XS(M);
      if (K && K.shallow !== !0 && kN(K)) return;
      const fe = ++zl;
      y.value = M, w.value = "";
      try {
        const $e = await uy({
          nativeCharacterId: Li(M),
          includeHistory: !1,
          includeWorldbooks: !1
        });
        if (fe !== zl || m.value !== M) return;
        CA($e);
      } catch ($e) {
        if (fe !== zl || m.value !== M) return;
        w.value = $e instanceof Error ? $e.message : String($e || "character_preview_failed");
      } finally {
        fe === zl && y.value === M && (y.value = "");
      }
    }
    async function fy(A = m.value) {
      const M = String(A || "").trim();
      if (!M) {
        x.value = null, N.value = !1, $.value = [];
        return;
      }
      const K = ++Ac;
      try {
        const fe = await Ar("xb-tavern:get-character-worldbook-state", { payload: { nativeCharacterId: Li(M) } });
        if (K !== Ac || String(m.value || "").trim() !== M) return;
        x.value = fe.result || fe, E.value = "";
      } catch (fe) {
        if (K !== Ac || String(m.value || "").trim() !== M) return;
        E.value = ba(fe);
      }
    }
    async function NN() {
      const A = String(m.value || "").trim();
      if (!(!A || I.value)) {
        I.value = !0, E.value = "";
        try {
          const M = Li(A);
          let K = await Ar("xb-tavern:activate-character-worldbook", { payload: { nativeCharacterId: M } }), fe = K.result || K;
          if (fe.action === "needs_import_confirmation") {
            const Ue = String(fe.name || "").trim(), Xe = fe.state;
            if (Xe && (x.value = Xe), !(Ue && await Rs({
              title: "覆盖世界书",
              message: `世界书「${Ue}」已存在，导入角色内嵌世界书会覆盖它。继续？`,
              confirmText: "继续覆盖",
              tone: "warning"
            }))) return;
            K = await Ar("xb-tavern:activate-character-worldbook", { payload: {
              nativeCharacterId: M,
              confirmed: !0
            } }), fe = K.result || K;
          }
          const $e = String(fe.action || "");
          if ($e === "selected" || $e === "imported") {
            const Ue = fe.state;
            Ue && (x.value = Ue), $e === "imported" && Ga("xb-tavern:refresh-context", {
              nativeCharacterId: M,
              includeHistory: !1
            }), gh(String(fe.name || ""));
            return;
          }
          if ($e === "needs_selection") {
            $.value = Vn(fe.worldbookOptions), N.value = !0;
            const Ue = fe.state;
            Ue && (x.value = Ue);
            return;
          }
          await fy(A);
        } catch (M) {
          E.value = ba(M);
        } finally {
          I.value = !1;
        }
      }
    }
    function xA() {
      N.value = !1;
    }
    async function $N(A) {
      const M = String(m.value || "").trim(), K = String(A || "").trim();
      if (!(!M || !K || I.value)) {
        I.value = !0, E.value = "";
        try {
          const fe = Li(M), $e = await Ar("xb-tavern:bind-character-worldbook", { payload: {
            nativeCharacterId: fe,
            name: K
          } });
          x.value = $e.result || $e, N.value = !1, Ga("xb-tavern:refresh-context", {
            nativeCharacterId: fe,
            includeHistory: !1
          }), gh(K);
        } catch (fe) {
          E.value = ba(fe);
        } finally {
          I.value = !1;
        }
      }
    }
    function LN(A) {
      const M = ZS.value;
      if (!M.length) {
        v.value = 0;
        return;
      }
      v.value = Math.max(0, Math.min(M.length - 1, Number(A) || 0));
    }
    function DN(A) {
      const M = _h.value;
      if (!M.length || S.value) return;
      const K = String(Cc.value?.characterKey || m.value || "").trim();
      if (!K) {
        m.value = M[A < 0 ? M.length - 1 : 0]?.characterKey || "";
        return;
      }
      const fe = Math.max(0, M.findIndex(($e) => $e.characterKey === K));
      m.value = M[Math.min(M.length - 1, Math.max(0, fe + A))]?.characterKey || "";
    }
    function ON() {
      m.value = _h.value[0]?.characterKey || "";
    }
    function FN() {
      m.value = _h.value.at(-1)?.characterKey || "";
    }
    function BN() {
      br.value += Pp;
    }
    async function UN() {
      const A = String(Cc.value?.characterKey || m.value || "").trim();
      !A || S.value || await ZN(A);
    }
    async function Th(A = {}) {
      await Xn.loadSelectedSessionMessageWindow(A);
    }
    function qN(A) {
      Xn.upsertLoadedSessionMessage(A);
    }
    function HN(A = "", M = Number.POSITIVE_INFINITY) {
      return Xn.pruneLoadedSessionMessagesFromOrder(A, M);
    }
    function zN(A, M = Date.now()) {
      Xn.touchSessionLocally(A, M);
    }
    async function Eh() {
      await Xn.refreshSessions();
    }
    async function KN(A = {}) {
      const M = String(Se.value || "").trim();
      if (!M) return null;
      const K = await n3(M, { contract: Fs({
        ...Fn.value,
        ...A
      }) });
      return K ? (Xn.updateSessionRecord(K), K) : null;
    }
    async function wa(A = Se.value) {
      Va(95, "refreshManagerRecords");
      const M = String(A || "").trim();
      if (!M) {
        Qe.value = [], pe.value = [], Me.value = [], Ie.value = [], he.value = null, gy(), oe.value = null, ht.value = [], It.value = "main", Lt.value = null, en.value = [], at.value = null, X.value = [], se.value = "", q.value = "";
        return;
      }
      const [K, fe, $e, Ue, Xe, Vt, Mt] = await Promise.all([
        Ls(M),
        i3(M, { limit: 18 }),
        qs(M, { includeCompleted: !0 }),
        Q3(M),
        Yq(M),
        Xq(M),
        li(M, "memory/state.md")
      ]), Pt = Ue && Ue.status === "ready" && Array.isArray(Ue.files) ? Ue : await Hf(M);
      if (M === Se.value && (Qe.value = K, pe.value = fe, Me.value = $e, Ie.value = Array.isArray(Pt.files) ? Pt.files.map((fn) => ({
        path: String(fn.path || ""),
        status: fn.status === "stale" ? "stale" : "active",
        createdAt: Number(fn.createdAt) || Number(fn.updatedAt) || 0,
        updatedAt: Number(fn.updatedAt) || 0,
        source: String(fn.source || ""),
        staleFromOrder: Number.isFinite(Number(fn.staleFromOrder)) ? Number(fn.staleFromOrder) : void 0,
        contentLength: Math.max(0, Number(fn.contentLength) || 0),
        title: String(fn.title || ""),
        preview: String(fn.preview || ""),
        searchText: String(fn.searchText || "")
      })) : [], he.value = Pt, oe.value = Mt, ht.value = Xe.documents, It.value = Xe.activeDocId, Lt.value = Xe.activeDocument, en.value = Xe.activePatches, at.value = Vt.document, X.value = Vt.patches, se.value = Vt.activeLocationKey, !Ie.value.some((fn) => fn.path === q.value))) {
        if (Fi.value && q.value) {
          ot.value = "当前档案已变化，草稿仍保留";
          return;
        }
        q.value = Ie.value[0]?.path || "";
      }
    }
    async function WN() {
      if (W8.value = Date.now(), !Tc && Se.value && !(!pe.value.some((A) => z8(A.status)) && !G.value && !ut.value)) {
        Tc = !0;
        try {
          await wa(Se.value);
        } finally {
          Tc = !1;
        }
      }
    }
    async function kA() {
      if (!Se.value) return;
      const A = Xr(me.value?.state || {}), M = await a3(Se.value), K = await e8({
        messages: M,
        contextSnapshot: await Sh(Se.value),
        chatPreset: Ee.value,
        historyMode: C.value,
        diagnostics: n.value,
        applySubstituteParams: wh,
        getNativeWorldInfoRuntime: ly
      });
      await r3(Se.value, {
        ...K,
        contract: A.contract,
        contextWindowStartOrder: vu({
          messages: M,
          contextWindowStartOrder: A.contextWindowStartOrder
        }).contextWindowStartOrder
      }), await Eh();
    }
    function Ch() {
      Pi += 1, Qa.resetChatRunPreviewState(), Jn.value = "", sr.value = "", Un.value = "", wr.value = "", Bn.value = !1, dn.value = "history", Ht.value = null, Ih(), hy(), Bv();
    }
    const Ih = Ye.resetWindowState, hy = Pe.resetWindowState, GN = Ye.revealOlderMessages, VN = Pe.revealOlderMessages;
    function JN(A = {}) {
      return [String(A.firstMessage || A.first_mes || "").trim(), ...Vn(A.alternateGreetings || A.alternate_greetings)].filter(Boolean);
    }
    async function YN(A, M, K = 0) {
      const fe = JN(M.character || {}), $e = Math.max(0, Math.min(fe.length - 1, Number(K) || 0)), Ue = String(fe[$e] || "").trim();
      Ue && await hd(A, {
        role: "assistant",
        name: String(M.character?.name || ""),
        content: Ue,
        contextSnapshot: M,
        chatPresetId: String(Ee.value.id || ""),
        chatPresetName: String(Ee.value.name || ""),
        presetId: String(Ee.value.id || ""),
        presetName: String(Ee.value.name || "")
      });
    }
    async function MA(A = {}) {
      const M = A.contextSnapshot || t.value, K = cM({
        context: M,
        chatPreset: Ee.value,
        currentUserMessage: "",
        historyMode: C.value,
        turn: 0,
        entryStates: {},
        diagnostics: n.value
      }), fe = await t3({
        title: String(M.character?.name || "未选择角色"),
        characterKey: String(M.character?.characterKey || ""),
        characterName: String(M.character?.name || "未选择角色"),
        contextSnapshot: M,
        buildSnapshot: K.buildSnapshot,
        chatPresetId: String(Ee.value.id || ""),
        chatPresetName: String(Ee.value.name || ""),
        presetId: String(Ee.value.id || ""),
        presetName: String(Ee.value.name || ""),
        state: {
          turn: 0,
          worldEntryStates: {},
          nativeWorldInfoTimedState: {
            sticky: {},
            cooldown: {}
          }
        }
      });
      return A.includeFirstMessage !== !1 && await YN(fe.id, M, A.greetingIndex), Xn.setSelectedSessionId(fe.id), await Eh(), fe;
    }
    async function PA(A = {}) {
      await MA(A), Ot.value = "chat", Wt.value = "chat", ja(!0);
    }
    async function XN() {
      if (G.value || H.value) return;
      const A = Se.value ? await Sh(Se.value) : t.value;
      Ch(), await PA({ contextSnapshot: A });
    }
    async function jN() {
      if (Ps.value) {
        n$();
        return;
      }
      IA();
    }
    function kc() {
      S.value = "", p.value = 0;
    }
    async function QN() {
      const A = S.value;
      if (!A || String(t.value.character?.characterKey || "").trim() !== A) return;
      if (!_a(t.value.character?.name || "")) {
        kc(), w.value = "没有读到这张角色卡。";
        return;
      }
      const M = p.value;
      kc(), w.value = "", Ch(), await PA({ greetingIndex: M });
    }
    async function ZN(A) {
      const M = String(A || "").trim();
      if (!M || S.value) return;
      const K = M === String(m.value || "").trim() ? v.value : 0;
      m.value = M, v.value = K, await Xn.clearSelectedSession({
        persist: !0,
        refreshManager: !0
      }), Ch(), w.value = "", b.value = "正在读取角色卡", S.value = M, p.value = K;
      try {
        Ga("xb-tavern:refresh-context", {
          nativeCharacterId: Li(M),
          includeHistory: !1
        });
      } catch (fe) {
        w.value = ba(fe), kc();
      }
    }
    async function e$(A) {
      await Xn.selectSession(A);
    }
    async function t$(A, M) {
      M?.stopPropagation(), await Xn.removeSession(A);
    }
    function n$() {
      Ot.value = "chat", Wt.value = "chat", ja(!0);
    }
    function r$(A = "history") {
      dn.value = A, Bn.value = !0;
    }
    function s$() {
      Bn.value = !1;
    }
    async function a$() {
      const A = Jn.value.trim();
      if (wr.value = "", Un.value = "", sr.value = "", !A) {
        wr.value = "先写一句话。";
        return;
      }
      const M = Pi + 1;
      Pi = M;
      const K = Se.value;
      Un.value = "模拟中";
      try {
        const fe = await Sh(K), $e = hN(fe.character?.nativeCharacterId), Ue = await U(), Xe = await zte({
          sessionId: K,
          agentConfig: r.value,
          contextSnapshot: fe,
          chatPreset: Ue,
          currentUserMessage: A,
          runtimeState: Xr(me.value?.state || {}),
          diagnostics: n.value,
          historyMode: C.value,
          applyRegex: $e,
          applySubstituteParams: wh,
          getNativeWorldInfoRuntime: ly,
          buildNativeChatPrompt: _A
        });
        if (M !== Pi || K !== Se.value) return;
        sr.value = Xe.requestSnapshot.rawRequestJson || Xe.requestSnapshot.rawMessagesJson || "", Un.value = `模拟完成 · ${Xe.requestSnapshot.providerLabel || Xe.provider} / ${Xe.model || "未选择模型"}`;
      } catch (fe) {
        if (M !== Pi || K !== Se.value) return;
        console.error("[小白酒馆] simulate request failed", fe), Un.value = "", wr.value = fe instanceof Error ? fe.message : String(fe || "simulate_failed");
      }
    }
    function py(A = "", M = 180) {
      const K = String(A || "").trim();
      return K.length > M ? `${K.slice(0, M)}...` : K;
    }
    async function o$(A = Se.value) {
      const M = String(A || "").trim();
      M && await ng(M);
    }
    async function my(A = Se.value, M) {
      const K = String(A || "").trim();
      if (!K) return;
      const fe = Number(M);
      await ng(K, (Number.isFinite(fe) ? Math.floor(fe) : (await Kp(K, Number.POSITIVE_INFINITY))?.order) ?? -1);
    }
    const { discardMemoryDraft: RA, enterMemoryEditMode: i$, formatMemoryFileMeta: l$, invalidateMemoryFileRecordLoad: gy, loadMemoryFileIntoEditor: NA, loadSelectedMemoryFileRecord: u$, memoryFileStatusLabel: c$, previewMemoryDraft: d$, saveSelectedMemoryFile: f$, selectMemoryFile: h$ } = Zme({
      memoryEditorBaseContent: st,
      memoryEditorDocumentAvailable: oA,
      memoryEditorDraft: De,
      memoryEditorDirty: Fi,
      memoryEditorLoadedPath: Je,
      memoryEditorMode: lt,
      memoryEditorReadOnly: Qv,
      memoryEditorStatus: ot,
      selectedMemoryFileEntry: Oi,
      selectedMemoryFilePath: q,
      selectedMemoryFileRecord: ye,
      selectedSessionId: Se,
      commitUserAcceptedState: my,
      confirmDialog: Rs,
      refreshRecords: wa
    });
    vt(Qv, (A) => {
      if (!A || lt.value !== "edit") return;
      const M = Fi.value;
      RA(), M && Ic("记忆正在维护，未保存修改已放弃", {
        tone: "warning",
        durationMs: 4e3
      });
    }, { immediate: !0 });
    async function p$(A) {
      const M = String(A.id || "");
      if (!(!M || !Se.value || Yv.value || Re.value)) {
        Re.value = M, Ae.value = "记忆正在重试。";
        try {
          const [K, fe] = await Promise.all([bl(Se.value, A.userOrder), bl(Se.value, A.assistantOrder)]), $e = K?.role === "user" ? K : null, Ue = fe?.role === "assistant" && !fe.error ? fe : null;
          if (!$e || !Ue) {
            Ae.value = "原文楼层不存在，无法重试。", await wa();
            return;
          }
          const Xe = await B6({
            sessionId: Se.value,
            agentConfig: r.value,
            userMessage: $e,
            assistantMessage: Ue,
            turn: A.turn,
            assistantPreset: Gl.value,
            sessionContract: Fn.value
          });
          Ae.value = Xe.ok ? "" : `失败：${Xe.error || "manager_retry_failed"}`;
        } catch (K) {
          Ae.value = K instanceof Error ? K.message : String(K || "manager_retry_failed");
        } finally {
          try {
            await wa();
          } finally {
            Re.value = "";
          }
        }
      }
    }
    function m$(A) {
      return !!A && !!Re.value && String(A.id || "") === Re.value;
    }
    function xh(A = "") {
      return A === "assistant" ? _a(me.value?.characterName || Nr.value.name || "", "角色") : A === "user" ? _a(Kv.value || "", "User") : {
        system: "规则",
        tool: "工具结果"
      }[A] || A || "未知";
    }
    function Bi(A) {
      return (Array.isArray(A) ? A : Array.isArray(A?.thoughts) ? A.thoughts : []).map((M, K) => ({
        label: String(M?.label || `思考 ${K + 1}`).trim() || `思考 ${K + 1}`,
        text: String(M?.text || "").trim()
      })).filter((M) => M.text);
    }
    function g$(A, M = !1) {
      const K = Bi(A);
      if (!K.length) return "";
      const fe = M ? "正在思考" : "展开思考块";
      return K.length > 1 ? `${fe}（${K.length} 段）` : fe;
    }
    function v$(A) {
      const M = Number(A) || 0;
      if (!M) return "";
      try {
        return new Intl.DateTimeFormat("zh-CN", {
          hour: "2-digit",
          minute: "2-digit"
        }).format(new Date(M));
      } catch {
        return "";
      }
    }
    function Ya(A) {
      return `${A.sessionId}:${A.order}`;
    }
    function Mc(A) {
      return `manager:${A.sessionId}:${A.order}`;
    }
    function vy(A) {
      return !G.value && !A.error && ["user", "assistant"].includes(A.role);
    }
    function yy(A) {
      return !ut.value && !A.error && ["user", "assistant"].includes(A.role);
    }
    function $A(A) {
      if (G.value) return !1;
      const M = F.value;
      return !M || M.sessionId !== A.sessionId || M.order !== A.order ? !1 : A.role === "user" ? !0 : A.role === "assistant";
    }
    function LA(A) {
      if (ut.value) return !1;
      if (A.role === "user") return !0;
      const M = [...Qe.value].sort((fe, $e) => fe.order - $e.order), K = M.findIndex((fe) => fe.order === A.order);
      return K < 0 ? !1 : M.slice(0, K + 1).some((fe) => fe.role === "user");
    }
    function DA(A) {
      return Bt.value === Ya(A);
    }
    function OA(A) {
      return Bt.value === Mc(A);
    }
    function FA(A) {
      return OA(A) && an.value.trim() !== String(A.content || "").trim();
    }
    function Bo(A, M, K) {
      const fe = `${Ya(A)}:${M}`;
      qn.value = {
        ...qn.value,
        [fe]: K ? "success" : "error"
      }, window.setTimeout(() => {
        const $e = { ...qn.value };
        delete $e[fe], qn.value = $e;
      }, 1100);
    }
    function Ui(A, M, K) {
      const fe = `${Mc(A)}:${M}`;
      qn.value = {
        ...qn.value,
        [fe]: K ? "success" : "error"
      }, window.setTimeout(() => {
        const $e = { ...qn.value };
        delete $e[fe], qn.value = $e;
      }, 1100);
    }
    function y$(A, M) {
      return qn.value[`${Ya(A)}:${M}`] || "";
    }
    function _$(A, M) {
      return qn.value[`${Mc(A)}:${M}`] || "";
    }
    async function BA(A = "") {
      const M = String(A || "");
      if (!M) return !1;
      try {
        if (navigator.clipboard?.writeText)
          return await navigator.clipboard.writeText(M), !0;
      } catch {
      }
      try {
        const K = document.createElement("textarea");
        K.value = M, K.setAttribute("readonly", "readonly"), K.style.position = "fixed", K.style.opacity = "0", K.style.pointerEvents = "none", document.body.appendChild(K), K.select(), K.setSelectionRange(0, K.value.length);
        const fe = document.execCommand("copy");
        return K.remove(), fe;
      } catch {
        return !1;
      }
    }
    async function b$(A) {
      Bo(A, "copy", await BA(A.content || ""));
    }
    async function w$(A) {
      Ui(A, "copy", await BA(A.content || ""));
    }
    function S$(A) {
      vy(A) && (Bt.value = Ya(A));
    }
    function A$(A) {
      yy(A) && (Bt.value = Mc(A), an.value = A.content || "", Ut(() => {
        const M = Rr.value?.querySelector(`[data-manager-message-editor="${Mc(A)}"]`);
        M && (_y(M), M.focus(), M.setSelectionRange(M.value.length, M.value.length));
      }));
    }
    function Xa() {
      Bt.value = "", an.value = "", Ut(() => {
        Oo(), Hl();
      });
    }
    function _y(A, M = {}) {
      if (!A) return;
      const K = Number(M.minHeight ?? 144), fe = Number(M.maxHeight ?? 420);
      A.style.height = "auto", A.style.height = `${Math.min(Math.max(A.scrollHeight, K), fe)}px`;
    }
    function by(A) {
      A && (A.style.height = "");
    }
    function T$(A, M) {
      if (A.key === "Escape") {
        A.preventDefault(), Xa();
        return;
      }
      A.key === "Enter" && (A.ctrlKey || A.metaKey) && (A.preventDefault(), UA(M));
    }
    function E$(A) {
      _y(A.target);
    }
    function C$(A) {
      const M = A.target;
      _y(M, M.closest(".manager-compose") ? {
        minHeight: 48,
        maxHeight: 180
      } : {
        minHeight: 36,
        maxHeight: 76
      });
    }
    async function I$(A, M = {}) {
      if (!vy(A)) return;
      const K = ("content" in M ? String(M.content || "") : String(A.content || "")).trim(), fe = M.rollbackState === !0;
      if (!K) {
        Bo(A, "edit", !1);
        return;
      }
      if (K === String(A.content || "").trim()) {
        Xa();
        return;
      }
      if (fe) {
        const Xe = await dx(A.sessionId, A.order);
        if ((Xe.willRollbackState || Xe.willCancelWork) && !await Rs({
          title: "保存楼层编辑",
          message: ["保存后将按这楼重新整理后续状态。", ...fx(Xe)].join(`

`),
          confirmText: "回滚保存",
          tone: "warning"
        }))
          return;
      }
      const $e = await X$(A, await Y$(A, K));
      ya.cancelJob(Ya(A));
      const Ue = await zd(A.sessionId, A.order, { content: $e });
      Ue && fe && (await ux(A.sessionId, A.order), await cx(A.sessionId, A.order)), Ue && Se.value && (Qa.resolveDeferredAssistantCommit({ sessionId: A.sessionId }), await Th({ sessionId: Se.value }), fe && await wa(Se.value)), Xa(), Bo(Ue || A, "edit", !!Ue), Ue && fe && await kA();
    }
    async function UA(A, M = {}) {
      if (!yy(A)) return;
      const K = an.value.trim();
      if (!K) {
        Ui(A, "edit", !1);
        return;
      }
      if (!M.rerun && !FA(A)) {
        Xa();
        return;
      }
      if (M.rerun && A.role !== "user") {
        Xa(), await wy(A);
        return;
      }
      const fe = await WU(A.sessionId, A.order, {
        content: K,
        clearProtocolPayload: A.role === "assistant"
      });
      fe && Se.value === A.sessionId && (Qe.value = await Ls(A.sessionId)), Xa(), Ui(fe || A, "edit", !!fe), fe && M.rerun && await wy(fe);
    }
    async function x$(A) {
      const M = await hw(A.sessionId, A.order);
      return M.length ? M : [A.order];
    }
    async function k$(A) {
      if (G.value) return;
      const M = await x$(A), K = Math.max(1, Number(A.order) + 1), fe = Math.min(...M), $e = await dx(A.sessionId, fe);
      if (!await Rs({
        title: "删除剧情楼层",
        message: [M.length > 1 ? `从第 ${K} 楼开始删除后续剧情？将移除 ${M.length} 楼。` : `删除第 ${K} 楼？`, ...fx($e)].filter(Boolean).join(`

`),
        confirmText: "删除",
        tone: "danger"
      })) return;
      ya.cancelJobsForMessageRange(A.sessionId, fe), await ux(A.sessionId, fe);
      const Ue = await s3(A.sessionId, M);
      Ue > 0 && await cx(A.sessionId, fe), Se.value && (Qa.resolveDeferredAssistantCommit({
        sessionId: A.sessionId,
        discardOrders: Ue > 0 ? M : []
      }), await Th({ sessionId: Se.value }), await wa(Se.value)), Ue > 0 && await kA(), Bt.value.startsWith(`${A.sessionId}:`) && Xa(), Bo(A, "delete", Ue > 0);
    }
    async function M$(A) {
      if (!$A(A)) {
        Bo(A, "rerun", !1);
        return;
      }
      const M = A.role === "user" ? A : await Kp(A.sessionId, A.order);
      if (!M) {
        Bo(A, "rerun", !1);
        return;
      }
      ya.cancelJobsForMessageRange(A.sessionId, M.order + 1), Bo(A, "rerun", !0), await KA({
        messageText: M.content,
        reuseUserMessageOrder: M.order
      });
    }
    function P$(A) {
      const M = [...Qe.value].sort(($e, Ue) => $e.order - Ue.order), K = M.findIndex(($e) => $e.order === A.order);
      if (K < 0) return [A.order];
      if (A.role !== "user") {
        let $e = K;
        for (let Xe = K; Xe >= 0; Xe -= 1) if (M[Xe]?.role === "user") {
          $e = Xe;
          break;
        }
        const Ue = [];
        for (let Xe = $e + 1; Xe < M.length; Xe += 1) {
          const Vt = M[Xe];
          if (Vt.role === "user") break;
          Ue.push(Vt.order);
        }
        return Ue.length ? Ue : [A.order];
      }
      const fe = [];
      for (let $e = K; $e < M.length; $e += 1) {
        const Ue = M[$e];
        if ($e > K && Ue.role === "user") break;
        fe.push(Ue.order);
      }
      return fe;
    }
    async function R$(A) {
      if (ut.value) return;
      const M = P$(A);
      if (!await Rs({
        title: "删除助手记录",
        message: A.role === "user" ? `删除这轮助手对话？将移除 ${M.length} 条记录。` : "删除这条助手回复？",
        confirmText: "删除",
        tone: "danger"
      })) return;
      const K = await $b(A.sessionId, M);
      Se.value === A.sessionId && (Qe.value = await Ls(A.sessionId)), Bt.value.startsWith(`manager:${A.sessionId}:`) && Xa(), Ui(A, "delete", K > 0);
    }
    async function wy(A) {
      if (!LA(A)) {
        Ui(A, "rerun", !1);
        return;
      }
      const M = [...Qe.value].sort((Xe, Vt) => Xe.order - Vt.order), K = M.findIndex((Xe) => Xe.order === A.order), fe = A.role === "user" ? A : [...M.slice(0, Math.max(0, K + 1))].reverse().find((Xe) => Xe.role === "user");
      if (!fe || !Se.value) {
        Ui(A, "rerun", !1);
        return;
      }
      const $e = M.filter((Xe) => Xe.order >= fe.order).map((Xe) => Xe.order);
      if ($e.length > 2 && !await Rs({
        title: "重新询问助手",
        message: `从这里重新询问助手会移除后面的 ${$e.length} 条记录。继续？`,
        confirmText: "继续",
        tone: "warning"
      }))
        return;
      const Ue = M.filter((Xe) => Xe.order < fe.order);
      Ui(A, "rerun", !0), await GA(fe.sessionId, fe.content, {
        historyBeforeTurn: Ue,
        replaceOrdersAfterAppend: $e
      });
    }
    const Pc = Ye.updateScrollButtons, qA = Pe.updateScrollButtons, ja = Ye.scrollToBottom, N$ = Ye.scrollToTop, Sy = Pe.scrollToBottom, $$ = Pe.scrollToTop, L$ = Ye.handleScroll, D$ = Pe.handleScroll, O$ = Ye.handleWheel, F$ = Pe.handleWheel, B$ = Ye.handleTouchStart, U$ = Pe.handleTouchStart, q$ = Ye.handleTouchMove, H$ = Pe.handleTouchMove;
    function z$(A) {
      A.key === "Enter" && (A.isComposing || A.shiftKey || A.altKey || (A.ctrlKey || A.metaKey || window.innerWidth >= 760) && (A.preventDefault(), KA()));
    }
    function K$(A) {
      A.key === "Enter" && (A.isComposing || A.shiftKey || A.altKey || (A.ctrlKey || A.metaKey || window.innerWidth >= 760) && (A.preventDefault(), VA()));
    }
    function HA() {
      Ri && (window.clearTimeout(Ri), Ri = null);
    }
    function kh(A) {
      const M = Ht.value || null, K = String(A.id || M?.id || `manager-compaction-${Date.now()}`), fe = Number(A.visibleSince) || (M?.id === K ? Number(M.visibleSince) : 0) || Date.now();
      Ht.value = {
        id: K,
        active: !0,
        resolved: !1,
        currentTokens: 0,
        yieldTokens: 0,
        triggerTokens: 0,
        status: "正在释放较早管理员对话...",
        ...M,
        ...A,
        visibleSince: fe
      };
    }
    function zA(A = 3e3) {
      const M = Ht.value?.id || "", K = Number(Ht.value?.visibleSince) || Date.now(), fe = Math.max(0, Date.now() - K), $e = Math.max(0, A - fe);
      HA(), Ri = window.setTimeout(() => {
        Ri = null, !(!M || Ht.value?.id !== M) && (Ht.value = null);
      }, $e);
    }
    function Ay(A, M) {
      const K = rn.value?.sessionId === A ? rn.value : {
        sessionId: A,
        messages: [],
        draft: null,
        nextOrder: bve
      }, fe = K.nextOrder;
      return K.nextOrder += 1, rn.value = K, {
        sessionId: A,
        order: fe,
        role: M.role,
        content: M.content,
        name: M.name,
        error: M.error === !0,
        createdAt: Date.now(),
        updatedAt: Date.now(),
        provider: M.provider,
        model: M.model,
        finishReason: M.finishReason,
        thoughts: M.thoughts,
        providerPayload: M.providerPayload,
        toolCalls: M.toolCalls,
        toolCallId: M.toolCallId,
        toolName: M.toolName,
        toolDisplay: M.toolDisplay
      };
    }
    function W$(A, M = {}) {
      return !Number.isFinite(Number(M.reuseUserMessageOrder)) && A.trim().startsWith("/");
    }
    function G$(A) {
      if (A == null) return "";
      if (typeof A == "string") return A.trim();
      if (typeof A == "number" || typeof A == "boolean" || typeof A == "bigint") return String(A);
      try {
        return JSON.stringify(A, null, 2);
      } catch {
        return String(A);
      }
    }
    async function V$(A, M = {}) {
      if (!W$(A, M)) return A;
      const K = await Ar("xb-tavern:run-slash-command", { payload: { command: A } }), fe = K.result || K, $e = fe.execution && typeof fe.execution == "object" ? fe.execution : {};
      if (fe.ok === !1 || $e.isError === !0 || $e.isAborted === !0) throw new Error(String(fe.error || $e.errorMessage || $e.abortReason || "slash_command_failed"));
      const Ue = G$(fe.pipe);
      return Ue || (P.value = "", Ut(() => by(Rn.value)), Ic("命令已执行，没有输出。", {
        tone: "info",
        durationMs: 2200
      }), "");
    }
    function J$(A = {}) {
      const M = {}, K = String(A.user?.name || "").trim(), fe = String(A.character?.name || "").trim();
      return K && (M.name1Override = K), fe && (M.name2Override = fe), M;
    }
    async function Y$(A, M) {
      const K = (de.value.find((fe) => fe.id === A.sessionId) || (Se.value === A.sessionId ? me.value : null))?.contextSnapshot || t.value || {};
      return (await wh([{
        id: `edit:${A.sessionId}:${A.order}`,
        text: M,
        options: J$(K)
      }])).items[0]?.text ?? M;
    }
    async function X$(A, M) {
      const K = ny(A);
      return K ? (await xc([{
        id: `edit:${A.sessionId}:${A.order}`,
        text: M,
        placement: K,
        options: {
          isEdit: !0,
          characterOverride: ay(A)
        }
      }])).items[0]?.text ?? M : M;
    }
    const Qa = lne({
      state: k,
      activeAssistantPreset: Gl,
      activeSession: me,
      agentConfig: r,
      chatAutoScroll: ns,
      chatComposeTextareaRef: Rn,
      chatMessageWindowLimit: Ft,
      diagnostics: n,
      hiddenOutsideCount: o,
      historyMode: C,
      selectedSessionCharacterError: _,
      selectedSessionId: Se,
      applyRegex: xc,
      applySubstituteParams: wh,
      buildNativeChatPrompt: _A,
      clearRuntimeDisplayRegexRequests: uA,
      createSessionFromContext: MA,
      describeError: ba,
      enhanceChatMarkdown: Oo,
      getNativeWorldInfoRuntime: ly,
      loadSelectedSessionMessageWindow: Th,
      normalizeHiddenOutsideCount: mo,
      persistSelectedSessionId: Xn.persistSelectedSessionId,
      pruneLoadedSessionMessagesFromOrder: HN,
      refreshManagerRecords: wa,
      refreshRuntimeChatPresetFromHost: U,
      refreshSessions: Eh,
      resetChatMessageWindowState: Ih,
      resetTextareaHeight: by,
      resolveRuntimeContextForSession: Sh,
      resolveSlashCommandMessageText: V$,
      scrollChatToBottom: ja,
      setSelectedSessionId: Xn.setSelectedSessionId,
      setSuppressNextChatWindowLimitReload: Xn.suppressNextChatWindowLimitReload,
      showToast: Ic,
      thoughtBlocks: Bi,
      touchSessionLocally: zN,
      updateChatScrollButtons: Pc,
      upsertLoadedSessionMessage: qN,
      cancelDrawJobsForMessageRange: ya.cancelJobsForMessageRange
    });
    bt = () => {
      const A = Qa.flushDeferredAssistantCommit();
      return A && Ut(() => {
        Oo(), Pc();
      }), A;
    };
    function j$() {
      Qa.handleChatSubmit();
    }
    async function KA(A = {}) {
      await Qa.runOnce(A);
    }
    function Q$() {
      Qa.clearRuntimeAssistantLiveState();
    }
    function Mh(A) {
      const M = String(A || "").trim();
      return M ? ((!rn.value || rn.value.sessionId !== M) && (rn.value = {
        sessionId: M,
        messages: [],
        draft: Ay(M, {
          role: "assistant",
          content: "正在思考..."
        }),
        nextOrder: 1000000001
      }), rn.value) : null;
    }
    function WA(A = "") {
      const M = String(A || "").trim();
      (!M || rn.value?.sessionId === M) && (rn.value = null);
    }
    function Z$(A, M) {
      const K = Mh(A);
      K && (K.draft = {
        ...K.draft || Ay(A, {
          role: "assistant",
          content: M.content
        }),
        ...M,
        role: "assistant",
        content: M.content,
        updatedAt: Date.now()
      });
    }
    function e7(A) {
      return Array.isArray(A.toolCalls) && A.toolCalls.length ? A.toolCalls.map((M) => ({
        id: typeof M?.id == "string" ? M.id : "",
        name: typeof M?.name == "string" ? M.name : "",
        arguments: typeof M?.arguments == "string" ? M.arguments : "{}"
      })) : Array.isArray(A.tool_calls) ? A.tool_calls.map((M) => ({
        id: typeof M?.id == "string" ? M.id : "",
        name: typeof M?.function?.name == "string" ? M.function.name : "",
        arguments: typeof M?.function?.arguments == "string" ? M.function.arguments : "{}"
      })) : [];
    }
    function t7(A, M) {
      const K = Mh(A);
      K && (K.messages = [...K.messages, Ay(A, {
        role: M.role,
        content: String(M.content || ""),
        name: M.name,
        thoughts: Array.isArray(M.thoughts) ? Bi(M.thoughts) : void 0,
        providerPayload: M.providerPayload,
        toolCalls: M.role === "assistant" ? e7(M) : void 0,
        toolCallId: M.role === "tool" ? String(M.toolCallId || M.tool_call_id || "") : void 0,
        toolName: M.role === "tool" ? String(M.toolName || "") : void 0,
        toolDisplay: M.role === "tool" ? M.toolDisplay : void 0
      })]);
    }
    function n7(A, M) {
      const K = String(A || "").trim();
      if (K) {
        if (M.type === "clear_stream_draft") {
          const fe = Mh(K);
          fe && (fe.draft = null);
          return;
        }
        t7(K, M.message);
      }
    }
    async function r7(A, M, K, fe) {
      const $e = (Array.isArray(M) ? M : []).filter((Mt) => Mt && ["assistant", "tool"].includes(Mt.role)), Ue = $e.length ? $e : [{
        role: "assistant",
        content: K
      }], Xe = [...Ue].map((Mt, Pt) => ({
        message: Mt,
        index: Pt
      })).reverse().find(({ message: Mt }) => {
        const Pt = Array.isArray(Mt.toolCalls) && Mt.toolCalls.length || Array.isArray(Mt.tool_calls) && Mt.tool_calls.length;
        return Mt.role === "assistant" && !Pt;
      })?.index ?? -1;
      Xe < 0 ? Ue.push({
        role: "assistant",
        content: K
      }) : String(Ue[Xe]?.content || "").trim() || (Ue[Xe] = {
        ...Ue[Xe],
        content: K
      });
      const Vt = [];
      for (let Mt = 0; Mt < Ue.length; Mt += 1) {
        const Pt = Ue[Mt], fn = Array.isArray(Pt.toolCalls) && Pt.toolCalls.length || Array.isArray(Pt.tool_calls) && Pt.tool_calls.length, qi = Pt.role === "assistant" && !fn && Mt === Ue.length - 1, ss = await Gh(A, {
          role: Pt.role,
          content: qi ? String(Pt.content || "").trim() || K : String(Pt.content || ""),
          name: Pt.name,
          thoughts: Pt.thoughts,
          providerPayload: Pt.providerPayload,
          toolCalls: Pt.toolCalls,
          tool_calls: Pt.tool_calls,
          toolCallId: Pt.toolCallId || Pt.tool_call_id,
          toolName: Pt.toolName,
          toolDisplay: Pt.toolDisplay,
          provider: Pt.role === "assistant" ? fe.provider : void 0,
          model: Pt.role === "assistant" ? fe.model : void 0,
          finishReason: Pt.role === "assistant" && !fn ? fe.finishReason : void 0,
          error: Pt.role === "assistant" && !fn ? fe.error : !1
        });
        Vt.push(ss);
      }
      return Vt;
    }
    async function GA(A, M, K = {}) {
      const fe = String(M || "").trim();
      if (!A || !fe) return;
      const $e = Number(mn.value.turn || 0), Ue = new AbortController();
      Sr.value = Ue, ut.value = !0, wt.value = !1, je.value = "运行中", ha.value = !0, hy();
      const Xe = ane(), Vt = (await Kp(A, Number.POSITIVE_INFINITY))?.order ?? -1;
      try {
        const Mt = await cte({
          sessionId: A,
          agentConfig: r.value,
          assistantPreset: Gl.value,
          question: fe,
          history: K.historyBeforeTurn,
          signal: Ue.signal,
          onCompactionStart: (qt) => {
            kh({
              id: `manager-compaction-${Date.now()}`,
              active: !0,
              resolved: !1,
              currentTokens: qt.currentTokens,
              yieldTokens: qt.yieldTokens || 0,
              triggerTokens: qt.triggerTokens,
              status: qt.status
            });
          },
          onCompactionProgress: (qt) => {
            kh({
              currentTokens: qt.currentTokens,
              yieldTokens: qt.yieldTokens || qt.currentTokens,
              triggerTokens: qt.triggerTokens,
              status: qt.status
            });
          },
          onCompactionComplete: (qt) => {
            kh({
              resolved: !0,
              currentTokens: qt.currentTokens,
              yieldTokens: qt.yieldTokens || qt.currentTokens,
              triggerTokens: qt.triggerTokens,
              status: qt.status
            }), zA(), wa(A);
          },
          onCompactionUnable: (qt) => {
            kh({
              resolved: !0,
              currentTokens: qt.currentTokens,
              yieldTokens: qt.yieldTokens || qt.currentTokens,
              triggerTokens: qt.triggerTokens,
              status: qt.status
            }), zA();
          }
        }), Pt = Array.isArray(K.historyBeforeTurn) ? Mt.history : await Ls(A), fn = await Gh(A, {
          role: "user",
          content: fe
        });
        Se.value === A && (Qe.value = [...Qe.value, fn].sort((qt, Hi) => qt.order - Hi.order)), Mh(A);
        const qi = [...new Set((K.replaceOrdersAfterAppend || []).map((qt) => Number(qt)).filter((qt) => Number.isInteger(qt) && qt >= 0))];
        qi.length && (await $b(A, qi), Se.value === A && (Qe.value = await Ls(A)));
        const ss = await dte({
          sessionId: A,
          agentConfig: r.value,
          assistantPreset: Gl.value,
          question: fe,
          history: Pt,
          turn: $e,
          signal: Ue.signal,
          onProtocolEvent: (qt) => {
            Xe.reset(), n7(A, qt);
          },
          onStreamProgress: (qt) => {
            const Hi = Xe.update(qt);
            Z$(A, {
              content: Hi.content,
              thoughts: Array.isArray(qt.thoughts) ? Bi(qt.thoughts) : void 0,
              toolCalls: Hi.toolCalls
            });
          }
        }), Ph = String(ss.text || "").trim() || "没有返回内容。";
        await r7(A, ss.protocolMessages, Ph, {
          provider: ss.provider,
          model: ss.model,
          finishReason: ss.ok ? "stop" : "error",
          error: !ss.ok
        }), WA(A), Se.value === A && (Qe.value = await Ls(A)), ((ss.changedFiles || []).length || (ss.changedTasks || []).length) && await my(A, Vt), await wa(A), je.value = "";
      } catch (Mt) {
        WA(A), Ue.signal.aborted ? (await Gh(A, {
          role: "assistant",
          content: "已停止。",
          finishReason: "aborted"
        }), Se.value === A && (Qe.value = await Ls(A)), je.value = "") : (await Gh(A, {
          role: "assistant",
          content: Mt instanceof Error ? Mt.message : String(Mt || "assistant_failed"),
          error: !0,
          finishReason: "error"
        }), Se.value === A && (Qe.value = await Ls(A)), je.value = "失败");
      } finally {
        Xe.reset(), Sr.value === Ue && (Sr.value = null), wt.value = !1, ut.value = !1;
      }
    }
    async function VA() {
      if (ut.value) {
        wt.value || (wt.value = !0, je.value = "正在停止..."), Sr.value?.abort();
        return;
      }
      const A = Be.value.trim();
      if (!A || !Se.value) return;
      const M = Se.value;
      if (Be.value = "", Ut(() => by(da.value)), ut.value = !0, wt.value = !1, je.value = "准备中", wt.value) {
        ut.value = !1, wt.value = !1, Be.value = A, je.value = "";
        return;
      }
      ut.value = !1, await GA(M, A);
    }
    vt([
      () => ge.value.length,
      () => YS.value.startIndex,
      () => tN.value,
      () => ve.value,
      () => `${z.value?.sessionId || ""}:${z.value?.order ?? ""}:${String(z.value?.content || "").length}`,
      () => V.value,
      () => rN.value,
      () => sN.value,
      () => a.value,
      () => sn.value,
      () => Ot.value,
      () => Wt.value
    ], () => {
      Ot.value === "chat" && Wt.value === "chat" && (ja(), Ut(() => {
        G.value ? Sc() : Oo(), Pc();
      }));
    }), vt([
      () => iN.value.length,
      () => ey.value.startIndex,
      () => lN.value,
      () => uN.value,
      () => cN.value,
      () => ut.value,
      () => sn.value,
      () => Ot.value,
      () => Wt.value
    ], () => {
      Ot.value === "chat" && Wt.value === "manager" && (Sy(), Ut(() => {
        Hl(), qA();
      }));
    }), vt([() => Ot.value, () => Wt.value], () => {
      Ot.value === "chat" && Wt.value === "chat" && ja(!0);
    }), vt(() => Ft.value, () => {
      Xn.handleChatMessageWindowLimitChanged();
    }), vt(() => Se.value, () => {
      Ih(), hy(), ns.value = !0, ha.value = !0, Ye.resetPositionState(), Pe.resetPositionState(), fr.value = "", ct.value = {}, Ut(() => {
        ja(!0), Sy(!0);
      });
    }), vt([
      () => String(Se.value || "").trim(),
      () => String(Oi.value?.path || "").trim(),
      () => Number(Oi.value?.updatedAt) || 0,
      () => Number(Oi.value?.contentLength) || 0
    ], async ([A, M]) => {
      if (!M) {
        if (Fi.value) {
          ot.value = "当前档案已变化，草稿仍保留";
          return;
        }
        gy(), NA(null);
        return;
      }
      if (Je.value === M && (lt.value === "edit" || Fi.value)) {
        ye.value?.sessionId !== Se.value && (ye.value = null), Fi.value && (ot.value = "档案已刷新，当前草稿仍保留");
        return;
      }
      const K = await u$(M);
      String(Se.value || "").trim() === A && String(q.value || "") === M && NA(K);
    }, { immediate: !0 }), vt(sn, (A) => {
      try {
        globalThis.localStorage?.setItem(kx, A ? "dark" : "light");
      } catch {
      }
    });
    const s7 = {
      activeView: Ot,
      alertTavernDialog: ah,
      chatFocus: Wt,
      confirmTavernDialog: Rs,
      homeThemeDark: sn,
      openPromptInspector: r$,
      postToHost: Ga,
      promptTavernDialog: _c,
      rememberBrokenAvatar: jS,
      shortText: py
    }, a7 = {
      chatMessages: we,
      chatMessageWindow: YS,
      createNewChatSession: XN,
      currentAssistantFloor: Oe,
      currentChatCharacterSessions: Gv,
      removeSession: t$,
      selectedCharacterSessions: Wv,
      selectedSessionId: Se,
      selectSession: e$,
      sessionDisplayTitle: Vv,
      sessionFloorLabel: eA,
      sessions: de,
      visibleChatMessages: ge
    };
    lk(n8, {
      shell: s7,
      character: {
        avatarAvailable: C8,
        batchSize: Pp,
        characterWorldbookBusy: I,
        characterWorldbookState: x,
        characters: Di,
        enterSelected: UN,
        filteredCount: ne(() => Ec.value.length),
        hiddenCount: I8,
        liveCharacterKey: E8,
        loadMore: BN,
        movePreview: DN,
        openCharacterWorldbook: NN,
        pendingCharacterSessionKey: S,
        pendingError: w,
        pendingPreviewCharacterKey: y,
        refresh: dy,
        rememberBrokenAvatar: jS,
        searchText: Dn,
        select: RN,
        selectFirstVisible: ON,
        selectGreeting: LN,
        selectLastVisible: FN,
        selectedCharacter: Cc,
        selectedGreetingIndex: v,
        shortText: py,
        syncWorldbookState: fy,
        visibleCharacters: _h
      },
      session: a7,
      draw: ya,
      chat: {
        actionFeedback: y$,
        cancelEditMessage: Xa,
        canEditMessage: vy,
        canRerunMessage: $A,
        canSendMessage: dN,
        currentAuthorNote: Ms,
        chatAutoScroll: ns,
        chatFocus: Wt,
        chatLayout: Gt,
        chatComposeTextareaRef: Rn,
        chatScrollControlsActive: Y,
        chatScrollRef: Xt,
        chatSubtitle: lA,
        copyMessage: b$,
        currentUserMessage: P,
        deleteMessageTurn: k$,
        displayMessageContent: vA,
        displayMessageRenderProjection: mN,
        displayMessageThoughtBlocks: gN,
        displayRuntimeContent: vN,
        displayRuntimeRenderProjection: yA,
        displayRuntimeThoughtBlocks: yN,
        displayCharacterName: P8,
        displayUserName: Kv,
        formatMessageTime: v$,
        handleChatScroll: L$,
        handleChatSubmit: j$,
        handleChatTouchMove: q$,
        handleChatTouchStart: B$,
        handleChatWheel: O$,
        handleComposeInput: C$,
        handleComposeKeydown: z$,
        isEditingMessage: DA,
        isCancellingRun: H,
        isRunning: G,
        markdownSignature: Yn,
        htmlRenderEnabled: a,
        messageKey: Ya,
        normalizeTavernSessionState: Xr,
        renderChatMarkdown: ch,
        rerunFromMessage: M$,
        revealOlderChatMessages: GN,
        roleLabel: xh,
        runtimeText: ve,
        runtimeThoughts: ke,
        runtimeActionCheckEvents: ee,
        runtimeFinalizedAssistantMessage: z,
        runtimeUserMessageVisible: be,
        runtimePendingUserMessage: V,
        saveEditMessage: I$,
        scrollChatToBottom: ja,
        scrollChatToTop: N$,
        saveCurrentAuthorNote: CN,
        showChatScrollBottom: D,
        showChatScrollTop: St,
        startEditMessage: S$,
        thoughtBlocks: Bi,
        thoughtSummaryLabel: g$,
        updateChatScrollButtons: Pc,
        visibleCharacterAvatar: A8,
        visibleUserAvatar: JS
      },
      manager: {
        activeMemoryFiles: Xv,
        archivedManagerRuns: N8,
        canEditManagerMessage: yy,
        canRerunManagerMessage: LA,
        canSendManagerMessage: fN,
        copyManagerMessage: w$,
        currentManagerWorkRun: $8,
        deleteManagerMessageTurn: R$,
        editingMessageDraft: an,
        enhanceManagerMarkdown: Hl,
        formatRunActivityLine: L8,
        formatRunIssueLine: D8,
        formatRunInputLine: O8,
        formatRunMapLine: F8,
        formatRunMemoryLine: B8,
        formatRunModelLine: U8,
        formatRunTaskLine: q8,
        handleEditInput: E$,
        handleManagerComposeKeydown: K$,
        handleManagerEditKeydown: T$,
        handleManagerScroll: D$,
        handleManagerSubmit: VA,
        handleManagerTouchMove: H$,
        handleManagerTouchStart: U$,
        handleManagerWheel: F$,
        hiddenManagerRunCount: H8,
        isEditingManagerMessage: OA,
        isEditingManagerMessageDirty: FA,
        isManagerRunRetrying: m$,
        isManagerAssistantCancelling: wt,
        isManagerAssistantRunning: ut,
        liveManagerChatDisplayItems: iA,
        managerActionFeedback: _$,
        managerAutoScroll: ha,
        managerBusy: Yv,
        managerCompactionOverlay: Ht,
        managerComposeTextareaRef: da,
        managerInputDraft: Be,
        managerInputStatus: je,
        managerMessageWindow: ey,
        managerRuns: pe,
        managerRunTone: K8,
        managerScrollControlsActive: qe,
        managerScrollRef: Rr,
        managerWorkRef: fa,
        managerStatusLabel: G8,
        managerToolStatusLabel: V8,
        managerToolTone: J8,
        managerToolTraceItems: Y8,
        managerToolTurnPreview: sne,
        managerToolTurnSummary: rne,
        memoryFileDisplayName: jv,
        memoryFiles: Ie,
        memoryIndexStatusLine: eN,
        retryManagerRun: p$,
        revealOlderManagerMessages: VN,
        rerunFromManagerMessage: wy,
        saveEditManagerMessage: UA,
        scrollManagerToBottom: Sy,
        scrollManagerToTop: $$,
        selectedMemoryFile: nA,
        showManagerScrollBottom: We,
        showManagerScrollTop: j,
        startEditManagerMessage: A$,
        toolTraceSummary: X8,
        updateManagerScrollButtons: qA,
        visibleManagerChatItems: ty
      },
      memory: {
        activeMemoryFiles: Xv,
        commitAcceptedState: o$,
        commitUserAcceptedState: my,
        discardMemoryDraft: RA,
        enterMemoryEditMode: i$,
        expandMemoryFileGroup: Q8,
        formatMemoryFileMeta: l$,
        markdownSignature: Yn,
        MEMORY_FILE_BATCH_SIZE: H_,
        MEMORY_TURN_BATCH_SIZE: Ix,
        memoryDirectoryGroups: Z8,
        memoryEditorDirty: Fi,
        memoryEditorDocumentAvailable: oA,
        memoryEditorDraft: De,
        memoryEditorLoadedPath: Je,
        memoryEditorMode: lt,
        memoryEditorReadOnly: Qv,
        memoryEditorStatus: ot,
        memoryFileDisplayName: jv,
        memoryFileKindLabel: rA,
        memoryFiles: Ie,
        memoryFileSearchText: fr,
        memoryFileStatusLabel: c$,
        previewMemoryDraft: d$,
        renderChatMarkdown: ch,
        saveSelectedMemoryFile: f$,
        selectedMemoryFileEntry: Oi,
        selectedMemoryFile: nA,
        selectedMemoryFilePath: q,
        selectMemoryFile: h$,
        stateMemoryFile: oe
      },
      workspace: {
        activeMemoryFiles: Xv,
        activeMapDocId: It,
        atlasActiveLocationKey: se,
        atlasStateDocument: at,
        atlasStatePatches: X,
        chatWorkspacePanel: pt,
        displayUserName: Kv,
        mapStateDocuments: ht,
        mapStateDocument: Lt,
        mapStatePatches: en,
        saveSessionContract: KN,
        sessionContract: Fn,
        stateMemoryFile: oe,
        tavernTasks: Me,
        visibleUserAvatar: JS
      },
      settings: dt
    });
    async function o7() {
      Va(92, "refreshPresets");
      const A = (await Promise.allSettled([zv(), Eh()])).find((M) => M.status === "rejected");
      A?.status === "rejected" && (b.value = ba(A.reason)), _t(), Ot.value === "settings" && wn.value === "api" && await Ut(re), Ot.value === "settings" && wn.value === "chatPreset" && zt(), Ot.value === "settings" && wn.value === "worldbooks" && Yt(), Ot.value === "settings" && wn.value === "regex" && vh(), Ot.value === "settings" && wn.value === "base" && Hv(), Ot.value === "settings" && wn.value === "characters" && dy(), Se.value && EA({
        sessionId: Se.value,
        force: !0
      }), Ot.value === "chat" && Wt.value === "chat" && ja(!0), Va(100, "enterTavern");
    }
    return w1(async () => {
      Wa.mount(), document.addEventListener("focusin", Mi, !0), document.addEventListener("focusout", Mi, !0), Kl = window.setInterval(() => {
        WN();
      }, 2e3), Ot.value === "settings" && wn.value === "api" && Ut(re), _t(), await Ut(), Wa.postToHost("xb-tavern:frame-ready"), ya.refreshTavernDrawStatus();
    }), Fg(() => {
      document.removeEventListener("focusin", Mi, !0), document.removeEventListener("focusout", Mi, !0), Wa.dispose(/* @__PURE__ */ new Error("tavern_unmounted")), uh(), Q$(), ya.clearCooldownTimer(), Y2(null), Qa.abortActiveRun(), Sr.value?.abort(), ya.abortAllJobs(), Ye.cleanup(), Pe.cleanup(), Vs && (window.clearTimeout(Vs), Vs = null), Kl && (window.clearInterval(Kl), Kl = null), cA(), HA(), kc();
    }), (A, M) => (O(), B("main", {
      class: Fe(["xb-tavern xb-os-shell", {
        "is-home-view": Ot.value === "home" || Ot.value === "about",
        "theme-dark": sn.value,
        "theme-light": !sn.value
      }]),
      "data-chat-font-size": s.value.chatFontSize,
      style: Nn(Ka.value)
    }, [
      g("section", ave, [
        Ot.value === "home" ? (O(), cn(Une, {
          key: 0,
          dark: sn.value,
          "has-session": Ps.value,
          subtitle: lA.value,
          "character-count": Di.value.length,
          onToggleTheme: M[0] || (M[0] = (K) => sn.value = !sn.value),
          onExit: M[1] || (M[1] = (K) => T(Ga)("xb-tavern:close")),
          onEnter: jN,
          onOpenCharacters: IA,
          onOpenSettings: T($i),
          onOpenAbout: M[2] || (M[2] = (K) => Ot.value = "about")
        }, null, 8, [
          "dark",
          "has-session",
          "subtitle",
          "character-count",
          "onOpenSettings"
        ])) : Te("", !0),
        Ot.value === "about" ? (O(), cn(kne, {
          key: 1,
          dark: sn.value,
          onToggleTheme: M[3] || (M[3] = (K) => sn.value = !sn.value),
          onBack: M[4] || (M[4] = (K) => Ot.value = "home")
        }, null, 8, ["dark"])) : Te("", !0),
        Ot.value === "chat" ? (O(), cn(Xme, { key: 2 })) : Te("", !0),
        Ot.value === "settings" ? (O(), cn(Mge, { key: 3 })) : Te("", !0)
      ]),
      Ne.value ? (O(), B("div", {
        key: Ne.value.id,
        class: Fe(["tavern-toast", `tone-${Ne.value.tone}`]),
        role: "status",
        "aria-live": "polite"
      }, te(Ne.value.message), 3)) : Te("", !0),
      Bn.value ? (O(), cn(mge, {
        key: 1,
        tab: dn.value,
        "onUpdate:tab": M[5] || (M[5] = (K) => dn.value = K),
        "simulate-input": Jn.value,
        "onUpdate:simulateInput": M[6] || (M[6] = (K) => Jn.value = K),
        "last-request-raw-json": R8.value,
        "last-request-snapshot": Jv.value,
        "simulate-status": Un.value,
        "simulate-error": wr.value,
        "simulate-json": sr.value,
        onClose: s$,
        onSimulate: a$
      }, null, 8, [
        "tab",
        "simulate-input",
        "last-request-raw-json",
        "last-request-snapshot",
        "simulate-status",
        "simulate-error",
        "simulate-json"
      ])) : Te("", !0),
      N.value ? (O(), B("div", {
        key: 2,
        class: "character-worldbook-picker-overlay",
        onClick: Kt(xA, ["self"])
      }, [g("section", ove, [
        g("header", null, [M[11] || (M[11] = g("strong", null, "选择角色世界书", -1)), g("button", {
          type: "button",
          class: "worldbook-picker-close",
          "aria-label": "关闭",
          onClick: xA
        })]),
        E.value ? (O(), B("div", ive, te(E.value), 1)) : Te("", !0),
        g("div", lve, [(O(!0), B(He, null, tt($.value, (K) => (O(), B("button", {
          key: K,
          type: "button",
          disabled: I.value,
          onClick: (fe) => $N(K)
        }, te(K), 9, uve))), 128))])
      ])])) : Te("", !0),
      (O(), cn(fk, { to: "body" }, [yn.value ? (O(), B("div", {
        key: 0,
        class: Fe(["tavern-dialog-overlay", sn.value ? "theme-dark" : "theme-light"]),
        role: "dialog",
        "aria-modal": "true",
        "aria-labelledby": "tavern-dialog-title",
        onClick: Kt(Dv, ["self"]),
        onKeydown: [M[10] || (M[10] = Qr((K) => ki(), ["esc"])), Qr(ih, ["tab"])]
      }, [g("form", {
        ref_key: "tavernDialogPanelRef",
        ref: Lo,
        class: Fe(["tavern-dialog", [`tone-${yn.value.tone}`, { "is-attention": Ci.value }]]),
        tabindex: "-1",
        onSubmit: Kt(Nv, ["prevent"])
      }, [
        g("header", cve, [g("div", null, [g("strong", dve, te(yn.value.title), 1)]), g("button", {
          type: "button",
          class: "tavern-dialog-close",
          "aria-label": "关闭",
          onClick: M[7] || (M[7] = (K) => ki())
        })]),
        g("div", fve, [yn.value.message ? (O(), B("p", hve, te(yn.value.message), 1)) : Te("", !0), yn.value.kind === "prompt" ? (O(), B("label", pve, [er(g("input", {
          ref_key: "tavernDialogInputRef",
          ref: rs,
          "onUpdate:modelValue": M[8] || (M[8] = (K) => yn.value.inputValue = K),
          type: "text",
          placeholder: yn.value.placeholder
        }, null, 8, mve), [[sa, yn.value.inputValue]])])) : Te("", !0)]),
        g("footer", gve, [yn.value.kind !== "alert" ? (O(), B("button", {
          key: 0,
          ref_key: "tavernDialogCancelRef",
          ref: nh,
          type: "button",
          class: "tavern-dialog-button secondary",
          onClick: M[9] || (M[9] = (K) => ki())
        }, te(yn.value.cancelText), 513)) : Te("", !0), g("button", {
          ref_key: "tavernDialogPrimaryRef",
          ref: yc,
          type: "submit",
          class: "tavern-dialog-button primary"
        }, te(oh.value), 513)])
      ], 34)], 34)) : Te("", !0)]))
    ], 14, sve));
  }
}), Sve = wve;
ML(Sve).mount("#app");
